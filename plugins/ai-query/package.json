{
  "name": "@pryzm/plugin-ai-query",
  "version": "0.1.0",
  "description": "PRYZM 2 — AI semantic-query plugin shell (S51 D3). Registers a plugin descriptor + sidebar entry for the read-only inspector workflow living in `@pryzm/ai-host`. Lazy: zero bytes added to the editor's first-paint chunk per [strategic ADR-014].",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./descriptor": "./src/descriptor.ts"
  },
  "files": ["src"],
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@pryzm/ai-host": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  }
}
