{
  "name": "@pryzm/plugin-toy-cube",
  "version": "0.1.0",
  "description": "PRYZM 2 \u2014 S02 toy plugin that registers `MoveCubeCommand` against the CommandBus.  Used as the smoke-test of the full L1\u2194L2 pipeline.  S05 adds the L5 `CubeCommitter` (THREE-side) and S06 adds `bootHelloCube` for the `?pryzm2=1` URL flag entry.",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./committer": "./src/committer.ts",
    "./hello-cube": "./src/HelloCubeBoot.ts",
    "./tool": "./src/tool.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@pryzm/editor": "workspace:*",
    "immer": "^10.1.1",
    "three": "^0.183.0",
    "@pryzm/plugin-sdk": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  }
}
