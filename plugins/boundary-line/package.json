{
  "name": "@pryzm/plugin-boundary-line",
  "version": "0.1.0",
  "description": "PRYZM — construction / setting-out BOUNDARY LINE plugin (L-7900 / C105 / ADR-0348). The AUTHORED line an architect draws to lay a scheme out at early-stage design; a HOST whose attached walls, slabs and columns move with it. ⛔ NOT the cadastral Parcel.boundary (C19 §1.4 — legal and immutable) and NOT RoomBoundingLine. Geometry math lives in @pryzm/geometry-boundary-line (pure).",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./store": "./src/store.ts",
    "./errors": "./src/errors.ts",
    "./handlers": "./src/handlers/index.ts"
  },
  "files": ["src"],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ci": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@pryzm/geometry-boundary-line": "workspace:*",
    "@pryzm/plugin-sdk": "workspace:*",
    "immer": "^10.1.1"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^3.0.0"
  }
}
