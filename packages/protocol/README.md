# @pryzm/protocol

The public DTO surface of PRYZM 2. Re-exports from `@pryzm/schemas`.

Stores, sync, AI, and plugins must import from here, not from `@pryzm/schemas`
directly — this is the barrel the architecture promises stability on.
