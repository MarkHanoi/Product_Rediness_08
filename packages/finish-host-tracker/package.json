{
  "name": "@pryzm/finish-host-tracker",
  "version": "0.1.0",
  "description": "PRYZM — floor-finish & ceiling host dependency trackers: the first consumers of FloorHostReferenceEdge / CeilingHostReferenceEdge. A moved wall re-projects hosted finishes the way it re-projects slabs (GR-12, C79 §5, §FINISH-FOLLOWS-WALL).",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@pryzm/core-app-model": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.1.5"
  }
}
