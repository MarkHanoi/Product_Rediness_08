# @pryzm/schemas

L1 contract for PRYZM 2 — Zod schemas, typed-ID brands, and the `createId`
factory. Every primitive that ever lives in a store, on the wire, or on disk
must be defined here first.

See `docs/04-reference/architecture-detail/schemas.md` for the full surface and design notes.
