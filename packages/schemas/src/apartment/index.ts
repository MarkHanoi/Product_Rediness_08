// D-α-0 (BIM 2/3) — public surface for the L0 apartment-parameter schemas.
// Exposed via a subpath import (`@pryzm/schemas/apartment`) to keep the
// root barrel uncluttered — same convention the annotation/view supplements
// use (see comment in ../index.ts).

export * from './ApartmentParameters.js';
