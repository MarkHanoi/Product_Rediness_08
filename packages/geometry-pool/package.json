{
  "name": "@pryzm/geometry-pool",
  "version": "0.1.0",
  "description": "PRYZM — swimming-pool geometry subsystem. Dimension resolution (record → systemType → documented default) and the pure pool ASSEMBLY (hole + walls + floor slab + water). §FEAT-SWIMMING-POOL-ELEMENT / ADR-0124.",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": ["src"],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ci": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@opentelemetry/api": "^1.9.0",
    "@pryzm/schemas": "workspace:*",
    "zod": "*"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
