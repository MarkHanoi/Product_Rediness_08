{
  "name": "@pryzm/view-state",
  "version": "0.1.0",
  "description": "PRYZM 2 — view state layer.  ViewDefinition / ViewRegistry / ActiveView / ViewController.  Spec: PHASE-1C §S17, ADR-0016.",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": ["src", "dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@pryzm/frame-scheduler": "workspace:*",
    "@pryzm/renderer": "workspace:*",
    "@pryzm/stores": "workspace:*",
    "@opentelemetry/api": "^1.9.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/three": "^0.183.1",
    "three": "^0.183.0",
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  }
}
