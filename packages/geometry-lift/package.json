{
  "name": "@pryzm/geometry-lift",
  "version": "0.1.0",
  "description": "PRYZM — vertical-circulation (lift / elevator) geometry subsystem: types, stores, placeholder mesh builder. Peer of @pryzm/geometry-stair (residential-building multi-family typology, plan §4).",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": ["src"],
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ci": "vitest run"
  },
  "dependencies": {
    "@pryzm/core-app-model": "workspace:*",
    "@pryzm/event-bus": "workspace:*",
    "@pryzm/renderer-three": "workspace:*"
  }
}
