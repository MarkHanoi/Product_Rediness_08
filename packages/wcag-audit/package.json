{
  "name": "@pryzm/wcag-audit",
  "version": "0.1.0",
  "description": "PRYZM 2 — WCAG 2.2 AA audit runner (S70 D6, ADR-0052 §B.2). Pure axe-core wrapper + critical-path declarations. The live-page wrapper lives in tests/browser-matrix; this package is the pure-HTML auditor consumed by both unit tests and the smoke gate.",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": ["src"],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  }
}
