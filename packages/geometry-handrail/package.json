{
  "name": "@pryzm/geometry-handrail",
  "version": "0.1.0",
  "description": "PRYZM — handrail geometry subsystem: builder, tool, run generators.",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": ["src"],
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ci": "vitest run"
  },
  "dependencies": {
    "@pryzm/core-app-model": "workspace:*",
    "@pryzm/renderer-three": "workspace:*",
    "@pryzm/command-registry": "workspace:*",
    "@pryzm/snapping": "workspace:*",
    "@thatopen/components": "^3.4.2"
  },
  "devDependencies": {
    "@pryzm/geometry-stair": "workspace:*"
  }
}
