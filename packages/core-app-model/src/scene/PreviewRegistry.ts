/**
 * @file src/engine/subsystems/core/scene/PreviewRegistry.ts
 *
 * Thin re-export shim — Wave A16-T3 (S122).
 * Canonical implementation lives in `packages/scene-committer/src/PreviewRegistry.ts`.
 * All consumers that import from this path continue to compile unchanged.
 */
export { PreviewRegistry, previewRegistry } from '@pryzm/scene-committer';
