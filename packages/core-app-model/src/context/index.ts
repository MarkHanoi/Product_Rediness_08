/**
 * @pryzm/core-app-model — context sub-barrel (Wave 10 Task 2 W10-A)
 */

export type {
    ProjectEventType,
    ProjectEventListener,
} from './ProjectContext.js';

export { EditorMode, ProjectContext, projectContext } from './ProjectContext.js';
