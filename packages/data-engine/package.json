{
  "name": "@pryzm/data-engine",
  "version": "0.1.0",
  "description": "PRYZM 3 — C28 Data Panel & Automation engine (DAT-α-3). Predicate registry + rule evaluator for the quality-rules subsystem. L3 pure: depends on @pryzm/schemas only.",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": ["src"],
  "scripts": {
    "test": "vitest run",
    "test:ci": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@pryzm/schemas": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  }
}
