{
  "name": "@pryzm/auto-dimension",
  "version": "0.1.0",
  "description": "PRYZM — deterministic AutoDimension engine (L2, PURE). Turns a floor-plan wall/opening snapshot into a non-redundant, architect-grade DimensionString[] via an 8-stage pipeline (connectivity graph → perimeter → opening segmentation → chain planning → placement → QA). No THREE/DOM/I/O/RNG. See docs/03-execution/spikes/SPIKE-AUTODIMENSION-ENGINE.md (§FEAT-AUTODIMENSION-P1, L-138).",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": ["src"],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ci": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@opentelemetry/api": "^1.9.0",
    "@pryzm/geometry-kernel": "workspace:*",
    "@pryzm/schemas": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  },
  "pryzm": {
    "layer": "L2",
    "forbiddenDependencies": ["three", "@types/three", "react", "@thatopen/components", "@thatopen/ui"],
    "note": "PURE deterministic dimension planner. No THREE/DOM/I/O/RNG (P2/P4/P5). Same input → byte-identical DimensionString[] (ADR-0061). P8: every exported fn opens ≥1 OTel span."
  }
}
