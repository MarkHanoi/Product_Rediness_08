{
  "name": "@pryzm/event-bus",
  "version": "0.1.0",
  "description": "PRYZM — typed event-bus package. IEventBus interface + DOMEventBus adapter (CustomEvent forwarding during transition) + NullEventBus (tests/headless) + withEventSpan() OTel wrapper + EventCatalog discriminated union of all 595 platform event names.",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": ["src", "dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@opentelemetry/api": "^1.9.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
