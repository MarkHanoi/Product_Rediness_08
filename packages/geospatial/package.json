{
  "name": "@pryzm/geospatial",
  "version": "0.1.0",
  "description": "PRYZM 3 — Geospatial coordinate transforms: LTP-ENU rebasing, proj4js geodetic transforms, IfcProjectedCRS round-trip. C12 contract implementation.",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": ["src"],
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@opentelemetry/api": "^1.9.0",
    "proj4": "^2.15.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  }
}
