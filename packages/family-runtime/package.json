{
  "name": "@pryzm/family-runtime",
  "version": "0.1.0",
  "description": "PRYZM 2 — Family-runtime expression DSL + resolver + unit coercion (S55, PHASE-3B-FAMILY-CREATOR-REWRITE-PLAN.md §7.5).  Pure-Node, dependency-free.  Used by the family editor (resolve at edit-time), the bake-worker (resolve at bake-time), and the AI worker (validate AI-proposed parameter values).",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./expression": "./src/expression/index.ts",
    "./resolution": "./src/resolution/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  }
}
