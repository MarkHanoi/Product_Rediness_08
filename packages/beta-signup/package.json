{
  "name": "@pryzm/beta-signup",
  "version": "0.1.0",
  "description": "PRYZM 2 — beta cohort sign-up surface (S48 D1). Validates payloads, persists to a Store<T>, and dispatches the confirmation email through @pryzm/email-transport. Server mount + Supabase `beta_signups` table provisioning bind to S48 D9 launch.",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": ["src"],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@pryzm/email-transport": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  }
}
