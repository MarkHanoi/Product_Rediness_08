{
  "name": "@pryzm/feature-flags",
  "version": "0.1.0",
  "description": "PRYZM 2 — feature flag + kill-switch registry (post-2B closeout / ADR-0030). Houses K2B-1..K2B-4 plus a small registry of named boolean toggles. Pure: no DOM, no THREE, no Node-only globals.",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": ["src"],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  }
}
