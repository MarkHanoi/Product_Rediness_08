{
  "name": "@pryzm/test-commands",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Command-bus CI gates — affectedStores contract, handler uniqueness, and pipeline invariants.",
  "scripts": {
    "test": "vitest run",
    "test:ci": "vitest run"
  },
  "devDependencies": {
    "vitest": "2.1.9"
  }
}
