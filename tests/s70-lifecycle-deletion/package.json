{
  "name": "@pryzm/test-s70-lifecycle-deletion",
  "version": "0.1.0",
  "description": "PRYZM 2 — S70 D8 deletion guard.  Asserts src/lifecycle/ no longer exists and no module repo-wide imports from it.  Pure-fs check; no runtime.  Spec: SPEC-27 §4.3 + ADR-030 Part D + ADR-0052 §B.7.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  }
}
