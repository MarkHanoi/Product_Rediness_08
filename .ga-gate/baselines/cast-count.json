{
  "count": 3,
  "ratchedAt": "2026-08-16T00:00:00.000Z",
  "comment": "4 → 3 by GENUINE REMOVAL (H4, 2026-08-16). Ratcheted down by hand rather than by the gate's auto-writer ONLY so the L-844 history below survives; the count is the gate's own measured reading, not a typed number. WHAT PAID IT: the strict arm read 5 against this ceiling of 4 and exited 3. Two of the five were `(window as any).addEventListener` swaps in apps/editor/src/engine/__tests__/schedulePanelGeometryStaleness.spec.ts, hand-rolling a spy; they are now `vi.spyOn(window,'addEventListener')` — typed, calls through, self-restoring, spec still 6/6 green. The 4th of the original four (PlanViewToolOverlay.ts, the load-bearing `(window as any)['commandManager']` described below) was deleted by afe0cdad the same day, so 4 − 1 production − 2 test = 3. THE REMAINING 3 are all in apps/editor/src/engine/views/plantools/__tests__/LinearDimThirdClickOffset.spec.ts (lines 98 and 116), which is lane F4's live territory — the exact patch is written up in H4's handoff and takes this to 0. PRIOR HISTORY, preserved verbatim: §FIX-CAST-STRICT-BASELINE (L-844). 0 → 4, RE-FROZEN AT THE MEASURED VALUE — a scope correction, not a relaxation. The 0 was frozen 2026-05-13 when the strict scope was the single cast-free window-shim.ts. The scope was widened to all of src/ + apps/editor/src/engine on 2026-08-09 WITHOUT re-freezing, which made the gate unsatisfiable (L-716 class: a ceiling that can never be met is a gate nobody reads) — and an early-return then HID this arm behind the repo-wide one for two days. With comments stripped (the arm was counting prose: 16 of its 20 matches were comments ASSERTING P4 compliance), the honest reading is 4: three in LinearDimThirdClickOffset.spec.ts, and ONE production cast at PlanViewToolOverlay.ts:786 that is LOAD-BEARING ACROSS TWO RATCHETS — its own comment says bracket notation exists to avoid the window.commandManager gate pattern, so rewriting it decrements this gate and increments check-no-commandmanager's window-alias ceiling: moving debt, not paying it. Shrink-only from here; auto-ratchets down as the four are genuinely removed. Do NOT raise."
}
