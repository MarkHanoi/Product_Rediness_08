<Window x:Class="PRYZM.Revit.Bridge.UI.TokenDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Set PRYZM Token"
        Height="180" Width="440"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Paste your PRYZM API token:" Margin="0,0,0,8"/>
        <PasswordBox Grid.Row="1" x:Name="TokenBox" Height="28"/>
        <TextBlock Grid.Row="2" Text="Generate a token at https://pryzm.app/account/tokens"
                   TextWrapping="Wrap" Margin="0,12,0,0" Foreground="#666"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="80" Margin="0,0,8,0" Click="OnCancel"/>
            <Button Content="Save" Width="80" Click="OnSave" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
