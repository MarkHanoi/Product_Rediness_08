Full Implementation Plan: Stream 1 & Stream 2 User Journey Alignment

---

Context: What each entry point currently does

There are **three separate surfaces** where upgrade/pricing is triggered, and they must all be aligned:

| **Surface** | **Trigger** | **Current destination** | **Works?** |
| --- | --- | --- | --- |
| **LandingPage** nav | "Pricing" link | `href="#"` — goes nowhere | ❌ |
| **LandingPage** nav | "Contact sales" button | Calls `onLogin()` → auth screen | ❌ wrong |
| **ProjectHub** header | "✦ Upgrade" button | `PricingPage` via `showPricing()` | ✓ |
| **In-app UpgradeModal** | Feature gate fires | "View plans & pricing" → `PricingPage` | ✓ |
| **PricingPage** | Enterprise card button | Opens `mailto:hello@pryzm.io` | ✓ |
| **PricingPage** | All other plan buttons | Browser `alert()` "Stripe coming soon" | ⚠️ placeholder |

**Stream 2 currently has zero entry point anywhere.** The UpgradeModal is fine as-is — it is feature-context-specific and should remain purely about Stream 1 gates. Stream 2 belongs on the Pricing page and the Landing page.

**One additional bug not yet discussed:** The "✦ Upgrade" button in `ProjectHub.ts` line 80 only renders when `plan === 'free'`. This means an Architect user ($59/mo) sees no upgrade path to Studio, and a Studio user sees no path to Firm. Those upgrade paths are silently missing from the hub.

---

Changes Required

---

Change 1 — Wire the "Pricing" nav link on the Landing page

**File: `src/ui/platform/LandingPage.ts`**

Step 1. Add `onPricing: () => void` to the `LandingPageCallbacks` interface (currently only has `onGetStarted` and `onLogin`).

Step 2. In the nav HTML inside `build()`, change:

```
<aclass="lp-nav-link"href="#">Pricing</a>
```

to:

```
<aclass="lp-nav-link"href="#"id="lp-nav-pricing">Pricing</a>
```

Step 3. In the event listener block at the bottom of `build()`, add:

```
el.querySelector('#lp-nav-pricing')!.addEventListener('click',(e) =>{
e.preventDefault();
this.callbacks.onPricing();
});
```

**File: `src/ui/platform/PlatformRouter.ts`**

Step 4. In `showLanding()`, the `LandingPage` constructor receives a callbacks object. Add `onPricing: () => this.showPricing()` to that object. The `showPricing()` method already exists — you are just connecting the button to it.

---

Change 2 — Fix the "Contact sales" button on the Landing page

**File: `src/ui/platform/LandingPage.ts`**

Step 1. Add `onContactSales: () => void` to the `LandingPageCallbacks` interface.

Step 2. In the event listener block, change the existing line:

```
el.querySelector('#lp-nav-contact')!.addEventListener('click',() =>this.callbacks.onLogin());
```

to:

```
el.querySelector('#lp-nav-contact')!.addEventListener('click',() =>this.callbacks.onContactSales());
```

**File: `src/ui/platform/PlatformRouter.ts`**

Step 3. In `showLanding()`, add:

```
onContactSales:() =>window.open('mailto:hello@pryzm.io?subject=PRYZM+Sales+Enquiry','_blank')
```

---

Change 3 — Add Stream 2 section to the Landing page

This is the most visible new content. It sits immediately below the closing `</section>` of the hero (line 83 in `LandingPage.ts`), still inside the `el.innerHTML` string.

**File: `src/ui/platform/LandingPage.ts`**

Step 1. After the hero `</section>`, add a new `<section id="lp-bespoke">` with two columns:

- **Left column:** Heading ("Building your own platform?"), 2-sentence description ("AI is making software cheap to build. We help enterprises deploy a bespoke BIM platform under their own brand — custom element libraries, your workflows, your infrastructure."), then two buttons: "Talk to us" (calls `onContactSales`) and "See enterprise options" (calls `onPricing`)
- **Right column:** A short bullet list: "Custom element & material libraries · Integration with Revit, ArchiCAD, ERP systems · White-label under your brand · On-premise or private cloud deployment · Dedicated build team & ongoing support"

Step 2. Wire the two new buttons in the event listener block at the bottom of `build()`:

```
el.querySelector('#lp-bespoke-contact')!.addEventListener('click',() =>this.callbacks.onContactSales());
el.querySelector('#lp-bespoke-pricing')!.addEventListener('click',() =>this.callbacks.onPricing());
```

**File: `src/styles/AppTheme.ts`**

Step 3. All `lp-` prefix styles live here. Add CSS for `.lp-bespoke`, `.lp-bespoke-inner`, `.lp-bespoke-col`, `.lp-bespoke-heading`, `.lp-bespoke-desc`, `.lp-bespoke-actions`, `.lp-bespoke-list`. The section should have a light grey/off-white background to visually separate it from the hero. Use the existing `--app-violet-1` token for the "Talk to us" button to match the hero CTA style.

---

Change 4 — Add Stream 2 section to the Pricing page

**File: `src/ui/platform/PricingPage.ts`**

Step 1. In the `render()` method, add a new `<div class="pr-bespoke-band">` section in the HTML string. Place it **between the plan cards grid (`#pr-plans`)** and the AI add-ons section (`.pr-addons-section`). Content:

- Heading: "Enterprise custom deployments"
- Sub-text: "For organisations that need PRYZM built into their own product — white-labelled, on-premise, and fully customised to your workflows."
- Bullet list: Discovery & scoping workshop · Custom IFC schema configuration · Bespoke element family libraries · Integration with your existing tools (ERP, Revit, project management) · White-label branding & private deployment · Handover, training, and ongoing engineering support
- A single CTA button `id="pr-bespoke-cta"` labelled "Talk to us about a bespoke build"

Step 2. Update `PLAN_FEATURES.enterprise` array. Add one line: `'Bespoke platform deployment option (scoped separately)'`

Step 3. In `attachListeners()`, add:

```
el.querySelector('#pr-bespoke-cta')!.addEventListener('click',() =>{
window.open('mailto:hello@pryzm.io?subject=PRYZM+Bespoke+Build+Enquiry','_blank');
});
```

**File: `src/styles/AppTheme.ts`**

Step 4. Add CSS for `.pr-bespoke-band` and child elements. It should span the full width of the page with a slightly different background (e.g. light violet tint using `--app-violet-1` at 5% opacity) to signal it is a different type of offering. The CTA button should use the standard `pr-plan-btn--highlighted` styling or match visually.

---

Change 5 — Fix the missing upgrade path for paid plan users in ProjectHub

**File: `src/ui/platform/ProjectHub.ts`**

Currently line 80:

```
${(this.user.plan||'free')==='free'?`<button class="ph-upgrade-btn" id="ph-upgrade-btn">✦ Upgrade</button>`:''}
```

This hides the upgrade button for Architect and Studio users, leaving them with no path to a higher tier from the hub.

Change the condition to show the button for any plan that has a tier above it — meaning everyone except `'enterprise'`:

```
${(this.user.plan||'free')!=='enterprise'?`<button class="ph-upgrade-btn" id="ph-upgrade-btn">✦ Upgrade</button>`:''}
```

The button label can also be contextual: free plan shows "✦ Upgrade", paid plan shows "✦ Manage plan". To do this, change the button text to:

```
${(this.user.plan||'free')==='free'?'✦ Upgrade':'✦ Manage plan'}
```

No other changes needed in `ProjectHub.ts` — the click handler already calls `this.callbacks.onUpgrade?.()` which routes to the Pricing page.

---

Change 6 — Update Enterprise tagline in PlanConfig

**File: `src/monetization/PlanConfig.ts`**

One line change. In `PLAN_PRICING.enterprise`, change:

```
tagline:'Custom pricing for large organisations',
```

to:

```
tagline:'Custom pricing · SaaS or bespoke deployment',
```

This propagates automatically to both the Pricing page card and the UpgradeModal's plan badge.

---

Should Stream 2 appear in the UpgradeModal?

**No.** The UpgradeModal fires in a specific feature-blocked context ("You need Architect for IFC export"). Adding Stream 2 there would confuse the message — the user is trying to do something specific and needs a direct answer. The modal already leads to the Pricing page via "View plans & pricing", and Stream 2 will be visible there after Change 4. The two-step journey (modal → pricing page → Stream 2 section) is correct and keeps the modal focused.

---

File change summary

| **File** | **What changes** |
| --- | --- |
| `src/ui/platform/LandingPage.ts` | Add `onPricing` + `onContactSales` to interface; wire Pricing nav; fix Contact sales; add Stream 2 section HTML |
| `src/ui/platform/PlatformRouter.ts` | Pass `onPricing` and `onContactSales` in `showLanding()` callbacks |
| `src/ui/platform/PricingPage.ts` | Add Stream 2 band section; update Enterprise feature bullet; wire Stream 2 CTA |
| `src/ui/platform/ProjectHub.ts` | Fix upgrade button visibility condition; contextual button label |
| `src/monetization/PlanConfig.ts` | Update Enterprise tagline (1 line) |
| `src/styles/AppTheme.ts` | Add `lp-bespoke-*` and `pr-bespoke-*` CSS classes |

No new files. No backend changes. No routing changes.

is this proposal in aligment with the contract (docs/01-BIM-ENGINE-CORE-CONTRACT, 02-BIM-SPATIAL-PROJECTION-CONTRACT, 03-BIM-SEMANTIC-MODEL-CONTRACT, 04-BIM-AI-MODIFICATION-PROTOCOL, 05-BIM-UI-ARCHITECTURE-CONTRACT, 06-BIM-PLATFORM-UI-CONTRACT,07-BIM-SECURITY-CONTRACT).?

Let me read all 7 contracts in parallel.

Now let me quickly check the remaining contracts for completeness.

I now have everything I need. Here is the complete contract compliance review.

---

Contract Compliance Report — Stream 1 & Stream 2 Implementation Plan

---

Contracts §01, §02, §03, §04 — Not applicable

The proposed changes touch only platform UI files (`LandingPage.ts`, `PricingPage.ts`, `ProjectHub.ts`, `PlatformRouter.ts`, `PlanConfig.ts`, `AppTheme.ts`). None of these files are in the BIM engine stack. No stores, no commands, no builders, no semantic model, no spatial projection, no AI protocol — §01 through §04 have no jurisdiction over these changes. All four are clean.

Note: **§07-BIM-SECURITY-CONTRACT does not exist** in the `docs/` folder. There is no such file.

---

Contract §05 — BIM UI Architecture Contract

**Status: ⚠️ Two violations found, one mandatory procedure gap**

---

**Violation 1 — `pr-` prefix is not registered (§05 §3)**

The prefix table in §05 §3 lists these registered prefixes: `gpp-`, `tp-`, `vb-`, `cw-`, `fpi-`, `lp-`, `am-`, `ph-`, `plat-`, `ai-`, `st-`, `ap-`.

`PricingPage.ts` already uses the `pr-` prefix throughout (`.pr-page`, `.pr-plan-card`, `.pr-addons-section`, etc.) but this prefix is **not in the §05 §3 table**. The plan proposes adding new `pr-bespoke-*` classes — these would compound an existing unregistered prefix.

**Required fix before implementation:** Add `pr-` (Pricing Page) and `um-` (Upgrade Modal, same issue) to the §05 §3 prefix table, and update §05 §5 Step 7 to document these panels. This is a documentation fix — the CSS itself is not broken, but the registry is incomplete.

---

**Violation 2 — §05 §10 Modification Declaration not included**

The contract requires a formal Modification Declaration before any UI file is changed. The plan does not include one. Any implementer must produce this declaration before touching code:

```
## UI MODIFICATION DECLARATION

Panel Affected: LandingPage, PricingPage, ProjectHub

CSS Scope: AppTheme.ts — LANDING_PAGE_STYLES (new lp-bespoke-* classes),
           AppTheme.ts — PRICING_PAGE_STYLES (new pr-bespoke-* classes)

Files Modified:
- src/ui/platform/LandingPage.ts
- src/ui/platform/PricingPage.ts
- src/ui/platform/ProjectHub.ts
- src/ui/platform/PlatformRouter.ts
- src/monetization/PlanConfig.ts
- src/styles/AppTheme.ts

Phase: Phase 1 (Shell — pure HTML/CSS additions, no BUI atom replacement)

Class Prefixes Used: lp- (registered), pr- (must be registered first), ph- (registered)

Impact Assessment:
- Logic Impact: Yes — new callbacks added to LandingPageCallbacks interface;
  ProjectHub upgrade button condition changed
- Store Impact: No
- BUI Shadow DOM: No
- display-toggle Safety: No section bodies with JS-toggled display are affected
- z-index / positioning: No

Risk Level: Low

Rationale: Adding Stream 2 entry points and fixing broken navigation links.
```

---

**Procedure gap — §05 §5 Steps must be followed for new CSS blocks**

The plan correctly identifies that new classes go in `AppTheme.ts`. However, the implementer must explicitly:

- Step 2: Write the CSS constant (`LANDING_PAGE_BESPOKE_STYLES` or append to existing `LANDING_PAGE_STYLES` constant)
- Step 3: Concatenate it in `injectAppTheme()` if it is a separate constant — or simply extend the existing named constant if appending
- Step 5: Run `npx tsc --noEmit` and confirm zero errors before considering the work done
- Step 7: Update §05 §3 table to register `pr-` and `um-`

---

**Everything else in §05 — compliant**

| **Rule** | **Status** |
| --- | --- |
| All CSS goes in AppTheme.ts | ✓ Plan correctly specifies this |
| No independent `<style>` injection | ✓ Plan doesn't propose any |
| All colours use `var(--app-*)` tokens | ✓ Plan specifies `--app-violet-1` and `--app-violet-soft` |
| No `!important` declarations | ✓ None proposed |
| No hardcoded colours that duplicate tokens | ✓ |

---

Contract §06 — BIM Platform UI Contract

**Status: ⚠️ One mandatory declaration gap, one rule to verify**

---

**Declaration gap — §06 §11 Platform Modification Declaration required**

Every platform file modification requires this declaration before implementation begins:

```
## PLATFORM MODIFICATION DECLARATION

Components Affected: LandingPage, PricingPage, ProjectHub, PlatformRouter

CSS Constants Modified:
- LANDING_PAGE_STYLES (new lp-bespoke-* classes)
- PRICING_PAGE_STYLES (new pr-bespoke-* band section)

Files Modified:
- src/ui/platform/LandingPage.ts
- src/ui/platform/PricingPage.ts
- src/ui/platform/ProjectHub.ts
- src/ui/platform/PlatformRouter.ts
- src/monetization/PlanConfig.ts
- src/styles/AppTheme.ts

Impact Assessment:
- BIM Engine Touched: No
- localStorage Keys Modified: None
- New CSS Prefix: pr- and um- must be formally claimed in §05 §3
- Destroy() updated: No — LandingPage.destroy() calls this.el.remove() which already
  removes all child nodes including any new sections; no change needed
- New tokens added to DESIGN_TOKENS: No (existing tokens are sufficient)

Risk Level: Low
Rationale: Stream 2 entry points, broken nav link fix, upgrade path for paid users.
```

---

**Rule to verify — no new localStorage keys (§06 §7)**

The plan introduces no new localStorage keys. The `window.open('mailto:...')` call is a browser-native API, not a storage access. This is clean.

---

**Everything else in §06 — compliant**

| **Rule** | **Status** |
| --- | --- |
| No BIM engine imports in platform components | ✓ None proposed |
| All CSS in AppTheme.ts named constants | ✓ |
| destroy() covers all new DOM nodes | ✓ — lp-bespoke section is inside this.el, which destroy() already removes via this.el.remove() |
| PlatformRouter remains the only entry point | ✓ |
| No #container access | ✓ |
| No independent `<style>` injection | ✓ |
| No server-side auth calls | ✓ — mailto: is a browser API, not a server call |
| Component files in src/ui/platform/ | ✓ — no new files proposed |
| §06 §12 compliance checklist must be run after | Required of implementer |

---

Summary

| **Contract** | **Status** | **Action required** |
| --- | --- | --- |
| §01 BIM Engine Core | ✓ Not applicable | None |
| §02 Spatial Projection | ✓ Not applicable | None |
| §03 Semantic Model | ✓ Not applicable | None |
| §04 AI Modification Protocol | ✓ Not applicable | None |
| §05 UI Architecture | ⚠️ Two gaps | Register `pr-` and `um-` in §05 §3 table; write Modification Declaration before coding |
| §06 Platform UI | ⚠️ One gap | Write Platform Modification Declaration before coding |
| §07 Security | — | File does not exist |

The implementation plan is **architecturally sound**. No rule in any contract prohibits any of the functional changes proposed. The two gaps are procedural — they must be completed before the first line of code is written, not after.

provide the full updated plan

Here is the complete, consolidated implementation plan — functional changes and contract compliance fully integrated.

---

Full Implementation Plan: Stream 1 & Stream 2 — Contract-Compliant

---

Pre-Work: Contract Compliance Steps (Must be done before any code)

---

Pre-Step 1 — Register missing CSS prefixes in §05

**File: `docs/05-BIM-UI-ARCHITECTURE-CONTRACT.md`**

In the prefix table in Section 3, add two missing rows that already exist in the codebase but were never formally registered:

| **Prefix** | **Panel** | **File** |
| --- | --- | --- |
| `pr-` | Pricing Page | `src/styles/AppTheme.ts` — `PRICING_PAGE_STYLES` |
| `um-` | Upgrade Modal | `src/styles/AppTheme.ts` — `UPGRADE_MODAL_STYLES` |

---

Pre-Step 2 — Write the Modification Declarations

Before touching any source file, the following two declarations must be recorded (they can be added as comments at the top of each modified file's git commit, or kept as a working note):

**UI Modification Declaration (§05 §10):**

```
Panel Affected: LandingPage, PricingPage, ProjectHub
CSS Scope: AppTheme.ts — LANDING_PAGE_STYLES (new lp-bespoke-* classes),
           AppTheme.ts — PRICING_PAGE_STYLES (new pr-bespoke-* classes)
Files Modified: LandingPage.ts, PricingPage.ts, ProjectHub.ts,
                PlatformRouter.ts, PlanConfig.ts, AppTheme.ts
Phase: Phase 1 (Shell — additive HTML/CSS only, no BUI atom replacement)
Class Prefixes Used: lp- (registered), pr- (now registered), ph- (registered)
Impact Assessment:
  - Logic Impact: Yes — new callbacks on LandingPageCallbacks; ProjectHub condition change
  - Store Impact: No
  - BUI Shadow DOM: No
  - display-toggle Safety: No JS-toggled section bodies affected
  - z-index / positioning: No
Risk Level: Low
Rationale: Stream 2 entry points, broken nav fix, upgrade path for paid plan users.
```

**Platform Modification Declaration (§06 §11):**

```
Components Affected: LandingPage, PricingPage, ProjectHub, PlatformRouter
CSS Constants Modified: LANDING_PAGE_STYLES, PRICING_PAGE_STYLES
Files Modified: LandingPage.ts, PricingPage.ts, ProjectHub.ts,
                PlatformRouter.ts, PlanConfig.ts, AppTheme.ts
Impact Assessment:
  - BIM Engine Touched: No
  - localStorage Keys Modified: None
  - New CSS Prefix: pr- and um- claimed in §05 §3 (Pre-Step 1)
  - destroy() updated: No — lp-bespoke section is inside this.el;
    LandingPage.destroy() calls this.el.remove() which removes it automatically
  - New tokens added to DESIGN_TOKENS: No
Risk Level: Low
Rationale: Stream 2 entry points, broken nav fix, upgrade path for paid plan users.
```

---

Implementation Changes

---

Change 1 — Register `pr-` and `um-` CSS constants in `injectAppTheme()`

**File: `src/styles/AppTheme.ts`**

The `PRICING_PAGE_STYLES` and `UPGRADE_MODAL_STYLES` constants must already exist in this file (PricingPage and UpgradeModal both call `injectAppTheme()`). Verify they are concatenated inside `injectAppTheme()`'s `style.textContent` assignment. If either is missing, add it. This is a verification step — no new content yet.

---

Change 2 — Wire the "Pricing" nav link on the Landing page

**File: `src/ui/platform/LandingPage.ts`**

Step 1. Add `onPricing: () => void` to the `LandingPageCallbacks` interface (currently only has `onGetStarted` and `onLogin`).

Step 2. In the nav HTML inside `build()`, change:

```
<aclass="lp-nav-link"href="#">Pricing</a>
```

to:

```
<aclass="lp-nav-link"href="#"id="lp-nav-pricing">Pricing</a>
```

Step 3. In the event listener block at the bottom of `build()`, add:

```
el.querySelector('#lp-nav-pricing')!.addEventListener('click',(e) =>{
e.preventDefault();
this.callbacks.onPricing();
});
```

**File: `src/ui/platform/PlatformRouter.ts`**

Step 4. Inside `showLanding()`, the `LandingPage` constructor receives a callbacks object. Add:

```
onPricing:() =>this.showPricing(),
```

`showPricing()` already exists on the router — this connects the nav button to it.

---

Change 3 — Fix the "Contact sales" button on the Landing page

**File: `src/ui/platform/LandingPage.ts`**

Step 1. Add `onContactSales: () => void` to the `LandingPageCallbacks` interface.

Step 2. In the event listener block, change the existing line:

```
el.querySelector('#lp-nav-contact')!.addEventListener('click',() =>this.callbacks.onLogin());
```

to:

```
el.querySelector('#lp-nav-contact')!.addEventListener('click',() =>this.callbacks.onContactSales());
```

**File: `src/ui/platform/PlatformRouter.ts`**

Step 3. Inside `showLanding()`, add:

```
onContactSales:() =>window.open('mailto:hello@pryzm.io?subject=PRYZM+Sales+Enquiry','_blank'),
```

---

Change 4 — Add Stream 2 section to the Landing page

**File: `src/ui/platform/LandingPage.ts`**

Step 1. After the closing `</section>` of the hero section (the last HTML block before the closing backtick of `el.innerHTML`), add a new `<section id="lp-bespoke">`. It must use only `lp-` prefixed classes. Content:

- **Left column** (`lp-bespoke-col lp-bespoke-col--left`):
    - Heading (`lp-bespoke-heading`): "Building your own platform?"
    - Description (`lp-bespoke-desc`): "AI is making software cheap to build. We partner with enterprises to deploy a bespoke BIM platform under their brand — custom element libraries, your workflows, your infrastructure."
    - Actions row (`lp-bespoke-actions`): two buttons:
        - `id="lp-bespoke-contact"` labelled "Talk to us" — primary style
        - `id="lp-bespoke-learn"` labelled "See enterprise options" — secondary style
- **Right column** (`lp-bespoke-col lp-bespoke-col--right`):
    - Unordered list (`lp-bespoke-list`) with these items:
        - Custom element & material libraries
        - Integration with Revit, ArchiCAD, and ERP systems
        - White-label under your brand
        - On-premise or private cloud deployment
        - Dedicated build team and ongoing support

Step 2. In the event listener block at the bottom of `build()`, add:

```
el.querySelector('#lp-bespoke-contact')!.addEventListener('click',() =>this.callbacks.onContactSales());
el.querySelector('#lp-bespoke-learn')!.addEventListener('click',() =>this.callbacks.onPricing());
```

**File: `src/styles/AppTheme.ts`**

Step 3. Inside the `LANDING_PAGE_STYLES` constant (the `lp-` block), append CSS for the new section. All rules must use `var(--app-*)` tokens — no hardcoded hex. Required classes and their roles:

- `.lp-bespoke` — outer section; full-width; background uses `var(--app-violet-soft)` (the ultra-soft violet tint already defined); padding top/bottom ~80px
- `.lp-bespoke-inner` — centered max-width container (same max-width as hero card); display flex; gap ~64px; align-items center
- `.lp-bespoke-col` — flex child; flex: 1
- `.lp-bespoke-heading` — font-size ~28px; font-weight 700; color `var(--app-text)`; margin-bottom 16px
- `.lp-bespoke-desc` — font-size ~15px; color `var(--app-text-2)`; line-height 1.6; margin-bottom 28px
- `.lp-bespoke-actions` — display flex; gap 12px; flex-wrap wrap
- `.lp-bespoke-actions button:first-child` — primary style using `var(--app-gradient)` background; white text; same border-radius and padding as `.lp-hero-btn`
- `.lp-bespoke-actions button:last-child` — secondary style; transparent background; border 1.5px solid `var(--app-violet-1)`; color `var(--app-violet-1)`
- `.lp-bespoke-list` — list-style none; padding 0; display flex; flex-direction column; gap 12px
- `.lp-bespoke-list li` — font-size ~14px; color `var(--app-text-2)`; padding-left 20px; position relative
- `.lp-bespoke-list li::before` — content `"✓"`; position absolute; left 0; color `var(--app-violet-1)`

Add a `@media (max-width: 768px)` rule: `.lp-bespoke-inner { flex-direction: column; }` so the two columns stack on mobile.

---

Change 5 — Add Stream 2 section to the Pricing page

**File: `src/ui/platform/PricingPage.ts`**

Step 1. In the `render()` method, after the closing `</div>` of `#pr-plans` and before the opening `<div class="pr-addons-section">`, insert a new `<div class="pr-bespoke-band">`. Content:

- Left side (`pr-bespoke-content`):
    - Heading (`pr-bespoke-heading`): "Enterprise custom deployments"
    - Description (`pr-bespoke-desc`): "For organisations that need PRYZM deployed as their own product — white-labelled, on-premise, and fully customised to your workflows. Scoped and priced separately from the plans above."
    - Unordered list (`pr-bespoke-list`):
        - Discovery and scoping workshop
        - Custom IFC schema configuration
        - Bespoke element family and material libraries
        - Integration with your existing tools (ERP, Revit, project management)
        - White-label branding and private deployment
        - Handover, training, and ongoing engineering support
- Right side (`pr-bespoke-cta-wrap`):
    - Button `id="pr-bespoke-cta"` labelled "Talk to us about a bespoke build"

Step 2. Update `PLAN_FEATURES.enterprise` array. Add as the first item:

```
'Bespoke platform deployment (custom scoping — see below)',
```

Step 3. In `attachListeners()`, add:

```
el.querySelector('#pr-bespoke-cta')!.addEventListener('click',() =>{
window.open('mailto:hello@pryzm.io?subject=PRYZM+Bespoke+Build+Enquiry','_blank');
});
```

**File: `src/styles/AppTheme.ts`**

Step 4. Inside the `PRICING_PAGE_STYLES` constant (the `pr-` block), append CSS for the new band. Required classes:

- `.pr-bespoke-band` — full-width section; margin-top 48px; background `var(--app-violet-soft)`; border-radius `var(--app-radius-lg)`; padding 48px; display flex; gap 48px; align-items flex-start
- `.pr-bespoke-content` — flex: 1
- `.pr-bespoke-heading` — font-size 22px; font-weight 700; color `var(--app-text)`; margin-bottom 12px
- `.pr-bespoke-desc` — font-size 14px; color `var(--app-text-2)`; line-height 1.6; margin-bottom 20px
- `.pr-bespoke-list` — list-style none; padding 0; display flex; flex-direction column; gap 8px
- `.pr-bespoke-list li` — font-size 13px; color `var(--app-text-2)`; padding-left 18px; position relative
- `.pr-bespoke-list li::before` — content `"✓"`; position absolute; left 0; color `var(--app-violet-1)`
- `.pr-bespoke-cta-wrap` — display flex; align-items center; flex-shrink 0
- `.pr-bespoke-cta-wrap button` — uses `var(--app-gradient)` background; white text; padding 14px 28px; border-radius `var(--app-radius-sm)`; border none; cursor pointer; font-weight 600; white-space nowrap; box-shadow `var(--app-shadow-glow)`

Add `@media (max-width: 768px)`: `.pr-bespoke-band { flex-direction: column; }`

---

Change 6 — Update Enterprise tagline in PlanConfig

**File: `src/monetization/PlanConfig.ts`**

In `PLAN_PRICING.enterprise`, change the `tagline` field from:

```
tagline:'Custom pricing for large organisations',
```

to:

```
tagline:'Custom pricing · SaaS or bespoke deployment',
```

This is a single data-only line change. It propagates automatically to the Pricing page card and the UpgradeModal plan badge.

---

Change 7 — Fix upgrade button visibility for paid plan users in ProjectHub

**File: `src/ui/platform/ProjectHub.ts`**

Current line 80:

```
${(this.user.plan||'free')==='free'?`<button class="ph-upgrade-btn" id="ph-upgrade-btn">✦ Upgrade</button>`:''}
```

This hides the upgrade button for Architect and Studio users, giving them no visible upgrade path to a higher tier.

Replace with:

```
${(this.user.plan||'free')!=='enterprise'?`<button class="ph-upgrade-btn" id="ph-upgrade-btn">${(this.user.plan||'free')==='free'?'✦ Upgrade':'✦ Manage plan'}</button>`:''}
```

This shows the button for all plans except Enterprise. Free users see "✦ Upgrade". Paid users (Architect, Studio, Firm) see "✦ Manage plan". The click handler at line 235 already calls `this.callbacks.onUpgrade?.()` — no change needed there.

---

Post-Implementation: Required Checklist

These steps are mandatory per §05 §5 and §06 §12 before the work is considered complete:

- [ ]  Run `npx tsc --noEmit` — zero errors required
- [ ]  No BIM engine imports added to any modified file (`src/core/`, `src/commands/`, `src/elements/`, `src/ai/`)
- [ ]  All new CSS is in `AppTheme.ts` named constants — no `<style>` injection anywhere
- [ ]  All new colours use `var(--app-*)` tokens — no hardcoded hex
- [ ]  `destroy()` verified: `LandingPage.destroy()` calls `this.el.remove()` — covers all new child nodes automatically
- [ ]  No `id=` attributes removed from existing JS-toggled elements
- [ ]  Restart the workflow and confirm the app loads without console errors
- [ ]  Confirm the Pricing nav link on the landing page opens the pricing page
- [ ]  Confirm "Contact sales" opens a mail client, not the auth screen
- [ ]  Confirm the Stream 2 section is visible on both the landing page and pricing page
- [ ]  Confirm an Architect-plan user sees "✦ Manage plan" in the project hub header
- [ ]  Update `docs/05-BIM-UI-ARCHITECTURE-CONTRACT.md` §3 table to confirm `pr-` and `um-` are registered (Pre-Step 1 now formally complete)

---

File Change Summary

| **File** | **What changes** | **Risk** |
| --- | --- | --- |
| `docs/05-BIM-UI-ARCHITECTURE-CONTRACT.md` | Register `pr-` and `um-` in prefix table | None |
| `src/ui/platform/LandingPage.ts` | Add `onPricing` + `onContactSales` to interface; wire Pricing nav link; fix Contact sales button; add Stream 2 section HTML and two new event listeners | Low |
| `src/ui/platform/PlatformRouter.ts` | Pass `onPricing` and `onContactSales` in `showLanding()` callbacks object | None |
| `src/ui/platform/PricingPage.ts` | Add `pr-bespoke-band` section; update Enterprise feature bullet; wire `#pr-bespoke-cta` button | Low |
| `src/ui/platform/ProjectHub.ts` | Fix upgrade button condition; contextual button label | Low |
| `src/monetization/PlanConfig.ts` | Change Enterprise `tagline` (1 line) | None |
| `src/styles/AppTheme.ts` | Add `lp-bespoke-*` CSS to `LANDING_PAGE_STYLES`; add `pr-bespoke-*` CSS to `PRICING_PAGE_STYLES` | Low |

No new files. No new localStorage keys. No backend changes. No BIM engine involvement.