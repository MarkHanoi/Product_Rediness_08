Here’s a deep architectural audit prompt you can give to Replit AI or another senior-engineering reviewer. It is framed the way a platform/engine lead would investigate a production-grade BIM/3D scene accuracy problem — not just “fix clicking”.

---

# PRYZM — 3D Scene Accuracy & Selection System Audit

You are acting as a senior graphics engineer, BIM engine architect, and interaction systems specialist.

I need a FULL TECHNICAL AUDIT of the PRYZM modelling engine focused on:

* 3D scene spatial accuracy
* Selection precision
* Raycasting correctness
* Coordinate system consistency
* Camera projection integrity
* Semantic-to-geometry alignment
* Fragment mesh registration
* Transform propagation
* World/local coordinate conversion
* Precision drift
* Scene graph integrity
* Picking architecture
* Input interaction reliability
* GPU/render synchronization
* Bounding volume accuracy
* BIM semantic authority vs rendered geometry consistency

The issue currently observed:

The mouse pointer is visually over one element, but another element gets selected.

This is unacceptable for a BIM authoring environment.

The system already supports:

* semantic modelling
* wall creation
* geometry generation
* fragments
* command system
* spatial authority
* scene graph registration
* element IDs
* raycasting
* selection manager
* BIM hierarchy

The issue appears mainly around:

* selection accuracy
* pointer projection
* picking consistency
* rendered geometry vs semantic geometry mismatch

I need a DEEP ENGINEERING AUDIT.

---

# PART 1 — FULL SELECTION PIPELINE TRACE

Audit the COMPLETE selection pipeline from:

1. DOM mouse event
2. viewport coordinate extraction
3. canvas coordinate normalization
4. DPR (devicePixelRatio) handling
5. viewport bounds calculations
6. NDC conversion
7. raycaster setup
8. camera matrices
9. world ray generation
10. mesh intersection
11. fragment/entity mapping
12. semantic element resolution
13. selection manager final resolution

For EACH step:

* explain what the code currently does
* identify potential precision failures
* identify architectural risks
* identify common Three.js pitfalls
* identify BIM-specific risks
* identify coordinate mismatch risks
* explain how inaccuracies propagate downstream

---

# PART 2 — WORLD SPACE & COORDINATE SYSTEM AUDIT

Audit all coordinate systems in the application:

* screen space
* canvas space
* normalized device coordinates
* camera space
* world space
* local object space
* BIM semantic space
* level-relative space
* fragment geometry space

Check:

* axis consistency
* handedness
* scale consistency
* unit consistency
* transform inheritance
* parent transforms
* nested object transforms
* matrix updates
* stale matrices
* world matrix propagation

Identify:

* where transforms can drift
* where semantic coordinates may diverge from rendered coordinates
* where object origins may be inconsistent
* where level elevation logic may affect picking

---

# PART 3 — CAMERA & PROJECTION AUDIT

Audit:

* PerspectiveCamera configuration
* OrthographicCamera behavior (if present)
* aspect ratio updates
* resize handling
* projection matrix updates
* frustum calculations
* near/far plane precision issues
* floating point instability
* zoom behavior
* orbit controls integration
* camera matrix synchronization

Specifically verify:

* raycaster uses latest camera matrices
* projection matrices update after resize
* DPR scaling is correctly applied
* CSS scaling does not desynchronize canvas interaction

---

# PART 4 — RAYCASTING ARCHITECTURE AUDIT

Audit the ENTIRE raycasting implementation.

Check:

* recursive raycasting usage
* invisible mesh filtering
* layer filtering
* stale mesh references
* duplicate meshes
* fragment wrappers
* transformed geometry
* merged geometries
* instanced meshes
* BVH acceleration usage
* bounding box correctness
* bounding sphere correctness

Verify:

* ray intersections occur on the correct geometry
* nearest hit logic is correct
* intersection sorting is correct
* hidden objects are excluded
* helper meshes are excluded
* overlay meshes are excluded

Identify:

* if the selected mesh differs from the rendered visual surface
* if proxy geometry is being hit instead
* if stale fragments remain in scene

---

# PART 5 — SEMANTIC ↔ GEOMETRY CONSISTENCY AUDIT

This is CRITICAL.

Audit whether semantic BIM elements remain perfectly synchronized with rendered geometry.

Check:

* wall baseline updates
* join resolution updates
* fragment rebuild timing
* geometry regeneration timing
* scene replacement timing
* fragmentMap consistency
* elementRegistry consistency
* stale geometry removal
* async update race conditions

Specifically verify:

* geometry generated by WallFragmentBuilder exactly matches semantic authority
* transformed geometry still maps correctly to semantic IDs
* joins do not visually diverge from semantic baselines
* wall offsets and miter calculations do not break selection accuracy

---

# PART 6 — HIGH-PRECISION BIM RISKS

Audit BIM-specific precision risks:

* large world coordinates
* floating origin issues
* precision loss at distance
* level elevation accumulation
* offset stacking
* nested transforms
* wall join offsets
* opening offsets
* curve tessellation inaccuracies

Determine whether:

* precision loss causes picking inaccuracies
* geometry differs from semantic intent
* fragment tessellation diverges from baseline calculations

---

# PART 7 — DEBUGGING TOOLING TO IMPLEMENT

Provide a PROFESSIONAL debugging toolkit proposal.

I want:

* ray visualization
* hovered face visualization
* selected triangle visualization
* world-coordinate overlays
* semantic baseline overlays
* bounding box visualization
* fragment ID overlays
* mesh origin visualization
* transform hierarchy inspector
* matrix inspection tooling
* camera frustum visualization
* intersection point visualization

Provide exact implementation strategy.

---

# PART 8 — PERFORMANCE VS ACCURACY ANALYSIS

Audit tradeoffs between:

* performance
* geometry batching
* merged meshes
* BVH acceleration
* instancing
* selection precision
* semantic fidelity

Identify:

* where optimization may have broken correctness
* where architectural shortcuts may exist
* where stale caches may exist

---

# PART 9 — ROOT CAUSE PRIORITIZATION

Provide:

* Top 10 MOST LIKELY causes
* ranked by probability
* ranked by architectural severity
* ranked by impact on BIM authoring reliability

For each:

* explain WHY it happens
* explain symptoms
* explain how to validate it
* explain exact fix strategy

---

# PART 10 — PRODUCTION-GRADE BIM SELECTION ARCHITECTURE

Finally:

Design what the IDEAL production-grade selection architecture should be for PRYZM.

Include:

* semantic authority model
* fragment registration model
* picking pipeline
* interaction system
* event flow
* transform synchronization
* render/update lifecycle
* debugging hooks
* precision strategy
* large-model strategy
* deterministic selection guarantees

The goal:
PRYZM must achieve professional BIM-authoring-grade interaction accuracy comparable to Revit, Rhino, or Blender-level selection reliability.

Do not provide superficial advice.

I want a REAL ENGINEERING AUDIT.
