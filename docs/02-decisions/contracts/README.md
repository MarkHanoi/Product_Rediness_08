# PRYZM — Contract Suite Index (C00)

> **Stamp**: 2026-05-31 · **Refreshed**: 2026-06-01 (folder restructure) · **Status**: CANONICAL
> **Authority**: these contracts govern every implementation decision in PRYZM.
> When code disagrees with a contract, the code is wrong — fix the code **or** open an ADR that supersedes the contract section; never do both.
>
> **Brand note**: "PRYZM 3" appears in some legacy contract bodies to mean "the current architectural epoch". The product name is **PRYZM** (no version suffix) — see [../../NAMING-CONVENTIONS.md §1](../../NAMING-CONVENTIONS.md). The internal epoch label survives only where historical context is the point.

---

## Conflict resolution order (strongest → weakest)

1. `docs/01-strategy/STR-02-product-vision.md` — product + business vision (foundation)
2. `docs/01-strategy/STR-03-engineering-vision.md` — engineering intent and principles (P1–P8)
3. `docs/01-strategy/STR-04-architecture.md` — system shape, lint gates, convergence booleans
4. **This contract suite** — per-subsystem binding contracts (**C01–C100 + C24.1**; **C61** is the ONLY RESERVED, unminted slot — **C76 was MINTED 2026-08-19** as [Platform & API Surface](./C76-PLATFORM-AND-API-SURFACE.md); its original reservation for the element-family register, COORD-01, was spent when that role was taken by **C84** instead). *Range corrected **2026-08-18** — this row read "C01–C83 + C24.1" while **C84 and C85–C100 existed as files**, so seventeen contracts, including the one binding on every element-family PR, sat outside the stated ordering. Re-measure, never re-transcribe:* `ls docs/02-decisions/contracts/ | grep -c '^C[0-9]'` → **100** *(re-measured **2026-08-19** after C76 was minted; it read 99 the day before, which is the point of re-running it. = C01–C100 minus C61, plus C24.1). The row and the range move together — that is the whole point of the rule, and it has now failed four times (C67 → C81 → C84/C85–C99 → C100).*
5. `docs/02-decisions/adrs/` — per-decision rationale (**268** ADRs — *measured 2026-08-18 with* `ls docs/02-decisions/adrs/ADR-*.md | wc -l`*; this row has read 196, then 251, then 267. Run the command.*)
6. `docs/03-execution/specs/` — per-system normative specs (**96** SPECs — *measured 2026-08-18 with* `find docs -name 'SPEC-*.md' | wc -l`*; this row has read 82, then 92, then 95. Run the command.*)

> ## ⛔ THE RANGE IN ROW 4 AND THE COUNT IN §"69 contract files" ARE BOTH FALSE — corrected 2026-08-18.
>
> **Measured** — `ls docs/02-decisions/contracts/C*.md | wc -l` → **98**, not 69.
> `ls ../adrs/ADR-*.md | wc -l` → **267**, not 251. `find docs -name 'SPEC-*.md' | wc -l` → **95**, not 92.
>
> **Row 4 reads `C01–C83 + C24.1`. At least fifteen contracts sit outside that range**, including
> **[C84 — ELEMENT INTEGRITY](./C84-ELEMENT-INTEGRITY.md)**, which is binding on *every PR touching
> an element family*, and the **C85–C99 per-element block** C84 §6 allocates — of which C85, C87,
> C88, C89, C90, C91, C92, C93, C94, C95, C96, C97, C98 and C99 have landed as files with **no row
> in the table below**.
>
> ⚠ **CLAUDE.md is wrong in the same way and by more**: its Governance section states the suite is
> `C01–C68 + C24.1`, leaving **thirty contracts outside the stated ordering**. An agent reading it
> literally would rank C84 *below an ADR*. That is the identical failure this row already records
> for C67 and C68 — and it has now recurred three times (C67 → C81 → C84/C85–C99), which is the
> finding: **the rule "the row and the range move together" is stated here and is not enforced by
> anything.**
>
> **DO NOT RE-TRANSCRIBE THESE NUMBERS.** Every count in rows 4–6 has been wrong at least twice.
> Run the command. Per [C69 §0.1](./C69-API-VERB-REGISTER.md)'s rule for the verb register — cite
> the generated artefact, never copy it — the same applies here.
>
> *Exit condition:* a gate asserts `ls contracts/` equals this table's row set **in both
> directions**, so neither a file without a row nor a row without a file can survive a commit.
> Until that gate exists, this banner is the only thing standing between the index and its next
> drift. **See [CONTRACT-AMENDMENT-REGISTER.md](./CONTRACT-AMENDMENT-REGISTER.md) for the full
> list of measured contract defects awaiting amendment.**

> ⚠ **Corrected 2026-08-11.** Row 4 read **"C01–C56"**, row 5 **"196 ADRs"**, row 6 **"82 SPECs"**.
> This list is **the authoritative ordering statement that `CLAUDE.md` defers to**, so a stale
> range here silently demotes real contracts: at C56 the range excluded **C57, C58, C59, C60,
> C62–C69** — among them **C66** (no capacity claim without a measurement), **C67** and **C68**
> (both CANONICAL, both binding on every capability PR). Anyone following the order literally
> ranked C68 *below an ADR*. Measured:
> `ls docs/02-decisions/contracts/ | grep -c '^C[0-9]'` → **69** (re-measured 2026-08-11 after C69 was minted; it read 68 an hour earlier — which is the point of re-running it) ·
> `ls docs/02-decisions/adrs/ADR-*.md | wc -l` → **251** ·
> `find docs -name 'SPEC-*.md' | wc -l` → **92**.
> **These three counts rot on every merge — re-run the commands rather than trusting the digits,
> and keep row 4's *range* correct even when the counts drift.**
7. `docs/archive/pryzm3-internal/` — archived PRYZM1/2 planning + contracts (informational only)

Cross-cutting docs that govern the contract suite as a whole:
- `docs/03-execution/plans/master-execution-tracker.md` — the live day-to-day delivery tracker (the former `master-implementation-plan.md` is archived at `plans/legacy/superseded-2026-06-01/master-implementation-plan-2026-05-31.md`)
- `docs/03-execution/status/prior-art-audit-2026-05-31.md` — code-state audit grounding the master plan

If a contract and an ADR disagree, **the contract wins**; the ADR must be updated or a new ADR raised. If a contract and the Vision/Architecture docs disagree, **Vision/Architecture wins**; amend the contract.

---

## Contract status vocabulary + Tier-1 launch gate (L-347)

> Added 2026-07-16 to close the L-347 vocabulary gap: prior to this, contracts were only `DRAFT` or `CANONICAL`, and neither certified that the **shipping code matches** — so a contract could read CANONICAL while prod contradicts it (C08 silent-LWW, C10 un-wired benches). Instructions like "do not launch until C08/C13 are ACTIVE" were therefore un-actionable. This section defines the missing vocabulary.

**Status ladder (each status is strictly stronger than the one above):**

| Status | Meaning |
|---|---|
| **DRAFT** | Authored, not yet ratified. Content MAY be aspirational (describe intended, not-yet-built behavior). A DRAFT contract binds nothing. |
| **CANONICAL** | Ratified + binding as the authority. The contract wins over code and ADRs. **CANONICAL does NOT assert the code conforms** — it asserts the *intent* is settled. A contract can be CANONICAL while prod contradicts it (that is a Known-Violation, logged as an L-item). |
| **ACTIVE** | CANONICAL **AND** the shipping code demonstrably matches the contract **AND** its §6 CI gate (where one exists) is green. **ACTIVE is the only status that certifies code-conformance.** Flipping to ACTIVE requires a passing test as evidence, never inspection. |

**Tier-1 (launch-blocking) contracts.** These govern the properties this product category is judged on (data integrity, collaboration correctness, isolation, performance, durability, privacy). **Launch is blocked until every Tier-1 contract is ACTIVE:**

| Tier-1 contract | Governs | Today |
|---|---|---|
| **C05** Persistence & File Format | save/load integrity, canonical format | CANONICAL (L-334 shipped; L-337 canonical-format decision open) |
| **C08** Collaboration & Security | conflict-safe sync (P8), auth/roles | CANONICAL, **NOT ACTIVE** — code ships silent LWW (L-53/L-335); non-owner access fixed (L-336) |
| **C10** Performance & Observability | NFT budgets, CI-gated benches | CANONICAL, **NOT ACTIVE** — benches not wired into CI (L-341) |
| **C13** Project Lifecycle & Isolation | per-project isolation invariants | CANONICAL, **NOT ACTIVE** — C13-G1…G7 teardown gaps open (L-342) |
| **C48-min** Backup & DR (minimum) | tested restore of a lost project | DRAFT, unimplemented (L-344) |
| **C22-min** Privacy & PII (minimum) | DSAR + residency honesty (EU) | DRAFT, unimplemented (L-345) |

The launch gate that enforces this is **G0-CONTRACTS** on the V1 launch gate board (`docs/04-reference/V1-LAUNCH-IMPLEMENTATION-PLAN.md`): it does not go green until every Tier-1 contract above is ACTIVE. New contracts are classified Tier-1 only by an explicit decision recorded here.

---

## Contract suite

| # | Contract | Governs | Key principle |
|---|---|---|---|
| **C00** | **Index** (this file) | Hierarchy, conflict resolution · Last updated 2026-05-31 (added C24–C30) | — |
| **C01** | [Architecture & Governance](./C01-ARCHITECTURE-AND-GOVERNANCE.md) | 8-layer model, boundary matrix, CI gates, convergence booleans | P1–P8 |
| **C02** | [Composition Root & Boot](./C02-COMPOSITION-ROOT-AND-BOOT.md) | `composeRuntime()`, `PryzmRuntime`, 3-stage boot, disposal. §3.1 F.events Bootstrap Invariant. §3.2 F-1.2 creation dual-write. §3.3 F-1.2 baseline-update dual-write (hosted-element void correctness). | P1, P3 |
| **C03** | [Schemas, Commands & State](./C03-SCHEMAS-COMMANDS-AND-STATE.md) | L0 schemas, command bus, Zustand stores, CQRS + undo. Hosted element schema: see C15. **§2.3.1** (2026-08-11) — a handler that reports success MUST have changed authoritative state (the rule itself is C16 §5.1). **§4.6 U-2b** — a declared `affectedStores` key MUST resolve, in `buildUndoStoreMap()`, to the store the handler actually WROTE; U-2 alone is satisfied by the corrupting case. §1.2/§2.1 gate paths corrected (L-812 class) and P6's "hard-fail" downgraded to the ratchet it really is. | P5, P6 |
| **C04** | [Rendering & Scheduling](./C04-RENDERING-AND-SCHEDULING.md) | `renderer-three` (single THREE owner), frame scheduler (single rAF), scene-committer | P2, P3 |
| **C05** | [Persistence & File Format](./C05-PERSISTENCE-AND-FILE-FORMAT.md) | `persistence-client`, `.pryzm` file format, project lifecycle, isolation | P6 |
| **C06** | [UI Shell & Tools](./C06-UI-SHELL-AND-TOOLS.md) | `PlatformRouter`, panels, tool registration, camera, keyboard. **§9 — Snapping & Reference Datums: level scoping** (added 2026-08-19, [ADR-0335](../adrs/ADR-0335-snap-references-are-level-scoped-in-three-tiers.md), L-1108): every snap candidate declares a level scope and **`SnapManager` alone acts on it** — providers classify, they do not filter, and a level is **never inferred from `point.y`**. **Three scopes**: DATUM (structural + maths grids, parcel boundary, envelope setback) is project-wide BY DESIGN on every storey and is never demoted or filtered — **grids appearing on an upper floor are CORRECT** (§9.2.1); ACTIVE is primary; OTHER-storey references are **offered but SUBORDINATE** (demoted by 1000 against a 210-point band, tagged, muted, labelled) so the cross-level alignment gesture ADR-0112 created survives while never winning silently; UNKNOWN is left alone. **§9.6** — a helper named `*2D` must measure in plan: `pointToLineDistance2D` measured in 3-D against a point pinned to `y = 0`, making CENTERLINE/EDGE/FACE **UNSATISFIABLE** above Level 0. **§9.8** names the gaps: `PlanSnapEngine` is level-scoped only as a consequence of the plan projection, no level BANDS, single global active storey. **§10 — App PHASE & phase-gated chrome** (added 2026-08-19, L-1186): the phase asks **"is a guided onboarding session in progress?"**, never "what am I looking at?"; **§10.2 the phase MUST be DERIVED from a fact declared at the seam that knows it, never REMEMBERED** — at 638cdf33 only the guided site-plan landing and a click on a BIM view-mode button ever moved it off `onboarding-globe`, so a hub click / deep link / reopen-after-reload left the launcher rail, the Split View toggle and the View-Properties launcher **skip-mounted for the whole session** (reported as "OFTEN missing" — the chrome appeared only for sessions that later clicked a view button); **§10.3** is the complete declare-register (start / dispose / project-open / guided create hop) and **MUST NOT** key the guided hop on `isNewProject`; **§10.5** SKIP-MOUNT ≠ HIDE; **§10.6** a phase spec MUST exercise the DIRECT-OPEN path through the production declaration, never `setAppPhase`. **§11 — Import underlays are VIEW-SCOPED** (added 2026-08-19, L-1197): the raster plane `FloorPlanUnderlayTool` adds to the SHARED THREE scene (a traced PDF/scan, or the aerial the `▦ Plan + Site` launcher composites) had **no view scope of any kind and nothing read one**, so it rendered in every view — the founder's *"even my 3D view has this image attached"*. It is NOT a BIM element (`isNonBIM`), so it is outside C09 §4.7; **§11.1** every underlay declares `viewScope: 'plan' | 'all'` and an unclassifiable view fails CLOSED; **§11.2** ONE authority — `mesh.visible` is COMPUTED (`userIntent AND scopeAllows`), a view switch never resurrects an import the user hid, and persistence saves the INTENT not the computed value; **§11.3** this is the FOURTH member of the initScene 2-D-overlay family (floor hatch · room fill · parcel fill) and the next one joins the authority rather than adding a fifth listener; **§11.4** an underlay must name its project — the isolation audit's underlay detector keyed on a `name` and a flag **no production code has ever set**, so the surface could not report the leak it was written to catch. **§12 — THREE layer numbers are SHARED with third-party libraries** (added 2026-08-19, L-1227): `@thatopen/components`' `TechnicalDrawing.addProjectionLines()` ends `ls.layers.set(1)`, and PRYZM's **`EDITOR_LAYER` IS layer 1** — the layer the 3D view deliberately ENABLES for the parcel boundary (§L-426), so 366 OBC projection lines rendered over the model. **§12.1** a camera-layer flip suppresses only objects PRYZM itself assigns; the assignment must be **DERIVED, not remembered** (a one-shot mount-time traverse cannot see lines added afterwards — third recurrence of that shape this week); masks are printed as **NAMES** because `mask 2` is layer **1** and the raw number cost a round trip; and a PRYZM layer is **never renumbered** to dodge a collision. **§12.2** `PLAN_SYMBOL_LAYER` is assigned by nothing, so three `camera.layers.disable()` calls and one ghost sweep have always been no-ops. | P1, P4, P6 |
| **C07** | [Plugin SDK & Marketplace](./C07-PLUGIN-SDK-AND-MARKETPLACE.md) | L6 SDK facade, L7 plugin structure, sandbox, Ed25519, marketplace | P1 |
| **C08** | [Collaboration & Security](./C08-COLLABORATION-AND-SECURITY.md) | CRDT sync, explicit conflicts, JWT auth, rate limiting, CORS, ISO 19650 | P8 |
| **C09** | [AI & Visibility Intent](./C09-AI-AND-VISIBILITY-INTENT.md) | AI host (L2), visibility intent (P7), plan critique, cost governance | P7 |
| **C10** | [Performance & Observability](./C10-PERFORMANCE-AND-OBSERVABILITY.md) | 17 NFTs, OpenTelemetry spans, CI gate inventory, DR runbook | P8 |
| **C11** | [Element Creation Pipeline](./C11-ELEMENT-CREATION-PIPELINE.md) | End-to-end orchestration of element creation for both UI-initiated (user gesture → tool → command) and AI-initiated (AI response → ai-host → command) paths; geometry build lifecycle; event-driven room redetection; batch coalescing. **Covers all three views (3D, plan, elevation) and all element types.** **AS-IS gaps updated 2026-05-04**: 5,627ms LONGTASK eliminated ✅; 117/120 `commandManager.execute()` sites bridged ✅; 2 remaining in `engineLauncher` + `RemoteCommandDispatcher`; 4 systematic interior-protocol gaps (OTel, `runtime.events.emit`, `produceWithPatches`, FrameScheduler) apply universally to all 177 handlers — S03 scope. **2026-05-19 amendments**: §6.2 rewritten with real plan-view trigger chain (5-stage storeEventBus mechanism); §6.3 hardened with RedetectRooms infinite-loop anti-pattern; §7.0 documents 3 critical bugs fixed (F-1.4-REDETECT-LOOP, P3.1-CW-PLAN, FT1-C11-SLAB-BOUNDARY); §10 Transitional Bridge Architecture; §11 Per-Element Compliance Matrix with checklist for new element types. Companion gap analysis: `04-PLAN-FORWARD/34-HANDLER-PROTOCOL-GAP-ANALYSIS.md`. | P3, P6, P8 |
| **C12** | [Geospatial & Coordinate Systems](./C12-GEOSPATIAL.md) | LTP-ENU rebasing (1 km recentre trigger), proj4js integration, IfcProjectedCRS read/write, logarithmic depth buffer for large-scale infrastructure projects. **§1.4** the ONE vertical datum boundary (photoreal-globe anchoring, L-259). **§7** (added 2026-07-17, Known-good ACTIVE — [ADR-0268](../adrs/ADR-0268-cesium-3d-tiles-georeferenced-building-placement.md)): georeferenced building placement on the photoreal 3D-Tiles globe (anchor == LTP-ENU origin; photoreal-tile-clamp ground datum, ELLIPSOIDAL WGS-84; seat-and-reveal; §FORMA-FULL-HEIGHT; CesiumThreeBridge camera-only coexistence) — verified live 2026-07-17, baseline `snapshot-cesium-3d-globe-working-2026-07-17`. | P1, P3, P8 |
| **C13** | [Project Lifecycle and Isolation](./C13-PROJECT-LIFECYCLE-AND-ISOLATION.md) | Normative project session model (open/active/close/switch); isolation invariants preventing per-project state (BatchCoordinator, wall-rebuild flags, pending wall events, rAF handles) from leaking across project boundaries; teardown sequence; E2E testability gates; OTel span requirement. **AS-IS gaps**: 7 gaps (C13-G1–G7) — `pryzm-project-switch` only resets `_levelCamReady`; no `forceReset()` on BatchCoordinator; no teardown surface for closure-private flags. Implementation plan: `04-PLAN-FORWARD/35-PROJECT-ISOLATION-WAVE.md`. | P1, P3, P8 |
| **C14** | [Legacy Elimination & PRYZM3 Enforcement](./C14-LEGACY-ELIMINATION-AND-PRYZM3-ENFORCEMENT.md) | ⚠ **The census in this row is a frozen 2026-05-16 SNAPSHOT, not the present tense** — it was written as though current and had drifted for three months. As audited then: `packages/` (80), `plugins/` (46), `scripts/` (27), `tools/ga-gate/` (15 gates). **Re-measured 2026-08-11: `packages/` 97 workspaces · `plugins/` 48 · `scripts/` 11 · `tools/ga-gate/` 32 gates** (`ls packages/*/package.json | wc -l`; `ls plugins/ | wc -l`; `ls scripts/ | wc -l`; `ls tools/ga-gate/check-*.ts | wc -l`). Treat the 2026-05-16 figures as historical context for the LP-01–LP-10 catalogue below, never as current state. Catalogues 10 legacy-pattern categories (LP-01–LP-10) found during the 2026-05-16 audit. Classifies every package as COMPLIANT / TRANSITIONAL-ZONE / LEGACY-ZONE. Prohibits all PRYZM1/2 patterns in new code. Maps each pattern to a migration phase. Identifies 5 GA gate gaps (G-NEW-01–G-NEW-05) not yet covered by existing gates. **Key legacy zones**: `@pryzm/ai-host` (44 window-store reads, 20 CustomEvent dispatches), `@pryzm/command-registry` (165 structuredClone undo snapshots, global-bridge.ts), `@pryzm/core-app-model` (92 window accesses, active StoreEventBus), `plugins/annotations` (12 commands using window.xStore fallback). | P4, P5, P6 |
| **C15** | [Hosted Element / Host-Wall Contract](./C15-HOSTED-ELEMENT-CONTRACT.md) | Parametric offset model for doors and windows; opening void geometry lifecycle; wall baseline update dual-write invariant for void correctness; drag constraint (single-axis, HostedElementDragController); OpeningsChildrenMismatch guard; BaselineReversal guard; OTel requirements. **Added**: 2026-05-17, REGRESSION-DIAGNOSIS.md §R2. | P3, P5, P6 |
| **C16** | [Command Authoring Protocol](./C16-COMMAND-AUTHORING-PROTOCOL.md) | The single **front door** for authoring a new command: command anatomy (the two transitional backends — bus handler vs legacy `Command`); the §3 command taxonomy → reference exemplar; the **21** authoring invariants (CA-1…CA-21) and the copy-paste checklist (§10); batch coalescing performance contract (§8); AI-initiated commands (§9). Codifies two **binding doctrines**: **CA-DOCTRINE-L (level-oriented)** — every element command resolves/stamps/registers a `levelId` across the spatial, view, and render-visibility authorities; **CA-DOCTRINE-S (semantic-first)** — the semantic record is canonical and registered before geometry. Substrate contract for the Semantic Design Assistant (§9). **Rev 2, 2026-08-11 — a THIRD doctrine, `CA-DOCTRINE-A` (§5.1, `CA-17`…`CA-21`): a command that reports success MUST have changed AUTHORITATIVE state (render / persist / export), and one that cannot reach it MUST REFUSE with a named reason; `affectedStores` must name the store actually written; registration order is contractual (the bus is first-registration-wins, so a plugin handler silently shadows a later editor bridge); and liveness is proven by an EXECUTED read-back, never declared.** Generalises what C03 §4.9 already said for annotations and C68 §5.a for chat-declared verbs only. CA-8 corrected in place — `produceCommand()` alone is NOT a mutation. §11.1 states, per invariant, the verified gate path and the four named exit conditions (G-CA-A1…A4); §5.1 is CANONICAL, **NOT ACTIVE**. **AS-IS**: OI-057 (post-batch wall-join correct but timing-implicit + untested; plugin-store retains pre-miter baselines); G-CA-L/G-CA-S CI gates pending. **Added**: 2026-05-25. | P5, P6, P7, P8 |
| **C17** | [Batch Creation Catalogue & Panel Binding](./C17-BATCH-CREATION-CATALOGUE-AND-PANEL-BINDING.md) | The single **registry** of batch-creation prompts: (a) the canonical catalogue (§4) of every batch command organised in the live `CREATE › Discipline › System › ⚡Batch › item` form (mirrors `CreatePanelLayout.ts CREATE_CONFIG`), (b) one shared NL **prompt string** per entry driving both the CREATE panel label and the AI prompt (§6), (c) the **panel-binding** rules CB-1…CB-8 (batch leaves are additive, dispatch a C16 batch command via `runBatch`, feasibility-gated with "Phase N" tooltips, scope-resolution in the command not the panel). Defines the batch **scope vocabulary** (§3: on-all-slabs, from-level-to-all-floors, per-room, per-facade, per-compartment, project…). Feasible-today (Phase 1, ✅) rows map to existing commands (`CREATE_WALLS_ON_ALL_SLABS`, `CREATE_SLABS_ON_ALL_FLOORS`, `CREATE_MULTIPLE_LEVELS`, `DUPLICATE_FLOOR_PLAN`, `CREATE_GRID_SYSTEM`, detect-rooms, roof-by-region); ⏳ rows unlock per `SPEC-SEMANTIC-DESIGN-ASSISTANT` phases. **§10–§11 = the AS-IS dispatch reference** (analysed 2026-05-25 before implementation): live path is `commandManager.execute` (Path A, the AIPanel path), per-command class + constructor args + bus-type + scope/selection resolution + preconditions + gaps (G-D1 DELETE_ALL_GRIDS has no command class; G-D2 DuplicateFloorPlan needs a target picker). Governed by C16. **Added**: 2026-05-25. | P6, P8 |
| **C18** | [Element Preview Visual Contract](./C18-ELEMENT-PREVIEW-VISUAL-CONTRACT.md) | The visual appearance of every creation / placement **preview** ("ghost") shown while drawing or placing an element, in all views (3D, plan, elevation/section, split). Unifies preview colour via `PREVIEW_COLOR` (3D) + `PREVIEW_CSS` (2D `<canvas>` handlers + snap markers), records out-of-scope colours (§2.4), preview parity (§5). Companion to C04/C06/C11. **Status**: CANONICAL. **Added**: 2026-05-22. | P4, P6 |
| **C24** | [Sheet Composition Engine](./C24-SHEET-COMPOSITION-ENGINE.md) | Governs the existing `plugins/sheets/` (PRYZM 2 S37 / ADR-0031) plus gap-fill to migrate it under the PRYZM 3 layered model. Codifies invariants for SheetStore, viewports, title blocks, widgets, book/sheet-set, and the rendering pipeline that produces vector output. Audit verdict: **AUDIT + EXTEND** (SheetStore + 11+ handlers + viewport + title-block + view-renderer + 6 widget types + book-exporter IMPLEMENTED; gaps are vector PDF backend, DXF backend, sheet UI in editor, dimension+annotation integration into sheets, section/elevation viewports). **Added**: 2026-05-31. | P2, P3, P5, P6, P8 |
| **C24.1** | [Auto-Documentation Sheets Protocol](./C24.1-AUTO-DOCUMENTATION-SHEETS-PROTOCOL.md) | The AUTO sibling of C24: governs AUTOMATIC generation of a documentation SET (per-level plans · 4 building elevations · per-level set-out plans · per-room cropped plan + interior elevations) auto-placed on numbered sheets, PDF-ready. PURE deterministic decision layer `DS1`–`DS6` (`buildSheetFromViews` · `computeBuildingElevationMarks` · `roomCropRegion`/`computeRoomInteriorElevationMarks` · `set-out` dim mode · `planDocumentationSet`) IMPLEMENTED + tested (ai-host 2082/2082); editor wiring (docSheets executor + `sheet.export.pdf`) pending. Coverage rule (no silent room omission), `A-1xx/2xx/3xx/4xx` numbering, rule-based label placement (force-directed deferred), C34 styles, C29 vector PDF. **Status**: DRAFT 2026-06-09. | P5, P6, P8 |
| **C25** | [IFC Export (Production-Grade)](./C25-IFC-EXPORT-PRODUCTION.md) | Governs the existing `plugins/ifc-export/` (PRYZM 2 Phase 3-B Sprint S56) plus gap-fill to reach production-grade IFC4X3 coverage. Codifies invariants for `IFC4X3Exporter`, per-entity exporters, `IFCMetaStore` round-trip, Pset authoring, spatial structure completeness, classification, COBie. Audit verdict: **AUDIT + EXTEND** (IFC4X3Exporter + 6 element exporters + Pset round-trip IMPLEMENTED; gaps are IfcSite/IfcSpace/IfcZone/IfcFurniture coverage, IfcAnnotation, classification, COBie). **Added**: 2026-05-31. | P5, P8 |
| **C26** | [Revit Round-Trip](./C26-REVIT-ROUND-TRIP.md) | Bi-directional `.rvt`/`.rfa` ↔ `.pryzm` via IFC4 as canonical interchange, plus an optional external Python adapter for Revit-API-specific extensions (phasing / worksets / design options). No `.rvt` parsing in monorepo. Family mapping table, parameter translation via `IfcPropertySet`, level/view/sheet translation, 10-project reference suite for round-trip diff testing. Audit verdict: **GENUINELY NEW** — no Revit code in monorepo. **Added**: 2026-05-31. | P5, P8 |
| **C27** | [BIM 3.0 Inspect Model](./C27-BIM3-INSPECT-MODEL.md) | Spatial-intelligence inspection surface: master tree (Site → Building → Level → Apartment → Room → ElementType → ElementInstance), selection-driven isolation via `packages/visibility/` (P7), graphical dashboards per node type. `InspectSelectionStore` + `IsolationVisibilityIntent` + `SpatialRelationshipResolver` + `IsolationAnimator`. Coexists with `plugins/ifc-inspector/` (becomes element-instance sub-panel) and supersedes the flat `apps/editor/src/ui/PropertyInspector.ts` with documented migration plan. Audit verdict: **GENUINELY NEW** with migration of existing 80-file flat inspector. **Added**: 2026-05-31. | P3, P6, P7, P8 |
| **C28** | [Data Panel & Automation](./C28-DATA-PANEL-AND-AUTOMATION.md) | Live data layer for `check / automate / update / review`. Wraps the existing `plugins/schedules/` (PRYZM 2 S41 / ADR-0032) and adds: (a) unified grid across all element types, (b) quality-rules engine sourcing 266+ rules from the 248+ constraint DB + dimensional G-classes + topology A-classes, (c) bulk-edit commands through commandBus (P6). Tier 1/2/3 rule execution (on-edit / on-save / on-demand). Export to Excel/CSV/JSON/IFC-Pset/SQL. Cron scheduling + email-on-violation. Audit verdict: **AUDIT + EXTEND** (Schedule store + 6 handlers + formula DSL + reactive table + CSV/XLSX IMPLEMENTED; quality-rules + bulk-edit + unified grid are the gaps). **Added**: 2026-05-31. | P5, P6, P8 |
| **C29** | [PDF Vector Export](./C29-PDF-VECTOR-EXPORT.md) | Fills the typed-stub `packages/drawing-primitives/src/backends/pdf.ts` (PRYZM 2 ADR-0029) with a true vector PDF writer via `pdf-lib`. Font embedding, line-weight calibration, PDF/A-3 compliance, optional IFC-embed (single-deliverable PDF + IFC). Print-calibration test harness (1m × 1m validation). Audit verdict: **FILL TYPED STUB**. **Added**: 2026-05-31. | P5, P8 |
| **C30** | [Drawing Set Management](./C30-DRAWING-SET-MANAGEMENT.md) | Aggregates Sheets ([C24](./C24-SHEET-COMPOSITION-ENGINE.md)) into SheetSets with revision tracking, issue register, transmittal package (single PDF/A-3 cover + drawing register + N sheets via [C29](./C29-PDF-VECTOR-EXPORT.md)), automatic sheet numbering. Revision status state machine (`draft → issued → superseded`, one-way). Revision-cloud annotations bound to revision ids. Audit verdict: **AUDIT + EXTEND** (`plugins/sheets/src/book/book-exporter.ts` PRYZM 2 S37 implements multi-sheet composition; revision tracking + issue register + transmittal generator are gaps). **Added**: 2026-05-31. | P6, P8 |
| **C31** | [Documentation Authoring Protocol](./C31-DOCUMENTATION-AUTHORING-PROTOCOL.md) | The binding rules for how every document in `docs/` is written, named, structured, versioned, and superseded. Anatomies for contract / ADR / spec / plan / status / guide / reference. Filename patterns enforced by `check-doc-naming.ts`. Immutability of sealed docs enforced by `check-{adr,contract,snapshot}-immutability.ts`. **Added**: 2026-06-01. | (cross-cutting — all P) |
| **C19** | [Site Model & Parcel](./C19-SITE-MODEL-AND-PARCEL.md) | Site · parcel · building-footprint · context-buildings · zoning. One Site per Project. Site is read-only outside dedicated edit commands. Footprint MUST lie inside parcel minus setbacks. Companion to C12 (which owns coordinate transforms). **Status**: CANONICAL (ratified 2026-07-16). 11 invariants. | P5, P6, P8 |
| **C20** | [Building & Apartment Aggregates](./C20-BUILDING-AND-APARTMENT-AGGREGATES.md) | Building → Level → Apartment → Room hierarchy formalisation. Today single-Building + single-Level apartments (multi-Building deferred to C20.1, multi-Level apartments to C20.2). Parameter propagation via D-α-3. Wraps C13 lifecycle with architectural hierarchy. **Status**: CANONICAL (ratified 2026-07-16). 13 invariants. | P5, P6, P8 |
| **C21** | [Climate Ingestion](./C21-CLIMATE-INGESTION.md) | EPW (priority 1) · NOAA normals (fallback) · solar position (recomputed) · wind rose · temp/humidity/precipitation. Per-site cache keyed at 0.01° rounded lat/lon. Climate is read-only — only ingestion replaces. Companion to C12 + C19. **Status**: CANONICAL (ratified 2026-07-16). 12 invariants. | P5, P8 |
| **C22** | [Privacy & PII Tier](./C22-PRIVACY-AND-PII-TIER.md) | DataTier separation: PII · PROJECT · TELEMETRY · DERIVED. GDPR · CCPA · DSAR ≤ 30 d · right-to-delete ≤ 90 d cascade. BYOK deny-default. Region residency. Breach notification ≤ 72 h. Companion to C08 (auth) + C23 (provenance). **Status**: DRAFT 2026-06-01. 12 invariants. | P5, P6, P8 |
| **C23** | [Provenance & AI Audit](./C23-PROVENANCE-AND-AI-AUDIT.md) | Every AI artefact records model · prompt · context-hash · cost · timestamp · user · project. Provenance graph links AI calls to produced elements. 7-year retention. Append-only. PII redaction in prompts. Reproducibility status (deterministic + seed, or non-deterministic flagged). Companion to C09 (AI cost) + C22 (PII redaction) + C10 (OTel). **Status**: DRAFT 2026-06-01. 13 invariants. | P5, P6, P8 |
| **C32** | [DXF / DWG Round-Trip](./C32-DXF-DWG-ROUND-TRIP.md) | Bi-directional AutoCAD DXF + DWG interchange. ASCII DXF for canonical write; ODA DWG conversion via licensed library. Layer mapping + linetype + block library. Reference suite of 10 third-party DXFs for round-trip diff testing. **Status**: DRAFT 2026-06-01. | P5, P8 |
| **C33** | [Rhino Interchange](./C33-RHINO-INTERCHANGE.md) | 3DM round-trip + Grasshopper bridge (definitions consumable from PRYZM AI host). NURBS preservation through interchange; mesh fallback for non-NURBS-capable consumers. **Status**: DRAFT 2026-06-01. | P5, P8 |
| **C34** | [Print & Drawing Standards](./C34-PRINT-AND-DRAWING-STANDARDS.md) | Sheet sizes · linetypes · scales · north arrow · title-block conventions per AIA / RIBA / DIN / NF / JIS / UNE / ABNT / GB / ISO 19650. Companion to C24 (sheet engine) + C46 (locale defaults). **Status**: DRAFT 2026-06-01. | P5 |
| **C35** | [COBie FM Handover](./C35-COBIE-FM-HANDOVER.md) | Facility-management handover export. Equipment lists · maintenance schedules · asset tagging · Tier-1 IFC + COBie Pset coverage. Companion to C25 (IFC). **Status**: DRAFT 2026-06-01. | P5, P8 |
| **C36** | [Clash Detection & Coordination](./C36-CLASH-DETECTION-AND-COORDINATION.md) | Federated clash detection across multi-discipline models. BCF round-trip companion. Tolerance-based intersection tests. Per-clash severity + workflow assignment. **Status**: DRAFT 2026-06-01. | P5, P8 |
| **C37** | [Schedule / 4D](./C37-SCHEDULE-4D.md) | Construction-phase scheduling export. Gantt visualisation + time-based phasing + animation. Phase model shared with C38. **Status**: DRAFT 2026-06-01. | P5, P6, P8 |
| **C38** | [Cost / 5D](./C38-COST-5D.md) | Quantity takeoff (from C25 Qto Psets) × pricing tables (RSMeans / BCIS / Spon's / custom) → cost estimate. Roll-up per CSI / NRM2 / Uniformat II. Excel / CostX / Bluebeam / SAP exports. **Status**: DRAFT 2026-06-01. 14 invariants. | P5, P6, P8 |
| **C39** | [Pricing & Plan Tiers](./C39-PRICING-AND-PLAN-TIERS.md) | Solo / Studio / Mid-firm / Enterprise tiers · entitlement registry · quota counters · billing state machine · trial / dunning / pause. Every feature gate flows through one resolver. Companion to C40 (developer payouts) + C42 (support SLA). **Status**: DRAFT 2026-06-01. 14 invariants. | P1, P5, P6, P8 |
| **C40** | [Marketplace Economics](./C40-MARKETPLACE-ECONOMICS.md) | Developer 70 % / PRYZM 30 % split · monthly payout via Stripe Connect · 14-day refund reserve · chargeback handling · tax-form gating · review-fraud + payout-laundering detection · established-developer status · curated vs. open categories. Companion to C39 + C07. **Status**: DRAFT 2026-06-01. 15 invariants. | P5, P6, P8 |
| **C41** | [Telemetry & Analytics](./C41-TELEMETRY-AND-ANALYTICS.md) | Three event tiers (Essential / Product / Marketing) · jurisdiction-aware consent defaults · pseudonymous ids via PII bridge · DSAR export + 90-day erasure cascade · retention schedule per tier · sampling for high-volume events. Sibling to C10 (operational spans). **Status**: DRAFT 2026-06-01. 15 invariants. | P5, P6, P8 |
| **C42** | [Customer Support Tier](./C42-CUSTOMER-SUPPORT-TIER.md) | Four channels (in-product / email / priority email / named CSM) · severity-tier SLAs · break-glass PII access with audit · refund/credit authority · SEV-1 post-mortem within 5 days · CSAT + NPS measurement · status page + monthly trust report. **Status**: DRAFT 2026-06-01. 15 invariants. | P5, P6, P8 |
| **C43** | [Accessibility (WCAG 2.2 AA)](./C43-ACCESSIBILITY.md) | Every UI surface meets WCAG 2.2 AA · 3D canvas bounded-responsibility (alternatives via property panel + inspect tree) · keyboard exhaustive · ARIA via single announcer service · reduced-motion + contrast policy · annual external audit + quarterly VPAT · documents carry accessibility metadata. Siblings C44 / C45 / C46. **Status**: DRAFT 2026-06-01. 15 invariants. | P5, P6, P8 |
| **C44** | [Mobile & Tablet](./C44-MOBILE-AND-TABLET.md) | Desktop + large-tablet primary; smaller form-factors are read-only viewer + form-only admin. Four breakpoints, four capability levels per surface. Bounded gesture vocabulary. Share-link viewer works on every form-factor. PWA install (no native app). **Status**: DRAFT 2026-06-01. 15 invariants. | P3, P5, P6, P8 |
| **C45** | [Browser & Device Matrix](./C45-BROWSER-AND-DEVICE-MATRIX.md) | Three browser tiers (fully tested / best-effort / unsupported) · per-version cutoff schedule with 30-day notice · hardware floor (WebGL2 / 8 GB RAM) · WebGPU opt-in rollout · AT compatibility matrix · per-release device-fleet QA · public support page. **Status**: DRAFT 2026-06-01. 15 invariants. | P5, P6, P8 |
| **C46** | [i18n & L10n](./C46-I18N-AND-L10N.md) | Eight TIER 1/2/3 locales · per-app messages/<locale>.json with en-US baseline · Intl-only formatting · RTL via CSS logical properties · architectural-units doctrine (project unitSystem vs. user display preference) · AI host locale-aware · IFC export's locale-aware units · per-locale drawing standards. **Status**: DRAFT 2026-06-01. 15 invariants. | P5, P6, P8 |
| **C47** | [File-Format Versioning](./C47-FILE-FORMAT-VERSIONING.md) | `.pryzm` formatVersion (SemVer) · 24-month backward-compat commitment · MAJOR-boundary migration runners · forward-only-fields tracking · writer signature attestation · per-element schema versioning · pre-migration originals preserved in backup tier. Companion to C05 (file content); C47 = lifecycle. **Status**: DRAFT 2026-06-01. 15 invariants. | P5, P6, P8 |
| **C48** | [Backup & Disaster Recovery](./C48-BACKUP-AND-DR.md) | Three data classes (Project / Billing / Telemetry) with class-specific RPO + RTO · hot / warm / cold tier retention · cross-region replication (sovereignty-bounded) · self-serve version history (90 days) · per-failure-mode runbooks · quarterly DR drill · continuous backup integrity verification. **Status**: DRAFT 2026-06-01. 15 invariants. | P5, P6, P8 |
| **C49** | [Multi-Region & Sovereignty](./C49-MULTI-REGION-AND-SOVEREIGNTY.md) | Four regions (EU / US / AP / UK) · per-org primary region binding · sovereignty-bounded failover (never cross sovereignty) · cross-region access gate (3 documented scenarios: DR / break-glass / migration) · region-scoped auth tokens · customer-initiated migration workflow · trust-page sovereignty section. Sibling to C48. **Status**: DRAFT 2026-06-01. 15 invariants. | P5, P6, P8 |
| **C50** | [Typology Pipeline](./C50-TYPOLOGY-PIPELINE.md) | The multi-typology generative-AI pipeline substrate. `TypologyRegistry` (one per runtime) + 7-stage `PipelineRouter` (brief → site → constraints → generative → validators → cognition → bim-emit). Stage 4 mandatory; others default to no-ops. AI-vs-deterministic engine selection deterministic per input. Plan-tier gate is soft-fail; programmer errors throw. Pure orchestration (no I/O); pack adapters live in apps/editor. Codifies the invariants A.1 implements (`packages/typology-pipeline/`, commit `172fc8c`, 54 tests). **Status**: DRAFT 2026-06-01. 14 invariants. | P1, P5, P6, P8 |
| **C52** | [Editable Building Graph](./C52-EDITABLE-BUILDING-GRAPH.md) | The normative form of [ADR-0061](../adrs/ADR-0061-building-graph-bidirectional-edit-substrate.md). The Living Building Graph as a **bidirectional edit substrate** (tracker A.26, "BIM 2.0/3.0"): a graph-node edit produces a structured PER-NODE layout-constraint delta that re-runs the EXISTING deterministic engine through the EXISTING generate trigger; the regenerated layout re-projects back into the graph. §2 editable attributes (area E1 ✅ · occupancy/type E2 ✅ · adjacency-pref E3 · sun/acoustic E4) each bind to an engine input the layout engine already consumes (no parallel knob). §3 write-path discipline (stash → `gatherLayoutPayload` merge → `triggerApartmentLayout`; P6; no parallel mutator/scorer). §4 invariants I1–I4 (deterministic · per-node override defaults to a no-op ⇒ byte-identical baseline · all mutation via command bus · cause/effect direction). Sibling of C20 (the read-only aggregate hierarchy made editable) + C50 (the engine the edits re-run). **Status**: CANONICAL 2026-06-08. | P1, P5, P6, P8 |
| **C53** | [Generative Layout Engine Architecture](./C53-GENERATIVE-LAYOUT-ENGINE-ARCHITECTURE.md) | The binding AS-IS→TO-BE architecture of the deterministic generative residential layout engine. **L-PRINCIPLE** (merge-blocking): strict separation of **Topological Graph Logic** (dimensionless rooms/adjacencies/program) from **Geometric Solvers** (walls/coordinates) — topology is truth, geometry a pure projection (invariants L1–L4). 6-tier pipeline (boundary → topology → vertical-stack → geometric-solver → emission → evaluation), typed data contract (`SemanticLayoutGraph` topology vs `ParametricBIMElement` geometry), determinism (ADR-0061) + **slider-as-intent** (UI controls modify graph weights, NEVER absolute dimensions — the reactivity-bug cure, §6), 3 deterministic variants (Core-Centric / Perimeter-Spread / Courtyard), Pareto scoring (circulation/acoustic/environmental), 5 CI gates, M0–M9 migration (M0/M1 shipped v69/v70). Governs SPEC-TGL / SPEC-APARTMENT-LAYOUT-GENERATOR / MASTER + the founder SPEC v3.0. **Status**: DRAFT 2026-06-08. | P1, P5, P6, P8 |
| **C51** | [Apex/App Deployment Split](./C51-APEX-APP-DEPLOYMENT-SPLIT.md) | The normative form of [ADR-0255](../adrs/ADR-0255-one-pryzm-cloudflare-supabase.md) §0. ONE codebase (`apps/editor/`) → TWO deploy artifacts: **apex** (`pryzm.so`, static pre-rendered marketing, sub-100 ms first paint, NO auth/DB/PII) + **app** (`app.pryzm.so`, SPA + server, strict CSP, EU region in Phase A, cookies scoped to `Domain=app.pryzm.so`, Supabase Pro PITR for paying customers). Codifies the DNS map (§4), the route table (§5), the build contract (§6: `pnpm build:apex` ≤ 200 KB + `pnpm build:app`), and 5 planned CI gates (§7) so the ADR-0252 drift trap cannot recur. **Status**: CANONICAL 2026-06-02. | P1, P5, P6, P8 |
| **C55** | [Geodata Analytical Layers](./C55-GEODATA-ANALYTICAL-LAYERS.md) | The normative form of [ADR-0065](../adrs/ADR-0065-geodata-analytical-layers-pluggable-provider.md). The Hektar-style **analytical geodata layer subsystem** on the Forma 3D site view — national open-data layers (flood 100/200/max · landslide · slope · terrain shading · soil · ground coverage · property/detail plans · ancient monuments · drainage basins · population per km² · noise · Natura 2000) **draped** over the Cesium terrain + white massing, each a **toggle + opacity slider**, grouped by country, sourced through a **pluggable `GeodataProvider` abstraction** (reference adapter = Sweden Lantmäteriet + SGU; generic OGC fallback). 9 invariants: declarative descriptor (§1.1); layers DRAPE never become BIM (§1.2); pluggable providers — no country hardcoded in core (§1.3); graceful per-layer absence (§1.4); mandatory per-source **attribution** as provenance (§1.5, C23); C22 **PII tier** on population/noise (§1.6); lazy-load + tile + **opacity-without-re-fetch** perf (§1.7); feeds analysis via EXISTING consumers, no parallel objective (§1.8, future flood/landslide-keep-out seam); L1/L2 `geodata-layers` package + renderer-three drape + editor panel via `geodata.*` commands (§1.9). Companion to C19 (Site) + C21 (Climate) + C54 (wind CFD sibling layer) + C12 (coords); cross-refs C22/C23/C45/C49. **Status**: DRAFT 2026-06-09. | P2, P3, P4, P5, P6, P8 |
| **C56** | [AutoDimension Engine](./C56-AUTODIMENSION.md) | The normative form of [ADR-0118](../adrs/ADR-0118-autodimension-engine.md). PRYZM's **deterministic AutoDimension engine** — the missing intelligence layer between the existing output half (`DimensionString` schema + `produceDimensions` + `evaluateDimensions` + plan-view render loop) and input half (`JunctionResolverV2` graph + `PlanarTopologyEngine` perimeter/room rings + `WallOccupancyStore` opening spans). A new **L2 PURE** package `@pryzm/auto-dimension` runs a deterministic **8-stage pipeline** (connectivity graph → segmentation → opening analysis → chain planning → chain geometry → placement → conflict resolution → QA) and returns a complete, deduplicated, stacked, QA'd `DimensionString[]`. 8 invariants: same geometry → byte-identical output, **NO AI/ML/LLM** (§1.1); the 8-stage pipeline + string-planner registry (§1.2); completeness DI-1…DI-6 — overall dims exist, every opening located+sized, every wall break ticked, **never dimension the same distance twice**, no avoidable overlap/crossing, QA rejects incomplete docs (§1.3); package boundary — L2 pure (no THREE/DOM/I-O/RNG), consumes L0 `@pryzm/schemas` + pure wall-graph algorithms, returns `DimensionString[]`, editor executor the only impure surface dispatching via command bus (P6) in one `runBatch` for single-undo (§1.4); rule-based placement, v1 overlap OK but RNG never (§1.5); determinism mechanics — stable ordering + ULID tie-breaks per ADR-0061 (§1.6); OTel span per exported fn (§1.7, P8); layered placement (§1.8). Companion to C24.1 (this is its missing dimension provider) + C34 (injected styles); cross-refs C03/C11/C15/C24 + ADR-0055/0061. **Status**: DRAFT 2026-07-06 (L-138). | P2, P4, P5, P6, P8 |
| **C54** | [In-Browser Wind CFD](./C54-IN-BROWSER-WIND-CFD.md) | The normative form of [ADR-0064](../adrs/ADR-0064-in-browser-wind-cfd-webgpu-lbm.md). An in-browser pedestrian-wind / wind-comfort solver — a WebGPU Lattice-Boltzmann (LBM) flow simulation around the site's buildings, client-side (no cloud/queue), tens of seconds, for early-stage comfort assessment (recirculation, roof-edge separation, corner accelerations). 8 invariants: the **honesty rule** (BETA + sheltered-zone calibration status MUST be surfaced, §1.1); WebGPU compute boundary with graceful no-WebGPU soft-fallback to the wind-rose overlay (§1.2); determinism per config (§1.3); **AIJ benchmark validation as a CI release gate** (Case-B r ≥ committed ≈0.84, §1.4); domain/inlet from the C19 Site + C21 wind rose, not hand-authored (§1.5); results refine the EXISTING wind driver / E.4 `naturalVentilation` input, no parallel objective (§1.6); tens-of-seconds perf + client-side privacy (§1.7); new L1/L2 compute package rendered via renderer-three, launched via `windcfd.*` commands (§1.8). Companion to C19 (Site) + C21 (Climate) + C12 (geospatial); cross-references SPEC-ENVIRONMENTAL-DESIGN-DRIVERS §3/§5 + SPEC-FORMA-SITE-VIEW §6. **Status**: DRAFT 2026-06-09. | P2, P3, P5, P6, P8 |
| **C57** | [Parcel Data Layer](./C57-PARCEL-DATA-LAYER.md) | Governs the parcel-data subsystem — provider-agnostic ingestion of real cadastral **geometry + attributes** for a user-selected plot. 10 invariants: **GeoJSON/WGS84 is the one canonical interchange** (§1.1); all upstream access via the same-origin proxy, **keys server-side only, never client/repo** (§1.2); a selected parcel commits down C19's **identical one-shot immutable** `site.parcel-boundary-set` path (§1.3); mandatory provenance (source/version/CRS/license) per fetched parcel (§1.4); **coverage-miss degrades honestly to draw — providers never throw** (§1.5); EPSG handled at the edge via the single C12 `proj4` projector (§1.6); pure fetch+parse L2 `@pryzm/site-parcel-data` (§1.7); `pryzm.parcel.<verb>` OTel span (§1.8, P8); mandatory attribution (§1.9); owns geometry+attributes only, not zoning/site (§1.10). The `ParcelProvider` interface + `CatastroParcelProvider` (Spain, keyless) ship today (`apps/editor/src/ui/site/parcel/` + `server/parcelZoningProxy.js`); DK/Matriklen (keyed) + CH/EGRID are proposed adapters. Companion to [C58](./C58-ZONING-RULES-AND-BUILDABLE-ENVELOPE.md); fills C19 §9/§10.2 (jointly with C58). **Status**: DRAFT 2026-07-17 (L-398/L-400/L-403). | P1, P4, P5, P8 |
| **C59** | [Multi-Pane View System](./C59-MULTI-PANE-VIEW-SYSTEM.md) | The **renderer-agnostic view-hosting** contract — panes (left/right, extensible to N) that host **any** view (MapLibre 2D site map · Cesium/Forma 3D Site · BIM WebGPU 3D · BIM Canvas2D plan · elevations) with the user able to **assign/swap any view into any pane, natively**. Replaces the three mutually-incompatible legacy owners (the `SplitViewManager` fixed Canvas2D right pane; Cesium hard-targeting `#container`; the MapLibre `inset:0` overlay) with a **view-type registry** + **pane hosts** that re-target the ONE Cesium viewer / ONE WebGPU device into a pane element (never a second instance). Invariants: **one instance per singleton renderer** (a singleton view MOVES between panes, never clones) (§2.1); **single rAF — panes subscribe to the composition-root frame bus** (§2.2, P3); **command-driven pane assignment** (§2.3, P6); no `window as any` (§2.4, P4); WebGL-fallback perf budget (§2.5); renderer-agnostic extensibility (§2.6). Pure layout algebra (`paneViewModel.ts`: assign/swap/validate) is Phase 1a (LANDED + unit-tested); the live `PaneHost` wiring is phased (1b 3D-Site-in-either-pane → 2 registry switcher, absorbs L-405 → 3 full swap + N-up + per-pane camera → 4 consolidation). Grounds C06 §8; subsumes historical "Contract 17 §4 split view". SPEC-MULTI-PANE-VIEW-SYSTEM + audit **L-412**. **Status**: CANONICAL 2026-07-18 (Phase 1a landed). | P1, P2, P3, P4, P6, P8 |
| **C58** | [Zoning Rules & Buildable Envelope](./C58-ZONING-RULES-AND-BUILDABLE-ENVELOPE.md) | The **core compliance value-prop** contract — the `ZoningProvider` adapters + the deterministic `ZoningRulesEngine` that solves `parcel + zoning-rules → BuildableEnvelope` (setback inset polygon + max height + FAR + coverage + permitted use + a derivation trace). 10 invariants: **deterministic, NO AI/ML/LLM on the envelope path** (§1.1); **two input fidelities — `structured` / `estimated-ruleset` / `none` — with a mandatory confidence label** (§1.2); every constraint **cites its source rule (`DerivationTrace` "explain-why")** (§1.3); **an estimated envelope is NEVER shown authoritative — CI fidelity-label gate** (§1.4, L-373); **jurisdiction-agnostic core + per-jurisdiction adapters, nothing hardcoded** (§1.5); curated, versioned, **per-field-provenance** `JurisdictionZoningContract` rule packs — the honest estimated-data flag (§1.6); output maps 1:1 onto C19 mutable fields via `site.updateZoning`, no new output schema (§1.7); **the envelope constrains generation** (inset + maxHeight → C50/C53 generators, slider-as-intent, no parallel knob) (§1.8); pure L2 engine, no THREE/DOM/I-O/RNG (§1.9); `pryzm.zoning.<verb>` span (§1.10, P8). **0% built today** (gap audit G-ENG-1). **Fills the C19 §9/§10.2 deferred jurisdiction registry.** Denmark-first (cleanest structured zoning). Companion to [C57](./C57-PARCEL-DATA-LAYER.md); ties C19 §1.4/§1.6, C50, C53; SPEC-COMPLIANCE-REPORT + [ADR-0269](../adrs/ADR-0269-compliance-authoring-parcel-zoning-envelope-strategy.md). **Status**: DRAFT 2026-07-17 (L-398/L-399/L-401/L-402). | P1, P5, P6, P8 |
| **C60** | [Site Entry & Jurisdiction Coverage](./C60-SITE-ENTRY-AND-JURISDICTION-COVERAGE.md) | How a user **ARRIVES** at a site — the `world → country → city → parcel` globe entry navigation — **and** the honest, engine-derived answer to *"where can PRYZM actually answer?"*. One contract because a navigation surface that offers a place is a claim about that place. Fills the `MISSING-CONTRACTS-AUDIT` gap "no contract owns the SITE-ENTRY / onboarding navigation flow" — the condition under which the L-593 globe would have become a **fourth** ad-hoc entry mechanism (the same disease C59 §0 cures one layer up). 10 invariants: the stage machine is a **pure reducer, and an altitude listener that assigns stage is a VIOLATION** (§1.1 — it flaps at band edges, has nowhere to put the coverage answer, and inverts cause and effect); **coverage is DERIVED FROM THE RULE-PACK REGISTRY — a hand-maintained coverage polygon/city list/country table is forbidden** (§2, the core invariant: the extent and predicate ARE `BARCELONA_BBOX`/`isInBarcelona`, the objects `siteDispatch` routes on, asserted by identity); **no pre-site stage writes site state — exactly one intent may hand off** (§4.1, C19 §1.3/§1.4 one-shot boundary); **no invented facts about places, and "not covered" is a statement about PRYZM, never about the law** (§3, C58 §1.4 at the navigation layer); ONE Cesium instance — the globe and the 3D Site are the same viewer at different altitudes (§6.5, C59 §2.1); single rAF (§6.6, P3); command-driven transitions where a rejection mutates nothing including the camera (§6.7, P6); **the entry flow holds no live BIM pane behind it** on the WebGL fallback (§6.8, C59 §2.5); no `window as any` (§6.9, P4); it is a **STATE of the `site-3d` view, not a new view mechanism** (§6.10). Founder decision **(A) the honest coverage globe** — Barcelona lit, elsewhere explicitly "not yet covered"; **(B) the open globe is a CONFIGURATION VALUE, not a fork** (§5 — `SiteEntryMode`, branched on in exactly two places). Depends on [C59](./C59-MULTI-PANE-VIEW-SYSTEM.md); ties C19 §1.3/§1.4, C57 §3.1, C58 §1.4/§1.5, C12, C06 §7/§233. Audit **L-593**; `SPIKE-L592-L593-*.md`. **Status**: DRAFT 2026-07-22 (Phase 1 implemented — pure machine + registry-derived coverage + store + panel; NOT yet wired into the shipping entry, sequenced behind C59 Phase 3). | P3, P4, P6, P8 |
| **C62** | [Data Confidence, Provenance & Unknown-Reason Model](./C62-DATA-CONFIDENCE-PROVENANCE-MODEL.md) | The ONE cross-cutting vocabulary for **how trustworthy a piece of data is** — a typed `UnknownReason`, an ordered `AuthorityRank`, a `ValidationState` lifecycle, a per-domain `DomainConfidence`, and a generic `MetadataEnvelope<T>` wrapper — that every subsystem (C57 parcel, C58 envelope, C55 layers, ADR-0277 heights/terrain, C63 completion) **inherits** instead of reinventing. Structural home of §CONTEXT-DATA-HONESTY: unknown stays explicitly unknown with a typed reason; a value is never fabricated to fill a slot. Schema shipped in `packages/schemas/src/site/metadata/`. Ratified by [ADR-0280](../adrs/ADR-0280-data-confidence-provenance-unknown-reason-model.md). **Status**: ACTIVE 2026-07-29 (schema shipped + exported; consumer migration sequenced). | P5, P8 |
| **C63** | [City Completion Scorecard & Dossier Standard](./C63-CITY-COMPLETION-AND-DOSSIER.md) | The ONE way PRYZM answers *"how complete is city X, across every replication layer?"* — a **7-axis, 0–100 % completion scorecard** (PARCEL · LEGISLATION · DATA-SOURCES · ENVELOPE · TERRAIN · HEIGHTS/LOD · CONTEXT) that is a **total function of data/bake/registry state** (never hand-typed; §1.1), each axis a [C62](./C62-DATA-CONFIDENCE-PROVENANCE-MODEL.md) `DomainConfidence` instance with `not-assessed` + a typed `UnknownReason` when unmeasured (§1.2). Plus the **OVERALL** number (a config weight vector `CITY_COMPLETION_WEIGHTS` — the default is a **FOUNDER DECISION**, §4) and the **fixed per-city dossier folder shape** (composite master `RATE.md` + LEGISLATION-RATE/LOD-RATE/NEXT/ENVELOPE/HEIGHT/RISK/sources; templates `jurisdictions/_TEMPLATE/` + `_TEMPLATE/_CITY/` + `NAMING-CONVENTION.md`, §5). Folds in the axes of `CITY-REPLICATION-STANDARD` + `ENVELOPE-REPLICATION-STANDARD` (ADR-0279) + `BUILDING-HEIGHT-REPLICATION-STANDARD`. Honesty companion (§3.1): a city can be 0 % complete and 100 % honest (all cited refusals); launch-blocking is `honestyOk`, not a completion threshold. Ratified by [ADR-0281](../adrs/ADR-0281-city-completion-scorecard-and-dossier-standard.md); [SPEC-CITY-COMPLETION-SCORECARD](../../03-execution/specs/SPEC-CITY-COMPLETION-SCORECARD.md). Companion to C57/C58/C60/C62/C12. **Status**: DRAFT 2026-07-30 (governance authored; scorecard schema/function + CI gate sequenced; weighting awaits founder sign-off). | P5, P8 |
| **C64** | [The Envelope Compiler](./C64-ENVELOPE-COMPILER.md) | The **cross-cutting owner** of *"given any parcel, resolve the minimum set of authoritative variables required to construct a legally defensible 3D buildable envelope, or return a typed determination naming what is missing."* **Sits ABOVE [C58](./C58-ZONING-RULES-AND-BUILDABLE-ENVELOPE.md) and does not replace it** — C58 remains the authority from the resolved rule set inward. Owns: the **nine-layer model** (0 parcel context · 1 legal stack · 2 variable dependency graph · 3 variable resolution · 4 dataset resolver · 5 provenance · 6 constraint composition · 7 envelope synthesis · 8 explainability), each with purpose/inputs/outputs/interface and a **pointer** to the register that measures its status; the **universal variable-resolution order** (Stage-0 gate ▸ *is a method prescribed?* ▸ published value ▸ published geometry ▸ published annotation ▸ referenced instrument ▸ legally constructible ▸ typed unknown); and the **determination taxonomy** — every non-envelope outcome terminates in exactly one of `legal-impossibility · missing-authoritative-data · external-authority-required · engineering-capability-missing · awaiting-legal-interpretation`, and the five MUST sum to 100 % of non-envelope land. 13 invariants, each **citing** its ADR and not re-arguing it ([ADR-0283](../adrs/ADR-0283-authoritative-publication-bounds-knowledge-unknown-is-valid.md)–[ADR-0291](../adrs/ADR-0291-pryzm-envelope-compiler-variable-first-architecture.md) + the binding blocker/evidence/decision standards), including **§2.13 — no measured figure may be transcribed into a governed document; cite the run that computes it**. Ratified by [ADR-0291](../adrs/ADR-0291-pryzm-envelope-compiler-variable-first-architecture.md); specced by [SPEC-ENVELOPE-COMPILER-PROGRAMME](../../03-execution/specs/SPEC-ENVELOPE-COMPILER-PROGRAMME.md). **Status**: DRAFT 2026-08-02 (governance authored; compiler layers 2/3/4 unbuilt — §9). | P1, P5, P6, P8 |
| **C65** | [Element Type System](./C65-ELEMENT-TYPE-SYSTEM.md) | Owns **what an element TYPE is, where it lives, how it is created, and how it survives a save** — the wall layer stack, the door leaf/frame, the window operation, the bed's dimensions. Does NOT own instance creation ([C11](./C11-ELEMENT-CREATION-PIPELINE.md)) or drawing ([C09 §4](./C09-AI-AND-VISIBILITY-INTENT.md)). Declares **four tiers** (T1 built-in · T2 **project** · T3 user library · T4 marketplace) and makes **T2 mandatory**: a user-created type is PROJECT state, written by `ProjectSerializer` and restored by `ProjectLoader` — not a runtime store, not localStorage. ⚠ Its load-bearing rule is **§3.4 — a MISSING type is an explicit visible state, NEVER a silent default**, because a silent default makes *"your type was lost"* and *"this is a default wall"* the same value (the §CONTEXT-DATA-HONESTY failure of L-716, L-752 and L-779). Also binds: creation through COMMANDS with working inverses (§3.2, P6/[C16](./C16-COMMAND-AUTHORING-PROTOCOL.md)); the store DECLARED in `declaredProjectScopes.ts` or it leaks across projects (§3.3, [C13](./C13-PROJECT-LIFECYCLE-AND-ISOLATION.md) §3.10/§3.11); **ONE abstraction, not N stores** (§3.5 — N divergent stores produced three disagreeing plan-limit tables, two typology id lists and two copies of one style resolver in a single week); type edits propagating to every instance with the count stated first (§3.6, Revit semantics); the layer editor deriving total thickness FROM the layers (§3.8); and **§3.9 — no "New type" affordance without a wired implementation**. Verification (§5) requires a **persistence round-trip test** and an **unresolved-type honesty test**, each with a POSITIVE CONTROL. Established by [ADR-0309](../adrs/ADR-0309-element-types-are-project-state.md). **Status**: ACTIVE 2026-08-09 (governance authored; **§4 AS-IS deliberately PENDING** the per-family EXISTS·WIRED·PERSISTED audit — recorded, not inferred). | P5, P6 |
| **C66** | [Concurrency & Scale](./C66-CONCURRENCY-AND-SCALE.md) | Owns **how many users PRYZM serves at once, what each instance may hold, and what must be true before a second instance exists.** The orthogonal axis to [C10](./C10-PERFORMANCE-AND-OBSERVABILITY.md): C10 asks *"how fast for one user"* (19 client NFTs), C66 asks *"how many users at once"* — and **nothing owned that question**, which is why the most load-bearing capacity statement in the repository was a comment in `fly.toml` (*"Tuned for ~50 concurrent live editors"*) and why L-770 could sit at *"acceptable for a closed beta"* without anyone having to say how big the beta was. Binds: **§1 a tier is HELD only when a k6 run passes; otherwise it is CLAIMED, and a CLAIMED tier MUST NOT be described as supported** (§CONTEXT-DATA-HONESTY applied to capacity); **§2.2 the four coupled numbers** — pool × instances vs the Supabase connection limit, body-limit × concurrent writes vs instance memory (**at 50 MB and 512 MB, ten concurrent saves exceed total RAM before Postgres is reached**), and the single-threaded CPU; **§3.1 a second instance MUST NOT start until every category-A per-process state is shared** — scale-out today is a *correctness regression*, not a capacity gain, because Socket.io rooms partition **silently**; **§3.3 the measured per-process state inventory** (11 correctness-critical, ~15 benign caches, 3 throttle windows) which surfaced two defects the audit missed — `exportGuard`'s single-use token Map means **exports fail (N−1)/N of the time at N instances**, and `manualAdminZoneStore`'s per-process sequence counter **collides ids across instances**; **§4 one owner per datum** (undo history is split three ways, `project_members` is never read, `version_count` is maintained three ways); **§5 refuse honestly** — no production path may substitute volatile for durable storage (the L-789 defect), and a refusal propagates its SQL state unwrapped so the existing classifiers can map it; **§6 the load-test contract**, incl. **§6.4 `STAGE=target` MUST NOT hit production while one instance serves it**. §7 lists the seven structural blockers against the Scale tier. Origin: [production-readiness-1000-users-2026-08-09](../../03-execution/analysis/production-readiness-1000-users-2026-08-09.md); remediation in [SCALE-REMEDIATION-PROGRAM](../../03-execution/plans/SCALE-REMEDIATION-PROGRAM.md). **Status**: CANONICAL 2026-08-09 (**§1 all three tiers CLAIMED, none HELD** — the harness is authored and has never been run; that is recorded, not inferred). | P8 |
| **C67** | [RAC: The Natural-Language Capability Control Plane](./C67-RAC-CAPABILITY-CONTROL-PLANE.md) | Owns **what the chat IS** — the resolution ladder, the capability registry, the honesty invariants, and the roadmap. **§0**: *the AI layer is never the source of truth for what the editor can do; the editor registers capabilities, language resolves against them, and the LLM is an escalation mechanism, not the command router.* **§1 AS-IS, measured** (re-measured at `f5f3a5a1`, 2026-08-11): 319 registered bus commands · **45 capabilities** covering 31 commands · 51 `CHAT_UNAVAILABLE` · 237 classified · **undeclared 0/0** · 138 declared examples EXECUTED through the real ladder. **§1.3 the generator pattern** — a capability of a known shape is a TABLE ROW, and four tables (`CapabilityExecutionSpec` · `CatalogueFamilies` · `PropertyVocabulary` · `DeleteFamilies`) generate the execution spec **and** the tier-0 grammar; U7.3 added four properties with zero changed lines in the resolver. **§1.4 the scope algebra** — base scopes (selection · ids · all · level · room · orientation) with FILTER scopes that WRAP a base, lifted out of the sentence by a pre-stripper so composition is free and no capability regex learned a filter. **§1.5** the compound-plan executor (one Confirm card, ordered dispatch, **undo cost stated as the SUM of its steps** — `runBatch` is undo-NEUTRAL per ADR-0314) and the **LLM as the LAST rung**, its vocabulary GENERATED from the registry, its schema intents-only, its validation rejecting-never-coercing, its zero-token guarantee pinned by test. **§1.6 the OPEN defects, named** — 29 pinned MISREAD phrasings including a destructive one (*"highlight walls taller than 3m"* resizes a wall), `create-wall`'s unclassified route, and the `beam.height` panel lie. **§4** the eleven binding rules (P6 absolutism · one semantic front door · two proofs per target · registry as only source of chat truth · known shapes are table rows · honesty invariants · **claiming discipline**, from L-823 · truthful undo cost · shrink-only gates · no fictional capabilities · determinism before models). **§5** the U0–U10 roadmap — **U9 is IN FLIGHT and not claimed.** Procedural expansion: [C68](./C68-ELEMENT-CHAT-ONBOARDING.md). Implementation decisions: ADR-0313 / ADR-0314 / [ADR-0315](../adrs/ADR-0315-universal-capability-architecture.md). **Status**: CANONICAL 2026-08-11 (rev 3). | P6, P8 |
| **C68** | [Element & Attribute Chat Onboarding](./C68-ELEMENT-CHAT-ONBOARDING.md) | Owns **what a new element type, or a new attribute on an existing element, OWES the chat before it can merge.** The procedural expansion of [C67](./C67-RAC-CAPABILITY-CONTROL-PLANE.md) §6 (which states the obligation in five lines and stops); C67 owns *what the chat is*, C68 owns *the moment of arrival*. **§1 the rule**: a new element or attribute is NOT DONE until the chat can either REACH it or REFUSE it out loud with a stated reason — and which of the two is true is decided by a CI gate, never by memory. **§2 why**: the `c1902a5a` defect (the batch command and the chat panel shipped in the SAME release; the founder typed *"make all walls interior partition"* and got *"I'm not sure how to help with that yet"* — and it RECURRED on 2026-08-10 with *"Create 3 bedroom apparment"*), and the `ElementCapabilities.ts` counter-example (Mirror/Offset/Scale advertised on seven wall-only families — **a capability table that lies is worse than none**, because a blank table produces an honest refusal while a lying one produces a confident "Done" over a mutation that never happened). **§5 the ten-item checklist** (rev 3 added **j**), each naming its file and its proof: (a) a LIVE command route — plugin `produceCommand` DTO stores are **presumed DEAD** (13/13 so far, L-620/L-815) — plus a BATCH command for mass edits since `runBatch` is undo-NEUTRAL (ADR-0314); (b) a declaration in exactly one of three disjoint places (capability · `CHAT_UNAVAILABLE` · classification with a falsifiable reason); (c) `targets` proven BOTH ways (executable probe across 16 kinds, exact set equality both directions + source-anchored `commandProof`); (d) every parameter naming a `valueSource`, resolved by the ONE table for its vocabulary (`colorRef` / `finishRef` / `resolveCatalogueRef`); (e) `scopeModes` only for scopes the resolver honours; (f) natural acceptance phrasings + adversarial pins (`descriptiveReportReason`, `Normalized.corrected`, `PROTECTED_FUNCTION_WORDS` — report text may never command, typo correction may never manufacture an imperative); (g) refusals quoting real values and honest partial outcomes; (h) **for catalogued families with no catalogue, AUTHOR one** (founder ruling 2026-08-11) on the `WallSystemTypeStore` shape — built-ins protected, layers with material + thickness, derived totals recomputed, project-scoped per C65 §3.3/C13; (i) batch-shaped capabilities land as **U4 SPEC TABLE ENTRIES** with zero new resolver case code. **§6 enforcement**: the per-item map onto GA gate 31 (`check-chat-capability-coverage.ts`) with its verbatim failure text, plus the shrink-only ratchets (**undeclared 0/0**; SDK-bypass 172) — (j) **CLAIMING DISCIPLINE (rev 3)** — a new grammar must be pinned against the misread corpus: a visibility/query opener (`highlight` · `isolate` · `hide` · `how many`) may **never** reach a mutation; a COUNT may never be read as a DIMENSION; a catalogue family must resolve its ref at PARSE time rather than swallowing a colour or dimension tail and printing a summary about a type that does not exist. **§6 enforcement**: the per-item map onto GA gate 31 (`check-chat-capability-coverage.ts`) with its verbatim failure text, plus the shrink-only ratchets (**undeclared 0/0**; SDK-bypass 172) — **and §6.3 keeps the honest ledger of what is NOT enforced.** Of the nine gaps it shipped with, `bccf08dc` closed five outright with **eight new checks** (4b example EXECUTION · 4c adversarial corpus · 3e global route liveness · 5c scope modes HONOURED · 6 one-dispatch + honest report · 7 catalogue source · 8 case-arm ratchet · 9 property surface), three are half-enforced with the unprovable (runtime) half named, and **G9 — ADR-0315 cited everywhere with no file — is CLOSED** ([ADR-0315](../adrs/ADR-0315-universal-capability-architecture.md), `147b94ab`). **Rev 3 appends G10**: §5.j has no *general* gate, and unlike the other open items it has a **known live destructive instance**. **§8** walks `set-door-type` (U4.3, ~94 lines of metadata, zero resolver code) through the checklist. **Status**: CANONICAL 2026-08-11 rev 3 (**not ACTIVE** — refusal quality, claiming discipline, the two runtime halves and six ratchets still stand between it and certification). | P6, P8 |
| **C69** | [The API Verb Register](./C69-API-VERB-REGISTER.md) | Owns **the enumeration of every command type the CommandBus accepts** — the wire identifiers — and the binding rule that the enumeration is **GENERATED from the handler sources, never transcribed**. C03 owns what a command *is*, C16 owns how one is *written*, C08 owns whether it *replicates*, C67/C68 own whether the *chat* can reach it; **nothing owned the list itself**, so the only answers to *"what verbs does this product have?"* were two CI gates that disagree with each other and with the code. **§0**: the founder asked for a contractual document; a hand-maintained one was refused on the evidence of this repo's own week — CLAUDE.md's `C01–C15` against a real `C01–C68`, four rival NFT target sets, three numbers for two invariants inside the file the strategy docs call *"the authority"*, and a capability count reading 41 in four documents while the gate measured 45. **§0.1 MUST**: no document may transcribe the verb list, the count, or any per-verb column — cite [`docs/04-reference/API-VERB-REGISTER.md`](../../04-reference/API-VERB-REGISTER.md), which is generated (C64 §2.13 applied to the command surface; **this contract states no counts of its own**). **§1.1 MUST**: a verb is a **wire identifier, not a label** — it is written into `project_command_log` and replayed in collaboration history, so **renaming one is a persistence-breaking change** governed by C47, not a refactor. **§1.2**: `CommandBus.register()` throws on a duplicate, so `initBusHandlers`' `if (registry.has(spec.type)) continue;` guard converts a loud boot error into a **silent SHADOWING** — first registration wins and the loser is dead code that logs nothing. **§2** fixes the seven columns, each derived from source: verb · owner · **liveness** · **authoritative store** · undo · sync (cited from `syncDisposition.ts`) · chat (cited from the C67/C68 registries). **§2.1** the four-value liveness vocabulary — `LIVE` (execution-authority root or legacy bridge) · `REFUSES` (`canExecute` cannot return valid, §FIX-DEAD-VERB-REFUSE) · `SHADOWED` (§1.2) · `UNKNOWN` (a lone plugin `produceCommand` handler — presumed dead 13/13 times, and still not *proven*, so the honest value is UNKNOWN). ⚠ **§2.2 MUST NOT**: UNKNOWN may never render blank or default to LIVE — **failure and emptiness are never the same value**, and an empty *authoritative store* cell is exactly what let seventeen verbs report success while writing a store nothing renders, persists or exports. **§2.3**: LIVE is not a quality claim (C66 §1.1). **§3.3 MUST**: adding/renaming/removing a bus command **requires regenerating the register in the same change** — the founder's *"whenever a new one is added it should be added there"*, made mechanical. **§3.4**: SHADOWED and UNKNOWN baselines are **named shrink-only lists checked in both directions**, never counts. **§4**: C69 takes the UNION of the two existing gates' subjects and treats their disagreement as a finding rather than a tie to break. **§5 the first output, all open**: **§5.1** `check-sync-disposition.ts` reports *"every property-mutation command type declares its sync disposition"* over a handler set its regex reduces to a small minority of the real one — it matches only the object-literal form, and most handlers are classes; **§5.2** the SHADOWED family (`roof.update` is not alone); **§5.3** element-kind discriminants (`floor`, `pool`, `door`, `rectangular`, `sitsOn`) counted as verbs by both existing gates; **§5.4** `cube.move`, registered outside `src/handlers/` and therefore outside the chat gate's *"UNDECLARED: 0"*. **§6** the gate (`tools/ga-gate/check-verb-register.ts`) — V0 subject floor at **exit 2, never aliased to 1** · V1 symmetry **both directions** · V2 no drift · V3 no blank cells · V4 named ratchets — negative-tested four ways with the failure text recorded, and **§6.1 names the four things it cannot see**. **Status**: CANONICAL 2026-08-11. | P6, P8 |
| **C70** | [BIM 3.0 Target & Golden Chain](./C70-BIM30-TARGET-AND-GOLDEN-CHAIN.md) | Owns **what BIM 3.0 IS as a testable claim** — the twelve capability pillars as numbered invariants, the **Golden Chain** (intent → command → authoritative state → geometry → topology → graph → propagation → persistence → undo/redo → collaboration → report) with its **no-partial-credit rule**, the **maturity ladder L4–L8**, the **Definition of Done**, and the **four-exit-code contract** every BIM 3.0 gate must obey (`0` clean · `1` declared · `2` MISCONFIGURED **never absorbable** · `3` RATCHET EXCEEDED **never absorbable**) with a minimum-evidence floor. **§0** argues the mechanism from this repo's own measured failures, each with its closing commit: ~15 green-and-blind gates; a per-package compile gate that fabricated **~90 PASS lines per run for its entire life** (`2b1e7e99`, which then surfaced 27 packages failing isolated compilation); a certification whose **EMPTY SEED outscored every real run** (`e5addac8`, now exit 2); `PlanegcsAdapter` delegating 100% to `MockSolver` with 31/33 tests testing the mock; four typed cascade events with zero listeners. **§0.1**: BY-READ is never an award. **§2.2**: UNPROVEN is neither pass nor fail. **§4.2**: machinery-present ≠ capability-reachable. **§7** maps ten gates to invariants — and records as a **finding** that seven of the ten have no file at HEAD. Evidence appendices: `BIM30-TARGET-DEFINITION.md`, `BIM30-CAPABILITY-MODEL.md` — **both deleted in the 2026-08-15 corpus collapse and citable from git history only**; the live plan is [`BIM30-IMPLEMENTATION-ROADMAP.md`](../../03-execution/plans/BIM30-IMPLEMENTATION-ROADMAP.md) and the live measured state is [`BIM30-MASTER-COMPLETION-TRACKER.md`](../../03-execution/plans/BIM30-MASTER-COMPLETION-TRACKER.md). **Status**: CANONICAL 2026-08-12. | P1, P6, P8 |
| **C71** | [Graph & Topology](./C71-GRAPH-AND-TOPOLOGY.md) | Owns **the relationship vocabulary and its per-edge semantics.** **§1 the six required semantics** — writer · reader · persistence · rebuild disposition · invalidation · deletion; **an edge without all six is a declaration, not a capability**, and an untyped enumeration does not count as a reader. **§2** the **9 REQUIRED** families (each with a demonstrated consumer) and the **12 PARKED**; *parked ≠ gap*; parked types may not be deleted (snapshot v3 deserialization); **unparking requires a named CONSUMER — writer-first is forbidden**; a new type lands with writer + reader + rebuild disposition + delete behaviour in ONE PR. **§3** `joinedTo` per [ADR-0321](../adrs/ADR-0321-wall-connectivity-is-joinedTo-not-connectedTo.md) — wall↔wall from the retained junction index, `connectedTo` stays room↔room, remove-and-re-emit at flush, never `WallJunctionRecord.id` as a key — with **§3.7 recording it as DECIDED-BUT-UNLANDED** (absent from `RelationshipType` at HEAD, verified). **§4** the three graphs **stay separate**; merging is forbidden (it would break snapshot v3 for a naming preference); the UBG's vocabulary is the canonical **query** vocabulary and the others map onto it. **§0** argues from four measured failures: 12 of 25 types with zero writers *and* zero readers; `sitsOn` **write-only** (a writer census graded it healthy); `contains` **read-only**; and the dirty-tree reader census whose correction found `HierarchyTreePanel` calling `getEdgesFromNode`, **a method that has never existed — so the Furniture group has never rendered**. **§5** the dated measurements: two byte-identical `_rebuildSemanticGraph` copies regenerating **5 of 25** types, leaving five families **persist-or-lose with NO gate**; `CreateWallCommand` writes zero edges; `3ee632f6` as the delete/verbatim-restore reference shape. Subordinate to [ADR-0320](../adrs/ADR-0320-relationship-vocabulary-scoped-by-consumers.md) and ADR-0321. **Status**: CANONICAL 2026-08-12. | P6, P8 |
| **C72** | [Propagation & prevState](./C72-PROPAGATION-AND-PREVSTATE.md) | Owns **how a change reaches everything that depends on it.** **§1** the two-arm definition: propagation requires **both** a listener **and** an emitter carrying `prevState` — either arm alone is theatre. **§0** argues from measurement: the generic cascade is **DEAD** — four CustomEvents with **zero listeners across 4,556 files**, all four carrying `TODO(TASK-15)`, `setRebuildDispatcher` **never called**, `operation === 'delete'` returning `[]` — and they *also* have typed entries in the event-bus catalog, so **a typed catalog entry reads like wiring to anyone auditing by grep**, which is how it survived. **§3** `StoreChangeEvent` carries no `prevState`, and `DependencyResolver` subscribes to the **bus**, not the stores, so it cannot receive one; `prevState` is a third callback argument on **5 stores out of 120**. **§3.4** codifies the seam rule from the openings fast path, which was unreachable for want of **two arguments** while every existing test passed **because the tests built `prevState` by hand** — a propagation test whose fixture supplies the value under test proves nothing. **§2** protects the bespoke wiring that genuinely works (`DoorDependencyTracker`, `WallRebuildCoordinator`, `RoomTopologyObserver`, cascade-delete) under a **wire-or-delete** rule for the generic path. **§4** suppression must be reversible — `clearGraphAuthoritative` has **0 production callers**. **§5** `RECONCILABLE_TYPES` is declared with **zero consumers**, while the real reconcile handles walls and slabs only. **§6.1.2(d) UNPROVEN**: no gate asserts the bespoke trackers still reach their pairs — *the only working propagation in the product is ungated.* **Status**: CANONICAL 2026-08-12. | P6, P8 |
| **C73** | [Geometry Determinism & Tolerance](./C73-GEOMETRY-DETERMINISM-AND-TOLERANCE.md) | Owns **geometry as a deterministic consequence of model state, and the epsilon policy.** **§0**, re-measured with the command recorded so it can be re-derived: **267 tolerance declarations** across packages/apps/plugins with at least **8 rival "small number means equal" conventions spanning three orders of magnitude** (1e-6 ×49, 0.05 ×24, 0.001 ×21, 1e-9 ×18 …), and `packages/geometry-kernel` **exports no epsilon at all**; **71 named point-in-polygon definitions across 116 files**, with one file (`CesiumViewport.ts`) holding **three copies carrying three different degenerate-divide guards**. **§2** one declared tolerance policy; new and modified predicates must consume it. **§3** the **R3 recipe** — canonical file + counting gate, **one family per PR**, never a blind mass refactor — drawn from `check-offset-implementations.ts`, including its own recorded lesson that its prose "3" measured **4**, which is why §3.4 pins at the reading rather than the prose. **§4 refusal, not substitution**, with the four working precedents named (`planOpeningRefit` relocating or refusing with **both measurements**, `BaselineReversalError`, the three named roof-collapse modes, `polygonOffset`'s labelled degenerate return). **§1** the three-state determinism model (REGENERABLE / PERSIST-OR-LOSE / destroyed). Also records `WallIntersectionResolver` ×3, `FloorPlanDiagnostics` ×2, `RoomStore.ts` ×2. **UNPROVEN**: cross-architecture float determinism; GPU/shader-side geometry; whether a refusal reaches the user or is caught and logged. **Status**: CANONICAL 2026-08-12. | P2, P6 |
| **C74** | [Constraint Honesty](./C74-CONSTRAINT-HONESTY.md) | Owns the rule that **no adapter may report a solve it did not perform.** **§0**'s central exhibit, found by re-measurement rather than briefed: `PlanegcsAdapter` declares `readonly kind = 'planegcs'` while the `MockSolver` it delegates to 100% honestly declares `kind = 'mock'` — **it takes the mock's behaviour and puts the real solver's name on the one externally visible identity the interface offers.** And the 31 passing tests inject `opts.underlying`, **the field whose own docstring says production callers must not pass it** — so the tests cover the one configuration production never uses, and the fallback production always takes is untested. `planegcs` is **not a dependency of anything** (0 hits across every `package.json`); `createWorkerHandler` has **zero production callers**; the adapter's own dates disagree with `engine.ts`'s. **§1** the four kinds of constraint work (VALIDATION / ENFORCEMENT / ADVISORY / SOLVING), with SOLVING reserved for simultaneous systems with no closed form — and **§4.5 UNPROVEN: no constraint family in this repo has been shown to require it**, which is why §4.1 is a MUST NOT rather than a caution. **§4.3** fixes the ordering — **adapter truthfulness first, binding second, and never in the same commit**, because landing them together makes the truth flattering in the same change that starts telling it, destroying the lesson. **§2** protects what is real: the `./compliance` registry (wired, 800 ms-debounced, advisory), `WallOccupancyStore.canPlace`, `annotationConstraints` (the one persisted family — checked, never solved) — and raises the duplicated `StairValidationAuthority` to a MUST: **the tested copy is not the shipped copy.** **Status**: CANONICAL 2026-08-12. | P2, P6 |
| **C75** | [Provenance](./C75-PROVENANCE.md) | Owns the rule that **the system must never present invented or inferred information as authored truth**, over the five-value vocabulary **AUTHORED / OBSERVED / COMPUTED / INFERRED / REGENERATED**. **§0** argues from the invention site itself — `roomSnapshotUtils.ts:156`, where a snapshot missing `detectionMethod` loads **as if topology had detected it**; the cast is `as any`, **defeating the union exactly where it would have caught this**, and `'auto-topology'` is not a neutral placeholder but the union member meaning *flood-fill from the wall graph* — so **an unknown origin is upgraded to the most authoritative origin available**. Located during authoring: the P1-5 repair path (`RoomDetectionEngine.ts:454` repairs, `:475` writes `'auto-topology'`) — **the repair goes to the console; it does not go to the model**, with `SlabFragmentBuilder.ts:706` as the refusing sibling form. **§0** also records that element schemas carry **no provenance at all** (`originDetail`, `derivationStatus`, `detectionMethod` → **0 hits in `packages/schemas`**), while the non-element domains carry it richly — those are the idioms to **copy, not invent** (`DataConfidence`, `heightProfile`'s mandatory 0..1 + tier, `ProvenanceEdge`, `LandBasis`'s unrepresentable-wrong-basis branding, `BuildableEnvelope`'s 11-member refusal union). New fields must be optional-with-default so existing snapshots parse. Peer of [ADR-0319](../adrs/ADR-0319-audit-fields-are-derived-not-authored.md) with an explicit MUST-NOT-restate; orthogonal to C62 (confidence). **§5 UNPROVEN and flagged as the largest open risk**: no export mapping for the five values into IFC/DXF — **provenance that stops at the export boundary protects nothing downstream.** **Status**: CANONICAL 2026-08-12. | P5, P6, P8 |
| **C76** | [Platform & API Surface](./C76-PLATFORM-AND-API-SURFACE.md) | Owns **the platform surface itself** — which symbols are public, what stability is promised on them and for how long, the layer boundary **as an enforced API rule** rather than a lint preference, deprecation policy, and the downstream consumers of the model. **§0**: C03 owns what a command *is*, C16 how one is *authored*, C69 the verb *register*, C67/C68 the chat control plane, C05/C47 the file format — **nothing owned the surface itself**, and its evidence lived only inside gate sources and package headers. This is the **C69 pattern, fourth instance** (after verbs, element families via COORD-01, and secrets via C77). ⚠ **§0.1 binds this contract and every document citing it**: the SDK export count, the verb counts, the layer-gate baselines, the NFT budget values and the ledger sizes may only be **CITED** from their artefacts, never transcribed — C64 §2.13 / C69 §0.1 applied to the platform surface. Every number lives in **§9 (AS-IS)** with the command that produced it and the date it was run, and **the artefact wins over §9 always**. **§1** the `@pryzm/plugin-sdk` facade: 8 subpath exports, the publish-time rename to `@pryzm/sdk`, the **v1.x lock** (ADR-0038 §A) and its price — **v2.0.0 + a one-year deprecation cycle**; **§1.3** the doctrine that a facade curates *capability*, not symbols — `register()` is deliberately withheld from the re-exported store registry: ***"Plugins ask; they do not fill."*** ⚠ **§1.4 records a live defect, not a footnote**: `@pryzm/protocol` and `plugin-sdk` **both** claim to be the stability boundary, and `plugin-sdk:232` re-exports `@pryzm/schemas` directly, **bypassing protocol** — two facades claiming one role is **C84 EI-9**, three end-states are costed, and ⛔ neither barrel may be narrowed until it is decided. ⚠ **§1.5**: wildcard re-exports mean the true public surface is **larger than any static count**, so the §1.2 lock is asserted over a set nothing enumerates. **§2** the boundary gate: identity resolved from **workspace manifests, never a resolver** (the predecessor rule was set to `'error'` with no resolver and **silently checked nothing**, L-809), **four independent numbers never merged into a score** — upward imports, facade bypasses, banned third-party imports, and **UNCLASSIFIED as a first-class ratchet so the gate cannot be made green by classifying less** — and **§2.4's three-value exit contract** where `2` = MISCONFIGURED (could not measure) is deliberately distinct from a failure and from `3` (ratchet exceeded). **§2.6 MUST**: report **headroom, not totals** — today one number sits at its ceiling with **zero headroom** while another has fallen ten below it, and a total hides both. **§3** the command API: `affectedStores` as a required part of the *type* (an undeclared store is a **silent undo defect**), boot-time validation that throws rather than failing at dispatch, **alias collisions FATAL not last-write-wins**, and §3.5 — a command type is a **wire identifier**, so *"renaming one is not a refactor, it is a protocol change"* (C47, per C69 §1.1). ⚠ **§3.7 records two NOT-FOUNDs as findings**: there is **no middleware/interceptor pipeline** and **no `VERB_ALLOWLIST` constant** — extension is by named seams only, and the register is generated. **§4** versioning: the OpenAPI spec's **byte-stable SHA-256 pinned in a test**, migrations **append-only with `toVersion === fromVersion + 1`**, and **§4.6** — a budget may only be asserted by a bench that measures its subject; `nftLimit()` **throws** rather than lend a contract budget to a proxy, because *"a green bench that measures something adjacent is worse than no bench: it manufactures false confidence."* **§5** downstream consumers, at the granularity where the verdict is **true**: the DXF and PDF **engines are IMPLEMENTED while their plugins are declared SHELLS**; ⛔ **`packages/pdf-to-bim` is a REVIEW QUEUE, NOT A CONVERTER** — no PDF parsing, no geometry extraction; and **Rhino has two independent import paths with no shared code**, recorded as a C84 §3.5 duplication. **§6 is the cross-cutting doctrine, and it is normative**: ***absence is TYPED, NAMED and COUNTED, never silent*** — `MigrationStubError` carries `code: 'migration-stub'`; the six shell plugins export a named `*_PROVENANCE_MAPPING` absence record; the verb register writes **UNKNOWN, never blank**, because *"an empty cell would read as 'fine' and mean 'nobody looked'"*; and gate exits `2`/`1`/`3` exist so *could-not-measure*, *measured-a-failure* and *ratchet-exceeded* cannot alias. **§6.3**: a gate landing RED on defects that **predate it** goes in `gate-newly-measured.json`, not `gate-debt.json` — calling it a regression blames the gate's author and absorbing it as debt **backdates a founder decision that was never made**. **§6.4**: a gate that starts **passing** while listed as debt is *also* a failure. **§8 NOT MEASURED (N1–N8)** carries the honest register, including **N5 — the deprecation policy is DECLARED AND UNTESTED (there has been no v2)** and **N7 — 13 packages have no layer at all, and whether the 8-layer model extends to the backend is an open, unowned question**. **Status**: CANONICAL 2026-08-19. | P1, P6, P8 |
| **C77** | [Secrets & Configuration Register](./C77-SECRETS-AND-CONFIGURATION-REGISTER.md) | Owns **the enumeration of every secret and sensitive env var the system depends on** — name, read-site, service, build-time-inlined-vs-runtime, failure mode, and **which services must hold the byte-identical value** — GENERATED from the read-sites, never transcribed, with a gate that reports **presence, never value**. **§0**: the founder asked whether every secret is contractually mapped; the audit checked all of C01–C75 and found **NO OWNER** for any of ~40 env names across four surfaces (BFF runtime, editor client build-time-inlined-and-served-publicly, sync-server, the nine deploy build-args) — the **C69 pattern, third instance** (after verbs and, per COORD-01, element families). The cost was paid in-session: a `SESSION_SECRET` misreading (a transient `flyctl` failure trusted over the tool) went uncaught because **a wrong reading had nothing to check it against**, and it reached a committed `fly.toml` header before an independent audit caught the code contradiction. **§1.2 MUST NOT**: a `build-time-inlined`/`VITE_` name may not be `SECRET` — vite bakes it into the public bundle. **§1.3**: an `ephemeral-fallback` (the `authStore.js:30` `randomBytes(48)` path that only `console.warn`s) is a **declared** failure mode — a missing secret must not masquerade as a working one. **§2.2 the shared-secret invariant**: `SESSION_SECRET` must be byte-identical across the BFF (signs) and sync-server (verifies WS upgrades), two independently-deployed apps with nothing asserting equality — a drift refuses every browser `bad-signature`, the third silent sibling of C-DEPLOY §3.2. **§2.3** absorbs the nine-build-arg deploy sub-contract (governed today only by the prose runbook). **§2.4 MUST NOT**: the gate never surfaces a value — a secrets gate that could leak is worse than none (the C74 lesson applied to credentials). Evidence: `BIM30-SECRETS-MAPPING-AUDIT.md` (deleted in the 2026-08-15 corpus collapse — see git history; the generated register below is the live artefact). **Gate** `tools/ga-gate/check-secrets-register.ts` + register `docs/04-reference/SECRETS-REGISTER.md` — both **UNBUILT at stamp time**, presence-not-value in every arm. **Status**: CANONICAL 2026-08-12. | P5, P8 |
| **C78** | [Universal Relationship & Consequence Contract](./C78-UNIVERSAL-RELATIONSHIP-CONTRACT.md) | Owns **the founder's governing rule for safe mode**: any element → any related element must **either** participate in the full safe-mode lifecycle **or** explicitly return **UNDETERMINED with a typed reason** — never silently ignore a known dependency, never return `[]`/`null` for "could not determine", never execute a different algorithm from the one previewed, never bypass the consequence system through an observer. **Generalises [C70](./C70-BIM30-TARGET-AND-GOLDEN-CHAIN.md)'s wall-specific Golden Chain across the element × relationship product; defers wholly to [C71](./C71-GRAPH-AND-TOPOLOGY.md) on the relationship vocabulary and to [C72](./C72-PROPAGATION-AND-PREVSTATE.md) on propagation.** **§0** argues from eight measured failures: the L1 contract is **already 36-of-36 element-agnostic** and it is the **L7 COMPOSITION** that violates the rule; real coverage is **1 verb of 323** (100 are consequential); a 956-LOC second planner is **authored and unreachable**; three couplings hold the system to one family (two `ReadonlyMap<string, ConsequencePlanner<WallMoveCommand>>`, `normalizeToWallMove`, `MovePlanToolHandler:243`); `preview()` returns bare `null` for **four causes, two of them semantic opposites**, and the overlay swallows a fifth; two reasons are encoded as **fake hash sentinels** because no typed reason existed; **12 of 14 dependency-naming fields are honoured by nothing** (`boundingWallIds: []` hardcoded on floor and ceiling); **9 indexes exist and exactly ONE can refuse**. **§0.3** holds up `rebuildSemanticGraph.ts` as the template — it already refuses to invent `sitsOn` and names `contains`/`connectedByLift` as unreconstructable — *this contract generalises something that works.* **§1.3 the three legal answers** every relationship must give to *"if A changes, how does the system determine whether B is affected?"*: **DETERMINED-affected · DETERMINED-unaffected · UNDETERMINED+reason**, with the MUST NOT that **"unaffected" is never inferred from missing data**. **§2–§18 the seventeen axes A–Q** (identity · relationship identity · direction · discovery · freshness · prediction · **UNDETERMINED** · hashing · execution binding · reconciliation · undo grouping · provenance · persistence · concurrency · blind mode · region · generated). **§8 is the heart**: **23 measured reason values across 6 modules in 5 conventions → ONE closed 11-member union** (4 existing + `INVALID_REQUEST`, `GEOMETRY_UNPREDICTABLE`, `RELATIONSHIP_NOT_RECORDED`, `RELATIONSHIP_NOT_READABLE`, `AGGREGATE_SCOPE_UNSUPPORTED`, `PLANNER_THREW`, and `TOPOLOGY_CHANGE_POSSIBLE` promoted to L1), **naming the twelve that lose information today** — 7 of 8 room-prediction refusals collapse through one ternary so `COLLAPSED` (a room at zero area) prints the same value as "a cache is stale" — plus §8.3 typed sub-reasons never prose, §8.4 `reason` ≠ `kind`, §8.7 the free-text producer sentence coexists. `UNSUPPORTED_ELEMENT_TYPE` has **zero producers** today. **§19 the fourteen U-INV invariants**; **§19.1** applies C70's no-partial-credit rule ACROSS the product — *one family with a complete lifecycle is one family, not an architecture*. Evidence: the four Phase-0 inventories — `BIM30-PHASE0-ELEMENT-INVENTORY.md` · `BIM30-PHASE0-RELATIONSHIP-INVENTORY.md` · `BIM30-PHASE0-CONSEQUENCE-MACHINERY.md` · `BIM30-PHASE0-CROSSCUTTING-INVENTORY.md` — **all deleted in the 2026-08-15 corpus collapse and not carried forward; citable from git history only**. **Gates §20**: **every one is a NAMED GAP or exists only at wall scope**, so every invariant is **UNPROVEN at stamp time** per C70 §7.1 — never an inherited green. **Status**: CANONICAL 2026-08-12. | P1, P3, P6, P8 |
| **C79** | [Region Semantics & Derived Boundary](./C79-REGION-SEMANTICS.md) | Owns **what a region MEANS**: a region-created element is bounded **BY REFERENCE, not by copied coordinates** — the **REGION SPECIALISATION of [C78](./C78-UNIVERSAL-RELATIONSHIP-CONTRACT.md)**, which it defers to and may not restate; the two were authored the same day in parallel without sight of each other, so **a cross-read is an exit condition (§9.7)** and C79 mints no rival reason vocabulary — its `undetermined` IS C78 §8.1's `UNDETERMINED`, with the mapping stated at §5.2.0 (`boundingWallIds: []` → `RELATIONSHIP_NOT_RECORDED`, C78's own row 8). **§0** argues from the founder's own five-word question — *closed polyline → slab by region → move a wall, does the slab follow?* — measured **no, silently**: `SlabRegionTracer` **received** the wall array, walked their centrelines, and **threw the wall ids away**, so `SlabDependencyTracker.registerSlab` (which indexes on `edge.type === 'hostReference'`, `:86`) built an **empty dependency graph**, while the pick-the-walls button in the same tool emitted `HostReferenceEdge`s and DID follow. **Two buttons that look identical behaved differently, with no error.** Closed for ONE of six paths in `e6c8cb58`; **the asymmetry moved rather than vanished** — `SlabTool.ts:1424` (3D) still traces bare rings. **§2 attribution is BY CONSTRUCTION, NEVER BY PROXIMITY**, because proximity is exactly where a WRONG `hostId` comes from and **a wrong host is strictly worse than none** (the element follows the wrong wall) — C75's invention defect in the boundary layer; failures are **named** (`curved` / `noWallId` / `ambiguous`, the last dropping to null rather than first-writer) and **counted and RETURNED, never absorbed** (3 host edges / 16 curved fallbacks on the reference fixture). **§3 contracts the reference frame** so no future implementer silently picks a face: `'centerLine'` at offset 0 is the traced frame, and naming a face both shifts every edge by half the wall thickness AND makes interior/exterior **a coin flip**, since the ring walk does not preserve authored direction. **§4** degradation is an **undoable command** (`DegradeSlabSketchCommand`) over a `fallback` that MUST exist **at authoring time** — `resolveOrFallback` returns `null`, degrading to **NOTHING**, if the host dies before any rebuild cached one. **§5** the five states — **preserved / resized / regenerated / conflicted / undetermined** — with **`undetermined` MUST NOT collapse into `preserved`** (same pixels, opposite facts: that conflation IS the §0 defect). **§6** the conformance table: **1 conforming region path of 6**; roof runs a **second, independent tracer** (`WallRegionDetector`) that **cannot carry a reference even in principle** (`:44,193`) while making the identical user-facing promise; **ceilings and floor finishes have NO region mode at all** (0 occurrences of `region`) and so cannot inherit a fix. **§7 MUST NOT: a field that names a dependency, written empty** — `boundingWallIds: []` hardcoded at `CreateFloorCommand.ts:231` / `CreateCeilingCommand.ts:191` (**14 fields name a dependency; 2 are honoured**); populate, remove, or DECLARE — an empty array is none of the three. **Gate** `check-region-host-attribution` + four siblings — **all UNBUILT at stamp time**; a region gate finding zero paths exits **2**, never 0. **Status**: CANONICAL 2026-08-12. | P6, P8 |
| **C80** | [Generation & Regeneration Authority](./C80-GENERATION-AND-REGENERATION.md) | Owns **the authority question** — *may this pass replace this element?* — as the **GENERATION SPECIALISATION of [C78](./C78-UNIVERSAL-RELATIONSHIP-CONTRACT.md)**, deferring to it on the whole consequence lifecycle (plan hashing §9, execution binding §10, undo grouping §12, blind mode §16) and restating none of it; **C78 §18 states the same destruction from the relationship side — C80 does not overturn a line of it**, the division being *C78 governs the lifecycle, C80 governs the entitlement*. Defers wholly to **[C75](./C75-PROVENANCE.md)** on the five-value vocabulary. **§0 is the suite's only EXECUTED-PROVEN destruction**, re-run for this contract at HEAD `e6c8cb58` through the **real `RoomStore` → real `CommandBus` → real `plugins/rooms` handler**: a level holding **1 AUTHORED + 1 generated** room, cleared by the §GRAPH-CLEAR-FIRST loop transcribed from `HouseLayoutExecutor.ts:1757-1766` → **`seeded=2 · remaining=0 · the authored room survived=false`**, with the two `DELETE_ROOM` dispatches visible in the run log — **all twelve floors green, so this is a working harness reporting a working destruction**, not a broken one. The founder's own scenario (*"regenerate and my hand edits survive"*) failing. **§0.1's four facts each close differently**: every generator is a **UI controller** and `*.regenerate` → **0 hits** (*"there is no central place"*); **no element carries provenance** (`generationId`/`generatedBy`/`isGenerated`/`sourceGenerator` = 0 first-party element hits; `RoomBoundary.detectionMethod` is the repo's ONLY element-grain signal, which is why every executable arm is about rooms); the vocabulary **already exists at L0** with `SystemWritableOrigin` making a generator's claim of human authorship a **compile error**; and — the most important line for planning — **the refusable form already exists and already refuses** (`planRegenerationClear` → `refuse=true · PROTECTED=1`, with a control proving refusal is a decision, not a constant). **Arms (b) and (c) run over ONE fixture and disagree**, so **Phase 1 wires the existing refusal, it does not design one.** **§2 the three legal answers — `may` / `protected` / `unknown-authority` — and §2.3 MUST NOT treat `unknown-authority` as permission**: the two collapses are catastrophic in **opposite** directions (to `may` destroys user work silently; to `protected` blocks every generator, and **every model today is a legacy model**). **§2.4** forbids the obvious shortcut by name — `metadata.createdBy` defaults to `'system'` and is stamped by STORES, so reading it as authorship mints `authored` from a default. **§4.2 MUST NOT default provenance in EITHER direction**: ⭐ *a wrong provenance is worse than an absent one, because regeneration ACTS on it* — absent yields a decision, wrong yields a confident deletion. **§5.2** UNDETERMINED-with-reason, **never `[]`** (which reads as *"nothing was generated"* and licenses a duplicate); **§5.3** the clear pattern is simultaneously **too aggressive AND too narrow** — it deletes authored rooms yet never removes a previous run's walls/doors/slabs/stairs/roofs/furniture, so a second run **stacks a whole building on the first** (two defects, two fixes, not one). **§6.4 MUST NOT rewrite a working engine**: [`BIM30-IMPLEMENTATION-ROADMAP.md`](../../03-execution/plans/BIM30-IMPLEMENTATION-ROADMAP.md) §6.1 row 11 / §6.2.6 (which absorbed `BIM30-DO-NOT-REBUILD` §6) protects the generation stack (D-TGL 67 tests, D-FLE, D-CE) — *"two wiring changes and no engine change"* — and every C80 requirement is satisfiable by wiring. **§7.3(b)** adopts the gate's own scope limit: **3 of the founder's 7 verifications are EXECUTED, 4 are NOT EVALUATED** — *"This gate does NOT claim the scenario passes"*; **§7.3(a)** names the gate's weakest link (the loop is transcribed, so it proves the PATTERN, not that instance). **§7.4 records that `manual-boundary` has TWO independent production producers** (`RoomTool.ts:199` + `RoomPlanToolHandler.ts:125`) — and preserves an authoring near-miss in which a timed-out search briefly declared the first citation dead, overturned by a full sweep (§PROBE-CAN-BE-WRONG-THREE-WAYS applied to the contract's own authoring). **§7.5** four named gaps with owners (**GEN-GAP-1 generation-as-verb is the UNBLOCKED one**). **§8 declines 0B OPEN QUESTION 8** (verb family vs. protection in the stores) with the store route's trade-off stated rather than dismissed. **Gate** `check-authored-state-protection` **EXISTS and is RED by design** (`[1] DECLARED-LEVEL — 7 findings`, pinned in `gate-newly-measured.json`); four siblings are **NAMED GAPS, UNPROVEN** per C70 §7.1 — never an inherited green. **Status**: CANONICAL 2026-08-12. | P6, P8 |
| **C82** | [Ribbon Capability Surface](./C82-RIBBON-CAPABILITY-SURFACE.md) | Owns **the three legal states of a rendered ribbon control** — **WIRED** (dispatches to a registered handler on the real bus, proven **EXECUTED-REACHED** by the H6 gesture-reach probe) · **DISABLED-WITH-REASON** (the capability does not exist; the control refuses through BOTH doors — mouse and `triggerCommand()` — and its reason names the missing capability by contract/spec reference) · **ABSENT** — and prohibits the fourth state, **a control that dispatches into nothing** (the §C-B1 silent no-op class, measured at **267 of 280** toolbar pairs at census `ae659c96`, **0** after `4bfe86f0`). **§0** argues from the executed instrument, never artefacts: the H6 probe (`b32d56ff`) clicked every `[data-command]` button on the real `CommandBus` and read **0 EXECUTED-REACHED / 4 RESOLVED-ONLY / 267 NOT-REACHED / 9 UNPROVEN** across 280 pairs, while **all 30 surfaces had zero production importers and 30 green spec files asserted dispatch against a MOCK bus** — C70 §4.2's machinery-present ≠ capability-reachable at family scale. **§2** the probe's EXECUTED-REACHED set **is the definition of "wired"**; RESOLVED-ONLY is not wired; BY-READ is never an award; the hand-written `BACKED_TOOLBAR_VERBS` is pinned against the generated census **both ways**. **§3** every WIRED verb is **C67/C68-bound** in the wiring PR — chat-reachable, `CHAT_UNAVAILABLE`, or classified; register regenerated per C69 §3.3. **§4** a new button lands ONLY with its handler or DISABLED-WITH-REASON citing its [SPEC-50](../../03-execution/specs/SPEC-50-RIBBON-HANDLER-BACKLOG.md) row; new surfaces join the probe's subject set in the same PR. **§5.4** carries the instrument's own not-measured list (palette, context menus, ~120 panel `executeCommand` sites, shortcuts, rendered-DOM mount) so the contract is never read as coverage. Governs the surface [ADR-0326](../adrs/ADR-0326-mount-the-ribbon-disable-the-unbacked.md)'s mount decision creates. **Gate**: probe + census EXIST and are hand-run; **no CI job executes them at stamp time — NAMED GAP, UNPROVEN per C70 §7.1**; the dispatch-side two-way pin is landed and green (31 spec files / 704 tests, `4bfe86f0`). **Status**: CANONICAL 2026-08-14. | P6, P8 |
| **C83** | [Spatial Validity & Design Logic](./C83-SPATIAL-VALIDITY-AND-DESIGN-LOGIC.md) | Owns **"is this arrangement of elements VALID?"** — asked of a design a human is authoring, *while* they author it. **§1 the three verdicts**, and the boundary is **"can context reverse it?"**, never severity: **IMPOSSIBLE** (self-contradictory geometry — an interior wall occupying a window opening is two mutually exclusive claims about one volume; refuse) · **INADVISABLE** (buildable but defeats an element's function — a sofa blocking a door; **offer, never impose**) · **FINE** (silent). **§1.3 MUST NOT infer severity from magnitude** — a 5 cm wall/window overlap is IMPOSSIBLE and a 5 cm swing intrusion is INADVISABLE, so the verdict is a property of the RULE, declared when written, never computed from the violation's size. **§2 rules key on ROLES, not element kinds** — `PASSAGE` / `APERTURE` / `STRUCTURE` / `OCCUPIABLE` / `CIRCULATION_SURFACE` — which is the founder's own distinction made machine-readable and the reason one rule answers all three of their cases without an exceptions list: *a sofa in front of a `PASSAGE` blocks what a passage is for; in front of an `APERTURE` it blocks nothing, so a bed under a window is silent.* An N×N kind matrix is refused as unaffordable by C82's own measured precedent (267 of 280 hand-maintained pairs silently dead). **§0.2 answers the founder's foundation question and is the section to read**: *does every room already know what is within it?* — it **holds for BOUND, holds with a caveat for HOSTED, and is FALSE for CONTAINED**. `RoomContentsService.ts:169` splits it three ways (`bounding`/`hosted`/`contained`) and is genuinely WIRED (`initBuilders.ts:526`; `PropertyPanelStoreEnricher.ts:109` runs for **every** selected element) — but the authority for BOUND is `room.boundingWallIds` (`Room.ts:140`), **not the graph**; `hosted` is DERIVED from it, so an unrecorded bounding-wall list silently empties doors *and* windows; and ⚠ **§0.2.3 REFUTES this contract's own first draft** — the `'contains'` edge **does not exist on ANY path**, hand-authored or AI: all 14 non-test `CreateFurnitureCommand` sites supply no `hostedSpaceId`, the AI path computes the right answer and **drops it three times**, and the family has **no refusing reader at all**. ⭐ **C83 rules MUST resolve containment geometrically and MUST NOT read the edge**; ⚠ `graphmove.cert.ts` classifies `contains` as **ID-KEYED**, which is right for `sitsOn`/`hosts` and wrong for a spatial containment — a stored edge under that classification would be **strictly worse than the absence it replaces**. §0.2.1 adds three live BOUND defects (`getBoundingWalls`, the refusal-bearing reader, has **zero production callers**, so a move-invalidated room reads as an affirmative *"bounded by nothing"* — C71 §4.4's collapse inside the family everyone calls mature; `clearGraphAuthoritative` has 0 production callers so generated levels never re-detect; hand-drawn rooms start `[]`). §0.2.2 names an independent defect: **3D writes the `hosts` edge and the plan tools do not**, and the whole **L6 plugin tier writes zero graph edges**. **§0.3's thesis is quoted from [SPEC-49](../../03-execution/specs/SPEC-49-CIRCULATION-INTEGRITY.md) §3 — *"The detection is not missing. The refusal is."*** — evidenced by `doorSwingKeepout.ts` (a correct 90° sector, imported by nothing but its own tests, **L-856**), `programRules.ts`'s `excludeDoorSwing` flag that **no code reads**, the 16-slice apartment validator family (A-1…A-8) with **zero production callers**, and the measured **3/288 vs 0/288** split between the true arc and the box that ships. **§4 mints no fifth offer surface** — a survey found FOUR, and the canonical one is `ConsequencePlan`→`ConfirmationFlow`→`ConfirmationCard` (gate-certified, already built around `wall.move`/`wall.create`/`opening.move`), spoken in chat through the shipped `ZeroTokenUiHooks.confirm()`; ⚠ `CommandProposal` is **declared twice, divergently, both exported** — do not add a third. **§4.2 MUST NOT offer a candidate it cannot defend**: where no defensible alternative computes, refuse with the reason and offer **nothing** — an offer carries an implicit claim of validity. **§4.3 no rule may auto-apply.** **§5 false positives are the primary risk**, so every rule ships with a **SILENCE TEST** (sofa-at-window and bed-under-window must produce ZERO findings) plus a near-miss control and a whole-corpus baseline — **a rule firing on a large fraction of known-good generated output is refuted by that run, not tuned until it passes**; §5.3 silence on UNDETERMINED, always; §5.4 dismissals persist, are attributed and are **counted per rule**, because a rule dismissed by most users is a rule with a wrong premise. **§6.1 MUST NOT: this is NOT a building-code engine** — and it names the existing breach rather than inheriting it: `ConstraintEngine`'s 17 rules transcribe UK Part M / BS 8300 / CIBSE TM52 constants client-side with **no jurisdiction resolution and no provenance**, applied by a product whose live legal work is Spanish; **a C83 rule may populate no `regulation` field**, and migrating the 17 is owed by C58's owner. **§6.2** the generators keep their constraints and are **the richest source of rules to PROMOTE** (`programRules.ts`'s authored millimetres), never rivals to re-author. **§7** typology varies whether an INADVISABLE rule applies and its threshold — **never a verdict class, never a role** — via the existing C50 pack mechanism, every override attributed in the finding. **§1.1.1 mints NO fifth vocabulary**: the three verdicts map onto **C74 §1.1's four canonical classifications** (IMPOSSIBLE = ENFORCEMENT, INADVISABLE = ADVISORY) after the survey found a hard/soft split independently decided **four times** already — and ⚠ C74 §2's protected table has **exactly one** ENFORCEMENT row, so a second is a contract edit in the same PR. **§2.2.1 ⭐ the role split is ALREADY MATERIALISED** as the `RoomContents` bucket boundary: *"door = passage, window = aperture, sofa = occupant"* is executable today as **code that does not exist** — read `hosted.doors` and `contained.furniture`, deliberately do not read `hosted.windows`. **§3.0/§3.1** two defects found by reading rather than trusting: of the `ConstraintEngine`'s three triggers **only one fires**, and it calls `run()` directly, **bypassing the `batchCoordinator.isBatching` suppression** — the guard sits on the path that never fires. **§5.2.1 the worst outcome already ships**: `RoomValidationService`'s area rules are guarded on `window.roomSystemTypeStore`, assigned **nowhere**, so the guard fails **closed** into a green *"✓ No issues found"* — a 2 m² bedroom renders identical to a conforming room. **§8 REORDERED 2026-08-14**: **S0** plan-view `canPlace` parity (a **regression** — plan view creates overlapping doors that 3D refuses; two lines) → **S1** `FURNITURE_BLOCKS_DOOR` ADVISORY (zero new geometry/vocabulary/UI/schema, ships on a **disclosed** proxy) → **Slice C** make `contains` reachable (its own slice, **not** an S1 blocker, gated on the cert reclassification) → **S3** `Door.swing` → `OpeningPose` (L-856) → **S4** `OCC_CROSSES_HOSTED_OPENING` ENFORCEMENT. **The founder's headline case moved to LAST — not because it shrank**, but because it costs a C74 §2 row *and* a preview trigger with **zero callers repo-wide**, and C11 §7.6 names what shipping it early produces: *"a dead click behind a perfect preview."* S4 reuses the generator's proven **`§DOOR-CLEAR-OFFSET`** predicate rather than inventing one. **§8.0** a rule predicate is authored at the LOWEST layer its inputs permit, per SPEC-49 §5's measured fault — *"the engine physically cannot call the invariant that judges it."* Extends `CanPlaceRefusalCode` (§1.4) and mints no rival reason union; defers to **C78** on the lifecycle, **C71** on the graph, **C15** on hosting, **C50** on packs, **C67**/**C68**/**C69** on chat reach and verb registration. **Gate**: four named, **ALL UNBUILT at stamp time — UNPROVEN per C70 §7.1**, never an inherited green. **Status**: DRAFT 2026-08-14. | P5, P6, P8 |

---

## Conventions used across all contracts

- **MUST / MUST NOT / SHALL / MAY** — RFC 2119 normative terms.
- **[ADR-NNN]** — links to `docs/02-decisions/adrs/ADR-NNN-*.md`.
- **[SPEC-NN]** — links to `docs/03-execution/specs/SPEC-NN-*.md`.
- **CI gate: hard-fail** — a merge-blocking lint or test failure.
- **CI gate: soft-fail** — a counter tripwire; becomes hard-fail at the stated phase exit.

---

## How to amend a contract

1. Edit the relevant `C0N-*.md` directly. Do NOT create a new `*-AUDIT.md` alongside it.
2. If the change alters a CI gate or convergence boolean, update `docs/01-strategy/STR-04-architecture.md §8` and `03-CURRENT-STATE.md §1` in the same commit.
3. If the change reverses an ADR decision, raise a superseding ADR first; then amend the contract to cite the new ADR.

---

## What is NOT a contract

- Sprint plans → `docs/archive/pryzm3-internal/04-PLAN-FORWARD/`
- Current status → `docs/archive/pryzm3-internal/03-CURRENT-STATE.md`
- Per-decision rationale → [`../adrs/`](../adrs/)
- Per-system normative spec → [`../../03-execution/specs/`](../../03-execution/specs/)
- Implementation plans → [`../../03-execution/plans/`](../../03-execution/plans/)

## Gaps + roadmap

- **2026-08-02 (PEC programme):** **C64 — The Envelope Compiler** minted (ratified by [ADR-0291](../adrs/ADR-0291-pryzm-envelope-compiler-variable-first-architecture.md), specced by [SPEC-ENVELOPE-COMPILER-PROGRAMME](../../03-execution/specs/SPEC-ENVELOPE-COMPILER-PROGRAMME.md)). It closes the gap the eight 2026-08-02 doctrine ADRs could not: **no contract owned `parcel → resolved rules`** — the legal stack, the variable dependency graph, the resolution order and the dataset binding. C57 owned the parcel and C58 owned `resolved rules → envelope`; the middle was hand-wired per city. C64 is **cross-cutting and sits above C58** — where C58 settles a rule, C64 defers and says so. ⚠ **C64 §2.13 binds the whole `docs/` tree**: no coverage, determination or completion percentage may be transcribed into a contract, spec, ADR or master document; cite the artefact that computes it (`tools/city-completion/measurements/*.measurements.json`, the [PEC dashboard](../../03-execution/plans/PEC-EXECUTION-DASHBOARD.md), the [National Capability Register](../../04-reference/standards/NATIONAL-CAPABILITY-REGISTER.md)). **C61 remains the RESERVED unminted slot** — C64 does not take it.
- **2026-08-09 (scale remediation):** **C66 — Concurrency & Scale** minted. It closes the gap the 2026-08-09 full-system audit found first and found hardest to state: **no contract owned "how many users at once."** C10 owns per-user latency; C49 owns region; C48 owns DR; nothing owned concurrency, per-instance limits, or the statelessness precondition for a second machine. The consequence was not theoretical — the only capacity statement in the repository was a `fly.toml` comment, and L-770's *"acceptable for a closed beta"* disposition was unfalsifiable while no document said what size beta was being accepted. ⚠ **C66 §1 binds every capacity claim**: a tier is HELD only when a k6 run passes against a production-shaped target and is recorded; otherwise it is CLAIMED and MUST NOT be described as supported. **All three tiers are currently CLAIMED.** ⚠ **C66 §3.1 binds deployment**: no second instance until the category-A state inventory is shared. Writing §3.3 surfaced two defects the audit missed — `exportGuard`'s process-local single-use token (exports fail (N−1)/N at N instances) and `manualAdminZoneStore`'s per-process id sequence (cross-instance collisions). **C61 remains the RESERVED unminted slot** — C66 does not take it.
- **2026-08-11 (RAC onboarding):** **C68 — Element & Attribute Chat Onboarding** minted. C67 minted the *control plane*; what nothing owned was the **obligation attached to a single PR**: when a new element type or a new attribute arrives, what does it owe the chat, and who says no? C67 §6 states it in five lines; C68 is the checklist with a named gate check behind each item — and, per the suite's NOT-YET-TRUE convention, **the honest list of what has no gate**. ⚠ **C68 §4 binds every PR that registers a bus command, adds an element kind, or adds a user-visible attribute** — but §6.3-G1 records that only the first is machine-detected: an attribute edited through an existing generic verb registers no new verb and trips no ratchet, so it binds by review. ⚠ **C68 §5.h is a founder ruling recorded for the first time**: a family with no catalogue does not get skipped, it gets a catalogue authored on the `WallSystemTypeStore` shape. ⚠ **C68 §6.3-G9 was a documentation defect surfaced by writing it**: `ADR-0315` was cited in `check-chat-capability-coverage.ts`, `CapabilityExecutionSpec.ts`, the RAC plans and the issue log with no such file. **CLOSED the same day** — [ADR-0315](../adrs/ADR-0315-universal-capability-architecture.md) (`147b94ab`) records the architecture as it was built, and every citation now resolves. **C61 remains the RESERVED unminted slot** — C68 does not take it.
- **2026-08-11 (RAC refresh, C67 rev 3 + C68 rev 3):** C67 was rewritten against the U0–U10 state and **C67 is now indexed above** — it had a file and a canonical status but **no row in this table**, so the suite index did not list one of its own contracts. C67 rev 2 described 16 capabilities and put the generator pattern, the scope algebra, the plan executor and the LLM rung in TO-BE; all four shipped within a day and rev 3 moves them to AS-IS with measured numbers (**45 capabilities, undeclared 0/0 at `f5f3a5a1`**). ⚠ **C67 §1.6 and C68 §5.j / §6.3-G10 are the honest half**: the U10.3 drain measurement classified every hand-written panel pill by EXECUTING it and found **29 phrasings the ladder claims and gets wrong**, one of them destructive — *"highlight walls taller than 3m"* dispatches `wall.updateDimensions`. The rule that covers them (**claiming discipline**) had never been written down; it is now C67 §4.7 and C68 §5.j, and it is **not discharged by fixing the 29**. ⚠ **U9 (batch-creation parametrics) is IN FLIGHT and is claimed by neither contract.**
- **2026-08-11 (verb register):** **C69 — The API Verb Register** minted. The founder asked for *"all the API verbs documented in a contractual document… whenever a new one is added it should be added there."* The gap was not that the answer was wrong; it was that **nothing owned the question**. C03 owns what a command is, C16 how one is written, C08 whether it replicates, C67/C68 whether the chat reaches it — and the only enumerations were two CI gates that disagree with each other *and* with the code. ⚠ **C69 §0.1 binds this suite and the whole `docs/` tree**: the verb list, the verb count and every per-verb column may only be CITED from the generated [`docs/04-reference/API-VERB-REGISTER.md`](../../04-reference/API-VERB-REGISTER.md), never transcribed — the same rule C64 §2.13 applies to coverage percentages, applied to the command surface, and the reason C69's own body states no counts. ⚠ **C69 §1.1 is a persistence rule wearing a naming costume**: a command type is written into `project_command_log` and replayed in collaboration history, so **renaming a verb is a C47 format change, not a refactor**. ⚠ **§5 records four findings the first generated register surfaced**, all open and none owned by C69 to fix: `check-sync-disposition.ts` polices a small minority of the handlers it claims (its regex matches only the object-literal form while most handlers are classes — the identical defect `check-chat-capability-coverage.ts` records having had in its own first draft, and its `< 20 property verbs` floor cannot catch it); a family of **SHADOWED** verbs whose live `initBusHandlers` bridge can never register because a plugin handler claimed the type first (`roof.update` is not alone); element-kind discriminants counted as verbs by both existing gates; and one verb registered outside the chat gate's field of view. **C61 remains the RESERVED unminted slot** — C69 does not take it.
- ~~**69 contract files exist (C01–C60 + C62–C69 + C24.1); C61 is a RESERVED slot**~~ — ⛔ **RETRACTED 2026-08-19 (lane REG1). This sentence was FALSE at HEAD, undated, and in the present tense**, which is the one form that cannot rot gracefully: measured the same day, `ls docs/02-decisions/contracts/ | grep -c '^C[0-9]'` → **100**, and the enumeration omitted C70–C100 entirely — among them **C84**, binding on every PR touching an element family. It survived four corrections to rows 4–6 above because it lives in changelog prose, where a banner sweep does not look. **Struck rather than deleted per C84 §6**, because *what kind of sentence survives four corrections* is the finding. ⭐ **Do not re-transcribe a count here at all.** The live count is asserted by `tools/ga-gate/check-contract-index-equivalence.ts`, which compares this file's row set against `ls contracts/` **in both directions** and is the gate the banner above records as its exit condition [L-1131]. Historic note follows: ⚠ *this sentence said **66** while its own enumeration in the same breath adds to 68 (60 + 7 + 1). Corrected 2026-08-11; measured `ls docs/02-decisions/contracts/ | grep -c '^C[0-9]'` → **68**. When a count and an enumeration disagree in one sentence, the enumeration is the one that was thought about.* (the context-scene height/terrain contract anticipated by ADR-0279 §6 / ADR-0280 — unminted). Refreshed 2026-07-30 — added **C63** (City Completion Scorecard & Dossier Standard, ratified by ADR-0281), which closes the `MISSING-CONTRACTS-AUDIT §GAP-RESOLVED (L-648)`: no contract owned cross-layer city-completeness or the dossier folder shape. C62 (Data Confidence / Provenance, ADR-0280) is also now indexed above (it was shipped 2026-07-29 but not yet listed here). Earlier refresh (2026-07-22) added C60, Site Entry & Jurisdiction Coverage. C01–C18 CANONICAL; C19–C23 + C32–C58 largely DRAFT pending stakeholder sign-off; C24–C31 + C50–C55 a mix (see each file's Status line). The full enterprise-grade contract suite proposed by MISSING-CONTRACTS-AUDIT-2026-06-01.md (audit removed 2026-08-09 — recoverable from git history) (18 gaps C18–C49) is now **entirely built** — that audit is RESOLVED. C57 (Parcel Data Layer) + C58 (Zoning Rules & Buildable Envelope) fill the compliance-authoring gap scoped by the Archistar-Europe gap audit (audit removed 2026-08-09 — recoverable from git history) (L-398..404) and the C19 §9/§10.2 deferred jurisdiction registry ([ADR-0269](../adrs/ADR-0269-compliance-authoring-parcel-zoning-envelope-strategy.md)). **2026-07-30 (L-650):** C63 was **extended** (no new contract minted) with the EQUAL country/city folder standard (§5.1 equal-shape invariant · §5.2 normative country folder shape · §5.3 findings/sources placement) + the city-completion rollout program (§8.2), ratified by [ADR-0282](../adrs/ADR-0282-equal-jurisdiction-folder-standard-and-city-completion-rollout-program.md) and specced by [SPEC-CITY-COMPLETION-ROLLOUT](../../03-execution/specs/SPEC-CITY-COMPLETION-ROLLOUT.md) (Phase 0 plan: `jurisdictions/_NORMALIZATION.md`).
- **2026-08-19 (platform surface):** **C76 — Platform & API Surface** minted (lane KT1), on the founder's ruling that the platform/API evidence *"belongs in a CONTRACT, with the most up-to-date status, not only in a strategy document."* The gap was the **C69 pattern for the fourth time**: C03 owns what a command is, C16 how one is authored, C69 the verb register, C67/C68 the chat plane, C05/C47 the file format — and **nothing owned which symbols are public, what is promised on them, or what a deprecation costs**. The evidence lived only in gate sources and package headers, which is the exact condition `check-verb-register.ts:9-34` already records **four measured drifts** from. ⚠ **C76 §0.1 binds the whole `docs/` tree**: the SDK export count, verb counts, layer-gate baselines, NFT budget values and ledger sizes may only be CITED from their artefacts — C64 §2.13 / C69 §0.1 applied to the platform surface — and §9 (AS-IS) carries every number with its command and date, with **the artefact winning over §9 always**. ⚠ **Writing it surfaced a defect nothing had named: §1.4 — `@pryzm/protocol` and `@pryzm/plugin-sdk` BOTH claim to be the stability boundary**, and the SDK re-exports `@pryzm/schemas` directly, bypassing protocol. Two facades claiming one role is C84 EI-9; three end-states are costed and ⛔ **neither barrel may be narrowed until it is decided**. ⚠ **§1.5 is the second**: the facade's wildcard re-exports mean its true surface is **larger than anything counts**, so the v1.x lock is asserted over a set nothing enumerates — the strongest promise in §1 is the least checkable one (N1). ⚠ **§6 makes a practice normative for the first time**: *absence is typed, named and counted, never silent*. The pattern already ran through `MigrationStubError`'s `code`, the shell plugins' `*_PROVENANCE_MAPPING` records, the register's **UNKNOWN-never-blank** rule, `nftLimit()`'s refusal to lend a budget to a proxy, and the non-aliasing `2`/`1`/`3` exit codes — it had simply never been written as a rule a future PR is measured against. ⚠ **§9.4 is kept at full strength on purpose**: **6 of 19 performance targets are measured**, and one of the 13 unmeasured carries the disclosure *"this assertion can never fail and therefore carries zero information"* while another's named bench file **does not exist in the repo**. **C61 remains the ONLY RESERVED unminted slot** — C76 no longer stands beside it, and the suite count moves 99 → 100 in this commit.
- **DRAFT → CANONICAL** transitions ratify on first PR after stakeholder sign-off + the implementation has passed the contract's §6 CI gates.
- Per-decision rationale → `docs/02-decisions/adrs/`
- Per-system normative spec (wire format, schema tables) → `docs/03-execution/specs/`
