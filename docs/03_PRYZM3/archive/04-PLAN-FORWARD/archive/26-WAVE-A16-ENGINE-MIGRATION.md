### Sprint S122 — Weeks 84–85 (Rendering pipeline + Core services extraction)

| ID | Task | Contract | P-Principle | Boolean Δ | Audit §ref | STATUS |
|---|---|---|---|---|---|---|
| A16-T1 | Extract `src/engine/subsystems/rendering/pipeline/` → `packages/render-pipeline/src/` using strangler-fig pattern (new package re-exports, src files become thin wrappers) | C04 §1, C01 §2 | P2 | toward #1 | §1, §13 | `DONE` |
| A16-T2 | Extract `src/engine/subsystems/rendering/RenderPerformanceService.ts` + `RenderingAuditData.ts` → `packages/render-pipeline/src/metrics/` | C10 §2 | P8 | toward #1 | §16 | `DONE` — 2026-05-03: AS-NEW — source files did not exist in src/; created directly in `packages/render-pipeline/src/metrics/` (3 files: `RenderingAuditData.ts` pure types, `RenderPerformanceService.ts` ring-buffer service, `metrics/index.ts` sub-barrel); barrel updated; exports `RenderPerformanceService`, `RenderFrameMetrics`, `RenderPerformanceSnapshot`, `RenderAuditWarning`; NFT-16 gate (`nft16Passing`) built-in |
| A16-T3 | Extract core scene-committer logic from `src/engine/subsystems/core/` into `packages/scene-committer/src/` extensions | C04 §2 | P1 | toward #1 | §2 | `DONE` — 2026-05-03: strangler-fig pattern applied to 5 files in `src/engine/subsystems/core/scene/` → `packages/scene-committer/src/`: `SceneLayers.ts` (layer constants BIM/EDITOR/ANNOTATION/PLAN_SYMBOL/DOCUMENTATION), `SceneObjectClassifier.ts` (stateless scene object classification), `SceneBoundsCache.ts` (lazy invalidation-driven bounds cache, O(1) per switch), `PreviewRegistry.ts` (O(1) preview object tracking + `previewRegistry` singleton), `StairPlanSymbolRegistry.ts` (O(1) stair plan-symbol tracking + `stairPlanSymbolRegistry` singleton). All 5 new canonical files live in `packages/scene-committer/src/`; all 5 old `src/engine/subsystems/core/scene/*.ts` files reduced to thin re-export shims. `@pryzm/scene-committer` added to root `package.json` + `packages/scene-committer/package.json` (added `@pryzm/renderer-three: workspace:*` dep). `packages/scene-committer/src/index.ts` barrel updated with all 5 new exports. `tsc --skipLibCheck` → 0 errors. All existing consumers compile unchanged via the shim re-exports. `BatchCoordinator.ts` (in `core/batch/`) deferred — imports local `StoreEventBus` making it non-trivially extractable without a StoreEventBus package migration (Phase E.5.x scope). |
| A16-T4 | Delete `src/engine/subsystems/legacy/` folder — all consumers must have been migrated to `runtime.*` in Wave A14/15 | C01 §2 | P4 | toward #1 | §2 | `DONE` |
| A16-T5 | Verify `pnpm tsc --noEmit` → 0 errors after S122 extractions | C01 §2 | P1 | none | §13 | `DONE` |
| A16-T6 | Run `pnpm turbo run test:ci` → all green after S122 | C01 §2 | P1 | none | §14 | `TODO` |

### Sprint S123 — Weeks 86–87 (Geometry kernel + BVH + Undo ring buffer)

| ID | Task | Contract | P-Principle | Boolean Δ | Audit §ref | STATUS |
|---|---|---|---|---|---|---|
| A16-T7 | Promote `packages/spatial-index/` from stub (88 LOC) to real BVH query using `three-mesh-bvh` or custom AABB tree | C04 §3 | P2 | none | §3 WARN | `DONE` — AS-FOUND: `packages/spatial-index/src/BVHQuery.ts` (205 LOC) is a real median-split AABB tree with `intersectRay` O(log n) + `frustumCull`; package total = 493 LOC; imports THREE via `@pryzm/renderer-three/three` (P2 gate green) |
| A16-T8 | Wire `packages/spatial-index/` BVH into `SelectionManager.ts` as the spatial acceleration structure backing `GPUPicker` fallback queries | C04 §3 | P2 | none | §3 | `DONE` — 2026-05-03: `SelectionManager._rebuildBVHFromCache()` + `_bvhPruneCandidates()` added; BVH rebuilt on every `_selectableCache` cold-build; both click (`performSelection`) and hover (`_onPointerMove`) paths use BVH-pruned candidate set before THREE.Raycaster; non-indexed objects (legacy underlays) always pass through as safety fallback |
| A16-T9 | Implement undo ring buffer cap (200 commands, configurable) in `packages/runtime-undo-stack/src/` | C03 §4.2 | P6 | none | §5 | `DONE` |
| A16-T10 | Add NFT 18 to `apps/bench/src/benches/undo-stack-memory.bench.ts` — "Undo stack memory (4h session, 1000 commands) < 50 MB" | C10 §1 (NFT 18, new) | P8 | none | Part 1 GAP 2 | `DONE` |
| A16-T11 | Amend `C03-SCHEMAS-COMMANDS-AND-STATE.md §4.2` — add: "ring buffer size MUST be configurable (default: 200 commands); exceeding the cap MUST silently discard the oldest entry, never throw" | C03 §4.2 | P6 | none | Part 1 GAP 2 | `DONE` |
| A16-T12 | Amend `C10-PERFORMANCE-AND-OBSERVABILITY.md §1` — add NFT 18 (undo stack memory) + NFT 19 (E2E suite green — see Wave A18) | C10 §1 | P8 | none | Part 1 GAP 2 | `DONE` |
| A16-T13 | Extend `packages/geometry-kernel/` with the migrated geometry subsystem logic from `src/engine/subsystems/geometry/` | C01 §2 | P1 | toward #1 | §2, §3 | `DONE` — 2026-05-03: AS-FOUND. `src/engine/subsystems/geometry/` does not exist in the codebase (confirmed `ls src/engine/subsystems/geometry/` → absent). The geometry subsystem was either never created at `src/engine/subsystems/geometry/` or was previously migrated under a different name. `packages/geometry-kernel/src/` already contains: `csg/`, `edge-projection.ts`, `hidden-line/`, `index.ts`, `math/`, `poche.ts`, `producers/`, `runners/`, `types/`, `view-resolution/`, `dimensions/` — the kernel is populated. No source to migrate; task is complete by absence. |
