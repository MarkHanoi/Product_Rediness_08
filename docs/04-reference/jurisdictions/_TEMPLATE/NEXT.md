<!-- P0 SCAFFOLD TEMPLATE — copy with the folder, replace every <PLACEHOLDER>, delete this comment.
     This is the §5 NEXT.md template from JURISDICTION-PLAYBOOK.md, verbatim in shape.
     TRIP-WIRES (§4) are the point of the whole standard: they are how a find in another
     jurisdiction reaches THIS one automatically. Never ship an empty §4. -->
# NEXT — <PLACE> (<CODE>, <REGION>, <COUNTRY>)

> **What this file is.** The single place recording **where we stopped on <PLACE>, exactly why, and
> precisely what to do to go further the moment it becomes possible** — so a source/technique found
> while working on any OTHER jurisdiction can be brought straight back here.
> **Last updated:** <DATE> · **Maintainer:** <NAME/UNASSIGNED> · **Status:** <SCAFFOLD | …>

## 1 — WHERE WE STOPPED (the one-paragraph truth)
<One honest paragraph. What is solved, what wall we hit, and whether the next gain is the same KIND
of work or a different kind.>

## 2 — THE NUMBER (what % of clicks, which denominator, and why exactly that)
<The number, the named denominator, and the arithmetic that produces it. Failure ≠ empty.>

## 3 — BLOCKERS (each: what · why it blocks · what would unblock it · the EXACT resume step)
### 3.1 — <blocker>
- **What it is.** <…>
- **Why it blocks.** <…>
- **What would unblock it (ascending cost).** <…>
- **THE EXACT RESUME STEP.** <the one measurement/query/read that moves it — a command, not a wish>.

## 4 — TRIP-WIRES (if you see X elsewhere, come back HERE and do Y)
- **4.1 — <if you ever see …>** → come back and <do …> (§3.<n>).

## 5 — WHAT IS ALREADY BUILT (do not redo)
- <capability / file / measurement that must not be re-derived>.

## 6 — VERIFIED SOURCES (endpoint · what it answers · confidence tier · the exact query)
| Source | Answers | Tier | Exact query / note |
|---|---|---|---|
| `<endpoint>` | `<…>` | `<VERIFIED-LIVE / document / …>` | `<…>` |

## 7 — DEAD ENDS (measured negatives — do NOT re-run hoping)
- <negative result, and the reason, so nobody re-attempts it>.

## 8 — THE SMALLEST NEXT STEP that moves the number, and its cost
<The single cheapest measurement or read that would move §2, and what each outcome implies.>
