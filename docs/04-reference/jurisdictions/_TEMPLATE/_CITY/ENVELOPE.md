<!-- CITY ENVELOPE.md (C63 §5 — feeds the ENVELOPE axis). Copy into the dossier, replace <PLACEHOLDER>s,
     delete this comment. Standard: ENVELOPE-REPLICATION-STANDARD.md (ADR-0279) + C58. -->
# Buildable-envelope status — <PLACE> (<code>)

> Feeds C63 **ENVELOPE** axis. The axis score is COMPUTED from `rulepacks/registry.ts packsByZone` × each
> clau's buildable-land share (C63 §3 Axis 4) — never hand-typed. A cited **refusal** is 100 % honest even at
> 0 % complete (C63 §3.1). `certified` needs a signed `sources/VERIFICATION.md` (L-449).

## Rule KIND (ADR-0270 / C58 §2.2)
`<setback | alignment | block-derived-alignment | tiered-occupation | coverage-and-far>` — ⚠ the wrong KIND is
a wrong SHAPE, not a wrong number.

## Coverage by zone / clau
| Zone / clau | Buildable-land share | Pack disposition (`certified` / `constructed-amber` / `cited-refusal` / `no-pack`) | Confidence | Note |
|---|---|---|---|---|
| `<zone>` | `<%>` | `<disposition>` | `<structured/constructed/…/null>` | <…> |

## Refusal ledger (honesty)
Which zones return a **cited refusal** and why (`legal · coverage-gap · construction-incomplete ·
regime-undetermined`). A refusal is a positive cited answer, never a fabricated figure.

## What would raise the ENVELOPE axis
`<action · buildable-share unlocked · effort>` — feeds `RATE-IMPLEMENTATION-PLAN.md`.

---
*Authority: C58 · ADR-0279 (`ENVELOPE-REPLICATION-STANDARD.md`) · C63 §3 Axis 4. Feeds: `RATE.md`.*
