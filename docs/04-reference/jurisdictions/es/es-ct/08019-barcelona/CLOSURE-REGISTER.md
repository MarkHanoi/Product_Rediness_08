# Barcelona — CLOSURE REGISTER (the complete blocker list)

> **Stamp 2026-08-01.** The single place that answers *"what is left before Barcelona is CLOSED?"*
> Companion to [`RATE.md`](./RATE.md) §CLOSURE (which scores axes) — **this file tracks BLOCKERS.**
> Every row must end in a **yes/no**, never an open question.

---

> ## ✍ SIG-4 — THE FOUNDER SIGNED THE 22a DECISION (2026-08-02)
>
> > **22a — Decision: SIGN.** Do not require collection of ~2,600 partial plans.
> > Publish: **municipal framework · quantified delegation · corpus boundary.**
> > Treat delegated plans as **outside scope unless individually analysed.**
>
> **This retires the blocker that required exhausting the derived-planning corpus before 22a could
> publish anything.** It is the corpus-wide publication doctrine applied — *"authoritative publication
> defines the boundary of knowledge, not necessarily the boundary of reality; unknown is a valid
> product state"* (founder, corpus-wide; **ADR in flight — cite it, do not re-argue it here**).
>
> **Landed:** the three named artefacts and nothing else — the framework
> (`BCN_22A_REGIME_NEUTRAL_LIMITS`, already shipped), the **quantified delegation**
> (`BCN_22A_DELEGATION_MEASURED` — **98.92 %** of clau-22a land, complete 81-polygon census,
> 2026-08-02) and the **corpus boundary** ([`CORPUS-BOUNDARY.md`](./CORPUS-BOUNDARY.md)). Recorded
> verbatim with scope, evidence and residual risk at [`sources/VERIFICATION.md`](./sources/VERIFICATION.md) §SIG-4.
>
> ⚠ **THE ENVELOPE AXIS DID NOT MOVE — 36.5 % before, 36.5 % after, verified by re-running the
> scorecard.** Scoping delegation OUT is not resolving it. `not-determined` at weight 0.0 remains the
> correct score (C63 §1.5 / L-656). **A signature that made the number go up would have been the
> failure this signature exists to prevent.**
>
> ⚠ **"Outside scope" is a REFUSAL, not a silent omission** — pinned by
> `__tests__/bcnCorpusBoundary.test.ts` (10 assertions): a 22a parcel reaches a cited refusal naming
> the instrument and Art. 350, never a pack, and the framework's FAR reaches **prose only**, never a
> numeric envelope field (C58 §1.13.3 — `storeyCap` and the generators read those and will extrude one).
>
> ⚠ **Two discrepancies recorded rather than smoothed over.** (1) The instruction was headed *"the 22@
> decision"*; **the signed text says 22a** and names *"~2,600 partial plans"*, which is unambiguously
> 22a's corpus (Art. 350.1) — `22@` is a separate 2.06 % zone closed by DEC-1 under Art. 8.1 MPGM.
> Recorded as signed; if `22@` was also intended it needs its own signature. (2) It also **corrected a
> false statement PRYZM has been shipping to every 22a owner** — *"Neither the cadastral record nor the
> Generalitat's planning map carries it"* — true of the MUC, **false of the AMB Refós**, which PRYZM
> already consumes for clau 18. **Sixth instance of that defect class in this dossier** (after 13b, 22a,
> 22@, bare 20a, 12b).

## The definition of CLOSED — ratified 2026-08-01 (founder)

> **A city is CLOSED when every parcel reaches a TERMINAL, EVIDENCE-BACKED state — either a
> constructed envelope, a cited delegation, or an explicit refusal with a documented reason.**

**It does NOT mean every parcel returns a numeric envelope.** That is unreachable and always was: some
categories are **legally delegated** (Pla Parcial, PERI, Pla Especial, OV) or **discretionary by design**.
Closure is about **exhausting the legal search space and making every outcome explicit** — not forcing a
number where the law provides none.

~~**Barcelona's arithmetic maximum RATE is ≈78 %** (LEGISLATION caps at ~48 % = 12.0/25; ENVELOPE at
~70 % = 14.0/20).~~ ⚠ **SUPERSEDED 2026-08-01 (L-677) — and both inputs were wrong in the same
direction.** Now that all seven axes are MEASURED, the arithmetic maximum is re-derived from the
axes' own cited ceilings: **LEGISLATION ~54 %** (13/24 — the 13 unpacked buildable claus are all
CITED LEGAL REFUSALS, and only `bare 20a` and `18` can ever become packed rows) and **ENVELOPE
~37–38 %** on the C63 axis, *not* the ~70 % coverage figure that was being read as an axis score. The
old ≈78 % was a **mixture of two rulers**. **≈19 of the 22 points that remain are still LAW, not
effort.** A city reporting 100 % would mean we had stopped being honest about what the ordinance does
not say.

> ### ⚠ ENVELOPE CEILING RESTATED — **~70 % → ~68 %**, realistic landing **~65 %** (DEC-1, 2026-08-01)
>
> Closing `22@` as a **permanent** refusal removes its **2.06 %** of private buildable land from the
> land that can ever carry a computed envelope, so the ENVELOPE axis ceiling drops from **~70 %** to
> **~68 %** (≈13.6/20), and the **realistic landing point is ~65 %**.
>
> **That is a MORE HONEST ceiling, not a worse outcome.** The 2.06 % did not become un-answerable —
> it became *correctly* answered, with a cited refusal naming the instrument that governs it. The
> arithmetic maximum RATE moves with it (≈78 % → ≈77 %). Every point removed this way is a point we
> were never entitled to claim.

> ### ⚠⚠ RESTATED AGAIN, DOWNWARD, AND THE PARAGRAPH ABOVE IS THE REASON — **~68 % → ≈62.2 % coverage / ≈37–38 % on the C63 axis** (L-677, 2026-08-01)
>
> **Three separate errors, and the third is the interesting one.**
>
> **① `~68 %` and the C63 ENVELOPE AXIS ARE DIFFERENT NUMBERS, and this register has been quoting one
> as the other.** `~68 %` is **COVERAGE** — the share of buildable land that gets *any* envelope. The
> axis is **Σ(share × tierWeight)**. Barcelona's coverage is **not uniformly `block-constructed`**:
> the `20a/*` family registers `kind: 'setback'` (`esBarcelona20aAillada.ts:535`), can never emit the
> `alignment.depthBinding` row that `ZoningRulesEngine.ts:957` promotes on, and therefore scores
> **0.4**; and the clau-18 OV route is expressly capped at `estimated-ruleset` by **SIG-3's own
> DOES-NOT-AUTHORISE clause**. `MASTER-ROI-TRACKER` §0.6's `56.0 % × 0.7 = 39.2 axis pts` applied the
> block tier to all of it — **a 3.3-point over-statement of the axis.** Measured today the axis is
> **36.5 %** at **60.6 %** coverage.
>
> **② The clau-18 component of `~68 %` was SUPERSEDED and never propagated.** §0.6 priced clau 18 at
> **+4.0…+7.8 axis pts** (≈ 5.7–11.1 pp of coverage). SIG-3 then **MEASURED** it — 26.4 % of clau-18
> land, over all **1,282** clau-18 polygons and 800 live points — which is **4.62 pp**, at or below
> the bottom of that range. The ceiling kept the range.
>
> **③ ⭐ THE ONE THAT MATTERS: row 6 closed `12b` as a PERMANENT cited refusal and NOBODY LOWERED THE
> CEILING.** That is *precisely* the DEC-1 move recorded in the paragraph directly above — the logic
> was written down, ratified, applied to `22@`, and then **not applied the very next time its own
> precondition was met.** `12b`'s **2.44 pp** should have left the ceiling on 2026-08-01 and did not.
> ⚠ Row 6 also states the cost as *"~1.8 % of private buildable land"*, which is the **275-point MUC
> grid** figure; the AMB census figure for the same clau is **2.44 %**. Two denominators, one clau,
> quoted interchangeably.
>
> ⇒ **Corrected COVERAGE ceiling ≈ 62.2 %** = 56.01 (packed, live-re-derived) + 4.62 (clau-18 OV) +
> 1.55 (bare `20a`, which reopens on a subzone **DATA layer**, not on law, and so stays in).
> ⇒ **Corrected AXIS ceiling ≈ 37–38 %.** No tier above `block-constructed` (0.7) is reachable for a
> constructed Art. 242.2 depth: `EnvelopeAxisWeight.ts` requires a determination **ISSUED** for
> `authoritative` and the authority publishing its numbers **AS DATA** for `structured`, and Barcelona
> does neither. **⇒ Barcelona measures 36.5 % against a ~37–38 % ceiling: the ENVELOPE axis is
> essentially FINISHED, and the gap to 100 % is LAW.**
>
> **The transferable lesson, and it is a NEW one for this register: a ceiling is a derived number and
> it goes STALE like any other.** Rows close, estimates get superseded by measurements — and unless
> someone re-derives the ceiling from its components, it silently keeps quoting the inputs that were
> replaced. **Re-derive the ceiling in the same pass that closes the row.**

## The standard every blocker must meet before it closes

**Evidence → Findings → Decision → Alternatives rejected.**

⚠ **No blocker may rest at *"needs founder decision"* until it is PROVEN that no authoritative evidence
exists.** A decision taken before the evidence is exhausted is a guess with a signature on it.
**Negative evidence closes a blocker** — *"no authoritative resolver exists, here is where we looked"* is
a valid, permanent closure.

## Taxonomy (ratified 2026-08-01)

| | Bucket | Unblocked by |
|---|---|---|
| **A** | **Evidence** | primary sources — DOGC, RPUC, municipal GIS |
| **B** | **Engineering** | implementation |
| **C** | **Contract** | architecture change |
| **D** | **Product policy** | a founder decision, *after* A is exhausted |

---

## The register

| # | Blocker | Bucket | Sev | Status | Closes when |
|---|---|:--:|:--:|---|---|
| **1** | **clau 18 certification** | B | P1 | ✅ **SIGNED SIG-3 2026-08-01, flag applied** — verified in code (`BCN_REFOS_OV_CERTIFIED === true`, `bcnRefosOVProvider.ts:90`; dispatch branch live at `siteDispatch.ts:4042`) | ✔ done — ENVELOPE **COVERAGE** 56.01 → **60.63 %** (re-derived live 2026-08-01: 17.51 % × 26.4 % = **4.62 pp**; `VERIFICATION.md`'s +4.68 pp is the same figure at coarser rounding). ⚠⚠ **THE C63 AXIS GAINS +1.85 POINTS, NOT +4.62** — SIG-3's own *DOES NOT AUTHORISE* clause caps this route at `estimated-ruleset` (**0.4**), not `block-constructed` (0.7). **This row conflated coverage with the axis score; so did §0.6 and so did the ceiling.** See the ceiling restatement above. |
| **2** | **L-660 — Arts. 327/328 superseded** | **D** | **P0** | 🟠 binding DOGC 4893 annex **retrieved**; `applied === false` | founder signs **SIG-2**. Everything else is prepared. |
| **3** | **clau 12 has no geographic predicate** | ~~A~~ → **A (narrowed)** | P2 | ✅ **ENGINEERING HALF CLOSED — §CLAU-12-PREDICATE (L-674), 2026-08-01** | ✔ **There is no geographic predicate to write, and writing one would be the error.** PGM **Art. 315.2** states *which nuclei* the two subzones were drawn for; it **delimits neither** — no boundary, no district, no coordinate — and it **hedges** the 12b half (*"referida **preferentment** a aquell"*). Art. 315.1 scopes only the ZONE. ⇒ **Classification `NOT-THE-RULE-KIND`:** the operative predicate is the ***plànol d'ordenació*, parcel by parcel, which the MUC serves and PRYZM already reads.** A polygon test coded in PRYZM would be a **second, weaker classifier that could only ever ACT where it DISAGREED with the plànol** — i.e. override the operative instrument exactly when doing so was wrong (`mucZoningProxy.js`'s standing warning in a new costume; C58 §1.11). Pinned as `BCN_CLAU_12_GEOGRAPHIC_PREDICATE_EXISTS = false` + `BCN_CLAU_12_PREDICATE_FINDING`. **Engineering mitigation shipped:** the `12` / `12b` **disjoint-code-path invariant** is now asserted, not commented — `12b` is absent from `BCN_NUCLI_ANTIC_ZONE_CODES` and from `CLASSIFICATIONS`, so the feared failure can arise **only from a wrong clau in the SOURCE, never from a routing mistake in PRYZM**. ⚠ **What survives, narrowed and re-bucketed:** *"is the MUC faithful to the plànol in Ciutat Vella?"* — an **evidence question about a data source (A)**, **not** a rule PRYZM failed to encode, and **not reducible by a predicate**. Severity drops P1 → P2: it is a source-fidelity risk, not an un-encoded rule. **ENVELOPE delta +0.00 pp** (the 9.38 % has been packed since 2026-07-22). See `claus/12/CLAU.md`. |
| **4** | **Block dissolve fails on real Eixample parcels** | A+B | ~~P1~~ **P3** | ✅ **CLOSED — MEASURED, and the row's own premise is REFUTED (§BLOCK-DISSOLVE-AUDIT, L-676), 2026-08-01** | ✔ **The criterion this row wrote for itself is met, and the answer inverts it.** Tool committed + re-runnable: `npx tsx tools/block-dissolve-audit/audit.mts` — it imports the **PRODUCTION** dissolve (the same function `siteDispatch.ts:4306` calls), so it cannot disagree with the path it measures. Corpus: **108 complete Eixample/Dreta *manzanas*, 1 917 parcels** (+ Ciutat Vella **77 / 960** as a second Barcelona sample), each parcel carrying its **published cadastral `areaM2`** — the INSPIRE official registry area, a number the dissolve never sees. **RESULT — Eixample 104/108 = 96.3 %** (exact 93 · t-junction-split 11), **Ciutat Vella 74/77 = 96.1 %**; **every** failure in both samples is `open-or-disjoint`. ⚠⚠ **THE PREMISE — *"the only remaining item that can make an ALREADY-PUBLISHED number wrong"* — IS FALSE, structurally.** A dissolve FAILURE reaches `refuseConstructionIncomplete('block-dissolve-refused')` (`siteDispatch.ts:4307`) and publishes **no envelope at all**; the only channel that can publish a wrong number is a dissolve that **SUCCEEDS on the WRONG ring**, and against the published areas that channel measures **ZERO**: **max deviation 0.34 % (Eixample) / 0.84 % (Ciutat Vella) over 178 rings, 0 alarms at a 2 % threshold.** ⇒ **SIGN: UNDER-STATES.** **This is the FOURTH time this register has recorded the same lesson — establish the SIGN before ranking the severity.** ⭐ **Clustered or random, answered by MECHANISM not by a statistic:** with k=4 of n=108 a spatial permutation test has no power, so the tool prints `INSUFFICIENT-POWER` and computes no p-value rather than dress absence-of-detection as detection-of-absence; instead it re-dissolves every failure across a 0.10→1.00 m ladder. **`tolerance-tail` cases: ZERO.** All 7 failures close at **no** tolerance ≤ 1.0 m — 10× the shipped `TJUNCTION_SPLIT_TOLERANCE_M` — so **no tolerance change could recover any of them.** Mechanisms: **3 `disjoint-blocks-under-one-prefix`** (the 5-char refcat prefix collected TWO REAL blocks — a block-**IDENTITY** defect already named in `SPAIN-DISSOLVE-FAILURE-TAXONOMY.md`, and there is no single correct ring to produce) + **4 `structural-input-defect`** (the published tiling). ⇒ **Neither "engineering bug" nor "robustness": the failures cluster on a mechanism that is not ours.** **56 % IS realisable** — the geometry layer delivers 96.3 %. ⚠ **Two things this does NOT show**, stated rather than hidden: the corpus is a **census of two bboxes**, not all Eixample (stronger than a random 100 for detecting a cluster it contains; silent about an unsampled bbox); and the oracle catches a wrong loop, not an area-preserving error. ⚠ **One NEW attribution:** Eixample `manzana 00329` dissolves **correctly** to **326 vertices** and is then hard-rejected by **C19 §7.3** — already open as **C19 KV-3**, now with a named Barcelona block; **UNDER-states**, and **must not** be closed by silently simplifying a ring that feeds a compliance number. **Residual lever (deferred, not refused):** the `manzanaPrefix` heuristic in `server/parcelZoningProxy.js`, 3/108 in the Eixample — **shared across Madrid/Córdoba/València**, so it must be measured against live Catastro before it lands. See `findings/L-676-BLOCK-DISSOLVE-EIXAMPLE-MEASURED.md`. |
| **5** | **clau 22@ has no *profunditat edificable*** | A→D | P2 | ✅ **CLOSED — permanent cited refusal (DEC-1), 2026-08-01** | ✔ done. The "prove no authoritative geometry exists" precondition was **met**: founder research across BCNROC, the 2024 municipal *Instrucció* on 22@ interpretation and the 2025 MPGM amendment found **no manual, no CAD/GIS geometry, no permit guidance** defining a city-wide depth — because 22@ resolves through **PMUs, *fitxes urbanístiques* and *plànols d'ordenació***. ⇒ **The omission is INTENTIONAL**, so this is not a sourcing gap and never becomes a rule-KIND call. `22@` ships a legally-grounded `derived-plan` refusal naming **Art. 8.1** and pointing at the derived instrument (`barcelona22ArrobaDerivedPlanRefusal`; pack stays OUT of `packsByZone`). ⚠ **Reopens only on** a published city-wide geometric specification for 22@, or an official instruction converting PMU/ordering-plan geometry into a computable depth. See `FOUNDER-DECISIONS-2026-08-01.md` DEC-1 + `BCN_22ARROBA_DEPTH_CLOSURE`. |
| **6** | **clau 12b — neighbour heights** | ~~A→B~~ → **A (governance)** | P2 | ✅ **CLOSED — permanent cited refusal, §CLAU-12B-TRAM-UNDEFINED (L-676), 2026-08-01** | ✔ **The row's own test — *"if the municipality derives it from an official dataset ⇒ engineering; if not ⇒ governance"* — resolves to GOVERNANCE, and the deciding fact is not about data at all.** Read **verbatim from the committed primary text** (`PGM-NNUU-metropolitana.pdf`, extracted directly this pass, printed pp. 105–106): **Art. 320.3a** — *«A la subzona II, de conservació del centre històric, l'alçada en **un tram de vial** serà la **mitjana de les edificacions existents**, sense que entrin al còmput les façanes dels solars no edificats. El nombre màxim de plantes admès serà… suposar una alçada mínima de planta baixa de 4 m. i una alçada mínima, inclòs el forjat, de 3,05 m. per planta pis.»* ⇒ the article **DOES** define the statistic (a **MEAN**, not the tallest), the population (**unbuilt plots' façades expressly EXCLUDED**) and the storey derivation. ⛔ **What it NEVER defines is *un tram de vial* — the DOMAIN the mean is taken over.** No length, no one-side-vs-both rule, no corner rule, no block boundary. ⭐ **And the undefined term is the one that FIXES the number**: widen or narrow the *tram* and the mean moves without bound. ⭐⭐ **THE DECIDING EVIDENCE IS ONE PARAGRAPH EARLIER IN THE SAME ARTICLE: Art. 320.2a** — *«…la profunditat edificable serà, com a màxim, la de les edificacions contigües existents, **mentre es redacti la determinació en particular i en detall al pla especial**»* — for the **same subzona**, on the **neighbouring parameter**, on the **same "match what is already there" premise**, the plan says the particular and detailed determination belongs to a ***pla especial***. **Art. 316.3** closes it: *«…no s'admeten les ordenacions de volums que impliquin modificacions de l'ordenació existent o de les alçades usuals.»* ⇒ **Classification `NOT-THE-RULE-KIND`: a rule whose DOMAIN is delegated.** **NEGATIVE EVIDENCE, with the search named:** **ICGC DOES publish height-capable models** (MDS 1 m Catalonia / **25 cm AMB**, MDT 25–50 cm, third LiDAR flown 2021–23, published Feb 2026; height derivable MDS−MDT) — but **no discrete licence-stamped *"alçada dels edificis"* product** was located; **Open Data BCN**: searched the `Edificis` tag, **no building-height dataset found** (searched, not catalogue-walked); the municipal WMS `ALCADES` field is *"informatiu, no normatiu"*; and **nothing in the *llicència d'obres* / *Informe d'Idoneïtat Tècnica* material references any LiDAR/MDS product as an input to an alçada determination.** What Ciutat Vella has instead is **instruments** — the *Pla especial de protecció del patrimoni … Districte de Ciutat Vella* (2000) and the four PERIs — **exactly the *pla especial* Art. 320.2a points at.** No interpretive *instrucció* or appeal found (BCNROC, CIDO, open web). ⇒ **`NEXT.md` §3.4/§4.3 are WITHDRAWN as written:** *"acquire a neighbour-height source and it resolves"* is false — **a height dataset supplies HEIGHTS, it cannot supply a *TRAM***, and choosing a radius would be PRYZM inventing the one term the plan withheld (C58 §1.11; the same error §CLAU-12-PREDICATE refused for blocker 3). ⚠⚠ **SIGN: UNDER-STATES — and the alternative has NO DEFINED SIGN, which is the stronger argument.** Refusing costs ~1.8 % of private buildable land, bounded and visible; constructing a mean over a *tram* PRYZM chose could land **either side** by an amount nobody can bound, because the error is the DOMAIN, not the measurement. **A number with an unknown sign, published under a legal citation, is worse than a bounded under-statement.** **SHIPPED:** `barcelona12bNeighbourMeanRefusal` — legally-grounded `derived-plan`, `BCN_12B_ORDINANCE_REF`, **no figure in the prose** (§DEC-1 leak rule). ⚠⚠ **It also fixed TWO shipped FALSE STATEMENTS ABOUT OUR OWN COVERAGE — the 5th and 6th of that pattern** (after 13b, 22a, 22@, bare 20a): `12b`'s coverage-gap copy was giving **subzona I's shape argument** — *"the façade sits on the street line … the wrong SHAPE"* — which is clau `12`'s reasoning, **and clau 12 SHIPPED A PACK on 2026-07-22 on exactly it**; and `BCN_ROADMAP_LINE` promised *"Next: 12b, whose rules are a survey of the existing neighbours"*, **a promise PRYZM cannot keep**, presenting a legal delegation as an engineering backlog item. **Both DELETED, not out-ranked.** ⚠ Consequence: **no ENUMERATED Barcelona clau is a coverage gap any more** — the card is now the fallback for an unenumerated clau, and the tests were re-aimed at that rather than left vacuous. ⚠ **Reopens only on** a *pla especial* or municipal instruction that **DEFINES the *tram de vial*** — a delimitation, not a dataset. See `findings/L-676-CLAU-12B-TRAM-UNDEFINED.md`. |
| **7** | **bare `20a` subzone resolver** | A→B | P3 | ✅ **CLOSED — cited `regime-undetermined` refusal (§BARE-20A-EXHAUSTED, L-673), 2026-08-01** | ✔ done, and **the exhaustive search ended in the PRIMARY TEXT, not in a portal** — which the register itself accepts as a valid permanent closure. **PGM Art. 314.5** (the plan's own *qualificacions zonals* catalogue) and **Art. 338.2** enumerate **ten** subzone qualificacions, **every one suffixed**, and **no unsuffixed `20a`**; **Arts. 340.1 · 342.1/.2/.3/.4/.5/.8 · 343.1/.2** key **every** envelope parameter — edificabilitat, minimum parcel, minimum façade, occupation, height, storeys, all three boundary separations — **to that suffix, and state none of them for the zone.** ⇒ Bare `20a` names a *zona*; the *qualificació* that carries numbers is `20a/N`. **What is missing is a SELECTOR, not a rule**, so no further reading of the plan can close it and no pack may be built on a "representative" subzone (the indices span **×6**, occupation **×4**, front separation **9 m**). ⚠ **The refusal CODE changed and that is the substance:** bare `20a` was getting `no-rule-pack`, whose copy said PRYZM did not hold *"THIS subzone's own sourced numbers: its separations, its minimum parcel size, its maximum occupation and its net buildability index"* — **all four have been in `bcn20aSubzones.ts`, for all ten subzones, since 2026-07-22.** Same false-statement-about-our-own-coverage defect that removed `13b`, `22a` and `22@` from `coverageGapReasonFor`; the branch is **DELETED, not out-ranked**. Now `barcelona20aSubzoneUndeterminedRefusal` — `regime-undetermined`, `legallyGrounded: false`, citing `BCN_20A_BARE_ORDINANCE_REF`, **with a mirrored §DEC-1 leak test: no figure in the prose.** It states the ONE subzone-neutral fact — **Art. 339, the ordination type is *edificació aïllada***, a rule KIND, not a number. ⚠ **The ceiling does NOT drop, unlike `22@`'s:** 22@'s omission is intentional in the law; bare `20a`'s numbers **exist** and a subzone-granular layer would return all **1.55 %** to the computable set. **ENVELOPE delta +0.00 pp** — a correctness move, not a coverage move. See `claus/20a/CLAU.md`. |
| **8** | **Terrain measured at the wrong datum** | B | P1 | 🟡 **HALF-CLOSED — the LEGAL half is done, the DATA half is the binding constraint (2026-08-01)** | ✅ **§V7 ANSWERED, and the answer is that nothing needs constructing.** **PGM-1976 Art. 240 «Regles sobre determinació d'alçades» STATES the entire rule**, read verbatim from the primary text in this repo (`PGM-NNUU-metropolitana.pdf`): **240.1.a** measure at the CENTRE of the façade from the *rasant de la voravia* when the highest end is < **0,60 m** above it; **240.1.b** otherwise from **0,60 m below the highest end** of the façade line; **240.1.c** divide the façade into *trams* wherever the pavement falls more than **3 m** below the datum, each tram re-measured «com si cada tram fos façana independent»; **240.2** one front; **240.4** two non-adjacent fronts = independent buildings. ⭐ **AND THE CORNER CASE IS STATED, in two limbs**: **240.3.a** equal regulated heights ⇒ develop the fronts as ONE façade («operant amb el conjunt de les façanes desenvolupades com si fos una de sola»); **240.3.b** different heights ⇒ the greater runs along the narrower street for max(1,5 × width, the *profunditat edificable* prolongation), capped at **30 m**. Transcribed + tested: `packages/site-parcel-data/src/geometry/facadeRasantDatum.ts` (28 tests). 240.3.b **REFUSES with the article quoted** — PRYZM holds neither the xamfrà flexion/tangency point nor the profunditat prolongation, and that is a gap in **us**, not in the plan (`legallyGrounded: false`). ⛔ **STILL OPEN — the DATA half, and it now clearly dominates.** Probe **V8**: served terrain posts at **57.34 m**, source **MDT25**, requirement **≤ 10 m** for a 20 m street; the Eixample centroid→façade distance is ~56.6 m, i.e. *the same cell*. **Wiring the sampler onto today's surface would close this item FALSELY.** The module ships a hard `terrain-posting-too-coarse` refusal so it cannot. **What closes #8: the MDT05 bake (V8 §V8.6, a two-value config change), then wire `resolveParcelRasantDatum` into the viewport's `formaTerrainBaseHeight` seat.** |
| **9** | **Supersession audit unexhausted** | A | ~~P1~~ **P2** | 🟡 **OPEN, MATERIALLY ADVANCED — 147 unread → 46 to read, 3 READ (§RPUC-SUPERSESSION, L-676), 2026-08-01** | ⚠ **It does NOT close, and saying so is the point** — one candidate is an unread scan and 41 remain unopened, so `NOT_VERIFIED` stands. What changed: **(1) the funnel is now a LIVE QUERY, not a number in a document.** `node tools/rpuc-supersession/classify.mjs` re-derives **1 755 → 773 → 147** against the RPUC live (`planejamenturbanisme.territori.gencat.cat`; ⚠ the old `rpucportal.…` host **no longer resolves**), reproducing L-660 **exactly** — 145 `Modificació de pla general d'ordenació` + 2 `Modificació normes subsidiàries`. Deliberately **not** frozen to a fixture: an instrument approved tomorrow is what the audit is looking for. **(2) Two ORTHOGONAL screens, whose UNION survives — scope + subject.** *Scope*: only a modification of the ***Normes urbanístiques*** can rewrite an article's TEXT; a delimited-àmbit instrument cannot. *Subject*: does the title name *alçada reguladora* / *profunditat edificable* / *interior d'illa* / *nucli antic* / *alineació de vial*? ⭐ **A subject hit survives WHATEVER its scope**, because a site-specific plan can **disapply** a general article inside its own àmbit and PRYZM, routing on the MUC clau, would never see the override. **Anything matching neither screen is `AMBIGUOUS` and joins the list — the screen FAILS OPEN, and a test pins that.** ⇒ **46 of 147 must be read; 101 set aside — a 69 % reduction.** **(3) THE TOP OF THE LIST WAS OPENED — a TEXT check, not a title check.** The screen independently recovers exactly L-660's family (art. 264 · aparcaments · equipaments · 22@ Normes) **plus the 2007 baseline instrument itself**, which is the cheapest available proof it is not dropping normative modifications. Of the four genuinely-later ones, **three were downloaded and text-checked with a passing DIGIT-INTEGRITY gate** (L-660 measured that these PDFs mis-render digits while reading as fluent Catalan, and an article search *is* a digit search — so the tool counts digit glyphs and refuses a clean result below a floor): **2018/067099/B** amends **Arts. 297–300** (aparcaments); **2023/080788/B** amends **Arts. 212–217** (equipaments); **2024/082320/B** (22@, 741 923 chars) amends **its own Arts. 1–40**. **None names 239, 320, 327 or 328.** ⛔ **2009/036679/B (art. 264) FAILED the gate — both documents are image-only SCANS (23 p → 22 chars). It has NOT been read**; its *title* names Art. 264, but a title is register metadata, not the instrument. ⚠ **TWO CONFIDENT-WRONG TRAPS CAUGHT LIVE, both false positives a bare grep would have filed as amendments:** *`Decret 328/2006`* (a Catalan decree, not a PGM article) and every `320`/`327` in the 22@ document (**area / *sostre* figures** — `320 m²`, `70.320 m² st`, `26.327`). **SIGN: EXACT if the not-found holds; UNKNOWN if it does not — this row has NO protective sign**, since an amendment could move a height either way. That is precisely why the honest move was to shrink the unread set and raise `confidence: medium → medium-high`, not to declare it answered. **Remaining ~½ day** (41 documents + 1 render-and-read), mechanical, recipe committed. ⚠ **Do not write `laterModifications: none` anywhere.** See `findings/L-676-SUPERSESSION-READING-LIST.md`. |
| **10** | **13E / 2002 *Ordenança de l'Eixample* — force UNKNOWN** | A | **P1** | ✅ **CLOSED — IN FORCE (L-667), 2026-08-01** | ✔ done. Resolved to **ACTIVE**, on the **primary source**: the founder read the 2026 repeal ANNEX itself (`GM_ordenanca-derogacio-consell-municipal-annex_2026.pdf`, hdl `11703/144636`). It repeals the **1986** Eixample ordinance and contains **no express reference** to the 2002 *text refós*, to clau `13E`, or to the provisions creating the subzone. ⚠ **This is the annex text, NOT the BCNROC `dc.relation.replaces` catalogue field** — the earlier "SURVIVES" draft rested on metadata and was withdrawn as an overclaim. Engineering: `13E` implemented as a **SUPPLEMENT over `13a`** (DEC-2) — one rule set, one explicit delta, **declared empty**. ⚠ **Reopens only on** an express repeal of the **2002** framework in a source not yet reviewed; the 1986 repeal is *not* that. **Residual (not this blocker):** transcribing the 2002 courtyard/*patis* rules — and it must start from the **CURRENT consolidated text** (Art. 15: 2012 partial nullity + 2018/2019/2023 modifications). See `findings/L-667-13E-IN-FORCE-CLOSED.md` + `BCN_13E_SUPPLEMENT`. |
| **11** | **Tribunes / *cossos sortints*** | ~~A+B P1~~ → **D** | **P3** | ✅ **CLOSED AS AN ENVELOPE BLOCKER — §COSSOS-SORTINTS (L-672), 2026-08-01** | ✔ **A tribuna is NOT part of the envelope. It is a permitted projection BEYOND it — settled by the PGM's own definition, and the ordinance is not ambiguous.** **Art. 223.2.g**: *«Cossos sortints. Són els que **sobresurten de l'alineació de façana**…»* — **defined by exceeding the alignment**, so it cannot lie inside the envelope whose outer surface IS that alignment. **Art. 229.2** names *«els miradors, **tribunes** i similars»* as the enclosed variety. **Art. 230** measures the *vol* **outward from the façade plane**, per ordination type. ⇒ **Classification `NOT-THE-RULE-KIND`:** it is not an envelope parameter of any KIND — it is a **morphology allowance OVER a resolved envelope**, and it can only be evaluated once an envelope exists, the **opposite dependency order** from the one this row assumed. ⚠⚠ **THE DIRECTION, WHICH IS WHY THE P1 WAS MISPLACED — measured per ordination type, not assumed:** *alineacions de vial* (13a/13E/13b/22a/22@, Art. 230.I) ⇒ omission **UNDER-states**, safe; ***edificació aïllada*** (`20a/*`, Art. 230.II — the projection is charged against the **same** occupation % and the **same** boundary separations the envelope is already drawn to) ⇒ omission is **EXACT**; clau `12`/`12b` (Art. 320.5a **prohibits** them, and Art. 230.I **excludes the nucli antic by name**) ⇒ omission is **near-exact**. **In no shipped Barcelona clau does omitting them over-state.** ⇒ The 13a pack header's *"a live risk, not a theoretical one"* rested on an unexamined premise and **is withdrawn**; folding tribunes IN is what would have over-stated. Invariant pinned as `BCN_COSSOS_SORTINTS_NEVER_OVERSTATE`. ⚠ **What genuinely remains is `D` (product policy), not `B`:** enclosed/semi-enclosed projections **count toward *sostre*** (Arts. 224.2 + 229.3.a/.b), so they matter for **saleable-area** reporting — and for `13a`/`13b` even that is moot, because Art. 322 defines edificabilitat as the *envolupant màxima de volum* (ADR-0271) with **no FAR ceiling to consume**. Figures transcribed and cited in `esBarcelonaCossosSortints.ts`, **recorded not applied**. **ENVELOPE delta +0.00 pp.** |
| **12** | **Art. 238 residual clauses** | ~~A+B P1~~ → **A** | P2 | 🟡 **STATUS ESTABLISHED — the core IS fixed and PINNED; four residuals, all A (data). No B work is open. (L-675, 2026-08-01)** | ⚠⚠ **FIRST, WHAT IS *NOT* BROKEN.** The **Art. 238.1.b/c MINIMUM** *amplada puntual* — the statistic itself — **shipped on 2026-07-22 (`0edd302b`) and was test-pinned on 2026-07-31 (`8e590846`)**; both are **ancestors of `main`, verified with `git merge-base --is-ancestor`**, and `governingPunctualWidth` returns `sorted[0]`. **`MASTER-ROI-TRACKER.md` §0.6 "Open risks" 2 still says "uses the MEDIAN … Fix in flight" and is STALE — corrected in this pass.** Do not re-fix it. **Residuals, with their DIRECTION measured rather than assumed:** **12A** *238.1.b tram partition* — we take the minimum per **cadastral edge**, the ordinance per ***tram* between two cross-streets**. ⚠ Partly **already neutral**: `governingStreetWidth` takes the **narrowest** across the edges a parcel fronts, so a multi-edge frontage is already minimised. The residual over-statement is only the part of the tram the parcel does **not** front. **⇒ NEWLY IDENTIFIED and engineering-closable with NO new data:** chain consecutive near-collinear block-ring edges into one *tram* before taking the minimum (Art. 236.3.g defines the *illa* as bounded by contiguous street alignments, so ring corners ≈ cross-streets). **Deliberately NOT shipped in this pass** — it moves published heights across Barcelona *and* Madrid on a shared geometry path, and the change must be measured against live Catastro before it lands, exactly as `8e590846` was. **12B** *238.1.d averaging* — cuts **opposite** to the minimum ⇒ omission **UNDER**-states. **SAFE**, and it needs the opposing block's zoning (*"sòl d'igual zonificació"*). **12C** *238.2 real afectació a l'ús públic* — needs a public-way layer; today any gap between frontages counts as a *vial*. **12D** *official alignments* — Art. 238.1.c measures between *alineacions oficials* (Art. 236.3.a: *«la línia que estableix límits a l'edificació al llarg dels vials»*); we measure between **cadastral boundaries**. Under this ordination type the two normally coincide (Art. 237.1) and the substitution is priced by `BAND_EDGE_GUARD_M`'s 0,5 m allowance and carried as `measured-cadastral`, **never as an *ample oficial***. Barcelona's only alignment layer is a **WMS raster** with no queryable geometry ⇒ digitise once, never again. ⇒ **12B/12C/12D are DATA (A). 12A is the one engineering lever and it is now named and sized.** **ENVELOPE delta +0.00 pp.** |
| **13** | ~~16.7 % band-edge refusals~~ | **D** | P2 | 🟡 **RECLASSIFIED — not a blocker** | This is the honesty contract **working**: the parcel spans a width threshold and we decline to pick. **A UX decision, not a legal one** — show *"Height range 15,70–19,05 m · parcel spans a band threshold"* rather than silence. No fabrication either way. |
| **14** | **C63 tier vocabulary does not exist in code** | ~~C~~ → **residual re-bucketed B** | ~~P0~~ **P1** | ✅ **CLOSED AS A CONTRACT BLOCKER — §ENVELOPE-CONFIDENCE-LADDER (L-664, commit `5e7adee6`), VERIFIED 2026-08-01** | ✔ **The row was STALE — the ruler was fixed on 2026-08-01 and the register had not caught up.** Verified rather than assumed: `git merge-base --is-ancestor 5e7adee6 HEAD` ⇒ **true**. **C63 was AMENDED IN PLACE** (§3.2/§3.3/§4.1, no derivative `*-AUDIT.md`) to the SCHEMA's vocabulary, with the exception to *"when code disagrees with a contract the code is wrong"* **argued, not assumed**: `structured` (numbers PUBLISHED) and `authoritative` (a determination ISSUED) are different facts and `certified` collapsed them, which would let a published-but-undetermined value score a perfect axis. `packages/schemas/src/site/completion/EnvelopeAxisWeight.ts` defines `ENVELOPE_AXIS_TIER_WEIGHT` as a **TOTAL `Readonly<Record<EnvelopeCoverageTier, number>>` with no default branch** — authoritative 1.0 · structured 0.9 · block-constructed 0.7 · estimated-ruleset 0.4 · pipeline-extracted-unverified 0.1 · not-determined 0.0 — plus the `'no-pack'` sentinel at 0.0, kept as a **separate word** because `not-determined` is a CITED legal refusal and `no-pack` is PRYZM's own coverage gap (they score alike and mean opposites). `tools/city-completion/computeScorecard.mjs:114` mirrors it byte-for-byte (drift = test failure), both stamped `ENVELOPE_AXIS_TIER_WEIGHT_VERSION = 'provisional-2026-08-01-L664'`. ⇒ **Contract compliance is now provable and the ENVELOPE axis is SCOREABLE.** ⚠⚠ **WHAT SURVIVES, RE-BUCKETED `C` → `B`, AND THE ROW'S OWN DIAGNOSIS WAS HALF-RIGHT:** the axis **still reads `not-assessed`** — measured today, `node tools/city-completion/computeScorecard.mjs --cities barcelona` ⇒ `envelope: {score: null, unknownReason: 'pending-implementation'}`, *"the tier→weight ladder exists (C63 §3.2, L-664) but the MEASUREMENT does not"*. **The ruler existed-not; now the READING does.** That is a per-clau × buildable-land-share coverage breakdown — **engineering, not architecture**, and no longer platform-wide (the ladder is city-agnostic and shipped once). ⚠ **Three of the six weights are PROVISIONAL pending founder ratification** (C63 §8, like the `derived-levels` HEIGHTS question) — the *vocabulary* and *ordering* are settled by L-664; the interior magnitudes are a weighting call. **SIGN: EXACT.** An unscoreable axis is renormalised OUT of the headline (C63 §1.5), never scored 0 — Barcelona today reports **87.4 % over `weightAssessed: 45 %`**, which neither over- nor under-states, it declares its own denominator. ⬆⬆ **THE RESIDUAL `B` IS NOW CLOSED TOO — FULLY CLOSED, 2026-08-01 (L-677).** The MEASUREMENT landed: `tools/city-completion/measurements/barcelona.measurements.json` supplies a **ten-slice per-clau × buildable-land-share coverage breakdown summing to 100.00 %** of a live-re-derived denominator (**31,794,683 m²**, AMB Refós per-clau area census, `CODI_INE='08019'`), and the axis now scores **`envelope: 0.36526`, `validationState: auto-validated`**. `node tools/city-completion/computeScorecard.mjs --cities barcelona` ⇒ **all seven axes assessed, `weightAssessed: 100 %`, overall 58.1 %, `honestyOk: true`** — the first complete C63 card in the repo. **RE-VERIFIED IN CODE, NOT INHERITED:** `packages/schemas/src/site/completion/EnvelopeAxisWeight.ts` exports `ENVELOPE_AXIS_TIER_WEIGHT` as a TOTAL `Readonly<Record<EnvelopeCoverageTier, number>>` with no default branch; `computeScorecard.mjs:114` mirrors it and `computeScorecard.test.ts` asserts `deepEqual` **plus** exhaustiveness over `ENVELOPE_COVERAGE_TIERS` **plus** that an unmapped tier THROWS — three tests, all green this pass. ⚠⚠ **THIS ROW HAS BEEN POISONING OTHER CITIES.** Its ORIGINAL claim — *"the C63 tier vocabulary does not exist in code, therefore no city can prove an ENVELOPE score"* — was copied into other dossiers and survived there after it was false here (València corrected its inherited copy in `716041f1`; Murcia had already computed its ENVELOPE axis at 9.4 % **through this very map**). **A blocker written in one city's register is not scoped to that city, and a stale one propagates faster than its correction.** Closing at the source is the fix; the transferable rule is **re-verify an inherited claim against the code before repeating it**. ⚠ Three of the six weights remain **PROVISIONAL** pending founder ratification (C63 §8) — the *vocabulary* and *ordering* are settled, the interior magnitudes are a weighting call. |
| **15** | **Hand-drawn boundary → `estimated-default`** | B | **P0** | 🔵 agent in flight | Never render fabricated geometry. No geometry until a boundary source exists; an approximate outline must render in a **visibly different style**, never the same as a real one. `honestyOk`-blocking. |
| **16** | **PARCEL axis unmeasured** | B | P1 | ✅ **CLOSED — MEASURED at 99 %, 2026-08-01** | ✔ done. `node tools/city-completion/computeScorecard.mjs --cities barcelona` ⇒ **`parcel: 0.9917`, `validationState: auto-validated`**, derivation: *mean over **N=120** sample of {high:1, medium:.5, low:0, no-parcel-here:0} (**high=118, medium=2, low=0, none=0**) via **catastro** (cadastral) in bbox `[2.09,41.32,2.23,41.47]`*, over the OSM non-public building-footprint denominator (the conservative independent proxy for private buildable land, L-656). ⚠ **The 2 `medium` are NOT rounding — they are the axis working**: a Catastro miss is scored 0.5, not 1, and the derivation names the count rather than the score alone. **SIGN: EXACT** — the axis measures what it says (Catastro reach over a bbox); it neither claims coverage it lacks nor discounts coverage it has. ⚠ **What it does NOT say:** the axis is `auto-validated`, not human-verified, and the denominator is an OSM PROXY, stated in the derivation string rather than hidden. |
| **19** | **`PD*` — the clau we read is a TRANSLATION of a derived instrument, on 70.69 % of buildable land** | **A** | **P1** | 🟠 **OPEN — `missing-evidence`, NEWLY QUANTIFIED 2026-08-02 (§SIG-4 census). It is the highest-value open question in the city.** | ⚠⚠ **NOT A NEW FINDING — AND THAT IS THE PROBLEM.** [`L-590c`](./L-590c-PLA-PARCIAL-REGIME-RESOLVED.md) §7 recorded it on 2026-07-2x in these words: *"The `*` suffix on `PD*` … is the **assimilation marker**, and it is telling us every time that the clau we read is a **translation, not the governing text**. We have been consuming the translation and citing it as the original."* It asked for an ADR. **The ADR was never written and no register row was ever opened, so the finding sat in a findings file for a week while the axis was published.** ⭐ **NOW MEASURED, live and re-runnably** (AMB Refós `PLAN` field, grouped by `CLAU_URB`, over the 31,794,683 m² buildable denominator): **`PD*` = 70.69 %** of private buildable land, `PG` = 29.31 %. Per clau: **`13a` 68.6 % · `12` 80.9 % · `13b` 37.4 %** — **the three families PRYZM publishes CONSTRUCTED envelopes on at `block-constructed` (0.7)** — plus `18` 97.1 %, `22a` 98.9 %, `22@` 100 %. ⚠⚠ **WHAT IS *NOT* BEING CLAIMED, AND THIS IS THE WHOLE DISCIPLINE OF THE ROW.** **MEASURED:** the Refós records a derived plan as the governing instrument on that land. **UNKNOWN:** whether it *displaces* Art. 242.2's *profunditat edificable* construction or merely *assigns the clau* whose article PRYZM then applies correctly. For `22a`, **Art. 350.1 says the Pla Parcial sets the regime, explicitly**; for `13a`/`13b`/`12` **no article has been read that says so — and none has been read that says otherwise.** ⇒ **NOTHING HAS BEEN DOWNGRADED.** Moving the ENVELOPE axis on a field whose semantics nobody has read would be *"inference beyond the demonstrated spatial extent"* — the exact move the founder's publication doctrine forbids — and **this register has recorded FOUR separate occasions where a gap was ranked before its sign was measured** (rows 4, 11, 12 and the L-676 audit's own first verdict). **SIGN: GENUINELY UNKNOWN — the first row in this register with no protective sign in either direction.** ⚠ **Closes on ONE documentary question, not 2,600:** what does the Refós `PLAN`/`*` marker mean for a zone whose article states its own construction? That is a metadata-semantics question for the AMB, or one paragraph of the Refós's own methodology note. **Do not close it by sampling derived plans.** ⚠ **Do not write "13a is wrong" anywhere on the strength of this row**, and do not write "13a is fine" either. See [`CORPUS-BOUNDARY.md`](./CORPUS-BOUNDARY.md) §3.1. |
| **18** | **The scorecard's config reader published ITS OWN failure as every city's missing data** | **B** | **P1** | ✅ **CLOSED — §EMPTY-PARSE-IS-NOT-AN-ABSENCE, fixed by the València agent in `531531ef` (L-676). Recorded here because Barcelona hit it independently and because the lesson is not València's.** | ✔ **A SHARED-CODE DEFECT, LIVE on `main`, and it under-stated every city on the board.** `tools/context-bake/bake.mjs` renamed its declaration `const REGIONS = [` → `const ALL_REGIONS = [` in **`8258d06f`** (the `--region` filter rebound `REGIONS` to a filtered IIFE). `computeScorecard.mjs`'s `parseBakeRegions` kept the OLD marker, matched nothing, and returned an **EMPTY SET** — which every consumer read as the MEASURED fact *"this city is in no baked region"*: `contextExtractSlot → none`, `axisContext → "0 layers present (measured)"`. **Barcelona's first baseline this pass read overall 70.8 % · DATA-SOURCES 80 % · CONTEXT 0 %, where this dossier said 87.4 / 100 / 89 — and the dossier was right.** ⇒ the **L-422/457/467/469 failure-vs-empty collapse turned on our own config reader**, and the same shape as `8258d06f`'s own §SS-ABORT-IS-NOT-A-CAP finding one layer up: *the commit that introduced this defect was itself a fix for reporting a failure as a measurement.* ⚠⚠ **THE FINDING WORTH KEEPING — `honestyOk` STAYED `true` THROUGHOUT.** The audit checks that a number **HAS** a derivation (C63 §1.1 H3), never that the derivation is **TRUE**. **A well-formed, fully-cited falsehood passes the launch-blocking honesty gate.** That is a real, named limit of §3.1 and it belongs in C63 §8, not in one city's register. ⚠⚠ **AND A PROCESS FINDING: TWO AGENTS FIXED THIS INDEPENDENTLY IN ONE SESSION.** Barcelona's version degraded to `null`/`not-assessed`; València's THROWS. Both honest, both defensible — and **shipping both would have been parallel wiring in a shared tool (P1)**. Only València's is on `main`; Barcelona's was **dropped in favour of it, not merged alongside it**. **In a five-agent fan-out the reconciliation for shared code is ADOPT, never ADD.** ⚠ **`tools/context-bake/` was NOT edited by either pass** — a national bake owns it on this SHA, and the repair belongs on the reader's side regardless. |
| **17** | **HEIGHTS axis unmeasured** | B | P1 | 🔵 **bake in flight — probed 2026-08-01, numbers below** | ⚠ **PROBED, NOT CLOSED — the re-bake is still running and closing on today's tiles would pin a number the bake is about to move.** `node tools/context-height-probe/probe.mjs --at 41.3874,2.1686 --name barcelona` against the **SHIPPED R2 tiles** (`…r2.dev/tiles/buildings.pmtiles?v=L659a`, 25/25 tiles read, 0 absent, 0 failed): **verdict `unmeasured`** · **6 306 footprints** · provenance **0 measured-lidar · 55 tagged · 4 124 derived-levels · 2 127 ASSUMED (9 m default)** · `assumedFraction 0.337` · `solidRenderFraction 0.009` · `measuredMarkerCount 0` · heights min 3.2 / median 16 / max 70.4 m. ⇒ **33.7 % of Barcelona's rendered context heights are FABRICATED**, not the 19.8 % this row previously carried — that figure is **superseded** (kept for the audit trail). Meanwhile the C63 axis reads `heightsLod: not-assessed (not-queried)` because the probe's histogram is not yet wired into the scorecard. **SIGN: OVER-STATES** — and it is the register's only currently-measured over-statement: a 9 m default rendered in the same style as a surveyed height *asserts* a height the data does not carry (L-459). ⬆ **2026-08-01 (L-677) — HALF CLOSED, AND THE NUMBER IS WORSE AT CITY SCALE.** ① **The axis now SCORES.** The histogram is wired: `measurements/barcelona.measurements.json` → `heightsLod: 0.00495` (`tagged/total = 503/101,542`), `validationState: auto-validated`. It no longer reads `not-queried`. ② **A CITY-SCALE probe was run and it supersedes the near-ring figure.** `probe.mjs --at 41.3874,2.1686 --half-deg 0.06` (bbox `[2.0886,41.3274,2.2486,41.4474]`, **900 covering / 632 read / 268 absent / 0 FAILED**) ⇒ **101,542 footprints · 0 measured-lidar · 503 tagged · 62,940 derived-levels · 38,099 assumed · `assumedFraction 0.375`**. ⇒ **37.5 % of Barcelona's rendered context heights are fabricated, not 33.7 %.** ⚠ **The 33.7 % is NOT withdrawn and is NOT a contradiction** — it is the `--half-deg 0.008` NEAR RING, re-run today and reproduced **EXACTLY** (6,306 · 0 · 55 · 4,124 · 2,127 · 0.337). **The half-degree is the whole difference; a heights figure without its bbox is not a number.** ⚠ The 268 absent tiles are a **MEASURED EMPTY** (sea/port/Collserola), not a failure — **0 tiles failed**. ③ **STILL OPEN, and this is now the LARGEST single headroom on the whole board:** `measuredMarkerCount: 0` — the MDS join has never stamped one Barcelona building. **Closes when** the buildings-only re-bake (L-657/L-677) publishes and the probe is re-run. ⚠ **`tools/context-bake/` and `heightSources.mjs` were NOT touched by this pass** — a national bake is in flight on this SHA and owns those files. **Do NOT edit the counts above to predict its outcome** (§SIZE-IS-NOT-PROVENANCE: L-658 shipped a green bake with zero measured heights, and `8258d06f` landed because a 4-hour green run had published nothing at all). |

---

## The hidden blocker — recorded, not yet numbered

**The ordinance pipeline is `PDF → human → TypeScript`, and it will not scale.** Every clau re-derives
legislation instead of referencing it, so a supersession anywhere means hunting every duplicate.

**The fix is a Barcelona Planning Knowledge Base:** article → paragraph → parameter → classification →
rule KIND → applicability → exceptions → source → DOGC → RPUC → effective date. Rule packs **compile
from** it rather than duplicating it. **This eliminates almost all future drift** and is the structural
answer to blockers 9, 10 and 12 recurring in the next city.

## Effort, honestly

> ### ⚠ RE-PRICED 2026-08-01 (L-676) — legal research fell by an order of magnitude
>
> The supersession line below read **~2–3 weeks**. §RPUC-SUPERSESSION cut the unread set from
> **147 to 41** and text-checked the three instruments that could have moved a shipped number
> city-wide, so what is left of *that* item is **~½ day**, mechanical, with the recipe committed.
> The weeks that remain in this row are the 2002 Eixample *transcription*, not the supersession
> crawl.
>
> **And row 6 removed a line that was never in this table: the LiDAR acquisition** `NEXT.md` §3.4
> had 12b waiting on. It is not deferred — **it would not have worked**, because the missing term
> is a delimitation, not a measurement.

| Category | Remaining | Difficulty |
|---|---|---|
| Legal research (supersession, amendments) — ⚠ **13E is CLOSED (L-667)**; what remains under it is the 2002 *transcription*, listed under morphology rules. ⚠ **supersession itself is now ~½ day (L-676), not weeks** | ~2–3 weeks | Medium |
| Engineering — ⚠ **shrunk on 2026-08-01.** Art. 238's statistic is **shipped + pinned** and the 20a resolver is **closed**; what is left is **12A** (*tram* chaining, ~1 eng-day + a live-Catastro blast-radius measurement), clau-18 routing and the confidence model | ~1–2 weeks | Medium |
| Data acquisition (Pla Parcial index, official alignment geometry, a 20a subzone layer) | ~3–6 weeks | High |
| Morphology rules (àtics, balconies, patis, roof forms) — ⚠ **tribunes are NOT in this bucket:** §COSSOS-SORTINTS establishes they are a projection BEYOND the envelope, so they are a **saleable-area (`D`)** feature, not an envelope one | ~2–3 weeks | Medium |
| QA against official *fitxes* | ~1–2 weeks | Medium |

**The only structurally impossible category** is where the law itself delegates to a site-specific
instrument. There the correct output is **a machine-readable reference to the governing instrument** —
which is a **complete and legally correct result**, just not a single numeric envelope.

## Recommended order

> ### ⚠ REWRITTEN 2026-08-01 (L-676) — the previous order's first three items are gone
>
> The old order was **14 → 4 → 9 → 2 → 8 → 6 → 3 → 12A**. **14, 4 and 6 are now CLOSED and 9 is
> demoted**, so the queue is:
>
> **2 → 17 → 8 → 9-residual → 3 → 12A**
>
> *(**4**, **5**, **6**, **7**, **10**, **11**, **14** and **16** are dropped: all CLOSED. Of the
> old eight-item order, **only 2, 8, 3 and 12A survive** — and 3 and 12A are already narrowed to
> data questions.)*
>
> **2 is first because it is the only P0 left and it is one signature.** Everything under it is
> prepared; it is not engineering, and no further evidence will improve it.
>
> **17 is second, and this is a CHANGE OF KIND from the old order's second item.** With row 4
> measured, **row 17 is the register's ONLY currently-measured OVER-statement**: 33.7 % of
> Barcelona's rendered context heights are a fabricated 9 m default, drawn in the same style as a
> surveyed one. Rows 4, 11, 12 and 12B all turned out to under-state or be exact. **Rank by the
> measured sign, and 17 rises to the top of the engineering queue.**
>
> **9 drops behind 8 and 6** — its unread set fell from 147 to 41 and its three city-wide-capable
> instruments have been text-checked, so the *expected* size of any surviving effect is a local
> override on specific parcels, not a city-wide error.

**Why 14 was first, and what closing it proved.** *"The scoring framework cannot express the
completion states we are trying to declare; fixing coverage before fixing the ruler produces an
unprovable claim."* That was right, and the ruler is fixed (L-664). ⚠ **But the ENVELOPE axis STILL
reads `not-assessed`** — the ladder exists, the *reading* does not. **The row's own diagnosis was
half-right: an unscoreable axis had two causes, and only one of them was the contract.**

> ### ⚠ THE ORDER CHANGED ON 2026-08-01, AND THE REASON IS WORTH READING
>
> **11 (tribunes) was second, as *"the highest-risk correctness issue"*. It is now closed at P3, and
> it was never a correctness issue at all** — the PGM defines a *cos sortint* as projecting BEYOND
> the alignment, so omitting it under-states rather than over-states, and **folding it in is what
> would have been the defect.** The row's own premise was the risk.
>
> **4 (block dissolve) is now second, and it is the right second**: it is the only remaining item
> that can make an *already-published* number wrong, and **every** 13a/13b/12 answer depends on it.
>
> **The transferable lesson, and it is the third time this register has had to record it:
> establish the SIGN before ranking the severity.** Two of the four items closed in this pass —
> tribunes (11) and the Art. 238 residuals (12) — were filed as over-statement risks and are
> under-statement or exact. One (12A) turned out to be a genuine over-statement nobody had named.
> **Ranking by how alarming a gap sounds inverts the queue.**
>
> ### ⚠⚠ THE FOURTH TIME — and this one hit the item the lesson had just been used to PROMOTE (L-676)
>
> **4 was promoted to second on the premise that it is *"the only remaining item that can make an
> already-published number wrong"*. Measured, that premise is FALSE — and structurally so.** A
> failed dissolve reaches `refuseConstructionIncomplete` and publishes **nothing**, so it can only
> ever UNDER-state; the sole over-statement channel is a dissolve that succeeds on the *wrong ring*,
> and across **178 real Barcelona blocks that channel measures ZERO** (worst 0.34 % / 0.84 % against
> the published cadastral areas). **The re-ranking that applied the lesson correctly to 11 and 12
> then mis-applied it to 4, because 4's sign had never actually been measured either.**
>
> **So the rule needs its sharper form, and it is now recorded twice over:**
> **the sign is a MEASUREMENT, not a property of how the gap is described.** "This can make a
> published number wrong" is a *hypothesis about a code path*; it is settled by reading the code path
> (`siteDispatch.ts:4307`) and by an independent oracle — not by how serious the row sounds.
>
> ⚠ **And one level down again: the L-676 audit's OWN first verdict was `OVER-STATES`, from an
> oracle that forgot to subtract interior courtyards** (Ciutat Vella `12215`: 2.34 % → **0.20 %**
> once its 101.9 m² pati was netted off). **Establish the sign before ranking the severity —
> including the sign your own instrument reports.**

---
*Authority: C63 · C58 · L-449 · L-656 · L-660 · L-661 · L-662. Signatures: [`sources/VERIFICATION.md`](./sources/VERIFICATION.md).
Maintainer: UNASSIGNED. Target: TBD.*
