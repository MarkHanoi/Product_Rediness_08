# RISK-REGISTER — Jeddah (UN/LOCODE JED)

> Fail-safe honesty guardrails. **Last updated:** 2026-07-30. **Maintainer:** UNASSIGNED.

| # | Risk | Guard (the honest default) |
|---|---|---|
| R1 | Reading a geo-fenced field as ABSENT | Balady cadastre / `NOOFFLOORS` / the exact vertical are **reachable-in-principle, not-from-here** — never 0/absent (L-606). Failure ≠ empty. |
| R2 | Rendering the national footprint on an Al-Balad parcel | Heritage parcels return a **correct refusal**, not the national fill — the UNESCO regime over-rides and the national footprint would over-state (C63 §3.1). |
| R3 | Crediting the reused pack as a completed ENVELOPE/LEGISLATION | Pack is `estimated-ruleset` (unwired, unsigned) → both axes `not-assessed` until L-449. The `~53 %` is the legislation PRIOR, not the composite. |
| R4 | Treating Overture density as height | Overture height ≈ 0 % in Saudi (ML footprints) → HEIGHTS `not-assessed`; the 7.2× footprint win brings NO skyline. |
| R5 | Reading TERRAIN 0 % as `not-assessed`, or inventing a DTM | TERRAIN is a **cited** rung-0 (terrain.mjs `jeddah` row `blocked` — no open GEOSA DTM); do not substitute a global DTM as a national drape without clearing the licence. |
| R6 | Reading `not-assessed` as 0 % | `not-assessed ≠ 0 %` (C63 §1.2); overall renormalised over the assessed subset + flagged `partial`. |

*Authority: C63 §3.1 (honesty companion), C62 (typed unknowns), L-606 (the measured geo-fences).*
