# RISK-REGISTER — Copenhagen / København (kommune 0101)

> Fail-safe honesty guardrails. **Last updated:** 2026-07-30. **Maintainer:** UNASSIGNED.

| # | Risk | Guard (the honest default) |
|---|---|---|
| R1 | Borrowing the national ~96 % as Copenhagen's LEGISLATION score | Axis 2 stays `not-assessed`; the ~96 % is the *country* prior (`dk/RATE.md`), not a per-city measured fill (§CONTEXT-DATA-HONESTY country-borrow). |
| R2 | Promoting the `dkPerimeterBlock` STUDY band to a certified envelope | It is a conservative human-gated STUDY default (L-619), `constructed` at best — never `certified` without the L-449 sign-off. Under-states the building on purpose (C58 §1.4). |
| R3 | Claiming TERRAIN is baked | Rung is `not-assessed` (`license-restriction`) — the DHM WCS is apikey-gated and "skips loudly until the key is in env"; no `layer.json`/round-trip probed. |
| R4 | Claiming measured heights before a probe | HEIGHTS `not-assessed` (`not-queried`): the DHM nDSM join is apikey-gated → OSM `assumed` without the key; no provenance histogram probed. |
| R5 | Reading `not-assessed` as 0 % | `not-assessed ≠ 0 %` (C63 §1.2); overall renormalised over the assessed subset (DATA-SOURCES · CONTEXT) + flagged `partial`. |
| R6 | Rendering an estimated triple where Plandata lacks numbers | `dkPlandataRefusal` returns a cited refusal (numbers are in the plan PDF), never the generic 3/1.5/3·12 m·FAR 2 estimate. |

*Authority: C63 §3.1 (honesty companion), C62 (typed unknowns), the `context-data-honesty-family` memory.*
