# LEGISLATION-RATE — Aarhus (kommune 0751)

> Structured legislation / data-fill rate (C58 comparable ruler; feeds C63 Axis 2). **Last updated:** 2026-07-30. **Maintainer:** UNASSIGNED.

## Rate: `not-assessed` (`pending-implementation`) — on a LIVE, keyless, `structured` national source

Zoning at Aarhus is resolved by `DkZoningProvider` + `mapPlandataToZoningRecord` over the **keyless national
Plandata WFS** (`server/plandataZoningProxy.js`) — C58 fidelity-1 `structured`, no PDF read. The **national**
number is ~96 % digital-data / ~87 % pure-structured byzone fill (`dk/RATE.md`, L-609/L-611).

**Why this cell is `not-assessed`, not ~96 %:** the C63 Axis-2 ruler is *per-city verified-cited clauses*, and
no per-city clau audit has been run for the 0751 extent (borrowing the country prior would be the
§CONTEXT-DATA-HONESTY country-borrow trap), and the L-449 sign-off is PENDING (`dk/sources/VERIFICATION.md`,
2 open items). C63 §1.6 forbids `human-reviewed` without it.

**What would move it:** run the byzone click-weighted fill probe scoped to the 0751 bbox, cite per-clau
values in a city `sources/SOURCES.md`, land the Danish-planner sign-off.

*Cross-refs: C58, C63 §3 Axis 2, `../../RATE.md` (national legislation-fill — legacy filename, pending rename to `LEGISLATION-RATE.md` per L-649), `./RATE.md`.*
