# Runbooks — Incident retrospectives

> Past-incident retrospectives + SEV-1 PMIs live here as `YYYY-MM-DD-<slug>.md` (per each runbook's §5 template + C42 support tier). This folder was created 2026-07-16 (L-350) so those cross-references resolve. It is empty until the first real incident is filed.

Naming: `YYYY-MM-DD-<short-slug>.md` (e.g. `2026-08-01-pg-primary.md`). Follow C31 authoring rules.
