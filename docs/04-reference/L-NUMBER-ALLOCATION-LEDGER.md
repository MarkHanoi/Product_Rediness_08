# L-NUMBER ALLOCATION LEDGER

> **Status:** LIVE ALLOCATOR — not a contract, not a log. The ISSUE LOG
> (`docs/04-reference/ISSUE-LOG.md`) remains the record of what each L-number *means*.
> This file records only **who may mint which numbers**, so two lanes cannot mint the same one.

## 0. Why this exists

**Grep-before-minting stopped working on 2026-08-19.** Measured that session:

- the frontier moved **L-1199 → L-1226 in a few hours** — 27 numbers;
- **three lanes minted `L-1202` within one hour** (HR5, SITE1, UND1);
- one lane renumbered **three times** (`1197/1198` → `1202` → `1221`, each taken mid-flight);
- another minted `L-1214` only for a third lane's commit to sweep the same number into HEAD first.

The convention was *"grep the ISSUE LOG, then take the next free number"*. That is a
**read-then-write against a file many writers append to concurrently** — a textbook race. It is
correct for one writer and cannot be made correct for six by trying harder. Every lane that hit it
followed the instruction exactly and still collided.

⭐ **The failure is the same shape this codebase keeps finding elsewhere: a value that must be
REMEMBERED rather than DERIVED, read at a moment that has already passed by the time it is used.**
Two independent lanes proposed the identical fix — **reserved blocks** — which is what this file is.

## 1. The rule

1. **The orchestrator allocates a block to a lane when the lane is launched**, and writes the row
   below **in the same action**. A lane never chooses its own numbers.
2. **A lane mints only inside its own block.** No grep, no frontier check, no "next free" —
   the block IS the authority, and it cannot race because nobody else holds it.
3. **A lane that exhausts its block asks the orchestrator for a second block.** It does not
   continue past the end. Blocks are 10 wide, which has covered every lane measured so far
   (largest single-lane consumption: 6).
4. **Unused numbers in a closed block are NOT reclaimed.** A gap in the ISSUE LOG costs nothing;
   a reissued number costs a day of two people describing different defects under one id.
5. **The frontier only ever moves forward.** New blocks start above the highest number ever
   *allocated*, never above the highest number *found in the log* — an unlanded lane's rows are
   invisible to grep and that is precisely the race.

## 2. Allocation table

**Highest number allocated: `L-1589`.** Next free block starts at **`L-1590`**.

| Block | Lane | Session | State |
|---|---|---|---|
| L-1227 – L-1239 | UND1 — 3D linework / underlay view scope (allocated from the buffer) | 2026-08-19 | **L-1227 ✅ FIXED, L-1228 logged** (2 of 13 used) |
| L-1240 – L-1249 | ELEV1 — window/door elevation symbols | 2026-08-19 | **L-1240, L-1241, L-1242 minted** (3 of 10 used) |
| L-1250 – L-1259 | ROUND1 — circular windows / arched doors | 2026-08-19 | **L-1250, L-1251, L-1252 ✅ SHIPPED** (3 of 10 used) |
| L-1260 – L-1269 | NL1 — wall finish / layer chat grammar | 2026-08-19 | **L-1260, L-1261, L-1262, L-1263 minted** (4 of 10 used) |
| L-1270 – L-1279 | JOIN1 — raked-wall joints (gap opens toward the top) | 2026-08-19 | **L-1270, L-1271, L-1272 minted** (3 of 10 used) |
| L-1280 – L-1289 | LOG1 — boot-log defect sweep (clash dead, durable thumbnails, triple open) | 2026-08-19 | **L-1280 … L-1289 minted — BLOCK EXHAUSTED** (10 of 10 used) |
| L-1290 – L-1299 | GPU1 — ShadowDepthTexture destroyed mid-submit on railing MATERIAL change | 2026-08-19 | **L-1290, L-1291, L-1292 ✅ FIXED; L-1293 ⛔ OPEN (measured)** (4 of 10 used) |
| L-1300 – L-1309 | PERF1 — load/render performance (145 warns, 13.2 MB autosave, cache thrash) | 2026-08-19 | **in use — L-1300 ✅ FIXED; L-1301–L-1305 logged; L-1306–L-1309 free** |
| L-1310 – L-1319 | SYNC1 — the plan-rejection latch + the blockers in the recovery path | 2026-08-19 | **L-1310, L-1311, L-1312 ✅ FIXED** (3 of 10 used; L-1313–L-1319 free) |
| L-1320 – L-1329 | SHAPE1 — shape modes (rect/circular/elliptical) across wall, curtain wall, slab, ceiling, floor | 2026-08-19 | **in use — ALL SIX CLOSED 2026-08-20. L-1320 ✅ FIXED (slab outline editor reachable; the predicate could not express its question) · L-1321 ✅ DECLARED (C87 R-11 + §13.14 — curtain wall refused by name) · L-1322 ✅ RECONCILED (one canonical set + name collision removed, pinned per EI-8a) · L-1323 ✅ FIXED (shape descriptor alongside the ring, invalidated on any vertex edit) · L-1324 ✅ CLOSED (3-D slab arm; C92 SL-Voc-2 closed with it) · L-1325 ✅ CLOSED (3-D wall arm) · L-1326–L-1329 free** |
| L-1330 – L-1339 | LIGHT1 — 20 LOD-200 lighting fixtures with photometrics | 2026-08-19 | **L-1330 ✅ SHIPPED** (20 families derived from ONE matrix; lumen-reader verdict: READ by the renderer, NOT by any analysis path — no illuminance calculation exists) · **L-1334 ✅ FIXED** (barrel-at-module-load broke repo-wide test collection) · **L-1331 ✅ FIXED** (C96 §9.1 / EI-3 CLOSED — exit (a): the LOD-200 matrix moved to L0 so `LightingKind` DERIVES its accepted set; 30-of-32 refused → 0; zero `apps/editor` changes) · **L-1332, L-1333 ⛔ OPEN** (generic plan symbols · beam angle does not narrow the light) — 5 of 10 used; L-1335–L-1339 free |
| L-1340 – L-1349 | LOG1 (second block) — project-open coalescing keyed on nothing | 2026-08-20 | **L-1340 minted** (1 of 10 used) |
| L-1350 – L-1359 | BG1 — grey WebGL-fallback viewport + WebGPU navigation ghost ("reminiscencia") | 2026-08-20 | **L-1350 ✅ FIXED** (§FRAME-STARTS-CLEAN-ON-EVERY-BACKEND — the per-frame OBC base clear was armed on the ONE backend whose overlay is opaque and DISARMED on the one whose overlay presents alpha 0 in empty space; the arms were INVERTED and a green test pinned the inversion) · **L-1351 ✅ FIXED** (the `background: #ffffff (all layers)` log line claimed three layers from a call that writes one on every Phase-5 backend) · **L-1352 ✅ FIXED** (§VIEWPORT-BG-PROBE enumerated the five BACKGROUNDS and could not name a grey that is DRAWN — ground shadow-catcher added as surface 0) · **L-1353 🟡 OPEN — MEASURED, NOT FIXED** (the grey is very likely the 4 km `ShadowMaterial` ground catcher, not the background stack; mechanism derived from three r183 `ShadowMaskModel`; needs ONE browser measurement to close) (4 of 10 used; L-1354–L-1359 free) |
| L-1360 – L-1369 | GIS1 (second block) — GIS panel phase 2b: legacy pill removal, PRYZM styling, buildability re-host | 2026-08-20 | **L-1360, L-1361, L-1362 ✅ FIXED · L-1363 ⭐ RESOLVED — two lanes minted the SAME contract section number (C06 §12) the same day; this ledger fixed L-numbers and left CONTRACT SECTION numbers on the same doomed read-then-write** (4 of 10 used) |
| L-1370 – L-1379 | RAC1 — chat near-miss: "raked 90 dregress" answered as a WALL TYPE | 2026-08-20 | **L-1370 ✅ FIXED** (the rake/orientation word set is now DERIVED from `DimensionFamilies.OTHER_CAPABILITY_WORD` and imported by the wall-type, hosted-type and colour grammars; the hosted parser's own five-word hand-copy had already rotted) · **L-1371 ✅ FIXED** (recognised-but-underspecified refuses BY NAME — *"I don't recognise the unit “dregress” — did you mean degrees?"* — ⛔ never auto-corrected) · **L-1372 ✅ FIXED** (the **FOURTH** hand-written spatial tail, folded into `SpatialScopeTail`; ⚠ a **FIFTH** is named and deliberately left in `WALL_COLOR_RE`) · **L-1373 ✅ DECIDED** (90° IS VERTICAL — the Confirm card now states the resulting orientation in words) — 4 of 10 used; L-1374–L-1379 free |
| L-1380 – L-1389 | LAND1 — landscape library + the furnish-vs-panel reach gap (both axes) | 2026-08-20 | **L-1380 ✅ FIXED** (the LANDSCAPE panel offered 8 INDOOR houseplants while a **25-species parametric outdoor tree library** with real species, mature heights, crown radii and true architectural plan symbols sat unreachable behind the Interiors carousel — REACH, not a rebuild; catalogue DERIVED from `TREE_SPECIES_ORDER`, zero materials minted, zero GLBs) · **L-1381 ✅ FIXED** (all eight `plant_0N` descriptors were IDENTICAL `0.6×0.6×0.8` **and the descriptor WINS over the builder default**, squashing the 1.8 m floor plant to 0.8 m on every placement) · **L-1382 ⛔ OPEN — MEASURED** (an auto-furnished `wardrobe` builds **ZERO meshes**; `corner_wardrobe` has no `case` at all; `rug` THROWS — and `wardrobe` is `required: true` in bedroom AND master; fix lives in lane RAC1's package) · **L-1383 ⛔ OPEN — MEASURED** (**38 interior types are GENERATOR-ONLY**: real builders, placed by auto-furnish, offered by no hand surface — `tv`, `wall_art`, `wall_tapestry`, `rug`, `curtain_*`, `wall_mirror` among them; blocked on the footprint table, see L-1385) · **L-1384 ⛔ OPEN — ABSENT** (⭐ "decoration items on top of the bed" **do not exist anywhere** — 12 of 12 keywords have no type, no builder, no card; the `soft_furnishings` category calls itself *'Curtains, rugs, cushions'* and holds neither a curtain nor a cushion) · **L-1385 ⛔ OPEN — MEASURED** (**no contract owns planting or soft furnishing** — C85–C99 is full and C61 is the only reserved slot; and TWO footprint tables exist, ai-host's declaring itself a *"mirror of geometry-furniture"* with **nothing checking they agree**) — 6 of 10 used; L-1386–L-1389 free |
| L-1390 – L-1399 | FURN1 — "Furnish all rooms (AI)" is SUPER SLOW: performance + architecture | 2026-08-20 | **L-1390 ✅ FIXED** (⭐ the always-on `§DIAG-ROOM-LOOP` audit read `combinedInput`, the RAW pre-repair array — **four** repair passes run before `buildWallGraph`, so `detectedRooms=24` and `unresolvedLoopBreaks=30` on the SAME line came from DIFFERENT arrays and neither described the other; the `_s\d+` half of its own regex could never match on the array it was given) · **L-1391 ✅ FIXED** (no angle test ⇒ a wall running PARALLEL at 400 mm was a "loop break", twice; no same-parent guard ⇒ two chords of ONE curved wall accused each other; ordered-pair double count. Compute MEASURED at ~0.20 ms/level — the founder's #2 suspicion **de-ranked**) · **L-1392 ✅ FIXED** (`thickShellTJunctionsRescued` is UNSATISFIABLE below a 360 mm host and every generator emits 100–300 mm — defect shape D; plus the BREAK band caps at 1000 mm, so "917 mm" was the CAP not the model) · **L-1393 ⚠ RETRACTED** (`_reconnectDanglingEnds`' root-cause docstring is false in BOTH halves — the trim was capped at 50 mm, and `connectedToWall` blocks the pass from EVER firing on the cluster geometry it describes) · **L-1394 ✅ FIXED** (the "cosmetic" active-level write was the ONLY thing telling LIGHTING which floor to light, and the `finally` restored it underneath a pending `setTimeout(0)`) · **L-1395 ✅ FIXED** (⭐⭐ **THE ANSWER** — 8 `activeLevelId` switches for a cosmetic HUD update ⇒ ~40 whole-scene traversals + 7 COLD plan projections; ⛔ the founder's ranking did not name it) · **L-1396 ✅ FIXED** (24 room tags = 24 commands and **24 undo entries**; `CreateManyAnnotationsCommand` had existed since L-145 with one consumer. ⚠ opens a NEW finding: `createSnapshot` has no `annotation`/`view` row, so **nine** commands declaring those scopes snapshot `{}` — the L-1050 defect, one table over, **OPEN**) · **L-1397 ✅ FIXED** (the perf instrument committed its OWN rule-1 defect — 18 keys it READS are bumped by nothing and printed as `0` under an ARMED header, falsely exonerating a named prime suspect) · **L-1398 ✅ FIXED** (§VIEW-GATE-NO-OP — three gates re-traversed 6113 meshes to write the values already there, on every level switch app-wide; + the gesture's first real instrumentation) · **L-1399 📋 RECORDED** (the founder's ranking re-ranked by measurement: #3 OVERTURNED, #5 already closed, #2 de-ranked, and the answer was not on the list) — 10 of 10 used |
| L-1400 – L-1409 | INST2 — "the scene is CLASHING": the 512-instance cap on a large-storey building with many windows | 2026-08-20 | **L-1400 ✅ FIXED** (the instance pool was a HARD CAP with NO OVERFLOW — 512 slots per `(level × geometry × material)` key and past it the element was refused; ⭐ a fixed-size pool with no overflow is an UNDECLARED CEILING ON MODEL SIZE. 512 is **arbitrary** — not GPU, not driver, not `THREE.InstancedMesh`; ⛔ raising it was refused because it only moves the cliff. Fixed by SHARD CHAINS: N shards of 512 = N draw calls, capacity unbounded) · **L-1401 ✅ FIXED** (⭐⭐ **THE REAL DEFECT** — a refused instance was **not** a fallback to a mesh: `WindowBuilder` strips its sub-meshes unconditionally, so **488 of 1 200** window parts on a 100-window storey were drawn by NOBODY. MEASURED: 12 slots/window, 10 of them frame ⇒ **512/10 = 51.2** ⇒ first casualty is window **52** part `#2`; windows 52+ rendered glazing + sill with **no frame**. Now falsifiable via `droppedInstanceCount`, which must read 0) · **L-1402 ✅ FIXED** (488 `console.warn`s with stack frames per storey during load — the flood is what HID the finding; aggregated to one line per NEW shard, and the message SURVIVES) · **L-1403 📋 RECORDED** (⭐ the deeper lever, honest half-answer: merging ACROSS materials is a REAL constraint, but the ten frame members are ONE material — merging them takes a window from 12 slots to 3 and a shard from ~51 windows to ~170; needs a per-window-TYPE merged-geometry cache + ADR-0297 material ownership, and with spill shipped it is pure PERF, not correctness) · **L-1404 📋 RECORDED, NOT CHASED** (report-only CSP `eval` violation; `powerPreference` ignored on Windows = upstream Chrome) — 5 of 10 used; L-1405–L-1409 free |
| L-1410 – L-1419 | SWAP1 — "why did the building not load in the 3D view?": the heavy-scene renderer swap on a freshly-loaded project | 2026-08-20 | **L-1410 ✅ FIXED** (§RETIRE-ZERO-IS-NOT-ONE-FACT — the retirement counter printed `0`, `3181`, `6936` and `6937` in one day and a bare `0` covered THREE different states; now carries a monotonic mint denominator + a derived kind, and the previously-SILENT `tracked & minted>0 & detached=0` case warns) · **L-1411 ⚠ THEORY REFUTED, PROBE SHIPPED** (⭐⭐ *"the 3,181 detached objects ARE the building and nothing re-attaches them"* is **MEASURED FALSE**: across a real `retireRenderer()` on a real 3,181-mesh `THREE.Scene`, meshes holding a live material **and** a non-empty position attribute go **3,181 → 3,181**; only the dead renderer's **6,362** dispose listeners come off. There is no missing re-attach — the classic `THREE.WebGLRenderer` the swap targets mints no `RenderObject` at all. `§SWAP-PAINTS-THE-BUILDING` now prints live-binding count + loop state + `getFrameSkipReport()` + draw calls at the swap and 1 s later, so the next report names the gate instead of the theory) · **L-1412 ✅ FIXED** (`[renderer-three] backend: webgl1` vs `[RenderPipelineManager] … WebGL2 backend detected` one line apart — the DECISION was right (`isRealWebGPUBackend` is a partition by construction), the LABEL hardcoded a backend name into a branch that resolves none; same defect `UnifiedFrameLoop`'s hardcoded `"WebGPU"` was corrected for) · **L-1413 ✅ FIXED** (⭐ C04 §1.4 quoted `isHeavyModel` — **≥15 levels AND ≥1000 elems, OR ≥4000** — as the swap's trigger; the founder's 233 elems / 3,191 meshes / 7 levels satisfies NEITHER arm yet swapped. MEASURED: the swap has always used its own `isSwapWorthyHeavyScene` (≥400 elems OR ≥1000 meshes), and **`isHeavyModel` has ZERO call sites outside its own file** while its JSDoc claimed the swap "reuses this EXACT predicate". The divergence is CORRECT (ADR-0267 §Fix-1 / L-366); ⛔ unifying re-opens L-361. Contract + JSDoc corrected in place, both predicates pinned, and the THIRD firing site named) · **L-1414 ⛔ OPEN — MEASURED, NOT FIXED** (⭐ `reason=tier:post-load`: the backend decision for a project OPEN is taken **after** 3,191 meshes exist — the point of maximum attached state, and it fires on every heavy project open. Deciding before the scene is populated removes the class, and two of the three firing sites already do; ⚠ the load path can supply element + level counts pre-build but **not a mesh count**, and the founder's project trips ONLY the mesh arm — so moving it earlier needs a per-family mesh **estimate**, i.e. exactly the arbitrary constant C04 §INST.2 refuses. Stated NOT-YET rather than claimed DONE) — 5 of 10 used; L-1415–L-1419 free |
| L-1420 – L-1429 | GLOBE2 — Cesium globe + the Real-GLB export path (building floating in the sky, not rendering realistically) | 2026-08-20 | **L-1420 ✅ FIXED** (⭐ the 2 553 km `minY` is the WGS-84 ECEF **Y** of the **Sydney Opera House** minus 7.921 m = `east_y · 9.04 m`, a house footprint — a DERIVATION, not a magnitude; `GISAreaLayout:556-560` calls `setAnchor()` **unconditionally at GIS init** with that hard-coded default — a call site C12 §1.5 explicitly recorded as *“NOT verified that it ran”* and got WRONG about; the exporter baked `matrixWorld` and inherited the ECEF frame, and the anchoring step removed only the **Y** component, leaving the model 5.8 Mm from its own GLB origin. Fixed by a frame boundary **DERIVED** two ways — declared `userData.pryzmSceneFrame` + measured ≥100 km — shallowest-ancestor rule, per-root probe, refuse-not-emit; the test decodes the emitted glTF and asserts METRES, red at HEAD-minus-fix with `expected 4646976.6 to be less than 1000`) · **L-1421 ✅ FIXED** (the overlay suppression set was a name list that could not REACH its offenders — root-only matching vs edge outlines living INSIDE element subtrees; **structurally blind, not merely incomplete**; now derived from the THREE class) · **L-1422 ✅ DECIDED + FIXED** (C12 §11.4's open globe/site asymmetry: glazing reads as glass on the globe — ⛔ explicitly **NOT** `formaWhite: true`, which would repaint every wall white and be LESS realistic, the opposite of the request) · **L-1423 ⛔ OPEN — MEASURED** (every project in every city is anchored to the Sydney Opera House at GIS init; closes only via C12 §9 item 3, not via a different hard-coded default) — 4 of 10 used; L-1424–L-1429 free |

| L-1430 – L-1439 | STAIR1 — stair auto-opening pierces the SLAB but not the FLOOR FINISH; + the tread-minimum EI-3 breach | 2026-08-20 | **L-1430 ✅ FIXED** (the sketch tool and the create command enforced DIFFERENT minima — 220 vs 250 mm — on DIFFERENT QUANTITIES: per-run `flightLength/stepCount` vs the averaged `Σlength/totalSteps` the adapter actually commits, which is why the founder's two messages read 218 and 222 rather than one number; ONE authority `StairGeometryLimits.ts` now owns the numbers, the predicate AND the quantity, and the parity spec — which drives the two REAL layers, not the shared function — goes RED on 10 samples with the old limits restored, including `L(2.50,2.50) … 222mm`, the founder's own number) · **L-1431 ✅ FIXED** (⭐ the floor-finish hole was NEVER CREATED, not created-and-unsnapshotted — the declared scope `[stair,opening,slab]` was HONEST; `addServiceHole` had **ZERO callers repo-wide**; the void set was ENUMERATED where it must be DERIVED, and the same enumeration hid a SECOND axis relayed by RAC2 — only `topLevelId` was pierced, so a Ground→L5 stair passed through four intact decks; now derived over families × levels, 9 tests against the PRODUCTION FloorStore/CeilingStore) · **L-1432 ⛔ OPEN — DECLARED** (move/param-change does not follow the new voids) · **L-1433 ✅ FIXED** (the SLAB's level axis — a Ground→L5 stair drove through four INTACT slabs, founder-reachable by the Top-level dropdown; carve+reconcile now LOOP the derived deck set with the per-deck bodies verbatim, and ⭐ the persisted top-deck id `opening-stair-<id>` is pinned as a LITERAL because re-keying it would orphan every saved project's void; 6 cases go RED under a forced top-only patch while the 6 id-stability cases stay GREEN) · **L-1434 ✅ FIXED, NOT AS FILED** (⭐⭐ enforcing the declared 16 would have REFUSED an ordinary 3.0 m storey, which solves to 17 risers — a limit nobody reads is a limit nobody validated; the count was a CATEGORY ERROR for a code rule about RISE between landings, so the cap is enforced as rise at a threshold DERIVED from existing constants (16 × 0.190 = 3.04 m), inventing no number and unable to refuse anything the old rule allowed) · **L-1435 ✅ FIXED** (RAC2's two command-side findings: `element.updateDimensionsBatch` validates POSITIVITY ONLY so a BULK width edit can set what the SINGLE route refuses — the L-1430 shape between a different pair of layers; predicate now shared, ⛔ the bulk route's per-family DISPATCH still missing and declared; and `StairTypeStore.get` vs `resolveCatalogueRef`'s `getById` — the family fell off the ONE catalogue ladder for a NAME) · **L-1436 ⛔ OPEN — LOGGED** (a COMMAND barrel reaches `@thatopen/ui` and touches `document` at MODULE LOAD via geometry-slab/SlabTool — every command-registry consumer is implicitly browser-only; NOT established whether it predates today) · **L-1437 ✅ FIXED** (⭐ self-inflicted: L-1430's fix ran BACKWARDS — two built-in stair types are LOOSER than the defaults, so the tool refused what the command permits; ⚠ one AUTHORITY is not one RESOLUTION of it, and the parity spec was structurally blind to it because its ctx omitted the type store) — 8 of 10 used; L-1438–L-1439 free |
| L-1440 – L-1449 | RAC2 — stair creation + bulk stair/railing edits in natural language (`packages/ai-host` ONLY) | 2026-08-20 | ⏳ IN FLIGHT |
| L-1450 – L-1459 | STAIRUX1 — stair draw MODES (linear / orthogonal / by-walls) mirroring the wall tool's shared mode strip | 2026-08-20 | ⏳ IN FLIGHT |
| L-1460 – L-1469 | MAT2 — materials never reach the pixel (C100 §9): furniture dies on save, door/window carry no `materialId` at all | 2026-08-20 | ⏳ IN FLIGHT |
| L-1460 – L-1469 | MAT2 — founder *"why do all furniture and walls + doors not have materials associated? Was that yesterday's C100 work?"* | 2026-08-20 | **L-1460 ✅ FIXED** (⭐⭐ **THE CENSUS MEASURED A PATH PRODUCTION DOES NOT TRAVEL.** C100 §9.1 files furniture under *"renders, then DIES ON SAVE"* and §9.8 S16 records its producer as ROUTED — both true of `geometry-kernel/producers/furniture.ts`, which the founder's furniture never reaches. `initTools.ts` §FT-FURNITURE says so itself: the PRYZM-3 payload *"does NOT match the legacy `FurnitureData` model"*, so plan tool, carousel, kitchen, wardrobe, copy/paste and the whole D-FLE furnish run go bus → event → legacy FurnitureStore → `FurnitureFragmentBuilder` → `MaterialService.getMaterial(color: number)`. ⛔ `materialId` existed at **NONE of five layers** — not lost, never reachable, i.e. the door/window mode, not the serializer mode. ⭐ And the panel was TRUTHFUL: its read-only **"Material"** row shows `material: wood|metal|fabric|glass` — a FOUR-value union against 205 master rows. Closed at all five layers + ARM E read-back, resolving at the ONE dispatch choke point so it is one call not sixty-two; ARM D 2→1, ARM F 4→3. RED-FIRST EXECUTED: reverting only the resolution turns **4 of 6** red and leaves BOTH nothing-repaints controls green) · **L-1461 ✅ FIXED** (the §P2.1 bus→legacy WALL mirror copied `materialColor` + `layers` and DROPPED `materialId`, while the column/slab/handrail mirrors beside it all carry it; `wall.created` had no top-level id either. ⚠ Changes nothing on screen TODAY and the code says so — the same hole already cost this mirror the residential façade colour AND the plan-view curve) · **L-1462 ✅ CLOSED BY DELETION** (`composeFamilyMaterialKey`, C100 §9.2's *"ZERO callers, has never run"*, STILL had zero — ⭐ and the reason is that the SHAPE is wrong: it mandates one key layout, which §9.6.b explicitly forbids. S16/S17 converged **ten of seventeen** producers without it. Not the unfinished half of that work — a RIVAL to it, and §9.6.a says *"MUST NOT let a third appear"*) · **L-1463 ✅ FIXED** (⭐ the gate's ARM F declared pair named `core-app-model/src/stores/FurnitureTypes.ts` as Furniture's runtime record — a **DEAD RIVAL**; `initBuilders.ts:758` builds the live `window.furnitureStore` from `@pryzm/geometry-furniture`. ARM F was reporting a TRUE statement about a file nothing renders from and would have gone GREEN on a field added to the dead copy. §9.7's *"one spelling of a thing"* shape with **spelling replaced by PATH** — strictly worse: a wrong spelling under-reports, a wrong PATH can be SATISFIED without touching the product) · **L-1464 ⛔ OPEN — MEASURED, DELIBERATELY NOT FIXED** (⭐ the founder's WALLS are **NOT-ASSIGNED**, not assigned-and-dropped: no creation path sets a top-level `materialId` (`WallTool.ts:1917-1927`, `WallPlanToolHandler.ts:619-630`), `CreateWallCommand` passes it through with no system-type read, and the default `wt-monolithic` type has **no `materialId` field at all** — its layer carries `materialColor: '#e8e8e8'`, which IS `WALL_SCHEMATIC_MATERIAL`, the "no material" grey. ⛔ Whether Plain Wall should NAME a master row is a **product decision** — exactly the one §9.9 refused to settle — and guessing repaints every wall. FOUNDER'S CALL. ⭐ Banked negative: the 15 system-type layers carrying both an id and a hex are **15/15 in agreement with the master**, so routing `layer.materialId` is zero-repaint whenever a lane owns `WallFragmentBuilder`) — 5 of 10 used; L-1465–L-1469 free |
| L-1470 – L-1479 | RENDER3 — founder *"ALL MATERIALS GONE ON THE VIEW"*: the `Attachment has zero size` flood | 2026-08-20 | **L-1470 ✅ FIXED** (⭐⭐ NOT the material database and NOT a background — a **SECOND** surface, OBC’s WebGL canvas, was sized to **0×0** by OBC’s own unguarded `ResizeObserver` (`@thatopen/components` index.mjs:14535 — no `Math.max`, no `> 0`, no `||`) the instant `mainRendererVisibility` set `display:none` on its **parent**; the existing §L-328 gate reads only the **WebGPU** renderer, so it passed while the per-frame OBC clear drew into an incomplete framebuffer. `1+245+9 = 255` is **Chrome’s error cap**, not a census. Fixed at ONE authority `packages/renderer-three/src/surfaceArea.ts`, derived from the **backing store**, ⛔ never a clamp; falsifiability measured — sabotaging the predicate turns **13 of 17** red) · **L-1471 ✅ FIXED** (`ViewController._forceRendererUpdate()` poked OBC’s loop on every view activation with no area check — the 245 draws; five geometry tools already guard this, the site was missed) · **L-1472 🟡 OPEN — TRIAGE ONLY** (`rail.pmtiles` / `trees.pmtiles` 404 in prod; ⛔ not this lane’s. Also CLEARED here: the `412` on `PUT …/versions` is the **handled** concurrent-writer path, **not** dropped saves) — 3 of 10 used; L-1473–L-1479 free |
| L-1480 – L-1489 | WEBGL4 — 3D view shows only LINE PROFILES of walls / slabs on the WebGL path (the same project renders solid on WebGPU minutes later) | 2026-08-20 | **L-1480 ✅ FIXED** (⭐⭐ **THE ROOT** — `_applyShadowFreezeState` wrote the PER-LIGHT `LightShadow.autoUpdate` inside `if (this._webGpuActive)` on the claim *“WebGL2 fallback … honours the renderer-level flag”*; three r183 `WebGLShadowMap.js:170` honours the PER-LIGHT flag TOO. A freeze taken on WebGPU plus a `dispose()` that zeroes the freeze COUNTERS without thawing the LIGHTS stranded the key light at `autoUpdate=false` on the classic renderer → `shadow.map` never allocated → `sampler2DShadow` bound to a 1×1 RGBA with `COMPARE_MODE=NONE` → `INVALID_OPERATION` → **every lit mesh draw dropped**, while `meshbasic.glsl.js` — the LineBasicMaterial program — declares no shadow sampler and kept drawing. Armed on the CONDITION now, not the backend: the L-1350 rule) · **L-1481 📋 RECORDED** (C04 §1.4's §AUTO-WEBGL-HEAVY amendment asserted *“the generated scene is 100% classic materials, so it renders correctly on the classic renderer”* — a prose-justified verdict with no gate, falsified by the founder's own screenshots) · **L-1482 ✅ SHIPPED** (`auditShadowCasters()` tripwire — a caster with `autoUpdate=false && needsUpdate=false && !map` is UNSATISFIABLE and silently invalidates every lit draw in the scene; C04 §SHADOW rules 12 + 13) · **L-1483 ⛔ OPEN — MEASURED** (the swap PERSISTS `'webgl'` over an explicit `'webgpu'` pin, so the pin is destroyed **on disk** and the next boot starts on WebGL before the heuristic runs — the L-203 defect re-created at a seam that already owns the non-persisting `backendOverride` parameter it needs) · **L-1484 ✅ FIXED** (`depth-buffer.test.ts` — 3 RED tests carried in six lane briefs as an *“upstream vitest-4 quirk”*; the mock used `vi.fn().mockImplementation(arrow)` with `new`, so the suite never reached ONE assertion about `logarithmicDepthBuffer` — the repo's only production site, and the single constructor-option asymmetry of the broken backend) · **L-1485 ⛔ OPEN — MEASURED** (`dispose()` drops the in-flight `'batch'` shadow suppression AND the user's `_shadowPrefs`, so a swap mid-generation runs a full depth pass over every `castShadow` mesh for the rest of it — the exact cost the swap was performed to avoid — and silently turns *Cast shadows OFF* back ON) · **L-1486 ⛔ OPEN — MEASURED** (NINE renderer-caching services are never rebound by §RENDERER-LIVE-SWAP; `ViewportPathTracer._renderer` is `readonly` and cannot be) · **L-1487 ⛔ OPEN — LATENT** (`PBRSceneUpgrader` stamps `mat.envMap` PER MATERIAL from a PMREM owned by a renderer that is not the live one; inert today ONLY because Phase 5 pins `hdriPresetId:'none'`) — 8 of 10 used; L-1488–L-1489 free |
| L-1490 – L-1499 | JOIN2 — mitred wall joins lost on project reopen (§WALL-JOIN-LOAD-SKIP deferral); creating an element repairs them | 2026-08-20 | ⏳ IN FLIGHT |
| L-1500 – L-1509 | DEPLOY — manual Fly deploy of 2026-08-20; the server artefact could not boot | 2026-08-20 | **L-1500 ✅ FIXED** (two barrel imports put `@thatopen/ui` in the SERVER bundle; `dist/index.cjs` died at boot — caught by the §L-442 smoke gate, nothing shipped; **closes L-1436** as a side effect) · **L-1501 ⛔ OPEN** (no structural gate on a browser-only external entering a server bundle) — 2 of 10 used |
| L-1510 – L-1519 | GPU2 — SSGI/TRAA collapse the scene the moment they are switched on; + no ad-hoc render-quality override above the ADR-0094 cap | 2026-08-20 | **L-1510 ✅ FIXED** (⭐ `PassNode.getTexture()` is NOT a read — three r183 clones the base texture and `renderTarget.textures.push()`es it for an unknown name, while `setMRT()` (the call that makes materials EMIT those outputs) is separate. `activateSSGI()` read `diffuseColor`+`normal` off a pass built `needsGBuffer=false` ⇒ 4 attachments, 1-output shaders, every submit rejected. ⭐ The founder's own error carried the proof: a declared G-buffer downgrades two targets to `RGBA8Unorm`, so **four half-float targets can only be clones of `output`**. ⚠ The §L-253 guard meant to catch it could NEVER fire — it tested `!ssgi && !traa`, a state `activateSSGI()` had already made unreachable. Fixed by tracking the layout the pass was BUILT with, never the INTENT; RED-first with real `three/tsl`, 5 of 9 red at HEAD) · **L-1511 ✅ FIXED** (the audit turned up the worse one: device-loss recovery mints a fresh pass, then `activateSSGI()`'s idempotency guard is satisfied by the STALE `_cachedAo`/`_cachedGi` and returns — SSGI composited a DEAD render target for the rest of the session. ⭐ an idempotency guard that reads a CACHE instead of the thing the cache describes will confirm a state that no longer exists) · **L-1512 ✅ SHIPPED** (render-quality pin over the ADR-0094 ≥1200-mesh cap; pin runs BEFORE `applyBackendGate` so a `cinematic` pin on WebGL2 still refuses SSGI — a pin is a PREFERENCE, never a CAPABILITY claim; unpinned proved byte-identical over a 13-step sweep; and the re-drive that makes it reach the shadow map is itself red-tested — delete it and the pin 'works' everywhere except on screen) · **L-1513 ⛔ OPEN — MEASURED** (TRAA ALONE builds the G-buffer, fires `enabled:true`, and composites nothing: `_buildPipeline()` never calls `createTRAAFilter`) · **L-1514 ⛔ OPEN** (`packages/render-pipeline/` is an orphan rival `ScenePass`/`ZonePass` copy with ZERO importers, still declaring MRT unconditionally — pre-§L-253) · **L-1515 ⛔ OPEN — MEASURED** (the tier log prints `shadows=standard` when the ≥8000-mesh ceiling 18 lines later has turned them OFF — reports the INPUT to a decision as the OUTCOME, the L-1397 family) — 6 of 10 used; L-1516–L-1519 free |
| L-1520 – L-1529 | OPEN2 — circular/arched windows and doors get a SQUARE frame around a correct curved opening | 2026-08-20 | **L-1520 ✅ FIXED** (root cause CONFIRMED by `grep -c openingProfile` → **0** in BOTH builders: head/cill/2 jambs were four unconditional `BoxGeometry` at every LOD. L-1200 shipped the hole, L-1250–L-1252 the controls, and **neither slice's coverage table listed the 3-D frame at all** — an unlisted gap, not a regression. ONE producer `OpeningProfileFrameGeometry.ts`, placed beside `openingOutline()` in `geometry-wall` after review moved it out of `geometry-door`. A circular frame IS a ring — no cill, no jambs) · **L-1521 ✅ FIXED** (arched door: curved head, transom, glazed fanlight, leaf stopping at the springing; base edge omitted by GEOMETRY not index, so it is not coupled to emission order — a doorway has no cill to trip over. Rejected an arched leaf, which would discard the whole rail/stile/ironmongery construction) · ⭐ **The lane CORRECTED ITS OWN FALSE CLAIM**: `a0fc4370`'s message asserted the rectangular byte-identity pin passed; it had not been run, and it **failed on all three digests**. A general head-datum formula reduces to the old literals in ℝ but NOT in IEEE-754 (`h − ft` = 2.0500000000000002665 vs `(h/2 − ft) + h/2` = 2.0499999999999998224) — **every RECTANGULAR door in every project had been moving by one ULP**. Fixed in `b6ddcd9e`; the pin that caught it is L-957's, written two features earlier · ⭐ **The predicted cache-key bug was NOT real; a worse one next door WAS**: `_convertGroupToInstances` recovers sub-box size from `BoxGeometry.parameters`, which an `ExtrudeGeometry` lacks, so it falls to its `?? 1` guards and renders **1 m CUBES** — and windows are the ONLY family with instancing default-ON (L-1180), so that would have been the DEFAULT RENDERING OF THIS VERY FIX · **L-1522 ⛔ OPEN — capability loss, stated not hidden** (glazing grid OMITTED for profiled windows: a `double × circular` reads as one oculus, not two half-moons. The cell grid is a bounding-box construction; real arched/circular windows carry fan or radial tracery that no field on the record can describe. Omitted rather than drawn wrong) — 3 of 10 used |
| L-1530 – L-1539 | STAIR2 — stair redo does not work; Base/Top level not editable | 2026-08-20 | **L-1530 ✅ FIXED** (⭐ `proposeRailings()` emitted an event and walked away — but the chain is SYNCHRONOUS (`DOMEventBus.emit` is `dispatchEvent`; `CommandBus` has no `await` before `handler.execute`), so the railings ran from INSIDE the stair's own `execute()` with `source:'HUMAN_DIRECT'`, were refused as `structuralChildren`, and were pushed as SIBLINGS BEFORE it — redo replayed a railing before the stair it hangs on. ⭐ And `CommandManagerImpl.redo()` POPS before executing and only restores `if (result.success)`, so a refused redo is DESTROYED, not retried — which is why the founder's presses vanished SILENTLY. ⭐ The tell was already in the file: `undo()` had ALWAYS removed the railings, so undo and create disagreed about what the command OWNS — when undo and redo disagree about ownership, the operation has two definitions of itself) · **L-1531 ✅** (stable id — redo returns the SAME element, not a new one) · **L-1532 ✅** (§STAIR-VOID-FOLLOWS-SPAN — closes **L-1432**) · **L-1533 ✅ SHIPPED** (editable Base/Top level. ⚠ NOT a field write: the two ids are the ends of the span the stair is SOLVED AGAINST — `riserHeight × Σ riserCount === topElev − baseElev` — so a bare write yields a stair that FAILS ITS OWN VALIDATOR, renders at the old height and pierces the wrong decks. Re-derives risers through the creation path's EXISTING `deriveRisers`, redistributing proportionally so an L/U keeps its landing. `evaluateStairLevelSpanChange` is ONE gate that OWNS NO LIMITS — it composes the three existing authorities, which is precisely the shape whose absence produced L-1430's rival gate) — 4 of 10 used |
| L-1540 – L-1549 | RAC3 — two founder stair sentences do nothing in chat | 2026-08-20 | **L-1540 ✅ FIXED** (both sentences were stage-(a) **MISSES** — `{kind:'miss'}`, no tool call, no refusal, nothing said. The anchor vocabulary was a literal 8-word list, every word taken from the sentence on screen when it was written, so *“aligned to”* walked straight past it; lifted into a relation+determiner CLASS shared by both grammars) · **L-1541 ✅ FIXED** (⭐ the L-shape was NOT missing — it shipped 2026-08-19 with a Create-palette button calling `activateStairPathTool('L')` and was unreachable ONLY FROM LANGUAGE. Chat now publishes shape to the one `StairToolConfigStore` chokepoint; the ALIGNMENT half is DISCLOSED as not applied and names By Walls) · **L-1542 ✅ FIXED** (copy-by-reference has NO command on ANY element kind — refuses, resolves the level it CAN, and names `duplicate-level` **plus the trap that it excludes stairs**) · ⭐ GA gate 31 caught an over-claim mid-lane: reading a bare *“the selected stairs”* as a spatial anchor stole another capability's declared example — **selection-as-SUBJECT is not selection-as-GEOMETRY-SOURCE, and only a relation word separates them**. No selection-geometry channel was added (C67 §4 rule 20.b/20.c) · ⚠ verified through the resolver + tool-registry seam, NOT a model round-trip or a browser click — 3 of 10 used |
| L-1550 – L-1559 | SELECT1 — SHIFT + click multi-select | 2026-08-20 | **L-1550 ✅ SHIPPED** (⭐ the model was ALREADY multi-capable — `SelectionBus` (C27 §4) has carried `currentIds`/`selectMany` since the marquee work and three consumers already read it as THE set. Missing were a SHIFT+CLICK producer and — the part that made the first unsafe to build on — ANY write-back from the 3-D viewport: `select()` fired `bim-selection-changed` and never told the bus, so chat's *“delete selected”* acted on the PREVIOUS element. ⭐ The divergence ran in the direction hardest to notice: the 3-D highlight — the thing the user is looking at — was the one that was RIGHT. Mirror guarded by a new `isDispatching`, because the bus's own source guard cannot stop a `'3d-canvas'`-labelled echo) · **L-1551 ✅/⚠** (marquee over instanced walls returned ONE synthetic `instanced-group-<key>` id tested against the group's UNION AABB — a rectangle in one corner selected the WHOLE LEVEL. **L-813 at a THIRD site**; fixed for the click path, LEFT STANDING in the marquee path and said so) · **L-1552 ✅** (delete at N>1 → `DeleteElementsBatchCommand`, ONE undo entry, proved by a test that BUILDS the bar and CLICKS its buttons) · **L-1553 ✅** (property panel REFUSES at N>1 rather than presenting one element's values as the set's) · **L-1554 ✅** (⭐ a button marked only `aria-disabled` still dispatches `click`, and the listener ran the action anyway — **Move on a selected grid LOOKED REFUSED AND EXECUTED**. `aria-disabled` is an ANNOUNCEMENT, not an enforcement; worth grepping estate-wide) · ⚠ honest gap: the GLOBAL Delete key still takes the single-element route — 5 of 10 used |
| L-1560 – L-1569 | MAT3 — materials “go off for no specific reason” when swapping views | 2026-08-20 | **L-1563 ✅ FIXED — THE ONE THAT MADE IT HAPPEN, AND IT WAS FOUND LAST** (⭐ `runtime.events` is a `Map` of handler sets whose `emit()` NEVER calls `window.dispatchEvent`, while `VGSceneApplicator` subscribes via `window.addEventListener` — **two channels, NO BRIDGE**, so `view-selected` had never reached the class and `activeViewId` was null ALL SESSION. What actually drove it was ViewController's DIRECT call ending in `applyAll(activeViewId ?? undefined)`: null identity, `viewType === undefined`, every mesh down the 2D poche path WHILE IN THE 3D VIEW) · **L-1560 ✅ FIXED** (the rule *“3D carries no VG fill”* existed but was encoded as an ALLOWLIST OF FOUR WALL TYPE STRINGS — **the rule is about the VIEW, not about walls**. 11 of 12 families drift (`SlabPart #336699→#e8e8e8`, `Stair #8b4513→#c8c8c8`, …); only `WallPart` survived, which is EXACTLY why it read as “no specific reason”. C84 EI-8: DELETES the second producer rather than extending the allowlist) · **L-1561 ✅** (`view-selected` carries `viewId:null` for 3D on every non-rail path; the handler was `if (viewId)`) · **L-1562 ✅** (⭐ the same defect arriving THROUGH the fix: `vgOriginalMaterial` taken once and never re-taken, so a re-assigned material was REVERTED by the restore path on the next switch) · **L-1564 ⛔ OPEN** (⭐ the hazard THIS LANE'S OWN FIX WIDENED, disclosed by the lane that widened it: an authored intent `surface3D` override still mutates the SHARED material — walls-only before, all twelve families now. **Widening a correct rule also widens whatever hazard was hiding in the narrow case**) · mechanisms (a) loss-on-rebuild and (c) stale-NME-key both RULED OUT with evidence · one test arm disclosed as VACUOUS rather than counted — 5 of 10 used |
| L-1570 – L-1579 | WALL3 — is there wall corruption, and does it persist? | 2026-08-20 | **L-1570 ✅ CONFIRMED + DETECTOR SHIPPED** (**VERDICT: YES, and it PERSISTS TO DISK.** What is corrupt is wall BASELINES — authored geometry, not the render: the model is VISUALLY CLOSED and TOPOLOGICALLY OPEN. `WallStore.ts:764` clears `_sourceBaseLine` on any baseline update that does not supply one — i.e. EVERY user drag — so `ProjectSerializer.ts:580` falls through to the corrupt `baseLine`. ⭐ The `integrity` checksum in the founder's log does NOT contradict this: it is a BYTE-corruption checksum (L-334/L-360), so **a perfectly-checksummed snapshot of a corrupt model verifies green**. Detector is pure, store-free and CAMERA-FREE — every band from a wall's own thickness, because L-919's bug was a geometric verdict taken against a zoom-derived number. ⭐ The probe caught its OWN first defect — an unbounded arm called a far endpoint 2.7 m clear a 2790 mm penetration — which is the argument for shipping the probe first, made by the probe) · **L-1571 ✅ FIXED** (⭐ `previewMoveReweld` laundered ALL SIX refusal codes into `INCUMBENT_EXTENSION_REQUIRED` + `incumbentBreach` — the ONE flag §L-942-UNBLOCK was authorised to downgrade to report-only, for a SINGLE named trade the founder called *known, visible, undoable*. Five other codes rode through on it, including `STEM_COLLAPSE`/`STEM_REVERSAL` — literally the *geometric impossibilities* §L-942 reserves its BLOCKING arm for. The founder's *“re-baseline 2 walls by up to 75 mm”* sentence was false in all three claims: nothing would be re-baselined, 75 mm is an AXIAL POSITION not a shift, and the 101 mm band was dropped) · ⭐ `AMBIGUOUS_WELD_AUTHORSHIP` is **UNDECIDED, not undecidable**: `JunctionResolverV2.ts:166-173` MEASURES `participant.role` and `SemanticGraph.ts:891` DROPS it — the root fix is one piece of edge-metadata plumbing in `core-app-model`, outside this lane · ⛔ deliberately did NOT build the T-join trim: that shape was MANUFACTURED by the unrepaired move, and trimming would silently shorten the founder's wall by 310 mm to conceal evidence of an upstream defect (ADR-0299: *a recovery that conceals is a defect*) · changed NO decision — `allowed`/`ok` byte-identical on every input; whether those five codes should BLOCK is a C83 §10.6 amendment and the FOUNDER'S CALL — 2 of 10 used |
| L-1580 – L-1589 | GIS2 — the per-parcel data panel is not accessible; restore it as a section under the GIS rail panel | 2026-08-20 | **L-1580 ✅ FIXED** (⭐ the deeper defect, and the reason re-mounting alone would have shown an EMPTY panel: at HEAD `git grep refcat -- packages/schemas/src` → **0**, and `ParcelFeature` reached exactly ONE consumer outside the providers — the map modal's closure. The card's `refcat`/address/registry-area/CRS/licence were a FETCH RESULT HELD IN A CLOSURE; `site.setParcelBoundary` persisted the ring and dropped the rest. C57 §1.4 breach. New L0 `ParcelProvenance` composing C62/ADR-0280 `SourceProvenance`, threaded through ONE command, ONE write) · **L-1581 ✅ FIXED** (⭐ the founder was RIGHT that nothing needed creating — 11 of the 13 files in `site/parcel/` pre-date this lane. The card was built INSIDE `SiteBoundaryMap2D`'s `showParcelCard` closure, so it lived and died with the modal and no other surface could mount it. LIFTED, not re-authored: −162/+138, the map now calls the same `buildParcelCard()`. ⭐ en route, a SUBSTRING test `/footprint/i.test(parcel.source)` was one provider rename from presenting an OSM building outline as a legal cadastral parcel with a `Ref` row — now a stored `kind`, regression-armed with source `overture-buildings`. Stub card's SAMPLE referencia + ≈500 m² area deleted: a fabricated number under a warning banner is still fabricated) · **L-1582 ✅ SHIPPED** (section + button, reconciling the founder's two phrasings; THREE states — no boundary / provenance not recorded / full card — and ALL THREE carry the button, because a refusal with no way to act on it is the L-942 shape. Button dispatches the DECLARED `site.map-2d` action, so it renders disabled with a reason if unregistered. Reachability proved at the DOM, not a function return) · **L-1583 ⛔ OPEN — DELIBERATELY NOT BUILT** (⭐ a 're-resolve provenance' button would fetch what the cadastre publishes at a point INSIDE the stored ring — for a hand-drawn boundary, the NEIGHBOURING legal parcel — and stamp a genuine referencia onto geometry no authority published. Indistinguishable on screen from a true record: strictly worse than honest absence. Area agreement is shape-blind; ring identity at the 0.111 m quantum is the guard needed) · **L-1584 ⛔ OPEN** (`gisActionRegistry.test.ts` 5 s timeout under parallel load — `transform 78.41s` vs `909ms` standalone; pre-existing, merge-blocking suite) · **L-1585 ⛔ OPEN** (Site Inspector needs a module-backed action kind; inverting the registry's `entryPoints:[] ⇒ disabled` invariant was not a trade to make unasked) — 6 of 10 used; L-1586–L-1589 free |
| L-2030 – L-2049 | INSP1 — Inspect mode: windows render black; "all categories should be mapped" | 2026-08-21 | **L-2030 ✅ FIXED** (⭐ the windows were not badly coloured — they were the ONLY elements NOT coloured: `_applyGhostToNonRoomMesh` `return false`d for every mesh under a door/window group, i.e. NO ghost material, leaving openings the only families still wearing authored opaque materials against a 4–10% translucent model. The skip was itself a "fix" for a REAL but different symptom — 12 stacked sub-meshes ghosting to a bright blob — and answered "too bright" with "not at all". Replaced by WEIGHT: ghost alpha is per ELEMENT, not per MESH. ⚠ the exact shading term that renders it BLACK is still unidentified; two candidates ruled out by measurement, one not) · **L-2031 ✅ FIXED** (three Inspect entry points overwrote the `colorWrite:false` hit-proxy material, one of them at opacity 0.9) · **L-2032 ✅ FIXED** (6 categories wired, 20 element-family stores published, 14 families absent — and SILENTLY so; ONE declaration + a coverage gate that compares SETS against the engine bootstrap; the 3D lens also stopped guessing `userData.elementType` by stripping a trailing "s") · **L-2033 ✅ FIXED** (TWO rival room-attribute ladders — a 21-entry dropdown over an 8-key switch; 13 attributes selectable and unreachable, and the two disagreed on Clear Height) · **L-2034 ✅ FIXED** (attributes now DERIVED from the records; an unmappable category REFUSES by name instead of drawing a Low→High legend over a column of dashes) · **L-2035 ✅ FIXED** (`_focusedElementType` cleared only by leaving Inspect, so returning to Rooms re-applied focus on the old family forever) — 6 of 20 used; L-2036–L-2049 free |
| L-2500 – L-2599 | AUD-3 — PERFORMANCE, end-to-end beyond the frame loop ("are there performance gaps that could be avoided?") | 2026-08-21 | **L-2500 ⛔ OPEN — MEASURED** (⭐ `OutlineNode.updateBefore` has NO early bail and `updateBeforeType='frame'`; PRYZM wires TWO nodes and `initScene.ts:3377` activates them unconditionally on every WebGPU session, from boot, forever. Probe against the real vendored `three@0.183.2`: per node, EMPTY selection — **2 full-scene renders, N `renderObject()` submissions, 7 quad passes, 10 RT switches**, flat from N=500 to N=8000. Full frame census: **6 scene-graph walks, ~11 694 submissions, 7 796 of them (67 %) outlining NOTHING**. The safe fix is three's own `return false` skip in `NodeFrame.updateBeforeNode` — no material, no shader, no recompile; measured at **0/0/0/0**) · **L-2501 ⛔ OPEN** (two corrections to the commissioning brief: it is **2×N** extra draw calls not 4×N — the mask pass submits ZERO when idle; and `7591 ≈ 2×3898` is **numerology**, see L-2502) · **L-2502 ⛔ OPEN — MEASURED** (⭐ on WebGPU `info.render.calls` is *"render calls SINCE THE APP HAS BEEN STARTED"* and `Info.reset()` does NOT zero it; PRYZM reads it in BOTH perf readers and labels it `drawCalls … ← LAST RENDERED FRAME`. The real field `info.render.drawCalls` is read nowhere. On `webgl-classic` the same field DOES mean per-frame draw calls — **one line, two meanings, no label**. ADR-0338 §1's "7589 draw calls" needs re-measuring; this lane did not) · **L-2503 ⛔ OPEN** (the `_viewSwitchInProgress` outline guard at `RenderPipelineManager.ts:1283-1310` is a **NO-OP** — all three consuming reads are in pipeline BUILD methods; its own comment asserts it works, three lines below a corrected comment warning about this exact class) · **L-2504 ⛔ OPEN — MEASURED mechanism / PROJECTED magnitude** (⭐ `CreateWallCommand.ts:446` calls the deep-cloning `wallStore.getAll()` per wall ⇒ **N(N−1)/2 clones**, for a `_neighbourSnapshot` read ONLY by `undo()` — and load pushes no undo and calls `clearHistory()`. **The identical guard already exists 200 lines above at `:245`.** Clone cost 1.36–17.34 µs ⇒ projected 2.2 s @1000 walls, 20.2 s @3000. `WallStore` is the ONLY store that deep-clones in `getAll()`) · **L-2505 ⛔ OPEN — MEASURED** (`WallStore.add()` is where a batch stops being a batch: **368 ms / 1000 walls, copies = N(N−1)/2 exactly**; `resolveLevel` cold ~0.5 s @1000 walls, hidden by a memo; `beginGenerationResolveCoalesce` has ONE caller and `HouseLayoutExecutor` is not it; nobody ever calls `endGenerationResolveCoalesce`, so every residential generation ends in a fixed **1.5 s dead-wait**) · **L-2506 ⛔ OPEN — MEASURED** (⭐ EdgeProjector: **99.6 % of every crop-drag re-projection is provably identical work** — 4 936.7 ms invariant vs 21.7 ms crop-dependent; stage-splitting the cache is **26×–228×**. Plan crops are 100 % projection-invariant; only the VERTICAL elevation/section crop is a real input. The 20 `CANCEL-SUPERSEDED` messages are the guard WORKING — ~1.1 s wasted, not ~99 s. **A rename re-projects 359 groups**) · **L-2507 ⛔ OPEN** (⭐ two caches in series with EXACTLY OPPOSITE crop policies — NME `cropKey` vs EPS `clipSignature` — so whichever family the founder drags in, one goes **100 % cold**; and `exportForView` runs OUTSIDE the supersede check, so a cancelled pass pays it in full) · **L-2508 ⛔ OPEN — MEASURED** (⭐ snapshot size tracks **EDIT HISTORY, not the model**: elements measure 1 133 B each, but the founder's own 793 elements → 16.6 MB, so **95 % is not his building**. `TemporalGraph.serialize()` copies an append-only log into EVERY snapshot behind a **warn-only** 200 000 cap ⇒ up to **1 018 MB stored**, O(n²) in log length. ⚠ the brief's "`:1387` embeds a copy in each of 20 snapshots" was WRONG as written but named the right line) · **L-2509 ⛔ OPEN** (`_versionMirror` CONFIRMED and worse — `.clear()` does not exist, the only `.delete` is project DELETION, and `warm()` cursors the ENTIRE store on hub mount, so **every project the user ever saved** is resident ×2 for the session. `EdgeProjectorService` and `NativeElementMeshExporter` already have the project-switch eviction pattern; `VersionCacheStore` is the outlier) · **L-2510 ⛔ OPEN — MEASURED** (⭐ `dist/index.html:5` — a **5 909 848 B parser-blocking `<script src="/cesium/Cesium.js">` with no defer/async/module, ABOVE the inline boot CSS**, so the paint-on-first-byte skeleton cannot paint. Injected by `vite-plugin-cesium`, **production only**, and the `vendor-cesium` manualChunk it should have used is **dead code**. **24 457 491 B raw / 5 935 804 B gzip paid before first paint** — and `src/main.ts:9-11` claims the exact opposite) · **L-2511 ⛔ OPEN — the redirect** (the frame loop is **CLEAN**: zero `.traverse(` in `UnifiedFrameLoop`/`RenderPerformanceService`, and all 7 in `RenderPipelineManager` are event-driven. A 123-site nested-scan sweep found **no second 550 ms-class quadratic on an interaction clock**. ⭐ So the stutter is L-2500's six scene draws + PERF1's pointer-clock reads — **three clocks, three owners**, and Scene Registry answers the EDIT clock, not this week's complaint. ⚠ the traverse census produced **four different numbers in one session** — 316 / 332 / 479 / 363, against ADR-0302's ~120: **quote the command or do not quote the number**) · **L-2512 ⛔ OPEN + honest negatives** (CLEAN and recorded so they are not re-audited: `setInterval` perfectly balanced, observers 11 vs 20 disconnects with 3 boot-only exceptions, `.subscribe(` disciplined, zero top-level `await`, view-switch and tool-activation teardown genuine, `init*` files one-shot not per-switch. **NOT clean:** `.on(` = **260** vs `.off(` = **8** in `apps/editor/src` and **all 8 are maplibre** — zero `.off()` against the PRYZM bus anywhere; and `closeProject()` is two lines that free **nothing**, a category the ADR-0297 position may not cover ⚠ cross-domain) — **deliverable [APPLICATION-PERFORMANCE-LEDGER](APPLICATION-PERFORMANCE-LEDGER.md)** (21-row backlog ranked saving × confidence × blast radius; 9 founder console commands with what each outcome would prove; explicit NOT-REACHED register). **No source changed.** — 13 of 100 used; L-2513–L-2599 free |
| L-2600 – L-2699 | AUD-4 — robustness / error handling: "is all super robust?" — the SILENCE class, where a failure and a legitimate result are the same value | 2026-08-21 | **READ-ONLY AUDIT, no source changed.** Register: [SILENT-FAILURE-REGISTER](SILENT-FAILURE-REGISTER.md) · **L-2600 ⛔ TOP FINDING** (IFC export: **20** bare `if (!mesh) continue;` / `if (!geometry) continue;` drop points across **10 of 13** readers with NO diagnostic, then `✅ IFC EXPORT SUCCESS`. ⭐ LIVE not latent: `findMesh` matches `userData.id`, an InstancedMesh has none, and `window/handrail/stairRailing` are instanced **by default** — handrail exports a rail with **no balusters or posts**; **every window exports as the invisible white hit-proxy BOX**, which is worse than absence because it looks plausible) · **L-2601 ⛔** (the export's only sub-`throw` error channel is `debug()` → `console.debug` = DevTools *Verbose*, **hidden by default**; the sibling arm 60 lines up was fixed for exactly this and the fix was not carried down; and `alert('… See the on-screen log …')` names a log that needs a hidden global) · **L-2602 ⛔** (⭐ `surfaceCommandFailure` — the repo's OWN cure, whose docblock says *"now every failure reaches the user"* — is used at **4 of 33** dispatches **in the file that defines it** and **0** elsewhere; estate-wide **188** optional-chained `?.bus?.executeCommand`: 8 awaited · **143 `.catch(console.*)`** · 9 real · **28 fire-and-forget**) · **L-2603 ⛔** (5 element-creation plan tools — elevation/floor/roof/room/slab — dispatch with no `await`, no `.catch`, no refusal) · **L-2604 ⛔** (Save paints **"Saved"** at `:343`, thirteen lines before the L-269 `indexPersisted` gate at `:356`, and `markClean()` on the way **disarms the `beforeunload` flush**) · **L-2605 ⛔** (⭐ `check-runtime-arg-omitted.ts` — the gate built for this very class — is COMMITTED and registered in **no** runner, **no** CI job, **no** npm script; 2 orphan gates of 67 tracked) · **L-2606 ⚠** (the gate's regex is NOT being dodged — **0** optional-marker, **0** multi-line evasions, stated as a negative; the gap is SCOPE: 21 seen vs **188 + 224** unseen, and ⭐ **it counts ARGUMENTS, never OUTCOMES**) · **L-2607 ⚠ LATENT** (two rival event catalogues: **212 vs 201** names, **83 shared, 34 with DIFFERENT key sets**; the dead half is the one self-describing as *"the single source of truth"* and imported by 84 files — L-1563 pre-loaded with 34 rounds) · **L-2608 ⚠** (`element.changeType` → `console.warn(… — ignored.)`; cure applied to `element.updateMark` 30 lines below) · **L-2609 ⚠** (`ScheduleExtractor` launders absence to `"0.00"` in 4 columns **one line above** 2 columns that emit `FINISH_UNDETERMINED` correctly) · **L-2610 ⚠** (an undo/redo jump that THROWS is silent; a partial one toasts — the worse outcome gets the weaker treatment) · **L-2611 ⚠** (**28 of 702** click handlers: bare guard-`return`, no surface anywhere — incl. the primary Start/Exit RENDER button) · **L-2612 📋** (catch census: **3865** blocks, **0** bare-empty ⭐, but **243 return-only + 1071 log-only = 1314** that tell nobody anything, against **275** `showToast` sites product-wide; `esbuild` does not strip `console`) · **L-2613 ✅ HONEST NEGATIVES** (`aria-disabled` CLOSED estate-wide 15/15; every `CHAT_UNAVAILABLE` escape hatch EXISTS; no new call-vs-record defect; `RenderQueuePanel` progress is real state) · **L-2614 🔬 PROPOSED GATE, DESIGN ONLY** (`check-dispatch-outcome-surfaced.ts` — 3 arms never folded, `§SILENT-BY-DESIGN` hatch mandatory, register in `run-all.ts` in the SAME commit; ⛔ 7 named blind spots, incl. ⭐ **it cannot check its own escape route** and ⭐ **it cannot see L-2600 at all**) · **L-2615 📋** (what was NOT swept: `server/`, every non-editor app, 48 plugins unread individually, DXF/PDF/sheet/glTF/Rhino, the CRDT path, and **any runtime behaviour** — source read only) — 16 of 100 used; L-2616–L-2699 free |

> ⚠ **L-1197 … L-1226 are PRE-LEDGER** and were minted under the old racing convention. Several were
> renumbered in flight. **Trust the ISSUE LOG for what they mean, not any lane report that cites
> them** — at least four lane reports quote a number they later changed, and one commit message
> (`bd15d644`) still cites `L-1202` for a row that landed as `L-1225`.

## 3. What this does NOT fix

- **It does not detect a collision.** Nothing yet asserts that each `L-NNNN` appears under exactly
  one heading in the ISSUE LOG, nor that a cited number exists at all. That is a real gate and it
  does not exist — the same class as the contract-suite gates built the same day
  (`check-contract-cited-paths.ts`, `check-contract-index-equivalence.ts`). **Proposed as its own
  row when someone picks it up.**
- **It does not stop a lane ignoring its block.** It is a convention with an owner, not an
  enforced invariant. ⭐ Do not describe it as enforced; that would be exactly the
  "declared but never called" defect the amendment register catalogues.
- **It says nothing about ADR or SPEC numbering**, which have their own drift (ADR 268 → 272 and
  SPEC 96 → 97 in a single day, the sixth recurrence of the count-staleness failure).
