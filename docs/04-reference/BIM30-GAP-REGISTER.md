# BIM 3.0 Gap Register — every gap, twice classified

> **Stamp**: 2026-08-14 · **Phase 0** of the founder's BIM 3.0 master directive · **Branch**: `main` · **HEAD**: `0c098d81`
> **Recount (lane L1)**: 2026-08-13, **MID-SESSION** — gates executed between `9fc6804d` and `b61f0ba3`; every re-run row carries its reading and the SHA it ran at in-row. Seven other lanes were landing commits during the pass (ten landed mid-recount), and readings marked *mid-flight* read uncommitted lane work in the shared tree. **An end-of-session re-verification pass will re-run every gate**; where it disagrees with a reading here, the later execution wins.
> **Recount (lane L-STAMP)**: 2026-08-14, **END-OF-SESSION** — this is that pass. Every deciding gate re-executed between `a5211b46` and `0c098d81` (~28 gate runs + 9 test-suite executions, each named in-row); other lanes were still landing commits under it, and rows whose number moved mid-pass carry both readings with their SHAs. Where this stamp and an earlier one disagree, **this one was measured later and wins — until the next execution.** Founder authorisation for this restamp: "stamp it from fresh gate runs" (2026-08-14).
> **Restamp (lane R2)**: 2026-08-14, **at `a75e8e1e`** — a second, later pass over the **13 rows a
> day of paying work had moved**, after the L-STAMP pass above. Every reading was produced by an
> instrument this lane executed itself: 11 gate runs + 5 test/harness executions, each named in-row.
> **§9.0 is 55 / 20 / 7.** Four rows closed (MT-10, CO-03, GE-04, PV-08); **six moved and did not
> close, two of which this lane was briefed were closable** — `GE-12` (its own new gate reads 13/13
> and corrects this register's arithmetic upward) and `GE-09` (85 offenders against a baseline of
> 86). Where a brief, a commit subject and a gate disagreed, **the gate decided, every time**
> (C70 §0.2). `MT-09` was **not** re-measured and says so in its own cell rather than carrying a
> two-day-old number forward as current.
> **This register is now STATUS-BEARING.** Every classified row carries a **Status** column with exactly three values — **CLOSED**, **OPEN**, **UNPROVEN** — and every CLOSED row names *how* the closure was established. A row closed on an **executed gate run or an executed test** says so and names the instrument. A row closed only on a commit subject is marked **CLOSED (BY-READ)** and is explicitly the weaker evidence; the two are never laundered into one another (C70 §0.1).
> **Companion**: [`BIM30-DO-NOT-REBUILD.md`](BIM30-DO-NOT-REBUILD.md) — the architecture-protection half. Read them as a pair: this document says what is missing, that one says what must survive the fixing.
> **Authority**: the contracts, not this file. [C70](../02-decisions/contracts/C70-BIM30-TARGET-AND-GOLDEN-CHAIN.md) · [C71](../02-decisions/contracts/C71-GRAPH-AND-TOPOLOGY.md) · [C72](../02-decisions/contracts/C72-PROPAGATION-AND-PREVSTATE.md) · [C73](../02-decisions/contracts/C73-GEOMETRY-DETERMINISM-AND-TOLERANCE.md) · [C74](../02-decisions/contracts/C74-CONSTRAINT-HONESTY.md) · [C75](../02-decisions/contracts/C75-PROVENANCE.md) carry the binding invariants and the measured facts. This register **indexes** them into one addressable list; where it and a contract disagree, **the contract wins**.
> **Doctrine**: no cell is inferred. Where evidence is absent the cell reads **UNPROVEN**, which is neither a pass nor a fail — it is *"nobody looked"* (C70 §2.2). No count below is a new measurement; every one is cited to the artefact that measured it, and counts rot in documents (C70 §0.2) — **re-run the gate, do not trust this line**.

---

## §0.0 — The register's own staleness, and the rule that stops it recurring

**This document went stale by ~199 commits and had to be reconstructed.** It was stamped at HEAD
`a8f15234` on 2026-08-12 and read at `bbcc11cd` on 2026-08-13 with **no status column at all** —
so the state of every one of its 82 classified rows had to be recovered by matching commit
*subjects* to rows. That is **BY-READ**, the mode C70 §0.1 forbids and the mode this repository has
documented, repeatedly, as reading better than it executes. A register whose entire job is to
answer *"how far are we"* answered it from prose.

Worse than staleness: the register was **wrong in the direction of pessimism**. Nine gates it
recorded as ABSENT exist and run; four rows it recorded as open defects are closed and pinned by
executed gates. A stale gap register overstates the gap, which is the same class of error as a
stale gate understating it — both substitute a document's memory for a measurement.

> ### The re-stamp rule — binding
>
> 1. **A row's status changes in the SAME commit that changes the row's state.** A commit that
>    closes a gap and leaves this file untouched is an incomplete commit.
> 2. **A status may only be set to CLOSED from an artefact that was RUN.** Name the gate or the
>    test. If the only evidence is a commit subject, the status is **CLOSED (BY-READ)** and it
>    stays that way until an instrument confirms it.
> 3. **Where a gate decides a row, the gate is the authority — not this table.** Counts rot
>    (C70 §0.2). Re-run the gate; do not cite this line.
> 4. **A closed row is never deleted and never renumbered** (C70 §5.4's spirit). It stays, with
>    its status and its evidence, so the closure is auditable and the id is never recycled.
> 5. **UNPROVEN is a permitted answer and is not a gap in the work.** It means nobody measured.
>    It is neither a pass nor a fail (C70 §2.2), and it may never be collapsed into either.

---

## §0 — Why every gap carries TWO classifications

The founder's directive demands both a **maturity** grade and an **implementation type**, and the
two answer different questions. Grading alone cannot distinguish the two failure modes this
repository actually has:

- **A disconnected subsystem is not a missing subsystem.** `DependencyResolver` emits four typed,
  catalogued cascade events and nothing listens (C72 §0). The generic propagation layer is not
  absent — it is *unreachable*. Fixing it is a call site; building it would be a quarter.
- **A missing subsystem is not a disconnected one.** There is no 2-D boolean union anywhere in the
  tree, and three files independently defer one (audit §17.6). No amount of wiring produces it.

Maturity says how far the capability has climbed. Implementation type says **what kind of work
closes it**, and therefore what it costs and who can do it. A register carrying only the first
would rank the mock solver and the dead cascade identically; they are nothing alike.

### The maturity ladder (per-gap)

| Grade | Meaning |
|---|---|
| **G0** | absent — no representation of the concept exists |
| **G1** | represented — a type, a name, a declaration exists; no instances flow |
| **G2** | persisted — instances exist and survive save/reload |
| **G3** | relational — the datum is connected to other data, addressable from them |
| **G4** | computational — an algorithm consumes it and produces an answer |
| **G5** | deterministic reasoning — the answer is reproducible, refusal-honest, gate-pinned |
| **G6** | closed-loop — the answer feeds back into the model and maintains itself |

### The implementation types (per-gap)

| Type | What the work actually is |
|---|---|
| **WIRING** | the machinery exists and is not connected to its caller, its subscriber, or its registration |
| **RETENTION** | a value is computed and discarded; the fix extends an existing computation's lifetime |
| **EXPOSURE** | a capability is reachable internally but has no surface (verb, slot, API) |
| **INVARIANT** | the behaviour is right sometimes; the fix is a rule, a gate, or a refusal that makes it right always |
| **MISSING VERB** | no command id, or an id with no handler |
| **MISSING ALGORITHM** | the computation does not exist in the tree at all |
| **MISSING SOLVER** | a simultaneous system with no closed form and no numerical procedure |
| **MISSING PERSISTENCE** | the value exists in memory and is never serialized, or is serialized and never rebuilt |
| **MISSING COLLABORATION** | multi-client behaviour that no single-client change can produce |
| **ARCHITECTURAL CHANGE** | the current structure cannot host the fix; something must be replaced |

> **§0.1 — the headline finding, stated before the table so it cannot be missed.**
>
> ## ARCHITECTURAL CHANGE count: **0**.
>
> **Not one gap in this register requires replacing a subsystem.** The audit's REPLACE list is
> empty and *survived the §17 downgrades* — the two downgrades that made the report worse did not
> add a single replacement item ([CD §H](BIM30-CONTINUITY-DELIVERABLE.md)). One candidate for
> replacement was considered and **rejected**: merging the dual edge vocabulary (SemanticGraph's
> 25 types vs the UBG's 10), resolved by **mapping** instead, because merging the stores would
> change the serialized shape of `ProjectSnapshot` and break snapshot v3 for a naming preference
> (C71 §4.1, CD §E).
>
> Four items are **construction** — new code, not new architecture — and are labelled as such
> rather than smuggled in: the planegcs WASM binding (fills an existing adapter, and **only if a
> constraint family proves it needs solving**, C74 §4.2), a model-space constraint store (follows
> the component-editor's existing store pattern), a general 2-D boolean, and a real clash engine.
> They appear below as MISSING SOLVER / MISSING PERSISTENCE / MISSING ALGORITHM. **Construction is
> not architectural change**, and conflating the two is how a wiring list becomes a rewrite
> proposal.
>
> **The distribution is the argument.** WIRING · RETENTION · EXPOSURE · INVARIANT dominate this
> register, and that is a *finding about PRYZM*, not a coincidence: the signature hazard here is
> **authored-but-unwired**, found five times in one session (C74 §0 enumerates them). An audit of
> *existence* passes in this repository; an audit of *reachability* is the one that fails.

---

## §1 — Graph & topology (owner: C71)

| ID | Gap | **Status @ `bbcc11cd`** | Maturity | Impl type | Evidence | Contract | Smallest change | Blocked by |
|---|---|---|---|---|---|---|---|---|
| **GR-01** | **`contains` has no first-party writer.** Two production surfaces read it; only IFC import writes it. On a native project *"this room contains nothing"* and *"nobody ever wrote this edge"* are the **same answer** | **CLOSED** — **executed gate** `check-graph-write-coverage` re-run at recount (`b61f0ba3`): `contains` reads **writer ●1** (`CreateFurnitureCommand.ts:249`, landed `e1e375d0`) · reader ●2 (`WorldModelAdapter.ts:200`) · rebuild ●1 (`rebuildSemanticGraph.ts:256`, `76a212aa`), and **no `contains` finding remains** — the gate's entire ledger is [1] at **2/2**, both rows `partOf` (writer + reader, each DECLINED with a written §2.5 rationale demanding an ADR first). ⚠ The row's headline is now factually wrong in the healthy direction. *Recount 2026-08-14:* gate re-executed → **[1] 2/2 unchanged**, and `contains` is now **REACHED at runtime** — H5 (`graphruntime.cert.ts`, 11/11, results regenerated this pass) reads `getTargets('grt-room-1','contains')=['grt-furn-1']` from the live graph after a real `CreateFurnitureCommand`. **Founder decision 2026-08-14, recorded here because both DECLINED ledger rows were waiting on it: `hierarchyStore` + `parentId` is the SOLE hierarchy substrate.** That settles the prior question the §2.5 rationales named; striking the two `partOf` rows now needs only the ADR that transcribes the decision. | **G1** (declared; no native instances) | **WIRING** | EV-04 §1 · EV-05 §1 | C71 §5.2, §2.1 row 7 | write the edge where containment is already computed — `RoomContentsService._containedByCentroid` is a pull-time query today (EV-03 §5.3) | — |
| **GR-02** | **12 declared families with zero writers AND zero readers** — `unitOf` `levelOf` `servesZone` `precededBy` `supersedes` `branchedFrom` `causedFailureOf` `wasMitigatedBy` `exceededBenchmark` `replacedBy` `maintainedBy` `decommissionedBefore` | **n/a** — PARKED is not a gap. | **G1** | **NOT A GAP — PARKED** | EV-04 §1 | C71 §2.2, §2.3 · ADR-0320 | **none.** Parked is a different state from gap. No census, status doc, audit or roadmap may count these as missing capability (C71 §2.3), and they may not be deleted — that breaks `deserialize` on snapshot v3 (§2.4) | — |
| **GR-03** | The founder's brief and the corpus both say *"13 relationship types without writers"*. **Read with GR-01 + GR-02: 12 of the 13 are PARKED and one (`contains`) is the gap.** Recorded so the number is never re-inflated | **n/a** — anti-recount note. | — | — | EV-04 §1 | C71 §5.1 | none — this row exists to stop a recount | — |
| **GR-04** | **`joinedTo` is decided and unlanded.** ADR-0321 ACCEPTED; measured 2026-08-12 it is **not a member of `RelationshipType` and no writer exists**. Until it lands, C70 **C-INV-2** (wall connectivity is a lookup, never a per-query resolver re-run) is **UNPROVEN** | **CLOSED** — **executed gate** `check-topology-survives` → **[0] CLEAN, hard-0**: all five transitions MEASURED-PASS, and **C-INV-2 MEASURED-PASS by two independent observations** (a JSON round-trip destroying every resolver input, then a poison scene yielding 0 junctions — `getJoinedWalls` still answered all 5 walls). Commit `56ee3337`. Residual, printed by the gate: live-session reachability of the `_flush` call site is UNPROVEN. Recount @ `b61f0ba3`: `check-graph-write-coverage`'s C-INV-1 matrix reads `joinedTo` writer ●1 · reader ●1 · delete ●1 (type-aware d1) · REGENERATED — fully healthy. | **G0** | **WIRING** (writer over an index that is now retained) | C71 §3.7 · ADR-0321 · `WallFragmentBuilder.levelJunctions` at HEAD | C71 §3 | one flush-time emitter reading the retained junction index, both directions, with `{junctionType, junctionDegree}`; **remove-and-re-emit per level** (§3.4) and **never** key on `WallJunctionRecord.id` (§3.5) | — |
| **GR-05** | **`sitsOn` was the most-written edge in the graph with no typed reader.** ADR-0320 states `DependencyResolver` and `buildBuildingGraph` have since gained reads; **EV-05 measured zero readers on 2026-08-11**. Whether the reader is *reached at runtime* is **UNPROVEN** — the graph has no CA-21 equivalent | **CLOSED** — **executed gate** `check-graph-write-coverage` re-run at recount (`b61f0ba3`): `sitsOn` reads **writer ●18 · reader ●1 · rebuild ●2** and **no `sitsOn` finding remains**. The typed reader landed in `809b0915` (`DeleteLevelCommand.ts:174`) — and caught a real defect on arrival: a level delete that strands every element on a reloaded project. The runtime-reachability caveat this row carried is inherited by GR-18, not erased. *Recount 2026-08-14:* that caveat is now **discharged for the creation paths** — H5 measures `sitsOn` REACHED on the live graph through five real command executions (column, roof, plumbing, furniture, wall), plus one NOT-REACHED negative control. | **G3→G4, UNPROVEN** | **WIRING** | EV-05 §1 (zero readers) vs ADR-0320 Context (reads gained) | C71 §2.1 row 5 | re-measure at HEAD under `check-graph-write-coverage`; do not carry either claim forward | GR-11 (gate unbuilt) |
| **GR-06** | **`_rebuildSemanticGraph` regenerates 5 of 25 types** (`hosts`, `hostedBy`, `boundedBy`, `adjacentTo`, `partOf`) and fires only when the graph is empty — so `sitsOn`, `supports`, `connectedTo`, `connectedByStair`, `connectedByLift` are **PERSIST-OR-LOSE**. A pre-graph snapshot loses them permanently | **CLOSED** — **executed gate** `check-graph-persistence` (`5fb70137`) plus `a55ed23e` and `df8f67a2`: the rebuild is widened from authoritative fields and ARM C now names its losses. Residual OPEN, ledgered: `measuredAt` and `decidedBy` still load silently. | **G2, lossy** | **MISSING PERSISTENCE** + **WIRING** | EV-04 §3 · EV-05 §3 | C71 §5.4 · C70 I-INV-2 | widen the one existing function — it protects four of the five priority rows at once (EV-05 §3) | — |
| **GR-07** | **The persist-or-lose list is prose in EV-05 §3 with NO gate.** C70 I-INV-2 requires it named, mechanical and shrink-only. *A ledger in a document is not a ledger* | **CLOSED** — **executed gate** `check-graph-persistence` exists and runs (`5fb70137`) at a **NAMED ledger of 3**. The ledger is no longer prose — it is `graph-persistence-debt.json`, checked in both directions. Re-executed at recount (`b61f0ba3`): [1] at **3/3**, both directions — holds. | **G2** | **INVARIANT** | C71 §5.4 · §7.k | C71 §6 (`check-graph-persistence`) | the ledger becomes the gate's input file, checked in **both directions** (C70 §5.5) | GR-11 |
| **GR-08** | **`_rebuildSemanticGraph` exists in TWO byte-identical copies** — `packages/persistence-client/src/loader/ProjectLoader.ts` and `apps/editor/src/engine/persistence/ProjectLoader.ts`. A rebuild widened in one copy and not the other is a divergence no test would see | **CLOSED** — **executed gate**: `check-graph-persistence` ARM D measures **1 rebuild definition** and prints that C71 §5.3's TWO byte-identical copies have been paid down since. §5.3 is superseded by this reading. | **G2** | **INVARIANT** (dedup) | EV-04 §3 (both confirmed at HEAD) | C71 §5.3, §7.j | one owner; the gate arm (d) of `check-graph-persistence` asserts exactly one | — |
| **GR-09** | **`CreateWallCommand` writes no graph edges at all** — zero graph calls. The loader rebuild is currently the only source of a wall's graph presence, and it regenerates neither `sitsOn` nor `joinedTo` | **CLOSED** — recount 2026-08-14. The row was RE-OPENED mid-session when its headline was found TRUE at HEAD (the earlier `e878bdad` closure covered reachability of `wall.create`, not the graph write) and H5 used `getTargets('grt-wall-1','sitsOn')=[]` as its NEGATIVE CONTROL. It closes now on the write landing (`44552e1f` — `CreateWallCommand.ts:426` writes `sitsOn`, first consumer named: `DeleteLevelCommand.ts:174`) **plus an executed runtime read-back**: H5 re-run this pass reads `getTargets('grt-wall-1','sitsOn')=['GRT-L0']` **REACHED** from the live graph, and the harness's negative control moved to a never-created id. Headline now narrowed to FALSE on both halves: `sitsOn` written in-command; `joinedTo` written at flush by `WallRebuildCoordinator` (ADR-0321, GR-04). | **G1 for walls** | **WIRING** | EV-04 §2 [EXEC] · §5.5 | C71 §5.5 | a `sitsOn` write beside the existing store write (EV-04 §6.4) | — |
| **GR-10** | **`deserialize` silently drops malformed edges.** Any relationship missing `id`/`type`/`sourceId`/`targetId` is discarded on load — so malformed edges are written, saved without complaint, and vanish on the next load. *A defect that self-erases on reload is a defect nobody can reproduce* | **OPEN** — `check-graph-persistence` **executed**: ARM E finding `deserialize/silent-malformed-drop` stands and is ledgered. Drops are still neither counted nor reported. | **G2** | **INVARIANT** | EV-04 §4 | C71 §5.7 | count and report the drops; do not discard silently | — |
| **GR-11** | **All three C71 gates are unbuilt at HEAD** — `check-graph-write-coverage`, `check-graph-delete-integrity`, `check-graph-persistence` | **CLOSED** — **all three gates executed at HEAD**: `check-graph-write-coverage` → [1] declared 5; `check-graph-delete-integrity` → [1] declared 10 (`4eedca8c`); `check-graph-persistence` → [1] declared 3 (`5fb70137`). ⚠ The row's text ("all three unbuilt at HEAD") is **now factually wrong**. | — | **INVARIANT** | C71 §6 ("Measured 2026-08-12: none of the three exists at HEAD") | C71 §6 | build them beside the certification gates, each declaring **which graph** it measures (§6.1) | — |
| **GR-12** | **Mutation-update on MOVE is UNPROVEN for every family.** Semantic 5 of the six required semantics has never been measured, and no arm of any of the three specified gates covers it. `boundedBy` after a room-boundary change is the row most likely to be wrong | **OPEN — now INSTRUMENTED** — **executed gate** `check-move-propagation` (`ebe57ddd`) runs at HEAD: **4 region-derived families enumerated, 7 arms driven, 4/4 controls proven** (bootstrapped-tracker positive PC1; stripped-references N1; unreferenced-wall N2; scanner-discrimination N3). The slab-follows arm closed mid-session (`798f2cfd` — the wire was cut in two places). Final recount reading at `b61f0ba3` (**mid-flight**): **exit 3 STALE LEDGER, 3 measured findings** — two further ledger rows (floor-finish + ceiling host-reference readers) were paid by L2 work still uncommitted in the shared tree, awaiting their strike; C79 §5.2's five-state channel is still MEASURED-ABSENT for every family. *Recount 2026-08-14 (L-STAMP):* `check-move-propagation` re-executed → **[1] at 3/3** (the stale entries were struck; the finish trackers are now CONSTRUCTED at engine boot, `5b36fad5` — the §10.2b false-green is a true green); and the semantic-graph half is **no longer UNPROVEN — it is MEASURED-FAILING**: **H6** (`graphmove.cert.ts`, `7e346718`, 5/5 executed this pass, results 2026-08-14T07:54Z) drives a real wall move and reads **`boundedBy` STALE** — the move performed no invalidation and the edge still names a wall that no longer bounds the room — while `sitsOn`/`hosts` SURVIVED correctly and the re-detect control proves the corrective pass is observable. The five-state channel (C79 §5.2) remains MEASURED-ABSENT for every family. *Restamp 2026-08-14 (lane R2) — **the red turned green, and the row still does not close.*** Both instruments re-executed at `a75e8e1e` by this lane. **H6 `graphmove.cert.ts` → 5/5, results regenerated 2026-08-14T12:04:43Z, and `boundedBy` now reads `["east","north","south","west"] → []` = INVALIDATED by the move command** (it read STALE eight hours earlier). The fix is writer-side and in the move itself, not in a subscriber: `02157ebb` + `9fa40ae2` invalidate the bounded rooms' region-derived conclusions in `UpdateWallBaselineCommand.execute()` (and symmetrically in `undo()`), gated on §STEP7's own 1 mm threshold so a sub-mm edit stays C79 §5.2 `preserved`, plus the `WallRebuildCoordinator._flush` chokepoint; `6c8197be` removed the raw epsilon that fix minted by consuming `COINCIDENT_M`. `sitsOn` and `hosts` SURVIVED (correct — id-keyed edges are move-invariant by construction), and the re-detect control still observes the corrective pass. `check-graph-write-coverage` re-run → **[1] 2/2**, and its cannot-see block now prints **both** H6 readings rather than only the first. **WHY IT STAYS OPEN, precisely:** `check-move-propagation` re-executed → **[1] DECLARED-LEVEL, 3 findings at 3** — C79 §5.2's five-state channel is **MEASURED-ABSENT for every family** (`preserved` and `undetermined` return the same byte-identical ring), and roof has **no field that can carry a boundary reference at all**; `adjacentTo` is **VACUOUS** in H6 (empty on both sides — it measures nothing about that family); and SUBSCRIBER REACHABILITY — whether any production path drives a re-detect on a move — is still UNPROVEN (GR-18/CE-05 territory). Two of the row's three headline claims are now false in the healthy direction; the third is a ledger that has not reached zero. | **UNPROVEN → MEASURED (semantic half healthy)** | **INVARIANT** | EV-05 §4 · C71 §6.2(c) | C71 §1.4 | a move-time arm; it is named "the first thing to add" by the contract itself | GR-11 |
| **GR-13** | **Room identity is recovered by centroid proximity, not preserved.** `RoomDetectionEngine` mints a fresh UUID per detection; `mergeWithExisting` writes back the matched id only within `CENTROID_MATCH_RADIUS` = **2.0 m**. Move a bounding wall far enough and the room is **destroyed and unregistered** from `bimManager`, `elementRegistry`, the semantic graph and the spatial index — name, program, finishes and every edge with it | **CLOSED** — **executed gate** `check-room-identity-survives-wall-move` → **[0] CLEAN, hard-0**. CHECK 2 moves a wall **40 m** (centroid shift 20 m — ten times the old 2.0 m radius) and identity survives; CHECK 3 proves the split partition; CHECK 4 proves a disjoint room is not claimed. The match is **structural** (`boundingWallIds` overlap), not a widened threshold. Commit `b73cd5f7`. | **G3, fragile** | **INVARIANT** | EV-03 §5.1, R-7 · `RoomDetectionEngine.ts:121/491/842/869` | C70 C-INV-3 | a **second** match key beside the centroid in `mergeWithExisting` — e.g. `boundingWallIds` set overlap. Existing function, one extra predicate | — |
| **GR-14** | **`RoomGraphService` returns `[]` for three distinguishable cases**, and two further production read paths return `[]` unconditionally: `SpeculativeEngine` guards on `getEdges` and `HierarchyTreePanel` calls `getEdgesFromNode?.(…) ?? []` — **neither method has ever existed** on `SemanticGraphManager`. **The hierarchy tree's Furniture group has never rendered** | **OPEN, half closed** — the `getEdgesFromNode` half is **CLOSED (executed)**: `60c6acf9` typed the graph global and *the compiler dropped two dead methods*, with the fix recorded in place at `HierarchyTreePanel.ts:615`. The `[]`-conflation half is **OPEN** and now instrumented: `check-no-empty-means-unknown` (`3fd164a1`) runs and is **exit 3 STALE LEDGER** at HEAD. Recount: [1] at **95/95** at `9fc6804d`; late in the pass **exit 3** again (*mid-flight* — 2 entries in `apartment-layout` paid by uncommitted L6 work, strike pending). *Recount 2026-08-14 (L-STAMP):* first run at `87832530` read **exit 3, 74 findings against 73**; re-run after the drain commits (`f16dbc36`, `d56f548b`, `67d5f88e`) reads **[1] at 71/71** — the ledger moved 95 → 71 this session, shrink-only, decided by execution both times. *Restamp 2026-08-14 (lane R2):* re-executed at `a75e8e1e` → **[1] DECLARED-LEVEL at 67/67**, so the ledger reads **95 → 71 → 67** across the day, shrink-only every time. Two clusters were paid in between and both are named: `67902b35` — `RoomContentsService` stops forging *"this room contains nothing"* out of *"nobody recorded it"* (an 11-site cluster, pinned by 26 tests), which is the exact conflation this row's headline describes; and `d7c27985` — the site-data card stops asserting *"no street frontage"* about plots nobody classified. What remains is dominated by one shape the gate prints in full: `affectedElementIds: result?.affectedElementIds ?? []` repeated across ~12 batch handlers, plus `wall.openings ?? []` across the wall/occupancy paths. ⚠ Carried forward verbatim because it bounds every number above: the gate's own UNPROVEN block says ARM C **carries residual doubt by design**, whole-store enumerations are excluded as honest empties, the scope is **name-shaped** so a relationship question in other vocabulary is invisible, and it is **static only** — a site returning `[]` and a caller that would have refused correctly are indistinguishable to it. It under-reports and never over-reports; that is the safe direction for a ratchet and it is **not** completeness. | **G4, dishonest** | **INVARIANT** | EV-04 §5b · EV-05 correction | C71 §4.4 · C70 L-INV-1 | typed refusal unions; call `getTargets`/`getRelationships`; and **type `window.semanticGraphManager`** so a wrong-signature read is a compile error — EV-04 §6.3 calls this the highest-leverage of the three | — |
| **GR-15** | **`measuredAt`'s writer is broken.** `PhysicsEngine.ts:380` calls `addRelationship` **positionally** with four arguments against a one-object signature; `window.semanticGraphManager` is `any`, so TypeScript cannot catch it. One junk edge per session; `measuredAt` is never written correctly | **CLOSED (executed)** — re-verified at source: the positional four-argument call at `PhysicsEngine.ts:380` is gone; the file now guards `typeof sgm?.addRelationship !== 'function'` and documents the old defect in place. The typed global landed in `60c6acf9`, which the compiler proved by deleting two dead methods. **NOT RE-MEASURED IN THE 2026-08-14 PASS** — carried on its 2026-08-13 source verification; still one of the three weaker source-verified closures. | **G1** | **WIRING** (one line) | EV-04 §5a [verified], incl. its **downward severity correction** | C71 §5 | pass the object literal; type the global | — |
| **GR-16** | **No `graph.*` bus verbs.** `graph.query` / `graph.neighbors` / `graph.path` do not exist; the UBG has no runtime home and lives behind a dev-hook. *Machinery yes, exposure no* | **CLOSED** — **executed**: `apps/editor/src/engine/graphQueryBusHandlers.ts` declares `graph.query`, `graph.neighbors` and `graph.path`, wired through `initBusHandlers.ts` with a spec suite beside it (`393f81bd`). ⚠ The row's "zero graph verbs" is **now factually wrong**. | **G4 internal, G0 exposed** | **EXPOSURE** + **MISSING VERB** | CD §E, scoring table | C70 D-INV-1/3 | UBG → a `composeRuntime` slot + three read-only, refusal-honest verbs in the existing read-only capability class | — |
| **GR-17** | **The UBG's own header claims snapshot persistence it does not have** — no `ubg` key in `ProjectSerializer.ts` | **CLOSED (executed)** — recount 2026-08-14: `805c333e` took the row's first permitted fix — **the header was corrected in favour of the code** (`packages/building-graph/src/types.ts` now states the UbgSnapshot is derived, not persisted, with both measured halves of the drift in the header) — and the reason was made **executable**: `ubgSnapshotIsDerivedNotAuthored.test.ts` **4/4 green, EXECUTED in this recount**, so the next header that claims persistence without a serializer key fails a test rather than waiting for a reader. | **G4, mis-documented** | **INVARIANT** | audit §17.6 | C71 §4 | correct the header or persist it; do not leave the doc-vs-code drift | — |
| **GR-18** | **No runtime probe has ever been executed against a live graph.** Every claim in EV-04/EV-05 is source-level. A writer that exists and is never reached still reads ✅ | **CLOSED (executed)** — recount 2026-08-14: the probe now EXISTS and RAN. **H5** (`graphruntime.cert.ts`) executes real commands through the real `CommandManager` against production stores and reads the **live** `semanticGraphManager` back — **11/11 green in this recount**, results regenerated 2026-08-14T06:54Z: six writer paths REACHED (`sitsOn` ×5, `contains` ×1), one NOT-REACHED negative control, so both verdict states are demonstrated every run. This is exactly the "CA-21-equivalent read-back discipline" the row's fix column named. Its own NOT-COVERED block is quoted, not erased: gesture reachability, persistence, multi-client/multi-level, the families not named in its cases, delete/move propagation — those live in CE-05 (gesture half), H6 (move half) and the persistence gates. | **G4, probed** | **INVARIANT** | EV-05 §4 · C71 §5.8 | C71 §5.8 | a CA-21-equivalent read-back discipline for the graph | GR-11 |

---

## §2 — Propagation & prevState (owner: C72)

| ID | Gap | **Status @ `bbcc11cd`** | Maturity | Impl type | Evidence | Contract | Smallest change | Blocked by |
|---|---|---|---|---|---|---|---|---|
| **PR-01** | **The dead cascade, arm A: four typed CustomEvents, zero listeners.** `pryzm-dep-cascade` · `pryzm-room-reval` · `pryzm-hosted-reval` · `pryzm-structural-cascade`, all four carrying `TODO(TASK-15)` **on the dispatch line itself**. `setRebuildDispatcher` — the escape hatch the file's own comment points at — has 2 hits, both inside `DependencyResolver.ts`, and is **never called** | **CLOSED** — **executed gate** `check-propagation-reaches` → **[0] CLEAN, hard-0**: ARM A finds a live listener for `pryzm-dep-cascade` (`initDependencyCascade.ts`); ARM B finds `prevState` reaching the emitter **and** declared on the payload. Commit `1341b3bc` — three dead events buried, one wired for real. The two-terminal-states rule was honoured: no third state persists. | **G1** | **WIRING** *or* **DELETE** | C72 §0 (re-measured at HEAD 2026-08-12) · audit §17.2 | C72 §2.1 | one of exactly two terminal states — WIRED (both arms) or DELETED (dispatch + catalog + ledger row in one commit). **No third state may persist** | PR-02 (the other arm) |
| **PR-02** | **The dead cascade, arm B: `StoreChangeEvent` carries no pre-mutation state.** `{elementId, elementType, operation, timestamp}` plus two `@deprecated` flags. `DependencyResolver` subscribes to the **bus**, not the stores — so even if listeners appeared tomorrow, the diff-based work they exist to do would still be unreachable | **CLOSED** — **executed gate**: `check-propagation-reaches` ARM B2 measures `prevState` **declared on the payload** and ARM B1 that it reaches the emitter. Both halves, per §8.b. Commits `1341b3bc`, `8fc4e982`. | **G1** | **RETENTION** | C72 §0.2 (`StoreEventBus.ts:63–80`) | C72 §3.2 | a pre-mutation field on the change-notification type. **Both halves, or neither** — a one-armed fix is theatre, and worse than none because it is green (§8.b) | — |
| **PR-03** | **`prevState` is emitted by 5 stores out of 120** `*Store.ts` files in `packages/` — `WallStore`, `SlabStore`, `core-app-model/stores/ColumnStore`, `geometry-column/ColumnStore`, `RoomBoundingLineStore`. `WallStore.ts:19` says *"DependencyResolver uses prevState"*; it cannot | **CLOSED** — **executed gate** `check-prevstate-contract` re-run 2026-08-14 → **[0] CLEAN, hard-0**: 116 `*Store.ts` files read, **35 of 35 `update` emit sites across 17 stores carry `prevState`** — `HandrailStore`, `RoofStore` (both copies), `StairStore` (both copies) and `LiftStore` all now on the list — with the P1/P2/P3 negative controls proven firing on a planted tree in the same run. The ledger went 17 → 0 this session and the gate left its baseline. ⚠ The row's headline (5 of 120) is now factually wrong in the healthy direction. | **G2** | **RETENTION** | C72 §0.2 · EV-03 §7 (10 emitters / 23 subscribers / **5 actually read it**) | C72 §3.1 | copy the `§STEP7` convention to each store on the declared diff-consumer list — on **every** `update` emit site, not merely the listener type | — |
| **PR-04** | **`operation === 'delete'` returns `[]`.** The operation with the largest dependent fan-out computes nothing — *not a conservative default, a silent one* | **CLOSED (executed)** — `1341b3bc` ("delete stops answering `[]`") with `check-propagation-reaches` running [0] CLEAN over the wired event. Residual named by `check-graph-delete-integrity`: a cascade that COMPUTES empty at runtime is invisible to a static gate — that half of F-INV-2 is **UNPROVEN**, not green. | **G1** | **INVARIANT** | C72 §0 · audit §17.2 | C72 §2.3 · C70 F-INV-2 | a delete never returns an empty cascade **by design** | PR-01 |
| **PR-05** | **`clearGraphAuthoritative` has 1 definition, 0 production callers, 1 test caller.** The only release that runs is the inline GR2 branch — a manual wall `add`/`remove` outside a batch. **Every generated level keeps its rooms suppressed by default**; a user who then drags a bounding wall gets no re-detection at all, and nothing distinguishes *"rooms are correct"* from *"rooms were never recomputed"*. The end-of-generation sweep is a **no-op by construction** | **OPEN** — `clearGraphAuthoritative` re-measured at HEAD: **1 definition, 1 test caller, 0 production callers**, unchanged. `check-suppression-is-reversible` is built (`2db42576`) and runs [1] at declared level 41, so the class is instrumented even though this row is not closed. | **G4, suppressed** | **WIRING** | C72 §4.1 · EV-03 §8.4, R-4 | C72 §4.1, §4.3 | let a wall **`update`** surrender authority in the existing GR2 branch, or clear the set before the lifecycle sweep. One condition, one existing branch | — |
| **PR-06** | **`initPersistence.ts:362` pauses `roomTopologyObserver` + `syncStateEngine` and resumes at `:372` with no `finally`.** A throw inside `ProjectLoader.load` leaves topology observation and sync-state recompute off for the rest of the session, silently, with the project loaded and looking fine | **CLOSED (executed)** — verified at source: `apps/editor/src/engine/initPersistence.ts:374` now carries a `finally`, with the C72 §4.2 rationale in the comment above it. Commit `4dbed18f`. Re-verified at recount (`b61f0ba3`): pause `:362`/`:366`, `finally` `:374`, resume `:376`/`:380`. No gate covers the load-window class — this closure stays in §9.0's weaker source-verified class. | **G4, fragile** | **INVARIANT** | C72 §4.2 · EV-03 R-5 | C72 §4.2 | `try { … } finally { resume() }`, copying `performUndoRedo.ts:357–364` verbatim | — |
| **PR-07** | **`RECONCILABLE_TYPES` names 13 element types, is exported, and has zero consumers anywhere.** The actual level-elevation reconcile is one callback rebuilding **walls and slabs only** — columns, beams, stairs, roofs, furniture, curtain walls and handrails on a re-elevated level are **stranded at the old elevation** | **CLOSED (executed)** — recount, at `b61f0ba3`: `10bdc639` took the row's own permitted fix — **narrowed to the truth AND wired**. `RECONCILABLE_TYPES` is now `['Wall', 'Slab']` (`SpatialAuthority.ts:433`), consumed by **the ONE production consumer** (`:265`, classification at `:204`), and non-members classify **STRANDED** instead of silently vanishing. Proven by execution in this recount: `SpatialAuthority.reconcile.test.ts` → **12/12 green**, negative-controlled (dropping `'Wall'` from the set makes wall ids classify STRANDED and the suite fail). **NOT RE-MEASURED IN THE 2026-08-14 PASS** — carried on the executed 12/12 reading of 2026-08-13. | **G1 (the list) / G4 partial (the reconcile)** | **WIRING** | C72 §5.1 (`SpatialAuthority.ts:243`; `initWallLevelSubscribers.ts:36–53`) | C72 §5.1, §5.2 | either a consumer that handles every type it names, **or narrow the list to the truth** — narrowing a claim to the truth is a fix (§7) | — |
| **PR-08** | **`check-prevstate-contract` and `check-suppression-is-reversible` are specified and NOT BUILT** | **CLOSED** — **both gates executed at HEAD**: `check-prevstate-contract` → [1] declared 17 and `check-suppression-is-reversible` → [1] declared 41 (`564825f8`, `2db42576`, registered `a45327eb`), each pinned at the measured reading. ⚠ The row's "specified and NOT BUILT" is **now factually wrong**. | — | **INVARIANT** | C72 §6.2, §6.3 | C72 §6 | build both; pin baselines **at the measured reading**, never above it | — |
| **PR-09** | **No gate asserts that the bespoke trackers still reach their pairs.** `check-propagation-reaches`'s subject is the four generic events only; the propagation that actually works is **ungated** | **CLOSED** — **executed gate** `check-propagation-trackers-reach` (`bbff7030`) → [1] declared 1, with **8 pairs exercised and 5 of 5 negative controls PROVEN**. It caught a real defect on its first run: `_computeWallSig` omits height and baseline-y, so a real geometry change is invisible to the observer's no-progress gate — reported, not worked around. The bespoke spine is no longer ungated. | **UNPROVEN** | **INVARIANT** | C72 §6.1.2(d) | C72 §6.1.2 | a per-pair seam test in the `WallOpeningEmitSeam.test.ts` shape — driving the real mutation entry point, never a hand-built fixture (§3.4) | — |
| **PR-10** | **Roof → walls-beneath propagation is MEASURED-ABSENT in both directions.** Every roof-store subscriber is rendering, selection, culling or persistence. No wall consumer, no room consumer, no cross-rule. The known flat-roof/slab clash is therefore **structurally unpreventable** — no subscriber exists that could detect it | **CLOSED (executed)** — recount 2026-08-14: both halves landed and both suites were EXECUTED green in this recount. The detector: `packages/geometry-roof/src/pure/roofWallClash.ts` (§GE-06-ROOF-WALL-SLICE — pure, deterministic, classifies each wall clean/PENETRATING/GAP, re-deriving the 2026-06-17 flat-roof defect from geometry) → `roofWallClash.test.ts` **13/13**. The subscriber: `roofWallClashAnnouncer.ts` wired through `initWallLevelSubscribers.ts` → `RoofWallClashAnnounced.test.ts` **8/8**. The row's blocked-by (GE-06) is discharged for THIS pair only — the general clash engine remains GE-06's missing algorithm. | **G0** | **WIRING** + **MISSING ALGORITHM** (the detector) | EV-03 §6 [EXEC greps] | C72 §1.1 | a subscriber first; the clash detector is GE-06 | GE-06 |
| **PR-11** | **The wall→room cascade rule is authored and never registered**, and the handler it would register is itself a no-op. `room.recomputeBoundary` returns empty forward/inverse; `registerCrossHandlers` has zero callers | **OPEN** — re-measured: `registerCrossHandlers` is exported from `plugins/cross/src/handlers/index.ts` and `plugins/cross/src/wall-room.ts` still dispatches `room.recomputeBoundary`. The contract's warning stands — implement the handler first. | **G1** | **WIRING** — *but see the warning* | EV-03 R-8 [EXEC: 2 hits, definition + re-export] | C72 §2.2 | implement the handler **first**. Registering a no-op and claiming coverage is the defect, not the fix | — |
| **PR-12** | **Schedules have no geometry subscription** — open panels show stale areas after a geometry change | **CLOSED (executed)** — recount 2026-08-14: the staleness was first MEASURED, not read (`b10828ff`: an open panel painted 24.00 m² against a 40.00 m² slab), then fixed in `befb4244` — `SchedulePanel` now subscribes to model-geometry events, deliberately NOT narrowed to the fixture's slab event alone. Proven by execution in this recount: `schedulePanelGeometryStaleness.spec.ts` → **6/6 green**. | **G4** | **WIRING** | audit §17.2 §5-matrix · EV-03 §10(2) — **UNPROVEN at HEAD** | C72 §1.1 | a subscription; verify the staleness first | — |
| **PR-13** | **Three stores are duplicated** (Column / Door / Window in both `core-app-model/src/stores` and `geometry-*`). **Any cascade wired to one is blind to the other** | **OPEN for Column only — two thirds CLOSED (executed)**: the **Door and Window forks are DELETED**, "one authority, **proven by the compiler**" (`f718d768`), re-verified at HEAD — `core-app-model/src/stores` holds no DoorStore or WindowStore. **Column remains duplicated** (`core-app-model/stores/ColumnStore.ts` + `geometry-column`). | **G2** | **INVARIANT** (dedup) | EV-03 R-12, §7.1 | C72 §0.3 | resolve to one owner before wiring any propagation through them | — |

---

## §3 — Geometry determinism & tolerance (owner: C73)

| ID | Gap | **Status @ `bbcc11cd`** | Maturity | Impl type | Evidence | Contract | Smallest change | Blocked by |
|---|---|---|---|---|---|---|---|---|
| **GE-01** | **There is no tolerance policy.** **267** tolerance declarations across `packages`/`apps`/`plugins`; **at least eight distinct "a small number means equal" conventions spanning three orders of magnitude** (`1e-6`×49, `0.05`×24, `0.001`×21, `1e-9`×18, …). **`packages/geometry-kernel` exports no epsilon at all** — the layer that owns geometry has no opinion on tolerance, so every consumer forms its own | **OPEN — the module half CLOSED (executed)**: `check-epsilon-policy` runs (`3dba3557`, `07173cdf`) and **E1 reads PRESENT ✓** — `geometry-kernel` now exports all three unit-qualified roles. ⚠ So the row's *"`packages/geometry-kernel` exports no epsilon at all"* is **now factually wrong**. Still OPEN: E2 = 272 declarations outside the module, E5 = 114 unit-unqualified names, E3 consumption = **3 sites**, and `PARALLEL_RAD` at **0 consumers**. *Recount 2026-08-14:* re-executed → [1] at **382/382**: E2 **270** (from 278 sites), E5 **112**, E3 consumption grew 3 → **7 production sites** (5 `EPSILON_ZERO` + 2 `COINCIDENT_M`); `PARALLEL_RAD` still 0 consumers — stated by the gate, not failed. *Restamp 2026-08-14 (lane R2) — **the fence was drained, and it is RED again by the time this lane read it.*** `3a7e2c50` drained the fence and **struck the ledger in the same commit** (E2 270 → 258, E5 112 → 99, ledger 382 → 357), which is the discipline C70 §5.5 asks for. But `check-epsilon-policy` re-executed at `a75e8e1e` reads **[3] RATCHET EXCEEDED — 359 findings against a declared level of 357**, with the **two new declarations named**: `packages/room-topology/src/OpenedRegionDetector.ts::WALL_COVER_TOL_M = 0.15` and `::GAP_STRAIGHTNESS_TOL_M = 0.15`, both minted hours later by `499360c6` (the opened-region detector). Sub-readings: E1 **PRESENT ✓** all three roles · E2 **260** from 268 sites (baseline 258, +2 new, 0 struck) · E3 consumption **13** production import sites, up from 7 — the direction §3.5 wants · E4 **0 widenings** · E5 **99** at baseline 99. **The finding is the mechanism, not the number:** a drained fence is not a policy, and the very next feature declared two raw metre tolerances at a call site. E5 reaching 0 *inside the drained fence only* is exactly the shape that lets this recur. | **G4, non-deterministic across callers** | **INVARIANT** | C73 §0.1 (measured at HEAD, command given) | C73 §2.1 | one declared tolerance module exported from `geometry-kernel`: a numeric-zero epsilon, a model-space coincidence tolerance, a parallelism tolerance — **unit-qualified in the name** (§2.3). Domain bands (`defaultJunctionBandM`, `CENTROID_MATCH_RADIUS`) are **not** epsilons and stay put | — |
| **GE-02** | **Point-in-polygon has ~71 named definitions across 116 files**, **61 distinct ray-cast bodies across 56 files** by structural signature. A prior audit reported 42 — *that number was a floor, not a ceiling*, the **second** hand-counted duplicate tally in this repo to come in low **in the direction of leniency**. **2 rival "shared" implementations with 3 consumers between them**; both were written to end the duplication, neither did | **CLOSED** — **executed gate** `check-predicate-canonical` re-run 2026-08-14 → **[0] CLEAN, hard-0, no baseline**: C1 reads **0 non-canonical point-in-polygon bodies in production** (canonical: `packages/geometry-kernel/src/pure/pointInPolygon.ts`, asserted exactly 1 by C2's integrity arm; 3 exclusions named in-gate with reasons; 14 test-tree bodies printed, deliberately not ratcheted), C2 0, C3 0, oracle fixture in. The ledger went 53 → 50 → **0** this session and the gate deleted its baseline. The C73 §6 exit condition for this family is met; correctness of the surviving body stays out of the gate's scope by design (§5.4a). | **G4, caller-dependent** | **INVARIANT** (canonicalisation) | C73 §0.2 | C73 §3.1 | the proven R3 recipe — canonical file + **structural** counting gate + named exclusions + baseline pinned at the reading. **One family per PR**, shipping consumer identified **first** (§3.5, §3.6) | GE-07 |
| **GE-03** | **One file contains three copies of the even-odd ray cast with three different degenerate-divide guards** — `CesiumViewport.ts:9621` (`|| **CLOSED** — **executed gate** `check-predicate-canonical` re-run 2026-08-14: **C3 reads 0 files holding the same family twice with different degenerate-divide guards** — the three `CesiumViewport.ts` copies were collapsed onto the canonical body in the same series that closed GE-02, and the gate that named them now reports the class empty, hard-0. | 1e-12`), `:10317` (`|| 1e-9`), and `pointInRing` (**no guard at all**). A horizontal edge is "inside" under one, "outside" under another, and a division by zero under the third — one file, one polygon, one session | **G4, self-contradictory** | **INVARIANT** | C73 §0.2 | C73 §2.4 | the guard comes from the declared numeric-zero epsilon or the predicate refuses. Where copies disagree, the collapse **states which behaviour is canonical and why** (§3.7) | GE-01 |
| **GE-04** | **`WallIntersectionResolver` ×3** (`ai-host`, `core-app-model/ai`, `room-topology` — two byte-identical) · **`FloorPlanDiagnostics` ×2** · **`RoomStore.ts` ×2** (`room-topology`, `stores`) | **CLOSED** — the last family collapsed. *Restamp 2026-08-14 (lane R2), all four families re-measured at `a75e8e1e`:* `WallIntersectionResolver` **0 class definitions** · `FloorPlanDiagnostics` **0** · `StairValidationAuthority` **1** (`packages/geometry-stair/src/StairValidationAuthority.ts:54`; **executed** `check-constraint-honesty` fires no H5) · **`RoomStore` — 1 class definition** (`packages/room-topology/src/RoomStore.ts:155`). The `RoomStore` triple was collapsed **in the order the row's own fix column demands — shipped copy identified by name FIRST**: `a61f9eb6` stamped room-topology as canonical *with the shipping paths enumerated in its header* (command surface `ctx.stores.roomStore`, `window.roomStore` renderers, persistence); `dbc37560` renamed the plugins/rooms rival `RoomsPluginStore` (readers: its own `index.ts`, nothing else); `b7968736` renamed the packages/stores rival `AggregateRoomStore` (readers: **none outside `packages/stores` itself** — its `runtime.stores.roomStore` slot has zero). One owner per family, which is what C73 §3 asks. **Residual, named rather than buried:** `packages/stores/src/RoomStore.ts:184` keeps an `@deprecated` `export { AggregateRoomStore as RoomStore }` alias so `composeRuntime` and runtime-composer types compile, and whether the C20 aggregate `Room` (it carries `apartmentId`; `RoomData` does not) should collapse onto the winner is a CONTRACT call that needs an ADR (C73 §3.7) — deliberately **not** decided by a silent pick. | **G4** | **INVARIANT** (dedup) | C73 §0.2 (measured same day) | C73 §3 | one owner per family; the shipped copy identified by name before collapsing — *fixing the copy the shipping path cannot reach reads as done and is not* (§7.h) | — |
| **GE-05** | **No general 2-D boolean.** ≥5 independent Sutherland–Hodgman half-plane clippers, **no general clipper, no union primitive**; three files independently defer "a future general clipper". *"Merge two footprints" is not expressible* | **CLOSED (executed)** — recount 2026-08-14: the construction LANDED. `packages/geometry-kernel/src/pure/polygonBoolean.ts` (`3f004a58` — intersection + union, oracle-pinned per C73 §3/§6), differential arm + barrel export with the resolution limit as a MEASURED bound (`64fa840e`), and the three deferring sites wired — `'non-convex-both'` is RETIRED, not renamed (`0b69836d`). Proven by execution in this recount: `polygonBoolean.oracle.test.ts` + `polygonBoolean.differential.test.ts` → **47/47 green**. | **G0** | **MISSING ALGORITHM** (construction) | audit §17.6 table | C73 §3 | construction, labelled — not wiring | — |
| **GE-06** | **Clash detection is a stub wearing a capability's clothes — and the two id sets do not even match.** `packages/command-bus/src/commands.ts:395–408` declares `ClashDetectionToolbarCommands` with **12** entries; `ClashDetectionToolbar.ts` emits **12** ids; **only 3 are common to both** (`clash-run`, `clash-filter-new`, `clash-report-export`). Measured at HEAD: **no handler in any plugin, and no detector anywhere in the tree** | **OPEN, narrowed** — re-measured 2026-08-14: `clash-run` still appears only in `command-bus/src/commands.ts` and `ClashDetectionToolbar.ts` (plus its spec); **no general engine, no plugin handler; the ten unhandled ids still do not refuse.** One REAL slice now exists and is not this row's subject: `roofWallClash.ts` (§GE-06-ROOF-WALL-SLICE, the roof→walls-beneath pair only, executed green — see PR-10). The general element-pair engine remains construction. | **G0** | **MISSING ALGORITHM** + **MISSING VERB** | audit §17.6 ("12 command ids, no engine") · verified at HEAD 2026-08-12 | C70 L-INV-1 | *first*, make the ten unhandled ids **refuse** rather than silently no-op; the engine is construction | — |
| **GE-07** | **Three C73 gates are specified and NOT BUILT** — `check-epsilon-policy`, `check-predicate-canonical`, `check-deterministic-regeneration` | **CLOSED** — **all three executed at HEAD**: `check-epsilon-policy` → [1] declared 386; `check-predicate-canonical` → [1] declared 53 (`07173cdf`); `check-deterministic-regeneration` → [1] declared 134 (`89623324` — "134 is its FIRST HONEST READING, not a regression"). ⚠ The row's "specified and NOT BUILT" is **now factually wrong**. *Recount 2026-08-14:* epsilon [1] 382/382 · predicate-canonical **[0] CLEAN hard-0** · deterministic-regeneration **[3] STALE LEDGER** — 2 paid entries awaiting their strike (`room.ts:153`, `WallOccupancyStore.ts:581`, both `unstable-sort`); the gate exists and runs, the stale ledger is the paying lane's hygiene debt to strike, named here so it is not lost. *Restamp 2026-08-14 (lane R2) — the second half of this row, "**a gate registered nowhere**", is now measured at **zero**.* Four gates existed and ran in **no runner at all**; all four are now in `tools/ga-gate/run-all.ts`: `derived-not-authored` + `predicate-canonical` (`e737b877`), `deterministic-regeneration` (`1892eccb` — registered only *after* its post-baseline site was fixed at `13d48db7`, a kill the ratchet captured and nobody had ledgered), `provenance-coverage` (`28c6b05c`, landing hard-0 on neither ledger), and `triangulation-canonical` (`5fe84c9f`). **Measured by this lane at `a75e8e1e`: of the 58 `tools/ga-gate/check-*.ts` scripts, 0 are absent from `run-all.ts`.** A gate outside the runner is a gate whose red nobody sees, which is the same defect class as a green-and-blind gate. Re-executed in the same pass: `deterministic-regeneration` → **[3] STALE LEDGER, 16 unstruck entries** (paid, never de-listed — `geometry-door/DoorSystemTypeStore.ts` ×4 among them), and it prints its own UNPROVEN: **0 of its classified field paths name GEOMETRY**, so the geometry half of C73 §1.3 has no classification input in this repo. | — | **INVARIANT** | C73 §5 | C73 §5 | build them; **counting** gates, matched on structure, not names (§3.2) | — |
| **GE-08** | **Cross-machine determinism and GPU-side geometry are UNPROVEN** — D1/D2 run in one process on one architecture; anything computed in a shader is outside every gate | **UNPROVEN** — `check-deterministic-regeneration` **executed** states it itself: cross-machine floating-point determinism and GPU-side geometry are NOT PROVEN by this gate (C73 §5.4b/c). It goes further — **0 of the classified field paths name GEOMETRY**, so the geometry half has no classification input at all in this repo today. *Recount 2026-08-14:* an instrument of the class now exists and was EXECUTED — `check-cross-process-determinism` (`tools/ga-gate/`) → exit 0, **4 fixtures byte-identical across 3 processes** — but its own scope block says it proves SAME-MACHINE cross-process only and *"GE-08 stays UNPROVEN"* for cross-MACHINE (§5.4b) and GPU (§5.4c). The row's status is the gate's own words. | **UNPROVEN** | **INVARIANT** | C73 §5.4(b)(c) | C73 §5.4 | say UNPROVEN; do not infer it from single-process green | GE-07 |
| **GE-09** | **No gate asserts that a refusal is surfaced to the user** rather than caught and logged | **OPEN — instrumented** — recount 2026-08-14: `check-refusal-identity` (§REFUSAL-IDENTITY, C58 §1.13.8 *"the resolver's distinction MUST reach the card"*) EXECUTED → exit 0 **at a NAMED baseline of 87 offenders** (arm A 54, arm B 33) — measured and still failing at a shrink-only ledger, which is OPEN, not UNPROVEN. Residual honestly stated: the gate checks that a *rendered* refusal carries its identity; that every refusal *reaches* a surface at all is the CE-05 reachability class. *Restamp 2026-08-14 (lane R2) — **narrowed, NOT closed**, and the gate is what refuses to close it.* `check-refusal-identity` re-executed at `a75e8e1e` → **85 findings (arm A 52, arm B 33) against a baseline of 86** — the ledger moved 87 → 85, shrink-only, so this row is *measured and still failing*, which is OPEN by this register's own definition. What DID land is one family, paid and **proven by execution**: `d18bc7a5` threaded the six-member `CanPlaceRefusalCode` through **all ten** `canPlace()` consumers (six command-registry commands + `DoorTool` ×2 + `WindowTool` ×2), each rendering via the shared `canPlaceRefusalText()` so the `[OCC_*]` identity survives string-only sinks; and `6688d82c` pins it at the **command seam** — `packages/command-registry/__tests__/canPlaceRefusalIdentity.test.ts`, **re-run by this lane → 9/9 green**, driving a real refusal through each consumer's `canExecute()` and asserting the `[OCC_*]` token reaches `CommandValidationResult.reason`, with a positive control (legal placement ⇒ `ok:true`, no reason). Six distinct verdicts that used to arrive as one manufactured sentence now arrive distinguishable. ⚠ The gate's exit is **3 — one baseline entry no longer matches** (`plugins/stair/src/handlers/CreateStair.ts`, a file another lane has staged for deletion in this shared tree); that is a mid-flight ledger-hygiene state, recorded rather than stamped around. | **G4, ledgered** | **INVARIANT** | C73 §5.4(d) | C73 §4.4 | a reachability arm; "Invalid geometry" is a shrug, not a refusal | — |
| **GE-10** | **No `wall.split` verb id.** ⚠ **Corrected here against the corpus**: `wall.cut` **exists and is opening-aware** — `plugins/wall/src/handlers/CutWall.ts` **rejects a cut whose openings straddle the cut point, naming the offending opening ids**, re-checks race-defensively, and partitions the surviving openings to left/right. **The opening-blind path is the legacy `CutWallCommand`**, which discards the far half including its openings. The gap is a `split` id, not a missing capability | **CLOSED (executed)** — recount 2026-08-14: `plugins/wall/src/handlers/SplitWall.ts` declares the `wall.split` id **over the SAME opening-aware CutWall handler instance** — exactly the row's prescribed fix, the cut path untouched; refusals and spans name `wall.split`. **Executed** `check-verb-register` first flagged it V4 UNKNOWN-liveness at `87832530`, then re-run at `0c098d81` reads **exit 0** — the register regenerated with the verb's disposition resolved. | **G4 (cut) / G0 (split)** | **MISSING VERB** | verified at HEAD 2026-08-12 (`CutWall.ts:17–20, 116–126, 152–159, 184–224`) vs CD §C, audit §5/§17 | C70 §3 | a `wall.split` id over the existing opening-aware handler; **do not rebuild the cut path** | — |
| **GE-11** | **The room spatial index is fed two incompatible AABB definitions** (true bbox vs circle approximation) — **a concave room can be missed**. A live correctness bug, not duplication | **CLOSED** — **executed gate** `check-room-aabb-canonical` (certification suite) run 2026-08-14 → **[0] CLEAN, hard-0**: ARM A reads **0 non-canonical insert sites** feeding the room spatial index, and ARM B REPRODUCES the concave-room miss on a planted CIRCLE-APPROX feed (probe interior by even-odd, query returns empty under circle-approx, returns the room under true-bbox) — so the comparator has teeth and the premise is proven, not assumed. Residuals printed by the gate: user-observability of a miss is GE-09's row; the arm is name-shaped. | **G4, incorrect** | **INVARIANT** | audit §17.6 | C73 §1 | one AABB definition; pin with a concave-room regression test | — |
| **GE-12** | **Triangulation ×5, segment intersection ×7+, ~30 `signedArea` + 7 `ensureCCW`, planar face tracing ×5** (incl. `PlanarTopologyEngine` ×3, two byte-identical 477-line clones beside the 220-line **shipped** one). A centroid-fan triangulation is **silently wrong on concave rooms**; simplicity is asserted at exactly ONE call path | **OPEN, substantially narrowed** — recount 2026-08-14, from the re-executed `check-predicate-canonical` [0]: **segment intersection — 9 production rivals collapsed** onto `pure/segmentIntersection.ts` (oracle-pinned, the one-family §3.6/§3.7 proof on the record); **signedArea/winding — canonicalised** as `polygonSignedArea2D`/`polygonSignedAreaOrdinates` with the kernel's 5 producer clones collapsed (oracle-pinned), ~80 further production accumulations census-measured but deliberately NOT ratcheted until negative controls exist; **triangulation ×5 and planar face tracing — no collapse landed**. *Restamp 2026-08-14 (lane R2) — the row now has its OWN counting gate, and it says this row does **not** close.* `check-triangulation-canonical` (`595b87c5`, registered in `run-all` at `5fe84c9f`) **executed at `a75e8e1e` → [1] DECLARED-LEVEL, 13 findings at a declared level of 13**, 6605 files scanned, controls proven (one plant per signature → 5 bodies detected; the five reject-shapes and the clean tree both → 0). **C1 = 7 triangulation bodies across 6 files + 4 `PlanarTopologyEngine` copies**, and **C2 = 2 counted families with NO named canonical file**. ⚠ **The gate corrects THIS ROW's arithmetic, upward:** the headline's "×5" and "×3" were name/file censuses; the structural count is **7 bodies / 4 engine copies** (`auto-dimension/src/perimeter.ts` is a fourth engine the class name never matched, and `producers/ceiling.ts` holds two bodies). The register keeps the ordering; the gate keeps the number. **This lane was briefed that GE-12 was closable. It is not** — an instrument that counts a duplication is not a collapse of it, and the gate's own exit condition (C1 → 1 per family, C2 naming the canonical file, an oracle fixture at a known answer) is unmet on both families. | **G4** | **INVARIANT** (canonicalisation) | audit §17.6 table | C73 §3.1 | R3 recipe, one family per PR, in the order the measured duplication argues for | GE-07 |

---

## §4 — Constraint honesty (owner: C74)

| ID | Gap | **Status @ `bbcc11cd`** | Maturity | Impl type | Evidence | Contract | Smallest change | Blocked by |
|---|---|---|---|---|---|---|---|---|
| **CO-01** | **The named mock.** `PlanegcsAdapter.ts:95` — `this.underlying = opts.underlying ?? new MockSolver()`; every `solve()`/`diagnose()` is a one-line delegation. And `:85` declares **`readonly kind = 'planegcs'`**. `MockSolver` is honest (`kind='mock'`); **the adapter takes the mock's behaviour and puts the real solver's name on it.** A caller inspecting the only externally visible identity the `SolverPorter` shape offers is told `planegcs` and gets projection arithmetic | **CLOSED (executed)** — `c084f5ba` ("the adapter stops naming an engine it does not run"), re-verified at source: `PlanegcsAdapter.ts:101` now declares `readonly kind: string` — resolved, not the hard-coded `'planegcs'` — and the old literal survives only in the header comment describing the defect. Confirmed by **executed** `check-solver-is-real` → [1] declared 1, whose R1 arm is **negative-tested against a planted tree** and fired. | **G1** | **INVARIANT** | C74 §0 | C74 §3.1, §3.2 | report the `kind` it performs, and emit a **non-suppressible first-call signal in a production build**. Satisfiable today, with no new dependency (§4.4) — **and it must land in its OWN commit, before any binding work** (§4.3), or the organisation learns nothing |  — |
| **CO-02** | **The silent fallback.** `loadSolver()` returns `MockSolver` on **both** paths — the no-URL miss and the fall-through *after* the URL is supplied. Supplying `PLANEGCS_WASM_URL` — the one action a caller could take to ask for the real thing — **changes nothing and reports nothing** | **CLOSED** — **executed gate** `check-solver-is-real` re-run 2026-08-14 → **[0] CLEAN, hard-0**: the R2 finding (which had generalised to `AnthropicRelay.ts`) is GONE — 171 manifests + 4538 sources scanned, both stand-ins now honest (`kind='mock'`), and the verdict is trusted because the R1/R2/R3 negative controls were watched firing on a planted dishonest adapter in the SAME run, clean-tree positive 0. The gate left its baseline. | **G1** | **INVARIANT** | C74 §0 (`engine.ts:468`, `:483`) | C74 §3.3 | "not configured" and "configured but could not load" become different observable outcomes, and the second is a **failure**, not a default | — |
| **CO-03** | **The 31 of 33 passing tests test the mock.** `PlanegcsAdapter.test.ts` verifies delegation by *injecting* `opts.underlying` — the field whose own docstring says *"Production callers MUST NOT pass this"*. **The configuration production actually uses (the `??` fallback) is the untested one** | **CLOSED (executed)** — *Restamp 2026-08-14 (lane R2), gate re-run at `a75e8e1e`:* `check-no-hidden-mock` now reports **0 FINDING M-C**, and the string `Planegcs` does not appear **anywhere** in its output — the two entries this row was ledgered on (`PlanegcsAdapter.test.ts:121` / `:150`) are gone. The fix was **deletion, not disclosure** (`8a595676`; restored after a shared-tree race by `7cd1db5e`): the `underlying?:` option is removed from `PlanegcsAdapterOptions`, the constructor reads `this.underlying = new MockSolver()` **unconditionally** (`PlanegcsAdapter.ts:130–133`, with a comment naming the deleted seam at `:64`), and a repo-wide grep finds **zero `underlying:` injection sites** — so the configuration production uses is now the only one the suite *can* exercise. The gate's own controls fired this run (planted tree: 4 findings, arms `[M-A, M-B, M-C]`; clean tree: 0), so the M-C silence is a measurement, not a blind comparator. ⚠ **Stated, not laundered:** the gate's overall exit is **[3] STALE LEDGER** — 14 *M-B* entries paid without being struck. Those are CO-06's hygiene debt (a repair lane owns it); the CO-03 arm is clean inside that same run. | **G1** | **INVARIANT** | C74 §0 | C74 §3.5, §3.6 | the test file states which production configuration it does **not** cover; no claim of the form "the solver works, N tests pass" without naming the bound subject | — |
| **CO-04** | **`planegcs` is not a dependency of any workspace** — `grep '"planegcs"' --include=package.json` → **0 hits** repo-wide. There is no WASM module to bind, at any version | **OPEN, and correctly unauthorised** — re-measured at HEAD: `grep '"planegcs"' --include=package.json` → **0 hits**. C74 §4.5 stands; no solver work is authorised. | **G0** | **MISSING SOLVER** (construction — *and unauthorised*) | C74 §0, §3.7 | C74 §4.1, §4.5 | **none yet.** §4.5 stands: **UNPROVEN — no constraint family in this repository has been shown to require SOLVING.** *Do not build a solver because "BIM 3.0 sounds like it needs one"* | CO-08 (the per-family classification) |
| **CO-05** | **`createWorkerHandler` has zero production callers** — 10 hits: the definition, a barrel re-export, three doc comments, five test lines | **CLOSED (executed re-measure)** — recount, at `b61f0ba3`: the **DELETE terminal state was reached on 2026-08-12** and this row's OPEN was stale at its own stamp. `./worker.js` (`createWorkerHandler` + its message types) is **deleted**, the barrel says so in place (`packages/constraint-solver/src/index.ts:36`), and the test file records the deletion (`engine.test.ts:16`). Zero code hits remain outside those two declarations. One of exactly two permitted terminal states, declared where the row demanded it. **NOT RE-MEASURED IN THE 2026-08-14 PASS.** | **G1** | **WIRING** *or* **DELETE** | C74 §0, §3.8 | C74 §3.8 | wire it, or say in the barrel that it is unwired | — |
| **CO-06** | **The scaffold is undated and its two halves disagree.** The adapter header says *"S52 D1 SCAFFOLD … binding lands at S52 D2"*; `engine.ts` says **S53 D1** in four places. *A scaffold whose retirement date is untracked is permanent architecture that nobody chose* | **OPEN** — `check-no-hidden-mock` **executed**: FINDING **M-B** fires 19 times for scaffolds carrying no date, no owner and no retiring assertion, two of them past the 90-day grace. The gate now measures the class this row describes. *Recount 2026-08-14:* re-executed → [1] at **58/58** (≈56 M-B — subject discovery widened, several past the grace — + 2 M-C at `PlanegcsAdapter.test.ts`, which are CO-03's). *Restamp 2026-08-14 (lane R2) — **narrowed hard, and the gate's own exit is a red this row must carry.*** Re-executed at `a75e8e1e` → **15 M-B findings measured** (down from ≈56), 4576 files scanned, 20 scaffold headers discovered, controls fired. Two payments and one **loophole closure**, in that order of importance: `45ffc533` shut the M-B grace clause that **sold compliance for a date alone** — negative-tested twice, so a scaffold can no longer buy its way past the gate by being dated without an owner or a retiring assertion; then `eb966187` (11 rows, two treatments, *"none a re-stamp"*) and `fb7d56ca` (16 rows) paid 27 headers. ⚠ **The gate exits [3] STALE LEDGER: 14 declared entries are no longer measured** — paid without being struck in the commit that paid them, which is the precise failure the ledger discipline exists to stop ("the next regression hides inside it"). So the honest statement of this row is **two numbers, not one: 15 measured, ~29 still declared.** A repair lane owns `tools/ga-gate/` and that strike; this lane did not touch the gate or its ledger, and records the exit-3 state rather than stamping around it. Remaining M-B subjects are concentrated in `apps/editor` UI panels and `apps/component-editor/src/sketch`, two of them (`familyCreatorPlaceholder.ts` ×2, `CurtainWallBuilder.ts`) past the 90-day grace. | **G1** | **INVARIANT** | C74 §0 | C74 §3.4 | an owner, a milestone, and an **assertion that fails when the milestone passes without the retirement** | — |
| **CO-07** | **`StairValidationAuthority` exists twice** — `geometry-stair` (production imports this one) and `constraint-solver` (**zero production importers**). Whichever copy the tests bind to, one of them is a rule set that can drift from shipped behaviour with a green suite | **CLOSED (executed)** — recount: `095cfa10` ("ONE `StairValidationAuthority` owner; the dead copy is deleted, not extended") deleted `packages/constraint-solver/src/StairValidationAuthority.ts`, and **executed** `check-constraint-honesty` re-run AFTER that commit landed (`b61f0ba3`) fires **no H5** — its own text now reads *"single owner since 2026-08-13 … production imports this geometry-stair copy"*. The surviving copy still lacks an executable witness — that is finding H3 on CO-08's ledger, not this row's subject. | **G4** | **INVARIANT** (dedup) | C74 §2.2 · audit §17.1.3 | C74 §2.2 | one owner **before either copy is extended**; until then neither may be cited as "the stair rules" without naming which | — |
| **CO-08** | **No constraint family carries a §1.1 classification.** VALIDATION / ENFORCEMENT / ADVISORY / SOLVING is written down per constraint, with its evidence, **before any solver work is authorised** — and it is not written down anywhere today | **CLOSED (executed)** — recount, at `b61f0ba3`: the classification exists (`7712d454`, 17 rules declaring a strength) **and the evidence caught up to it mid-session**: `4103a99f` landed executable evidence for the last 11 registered rule families, and re-run `check-constraint-honesty` reads **[1] at 2/2** — G-INV-2 enumerates **20 families, 18 REAL by executed witness** (down from 1 REAL / 14 unwitnessed at the last stamp). The residual is the gate's own shrink-only ledger: **`StairValidationAuthority` and `annotationConstraints` still declare VALIDATION with no executable witness** — named, ledgered, and the next things this ratchet forces. | **G0** | **INVARIANT** | C74 §1.3, §7.7 | C74 §1.3, §4.2 | the written classification. A family with none is treated as VALIDATION — the cheapest kind, not the most expensive | — |
| **CO-09** | **No model-space constraint store.** `annotationConstraints` is the **only** persisted constraint family in the system — written to the snapshot, read back, checked, never solved | **OPEN, correctly deferred** — blocked on CO-08 reaching §4.2(c) with evidence. One family now has evidence; the rest do not, so no store is authorised. **NOT RE-MEASURED IN THE 2026-08-14 PASS** — though its blocker CO-08 was: `check-constraint-honesty` re-executed at 2/2, 18 of 20 families REAL. | **G2 for one family, G0 otherwise** | **MISSING PERSISTENCE** (construction, following an existing store pattern) | C74 §2 · audit §17.1.6 | C74 §4.2 | none until a family reaches §4.2(c) with evidence | CO-08 |
| **CO-10** | **Violations are log lines, not queryable model state.** The `violates` edge + `constraintAdapter` are **dev-only reachable** — `provideLiveGraphSources`'s only caller is a dev-tools file | **CLOSED (executed)** — recount 2026-08-14: `provideLiveGraphSources` now has a PRODUCTION caller — `installLiveGraphWiring()` at `AIAreaLayout.ts:317`, called unconditionally OUTSIDE the DEV guard (`installLiveGraphWiring.ts:44` attaches the live `semanticGraphManager` + constraint source). Proven by execution in this recount: `devToolsInstallGuard.test.ts` → **7/7 green**, including B3 (*"the PRODUCTION graph wiring is called OUTSIDE the guard"*) and B4 (the dev module carries no production wiring). The `violates`/`constraintAdapter` path is no longer dev-only reachable. | **G2-dev-only** | **WIRING** | audit §17.1.4 | C70 G-INV-3 | a production caller for `provideLiveGraphSources`; validators emit `violates` edges | — |
| **CO-11** | **All three C74 gates are UNBUILT** — `check-constraint-honesty`, `check-solver-is-real`, `check-no-hidden-mock` | **CLOSED** — **all three executed at HEAD**: `check-constraint-honesty` → [1] declared 15; `check-solver-is-real` → [1] declared 1 **with both executed controls passing** (planted-tree negative fired R1/R2/R3, clean-tree positive = 0); `check-no-hidden-mock` → [1] declared 59 (`564825f8`). Every arm negative-tested before it was trusted, per §6.2. ⚠ The row's "all three UNBUILT" is **now factually wrong**. | — | **INVARIANT** | C74 §6 ("all three UNBUILT at stamp time") | C74 §6 | build them; each carries an **exit-2 floor** on its own subject discovery and each arm is **negative-tested against a planted violation before it is trusted** (§6.2) | — |
| **CO-12** | **No gate enumerates the `./compliance` registry's rules** — only its wiring is checked | **CLOSED** — **executed gate** `check-constraint-honesty` re-run 2026-08-14: **G-INV-2 enumerates the registry's rules** — `RULE_REGISTRIES` names `ConstraintEngine.ts` (which IS the `@pryzm/constraint-solver/compliance` subpath per C74 §2's own table), parses all 17 `register({id, tier, severity})` families plus the 3 named non-registry families, and grades each for executable evidence at its declared strength: **18 of 20 REAL**, the 2 without evidence (`StairValidationAuthority`, `annotationConstraints`) ledgered as H3 findings → [1] at 2/2. The enumeration arm this row asked for exists and runs. | **G4** | **INVARIANT** | C74 §6.3(d) | C74 §6.3 | an enumeration arm | CO-11 |

---

## §5 — Provenance (owner: C75)

| ID | Gap | **Status @ `bbcc11cd`** | Maturity | Impl type | Evidence | Contract | Smallest change | Blocked by |
|---|---|---|---|---|---|---|---|---|
| **PV-01** | **Provenance is invented at deserialisation.** `roomSnapshotUtils.ts:156` — `detectionMethod: (raw['detectionMethod'] as any) \|\| **CLOSED (executed)** — re-verified at source: `roomSnapshotUtils.ts` no longer defaults to `'auto-topology'`. It writes **`detectionMethod: 'origin-unknown'`** with a **typed reason** — `predates-provenance` for an absent field (`:194`), `conflicting-records` for an unrecognised one (`:205`) — and the old `|| 'auto-topology'` line survives only as a comment at `:148` documenting the defect. Exactly the UNKNOWN-with-reason the row asked for (`57f2b539`). Independently confirmed by **executed** `check-provenance-not-invented` → [1] declared 2, whose two remaining V1 findings are elsewhere (`from-pipeline.ts:268`, `ZoningRulesEngine.ts:163`). Recount (`9fc6804d`): re-run → [1] at **2/2**, same two findings, both still elsewhere — and this closure is no longer regression-blind: `check-derived-not-authored`'s executed negative control plants exactly the old defect (a silent `detectionMethod` upgrade to `'auto-topology'`) and arm **A2 FIRED** on it. | 'auto-topology'`. A snapshot missing the field is loaded **as if topology had detected the boundary**; `'auto-topology'` is not a neutral placeholder, it is the **most authoritative member of the union**. The `as any` defeats the union at exactly the point it would have caught this, and the serialised shape declares the field as bare `string` | **G2, false** | **INVARIANT** | C75 §0 Finding 1 | C75 §2.1, §7.1 | record **UNKNOWN-with-reason**; type the serialised field `RoomDetectionMethod` | — |
| **PV-02** | **Element schemas carry no provenance whatsoever.** Across `packages/schemas`: `originDetail` **0 hits**, `derivationStatus` **0 hits**, `detectionMethod` **0 hits**; every `origin:` in `elements/*` is a geometric `Vec3` — a *point*, not a provenance. The room/floor/ceiling family is the only element-level provenance in the system **and it does not live in the schema package at all** | **CLOSED (executed)** — recount (`9fc6804d`): **executed** `check-provenance-coverage` → [1] at **1/1**, ledger **28 → 1**. All **27 discovered element kinds** now carry `provenance: RetrofittedProvenanceSchema` in their own L0 schema (C75 §2.4, optional with an UNKNOWN-with-reason default per §2.5), landed inside `221d08a6` — a commit whose message describes ONE of its 32 files; `81fb66c0` records the true contents — with a 274-test backward-compatibility suite (`elementProvenance.test.ts`). The vocabulary half was already closed (`ValueOrigin.ts`, `57f2b539`). The ONE remaining ledger row is a different defect shape: **C2 — `FloorDetectionMethod` names a family with NO `defineElement('floor')` in L0 at all**; it closes only by the kind gaining a schema or being argued out of scope in writing. ⚠ Numbering caution: the closing commits say "PV-04" — that is C75 exit-condition numbering, not this register's row ids; do not double-credit. | **G0 for every kind but one** | **MISSING PERSISTENCE** | C75 §0 Finding 2, §3.3 | C75 §2.4 | a five-value provenance type **in `packages/schemas`**, built from the idioms that already exist (Finding 3), optional with an `UNKNOWN` default so existing snapshots parse unchanged (§2.5) | MT-01 — *provenance fields are worthless while a verb can stamp them on a detached DTO store* (audit §17.3) |
| **PV-03** | **The repair path stamps invented geometry as detected.** `RoomDetectionEngine.ts:454` runs `repairToSimplePolygon()` on a self-intersecting boundary — replacing it with *the largest simple ring*, a polygon the topology never traced — and then writes `detectionMethod: 'auto-topology'` at `:475`, identical to a genuinely flood-filled room. **The repair is logged to the console and not to the model** | **CLOSED (executed)** — recount: `7b015805` (subject says "PV-02" — C75-numbering; its body is this row's defect, C75 §0 Finding 4) extracted the decision whole into `detectedRoomBoundary.ts`, pure and directly executable, with exactly the permitted outcomes: already-simple ring → `'auto-topology'`, no detail · substituted ring → **`'repaired-ring'` + a `detectionDetail` naming the discarded vertices, in the MODEL** · irreparable ring → a **REFUSAL** in the `SlabFragmentBuilder.ts:706` form. Verified at source AND by execution in this recount: `repairedRingIsDistinguishable.test.ts` → **6/6 green** (negative-controlled — re-planting the old stamp fails 3 of 6, and the distinction survives the persisted Zod round-trip), with `check-provenance-not-invented` arm **V3** (repair-path stamp recorded only to console) EVALUATED and clean, and `check-derived-not-authored` **[0] CLEAN hard-0** with all five arms proven against a planted tree. | **G2, false** | **INVARIANT** | C75 §0 Finding 4 | C75 §2.3 | write **INFERRED** plus the reason — *the fix IS writing the field* — or refuse in the `SlabFragmentBuilder.ts:706` form, which is twenty files away | PV-02 |
| **PV-04** | **No export mapping exists.** How AUTHORED/OBSERVED/COMPUTED/INFERRED/REGENERATED land in IFC or DXF is undefined. **The contract names this its largest open risk**: provenance that stops at the export boundary protects nothing downstream — it is how a generated guess ends up in an IFC export as a surveyed fact | **CLOSED (executed, 2026-08-14 — the cell below records the earlier UNPROVEN readings this closure supersedes)** — the export mapping was unbuilt and unmeasured; **executed** `check-provenance-not-invented` named **the EXPORT boundary (§5)** in its own STILL UNPROVEN block. A `ProvenanceExport.ts` now exists under `packages/schemas/src/provenance/`, which is *not* evidence the mapping is defined — flagged here, deliberately not credited. Recount (`9fc6804d`): unchanged — the re-run gate still names **the EXPORT boundary (§5)** in its own STILL-UNPROVEN block, and the mid-session commits labelled "PV-04" (`81fb66c0`/`221d08a6`) are this register's **PV-02** per-kind retrofit under C75 exit-condition numbering, not this mapping. Still: nobody measured it. *Recount 2026-08-14:* somebody measured it — **executed gate** `check-provenance-export-boundary` (certification suite) → **[0] CLEAN, hard-0**: E1 finds the C75 vocabulary crossing the export boundary on **4 surfaces** (ifc-export 16 refs, dxf, export-pdf, file-format/export — the dxf/pdf surfaces via explicit `provenance-absence` modules, i.e. the §7.7 "absence recorded by name" branch where mapping is not yet real), E3 reads §7.7 **satisfied**, self-test PASSED on a planted tree. Its own NOT-MEASURED block is this row's residual, not erased: whether a mapping is CORRECT for the downstream reader, and runtime export execution (CE-05's class). Status: **CLOSED** at the gate's subject — the mapping-or-named-absence now exists and is gate-pinned. | **G2, gated** | **MISSING PERSISTENCE** (export) | C75 §5, §6.3(c) | C75 §7.7 | the mapping, or its absence recorded **by name** as an accepted limitation — never left blank | PV-02 |
| **PV-05** | **`ProvenanceStore` (C23 AI lineage) is not persisted at all** — destroyed on every reload | **CLOSED (executed)** — recount 2026-08-14 (`a5211b46`+`8cab70c1`): the serializer writes an optional, ADDITIVE `provenance` slice (`ProjectSerializer.ts` PV-05 block — written only when a store is wired, so an unwired session can never overwrite a real lineage with a misleading empty one) and the loader hydrates it with **named absence**: `hydrate(undefined)` → `absent: 'predates-provenance-persistence'`, and a slice arriving with NO wired store logs that the lineage is NOT loaded and must not be re-saved. Proven by execution in this recount: `provenanceSlicePersistence.test.ts` → **11/11 green**. | **G1** | **MISSING PERSISTENCE** | audit §17.3 | C70 I-INV-2 | persist it, or place it on the named persist-or-lose ledger | GR-07 |
| **PV-06** | **`confidence` has zero hits repo-wide** on the element side, while the site/context/climate/zoning domains carry rich, mandatory confidence | **CLOSED (executed)** — recount 2026-08-14 (`d3c69ee7`): element kinds now carry `confidence: RetrofittedConfidenceSchema` in their L0 schemas (**22 of 29 element-schema files grep-confirmed**, e.g. `Wall.ts:89`), with the vocabulary **REUSED from C62** (`site/metadata/DataConfidence.ts`, ADR-0280) exactly as §4.h demands — `ElementConfidence.ts`'s own header records the before-count (elements 0 / site 48). Proven by execution in this recount: `elementConfidence.test.ts` → **149/149 green**. ⚠ The row's headline is now factually wrong in the healthy direction. | **G2** | **MISSING PERSISTENCE** | audit §17.3 | C75 §1.3 (C62 owns confidence) | reuse C62's model — do **not** reinvent it (§4.h) | PV-02 |
| **PV-07** | **All three C75 gates are UNBUILT** — `check-provenance-not-invented`, `check-provenance-coverage`, `check-derived-not-authored`. Per-kind coverage is therefore **UNPROVEN**, not zero | **CLOSED** — **all three executed at HEAD**: `check-provenance-not-invented` → [1] declared 2 (arms V1–V5 all EVALUATED, with a real-tree positive control that came back clean); `check-provenance-coverage` → [1] declared 28; `check-derived-not-authored` → **[0] CLEAN, hard-0** with arms A1–A5 all EVALUATED at hard-0. ⚠ The row's "all three UNBUILT" is **now factually wrong**, and per-kind coverage is no longer UNPROVEN — it is **measured at 28 findings**. Recount (`9fc6804d`): all three re-executed — not-invented [1] **2/2** · coverage [1] **1/1** (ledger 28 → 1, struck in the same commit that paid it; see PV-02) · derived-not-authored **[0] CLEAN hard-0**, five arms proven against a planted tree. | — | **INVARIANT** | C75 §6, §3.3 | C75 §6 | build them with exit-2 floors, each arm negative-tested before it is trusted | — |
| **PV-08** | **`detectionMethod` is three non-unified enums** across the one family that has provenance | **CLOSED (executed)** — the row got an arm of its own, and it lands hard-0. *Restamp 2026-08-14 (lane R2), gate re-run at `a75e8e1e`:* `check-provenance-coverage` **arm C4 IS this row** — *"every declared vocabulary member is NAMED in the L0 translation map"* — and the gate reads **[0] CLEAN — 0 findings, hard-0, no baseline**: **28** element kinds discovered / **28** covered, 30 schema files read, 4375 outside-schemas files scanned, **18 distinct vocabulary members, all named** in `packages/schemas/src/provenance/DetectionMethodOrigin.ts`, with **7 planted-tree controls fired** — including the two that prove C4 is scoped to ABSENCE (a mapped member stays silent; three unmapped ones all fire) rather than to narrowing. The ledger moved **1 → 0** when `floor` gained its L0 schema (`30fa5f61`, `5c9b48e7`), and the gate itself was un-orphaned into `run-all` at `28c6b05c`. `check-derived-not-authored` re-run in the same pass: **[0] CLEAN, hard-0**, all 5 arms proven firing against a planted tree. ⚠ **What this closure does NOT say.** The three vocabularies still exist as separate declarations and the gate *prints them* (`RoomDetectionMethod` ×2, `Floor`/`CeilingDetectionMethod` ×3, `DeterminedRoomDetectionMethod`). They are unified **by exhaustive, type-checked mapping onto the L0 `ValueOrigin`** — not by merging the enums. That mapping is the criterion the instrument encodes, and it is the criterion this row closes on; a future merge would be a separate, contract-level change. | **G2** | **INVARIANT** | audit §17.3 | C75 §1.2 | one vocabulary; the five values may not be aliased per package | PV-02 |

---

## §6 — Model truth, verbs & identity (owners: C70, ADR-0318, C69)

| ID | Gap | **Status @ `bbcc11cd`** | Maturity | Impl type | Evidence | Contract | Smallest change | Blocked by |
|---|---|---|---|---|---|---|---|---|
| **MT-01** | **`wall.create` and `slab.create` are readback-negative, and `room.create` dispatch-throws** on the composed bus — the plugin handlers write **DTO stores nobody reads**. *A mutation that changes a DTO nobody reads is a lie, not a capability* (C70 A-INV-3). Newly measurable only because C6 made the stores reachable; the generated verb register still grades all three **UNKNOWN** | **CLOSED (executed)** — `7f380fbe` ("readback-negative rows reach ZERO — and `room.create` could never have executed at all") and `80845b3e` (wall, slab and room become headless-authoritative), re-confirmed by **executed** `check-authoritative-state` → [1] declared 1, which drives 18 mutating verbs through a real bus over real stores and measures a real refusal. `bbcc11cd` closes the `wall.create` half with a determinism harness. | **G1** | **WIRING** | [BIM20-ACCEPTANCE §0.1 follow-up 1](../03-execution/plans/BIM20-ACCEPTANCE-10-OF-10.md) | C70 A-INV-3 · ADR-0318 | per-kind ADR-0318 adoption (wall, room, slab first — they gate everything graph-shaped); each migration PR extends `adr0318.stores.probe.ts` with that kind's same-instance assertion | — |
| **MT-02** | **`view.create` raises a TypeError**, and **`sheet.create` / `schedule.create` / `hierarchy.createSite` are registered verbs that no handler answers** | **CLOSED — narrowed, then resolved by the gate** — recount 2026-08-14: **executed** `check-verb-register` at `0c098d81` → **exit 0**, and the regenerated register reads the row's subjects as: `room.regenerate` **REFUSES** (the §REFUSAL-IS-A-VALUE shape — the row's own permitted terminal state) · `hierarchy.createSite` **LIVE** · `view.create` / `sheet.create` / `schedule.create` **UNKNOWN-liveness with produceCommand handlers** — so "registered verbs that no handler answers" is factually wrong, and the TypeError is not reproduced. Residual, named: UNKNOWN-liveness is MT-04's class, not silence. | **G1** | **MISSING VERB** (handler) | acceptance §0.1 follow-up 1 | C69 · C70 L-INV-1 | a handler, or a **refusal** — a registered id that silently does nothing is the worst of the three states | — |
| **MT-03** | **9 SHADOWED verbs remain** (14→9 at close-out). A bridge is the designated winner in every case; per-verb read-back tests are required | **OPEN — 9 → 6 this session** — recount 2026-08-14: **executed** `check-verb-register` read 8 at `87832530`, then **6** at `0c098d81` (`API-VERB-REGISTER.md:22` regenerated in-pass; `element.updateMark`, `template.assignToNode` and one more resolved mid-session). Six dead routes remain; per-verb read-back tests still owed. *Restamp 2026-08-14 (lane R2) — **6 → 1 committed, and the read-back tests are no longer owed for the verbs that moved.*** Two resolutions landed, each **decided by an executed read-back rather than a preference**: `ce7cc5ae` — `stair.create`, where the plugin arm lost *on store, on payload AND on function* after a geometry-store read-back, and was deleted (3 → 2); `a10afb31` — `stair.move`, resolved by **MERGE in the reversed direction** (the plugin dual-store hybrid IS the merged handler; the editor bridge was the dead one), 2 → 1. ⚠ **NOT closed, and the reason is tree state, not evidence.** `check-verb-register` executed by this lane reads **SHADOWED = 0** with `API-VERB-REGISTER.md` matching the code in both directions — **but that reading is MID-FLIGHT**: both `tools/ga-gate/check-verb-register.ts` and `docs/04-reference/API-VERB-REGISTER.md` are **uncommitted in the shared tree** at `a75e8e1e`, carrying another lane's in-progress `furniture.updateParameters` resolution. The committed artefact at HEAD still reads **SHADOWED = 1**. A register row may not close on work that is not committed, so: **9 → 1 at HEAD, → 0 pending that lane's commit.** | **G2** | **WIRING** | acceptance §0.1 follow-up 2 · `tools/ga-gate/check-verb-register.ts` | C69 | resolve per verb with a read-back test, not a preference | — |
| **MT-04** | **11 store kinds remain unadopted** by the ADR-0318 registry — ABSENT-headless, present in-browser via `registerAllStores` | **UNPROVEN** — the 11-unadopted-kinds count was not re-measured and no gate reports it. **Executed** `check-authoritative-state` scopes itself explicitly to the 18 verbs `world.ts` composes and states it is **NOT repo-wide coverage**, so its green cannot be read onto this row. | **G2 in-browser / G0 headless** | **WIRING** | acceptance §0.1 follow-up 2 · ADR-0318 Exit condition | ADR-0318 I-3 | per-kind construction behind a headless-safe factory; **honest absence** (`undefined`) until then — no scaffold, no empty stand-in | — |
| **MT-05** | **`engineLauncher` hands `window.columnStore ?? columnStoreInstance` to the serializer** for column and curtain-wall while registering the instance into the registry. If the window global is ever a *different* instance, **registry identity and serializer identity diverge for those two kinds** — and the same-instance probe cannot see it headlessly | **UNPROVEN** — the `??` fallbacks in `engineLauncher` were not re-measured, and ADR-0318's own note says the same-instance probe **cannot see this headlessly**. Blocked behind MT-04 as recorded. | **G2, at risk** | **INVARIANT** | ADR-0318 "Named residual risks" | ADR-0318 I-1 | the per-kind migration deletes the `??` fallbacks | MT-04 |
| **MT-06** | **`persist:opening` is MISCONFIGURED for its kind** — `openingStore` holds 0 records after seed and **says so**, honestly. Whether it is authoritative at all is unsettled; openings have **two authoritative copies** (`OpeningStore` + `WallData.openings[]`) with nothing pinning them | **OPEN** — no evidence the single authority was declared or the loser deleted. The rival stores' EXISTENCE was re-verified 2026-08-14 (`core-app-model/src/stores/OpeningStore.ts` present); **the authority question itself was NOT RE-MEASURED IN THIS PASS**. | **G2, rival authorities** | **INVARIANT** | acceptance §0.1 follow-up 5 · audit §17.3, §17.6 | C70 A-INV-1 | declare the single authority and delete the loser. Note `wall.openings[]` is already invariant-checked and `WallOccupancyStore` **stores nothing** — the design property is good, the rival is the problem | — |
| **MT-07** | **Three rival level records** — `WallStore` levels (the persisted one) vs `packages/stores` `LevelStore` vs the hierarchy — and wall `baseLine[*].y` stores **absolute world Y**, so the persisted DTO goes stale after a level-elevation change even though geometry re-projects | **OPEN — narrowed, re-measured at source 2026-08-14 (lane R, HEAD `28a25791`)** — a source-verification measurement (the weaker class; no gate covers this rivalry). One rivalry is RESOLVED by declaration-and-deletion: **§WALL-AUDIT-2026-M3** declares *"Levels are owned exclusively by BimKernel"* (`geometry-wall/src/WallStore.ts:269`; `command-registry/src/levels/AddLevelCommand.ts:58` — *"BimManager is the authoritative owner of levels"*), `WallStore.addLevel()` is deleted, `WallStore.getLevels()` delegates to `this._bim.getLevels()` (`:272–273`), and `removeLevel()` survives only as a wall-cascade holding no level record — so "three rival level records" is now factually **two**. The remaining rivalry is UNDECLARED: the C20 `LevelStore` is composed live in production (`runtime-composer/src/composeRuntime.ts:1028`) beside BimManager's levels, and a grep over `LevelStore.ts` + the `level*` aggregate-commands finds **zero** mention of BimKernel/BimManager or any authority declaration between the two records. The second half **stands unchanged**: `baseLine[*].y` carries absolute level elevation *by schema contract* — `WallDataSchema.ts:195` (*"baseLine.y carries the level elevation"*) and `:209–222` enforce both endpoints equal to it — so the persisted DTO still goes stale on a level-elevation change. | **G2, rival authorities** | **INVARIANT** | audit §17.3, §17.6 | C70 A-INV-1 | one authority per rivalry, declared | — |
| **MT-08** | **The undo gesture race** — pinned RED-BY-DESIGN by 3 `it.fails` at the 250 ms window | **CLOSED (executed)** — `774a91e6`: *"a gesture is an identity, not a stopwatch reading — the 250 ms window is deleted"*, verified at source: `CommandBus.ts:363` resolves `opts.gestureId ?? currentGestureId() ?? newGestureId(type)` under §UNDO-GESTURE-ID (C03 §4.6 U-10), and the three `it.fails` at the 250 ms window are gone. **NOT RE-MEASURED IN THE 2026-08-14 PASS** — carried on its 2026-08-13 source verification (the third weaker source-verified closure), though `undoredo.cert.ts`'s header was independently corrected this session to stop citing the deleted pins (see CE-04). ⚠ **Founder-facing correction**: the row records this as blocked on a decision *"explicitly NOT taken as decided"*. It has since been implemented, so the Blocked-by cell is now factually wrong. | **G4, racy** | **INVARIANT** | acceptance §0.1 follow-up 3 | C70 §3.1 undo/redo link | **founder decision on §UNDO-GESTURE-ID.** Explicitly *not* taken as decided by the founder's "agree" of 2026-08-12 (ADR-0321 status note) | founder |
| **MT-09** | **27 packages fail isolated compilation**, masked by root `skipLibCheck` and surfaced only when the compile gate was un-blinded (it had fabricated ~90 PASS lines per run **for its entire life**) | **OPEN — measured at 26** — `check-per-package-compile` **completed on a long budget** earlier this session (run recorded in `0e28e824`): **26 packages fail isolated compilation, 84 compile, 9 skipped as known issues**. The quoted 27 was a commit subject nobody had executed. The difference matters: roadmap Phase 9's exit is "< 27", so the ratchet PASSES at 26 — but 26 packages still failing is a defect at a measured level, not a closure. Not re-run in this recount (compile-scale checks run at cover time, per the session brief). **NOT RE-MEASURED IN THE 2026-08-14 PASS EITHER** — the 26 stands as the last EXECUTED reading (`0e28e824`), not as a current one. **NOT RE-MEASURED IN THE LANE-R2 RESTAMP EITHER (2026-08-14 pm), and stated rather than carried silently:** `check-per-package-compile` spawns ~85 real `tsc` invocations and runs for hours; this lane did not have that budget, and a number nobody ran this session is worth less than an admission that nobody ran it (§0.0 rule 5). **The 26 is now a two-day-old reading cited three passes running.** It is also the reading a roadmap exit condition ("< 27") is scored against, which makes re-running it, not re-citing it, the next action on this row. | **G4, unverified** | **INVARIANT** | acceptance §0.1 C9, follow-up 4 · commit `2b1e7e99` | C70 §0 | exit-3-fenced per territory; shrink-only | — |
| **MT-10** | **8 post-freeze XSS sites** | **CLOSED (executed — re-closed by the same gate that re-opened it; fourth swing, see end of cell)** — recount (`9fc6804d`): **executed** `check-xss-guards` → **exit 0**, *"no new unguarded HTML-sink interpolations"* — the post-freeze count, this row's subject, is **0**. The consequence-surface sites were fixed (`ae0ef1c0` — element names were LIVE MARKUP in both consequence surfaces) and the baseline was TIGHTENED, 5 files fully cleared (`2f2ea5da`). The same instrument that re-opened this row against `9b60f2e2` now agrees — a row decided by the gate in both directions, which is the only closure it was ever allowed. Distinct and still standing: **547 baselined PRE-freeze findings across 142 files**, shrink-only, a separate ledger from the 8 this row named. *Recount 2026-08-14 (L-STAMP):* **RE-OPENED — the instrument disagrees again**: `check-xss-guards` executed twice this pass (at `87832530` and again at `0c098d81`) → **exit 3 both times, 1 NEW unguarded HTML-sink**: `apps/editor/src/ui/house-layout/houseCirculationNotice.ts:159` (`innerHTML` ← `buildHouseCirculationBannerHtml`). Third swing of this row, decided by the gate every time — post-freeze count is 1, not 0, and this is a standing pre-deploy red. Status: OPEN. *Restamp 2026-08-14 (lane R, HEAD `28a25791`):* **CLOSED — the row was re-opened by a gate and closes by the same gate.** **Executed** `check-xss-guards` by this lane at HEAD → **exit 0**, *"no new unguarded HTML-sink interpolations"*, 547 baselined finding(s) across 142 files, 4614 files scanned — the one sink the third swing named (`houseCirculationNotice.ts:159`) is gone. The fix is `465d01f3` (§XSS-NO-SINK — the sealed-room banner is DOM-built via `textContent`, no markup sink; commit carries its 16/16-test suite), verified an ancestor of HEAD with zero working-tree drift on the fixed files at gate time. The deploy lane reports the fix DEPLOYED to production with bundle proof 6/6 (2026-08-14) — cited as that lane's record, not this lane's run; this stamp's executed evidence is the gate. The 547-finding pre-freeze ledger remains a separate, shrink-only ledger, exactly as before. Status: **CLOSED**. | **G4, unsafe** | **INVARIANT** | acceptance §0.1 follow-up 4 | — (security; owned by territory) | fix per site, exit-3-fenced | — |

---

## §7 — Collaboration (owner: C70 pillar K, C08, C66)

| ID | Gap | **Status @ `bbcc11cd`** | Maturity | Impl type | Evidence | Contract | Smallest change | Blocked by |
|---|---|---|---|---|---|---|---|---|
| **CB-01** | **Leg C is not deployed — no CRDT transport exists in any environment.** Collaboration is UNPROVEN on every certification row **by construction**, which caps every row at PARTIALLY VERIFIED. *No amount of further engineering raises the ceiling; one founder decision does* | **OPEN — founder-blocked, and the code half is now MEASURED** — **executed** `check-two-client-convergence` (`0e7c2d37`): composed clients, **8 crossings transported, both controls fired**. Its own SCOPE block states the wire is SIMULATED, production still runs socket.io last-writer-wins, and **no capacity tier moves from CLAIMED to HELD on this evidence**. C8 remains FAIL, blocked on the L-391 founder decision. The ceiling is unchanged by engineering, exactly as the row says. *Recount 2026-08-14:* `check-two-client-convergence` re-executed → exit 0 (same SIMULATED-wire scope); `check-collab-graph-integrity` re-executed → **exit 1 UNPROVEN** (no deployed transport, its designed reading). **Founder decision 2026-08-14: COMMITTED, being scoped** — the row stays OPEN but is now blocked-by-plan, not blocked-by-undecided. *Recorded verbatim (lane R, later on 2026-08-14 — supersedes the "being scoped" note preceding it):* "Founder decision 2026-08-14: sync-server deploy DEFERRED — manual deploy only for now; no always-on process. Rows stay OPEN/UNPROVEN by decision (blocked-by-decision, no longer being-scoped)." | **G0 deployed / G4 client-side** | **MISSING COLLABORATION** | CD §A-3 · acceptance §0.1 C8 · C66: 0 tiers HELD | C70 K-INV-3 | deploy behind a staging-only flag; the code side is PROVEN locally (hosting edge survives concurrent editing, WS auth fail-closed) and the decision is *a single reversible flip* | **founder** |
| **CB-02** | **`PgAuthz` has not been written**, `MemoryAuthz.addMember()` is called **nowhere in production**, and the `project_members` table exists with nothing hydrating from it. So `PRYZM_AUTHZ_MODE=memory-allow-by-default` means **any signed-in PRYZM user who can name a room can join it**; `memory-deny` means nobody can join anything. Acceptable for a staging cohort; **not for production** | **OPEN — the adapter half CLOSED (executed)**: `a693b35d` — **`PgAuthz` has been written**, so the row's headline is **now factually wrong**. The same commit records that *the schema it would obviously have been written against is a table nothing has ever created*, so the hydration half is OPEN and the production flip stays blocked. **NOT RE-MEASURED IN THE 2026-08-14 PASS.** *Founder decision recorded verbatim (lane R, 2026-08-14):* "Founder decision 2026-08-14: sync-server deploy DEFERRED — manual deploy only for now; no always-on process. Rows stay OPEN/UNPROVEN by decision (blocked-by-decision, no longer being-scoped)." | **G1** | **MISSING COLLABORATION** (authz) | [L-391 §4.1](../03-execution/plans/L-391-COLLAB-DEPLOY-DECISION.md) | C08 | `PgAuthz` is a **drop-in behind an existing seam** — the gate already asks the `Authz` boundary on every upgrade and refuses with `403 / not-a-project-member`; no call sites change | prerequisite of the **production** flip, not of CB-01 |
| **CB-03** | **`check-collab-graph-integrity` lives in `tools/ga-gate/`, not beside the BIM 3.0 certification gates.** Recorded as a **finding**, not blessed: two suites with two exit-code implementations is how the four-exit-code contract quietly becomes two contracts | **CLOSED — evidence corrected 2026-08-14**: the file is (again) in `tools/ga-gate/` — but **by declared design, and the actual defect this row filed is resolved**: the four-exit-code contract now has ONE owner, `tools/rac-conformance/certification/contract.ts`, which the ga-gate file IMPORTS (`check-collab-graph-integrity.ts:140`, its header §CB-03 names this row), and `certify.ts:288` declares the ownership split (transport vs merge semantics) as deliberate non-duplication. One contract, two declared subjects — the "two exit-code implementations" defect is gone. Re-executed this pass: exit 1 UNPROVEN pending a real transport, as designed. | **G1** | **INVARIANT** | C70 §7.1, §7.2 | C70 §7.2 | one suite, one `contract.ts` | — |
| **CB-04** | **Conflict-surfacing has no gate at all.** C70 K-INV-2 — *any merge discarding a user's authored state produces an explicit, resolvable conflict artefact; no silent substitution* — is unmeasured by anything | **CLOSED (executed)** — recount 2026-08-14: the gate EXISTS and RAN — `check-conflict-surfacing` (certification suite; `a926a93d` closed S2-INTERVENING 103→0 SILENT) → **[0] CLEAN, hard-0**: ARM A classifies all 197 declared command types (105 replicated, 92 not-synced, 2 last-writer-wins WITH stated reasons — silently converged BY DECLARATION, never by omission); ARM B drives **3×103 REAL merges** through real `YjsDocAdapter` pairs — every authored-state discard produced a resolvable artefact, **0 SILENT**; ARM D finds 1 production `onConflict` subscriber (`engineLauncher.ts:1026`). Residuals printed by the gate, not erased: the wire is simulated (CB-01's subject), artefact QUALITY at the dialog is not graded, structural loss out of scope. | **G0** | **INVARIANT** | CD §A-3, capability model §14 | C70 K-INV-2 | a gate; it can be specified before the transport exists | CB-01 to run it |
| **CB-05** | **The RAC's strongest capabilities are its least syncable** — late-bound `'all'` subjects (P1-10) | **UNPROVEN** — per-capability convergence proofs remain gated behind CB-01. Unchanged, except: **founder decision 2026-08-14 — COMMITTED, being scoped** (blocked-by-plan, no longer blocked-by-undecided). *Recorded verbatim (lane R, later on 2026-08-14 — supersedes the "being scoped" note preceding it):* "Founder decision 2026-08-14: sync-server deploy DEFERRED — manual deploy only for now; no always-on process. Rows stay OPEN/UNPROVEN by decision (blocked-by-decision, no longer being-scoped)." | **UNPROVEN** | **MISSING COLLABORATION** | audit §12 | C08 | per-capability convergence proofs after CB-01 | CB-01 |

---

## §8 — Certification & gates (owner: C70 §5, §7)

| ID | Gap | **Status @ `bbcc11cd`** | Maturity | Impl type | Evidence | Contract | Smallest change | Blocked by |
|---|---|---|---|---|---|---|---|---|
| **CE-01** | **6 of the 10 gates C70 §7 names have no file at HEAD.** Present: `check-identity-roundtrip`, `check-derived-regenerable`, `check-propagation-reaches` (certification suite) and `check-collab-graph-integrity` (ga-gate). **Absent**: `check-topology-survives`, `check-graph-write-coverage`, `check-constraint-honesty`, `check-epsilon-policy`, `check-provenance-not-invented`, `check-derived-classification` | **CLOSED** — **all six named-absent gates now exist and were EXECUTED at HEAD**: `check-topology-survives` [0] CLEAN · `check-graph-write-coverage` [1] declared 5 · `check-constraint-honesty` [1] declared 15 · `check-epsilon-policy` [1] declared 386 · `check-provenance-not-invented` [1] declared 2 · `check-derived-classification` [1] declared 1. **10 of 10** (`ff421f96`). ⚠ The row's "6 of the 10 gates C70 §7 names have no file at HEAD" is **now factually wrong**, and is the single largest correction in this re-stamp. Recount @ `b61f0ba3`: all ten files still present (five in the certification suite, five in `tools/ga-gate/`) and the ledgers moved the right way mid-session: write-coverage 5 → **2**, constraint-honesty 15 → **2** (`4103a99f` evidenced 11 families; `095cfa10` resolved H5). | — | **INVARIANT** | C70 §7.1 (measured 2026-08-12); confirmed at HEAD: `tools/rac-conformance/certification/gates/` holds exactly the three | C70 §7.1 | build them **on the existing harness — none needs new infrastructure** (CD §I) | — |
| **CE-02** | **The Geometry axis is UNPROVEN everywhere headlessly** — no fragment builders run in the certification harness | **CLOSED (executed)** — `221fdef7`: *"the Geometry axis has a SUBJECT, scored from an EXECUTED headless build"*. **Residual DISCHARGED in this recount (2026-08-14)**: **executed** `check-authoritative-state` now runs a headless fragment build over the same world its cases measure — **5 of 5 fragment families produced geometry** (wall/slab/column/beam sync + roof frame-bus; 15 meshes, 500 vertices, 224 triangles, 6 frames pumped). Its own scope line survives: this proves meshes were BUILT, never that they are RIGHT (dimensional fidelity = `geometry.cert.ts`'s oracle arm) and no GPU path is exercised. | **UNPROVEN** | **WIRING** (harness) | capability model §16 GAP · CR | C70 §6.1 | a headless fragment build in the harness so the axis stops reading UNPROVEN | — |
| **CE-03** | **Browser I/O is uncertified** — IndexedDB / Supabase / autosave are never exercised; only the in-memory serializer/loader pair is | **UNPROVEN as certification — UNCERTIFIED, NOT UNTESTED** (recount 2026-08-14): the row's "never exercised" wording is FALSE — `IndexedDBStore.test.ts` **8/8** and `versionRepositoryIndexedDb.test.ts` green, **EXECUTED in this recount** — but executed unit suites with stub environments are evidence, not certification (C70 §0.1): the certification harness still exercises only the in-memory serializer/loader pair, and Supabase/autosave remain untouched by any suite. The certification claim stays UNPROVEN; the "nobody looked" reason no longer applies to IndexedDB. | **UNPROVEN** | **INVARIANT** | CR UNPROVEN list · capability model §3 | C05 | name it; do not let in-memory green read as persistence green | — |
| **CE-04** | **The unified `performUndoRedo` path is uncertified** — the legacy stack only | **UNPROVEN as certification — UNCERTIFIED, NOT UNTESTED** (recount 2026-08-14): the unified-path suites run green — `performUndoRedo.test.ts` **EXECUTED green in this recount** (28 tests across the paired run), and `undoredo.cert.ts`'s header was corrected (L-CETEXT) to stop citing the deleted 250 ms pins and to record exactly this state: executed unit suites with stub stores are evidence, not certification (C70 §0.1), and extending the harness to the unified path is **no longer pinned-blocked** — it is simply not done. The certification claim stays UNPROVEN. | **UNPROVEN** | **INVARIANT** | capability model §16 GAP | C03 §4.5–4.8 | extend `undoredo.cert.ts` to the unified path | — |
| **CE-05** | **Static discovery counts authored-but-unreached code as present.** No gate in the suite answers the **reachability** question, which is the one this repository keeps failing | **OPEN — the instrument class now EXISTS and is EXECUTED; the gesture half remains the gap** (recount 2026-08-14). Command-level reachability is measured by real drives with read-back: **H5** (graph runtime read-back, 11/11), **H6** (move invalidation, 5/5), `check-propagation-trackers-reach` (8 pairs, 5/5 controls), `check-move-propagation` (7 arms, 4/4 controls), `check-conflict-surfacing` ARM B (309 real merges). The remaining gap is the one H5's own NOT-COVERED block names first: **GESTURE/UI reachability** — no probe drives a user gesture, tool, or browser session (the L-847/L-860 class). That half now has an owning contract: **C82 (ribbon capability surface) + ADR-0326 ("mount the ribbon, disable the unbacked") + SPEC-50**, landed this session. | **G4, half-instrumented** | **INVARIANT** | C70 §7.3(a) · C71 §6.2(a) | C70 §4.2 | *"Machinery present ≠ capability reachable"* — the reachability probe is the missing instrument class | — |
| **CE-06** | **An executed run proves the seeded fixture, not the user's project**, and every non-collaboration gate is **single-client by construction** | **UNPROVEN, and restated by the instruments** — **executed** `check-derived-classification` prints *"THE SEED, NOT THE USER'S PROJECT"*; **executed** `check-topology-survives` prints *"MULTI-LEVEL and MULTI-CLIENT — every fixture is one level, one client"*. Stated, not fixed — exactly as the row directs. | **UNPROVEN** | **INVARIANT** | C70 §7.3(c)(d) | C70 §7.3 | stated, not fixed — the fixture is itself a declared, reviewable artefact | — |

---

## §9 — Implementation-type distribution

Counted over the **84 rows** above, excluding the two non-gap rows (GR-02 PARKED, GR-03 the
recount note) — **82 classified gaps**, of which four carry two types (GR-06, GR-16, PR-10, GE-06),
giving **86 type-instances**.

| Implementation type | Count | Reading |
|---|---|---|
| **INVARIANT** | 48 | the largest class by far: the behaviour exists and is right *sometimes*; a rule, a gate or a refusal makes it right *always*. **Roughly half of these are the gates themselves** — 16 named gates are specified-and-unbuilt across C71–C75 |
| **WIRING** | 18 | authored-but-unwired — the signature hazard |
| **MISSING PERSISTENCE** | 6 | mostly provenance fields, the persist-or-lose ledger, and the constraint store |
| **MISSING VERB** | 4 | `wall.split` · the registered ids no handler answers · the graph verbs · the clash ids |
| **MISSING ALGORITHM** | 3 | 2-D boolean · clash engine · the roof-clash detector (the same engine) |
| **MISSING COLLABORATION** | 3 | transport · authz · per-capability convergence |
| **RETENTION** | 2 | `prevState` on `StoreChangeEvent`; `prevState` at the remaining stores |
| **EXPOSURE** | 1 | the UBG → `composeRuntime` slot and the three graph verbs |
| **MISSING SOLVER** | 1 | and it is **unauthorised** — C74 §4.5 stands |
| **ARCHITECTURAL CHANGE** | **0** | — |

### §9.0 — Status distribution — **RESTAMP 2026-08-14, lane R2**, at `a75e8e1e` (supersedes the L-STAMP table below it)

> **This table was restamped by a second, later pass and the numbers changed.** Lane R2 re-executed
> the deciding instrument for **13 rows** — the ones a day of paying work had moved — and left the
> other 69 exactly as lane L-STAMP measured them. **Rows this restamp re-measured:** MT-10, MT-07
> (batch 1) · CO-03, GE-04, PV-08, GE-12, GE-09, GR-12, GE-07, MT-03, GE-01, GR-14, CO-06. **MT-09
> was NOT re-measured** and says so in its own cell. Every other row is *carried*, and carrying is
> not re-verifying (§0.0 rule 5).
>
> | Status | **Restamp @ `a75e8e1e`** | was @ `0c098d81` | movement |
> |---|---|---|---|
> | **CLOSED** | **55** | 51 | +4 — MT-10 (re-closed on its own executed `check-xss-guards`, batch 1) · **CO-03** · **GE-04** · **PV-08** |
> | **OPEN** | **20** | 24 | −4, the same four |
> | **UNPROVEN** | **7** | 7 | unchanged |
>
> **55 + 20 + 7 = 82.** Counted mechanically over the Status column, not hand-incremented; the two
> `n/a` rows (GR-02 PARKED, GR-03 the recount note) are excluded as always.
>
> **⚠ Read the movement before the number. Four rows closed; SIX moved and did NOT close, and two
> of those were handed to this lane as closable.** `GE-12` — its own new counting gate reads **13
> findings at a declared level of 13**, and *corrects this register's arithmetic upward* (7
> triangulation bodies / 4 engine copies, not "×5" and "×3"): an instrument that counts a
> duplication is not a collapse of it. `GE-09` — `check-refusal-identity` reads **85 offenders
> against a baseline of 86**; one family is paid and executed-proven, eighty-five are not.
> `GR-12` — the most valuable movement of the day and still open: **`boundedBy` went STALE →
> INVALIDATED** under H6, re-run here, and `check-move-propagation` still holds **3 findings** with
> C79 §5.2's five-state channel MEASURED-ABSENT for every family. `MT-03` — 9 → **1 at HEAD**, not
> 0; the 0 reading exists only in **uncommitted** shared-tree work. `GE-01` — the fence was drained
> to 357 and is **RED again at 359**, the two new declarations minted by the very next feature.
> `CO-06` — **15 measured, ~29 still declared**, because its gate exits **[3] STALE LEDGER**.
>
> **Two gates are RED at this restamp and neither is hidden in a count:** `check-epsilon-policy`
> **[3] RATCHET EXCEEDED (359/357)** and `check-no-hidden-mock` / `check-deterministic-regeneration`
> **[3] STALE LEDGER** (14 and 16 unstruck entries). A ledger that keeps paid entries can absorb the
> next regression silently — that is the *mechanism* these rows exist to protect, and it is
> currently unprotected in three places.

---

### §9.0-PRIOR — Status distribution — recount 2026-08-14, lane L-STAMP, `a5211b46` → `0c098d81` (END-OF-SESSION)

Counted over the same **82 classified rows**, re-produced by an executed end-of-session recount
(~28 gate executions + 9 test-suite executions, every one named in-row) — **counted, never
hand-incremented**. This table rots the moment a row closes — re-stamp it, per §0.0 rule 1.
Other lanes were still landing commits under this pass; where a number moved mid-pass the row
carries both readings with their SHAs (GR-14: 74→71 · MT-03: 8→6 · GE-10: V4 red→exit 0), and a
later execution wins over every line here. The prior distributions: 25 / 39 / 18 at `bbcc11cd`,
then 34 / 33 / 15 at `b61f0ba3`.

**Rows NOT re-measured in this pass, carried on their prior stamps (stated per §0.0 rule 5 —
carrying is not re-verifying):** GR-15, PR-07, MT-06 (the authority question; the rival stores'
existence WAS re-verified), MT-07, MT-08, MT-09 (isolated compile not re-run; cited at its
earlier executed reading of 26), CB-02, CO-05, CO-09.

| Status | Count | Reading |
|---|---|---|
| **CLOSED** | **51** | executed evidence in **48** of the 51 — a gate run or a test suite, named in-row and run by this lane. **3** remain source-verification closures (PR-06, GR-15, MT-08), the weaker class, flagged as such; GR-17 left that class this pass (its doc-fix now carries an executed 4/4 test). **Nineteen rows moved to CLOSED in this recount** — GR-09 (re-opened mid-session, re-closed on `44552e1f` + H5 runtime read-back) · GR-17 · GR-18 · PR-03 · PR-10 · PR-12 · GE-02 · GE-03 · GE-05 · GE-10 · GE-11 · CO-02 · CO-10 · CO-12 · PV-04 · PV-05 · PV-06 · MT-02 · CB-04 — every one on an executed gate or test reading, none on a commit subject. |
| **OPEN** | **24** | measured and still failing, most at a NAMED shrink-only ledger in an executed gate (no-empty 71 · epsilon 382 · no-hidden-mock 58 · suppression 41 · delete-integrity 10 · graph-persistence 3 · move-propagation 3 · write-coverage 2 · constraint-honesty 2 · refusal-identity 87 baselined). Two rows *entered* OPEN from UNPROVEN by gaining a completed measurement — GE-09 (`check-refusal-identity` executed at baseline 87) and CE-05 (the reachability instrument class now exists and executes; the gesture half is the named remainder, owned by C82). **One row moved the wrong way: MT-10 re-opened** — `check-xss-guards` exit 3 twice this pass, 1 NEW unguarded sink (`houseCirculationNotice.ts:159`), a standing pre-deploy red. |
| **UNPROVEN** | **7** | nobody measured — GE-08 (cross-MACHINE + GPU, stated by its own new executed instrument) · MT-04 · MT-05 · CB-05 (founder-committed, being scoped) · CE-03 · CE-04 (both now UNCERTIFIED-NOT-UNTESTED: executed unit suites green this pass, certification harness still not extended) · CE-06. Neither a pass nor a fail (C70 §2.2). |

**51 + 24 + 7 = 82.** The two non-gap rows (GR-02 PARKED, GR-03 the recount note) carry `n/a` and
are excluded, exactly as §9's type distribution excludes them.

> ⚠ **SUPERSEDED by §9.0 above (lane R2, `a75e8e1e`, later the same day): 55 / 20 / 7.** This
> table is kept, not deleted, so the movement is auditable — §0.0 rule 4's spirit applied to the
> counts as well as to the rows. Where the two disagree, the **later execution wins**.

> **§9.0.1 — the shape of the movement, stated so it is not mistaken for progress on the defects.**
> The largest single change since `a8f15234` is **not** that gaps closed. It is that **16 gates the
> register recorded as specified-and-unbuilt now exist and run** — every gate row (GR-11, PR-08,
> GE-07, CO-11, PV-07, CE-01) flipped from OPEN to CLOSED, and C70's gate table reads **10 of 10**.
> The defects those gates measure are mostly still OPEN. That is the correct outcome: a gate's job
> is to make a defect *visible and shrink-only*, not to fix it. A register that scored the gate
> landings as capability progress would be repeating the exact error §9.1 warns about.
>
> **§9.0.2 — what changed in the 2026-08-14 end-of-session pass, and what it does NOT mean.**
> CLOSED went 34 → **51**, and this time the movement IS mostly defects, not instruments: the
> duplication ratchets that dominated §3 reached hard-0 (`check-predicate-canonical` 53 → 50 → **0**,
> baseline deleted; `check-prevstate-contract` 17 → **0**; `check-solver-is-real` → **0**), two
> constructions the register had labelled unstarted actually landed and are oracle-pinned (GE-05's
> 2-D boolean, PR-10's roof→walls detector), and **the reachability instrument class this repository
> has failed at five times finally exists and RUNS** — H5 reads writes back out of a live graph, H6
> reads `boundedBy` back as STALE after a real move. That last one is the most valuable finding in
> this pass and it is a RED: GR-12 stopped being UNPROVEN by being *measured failing*.
> **Three cautions, so this number is not over-read.** (1) **MT-10 re-opened on a new unguarded XSS
> sink** and is red at deploy time. (2) Nine rows were carried, not re-measured, and each says so
> in-row — including MT-09, whose "26 packages fail isolated compilation" is a two-day-old reading.
> (3) CE-03/CE-04 moved from "never exercised" to **UNCERTIFIED-NOT-UNTESTED** and stayed UNPROVEN;
> executed unit suites are evidence, never certification (C70 §0.1). Sixteen of these closures are
> hours old and the tree was moving under the pass — **re-run the gate, do not cite this line.**
>
> **One row moved the wrong way.** MT-10 (XSS) was closed by commit `9b60f2e2` and is re-opened
> here on an **executed** `check-xss-guards` reading that contradicts it. Where a commit subject and
> a gate disagree, the gate wins (C70 §0.2) — that rule is what this column exists to enforce.
> *Recount 2026-08-13:* the same instrument now reads **exit 0** and MT-10 is re-closed on that
> reading — decided by the gate in both directions, which is the column working as designed.

> **§9.1 — say it loudly, because it is the headline.** **ARCHITECTURAL CHANGE is EMPTY.**
> Eighty-two rows of measured defects, spanning six contracts and every one of the twelve pillars, and **not
> one of them requires replacing a subsystem.** The dominant work is connecting things that are
> already built and pinning behaviours that are already mostly right. The single replacement
> candidate anyone proposed — merging the dual edge vocabulary — was **considered and rejected**,
> because it would break snapshot v3 for a naming preference and deliver no capability on the
> other side (C71 §4.1).
>
> **The corollary is a warning, not a comfort.** A repository whose entire defect list is wiring
> and invariants is a repository where *reading the code will always look better than running
> it*. That is precisely the condition under which fifteen gates went green-and-blind, a compile
> gate fabricated ~90 PASS lines per run for its entire life, and an **empty seed scored better
> than any real run**. The absence of architectural change raises the value of the gates, it does
> not lower it.

---

## §10 — Coverage matrix (directive §22)

**TARGET → CURRENT → GAP → IMPLEMENTATION → TEST → GATE**, one row per capability domain of
[`BIM30-CAPABILITY-MODEL.md`](BIM30-CAPABILITY-MODEL.md). *CURRENT* cells are cited, never
inferred; **UNPROVEN** means nobody looked and is neither a pass nor a fail.

| # | Domain | TARGET | CURRENT | GAP (ids) | IMPLEMENTATION | TEST | GATE |
|---|---|---|---|---|---|---|---|
| 1 | **Model truth** | one authoritative store per kind, named by the composition root | ADR-0318 slot live; door/window headless-authoritative **by design**; 11 kinds ABSENT-headless | MT-01…07 | WIRING + INVARIANT | per-kind mutate→read-back→same-instance (`adr0318.stores.probe.ts` pattern) | CA-21 census + `check-identity-roundtrip` store-reach floor |
| 2 | **Identity** | id + GUID byte-stable across save/reload/undo/redo/export | **HOLDS** — 17/17 kinds keep id **and** GUID; `check-identity-roundtrip` **0 CLEAN** | — | — | executed, green | `check-identity-roundtrip` ✅ |
| 3 | **Persistence** | authoritative byte-for-byte, derived equivalent, incidental enumerated | persistence **0 FAILED**, exclusions enumerated per ADR-0319 and printed per row | MT-06, CE-03 | INVARIANT | H1 comparator + two-cycle drift run | `check-identity-roundtrip` + `check-derived-regenerable` ✅ · **browser I/O ⛔ UNMAPPED** |
| 4 | **Topology** | REQUIRED families with writer + typed reader + rebuild + mutation-update on delete **and** move | 4 HEALTHY of 25 declared; junctions **now retained**; `joinedTo` unlanded | GR-01, 04, 05, 06, 09, 12, 13 | WIRING + RETENTION + INVARIANT | move/resize/save-load/undo → connectivity answered as a lookup with no resolver re-run | `check-topology-survives` ⛔ **absent** · `check-graph-write-coverage` ⛔ **absent** |
| 5 | **Graph** | one canonical query vocabulary, exposed as refusal-honest bus verbs | *machinery yes, exposure no* — **zero graph verbs**; UBG behind a dev-hook | GR-14, 16, 17 | EXPOSURE + MISSING VERB | rebuild twice → identical snapshots; the golden query through the bus verb **plus its refusal path** | `check-graph-write-coverage` ⛔ **absent** |
| 6 | **Geometry** | deterministic consequence of state; one predicate per family under one epsilon policy | determinism **YES — the strongest finding in the assessment**; substrate duplicated and unguarded | GE-01…12 | INVARIANT + MISSING ALGORITHM | per-family counting gates at pinned counts + an oracle fixture at a known answer (offset's 300 mm, spread 0.000) | `check-epsilon-policy` ⛔ · `check-predicate-canonical` ⛔ · `check-deterministic-regeneration` ⛔ (offset gate ✅ is the template) |
| 7 | **Propagation** | every declared cascade has a live listener **and** a `prevState`-carrying emitter | generic cascade **dead**; bespoke spine **works and earns Level 4** | PR-01…13 | WIRING + RETENTION | move one wall → exactly the predicted dependents change, `prevState` diff as oracle | `check-propagation-reaches` ✅ (8 findings, all declared) · `check-prevstate-contract` ⛔ · `check-suppression-is-reversible` ⛔ · **bespoke trackers ⛔ UNMAPPED** |
| 8 | **Constraints** | declared rules at proven strengths; adapters truthful; violations queryable | four real components (ADVISORY/ENFORCEMENT/VALIDATION×2); **the solver is a mock** | CO-01…12 | INVARIANT first, then WIRING; SOLVER only on evidence | an executed run in which the adapter **reports its own nature** | `check-constraint-honesty` ⛔ · `check-solver-is-real` ⛔ · `check-no-hidden-mock` ⛔ |
| 9 | **Spatial reasoning** | the eight topology questions as lookups where retention permits | 2 of 8 genuine indexed lookups; room index fed two incompatible AABBs | GE-11, GR-04, GR-16 | RETENTION + EXPOSURE + INVARIANT | each of the eight answered through the composed runtime; point-in-room proven on a **concave** fixture | `check-topology-survives` ⛔ (AABB pinned by regression test, not a gate) |
| 10 | **Provenance** | five-value vocabulary, never invented, repair legible | one family of ten, **outside `packages/schemas`**, **invented on load** | PV-01…08 | MISSING PERSISTENCE + INVARIANT | load a snapshot with a missing origin → reads `'unknown'`, not `'auto-topology'` | `check-provenance-not-invented` ⛔ · `check-provenance-coverage` ⛔ · `check-derived-not-authored` ⛔ · **export mapping ⛔ UNMAPPED** |
| 11 | **Regenerability** | rebuild ≡ restored snapshot; persist-or-lose named and shrinking | rebuild is a **slice** (5 of 25), in two copies; ledger is prose | GR-06, 07, 08, PV-05 | MISSING PERSISTENCE + INVARIANT | serialize → restore → rebuild → diff ∅; a pre-graph snapshot reports every loss **by name** | `check-derived-regenerable` ✅ · `check-graph-persistence` ⛔ |
| 12 | **Algorithmic generation** | deterministic engines through commands, with provenance and typed refusals | **complete and grep-confirmed AI-clean**; production ships with no AI key | PR-05 (suppression), PV-02 (no provenance fields) | WIRING | a generation run then the full chain: one undo reverts it entirely, reload preserves it, a later manual wall move propagates in | `check-topology-survives` over a generated fixture ⛔ (engine suites ✅ are executable evidence today) |
| 13 | **Query / reasoning** | certified read paths; failure ≠ empty everywhere | zero graph verbs; three named `[]`-conflation sites, two of them calling methods that never existed | GR-14, 16 | EXPOSURE + INVARIANT | every query proven to distinguish **empty from failed** by probing both | verbs' CA-21 conformance rows ⛔ (verbs absent) |
| 14 | **Collaboration** | concurrent edits converge with topology intact; conflicts explicit | **UNPROVABLE — no transport.** C66: 0 tiers HELD | CB-01…05 | MISSING COLLABORATION | two clients edit a wall and its hosted door → converge, hosting edge intact, lost-update emits a resolvable conflict | `check-collab-graph-integrity` ✅ exists, **blocked on CB-01**, and in the wrong suite (CB-03) · **conflict-surfacing ⛔ UNMAPPED** |
| 15 | **AI boundary** | the LLM sits above the model, never underneath | **YES** — read-only default, read-only capability class CLOSED, zero-token deterministic tiers, no AI key in production | GR-16 (verbs), PV-02 (INFERRED stamping) | EXPOSURE + INVARIANT | a chat query answers a topology question **through the same verb a script would use, including the refusal case** | RAC ladder probes ✅ + CA-21 rows per AI-reachable verb |
| 16 | **Certification** | every claim in 1–15 is an EXECUTED claim | harness real, falsifiability proven per row, four-exit-code contract live, **empty-seed impossibility landed** (`e5addac8`) | CE-01…06 | INVARIANT | the certification **of** the certification: each new gate shows a tampered state going red and an unreadable subject reading MISCONFIGURED **before its first green is believed** | this domain **is** the gate suite — 6 of 10 named gates ⛔ absent |

### §10.1 — UNMAPPED entries. **An unmapped anything blocks readiness.**

Stated explicitly, per the directive. These are not "low priority" — they are **holes in the
instrument**, and a domain with an unmapped gate cannot be scored at all.

| Kind | What is unmapped | Where it is recorded |
|---|---|---|
| **GATE** | **Move-time invalidation** (semantic 5 of C71 §1.2) — no arm in any of the three specified graph gates, and UNPROVEN for **every** relationship family | C71 §1.4, §6.2(c) · GR-12 |
| **GATE** | **The bespoke propagation trackers** — the propagation that actually works, and the thing Level 4 rests on, is **ungated**. `check-propagation-reaches`'s subject is the four dead events only | C72 §6.1.2(d) · PR-09 |
| **GATE** | **Conflict-surfacing** (K-INV-2) — no gate exists, specified or built | capability model §14 · CB-04 |
| **GATE** | **Refusal reachability at the UI** — no gate asserts a refusal is surfaced rather than caught and logged | C73 §5.4(d) · GE-09 |
| **GATE** | **The `./compliance` registry's rules** — only its wiring is checked, never its rule set | C74 §6.3(d) · CO-12 |
| **GATE + IMPLEMENTATION** | **Clash detection** — 12 declared ids, 12 different toolbar ids, 3 in common, no handler, no detector, no gate | GE-06 |
| **IMPLEMENTATION + GATE** | **Provenance export mapping** — the contract's own "largest open risk" | C75 §5, §7.7 · PV-04 |
| **TEST** | **Runtime reachability of graph writers** — no probe has ever run against a live graph; there is no CA-21 equivalent for the graph | C71 §5.8 · GR-18 · CE-05 |
| **TEST** | **Browser I/O persistence** — IndexedDB / Supabase / autosave never exercised | CE-03 |
| **TEST** | **Cross-machine determinism and GPU-side geometry** | C73 §5.4(b)(c) · GE-08 |
| **TEST** | **The unified `performUndoRedo` path** — legacy stack only | CE-04 |
| **TARGET** | **Whether `metadata.version` should be persisted at all**, and whether `_renderVersion` belongs on the model — ADR-0319 "Not decided here" | ADR-0319 |
| **TARGET** | **Does the layer model extend to the backend?** — eight backend packages have no layer at all | `CLAUDE.md` §architecture |

> **§10.2 — the readiness verdict this matrix produces.** **BIM 3.0 is NOT READY to be scored**,
> and the blocker is not the gap count — it is that **thirteen entries above are unmapped**, six of
> them gates. A capability whose gate does not exist cannot be UNPROVEN-then-proven; it cannot be
> *addressed at all*, because nothing can tell you when you are done. Per C70 §3.4 a chain
> containing an UNPROVEN link scores as **incomplete, never as passing** — and per §5.2 a gate that
> cannot establish its subject exits **2 MISCONFIGURED**, which is never absorbable as debt.
>
> The corresponding good news is bounded and real: **every unmapped entry above is an instrument
> to build, not an architecture to replace.**

---

## §11 — What this register does NOT establish

Stated so the boundaries are not inferred from silence.

> ⚠ **Superseded in part by the 2026-08-13 re-stamp.** Item 1 below was true of the `a8f15234`
> stamp and is **no longer true of the Status column**. Twenty-six gates were **executed at
> `bbcc11cd`** for that column and their exit codes and findings are quoted in-row; four rows were
> closed on direct source re-verification of the indicted line. Items 2–5 stand unchanged, and item
> 3 in particular still stands: **no runtime probe was executed against a live graph or a composed
> browser session** — every gate run here is static or headless, and each one says so in its own
> not-measured block.

1. **No measurement was performed for this document.** Every count and line citation is carried
   from the contracts, the ADRs, EV-03/04/05, the audit §17 addendum, or the BIM 2.0 close-out.
   Four claims were **independently re-verified at HEAD** for this register and are marked as such
   in-row: the clash id sets and their 3-way intersection, the absence of any clash handler or
   detector, the opening-awareness of `plugins/wall/src/handlers/CutWall.ts`, and the contents of
   `tools/rac-conformance/certification/gates/`. Everything else is cited, not re-run.
2. **Green-ness is not asserted for any existing gate.** C70 §0.2 is binding here: read the gate,
   not this line.
3. **No runtime probe was executed against a live graph, a live cascade, or a composed browser
   session** — the same limitation EV-03 §10 and EV-05 §4 declare, inherited unchanged.
4. **Severity is not ranked.** Rows are grouped by owning contract, not by importance; a
   priority ordering is a founder decision and would be an inference here.
5. **The counts in §9 are a count of *this document's rows*, not of the repository.** They describe
   how the known defects distribute across implementation types. A defect nobody has found yet is
   in none of them.

---

## §12 — BROWSER-OBSERVED DEFECTS AWAITING ROW ASSIGNMENT (2026-08-13, deploy `517f7a70`)

> ⚠ **THESE ARE NOT COUNTED ROWS.** Nothing in this section is part of the 82 rows above, and
> **none of it changes any count in §9.0.** The denominator is owned by the recount lane and must
> not move on the strength of an appendix. These entries are recorded here **only** so that the
> next recount has them in one place with their evidence already attached; a recount assigns row
> IDs, not this section. Until then, cite the ISSUE-LOG numbers, never a row number.

**Why this section exists at all.** §11 item 3 of this register declares, as a limit on everything
above it: *"No runtime probe was executed against a live graph, a live cascade, or a composed
browser session — every gate run here is static or headless."* On 2026-08-13 that probe finally ran.
**It was a human running it**, against the live `517f7a70` deploy, and it produced first-party
OBSERVED evidence (C75) on the one axis this register has never been able to reach. That evidence
belongs adjacent to the register even though it cannot yet be scored by it.

**The evidence is asymmetric, and the asymmetry matters.** For the viewport axis a founder
observation is *stronger* than any gate reading in this document — it is the only instrument that
sees what the product actually does. For everything the founder did not look at, it is *silent*, and
silence is not a pass.

### The observations

| ISSUE-LOG | What was OBSERVED | Diagnosis (verified at HEAD) | Contract |
|---|---|---|---|
| **L-859** | ✅ Move a wall → **the slab adapts**. ✅ **The walls extend**. ✅ The L-847 model tree renders real data. ✅ Envelope panel refuses honestly in production. | The `§FIX-SLAB-TRACKER-EVENT-SHAPE` wire fix is live and REACHED. Walls extend via `SlabWallConnectivityService`'s slab-loop corner weld (`CASCADE_WALL_BASELINE`), **not** via the mitre pass, which remains untouched. | C72 · C79 §5 · C75 |
| **L-860** | 🔴 Floor finish and ceiling **do not adapt** when the wall moves. | `packages/finish-host-tracker` has **zero wiring sites** (grep at HEAD, repo-wide). Upstream, `FloorTool`'s DRAW path creates **unhosted** floors without refusing. | C79 §2.3 · C78 §1.1 · C70 §4.2 |
| **L-861** | 🔴 Interior partitions drawn by polyline **do not extend** when a bounding wall moves. | **NEW CLASS — room-boundary preservation.** The propagation graph is keyed on pick-walls slab-loop membership; a partition is in no loop, so no cascade is ever emitted for it. | C72 · C79 §5 · C79 §2.3 |
| **L-862** | 🔴 **121 room-compliance ERRORS** tracked after a residential generation (2 on a 10-wall manual test) with no proactive signal. | Detection works. The viewport tint and the IntentPrompt toast are both gated on `showRoomComplianceMessages`, default `false`. `CompliancePanel` *is* ungated — so the errors are reachable, but **nothing tells the user to look**. | C78 §1.1 · C75 |
| **L-863** | 🔴 **0 furniture** in a 753-element, 7-level building. | `§CHAIN-TIMEOUT` at `FALLBACK_MS = 12_000` — the furnish stage exceeded its budget and the chain advanced **silently**. Two defects: engine too slow at building scale, and a dropped stage that reports nothing. | C78 §1.1 · C75 · C66 |
| **L-864** | 🔴 Generated buildings have **no unit containment** — *"Unassigned rooms on Level 01"*, every room flat under the level. Building created with empty `BUILDING USE` / `STOREYS`. | The generator never triggers the spatial-structure auto-setup, and never groups rooms into units. Semantic loss, not display. | C68 · C78 §1.1 · C70 §4.2 |
| **L-865** | Room-redetect suppression (staleness **unmeasured**); per-batch CRDT blackout (note only); CRDT conflict surfacing **verified wired** — a lead that checked out clean. | Recorded at measured strength. Two of the three are hypotheses or notes, **not defects**. | — |

### The finding that outranks the individual rows: **CE-05 is now evidenced four times in one day**

Row **CE-05** above states the gap as *"Static discovery counts authored-but-unreached code as
present. No gate in the suite answers the reachability question, which is the one this repository
keeps failing"*, status **UNPROVEN**. On 2026-08-13 that class produced, in a single session:

1. **L-847** — a whole Data workbench, model tree included, unreachable.
2. **L-849** — a whole `geometry-lift` test suite that belonged to no runner and had never run.
3. *(ledger rows)* — `FloorHostReferenceEdge` / `CeilingHostReferenceEdge`, correct at creation, **zero consumers**.
4. **L-860** — `packages/finish-host-tracker`, authored *as the fix for #3*, with **zero wiring sites**.

With **L-851** (root vitest patterns aimed at a non-existent directory) and **L-864** (a reachable
panel with unpopulated data) the shape is not coincidence. **CE-05 is not a philosophical row; it is
the register's highest-leverage one**, and the standing repo lesson it encodes —
*audit REACHABILITY, not existence* — was violated four times by the same day's work.

### ⚠ The sharpest instance: a gate arm turned GREEN by the unreachable fix

`check-move-propagation` arms **A5 (floor-finish)** and **A6 (ceiling)** ask *"is the host reference
symbol READ by something?"* and count consumer files. Re-executed at HEAD for this appendix
(exit `[1] DECLARED-LEVEL`, 3 findings), both print **✓** — because the count moved 0 → 1 and the one
consumer is `packages/finish-host-tracker`, **which is wired into nothing**. Both rows were struck
from `declaredFindings` in `move-propagation.json`, recording the debt as paid.

**It was not paid. It was re-authored one layer further from the user.** A gate built to detect
authored-but-unreachable code was satisfied by authoring more unreachable code — while the founder,
in the browser, watched the finishes fail to move. **Those two ledger rows are a FALSE GREEN and
must be re-opened by whichever lane owns that ledger.** This is also the cleanest available proof of
C70 §0.2 (*read the gate, not this line*) cutting in the unexpected direction: here the gate itself
was the thing that needed reading past.

### One honest narrowing of CE-05's wording

A gate of exactly the missing class now **exists**: `check-propagation-trackers-reach` (commit
`bbff7030`) drives real mutations through real stores and reads the downstream effect back,
precisely because *"a tracker whose `subscribe()` call is intact … and whose `touch()` line has been
commented out passes every grep ever written."* Its subject is the **four bespoke pairs** —
`DoorDependencyTracker`, `WindowDependencyTracker`, `DeleteElementCommand` §CASCADE-DELETE, and
`RoomTopologyObserver` — **not** the finish families.

So CE-05's gap is now **narrower and better-defined** than "no gate exists": the instrument class is
proven buildable and shipped once, and the fix for arms A5/A6 is to **extend that gate's executed
pattern** rather than invent an instrument. Recorded because leaving CE-05's wording unqualified
would overstate the gap in the opposite direction — the same failure mode, mirrored.

### What this section does NOT establish

1. **No row IDs, no scores, no count changes.** See the banner at the top of this section.
2. **Nothing about what the founder did not exercise.** The session covered wall/slab/finish moves
   and one residential generation. Doors, windows, roofs, stairs and curtain walls were not
   exercised; `doorsRegistered=0 windowsRegistered=0` in every `[PickDiag]` line of the manual test
   is consistent with a walls-only fixture and is **not** evidence about doors.
3. **Nothing about whether the 121 compliance errors are correct.** The product measured them; nobody
   has checked whether the detector is right at that scale.
4. **No causal link between L-865's redetect suppression and L-862's error count.** That is a stated
   hypothesis with a named test, and it must not be cited as a cause until the test is run.

---

### ADDENDUM 2026-08-14 (lane R2) — three items with executed evidence and **NO owning row**

> **Same banner as the section above: these are NOT counted rows and they change no number in
> §9.0.** They are recorded here because lane R2 was asked to judge whether an existing row covers
> them and the honest answer is **no** — inventing rows is a recount's job, not a restamp's. Each
> item below states what this lane *executed itself* separately from what it was *told*.

**1 — L-861's class was paid, and the founder verified it in production.** §12's `L-861` above reads
*"🔴 Interior partitions drawn by polyline do not extend when a bounding wall moves — NEW CLASS,
room-boundary preservation."* That class is now **L-871 – L-875** in the ISSUE-LOG, with root causes
at file:line, and it is fixed: `ca878883` (wire `computeMoveReweld` dispatch + the cascade/revert
latches, §L-872 T-SEAT-GUARD for a corner strictly interior to the moved span), `f2256eba`
(one-undo composition in `CommandManagerImpl` + §L-875 WELD-PRECONDITION, so two region slabs'
welds of the same shared wall both survive instead of last-wins discarding one). **Executed by this
lane at `a75e8e1e`:** `packages/command-registry/__tests__/wallMoveReweldSeam.test.ts` → **8/8 PASS**
— the seam suite that pins L-871 (identity-suppression of no-move entries), L-872/L-873 (pre-fix
wiring pinned as a negative control, fixed wiring asserted), L-874 (**one gesture ⇒ one history
entry, one undo byte-equal restore, one redo replay**) and L-875 (two-slab shared wall).
**Reported to this lane, NOT verified by it:** the founder's production acceptance of that work.
That acceptance is the strongest instrument available on the viewport axis (§12's own asymmetry
argument) and it is *reported*, so it is written as reported. **Why it has no row:** the register's
move rows are `GR-12` (semantic-graph edges surviving a move) and `check-move-propagation`'s
region-derived geometry families. *Interior partitions re-welding to a moved host* is neither — it
is the L-861 class, and L-861 was already parked in this section awaiting a row.

**2 — Contract C83 was minted and nothing in this register points at it.** `28b47ec1` +
`2e5204f0` created `docs/02-decisions/contracts/C83-SPATIAL-VALIDITY-AND-DESIGN-LOGIC.md`, whose
thesis — *"the detection is not missing, the refusal is"* — is the same shape as this register's
`GR-14` (`[]` conflated with unknown) and `CO-01`. **This document's Authority line names C70–C75
only.** A CANONICAL contract that binds spatial-validity work while sitting outside the register's
stated authority chain is a governance gap, not a defect; naming it is the fix, and the next
recount should either extend the Authority line or state why C83 is out of scope.

**3 — The living graph now NOTICES an opened region and ASKS — and no row scored this capability.**
`499360c6` added `packages/room-topology/src/OpenedRegionDetector.ts`, a **pure** before/after
room-set comparison (**executed by this lane: 16/16 PASS**); `a75e8e1e` wired detect → **offer** →
execute: `RoomTopologyObserver` raises it, `OpenedRegionProposal` puts an accept/decline question on
the C83-canonical chat prompt, and **Confirm dispatches ONE undoable `wall.create` through the bus**
(**executed by this lane: 17/17 PASS** — the commit subject says 13/13; the file carries 17 cases
and 17 is what ran). ISSUE-LOG `L-880` records the measurement that decided where the fix belongs,
and it is the valuable part: **three signals already existed and none of them was this signal** —
`unresolvedLoopBreaks=0` was *correct* (a thick-shell T-junction clamp diagnostic with a 1 m
ceiling), the compliance overlay is a downstream symptom channel that cannot name a missing edge,
and §GR12's `boundedBy` undetermined mark is the right *state* in the wrong *shape* (a mark, not a
geometry). **Why it has no row:** this register grades *gaps*, and G6 — *"closed-loop: the answer
feeds back into the model and maintains itself"* — has no row anywhere above claiming it. A
detect → ask → one-undoable-command loop is the first G6-shaped behaviour in the tree, and the
register currently has no cell in which to say so.

---

## §13 — EVIDENCE DELTA, SESSION 2026-08-14 (NIGHT) — rows with NEW EXECUTED evidence, **Status column deliberately UNCHANGED**

> ⚠ **This section does NOT restamp anything.** §0.0''s rule binds: a row moves only on a recount
> that re-runs the gate itself. What follows is the *evidence* this session produced, per row, with
> the SHA and the executed reading, so the next recount starts from measurements instead of memory.
> **Run the gate named in each line before moving any Status.**

| Row | New executed evidence this session | Gate to re-run |
|---|---|---|
| **PR-05** | **Behavioural half LANDED** `372b6635` — hand wall UPDATE surrenders graph authority in the GR2 branch, guarded by `!isBatching && !__pryzmBuildingGenActive()`. Seam test 4/4 driving the REAL store emit (positive + 2 negative controls + scope); package suite 219/219. ⚠ `check-suppression-is-reversible` stays **41/41 by design** — S1 counts production callers OUTSIDE the defining file and the release is same-file (§PR-05-ONE-RELEASE-AUTHORITY); the row''s annotation was corrected instead of hand-struck. **Founder decision 2026-08-14: UPDATE-SURRENDER, not pre-sweep clear.** | `check-suppression-is-reversible` |
| **MT-03** | **CLOSED** `e090f826` — the dead `furniture.updateParameters` initBusHandlers bridge DELETED; the PLUGIN arm owns the verb (it carries the L-72 undo capture the bridge lacked). SHADOWED_BASELINE **1 → 0**: the SHADOWED class the list was minted to hold is now **EMPTY**. Verb moves to UNKNOWN_LIVENESS with the recorded hybrid-classifier limit. 7/7 executed. | `check-verb-register` |
| **GR-10 / GR-14** | Drain moved on two fronts: `d8cbcde8` (4 street-width sites paid AT THE PRODUCER — `neighbours` is now `ReadonlyArray\|null`, null = never answered, plus `neighboursDropped`; 21/21) and `15e4588c` (HierarchyTreePanel row PAID, all 6 remaining sites). Gate reconciled from **exit 3 at 68-vs-67** to **exit 1 at a truthful 68/68** (`48989c03`: 2 stale counts struck, 2 ARRIVED sites DECLARED by name) — independently re-run by the orchestrator. | `check-no-empty-means-unknown` |
| **GE-01** | Epsilon drain `808337c1` — site-parcel-data cluster: **357 → 338 declared** (E2 258→252, E5 99→87, E3 consumers 14→21, E4=0). 12 rows paid: 6 consumed kernel roles, 6 **truthful renames** each arguing why role-adoption would have changed behaviour (one would have widened 1000×, one tightened 50× and flipped verdicts). 3139/3139 site-parcel-data tests. | `check-epsilon-policy` |
| **GE-09** | `66ba6fd7` (stale stair row de-listed — its file was deleted in `ce7cc5ae`; gate exit 3→0, baseline 86→85) + `87e7fd65` (doors family: new shared `childRefusalText.ts` carrying `[REFUSED_WITHOUT_REASON]` + validator; all 3 seams threaded; **85 → 83**). OCC_* family re-verified 9/9. Successor: windows/ceilings/slabs/walls/generic/project families unpaid, same pattern ~30 min each. | `check-refusal-identity` |
| **GE-12** | **Triangulation family COLLAPSED** `2a4853ae` + `2fe73010` — **7 bodies → 1 canonical** `packages/geometry-kernel/src/pure/triangulatePolygon.ts` with an oracle test; 6 consumers repointed, shipping consumer named per §3.6, one §3.7 divergence resolved in-file. Gate **13 → 6**; geometry-kernel **798/798**. PlanarTopologyEngine ×4 deliberately NOT touched (scope-first). | `check-triangulation-canonical` |
| **CO-06** | **15 → 9** `62625504` — six M-B rows paid by RECLASSIFICATION (the ratified TASK-08-in-header shape). §CO-06-GRACE-FIX **verified present** at HEAD (10 hits; the `7cd1db5e` restore held). Successor list is per-row in the lane report: 2 dataworkbench rows were forbidden to that lane; 5 planned-unstarted incl. `familyCreatorPlaceholder` (**both** ledger rows resolve to a third dead file — correction to the lane''s own report). ⚠ `gate-newly-measured.json` pinnedTo says 15, gate reads 9 — update by RUNNING it (742b0dda doctrine). | `check-no-hidden-mock` |
| **BAR 3** (not a row — the second half of 100%) | **131 → 128** `78394be2` — the **hosted-opening CREATE family landed WHOLE** per C78 §19.1: `wall.opening.create` + `door.create` + `window.create` + `wall.createOpening` → 4 normaliser rules + a new `WallOpeningCreateConsequencePlanner` registered once in the shared factory (= both preview AND execution maps). Silent verbs 96→93; structurally-answerable →42. RED-first proven (14 failed with entries withheld), then **21/21** driving the real occupancy store incl. plan-twice determinism. ⚠ **OWED**: `check-plan-determinism` ARM 4 now flags the verb registered-without-harness — add one mirroring `openingMoveHarness`. Next-cheapest family: **wall.delete / opening-delete** (3 findings) on the `3ee632f6` cascade-purge substrate. | `check-relationship-determination` · `check-plan-determinism` |
| **CI-1 / CI-4** (generative programme — NOT of the 82) | **LEDGERED** `45a99b86` on the founder''s 2026-08-14 §4 decision: 38 NAMED rows pinned at the measured reading from **166 real generator runs** (unreachable apt 5/106 · house 7/24 · resi 0/34; doorless 7/106 · 11/24 · 0/34; corridor-gap house 8/24). Negative-tested BOTH directions before trust. **A generation-quality regression now fails CI.** Open founder decision: CI-1''s runtime half (refuse vs blocking banner). | `check-generator-circulation` |
