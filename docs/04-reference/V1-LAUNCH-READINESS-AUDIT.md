# V1 Launch-Readiness Audit — Basic Modeling UX (LIVING DOCUMENT)

> **Status**: LIVING · **Owner**: engine · **Started**: 2026-07-02 · **Target**: v1 launch (next week)
> **Companion plan**: `docs/04-reference/V1-LAUNCH-IMPLEMENTATION-PLAN.md` (phased/subphased, side-by-side).
> **Feeds from**: `ADR-0098` (element-lifecycle conformance audit), `ADR-0099` (host-wall freeze fix),
> `ELEMENT-LIFECYCLE-REMEDIATION-PLAN.md` (element detail + Q-queue).
> **Governance**: this is a launch-readiness rollup, NOT a `*-AUDIT.md` contract-derivative — it references the
> canonical C-contracts (`docs/02-decisions/contracts/`) and never redefines them.

## How to use this document
Every issue the founder reports (past + future) is logged in **§2 Issue Log** with an `L-NN` id and mapped to a
functional area in **§3**. Append new issues to the bottom of §2 and tick the area table in §3. The companion
plan turns these into phased work. **Nothing is dropped.**

Verdict legend: **OK** (verified working) · **GAP** (works but diverges/incomplete) · **BROKEN** (reproduced
defect) · **N/V** (needs source verification — file cited).

### L-L — ON A SHARED CHECKOUT, `git stash` IS AS DANGEROUS AS `git add -A`. IT IS A WHOLE-TREE OPERATION WEARING A PATH-SCOPED MASK.

Two agents hit this within an hour, independently:

* One ran `git stash push -- apps/editor/src …` — a **path-scoped** stash, which looks safe — to
  prove some failures were pre-existing. It **stashed four other agents' in-flight files**, and the
  pop then conflicted. Everything was recovered (each file restored from the stash object, the newer
  worktree version kept where it had moved on, every agent's diff verified back) — **barely**.
* Another had its edits to a contended file **rolled back by an external write**, and caught it only
  because it **re-grepped the file** instead of trusting that its edit had stuck.

**THE RULES, now standing:**
- **NEVER** run `git stash`, `git checkout -- .`, `git reset --hard`, or any blanket tree operation.
  Other agents' uncommitted work lives in this tree. A blanket op destroys it.
- Commit with **explicit pathspecs only**: `git commit -m "…" -- <paths>` (see **L-I**).
- `git status --short` before every commit. Anything staged you did not stage is a **STOP**.
- **After editing a contended file, RE-GREP IT** to confirm your change survived, before building on it.
- **Commit early.** An uncommitted file is a file another agent can destroy.

This is the sibling of **L-I**. L-I: a *commit* silently takes what you did not stage. L-L: a *stash*
silently takes what you did not touch. **Both are whole-tree operations that look scoped — and on a
shared checkout, "looks scoped" is the trap.**

---

### L-M — WHEN EVERY TEST PASSES BUT ONE, **THAT ONE IS THE DESIGN**. FIND IT BEFORE YOU BUILD.

Making dimensions live in a Set Out view has an obvious wrong implementation — **delete every
dimension and rebuild the set from scratch**. It is simpler, and it is what most people would write.

**It passes almost every test you would think to write:**
- *"add a door → it gains a dimension"* — ✅ passes.
- *"delete a window → its dimension goes, no orphan"* — ✅ passes.
- *"change the crop → the set matches the visible set"* — ✅ passes.
- *"the chain still partitions the façade"* — ✅ passes.
- *"run the reconcile twice → nothing changes"* — ✅ **passes too**, unless you additionally assert
  that the annotation **IDs** are unchanged.

**And it is catastrophically wrong**, because a rebuilt annotation is reborn with a new id — and the
user's `offset` and `screenOverride` live **on the record**. So **every dimension line the architect
dragged into place is silently wiped on every model edit.**

**The ONE test that discriminates:** *drag a dimension, then edit the model → the drag survives* (same
id, `offset` and `screenOverride` byte-identical). A delete-and-rebuild fails there, **and nowhere
else**.

**THE RULE: when you can name a wrong implementation that passes your whole suite, you have not yet
written the guard that matters.** Do not go looking for it afterwards — *derive the design from it*.
Here, the founder's own requirement (*"the user's placement must survive"*) **forced** the identity
model: presentation lives on the record ⇒ a regeneration must be a **reconcile**, never a
delete-and-recreate. **The discriminating test WAS the architecture.**

This is the sharpest form of **L-J** (*a guard that cannot fail is not a guard*): here the guards
*could* fail — they just could not fail **on the bug that mattered**. A suite that is green against
the wrong implementation is not a suite, it is a decoration.

---

### L-J — A GUARD THAT **CANNOT FAIL** IS NOT A GUARD. PROVE YOUR TEST CAN GO RED.

I briefed an agent on the overall-dimension bug (L-281) with what I thought was the sharp
instruction: *"guard it on an L-SHAPED plate — his exact case is L-shaped, and A RECTANGLE
WOULD PASS BY LUCK."*

**The agent measured it and refuted me: AN L PASSES BY LUCK TOO.** Every extreme node of an
L **is** a bbox corner, so the overall never crosses the plate. The min-X node of an L is
`(0,0)` — clearance zero, no crossing. My "sharp" guard was **unfalsifiable**: it would have
gone green on a broken product and I would have shipped it believing it was proved.

The defect needs an extreme node that is **NOT a bbox corner** — a **stepped / T / U** plate.
The suite now **reproduces the crossing on a T** (the vertical overall drawn straight across
the plate — the founder's exact line), then proves the fix, and keeps the L as a *regression*
case **with a comment saying why it is not a proof**.

**THE RULE: before you trust a guard, MAKE IT GO RED.** Break the product deliberately and
watch the test fail. A test that has never failed has never tested anything — and the more
confident the brief that specified it, the more dangerous it is.

This is L-C's twin. L-C says: *when a test is red, ask whether the TEST or the PRODUCT is
wrong.* L-J says: *when a test is GREEN, ask whether it COULD have been red.*

---

### L-K — CHECK THE INSTRUCTION AGAINST THE MODEL, NOT AGAINST THE INSTRUCTOR.

In the same session I briefed the auto-tag agent to display **`ElementCode`** as the tag mark
— I had seen `WA-00-001` in the founder's inspector and assumed it was the source.

**It is not.** `ElementCode` mints a *different*, dash-free identifier (`WA001`). The mark the
founder sees comes from `MarkGenerator` (`element.mark`) — **and the door/window SCHEDULE JOINS
ON `element.mark`.**

Had the agent complied with my brief, **every tag in the product would have failed to join its
schedule (C28)** — the tag would have looked perfect and been useless, which is this codebase's
signature failure. It checked the model instead of obeying me, and said so.

**THE RULE: an instruction from the orchestrator is a HYPOTHESIS, not a fact.** If the code
disagrees with the brief, THE BRIEF IS WRONG — say so, in writing. Every agent brief on this
project now carries that line, and it has paid for itself twice today.

---

### L-I — `git commit` COMMITS THE **INDEX**, NOT YOUR ARGUMENTS. IN A SHARED CHECKOUT, THAT SHIPS OTHER PEOPLE'S HALF-DONE WORK.

**I broke `main` with a DOCS commit.** I ran `git add docs/04-reference/ && git commit` — scoped, careful, docs only. But a live agent had **staged** `packages/core-app-model/src/index.ts` (its new barrel exports), and **`git commit` commits the whole index**, not just the paths I passed to `git add`. So my "docs" commit silently carried that file, and `main` spent ~20 minutes **re-exporting `./drawing/DrawingZone.js` with the file absent** — a broken build on the founder's deploy branch.

The agent caught it, said so plainly, and closed it in its own commit. **It also did the right thing on the way out: it split the shared `index.ts` BY HUNK, so the annotations agent's still-unstaged export stayed unstaged.**

**THE RULES, and they are cheap:**
- In a shared checkout, **`git commit -- <paths>`** (or `git commit <paths>`), **never a bare `git commit` after a scoped `git add`.** The bare form is a snapshot of whatever is staged — including work you have never read.
- **Check `git status --short` before EVERY commit.** If something is staged that you did not stage, STOP.
- This is the third face of **L-A** (*a caller and its callee are one change*): the first two shipped **half a change**; this one shipped **someone else's half a change**. All three have the same shape — **a commit is only safe if you can name everything inside it.**
- After pushing to `main`, VERIFY THE OUTCOME (**L-B**): `git show origin/main:<the file the export points at>`. A barrel that re-exports a missing file is a build break that no local test will catch, because the file is right there in *your* tree.

---

## §1 Scope — the "basic modeling must be perfect" surface

The v1 promise is that an architect can, flawlessly and responsively: **navigate the camera**, **create elements**,
**move/rotate/edit elements** (dimensions, materials, hosting), **select**, **create & manage views**
(plan / 3D / elevation / section / sheets), **annotate & dimension**, **undo/redo**, and **save/load** — on both
an empty project and a heavy one, without freezes or stutter.

---

## §2 Issue Log (append-only; founder-reported + audit-found)

| ID | Reported | Issue | Area | Verdict | Maps to |
|----|----------|-------|------|---------|---------|
| L-01 | founder | Wall-move with hosted door/window **freezes** the app | Editing/Hosting | BROKEN→FIXED | ADR-0099 (shipped 52ce2693); amplifier Q6 in flight |
| L-02 | founder | Heavy 40-storey tower **navigation too slow** | Camera/Perf | BROKEN (in progress) | Q2 (agent)  **PERF-PLAN+INSTRUMENT (4d16580c):** root-caused — most heavy-nav paths already mitigated; the remaining top lever is element instancing being DEFAULT-OFF for columns/beams/furniture (`__pryzmElementInstancingV1`) → thousands of un-instanced draw calls at 40 storeys. Shipped Phase P1 `§PERF-L02-L03-INSTRUMENT` (gated `__pryzmPerfTrace`, §PERF-L02-DRAWCALLS/FRAME) to attribute the cost. FIX PENDING (P4: enable instancing WITH a shared colour-keyed material cache + P3 Scene Registry/OI-058) — needs a heavy-load `__pryzmPerfTrace=true` capture to target. Fenced off wall math. C04. |
| L-03 | founder | Heavy project **load fails** ("timeout 30s") + frozen view; autosave-during-load; clear-on-switch O(N); 940 RBLs | Save/Load | BROKEN (P2 shipped, confirm pending) | Q3 (agent) / ADR-0098 F2  **§FIX-LOAD-TRAVERSE-BATCH (4d16580c):** root cause — during load, `ImportProjectCommand` drains under the storeEventBus batch (NOT batchCoordinator), so each of thousands of `bim-*-added` events ran TWO full `scene.traverse()` passes over a growing scene → O(n²). Fix: guard the per-add tier/PBR pass on `__pryzmProjectLoadActive` (skip during load) + one consolidated pass at `pryzm-project-loaded` (same result, O(n)). 8/8 specs. Plus P1 `§PERF-L03-PHASE/TIER-TRAVERSE` instrumentation to confirm on next prod heavy load. Deferred per-level resolve scheduling (P5) + redetect remain if data shows residue. Fenced off wall/room math. C11/C04. |
| L-04 | founder | **Selection picks wrong element** (window→door), worse with density → **FIXED** `§SELECT-EXACT-PIXEL-FIRST`: the id at the exact cursor pixel wins outright; the radius scan is a fallback engaged ONLY when the centre pixel is background (nearest-candidate + thin/slot-id tiebreak). Verified already implemented in `packages/picking/src/gpu-pick.ts` (`chooseNearestThinSlot`); tests `packages/picking/__tests__/gpu-pick.test.ts` (33 pass). | Selection | BROKEN→FIXED | Q4 (agent) / F4 |
| L-05 | founder | **WebGL2 ghost/duplicate on rotate** (WebGPU fine) → **FIXED** `§FIX-WEBGL2-GHOST-ON-ROTATE` (ADR-0108): the silenced OBC base framebuffer is re-cleared/invalidated per lightweight move-frame via a pre-frame hook; WebGL2 path ONLY (the native-WebGPU TSL path never invokes it). Verified already implemented in `packages/renderer-three/src/pipeline/RenderPipelineManager.ts` + hook armed in `apps/editor/src/engine/initScene.ts`; tests `RenderPipelineManager.backend.test.ts` (29 pass). | Camera/Render | BROKEN→FIXED | Q5 / F7 |
| L-06 | audit | Per-edit **redetect + full plan re-projection storm** (freeze amplifier) → **FIXED** `§FIX-WALLMOVE-REDETECT-DEFER` (+ L-65/L-89/L-90 incremental graft): per-move redetect + full plan reproject are deferred to drag-end and replaced by an incremental split/graft. Verified already implemented in `packages/core-app-model/src/views/PlanElementDragController.ts` + `packages/room-topology/src/RoomTopologyObserver.ts`; tests `wallMovePlanReprojectionDefer.test.ts`, `planProjectSplitIncremental.test.ts`, `wallMoveRedetectDefer.test.ts` (all pass). | Editing/Perf | BROKEN→FIXED | Q6 (agent) / F3 |
| L-07 | audit | **No first-class Rotate command**; rotation absent for most elements | Editing | GAP | Q7 / F5 |
| L-08 | audit | **Uneven material commands** (only walls/doors/windows) → **FIXED** `§FEAT-UNIFORM-MATERIAL-COMMAND` (ADR-0117): uniform per-family `<family>.setMaterial` handlers for slab/ceiling/roof/floor/column/beam/stair/handrail/furniture/plumbing/lighting/curtain-wall (+ existing wall/room/structural), unified at the panel by one `MaterialDispatch` facade. Impl row L-57. | Editing/Materials | FIXED | Q7 / F6 |
| L-09 | founder | **Wall-draw rubber-band preview stutters/gets stuck** (empty project) → **FIXED** `§FIX-WALLPREVIEW-RENDER-REQUEST` + `§DEFER-TIER-DURING-DRAW` (ADR-0101): every preview pointer-move requests a repaint (OBC MANUAL `needsUpdate`, no new rAF), and tier-escalation / pipeline-rebuild / TRAA-enable are latched-deferred out of the live draw (so TRAA never turns on mid-rubber-band). Verified already implemented in `packages/geometry-wall/src/WallTool.ts` + `packages/core-app-model/src/rendering/ToolInteractionRef.ts` (arm sites in `apps/editor/src/engine/initScene.ts`); test `ToolInteractionRef.test.ts` (pass). | Modeling | BROKEN→FIXED | Q8 (agent) / W6 |
| L-10 | founder | Wall-move freeze **"still present"** — was testing pre-F1 bundle; F1 deploying | Editing | INFO | verify post-deploy + Q6 |
| L-11 | founder | **Environment & Camera panel must be REAL**: real sun (Forma/Cesium ephemeris basis), real shadows + an invisible L0 shadow-catcher so every element casts a ground shadow, and every button meaningful (climate/heat/wind/population/post-fx) | Camera/Render/Env | **FIXED** `§FEAT-REAL-ENVIRONMENT` (ADR-0106) | **8A real sun** — `RealSunService` now DRIVES the Pascal key light (the scene's sole real shadow caster) from the true solar position at the site lat/lon + time-of-day (same NOAA basis as Cesium/Forma/`@pryzm/solar-analysis`), via a `KeyLightHost` seam — no parallel light, one caster, so `§PERF-HEAVY-SHADOW-OFF`/ADR-0111 stay intact. Panel gains a **Real sun** mode toggle (real+offset vs manual), **Time of day**, and az/el/intensity as offsets/multiplier. **8B ground** — new `GroundShadowCatcher` (renderer-three, P2): invisible receive-only `ShadowMaterial` plane at L0 (opacity<0.5 so never a caster, non-raycastable, reversible, no GPU dispose) → every element casts a grounded shadow even with no slab; **Ground shadows** toggle (default ON). Shadows toggle + climate/heat/wind/population/post-FX buttons already wired (`§ENV-CLIMATE-VISIBLE`/`EnvironmentHud`/initUI). Wired in `initScene`. Tests: `RealSunService.driveKeyLight.test.ts` (6) + `GroundShadowCatcher.test.ts` (6). **Remainder:** post-FX (AO/bloom/exposure) still target the OBC PostproductionRenderer path — verify they reach the live WebGPU renderer (separate follow-up). C04/C12/C19/C21/ADR-0074. |
| L-12 | founder | **Wall T-junction draws an "arrow"/spike** — guest wall should butt clean on the host, host unchanged | Editing/Geometry | BROKEN→FIXED | ADR-0055 §FIX-WALL-TJUNCTION-BUTT (batch 2) |
| L-13 | founder | **Plan-view door: wall lines don't close onto the frame — gap at the jamb** | Views/Drawing | BROKEN→FIXED | ADR-0104 §FIX-PLAN-DOOR-JAMB-SEAM (batch 2) |
| L-14 | founder | **Floor Finish default thickness was coupled to base offset** — decoupled: thickness is now an independent realistic ~15 mm finish layer, base offset (FFL build-up) stays 75 mm; user-overridable | Creation | GAP→FIXED | §FIX-FLOOR-FINISH-DEFAULT-THICKNESS (C11/C03; keeps L-87 FFL consistent) |
| L-15 | founder | **Properties panel must be professional** — remove stray lines, absolute alignment, organic to use | UI | BROKEN→FIXED | ADR-0103 §FIX-PROPERTIES-PANEL-POLISH (batch 2) |
| L-16 | founder | **Spacebar rotates the preview element 90° CW during placement** (all placeable elements, 3D+plan) until click/Enter commit / Esc cancel | Modeling/Creation | GAP | queued; extends F5/Q7; C06/C11 |
| L-17 | founder | **Every element needs a "change type" dropdown** to swap it for another type (e.g. sofa→another sofa). Exists for walls (WALL TYPE) but **doesn't work for existing placed elements** — select an element → replace with a different type | Editing/Types | SHIPPED | ADR-0105 §FEAT-ELEMENT-CHANGE-TYPE (batch 3) |
| L-18 | founder | **Duplicate furniture on open** — moving a sofa reveals an identical one underneath (collab catch-up re-creates already-placed furniture; dedup covers rooms/slabs/floors but not furniture) | Save/Load/Collab | **FIXED (f8b64a39, live)** | §FIX-CATCHUP-DUPLICATE-CREATE — **ROOT CAUSE (2026-07-10):** TWO gaps. (1) The reproducing one: `CreateFurnitureCommand.execute()` registered the element in `bimManager` but NEVER in `elementRegistry`, while every other Create* family does (`CreateWallCommand.ts:269` -> `'wall'`). `RemoteCommandDispatcher.isAlreadyAppliedCreate` keys dedup on `elementRegistry.getStoreType(id) !== undefined`, so the replayed `CREATE_FURNITURE` was never skipped -> re-executed on catch-up (re-minting a fresh UUID, since `payload.id` was absent) -> the duplicate sofa underneath. That is exactly why walls dedup and furniture does not. (2) Structural: the dedup gate `type.startsWith('CREATE_')/'BATCH_CREATE_'` + `targetIds` is blind to the ENTIRE dotted-bus create family (`furniture.create`, `lighting.create`, `wall.batch.create`, …) whose ids live in `payload.id` / per-entry ids. FIX: register furniture (+ dining-chair children) in `elementRegistry` on execute, unregister on undo; add a dotted-bus branch (`type.endsWith('.create')`) with a `collectBusCreateTargetIds` helper (conservative: skip only when EVERY extracted id is registered; non-minting verbs like `wall.createOpening` correctly excluded); bridge bus-placed furniture via `initTools §FT-FURNITURE`. Legacy branch preserved byte-for-byte. Gate: 18/18 idempotency (+9 new), 29/29 furniture suites, root tsc exit 0. RESIDUAL: other bus families (lighting/handrail/beam/curtainwall) still need their own `elementRegistry` registration for their dedup to fire; and bus commands are never socket-broadcast to `project_command_log` (only the legacy `commandManager` path is), so live cross-user sync of bus-placed elements does not occur — see L-206. **SUPERSEDED RECONCILE NOTE (2026-07-05):** The `§DUPLICATE-ROOMS-PERSIST` dedup (`RemoteCommandDispatcher.isAlreadyAppliedCreate`, `apps/editor/src/engine/RemoteCommandDispatcher.ts:72-79`) is type-agnostic but keys on `type.startsWith('CREATE_')`/`BATCH_CREATE_` + populated `targetIds`, applied only to serialized commands replayed through `RemoteCommandDispatcher`/`commandManager.onCommandExecuted`. Production furniture placement dispatches the **bus** `furniture.create` (`CreateFurnitureHandler`, lowercase dotted type) from `FurnitureDragDropHandler`/`FurniturePlanToolHandler`, bridged to the legacy store via `furniture.created` events (`initTools §FT-FURNITURE`) — it does NOT flow through the legacy `commandManager` broadcast as a `CREATE_FURNITURE` serialized command, so the generic dedup demonstrably does not cover it. No furniture idempotency test exists (`RemoteCommandDispatcherIdempotency.test.ts` covers room/stair/slab only). Also founder-HELD (line ~298). |
| L-19 | founder | **Rotate gizmo shows full 3-axis sphere** — most elements only rotate about the VERTICAL axis (yaw); need single-axis default + per-type exceptions (roof/beam slope) | Editing/Rotation | GAP | routed to Rotate agent (a476739); C16/C11/C03 |
| L-20 | founder | **Placement preview must appear IMMEDIATELY** on element select — currently the ghost only shows AFTER the first click (which already creates the element), so the 1st placement has no preview. Applies to ALL placeable elements (furniture, windows, …) | Modeling/Creation | FIXED (reconciled: §FIX-PLACEMENT-PREVIEW) | consolidated into row L-20/21/23. Ghost is now built at tool-activate BEFORE the first click — `KitchenCabinetTool.ts:161`, `WardrobeCabinetTool.ts:118` (§FIX-PLACEMENT-PREVIEW L-20); the carousel GLB path shows its ghost on pointermove (pre-click) and the first click COMMITS rather than creating-then-previewing (`FurnitureDragDropHandler._handlePointerMovePlacement`). Test `apps/editor/__tests__/ParametricPlacementSpaceRotate.test.ts` |
| L-21 | founder | **Placement preview geometry must be PRECISELY ACCURATE** — a huge rectangle shows for a tiny bedside table; the ghost box doesn't match the element's real footprint/size (placeholder-box default when GLB 404s). Audit all elements | Modeling/Creation | FIXED (reconciled: §FIX-PLACEMENT-PREVIEW) | consolidated into row L-20/21/23. GLB click-to-place ghost is sized from the catalog descriptor's declared footprint (`FurnitureDragDropHandler.ts:234-246` — falls back to a block only when no dims supplied), forwarded via `FurnitureSidePanel.ts:325`; parametric kitchen/wardrobe ghosts are the real engine geometry (ADR-0113). Test `apps/editor/__tests__/ParametricPlacementSpaceRotate.test.ts:112` (§FIX-PLACEMENT-PREVIEW L-21 — ghost dims track config) |
| L-22 | founder | **Furniture/interiors library card previews are low-quality / "tacky"** — `/items/**/thumbnail.webp` 404s in prod (tracker OBJECT-STORAGE-GLB) so cards fell back to mixed-metaphor side-view icons + a raster 3D isometric on a light-blue bg, inconsistent with PRYZM's clean plan symbols | UI/Symbols | BROKEN→FIXED | ADR-0110 §FIX-LIBRARY-DIAGRAM-ICONS (batch) — new pure `furniturePlanIcon.ts` renders clean **diagrammatic top-view** plan symbols (PRYZM purple `#6600FF`, single ink, no black) reusing the drawing's `*PlanSymbolBuilder` vocabulary; symbol is default+fallback for every card (real photo upgrades in if ever hosted); covers wardrobe/chairs/tables/beds/kitchen/sofas/trees/sanitary/cabinets + generic. Drawing symbols unchanged; C06/C18 |
| L-20/21/23 | founder | **Placement preview** — appear immediately on select (L-20), accurate footprint ghost (L-21), SPACE-rotate on ALL flows incl parametric kitchen/wardrobe (L-23) | Modeling/Creation | BROKEN→FIXED | ADR-0107 (amended) `§FIX-PLACEMENT-PREVIEW` + `§FIX-PARAMETRIC-SPACE-ROTATE` — kitchen/wardrobe reuse shared `PrePlacementRotation`; carousel GLB ghost sized to real descriptor footprint; per-flow audit table in §3.2 |
| L-25 | founder | **WebGPU 3D goes BLACK** after creating a few walls with lots of mouse motion, then clicking (2D plan still shows elements — it's Canvas2D). Console: `131× Destroyed texture [ShadowDepthTexture] used in a submit` — WebGPU **device-loss cascade**. Root cause: the `§PERF-NAV-LOD` nav lever dropped shadows during motion by clearing `keyLight.castShadow`; on WebGPU that makes THREE **destroy** the ShadowDepthTexture inside the next `rp.render()` while the prior frame's submit (referencing it) is still in flight → device lost. Rapid mouse motion thrashed the destroy/realloc every few frames. | Camera/Render | BROKEN→FIXED | ADR-0111 `§FIX-SHADOW-MIDSUBMIT-DESTROY` — nav-LOD now **freezes** the shadow map (`shadowMap.autoUpdate=false`, never destroys the texture) via new `RenderPipelineManager.setShadowPassSuppressed()`, with a debounced restore; heavy-tier gate decoupled from the nav gate. C04 / §SHADOW-DEVICE-LOSS-FIX |
| L-26 | founder | **Wall-draw alignment guide** should prefer the PERPENDICULAR (ortho) direction of the draw, not the colinear same-direction extension (which conveys nothing) | Modeling/Creation | BROKEN→FIXED | `§FIX-ALIGN-GUIDE-PERPENDICULAR` — new `WallAlignmentGuide.guideAxisPreference(dx,dz)` suppresses the colinear axis's guide+snap (±22.5° neutral zone keeps both for diagonal draws); `WallTool` passes draw dir; C06/snapping, no ADR |
| L-27 | founder | **Wall T-junction STILL shows an "arrow" spike** — interior partition meeting a wall in a T; L-12's `§FIX-WALL-TJUNCTION-BUTT` (JunctionResolverV2) shipped but is INSUFFICIENT for the real thin-partition case (black wedge in 3D + arrow tip in plan). Re-opened. | Geometry/Walls | BROKEN→FIXED | ADR-0055 `§FIX-WALL-TJUNCTION-BUTT-2` (batch 3). **Real root cause = the ring-sweep PIVOT, not the classification.** L-12 correctly reclassified host→passthrough (guest side-corners butt on the host near face) — but `applyRingSweep` still wrote a centreline PIVOT vertex at the host centreline for the T-attacher and `WallFootprint2D` inserted it BETWEEN the two near-face corners → a triangular tongue from the near face (z=+halfT) DOWN to the host centreline (z=0): the founder's 3D wedge / plan chevron. L-12's `§WALL-BODY-INNER-FACE` suppressed that pivot ONLY when the host was ≥1.5× thicker, so an equal-thickness partition-into-wall T still spiked. Fix: suppress the centreline pivot for EVERY real endpoint abutting a passthrough (a T is not an X), thickness-independent; AND re-point the ring-sweep pivot for every clean 1-real-end T onto the host-centreline foot so a guest ending on the host inner face butts flush (no half-thickness gap). Plan chevron = projection of the fixed 3D footprint (`EdgeProjectorService` has no T-logic) → auto-resolves. Layered/opening walls use the legacy `WallJoinResolver._clampEndToShellInnerFace` which already butt-clamps body-Ts (arrow is V2-footprint-specific). **Cluster sub-case `§FIX-WALL-CLUSTER-DEGENERATE`:** the founder's other repro is an L-corner (two walls co-terminate) that ALSO receives a THIRD wall at the SAME vertex → black triangular spike. Reproduced in a footprint harness: a genuine 3-wall node of FULL-LENGTH walls tiles cleanly (all positive-area, shared pivot); the spike is a DEGENERATE cluster member — a wall shorter than the 0.20 m band whose BOTH endpoints snap to one node, so the ring sweep hinges both ends on one pivot → a bow-tie / negative-area (inverted-normal, area −0.03) footprint that also distorts the L-corner. This stub (0.158 m) clears the legacy 0.15 m length gate and the legacy 0.12 m band never doubles it, so only the V2 path renders it. Fix: detect a wall doubled in a cluster, STRIP it from the sweep (the L-corner walls then render clean) and mark its `WallMiter`/`WallFootprint` `invalid` → `WallFragmentBuilder.buildWall` skips its mesh at the shared `§WJR-INVALID` checkpoint. Genuine N-way full-length nodes unaffected (no false positive). Footprint-level regression tests added (thin-partition T + L-corner-plus-stub cluster: 17 cases green; 114 geometry-wall + 67 V2 tests green). C15/C11. |
| L-28 | founder | **Wall tool needs "Apply" on plan view** — on 3D, `WA` activates the wall tool immediately (can set first point straight away); on PLAN view the "Select Wall Type" panel requires clicking **Apply** before drawing. Should be active-by-default with the default (Plain Wall) pre-applied, matching 3D. Related to L-28-adjacent tool-activation parity. | Modeling/Creation | BROKEN→FIXED | `§FIX-PLAN-WALLTOOL-DEFAULT-ACTIVE` — root cause was **perceptual, not functional**: `WallPlanToolHandler` is armed the instant the tool activates and its `onClick` sets the first point UNCONDITIONALLY, committing with the tool's type (default `undefined` → Plain Wall / thickness 0.2, identical to 3D). No Apply was ever functionally required — but the `showWallPreDraw` panel (`PropertyPanelPreDraw.ts`) title "Select Wall Type" + hint "Choose a type, then click on the canvas to draw" + prominent Apply button made the founder believe an Apply click gated drawing. Fix: panel now (a) **pre-applies** the default type on open via `canonicalWallTool.setSystemTypeId(current ‖ undefined)` so the default is explicitly seeded (idempotent; preserves a mid-session switch), and (b) reframes the copy to "Draw Wall" / "✓ Plain Wall ready — click on canvas to draw. Change type below (optional)." Type dropdown stays apply-on-change for live switching; ESC-cancel, wall-mode, align guide untouched; 3D path unchanged. Test: `apps/editor/__tests__/PlanWallToolDefaultActive.test.ts` (first click arms start point with no Apply / no type; commits Plain Wall thickness 0.2; mid-session switch honoured, never blocks initial draw). C06/C11; no ADR (no tool-activation contract changed). |
| L-29 | founder | **Wall-MOVE preview dimensions (orthogonal to wall vector)** — the wall-DRAW first-point already shows well-defined witness dimensions to the nearest orthogonal walls (great). Want the SAME when a wall is selected on plan and moved: live preview dimensions in the direction **orthogonal** to the wall's direction vector (a wall normally translates perpendicular to itself). | Editing/Move | FIXED | `§FEAT-WALL-MOVE-DIMENSIONS` — new pure `computeWallMoveDimensions` (`packages/core-app-model/src/geometry/wallMoveDimensions.ts`, 14 tests): classifies the moving wall's dominant axis (±22.5° neutral zone, L-26 convention → near-diagonal = no dim), takes the perpendicular, returns the nearest PARALLEL neighbour gap on each side (+/−) that the wall projects onto. `PlanElementDragController._renderWallOverlay` renders them live per drag move via the SAME blue dashed `_drawDimensionLine` the move-delta/hosted-opening dims use; transient overlay (read-only, no store writes P6, no new rAF P3), cleared on drag end/cancel. Descriptor shape kept reusable for L-30. C06/C16, no ADR |
| L-30 | founder | **(follow-up to L-29) Door/Window-MOVE preview dimensions (along wall vector)** — for wall-hosted elements show move dimensions **along** the host wall's vector (hosted elements slide only along the wall). | Editing/Move/Hosting | FIXED | `§FEAT-HOSTED-MOVE-DIMENSIONS` — new pure `computeHostedMoveDimensions` (`packages/core-app-model/src/geometry/hostedMoveDimensions.ts`, 9 tests): works in the host wall's own 1D parameter space (offset in metres from `baseLine[0]`), returns the along-wall gap on each side (`start`/`end`) from the moving opening's near EDGE to the nearest reference — the wall end OR the facing edge of the nearest adjacent opening on the SAME wall, whichever is closer. Edge cases handled: opening hard against a wall end → one-sided; several neighbours → nearest facing edge on each side wins; no neighbours → falls back to wall-start/wall-end (the prior L-behaviour); a neighbour overlapping the moving span is ignored. `PlanElementDragController._renderDoorWindowOverlay` now feeds live `currentOffset` + width + the same-wall doors/windows (`_collectHostedNeighbours` over `getAllDoors`/`getAllWindows`) into the helper and projects each returned 1D offset onto the wall's on-screen line (t = offset/wallLength lerp), reusing the SAME blue dashed `_drawDimensionLine` as L-29's move dims + the pre-existing hosted-drag dims — replacing the old fixed wall-start/wall-end-only readout. Transient overlay (read-only, no store writes P6, no new rAF P3 — reuses the existing overlay schedule), cleared on drag end/cancel. Descriptor shape mirrors L-29's `WallMoveDimension`. C06/C15/C16, no ADR |
| L-31 | founder | **Cross-level REFERENCES (Revit-like, better)** — when drawing walls on an UPPER floor, PRYZM should intelligently offer sound references to the **slab corners** (which are defined by the lower floor's walls) to place the 1st + subsequent points. Also fixes the `SnapBoundsError: SpatialGrid.getCellKeysForBounds total cell count 11039838 exceeds cap` seen when snapping far from origin on L1. | Modeling/Snapping/Refs | FIXED | `§FEAT-SLAB-CORNER-REFS` + `§FIX-SNAP-BOUNDS-OVERFLOW` (ADR-0112). (1) `SlabSnapProvider` extended: emits slab-polygon vertices as ENDPOINT (the corners), edge midpoints as MIDPOINT, edge nearest-points as EDGE; level-gated to the active draw floor via a lazy `getActiveLevelId()` accessor. Wired by default in `SnapManager.createWithDefaults()` — falls back to read-only `window.slabStore` + `window.projectContext.activeLevelId` so WallTool (which forwards only `{gridStore}`) gets the reference untouched; reuses the existing SnapVisualizer indicator + rankCandidates pipeline (composes with L-26 guide/ortho). (2) root cause of the 11039838-cell throw: `SpatialGrid.getCellKeysForBounds` SOLID-FILLED an oversized/degenerate (origin-spanning or geolocated far-from-origin) element AABB, and `insert()` — unlike `query()` — did NOT catch the `SnapBoundsError`, so it propagated out of the wall-draw flow. Fix = degrade oversized fills to the AABB's ≤8 CORNER cells (bounded, snapping stays correct at corners/endpoints, never throws on insert); non-finite bounds still throw and `query()` degrades to `[]`. 3 tests (far-from-origin under cap; slab corners as candidates; draw-point snaps to corner) + level-gate/fallback/EDGE tests. C06/snapping |
| L-32 | founder | **Slab-by-Region PREVIEW mirrored through project origin** — creating a slab via Region: the slab commits in the CORRECT place, but the transient PREVIEW renders far away, mirrored across the project origin (see image). Only slabs. | Modeling/Creation/Slab | BROKEN→FIXED | `§FIX-SLAB-REGION-PREVIEW-MIRROR` (wave 4d) — the By-Region preview built its `THREE.Shape` from world-XZ then rotated `-π/2`, negating Z → mirror through origin; commit path never did. Fix pre-negates Z to cancel the rotation (matches polyline-preview convention). 14/14 tests. C06/C11, no ADR |
| L-33 | founder | **Second parametric kitchen creation fails** — after placing one L-Shape kitchen run, trying to create a SECOND one had an issue (couldn't place / errored). | Modeling/Creation/Kitchen | FIXED (reconciled: §FIX-KITCHEN-SECOND-PLACE, ADR-0113) | root cause = `_placeKitchen()` sent a Euler-OBJECT `rotation` that `CreateFurnitureHandler.canExecute` rejected (`Number.isFinite`) + the tool `deactivate()`d after one place. Fix commits a **scalar** yaw and the tool STAYS ARMED for continuous placement, with `newKitchenRunId()` monotonic ids (`KitchenCabinetTool.ts:46-51,376,402-407`). ADR-0113 (Accepted 2026-07-02) |
| L-34 | founder | **Kitchen placement PREVIEW is a bounding RECTANGLE, not the real L-shape** — the ghost (plan dashed rect + 3D) must PRECISELY match the real parametric kitchen footprint (L / U / galley / single-wall) in BOTH plan and 3D, not a coarse box. | Modeling/Creation/Kitchen | FIXED (reconciled: §FEAT-KITCHEN-ACCURATE-PREVIEW, ADR-0113) | the 3D ghost is now built by the SAME `KitchenCabinetEngine.create(cfg)` that produces the committed run (re-skinned purple), and the plan ghost draws `KitchenPlanSymbolBuilder.buildConfigLinework(cfg)` — the exact placed linework, not the bounding rect (`KitchenCabinetTool.ts:70,188,224`, `FurniturePlanToolHandler`). Preview ≡ placed by construction. Test `packages/geometry-furniture/__tests__/kitchenAccuratePreview.test.ts` |
| L-35 | founder | **Kitchen 2D PLAN SYMBOL must be professional/elegant/modern** — currently a poor rectangle; needs true cabinet runs + sink/hob/fridge glyphs + cabinet-door-swing arcs + work-triangle, per typical-kitchen-layout drafting convention (single-wall/galley/L/U/island — ref image). Accurate to the actual configured run. | Views/Drawing/Kitchen | FIXED (reconciled: §FEAT-KITCHEN-PLAN-SYMBOL, ADR-0113) | `KitchenPlanSymbolBuilder` already drew run outlines + per-unit dividers + sink/hob/fridge/washer glyphs + door-swing arcs + countertop line; the fix ADDS the standard dashed **work-triangle** connecting the sink/hob/fridge glyph centres (transformed into root coords) and inks the placed symbol in single-ink PRYZM purple `#6600FF`. Tests `kitchenPlanSymbolPro.test.ts` + `kitchenAccuratePreview.test.ts` (work-triangle poles). ADR-0113 |
| L-23-RV | founder | **Kitchen Space-rotate re-verify** — founder reports kitchen doesn't rotate on SPACE; `§FIX-PARAMETRIC-SPACE-ROTATE` shipped wave 4c but founder screenshot shows OLD "Press R" copy (pre-fix bundle). Verify live end-to-end for the L-shape kitchen; fix if genuinely broken. | Editing/Rotation/Kitchen | VERIFY | kitchen-fidelity agent verifies; ADR-0107 |
| L-36 | founder | **Shower plumbing fixture faces the WRONG direction** — the wall-mounted shower (riser + rain head + mixer) is oriented incorrectly when placed/hosted (head/arm not facing correctly relative to its wall). | Plumbing/Placement | FIXED | `§FIX-SHOWER-ORIENTATION` (ADR-0114). Root cause: `PlumbingTool` applied `lookAt(point+normal)` (which already aims local +Z along the outward normal) **then** an extra `rotateY(π)` for the shower — driving its +Z front (riser→head→tray→glass, authored at +Z) INTO the wall. Toilet/sink front is at −Z so they need the flip; the shower does not. Fix = drop `'shower'` from the flip, restoring parity with the plan tool (which never flips). Test (a). |
| L-37 | founder | **New composite shower TYPE(s): shower + glass + gutter** — today only a bare shower head/riser exists. Want at least one richer type providing the shower PLUS a base/tray, OR at least a linear GUTTER (channel) drain (ref image 2: rain head + hand-shower + mixer + linear drain), PLUS a side GLASS panel whose direction the user can choose. A parametric walk-in shower enclosure. | Plumbing/Catalogue | FIXED | `§FEAT-SHOWER-ENCLOSURE-TYPE` (ADR-0114). Added composite walk-in built from the existing sub-assembly builders: rain head + hand-shower + round mixer + niche + **linear gutter drain** (metal trough/grate) + low tray + **frameless glass**. User-chosen glass **direction** encoded as the variant slug (`shower_walkin_left`/`_right`/`_corner`) — same type-as-data pattern as sliding/open, no schema change, undoable via `UpdatePlumbingParametersCommand`. Auto-registers in the catalogue/type-picker; 3D + variant-aware plan symbol (gutter line + glass-side line + head glyph, ADR-0110). Tests (b)+(c). |
| L-38 | founder | **URGENT — Site-plan overlay (PDF/image) + Project North / True North.** (1) Upload a PDF/image → it drops onto the floor plan where the user can **move / rotate / scale** it interactively, then **OK** — NO boundary tracing needed; the goal is just a properly located + **geolocated** + scaled underlay. (2) Dual north: the image/PDF defines **PROJECT NORTH** (its orthogonal axis) — PRYZM main **plan view always works on project north** (orthogonal to the underlay); the **3D site / 3D globe works on TRUE NORTH** (the underlay geolocated correctly on the map). | Site/Geo/Views | BROKEN(feature)→PARTIAL | ADR-0115 (wave 4g) `§FEAT-PROJECT-TRUE-NORTH` — dual-north model shipped: pure `projectToTrueNorth`/`trueToProjectNorth` round-trip (rigid rotation about C19 base point, θ=SiteLocation.trueNorth, θ=0 identity), "✓ Use this placement (set Project North)" OK path captures θ via new P6 `dispatchSiteTrueNorth` (no boundary trace), back-compat persistence. **Remaining**: wire the plan-canvas `FloorPlanUnderlayTool` OK→captureUnderlay→dispatch (helpers ready); globe reads θ to rotate massing; durable geolocation schema. 10/10 tests. |
| L-40 | founder | **3D globe / 3D site not reachable from 3D view** — must ALWAYS be available; currently the entry point is missing/disabled | Site/Geo/Views | BROKEN→FIXED | ADR-0115 (wave 4g) — stable `window.pryzmEnterSiteView` registered at boot + always-present floating "◉ 3D Site / Globe" launcher on the 3D viewport + hardened GIS-rail button (activate-then-retry, never dead-ends; enters on default centre with no site). |
| L-41 | founder | **Plan view ignores chosen wall type** — any wall type selected in plan view still commits Plain Wall default | Modeling/Types | FIXED (reconciled: §FIX-PLAN-WALL-TYPE-IGNORED) | `WallPlanToolHandler._commitWall` (the MAIN plan wall tool, wired via `planToolHandlerRegistry` `'wall'`) now resolves the selected `systemTypeId` (`resolveActiveWallSystemTypeId`) AND that type's thickness from the SAME catalogue the picker reads (`window.wallSystemTypeStore.getTotalThickness`), and STORES both — instead of always dispatching `WALL_DEFAULT_THICKNESS` and trusting a handler override that no-oped due to catalogue divergence (`WallPlanToolHandler.ts:319-338,658-665`). Test `apps/editor/__tests__/PlanWallTypeThickness.test.ts` (picked `wt-interior-partition` → stored 0.1 m, not 0.2). NOTE: underlying dual-catalogue arch debt tracked separately as L-50. |
| L-42 | founder | **Navigation not working after project load** — model loads & renders but camera orbit/pan/zoom is dead. Console: `§LOAD-WATCHDOG stuck in "setup" ~16s` (load took 19.6s), runaway `requestAnimationFrame` recursion in the ThatOpen/OBC camera-controls update loop. Likely tied to the load-time render/device state (sibling of L-39/L-25) and/or camera control-lock not cleared. | Camera/Nav/Load | FIXED (reconciled: §WEBGL2-VIEW-UNSTICK + L-108 §FIX-EMPTY-LOAD-HANG) | TWO halves, both closed. (a) camera-nav-DEAD + OBC rAF loop: `§WEBGL2-VIEW-UNSTICK` (`packages/core-app-model/src/BimWorld.ts:150-202`) wraps OBC `Components.update` so a throwing component `update()` (touching a half-restored element after load) no longer escapes BEFORE the `requestAnimationFrame` re-arm — the loop that ticks `camera.controls.update` is ALWAYS re-armed, so the camera stays live instead of freezing forever (the exact "3D view stuck — cannot rotate/move the camera when opening an old project" symptom). (b) the 16s stuck-`setup` load-time: L-108 `§FIX-EMPTY-LOAD-HANG` (`ProjectLoader.ts`). |
| L-43 | founder | **Move/rotate for walls in PLAN view doesn't work** — selecting a wall in plan attaches the gizmo (`[WallTransform] gizmo aligned`) but move/rotate has no effect. Applies to walls (check all element transforms in plan). | Editing/Move/Plan | FIXED (reconciled: §FIX-PLAN-WALL-TRANSFORM, L-43/L-73/L-95) | plan wall transform is owned by `PlanElementDragController` (the Canvas2D surface has no interactive 3D gizmo): body drag → MOVE (both endpoints by grid-snapped delta), endpoint drag → ROTATE/stretch (swing about the other end), both committing via the bus `wall.updateBaseline` (`PlanElementDragController.ts:43,89,165,259,391-408`; attached to BOTH main + SVP canvases). Test `packages/core-app-model/src/views/__tests__/planWallTransform.test.ts` proves both gestures dispatch the moved/swung line (refutes "no effect"). |
| L-44 | founder | **T-junction with 3 DIFFERENT walls — preview perfect, EXECUTION bad** — the live preview shows a clean junction but the committed geometry is wrong (not the degenerate-stub case L-27 fixed; a real 3-wall T). Log shows `§MULTI-CLUSTER-PARTITION-TRIM ... angled arm 0.004m off → square-cap`. | Geometry/Walls | FIXED | §FIX-WALL-JOIN-BASELINE-IMMUTABLE (a2f12335). UNIFIED root w/ L-46/L-47. Root cause: preview + V2 commit render the wall from its AUTHORED `_sourceBaseLine` (clean, band=0.20 m so the 0.004 m-off arm is coincident), but `WallRebuildCoordinator._rebuildWalls` PERSISTED the legacy resolver's trimmed/square-capped baseline back to the store (`store.update({ baseLine:_newBL })`) for any move the destructive-only §POST-RESOLVE-PRESERVE guard let through → the stored baseline diverged from the previewed footprint. Fix: a join re-resolve now anchors every moved, authored-VALID wall back to `_sourceBaseLine` (render-time footprint only, never persists a trim); degenerate stubs still skip (§FIX-WALL-CLUSTER-DEGENERATE intact). ADR-0055 P4. Tests: geometry-wall `junctionResolverV2.tJunctionNearNode` (4) + editor `wallJoinBaselineImmutable` (4). |
| L-45 | audit | **Underlay bugs** — (a) `CommandBusError: no handler registered for DELETE_UNDERLAY` (deleting a floor-plan underlay throws); (b) `QuotaExceededError` persisting the underlay to localStorage (raster stored in localStorage exceeds quota). | Site/Underlay | FIXED | §FIX-UNDERLAY-DELETE-AND-STORAGE. **(a)** Root: `Step6CommitView.handleRemoveUnderlay` dispatches `runtime.bus.executeCommand('DELETE_UNDERLAY', delCmd)` (P6) but no bus handler was registered under the `DELETE_UNDERLAY` CommandType key, so the bus threw BEFORE `DeleteUnderlayCommand.execute()` (which owns the teardown) could run — the delete was lost (CREATE_UNDERLAY had the same latent gap → lost create-undo). Fix: registered CREATE/TRANSFORM/DELETE_UNDERLAY bridge handlers in `initBusHandlers.ts` that forward the pre-built non-semantic Class-A command to the legacy commandManager (mirrors the UPDATE_ANNOTATION bridge) — execute() tears down via `__pryzmRemoveUnderlayInternal`, undo() recreates via `__pryzmRecreateUnderlayInternal` (fully undoable, C06/C11, Contract 01 §2.1 / 04 §3.1). **(b)** Root: `UnderlayPersistence.flushSave` serialised the multi-MB `imageDataUrl` raster into one localStorage key (`pryzm.floorPlanUnderlay.v2.<proj>`) → QuotaExceededError. Fix: new `UnderlayRasterStore` (IndexedDB, per-project, mirrors ThumbnailCacheStore/GeometryCacheStore) holds the raster BYTES; localStorage keeps only the lean metadata pointer (transform/scale/dims/lock/visibility). Restore reads the raster back from IDB; a legacy inline localStorage raster is migrated → IDB and stripped on first restore; clear/remove drops the IDB raster too (project-switch no-op preserved, Contract 48 isolation). No persistence-contract change → no ADR. Tests: editor `underlayDeleteCommand` (3 — registration + delete teardown + undo restore) + `underlayPersistenceStorage` (4 — 300 KB raster→IDB with lean localStorage under a 4 KB budget, legacy migration, clear, metadata-only read). |
| L-46 | founder | **Changing wall TYPE changes the wall's LENGTH** — on `UPDATE_WALL_SYSTEM_TYPE` the wall's baseline endpoints are moved (log: `§DIAG-WALL-SPIKE srcLen=1.531m newLen=1.153m dLen=-0.378m preserve=false bMoved=true`; the `§MULTI-CLUSTER-PARTITION-TRIM` square-caps the arm to "consensus"). A type change must only alter thickness/material, NEVER the baseline/length. | Geometry/Walls/Types | FIXED | §FIX-WALL-JOIN-BASELINE-IMMUTABLE (a2f12335). Same root as L-44/L-47: the type-change whole-level re-resolve trims (thickness changes the trim amount) and the coordinator PERSISTED the trimmed baseline (`preserve=false bMoved=true` in §DIAG-WALL-SPIKE). Fix anchors the wall to its authored `_sourceBaseLine` on the re-resolve, so a type change alters only thickness/material — baseline byte-identical. Regression test: editor `wallJoinBaselineImmutable` "changing a wall TYPE leaves the baseline byte-identical". |
| L-47 | founder | **Creating a wall NEARBY shrinks an UNRELATED, already-joined wall** — two walls cleanly joined in an L; drawing a new wall nearby makes one of the L walls change length. Root = the SAME as L-44/L-46: `WallJoinResolver` re-resolves the cluster on any nearby change and **writes back a moved/trimmed baseline** (`§MULTI-CLUSTER-PARTITION-TRIM` square-cap-to-consensus, `§NEAR-CORNER-L`) onto other walls' stored baselines. **Geometry-integrity violation.** | Geometry/Walls | FIXED | §FIX-WALL-JOIN-BASELINE-IMMUTABLE (a2f12335). THE decisive report that pinned the unified root: a whole-level join re-resolve fires on ANY nearby change and the coordinator wrote a moved/trimmed baseline back onto OTHER walls' stored baselines. Fix enforces the principle "authored baseline is IMMUTABLE except by an explicit user move/edit; joins are render-time footprint operations only" — `WallRebuildCoordinator` anchors every moved authored-valid wall to `_sourceBaseLine` and never persists a join trim (the §CLAMP-COSHARE-WELD hazard `§MULTI-CLUSTER-PARTITION-TRIM` had reintroduced). Explicit user moves are unaffected (UpdateWallBaselineCommand §R5-FIX refreshes `_sourceBaseLine`). Regression test: editor `wallJoinBaselineImmutable` "creating a NEARBY wall does NOT change any existing wall length/baseline". |
| L-48 | founder | **SHIFT multi-select** — holding SHIFT while clicking should ADD elements to the selection, so the user can select several and change a common property at once (multi-select + multi-property edit). | Selection/Editing | GAP(feature) | QUEUED → dispatch after L-43 (both touch SelectionManager); C06/C03; SelectionManager + SelectionBus + properties-panel multi-edit + a multi-target property command |
| L-49 | founder | **Moving a wall in 3D then Undo does NOT undo the move** — the moved wall is not considered by undo (robustness failure). Every element mutation MUST be reliably undoable. | Editing/Undo | FIXED (reconciled: §FIX-WALL-MOVE-UNDO-CAPTURE) | routed → undo-robustness agent; C03 §4.5–4.8; unified `performUndoRedo` (ring-first + cm-fallback) — the gizmo wall-move must be captured in the undo history and restore the baseline — **RECONCILED FIXED (2026-07-05): §FIX-WALL-MOVE-UNDO-CAPTURE.** The 3D-gizmo wall drag-end dispatches `wall.updateBaseline` with `_recordUndo: true` + `prevBaseLine` (`registerTransformDragHandler.ts:160-165`); `UpdateWallBaselineHandler` then records EXACTLY ONE ring-buffer PatchPair whose inverse restores the pre-move baseline (was previously an empty-patch record SKIPPED by the ring buffer, so the ring-first undo reverted the wrong entry). Test `plugins/wall/__tests__/updateBaseline-undo-capture.test.ts` (inverse.value === PREV baseline). Status → **FIXED**. |
| L-50 | audit | **Dual wall-type catalogue (P1 composition-root violation)** — TWO separate `WallSystemTypeStore` catalogues coexist: (#1) the PLUGIN store `plugins/wall/src/system-type-store.ts` (seeded from `BUILTIN_WALL_TYPES`, bound to the authoritative `CreateWallHandler` at `PluginRegistry.ts:170-173`) and (#2) the geometry-wall singleton `packages/geometry-wall/src/WallSystemTypeStore.ts` `wallSystemTypeStore` (exposed as `window.wallSystemTypeStore` at `initBuilders.ts:536`, read by the plan picker + 3D builder). Different built-in sets, different APIs, even a different `wt-monolithic` thickness (0.1 m vs 1.0 m). The `§WALL-TYPE-WIRE` adapter that tries to feed #2 into the handler (`engineLauncher.ts:448`) is a **NO-OP** — the bus Proxy is "first-registration-wins" (`engineLauncher.ts:406-422`) and composeRuntime already registered the #1-bound handler first. `CreateWallHandler.execute` (`plugins/wall/src/handlers/CreateWall.ts:161`) validates/resolves `cmd.systemTypeId` against #1 while the id came from #2 → unknown ids fall to default; user-defined wall types (added to #2) never reach the authoritative handler. L-41's `§FIX-PLAN-WALL-TYPE-IGNORED` masks the thickness symptom (resolves locally before dispatch) but the catalogues still diverge. | Architecture/Types | BROKEN(arch) | P1; C11/C16 — unify to ONE composition-root-seeded store; delete the dead §WALL-TYPE-WIRE re-registration |
| L-51 | audit | **Wall-move undo dispatch asymmetry** — a PLAN-view wall move commits via `window.commandManager.execute(UpdateWallBaselineCommand)` directly (`PlanElementDragController.onEnd`, no `_recordUndo`) → legacy commandManager stack only; a 3D gizmo move commits via `runtime.bus.executeCommand('wall.updateBaseline', {_recordUndo:true})` → unified ring buffer (L-49). `performUndo` is ring-first, so interleaving a plan move and a 3D move can undo them OUT of the user's expected order. | Editing/Undo | GAP | C03 §4.5-4.8 — route the plan-view commit through the same bus path with `_recordUndo` |
| L-52 | audit | **Plan-projection cache ~0% hit-rate under editing (L-06 amplifier)** — `EdgeProjectorService._cwProjectionCache` keys on `group.userData.version`, but `WallFragmentBuilder.buildWall` bumps `_geometrySeq++` UNCONDITIONALLY at entry (`:678`) and stamps it on every rebuilt wall (`:746`). A single wall edit runs a WHOLE-LEVEL rebuild (`WallRebuildCoordinator._flush` calls `buildWall` for every wall on the level to recompute miters) → re-versions EVERY wall → every wall's cached plan projection is invalidated on any one edit (`hitRate≈0%`). Cache only pays off when nothing changed (camera/view toggles). | Perf/Views | GAP | C04 — content-hash the version (skip bump when geometry unchanged) and/or touch only affected walls |
| L-53 | audit | **Concurrent wall baseline move is silent LWW (collab data loss)** — `YjsDocAdapter.applyCommand` writes each command field into a `Y.Map` per elementId key; a wall `baseLine` is just another key → pure Yjs last-timestamp-wins. `emitConflict` fires ONLY for `_detectElevationMismatch` + `_detectCwLevelYMismatch`; `CRDTConflictResolver` (scalar height/thickness) is never called on the baseline write path. Two users moving the same wall → the earlier move is silently discarded, no banner/dialog — violating the stated C08 §3.1/§3.3 "silent LWW forbidden" invariant. | Collab/Robustness | GAP | P8; C08 — surface a baseline conflict (or field-merge) instead of silent LWW |
| L-54 | audit | **Redundant dual junction-resolver compute; legacy trim output now dead** — every wall flush runs BOTH `WallJoinResolver.resolveLevel` (legacy, THREE-coupled) AND `JunctionResolverV2` (via `builder.refreshV2Cache`) over the whole level. Since `§FIX-WALL-JOIN-BASELINE-IMMUTABLE` anchors every authored-valid wall back to `_sourceBaseLine`, the legacy resolver's trimmed-baseline output (`§MULTI-CLUSTER-PARTITION-TRIM` / `§PARTITION-SHELL-INNER-FACE` / `§NEAR-CORNER-L` / `§RESOLVED-STUB-SWEEP`) is DISCARDED for all valid walls — only its miter normals + `invalid` flags are consumed. Full O(n) trim math computed then thrown away, plus a second full junction system. Latent risk: the two resolvers can disagree, and opening/layered walls (legacy MiterPrism path) now render on UNTRIMMED centrelines — opening-wall corner-cap coincidence should be re-verified. | Perf/Tech-debt | GAP | ADR-0055 (A P4a/P4b) — reduce legacy pass to miter-normals-only or retire once V2 covers opening/layered walls |
| L-55 | audit | **Wall command handlers/committer add no own OTel span (P8)** — `CreateWall.ts:18-21` explicitly adds no nested span (relies on the bus `pryzm.command.execute` wrapper); `wall-committer.ts` references a `pryzm.committer.commit` span helper that is not actually wired. Undo/redo (`pryzm.undo/redo`) + sync (`pryzm.sync.applyCommand`) spans do exist. Minor observability gap vs a strict "≥1 span per exported fn". | Tech-debt/Observability | GAP | P8 — add committer/handler spans or codify the bus-wrapper-covers-handlers exception |
| L-56 | audit | **Plan door/window hosted on a SLAB id → orphaned opening** — `[DoorBuilder] Wall not found (wallId=slab_…)`; opening never anchors (violates C15). | Creation/Hosting | BROKEN→FIXED | `§FIX-DOOR-SLAB-HOST` (wave 4h) — host was resolved via `PlanViewCanvas.hitTest` (nearest PROJECTED line of ANY type; a slab edge could win) passed straight through as `wallId`; guard only rejected curved walls. Fix: accept hit only if it resolves to a wall in `wallStore`, else nearest-WALL-only fallback, else reject (`if(!targetWall)return`). Both plan overlays covered; wall untouched. Test `DoorHostResolution.slab.spec.ts` (5). C11/C15 |
| L-57 | impl | **Uniform material-set command** `§FEAT-UNIFORM-MATERIAL-COMMAND` (ADR-0117) — 12 new per-family `<family>.setMaterial` bus handlers (undoable via `produceCommand`, one OTel span each via `withHandlerSpan`), registered in each plugin's `handlers/index.ts`; new UI facade `apps/editor/src/ui/property-inspector/MaterialDispatch.ts` maps elementType→family command and is dispatched once from `PropertyInspectorApply.applyChanges`. Material fields (`materialId`/`materialColor`) were already on each store DTO + honoured by the committer `MATERIAL_FIELDS` dirty-check, so persistence/render needed no change. Tests: slab/column/furniture set+undo + facade routing. | Editing/Materials | FIXED | Q7 / F6 / ADR-0117 |
| L-58 | founder | **"Overlay a plan / PDF" onboarding flow doesn't visibly work** — the Site-plan overlay PANEL + controls render (Move/Rotate/Scale/2-pt calibrate/Opacity/Lock/Hide/Remove/"Use this placement (set Project North)") but the founder can't see the uploaded image/PDF overlaid on the satellite map, nor complete the flow (Step 2 → trace plot). Extends L-38 (PARTIAL) + the L-45 finding that the raster was stored INLINE in localStorage (QuotaExceededError → save/load fails → never paints). Founder-specified UX: choose file → image appears on map (auto-place) → move/rotate/scale to fit → "Use this placement" → next step. | Site/Geo/Onboarding | BROKEN(feature)→FIXED | `§FIX-SITE-OVERLAY-RENDER-AND-FLOW` (ADR-0115 extended; C18/C19). **Placement = AUTO-place** (on file-select the raster drops centred on the current map view at the default 50 m span — no click ceremony). **THE render root cause (non-storage):** the Map↔Satellite basemap toggle calls `map.setStyle({diff:false})`, which tears down every custom source/layer; `SiteBoundaryMap2D` re-added only the boundary ring in `style.load`, NOT the site overlay — so the raster VANISHED, most visibly on the satellite basemap the founder was viewing. Fix: `SitePlanOverlayLayer` now SELF-HEALS on `style.load` (idempotent re-install, opacity/visibility preserved). Also fixed: origin fell back to lat/lon `(0,0)` (Gulf of Guinea, off-view) when no location geocoded → now the map's current centre; and a fresh upload eases the map to the plan's bounds so "choose file → image appears" is reliably true. **Storage (the L-45 quota root):** new `SiteOverlayRasterStore` (IndexedDB, per-project, mirrors L-45 `UnderlayRasterStore`) holds the raster BYTES; localStorage keeps only lean metadata (`writePersistedOverlay` strips the raster, `readPersistedOverlayMetadata` tolerates its absence, a legacy inline record migrates to IDB on first restore) → no more QuotaExceededError, so persist/reload/repaint works. **Transforms** (move/±size/⟲⟳5°/opacity/2-pt calibrate/lock/hide/remove) act live on the visible raster (unchanged, now visible). **Flow completion:** "✓ Use this placement" still `dispatchSiteTrueNorth(θ)` (ADR-0115) AND now emits the typed `site.overlay-placement-committed` runtime event; the onboarding wizard listens and advances Step 2 → the plot/confirm step (L-38: a geolocated + calibrated plan is a valid located plot — no mandatory boundary trace to proceed). **PDF:** already wired (pdf.js rasteriser + page nav) and rides the same overlay path — unchanged, works. Tests: `sitePlanOverlayLayer` (5 — self-heal across a style wipe, opacity/visibility preserved, dispose stops healing), `sitePlanOverlayStorage` (5 — lean localStorage under a 4 KB budget, metadata round-trip, legacy-inline detection, invalid reject), existing `sitePlanOverlay` (24) green. Moves L-38 PARTIAL → the overlay half is now FIXED (plan-canvas gizmo bridge + globe θ still per ADR-0115 §Remaining). |
| L-59 | founder | **SSGI causes constant flickering in WebGPU + TRAA black-flash on selection** — (1) with WebGPU active, ALL elements FLICKER constantly; historically this was SSGI being on (SSGINode). Make **SSGI OFF by default** (keep the toggle). (2) TRAA: clicking an element used to make rendered elements vanish; now they go BLACK for ~1s then return with no change — the TRAA colour-filter rebuild on selection flashes black. | Camera/Render/Post-FX | FIXED | `§FIX-SSGI-DEFAULT-OFF-TRAA-SELECT-FLASH` (C04 / ADR-0111). **Root cause:** the `cinematic` + `balanced` tiers in `SceneQualityTierManager` set `ssgi:true, traa:true`, and `initScene` unconditionally called `renderPipelineManager.activateSSGI()` at startup — so on WebGPU, SSGINode's per-frame denoise temporal accumulation flickered every element (issue 1). The tier's TRAA hook (`RenderingPipelineCoordinator._onTierTraa → rpm.activateTRAA/deactivateTRAA`) rebuilds the pipeline (`_rebuildPipelineWithCurrentState` → new WebGPU `RenderPipeline` → shader PSO recompile ≈ ~1s BLACK frame) on ANY tier transition; scene/selection-driven `applyTierForMeshCount` re-evaluations therefore flashed the viewport black on select (issue 2). **Fix:** SSGI + TRAA now default OFF at every tier (cinematic/balanced set `ssgi:false, traa:false`, matching performance/survival), the startup `activateSSGI()` call is removed (outlines still activate → selection highlight intact), and the RenderRail SSGI toggle defaults OFF — both remain fully user-opt-in via `rpm.activateSSGI/activateTRAA`. Because the tier no longer drives SSGI/TRAA on, no tier/selection-driven rebuild-to-black; `setSelectedObjects()` only mutates the live outline arrays (never rebuilds). Device-loss/backend-swap recovery (`recoverPipeline`/`tryUpgradePostFx`/legacy fallback) now restores SSGI only if the user had it enabled (gated on `_ssgiActive`/`status.ssgiActive`) so the flicker can't silently return after a swap. **Shadow fixes intact:** only the `ssgi`/`traa` booleans changed — `shadows`, `shadowLevel`, the `§FIX-SHADOW-MIDSUBMIT-DESTROY`/`§FIX-SHADOW-LOAD-TIER-DESTROY`/`setShadowReallocFrozen`/`setShadowPassSuppressed` paths (ADR-0111) untouched; WebGL path unaffected. Tests: `SceneQualityTierManager.test.ts` (cinematic/balanced SSGI+TRAA OFF on WebGPU) + new `RenderPipelineManager.selectionNoRebuild.test.ts` (selection triggers NO pipeline rebuild). |
| L-60 | founder | **UI buttons overlap** — (1) the Split-View toggle (bottom-left, near GPU: Auto/WebGPU/WebGL + Living Graph/Graph) overlaps — move the 3 up a little; (2) the **Site / Globe** launcher (top-left) overlaps the PRYZM logo/menu. | UI/Layout | BROKEN→FIXED | §FIX-UI-BUTTON-OVERLAP. (1) The split-view toggle sat at bottom:24px (32px tall → 24-56px), clashing with the GPU renderer pill (bottom:10px, ~21px → 10-31px); the 33px gap up to `⚛ Graph` (bottom:64px) is too small for the button, so it moved to the TOP of the shared left column at bottom:144px — final bottom-to-top stack GPU(10)·⚛Graph(64)·✦Living Graph(104)·Split View(144), consistent 40px rhythm, nothing overlapping (`apps/editor/src/engine/initUI.ts`). (2) The Site/Globe launcher moved from top:12/left:12 (over the 198px `.plat-left-panel` PRYZM header + top-centre toolbar band) to top:56px/left:220px — right of the fixed 198px+11px left panel and below the ~42px top toolbar band, clear of the logo/menu at every viewport width (`apps/editor/src/ui/layout/GISAreaLayout.ts`). Pure CSS/anchor reposition; no logic/handlers touched. Site/Globe launcher was added by L-40 |
| L-61 | founder | **L-corner + 3rd-wall-T STILL not clean** — two walls joined in an L (shared corner); a THIRD wall joins to form a T → (img1) a triangular SPIKE/tongue at the junction + (img2) DOUBLED/overlapping parallel lines on the horizontal wall inside the junction. Builds on L-27 (`§FIX-WALL-TJUNCTION-BUTT-2` / `§FIX-WALL-CLUSTER-DEGENERATE`) + L-44/46/47 (`§FIX-WALL-JOIN-BASELINE-IMMUTABLE`), which are intact. | Geometry/Walls | BROKEN→FIXED | `§FIX-WALL-LCORNER-T-CLEAN` (JunctionResolverV2, ADR-0055 P1). **Root cause (spike + doubled edge are ONE bug):** when the 3rd wall C tees onto arm A's BODY a little before the corner, C's terminating endpoint lands INSIDE the 0.20 m §RESI-L0 cluster band of the L-corner node, so `clusterEndpoints` FUSES C's endpoint into the A+B corner cluster → the cluster now has THREE real endpoints {A.end, B.start, C.start}. `§FIX-WALL-TJUNCTION-BUTT` then sees C teeing interior on A and reclassifies **A → passthrough** — but A is ALSO the L-arm co-terminating with B, so making it a passthrough DISSOLVES the A–B mitre: A square-caps straight THROUGH the corner (its end face pokes past x=corner = the **spike**) and A's outer face then OVERLAPS B's body in the junction (two coincident parallel lines on the horizontal wall = the **doubled edge**). Reproduced in a footprint harness (`resolveJunctions`→`buildWallFootprint`): tee at x=4.85 (0.15 m before the (5,0) corner) → A collapses to a 4-gon square cap at x=5 (L-mitre gone), C emits a degenerate 5-gon with a repeated vertex. **Fix:** BEFORE the in-place T-butt reclassification, a cluster-SPLIT pass — in a ≥3-real-endpoint cluster, detect each tee-attacher (a member whose endpoint projects strictly interior on another member's body, on-face, continuing past that member's own end by ≥ its half-thickness, not collinear — the exact §FIX-WALL-TJUNCTION-BUTT invariant); if removing the tee-attacher(s) STILL leaves ≥2 real endpoints (a genuine surviving corner), EXTRACT each tee-attacher into its OWN T-junction on its host body (real end = C, passthrough = A, pivot = C's foot) and remove it from the corner cluster. A+B then resolve as the clean L they are (byte-identical to the L with no third wall — verified), and C butts flat on A's near face (a clean 4-gon, no pivot, no tongue). When removing the tee-attachers leaves <2 real ends (the simple near-end T of L-27, no corner) the split is a no-op and the existing reclassification path is byte-unchanged. Genuine 3-way Y (C at the corner vertex exactly) is untouched — the co-terminating fan tiles cleanly. DETECTION-FRAME ONLY: splits a cluster + points the new junction at the host-body foot; NEVER relocates a centreline baseline (no §CLAMP-COSHARE-WELD / ADR-0072 P3c-b doubling regression). Gated via the existing `__pryzmWallV2TJunctionButt` escape hatch. Tests: geometry-wall `junctionResolverV2.lCornerT` (4 — near-corner T keeps the A–B mitre + C flat butt + no spike/no doubling; exact-vertex Y clean; far T unchanged; L-27 near-end T byte-unchanged). 129 geometry-wall + 107 ai-host wall/junction tests green. C15/ADR-0055. |
| L-62 | founder | **Layered wall type not honored in PLAN view** — after choosing a layered system type (e.g. "Interior – Partition 100 mm", a LAYERED wall) the 3D creation is CORRECT (proper layered wall), but in plan view the wall renders as the PLAIN default single-volume wall at the right 100 mm thickness — the layered composition (layer boundary lines / core+finish) is missing. systemTypeId is dispatched correctly (log: `systemTypeId=wt-interior-partition thickness=0.2`), so the loss is in the plan projection path, not the store. | Geometry/Walls | BROKEN→FIXED (§FIX-PLAN-LAYERED-WALL-SYMBOL, f0b1e028 — layered wall now shows layer lines in plan) | routed → SOLE WALL AGENT (queued behind L-61); plan-view projection (WallFootprint2D → EdgeProjectorService/NativeElementMeshExporter) must emit the LAYERED footprint (two render paths — see mem wall-opening-seam-two-paths), not the plain single-volume. ADR-0055; must map to C15/plan-projection |
| L-63 | founder | **BLOCKER — moving a wall that HOSTS a door FREEZES the app** (distinct from ADR-0099's quadratic-scan freeze). Prod log: `[WallJoinResolver] §MULTI-CLUSTER-PARTITION-TRIM … angled arm 0.313m off junction → square-cap to consensus` → `[RoomDetectionEngine] §DIAG-PARTITION-REACH … closed a 307mm dangling gap` → `§DIAG-ROOM-LOOP BREAK … endpoint 307mm from centreline EXCEEDS hostSnap 200mm → loop will NOT close`. The non-closing loop re-armed a whole-level room redetect every frame → main-thread peg → hard freeze. | Geometry/Walls + Rooms | BROKEN→FIXED (loop guard); geom Part-1 deferred | `§FIX-ROOMREDETECT-NOPROGRESS-GUARD` (ADR-0117). **Root cause:** the join resolver square-caps the moved arm to a cluster consensus > hostSnap (200 mm) from the host body → a dangling gap the room loop can't close; the resulting `bim-wall-mutation-committed` re-armed a whole-level `RoomDetection` every frame. **Fix (definitive — the coordinator's explicit safety net):** room re-detection is a pure function of the level's wall geometry, and a join never mutates a stored baseline (§FIX-WALL-JOIN-BASELINE-IMMUTABLE), so the STORE geometry is stable across the loop. `RoomTopologyObserver` now bounds rescheduling on a cheap per-level wall signature (id + baseline @ mm + thickness): (1) a **committed-path no-progress gate** skips arming the soft-coalesce redetect when the signature is unchanged since the last completed redetect (the loop breaks after exactly ONE redetect); (2) an **execution circuit-breaker** trips if the same signature reaches `_executeRedetect` > 6× within 1 s (a per-frame runaway from the forced-fire / coalesce paths). A genuine wall edit changes the signature → both release immediately; non-wall (slab/column/CW/bounding) triggers are unaffected; the drag-defer (§FIX-WALLMOVE-REDETECT-DEFER) + ADR-0099 coalesce are untouched. **Part 1 (resolver trim) deferred, transparently:** the same `§MULTI-CLUSTER-PARTITION-TRIM` consensus cap is *desired* in a sibling case (`WallJoinResolver._repro_passthrough` — a 290 mm partition MUST be pulled onto a committed-corner consensus ON the host body so the loop closes). The discriminator ("consensus on the host body" vs "consensus off a *different* host the room needs") is not derivable from cluster info (the relevant host is chosen at detection time, downstream), so a `dConsArm`/consensus heuristic either regresses the 290 mm test or is a no-op on the repro while risking silent regressions in untested generated-house room-sealing. Per the whole-architecture mandate a speculative trim change was NOT shipped; a precise Part-1 fix needs the exact failing project geometry (follow-up). Tests: `packages/room-topology/src/__tests__/roomRedetectNoProgressGuard.test.ts` (4 — bounded/converging redetect + inert-harness). 46 room-topology + 129 geometry-wall tests green. C11 / ADR-0117; builds on ADR-0099 / ADR-0098 F3. |
| L-64 | founder | **Black flash on polyline wall CLOSE** — during polyline wall creation, the instant the user presses Enter (just before the loop closes) the walls flash BLACK for a microsecond. Log shows `Destroyed texture [Texture "ShadowDepthTexture"] used in a submit.` immediately after `[PascalSceneLighting] Shadow flags set on 40 mesh(es)` — the shadow map is destroyed/reallocated MID-SUBMIT as the new walls enter the scene (SceneQualityTier re-tier on element-count change). Same device-loss class as ADR-0111. | Rendering/WebGPU | BROKEN→FIXED (§FIX-SHADOW-WALLCOMMIT-DESTROY, 21142827 — no black flash on wall close) | routed → RENDER LANE (extends L-59 agent, ADR-0111 family). Ref-count/defer the shadow-map realloc across the wall-commit frame like §FIX-SHADOW-MIDSUBMIT-DESTROY / setShadowReallocFrozen; must NOT regress L-39/L-59 shadow fixes |
| L-65 | founder | **Plan-view element creation lags ~0.5 s (should be instant)** — when creating an element (e.g. a wall) the 3D preview + creation is instant, but the same element takes ~half a second to appear in PLAN view. Log shows every single element add re-runs the WHOLE plan projection: `NativeElementMeshExporter` re-exports ALL elements (11→23…), `EdgeProjectorService.project()` re-projects every group, plus `HiddenLineRemoval` + `DoorPlanSymbolBuilder`/`WindowPlanSymbolBuilder` + `RoomTagAutoPopulator` + `VGSceneApplicator`. NME cache hit-rate decays 80%→44% as the model grows (each add invalidates+reprojects the whole view). 3D is O(1) (one mesh appended); plan is O(N) per add. | Perf/Plan-projection | SLOW→FIXED | `§FIX-PLAN-PROJECT-INCREMENTAL` (C04 §3.3 / DOC-1.4 Re-projection; P8 span `pryzm.plan-view.project-elements-incremental`). **Root cause (two compounding factors, both in the generic projection/dirty layer — NOT wall geometry):** (1) On a `create`, `ViewDependencyTracker` forced the coarse whole-drawing `invalidate(viewId)` (full dispose) rather than the element-scoped `invalidateElement`, so the cached drawing was thrown away and rebuilt from ALL N elements. (2) The `invalidateElement` warm-drawing machinery (from the L-06/wall-move work) was never actually leveraged for perf, because **no incremental-append projector was ever wired** (the docs' "Day 2 `EdgeProjectorService.projectElement`" was TODO) — so BOTH re-projection drivers (`PlanViewManager._onProjectionStale`, the 30 ms active-canvas path, AND `ViewDependencyTracker._flush`→`initScene.onReprojectionNeeded`, the 300 ms DOC-1.4 path) called the full `project()`, which re-iterates every element, re-runs every whole-view symbol builder (`Door`/`WindowPlanSymbolBuilder`, stair/roof/column) + `RoomTagAutoPopulator` + `VGSceneApplicator` + `HiddenLineRemoval`, and (per add) churns the bounded NME LRU cache (the 80%→44% decay). The active view was in fact re-projected in FULL **twice** per add (both drivers target it). **Fix — a true incremental graft:** (a) `ViewDependencyTracker` now keeps the drawing WARM (element-scoped `invalidateElement`) for a `create`/`update` of a **pure-projection** element type (`PLAN_INCREMENTAL_SAFE_TYPES` = wall/slab/beam/ceiling/floor — the types whose entire plan representation IS their own UUID-tagged base projection, no separate symbol pass), flags the view GRAFT-ELIGIBLE, and hands the dirtied element ids to the driver via a new 3rd `graftElementIds` callback arg; a view also touched by a non-pure-projection change (e.g. a door update) is demoted to full re-projection (`_viewsGraftIneligible`). (b) New `EdgeProjectorService.projectElementsInto(targetDrawing, viewDef, dirtyGroups, dirtyIds)` projects ONLY the dirty groups (via the SAME `project()` path → pixel-identical, no divergent code) into a throwaway drawing, then **idempotently grafts** only the lines tagged with a dirty element's UUID onto the already-warm cached drawing (clone-in; remove-then-add so the 30 ms + 300 ms drivers cannot duplicate linework). (c) Both drivers take the graft fast-path when eligible + a warm drawing exists + native-only plan view, and **self-heal to a full reprojection on any error** (fallback = prior behaviour, zero-regression by construction). A single wall/slab add is now O(dirty) — only the new element re-projects; every other element re-uses its cached projection (no whole-drawing dispose, no full symbol/HLR re-run, no double full-projection). Doors/windows/furniture creates (symbol-bearing) intentionally keep the full path for correctness (documented follow-up: per-element symbol inject to extend the graft). Builds on L-52 §FIX-WALL-VERSION-CONTENT-HASH + L-06 per-move storm. Tests: `wallMovePlanReprojectionDefer` (6 — create-safe-type→element-scoped, create-non-safe→coarse, delete→coarse, single create passes ONLY that id as the graft set, mixed non-safe change → graftIds undefined; wall-move deferral unchanged). Files: `ViewDependencyTracker.ts`, `EdgeProjectorService.ts`, `PlanViewManager.ts`, `initScene.ts`. Map to C04/DOC-1.4; P8 span added. |
| L-66 | founder | **Plan-view wall tool needs an extra trigger to arm (3D is immediate)** — pressing `WA` (or picking the wall tool) in the 3D scene arms drawing immediately; in PLAN view the "Draw Wall" panel shows "Plain Wall ready — click on canvas to draw" but the tool does NOT actually arm until the user clicks **Apply** on the wall-type panel. Parity break: plan must arm on tool-activate exactly like 3D, with the type panel being optional (change-type only), never a gate. | Creation/Plan-tools UX | BROKEN→FIXED (§FIX-PLAN-WALLTOOL-ARM-ON-ACTIVATE, f968cc6d — plan wall tool arms on activate) | routed → PLAN-TOOLS LANE (own agent, NOT sole-wall geometry — scope to `WallPlanToolHandler` arming + `SvpPlanToolOverlay` activation + the wall-type panel "Apply" gate; MUST NOT edit wall geometry/join files owned by sole-wall lane). Arm the plan handler on activate with the current/default systemType; make Apply optional. Map to C11 element-creation pipeline; add P8 span. Sibling of L-41/L-50 (plan wall-type wiring) |
| L-67 | founder | **REGRESSION: catalog preview thumbnails lost — replaced by generic line-art icons** — the object/furniture library panels (e.g. INTERIORS → Tables Library, image) previously showed rich PREVIEW IMAGES per catalogue item; now every card shows the same terrible generic purple parametric line-art icon (square-with-corner-marks) instead of the real thumbnail. A recent change swapped the image-thumbnail render path for the parametric-icon fallback. Revert to the previous image previews. | UI/Catalog panels | REGRESSION→FIXED (§FIX-CATALOG-THUMBNAIL-RESTORE, e7aa1ec6 — catalog preview images restored) | routed → UI/CATALOG LANE (own agent, NOT sole-wall / NOT render). Find the regressing change in the catalogue card thumbnail render (FloatingObjectCarousel / object-library panel / AssetCatalog thumbnail resolver) and restore the image-preview path (fall back to the parametric icon only when a thumbnail is genuinely absent). Check vs the GLB-404 (mem furniture-glb-404-object-storage) — thumbnails must not depend on the un-hosted GLBs. Map to the relevant UI contract; keep the icon as fallback-only |
| L-68 | founder | **Furniture type-swap: wrong candidate list + broken undo (TWO bugs)** — (1) selecting a BED and opening the properties-panel TYPE dropdown lists the WRONG element types (image: FU-FF-002 offers Dresser / Round Mirror / Rectangular Mirror — no bed variants); the CHANGE_FURNITURE_TYPE candidate resolver isn't filtering to the same furniture sub-family/category. (2) **MASSIVE:** after swapping to another type, UNDO does NOT restore the original bed. Log: `[elementUndoStoreAdapter] skip remove — not found in store: furniture_01KWKNVX…` then `[Undo] ring-buffer applied … shadow-dropped cm entries:1` — the type-swap mints a NEW furniture id instead of mutating in place, so the inverse patch references an id that never existed pre-swap → undo removes the new item but cannot re-add the original. | Editing/Type-swap + Undo | BROKEN (2)→FIXED | `§FIX-FURNITURE-TYPE-LIST-AND-UNDO` (C16 command authoring; C03 §4.5–4.8 undo; ADR-0105). **(1) Wrong dropdown list — root cause: two divergent furniture taxonomies.** The property-panel "Type" dropdown reads its peer items from the carousel `FurnitureCategoryRegistry` (`getItemsForCategory`), but resolved the element's category from the SEPARATE `FURNITURE_TYPE_TO_CATEGORY` map (geometry-furniture) / the stored `furnitureCategory`. That map classifies `bed → 'bedroom'`, but the registry stocks the `bedroom` category with **dresser + round/rect mirror** and places the `bed` TYPE in the `beds` category — so a bed offered Dresser / Round Mirror / Rectangular Mirror (the founder's screenshot). **Fix:** new authoritative reverse index `getCategoryForType()` on the registry (type→the category that actually CONTAINS it); the widget resolves the category via it FIRST (bed→`beds`), so the peer list is always the element's own family and the current type is guaranteed present. Also de-duplicated options by `type` (the `sofas` category stocks 3 colour cards sharing `sofa_3seat`, which broke single-select pre-selection) and set `select.value` explicitly so the dropdown opens on the element's current type (a no-change Apply is a correct no-op). **(2) Broken undo — root cause: the swap owned no ring-buffer entry.** `ChangeFurnitureTypeCommand` already mutates IN PLACE with a stable id (ADR-0105) and rebuilds the mesh via `FurnitureStore.update → bim-furniture-updated`, but it runs through the legacy CommandManager, which records NO ring-buffer patch. The unified undo (`performUndoRedo`) is RING-BUFFER-FIRST, so with no swap entry on the ring the ring's TOP was the element's earlier CREATE — Ctrl+Z popped the CREATE (deleted the bed) and shadow-dropped the cm swap twin, so the original type was never restored (founder log: `skip remove — not found: furniture_01KWKNVX…` / `ring-buffer applied … shadow-dropped cm entries:1`; the older prod path minted a new id, same net failure). **Fix:** the `element.changeType` furniture branch now snapshots the record before/after and pushes an invertible **whole-element replace** `PatchPair` (stable id: forward `replace[id]=newData`, inverse `replace[id]=oldData`, `affectedStores:['furniture']`) onto the ring buffer. The ring-first undo now reverses THE SWAP — `applyRingBufferSide` → `elementUndoStoreAdapter` → `furnitureStore.update(id, oldData)` → mesh rebuild → the original bed type is restored on the same element (never a phantom-id remove); redo re-applies. The undo engine itself is unchanged. Tests: `FurnitureTypeSelectorWidget` (+2 — a bed lists bed-family peers, NOT dresser/mirrors, even with the mismatched stored `'bedroom'` category / with no stored category), `furnitureTypeSwapUndo` (2 — swap→undo restores the original type in place with no "skip remove", redo re-applies; a further undo then reverses the create), `changeFurnitureType` (6) green. |
| L-69 | founder | **(follow-up to L-58) 2-pt calibration dead + PDF/image import forces generate** — "the 2 point calibration doesn't work — even selecting it still goes default to generate the footprint/boundary of the building — when doing PDF/image import we ONLY want to place the image accurately and go to PRYZM canvas to start working." | Site/Geo/Onboarding | BROKEN→FIXED | `§FIX-SITE-OVERLAY-CALIBRATION-EXCLUSIVE` + `§FIX-SITE-OVERLAY-IMPORT-TERMINAL` (ADR-0115; C18/C19/C06). **(1) Calibration dead — TWO root causes.** (a) The overlay map-click listener was registered `map.on('click', onMapClick)` but read the MapMouseEvent AS the lng/lat (`e.lng`/`e.lat`, undefined) instead of `e.lngLat` → every calibration point resolved to NaN → null → no point captured (silent). Fixed to read `e.lngLat`. (b) The boundary DRAW tool also listens on the map; in rectangle/circle mode its two clicks COMMITTED a parcel boundary (→ `dispatchParcelBoundary` → forced the generate flow) before calibration could use them. Fix: the controller exposes `isCalibrating()` and `SiteBoundaryMap2D.onClick` now YIELDS (early-returns, adds no vertex / no commit) whenever calibration is active, so both clicks land as scale points a & b; the overlay's own listener still records them. Works whether or not the onboarding draw step is active. **(2) Decouple import from boundary/generate — terminal path.** Previously (`§FIX-SITE-OVERLAY-RENDER-AND-FLOW`) "✓ Use this placement" routed into `fallbackDefaultRectToConfirm` → a default plot + the generate-confirm step (exactly the coupling the founder rejected). Now the overlay commit is TERMINAL: it still `dispatchSiteTrueNorth(θ)` (ADR-0115) and keeps the calibrated plan durably persisted per-project, then DISPOSES the wizard leaving the Site + placed plan intact — **NO parcel-boundary trace, NO auto-generate**. Boundary-trace / generate stay SEPARATE explicit choices (the other onboarding branches, untouched). Tests: `sitePlanOverlayCalibration` (4 — restore+render, isCalibrating toggles between the two clicks, both clicks captured via e.lngLat + a real scale applied + calibration ends, no accidental calibration when idle); existing overlay suites (34) green. Remaining (ADR-0115 §Remaining #1): surfacing the placed plan as a live underlay INSIDE the 3D/plan editor canvas is the plan-canvas gizmo bridge — today the plan is durably persisted + visible on the site map. |
| L-70 | founder | **(with L-71) PDF/image import path still armed the draw tool + generate prompt** — the overlay branch was labelled "overlay a plan/PDF THEN DRAW", armed `pryzmStartBoundaryDraw()`, and (via the draw watchdog) could still surface "Generate your house with AI?". For the import use case none of that should appear — "we won't generate the house, we just want to go straight to the canvas." | Site/Geo/Onboarding | BROKEN→FIXED | `§FIX-SITE-OVERLAY-IMPORT-TERMINAL` (ADR-0115; C06). The overlay branch now runs a DEDICATED `startOverlayImport()` (not the shared `startDrawThenGenerate`): it opens the 2D map in OVERLAY-ONLY mode via a new `window.pryzmStartSitePlanOverlayImport` → `mountSiteBoundaryMap2D({overlayOnly:true})`, which DISARMS the boundary draw tool (`onClick` early-returns → no vertices, no commit) and hides the draw mode-strip + "click two corners" instruction. No draw watchdog, no `site.parcel-boundary-set` subscription, no generate-confirm step. "✓ Finish" is the sole terminal action → creates the L-71 canvas underlay, disposes the wizard, closes the map, exits GIS (lands in the editor). The choice card is re-labelled (drops "then trace the boundary"). Default-footprint + draw-plot branches untouched. Tests: `onboardingOverlayImportBranch` (3 — re-label, arms overlay-only NOT boundary-draw, Finish lands in canvas with no generate prompt). |
| L-71 | founder | **THE GOAL — plan on the PRYZM canvas, geolocated + real-size + on PROJECT NORTH (orthogonal in plan view)** — "once the pdf/jpg is layered correctly... I want to go to pryzm canvas with the pdf/image properly geolocated — correct location and correct size — positioned with the edges on PROJECT NORTH (NOT true north) so it shows ORTHOGONALLY on plan view — this helps the user draw walls. That is the goal." | Site/Geo/Canvas/Underlay | GAP(feature)→FIXED | `§FEAT-SITE-OVERLAY-PLAN-UNDERLAY` (ADR-0115 §Remaining #1 — DONE; C18/C19). Pressing "✓ Finish" INSTANTIATES the calibrated plan as a live underlay INSIDE the editor canvas (plan + 3D), REUSING the existing floor-plan underlay pipeline (`FloorPlanUnderlayTool` + `CreateUnderlayCommand` via `runtime.bus` → the L-45 `CREATE_UNDERLAY` bridge — P6, undoable). Placement = pure `computePlanUnderlayPlacement(transform)` (projectTrueNorth.ts): **size** `pxPerMeter=1/mpp` (2-pt calibration); **rotation `0` = AXIS-ALIGNED** in plan view (the map shows the plan at true north; the canvas removes that rotation → project north → orthogonal for wall tracing — `overlayInProjectFrame` applied to the renderer, the dual-north point); **location** = overlay centre re-expressed in the project frame (`trueToProjectNorth` about the site origin). Engine bridge `apps/editor/src/engine/createSiteOverlayUnderlay.ts` (`createPlanCanvasUnderlayFromSiteOverlay`, 1 OTel span P8; P2-clean — mutates the mesh via the tool's state handle, no THREE import). Wired: controller `onEnterCanvas` → SiteBoundaryMap2D → engine helper; θ still lands on `SiteLocation.trueNorth` (globe). Tests: `sitePlanUnderlayPlacement` (5 — off-axis 30° map plan → rotationZ 0 axis-aligned, scale=1/mpp, project-frame centre, θ=0 identity, degenerate-mpp guard) + `sitePlanOverlayCalibration` (+2 — Finish calls onEnterCanvas with axis-aligned params; ordering before placement-committed). Closes ADR-0115 §Remaining #1; L-38 fully satisfied for the map-overlay path. |
| L-72 | founder | **[CRITICAL, SYSTEMIC] Undo does not capture element MOVE/ROTATE/property-change in 3D (any element)** — after moving an element in 3D and pressing Undo, the move is NOT reverted — the undo doesn't pick it up. Founder: must be SUPER robust for EVERY element after it is moved, rotated, or has properties changed — no matter where/how, never miss one. Same class as L-49 (3D wall-move undo was uncaptured because the transform command declared `affectedStores:[]` → empty patch → never pushed to the ring buffer) — but the founder reports it still happening for other elements/gestures, so the fix was point-not-systemic. | Editing/Undo robustness | BROKEN (systemic)→FIXED | `§FIX-UNDO-CAPTURE-SYSTEMIC` (a8bd98c9; C03 §4.5–4.8 ring-first undo, C16 command authoring). **Audit of the 3D transform-gizmo drag-commit path (`registerTransformDragHandler.ts` — the actual bug surface: it dispatches ONE bus command per element type on drag-end):** the empty-patch (silently non-undoable) set was **furniture** (`furniture.updateParameters` plugin handler declared `affectedStores:[]` + `{forward:[],inverse:[]}`), **column** (`column.update`), **beam** (`beam.update`), **floor** (`floor.update`) — the last three are `initBusHandlers` commandManager BRIDGES with `stores:[]`. Each was the L-49 bug unfixed: EMPTY-PATCH record → CommandBus SKIPS the ring buffer → ring-first `performUndo()` reverts some OTHER covered element and the moved one "stays moved". **Already-undoable (verified, no change):** wall (L-49), roof/ceiling/curtain-wall (`Update*Handler` produceCommand), stair (`stair.move`), plumbing (`plumbing.move`), slab/structural (`*.move`) — all emit real forward/inverse patches via produceCommand plugin handlers that win at compose over the bridges. door/window use the hosted-drag path (separate); handrail 3D-move is intentionally unsupported (path-geometry — snaps back). **Fix (mirrors §FIX-WALL-MOVE-UNDO-CAPTURE):** the four commands now declare their element store in `affectedStores` AND emit a forward/inverse PatchPair (store-relative `[id,field]` replace ops) when the drag-end opts in via `_recordUndo:true` + a pre-move `_prev` snapshot supplied by the drag handler; the authoritative commandManager mutation + mesh rebuild are unchanged (patches drive undo/redo ONLY, via `elementUndoStoreAdapter → window.<x>Store.update`; cm twin shadow-dropped — dual-dispatch like a 3D CREATE). Non-drag callers (property panel, AI) keep empty-patch behaviour → routing untouched. Shared `_movePatchPair` helper centralises the three bridges (structural, not N copy-paste). Tests: `plugins/furniture/__tests__/updateParameters-undo-capture` (4 — move+rotate one invertible ring entry, rotate-only, no-opt-in skip, missing-prev skip) + `apps/editor/__tests__/transformDragUndoCapture.matrix` (13 — every element move/rotate records exactly ONE invertible ring entry keyed on its store; column/beam/floor bridge mechanism; no-opt-in skip). Ref L-49/§FIX-WALL-MOVE-UNDO-CAPTURE, performUndoRedo.ts, mem undo-architecture-three-stores; P8 spans preserved. |
| L-73 | founder | **Plan-view FEATURE PARITY: main (full) plan view ≠ split-view plan pane** — some interactions work in one and not the other. Concrete example: doors/windows move/drag nicely in the MAIN plan view but not in SPLIT view. Goal: exact parity both ways (element move/rotate, hosted door/window drag, snapping, selection/hover, draw tools). | Creation/Plan-tools parity | BROKEN→FIXED | `§FIX-PLAN-VIEW-PARITY` (C11 element-creation pipeline). **Audit of the two surfaces — `PlanViewToolOverlay` (main) vs `SvpPlanToolOverlay` (split):** (1) **Draw/annotation tool set — the real divergence.** Each overlay declared its OWN hand-maintained handler map; they drifted — `north-arrow`, `scale-bar`, `matchline` existed ONLY in the main overlay, so those tools silently did nothing in split view. **Fix:** a single shared registry `plantools/planToolHandlerRegistry.ts` (`createPlanToolHandlers()`, P8 span `pryzm.plan_tools.create_handler_set`) is now the sole source of truth; BOTH overlays build their map from it, so the capability set is identical BY CONSTRUCTION and can never drift again. The factory returns FRESH instances per overlay, preserving the long-standing invariant that split-pane tool state never aliases the standalone plan view's (the SVP `north-arrow`/`scale-bar`/`matchline` gap is closed; Move/Align/Copy — previously re-added ad hoc — now come from the same source). (2) **Hosted door/window (and wall) move/drag, rotate, annotation/level/grid drag, snapping, hover, selection, context menu — already at parity.** These are NOT owned by the tool overlays: they live in `PlanViewInteraction` (driving the shared `planElementDragController` singleton), which `PlanViewManager` attaches to the MAIN canvas and `SplitViewManager` attaches to the SVP canvas (`SplitViewManager.ts:237‑238`, present since 2026-05-19). The controller is deliberately canvas-agnostic (planCanvas + domCanvas are start-drag args), so a hosted door/window drag dispatches the SAME command (`SetDoorOffsetCommand` / `SetWindowOffsetCommand`, `{source:'HUMAN_DIRECT'}`) regardless of which pane started it — now pinned by test. The split pane's own pan/select handlers already yield to the interaction layer via the shared `__pryzmToolHandled` flag (bubble-phase pan early-returns) and defer click-selection when `_planInteraction` is attached; the `__pryzmForwarded` stale-hover-anchor workaround (mem select-svp3d-stale-hover-anchor) is scoped to the SVP **3D-mirror** mode only and does not touch the plan pane. **Net:** the divergent-subset hazard is eliminated (single registry) and hosted-drag parity is proven; both surfaces share ONE capability + interaction set. Tests: `PlanViewParity` (2 — registry exposes all keys incl. north-arrow/scale-bar/matchline + fresh instances per pane), `PlanHostedDragParity` (2 — door & window drag commit the same command in the canvas-agnostic controller). Sibling of L-66 (`§FIX-PLAN-WALLTOOL-ARM-ON-ACTIVATE`, same overlay pair). |
| L-74 | founder | **RECURRENT: black triangular-prism solids at T/L wall joint intersections** — on many T and L wall connections a black solid (triangular prism) sits in the joint intersection; persists across project close/reopen (loads from persisted baselines). Log shows `[WallJoinResolver] §MULTI-CLUSTER-PARTITION-TRIM … square-cap to consensus` at the cluster — same family as L-27 (`§FIX-WALL-CLUSTER-DEGENERATE`, bow-tie/neg-area footprint = black) and L-61. Need COMPLETELY clean joints. | Geometry/Walls | BROKEN→FIXED (§FIX-NEWWALL-LCORNER-SKEW / §MITER-SEGMENT-CLAMP, c6e8caae — black prisms/spike gone) | routed → SOLE WALL AGENT (queue behind L-63/L-62). The square-cap/partition-trim path still emits a self-intersecting / negative-area (bow-tie) footprint prism at some T/L clusters → black solid. Detect+repair (or avoid) the degenerate cap so no prism is emitted; verify on a reopened project (persisted baselines). ADR-0055; builds on L-27/L-61/L-63; add regression test with a footprint-area assertion |
| L-62 | founder | **Layered wall type not honored in PLAN view** — after choosing a LAYERED system type (e.g. "Interior – Partition 100 mm"), 3D creation is correct (proper layered wall) but PLAN renders the PLAIN default single-volume wall at the right thickness — the layer composition (layer boundary lines / core+finish) is missing. `systemTypeId` dispatches correctly (`systemTypeId=wt-interior-partition thickness=0.2`), so the loss is in the plan PROJECTION path, not the store. | Views/Drawing/Walls | BROKEN→FIXED | `§FIX-PLAN-LAYERED-WALL-SYMBOL` (ADR-0055 plan-projection). **Root cause:** in 3D `WallFragmentBuilder` builds ONE mesh per layer (offset along the wall normal, centred on the baseline), but the plan projection only draws the wall's OUTER footprint outline — there is no emitter for the N−1 lines BETWEEN adjacent layers, so plan shows a single volume. Every other element type (door/window/column/…) supplies a `*PlanSymbolBuilder` that `EdgeProjectorService` injects; walls had none. **Fix (wall-package-scoped, follows the established symbol-builder pattern — does NOT touch the generic projector cache owned by L-65):** new pure `computeWallLayerLines` (`packages/geometry-wall/src/WallLayerPlanLines.ts`) emits the internal layer-boundary segments in world XZ, using the EXACT 3D layer offsets (`cursor = −Σt/2`, outward normal `(−dir.z, +dir.x)`, boundary after each layer k for k=0…N−2) so plan and 3D agree, clipped at any opening whose void crosses the 1.2 m cut plane (same rule as the wall faces, C15). New `WallLayerPlanSymbolBuilder` (mirrors `ColumnPlanSymbolBuilder`: mutable singleton + `installWallLayerPlanSymbolBuilder(store)` factory + `window.wallStore` fallback) converts them via `OBC.TechnicalDrawing.toDrawingSpace` → `addProjectionLines('A-WALL')`. Wired with ONE `.inject()` line in `EdgeProjectorService.project()` (next to the 8 existing symbol builders — the sanctioned extension point, not the incremental-projection cache) and `installWallLayerPlanSymbolBuilder(wallStore)` in `initBuilders.ts`. Plain (single-layer) and curved walls emit nothing → non-layered plan is byte-identical. Tests: geometry-wall `wallLayerPlanLines` (6 — N−1 boundaries at exact offsets; door void breaks the lines at the jamb; a high clerestory does NOT break them; direction-correct offsets; plain/curved emit nothing). 135 geometry-wall tests green. C15 / ADR-0055; coordinates with L-65 (EdgeProjectorService/NME owner) by keeping the emission in the wall package. |
| L-75 | founder | **Room rename doesn't apply on Enter** — double-click a room → edit Name field → Enter does NOT update the room name. Console: `CommandBusError: room.setName: required store 'room' is missing from HandlerContext.stores. The bus does NOT fall back to globals — declare the store in your storesProvider. (ADR-0202 §3 / R1A-16)`. The `room.setName` command handler's `storesProvider` omits the `room` store, so `buildContext` throws before the mutation. | Editing/Rooms | BROKEN→FIXED | `§FIX-ROOM-SETNAME-STORE` (ADR-0202 §3 command-bus store injection / C16 command authoring). **Root cause (two layers).** (1) The bus threw because `SetRoomNameHandler` declared `affectedStores: ['room']` while the plugin RoomStore is contributed to the bus `storesProvider` under storeKey **`'rooms'`** (`apps/editor/src/PluginRegistry.ts` rooms descriptor) — so `buildContext` found no `'room'` key and threw `required store 'room' is missing` (R1A-16) before the handler ran. (2) **Deeper:** even with the store present the rename would have been invisible — the DETECTED / RENDERED / PERSISTED rooms live in the **legacy** room-topology `RoomStore` (`window.roomStore`, built in `initBuilders.ts`; render is driven by the `bim-room-updated` window event → `RoomLabelRenderer`), NOT in this plugin's SDK `RoomsState`, which no renderer or the project-persistence path ever reads. The old handler mutated the disconnected plugin store. **Fix:** converted `room.setName` into a LEGACY BRIDGE (`affectedStores: []`) that forwards `new RenameRoomCommand(roomId, { name })` through `window.commandManager` — the EXACT pattern its siblings `room.rename` (`RenameRoomHandler`) and `room.delete` (`DeleteRoomHandler`) already use. `RenameRoomCommand.execute()` calls `roomStore.update()` on the legacy store → emits `bim-room-updated` → the plan room tag re-renders, the change persists (survives save/reload), and it registers its own inverse on the legacy undo stack. The double-click→edit→Enter path already dispatches `room.setName` (both `RoomPropertySection.ts` and the plan-label inline editor in `initUI.ts`), so no UI change was needed. P8 span (`room.setName.handler`) preserved. Tests: `plugins/rooms/__tests__/handlers.test.ts` — new `room.setName — legacy bridge` block (4: forwards one `RENAME_ROOM` targeting the room via a bus whose provider has NO `'room'` key — the production condition that reproduces the original throw and guards against a regression to `affectedStores:['room']`; pre-init no-op; rejects empty name; rejects empty roomId). TODO(F-1.4): merge with `room.rename` once the detected-room world migrates off the legacy RoomStore. |
| L-76 | founder | **New wall onto an existing L-joint: PREVIEW ≠ RESULT (skewed/tapered)** — draw a NEW wall (perpendicular, 100 mm, snapped near the corner) onto/near an existing L-shape joint; the live PREVIEW is clean (perpendicular, snapped to the corner) but the EXECUTED wall is mangled — tapered/skewed, not joining the L cleanly. The new wall's endpoint lands in the L-corner cluster and the resolver trims it to a consensus that skews the stored/rendered baseline away from the previewed perpendicular. This is the concrete geometry that unblocks **L-63 Part-1** (the resolver-trim discriminator). | Geometry/Walls | BROKEN→FIXED | `§FIX-NEWWALL-LCORNER-SKEW` (ADR-0055). **Root cause (verified by resolveLevel repro):** the new wall C, drawn PERPENDICULAR to arm A, is therefore PARALLEL to arm B. The legacy resolver's pass-through detection tested DIRECTION only (`|dirI·dirJ| ≥ cos 10°`), so it mis-classified B + C as a collinear "pass-through pair" even though their centrelines are **0.313 m apart laterally** → the whole cluster went through §PASS-THROUGH-FLUSH, square-capping C to the corner consensus (5,0) → C's joining endpoint dragged 0.313 m off its axis = the skew/taper (and the self-intersecting bow-tie footprint, L-74). **Fix:** a genuine pass-through pair is two segments of ONE straight wall — collinear in direction AND on the SAME LINE. Added a lateral-coincidence gate: the perpendicular offset between the two candidate walls' junction endpoints must be within a half-thickness band (`max(0.05, max(t_i,t_j)/2)`). C (0.313 m offset) is now correctly NOT a pass-through → it routes to the T-into-corner path and `_applyT` butts it on A's face at its OWN foot (perpendicular, length preserved). The genuine collinear pass-through (offset ≈ 0, incl. resi `_repro_passthrough` — 290 mm partition onto a committed-corner consensus ON the through-line) is byte-unchanged. Discriminator = *lateral coincidence*, exactly the "consensus-on-host-body vs off-a-different-host" split, and it is derivable from the cluster (no downstream dependency). Detection/footprint frame — the stored baseline is only trimmed toward the host it truly abuts, never skewed off-axis. Gated `__pryzmPassThroughCoincidentGate` (default ON). Tests: geometry-wall `WallJoinResolver.newWallLCornerSkew` (5 — executed baseline perpendicular + on-axis + length preserved; positive-area non-self-intersecting footprint; V2 preview == executed; **stable on reopen** re-resolving persisted baselines; resi pass-through unaffected). 140 geometry-wall + 107 ai-host tests green. Completes L-63 Part-1. ADR-0055. |
| L-74 | founder | **Black bow-tie / negative-area prism at an L-cluster junction** — a new wall joining near an existing L-corner rendered a self-intersecting (inverted-normal, negative signed-area) footprint prism at the junction (renders black). Same root as L-76 — the `§MULTI-CLUSTER-PARTITION-TRIM` / near-corner square-cap emitting a skewed/degenerate footprint at an L cluster. | Geometry/Walls | BROKEN→FIXED | `§FIX-NEWWALL-LCORNER-SKEW` (ADR-0055) — SAME fix as L-76. The bow-tie is the footprint of the skewed baseline: once the new wall is (mis)classified as a pass-through and square-capped to the corner consensus while its far end stays on its own axis, the resulting quad self-intersects → negative area → black prism. Fixing the pass-through mis-classification (lateral-coincidence gate) makes the new wall butt the arm's body cleanly, so its footprint is a positive-area, non-self-intersecting rectangle. The regression test asserts `selfIntersects(footprint) === false` and `|signedArea| > 0` at the cluster, fresh-drawn AND on reopen. 140 geometry-wall tests green. ADR-0055. |
| L-77 | founder | **(regression from L-70) DOUBLE PANEL** — after picking "Overlay a plan / PDF", the "Site plan overlay" controls panel AND the onboarding "How do you want to set your plot?" choice card are both on screen, overlapping ("the old menu to select the pdf ... after i zoomed in another new one"). | Site/Geo/Onboarding | BROKEN→FIXED | `§FIX-SITE-OVERLAY-DOUBLE-PANEL` (ADR-0115; C06/C11). Root: `startOverlayImport()` set the drawing presentation but never `clearBody()`, so the three choice cards stayed behind the overlay panel; a SECOND uploader (the map's `📄 Overlay plan / PDF` button, redundant with the auto-opened picker + the panel's own upload button) added to the clutter. Fix: (1) the import step now `clearBody()`-s and renders a slim "Place, scale and rotate your plan… ✓ Finish" banner with a single "← Back" affordance — the choice cards are DISMISSED (single active panel) and RESTORED (`renderSiteStep()`) on Back/cancel; (2) the map's redundant `site-overlay-open-btn` is hidden in `overlayOnly` mode. Tests: `onboardingOverlayImportBranch` (2 new — choice card dismissed when the import step opens, restored on Back). |
| L-78 | founder | **CRITICAL — "✓ Finish — enter canvas" brings up NOTHING** — no canvas, no plan underlay; the founder is fully blocked on the defining feature. | Site/Geo/Canvas/Underlay | BROKEN→FIXED | `§FIX-SITE-OVERLAY-ENTER-CANVAS` (ADR-0115; C18/C19; P8). **Traced root cause = VISIBILITY, not command failure.** The underlay MESH is created directly by `FloorPlanUnderlayTool.create()` (it adds the mesh to `window.scene` + sets `floorPlanUnderlayRef` → the Underlay Render Service paints it) — this does NOT depend on `CREATE_UNDERLAY` succeeding (the command is only for undo, and the engine helper already try/catches it, so a `CommandBusError` can't hide the plan). The real break: on Finish, `finishImport` closed the map + `pryzmToggleGIS(false)` but NEVER switched to plan view or framed the camera, so the user landed in an unframed view (underlay off-screen) → "nothing"; plus an async race (GIS exited before the texture finished loading). Fixes: (1) the controller `commitProjectNorth` is now async and AWAITS `onEnterCanvas` (underlay mesh + texture placed) before signalling completion; (2) a new `window.pryzmActivateBimView(mode)` (GISAreaLayout → `activateView` → `toggleGIS(false)` → `ViewController.activate`) EXITS GIS and switches to PLAN ('Top') view; (3) `landInCanvasWithUnderlay()` closes the map → `pryzmActivateBimView('Top')` → `window.viewController.zoomToFit()` → disposes the wizard, so the founder lands framed on the plan; degrades to a plain `toggleGIS(false)` if the hook is absent. Tests: `createSiteOverlayUnderlay` (5 — mesh created at calibrated size + project-frame centre + axis-aligned, CREATE_UNDERLAY dispatched, **a bus failure does NOT abort the underlay**, not-ready-scene → false, undo/redo hooks registered) + `onboardingOverlayImportBranch` (2 — Finish closes map + exits GIS via `pryzmActivateBimView('Top')` + zoom-to-fit + disposes + no generate prompt; fallback path). |
| L-79 | audit (via L-75) | **Sibling room command handlers share the room.setName store-key bug** — `room.setNumber`, `room.setOccupancy`, `room.setMaterial`, `room.setHeightOffset`, `room.move`, `room.recomputeBoundary` all declare `affectedStores:['room']` against the bus storeKey `'rooms'` AND mutate the disconnected plugin `RoomsState` (not the legacy `window.roomStore` the renderer/persistence read). So each throws the same `required store 'room' is missing` (ADR-0202 §3) or silently no-ops. `room.setNumber` is dispatched by the SAME ROOM-panel/inline editor as rename → founder WILL hit it. | Editing/Rooms | BROKEN→FIXED | `§FIX-ROOM-SIBLING-HANDLERS-STORE` (ADR-0202/C16; P8). All six sibling handlers converted to legacy bridges (`affectedStores:[]`, no plugin-store injection → the R1A-16 missing-store throw is gone) forwarding the corresponding legacy command through `window.commandManager` so the mutation lands in the legacy `window.roomStore` the renderer (`bim-room-updated`) + persistence read: **setNumber** → `RenameRoomCommand({roomNumber})` (empty string clears); **setOccupancy** → `SetRoomOccupancyCommand` (empty→`unclassified`); **setMaterial** → `UpdateRoomCommand({colour})` (the visible plan/3-D room fill — the only material field the uniform `MaterialDispatch` path sends for rooms; a catalogue-`materialId`-only payload has no legacy top-level room field → documented no-op); **setHeightOffset** → `UpdateRoomCommand({boundary:{…,baseOffset}})` (reads the current boundary from `window.roomStore` since the legacy update-gate validates the FULL boundary; range-guarded [-10,10]); **move** → `UpdateRoomBoundaryCommand` (translates the polygon by `delta.x/z`, lifts `baseOffset` by `delta.y`); **recomputeBoundary** (fired by the wall→room cascade on every wall edit — the primary throw site) → a documented NO-OP, since the legacy RoomStore owns room analytics and the only legacy analog (`ReDetectRooms`, all-rooms/renumber) would be catastrophic per wall edit. Each preserves its P8 OTel span. Tests: `plugins/rooms/__tests__/handlers.test.ts` rewritten for the bridge behaviour (22 green — each forwards the right legacy `CommandType`, no throw when the bus has no `'room'` key / pre-init, still validates payloads, mutates no plugin store). Env note: the rooms vitest env moved `node`→`happy-dom` (the bridges import `@pryzm/command-registry`, whose barrel touches `window` at module load — the suite couldn't even collect under `node`; pre-existing, masked because the package has no `test:ci` script). No wall/geometry/render touched. |
| L-80 | founder | **Schedules — add edit / view mode** — the user needs schedules (door/window/room/etc. schedules) to have a proper EDIT mode and VIEW mode (currently missing/incomplete). Add the ability to view schedules and edit their cells/definitions. | Data/Schedules | GAP(feature)→FIXED | `§FEAT-SCHEDULE-VIEW-EDIT` (C03 command-first/P6; C28 data-panel; C37 schedule). **What already existed (investigated first, then extended — no parallel surface).** The seeded, persisted, window-exposed `scheduleStore` (`packages/core-app-model/src/views/ScheduleStore.ts` — the one that logs `Schedule Store initialized and default schedules seeded`; 17 built-in `ScheduleDefinition`s `{id,name,scheduleType,fields[]}`) is the authoritative definition store; `SchedulesRailPanel` lists it and opens `window.schedulePanel.show(id)` → `SchedulePanel` (`apps/editor/src/ui/SchedulePanel/SchedulePanel.ts`), a read-only table sourced from `ScheduleRegistry` (rendering columns, ids matching the store `fields` 1:1) + `ScheduleExtractor` (element rows). Legacy `Create/Update/DeleteScheduleCommand` (P6, undoable, mutate `scheduleStore`) existed but were dispatched ONLY by the AI `ViewAuthoringIntentMapper` — no UI edit path, and the panel's column show/hide was **session-only** and disconnected from the persisted store. So: VIEW rendered from a registry snapshot, not the store; EDIT did not exist. **Built.** (1) **VIEW now renders from the store** — new pure DOM-free view-model `scheduleViewModel.ts` (`resolveVisibleColumns` intersects the persisted `scheduleStore.fields` with the registry columns in stored order; `projectRows` maps elements→cells). `SchedulePanel` now derives its visible columns + name from `scheduleStore` (falls back to the full registry set for registry-only Data-Platform schedules). (2) **EDIT mode** — an Edit/Done toggle (shown only when `scheduleStore.has(id)`); in edit mode the header name becomes an input and a column checklist is shown. Rename + add/remove-column each dispatch `runtime.bus.executeCommand('schedule.update', { scheduleId, patch })` — **never a direct store write** (P6). (3) **Bus bridge** — new `schedule.update` bridge in `apps/editor/src/engine/initBusHandlers.ts` (the sanctioned bridge location, excluded from the `check:commandmanager` scan) forwards to `new UpdateScheduleCommand(scheduleId, patch)` via `_cmExec`, so the edit lands in `scheduleStore` and is undoable on the commandManager stack; the panel re-renders on the `sched:schedule-updated` event (incl. undo/redo). Mirrors the roof.update/ceiling.update bridges and the L-75/L-79 room-bridge pattern. Do-not-touch honoured: no wall/geometry/render/site-overlay. Tests: `apps/editor/__tests__/scheduleViewEdit.test.ts` (7 — VIEW resolves columns from store fields in order; `projectRows` renders rows/cells incl. em-dash fallback; toggle add/remove in canonical order; **edit dispatches `schedule.update` with the right payload AND the bridge's UpdateScheduleCommand lands it in the store**; rename via same path; undo restores the prior definition; no-bus → dropped, store untouched). Root `tsc -p tsconfig.json --noEmit` clean (0 errors). |
| L-81 | founder | **Project DUPLICATION doesn't work** — duplicating a project fails / the duplicate is not created correctly (the duplicate opens broken/empty). | Persistence/Project-lifecycle | BROKEN→FIXED | `§FIX-PROJECT-DUPLICATE-OPEN`. **Root cause:** duplication created a new project id + metadata but copied **no snapshot**. Server `projectStore.duplicateProject` did a bare `INSERT INTO projects …` with **zero** version rows, and the client `ProjectHub.duplicateProject` saved the new meta with `versionCount:0` **without** copying the source's local `versionRepository` history under the new id. So on open, the local-first path (`PlatformShell.setProjectContext` → `getVersions(newId)`) found nothing and fell through to `GET /api/projects/:id/latest-version`, which returned `{version:null}` / 404 (`[persistence.tier.streamLoad] Server 404`) → the duplicate opened empty/broken. **Fix (two authoritative halves):** (1) **Server** — `duplicateProject` now copies the project row AND its **latest** version snapshot atomically in one `withTransaction`, via a pure `INSERT INTO project_versions … SELECT … FROM project_versions WHERE project_id=$source ORDER BY created_at DESC LIMIT 1` (JSONB duplicated inside Postgres, no Node round-trip) and sets `version_count=1`; the no-pool in-memory dev path is guarded so it no longer throws. Latest-only (not the full 20-deep history) keeps memory bounded — the open path restores only the latest, and history re-accrues on the duplicate's future saves. (2) **Client** — new `LocalVersionRepository.duplicateInto(sourceId, targetId, name)` deep-copies the source's local version history under the new id (re-keying each record + embedded snapshot `projectId`/`projectName`, marked `synced`), called from `ProjectHub.duplicateProject` BEFORE saving the meta (meta `versionCount` = copied count) so the local-first open restores the source's elements instantly / offline. Returns 0 when the source was never opened on this device — the server-side copy then carries the open. Tests: server `projectStore.test.js` (2 source-shape guards — transactional version copy + no-pool guard) and editor `projectDuplicateOpen.test.ts` (3 — duplicate has a loadable latest snapshot carrying the source's elements under the new id; source history unmutated; empty-source → 0/no-op). C13/C05; C05 §1.1 atomic transaction. |
| L-82 | founder | **Window dimension change beyond wall bounds destroys the opening permanently + window undeletable** — while changing a window's dimensions, if the window FRAME goes outside the host wall, the wall stops being cut and the OPENING disappears; then even when the dimensions are brought BACK within the wall, the opening does NOT reappear; and such windows CANNOT be deleted. | Creation/Hosted-elements/Openings | BROKEN→FIXED | `§FIX-WINDOW-OOB-OPENING-RESTORE` (C15 hosted elements). **Root cause — NO wall-extent guard on window dimension edits.** Only the MOVE path (`SetWindowOffsetCommand`) validated placement via `WallOccupancyStore.canPlace`; every DIMENSION path (`UpdateWindowParameterCommand` from the Doors/Windows panel, `UpdateElementParameterCommand`/`window.setSize` from the property inspector, `UpdateWindowWidthCommand`) checked only `> 0`. So a width / height / offset / sill edit could push the opening span `[offset, offset+width] × [sill, sill+height]` past the host wall. The failure triad then follows once the hosted element desyncs — it survives in the external `windowStore` (drives the selectable 3-D mesh) but is dropped from `WallStore.windows` / `wall.openings`: (1) the wall stops being cut (opening gone from `wall.openings`); (2) it never re-cuts because `WallStore.updateWindow` early-returns for a window missing from its internal map, so bringing the size back in bounds does nothing; (3) `DeleteElementCommand` resolves windows ONLY via `wallStore.getWindow` → an external-store-only window is unresolvable → `canExecute` rejects → undeletable. **Fix (three parts).** (1) GUARD/CLAMP — new pure `WallOccupancyStore.clampToWall(wall, dims)` keeps the frame span entirely inside the wall (width∈[MIN,wallLen], offset∈[0,wallLen−width]; height∈[MIN,wallH], sill∈[0,wallH−height]) — a width that fits is preserved by shifting the offset inward (mirrors WindowTool's placement clamp), only an over-wall width is itself shrunk. Applied at BOTH the command (`UpdateWindowParameterCommand` — clamps the patch before writing so the frame `windowStore` and opening `wallStore` get the SAME in-bounds values, incl. any derived offset shift, undoable) AND the universal store choke point (`WallStore.updateWindow` — every caller funnels through it). The out-of-bounds state is now impossible. (2) RE-CUT/RESTORE — `WallStore.updateWindow` self-heals: if the window's opening is missing from the wall it RE-CREATES it from the (clamped, in-bounds) window record, so the wall re-cuts instead of staying solid. (3) DELETE ROBUST — `DeleteElementCommand` gains an orphaned-hosted-element branch: `canExecute` + `execute` also resolve via the external `windowStore`/`doorStore`, so a window/door present only there is still deletable (frees the external record, drops any lingering wall opening, unregisters) and undo restores it. Do-NOT-touch honoured: no wall JOIN geometry edited (JunctionResolverV2/WallFootprint2D/WallJoinResolver/WallRebuildCoordinator untouched). Tests: geometry-wall `WallOccupancyStore.clampToWall` (8 — width-shift-preserve, over-wall shrink, vertical clamp, MIN floor, recovery, degenerate) + command-registry `windowOobOpeningRestore` (4 — command clamps OOB width consistently across both stores, in-bounds no-op, orphaned window deletable, undo restores). geometry-wall 148/148, root `tsc --skipLibCheck` clean. |
| L-83 | founder | **Opening a project doesn't work** — opening a project fails (separate from duplication). Prod shows `/api/projects/<id>/latest-version 404` → `[persistence.tier.streamLoad] Server 404` → the open path can't load the latest version and fails / yields empty. | Persistence/Project-lifecycle | BROKEN→FIXED | `§FIX-PROJECT-OPEN-STREAMLOAD-FALLBACK` (C13/C05; P8). **Traced the real runtime open path** (`PlatformRouter.launchWorkspace` → `runtime.persistence.openProject` [`buildPersistence.ts`] → `tier.streamLoad` fetches `/latest-version`, returns `null` on 404 → `WorkspaceSurface.setProjectContext` → `PlatformShell.setProjectContext`). L-81 already made the list-resolution gate soft-fall-through (OI-059) and `setProjectContext`'s *local-first* branch restores a warm-mirror local version; the remaining hole was the **server leg** `PlatformShell._loadLatestVersionFromServer`: on a non-ok response (404) — or `{version:null}`, or a fetch/parse error — it `return`ed and fired `pryzm-project-loaded(empty:true)`, opening EMPTY with **no local fallback**. This leg is also reached DIRECTLY from the empty-load `.catch` call site, which bypasses the earlier local-first check entirely. **Fix:** new private `PlatformShell._restoreLatestLocalVersion(projectId)` — AWAITS `warmVersionCache()` (the synchronous IDB mirror can be cold on a deep-linked/hard-reload open), re-checks `activeProjectId` (discard on project-switch), reads `versionRepository.getVersions()` and, if present, restores the latest via `versionCtrl.loadVersion` (which owns its own overlay/setLoading/`pryzm-project-loaded`). `_loadLatestVersionFromServer` now calls it on ALL three "server has nothing usable" outcomes (404 / `{version:null}` / fetch error) and only fires the empty event when local is ALSO empty — so a project with only-local history (server-forgotten volatile record or offline) opens with its elements, while server-present projects still load the server copy unchanged (server authoritative, local authoritative fallback). No wall/geometry/render/site-overlay touched. Tests: `apps/editor/__tests__/projectOpenStreamLoadFallback.test.ts` (4 — 404+local → restores local elements, no empty event; 404+no-local → opens empty once; `{version:null}`+local → restores local; server-200 → loads server copy, local not consulted). Root `tsc --skipLibCheck` clean. Sibling of L-81. |
| L-84 | founder | **Copy is not working** — copying element(s) (copy/paste) does nothing / fails. | Editing/Selection | BROKEN→FIXED | `§FIX-COPY-PASTE` (C16; P6/P8). **Root cause = the copy/paste bus handlers were permanent no-op stubs.** `CopySelectionHandler` and `PasteClipboardHandler` (registered at EngineBootstrap, dispatched by the MainToolbar Copy/Paste buttons and the `copy-selection`/`paste-clipboard` bus commands) were the TASK-08 "Option A" stubs whose `canExecute` **always returned `{ valid:false, reason:'Copy/paste not yet implemented' }`** and whose `execute` only `console.warn`'d — so every copy AND paste was rejected by the bus and did nothing. (A separate, proven legacy path exists — `ContextualEditBar` Ctrl+C/V → `CopyPasteTool` → `CopyElementCommand` — but the toolbar/bus path never reached it.) **Fix (real clipboard, P6, undoable).** New `plugins/selection/src/handlers/clipboard.ts`: a THREE/DOM-free `SelectionClipboard` singleton + a `SelectionPastePort` interface (dependency-injected re-creation, so the L7 plugin stays on `@pryzm/plugin-sdk` only and the handlers are unit-testable). `CopySelectionHandler` now snapshots the CURRENT selection from the canonical `ctx.stores.selection` `SelectionStore` into the clipboard (filtered to port-copyable kinds; rejects with a typed reason when nothing is selected/copyable — no silent success). `PasteClipboardHandler` re-creates each copied element with a **fresh id + small offset on the SAME level** via the injected port, returning empty patches (the child creations own their own undo). The concrete port is wired in `engineLauncher.ts` (`§FIX-COPY-PASTE`): it routes re-creation through the registered `CopyElementCommand` (the proven wall+furniture clone pipeline — new id, source level preserved, offset applied, undoable) so this fix touches **no wall/geometry/render/site-overlay** package. `registerSelectionHandlers(bus, { pastePort })` threads the port through. Tests: `plugins/selection/__tests__/handlers/CopyPaste.test.ts` (5 — copy captures selection → paste re-creates with NEW id + same type; multi-select skips non-copyable kinds and mints distinct ids; empty-selection copy rejects; empty-clipboard paste rejects; no-port paste rejects with feedback). Selection suite 11/11 green; root `tsc -p tsconfig.json --noEmit` clean. |
| L-85 | founder | **Kitchen, wardrobe and lighting elements missing after project re-open** — these element types are present after creation but GONE when the project is closed and re-opened (not persisted/restored). | Persistence/Serialization | BROKEN→FIXED | `§FIX-PERSIST-KITCHEN-WARDROBE-LIGHTING` (C13/C05; P8). **Root cause = the DEFAULT-ON fast load path (`ImportProjectCommand`) omitted the LIGHTING restore step.** Lighting fixtures ARE serialized (`ProjectSerializer` §PERSIST-LIGHTING sources them from the window-managed `LightingStore`) and the `LightingStore` is registered in `ProjectScopeRegistry` (`initStores.ts` `r('lighting', …)`), so `ClearProjectCommand.clearAll()` WIPES it at the start of every open — but only the LEGACY per-command `ProjectLoader` path had a lighting restore (Step 10b via `CreateLightingCommand`); the `ImportProjectCommand` fast path (default-on, `PRYZM_USE_IMPORT_COMMAND`) had NO lighting step, so every light was cleared and never re-created on reopen. **Kitchen/wardrobe** are ordinary `FurnitureData` in the geometry `FurnitureStore` (kitchen/wardrobe tools → `furniture.create` bus command → `furniture.created` bridge in `initTools.ts` mirrors them to `window.furnitureStore` WITH `kitchenConfig`/`wardrobeCabinetConfig`); they ARE serialized (`serializeFurniture`) and WERE restored by the fast path's furniture step, but their parametric RUN configs (which `FurnitureFactory` requires — Contract 13 §2) are now single-sourced through a tested mapping so a dropped config can't silently collapse a RUN on reopen. **Fix:** (1) added a **Step 10b lighting restore** to `ImportProjectCommand._orchestrate` (mirrors the legacy path) — iterates `snapshot.lighting`, rebuilds each via `CreateLightingCommand` (fires `bim-lighting-placed` → `LightingStore` + `LightingFragmentBuilder`); (2) extracted two pure, barrel-free, unit-testable restore-payload builders into `projectLoaderUtils.ts` — `buildLightingRestorePayload` (maps a serialized fixture → `CreateLightingPayload`, drops malformed records) and `buildFurnitureRestorePayload` (byte-faithful furniture mapping incl. `kitchenConfig`/`wardrobeCabinetConfig`/`furnitureCategory`/`metadata`), now used by the fast path's furniture + lighting steps. Legacy `ProjectLoader` path left verbatim as the rollback. Tests: `packages/command-registry/__tests__/persistKitchenWardrobeLighting.test.ts` (6 — kitchen RUN preserves `furnitureType`+`kitchenConfig`+placement; wardrobe preserves `wardrobeCabinetConfig`+`lo3`; furniture defaults baseOffset/material; lighting round-trips all `CreateLightingCommand` fields; malformed lighting dropped; optional bindings absent). Root `tsc --skipLibCheck` clean; 23/23 command-registry persistence-helper tests green. No wall/geometry/render/site-overlay touched. |
| L-86 | founder | **Base offset not present on many furniture elements** — many furniture elements lack a base-offset (vertical placement offset) property in their DTO/inspector, so they can't be raised/lowered and don't sit correctly. | Creation/Furniture | **FIXED** `§FIX-FURNITURE-BASE-OFFSET` | ROOT CAUSE was the DEFAULT, not the field: `baseOffset` already existed on `FurnitureData` (geometry-furniture + core-app-model), was exposed+editable in the property inspector (`FurniturePropertySection` 'Base Offset' → `UpdateFurnitureParametersCommand`), and honoured by the builder (`furnitureWorldY(floorY, mountOffset) = floorY + mountOffset`). But the DEFAULT was **0.2 m** in BOTH `CreateFurnitureCommand` and `FurnitureFragmentBuilder`, so every item created without an explicit offset FLOATED 200 mm ("don't sit correctly"). Fix: default → **0** (floor-standing) in both places; the mount offset now STACKS on the FFL baseline (L-87) so a 0-offset floor item rests exactly on the finished floor. Field/inspector/builder-honour path left intact (already correct). C03 (DTO field, no `packages/schemas` touch — furniture has no Zod schema there; DTO kept pure) / C15. Tests: `geometry-furniture/__tests__/furnitureBaseOffset.test.ts` (4 — field present, builder applies offset once, absent offset defaults to 0 not 0.2). Pairs with L-87. |
| L-87 | founder | **Furniture should default to FFL (finished floor level), not slab** — newly created furniture is placed at the slab top; it should sit on the FLOOR FINISH level (FFL) by default. | Creation/Furniture | **FIXED** `§FIX-FURNITURE-FFL-DEFAULT` | ROOT CAUSE: `CreateFurnitureCommand` datumed `position.y = level.elevation` — the STRUCTURAL SLAB TOP — ignoring the applied floor finish, so furniture sank the finish thickness below the finished floor. The floor-finish subsystem already models FFL as `level.elevation + floor.boundary.baseOffset` (the finish's TOP face; `FloorTypes` §A.21.D48). Fix: new pure resolver `resolveFflOffset(floors, {x,z})` in `core-app-model/stores/FloorTypes.ts` (leaf-pure, no I/O/THREE/DOM — mirrors `resolveFinishSeating`) turns the finishes covering a level into the FFL offset above the datum: it prefers the finish whose polygon CONTAINS the item's plan point (per-room FFL), else the greatest finish offset on the level, else 0 (bare slab → datum IS the FFL). `CreateFurnitureCommand` reads `context.stores.floorStore.getByLevel(levelId)` and sets `position.y = level.elevation + fflOffset`; the mount `baseOffset` (L-86, default 0) STACKS on top downstream (`furnitureWorldY = position.y + baseOffset`). `UpdateFurnitureParametersCommand`'s baseOffset-edit Y-recompute was made FFL-consistent too (`level.elevation + fflOffset + baseOffset`) so editing the offset no longer drops the item to the slab. Coordinated with the floor-finish subsystem (no new floor state). C15. Tests: `core-app-model/stores/FflResolve.test.ts` (5 — bare slab→0, covering finish→its top offset, per-room point-in-polygon selection, fallback-to-greatest, hidden-finish ignored) + `command-registry/__tests__/furnitureFflBaseOffset.test.ts` (4 — create seats Y at FFL not slab top, no-finish→slab top, default baseOffset 0, supplied offset preserved). Pairs with L-86. |
| L-88 | founder | **Import Manager must be SOUND at all times, especially after project close→reopen** — imported PDF/Image underlays should persist + re-list on reopen, and every row action (visibility / pin / disable / delete) must work in all states. "Revisit the whole panel." | Site/Geo/Import/Persistence | BROKEN→FIXED | `§FIX-IMPORT-MANAGER-SOUND` (C13/C32; ADR-0115; C18/C19). **Traced the real path — three concrete gaps.** (G1/G2) the L-71 site-plan→canvas underlay was created by `createSiteOverlayUnderlay` but NEVER fired the Contract-§32 `pryzm-floor-plan-underlay-placed` event, so it neither REGISTERED in the Import Manager nor got PERSISTED (UnderlayPersistence saves on that event) → it vanished on reopen. Fix: the engine helper now emits `pryzm-floor-plan-underlay-placed` (with the uploaded fileName, stamped on `mesh.userData`) — reusing the PROVEN pipeline (UnderlayPersistence → localStorage metadata + `UnderlayRasterStore` IndexedDB raster; on reopen `restoreUnderlayForProject` recreates the tool + re-emits the placed event → the panel re-lists it). (G3) the Import Manager's visibility + pin row actions were handled in `FloorPlanImportPanel` against `state.underlayTool`, which is NULL for a RESTORED (reopen) or L-71 underlay (created outside that panel) → the actions were silent no-ops after reopen. Fix: those handlers now fall back to the canonical `window.floorPlanUnderlayTool` (set by every `FloorPlanUnderlayTool.create()`), so pin/visibility work in all states; delete already used that fallback + the `DELETE_UNDERLAY` bus command (L-45, P6). Also hardened `ImportManagerPanel` to reconcile from the restored live tool on a short delay after `pryzm-project-loaded` (the restore is async — an immediate reconcile finds nothing; idempotent + dedupes, so no duplicate rows). **Architecture note:** underlays are non-BIM (Contract 32 / 04 §3.1 — never in an ElementStore, never on the Store Event Bus), so they persist via the per-project UnderlayPersistence (localStorage + IDB, Contract 45/46 isolation), NOT the ProjectSerializer BIM snapshot — that separation is deliberate and preserved. Tests: `importManagerPersistence` (6 — fresh import registers; rebuilds from the restored live tool on reopen; visibility/pin/delete dispatch the correct events after reopen) + `createSiteOverlayUnderlay` (+1 — emits the placed event with fileName + stamps `mesh.userData`). |
| L-89 | founder | **Plan-view projection engine too slow on element creation (esp. SPLIT view) - review the engine** — the founder loves the plan projection's ACCURACY but it's too slow: split-view plan doesn't react instantly on element create; feels like a wait behind the scenes (e.g. before the 2nd click of wall creation) and a full re-projection every single time an element is created. Log confirms per-element `REDETECT_ROOMS` + full `EdgeProjectorService.project()` + NME with 0% cache on new elements. Extends L-65 (`§FIX-PLAN-PROJECT-INCREMENTAL`, which grafted incrementally for safe types) but the founder still perceives lag - review the WHOLE projection engine + the split-view driver + the redetect-then-reproject sequence. | Perf/Plan-projection | **FIXED** `§FIX-PLAN-PROJECT-SPLIT-INCREMENTAL` | ROOT CAUSE of the split-view perceived lag: in SPLIT view the main viewport is 3D, so `PlanViewManager` (the snappy 30 ms `_onProjectionStale` driver used by the FULL plan view) is INACTIVE — the secondary plan pane is reprojected ONLY by `ViewDependencyTracker._flush`→`onReprojectionNeeded`, which sat behind a flat **300 ms** debounce. So every create waited ~300 ms before the split plan even started projecting — exactly the "wait behind the scenes before the 2nd click." VERIFIED the L-65 graft DOES fire in split view (VDT resolves the new element's level via `bimManager.getLevelForElement`; graft path in `initScene.onReprojectionNeeded` projects ONLY the dirty element and grafts it onto the warm drawing — "0% cache hit on new elements" is expected/correct: the ONE new element is a miss, all others reuse cache). FIX: `ViewDependencyTracker._scheduleDebouncedFlush` now picks the envelope per re-arm — a purely GRAFT-ELIGIBLE flush (every dirty view touched only by create/update of pure-projection types wall/slab/beam/ceiling/floor) flushes on a FAST ~48 ms (~3-frame) envelope so the split plan reacts near-instantly and grafts only the new element (element-scoped `invalidateElement`, warm drawing kept); coarse/mixed/batch changes keep the 300 ms envelope (`§PERF-VIEW-BATCH-SUPPRESS` batch path unaffected — it marks views coarse). MEASURED: split-plan create latency 300 ms → ~48 ms (~6× faster to first paint), same projection = same accuracy. REDETECT_ROOMS already coalesces to one run per 300 ms idle (`§WS-2.B`) and does NOT drive the split plan (rooms aren't in VDT's `GEOMETRY_ELEMENT_TYPES`), so it no longer blocks the interactive paint. Tests `planProjectSplitIncremental.test.ts` (4) assert fast element-scoped flush for creates + 300 ms for coarse. No wall geometry touched. C04 §3.3 / DOC-1.4; P8. |
| L-90 | founder | **[CRITICAL/BLOCKER] Plan view goes BLANK after creating a wall - all walls disappear (3D is fine)** — creating a wall in plan view: 3D shows everything, but PLAN VIEW shows NOTHING (all walls vanish). Log: `[ViewTechnicalDrawingCache] Stale projection rejected — staleGen=17 currentGen=18` — a concurrent generation bump (the 2nd `REDETECT_ROOMS`/reproject) marks the freshly-computed projection STALE, so it is rejected and never applied; combined with the L-65 incremental graft having already invalidated/replaced the warm drawing, the plan is left EMPTY. Likely a REGRESSION from L-65 (`§FIX-PLAN-PROJECT-INCREMENTAL`) interacting with the stale-gen guard. | Perf/Plan-projection | **FIXED** `§FIX-PLAN-BLANK-STALEGEN` | ROOT CAUSE: TWO independent re-projection drivers race the SAME view's monotonic generation counter — `PlanViewManager._onProjectionStale` (30 ms) and `ViewDependencyTracker._flush`→`onReprojectionNeeded` (300 ms). A wall create fires BOTH, and the follow-up `REDETECT_ROOMS` (room type, not graft-safe) fires the coarse full path too. One driver's `invalidate(viewId)` disposes+DELETES the only cached drawing AND bumps the generation WHILE the other driver's `project()` is still in flight; that projection then completes tagged `staleGen<currentGen` → `ViewTechnicalDrawingCache.setIfCurrent()` REJECTED it → cache left EMPTY → blank plan. FIX (central, single-point, correctness-first): `setIfCurrent()` now, on a stale-gen rejection, checks whether the cache is EMPTY for that view — if so it ACCEPTS the completion anyway (a slightly-out-of-gen but fully-computed drawing beats a blank view; project() reads live geometry so "stale" is ordering not content; any still-in-flight current-gen projection overwrites it when it lands). When the cache is non-empty the reject is preserved (never clobber newer-good with older). This makes the graft/redetect gen-race HARMLESS across ALL 4 `setIfCurrent` sites (PlanViewManager main + split, ViewDependencyTracker graft + full) WITHOUT disabling the L-65 graft. Regression test `packages/core-app-model/src/views/__tests__/planBlankStaleGen.test.ts` reproduces the staleGen<currentGen race and asserts the drawing is NEVER empty after a create (4 tests green). No wall geometry touched. C04 §3.3 / DOC-1.5f; P8. |
| L-91 | founder | **New wall snapped to an L-corner — executed still BIASED vs preview** (residual after §FIX-NEWWALL-LCORNER-SKEW). Preview shows a clean PERPENDICULAR wall snapped to the corner (100 mm, ~1897 mm, "Perpendicular" snap) but the CREATED wall has a visible bias/skew — executed baseline diverges from the previewed one. Different residual case than the L-74/L-76 pass-through square-cap (that dragged the endpoint to the corner consensus); here the preview is perpendicular+snapped yet the executed geometry shifts. | Geometry/Walls | BROKEN→FIXED | `§FIX-NEWWALL-LCORNER-BIAS` (ADR-0055). **Root cause (verified by a resolveLevel-vs-V2 repro):** a new wall whose endpoint SNAPS onto the existing L-corner node is PINNED there. The legacy resolver deferred a pinned wall to the pair-wise `_applyCorner` bisector miter — correct for a genuine 2-wall L, but for a 3rd wall AT the corner it ROTATES the stored baseline off its authored axis (a diagonal 3rd wall's join end drifted (5.000,0)→(5.099,0) with a ~2° tilt; the V2 preview keeps the baseline immutable). **Fix:** a pinned wall that is NOT part of the primary corner pair is routed to the on-axis §CONSENSUS-ON-CENTRELINE trim (endpoint projected onto the wall's OWN centreline → zero rotation, angle + length preserved) instead of the pair-wise tilt. A genuine 2-wall L corner (both walls in the primary pair) still defers → the bisector miter is byte-unchanged; with no primary pair the old defer is kept. Gated `__pryzmNewWallLCornerBiasFix` (default ON). Detection frame — baseline only ever trimmed ALONG its axis, never rotated. Tests: geometry-wall `WallJoinResolver.newWallLCornerBias` (5 — executed baseline == preview angle+length for diagonal/perpendicular 3rd walls; stable on reopen; genuine 2-wall L still mitres). 150 geometry-wall + 117 ai-host tests green. ADR-0055; pairs with L-61/L-74/L-76. |
| L-92 | founder | **Door flip on SPACE during placement (like furniture rotate)** — add the same mechanism doors already lack: while placing a door, pressing SPACE flips it, cycling through ALL 4 configurations — swing inwards/outwards (inside/outside the wall) AND hinge left/right. Same UX as the furniture space-rotate (L-16/ADR-0107). | Creation/Doors placement UX | **FIXED** `§FEAT-DOOR-FLIP-ON-SPACE` | Mirrored the furniture spacebar-rotate pattern (ADR-0107) for the wall-hosted door, which ADR-0107 had deliberately excluded from `PrePlacementRotation` because for a hosted element "rotation" is a host-side FLIP (in/out × hinge), not a free 90° yaw (C15). Introduced a NEW shared single-source-of-truth state `DoorPlacementFlip` (`packages/core-app-model/src/preview/DoorPlacementFlip.ts`, exported from `@pryzm/core-app-model`) — the door analogue of `PrePlacementRotation`: a cyclic 4-state flip (`DOOR_FLIP_STATES` = swing `inward`/`outward` × hinge `left`/`right`), advanced +1/press and wrapping 3→0, exposing `swingDirection()` / `hingesSide()` (which map EXACTLY onto the `DoorOpening` schema fields the `DoorPlanSymbolBuilder` reads for the swing arc + leaf) plus `label()` for the HUD, and `attach()`/`detach()` (capture-phase `document` keydown, form-field-guarded + `preventDefault` — no scroll, no leak). BOTH door placement flows converge on it: (1) **plan** — `DoorPlanToolHandler` constructs it, advances from the overlay-routed `onKeyDown` on SPACE (PlanToolHandler contract §21 §2 — it does NOT attach its own DOM listener), re-draws the swing-arc preview parametrically to reflect hinge side (−/+halfPx) × swing side (±perpendicular) matching the placed symbol's `swingDir`/`hingesSide` math, shows a `Click to place · Space to flip (In · Left) · Esc to cancel` HUD, and threads `hingesSide`/`swingDirection` into the `wall.opening.create` payload (P6); (2) **3D** — `DoorTool` `attach()`es on activate / `detach()`es on deactivate, surfaces the config on its HUD, and threads the two fields into the `CreateWallOpeningCommand` payload. Both DoorStore mirroring bridges (`initTools.ts` §P2.3-DOOR for the bus/plan path; `CreateWallOpeningCommand` for the 3D path) now carry the two fields onto the `doorStore.add()` record (omitted → schema defaults `left`/`inward`, so pre-L-92 callers are unchanged). Tests: `packages/core-app-model/src/preview/DoorPlacementFlip.test.ts` (15 — 4-state cycle + wrap + distinct combos + defaults + label + form-guard/preventDefault/detach) and `apps/editor/src/engine/views/plantools/__tests__/DoorFlipOnSpace.spec.ts` (4 — SPACE cycles all 4 states and the COMMITTED door's `wall.opening.create` payload carries the previewed swing/hand; Esc resets; unhandled keys pass through). Map to C11 (element-creation pipeline) / C15 (hosted elements) + ADR-0107 (the flip is the hosted-element counterpart the ADR named but left open). No wall JOIN geometry touched. P8 — the new state is a pure DOM/preview class with no I/O (mirrors `PrePlacementRotation`, which carries no span for the same reason); the mutation path stays the existing spanned `wall.opening.create` handler. |
| L-93 | founder | **L-corner + DOOR on one arm near the corner → plan footprint spike/notch** (red arrow in the image) instead of a clean mitre. The corner mitre breaks specifically when an opening is close to it. | Geometry/Walls | BROKEN→FIXED | `§MITER-SEGMENT-CLAMP` (ADR-0055/C15). **Root cause (verified by building the actual miter-prism geometry):** opening-bearing walls render via the SEGMENTS path (`buildMiterPrism` per body segment between openings), NOT the plain-wall V2 footprint (the two-render-paths seam: plain→CSG single-volume, opening→segments/MiterPrism). The miter NORMAL already matches V2 (both 45° at a 0.2×0.2 L), but when the door sits within a half-thickness (the 45° miter reach ≈ 100 mm) of the corner, the tiny wall sliver between the door jamb and the corner is shorter than the miter reach, so the miter's INNER cap vertex projects BACKWARD past the jamb into the door void → a self-intersecting triangular spike/notch in plan (reproduced: door 50 mm from corner → footprint minX 4.900 < jamb 4.950). **Fix:** in `buildMiterPrism`, clamp each projected cap vertex so it may extend PAST its own end (the legitimate outer overhang that meets the neighbour's face) but NEVER retreats past the OPPOSITE miter plane along the wall axis. The near-corner sliver is now a clean, positive-area, non-self-intersecting prism (outer face mitres to the corner, inner face stops at the jamb) — no spike into the void. Segments longer than the miter reach are byte-identical (clamp never triggers), so all layered/curved miter-prism walls are unaffected. Tests: geometry-wall `MiterPrismBuilder.nearCornerOpening` (5 — door 10/20/50 mm from corner: no spike behind jamb + positive-area + non-self-intersecting + outer overhang preserved; a far door keeps the full mitre; deterministic on reopen). 150 geometry-wall + 117 ai-host tests green. ADR-0055/C15. |
| L-94 | founder | **[RECURRENT, HIGH] New wall created ON an existing L-joint corner still renders a WRONG/broken joint (should be flush like the preview)** — two walls already mitred in an L (inglete); adding a NEW wall onto that joint corner (or clicking the midpoint intersection of the two walls) produces a broken/spiky joint in the EXECUTED result (image 1), NOT the clean perpendicular partition sitting adjacent/pegado to the two initial walls that the PREVIEW shows (image 3). Reported multiple times. L-91 (§FIX-NEWWALL-LCORNER-BIAS) fixed the pinned-3rd-wall baseline TILT, but this joint-cleanliness/flush case at the L-corner is still wrong. | Geometry/Walls | FIXED §FIX-NEWWALL-LCORNER-FLUSH (9960fcbe) | routed → SOLE WALL AGENT (continues L-91/L-93/L-61/L-63/L-74/L-76). The new wall must join the existing L cleanly — its near face flush against the corner, both existing arms' mitre preserved, no spike/gap/overlap, executed == preview — including when the new wall's endpoint snaps to the exact corner node OR the midpoint of the two-wall intersection. Reproduce that exact topology; assert clean flush footprint (positive-area, non-self-intersecting, no spike) fresh + on reopen; executed baseline == previewed. ADR-0055/C15. |
| L-95 | founder | **Element-TOOL parity broken between MAIN plan view and SPLIT plan view — must be CONTRACTUAL** — the ContextualEditBar element tools (Move / Rotate / Scale / Copy) work in the MAIN plan view but NOT in the split-view plan pane. Repro (log): select a window → ContextualEditBar 'Move' → `[MoveTool] Activated`, `[PlanViewToolOverlay] setActiveTool → move`, MoveTool origin/destination → `MOVE_WINDOW` — this fires in the MAIN plan view; in the SPLIT plan pane the same tools don't work. Founder: both plan views must behave identically, always, and this parity must be CONTRACTUALLY explicit. (L-73 §FIX-PLAN-VIEW-PARITY unified the DRAW/annotation tools + proved direct-click-drag parity, but the ContextualEditBar element-transform tools are attached only to `PlanViewToolOverlay`, not `SvpPlanToolOverlay`.) | Plan-tools UX / Contract | FIXED §FIX-PLAN-ELEMENT-TOOL-PARITY (3f5f52e6) | routed → PLAN-TOOLS LANE (same agent as L-73/L-66). Extend the L-73 shared `planToolHandlerRegistry` so the ContextualEditBar element tools (move/rotate/scale/copy) route through BOTH overlays identically (SvpPlanToolOverlay.setActiveTool must accept + drive the same MoveTool/RotateTool/etc. against the split canvas, like PlanViewToolOverlay). Write a CONTRACT clause (extend C11/plan-tools contract or a short ADR) stating: 'both plan surfaces (main PlanViewToolOverlay + split SvpPlanToolOverlay) expose the identical element-tool + interaction capability set, enforced by a single registry.' Add a parity test asserting each ContextualEditBar tool activates + commits the same command on BOTH overlays. Map to C11; P8 |
| L-96 | founder | **Split-view 3D pane must MATCH the main 3D view exactly (true mirror) — currently misbehaves** — the right-hand split pane in 3D mode is 'absolutely not well behaving'. Founder: it should be basically the SAME view as the main 3D — any change on the main 3D view (geometry, camera, selection, edits) must ALWAYS be present on the split 3D pane, and interactions must behave identically. Log root: the split 3D pane doesn't render a synchronized 3D viewport — it FORWARDS clicks to the main canvas (`[SplitViewManager] 3D click forwarded → main canvas (svp X,Y → main X,Y)`), a fragile coordinate-remap mirror → stale hover anchors (mem select-svp3d-stale-hover-anchor), repeated `Escape → deactivateAll`, and drift from the main view. | Split-view / Rendering / Contract | FIXED §FIX-SPLIT-3D-MIRROR (355e19e5) | routed → SPLIT-VIEW LANE (same agent as L-95; do AFTER L-95). Make the split 3D pane a TRUE mirror of the main 3D view: render the same scene from a synced camera (or blit/share the main 3D render) so it always reflects the main 3D state; replace/harden the click-forwarding coordinate remap so picking + interaction land correctly (no stale anchor, no Escape flood). Extend the L-95 view-parity CONTRACT clause to cover the 3D split pane (main 3D ≡ split 3D). Map to C04 rendering + the split-view/L-40 site-view design + the vision (docs/03_PRYZM3); P8. Do NOT touch wall geometry. Add a test: a change to the main 3D scene/camera/selection is reflected in the split 3D pane; a split-pane pick resolves the correct element. |
| L-97 | founder | **BLOCKER — wall with a hosted door → move the wall → app FREEZES** (a DIFFERENT root than L-63's room-redetect loop). Founder stack: an infinite `requestAnimationFrame` loop in the WALL-REBUILD flush — `[WallJoinResolver] §SELF-CLUSTER-GUARD: skipped … → _handleMultiWallClusters → resolveLevel → _flush → (anonymous)@engineLauncher → tick → requestAnimationFrame → scheduleNext → tick →` repeating forever. `WallRebuildCoordinator._flush` re-arms every rAF frame and never converges → main thread pegged. | Geometry/Walls + Orchestration | BROKEN→FIXED | `§FIX-WALLFLUSH-NOPROGRESS-GUARD` (ADR-0117 sibling; ADR-0055/ADR-0099 context). **Root cause:** `_flush` writes each resolved wall's baseline back to the store (`store.update`); that mutation fires a BUFFERED `wall:update` that re-arms `_scheduleFlush` AFTER `_joinsResolving` clears. §PRESERVE-IDEMPOTENT already stopped the *preserve* branch from re-writing, but when the moved door-bearing wall clusters with a §SELF-CLUSTER-GUARD wall (a wall whose BOTH endpoints fall in one junction cluster — e.g. a short stub / degenerate wall near the corner), the moved wall's resolve is **non-idempotent** — the `_bMoved && !_preserve` branch writes a (re-)trimmed baseline every flush → re-arm → freeze. **Fix (mirrors L-63's `§FIX-ROOMREDETECT-NOPROGRESS-GUARD`, but for the wall flush):** `_flush` is a pure function of the level's wall REBUILD inputs. Added (1) a per-level **no-progress gate** at the TOP of `_flush` (before both the openings-only fast path and the whole-level path): if every affected level's rebuild signature (id + baseline @ mm + thickness + height + baseOffset + openings + layers + curve + material) is byte-identical to what the last completed flush built, the flush is skipped entirely — no resolve, no `store.update`, no `bim-wall-mutation-committed` → the loop terminates after ONE flush; (2) a **circuit-breaker** tripping if the same signature reaches `_flush` > 8× within 1 s (any re-arm source / residual oscillation). The signature covers material/height/baseOffset so a legitimate material or elevation edit (join geometry unchanged) still changes it and is NEVER suppressed. The gate skips the re-armed write regardless of whether the resolver would produce a stable or an oscillating baseline (it compares the CURRENT store geometry to the last built output). Tests: `apps/editor/__tests__/wallFlushNoProgressGuard.test.ts` (2 — a wall-move-with-door converges in bounded frames, not a per-frame runaway; N no-op re-arms on unchanged geometry cause ZERO extra rebuilds while a genuine edit still rebuilds). 39 existing wall-coordinator/freeze/preserve tests + the new suite green. ADR-0117 (extended) / builds on ADR-0099 / ADR-0055. |
| L-98 | founder | **[RECURRENT] Split-view plan wall creation drops the wall TYPE — creates a PLAIN wall, not the LAYERED interior type** — reported before, still not solved. User picks Wall tool → picks Interior (layered) wall type → draws in the SPLIT-VIEW plan → the wall gets the right thickness but is a plain wall (single volume) instead of the layered wall it should be. ROOT from log: `[WallTool] Dispatching wall.create via runtime.bus … systemTypeId=none thickness=0.2` — the active `systemTypeId` (which carries the layered composition) is NOT threaded into `wall.create` from the split-view `SvpPlanToolOverlay` path; it resolves to `none`, so `CreateWallCommand` builds a plain wall. In the MAIN plan view the selected systemTypeId is passed through correctly → parity gap. | Walls / Split-view parity | FIXED §FIX-SPLIT-WALL-SYSTEMTYPE (71ee8066) | routed → PLAN-TOOLS/split-view agent (owns L-66 arm + L-95/L-96 split parity). Thread the active wall `systemTypeId` from the split-view plan tool into `wall.create` exactly as the main plan view does — resolve the selected wall-type at dispatch (not `none`), so the layered composition is built. Tie into the L-95 view-parity CONTRACT (main plan ≡ split plan for element authoring, INCLUDING the applied system type, not just thickness). Add a test: create wall in split view with an Interior layered type → dispatched `wall.create` carries that systemTypeId → wall is layered. §FIX-SPLIT-WALL-SYSTEMTYPE. ADR-0055; C16 wall authoring; no shortcuts; P8 |
| L-99 | founder | **[HIGH] 3D element SELECTION corrupted — main 3D view (when split enabled) AND split 3D view; PLAN VIEW WORKS PERFECTLY** — user clicks directly on an element in 3D and the selection often does NOT trigger. Two roots in the log: (1) **GPU-pick target size desyncs from the on-screen 3D viewport across split toggles** — `[GpuPick] §SELECT-PICK-RESOLUTION target=725x976` while split is active vs `target=2037x976`→`2048x981` after `[SplitViewManager] Split view deactivated`; when split resizes/reclaims the 3D canvas the pick buffer is sized to a stale/wrong viewport, so the screen→pick pixel mapping is off and clicks sample the wrong pixel → miss. (2) **Gizmo attaches to a DETACHED object → per-frame flood** — `[SelectionManager] §SELECT-GIZMO-REATTACH gizmo attached to a detached object — detaching to stop the per-frame flood` + `TransformControls: The attached 3D object must be a part of the scene graph`; after an edit rebuilds a mesh the selection still references the disposed Object3D, corrupting subsequent picks/state. Plan view is immune because it selects id-based (`[PlanViewInteraction] Element selected via plan view click`), not via GPU pick — confirms the 3D GPU-pick path is the culprit. Founder asks to study pascalorg/editor selection for robustness insights. | Selection / Picking / Split-view | FIXED §FIX-3D-SELECTION-ROBUST (4c21c36a) | routed → PLAN-TOOLS/split-view agent (owns L-96 split-3D + picking hardening) — do AFTER L-96, single owner of SelectionManager/gpu-pick/SplitViewManager. Fix: (a) resize the GPU-pick target to the ACTUAL live 3D viewport on every split enter/exit + canvas resize (drive pick target from the same viewport rect the renderer uses, not a cached size); (b) make selection resolve through a Scene Registry (id→Object3D, mem pascalorg-editor-research / OI-058) so a rebuilt/disposed mesh re-resolves to the current object instead of a stale one — kill the detached-gizmo flood at the source (re-attach gizmo to the current object by id after rebuild, or clear selection cleanly). Compare pascalorg/editor selection model for a more robust id-based pick. Add tests: pick in 3D while split active resolves the element under the cursor; edit→rebuild keeps a valid selection/gizmo. §FIX-3D-SELECTION-ROBUST. C04 rendering/scheduling + picking; ADR for GPU-pick; no shortcuts; P8 |
| L-99b | founder | **[HIGH, follow-up to L-99] Cannot select a DOOR in 3D at all (MAIN 3D too, not only split) - hosted door LOSES the pick to its host wall** - user tries from many angles, door never selects. Log proof: every hover/click resolves to the WALL or FLOOR, NEVER the door: `[PickResolver] hover-anchor hit=wall_...` repeatedly + `[PickResolver] §97 click hit type=wall ... strategy=gpu-pick` / `type=floor dist=52.22` - there is no `type=Door` hit even when the cursor is on the door. So the GPU-pick/PickResolver resolves the large host wall (and slab/floor) over the small hosted door. Founder asks to ADD MORE diagnostic console logging to understand why 3D selection is so bad. | Selection / Picking / Hosted elements | FIXED §FIX-3D-DOOR-PICK-PRIORITY (4c21c36a) | routed -> PLAN-TOOLS/split-view agent (owns L-99). (1) ADD DIAGNOSTICS: log ALL pick candidates under the cursor with their type+id+depth (not just the winning hit), the pick-target size vs viewport, and whether the door mesh has a GPU-pick id registered - so the failure is observable. (2) FIX hosted-element pick PRIORITY: a hosted door/window under the cursor must WIN the pick over its host wall (and over floor/slab) - either depth-nearest already favours it (then why is wall winning? verify the door mesh is even in the pick target / has a pick id) or add a hosted-element priority/epsilon so the door is selectable. Tie to L-99 Scene-Registry id->Object3D + pick-target-resize work. Verify door has a userData.id / GPU pick id (cf memory 3d-selection-instanced-gpu-pick-gap: instanced elements lack group userData.id -> GPU id-pick misses them). Add test: click on a wall-hosted door in 3D selects the DOOR, not the wall. §FIX-3D-DOOR-PICK-PRIORITY. C04 + picking; ADR for GPU-pick; P8 |
| L-100 | founder | **[PERF/ROBUSTNESS] Furnishing BATCH creation is too slow + 3D view becomes unmanageable afterwards** — furnish-all takes too long and the viewport is laggy/unusable after. Root signals: furniture is created effectively per-element (placeholder meshes, each with its own material) so the scene crosses the mesh budget (`[SceneQualityTier] 1213 meshes → tier=performance`) and GPU instancing is defeated (known per-element-unique-material issue, mem webgpu-heavy-scene-crash-and-instancing); the batch path likely produces N commands / N rebuilds instead of one. | Furniture / Batch / Perf | **FIXED** `§FIX-FURNISH-BATCH-PERF` (b5f403a5) | ROOT CAUSE: the auto-furnish path (`FurnishLayoutExecutor`) dispatched ONE `furniture.create` bus command **per placed item**, so furnishing a whole apartment ran N separate `produceCommand`s — each an Immer draft + structural snapshot that is O(current store size) → **O(N²)** total, which made "furnish all" slow (300 items ≈ 380 ms + 300 undo entries). FIX (mirrors the proven `wall.batch.create`/`slab.batch.create` pattern): (1) new `furniture.batch.create` command type (`command-bus/commands.ts`); (2) `CreateFurnitureBatchHandler` (`plugins/furniture`) — **ONE `produceCommand` over one Immer draft writes all N items** → one forward+inverse patch → one undo entry (O(N)); registered in the handler set; `withHandlerSpan` (P8); (3) `CommandEventBridge` gains a `furniture.batch.create` case that **fans out one `furniture.created` per item** so the §FT-FURNITURE legacy-store→builder render path (3D mesh + plan symbol) is byte-identical to the single-create path; (4) `FurnishLayoutExecutor` now maps its `set.commands` to ONE `furniture.batch.create` dispatched inside the existing `batchCoordinator.runBatch` — keeps the BatchCoordinator render/reprojection suppression + single flush (`skipRedetectRooms` — furniture isn't room-bounding; `skipPbrUpgrade` — §POSTGEN-PERF). MEASURED (real plugin CommandBus, N=300): before ≈ 382 ms / **300 undo entries** → after ≈ 30 ms / **1 undo entry** (~13× faster; O(N²)→O(N), asserted structurally by undo-stack depth 300→1). NOTE: furniture geometry **instancing** (the `1213 meshes → tier=performance` mesh-count explosion) is deferred — the flag-gated `§PERF-WEBGPU-FURNITURE-INSTANCING` bridge already exists in `FurnitureFragmentBuilder` and materials are already shared centrally by `MaterialService` (cache-by-colour); enabling/expanding instancing is a renderer-side change tracked with L-102. C11/C17/C04; P6/P8. Tests: `plugins/furniture/__tests__/createFurnitureBatch` (4) + `furnishBatchPerf.bench` (undo-depth 300→1). Root `tsc --skipLibCheck --noEmit` exit 0. |
| L-101 | founder | **[FUNCTIONAL] "Furnish furniture in ALL apartments on ALL floors" does not furnish them all** — user selects furnish across all apartments/all floors but it doesn't do it (only some/none). `pryzmFurnishAllFloors()` is registered but the multi-floor / multi-apartment fan-out doesn't cover every unit. | Furniture / Coverage | **FIXED** `§FIX-FURNISH-ALL-FLOORS-COVERAGE` (b5f403a5) | ROOT CAUSE: the all-floors driver (`triggerFurnishAllFloors`) furnished each floor by MUTATING the global `projectContext.activeLevelId` as a side-channel and the executor read it back via `resolveActiveLevel()` — when the switch didn't land (or the loop advanced before it settled) a floor was furnished with the WRONG/previous level's rooms, so units were skipped; additionally a single room throwing inside the executor aborted the ENTIRE level, and the no-rooms path returned WITHOUT emitting `furnish.layout-executed`, stranding the driver on a 12 s per-storey timeout. FIX: (1) `furnish.layout-execute` now carries an explicit `levelId` — the executor furnishes THAT level deterministically (new `resolveLevelById()`), no active-level dependence (`setActive` still runs for the UI/HUD but correctness no longer hinges on it); (2) per-room `try/catch` — one room throwing is recorded as a skip and the loop CONTINUES (never aborts the level or the run); (3) per-unit **coverage report** — every unfurnished room records an explicit reason (`no archetype for occupancy=…`, `degenerate polygon`, `furnish threw: …`), surfaced on `furnish.layout-executed` (`roomsFurnished`/`roomsSkipped`/`skipped[]`); the no-rooms path now emits the event so the loop advances immediately; (4) new **pure `furnishAllFloorsDriver`** (`driveFurnishAllFloors` + `summariseFurnishCoverage`, both P8-spanned) — enumerates EVERY level, tolerates a per-level failure, and rolls a per-unit report (furnished / skipped / timed-out) logged as `§COVERAGE-ALL-FLOORS`. The executor already iterates ALL rooms on a level (covering every apartment on that floor), so N floors × M apartments are now all reached. C11; P6/P8. Test: `apps/editor/__tests__/furnishAllFloorsCoverage` (4 — every level×apartment furnished; one level throwing does NOT abort the rest; per-unit skip reasons + timeouts surfaced; empty level list safe). Root `tsc` exit 0. |
| L-102 | founder | **[AUDIT+PLAN] Batch-creatable furniture/accessory elements are NOT exposed via UI + unknown contract compliance** — batch furniture creation can produce many element types (furniture, TV, bathroom mirror, curtains, and more) that have NO button in the right-hand tools bar, so users can't place them manually. Also unknown whether these newer element types are compliant with docs/02-decisions (contracts / ADR / spec). Founder asks: AUDIT + ANALYSE + REVIEW + PLAN → produce an implementation plan to (a) surface every batch-creatable type via UI in the most relevant right-hand toolbar section, and (b) confirm/achieve contract compliance for each. | Furniture / UI / Contracts | OPEN (audit+plan) | routed → FURNITURE-UI-AUDIT agent (READ+PLAN only, no code edits — furnish agent owns the furniture command path for L-100/L-101). Deliverable: (1) full enumeration of batch-creatable furniture/accessory types (from the batch catalogue / FurnitureType union / catalogue entries) vs what the right-hand toolbar exposes today → the gap list; (2) per-type compliance check against docs/02-decisions (contract/ADR/spec) — flag any type lacking a schema/contract home; (3) identify the correct right-hand toolbar section + UX for each; (4) a phased implementation plan written into V1-LAUNCH-IMPLEMENTATION-PLAN.md. C11 element-creation, C17 batch catalogue, C03 schemas; memory family-platform-strategic-direction (FurnitureType union), ai-creation-default-element-types-queue. No shortcuts; every proposed change contract-mapped |
| L-103 | founder | **[UI POLISH] "3D Site / Globe" launcher pill floats awkwardly mid-canvas** — the always-on 3D Site launcher (L-40, `[gis][site-view] always-on 3D Site launcher mounted`) sits in the middle-left of the canvas instead of being cleanly docked. Reposition it to a deliberate anchor (docked to the left tool rail / a proper canvas corner) so it reads as a real control, not a floating stray. | Site/GIS UI | FIXED (`§FIX-SITE-LAUNCHER-POSITION`, SHA `b0e97d0a`) | GIS/SITE agent. Re-anchored the always-on `#pryzm-site-view-launcher` (GISAreaLayout.mountSiteViewLauncher) from the mid-left `top:56px/left:220px` (which stranded mid-canvas when the ViewBrowser left panel is collapsed/absent) to the **bottom-left canvas corner** (`position:absolute; bottom:48px; left:10px`), stacked just above the bottom-left GPU renderer toggle (`bottom:10px`) so the two corner controls form a clean left-edge stack with no overlap. Kept always-on + idempotent + brand-styled. Root tsc gate green. |
| L-104 | founder | **[FEATURE] Add a "Plan View GIS" option — plan view WITH the real building/GIS context** — user wants to view the PLAN VIEW showing the real building on GIS (real-world context, project-north, orthographic), analogous to the 3D Site/Globe but for plan. Founder: analyse → review → plan → implement. | Site/GIS + Plan view | FIXED — plan written + implemented (`§FEAT-PLAN-VIEW-GIS`, SHA `b0e97d0a`) | GIS/SITE agent. Plan in V1-LAUNCH-IMPLEMENTATION-PLAN.md (§"L-104 — Plan View GIS", 4 phases, contract-mapped C12/C19/C20/C22/C23 + ADR-0115). Implemented a **"▦ Plan + Site"** always-on launcher (bottom-left, paired with the L-40 3D-Site pill) + typed global `window.pryzmEnterPlanViewGis`: switches to the orthographic **Top** view (the building already projects to plan — no new projector) and composites the real-world **GIS context** UNDER it as a plan-canvas underlay, on **PROJECT NORTH**. REUSE: new `buildSiteGisContextRaster` composites keyless **ESRI World Imagery** tiles (same endpoint as Cesium + the 2D satellite basemap — no CSP change) → placed via the EXISTING L-71 `createPlanCanvasUnderlayFromSiteOverlay` pipeline (`FloorPlanUnderlayTool` + `CreateUnderlayCommand`, P6/undoable), centred on the site origin, rotated `rotationZ = θ` (`SiteLocation.trueNorth`, ADR-0115 dual-north; θ=0 ⇒ identity). Pure tile-math + rotation in headless `siteGisContextGeometry.ts` (10 unit tests, ADR-0115-documented spanless L5 geo-math family); impure raster builder carries 1 OTel span (P8). Degrades gracefully (no site ⇒ plan only; imagery/CORS failure ⇒ toast + building only). No parallel engine; SplitView/selection/wall untouched. Root tsc gate green. |
| L-105 | founder | **[FEATURE] Floor-finish AND ceiling creation panel has NO type selector - user cannot pick the finish on creation** - the FLOOR FINISH 'Set parameters before creating' panel shows only AREA, ASSEMBLY THICKNESS, BASE OFFSET (screenshot) - no way to choose WHICH floor finish. The finish types already exist (a dropdown lists ~19: Porcelain Tile, Marble, Carpet Tile, Engineered Timber, Solid Oak, LVT, Rubber Sports, Epoxy Resin, Underfloor Heating in Screed, Raised Access, Wet-Area Tanked, Oak/Smoked/Walnut Herringbone/Chevron Parquet, Scandinavian Oak, Carrara/Calacatta/Marquina Marble...) but ONLY post-creation. Same for CEILING. | Floor-finish/Ceiling UI | **FIXED** `§FEAT-FLOOR-CREATE-TYPE-PICKER` (SHA `a2939453` — my scoped floor/ceiling/modal files + 2 tests were swept into the concurrent GIS-agent commit on the shared branch; code is byte-clean in HEAD) | FLOOR-FINISH agent. Added a finish/assembly **TYPE dropdown** to the shared "Set parameters before creating" panel (`ElementCreationModal`, used by BOTH the floor-finish AND ceiling create tools — so ceiling is covered identically, NOT a follow-up). Catalogue is passed IN by the tool (store-free UI, §05-BIM-UI §1.5): `FloorTool._resolveFloorTypeOptions()` / `CeilingTool._resolveCeilingTypeOptions()` read the SAME `floorSystemTypeStore` / `ceilingSystemTypeStore` the post-creation FLOOR TYPE dropdown reads (single source of truth). Selecting a concrete type auto-fills the thickness field with that type's assembly total and returns `systemTypeId` via `onConfirm`; the tools thread it into `_pendingSystemTypeId`, and the EXISTING `_createFloor`/`_createCeiling` logic resolves that id → its layer snapshot and dispatches CREATE (P6) — so create and the L-106 swap share ONE type→layers resolution. "— Plain Floor/Ceiling —" → undefined (existing plain default). Also added the missing `setSystemTypeId` alias both tools' callers (BimService, mode picker) already invoked, so the pre-draw type pre-selection sticks + pre-fills the dropdown. Test `apps/editor/__tests__/elementCreationTypePicker.test.ts` (6: render-from-catalogue, auto-fill+return-id, plain→undefined, pre-select, ceiling parity, no-picker-without-catalogue). Root tsc `--skipLibCheck --noEmit` exit 0. C11/C17; helpers are module-private/methods (no new exported fn → no P8 span needed). |
| L-106 | founder | **[BUG] Floor-finish TYPE SWAP does not work - selecting a created floor finish and changing its type is rejected** - user selects a floor finish, picks another type, Apply -> nothing. Log: `[PropertyPanel] floor.updateLayers failed: CommandBusError: floor.updateLayers: canExecute rejected - floor not found: b13edb7e-7484-...` even though that id was JUST created (`[FloorTool] Floor created: b13edb7e-...`). So `floor.updateLayers.canExecute` looks in the WRONG store (or wrong id space) - the floor-finish element exists but the command can't find it. | Floor-finish / Commands | **FIXED** `§FIX-FLOOR-TYPE-SWAP` (SHA `7b5dbc48`) | FLOOR-FINISH agent. ROOT CAUSE: the property-panel FLOOR TYPE Apply dispatched the `floor.updateLayers` bus command, whose plugin handler resolves the floor from the **plugin Immer floor store** — a DETACHED store that is EMPTY for FloorTool-created floors (the 3D `FloorTool` writes the **legacy** `core-app-model` `FloorStore` via `cm.execute(CreateFloorCommand)`, not the bus create handler), so `canExecute` rejected "floor not found: <id>" moments after `[FloorTool] Floor created`. FIX (mirrors wall/door/window ADR-0105 uniform surface): route the floor swap through the `element.changeType` command, whose new **floor branch** runs the legacy `UpdateFloorLayersCommand` on the legacy `FloorStore` — the store the 3D tool AND the plan-view §P3.2-FL bridge both populate, and the one that drives `FloorFragmentBuilder`. `floorStore.update()` fires `bim-floor-updated` → mesh rebuilds with the new assembly + material. Ring-buffer parity (§FIX-FURNITURE-TYPE-LIST-AND-UNDO L-68): pushes an invertible whole-element replace PatchPair on the stable id so ring-first Ctrl+Z reverses THE SWAP (not the element's earlier CREATE); cm twin shadow-dropped (one undo). Test `apps/editor/__tests__/floorTypeSwap.test.ts` (4: command FINDS the floor + applies new type/layers/thickness; still rejects a genuinely-missing floor; ring-buffer undo reverses the swap in place + redo re-applies; a further undo reverses the create — real RingBufferUndoStack + elementUndoStoreAdapter + FloorStore). Root tsc gate green. C03/C11; ADR-0105; P8. |
| L-107 | founder | **[REGRESSION from L-11] Grey rectangular plane visible on an EMPTY project - the ground shadow-catcher is not invisible** - right after project creation a grey rectangle appears in 3D (screenshot), then disappears as soon as the first element is created. It is the L-11 §FEAT-REAL-ENVIRONMENT ground shadow-catcher (log: `[RealEnvironmentService] §FEAT-REAL-ENVIRONMENT enabled - site=default ground=on` / `ground shadow-catcher active`) - the receive-only ShadowMaterial plane at L0 is rendering as visible grey when nothing casts a shadow (on WebGPU ShadowMaterial with no shadow map / no casters can render opaque), instead of being fully transparent/absent. | Rendering / Environment (L-11 regression) | **FIXED** `§FIX-GROUND-CATCHER-INVISIBLE-WHEN-EMPTY` (SHA `5e33da9c`) | Fix (option a — deterministic): the catcher is in the scene graph ONLY while there is shadow-CASTING geometry to catch a shadow from, so an empty project shows no plane at all (the ShadowMaterial is never rendered with a no-caster/empty shadow map). `RealEnvironmentService.enable()` no longer attaches the catcher; it defers to `_syncCatcherPresence()` gated on `setSceneHasCasters()`. `initScene` detects the caster set on the same debounced cadence as the Pascal shadow-flag pass (`bim-*-added/updated/removed`, 120ms) — traverses for any `castShadow` mesh (excluding the catcher), attaches on first caster, detaches when the scene goes empty. `setGroundShadows()` also honours the gate. Reversible with NO GPU dispose (detach only removes from the scene graph; geometry/material retained) -> ADR-0111 shadow-freeze + L-11 device-loss protections untouched. Test `RealEnvironmentService.groundCatcher.test.ts` (5). Root tsc clean. ADR-0106; P8 |
| L-108 | founder(log) | **[PERF REGRESSION?] Empty project takes ~16s to load - LOAD-WATCHDOG fires repeatedly** - on a brand-new empty project the loader logs `§LOAD-WATCHDOG load still running after 7.4s/10.2s/15.2s - phase="setup" stuck` and `§LOAD-PHASE name=hydrate total=16081ms` for 0 elements. A 0-element project should load near-instantly; something in setup/hydrate blocks ~16s. Possible L-11 init interaction (RealEnvironmentService / site read / ground catcher at initScene) or an unrelated setup stall. | Perf / Project load | **FIXED** `§FIX-EMPTY-LOAD-HANG` + `§DIAG-EMPTY-LOAD-HANG` (SHA `1fc44135`) | **Finding: not the L-11 environment code.** Evidence: (1) `§FEAT-REAL-ENVIRONMENT` lives entirely in `initScene.ts` (engine BOOT, one-time) + 3 rendering-package files; **`ProjectLoader.ts` was never touched by L-11/L-107**. (2) The ~16s is inside `ProjectLoader.load`'s setup->hydrate window (lines 460->1744) — a DIFFERENT call site my boot-time init never runs in. (3) For 0 elements NO `bim-*-added` events fire, so the L-107 `_syncGroundCasters` debounce + caster traverse never execute; `enable()`/`bind()`/`GroundShadowCatcher` creation are synchronous O(1) (a 4-vertex 4000x4000 plane + NOAA arithmetic) — nowhere near 16s. (4) **Likely real culprit:** the hydrate window runs several COLD `await import(...)` of plugin packages regardless of element count — `@pryzm/plugin-annotations` (x4: constraintStore, annotationVisibilityStore, obcAnnotationAdapter, AnnotationDependencyGraph.rebuild ~1694-1736), `@pryzm/file-format` dxfOverlayStore (~1657), `ViewTemplateToIntentMigration` (~1500). On the Vite **dev** server (MEMORY: localhost dev unusable — dev starves the event loop transforming Cesium/THREE/46 plugins) those first-use transforms are the fixed ~16s stall independent of 0 elements; `phase=setup stuck` vs `name=hydrate total=16081ms` is the watchdog naming the last-COMPLETED phase (setup) while the hydrate body runs. **Recommend:** confirm on prod `pryzm.fly.dev` (pre-built bundle) — if the 16s vanishes there it is the dev-server transform cost, not a runtime regression; else route to the LOAD/persistence agent to lazy-defer those annotation/DXF/migration dynamic imports. C05/C13 load; P8  **FINDING (env agent): NOT L-11** — ProjectLoader untouched by L-11/L-107; boot-time env init is O(1); no bim-* events fire for 0 elements so the caster-sync never runs. **NB: the log is from `scope: https://pryzm.fly.dev/` = PRODUCTION**, so it is NOT merely dev-transform cost — it is a real prod hydrate stall. Likely culprit: cold `await import()` of plugin chunks during the hydrate window (@pryzm/plugin-annotations ×4, file-format dxfOverlayStore, ViewTemplateToIntentMigration) fired regardless of element count. Re-routed → LOAD/PERSISTENCE agent to lazy-defer those dynamic imports off the hydrate critical path. §DIAG-EMPTY-LOAD-HANG. C05/C13.  **FIX (LOAD-PERF agent, SHA `1fc44135`):** CONFIRMED root cause = element-count-INDEPENDENT work in the setup→hydrate window (the element loops are no-ops for 0 elements). Two contributors, both removed from the empty-load critical path: **(1) Cold dynamic imports fired on EVERY load** — `@pryzm/plugin-annotations` was ALREADY statically imported (`annotationStore`, line 65), so the 3× `await import('@pryzm/plugin-annotations')` (constraintStore / annotationVisibilityStore / obcAnnotationAdapter) + the `await import('./migrations/ViewTemplateToIntentMigration')` were pointless indirection: each yields the event loop and in a prod build can trigger a lazy-chunk fetch. Converted all four to **static imports** (same module graph, already loaded → no lazy fetch, no await yield; restore logic byte-identical; DXF `@pryzm/file-format` import left dynamic + gated since it is NOT otherwise loaded and only needed when overlays exist). **(2) Frame-yielding chunked executor run for a 0-element snapshot** — chunking (C11 §6.1) exists only to spread heavy per-element geometry across frames; with no geometry the chunked driver still performs several FrameScheduler round-trips (`await yieldFrame()` per build-step), pure latency that stalls on the frame loop. New exported pure **`snapshotHasElements()`** gates the path: elementless snapshot → synchronous one-task import (no frame round-trips); any geometry → progressive chunked build unchanged. **Watchdog mislabel explained + kept honest:** `phase="setup"` came from `Object.keys(__phase_starts).pop()` naming the last STARTED boundary (setup) while the hydrate body ran; added fine-grained `§LOAD-HYDRATE-STEPS` sub-step timing so future prod loads attribute the budget precisely instead of the single coarse `hydrate` boundary. **Before/after:** empty load previously blocked ~16 s (3 watchdog cries at 7.4/10.2/15.2 s, `hydrate total=16081ms`); after, the empty path does ZERO dynamic imports and ZERO frame round-trips, so it completes well under the 7.4 s first-watchdog threshold (no lazy-chunk fetch / no frame-loop wait on the critical path). Projects WITH annotations/DXF/view-templates still restore correctly (same code, static bindings; DXF still lazy-loaded when overlays present). Test `apps/editor/__tests__/emptyLoadSkipsChunk.test.ts` (6) + `loadRedetectFreeze` (5) green; root `tsc --skipLibCheck --noEmit` exit 0. New exported fn is pure (no I/O) → no OTel span required. §FIX-EMPTY-LOAD-HANG. C05/C13. |
| L-109 | founder | **[FEATURE] Project Origin / Base Point (Revit-style) - new element CATEGORY, always-on blue sphere = project origin** - like Revit's Project Base Point (ref: Autodesk RVT 2025 GUID-363D008B). A new, distinct element category: a blue sphere ALWAYS present in the canvas marking the PROJECT ORIGIN; serves as the model-coordination / collaboration center and the center of the project (shared coordinate origin). Must align with PRYZM's View-Intent/visibility strategy (check contracts) and, like any element, be toggleable ON/OFF from the View Intent settings on demand. Founder: analyse the best way + implement. | New element / Coordination / View-Intent | **FIXED** (§FEAT-PROJECT-ORIGIN, 6 phases: 1f658dd4 P1 schema · b3889614 P2 store · 798cd92d P3 blue-sphere marker · 993c028f P4 boot+View-Intent · f7993658 P5 commands) | routed -> PROJECT-ORIGIN agent. **ANALYSIS:** no pre-existing origin ELEMENT — the shared-coordinate datum already lives as `SiteLocation.basePoint` (C19 §1.3 LTP-ENU = ADR-0115 "project base point"), but had NO first-class element, marker, or visibility category; `projectNorth`/`trueNorth` are an ANGLE about that point, not a point element. L-109 adds the missing element+marker+category and ties its position to that datum (full bidirectional basePoint sync = documented follow-up). **SHIPPED:** (1) pure L0 `ProjectOrigin` singleton schema (C03/P5) + Id brand + SCHEMA_REGISTRY; (2) `ProjectOriginStore` L3 singleton (C11) auto-seeded at world origin, re-seeded on project switch, exposed as `window.projectOriginStore`; (3) `ProjectOriginMarker` always-on BLUE SPHERE via renderer-three ONLY (P2), a non-pickable coordination gizmo (raycast off, selectable:false) — NOT a normal selectable/deletable element; (4) `initProjectOrigin` boot wiring + "Project Origin" View-Intent category (P7/C09) toggleable ON/OFF exactly like any element, default ON; (5) `projectOrigin.setPosition`/`.setVisible` via runtime.bus (P6), P8 spans (bus `pryzm.command.execute` + `withHandlerSpan`). C17: system singleton, deliberately NOT a catalogue draw-tool. Plan: V1-LAUNCH-IMPLEMENTATION-PLAN.md 'L-109'. Tests: schema(4)+store(6)+marker(5). Design: (1) L0 schema for a singleton ProjectBasePoint/Origin element (C03, packages/schemas - pure, no THREE/DOM); (2) element-creation/registration through the pipeline (C11) as a project singleton (one per project, at world origin by default, position editable = shared coordinates); (3) render the blue sphere via renderer-three (P2 - THREE only there) as an always-on gizmo/marker that does not participate in normal selection/geometry ops unless intended; (4) integrate with VISIBILITY-INTENT / View Intent (P7 - packages/visibility is domain intent, NOT UI state; VG governance + view-intent-instance store) so it is a toggleable layer in the View Intent settings panel exactly like other categories; (5) coordination semantics tie to ADR-0115 dual-north / shared coordinates (project origin = shared-coordinate datum). Mutations via commandBus (P6). Analyse + write a phased plan into V1-LAUNCH-IMPLEMENTATION-PLAN.md (heading 'L-109 - Project Origin / Base Point') BEFORE/as implementing; map every step to the contracts (C03/C11/C17 + the view-intent/visibility contract + ADR-0115) and the vision (docs/03_PRYZM3). P8 spans on new exported fns. No shortcuts |
| L-110 | founder | **[FEATURE] Guarantee 4 default ELEVATIONS (North/East/South/West) on every project startup** - follow-up to L-109. Today DefaultViewsManager guarantees a Ground Floor plan + a 3D view on every project (log: `[DefaultViewsManager] ... default views are guaranteed on every project`). Add the 4 cardinal elevations (N/E/S/W) so every project ALWAYS starts with Ground Floor + 3D + 4 elevations. They must be DELETABLE by the user but always (re)created on startup, and must be architecturally SOUND (real, correctly-oriented, projecting elevation views - not empty stubs). | Views / Default scaffolding | **FIXED** (§FEAT-DEFAULT-ELEVATIONS, 93a2f274) | routed -> PROJECT-ORIGIN agent (single owner of DefaultViewsManager). **SHIPPED:** `DefaultViewsManager.ensureDefaultViews()` now guarantees 4 elevation ViewDefinitions `vd-sys-elev-{north,east,south,west}` (viewType `elevation`) alongside plan+{3D}, mirroring their exact pattern (`createdBy:'system'` ⇒ no undo pollution per §01 §2; `_ensureVgBridge` + `_ensureDefaultIntent`). Each carries `spatial.projectionDirection` = the project-north `VIEW_PROJECTION_DIRECTIONS` presets — the SAME shape EdgeProjectorService reads for the shipped documentation-set N/S/E/W elevations, so each projects a REAL elevation (not a blank stub); oriented to PROJECT NORTH (ADR-0115: plan/authoring frame IS project north). Deletion guard extended so deleting any elevation re-creates it. +3 tests (present-on-startup, direction/projection soundness, delete→re-create). Extend DefaultViewsManager to create 4 elevation ViewDefinitions (North/East/South/West) via the ViewDefinition store + elevation view type / view.createDefinition (P6 bus), oriented to PROJECT NORTH (ADR-0115 dual-north - N/E/S/W relative to project north), alongside the existing plan+3D. Deletable but re-guaranteed on startup exactly like the existing defaults. Ensure they actually PROJECT a real elevation (verify the elevation projection path - EdgeProjectorService / elevation.create - renders geometry, not a blank view). Map to the view/documentation contract + C04 rendering + ADR-0115; align with View-Intent strategy so they appear in the view list like the plan/3D. P8. No shortcuts |
| L-111 | claude(L-11 follow-up) | **[RENDERING] Post-processing controls (Ambient Occlusion + Exposure) did NOT reach the live WebGPU renderer (prod default)** — the View Properties POST-PROCESSING toggles wrote only to the OBC PostproductionRenderer, which is silenced (MANUAL, postproduction.enabled=false) on the WebGPU default; AO painted nothing and Exposure wrote the wrong (silenced OBC) renderer instead of `window.pryzmRenderer`. Bloom already worked (canvas-swap EffectComposer). | Rendering / Post-FX | FIXED §FIX-POSTFX-WEBGPU (55281261) | Backend-routed both controls (new `apps/editor/src/engine/postFxRouting.ts`, testable, P2/P4/P8-clean): **AO** on WebGPU drives the TSL screen-space SSGI-AO pass via `RenderPipelineManager.activateSSGI()`/`deactivateSSGI()` (default SSGI giIntensity=0 → AO-only, same path as the RenderRail SSGI toggle; OBC aoPass retained as WebGL fallback); **Exposure** now writes tone-mapping to every live renderer incl `window.pryzmRenderer` (applies next TSL frame, no pipeline rebuild). Honors §FIX-SSGI-DEFAULT-OFF (SSGI not forced on at boot — only on explicit AO opt-in), ADR-0111 shadow-freeze + §FEAT-REAL-ENVIRONMENT (no sync GPU dispose). 7/7 specs `postFxRouting.spec.ts`. C04. |
| L-112 | founder | **[RENDERING, L-11/L-107 lineage] Ground shadow-catcher shows a big GREY RECTANGLE, not the true time-of-day building shadow** — on opening a project with a building, the invisible-ground real-shadow feature renders a large grey rectangle (≈ site-plane sized) that moves, instead of the ACCURATE cast shadow of the building (correct silhouette + direction/length that changes with time of day). Founder compared 9.00h vs 18.50h (images): the shape shifts as a big block but is NOT the real building shadow projection. L-107 correctly deferred the catcher until a caster exists (`ground=on (catcher deferred until first caster)` in log) — so the catcher IS attached now, but it is not producing a correct shadow: it reads as an opaque/uniform grey plane rather than transparent-except-where-the-building-shadow-falls. | Rendering / Environment (L-11/107 lineage) | FIXED (shadows restored) §FIX-SHADOW-CATCHER-RESTORE (72e34915) | routed → ENVIRONMENT agent (owns GroundShadowCatcher / RealEnvironmentService). The catcher must display the REAL sun-cast shadow of the building — accurate silhouette, correct direction+length for the current Time-of-day/azimuth/elevation, updating live as the sun moves. Likely roots to investigate: (a) THREE.ShadowMaterial on WebGPU not sampling the shadow map → whole plane paints grey (opaque) instead of only the shadowed region; (b) the sun DirectionalLight shadow-camera FRUSTUM not tightly bounding the building (too large → shadow is a low-res blocky blob; log shows shadow map 512px radius 1) so the silhouette is lost; (c) catcher plane oriented/positioned so it captures a projection footprint, not a shadow. Fix so a real, crisp, time-of-day-correct building shadow lands on the ground plane on the WebGPU default; keep ADR-0111 shadow-freeze + §FEAT-REAL-ENVIRONMENT (no sync GPU dispose) intact; do not regress L-107 (still invisible/absent when no caster). Add a test: with a caster + a given sun vector, the catcher receives a shadow whose footprint matches the caster silhouette (not a full-plane fill). ADR-0106; C04.  **CRITICAL UPDATE (founder): THIS IS A REGRESSION FROM L-107.** Yesterday the invisible-ground real shadows rendered GREAT (founder: 'amazing'). Today's build shipped L-107 §FIX-GROUND-CATCHER-INVISIBLE-WHEN-EMPTY (catcher changed to deferred/attach-on-first-caster) and it BROKE the working shadow rendering AND still shows the grey rectangle. PRIORITY: RESTORE yesterday's working behavior (invisible ground catcher that renders the true sun-cast building shadow). Likely the deferred-attach/detach logic in L-107 broke the catcher's shadow-receive wiring — REVERT L-107's approach (or fix so the attached catcher actually receives shadows) and re-solve the empty-project grey rectangle WITHOUT breaking shadows. Restoring the working shadow is far more important than the cosmetic empty-grey; if a clean both-fix isn't immediate, restore the shadows first (revert 5e33da9c) and re-approach the empty-grey separately. | **RESOLVED: real building shadow renders again** (reverted L-107's deferred/late-attach which had dropped the receiver out of the WebGPU shadow-sampling set; catcher attaches up front as in original L-11; ADR-0111 intact; 11 tests). DEFERRED FOLLOW-UP (separate, lower priority): the empty-project grey rectangle returns (L-107's original target) — fix by tuning ShadowMaterial opacity-where-unlit on WebGPU + fitting the sun shadow-camera frustum to caster bounds for a sharper silhouette, WITHOUT touching the receive path (never re-introduce presence-gating/late-attach). Tracked here; not a blocker.
| L-113 | founder | **[EDITING] Level STACKED/UNSTACKED (level-explode) breaks element coordination — selected elements float; moving while stacked commits WRONG (jumps to original position)** — with the stacked/unstacked feature (`LevelExplodeController`), selecting an element while stacked/unstacked can leave it uncoordinated (sometimes elements stay FLOATING), and if you MOVE an element while stacked the element does NOT land in the correct position — it goes back to its ORIGINAL (unstacked) position. | Editing / Level-explode / Coordination | FIXED §FIX-LEVEL-EXPLODE-COORDINATION (a90e2be7) | routed → LEVEL-EXPLODE agent. Root hypothesis: the explode/stack applies a VISUAL vertical offset per level (display transform), but the pick/gizmo/move path operates in EXPLODED visual space and the move command commits the position WITHOUT subtracting the per-level explode offset (or the element mesh isn't re-parented consistently) → committed model position is wrong (snaps to unstacked pos) and selection highlight/gizmo doesn't follow the exploded mesh (floating). FIX architecturally: the explode offset must be a PURE view/display transform that never pollutes the model position; the interaction layer (pick, gizmo anchor, move commit) must convert between exploded-visual space and true model space using the active per-level explode offset, so moving while stacked commits the correct unstacked world position AND the visual stays coordinated. (Alternatively, if moving-while-exploded is out of scope, the move must still be correct on unstack — but preferred is explode-aware transforms.) Ensure selection/gizmo track the exploded position (no floating). Grep LevelExplodeController, level explode offset, the transform/gizmo drag-end commit, PickResolver. Add a test: with levels exploded, moving an element commits the correct model position (explode offset removed) and selection anchors on the exploded mesh. C04/C16; ADR for level-explode if one exists; no shortcuts; P8 | **RESOLVED:** the explode Y-offset stays a pure view transform (never pollutes model — move commits already take XZ from gizmo + Y from model elevation). Fixed 3 coordination gaps: (1) rebuilt meshes re-lift into the stack via debounced reconcile on bim-*-updated/added/removed (preserves survivor baseY, no double-lift); (2) selection re-anchors at settle (highlight re-applied + pryzm-reanchor-transform re-syncs wall/stair gizmo proxies); (3) instanced-wall/per-instance OBB centre.y adds the active offset. Exact model-Y restore on unstack. 8 tests + existing suites green. C04/C16.
| L-114 | claude(obs) | ✅ **FIXED `1631c5c8`→main** — the literal symptom (fixed 500-entry LRU vs a 909-element elevation) was ALREADY fixed by `11f20cd9` §FIX-NME-ADAPTIVE-LRU (cap = `ceil(count×1.25)`, HARD_CAP 6000). The REAL residual the agent found: the LRU is a **single GLOBAL map**, and the cap was ratcheted from **one** view's count — so plan + the 4 default L-110 elevations + 3D **evict each other**; switching the active view (South→East→South) re-thrashes to 0%. True working set ≈ 4600 entries vs a ~1137 cap. L-202's 200 m default depth makes each view's set heavier. FIX: cap is now the HARD_CAP-bounded **sum of per-view desired caps** (`Map<viewId, ceil(count×1.25)>`, ratchet-up only), so concurrent views coexist; the exported element set is UNCHANGED (nothing culled) and every ratchet logs `§FIX-ELEV-NME-CACHE-MULTIVIEW` (no silent sizing). **Frustum/depth culling REJECTED** (and previously rejected by L-118): a default elevation's crop spans the whole model and `DEFAULT_ELEVATION_FAR_DEPTH` is now 200 m, so almost nothing is cullable — and culling would drop the set-back geometry L-196 needs to classify `:beyond`/hidden and L-190's window/door roots. 9 tests (3 new + L-190 2 + L-196 4); the 3 tsc errors at commit time were in `PlanViewAnnotationRenderer.ts`, a concurrent lane's uncommitted WIP, not in this commit. Residual: `_viewDesiredCap` is not pruned on view deletion (no delete event at this layer) — harmless, cap stays an upper bound clamped by HARD_CAP; memory is descriptor-only (geometry refs are shared). ORIGINAL: **[PERF] Elevation view NME cache thrashes to 0% hit rate on a heavy multi-level building** — on the South elevation of a 7-level / 909-element project the NativeElementMeshExporter §H2 cache evicts continuously (`cacheSize=500/500 hitRate=0% misses=776`, `No levelId — exporting all 909 elements across 7 levels (viewType=elevation)`). The 500-entry LRU is far smaller than the elements an elevation projects (all levels at once), so every projection is a full miss + full eviction → wasted CPU each elevation reprojection. Surfaced by the new L-110 default elevations on heavy buildings. | Perf / Views / Elevations | OPEN (observation) | routed → tie into L-02/L-03 perf lane / views. Options: size the NME cache adaptively (or per-view) so an elevation of N elements isn't guaranteed 0% hit; or scope elevation export by view-frustum/depth so it doesn't export all 909 across 7 levels every time; or cache elevation projections per (view,gen) so an unchanged elevation reuses its result. Confirm impact with §PERF markers on a heavy load. C04; no shortcuts |
| L-115 | founder | **[RECURRENT, MANY TIMES] Layered wall (Interior Partition 100mm) created in PLAN VIEW comes out as a PLAIN wall — but 3D shows the real layered wall** — reported repeatedly, still not solved. ROOT revealed by the panel screenshot: the Draw Wall panel says **'✓ Plain Wall ready'** while the WALL TYPE dropdown shows **'Interior – Partition 100mm'** with a SEPARATE **Apply** button. So the dropdown SELECTION does not ARM the type — the tool stays armed as Plain Wall unless the user clicks Apply. Log confirms: walls before Apply dispatch `systemTypeId=none` (plain); only after Apply do they dispatch `systemTypeId=wt-interior-partition`. The user picks the layered type in the dropdown, the panel still says 'Plain Wall ready', they draw → PLAIN wall. (Prior L-41 §FIX-PLAN-WALL-TYPE-IGNORED + L-62 §FIX-PLAN-LAYERED-WALL-SYMBOL fixed the THREADING once a type is armed, but NOT this arming/UI mismatch.) Also verify the ARMED layered wall renders its layer composition in plan (WallLayerPlanSymbolBuilder injects layer lines per log, but confirm the just-created layered wall shows layers, not a plain box). | Walls / Plan wall-type arming UX | FIXED §FIX-PLAN-WALL-TYPE-ARM-ON-SELECT (8ce385d8) | routed → PLAN-TOOLS/wall-type agent (owns WallPlanToolHandler + activeWallSystemType + L-41/L-62/L-98). FIX: selecting a wall type in the Draw Wall dropdown must ARM that type IMMEDIATELY (the 'ready' state + the armed systemTypeId must follow the dropdown selection — no separate Apply required, or Apply auto-fires on select), so drawing produces the SELECTED layered wall. The 'ready' label must never say 'Plain Wall ready' while the dropdown shows a layered type. Guarantee plan ≡ 3D for wall TYPE: a layered interior partition drawn in plan is layered in both plan (layer symbol/composition) and 3D. Reproduce EXACTLY: pick Interior Partition in the plan Draw Wall dropdown WITHOUT clicking Apply → the drawn wall must be the layered interior partition (systemTypeId=wt-interior-partition), and its plan symbol shows the layers. Add a test asserting dropdown-select arms the type into the dispatched wall.create. §FIX-PLAN-WALL-TYPE-ARM-ON-SELECT. ADR-0055 + C16 wall authoring; no shortcuts; P8 | **RESOLVED:** Draw Wall dropdown now arms the type ON SELECT (armWallType writes activeWallSystemType [L-98] + window.wallTool immediately, no Apply); 'ready' label reflects the selected type (never 'Plain Wall ready' while a layered type is chosen); drawing dispatches the selected systemTypeId → plan renders layered (WallLayerPlanSymbolBuilder). 3 tests incl. end-to-end draw. ADR-0055/C16.
| L-116 | founder | **[FEATURE, follow-up to L-110] Default elevations (N/E/S/W) need an ELEVATION MARKER on the Ground Floor plan** — like any BIM/Revit elevation, each of the 4 default elevations created by L-110 (§FEAT-DEFAULT-ELEVATIONS) should have a corresponding elevation marker/tag on the Ground Floor plan showing its position + view direction. Today the elevations exist as views (log: North/East/South/West Elevation created) but there is no marker on the plan. The marker must be a PROPER annotation element that respects the annotation/documentation CONTRACT rules — no shortcut symbol. | Views / Annotations / Documentation | FIXED §FEAT-ELEVATION-MARKERS (a1050f6e) | routed → DEFAULTVIEWS/ELEVATION-MARKER agent (owns DefaultViewsManager + L-110). When the 4 default elevations are (re)created on startup, ALSO create one elevation-MARKER annotation per elevation on the Ground Floor plan, using the EXISTING elevation-mark / section-mark annotation infrastructure (grep elevation.mark/section.mark handlers, buildingElevations.ts, AnnotationRailPanel, the annotation store) — do NOT invent a parallel marker. Requirements: (1) it is a first-class ANNOTATION element via the annotation store, created through the command bus (P6), schema-pure (C03), respecting the annotation + auto-documentation contracts (C24.1-AUTO-DOCUMENTATION-SHEETS-PROTOCOL + the annotation rules in C06/C09); (2) positioned on the plan + oriented to PROJECT NORTH (ADR-0115) pointing in each elevation's view direction (N/E/S/W); (3) LINKED to its elevation view (clicking navigates to the elevation, Revit behaviour) if the annotation model supports it; (4) lifecycle matches the elevations — deleted with the elevation, re-guaranteed on startup like the default views (mirror the L-110 deletion-guard/re-create pattern). Add a test: a fresh project has 4 elevation markers on the ground plan, one per default elevation, correctly oriented; deleting an elevation removes its marker. §FEAT-ELEVATION-MARKERS. C24.1 + C03 + ADR-0115; no shortcuts; P8 | **RESOLVED:** DefaultViewsManager guarantees one DOC-2.7 elevation-mark annotation per default elevation on the Ground Floor plan — reuses the existing elevation-mark annotation kind + annotation store (schema-shaped C03 record, no parallel type), project-north oriented (ADR-0115, facingDirection = elevation projectionDirection), linked (parameters.linkedViewId → click navigates to the elevation), system-seeded (no undo pollution), lifecycle tied to the elevations (removed on vd:view-deleted, re-guaranteed on startup). 8 tests green. C24.1 + C03 + ADR-0115.
| L-117 | founder | **[BLOCKER, L-110 interaction] Big circular tower (2000+ elements) opens, then CRASHES on first 3D navigation — eager full-scene reprojection of the 4 default elevations thrashes the NME cache/WebGPU** — old project opens fine, but the first attempt to orbit/navigate the 3D view crashes (founder: 'probably too many elements, maybe WebGPU'). Log: a flood of `[NME] §H2 evicted LRU entry key=…:vd-sys-elev-west:…:full` (wall indices to 2200+) + `[NativeElementMeshExporter] No levelId — exporting all N elements across 7 levels (viewType=elevation)` + `[ViewDependencyTracker] flush — 5 dirty view(s): vd-sys-p, vd-sys-e, vd-sys-e, vd-sys-e, vd-sys-e`. ROOT: the 4 always-present L-110 default elevations (§FEAT-DEFAULT-ELEVATIONS) are reprojected on EVERY view-dependency flush, and each elevation exports ALL elements across ALL levels (no per-level scoping for elevations), thrashing the 500-entry NME LRU to 0% hit. On a 2000+ element tower this is 4×thousands of projections per flush → runaway CPU/GPU + memory churn → WebGPU crash on first navigation. This is the catastrophic form of L-114, caused/exposed by L-110. | Views / Projection / Perf (L-110 interaction) | FIXED §FIX-LAZY-INACTIVE-VIEW-PROJECTION (11f20cd9) | routed → VIEW-PROJECTION agent. Architecturally: an INACTIVE view must NOT eagerly reproject. Fix: (1) ViewDependencyTracker should only eagerly reproject the ACTIVE view; mark inactive views (the 4 elevations while in 3D/plan) dirty but DEFER their reprojection until the view is actually activated (lazy projection — a view shouldn't project until viewed). (2) NME/EdgeProjector elevation export must not export ALL elements every time — scope by the elevation's view depth/frustum and/or cache per (view,gen) so an unchanged tower reuses its projection; size the NME cache adaptively so a 2000-element view isn't guaranteed 0% hit (see L-114). (3) Guard against the WebGPU crash under massive projection churn. Do NOT regress L-110 (elevations still exist + are correct when opened) or L-108 (empty-load). Reproduce with a large multi-level tower: open → navigate 3D → must not reproject the 4 elevations or crash. Add a test: with 4 default elevations + a large model, a 3D-view edit does NOT trigger elevation reprojection (only the active view). §FIX-LAZY-INACTIVE-VIEW-PROJECTION. C04 rendering/scheduling + the view/documentation contract; ADR-0115; no shortcuts; P8 | **RESOLVED:** ViewDependencyTracker now defers INACTIVE views — active-view predicate (main 2D view + split pane); in pure 3D 0 views reproject, plan-active 1; the 4 elevations reproject 4→0 per edit, projected once on demand via notifyViewActivated. 6 tests. Fixes the tower crash.
| L-118 | founder | **[BLOCKER, elevation projection unsound] Elevation projection BLEEDS into the 3D view + the elevation views render NOTHING + extreme slowness** — testing a project with the L-110 default elevations: (1) elevation projection geometry (dashed elevation lines/frames) appears ON/around the 3D-view elements — it should ONLY appear in the elevation view (images 1-2); (2) the East/West Elevation views themselves render NO building — only the level datum lines + a permanent 'Generating view…' (images 3-4); (3) just opening a view makes the project extremely slow — log: `[NativeElementMeshExporter] No levelId — exporting all 1006 elements across 7 levels (viewType=elevation)` with continuous `[NME] §H2 evicted …:vd-sys-elev-east/west:…:full` thrash (0% hit). Founder: check → review → analyse → plan; architecturally sound; consider performance. | Views / Elevation projection / Rendering | FIXED (perf+isolation) (11f20cd9) | routed → VIEW-PROJECTION agent (EXPAND the L-117 lane — same elevation-projection subsystem; single owner). Make the elevation projection SOUND: (a) ISOLATION — the elevation's projected geometry must live in the elevation view's own layer/render-target ONLY and must NOT be added to the shared 3D scene (no bleed into 3D); verify which layer/scene the EdgeProjectorService/NME elevation output is inserted into vs the 3D BIM layer. (b) ACTUALLY RENDERS — the elevation view must display the projected building (currently stuck 'Generating view…' — the projection never completes because it exports all 1006 elements/thrashes; fix so it produces + shows the drawing). (c) LAZY + PERFORMANT — only project the ACTIVE view (L-117), scope/cache the elevation export, adaptive NME cache (L-114) so opening a view isn't O(all-elements)×thrash. Do NOT regress L-110 (elevations exist) or L-116 (markers) — but CHECK whether the 3D 'dashed' bleed is the elevation projection OR the L-116 markers rendering outside the plan; if it's the markers, flag it for the defaultviews agent. Analyse + note the plan in your report. Reproduce: open an elevation on a multi-level building → it renders the building (not empty), nothing bleeds into 3D, no thrash. §FIX-ELEVATION-PROJECTION-ISOLATION-AND-RENDER. C04 + view/documentation contract; ADR-0115; no shortcuts; P8 | **CLARIFICATION (founder): the elevations DO render** (West/South show the full building) — they are NOT empty; the 'Generating view…' was just the slow projection in progress. So the CORE issue is purely ARCHITECTURE + PERFORMANCE: opening an elevation is WAY too slow because it projects ALL 1006 elements across 7 levels with the NME cache thrashing to 0% ('it is not normal'). PRIORITY = make opening/projecting an ACTIVE elevation FAST (scope by frustum/level-band + per-(view,gen) cache + adaptive NME size), in addition to the L-117 lazy-inactive-view fix. The 3D-bleed (elevation output appearing in the 3D scene) is the secondary correctness part. Not an empty-render bug. **RESOLVED:** adaptive NME proxy-cache cap (500 → ≥ view element set +25%, HARD_CAP 6000) so a ~1006-element elevation caches fully (no 0% thrash); re-open already instant via viewTechnicalDrawingCache. ISOLATION confirmed: elevation projection is NOT added to the 3D scene (DOCUMENTATION_LAYER / mount gate) — the 3D 'dashed bleed' is NOT the projection (likely the L-116 markers/datum lines → routed to defaultviews). Frustum/depth culling intentionally skipped (default elevations legitimately show all levels).
| L-119 | founder | **[RENDERING STYLE] Elevations render as SOLID BLACK filled regions instead of projection LINEWORK** — an architectural elevation must show ONLY the projection lines (wall/window/balcony outlines + level datum lines) on a white/transparent background; instead the whole facade is drawn as an opaque BLACK silhouette that hides the detailed elevation linework (image). The EdgeProjectorService already produces edge geometries, but the projected element FACES are being drawn filled (dark material) in the elevation view. Founder: 'the elevations should render just projection lines.' | Views / Elevation rendering style / Documentation | FIXED | routed → VIEW-PROJECTION agent (same elevation-projection subsystem as L-117/L-118; single owner). Render the elevation view as a proper 2D technical LINE drawing: show the projected edge linework (with hidden-line removal as already present, `[HiddenLineRemoval] v1 pass`), and make element FACES transparent/white (or a light poché) — NOT solid black fill. Match the plan-view technical-drawing convention (the same NME/EdgeProjector linework style that plan views use — plan shows linework, elevation should too). Verify which material/fill the elevation NME output uses vs the plan output and align them. Keep it architecturally sound per the documentation contract (C24.1 auto-documentation / drawing style) — elevations are line drawings. Reproduce: open an elevation → see crisp black linework on white, not a black-filled silhouette. §FIX-ELEVATION-LINEWORK-STYLE. C24.1 + C04; no shortcuts; P8 |  ✅ FIXED 15425570 (ViewScope) — new packages/core-app-model/src/views/ViewScope.ts resolveViewScope(): elevation={cut:false,poche:false}; EdgeProjectorService emits :proj/:beyond only (no A-WALL:cut), PlanViewCanvas poche gated off → no solid-black façade. Prod-verify.
| L-120✓ | founder | **[ARCHITECTURE + PERF REVIEW] Make the whole DOCUMENTATION/view system as low-performance-consuming as possible + review its full architecture across ALL view types (plan, elevation, section, others)** — the elevation perf issues (L-117/L-118/L-119) exposed that the documentation projection pipeline needs a holistic architecture + performance review, not per-view patches. Founder: investigate to make it as low-perf-consuming as possible; review the full architecture toward documentation in general — elevations, plan view, sections, and other view types. | Views / Documentation / Architecture | OPEN (review+plan) | routed → DOC-ARCHITECTURE review agent (READ-ONLY Plan — runs alongside the L-117/118/119 implementation without touching code). Deliverable: a holistic root-cause + phased architecture plan covering the ENTIRE documentation/view-projection stack — EdgeProjectorService (plan clip-range / elevation depth / section cut+depth / other dirs), NativeElementMeshExporter + the §H2 NME LRU cache, ViewDependencyTracker dirty-view flush, ViewTechnicalDrawingCache (gen/staleGen), the per-view-type projection + technical-drawing style (WallLayerPlanSymbolBuilder, HiddenLineRemoval, symbol builders), incremental-vs-full reprojection, lazy activation, and shared caching. Goals: (1) ONE unified, sound projection+cache model across ALL view types (plan/elevation/section/future) — no per-type divergence; (2) reprojection is INCREMENTAL + LAZY + SCOPED (only active view, only changed elements, frustum/level-band scoped) so opening/editing is cheap regardless of model size; (3) cache sizing/keying correct per view (subsumes L-114/L-117/L-118); (4) linework style consistent + line-drawing not filled (L-119); (5) extensible for sections + other view types + sheets (C24.1). Map every recommendation to C04 + C24.1 + the view/documentation contract + ADR-0115; note which parts the in-flight L-117/118/119 already cover and what remains. Return the plan (no code). §DOC-ARCH-PERF-REVIEW |
| L-121 | founder | **[BLOCKER, recurrent freeze — now amplified by L-110 elevations] Wall + hosted door, then MOVE the wall → project FREEZES** — reported many times; L-63/L-97 killed the earlier infinite loops but it STILL freezes. NEW root from log: since L-110 added the 4 default elevations, EVERY wall/door edit triggers `[ViewDependencyTracker] flush — 5 dirty view(s): vd-sys-p, vd-sys-e, vd-sys-e, vd-sys-e, vd-sys-e` and each of the 4 elevations does `[NativeElementMeshExporter] No levelId — exporting all N elements across levels (viewType=elevation)` — so every edit now reprojects 5 views instead of 1 (5× amplification). Moving a wall with a hosted door (wall rebuild + opening update + room redetect) × 5-view reprojection = heavy enough to freeze. Same root as the L-117 tower crash / L-118 elevation perf. | Geometry/Walls + Views/Projection | FIXED (via L-117 lazy projection) (11f20cd9) | routed → VIEW-PROJECTION agent (SAME as L-117/118/119; single owner). The L-117 lazy-active-view fix directly resolves this: a wall/door edit while in the 3D (or plan) view must reproject ONLY the active view, NOT the 4 inactive elevations — dropping the per-edit work from 5 views to 1. REQUIRE an explicit test: create wall + hosted door + move the wall while the 3D view is active → the 4 elevations are marked dirty but NOT reprojected (deferred), and the move completes without freeze (bounded frames). Also confirm the L-97 §FIX-WALLFLUSH-NOPROGRESS-GUARD still holds under this load. Keep L-110 correct (elevations reproject when opened). ADR-0055 + C04; builds on L-63/L-97/L-117; no shortcuts; P8 | **RESOLVED:** wall+door move now defers the 4 inactive elevations (test log: 'deferred 5 inactive view(s)' during the move) → per-edit reprojection 5 views → 1 (active) → no freeze. L-97 wall-flush guard untouched + still converges.
| L-122 | founder | **[RECURRENT, HIGH — wall joins] Interior wall (different type) joining an EXTERIOR L-corner: the two original exterior walls must stay UNCHANGED + the new interior wall must join cleanly (executed == preview)** — very common scenario, still wrong. SCENARIO: two EXTERIOR walls meet in an L-corner, mitred (inglete) perfectly clean. Then an INTERIOR wall (a DIFFERENT wall type) comes to join at/near that corner. RULE (architectural invariant): the original two exterior walls should ALWAYS remain exactly as they were (their mitre preserved, immutable) and ONLY the new interior wall adapts to join cleanly (adjacent/pegado). Current result distorts the corner (small notch/gap at the L, the interior join is messy — images 1-2), instead of the clean result the PREVIEW shows (image 3: interior wall comes straight down, clean, the two exterior walls untouched). Builds on L-94 (§FIX-NEWWALL-LCORNER-FLUSH) but adds the different-wall-TYPE case + the strict 'existing walls immutable' rule. | Geometry/Walls (joins) | FIXED | routed → SOLE WALL AGENT (owns wall geometry / L-61/91/93/94; single owner, no parallel wall editors). Honor ADR-0055 BASELINE IMMUTABILITY for the pre-existing walls: when a NEW wall joins an existing junction/cluster, the WallJoinResolver must NOT re-mitre or move the two existing exterior walls — their baselines + corner mitre stay byte-identical; only the NEW interior wall's near end is trimmed/seated to join cleanly (flush/pegado) onto the corner, regardless of it being a different wall type. Executed result must equal the V2 preview (image 3). Reproduce EXACTLY: two exterior walls in a clean L-mitre + interior wall (different systemTypeId) joining the corner → assert (a) the two exterior walls' baselines + footprint are UNCHANGED before/after the interior wall is added, (b) the interior wall seats clean (positive-area, non-self-intersecting, no notch/gap/overlap), (c) executed == preview, fresh + on reopen. ADR-0055 (baseline immutability) + C15/C16; builds on L-94; no shortcuts; P8 |  ✅ FIXED 48dcce70 — existing exterior L frozen on interior-wall join; gated __pryzmExistingCornerImmutable (default ON); 161+147+9 wall tests green. Residual: V2/plain-wall path (systemTypeId thread) = follow-up.
| L-123 | founder | **[BLOCKER, elevation incomplete] Elevation renders only a PORTION of elements — aggressive crop-region CULLING drops walls that straddle the Active-cut boundary** — the founder confirms the documentation projection is generally amazing, but in this case the elevation shows only a fragment of each element / most walls missing (left = elevation with only a few wall segments; right = plan showing the full room + Active cut). Log root: `[NativeElementMeshExporter] Culled 7/10 elements outside cropRegion (3 passed)` — the elevation crop (Active cut) culls elements by a BINARY per-element in/out test, so any wall that straddles the crop boundary is DROPPED entirely; only elements fully inside pass → incomplete elevation. Secondary: `[VGSceneApplicator] applied=0/14 layers` for the elevations (vs 4/14 for plan) — elevation layer/graphic styling not applied. | Views / Elevation crop + projection | BROKEN (BLOCKER) | routed → VIEW-PROJECTION agent (same elevation subsystem as L-117/118/119/121/123; single owner). Fix the crop-region handling: an element that INTERSECTS the crop region must be INCLUDED and CLIPPED to the crop boundary (render the visible portion), NOT culled whole — so a wall crossing the Active-cut edge shows its in-crop segment. Ensure the DEFAULT elevation crop encompasses the whole model (so all walls show unless the user deliberately crops). Investigate the `applied=0/14 layers` on elevations — the elevation projection must get the same layer/graphic application the plan does (ties to L-119 linework style). Reproduce: a room whose walls extend beyond the default crop → the elevation shows ALL the walls (clipped at the crop, not dropped), complete linework. §FIX-ELEVATION-CROP-CLIP. C04 + view/documentation contract; ties to L-118/L-119/L-120; no shortcuts; P8 |  ✅ FIXED 15425570 (ViewScope) — exportForView flat-XZ crop cull now PLAN-family only; elevation/section depth scoping via oriented sectionVolumeBox + clipSegmentToSectionBox (true clip-not-cull) → straddling walls no longer culled whole; elevation NME output crop-independent (cache reuse). Prod-verify.
| L-124 | founder | **[HIGH, refines L-123] Elevation projection lines INCOMPLETE/inaccurate as the crop scope is EXTENDED — small crop = perfect** — founder confirms: with a small/precise crop the elevation renders perfectly ('amazing', image 1); as the crop is EXTENDED, it 'actually works because the scope of the crop is correct' BUT the elements coming into scope 'are not accurate in the way that is coping with all the true projection lines' — i.e. the crop SELECTION is right, but the projection COMPLETENESS/accuracy of the included elements degrades (missing/partial true lines). Log: as the crop grows, the elevation depth/far grows (`far=8.020 → 20.746`) and cull count drops (`Culled 5/10 → 1/10`, more pass), yet `[VGSceneApplicator] applied=0/14 layers` persists on the elevation. | Views / Elevation projection completeness | FIXED | routed → VIEW-PROJECTION agent (elevation drawing-correctness cluster with L-119 + L-123; single owner). As the crop expands (larger cropRegion + deeper far), every in-scope element must project ALL its TRUE lines (complete edge linework + correct hidden-line removal), not a partial set. Investigate: (a) does the edge projection / HLR drop lines for elements deeper in the expanded far-range or partially in the crop? (b) is the crop clipping geometry mid-element without emitting the full visible edges (ties to L-123 clip-not-cull)? (c) why `applied=0/14 layers` on elevations (vs 4/14 plan) — the VG layer/graphic application that gives plan its complete styled linework isn't applied to elevations (ties to L-119). Reproduce: open an elevation with a small crop (perfect), then extend the crop to the whole facade → ALL walls/openings render their complete true projection lines (matching what the small crop showed for its subset). §FIX-ELEVATION-PROJECTION-COMPLETENESS. C04 + view/documentation contract; ties to L-118/119/120/123; no shortcuts; P8 |  ✅ FIXED 15425570 (ViewScope) — ViewTechnicalDrawingCache.setIfCurrent stale-accept now marks provisional + dispatches vd:reprojection-required → ViewDependencyTracker.forceReproject (P8 span) schedules debounced full reproject at current gen; final crop lands complete linework; L-117 lazy gate preserved. Prod-verify applied>0/14.
| L-125 | founder | **[MED] ESC does not deselect a selected ELEVATION (section does) — selection/gizmo persists** — with an elevation marker + its crop gizmo selected in plan, pressing ESC fires `[EngineBootstrap] Shortcut Escape → deactivateAll` (twice, log confirms) but the elevation stays selected — a section element deselects correctly on ESC, an elevation does not. Log context: repeated `SET_VIEW_CROP` churn from the crop gizmo while selected, `deactivateAll` reached but the elevation view/crop-gizmo selection was not cleared. | Views / selection — ESC deactivateAll must clear elevation view+crop-gizmo selection | FIXED | routed → dedicated selection agent. `deactivateAll` / SelectionManager.unselectAll must clear the elevation crop-gizmo + marker selection exactly as it clears a section: find where section selection is torn down on ESC and give elevation the SAME teardown path (single owner of the deselect intent, no per-type shortcut). Reproduce: select an elevation in plan → ESC → gizmo + selection cleared. §FIX-ESC-DESELECT-ELEVATION. C04 + view/documentation contract; P6 (mutation via command / selection intent); no shortcuts; P8 |  ✅ FIXED d7ffb263 — ESC/unselectAll now clears window.__pryzmSelectedAnnotationId (elevation crop-gizmo+marker); shared teardown w/ section; input-host typecheck green.
| L-126 | founder | **[FEATURE — wall set-out dims] Project set-out dims to ALL FOUR sides + blue/grey pairing + TAB-to-edit numeric entry** — the dashed set-out dim on wall-creation preview ('amazing, perfect', shows distance to nearest wall) needs 3 incorporations: (1) project the dim to the TWO OTHER sides too when walls exist — i.e. up to 4 set-out dims (both directions on each axis), not just the nearest per axis; (2) colour-differentiate: TWO blue + TWO grey so the user distinguishes the pairs; (3) TAB jumps focus INTO a dim field to type the exact number → that number DEFINES the wall point (the placed vertex snaps so that set-out equals the typed value); continued TAB cycles to the next dim; ENTER commits. | Wall creation preview — set-out dimensions (`§WALL-SETOUT`) + dynamic numeric input | FIXED | routed → dedicated wall-setout agent (FENCED OFF wall-join geometry: MUST NOT touch WallJoinResolver.ts / JunctionResolverV2 / MiterPrismBuilder / WallOccupancyStore — sole-wall agent owns those). Owner files: `apps/editor/src/engine/views/plantools/setOutDimensions.ts` (pure maths — extend `computeSetOutDimensions` to return up to 4 dims, both dirs per axis, tagged primary/secondary for blue/grey) + `setOutDimensions.test.ts` + `apps/editor/src/engine/views/plantools/WallPlanToolHandler.ts` `_drawWallPreview` (render 2 blue + 2 grey; add TAB-focus numeric input overlay that back-solves the placed vertex from the typed set-out, TAB-cycle, ENTER-commit). Keep maths PURE (no DOM/THREE). Dynamic input must dispatch the placement through the command path (P6), not mutate directly. Maps C11 element-creation pipeline + C16 command authoring + the wall preview contract; architecturally sound, no shortcuts; every new exported fn ≥1 OTel span (P8). |   ✅ FIXED 7f3a6e80 — computeSetOutDimensions returns up to 4 dims (side +role primary/secondary); WallPlanToolHandler renders 2 blue(#1e40af)+2 grey(#64748b); pure solveSetOutPoint(P8 span) back-solves vertex from typed mm; TAB cycles, ENTER commits via wall.create (P6); 16/16 tests. Browser-verify TAB routing on prod.
| L-127 | founder | **[HIGH — door preview] SPACE-flip direction + dimensionally-EXACT preview (not default door) + render the door FRAME (currently gaps at jambs)** — 3 parts: (1) door preview must change direction (inward/outward × left/right hinge) on SPACE so the user orients precisely BEFORE creation — infra EXISTS (`§FEAT-DOOR-FLIP-ON-SPACE`, L-92/ADR-0107, DoorPlacementFlip) so VERIFY it actually cycles + the arc preview updates in this flow; (2) the preview must be dimensionally EXACT to the SELECTED door type — today `DoorPlanToolHandler` uses a hardcoded `DOOR_WIDTH` constant (line ~129 `width=1m`) → 'not a default door, dimensions must be exactly correct'; resolve the chosen door systemType's real width/height/leaf and preview THAT; (3) render the door FRAME (jambs + head) — currently gaps at the jambs (image: openings show gap where frame should close the reveal). | Door creation preview + door frame geometry (C15 hosted elements) | FIXED | routed → door-preview agent (FENCED off copy-tool + wall-join + view-projection + wall-setout lanes). Owner: `apps/editor/src/engine/views/plantools/DoorPlanToolHandler.ts` (preview: use selected type dims, verify SPACE-flip), door geometry/frame in `packages/geometry-door` + `plugins/door` (frame jambs/head so no gaps — preview ≡ placed). Preview ≡ placed door (same swing/hand/dims/frame). Maps C15 (hosted elements) + C11 (creation pipeline); no shortcuts; P8 spans. §FIX-DOOR-PREVIEW-EXACT + §FIX-DOOR-FRAME. |   ⏸ door agent a60c395c FAILED mid-edit on session limit; substantial partial work (new DoorDimensions.ts + DoorSystemTypeStore/DoorBuilder/DoorPlanSymbolBuilder/DoorTool/DoorPlanToolHandler) STASHED (git stash@{0} 'L-127-door-WIP-a60c395c-failed-midedit'). RESUME after session reset (11:50 Europe/London): pop stash or re-run; L-129 (SPACE routing) now unblocks the door flip half.  ✅ FIXED 44d15c1c — new DoorDimensions.resolveDoorDimensions() single source of truth (9 built-in types carry real nominal dims); preview+swing-arc+3D+placement all read it (preview ≡ placed, single & double); frame = 3D posts+head bar (frameThickness, wall.thickness+0.02 depth) + plan reveal-closing face lines + jamb ticks (no gaps). SPACE flip now reaches handler via L-129. tsc0. Prod-verify.
| L-128 | founder | **[HIGH] COPY tool does not create the copy — hosted-element (door) copy fails `wall.createOpening: canExecute rejected — opening must be an object`; founder believes it affects ALL elements** — log root cause: `[CopyTool] door copied → new IDs … | [CopyTool] wall.createOpening failed: CommandBusError: wall.createOpening: canExecute rejected — opening must be an object` at `_copyHosted → _commitCopy`. The copy path for a HOSTED element builds a malformed `wall.createOpening` payload (opening not a proper object). Founder: 'copy doesnt work, it just doesnt create a new element copy at the desired place' and 'happens with all elements' — so verify BOTH the hosted path (door/window `_copyHosted`) AND the plain-element copy path (walls etc.) actually produce a persisted new element at the destination delta. | Copy tool — hosted + plain element copy (command payload) | FIXED | routed → copy-tool agent (FENCED off door-preview + wall-join + view-projection + wall-setout lanes). Owner: `apps/editor/src/engine/views/plantools/CopyPlanToolHandler.ts` (`_copyHosted`/`_commitCopy`) — build the correct `wall.createOpening` payload shape (the `opening` object the command's canExecute expects — cross-check the working DoorPlanToolHandler placement payload + the command schema in command-registry/command-bus). Verify plain-element copy too. Copy MUST dispatch through the command path (P6) and land a persisted element. Maps C11 (creation pipeline) + C16 (command authoring) + C15 (hosted); no shortcuts; P8 spans. §FIX-COPY-HOSTED-OPENING-PAYLOAD. |   ✅ FIXED 6f9f7398 — copy dispatched wrong command name (wall.createOpening w/ openingData key) → switched to wall.opening.create {wallId,openingData} mirroring DoorPlanToolHandler; hosted+plain copy persist; 2/2 + 5/5 tests green.
| L-129 | founder | **[HIGH] SPACE rotation/flip preview does NOT work in PLAN view (main + split) — works GREAT in 3D** — in 3D the user clicks the element (e.g. sofa) preview and SPACE rotates before placement; in plan view (both main plan and split plan view) SPACE does nothing. Applies to furniture AND general elements. ROOT (code-confirmed): `FurniturePlanToolHandler.onKeyDown` ALREADY handles SPACE → `_rotation.advance()` + `_drawPreview()` (§FEAT-PLACEMENT-SPACEBAR-ROTATE, ADR-0105) and returns true — so SPACE is NOT being ROUTED to the active plan handler's onKeyDown (or a global EngineBootstrap SPACE shortcut preempts it). This is the plan-overlay keyboard routing (PlanViewToolOverlay / SvpPlanToolOverlay). SAME root gates the door SPACE-flip in plan (L-127 part-1). | Plan-view key routing — SPACE to active plan handler.onKeyDown (furniture + door + general) | FIXED | routed → furniture/plan-key agent = SINGLE OWNER of plan-overlay SPACE routing (fixes furniture + door + ALL plan placement previews at once). Owner: `apps/editor/src/engine/views/plantools/PlanViewToolOverlay.ts` + `SvpPlanToolOverlay.ts` (forward keydown, esp. SPACE, to active handler.onKeyDown; honour the handler's true-return preventDefault) + EngineBootstrap SPACE shortcut precedence + `FurniturePlanToolHandler.ts`. Verify main AND split plan. Maps C04 + PlanToolHandler contract §21/§2 (overlay-routed keys, handlers do NOT attach own DOM listeners); no shortcuts; P8. §FIX-PLAN-SPACE-ROUTING. |   ✅ FIXED cb6c39d6 — root was capture-phase PRECEDENCE (3D tool's document-capture SPACE listener swallowed the key before the plan overlay's bubble-phase router). Moved overlay router to window CAPTURE phase + stopImmediatePropagation + form-field guard; initTools space-pan yields to planOverlay.isPlacing(). Covers main+split plan, furniture+door+any PlanToolHandler.onKeyDown. tsc0, 4/4. Prod-verify.
| L-130 | orchestrator (L-122 residual) | **[LOW follow-up] Freeze existing exterior corner on the V2/plain-wall render path too** — L-122 (48dcce70) froze the existing exterior L for the LEGACY MiterPrism path (windowed/layered walls) + the persisted ADR-0055 baseline (byte-identical pre/post interior join). Residual: for PLAIN, opening-free exterior walls the 3D body renders via `JunctionResolverV2`, whose `WallInput` carries no `systemTypeId`, so V2 still re-mitres the collinear/vertex case. Real exterior walls almost always carry windows → legacy path (already fixed), so this is the uncommon case. | Walls / V2 pipeline (JunctionResolverV2) | FIXED | route → sole-wall agent when picked up: thread `systemTypeId` through `WallInput` → `LevelWallSpec` → `refreshV2Cache` (preview + executed caches) and apply the SAME different-type guard in V2's ring sweep as the legacy resolver's §FIX-EXISTING-CORNER-IMMUTABLE. Multi-file; not done blind. Maps ADR-0055 baseline immutability + ADR-0055 Pascal V2 pipeline; no shortcuts; P8. |   ⚠ FOUNDER-CONFIRMED 2026-07-06 on prod (main-DnAGwOiG): drawing PLAIN exterior L-mitre + 3rd interior wall → existing corner gets re-mitred/doubled (images 1/3) instead of staying clean (image 2). This IS the V2/plain-wall residual — L-122's §FIX-EXISTING-CORNER-IMMUTABLE only froze the LEGACY MiterPrism path; plain opening-free walls render via JunctionResolverV2 which lacks systemTypeId so the guard never engages. routed → SOLE WALL agent.  ✅ FIXED 84ca33f0 (in deploy) — V2 pipeline freezes existing plain-wall exterior L (byte-identical to 1e-9) + butts newcomer clean; flag __pryzmWallV2ExistingCornerImmutable; 176+68+3 tests.
| L-131 | founder | **[PERF — INDEPENDENT AGENT] Deeply evaluate + massively speed up INITIAL PROCESSING/GENERATION of a LARGE multi-family building (80-apartment Paris residential)** — founder: 'it is too large and takes really long time for initial processing — deeply evaluate how this can be sped up and increase MASSIVELY performance and orchestration yet NOT compromising the architecture.' Founder explicitly wants a DEDICATED performance agent ALONE on this. Scope: the whole large-model GENERATION + first-processing critical path — multi-apartment floor-plate engine, per-floor replication ×N storeys, batch element creation (walls/slabs/doors/windows/furniture), wall-rebuild + room-detect sweeps, projection/documentation regen, WebGPU instancing, save/serialize, and orchestration (worker offload, batching, streaming/progressive reveal). | Performance / large-model generation + orchestration | P0/P1/P3 SHIPPED — P2/P4-P6 queued | routed → INDEPENDENT performance agent (READ-ONLY deep evaluation first, like L-120): profile the 80-apt critical path, root-cause the dominant costs, produce a phased perf+orchestration plan (SAFE quick-wins vs deep refactors) mapped to contracts/ADRs/vision — reuse proven patterns ([[batch-creation-perf-pattern]] bus *.batch.create=one produceCommand, [[instanced-aggregate-level-visibility]], [[house-postgen-perf-audit]] dead-wait watchdogs, [[webgpu-heavy-scene-crash-and-instancing]] per-element-unique-material instancing defeat, [[resi-building-generator-and-corner-cell-blocker]]). NO architecture compromise; NO code in the eval phase — deliver the plan, then implement in gated phases. Maps C04 (scheduling) + C11 (creation pipeline) + C17 batch catalogue + platform-spine typology-agnostic vision; P3 single-rAF; P8. §PERF-LARGE-MULTIFAMILY-GEN. |   📊 2/3 profilers landed (batch/wall-rebuild + load/serialize/save); generation-replication profiler failed on session limit (re-run). Phased plan recorded in IMPLEMENTATION-PLAN. ROOT #1 = WALL REBUILD re-run k≈3×/level over ENTIRE level (mitre + openings repair) = O(F·W_floor²)·k; #2 = fragmented ~320+ per-apartment commands; #3 = synchronous main-thread serialize/save (pretty-print + 2-3× per autosave + 20-version deflate → quota thrash); #4 = 100% main-thread generation blocks rAF (only LOAD is chunked).  ✅ P0 96332c7f gated §PERF-GEN-INSTRUMENT (__pryzmPerfTrace) logs per-level resolveCountThisGen; ✅ P1 96332c7f §PERF-WALL-RESOLVE-ONCE-PER-GEN (flag __pryzmWallResolveOncePerGen default ON) — generation-coalesce window drains all accumulated wall-ids in ONE _flush → ~3 resolves/level → 1, geometry byte-identical (169/169 wall + 36/36 rebuild + 53/53 resi tests green); ✅ P3 a58f45b0 §PERF-SERIALIZE-COMPACT (drop pretty-print) + §PERF-AUTOSAVE-SINGLE-SERIALIZE (one serialize/save vs 2-3×, hash-consistent, round-trip verified). ✅ P2 SHIPPED c5992ad8 (§PERF-BATCH-COALESCE-PER-LEVEL, flag __pryzmBatchCoalescePerLevel default ON — ~400 per-apt cmds → ~4/level, geometry-identical, 53+169+41 tests green). NEXT: P5 progressive-reveal; P4 worker offload; P6 instancing / progressive reveal / delta history. Prod-verify with __pryzmPerfTrace before/after.
| L-132 | founder | **[CRITICAL robustness] Project creation HANGS on 'PREPARING WORKSPACE' — create POST has NO client timeout, so any transient server-unavailable window (e.g. Fly redeploy) hangs onboarding indefinitely** — console: `[PlatformRouter] createAndOpenProject — creating "AS"` then NO `created; opening` line → `[onboarding-bootstrap] timed out waiting for pryzm-project-loaded — the project may have failed to open`. Root: `createAndOpenProject` awaits `client.create(name)` → `ProjectListClient.create` = `POST /api/v1/projects` with NO AbortController/timeout/retry; if the server is briefly unavailable (redeploy rollout / blip) the fetch hangs forever and the UI is stuck on the loader. Server verified HEALTHY at report time (GET / →200 0.57s, POST /api/v1/projects →401 0.24s) → the founder's hang was a transient deploy-window blip, NOT a code regression (no shipped change touched the create endpoint/persistence client). SEPARATE pre-existing: `localStorage quota exceeded — project index not saved (eviction exhausted)` flood (50 projects) — non-fatal warning, addressed by L-131 P4 (IndexedDB offload) + needs old-project cleanup. | Onboarding / project create robustness (persistence client) | FIXED | routed → create-hardening agent. Add AbortController timeout (~10-15s) + bounded retry/backoff to `ProjectListClient.create` (and ideally the whole request helper); `createAndOpenProject` must surface a RETRIABLE error state (not a stuck spinner) when create fails/times out; onboarding shows 'creation failed — retry' instead of PREPARING WORKSPACE forever. Immediate workaround: hard-refresh + retry (server is up); clear old projects to relieve localStorage. Maps C05 persistence + onboarding UX; no shortcuts; P8. §FIX-CREATE-TIMEOUT-RETRY. |   ✅ FIXED 4f5a7b2d — shared persistence request helper now has 12s AbortController timeout + 2 backoff retries (400/800ms, transport-failures only, not 4xx/5xx); create rejects with typed retriable ProjectListClientError instead of hanging; PlatformRouter surfaces retriable overlay + briefBootstrap emits visible retry toast (no more stuck PREPARING WORKSPACE). 19/19 persistence tests. Prod-verify.
| L-133 | founder | **[HIGH infra — launch] Every deploy causes a ~30-60s full 503 outage window (no zero-downtime rollout)** — founder hit HTTP 503 'pryzm.fly.dev can't currently handle this request' + all `/api/v1/projects` GET/POST timing out (HTTP 0, 12s) DURING the 389d75a0 deploy rollout; server verified fully RECOVERED right after (GET / →200 0.5s, GET /api/v1/projects →401 0.3s). Root: Fly deploy stops the old instance before the new one is health-passing (single-machine / non-rolling), so there is a brief window with NO healthy instance → whole site 503s. Amplified this session by MANY rapid pushes (each = a deploy = an outage window). L-132 (4f5a7b2d, now live) makes the CLIENT degrade gracefully in that window (12s timeout + retriable toast instead of infinite 'PREPARING WORKSPACE' hang) — confirmed working in the founder's console — but the server-side outage window itself remains. | Infra / Fly deploy strategy (zero-downtime) | FIXED (founder-approved) | investigate fly.toml deploy strategy: rolling with `min_machines_running`>0 / a second machine / health-check grace so the old instance stays until the new one passes health checks (blue-green or rolling) → zero-downtime. ALSO: orchestrator must BATCH pushes (not push per-fix) to minimise deploy windows during founder testing. Do NOT change deploy infra unilaterally — confirm with founder (a bad fly.toml change can itself cause an outage). Maps deploy/ops; no shortcuts. §OPS-ZERO-DOWNTIME-DEPLOY. |   ✅ FIXED (fly.toml [deploy] strategy=bluegreen, §OPS-ZERO-DOWNTIME-DEPLOY, founder-approved) — boots green machine, health-checks (migrations done), then cuts traffic + stops blue → ZERO downtime, no 503/migrations window. Transient 2nd machine only during deploy (~$0). Composes with L-137 pool-drain. Held to batch with P2 = first bluegreen deploy.
| L-134 | founder | **[CRITICAL] Create fails 'Failed to create project' during server boot — client does NOT retry the server's own `migrations_in_progress` 503 retry-signal** — server returns HTTP 503 `{code:'migrations_in_progress', error:'Server is starting up — database migrations in progress. Retry in a few seconds.'}` during boot (server/dbMigrate.js gate). L-132 (4f5a7b2d) retries transport failures (timeout/HTTP 0) but EXPLICITLY does NOT retry a completed 5xx → so a boot-window 503 fails hard instead of retrying the few seconds the server asks for. Monitor confirms the machine is FLAPPING (root/api 200→000→200 every ~4-5min = repeated restart+migration windows), driven by this session's ~15 rapid deploys each triggering a boot-migration gate. Server is healthy BETWEEN windows (200/401 fast). Ties to memory [[migration-gate-vs-inmemory-fallback]] (503 migrations_in_progress) + L-133 (no zero-downtime deploy). | Onboarding/persistence retry on retriable 503 + deploy churn | FIXED | routed → retry-hardening agent. Extend the L-132 request helper (`ProjectListClient.req`) to ALSO retry on HTTP 503 when the server marks it retriable (`code:'migrations_in_progress'` or a `retriable`/Retry-After signal), with backoff up to a ~45-60s total budget (migrations can take longer than transport retries) — so create/list SUCCEED once migrations finish instead of erroring. Keep NON-retriable 4xx/5xx (real errors) failing fast. ORCHESTRATOR: STOP the deploy churn — batch one final deploy carrying this fix; investigate L-133 zero-downtime + whether boot migrations exceed Fly health-check grace (restart loop). Maps C05 + onboarding UX + [[migration-gate-vs-inmemory-fallback]]; no shortcuts; P8. §FIX-RETRY-MIGRATIONS-503. |   ✅ FIXED 209d62a6 (in deploy) — client retries 503 migrations_in_progress up to ~60s (Retry-After honored); 25/25 tests.
| L-135 | founder | **[FEATURE — plan wall drawing] Alignment/inference SNAP guides on plan-view wall creation (2nd point 'tendency to stop' at perpendicular intersections) — 3D has guidelines, 2D plan does NOT** — when drawing a wall in plan view, the second point should infer + softly snap to alignment references (esp. a PERPENDICULAR projection from existing wall endpoints / points, plus extension/collinear alignment), with a visible inference guide (dashed guideline) like the 3D wall tool already shows. Today plan-view wall drawing has no alignment inference, so precise perpendicular placement is hard. | Wall creation UX — plan-view snapping/inference (SnapManager + plan wall tool) | FIXED | routed → plan-wall-snap agent (FENCED off L-130 geometry-wall + P6 renderer-three). Mirror the EXISTING 3D wall guideline/inference behaviour into the plan path: `apps/editor/src/engine/views/plantools/WallPlanToolHandler.ts` (2nd-point inference + render dashed guide on the plan overlay) driven by the shared snap infra (`packages/snapping` / SnapManager wall providers — the 3D WallTool already uses guidelines; reuse the same inference math, don't fork). Provide: perpendicular-from-existing-endpoint inference, collinear/extension alignment, and endpoint/midpoint snap, with a soft-snap tolerance + visible guideline; ENTER/click commits the snapped point. Keep pure inference math testable. Maps C11 (creation pipeline) + the snapping subsystem + wall preview contract; architecturally sound, no shortcuts; P8 spans. §FEAT-PLAN-WALL-ALIGN-INFERENCE. |   ✅ SHIPPED f86ad52e (in deploy) — plan-view perpendicular/alignment inference + dashed guide (blue/cyan) + snap chip; pure WallAlignmentInference; flag __pryzmPlanWallAlignInference; 12 tests.
| L-136 | founder | **[CRITICAL] Selecting another project won't open it (card greys) — server access-check FAILS CLOSED on a transient DB error** — console: `[initCollaboration] join-project denied for proj-… — reason: database error during access check` + `/api/projects/…/versions 500` + `/api/projects/…/thumbnail 500`. ROOT (code-confirmed): `server/projectAccess.js:57-61` — on ANY Supabase error the access check returns `{allowed:false, reason:'database error during access check'}` (fails closed), so a TRANSIENT DB hiccup is treated as 'not owner' → collaboration join denied → project never opens. The Supabase/pg pooler is currently DEGRADED (live probe: GET /api/v1/projects 401 in **7.5s** vs ~0.3s normal; health/ready ok:true), intermittently erroring under heavy version-save load (20 large versions/project + 50 projects). INCONSISTENCY: the PG path (`:91-94`) falls THROUGH on error but the Supabase path fails closed; neither returns a RETRYABLE signal so the client can't retry (unlike L-134 create). NOT a regression from shipped work (server DB code untouched); pre-existing fail-closed UX bug + Supabase flakiness ([[migration-gate-vs-inmemory-fallback]], server OI-060/§SERVER-PG-DEGRADE). | Server project access-check resilience + client open-retry | FIXED | routed → server-resilience agent. (a) SERVER `projectAccess.js`: a transient Supabase error must NOT hard-deny — FALL THROUGH to PG/in-memory (match the PG path), and if ALL sources are unverifiable due to DB errors return a distinct RETRYABLE signal (`{allowed:false, retryable:true, reason:'transient database error'}`); the join-project handler + HTTP access-check translate retryable→503 via `server/errors.js` (OI-060 classification), NEVER a permanent deny. PRESERVE the security contract — never fail OPEN (don't grant access you can't verify); only convert 'can't-verify-transient' → retry. (b) CLIENT open/join path retries on retryable 503 + surfaces 'couldn't open — retry' (mirror L-134), not a silent grey card. (c) Note the DB stress: slow/flaky Supabase pooler + heavy autosave→/versions writes → infra follow-up (pooler health / reduce save-write load; L-131 P4 helps client-side only). Maps C05 persistence + security contract + [[migration-gate-vs-inmemory-fallback]]; no shortcuts; P8. §FIX-ACCESS-CHECK-TRANSIENT-RETRYABLE. |   ✅ FIXED 1747eca8 (in deploy) — access-check falls through + retryable (never fails open); client open-retry toast; 89/89 server tests.
| L-137 | founder | **[CRITICAL — systemic] 'Why all of a sudden?' — create + open + save + general slowness ALL trace to ONE cause: the production Supabase pooler is SATURATED/DEGRADED (positive-feedback cascade)** — live probe: `POST /api/v1/projects` **HTTP 000 @ 15s** (timed out) ×2; `GET` 401 @ 7.5s (normal 0.3s). ROOT-CAUSE ANALYSIS in `docs/03-execution/analysis/INCIDENT-2026-07-06-DB-DEGRADATION-CASCADE.md`. Cascade = (A) my ~15 rapid DEPLOYS today (each = server restart → new pg Pool(max:10) → up-to-10 conns linger on Supabase if not drained within kill_timeout=10s → many stale conns vs Supabase's finite pooler client-limit) + (B) heavy autosave→/versions DB writes (20×~0.8MB/project ×N projects) + (C) `ServerSyncQueue` RETRY storm amplifying load on an already-saturated pool. Mechanism: pool max:10, a hung query holds a slot (pgClient.js:96) → all 10 slots held → every create/open/save queues → times out at connectionTimeoutMillis 10s → access-check reads error → projectAccess FAILS CLOSED → opens denied. SINCE: early session healthy; degraded over last ~1-2h as deploy count climbed + large saves accumulated. NOT a product-code regression (no server DB/pool/migration code changed this session). Subsumes the create-fails ('Create & guide me' residential) + open + save symptoms. | Server DB resilience + pool hygiene + deploy discipline (systemic) | FIXED (code) + INFRA follow-up | IMMEDIATE (no code): STOP deploy churn (holding) → Supabase drops idle conns, pool recovers; pause heavy saves a few min. CODE (next single batch): P-A=L-136 projectAccess fall-through+retryable-503 (never fail open); P-B ServerSyncQueue retry cap+backoff+circuit-breaker; P-C pg pool drain-on-SIGTERM + max sizing vs Supabase limit; P-D reduce server autosave-write freq (IndexedDB is primary via P4). INFRA (founder): Supabase pooler tier/limit, zero-downtime deploy (L-133), batch pushes. Maps C05 + security contract + [[migration-gate-vs-inmemory-fallback]]; no shortcuts; P8. §FIX-DB-SATURATION-RESILIENCE. |   ✅ code FIXED 1747eca8 (in deploy) — pool drain-on-shutdown + ServerSyncQueue circuit-breaker; DB recovered (0.35s). INFRA follow-up (founder): Supabase tier + L-133 zero-downtime + batch-push discipline.
| L-138 | founder | **[SPIKE + FEATURE — documentation core] Deterministic AutoDimension engine (walls/doors/windows → construction-ready dimension set)** — founder wants a DEEP design spike for a fully-deterministic (NO AI/ML/LLM) auto-dimensioning engine: given walls+doors+windows, generate overall + exterior-chain + opening + door/window-location dimensions (interior rooms future), non-redundant, non-overlapping, architect-grade. 8-stage pipeline (geometry analysis→graph; wall segmentation; opening analysis; chain planning; dimension selection min-complete-set; placement offsets/text/stack; conflict resolution; QA validation). 11 deliverables (architecture, data structures, geometry algos, graph rep, planning, placement, conflict detection, QA rules, per-stage pseudocode, complexity O(n log n) target, extensible modular architecture for rooms/grids/columns/sections/elevations/annotations). | Documentation subsystem — new AutoDimension engine (deterministic) | P1 SHIPPED (spike+governance+engine+button) — P2/P3/P4 queued | routed → deep-spike design agent. MUST ground the design in PRYZM's EXISTING architecture (do not design in a vacuum): the wall connectivity graph already exists (JunctionResolverV2 detectJunctions / WallFootprint2D / room-topology RoomDetectionEngine loop graph), the view-projection/documentation core (EdgeProjectorService, NativeElementMeshExporter, ViewScope, PlanViewManager, WallLayerPlanSymbolBuilder), the annotation/dimension command + store (LinearDimPlanToolHandler, AnnotationManager, dimension handlers §P3.5-DI, setOutDimensions perpendicular math), C03 (schemas/commands/state), C11 (creation), C15 (hosted openings), the C-contract doc suite + the DOC-* view contracts + ADR-0055 wall pipeline. Deliver a spike doc under docs/03-execution/spikes/ with the 11 deliverables mapped to these subsystems + a phased implementation plan (P1 exterior-chain+openings MVP → … → rooms/grids/sections). Deterministic only; no shortcuts; every new exported fn ≥1 OTel span (P8). §SPIKE-AUTODIMENSION-ENGINE. |   ✅ SPIKE 842dc2be (docs/03-execution/spikes/SPIKE-AUTODIMENSION-ENGINE.md) — new L2 pure `packages/auto-dimension`; PRYZM has both halves (naive produceDimensions emitter + DimensionString schema/render) but MISSING the intelligence layer. NOW BUILDING: governance (Contract C56 + ADR-0118 + Spec) + P1 MVP (overall+exterior-chain+opening dims, reuse detectJunctions/getOccupiedSpans/PlanarTopologyEngine) + AI-batch-panel BUTTON. Deploy 16b9a8cf HEALTHY (14/14, pool-drain = clean restart).  ✅ SHIPPED (in deploy): governance C56+ADR-0118+SPEC (4edd4387); engine @pryzm/auto-dimension L2 pure P1 (f2a74c19) — deterministic overall+exterior-chain+opening dims (ported detectJunctions/PlanarTopologyEngine to stay P2-clean); dimension.createMany one-undo; applyAutoDimensions executor; 'Auto-dimension plan' BUTTON on AI batch panel (AIPanel.ts). 8/8 engine + 17/17 dim tests; determinism byte-identical+order-independent. Prod click-through to verify. NEXT P2 outward-placement+conflict-resolution, P3 rooms, P4 grids/sections.  ✅ P2 SHIPPED 03523544 (first BLUE-GREEN deploy) — Stage-6 real outward-side placement (area-weighted centroid → signed per-side offset) + Stage-7 deterministic conflict resolution (span-dedupe DR-1/DR-2, tiny-skip, text-overlap row-bump, geometry-crossing push+QA-note); 16/16 tests; determinism re-proven. NEXT P3 room chains.  🔧 P1-HOTFIX (§FIX-DIMENSION-STOREKEY-SINGULAR): P1 computed dims (7 strings/6 runs in console) but dimension.createMany threw 'required store dimension is missing' — PluginRegistry storeKey was 'dimensions'(plural) ≠ handlers' affectedStores=['dimension'](singular); first bus dispatch of a dimension verb (AutoDimension) exposed the latent §LIGHTING-STORE-FIX-class mismatch. Fixed storeKey→'dimension'. Now the button actually persists dims.  🔧 P1-RENDER-SINK (§FIX-AUTODIM-RENDER-SINK): after the store-fix, dimension.createMany SUCCEEDS (no error) but dims DON'T APPEAR — executor sinks to the DimensionStore, but the plan renderer (PlanViewAnnotationRenderer.getDimensionsByView) draws the ANNOTATION store; nothing renders DimensionStore on the plan. Manual dim tool (LinearDimPlanToolHandler) uses annotation.create type 'linear-dim' (viewId + geometry2D.modelPoints + offset). FIX = rewire executor to emit 'linear-dim' annotations (pure engine unchanged).  ✅ RENDER-SINK FIXED de2e0fe7 (deploying) — executor emits 'linear-dim' AnnotationElements (the sink PlanViewAnnotationRenderer actually draws), chains→N-1 segment dims, signed outward offset→geometry2D.offset, active plan viewId, ONE undo (runBatch), 'open a plan view first' guard. 2/2 tests. AutoDimension P1+P2 now VISIBLE on the plan — prod-test.
| L-139 | founder | **[CRITICAL] Large OFFICE building (1366 elements / 40 levels / 1065 walls / curtain-walls) — 3D view impossible to navigate; WebGPU device-loss cascade → renderer dies** — console: shader-compile failures across ALL material types (ShaderMaterial/MeshPhysical/MeshStandard/Shadow/Basic/Sprite/LineBasic, `useProgram: program not valid`) → `WebGPURenderer: WebGPU Device Lost: A valid external Instance reference no longer exists` → WebGL2 fallback → device lost AGAIN → browser BLOCKS context (`Web page caused context loss and was blocked`) → `no GPU renderer available — neither WebGPU nor WebGL2`. Renderer fully dead → cannot navigate. Recurrence of the 40-storey heavy-scene crash ([[webgpu-heavy-scene-crash-and-instancing]], §SHADOW-DEVICE-LOSS-FIX partial). Aggravators in log: CreateCurtainWallsOnAllSlabsCommand (per-panel glass materials → PSO explosion), NME plan export 0% cache hit (81 elem), autosave hammering during the crash loop (L-137 circuit-breaker DID reset — working). | Rendering / heavy-scene WebGPU device loss + large-model 3D scalability | FIXED (pending push) | routed → heavy-scene rendering agent (investigation-first). Root-cause the device loss on 1366-elem/40-level office, then implement highest-leverage SAFE mitigations: (a) LEVEL-SCOPED 3D render culling for large models — a 40-storey building should NOT render all 40 levels of full BIM geometry at once (render active±N levels + massing LOD for the rest; leverage FrustumCullingService >500-elem threshold + LevelExplode + the Massing tier A.24); (b) device-loss RECOVERY CAP → degrade to a STABLE safe mode (WebGL, shadows off, reduced quality) instead of thrashing to a dead renderer + browser context-block; (c) verify/extend P6 §PERF-INSTANCE-MATERIAL-DEDUP (e8a42756) engages for curtain-wall glass panels (per-panel unique materials defeat instancing → PSO/shader explosion = likely device-loss root); (d) shadow/PSO budget at scale. Gated + measurable (__pryzmPerfTrace). Maps C04 rendering + P2/P3 + [[webgpu-heavy-scene-crash-and-instancing]] + [[render-tiers-massing-and-presentation]]; no shortcuts; P8. §FIX-HEAVY-SCENE-3D-SCALABILITY. |   ✅ FIXED 2f91af03 — root: 3D renders ALL 40 levels + uncapped device-loss recovery loop. (A) LevelScoped3DCullingService (active level ±1, 40→3 levels, ~13× fewer draw calls, P7 visibility, gate __pryzmLevelScoped3DCulling); (B) device-loss recovery cap→WebGL safe-mode after 2 losses (gate __pryzmDeviceLossRecoveryCap, __pryzmRenderSafeMode); (C) curtain-panel material dedup. 157/157 renderer + 14/14 service tests. Prod-verify office nav.
| L-140 | founder | ⚠ **RE-TEST after `0489984e`** — very likely the SAME root as L-200's true cause (§FIX-WEBGPU-SCENEPASS-FIRST-CASTER): the WebGPU TSL ScenePass was compiled once at boot against a caster-less scene and never rebuilt, so casters added later (generation included) were absent from the shadow graph. The original InstancedMesh hypothesis below may be a red herring. Re-test a generated building on `0489984e`+; if it still casts no shadow while hand-drawn does, THEN pursue the instancing angle. ORIGINAL: **[HIGH] Batch-GENERATED building casts NO shadow on the invisible ground-floor catcher — from-scratch/hand-drawn works great** — founder: the new real-shadow on the invisible ground layers (L-11 §FEAT-REAL-ENVIRONMENT / GroundShadowCatcher) renders perfectly for a blank/from-scratch project, but a building produced by generation (resi-from-boundary batch) casts no shadow. Founder notes batch itself isn't the point — the generated RESULT doesn't shadow. HYPOTHESIS (from logs): generated buildings render walls/elements as INSTANCED meshes (`InstancedElementRenderer` §PHASE-3 WallInstanceBridge, 'Phase 7 GPU instancing active'), and the shadow-flag setter (`PascalSceneLighting` 'Shadow flags set on N mesh(es)') does NOT set castShadow on the InstancedMeshes (hand-drawn = non-instanced simple meshes that DO cast) → generated building never contributes to the shadow pass / GroundShadowCatcher. SECONDARY: GIS/site path applies an ENU transform ('ENU transform applied to GIS BIM root') → generated building may sit in a frame offset from the L0 shadow-catcher plane. | Rendering / shadows on instanced (generated) geometry | OPEN (follow-up — needs prod bisection) | routed → rendering lane (SEQUENCED after L-139 — same renderer-three shadow/instancing files, cannot safely parallelize). Investigate: does PascalSceneLighting / the shadow-flag pass set castShadow on InstancedMesh (and does the WebGPU shadow pass include instanced meshes)? Ensure ALL generated element meshes (instanced + simple) cast shadow onto GroundShadowCatcher; verify the catcher plane spans the generated building's frame (ENU). Ties L-11 §FEAT-REAL-ENVIRONMENT + [[webgpu-heavy-scene-crash-and-instancing]] + P6 §PERF-INSTANCE-MATERIAL-DEDUP + [[instanced-aggregate-level-visibility]]. Maps C04 rendering; no shortcuts; P8. §FIX-INSTANCED-GENERATED-SHADOWS. |   📋 L-139 agent findings: 'missing castShadow' is FALSE (InstanceGroup sets castShadow=true; InstancedMesh IS a Mesh → PascalSceneLighting covers it). Real candidates (prod-bisect): (a) shadow-off-at-scale BUDGET (survival tier / ≥8000-caster shadow-off → generated building casts no shadow BY DESIGN; SAME subsystem as L-139, re-enabling = device loss); (b) InstancedMeshCoalescer inherits castShadow=false if source built during batch shadow-deferral; (c) transparent-glass skip; (d) GIS/ENU frame off L0 catcher. Sequence as separate follow-up (candidate (a) conflicts with L-139).
| L-141 | founder | **[MED — UX polish] Add a quick LOADING OVERLAY during renderer backend live-swap (WebGL ↔ WebGPU) + device-loss recovery** — the live backend swap (ADR-0077 §RENDERER-LIVE-SWAP, `initScene.swapRendererBackend`) and the WebGPU device-loss→WebGL fallback disrupt the viewport (blank/flicker) with no indicator; log shows `live swap complete — backend now: webgl-fallback` + `WebGPU device lost: reason=destroyed`. Founder wants a brief loading layer covering the swap. | Rendering / renderer live-swap UX (ADR-0077) | FIXED (pending batch push) | routed → rendering lane, SEQUENCED after L-139 (both edit initScene swapRendererBackend + the device-loss recovery swap — cannot safely parallelize; the swap emits NO event to hook a pure-UI overlay onto). Show a lightweight loading overlay on swap START (initScene.ts ~3588) + on the device-loss recovery swap; hide on COMPLETE (~3728) and ROLLBACK (~3741-3757). Bonus: covers L-139's new safe-mode recovery swap. Reuse existing loading/overlay UI (e.g. the engine loading overlay / BatchLoadingIndicator pattern), gate none (always-on). Maps ADR-0077 §RENDERER-LIVE-SWAP + C04; P8. §FEAT-SWAP-LOADING-OVERLAY. |   ✅ FIXED 55539267 — always-on loading overlay (reuses PryzmLogoSpinner; CSS-compositor spin survives main-thread GPU stall, P3-safe) on manual swap (initScene.swapRendererBackend) + device-loss recovery (createRenderer), finally-guaranteed hide + 20s backstop, ref-counted. 6/6 tests.
| L-142 | founder | **[HIGH — GIS] 3D globe (Cesium) opens to BLACK SCREEN / building floating in space; only 'Zoom to Site' frames it in context** — founder wants the INITIAL globe-open to show what 'Zoom to Site' shows (building centred in photoreal 3D-tiles context). ROOT (log-confirmed, two causes): (1) **CSP blocks Cesium World Terrain** — `Connecting to https://assets.ion.cesium.com/...CesiumWorldTerrain... violates connect-src` → terrain load FAILS → falls back to an ASYNC photoreal-tile height clamp (`getPhotorealBareEarthTerrain`/`samplePhotorealBareEarthHeight`/`clampToPhotorealTilesThenReplace`). (2) **Camera framed BEFORE the async clamp/re-placement** — building placed at base 0 m + `§GLOBE-FIT-BUILDING flyToBoundingSphere` + `reframeSiteIn3D` fire, THEN the clamp resolves real ground height (707 m) and `§FORMA-FULL-HEIGHT` RE-PLACES the building at base 707.2 m — but the camera keeps the stale base-0 frame → looking at empty space (black). 'Zoom to Site' re-frames to the final placement → works. | GIS / Cesium globe framing + CSP terrain (C18-C23, ADR-0255) | REVERTED (b448f4c8) — my fix regressed placement | routed → GIS/geospatial agent (disjoint from renderer/auto-dim lanes). FIX (a) CSP: add `assets.ion.cesium.com` (+ needed `*.ion.cesium.com`) to server.js connect-src so Cesium World Terrain loads (or intentionally keep the photoreal-clamp path but make it deterministic). FIX (b) TIMING: issue the globe-open camera framing (flyToBoundingSphere/reframeSiteIn3D) AFTER the async height-clamp + re-placement settles — i.e. the initial open performs the SAME framing as 'Zoom to Site', keyed off the FINAL formaMassingOrigin (post-clamp), not the base-0 placement; await/sequence the clamp before the first fly, or re-fly on placement-settled. Files: apps/editor/src/ui/geospatial/CesiumViewport.ts + apps/editor/src/ui/layout/GISAreaLayout.ts + server.js CSP (minimal). Maps C18-C23 geospatial + ADR-0255 (§CSP) + [[geospatial-foundation-strategic-direction]] + [[casa-demo-layout-blockers-2026-06-05]]; no shortcuts; P8. §FIX-CESIUM-GLOBE-OPEN-FRAMING. |   ✅ FIXED 3c0a6e63 — (A) CSP: added assets.ion.cesium.com + *.ion.cesium.com to securityHeaders.js connect-src → Cesium World Terrain loads (37/37 security gates pass); (B) GISAreaLayout globe-open now placeBuildingOnGlobe(true) → arms existing §GLOBE-FRAME-NO-JUMP settle machinery → re-frames via the SAME flyToFormaSite as Zoom-to-Site AFTER the async height-clamp settles. CesiumViewport untouched. Initial open == Zoom-to-Site.  ⛔ REVERTED b448f4c8 (2026-07-07, founder: 'you have done WORST — regression'): enabling ion World Terrain (the CSP change) made the globe sample real terrain (~727.9 m) and re-place the MASSING there, but the REAL model still placed at base 0.00 m → 727 m UNDERGROUND; and the armed settle-reframe is suppressed once the user moves the camera (§GLOBE-FRAME-NO-JUMP) so the camera never corrected. PRE-L-142 state (no terrain → building at base 0 on flat ground + MANUAL Zoom-to-Site) worked; reverted BOTH hunks (GISAreaLayout placeBuildingOnGlobe(true)→() + securityHeaders ion-terrain CSP) to restore it. LESSON: the ONLY thing needed was camera-focus (Zoom-to-Site already did that) — do NOT enable terrain that the real-model placement doesn't also clamp to. Any future globe-framing work must keep real-model + ground on the SAME base and must NOT reintroduce the underground mismatch.
| L-143 | founder | **[HIGH — perf] Sun Hours solar analysis on 3D Site/Forma view takes ~1 MINUTE; founder suspects navigating the 3D view RE-TRIGGERS the compute** — log: `[site-metric] §perf cell-cap: 14400 cells would exceed 5000; clamping 4.0→6.8m` → `sunHours heatmap 512×512 from 1621/1621 cell(s), radius 240m (chunked)`. Cost = 1621 cells × sun-time-samples × raycasts against a LARGE occluder set: 4755 OSM context buildings + 958 context road ribbons + 192 walls + 26 slabs etc. Raycasting thousands of occluders per cell per sun-sample = the ~1min. Founder's navigation suspicion = plausible: if a camera-move listener re-invokes the COMPUTE (not just re-paints the heatmap) it re-runs the whole minute. | Solar analysis / geospatial site-metric perf (C21-CLIMATE-INGESTION, ADR-0074, @pryzm/solar-analysis) | FIXED (pending push) | routed → solar/geospatial-perf agent, SEQUENCED after L-142 (sun-hours heatmap render lives in CesiumViewport.ts which L-142 owns — avoid collision; compute-side @pryzm/solar-analysis + worker + trigger is disjoint bulk). FIX: (1) confirm+kill navigation-triggered RECOMPUTE — camera move only re-renders the existing heatmap texture, NEVER re-runs the raycast; gate compute to explicit trigger + cache by (geometry+sunparams+date); (2) OFF-MAIN-THREAD worker for the raycast compute (mirror compress.worker / geometry.worker pattern) — non-blocking + cancellable; (3) SPATIAL-INDEX occluders (BVH/three-mesh-bvh or the spatial-index pkg) — 4755 context buildings raycast naively is the dominant cost; (4) adaptive cell/sun-sample resolution + progressive reveal (already chunked). Files: @pryzm/solar-analysis (pryzmComputeSunHours raycast+heatmap), the sun-hours trigger (FormaSiteAnalysisControls sun scrubber), siteRealData site-metric; CesiumViewport site-metric render ONLY after L-142. Maps C21 + ADR-0074 + [[real-solar-heat-thermal-queue]] + [[geospatial-foundation-strategic-direction]]; no shortcuts; P8. §PERF-SUNHOURS-WORKER-BVH-NO-RECOMPUTE.   ✅ FIXED 3fe8848a (pending push) — ROOT of navigate→recompute was NOT the ground heatmap (already texture-cached + camera-independent per §SITE-METRIC-RADIUS-DECOUPLE/ADR-0079 → orbit/zoom only repaints) but the FAÇADE study `renderFacadeAnalysis` (NO cache) re-invoked by every renderFormaMassing incl. terrain-settle re-clamp re-places that stream during nav → full per-point raycast re-ran. FIX: (1) BVH-style occluder pruning — new pure THREE-free packages/solar-analysis/occluderIndex.ts (uniform grid over occluder bboxes); sunBlockedIndexed tests only occluders whose bbox the shadow ray could cross (bounded by roof-clearance) not all ~4755 → O(4755)→O(near-ray), ~2 orders on the dominant term, keeps chunked build smooth WITHOUT a worker; determinism byte-identical (index returns a proven SUPERSET, same rayBlockedByPrism OR-ed). (2) No-recompute caches — occluder geometry signature folded into the ground cache key (was origin-only → stale after refetch) + façade memo (facadeAnalysisKey) so renderFacadeAnalysis no-ops when inputs unchanged+painted. 46 solar + 53 siteMetric tests (naive-vs-indexed equivalence over dense city × summer/winter/equinox); root tsc exit 0. DEFERRED (ADR-0110, designed not shipped): (#2) off-main-thread worker (BVH made main-thread smooth; worker-ready), (#4) adaptive coarse→refine resolution. | 
| L-144 | founder | **[HIGH — analysis fidelity] Forma FAÇADE / sun analysis runs on a SIMPLIFIED MASSING PROXY, not the ACTUAL building geometry** — founder: façade analysis is done on a geometry of 'similar shape' to the building, wants it on the EXACT real geometry. Log-confirmed: the Forma study builds a massing prism (`§FORMA-FULL-HEIGHT` perimeter-prism extrusion, `§FORMA-FACADE-FOOTPRINT-FIX footprint from floor-slab (4 pts)`, `§FORMA-FACADE-SMOOTH painted 5 façade surface(s): 4 wall face(s) + roof, 539 lattice node(s)`) — analysis paints onto the 4-face prism. Meanwhile the REAL model IS present: `§forma6 REAL full-fidelity model placed … massing blocks hidden` (192 real walls, 228 openings, 1265 elements). So the analytic façade/sun study surface ≠ the visible real building. | Solar/Forma façade analysis fidelity (C21-CLIMATE-INGESTION, ADR-0074) | DEFERRED (designed; staged behind L-143 worker) | routed → solar/Forma-analysis agent, BUNDLED with L-143 (perf) and SEQUENCED after L-142 (all share CesiumViewport). Run the façade/sun-hours analysis against the REAL building façade surfaces (actual wall faces WITH window openings) — reuse the already-placed REAL geometry (the GLB / real wall meshes) as the analysis surface instead of the 4-face massing prism; map the heatmap/lattice onto real faces. DEPENDS on L-143 (worker-offload + BVH) so running the study on the heavier real geometry stays fast (else it's slower than the current ~1min). Keep the massing as a fast-preview/fallback tier (A.24) but the RESULT must be on the real geometry. Files: CesiumViewport forma-facade (§FORMA-FACADE-*), @pryzm/solar-analysis, FormaSiteAnalysisControls. Maps C21 + ADR-0074 + [[real-solar-heat-thermal-queue]] + [[render-tiers-massing-and-presentation]]; no shortcuts; P8. §FIX-FACADE-ANALYSIS-REAL-GEOMETRY.   ⏳ DEFERRED (Agent C, ADR-0110) — designed + UNBLOCKED by L-143 (BVH + memo), but intentionally staged BEHIND the off-main-thread worker (#2): running the study on the heavier REAL wall-faces-with-openings without the worker risks the perf bar, and the no-shortcuts mandate forbids shipping it half-fast. NEXT: land the solar worker (#2), THEN swap the façade analysis surface from the 4-face massing prism to the real placed wall meshes (massing kept as fast-preview tier A.24). Route as a follow-up once the worker lands. | 
| L-145 | founder | **[CRITICAL — blocks AutoDimension] Auto-dimension button computes dims but NONE render — `annotation.create` Zod-REJECTS the id** — console: `AnnotationSchemaError … pattern /^annotation_[0-9A-HJKMNP-TV-Z]{26}$/ … Expected annotation_<ulid> id` ×7 (one per string). ROOT: the L-138 render-sink executor (§FIX-AUTODIM-RENDER-SINK) minted the annotation id via `crypto.randomUUID()` (bare UUID), but AnnotationSchema id MUST be `annotation_<ULID>` (ADR-0061 typed ids). The `annotation.create` handler does `cmd.id ?? createId('annotation')`, so a supplied UUID is used verbatim → Zod reject → 0 annotations → nothing renders. LATENT TWIN: the MANUAL `LinearDimPlanToolHandler` uses the SAME `crypto.randomUUID()` (error only `.catch`-logged) → manual linear dims fail identically. | Documentation / AutoDimension annotation-id contract (ADR-0061) | FIXED (pending combined push) | Fixed BOTH plan-tool sinks to mint `createId('annotation')` (canonical factory → `annotation_<ULID>`): apps/editor/src/ui/documentation/applyAutoDimensions.ts + apps/editor/src/engine/views/plantools/LinearDimPlanToolHandler.ts. NOTE: plugins/annotations tools also pass `crypto.randomUUID()` into makeAnnotationElement — flagged for the compliance sweep (agent a2997db2) to confirm which dispatch paths are affected. Maps C03 + ADR-0061 (typed ULID ids) + C56 AutoDimension; no shortcuts; P8. §FIX-AUTODIM-ANNOTATION-ID. |   ✅ id FIXED bdb397ac (createId('annotation') at both sinks) — removes the Zod error. ⚠️ BUT compliance audit (COMPLIANCE-AUTODIMENSION-2026-07-07.md) confirms a DEEPER P0: the render-sink dispatches the full 'linear-dim' AnnotationElement through the BUS `annotation.create`, whose handler (CreateAnnotation.ts) keeps only viewId/kind/anchor/text and DROPS geometry2D+references, writing a flat AnnotationData to AnnotationsState — NOT the subsystem `annotationStore` that PlanViewAnnotationRenderer reads. The render-sink mirrored the WRONG manual tool (plan-tool LinearDimPlanToolHandler also uses the bus verb + also doesn't render); the WORKING plugin tool writes via CreateAnnotationCommand→annotationStore.add. So dims STILL won't render until the executor (and LinearDimPlanToolHandler) write to the subsystem annotationStore. Definitive fix + missing pryzm.autodim.apply span + DI-6 QA-errors + superseding ADR (reconcile the 3 dimension representations) routed → AutoDim-render-completion agent. §FIX-AUTODIM-SUBSYSTEM-STORE-SINK.   ✅ FIXED e4cbc1d2 (pending combined push) — new `CreateManyAnnotationsCommand` (plugins/annotations) writes the whole set to the SUBSYSTEM annotationStore (the store PlanViewAnnotationRenderer reads via getByView) as ONE undo (C11/C24.1); executor dispatches it via CommandManager inside runBatch, wrapped in the mandated `pryzm.autodim.apply` span (C56 §1.7, P8); LinearDimPlanToolHandler now writes the subsystem store via CreateAnnotationCommand too. GOVERNANCE: ADR-0119 supersedes ADR-0118's sink half (canonical RENDER sink = subsystem annotationStore 'linear-dim'; dimension.createMany/DimensionStore RETAINED for schedules/export, not rendering); C56 + SPEC-AUTODIMENSION amended in place. Root tsc exit 0; 16/16 auto-dim + 55/55 annotations (incl new 5/5) + 2/2 executor. DEFERRED (ADR-documented, P2): hard-reject QA `errors` + QA-2 chain-gap/overlap; deterministic persisted annotation ids.
| L-146 | founder | **[CRITICAL — wall joins] Starting a NEW wall at the SHARED CORNER of two walls already joined in an L malforms the TWO EXISTING walls (geometry transforms) — must NEVER happen** — founder: two walls meeting in a clean L (mitre); clicking that L-corner vertex as the 1st point of a 3rd wall deforms the original two walls (screenshots: clean L → corner-start → malformed). CONFIRMED CONTRAST: starting the 3rd wall at a wall MID-POINT gives a SOUND T-junction ('on midpoint the junction is perfect'). So the mid-point/T path is the reference-good behaviour; the near-shared-corner re-solve is the defect. REFINEMENT (founder, image): the malform triggers when the 1st point is NEAR the corner but NOT *precisely* the shared END point of both walls (558mm/681mm dims show it slightly off) → the resolver builds a NEAR-COINCIDENT (not exactly coincident) vertex cluster and drags the existing mitre. Fundamentally a SNAP-PRECISION + near-coincident-cluster tolerance bug: an imprecise near-corner start must either snap EXACTLY to the shared vertex (clean junction) or be rejected/guarded — it must NEVER deform the existing walls. Likely JunctionResolverV2 re-clusters the corner as a 3-wall junction and MOVES the existing shared endpoints/mitre despite the L-130 existing-corner-immutable guard (`__pryzmWallV2ExistingCornerImmutable`) — flag off in prod, or guard doesn't cover a NEW wall arriving at an existing corner. Founder proposal: constrain WHERE a new wall may start near an existing L-corner, and/or allow only a T (mid-point) join. | Wall junction resolver — 3rd-wall-at-existing-L-corner (ADR-0055 Pascal pipeline, JunctionResolverV2) | FIXED (pending combined push) | routed → wall-join agent (investigation-first; FENCED to packages/geometry-wall JunctionResolverV2/WallPipelineV2/WallFragmentBuilder + apps/editor WallRebuildCoordinator + WallPlanToolHandler snap — DISJOINT from AutoDim/render/GIS lanes). Root-cause why a 3rd wall at an existing L-corner mutates the two existing walls; those two walls' geometry MUST stay immutable (verify/repair __pryzmWallV2ExistingCornerImmutable covers new-wall-at-corner AND is ON in prod). The sound mid-point T path is the reference — make corner-start EITHER produce an equally-sound junction OR snap-guard the start off the exact shared vertex (founder's T-only proposal) as an explicit testable rule. Ties [[walljoinresolver-multi-cluster-bug]] + [[adr-0055-pascal-wall-pipeline]] + L-130. Maps ADR-0055 + C11 + C-wall contract; no shortcuts; P8 spans. §FIX-WALL-3RD-AT-LCORNER-IMMUTABLE.   ✅ FIXED 1bdc51ba (pending combined push w/ AutoDim) — ROOT: near-but-not-exact corner start is fused by clusterEndpoints into the L-corner node → applyRingSweep runs a 3-way miter around the CENTROID (A.end+B.start+C.start)/3, dragged off the true vertex → rewrites the two existing walls' miters/footprints = deform. Explains founder's refinement (exact-vertex & mid-point-T sound; only near-but-off drags). L-130 guard WAS on in prod but INSUFFICIENT — its systemTypeId-difference key fails for a SAME-type/type-less 3rd wall. FIX (JunctionResolverV2, type-INDEPENDENT generalisation keyed on coincidence TIGHTNESS): in a pure co-terminating ≥3-endpoint cluster, tight-cluster at 1mm; when one tight group forms a non-collinear corner and ≥1 endpoint sits outside it, FREEZE the corner (byte-identical L/N-way miter → existing walls truly immutable) + re-seat each near newcomer as its own T butting the most-perpendicular arm. Detection-frame only (never moves a baseline → no §CLAMP-COSHARE-WELD/ADR-0072 doubling). Flag __pryzmWallV2ThirdAtLCornerImmutable default-ON. 184/184 geometry-wall tests (new near-corner immutability suite across 4 angles + exact-Y/mid-T/L-44 regression guards). FOLLOW-UP (nice-to-have, orchestrator to route → core-app-model agent): make PlanSnapEngine endpoint-snap WIN near an L-corner so the plan tool sets the start exactly on the vertex (UX complement; resolver fix is the durable path-independent backstop). |
| L-147 | founder | **[HIGH — AutoDimension output QUALITY on non-rectangular footprints] Dims now RENDER (L-145 confirmed working) but the generated SET is not architect-correct: (a) SPURIOUS DIAGONAL dims + (b) INCOMPLETE exterior chains** — founder screenshots (L-shaped/notched footprint): AutoDimension draws a `34601 mm` dimension running CORNER-TO-CORNER diagonally across the shape (plus faint diagonal lines to the bottom-left corner) — a point-to-point distance between two non-axis-aligned extreme corners. Auto-dims MUST be ORTHOGONAL only (each string projected onto the X or Y axis; overall extents = clean offset chains OUTSIDE the footprint), NEVER a diagonal across it. AND the exterior chain is INCOMPLETE (founder red arrows): the L-shape's jogs/segments (the 6000/7000 bottom-right notch, the left-side step ~29262/14000) are not each dimensioned — every exterior wall run on each cardinal side needs its segment + overall chain. Root: the engine's Stage-4 chain-planning / Stage-5 selection (C56/SPEC) handles a simple rectangle but on a non-rectangular (L/notched) footprint it (i) emits an overall as a point-to-point diagonal instead of axis-projected H/V extents, and (ii) doesn't build a complete per-side exterior chain over the jogged perimeter. | Documentation / AutoDimension engine quality — non-rectangular footprints (C56, SPEC-AUTODIMENSION, ADR-0118/0119) | FIXED (pending push) | routed → AutoDimension-engine-quality agent (FENCED to packages/auto-dimension + apps/editor applyAutoDimensions; DISJOINT from the live solar agent + wall code). FIX in the PURE engine: (1) NEVER emit a diagonal — every DimensionString is axis-aligned (project extreme points onto X or Y; overall = bbox H + V as offset chains outside the footprint on the appropriate side); guard/reject any string whose endpoints aren't collinear on a cardinal axis. (2) COMPLETE exterior chains for non-rectangular footprints: walk the perimeter per cardinal side (using the wall connectivity graph / footprint the engine already ingests), emit a segment chain covering every wall run + jog on that side, plus the overall. (3) This is also where the deferred QA-2 (chain-gap/overlap) + DI-6 completeness errors belong — a gap in the exterior chain should be a QA error. Deterministic; reuse detectJunctions/PlanarTopologyEngine footprint; every new exported fn ≥1 span (P8). Maps C56 §Stage-4/5 + SPEC QA-2 + ADR-0118/0119 + [[typology-pipeline-and-hektar-gap]]; no shortcuts. §FIX-AUTODIM-ORTHO-COMPLETE-CHAINS.   ✅ FIXED 297b2c16 (pending push) — ROOT of the 34601 diagonal: planOverall references the two EXTREME perimeter corners (min-X/max-X, min-Z/max-Z) which on an L/notched footprint are NOT collinear on the cross-axis; the string was correctly tagged orientation:horizontal but the executor built geometry2D.modelPoints from the true-corner p1World/p2World with NO measurementNormal → PlanViewAnnotationRenderer fell into its DIAGONAL branch (drew corner-to-corner, labelled bbox hypot). FIX: new cardinalMeasurementAxis(orientation) (C56 DI-7 orthogonal-only invariant, P8-spanned); executor stamps geometry2D.measurementNormal from the string orientation (mirrors LinearDimensionAnnotationTool §DIM-ORTHO) → renderer draws axis-aligned + labels bbox W/H, never the diagonal (ADR-0119 render sink + span preserved). Completeness: engine ALREADY covers every jog on connected perimeters (per-run chains); added detectChainCoverageGaps() = SPEC QA-2 chain-gap/overlap (was deferred, non-blocking warnings) + QA-7 non-orthogonal guard. Docs amended in place (C56 §1.3 DI-7+QA-2, SPEC INV-7/QA-2/QA-7). 27/27 auto-dim tests (+11 new: notched-L zero-diagonal, overall=bbox not hypot, determinism), root tsc exit 0. DEFERRED (flagged): executor converts sheet-mm offsetMm→world naively (/1000) so at building scale dims sit ~8-24mm off the wall (near-overlapping) not visibly OUTSIDE — separate offset-scale root cause → follow-up (queue as L-155). |
| L-148 | observed (founder log) | ✅ **RESIDUAL FIXED `8d2572ea`→main** (⚠ the earlier "already fixed" verdict was WRONG — the founder's live prod console still spammed the warn ~once per project on every load; orchestrator relayed the agent's claim without checking it against the log. Corrected.) **TRUE RESIDUAL, all three leads confirmed:** (a) `_setItemWithEviction` could only free `bim-project-<id>-versions` keys (`_versionKeysOldestFirst`, :297) — every other consumer was unreclaimable (`pryzm.floorPlanUnderlay.v2.*` multi-MB legacy rasters migrated to IDB only on project OPEN, `pryzm-sync-queue`, prefs), and even version blobs `warmVersionCache` had ALREADY copied to IDB were left as dead duplicates; (b) **the warms raced the sync** — `warmVersionCache()` was fire-and-forget while only `warmThumbnailCache()` was awaited, so per-project `saveProject` writes fired while the heavy legacy blobs were still resident; (c) `syncFromServer` looped `saveProject` per project, each doing a full `_serializeIndex` + `_setItemWithEviction` → a first-ever 50-project sync = **50 full-index writes and up to 50 quota warns** (exactly what the founder saw). **FIX:** new `saveProjectsBatch(upserts, deleteIds)` reads the index once, applies all upserts (routing/stripping thumbnails) + deletes, writes **exactly ONCE** (`syncFromServer` accumulates then calls it once — proven by a test counting `setItem('bim-projects-index')`); `_warmThenSync()` AWAITS both warms before the first write (purge check moved to the IDB-mirror-aware `versionRepository.getVersions(id)`, since the raw localStorage read would now miss migrated blobs and wrongly purge projects with local history); `_setItemWithEviction` gains a zero-data-loss **Tier-1 `_reclaimRedundantLegacyVersionStores()`** — drops localStorage version blobs already mirrored in IDB (authoritative copy untouched) before Tier-2 oldest-first eviction. Only this module's own keys are touched — foreign bloat (underlay rasters) is never deleted, honouring C13 + the §06 §7 single-writer rule. Genuine exhaustion now degrades with **exactly one** warn, no data loss. 34/34 persistence tests green; the single tsc error at commit time was `initTools.ts` from the concurrent furniture lane's uncommitted WIP, not this commit. **RESIDUAL (honest):** if localStorage is saturated by *live, not-yet-migrated* underlay rasters (owned by the underlay lane), the single write can still fail with one clean warn — that raster migration belongs to the underlay lane and cannot be reclaimed here without breaching single-writer. Earlier work (still valid): thumbnails → IDB (`ThumbnailCacheStore`), versions + sync queue → IDB (`VersionCacheStore`), lean metadata index stays in localStorage; migration coverage `52c12339`.   ORIGINAL prior verdict (superseded): "ALREADY FIXED (`20562684` / `36fa48ef` / `8f7ba7f7`)". ROOT: thumbnails (~5–500 KB WebP data-URLs) were stored INLINE in the single `bim-projects-index` blob (`ProjectRepository.saveProject` → `_setItemWithEviction`), so ~50 projects blew the ~5–10 MB origin cap; eviction "exhausts" because it can only delete OTHER projects' version stores, never shrink the index's own inline payload. FIX (shipped): thumbnail bytes → IndexedDB (`ThumbnailCacheStore`, sync in-memory mirror for the render path, fire-and-forget so a thumb write can't cascade into "index not saved"); version snapshots + sync queue → IndexedDB (`VersionCacheStore`, localStorage only a cold fallback); the lean metadata index stays in localStorage; quota path degrades observably (eviction-with-retry keeps the newest save + a single toast, no per-project churn); migrations MOVE legacy inline thumbs + legacy version stores into IDB then strip them, C13 isolation preserved. `52c12339` adds the 6 untested migration cases; 23/23 persistence tests green, tsc 0. RESIDUAL (cross-cutting): several suites under `apps/editor/__tests__` are not matched by the root vitest include globs, so these persistence gates don't run in CI — worth closing. ORIGINAL: **[MED — client persistence] `localStorage quota exceeded — project index not saved (eviction exhausted)` repeats on every project sync (50 projects)** — `ProjectRepository.saveProject` (main bundle) can't persist the project INDEX because localStorage is full and its eviction can't free enough; fired once per project across all 50 on load. Risk: the local project index/thumbnails don't persist → project list may not survive a reload offline, and the write churns on every sync. Thumbnails-in-localStorage is the likely bloat. Ties [[localhost-dev-unusable-test-on-prod]] persistence + L-131 P4 (IndexedDB is the primary version store). | Client persistence — ProjectRepository localStorage quota | OPEN (queued, lower priority) | route → client-persistence agent when reached: move the project index + thumbnail blobs OFF localStorage onto IndexedDB (the VersionRepository tier already used for versions), or store only lightweight index metadata in localStorage + thumbnails in IndexedDB/object cache; make eviction bounded + non-churning (don't rewrite the whole index per project). Maps C05 persistence + [[fly-production-deploy]]; no shortcuts; P8. §FIX-PROJECT-INDEX-INDEXEDDB. |
| L-149 | founder | **[UX — UI layering / overlap] The 3D-Globe + Plan/Site launcher rail ("3D Site / Globe", "Plan + Site", "Graph", "Living Graph") sits BELOW other UI and OVERLAPS — bring it to the top; UI must be overlap-aware; needs a CLEAR UI/UX interface CONTRACT (analyse→plan→fix)** — founder screenshot: the always-on GIS launcher pills (mounted at initUI: `[gis][site-view] 3D Site launcher (L-40)` + `[gis][plan-gis] Plan + Site (GIS) launcher (L-104)` + graph/living-graph) render under/over adjacent chrome. ROOT (systemic): NO central z-index/layer scale — **325 hardcoded `z-index` values across apps/editor/src/ui** with no single source of truth → arbitrary stacking, overlaps, and no element is 'aware of each other'. Founder wants a formal UI/UX layering contract + an overlap analysis + the fix, not a one-off z-bump. | UI shell / layout & z-index layering (C06-UI-SHELL-AND-TOOLS) | FIXED (pending push) | routed → UI-layering agent (FENCED to apps/editor/src/ui UI-chrome + initUI.ts + launcher components + a global z-layer token module; EXCLUDES apps/editor/src/ui/documentation/** + packages/auto-dimension [live Agent D] + CesiumViewport/solar). DELIVER: (1) CONTRACT — extend C06 (or a new C57-UI-LAYERING-AND-OVERLAP) defining a single NAMED z-index LAYER SCALE (tokens: base/canvas < panels < launchers/rails < popovers < modals < toasts < spinner/overlay) as the ONLY source of stacking truth, + a no-overlap layout policy (elements declare their region; chrome must not occlude peers), + migration rule (no raw z-index literals; use the token). (2) ANALYSE — z-index/overlap audit of the 325 sites; map the launcher rail + neighbours to the scale. (3) FIX — raise the launcher rail to its correct layer (above map/canvas, below modals/toasts) and de-overlap it from adjacent chrome; introduce the token module + migrate the launcher + its immediate neighbours (full 325 migration = phased follow-up). Maps C06 + the vision (professional BIM UI) + [[onboarding-site-generate-view-flow]] brand; no shortcuts; P8 where new exported fns. §FIX-UI-LAYERING-ZINDEX-CONTRACT.   ✅ FIXED 5baf97a0 (pending push) — ROOT: launcher pills were position:absolute inside a no-stacking-context container → competed at ROOT level + lost to toolbar(9000)/nav-rail(9999); AND the 4 pills came from 2 code paths with hand-picked bottom-left offsets that physically overlapped. CONTRACT: extended C06 §7 'UI Layering & Overlap' (chose C06 extension over new C57 — same subsystem) = single named z-layer token scale (canvas 0 · underlay · viewportHud · panel · rail · toolbar 9000 · contextualBar · launcher 10000 · popover · drawer · modal · toast · loadingOverlay · critical) mirrored in apps/editor/src/ui/layout/zLayers.ts + layout.css --z-* vars, + no-overlap layout policy (launcherRailStyle slot column) + migration rule (no raw z-index literals). FIX (Phase 1 proof): all 4 always-on launchers → position:fixed at launcher layer, appended to body, collision-free 44px-pitch column → beats all chrome, sits below modals/toasts/spinner. Phases 2-7 (overlays/modals, platform shell, docks/panels, canvas overlays, result bars, CesiumViewport) logged in C06 §7.4 for the remaining ~325 sites. Root tsc exit 0; scoped 6 files. Maps C06 §7 + vision; no shortcuts; phased so nothing left half-done. |
| L-150 | founder | **[HIGH — rendering] Opening a large existing project (40-storey circular 'Paris' tower, 1238 elems) — NOT all slabs/walls render in 3D** — founder: big circular tower ~40 stories, most floors' geometry missing. ROOT (log-confirmed, NOT a glitch): this is the L-139 §FIX-HEAVY-SCENE-3D-SCALABILITY `LevelScoped3DCullingService` doing its job — `3D view scoped to active level ± 1 on large models (>500 elems or >5 levels)` + `Loading 40 levels`. The service ONLY toggles `Object3D.visible=false` on out-of-scope levels (verified: `LevelScoped3DCullingService.ts` hides roots, tracks `_hidden`, NO massing/LOD fallback) → only 3 of 40 storeys render. L-139 shipped the CULLING half but not the MASSING-LOD half its own design called for ('render active±N levels + massing LOD for the rest'). Net: the heavy-scene crash is prevented but the user can't see their whole building. Escape hatch exists (`globalThis.__pryzmLevelScoped3DCulling=false`) but no UI + no LOD. Ties [[render-tiers-massing-and-presentation]] (A.24 Massing tier) + [[webgpu-heavy-scene-crash-and-instancing]] + L-139/L-140. | Rendering / heavy-scene level scoping — missing massing LOD (C04) | FIXED (pending push) | routed → rendering lane (FENCED to packages/core-app-model/src/rendering/** [LevelScoped3DCullingService + a new massing-LOD service] + apps/editor/src/engine/initScene.ts wiring + packages/renderer-three massing geometry; DISJOINT from Agent D auto-dimension + Agent E initUI/ui-chrome; SEQUENCE-note: same subsystem as L-140 gen-building shadows — coordinate/bundle). FIX (complete the L-139 design, architecturally sound per A.24): out-of-scope levels must render as a lightweight MASSING LOD (simplified per-level extruded shell / instanced blocks — reuse the Massing tier / InstancedElementRenderer, NOT full BIM geometry) so the FULL building silhouette is always visible, with full-detail BIM only near the active level. PLUS a user-facing control (view option) to toggle full-detail vs level-scoped (not just a console flag), and a sensible default (massing-LOD ON so the tower always shows). Keep the device-loss safety (never load 40 levels of full geometry on WebGPU). Maps C04 + A.24 + [[render-tiers-massing-and-presentation]]; no shortcuts; P8. §FIX-HEAVY-SCENE-MASSING-LOD.   ✅ FIXED 6fa17250 (pending push) — new LevelMassingRenderer (core-app-model/rendering) renders each out-of-scope level as ONE instanced block sized to its world-AABB, reusing InstanceGroup → all far levels collapse to 1 draw call/1 PSO regardless of storey count (WebGPU analogue of A.24 Massing tier). Device-loss safety preserved: far full geometry stays visible=false (never submitted), only its CPU bbox is read; massing mesh castShadow=false. Three-way mode on the culling service — massing (NEW DEFAULT, whole tower shown) / scoped (old L-139 hide-only) / all (confirm-gated heavy escape hatch); legacy __pryzmLevelScoped3DCulling===false ⇔ all. User control = a '3D detail' select mounted from initScene (mountLevelScoped3DViewControl, NOT in Agent E's ui zone). setMode/getMode P8-spanned. Draw calls: 3 full levels + 1 instanced (≤512 inst) for the other 37 → full silhouette at negligible GPU cost. tsc exit 0; 11/11 new + 110/110 core-app-model rendering tests. ARCH NOTE: placed in core-app-model/rendering not renderer-three (renderer-three is a LOWER layer, can't import InstanceGroup; P2 satisfied via the @pryzm/renderer-three/three facade; fence permitted it). DEFERRED: L-140 gen-building shadows (same shadow path; massing mesh castShadow=false so no interaction) — sequenced follow-up. NOTE: core-app-model .test.ts rendering suites aren't picked up by the plain package test script (root config globs .spec.ts under src/ui) — test-wiring gap to fix. |
| L-151 | founder | **[UX] Default elevation MARKERS sit too close to the origin — double the distance** — founder: the four N/E/S/W elevation marks on the Ground Floor plan are too close to origin (inside larger footprints). | Views / default elevation-mark placement (C06, L-116) | FIXED | Doubled the documented `ELEV_MARK_RADIUS_M` constant 6→12 m in DefaultViewsManager.ts (§FEAT-ELEVATION-MARKERS L-116). Follow-up (queued): bounds-relative placement so marks clamp OUTSIDE the building footprint regardless of size (a fixed radius still won't scale to very large plans). Maps C06; no shortcuts (tuning a named documented param). §FIX-ELEV-MARK-RADIUS-DOUBLE. |   ✅ FIXED f619ea62 (pending push) — 6→12 m. |
| L-152 | founder | **[HIGH — wall creation] Creating the LAST wall in a sequence re-mutates EARLIER, already-correct wall JOINTS** — founder screenshots (before/after): drawing a run of walls, the first joints mitre correctly; but when the final wall is created, previously-correct corners (top corners of Rooms 00-002/003) CHANGE — walls overrun/overlap where they were clean. ROOT (systemic): each `wall.create` (log: repeated CREATE_WALL → REDETECT_ROOMS) triggers a WHOLE-LEVEL junction re-resolve (WallRebuildCoordinator → JunctionResolverV2) that re-miters ALL joints — so adding wall N re-computes (and shifts) the mitres of walls 1..N-1 that were already correct. The resolve is NOT idempotent/stable: a topology change from the new wall perturbs unrelated settled junctions. RELATED to L-146 (Agent B froze the near-3rd-wall-at-L-corner case) but BROADER: the general 'each new wall re-miters the whole level' instability. | Wall junction resolver — non-idempotent whole-level re-resolve (ADR-0055 JunctionResolverV2, WallRebuildCoordinator) | FIXED (invariant already held; locked by tests) | routed → wall-join agent (RESUME Agent B — JunctionResolverV2 context; FENCED to packages/geometry-wall + WallRebuildCoordinator + WallPlanToolHandler; DISJOINT from D/E/rendering agents; NEVER git stash). FIX: the junction resolve must be STABLE/IDEMPOTENT — adding a wall may only change the junctions the NEW wall participates in; all other already-resolved joints must remain byte-identical (no re-miter of unaffected clusters). Investigate whether the whole-level re-resolve recomputes every cluster from scratch (order/adjacency-sensitive) and scope it to the affected junction set, OR make the mitre solve deterministic+position-stable so a re-resolve reproduces identical geometry for unchanged clusters. Regression guards: L-146 near-corner, exact-Y, mid-T all still sound; a 5-wall sequential draw leaves joints 1..N-1 unchanged after wall N. Ties [[walljoinresolver-multi-cluster-bug]] + [[adr-0055-pascal-wall-pipeline]] + L-146. Maps ADR-0055 + C11; no shortcuts; P8. §FIX-WALL-RESOLVE-IDEMPOTENT-JOINTS.   ✅ FIXED c542e6c7 (pending push) — Agent B (JunctionResolverV2 context) empirically PROVED via throwaway harnesses that the invariant ALREADY HOLDS: pure V2 resolveJunctions is idempotent/order-independent/position-stable (every change under sequential-draw was legitimate participation; non-touched joints byte-identical; a mid-span divider added last perturbs nothing); legacy WallJoinResolver.resolveLevel freezes bit-exact corners (pinned primaryPair + T-into-corner); baseline immutability shipped (§FIX-WALL-JOIN-BASELINE-IMMUTABLE L-44/46/47, re-seed from _sourceBaseLine each pass → no trim drift). The correctness was implicit + untested → LOCKED it with a 7-test regression suite (5-wall sequential joints byte-identical, mid-span divider last, order-independence ×4 permutations, L-146 near-corner, legacy resolveLevel corner-freeze ×3). 191/191 geometry-wall tests, tsc exit 0. RESIDUAL RISK (routed as follow-up, NOT patched blind): (1) DRIFTED corners (welded/generated shells, arms 20-285mm apart) — L-146's 1mm tight-corner detection won't see them as a corner; widening collides with §RESI-L0 loose-band close → needs dedicated pass; (2) legacy MiterPrism for opening/layer walls — if the founder's overrun persists post-L-146, needs the EXACT repro geometry to target §MULTI-CLUSTER safely. |
| L-153 | founder | ✅ **FIXED `916f65b7`→main** (bundled with L-203; ⚠ live-browser confirm) — ROOT (asymmetry confirmed): WebGPU→WebGL builds NO TSL pipeline (`newIsRealWebGPU=false` → RPM lightweight no-op) so it cannot fail; WebGL→WebGPU must re-acquire a GPU device AND build the full TSL pipeline (`recoverPipelineOrBind` → `recoverPipeline`, initScene.ts:3891), which re-throws non-shader failures (RenderPipelineManager.ts:1850) → the swap rolls back and returns `false` → `RendererBackendToggle._reloadInto` fired on that `false` (:139/143) → `location.reload()` → boots to `#/projects`. So the "crash" was the toggle's **reload fallback**, not a dead viewport. FIX: reload ONLY when no live-swap entry point exists; a failed/rolled-back swap now stays on the live renderer with a non-destructive notice (new pure `shouldFallBackToReload`); initScene snapshots + restores the persisted preference on rollback so it can't desync from the live backend. Tests: toggle reload-decision (2) + backend-preference override (3) + RPM device-boundary guard (5). ORIGINAL: **[HIGH — rendering] Live backend swap WebGL→WebGPU CRASHES (falls back to the project page); WebGPU→WebGL is fine** — founder: switching to WebGL works, but switching WebGL→WebGPU crashes the editor back to the project hub. Log (post-crash reboot) shows it then boots WebGPU cleanly (`createRenderer resolvedPreference=webgpu … explicitly persisted by the backend toggle`) — so the CRASH is during the LIVE swap, not on boot; the toggle persisted `webgpu`, and the reload succeeded. ASYMMETRY ROOT (hypothesis, code-confirm needed): swapping TO WebGPU must (re)acquire the GPU adapter/device AND initialise the TSL/PSO pipeline (recall the earlier `[ScenePass] TSL module not loaded. Call initTSL() before createScenePass()` during an SSGI rebuild) — the WebGL→WebGPU path likely calls into the TSL/post-FX pipeline before `initTSL()`/device-ready, throws, and the ViewportCrashGuard escalates to a full remount (→ project page). The reverse (WebGPU→WebGL) needs NO TSL init so it's safe. Ties L-141 (§FEAT-SWAP-LOADING-OVERLAY — overlay covers the swap visually but does NOT fix the crash) + ADR-0077 §RENDERER-LIVE-SWAP (swapRendererBackend) + [[webgpu-heavy-scene-crash-and-instancing]] + [[null-at-mount-runtime-event-race]]. | Rendering / renderer live-swap WebGL→WebGPU (ADR-0077, C04) | OPEN (SEQUENCED — shares initScene/createRenderer with the live L-150 rendering agent; dispatch AFTER it lands) | routed → rendering lane, SEQUENCED after L-150 agent (a149261b) — both edit initScene.ts swapRendererBackend + createRenderer.ts; cannot parallelize. FIX: make the WebGL→WebGPU swap await device/adapter re-acquisition AND `initTSL()`/pipeline readiness BEFORE any TSL/ScenePass/post-FX call (order the swap: create WebGPU renderer → initTSL → rebuild pipeline → only then resume render); on ANY failure during swap, ROLL BACK to the previous working backend instead of letting ViewportCrashGuard remount the app (never lose the project). Reuse L-141's overlay + finally-guaranteed hide during the (now-safe) swap. Add a swap smoke path both directions. Maps ADR-0077 §RENDERER-LIVE-SWAP + C04 + P3 (single rAF) + [[webgpu-heavy-scene-crash-and-instancing]]; no shortcuts; P8. §FIX-SWAP-WEBGL-TO-WEBGPU-CRASH. |
| L-154 | founder (advice→UX gap) | **[UX] Elevation crop extent is editable but UNDISCOVERABLE — navigate-vs-edit collide on the mark; you can only ever open the view** — founder: 'no matter what I do I only trigger opening the elevation when selecting the marker.' TRUTH (code-confirmed): the plan elevation-mark DOES have crop drag handles (PlanViewInteraction hitTestScopeHandle, handles: depth/width-left/width-right/cut-plane) AND numeric crop in ViewPropertiesPanel (Crop section + View Range depth). BUT the handles have only a ~10px grab tolerance and aren't visually prominent, and a body click hits §FIX-MARK-NAVIGATE (open linked view) — so in practice every interaction navigates. Not a loop/crash (the log spam is repeated body-clicks). | Views / elevation-mark crop-edit UX (C06, DOC-2.x) | **FIXED (d84632e9, live)** — §FIX-ELEV-MARK-CROP-DISCOVERABLE. **CORRECTION to the framing below:** navigate-vs-edit did NOT collide — L-157 had already split them (single-click selects, double-click navigates, `_onDblClick`). The residual defect was purely DISCOVERABILITY. Handles were hit-tested BEFORE the body (correct) but had a ~10px grab tolerance, were drawn as plain 8x8 white squares only while selected, and set a resize cursor only DURING a drag — never on hover. FIX (kept L-157's rule; added a discoverability layer on top, mirroring the linear-dimension idiom of §FIX-DIMENSION-FIRST-CLASS-SELECTABLE-L173): 12px grab squares enlarging to 16px + PRYZM purple `#6600ff` on hover, thickened cut line on hover, a "Drag handles to crop · double-click to open" hint, matching ns/ew/move resize cursor on hover, grab tolerance 10 -> 14px (`SCOPE_HANDLE_GRAB_PX`), hover cleared on detach. `view.setCrop` dispatch path unchanged (P6). Gate: 10 new tests (hit priority, navigate-only-on-double-click, handle drag dispatches `view.setCrop` with no direct `annotationStore.update`) + 7/7 existing cropHandleDrag; root tsc exit 0. Live-canvas confirmation still advised for the two purely visual behaviours (hover cursor + purple paint) headless cannot drive. | _original triage:_ route → views/UX agent when reached: make crop handles DISCOVERABLE + grabbable — reveal visible crop handles on hover/selection, enlarge hit area, and separate SELECT (single-click selects the mark + shows crop handles) from NAVIGATE (double-click, or a dedicated 'open' affordance) so editing the crop no longer forces navigation. Reuse existing _scopeDrag handles + view.setCrop/SetViewCropCommand + ViewPropertiesPanel Crop/View-Range (no new crop model). Maps C06 + DOC view contracts + [[elevation-marker...]]; no shortcuts; P8. §FIX-ELEV-CROP-HANDLE-DISCOVERABILITY. |
| L-155 | agent-flagged (Agent D) | **[MED — AutoDimension polish] Auto-dim lines sit ~8-24 mm off the wall (near-overlapping) instead of visibly OUTSIDE the footprint** — the executor dimensionStringsToLinearDimAnnotations converts the engine's sheet-mm `offsetMm` to world metres naively (/1000), so at building scale the signed per-side offset collapses to millimetres → dim lines hug/overlap the wall rather than standing off outside. Separate from the L-147 diagonal/completeness fix. | Documentation / AutoDimension offset-scale (C56 §Stage-6, SPEC) | FIXED (pending push) | route → auto-dim agent: the offset must be a WORLD-space standoff scaled to the drawing (paper-mm × view scale, or a sensible world standoff outside the footprint bbox), not sheet-mm/1000. Keep deterministic; map C56 Stage-6 placement + SPEC. §FIX-AUTODIM-OFFSET-WORLD-SCALE.   ⬆ FOUNDER-CONFIRMED (2026-07-07): after L-147 the dims are 'much better' (diagonal gone) but still 'not offset outwards from the walls' — confirms the /1000 offset-scale collapse. Priority ↑ (bundle with L-161 usability).   ✅ FIXED be5d2fb7 — ROOT: engine serialize() emitted offsetMm as sheet-PAPER mm (8/16/24), executor /1000 → 8-24mm world → hugging. geometry2D.offset is consumed as WORLD metres everywhere. FIX: serialize() emits the WORLD standoff reusing Stage-7's own world scale → 0.5/1.0/1.5/2.0 m tiers (row-0 = 0.5m beyond outermost wall; overall furthest, location nearest, no chain overlap); signed side preserved; unifies emitted/rendered offset with the Stage-7 crossing check (fixed a latent inconsistency). Deterministic. L-145 sink + span + L-147 measurementNormal preserved. 30/30 auto-dim + 6/6 executor; tsc exit 0. |
| L-156 | founder | **[HIGH — resi generator] Residential-building typology: façade/perimeter walls are created ONLY on the GROUND floor; upper floors get slabs + columns but NO exterior walls** — founder screenshot: multi-storey resi building, ground floor has full brick façade + windows, floors above show only floor plates + columns, no perimeter/façade walls. Generator (apps/editor/src/ui/residential-building/ResidentialBuildingExecutor.ts, groundFloorPlacement.ts, residentialFromBoundary.ts) evidently emits the shell/façade wall ring for level 0 only and doesn't replicate it per storey. Ties [[resi-building-generator-and-corner-cell-blocker]] + [[residential-building-floorplate-defects]] + [[reuse-residential-house-pipeline-patterns]]. | Generative / residential-building executor (typology pack) | RESOLVED (misdiagnosis — was L-164) | routed → resi-building generator agent (FENCED to apps/editor/src/ui/residential-building/** + any shared shell-wall helper it calls; DISJOINT from UI/views/wall lanes). Root-cause why the perimeter/façade wall ring is emitted for the ground level only; emit the façade wall ring for EVERY storey (per-level loop over the shell footprint, correct level stamping + heights), mirroring the proven house/resi upper-floor pipeline (§HOUSE-SHELL-CONTAIN-ALL-FLOORS analogue). Verify walls stamp the right levelId per floor + join per level. Maps C11 + the typology pack + [[reuse-residential-house-pipeline-patterns]]; no shortcuts; P8. §FIX-RESI-FACADE-ALL-FLOORS.   ✅ RESOLVED via L-164 + regression test 7d88c19c (Agent C) — the L-156 hypothesis was FALSE: the ResidentialBuildingExecutor ALREADY emits the façade/perimeter ring on EVERY storey (verified by driving the real executor headlessly — ground 12 walls + each upper 44 walls wrapping all 4 edges, correct cascaded levelId/base/height; joins per-level via WallRebuildCoordinator levelId filter). The founder-visible 'no upper-floor façade' was the SAME L-164 massing-LOD collapsing upper (out-of-scope) floors to a grey block on a modest building → fixed by L-164 (a1883cf1). No generation change warranted; added regression test residentialFacadeAllFloors.test.ts (3/3) locking per-storey emission. groundFloorPlacement.ts was a red herring (entrance-door resolver only). |
| L-157 | founder | **[HIGH — REGRESSION] Elevation CROP edit broke — 'yesterday I could drag the crop rectangle X/Y in plan and the elevation updated live; now no matter what I select it won't enter edit mode'** — founder confirms it worked yesterday and suspects a change since. Crop edit = grabbing the plan scope handles (PlanViewInteraction.hitTestScopeHandle → PlanViewAnnotationRenderer, handles depth/width-left/width-right/cut-plane). The crop-handle CODE was NOT changed today (last touch: cb6c39d6 L-129, 15425570 L-119/123/124). SUSPECT REGRESSION from today's deploys: (a) **L-149** (5baf97a0) moved launchers to position:fixed on document.body at z:10000 — if the fixed launcher column/layer intercepts pointer events over the plan/split canvas, hitTestScopeHandle never receives pointerdown (this ALSO caused L-159 split-view-button overlap → strong signal); (b) **L-151** (f619ea62) moved the default elevation MARK 6→12 m — the scope rectangle+handles are anchored to the mark (_scopeDepthHandleScreenPoint uses mark position), so they shifted 6 m further out and may sit off where the user clicks. | Views / elevation crop-handle interaction — regression (C06, DOC-2.x) | FIXED (pending batch push) | ROOT-CAUSE CONFIRMED (orchestrator, code-read): the crop ADJUST ARROWS + drag handles render ONLY for a SELECTED elevation mark (PlanViewAnnotationRenderer._renderSelectedScopeOverlay, gated on _getSelectedAnnotationId → __pryzmSelectedAnnotationId). Clicking the mark (PlanViewInteraction _onMouseDown ~line 604) DOES set __pryzmSelectedAnnotationId, BUT lines 615-616 §FIX-MARK-NAVIGATE then IMMEDIATELY navigate to the elevation view → the plan deactivates → SelectionManager.unselectAll() clears __pryzmSelectedAnnotationId (SelectionManager:2242) → back on the plan the mark is NOT selected → _renderActiveCut draws only the dashed 'Active cut' rect with NO arrows (founder's screenshot). So 'navigate-on-single-click' defeats select-to-adjust. FIX (sound, merges L-154): single-click SELECTS the mark (arrows appear, crop adjustable via existing _scopeDrag handles); DOUBLE-click NAVIGATES (opens the elevation) — preserves §FIX-MARK-NAVIGATE (L-129/G8) intent without stealing selection. Add dblclick detection to PlanViewInteraction. §FIX-ELEV-CROP-EDIT-REGRESSION.   ✅ FIXED 9964b52b (Agent B) — split SELECT from NAVIGATE: single-click now ONLY selects the mark (continuous plan render draws the arrows/handles); navigation moved to a new dblclick handler (_onDblClick), preserving §FIX-MARK-NAVIGATE (L-129/G8). routed → UI/views agent (resume Agent E — z-layer context). (ORIGINAL two suspects, now resolved to the navigate-steals-selection root:) (a) does the position:fixed launcher column (or any L-149 z-layer change) capture pointer events over the plan/split canvas? test pointer-events + stacking over the plan region; (b) did L-151's mark move shift the crop handles out of reach? Fix whichever regressed (restore edit-on-drag). If launchers are clean, hand L-157 back for the L-151/DefaultViewsManager angle. Maps C06 + DOC + L-149/L-151; no shortcuts; P8. §FIX-ELEV-CROP-EDIT-REGRESSION. |
| L-158 | founder | **[MED — views] Default elevation MARKERS appear only on the GROUND floor plan — should be present on ALL floor plans** — founder: the four N/E/S/W default elevation marks show on Ground Floor only; every level's plan view should show them. DefaultViewsManager._ensureElevationMark currently creates the marks owned by the ground plan view (vd-sys-plan-l0) only. | Views / default elevation-mark per-level presence (C06, L-116) | FIXED (pending batch push) | routed → views agent (FENCED to packages/core-app-model/src/views/DefaultViewsManager.ts + the elevation-mark placement; DISJOINT). Emit the elevation-mark annotations for EVERY level's plan view (or make the renderer draw the shared marks on all plan views), so each floor plan shows the N/E/S/W marks. Reuse the existing elevation-mark annotation + createId('annotation'); keep it idempotent (no duplicates on reload). Coordinate with L-151 (same file). Maps C06 + L-116; no shortcuts; P8. §FIX-ELEV-MARKS-ALL-FLOOR-PLANS.   ✅ FIXED 7ab93fae (Agent D, pending batch push) — ROOT: _ensureElevationMark hard-coded ownerViewId=DEFAULT_PLAN_VIEW_ID + marks emitted only against the ground plan; the renderer filters by ownerViewId so no other plan saw them. FIX (DefaultViewsManager only): emit the 4 N/E/S/W marks on EVERY plan view (getByType('plan')), one owned set per view; a vd:view-created listener gives later per-level plan views their marks automatically; Ground Floor keeps stable L-116 fixed ids, other plans mint annotation_<ULID> (inlined Crockford-ULID, no new cross-layer dep); idempotent (owner+linkedViewId scan, no dup on reload); elevation-delete now clears marks from ALL plans. L-151 12m radius preserved. 9/9 tests (also fixed a pre-existing silently-failing assertion that checked *6 vs the shipped *12). tsc exit 0. Did NOT touch PlanViewAnnotationRenderer (per-view emission approach). |
| L-159 | founder | **[HIGH — REGRESSION from L-149] The 'Living Graph' + 'Graph' launcher pills now COVER the 'Split View' button — move them to the bottom** — founder screenshot: the launcher column (Living Graph / Graph / Plan+Site / 3D Site-Globe) overlaps the split-view toggle. DIRECT regression from L-149 (5baf97a0): the new collision-free position:fixed bottom-left launcher COLUMN (launcherRailStyle slots) now sits over the split-view button that lives in that corner. The de-overlap fix created a new overlap with a non-launcher control. | UI shell / launcher rail vs split-view button (C06 §7) | FIXED (pending push) | routed → UI agent (resume Agent E). Re-anchor the launcher column so it does NOT occlude the split-view toggle (register the split-view button in the C06 §7 layer/slot system so it's overlap-aware, OR shift the launcher column clear of it). Per C06 §7 no-overlap policy — chrome must be aware of peers; this is exactly the class the contract governs, so fold the split-view button into the slot accounting. Maps C06 §7; no shortcuts; P8. §FIX-LAUNCHER-COVERS-SPLITVIEW.   ✅ FIXED ce0cf451 (salvaged from Agent E, which session-limit-terminated at self-gate) — the Split View toggle is folded into the launcher-rail SLOT system (C06 §7.2): splitView=slot 0 (bottom, above the GPU toggle), launchers re-slotted 1-4, all via launcherRailStyle() — no hand-picked bottom/z-index, so a re-slotted pill can never re-occlude it. Root: L-149 re-slotting landed 'Graph' on bottom:142, right on the button's old hand-picked bottom:144. Root tsc exit 0. |
| L-160 | founder | **[HIGH — GIS/Cesium, re-review] The Cesium fixes are NOT resolved end-to-end: (a) 3D GLOBE still doesn't frame to the building in context on open (still must click 'Zoom to Site'); building sometimes ABOVE ground / mispositioned; (b) the 'Real' building on 3D Site is STILL a transparent PLACEHOLDER CUBE, not real geometry; (c) Sun analysis is quicker but still needs more perf** — founder re-flag after L-142/L-143/L-144. (a) L-142 (3c0a6e63 CSP ion-terrain + placeBuildingOnGlobe settle-reframe) did NOT achieve initial-open framing == Zoom-to-Site in practice, and building placement vs ground is still wrong sometimes (base-height/clamp race persists). (b) L-144 (real-geometry façade) was DEFERRED behind the solar worker → the Forma/site 'Real' surface is still the 4-face massing prism cube (screenshot: green/teal transparent cube + orange stripe, 'Façade analysis: ON'), NOT the actual walls-with-openings. (c) L-143 (BVH + no-recompute, 3fe8848a) helped (quicker) but founder wants more. On webgl-fallback. | GIS / Cesium globe framing + Forma real-geometry + solar perf (C18-C23, C21, ADR-0074/0079) | PARTIALLY RESOLVED via REVERT (globe framing) — L-144 real-geo + perf still open | routed → Cesium/Forma agent (CesiumViewport now FREE; FENCED to apps/editor/src/ui/geospatial/CesiumViewport.ts + GISAreaLayout.ts + apps/editor/src/ui/climate/** + @pryzm/solar-analysis + FormaSiteAnalysisControls + siteRealData; DISJOINT from E/resi/views lanes). SCOPE: (a) RE-FIX globe initial-open framing — the open must deterministically await the terrain height-clamp + building re-placement (§FORMA-FULL-HEIGHT base) and THEN fly the SAME frame as Zoom-to-Site; fix the above-ground/mispositioned placement (clamp base height correctly before framing). (b) PROMOTE L-144 — run façade/site analysis on the REAL placed building geometry (real wall meshes/GLB with openings), not the massing prism; land the deferred solar WORKER (#2, ADR-0110) so the heavier real geometry stays fast; keep massing as fast-preview only. (c) further sun-hours perf (worker + adaptive #4). Maps C18-C23 + C21 + ADR-0074/0079 + ADR-0110 + [[render-tiers-massing-and-presentation]] + [[geospatial-foundation-strategic-direction]]; no shortcuts; P8. §FIX-CESIUM-GLOBE-REAL-GEOMETRY-PERF. |
| L-161 | founder | **[HIGH — AutoDimension usability] Auto-generated (and manual) linear DIMENSIONS are not SELECTABLE / EDITABLE in plan** — founder: dims are 'much better' (L-147 diagonal fixed) but 'not selectable / editable'. The 'linear-dim' AnnotationElements render (L-145 sink) but can't be picked/moved/value-overridden/deleted in plan view. Likely root: PlanViewInteraction.hitTestAnnotation only drags DRAGGABLE_ANNOTATION_TYPES — if 'linear-dim' isn't in that set (or there's no edit/override affordance + delete), the dims are inert. SHARES ROOT with L-157 (plan-view annotation hit-testing / scope-handle) — coordinate. | Documentation / dimension selection+edit in plan (C03, DOC-2.x, C56) — REGRESSION | FIXED (pending batch push) | routed → AutoDim/annotation-usability agent, AFTER Agent E's L-157 verdict (both touch PlanViewInteraction). Make linear-dim (+ the auto set) SELECTABLE + EDITABLE in plan: hit-test/select the dim, drag to move the whole string + its offset, edit the value (override text) + delete — reuse the manual dimension edit path (LinearDimPlanToolHandler / AnnotationManager / annotation.update). Add 'linear-dim' to the draggable/selectable annotation types if missing; wire an edit affordance (double-click or contextual bar) + P6 annotation.update/delete commands. BUNDLE with L-155 (offset-outward) so the dims land outside the wall AND are editable. Maps C03 + C56 + DOC-2.x annotation edit; no shortcuts; P8. §FIX-AUTODIM-DIMS-SELECTABLE-EDITABLE.   ⬆ FOUNDER ESCALATION (2026-07-07): NOT just the batch/auto — the MANUAL linear dimension is ALSO not selectable/editable now, and it 'WAS IN THE PAST' → REGRESSION. Prime suspect: TODAY's L-145 render-sink rewire (e4cbc1d2/ADR-0119) moved BOTH the auto executor AND the manual LinearDimPlanToolHandler to write 'linear-dim' AnnotationElements via CreateAnnotationCommand → subsystem annotationStore. The OLD manual path (before today) evidently produced dims that WERE selectable/editable (+ likely undoable) — the new 'linear-dim' annotation path renders but is NOT wired for selection/edit (probably 'linear-dim' missing from PlanViewInteraction DRAGGABLE_ANNOTATION_TYPES / no select→edit overlay for linear-dim, unlike section/elevation marks which have _renderSelectedScopeOverlay). REVIEW IN DETAIL what the old manual dim path wired that the new one dropped; wire linear-dim selection (hitTestAnnotation), drag-move, value/override edit, delete — for BOTH manual + auto. |
| L-162 | founder | **[CRITICAL — contract violation] AutoDimension UNDO does NOT work — the dimensions REMAIN after undo (undo removes prior elements but never the dims)** — founder: 'IT WILL UNDO ELEMENTS DONE BEFORE BUT THE DIMENSIONS WILL REMAIN - ABSOLUTELY AGAINST THE CONTRACT.' Violates C11 / C24.1 §1.2 (one undoable unit; every mutation is undoable). The L-145 fix (e4cbc1d2) dispatches ONE CreateManyAnnotationsCommand through the legacy CommandManager inside batchCoordinator.runBatch — but undo doesn't pop the created annotations. Likely: CreateManyAnnotationsCommand isn't registered on the CommandManager UNDO stack (or its undo()/inverse isn't implemented, or runBatch dispatches it outside the tracked undo ring), so the auto-dim set is created-but-not-undoable. SHARES the render-sink lineage with L-161 (both from the ADR-0119 subsystem-store path). | Documentation / AutoDimension undo (C11, C24.1, ADR-0119) | FIXED (pending batch push) | routed → AutoDim/annotation agent (bundle L-155 + L-161 + L-162 — all the ADR-0119 dim path). Make the auto-dim set ONE undoable unit: verify CreateManyAnnotationsCommand pushes onto the CommandManager undo stack with a working undo (remove all created annotations) + redo; ensure batchCoordinator.runBatch doesn't bypass the undo ring; if the subsystem annotationStore mutation isn't undo-tracked, wire it (mirror how manual annotation create/delete undo works). Add a test: create auto-dims → undo → all dims gone → redo → back. Maps C11 + C24.1 + ADR-0119; no shortcuts; P8. §FIX-AUTODIM-UNDO-ONE-UNIT.   🔎 ORCHESTRATOR FINDING (code-read): CreateManyAnnotationsCommand ALREADY implements a correct undo() (tracks _added, store.remove() in reverse) — so the bug is NOT a missing undo method. Root is that the command is dispatched via CommandManager.execute() INSIDE batchCoordinator.runBatch and is NOT being recorded on the CommandManager UNDO HISTORY stack (so Ctrl-Z never reaches it). FIX target: ensure the auto-dim CreateManyAnnotationsCommand is pushed onto the undo ring (verify runBatch/CommandManager.execute records history for this command; compare with how manual CreateAnnotationCommand gets onto the stack). Bundle L-155/L-161/L-162. |
| L-163 | founder | **[HIGH — stair] L-stair LANDING is wrong: the direction of the SECOND RUN toward the landing is mis-fitted, and the LANDING RAILING is mapped incorrectly** — founder screenshot: an L-shaped switchback stair (2 flights + landing); the landing + its railings are malformed — the second run's direction relative to the landing looks wrong, and the landing railing segments wrap at odd angles / don't follow the landing edge. Log: CreateStairCommand points=3 shape=L, 2 flights; CreateStairRailingCommand left+right each '2 flight(s)' — the railing builder maps flights but the LANDING segment (the turn) is not correctly oriented. Founder: 'not sure how it is fitting the direction of the second run towards the landing and therefore how it is mapping the creation of the landing railing.' | Stair / landing geometry + landing railing mapping (geometry-stair, StairMeshBuilder, StairRailingBuilder) | FIXED (pending batch push) | routed → stair agent (FENCED to packages/geometry-stair/** + StairMeshBuilder + StairRailingBuilder + the stair path→flight/landing solver; DISJOINT). Root-cause how the second run's direction is derived at the landing (the turn geometry) and how the landing railing polyline is built from the flights+landing; fix the landing orientation + railing mapping so the landing railing follows the landing perimeter continuously between flights. Deterministic; maps the stair contract + C11; no shortcuts; P8. §FIX-STAIR-LANDING-DIRECTION-RAILING.   ✅ FIXED 92ea3f4e (Agent F, pending batch push) — ROOT: StairRailingBuilder.buildLandingSegment() discriminated U-switchback vs L-corner by the PRESENCE of nextFlight.startOverride, but §STAIR-PREVIEW-MATCH (2026-04) made the 2D path adapter corner-PIN every non-first flight → L-shapes now also carry startOverride → every 2D-drawn L landing was wrongly routed into buildULandingGuard (the half-turn guard) → railing wrapped at odd angles. (Mesh builder correctly keyed on flight-to-flight ANGLE, so mesh+railing disagreed.) Second defect: nextFlightStart used the legacy auto-advance projection, mis-fitting flight 2 vs the corner-pinned landing. FIX: (1) discriminator now uses the SAME angle test as StairMeshBuilder (flatDir·nextFlatDir < -0.7 = U); 90° L falls through to the L-landing wrap. (2) corner-pinned flight-2 rail start read from resolveFlightPositions() (same source flight-2 rails use); legacy path keeps auto-advance. Both legacy StairCreationController paths unchanged. 7 files/64 geometry-stair tests + new StairLLandingRailing.spec; tsc exit 0. Visual glance on a real L-stair worth doing. |
| L-164 | founder | **[HIGH — REGRESSION from L-150, TODAY] Opening an existing batch-created RESIDENTIAL building shows a weird grey 'envelope shade' around the building (not just the top floor); founder suspects shadow** — founder: 'really recent, check all git commits.' ROOT (orchestrator, confident): it is TODAY's L-150 §FIX-HEAVY-SCENE-MASSING-LOD (6fa17250) — LevelScoped3DCullingService now defaults to mode='massing' (log: 'Active (mode=massing)'), engaging at >500 elems OR >5 levels. A ~6-storey residential building trips the threshold: the active (ground) level renders full-detail and ALL out-of-scope levels collapse into ONE grey instanced AABB block (LevelMassingRenderer) → reads as a translucent grey ENVELOPE/shell around the upper floors ('doesn't cover only the top floor'). NOT a shadow (massing mesh is castShadow=false) — it's the massing LOD proxy itself, visible because the threshold is far too aggressive for a normal building the GPU can render in full. Only the genuinely huge 40-storey tower (L-150's target) should get massing LOD. | Rendering / heavy-scene massing-LOD threshold + default (C04, A.24, L-150) | FIXED (pending batch push) | routed → rendering lane (FENCED to packages/core-app-model/src/rendering/LevelScoped3DCullingService.ts + LevelMassingRenderer.ts + initScene mount; re-dispatch when session-limit resets). FIX: (1) RAISE the massing-LOD engagement threshold dramatically — full BIM detail for anything a normal GPU handles (e.g. only engage massing above ~12-15 levels AND a high element count, not >5 levels / >500 elems); a 6-storey building must render ALL floors full-detail, no massing block. (2) Consider default mode='all' (full detail) with massing as an opt-in/auto-escalation ONLY on device-loss-risk-scale models; keep the tower (L-150) protected via the high threshold or auto-escalation. (3) Ensure massing blocks never overlay/shade VISIBLE real geometry. IMMEDIATE WORKAROUND for founder: console `globalThis.__pryzmLevelScoped3DMode='all'` (safe on a modest building — the device-loss risk was only the 40-storey case). Maps C04 + A.24 + [[render-tiers-massing-and-presentation]] + L-150/L-139; no shortcuts; P8. §FIX-MASSING-LOD-THRESHOLD-TOO-AGGRESSIVE.   ✅ FIXED a1883cf1 (Agent A, pending batch push) — DEFAULT_MODE now 'all' (full detail is the resting state); massing is an AUTO-ESCALATION gated on isHeavyModel = (levels≥15 AND elements≥1000) OR (elements≥4000). Level count is the primary discriminator (6-storey vs 40-storey — element count alone can't separate a furnished low-rise from the office, which is why L-150's >500 gate regressed). __pryzmLevelScoped3DMode override + __pryzmLevelScoped3DCulling===false back-compat kept. LevelMassingRenderer now never builds a block for a fully-visible level (never shades real geometry). house/apartment/6-storey → full detail; 40-storey tower → massing (L-139/L-150 device-loss safety preserved). 13/13 tests (incl 3 L-164 regression guards); tsc exit 0. NOTE: core-app-model .test.ts suites aren't matched by root vitest include (.spec.ts only) — CI test-wiring gap to fix (also flagged by L-150 agent). |
| L-165 | founder | **[LOW — SPIKE, perf-conditional] Shadow projection is too PIXELATED — can it be more accurate? (spike first; if it costs too much perf, leave as-is)** — founder screenshot: the ground shadow-catcher shadow edge is stair-stepped/aliased. ROOT: low shadow-map resolution — log 'ShadowQualityUpgrader Applied "standard" — map: 512px radius:1'. A 512px shadow map over a building-scale scene → coarse texels → pixelated penumbra. Founder EXPLICITLY wants a SPIKE weighing accuracy vs perf, and is fine leaving it if too costly. | Rendering / shadow quality (C04, C10 perf, ShadowQualityUpgrader / PascalSceneLighting) | OPEN (SPIKE — low priority, perf-gated) | routed → rendering-spike agent (when capacity). DELIVER a spike doc under docs/03-execution/spikes/ evaluating: (a) shadow-map RESOLUTION bump (512→1024/2048) cost/quality; (b) CSM (cascaded shadow maps) for large scenes so near geometry gets high-res texels without a huge single map; (c) PCF/PCSS soft-shadow filtering for smoother penumbra; (d) tie into the existing SceneQualityTier so it scales DOWN on heavy scenes (never regress the 40-storey device-loss budget — L-139/L-150). Recommend a tier-aware default (e.g. 1024 + PCF on modest scenes, 512 on heavy) ONLY if the spike shows acceptable cost; otherwise document 'leave as-is' with rationale. Reuse ShadowQualityUpgrader tiers — no new shadow system. Maps C04 + C10 + [[render-tiers-massing-and-presentation]] + [[webgpu-heavy-scene-crash-and-instancing]]; no shortcuts; P8 if new exported fns. §SPIKE-SHADOW-MAP-ACCURACY.   ✅ SPIKE DELIVERED + SAFE FIX SHIPPED fa948ff4 (pushed→main) — ROOT: one caster (Pascal key light, ADR-0106); every generated building (≥1200 meshes) capped at 'performance' tier (ADR-0094) → 'standard' 512px map whose frustum (RealSunService.refitShadowToScene) stretches to ±80m → ~0.2-0.3 m/texel stair-stepping. FIX (implemented, trivially-safe subset): bump QUALITY_CONFIGS.standard 512→1024px + radius 1→2; leave high(2048)/ultra(4096). REJECTED CSM (multiplies caster passes 3-4×, breaks single-caster suppression lever, new realloc surface) + PCSS (unbudgeted TSL node, doesn't fix stair-step). DEVICE-LOSS SAFE: static config on the ALREADY-GUARDED tier-transition realloc (freeze→mutate→deferred thaw + post-submit dispose) — no new/mid-submit realloc; >8000 survival tier + 40-storey budget untouched (both DISABLE the map, never resize). +~3MB GPU, depth-fill only. Spike doc docs/03-execution/spikes/SPIKE-SHADOW-MAP-ACCURACY.md; 4/4 tests; tsc 0. |
| L-166 | founder | **[HIGH — UX] TWO competing view-mode bars: the busy Forma toolbar (Views|2D Map|Plan|3D|Zoom to Site|Analysis|Real|Massing) vs the clean segmented control (3D+plan | 3D globe | 3D Site) — keep ONLY the segmented one; the Forma bar covers/overlaps, user can't tell which view is active or swap; clicking 'Views' (left) makes the panel disappear** — founder: clicking '3D Site / Globe' launcher opens a mode but 'it covers both, I don't know which view I'm in, and I can't swap to others.' The segmented '3D + plan | 3D globe | 3D Site' bar (screenshots 2-4) is the desired one and 'works' (triggers on new building/house batch). REMOVE / consolidate the other (Forma toolbar). | UI shell / geospatial view-mode bar consolidation (C06, C12/C18-23 geospatial, C06 §7) | FIXED (pending batch push) | routed → GIS/UI agent (FENCED to the view-mode-bar components — the Forma toolbar + the '3D+plan/3D globe/3D Site' segmented control in apps/editor/src/ui/geospatial + GISAreaLayout + SplitViewManager mount; DISJOINT from rendering/annotation lanes; coordinate with L-149/L-159 C06 §7). Consolidate to ONE view-mode control (the segmented 3D+plan/3D globe/3D Site) that ALWAYS shows the active mode + lets the user switch between plan/3D/globe/site freely; remove or fold the redundant Forma toolbar (preserve its unique actions — Zoom to Site / Analysis / Real / Massing — into the single control or a secondary contextual bar, don't lose them). Fix the 'Views' button hiding the panel. Maps C06 + C12/C18-23 + [[render-tiers-massing-and-presentation]]; no shortcuts; P8. §FIX-VIEWMODE-BAR-CONSOLIDATE.   ✅ FIXED 791f1a54 (view-bar agent, pending batch push) — both bars live in GISAreaLayout.ts: Bar A = segmented result-toggle (3D+plan/3D globe/3D Site, DESIRED) built by showSiteResultView, mounted only by onboarding handoff; Bar B = busy Forma toolbar (Views/2D Map/Plan/3D/Zoom-to-Site/Analysis/Real/Massing) via the launcher pill/GIS rail. ROOT: Bar B hid Bar A (display:none) but the launcher path called mountFormaViewToggle DIRECTLY so Bar A was never mounted → user saw only Bar B, no active-view indicator, and 'Views' → removeFormaViewToggle restored a NULL Bar A → whole panel vanished. FIX: tri-state activeSegment ('2D'|'3D'|'forma') always painted; idempotent mountResultToggleBar/ensureResultToggle so the switch is ALWAYS mounted; mountFormaViewToggle no longer hides Bar A — lights '3D Site' + mounts Forma controls as a SECONDARY sub-bar one row below (top:108 vs 64, no overlap); ALL Forma actions preserved; removed the dead-end 'Views' back button (leave via the always-present switch) → panel can't disappear. View-bar file type-clean (the 5 tsc errors were the shadow agent's uncommitted PascalSceneLighting WIP). Verify on prod after push. |
| L-167 | orchestrator (3 agents flagged) | **[MED — CI/test infra] core-app-model `.test.ts` suites don't run in CI — root vitest config includes `.spec.ts` ONLY** — the L-150, L-158, L-164 agents each independently found that `packages/core-app-model/**/*.test.ts` (the rendering + views suites: LevelScoped3DMassingLod, DefaultViewsManager.elevationMarks, etc.) are NOT matched by the root `vitest.config.ts` include (`.spec.ts` only) and `packages/core-app-model` has no local vitest config, so `pnpm --filter @pryzm/core-app-model test` finds no files. These suites pass when run ad-hoc but are effectively DARK in CI — a real coverage gap (a regression in those files wouldn't be caught by the gate). Evidenced by L-158 finding a pre-existing SILENTLY-FAILING assertion (`*6` vs the shipped `*12` since L-151). | CI / test wiring (C10 observability, GA-gate) | FIXED (pending batch push) | route → CI/test-infra pass: either add `**/*.test.ts` to the root vitest include (or a per-package vitest config for @pryzm/core-app-model) so these suites run in `test:ci`; audit which other packages have dark `.test.ts` files; ensure the GA-gate/CI actually executes them. Maps C10 + the GA-gate; no shortcuts. §FIX-CI-COREAPPMODEL-TEST-WIRING.   ✅ FIXED ef7c52dd (CI agent, pending batch push) — added a per-package packages/core-app-model/vitest.config.ts (happy-dom default) + `test:ci` script so `pnpm -r run test:ci` picks it up (no dep change → pnpm-lock untouched). Was 0 suites in CI → NOW 26 files / 235 tests run + green (incl the previously-dark LevelScoped3DMassingLod + DefaultViewsManager.elevationMarks). No broken assertion remained. FOLLOW-UP (surfaced, queued): 4 MORE packages have dark .test.ts (no local vitest config + no test:ci so `-r` skips them): command-registry (8 suites, WORST — no test script at all), beta-signup, crash-reporter, email-transport. Route command-registry (8 dark suites) as the priority test-wiring follow-up. §FIX-CI-COREAPPMODEL-TEST-WIRING (+ §FOLLOWUP-DARK-TEST-PACKAGES). |
| L-168 | founder | **[HIGH — rendering] After L-164 the massing envelope is gone (good) but the GROUND-FLOOR shadow-catcher shadow ALSO disappeared — building floats with no ground shadow** — founder tested the L-164 fix on the batch resi building: full building renders (envelope gone ✅) BUT the real ground shadow (L-11 §FEAT-REAL-ENVIRONMENT GroundShadowCatcher) is now missing. CAUSAL CHAIN (log-confirmed): L-164 makes ALL floors render full-detail → 3931 meshes → SceneQualityTier drops to tier='performance' (was cinematic when massing hid most levels) → `shadows=standard, decorativeShadows=off` + the shadow-at-scale budget → the building no longer casts onto the ground catcher. This is exactly the L-140 candidate (a) 'shadow-off-at-scale BUDGET (survival tier)' — now reproducibly triggered by rendering the whole building. Ties L-140 (batch-generated building no shadow) + L-164 + [[webgpu-heavy-scene-crash-and-instancing]] + L-11 §FEAT-REAL-ENVIRONMENT. | Rendering / ground shadow-catcher at performance tier (C04, C10) | FIXED (pending push) — real root was shadow FRUSTUM not tier | routed → rendering-shadow lane (bundle with L-140; FENCED to the shadow/scene-tier path — SceneQualityTier, ShadowQualityUpgrader, PascalSceneLighting, GroundShadowCatcher/RealEnvironmentService; DISJOINT). Ensure the KEY-LIGHT ground shadow (building→catcher) survives at performance tier even when DECORATIVE shadows are off — the primary sun shadow onto the ground is not 'decorative', it's the L-11 feature; keep it on (or on a cheaper single-cascade path) while dropping only self-shadow/contact detail; ensure the shadow map/frustum covers the full building footprint. Verify the catcher still receives at 3931-mesh scale without re-triggering device loss (respect L-139/L-150 budget). Maps C04 + C10 + L-11; no shortcuts; P8. §FIX-GROUND-SHADOW-AT-PERF-TIER.   ✅ FIXED f4533641 (shadow agent) — CORRECTED ROOT: shadows are NOT suppressed at 3931 meshes (suppress gate = !shadows || meshCount≥8000; at 3931/performance shadows=true) — the tier-drop (2048→512) was only a correlate. Real cause: the sole ground-shadow caster is the Pascal key light (single-caster, ADR-0106) with a HARDCODED shadow camera (ortho ±50, near 1, far 100) + RealSunService orbits it at ~17 m — after L-164 rendered ALL floors full-detail the building OUTGREW the ±50/far-100 frustum AND the light sat INSIDE the building → no clean shadow on the L0 GroundShadowCatcher → 'building floats'. FIX: RealSunService.setShadowCoverage(center,radius) re-homes the light OUTSIDE the model along the sun ray (dist=radius·3), aims target at centre, sizes the ortho frustum (±radius·1.35 + near/far) to bracket the whole building; RealEnvironmentService.refitShadowToScene() computes the live model AABB (excludes catcher/helpers/hidden massing) + calls it, wired in initScene on a DEBOUNCED (300ms) geometry-add/level-change listener. NEVER touches shadow.mapSize → CANNOT re-trigger the ShadowDepthTexture device-loss cascade (L-139/L-150 safe); 8000-caster survival ceiling untouched. 19/19 tests; tsc exit 0. ALSO addresses L-140 (batch-generated building no shadow) for <8000-mesh models. RESIDUAL: (1) >8000-mesh buildings still lose ground shadow (survival ceiling — deliberate; a coarse single-cascade over a caster LOD is a separate effort); (2) at performance tier the 512px map over the larger frustum is softer/coarser (acceptable); (3) very low sun angles can clip the long shadow at the frustum edge. |
| L-169 | founder | **[MED — rendering UX] The massing 'envelope' STILL shows on a genuinely-huge tower (40-storey office) — the L-164 threshold correctly keeps massing here (device-loss protection), but the founder still doesn't want the grey shell** — after L-164, massing engages only for isHeavyModel=(levels≥15 AND elems≥1000) OR (elems≥4000); a 40-storey office (~40 levels, >1000 elems) trips it BY DESIGN so the WebGPU renderer doesn't die (L-139/L-150). Founder sees a translucent grey shell over the tower on WebGPU and asks why it's 'still coming'. Tension: seeing all 40 full-detail levels on WebGPU IS the device-loss crash. | Rendering / massing appearance + heavy-tower UX (C04, A.24, L-139/L-150/L-164) | OPEN | routed → rendering lane (after the current shadow agent; DISJOINT). Resolve the tension WITHOUT re-introducing device loss: (a) make the massing proxy look INTENTIONAL not like a jarring translucent grey shell over the real geometry — opaque, building-like massing (A.24 Massing tier styling) OR hide the real geometry entirely under massing so there's no double-image; (b) consider full-detail-all on the WebGL backend (safer than WebGPU at scale) as the huge-model default while WebGPU keeps massing; (c) surface the '3D detail' control prominently + a clear tooltip ('showing massing for a large model — switch to Full to render all floors') so the user understands + can toggle (escape hatch __pryzmLevelScoped3DMode='all'). Maps C04 + A.24 + [[render-tiers-massing-and-presentation]]; no shortcuts; P8. §FIX-MASSING-HUGE-TOWER-UX.   ⬆ FOUNDER ESCALATION (2026-07-07, was reported as "L-177" — folded here, NOT a duplicate): "on the 3D view for the COMMERCIAL/office typology, why is the envelope STILL present? In the residential building or free canvas it is not present anymore." CONFIRMED same root: the log shows the office at ~40 levels / `[SceneQualityTier] 14609 meshes → tier=performance` + `[PascalSceneLighting] §PERF-HEAVY-SHADOW-OFF shadow pass SUPPRESSED` — isHeavyModel trips → massing LOD → grey envelope; the 6-storey resi / free-canvas stay UNDER the threshold → full detail → no envelope. So this is L-164-by-design working exactly as intended, surfacing as a typology-inconsistency the founder reads as an office-only defect. This escalation RAISES the priority to HIGH and makes the founder intent explicit: he does NOT want the grey envelope on the office 3D either. RESOLUTION must still respect the 40-storey WebGPU device-loss ceiling (L-139/L-150) — favour option (a) intentional/opaque building-like massing that reads as the tower (not a translucent grey shell over ghost geometry) and/or (c) a prominent, discoverable '3D detail: Massing ⇄ Full' toggle with the tooltip, and evaluate (b) full-detail on the WebGL backend for huge models. GATED: dispatch AFTER the L-171 shadow agent lands (both edit packages/core-app-model/src/rendering/** — LevelScoped3DCullingService + LevelMassingRenderer vs the shadow services — must not collide).   ✅ FIXED 5a5a2f9e (pushed fa6c17cc→main) — LevelMassingRenderer now renders the massing as an opaque, building-like block and hides the real far-level geometry under it (no translucent grey double-image); device-loss thresholds (L-164) + escape hatches preserved. Agent tests + root tsc exit 0. RESIDUAL: a prominent '3D detail: Massing⇄Full' toggle UI is still desirable (mount was out of the rendering fence) — follow-up. |
| L-170 | founder (log) | **[HIGH — office generator perf/data] Office building creates HUNDREDS of 'office-circ' room-bounding-line annotations with UNDEFINED placement.start/end (partial/legacy records, not rendering) — flooding CreateRoomBoundingLineCommand + tripping the redetect circuit-breaker 65+×** — log: RB-01-722…839+ each 'CreateRoomBoundingLineCommand Created office-circ-annotation_… ' immediately followed by '§RBL-PLACEMENT-GUARD skipping … placement.start/end undefined (partial/legacy record); not rendering', plus 'RoomTopologyObserver redetect circuit-breaker TRIPPED (N same-geometry redetects <1000ms)'. So the office generator emits room-bounding-LINE records with NO start/end (invalid), which are created (command + mark id burned) but skipped at render — pure waste + it hammers room-redetect. | Generative / office-building circulation room-bounding-lines (office executor, RoomBoundingLine) | FIXED (pushed 9801c97b→main) | routed → office-generator agent (FENCED to apps/editor/src/ui/office-building/** + the office-circ RoomBoundingLine emission; DISJOINT). Root-cause why office-circ RoomBoundingLine records are emitted with placement.start/end undefined — either STOP emitting the invalid/partial ones (validate start/end before CreateRoomBoundingLineCommand) or populate placement correctly if they're meant to render. Also throttle the redetect storm (the circuit-breaker is a band-aid; the generator shouldn't fire N same-geometry redetects). Maps C11 + the office pack + [[office-building...]]; no shortcuts; P8. §FIX-OFFICE-CIRC-RBL-UNDEFINED-PLACEMENT.   ✅ FIXED 9801c97b (office-gen agent, pushed to main) — CORRECTED ROOT: the office emission was NOT producing undefined placement (circulationRingSegments already routed every edge through the finite/non-degenerate ringPlanSegments guard; CreateRoomBoundingLineCommand.canExecute rejects non-finite endpoints; the store persists a valid placement). The `§RBL-PLACEMENT-GUARD skipping … undefined` message is a SHARED-CODE latent bug (see L-172): RoomBoundingLineStore.add fires bim-room-bounding-line-added with an {id}-ONLY detail (F.events.17), and initBuilders.ts:965 passes that {id}-only object into builder.build() → the builder sees no placement and skips EVERY RBL of EVERY typology (harmless elsewhere — detection reads the store, not the dashes). Office-specific damage: hundreds of circulation-ring RBLs/floor (~190) that (a) never carve rooms — office floors are graph-authoritative via BatchCreateRoomsCommand+markGraphAuthoritative, (b) never render, (c) each add directly notifies RoomTopologyObserver's RBL subscription (NOT gated by the batch's skipRedetectRooms) → the same-geometry redetect storm / 65× circuit-breaker. FIX (STOP emitting): office floor now materialises RoomBoundingLines ONLY for the real rectangular support rooms via a new pure officeFloorArchitectureBoundingLineSegments() (routed through the shared ringPlanSegments guard); circulation stays represented by the graph-authoritative named Corridor / Open-Plan Office rooms (which DO render + get names + finishes). Removed circulationRingSegments + its ring constant + the executor import → no per-add redetect triggers → storm eliminated at source (≤16 support-room lines/floor, coalesced by the per-level debounce). 34/34 office tests green (new §FIX-OFFICE-CIRC-RBL-UNDEFINED-PLACEMENT regression tests); root tsc exit 0. |
| L-171 | founder | **[HIGH — rendering REGRESSION] The ground-floor invisible-layer shadow is GONE AGAIN — founder: 'before we had the recently implemented feature of shadows in the groundfloor invisible layer, but not anymore — we want them back, architecturally sound'** — tested on prod (backend=**WebGPU**, `[renderer-three] backend: webgpu`); log confirms `[RealEnvironmentService] §FEAT-REAL-ENVIRONMENT enabled — ground=on (catcher attached — receiving real shadow)` yet NO ground shadow is visible on either a tiny 4-wall shell OR the residential building (2 founder screenshots). This is AFTER L-168 (f4533641, shipped) which fit the key-light shadow frustum to the building AABB. So either L-168's frustum-fit did not actually restore the shadow on the real prod WebGPU build, or a distinct WebGPU shadow-receive regression remains. | Rendering / ground shadow-catcher visibility on WebGPU (C04, C10, L-11 §FEAT-REAL-ENVIRONMENT, L-168, ADR-0106/0111) — REGRESSION | FIXED (0a72a3ee → 986a4ba4→main) | routed → rendering-shadow lane (shadow lane free after L-168 shipped; FENCED to the shadow-RECEIVE path — packages/renderer-three GroundShadowCatcher + its ShadowMaterial/WebGPU shadow config, packages/core-app-model/src/rendering/{RealEnvironmentService,PascalSceneLighting,ShadowQualityUpgrader,SceneQualityTier}.ts; COORDINATE with L-160 agent which touches solar/Cesium — do NOT edit RealSunService solar-position code or initScene.ts concurrently, DESCRIBE any needed initScene wiring for the orchestrator to apply post-merge; DISJOINT from office lane). ROOT-CAUSE ON THE REAL PROD BUILD WITH THE RESIDENTIAL BUILDING (the console log provided is a fresh EMPTY project — not diagnostic; a tiny shell also shows no shadow). HYPOTHESES to verify (do NOT assume — L-162/L-168 both had the orchestrator hypothesis corrected): (a) **WebGPU/TSL shadow-receive**: under WebGPURenderer the GroundShadowCatcher ShadowMaterial may not sample the shadow map the same way it does on WebGL (note the log's `THREE.WebGLShadowMap: PCFSoftShadowMap deprecated → PCFShadowMap` under a webgpu backend, and map:512px) — the catcher renders its base grey but may be OUT of the WebGPU shadow-sampling set (the RealEnvironmentService header itself flags a prior L-107 late-attach dropped the receiver on WebGPU/TSL). (b) **refit not triggered on the project-LOAD/batch path**: L-168 wired refitShadowToScene on a debounced geometry-ADD listener — verify it actually fires when a building loads via ProjectLoader/ImportProjectCommand or batch-generate (if batch suppresses the add events, the frustum stays at the empty-scene legacy ±50/far100 → building outgrows it → no shadow). (c) **near-overhead midday sun** (alt 71.6°) puts the shadow directly under the walls → invisible; verify with an angled time-of-day. (d) confirm L-168 (f4533641) is actually in the live bundle. FIX must keep the L-139/L-150 device-loss budget (NEVER touch shadow.mapSize; single-caster Pascal key light) and the L-168 frustum-fit. Add a WebGPU shadow-receive regression test. Maps C04 + C10 + L-11 + ADR-0106/0111 + [[webgpu-heavy-scene-crash-and-instancing]] + [[null-at-mount-runtime-event-race]]; no shortcuts; P8. §FIX-GROUND-SHADOW-WEBGPU-RECEIVE.   ✅ FIXED 0a72a3ee (shipped 986a4ba4→main) — CORRECTED ROOT (all orchestrator hypotheses refuted): the catcher receives correctly (receiveShadow, ShadowMaterial, shadowMap.enabled, PCFShadowMap all sound) AND L-168's frustum math was right. The gap: L-168 re-homed the key light + re-framed its shadow camera on a DEBOUNCED idle-scene settle but NEVER requested a shadow-map re-render or repaint — on PRYZM's on-demand single-rAF WebGPU loop no frame renders after that move, so the fitted sun shadow is computed but never drawn onto the L0 catcher (pure-THREE unit tests never render → passed it through). FIX: RealSunService.onKeyLightDriven callback (mirrors onPositionChange field — no new exported fn) fires ONE guarded shadow refresh (shadowMap.needsUpdate=true + frameScheduler.markDirty — NEVER touches shadow.mapSize = device-loss-safe, identical to the RenderPipelineManager thaw) on the host-driven key-light path; skipped while batchCoordinator.isBatching, inert when shadows OFF (survival tier). Also fixes sun time/offset sliders updating shadows live. 23/23 tests; tsc 0. RESIDUALS: (1) BatchCoordinator shadow-suppress leak — _setupBatch sets shadowMap.enabled=false for the whole batch, restored by CurtainWallBuilder._reactivateShadows / _executeFinalSweep; a no-curtain-wall or aborted generation that misses the restore leaves shadows globally off and the nudge inert (→ if founder still repros, route an agent to packages/core-app-model/src/batch + geometry-curtain-wall); (2) enable() defaults to 12:00 UTC — a far-from-UTC site can yield a low/below-horizon sun (zero shadow by physics). |
| L-172 | orchestrator (L-170 agent surfaced) | **[MED — latent, all typologies] RoomBoundingLine dashed lines NEVER render for ANY typology — RoomBoundingLineStore.add fires `bim-room-bounding-line-added` with an `{id}`-ONLY detail (F.events.17), and initBuilders.ts:965 passes that `{id}`-only object straight into builder.build() → the builder sees no `placement` and skips every RBL** — surfaced by the L-170 office agent while root-causing the `§RBL-PLACEMENT-GUARD skipping … placement undefined` flood. Cosmetic today (room DETECTION reads the store, not the rendered dashes, so rooms are unaffected) but a real latent defect: any feature that expects visible room-bounding dashed lines (apartment/house/office authoring) gets nothing. | Rendering / RoomBoundingLine builder wiring (event-bus/catalog.ts:88 F.events.17, initBuilders.ts:965, RoomBoundingLineStore.add, RBL builder) | OPEN | route → RBL/builder-wiring pass (SEPARATE from L-171 shadow lane; DISJOINT). FIX: initBuilders' bim-room-bounding-line-added handler must resolve the FULL record from the store by id (or the store must emit the full placement in the event detail) before calling builder.build(), so the dashed line renders with its real start/end. Add a test: add an RBL → the builder receives finite placement → a line mesh is produced. Maps C11 + F.events.17 + the RBL subsystem; no shortcuts; P8. §FIX-RBL-IDONLY-EVENT-NO-RENDER.   ✅ FIXED fbcfb954 (pushed→main) — the bim-room-bounding-line-added handler in initBuilders.ts now resolves the FULL RBL record from the store by id (keeping the {id}-only event contract) before builder.build(), so the placement-guard passes with real start/end and the dashed line renders. 2 files (initBuilders.ts + RoomBoundingLineIdOnlyEventRender.test.ts, +97 test lines); tsc exit 0. |
| L-173 | founder | **[HIGH — AutoDimension/annotation UX] A dimension must be a first-class SELECTABLE element with PROPERTIES shown in the Properties Panel — "like a wall" — starting from the dimension drawn via the UI** — founder: "THE DIMENSION ELEMENT IS BEING BETTER THIS TIME — the batch execution is performing well — BUT the dimension should be an ELEMENT LIKE A WALL: it should be SELECTABLE and it should have PROPERTIES within like a wall — the properties will show in the PROPERTIES PANEL, starting from the dim used via UI." Follows L-161 (linear-dim selectable/editable in plan, FIXED) — now the selected dim must surface its property inspector. | Documentation / dimension selection → Properties Panel (C03, DOC-2.x, C56, ADR-0119) | FIXED (986a4ba4→main) | routed → AutoDim/annotation-usability agent (FENCED to apps/editor/src/ui/property-panel/** + the selection→panel wiring in apps/editor/src/engine/{engineLauncher.ts selection handler / initTools §ANN-SEL / AnnotationManager.setPropertyPanel path}; may READ but MINIMALLY edit PlanViewInteraction.ts which L-161 already committed; DISJOINT from the L-171 shadow lane — do NOT touch initScene.ts or packages/*/rendering — and from the L-160 cesium lane). 🔎 ORCHESTRATOR SCOPING (code-read): the dimension Properties Panel ALREADY EXISTS and is complete — PropertyPanelAnnotations.ts `showLinearDimension()` renders MEASUREMENT (unit + measured/drive-wall), APPEARANCE (text size/arrow/line+text colour), LABEL (prefix/suffix/override), CONSTRAINTS (lock/soft-hard), APPLY (annotation.setTextHeight/setColor/annotation.update — all P6 commands) + Delete (annotation.delete). `annotationManager.setPropertyPanel(inspector)` (initTools.ts:2200 §ANN-SEL) wires it for the OBC/AnnotationManager annotation path. THE GAP: a UI-drawn 'linear-dim' is an ADR-0119 SUBSYSTEM-STORE AnnotationElement rendered by PlanViewAnnotationRenderer (L-145 sink) and selected via PlanViewInteraction (L-161) → it emits `pryzm-element-selected {annotationId, source:'plan-view'}` (PlanViewInteraction.ts:536/609). Verify whether ANY `pryzm-element-selected` listener (engineLauncher.ts:337 / AnnotationManager) resolves that annotationId to the AnnotationElement in the runtime annotation subsystem store and calls `inspector.showLinearDimension(ann, selectedWallId?)` — grep found NO caller of the adapter's showLinearDimension in the engine, so the selection→panel trigger for the new ADR-0119 path is the missing wire (same lineage as L-161). FIX: on selecting a dimension annotation (plan-view + 3D where applicable), resolve the AnnotationElement from the subsystem annotationStore by id and open the panel via inspector.showLinearDimension — mirror how a wall selection reaches inspector.showElement. ARCHITECTURE: keep the dimension an AnnotationElement (documentation layer — Revit-parity: dims are annotation elements with a Properties palette, NOT model geometry); "like a wall" = the SAME Properties Panel surface + selectability, NOT promotion to a geometry element. Mutations stay P6 (already are). Add a test: draw a UI dim → select it → showLinearDimension is invoked with the correct AnnotationElement → APPLY drives an annotation.update. Maps C03 + C56 + DOC-2.x + ADR-0119 + [[c16-command-authoring-and-semantic-engine]]; no shortcuts; P8. §FIX-DIM-SELECT-PROPERTIES-PANEL.   ✅ FIXED 986a4ba4 (shipped→main) — the panel + click path already existed (L-161, predated founder's report); real residual = the selection→panel trigger was a per-view point-fix in one pointer handler, NOT driven by the shared pryzm-element-selected bus like every other selection → didn't fire on drag-commit re-select, passed no co-selected wall (drive-wall affordance missing from plan). FIX: new pure openDimensionPropertiesOnSelect(detail, deps) resolver registered on pryzm-element-selected in engineLauncher (mirrors updateInspector→showElement), resolves id→linear-dim AnnotationElement, opens inspector.showLinearDimension(ann, selectedWallId); skips non-dim/BIM ids; removed L-161's redundant direct call to avoid double-open. Dimension stays an AnnotationElement (no geometry promotion). 8/8 tests (incl real showLinearDimension APPLY→annotation.update P6); tsc 0.   ⬆ RE-REPORTED (2026-07-08, founder: "dimensions CAN NOT be SELECTED, MOVED or DELETED — I thought this was done"): agent re-traced the full path — SELECT (hitTestAnnotation includes 'linear-dim' + emits pryzm-element-selected → panel opens) and MOVE (mouseup commits UpdateAnnotationCommand, P6) are BOTH wired & functional (L-161/L-173). The literal break was **DELETE**: `deleteSelected()` (initUI.ts:2156) only handled `selectionManager.selectedObject` (a THREE.Object3D) — a plan dimension is a Canvas2D AnnotationElement, never a scene object → the handler early-returned "No element selected". The `annotation.delete` command existed (the panel's Delete button used it) but the keyboard Delete/Backspace path never reached annotations. Secondary: empty-space click never cleared `__pryzmSelectedAnnotationId` → stale highlight + stale delete-target. ✅ FIXED 5822189f→main — new pure `deleteSelectedDimension(deps)` dispatches `annotation.delete` (skips when a 3D object is selected — element-delete precedence — or id is stale); `deleteSelected()` calls it before the BIM branch + `unselectAll()`; empty-space click now clears the selected-annotation id. 13/13 tests (delete/precedence/no-op/stale + move UpdateAnnotationCommand); tsc 0, P6. Live-prod re-verify of select/move recommended (headless can't drive the Canvas2D pick). §FIX-DIMENSION-FIRST-CLASS-SELECTABLE-L173. |
| L-174 | founder | **[HIGH — REGRESSION, camera/UX] A batch RESIDENTIAL building no longer auto-frames into the 3D view on generation — "it was coming before, but now I need to click Fit All so the building appears"** — founder: after generating a batch resi building the 3D camera does NOT frame the new geometry; user must manually zoom-to-fit ("Fit All"). Log (build main--xN04I6h.js — post-L-170): generation completes fully (apartments finished, 6 floors + finishes laid, `[BatchCoordinator] §TRACE BUILD-QUEUE-DRAINED … SHADOW-REACTIVATION-START T=+6741ms`), geometry drains across many frames via WallFragmentBuilder/SlabFragmentBuilder RAF_DRAIN (L-131 P5 progressive generation), but NO zoom-to-all frames the result. | Rendering / camera auto-frame after batch generation (C04, C06, L-131 P5, [[onboarding-site-generate-view-flow]]) — REGRESSION | OPEN (QUEUED — dispatch after L-173 frees initTools/engineLauncher; overlaps L-171 initScene too) | routed → generative/camera-framing agent (dispatch AFTER L-173 lands to avoid a shared-file collision on initTools.ts/engineLauncher.ts/initScene.ts). 🔎 ORCHESTRATOR SCOPING (code-read): the app has THREE auto-frame paths — (1) §3D-FRAME-ON-VIEW-SWITCH (initTools.ts ~1859-1943) fires ONCE per project session, gated by `_3dViewFirstFrameDone`, re-armed on project-switch, suppressed while a draw tool is active (§AUTOFRAME-NO-HIJACK-WHILE-DRAWING); (2) §VIEW-AUTOFRAME (SplitViewManager.ts ~261) frames the main 3D viewport on split-view entry via a deferred zoomToAll that self-guards an empty scene; (3) §SVP3D-FRAME-ON-SWITCH (SplitViewManager ~895) on right-pane 3D switch. PRIME HYPOTHESIS (verify, don't assume): on a NEW project the 3D view + split-view are entered at PROJECT-OPEN while the scene is EMPTY → the once-per-session §3D-FRAME flag is consumed (and §VIEW-AUTOFRAME zoomToAll no-ops on the empty scene) → when the building generates SECONDS later across the L-131 progressive RAF drain, NO path re-frames (flag already spent; no batch-complete re-frame). Regression candidates for "worked before": L-131 P5 progressive-generation timing (geometry now drains well after any fixed deferred-frame window), and/or the L-166 view-result-bar consolidation changing the batch→result-view handoff. ARCHITECTURALLY-SOUND FIX: hook the auto-frame to the GENERATION-COMPLETE / geometry-settled signal, not a fixed timer or the first-view-activation flag — e.g. after `[BatchCoordinator] BUILD-QUEUE-DRAINED`/SHADOW-REACTIVATION (the same drained signal shadow reactivation uses), dispatch a zoom-to-fit of the 3D view (reuse the existing zoom-fit command → zoomToAll; keep the §AUTOFRAME-NO-HIJACK-WHILE-DRAWING guard so it never hijacks mid-draw). Make it TYPOLOGY-GENERIC (house/apartment/office/resi all auto-frame after batch generate), not resi-only. Respect the L-131/L-139/L-150 perf budget (one frame after drain, not per-chunk). Maps C04 + C06 + L-131 + [[onboarding-site-generate-view-flow]] + [[batch-creation-perf-pattern]]; no shortcuts; P8. §FIX-BATCH-GEN-AUTOFRAME-3D.   ✅ FIXED fa6c17cc (pushed→main) — auto-frame hooked to the generation-complete/geometry-settled signal (new batchAutoFrame module) instead of the once-per-session flag; keeps the no-hijack-while-drawing guard; typology-generic. 7 tests + root tsc exit 0. |
| L-175 | founder | **[HIGH — documentation UX] Elevation view: allow EXTENDING the crop with drag-handles, EXACTLY like plan view** — founder: "the crop is working great in plan view; now when I am in the elevation I want to also be able to extend the crop, exactly the same as plan view — allow me to do it." Screenshot (South Elevation): the crop rectangle + corner drag-handles already RENDER (dashed blue rect + blue square handles), but dragging them to resize the crop does nothing in elevation (works in plan). Log confirms elevation views ARE crop-aware: `EdgeProjectorService project() viewId=vd-sys-elev-south … cropRegion=[-22.94,3.86 → 22.94,11.96]`. | Documentation / view crop interaction in elevation views (C06, DOC crop-region, L-154/L-157) | OPEN (QUEUED — dispatch after L-173 frees PlanViewInteraction.ts) | routed → view-crop agent (dispatch AFTER L-173 lands; FENCED to the crop-resize interaction — PlanViewInteraction.ts crop-handle drag (`_renderSelectedScopeOverlay` handles + `_cropRegionFromSectionVolume` + `view.setCrop`) and whatever elevation-view interaction host mirrors it; DISJOINT from L-174 autoframe (SplitViewManager/initTools) and L-176 dim-tool). 🔎 ORCHESTRATOR SCOPING: the plan-view crop drag lives in PlanViewInteraction.ts (line ~620 `_renderSelectedScopeOverlay`'s crop adjust-arrows + drag handles; ~1060 `_cropRegionFromSectionVolume`; ~1076 `view.setCrop` command). The overlay ALREADY renders in elevation (per screenshot) — the gap is that the resize-drag hit-testing/handler is gated to plan views (or the elevation interaction host doesn't wire it). FIX: enable the SAME crop-resize drag → `view.setCrop` (P6 command) for elevation ortho views; reuse the plan crop-region math (elevation crop is a Y/X-Z window vs plan's X-Z — respect the elevation projection frame). Keep it a view-crop (documentation) concern, not a geometry mutation. Maps C06 + DOC crop-region + L-154/L-157; no shortcuts; P8. §FIX-ELEVATION-CROP-EXTEND.   ✅ FIXED bf0741c4 (committed; batched for next push) — the crop rect + handles already RENDERED in elevation but had no hit-test/drag (only the plan scope-box was draggable). Added PlanViewCanvas.hitTestCropHandle (gated to elevation/section via _sectionFlipV) + pure cropFromHandleDrag (affine region↔canvas calibration — correct for plan/section-volume/elevation frames without per-frame duplication) + PlanViewInteraction _cropDrag (mousedown hit-test → throttled live preview → mouseup commit via view.setCrop, P6). 7/7 tests + root tsc exit 0. Residual: plan-view crop-boundary handles stay non-draggable (plan uses the scope-box path — out of L-175 scope). |
| L-176 | founder | **[HIGH — AutoDimension/annotation UX] Manual linear dimension ignores the 3rd click's position — the dim lands at a DEFAULT offset instead of where the user clicked / where the PREVIEW rendered it** — founder: "the linear dimension works almost perfect — first click (ref A), second click (ref B), the third click really triggers the creation — HOWEVER when the user clicks on a place (which the preview actually renders it) is WHERE the dimension should be placed — but it is going too short to a default location." Via UI. Log: LinearDimPlanToolHandler sets ref A, ref B, "Parallel wall constraint applied — perpendicular dim", then CREATE_ANNOTATION — and IMMEDIATELY after, `[PlanViewInteraction] Annotation drag started … Annotation moved` (the founder is having to DRAG it to the right offset because the 3rd click's standoff wasn't honoured). | Documentation / manual linear-dim 3-click standoff placement (C56, DOC-2.x, ADR-0119, L-155) | OPEN (QUEUED — dispatch after L-173 frees the dim/LinearDimPlanToolHandler subsystem) | routed → AutoDim/annotation-usability agent (dispatch AFTER L-173; FENCED to apps/editor/src/engine/views/plantools/LinearDimPlanToolHandler.ts + the dim preview + the geometry2D.offset it passes to CreateAnnotationCommand; DISJOINT from L-175 crop (PlanViewInteraction) — but note both touch the plan-tool area, so sequence after L-173 and keep fences tight). 🔎 SCOPING: the 3-click linear-dim state machine is LinearDimPlanToolHandler (ref A → ref B → 3rd click = the dimension-LINE standoff/offset). The bug: the 3rd click's perpendicular offset (the live preview position) is NOT captured into the created annotation's `geometry2D.offset` (WORLD metres — see L-155) — it falls back to a default standoff, so the dim renders too close/short and the user must drag it. FIX: capture the 3rd click's perpendicular distance from the A–B measurement line and pass it as the annotation's world `offset` at CREATE time (mirror the live preview's offset exactly), so the dim is placed where the preview showed it — no post-create drag needed. Reuse the L-155 world-standoff convention (geometry2D.offset consumed as WORLD metres). Keep P6 (CreateAnnotationCommand). Maps C56 + DOC-2.x + ADR-0119 + L-155/L-161; no shortcuts; P8. §FIX-DIM-3RD-CLICK-OFFSET-PLACEMENT.   ✅ FIXED 45825136 (pushed fa6c17cc→main) — the 3rd click's perpendicular standoff is captured into geometry2D.offset (world m) at CREATE, matching the preview; no post-create drag. 3 tests + root tsc exit 0. |
| L-177 | founder | **[HIGH — analysis fidelity] Façade sun-hours analysis must be mapped onto the REAL building geometry, not a separate simplified translucent prism** — founder (2 images): image 1 = today's output — a boxy translucent green/orange analytic surface floating over the model; image 2 = the REAL full-fidelity building (balconies, setbacks, real openings) where he wants "the real data graphically mapped." Log: `[CesiumViewport][forma-facade] §FORMA-FACADE-SMOOTH + §FIX-FACADE-ANALYSIS-REAL-GEOMETRY painted 5 smooth textured façade surface(s) (4 wall face(s) + roof, 508 lattice node(s), 93 real opening hole(s) from 219 authored)` — L-160 punched the openings into a 5-face prism, but it's STILL a simplified separate surface, not the actual REAL GLB model's façade. Founder wants the sun-hours colour mapped onto the REAL model surface itself. | Geospatial / façade sun-hours analysis on real model geometry (C18-23, A.24, ADR-0074/0110, [[real-solar-heat-thermal-queue]], L-160/L-144) | ⚠ PARTIAL 645495b3→main — DRAPE MACHINERY SHIPPED but founder-visible prism PERSISTS → follow-up **L-193** (GISAreaLayout lane). What shipped: `buildRealModelSunDrape` (siteMetricGrids.ts) resamples the byte-identical BVH intensities into cylindrical WALL (U=centroid-angle, V=height) + top-down ROOF lookups (same `sunHoursRgb(Vivid)` ramp, openings punched to alpha 0); `applyRealModelSunDrape` (CesiumViewport) uploads them as `TextureUniform` + attaches an UNLIT `MODIFY_MATERIAL` CustomShader reading `positionMC` (§A.21.D54 ENU) → sun-hours drapes onto the REAL placed `Cesium.Model`'s own faces, no separate prism; falls back to the polygon envelope only in massing mode / if CustomShader unavailable; re-drapes if the real model is placed after analysis toggles on. 14 pure tests, tsc 0. **WHY FOUNDER STILL SEES PRISM (root, OUT of this fence):** the Forma BASE building is still rendering as MASSING not the real GLB (console `massing rendered: 58 wall(s)` proves `renderRealModelOnForma` never ran — on success it calls `clearFormaMassingEntitiesOnly`); the drape only activates when a real model is present, so it correctly falls back to the massing-face envelope. The real-model placement is orchestrated by `GISAreaLayout.ts` `placeRealModelOnForma` (~line 1874) — logged as **L-193**. NOT a regression from this WIP (renderFormaMassing untouched). §FIX-FACADE-ANALYSIS-ON-REAL-MODEL.   ✅ UNBLOCKED by L-193 3f132de9→main — refined root: `placeRealModelOnForma` DOES run on first Forma entry (default fidelity='real'); the prism only reappeared AFTER a photoreal-globe round-trip destroyed the Forma real-model primitive without invalidating the Forma placement cache → reuse guard returned 'reuse-placed' for a destroyed model → massing fallback. L-193 invalidates the Forma cache on globe entry + re-places on re-entry, so the real house returns and this drape lands on it. Live-Cesium confirmation pending. | routed → Cesium/Forma façade agent (BUNDLE with L-178 + L-179 — all CesiumViewport.ts + siteMetricGrids.ts + solar workers; ONE agent, same files, no self-collision; FENCED to apps/editor/src/ui/geospatial/CesiumViewport.ts + apps/editor/src/ui/climate/siteMetricGrids.ts + apps/editor/src/workers/Solar*.ts; DISJOINT from every other lane). FIX: paint the sun-hours analytic result as a vertex/texture colour ONTO the REAL placed model's façade surfaces (the §A.21.D49/forma6 REAL GLB that is already placed — "massing blocks hidden"), sampling per-surface incident sun-hours, instead of building a separate 5-face analytic prism floating over it. Keep the existing BVH sun-hours evaluator (byte-identical, ADR-0110) as the data source; only change the DISPLAY target from the simplified prism to the real model faces (or drape the analysis texture onto the real geometry). Respect A.24 Presentation tier + the device-loss budget. Maps C18-23 + A.24 + [[real-solar-heat-thermal-queue]] + [[revit-roundtrip-audit-and-thatopen]]; no shortcuts; P8. §FIX-FACADE-ANALYSIS-ON-REAL-MODEL.   ⏸ STILL OPEN (deferred by the Cesium agent, NOT committed) — painting sun-hours onto the REAL placed GLB's actual faces (balconies/setbacks/openings) requires a Cesium CustomShader / per-vertex sun-hours drape on the Cesium.Model (read GLB vertex positions/normals, evaluate the BVH per vertex, inject a colour attribute) — a substantial feature that can't be verified headless (no live Cesium) and risks the A.24 device-loss budget. Correctly left out rather than shipping a half-drape; the current analytic prism (renderFacadeAnalysis) is untouched. RE-ROUTE as its own focused Cesium-façade task (needs live-view verification).   ⬆⬆ FOUNDER ESCALATION (2026-07-08, "requested MULTIPLE times", HOUSE example): the 3D-Site façade analysis STILL shows the transparent coloured ENVELOPE PRISM, NOT the real house (screenshots: transparent multi-coloured box vs the actual house with roof/windows). Log confirms it's the L-160 prism: `§FORMA-FACADE-SMOOTH … painted 5 smooth textured façade surface(s) (4 wall face(s) + roof, 96 lattice node(s), 5 real opening hole(s) from 7 authored)`. NOW HIGH-PRIORITY. REQUIRED: (1) paint the sun-hours onto the REAL placed GLB model's faces (per-vertex colour or a projected/draped analysis texture on the Cesium.Model), (2) HIDE the separate analytic prism so there is no misleading transparent envelope. Keep the BVH evaluator byte-identical (ADR-0110) — change only the DISPLAY target. Acceptable interim if a full per-vertex drape is too large in one pass: hide the prism + tint the real model faces by a per-face sun-hours average — but the goal is the REAL house carrying the data. GATED: dispatch AFTER the L-183/L-184 Cesium agent frees CesiumViewport.ts; BUNDLE with L-185 (sea context) as the next dedicated Forma-view agent. §FIX-FACADE-ANALYSIS-ON-REAL-MODEL. |
| L-178 | founder (log) | **[HIGH — perf defect, L-160 follow-up] The just-shipped solar-hours WORKER CRASHES in prod and falls back to the slow synchronous raycast — perf win inert** — log: `[SolarWorkerPool] worker error — falling back to synchronous raycast: Uncaught Error: secure crypto unusable, insecure Math.random not allowed`. The L-160 §PERF-SUNHOURS-WORKER (solar.worker.ts) uses `Math.random` (or crypto) in the worker context where it is blocked → the worker throws → falls back to the exact ~1-min main-thread freeze the worker was meant to eliminate. So L-160's off-main-thread perf benefit is NOT active on prod. | Geospatial / solar worker determinism (ADR-0110, C10 perf, L-160) — DEFECT in shipped 21f698a5 | OPEN | routed → Cesium/Forma façade agent (BUNDLE with L-177 + L-179; same solar-worker files). FIX: remove the `Math.random`/insecure-crypto usage from the worker hot path — the sun-hours raycast is DETERMINISTIC (fixed sun samples, BVH); any RNG (e.g. jitter/sampling seed) must be replaced with a deterministic sequence or seeded PRNG that needs no `crypto`/`Math.random`, so the worker runs instead of throwing. Verify the worker path now actually executes (no fallback log) and stays byte-identical to the main-thread evaluator. Maps ADR-0110 + C10; no shortcuts; P8. §FIX-SOLAR-WORKER-NO-RANDOM.   ✅ FIXED bd158aa3 (pushed→main) — ROOT was a MODULE-LOAD side effect, not RNG in the raycast (the compute chain is provably RNG-free). solar.worker.ts imported computeSunIntensitiesForProbes from siteMetricGrids, which statically pulled in the whole climate/street graph (@pryzm/climate-host, @pryzm/street-analytics, climateChartData…) whose transitive dep runs a secure-crypto id generator at LOAD — unusable in a DedicatedWorkerGlobalScope → throw → sync fallback. FIX: moved the pure raycast core (Prism/toPrisms/pointInRing/rayBlockedByPrism/sunBlocked/PrismShadowIndex/…/computeSunIntensitiesForProbes) verbatim into the worker's solarCodec leaf; worker runtime graph is now exactly {solarCodec → @pryzm/solar-analysis} (pure, crypto-free); siteMetricGrids re-exports them so all main-thread callers (+ GISAreaLayout) unchanged. Byte-identical worker==main test. tsc 0. |
| L-179 | founder | **[CRITICAL — geospatial REGRESSION-feel] 3D GLOBE (Cesium photoreal 3D-tiles) view: the building is UNDERGROUND and the camera doesn't frame it — "before you tried to fix it today at least the building was in the correct location; now neither the camera nor the building"** — founder points to today's commits. 🔎 ORCHESTRATOR FORENSIC (git): today's L-160 (21f698a5) changed CesiumViewport.ts +104 lines but ONLY the FAÇADE-analysis sun params — the globe placement (placeBuildingOnGlobe / §A.21.D49 / reframeSiteIn3D / base-height) was NOT touched (diff grep for placeBuilding|reframe|clamp|sampleHeight|ellipsoid|terrain = 0 hits except one façade `sunDay` line). So the underground building is NOT an L-160 regression. REAL ROOT (log): `[CesiumViewport][forma] terrain clamp degraded (no real terrain provider attached (keyless ellipsoid ground) — clamping to flat base 0)` + `§A.21.D49 REAL model placed on photoreal tiles … base 0.00 m` — the model is clamped to ellipsoid height 0 while the Google **Photorealistic 3D Tiles** put the real ground at the site's true elevation (Madrid ≈ 650 m), so the model sits ~650 m below the visible tile surface = underground. The L-142 revert restored the prior placement code, but that code still clamps to 0 (or the "correct before" state used a path that sampled the tile height). | Geospatial / globe REAL-model placement clamped to tile surface (C18-23, ADR globe, L-142, [[localhost-dev-unusable-test-on-prod]]) | OPEN (URGENT) | routed → Cesium/Forma façade agent (BUNDLE with L-177/L-178; same CesiumViewport.ts — PRIORITISE this first). DEEPER FORENSIC then FIX: compare the globe REAL-model placement (§A.21.D49 / placeBuildingOnGlobe / reframeSiteIn3D) across today's commits AND the L-142 revert (b448f4c8) to find exactly what changed the "correct before" placement; then CLAMP the placed model (and the camera frame) to the loaded Google Photorealistic 3D Tiles SURFACE height at the site lat/lon — via Cesium `sampleHeightMostDetailed`/`clampToHeightMostDetailed` against the tileset (NOT ellipsoid 0, and NOT by re-enabling ion World Terrain — that caused the L-142 727 m-underground regression; DO NOT re-add ion terrain hosts to securityHeaders.js CSP). Frame the camera on the placed model. Verify the building sits ON the photoreal ground and the camera focuses it. Maps C18-23 + L-142 lesson + [[localhost-dev-unusable-test-on-prod]] + [[fly-production-deploy]]; no shortcuts; P8. §FIX-GLOBE-CLAMP-TO-PHOTOREAL-TILES.   ✅ FIXED bd158aa3 (pushed→main) — CORRECTED CULPRIT via git forensic: NOT L-160 (21f698a5 touched only façade/solar params). The regressor is bd23f6a3 "§GLOBE-TERRAIN-HEIGHT bare-earth base" (2026-07-01), which re-added ion World Terrain (getPhotorealBareEarthTerrain/samplePhotorealBareEarthHeight via createWorldTerrainAsync) that the L-142 revert b448f4c8 had removed — landing after the working 0377f160 street-ring clamp. clampToPhotorealTilesThenReplace PREFERRED the ion terrain sample over the tile-surface clamp: on Madrid (~650m) it mis-seats when the terrain datum disagrees with the tiles (L-142 727m class), and on failure the token-abort guard returns out of the WHOLE clamp before the tile-mesh fallback → base stays at ellipsoid 0 while the real GLB is seated at 0 = ~650m underground. FIX: removed the ion World Terrain path entirely (no CSP terrain host); globe now clamps model + camera ONLY to the loaded Google 3D-Tiles surface via clampToHeightMostDetailed/sampleHeightMostDetailed (min over footprint + street ring; tileset bounding-sphere ground as last resort) + pure selectPhotorealTileBaseHeight (unit-tested); existing reseat/reframe re-frames after base settles. tsc 0; 82 tests (globe-clamp 8 + no-jump 4 + worker/site 60+ …). Verified via pure-fn tests + tsc (no live Cesium headless). |
| L-180 | founder | **[HIGH — AutoDimension correctness] Doors/windows are NOT correctly dimensioned — the opening ORIGIN is misaligned with the dim (witness lines don't land on the opening's real jamb edges)** — founder image: an auto-dim chain on a wall with a window/door; "there is an issue with the origin of the door/window and a misalignment with the dim; everything else looks sound." The opening dim references the opening's insertion origin/centre rather than its actual left/right jamb edges, so the witness lines are offset from the real opening. Log shows openings placed with an along-wall `offset` (e.g. `[DoorPlanToolHandler] Door placement … width=0.926m offset=2.358m`) — the auto-dim must convert (offset, width) into the true jamb edge positions in the SAME origin/frame the dim chain uses. | Documentation / auto-dimension opening (door/window) edge derivation (C56, DOC-2.x, ADR-0119, L-155/L-147) | OPEN | routed → AutoDim engine agent (FENCED to packages/auto-dimension/** (openings.ts RunOpening edge derivation + planners.ts planOpeningChain/planOpeningLocations + planAutoDimensions.ts) + apps/editor/src/ui/documentation/applyAutoDimensions.ts opening extraction; DISJOINT from L-176 which is the MANUAL LinearDimPlanToolHandler, and from all other lanes). 🔎 SCOPING: planAutoDimensions.ts:253 already intends "a width dim = both refs are the opening's own left/right edges" and openingsOnRun (openings.ts) builds RunOpening from the wall's openings; the DEFECT is that the opening's jamb-edge positions are derived from a wrong origin/reference — likely the store opening's along-wall `offset` (measured from one wall end / the opening centre) is mapped to edges in a frame that doesn't match the dim chain's wall-run parameterisation (off by the wall start origin, the centre-vs-edge convention, or the opening insertion point), so the witness lines land off the real jambs. FIX: derive each opening's left/right jamb positions as offset ± width/2 (or the correct store convention) in the EXACT wall-run coordinate frame the auto-dim chain uses (same origin as the wall segment endpoints the perimeter/chain planners use), so the opening dim witness lines land on the true jamb edges. Verify against a door with a known offset+width and a window. Reuse the L-155 world-scale convention. Keep P6/pure-engine purity. Maps C56 + DOC-2.x + ADR-0119 + L-147/L-155; no shortcuts; P8. §FIX-AUTODIM-OPENING-EDGE-ORIGIN.   ✅ FIXED 3806a584 (pushed→main) — ROOT: two halves disagreed on the store `offset` meaning. Store convention (§OPENING-OFFSET-LEFTEDGE-UNIFY, C15 §2): offset = LEFT EDGE of [offset, offset+width] (DoorPlanToolHandler/WindowPlanToolHandler emit left edge; edge-projection/poche cut voids as [offset, offset+width]; the pure engine openings.ts correctly uses left/right/centre from it). BUT the geometry-kernel evaluator (resolveDoorAnchor/resolveWindowAnchor) reads DoorLikeEvaluator.offset as the CENTRE (center=a+u·offset). The executor's buildEvalSnapshot fed the store LEFT-EDGE offset into the CENTRE-expecting evaluator → every opening dim shifted along-wall by −width/2 + the set-out location dim referenced the left edge not the centre = the founder's misalignment. FIX (in-fence, at the impure executor adapter — engine + evaluator untouched): buildEvalSnapshot converts left→centre (offset + width/2); resolved world jambs now coincide with the chain planner's run-frame stations. 30/30 auto-dim + 9/9 executor tests; tsc 0. RESIDUAL: same store-left-edge vs evaluator-centre mismatch would affect any OTHER caller building a DoorLike/WindowLikeEvaluator directly from store offsets (e.g. generateDocumentationSet, the manual dim path) — follow-up audit worth doing. |
| L-181 | founder | **[CRITICAL — project isolation/lifecycle] After creating/opening a project and working in it, going back to the project page and opening/creating ANOTHER project leaves the second project CORRUPTED — no clean initial 3D canvas, no blue origin sphere, cannot create elements; state bleeds from the previous project** — founder: "when a project has been created/opened the user works in it, then goes back to the project page and opens or creates a new one — somehow it is corrupted; the user doesn't see the same initial version (no 3D canvas with the blue origin sphere), can't even create elements — it is being influenced by the previous project." A second project open/create in the same session does NOT fully re-initialise → previous project's scene/stores/runtime leak in, and the new-project init (empty scene + §FEAT-PROJECT-ORIGIN blue datum sphere + working tools) never re-establishes. | Client / project-switch isolation + scene/runtime re-init on 2nd open/create (project isolation, C-lifecycle, check:isolation CI, [[auth-session-leak-account-switch]], [[503 migrations_in_progress deadlock]]) | OPEN (CRITICAL — dispatch after 4:40pm session reset) | routed → project-lifecycle/isolation agent (INVESTIGATE the full open→work→back→open/create-2nd path). Likely surfaces: PlatformShell/PlatformRouter project open+switch handoff, ProjectLoader, ClearProjectCommand (§CLEAR-PROJECT-BATCH — does the 2nd switch actually clear ALL stores/scene?), initScene `pryzm-project-switch`/`pryzm-project-loaded` re-init, initProjectOrigin (§FEAT-PROJECT-ORIGIN blue sphere — why absent on 2nd project = re-init not firing), ProjectScopedStorage + ProjectIsolationAudit (the existing "will audit every empty project load" tool — read its output), runtime/store disposal on switch, and whether the 2nd load re-arms the tool/command wiring (can't-create-elements = tools/commandBus not re-bound or scene not writable). ROOT-CAUSE which subsystem retains prior state (stores not cleared, scene not disposed, runtime singletons re-used with stale refs, or a listener registered once that no-ops on the 2nd project — cf. [[null-at-mount-runtime-event-race]]). FIX so EVERY project open/create yields a pristine, isolated project (clean scene + origin sphere + working tools + no cross-project bleed). Add a test/audit gate: open A → create/open B → assert B's scene is empty-but-initialised (origin present, 0 leaked elements from A, tools armed). Maps project isolation + the isolation CI (`npm run check:isolation`); no shortcuts; P8. §FIX-PROJECT-2ND-OPEN-ISOLATION.   ✅ PARTIAL FIX 3f8da8f8 (pushed→main) — origin-sphere leak (symptom 1) ROOT-CAUSED + FIXED: ProjectOriginStore.reset() is documented as the canonical project-switch hook but has ZERO callers (grep-confirmed), AND the store was ABSENT from projectScopeRegistry (the registry ClearProjectCommand iterates for clearAll()+reseedAll()) — the only per-project store missing. So Project A's origin datum (hidden/moved) leaks into B and initProjectOrigin's pryzm-project-loaded re-apply faithfully re-paints A's leaked state onto B → the missing blue sphere. FIX (initProjectOrigin.ts, defence-in-depth idempotent): (a) register projectOriginStore in projectScopeRegistry (clear/reseed→reset()) so ClearProjectCommand re-seeds the datum at world origin + visible on every open (same path as the other ~34 per-project stores); (b) subscribe pryzm-project-switch to re-seed + re-attach + re-paint the marker BEFORE B hydrates. 4/4 new tests + 741/741 stores + 157/157 renderer-three green; tsc 0; Contract-45 registry guard green (check:isolation's exit-1 is a PRE-EXISTING unrelated main.ts:56 pryzm:chunk-reload-at finding, not this change). ⏸ RESIDUAL — symptoms 2 (cannot create elements) + 3 (geometry bleeds from prior project) NOT reduced to a specific defect by static analysis (scene/store clear + switch-teardown paths are structurally sound); agent refused to ship a guess. STRONG HYPOTHESIS: a batch/suppression latch left imbalanced when Project A ran a batch/AI-generation op and the user navigated away mid-settle (only bites the 2nd project). NEXT STEP (needs FOUNDER live prod repro per [[localhost-dev-unusable-test-on-prod]]): open A (batch-generate) → back → open B → capture the already-wired ProjectIsolationAudit `[CONTRACT 48 VIOLATION]` console line on the 2nd empty load — it names the exact leaked surface (scene.bimElement vs window.globals) + which store/mesh survived. §FIX-PROJECT-2ND-OPEN-ISOLATION-RESIDUAL. **✅ ROOT-CAUSED + FIX SHIPPED via L-342 (89145bd1, 2026-07-16, deploying):** the direct-LOAD path (`ProjectLoader.load()`) skipped the pipeline teardown that the SWITCH path already ran — a leaked BatchCoordinator `_isBatching` / `_wallRebuildDiscarding` latch from an interrupted prior-project AI batch survived into the next project (= the can't-create + geometry-bleed residual). L-342 runs `runTeardown('load')` at the load chokepoint. PENDING founder live confirm (open A + AI-batch -> back -> open B -> B loads clean + can create elements). |
| L-182 | founder | **[HIGH — documentation fidelity] ELEVATION views have NO line-weight hierarchy (cut/projection/beyond/hidden) — the Visibility Intents 'elevation' view-type tab is EMPTY while 'plan' has real modifiers** — founder (3 screenshots, evidence-first): plan view shows real line-weight differentiation; the elevation viewport shows basically uniform line weight with no cut/beyond/hidden/projection hierarchy; the Visibility Intents panel ('Architectural Documentation (Auto)') shows the `plan` view-type tab has populated door/window modifiers (cut/beyond/hidden/projection fields) while the `elevation` tab says "No modifiers defined for this view type." So either the differentiation is hardcoded/defaulted outside the intent system, or elevations get uniform default weight with no real classification. DO NOT ASSUME which — investigate + report evidence before fixing. | Documentation / elevation line-weight via Visibility Intents (C06 §7, DOC-1.x/DOC-2.x view rendering, VGSceneApplicator, EdgeProjectorService, visibility-intent schema+seed) | OPEN | routed → documentation/visibility-intent agent (FENCED to the visibility-intent schema + default-seed + the elevation edge-classification/technical-drawing render path (EdgeProjectorService/VGSceneApplicator/WallEdgeVisibilityService/PlanViewCanvas elevation branch) + the Visibility Intents UI panel view-type modifier rendering; DISJOINT — FORBID the L-181 lifecycle files (PlatformShell/ProjectLoader/initScene/initBuilders), Cesium/solar, packages/auto-dimension, LinearDimPlanToolHandler). §ELEV-LINEWEIGHT. INVESTIGATION (evidence-first, quote actual schema/seed/code — no inference from behavior): (1) does the visibility-intent schema support `elevation` view-type modifiers at all, or is `elevation` simply missing from the seed that populates `plan`? (2) Trace where cut/beyond/hidden/projection is COMPUTED geometrically for elevation (classify each edge vs the elevation cut plane + frustum boundaries) — is it wired only for plan/section? (3) Compare plan's working render-time lookup (element type → view type → category → style) and check whether the SAME path is even invoked for elevation or short-circuits/defaults. (4) Explain the 'visible pattern' contradiction — since elevations show SOME consistent pattern with no UI modifiers, find its source (hardcoded default weight? fallback rule? per-element-type static styling bypassing the intent system?) and quote the exact code path. (5) REPORT before fixing: (a) does elevation modifier support exist in schema, (b) is the classification computed geometrically for elevations at all, (c) where the current visual output comes from, (d) the minimal changes to expose elevation modifiers in the UI like plan AND feed the geometric classification to those modifiers at render time. DEFINITIONS to validate against code intent: Cut = elevation plane intersects solid (thickest); Projection = pure projection onto view plane, in direct sightline, no cut (most of the facade); Beyond = geometry past the projection boundary, further from viewer, thinner solid; Hidden = occluded-but-useful, dashed thinnest. FIX SCOPE (only after report): add/populate elevation view-type modifiers (parity with plan door/window), ensure the cut-plane intersection test runs for elevation + buckets each element cut/projection/beyond/hidden, apply weight hierarchy cut>projection>beyond>hidden(dashed); test-first with the East Elevation fixture (most of facade = projection, any wall the plane cuts = cut, receding/occluded = beyond/hidden). DO NOT touch plan/section/ceiling-plan modifier behavior — scoped strictly to elevation. ALSO (cheap while in there): check whether `3d`/`detail`/`drafting`/`legend` view-types have the same empty-modifiers gap and REPORT (don't fix those unless trivial). Maps C06 §7 + DOC view rendering + the Visibility Intents/VG system; no shortcuts; P8.   ✅ FIXED 44313646 (pushed→main) — EVIDENCE: (a) schema DOES support elevation modifiers (ViewTypeModifier.viewType is free string; panel lists+filters elevation; IntentRuleResolver honors it) — it was purely a MISSING SEED (SystemIntents.ts seeded plan/3d/section/ceiling-plan/detail, zero for elevation). (b) cut/proj/beyond IS computed geometrically for elevation (EdgeProjectorService classifyByProjectionDepth; isSectionDepthView includes 'elevation'), BUT the elevation emit branch FOLDED cut into :proj (old §FIX-ELEVATION-POCHE/L-119) so the cut tier never rendered. (c) the flat pattern = the intent system running with an empty elevation profile + cut-fold (whole façade at single projection pen), NOT hardcoded. FIX: (1) added ELEVATION_MODIFIERS seed (wall/slab/roof/column cut ×1.5 + door/window projection 0.18mm) to both documentation intents; (2) un-folded the elevation emit so the depth-classified cut band routes to :cut (heavy). Ladder now cut 0.75-1.05 > projection 0.25 > beyond 0.13 dashed > hidden. L-119 poché no-regression verified (poché gated independently on resolveViewScope.poche=false for elevation → cut linework can't bring back the black façade). 8/8 new tests; tsc 0. VIEW-TYPE gap check: 3d + detail already have modifiers; drafting/legend still empty but arguably correct (_NON_TECHNICAL_SCOPE, no geometry through cut/proj pipeline) — flagged for founder's call (placeholder rows for UI consistency?). RESIDUAL: IFC elevation edge path (classifyByVertexY) untouched — follow-up if IFC-imported models need elevation cut emphasis. |
| L-183 | founder | **[CRITICAL — geospatial crash] The 3D GLOBE (Cesium photoreal tiles) view CRASHES when selected; founder also unsure the L-179 underground fix is confirmed** — after L-179 (bd158aa3) removed the ion World-Terrain path and switched globe placement to clamp against the Google Photorealistic 3D-Tiles surface via Cesium `clampToHeightMostDetailed`/`sampleHeightMostDetailed`, selecting "3D globe" now crashes. Prime suspect: a REGRESSION from L-179's new clamp path — those Cesium height APIs reject/throw if the tileset hasn't streamed/isn't ready, or if the scene/viewer is torn down mid-await; if that runs unguarded on globe activation it can crash the whole view. (Founder pasted only the boot log — backend=webgpu, renderer prewarmed — the actual crash stack is AFTER the '3D globe' click and NOT yet captured.) | Geospatial / Cesium globe activation + L-179 tile-clamp crash (C18-23, L-179, [[localhost-dev-unusable-test-on-prod]]) — possible REGRESSION from bd158aa3 | OPEN (CRITICAL) | routed → Cesium agent (lane free — L-179/178 shipped, L-177 deferred; DISJOINT from L-165 shadow/docs). NEED the crash stack (founder to paste console after clicking 3D globe). INVESTIGATE: the globe-activation path (GISAreaLayout '3D globe' launch → CesiumViewport photoreal/globe mode → renderRealModelOnGlobe → the L-179 clampToPhotorealTilesThenReplace / selectPhotorealTileBaseHeight / clampToHeightMostDetailed / sampleHeightMostDetailed). HARDEN: every Cesium height-sample/clamp await must be try/caught + guarded on (viewer not destroyed, tileset ready, scene alive) so a sample failure DEGRADES GRACEFULLY (fall back to tileset bounding-sphere ground or a safe base) instead of throwing and crashing the view — mirror the pre-L-179 resilience. Verify the underground fix still holds (building on tile surface) AND the view no longer crashes. MUST NOT re-enable ion terrain (L-142) or re-add CSP terrain hosts. Maps C18-23 + L-179 + L-142 lesson; no shortcuts; P8. §FIX-GLOBE-3DTILES-CRASH.   ✅ FIXED bf42add8 (pushed→main) — ROOT: clampToPhotorealTilesThenReplace is dispatched as a VOID-ed promise; when the Google 3D-tileset hasn't streamed a height, clampToHeightMostDetailed/sampleHeightMostDetailed REJECT → the L-179 try/catch `return`ed on a sample-throw but a throw OUTSIDE it (pre-sample ENU math / commitPhotorealBase re-place / reframe) rejected the void-ed promise → unhandled window 'error' → ViewportCrashGuard consecutive-throw escalation → SceneCrashFallback full reload = the intermittent crash. FIX: (a) pure safeSampleTileHeights runs each height API + NEVER rejects (empty array on throw) → not-ready throw falls through to sphere-ground fallback + retry, SELF-HEALS as tiles stream; (b) top-level try/catch wrapper so nothing the clamp does can produce an unhandled rejection (degrades to flat base 0). L-179 correct-location preserved; no ion terrain / no CSP host. 25/25 tests; tsc 0. Live-Cesium visual confirm still recommended. |
| L-184 | founder | **[MED — geospatial polish] 3D globe two refinements: (a) globe entry does NOT auto-frame the BUILDING — user must click "Zoom to Site"; (b) the house sits SLIGHTLY ABOVE the photoreal-tile ground (small float gap)** — GOOD NEWS FIRST: founder CONFIRMS the L-179 underground fix WORKS — the house is in the RIGHT LOCATION on the Sydney Google 3D-tiles (screenshot). Remaining polish: (a) on globe entry the log frames the SITE at `height 600m pitch -80°` (high overview) — the user must click "Zoom to Site" to reach the building; it should auto-frame/fly to the placed BUILDING on entry. (b) the model base sits a touch ABOVE the tile ground — the L-179 min-over-footprint+street-ring `sampleHeightMostDetailed` clamp is likely catching a slightly-HIGH sample (adjacent roof / tree canopy / tile-noise) so the base perches; seat it flush on the ground. | Geospatial / globe auto-frame-to-building + tile-clamp base accuracy (C18-23, L-179) | OPEN — FOLDED into the L-183 Cesium agent (same CesiumViewport globe path; do NOT dispatch a 2nd agent on the file) | (a) make globe entry reframe/fly to the placed BUILDING (reframeSiteIn3D / performInitialReframe / flyToBoundingSphere on the real model), not a 600 m site overview, so no manual "Zoom to Site" is needed. (b) tighten the tile-clamp so the base sits ON the ground: the min-over-samples can catch high features — use a ROBUST ground estimate (e.g. lowest percentile of a denser sample set / prefer ground-classified samples / small downward epsilon) so the model seats flush, not floating. Preserve the L-179 correct-location fix + the crash hardening; NO ion terrain / NO CSP terrain hosts. Maps C18-23 + L-179; no shortcuts; P8. §FIX-GLOBE-AUTOFRAME-AND-SEAT.   ✅ FIXED bf42add8 (pushed→main) — (a) auto-frame: globe placement ran frameCentroid:false + the immediate reframe framed flat base 0, and the async clamp settling the real ground never re-framed (hence manual "Zoom to Site"); new armGlobeReframeOnBaseSettle() arms the existing one-shot performInitialReframe on a fresh globe ENTRY (not fidelity flips) so base-settle re-frames the building. (b) seat flush: denser/wider street sampling (two 16-pt rings @ ext+25/+55m) recovers a truer ground min + a 0.3m downward seat epsilon (real pick only, never sphere fallback) so it seats flush without going underground. 25/25 tests; tsc 0. |
| L-185 | founder | **[MED — geospatial context feature] Add SEA / WATER context to the FORMA (3D Site) view — the Sydney site is right on the water (Rose Bay) but no sea is shown** — founder: "in the forma view, can you add sea context? we are really close to the sea but can't see it — bring water context graphical data." ROOT (log-evidenced): (1) Forma mode paints a NEUTRAL ground (`FORMA mode applied: neutral ground #DDDCD9`) + clamps to flat base 0 (`terrain clamp degraded — no real terrain provider (keyless ellipsoid ground)`), so the real coastline/satellite water is hidden; (2) the OSM context fetch (contextBuildings + parks/roads/water via /api/overpass) returns water only as closed `natural=water`/`waterway` polygons — the OPEN OCEAN/bay is `natural=coastline` WAYS, not a closed polygon, so a sea needs a coastline-derived water MASK, not a footprint fetch (earlier office log showed `context water: 0 area(s)`). | Geospatial / Forma-view water/sea context (C18-23, [[geospatial-foundation-strategic-direction]], the Forma context layer: contextBuildings/parks/roads/water) | ✅ FIXED 645495b3→main — `contextWater.ts` now fetches `natural=coastline` ways, stitches them (`stitchCoastlineWays`), clips to the site bbox (Liang–Barsky `clipPolylineToBbox`), and closes each crossing strand on the water side (right-hand OSM convention) into a closed **sea** ring (`buildSeaMaskFromCoastline`); CesiumViewport renders them as blue water surfaces beneath lakes/rivers/roads. 6 tests (both coastline orientations), tsc 0. §FEAT-FORMA-SEA-CONTEXT. | routed → Cesium/Forma-context agent (dispatch after L-183/L-184; FENCED to the OSM-context fetch module (contextBuildings.ts + any context-water sibling) + the Forma water-render in CesiumViewport.ts; DISJOINT). FIX: render sea/water context in the Forma view — fetch OSM `natural=water` + `waterway` polygons (bays/rivers, which ARE closed) AND build a SEA MASK from `natural=coastline` ways (or clip the site bbox against the coastline) so open water renders as a water surface/plane at ground level, styled to read as water (not the neutral grey). Reuse the existing /api/overpass proxy + the parks/roads context pattern (§FORMA-CTX-PARKS). Consider the GLOBE (photoreal) path already shows real water via satellite tiles — this is specifically the FORMA stylized path. Respect the device-loss budget + the A.24 Massing/site tier styling. Maps C18-23 + [[geospatial-foundation-strategic-direction]] + the Forma context system; no shortcuts; P8. §FEAT-FORMA-SEA-CONTEXT. |
| L-186 | founder | **[HIGH — geospatial state] On the 3D GLOBE the PRYZM-created house is LOST after globe → Forma "3D Site" → back to globe** — founder: first click "Zoom to Site" brings the user to the location (house visible); but after switching to the Forma "3D Site" view and then BACK to "3D globe", the house created in PRYZM is no longer there. So globe RE-ENTRY does not re-place/restore the REAL model — it's placed on first globe entry but dropped after a forma round-trip. | Geospatial / Cesium globe re-entry real-model re-placement (C18-23, L-179, view-switch state) | OPEN — FOLDED into the running L-183/L-184 Cesium agent (same CesiumViewport globe-activation/placement path; do NOT spawn a 2nd agent on the file) | ROOT HYPOTHESIS (verify): the real GLB is placed once via renderRealModelOnGlobe/§A.21.D49 on first globe entry; switching to forma places/positions the model for the forma study, and the `[gis][forma6] geometry unchanged — reusing placed real model (no re-export)` cache means globe re-entry REUSES a model that forma moved/hid (or that was removed on forma teardown) instead of re-placing it on the photoreal tiles. FIX: on EVERY globe activation, re-place (or restore + reposition) the real model on the photoreal tiles (re-run the clamp + placement, or re-attach the cached model at the correct globe ECEF transform) so the house is always present on globe re-entry — idempotent across globe⇄forma⇄globe switches. Preserve the L-179 clamp + L-184 auto-frame/seat. Add a test for the globe→forma→globe re-entry (model present + correctly placed). Maps C18-23 + L-179 + [[null-at-mount-runtime-event-race]]; no shortcuts; P8. §FIX-GLOBE-REENTRY-MODEL-LOST.   ✅ FIXED bf42add8 (pushed→main) — ROOT: the Forma "3D Site" study path renders massing with keepPhotoreal:false → clearRealModelOnGlobe() DESTROYS the globe model; on globe re-entry GISAreaLayout's perf cache (globeRealPlaced stale-true + unchanged signature) short-circuited without re-placing → empty globe (house lost). FIX: new hasRealModelOnGlobe() + pure shouldReuseGlobeRealModel() gate the cache reuse on the model being STILL LIVE — a model cleared by a forma round-trip is re-placed on re-entry; plain camera/metric switches still skip the re-export. 25/25 tests; tsc 0.   ⬆ RE-REPORTED STILL BROKEN (2026-07-08, founder + logs): building still lost on manual globe→forma→globe. Forma-agent root-cause (out of its CesiumViewport fence): `GISAreaLayout.ts` re-activation branch (lines 291–308, "Re-activating existing Cesium viewer") only re-loads via `loadBimGltf` GATED on `isBimPlacedOnEarth` — it does NOT re-run `renderRealModelOnGlobe` or re-frame `flyToBoundingSphere`; when the building was placed via the newer real-model path (not `placeBimOnEarth`), nothing re-places on re-entry. CesiumViewport `setVisible(false)` does NOT destroy the viewer/model (intact). → follow-up **L-193** (GISAreaLayout lane).   ✅ FIXED via L-193 3f132de9→main — caller-side complete: the direct nav-rail/onboarding re-entry (`toggleGIS`, gated on legacy `isBimPlacedOnEarth`) now also runs `restorePhotorealGlobeContent()` → re-places via the modern `placeBuildingOnGlobe` path + reframes on every globe entry, regardless of which path first placed it. Live-Cesium confirmation pending. |
| L-187 | founder (question) | **[LOW — geospatial enhancement, perf-gated] Extend the Forma (3D Site) CONTEXT-BUILDINGS radius ~5× — feasibility + perf?** — founder asks how easy + whether it compromises performance. ANSWER/ANALYSIS: the bbox is a trivial constant in the OSM-context fetch (contextBuildings), so the RADIUS change is easy; the PERF is the constraint. Current ~2 km box → 406 footprints (Sydney); 5× LINEAR = 25× AREA → ~8-10k+ footprints in a dense city → (a) Overpass fetch ~25× heavier (multi-sec + proxy load), (b) Cesium renders each as an EXTRUDED PRISM with soft shadows 2048@600m → 10k shadow-casters drops FPS + inflates the shadow pass. 5× AREA (≈2.2× radius) ≈ ~2k buildings = manageable-but-noticeable. | Geospatial / Forma context-building radius + LOD (C18-23, A.24, [[geospatial-foundation-strategic-direction]], contextBuildings + CesiumViewport forma render) | ✅ APPROVED by founder (2026-07-08 — "do that: extend radius + distance-LOD + count-cap + progressive fetch") — MED; QUEUED, bundle with the Forma-context Cesium work (dispatch after the L-183/184/186 agent frees CesiumViewport.ts) | routed → Cesium/Forma-context agent (bundle with L-185 sea + L-177 façade when that lane frees). BUILD THE LOD APPROACH (founder-confirmed): extend the context radius WITH distance-based LOD — near ring = extruded + shadows (as now), far ring = flat/low-poly footprints with shadows OFF — + a building-count CAP (fetch/keep N nearest) + progressive async fetch, so the fetch + shadow + geometry budget stay bounded. ARCHITECTURALLY-SOUND APPROACH (don't just 5× the constant): extend the radius WITH distance-based LOD — near ring = extruded + shadows (as now), far ring = flat/low-poly footprints, shadows OFF; + a building-count CAP (fetch/keep N nearest) + progressive async fetch so the fetch + shadow + geometry budget stay bounded (never regress the device-loss/A.24 budget). Reuse the existing /api/overpass proxy + the FORMA context render. Maps C18-23 + A.24 + [[geospatial-foundation-strategic-direction]]; no shortcuts; P8. §FEAT-FORMA-CONTEXT-EXTENT-LOD.   ✅ FIXED 645495b3→main — `contextBuildings.ts` gains `fetchContextBuildingsFarRing` + pure `selectFarRingFootprints`/`ringCentroidLonLat`: a wider annulus (0.016°) fetched PROGRESSIVELY after the near ring, excluding the inner disc, nearest-N capped (900), tagged `ring:'far'`. CesiumViewport renders the far ring as flat/low-poly blocks with shadows OFF (height cap 24 m, no outline); near ring stays extruded + shadowed. Bounds the shadow/geometry budget (no naïve-radius cliff). 4 tests, tsc 0. |
| L-188 | founder | **[CRITICAL — data loss] A project created WITH GIS data (Cesium 3D globe + 3D site + site location) LOSES its GIS/site data on close+reopen — the BIM loads but the site location/boundary/globe context is GONE and defaults to Madrid** — founder: "create a project with GIS data, close it, open PRYZM again, open the project → it loads but the 3D data is gone, it goes default." 🔎 LOG-CONFIRMED ROOT: on reopen the BIM restores (4 walls/1 slab/1 roof/1 room, 8 annotations, semantic graph) BUT (1) `[ProjectLoader] Hierarchy store restored from snapshot (0 nodes)` — the SITE hierarchy node (which holds the location) was NEVER serialized; (2) `[RealSunService] Enabled — lat: 40.4168 lng: -3.7038` = the site SILENTLY DEFAULTED TO MADRID instead of Sydney (-33.87). So the SITE LOCATION (lat/lon), the parcel BOUNDARY, and the C19 GEOSPATIAL ORIGIN (LTPENURebase) are NOT part of the persisted project snapshot / not restored on load. (Also: server `/api/projects/…/latest-version` 404 → fell back to the local IndexedDB Auto-save, which carries BIM but no GIS.) | Persistence / project snapshot must include + restore the GIS site state (C19 geospatial, ProjectSerializer/ProjectLoader, Site/hierarchy store, LTPENURebase origin) | OPEN (CRITICAL) | routed → persistence/geospatial agent (DISJOINT from the Cesium-render lane — this is the SITE STATE serialization, not CesiumViewport rendering). INVESTIGATE: what onboarding creates for a GIS project (site.created → hierarchy.createSite; LTPENURebase.setOrigin LAT/LON; site.parcel-boundary-set) and WHY none of it survives the ProjectSerializer snapshot (Hierarchy store serialized 0 nodes; is the site a persistent hierarchy node? where is the site lat/lon + C19 origin + boundary stored, and is that store serialized at all?). FIX: persist the full GIS/site state — site location lat/lon, parcel boundary, the C19 geospatial origin/anchor, and the site hierarchy node(s) — in the project snapshot (ProjectSerializer) AND restore them in ProjectLoader, so reopening a GIS project restores its REAL location + boundary + globe/site context (not the Madrid default). On restore, re-seed LTPENURebase + RealSunService site + the Cesium/GIS launch state from the persisted location. Add a test: create project with site @ known lat/lon + boundary → serialize → deserialize → site location/boundary/origin restored (not defaulted). Maps C19 geospatial + the persistence contract + [[geospatial-foundation-strategic-direction]] + [[onboarding-site-generate-view-flow]]; no shortcuts; P8. §FIX-GIS-SITE-STATE-NOT-PERSISTED.   ✅ FIXED 37cd943d (pushed→main) — ROOT CONFIRMED: the C19 SiteModel (site lat/lon, parcel boundary, geospatial origin, footprint, context) lives ONLY in the per-runtime SiteModelStore (packages/stores/src/SiteModelStore.ts, runtime.siteModelStore); ProjectSerializer never read it + ProjectLoader never restored it (the site is a SEPARATE store, NOT a hierarchy node — hence 'Hierarchy store restored 0 nodes' while BIM restored fine) → on reopen readSiteLatLon()=null → sun/globe fell back to Madrid default. FIX: (1) ProjectSerializer adds site?:SiteModel to the snapshot (structuredClone of siteModelStore.getSite()); (2) initPersistence threads runtime.siteModelStore into the serialize bundle; (3) new siteDispatch.restoreSiteState() (OTel span, P8) hydrates the store + force re-seeds the LTP-ENU origin (setLtpOriginForce, C19 §1.3) + re-emits site.created→location-changed→parcel-boundary-set so RealEnvironmentService sun + CesiumViewport + ParcelBoundarySceneRenderer re-anchor; resets on no-site (C13 isolation); (4) ProjectLoader calls it after hierarchy restore (both import + legacy paths); (5) SnapshotStreaming header/split/merge carries `site` so it survives the streaming round-trip. Server + IndexedDB carry the same snapshot blob (ServerSyncQueue transmits verbatim) — no server follow-up; the /latest-version 404 is unrelated networking. 3/3 new (Sydney round-trip asserts NOT Madrid + events re-emitted + null-isolation) + 11/11 ProjectLoader tests; tsc 0 (the only tree error is the Forma agent's uncommitted CesiumViewport WIP, not this commit). NOTE (out-of-fence, pre-existing): the parallel packages/persistence-client ProjectSerializer duplicate (DataSheet/Portfolio) is separately lossy for ifcElementMeta/userMaterials — future consolidation, not this bug.   🏛️ COMPLIANCE PREREQUISITE (tagged 2026-07-18, fix-status unchanged): this is a HARD PREREQUISITE for the Pipeline A compliance-authoring pillar (L-398–L-402) — parcel / zoning / buildable-envelope state (site location, parcel boundary, the C19 geospatial origin, and any persisted zoning/envelope) MUST survive project close+reopen or the compliance loop silently loses its inputs on every reopen. Cross-link: L-398/L-400/L-401, C19 §1.4/§1.6, C57. NOTE: the reverted-checksum reality (L-334/L-360) means there is no integrity guard on this persisted site state either — a further reason the compliance pillar depends on this staying durable. |
| L-189 | orchestrator (log) | **[HIGH — possible L-165 regression, device-loss] `Destroyed texture [Texture "ShadowDepthTexture"] used in a submit` ×8 on PROJECT OPEN — appears now that L-165's 1024px shadow map is live** — the exact WebGPU device-loss signature ([[webgpu-heavy-scene-crash-and-instancing]] §SHADOW-DEVICE-LOSS): a shadow-depth texture is destroyed while a GPU submit referencing it is still in flight. It fires at project-switch, where the log shows `ShadowQualityUpgrader Level changed to "high"` (a tier transition → shadow-map REALLOC). Suspicion: L-165's 512→1024 `standard`-tier bump (fa948ff4) makes the project-switch shadow realloc destroy the old ShadowDepthTexture mid-submit — an UNGUARDED realloc the L-165 spike believed was covered by the deferred-thaw/deferReleaseShadowMap path. | Rendering / shadow-map realloc device-loss on project-switch tier change (C04, L-165, ADR-0111) — possible REGRESSION | OPEN | routed → rendering-shadow agent (lane free — L-165 shipped; DISJOINT from L-188 persistence + the Cesium lane). VERIFY: is the destroyed-texture-in-submit NEW since L-165 (compare the realloc path at 512 vs 1024 on the project-switch "standard→high" tier change) or pre-existing? If L-165-caused: ensure the shadow-map realloc on a project-switch tier transition defers the OLD ShadowDepthTexture dispose until after the in-flight submit completes (mirror the RenderingPipelineCoordinator freeze→mutate→deferred-thaw + _deferReleaseShadowMap that L-165 relied on) so no texture is destroyed mid-submit; if it can't be made safe quickly, REVERT the 1024 bump to 512 (keep the spike doc). MUST NOT regress the >8000-mesh survival tier / 40-storey budget. Add a regression test for the realloc-during-active-submit guard. Maps C04 + L-165 + ADR-0111 + [[webgpu-heavy-scene-crash-and-instancing]] + [[null-at-mount-runtime-event-race]]; no shortcuts; P8. §FIX-SHADOW-REALLOC-DEVICE-LOSS-PROJECT-SWITCH.   ✅ REVERTED e5709cdd (pushed→main) — founder ESCALATED "ground shadow on the invisible layer is GONE, need it back". The L-165 512→1024 `standard` bump widened the device-loss window: at project-open the startup applies `standard` (now 1024) then the scene-tier bumps to `high` (2048) — the extra 1024 realloc destroys the old ShadowDepthTexture mid-submit → shadow pass produces nothing → ground catcher shadow disappears. FIX: reverted standard back to 512px/radius 1 (known-good pre-L-165). ⚠ IF THE GROUND SHADOW IS STILL GONE after this deploy, the root is SEPARATE (candidates: the L-171 residual BatchCoordinator shadow-suppress leave-off — shadowMap.enabled stuck false after a batch/generation; OR a GroundShadowCatcher WebGPU receive regression) → dispatch a focused shadow root-cause agent next. The guarded-1024 re-attempt (defer old-texture dispose past in-flight submit) is deferred until device-loss-safe.   ✅ AGENT VERDICT (confirmed): IS an L-165 regression; 512 revert is CORRECT + SUFFICIENT for the reported loss. EXACT SITE: ShadowQualityUpgrader._deferReleaseShadowMap() (line ~138) via setLevel(standard→high) on the project-switch tier realloc — it nulls sh.map + disposes the OLD ShadowDepthTexture on a single setTimeout(0); the renderer freeze (shadowMap.autoUpdate=false) stops THREE's own realloc but NOT the upgrader's explicit .dispose(); one macrotask doesn't guarantee the pre-freeze frame's GPU submit (still referencing the old texture) has drained — the 1024 map's submit drained slower than the deferral → destroyed mid-submit → shadow pass invalidated → GroundShadowCatcher (receiveShadow=true) has nothing to receive → ground shadow gone. 512 restores the timing margin (small old map drains before the setTimeout fires). Ruled out the BatchCoordinator §BATCH-SHADOW-MAP-SUPPRESS path (distinct, not L-165, wouldn't emit the destroyed-texture signature). Regression TEST committed 1aeca146 (pins standard===512/radius1 + asserts setLevel defers old-texture dispose; 5/5). RESIDUAL (future, out-of-fence): the setTimeout(0) deferral is not PROVABLY safe even at 512 — the provable fix is a GPU-fence-gated dispose (device.queue.onSubmittedWorkDone().then(dispose)) in renderer-three/initScene; that is the prerequisite before ANY future re-bump to 1024. §FIX-SHADOW-DISPOSE-GPU-FENCE (future). |
| L-190 | founder | **[HIGH — elevation pipeline, continuation of L-182 §ELEV-LINEWEIGHT-02] Two elevation defects: (A) WINDOWS absent from elevation geometry (wall renders as a plain solid rectangle, no window openings/sills/frames — but plan shows the windows); (B) a SET-BACK wall (behind the front façade plane, occluded) renders SOLID instead of HIDDEN/dashed** — founder (South Elevation vs Ground Floor plan, red arrows): (A) the selected wall run has windows in plan (Room 00-001/002) but the elevation shows a blank solid rectangle → window geometry appears to not enter the elevation view's element set at all (filter-stage exclusion, or no elevation-facing window profile, or window category has no elevation modifier so it renders invisible). (B) the arrowed wall is an interior wall set back from the façade — fully occluded to a front viewer — yet renders continuous solid; it should classify as `hidden` → dashed. | Documentation / elevation element query (geometry inclusion) + occlusion classification (C06 §7, DOC-1.x, EdgeProjectorService, NativeElementMeshExporter, VG intents; continuation of L-182) | ✅ FIXED 19f12ba5→main — SEPARATE roots from L-182 (that was style/classification; these are downstream). BUG A = QUERY/FILTER: windows have real geometry + register a scene root, but are only added to `Level.childrenIds` by `CreateWallOpeningCommand`; on reload/batch/generated typologies the opening is rebuilt via `WallStore.repopulateFromWallData` (no registerElement) → window absent from childrenIds → absent from elevation (plan still shows them because `WindowPlanSymbolBuilder` reads the wall/window store directly). FIX: elevation/section/3D branch of `NativeElementMeshExporter` now unions every hosted-opening root from `elementRegistry.getAllRoots()` filtered by window/door type, deduped vs childrenIds. BUG B = missing OCCLUSION stage: `classifyByProjectionDepth` buckets by absolute depth only (no occlusion test) so a set-back wall <12 m → `:proj` → solid; and `removeHiddenLines` builds occluders only from `:cut` (an outside elevation mark cuts nothing → HLR no-ops). FIX: new `reclassifyOccludedElevationLines` — EdgeProjectorService stamps per-element `elevationDepth`; pass moves any `:proj` segment fully occluded by a nearer element onto its `:beyond` (dashed beyond-pen) layer; flush windows (same depth) stay solid. Scoped to elevation only; plan/section untouched. 4/4 new + 8/8 L-182 tests, tsc 0. Residual: IFC-scene elevation path not yet fed the depth stamps (native PRYZM elements fully covered). §ELEV-LINEWEIGHT-02. | routed → documentation/elevation agent (lane free — L-182 shipped; DISJOINT from the Forma-Cesium lane, the L-188 persistence lane, and the shadow lane). EVIDENCE-FIRST — REPORT which pipeline STAGE fails (query/filter, geometry-generation, classification, or style) with exact code refs BEFORE fixing, and state whether A/B share the L-182 root (elevation bypasses the plan classification path) or are separate. BUG A: confirm whether window elements are in the elevation element query (compare to plan's include-windows path); find exactly where windows drop out (filter excludes window type from elevation query? window has no elevation-facing projection geometry? window classifies into a category with no elevation modifier → silently invisible vs default). BUG B: quote the arrowed wall's actual depth vs the cut plane + nearest projection boundary; trace the occlusion test that should bucket `hidden` (occluded) vs `beyond` (visible, further) vs `projection` — is it running for WALLS at all, or does anything without an explicit classification default to solid/projection? FIX (after report): (1) include window elements in the elevation element query + projection geometry with correct open/frame/sill representation; (2) fix occlusion classification so ANY element behind the nearest visible projection/beyond plane buckets as `hidden` → dashed, regardless of element type (wall/window/door). Test fixture on THIS South Elevation wall run: windows appear + the set-back wall renders dashed not solid. DO NOT touch plan/section styling — strictly elevation geometry inclusion + occlusion. Maps C06 §7 + DOC-1.x + the elevation projection pipeline + L-182; no shortcuts; P8. §ELEV-LINEWEIGHT-02. |
| L-191 | founder | **[HIGH — AutoDimension, PRIORITISE] Perimeter dimensions must ALWAYS be placed OUTSIDE the perimeter shell (outward) — the BOTTOM dims are perfect (below/outside) but the TOP and LEFT perimeter dims are NOT pushed outward** — founder (2 plan screenshots): "the dimensions are already great, but the top and left should be done from OUTSIDE of the wall — all the perimeter dimension should be done from outside of the perimeter shell, ALWAYS outwards!" So the perimeter dim chains render correctly on the bottom edge (outward = down) but on the top edge (outward should be UP/above the building) and the left edge (outward should be LEFT of the building) they land on/inside the shell instead of outside. | Documentation / auto-dim perimeter chain OUTWARD side selection (C56, DOC-2.x, L-155 world-standoff) | ✅ FIXED 081140a2→main — ROOT: engine chose each string's `side` on a world-**+axis** convention, but the plan renderer applies `offset` along `leftPerp(measurementDir)`. Horizontal runs → `leftPerp(+X)=+Z` agreed (bottom/top were already correct); vertical runs → `leftPerp(+Z)=−X` inverted → left/right chains rendered inward on/inside the shell. FIX (placement.ts): new `measurementDir()` helper; `side` chosen so `leftPerp(measDir)·side == outwardNormal` (perpendicular away from shell centroid) → all N cardinal runs provably outward; conflicts.ts crossing-scan uses the same `outwardNormal` to match where the renderer draws. Tests corrected to the renderer's `leftPerp` convention + new L-191 block (rect: 4 sides outward & none inside AABB; L-shell + notched-corner: every chain strictly farther from centroid than its wall). 34/34 pass; tsc clean (only error = Forma's uncommitted CesiumViewport WIP). Renderer untouched. §FIX-AUTODIM-PERIMETER-ALWAYS-OUTWARD | routed → AutoDim engine agent (lane free — L-180 shipped; FENCED to packages/auto-dimension/** (perimeter.ts + planners.ts + placement.ts + planAutoDimensions.ts + geometry.ts) + apps/editor/src/ui/documentation/applyAutoDimensions.ts; DISJOINT from L-190 elevation (EdgeProjectorService), the Forma-Cesium lane, the L-188 persistence lane). ROOT (verify): the perimeter chain SIDE selection places the chain + its L-155 world offset on the wrong side for the top/left runs — it must place EVERY perimeter run's chain on the OUTWARD side = the direction AWAY from the building interior/centroid (bottom run→below, top run→above, left run→left, right run→right). Likely the outward-normal sign is only correct for the near/bottom side, or the offset is applied toward the centroid on some runs. FIX: for each perimeter run compute the outward normal (perpendicular to the run, pointing away from the shell centroid/interior) and place the dimension chain + its standoff on that outward side, so ALL four (all N) perimeter chains sit outside the shell, never on/inside it. Reuse the L-155 world-standoff. Keep P6/pure-engine. Add a test: a rectangular (and L) shell → all perimeter chains are outside the shell bbox on their outward side. Maps C56 + DOC-2.x + L-155/L-147; no shortcuts; P8. §FIX-AUTODIM-PERIMETER-ALWAYS-OUTWARD. |
| L-192 | orchestrator (log) | ✅ **SUPERSEDED + FIXED** by L-195 (`setLevel` never reallocs) + L-200 `507c12d7` (`apply()` never reallocs/disposes a LIVE caster's shadow map — the true destroy source). The founder's post-deploy logs show NO `Destroyed texture ShadowDepthTexture` through the cinematic-tier transition. The GPU-fence dispose is therefore no longer a prerequisite for a sharper map, though it remains the principled long-term guard if a live realloc is ever reintroduced. ORIGINAL: **[HIGH — device-loss residual, confirmed] `Destroyed texture ShadowDepthTexture used in a submit` STILL fires (×21) on project-open even at 512px — the L-189 revert mitigated but did NOT eliminate the device-loss** — on opening a 38-wall project the tier realloc (standard 512→high 2048, `Level changed to "high"`) still disposes the old ShadowDepthTexture on a `setTimeout(0)` that doesn't guarantee the in-flight submit has drained → ×21 destroyed-texture-in-submit. Ground shadow currently survives at 512 (timing margin restored), but this is a live WebGPU device-loss risk that could cascade to a white-screen; it is exactly the residual the L-189 agent flagged. | Rendering / shadow-map dispose GPU-fence (C04, ADR-0111, L-189, [[webgpu-heavy-scene-crash-and-instancing]]) | OPEN (queued — real fix, prerequisite before any 1024 re-attempt) | routed → renderer/shadow agent (when capacity; FENCED to the shadow-dispose path — packages/core-app-model/src/rendering/ShadowQualityUpgrader._deferReleaseShadowMap + the renderer-three/initScene GPU-fence hook + RenderingPipelineCoordinator._reallocShadow). FIX: replace the setTimeout(0)-deferred old-ShadowDepthTexture dispose with a GPU-FENCE-GATED dispose — `device.queue.onSubmittedWorkDone().then(() => oldTexture.dispose())` (WebGPU) / the WebGL equivalent — so the old texture is only freed AFTER the submit that references it has provably completed; NEVER mid-submit. This makes the tier realloc device-loss-PROOF at any map size (and is the prerequisite before re-attempting the L-165 1024 accuracy bump). Respect L-139/L-150 survival tier. Add a test/guard. Maps C04 + ADR-0111 + L-189 + [[webgpu-heavy-scene-crash-and-instancing]]; no shortcuts; P8. §FIX-SHADOW-DISPOSE-GPU-FENCE. |
| L-193 | derived (Forma-agent root-cause of L-177 + L-186) | **[HIGH — geospatial orchestration, unblocks two founder-visible bugs] The Forma "3D Site" base building renders as MASSING not the real GLB, and the globe loses the building on manual view re-entry — BOTH root into `GISAreaLayout.ts` never (re-)running the real-model placement** — (A) Forma path: console `[CesiumViewport][forma] massing rendered: 58 wall(s) … single 4-vertex perimeter prism` proves `renderRealModelOnForma` never runs (on success it would call `clearFormaMassingEntitiesOnly`) → founder sees the prism + the L-177 sun-hours drape (already shipped 645495b3) has no real model to land on, so it falls back to the massing envelope. Placement is orchestrated by `placeRealModelOnForma` (~GISAreaLayout.ts:1874). (B) Globe re-entry: the re-activation branch (GISAreaLayout.ts:291–308, "Re-activating existing Cesium viewer") only re-loads via `loadBimGltf` gated on `isBimPlacedOnEarth` and does NOT re-run `renderRealModelOnGlobe`/`flyToBoundingSphere`; when placed via the newer real-model path (not `placeBimOnEarth`) nothing re-places → house lost (the L-186 bf42add8 CesiumViewport-side cache-liveness gate was necessary but not sufficient — the caller never triggers re-placement). | Geospatial / GISAreaLayout real-model placement orchestration (C18-23, A.24, L-177/L-186, [[geospatial-foundation-strategic-direction]]) | OPEN (HIGH — unblocks L-177 façade-on-real + L-186 re-entry) | route → GISAreaLayout agent (FENCE: apps/editor/src/ui/geospatial/GISAreaLayout.ts + any pure placement-decision sibling; DISJOINT from CesiumViewport.ts internals which the Forma agent owns — call the existing `renderRealModelOnForma`/`renderRealModelOnGlobe` entry points, do not re-implement them). FIX: (A) ensure the Forma "3D Site" activation actually invokes `placeRealModelOnForma`/`renderRealModelOnForma` with fidelity='real' (so the real GLB replaces the massing prism, then `clearFormaMassingEntitiesOnly` runs) — the L-177 drape then lands on the real house; (B) make the globe re-activation branch idempotently re-run real-model placement + reframe on EVERY globe entry regardless of which path first placed it (not gated on `isBimPlacedOnEarth` alone). Add tests: forma activation → real model present (not massing); globe→forma→globe → model present + framed. Verify live (Cesium). Maps C18-23 + A.24 + L-177/L-186; no shortcuts; P8. §FIX-GISLAYOUT-PLACE-REAL-MODEL-FORMA-AND-GLOBE-REENTRY.   ✅ FIXED 3f132de9→main (⚠ needs live-Cesium confirmation) — deeper root than first thought: BOTH symptoms are a view-switch destroying a Cesium primitive while a caller-side "already placed" cache flag stays stale-true → re-entry short-circuits to an empty/massing view. (A) Forma: `placeRealModelOnForma` DOES run on first entry (default fidelity='real'), but visiting the photoreal globe runs `restorePhotorealMode()` → `clearRealModelOnForma()` which DESTROYS the Forma real-model primitive, and the Forma cache (`formaRealPlaced`/`formaRealLastSig`) was never invalidated (the L-186 liveness fix was globe-only; CesiumViewport exposes no `hasRealModelOnForma`) → on globe→forma the reuse guard returned 'reuse-placed' for a destroyed model → massing fallback stayed. (B) globe DIRECT re-entry (nav-rail GIS button / onboarding `pryzmToggleGIS`, not the result-toggle bar): the `toggleGIS` re-activation branch only re-synced via `loadBimGltf` gated on `isBimPlacedOnEarth` (set only by the LEGACY `placeBimOnEarth`), so buildings placed via the modern `placeBuildingOnGlobe` path re-placed NOTHING. FIX: new pure sibling `apps/editor/src/ui/geospatial/globePlacementDecisions.ts` (`decideGlobeReactivationAction`/`decideFormaRealPlacement`/`invalidateFormaRealCacheOnPhotorealGlobeEntry`) + GISAreaLayout `restorePhotorealGlobeContent()` (setFormaMode(false) → invalidate Forma real cache → arm L-184 reframe → placeBuildingOnGlobe (L-186 liveness + L-179 clamp preserved) → reframeSiteIn3D); re-activation branch now adds restore for modern re-entry; result-view/forma-engage set a self-place-suppress flag so they don't double-place. Idempotent across globe⇄forma⇄globe; no ion terrain re-enabled. 10/10 new tests (incl. round-trip re-place) + 29/29 existing globe tests; tsc 0. Unblocks L-177 (drape now lands on the re-placed real house). |
| L-194 | founder | **[HIGH — onboarding "Overlay a plan / PDF" flow, PRIORITISE] Three defects in the STEP-2 plot overlay-import flow** — founder (6 screenshots, live test): (1) **DOUBLE PANELS / no map-zoom** — the overlay-a-plan path still has two panels; the "first" path drops the imported PDF/JPG straight into the PLAN (BIM canvas) view with NO map to zoom into, instead of onto the zoomable MAP. Remove the first panel/path so the overlay ALWAYS lands on the map where the user can zoom + position (screenshots 3/4 = the correct map-based placement panel). (2) **Boundary-draw must be DISABLED in the overlay path** — after the user imports + places the plan on the map, the flow STILL runs the boundary-draw tool (bottom bar "Click each corner · double-click or Enter to close", and screenshot 5 shows draw dims 4.0/8.4 m + a "Generate your house with AI?" confirm firing). Once a plan is imported+placed, the plot boundary requirement should be DISABLED — the imported plan IS the reference; no corner-by-corner trace, no generate-confirm. (3) **"Finish" must auto-open the 3D SPLIT view ready to draw** — clicking "✓ Finish — enter canvas" / "Finished — update & re-enter canvas" (screenshot 6) should bring the user straight into the 3D split view (plan + 3D) ready to continue drawing walls over the imported plan; at the moment it does not (lands in Plan/Top only, or doesn't transition cleanly). | Onboarding / site-plan-overlay import flow (ADR-0259 site-plan-overlay, SPEC-SITE-PLAN-OVERLAY, C19 geospatial, O.2 onboarding; OnboardingStepController.startOverlayImport/landInCanvasWithUnderlay §FIX-SITE-OVERLAY-* L-70/L-77/L-78, SitePlanOverlayController, SiteBoundaryMap2D) | OPEN (PRIORITISE) | route → onboarding/site-overlay agent (lane free; FENCE: apps/editor/src/ui/onboarding/OnboardingStepController.ts + apps/editor/src/ui/site/overlay/SitePlanOverlayController.ts + apps/editor/src/ui/geospatial/SiteBoundaryMap2D.ts (overlay-only-mode gate) + their __tests__; DISJOINT from GISAreaLayout real-model lanes). EVIDENCE-FIRST — REPORT what "double panels"/"first path" is in the current code (is there a straight-to-plan import path separate from the map path? which panel is redundant?) BEFORE fixing. FIX: (1) single map-based overlay path with zoom (remove/redirect any straight-to-plan-canvas import so the plan always goes onto the zoomable map); (2) in overlay-only mode, SUPPRESS the SiteBoundaryMap2D corner-by-corner boundary-draw tool + the generate-confirm (the overlay path is terminal — §FIX-SITE-OVERLAY-IMPORT-TERMINAL already intends "NO boundary trace, NO auto-generate" but the draw tool is still armed on the map → gate it off when overlay-only); (3) `landInCanvasWithUnderlay` must land in the 3D SPLIT view (plan + 3D) via the SplitViewManager entry point, not Plan(Top) alone, with the underlay visible + framed, ready to draw. Align with ADR-0259 + SPEC-SITE-PLAN-OVERLAY; keep P6; no shortcuts; P8. §FIX-ONBOARDING-OVERLAY-SINGLE-PANEL-NO-BOUNDARY-SPLIT3D.   ✅ FIXED 6a0cc801→main — evidence: there was NO separate straight-to-plan import path (overlay card → only `startOverlayImport` → map path via `pryzmStartSitePlanOverlayImport`→`startBoundaryDraw({overlayOnly:true})`); the "two panels" was the pre-L-77 choice-card+overlay-panel overlap (already dismissed) + the map's redundant close chrome (now hidden in overlay-only). Defect 2 root: `SiteBoundaryMap2D.onClick` early-returned on `overlayOnly` but the draw handlers were still ATTACHED (map.on('load') block) + Enter/mode keys still processed → machinery live. Defect 3 root: `landInCanvasWithUnderlay` called `pryzmActivateBimView('Top')` only. FIX: (1) overlay-only mode attaches NO draw handler + inerts Enter/mode keys + hides the map ✕; (2) Finish now opens the plan+3D split via `window.splitViewManager.activate()` (idempotent) then zoom-to-fit. 24/24 tests; tsc 0. §FIX-ONBOARDING-OVERLAY-SINGLE-PANEL-NO-BOUNDARY-SPLIT3D. |
| L-195 | founder | **[CRITICAL — render regression, "WORKING BEFORE"] Ground shadows on the invisible ground-catcher STILL not rendering — device-loss regression** — founder (repeat, furious): the soft ground shadow on the invisible ground plane is gone; "WHY THIS REGRESSION?" Console (two fresh projects) shows the exact sequence: `ShadowQualityUpgrader Applied "standard" — map: 512px` → `SceneQualityTier 71 meshes → tier=cinematic (shadows=high)` → `ShadowQualityUpgrader Level changed to "high"` → `Destroyed texture [ShadowDepthTexture] used in a submit`. | Rendering / ShadowQualityUpgrader.setLevel shadow-map realloc device-loss (C04, ADR-0111 shadow-freeze, L-165/L-189/L-192, [[webgpu-heavy-scene-crash-and-instancing]], [[null-at-mount-runtime-event-race]]) | ✅ FIXED 01465ee2→main (⚠ needs founder live-confirm) — ROOT: on every small/new scene SceneQualityTier's `cinematic` tier sets shadows=`high`, so `setLevel('high')` GREW the shadow map 512→2048 (`sh.mapSize.set(2048,2048)`); THREE reallocates the ShadowDepthTexture and the OLD texture is disposed while the in-flight WebGPU submit still references it → "Destroyed texture ShadowDepthTexture used in a submit" → device-loss on the shadow pass → the GroundShadowCatcher receives nothing → ground shadow disappears. L-189's deferred (single `setTimeout(0)`) dispose did NOT guarantee the submit had drained, so it still fired. The `standard` config's own comment already forbids growing above 512 without a GPU fence — `setLevel`'s 2048 grow was exactly that un-fenced realloc. FIX: a LIVE tier change no longer touches `sh.mapSize` or `sh.map` at all — it keeps apply()'s device-safe allocation and tunes only the NON-reallocating params (radius/bias/normalBias), so no texture is ever destroyed mid-submit and the ground shadow survives; high/ultra still soften via PCF `radius`. Map growth behind a GPU fence remains the future L-192. Test rewritten to the new no-realloc contract (5/5 pass); tsc 0. §FIX-SHADOW-GROUND-REGRESSION.   ⚠ INSUFFICIENT — shipped but the founder STILL sees no ground shadow + the `Destroyed texture ShadowDepthTexture used in a submit` device-loss STILL fires (log confirms this fix IS deployed). CORRECTION: `ShadowQualityUpgrader` is bound to the **OBC WebGL** renderer (silenced in Phase 5; RenderingPipelineCoordinator.ts:656-660) — it never touches the live **WebGPU** Pascal key light that actually draws the ground shadow. L-189 + L-195 both fixed the WRONG renderer. The real fix is on the WebGPU shadow path (RealSunService/RealEnvironmentService/PascalSceneLighting/GroundShadowCatcher) → tracked as **L-197**. §FIX-SHADOW-GROUND-REGRESSION. |
| L-196 | founder | **[HIGH — elevation occlusion, continuation of L-190 §ELEV-LINEWEIGHT-03] Set-back wall must be PARTIALLY hidden — a single wall edge is occluded along PART of its length (per-segment), not whole-element** — founder (South Elevation, red mark on the selected wall + plan red arrow): the highlighted wall run is occluded by the nearer L-shaped massing for PART of its length and genuinely visible for the rest, but it renders as one continuous SOLID line end-to-end. The hidden portion should render DASHED and the visible portion SOLID, with the transition at the massing's step-back point. L-190's `reclassifyOccludedElevationLines` only re-buckets a `:proj` segment when it is FULLY occluded by a nearer element's 2D silhouette AABB (v1, Contract 23 §9 limitation the L-190 agent explicitly flagged) → a long edge partially covered is never split, so it stays fully solid. | Documentation / elevation per-segment (partial) occlusion — edge splitting at occlusion-state transitions + true occluder silhouette (C06 §7 / Contract 23 §9, DOC-1.x, HiddenLineRemoval.reclassifyOccludedElevationLines, EdgeProjectorService; continuation of L-182/L-190) | ✅ FIXED cebd81e2→main — ROOT: L-190's Pass 2 was per-segment but WHOLE-COVERAGE-ONLY (Cohen-Sutherland trivial-accept: re-buckets a segment only when BOTH endpoints lie in the occluder AABB), and the occluder was a coarse per-element AABB — so a partially-covered far edge never split → stayed solid; occluder (L-massing) was present with correct depth (gap was the test, not absence); systemic across every L/stepped/recessed massing. FIX: Pass 1 also collects each occluder's TRUE projected silhouette; new `splitSegmentByOccluders` splits each `:proj` edge at exact silhouette crossings, midpoint-classifies each sub-interval (visible→solid `:proj`, occluded by strictly-nearer→dashed `:beyond`), merges spans; true silhouette preferred, AABB fallback logged (no silent cap). Interior openings (window rect in wall) correctly non-occluding. 4/4 tests (incl L-notch: hidden span ends at real step-back x=2 not AABB x=4); tsc 0. Residual: IFC-scene elevation path not fed depth stamps (native elements covered). §ELEV-LINEWEIGHT-03. | route → documentation/elevation agent (lane free — L-190 shipped; FENCE: packages/core-app-model/src/drawing/HiddenLineRemoval.ts + apps/editor/src/engine/views/EdgeProjectorService.ts (depth-stamp only) + their __tests__; DISJOINT from the dimension-selection lane, the onboarding-overlay lane, the shadow lane). EVIDENCE-FIRST — REPORT whether the current occlusion verdict is per-ELEMENT or per-SEGMENT and confirm the occluder (nearer L-massing) is in the elevation dataset with correct depth, BEFORE fixing. FIX: implement per-SEGMENT occlusion for elevation — split each projected `:proj` edge at the points where occlusion state changes (enter/exit a nearer element's projected silhouette), classify each sub-segment independently (visible→solid `:proj`, occluded→dashed `:beyond`/hidden); prefer a true occluder silhouette over the coarse AABB so a partially-covered edge splits at the real boundary (fall back to AABB only if silhouette is too costly, and LOG the degradation). Confirm cascading scope (every L-shaped/stepped/recessed massing in every elevation, not just this wall). Test fixture on THIS South Elevation wall run: the wall renders solid for its visible portion + dashed for its occluded portion, transition at the massing step-back. Strictly elevation; do NOT touch plan/section styling. Maps C06 §7 + Contract 23 §9 + L-182/L-190; no shortcuts; P8. §ELEV-LINEWEIGHT-03. |
| L-197 | founder (correction of L-195) | **[CRITICAL — WebGPU ground-shadow device-loss, 3rd attempt] The invisible-L0 sun→ground shadow is gone on populated scenes + `Destroyed texture ShadowDepthTexture used in a submit` (renderContext_2 = live WebGPU) fires ~500× right after the cinematic tier applies** — founder: "it was WORKING yesterday, since I asked to IMPROVE them they went out." L-189 (512 revert) + L-195 (setLevel no-realloc) fixed the OBC-WebGL `ShadowQualityUpgrader`, which per RenderingPipelineCoordinator.ts:656-660 never reaches the WebGPU Pascal key light — so both deployed with zero effect. The REAL ground shadow = WebGPU Pascal key light + `GroundShadowCatcher`, driven by RealSunService/RealEnvironmentService/PascalSceneLighting. The "improve" commits (2026-07-07) on that exact path: **f4533641** §FIX-GROUND-SHADOW-AT-PERF-TIER (L-168, frustum-fit — RealEnvironmentService.refitShadowToScene + RealSunService) + **0a72a3ee** §FIX-GROUND-SHADOW-WEBGPU-RECEIVE (L-171, needsUpdate refresh). NB the empty-project GREY is a separate KNOWN cosmetic (RealEnvironmentService.ts:106-112, L-112 — catcher base ShadowMaterial with no casters). | Rendering / WebGPU key-light ShadowDepthTexture lifecycle + freeze latch (C04, ADR-0111, L-168/L-171/L-112, [[webgpu-heavy-scene-crash-and-instancing]], [[null-at-mount-runtime-event-race]]) | OPEN — IN FLIGHT (deep-dive agent) | route → WebGPU-shadow agent (FENCE: RealSunService/RealEnvironmentService/PascalSceneLighting + renderer-three GroundShadowCatcher/RenderPipelineManager shadow handling + initScene freeze-latch/refit/refresh wiring + tests; DISJOINT). EVIDENCE-FIRST — trace the WebGPU key-light ShadowDepthTexture lifecycle, find the exact op that reallocs/disposes/refreshes it OUTSIDE the `setShadowReallocFrozen` latch mid-submit (suspects: refit touching mapSize/map/needsUpdate, or L-171 refresh firing on tier-change/settle unguarded), git-confirm the introducing commit, then FIX (freeze-latch the op, OR make refit/refresh non-destructive — camera-only + idle-frame depth re-render via frame scheduler P3, OR clean targeted revert of L-168/L-171 if that restores the working state). Also suppress the empty-project catcher grey if cheap. Success: no ShadowDepthTexture-destroyed on open/tier/settle + the sun→ground shadow renders on the invisible L0 catcher. Preserve L-168 frustum-fit + L-171 refresh intent. Maps C04 + ADR-0111 + L-168/L-171; P2/P3/P8; no shortcuts. §FIX-WEBGPU-GROUND-SHADOW-DEVICE-LOSS. |
| L-198 | founder | **[HIGH — geospatial, 3D globe] On the Cesium photoreal-tiles globe the REAL detailed building is placed UNDERGROUND — the camera flies to the correct location but the model sits ~707 m below the surface** — founder: "the building is NOT in the correct location on the 3D globe / Cesium 3D-tiles view — it is UNDER GROUND, although the view goes to the correct location." Log (Madrid, terrain ≈707 m): the MASSING correctly re-places after the async clamp — `photoreal-tile clamp: base height 706.90 m — re-placing` → `massing rendered ... base 706.9 m` — but the REAL model is placed at `§A.21.D49 REAL model placed on photoreal tiles ... base 0.00 m` (ellipsoid/sea-level), AFTER 706.9 m was already known → ~707 m underground. | Geospatial / Cesium real-model globe placement base height vs photoreal-tile clamp (C18-23, L-179 §FIX-GLOBE-CLAMP-TO-PHOTOREAL-TILES, L-193, A.24; CesiumViewport.renderRealModelOnGlobe:6506 + reseatRealModelOnGlobe:6669 + commitPhotorealBase:3691) | OPEN | route → Cesium/globe agent (lane free; FENCE: apps/editor/src/ui/geospatial/CesiumViewport.ts (real-model placement height) + the `renderRealModelOnGlobe` call site in apps/editor/src/ui/layout/GISAreaLayout.ts + tests; DISJOINT from the L-197 WebGPU-shadow lane — do NOT touch RealSun/RealEnv/Pascal/renderer-three). EVIDENCE-FIRST: `renderRealModelOnGlobe` (:6506) sets `position = fromDegrees(lon, lat, baseHeight)` (:6565) where `baseHeight` (:6536-6538) falls back to ~0 when `input.baseHeight` is not a finite number — the caller passes 0/undefined, NOT the clamped photoreal-tile height the massing uses (`formaTerrainBaseHeight` / `commitPhotorealBase` sampled 706.9 m). CONFIRM the caller + the fallback value BEFORE fixing. FIX: place the REAL model at the SAME clamped photoreal-tile base height as the massing (thread the sampled/clamped height into `renderRealModelOnGlobe`, and drive `reseatRealModelOnGlobe` when the async clamp settles so the real model re-seats to 706.9 m exactly like the massing re-places). Preserve L-179 (clamp to Google 3D-tiles surface, NEVER ion terrain — that caused the 727 m-underground regression) + L-193 (re-entry). Add a test: real-model placement uses the clamped base height, not 0. Maps C18-23 + L-179 + L-193 + A.24; no shortcuts; P8. §FIX-GLOBE-REAL-MODEL-UNDERGROUND-CLAMP.   ✅ FIXED 15b870dd→main (⚠ live-Cesium confirm) — ROOT (a race, not a literal 0): `renderRealModelOnGlobe` captured `baseHeight = formaTerrainBaseHeight` BEFORE its `await Cesium.Model.fromGltfAsync(...)`; the async photoreal-tile clamp (706.9 m) settles DURING that GLB parse, and the reseat-on-settle (`reseatRealModelOnGlobe`) fired while `realModelOnGlobe` was still null (the primitive is assigned only AFTER the await) → skipped → the model placed at the stale pre-await ellipsoid base ~0 → ~707 m underground, no later correction. FIX: re-evaluate the seat base AFTER the parse against the tile-clamped SSOT + re-seat the primitive matrix before adding it; extracted pure `resolveGlobeRealModelBaseHeight`. Scoped to the GLOBE path only — the Forma flat study (base 0 correct) untouched; L-179 no-ion preserved. 30/30 tests, tsc 0. §FIX-GLOBE-REAL-MODEL-UNDERGROUND-CLAMP. |
| L-199 | founder | **[HIGH — Forma façade analysis QUALITY, continuation of L-177] The "Analyse building façade" sun-hours drape on the Forma "3D Site" real model is LOW-QUALITY / garbled — needs a proper per-surface layer like the GROUND heatmap, on the complete building** — founder (2 images): image 1 (analysis OFF) = clean real building + smooth ground sun-hours heatmap (correct); image 2 (façade ON) = a chaotic teal drape with triangulated smear artifacts across the whole tower, NOT a clean per-face sun-hours gradient. "we need a proper layer like the one in the ground but on the complete building." Log: L-177 drape IS active (`§FIX-FACADE-ANALYSIS-ON-REAL-MODEL draped sun-hours onto the REAL GLB model (wall 512×192, roof 128×128, 4 face(s), 101 opening(s), H 24.2 m)`) — but the WALL lookup is a **cylindrical U=centroid-angle / V=height unwrap** (`buildRealModelSunDrape`, siteMetricGrids.ts), which smears/wraps on a non-cylindrical rectangular+balconied building (the artifacts). Contrast the GROUND (correct): `sunHours heatmap: smooth texture 512×512 (≈0.9 m/texel) from 1639 cells, radius 240 m` — a proper raycast grid. | Geospatial / Forma façade sun-hours analysis quality (C18-23, A.24, ADR-0074/0110 byte-identical BVH, L-177/L-160, [[real-solar-heat-thermal-queue]]; buildRealModelSunDrape/applyRealModelSunDrape in siteMetricGrids.ts + CesiumViewport.ts) | OPEN | route → Cesium/Forma-façade agent (BUNDLE with L-198 — SAME CesiumViewport.ts file, one agent, no self-collision). EVIDENCE-FIRST: confirm the cylindrical-unwrap root of the smear + compare to the ground heatmap's per-cell raycast. FIX: replace the angular-cylinder wall LUT with a PROPER per-FACE (planar) sun-hours mapping — for each real façade face, raycast the sun (reuse the byte-identical BVH evaluator, ADR-0110 — data source unchanged) per texel on that face's own UV plane, at a resolution matching the ground heatmap (≈1 m/texel), punch authored openings to alpha 0; so every wall face carries a clean smooth gradient like the ground, on the COMPLETE building (all storeys/faces/roof). Keep the CustomShader drape target on the real Cesium.Model (no envelope prism). Respect A.24 Presentation tier + device-loss budget. Maps C18-23 + A.24 + ADR-0110 + L-177; no shortcuts; P8. §FIX-FORMA-FACADE-ANALYSIS-QUALITY-PER-FACE.   ✅ FIXED 45982d47→main (⚠ live-Cesium confirm) — ROOT: `buildRealModelSunDrape` built a CYLINDRICAL wall LUT (U=centroid-angle, V=height) + the shader looked up each fragment by `atan2` angle → on a rectangular/balconied tower it wraps/smears across faces AND all angles converge at the footprint centre → a POLE SINGULARITY = the founder's radial spikes from the roof apex + teal smear. FIX (display mapping only, BVH data byte-identical/ADR-0110): each façade face gets its OWN planar gradient cell (U=along-face, V=height, ~1 m/texel) packed into a horizontal wall ATLAS; a 16-bit face-table (NEAREST data texture) lets the shader pick the nearest face by PLANAR projection (balconies/insets snap to parent wall, no angular wrap); half-texel inset so LINEAR never bleeds cross-face; openings→alpha 0; roof stays top-down planar (kills the apex singularity). A.24: one UNLIT shader + 3 small uploads, atlas capped faceCount·cellW ≤ 4096 (logged), per-face polygon-envelope fallback on shader failure. 7/7 tests (face-table 16-bit encode/round-trip mirrors shader decode), tsc 0. §FIX-FORMA-FACADE-ANALYSIS-QUALITY-PER-FACE. |
| L-200 | founder | **[CRITICAL — ground shadow, 4th attempt] The massive OPAQUE GREY plane fills the BIM 3D viewport (empty + populated) and there are NO sun-projection shadows of elements on the ground — the `Destroyed texture ShadowDepthTexture used in a submit` device-loss STILL fires on the L-197 build** — founder screenshots: a solid grey rectangle covering the viewport on a fresh project (just the datum sphere). Console (L-197 IS deployed — new bundle hashes): `SceneQualityTier 71 meshes → cinematic (shadows=high)` → `ShadowQualityUpgrader Level changed to "high" (§FIX-SHADOW-GROUND-REGRESSION)` → `applied tier "cinematic"` → `500× Destroyed texture [ShadowDepthTexture] used in a submit (renderContext_2 = WebGPU)`. DIAGNOSIS: the GroundShadowCatcher (packages/renderer-three/src/GroundShadowCatcher.ts) correctly uses a TRANSPARENT `THREE.ShadowMaterial` (opacity 0.32, transparent, receiveShadow) — so the OPAQUE GREY plane is a SYMPTOM of the device-loss (a broken WebGPU shadow pass can't composite the transparent ShadowMaterial → it falls back to opaque), NOT a material bug. L-197 gated the RealSunService sun-refresh `needsUpdate` poke, but this destroy fires at the CINEMATIC-TIER transition on a small/empty scene with no sun-refresh in the log → a DIFFERENT, still-unguarded source (the WebGPU shadow-map rebuild / `SHADOW_REBUILD_SCHEDULED` / RenderPipelineManager pipeline rebuild / the `_reallocShadow` deferred thaw). | Rendering / WebGPU shadow-map churn at the tier transition device-loss + GroundShadowCatcher composite (C04, ADR-0111, L-165/L-189/L-195/L-197/L-112, [[webgpu-heavy-scene-crash-and-instancing]]) | OPEN — IN FLIGHT (definitive agent) | route → WebGPU-shadow agent (FENCE: packages/renderer-three/src/pipeline/RenderPipelineManager.ts + packages/renderer-three/src/GroundShadowCatcher.ts + packages/core-app-model/src/rendering/{RenderingPipelineCoordinator,ShadowQualityUpgrader}.ts + packages/core-app-model/src/rendering/RealEnvironmentService.ts + apps/editor/src/engine/initScene.ts shadow wiring + tests; DISJOINT from the L-198/L-199 Cesium lane). EVIDENCE-FIRST: trace the EXACT WebGPU op that destroys/recreates the key-light ShadowDepthTexture at the cinematic-tier transition on a SMALL scene (NOT the sun refresh L-197 already gated). ROBUST FIX DIRECTION: the init apply() already configures shadows correctly at "standard"; the tier escalation to cinematic/high is what churns the WebGPU shadow map and triggers the destroy — either (a) make the tier transition NOT rebuild/realloc the WebGPU shadow map on normal/small scenes (keep the init setup stable — no shadow churn), or (b) ensure EVERY shadow-map touch at the transition (WebGPU rebuild + freeze/thaw) is deferred past the in-flight submit / GPU-fenced. Prefer eliminating the churn on normal scenes. THEN verify the ShadowMaterial catcher renders INVISIBLE (no grey) + real element shadows appear; if a genuine empty-scene grey remains, hide the catcher when zero casters. Success: no ShadowDepthTexture-destroyed on open/tier/settle + invisible ground + sun shadow of elements on the ground. Maps C04 + ADR-0111 + L-197/L-112; P2/P3/P8; no shortcuts. §FIX-WEBGPU-SHADOW-TIER-DESTROY-AND-GREY-CATCHER.   ✅ FIXED 507c12d7→main (⚠ needs founder live-confirm) — TRUE ROOT (the hole L-195/L-197 both missed): `ShadowQualityUpgrader.apply()` traverses the SHARED THREE scene and mutates EVERY shadow-caster — including the LIVE Pascal key light the WebGPU renderer draws (the coordinator's `_renderer` is the silenced OBC-WebGL, but `scene.traverse` reaches the live caster). On project open `apply('standard')` SHRANK the live key light's shadow map 1024→512 and disposed the live ShadowDepthTexture via `_deferReleaseShadowMap`'s `setTimeout(0)`; the freeze latch only stops THREE's OWN in-render realloc, not apply()'s explicit resize+dispose, and setTimeout(0) is a timing hope not a GPU fence → the live texture is destroyed while the in-flight submit still references it + the pipeline re-binds the stale handle every frame → 500× "used in a submit" → device-loss → the transparent catcher can't composite → OPAQUE GREY. (L-195 made setLevel non-realloc, L-197 gated the sun-refresh — apply() was the remaining hole.) FIX: apply() sets mapSize + primes a regen ONLY when the caster's map is not yet allocated (cold start); an already-allocated LIVE caster keeps its first device-safe allocation + tunes only non-realloc params (mirrors the L-195 setLevel contract). GREY CATCHER (reconciles L-107+L-112 per the founder's git archaeology): keep the receiver ATTACHED from frame 1 (L-112 — stays in the WebGPU shadow-sampling set, no receive break) but gate only its VISIBILITY on caster presence (L-107 — invisible with 0 casters, visible+receiving with ≥1), driven by refitShadowToScene. Tests: live-caster-no-realloc + 0-casters→hidden / ≥1→visible+receiving; all prior shadow tests green; tsc 0. Live-WebGPU confirms the actual absence of the destroy + shadows on ground.   ⬆ PARTIAL on live deploy (founder + log): ✅ device-loss GONE (no `Destroyed texture ShadowDepthTexture` in the log through the cinematic transition) + ✅ empty-project grey GONE (`catcher attached; hidden until first caster`). ❌ RESIDUAL: on a POPULATED scene the catcher flips visible but renders SOLID GREY + NO sun shadow projects. Diagnosis (archaeology): `0a72a3ee §FIX-GROUND-SHADOW-WEBGPU-RECEIVE (L-171)` was THE commit that made the WebGPU ground shadow RENDER (forced `shadowMap.needsUpdate` after the key-light refit); L-197 (`1f885307`) routed that through `requestShadowRefresh()` which NO-OPS while a freeze latch holds → when a caster is added the WebGPU key-light shadow map is never re-rendered → empty shadow map reads as ALL-shadowed → ShadowMaterial catcher = solid grey, no shape. FIX IN FLIGHT (agent resumed): on caster-add / catcher-visible flip, actually fire the shadow refresh (requestShadowRefresh when NOT frozen; confirm `shadowMap.autoUpdate` isn't left false after freeze/thaw) + verify the L-168 refit frustum encloses the casters+catcher — restore L-171's receive WITHOUT reintroducing the device-loss. §FIX-WEBGPU-SHADOW-TIER-DESTROY-AND-GREY-CATCHER (receive follow-up).   ⬆ RECEIVE FIX SHIPPED c5ee5707→main (requestShadowRefresh now restores `autoUpdate=true`) but founder-tested: catcher STILL renders SOLID GREY + no projected shadow once an element exists. NEW PRIMARY HYPOTHESIS (agent re-engaged): **THREE.ShadowMaterial does NOT composite as transparent-shadow-only on the WebGPU/TSL renderer — it renders OPAQUE grey** (GroundShadowCatcher uses `new THREE.ShadowMaterial`; WebGPU ShadowMaterial support is known-flaky). This explains why NO shadow-map fix (setLevel/sun-refresh/autoUpdate) changed the grey — the MATERIAL is opaque on WebGPU regardless of the shadow map. FIX DIRECTION: replace the catcher material with a WebGPU/TSL-compatible shadow-catcher (TSL NodeMaterial sampling the directional shadow → alpha=shadowIntensity, transparent where lit) so the ground is invisible + only the real projected shadow shows on WebGPU, keeping the L-200 visibility gate + device-loss safety. §FIX-WEBGPU-SHADOWMATERIAL-CATCHER.   ✅ TRUE ROOT FOUND + FIXED 0489984e→main (⚠ founder live-confirm) — the ShadowMaterial hypothesis was WRONG and the founder's decisive clue disproved it: **manually swapping WebGL→WebGPU makes the grey DISAPPEAR**, so ShadowMaterial works fine on WebGPU. REAL ROOT: `RenderPipelineManager._buildPipeline()` composes the TSL graph via `createScenePass(this._scene, …)`, which runs ONCE at boot — when the scene holds NO shadow-casting geometry. Adding a caster later NEVER rebuilds the pass, so the WebGPU shadow graph never learns a caster exists → the invisible L0 catcher composites as FULLY shadowed (opaque grey plane) + no sun shadow projects. The backend swap only "fixed" it because its `recoverPipeline → activateOutlines → _buildPipeline` recreates the ScenePass against the now-populated scene. Compounding: `initScene.ts:2268` carried an explicit `NOTE: scheduleShadowRebuild() is intentionally NOT called here` — right that meshes don't own ShadowDepthTexture, but it missed that the TSL ScenePass is compiled AGAINST THE SCENE; its "Destroyed texture" fear was the `apply()` live-map realloc, already fixed in 507c12d7. FIX: initScene schedules ONE ScenePass rebuild the first time real geometry is added, LATCHED per project (reset on `pryzm-project-switch`) — meeting the old NOTE's valid "no per-event churn" concern. `scheduleShadowRebuild()` is WebGPU-gated, debounced, and coalesces in-flight rebuilds. tsc 0. §FIX-WEBGPU-SCENEPASS-FIRST-CASTER. |
| L-201 | founder | **[LOW — documentation UX] Default elevation MARKS should sit 2× further from the origin (further apart)** — founder (plan screenshot): the N/E/S/W elevation "E" tags land too close to / on the shell on larger footprints; wants them pushed out to double the current standoff. | Documentation / default elevation-mark placement radius (C24.1, DOC-2.7, DefaultViewsManager, L-116/L-151) | ✅ FIXED f2822b65→main — `ELEV_MARK_RADIUS_M` doubled 12 → 24 m in DefaultViewsManager.ts (L-151 had already doubled 6 → 12). Both the Ground-Floor + per-level (§FIX-ELEV-MARKS-ALL-FLOOR-PLANS L-158) marks move out; orientation/link unchanged. 9/9 tests updated + green; tsc clean. Residual (unchanged from L-151): a bounds-relative placement (clamp marks strictly OUTSIDE the footprint per-project) remains the proper future refinement vs a fixed radius. §FIX-ELEV-MARK-RADIUS-DOUBLE-2. |
| L-202 | founder | **[MED — elevation live-crop update + default scope] Two elevation-crop asks: (A) when the Ground-Floor plan crop changes and the elevation updates LIVE, the elements newly projected as the elevation scope INCREASES are incorrect (the original set is sound); (B) increase the default elevation crop scope 4×** — founder: "the elevations are sound; it's just that when the crop changes in ground floor and the elevation updates live, the elements being projected as the elevation increases are not correct, whereas the original ones are sound — also increase the crop default by 4× the existing scope." | Documentation / elevation live-crop re-projection correctness + default crop depth (C06 §7, DOC-1.x, EdgeProjectorService `DEFAULT_ELEVATION_FAR_DEPTH`=50 + `resolveClipRange`/crop region + classifyByProjectionDepth; cont. L-182/L-190/L-196) | OPEN | route → documentation/elevation agent (lane free — L-196 shipped; FENCE: apps/editor/src/engine/views/EdgeProjectorService.ts + packages/core-app-model/src/drawing/HiddenLineRemoval.ts (if classification) + the crop→elevation live re-projection trigger (ViewDependencyTracker / view.setCrop path) + tests; DISJOINT from the L-200 shadow lane + the L-198/L-199 Cesium lane). PART B (easy): bump `DEFAULT_ELEVATION_FAR_DEPTH` 50 → 200 m (4×) — and confirm any dependent default (projectionDepth / crop region default) scales sanely so classification still works at the larger scope. PART A (evidence-first): REPORT why the NEWLY-included elements (as the live crop/scope grows via a plan-crop change) project incorrectly while the original set is sound — is it a stale classification cache (the §PERF-CACHE / ViewTechnicalDrawingCache not invalidating the new elements), a depth reference that shifts when the crop grows (classifyByProjectionDepth projectionDepth vs the enlarged far), or the crop→elevation live-update re-projecting only a partial set? Quote file:line. FIX: the live crop-driven re-projection must classify/position the newly-captured elements IDENTICALLY to a fresh full projection (no stale cache, correct depth reference at any crop size). Test: grow the crop → the newly-included elements match a from-scratch projection. Strictly elevation; do NOT touch plan/section styling or the shadow/Cesium lanes. Maps C06 §7 + DOC-1.x + L-182/L-190/L-196; no shortcuts; P8. §FIX-ELEV-LIVE-CROP-REPROJECT-AND-4X-DEFAULT.   ✅ FIXED 80c8bae1→main — PART B: `DEFAULT_ELEVATION_FAR_DEPTH` 50 → 200 m (4×) so a default elevation captures the full depth of large/deep buildings; kept INDEPENDENT of `DEFAULT_SECTION_PROJECTION_DEPTH` (~12 m) — a deeper capture only pulls more receding geometry in as `:beyond` linework, it does not move where projection flips to beyond. PART A ROOT: the per-element projection cache keyed ONLY on the element `version`, so an entry produced under the OLD (shallower) clip range was re-served verbatim after the crop grew → the newly-captured elements inherited a stale clip/classification while the original set stayed sound (exactly the founder's report). FIX: new `computeClipSignature()` folds every capture/classification-affecting input (viewType, projection direction, near/far, cut plane, plan below-band, section depth bands, section volume box) into the cache key alongside `version` (checked at the cache-hit gate), so a crop change misses the cache and re-projects from scratch while unchanged views still hit. 5 new tests pin the invariant (grow crop → signature changes → re-project); existing elevation tests green; tsc 0. §FIX-ELEV-LIVE-CROP-REPROJECT-AND-4X-DEFAULT. |
| L-203 | founder | **[HIGH — render backend] "Are we actually using WebGPU? It feels like WebGL even when on WebGPU" — the renderer OSCILLATES between backends in a swap LOOP + a WebGL2 sub-context rides under the WebGPU adapter** — founder + console: the log repeats `resolvedPreference=webgl (forceWebGL=true) → backend: webgl2 → §RENDERER-LIVE-SWAP backend now: webgl-fallback → WebGPU device lost "destroyed" → resolvedPreference=webgpu → backend: webgpu → §RENDERER-LIVE-SWAP backend now: webgpu → WebGL context lost on <WebGPURendererAdapter/webgl2>` — CYCLING. Two issues: (1) a persisted `forceWebGL` toggle flip-flopping true↔false drives a repeated live-swap loop (WebGPU device destroyed each cycle); (2) even in the `backend: webgpu` state a `/webgl2` context-loss fires under the WebGPU adapter → the "WebGPU" path may be silently on a WebGL2 fallback (founder's "webgl also when using webgpu"). On WebGL2, `§PERF-WEBGL2-NO-TSL` keeps the TSL pipeline (SSGI/outlines/post-FX + shadows) OFF → quality drops. | Rendering / renderer backend resolution + §RENDERER-LIVE-SWAP oscillation + WebGPURendererAdapter WebGL2 fallback (C04, [[webgpu-heavy-scene-crash-and-instancing]] backend toggle key pryzm.renderer.backend) | OPEN — IN FLIGHT (diagnostic agent) | route → render-backend agent (FENCE: createRenderer.ts + RendererBackendToggle.ts + RendererSwapOverlay.ts + the §RENDERER-LIVE-SWAP section of initScene.ts + WebGPURendererAdapter.ts + tests; DISJOINT from the L-200 shadow lane (do NOT touch RenderPipelineManager) + the L-202 elevation lane). DIAGNOSTIC-FIRST: state plainly whether WebGPU is genuinely active by default or falling back to/oscillating with WebGL2, and the exact trigger of the repeating swap loop (persisted toggle re-applied? device-lost→recover→swap cycle? toggle-UI re-fire?) with file:line. FIX: stop the unintended oscillation so the chosen backend stays stable (no repeated device-destroy/swap), keep WebGPU the stable default when the toggle isn't forcing WebGL, and ensure `WebGPU:true` is not secretly a WebGL2 fallback (or surface the fallback honestly). Preserve the intentional user WebGL toggle + the genuine device-loss recovery. Maps C04 + [[webgpu-heavy-scene-crash-and-instancing]]; no shortcuts; P8. §DIAG-FIX-WEBGPU-BACKEND-OSCILLATION.   ✅ FIXED 916f65b7→main (bundled with L-153; ⚠ live-browser confirm) — **DIAGNOSTIC VERDICT, three myths killed:** (1) **WebGPU IS genuinely active** — not a secret WebGL2 fallback. `WebGPURendererAdapter.create` sets `type='webgpu'` only when `renderer.backend.isWebGPUBackend === true` (:168), threaded authoritatively into `RPM.bind` → `_webGpuActive=true`. The `WebGL context lost on <WebGPURendererAdapter/webgl2>` line is the **OUTGOING** webgl-fallback renderer being disposed by the swap (its context released on dispose) — a disposal artifact, NOT a live fallback. (2) **There is no autonomous swap loop** — whole-repo grep proves `swapRendererBackend`'s ONLY caller is `RendererBackendToggle._choose` on a user click. The apparent loop had two amplifiers: L-153's hub-crash made the user retry, and the device-loss safe-mode cap in `createRenderer` **silently persisted `'webgl'`**, clobbering the user's stored choice (the "forceWebGL flip-flopping"). (3) **`usedTimes` is never undefined on the pipeline** — `RenderPipeline.dispose()` → `NodeManager.delete()` reads it on internal render objects; after a swap the renderer's NodeManager belongs to a **different GPUDevice**, so `this.get(staleRenderObject)` is `undefined`. Root = disposing across a device boundary; the old `pipeline.usedTimes=0` band-aid targeted the wrong object entirely. **L-153 root:** WebGPU→WebGL builds no TSL pipeline so it can't fail; WebGL→WebGPU must re-acquire a device AND build the full TSL pipeline (`recoverPipeline`), which re-throws non-shader failures → swap returns `false` → the toggle's `_reloadInto` fired on that `false` → `location.reload()` → boots to `#/projects`. The "crash" was the reload fallback, not a dead viewport. **FIXES:** RPM records the GPUDevice each pipeline was built against and `_safeDisposeRenderPipeline` SKIPS THREE's teardown across a device boundary (resources already reclaimed) instead of walking a foreign NodeManager — band-aid removed; the toggle reloads ONLY when no live-swap entry point exists (a failed/rolled-back swap now stays on the live renderer with a non-destructive notice); the safe-mode cap no longer persists `'webgl'` (per-call `backendOverride`) so the preference can't flip-flop; initScene snapshots/restores the persisted preference on rollback. 168/168 renderer-three + 10/10 editor tests; tsc 0. ⚠ **FLAGGED TRADE-OFF (orchestrator/founder decision):** safe-mode is now session-only, so on a genuinely WebGPU-unstable machine each reload re-tries WebGPU instead of staying pinned to WebGL (favours L-203 "no silent downgrade" over L-139 persistent pinning). Revisit if heavy-scene device-loss recurs. §DIAG-FIX-WEBGPU-BACKEND-OSCILLATION + §FIX-SWAP-WEBGL-TO-WEBGPU-CRASH. |
| L-204 | orchestrator (discovered while closing L-148) | **[HIGH — CI/quality hole] The entire `apps/editor` test suite (165 files) NEVER runs in CI — and 11 of its files / 27 tests are currently RED** — CI runs `pnpm -r run test:ci` (per CLAUDE.md), but `apps/editor/package.json` defines only `test` (`vitest run`), **no `test:ci`** — so `pnpm -r` silently skips the whole app. Its own `apps/editor/vitest.config.ts` globs `__tests__/**/*.test.ts` = 165 suites (globe clamp, project-repository migration, onboarding overlay, façade sun-drape, office plans, command replay factories, level-explode, …). The root `vitest.config.ts` only globs `*.spec.ts` under a handful of `src/**/__tests__` dirs and never matches `apps/editor/__tests__/**`. Consequence: essentially every gate written by this session's agents was green locally but **never enforced**. Verified by running the suite directly: **153 files pass / 11 fail; 1374 tests pass / 27 fail / 1 skipped**. RED FILES: `CesiumViewportClickNoNav`, `FormaSiteAnalysisControls` (3), `bootstrap-shape`, `bootstrap.data`, `bootstrap.everything`, `ensureSiteClimate` (1), `featureFlags.plan-view` (whole file), `hello-12-elements`, `layoutModalHtml`, `levelExplodeCoordination` (1), `residentialBuildingController`. Several are file-level collect/import failures, not assertion failures. | CI / test wiring + pre-existing red suites (CLAUDE.md test topology; `.github/workflows/ci.yml`) | OPEN (HIGH — do NOT simply add `test:ci` first; that would turn CI red) | route → CI/test-hygiene agent. SEQUENCE MATTERS: (1) TRIAGE the 11 red files — decide per file: genuine regression (fix), stale test vs shipped behaviour (update), or obsolete (delete with justification). Do NOT mass-skip. (2) THEN add `"test:ci": "vitest run"` to `apps/editor/package.json` so `pnpm -r run test:ci` picks the app up, and confirm `.github/workflows/ci.yml` actually gates on it. (3) Consider consolidating the root vitest include globs so the `*.spec.ts` vs `*.test.ts` + `src/**/__tests__` vs `apps/editor/__tests__` split stops stranding suites. Maps CLAUDE.md's stated test topology; no shortcuts; the point is that the GA-gate is only as good as what it actually runs. §FIX-CI-EDITOR-SUITE-NOT-GATED. |
| L-205 | founder (recurring, 6th attempt) | **[CRITICAL] Massive grey square on the first element + NO sun-projected ground shadow** — empty project renders clean; the instant the first wall is created the L0 ground catcher paints a UNIFORM GREY square over the whole 3D viewport and no shadow is projected. Founder's decisive clue: **swapping backend webgl -> webgpu makes the grey disappear.** MECHANISM (established): the catcher is a `THREE.ShadowMaterial` plane painting `opacity x (1 - shadowMask)`. Uniform grey == every fragment reads *fully shadowed* == **the key light's shadow DEPTH MAP is never rendered**. A backend swap builds a fresh `WebGPURendererAdapter` which sets `shadowMap.enabled = true` (adapter :155), which is why the swap 'fixes' it. REFUTED so far (do not re-attempt): (a) *ScenePass never rebuilt on first caster* — the path IS correct (`scheduleShadowRebuild` -> SSGI-off -> `_rebuildPipelineWithCurrentState` -> `_buildPipeline` -> `createScenePass(scene, camera)`); (b) *catcher outside the fitted shadow frustum* — three@0.183.2 `ShadowNode.js:320` does an explicit `frustumTest` and returns `float(1)` (LIT) outside; (c) *freeze latch unbalanced* — `_thawShadowAfterLoad()` runs unconditionally (initScene:2869), wall-commit freeze releases at :3190; (d) *ViewController disables shadows* — it targets the SILENCED OBC WebGL renderer by design. PRIME SUSPECT: `shadowMap.enabled` on the live WebGPU renderer is a **global GPU flag with five save/restore writers across four packages and no owner** — `BatchCoordinator:1372-1383` (stashes prior value on a **`window` global** `__pryzmBatchShadowWasEnabled` and delegates restore ACROSS PACKAGES to `CurtainWallBuilder:1389,1459` ~30s later), `initUI:1352/1396` (thumbnail capture), `initUI:2566` (user Cast-shadows toggle), `PerformanceModePanel:368`, `ShadowQualityUpgrader:184/277/375` (which is bound to the OBC WebGL renderer, so its writes never reach WebGPU). Any throw between save and restore leaks `enabled=false` **permanently**, and `RenderPipelineManager.requestShadowRefresh()` then **silently returns** (`if (!shadowMap || shadowMap.enabled === false) return;` :940) so nothing recovers it. | Rendering / shadow caster-set ownership (C04 §SHADOW; ADR-0120; ADR-0111) | **FIXED (92f437a0, live, founder-confirmed on prod).** **TRUE ROOT CAUSE (attempt 10 of 10; nine prior answers were WRONG and shipped).** `PascalSceneLighting._enableShadowsOnScene()` set `castShadow = true` on **EVERY mesh in the scene**, filtered only by whether the mesh NAME contained `edge`/`grid`/`collision`. The scene contains non-BIM meshes — notably a ground-level plane installed by the OBC `ShadowedScene`. It became a shadow caster. **A ground-level plane that casts a shadow shadows the ENTIRE L0 catcher.** Every catcher fragment inside the shadow camera then reads `shadowMask = 0` and paints `alpha = opacity` ⇒ a solid grey rectangle **bounded exactly by the shadow camera's footprint**. The catcher was kept out of the caster set ONLY by an incidental `transparent && opacity < 0.5` check — which merely `return`s, so it never CLEARED a `castShadow` an earlier pass had set. **WHY NINE ATTEMPTS FAILED — the grey rectangle is ALWAYS exactly the size of the shadow camera's ground footprint.** `±113,657 m` (runaway fitted frustum) ⇒ grey to the horizon. `±50 m` (fixed camera) ⇒ a ~100 m grey diamond. Every fix that resized the shadow camera changed the grey's SIZE and nothing else — which repeatedly LOOKED like progress. The size of the symptom was read as evidence about its cause. It never was. **THE DECISIVE EVIDENCE WAS IN EVERY LOG EVER CAPTURED**, on an EMPTY project, before any wall and before the catcher was attached: `[PBRSceneUpgrader] Applied — meshes: 2` then `[PascalSceneLighting] Shadow flags set on 1 mesh(es).` — two meshes, ZERO BIM elements, and one just became a shadow caster. **FIX (`92f437a0` §FIX-SHADOW-CASTER-DENYLIST):** explicitly **DEMOTE** (clear, never merely skip) any mesh that exists to RECEIVE a shadow (`role === 'ground-shadow-catcher'`, or any `ShadowMaterial`) and any mesh whose world bounding radius exceeds `MAX_CASTER_RADIUS_M = 500` (a 40-storey tower is ~75 m; a scene/ground plane is thousands). Receivers keep `receiveShadow`. Demotions are LOGGED by name/type/role/radius so an offending mesh names itself in prod. Gate: 5 new tests (`PascalSceneLighting.casterDenylist.test.ts`) incl. the exact bug shape — a PRE-POISONED catcher must be cleared, not skipped — and a 40-storey tower that must still cast; core-app-model 262/262; root tsc exit 0. Verified in the served bundle (`domain-engine-Dygm4jwC.js`), health 200, **founder confirmed working on prod**. **THREE REAL, ADJACENT DEFECTS were found and fixed en route — none was the grey:** (1) `d9b8f7cf` an off-frame `createScenePass()` + pipeline dispose destroying the live `ShadowDepthTexture` mid-submit (`Destroyed texture ... used in a submit` ×485, climbing) — a genuine WebGPU device-loss hazard; (2) `f4533641`'s scene-AABB frustum fit deriving an **84 km** radius from a ONE-WALL scene — genuine, and the reason the grey once reached the horizon (reverted; the offending mesh is STILL UNIDENTIFIED — latent, see C04 §SHADOW.4); (3) `f3b28961` two renderers (OBC WebGL + PRYZM WebGPU) sharing one light's `shadow.map` slot — genuine, though three's `ShadowNode.setupShadow()` always creates its own target, so it was not the grey. **Finding a real bug adjacent to a symptom is not the same as finding THE bug.** That conflation cost the week. **NINE REFUTED HYPOTHESES — never re-attempt:** ScenePass-not-rebuilt-on-first-caster; catcher-outside-the-frustum; freeze-latch-unbalanced; `ViewController` disabling the live shadow map (it writes the SILENCED OBC renderer; `world.renderer` is assigned exactly once at `BimWorld.ts`); leaked `shadowMap.enabled=false`; transition-only `autoUpdate` writer desyncing across `bind()`; the off-frame ScenePass rebuild; the exploded frustum (only the grey's SIZE); WebGPU reusing a foreign `WebGLRenderTarget` (refuted from three's source). **ALSO LEARNED (real, and it invalidated several 'fixes'):** on the WebGPU node path `renderer.shadowMap.autoUpdate` is **INERT** — `ShadowNode.updateBefore()` gates on the per-LIGHT `shadow.autoUpdate`/`needsUpdate`; `renderer.shadowMap.enabled` is read once at COMPILE time (`ShadowNode.setup`). Several L-205-era fixes adjusted a switch wired to nothing. **DOCUMENTED:** `docs/02-decisions/contracts/C04-RENDERING-AND-SCHEDULING.md` **§SHADOW** (mental model; true root cause; **11 normative rules**; a **measure-first debugging protocol** whose step 3 — `console.table` every `castShadow` mesh sorted by radius — would have ended this in minutes; the nine refuted hypotheses) and **ADR-0120** (shadow caster-set ownership: explicit, receiver-never-casts, size-bounded, self-logging). **RESIDUAL, ACCEPTED:** the ground shadow is now correct but **SOFT** (`metresPerTexel ≈ 0.195 m` at ±50 m / 512²). Sharpening is deferred to the ordered, measured path in C04 §SHADOW.4 — raise `mapSize` ONCE at allocation on the STABLE camera (512→2048 ⇒ 0.049 m/texel, 4×), device-safe + tier-gated; a clamped fit only after. **This is the exact request whose careless implementation started L-205.** |
| L-206 | orchestrator (surfaced by the L-18 collab agent) | **[HIGH] Bus-dispatched creates are never broadcast to the command log — no live cross-user sync for bus-placed elements** — only the legacy `commandManager.onCommandExecuted` path is socket-broadcast to `project_command_log` (`initCollaboration.ts:846-866` -> `server.js:534-574`). The entire dotted-bus create family (`furniture.create`, `lighting.create`, `handrail.create`, `beam.create`, `curtainwall.create`, `wall.batch.create`, …) — which is the PRODUCTION placement path for carousel drag-drop, plan tools and AI furnish — is never persisted to the log and never replayed to peers. Consequence: a second user in the same project does not see bus-placed elements appear, and catch-up cannot replay them. (Discovered while closing L-18: the dedup gate was extended to cover the dotted-bus family defensively, but the broadcast itself does not happen, so that dedup is currently forward-looking rather than load-bearing.) | Collab / CRDT + command log (C08; P6 commands are the only mutation path) | OPEN (HIGH — confirm intent before building) | route -> collab/C08 agent. FIRST decide the intended contract: is the bus the canonical mutation path (P6 says yes), in which case the broadcast+replay must move to the bus and the legacy `commandManager` broadcast becomes the compatibility shim? Or is bus-create deliberately local-only pending the CRDT applier (`EngineBootstrap` logs `G3-T2: runtime.inner.bus not accessible — CRDT applier not wired`)? Map against C08 + P6 + the CRDT/Yjs design before writing code; the L-18 dedup already handles idempotent replay of these types once they DO arrive. §FIX-BUS-CREATE-NOT-BROADCAST. |
| L-207 | founder | **[HIGH] Commercial tower appears to be ONLY an envelope** — "the commercial tower has the envelope"; the interior (structural core, floor plates, fit-out, rooms, furnish, lighting) appears absent. **NOTE ON EVIDENCE:** the console the founder pasted with this report is from the **HOUSE** pipeline, not the tower — `[house-postgen]` is logged solely by `apps/editor/src/ui/house-layout/runHousePostGenChain.ts`, and the office lane (`apps/editor/src/ui/office-building/*`, entry `pryzmGenerateOfficeBuilding({stories:40,radiusM:22})`) never calls it. So no tower log exists yet; the audit is being done from code. Suspected mechanisms to prove/refute: a silent-fallback early return (this codebase has form — §ENVELOPE-DIAGNOSTIC: an envelope HARD-reject once fell through to `[]` silently); a stage gated off in the default 40-storey path; or the CIRCULAR plate (`deriveOfficeCircle.ts`) breaking the orthogonal assumptions the house/resi executors rely on (cf. the known `shellWallMatch` failure on non-orthogonal shells that zeroed windows in the Casa demo). Also to check: parity against the standing rule that office/new typologies MUST mirror the proven resi-building + house executors (core/stairs/toilets/naming/finishes/perf) rather than reinvent. | Typology packs / office-commercial executor (C11 element-creation pipeline; typology-pipeline registry) | **ROOT-CAUSED (2026-07-10) — design/parity gap, NOT a silent failure** | **AUDIT RESULT.** The tower DOES build an interior — but only on **2 floors of N**, and emits **zero furniture and zero lighting by default**. No stage silently dies; the shortfall is SCOPING + DEFAULT FLAGS. (a) Emitted on EVERY storey (`OfficeBuildingExecutor.ts:369-461`): circular slab, perimeter wall ring (decimated to a <=24-gon, :283), per-segment curtain glazing (:396-400), square RC core walls (:407-419), roof cap on top (:446), and a continuous vertical-circulation core (switchback + fire stairs per level pair, lift cab per level, `_buildCoreServices` :957-992). (b) Emitted on ONLY ground `0` + `representativeFloorIndex` (`detailedIndices = [0, representativeFloorIndex]`, :493): WC block + fire lobby + corridor partitions (:994-1026), circulation-first floor architecture (`_buildFloorArchitecture` :507-516), graph room naming + floor finishes (`_nameAndFinishFloors` :521). (c) Emitted NEVER by default: furniture + lighting — `execute()` is architecture-only unless `opts.withInterior === true` (:563-565), and the AI-panel path passes NO `withInterior` (`OfficeBuildingController.ts:154`); onboarding defaults it to `false` ("Architecture Only", `OnboardingStepController.ts:1647`). Even WITH `withInterior`, `furnishOfficeInterior` furnishes only rep+ground and lights only rep (`officeFurnish.ts:131-171`). **NET VISUAL: 38 of 40 floors are bare shells — slab + transparent glazing + core walls + core shaft. Through the glazing you see stacked empty slabs around a central core: a glass envelope.** That is exactly the founder's report. **HYPOTHESIS REFUTED:** the non-orthogonal-plate hazard is NOT triggered. The office does not use `shellWallMatch`; glazing/doors carry an explicit host `wallId` per n-gon edge and clamp to the stored wall length via `clampOpeningToWall` (:672-685). The circular plate does not break window hosting. (The one curved-geometry casualty is room-bounding lines, which the code's own L-170 note says never render — see the minor item below.) **PARITY VIOLATIONS vs the standing rule (office MUST mirror the proven resi+house executors):** the STRUCTURAL half is faithful (mirrors `ResidentialBuildingExecutor`, reuses `CreateSlabCommand`/`CreateStairCommand`/`CreateVerticalCirculationCommand`/`BatchCreateRoomsCommand`/`CreateFloorCommand`/`wall.batch.create`, `recordStairVoid` void-cut) — sound, not reinvented. The FINISH half diverges and IS the root of "envelope only": (1) **no per-storey finish fan-out** — the office never imports `runHousePostGenChain`/`triggerCeilingLayout` and emits no `ceiling|furnish|lighting.layout-execute` (zero matches under `office-building/`); the house drives name->floor+ceiling->furnish->lighting for EVERY storey. (2) **No ceilings at all, on any floor.** (3) Furniture/lighting gated behind a second command (`pryzmFurnishOffice`) instead of being the chain terminus. (4) **Reinvented lighting** — a bespoke `ceilingLightGrid` -> `CreateLightingCommand` loop (`officeFurnish.ts:156-171`) instead of the shared occupancy-aware `LightingLayoutExecutor`; likewise hard-coded disc finishes instead of per-room `_finishPublicFloors`. **PERF (office lane, distinct from the house numbers):** ~960 perimeter walls + ~960 windows + ~160 core walls + ~40 slabs + ~78 stairs + per-level lifts for a 40-storey run. The office's dead-wait analog is FOUR independent `tryPunch(40)` readiness polls (150ms x up to 40 = ~6s cap each) at :664/:816/:880/:907, spinning until async `wall.batch.create` walls land in the store. Bottleneck = element count + async-landing polls, NOT the house chain's settle cascade. The house's 8s zero-wall watchdogs are NOT present here. **NEXT — needs a FOUNDER DECISION before code (how many floors to fully fit out; 40 x full fit-out is a perf question, not just a flag):** then mirror `runHousePostGenChain` per storey (or an explicit larger `detailedIndices` set), add ceilings, and either furnish+light by default or make "Architecture Only" explicit in the UI. §FIX-OFFICE-TOWER-FITOUT-FANOUT. **MINOR:** the office still emits non-rendering room-bounding lines (zone lines :438/:450, core room lines :999-1003) that its own L-170 note says never render and cause redetect churn — drop them as the floor-architecture rings already were. |
| L-208 | founder | **[MEDIUM — expectation gap, NOT a rendering bug] "WebGPU and WebGL look the same"** — CONFIRMED BY DESIGN, and the founder is right to notice. The WebGPU TSL pipeline boots to **Phase 4 = Outlines only**, with **SSGI and TRAA OFF by default** (`initScene.ts:2680` logs `TSL pipeline at Phase 4 (Outlines only). SSGI + TRAA OFF by default (user-opt-in)`; `RenderPipelineManager.ts:222-223` `_ssgiActive=false`, `_traaActive=false`). The founder's own log confirms it live: `Phase: phase4 \| WebGPU: true \| SSGI: false \| TRAA: false`. Both backends therefore render the SAME standard PBR + the same shadow pass, so they are visually near-identical — the only WebGPU-exclusive effects (screen-space GI, temporal AA) are opt-in behind the RenderRail toggle and nobody turns them on. Consequence: the entire WebGPU investment is INVISIBLE in the default product, while still carrying the WebGPU-only risks (device-loss cascades, ShadowDepthTexture mid-submit, backend-swap fragility) that have generated L-139/L-153/L-197/L-200/L-202/L-203/L-205. | Rendering / product differentiation (C04; ADR-0087 recovery-downgrade) | OPEN (product decision, not a defect) | Needs a FOUNDER DECISION, not a code fix. Three coherent options: **(1) Make WebGPU visibly better** — enable SSGI and/or TRAA by default on capable devices behind the SceneQualityTier (`cinematic` already implies the intent; today it explicitly does NOT enable them), accepting the perf + device-loss risk and gating by tier/element-count. **(2) Keep parity and justify WebGPU on perf/compute** (instancing, compute pickers, future GPU solar), and stop treating it as a visual tier — then the correct default may be WebGL2 for stability, with WebGPU opt-in. **(3) Status quo** — WebGPU default, no visual delta, ongoing WebGPU-only risk with no user-visible payoff (hardest to defend). Recommend deciding this BEFORE more WebGPU-only bugfix spend. Maps C04 + the Presentation/Massing render-tier strategy (A.24). §DECIDE-WEBGPU-VISUAL-DIFFERENTIATION. |
| L-209 | orchestrator (found in the founder's L-207 log) | **[HIGH] `runBatch` nesting silently drops the batch guards — 59 lighting fixtures are NOT one undo unit** — `apps/editor/src/ui/lighting-layout/LightingLayoutExecutor.ts:134` calls `batchCoordinator.runBatch(...)` explicitly to obtain ONE undo unit ("ONE runBatch — single undo unit", :131). During the house post-gen chain a batch is ALREADY open, so it trips the nesting guard at `packages/core-app-model/src/batch/BatchCoordinator.ts:867` — `runBatch called while already batching — nesting not supported. Running fn() without batch guards.` — and the body runs UNGUARDED. Two consequences, both visible in the founder's log: (a) **undo is broken** for auto-lighting — the 59 fixtures are individual undo entries, not one; (b) **perf** — with the batch guards gone every fixture takes the per-element path, producing 59 separate `§FT-LIGHTING: lighting mirrored to legacy store` lines and `step=lighting stepMs=8998`. A warning-and-continue on a correctness-critical guard is the wrong failure mode: it converts a structural violation into silent data damage. | Batch / undo semantics (C17 batch catalogue §II-2; P6 commands are the only mutation path; ADR undo three-store) | **FIXED (127d8f43, live)** — G0 of the L-207 fan-out sequencing. §FIX-RUNBATCH-NESTING-DROPS-GUARDS: the commit + `layout-executed` emit + toast were extracted into one `commitAndAnnounce()` closure, gated on `batchCoordinator.isBatching`: false -> run synchronously (apartment path unchanged, byte-for-byte); true -> defer via `batchCoordinator.onNextSettle(() => setTimeout(commitAndAnnounce, 0))` so the commit lands on a CLEAN, non-nested batch. The `setTimeout(0)` is load-bearing, not decoration: `onNextSettle`'s callback fires SYNCHRONOUSLY inside the settling batch's `onComplete` tail (`_flushSettleListeners` :1558 runs after `_isBatching=false` :1549 but before the tail re-reads `_batchStartTime` at :1603), so a synchronous re-entrant `runBatch` would set `_isBatching=true` and mutate `_batchStartTime`/`_pendingLevelIds` mid-tail and corrupt the settling batch. The macrotask yield unwinds that tail first (P3-consistent; mirrors `runHousePostGenChain`'s §POSTGEN-SETTLE). NO DEADLOCK: the ambient batch is the house STRUCTURAL batch (`HouseLayoutExecutor.ts:1313`), whose settle is driven by its own geometry drain and never awaits the lighting commit; and the chain's 12000ms `LIGHTING_TIMEOUT_MS` backstop degrades to "no fixtures", never wedges. Gate: 4/4 new tests (`LightingLayoutExecutor.test.ts`), root tsc exit 0. **SECOND CORRECTION — the orchestrator's brief was ALSO partly wrong, and the agent refuted it:** cost (a) *undo fragmentation* applies ONLY to LIGHTING, whose `buildLightingCommands` returns N flat `lighting.create` commands. CEILING (`CeilingLayoutExecutor`) and FURNISH (`FurnishLayoutExecutor.ts:499`) hit the identical nesting mechanism but each dispatch a SINGLE `*.batch.create` command — one command = one undo entry — so their undo was NEVER fragmented; for them only cost (b) is real (dropped `skipRedetectRooms` / `skipPbrUpgrade` => an unwanted redetect + PBR pass can fire mid-house-build). Ceiling was fixed anyway (in-fence, identical mechanism, restores the guards) with no undo-win claimed. FLOOR (`floorLayoutTrigger.ts`) has NO `runBatch` at all — it dispatches a single `CreateFloorsByRoomTypeCommand` via `commandManager.execute` — so there was nothing to fix; its "one runBatch unit" comment is stale wording only. **RESIDUAL (out of the agent's fence, still OPEN):** `FurnishLayoutExecutor.ts:499` loses `skipRedetectRooms`/`skipPbrUpgrade` the same way — apply the same caller-side `onNextSettle` deferral. Also: keep the nesting case a hard error in dev thereafter, never a silent warn-and-continue; `beginBatch` (`BatchCoordinator.ts:1215`, "Ignoring.") has the same shape. | route -> batch/undo agent (office agent is checking whether the office+furnish lanes trip the same guard). Decide the contract properly: EITHER make `runBatch` re-entrant/ref-counted (inner calls JOIN the ambient batch and the outermost commit closes one undo unit) OR make callers detect an ambient batch and join it rather than opening their own — and make the nesting case a hard error in dev, never a silent downgrade. Note `beginBatch` has the same shape (`BatchCoordinator.ts:1215` — "Ignoring."). **CORRECTION (2026-07-10, from the L-207 audit) — the orchestrator's stated cost was wrong.** I claimed the 59 `§FT-LIGHTING: lighting mirrored to legacy store` lines and `step=lighting stepMs=8998` were CAUSED by the dropped batch guards. They are not. That log line is emitted once per `lighting.created` by the bus->legacy-store bridge subscriber (`apps/editor/src/engine/initTools.ts:1762`), which is NOT gated by the batch or by `skipRedetectRooms` — you get 59 lines whether or not the batch nests. The ~9s is dominated by the settle drain plus 59 sequential `CreateLightingCommand` executions, not by the lost guards. **The REAL cost of the dropped guards is:** (a) **undo grouping** — the 59 fixtures become 59 transactions instead of one, so auto-lighting cannot be undone as a unit (this is the actual user-visible defect, and it stands); and (b) the discarded `skipRedetectRooms` opt lets room re-detection fire during the sweep. **SCOPE CORRECTION:** the OFFICE lane does NOT hit this guard — it never routes through `LightingLayoutExecutor` (no `lighting.layout-execute` emit anywhere under `office-building/`); its lighting is a direct `CreateLightingCommand` loop inside its own clean `runBatch` in a `deferWork(...,400)` frame (`officeFurnish.ts:158`), and its four furnish batches are staggered (200/300/400/500ms) so they never nest. The office structural lane DOES nest once (`OfficeBuildingExecutor.ts:1015` inside the structural batch at :369) but that is **data-safe and mirrors the accepted resi pattern** (`ResidentialBuildingExecutor.ts:679`): the outer async bracket is still open, so the inner `fn()`'s store events buffer and flush with the structural batch. Nothing is lost; it only warns. **FIX DECIDED (caller-side, NOT a re-entrant `runBatch`):** making `BatchCoordinator.runBatch` ref-counted would change global semantics and still would not make nested work a separate undo unit. `BatchCoordinator` already exposes the right primitive: `onNextSettle(...)` (contract at `BatchCoordinator.ts:796-804` — fires on the next microtask when idle, and after settle when batching). Guard the commit block at `LightingLayoutExecutor.ts:133-153` so that when `batchCoordinator.isBatching` is true it defers via `onNextSettle`, else runs as today. The lighting commit then always opens a CLEAN, non-nested batch => one undo unit (C17 DI-6 / PS-3: each dispatch is exactly one undo unit), and it is a behaviour-preserving no-op for the apartment single-level path. Keep the nesting case a hard error in dev thereafter, never a silent downgrade — `beginBatch` (`:1215`, "Ignoring.") has the same shape. §FIX-RUNBATCH-NESTING-DROPS-GUARDS. |
| L-210 | founder | **[MEDIUM] Pink/violet dashed lines drawn THROUGH the building in 3D** — founder: "corridor pink dash lines... should be related to corridor floor finishes - but it is not needed nor requested". **DIAGNOSIS (not floor finishes):** they are **room-bounding lines** (`packages/geometry-wall/src/RoomBoundingLineBuilder.ts`), `ACTIVE_COLOR = 0xA855F7` (violet-500). Two defects make them read as a bug: (1) the `LineDashedMaterial` and the vertex diamonds are built with **`depthTest: false`** (`:101`, `:110`) and `renderOrder = 1` (`:105`), so they render OVER all geometry — the founder's screenshots show them punching through the roof slab and the façade, x-ray style, from every floor at once; (2) **nothing ever hides them in 3D** — `setVisible()` exists (`:42`) but no view / level / V-G / visibility-intent path ever calls it, so they are unconditionally on. They are emitted by every generator (`ApartmentLayoutExecutor:395`, `HouseLayoutExecutor:3393`, `OfficeBuildingExecutor:38`, resi). **ARCHITECTURAL FRAMING:** a room-bounding line is a **plan / documentation** construct (it defines a room boundary for area + finish computation). Rendering it into the 3D scene, always-on and depth-ignoring, violates **P7** (visibility is DOMAIN intent, not a hardcoded renderer flag) and bypasses the visibility-intent system entirely. Related: L-170 already records that the OFFICE lane emits non-rendering room-bounding lines that cause redetect churn — same element, different symptom. | Rendering / visibility intent (P7; C09 AI & visibility-intent; C06 UI shell + tools) | OPEN (queued) | route -> rendering/visibility agent. (1) **Default the 3D representation OFF** and govern it through the visibility system (a V/G togglable category / visibility-intent), NOT a hardcoded `visible = true`. (2) **Remove `depthTest: false`** from the line + diamond materials so that, when a user DOES enable them, they occlude correctly instead of x-raying the model. (3) Keep the plan-view representation unchanged — this is a 3D-only defect. (4) Do NOT stop the generators emitting the ELEMENTS (rooms/areas/finishes depend on them); only their 3D mesh representation is at issue. §FIX-RBL-3D-XRAY-ALWAYS-ON. |
| L-211 | founder | **[MEDIUM] Layered wall renders its layers in 3D but NOT in plan** — "it is a layered wall — in 3d it renders good — in plan doesn't create the layers". **DIAGNOSIS (from the founder's own log):** `WallLayerPlanSymbolBuilder` IS running and DOES work — `[WallLayerPlanSymbolBuilder] injected layer lines for 2 layered wall(s) in plan view "vd-sys-plan-l0"` fires on every reprojection. So the plan symbol path is healthy. The gate is `packages/geometry-wall/src/WallLayerPlanSymbolBuilder.ts:68`: `if (!wall.layers \|\| wall.layers.length < 2 \|\| wall.curve) continue;` — a wall is skipped unless the **instance** carries a populated `layers` array. **Hypothesis to prove:** assigning a layered wall TYPE via the properties panel (`WALL TYPE` dropdown -> Apply) updates the 3D mesh (which reads the systemType catalogue) but never writes `layers` onto the wall INSTANCE, so plan sees a plain wall. Note the founder's own screenshot shows `WALL TYPE: — Plain Wall —` on the inspected wall. Also observed in the same log: `[TechnicalDrawing] Layer "A-WALL" does not exist. Falling back to "0".` — a separate CAD-layer registration gap worth checking while in there. | Documentation / plan symbols (C06; DOC-2.x; C11 element-creation) | OPEN (queued) | route -> documentation/plan-symbols agent. Establish the single source of truth for a wall's layer stack (systemType catalogue vs instance `layers`) and make BOTH the 3D builder and `WallLayerPlanSymbolBuilder` read it. Do NOT duplicate the stack onto the instance if the catalogue is canonical — resolve through the systemType store. Prove with a test that a wall whose type is layered draws N-1 layer lines in plan. §FIX-LAYERED-WALL-PLAN-LINES. |
| L-212 | founder | **[MEDIUM — quality] "Could we create way more photorealistic floor finishes?"** — the floor-finish catalogue is already architecturally rich (Porcelain/Marble tile, Carpet tile, Broadloom, Engineered timber, Solid oak, LVT, Rubber sports, Epoxy resin, UFH screed, Raised access, Wet-area tanked, Oak herringbone, Smoked-oak chevron, Walnut herringbone…) but each renders as a flat tinted `MeshStandardMaterial` — no albedo/normal/roughness maps, no per-finish UV scale, no pattern (herringbone/chevron are named but not drawn). | Rendering / materials (C04; A.24 Massing+Presentation render tiers; PBRSceneUpgrader / ClearcoatMaterialUpgrader) | OPEN (queued — quality, not a defect) | route -> rendering/materials agent. Architecturally this is the **Presentation tier** (A.24), not a new engine: give each floor-finish systemType a PBR material definition (albedo + normal + roughness + optional AO, real-world UV scale in metres, pattern rotation for herringbone/chevron), sourced as tileable textures. **Constraints:** textures must be self-hosted (CSP `connect-src 'self'` — the log already shows external fetches blocked); memory-budgeted and tier-gated (do NOT ship 4K maps to the 40-storey tower — see the WebGPU device-loss history); one shared material per finish TYPE, never per element (per-element unique materials already defeat instancing — see G1). Map onto C04 + the render-tier strategy before writing code. §FEAT-PBR-FLOOR-FINISHES. |
| L-213 | founder | **[HIGH — correctness] UI-created floor finishes are placed on the wall CENTRELINE; batch-created ones are placed correctly inside the walls** — "floor finishes on the batch 'residential house creation' and 'residential building creation' are created automatically within the walls, but when created via UI they are created on the centreline of the wall, which is wrong. Can you make them sound as they are when created during the batch execution, also via UI?" **This is a genuine geometric defect, not a preference:** the same conceptual element (a room's floor finish) has TWO different boundary derivations depending on the entry point. The batch path derives the finish polygon from the room's INNER face boundary; the interactive path derives it from the wall centreline, so every UI-drawn finish overshoots into the wall by half its thickness on every edge. Areas, material take-off and IFC export inherit the error. | Element creation pipeline (C11 — one element, one creation path; P6 commands are the only mutation path) | OPEN (queued, HIGH) | route -> element-creation/floor agent. **The architectural fix is convergence, not a second offset:** the batch executor and the UI floor tool must call the SAME boundary-derivation function. Find where the batch path computes the room's inner-face polygon (`CREATE_FLOORS_BY_ROOM_TYPE` / `floor-layout` executor) and make the interactive `FloorTool`/`floor.create` handler use it. This is exactly the C11 "single element-creation pipeline" rule; two divergent derivations for one element type is the defect. Add a test asserting UI-created and batch-created finishes for the same room produce an IDENTICAL polygon + area. §FIX-FLOOR-FINISH-BOUNDARY-UI-VS-BATCH. |
| L-214 | founder | **[HIGH] Wardrobe is never created in 3D (preview works, creation fails); and Space-rotation is lost on commit** — "first the wardrobe is not being created in 3D — not sure why — the preview works but the creation not. And in plan view, even if the wardrobe IS created and the preview rotates on SPACE, when the creation is executed the rotation on SPACE is not working." **ROOT CAUSE IS IN THE FOUNDER'S LOG, exactly:** `[WardrobeCabinetTool] furniture.create failed: CommandBusError: furniture.create: canExecute rejected — rotation must be finite` with the stack `sB._placeWardrobe (engineLauncher…:1944:10134) <- e._onPointerDown`. The 3D `WardrobeCabinetTool._placeWardrobe()` builds a `furniture.create` payload whose `rotation` is **NaN / undefined**, so the command's `canExecute` guard correctly rejects it and NOTHING is created. The PREVIEW never passes through `canExecute`, which is precisely why it renders while the creation silently fails. **Same defect explains the second symptom:** the Space-key rotation the preview accumulates never reaches the command payload — so even on the PLAN path (`FurniturePlanToolHandler`, which DOES succeed: `Wardrobe run created furniture_01KX66BQ… wardrobe_straight`), the committed element ignores the previewed rotation. **Two tools (3D `WardrobeCabinetTool` + plan `FurniturePlanToolHandler`) build the same `furniture.create` payload independently; one of them loses/never sets `rotation`.** That is the same class of defect as L-213 (one element type, divergent creation paths — a C11 violation). **Note the guard is CORRECT and must stay:** `rotation must be finite` caught a real bug. The failure is that it was swallowed into a console error instead of surfacing to the user (silent no-op on click). | Element creation pipeline / tools (C11 one element ⇒ one creation path; C06 UI shell & tools; P6 commands are the only mutation path) | **FIXED (b107eae9)** — §FIX-WARDROBE-CREATE-ROTATION-NAN. **⚠ THE ORCHESTRATOR'S ROOT CAUSE ABOVE WAS WRONG; the agent refuted it from source.** It is **not** NaN arithmetic and **not** an `atan2` on a zero-length direction. The yaw was **always finite**. It is a **payload-shape/type mismatch**: `WardrobeCabinetTool.ts:352` sent `rotation: { x: 0, y: rotY, z: 0, order: 'XYZ' }` — a THREE **Euler object** — while the live bus handler `plugins/furniture/src/handlers/CreateFurniture.ts:47` declares `rotation?: number` (a **scalar** yaw in radians) and guards with `!Number.isFinite(cmd.rotation)`. `Number.isFinite({…})` is `false`, so the command was rejected. The guard's message *"rotation must be finite"* is therefore misleading — the yaw *was* finite; the payload **shape** was wrong. The guard is correct and was kept. Previews render because previews never run `canExecute`; the commit is the only path that does, and the `.catch` swallowed the throw into a `console.error` → silent no-op on click. **Symptom 2** (plan wardrobe ignores SPACE) was a *separate* line: `FurniturePlanToolHandler._commit` hard-coded `rotation: 0` on the kitchen and wardrobe branches, dropping the preview's accumulated yaw (the plain-furniture branch already read `this._rotation.rotationY()` correctly). **Decisive corroboration:** `KitchenCabinetTool._placeKitchen` had this EXACT bug and was already fixed under §FIX-KITCHEN-SECOND-PLACE — its comment (`:376-383`) documents the Euler-object rejection verbatim. Wardrobe was the one tool never converted. **Fix:** new canonical `buildFurnitureCreatePayload()` (`apps/editor/src/engine/furniture/furnitureCreatePayload.ts`, L5 — an app-level adapter from placement tools to the CommandBus, a dispatch concern, NOT geometry, so it does not belong in L2 `@pryzm/geometry-furniture`); all three surfaces feed it; `rotation: number` is typed, so **the Euler-object regression is now a COMPILE error at every call site**, not a runtime rejection. Rotation is read from `PrePlacementRotation.rotationY()` at commit, never recomputed. Rejected `canExecute` now surfaces via the existing `runtime.events.emit('pryzm:toast', …)` channel instead of a silent console error. Gate: 8 new tests (`apps/editor/__tests__/FurnitureCreatePayload.test.ts`) incl. 3D and plan call-sites producing an **identical** payload, the builder throwing on a NaN/Euler rotation, and the real `CreateFurnitureHandler.canExecute` still rejecting `Infinity` **and** a Vec3 while accepting a finite scalar (guard not weakened); 49 related tests green; root tsc exit 0. **Latent sibling, out of scope → L-218.** | route -> element-creation/tools agent. (1) Find where `WardrobeCabinetTool._placeWardrobe` derives `rotation` and why it is non-finite (likely an unset preview-rotation accumulator, or a `Math.atan2` on a zero-length direction vector). (2) **Converge**: the previewed transform (position + rotation) must be the SINGLE source that both the 3D tool and the plan tool hand to `furniture.create` — do not fix one tool's payload in isolation. (3) Rotation state (Space-key) must live with the preview and be read at commit, not recomputed. (4) A rejected `canExecute` must surface to the user (toast), never a silent no-op. (5) Tests: a Space-rotated preview commits an element whose rotation EQUALS the preview's; a zero-length placement direction cannot produce NaN. §FIX-WARDROBE-CREATE-ROTATION-NAN. |
| L-215 | founder | **[HIGH] Editing stair parameters does not regenerate the dependent geometry** — "when updating some fields some elements don't update accordingly — for example the width doesn't update the landing shape; when updating the riser height the preview doesn't get updated; the tread depth doesn't update." Screenshots show the OLD purple selection geometry still overlapping the newly-parameterised stair. **ROOT CAUSE (code-confirmed):** `packages/command-registry/src/generic/UpdateElementParameterCommand.ts` carries EXPLICIT post-update rebuild branches for **window** (:165 "sync rich WindowStore so WindowBuilder rebuilds the frame geometry"), **door** (:171) and **roof** (:251 "rebuild is handled automatically via the bim-roof-updated event") — and **NO branch for stair**. So a stair parameter edit writes the store and stops: `GenerateStairGeometryCommand` (`packages/command-registry/src/stair/`) is never re-run, the flight/landing/tread geometry is never rebuilt, and the stale mesh + stale selection overlay remain. `width -> landing shape`, `riserHeight -> riser count + total run`, `treadDepth -> going` are all DERIVED quantities that only `GenerateStairGeometryCommand` recomputes. **ARCHITECTURAL SMELL:** a per-element-type `if` ladder inside a *generic* command is the defect. Every new element type must remember to add its own rebuild branch, and stair was forgotten. | Element creation / parametric rebuild (C11; C03 §commands+state; P6 commands are the only mutation path) | **FIXED (e7aebbb8)** — §FIX-STAIR-PARAM-NO-REGEN. Root cause confirmed as stated. Fix: a **declarative rebuild contract** (`packages/command-registry/src/generic/ElementRebuildRegistry.ts` — `ElementRebuildDescriptor { elementTypes, geometryParams, createRebuildCommand(id) }`). `UpdateElementParameterCommand.triggerGeometryRebuild` now resolves a descriptor and dispatches its rebuild command; the stair `if` branch is **deleted**; window/door/roof fall through to the legacy ladder **byte-for-byte unchanged** (pinned by a REGRESSION GUARD test). The pure derivation (`deriveStairGeometry`) lives at **L2** `geometry-stair/src/StairParameterReconciler.ts` — no THREE/DOM/IO. **AGENT CORRECTION to the brief:** re-dispatching `GenerateStairGeometryCommand` *alone would not have fixed it* — that command only rebuilt the mesh from the (still stale) store. It was made the true owner of "make geometry agree with parameters": reconcile derived fields → persist via `stairStore.update` → rebuild mesh. **Measured derived fields** (read from `StairMeshBuilder`, not guessed): `width` → tread box width (`:434`), riser width (`:447`) **and landing polygon** (`landing.depth = width` for L, `2·width` for U — `StairCreationController.ts:361/375`); `riserHeight` → **riser count** `round(levelHeight/riserHeight)` (`:343`); `treadDepth` → the **going** (`StairMeshBuilder.ts:423/429`). **P4 (stale purple ghost) needed NO new code** — `StairStore.update` emits `bim-stair-updated`, which `SelectionManager` (§SELECT-GIZMO-REATTACH, `:911-918`) already handles by re-resolving the live Object3D and re-highlighting. The ghost persisted **only because the rebuilt mesh was geometrically identical** (stale derived fields). Once the mesh genuinely changes, the existing machinery refreshes it. **Documented limitation:** authored polyline/curved stairs (per-flight `treadDepth` / `landing.center`, StairPathAdapter) return `null` from `deriveStairGeometry` and are rebuilt without re-deriving — a width/tread edit won't resize their bespoke flights. Deliberate: better than corrupting them. Gate: geometry-stair 70/70, command-registry 6/6, root tsc exit 0. | route -> element-creation/stair agent. (1) Make a stair parameter update re-run `GenerateStairGeometryCommand` so ALL derived geometry (landing shape, riser count, going, total run, railing) rebuilds. (2) **Do NOT just add a 4th `if` branch** — that perpetuates the smell. The architecturally sound fix is a **declarative rebuild contract**: an element type declares which parameters are geometry-affecting and which rebuild command owns them; `UpdateElementParameterCommand` consults that registry instead of hard-coding types. Map onto C11 (single element-creation pipeline) + C16 (command-authoring). Migrate stair first, leave window/door/roof branches working, and note the migration path. (3) Selection overlay + preview must invalidate with the mesh (the stale purple ghost). (4) Tests: changing width changes the landing polygon; changing riserHeight changes riser COUNT; changing treadDepth changes going; the old mesh is disposed. §FIX-STAIR-PARAM-NO-REGEN. |
| L-216 | founder | **[MEDIUM — quality] Only 5 stair types, no railing spec, no PBR materials** — "I want at least 15 stair style types with sound materials, all of them also including the railing — hyper-realistic stairs." **CURRENT STATE (code-confirmed):** `packages/core-app-model/src/stores/StairTypeDefinitions.ts` defines exactly **5** types (Monolithic Concrete, Steel Open Riser, Timber Closed String, Residential Timber, Marble Luxury). Each carries a single `material: 'concrete' \| 'steel' \| 'wood'` STRING — there is **no railing/balustrade specification in the type at all**, and no PBR material definition. The railing is currently built independently (`StairRailingBuilder`) and is not part of the stair TYPE. | Element types / materials (C11; C04 rendering; A.24 Massing+Presentation render tiers; related to L-212 PBR floor finishes) | OPEN (queued — quality) | route -> element-types/materials agent. **Sequence AFTER L-215** (a richer type system on top of a broken rebuild path just multiplies the bug). (1) Extend the stair TYPE schema so a type declares its tread, riser, stringer AND **balustrade/railing** sub-specs (profile, material, baluster spacing, handrail section) — the railing becomes part of the type, not a separate builder decision. (2) Author >=15 architecturally-real types (in-situ + precast concrete, steel open-riser, steel + timber tread, timber closed/cut string, oak/walnut, marble, terrazzo, glass balustrade variants, floating/cantilever, helical…). (3) PBR materials shared **per TYPE, never per element** (per-element unique materials already defeat instancing — see G1) and tier-gated; textures **self-hosted** (CSP is `connect-src 'self'`). Reuse the L-212 floor-finish PBR material pipeline rather than inventing a second one. (4) Tests: every type produces a complete stair + railing; one shared material per type. §FEAT-STAIR-TYPES-PBR-RAILINGS. |
| L-217 | founder | **[HIGH] Stair creation does not work in plan view — the tool silently activates the 3D handler while the active view is an orthographic plan** — "check why the stair creation now is not working on plan view — maybe something is not wired?" **ROOT CAUSE (code-confirmed, and visible in the founder's own log).** His log shows the view is unambiguously a plan view — `dispatching "view-activated" event (mode="Top", type="orthographic")`, `view-selected (viewId="vd-sys-plan-l0")`, `[WallEdgeVisibilityService] Edge render mode set to 'plan'` — and yet the last line is `[StairPath3DToolHandler] activated in 3D (shape=L, groundY=0)`. The routing decision is made at `apps/editor/src/engine/BimService.ts:292-297`: `const inPlanView = cam ? planView2DCreationMode.isInPlanView(cam) : false; if (!inPlanView && window.stairPath3DTool) { ...activate 3D... }`. But `isInPlanView()` (`packages/core-app-model/src/views/PlanView2DCreationMode.ts:46-49`) is a **conjunction**: `camera instanceof THREE.OrthographicCamera && activePlanDrawingRef.drawing !== null`. **Its name lies about its meaning.** Per its own header it answers *"is 2D snapping available?"* (orthographic camera AND a `TechnicalDrawing` mounted) — it does NOT answer *"is the active view a plan view?"*. `SlabTool` uses it correctly, as a snap-availability guard. `BimService` misuses it as a **view-mode discriminator**, so whenever the `TechnicalDrawing` is absent the stair tool concludes "we must be in 3D" and binds the 3D sketch handler over an orthographic plan camera — pointer rays resolve against a ground plane that the plan camera does not project onto, and nothing is ever created. **And the drawing IS absent:** `apps/editor/src/engine/views/PlanViewManager.ts:251` executes `activePlanDrawingRef.drawing = null` on deactivate — a **THIRD writer**, directly contradicting that ref's own contract header (`ActivePlanDrawingRef.ts:18`: *"Write access: ViewController._mountDrawing() and ._unmountDrawing() only"*). The founder's log shows exactly that teardown (`[SvpPlanToolOverlay] Detached`, `[SplitViewManager] Split view deactivated`) immediately before the stair activation. The accompanying `GL_INVALID_FRAMEBUFFER_OPERATION: Framebuffer is incomplete: Attachment has zero size` spam (hundreds of lines, then "too many errors") indicates the drawing/render-target also fails to re-mount at a non-zero size — likely why the ref stays null rather than being repopulated. **The authoritative view mode was available all along and was never consulted:** `ViewController.viewMode` (`ViewController.ts:587`) returns `ViewMode = '3D' \| 'Top' \| 'Ceiling' \| ...` and was `'Top'` throughout. **ARCHITECTURAL FRAMING:** tool routing must be a function of **view state**, never of whether a rendering artifact happens to be mounted. This is the same class as L-213/L-214 — one element type, divergent creation paths chosen by an unreliable predicate. | View state / element-creation routing (C11 one element ⇒ one creation path; C06 UI shell & tools; DOC-5.2/5.3; SPEC-STAIR-3D-CREATION) | **FIXED (f52f71f)** — §FIX-STAIR-PLAN-ROUTING-VIEWSTATE. Root-cause chain **confirmed end-to-end**; `BimService:293` was the **sole** misuser of the predicate (`SlabTool.ts:647` uses it correctly as a snap guard). Fix: routing now consults `ViewController.currentMode` via the typed `window.viewController` seam (P4-clean, `globals.d.ts`, the same accessor `ViewsRailPanel`/`GridsLevelsRailPanel` already use); the decision is a pure, dependency-free helper `apps/editor/src/engine/stairSketchRouting.ts` so it is testable without booting `BimService`. `isInPlanView()` renamed to `is2DSnapAvailable()`. **Modes decided:** `'3D'` → 3D sketch handler (SPEC #101); `'Top'`/`'Ceiling'`/`'ceiling-plan'` → plan handlers (horizontal authoring plane); `'Front'/'Back'/'Left'/'Right'` → NOT the 3D handler (a horizontal footprint is meaningless on a vertical view). **Critical detail the agent surfaced:** the elevation presets use a **perspective** projection (`ViewController.ts:932`), so *the camera alone cannot distinguish an elevation from the 3D view* — which is precisely why routing must read the view MODE, not the camera. **⚠ AGENT CORRECTION — the orchestrator's "third writer" framing was WRONG, and its prescribed fix would have broken Canvas2D snapping.** `activePlanDrawingRef.drawing` is written at **~11 sites** inside `PlanViewManager` (`:251, 543, 596, 616, 633, 658, 782, 844, 866, 995, 1015`) — it both **sets** the ref when a drawing becomes ready and **clears** it on teardown/level-switch/invalidation. PlanViewManager is the **designed owner** of the ref for the entire Canvas2D plan-render path. `ViewController._mountDrawing()` (`:1245-1249`) **early-returns** on the Canvas2D branch, leaving `_mountedDrawing` null, so `_unmountDrawing()`'s guard can never clear the ref there — ViewController *deliberately abdicates* ownership when Canvas2D is active. The two paths are **mutually exclusive**, so there IS a clean single-*active*-writer invariant, partitioned by render path. The header was simply stale. Resolved by **correcting the header** to document both owners + the mutual-exclusion invariant; deleting the write (as the brief proposed) would have broken Canvas2D snapping outright. **Zero-size framebuffer: investigated, and it is NOT upstream of the null ref.** `PlanViewManager._hideRendererContainer()` (`:263-269`) sets the main renderer container to `display:none` while Canvas2D plan is active, collapsing the WebGL drawing buffer to 0×0; continued render passes then log the GL error. That is a **frame-loop defect — rendering is not gated on container visibility** — living in `RenderPipelineManager`/`BimWorld`/frame-scheduler, i.e. **inside the L-205 no-touch fence**, so it was correctly left alone. Tracked separately. The routing bug is fully explained by the predicate misuse alone. Gate: 3 + 6 new tests green, root tsc exit 0, 7 files. | route -> view-state/element-creation agent. (1) **Route on view state, not on snap availability.** `BimService.activateStairPathTool` must ask the authoritative view mode (`ViewController.viewMode` / the active `ViewDefinition` type), not `isInPlanView(camera)`. (2) **Rename `isInPlanView()` to what it actually means** (e.g. `is2DSnapAvailable()`), and if a genuine plan-view predicate is wanted on that class, derive it from view state. `SlabTool`'s usage is a correct snap guard and must keep working. (3) **Restore the single-writer invariant on `activePlanDrawingRef`** — either `PlanViewManager` stops writing it and defers to `ViewController._unmountDrawing()`, or the ref's contract header is amended and the ownership genuinely moved. Three writers with a header claiming two is the latent hazard that made this reachable. (4) Investigate the zero-size framebuffer — a `TechnicalDrawing` mounted at 0×0 is a real defect, not just noise (see also the `[TechnicalDrawing] Layer "A-WALL" does not exist` gap noted under L-211). (5) Tests: with an orthographic plan camera and **no** mounted drawing, stair activation resolves to the PLAN handler, not the 3D handler; a 3D perspective view still resolves to the 3D handler. §FIX-STAIR-PLAN-ROUTING-VIEWSTATE. |
| L-218 | L-214 agent (found while fixing the wardrobe; NOT founder-reported) | **[MEDIUM — latent, same defect class as L-214] The furniture-carousel drag-drop path still ships the Euler-object rotation payload** — `apps/editor/src/ui/furniture-carousel/FurnitureDragDropHandler.ts:479` dispatches `furniture.create` on the bus with `rotation: { x: 0, y: 0, z: 0 }`, an **object**, into a field the live handler declares as `rotation?: number` and guards with `Number.isFinite`. This is byte-for-byte the L-214 wardrobe defect. It has not been reported because the carousel drag-drop path is marked **deprecated**, so it is presumably little-used — but if it is reachable, dropping furniture from the carousel silently fails exactly as the wardrobe did (rejected `canExecute`, swallowed into a `console.error`, no element, no toast). **The agent flagged it and deliberately did NOT fix it**, correctly refusing to widen an out-of-fence change; recorded here instead. | Element creation pipeline / tools (C11 one element ⇒ one creation path; P6) | OPEN (queued, MEDIUM) | route -> element-creation/tools agent (small; can ride with any furniture pass). Route it through the SAME `buildFurnitureCreatePayload()` builder introduced by L-214 — that is precisely why the builder exists, and its `rotation: number` type turns this into a compile error. **First establish whether the deprecated carousel path is still reachable from the UI at all**: if it is dead, DELETE it rather than fix it (a deprecated parallel creation path for an element type is itself a C11 violation and a standing hazard); if it is live, converge it. Do not leave a third divergent `furniture.create` payload builder in the tree. §FIX-CAROUSEL-FURNITURE-ROTATION-PAYLOAD. |
| L-219 | founder (2026-07-10; escalation of **L-203**, blocks **L-208**/G3) | **[HIGH — render backend] The WebGPU/WebGL toggle produces IDENTICAL pixels because BOTH positions resolve to the same backend — `webgl-fallback`. WebGPU is never actually engaged.** Founder: *"still webgl and webgpu render the same — mistake! I think webgl is not being triggered."* Two side-by-side screenshots of the same model, toggle in different positions, are pixel-indistinguishable. **HARD EVIDENCE, from the founder's own log and from source:** (1) his log prints `[RenderPipelineManager] Phase: phase2 \| WebGPU: false \| SSGI: false \| TRAA: false` — the pipeline knows it is not on WebGPU; (2) his footer reads `GPU: … WebGL · webgl-fallback`, and per `apps/editor/src/rendering/createRenderer.ts:56-63` **`'webgl-fallback'` means WebGPURenderer using its WebGL2 backend** — it is emphatically NOT `'webgl-only'` (a plain `THREE.WebGLRenderer`, "TSL pipeline NOT available on this path"). So the toggle's **"WebGL" position never selects a WebGL renderer** — it forces the *WebGPU* renderer onto a WebGL2 backend. (3) `getRendererBackendPreference()` (`:136-144`) defaults to **`'webgl'`** when unset (founder decision 2026-06-25, §PERF-WEBGPU-FRAGMENT / ADR-0076), so the resting state of the product is the fallback path. (4) Every "working" log captured during the L-205 grey-square investigation — including the runs the founder described as rendering *perfectly* — was likewise on `webgl-fallback`. **THE ARCHITECTURAL CONSEQUENCE:** if the `'webgpu'` preference ALSO degrades to `webgl-fallback` (adapter acquisition failing, the device-loss safe-mode cap `__pryzmDeviceLossRecoveryCap` latching, or the L-203 swap-loop oscillation `resolvedPreference=webgl (forceWebGL=true)`), then **both toggle positions land on the identical renderer + identical TSL/no-TSL pipeline, and identical pixels are the CORRECT and inevitable output.** The toggle would then be a no-op UI affordance that *claims* to switch backends — worse than absent, because it makes the product look like it has a rendering choice it does not have. **This is not a cosmetic issue: it invalidates L-208.** The founder's decision "make WebGPU visibly better" (SSGI/TRAA default-on, G3) is unobservable and untestable while the native WebGPU backend is never acquired — and `SSGI: false \| TRAA: false` in his log is exactly what §PERF-WEBGL2-NO-TSL prescribes for the WebGL2 path. **Do not build G3 on top of an unverified backend.** | Rendering backend (C04 rendering & scheduling; ADR-0007 WebGPU/WebGL2 dual mode; ADR-0076 §PERF-WEBGPU-FRAGMENT; ADR-0077 §RENDERER-LIVE-SWAP; L-203; L-208) | OPEN — **DIAGNOSTIC COMPLETE (read-only, no code changed). THREE stacked defects, any ONE of which alone produces identical pixels.** **⚠ THE ORCHESTRATOR'S FRAMING WAS INCOMPLETE AND ITS IMPLIED FIX WAS WRONG — the agent refuted it.** **(i) `'auto'` and `'webgpu'` are the IDENTICAL code path.** The sole consumer of the preference is `forceWebGL = pref === 'webgl'` (`createRenderer.ts:203`); nothing anywhere branches on `'auto'` vs `'webgpu'`. The toggle has **three buttons but only two possible code behaviours** — and on hardware without a working WebGPU adapter, exactly **one** observable outcome. **(ii) `'webgl-only'` is UNREACHABLE by preference.** It occurs only if `WebGPURenderer` construction itself *throws* (`RendererHandleFactory.ts:160-173`, `:198-224` → `createRenderer.ts:425`). On any machine that can make a WebGL2 context — i.e. essentially all of them — the plain `THREE.WebGLRenderer` path documented at `createRenderer.ts:59-61` is **dead code in production**. The type's three-way framing oversells a distinction the runtime almost never makes. **(iii) THE DECISIVE REFUTATION — acquiring native WebGPU would NOT make the two positions look different today.** Even the *successful* native-WebGPU boot reaches `phase4` with **outlines only**; SSGI and TRAA are **default-OFF**, opt-in via the RenderRail (`initScene.ts:2659-2674`, §FIX-SSGI-DEFAULT-OFF-TRAA-SELECT-FLASH, founder L-59). Outlines paint only on hover/selection; shadows are the same fixed ±50 m soft shadow on both paths. So a native-WebGPU frame and a `webgl-fallback` frame differ only by TSL ScenePass/tonemap vs OBC forward render — **invisible on a static, unselected grey model.** Therefore *"WebGPU is never engaged"* is REAL but **NOT SUFFICIENT** to explain the identical pixels: the default-off post-FX gating is an equal, independent cause. **Any fix that only chases backend acquisition will reproduce the founder's "still identical" complaint.** **(iv) L-203's fix HOLDS — no localStorage latch strands anyone.** The only persisted key is `pryzm.renderer.backend` (`createRenderer.ts:96`); the device-loss safe-mode cap is deliberately **session-only** via a per-call `backendOverride`, never persisted (`:294-303`, `:332-337`) — that IS the L-203 fix. But the unset default is `'webgl'` (`:143`), so **a fresh profile never even attempts WebGPU** until the user picks it. **(v) The contract violation is in the UI PROMISE, not the plumbing.** ADR-0076/0077 are honoured; the live-swap works (`initScene.ts:3740-3944`) and correctly gates TSL on `backend === 'webgpu'` (`:3846`). The footer honestly shows the **resolved** backend in its `· <backend>` suffix (`RendererBackendToggle.ts:118-124`), but the three **buttons** highlight the **requested/persisted** preference (`:99`, `:114-116`). So the user sees `[WebGPU]` highlighted next to `· webgl-fallback`, with no explanation that the request was silently downgraded — and the WebGPU button's advertised "full WebGPU pipeline: SSGI/TRAA/shadows" (`:115`) is **false by default even on hardware where WebGPU does engage.** That juxtaposition is the lie the founder is reacting to. **STILL UNMEASURED (needs the founder):** whether `navigator.gpu.requestAdapter()` succeeds on his machine — the decisive cell, cross-referenced with `backend.isWebGPUBackend`. **PROPOSED CONTRACT (agent's Option A, recommended, for the orchestrator to write into C04 + a new ADR):** collapse the dead `'auto'`/`'webgpu'` duplication and redefine the positions as **fidelity tiers** (Stable / Balanced / Cinematic) where the top tier **forces SSGI+TRAA on** and **hard-requires** `backend === 'webgpu'` — if the adapter cannot be acquired that position renders **disabled with a reason**, never silently degraded. The label always names the config **actually in force**; a downgraded request surfaces the downgrade and why (promoting the existing honest console warning at `createRenderer.ts:442-450` into the UI). **This unblocks L-208 by construction:** choosing the top tier is the same control that turns SSGI/TRAA on, so acquisition and pipeline-gating are resolved together rather than sequentially. | route -> rendering/backend agent, **read-only until the backend is MEASURED.** After L-205 cost ten shipped root-cause attempts by reasoning ahead of measurement (C04 §SHADOW.3), the first deliverable here is **evidence, not a fix**. Establish, on the founder's actual machine: (a) does `navigator.gpu.requestAdapter()` succeed at all; (b) what `window.pryzmRendererBackend` resolves to for EACH of the three preference values (`auto`/`webgpu`/`webgl`) — the suspicion is `webgl-fallback` for all three; (c) whether the device-loss safe-mode cap or the L-203 oscillation is forcing `forceWebGL=true`; (d) whether `webgl-only` is reachable at all. **Then** decide the contract: EITHER the toggle's three positions must map to three genuinely distinct, observable renderer configurations (and the label must name the backend actually in force, never the one requested), OR the toggle is honest about degradation and surfaces WHY it degraded. A toggle whose positions are indistinguishable is a lie in the UI. Do NOT "fix" it by making the labels differ. **Sequencing: this BLOCKS L-208/G3.** §DIAG-WEBGPU-NEVER-ENGAGED. |
| L-220 | founder ("audit on ALL elements — I moved a toilet in 3D but the 2D view did not update; the sofa moves, the toilet not") | **[HIGH — correctness, SYSTEMIC] The 3D transform-drag builds a DIFFERENT command payload per element type, by hand, against an UNTYPED bus — so several element types silently fail to commit their move.** **ROOT CAUSE, printed verbatim in the founder's log:** `[TransformDrag] plumbing.move failed: CommandBusError: plumbing.move: canExecute rejected — plumbingId must be a non-empty string`. The drag dispatches (`apps/editor/src/engine/registerTransformDragHandler.ts:256-258`) `executeCommand('plumbing.move', { id, to: {…} })` — but `MovePlumbingHandler.canExecute` (`plugins/plumbing/src/handlers/MovePlumbing.ts:28-33`) requires **`plumbingId`** *and* a **`delta`** with finite x/y/z. **Both the key name AND the semantics are wrong** (absolute `to` vs relative `delta`). So the gizmo moves the mesh visually, the command is rejected, the store is never written, and the plan view — which re-projects from the store — correctly shows the toilet where it always was. The sofa moves because `furniture.updateParameters` happens to use `id`. **The code even admits it was never verified:** the comment at `:241-243` reads *"NOTE: this commit only fires if the gizmo actually attaches to the fixture on selection — pending in-browser confirmation (harmless no-op if it does not)."* It was not harmless. **A SECOND, independent defect in the same log:** `[TransformDrag] floor.update failed: Error: [Immer] minified error nr: 18` thrown from `applyPatches` — the floor drag's inverse-patch payload (`:426-431`, `_prev.boundary.polygon`) is malformed, so **dragging a floor throws**. **THE SYSTEMIC ROOT — and the reason the founder is right to demand an audit of ALL elements:** the id key is ad-hoc per command (`plumbingId`, `beamId`, `floorId`, bare `id`…) and the dispatch site is **stringly-typed with an `unknown` payload**: `packages/runtime-composer/src/types.ts:3394` declares `executeCommand(type: string, payload: unknown): unknown`. A fully-typed `CommandRegistry` **already exists** (`packages/command-bus/src/commands.ts:4` — *"The typed contract for every command dispatched through `runtime.bus.executeCommand`"*) and **`runtime.bus` throws that typing away.** Every payload mismatch is therefore a *runtime* rejection instead of a *compile* error. **This is the THIRD founder-visible bug of this exact class in one day** — L-214 (wardrobe: Euler object into a scalar field), L-218 (carousel: same), now L-220 (plumbing: wrong key + wrong semantics; floor: malformed patch). They are one defect wearing three costumes. | Command bus typing / element transform (C03 §commands+state; C16 command authoring; P6 commands are the only mutation path; C11) | **FIXED (99f90411)** — §FIX-TRANSFORM-DRAG-PAYLOAD-AUDIT. **⚠ BOTH of the orchestrator's root causes were WRONG; the agent refuted them from source and fixed the real ones.** **PLUMBING:** the `{id, to}` payload was **CORRECT** for the legacy `MovePlumbingCommand` (takes `{id,to}`, translates a bath's start/end by the delta, updates the **geometry** `window.plumbingStore` the plan reads). The bug: a plugin `MovePlumbingHandler` (`{plumbingId,delta}`) was registered FIRST and **shadowed** the bridge (`initBusHandlers` skips on `registry.has`), rejecting `{id,to}` — AND that plugin handler mutates a **detached plugin DTO store** (`PluginRegistry.ts:351 new PlumbingStore()`) with **no bridge to the geometry store**, so the brief's "send `{plumbingId,delta}`" fix would have passed `canExecute` and **still left the 2D plan stale**. Fix: gave the working legacy bridge a distinct un-shadowed type `plumbing.moveFixture`; the drag dispatches `{id,to}` — the exact path furniture uses. **No handler guard weakened** (`MovePlumbing.ts` untouched). **FLOOR / COLUMN / BEAM (Immer 18):** immer 10.2.0 error 18 = *"Cannot apply patch, path doesn't resolve"* — **NOT a malformed patch.** The bridge declares `affectedStores:['floor']` (correct — ring-buffer undo needs it), but that also drives `attachStores` (`packages/stores/src/attachStores.ts:36`) to re-apply the FORWARD patch to the bus's `floor` store, which is a **detached empty `new FloorStore()`**; `Store.applyPatch` runs `applyPatches({}, [replace /floorId/boundary])` → id doesn't resolve → throw → the whole `executeCommand` rejects. Column/beam are structurally identical. **The matrix test missed it because it wired NO `attachStores`** — the exact gap. Fix: hardened base `Store.applyPatch` to skip a nested (`path.length>1`) patch whose root id is absent (a no-op), **mirroring `ElementStore.applyPatch`'s existing `current !== undefined` guard** — a genuine latent inconsistency between the two Store impls; real plugin handlers (ids guaranteed present by `canExecute`) unaffected. **P3 (close the class):** added `dispatchTyped(bus,type,payload)` to `@pryzm/command-bus` keyed to the existing `CommandRegistry` + a `TransformDragCommands` group; migrated all 11 drag sites via a `dragDispatch()` wrapper → a wrong payload for a known command id is now a **compile error** (verified: `{plumbingId,delta}` → `TS2353`). **Honest correction:** a typed *overload* on `executeCommand` CANNOT enforce this — the loose `(string,unknown)` overload always matches and swallows the error; the correct incremental seam is a typed **wrapper** (L1 `command-bus`), which is what was built; non-drag dispatches stay on the loose path (intended residue). **P4:** `dragDispatch` raises a `pryzm:toast` on rejection, never a swallowed `console.error`. **NEW DEFECTS FOUND → L-229:** slab / grid / lighting / opening have **no 3D-drag branch at all** (dragging them silently does nothing); ceiling commits but sets no `_recordUndo` (3D-drag ceiling move not ring-captured). Gate: `@pryzm/stores` 745 pass (+4 new), matrix test 14 pass (+ a production-wiring regression that wires `attachStores`+empty Store and asserts `floor.update` no longer throws), command-bus 26 pass; every fenced file tsc-clean (the sole root-tsc error is `CesiumViewport.ts:5346 normalizeFacadeStudy` — a FOREIGN file mid-edited by the live L-227 agent, not this fence). Commit `99f90411`, 7 files, not pushed by agent. | route -> command-bus/transform agent. **(1) AUDIT EVERY ELEMENT TYPE** in `registerTransformDragHandler.ts` — wall, slab, column, beam, floor, ceiling, roof, furniture, plumbing, lighting, stair, curtain-wall, grid, opening — and for each, prove drag → command → store → **plan re-projection** end-to-end. Produce a table: element × id-key × payload shape × handler expectation × pass/fail. **(2) Fix `plumbing.move` (`plumbingId` + `delta`) and `floor.update` (the Immer patch).** **(3) THE REAL FIX — close the class, do not patch the instances.** Type `runtime.bus.executeCommand` against the existing `CommandRegistry` so `executeCommand<'plumbing.move'>(…)` type-checks its payload at every call site. `runtime-composer/src/types.ts:3394`'s `payload: unknown` is the defect; `command-bus/src/commands.ts` already holds the contract it should reference. Then every one of these bugs — past and future — becomes a **compile error**, exactly as L-214's `buildFurnitureCreatePayload()` did locally. Do this incrementally (typed overload alongside the loose one, migrate call sites) — a big-bang retype of every dispatch is unreviewable. **(4)** A rejected `canExecute` on a user drag must **surface** (toast), never a swallowed `console.error` — same rule L-214 established. **(5)** Tests: for each element type, a drag commits a store change AND the plan view re-projects. §FIX-TRANSFORM-DRAG-PAYLOAD-AUDIT. |
| L-221 | founder | **[MEDIUM — quality] Plumbing fixtures have NO 2D symbol: plan and elevation draw the raw triangulated mesh edges, producing hundreds of lines per toilet** — "the existing toilets, showers etc. elevations and plan view are TRUE PROJECTIONS — somehow toooo many lines. Please make sound quality plan view and elevation projections for all plumbing fixtures." His screenshot shows toilets/basins/taps rendered as a dense scribble of triangulation edges rather than clean architectural symbols. **CONFIRMED IN CODE AND IN HIS LOG.** Every other element type that needs a 2D representation has a dedicated symbol builder: `DoorPlanSymbolBuilder`, `WindowPlanSymbolBuilder`, `ColumnPlanSymbolBuilder`, `WallLayerPlanSymbolBuilder`, and for furniture `SofaPlanSymbolBuilder`, `BedPlanSymbolBuilder`, `ChairPlanSymbolBuilder`, `KitchenPlanSymbolBuilder`, `WardrobePlanSymbolBuilder`, `TreePlanSymbolBuilder`. **There is NO `PlumbingPlanSymbolBuilder`.** So plumbing falls through to `EdgeProjectorService`'s generic true-edge projection of the full 3D mesh. His log measures the cost exactly: `§DIAG-EPS-01 edgesGeo elemType=PlumbingFixture faceCount=3916 edgeVertices=6552 allocMs=20.60ms` (six such meshes for ONE toilet, 2108/1996/992/2396/1596/3916 faces), and `HiddenLineRemoval` segment count jumps **894 → 11,706** the moment the fixture is placed. **This is a correctness issue for drawings, not only aesthetics:** an architectural plan shows a toilet as a standardised symbol (bowl outline + cistern rectangle), not a wireframe of its mesh. It is also a real perf cost (~55 ms of edge extraction per fixture, 11k segments through hidden-line removal). **The founder explicitly asks for BOTH plan AND elevation** — note the existing builders are plan-only, so elevation symbology is a genuine extension, not a copy. | Documentation / plan+elevation symbols (C06; DOC-2.x; C11; related L-211 layered-wall plan lines) | **FIXED (3125c377)** — §FEAT-PLUMBING-PLAN-ELEV-SYMBOLS. **The suppression mechanism DID exist** and was reused verbatim: `mesh.userData.skipInPlan === true`, set by each 3D builder, checked at `EdgeProjectorService.ts:1923` (`if (isPlanView && mesh.userData.skipInPlan === true) return;`). Doors/sofas/kitchens/beds/wardrobes/trees all use it and do **not** double-draw. `PlumbingFragmentBuilder` now tags every fixture mesh `skipInPlan: true`, so the 11,706-segment HLR load **goes away at source** rather than being overdrawn. **⚠ THE ORCHESTRATOR'S FIXTURE LIST WAS WRONG.** The real union is `packages/geometry-plumbing/src/PlumbingTypes.ts:13` → `'toilet' | 'sink' | 'urinal' | 'bidet' | 'bath' | 'shower' | 'accessory'` — **seven** types. There is **no `tap/mixer` and no `cistern` fixture type**; those are sub-parts of the toilet/shower meshes. "Basin" is `sink`. `accessory` is a family of 6 variants; toilet has 4 (`TOILET_VARIANTS`), shower 7 (`SHOWER_VARIANTS`). All authored. **ELEVATION WAS A GENUINELY NEW SEAM** (P3, the judgement call): `skipInPlan` was plan-only, and every prior symbol builder is plan-gated, so nothing suppressed the mesh in elevation. The agent added a symmetric `skipInElevation` flag checked immediately after the plan check and **scoped to elevation only — sections are deliberately left alone, because a section legitimately CUTS the fixture.** It chose option (b): a new `PlumbingElevationSymbolBuilder` emitting a simplified silhouette + profile derived from the fixture's **parametric footprint**, not its triangulated mesh and **not** the plan symbol. It emits a FRONT profile (stepped silhouette, X-Y) plus a bounding SIDE profile (Z-Y), so any of the four cardinal elevations reads correctly — whichever profile faces the viewer is the silhouette, the perpendicular one collapses to a clean vertical line, and no view-direction lookup is needed inside the pure builder. A test explicitly asserts **elevation linework ≠ plan linework** (no silent reuse — the brief's stated hazard). **MEASURED RESULT:** before — 6 meshes edge-extracted per toilet (2108/1996/992/2396/1596/3916 faces), ~55 ms, HLR 894 → 11,706 segments. After — 0 ms edge extraction for the fixture; plan ≤ 34 segments (cistern rect + bowl D-outline + seat D), elevation ≤ 13. Tests assert hard upper bounds of 80 (plan) / 60 (elevation) across every type × variant, so the *unbounded triangulation* regression cannot return. Gate: geometry-plumbing 16/16, root tsc exit 0, 7 files (+751/−2). **NEW DEFECT FOUND EN ROUTE → L-225.** | route -> documentation/plan-symbols agent. (1) Author a `PlumbingPlanSymbolBuilder` for every fixture type (toilet, basin, shower, bath, bidet, urinal, tap/mixer, cistern), following the established `SofaPlanSymbolBuilder`/`DoorPlanSymbolBuilder` pattern **exactly** — do not invent a second symbol mechanism. (2) **Suppress the generic true-edge projection for any element that has a symbol builder.** Establish how door/window/sofa already do this (they must, or they would double-draw) and reuse it; the 11,706-segment HLR load must go away, not merely be overdrawn. (3) **Elevation symbology is new** — the existing builders are plan-only. Decide, and record in C06/DOC-2.x, whether elevation gets its own symbol set or a simplified silhouette+profile projection. Do not silently make plan symbols appear in elevation. (4) Symbols must follow architectural convention (a toilet is a bowl outline + cistern, not a mesh trace) and scale with the drawing, not the model. (5) Tests: a placed toilet contributes a bounded, deterministic number of plan segments (assert an upper bound — the regression is unbounded triangulation); the generic edge path is not invoked for fixtures with a symbol. §FEAT-PLUMBING-PLAN-ELEV-SYMBOLS. |
| L-222 | founder ("the elevations are not sound — really good to be honest — but when the user moves the crop view the live elevation updates, but flickering a bit. Would it be possible to update the performance and make it more organic, like flowing?") | **[MEDIUM — perf + correctness] Live crop-drag re-projects the ENTIRE elevation twice per pointermove, at two different clip depths, then knowingly displays the STALE one.** The founder reads this as flicker. It is four separate defects, all named in his own log. **(1) TWO commands per pointermove.** Each tick executes `UPDATE_VIEW_DEFINITION` **and** `SET_VIEW_CROP` (`SetViewCropCommand`), and each independently invalidates `ViewTechnicalDrawingCache` and drives a full `EdgeProjectorService.project()` + `HiddenLineRemoval` pass. Double work, every frame. **(2) The two projections disagree.** The paired `project()` calls in one tick carry **different `far` values** — e.g. `far=3.669` then `far=2.557`; `far=12.135` then `far=28.895` — because `resolveClipRange()` derives elevation depth from the crop, and the first projection still sees the PREVIOUS crop's depth. So each tick renders two different drawings. **(3) The stale one sometimes wins.** `[ViewTechnicalDrawingCache] §FIX-PLAN-BLANK-STALEGEN — accepting a stale projection into an EMPTY cache to avoid a blank view: staleGen=1206 currentGen=1209` fires on **every single tick** (1206/1209 → 1212/1215 → 1218/1221 …, generation advancing by 3 per drag step). That guard exists to avoid a blank view, and it is honest about what it does — but during a drag it means the displayed drawing is periodically the OLDER of the two competing projections. **That is the flicker mechanism, printed in the log.** **(4) The edge cache is 100% useless during the drag.** `§PERF-CACHE-STATS … cacheHits=0 cacheMisses=18 hitRate=0%` on every tick, because the cache key includes the crop-derived `far`. **But moving a crop does not change any element's edge geometry** — it changes only the clip range and which segments are classified `proj` vs `beyond` (dashed). Re-extracting all 18 groups' edges per mouse-move is pure waste; the 0% hit rate is a cache-key defect, not a law of nature. **ARCHITECTURAL FRAMING.** A crop drag is ONE user gesture, not N commands. Per **P6** the *commit* is a command (one undo entry, on pointer-up); the *live preview* between pointer-down and pointer-up is view state, not a mutation, and must not round-trip the command bus + undo stack 60×/s. Per **P3** all animation/refresh coalesces through the single frame bus — at most one projection per frame, superseded ticks dropped. And a live view must **never regress to an older generation**: hold the last good drawing until the new one is ready (the flicker is a *correctness* bug about ordering, not a *speed* bug). **`FastPathProjectorService` ALREADY EXISTS** and boots (`[main] FastPathProjectorService initialized (sub-50ms interactive projection)`) — the "organic, flowing" behaviour the founder wants is progressive refinement: fast path while dragging, full-quality projection on settle. | Views / documentation projection + scheduling (C04 §scheduling, **P3** single rAF / frame bus; **P6** commands are the only mutation path; C06; DOC-1.4/1.8) | **FIXED (078fa550)** — §PERF-ELEV-CROP-DRAG-FLOW. Root cause D1/D2 confirmed: the "crop drag" is a section/elevation **scope-box depth-handle** drag; `PlanViewInteraction._applyScopeDragFromPointer` (`:1139-1157`) fired **two** legacy `commandManager.execute` commands per throttled pointermove (`view.updateDefinition` + `view.setCrop`), each dispatching `vd:view-updated` → `PlanViewManager._onViewUpdated` (`:855`) reprojected on EACH → two projections per tick; the first (from `updateDefinition`, before `setCrop` ran) saw the **stale** `far` via `resolveClipRange` (`EdgeProjectorService.ts:2856`) — **so D2 is caused by the two-command split, and P1 subsumes P3** (one atomic write ⇒ one projection ⇒ one consistent `far`). Fix: live drag now does a **direct view-state write (0 undoable commands)**, committing ONE `view.updateDefinition` on pointer-up (**undo entries per gesture: dozens → 1**); projections frame-coalesced (superseded dropped); the drawing cache holds the **warm** drawing on a generation bump (`ViewTechnicalDrawingCache.bumpGeneration`) so a superseded projection hits the reject path, never the stale-accept path — **displayed generation is now monotonic** (the flicker was non-monotonic display), and the camera no longer refits every tick. **§FIX-PLAN-BLANK-STALEGEN (L-90) PRESERVED** — the empty-cache blank-view guard is untouched and green (4/4); only the drag-time stale *display* was removed. **P6 (FastPathProjectorService) NOT routed** — it is hard-gated to `OrthographicCamera` plan previews (`:149`) and draws raw `EdgesGeometry` to an overlay; elevations render `OBC.TechnicalDrawing` into the Canvas2D pane, and its raw-edge output would change the look mid-drag (forbidden). "Flowing" delivered by the coalesce + hold-last-good instead. **⚠ ORCHESTRATOR CORRECTION — P5 (edge-cache key) was a WRONG root cause; the agent refuted it and deferred rather than regress.** `_cwProjectionCache` does NOT store crop-invariant edge geometry — it stores **post-classification drawing-space geometry** (proj-vs-`beyond` banding under the crop-derived `far`), and the `clipSignature` including `far` **IS L-202's correctness fix** (`:1198-1250`); removing `far` from the key would replay geometry classified under the OLD crop and reintroduce the L-202 defect. The sound version is a **two-level cache** (crop-invariant extraction keyed on element+version, then cheap per-crop re-classification) — a real refactor, deferred → **L-228**; the `hitRate=0%` during drag is now harmless because the warm drawing renders every frame. Gate: core-app-model views 49/49 (incl. preserved planBlankStaleGen 4/4), geometry-plumbing 16/16, root tsc exit 0, 6 files. | route -> views/projection agent. **Sequence AFTER L-221** — that agent currently owns `EdgeProjectorService`. (1) **One gesture ⇒ one command.** Collapse the `UPDATE_VIEW_DEFINITION` + `SET_VIEW_CROP` pair; commit ONE `SET_VIEW_CROP` on pointer-up (one undo entry). Live drag updates view state / the projector directly, not the command bus. (2) **Coalesce to the frame bus (P3):** at most one projection per frame; drop superseded ticks; never run two projections for one tick. (3) **Fix the clip-range ordering** so a projection can never run against the previous crop's `far`. (4) **Never display a stale generation.** Replace the `§FIX-PLAN-BLANK-STALEGEN` drag behaviour with hold-last-good-drawing (double-buffer): swap only when the new generation is ready. Keep the blank-view guard for the genuine cold-cache case it was written for — do not just delete it. (5) **Fix the edge cache key** so it does NOT include the crop-derived `far`: element edge geometry is crop-invariant; the crop affects clipping + `proj`/`beyond` classification only. Target a high hit-rate during drag. **This is the biggest single win and it is deferred until L-221 lands (`EdgeProjectorService` fence).** (6) **Progressive refinement:** drive the drag through the existing `FastPathProjectorService`, settle to full-quality `EdgeProjectorService` + `HiddenLineRemoval` on pointer-up. (7) Tests: one drag gesture ⇒ exactly one committed command + one undo entry; N pointermoves ⇒ ≤ N projections and never two per tick; the displayed generation is monotonic (never decreases); edge-cache hit-rate > 0 during a pure crop drag. §PERF-ELEV-CROP-DRAG-FLOW. |
| L-223 | founder ("audit the visibility intent full and complete architecture — orchestration, element, and compliance with contracts. The concept is sound but I believe it is not connected and wired with the actual real view properties… the elevation view modifiers look just like placeholders… ideally if the user changes the colours/values the view should update, and the user should be able to change the values — at the moment I click a colour but can't change it") | **[HIGH — architecture audit] Visibility Intent is a partially-wired subsystem: its authoring surface is fully disabled for every intent the user actually has, and the drawing pipeline that produces the correct elevation appears not to read it at all.** Two grounded findings, plus one duplicate-module hazard. **(1) THE COLOUR PICKER IS DISABLED BY DESIGN, and nothing says so.** The live panel is `apps/editor/src/ui/VisibilityIntentPanel.ts` (1155 lines). It stamps `${intent.isSystem ? 'disabled' : ''}` onto **every** input — **17 `isSystem` gates** (name `:140`, description `:141`, visible `:249`, line.colour `:265`, line.opacity `:267`, line.style `:269`, fill.style `:274`, fill.colour `:280`, fill.opacity `:284`, symbolicRule `:286`, the 3D-surface section `:326-330`, `Add Modifier` `:399`/`:481`, …). **All four intents in the founder's sidebar are `· system`** ("Architectural Documentation (Auto) · system", "Architectural Plan — Current Level Only · system", "Clean Presentation · system", "Structural Coordination · system"). So the entire form is inert for every intent that exists out of the box. The `Duplicate` button is the only escape hatch, and the UI never explains that. **This is a UX-honesty defect of the same family as L-219's renderer toggle: a control that looks editable, is not, and gives no reason.** **(2) THE MODIFIERS APPEAR NOT TO DRIVE THE REAL ELEVATION.** The elevation's correct pen weights are produced by `packages/core-app-model/src/drawing/HiddenLineRemoval.ts` (§ELEV-LINEWEIGHT-02 / §ELEV-LINEWEIGHT-03, L-190/L-196) — and a grep of that module finds **no reference to the visibility-intent store or to `IntentStyle`**. Meanwhile `[VGSceneApplicator] DOC-1.13 applyToProjectionLayers() — applied=9/14 layers` proves the intent system IS applied to *projection layers*, and `[IntentStylePrewarmer] Pre-warmed 1632 style slots` proves a style cache is populated. **So the subsystem is genuinely half-connected**: layer visibility flows; per-view-type CUT/BEYOND/HIDDEN/PROJECTION appearance (line weight, colour, fill) apparently does not reach the drawing that HLR emits. `applied=9/14 layers` also means **5 layers are never applied** — unexplained. This is exactly the founder's read: *"the ones on the view intent look just like placeholders."* **(3) DUPLICATE MODULE HAZARD.** There are **two** classes named `VisibilityIntentPanel`: the real one above, and `apps/editor/src/ui/visibility/VisibilityIntentPanel.ts` — a **50-line Phase-F stub** whose own header states *"Phase F stub: evaluates all elements as visible; Phase 3A (Wave 19, S114-WIRE) wires the real manifest-honoured evaluation."* `initUI.ts:92-93` lazily imports the 1155-line one; the stub is a leftover. A stub that claims to evaluate visibility, sitting beside the real panel under the same class name, is a live trap. **(4) The panel mutates via the LEGACY path.** Seven `window.commandManager` sites (`:764, 785, 883, 913, 926, 1038-1039`), each carrying `TODO(E.5.x): legacy commandManager — replace with runtime.bus.executeCommand`. Note `npm run check:commandmanager` is a CI guard against exactly this. | Visibility intent (**P7** — visibility intent is a DOMAIN concept, not UI state; **C09** AI & visibility-intent; C06 UI shell; C03/C16 commands; DOC-1.13 projection layers; P6) | **PHASE A AUDIT COMPLETE (read-only, no code changed) — AWAITING FOUNDER DECISION before Phase B.** **⚠ THE ORCHESTRATOR'S ROOT CAUSE ("HLR never reads the intent store") WAS A RED HERRING; the agent refuted it from all 668 lines of `HiddenLineRemoval.ts`.** HLR does **occlusion only** (hidden-segment removal + `:proj`→`:beyond` reclassification, §ELEV-LINEWEIGHT-02/03); it has never held pen appearance, so its ignorance of the intent store is irrelevant. **THE REAL ROOT CAUSE IS A TWO-AUTHORITY COLLISION.** The intent store **is** wired into the elevation pen resolver — `GraphicsRulesEngine.resolveStyle()._intentRules()` (`GraphicsRulesEngine.ts:187-218`) reads `visibilityIntentStore` + `viewIntentInstanceStore` and produces a priority-1000 pen over the priority-0 `PenWeightTable`, computing intent width/colour/opacity/dash into `_pen`. **BUT the on-screen elevation renderer is `PlanViewCanvas` (Canvas2D — `ViewController.ts:959-967` routes elevation/section to `PlanViewManager`→`PlanViewCanvas`, NOT the OBC THREE scene), and at `PlanViewCanvas.ts:321-329` it OVERWRITES `_pen.color` with `vgEdge` and `_pen.widthMm` with `vgLineWeight`** — both sourced from the **legacy `vgGovernanceStore`** (`PlanViewManager.ts:669-686`), which returns a non-null value for every real category, so the override **always fires.** Net: intent **opacity + dash + the door/window symbolic path survive to screen; intent WIDTH + COLOUR are computed then silently discarded.** The elevation weights the founder admires come from `PenWeightTable` via `vgGovernanceStore`, not from the intent panel he is editing. **The panel edits the LOSING authority — and can't even do that, because all four shipped intents are `isSystem` → all 17 inputs `disabled`.** **A3 CORRECTION:** the values are NOT `inherit` placeholders — system intents are seeded with concrete `PenWeightTable` values from `SystemIntents.ts` (`VisibilityIntentStore.ts:20-23`); `VGToIntentMigration`'s "0 intents migrated" is a no-op (no legacy user templates), NOT the cause. They *look* like placeholders because they are disabled/greyed and because the elevation-specific weights live in the (also-disabled) **View Modifiers** tab, not the base **Element Rules** form on screen. **A1:** `applied=9/14` is expected — `VGSceneApplicator` iterates 14 DXF categories; a layer exists only if EdgeProjectorService projected ≥1 element of that type into the view, so the 5 skipped (furniture/plumbing/ceiling/handrail/a curtain-alias) simply had no geometry in the south elevation. Not a bug, and the legacy VG path, not the intent path. **A4 contract breaches:** **P7/C09** split-brain (two style authorities for one domain fact; `VGSceneApplicator` is self-marked `@deprecated`, "will be replaced by IntentSceneApplicator"); **P6** — 7 `cm?.execute?.` sites, and `check:commandmanager` **silently misses them** because it scans only `packages/`+`plugins/` for the literal `commandManager.execute`, while the panel is in `apps/` and writes `cm?.execute?.` (a real gate blind-spot to record); **P1** — the 50-line stub panel has zero importers (safe to delete). **THE B2 SEAM (the real fix, needs a decision):** invert/retire the VG override so the intent-inclusive `_pen` is authoritative for width+colour and `vgGovernanceStore` is demoted to fallback (or removed for elevation/section) — this IS the `IntentSceneApplicator` supersession `VGSceneApplicator`'s own header promises — **plus** a per-view "assign intent" affordance (the panel has none; `viewIntentInstanceStore.assign` is only called by collab/migration). **B4 caveat:** 3 of the 7 legacy sites (`BulkApplyAppearanceCommand`, `Copy/PasteAppearancePatch`) have **no bus handlers at all**, and the panel is constructed with a **null runtime** (`initUI.ts:596`), so B4 is not mechanical. **Unlocking the swatches without B2 would be the exact L-219 lie-in-the-UI** (edits still overwritten by VG). **→ FOUNDER DECISION REQUIRED: retire the legacy `vgGovernanceStore` style authority for elevation/section (the `IntentSceneApplicator` migration) — scope + timing.** **[UPDATE 2026-07-10 — Phase B halted at B0; the "retire VG everywhere" decision rests on a FALSE premise that the B0 oracle refuted.]** The agent built an executable oracle (`packages/core-app-model/src/drawing/IntentAuthorityOracle.test.ts`, passing, uncommitted) driving the real stores over 12 categories × 3 zones × 3 view types = 108 cells. **102 of 108 change** if VG is retired — plan, elevation AND section all shift. My convergence premise was wrong at the source: **`vgGovernanceStore` never used the pen table.** VG computes `width = max(hairline, lineWeight × hairline)` (flat integer, default → 2px CUT for every category, DPR-dependent), black projection, `#9ca3af` beyond. The intent pen computes `widthMm × 3.779` (category-specific, DPR-independent) with the §ELEV-LINEWEIGHT ×1.5 elevation ladder and category-coded projection greys. Different unit bases, never calibrated. **THE KICKER: the elevation the founder calls "correct" is produced BY VG — the authority being retired.** `elevation/wall/CUT` today = flat 2px (VG); the intent seeds resolve it to 2.835px. So "make the panel authoritative" and "keep the elevation exactly as it looks" are in direct conflict. The panel's "placeholder" values are the *more* AEC-correct pen ladder, simply not yet the authority. **DECISION MENU (agent's §5): (A) accept the new pen-ladder appearance — arguably more correct (true cut>proj>beyond ladder + depth greys); re-baseline the oracle as truth [agent + orchestrator recommend the founder EYEBALL this before rejecting]. (B) recalibrate the system-intent seeds to reproduce VG exactly — true no-pixel-change, but ports VG's cruder flat look into the seeds and discards the pen ladder (a downgrade wearing a no-op badge). (C) keep VG as the resolver but make the intent panel its single WRITE surface — today's pixels, larger design change. (D) retire VG but first tune `SCREEN_PX_PER_MM`/pen mm-values to land within tolerance of VG on width, accepting only the projection-colour change.** **B3 (stub delete — zero importers re-confirmed) and B4 (bus migration + `check:commandmanager` gate-fix) are pixel-neutral and can proceed under ANY option.** Agent halted per its stop-instruction; no render path touched, nothing committed. **AWAITING FOUNDER PICK (A/B/C/D).** | route -> visibility-intent agent. **Deliverable 1 is the AUDIT the founder asked for, not a patch.** Trace end-to-end and produce a wiring map: `VisibilityIntentStore` / `VisibilityIntentTypes` / `VisibilityIntentDefaults` → `viewIntentInstanceStore` → `IntentStylePrewarmer` → `VGSceneApplicator.applyToProjectionLayers()` → `EdgeProjectorService` → `HiddenLineRemoval` → the rendered elevation. **For each hop state whether appearance data (line wt / colour / fill / visible, per CUT|BEYOND|HIDDEN|PROJECTION, per view type) actually flows, or stops.** Then: (a) why `applied=9/14` — name the 5 layers and why they are skipped; (b) does ANY intent appearance field reach HLR's elevation lineweight decision, or is `§ELEV-LINEWEIGHT-03` wholly independent? (c) is `VGToIntentMigration` leaving the four system intents in a state where every field is `inherit`, so the panel would show placeholders even if editable? **Only then** fix, in this order: **(1)** system intents must be **visibly read-only with an explanation + a prominent "Duplicate to edit"** — do NOT simply unlock system intents (they are the shipped defaults; a user editing them in place has no way back, and P7 makes them domain state, not preferences). **(2)** Make a user-owned intent's appearance edits actually reach the elevation/plan — that is the real request. If the pipeline cannot express per-view-type appearance today, say so and propose the seam rather than faking it. **(3)** Delete or rename the 50-line stub panel (P1 single composition root — no parallel wiring). **(4)** Migrate the 7 `window.commandManager` sites to `runtime.bus.executeCommand` (P6; `check:commandmanager` guard). **Non-goal:** do not make the swatches *look* editable while the value still cannot reach the view — that is the L-219 lie-in-the-UI failure mode, restated. §AUDIT-VISIBILITY-INTENT-WIRING. |
| L-224 | founder ("audit the project isolation end to end — when the user opens a project, works on it, goes back to Projects and either creates a project or opens another, there is a *reminiscencia* from the previous one. It is not a proper start-from-scratch concept") | **[HIGH — data integrity] Project isolation has a guard (Contract 48 `ProjectIsolationAudit`), and the guard is blind to exactly the case the founder hit.** Two **structural** findings, established in code, plus suspects to be measured. **STRUCTURAL FINDING 1 — the audit never runs when you OPEN another project.** `packages/core-app-model/src/persistence/ProjectIsolationAudit.ts:144`: `if (detail.empty !== true) return; // only audit fresh / cleared projects`, and its own header (`:38-39`) states *"The audit runs only on `empty:true` loads to keep the false-positive rate at zero — non-empty loads legitimately bring back saved geometry."* The founder's scenario is *"go back to Projects and either CREATE a project or OPEN another"*. **Creating is audited. Opening another project is never audited at all** — the coverage hole is precisely the reported bug. The "false-positive" reasoning is sound for *geometry* (a loaded project legitimately has walls) but it throws away all coverage of the surfaces where leakage actually shows: stores, caches, graphs, registries, undo history. **STRUCTURAL FINDING 2 — the audit inspects the THREE scene and TWO window globals. It does not inspect a single store.** `runAudit()` (`:111-129`) checks only: underlay meshes, IFC groups, DXF overlays, surviving BIM meshes (`inspectScene`, `:63-94`), and `window.floorPlanUnderlayTool` + `window._ifcServerUploadIds` (`inspectGlobals`, `:96-109`). Boot logs `StoreRegistry: 37 stores registered — visibility-intent, view-intent-instance, vg-governance, view, phase-filter, RoomRequirement, title-block, schedule, user-material, sheet, view-template, AssetCatalogEntry, wall, slab, column, beam, stair, …, annotation`. **A stale schedule, sheet, view-template, annotation, room-requirement or visibility-intent carried from project A would pass this audit "✓ loaded clean".** Nor does it inspect `SemanticGraph` / `TemporalGraph` / `RoomGraphService`, the `ViewTechnicalDrawingCache` / NME / EdgeProjector caches, `ElementRegistry`, `BimManager`'s level registry, the selection, or the undo ring-buffer. The guard is a scene-graph guard wearing the name of a project-isolation guard. **SUSPECT (real evidence, not yet proven causal) — the UNDO RING-BUFFER survives the switch.** The founder's log shows `[elementUndoStoreAdapter] skip remove — not found in store: furniture_01KX68Z34JZZNQBBQ528BEKAH3` and `… plumbing_01KX68T0455A479V3YP5VYWD9P`, while the fixture he had just created in the current project carried id `9e44427e-9bbf-4224-834b-9558616bab7f`. **Two different id namespaces in one undo stack.** Ctrl+Z was reaching for elements that do not exist in the open project. **SUSPECT — collaboration catch-up replays across the switch.** `[initCollaboration] Catch-up: requesting commands since 2026-07-10T14:53:36.440Z` → `Catch-up: replaying 28 missed command(s)` → `Applied remote command: ADD_OPENING` ×N, immediately after a project switch, with `§DUPLICATE-ROOMS-PERSIST — skipping already-applied create` firing 21 times. Several `ADD_OPENING` replays were then **rejected** by `CreateWallOpeningCommand.canExecute` as overlapping an identical existing opening (`new=[1.561,2.487] vs existing … [1.561,2.487]`) — i.e. the replay tried to re-add openings that were already there. **NOT A LEAK (checked, and I was wrong to suspect it):** `[UnderlayPersistence] Clear skipped — no current project bound` is **deliberate and correct** — `clearPersistedUnderlay` (`apps/editor/src/engine/UnderlayPersistence.ts:112-124`) documents *"No-op when no project is bound, which is exactly what we want during project-switch teardown — the outgoing project's record must survive for next visit."* Recorded so nobody re-litigates it. | Persistence / project lifecycle (**Contract 48** project isolation; C02 composition-root & boot; C03 stores; P1) | **FIXED (51c3109a) — Phase B done.** **PHASE A AUDIT COMPLETE (read-only) — ROOT CAUSE FOUND, both orchestrator suspects REFUTED, Phase B GREENLIT.** **THE REAL ROOT CAUSE — a bus mismatch, not a missing clear.** The `F.events` migration re-pointed the EMITTERS of `pryzm-project-switch` / `pryzm-project-loaded` to the typed in-memory `runtime.events.emit()` (`PlatformShell.ts:161/192/198/386`, `PlatformVersionController.ts:417`; `EventBus` at `composeRuntime.ts:754` does NOT touch `window`), **but left six isolation-critical listeners bound with `window.addEventListener` — so they are DEAD and never fire:** `ProjectLifecycleController.ts:48` (**the entire C13 §4 teardown owner** — `batchCoordinator.forceReset()`, wall-rebuild reset, `clearUndoStacks()`), `ProjectIsolationAudit.ts:142` (**the tripwire itself**), `ProjectScopedStorage.ts:60/67`, `ConstraintEngine.ts:111`, `AmbientIntelligence.ts:118/121`, `ImportManagerPanel.ts:99`. ~30 OTHER listeners were migrated to `runtime.events.on()` and DO fire, so the bus works — these six were orphaned. **Consequence: the teardown controller written specifically to prevent this class of bug has not run since the migration, and the audit that should have surfaced the leak observes NOTHING** (no `✓ loaded clean`, no `[CONTRACT 48 VIOLATION]` ever logged). So the reminiscencia is real, and it is `BatchCoordinator` / wall-rebuild / `ConstraintEngine` / `AmbientIntelligence` state surviving the switch. **⚠ BOTH ORCHESTRATOR SUSPECTS REFUTED WITH EVIDENCE. S1 (undo ring-buffer):** every load routes through `ProjectLoader.load` whose `finally` (`:2079-2083`) calls `clearHistory()` + `runtime.bus.clearUndoStacks()` (ring + legacy), so A's entries are gone before B is interactive; the `skip remove … furniture_01KX…/plumbing_01KX…` lines are within-B ring-apply failures (two creation code-paths, ULID vs UUID — NOT two projects). The dedicated switch-time undo clear IS dead, but the load-time clear makes it redundant (a backstop gap, not a live leak). **S2 (collab catch-up):** catch-up requests B's OWN projectId with a per-project `sessionStorage` cursor; replayed commands dispatch `source:'REMOTE'` → `suppressUndo`, never touching the ring; the 21 duplicate-skips + opening-overlap rejects are the idempotency guards working as designed — noisy, not corrupting. **⚠ CONTRACT-NUMBER CORRECTION:** this is **C13 (Project Lifecycle & Isolation)**, NOT "Contract 48" — C48 is Backup & DR. `ProjectIsolationAudit.ts` / `ProjectScopedStorage.ts` cite "Contract 48" and `ProjectScopeRegistry.ts` cites a non-existent `44/45-*` doc — stale references from old numbering; orchestrator to fix the doc refs and land the invariant in C13 §3. **Also confirmed:** the underlay `clearPersistedUnderlay` no-op IS deliberate and correct (my earlier catch stands); `ProjectScopedStorage` DOM listeners are dead but INERT (zero production read/write callers — vestigial). **Cross-fence flag:** `EdgeProjectorService`'s CW drawing cache has no `bim-project-cleared` subscription → possible cross-project drawing reminiscencia (L-221's fence, flagged not touched). **PHASE B GREENLIT (no founder decision needed — unlike L-223, nothing changes appearance):** migrate the 6 orphaned listeners to `runtime.events.on`/`onRuntimeEvent`, reviving the teardown controller + audit; then run the audit on EVERY load vs the loaded snapshot's expected contents, extend it to stores/graphs/caches/undo, and add a CI gate in `check-project-isolation` failing any `window.addEventListener('pryzm-project-{switch,loaded,context-set}'`. | route -> persistence/project-lifecycle agent. **Deliverable 1 is the end-to-end audit the founder asked for.** Enumerate EVERY stateful surface that must be reset on project switch and classify each **CLEARED / RESTORED-FROM-SNAPSHOT / LEAKS**: all 37 registered stores; `SemanticGraph`, `TemporalGraph`, `RoomGraphService`, `DecisionRecordStore`, `ConstraintEngine`, `PhysicsEngine`; `ElementRegistry`, `BimManager` levels; `ViewTechnicalDrawingCache`, NME cache, `EdgeProjectorService` cache, `ViewRenderCache`, `IntentStylePrewarmer`; `SelectionManager`; the **undo ring-buffer + `commandManager` history**; `RemoteCommandDispatcher`'s catch-up cursor; `window.*` singletons; `localStorage`/IndexedDB scoping. Reconcile against what `ClearProjectCommand` + `ProjectLoader` actually do, and against the existing static guards (`npm run check:isolation` → `scripts/check/check-project-isolation.mjs` + `check-storage-isolation.mjs`). **Then fix, in order:** (1) **Make the audit cover the reported case** — run it on EVERY project load, not just `empty:true`, comparing against the loaded snapshot's expected contents rather than against "must be empty" (that is how you keep the false-positive rate at zero *without* abandoning coverage). (2) **Extend it beyond the scene** to the store/cache/graph/undo surfaces above; a "✓ loaded clean" that never looked at 37 stores is worse than no audit, because it manufactures confidence. (3) Prove or refute the undo-ring-buffer suspect and the collaboration-catch-up suspect **with evidence**, then fix what is real. (4) Establish the invariant in `Contract 48`: *project switch is a full teardown; every surface has a named owner responsible for clearing it, and the audit enumerates owners rather than symptoms.* **Non-goal:** do not silence the audit or narrow its scope to make it pass. **Note:** `ProjectIsolationAudit` reads `window` via a `(window as any)` cast helper (`:59-61`) — check this against **P4** and the allowlist while you are in there. | §AUDIT-PROJECT-ISOLATION-E2E. |
| L-225 | L-221 agent (found while authoring the plumbing symbols; NOT founder-reported) | **[MEDIUM — correctness] A `urinal` and a `bidet` render in 3D as a TOILET.** Both are members of the fixture union (`packages/geometry-plumbing/src/PlumbingTypes.ts:13` — `'toilet' \| 'sink' \| 'urinal' \| 'bidet' \| 'bath' \| 'shower' \| 'accessory'`) but **neither has a 3D geometry factory**. `PlumbingFragmentBuilder` falls both through its `else` branch to `createToiletMesh()`. So a user who places a urinal gets a toilet, silently, with no warning — the model, the schedule, the IFC export and the take-off all disagree with the drawing. The **2D symbols are already correct** for both (L-221 authored conventional sanitaryware footprints for the full union so the exhaustiveness test holds), which means the plan/elevation now show a urinal while the 3D view shows a toilet — a *visible* inconsistency that did not exist before, and the honest way round. **The agent flagged this and correctly did not widen its fence to fix it.** | Element geometry (C11 one element type ⇒ one creation pipeline; C06) | OPEN (queued, MEDIUM) | route -> geometry/plumbing agent (small; can ride with any plumbing pass). (1) Author real `createUrinalMesh()` / `createBidetMesh()` parametric geometry, matching the footprints the L-221 symbols already declare — the 2D symbol is now the spec. (2) **Remove the silent `else` fallback.** A fixture type with no geometry factory must fail loudly (dev-time error / typed exhaustiveness switch), never render as a different fixture. That silent fallback is the actual defect; the two missing meshes are only its symptom. (3) Test: every member of `PlumbingFixtureType` produces geometry distinct from every other member. §FIX-URINAL-BIDET-RENDER-AS-TOILET. |
| L-226 | founder ("Cesium 3D tiles view — '3D Globe' — is sound, but I always need to click 'Zoom to Site' to see the correct building on the scene. Could you add this on the default pipeline?") | **[MEDIUM — UX] The globe opens parked on a base-0 overview instead of framed on the placed building; the founder manually clicks 'Zoom to Site' every time.** **This has a HALF-BUILT auto-frame already** (§FIX-GLOBE-AUTOFRAME-AND-SEAT, L-184) — the exact machinery for "no manual Zoom to Site needed": `armGlobeReframeOnBaseSettle()` (`CesiumViewport.ts:3957`) arms a ONE-SHOT corrective reframe routed through `performInitialReframe()` (`:3995`, fires ≤ once, honours a user who has grabbed the camera, frames the building's bounding sphere at the *settled* Google-tile base). The design intent is spelled out in the header (`:3942-3956`): the globe placement path `renderBuildingOnGlobe` runs `frameCentroid:false` (deliberately does not fly), leaving `GISAreaLayout.reframeSiteIn3D()` (`GISAreaLayout.ts:441`) to frame the building — but that immediate frame lands at FLAT base 0 before the async photoreal-tile height clamp settles, and when the clamp re-seats the model on the real ground (e.g. ~382 m) the camera is NOT re-framed → it stays parked at the base-0 overview, which is exactly the founder's symptom. **So the fix is almost certainly "arm the existing one-shot on the DEFAULT globe entry," not new camera code.** The Zoom-to-Site button (`GISAreaLayout.ts:795`) calls the same `reframeSiteIn3D()` the auto-path should already run — which is why clicking it works. **Hypothesis to prove:** `armGlobeReframeOnBaseSettle()` is either not called on the default '3D globe' entry, or is called but its latches (`formaInitialReframeFired` / `formaUserMovedCamera`) are consumed/short-circuited before the base-settle fires, or the base-settle callback isn't wired to `reframeAfterBaseSettle()` on the photoreal-tile path. **FOUNDER RE-CONFIRMED 2026-07-11 with a decisive new detail: "the default location of the camera seems to point to Z = 0 (sea level)."** This nails the L-184 hypothesis — the immediate `reframeSiteIn3D()` frames at FLAT base 0 (sea level) BEFORE the async photoreal-tile height clamp settles the real ground (his own earlier log showed `photoreal-tile clamp: base height 701.95 m at LAT 40.415552` for Madrid); when the clamp re-seats the model at ~702 m the camera stays at the sea-level overview, so the building appears far below/away → he clicks "Zoom to Site" to re-frame. **FIXED (418cd197).** ⚠ Cause is (b) but the MECHANISM differs from the brief — agent refuted my phrasing from source. NOT the initial framing tripping the latch; it is a SHARED-BOOLEAN CLOBBER across overlapping cancelled camera flights. `formaProgrammaticFlyInFlight` was one shared bool written by three flights. Default entry: `reframeSiteIn3D` → `flyToFormaSite` fires ~350ms in and SUPERSEDES the still-gliding cinematic site-arrival flight; Cesium cancels the arrival tween, whose now-stale `cancel` callback clears the shared flag to false WHILE the newer reframe flight is still mid-glide → that flight's `moveStart` mis-latches `formaUserMovedCamera=true` → when the tile base later settles (Madrid 701.95m), `performInitialReframe` self-suppresses (the founder's `§GLOBE-FRAME-NO-JUMP — base settled but the user already moved the camera` log) → building re-seated at 701.95m, camera stranded at the base-0 frame = 'points at Z=0'. Timing-dependent (only when the arrival flight is still airborne at t≈350ms) — matches 'some entries'. Both (a) and (c) refuted from source: `armGlobeReframeOnBaseSettle('oblique')` IS called on the default entry (`GISAreaLayout.ts:1747`) and `reframeAfterBaseSettle()` IS wired at every clamp exit. **Fix:** generation-token the flag (mirrors `formaTerrainToken`): `beginProgrammaticFly()` returns `++formaFlyToken`; `endProgrammaticFly(token)` releases ONLY when `token===formaFlyToken`, so a superseded flight's teardown is a no-op. `performInitialReframe` untouched — all three §GLOBE-FRAME-NO-JUMP invariants hold (fire-once; a genuine user drag has no active flight token → flag false → still latches correctly; never NaN). Gate: 6/6 (+2 token-race tests), root tsc exit 0, 2 files (+113/-8). | Geospatial / Cesium (C06 §7 launcher layer; A.24 render tiers; DOC globe-placement; L-40/L-104/L-184 lineage) | route -> geospatial/Cesium agent. (1) Trace the DEFAULT '3D globe' entry (`GISAreaLayout` globe launcher → `renderBuildingOnGlobe` → `clampToPhotorealTilesThenReplace`) and establish WHY the existing L-184 one-shot reframe does not fire (or fires against base-0). (2) Wire `armGlobeReframeOnBaseSettle()` into the default entry so the settle-triggered `performInitialReframe()` frames the building at the settled tile base — reuse the existing funnel, do NOT add a parallel flyTo (the header warns re-flying on every settle yanks the camera; the ≤once + user-moved latches are load-bearing). (3) Preserve the three invariants (`performInitialReframe` §GLOBE-FRAME-NO-JUMP): fire at most once, never after the user grabs the camera, never to a NaN target. (4) The manual 'Zoom to Site' button stays (explicit re-frame). (5) Test: default globe entry ends framed on the building's bounding sphere at the settled base, with zero manual clicks, and does not re-yank after a subsequent tile-height restream. §FEAT-GLOBE-DEFAULT-AUTOFRAME. |
| L-227 | founder ("Regarding the Forma '3D Site' view — can you make the 'Façade analysis ON' as sound as the Sun Hours? The Sun Hours have an amazing quality gradient; on the building it is not nice, not sound. It needs to be perfect — the exact quality in ALL the façades of the building.") | **[MEDIUM — quality] The building's façade sun-analysis renders as a flat/low-fidelity coat instead of the smooth per-face gradient the GROUND sun-hours heatmap achieves.** His screenshot: the ground carries the gorgeous purple→magenta→pink→orange→yellow sun-hours gradient; the building is coated near-flat cyan. **The two are DIFFERENT pipelines, which is why the quality differs.** The ground heatmap he loves = `computeSunHoursOnModel()` + the 5-stop `DEFAULT_SUN_HOURS_RAMP` (`packages/renderer-three/src/solar/heatmapRamp.ts:30-36` — `#6600FF → magenta → pink-red → orange → warm yellow`) sampled per-face. The façade path is a **separate** subsystem: `prepareFacadeSunGrid` → `rasterizeFacadeSunTexture` (§FORMA-FACADE-SMOOTH, founder 2026-07-01) → Cesium entities (`CesiumViewport.ts:71-95, 656-679`, §FORMA-FACADE-ANALYSIS / ADR-0093), re-exported via `@pryzm/renderer-three` and consumed in `apps/editor/src/ui/climate/siteMetricGrids.ts` + `apps/editor/src/workers/solarCodec.ts`. **Hypotheses to prove (measure, do not assume):** (a) the façade rasteriser does NOT use `DEFAULT_SUN_HOURS_RAMP`, so the colour mapping differs from the ground; (b) the façade sun grid samples at a far lower density (or a single band) than `computeSunHoursOnModel`, so the smooth gradient collapses to a flat tint; (c) the façade drape is a Cesium-baked texture whose resolution / UV layout under-samples the wall, losing the gradient; (d) the near-flat cyan is a *fallback* colour (e.g. occlusion or the sun-vector step returned a constant), not a real per-point field. The founder's phrase "in ALL the façades" suggests some faces get no gradient at all — likely the self-shadowed / low-sun faces clamping to the ramp's `t=0` end while the sunny faces also read flat, i.e. the field itself is not varying per-point. | Solar analysis / Forma render tiers (C21-CLIMATE-INGESTION; ADR-0074 solar; ADR-0093 façade analysis; A.24 Presentation tier; §FORMA-FACADE-SMOOTH) | **FIXED (a6bcd902)** — §FEAT-FACADE-ANALYSIS-MATCH-SUNHOURS-QUALITY. **⚠ THE ORCHESTRATOR'S NAMED GROUND PIPELINE WAS WRONG; the agent refuted it from source.** The purple `DEFAULT_SUN_HOURS_RAMP` + `computeSunHoursOnModel` (renderer-three) is a **THIRD, unrelated subsystem** — the BIM-model heatmap from the `pryzmComputeSunHours` console command, painting editor-scene meshes; it is **never used by the Cesium Forma 3D Site view** (no ref to either in `CesiumViewport.ts`). In the ACTUAL Forma view, both surfaces live in `apps/editor/src/ui/climate/siteMetricGrids.ts`: **ground** = `prepareSunHoursGrid`→`rasterizeSunHoursTexture`→`sunHoursRgb` (`:410`, blue→teal→gold→warm `#2C3E80→#3FA796→#F6C445→#F4753A`); **façade** = `prepareFacadeSunGrid` (`:1271`)→`rasterizeFacadeSunTexture` using **`sunHoursRgbVivid`** (`:448`, the SAME ramp ×1.45 saturation ×1.18 contrast). **REAL ROOT CAUSE (two compounding defects, H-a + H-d):** (1) the vivid variant pushes the mid-band teal `#3FA796` into saturated **cyan** — the founder's "cyan" is a cosmetic over-saturation of the real teal, not a fallback; (2) `evaluateIntensity` returns `lit / samples.length`, but a WALL only accumulates sun on its outward side (back-face cull `:1305-1308`), so a vertical face can NEVER reach the all-day sample count it divides by → the whole building collapses into the COLD half (blue→teal) and never reaches gold/warm, while horizontal open ground is lit by every daylight sample → 1.0 → full ramp (exactly why the ground looks rich). H-b FALSE (façade lattice 2.5 m is FINER than ground 4 m). H-c FALSE (atlas ~1 m/texel, 256×512/face — adequate). Material is `LightingModel.UNLIT` so it is NOT a lighting wash. **Fix:** façade now renders through the **plain `sunHoursRgb`** (dropped `vivid`) AND normalizes the field to fill the same cold→warm span the ground uses (porting `computeSunHoursOnModel`'s own-realised-max normalization pattern). Drape MECHANISM untouched; renderer-three untouched; **no density/texture increase → zero added GPU cost** (deliberate, given the 40-storey device-loss history). Occlusion parity confirmed (both use `siteMetricFootprints`→`buildPrismShadowIndex`→`sunBlockedIndexed`, identical occluder set). Gate: apps/editor 64/64 (+4: range-expansion+variance-floor, exact ramp identity asserting **never-cyan**, sunny-warm/shaded-cold per-face), solar-analysis 46/46, renderer-three solar 28/28, root tsc exit 0, 3 files. **⚠ OPEN FOUNDER QUESTION (optional, non-blocking):** the founder's screenshot ground colours (purple→magenta→pink→orange→yellow) match the renderer-three `DEFAULT_SUN_HOURS_RAMP`, NOT the Forma ground's blue→teal→gold→warm. Possibility: the "amazing gradient" he remembers is the BIM-model console heatmap, and he may want the WHOLE Forma sun-hours feature (ground+façade+legend) switched to that purple ramp. Agent did NOT do this (founder said the ground is already amazing; changing `sunHoursRgb` repaints ground+legend+every sun-hours surface). It is a one-function change (`sunHoursRgb`'s 4 stops → the purple 5-stop, keeping ground+façade unified) if the founder confirms. → §FEAT-FORMA-SUNHOURS-PURPLE-RAMP (L-230, awaiting founder). | route -> solar/Forma agent. **Converge the two pipelines on quality, not duplicate a second ramp.** (1) Establish, by measurement, WHY the façade field is flatter than the ground: compare the façade sun-grid sample density, ramp, and occlusion inputs against `computeSunHoursOnModel` for the SAME building+day. (2) The façade must use the **same `DEFAULT_SUN_HOURS_RAMP`** as the ground (single source of truth — the founder wants *identical* quality, so identical colour mapping) and a per-face sample density high enough that the gradient is smooth across each façade, not banded. (3) If the drape is a baked Cesium texture, raise its resolution / fix the UV so it does not under-sample; if it is entity-per-sample, raise the grid. Tier-gate + memory-budget (this is the 40-storey WebGPU device-loss history — do not ship 4K drapes to a tower). (4) Occlusion parity: the ground study shadows by massing + OSM context; the façade study must shadow the same way, or self-shadowed faces will read wrong. (5) Test: for a reference building+day, the façade per-face sun-hours field has the same value range and ramp mapping as the ground field on a co-located probe, and varies smoothly across each face (assert non-constant per-face variance — the regression is a flat tint). §FEAT-FACADE-ANALYSIS-MATCH-SUNHOURS-QUALITY. |
| L-228 | L-222 agent (deferred sub-task, NOT founder-reported) | **[LOW — perf, deferred by design] The elevation/section drawing cache re-extracts + re-classifies edges together, keyed on the crop-derived `far`, so a crop drag gets 0% cache hit.** During L-222 it was established that `_cwProjectionCache` (`EdgeProjectorService.ts:1198-1250`) stores post-classification drawing-space geometry whose `clipSignature` MUST include `far` (that is L-202's correctness fix — dropping it replays geometry classified under a stale crop). So the naive "remove `far` from the key" is wrong. The sound optimisation is a **two-level cache**: (L1) crop-invariant edge extraction keyed on `element + geometryVersion`; (L2) cheap per-crop `proj`/`beyond` re-classification keyed on `far`. L-222's double-buffer already removed the *flicker* and the drag is throttle-bounded, so this is now a pure-perf nicety, not a correctness or UX issue — hence deferred. | Views / projection perf (C04; DOC-1.4/1.8; L-202 correctness must be preserved) | OPEN (queued, LOW) | route -> views/projection agent (after L-222 lands; same subsystem). Split `EdgeProjectorService`'s CW projection cache into crop-invariant extraction (L1, element+version) + per-crop re-classification (L2, `far`). **Must not regress L-202** — add a test feeding the L-202 scenario (element captured as the scope grows) and assert it re-classifies against the CURRENT crop, not a cached one. Target a high L1 hit-rate during a pure crop drag. §PERF-EDGEPROJECTOR-TWO-LEVEL-CACHE. |
| L-229 | L-220 agent (found during the drag audit; NOT founder-reported) | **[MEDIUM — correctness] Four element types silently do nothing when dragged in 3D; ceiling drags aren't undoable.** The L-220 audit of `registerTransformDragHandler.ts` (branches at `:97-565`) found: **slab, grid, lighting, opening have NO drag branch at all** — the gizmo moves the mesh visually but no command is ever dispatched, so the move is lost on the next rebuild and the plan never updates (same *silent no-op* class as the plumbing bug the founder DID report, just not yet noticed on these types). A `MoveSlabHandler` / `slab.move` already exists but the drag never calls it. Separately, **ceiling** DOES commit (`ceiling.update {ceilingId,updates}`, bridge `initBusHandlers.ts:334`) but sets **no `_recordUndo`**, so a 3D-drag ceiling move is not captured on the ring buffer (undo falls back to `commandManager` only — inconsistent with wall/floor/furniture which ring-capture). | Element transform / tools (C11; P6; C03 undo) | OPEN (queued, MEDIUM) | route -> command-bus/transform agent (small; rides with any transform pass; same fence as L-220). (1) Add drag branches for slab (dispatch the existing `slab.move` / `MoveSlabHandler`), grid, lighting, opening — each converging on the typed `dragDispatch()` L-220 introduced (a wrong payload is then a compile error). (2) Add `_recordUndo` + `_prev` to the ceiling drag so it ring-captures like its siblings. (3) Establish WHY these were missed — likely the same per-type `if`-ladder smell; consider whether the drag handler should assert an exhaustive element-type switch so a new draggable type without a branch fails loudly rather than silently. (4) Tests: dragging each of slab/grid/lighting/opening commits a store change AND re-projects the plan; a ceiling drag is undoable in one step. §FIX-DRAG-MISSING-BRANCHES. |
| L-230 | L-227 agent (colour-scheme question raised during the façade fix) | **[LOW — quality, AWAITING FOUNDER] Possibly switch the whole Forma sun-hours feature to the purple `DEFAULT_SUN_HOURS_RAMP`.** The founder's screenshots of the sun-hours gradient he calls "amazing" show **purple→magenta→pink→orange→yellow** — which is the renderer-three `DEFAULT_SUN_HOURS_RAMP` (the BIM-model console heatmap), NOT the Forma 3D Site ground's actual `sunHoursRgb` (blue→teal→gold→warm). So the founder may be remembering a *different* surface's colours and want the Forma feature restyled to match. L-227 deliberately did NOT do this — it converged the façade onto the ground it actually sits beside (correct for "match its ground"), and changing `sunHoursRgb` would repaint the ground + legend + every Forma sun-hours surface at once. | Solar / Forma colour scheme (C21; ADR-0074; A.24) | OPEN (awaiting founder confirmation) | route -> solar/Forma agent IF confirmed. One-function change: `sunHoursRgb`'s 4 stops → the purple 5-stop from `DEFAULT_SUN_HOURS_RAMP` (`heatmapRamp.ts:30-36`), keeping ground + façade unified (L-227 already made them share the ramp, so both move together). Update the legend swatch to match. Verify against the founder's screenshot. **Do NOT start until the founder confirms which gradient he wants** — the current Forma ramp may be the deliberate one. §FEAT-FORMA-SUNHOURS-PURPLE-RAMP. |
| L-231 | founder ("check why the 3D view graphics broke while going to Cesium 3D tiles / 3D globe") | **[CRITICAL — crash] WebGPU device loss on a heavy residential scene cascades into a dead Cesium render.** The log is an unambiguous chain: on a 6-storey residential building (3908 scene meshes, 1492 shadow casters), the ground-shadow-fit fired with `shadowMapAllocated=false` → `[RenderPipelineManager] Rebuilding pipeline after shadow-map update` → `SHADOW_REBUILD_COMPLETE elapsed=**6019.7ms**` (a **6-second** shadow-pipeline rebuild), during/after which `Destroyed texture [ShadowDepthTexture] used in a submit` fired **×24** → `THREE.WebGPURenderer: WebGPU Device Lost: "A valid external Instance reference no longer exists"`. The device recovered (`WebGPU device recovered — renderer recreated`, `§FIX-DISPOSE-USEDTIMES-DEVICE`, `§SHADOW-DEVICE-LOSS-FIX outline dispose … swallowed`), the founder toggled to the **3D globe**, and Cesium died: `[Cesium WebGL] Fragment shader compile log: null` → `RuntimeError: Fragment shader failed to compile. Compile log: null` → **"An error occurred while rendering. Rendering has stopped."** Footer fell to `WebGL · webgl-fallback`. **RECURRENCE OF THE 40-STOREY OFFICE DEVICE-LOSS CASCADE** (ShadowDepthTexture-mid-submit → device loss), previously addressed by §SHADOW-DEVICE-LOSS-FIX / ADR-0111 — the fix is incomplete for heavy *residential* scenes. **Two linked defects: (A)** the shadow rebuild still destroys `ShadowDepthTexture` mid-submit despite §#47 coalescing + §FIX-SHADOW-REBUILD-LATCH-ASYNC (`RenderPipelineManager.ts:735-819`). A 6-second `_rebuildPipeline()` is a huge window; the ×24 mid-submit destroy means a GPU submit still references the depth texture when it is torn down — the latch/coalesce does not cover this path (recovery-path dispose? OBC/Pascal submit during the async rebuild? the shadow-map reallocation itself?). "External Instance reference no longer exists" is a Dawn instance teardown = the whole GPU device died. **(B)** Cesium cannot survive the device loss — after WebGPU is lost/recovered, Cesium's WebGL context is left invalid (`Compile log: null` = lost/reset GPU process), so the globe toggle hits a shader-compile failure and Cesium halts with no recovery. **NOTE:** the L-205 grey-square investigation that froze this subsystem is RESOLVED (C04 §SHADOW + ADR-0120 shipped); this is a DEVICE-LOSS crash in the ADR-0111 domain, so the freeze is **LIFTED for L-231** (fix must still honour C04 §SHADOW: never resize a live map, never dispose off-frame). | Rendering / WebGPU device loss (C04 §SHADOW; **ADR-0111**; A.24) | **FIXED (068ff74d).** **MEASURED CAUSE - two compounding defects, both named in C04 §SHADOW.** **(1) The rebuild never paused rendering (rule 7).** The pass holding the stale handle is the **TSL ScenePass shadow depth pass driven from `rp.render()`** (`RenderPipelineManager.ts:551`) - the live pipeline's own per-frame WebGPU submit; NOT OBC, NOT Pascal, NOT the recovery dispose. `scheduleShadowRebuild`'s *normal* branch (`:795`) calls `_rebuildPipeline()`, which clears `_hasPipelineError = false` **synchronously at its top (`:2031`), before awaiting the async work** - so the render gate (`:523`) stayed **OPEN for the entire ~6 s rebuild** and kept submitting frames while `_safeDisposeRenderPipeline()` (`:1721`/`:1851`) tore the pipeline down under them. The *plan-view* branch (`:764`) DID pause; the normal branch - the one that fires on project load - never did. **(2) THE ENTIRE SHADOW-FREEZE FAMILY HAS BEEN WRITING AN INERT FLAG (rule 10).** `_applyShadowFreezeState` wrote only `renderer.shadowMap.autoUpdate` (`:928`), which C04 §SHADOW rule 10 states is **INERT on the WebGPU node path** (three gates the depth redraw on the **per-light** `light.shadow.autoUpdate`/`.needsUpdate`). So **every** freeze guard has been a **no-op against the real WebGPU shadow pass**; the depth texture kept regenerating and reallocating mid-submit while nominally 'frozen'. **This invalidates any past conclusion of the form 'the freeze is in place, so this cannot be the shadow map'.** **Fix:** the freeze now also writes the per-light flags (new `_forEachShadowCastingLight`), making every existing guard actually effective; and the async rebuild is wrapped so it **pauses WebGPU submits** (`_shadowRebuildPaused`, checked in `render()`) and freezes the map for its duration, thawing one macrotask past the in-flight submit. **Cesium (part B):** `createRenderer` publishes `__pryzmRendererRecovering` for the whole device-loss->rebind window; `CesiumViewport.mount()` awaits it (bounded 12 s) so the globe never comes up against a resetting GPU process (the `Compile log: null` cause); and the Cesium canvas now has `webglcontextlost`/`webglcontextrestored` handlers with the loss **`preventDefault()`-ed** - **without that the browser NEVER restores the context, which is exactly why the failure was a permanent dead end.** **Tier (part C):** no map size raised. **Rule-8 follow-up flagged, not done:** the 6 s full rebuild is unnecessary for shadows, but its real callers (`initUI.ts:489`/`:1418`) use it as a geometry-upload barrier guarding a `drawIndexed` crash - the agent made it SAFE rather than deleting it blind. **Honest limitation:** static trace + log ordering + C04's normative model, NOT an instrumented GPU capture - definitive confirmation is a prod run on the 6-storey scene showing the x24 destroy gone. Gate: renderer-three 185/185, core-app-model 282/282, +6 Cesium tests, root tsc exit 0, 5 files. | route -> rendering/WebGPU-device-loss agent (L-205 freeze LIFTED). **(A)** Find why a submit still references `ShadowDepthTexture` when the 6s rebuild tears it down — instrument the ×24 destroy, identify which pass holds the stale reference, make teardown wait for the in-flight submit to drain (ADR-0111: never dispose across a live submit). A 6s rebuild on 1492 casters is itself a signal — question whether a shadow-map *update* must rebuild the WHOLE pipeline. **(B)** Make Cesium activation survive/recover a device loss: gate the globe toggle until the renderer is confirmed live, and/or give the Cesium viewer a context-lost/restore handler that recompiles rather than halting — a `Compile log: null` failure must not be a dead-end panel. **(C)** Tier-gate for 1000+ casters (memory: oversized GPU resources + per-element unique materials are the device-loss triggers). **(D)** Tests: a simulated device-loss during a shadow rebuild does not destroy a submit-referenced texture; a device-loss then Cesium activation recovers instead of halting. §FIX-WEBGPU-DEVICE-LOSS-CESIUM-CASCADE. |
| L-232 | founder ("check the façade analysis image — Forma 3D Site — it is still really bad; make it sound as ground, with this level of quality") | **[MEDIUM — quality, L-227 FOLLOW-UP] The façade sun-analysis is now the right COLOUR (L-227 fixed the cyan over-saturation + cold-band collapse) but the SPATIAL quality is still blotchy/banded, not the smooth continuous gradient the ground has.** The founder's image shows strong per-storey horizontal banding and patchy teal/cyan facets on the walls, while the ground carries a smooth blue→teal→gold→warm field. **This is a DIFFERENT defect from L-227** (which was colour-mapping + normalization, not resolution). **Grounded root causes:** **(1) sample spacing is TOO COARSE for a façade.** `prepareFacadeSunGrid` targets **2.5 m** spacing (`siteMetricGrids.ts:1150` — comment: *"Default 2.5 (clean per-face read)"*). Storeys are ~3 m tall, so there is barely ONE vertical sample per floor → the per-storey banding in the image. The ground raster is continuous; the façade is under-sampled. **(2) the SMOOTH path is spec'd but not the active one.** `siteMetricGrids.ts:1108` states verbatim: *"clean per-face colouring; the smooth-interpolated-per-face version is SPEC'd in **ADR-0093**"* — so ADR-0093 specifies a smooth-interpolated façade field that is NOT what renders; the coarse per-point `colourFor` path is. **(3) balcony/slab self-shadow hard edges.** The building's balconies and floor slabs cast onto the wall below; at 2.5 m sampling those occlusion boundaries resolve as hard blotches rather than a soft gradient (the ground has no such fine self-occluders, which is why it looks clean). **(4) POSSIBLE per-face normalization seams** — L-227 added a fill-the-span normalization; if it normalizes per-face (each face to its own max) rather than by the model's global realised max (as `computeSunHoursOnModel` does), adjacent faces get different scales → visible seams. Must verify L-227's normalization is GLOBAL, not per-face. | Solar / Forma render tiers (C21; ADR-0074; **ADR-0093**; A.24) | **FIXED (9c8535e9).** **THREE OF MY FOUR ROOT CAUSES REFUTED.** **RC4 (per-face normalization seams) - REFUTED:** `normalizeFacadeStudy` takes a single `max` across ALL wall faces and divides every face by that one divisor. It is **GLOBAL** - adjacent faces share a scale, so it is not the blotch source. **RC2 (smooth path spec'd but inactive) - REFUTED:** the smooth bilinear per-face texture **IS** the active path (`rasterizeFacadeSunTexture` + `buildRealModelSunDrape`, both bilinear). **ADR-0093's 'SPEC'd next increment' note is STALE** - §FORMA-FACADE-SMOOTH shipped it the next day (2026-07-01); the `:1108` comment I quoted was simply never updated (now corrected). **RC3 (balcony/slab hard self-shadow) - REFUTED:** the occluder set (`siteMetricFootprints`) is **single coarse prisms** - OSM context boxes plus the proposed massing as ONE box. There are **no per-storey slab/balcony occluders in the shadow test at all**, so that self-shadow is not in the field. P4 was therefore a **no-op** - adding an AO term would invent a signal the study does not compute. (Real balcony self-shadowing would be a NEW feature: feed per-storey geometry into the occluder set.) **THE ACTUAL MECHANISM - and it is a side-effect of my own L-227 fix.** Sun-hours is a **binary lit/blocked raycast** per sun sample, so a point's value is a `lit/N` **staircase**, quantized to the sun-sample count. The **ground hides this**: normalized by the full-day count its step is `1/N ~= 2.9%` at the 25-min cadence - below the banding threshold. The **facade cannot**: a vertical wall only takes samples on its outward side, so **L-227's wall-max stretch (which correctly filled the ramp) DOUBLED the step to ~1/(N/2) ~= 5%** - and those iso-sun-hours contours read as **horizontal bands**. Before L-227 they were invisible inside the compressed cyan; **the correct full-range ramp EXPOSED them.** **Fix = halve the CADENCE, not raise the density.** `planFacadeSampling` (pure, tested) sets the facade cadence to ~half the ground's (25 -> **13 min**), countering the L-227 stretch exactly: steps 5% -> **2.6%**, at/below the ground's 2.9% -> smooth. **Zero GPU cost - this is CPU raycast work only, chunked across frames; the drape textures are UNTOUCHED** (`DRAPE_TEXELS_PER_M=1`, caps 256x512/face, atlas 4096), because `buildRealModelSunDrape` *resamples* whatever lattice it is given into a **fixed-cap** atlas. Denser sampling therefore adds **exactly zero GPU bytes** (atlas bounded <= ~8 MB regardless) - **the L-231 device-loss path cannot recur from here.** Work budget `clamp(600k x 40/max(40,H), 100k, 600k)` node-sample raycasts **shrinks with height**; `heavy` halves it; spacing auto-coarsens if the sub-storey target would blow the cap. House (12 m): 1.20 m spacing (was 2.5). **120 m tower: keeps its prior 2.5 m - never denser, never worse.** Megatower auto-coarsens to 6.58 m. Gate: siteMetricGrids 69/69 (+5, incl. an explicit work-bound / height-shrink guard), renderer-three solar 28/28, solar-analysis 46/46, root tsc **exit 0**. **THIS COMMIT REPAIRED A BROKEN `main`** - see L-236. **RESIDUAL (flagged, not fixed):** any remaining 'patchy facets' may be the drape's **nearest-face planar selection seaming at building corners/insets** - a display-mapping (shader/UV) artifact independent of sampling. If the new build still shows facets at corners, that is the next thread. | route -> solar/Forma agent (resume the L-227 agent — it owns this code). (1) Implement the **smooth-interpolated-per-face** façade field that **ADR-0093 already specifies** (`:1108`/`:1462-1464` reference it) so the field is smooth within a storey AND across every storey seam. (2) **Raise façade sample density** well below storey height (2.5 m → sub-metre) so balcony/slab self-shadow gradients resolve smoothly — **but TIER-GATE + memory-budget it HARD**: this is the exact 40-storey WebGPU device-loss territory (see **L-231**, an active CRITICAL device-loss crash on a heavy residential scene) — denser sampling + bigger drape textures must scale DOWN on heavy scenes / lower tiers, never up on a tower. (3) Verify L-227's normalization is by the model's **global** realised max (like `computeSunHoursOnModel`), not per-face — per-face normalization creates the seams. (4) Occlusion smoothing: soften the balcony/slab self-shadow boundaries (higher sample density near occluder edges, or an AO-style soft term) so they read as gradient, not blotch. (5) Test: façade per-face field variance is smooth across storey seams (no step discontinuity at floor lines beyond the real sun gradient); a co-located ground-vs-façade probe matches value + ramp; sample density is bounded per tier (assert the heavy-scene budget). **Coordinate with L-231** (device-loss) — do not raise GPU resource use on heavy scenes while that crash is open. §FEAT-FACADE-ANALYSIS-SMOOTH-PER-FACE. |
| L-233 | founder ("when the levels are stacked, if the user moves or creates an element, many of the elements in the view get un-stacked — back to normal. Is this intended?") | **[MEDIUM — correctness] In exploded/stacked level view, editing one element drops MANY elements out of the stack because the rebuild-reconcile allowlist is incomplete.** NOT intended — it is a documented, partially-fixed bug. `LevelExplodeController` (`apps/editor/src/engine/inspect/LevelExplodeController.ts`) is a VISUAL-only per-level Y-offset (`EXPLODE_GAP = 5 m`, `:42`); it lifts element roots per BIM level. Its own comment (`:48-50`) states the exact symptom: *"While the explode is active, a rebuild (move / property edit) regenerate[s] the mesh at its true (model) elevation and drops OUT of the exploded stack — **the founder's** [bug]."* There is a partial fix — **§FIX-LEVEL-EXPLODE-COORDINATION (L-113)** — which re-runs `_buildLevelGroups()` on a list of `REBUILD_RECONCILE_EVENTS` (`:55-66`) to re-lift rebuilt meshes. **THE ALLOWLIST IS INCOMPLETE — that is the defect.** It covers wall/slab/floor/ceiling/furniture/column/beam/roof/stair/curtainwall/door/window, but **NOT** `bim-room-*` / room-bounding-lines / room labels, `bim-handrail-*`, `bim-opening-*`, `bim-plumbing-*`, `bim-lighting-*`, `bim-stair-railing-*`, `verticalCirculation`, `annotation`. **The founder's log proves the mechanism:** the exploded view has `rooms 526, labels 263` (`[§LEVEL-STACK] exploded: … total 1732; rooms 526, labels 263`); he then executes `MOVE_WINDOW`, which fires `bim-window-updated` + `bim-wall-updated` (both IN the list → wall re-lifts) BUT also triggers a room re-detect (rooms + labels rebuild) — and rooms/labels are NOT in the list, so **789 room/label roots drop to model Y while the walls stay exploded.** That is "many elements un-stacked from one edit." **ARCHITECTURAL SMELL (same class as L-215 / L-229):** a per-element-type event ALLOWLIST inside a view controller — every new element type must be remembered, and derived elements (rooms/labels) that rebuild as a side-effect of an unrelated edit are the ones most easily forgotten. **Secondary symptom in the same log:** after the window move, `[SelectionManager] §SELECT-GIZMO-REATTACH gizmo attached to a detached object — detaching to stop the per-frame flood` with a massive rAF flood stack — the selection gizmo re-attached to the pre-rebuild (detached) window object and flooded per-frame until guarded; worth fixing alongside (the moved element's selection should re-anchor to the rebuilt mesh, cf. §SELECT-GIZMO-REATTACH). | View / inspect mode (C06; C09; P7; L-113 lineage) | **FIXED (15255b4d).** Root cause CONFIRMED as stated (the allowlist - actual constant name `REBUILD_EVENTS`). **Missed types:** `bim-room-*`, `bim-room-bounding-line-*`, room **labels**, handrail, opening, plumbing, lighting, stair-railing, stair-landing, lift, railing - all present in `packages/event-bus/src/catalog.ts`, simply never listed. The MOVE_WINDOW mechanism was exactly right: wall re-lifted (listed), room re-detect rebuilt 526 rooms + 263 labels (unlisted) -> 789 roots dropped. **Type-agnostic fix:** a `childadded`/`childremoved` listener pair on the **THREE scene root**. The agent first REJECTED the alternatives with evidence - there is no bus wildcard (`EventBus.on` is name-keyed; `EventCatalog` is a compile-time interface, not runtime-enumerable), and `pryzm-batch-ended` is **insufficient** (it only fires from batchCoordinator's bulk-generation lifecycle; a single gizmo move -> WallRebuildCoordinator flush -> room redetect does NOT go through it, so it would have missed the founder's exact scenario). The invariant used instead: **every level-tracked root is a direct child of the scene root**, and a rebuild is always `scene.remove(old)` + `scene.add(new)` (verified across WallFragmentBuilder/Slab/Column, InstancedMeshCoalescer, and critically `RoomLabelRenderer` - the 263 labels). One listener pair catches every rebuilt root of every type, **including future types, with no list**. The perf trap the old allowlist accidentally avoided (selection outlines / TransformControls helper triggering a whole-scene re-bucket on every hover) is closed with two **type-knowledge-free** gates: skip when mode is `stacked` (offsets are 0 - nothing to fix), and skip nodes with no `levelId`/`id`/`roomId` (precisely the trackability predicate `_buildLevelGroups` already keys on). **P4 (the §SELECT-GIZMO-REATTACH per-frame flood) FIXED without touching SelectionManager** - the flood's proximate cause was inside the explode controller: `_refreshSelectionAnchor` emitted `pryzm-reanchor-transform` unconditionally with a mesh the builder had just `scene.remove()`d, re-arming the very stale binding `SelectionManager.guardTransformControlsAttachment()` had just torn down (which is why the guard's warning appeared *alongside* the flood rather than preventing it). Added a detached-check + bail. `preservedBaseY` untouched, now with explicit regression cover. Gate: 25/25 across all three level-explode suites, root tsc **exit 0**, 2 files (+437/-35). | route -> view/inspect agent. **Do NOT just add the missing event types to the allowlist** — that perpetuates the smell and the next element type drops again. The sound fix: make the explode offset re-apply **type-agnostically to ALL level-tagged roots** after a rebuild settles — reconcile on the batch-complete / frame-settle signal (or a single `bim-element-*` chokepoint), re-running `_buildLevelGroups()` for every root regardless of element type, so rooms/labels/handrails/openings and any future type are covered by construction. Preserve the L-113 `preservedBaseY` double-lift guard (`:292-300`). Coalesce so a batch edit (one move → N rebuilds) reconciles ONCE after the batch, not per-event. (2) Fix the gizmo/selection re-anchor after a rebuild-during-explode (the §SELECT-GIZMO-REATTACH flood) so the moved element's selection tracks the rebuilt mesh at its exploded Y. (3) Tests: in exploded mode, moving a window keeps rooms+labels+walls all lifted; creating an element in exploded mode places it in the stack, not at model Y; collapsing restores exact original Y (no double-lift). §FIX-LEVEL-EXPLODE-RECONCILE-ALL-TYPES. |
| L-234 | founder ("RECURRENT — moving a wall with a hosted door freezes the project, but the logs don't seem to say much. Raised many times. Review, deeply analyse and fix.") | **[CRITICAL — hang, RECURRENT] Moving a wall that hosts a door/window freezes the tab, with no log output.** *"No logs"* is the decisive clue: a frozen tab that emits nothing is a **synchronous main-thread hang** — an infinite loop or unbounded (O(n^2)/recursive) blow-up that never yields, so the event loop never flushes console or renders. That is why prior attempts found nothing: there is no error to log; the thread never returns. **Trigger confirmed in the log:** `[WallTransform] Wall ... gizmo aligned with direction N {x:-1, y:0, z:1.157487755090348e-16}` -> `[CommandManager] EXECUTE: UPDATE_WALL_BASELINE`. The near-zero `z` (1.16e-16) is a **degeneracy smell** — the moved baseline is almost degenerate. **Suspect: `packages/command-registry/src/walls/UpdateWallBaselineCommand.ts`** (`:116-208`), which handles the fragile hosted-opening case: the drag recomputes the baseline; for a wall hosting doors/windows `WallStore.update()` throws `BaselineReversalError` if endpoints swap (opening offsets are measured from endpoint[0]); the command detects reversal via a `dot` product (`:142-149`), swaps to preserve offsets, then force-rebuilds (`:165-200`). **Hang hypotheses (MEASURE, do not assume):** (a) the reversal `dot` check misbehaves at a near-degenerate baseline (dot ~ 0), oscillating swap/no-swap; (b) the force-rebuild re-hosts the opening -> re-triggers a baseline update -> rebuild -> re-host cascade with no fixpoint; (c) the door/window re-projection onto the moved wall loops when the opening no longer fits / falls off the wall (WallOccupancyStore.canPlace, interior-wall-on-opening conflict class); (d) `WallJoinResolver`/`JunctionResolverV2` enters an unbounded loop on the degenerate near-zero-length wall (known multi-cluster degenerate-wall bug). Related: **ADR-0257** (realtime-edit perf — door move -> whole-level rebuild). | Wall/opening geometry + realtime edit (C11; C15 hosted elements; ADR-0257; P6) | **PERF CLIFF PROVEN BY MEASUREMENT (39dd3158). THE FOUNDER FINALLY HAS A NUMBER.** Real `WallStore` + real `UpdateWallBaselineCommand` + real `WallJoinResolver.resolveLevel` + real `WallFragmentBuilder.buildWall` + real `buildWallHoleBodyGeometry`, on a connected grid of rooms with real L/T joins; the moved wall hosts a door; the move is the pure translation the gizmo dispatches. **| N walls on level | walls rebuilt on ONE move | opening-body passes | wall-clock ms | of which resolveLevel |** **| 5 | 5 | 1 | 16 ms | 1 ms |** **| 50 | 50 | 1 | 49 ms | 31 ms |** **| 200 | 200 | 1 | 268 ms | 207 ms |** **| 200 (1-in-4 walls hosts a door) | 200 | 50 (48 real ExtrudeGeometry passes) | 224 ms | 144 ms |** **`rebuilt == N` EXACTLY at every N - not approximately N, literally N.** One wall move re-extrudes **every wall body on the level**, synchronously on the main thread, and at realistic door density **re-cuts every door on the level too**. 268 ms at N=200 is in a HEADLESS harness with **no renderer, no scene-graph upload, no hosted-child re-anchor, no material work** - on a real multi-level project with a live GPU pipeline that is straightforwardly the multi-second stall the founder reads as a freeze. It is silent because the only instrumentation on this path is gated behind `perfTraceOn()`. **WHY THE ADR-0257 FAST PATH NEVER CATCHES IT (the gate, file:line):** `WallDeltaClassifier.ts:157` - `if (joinGeometryChanged(prevState, wall)) return { kind: 'whole-level', reason: 'join-geometry-changed' }`; and `joinGeometryChanged` opens with `if (baselineMoved(prev, next)) return true` (`:88`), tripping at `BASELINE_EPS_M = 0.001`. **A wall move IS a baseline move, so it fails the fast-path gate BY CONSTRUCTION.** ADR-0257 was built for door/window OFFSET edits and its invariance proof only holds while baselines are stable - the classifier is correct, it simply has nothing to offer a move. **THE CONSUMER:** `WallRebuildCoordinator.ts:1180` takes the fast path only on `openings-only`; otherwise it falls through to `:1221` `store.getAll().filter(levelId)` -> `:1232` `resolveLevel(levelWalls)` -> `:1291` `adjustments.forEach(...)` -> **`:1543` `builder.buildWall(updated, adjustment, ...)` UNCONDITIONALLY.** That call site does **not** consult the builder's `_lastBuiltVersion` dirty cache (that guard lives in `updateWall`/`_buildWallInternal`), so **no wall is ever skipped**, and `resolveLevel` returns an adjustment for every wall participating in a join - i.e. every wall on a real floor plate. **This is exactly what ADR-0099's own 'Consequences' section DEFERRED** ('making `WallJoinResolver.resolveLevel` incremental for a single moved wall'). The number for that deferral is now on the record. **AGENT CAVEAT (do not misread the table):** the strict `ExtrudeGeometry` hole-cut fires only for an opening-bearing wall with NO miter end (`WallFragmentBuilder.ts:1966-1971` gates on `!_hasMiterEnd`). A corner-mitered opening-bearing wall instead builds before/sill/header/after box segments and runs `_mergeWallBodySegments` (mergeGeometries + toCreasedNormals). **Both are per-wall-body geometry passes** - so a founder wall showing `extrudes = 0` is NOT free; the cost is on the other branch. Gate: geometry-wall 202/202 (+2), root tsc exit 0. **-> FIX DISPATCHED: §PERF-WALL-MOVE-INCREMENTAL-REBUILD.** [Phase 1, retained] NO HANG / NO LOOP EXISTS. All four loop hypotheses refuted from source. **MY ROOT CAUSE WAS WRONG, AND SO WAS MY MID-FLIGHT CLAIM.** I told the agent it had 'located the hang in WallJoinResolver' based on the FILENAME of its test (`...hostedDoorMoveNoHang.test.ts`). It had not - that test **PINS THE ABSENCE** of a hang. I asserted a finding from a filename instead of reading the work. Recorded so the error is not repeated. **THE `z=1.157e-16` TRIGGER IS A RED HERRING.** It is NOT a degenerate baseline - it is the float-noise `z` of a **normalised unit direction** vector, emitted by `WallTransformController.activateFor` (`packages/input-host/src/WallTransformController.ts:224`) which runs on **SELECTION, not on the move**, reporting the wall's own axis (a wall running along -X). The entire 'degeneracy smell' premise collapses on that fact. **(a) REFUTED** - a 3D-gizmo wall move is a **pure translation**: both endpoints shift by the same (dx,dz) (`registerTransformDragHandler.ts:148-149`), so direction is preserved => dot>0 => the reversal swap (`UpdateWallBaselineCommand.ts:141`) is **never taken on a move**. Nothing to oscillate. **(b) REFUTED** - the re-host cascade is already bounded: `WallRebuildCoordinator._joinsResolving` re-entry block (`:299-302`) plus SS-FIX-WALLFLUSH-NOPROGRESS-GUARD (L-97, `:1130-1166`); and `apps/editor/__tests__/wallFlushNoProgressGuard.test.ts` **already drives the real coordinator through 'move a door-bearing wall'** asserting convergence (<=6 commits / 80 frames). It passes. **(c) REFUTED** - `WallOccupancyStore` is pure with one bounded loop; `DoorBuilder.rebuildForWall` (`:229`) is O(K) via the ADR-0099 reverse index and drains **frame-throttled** (`_MAX_BUILDS=12`), so it cannot peg the main thread in one span. **(d) REFUTED** - `WallJunctionClustering.find()` (`:75-78`) is path-compressed union-find (strictly descends to root); `JunctionResolverV2` contains **zero `while` loops**; `LayeredWallOpeningBuilder.growX/growY` are bounded by a small breakpoint grid with finiteness guards (`:134-138`). **Both historical hang classes are ALREADY FIXED** - which is why it kept 'recurring' past each attempt: ADR-0099 killed the O(walls x openings) scan, and SS-WJR-NAN-GUARD (producer `WallJoinResolver.ts:2146-2191` + consumer `WallFragmentBuilder.ts:914-932`) killed the NaN-geometry class. Measured: `resolveLevel` on the founder's exact hosted-door move completes in **20 ms**, returns finite geometry, and is **idempotent (a fixed point)**. Regression test committed (`e6defe35`, 3 tests). **-> THE REAL SUSPECT IS A PERF CLIFF, NOT A LOOP.** A baseline move does **not** qualify for ADR-0257's openings-only fast path, so **every** gizmo move takes the whole-level path and **synchronously rebuilds every wall body on the level** (`buildWall` x N, CSG/extrude per opening-bearing wall) - near-silent because those logs are gated behind `perfTraceOn()`/`_wallDiagOn()`. This is **precisely the follow-up ADR-0099's own 'Consequences' section explicitly DEFERRED** ('making `WallJoinResolver.resolveLevel` incremental for a single moved wall'). On a large project this is a multi-second synchronous stall that reads as a freeze. **FOUNDER 1-MINUTE CONFIRMATION:** set `globalThis.__pryzmWallCornerDiag = true`, then move a hosted-door wall on the BIG project - **if the stall scales with wall count, it is the cliff, not a loop.** | route -> wall/hosted-element agent. **MEASURE-FIRST (hang with no logs = the L-205 discipline): the FIRST deliverable is LOCATING the hang, not a fix.** (1) Add a **main-thread watchdog / loop-iteration guard** across wall-move -> baseline-update -> rebuild -> opening-re-host -> join-resolve: bounded iteration counters that throw+log after N iterations, and a frame-budget watchdog that dumps the active call stack when a synchronous span exceeds ~1 s. This converts the silent freeze into a located, logged failure — the thing every prior attempt lacked. (2) **Reproduce deterministically:** a wall hosting a door moved to a near-degenerate / endpoint-reversing baseline (the z~1e-16 case); add it as a fixture. (3) Only then fix the loop — likely a missing fixpoint/guard in the re-host cascade or a degenerate-baseline guard in the reversal/join path. **A degenerate baseline (near-zero length or dot~0) must be REJECTED with a user toast, never processed into a loop.** (4) Preserve the `BaselineReversalError` guard (CORRECT — offsets depend on endpoint[0]); stop the swap/rebuild CASCADING, do not remove the guard. (5) Tests: moving a hosted-door wall to a degenerate baseline terminates (bounded), commits or cleanly rejects, never hangs; the re-host cascade reaches a fixpoint in <=1 pass. §FIX-WALL-MOVE-HOSTED-DOOR-FREEZE. |
| L-235 | orchestrator (found while landing C13 §3.9/§3.10 from L-224) | **[LOW — doc hygiene] `geometry-furniture` cites a contract number that cannot be right.** `BedBuilder.ts:55`, `BedPlanSymbolBuilder.ts:2`, `CornerSofaBuilder.ts:130/133/351`, `SofaPlanSymbolBuilder.ts:2` all cite **"Contract 48 §3.4 / §3.5 / §5"** for **plan-symbol / elevation-silhouette rules** (e.g. *"structural shells are skipped in plan view"*, *"silhouette in elevation projections"*). But **C48 is Backup & DR** — it says nothing about plan symbols. This is a stale reference from an older numbering scheme (the same class of rot L-224 found in `ProjectIsolationAudit`/`ProjectScopedStorage`, which cited "Contract 48" when they meant **C13**). **Not renamed blindly** — unlike the persistence case, it is NOT obvious which contract these furniture rules actually belong to (C06? DOC-2.x plan symbols?), and guessing would replace one wrong citation with another. | Doc hygiene / contract references (C00 contract-suite index) | OPEN (queued, LOW) | route -> documentation agent (small; can ride with any plan-symbol pass, e.g. L-211/L-225). Establish which contract actually governs the furniture plan-symbol / elevation-silhouette rules (the §3.4/§3.5/§5 numbering suggests a specific doc — find it in `docs/02-decisions/contracts/` or the archive), then correct all 6 citations. If the governing rules were never written into a live contract, that is the real finding — say so and raise it, rather than pointing the comments at an arbitrary C-number. **Related:** the plumbing symbol builders (L-221) and any new symbol work should cite the SAME contract once it is identified. §FIX-STALE-CONTRACT48-FURNITURE-REFS. |
| L-236 | orchestrator (process failure, self-reported) | **[HIGH - process] Multi-agent shared-checkout collisions destroyed commits and pushed a non-compiling `main`.** Three distinct failures in one round, all from agents sharing ONE git checkout. **(1) `git reset` destroyed peers' commits.** Two agents ran `git reset HEAD~1` - almost certainly to undo their own broad `git add` - and each deleted the PREVIOUS agent's commit (`a7896bcd` L-232, `5d91c6c8` L-231). Both were recovered from reflog, but only because the reflog was checked. **(2) Whole-file `git add` on a SHARED file cross-contaminated commits.** L-231 and L-232 both edited `CesiumViewport.ts`; each `git add`-ed the whole file, so each commit absorbed the other's hunks. L-234's test file was swept into L-231's commit the same way. **(3) THE REAL DAMAGE - a broken `main`.** That cross-contamination meant `068ff74d` shipped a `CesiumViewport.ts` which *imports* `planFacadeSampling` while its *definition* sat in L-232's still-uncommitted `siteMetricGrids.ts`. **HEAD did not compile and the orchestrator pushed it** (`34819f03`). Root cause of the push: the orchestrator gated on the AGENTS' tsc runs instead of re-running tsc on the merged HEAD. Repaired by `9c8535e9`; `main` is green again (tsc exit 0). | Process / multi-agent orchestration (memory: multi-agent-shared-tree-collisions) | OPEN (process fix - applies to every future dispatch) | **Already mandatory in every brief:** (a) **NEVER** `git reset` / `checkout -- .` / `stash` / `rebase` / `commit --amend` - if work goes missing, REPORT, do not self-recover; (b) **NEVER** `git add -A`/`.`/`-u` - stage only own files by explicit path. **NEW rules from this incident:** (c) **Never dispatch two agents onto the same FILE.** Fences must be per-FILE, not per-concern - 'you own the facade sections, they own the device-loss sections' is **unenforceable through `git add <file>`**. The orchestrator must SERIALIZE edits to a shared file. (d) **The orchestrator MUST re-run root `tsc` on the merged HEAD immediately before every push.** An agent's green tsc only proves ITS tree compiled - not that HEAD does once another agent's commit lands. This one rule would have caught the broken push. (e) Prefer having agents report a patch for a shared file rather than commit it. §FIX-MULTI-AGENT-TREE-DISCIPLINE. |
| L-237 | founder (the L-205 grey rectangle, with DECISIVE new evidence) | **[HIGH] The grey rectangle appears EXACTLY when the first caster is created, and a renderer swap in EITHER direction fixes it permanently.** Founder: *"when project starts the canvas is white, the 3D renders perfect. After the first wall is created the wrong rectangular grey shape renders. Then I switch to WebGL - render is perfect. Then I switch back to WebGPU - also renders perfect, all walls with the shade projection on the invisible ground layer, sound. However during the first element creation and until I switch, the wrong grey rectangle is present."* **THIS IS THE MOST DIAGNOSTIC EVIDENCE THE L-205 FAMILY HAS EVER HAD**, and it closes the mechanism. **(1) THE TRIGGER IS THE CATCHER-REVEAL, NOT A ROGUE CASTER.** `RealEnvironmentService` deliberately keeps the ground catcher **attached but INVISIBLE until the first caster** (`_applyCatcher` -> `this._ground.setEnabled(this._hasCasters)`, `:184-189`; boot log: `ground=on (catcher attached; hidden until first caster)`). That visibility gate is itself a **workaround** for the grey on an empty project. The moment the first wall lands, `updateGroundCatcherVisibility()` reveals the plane - and the grey appears. **(2) THE CODE ALREADY CONFESSES THE REAL BUG.** `RealEnvironmentService.ts:141-143`, verbatim: *"(The cosmetic empty-project grey - **ShadowMaterial not fully transparent where unlit on WebGPU** - is a SEPARATE follow-up that must NOT touch this receive path.)"* So a known, never-fixed WebGPU ShadowMaterial compositing defect is on record; the caster-visibility gate merely HID it on an empty scene and it re-surfaces the instant the catcher is shown. **(3) THE MECHANISM (C04 §SHADOW.0).** The catcher paints `alpha = opacity x (1 - shadowMask)`. Lit => `shadowMask=1` => alpha 0 => invisible. If the shadow map is **not validly bound / not yet rendered** when the catcher first composites, every fragment reads `shadowMask = 0` => `alpha = opacity` => **a solid grey plane bounded by the shadow camera** - which is exactly the founder's rectangle. **(4) WHY THE SWAP CURES IT.** `pryzmSwapRendererBackend` rebuilds the whole renderer + TSL pipeline, recompiling the ShadowNode material against a freshly-bound shadow map. After that the mask resolves correctly and the grey never returns - **in either direction**, which rules out 'WebGL is better' and proves the defect is a **transient invalid-shadow-map state at first-caster**, not a backend difference. (Consistent with the founder's own note: *'I don't believe WebGL renders - I think it is always WebGPU'*.) **(5) DIRECT LINK TO L-231 (just landed, 068ff74d).** The first caster fires `scheduleShadowRebuild()`. L-231 MEASURED that the normal branch of that path cleared `_hasPipelineError = false` **synchronously before awaiting the async rebuild**, so the render gate stayed **OPEN for the entire rebuild** and frames kept submitting against a pipeline being torn down and a shadow map being reallocated - **precisely the window in which the catcher would read `shadowMask = 0`.** L-231 also found the whole shadow-freeze family was writing `renderer.shadowMap.autoUpdate`, which is **INERT on the WebGPU node path** (C04 §SHADOW rule 10), so the map was regenerating uncontrolled while nominally frozen. **L-231's fix pauses WebGPU submits for the rebuild AND writes the real per-light shadow flags - it may already fix this grey.** | Rendering / shadow compositing (C04 §SHADOW.0 + rules 7/10/11; ADR-0111; ADR-0120; L-205 lineage) | OPEN (queued, HIGH) - **RE-TEST ON THE L-231 BUILD FIRST** | route -> rendering/shadow agent. **STEP 0 (founder, 1 minute): re-test on the deploy containing L-231 (068ff74d).** If the grey is gone, this is closed by L-231 and the remaining work is only the regression test. **If it persists:** (1) The defect is the catcher compositing against an **unbound/unrendered shadow map** at the first-caster transition. The sound fix is an **ordering guarantee**: the catcher must not be made visible until the shadow depth map has actually been rendered at least once WITH the new caster - i.e. gate `setEnabled(true)` on 'shadow map valid', not merely on 'a caster exists'. (2) **Do NOT re-litigate the refuted L-205 hypotheses** (C04 §SHADOW.3 lists nine; the caster-denylist and the shadow-camera size are NOT the cause - resizing the camera only ever changed the grey's SIZE). (3) Investigate the recorded WebGPU `ShadowMaterial` transparency defect (`RealEnvironmentService.ts:141-143`) on its own terms - is `ShadowNodeMaterial`/`ShadowMaskModel` compositing correctly when the depth map is empty? Per C04 §SHADOW.11 the frustum test returns LIT outside the frustum, so an **empty/unrendered map inside the frustum** is the suspect state. (4) The caster-visibility gate is a WORKAROUND masking a real bug - once the compositing is correct, consider whether the gate is still needed. (5) Tests: creating the FIRST caster in a fresh project does not produce a fully-opaque catcher; the catcher's alpha is 0 wherever lit, on the WebGPU path, WITHOUT a renderer swap. §FIX-GREY-CATCHER-FIRST-CASTER-UNBOUND-SHADOWMAP. |
| L-238 | founder ("project isolation is still not sound - I open an old project, go back to Projects, create a NEW project, and the new project has an object on screen - it seems one of those corrupted triangular shapes created in wall joints of 3 walls in L or T shape") | **[HIGH - data integrity] An UN-REGISTERED wall-junction mesh leaks into a new project, and BOTH the teardown and the audit are structurally blind to it.** The founder's screenshot shows a fresh, empty project containing one stray wall-ish shard plus the origin sphere. **L-224's C13 teardown IS running** (his log: `[ProjectLifecycleController] C13 project-switch`, `[BatchCoordinator] C13 forceReset()`, `[WallRebuildCoordinator] C13 resetWallRebuildState()`, `C13 teardown complete`, and `[ProjectIsolationAudit] Installed - will audit every project load`) - **and the leak still happens, with NO violation reported.** That is the tell. **ROOT CAUSE - the id-less-mesh blind spot (code-confirmed).** `ProjectIsolationAudit.ts:126` gates its scene check on: `if (idKnown && ud.elementId != null && ud.elementType != null)`. **A scene node with NO `elementId` is never examined.** A wall junction / mitre / corner-weld mesh - precisely the *"corrupted triangular shape at an L or T joint"* the founder describes, and the known `WallJoinResolver` degenerate-wall artifact class (a degenerate wall is skipped by the guard but its MESH still renders) - carries **no element id**. Consequently: **(a)** `ClearProjectCommand` clears by element id, so it does not remove it; **(b)** `WallFragmentBuilder.dispose()` (which does fire: `[ProjectIsolation] WallFragmentBuilder disposed`) only clears geometry it OWNS - a junction artifact parented elsewhere, or re-emitted from stale join state, survives; **(c)** the audit **skips it silently**, so the tripwire reports nothing. An id-less mesh is invisible to the clear AND to the guard. **THIS BLIND SPOT IS A DIRECT CONSEQUENCE OF THE L-224 DESIGN THE ORCHESTRATOR APPROVED** (skip nodes with no trackable id, to hold the false-positive rate at zero). The trade-off let a real leak through - recorded plainly. Note L-233's agent independently applied the same skip-if-no-id predicate for its explode reconcile, on the same reasoning; that is fine for a VISUAL offset, but **for an isolation guard it is a hole.** **ARCHITECTURAL FRAMING - C13 §3.10 already forbids this.** The invariant just landed says the audit must enumerate **OWNERS**, not symptoms: *every stateful surface has exactly one named owner.* **A mesh in the scene with no element id has NO OWNER - it is, by definition, the violation C13 §3.10 exists to catch.** After a clear, the scene root must contain no un-owned children. | Persistence / project isolation + wall-join geometry (**C13 §3.10** - the owner invariant; C11; ADR-0120; WallJoinResolver degenerate-wall class) | OPEN (queued, HIGH) | route -> persistence/project-lifecycle agent (the L-224 agent - it owns this code and designed the expectation-set check). **(1) Close the audit hole: detect ORPHAN meshes.** The audit must flag any scene-root descendant that is renderable but carries **no owner** (no `elementId`, and not in the known-infrastructure allowlist: catcher, grid, origin sphere, gizmo, outline, TransformControls helper). It must NOT silently skip them. This is the C13 §3.10 owner-enumeration rule applied to the scene graph. **Beware the L-233 perf trap** - selection outlines / helper objects are legitimately id-less, so use a positive infrastructure allowlist, not a blanket skip. **(2) Close the teardown hole:** establish WHO owns wall junction/mitre meshes and ensure the C13 teardown disposes them. If they are parented outside `WallFragmentBuilder`, that is the bug - a geometry artifact with no owner must not exist (ADR-0120 made the same argument for the shadow caster set). **(3) Trace the actual leaked object** - instrument the fresh project to dump every scene-root child with its `userData` after `ClearProjectCommand` completes; name the shard. Do not guess which mesh it is. **(4)** Related: the `WallJoinResolver` degenerate-wall bug (guard skips the wall, mesh still renders) is the likely PRODUCER - an artifact that should never have been created in the first place. Fix the producer as well as the cleanup. **(5)** Tests: after `ClearProjectCommand`, the scene root has zero renderable descendants outside the infrastructure allowlist; a junction artifact from project A is not present in project B; the audit REPORTS an id-less orphan mesh (it must fail loudly, not skip). §FIX-ORPHAN-MESH-ISOLATION-BLINDSPOT. |
| L-239 | founder (RECURRENT, raised many times) | **[HIGH - correctness] A layered wall created in PLAN VIEW gets the right thickness but NO LAYERS; the same wall created in 3D gets its layers. Root cause: the layer stack is NEVER STORED on the wall - 3D DERIVES it at render, plan READS it.** Founder: *"The user chooses a layered interior wall and creates it on plan view. The wall is created, the correct thickness is applied - however the layers are not present. Whereas if the user creates the wall in 3D, same interior wall, the wall is created with the correct layers. Why?"* **THE CODE ANSWERS IT VERBATIM.** `apps/editor/src/engine/views/plantools/WallPlanToolHandler.ts:422-423`, in the header of the earlier L-41 fix: *"3D looked right because **the 3D builder re-resolves layers/thickness from `systemTypeId` at render**; PLAN view reads the **stored** thickness."* **So the wall's layer stack is never persisted on the INSTANCE.** The 3D builder DERIVES it from `systemTypeId` every render, so 3D can never be wrong. Plan reads what is actually on the wall record - and there is no `layers` array there. The prior fix (**§FIX-PLAN-WALL-TYPE-IGNORED, L-41**, `:410-430`) hit exactly this wall for **thickness** and worked around it by resolving the thickness LOCALLY (`_getSelectedWallThickness()`) and storing it explicitly - **but nobody did the same for `layers`.** That is why the founder sees the right thickness and no layers: **the thickness was patched onto the instance; the layers never were.** **This also explains L-211** (layered wall renders layers in 3D but not in plan): `WallLayerPlanSymbolBuilder.ts:68` gates on `if (!wall.layers || wall.layers.length < 2 || wall.curve) continue;` - it reads `layers` **from the instance**, which is empty. L-211 and L-239 are **the same root**, seen from the symbol side and the creation side. **Contributing hazard named in the same comment:** the `systemTypeId` -> thickness override (§WALL-TYPE-THICKNESS in `CreateWall.ts`) fires ONLY when a populated `WallSystemTypeStore` is wired into the ACTIVE `wall.create` handler, and a **"first-registration-wins" facade** means the picked `systemTypeId` reaches the store but its thickness never resolves. So the handler that wins registration decides whether type resolution works at all - a P1/composition-root smell. **ARCHITECTURAL CLASS: the same C11 violation as L-213 (floor finishes), L-214 (wardrobe), L-220 (plumbing/floor drag) - ONE element type, TWO creation paths, and a field silently dropped by one of them.** | Element creation / wall types (C11 - one element, one creation pipeline; C03 store schema; L-41 + L-211 lineage) | **FOUNDER DECISION (2026-07-12, BINDING): the INSTANCE is canonical.** *"The layers NEED to be STORED IN THE STORE - and have an architecturally sound engine strategy for ANY type of creation."* So option (b): `layers` is persisted on the wall record, and the strategy must hold for **every** creation path - plan tool, 3D tool, batch generators, AI, import, paste - **by construction, not by each tool remembering.** **THE SEAM THAT MAKES IT BY-CONSTRUCTION:** resolve `systemTypeId` -> `layers` **ONCE, inside the `wall.create` COMMAND HANDLER** (the chokepoint every creation path already dispatches through), NOT in the individual tools. That is precisely why this bug exists - each tool builds its own payload, so one forgot. Move the resolution below the tools and no future tool can forget. The 3D builder then READS the stored `layers` instead of re-deriving from `systemTypeId`, which collapses the two-source split at its root. | **FIXED (e908f99d) - §FIX-WALL-LAYERS-PLAN-VS-3D-CREATION. CLOSES L-211 TOO. MY ROOT CAUSE WAS DIRECTIONALLY RIGHT BUT MECHANICALLY WRONG - THE AGENT REFUTED IT FROM SOURCE AND FOUND TWO LATENT BUGS I NEVER SUSPECTED.** **REFUTED (my claim):** *"the 3D builder re-resolves layers from `systemTypeId` at render, so 3D can never be wrong."* **FALSE. The 3D builder derives NOTHING.** `WallFragmentBuilder.ts:1092` reads `wall.layers` **off the INSTANCE**, exactly like `WallLayerPlanSymbolBuilder.ts:68`. **Both readers were already correct; the instance was ALREADY canonical for rendering.** So P3 of my plan was a no-op, and **the L-41 comment I quoted as the smoking gun (`WallPlanToolHandler.ts:422-423`) is simply WRONG** - it has been corrected in place. *A load-bearing comment that was false for months, and I took it as evidence. Read the code, not the comment about the code.* **THE ACTUAL MECHANISM (two independent faults that happened to cancel out on ONE path):** **(1)** `CreateWallHandler` **declared `layers` on its payload (line 60) and never wrote it into `Wall.parse`.** The canonical bus handler **dropped the field on the floor** - no bus-created wall has EVER carried a layer stack. **(2)** 3D only *looked* right because `WallTool` dispatches `wall.create` **and then ALSO falls through to the legacy `CreateWallCommand`** (`WallTool.ts:1788`), which DOES resolve and stamp `layers`. The bus command is async, so the legacy write lands FIRST and the bridge's `alreadyMirrored` guard skips. **The plan tool is bus-ONLY** - no legacy dual-write, no stamp, plain wall in BOTH views. **So the founder's fingerprint (thickness right, layers absent) came from a MISSING LINE in the handler plus an ACCIDENTAL legacy dual-write - not from render-time derivation.** **A SECOND SEAM HAD TO MOVE OR THE FIX WOULD HAVE BEEN INVISIBLE:** `CommandEventBridge` emitted `wall.created` from **`record.payload` - the REQUEST, not the COMMIT.** The legacy-store mirror (which feeds BOTH the 3D mesh and the plan) is driven by that event, so resolving in the handler alone would have populated the PRYZM3 store and changed **nothing on screen**. The bridge now reads the committed Immer `add` patch. **An event must describe the COMMIT, not the request (ADR-0202 §5).** **LATENT BUG #1 FOUND AND FIXED - BATCH WALLS WERE NEVER TYPED.** The batch path (`wall.batch.create`: AI, apartment, house, office generators) **resolved NEITHER thickness NOR layers.** **Every generator-created typed wall in PRYZM has been unlayered.** Closed by the same chokepoint. **LATENT BUG #2 FOUND AND FIXED - CUSTOM-TYPE WALLS LOST THEIR LAYERS ON EVERY LOAD (data loss).** `ProjectSerializer` has ALWAYS persisted `wall.layers`, but `ProjectLoader` **dropped it on restore** and asked `CreateWallCommand` to re-derive from `systemTypeId`. That CANNOT work for a **user-defined** type: `ProjectLoader.load()` restores walls at `:765` and custom `wallSystemTypes` at `:1336` - **AFTER**. So a wall of a custom type came back **plain, every single load.** Now: persisted stack wins; absent ⇒ catalogue backfill (upgrades legacy projects); type missing ⇒ unlayered at persisted thickness + a loud warn (no corruption). Idempotent, and that is tested. **P5 WAS ALREADY FIXED, NOT BY ME.** The "first-registration-wins" facade was closed at the composition root by **ADR-0116** (2026-07-03, `buildSharedWallCatalogue()` in `PluginRegistry.ts:207`). Resolution no longer depends on registration order. Only the stale comment needed correcting. **CHOKEPOINT ACHIEVED (the founder's binding decision, honoured):** all four bus paths - `wall.create` (plan + 3D), `wall.batch.create` (AI/apartment/house/office), `wall.createBetweenMarks`, `wall.createFromSlab` - now resolve `systemTypeId` -> `{thickness, layers}` via ONE new pure module, `plugins/wall/src/handlers/resolveWallSystemType.ts`. **KNOWN REMAINING BYPASS (declared, not hidden):** the legacy `CreateWallCommand` (`packages/command-registry/src/walls/CreateWallCommand.ts`) writes the legacy `WallStore` - the store BOTH renderers read - **without passing through `wall.create`**. It is used by `WallTool`'s dual-write, `CreateWallsFromSlab` and `ProjectLoader`. **Because it is not yet the sole writer, the plan tool's local thickness resolution was NOT deleted** (my brief gated that deletion on sole-writer status). It is now a redundant-but-idempotent fallback, documented as such. **-> FOLLOW-UP L-244: collapse the legacy dual-write so the chokepoint is the ONLY writer.** **Gate:** new suite `apps/editor/__tests__/wallLayersChokepoint.test.ts` **8/8** (wired to the REAL production catalogue adapter; includes the L-213-style guard that a plan-created and a 3D-created wall produce **identical stored records including `layers`**); `geometry-wall` 202/202; `runtime-composer` 136/136; ADR-0116 suite green. Zero tsc errors in the agent's files. | Element creation / wall types (C11; C03; ADR-0202 §5 events-describe-commits; ADR-0116) | **FIXED (e908f99d)** - awaiting merged-HEAD tsc + push. **CLOSES L-211.** | **FOUNDER SIGN-OFF WANTED (1 item):** when a caller supplies BOTH a `systemTypeId` AND a conflicting explicit `thickness`, **the TYPE's total now wins** in the batch path (it already did for single creates). This is forced: `thickness` must equal `sum(layers)` or plan and 3D diverge again. Generators only pass `systemTypeId` when a real type is chosen, so nothing regressed. Both are defensible; what is NOT defensible is today's split, where 3D derives from the catalogue and plan reads the instance. **(a) If the catalogue is canonical** (it is what the 3D builder already trusts), then `WallLayerPlanSymbolBuilder` and every plan/doc consumer must **resolve through the systemType store**, exactly as the 3D builder does - and `layers` must NOT be duplicated onto the instance. **(b) If the instance is canonical**, then `wall.create` must persist the resolved `layers` at creation, from BOTH paths, and the 3D builder must read the instance rather than re-deriving. Pick one, write it into C11/C03, and make every consumer obey it. **(2) Fix the `wall.create` payload divergence** between `WallPlanToolHandler` (`resolveActiveWallSystemTypeId()` + locally-resolved thickness) and `WallTool` (`this.selectedSystemTypeId`, thickness overridden by the command) - **converge them on ONE payload builder**, the way L-214's `buildFurnitureCreatePayload()` did, so a dropped field becomes a compile error rather than a silent visual difference. **(3) Fix the first-registration-wins facade** so `systemTypeId` resolution does not depend on which handler happened to register first (P1). **(4) Tests: a layered wall created in PLAN and the SAME type created in 3D produce IDENTICAL stored records AND identical layer rendering in BOTH views** - this is the L-213 equality-test pattern and it is the guard that keeps it fixed. **(5) Closes L-211 as the same root.** §FIX-WALL-LAYERS-PLAN-VS-3D-CREATION. |
| L-240 | founder (RECURRENT - "this is a recurrent error that I have already requested") | **[HIGH - correctness] The AUTO floor-finish lands on the wall CENTRELINE, not the wall INNER FACE - because the 3D FloorTool's AUTO_FROM_ROOM mode is a THIRD creation path that L-213 never converged.** Founder: *"On floor finish creation - AUTO - the floor finish gets created perfectly fine, HOWEVER the perimeter of it is aligned with the CENTER of the wall. I would like it aligned with the INNER FACE of the wall. This is already happening correctly on the BATCH element creations during the residential house and residential building AI batch creation."* **His diagnosis is exactly right, and it is code-confirmed.** **ROOT CAUSE (file:line).** `packages/geometry-slab/src/floor/FloorTool.ts:921`, in the `AUTO_FROM_ROOM` branch: `const polygon: FloorVertex[] = room.boundary.polygon.map((v) => ({ x: v.x, z: v.z }));` - it passes the room boundary polygon **STRAIGHT THROUGH** to `_createFloor(polygon)`. And the room boundary **runs along wall CENTRELINES** (`LAYOUT-GENERATION-ALGORITHM.md:1780`). So the finish is laid on the centreline ring. `packages/geometry-slab` does not even import `@pryzm/room-topology` - **the inner-face inset is never applied on this path.** **THE CANONICAL FIX ALREADY EXISTS AND IS ALREADY SHARED - BY THE OTHER TWO PATHS ONLY.** `deriveRoomFinishBoundary(centreline, walls)` (`packages/room-topology/src/RoomPolygonUtils.ts:845`, wrapping `insetPolygonToInnerFaces` at `:233`, tagged `§FLOOR-INNER-FACE`) offsets each edge inward by the bounding wall's half-thickness and miters the corners. Its callers today are **exactly two**: `apps/editor/src/engine/views/plantools/FloorPlanToolHandler.ts:246` (the PLAN floor tool) and `packages/command-registry/src/floors/CreateFloorsByRoomTypeCommand.ts:311` (**the BATCH generator - which is why the founder correctly observes the residential house/building AI batch gets it RIGHT**). The 3D `FloorTool.AUTO_FROM_ROOM` path calls **neither**. **WHY IT IS 'RECURRENT': L-213 IS THE SAME BUG AND ITS FIX WAS INCOMPLETE.** L-213 (floor finishes) converged the plan tool and the batch command onto the single canonical `deriveRoomFinishBoundary` - and the convergence test `packages/room-topology/src/__tests__/deriveRoomFinishBoundary.test.ts:22` is literally titled *"(L-213 UI<->batch convergence)"*. **It asserts the two paths agree - it does not assert that they are the ONLY paths.** The third path (3D FloorTool AUTO) was never enumerated, so it silently kept its raw-centreline behaviour and the equality test stayed green. **The guard tested the paths it knew about, not the element type.** **ARCHITECTURAL CLASS: the C11 violation AGAIN - ONE element type, N creation paths, a derivation silently omitted by one of them.** Fifth instance this cycle: L-213 (floor finishes), L-214 (wardrobe rotation), L-220 (plumbing/floor drag), L-239 (wall layers), now L-240. **THE META-LESSON IS NOW UNAVOIDABLE: converging TWO paths is not a fix, it is a coincidence. The fix is to make the derivation IMPOSSIBLE TO BYPASS - push it BELOW every tool, into the command chokepoint - which is exactly the seam the founder mandated for L-239.** | Element creation / floor finishes (**C11 §5.4** - NEW normative rule added by this fix; C16; §FLOOR-INNER-FACE; **L-213 lineage - incomplete fix**) | **FIXED (31c16d0d)** - awaiting merged-HEAD tsc + push. **MY BRIEF WAS REFUTED ON THE DECISIVE POINT AND THE AGENT WAS RIGHT.** **(1) I said "put the derivation in the `floor.create` COMMAND HANDLER". THAT WOULD HAVE FIXED ONE PATH AND MISSED THE FOUNDER'S.** There are **TWO command implementations for `floor`**: **10 of the 11 creation paths go through the LEGACY `CreateFloorCommand`** (`@pryzm/command-registry`, via `commandManager.execute()`) - **only the plan tool reaches the bus `floor.create` handler.** The founder's 3D AUTO path never touches `floor.create`. **The real chokepoint is `CreateFloorCommand`**, and that is where the derivation now lives. **(2) I said "key it off `hostRoomId`". INSUFFICIENT** - DRAW mode ALSO sets `hostRoomId` (via centroid autodetect for HOSTING), so keying on it alone would have silently re-inset hand-drawn floors, violating my own P3. The agent added an explicit `boundarySource` discriminator **plus a `ringsCoincide` safety net**: a payload whose polygon IS the host room's centreline ring is recognised as room-derived and inset **even if the tool forgot to declare it**. **The discriminator is SAFE WHEN OMITTED - so omission is no longer fatal, which is the property that actually kills this bug class.** **(3) My P1 layering hypothesis was RIGHT:** `geometry-slab` -> `room-topology` is NOT sound (it adds an arc to the existing `command-registry <-> room-topology <-> geometry-slab` SCC - the barrel-at-module-load white-screen class) so the import was NOT forced; `command-registry` already declares `room-topology`, so the fix added **zero new dependency edges**. The bus handler is **structurally incapable** of hosting the rule anyway (its `HandlerContext` exposes only the `floor` store; rooms + walls are legacy singletons, and P4 forbids a plugin handler reaching them). **PATH ENUMERATION DELIVERED (11 paths)** - the deliverable L-213 never produced. **THIRD BUG FOUND AND FIXED:** the plan tool never sent `hostRoomId` at all, so **plan-AUTO floors were never linked to their room** (empty `coveredRoomIds`, no room finish absorbed, no floor->room schedule join). **DUPLICATED LOGIC DELETED:** `_innerFacePolygon` existed **verbatim** in BOTH `CreateFloorsByRoomTypeCommand` and `FloorPlanToolHandler`; both now call one resolver. **C11 §5.4 ADDED (normative):** *element-type domain rules resolve at the COMMAND, never in the tool* - tools capture INTENT, not geometry; any rule of the form "a <type> is always..." belongs in the handler; payload discriminators must be **safe when omitted**; **convergence tests must be N-WAY and must exercise the CHOKEPOINT, not the helper** (that last rule is exactly why L-213's 2-way helper test stayed green while the bug shipped). Plus §5.4.1 the floor-boundary rule, §5.4.2 DRAW-vs-AUTO ("settled, do not re-litigate"), §5.4.3 the dual-command debt. **Gate:** new N-way suite `apps/editor/__tests__/floorFinishInnerFaceAllPaths.test.ts` **9/9** against the REAL `CreateFloorCommand` - 3D-AUTO / BATCH / PLAN produce an **identical polygon and area** (10.64 m2, vs the 12.00 m2 centreline overshoot); DRAW stored verbatim; an undeclared centreline is still inset; an already-inset ring is **never inset twice**; stored polygon + `coveredRoomIds` + `IfcCovering` all inherit the corrected ring. `room-topology` 59/59, `geometry-slab` 16/16, **root tsc exit 0**. Batch output **byte-identical** (non-goal honoured). | route -> element-creation/floor agent. **(1) THE ONE-LINE SYMPTOM FIX IS NOT THE TASK.** Making `FloorTool.ts:921` call `deriveRoomFinishBoundary` fixes the founder's bug and leaves the class alive for the next tool. **(2) DO IT THE L-239 WAY: move the derivation to the CHOKEPOINT.** The inner-face inset is a **domain rule of the floor-finish element type** ("a floor finish is bounded by the inner faces of its bounding walls"), not a per-tool decision. It belongs in the `floor.create` COMMAND HANDLER, keyed off `hostRoomId`: if a floor is hosted by a room, the handler derives the boundary from that room's centreline ring + the bounding walls. Then the 3D tool, the plan tool, the batch generator, AI, import and paste ALL get it by construction, and no future tool can omit it. Note `FloorTool` already sets `this._pendingHostRoomId = room.id` (`:919`) - **the host-room link the handler needs is already there and is currently dropped on the floor.** **(3) LAYERING CHECK (do this before coding):** `packages/geometry-slab` does not import `@pryzm/room-topology` today - verify the dependency direction is legal under the 8-layer rule before adding it. If it is not, that is further evidence the derivation belongs at the command layer, not in the tool. **(4) ENUMERATE EVERY floor-finish creation path and prove each one goes through the chokepoint** - the enumeration is the deliverable; L-213 failed precisely because nobody enumerated. **(5) Tests: an N-WAY equality test, not a 2-way one** - a floor finish created via 3D AUTO_FROM_ROOM, via 3D DRAW, via the PLAN tool, and via the BATCH generator, in the same room, must produce the SAME boundary polygon, inset to the inner faces. Upgrade `deriveRoomFinishBoundary.test.ts` from "UI<->batch" to "all paths". **(6)** Confirm the DRAW mode (`FloorTool.ts:553`, which auto-detects the host room from the polygon centroid) does the right thing - a hand-drawn polygon is the user's explicit intent and should NOT be silently re-inset; only ROOM-DERIVED boundaries get the inset. State that rule explicitly in the contract. §FIX-FLOOR-FINISH-INNER-FACE-ALL-PATHS. |
| L-241 | founder (FEATURE REQUEST + quality defect, 3 reference images) | **[MEDIUM-HIGH - drawing quality / feature] The door plan symbol is geometrically TRUE but graphically UNSOUND, and PRYZM has no DETAIL LEVEL. Founder wants BOTH an LOD-100 and an LOD-200/300 door symbol, selectable per-view AND per-element.** Founder: *"Look at the quality of the door in plan view - I want a SOUND door, still absolutely accurate with regards to its element dims. Check the second image - I want exactly this. I have attached an LOD 200-300 image (second) and the third image [is] probably an LOD 100. I WANT ALL DOORS TO HAVE THE TWO OPTIONS - in the properties panel the user can choose - ALSO THROUGH THE VISIBILITY INTENT. In any case the FRAME of the door is present, the LEAF of the door in plan view renders OPENED. At the moment the projection in plan view is TRUE - although it has a symbol rendering in plan view, THE SYMBOL NEEDS TO BE BETTER DONE - MORE SOUND."* **He is diagnosing correctly: the geometry is right, the DRAUGHTING is wrong.** This is a presentation-quality defect, not a correctness defect - the opposite of most of this log, and it must be fixed WITHOUT touching dimensional accuracy. **FINDING 1 - `detailLevel` ALREADY EXISTS END-TO-END AND NOTHING CONSUMES IT. IT IS A DEAD KNOB.** The user can already pick Coarse/Medium/Fine in the UI **and it changes nothing on screen**: schema `packages/schemas/src/view/view-template.ts:224` (`z.enum(['Coarse','Medium','Fine']).default('Medium')`); view store `packages/core-app-model/src/views/ViewDefinitionTypes.ts:245` (`detailLevel?: 'coarse'|'medium'|'fine'`); command validator `packages/command-registry/src/views/SetViewOutputCommand.ts:57-58`; defaults `DefaultViewsManager.ts:279/302/329` (every default view ships `detailLevel: 'medium'`); **a LIVE dropdown** at `apps/editor/src/ui/ViewPropertiesPanelBuilders.ts:258-265`; a lockable view-template field (`ViewTemplateManagerPanel.ts:26`); a template-sync case (`SyncStateEngine.ts:315`); an intent migration mapping (`ViewTemplateToIntentMigration.ts:83`). **Every layer of the plumbing is built. NO SYMBOL BUILDER READS IT.** So the founder's feature is ~70% already in the tree - what is missing is exactly the CONSUMER. **This is the same dead-wiring class as L-224** (a listener on the wrong bus) and **L-219** (an inert renderer toggle): a control the user can operate that is not connected to anything. **FINDING 2 - THE ENUM IS FORKED (a real latent bug).** `schemas` says **`'Coarse'|'Medium'|'Fine'`** (capitalised); `core-app-model` and `SetViewOutputCommand` say **`'coarse'|'medium'|'fine'`** (lower-case). **Two competing spellings of the same domain enum across a layer boundary** - a P5/C03 violation (schemas are the single source of type truth). Any round-trip through both will mis-compare. **This must be unified BEFORE the consumer is written, or the consumer will silently never match.** **FINDING 3 - the current symbol (`packages/geometry-door/src/DoorPlanSymbolBuilder.ts`, 437 lines).** It is already doing the RIGHT structural things and already has a **lineweight hierarchy** (`:95-96`: `A-DOOR-CUT` -> leaf rectangle, HEAVY, cut by the section plane; `A-DOOR-PROJ` -> swing arc + open-position line, LIGHT), a 32-segment arc (`:37`), correct double-leaf handling (`:123-125`), and the L-127 fix that resolves frame/leaf thickness from the SELECTED DOOR TYPE so the symbol matches the placed 3D door exactly (`:232-238`). **So dimensional truth is already guaranteed and MUST BE PRESERVED.** What the founder's image 1 (current PRYZM) shows against his image 2 (target) is a DRAUGHTING gap, not a geometry gap: (a) **no wall poche** - the cut wall is a thin outline, so the drawing reads flat and the door does not read as a hole in mass; (b) **the lineweight hierarchy is too weak to see** - cut and projection look nearly identical on screen; (c) **no frame reveal / jamb rebate detail** at Fine; (d) **no door hardware (lever)** at Fine; (e) the arc + open-leaf + closed-leaf lines together read as a confusing extra chord. | Drawing / plan symbols + view detail level (**C09**; **C03/P5**; **Contract-23 §8** pen table; C06; L-127 lineage) | **FIXED (47e74e99)** - awaiting merged-HEAD tsc + push. **THE BIG FINDING IS NOT ABOUT DOORS: THE ENTIRE PEN TABLE WAS DEAD - EVERY ELEMENT TYPE, EVERY PLAN VIEW.** In `PlanViewCanvas.render()`, TWO bugs cancelled the whole Contract-23 §8 line-weight system: **(1)** `const materialLineWeight = Number(baseMaterial.linewidth)` - THREE's `LineBasicMaterial` **defaults `linewidth` to 1**, so every `LineSegments` child *looked like* it carried an authored weight of exactly 1; **(2)** `ctx.lineWidth = Math.max(hairline, vgLineWeight * hairline)` **unconditionally OVERWROTE** the pen table's `_pen.widthMm * SCREEN_PX_PER_MM` - and the VG resolver ALWAYS returns a weight (`BUILT_IN_DEFAULT.lineWeight = 1`), so it ALWAYS fired. **A 0.50 mm wall CUT and a 0.18 mm door PROJECTION both collapsed to `1 x hairline`. Identical. Every line in every plan drawing.** (It also multiplied a *unitless* VG emphasis index by a *pixel* hairline - a units error.) **Fixed:** the pen table supplies the base width in mm; the VG weight is the **relative emphasis factor** it was always meant to be (`penPx * vgWeight / VG_BASE_LINE_WEIGHT`). Door CUT (0.35 mm) now genuinely outweighs door PROJ (0.18 mm); wall CUT outweighs both. **This improves EVERY plan symbol in the product, not just doors - and it is very likely a contributing cause of the founder's other drawing-quality reports (cf. L-221 "TOOOOO MANY LINES").** **POCHE: MY BRIEF WAS REFUTED. Wall poche was ALREADY BUILT AND WIRED** (`ISO_CUT_LAYER_TO_POCHE_FILL['A-WALL']`, `_renderPocheFills`, gated on `ViewScope.poche`). It was **invisible for the same reason the hierarchy was.** Nothing to add. The agent deliberately did NOT give doors a poche entry - filling a leaf/frame solid would be **wrong draughting**. **Finding 1 CONFIRMED - `detailLevel` was a dead knob** (written, persisted, locked, synced, migrated; **zero geometry consumers**). Same class as L-224 / L-219. **Finding 2 CONFIRMED - and it was a THREE-way fork:** `ViewPropertiesPanel.ts:22` carried a *third* Title-Case spelling that **could never have matched what its own dropdown writes**. Unified in `packages/schemas/src/view/detail-level.ts`; canonical = **lower-case** (what the store, the command, the defaults and the live dropdown already spoke - so **nothing changes value at runtime**); `DetailLevelSchema` is a **tolerant reader / canonical writer**, so persisted `'Medium'` still parses and upgrades in place - **no migration script**. Type-imports only into `core-app-model` (erased at build) ⇒ **no `package.json` dep, no lockfile churn** (the frozen-lockfile hazard, deliberately avoided). **P2 resolver built ONCE as shared drawing infrastructure** (`core-app-model/src/drawing/DetailLevelResolver.ts`): precedence = **C09 per-element `GraphicOverride` -> per-elementType -> per-category -> the VIEW's `output.detailLevel` -> `'medium'`**. **Both of the founder's knobs, ONE authority** - the per-element override rides the EXISTING C09 mechanism (`ElementStateAppearance.detailLevel`), so **no new per-door `lod` field** and no L-223-style second authority. Window / stair / plumbing / furniture adopt it with a one-line call - the precedence is not re-implementable per type (so this cannot become a fourth if-ladder). **WHAT EACH LEVEL EMITS:** **coarse (LOD 100)** = 2 jamb ticks + 2 frame face lines + a **single-line leaf**, + the arc. **medium (LOD 200)** = frame + a **4-edge leaf rectangle at the real `leafThickness`**, + arc. **fine (LOD 300)** = frame + leaf + **reveal ticks + rebate/door-stop**, + arc + **threshold** + **lever hardware**. **DELIBERATE DEVIATION FROM MY P4 - MEDIUM IS *NOT* BYTE-IDENTICAL, AND THE AGENT WAS RIGHT TO BREAK MY RULE. FOUNDER SIGN-OFF WANTED.** I said "keep medium exactly as today so nothing regresses by default." But the founder's own words were *"the LEAF of the door in plan view renders OPENED"* - and today it renders **CLOSED** (a leaf rectangle lying in the opening) **PLUS** a radial open-position line **PLUS** the arc: **three readings of one leaf. THAT IS his "confusing extra chord".** Freezing medium would have left the DEFAULT view still showing the exact symbol he complained about. **The leaf now draws ONCE, OPEN, at every LOD**, and the redundant open-position line is gone. **Bonus: the placed symbol now AGREES with the tool preview** (`DoorPlanToolHandler` already drew the leaf open) - a preview/placed divergence that had been live and unreported. **L-127 DIMENSIONAL TRUTH HELD:** every value resolves from `resolveDoorDimensions(systemTypeId, doorType)` - **not one literal in the symbol** (even the lever is derived from `leafThickness`). Tests assert hinge point, leaf width (= arc radius), leaf thickness and the void-edge jamb ticks are **bit-identical across coarse / medium / fine**. **Gate:** 19 new tests green (8 resolver-precedence + 11 door-symbol - `geometry-door` had **no test runner at all**; one was added, no new deps). schemas 1110/1110; core-app-model 290/290; geometry-kernel 600/600. Two pre-existing suite failures reproduced on a pristine HEAD worktree (unrelated mock gap). Zero tsc errors in the agent's files. | **P6 UI NOT SHIPPED (deliberate, per my brief):** the per-element override MECHANISM works (an override beats the view) but there is **no UI yet to author a per-door override**. The **view-level** path is proven end-to-end - **the founder's existing Detail Level dropdown is now LIVE.** A per-element control can hang off the C09 graphic-override surface whenever he wants it. | route -> drawing/plan-symbol agent. **(1) THE ARCHITECTURE IS ALREADY DECIDED BY THE CODEBASE - HONOUR IT.** Detail Level is a **VIEW property** (Revit's Coarse/Medium/Fine), it already exists on `ViewDefinition.output.detailLevel`, and per **P7** it is *visibility INTENT, not UI state*. **Do NOT invent a new per-door `lod` field as the primary mechanism** - that would be a second authority, the exact C09/L-223 two-authority collision. **Resolution order: per-element OVERRIDE (via the existing C09 graphic-override / visibility-intent mechanism) -> VIEW's `detailLevel` -> view-template default.** The founder asked for both knobs; this gives him both with ONE authority and a defined precedence. **(2) FIRST, unify the forked enum** (Finding 2) in `packages/schemas` and make `core-app-model` + `SetViewOutputCommand` import it. This is a prerequisite, not a nice-to-have. **(3) THEN write the CONSUMER** - `DoorPlanSymbolBuilder` resolves the effective detail level for the view being projected and emits: **COARSE (LOD 100)** = heavy opening jamb ticks + a single thin leaf line + a thin swing arc (founder's image 3); **FINE (LOD 200-300)** = frame/jamb with reveal, leaf drawn as a true double-line rectangle at its real `leafThickness`, swing arc, threshold, and lever hardware (founder's image 2). Keep `Medium` as today's symbol so nothing regresses by default. **(4) DIMENSIONAL TRUTH IS NON-NEGOTIABLE** - the L-127 rule stands: every symbol dimension resolves from the selected door TYPE (`DoorDimensions`), never a hard-coded literal. The founder was explicit: *"still absolutely accurate with regards to its element dims."* **(5) THE LINEWEIGHT HIERARCHY IS THE SINGLE BIGGEST QUALITY WIN AND IT IS ALREADY THERE BUT INVISIBLE** (`A-DOOR-CUT` vs `A-DOOR-PROJ`, `:95-96`) - find out why the two layers render at near-identical weight and make CUT genuinely heavy vs PROJ genuinely light. Add wall **poche** for the cut. This alone moves image 1 most of the way to image 2. **(6) GENERALISE, DO NOT SPECIAL-CASE THE DOOR.** Detail Level is a view-wide concept; windows, stairs, plumbing (L-221) and furniture all have plan symbols. Build the RESOLVER once (`resolveEffectiveDetailLevel(elementId, viewId)`) as shared drawing infrastructure, and let the door be its first consumer. Otherwise this becomes the next per-element-type if-ladder (L-215/L-229/L-233). **(7)** Properties-panel UI: the VIEW dropdown already exists (`ViewPropertiesPanelBuilders.ts:258`) and will start working the moment the consumer lands - **verify that end-to-end first, before building any new per-element UI.** **(8)** Tests: a Fine door symbol and a Coarse door symbol of the SAME door produce different line counts but **identical leaf width, frame thickness and hinge position**; changing a view's `detailLevel` re-projects the symbol; a per-element override beats the view value. §FEAT-DOOR-PLAN-SYMBOL-DETAIL-LEVEL. |
| L-242 | founder ("CAN YOU REVIEW THE WALL JOINTS - THEY SHOULD BE SOUND - BUT SOMETIMES THERE ARE ERRORS - SPECIALLY AROUND T AND L JOINTS - DO THIS REALLY CAREFULLY", 2 plan screenshots) | **[HIGH - correctness] L/T joint artifacts persist because the engine that FIXES L/T/X joints BY CONSTRUCTION only runs on PLAIN walls. LAYERED walls and walls WITH OPENINGS - i.e. exactly the walls in the founder's screenshots - still fall back to the LEGACY miter path. ADR-0055 says so in writing.** His screenshots: (img 1) an L-corner with a mitre diagonal plus a small notch/sliver where the two wall faces fail to meet; (img 2) a run of T-joints between LAYERED walls that CONTAIN DOORS, with a spurious diagonal at the right-hand junction. **Both images show the two wall classes that the V2 pipeline does not handle.** **ROOT CAUSE - THE FIX WAS SHIPPED FOR THE WRONG HALF OF THE WALLS (documented, not speculative).** ADR-0055 (Pascal-style wall junctions) built `JunctionResolverV2` + `WallFootprint2D` + `WallPolygonExtruder`, which produce **edge-coincident corners at L/T/X junctions BY CONSTRUCTION - no wedge, no overlap, no infill prism**. It is **DEFAULT-ON** (`WallPipelineV2.ts:44` `isWallPipelineV2Enabled()`; escape hatch `window.__pryzmWallPipelineV2 = false`). **But it is wired into ONE call site only:** `WallFragmentBuilder.createWallBodyFragment` - and ADR-0055 P3b states that site is *"(non-layered, no-openings - the simplest call site)"*. **ADR-0055 P4 - 'Extend V2 to the layered + opening call sites; delete `WallJunctionInfill*`' - is BACKLOGGED.** Verbatim, ADR-0055:118: *"The current ship state (P1+P2+P3a+P3b, default-ON) already closes the wedge for the **dominant** production case (plain partition walls)... **Layered + opening junctions retain `WallJunctionInfill` as the interim mitigation until P4a/P4b ship.**"* And the contract-sync audit repeats it: *"the layered-and-openings path still routes through the legacy `WallJunctionInfill`."* **So there are TWO join engines live in the same scene, and which one a wall gets depends on whether it has layers or a door in it.** That is the defect class, and it is why the founder's report is intermittent (*"SOMETIMES there are errors"*): a plain wall joins cleanly, the layered/doored wall beside it does not. **WHY THE LEGACY PATH SPECIFICALLY FAILS AT T AND L.** ADR-0055's own rationale for making V2 default-ON records that running every wall through the legacy `MiterPrismBuilder` *"made the 3-WALL (T/X) JOINS WORSE: legacy **over-extends at complex junctions into degenerate dark slivers**"* (founder, 2026-06-19: *"black shapes appearing in joins, often 3 wall joins, got worse"*) - `WallPipelineV2.ts:33-42`. The legacy path is a **prism-overlap + `polygonOffset` infill hack** (`WallJunctionInfill*`, `MiterPrismBuilder`), which patches junctions rather than solving them; the residual slivers/notches ARE its known failure mode. **The founder is seeing the legacy engine's documented failure mode, on the walls that never got migrated.** **FOUR JOIN SUBSYSTEMS COEXIST IN `packages/geometry-wall`:** `JunctionResolverV2` + `WallFootprint2D` + `WallPolygonExtruder` + `WallPipelineV2` (the V2 solve); `WallJoinResolver` (baseline trimming, carrying accreted patches: §MULTI-CLUSTER, §WJR-INVALID, §WJR-DIFF-THICKNESS, §PARTITION-SHELL-INNER-FACE); `WallJunctionInfill` + `WallJunctionInfillManager` + `MiterPrismBuilder` (the legacy prism/infill patch); `WallJunctionClustering`. **ADR-0055 P4c exists precisely to DELETE the third group.** **CROSS-LINKS - this is the hub of a cluster of open tickets, not an isolated bug.** **L-238** (the founder's *"corrupted triangular shape created in wall joints of 3 walls in L or T shape"* leaking into a NEW project) is almost certainly the SAME artifact, and the same `§V2-SPIKE-GUARD` / `§WJR-INVALID` degenerate-mesh family (`WallFragmentBuilder.ts:880-900`, `:2963`). **L-239** (layered walls) is the same population of walls. **L-234** (wall-move freeze) lives in `WallJoinResolver.resolveLevel`. **The known multi-cluster degenerate-wall bug** (guard skips the degenerate wall but its MESH still renders) is recorded in the plan at `V1-LAUNCH-IMPLEMENTATION-PLAN.md:2314`. | Wall junctions (**ADR-0055** P4a/P4b/P4c - the migration this ticket demands; ADR-0055A; C11; §WJR-INVALID; §V2-SPIKE-GUARD; L-238 / L-239 / L-234 cluster) | **INVESTIGATED (read-only, numerically reproduced against the REAL resolvers). ROUTING CLAIM CONFIRMED; MECHANISM CLAIM REFUTED - AND THE TRUTH IS FAR WORSE: WE LOBOTOMISED THE LEGACY JOIN ENGINE OURSELVES, NINE DAYS AGO.** **THE HEADLINE - A REGRESSION WE SHIPPED (2026-07-02).** The legacy L/T corner fixes work **exclusively by MOVING BASELINE ENDPOINTS** (they set **no miter normal at all**: reproduced, `MN start=NULL end=NULL`). `§FIX-WALL-JOIN-BASELINE-IMMUTABLE` (**L-44/46/47, shipped 2026-07-02**) now anchors every moved, authored-valid wall back to `_sourceBaseLine` (`WallRebuildCoordinator.ts:1405-1424`, `_baselineImmutable`, **DEFAULT ON**) - **and therefore DISCARDS every one of those corrections.** The legacy join engine has been silently lobotomised. **V2 walls were immune, so nobody noticed.** **REPRODUCED, different-thickness L-corner** (A t=0.30 -> B t=0.10 at (5,0)): the resolver extends A to x=5.050 and trims B to z=0.149; **both moves are discarded**; both walls render square-capped at the authored endpoints. Result: the rectangle **x in [5.000, 5.050] x z in [-0.150, 0.000] is covered by NEITHER wall - an open 50mm x 150mm notch at the outer corner. THAT IS THE FOUNDER'S IMAGE 1.** Simultaneously B's cap is buried 150 mm inside A -> a 76.5 cm2 overlap whose cap edge draws the spurious line across A in plan. **AND THAT IS WHY IT IS *SOMETIMES*:** an **equal-thickness** L uses a **miter normal** (`MN end=set`), moves no baseline, is untouched by the anchor, and renders a **perfect shared 45deg miter**. **The defect is keyed to THICKNESS INEQUALITY** - and layered walls are exactly the walls whose thickness differs from the plain partitions beside them. The legacy resolver's own comment (`WallJoinResolver.ts:2083-2086`) names the very defect it exists to prevent: *"the convex OUTER quadrant of the corner ... is filled by NEITHER wall: an open notch at the building's outer corner."* **We disabled the fix and the notch came back.** **THE T (image 2):** all three walls at a 3-wall cluster get `MN = NULL` and **no trim** - the guest's trim onto the host face is a baseline move, **discarded** - so a thin stem starts on the bar's CENTRELINE and pokes 150 mm into it, drawing its outline across the bar's inner face. The spurious **diagonal**: opening-bearing walls render via the SEGMENTS path, and `MiterPrismBuilder` receives an MN computed against the TRIMMED baseline but applies it at the UN-TRIMMED endpoint -> the miter plane lands at the wrong station; on a short end-segment (door near the junction) that cut slices diagonally across the segment. (Same family as ADR-0055 §MITER-SEGMENT-CLAMP / L-93.) **THREE OF MY OWN ASSUMPTIONS REFUTED:** **(i)** `WallJunctionInfill` is **NOT** the producer of the L-notch - it **never fires at an L** (`WallJunctionInfill.ts:77`: `if (wallIdsInCluster.length < 3) continue;`). **(ii)** The infill is **invisible in plan view** entirely (it is added to the scene root with **no element id**, and `EdgeProjectorService` projects only meshes in the `elementRegistry`) - **both founder screenshots are plan views, so NEITHER shows an infill prism.** Retiring the infill (P4c) is a **3D cleanup**, not the fix for these screenshots. **(iii)** My proposed P4a option (b) *inset-from-envelope* is **also broken** - cross-wall edge-coincidence still requires every wall at the junction to share a layer structure; a 3-layer partition meeting a 5-layer shell has no layer correspondence to be coincident with. **ADR-0055A already analysed this and chose Algorithm C** (ONE V2 envelope prism at full thickness + per-layer `geometry.groups` material bands). It is right, and it is safe: IFC composition is stamped from `wall.layers` (not derived from the mesh), and plan layer lines come from pure math (`WallLayerPlanLines.computeWallLayerLines`), so neither output depends on per-layer solids. **BONUS BUG:** the infill is fed a **filtered population** (`WallRebuildCoordinator.ts:1732-1737` filters to `openings>0 || layers>0` BEFORE clustering), so a junction whose third wall is PLAIN drops below 3 and gets **no infill at all** - mixed junctions are left with an unplugged void. And in 3D the infill is inflated 25 mm past the wall faces (`§JUNCTION-INFLATE`) with `polygonOffset(-1,-1)`, so it **wins the depth test against the walls it protrudes through** - a self-inflicted 3D artifact. **L-238 PRODUCER NAMED:** three different junction-clustering radii coexist - **V2 = 0.20 m** (`JunctionResolverV2.ts:123`), **legacy = 0.12 m**, **infill = 0.5 m** (`WallJunctionInfill.ts:31`). A wall shorter than V2's band gets BOTH endpoints in ONE cluster -> the ring sweep hinges both ends on one pivot -> a **bow-tie, negative-area (inverted-normal) polygon -> renders BLACK from every camera** (ADR-0055:182, signed area -0.03). The legacy 0.12 m band never doubles it, so `§WJR-INVALID` never fires. **Fix the producer** (unify the three bands onto one constant; refuse a sub-band stub AT COMMIT), not the cleanup. **THE LOAD-BEARING CONCLUSION:** *the legacy engine's L and T corrections are implemented as ENDPOINT MOVES; endpoint moves are now FORBIDDEN (correctly - L-44/46/47, and the reverted §CLAMP-COSHARE-WELD which surfaced DOUBLED walls); and **V2 is the ONLY engine in the codebase that produces watertight corners WITHOUT moving an endpoint.*** **Finishing ADR-0055 is therefore not the tidy option - it is the only option that satisfies both invariants at once. P4 is promoted from an enhancement to a CORRECTNESS PREREQUISITE.** **DO NOT 'FIX' THIS BY RE-ENABLING THE BASELINE MOVES** - that is the shortest path and it walks straight back into doubled walls. (Constraint relayed to the live L-234 agent, which owns `WallRebuildCoordinator.ts`.) | Wall junctions (**ADR-0055** P4a/P4b/P4c + **ADR-0055A** Algorithm C - `status: proposed`, MUST BE ACCEPTED FIRST; §FIX-WALL-JOIN-BASELINE-IMMUTABLE L-44/46/47; §WJR-INVALID; L-238 / L-239 / L-234 cluster) | OPEN (**HIGH - now a CORRECTNESS prerequisite, not polish**) - design complete, fix not yet dispatched | route -> wall-junction agent. **(1) THE ANSWER IS ALREADY WRITTEN: SHIP ADR-0055 P4a + P4b.** Do not invent a new approach and do not add a fifth patch to the legacy infill. The architecture already decided that V2 is the correct model and that the infill must be RETIRED (P4c). This ticket is the business case for finishing that migration. **(2) P4a (layered walls, no openings)** - ADR-0055:110 already names the hard part: `WallMiter` stores absolute world-XZ corners computed against the wall's HALF-THICKNESS, so a layered wall cannot reuse one wall-level miter (each layer has a different lateral offset + thickness). ADR offers **(a)** run `resolveJunctions` once per layer offset, or **(b)** derive per-layer corners by INSET from a single wall-level envelope. **(b) is the architecturally sound one** - one junction solve remains the single source of truth and layers are derived from it; (a) creates N independent solves that can disagree at a corner. **This requires an ADR-0055 ADDENDUM - write it, do not decide it in code.** **(3) P4b (walls with openings)** - ADR-0055:111: the END segments abutting a junction need a polygon footprint that also respects the opening's left/right edges (a polygon-vs-rectangle carve). **Note this interacts with the L-234 fix** (which is about how OFTEN we rebuild these bodies) - coordinate, do not collide. **(4) P4c** - delete `WallJunctionInfill*` + the `polygonOffset` hack, gated on P4a+P4b verification. **The `polygonOffset` patch is a rendering band-aid over a geometry defect and must not survive V1.** **(5) DO NOT TOUCH `§CLAMP-COSHARE-WELD`** - that corner fix was REVERTED because moving shared baseline endpoints surfaced DOUBLED walls; any new corner work must not move endpoints. **(6) Tests: a golden-plan junction matrix** - {L, T, X} x {plain, layered, opening-bearing, layered+opening} x {same thickness, different thickness} - asserting NO sliver, NO notch, NO negative-area polygon, and edge-coincident faces. That matrix is the deliverable; it is what makes 'sometimes there are errors' measurable. **(7)** Fold **L-238**'s orphan/triangular junction mesh into this - fix the PRODUCER, not just the cleanup. §FIX-WALL-JOINTS-ADR0055-P4. |
| L-243 | founder ("stair in plan view can not yet be created") - **RE-REPORT of L-217, which I marked FIXED (f52f71f0). The routing fix was real but it was NOT the blocker.** | **[HIGH - blocker] A stair CANNOT be created in a fresh project AT ALL, in plan or in 3D, because the stair handler HARD-REQUIRES a level ABOVE the base level - and a new PRYZM project boots with exactly ONE level (Ground).** **ROOT CAUSE #1 - THE MISSING-UPPER-LEVEL HARD ABORT (file:line).** `apps/editor/src/engine/views/plantools/StairPlanToolHandler.ts:93-104`: `const topLevelId = this._resolveTopLevel(baseLevelId, cm); if (!topLevelId) { console.error(...); emit('pryzm:toast', { message: 'Add a second level before placing a stair - go to Levels and create the floor above.', severity: 'error' }); … return; }` - **the handler discards the in-progress stair and returns.** `_resolveTopLevel` (`:236-259`) looks for the ADJACENT level ABOVE `baseLevelId` and returns `null` if there is none. **A fresh project has one level.** The boot log confirms it (`[ProjectLoader] Loading 0 levels`; `[BimManager] Cannot delete the default Ground level`; `DefaultViewsManager` seeds exactly one *Ground Floor* plan view). **So on a new project the stair tool is DEAD ON ARRIVAL and the only feedback is a toast.** This is almost certainly what the founder means by *"can not yet be created"* - it is not a wiring bug, it is a **domain precondition the product never satisfies by default.** **WHY L-217's FIX DID NOT CLOSE IT (recorded honestly).** L-217 (`f52f71f0`, §FIX-STAIR-PLAN-ROUTING-VIEWSTATE) fixed a REAL bug - `BimService.activateStairPathTool` was using a SNAP-availability predicate (`planView2DCreationMode.isInPlanView`) as a plan-vs-3D discriminator, so an orthographic plan camera could mis-route to `StairPath3DToolHandler`. That fix stands. **But it fixed WHICH HANDLER RUNS, not WHETHER THE HANDLER CAN SUCCEED.** Once correctly routed, the plan handler still hits the missing-upper-level abort. **I closed L-217 on the routing evidence without ever driving the tool to a created stair.** That is the process lesson: *a fix verified at the seam is not a fix verified at the outcome.* **ROOT CAUSE #2 - THE PLAN TOOL READS STATE THE 3D TOOL WRITES (the C11 disease AGAIN).** `StairPlanToolHandler.ts:133-145`: `const config = window.activeStairConfig; const shape = (config?.shape) ?? 'I'; const requestedWidth = config?.width ?? …; const typeId = config?.typeId;` - and the comment says it plainly: *"read the architect's chosen shape from the global **stamped by BimService.createStair's onConfirm**. Falls back to 'I' (straight) if no setup-panel ever ran."* **The PLAN path has no config of its own; it scavenges a global left behind by the 3D path's setup panel.** So: the user's chosen SHAPE (I/L/U), WIDTH and stair TYPE silently do not reach a plan-created stair unless they first went through the 3D flow. **This is EXACTLY L-239 (wall layers), L-213 (floor finishes), L-240 (floor inner face): ONE element type, TWO creation paths, and the plan path silently drops what the 3D path resolved.** The file even carries its own TODO admitting it: `TODO(STAIR-PLAN-DI): replace with a PlanToolDrawContext.stairConfig slot … so this handler matches the WallPlanToolHandler / SlabPlanToolHandler DI shape and complies with PRYZM-3 P4 (no window reads)`. It is also a **live P4 violation** (`window.*` global read). **ROOT CAUSE #3 (design question, must be ANSWERED not patched) - SHOULD A STAIR REQUIRE A PRE-EXISTING UPPER LEVEL AT ALL?** The current model derives `riserHeight` from `levelHeight` so `riserHeight x riserCount == levelHeight` exactly (`:122-131`) and `CreateStairCommand.canExecute` validates it against a HEIGHT_TOLERANCE. That is a sound INVARIANT, but it makes the upper level a **hard input**. In real BIM a stair is authored by HEIGHT and commonly CREATES/implies the level above. **Forcing the user to hand-create the floor above before they may draw a stair is a workflow trap, and it is the reason the tool appears broken.** | Element creation / stairs (C11; C16; P4; **ADR-0098 NEW**; L-217 lineage; unblocks L-216) | **FIXED (87ff6513)** - awaiting push. **MY ROOT CAUSE #1 WAS REFUTED - I NAMED THE WRONG HANDLER AND THE WRONG ABORT. THIRD REFUTATION TODAY.** The agent drove the real tool end-to-end in a fresh single-level project and probed it: `PROBE ONE-LEVEL executeCalls=0 warns=["[StairPathToolController] Cannot finish: invalid - Riser too small (0 mm - min 100 mm)"]`. **The stair does NOT die at the `topLevelId` abort I cited.** **THE HANDLER I WROTE THE ENTIRE BRIEF ABOUT IS NOT THE ONE THE FOUNDER HITS.** The plan-view stair the ribbon actually reaches is **`StairPathPlanToolHandler`** (tool key **`stair-path`**), not `StairPlanToolHandler` (key `stair`). Chain: `CreateRailPanel -> runtime.tools.activate('stair') -> BimService.activateStairPathTool -> ToolManager.activateStairPath -> activeTool='stair-path'`. `StairPlanToolHandler`'s hard abort is real code - but it is only reachable via RadialMenu -> `BimService.createStair` -> `StairSetupPanel`. **I read a plausible file and never checked which file the button actually reaches.** **THE REAL DEATH - three steps deep and COMPLETELY SILENT:** **(1)** `StairPathPlanToolHandler._resolveAdjacentLevel()` **LIED**: when the base level is topmost it returned `top = base` - **a ZERO vertical span** (and when the base level wasn't found it FABRICATED `${baseLevelId}:top`, an id in no store). **(2)** Zero span -> `StairSolver2D` got `totalHeight = 0` -> `riserHeight = 0` -> `isValid = false`. **(3)** `StairPathToolController._finish()` logged a `console.warn` and **returned**. **No stair, no toast, no error.** That is the founder's *"can not yet be created"* - a silent `return` behind a `console.warn`. **A THIRD CORRECTION TO MY BRIEF:** a `StairLevelRequiredPanel` + `_ensureTwoLevelsForStair` guard **already existed** (`BimService:350`) - I claimed the only feedback was a toast. **But it asks the WRONG QUESTION** (`levels.length >= 2`): simultaneously **too strict** (a fresh project cannot activate the tool at all) **and too weak** (a 2-level project with the **TOP** level's plan open passes the gate, then dies silently). **Counting levels was never the invariant.** **ROOT CAUSE #2 CONFIRMED EXACTLY** (`window.activeStairConfig`) - and it is now **GONE**: deleted from `globals.d.ts`, `BimService` and both handlers, replaced by a `StairToolConfigStore` injected via the new `PlanToolDrawContext.stairConfig`, matching the Wall/Slab DI shape. **P4 violation cleared; `TODO(STAIR-PLAN-DI)` discharged. L-216 (15 stair types) is UNBLOCKED** - `typeId` is threaded end-to-end, so a type picker now only needs `setStairToolConfig({ typeId })` and plan / 3D / path / batch / AI all inherit it. **ADR-0098 WRITTEN** (`docs/02-decisions/adrs/ADR-0098-stair-authored-by-height-implied-level-above.md`): a stair is **authored by HEIGHT**; when no level exists above, it **IMPLIES one** via `AddLevelCommand`. The agent's refinement of my option list is right: **(a) and (b) are not alternatives - (b) is HOW (a) is delivered without weakening the invariant.** It rejected the literal reading of (a) (making `topLevelId` optional on `CreateStairInput`) because that id is load-bearing for `StairLevelCleanupHandler`, `LevelTraversalPolicy`, IFC export and the schedule extractor. **`riserHeight x riserCount === height` is preserved exactly** by `deriveRisers()`. **And it proved my 'do not delete the guard' instruction was based on a false premise: the level-count guard NEVER protected that invariant** (`deriveRisers` + `canExecute` do) - which is why removing it weakens nothing. **Gate:** `geometry-stair` 88/88 (18 new); plan-creation specs 9/9 - including **the fresh-single-level-project guard L-217 lacked**, plan-vs-3D config equality, and the height invariant; **root tsc exit 0**. | **FOUNDER SIGN-OFF WANTED (2 accepted trade-offs, both flagged in ADR-0098):** **(1)** implying a level = **two commands = two undos**. **(2)** drawing a stair on the TOP storey now **ADDS a storey** (announced by a toast). Both deliberate - say the word if you want them different. | route -> element-creation/stair agent. **(0) REPRODUCE FIRST, IN A FRESH SINGLE-LEVEL PROJECT.** Do not trust my root cause - drive the plan stair tool end-to-end and report exactly where it dies. If it dies somewhere other than the `topLevelId` abort, say so; my L-217 call was already wrong once on this very ticket. **(1) ANSWER ROOT CAUSE #3 BEFORE WRITING CODE** - this is an architecture decision, not a bug fix, and it belongs in an ADR. Options: **(a)** stair is authored by an explicit HEIGHT (default = level height when an upper level exists, otherwise a sane default), and the upper level becomes OPTIONAL; **(b)** drawing a stair with no level above OFFERS to create it (a command, not a toast); **(c)** keep the hard requirement but make it a first-class, discoverable precondition rather than an error toast AFTER the user has drawn. **(a) is the architecturally strongest** - it makes the stair's geometry self-determining and preserves the `riserHeight x riserCount == height` invariant, which is the thing that actually matters. **Do NOT simply delete the guard** - the invariant it protects is real. **(2) Fix root cause #2 the L-239 way: converge the config at the CHOKEPOINT.** Stop reading `window.activeStairConfig`. The stair's shape / width / typeId must reach `stair.create` identically from the plan tool, the 3D tool, batch generators and AI - resolved ONCE below the tools, not scavenged from a global one tool happens to set. Thread `PlanToolDrawContext.stairConfig` per the file's own TODO, and match the `WallPlanToolHandler` / `SlabPlanToolHandler` DI shape. **This also clears a P4 violation.** **(3) Tests: (i) a stair can be created in a FRESH single-level project** (the founder's actual scenario - this is the regression guard that L-217 lacked); **(ii)** a plan-created stair and a 3D-created stair with the SAME chosen shape/width/type produce **identical stored records** (the L-213 equality pattern); **(iii)** the `riserHeight x riserCount == height` invariant holds on every path. **(4)** Coordinate with **L-216** (15 stair types) - same subsystem, and the `typeId` plumbing in root cause #2 is the same plumbing L-216 needs. §FIX-STAIR-PLAN-CREATION-BLOCKED. |
| L-244 | orchestrator (surfaced by the L-239 agent; **declared, not hidden**) | **[MEDIUM - architecture] The `wall.create` chokepoint is not yet the SOLE writer: a LEGACY `CreateWallCommand` still writes the legacy `WallStore` - the store BOTH renderers read - WITHOUT passing through the bus command.** Found while landing L-239. `packages/command-registry/src/walls/CreateWallCommand.ts` writes the legacy `WallStore` directly. It is invoked by **(a)** `WallTool`'s **dual-write** (`WallTool.ts:1788` - it dispatches `wall.create` AND then also runs the legacy command), **(b)** `CreateWallsFromSlab`, **(c)** `ProjectLoader`. **This dual-write is the very thing that MASKED L-239 for months:** the 3D wall only looked correct because the legacy command stamped `layers` behind the bus handler's back, while the bus handler silently dropped the field. **A defect hidden by a redundant second write is worse than a visible defect** - it makes the bus handler untestable through the UI, and the next dropped field will hide exactly the same way. **CONSEQUENCE ALREADY PAID:** because the chokepoint is not the sole writer, the L-239 agent **could not delete the plan tool's local thickness resolution** (the L-41 workaround). It now stands as a redundant-but-idempotent fallback - correct, but a smell that only clears when the dual-write does. **This is a P6 concern** (commands are the only mutation path) **and a C11 concern** (one element, one creation pipeline): today wall creation has a canonical path AND a shadow path, and the shadow path is the one the renderers actually trust. | Element creation / walls (**P6** commands-only mutation; **C11** one creation pipeline; C16; L-239 lineage) | OPEN (queued, MEDIUM) | route -> element-creation/wall agent - **but SEQUENCE IT AFTER L-234 and L-242**, which both own `packages/geometry-wall` files (L-236 rule (c): never two agents on one file). **(1) Collapse the dual-write.** `WallTool` must dispatch `wall.create` and STOP - no legacy fall-through. `CreateWallsFromSlab` and `ProjectLoader` route through the chokepoint (or, for the loader, through an explicit restore path that is ALSO the chokepoint - note L-239 already fixed the loader's layer-drop, so be careful not to regress it). **(2) Then DELETE the plan tool's local thickness resolution** (the L-41 workaround) - it only exists because the chokepoint was not authoritative. Its deletion is the PROOF the chokepoint is now sole writer. **(3) Add a CI guard** in the spirit of `check:commandmanager`: no production site may write `WallStore` outside the `wall.*` handlers. A rule that is merely intended is a rule that rots; make it merge-blocking. **(4)** Tests: a wall created through EVERY path (plan, 3D, batch, betweenMarks, fromSlab, load, paste) has identical stored shape; and removing the legacy dual-write does NOT change any rendered output (that is the regression risk). §FIX-WALL-CREATE-SOLE-WRITER. |
| L-245 | orchestrator (surfaced by the L-240 agent; **declared, not hidden**) | **[MEDIUM - architecture, + a small visible cosmetic] `floor` has TWO command implementations, and the canonical bus one is the road less travelled: 10 of 11 creation paths go through the LEGACY `CreateFloorCommand`; only the plan tool reaches the bus `floor.create` handler.** Found while landing L-240 - and it is the reason my own brief pointed the fix at the wrong chokepoint. **This is the SAME defect shape as L-244 (walls): a canonical command path and a legacy shadow path, where the shadow path is the one production actually uses.** Recorded by the agent as C11 §5.4.3 debt. **Consequence already paid:** anyone reasoning about "the floor creation pipeline" from the bus handler is reading a path that 10 of 11 callers never take. **ALSO (small, visible):** the 3D AUTO creation MODAL still previews the **centreline** area (e.g. **12.00 m2** instead of the correct **10.64 m2**) because `FloorTool` cannot compute the inset without the dependency that L-240 correctly refused to add. **The STORED floor is correct; only the pre-commit estimate in the dialog is optimistic.** The founder may well notice the number. | Element creation / floors (**C11 §5.4.3**; **P6** commands-only mutation; L-240 + L-244 lineage) | OPEN (queued, MEDIUM) | route -> element-creation/floor agent. **(1) Converge the two floor commands** - one canonical implementation. Prefer migrating the bus handler's callers onto the legacy command's (now correct) derivation, or promote the legacy command to BE the handler; do NOT leave two. **Sequence WITH L-244** (walls) - it is the identical problem and should get the identical answer, and probably ONE CI guard covering both: *no production site may write an element store outside its canonical command.* **(2) The modal area** - the dialog must ask the COMMAND LAYER for the derived area rather than computing its own. That is the architecturally correct fix and it also removes the last place a tool computes floor geometry. **(3)** Tests: the previewed area equals the committed area, on every path. §FIX-FLOOR-COMMAND-SOLE-WRITER. |
| L-246 | founder (door review round 2, with the SAME LOD-200/300 reference image) | **[HIGH - drawing correctness] The wall does not STOP at the door. In plan, a placed door must CUT the wall - the wall's face lines must terminate on the opening's void edges and the jambs must close the void. Today the wall lines run straight THROUGH the opening.** Founder: *"The door - when placed - should DISCONTINUE THE WALL in this precise section. Check the second photo - this is the quality of the door I need."* His reference (unchanged from L-241) shows the wall poche stopping dead at each jamb with a rebate/reveal notch, the void open, and the leaf + arc reading against a genuinely interrupted wall. **The door SYMBOL itself is now largely right (L-241, 47e74e99, shipped). What is wrong is the WALL.** **THE MACHINERY TO DO THIS ALREADY EXISTS AND IS NOT FIRING** - so this is a *why doesn't it run* hunt, not a *build it* task. `apps/editor/src/engine/views/EdgeProjectorService.ts:254-275`, **`_suppressPlanViewOpeningLines`** - header verbatim: *"Plan-view door/window opening line suppressor - DOC-PLAN-OPENING-CLIP ... produces along-wall edge lines that cross through door and window openings. This function clips those lines at the opening boundaries so that only the portions outside the openings are drawn - producing the correct AEC plan-view representation **where wall lines stop cleanly at door/window jambs**."* It is called at **`:2114`**, gated on **`group.userData.openings`**, which is populated at `WallFragmentBuilder.ts:833` (`wallGroup.userData.openings = wall.openings ?? []`). It even carries the exact invariant the founder is asking for, added by **§FIX-PLAN-DOOR-JAMB-SEAM (2026-07-02)** at `:344-353`: **`INVARIANT: wall face line terminus === opening void edge === frame jamb tick.`** And the layered-wall path has its OWN clip (`WallLayerPlanLines.ts:54-94`, *"clipped at any opening whose void crosses the cut plane"*, C15). **So there are at least TWO independent opening-clip implementations, and the founder still sees an uncut wall.** **HYPOTHESES (ALL UNPROVEN - the agent must find the truth, not confirm mine; I have been refuted on the last four tickets in a row):** **(H1)** the cut-plane test rejects the opening - the clip only runs for openings whose world-Y range **straddles the plan cut plane** (`:307-323`, 5 cm tolerance). A door whose void does not straddle the current cut height is **correctly** skipped by this code and **incorrectly** left uncut on screen. **Check the cut-plane height against the door's actual sill/head.** **(H2)** the wall group reaching the projector has an **empty or absent `userData.openings`** - e.g. an INSTANCED wall (instanced meshes carry `levelId` but NOT per-element data - see the known instanced-aggregate gap), a V2-pipeline wall, or a wall rebuilt on a path that does not re-stamp `userData`. **The clip is a no-op if that array is empty (`:288` early-returns).** **(H3)** the wall in the founder's screenshot is **LAYERED**, so its faces come from a different builder than the one `_suppressPlanViewOpeningLines` clips, and only the *layer* lines get clipped while the outer faces do not (**exactly the 'two render paths' split that L-242 proved for joins: layered/doored walls take the LEGACY path**). **(H4)** the poche FILL is not clipped at the opening even when the LINES are - so the void reads as filled wall. **STRONG PRIOR: this is the SAME DISEASE AS L-242.** L-242 proved that **layered walls and opening-bearing walls take a DIFFERENT render path from plain walls** (V2 only ever shipped for `!layers && !curve && openings.length === 0`). **A wall with a door in it is BY DEFINITION opening-bearing, so it is on the legacy path** - and the founder's wall is the one class the modern pipeline never sees. **H3 is therefore the leading candidate, and this ticket may well be CLOSED BY ADR-0055 P4b.** | Drawing / plan projection + wall-opening cut (**C15** hosted openings; DOC-PLAN-OPENING-CLIP; §FIX-PLAN-DOOR-JAMB-SEAM; Contract-23 §8 pen table (L-241); **L-242 / ADR-0055 P4b lineage**) | **FIXED (2fe5cf8b)** - awaiting push. **ALL FOUR OF MY HYPOTHESES (H1-H4) ARE REFUTED - AND SO IS THE PREMISE UNDERNEATH THEM.** I wrote the brief as a *"why doesn't the clip fire"* hunt, on the strength of the machinery already existing (`_suppressPlanViewOpeningLines`, the layered-wall clip, the §FIX-PLAN-DOOR-JAMB-SEAM invariant). **Wrong question. The clip was never the broken thing.** Something UPSTREAM of it never existed at all. **THE REAL ROOT CAUSE - `A-WALL:cut` WAS *ALWAYS* EMPTY, ON EVERY WALL, IN EVERY PLAN VIEW.** The plan `:cut` layer was populated ONLY by `classifyByVertexY()`, which tags an EDGE as cut when an endpoint lies within `CUT_LINE_EPSILON` (**15 cm**) of the cut plane. **A wall solid has NO edge anywhere near a 1.2 m cut plane** - its horizontal edges sit at the base and the head, its vertical edges have endpoints only at those two elevations. **So no wall ever contributed a single vertex to `:cut`.** This is no longer a deduction, it is an assertion in the suite: `planCutSection.doorVoid.test.ts` locks `classifyByVertexY(...).cutGeo === null` for a plain wall box, so the regression cannot creep back silently. **THIS ONE FACT EXPLAINS TWO SEPARATE FOUNDER REPORTS:** (i) **no wall poche** - `_renderPocheFills` scans `:cut` for a fill to stitch and there was never anything there (*"the drawing reads flat"*, **L-241**); (ii) **no CUT lineweight hierarchy for walls** - every wall line was `:proj`. **THE FIX - CUT THE SOLID, DON'T CLIP THE LINES.** `buildPlanCutSectionGeometry()` intersects the mesh's triangles with the plane `y = cutPlaneY` and returns the TRUE section. For a wall carrying a door that is the outline of the solid **that actually exists at the cut height**: the pier left of the jamb and the pier right of the jamb, each closed on itself at the void edge. **The opening is voided BY CONSTRUCTION** - no line and no fill can cross it on ANY wall render path (plain, layered, opening-bearing, curved, CSG single-volume or V2), because at that height **the solid simply is not there**. The invariant §FIX-PLAN-DOOR-JAMB-SEAM *asserted about* the geometry (`wall face line terminus === opening void edge === frame jamb tick`) is now **OBTAINED FROM** the geometry - it cannot be violated by a render path that forgets to call the clip, **which is precisely the class of bug L-242 proved we have.** **This is also why H2/H3 no longer matter:** the new path never reads `userData.openings`, so an instanced wall, a V2 wall, or a wall whose userData was never re-stamped is cut correctly regardless. Reuses the existing `perElemLayerCutGeos` -> `:cut` -> poche plumbing already proven by section views; plan and section are mutually exclusive branches, so no wall is cut twice. Scope-guarded to A-WALL in plan (an AABB straddle-reject keeps slabs out, or the whole floor plate would paint as poche - tested). **WHAT IS PROVEN, AND WHAT IS NOT - THE L-243 LESSON APPLIED TO MY OWN WORK.** **PROVEN:** the `:cut` layer exists for walls for the first time; the door void is empty in it; the piers terminate exactly on the jambs; the lintel above the head does not re-fill the void. **NOT PROVEN:** that the **PROJECTION** lines are clipped on the founder's actual wall - that is still `_suppressPlanViewOpeningLines`, and **H2/H3 remain untested**. If the wall still reads uncut on screen after this push, **THAT is the surviving bug, and it is now isolated to one function instead of the whole pipeline.** **Gate:** 6 new plan-cut specs + 3 sibling classifier specs 9/9; geometry-kernel poche 18/18 + edge-projection 34/34 with **snapshots UNCHANGED**; **full editor suite shows NO regression** - 34 failures / 13 files, identical to a clean-HEAD baseline run I took specifically to check (pre-existing debt, all unrelated to edge projection); **root tsc exit 0**. **FOUNDER: this one needs your eyes** - the poche/cut half is proven in tests, the projection-line half can only be confirmed on screen. | route -> drawing/plan-symbol agent (the L-241 agent - it owns this code and just fixed the pen table). **(1) REPRODUCE AND INSTRUMENT FIRST. Do not trust my hypotheses.** Build the founder's exact case (a door in a wall, plan view) and log whether `_suppressPlanViewOpeningLines` runs, what `group.userData.openings` contains, and whether the cut-plane test passes. **Name the real reason before changing a line.** **(2) The invariant is already written down** (`EdgeProjectorService.ts:352`) - *wall face line terminus === opening void edge === frame jamb tick*. **Make it TRUE, and make it true for LAYERED and OPENING-BEARING walls too**, which is where L-242 proves the pipeline forks. **(3) Unify, do not add a third clip.** There are already two implementations (`_suppressPlanViewOpeningLines` + `WallLayerPlanLines`). **A third would be the fourth if-ladder.** One opening-clip rule, applied to every wall render path. **(4) The poche must be clipped too** (L-241 proved poche is built and was merely invisible; now that it is VISIBLE, an unclipped fill will read as a solid wall across the door - check this explicitly). **(5) Coordinate with L-242** - if the fix belongs in ADR-0055 P4b (the opening-bearing junction/segment carve), SAY SO and fold it in rather than patching the legacy projector. **(6) Tests: a door in a plain wall, a door in a LAYERED wall, and a door in a DIFFERENT-THICKNESS wall all produce a plan in which the wall face lines TERMINATE on the void edges, the jambs close the void, and no line or fill crosses the opening.** §FIX-PLAN-DOOR-CUTS-WALL. |
| L-247 | agent (measured while gating L-246; **founder: "still check - and fix those"**) | **[CRITICAL - governance] THE UNIT-TEST ESTATE IS NOT A CI GATE. `apps/editor`'s 1,495 tests - and the suites of 121 other workspaces - NEVER RUN IN CI. 34 of them are RED on `main` and nothing has been reporting it.** Found while proving L-246 caused no regressions: a clean-HEAD baseline run of `@pryzm/editor` returns **34 failed / 1,454 passed across 13 files**, all pre-existing. **The failures are the symptom. The root cause is that nothing was ever going to catch them.** **THE MECHANISM, IN THREE STEPS (each verified against source, not inferred):** **(1)** `.github/workflows/ci.yml` runs exactly ONE test job - `- run: pnpm run test:server` (`:106`) - plus the Playwright a11y job (`:234`). **It NEVER calls `test:ci`, and it never calls `test:pryzm1`.** **(2)** Root `package.json:44` defines `"test:ci": "pnpm -r --workspace-concurrency=1 --if-present run test:ci"` - so even if CI called it, **`--if-present` SILENTLY SKIPS any workspace that does not define a `test:ci` script.** **(3)** A single-pass scan of every `packages/*`, `apps/*`, `plugins/*` manifest: **only 12 workspaces define `test:ci`. 122 define `test` but NOT `test:ci`** - and are therefore invisible to the aggregator even when it runs. **`@pryzm/editor` is one of the 122** (`apps/editor/package.json:25` - scripts are exactly `test`, `test:watch`, `typecheck`; **there is no `test:ci`**). **So the editor's entire 1,495-test suite has zero CI enforcement, and has had none for as long as the script has been shaped this way.** **WHY THIS IS A GOVERNANCE DEFECT, NOT A CHORE.** `CLAUDE.md` and **C01** state that the 8 principles are *"CI-enforced and merge-blocking"*. That claim is TRUE for lint / `eslint-plugin-boundaries` / `check:isolation` / `check:commandmanager` / `tools/ga-gate` - and **FALSE for every unit test in `packages/` and `apps/`**. The audit's own closing section already half-saw this and filed it as a *"non-blocking engineering follow-up: align root vitest config so `packages/**/*.test.ts` regression suites run in CI"*. **It is not non-blocking. It is the reason L-248 (below) went unnoticed, and it is the reason any fix in this audit can silently regress the moment the next agent touches the file.** A green board that nobody runs is worse than a red one, because it is quoted as evidence. **THE 34, GROUPED BY ROOT CAUSE (measured, not guessed):** **(A) DOM ENVIRONMENT NEVER CONFIGURED - 6 files, ~20 tests.** `window is not defined` (`bootstrap-shape`, `bootstrap.data`, `bootstrap.everything`, `hello-12-elements`), `HTMLElement is not defined` (`residentialBuildingController`), `document is not defined` (`featureFlags.plan-view`, 16 tests). These are browser-surface tests running in vitest's default **node** environment: they lack the `// @vitest-environment happy-dom` pragma that the passing siblings carry (`classifyByVertexY.viewRange.test.ts:1`, and the L-246 spec I just added). **They did not rot - they never ran green.** Someone added them, they died on import, and no gate objected. **(B) A LIVE-NETWORK DEPENDENCY IN A UNIT TEST - `ensureSiteClimate`.** *"uses LIVE Open-Meteo data when the injected fetch succeeds"* - expected `'noaa-normals'`, got `'fallback-defaults'`. A unit test that reaches the public internet is non-deterministic **by construction** and will fail in any offline/sandboxed CI runner. The injected-fetch seam is not being honoured. **(C) A P6 VIOLATION THAT ITS OWN GUARD IS REPORTING - `ElevMarkCropDiscoverable`.** The test is literally titled *"dragging a crop handle dispatches `view.setCrop` (P6), not a direct store write"* and it fails `expected 0 to be greater than 0` - i.e. **the command was NOT dispatched.** If that reproduces at runtime it is a **live P6 breach** (C03/C06: commands are the only mutation path), not a test bug. **MUST be triaged as product-first.** **(D) DOM/RENDER ASSERTION DRIFT - `FormaSiteAnalysisControls` (incl. a test named "REPAINTS the rose ... (the stale-rose bug)"), `layoutModalHtml`.** Could be stale expectations or real UI regressions - unknown until triaged. **(E) TEST-AUTHORING BUG - `CesiumViewportClickNoNav`:** *"error when mocking a module ... no top level variables inside `vi.mock` factory"* (hoisting). **(F) THE LEVEL-EXPLODE EXPLOSION -> SPLIT OUT AS L-248 -> now FIXED (7192e4cb).** **UPDATED COUNT: the red surface is now 3 failed / ~1,500 passed** (was 34 / 1,454 across 13 files). **CLOSED SO FAR:** group **C** (the "P6 breach" - product was RIGHT, L-222 collapsed the two commands into one; guard rewritten to assert the invariant, not the old command name); group **E** (`vi.mock` hoisting -> `vi.hoisted`); group **A** in part (`featureFlags.plan-view` 16, `residentialBuildingController` 6, `bootstrap-shape` 3 - the happy-dom pragma, applied ONLY where the file genuinely needs a DOM); group **B** -> **this became L-249, a LIVE PRODUCTION BUG** (live climate normals could never load); and group **D** in part (`layoutModalHtml` - the product was right: it asserted the exact attribute string `class="alm-thumb"`, which stopped matching when DEMO-2 added the Plan/Graph toggle classes; now pins the CLASS, not the attribute string). **STILL RED - 3 assertions in `FormaSiteAnalysisControls`, and they are PRODUCT-SUSPECT, NOT test rot. DO NOT "fix" them by editing assertions.** Triaged as far as context allowed, and the finding is precise: at mount the wind rose **DOES draw speed-band bars** (`FormaSiteAnalysisControls.ts:1243`, `stroke-width 6` - the detector is valid), and the panel logs *"proactive ensureSiteClimate -> dataset present"* - **yet `climateStore.resolveSite(siteStore.getSite()!.id)` returns NULL.** **Bars are being rendered from a dataset that `resolveSite()` cannot find under the site's own id.** That is a **KEYING MISMATCH** - the same class of defect as **L-249** and the §A.21.D33(f) keying gap - and if it reproduces at runtime it means the climate card can paint from a dataset that no other consumer (3D wind/heat overlay, degree-days, sun-hours) can resolve. **Needs its own ticket and a proper investigation; it is NOT a stale assertion.** (The other 3 red files - `bootstrap.data`, `bootstrap.everything`, `hello-12-elements` - still fail at COLLECT, blocked on the `geometry-slab` -> `@thatopen/ui` layering violation described below; they have ZERO failing assertions.) **L-248 IS THE ARGUMENT FOR THIS TICKET, IN ONE EXAMPLE:** it was a real latent product defect (a two-clock dt that inverts an animation lerp), it sat in `main` undetected, its own regression guard had been RED for an unknown length of time, and **the only reason anyone found it is that I happened to run the suite by hand while gating an unrelated fix.** That is not a process. **G0 is what turns that accident into a gate.** | Governance / CI gates (**C01** 8-layer + CI gates; **C10** §CI gate inventory; **C31** doc-authoring); `.github/workflows/ci.yml:106`; root `package.json:44`; `apps/editor/package.json:25` | **IN FLIGHT - phase 0.5b SHIPPED (d98f75e1); 0.5c-0.5e OPEN (CRITICAL).** **0.5b:** `apps/editor` now defines `test:ci`, and a new **`test-unit`** job in `ci.yml` runs its 1,495 tests - **the first time any unit test in `apps/` or `packages/` has executed in CI.** Landed **`continue-on-error: true`** (mirroring the `ga-gate` precedent) and **deliberately NOT added to the required-checks list** - it joins that list at **0.5d**, once the 22 are triaged. No dependency change, so the lockfile and `--frozen-lockfile` are untouched. **A SECOND HOLE, FOUND WHILE LANDING THE FIRST - AND IT PARTLY DEFEATS THE GATE.** `ci.yml`'s triggers are **`pull_request` -> `main` + `workflow_dispatch` ONLY** (§A.10.i, 2026-06-05, to conserve Actions minutes): **CI does NOT run on a direct push to `main`.** But **the founder's actual loop is push-straight-to-`main`** (that is how every fix in this audit has shipped, including L-246 and L-248 today). **So a gate that only fires on PRs will almost never fire.** G0 is therefore NOT closed by 0.5b - the suite now *can* run in CI, but the workflow the team actually uses still bypasses it. **FOUNDER DECISION REQUIRED (0.5f, and it costs Actions minutes either way):** **(a)** run `test-unit` on push to `main` too (add `push: branches: [main]` scoped by `paths-ignore`, so doc-only pushes stay free) - the honest fix, and it prices in as roughly one editor-suite run per push; **(b)** move to a PR-based flow for code changes (docs can still go direct); or **(c)** accept that G0 guards PRs only and keep pushing to `main` unguarded - **in which case the gate is decorative and this ticket should say so out loud rather than pretend otherwise.** **My recommendation is (a)**, because it is the only option that makes the gate true without changing how the founder works. | route -> **platform/CI-governance agent** (owns `ci.yml`, `ga-gate`, root scripts). **SEQUENCE IS LOAD-BEARING - do not reorder:** **(1) MAKE THE SUITE A GATE FIRST, RED.** Add `test:ci` to `apps/editor` (and the other 121), and add a `test-unit` job to `ci.yml` that actually calls `pnpm run test:ci`. Land it **allowed-to-fail / non-blocking for exactly one PR** so the true red surface is visible in CI output rather than on one agent's laptop. **(2) TRIAGE PRODUCT-FIRST, NOT TEST-FIRST.** For each of A-F ask *"is the TEST wrong, or is the PRODUCT wrong?"* **Group C (P6) and L-248 are product-suspect and must be answered before anyone edits an assertion.** **Deleting or `.skip`-ing a red test to green the board is FORBIDDEN** - it is the exact failure mode this ticket exists to kill. Any test that must be parked goes to `test:quarantined` (root `package.json:45`, **which already exists**) with an L-id and a reason. **(3) FLIP THE GATE TO BLOCKING** once green, and only then. **(4)** Record the new gate in **C10's CI gate inventory** and correct the *"CI-enforced"* claim in **C01**/`CLAUDE.md` so the docs stop asserting an enforcement that does not exist. **(5)** Group A is a 6-file pragma fix - cheap, do it early to shrink the noise, but **do not let its cheapness set the tone for C/F.** §GATE-TEST-ESTATE-NOT-A-CI-GATE. |
| L-248 | agent (split out of L-247; **the guard was already there and already red**) | **[HIGH - suspected LIVE regression of an ALREADY-FIXED founder bug] The LEVEL-EXPLODE offset is COMPOUNDING WITHOUT BOUND. The regression guard for L-113 - a bug the founder reported from PROD - is RED, and the lift it measures has blown up to ~1e+119.** `levelExplodeCoordination.test.ts` and `levelExplodeReconcileAllTypes.test.ts` (10 tests) assert the exploded lift equals `index x GAP` (e.g. **8**). They now measure **`-8.36e+119`**, **`-5.01e+120`**, **`-2.72e+100`**. A finite target reached by a value that large is not drift and not a rounding fault - **it is the same transform being re-applied over and over**, each pass multiplying the last. **WHAT MAKES THIS HIGH RATHER THAN JUST RED:** these two files are the **regression guards written for L-113**, a founder-reported production defect (*"selecting an element while stacked/unstacked leaves it UNCOORDINATED"* / *"moving an element while stacked does NOT land correctly"*). The guard's own header states the invariant it protects: **"the offset is a PURE view transform - userData/model baseline is untouched"**. An unbounded, compounding offset is precisely the failure that invariant exists to forbid. One of the red tests is even named **"THE FOUNDER SCENARIO: MOVE_WINDOW rebuilds wall + room + label"**. **So the live hypothesis is that L-113 IS SILENTLY REGRESSED IN PRODUCTION** - elements flying out of the stack on explode, or a move landing in the wrong place - **and we would not know, because L-247 means nobody has run this suite.** **HYPOTHESES - ALL UNPROVEN. DO NOT TRUST THEM (the L-243 / L-246 lesson: I have been refuted on my own root cause repeatedly).** **(H1)** the lift is applied per *reconcile event* instead of per *level*, so every rebuild (`MOVE_WINDOW` -> wall + room + label rebuilds) re-lifts an already-lifted root - compounding geometrically, which fits the exponent. **(H2)** `LevelExplodeController` reads the CURRENT (already-offset) `position.y` as its baseline instead of the stored model Y, so each pass squares the error. **(H3)** it is a HARNESS artifact only - e.g. the controller subscribes to a frame/store bus that the test now ticks unboundedly - in which case the product is fine and the GUARD is what's broken. **H3 would be the comfortable answer and is exactly why it must be checked LAST, not first.** | Level explode / view transform + reconcile (**C04** rendering & scheduling - scene-committer, single rAF; **C11** §6 event-driven rebuild/redetect; **C15** hosted-element rebuild on `MOVE_WINDOW`); `apps/editor/src/engine/inspect/LevelExplodeController.ts`; L-113 lineage | **FIXED (7192e4cb)**. **MY ESCALATION WAS WRONG AND I AM RECORDING IT FIRST: H3 - the "comfortable answer" I told the next agent to check LAST - IS THE CORRECT ONE. The GUARD broke, not the product. L-113 is NOT regressed; users are NOT seeing this.** I raised a HIGH "suspected live prod regression" off a red test without first establishing that the red test described a live defect - **precisely the mistake L-243 exists to teach me.** **But the guard did not rot on its own - the PRODUCT had a latent two-clock defect, and the harness is what exposed it.** **ROOT CAUSE - TWO CLOCKS, NOTHING GUARANTEEING A SHARED ORIGIN.** `_startRaf()` seeded `this._lastTime = performance.now()` (**the ambient global**), while `_tick(now)` differences that against a `now` **INJECTED by the FrameScheduler**. In the browser both happen to originate at `performance.now()`, so `dt >= 0` and the bug stays invisible - **that is luck, not design; nothing in the FrameScheduler contract promises it.** **WHY A NEGATIVE dt IS CATASTROPHIC:** `k = 1 - 0.01^(dt * LERP_FACTOR)` (LERP=10), then `position.y += diff * k`. For `dt >= 0`, `k` is in (0,1] and the position CONVERGES. For `dt < 0`, `0.01^(negative)` explodes, `k` goes large-NEGATIVE, and `diff * k` overshoots **away** from the target, further every frame - **the exponential approach becomes an exponential divergence.** `dt = -4s` gives `k ~ -1e80`. **That is the 1e+119.** The old guard was `Math.min(dt, 0.05)` - an **UPPER clamp only**; it defends against a long stall teleporting the stack and does **nothing** about time running backwards. **WHY IT WENT RED WITH ZERO PRODUCT CHANGE - and this is the part worth keeping:** the guard drives `_tick` with a **synthetic clock starting at `now = 1000`** while `_lastTime` came from the **real** `performance.now()`. **So the test only ever passed while the suite reached that assertion in under one second of wall-clock.** As the repo grew (this suite alone now spends ~100s in collect), the real clock outran the synthetic one, `dt` went negative, and the guard began failing silently. **It was always fragile; it simply took until now to tip - and nobody saw it, because the editor suite has never run in CI. That is L-247, and THIS is the class of thing it was hiding.** **THE FIX - ONE CLOCK, THE INJECTED ONE.** `_lastTime: number \| null`, seeded `null`; the first tick adopts its own `now` (dt = 0 by definition), so the ambient global is never consulted and the two clocks **cannot** disagree - which also removes an unguarded ambient-global read from a controller that is otherwise fed its time by injection. `dt` is then clamped to **[0, 0.05]**: the upper bound is the pre-existing stall guard, the **lower bound is load-bearing** - time must never run backwards in an animation - and with it the lerp is correct for **any** timestamp source, monotonic or not. Defence in depth: the null-seed removes today's cause, the clamp removes the **bug class**. The L-113 invariant ("the explode is a PURE view transform - userData/model baseline untouched") is unchanged and still asserted. **Gate:** levelExplodeCoordination 8/8 + levelExplodeReconcileAllTypes 11/11 = **19/19 green (was 10 red)**; full editor suite **22 failed / 1,472 passed, from 34 / 1,454** - closes 12 tests + 2 files, introduces no new failure; **root tsc exit 0**. | route -> **rendering/view-transform agent** (L-113's owner if still assigned). **(0) REPRODUCE ON THE DEPLOYED BUILD BEFORE READING ANY CODE.** Open a multi-level project on `pryzm.fly.dev`, explode the levels, then MOVE a window (the exact founder scenario the red test is named after) and watch whether the element leaves the stack or lands wrong. **Report what you SEE first.** If it reproduces, this is a Phase-0-class live regression and jumps the queue. If it does NOT reproduce, then H3 is live and the GUARD is the defect - say so plainly and fix the guard, **but do not delete it.** **(1)** Do not "fix" the test by widening the tolerance. A value of 1e+119 is not a tolerance problem. **(2)** Whatever the outcome, the guard must end GREEN and must be inside the L-247 CI gate, or L-113 can regress a third time. §FIX-LEVEL-EXPLODE-OFFSET-COMPOUNDING. |
| L-249 | agent (found by triaging L-247's red suite product-first) | **[HIGH - LIVE PRODUCTION BUG, and the vindication of Gate G0] LIVE CLIMATE NORMALS COULD NEVER LOAD. The offline-first stage POISONED ITS OWN CACHE, so the live upgrade never even attempted a fetch. Every PRYZM site in production has been pinned to `fallback-defaults` climate for the entire life of this code path.** `ensureSiteClimate.test.ts` was one of the 22 red tests in L-247 and looked like textbook rot (*"uses LIVE Open-Meteo data when the injected fetch succeeds: expected `'fallback-defaults'` to be `'noaa-normals'`"*). **It was not rot. It was telling the truth.** **THE MECHANISM - a two-stage design defeated by a one-stage cache.** `ensureSiteClimate` is offline-first (**C21 §1.2**): **stage 1** resolves the BUNDLED tier with no `fetchImpl` so the climate card paints instantly and offline; **stage 2** re-resolves WITH a `fetchImpl`, in a deliberately backgrounded task, to upgrade the site to live measured normals (`noaa-normals`, Open-Meteo + PVGIS). **But `resolveNormals` caches by lat/lon ONLY, and stage 1 wrote its BUNDLED result into that cache** (`packages/climate-host/src/noaaNormalsReader.ts:175`). **So stage 2 hit the cache on entry, returned `bundled`, and NEVER MADE THE FETCH.** Not *"the fetch failed"* - **the fetch was never attempted.** The upgrade was **structurally unreachable**. The whole live integration - the Open-Meteo adapter, the URL builders, the PVGIS refinement, the 8s timeout - **has been dead code sitting behind a cache hit**, and every downstream consumer (climate card, degree-days, design temps, wind rose, and the solar / sun-hours analysis that reads these normals) has silently been running on builtin regional templates. **Trust note:** nothing was mislabelled - the datasets correctly carried `vendor: 'PRYZM-builtin'`. They simply were never upgraded. | Climate ingestion (**C21** §1.2 offline-first, §7.4 degrade-to-bundled); `packages/climate-host/src/noaaNormalsReader.ts`; `apps/editor/src/ui/climate/ensureSiteClimate.ts`; consumers = solar-analysis / degree-days / wind rose | **FIXED (d5e5176e).** **THE CACHE IS NOW TIER-AWARE.** A BUNDLED entry is a **fallback, not an answer**: it may satisfy a caller with no way to do better (no `fetchImpl`), but it must **never** short-circuit a caller that CAN fetch live. Only a `noaa-normals` entry is a final answer for everyone - `const hitIsFinal = hit && (hit.tier === 'noaa-normals' \|\| !opts.fetchImpl)`. Preserves both properties that matter: once live normals are cached nobody re-fetches, and bundled-only callers keep their instant hit. **Does not widen the network surface** - the live fetch now happens exactly where the design always said it would - and the C21 §7.4 degrade path is untouched. **The TEST also had a real bug of its own** (fixing the product alone would not have made it honest): it read the store the instant `ensureSiteClimate` resolved, **racing the deliberately fire-and-forget stage-2 upgrade it never waited for** - it only ever passed on microtask luck. It now waits (bounded) for the tier to land, which asserts the ACTUAL contract and keeps a never-arriving upgrade a FAILURE rather than a hang. **Gate:** ensureSiteClimate **9/9** (was 1 red) and the run now logs *"upgraded to live measured normals"* - **the first time that line has ever fired**; climate-host **99/99**; root tsc exit 0. | **THIS IS THE ARGUMENT FOR GATE G0 (L-247), STATED AS PLAINLY AS IT CAN BE.** A live product defect sat in `main` behind a green-looking board and a red test nobody ran. It was not found by a user, a monitor, or a code review - **it was found because a CI-gate ticket forced someone to ask, of every red line, "is the TEST wrong, or is the PRODUCT wrong?" instead of assuming the former.** That question is the entire value of the gate. **FOUNDER: worth a look on the deployed build** - the climate card should now show a real vendor (Open-Meteo + PVGIS) rather than `PRYZM-builtin`, and sun-hours / degree-days will shift to measured data. §FIX-CLIMATE-BUNDLED-CACHE-BLOCKS-LIVE-UPGRADE. |
| L-250 | **founder (RECURRENT - raised MANY times; explicitly escalated 2026-07-13 with a live console log + screenshot: *"A wall moved with a hosted door - freezes the app - everything gets stucked - still you could not solve it after i have requested many times"*)** | **[CRITICAL - BLOCKER, Gate G1 REOPENED] Moving/resizing a wall that hosts a door FREEZES the app. This has now survived THREE separate "fixes" (L-01/ADR-0099, L-97, L-234). Treat every prior root cause as REFUTED until re-proven.** **WHAT THE SCREENSHOT ACTUALLY SHOWS - AND IT MAY NOT BE THE PATH ANYONE FIXED.** The founder is editing the **`Length` field in the property panel**, not dragging. `Length` is the **only editable field** in `PlacementEditor` (`apps/editor/src/ui/property-panel/PlacementEditor.ts:84-89`); committing it runs `PropertyPanelSections.ts:98-123`, which dispatches **`wall.updateBaseline`** with a recomputed `newEnd` - **and WITHOUT `_recordUndo`, without `_skipBridge`, and with no drag context.** **There are FOUR call sites into `wall.updateBaseline`** (`PropertyPanelSections` = property panel; `registerTransformDragHandler:186` = 3D gizmo; `AlignPlanToolHandler:342`; `MovePlanToolHandler:343`). **L-234 MEASURED AND FIXED THE *DRAG* PATH.** Its own commit body says so: *"the drag path (registerTransformDragHandler / MovePlanToolHandler)"*. **NOBODY HAS EVER MEASURED THE PROPERTY-PANEL PATH.** This is the repo's signature disease - **ONE element, TWO paths, and the second one silently keeps the old behaviour** (L-239 plan-vs-3D wall layers; L-242 layered-vs-plain joins; L-246 the wall cut; L-240 floor finish). **A `Length` edit is also NOT a translation - it MOVES ONE ENDPOINT**, which changes junction topology in a way a rigid drag never does. **THE FREEZE MECHANISM IS ALREADY DESCRIBED, IN OUR OWN SOURCE.** `apps/editor/src/engine/WallRebuildCoordinator.ts:227-234` (§FIX-WALLFLUSH-NOPROGRESS-GUARD, L-97), verbatim: *"a whole-level `_flush` writes each resolved wall's baseline back to the store; that store mutation fires a buffered `wall:update` that re-arms `_scheduleFlush` AFTER `_joinsResolving` clears … when a **moved door-bearing wall** lands in a `§SELF-CLUSTER-GUARD` cluster (**a wall whose BOTH ends fall in one junction cluster**) the resolver keeps wanting to re-write it, so **the flush re-arms EVERY rAF frame and never converges → the founder's hard freeze**."* **So the failure mode is a NON-CONVERGING rAF FLUSH LOOP, a guard for it EXISTS, and the founder still freezes - therefore the guard does not cover his case.** **HYPOTHESES - ALL UNPROVEN. I have been refuted on my own root cause FOUR times in this audit (L-239, L-243, L-246, L-248); the agent must REPRODUCE, not confirm.** **(H1 - LEADING)** the property-panel `Length` path never reaches L-234's incremental scoping (or `WallDeltaClassifier` classifies an endpoint-move as a full-topology change), so it still triggers the **O(level)** rebuild storm L-234 measured (*"moving ONE wall re-extruded EXACTLY N wall bodies - 5/5, 50/50, 200/200 - and re-cut EVERY door void on the level"*). **(H2)** the L-97 no-progress guard bounds the *re-arm* but not the *self-cluster* condition itself, and shortening a wall by dragging one endpoint is precisely how you make BOTH ends land in one junction cluster. **(H3)** the freeze is the `BaselineReversalError` catch path (`UpdateWallBaselineCommand.ts:195-211`) thrashing: reject → `bim-wall-updated` → rebuild → reject … **(H4)** it is NOT a loop at all but a synchronous CSG storm - a door-bearing wall takes the LEGACY single-volume CSG path (L-242: V2 only ever shipped for `!layers && !curve && openings.length === 0`), so a wall WITH a door is by definition on the slow path. **The founder's console shows NO error and NO warning at the freeze - consistent with a hang or a sync storm, NOT an exception.** | Hosted elements / wall rebuild (**C15** hosted-element + baseline-update dual-write invariant; **C11** §6 event-driven rebuild; **C04** scheduling / single rAF; **C16**; **ADR-0099**, **ADR-0057**, **ADR-0055**; lineage **L-01 → L-97 → L-234 → L-250**); `WallRebuildCoordinator.ts`, `WallJoinResolver.ts`, `WallDeltaClassifier.ts`, `UpdateWallBaselineCommand.ts`, `PropertyPanelSections.ts:98` | **SHIPPED 2fe360d8 (§PERF-WALL-MOVE-INCREMENTAL-REBUILD) — buildWall memoized via composeWallGeometryHash; O(affected) not O(level); equality-proof byte-identical across L/T/X/loop corners; 259/259 geometry-wall + 13/13 editor green. Was OPEN (CRITICAL - Gate G1 REOPENED) - but MY LEADING HYPOTHESIS (H1) IS NOW REFUTED FROM SOURCE + MEASUREMENT, AND THE SEARCH SPACE IS MUCH SMALLER.** **H1 REFUTED - THE PANEL PATH IS *NOT* OFF THE FAST PATH.** I claimed the property-panel `Length` edit never reaches L-234's incremental scoping. It does. Three checks, each against source: **(a)** the panel dispatches `wall.updateBaseline`, whose handler bridges to **the same `UpdateWallBaselineCommand`** the 3D gizmo uses - **one command, not two paths**; **(b)** `WallDeltaClassifier` is **baseline-agnostic** (`baselineMoved()` compares BOTH endpoints with an epsilon, `WallDeltaClassifier.ts:93-101`) - so moving ONE endpoint (a Length edit) is classified `moved-wall`, i.e. **the incremental path, exactly like a drag**; **(c)** the `no-prevState -> whole-level` bail-out (`:206`) does not fire either, because **`WallStore.update()` always emits `prevState`** (`WallStore.ts:551`). **So the Length edit gets the L-234 fast path. My "signature two-paths disease" call was wrong here - and a fix built on it would have been wrong too.** **AND THE REBUILD STORM IS MEASURABLY GONE - INCLUDING FOR HOSTED DOORS.** L-234's own harness (`WallMoveRebuildCost.measure.test.ts`) drives the real store + real `UpdateWallBaselineCommand` + real `resolveLevel` + real `WallFragmentBuilder`. Measured on HEAD, for ONE hosted-door wall move on a level of **200 walls where 1-in-4 hosts a door**: **wall bodies rebuilt 200 -> 4; opening bodies re-cut 50 -> 1; CSG hole-extrudes 48 -> 0.** **That is O(affected), and it is NOT a freeze.** **THEREFORE THE FREEZE IS NOT THE REBUILD COST, AND H4 (legacy CSG storm) IS ALSO REFUTED** - the measured extrude count on the fast path is **zero**. **WHAT SURVIVES - the search is now narrow:** **(H2, now the ONLY strong candidate)** the **non-converging rAF flush** described in our own source at `WallRebuildCoordinator.ts:227-234` (L-97): a moved **door-bearing** wall landing in a `§SELF-CLUSTER-GUARD` cluster makes `_flush` re-arm **every rAF frame and never converge - "the founder's hard freeze"**. **A hang is invisible to a COST test** - which is precisely why L-234's measurement passed while the founder still froze. **The next agent must measure CONVERGENCE (flush iterations to settle), not cost.** **(H5, NEW)** the founder's build may simply **predate** L-234 (`2fe360d8`, 2026-07-12) - **verify the deployed bundle before anything else; rapid pushes CANCEL older Fly deploys.** **TWO REAL DEFECTS FOUND EN ROUTE, BOTH FIXED (see verdict of the fix commit):** **(1)** `PlacementEditor.ts` - **Enter committed TWICE** (`commit(); inp.blur();` re-entered the handler through the `blur` listener), so ONE user edit dispatched **TWO `wall.updateBaseline` commands = two rebuild passes + two undo entries**; and **Escape *committed* the half-typed value** instead of cancelling (it called `blur()`, which fired the commit). **(2)** `WallMoveRebuildCost.measure.test.ts` asserts **wall-clock ms** and therefore **FAILS under full-suite load while passing in isolation** - a timing-flaky gate (an L-247 concern). | route -> **hosted-element / wall-rebuild agent**. **THE BRIEF HAS CHANGED - do NOT re-run the refuted work.** **(0) VERIFY THE DEPLOYED BUNDLE FIRST (H5).** L-234 shipped 2026-07-12; if the founder's freeze was on an older bundle, this may already be closed. Cheapest possible check, do it before touching code. **(1) MEASURE CONVERGENCE, NOT COST (H2).** The rebuild cost is measured and fine. Instrument `WallRebuildCoordinator._flush`: **count flush iterations to settle** for a door-bearing wall whose BOTH ends fall in one junction cluster (`§SELF-CLUSTER-GUARD`). If the count does not converge, that is the freeze, deterministically. **A cost test cannot see a hang - that is the lesson of this ticket.** **(2)** The L-97 no-progress guard EXISTS and the founder still froze - so **find the case it does not cover** rather than re-deriving it. **(3)** Ship the guard the last three fixes lacked: *a door-bearing wall whose Length is edited from the panel must settle in a BOUNDED number of flushes.* Assert the BOUND, not the milliseconds. **-- UPDATE (guard SHIPPED, and H2 IS ALSO REFUTED): `WallJoinResolver.hostedDoorLengthEditNoHang.test.ts` now drives the EXACT property-panel transform (`newEnd = start + dir x newLength`, one endpoint, start pinned) across the three topologies a Length edit can create and a translation never can - SHRINK away from a corner (destroys a junction), EXTEND into a neighbour's midspan (CREATES a T-junction), and shorten until BOTH ends fall in ONE cluster (the §SELF-CLUSTER condition our source names as the freeze). It asserts CONVERGENCE, not cost: `resolveLevel(resolveLevel(w)) === resolveLevel(w)`, folding each pass's output back in as the store would. **All four pass. `resolveLevel` IS a fixed point on every one of them, and never yields NaN.** So the resolver does NOT oscillate, even in the self-cluster case - **H2 is refuted at the resolver level.** The guard is permanent: it closes the exact hole L-234 left, whose own header premised every termination argument on *"a MOVE is a pure TRANSLATION ... the baseline KEEPS ITS DIRECTION"* - which a Length edit does not.** **WHERE THIS LEAVES L-250 - ALL FIVE CODE-LEVEL HYPOTHESES ARE NOW DEAD:** H1 (panel off the fast path) refuted from source; H3 (BaselineReversal thrash) refuted by L-234 and by the direction being preserved; H4 (legacy CSG storm) refuted by measurement (48 extrudes -> 0); H2 (non-converging resolve) refuted by this new guard. The rebuild is O(affected), the resolver converges, the geometry is finite. **=> H5 IS NOW THE LEADING EXPLANATION: the founder's frozen session ran a bundle that PREDATES L-234 (`2fe360d8`, shipped 2026-07-12).** **THE DECISIVE NEXT STEP IS THE FOUNDER'S, NOT AN AGENT'S: re-test the wall+door move AND the Length edit on the CURRENT deploy** (which now also carries §FIX-PLACEMENT-EDITOR-DOUBLE-COMMIT). **If it still freezes on a fresh bundle, the freeze is NOT in the geometry layer at all** - and the next agent should instrument `WallRebuildCoordinator._flush` iteration counts in the LIVE app rather than re-deriving any of the above, because every code-level path here is now green under test. §FIX-WALL-LENGTH-EDIT-HOSTED-DOOR-FREEZE. | route -> **hosted-element / wall-rebuild agent** (L-234's owner - it owns `WallRebuildCoordinator` and just instrumented this exact path). **(0) REPRODUCE THE FOUNDER'S EXACT GESTURE FIRST, AND DO NOT TRUST MY HYPOTHESES.** He did **not** drag - **he typed a new `Length` into the property panel on a wall hosting a door.** Drive `wall.updateBaseline` the way `PropertyPanelSections.ts:98` does (no `_recordUndo`, no `_skipBridge`, one endpoint moved) on a door-bearing wall, and **count `_flush` iterations + `produceWall` calls**. L-234 already built the harness - `WallMoveRebuildCost.measure.test.ts` and `WallMoveIncrementalRebuild.equality.test.ts` - **extend those to the PANEL path rather than writing new ones.** If the flush never converges, you have the freeze deterministically and the fix is provable. **(1) A FIX IS NOT DONE UNTIL THE COST IS MEASURED ON *THIS* PATH.** L-234 was closed on a measured drag path; that is exactly why it did not close this. **(2) The architectural fix, if H1 holds, is CONVERGENCE not optimisation:** every `wall.updateBaseline` call site must enter ONE rebuild path with ONE scoping rule - do not add a fifth special case. **(3) Add the regression guard the last three fixes lacked: a door-bearing wall whose Length is edited from the panel must settle in a BOUNDED number of flushes.** **(4)** Verify on the deployed build - the founder tests on `pryzm.fly.dev`, and localhost dev is unusable for this. §FIX-WALL-LENGTH-EDIT-HOSTED-DOOR-FREEZE. |
| L-251 | **founder (2026-07-13, two screenshots + live console; RECURRENT - the wall-joint family has now been raised across L-12, L-242, ADR-0055 and §CLAMP-COSHARE-WELD)** | **[HIGH - geometry correctness] A CLEAN 2-WALL MITRE IS DESTROYED THE MOMENT A THIRD WALL JOINS THAT CORNER. Two walls meeting *en inglete* (mitre) render correctly (2nd image). Add a third wall into the same corner (L+I / T) and the mitre BREAKS: a dark triangular WEDGE opens at the junction in 3D, and the plan shows a small square NOTCH at the corner (1st image, both arrowed).** Founder: *"it should be a clean join - there were originally two walls joining in inglete - second image - correct - another wall comes to join in L + I or T junction - and the first image is the result - which is wrong."* **THE MECHANISM IS NAMED IN OUR OWN SOURCE, AND IT IS A TWO-ALGORITHM SEAM.** `packages/geometry-wall/src/WallJoinResolver.ts:258` - **`§MULTI-CLUSTER: Handle 3+ endpoint clusters BEFORE pair-wise loop`**: *"Pre-clustering pass runs BEFORE the pair-wise loop. All walls in a cluster are trimmed to a **consensus meeting point**"* (`:152-153`), whereas a 2-wall corner is mitred by the pair-wise path: *"Each wall's end cap is cut by the bisector plane → miter cut. The shared miter plane passes through the centreline intersection"* (`:143-144`). **So a corner with TWO walls is MITRED, and the SAME corner with THREE walls is CONSENSUS-TRIMMED by a different algorithm that runs first and overrides it.** The founder is not reporting a bad mitre - **he is reporting that adding a wall SILENTLY SWITCHES the corner onto a different join algorithm.** The residual wedge is the geometric difference between the two. **This is the same disease as L-242/L-246/L-239/L-250 - ONE corner, TWO code paths - and it is the fourth time it has surfaced in this audit.** **CORROBORATING PRIOR ART (do not re-derive it):** the multi-cluster pass is already implicated in a known defect - a 5-endpoint cluster where *"the guard skips a degenerate wall but the mesh still renders → black spike"*; `:83-85` and `:492` both warn that this path can produce *"degenerate overlapping geometry → phantom spike"*; and **§CLAMP-COSHARE-WELD was REVERTED** because moving shared endpoints to fix corners surfaced DOUBLED walls. **The wedge in the founder's 3D image is exactly a phantom/degenerate face, and the plan notch is its footprint.** **HYPOTHESES - ALL UNPROVEN; REPRODUCE, DO NOT CONFIRM (I have been refuted on my own root cause five times in this audit: L-239, L-243, L-246, L-248, and L-234's fix did not close L-250).** **(H1 - LEADING)** the `§MULTI-CLUSTER` consensus-trim runs for the 3-endpoint cluster and **replaces** the pair-wise mitre, so the two originally-mitred walls are re-trimmed to a consensus point that does not lie on their old bisector - leaving an uncovered wedge between the mitre face and the consensus cut. **(H2)** the consensus pass trims but does not re-issue a mitre NORMAL for the pair, so the geometry builder extrudes a square end-cap where a bisector cut is required (that is precisely a square NOTCH in plan). **(H3)** ADR-0055's **JunctionResolverV2 never runs here at all**: L-242 proved V2 only ever shipped for `!layers && !curve && openings.length === 0` - **and the founder's wall HOSTS A DOOR (visible in the 3D image)**, so this corner is on the LEGACY resolver by construction. **H3 would mean the modern junction pipeline has never once seen the founder's actual geometry.** **(H4)** the third wall is a partition hitting a shell, so `§PARTITION-SHELL-INNER-FACE` clamps it to the inner face while the other two remain centreline-mitred - mixing two reference lines at one corner. | Wall joins / junction resolution (**ADR-0055** Pascal wall pipeline: P1 JunctionResolverV2 + P2 WallFootprint2D + P3 Extruder; **C11** element creation/rebuild; **C04** rendering; **C15** - the wall HOSTS A DOOR, which per L-242 forces the legacy path; lineage **L-12 §FIX-WALL-TJUNCTION-BUTT → L-242 → §CLAMP-COSHARE-WELD (reverted) → L-251**); `packages/geometry-wall/src/WallJoinResolver.ts:143-153, :258` | **REPRODUCED, ROOT-CAUSED - AND IT IS NOT A BUG I CAN FIX. IT IS A SPEC CONFLICT AND IT NEEDS A FOUNDER DECISION.** **REPRODUCED EXACTLY** (`WallJoinResolver.sameTypeCornerImmutable.test.ts`, new): build the mitred L first (arm A's end miter normal = `707107,707107`, a real 45deg bisector), then add a third wall - **arm A's miter normal becomes `null`.** The bisector cut is DELETED and the corner squares off to a butt. **That null IS the founder's wedge in 3D and his square notch in plan.** **THE TRIGGER IS NARROWER THAN THE REPORT SUGGESTS - AND THIS MATTERS:** the mitre **survives** an **L (45deg)** newcomer and a **T (perpendicular)** newcomer of the same type. **It is destroyed ONLY when the third wall is COLLINEAR with one of the mitred arms** (the `I` case) - i.e. it continues that arm's line straight through the corner. **WHY THE EXISTING GUARD DID NOT PROTECT HIM.** L-122 (§FIX-EXISTING-CORNER-IMMUTABLE) froze the committed corner **only when the newcomer carried a DIFFERENT `systemTypeId`** - `WallJoinResolver.ts:1146`, `_wallType(wI) !== _wallType(wJ)` - and the comment above it concedes the hole verbatim: ***"a same-type newcomer still passes through."*** **The founder draws with the DEFAULT wall type** (his L-250 screenshot shows the picker reading *"- Plain Wall -"*), so **every wall on his level shares one `systemTypeId`, the guard never fires, and §PASS-THROUGH-FLUSH re-cuts his committed corner.** **THE PART THAT MAKES THIS A DECISION, NOT A PATCH.** The founder's corner and a **genuine T-junction are THE SAME GEOMETRY.** The existing guard `existingCornerImmutable.test.ts` -> *"a SAME-type collinear through-wall is UNCHANGED"* builds `a=(0,0)->(4,0) t1`, `b=(4,0)->(4,3) t2`, `c=(4,0)->(7,0) t3`, same type - **identical topology, identical creation order, identical types to the founder's A/B/C** - and **asserts the mitre MUST be destroyed** (`a.endMN === null`, square caps), because for a **through-wall + stem** that is CORRECT. The founder's case asserts the mitre **must survive**. **Two collinear + one perpendicular at a node has two valid readings - "a through-wall with a stem" and "a mitred corner with a butting newcomer" - and NO GEOMETRIC RULE CAN SEPARATE THEM, because they are not geometrically different. They differ only in what the author MEANT.** **I tried the obvious fix (disambiguate by `createdAt` - freeze a corner committed BEFORE the newcomer arrived) and REVERTED IT: it turns the genuine T-junction guards red. That is the same bug wearing the other hat.** **Both collinear cases are now `it.fails` in the new suite - documented, red-by-contract, suite green (249/249) - so the day this is resolved they flip LOUDLY red-on-pass and force the decision to be recorded. Per L-247, nothing is `.skip`ped to green a board.** | route -> **wall-geometry / junction agent** (ADR-0055's owner; same agent as L-250 - do NOT split them across agents, they will collide on `WallJoinResolver`). **(1) REPRODUCE THE EXACT SEQUENCE, IN ORDER: build the 2-wall mitre FIRST, confirm it is clean, THEN add the third wall and diff the corner geometry.** The ORDER is the whole bug - a corner authored as 3 walls from the start may well be fine, and that difference is the evidence. **(2) ANSWER H3 FIRST, IT IS CHEAP AND IT DECIDES THE SHAPE OF THE FIX:** log which resolver actually runs for the founder's corner. If the door forces the legacy path (L-242), then **the real ticket is "ADR-0055 P4b: land V2 for opening-bearing walls"**, and patching the legacy consensus-trim is polishing a path we intend to delete. **(3) The architecturally sound fix is CONVERGENCE, not another special case:** a corner's join must be decided by ONE algorithm whose result is CONTINUOUS as wall-count goes 2 → 3. **Do not add a fourth branch to `WallJoinResolver`** - it already carries a pair-wise miter, a multi-cluster consensus trim, a partition-shell clamp and a diff-thickness butt, and this ticket exists BECAUSE those disagree. **(4) DO NOT re-attempt §CLAMP-COSHARE-WELD** (moving shared endpoints) - it was reverted for DOUBLING walls; solve it in the FOOTPRINT/mitre-normal, not by moving baselines. **(5) Regression guard the last three join fixes lacked: a 2-wall mitre that gains a third wall must remain WATERTIGHT - assert zero uncovered area at the junction, not merely "no exception".** §FIX-WALL-JOIN-MITRE-BROKEN-BY-THIRD-WALL. |
| L-252 | founder (with the LOD-300 reference image, 2026-07-13): *"The door element symbol is clearly better - but it needs to be real sound - can you make it like the second?"* | **[MEDIUM - drawing quality] The plan door had no frame REBATE, no THRESHOLD and no HARDWARE. Not because the code was missing - because NO VIEW EVER ASKED FOR IT.** **THE FULL LOD-300 SYMBOL HAS BEEN IMPLEMENTED SINCE L-241 AND HAS NEVER ONCE RUN.** `packages/geometry-door/src/DoorPlanSymbolBuilder.ts` draws the reveal/rebate door stop (`:358`), the threshold (`:437`) and the lever hardware (`:546`) - all three gated on `lod === 'fine'`, all three unit-tested (11/11 green). But `DEFAULT_DETAIL_LEVEL` was **`'medium'`**, and `DefaultViewsManager` stamped a hard-coded `detailLevel: 'medium'` on **every** view it creates (`:280, :303, :330`). **LOD 200 excludes exactly those three features BY DEFINITION** (`detail-level.ts`: *"'medium' - LOD 200: standard symbol; 'fine' - LOD 300: full symbol (frame reveal/rebate, threshold, hardware)"*). **So the founder was looking at a door drawn to a standard that deliberately omits the detail he was asking for, while the code that draws it sat one default away.** | Drawing / plan symbols (**C09** detail level is visibility INTENT, not UI state; **Contract-23** pen table; L-241 lineage; composes with **L-246**) | **FIXED.** `DEFAULT_DETAIL_LEVEL` **`'medium'` -> `'fine'`**, and `DefaultViewsManager`'s three hard-coded `'medium'` literals now read the **shared constant** instead of re-spelling it - **the very fork L-241 was written to kill** (one enum, three spellings, three layers). One source of truth, so it cannot drift again. **The knob stays LIVE** (C09): any view can drop to `'medium'`/`'coarse'` from the properties panel, and coarse remains right for a 1:200 key plan. What changed is only what you get **without asking**. **A drawing default should be the quality you would hand a builder, not the cheapest thing that renders.** **COMPOSES WITH L-246** (§FIX-PLAN-DOOR-CUTS-WALL, shipped the same day): the wall is now truly sectioned at the cut plane, so the opening is a REAL VOID with poche terminating on the jambs - and the door now draws its rebate, threshold and lever INTO that void. **Together they are the founder's reference image.** **Gate:** geometry-door 11/11; core-app-model 290/290; root tsc exit 0. | **FOUNDER: verify on the deployed build** - the door should now show the jamb rebate notch, the threshold line across the opening, and the lever. If any of the three is still missing, the symbol code is right and the VIEW is not resolving to `'fine'` - check the view's Detail Level in the properties panel first. §FEAT-DOOR-PLAN-SYMBOL-LOD300-DEFAULT. |
| L-253 | **founder (2026-07-13, HIGH IMPORTANCE, with a full console): *"not sure what you did on the main 3D scene - but is freezing on moving - when I move in the scene is not flowing... then I swap to WebGL (which doesn't work) and works well, and back to WebGPU and works well also."*** | **[CRITICAL - perf] THE 3D SCENE STUTTERS ON CAMERA MOVE UNDER WEBGPU - AND A LIVE BACKEND SWAP *CURES IT*. THIS ALMOST CERTAINLY REFRAMES L-250: THE "FREEZE" IS IN THE RENDERER, NOT THE WALL PIPELINE.** **THE DECISIVE CLUE IS THE CURE, NOT THE SYMPTOM.** Swapping WebGPU -> WebGL -> WebGPU makes it smooth. **Geometry does not care which backend draws it.** A wall's baseline, the join resolver and the rebuild coordinator are all backend-agnostic - so if changing the *renderer* fixes it, the fault is in the *renderer*, not in the geometry. **THAT IS WHY EVERY L-250 HYPOTHESIS I TESTED CAME BACK CLEAN:** the rebuild is measurably O(affected) (200 wall bodies -> 4, 48 CSG extrudes -> 0), `resolveLevel` is a proven fixed point even on the §SELF-CLUSTER topology, and the no-progress guard's level signature is millimetre-rounded geometry with no version counter, so it cannot be defeated. **I was hunting in the wrong subsystem, and the founder's own workaround is the evidence that says so.** **TWO SMOKING GUNS IN HIS CONSOLE.** **(1)** `WebGPU: too many warnings, no more warnings will be reported to the console for this GPUDevice.` - **the device is FLOODING validation warnings.** A per-frame validation warning storm is expensive and is exactly the shape of "not flowing". **What is it warning about? Nobody has looked. That log line is the thread to pull.** **(2) THE FIRST WEBGPU RENDERER IS THE *PRE-WARMED* ONE:** `[RendererPrewarm] webgpu renderer pre-warmed in 226 ms` -> `[initScene] Phase 5: pre-warmed renderer consumed - 2,401 ms LONGTASK skipped`. **A live backend swap DISCARDS that renderer and builds a fresh one - and that is precisely when it becomes smooth.** So the prewarmed instance is the prime suspect: it is created early, off the critical path, possibly against a different canvas/DPR/size or before the scene's real configuration exists, and the app then runs on it for the whole session. **HYPOTHESES - ALL UNPROVEN. REPRODUCE, DO NOT CONFIRM (I have been refuted on my own root cause SIX times in this audit - L-239, L-243, L-246, L-248, and twice on L-250).** **(H1 - LEADING)** the **pre-warmed WebGPU renderer** is in a degraded state (wrong size/DPR, stale swapchain, or a pipeline configured before the real scene existed), so every frame pays a cost a freshly-constructed renderer does not. **The founder's swap-and-swap-back IS the control experiment: it replaces exactly one thing - the renderer instance - and the stutter goes.** **(H2) - NO LONGER A HYPOTHESIS. THE FOUNDER'S SECOND LOG CONTAINS THE WARNINGS, AND THEY NAME THE BUG OUTRIGHT:** ``THREE.Color target has no corresponding fragment stage output but writeMask (ColorWriteMask::(Red\|Green\|Blue\|Alpha)) is not zero. - While validating **targets[1]** framebuffer output. - While validating fragment state. - While calling [Device].CreateRenderPipeline(...)`` followed by ``[Invalid RenderPipeline "renderPipeline_RenderPipeline_383"] is invalid due to a previous error. - While encoding [RenderPassEncoder].SetPipeline(...)`` and ``[Invalid CommandBuffer] ... While calling [Queue].Submit(...)``. **THE MRT SCENEPASS DECLARES A SECOND COLOUR ATTACHMENT (`targets[1]`) THAT THE FRAGMENT SHADER NEVER WRITES.** `RenderPipelineManager`'s ScenePass is built with **MRT (output, diffuseColor, normal, velocity)** - so a material whose fragment stage emits only ONE output, while the pass still has a non-zero write mask on target 1, produces an **INVALID RENDER PIPELINE**. Every frame then encodes with an invalid pipeline, every submit is rejected, and WebGPU floods validation errors until it caps them (*"too many warnings"*). **That is the stutter: the GPU is not slow, it is REJECTING WORK, every frame.** And it explains the cure exactly - a live swap tears the pipeline down and rebuilds it (the log shows `§RPM-RECOVERY-DOWNGRADE recoverPipeline complete - phase=phase4`), and the rebuilt one is valid. **Also in the same log and almost certainly related: `Destroyed texture [Texture "ShadowDepthTexture"] used in a submit` (the known §SHADOW-DISPOSE-DEFER hazard) and `[ViewportCrashGuard] suppressed non-fatal GPU internal: Cannot read properties of undefined (reading 'usedTimes')` - a material/texture disposed while still referenced.** **(H3)** `§PERF-WEBGL2-RENDER-ON-MOVE` reveals an asymmetry: the WebGL2 path explicitly turns ON continuous per-frame repaint during camera movement, and the WebGPU path does not. If WebGPU is relying on dirty-flag rendering while the camera moves, frames may be dropped/coalesced badly - which reads as "not flowing" even at a high FPS. **(H4)** device-loss churn: his log shows repeated `WebGPU device lost: reason="destroyed"` around the swaps, plus `captureThumbnail: Read a blank/transparent frame (WebGPU swapchain texture not readable)` - the swapchain may be in a bad state from boot. | Rendering / WebGPU (**C04** rendering & scheduling, single rAF; ADR-0076 §PERF-WEBGPU-FRAGMENT; §RENDERER-LIVE-SWAP; RendererPrewarm; §PERF-WEBGL2-RENDER-ON-MOVE; **very likely the true root of L-250**) | **SHIPPED 2fe360d8 (§PERF-WALL-MOVE-INCREMENTAL-REBUILD) — buildWall memoized via composeWallGeometryHash; O(affected) not O(level); equality-proof byte-identical across L/T/X/loop corners; 259/259 geometry-wall + 13/13 editor green. Was OPEN (CRITICAL) - and it OUTRANKS the wall work, because it may BE the wall work.** | route -> **rendering / WebGPU agent** (owner of `RendererHandleFactory`, `RenderPipelineManager`, `rendererPrewarm`). **(1) READ THE WEBGPU WARNINGS FIRST.** They are being suppressed after a threshold; capture them (fresh tab, Chrome devtools, before the cap trips). **They may name the bug outright, and it is the cheapest possible step.** **(2) RUN THE FOUNDER'S OWN CONTROL EXPERIMENT PROPERLY:** boot -> measure frame time on camera orbit -> live-swap to WebGL -> back to WebGPU -> measure again. **The ONLY variable is the renderer instance.** If the fresh instance is smooth and the prewarmed one is not, H1 is proven and the fix is in `rendererPrewarm` (either configure it correctly, or stop consuming it for the live scene). **(3) DO NOT "FIX" THIS BY DELETING THE PREWARM** - it exists because it removes a 2,401 ms LONGTASK from project-open. Make it correct, not absent. **(4) ANSWER THE ASYMMETRY IN H3:** why does WebGL2 need continuous repaint on camera move and WebGPU not? One of the two is wrong. **(5) RE-TEST L-250 AFTERWARDS** - if the wall+door freeze disappears when the renderer is healthy, L-250 was a symptom of this all along, and the audit should say so out loud. §FIX-WEBGPU-PREWARM-STUTTER. |
| L-254 | founder (2026-07-13, with an AS-IS screenshot + a LOD-300 reference): *"I want to also have sound WINDOW symbols"* | **[MEDIUM - drawing correctness] The WINDOW plan symbol is not architecturally sound.** AS-IS (his screenshot): in plan the window reads as a **flat purple selection band** - a couple of parallel lines with no frame, no sill, no glazing line, no jamb rebate, and no lineweight hierarchy. The reference shows the correct LOD-300 window in plan: **the wall CUT with poche terminating on the void edges; a frame block in section at each jamb with a REBATE/reveal step; the glazing as a THIN double line on the centreline of the frame; a distinct sill/board line; and a clear pen hierarchy (heavy cut wall -> medium frame -> thin glazing).** **THE DOOR HAS THIS AND THE WINDOW DOES NOT - AND THAT IS THE POINT.** `packages/geometry-door/src/DoorPlanSymbolBuilder.ts` implements a full three-tier LOD (coarse/medium/fine) with a frame reveal, a threshold and hardware, all dimensionally resolved from the door's real record (L-241, L-127). **The window has no equivalent.** So the two hosted elements - which C15 governs as ONE family - are drawn to two completely different standards. **This is the same "one concept, two implementations" disease as L-239 / L-242 / L-246 / L-251, in the drawing layer.** | Drawing / plan symbols (**C15** hosted elements - doors and windows are ONE family and must be drawn to ONE standard; **C09** detail level is visibility INTENT; **Contract-23** pen table; L-241 / L-252 lineage; composes with **L-246** the true plan cut) | **SHIPPED bde97da1 (verification-sweep 2026-07-16, guard test green) - FEAT-WINDOW-PLAN-SYMBOL-SOUND; geometry-window suite green. NEEDS LIVE VERIFY for any pixel/interaction placement. Was OPEN (queued).** **Depends on L-246 (shipped) - the wall must be genuinely sectioned for the window to have a void to draw into - and should REUSE L-241's detail-level machinery rather than invent a second one.** | route -> **drawing / plan-symbol agent** (the L-241/L-252 owner - it owns `DoorPlanSymbolBuilder` and has just done exactly this job for the door). **(1) DO NOT WRITE A SECOND SYMBOL ENGINE.** `WindowPlanSymbolBuilder` must mirror `DoorPlanSymbolBuilder`'s structure: the same `DetailLevel` enum (`coarse`/`medium`/`fine` - already the single source of truth in `packages/schemas/src/view/detail-level.ts`), the same "dimensional truth" rule (**L-127: every dimension resolved from the element's real record - frame thickness, glazing thickness, sill depth - NEVER a magic literal**), and the same invariant the door carries: **the frame-cut jamb ticks MUST land on the opening VOID EDGES so the wall lines close onto them with no seam.** **(2) The three LOD tiers, by analogy with the door:** *coarse* = the framed opening + a single glazing line; *medium* = frame block + TRUE double-line glazing at its real thickness; *fine* = medium + jamb REBATE/reveal + sill/board line + (if the type carries it) an opening-sash swing. **(3) Pen hierarchy is not decoration** - heavy on the CUT wall, medium on the frame, thin on the glazing. That is what makes the founder's reference read as a drawing rather than a diagram, and it is Contract-23's job. **(4) The AS-IS purple band suggests the plan is drawing a SELECTION/preview style rather than a symbol at all** - confirm what is actually being emitted before writing new code. §FEAT-WINDOW-PLAN-SYMBOL-SOUND. |
| L-255 | founder (2026-07-13, with a screenshot of the FLOOR · Auto tool active in plan): *"Floor finish creation in PLAN VIEW (auto) doesn't bring the UI modal that is required and IS WORKING ON 3D VIEW - which provides the ELEVATION LEVEL of the floor finish. Check, analyse and implement."* | **[HIGH - correctness] The floor-finish tool in PLAN VIEW silently SKIPS the setup modal that the 3D tool shows - so the floor finish is created with a DEFAULT elevation the user never chose.** In 3D, invoking the floor-finish tool opens a modal that asks for (among other things) the **elevation/level of the finish**. In plan view (`FLOOR: Linear | Orthogonal | Curved | Rectangle | **Auto**`, per his screenshot), clicking inside a room creates the finish **immediately, with no modal** - so a parameter the product itself considers *required* is silently defaulted. **THIS IS THE REPO'S SIGNATURE DISEASE, FOR THE SIXTH TIME IN THIS AUDIT: ONE ELEMENT, TWO CREATION PATHS, AND THE PLAN PATH SILENTLY DROPS WHAT THE 3D PATH RESOLVES.** It is *precisely* **L-239** (a layered wall drawn in plan got the right thickness but NO layers, because 3D re-derived them at render and plan read the stored record), **L-240** (floor finish on the wall CENTRELINE instead of the inner face - **the same element**), **L-243** (the plan stair scavenged `window.activeStairConfig`, a global the 3D setup panel happened to leave behind), and **L-246**. **The pattern is now so consistent it should be treated as an architectural defect in its own right, not as N separate bugs:** *every element type that has both a 3D tool and a plan tool has a class of input that only the 3D tool collects.* | Element creation / floor finish (**C11** element-creation pipeline - the plan and 3D tools MUST converge on ONE chokepoint; **C16** command authoring; **C06** UI shell / tools; lineage **L-213 -> L-240 (§FIX-FLOOR-FINISH-INNER-FACE-ALL-PATHS) -> L-255**; same disease as L-239 / L-243 / L-246) | **SHIPPED 03b36257 (verification-sweep 2026-07-16, guard test green) - FloorPlanToolHandler now shows the floor-finish creation modal w/ baseOffset elevation (FIX-FLOOR-FINISH-CREATION-PARITY) - parity with the 3D tool. NEEDS LIVE VERIFY for any pixel/interaction placement. Was OPEN (HIGH, queued).** | route -> **element-creation / floor agent** (the L-240 owner - it already fixed the *geometry* half of this exact element at the `floor.create` chokepoint, and this is the *input* half of the same seam). **(1) DO NOT BOLT A SECOND MODAL ONTO THE PLAN TOOL.** That would make it THREE paths. **The 3D tool and the plan tool must both resolve their input through ONE surface** - exactly as L-240 fixed the geometry by deriving the inner-face boundary *at the `floor.create` chokepoint* rather than per-tool, and exactly as **L-243** fixed the stair by replacing a scavenged `window.activeStairConfig` global with an injected `PlanToolDrawContext.stairConfig`. **Follow that precedent: a `PlanToolDrawContext.floorFinishConfig`, populated by the SAME setup surface the 3D tool uses.** **(2) ANSWER THE DESIGN QUESTION BEFORE CODING, because it is a real one:** a modal on every click in plan would wreck the `Auto` flow (his screenshot shows *"Click inside a room to place - Esc to finish"*, i.e. a RAPID, repeat-click tool). **So the right shape is almost certainly: ask ONCE when the tool is activated (or show a persistent options bar, like the FLOOR mode picker already in his screenshot), then apply that config to every subsequent click** - NOT a modal per floor. Confirm with the founder which he wants; do not guess. **(3) The elevation must reach `floor.create` identically from the 3D tool, the plan tool, batch generators and AI** - one resolver below the tools, per C11. **(4) Regression guard the L-213/L-240 pattern lacked: a floor finish created in PLAN and one created in 3D with the same chosen elevation must produce BYTE-IDENTICAL stored records** (the L-213 equality test, which is the guard that would have caught L-239, L-240 and this). §FIX-FLOOR-FINISH-PLAN-TOOL-NO-SETUP-MODAL. |
| L-256 | founder (2026-07-13, dimensioned-plan screenshot): *"Please make sure DIMENSION is fully aligned with all contractual documents, spec, ADR etc. DIMENSION ELEMENT SHOULD BE SELECTABLE AND EDITABLE - please check."* | **[HIGH - correctness + governance] The DIMENSION element is not first-class: it cannot be SELECTED or EDITED, and its conformance to the contract suite has never been audited.** His screenshot shows a fully dimensioned plan, so dimensions RENDER - what he cannot do is CLICK one and change it. **Under P6 + C03 a dimension is an ELEMENT like any other: schema, store, commands (create/update/delete), ONE undo entry, selection, properties surface.** If any of those is missing, a dimension is a DRAWING ARTEFACT masquerading as an element - the same class of defect this audit keeps finding (a thing that renders but is not backed by a governed record). **The founder explicitly asked for a CONFORMANCE AUDIT first, then the fix.** **His log also carries a SECOND GPU fault, recorded under L-253: `GL_INVALID_OPERATION: glDrawElements: Mismatch between texture format and sampler type (signed/unsigned/float/shadow)` on the WebGL2 path - a shader handed a texture whose format does not match its declared sampler. Same family as the WebGPU invalid-pipeline bug; whoever takes L-253 fixes both backends.** | Annotations / dimensions (**C03** schemas+commands+state; **C16** CA-1..16; **P6**; **C06** selection; **Contract-23** pen table; `packages/auto-dimension`, annotation/dimension handlers) | **SHIPPED f2d92a5b (verification-sweep 2026-07-16, guard test green) - driveDimension.ts + PropertyPanelAnnotations - dimension selectable+editable (assoc. witness via L-287). NEEDS LIVE VERIFY for any pixel/interaction placement. Was OPEN (HIGH, queued). A CONFORMANCE ticket first, a feature ticket second - the founder asked for the audit explicitly and it must not be skipped.** | route -> **annotations / dimensions agent**. **(1) AUDIT BEFORE CODING - produce the six-point conformance matrix:** does a dimension have (a) an L0 **schema** (P5), (b) a **store** in the StoreRegistry, (c) **commands** satisfying C16 CA-1..16, (d) a **single undo entry** per edit, (e) a **pen-table** entry (Contract-23), (f) **OTel spans** (P8)? **The MISSING entries ARE the work.** **(2) SELECTION:** confirm what is already pickable before building - his own log shows `[PlanViewInteraction] Element selected via plan view click: <uuid>` for annotations, so the gap may be narrower than it looks. **(3) EDIT - THE ADR QUESTION THE FOUNDER MUST ANSWER FIRST:** changing a dimension VALUE is not a text edit, it is a **constraint on the geometry it measures**. Decide explicitly whether an editable dimension **DRIVES the model** (Revit-style, a real constraint that moves the wall) or merely **OVERRIDES the displayed text** (documentation-only). **These are different products. Do not guess.** **(4) Reuse, do not reinvent:** extend `packages/auto-dimension` + the existing annotation commands at the chokepoint - do NOT add a parallel dimension path (the L-239 / L-243 / L-255 disease). §FIX-DIMENSION-SELECTABLE-EDITABLE-CONFORMANCE. **RE-RAISED BY THE FOUNDER 2026-07-13 (second time), AND HE HAS NOW GIVEN THE ACCEPTANCE CRITERIA IN HIS OWN WORDS - USE THEM VERBATIM AS THE DEFINITION OF DONE:** *"A dimension should be selected as easily as a door element - at the moment it is not. If it is possible to select at all, it is fairly hard. Once selected, the properties panel should show its properties, as it does when a door is selected. Please review, analyse, plan and fix."* **THIS SHARPENS L-256 FROM AN AUDIT INTO TWO FALSIFIABLE TESTS, AND IT IS THE SAME 'IS IT A REAL ELEMENT?' QUESTION THE AUDIT WAS ALREADY ASKING - HE HAS SIMPLY NAMED THE TWO PLACES WHERE THE ANSWER SHOWS:** **(A) PICKABILITY.** A dimension is a hairline (a 1-2 px line + witness ticks). Picking it by exact geometry is near-impossible for a human, so a door-grade hit-test **requires a PICK TOLERANCE / hit-corridor around the annotation's geometry** (and hit-testing the TEXT box as well as the line) - the same thing every CAD tool does. **The likely truth is not that selection is 'broken' but that annotations were never given a pick representation at all** - which is exactly what L-256's conformance audit is there to establish. **Prove it before fixing it.** **(B) PROPERTY-PANEL PARITY.** The panel is driven off the element's schema/record. **If the dimension has no proper record (L-256's core question), the panel has nothing to show - so (B) is a SYMPTOM of the conformance gap, not a separate UI bug.** Fixing the panel without fixing the record would be the shortcut, and it is forbidden. **Order: RECORD -> PICK -> PANEL. Fix them in that order or you are painting over it.** |
| L-257 | agent (found in the founder's own console while triaging L-253) | **[HIGH - drawing correctness, SILENT] Every LAYERED wall's plan linework was drawn OUTSIDE the ISO-13567 layer system - on layer "0", which carries NO pen weight, NO colour and NO visibility-graphics override.** The founder's log repeats, on EVERY re-projection: `[TechnicalDrawing] Layer "A-WALL" does not exist. Falling back to "0".` **`WallLayerPlanSymbolBuilder.ts:94` called `drawing.addProjectionLines(projected, 'A-WALL')` without ever creating that layer.** The EdgeProjector creates the SUB-layers (`A-WALL:cut`, `A-WALL:proj`) for its own linework, but **nothing created the BASE `A-WALL` layer this builder targets** - so OBC silently dumped the lines on layer `"0"`. **Consequence: no cut/projection lineweight hierarchy, no VG styling, no poche association, for every layered wall.** The pen table (**Contract-23**) never got a chance to apply. **This is a drawing-wide correctness bug that makes a plan 'read flat' NO MATTER HOW GOOD THE SYMBOLS ARE - and it silently undercut L-241, L-246 and L-252.** The same fact appears from the other end in his log as `VGSceneApplicator ... applied=12/14 layers` (vs 14/14 elsewhere): two layers it expected to style did not exist. | Drawing / plan projection (**Contract-23** pen table; ISO 13567 layering; DOC-1.13 VG applicator; composes with **L-241** / **L-246** / **L-252**) | **FIXED.** `drawing.layers.create(WALL_LAYER)` before `addProjectionLines` in `WallLayerPlanSymbolBuilder`. `layers.create()` is idempotent, so it is safe on every path and cannot double-create. **Gate:** geometry-wall 249/249; root tsc exit 0. | **FOUNDER: this is a likely part of why the door/wall plan still did not look right after L-252.** With the layered walls finally ON the A-WALL layer, the pen hierarchy and VG styling can actually reach them. Re-check the plan after this deploy. §FIX-PLAN-AWALL-LAYER-FALLBACK. |
| L-258 | founder (2026-07-13, three screenshots of the Site-plan-overlay flow + full boot log) | **[HIGH - workflow correctness] THE SITE-PLAN OVERLAY FLOW IS INVERTED, AND THEN IT NEVER COMPLETES. Two distinct defects in one flow - and this is the FIRST THING A NEW USER DOES.** **(A) THE ORDER IS WRONG - THE IMPORT FIRES BEFORE THE USER HAS LOCATED THEMSELVES.** Founder: *"The goal is to allow the user to zoom in and out, NAVIGATE the plan, and THEN when ready, import and overlay the pdf/image. At the moment as soon as the user chooses this option it goes into import, and then will import in a RANDOM, not accurate location - this is wrong."* The intended sequence is: **(1)** the user pans/zooms the map to find their ACTUAL site; **(2)** THEN clicks the *Site plan overlay* button (which already exists); **(3)** uploads, then places / rotates / scales / 2-point-calibrates against the map view they chose. Today step 2 fires on mode entry, so the overlay lands at an arbitrary location the user never picked - and every later move/rotate/scale is a fight to recover from a wrong start. **The overlay must be anchored to a location the user has ALREADY chosen, not the reverse.** **(B) "FINISH - ENTER CANVAS" DOES NOT ENTER THE CANVAS.** The panel's primary action reads *"Finish - enter canvas"* (and after re-entry, *"Finished - update & re-enter canvas"*), and its own helper text promises: *"When the plan lines up, finish to keep it as a reference and start working."* **It does not.** The founder stays on the map/overlay screen (3rd screenshot); the app never transitions. **Expected: land in the PRYZM 3D view with the imported PDF/JPG committed as an UNDERLAY, in SPLIT VIEW (3D + plan).** Because the transition never fires, the user is stranded in setup with an Import Manager entry (`IMPORT MANAGER (1) - PDF/Image - 02-ground-floor-plan-2.jpg`) and no way into the model - *"the process is corrupted"*. **EVIDENCE FROM HIS BOOT LOG (the machinery all EXISTS - so this is a WIRING/ORDERING bug, not a missing feature):** `[initBusHandlers] CREATE_UNDERLAY registered (bridge)`, `TRANSFORM_UNDERLAY registered`, `DELETE_UNDERLAY registered`; `[UnderlayPersistence] Installed (per-project, v2)`; `[main] Underlay Render Service initialized`; `[initUI] ImportManagerPanel mounted`; `[initScene] SplitViewManager ready`; `[gis][plan-gis] always-on Plan + Site (GIS) launcher mounted (L-104, C06 §7 launcher layer)`. **Every piece the flow needs is initialised. They are simply not being driven in the right ORDER, and the final transition is not being fired.** | Geospatial / site + underlay (**C12** LTP-ENU - the overlay placement must be a real geo-anchored transform that survives a 1 km re-centre, NOT a screen-space guess; **C19** Site model & parcel - one Site per Project, site is read-only outside dedicated edit commands; **C06** §7 UI shell + launcher layer; **P6** the overlay transform MUST commit through `CREATE_UNDERLAY`/`TRANSFORM_UNDERLAY` as ONE undo entry, never a direct store write); `SiteBoundaryMap2D`, `GISAreaLayout`, `UnderlayPersistence`, `ImportManagerPanel`, `SplitViewManager`, the site-first onboarding path | **SHIPPED 2ca3604f + 7f8d8d08 (FIX-SITE-PLAN-OVERLAY-ORDER-AND-ENTER-CANVAS) - locate FIRST then overlay; Finish enters the 3D canvas with the image as underlay (also closes L-311 import->view). 39/39 tests. NEEDS LIVE VERIFY: full onboarding locate->overlay->calibrate->Finish->3D split with underlay. Was OPEN (HIGH, queued).** **This is the onboarding path (site-first: location -> draw/import -> author), so a corrupted flow here is a FIRST-RUN FAILURE, not a niche bug.** | route -> **geospatial / site-onboarding agent**. **(1) SEPARATE THE TWO DEFECTS - THEY HAVE DIFFERENT ROOT CAUSES.** (A) is a STATE-MACHINE ORDERING bug; (B) is a MISSING/FAILING view TRANSITION on Finish. Do not conflate them, and do not paper over (A) by fixing (B). **(2) FOR (A) - MODEL THE FLOW AS A REAL STATE MACHINE, because that is what it is:** `locating` (map free: pan/zoom; NO overlay, NO upload) -> user clicks *Site plan overlay* -> `placing` (upload + move/rotate/scale/calibrate against the CURRENT map view) -> `finished` (commit + enter canvas). **The upload must NOT auto-fire on mode entry.** The overlay's initial transform must derive from the map viewport the user actually navigated to and be anchored in **LTP-ENU (C12)** so it survives a re-centre - never a default/origin guess. **(3) FOR (B) - REPRODUCE FIRST, DO NOT GUESS** (this audit has refuted its own root cause six times). Establish WHY the transition does not fire: is the Finish handler unwired, throwing, or firing while the view switch is suppressed? The machinery is all present per his log, so instrument the Finish path and find the break. **Success criterion, concrete and testable: after Finish, the app is in the 3D canvas, in SPLIT VIEW (3D + plan), with the image committed as an underlay VISIBLE IN BOTH PANES.** **(4) P6:** the final transform commits via the underlay commands as ONE undo entry - not a direct store write. **(5) GUARD IT:** an end-to-end test driving locate -> upload -> place -> finish, asserting the resulting view state AND the persisted underlay. §FIX-SITE-PLAN-OVERLAY-ORDER-AND-ENTER-CANVAS. |
| L-259 | founder (2026-07-13, Menorca, with a 3D-globe screenshot + full generation log) | **[HIGH - geospatial correctness] THE CESIUM 3D-TILES GLOBE PLACES THE BUILDING AT THE WRONG ELEVATION *AND* THE WRONG LOCATION - AND IT IS INTERMITTENT.** Founder: *"Why is the Cesium 3D tiles (3D globe view) still incorrect? It is still at 0 elevation, it seems? Why sometimes is it correct and sometimes not? In this case we are in Menorca - really close to sea level - still goes underground. The house is not in the correct location, neither the view."* **THE INTERMITTENCY IS THE MOST IMPORTANT CLUE AND MUST NOT BE DISMISSED AS FLAKINESS - IT IS A RACE.** A geodetic transform is deterministic: given the same lat/lon it CANNOT be right sometimes and wrong other times. So the variable is not the maths, it is **WHEN the terrain height is known.** The overwhelmingly likely mechanism: the model is anchored while the **terrain-height sample is still unresolved** (Cesium terrain / 3D-Tiles load ASYNCHRONOUSLY), so the anchor falls back to **height = 0 (the WGS-84 ellipsoid)** - and at Menorca, where the true ground is only a few metres above sea level BUT THE GEOID/ELLIPSOID SEPARATION IS ~48-50 m, ellipsoid-zero puts the house **tens of metres UNDERGROUND**. When the tiles happen to be resolved first, it looks right. **That single fact explains BOTH the 'still 0 elevation' AND the 'sometimes correct' report at once, which is why it is the leading hypothesis.** **NOTE THE SECOND HALF OF THE REPORT, WHICH IS NOT THE SAME BUG:** *"the house is not in the correct LOCATION, neither the view."* An elevation error CANNOT move the building horizontally. **So there are (at least) TWO defects here and they must be triaged separately: (i) a vertical datum/timing bug; (ii) a horizontal georeferencing bug** (LTP-ENU origin, or the camera/view not being framed on the site). **ALSO IN THE SAME LOG - TWO REAL RENDERER FAULTS, RECORDED HERE SO THEY ARE NOT LOST (they belong to the L-253 family):** `Error: [ScenePass] TSL module not loaded. Call initTSL() before createScenePass()` thrown from `_fullRebuild` <- `_setSsgi` <- `_enablePerfMode` <- `autoEnablePerf` <- `BatchCoordinator._onBatchStart` - **the batch perf-mode path rebuilds the render pipeline BEFORE TSL is initialised**, and `ViewportCrashGuard` is swallowing it (`consecutive 1/4`); and `THREE.TSL: Invalid generated code, expected a "float"`. **Neither is benign - a swallowed pipeline rebuild failure is exactly the class of bug that produced L-253.** | Geospatial (**C12** LTP-ENU rebasing + IfcProjectedCRS + logarithmic depth buffer - this is C12's core promise; **C19** Site model; ADR/SPEC for the Cesium/3D-Tiles integration); `apps/editor/src/ui/geospatial/CesiumViewport.ts`, the site-origin/LTP-ENU code, `getCurrentSiteOrigin`, the 3D-globe launcher; **plus L-253 family** for the two TSL faults | **SHIPPED cfc02246 (2026-07-16, §FIX-CESIUM-GLOBE-ELEVATION-AND-GEOREF) - (i) globeGroundAnchor never coerces unknown ground to 0 (hold-hidden-retry / seat-and-reveal photoreal-tile-clamp) kills the ~49m Menorca burial; (ii) readSiteLocation returns ltp ?? address so the LTP-ENU origin is the SSOT for anchor+camera (fixes wrong location); (iii) framing uses the same origin. 85/85 green. NEEDS LIVE VERIFY: building seats flush on Menorca photoreal tiles at correct lat/lon. Was OPEN (HIGH, queued).** **C12 is the contract that exists to make this correct - a building that renders underground on the globe is a direct C12 violation, not a cosmetic issue.** | route -> **geospatial / Cesium agent** (owner of `CesiumViewport` + the LTP-ENU origin code). **(1) SPLIT THE TICKET FIRST - THERE ARE TWO (OR THREE) DEFECTS.** (i) VERTICAL: wrong height / underground. (ii) HORIZONTAL: wrong location. (iii) The camera/view not framing the site. **Do not fix one and declare victory.** **(2) FOR (i) - CHASE THE RACE, NOT THE MATHS.** The transform is deterministic; the intermittency proves the INPUT is not ready. Instrument the anchor path and log, at the moment the model is placed: the sampled terrain height, whether the terrain/3D-Tiles provider had RESOLVED, and which height was actually used. **Prove whether the fallback is ellipsoid-zero.** If it is, the fix is architectural: **the model must not be anchored until the terrain height has resolved** (await the sample; re-anchor on terrain-ready; never silently fall back to 0). **(3) GEOID vs ELLIPSOID IS THE LIKELY KILLER AND MUST BE STATED EXPLICITLY:** Cesium's default `Cartesian3.fromDegrees(lon, lat, height)` treats `height` as **ellipsoidal**, but architectural/site elevations are **orthometric (above mean sea level)**. In the Balearics the separation is roughly **+48-50 m**. **Feeding an MSL height straight into an ellipsoidal API buries the building by ~50 m** - which is exactly what 'really close to sea level, still goes underground' looks like. Establish which datum every height in the pipeline is in, write it down, and convert ONCE at a documented boundary. **This belongs in C12 as a normative rule.** **(4) FOR (ii) - verify the LTP-ENU origin is the SITE's lat/lon** and that the Cesium model matrix is built from it (not from a stale/default origin, and not from the project origin at 0,0). **(5) GUARD IT:** a test that anchors a model at a known lat/lon/height and asserts the resulting ECEF position is within tolerance of the expected point - **including a case where the terrain sample resolves LATE**, which is the regression that keeps coming back. **(6) HAND THE TWO TSL FAULTS TO THE L-253 (renderer) AGENT** - `initTSL()` must run before any `createScenePass()`, and `ViewportCrashGuard` must not silently swallow a pipeline-rebuild failure. §FIX-CESIUM-GLOBE-ELEVATION-AND-GEOREF. |
| L-260 | founder (2026-07-13, split-view screenshots: 3D + plan of the same two doors) | **[HIGH] THREE defects in one screenshot. They are separate bugs and MUST be triaged separately.** **(A) THE SAME DOOR RENDERS DIFFERENTLY DEPENDING ON *WHERE IT WAS CREATED*.** Founder: *"The doors are rendering different - one was created on PLAN VIEW (split mode), the other in 3D - why?"* His 3D pane shows two doors that are visibly NOT the same object: different leaf/frame treatment, different hardware. **THIS IS THE SEVENTH CONFIRMED INSTANCE OF THE SIGNATURE DISEASE: ONE ELEMENT, TWO CREATION PATHS, AND THE PLAN PATH SILENTLY RESOLVES SOMETHING DIFFERENTLY FROM THE 3D PATH.** Prior instances, all the same shape: **L-239** (a layered wall drawn in plan got the right thickness but NO layers - 3D re-derived them at render, plan read the stored record); **L-240** (floor finish on the wall CENTRELINE instead of the inner face); **L-243** (the plan stair scavenged `window.activeStairConfig`, a global the 3D setup panel happened to leave behind); **L-246**; **L-251**; **L-255** (the plan floor-finish tool skips the modal that collects the elevation). **The pattern is now so consistent that it should be treated as an ARCHITECTURAL DEFECT IN ITS OWN RIGHT: every element type with both a 3D tool and a plan tool has a class of input that only ONE of them resolves.** For doors, the prime suspects are `systemTypeId` (his log shows `[WindowPlanToolHandler] §MAT systemTypeId=wt-timber-casement` - does the DOOR plan handler stamp its type at all?), the frame/leaf material, and the door TYPE defaulting. **(B) THE SLAB IS VISIBLE THROUGH SOLID WALLS IN PLAN.** Founder: *"Plan view in theory is a cut section projection about 1.2 m from the level; walls are solid, and so are slabs - the slab should NOT be visible through the walls. This line of slab should not be visible in plan view as the walls should be SOLID."* His 2nd/3rd images show a continuous slab edge line running straight THROUGH the wall body. **The wall is CUT at 1.2 m and is therefore a SOLID POCHE region - nothing behind or below it may show through.** The log shows `[HiddenLineRemoval] v1 pass - 2 occluder(s), 0/1532 segments removed` - **HLR is RUNNING and REMOVING NOTHING.** That is the thread to pull: either the walls are not registered as OCCLUDERS, or the slab's projected edges are not being tested against them, or the poche fill is drawn but the slab line is drawn ON TOP of it (a draw-order / layer-priority bug rather than an HLR bug). **(C) THE CUT LINE IS NOT HEAVIER THAN THE PROJECTION LINE.** Founder: *"The line of the walls, as it is a CUT line, should be thicker - like the Revit principle - and this should also be managed through the VIEW INTENT."* **This is the ISO-13567 / Revit pen hierarchy: CUT is heavy, PROJECTION is light, BEYOND is lightest.** He is explicitly asking that it be governed by **VISIBILITY INTENT (P7 / C09)**, not hardcoded - i.e. the pen weight per (category x zone) is INTENT, and the view resolves it. **NOTE: L-257 (§FIX-PLAN-AWALL-LAYER-FALLBACK, shipped today) is very likely a PART of this** - layered walls were being drawn on layer `"0"`, which carries NO pen weight and NO VG override, so the pen table could never reach them. **Verify on the current deploy BEFORE assuming (C) is still fully broken.** | Drawing / plan projection + element creation (**C11** element-creation pipeline - the plan and 3D tools MUST converge on ONE chokepoint; **C15** hosted elements; **C09** + **P7** visibility INTENT is a domain concept, and the pen hierarchy belongs to it; **Contract-23** pen table; ISO 13567 layering; DOC hidden-line removal; composes with **L-241**, **L-246**, **L-252**, **L-257**) | **SHIPPED ab973c1e (verification-sweep 2026-07-16, guard test green) - door parity path sound + typed records; doorCreationParity 4/4; solidity via L-264/L-282. NEEDS LIVE VERIFY for any pixel/interaction placement. Was OPEN (HIGH, queued).** | route -> **(A) to the element-creation agent; (B)+(C) to the drawing / plan-symbol agent** (the L-241 / L-252 / L-257 owner). **THEY ARE DIFFERENT SUBSYSTEMS - DO NOT GIVE ALL THREE TO ONE AGENT.** **(A) METHOD:** create the SAME door from the 3D tool and from the plan tool, then **DIFF THE TWO STORED RECORDS FIELD BY FIELD.** The differing fields ARE the bug. Then fix it the way **L-243** fixed the stair: stop scavenging globals / per-tool defaults, and **resolve the config ONCE at the `door.create` chokepoint** via an injected `PlanToolDrawContext.doorConfig`, so 3D, plan, batch and AI all inherit the same resolution. **Ship the L-213 equality guard: a door created in PLAN and one created in 3D with the same chosen type must produce BYTE-IDENTICAL stored records.** That single test would have caught L-239, L-240, L-243, L-255 and this. **(B) METHOD:** establish WHY `HiddenLineRemoval` reports `0/1532 segments removed` with 2 occluders. Is the wall registered as an occluder? Is the slab edge being tested? Or is this not an HLR problem at all but a DRAW-ORDER / layer-priority problem (the slab line painted over the wall poche)? **Reproduce and prove before changing anything.** The invariant to guard: **nothing below or beyond the cut plane may be drawn inside a cut wall's poche region.** **(C) METHOD:** the pen hierarchy must be resolved from the VIEW INTENT (P7/C09) through the pen table (Contract-23) - **not hardcoded in a builder**. First VERIFY whether L-257 already fixed it (layered walls now reach the A-WALL layer, so the pen table can finally apply). If a gap remains, close it in the intent -> pen-table -> layer chain, and add a guard asserting `weight(CUT) > weight(PROJECTION) > weight(BEYOND)` for the wall category. §FIX-DOOR-CREATION-PARITY-AND-PLAN-CUT-HIERARCHY. |
| L-261 | founder (2026-07-13, with a reference architectural plan) | **[HIGH - drawing correctness] WALLS HAVE NO POCHE FILL IN PLAN. A cut wall must be FILLED, not outlined.** Founder: *"Walls in plan view should render also with a filled light grey - the plain wall - by DEFAULT through the VISIBILITY INTENT settings. And all LAYERED walls should also render in plan view with grey-scale colour filling the inside part."* His reference shows the canonical drawing convention: **cut walls read as SOLID FILLED regions (poche)**, which is what makes a plan legible at a glance - the eye reads figure/ground, not a tangle of lines. Today PRYZM draws wall OUTLINES only, so the drawing *"reads flat"* (his phrase, and the same complaint that drove **L-241**). **TWO DISTINCT REQUIREMENTS - do not collapse them:** **(i)** a **PLAIN wall** gets ONE uniform light-grey fill across its cut body; **(ii)** a **LAYERED wall** gets a **grey-scale fill PER LAYER** (the inside/core layers differentiated by tone), which is how a drawing communicates construction build-up. **AND THE GOVERNANCE POINT, WHICH IS THE ARCHITECTURAL HEART OF THE TICKET:** he says *"by default through the visibility intent settings"* - **the fill is INTENT (P7 / C09), not a hardcoded material.** A poche fill is a per-(category x zone) graphic property exactly like a pen weight, and it must be resolved through the same intent -> pen/graphics -> layer chain, so a view can override it and a template can carry it. **Do NOT hardcode a grey into a builder.** **PRE-REQUISITES ALREADY SHIPPED TODAY - READ THEM FIRST, THE GROUND HAS MOVED:** **L-246** (§FIX-PLAN-DOOR-CUTS-WALL) made the plan cut a TRUE plane-triangle section of the solid, so `A-WALL:cut` genuinely exists for the first time and **there is finally a closed region to fill** (before it, `_renderPocheFills` scanned an ALWAYS-EMPTY `:cut` layer - that is why there was never a fill); and **L-257** (§FIX-PLAN-AWALL-LAYER-FALLBACK) stopped layered walls being dumped on layer `"0"`, which carried no pen weight and no VG override. **Verify what the current deploy actually draws before writing any code.** | Drawing / plan projection (**C09** + **P7** visibility INTENT is a domain concept, not UI state - the fill is intent; **Contract-23** pen/graphics table; ISO 13567; builds directly on **L-241**, **L-246**, **L-257**, **L-260(C)**) | **SHIPPED 90be1b3a (verification-sweep 2026-07-16, guard test green) - poche-by-intent + true door void; wallPocheFill + planCutSection.doorVoid green. NEEDS LIVE VERIFY for any pixel/interaction placement. Was OPEN (HIGH, queued).** | route -> **drawing / plan-symbol agent** (the L-241 / L-252 / L-257 / L-260(B,C) owner - same subsystem, and it must be done AFTER or WITH L-260(C), because the cut lineweight and the cut fill are the same intent chain). **(1) The poche fill is resolved from the VIEW INTENT, per (category x zone), through the pen/graphics table - never a literal in a builder.** **(2) `_renderPocheFills` already exists and scans the `:cut` sub-layer** - establish what it does now that L-246 has given it a real closed region, BEFORE building anything new. **(3) Layered walls need a fill PER LAYER**, keyed off the wall's real layer stack (L-127 dimensional truth: tones/regions derive from the stored `layers`, never a magic literal). **(4) Guard it:** a plain cut wall yields ONE filled region; a layered cut wall yields N regions matching its layer count; and both are overridable from the view intent. §FEAT-WALL-POCHE-FILL-BY-INTENT. |
| L-262 | founder (2026-07-13) | **[STUDY -> FEATURE] DEEP STUDY: LOD 200 vs LOD 300 AS A FIRST-CLASS VIEW-INTENT SETTING, ACROSS PLAN, ELEVATION *AND* SECTION.** Founder: *"Do a study - deep - apply, do a LOD 200 and LOD 300 in the VIEW INTENT setting, to have different plan-view quality in the documentation, for PLAN VIEW, ELEVATION and SECTION."* **WHAT EXISTS ALREADY (do not reinvent it - this is an EXTENSION, not a new system):** the `DetailLevel` enum (`coarse` / `medium` / `fine` = LOD 100 / 200 / 300) is the single source of truth in `packages/schemas/src/view/detail-level.ts` (**L-241 P1** created it precisely to kill a three-way fork of the enum across layers). **L-252** raised `DEFAULT_DETAIL_LEVEL` to `'fine'` and pointed `DefaultViewsManager` at the shared constant. `DoorPlanSymbolBuilder` is the ONLY element that fully honours all three tiers (frame reveal/rebate + threshold + hardware at `fine`). **THE GAP, STATED PRECISELY:** detail level is **declared** but almost nothing **consumes** it. It is honoured by the door, partially or not at all by every other element, and - critically - **it is a PLAN-VIEW story only. ELEVATION and SECTION have no LOD story at all.** So the same building documented at 1:50 and at 1:200 draws identically, which is not a drawing set, it is one drawing at three scales. **THIS IS A STUDY FIRST, THEN A FEATURE - and the founder asked for it in that order. The deliverable is an ADR + a conformance matrix BEFORE any code.** | Drawing / documentation (**C09** + **P7** detail level IS visibility intent; **C24** sheet composition; **C24.1** auto-documentation sets; **Contract-23** pen table; **C15** hosted elements; L-241 / L-252 lineage) | **SHIPPED 3b29bbae (verification-sweep 2026-07-16, guard test green) - ADR-0265 + DetailLevelResolver/solid-occlusion implemented. NEEDS LIVE VERIFY for any pixel/interaction placement. Was OPEN (queued) - STUDY + ADR REQUIRED BEFORE IMPLEMENTATION.** | route -> **drawing / plan-symbol agent** (owner of the DetailLevel machinery). **(1) DELIVER THE STUDY FIRST, AS AN ADR** (`docs/02-decisions/adrs/`), not code. It must contain: **(a) a CONFORMANCE MATRIX - element x view-type (plan/elevation/section) x LOD (100/200/300) - stating for EVERY element what each tier draws today and what it SHOULD draw.** The empty cells are the backlog, and they will be most of the matrix. **(b) the normative rule for what each tier MEANS** per view type (LOD 200 = the standard symbol; LOD 300 = full construction detail - reveals, rebates, thresholds, hardware, layer build-up, poche per layer), so it is a DEFINITION, not a per-element opinion. **(c) how ELEVATION and SECTION consume it** - today they do not consume it at all, and that is the biggest hole. **(2) THEN implement, element by element, against the matrix**, reusing the SHARED `DetailLevel` enum and the door builder as the reference implementation (L-127 dimensional truth: every dimension from the element's real record, NEVER a magic literal). **(3) It must remain INTENT (P7/C09): a view resolves its detail level; a view TEMPLATE carries it; a sheet at 1:200 and a sheet at 1:50 differ BY INTENT, not by a code branch.** **(4) Do NOT let this become a second symbol engine per view type** - one resolver, three consumers. §STUDY-LOD-200-300-ACROSS-VIEW-TYPES. |
| L-263 | founder (2026-07-13) | **[FEATURE] AUTO-DIMENSION runs on the batch-element execution for PLAN views and works well - but ELEVATION views get NOTHING.** Founder: *"Can you also add the auto-dimension from the batch element execution to ELEVATION views? At the moment it is working great for plan view."* **THIS IS THE SAME STRUCTURAL GAP AS L-262, FOUND FROM THE OTHER END, AND THE TWO SHOULD BE READ TOGETHER.** L-262's finding was that the `DetailLevel` (LOD) machinery is a **plan-view story only** - elevation and section consume none of it. This ticket says the same of AUTO-DIMENSION: it is wired into the batch/generation pipeline for plan and **nowhere else**. **The pattern is now unmistakable: PRYZM's documentation capabilities are being built PLAN-FIRST and then not carried across to the other view types.** That is a systemic gap in the documentation layer, not three separate feature requests, and it should be recorded as such. **WHAT AN ELEVATION DIMENSION *IS* - and why this is not a copy-paste of the plan implementation:** a plan auto-dimension measures in the **horizontal (XZ) plane** - wall runs, openings along a wall, room extents. An elevation dimension measures in the **VERTICAL** dimension against a **projected** view: floor-to-floor heights, sill and head heights, parapet/eaves/ridge levels, opening heights, and level datums. **The witness lines, the chain rules and the level references are all different.** So the geometry is genuinely new even though the machinery (`packages/auto-dimension`, the dimension commands, the annotation store) is shared and MUST be reused. | Documentation / annotations (**C24** sheet composition; **C24.1** auto-documentation sets - the auto-doc set explicitly promises *per-level plans + 4 building ELEVATIONS + sections*, so elevations without dimensions are an unmet contract promise; **C09/P7** view intent; **C03/C16** the dimension is an element and its creation is a command; `packages/auto-dimension`, `applyAutoDimensions`; **sibling of L-262 and L-256**) | **SHIPPED fb9eb26f (verification-sweep 2026-07-16, guard test green) - elevation auto-dimension shipped; auto-dimension suite green. NEEDS LIVE VERIFY for any pixel/interaction placement. Was OPEN (queued).** **Sequence it AFTER / WITH L-256** - L-256 is the CONFORMANCE AUDIT of the dimension element itself (does it have a schema, a store, C16-compliant commands, ONE undo entry, a pen-table entry, an OTel span - and is it selectable/editable?). **Auto-generating MORE dimensions onto elevations before that audit lands would simply multiply a non-conformant element.** Say that plainly to the founder rather than shipping volume over soundness. | route -> **annotations / dimensions agent** (the L-256 owner - same element, same subsystem). **(1) DO NOT FORK `auto-dimension`.** The rule-engine, the chain/witness-line generation and the dimension COMMANDS are shared; what differs is the MEASUREMENT PLANE and the rule set. Extend the existing engine with an elevation strategy - one engine, two (then three, with section) consumers. **(2) DEFINE THE ELEVATION RULE SET EXPLICITLY, because it is not the plan rule set rotated:** floor-to-floor + level datums; opening SILL and HEAD heights; parapet / eaves / ridge; overall building height. Write it down before coding. **(3) The view PLANE is the discriminator** - `PlanToolDrawContext.viewPlane` already distinguishes `isVertical` (section/elevation remap `worldZ` -> world-Y). **Reuse that abstraction; do not invent a second notion of 'which way is up'.** **(4) It must be INTENT-driven (P7/C09)** like everything else in the drawing layer - which dimensions appear, and at which LOD, is a property of the VIEW, not a hardcoded batch step. **This is precisely the L-262 conformance matrix in action: 'auto-dimension x elevation x LOD-300' is one of its empty cells.** **(5) Guard it:** a generated elevation carries the expected dimension chains, each as ONE undo entry, resolved from the model's real levels/openings (L-127 dimensional truth - never a literal). §FEAT-AUTO-DIMENSION-ELEVATION-VIEWS. |
| L-264 | **founder (2026-07-13, RE-RAISED with visible frustration: *"I ALREADY REQUESTED THIS"*)** | **[HIGH - ARCHITECTURAL] ELEMENTS ARE NOT TREATED AS SOLIDS BY THE DRAWING LAYER, UNIFORMLY, ACROSS PLAN + ELEVATION + SECTION. This is ONE architectural gap, not five bug reports, and it is now being escalated as such.** Founder: *"Could you please target the SLAB LINE VISIBILITY and in general VISIBILITY in plan view AND elevations AND sections, considering elements as SOLIDS? I already requested this. In this example the slab line should not be visible through the wall, as the wall is a solid element. Also, when are you targeting the walls FILLED GREY and the THICKNESS OF THE LINES in cut - plan view, but also in SECTION and ELEVATIONS - all in the context of the VIEW INTENT?"* **FIRST, THE HONEST STATUS OF WHAT HE IS LOOKING AT.** The slab-through-wall defect he screenshotted **HAS been root-caused and fixed** (§FIX-PLAN-CUT-SOLIDITY-AND-PEN-HIERARCHY, commit `3b67ec57`, on `main`). The root cause was exactly the one he intuits: `HiddenLineRemoval` hid a segment **only when BOTH endpoints lay inside an occluder AABB** - so **a slab edge CROSSING a wall has neither endpoint inside, and was never removed** (hence his log's `2 occluder(s), 0/1532 segments removed`: it was running and removing nothing). It now clips crossing spans against the **TRUE projected silhouette** - the plane-solid section that **L-246** made available for the first time this morning. **He was almost certainly testing a bundle that predates it; he must hard-refresh.** **That is a statement of fact, not a defence - and if it still shows through on a fresh bundle, THAT is the surviving bug and it must be reported as such.** **SECOND, AND THIS IS THE REAL TICKET: HIS ASK IS BROADER THAN ANY FIX SO FAR, AND HE IS ARCHITECTURALLY RIGHT.** He is not asking for another patch. He is asking for a **single, uniform rule**: *an element is a SOLID; a view CUTS it or PROJECTS it; and what you see is governed by VIEW INTENT.* Today that rule is implemented **piecemeal, plan-first, and differently in each view type**: `removeHiddenLines()` REMOVES occluded plan segments; `reclassifyOccludedElevationLines()` RE-CLASSIFIES elevation segments (a different verb, a different behaviour); **SECTION appears to have neither**; and poche fill + cut lineweight exist (barely, and only after L-241 / L-246 / L-257 / L-260) **for plan only**. **THIS IS THE THIRD TIME TODAY THE SAME SHAPE HAS SURFACED, FROM THREE DIFFERENT DIRECTIONS - AND THAT CONVERGENCE IS THE FINDING:** **L-262** (the LOD/DetailLevel machinery is plan-only; elevation and section consume none of it), **L-263** (auto-dimension is plan-only), and now **L-264** (solidity/occlusion/poche/lineweight are plan-only). **PRYZM's documentation layer is being built PLAN-FIRST and never carried across.** Three symptoms, one disease. **It should be fixed ONCE, as an architecture, not three times as features.** | Drawing / documentation architecture (**C09 + P7** - visibility INTENT is the governing concept and the founder is explicitly invoking it; **Contract-23** pen table; **C04** rendering; ISO 13567; **C24/C24.1** documentation sets; **unifies L-241 / L-246 / L-252 / L-257 / L-260(B,C) / L-261 / L-262 / L-263**) | **SHIPPED 386de94a (verification-sweep 2026-07-16, guard test green) - FEAT-SOLID-OCCLUSION-AND-POCHE-ACROSS-ALL-VIEW-TYPES in EdgeProjectorService; perSolidZone+wallPoche green. NEEDS LIVE VERIFY for any pixel/interaction placement. Was OPEN (HIGH) - THE UNIFYING ARCHITECTURAL TICKET. The individual fixes (L-260 B/C, L-261) stay, but they are INSTANCES of this; this row is the RULE they must all satisfy.** | route -> **drawing / plan-symbol agent** (already holds L-261 + L-262 - fold this in; it is the same subsystem and the same rule). **(1) STATE THE RULE ONCE, NORMATIVELY, AND PUT IT IN A CONTRACT (C09 and/or Contract-23), because a rule that lives only in a builder gets re-invented per view type - which is exactly how we got here:** *Every element is a SOLID. For a given view, the element is either **CUT** by the view plane (-> heavy outline + POCHE FILL), **PROJECTED** in front of it (-> light outline, and it OCCLUDES what is behind), or **BEYOND** (-> lightest, or hidden). **Nothing behind a solid is drawn through it - in ANY view type.** The weight, the fill and the visibility of each zone are resolved from **VIEW INTENT** (P7/C09) through the pen/graphics table - never hardcoded.* **(2) UNIFY THE THREE VIEW TYPES ON ONE OCCLUSION ENGINE.** Today plan REMOVES and elevation RE-CLASSIFIES and section does neither. **One engine, three consumers** - the only legitimate difference is the view PLANE and the cut/projection classification, and `viewPlane.isVertical` already exists to express that. **Do not write a third occluder.** **(3) SECTION IS THE BIGGEST HOLE - verify what it does today before assuming anything.** **(4) POCHE + LINEWEIGHT must follow the same intent chain in section and elevation as in plan** (a cut wall in a SECTION is just as cut as a cut wall in a PLAN - the founder is right that these are the same concept). **(5) GUARD THE RULE, NOT THE INSTANCE:** a test per view type asserting *no segment behind a solid survives inside that solid's projected silhouette*, and that CUT weight > PROJECTION weight > BEYOND weight, resolved from intent. §FEAT-SOLID-OCCLUSION-AND-POCHE-ACROSS-ALL-VIEW-TYPES. |
| L-265 | founder (2026-07-13, with a reference tagged plan) | **[FEATURE] AUTO-TAG batch executor - a button beside auto-dimension - that tags DOORS, WINDOWS and WALLS in PLAN and ELEVATION.** Founder: *"Feature request - we need to have an AUTO-TAG batch execution button, as we have auto-dimension, for plan view and elevation. It will create tags in the view for doors, windows and walls initially."* His reference shows the canonical convention: **hexagon/circle tags carrying the door + window TYPE MARK, and diamond tags carrying the WALL TYPE mark** (`WallA`), each on a leader line to the element it names. **A tagged plan is what makes a drawing SCHEDULABLE - the tag is the join between the drawing and the door/window/wall schedule.** **THE GOOD NEWS, AND IT DECIDES THE DESIGN: BOTH HALVES OF THIS ALREADY EXIST - FOR ROOMS ONLY.** **(i)** `RoomTagAutoPopulator` already auto-tags ROOMS in a plan view, per level, idempotently, and already cleans up duplicates and orphans (his own log: `[RoomTagAutoPopulator] viewId=vd-sys-plan-l0 level=L0: 8 room-tag(s) created, 0 refreshed, 0 duplicate(s) removed, 3 orphan(s) removed`). **The tag lifecycle problem is SOLVED - for one element type.** **(ii)** `ElementCode` auto-assignment is already wired to the StoreEventBus (`initDataPlatform.ts` §6.3) and is the source of the marks the founder sees in his inspector (`WA-00-001`). **So the MARK a tag must display already exists for every element - nothing needs inventing.** **THE WORK IS THEREFORE NOT 'BUILD A TAG SYSTEM'. IT IS 'GENERALISE THE ONE WE HAVE FROM ROOMS TO DOORS/WINDOWS/WALLS, AND FROM PLAN TO ELEVATION.' Anyone who writes a second tag engine has misunderstood the ticket.** **AND THIS IS THE FOURTH SIGHTING TODAY OF THE SAME DISEASE - the convergence is now overwhelming and should be treated as the headline, not a footnote:** **L-262** (LOD/DetailLevel is plan-only), **L-263** (auto-dimension is plan-only), **L-264** (solidity/poche/lineweight are plan-only), and now **L-265** (auto-tagging exists for ONE element type, in ONE view type). **PRYZM's documentation layer is built PLAN-FIRST and ONE-ELEMENT-AT-A-TIME, and then never carried across.** | Documentation / annotations (**C24.1** auto-documentation sets - a documentation set that is dimensioned but not TAGGED is not a documentation set; **C03** + **C16** - a tag is an ELEMENT and its creation is a COMMAND (one batch = ONE undo entry); **C09/P7** - WHICH categories are tagged, and at which LOD, is VIEW INTENT, not a hardcoded batch step; **C28** schedules - the tag is the join between drawing and schedule; **Contract-23** pen table; siblings **L-256 / L-262 / L-263 / L-264**) | **SHIPPED 57347a18 (verification-sweep 2026-07-16, guard test green) - FEAT-AUTO-TAG-BATCH-EXECUTOR; tagEngine green. NEEDS LIVE VERIFY for any pixel/interaction placement. Was OPEN (queued).** **Sequence AFTER L-256** (the dimension/annotation CONFORMANCE audit) for exactly the reason given there: an auto-TAG executor that mass-produces a non-conformant annotation element just multiplies the problem. **Soundness before volume.** | route -> **annotations / dimensions agent** (already holds **L-256** + **L-263** - same subsystem, same batch-executor pattern, same element family). **(1) GENERALISE, DO NOT FORK.** `RoomTagAutoPopulator` already solves the hard part - idempotent create, refresh, duplicate removal, orphan removal, per view + per level. **Extract that lifecycle into a generic tag populator parameterised by (category, mark source, anchor rule, leader rule), then register room / door / window / wall as CONSUMERS of it.** One engine, N categories - the exact opposite of what L-262/263/264 keep exposing. **(2) THE MARK IS ALREADY THERE:** `ElementCode` (`WA-00-001`) and the wall/door/window **systemTypeId** give you both the INSTANCE mark and the TYPE mark. His reference tags the **TYPE** for walls (`WallA`) and a **type number** for doors/windows - so the tag must be able to display EITHER, chosen by intent. **Resolve it from the element's real record (L-127) - never a literal.** **(3) ELEVATION IS NOT PLAN WITH DIFFERENT NUMBERS.** In elevation the anchor and leader live in the VERTICAL projected plane, and the useful tags differ (window/door type marks on the facade; wall type on the visible face). `viewPlane.isVertical` already expresses the difference - reuse it, do not invent a second notion of 'up'. **(4) IT IS INTENT-DRIVEN (P7/C09):** which categories get tagged, and at which detail level, is a property of the VIEW (and carried by a view TEMPLATE) - not a hardcoded step inside a batch button. This is one of the empty cells of L-262's matrix. **(5) ONE BATCH = ONE UNDO ENTRY** (C16), and tags must survive a re-projection and be cleaned up when their element is deleted - `RoomTagAutoPopulator`'s orphan sweep already proves the pattern. **(6) GUARD IT:** running the executor twice is idempotent (no duplicates); deleting a tagged element removes its tag; a tagged plan and a tagged elevation both resolve their marks from the model, not from literals. §FEAT-AUTO-TAG-BATCH-EXECUTOR. |
| L-266 | founder (2026-07-13, with AS-IS vs REFERENCE images) | **[HIGH] DOOR PARITY STILL NOT CLOSED, AND THE PLAN SYMBOL LOD IS FAR BELOW THE REFERENCE - FOR BOTH DOORS AND WINDOWS.** Founder: *"Door parity is still not correct. Also the door symbol is not as accurate as I want - neither the windows. Window parity not correct and LOD required not correct. Check images 4 and 5 to see the reference level of detail I want."* **READ HIS IMAGES CAREFULLY - THEY ENCODE A SPEC, AND IT IS NOT THE ONE WE BUILT.** **His AS-IS (images 1-3):** two doors on the same wall render **DIFFERENTLY from each other** (one leaf reads heavy/doubled, the other thin) - i.e. **parity is still broken between the two creation paths**; and the symbol itself is a bare leaf-line + a quarter arc. Windows read as a flat stack of parallel lines with no frame articulation. **His REFERENCE (image 4, door):** a **jamb/rebate profile cut into the wall reveal** (the stepped notch at each side), a **leaf drawn as a true rectangle with thickness**, a **swing arc**, and - at high LOD - the **hardware/handle and the open-leaf ghost** drawn as a **lighter, dashed sub-symbol**. **His REFERENCE (image 5, window):** the frame drawn as a **real multi-line profile inside the reveal** - outer frame, sash, **mullion/meeting-stile at the centre**, and the glazing line - i.e. **the window symbol is DRAWN FROM THE WINDOW'S REAL LAYERS (frame / sash / glazing / mullion), not from a stack of arbitrary offsets.** **THE ARCHITECTURAL POINT, AND IT IS THE SAME ONE AS L-262/L-264:** what he is asking for is **LOD-300 plan symbols**, and the ONLY sound way to get them is to derive the symbol from the element's **REAL RECORD** (leaf thickness, frame thickness, sash, glazing, rebate depth, hand/swing) at the **detail level the VIEW asks for** - not to hand-draw a prettier fixed glyph. **A richer hardcoded glyph would be the SAME BUG at higher resolution.** The graded symbol (what is drawn at LOD-200 vs LOD-300) is exactly the empty cell L-262's matrix predicts. | Drawing / hosted elements (**C15** hosted elements - the door/window IS its layers; **C11** creation-path parity; **C09/P7** the LOD is VIEW INTENT; **Contract-23** pen table - the ghost leaf + hardware are a LIGHTER pen, not a different colour; **C24.1**; ISO 13567; **continues L-260A door parity; sibling of L-261 / L-262 / L-264**) | **SHIPPED 9dff8721 (verification-sweep 2026-07-16, guard test green) - door/window symbol parity + LOD300; both parity suites green. NEEDS LIVE VERIFY for any pixel/interaction placement. Was OPEN (HIGH).** **L-260A (door plan/3D config parity) is NOT closed** - the founder is still looking at two doors that disagree. Re-open it as part of this row: **parity FIRST (same record -> same symbol), THEN fidelity (LOD-300 profile).** Fixing fidelity while parity is broken just gives us two beautiful symbols that disagree. | route -> **drawing / plan-symbol agent** (already holds L-261 poche + L-262 LOD - this is the same subsystem and the same rule; door/window symbols are the FIRST consumers of the LOD matrix). **(1) PARITY BEFORE BEAUTY.** Prove with a test that a door created in PLAN and a door created in 3D produce the **byte-identical record** and therefore the **identical symbol**. That is the SEVEN-TIMES-REPEATED disease (L-239/240/243/246/251/255/260A) and it must be killed at the record, not at the renderer. **(2) DERIVE THE SYMBOL FROM THE RECORD, NEVER FROM LITERALS.** The reference door's rebate, leaf thickness and swing come from `DoorDimensions`/systemType; the reference window's frame/sash/mullion/glazing come from the **window's real layers** - `WindowDimensions` already resolves frameThickness/glazingThickness through a record -> systemType -> DEFAULT chain with **no magic numbers** (verified). **Use it. Do not invent a second set of offsets in the symbol builder.** **(3) THE LOD IS THE VIEW'S DECISION (C09/P7):** LOD-200 = opening + leaf + arc; LOD-300 = + rebate/reveal profile, frame/sash/mullion articulation, hardware + open-leaf ghost on a LIGHTER pen. Wire it to `DetailLevel`, which L-262 is already generalising - **do not add a private 'detailed' boolean.** **(4) GUARD IT:** same record -> same symbol regardless of creation path; LOD-300 emits strictly MORE geometry than LOD-200 and every extra line resolves to a pen-table weight; no literal dimension appears in either symbol builder. §FIX-DOOR-WINDOW-SYMBOL-PARITY-AND-LOD300. |
| L-267 | founder (2026-07-13, **re-raised: *"I logged one request some time ago"***) | **[MEDIUM] Wardrobe (furniture) created in the 3D view can be ROTATED; created in PLAN view it *"is not rotated"*. Founder asks why it was never implemented.** **THE HONEST ANSWER IS NOT 'IT ISN'T IMPLEMENTED' - AND THE FILE ITSELF IS WHY HE BELIEVES THAT.** **PRE-placement rotation in plan IS implemented and IS on `origin/main`:** `§FEAT-PLACEMENT-SPACEBAR-ROTATE` (ADR-0105, commit `eb843bab`) - **Spacebar rotates the plan placement preview +90° CW**, and `§FIX-FURNITURE-ROTATION` routes **every** branch of `FurniturePlanToolHandler` (including the wardrobe branch, which previously **hard-coded `rotation: 0`**) through the canonical scalar-yaw builder `this._rotation.rotationY()`. **BUT THE FILE'S OWN DOCSTRING STILL SAID, VERBATIM, DIRECTLY ABOVE THAT CODE: *"Plan view does not support rotation at placement time - rotation defaults to 0."*** So when the founder asked *"why is it not implemented"*, **the source agreed with him.** That stale docstring is fixed in this commit (**§DOC-LIE-PLAN-ROTATION**) - a doc that contradicts the code beneath it is not a cosmetic defect, it is a **false statement about the product that we then repeat back to ourselves.** **THE REAL SURVIVING GAP, STATED PRECISELY:** what exists is **PRE-placement** rotation, in **90° steps**, at creation time. What he is describing - **select a placed wardrobe in the PLAN view and rotate it** - is **POST-placement** rotation, and in plan view **it does not exist**: rotation is the 3D gizmo's job. **That is Gate G7 (`move + rotate + material on ALL elements`), which is still `queued`.** So this is not a furniture bug at all - it is the **plan view lacking a transform gizmo**, which affects EVERY element type, not just wardrobes. | Editing / transforms (**G7**; **C11** creation-path parity; **C03/C16** rotation is a COMMAND (`UpdateFurnitureCommand`), one gesture = ONE undo entry; **C09/P7**; **ADR-0105** placement rotation; `FurniturePlanToolHandler`, `PrePlacementRotation`) | **PARTIALLY SHIPPED + RE-SCOPED.** PRE-placement plan rotation: **DONE** (`eb843bab`, on `main` - he should hard-refresh and try **Spacebar** while placing). Stale docstring: **FIXED** this commit. **POST-placement rotation from the plan view: OPEN, folded into Gate G7** as a general plan-view transform gizmo (all elements), not a furniture special case. | route -> **G7 transforms agent** (NOT the furniture agent - the fix does not belong to furniture). **(1) DO NOT BUILD A WARDROBE-SPECIFIC ROTATE.** The moment we do, we own N rotate implementations and the next element re-raises the same ticket. **Plan view needs the SAME transform contract 3D has - move / rotate / (where meaningful) scale - dispatched through the SAME commands** (P6: commands are the only mutation path; the 3D gizmo already does this). **(2) The discriminator already exists:** `PlanToolDrawContext.viewPlane` distinguishes horizontal from vertical - a plan rotate is a yaw about world-Y, which is precisely the scalar the furniture record already stores. **No new rotation representation may be introduced** (C11 §7.0: rotation is a scalar yaw in radians; `furniture.create` validates `Number.isFinite(rotation)`). **(3) ONE gesture = ONE undo entry** (C16). **(4) Guard it:** rotating a placed element in PLAN produces the same record mutation as rotating it in 3D - **the parity test, again**, because this is the same disease (two paths, one drops what the other resolves) wearing its EIGHTH hat. **(5) Tell the founder the Spacebar shortcut exists** - a shipped feature nobody can discover is a feature we did not ship. §FEAT-PLAN-VIEW-TRANSFORM-GIZMO. |
| L-268 | founder (2026-07-13, screenshot: two buildings, only ONE dimensioned) | **[HIGH] AUTO-DIMENSION only dimensions ONE of the TWO buildings on the level.** Founder: *"This is interesting - I have two 'buildings' on my project, but the auto-dimension only targeted one. Why?"* **ROOT-CAUSE DIRECTION - AND IT IS A MISSING CONCEPT, NOT A MISSING BRANCH.** `applyAutoDimensions.ts:153` feeds the planner **every wall on the level, undifferentiated**: `wallStore.getAll().filter(w => w.levelId === level.id)`. There is **no connected-component split, no per-footprint grouping, and no notion of a BUILDING anywhere in the documentation layer.** The planner resolves ONE overall extent + ONE exterior chain set from that single undifferentiated bag - so **two disjoint footprints collapse into one and only one gets dimensioned.** **PRYZM's documentation layer silently assumes ONE CONTIGUOUS FOOTPRINT PER LEVEL.** The founder just violated that assumption for the first time and it failed **silently** - no warning, no partial-coverage report; the drawing simply came back half-dimensioned and confident. **THE AGENT MUST REPRODUCE, NOT CONFIRM.** The exact collapse point (largest-component selection vs first-loop vs bbox-extreme-face collinearity) is **NOT yet proven** and must be established from a FAILING TEST before any fix - I have been refuted on my own root cause six times on this project and will not guess a seventh. **What IS proven is line 153: the input carries no building concept.** **SAME DISEASE AS L-262/263/264/265, IN A NEW ORGAN: built for the ONE case, never generalised.** Here the ONE case is not 'plan view' - it is **'ONE BUILDING'**. | Documentation / annotations (**C24.1** auto-documentation sets - a set that silently omits half the project is not a documentation set; **C19** site model - a SITE legitimately holds N buildings, so the MODEL already contemplates what the DRAWING layer does not; **C03/C16**; **C09/P7**; `packages/auto-dimension`, `applyAutoDimensions.ts:153`; siblings **L-256 / L-263 / L-265**) | **SHIPPED fb9eb26f (verification-sweep 2026-07-16, guard test green) - FIX-AUTODIM-MULTI-BUILDING + twoBuildings.test; auto-dimension green. NEEDS LIVE VERIFY for any pixel/interaction placement. Was OPEN (HIGH).** **Blocks the auto-documentation promise** - the same one-footprint assumption almost certainly also breaks **auto-tag (L-265)**, **building elevations** (four exterior elevations of *which* building?), and **room-interior elevations**. **Check them all before declaring this fixed; do not fix it only where the founder happened to look.** | route -> **annotations / dimensions agent** (holds L-256 + L-263 + L-265 - same subsystem). **(1) INTRODUCE THE MISSING CONCEPT ONCE, IN ONE PLACE:** a **connected-component / footprint partition** of the level's walls (walls that touch, transitively, are one building). **Do NOT special-case 'if two buildings' - partition ALWAYS, then run the existing planner PER COMPONENT.** One building is simply the N=1 case and the code path stays identical: that is how this stops recurring. **(2) IT IS A DOMAIN CONCEPT, NOT A DRAWING CONCEPT** - so it does **not** belong in `applyAutoDimensions`. Put the partition where the MODEL lives so **auto-tag, elevations and interior elevations can all consume it**. This is precisely the mistake L-262/264 keep punishing: solving it in the plan-view CONSUMER instead of the shared layer. **C19 already contemplates a SITE with N buildings - align with it; do not invent a parallel notion.** **(3) ONE BATCH = ONE UNDO ENTRY (C16)** even across N components. **(4) NEVER FAIL SILENTLY AGAIN:** if the planner covers only part of the level it must **report partial coverage** - the `warnings` channel already exists (`opening-undimensioned` proves the mechanism). **A half-dimensioned drawing must SAY SO.** **(5) GUARD IT:** a level with two disjoint footprints yields **two** overall chains and **two** exterior chain sets; a level with one yields exactly today's output (no regression). §FIX-AUTODIM-MULTI-BUILDING-FOOTPRINT. |
| L-269 | **found by me in the founder's L-268 log - he did not report it** | **[HIGH - SILENT DATA-LOSS RISK] Browser storage QUOTA IS EXHAUSTED, autosave's index write is FAILING SILENTLY, and an uncaught `QuotaExceededError` is escaping into the console.** From his log, on **every autosave**: `[VersionRepository] Quota exceeded - project meta index not updated (eviction exhausted)`, alongside `Uncaught QuotaExceededError: Failed to execute 'setItem' on 'Storage': Setting the value of 'bim-pp-pos' exceeded the quota.` **WHY THIS IS SERIOUS, NOT COSMETIC:** the version BODY still reaches IndexedDB (`12 version(s) persisted... ~0.6 MB`), but the **META INDEX write FAILS** - and **eviction is already exhausted**, i.e. the safety valve has been fully consumed and is now a no-op. **A version whose body exists but whose index entry does not is a version the user cannot find or restore.** And the log shows `PlatformSaveController` printing **'Version saved'** immediately afterwards - **the product tells him his work is safe while the thing that makes it findable did not persist. A FALSE SUCCESS is the worst class of bug we ship.** **Separately:** `bim-pp-pos` is a property-panel POSITION - **pure UI chrome** - written to `localStorage`, now **throwing uncaught**. A cosmetic preference is competing for the same quota as the user's project data, and failing loudly. | Persistence (**C03** state; **P8** - a persistence failure that loses data must surface as a **user-resolvable conflict**, never a `console.warn`; `VersionRepository`, `PlatformSaveController`, `SaveOrchestrator`; sibling of **L-249**, another 'the safety path silently never ran' bug) | **FIXED (`bae46535`, on `main`) - AND TWO OF MY THREE MECHANISMS WERE REFUTED BY MEASUREMENT. BOTH REFUTATIONS MATTER MORE THAN THE FIX.** **CONFIRMED, and WORSE than I described:** `saveVersionWithMeta()` returned `void` and swallowed the quota-blocked index write with a `console.warn`, so `PlatformSaveController` had **no way to know** and logged 'Version saved' unconditionally. **The precise data-loss shape is an ORPHAN:** for a project not yet in `bim-projects-index`, the version BODY lands in IndexedDB while **the project row never persists** - the versions exist, the project is not listed, and **it can never be reopened.** Proven by test (body readable, index empty). **REFUTED #1 - 'eviction exhausted' does NOT mean the valve is spent. IT MEANS THE VALVE NEVER RAN.** The evictor only ever considers `bim-project-<id>-versions` keys - **the exact family `§VERSION-QUOTA-INDEXEDDB` already migrated OUT of localStorage.** With none present the candidate list is empty, the loop runs **zero iterations**, and it prints 'exhausted' having evicted **not one byte** (`removeItem` is never called). **The valve is aimed at a bucket that is empty by design.** *That is the L-249 family again: the safety path silently never ran.* **REFUTED #2 - 'why is quota exhausted at ~0.6 MB?' was THE WRONG QUESTION.** That 0.6 MB is in **IndexedDB** and costs the localStorage origin cap **nothing**. The store that is full is **localStorage**, filled by a key family the evictor does not own. **So rather than keep guessing, the fix makes the app MEASURE AND NAME THE HOG at the moment of failure** (`StorageQuotaDiagnostics`). **SHIPPED:** write outcomes are now `{ok, indexPersisted, reason, usage}`; a failed index write is a `console.error` + a **latched terminal state** + a **user-resolvable banner (P8)** with **Export project** / **Free up space** - **no success log, no success toast**; UI chrome (`bim-pp-pos`) writes go through a funnel that **drops the preference on quota** so a panel position can never take the app down. Server enqueue still runs - the server copy is the recovery path. **9 new tests; 51/51 green.** **Residual (the origin is still full): L-273.** | route -> **persistence agent**. **(1) A FAILED INDEX WRITE MUST NOT REPORT SUCCESS.** `PlatformSaveController` logs *'Version saved'* after `VersionRepository` has already reported the index was NOT updated. **Propagate the failure** - **P8** is explicit that data-losing outcomes surface to the USER. **(2) 'Eviction exhausted' is a TERMINAL state - treat it as one:** when the valve is spent, tell the user and offer a purge/export; do not silently re-enter the failing loop on every autosave. **(3) UI CHROME MUST NEVER THROW INTO THE APP:** wrap the `bim-pp-pos` `setItem` in try/catch and **drop the preference** on quota - a panel position must not compete with project data for storage, and must never take anything down. **(4) INVESTIGATE WHY QUOTA IS EXHAUSTED AT ~0.6 MB OF VERSIONS** - that is tiny; something else is filling the origin's storage, and THAT is the real question. **(5) GUARD IT:** a simulated quota-exceeded on the index write **fails the save LOUDLY**; a quota-exceeded on a UI preference is swallowed and the app survives. §FIX-STORAGE-QUOTA-SILENT-INDEX-FAILURE. |
| L-270 | founder (2026-07-13) | **[FEATURE] LOADING SCREEN for the '3D Globe' and '3D Site' view activations - reusing the EXISTING batch-execution overlay.** Founder: *"Could you add some loading screens during the triggering of '3D globe view' and '3D Site' view? They take their time to be ready and it would be cool to have a loading screen - the same we have during batch element execution, with the white transparent background - so the user sees what's going on, and only when everything is loaded and ready can the user jump in and navigate."* **THE OVERLAY ALREADY EXISTS - HIS OWN LOG PROVES IT.** `[BatchCoordinator] §TRACE ON-BATCH-END-DONE (overlay dismissed post-dual-PSO-compile; §FIX-DUAL-LONGTASK)` and `§TRACE ON-BATCH-END-DEFERRED (suppress lifted NOW; overlay dismiss scheduled for post-render)`. **So the work is NOT 'build a loading screen'. It is: the overlay is wired to the BATCH lifecycle ONLY - teach it the VIEW-ACTIVATION lifecycle as well.** **Anyone who writes a SECOND overlay component has misunderstood the ticket** - that is the identical mistake L-262/L-264/L-265 keep punishing (built for the ONE case, never carried across). **AND HE HAS NAMED THE REAL REQUIREMENT, WHICH IS NOT COSMETIC: *"only when everything is loaded and ready can the user jump in and navigate."*** That is an **INPUT GATE tied to a READINESS SIGNAL**, not a spinner. A spinner that dismisses on a timer while Cesium terrain is still streaming is **worse than no spinner**, because it lies. **The overlay must dismiss on a real readiness event - the same discipline as §LOAD-TIMEOUT-PROGRESS (G2/0.3a), where we replaced a fixed-30s race with a PROGRESS-RESET watchdog.** Note the batch overlay already dismisses on a genuine signal (post-dual-PSO-compile), NOT on a timer - **copy that discipline, do not copy a timeout.** | UX / view lifecycle (**C04** rendering + scheduling; **C09/P7** view intent - the readiness of a VIEW is a property of the view; **C12** geospatial/LTP-ENU - the globe's readiness includes TERRAIN LOAD, which is exactly what L-259 taught us: *anchoring before the terrain has loaded puts the building underground*; **C19** site model; `BatchCoordinator`, `initBatchLifecycle`, `CesiumViewport`, the 3D-Site/3D-Globe view activation path) | **SHIPPED (2026-07-13, §FEAT-VIEW-ACTIVATION-LOADING-OVERLAY).** **Generalised, not forked — and the readiness signal ALREADY EXISTED, exactly as predicted.** (1) `BatchLoadingIndicator` -> **`LoadingOverlayView`**: a producer-agnostic surface (title / phase label / ABSOLUTE progress ratio / note / ERROR state). The `_cumBuilt += built` accumulation was BATCH semantics, not overlay semantics -> moved to the pure `loadingProgress.ts`. (2) **`LoadingOverlayController`** = the ref-counted producer registry (one surface, N producers). Producers: **batch execution** (`initBatchLifecycle`) and **view activation** (`viewActivationLoading.ts`). A batch that ends mid-activation can no longer dismiss the overlay out from under it. (3) **THE READINESS SIGNAL IS L-259's OWN.** `reframeAfterBaseSettle()` is the chokepoint EVERY ground-clamp terminal funnels through (seat-and-reveal / base-unchanged / centroid-unchanged / retries-exhausted / crash-guard catch) - and `hold-hidden-retry` deliberately does NOT reach it, i.e. it is *already* exactly 'the tiles are still streaming'. Exposed as **`CesiumViewport.whenGroundSettled()`**. No second notion of terrain-readiness was invented. (4) Chain: `whenReady()` -> Cesium's OWN tile counters (`onTileLoadProgress` + `sampleTileLoadProgress` - **polled**, because the viewer runs `requestRenderMode:true` and event-only would wait for an event that never comes) -> the GLB/real-model placement promise -> **ground settled**. Real numbers on the bar ('412 / 1,205 tiles'), never a time guess. (5) **INPUT GATE**: backdrop `pointer-events:all` (z 88880 over Cesium's z 15) **AND** `setNavigationEnabled(false)` on Cesium's own camera controller - released on EVERY exit path (ready / failure / cancel). (6) **NEVER HANGS**: a progress-FREEZE watchdog (not a deadline) surfaces an ERROR card with 'Try again' / 'Continue anyway'; a viewport torn down mid-clamp reports `settled:false` -> error, not an eternal spinner. 11 guards in `apps/editor/__tests__/viewActivationLoadingOverlay.test.ts`, **verified RED** by a mutant that dismisses on content instead of ground (4 tests fail). Contracts updated **in place**: **C11 §6.6** rewritten (one surface, N producers; CI-1..CI-8), **C12 §1.4 WRITTEN** (it did not exist - L-259's code had been citing a section that was never added), C06 + C17 refs. **Directly related to L-259** (§FIX-CESIUM-GLOBE-ELEVATION-AND-GEOREF): the globe was placing the building underground **because it anchored before the terrain had loaded**. A real terrain-readiness signal is therefore **needed by BOTH tickets** - if L-259's fix introduced one, **this ticket should CONSUME it, not invent a second.** **Check that first.** | route -> **view-lifecycle / batch-overlay agent** (also holds L-271 - same subsystem: the batch lifecycle and its overlay). **(1) GENERALISE, DO NOT FORK.** Extract the existing overlay from the BATCH lifecycle into a **readiness-gated overlay** driven by a (label, progress, readiness-signal) triple, then register **batch execution** AND **3D-Globe activation** AND **3D-Site activation** as CONSUMERS. One overlay, N producers. **(2) DISMISS ON A REAL SIGNAL, NEVER A TIMER.** For the globe that means terrain tiles loaded + camera anchored (see L-259); for 3D Site it means the scene/tileset is ready to navigate. **A spinner that lies is worse than no spinner.** **(3) GATE INPUT, NOT JUST PIXELS** - he explicitly asked that the user cannot navigate until ready. **(4) SHOW WHAT IS HAPPENING** - he asked to *"see what's going on"*: use the real progress signal (tiles/elements), not an indeterminate bar, wherever one exists. **(5) NEVER HANG:** if readiness never arrives, fail VISIBLY with a retry - do not trap the user behind a permanent overlay. **A hang is invisible to a cost test** (the L-250 lesson) - so guard the FAILURE path too. **(6) GUARD IT:** overlay appears on globe/site activation; dismisses ONLY on the readiness signal; input is blocked until then; a never-arriving signal surfaces an error rather than an eternal spinner. §FEAT-VIEW-ACTIVATION-LOADING-OVERLAY. |
| L-271 | **found by me in the founder's L-270 log - he did not report it** | **[HIGH - CORRECTNESS] `BatchCoordinator.runBatch()` called RE-ENTRANTLY silently DROPS ITS BATCH GUARDS - and this violates the C16 'one batch = ONE undo entry' invariant.** From his live console, repeatedly, during a resi-building generation: `[BatchCoordinator] runBatch called while already batching - nesting not supported. Running fn() without batch guards.` **READ WHAT THAT LINE ACTUALLY SAYS.** It detects the unsupported case, **announces it**, and then **proceeds anyway with the guards OFF**. The guards are not decoration - they are what make a batch **ONE undo entry**, what suppresses the view-dependency tracker, what suppresses room redetection, and what holds the CRDT blackout. **Running `fn()` without them means the inner work is committed OUTSIDE the batch's atomicity.** **C16 is explicit: one batch = ONE undo entry.** A nested batch that drops its guards can therefore **fragment undo** (the user's single 'generate' becomes N undo steps), and bypass the suppression that the surrounding batch is relying on. **IT IS FIRING ON A REAL USER FLOW, NOT A CORNER CASE:** his log shows it during the resi-building chain, immediately around the **ceiling** and **lighting** fan-outs - i.e. **an executor is calling `runBatch` from inside a batch that is already open.** That is exactly the shape of the known `LightingLayoutExecutor`-commits-inside-a-batch hazard already recorded on the office fan-out gate board. **ALSO VISIBLE IN THE SAME LOG AND WORTH TRIAGING WITH IT (do not fix blind - REPRODUCE):** (a) `[lighting-layout] §CHAIN-TIMEOUT - no furnish.layout-executed within 12000 ms - firing lighting anyway` - a **12-SECOND DEAD-WAIT** on every run, which is the same 'dead-wait, not compute' signature as the house post-gen perf audit; (b) `THREE.TSL: Invalid generated code, expected a "float"` - a **TSL shader defect** (and L-253 has already taught us that an invalid pipeline makes the GPU **reject frames**, so this is not cosmetic); (c) a **40-second CRDT blackout** (`§E1-CRDT-BLACKOUT duration=40233ms`). | Commands / batching (**C16** command authoring - ONE batch = ONE undo entry; **C03** §4.5-4.8 undo invariants; **P6** commands are the only mutation path; **C17** batch catalogue; `BatchCoordinator.runBatch`, `initBatchLifecycle`, `LightingLayoutExecutor`, `CeilingExecutor`; related to the recorded `LightingLayoutExecutor` / `onNextSettle` hazard) | **SHIPPED cae813ec (verification-sweep 2026-07-16, guard test green) - FIX-NESTED-BATCH-DROPS-GUARDS; batchNestingUndo 4/4 green. NEEDS LIVE VERIFY for any pixel/interaction placement. Was OPEN (HIGH).** **Undo atomicity is a CONTRACT, and this silently breaks it on a real flow.** It also directly threatens **G10** (undo/redo at scale), which we just marked VERIFIED - **G10's guards pin the invariant, but nothing pins it under NESTED batching**, so the gate is green and the invariant is still breakable. **That is a gate that does not cover - the same lesson as G0.** | route -> **view-lifecycle / batch-overlay agent** (same subsystem as L-270: BatchCoordinator + batch lifecycle). **(1) REPRODUCE FIRST.** Write a FAILING test: a nested `runBatch` must NOT produce more than one undo entry. Prove the fragmentation before fixing it - I have been refuted on my own root cause six times on this project. **(2) DECIDE THE SEMANTICS DELIBERATELY, AND WRITE THEM INTO C16 - do not pick one in a code comment.** The honest options are **(a) SUPPORT NESTING** via depth-counting (inner `runBatch` joins the outer batch; guards released only at depth 0 - this is what `StoreEventBus.beginBatch()` ALREADY does with its `depth now 1`, so there is a precedent IN THIS CODEBASE to follow rather than invent), or **(b) MAKE IT A HARD ERROR** so no executor can ever do it. **What is NOT acceptable is the current third option: detect it, warn, and carry on with the guards off.** **A warning is not a guard.** **(3) FIX THE CALLER TOO:** find the executor(s) opening a nested batch (ceiling/lighting fan-out) - they may simply be wrong to call `runBatch` at all. **(4) EXTEND G10's GUARDS TO COVER NESTING** - the gate is green today and the invariant is still breakable. **(5) Triage the 12 s §CHAIN-TIMEOUT dead-wait and the TSL 'expected a float' defect in the same pass, but REPRODUCE each; do not assume they share a cause.** §FIX-NESTED-BATCH-DROPS-GUARDS. |
| L-272 | founder (2026-07-13, with 3 images: analysis-on vs Real) | **[HIGH] FACADE sun/solar analysis renders TERRIBLY on building surfaces - while the GROUND 'sun hours' heatmap is smooth and gradient-perfect.** Founder: *"Is there not a way to improve the quality of the 'Facade analysis'? Check closely - the detail is really bad. The building facade (3rd image) is fairly clean and smooth; the graphics overlay for sun analysis (2nd image) is terrible. The floor 'sun hours' or any other is SOUND - gradiently great - but on the building surfaces it is terrible."* **HE HAS ISOLATED THE VARIABLE HIMSELF, AND THAT IS THE WHOLE VALUE OF THIS TICKET: THE SAME ANALYSIS, THE SAME MODEL, THE SAME FRAME - GROUND IS BEAUTIFUL, FACADE IS BROKEN.** So it is NOT the solar engine, NOT the colour ramp, and NOT the sun model. **It is the DRAPE.** **WHAT IS PROVEN FROM HIS LOG.** The GROUND path builds a **continuous resampled field**: `[CesiumViewport][site-metric] sunHours heatmap: smooth texture 512x512 (~0.9 m/texel) from 1623/1623 cell(s), radius 240 m (chunked)`. One plane, one field, one interpolated texture - hence the clean gradient. **WHAT IS REFUTED - AND I RECORD IT BECAUSE IT WAS MY OWN FIRST GUESS.** I assumed the facade had NO texture and simply tinted meshes per-face. **THAT IS FALSE.** `CesiumViewport.ts` already imports `prepareFacadeSunGrid`, `planFacadeSampling`, `rasterizeFacadeSunTexture`, `facadeOpeningUvRects`, `FacadeDrapeFace`, `FacadeSunTexture` - **the facade path ALREADY rasterises to a texture.** So *"give the facade a texture like the ground has"* is the WRONG FIX and would have sent an agent chasing a non-bug. **The hypothesis space stays OPEN and must be closed by REPRODUCTION, not by my intuition - I have been refuted on my own root cause SEVEN times on this project today, including on my own L-246 fix.** **THE HYPOTHESES WORTH TESTING, IN ORDER (the agent must MEASURE, not pick):** **(H1) PER-FACE DRAPE ON A SHATTERED SURFACE.** The ground is ONE plane; the facade is `FacadeDrapeFace[]` over a **Real** model with **192 walls + balconies + interior partitions** (his log: `192 wall(s), 26 slab(s), 229 opening(s)`). If each face gets its own small texture, you get **per-face resolution, per-face normalisation and visible SEAMS** - which is exactly what 'blotchy' looks like. **(H2) SAMPLING RESOLUTION.** The ground gets ~0.9 m/texel over 1623 cells. **What does `planFacadeSampling` actually give per face?** If it is a handful of samples per wall, the texture is smooth but the FIELD is coarse - smooth garbage. **(H3) IT IS DRAPING ONTO GEOMETRY IT SHOULD NOT SEE** - interior partitions and balcony returns are visible in his image-1/2, and analysis on an interior wall is meaningless noise. **(H4) NORMALISATION IS PER-FACE, NOT GLOBAL** - if each face normalises its own min/max, adjacent walls get discontinuous ramps and the building reads as patchwork **even when every sample is correct.** | Analysis / geospatial (**C21-CLIMATE-INGESTION** + **ADR-0074**; `@pryzm/solar-analysis` (33 tests, `pryzmComputeSunHours` raycast + heatmap); `CesiumViewport.renderFacadeAnalysis`, `prepareFacadeSunGrid`, `planFacadeSampling`, `rasterizeFacadeSunTexture`; **C12** geospatial; **C04** rendering; **A.24** render tiers - Presentation quality is a PRODUCT promise, and a solar study is a SALES artefact) | **SHIPPED 9aedb9d7 (verification-sweep 2026-07-16, guard test green) - FIX-FACADE-ANALYSIS-DRAPE-QUALITY in CesiumViewport/siteMetricGrids. NEEDS LIVE VERIFY for any pixel/interaction placement. Was OPEN (HIGH).** **This is a credibility bug, not a cosmetic one.** A solar study is something an architect SHOWS A CLIENT. The ground heatmap already proves we can do it beautifully; the facade makes the same product look amateur next to it. | route -> **view-lifecycle / geospatial agent** (already holds L-270 + L-271 and is already in `CesiumViewport.ts` - **same file, so route it here to avoid a collision on a shared checkout**; sequence it AFTER L-271, which is a correctness bug). **(1) REPRODUCE AND MEASURE BEFORE YOU TOUCH ANYTHING.** Instrument the facade path and report the REAL numbers the way the ground path already does: **texels/m, samples per face, number of drape faces, and whether normalisation is per-face or global.** The ground path's own log line (`512x512, ~0.9 m/texel, 1623/1623 cells`) is the BENCHMARK - print the facade's equivalent and the gap will name the bug. **(2) DO NOT ADD A SECOND ANALYSIS ENGINE, AND DO NOT RE-RASTERISE FROM SCRATCH** - the rasteriser EXISTS. **One field, one sampler, two drapes (ground plane + facade faces).** This is the same 'one engine, N consumers' rule that L-262/264/265/268 keep teaching. **(3) IF IT IS H1/H4 (per-face seams / per-face normalisation): the FIELD must be global and the drape merely samples it** - a facade study is one continuous field over the building envelope, not N independent studies stapled together. **(4) IF IT IS H3: drape only the ENVELOPE.** The `resolveBlindFacades` / party-wall machinery already in this file shows we can distinguish exterior from interior - use it; do not paint interior partitions. **(5) OPENINGS ARE ALREADY HANDLED** (`facadeOpeningUvRects`) - do not regress them. **(6) GUARD IT:** a facade study over N adjacent coplanar walls yields a CONTINUOUS ramp across the seam (no per-face discontinuity), and the facade's texels/m is within a stated factor of the ground's. §FIX-FACADE-ANALYSIS-DRAPE-QUALITY. |
| L-273 | **follow-up the persistence agent refused to close silently (L-269)** | **[MEDIUM] The localStorage ORIGIN IS STILL FULL on the founder's machine - L-269 made the app TELL him instead of lying to him, but it did not empty the store. The prime suspect is a LEGACY INLINE UNDERLAY RASTER that only migrates to IndexedDB when its project is REOPENED.** **WHY THIS EXISTS AS A SEPARATE TICKET, AND WHY THAT IS THE RIGHT CALL:** L-269 fixed the FALSE SUCCESS (the app now fails loudly, names the hog, and offers Export / Free-up-space). But `ProjectRepository` **must not delete another module's keys** - **C13 single-writer**. The `pryzm.floorPlanUnderlay.v2.*` family is owned by **`UnderlayPersistence`**, and a proactive sweep belongs to ITS owner. **An agent that reached across the ownership boundary to 'just clear it' would have been a shortcut, and it correctly refused.** That refusal is the architecture working. | Persistence (**C13** single-writer ownership; **C03**; **P8**; `UnderlayPersistence`, `StorageQuotaDiagnostics` (new, from L-269), `ProjectRepository.reclaimRedundantLocalStorage()`; **follow-up of L-269**) | **OPEN (MEDIUM).** L-269 shipped the DIAGNOSIS (`bae46535`, on `main`): the app now ranks the origin by key family at the moment of failure and NAMES the subsystem eating the quota. **So the next person does not have to guess - the product will simply tell us.** Confirm from the founder's next quota banner WHICH family it actually names before building the sweep: **measure, then fix.** | route -> **persistence agent** (after its current work). **(1) DO NOT GUESS THE HOG - READ IT.** `StorageQuotaDiagnostics` now prints the ranking on every failure. Get that output from the founder (or reproduce it) and let it name the family. The suspect is `pryzm.floorPlanUnderlay.v2.*` (multi-MB inline rasters), but **that is a suspicion, not a finding** - and this project has punished eight of my suspicions today. **(2) THE SWEEP BELONGS TO THE OWNER (C13).** `UnderlayPersistence` migrates its rasters to IndexedDB **only when the project is reopened** - so a project the user never reopens keeps its multi-MB raster in localStorage forever. **Make the migration PROACTIVE (or lazily swept), inside UnderlayPersistence.** Do not reach across the boundary from `ProjectRepository`. **(3) MIGRATION IS NOT DELETION** - the raster must land in IDB before its localStorage key is dropped, and a failure mid-migration must not lose the underlay. **(4) GUARD IT:** a legacy inline underlay is migrated to IDB and its localStorage key freed, WITHOUT reopening the project; a mid-migration failure leaves the original intact. §FIX-LEGACY-UNDERLAY-RASTER-QUOTA-SWEEP. |
| L-274 | **found by me while REFUSING to re-fix a working path (L-266 door half)** | **[HIGH] A door/window record with NO `systemTypeId` is silently A DIFFERENT ELEMENT - and NOTHING BACKFILLS IT. Fixing the CREATION path never heals the RECORDS the broken path already wrote.** **MEASURED, NOT ASSUMED:** `resolveDoorDimensions(undefined, 'single')` -> `DEFAULT_DOOR_DIMENSIONS` = **0.900 m x 2.100 m**. `resolveDoorDimensions('dt-solid-timber', 'single')` -> **0.926 m x 2.040 m** (the founder's own log). **A legacy door is 26 mm narrower and 60 mm taller than a new one: a different opening, a different leaf, a different swing radius.** It is not 'broken' - it is **A DIFFERENT DOOR, DRAWN CORRECTLY, FOREVER.** The identical trap exists for windows (`wt-single-pane` vs `wt-timber-casement` - and a different type means different column ratios, i.e. **a mullion, or none**). **HOW THIS WAS FOUND - AND THE PROCESS POINT MATTERS MORE THAN THE BUG.** The founder said *"door parity is still not correct"* AFTER L-260A shipped a door-parity fix. The obvious move was to re-fix the creation path. **I READ IT FIRST, AND IT WAS ALREADY SOUND** - `DoorPlanToolHandler` resolves through `getDoorToolConfig()` + `resolveDoorDimensions()` in BOTH the commit and the preview, with no literals and no `window.doorTool` global. **L-260A genuinely closed it.** Re-fixing it would have burned an agent on a working path and left the real defect standing. **That check is now pinned as a test (P-1/P-2) so nobody 'fixes' it again.** **THE RESIDUAL OF THE SIGNATURE DISEASE, AND IT GENERALISES.** Seven creation-path bugs have been fixed by making the paths converge. **But EVERY ONE of them left behind records that the broken path had already written** - and those records are still in the founder's project, still rendering as different elements. **A CREATION-PATH FIX IS ONLY HALF A FIX WITHOUT A BACKFILL.** | Hosted elements (**C15**; **C11 §3** parity by construction; **L-127** dimensional truth; **C03** records; `resolveDoorDimensions` / `resolveWindowDimensions` fall back to `DEFAULT_*_DIMENSIONS`; residual of **L-266 / L-260A**) | **SHIPPED 2d602666 (2026-07-16) - systemTypeId now represented (optional) on canonical @pryzm/schemas Door/Window so it is not stripped on L0 round-trip; record-repair half already shipped (8c8af20e BackfillHostedElementTypesCommand). Was OPEN (HIGH).** Guarded by `apps/editor/__tests__/doorCreationParity.test.ts` **D-2**, which asserts the divergence is REAL (`typed.width !== untyped.width`). **While that test passes, ANY record missing a systemTypeId is a live rendering divergence on the drawing.** | route -> **hosted-element symbol agent** (holds L-266). **(1) THE FIX IS A BACKFILL OF THE RECORDS, NOT ANOTHER CHANGE TO THE CREATION PATH** - P-1/P-2 prove the creation path is sound. **Resist the reflex.** **(2) A BACKFILL IS A MIGRATION, AND MIGRATIONS ARE COMMANDS (C03/C16):** assign the catalogue default type to every door/window whose `systemTypeId` is absent, as ONE undoable operation, with an OTel span. **(3) DECIDE WHAT THE DEFAULT *IS*, DELIBERATELY - AND STATE THE TRAP UP FRONT:** if the backfill picks a type whose dimensions differ from `DEFAULT_*_DIMENSIONS`, **the legacy door will VISIBLY CHANGE SIZE when the migration runs.** That may well be correct (it becomes a real door) but it MUST be a decision, not a surprise - **the founder's drawing will move.** **(4) THE DEEPEST FIX IS TO MAKE 'NO TYPE' IMPOSSIBLE AT THE SCHEMA:** a door without a type is not a valid door (C15), so the record can never be written untyped again. **(5) GUARD IT:** after migration no door/window record lacks a `systemTypeId`; a legacy fixture migrates to a typed record; the operation is ONE undo entry. §FIX-UNTYPED-HOSTED-ELEMENT-BACKFILL. |
| L-277 | founder (2026-07-13, with a normative spec written in his own words) | **[HIGH - ARCHITECTURAL] THE DRAWING LAYER CONFLATES *PROJECTION* WITH *HIDDEN*. Distance from the viewer is being treated as occlusion, so far-away-but-VISIBLE geometry is drawn DASHED. Founder: *"The current rendering logic is incorrectly treating Projection geometry as if it were Hidden geometry. Elements that are simply farther away from the view, or beyond the cut plane, should not be rendered with dashed lines. Dashed lines should be reserved ONLY for true hidden edges."*** **HE HAS HANDED US THE NORMATIVE MODEL - FOUR ZONES, AND ONLY ONE OF THEM DASHES. Treat this as the SPEC, verbatim:** **(1) CUT** - geometry intersected by the view's cut plane. **SOLID**, heaviest weight, **cut fill (poche)** if defined. **NEVER dashed.** *(walls cut in plan; a column intersected by the section plane)* **(2) PROJECTION** - visible to the viewer but NOT intersected by the cut plane: objects above/below the cut plane that are directly visible, surfaces seen in elevation, geometry behind the cut plane that is still directly visible. **SOLID**, thinner than CUT. **NO dashes.** ***DISTANCE FROM THE VIEWER DOES NOT MAKE AN EDGE HIDDEN*** - projection stays SOLID however far away it is. *(furniture in plan; an un-cut door; a visible wall face in elevation)* **(3) BEYOND** - geometry past the plan cut plane that we deliberately keep showing. **This is NOT hidden geometry.** **SOLID**, lighter than CUT, **never dashed unless the user explicitly overrides.** *(the lower run of a stair; objects below the cut plane that remain visible)* **(4) HIDDEN** - geometry OCCLUDED by other geometry but intentionally shown with hidden-line graphics. **THE ONLY ZONE THAT DASHES BY DEFAULT.** Dashed, thin, no fill. *(a pipe behind a wall; a back-face edge when hidden lines are on)* **WHY THIS IS ARCHITECTURAL AND NOT A STYLING BUG:** we currently have (at least) a CUT/PROJECTION split and a depth-based dash. **There is no BEYOND zone at all, and HIDDEN is being inferred from DEPTH rather than from OCCLUSION.** Those are different questions - *'is it far?'* vs *'is something in front of it?'* - and the drawing layer is answering the wrong one. **This is the missing half of C09 §4.6 (the solidity rule, landed today in ADR-0265): that rule says an element is CUT or PROJECTED and that nothing behind a solid is drawn through it. It does not yet say WHAT PEN each zone gets, and it has no BEYOND zone. L-277 completes it.** | Drawing / documentation (**C09 §4.6** + **ADR-0265** - completes the solidity rule with its PEN semantics; **Contract-23** pen table - the four zones ARE pen-table rows; **C24/C24.1**; ISO 13567; **P7** the zone->pen mapping is VIEW INTENT and must be user-overridable; unifies **L-264 / L-261 / L-262**) | **SHIPPED d18652f4 (verification-sweep 2026-07-16, guard test green) - FEAT-REVIT-LINE-TYPE-SEMANTICS; HiddenLineRemoval.elevationOcclusion + ElevationLineWeightHierarchy green. NEEDS LIVE VERIFY for any pixel/interaction placement. Was OPEN (HIGH).** **Blocks credible elevations and sections** - a drawing where every distant edge is dashed does not read as an architectural drawing. | route -> **drawing / plan-symbol agent** (holds L-261/L-262/L-266 - same subsystem). **(1) MODEL THE FOUR ZONES EXPLICITLY, AS A TYPE.** Today the code reasons in ad-hoc booleans (`isTrueSection`, `:cut` vs `:proj` layer suffixes, an elevation depth). **Introduce `DrawingZone = 'cut' | 'projection' | 'beyond' | 'hidden'` and make EVERY projected segment carry one.** A zone that is not named cannot be styled correctly, and that is precisely how PROJECTION got dashed. **(2) HIDDEN IS AN OCCLUSION FACT, NOT A DEPTH FACT.** It must come from `HiddenLineRemoval` (does a solid lie IN FRONT of this segment?), NEVER from `elevationDepth`. **Any dash keyed on distance is the bug and must be deleted.** **(3) BEYOND DOES NOT EXIST YET - ADD IT** (the founder's stair example is the canonical case: the lower run must show, solid, lighter). **(4) THE ZONE->PEN MAP IS THE PEN TABLE (Contract-23) AND IS VIEW INTENT (P7)** - CUT heaviest + fill; PROJECTION thinner; BEYOND lighter; HIDDEN dashed+thin+no fill. **No literal weights or dash arrays in a builder.** **(5) ONE ENGINE, THREE VIEW TYPES** - plan, elevation and section must classify through the SAME zone resolver (this is the L-264 rule; do not write a third). **(6) GUARD IT:** a segment that is merely FAR is SOLID; a segment that is OCCLUDED is DASHED; a BEYOND segment is SOLID and lighter than CUT; and CUT weight > PROJECTION weight > BEYOND weight, all resolved from intent. §FEAT-REVIT-LINE-TYPE-SEMANTICS. |
| L-276 | founder (2026-07-13, with the Autodesk Forma inspector as reference) | **[UX] PROPERTY PANEL REDESIGN - card-based, minimalist, modern.** Founder: *"Redesign the properties panel - make it more elegant, more simple and minimalist, yet more modern. Check the Forma properties panel: each SECTION is an individual component; BETWEEN them is TRANSPARENT; although everything as a whole is the panel."* **HIS SENTENCE IS THE WHOLE SPEC, AND IT INVERTS WHAT WE HAD.** Before: ONE heavy opaque slab (white card + border + 24px shadow + padding) with grey sections stamped INSIDE it - **the container was the object and the sections were decoration on it.** Forma's model is the opposite, and it is why it reads as calm: **THE PANEL IS NOT A SURFACE. It is a transparent COLUMN - a layout, nothing more.** Each section is an autonomous card floating on the scene, and the gaps between them are **the scene itself showing through**. Depth comes from the CARDS, never from the frame. | UX / brand (**preview-color-unified-pryzm-purple** - white + #6600FF, never black: the saturated purple header slab is now a 3px identity RAIL, since purple is a brand ACCENT and not a background; `PropertyInspector.ts`) | **SHIPPED.** Container: no background, no border, no shadow, no padding - a flex COLUMN with a gap. `.pi-section`: autonomous card (translucent surface + blur + 14px radius + soft shadow + hover lift). Header: a quiet card with a purple identity rail. Inputs: flat and borderless until touched. **`display:flex` NOT `block`** - the transparent gap is the design, and `block` would collapse it (guarded in a comment: a one-word regression silently returns the panel to a stacked slab). | route -> **done by the orchestrator.** Residual for the next UI pass: the legacy section builders still set INLINE chrome (`container.style.background = '#f1f3f5'`), which the card stylesheet must currently override with `!important`. **Migrate those ~15 call sites to drop their inline styling, then delete the `!important`s** - they are a symptom, not the design. §FEAT-PROPERTY-PANEL-CARD-LAYOUT. |
| L-278 | **carried forward by the door agent, which refused to fake it (L-266)** | **[HIGH] THE WINDOW IS NOW THE DOOR'S POOR RELATION: its 3D mesh EDGE-DUMPS into the plan symbol, and its 3D/elevation LOD cells are STILL ZERO. AND THE DOOR'S FIX IS NOT TRANSFERABLE - that is the whole point of this ticket.** **WHY IT CANNOT BE COPIED, AND WHY COPYING IT WOULD BE A REAL BUG:** the door's cure was `skipInPlan` - *an element that has a plan SYMBOL does not also emit its MESH EDGES*, because at the cut height **a door opening is EMPTY**. **A WINDOW IS THE OPPOSITE CASE.** At 1.2 m the cut plane passes THROUGH the window's frame and glazing: those spanning lines are **REAL CUT GEOMETRY**, not phantoms. Tagging a window `skipInPlan` would DELETE the very lines that make it a window. **The window needs the cut/projection split done PROPERLY (L-277's zone model), not the door's blunt instrument.** **WHAT IS ACTUALLY OWED:** **(a)** the window's 3D mesh must contribute its **CUT** section (frame, sash, glazing, mullion - all genuinely cut) and must NOT dump its **PROJECTION** edges (head, sill, opening lights above the plane) across the symbol; **(b)** its 3D + ELEVATION LOD cells are **still ✗** in ADR-0265's matrix - the door row is now closed and elevation is no longer zero (4/42), and the window is the obvious next row; **(c)** the elevation window wants what the door got: real frame rebate, sash/meeting-stile articulation, glazing line - **derived from `WindowDimensions` + the record's EXISTING `columnDividerThickness`/`columnRatios`, never a new field** (an earlier draft invented `mullionThickness` and it was reverted as a second source of truth). | Hosted elements (**C15**; **C09 §4.6** + **L-277** zone model - the window is the case that PROVES the four zones are needed; **ADR-0265** LOD matrix; **Contract-23** pen table; Contract 48 §5 `skipInPlan`; continues **L-266**) | **SHIPPED 2aeae3ec (verification-sweep 2026-07-16, guard test green) - FEAT-WINDOW-CUT-ZONE-AND-LOD; geometry-window green. NEEDS LIVE VERIFY for any pixel/interaction placement. Was OPEN (HIGH).** **Sequence AFTER L-277 lands** - the window is exactly the element that needs a real CUT vs PROJECTION classification, and doing it before the zone model exists would mean hand-rolling one. | route -> **hosted-element symbol agent**. **(1) DO NOT COPY THE DOOR'S `skipInPlan`.** A window's frame and glazing ARE cut. **Copying it would delete real geometry** - and the door agent said so explicitly rather than shipping a fake win. **(2) USE L-277's ZONE MODEL:** the window's mesh contributes CUT (frame/sash/glazing/mullion at the plane) and must be suppressed for PROJECTION (head bar, upper lights). **(3) CLOSE THE WINDOW ROW OF ADR-0265's MATRIX** (3D + elevation), following exactly the pattern the door row now sets: same `resolveEffectiveDetailLevel`, LOD-300 ⊃ 200 ⊃ 100, elevation INHERITS by projecting the mesh (no second symbol engine, ADR-0265 §4.3). **(4) NO NEW FIELDS** - the mullion comes from `columnDividerThickness` + `columnRatios`. **(5) ALSO: L-274's backfill is in your scope** - an untyped window resolves to DIFFERENT dimensions than a typed one (`wt-single-pane` vs `wt-timber-casement` = a mullion, or none), and nothing backfills legacy records. **(6) GUARD IT:** no PROJECTION line of a window bridges its own symbol; the CUT lines survive; LOD-300 ⊃ 200; no literal dimension in any builder. §FEAT-WINDOW-CUT-ZONE-AND-LOD. |
| L-280 | founder (2026-07-14, 3D-vs-plan side by side) | **[HIGH] THE WINDOW'S PLAN-SYMBOL FRAME IS MUCH THICKER THAN THE REAL WINDOW.** Founder: *"The frame in plan view is much 'thicker than in reality' - please fix."* His screenshot puts the 3D window and its plan symbol side by side: the 3D frame is a slender timber section; the plan symbol draws a **fat, chunky band** occupying most of the wall reveal. **THIS IS L-127 (DIMENSIONAL TRUTH) FOR THE WINDOW.** `resolveWindowDimensions()` ALREADY resolves `frameThickness` / `frameDepth` / `sashThickness` / `glazingThickness` through a record -> systemType -> `DEFAULT_WINDOW_DIMENSIONS` chain **with NO magic numbers** (verified when L-266 shipped). **So the resolver is right and the SYMBOL is not asking it.** | Hosted elements (**C15**; **L-127 dimensional truth** - the symbol must be dimensionally IDENTICAL to the placed element; **Contract-23** pen table - **a WEIGHT is not a THICKNESS**; `WindowPlanSymbolBuilder`; sibling of **L-266 / L-278**) | **SHIPPED 94e4ab8f (verification-sweep 2026-07-16, guard test green) - FIX-WINDOW-PLAN-FRAME-THICKNESS; geometry-window green. NEEDS LIVE VERIFY for any pixel/interaction placement. Was OPEN (HIGH).** Trivial to see, and it undermines every drawing containing a window. | route -> **hosted-element records agent** (owns `packages/geometry-window`). **(1) MEASURE FIRST:** print what the symbol uses for the frame band vs what `resolveWindowDimensions()` returns. **The gap IS the bug** - do not eyeball it. **(2) TWO CANDIDATES LOOK IDENTICAL ON SCREEN AND HAVE OPPOSITE FIXES - THIS IS THE PART WORTH THINKING ABOUT:** is the frame drawn at the wrong **WIDTH** (a geometry error - fix the geometry), or at the RIGHT width with a **CUT pen so heavy that the STROKE ITSELF reads as a fat band** (a pen error - fix the pen)? **L-277 has just made CUT the heaviest weight**, so a correct 20 mm frame stroked with a heavy cut pen at 1:100 could easily be exactly what he is seeing. **REPRODUCE BEFORE CHANGING ANYTHING.** **(3) DERIVE FROM THE RECORD, NEVER A LITERAL.** **(4) GUARD IT:** the symbol's frame band equals `resolveWindowDimensions().frameThickness` within tolerance, and NO literal thickness survives in the builder. §FIX-WINDOW-PLAN-FRAME-THICKNESS. |
| L-281 | founder (2026-07-14, marked screenshot) | **[HIGH] THE 'TOTAL' (OVERALL) DIMENSION IS DRAWN STRAIGHT THROUGH THE BUILDING.** Founder: *"Please, the 'total' dims ALWAYS OUTSIDE and at the END of the dim pipeline - never through the building."* His screenshot shows the **13000 mm** overall running as a vertical line **right across the floor plate**, over the rooms. **HE IS STATING A DRAWING CONVENTION THAT IS NOT NEGOTIABLE, AND IT IS TWO RULES IN ONE SENTENCE:** **(a) OUTSIDE** - a dimension line never crosses the thing it measures. **(b) AT THE END OF THE PIPELINE** - dimension chains **STACK OUTWARD IN TIERS**: openings innermost, then the exterior chain, and the **OVERALL is the OUTERMOST tier, furthest from the building**. That ordering is what makes a dimensioned plan readable, and it is why every drawing office does it the same way. **WHAT THIS TELLS US ABOUT THE ENGINE - THE REAL FINDING:** the planner places the overall **from the two extreme perimeter corners** (SPEC §4.2), which is **correct for the VALUE**, and then draws the line **on the axis those corners define** - which on an L-shaped or notched footprint **passes straight over the plate**. **The value is right; the OFFSET is missing.** The overall carries no notion of *"I am the outermost tier; I must be pushed clear of the footprint and of every chain already placed."* | Documentation / annotations (**C24.1** auto-documentation; **C24** sheet composition - the tier gap is SCALE-aware; **C03/C16**; **L-127**; `packages/auto-dimension` - `perimeter.ts`, `planAutoDimensions.ts`; siblings **L-263 / L-268**) | **SHIPPED 845a4590 (verification-sweep 2026-07-16, guard test green) - FIX-OVERALL-DIM-OUTSIDE-AND-OUTERMOST; overallTiers green. NEEDS LIVE VERIFY for any pixel/interaction placement. Was OPEN (HIGH).** **Every auto-dimensioned plan he generates has this.** | route -> **annotations / dimensions agent** (owns `packages/auto-dimension`). **(1) THE FIX IS A TIER MODEL, NOT A NUDGE.** Do not add an ad-hoc offset to the overall. Model the chains as **TIERS** with a stacking rule: tier 0 = openings, tier 1 = exterior chain, tier N = **OVERALL, OUTERMOST**. Each tier is offset from the **FOOTPRINT BOUNDING BOX** by a scale-aware gap, and every tier clears the one inside it. **One rule fixes every case; a nudge fixes one screenshot.** **(2) IT MUST BE OUTSIDE THE FOOTPRINT, NOT MERELY OUTSIDE THE WALL.** On an L-shaped or notched plan, *'outside the nearest wall'* is still **INSIDE the building** - which is exactly what his screenshot shows. The reference is the **footprint bbox per axis**. **(3) L-268's `partitionBuildings()` ALREADY GIVES YOU THE FOOTPRINTS** - use it; two buildings means two independent tier stacks, each clear of its OWN plate. **(4) NO LITERALS** - the tier gap resolves from the view's drawing scale (C24), never a hardcoded millimetre value. **(5) GUARD IT ON AN L-SHAPED PLATE** (his exact case - **a rectangle would pass by luck**): NO dimension line intersects the footprint polygon, and the OVERALL is strictly OUTSIDE every other chain on its axis. §FIX-OVERALL-DIM-OUTSIDE-AND-OUTERMOST. |
| L-282 | founder (2026-07-14, with a normative spec + 3 screenshots) | **[CRITICAL - ARCHITECTURAL] HOST RELATIONSHIPS PROPAGATE VISIBILITY STATE: a hosted DOOR touching the depth/cut plane promotes ITS ENTIRE HOST WALL from PROJECTION to CUT.** Founder: *"The renderer is incorrectly classifying an entire host wall as CUT simply because a hosted door begins intersecting the view depth / clipping plane. This is not how Revit works. A hosted family MUST NEVER change the visibility classification of its host wall. Each object's geometry must be classified INDEPENDENTLY."* **HIS SCREENSHOTS SHOW IT EXACTLY:** as the depth plane reaches a door, the door starts rendering CUT (which may be correct) **and the whole wall flips to cut graphics** - cut edges, a visible cut profile, the opening exposed as though the wall itself had been sectioned. **The red-arrow wall should still be PROJECTION.** **THE RULE HE IS STATING, AND IT IS NOT NEGOTIABLE:** *classification is performed **PER GEOMETRY**, not per element hierarchy.* `wallClass = classify(wallSolid)` and `doorClass = classify(doorSolid)` are **completely independent**. `Wall -> Door` does **NOT** mean `Door == Cut => Wall == Cut`. **No logic of the form `if (door.isCut()) wall.setCut(true)` - or any equivalent - may exist.** Host/hosted, parent/child, assembly members and family instances must **NEVER** inherit or propagate visibility state. **The ONLY determinant of CUT vs PROJECTION is whether THAT SPECIFIC SOLID intersects the cut plane.** **AND THE COROLLARY HE SPELLS OUT, WHICH IS THE HALF THAT WILL BE MISSED:** *the opening created by a hosted door appears as a CUT opening **only if the WALL ITSELF is intersected**.* If the wall stays PROJECTION: **do NOT** draw cut edges around the opening, **do NOT** draw cut faces, **do NOT** switch the wall to cut graphics, **do NOT** expose opening edges that exist only in the cut representation. The wall renders exactly like any other projection wall. **WHY THIS IS ARCHITECTURAL, NOT A BUG:** **L-277 (shipped today) built the four-zone model** - `DrawingZone = cut | projection | beyond | hidden` - and made every segment carry one. **This ticket says the CLASSIFIER is being applied at the wrong GRANULARITY: to the ELEMENT (or the host tree), not to the SOLID.** The zones are right; the thing being classified is wrong. **This is the third granularity bug in the drawing layer today** (L-275: the wall's ISO layer was resolved from a case-sensitive TYPE NAME, not from the element; L-266: the door's mesh edges were emitted alongside its own symbol) - and all three have the same shape: **a decision taken at the wrong level of the hierarchy.** | Drawing (**C09 §4.6** - completes L-277's zone model with its GRANULARITY rule; **C15** hosted elements - a host relationship is a MODEL fact, never a DRAWING fact; **Contract-23** pen table; **ADR-0265**; `EdgeProjectorService`, `classifyByVertexY`, `classifyByProjectionDepth`, `HiddenLineRemoval`) | **SHIPPED 7666b9c5 (verification-sweep 2026-07-16, guard test green) - FIX-PER-SOLID-ZONE-CLASSIFICATION; perSolidZoneClassification 11/11 green. NEEDS LIVE VERIFY for any pixel/interaction placement. Was OPEN (CRITICAL).** **Breaks every elevation and section that contains a door or a window** - i.e. all of them. | route -> **drawing agent** (owns the zone model it just built). **(1) FIND THE PROPAGATION AND DELETE IT.** Grep for anything that lifts a host's classification from a hosted child, or that classifies a GROUP/element rather than a MESH: a `Group` whose classification is derived from any child; a wall's `:cut` flag set because an opening/door mesh was cut; a shared `userData` cut flag on a parent. **The founder has named the anti-pattern precisely - `if (door.isCut()) wall.setCut(true)` - so look for its real-world shapes, not that literal line.** **(2) CLASSIFY PER RENDERABLE SOLID.** `for each renderableSolid: zone = classify(solid, view)`. A door-hosting wall is segmented into `WallPart`/`WallLayer` meshes (established in L-266) - **each of those is its own solid and must be classified on its own geometry.** **(3) THE OPENING IS NOT A CUT UNLESS THE WALL IS CUT.** The opening's edges belong to the wall's CUT representation; if the wall is PROJECTION they must not appear at all. **This is the half most likely to be missed - assert it explicitly.** **(4) THE MODEL HIERARCHY STAYS - IT IS THE DRAWING THAT MUST STOP READING IT.** C15's host relationship is real and load-bearing for geometry (an opening IS a void in a wall). **What must not happen is the DRAWING layer inferring a zone from it.** Say that in C09 in as many words. **(5) GUARD IT (and this is the whole ticket):** a door whose solid intersects the plane while its host wall's solid does NOT -> **door = CUT, wall = PROJECTION**, and the wall emits **no cut edges, no cut faces and no opening edges**. Then the converse: push the plane INTO the wall -> **both** CUT. **A test that only checks the second case passes today.** §FIX-PER-SOLID-ZONE-CLASSIFICATION. |
| L-283 | founder (2026-07-14, with a marked South Elevation) | **[HIGH] AUTO-DIMENSION ON ELEVATION WORKS - BUT IT IS VERTICAL-ONLY. EVERY HORIZONTAL DIMENSION IS MISSING.** Founder: *"The autodim elevation is working, but we are missing ALL the horizontal dims."* His screenshot shows the vertical chains landing correctly (sill **1000 mm**, opening height **1200 mm**, floor-to-floor **2800 mm**) and **not one horizontal dimension anywhere** - no window widths, no positions along the façade, no overall façade length. His arrows point along the horizontal axis, twice. **THIS IS NOT A BUG - IT IS THE OTHER HALF OF L-263, AND IT WAS NEVER BUILT.** When elevation auto-dimension shipped, the rule set that was written was the genuinely NEW one: the VERTICAL measurements an elevation needs and a plan does not (floor-to-floor, sill, head, parapet/eaves/ridge, level datums). **The horizontal axis was left implicit - and 'implicit' means absent.** An elevation dimension set is TWO chains, not one: **VERTICAL** (heights, which we have) **and HORIZONTAL** (widths and positions along the façade, which we do not). **WHAT THE HORIZONTAL CHAIN MUST CONTAIN** (and note this is NOT the plan chain rotated - it measures the FAÇADE PLANE): opening **widths**; the **spacing between openings** along the façade; opening positions from the **façade ends / corners**; and the **overall façade length** as the outermost tier. | Documentation / annotations (**C24.1**; **C24** - the tier/offset rules of **L-281** apply to BOTH axes; **C03/C16**; `packages/auto-dimension/src/elevation/`, `applyElevationAutoDimensions.ts`; **completes L-263**; siblings **L-268 / L-281**) | **SHIPPED 33ae227d (verification-sweep 2026-07-16, guard test green) - FIX-ELEVATION-HORIZONTAL-CHAIN; auto-dimension green. NEEDS LIVE VERIFY for any pixel/interaction placement. Was OPEN (HIGH).** **An elevation with only vertical dimensions is not a dimensioned elevation** - you cannot build from it. | route -> **annotations / dimensions agent** (owns `packages/auto-dimension`, and wrote the elevation strategy). **(1) THE HORIZONTAL CHAIN IS A SECOND RULE SET ON THE SAME ENGINE - do NOT fork the elevation strategy.** The engine, the commands and `commitAnnotationSet` are shared; what differs is the MEASUREMENT AXIS. **(2) IT IS NOT THE PLAN CHAIN ROTATED.** A plan chain measures wall runs in the XZ plane; the elevation horizontal chain measures **along the FAÇADE PLANE** - opening widths, inter-opening spacing, distance to the façade ends, and the overall façade length. Write the rule set down BEFORE coding it, exactly as the vertical one was. **(3) `viewPlane.isVertical` ALREADY tells you the plane; the HORIZONTAL axis WITHIN that plane is the façade's own U axis** - reuse the projection basis the elevation projector already computes. **Do not invent a second notion of 'along'.** **(4) L-281's TIER RULE APPLIES TO BOTH AXES:** openings innermost, then the exterior chain, then the **OVERALL façade length OUTERMOST** - and all of it **OUTSIDE the elevation's silhouette**, never across it. Land L-281 first and this inherits it for free. **(5) GUARD IT:** an elevation containing two windows yields a horizontal chain with **both widths, the gap between them, both end distances, and one overall** - and no horizontal dimension crosses the building silhouette. §FEAT-ELEVATION-HORIZONTAL-DIM-CHAIN. |
| L-284 | founder (2026-07-14, zoomed plan door symbol) | **[MEDIUM] THE DOOR PLAN SYMBOL: THE HANDLE IS WRONG, AND THE LEAF IS NOT ALIGNED WITH THE WALL FACE.** Founder: *"Please make the door HANDLE correct - also make the door LEAF aligned with the SLAB LINE."* His zoom shows **(a)** the hardware drawn as a **cross / plus-sign crossbar** projecting off both sides of the leaf - it reads as a T-bar, not a lever; **(b)** the open leaf's hinge end **not sitting on the wall face line** - the leaf floats off the reveal instead of pivoting flush with it. **BOTH ARE DIMENSIONAL-TRUTH DEFECTS (L-127), NOT STYLING:** a lever handle is an **offset L** on ONE face of the leaf at the latch end (its projection resolved from the record), not a symmetric crossbar; and an open leaf **PIVOTS ON THE HINGE, WHICH LIES ON THE JAMB AT THE WALL FACE** - so the leaf's hinge end must be COINCIDENT with the wall-face line (the founder's *"slab line"*), not offset from it. **If the leaf does not touch the wall face, the swing arc it sweeps is geometrically wrong, and so is every clearance read off the drawing.** **CONTEXT - THE SYMBOL WAS JUST REBUILT:** §FIX-DOOR-PLAN-SYMBOL-PURITY (L-266) stripped the imaginary chords and rebuilt the symbol as frame + leaf + arc (+ ghost/hardware at LOD-300), deriving the lever from the type's leaf thickness. **These two defects are in that new geometry** - so this is a correction to fresh work, not a legacy fix. | Hosted elements (**C15**; **L-127 dimensional truth**; **Contract-23** pen table; `DoorPlanSymbolBuilder`, `resolveDoorDimensions`; **continues L-266**) | **SHIPPED f972f0b7 (verification-sweep 2026-07-16, guard test green) - door plan-symbol purity + handle/leaf alignment; geometry-door green. NEEDS LIVE VERIFY for any pixel/interaction placement. Was OPEN (MEDIUM).** | route -> **hosted-element records agent** (owns `packages/geometry-door`; take after L-274/L-280). **(1) THE HANDLE IS A LEVER, NOT A CROSSBAR.** An offset **L** on ONE face at the LATCH end (opposite the hinge): a short escutcheon plate on the leaf face plus a lever projecting perpendicular. It must NOT be symmetric about the leaf, and it must NOT project from both faces. Its projection and plate size resolve from the record (leaf thickness / type), **never a literal**. **(2) THE LEAF PIVOTS AT THE WALL FACE.** The hinge point is on the JAMB, ON the wall-face line. Assert the leaf's hinge end is COINCIDENT with the wall face - it currently floats. **The arc's centre IS the hinge; if the leaf is offset, the arc is a lie.** **(3) DERIVE THE HINGE FROM THE OPENING, NOT FROM THE LEAF.** The opening's void edges (which L-246's true cut section gives you exactly) define the jamb; the hinge sits at the correct void edge for the door's hand. **(4) GUARD IT:** the leaf's hinge end is coincident with the wall face within tolerance; the arc's centre EQUALS the hinge point; the handle is on ONE face only and at the LATCH end; no literal dimension survives in the builder. §FIX-DOOR-SYMBOL-HANDLE-AND-LEAF-ALIGNMENT. |
| L-285 | founder (2026-07-14) | **[HIGH] INTERIOR WALLS MUST DRAW SLIGHTLY LIGHTER THAN EXTERIOR/PERIMETER WALLS - IN PLAN, AND IN ELEVATION/SECTION TOO.** Founder: *"Please make interior wall thickness lines slightly less thick than the perimeter exterior wall thickness lines in plan view - but also in elevation etc."* Today **every wall is drawn with the same pen**, so a 100 mm partition and a 300 mm external shell read as equally important, and the plan loses its hierarchy: **the eye can no longer find the building's envelope.** **THE ARCHITECTURAL POINT, AND IT IS THE WHOLE TICKET: THE WEIGHT MUST COME FROM THE WALL'S *FUNCTION*, NOT ITS *THICKNESS*.** It is tempting to key the pen off `wall.thickness` - and it is **wrong**: a 300 mm acoustic interior wall would then draw as heavy as the shell, and a thin exterior infill panel would vanish. **The fact we need is already in the record** - the founder's own property panel shows it: **`Interior - Partition 100mm`**. The wall's **systemType carries its FUNCTION** (exterior / interior / partition / structural). **Resolve the pen from THAT.** **WHAT THIS ACTUALLY IS: A NEW AXIS IN THE PEN TABLE.** Today the pen resolves as `pen = f(ZONE, CATEGORY)` - L-277 shipped that this morning (CUT heaviest > PROJECTION > BEYOND; HIDDEN dashed). He is asking for `pen = f(ZONE, CATEGORY, **FUNCTION**)`. **That is not a hack - it is exactly what ISO 13567 sub-categorisation and Revit's subcategory line weights are for, and it is the standard way a drawing expresses hierarchy.** The zone ladder is UNCHANGED and must stay strict: **an interior CUT wall is still heavier than an exterior PROJECTION wall** - function MODULATES within a zone; it never re-orders the zones. **Getting that precedence backwards would undo L-277.** | Drawing (**Contract-23** pen table - this adds the FUNCTION axis; **C09 §4.6** zone model from L-277 - the ladder is unchanged; **C09/P7** - the weights are VIEW INTENT and must be overridable, never literals; **C15**; ISO 13567; `PenWeightTable`, `resolvePen`, the graphics-rules engine) | **SHIPPED 94e4ab8f (verification-sweep 2026-07-16, guard test green) - FEAT-PEN-WEIGHT-BY-WALL-FUNCTION; PenWeightByFunction 15/15 green. NEEDS LIVE VERIFY for any pixel/interaction placement. Was OPEN (HIGH).** **This is what makes a plan read as a building rather than a wireframe** - and it applies to plan, elevation AND section, so it must be resolved in the SHARED pen chain, not in a plan-only branch (the plan-first disease has been diagnosed FIVE times: L-262/263/264/265/268). | route -> **drawing agent** (owns the pen table + the zone model it just built; take after L-282). **(1) THE FACT IS ALREADY IN THE MODEL - FIND IT, DO NOT ADD IT.** The wall's systemType carries its function (his panel: *Interior - Partition 100mm*). Read it. **Do NOT add a new field, and do NOT infer function from thickness** - a 300 mm acoustic partition is still INTERIOR. **(2) ADD FUNCTION AS AN AXIS OF THE PEN TABLE, NOT AN IF-STATEMENT IN A BUILDER.** `pen = f(zone, category, function)`. A literal weight typed into a symbol builder is the bug this codebase has been paying for all day. **(3) THE ZONE LADDER IS SACRED AND MUST NOT BE RE-ORDERED:** CUT > PROJECTION > BEYOND remains strict; FUNCTION modulates the weight **WITHIN** a zone (an interior CUT wall is a touch lighter than an exterior CUT wall, but still heavier than ANY projection line). **Assert both facts - the modulation AND the ladder - or this fix silently undoes L-277.** **(4) IT IS INTENT (P7):** the exterior/interior weight ratio is a property of the VIEW/template, overridable, never hardcoded. **(5) ONE CHAIN, THREE VIEW TYPES:** plan, elevation and section resolve through the SAME pen chain. The founder said *"but also in elevation etc."* - **a plan-only fix is the disease.** **(6) GUARD IT:** exterior CUT > interior CUT > ANY projection > ANY beyond, all resolved from intent; a THICK interior wall still draws lighter than a THIN exterior wall (**the test that catches the thickness shortcut**); and no literal weight survives in any builder. §FEAT-PEN-WEIGHT-BY-WALL-FUNCTION. |
| L-286 | founder (2026-07-14) - **HEADLINE FEATURE, DEMO-DRIVING** | **[FEATURE - STRATEGIC] 'SET OUT' VIEW INTENT: LIVE, SELF-MAINTAINING DOCUMENTATION. Tags and dimensions that stay correct FOREVER, with no regenerate command, ever.** Founder: *"A Set Out view is a self-maintaining documentation view. Whenever the model changes, the documentation updates automatically... The user should never think about regenerating tags or dimensions. This should become one of PRYZM's DEFINING FEATURES."* Add a door -> it is tagged and dimensioned. Delete a window -> its tag vanishes and the chain re-closes. Change the elevation depth or the crop -> newly visible elements gain annotation, departing ones lose it. Move a wall -> dimensions follow. **No manual 'Update Documentation' command may ever exist.** **THE MOST IMPORTANT THING IN THIS TICKET: THIS IS NOT A NEW ENGINE. IT IS A BINDING OF THREE THINGS PRYZM ALREADY HAS - and whoever starts by writing a 'live documentation engine' from scratch has misread it.** **(1) CHANGE DETECTION ALREADY EXISTS AND ALREADY WORKS.** `ViewDependencyTracker` already watches the `StoreEventBus`, already computes WHICH VIEWS a model change dirties, and already flushes them (his own logs, all day: `[ViewDependencyTracker] flush - 1 active dirty view(s): vd-sys-p (+4 deferred inactive)`). **That is the `detectChange()` step he is asking for. It is shipped.** **(2) THE IDEMPOTENT RECONCILER PATTERN ALREADY EXISTS.** `RoomTagAutoPopulator` already reconciles a view's ROOM tags to the model - **create the missing, refresh the stale, remove duplicates, remove orphans** (his log: `8 room-tag(s) created, 0 refreshed, 0 duplicate(s) removed, 3 orphan(s) removed`). **That IS the synchronisation rule he writes out ('exactly one tag per visible element, no duplicates, no orphans') - already solved, for one element type.** **L-265 (in flight NOW) is generalising exactly that reconciler to doors/windows/walls, plan AND elevation.** **(3) THE COMMIT CHOKEPOINT ALREADY EXISTS** - `commitAnnotationSet()` commits a reconciliation (creations AND deletions) as ONE undoable unit. **SO 'SET OUT' IS: A VIEW INTENT THAT (a) TURNS THE RIGHT ANNOTATION CATEGORIES ON, AND (b) SUBSCRIBES THE RECONCILERS TO THE DEPENDENCY TRACKER'S FLUSH.** The 'live documentation engine' he describes is `ViewDependencyTracker` + reconcilers + one commit. **That is the whole feature, and it is why it is achievable rather than fantastical.** **THE ONE DECISION HE HAS NOT MADE, AND IT MUST NOT BE GUESSED - IT DECIDES WHETHER THE FEATURE IS USABLE OR UNBEARABLE: WHAT DOES CTRL-Z DO?** If every model edit spawns an annotation reconcile as its OWN undo entry, then **moving one wall costs the user TWO undos**, and Ctrl-Z becomes a lottery. **The reconcile must EITHER (i) ride the SAME undo entry as the edit that caused it** (annotation patches folded into the user's gesture - one gesture, one undo, documentation included), **OR (ii) be excluded from undo entirely (`suppressUndo`) and simply RE-RECONCILED after any undo** (the drawing is a pure function of the model, so undoing the model implies re-deriving the drawing). **(ii) is architecturally cleaner and matches the founder's mental model - 'the documentation simply stays in sync' - but it is a REAL decision with a REAL contract consequence (C03 §4.5-4.8, C16), and it must be made deliberately and written into a contract, not discovered in a bug report.** | Documentation / views (**C09/P7 - a view INTENT is exactly the right home for this, and it vindicates the intent architecture**; **C24.1** auto-documentation sets; **C03 §4.5-4.8 + C16** undo semantics - THE open decision; **C28** schedules - a live-tagged model is a live SCHEDULE; `ViewDependencyTracker`, `RoomTagAutoPopulator`, `commitAnnotationSet`, `packages/auto-dimension`; **BUILDS ON L-265 / L-263 / L-281 / L-283**) | **OPEN - STRATEGIC. Founder flags it for a DEMO. It DEPENDS on L-265 (the generalised tag reconciler, in flight) and is far stronger once L-281/L-283 land (dimensions outside + the elevation horizontal chain).** **Honest sequencing: the reconciler is the foundation; Set Out is the intent that makes it LIVE. Building the intent on a half-generalised reconciler would produce a view that confidently maintains the WRONG drawing.** | route -> **annotations / dimensions agent** (owns the reconciler, the dimension engine and the commit chokepoint - this is the natural continuation of L-265). **(1) DO NOT WRITE A NEW ENGINE.** Bind `ViewDependencyTracker` (change detection, shipped) to the generalised reconciler (L-265) through `commitAnnotationSet` (shipped). **If you find yourself writing a change-detection loop, STOP - it exists, and a second one would drift from the first.** **(2) 'SET OUT' IS A VIEW INTENT (P7/C09), NOT A MODE FLAG.** It declares WHICH categories are documented (doors, windows, walls, optionally rooms) and that the view is LIVE. It is carried on the view/template like every other intent, and it must work in PLAN **and** ELEVATION from day one (`viewPlane.isVertical` already discriminates) - **a plan-only Set Out would be the SIXTH instance of the plan-first disease.** **(3) IDEMPOTENCE IS THE WHOLE CORRECTNESS ARGUMENT.** A reconcile must be safe to run at any time, any number of times, and converge to 'exactly one tag per visible element, no duplicates, no orphans'. **If the reconciler is idempotent, 'live' is just 'run it on every flush' - the hard problem collapses.** `RoomTagAutoPopulator` already proves the pattern works. **(4) INCREMENTAL IS AN OPTIMISATION, NOT THE DESIGN.** He asks for a dependency graph - and he is right that a full regenerate per keystroke will not scale. **But get it CORRECT (idempotent, whole-view reconcile, scoped to the changed levels) BEFORE getting it INCREMENTAL.** `ViewDependencyTracker` already tells you which views are dirty, which is most of the win. Measure before optimising. **(5) VISIBILITY IS THE INPUT, NOT THE MODEL.** His Examples 3 and 4 (elevation depth, crop) are the ones that will break a naive implementation: the reconciler must operate on **what the view SHOWS** (post-crop, post-depth, post-visibility-intent), NOT on what the LEVEL contains. **Reconcile against the projected drawing's element set, not the store's.** **(6) ANSWER THE UNDO QUESTION FIRST, IN AN ADR, BEFORE WRITING THE BINDING.** See above - it is the difference between a beloved feature and an unusable one. **(7) GUARD IT:** add a door -> exactly one new tag, no duplicates; delete it -> tag and dimension gone, no orphans; change the crop -> annotation set matches the VISIBLE set exactly; run the reconcile twice -> nothing changes the second time (idempotence); and ONE user gesture remains ONE undo entry. §FEAT-SET-OUT-LIVE-DOCUMENTATION-INTENT. |
| L-287 | founder (2026-07-14) - **PREREQUISITE FOR L-286** | **[CRITICAL - ARCHITECTURAL] A DIMENSION MUST BE *ASSOCIATIVE*: it may be MOVED, but its WITNESS LINES MUST STAY BOUND TO THE ELEMENTS THEY MEASURE.** Founder: *"Dimensions can now be selected and moved - this is brilliant - BUT remember they should always [be bound] to their reference planes. They can be moved forward and backward, the dim NUMBER can be moved also, but the REFERENCE LINES SHOULD ALWAYS BE BOUNDED TO THE ELEMENTS THAT THEY REFERENCE."* **HE IS DRAWING THE ONE LINE THAT SEPARATES A CAD DRAWING FROM A BIM MODEL, AND IT IS THE MOST IMPORTANT SENTENCE HE HAS WRITTEN ABOUT ANNOTATION.** A dimension has **TWO KINDS OF STATE, AND THEY MUST NEVER BE CONFUSED**: **(1) WHAT IT MEASURES - IMMUTABLE BY DRAG.** The **REFERENCES**: which elements, and which face/edge/plane of each. **The user may NEVER move these with the mouse.** They are a QUERY against the model, re-evaluated whenever the model changes. **(2) HOW IT IS PRESENTED - FREELY MOVABLE.** The dimension line's **OFFSET** from the geometry (his *"forward and backward"*), and the **TEXT's** position along/off the line. **Dragging a dimension edits (2). IT MUST NEVER EDIT (1).** **THE FAILURE MODE THIS PREVENTS, AND WHY IT IS CRITICAL:** if a drag BAKES the witness lines into absolute coordinates, the dimension **silently detaches from the model**. It will still LOOK right - until a wall moves, and then it measures **nothing**: a number floating over geometry it no longer describes. **A detached dimension is worse than a missing one, because it is CONFIDENTLY WRONG and a builder will build from it.** **AND IT IS THE FOUNDATION OF L-286 ('SET OUT' LIVE DOCUMENTATION).** His own Example 5 there - *"walls move -> dimensions move, chains remain valid, no broken references"* - is **IMPOSSIBLE unless the dimension stores REFERENCES rather than POINTS.** **You cannot make documentation 'live' on top of dimensions that have been baked into coordinates.** **L-287 IS THEREFORE A HARD PREREQUISITE FOR L-286, NOT A SIBLING OF IT** - and if the current record stores baked points, that must be found NOW, before the live engine is built on sand. | Annotations (**C03** - the RECORD is the whole ticket: references, not points; **C16**; **ADR-0266** - the DRIVE-vs-OVERRIDE decision, same element; **C24.1**; **L-127** dimensional truth - the VALUE is derived from the model, never stored; **L-256** selectability, which just shipped and enabled this; **HARD PREREQUISITE OF L-286**) | **SHIPPED e3522a6b (2026-07-16) - extracted computeLinearDimGeometry (pure): witness feet refA/refB derive ONLY from reference points, offset reaches ONLY the dim line, so a dim move structurally cannot detach the witness lines; feet re-derive from live model via the dependency graph. 6/6 guard tests, tsc clean. NEEDS LIVE VERIFY: move a wall -> feet follow; move the dim -> feet stay. Was OPEN (CRITICAL). Selectability just landed (L-256) - so the drag path is NEW, and this is the moment to get its semantics right, BEFORE users start dragging dimensions off their references and saving those projects.** | route -> **annotations / dimensions agent**. **(1) FIRST, ESTABLISH THE TRUTH ABOUT THE RECORD - THIS IS THE WHOLE TICKET.** Does the dimension store **REFERENCES** (elementId + which face/edge/plane) or does it store **BAKED POINTS**? **REPORT THAT ANSWER BEFORE WRITING ANY CODE.** If it stores points, the fix is a RECORD change and everything else follows from it. **(2) SPLIT THE RECORD EXPLICITLY INTO 'WHAT IT MEASURES' AND 'HOW IT IS SHOWN'.** References + a presentation block (line offset, text offset). **Drag mutates ONLY the presentation block.** Make it structurally impossible for a drag to touch a reference - do not rely on a code comment to enforce it. **(3) THE VALUE IS DERIVED, NEVER STORED (L-127).** A dimension's number is `measure(refA, refB)` evaluated against the LIVE model. **The moment you cache it, it can lie.** **(4) THE WITNESS LINES ARE DERIVED TOO** - they run from the referenced geometry to the dimension line, so they FOLLOW the element for free once (1) and (2) are separate. **This is the property that makes L-286 possible at all.** **(5) A BROKEN REFERENCE MUST SURFACE, NEVER SILENTLY VANISH (P8).** If a referenced element is deleted, the dimension is INVALID - it must be removed (that is the auto-doc reconcile path, L-265/L-286) or flagged. **It must never quietly keep drawing its last-known number.** **(6) ALIGN WITH ADR-0266** (does an editable dimension DRIVE the geometry or only OVERRIDE the displayed text) - **the two decisions live on the same record and must not contradict each other.** **(7) GUARD IT:** drag a dimension -> its references are BYTE-IDENTICAL, only the offset changed · MOVE THE WALL -> the dimension's value and witness lines FOLLOW (the test that proves associativity) · delete a referenced element -> the dimension does NOT survive with a stale number · the value is never read from a cache. §FEAT-ASSOCIATIVE-DIMENSION-REFERENCES. |
| L-288 | **found by the drawing agent while PROVING L-285, and ASSERTED as a test rather than hidden** | **[HIGH] AT `devicePixelRatio = 1` THE CANVAS FLOORS EVERY STROKE AT A 1-PIXEL HAIRLINE - SO THE ENTIRE PROJECTION HIERARCHY IS FLAT ON A STANDARD DISPLAY.** Every pen below **~0.265 mm** clamps to exactly 1 px: **wall PROJECTION (0.25 mm), door PROJECTION (0.18 mm) and ceiling PROJECTION (0.13 mm) all render identically.** **THIS PRE-DATES THE FUNCTION AXIS AND SWALLOWS THE WHOLE LADDER.** L-277 built the zone hierarchy (CUT > PROJECTION > BEYOND); L-285 added the FUNCTION axis - and **on a 1x monitor a user sees NONE of the projection-side distinctions**, because they all sit beneath the hairline floor. It is **visible at dpr >= 2** and **CORRECT IN EXPORT** (at `EXPORT_DPI` those pens are 2.95 px vs 2.07 px - the hierarchy is real on paper). **So the drawing is RIGHT and the SCREEN is LYING** - which is the worst place for it to lie, because the screen is where the founder judges it. **THE AGENT ASSERTED IT AS A TEST RATHER THAN HIDING IT BEHIND THE dpr-2 CASE** - so it is a MEASURED FACT in the suite, not a future *"your feature does nothing on my laptop"* bug report. | Drawing (**Contract-23** - a weight the display cannot render is a weight the user does not have; **C09 §4.6.4** the zone ladder; **C04**; same class as **L-241**; the `PlanViewCanvas` hairline floor) | **SHIPPED 11b144ce (verification-sweep 2026-07-16, guard test green) - FIX-PLAN-CANVAS-HAIRLINE-FLOOR + CanvasRenderScale; CanvasHairlineFloor 9/9 green. NEEDS LIVE VERIFY for any pixel/interaction placement. Was OPEN (HIGH).** **It silently flattens L-277 AND L-285 for anyone on a 1x display** - most laptops, and most projectors. **A demo on a 1x projector would show none of the line hierarchy we have just spent a day building.** | route -> **drawing agent** (owns the canvas + pen chain). **(1) THE PENS ARE CORRECT - THE RASTERISER IS THE PROBLEM. Do NOT 'fix' this by inflating the pen table**; that would corrupt the export, where the hierarchy is already right. **(2) The honest options, to be WEIGHED not guessed:** **(a)** render the plan canvas at a **backing-store scale >= 2** regardless of `devicePixelRatio` (costs fill-rate, buys the whole hierarchy); **(b)** a **display-only weight expansion** - a monotonic paper-mm -> device-px map that PRESERVES ORDERING above the hairline floor, applied at the stroke and NOWHERE else, so export is untouched; **(c)** accept and document it (**the weakest - it means the product's line hierarchy is invisible to most users**). **(3) THE INVARIANT, WHATEVER YOU CHOOSE: STRICT ORDERING MUST SURVIVE ON SCREEN AT dpr 1** - CUT > PROJECTION > BEYOND, and exterior > interior within a zone, VISIBLY distinct at 1x. **Assert it in DEVICE PIXELS, not in millimetres.** **(4) The export path must be byte-identical afterwards.** §FIX-HAIRLINE-FLOOR-FLATTENS-PEN-HIERARCHY. |
| L-289 | **the REAL cause of the founder's "fat window frame" - SPECIFIED by the drawing agent, NOT YET APPLIED** | **[HIGH] THE WINDOW PLAN SYMBOL BRIDGES ITS FRAME'S WALL-FACE CUT LINES ACROSS THE FULL OPENING WIDTH - so the window reads as a SOLID SLAB of full wall thickness instead of `frame | glazing | frame`.** This is his *"the frame in plan view is much thicker than in reality"* - and it is **NOT a pen problem and NOT a geometry problem.** **BOTH EARLIER DIAGNOSES WERE WRONG, AND THAT IS THE INTERESTING PART.** The records agent **measured the band as dimensionally exact** (0.050 m band == 0.050 m record `frameThickness`) - **correct, and no geometry was changed.** It then proposed a PEN cause (`A-GLAZ-CUT` and `A-GLAZ-PROJ` getting the same weight) - and the drawing agent **REFUTED THAT TOO**: `symbolicRuleForLayer()` returns `null` for any `:cut` layer, so **the window's cut frame never enters the symbolic path at all** and gets the full 0.35 mm CUT pen. **The cut/projection hierarchy for windows was reaching the screen all along.** (Pinned as a test so it cannot be re-asserted from memory.) **THE ACTUAL CAUSE IS A DRAWING CONVENTION:** `WindowPlanSymbolBuilder` closes the symbol into a **rectangle** by running the frame's wall-face CUT lines **ACROSS THE GLAZING**. **A heavy CUT line drawn along the wall face across the glass ASSERTS A SOLID THAT IS NOT THERE.** And **the bridge buys nothing** - the jamb seam is already sealed by the host wall's own void-edge clip. | Drawing / hosted elements (**C09 §4.6.4c - the exact edit is ALREADY SPECIFIED in the contract**; **C15**; **L-127**; `WindowPlanSymbolBuilder`; the real half of **L-280**) | **SHIPPED c7dbeb3d (verification-sweep 2026-07-16, guard test green) - FIX-WINDOW-SYMBOL-FRAME-BRIDGE; geometry-window green. NEEDS LIVE VERIFY for any pixel/interaction placement. Was OPEN (HIGH) - SPECIFIED BUT NOT APPLIED.** The drawing agent wrote the rule into C09 §4.6.4c and **deliberately did not apply it, because `geometry-window` is another agent's fence.** **That restraint was correct** - it is exactly the shared-checkout discipline that stopped agents destroying each other's work this week. The edit now needs its owner. | route -> **hosted-element records agent** (owns `packages/geometry-window`). **(1) READ C09 §4.6.4c FIRST - THE EDIT IS ALREADY SPECIFIED. You are APPLYING a decision, not making one.** **(2) A window in plan reads `frame | glazing | frame`.** Do NOT draw a CUT line along the wall face across the glazing: **there is no solid there.** **(3) THE BRIDGE IS NOT LOAD-BEARING** - the jamb seam is already sealed by the host wall's void-edge clip (L-246's true cut section is void at the opening BY CONSTRUCTION). Removing it costs nothing. **(4) GUARD IT:** no CUT-zone segment spans the glazing; the jamb seam REMAINS CLOSED (assert it - do not assume the clip still holds); and the frame band still equals `resolveWindowDimensions().frameThickness`. **DO NOT 'FIX' THE WIDTH WHILE YOU ARE IN THERE. IT WAS NEVER WRONG.** §FIX-WINDOW-SYMBOL-FRAME-BRIDGE. |
| L-290 | founder (2026-07-14, DECIDED via explicit question) | **[HIGH] IN ELEVATION AND SECTION, THE *BEYOND* ZONE MUST DRAW **DASHED** BY DEFAULT. In PLAN it stays SOLID.** Founder: *"Beyond line in elevations and section - should be dash."* **He was then asked explicitly, because his own normative spec says the opposite, and he chose: dashed by default in ELEVATION/SECTION ONLY; PLAN keeps BEYOND solid + lighter.** **THIS IS NOT A CONTRADICTION OF L-277 - IT IS THE OVERRIDE CLAUSE HIS OWN SPEC ALREADY CONTAINED.** His words: *"BEYOND ... never dashed **(unless explicitly overridden by the user)**"*. **The bug L-277 fixed was DASHING BY DISTANCE** - far-but-VISIBLE geometry drawn dashed because occlusion and depth shared one bucket. **That stays fixed.** This is a deliberate DRAFTING CONVENTION applied per VIEW TYPE, expressed as **DATA (intent), not a code branch.** **AND HERE IS THE COLLISION THAT MAKES THIS TICKET NON-TRIVIAL - IT WAS ALREADY MEASURED BY THE L-285 AGENT:** **`BEYOND` AND `HIDDEN` SHARE A BASE WIDTH (0.09 mm). THEY CURRENTLY DIFFER BY *DASH*, NOT BY *WEIGHT*.** So the moment BEYOND dashes in elevation/section, **BEYOND AND HIDDEN BECOME INDISTINGUISHABLE IN EXACTLY THE VIEWS HE ASKED FOR** - a stair's lower run would read identically to a pipe behind a wall. **That is the whole engineering content of this ticket, and a naive 'set dash=true on beyond' would ship it.** | Drawing (**C09 §4.6.4** - the dash rule, which this AMENDS with a per-view-type default; **Contract-23** pen table - the two zones must now differ by WEIGHT and/or DASH PATTERN, not by dash alone; **P7** - it is INTENT, per view type, overridable per view; **L-277**, **L-285**, **L-279**) | **SHIPPED 34952c07 (verification-sweep 2026-07-16, guard test green) - FEAT-BEYOND-DASH-IN-ELEVATION - dashed in elev/section, stays SOLID in plan, separated from HIDDEN. NEEDS LIVE VERIFY for any pixel/interaction placement. Was OPEN (HIGH).** **Do not ship the dash without separating BEYOND from HIDDEN**, or the drawing gains a dash and loses a distinction. | route -> **drawing agent** (owns the pen table, the zone model and C09 - it MEASURED the shared base width, so it already knows the trap). **(1) SEPARATE BEYOND FROM HIDDEN *BEFORE* YOU DASH BEYOND.** They must be distinguishable at a glance in a view where BOTH dash. The honest axes: **DASH PATTERN** (a long-dash / chain for BEYOND vs a fine short-dash for HIDDEN is the standard drafting distinction) **and/or WEIGHT** (his spec: BEYOND *"usually lighter than CUT"*; HIDDEN *"usually thin"* - so HIDDEN should be the LIGHTER of the two). **State which axis you chose and why.** **(2) IT IS A PER-VIEW-TYPE DEFAULT IN THE DATA, NOT AN `if (isElevation)` IN A BUILDER.** `ViewScope` already carries per-view-type defaults (that is how `occlusionDisposition` works, L-279) - follow that precedent exactly. **(3) IT MUST STAY OVERRIDABLE PER VIEW (P7)** - his own spec says *unless explicitly overridden by the user*, and L-279 already built the ViewDefinition override channel. **Reuse it; do not invent a second one.** **(4) THE LADDER IS STILL SACRED:** CUT > PROJECTION > BEYOND remains strict in WEIGHT. Dashing BEYOND changes its STYLE, never its rank. **(5) PLAN IS UNCHANGED** - BEYOND stays SOLID there (his stair lower run). **Assert that explicitly, or this fix silently re-opens the bug L-277 closed.** **(6) GUARD IT, AND MAKE IT GO RED FIRST:** in ELEVATION and SECTION, BEYOND is dashed AND is visibly distinct from HIDDEN (assert the distinguishing property, not just the dash) · in PLAN, BEYOND is SOLID · the weight ladder still holds in all three · and a per-view override still wins. §FEAT-BEYOND-DASH-IN-ELEVATION-AND-SECTION. |
| L-291 | founder (2026-07-14, tagged elevation screenshot) | **[HIGH] TAGS DO NOT SCALE WITH THE VIEW, AND THEY ARE NOT SELECTABLE / EDITABLE.** Founder: *"The tags are great - but can they scale down or up like the dimensions? Also the tags should be selectable and editable, like an annotation element."* **His screenshot shows it plainly: the tag bubbles are the size of a ROOM** - a *Timber Casement* bubble is wider than the window it names. **They are being sized in WORLD METRES, not in PAPER MILLIMETRES.** **PART A - SCALE. THIS IS THE C24 PAPER-SPACE RULE, AND THE DIMENSIONS ALREADY OBEY IT.** An annotation's SIZE is a property of the **PAPER**, not of the model: a tag bubble is *(say)* **8 mm across ON THE SHEET** at any scale, so at 1:50 it covers half the world-metres it covers at 1:100. **`packages/auto-dimension` already does exactly this** - L-281's tier gap is a **paper constant scaled by the view** (`tierGapWorldM`), with **no world literals**. **The tag renderer must use the SAME mechanism. It is not a new idea; it is an existing one that tags never got** - the one-consumer disease in yet another organ. **PART B - SELECTABLE + EDITABLE. THE MACHINERY EXISTS AND TAGS ARE ALREADY HALF-WIRED INTO IT.** L-256 made dimensions selectable; L-287 split every annotation's record into **WHAT IT MEASURES** (references - immutable by drag) and **HOW IT IS SHOWN** (presentation - freely movable), and **already implemented the tag case**: *dragging a tag moves the BUBBLE and leaves its leader anchor on the element*. **So the drag semantics are correct and shipped.** What is missing is the **PICK REPRESENTATION** and the **PROPERTY PANEL** - the same RECORD -> PICK -> PANEL chain, in the same mandatory order. | Annotations (**C24** sheet composition - **a tag is a PAPER-SPACE object**; **C24.1**; **C03/C16** - a tag IS an element and its edit is a COMMAND; **L-256** selectability; **L-287** the presentation/reference split, which already covers the tag drag; **L-265** the tag reconciler; `tierGapWorldM` is the precedent) | **SHIPPED f2d92a5b (verification-sweep 2026-07-16, guard test green) - FEAT-TAG-PAPER-SCALE-AND-SELECTABILITY; tagPaperScale green. NEEDS LIVE VERIFY for any pixel/interaction placement. Was OPEN (HIGH).** **A tag the size of a room is not a tag** - and an un-selectable tag cannot be moved when it overlaps, which on a real drawing it always will. | route -> **annotations / dimensions agent** (owns tags, the reconciler and the selectability chain it just built). **(1) PART A IS NOT A NEW MECHANISM - REUSE `tierGapWorldM`'s.** The bubble, the text height and the leader are **PAPER dimensions in mm**, converted to world by the VIEW's drawing scale (C24). **NO WORLD-METRE LITERAL MAY SURVIVE IN THE TAG RENDERER** - assert its absence, exactly as `windowCreationParity` P-4 forbids literals in the symbol builders. **(2) THE TEXT MUST SCALE WITH THE VIEW'S SCALE, NEVER WITH ZOOM** - a tag is legible on the SHEET; zooming the screen is not a scale change. **Get this wrong and the tag GROWS as you zoom out** - the classic bug. **(3) PART B: RECORD -> PICK -> PANEL, IN THAT ORDER** (the L-256 rule, non-negotiable - fixing the panel before the record is painting over it). **THE DRAG IS ALREADY DONE** (L-287) - **do not redo it, and do not let a drag reach the reference.** What is owed is a **PICK CORRIDOR** around the **BUBBLE** *and* its **LEADER** (a leader is a hairline - the same hit-tolerance problem dimensions had), plus a panel driven from the tag's real record. **(4) 'EDITABLE' MEANS THE MARK IS EDITABLE - AND THE MARK IS THE SCHEDULE JOIN (C28).** You proved the schedule joins on `element.mark`. **So editing a tag's text must EITHER edit the ELEMENT'S MARK (and therefore the schedule) OR be a pure display OVERRIDE - and those are DIFFERENT PRODUCTS.** **Decide it deliberately, in the ADR, alongside ADR-0266's DRIVE-vs-OVERRIDE decision for dimensions. The two must not contradict each other.** **(5) GUARD IT:** a tag's bubble is the same PAPER size at 1:50 and 1:100 (its world size differs; its paper size does not) - no world literal in the renderer - a tag is pickable by its bubble AND its leader - dragging a tag moves ONLY presentation, never the reference - the panel renders from the record. §FEAT-TAG-PAPER-SCALE-AND-SELECTABILITY. |
| L-292 | founder (2026-07-14) - **NEW ELEMENT FAMILY** | **[FEATURE] SWIMMING POOL - a new element category, defined by the user ON A SLAB.** Founder: *"Create a swimming pool element - a new category. The user will define it on a slab - like the HOLE ON SLAB element under Structure - and a hole will be created; WALLS under the level, 1.2 m high; a SLAB at the bottom of the walls, within the walls; and a NEW ELEMENT looking like WATER - transparent, blueish."* **THE FINDING THAT DECIDES THE DESIGN, AND IT SHRINKS THE TICKET ENORMOUSLY: THREE OF THE FOUR PARTS ALREADY EXIST. ONLY THE WATER IS NEW.** **(1) THE HOLE IS SHIPPED.** `SlabData.holes` already carries hole polygons, `SlabFragmentBuilder` already builds a fully-capped slab around them (*"a wall is generated along each hole edge so the void is fully enclosed"*), and an `openingStore` already provides hole profiles - **this is exactly the mechanism the STAIR already uses to carve its own void through a floor plate.** **(2) THE POOL WALLS ARE WALLS** (`geometry-wall`) - the only unusual thing about them is a **negative base offset** and a 1.2 m height, and the wall record ALREADY carries `baseOffset`. **(3) THE POOL FLOOR IS A SLAB** (`geometry-slab`). **(4) ONLY THE WATER IS A GENUINELY NEW ELEMENT.** **SO THE POOL IS AN *ASSEMBLY*, NOT A PRIMITIVE - AND THE ARCHITECTURE MUST SAY SO.** It is ONE user gesture that composes FOUR elements. This is the same shape as the curtain wall (panels + mullions) and the stair (flights + railings + handrail), and it must follow whichever of those the codebase has already made canonical - **not invent a fifth pattern.** | New element family (**C11** element creation pipeline - one element, one pipeline, ONE chokepoint; **C16** - one gesture = **ONE undo entry** across all four sub-elements; **C03** schemas; **C15** hosted/compound elements; **C28** schedules - a pool is a schedulable asset with a volume; **C09/P7** - it needs plan/elevation/section representations and a DetailLevel row in **ADR-0265**; precedents: `SlabData.holes` + `openingStore` (the stair's own void-carving), `geometry-curtain-wall`, `geometry-stair`) | **SHIPPED cc695f86 (verification-sweep 2026-07-16, guard test green) - FEAT-SWIMMING-POOL-ELEMENT + @pryzm/geometry-pool; pool suite green. NEEDS LIVE VERIFY for any pixel/interaction placement. Was OPEN (FEATURE).** **Sequence AFTER the demo work** - it is a new family, not a defect, and the founder's demo depends on the documentation pipeline, not on pools. | route -> **NEW element-family agent** (do not give this to a drawing/annotation agent - it is a creation-pipeline ticket). **(1) COMPOSE, DO NOT INVENT.** The hole = `SlabData.holes` / `openingStore` (**the stair already carves a slab this way - READ THAT PATH FIRST**). The pool walls = `geometry-wall` with a negative `baseOffset`. The pool floor = `geometry-slab`. **If you find yourself writing a new hole-punching routine, a new wall type or a new slab builder, STOP - you have misread the ticket.** **(2) THE ONLY NEW GEOMETRY IS THE WATER**: a volume filling the pool to a stated level, with a transparent blue material. **Decide deliberately whether WATER is (a) a new element family with its own record, or (b) a slab-like volume with a water MATERIAL** - and say why. **(a)** is right if water carries its own properties (level, volume, temperature - and it is schedulable, C28); **(b)** is a shortcut that will be regretted the moment anyone asks for the pool's volume. **(3) ONE GESTURE = ONE UNDO ENTRY (C16).** Creating a pool creates four elements; **Ctrl-Z must remove the pool, not one wall of it.** Use the batch chokepoint (`*.batch.create` -> one `produceCommand` -> one patch pair -> one ring entry) - **NOT four separate commands**, which would cost the user four undos. **(4) THE POOL MUST BE ONE THING TO EDIT AND ONE THING TO DELETE.** Follow the CANONICAL compound-element pattern already in this codebase (curtain wall / stair) - **read both and pick the one the contracts bless; do not invent a third.** Deleting the pool must remove its walls, its floor, its water AND heal the slab hole (the orphan-cleanup problem, already solved for tags - the same discipline applies). **(5) IT NEEDS DRAWING REPRESENTATIONS FROM DAY ONE (C09/ADR-0265):** in PLAN it is cut by the plane (poche + a water hatch is the convention); in SECTION the walls and floor are cut and the water is a filled region; add its row to ADR-0265's LOD matrix rather than leaving a 43rd empty cell. **(6) NO LITERALS (L-127):** the 1.2 m depth, the wall thickness and the water level all resolve from the pool's RECORD -> its systemType -> a documented default. **The founder said 1.2 m; that is a DEFAULT, not a constant.** **(7) GUARD IT:** creating a pool punches exactly one hole and produces exactly four elements in ONE undo entry - Ctrl-Z removes ALL of it and RESTORES the slab - the water's top surface sits at the record's water level, not at a literal - no literal dimension survives in any builder. §FEAT-SWIMMING-POOL-ELEMENT. |
| L-293 | **found by the drawing agent while fixing L-290, and REFUSED to smuggle into that ticket** | **[HIGH] THE WORKER RENDER PIPELINE RESOLVES PENS THROUGH ITS OWN MINI RULES ENGINE - NOT `graphicsRulesEngine.resolveStyle()`. SO L-285's WALL-FUNCTION AXIS NEVER REACHES IT.** A pre-existing **Contract-23 §7.1 divergence** - and it is **the SAME CLASS as L-280's symbol renderer**, which resolved a pen through the full chain and then threw it away at the last mile. **Two render paths, two pen authorities, and only one of them knows about VIEW INTENT.** **WHY IT MATTERS BEYOND THE FUNCTION AXIS:** the worker has **no `ViewDefinition`**, so it **structurally CANNOT know view intent** - not the detail level, not the per-view occlusion disposition, not the beyond-line style, not per-element graphic overrides. **Anything we build on the intent chain is invisible to that path by construction.** The agent fixed the L-290 dash there directly (because *"a fix applied to one of two identical paths is not a fix"*) but correctly refused to smuggle the wider unification into a dash ticket. **THIS IS THE 'ONE ENGINE, N CONSUMERS' RULE FAILING AT THE LAST MILE, FOR THE THIRD TIME THIS WEEK** (L-275: the ISO layer resolved from a case-sensitive type NAME; L-280: the symbol renderer bypassing the pen table; L-293: the worker holding a second pen authority). **All three have the same shape: a decision taken somewhere the intent chain cannot see.** | Drawing (**Contract-23 §7.1** - `graphicsRulesEngine.resolveStyle()` is THE ONLY sanctioned style entry point; **C09/P7** - intent must reach every consumer; **C04**; unifies the residue of **L-280 / L-285 / L-290**) | **SHIPPED 1316d8eb (2026-07-16) - worker pen resolution now goes through the single GraphicsRulesEngine.resolveStyle authority (resolved on main thread, PenStyle shipped across the worker boundary); private workerResolveStyle/systemResolvePen fork deleted; one categoryFromLayerTag classifier. 130/130 + 49-test guard proving parity incl. function-axis + beyond-dash. Was OPEN (HIGH).** **Not a regression - a pre-existing fork, now NAMED.** Every future intent feature silently misses this path until it is closed. | route -> **drawing agent** (it found it and knows the shape). **(1) THE WORKER MESSAGE PROTOCOL MUST CARRY VIEW INTENT.** That is the actual work: the worker cannot resolve intent it was never sent. Decide WHAT to send - the resolved STYLE per segment (simplest, and keeps ONE authority on the main thread) vs the raw INTENT (more flexible, but duplicates the resolver). **My instinct is to send the RESOLVED STYLE and keep exactly one rules engine in the process - the whole lesson of L-280 is that a second style authority drifts.** Refute me with evidence if the perf says otherwise. **(2) DELETE THE MINI ENGINE.** A second pen authority is a second truth, and this codebase has paid for that repeatedly (`mullionThickness`; the detached material stores; the re-forked `DEFAULT_DETAIL_LEVEL`). **(3) GUARD IT:** the worker path and the main path produce the BYTE-IDENTICAL pen for the same segment, for every zone x category x FUNCTION - **assert equality between the two paths, so they cannot drift again.** That single assertion is the fix's real deliverable. §FIX-WORKER-PEN-AUTHORITY-FORK. |
| L-294 | **found by the view-intent agent while building the Set Out preset - it would have made the preset FAIL** | **[CRITICAL - FIXED `4c395c4b`] THE DETAIL LEVEL DROPDOWN WAS A SILENT NO-OP. SO WERE SCALE, VISUAL STYLE, DISPLAY MODEL AND SHADOWS. THE ENTIRE VIEW-OUTPUT PANEL WAS DEAD.** The panel fired `view.setOutput`. **NOTHING IN THE EDITOR HANDLES IT.** `initBusHandlers` bridges `view.setCrop`, `view.updateDefinition` and `view.createDefinition` - **but not `view.setOutput`.** It registers BEFORE `registerViewHandlers` (first-registration-wins), so the type fell through to `plugins/view`'s `SetViewOutputHandler`, whose `canExecute` asks `ctx.stores.view.getState().has(viewId)` - **the `@pryzm/view-state` `ViewRegistry`, a store the real ViewDefinitions ARE NEVER WRITTEN INTO.** They live in core-app-model's `viewDefinitionStore`, which every READER (including `resolveEffectiveDetailLevel`) actually reads. **So `has()` was ALWAYS false, the bus rejected every dispatch, and a bare `.catch(() => {})` ATE THE REJECTION.** **THIS IS THE MATERIAL BUG (L-283) IN A SECOND ORGAN - THE UI/RECORD AXIS OF THE SIGNATURE DISEASE.** A control writes to a store nobody reads; the rejection is swallowed; the user changes a setting and nothing happens, with no error. **AND IT IS WHY THE FOUNDER'S SAVED VIEWS ARE STUCK ON `medium`: he could not have fixed it by hand even if he had tried - the dropdown was dead.** **AND THE TESTS WERE ALL GREEN, FOR THE EXACT REASON THAT MAKES THIS CLASS SO DANGEROUS: THEY ALL ASSERT AT `resolveEffectiveDetailLevel()` - A SEAM *BELOW* THE BREAK.** Identical to the pen thrown away in `PlanViewCanvas`, and to the true cut section computed and discarded in L-246. **See LESSON L-M.** | View intent (**C09** visibility intent; **C03/C16** - a rejected command MUST surface; **P6**; **L-283** the material twin; **L-246**; `initBusHandlers` vs `plugins/view`; `ViewRegistry` vs `viewDefinitionStore`) | **FIXED `4c395c4b`** - the panel is routed through `view.updateDefinition` (the bridge that IS live, writes the real store, one undo entry). **THE ROOT HOLE IS STILL OPEN**: `view.setOutput` remains a live command type with **no reachable handler** - routed to the view-intent agent to EITHER bridge it OR delete it as dead. **A live command type with no handler is a trap laid for the next agent - it is exactly how this bug was born.** | route -> view-intent agent (item 3). §FIX-VIEW-OUTPUT-DEAD-DISPATCH. |
| L-295 | **found by the view-intent agent - it REFUTED ITS OWN first hypothesis to get here** | **[HIGH] TURNING THE DOOR OR WINDOW CATEGORY OFF IN V/G DOES NOT REMOVE ITS SYMBOL FROM THE PLAN - because the symbol builders and the visibility applicator SPELL THE SAME LAYER TWO DIFFERENT WAYS.** `VGSceneApplicator.applyToProjectionLayers` probes the **COLON** form (`A-WALL:cut`, `A-WALL:proj`). The door and window symbol builders emit on the **HYPHEN** form (`A-DOOR-CUT`, `A-DOOR-PROJ`, `A-DOOR-BEYOND`, `A-GLAZ-CUT`, `A-GLAZ-PROJ`). **The two never meet - so the category switch cannot see the symbols, and DOOR AND WINDOW ARE PRECISELY THE TWO FAMILIES THAT HAVE SYMBOLS AT ALL.** **THE AGENT REFUTED ITS OWN FRAMING TO FIND THIS.** It suspected V/G never reached the drawing at all (base `A-WALL` vs zone siblings). **WRONG** - the applicator handles the zone siblings correctly. The defect is narrower and nastier: **it handles the ones spelled with a colon.** **SECOND GAP, SAME AUTHORITY:** the applicator **never touches the `:hidden` zone** (L-277's) - so occluded linework survives its category being switched off. **THIS IS THE THIRD SHAPE, FOURTH SIGHTING: *a decision taken somewhere the intent chain cannot see*.** A layer name built by string concatenation at the emitter is a naming decision that the visibility chain was never told about. | Drawing / visibility (**C09** - V/G is the intent chain's hands; **P7**; **L-275**'s `resolveProjectionLayer` canonical resolver is the EXACT precedent; **L-277** the hidden zone; `VGSceneApplicator`, `DoorPlanSymbolBuilder`, `WindowPlanSymbolBuilder`) | **SHIPPED (2026-07-16) - layerForZone() is now the ONLY layer-name constructor; VGSceneApplicator + Door/WindowPlanSymbolBuilder agree on the hyphen layer form, so V/G category-off removes the plan symbol. 6/6 guard green. Also advances L-330. Was OPEN (HIGH) - DEMO-VISIBLE, ON THE DEMO PATH ITSELF.** The founder's demo IS the view intent. **If he switches the door category off and the door stays on the drawing, the feature being demonstrated visibly does not work.** | route -> **view-intent agent, ahead of the LOD cells.** **DO NOT FIX THIS BY ADDING A SECOND PROBE - that is the alias reflex, and it was already refused once this week** (L-275: when `ELEMENT_TYPE_TO_PROJECTION_LAYER` keyed walls capitalised and floors lowercase, the fix was **ONE canonical resolver**, not a capitalised alias). **Two spellings of one layer is the same defect wearing a new coat.** There must be **ONE zone-qualified layer-name authority that BOTH the emitters and the applicator call**, and **nobody may construct a zone layer name by concatenation ever again - assert that.** **GUARD AT THE DRAWING:** door category OFF -> the plan emits **ZERO** door-symbol segments in **EVERY** zone (cut, projection, beyond, **hidden**). ***"The V/G record says hidden" would score 1.000 on the broken build*** - the record was never the problem (**LESSON L-M**). §FIX-VG-HYPHEN-FORM-LAYERS. |
| L-296 | **G7 transforms agent - CLOSING the lying buttons, and REFUTING MY FRAMING THREE TIMES** | **[HIGH - FIXED `4a6f3fd9` + `680b6673`] SLAB AND RAILING MOVE NOW COMMIT; WALL / SLAB / DOOR-FRAME / WINDOW-FRAME / HANDRAIL MATERIAL NOW REACHES THE RECORD. AND FIVE FAMILIES DELIBERATELY DO NOT.** **REFUTATION 1 - 'NO HANDLER EXISTS ANYWHERE' WAS FALSE.** `UpdateWallColorCommand`, `UpdateSlabDimensionsCommand`, `UpdateSlabPolygonCommand`, `UpdateDoorFrameColorCommand`, `UpdateWindowFrameColorCommand`, `UpdateHandrailCommand` **all already existed and all already wrote the geometry store.** The handlers were missing **on the bus**, not in `command-registry`. **Only the ROUTE was missing.** (Bridging slab material also closed slab **dimensions**, which rode the same dead `slab.update`.) **REFUTATION 2 - 'HANDRAIL IS PATH POINTS' WAS FALSE.** `HandrailData.baseLine: [Point3D, Point3D]` - **a LINE family, identical in shape to a beam.** The 3-D refusal message, the `elementMove.ts` header AND the `registerTransformDragHandler` doc-comment **all asserted a premise that was simply untrue - and that false premise is WHY NOBODY EVER WIRED IT.** **A comment can be a bug.** **REFUTATION 3 - AND THE MOST IMPORTANT: 'DETACHED PLUGIN STORE' WAS THE WRONG DIAGNOSIS FOR FIVE FAMILIES.** beam / stair / plumbing / lighting / structural each **DO** have a legacy command reaching the geometry store - **and bridging it would still have shown the user NOTHING**, because `BeamFragmentBuilder` hardcodes two shared materials, `StairData` carries only an enum, only `createBathMesh` honours `data.color`, `LightingData` has no material field at all, and `structural` has **no store, no builder and no command**. **Bridging them would have produced a record that carries a material and a mesh that never shows it - THE SAME LIE, ONE LAYER DEEPER.** They now **declare** and toast instead, and **a guard fails if a refusal reason names only the bus** - a refusal must name the **BUILDER**. **AND ONE C16 VIOLATION AVOIDED:** bridging door/window's generic Material dropdown would have given one gesture **two commands and two undo entries**, since frame colour already has its own control on the same command. They declare and point at the working control. | Transforms / material (**C16** one gesture = one undo entry; **P6**; **C03**; **L-283** the material twin; the capability-declaration pattern) | **FIXED.** Rejection surfacing also landed: the `initBusHandlers` bridge wrapper **caught a failed command and returned as if it had succeeded** - it now toasts and re-throws, which **also un-swallowed `viewTemplate.delete`'s deliberate error**, silently eaten for who knows how long. `wall.setColor`'s `{wallId}`/`{id}` mismatch is **deleted, not patched.** | **TWO NOTES THE AGENT MADE AGAINST ITSELF, BOTH WORTH MORE THAN THE FIX:** **(1) a mutation came back GREEN because the MUTATION was flawed** (it concatenated the original text back) - ***a toothless mutation looks EXACTLY like a toothless guard.*** **(2) `npx vitest run MaterialDispatch` FROM THE ROOT MATCHES NOTHING** - the suite lives in `apps/editor/__tests__/*.test.ts`, which no root `include` pattern covers. **It nearly reported the suite green having never run it; when it did run it, there were 2 REAL FAILURES.** **The same failure mode as the bug itself: VERIFY WHERE THE THING ACTUALLY EXECUTES.** (It DOES run in CI, via the editor workspace's own `test:ci` - so this is a LOCAL-COMMAND trap, not a CI hole. **But `vitest run <name>` exits NON-ZERO on 'no test files', which is the one mercy here.**) §FIX-MOVE-SLAB-AND-HANDRAIL + §FIX-MATERIAL-REACHES-RECORD. |
| L-297 | founder (2026-07-14, live) | **[HIGH] DIMENSIONS ON AN ELEVATION DRAG IN THE OPPOSITE DIRECTION - mouse DOWN, the dim goes UP.** **AND IT IS MY OWN FIX, HALF-DONE.** `annotationDragIntent.ts:82`: `delta = dH * axis.x + dV * axis.z`. **`dimOffsetAxis()` computes the offset direction from `measurementNormal.x` / `.z` ONLY - a vector in PLAN WORLD AXES.** But `dH`/`dV` are **VIEW-PLANE** deltas. In a plan, V = world Z, so `dV * axis.z` is right **by coincidence.** **IN AN ELEVATION, V IS WORLD Y** - and the code multiplies the vertical drag by a plan vector's **Z** component. Wrong space. **THE LESSON, AND IT IS THE SHARPEST ONE THIS WEEK: I FIXED THE TAG BRANCH OF THIS EXACT FUNCTION LAST NIGHT (SS-FIX-TAG-DRAG-2D) AND LEFT A COMMENT ON THE DIMENSION BRANCH SAYING *"A DIMENSION GENUINELY IS 1-D IN ITS PRESENTATION ... THAT IS CORRECT, AND IT STAYS."*** **THAT CLAIM IS TRUE, AND IT IS EXACTLY WHY I MISSED THIS.** A dimension **is** 1-D. **I fixed its DIMENSIONALITY and never questioned its FRAME.** The axis it is 1-D *along* is still computed plan-first. **Two independent properties; I verified one and assumed the other. A TRUE STATEMENT MADE ME STOP LOOKING.** **SUSPECTED SECOND, SILENT BUG IN THE SAME FUNCTION:** the `p-q` fallback needs `hypot(dx, dz) > 1e-9`. **A VERTICAL elevation dim (the 2040 mm door-height dim in his screenshot) runs along world Y, so dx=dz=0 -> returns `null` -> THE DIM IS NOT DRAGGABLE AT ALL.** He has not noticed because the inverted horizontal ones are louder. | Annotations (**L-287** presentation/reference split; **C24** paper space; **C24.1**; `annotationDragIntent.ts`, `_renderLinearDim`) | **SHIPPED `054ff748` (2026-07-15).** Frame-aware offset axis; renderer-derived sign; vertical dims now draggable; 27/27 green red-first. **THE SIGN WAS DERIVED FROM THE RENDERER, NOT GUESSED** - `offset` means whatever `_renderLinearDim` says it means. **And an inversion is the one bug a magnitude test cannot see: the magnitude is ALREADY RIGHT.** The strong constraint handed to the agent: **the TAG branch works** (he confirms tags drag up correctly), so `dV` is positive-UP by the time it reaches this function - **therefore the inversion is NOT in `dV`; it is in the axis or in the renderer's reading of `offset`.** | route -> annotations agent. **SHIPPED `054ff748`.** SS-FIX-DIM-DRAG-FRAME. |
| L-298 | **found by the POOL agent, and it has NOTHING TO DO WITH POOLS - it is a live data-corruption bug in the founder's model TODAY** | **[CRITICAL] `DeleteStairCommand` NEVER REMOVES THE SLAB OPENING THAT `CreateStairCommand` PUNCHED. DELETE A STAIR IN PRYZM TODAY AND THE VOID STAYS IN YOUR FLOOR FOREVER.** Its `undo()` removes the hole. **Its DELETE does not.** And `OpeningCleanupHandler` listens only for **level** or **slab** removal - never for the stair itself. **So the floor keeps a permanent hole where a stair used to be, with nothing on screen to say why, and no way to remove it.** **Related, same family:** `slab.addHole` uses **`push`** rather than a whole-array replace - **a deep patch, which dies in the legacy undo adapter and would wipe EVERY hole on the slab.** The pool agent proved this is not theoretical by injecting it: **4 tests red.** | Elements / undo (**C11** creation pipeline; **C16**; **C03 SS4.5-4.8** undo invariants; ADR-0121; `DeleteStairCommand`, `OpeningCleanupHandler`, `slab.addHole`) | **SHIPPED `c0b1cb22` (+ cleanup `10d18e8f`).** Delete now heals the slab inside the delete command (mirrors door/window->wall + slab-delete cleanup); the REAL UI path (`DeleteElementCommand` stair branch) delegates to `DeleteStairCommand` so both paths heal with zero drift; removes from `openingStore` (the exact field `SlabFragmentBuilder` triangulates). ONE undo restores stair AND hole; sibling holes untouched. 4 guards red-first, 75/75 green; root-tsc clean. | route -> **elements agent.** The create punches; **the delete must heal.** Symmetry is the invariant: **whatever `undo()` restores, `delete()` must remove.** Assert it on the SLAB RECORD (holes array), not on the command. SS-FIX-STAIR-DELETE-LEAVES-HOLE. |
| L-299 | **found by the POOL agent - DEAD CODE THAT WOULD HAVE SPRUNG ON THE FIRST MULTI-STORE COMMAND** | **[HIGH - FIXED] THE MULTI-STORE UNDO PATH WAS BROKEN BY CONSTRUCTION, AND NOTHING HAD EVER WALKED IT.** Both routers dispatch by `path[0] === storeKey`, but `produceWithPatchesPerStore` emits store-**relative** paths -> **patches routed to nothing** -> `applyRingBufferSide` applied **ZERO** stores -> **undo fell silently through to `commandManager`.** And neither router **stripped** the key before its consumers, which read `path[0]` as **the element id**. **IT NEVER SPRANG BECAUSE NO BUS HANDLER HAD EVER DECLARED TWO STORES.** The pool is the first element that spans four (pool + walls + slab + water) - **so the first genuinely compound element in PRYZM would have shipped with a silently broken undo.** **Fixed at the SEAM (`produceMultiStoreCommand` + symmetric strips), NOT in the pool** - the pool was the discoverer, not the owner. | Commands / undo (**C16 SS8.6**; **C03 SS4.5-4.8**; ADR-0121; the ring-buffer routers) | **FIXED `cc695f86`.** **The agent REFUTED MY BRIEF to get here:** I instructed it to *"use the `*.batch.create` chokepoint"*. **That works for `wall.batch.create` BECAUSE IT IS SINGLE-STORE.** A pool spans four. **My central instruction was wrong, and following it would have produced a pool with no undo.** | SS-FIX-MULTI-STORE-PATCH-ROUTING. |
| L-300 | **found by the POOL agent - the trap under 'the hole is already shipped'** | **[HIGH] THERE ARE TWO `holes` FIELDS WITH THE SAME NAME IN DIFFERENT COORDINATE SPACES, AND ONE OF THEM RENDERS NOTHING.** L1 `Slab.holes` (`Vec3`, **y = ELEVATION**) - written by `slab.addHole`, **renders nothing at all**; and legacy `SlabData.holes` (`{x,y}`, **y = WORLD Z**) - the one `SlabFragmentBuilder` actually triangulates. **FEED AN L1 LOOP INTO THE LEGACY FIELD AND EVERY VERTEX COLLAPSES ONTO z = elevation: A ZERO-AREA HOLE THAT PUNCHES NOTHING, SILENTLY.** **My brief asserted *"THE HOLE IS SHIPPED"* - the agent found that is HALF true, and it is the WRONG half.** Named and homed in `SlabHoleCoords.ts` (additive, no behaviour change). | Geometry (**L-127** dimensional truth; **C11**; `SlabFragmentBuilder`, `slab.addHole`) | **NAMED + HOMED `cc695f86`** - the two spaces now have one authority and one conversion. **A silent zero-area hole is the worst kind of bug: the command succeeds, the record is written, and the building does not change.** | SS-SLAB-HOLE-COORD-SPACES. |
| L-302 | founder (2026-07-15, two-level house) | **[HIGH] AN ELEVATION ONLY TARGETS ONE LEVEL, AND EXTENDING ITS CROP BOUNDARY IN PLAN DOES NOTHING - because THE ELEVATION CROP IS A 2-D FOOTPRINT WITH NO VERTICAL DIMENSION.** `PlanViewInteraction._cropRegionFromSectionVolume()` returns `{minX, minZ, maxX, maxZ}` - **X and Z only. There is no Y (height) anywhere in the crop region.** So dragging the elevation's boundary in the plan changes how WIDE and how DEEP the elevation looks; **it CANNOT change how TALL it looks, because height is not in the box being dragged.** The log confirms it: `resolveClipRange() elevation depth near=0.000 far=15.000` - `far` is DEPTH-into-the-model (what the drag controls); **the vertical extent is decided elsewhere and defaults to a SINGLE STOREY** (the level the mark was dropped on). The second storey sits above that window. **THIS IS THE PLAN-FIRST DISEASE IN THE CROP MODEL: a documentation view authored one-level-at-a-time.** An elevation in any BIM tool must span the FULL BUILDING HEIGHT by default - Revit, ArchiCAD, Vectorworks all do. | Views / documentation (**C24** sheet/crop; **C09** view scope; **C24.1**; `PlanViewInteraction._cropRegionFromSectionVolume`, `resolveClipRange`, `ViewSectionVolume`) | **SHIPPED 00a8ce33 (SS-FIX-ELEVATION-SPANS-ALL-LEVELS) - elevation vertical extent = union of level bands the footprint intersects (from level stack, no literal) + vertical crop handle. NEEDS LIVE VERIFY: on-screen crop-handle drag. Was OPEN (HIGH) - DEMO-VISIBLE ON A MULTI-STOREY HOUSE.** The founder's demo house has two levels; his elevations show one. | route -> **views agent.** **(1) CONFIRM THE MECHANISM: is the second level CLIPPED (a Y-range check in the projector) or merely OFF-FRAME (auto-frame fits one level's bounds)?** The code fact is settled - the crop has no Y - but the SYMPTOM's exact seam decides whether the fix is on the crop or on the frame. **(2) AN ELEVATION MUST SPAN FULL BUILDING HEIGHT BY DEFAULT** - vertical extent = union of all level bands the footprint intersects, NOT the authoring level. **(3) A VERTICAL CROP HANDLE** in the elevation view (C24 paper crop is 2-D on the SHEET, but the view's spatial extent is 3-D). **(4) DO NOT bake a level height literal (L-127)** - derive from the level stack. **(5) GUARD: a 2-level house's North elevation projects geometry from BOTH level bands; extending the plan crop is orthogonal to (never changes) the vertical extent.** **SHIPPED `00a8ce33`** — projector derives vertical extent from the live level stack + honors `crop.region[1]`; a level-2 wall now survives `sectionBoxIntersectsWorldAABB`; red-first. SS-FIX-ELEVATION-SPANS-ALL-LEVELS. |
| L-303 | founder (2026-07-15, same session) - **A HARD CRASH, same WebGPU-dispose family as L-301** | **[CRITICAL] EDITING A LEVEL'S ELEVATION CRASHES THE COMMAND: `[CommandManager] FATAL ERROR DURING EXECUTION TypeError: Cannot read properties of undefined (reading 'usedTimes')` at `_disposeLevel` -> THREE material dispose -> WebGPU backend.** `UPDATE_LEVEL` traverses the departing level and disposes its materials MID-FRAME (`_disposeLevel` -> `object.traverse` -> `material.dispose()` -> WebGPU `tq.delete` reads `usedTimes` of an undefined backend entry). **This is the SHADOW-DEPTH-TEXTURE-MID-SUBMIT family** (memory: null-at-mount runtime-event race, SS-SHADOW-DISPOSE-DEFER; ShadowDepthTexture destroyed-texture-used-in-submit warnings appear in the SAME log). **Two more WebGPU faults in the same session:** `Vertex buffer slot 1 required by RenderPipeline LineDashedMaterial was not set` (the HIDDEN/BEYOND dashed-line pipeline draws before its buffer is bound), and `[FrameScheduler] tick listener 'once:dep-resolver-flush' error: reading 'substring'` after `ADD_OPENING`. | Rendering (**C04** scheduling; **P2** single THREE owner; **P3** single rAF; same root as **L-301**; `_disposeLevel`, WebGPU material dispose, `LineDashedMaterial`) | **SHIPPED 489dd564 — _disposeLevel routes GPU dispose through deferDisposePastSubmit (post-submit macrotask) + defensive usedTimes swallow; 5-test guard + 459/459 core-app-model green. Was OPEN (CRITICAL).** Folded into the L-301 render-crash investigation - **same disease: a THREE resource disposed inside a live submit.** The fix is a DEFERRED dispose (dispose after the frame's submit completes, never during traversal mid-submit), consistent with SS-SHADOW-DISPOSE-DEFER. | route -> elements/rendering. **SHIPPED `489dd564`** — `LevelVisualizer._disposeLevel` defers GPU release past submit (reuses the shadow-dispose pattern); a twin sprite-dispose on setActiveLevel fixed too; 5 tests red-first. SS-FIX-LEVEL-DISPOSE-MID-SUBMIT. |
| L-304 | founder (2026-07-15) - minor, same log | **[LOW] THE 4-DIRECTION ELEVATION-MARK TOOL DISPATCHES A REJECTED COMMAND FOUR TIMES: `[AnnotationStore] add(): parameters failed schema check - [elevation-mark] facingDirection.y: Invalid input`.** The tool writes a 3-D `facingDirection` (`{x, y, z}`) but the elevation-mark schema accepts a 2-D `{x, z}` (P5 schema purity - an elevation faces a compass direction, it has no vertical component). It RECOVERS (the marks are created), but four rejected dispatches per placement is noise that hides real failures. | Schemas / annotations (**P5** pure schemas; **C03**; `ElevationPlanToolHandler`, elevation-mark schema, `facingDirection`) | **SHIPPED (tool) (verification-sweep 2026-07-16, guard test green) - ElevationMarkTool.ts:159 emits facingDirection {x,z} (no y) - the rejected-dispatch storm is gone. NEEDS LIVE VERIFY for any pixel/interaction placement. Was OPEN (LOW).** The tool must build a 2-D facing vector, OR the schema must strip `y` before validation - **decide which is canonical** (the direction IS 2-D, so the TOOL is wrong). | route -> annotations agent, low priority. SS-FIX-ELEVATION-MARK-FACING-2D. |
| L-305 | founder (2026-07-15) - **PRIORITISED, quick** | **[HIGH] THE ELEVATION MARK'S ORIGIN CANNOT BE MOVED.** The founder can move the mark's LINE and resize its crop, but he cannot grab the ORIGIN - the circle glyph the mark starts from - and reposition the whole marker. `PlanViewInteraction._scopeDrag` offers handles `depth | width-left | width-right | cut-plane` and `_cropDrag` offers the crop corners - **there is no TRANSLATE handle on the origin circle.** **THE CRUX: an elevation mark is TWO linked things** - an ANNOTATION with a circle glyph + `facingDirection` in the plan, AND a LINKED VIEW with a section volume (`volume.origin`). Moving the origin must move BOTH, as ONE gesture = ONE undo (C16). Move only the volume and the circle stays behind; move only the glyph and the view does not follow. | Views / annotations (**C16** one gesture = one undo; **C24/C09** the mark is a view + an annotation; **P6** commands only; **L-154** grabbable handles; **L-222** live-preview flow; **L-267** no lying handle; `PlanViewInteraction`, `_scopeDrag`, `volume.origin`, `linkedViewId`) | **SHIPPED 107546ed (SS-FIX-ELEVATION-MARK-MOVE-ORIGIN) - 5th origin translate grab co-moves the glyph AND volume.origin as one gesture/one undo (MoveMarkOriginCommand). NEEDS LIVE VERIFY: glyph+volume co-move on live drag. Was OPEN (HIGH) - PRIORITISED, routed as PRIORITY-ZERO to the views agent (ahead of L-302).** Same file as the elevation vertical-crop work - one owner, no collision. | **SHIPPED `107546ed`** (MoveMarkOriginCommand — dual-store, one undo; glyph+volume co-move; 8 tests). route -> views agent. Add a fifth `'origin'` (translate) grab: hit-test the circle (L-154 tolerance), pure-translate `volume.origin` (direction/depth/width invariant), move the ANNOTATION glyph AND the linked view's volume TOGETHER through the command path (one undo entry), live preview (L-222). **GUARD: origin moves by exactly the drag delta with direction/depth/width byte-identical; glyph and volume co-move to the SAME point; ONE Ctrl-Z restores both; the other handles are unchanged.** **SHIPPED `107546ed`.** SS-FIX-ELEVATION-MARK-MOVE-ORIGIN. |
| L-301 | founder (2026-07-15) - **HARD RENDER CRASH, worked around by swapping WebGPU->WebGL->WebGPU** | **[CRITICAL - SHIPPED `3ac9d096`] OPENING THE 3D VIEW FROM AN ELEVATION/PLAN CRASHES THE RENDER PIPELINE: `PIPELINE_FAILURE reason="Cannot read properties of undefined (reading 'replace')"` in three's WebGPU `acquireProgram`, 3 retries, then ViewportCrashGuard -> no-post-FX.** **ROOT (refined from my hypothesis by the renderer-three agent): the projection-toggle FAST PATH compiles the pipeline against a STALE CAMERA object, not a stale uniform.** On elevation->3D, `notifyProjectionToggle(false)` sets the authority `_uIsOrthographic=0` (perspective) SYNCHRONOUSLY, but OBC's camera flip to Perspective propagates ASYNC - so at `view-activated`, `updateCamera` binds the STILL-ORTHOGRAPHIC `newCamera` onto the passes and `_rebuildPipelineGraphOnly` compiles against it. A camera-type-dependent TSL node resolves a shader string to `undefined` -> three calls `.replace()` on it -> crash. **The `"built for orthographic camera"` log line was a HARDCODED LIE (reads no state), which is why the console looked self-contradictory.** The backend swap 'fixed' it by doing a FULL pass reconstruction later, once the perspective camera settled. **Fires on plan->3D too; 3D->3D is safe.** | Rendering (**C04** pipeline graph + rAF; **P2** single THREE owner - fix in renderer-three; **P3** single rAF; DISTINCT from SS-SHADOW-DEVICE-LOSS-FIX and the null-at-mount race; `RenderPipelineManager.updateCamera/_rebuildPipelineGraphOnly/notifyProjectionToggle`, `ViewController._activate3DView`) | **SHIPPED `3ac9d096`.** The fast path now RECONCILES the compile camera against the `_uIsOrthographic` AUTHORITY before building: if the camera contradicts the authority (mid-flip), it DEFERS the rebuild one macrotask (bounded 8 retries -> full-rebuild fallback) rather than compiling against the mid-transition camera. Fast path preserved (no pass reconstruction). Lying log fixed to interpolate the real projection. 202/202 renderer-three tests, 11 new, PROVEN RED (neutralised guard -> 3 behavioural tests fail -> reverted byte-identical). **NO `?? ''` symptom-guard on `.replace` - fixed at the disease.** | **AWAITING FOUNDER CONFIRMATION:** shipped on the STATIC root cause (no live WebGPU repro reachable - localhost dev unusable, test on prod). Founder action: open 3D from an elevation on the next deploy WITHOUT the backend swap. One-line runtime probe ready if it recurs. §FIX-PROJECTION-TOGGLE-STALE-GRAPH. |
| L-306 | founder (2026-07-15) - purely aesthetic, follow-up to L-305 | **[LOW] WHEN THE ELEVATION-MARK ORIGIN IS MOVED, THE DASHED CROP-SCOPE BOX 'GOES OFF' - it stretches/detaches from the mark instead of translating rigidly.** Reported right after L-305 (origin-move) shipped. **The COMMAND is NOT the bug:** `MoveMarkOriginCommand` already translates BOTH `nextCropRegion` and `nextCrop`, so the committed crop follows the origin. The visual stretch is in the OVERLAY RENDER (`_renderSelectedScopeOverlay`) or the LIVE-PREVIEW path - one edge tracks the moving origin while another reads a stale/independent source. | Views (**L-305** origin-move; **L-222** live-preview flow; **C24** crop overlay; `PlanViewInteraction._renderSelectedScopeOverlay`, `_cropRegionFromSectionVolume`) | **SHIPPED 28a4e2e0 (SS-FIX-SCOPE-OVERLAY-FOLLOWS-ORIGIN) - dashed crop-scope box tracks the moved origin in lock-step (shape-invariant). NEEDS LIVE VERIFY. Was OPEN (LOW) - routed to the views agent (owns L-305 + the overlay).** A 1-file polish on a just-shipped feature. | route -> views agent. Overlay must derive the dashed box from ONE coherent source updated atomically with the origin; live preview re-derives the box every frame via `_cropRegionFromSectionVolume`. **GUARD: moving the origin by (dx,dz) shifts ALL FOUR crop-box corners by (dx,dz); the box SHAPE (width/depth spans) is INVARIANT under an origin move - a stretch fails shape-invariance.** **SHIPPED `28a4e2e0`** — live-preview write order fixed (glyph before view); box tracks origin in lock-step. SS-FIX-SCOPE-OVERLAY-FOLLOWS-ORIGIN. |
| L-307 | founder (2026-07-15) - **PERF SPIKE requested** | **[PERF] EVERY SINGLE ELEMENT CREATED ON THE FLOOR PLAN RE-RENDERS THE WHOLE PLAN (~0.5s+).** Console: each create runs ALL `89 group(s)` / `116 elements` through `EdgeProjectorService.project()` + `HiddenLineRemoval` (163-413 seg-equiv), and does so **3-4 TIMES per creation** - the gen counter jumps ahead of each completing pass (`staleGen=155 currentGen=158`, `156->160`), so most full passes are COMPUTED then DISCARDED as stale. Cache thrash: one pass `hitRate=0%` (241 edge geos rebuilt), next `100%`. Suspected redundant triggers per create: `UPDATE_ANNOTATION`x2 + `CREATE_ANNOTATION` + `RoomTagAutoPopulator` + `RoomBoundaryBuilder` compliance + `REDETECT_ROOMS` + `ProjectSerializer`/`VersionRepository ~7.7MB` autosave + `captureThumbnail` (blank-frame retry) - each may independently invalidate the view. **Anomaly (possible correctness bug): `VGSceneApplicator applied=22/14 layers` - applied count EXCEEDS the total (also 21/14, 17/14) - looks like DOUBLE application.** | Rendering/scheduling (**C04** projection pipeline + single rAF; **P3**; **C24/C24.1** plan drawing; batch-coalescing **C16/C17** `*.batch.create`; perf tags §PERF-*, §H2-NME-CACHE, §FIX-PLAN-BLANK-STALEGEN, §FIX-LAZY-INACTIVE-VIEW-PROJECTION) | **SPIKE COMPLETE.** ROOT (one sentence): a single Canvas2D create is NOT bracketed by any transaction, so it fans out to TWO uncoordinated whole-view projection drivers (`PlanViewManager` 30ms + `ViewDependencyTracker` 48/300ms) PLUS an uncoordinated `REDETECT_ROOMS` whose per-room `roomStore` writes re-fire the first driver again - producing **3-4 full-plan projections per gesture, all but the last COMPUTED-THEN-DISCARDED as stale** (the gen jumps 3-4 ahead). The per-element EPS cache mostly WORKS (the `hitRate=0%` then `100%` is a redundant warm-replay pass, not thrash). **`BatchCoordinator.runBatch` ALREADY suppresses all of this (view-reproject + redetect + autosave) but is entered ONLY by `*.batch.create` - a single create dispatches a plain `*.create` and bypasses every suppression.** Incremental graft (`projectElementsInto`, §FIX-PLAN-PROJECT-INCREMENTAL) exists but only for `{wall,slab,beam,ceiling,floor}` - door/window/furniture/stair always force a whole-view symbol pass, and a wall create is poisoned by the `room` write (room is not graft-safe and has NO type-gate in `ViewTechnicalDrawingCache._onStoreChange`). RANKED FIX: **(1) coalesce the burst to ONE projection per gesture** (single-owner for the active view OR a lightweight gesture micro-batch reusing BatchCoordinator suppression) - **~50-65%**, C04/P3, medium risk (touches gen/settle timing - DEMO-SENSITIVE); **(2) room-store changes must NOT invalidate the edge-projection drawing** (add a `room` skip mirroring the `annotation:*` cut at ViewTechnicalDrawingCache L502 + debounce REDETECT) - **~20-30%**, C24, LOW risk; **(3)** widen graft to symbol types - ~15-25%, higher risk; **(4)** autosave/serialize/thumbnail OFF the path (idle) - NOT part of the 0.5s but a 2.5s-later jank (double-serialize in SaveOrchestrator L179-188). CORRECTNESS found: **`applied=22/14` is mostly a MISCOUNT** (14 = categories; numerator counts base+4 zone sublayers) BUT 3 categories genuinely double-style a shared base layer last-write-wins (curtain-wall+wall->A-WALL, curtain-panel+window->A-GLAZ, handrail+stair->A-STRS) - real minor bug; and **§FIX-PLAN-BLANK-STALEGEN AMPLIFIES the race** (accepts a stale drawing then dispatches `vd:reprojection-required` -> ANOTHER full pass; becomes dead code once (1) lands). | route -> perf spike agent (read-only). SS-SPIKE-PLAN-REPROJECT-PER-ELEMENT. |
| L-308 | founder (2026-07-15, screenshot: floating window frame) | **[HIGH] DELETING A WINDOW LEAVES ITS 3D FRAME MESH FLOATING IN THE SCENE.** The opening is removed and the wall heals SOLID, and the PLAN correctly drops the window symbol (`WindowPlanSymbolBuilder 7->6->5`) - but the window's frame+glazing MESH is never disposed and floats on the now-solid wall. **Same family as L-298: `DELETE_ELEMENT` cleanup incomplete for a hosted element** (there it left the slab hole; here the frame mesh). **SMOKING GUN: `DELETE_ELEMENT scope=[wall,slab,column,curtainWall,furniture,handrail,roof,floor,ceiling,beam,plumbing,stair,level]` - `window` and `door` are ABSENT from affectedStores.** Plus `[VDT] §G3-STALE-EVENT for unregistered element ... type=window` - the record is unregistered but the mesh persists. So the opening removal works; the MESH disposal (and likely the windowStore record removal / builder disposal event) does not fire. | Hosted elements (**C15** hosted-element lifecycle - the window mesh is owned by its host relationship; **C11** create/delete symmetry; **C16** one undo; **C03 §4.5-4.8**; **C04/P3** dispose is a frame event - watch the **L-303** mid-submit family; `DeleteElementCommand`, `WindowBuilder`, windowStore) | **SHIPPED `33ddaee7`.** ROOT: `DeleteElementCommand` window branch called `wallStore.removeWindow` + `elementRegistry.unregister` but NEVER `windowStore.remove(id)` - and `WindowBuilder` disposes the mesh ONLY on a `windowStore` 'remove' event, so the frame+glazing group survived (explains the `§G3-STALE-EVENT unregistered element` while the mesh persisted). DOOR shared it identically (never `doorStore.remove`). The host-WALL-delete cascade already did `windowStore.remove` - that ASYMMETRY was the bug. Fixed both; `window`/`door` added to `affectedStores`; undo re-adds the record -> builder rebuilds (one Ctrl-Z restores mesh+record+opening). Builder uses the existing WebGPU-safe dispose (no new mid-submit). 5/5 red-first (tooth = `builder.meshes.has(id) === false` after delete); root-tsc clean. | route -> hosted-element delete agent. Mirror the window-when-host-wall-deleted disposal (which works) so a single-window delete converges to the SAME teardown; add `window`/`door` to affectedStores; ONE undo restores mesh+record+opening. **GUARD on the SCENE: the mesh is GONE from the scene after delete (the tooth - 'record removed' is vacuous, the founder sees the MESH); sibling window untouched; one Ctrl-Z restores it.** SS-FIX-WINDOW-DELETE-LEAVES-MESH. |
| L-309 | founder (2026-07-15) | **[HIGH] ROOM TAGS CANNOT BE MOVED.** Element tags (door/window/wall) are movable (L-291/L-256/L-287); room tags are not. Two plausible roots: **(A)** room tags are NOT in the pick corridor -> can't be grabbed; **(B)** `RoomTagAutoPopulator.populate()` runs on EVERY plan projection (inside `onReprojectionNeeded`, per the L-307 spike) and RE-DERIVES the tag position from the room centroid, WIPING any user drag (rebuild-not-reconcile) - the log shows `RoomTagAutoPopulator ... 0 created, 2 refreshed`. May be BOTH (can't grab; and even if grabbed it snaps back). | Annotations (**C24/C24.1** tag = paper-space; **L-287** presentation/reference split; **L-286b** reconcile-not-rebuild; **L-291** tag pick corridor + drag; **P6**; `RoomTagAutoPopulator`, `annotationDragIntent`, `PlanViewInteraction`) | **SHIPPED 57347a18 (verification-sweep 2026-07-16, guard test green) - RoomTagAutoPopulator uses reconcileTagSet() (reconcile-not-rebuild) - user drag survives. NEEDS LIVE VERIFY for any pixel/interaction placement. Was OPEN (HIGH) - agent dispatched.** | route -> annotations agent. Mirror element tags: same pick corridor + `planAnnotationDrag`; `RoomTagAutoPopulator` must RECONCILE (preserve `offset`/`screenOverride`, re-derive only the room-centroid anchor), not rebuild. **THE TOOTH (L-286b): drag a room tag, trigger REDETECT_ROOMS/re-projection -> the drag SURVIVES (offset byte-identical, id unchanged). A rebuild fails HERE and nowhere else** - 'tag exists after populate' is vacuous. **SHIPPED `84b6dd6d`** — root was NEITHER hypothesis: the drag COMMITTED + SURVIVED populate, but `_renderRoomTag` + the room-tag hit-test read `references[0].cachedPosition` (the CENTROID) instead of `modelPoints[0]` (the dragged point), repainting to centre every frame. Fixed to prefer `modelPoints[0] ?? cachedPosition`. 18/18 + 36/36 red-first. SS-FIX-ROOM-TAG-NOT-MOVABLE. |
| L-312 | founder (2026-07-15) - **RECURRING, 'more and more'** | **[HIGH] WEBGPU DEVICE-LOSS RECOVERY CASCADE - TWO problems.** **(TRIGGER)** a huge instant batch loses the GPU device: a resi-building gen creates 1278 elements / 7 levels / **9350 meshes**; the curtain-wall batch alone runs ~10s (`BUILD-QUEUE-DRAINED totalExpected=112 T=+9958ms`), and mid-batch `OperationError: Instance dropped in popErrorScope` -> `Context Lost`. **(RECOVERY WORKS)** §FIX-WEBGPU-DEVICE-LOSS-CESIUM-CASCADE / §SHADOW-DEVICE-LOSS-FIX / §FIX-DISPOSE-USEDTIMES-DEVICE / §RPM-RECOVERY-DOWNGRADE fire and the renderer recreates. **(POST-RECOVERY BUG - the concrete one)** a FLOOD of `depth stencil attachment size (1145x915) does not match the other attachments' base plane (632x915 / 417x915 / 1152x919)` -> every render pass INVALID (`Invalid CommandBuffer ... Submit`). **The DEPTH buffer was not resized to the SPLIT-VIEW color attachments after context restore** (widths 632/417 = split panes). Plus `glClear: Framebuffer incomplete: Attachment has zero size`. | Rendering (**C04** render targets + pass sizing + recovery path; **P2** single THREE owner; **P3**; DISTINCT from L-301/L-303 but same device-loss FAMILY; prior art: memory 'WebGPU heavy-scene crash + instancing' + §SHADOW-DEVICE-LOSS-FIX 40-storey office; `RenderPipelineManager.recoverPipeline`, `createRenderer` device-recovered, depth/render-target alloc, `SplitViewManager` resize) | **SHIPPED (CLOSE-VERIFIED 2026-07-16) - Problem A (post-recovery depth/split-view size mismatch) handled by _reconcileRenderSize() on recoverPipeline() + per-frame; L-324 terminal-reload + L-326 fallback-white in createRenderer; L-312B material-share (6225ef05). Problem B bounded by device-loss cap -> WebGL safe-mode (stable default). No gap found. Was OPEN (HIGH) - agent dispatched (renderer-three).** Recovery succeeds but leaves the view broken until another resize; and the device loss recurs on every heavy generation. **Demo workaround: WebGL (told founder).** | route -> render agent. **(A) FIX the depth/split-view size mismatch:** after device recovery AND on split resize, reallocate the depth attachment to EXACTLY the color attachments' size (one source of truth); no pass begins with mismatched depth. GUARD: depth.size === color.size at pass-begin (tooth). **(B) REDUCE device-loss frequency:** find the dominant cause during the batch (PSO-compile storm from per-element unique materials / single GPU-alloc spike / shadow-outline work) and PROPOSE a ranked, low-risk mitigation (chunk/throttle PSO compile across frames; defer CW prewarm on huge scenes) - implement only if clearly safe, else authorize. **DO NOT silence the depth-mismatch validation - RESIZE the buffer.** SS-FIX-RENDER-RECOVERY-DEPTH-AND-DEVICE-LOSS. |
| L-313 | founder (2026-07-15) | **[HIGH] THE 3D GLOBE (CESIUM/GIS) CRASHES ON ACTIVATION -> 'Opening the 3D globe - taking too long'.** `Cesium.js Uncaught (in promise) TypeError: Cannot read properties of undefined (reading 'scene') at C.activate` (**CesiumThreeBridge.activate**, `index-CvPcRz1P.js`), then `[viewActivationLoading] globe - readiness NEVER ARRIVED at stage 'content' (no progress for 25000 ms)`. **DOWNSTREAM OF L-312:** earlier the WebGPU device-loss recovery DISPOSED+RECREATED Cesium (`§FIX-WEBGPU-DEVICE-LOSS-CESIUM-CASCADE Disposing Cesium... / re-initialised`); on re-activation the bridge reads `.scene` on an UNDEFINED viewer -> **stale reference to the disposed viewer** (or awaited the wrong readiness: `whenReady` resolved but `.scene` still undefined). The throw hangs the loading overlay 25s and dead-ends. | Geospatial (**C06** GIS surface; **C12/C18/C19** geospatial subsystem; the view-activation-loading overlay contract - readiness must resolve or FAIL, never hang; **P4**; downstream of **L-312**; `CesiumThreeBridge.activate`, `CesiumViewport` dispose/recreate) | **SHIPPED 01fe2d7e + b5f69afc (2026-07-16) - CesiumThreeBridge re-acquires the live viewer via a provider + fail-fast guard; containViewActivation() wraps every globe entry so a failed activation surfaces in-editor retry instead of ejecting to /projects. 85/85 GIS tests green. NEEDS LIVE VERIFY: globe opens without hang after a real WebGPU device-loss on prod. Was OPEN (HIGH) - agent dispatched (geospatial).** Blocks the 3D globe entirely after any device-loss recovery. | route -> geospatial agent. Bridge must RE-ACQUIRE the current live viewer on activate (never a stale captured ref), await `viewer && viewer.scene` before reading, and on genuine failure SURFACE retry (wire the existing 'Try again' button) instead of a 25s overlay hang. **GUARD: after a CesiumViewport dispose+recreate, activate binds to the NEW viewer and never throws `.scene`-of-undefined (tooth); overlay fails-to-retry, never hangs.** SS-FIX-GLOBE-ACTIVATE-STALE-VIEWER. |
| L-311 | founder (2026-07-15) | **[HIGH - SHIPPED `7f8d8d08`] PDF/IMAGE IMPORT: CLICKING 'FINISH' DID NOT OPEN 3D + SPLIT VIEW - the process stalled on the last step.** ROOT (case 2 - reached the terminal call, wired to the wrong API): the site-plan overlay Finish (`SitePlanOverlayController.commitProjectNorth` -> `SiteBoundaryMap2D.onPlacementCommitted`) EMITTED `site.overlay-placement-committed`, whose ONLY subscriber is an ephemeral listener inside the ONBOARDING WIZARD. From the always-on GIS launcher / 'Overlay plan/PDF' / a re-entry on an already-onboarded project, the emit hit an EMPTY BUS -> the `ViewController.activate('3D')` + `SplitViewManager.activate()` landing NEVER ran. **The L-258 fix (`2ca3604f`) shipped the landing module `enterCanvasWithSitePlan.ts` but EXCLUDED the one host-wiring line, which sat UNCOMMITTED in the working tree - prod ran the empty-bus version.** Tests passed because they MOCK `onPlacementCommitted`. | Import / geospatial (**C06** import surface; the view/split-view subsystem; **L-258** the landing module; `SitePlanOverlayController`, `SiteBoundaryMap2D`, `enterCanvasWithSitePlan`) | **SHIPPED `7f8d8d08`.** Committed the host wiring: `onPlacementCommitted` -> `onSitePlanPlacementCommitted(runtime)` which emits (for the wizard) AND performs the landing itself (`pryzmActivateBimView('3D')` -> `splitViewManager.activate()` -> `zoomToFit()`), idempotent via `inFlight`. **Bounded the `onEnterCanvas` await (8s race) so a hung texture decode can't stall the landing, and a failure now TOASTS instead of a swallowed `console.warn`.** 12/12 tests (tooth: Finish with a HANGING `onEnterCanvas` still fires the view hooks past the await). | route -> import/site agent. SS-FIX-IMPORT-FINISH-OPENS-EDITOR. |
| L-314 | founder (2026-07-15) - **THE UNIFYING RENDER ROOT** | **[HIGH] ON FIRST LOAD (WebGPU) SHADOWS DON'T CAST + SCENE 'NOT SMOOTH'; SWAPPING WebGPU->WebGL->WebGPU MAKES IT FINE.** Founder: *'created a few walls on webgpu, shadows not casting, scene not smooth; moved to webgl then back to webgpu - AND NOW WORKING FINE!! WHY?'* **ROOT: the PREWARM FAST PATH leaves the pipeline INCOMPLETE.** Console: `[initScene] Phase 5: pre-warmed renderer consumed - 2,401 ms LONGTASK skipped` - the fast path SKIPS a full pipeline construction to make project-open fast, so (a) PSOs compile LAZILY -> first-frame stutter ('not smooth'), (b) the SHADOW PASS is not fully attached/rebuilt for newly-created geometry -> shadows don't cast. **The backend SWAP tears down + fully reconstructs the renderer (`createRenderer` rebuilds every pass, compiles PSOs, attaches shadow pass) - so the swap is doing the FULL BUILD the fast path DEFERRED.** **THIS IS THE UNIFYING THREAD BEHIND MANY RENDER COMPLAINTS** ('shadows not casting', 'not smooth', 'needs a swap', and why device-loss recovery [L-312] and project-switch look correct): the first-load fast-path pipeline is incomplete, and ONLY a full reconstruction settles it. | Rendering (**C04** pipeline construction + scheduling; **P2** single THREE owner; **P3** idle settle via frame scheduler; same subsystem as **L-312**, same 'full build settles it' family as L-301/L-303/L-312 but DISTINCT trigger = prewarm fast-path; `RenderPipelineManager`, `rendererPrewarm`, `ShadowQualityUpgrader`, the shadow pass) | **SHIPPED ec36b556 (2026-07-16) - one-shot _firstLoadShadowReattachPending armed on WebGPU bind(); fires scheduleShadowRebuild() once on the first frame with a caster+mesh (mirrors the WebGPU->WebGL->WebGPU swap). NEEDS LIVE VERIFY: shadows cast on first load without a swap. Was OPEN (HIGH) - routed to the L-312 render agent (renderer-three), AFTER its L-312 fixes.** Explains a whole class of complaints; durable fix removes the need to ever swap. | route -> render agent (renderer-three), 3rd task. **FIX: after the fast-path open, the pipeline SETTLES to full quality ON ITS OWN** - on first idle after project-load (requestIdleCallback / frame scheduler, P3, AFTER first paint, bounded, no 2.4s LONGTASK regression), reconcile to the full pipeline (attach/compile the shadow pass, warm PSOs) so shadows cast + scene is smooth with NO user swap; reuse the reconstruction path the swap uses. **GUARD: after fast-path open + first-idle settle, the SHADOW PASS actually RENDERS (a caster produces a shadow-map) and the pipeline is at full phase WITHOUT a swap. Tooth: distinguish 'shadow flags set' (vacuous, already true) from 'shadow pass renders' (the fix).** SS-FIX-FIRST-LOAD-PIPELINE-INCOMPLETE. |
| L-315 | founder (2026-07-15) - **DEMO TOMORROW** | **[HIGH] SPLIT-VIEW PERFORMANCE IS POOR EVEN WITH A SMALL PROJECT (~68 elements: 26 walls / 18 furniture / 2 levels).** ROOT = **L-307** (the per-gesture full-plan reproject: 3-4 redundant full passes, most discarded), **AMPLIFIED in split view because the PLAN pane is ALWAYS live alongside 3D** - so every edit/selection pays the full plan re-projection (`HiddenLineRemoval` over all 35 occluders / 795 segments + re-injection of EVERY symbol builder: chairs x12, windows x9, stair, roof, columns, wall-layer) WHILE the 3D pane renders. **The cost is FIXED per-gesture, not per-element - which is why it hurts even at 68 elements.** Log also shows the `applied=24/14 layers` double-apply. | Rendering/scheduling (**L-307** the root; **C04** projection invalidation; **C24/C24.1** rooms are not plan linework; **P3**; `ViewTechnicalDrawingCache._onStoreChange`, `PlanViewManager`, `ViewDependencyTracker`, `VGSceneApplicator`) | **SHIPPED `139ae769` (SAFE fix); risky fix DEFERRED.** One surgical line in `ViewTechnicalDrawingCache._onStoreChange` (`if (elementType==='room') return;` after the annotation cut) - room writes no longer force a full plan reproject (removes 1-2 of the 3-4 passes/gesture). 4 tests red-first (tooth: a room write dispatches ZERO `vd:projection-stale`; a wall write still dispatches one). **FAVORABLE FINDING: pure SELECTION does NOT reproject** (SelectionBus emits no store events; PlanViewManager repaints the CACHED drawing) - so split-view waste is purely the geometry-gesture burst (Fix 1). **DEFERRED to post-demo:** the VGSceneApplicator `applied=N/14` layer double-apply (3 categories share a base DXF layer, last-write-wins) needs a PRECEDENCE/MERGE POLICY decision (contract), not a naive dedup. **ORIGINAL:** Given the demo is TOMORROW, ONLY the low-risk fix ships now; the scheduler-timing coalesce (the big win) is held for post-demo authorization - a projection change that breaks the plan is worse than slow perf. | route -> perf agent (SURGICAL). **IMPLEMENT Fix 2 ONLY (low-risk, ~20-30%): room-store changes must NOT invalidate the edge-projection drawing** (add a `room` skip in `ViewTechnicalDrawingCache._onStoreChange` mirroring the `annotation:*` cut - rooms contribute no linework). **DO NOT touch the scheduler timing (Fix 1, ~50-65%) - propose for post-demo.** Optional trivial: dedup the 3 shared-base-layer double-styles (curtain-wall+wall->A-WALL etc.). **GUARD (tooth): a ROOM write triggers ZERO plan reprojection (no `vd:projection-stale` elementType=room); a WALL write still does (fence).** Investigate whether pure SELECTION dirties the plan in split view (report, don't fix). SS-FIX-SPLIT-VIEW-PERF-SAFE. |
| L-316 | founder (2026-07-15) - **recurring across project switches** | **[HIGH] STARTING ANOTHER PROJECT IN THE SAME SESSION IS 'NEVER CLEAN' - the RENDER PIPELINE'S GPU STATE LEAKS ACROSS THE SWITCH (the DATA is clean).** `[ProjectIsolationAudit] ✓ project loaded clean - no leftover state` (data OK), but a flood of RENDER errors on switch: `Destroyed texture [ShadowDepthTexture] used in a submit` (old shadow depth texture disposed but the shadow PASS still references it mid-submit), `GL_INVALID_FRAMEBUFFER_OPERATION ... Framebuffer is incomplete: Attachment has zero size` (render targets not reallocated for the new project), `GL_INVALID_OPERATION: Mismatch between texture format and sampler type (shadow)` (stale shadow sampler binding). On switch `[RenderPipelineManager] onProjectSwitch` clears OUTLINE refs + retry counter but NOT the shadow pass depth texture nor the framebuffers/render-targets. | Rendering (**C04** pipeline lifecycle across a reconstruction boundary; **C13** the RENDER side of project-switch isolation - complements the data-side `ProjectIsolationAudit`; **P2/P3**; SAME 'reconstruction must fully reset GPU state' family as **L-303/L-312/L-314**, DISTINCT trigger = project-switch; `RenderPipelineManager.onProjectSwitch`, shadow pass, render targets; §SHADOW-DISPOSE-DEFER / §SHADOW-DEVICE-LOSS-FIX) | **SHIPPED `e9f240ad`.** onProjectSwitch now treats the switch as a reconstruction boundary: `_reconcileRenderSize()` heals the zero-size framebuffer + `scheduleShadowRebuild()` routes shadow-pass reconstruction through the guarded cycle (pauses submits + freezes the shadow map) so the OLD ShadowDepthTexture is disposed with NO submit referencing it. No-op-safe on WebGL. 4 tests. ORIG: Part of the UNIFYING render root: every reconstruction boundary (device-loss, first-load, PROJECT-SWITCH) must fully reset GPU resources. | route -> render agent (renderer-three), 4th task. **FIX: treat project-switch as a first-class reconstruction boundary - on `onProjectSwitch` fully reset+reallocate the shadow pass depth texture + render targets to the current size, disposing the old resources SAFELY (deferred past the submit, never mid-submit - reuse §SHADOW-DISPOSE-DEFER).** Route through the same settle/reconcile path as L-314 if generalized. **GUARD: after a project switch, NO shadow-pass reference to a disposed texture (deferred-disposed), render targets reallocated to current size (no zero-size FBO), sampler matches the new format. Tooth: distinguish 'onProjectSwitch ran' (vacuous) from 'shadow pass + framebuffers actually reset+reallocated'.** Demo-tomorrow: if full fix risky, ship the SAFE part (defer the shadow dispose past submit) + propose the rest. SS-FIX-PROJECT-SWITCH-GPU-STATE-NOT-RESET. |
| L-317 | founder (2026-07-15) - **DEMO TOMORROW, backend tradeoff** | **[CRITICAL] WEBGL2: GEOMETRY DOUBLES UP / GHOSTS ON NAVIGATION (+ slow nav); WEBGPU: clean but chunky.** Founder tried WebGL - fast + stable but on rotate the geometry DOUBLES (screenshot: roof offset from walls, doubled wall edges). Console: `§PERF-WEBGL2-NO-TSL WebGL2 backend detected - Lightweight WebGL render path active`; `§PERF-WEBGL2-RENDER-ON-MOVE ... §FIX-WEBGL2-GHOST-ON-ROTATE per-frame OBC base clear ARMED` - **the mitigation is armed but INCOMPLETE.** Two render layers composite on WebGL2 (OBC/ThatOpen base + PRYZM/Pascal); on rotate BOTH show (stale base, or two passes at slightly different camera → the consistent OFFSET points at a two-pass camera/timing mismatch, not just a stale buffer). | Rendering (**C04** compositing/scheduling; **P2** single THREE owner; **P3**; the WebGL2 sibling of the WebGPU render family L-312/L-314/L-316; `RenderPipelineManager` §PERF-WEBGL2-NO-TSL / §PERF-WEBGL2-RENDER-ON-MOVE / §FIX-WEBGL2-GHOST-ON-ROTATE, the OBC base composite) | **SHIPPED `b8fce8cd`.** ROOT: the WebGL2 lightweight overlay cleared to TRANSPARENT (`setClearAlpha(0)`), so the frozen OBC base canvas underneath re-composited its LAST frame (camera Cn-1) THROUGH the overlay's transparent pixels while the live overlay drew at Cn - two camera states = doubled/offset geometry that settles when motion stops. WebGPU was clean because its per-frame base clear isn't armed. FIX: render the WebGL2 overlay OPAQUE (clear to theme bg, alpha 1) - the base cannot bleed through at all. 5 tests. **WebGL is now the smooth, correct backend.** ORIG: THE STRATEGIC CALL: fixing the ONE contained WebGL2 compositing bug gives a smooth demo backend FASTER than fixing the whole WebGPU reconstruction family (WebGL is already fast + stable on the founder's machine). | route -> render agent (renderer-three), FIRST. **FIX: on WebGL2 the base+overlay must composite from a SINGLE cleared frame at a SINGLE camera state each frame - clear BOTH color AND depth of the base target before the pass, OR drive both passes from the same camera so they don't offset; if two renderers double-draw the same geometry, only ONE draws it on WebGL2.** Investigate WebGL2 nav slowness (per-frame OBC base clear / culling-instancing inactive?) - cheap win only. **GUARD: after a camera rotation the frame contains the geometry ONCE (base target cleared color+depth each frame; both passes same camera matrix). Tooth: old path leaves stale/mismatched → doubling; fix → single image.** SS-FIX-WEBGL2-GHOST-ON-ROTATE-INCOMPLETE. |
| L-318 | founder (2026-07-15) - **RECURRENT** | **[HIGH] CLICKING 3D GLOBE / GIS NAVIGATES THE USER OUT TO THE PROJECT PAGE (and the app re-boots).** Console after the click is a FRESH BOOT (`[sw] registered`, ConstraintEngine re-registered, new project id) - i.e. it routed to /projects and hard-reloaded. **Downstream of L-313:** founder is on bundle `BzIL_aVs` which HAS the L-313 fix (the `.scene` stale-viewer crash + fail-to-retry), so the HANG is gone - but now a globe-activation FAILURE ESCAPES to the ROUTER / a global error-boundary / a crash-guard reload, ejecting the user to the hub instead of staying in the editor. | Geospatial / routing (**C06** GIS surface; the view-activation-loading overlay contract - failure -> retry IN PLACE, never navigate out; **P4**; downstream of **L-313**; `GISAreaLayout.toggleGIS`, `bridge.activate`, the router/error-boundary/unhandled-rejection handler) | **SHIPPED `b5f69afc`.** ROOT confirmed (b)/(c): the globe error ESCAPES `ViewportCrashGuard` (which only catches errors matching RENDER keywords - that's why the `TSL module not loaded` render-keyword rejection IS swallowed but a Cesium `.scene` error is NOT) -> unhandled -> `SceneCrashFallback`'s 'Back to projects' `<a href="/">` / vite `preloadError` auto-reload -> the hash router defaults to the hub. FIX: new `containViewActivation(step,onFail,label)` helper wraps EVERY globe/site entry (segment click, overlay retry, onboarding, direct nav-rail re-entry, the sync `toggleGIS(true)` throw in both orchestrators) - catches any throw/rejection, surfaces the L-313 in-editor retry, NEVER re-propagates, always resolves. No globe error can become an unhandled rejection -> never reaches the crash-guard/reload/hub path. 4 tests (tooth: a rejecting/throwing activation surfaces retry + invokes NO navigation/reload API). **GIS is now demo-safe.** | route -> geospatial agent. **Determine the navigate-out mechanism: (a) explicit router.push('/projects') in a catch, (b) unhandled error/rejection caught by a global boundary defaulting to the hub, or (c) a crash-guard hard reload** (the fresh-boot console points at b/c). **FIX: contain the globe error at the GIS activation boundary - catch locally, route to L-313's `activeViewActivation.fail(...)` retry overlay, NEVER let it reach the router/global handler; no navigation, no reload.** GUARD: a failing globe activate shows the in-editor retry and invokes NO navigation/reload API (tooth: distinguish 'activate was called' from 'a failing activate keeps the user in-editor with retry'). SS-FIX-GLOBE-CLICK-NAVIGATES-OUT. |
| L-319 | founder (2026-07-15) - **likely the SOURCE of the L-318 eject** | **[HIGH] `[ScenePass] TSL module not loaded. Call initTSL() before createScenePass().` - an UNHANDLED PROMISE REJECTION in the render context, thrown during a batch when auto-performance-mode rebuilds SSGI.** Stack: `_setSsgi -> _enablePerfMode -> autoEnablePerf -> _onBatchStart -> runBatch`. **A pipeline-ORDERING race: `createScenePass()` is called before `initTSL()` has run** (the TSL module isn't loaded yet). Caught+swallowed by `ViewportCrashGuard` HERE (viewport kept alive), but it is EXACTLY the class of unhandled render rejection that ESCAPES the guard on the globe path and EJECTS the user (**L-318**). **Same family as L-314** (pipeline not fully initialized / init-ordering). | Rendering (**C04** pipeline init ordering + scheduling; **P2**; **P3**; SAME init-ordering family as **L-314**; likely the unhandled-rejection SOURCE behind **L-318**; `initTSL`, `createScenePass`, `_setSsgi`/`autoEnablePerf`, ViewportCrashGuard) | **SHIPPED ec36b556 (2026-07-16) - guarded all 3 createScenePass callers (_buildPipeline/_buildPhase3Pipeline/_fullRebuild) on _tslLoaded so the batch autoEnablePerf window cannot call createScenePass before initTSL. 232/232 + 7 new guards green. Was OPEN (HIGH) - routed to the render agent (renderer-three), fold into L-314 (init ordering).** | route -> render agent. **FIX: `autoEnablePerf`/`_setSsgi`/`createScenePass` must GUARD on `initTSL()` having completed - either await/ensure TSL is loaded before building a ScenePass, or skip the SSGI rebuild until the TSL pipeline is ready (it is a perf-mode upgrade, not load-bearing).** No unhandled rejection may escape the render pipeline. **GUARD: calling autoEnablePerf/_setSsgi before initTSL completes does NOT throw an unhandled rejection - it defers/skips until TSL is ready. Tooth: distinguish 'the error was swallowed' (ViewportCrashGuard, vacuous) from 'the pipeline never attempts createScenePass before initTSL'.** SS-FIX-TSL-NOT-LOADED-BEFORE-SCENEPASS. |
| L-321 | founder (2026-07-15) - **office batch pipeline** | **[HIGH] THE OFFICE BUILDING 3D VIEW STILL SHOWS A SOLID GREY ENVELOPE / MASSING SHELL over the real per-floor geometry** - screenshot: a solid 25-storey grey tower with only the ROOF level detailed (curtain wall + railing), body a monolith. The log confirms the REAL geometry IS built (25 storeys of walls, 550 curtain windows punched, floors, rooms, curtain walls). **Founder: 'this issue was occurring in OTHER INSTANCES and WAS SOLVED - but in the OFFICE batch pipeline it is still present - we document that in the contracts.'** So the envelope-disposal fix that works for house/apartment/resi was NOT inherited by the office pipeline. | Typology pipeline (**C11** element-creation; the typology-pipeline contract; the envelope/massing ADR/spec; memory reuse-residential-house-pipeline-patterns [office MUST mirror resi+house], envelope-reject-silent-fallback; `OfficeBuildingExecutor`/`pryzmGenerateOfficeBuilding`, the house/resi executors' envelope disposal; possible confound: `LevelScoped3DCullingService` massing-LOD auto-escalation on >=15 levels) | **SHIPPED `b61f6720`.** ROOT: candidate (B) - it is NOT an envelope element (the office pipeline emits only real geometry). It is the MASSING LOD: `LevelScoped3DCullingService.isHeavyModel()` auto-escalates any model >=15 levels AND >=1000 elements (or >=4000) to `'massing'` mode; `LevelMassingRenderer` draws each out-of-scope storey as an opaque grey block (`0xb9c0cc`) - the L-169 comment literally calls it the '40-storey office envelope shell'. House/apartment/resi never trip it (under 15 storeys - the L-164 gate). FIX: `OfficeBuildingExecutor` calls `showOfficeFullDetail()` -> `setMode('all')` (the service's documented override) after a successful build; safe now that the office pipeline removed the device-loss aggravators (perimeter coarsen, shared glazing systemType, skipPbrUpgrade). Reversible via the viewport '3D detail' control. 2 tests. **CAVEAT: full detail on a 25-storey office is heavier - if slow/unstable on the demo machine, toggle 3D detail back or demo a smaller office; the massing-LOD device-loss protection is now off for the office (L-312B PSO-storm mitigation not yet shipped).** | route -> office-generator agent. **READ the envelope contract + how the house/apartment/resi executors DISPOSE the envelope after the detailed building lands; apply the SAME mechanism to the office executor (do NOT invent a new one - office MUST mirror resi/house).** Disambiguate (A) generator leaves envelope vs (B) massing-LOD auto-escalation. **GUARD: after `pryzmGenerateOfficeBuilding`, NO massing-envelope element/mesh remains (or its visibility is off); the detailed floors are the only geometry. Tooth: distinguish 'the building was generated' (vacuous) from 'the envelope is gone after generation'.** Contract-mapped (C11 / typology contract - when code disagrees with the contract, the code is wrong). SS-FIX-OFFICE-ENVELOPE-NOT-DISPOSED. |
| L-320 | founder (2026-07-15) - **recurrent across project switches** | **[HIGH] STARTING A NEW PROJECT SHOWS FLOOR FINISHES AND RAILINGS (handrails) FROM THE PREVIOUS PROJECT - leftover 3D geometry.** The DATA is clean (`ProjectIsolationAudit ✓ loaded clean - no leftover state`), but certain element BUILDERS do not dispose their scene meshes on project switch. Log: ONLY `[ProjectIsolation] WallFragmentBuilder disposed - scene cleared of wall geometry` - the FLOOR-FINISH and HANDRAIL/RAILING builders do NOT log a disposal -> their meshes persist into the new project. Plus a FLOOD of `[ViewportCrashGuard] §I3 suppressed ... reading 'usedTimes'` DURING `ClearProjectCommand` (the builders that DO dispose tear down GPU resources MID-SUBMIT - the L-303 family). | Isolation / rendering (**C13** project-switch isolation - the GEOMETRY side, complementing the data-side `ProjectIsolationAudit`; **C04** dispose is a frame event - deferred past submit; **C11**; same safe-dispose family as **L-303/L-316**; DISTINCT root = builders OMITTED from the isolation teardown; `ProjectIsolation`, `ClearProjectCommand`, `FloorFinish`/`Handrail`/`StairRailing` builders) | **SHIPPED `30336ec5`.** ROOT: the `bim-project-cleared` teardown (initTools) disposed ONLY `WallFragmentBuilder`; `FloorPanelBuilder` + `HandrailFragmentBuilder` + `StairRailingBuilder` were OMITTED, and their raw `dispose()` RE-THREW the WebGPU `usedTimes` device-loss error, ABORTING the teardown mid-loop -> roots left in the scene + the builder registry Map -> bled into the next project. FIX: register all three in the same teardown, added missing `dispose()` to the railing builders, routed through `safeDisposeObject3D` (deferred-safe, reused from the wall builder) so `usedTimes` can't abort the sweep. 6 tests red-first (assert on SCENE+registry, not store). **NOTE: this commit also carries the REVIEWED-AND-APPROVED L-266 door/window symbol-parity fix** (a pre-existing uncommitted `initTools.ts` hunk, unbundle-able in this env) - it routes BOTH plan+3D door/window record construction through the ONE `buildDoorStoreRecord`/`buildWindowStoreRecord` chokepoint (C11 §3 parity by construction), killing the plan-defaults-`wt-single-pane`-vs-3D-`wt-timber-casement` divergence = the founder's 'window parity not correct'. Non-fatal on failure (caught). | route -> isolation agent. **Every element builder that adds scene geometry MUST be disposed on project switch - register the FLOOR-FINISH + HANDRAIL/RAILING builders (audit ALL builders) in the SAME teardown that disposes WallFragmentBuilder, disposing SAFELY (deferred past submit, mirroring §SHADOW-DISPOSE-DEFER) so it doesn't add to the `usedTimes` crash.** GUARD on the SCENE: after a switch, the floor-finish + handrail builders hold ZERO meshes. **Tooth: distinguish 'the store was cleared' (vacuous - data was already clean) from 'the builder's scene meshes are gone' (the bug the founder SEES).** SS-FIX-BUILDER-ISOLATION-LEAK. |
| L-310 | founder (2026-07-14/15) | **[HIGH] CREATE OPENING (hole in slab) STOPPED WORKING - the command succeeds but the hole does not appear, and a WebGPU geometry error fires.** Console: the OpeningTool activates on a slab, 2-point + polyline draw, `[CommandManager] EXECUTE: CREATE_OPENING` -> `Opening created ... on slab` (the record IS created) - BUT no slab re-mesh follows and `THREE.AttributeNode: Vertex attribute "position" not found on geometry` (x3) fires on tool activation. So the opening RECORD lands but the slab is not re-CSG'd with the void, and a geometry with no `position` attribute reaches the WebGPU/TSL path. **Likely WebGPU-specific** (the `position`-not-found is a TSL error) and **likely related to L-300** (the slab has TWO `holes` fields in different coordinate spaces - the opening may write the field `SlabFragmentBuilder` does NOT triangulate -> zero-area/absent hole). | Geometry / rendering (**L-300** two slab-holes coordinate spaces; **C11**; **L-127**; `OpeningPlanToolHandler`, `CreateOpeningCommand`, `SlabFragmentBuilder`, `openingStore`/`SlabData.holes`; the WebGPU `position`-not-found is TSL/renderer-adjacent) | **SHIPPED 2111a5e2 (2026-07-16) - data/rebuild path verified sound (CreateOpeningCommand->triggerRebuild->SlabFragmentBuilder triangulates both plain+layered); hardened buildSlabGeometry to drop degenerate hole loops + try/catch rebuild-without-offender so a bad opening never aborts the re-mesh. 4/4 guard. REMAINING (render dep, routed): Vertex-attribute-position error x3 on OpeningTool activation = empty preview geometry in OpeningPlanToolHandler/TSL (WebGPU-only). Was OPEN (HIGH) - NOT yet dispatched (post-demo; likely works on WebGL).** The `position`-not-found is WebGPU-only, so on the WebGL demo backend the opening may render - founder should TEST on WebGL first. | route -> geometry/opening agent (post-demo). **Trace: does CREATE_OPENING trigger a slab re-mesh, and does it write the hole to the field `SlabFragmentBuilder` triangulates (the L-300 legacy `SlabData.holes` / `openingStore`, not the L1 `Slab.holes`)? Fix the empty-`position` geometry reaching TSL (a degenerate/zero-area hole produces empty BufferGeometry).** GUARD: after CREATE_OPENING the slab re-meshes with a visible void; no geometry lacking a `position` attribute reaches the renderer. SS-FIX-CREATE-OPENING-NO-SLAB-REMESH. |
| L-322 | founder (2026-07-15) - **revealed once L-321 removed the envelope** | **[HIGH] THE OFFICE TOWER IS HOLLOW - NO PER-STOREY SLABS (floor plates).** With the L-321 grey envelope gone, the 3D shows the structural frame + curtain-glass skin but NO floor between storeys - you can see straight through it. Log: 23 storeys of walls/windows/doors/curtain-walls/rooms, but `CREATE_FLOOR` + `§OFFICE-CORE-WELLPROPORTIONED - laid 16 floor finish(es) on L0` appear ONLY for L0 -> only the ground floor gets FLOOR FINISHES; the upper 22 storeys have no structural SLAB. **The envelope was hiding this pre-existing office-pipeline gap.** | Typology pipeline (**C11** element-creation; office MUST mirror resi/house per-storey slab creation - memory reuse-residential-house-pipeline-patterns; DISTINGUISH structural SLAB [storey plate] from FLOOR FINISH [room-scoped finish]; `OfficeBuildingExecutor`, the resi/house per-storey slab loop; possible confound: slabs created but not rendered via the batch/L-320 builder path) | **SHIPPED 25a7fc49 (2026-07-16).** Root disambiguated: NOT missing slabs (b, not a) - one CreateSlabCommand/storey already runs; the gap was a visible FINISHED floor on only the first detailed level. Fix mirrors ResidentialBuildingExecutor._finishPublicFloors: new pure officeStoreyFloors.ts buildStoreyFloorPlates() + _finishStoreyFloors dispatches one CreateFloorCommand/non-detailed storey (holes cut over open stairs). Guard officeStoreyFloors.test.ts (3 tests) green; root tsc clean; fenced to 3 office files. CAVEAT: if bare structural slabs still do not render at runtime, that is a geometry-slab deferred-build issue outside the office fence. | route -> office-generator agent. **Determine (a) slabs created only on L0/subset vs (b) created per-storey but not rendered; then create a structural SLAB per storey (minus intentional atrium voids over open stairs), mirroring the resi/house per-storey slab loop.** GUARD: after `pryzmGenerateOfficeBuilding({stories:N})`, ~N storey slabs exist (one per level minus voids), not just L0. **Tooth: distinguish 'L0 has a floor' (current vacuous state) from 'every storey has a floor plate'.** SS-FIX-OFFICE-MISSING-PER-STOREY-SLABS. |
| L-323 | founder (2026-07-16, 23-storey office GIS) | **[MEDIUM] THE "3D SITE" (FORMA) VIEW IS SLOW TO GENERATE - THREE COMPOUNDING COSTS, ALL CONFIRMED IN CODE.** **(1) THE MODEL IS SERIALISED TO A ~40 MB GLB *TWICE* PER GIS SESSION.** The photoreal GLOBE path calls `exportFragmentsToGLB(scene)` (`GISAreaLayout.ts:1952`, real materials) and the Forma "3D Site" path calls `exportFragmentsToGLB(scene, { formaWhite: true })` (`GISAreaLayout.ts:2111`, white study tint). **Separate signature caches** (`globeRealLastSig` vs `formaRealLastSig`) + different-tinted output mean neither reuses the other - so the 1523-root geometry walk runs TWICE (log: `Blob size: 41560508` then `40770608`). The geometry walk (the expensive part) is byte-identical; only the material tint differs. **ROOT: ADR-0093 baked the Forma-white tint into a GLB-EXPORT OPTION rather than a VIEW-side material override - so a pure PRESENTATION concern forces a second full serialisation.** **(2) OVERPASS CONTEXT OVER-FETCH.** `context buildings: 13974 OSM footprint(s)` fetched + parsed for the far bbox, then `§FEAT-FORMA-CONTEXT-EXTENT-LOD` RENDERS ONLY 900 - ~14k downloaded/parsed to draw 900, plus 2921 road ways + water, over sequential `/api/overpass` round-trips with NO cache shared across the globe->forma re-entry. **(3) THE 25 s "tiles" STALL.** `site - readiness NEVER ARRIVED at stage "tiles" (no progress for 25000 ms)`. The readiness watchdog gates on `snap.tilesLoaded && outstanding === 0` (`viewActivationLoading.ts:251`), but the Forma flat-ground study runs on a KEYLESS ELLIPSOID with no terrain provider (`terrain clamp degraded`) - tiles never finish streaming, so the overlay sits until the stall timeout fires. On a flat-ground study, tile-streaming should not be a readiness gate. | GIS / geospatial (**C06** GIS surface; **ADR-0093** forma-white model; **P7** presentation intent != data; **C04** scheduling; **§FEAT-VIEW-ACTIVATION-LOADING-OVERLAY**; `GISAreaLayout.ts:1952/2111`, `exportFragmentsToGLB`, `viewActivationLoading.ts:251`, `§FEAT-FORMA-CONTEXT-EXTENT-LOD`) | **PARTIAL — FIX B SHIPPED 68532611 (roads/water Overpass in-flight dedup); FIX A (single export) DEFERRED next-wave: Cesium.Model has only ONE uniform color, cannot reproduce per-primitive glass/opaque — clean design = exporter TAGS primitives glass|opaque in glTF extras (data-side, no tint) → ONE untinted export → Cesium CustomShader whitewashes opaque, keeps glass translucent (P7/ADR-0093-faithful, multi-file, needs live visual verify). Was OPEN (MEDIUM) - DEMO-VISIBLE (slow "3D Site" generation on the heavy office), NOT a crash.** The site view is SOUND once generated; the cost is entry latency. **Not hot-patched the night before the demo - it is a real change to the GIS surface (single-export + context cache + readiness-gate).** Deferred behind the office slab work (L-322) unless the founder prioritises. | route -> **GIS/geospatial agent.** **(A) SINGLE EXPORT:** move the Forma-white tint OUT of the GLB export and onto the Cesium primitive at placement (a VIEW material override, ADR-0093 conflict-resolution: presentation != data, P7) - one signature-keyed export then serves BOTH globe and Forma (halves the local cost). **(B) CONTEXT CACHE + FETCH CAP:** cap the Overpass FETCH (not just the render) to the LOD extent, and share one signature-keyed context cache across globe<->forma re-entry. **(C) READINESS GATE:** on a keyless/no-terrain flat-ground study, do NOT gate readiness on tile streaming (resolve `waitForTiles` immediately when no real tile provider is attached) - the 25 s stall is a false wait. **GUARD: a single GIS session that shows BOTH globe and Forma performs exactly ONE `exportFragmentsToGLB`; Overpass fetch count <= rendered-footprint cap; the flat-ground Forma study reports READY without a tiles stall.** SS-FIX-FORMA-SITE-SINGLE-EXPORT-AND-CONTEXT-CACHE. |
| L-324 | founder (2026-07-16, 23-storey office) - **RECURRENT; the "recovery render issue" that ends in a stuck "RECOVERING RENDERER..." overlay** | **[CRITICAL] THE DEVICE-LOSS RECOVERY LOOP THRASHES GPU-CONTEXT CREATION UNTIL THE BROWSER BLOCKS ALL CONTEXTS FOR THE PAGE, LEAVING A TERMINAL "RECOVERING RENDERER..." HANG WITH NO EXIT.** The chain, in order from the log: **(1) THE LIVE BACKEND SWAP IS ITSELF THE TRIGGER** - `§RENDERER-LIVE-SWAP live swap complete - backend now: webgpu` is immediately followed by `WebGPU Device Lost: A valid external Instance reference no longer exists.` On the heavy office the swap creates a new GPU device while the old scene's resources are still live -> instant loss. **(2) CESIUM CASCADE** - `§FIX-WEBGPU-DEVICE-LOSS-CESIUM-CASCADE` fires; Cesium is disposed + re-mounted MID-RECOVERY and FAILS (`INVALID_OPERATION: delete: object does not belong to this context`, `Error constructing CesiumWidget`, `The browser supports WebGL, but initialization failed`) - each re-mount spends a context-creation attempt. **(3) DEVICE-LOSS CAP HITS 2/2** - `§FIX-HEAVY-SCENE-3D-SCALABILITY device-loss cap reached (2/2) - switching to STABLE WebGL safe-mode`, the intended safety net. **(4) THE SAFETY NET IS ALSO BLOCKED** - `THREE.WebGLRenderer: A WebGL context could not be created. Reason: Web page caused context loss and was blocked`. By now Chrome has BLACKLISTED the page's GL contexts, so NEITHER WebGPU NOR the WebGL2 safe-mode can acquire a context. **(5) NO TERMINAL STATE** - the swap rolls back (`§RENDERER-LIVE-SWAP swap failed - rolling back`) and RPM even completes a shadow rebuild, but the **"RECOVERING RENDERER..." overlay is never dismissed** because the recovery-complete signal never fires on the blocked/failed path -> the founder sees a frozen recovery screen with no way forward but a manual reload. | Rendering (**C04** scheduling; **P2** single THREE owner; **P3** single rAF; same family as **L-301/L-303/L-312**; `§RENDERER-LIVE-SWAP`, `RendererBackendToggle`, `§FIX-HEAVY-SCENE-3D-SCALABILITY`, `§FIX-WEBGPU-DEVICE-LOSS-CESIUM-CASCADE`, `createRenderer`, `RenderPipelineManager`) | **SHIPPED 855c641a+37edbecb (2026-07-16) — renderer terminal-reload state + swap reconstruction-boundary + Cesium re-mount gate. was OPEN (CRITICAL) - RECURRENT, and the terminal form of the L-312 device-loss family.** DEMO-CRITICAL: it strands the whole viewport. **DEMO MITIGATION (no code): stay on WebGL from the start and DO NOT toggle the backend - the trigger here is the live WebGL->WebGPU swap on the heavy office.** WebGL is the stable demo backend on this Windows box (memory: render-reconstruction-boundary + localhost-dev-unusable). | route -> **render-crash agent (L-301/L-312 family).** **(A) ROOT FREQUENCY = L-312B PSO-COMPILE STORM** (per-element unique CW materials mint thousands of pipelines in one flush -> device loss). Durable fix: share CW materials per (level,kind) via §INSTANCE-MAT-SHARE + `rpm.setSuspended()` around the batch. **(B) SWAP IS A RECONSTRUCTION BOUNDARY** - the live backend swap must FULLY QUIESCE + dispose the OLD device before creating the new one (never two live devices overlapping); block the swap outright while a device-loss recovery is in flight. **(C) RECOVERY MUST HAVE A TERMINAL STATE** - once the browser reports `Web page caused context loss and was blocked`, STOP all context-creation attempts (WebGPU AND WebGL) and surface ONE honest "reload required" action instead of an infinite "RECOVERING RENDERER..." overlay; the 2/2 cap counts WebGPU losses but ignores that the WebGL2 fallback is ALSO blocked. **(D) CESIUM RE-MOUNT** must NOT attempt a fresh WebGL context while the page is in the blocked state - skip re-mount when renderer recovery has hit its terminal cap. **GUARD: (1) a live backend swap on the heavy office never overlaps two GPU devices; (2) after the browser blocks contexts, the overlay resolves to a single reload CTA within one recovery cycle - never hangs; (3) Cesium does not re-mount during a blocked/terminal recovery.** SS-FIX-RECOVERY-LOOP-TERMINAL-STATE. |
| L-325 | founder (2026-07-16, new project right after the office tower) - **RECURRENT: "project isolation is still not working"** | **[CRITICAL] A NEW PROJECT INHERITS THE PREVIOUS PROJECT'S ELEMENTS IN THE PROJECTION/CULLING REGISTRIES WHILE ITS OWN 3D SCENE IS EMPTY - THE TWO ISOLATION DOMAINS DIVERGED.** Symptoms + the smoking gun from the log: the new project's `[ProjectSerializer] Snapshot created: 10 elements` (9 walls, 1 room) - **but `[NativeElementMeshExporter] Plan view - exporting 117 elements`** and **`[FrustumCullingService] Audit complete - 878 element(s), 1672 mesh(es)`**. 107 plan elements + 868 culling elements are STALE - the office tower is still resident. Confirmed source: `NativeElementMeshExporter.ts:254` iterates `elementRegistry.getAllRoots()`, and `FrustumCullingService` audits the same global registry - **so the plan projection + culling still see the OLD project.** On screen: plan view shows the new room PLUS reminiscence window-symbols from the office (founder's red marks); **the 3D view is EMPTY and the ProjectOrigin (blue sphere) never appears** - i.e. the 3D SCENE ROOT was cleared but the ELEMENT/PROJECTION REGISTRIES were not, and the new project's 3D geometry + origin were never (re)built. **`ClearProjectCommand.ts:81` DOES call `elementRegistry.clear()` - so the explicit Clear path is correct; the "new project" entry path the founder used BYPASSES that chokepoint (or repopulates after it).** **STRONGLY COUPLED TO L-324: this new project was created immediately after the recovery-render meltdown - a new project created against a torn/blocked renderer skips or corrupts the full teardown+reconstruction.** | Isolation / lifecycle (**C13** project lifecycle + isolation - the RENDER/PROJECTION side, distinct from the DATA-store side that `ProjectIsolationAudit` already audits clean; **P1** single composition root; same family as **L-316/L-320**; `elementRegistry.getAllRoots`, `NativeElementMeshExporter.ts:254`, `FrustumCullingService`, `ClearProjectCommand.ts:81`, `initProjectOrigin`, `ProjectLifecycleController`, `projectOriginIsolation.test.ts`) | **SHIPPED e020fe18 (2026-07-16) — pryzm-project-switch render-side teardown chokepoint purges elementRegistry+NME+FrustumCulling+TechDrawingCache + re-seats ProjectOrigin + C13 dev-assert. Root: ElementRegistry.clear() does not fire onUnregister so NME/culling caches stayed resident. was OPEN (CRITICAL) - RECURRENT and DEMO-CRITICAL: a fresh project is unusable (empty 3D, no origin, ghost plan symbols).** The founder has flagged project isolation repeatedly. **DEMO MITIGATION: RELOAD THE PAGE between projects (a hard reload runs the full boot teardown), and do NOT create a new project immediately after a render-recovery event (L-324).** | route -> **isolation/render agent (C13 family, L-316/L-320 lineage).** **(1) ONE TEARDOWN CHOKEPOINT FOR EVERY PROJECT-ENTRY PATH** - new/create/switch/import must ALL route through the SAME C13 teardown that `ClearProjectCommand` runs (P1: one path, not per-entry-point). **(2) THE CHOKEPOINT MUST PURGE THE RENDER/PROJECTION REGISTRIES, not just the data stores:** `elementRegistry.clear()`, FrustumCulling registry reset, `NativeElementMeshExporter` proxy cache, `EdgeProjectorService` cache, `ViewTechnicalDrawingCache.clear()`. **(3) IT MUST RE-SEAT THE PROJECTORIGIN** in the new 3D scene (the blue sphere) - covered by intent in `projectOriginIsolation.test.ts`, evidently not firing on this path. **(4) IT MUST BE ROBUST TO A MID-RECOVERY RENDERER (L-324):** sequence teardown -> renderer settle -> reconstruct -> origin; never build the new project against a blocked/recovering device. **(5) ADD A DEV-ASSERT/GATE:** after a project switch, `elementRegistry` root-count == the new snapshot's element-count (117 != 10 must fail loudly, mirroring `ProjectIsolationAudit` on the render side). **GUARD: creating a new project after ANY prior project leaves elementRegistry/culling/NME/EdgeProjector/technical-drawing holding EXACTLY the new project's elements; the ProjectOrigin renders; the 3D view shows the new geometry; plan view shows NO reminiscence symbols.** SS-FIX-PROJECT-ISOLATION-PROJECTION-REGISTRIES. **[LOAD-PATH FOLLOW-UP 2026-07-16]** The shipped L-325 dev-assert FIRED on a real reload: `[C13 VIOLATION] §L-325 render-registry isolation leak on load … elementRegistry holds 895 root(s) but the snapshot declared 1246 — 132 FOREIGN root(s) from a prior project`. GOOD: the guard is live in prod and catches the leak loudly (as designed). GAP: the purge wired on `pryzm-project-switch` does NOT fully clear on the OPEN-SAVED-PROJECT (load) path — foreign roots survive into `pryzm-project-loaded`. Follow-up: route the SAME render-registry purge (elementRegistry/NME/culling/tech-drawing) through the load chokepoint before hydrate, so opening a saved project starts from an empty registry. SS-FIX-PROJECT-ISOLATION-LOAD-PATH-PURGE (extends L-325). |
| L-326 | founder (2026-07-16, after office + residential batch generation) | **[MEDIUM] THE MAIN 3D VIEWPORT BACKGROUND TURNS GREY (should be white) ON HEAVY BATCH SCENES - because THE WHITE BACKGROUND IS WEBGPU-ONLY AND THE HEAVY BATCH DROPS THE RENDERER TO THE WEBGL2 FALLBACK, WHICH NEVER PAINTS IT.** The white viewport is produced by the **WebGPU TSL background uniform** (`BackgroundUniform.ts:42` `LIGHT_BG_HEX='#ffffff'`), and on WebGPU `RenderPipelineManager` deliberately NULLS `scene.three.background` (`:1779`) + clears transparent (`:1783` `setClearColor(0x000000, 0)`) so the TSL bgUniform owns the fill. **But the office/residential batch generation trips the device-loss cascade (L-312/L-324 family) into the WebGL2 FALLBACK backend** (screenshot GPU bar: `webgl-fallback`), **where the TSL pipeline does not run - so nothing paints the white, and the transparent clear over the grey page/container shows through as GREY.** The opaque white-fill DOES exist (`:636` `setClearColor(this._lightweightBgColor=LIGHT_BG_HEX, 1)`) but ONLY on the `§PERF-WEBGL2-RENDER-ON-MOVE` lightweight path - which the log shows DISABLED - not on the full WebGL2 fallback backend. **This is the L-317 family one path further: every non-WebGPU render path must independently paint the theme background opaque; the full fallback backend was missed.** (Secondary suspect to rule out: `SceneQualityTier tier=performance` on 7854+ meshes dropping the studio environment/IBL.) | Rendering (**C04** scheduling; **P2** single THREE owner; same family as **L-317** WebGL2 opaque overlay + **L-312/L-324** device-loss cascade; `RenderPipelineManager.ts:636/1779/1783`, `BackgroundUniform.ts:42`, `SceneQualityTier`) | **SHIPPED 3f6872b6 (2026-07-16) — recoverPipeline() enables the lightweight opaque theme-bg render on the WebGL2 fallback branch (extends L-317). was OPEN (MEDIUM) - DEMO-VISIBLE (grey instead of PRYZM white) but purely cosmetic; the model renders fine.** Downstream of the device-loss fallback: if L-312B stops the heavy batch from losing the device, the session never drops to WebGL2 and the grey never appears - but the fallback must be white REGARDLESS. Brand: unified PRYZM white+purple, NO grey/black backgrounds (memory: preview-color-unified + onboarding brand). | route -> **render agent (L-317/L-312 family).** **(1) CONFIRM the active path:** full WebGL2 fallback backend clear vs `performance`-tier environment drop (the screenshot's `webgl-fallback` points at the former). **(2) THE WEBGL2 FALLBACK BACKEND MUST PAINT `LIGHT_BG_HEX` OPAQUE** (`setClearColor(_lightweightBgColor, 1)` with `scene.background` set to white, OR keep the TSL-null contract but ensure the fallback clears to opaque white) - mirror L-317's opaque-overlay fix for the FULL fallback, not just render-on-move. **(3) THEME-AWARE:** white in light theme, `DARK_BG_HEX` in dark - reuse `_lightweightBgColor` which already tracks theme. **GUARD: a light-theme heavy scene (office/resi batch) shows a WHITE viewport background on BOTH the WebGPU pipeline AND the WebGL2 fallback backend; switching to dark theme yields `#0a0f2c` on both.** SS-FIX-WEBGL-FALLBACK-WHITE-BACKGROUND. **[REOPEN 2026-07-16 — PARTIAL]** Founder confirms: WebGPU bg WHITE (correct), WebGL bg still GREY. The shipped L-326 (3f6872b6) enabled the opaque white lightweight render on the device-loss RECOVERY branch of recoverPipeline(), but the founder reaches WebGL via the MANUAL BACKEND TOGGLE / live-swap in the STATIC case. Root confirmed in code: white is painted ONLY inside the  render branch (RenderPipelineManager.ts:651 ); WebGPU paints white via its NATIVE TSL bgUniform which does NOT run on the WebGL backend. So on any WebGL variant where  is not true (e.g. the toggle resolves to a plain WebGLRenderer , or webGpuActive/lightweight get out of sync), neither the TSL bg NOR the opaque fill paints → grey container shows through. FIX: the theme-bg white must be painted opaque EVERY frame on ANY non-WebGPU backend (static included), across ALL variants (webgl-fallback AND webgl-only), not gated on recovery/on-move. Cosmetic (WebGPU is the correct default) but brand-visible. SS-FIX-WEBGL-BG-WHITE-ALL-VARIANTS (extends L-326). |
| L-327 | founder (2026-07-16, 2nd report - Barcelona resi, 7 storeys) - **the visible "Opening the 3D Site - taking too long" error; child of L-323** | **[HIGH] THE FORMA "3D SITE" TILES-READINESS STAGE IS A GUARANTEED FALSE-WAIT ON KEYLESS GROUND - IT SHOWS "THE MAP TILES HAVE STOPPED STREAMING" AFTER 25 s EVEN THOUGH NOTHING IS WRONG, AND IT STARVES CONTEXT-BUILDING LOAD SEQUENCED BEHIND IT.** The readiness chain gates on `snap.tilesLoaded && outstanding === 0` (`viewActivationLoading.ts:251`), but the Forma flat-ground study runs on a KEYLESS ELLIPSOID with no tile/terrain provider (`terrain clamp degraded — no real terrain provider attached (keyless ellipsoid ground)`) - **so tiles NEVER stream, the `tiles` stage NEVER advances, and the stall watchdog trips at 25 s** showing the founder the ERROR dialog. **The `ViewActivationSignals` port (`viewActivationLoading.ts:62`) has NO signal for 'is a real tile provider attached', so the state machine cannot tell 'tiles still loading' from 'there will never be tiles'.** **SECOND SYMPTOM, SAME ROOT:** the founder reports context BUILDINGS did not load (the log shows `context roads: 3053 way(s)` + `context parks: 461` loaded, but NO `context buildings:` line before the stall) - **buildings appear only 'sometime after', consistent with context-building fetch being sequenced AFTER the tiles-ready gate that never fires** (roads/parks fire earlier; buildings starve behind the dead gate + the 17 MB GLB export). | GIS / geospatial (**C06** GIS surface; **C01 §2** the loading state machine is Cesium-free via the injected port; **§FEAT-VIEW-ACTIVATION-LOADING-OVERLAY**; **L-259** seat-and-reveal; child of **L-323**; `viewActivationLoading.ts:62/251`, `ViewActivationSignals`, `sampleTileLoadProgress`, `waitForTiles`) | **SHIPPED dbcc8819 (2026-07-16) — hasRealTileProvider() Cesium-free port signal skips the tiles-readiness gate on keyless flat-ground Forma. was OPEN (HIGH) - DEMO-BLOCKING and RECURRENT (2nd report). This is the SHIPPABLE QUICK-WIN extracted from L-323 cause #3** - smaller + safer than L-323's export-dedup/context-cache refactor, and it is the piece the founder actually SEES. | route -> **GIS/geospatial agent (with L-323).** **(1) ADD A PORT SIGNAL** to `ViewActivationSignals` — `hasRealTileProvider(): boolean` (or a `providerPresent` flag on `TileStreamSnapshot`) so the state machine stays Cesium-free (C01 §2) yet KNOWS when tiles can never stream. **(2) SKIP/IMMEDIATELY-RESOLVE THE `tiles` STAGE** when no real tile provider is attached (keyless ellipsoid flat-ground Forma study) — `waitForTiles()` resolves at once instead of waiting 25 s for a stream that will never begin. **(3) DECOUPLE CONTEXT-BUILDING LOAD from the tiles gate** — context buildings (+roads/parks) must fetch on their own schedule, never chained behind tile readiness; confirm the sequencing and parallelise/cap per L-323 cause #2. **(4) KEEP the real stall watchdog** for the case where a tile provider IS attached and genuinely stalls (do not blanket-disable it). **GUARD: a keyless-ground Forma "3D Site" reaches READY with NO 25 s tiles-stall and NO error dialog; context buildings render without waiting on tile readiness; when a real tile provider IS attached, a genuine streaming stall still surfaces the retry.** SS-FIX-FORMA-TILES-READINESS-KEYLESS-GATE. |
| L-328 | founder (2026-07-16) - **"just creating a simple elevation view" - a TRIVIAL 53-mesh scene, NOT a heavy batch** | **[HIGH] CREATING AN ELEVATION VIEW ALLOCATES A ZERO-SIZE RENDER TARGET; THE ENGINE KEEPS SUBMITTING AGAINST THAT INCOMPLETE FRAMEBUFFER, WHICH TRIPS A SHADER-VALIDATE BURST AND THEN A FULL WEBGPU DEVICE LOSS + RECOVERY CASCADE.** Order from the log: split view deactivates (elevation view being created) -> **`THREE.WebGLProgram: Shader Error 1282/1286 VALIDATE_STATUS false`** on FIVE material types (MeshPhysicalMaterial, ShadowMaterial, LineDashedMaterial, SpriteMaterial, MeshBasicMaterial) with EMPTY Program Info Log (classic 'context going down mid-validate') -> **`WebGPU Device Lost: A valid external Instance reference no longer exists.`** -> recovery, during which **`GL_INVALID_FRAMEBUFFER_OPERATION … Framebuffer is incomplete: Attachment has zero size`** floods (glClear/glDrawElements/glDrawArrays/glBlitFramebuffer) until `_reconcileRenderSize` drifts 2037x976 -> 2048x981 -> **807x976** (the split pane finally laid out). **THE CRUX: the elevation/split render target is created BEFORE its pane has a measured size (0x0). The existing zero-size guard (`RenderPipelineManager.ts:798` `if (w<=0||h<=0) return false`) only SKIPS THE RESIZE - it does NOT SUPPRESS THE RENDER SUBMIT - so the loop keeps drawing against the incomplete framebuffer, and the driver kills the device.** This is the same device-loss family as L-312/L-324 but with a NEW, LIGHTWEIGHT trigger (a basic documentation action), and a distinct root (zero-size target on view creation, not a heavy PSO storm). | Rendering / views (**C04** scheduling; **C24/C24.1** documentation views; **P2** single THREE owner; **P3** single rAF; same device-loss family as **L-312/L-324**, render-size family as **L-312A/L-317**; `RenderPipelineManager.ts:783-798 _reconcileRenderSize`, `SplitViewManager`, elevation-view creation, EdgeProjectorService) | **SHIPPED 3f6872b6 (2026-07-16) — render() skips the submit while the target is 0x0 (never submits an incomplete framebuffer). was OPEN (HIGH) - DEMO-CRITICAL: elevations are a CORE documentation workflow, and this loses the GPU device on a trivial scene.** Recoverable (unlike L-324's terminal hang) but leaves the elevation view degraded through the cascade. | route -> **render agent (L-312/L-324 + L-312A family).** **(1) SUPPRESS THE RENDER SUBMIT (not just the resize) WHILE THE TARGET IS ZERO-SIZE** - `_reconcileRenderSize` returning false on 0x0 must pair with a render-pass GATE that SKIPS the submit entirely until the pane has a non-zero measured size (never submit against an incomplete framebuffer). **(2) DEFER RENDER-TARGET ALLOCATION for a newly-created view/split-pane until its container reports non-zero `clientWidth/Height`** (ResizeObserver / first-laid-out frame), so a zero-size attachment is never created in the first place. **(3) CONFIRM whether the shader-VALIDATE burst is CAUSE or SYMPTOM** - the empty info log points to symptom (context already going down from the incomplete-framebuffer submit); if so, (1)+(2) remove it. **(4) This is another L-312B pressure point** - a device loss the recovery must survive gracefully (it does here) but should never have been provoked. **GUARD: creating an elevation view on a trivial scene issues NO submit against a zero-size framebuffer, produces NO `Attachment has zero size` GL error, and does NOT lose the WebGPU device; the elevation renders correctly on first layout.** SS-FIX-ELEVATION-VIEW-ZERO-SIZE-RENDER-TARGET. |
| L-329 | founder (2026-07-16) - **SEEN MANY TIMES; dedicated agent requested** | **[HIGH] ELEMENT SELECTION CORRUPTS AFTER SOME TIME - 3D CLICKS STOP SELECTING ANYTHING, WITH NO WARNING. ROOT: A DEVICE-LOSS/RECOVERY LEAVES THE GPU-PICK RENDER TARGET STALE, AND PICKING IS NOT REBUILT ON RECOVERY.** The trigger in the log is the **ShadowDepthTexture-mid-submit** family (`Destroyed texture [Texture "ShadowDepthTexture"] used in a submit` - the §SHADOW-DISPOSE-DEFER / null-at-mount-runtime-event race) -> `WebGPU Device Lost: A valid external Instance reference no longer exists` -> `GL_INVALID_FRAMEBUFFER_OPERATION … Attachment has zero size` flood -> context lost/restored. **3D element selection uses GPU id-picking** (`packages/picking/src/gpu-pick.ts`, `§SELECT-PICK-RESOLUTION`, viewport-sized pick target + search radius - memory gpu-pick-resolution-and-highlight, 3d-selection-instanced-gpu-pick-gap). After the device is lost + recreated, **the pick render target (and/or the per-element id registrations the GPU pick reads) is stale / zero-size / bound to the superseded GPU device - so every pick returns nothing and the user 'cannot select elements anymore'.** The pick target is never re-created on recovery. This is the SELECTION-side victim of the L-312/L-324 device-loss family; the founder experiences it as selection death, not a crash. | Picking / selection (**C04** rendering/scheduling; **P1** composition root - picking slot via `buildPickingSlot`/composeRuntime; **P8** span; device-loss family **L-312/L-324**; §SHADOW-DISPOSE-DEFER trigger; `packages/picking/src/gpu-pick.ts`, `PickStrategyResolver`, `packages/input-host/src/SelectionManager.ts`, `buildPickingSlot.ts`) | **SHIPPED b24425a4 (2026-07-16) — gpu-pick self-heals a zero-size/stale-device pick target + reconciles __pryzmDeviceLossCount generation. was OPEN (HIGH) - RECURRENT and DEMO-CRITICAL: losing the ability to select elements makes the editor unusable mid-session.** DEDICATED AGENT (founder-requested). Independent of but complementary to the render family: the render agents (L-324/L-312) stop the device loss; THIS makes selection SURVIVE a loss if one still happens. | route -> **DEDICATED selection-recovery agent (picking subsystem only).** **(1) SELF-HEALING PICK TARGET (primary, self-contained):** at pick time, detect an invalid / zero-size / stale-device GPU-pick render target and REBUILD it before reading - picking becomes idempotently recoverable without depending on cross-subsystem event wiring. **(2) RECOVERY-AWARE (secondary):** subscribe to the existing device-loss/recovery signal (find it - do NOT edit RenderPipelineManager) and re-create the pick target + re-register pickables on recovery. **(3) VERIFY the id-registration survives** a project-independent device loss (instanced walls have per-instance ids but no group userData.id - memory 3d-selection-instanced-gpu-pick-gap; ensure recovery re-establishes those). **GUARD: after a simulated WebGPU device loss + recovery, a click on a wall/window/door in 3D still selects it (GPU pick returns the correct id); the pick target is rebuilt (non-zero size, current device); no permanent selection-dead state.** SS-FIX-SELECTION-SURVIVES-DEVICE-LOSS. |
| L-330 | founder (2026-07-16) - **maturity/architecture: 'view graphics are good but not connected to the visibility intent yet'** | **[HIGH - ARCHITECTURAL] THE VISIBILITY-INTENT ENGINE IS BUILT, PURE, AND TESTED - BUT IT IS NOT WIRED AS THE LIVE DRIVER OF VIEW GRAPHICS. Two disconnected systems.** CONFIRMED IN CODE: `packages/visibility` ships a full **11-wave chain** (`w01LevelScope … w02CategoryVisibility … w06FilterOverrides … w09ElementHide … w11GhostLayer`, `runWaveChain`, `evaluateViewVisibility`) described in its own header as *'the canonical runtime for live visibility evaluation'* - **but `runWaveChain`/`evaluateViewVisibility` have ZERO call sites outside `packages/visibility` and its `__tests__`** (grep-verified). Meanwhile the ACTUAL view graphics are produced by a SEPARATE path - view definitions -> `ViewVisibilityMap` (core-app-model, still carries a `TODO(TASK-08)`) -> `VGSceneApplicator.applyToProjectionLayers` (plan/elevation) + the symbol builders + the 3D scene-committer. **So the founder's V/G edits do not flow through the intent SSOT, and the intent SSOT does not drive the views.** The founder wants the inverse: visibility intent = the source of truth ALIGNED with the current view-graphics setup, EDITABLE ON THE FLY, with views re-rendering accordingly. **This is not a single bug - the full 11-wave port + live wiring was EXPLICITLY DEFERRED to S49 / Phase 3A (ADR-0036), and the package header says so.** Concrete leaks of the disconnect already filed: **L-295** (turning door/window category off in V/G does not remove the plan symbol - layer-name vocabulary mismatch colon vs non-colon in `VGSceneApplicator`), and the deprecated parallel `vgGovernanceStore`/`applyVisibilityIntent` legacy reducer still in use by builders. | Visibility / views (**C09** AI & Visibility-Intent - P7 intent != UI state; **ADR-0036** wave-chain phasing / S49 Phase-3A port; **ADR-0030** VI legacy adapter; **P6** commands-only mutation; **P7**; the 11-wave chain in `packages/visibility`, `runWaveChain`/`evaluateViewVisibility`, `ViewVisibilityMap`, `VGSceneApplicator`, scene-committer 3D visibility; related **L-295**; memory editable-living-graph-bim2-3) | **OPEN (HIGH) - MATURITY/ARCHITECTURE, not demo-blocking; the founder is flagging the gap, not a crash.** This is a PROPER Phase-3A wiring project (needs live browser verification across plan/elevation/3D), NOT a rush change. Do NOT hot-wire it - do it as a governed port. | route -> **view-intent / visibility agent (its own dedicated queue).** Make the wave chain the LIVE, canonical per-view evaluator and the SINGLE source of truth for view graphics: **(P1)** call `runWaveChain`/`evaluateViewVisibility` on live element+view state to produce per-view hidden/halftone/override results; **(P2)** every V/G gesture (category toggle, element hide, halftone, filter, phase, isolation) dispatches as an INTENT through the command bus (P6/P7), never a direct store write; **(P3)** the DRAWING layers (`VGSceneApplicator`) consume the wave result - and UNIFY the layer-name vocabulary so L-295 closes by construction; **(P4)** the 3D scene (scene-committer) applies the SAME result (hidden/halftone opacity/isolation); **(P5)** LIVE REACTIVITY - an intent edit re-runs the chain for affected views and updates graphics on the fly (views subscribe to the intent side-table); **(P6)** persistence via the existing per-view side-table round-trip (byte-stable). Retire the deprecated `vgGovernanceStore`/legacy reducer once builders read the chain result. **GUARD: toggling a category OFF in V/G removes it from BOTH plan AND 3D live; hide/halftone/isolate reflect immediately across ALL open views; the same intent persists + round-trips; NO parallel visibility path remains.** SS-WIRE-VISIBILITY-INTENT-AS-LIVE-VIEW-GRAPHICS-SSOT (closes L-295). |
| L-331 | founder (2026-07-16, testing office on the new build) - **BLANK RENDER, high urgency** | **[CRITICAL] THE HEAVY OFFICE BUILDS FULLY BUT RENDERS BLANK - a shadow-sampler / texture-format mismatch rejects the draw.** The model is COMPLETE (log: L-322 laid 23 storey floor plates; L-312B CW material-share working ; FrustumCulling sees **2008 element(s) / 13267 mesh(es)**), so this is a RENDER failure, not generation. Smoking gun:  fired mid heavy-wall-drain, WHILE the heavy-scene survival tier forces shadows OFF (; ; ;  yet ). **ROOT HYPOTHESIS: the survival/performance tier DEALLOCATES the shadow map but the material shaders still bind a shadow sampler (sampler2DShadow / comparison sampler) against a now-missing / wrong-format texture -> glDrawElements rejected -> whole draw blank.** The shadow-OFF path does not recompile/reconcile the materials to drop the shadow sampler (or binds a wrong-format placeholder). | Rendering (**C04** scheduling; **P2** single THREE owner; shadow-tier machinery //// shadow path; §SHADOW-DEVICE-LOSS-FIX; related device-loss family L-312/L-324) | **SHIPPED ec36b556 (2026-07-16) - _reconcileWebGlShadowSamplers() re-arms any shadow-caster whose depth map is null while shadowMap.enabled (the sampler2DShadow mismatch that blanked the heavy office); WebGL-only, WebGPU untouched. SAFETY-NET fix. FOLLOW-UP (out-of-fence): route the tier shadows-off intent to the renderer latch (RenderingPipelineCoordinator/PascalSceneLighting) for the durable perf path. NEEDS LIVE VERIFY on heavy-office WebGL. Was OPEN (CRITICAL) - founder BLOCKED on the office; DEMO-CRITICAL.** Appears on the heavy office (~13k meshes) that trips the survival tier. **MUST CHECK: is this a REGRESSION from the just-shipped L-312B / L-326, or pre-existing heavy-tier shadow-off? Bisect first.** | route -> **render agent (urgent).** **(1) REGRESSION BISECT FIRST:** confirm whether reverting L-312B (6225ef05) and/or L-326 (part of 3f6872b6) restores the office render - if either is causal, REVERT it from main immediately (orchestrator pushes) and re-approach. **(2) IF NOT a regression, FIX the shadow-sampler reconciliation:** when the survival/performance tier turns shadows OFF and deallocates the shadow map, the materials MUST be reconciled so no shader samples a missing/mismatched shadow texture (recompile with shadows-off define, OR keep a correctly-formatted depth placeholder bound, OR gate the draw until reconciled). Never issue a draw whose bound shadow texture format != the sampler type. **GUARD: the heavy office (>=13k meshes, survival tier, shadows off) RENDERS (non-blank) with NO  GL error; toggling the tier does not blank the scene.** SS-FIX-HEAVY-TIER-SHADOW-SAMPLER-MISMATCH. **[UPDATE 2026-07-16] WEBGPU IS CONFIRMED GOOD** — founder: 'using webgpu everything worked well' (office generates + renders: L-322 floors + L-312B material-share both working). So this blank is the WEBGL-FALLBACK / survival-tier path only (earlier log showed the `[.WebGL-…]` context + shadows-off). Downgrade urgency: it bites only after a device-loss drop to WebGL fallback on a heavy scene. Still fix the shadow-sampler reconcile so the fallback isn't blank. |
| L-332 | founder (2026-07-16, reopened a saved office tower) - **the grey ENVELOPE is BACK on reload; L-321 regressed across LOAD** | **[HIGH] A SAVED OFFICE TOWER REOPENS AS THE GREY MASSING 'ENVELOPE' INSTEAD OF THE DETAILED TOWER - because L-321's full-detail override runs ONLY at GENERATION, never on LOAD.** L-321 fixed the envelope by pinning `levelScoped3DCullingService.setMode('all')` inside `OfficeBuildingExecutor` (generation time). On REOPEN the executor does not run (it is a load, not a generation), so the heavy office (log: 10092 meshes -> `SceneQualityTier tier=performance`, 23 levels >= the massing-LOD auto-escalation threshold of >=15 levels + >=1000 elems) RE-ESCALATES to grey massing blocks (`0xb9c0cc`). The founder sees the envelope he already 'fixed' return. **COUPLED TO L-331 (do not fix in isolation):** the massing LOD is precisely what AVOIDS the L-331 shadow-sampler blank on the heavy office - grey blocks render without the shadow pass. FORCING full detail on reload WITHOUT first fixing L-331 would swap the envelope for a BLANK viewport. Fix order is L-331 (shadow-sampler reconcile) THEN L-332 (durable full-detail on load). | Rendering / office (**C04**; **P7** visibility/LOD intent not UI state; L-321 lineage; `OfficeBuildingExecutor.showOfficeFullDetail`, `LevelScoped3DCullingService.setMode/isHeavyModel`, the massing-LOD auto-escalation; coupled to **L-331**) | **SHIPPED f237815a (2026-07-16) - LevelScoped3DCullingService persists a durable per-project full-detail LOD intent (P7, localStorage keyed by projectId); massing auto-escalation gated on it; pryzm-project-loaded clears the transient session mode so no cross-project leak; officeShowFullDetail sets the intent. 132/132 + 20/20 massing. NEEDS LIVE VERIFY: reopen office shows detail not grey envelope. Was OPEN (HIGH) - DEMO-VISIBLE and RECURRENT (founder already 'fixed' it once via L-321).** | route -> **office/render agent (with L-331).** Make the full-detail intent DURABLE across load: persist a per-project 'office full-detail' visibility/LOD intent (P7 - it is a domain intent, not a generation side-effect) and RE-APPLY `setMode('all')` on `pryzm-project-loaded` for office projects (or gate the massing-LOD auto-escalation on that persisted intent). Do this ONLY after L-331 lands so full detail does not blank. **GUARD: reopening a saved office shows the DETAILED tower (not grey massing), with NO blank viewport; the LOD intent round-trips through save/reload.** SS-FIX-OFFICE-FULLDETAIL-DURABLE-ON-LOAD. |
| L-333 | orchestrator (noticed in the founder's Villa 01 load log) - **SILENT DATA LOSS ON REOPEN** | **[HIGH] REOPENING A SAVED PROJECT SILENTLY DROPS ELEMENTS THAT FAIL RE-VALIDATION ON LOAD.** In the Villa 01 (204-element) load, `ImportProjectCommand` reported: (1) TWO slabs `Failed: Slab undefined: thickness Invalid input` (the persisted slab record has a missing/invalid `thickness` -> Zod reject -> slab NOT restored), and (2) ONE stair `Failed: Stair …: Total stair height 3000mm does not match level height 2939mm (tolerance: 50mm)` (a 61 mm mismatch trips the create-time validator on LOAD). Also logged: `[CreateSlabCommand] §2.6 No id injected in payload … breaks redo symmetry`. **NET: the reopened Villa is MISSING 2 slabs + 1 stair that existed when saved — silently (only a console line).** A round-trip (save -> reopen) MUST be loss-less: whatever was validly created and persisted must reload, OR the load must surface a visible, itemised recovery (never a silent drop). | Persistence / load (**C05** persistence + file-format round-trip; **C11** creation pipeline; **C03** schema; `ImportProjectCommand`, `CreateSlabCommand` slab `thickness` schema, `CreateStairCommand` height-vs-level-height validator, id-injection §2.6) | **SHIPPED 97693135 (2026-07-16) - resolveSlabThicknessOnLoad() backfills slab thickness (explicit->layer-sum->0.2m) + injects slab id; persisted stair reconciles on load (isRestore accepts height!=level mismatch, fresh creates still strict); backfills itemised in stats.warnings (visible). 9/9 guard tests. Was OPEN (HIGH) - DEMO-VISIBLE: a saved project loses geometry on reopen.** Two distinct roots: (a) SLAB THICKNESS not persisted/restored (schema/serialise gap), (b) STAIR height-vs-level tolerance too strict for a persisted stair (a stair saved against a 2939 mm level but carrying 3000 mm height should reconcile on load, not be rejected). | route -> **persistence/elements agent.** (a) Ensure slab `thickness` round-trips (serialise + restore); if legacy records lack it, backfill from the systemType/default at load (never reject a persisted element for a derivable field). (b) On LOAD, the stair height validator must RECONCILE to the level height (or accept within a persisted-tolerance) rather than drop the stair — a saved element is authoritative. (c) Inject `id` on the load path (§2.6). **GUARD: save a project with N slabs + a stair, reopen -> exactly N slabs + the stair restore (0 dropped); a persisted slab with a legacy-missing thickness backfills instead of failing; any genuinely-unloadable element surfaces a VISIBLE itemised warning, never a silent console-only drop.** SS-FIX-LOSSLESS-PROJECT-REOPEN. |
| L-334 | CATEGORY-READINESS-AUDIT 2026-07-16 (rows 1/2/5) - **ARCHITECTURAL LAUNCH-BLOCKER** | **[CRITICAL - ARCHITECTURAL] SAVE/RELOAD DATA INTEGRITY: silent element-loss is reported as SUCCESS, there is no whole-snapshot validation, no quarantine of dropped records, and no checksum anywhere.** Save path has NO Zod-on-write (`ProjectSerializer.serialize`), server stores the snapshot as opaque JSONB with no checksum (`projectStore.js createVersion`), load runs `JSON.parse` + per-element re-creation whose Zod failures DROP the element with `console.warn` only (`ImportProjectCommand.ts:256-280`), and a load WITH drops is still reported SUCCESS (`ProjectLoader.ts:1880` `result.success = errors.length===0 || loaded>0`) - the user sees a 5 s toast, not a blocking warning. Corruption is undetectable (no hash). Forward-compat is a silent pass-through, not a hard refuse (`MigrationEngine.ts:241-247`), contradicting C47 §1.6. For a tool whose files become construction/legal documents this is the #1 trust-killer. | Persistence / data integrity (**C05** persistence + file-format; **C47** format-versioning §1.6 forward-refuse; **C48**-adjacent durability; **C10** observability; extends **L-333** slab/stair drop + **L-269** false-save; `ProjectLoader.ts:1880`, `ProjectSerializer.ts`, `ImportProjectCommand.ts:256-280`, `MigrationEngine.ts:241`, `projectStore.js createVersion`, `SlabValidator.ts:23`) | **SHIPPED 22287477+d9e31dac+1e0beee6+eddaf242+cb4b1800 (Fix 1, 2026-07-16) - P1 deriveLoadStatus: any dropped element -> success=false/partial|failed + a PERSISTENT non-dismissing UI warning enumerating type+count+reason+quarantine handle (was ProjectLoader.ts:1880 success-if-any-survived + 5s toast). P2 SnapshotIntegrity.validateSnapshotForLoad distinguishes corrupt-file (not-object/bad-shape/forward-version C47/checksum-mismatch -> HARD FAIL, open project untouched) from valid-but-partial. P3 QuarantineStore: raw dropped records preserved (in-memory+localStorage) + retrievable, not console.warn-lost. P4 stable FNV-1a content checksum stamped at save, verified at load (tamper -> explicit corruption). 14 new regression tests + 185/185 persistence + 89/89 command-registry. FOLLOW-UPS: server-side checksum column (deferred, client-side only); legacy per-command loader fallback gets status but not quarantine (minor). Was OPEN (CRITICAL) - FIX 1 (founder-directed, implementing now). 🔎 CODE-VERIFIED 2026-07-18 (register reconciliation): the P2/P4 checksum machinery this row records as SHIPPED is NOT in the current tree — it was REVERTED by the L-360 fix. Repo-wide grep `SnapshotIntegrity|computeSnapshotChecksum|SnapshotChecksum|integrityHash` = 0 matches in any `.ts/.js` (docs/reports only). No integrity digest is computed at save or verified at load anywhere: `apps/editor/src/ui/platform/SaveOrchestrator.ts` (`getHash`/`lastHash`, :8/:317-319/:364-365/:507-527) is an in-memory DIRTY-CHECK that skips no-op saves — not a persisted checksum; `apps/editor/src/ui/platform/ServerSyncQueue.ts` transmits the snapshot verbatim (0 integrity matches); the server version-save `server.js:3186-3260` validates ONLY `isValidProjectId` + a 50 MB `SNAPSHOT_LIMIT_BYTES` cap (:3213) + a `.passthrough()` Zod that types ONLY the furniture array (:3250-3253); `apps/editor/src/engine/persistence/ProjectSerializer.ts` + `ProjectLoader.ts` have 0 checksum/integrity/digest matches. NET: the data-integrity GAP this row set out to close (no corruption detection) is effectively OPEN again — corruption stays undetectable. Therefore NOT "shipped/ACTIVE": P4 (checksum) is reverted; the P1–P3 load-truth/quarantine claims need live re-confirmation (UNVERIFIED). Do not re-mark Fixed without RED/GREEN evidence.** | route -> **persistence agent.** **(1) LOAD-TIME TRUTH:** any dropped element -> the load reports PARTIAL/FAILED (not SUCCESS); surface a persistent/blocking warning, not a 5 s toast. **(2) WHOLE-SNAPSHOT VALIDATION PASS on load** BEFORE per-element re-creation - distinguish 'corrupt file' from 'valid file, some elements incompatible'. **(3) QUARANTINE not discard:** preserve the raw dropped record (recoverable by support) - never console.warn-and-lose. **(4) CHECKSUM at save + verify at load** so corruption is detected explicitly, not inferred from parse failure. GUARD: a persisted project with >=1 element that fails its Zod validator loads as a VISIBLE non-dismissible warning AND the raw element data is still retrievable afterward. SS-FIX-LOAD-INTEGRITY-TRUTH-QUARANTINE-CHECKSUM. |
| L-335 | CATEGORY-READINESS-AUDIT 2026-07-16 (row 3) - **ARCHITECTURAL LAUNCH-BLOCKER; DECISION-GATED** | **[CRITICAL - ARCHITECTURAL] REAL-TIME COLLABORATION IS NOT CONFLICT-SAFE: the advertised Yjs CRDT is DEAD-WIRED and the live path is silent last-writer-wins.** Two parallel sync systems: (A) `packages/sync-client` Yjs CRDT is written + unit-tested but has NO transport in prod - `SyncClient.ts:173-179` provider factory THROWS, the prod `YjsDocAdapter` (`engineLauncher.ts:817`) is never given a provider; (B) the LIVE path is JSON command rebroadcast (`initCollaboration.ts` <-> `server.js:534-599`) with no merge/version-vector/conflict field. Per mutation: moves + property-edits = **silent LWW (L-53)** (`YjsDocAdapter.ts:559-562` raw `set()`, `CRDTConflictResolver` never called from any write path); bus-dispatched creates (drag-drop / plan tools / AI furnish) = **never broadcast at all (L-206)**. The unit tests actively BLESS silent LWW; there is NO test that a concurrent edit surfaces `emitConflict`. Against C08 §3.1/§3.3 (P8: silent LWW FORBIDDEN), silent LWW is the ONLY behaviour for moves/edits. | Collaboration (**C08** §3.1/§3.3 CRDT + explicit conflicts, **P8**; ties **L-53** silent-LWW + **L-206** bus-creates-not-broadcast; `SyncClient.ts:173`, `YjsDocAdapter.ts:559`, `initCollaboration.ts:846`, `server.js:534-599`, `CRDTConflictResolver`) | **OPEN (CRITICAL) - FIX 2 (founder-directed, DECISION-GATED: recommendation reported, NO implementation until founder confirms path a/b).** | route -> **collab agent, AFTER founder confirms path.** (a) wire SyncClient providerFactory to a real transport (y-websocket or the existing socket.io channel) so the CRDT path is live + route ALL mutation types incl. bus-creates through it, retiring the JSON/LWW path; OR (b) per-field conflict detection on the CURRENT JSON path - moves/property-edits call `CRDTConflictResolver` (not raw overwrite) + bus-creates added to the broadcast hook (closes L-206). Either path ENDS with a real two-client concurrent-edit test that merges OR surfaces a conflict - never silently overwrites. SS-FIX-COLLAB-CONFLICT-SAFETY. |
| L-336 | CATEGORY-READINESS-AUDIT 2026-07-16 (row 4) - **ARCHITECTURAL LAUNCH-BLOCKER** | **[CRITICAL - ARCHITECTURAL] THE MULTI-TENANT ACCESS MODEL IS OWNER-ONLY: `canUserAccessProject()` ignores `project_members`, so invited teammates cannot open a shared project on ANY read/write path.** The single enforcement primitive (`server/projectAccess.js:96-137`) compares ONLY `owner_id !== userId` on all three source branches - it never consults `project_members`, despite its own header + test claiming it verifies a collaborator. Consequence: a user invited via `POST /api/projects/:id/members` (written to `project_members` with a role) is DENIED the Socket.io room (`server.js:492-518` owner-only join) and 403'd on visibility-intent / IFC / command-log reads. It fails CLOSED (safe, not a breach) but **real-time collaboration for non-owners does not function** - a launch-blocker for a 'collaborative' tool. (`resolveProjectRole` reads members but only on the version-transition route, not join/read.) | Auth / multi-tenant (**C08** auth + roles + ISO-19650; **C13**; `server/projectAccess.js:96-137`, `server.js:492`(join), `server.js:4029`(invite), `resolveProjectRole` `server.js:3857`, `project_members` schema) | **SHIPPED 1fe50f94 (2026-07-16) - canUserAccessProject() now allows project_members (role-aware: owner OR member) across all 3 branches (in-memory/Supabase/PG) via projectMembers.getUserRole; fail-closed + retryable-503 preserved (member-lookup DB error -> allowed:false+retryable:true, never fail-open); anonymous still hard-denied. 8 new tests T28-T35, 97/97 server green. Transitive: every HTTP + socket join path gates on this fn. Was OPEN (CRITICAL) - FIX 3 (founder-directed, implementing now).** | route -> **server/auth agent.** Extend `canUserAccessProject` to include a `project_members` lookup (role-aware: owner OR member-with-role) across ALL three branches, keeping fail-closed + the retryable-503 semantics intact (never fail open). GUARD (test evidence, not code inspection): a user added as a project MEMBER (not owner) can open the project via HTTP AND join the Socket.io room for it; a non-member is still 403/denied. SS-FIX-NONOWNER-PROJECT-ACCESS. |
| L-337 | DOC-ARCH-READINESS-SCORECARD 2026-07-16 (Strategy 2) - **ARCHITECTURAL** | **[CRITICAL - ARCHITECTURAL] NO CANONICAL PERSISTENCE FORMAT + the contracts CONTRADICT each other.** Three mutually-inconsistent definitions of `.pryzm`: C05 §2.1 (ZIP + project.json ElementStore snapshot), pryzm-binary.md (ZIP + events/*.evt.bin MessagePack + chunks/*.glb, schemaVersion 1), C47 §2.3 (raw JSON with top-level formatVersion) - and C47 §10.2 STILL lists 'binary container format' as an OPEN QUESTION. Meanwhile code ships BOTH: live JSON ProjectSnapshot `SNAPSHOT_SCHEMA_VERSION=5` (ProjectSerializer.ts) AND the ZIP envelope v1. A genuine unresolved architectural fork - only the `.pryzm` envelope has the byte-equal round-trip test; the live JSON path (what actually saves/loads projects) does not. Cannot honestly answer 'which format is authoritative' or make durability/backup guarantees until resolved. | Persistence / file-format (**C05** persistence §2.1; **C47** versioning §2.3/§10.2 open-Q; **pryzm-binary.md**; ties **L-334** integrity; needs a superseding **ADR**; `ProjectSerializer.ts` SNAPSHOT_SCHEMA_VERSION=5, `SnapshotConstants.ts`, `.pryzm` envelope) | **ADR AUTHORED / PROPOSED (2026-07-16, ADR-0130) - NEEDS FOUNDER RATIFY + a code check.** ADR-0130-canonical-persistence-format proposes the two-axis resolution: JSON `ProjectSnapshot` v5 = canonical LIVE persisted-state format; `.pryzm` envelope = canonical PORTABLE export/archive (a serialization of the snapshot, NOT a parallel live store) - removing the 'which is authoritative?' ambiguity. It also reconciles the 3 contradictory `.pryzm` definitions (C05 §2.1 vs pryzm-binary.md vs C47 §2.3/§10.2-open-Q) to ONE, and adds a live-snapshot round-trip test (closing the L-334 asymmetry). OPEN before CANONICAL: (1) confirm the ACTUAL on-disk `.pryzm` structure in the exporter code, (2) founder sign-off, (3) amend C05/C47/pryzm-binary in one pass. Was OPEN (CRITICAL) - Track A (decision) + doc-reconcile. | route -> **persistence/architecture.** Raise an ADR (or C05 §0 authority note) declaring ONE authoritative persisted-state format going forward; reconcile the 3 conflicting `.pryzm` descriptions (C05 §2.1 vs pryzm-binary.md vs C47) to that single answer; close C47 §10.2 open-Q; state migration path for the non-canonical format. GUARD: one doc names the canonical format + all three contract descriptions agree + round-trip test exists for the canonical path. SS-DECIDE-CANONICAL-PERSISTENCE-FORMAT. |
| L-338 | DOC-ARCH-READINESS-SCORECARD 2026-07-16 (Strategy 1) | **SUPPORTED-PROJECT-SCALE is defined at only ONE tier (10k elements).** C10 §1 + engineering-vision §5.1 commit measured budgets at 10k (load <6s p95, <1.5GB/1h, IFC <20s) - but 100k/500k are undefined and senior-architect-audit.md:135/139 flags 'no explicit streaming/LOD/spatial-paging strategy' and frame-budget risk >100k. Performance claims are therefore unbounded above 10k: nobody can say what scale PRYZM promises. | Strategy / performance (**C10** §1 perf budgets; **engineering-vision** §5.1; `senior-architect-audit.md:135/139`) | **OPEN (MEDIUM) - Track C.** | route -> **architecture/docs.** Add a 'Supported project scale' ladder to C10 §1 (e.g. <=10k supported+measured / 10k-100k degraded best-effort / >500k out-of-scope pending LOD-streaming) so the 10k bench fixture becomes an explicit promised-scale ladder. SS-DEFINE-SUPPORTED-SCALE-LADDER. |
| L-339 | DOC-ARCH-READINESS-SCORECARD 2026-07-16 (Strategy 3) | **NO single consolidated threat-model / security-posture doc.** SPEC-08 §9 is ~15 lines (top-threats + out-of-scope, not per-endpoint STRIDE); the specced `apps/sync-server/threat_model.md` does NOT exist; no SECURITY.md. Strong honest material IS scattered (C08 controls + dated `04-reference/security/*` audits that openly report 2 critical + 8 high SCA). Enough to ANSWER a customer questionnaire, but no coherent artifact + no data-flow-per-region diagram + no sub-processor list. | Strategy / security (**C08** controls; **SPEC-08** §9; `04-reference/security/*` audits; **ADR-0221** threat-model-review S72 future-dated) | **OPEN (MEDIUM) - Track C.** | route -> **security/docs.** Promote SPEC-08 §9 to a full STRIDE per-endpoint/trust-boundary analysis (or produce `apps/sync-server/threat_model.md`); add region data-flow diagram + sub-processor list; index the scattered security audits + C08 controls from it. SS-CONSOLIDATE-THREAT-MODEL. |
| L-340 | DOC-ARCH-READINESS-SCORECARD 2026-07-16 (Decisions C08) | **C08 is stamped CANONICAL but ASSERTS a capability the code does not provide.** §3.1 (Wave-A19 amendment) claims 'server-side Y.applyUpdate merge replaces the previous LWW path'; §3.2 states silent LWW is FORBIDDEN (P8) + all conflicts MUST go through CRDTConflictResolver.mergeElement(). Shipping code ships silent LWW: `YjsDocAdapter.ts:558-561` raw set(), resolver never on a write path, `SyncClient.ts:173` provider throws, live path = `initCollaboration.ts`+`server.js:597` JSON rebroadcast. Doc overstates reality. (Code-half tracked as L-53/L-335 Path-a.) | Collaboration (**C08** §3.1/§3.3 P8; ties **L-53** silent-LWW + **L-335** real-CRDT; `YjsDocAdapter.ts:558`, `SyncClient.ts:173`, `initCollaboration.ts`, `server.js:597`) | **OPEN (HIGH) - Track A doc-half NOW + Track B code via L-335.** | route -> **collab/docs.** EITHER land the L-335 server-side merge path (then C08 becomes true), OR if merge is not landing pre-launch, retract the Wave-A19 'Y.applyUpdate replaces LWW' claim and mark C08 §3.1 as the honest JSON-command-LWW state until L-335 ships. Never leave a CANONICAL contract describing a capability that is not wired. SS-C08-DOC-CODE-RECONCILE. |
| L-341 | DOC-ARCH-READINESS-SCORECARD 2026-07-16 (Decisions C10) | **C10 claims perf benches are 'measured contracts... MUST run in CI... regression = merge blocker' - but NOTHING invokes them in CI.** Benches exist (`apps/bench/src/benches/*.bench.ts`, scripts bench/bench:baseline/bench:check) but `ci.yml` -> `ga-gate.mjs` runs lint/typecheck/boundary/otel/visual-diff, NOT the 17 NFT benches (0 grep hits for bench:check across .github + scripts). The §4 merge-blocker gate is unwired; the 'measured, merge-blocking' language is false. | Performance / CI (**C10** §1/§4; `.github/workflows/ci.yml`, `packages/release/src/ga-gate.mjs`, `apps/bench`) | **OPEN (HIGH) - Track A doc-downgrade OR Track B wire-gate.** | route -> **build/CI.** Add a required CI job running `pnpm --filter @pryzm/bench bench:check` against a committed baseline (make C10 true), OR downgrade C10 §1/§4 'measured/merge-blocking' language to 'authored, not yet CI-gated' until the job lands. GUARD: either the bench gate is green+required in ci.yml OR C10 no longer claims merge-blocking. SS-C10-BENCH-CI-RECONCILE. |
| L-342 | DOC-ARCH-READINESS-SCORECARD 2026-07-16 (Decisions C13) | **C13 is CANONICAL but its OWN §6 self-documents C13-G1..G7 as Open** - core teardown invariants unmet: the `pryzm-project-switch` listener only resets `_levelCamReady=false` (none of the §3.1-3.6 teardown runs); `BatchCoordinator` has no `forceReset()`; wall-rebuild flags / `_wallRafHandle` / `_pendingWallEvents` / in-flight `_executeFinalSweep` redetect not cancelled on switch. (G8-G10 fixed in live testing.) Isolation audit does NOT false-fire (0 false-positive), so it is PARTIAL not NO. | Lifecycle / isolation (**C13** §3/§4.2/§6 G1-G7; `ProjectLoader.ts`, `ClearProjectCommand.ts`, `BatchCoordinator`; ties SCC memory) | **✅ FIXED (committed, unpushed — orchestrator reviews before deploy) — C13-G1..G7 CLOSED.** Investigation found the batch/wall-rebuild teardown surfaces were ALREADY implemented by the Wave 35 isolation wave (`BatchCoordinator.forceReset()` + `_sweepCancelled`, `WallRebuildCoordinator._resetState()` via `window.__engineTeardown`, `ProjectLifecycleController` on the typed `pryzm-project-switch` bus) but the C13 §6 doc still listed them Open. The genuine remaining hole was the direct-**LOAD** path (open saved project / restore version / hydrator replay) which never emits `pryzm-project-switch`, so the teardown never ran there — the founder's "direct-LOAD still broken" + the G2 latch-leak hypothesis for #4's residual. FIX: extracted the §4 sequence into a public `ProjectLifecycleController.runTeardown(reason)` (one owner, P1), exposed it as `window.__pryzmProjectTeardown`, and called it at the TOP of `ProjectLoader.load()` (before any pause/beginBatch, before hydrate) so BOTH switch AND load run the identical §3.1–§3.6 teardown. Idempotent + never-throws (each step guarded). §6 G1–G7 flipped to Fixed; 7/7 ProjectLifecycleController tests (incl. 3 new load-path guards) + 7/7 BatchCoordinator.nesting green; root `tsc --skipLibCheck` = 0; `check:isolation` green. Was **OPEN (HIGH) - Track B.** | route -> **runtime/isolation agent.** Implement C13-G1..G7: `BatchCoordinator.forceReset()` + external cancellation, reset wall-rebuild flags + `_wallRafHandle` + `_pendingWallEvents`, cancel in-flight redetect on project-switch; then flip §6 rows to Fixed. GUARD: isolation audit passes on every switch with all §3.1-3.6 steps run. SS-C13-CLOSE-TEARDOWN-GAPS. |
| L-343 | DOC-ARCH-READINESS-SCORECARD 2026-07-16 (Decisions 6) | **MISSING-CONTRACTS-AUDIT marked 'RESOLVED - all 18 built' but 'built' = document exists, NOT implemented.** All 18 gap-slots (C19-C23, C32-C49) exist as files, but its §5 claim 'no code work - rules mostly already exist in code' is FALSE for C22 + C48 (both DRAFT, code entirely '(to be created)'). Mislabels authored-vs-implemented, hiding two unimplemented compliance contracts behind a green 'RESOLVED'. | Governance (**MISSING-CONTRACTS-AUDIT-2026-06-01.md**; **C22**, **C48**, **C49** DRAFT; contracts/README C00) | **SHIPPED (2026-07-16, Track A) - MISSING-CONTRACTS-AUDIT header retitled RESOLVED/built -> AUTHORED, with an explicit 'authored != implemented' warning naming C22/C48/C49 as DRAFT-with-unbuilt-code; points to the C00 status vocabulary for real implementation status. Was OPEN (LOW) - Track A honesty.** | route -> **docs.** Retitle 'RESOLVED/built' -> 'all 18 contracts AUTHORED'; add an implementation-status column; keep C22/C48/C49 DRAFT until code + CI gates land. SS-MISSING-AUDIT-AUTHORED-VS-IMPLEMENTED. |
| L-344 | DOC-ARCH-READINESS-SCORECARD 2026-07-16 (Decisions C48) | **C48 Backup/DR is DRAFT describing entirely-aspirational tooling.** WAL-PITR, RPO<=5m/RTO<=1h, 7-yr retention, paired restore drills, DR runbooks - NONE exist. `apps/backup-worker` MISSING; no WAL streaming/PITR/restore-drill/`pryzm.app/dr-status` in server/ or infra. Only real primitive = `project_versions` snapshotting + 24h command-log purge. A DRAFT describing fiction is a launch-risk (staff act on it - see L-349 runbooks). | Backup/DR (**C48**; `apps/backup-worker` MISSING; `server.js` project_versions; ties **L-349** DR-runbook fiction) | **PARTIAL (2026-07-16) - a RUNNABLE restore drill is now DOCUMENTED: DR-DRILL-RUNBOOK §0 gives the REAL procedure against the actual Fly-Postgres stack (pg_dump -> scratch restore -> row-level + app-open verify -> record in incidents/), grounded in tools that exist today (NOT the fictional pryzm-ops). STILL OPEN: (1) the founder RUNS it ONCE to produce the 'one proven restore' evidence artifact, (2) the C48 build (scheduled/automated backups + retention + PITR). Was OPEN (HIGH) - Track B build-min OR Track A honest-rewrite.** | route -> **infra/ops.** EITHER build minimum DR (backup-worker + snapshot cadence + one automated restore test), OR rewrite C48 to the honest weaker-but-true state (nightly snapshot + version-history, NO PITR) and remove the aspirational SLA numbers until real. Do NOT ship DR runbooks (L-349) that assume C48 tooling. SS-C48-DR-BUILD-OR-HONEST. |
| L-345 | DOC-ARCH-READINESS-SCORECARD 2026-07-16 (Decisions C22) | **C22 Privacy/PII is DRAFT + fully unimplemented - an EU/GDPR launch blocker.** Every path '(to be created)' & MISSING: `anonymise.js`, `storageRouter.js`, `consentStore.js`, `dsarStore.js`, `breachIncidentLog.js`, `apps/dsar-worker`, `apps/retention-worker`, `PrivacyPanel.tsx`; CI gates (check-data-tier-tag/check-pii-*) don't exist. Only real control = C08 EU-Supabase default (residency-by-deployment), NOT tier-tagging/DSAR. Hosted in Frankfurt => WILL have EU users. | Privacy/PII (**C22**; **C08** §8 EU-residency; MISSING dsar/retention workers + StorageRouter; SOC2/GDPR) | **OPEN (HIGH, EU BLOCKER) - Track B build-min OR Track A honest.** | route -> **backend/compliance.** Implement minimum: DSAR request pipeline (<=30d) + data-tier tag-at-write + retention + the CI gates (soft-warn -> hard-fail); OR honest-rewrite C22 to today's residency-by-deployment reality + a manual-DSAR SOP until automated. GUARD: a DSAR can be fulfilled (even manually-documented) + C22 status matches code. SS-C22-PRIVACY-BUILD-OR-HONEST. |
| L-346 | DOC-ARCH-READINESS-SCORECARD 2026-07-16 (Execution 1) | **master-execution-tracker has NO Owner + NO Target-date column** (row format Phase|Goal|Description|Status; 1 inline 'Owner:' in 657KB) and no P0/P1 priority axis. Open work has no accountable owner or date - process failure that lets the launch date slip silently. | Process (**master-execution-tracker.md** §3-§7 tables; ties **L-347** go/no-go) | **OPEN (MEDIUM) - Track C process.** | route -> **PM/docs.** Add explicit Owner + Target-date columns to the tracker tables + a discipline that every in-flight (green/amber) row carries both; backfill open rows. SS-TRACKER-OWNER-DATE-COLUMNS. |
| L-347 | DOC-ARCH-READINESS-SCORECARD 2026-07-16 (Execution 2 + meta) | **No go/no-go gate tied to contract status - AND the vocabulary to express it does not exist.** The G1-G10 launch gate board is DEFECT-driven, not contract-status; contracts are only CANONICAL/DRAFT (no 'ACTIVE' state, no 'Tier-1' launch-blocking classification). So 'do not launch until C08/C13/C48-min/C22-min are ACTIVE and code matches' cannot be actioned until ACTIVE + Tier-1 are first defined. Meta-blocker: enables L-340/341/344/345 to be gated honestly. | Governance / launch (**contracts/README** C00 status model; **V1-LAUNCH-IMPLEMENTATION-PLAN** G1-G10 gate board) | **SHIPPED (2026-07-16) - C00 README now defines the ACTIVE status ladder (DRAFT<CANONICAL<ACTIVE) + the Tier-1 launch-blocking set (C05/C08/C10/C13 + C48-min/C22-min) + the G0-CONTRACTS gate on the V1 board. Was OPEN (HIGH, META) - Track C - do first, unblocks the rest.** | route -> **architecture/docs.** Define in C00: an 'ACTIVE' contract status (= CANONICAL + code-matches + §6 CI gate green) and a 'Tier-1' launch-blocking tier (C08/C13/C05/C10 + C48-min/C22-min). Add a G0-CONTRACTS gate to the board that blocks launch until every Tier-1 contract is ACTIVE. SS-DEFINE-ACTIVE-TIER1-GATE. |
| L-348 | DOC-ARCH-READINESS-SCORECARD 2026-07-16 (Execution 3) | **Spec status-fields drift from code (~20-25% sampled) - inverted: DRAFT/PLAN/STUDY labels on code that is built+tested+wired.** Clearest: SPEC-AUTODIMENSION DRAFT while `packages/auto-dimension` is fully built + live-wired into the editor. (No IMPLEMENTED-but-dead found in the sample - understatement, not overstatement, but still dishonest status.) | Execution (**docs/03-execution/specs/**; SPEC-AUTODIMENSION, SPEC-WALL-SINGLE-VOLUME-CSG) | **SHIPPED (2026-07-16, Track A) - SPEC-AUTODIMENSION flipped DRAFT->IMPLEMENTED (package built+tested+wired); added a 'last code-synced' honesty note. Spot-check confirmed the understatement pattern. Was OPEN (LOW) - Track A honesty.** | route -> **docs.** Add a 'last code-synced' discipline: flip a SPEC status when its package ships; immediate: SPEC-AUTODIMENSION DRAFT->IMPLEMENTED, re-check SPEC-WALL-SINGLE-VOLUME-CSG plain-wall path. SS-SPEC-STATUS-CODE-SYNC. |
| L-349 | DOC-ARCH-READINESS-SCORECARD 2026-07-16 (Reference 2) | **[CRITICAL - HARMFUL] DR runbooks instruct staff to use tooling that DOES NOT EXIST.** `RUNBOOK-ACCIDENTAL-DELETE.md` directs to a trash UI (pryzm.app/trash), CS 'Admin Dashboard -> Trash -> Restore', and a `pryzm-ops restore-cold` CLL + cold-tier retention - none exist. `DELETE /api/projects/:id` (server.js:2911) is a PERMANENT cascade delete; no `deleted_at`/trash column anywhere. Same fiction in RANSOMWARE/DB-PRIMARY-FAILURE/REGIONAL-OUTAGE runbooks (all invoke non-existent `pryzm-ops`). A CS rep following this during a real incident promises an impossible recovery. | Reference / ops (**RUNBOOK-ACCIDENTAL-DELETE.md** + DR-runbook siblings; `server.js:2911` hard-delete; ties **L-344** C48) | **SHIPPED (2026-07-16, Track A - honesty) - REWROTE RUNBOOK-ACCIDENTAL-DELETE reality-first: a new §0 'REAL procedure today' (deletes are PERMANENT; the only recovery is a still-open client's version-history re-save, the DELETE `errorId` for log correlation, or a customer-held .pryzm export) + a bold reality banner; the fictional trash/cold-tier/`pryzm-ops` steps are quarantined under a 'TARGET STATE (C48 - NOT BUILT, L-344)' divider. Reality-check banners added to RUNBOOK-RANSOMWARE / DB-PRIMARY-FAILURE / REGIONAL-OUTAGE / DR-DRILL / runbooks README. A CS rep can no longer follow a step that promises impossible recovery. Real tooling remains L-344. Was OPEN (CRITICAL) - Track A this-week.** | route -> **ops/docs.** Rewrite the DR runbooks to the REAL hard-delete + version-history reality + a bold 'recovery tooling NOT yet built - deletes are permanent' banner; document the real `errorId` correlation key. Independent of whether L-344 builds the real tooling. SS-RUNBOOKS-MATCH-REALITY. |
| L-350 | DOC-ARCH-READINESS-SCORECARD 2026-07-16 (Reference 3) | **Dangling doc cross-references.** `docs/04-incidents/` (real = 04-reference/) cited in C48 + C42; `docs/archive/pryzm3-internal/runbooks/` cited 3x by the on-call guide - absent; `runbooks/incidents/` referenced, not created. Broken links during a real incident waste responder time. | Reference (**C48**, **C42**, `status-page-and-on-call.md`; broken relative md links) | **SHIPPED (2026-07-16, Track A) - repointed the dangling refs in the ACTIVE governance docs: C42 (docs/04-incidents/ -> docs/04-reference/runbooks/incidents/), C48 (04-incidents/runbooks|drills -> 04-reference/runbooks/), on-call guide (archive/pryzm3-internal/runbooks/ -> 04-reference/runbooks/); created docs/04-reference/runbooks/incidents/README.md so runbook §5 refs resolve. LEFT: sealed ADR-0051/0053 + pryzm-2-build post-mortem still cite the archive path (immutable per C31 - pre-existing debt, not introduced here). Was OPEN (LOW) - Track A 5-min fix.** | route -> **docs.** Repoint 04-incidents -> 04-reference/runbooks, archive/pryzm3-internal/runbooks -> 04-reference/runbooks; create runbooks/incidents/ or soften the reference. SS-FIX-DANGLING-DOC-REFS. |
| L-351 | DOC-ARCH-READINESS-SCORECARD 2026-07-16 (Guide 4-7) | **The GUIDE layer (what a human does with the system TODAY) is largely MISSING.** (a) NO real support runbook (project broken/gone -> steps with real tools; the only one is the fictional L-349); (b) on-call guide exists but placeholder-heavy + dangling DR pointers + thin on 'where the REAL logs are' (Fly logs); (c) customer security/privacy page = in-app TrustPage.ts but aspirational ('Region-locked PII - In flight', 'delete in 30 days' tied to nonexistent trash); no factual Frankfurt one-pager; (d) NO new-engineer onboarding explaining the two persistence formats + two sync systems - the exact confusion to pre-empt. | Guides (**docs/05-guides/**; `status-page-and-on-call.md`, `TrustPage.ts`; ties **L-337** formats + **L-335** sync + **L-349** support) | **PARTIAL (2026-07-16) - (d) new-engineer ONBOARDING shipped: docs/05-guides/developer/onboarding.md maps the two persistence formats (JSON snapshot v5 live vs .pryzm export, per ADR-0130) + the three sync code-paths (Yjs CRDT foundation / JSON-LWW prod / apps/sync-server relay) so the confusion can't be re-introduced. (a) real support runbook - partially met by the L-349 RUNBOOK-ACCIDENTAL-DELETE §0 reality rewrite. (b) on-call pointers - fixed by L-350. STILL OPEN: (c) a factual customer security/privacy one-pager (current Frankfurt-hosted posture), and a fuller support runbook beyond accidental-delete. Was OPEN (MEDIUM) - Track B/C authoring.** | route -> **docs.** (a) write a support runbook on real tools + 'deletes unrecoverable today'; (b) fix on-call pointers + add real-logs section + page-who; (c) ship a factual current-posture security one-pager + reconcile TrustPage claims; (d) write developer/onboarding.md mapping the two formats + two sync systems. SS-BUILD-GUIDE-LAYER. |
| L-352 | founder 2026-07-16 (prod pryzm.fly.dev/#/projects) + 2 screenshots + console log | **[P2 - PERCEIVED QUALITY] PROJECTS HUB FLASHES A STALE STATE ON FIRST PAINT (~1-2 s) then swaps to the correct server-synced list.** Opening `#/projects` first renders from LOCAL cache (screenshot 1: count 62, generic bar-chart PLACEHOLDER thumbnails, missing the 26-min-old 'Testing tower') then, after `[ProjectHub] Synced with server: 50 project(s)` resolves ~1-2 s later, a FULL re-render swaps to the correct list (screenshot 2: count 63, real 3D thumbnails). Root cause = optimistic local-first render reconciled against the server sync via a full-swap re-render, with thumbnails NOT persisted locally (so the first paint falls back to placeholders) and the local list slightly stale (62 vs 63). SECONDARY (to confirm in code): the log shows `[PlatformShell] Initialized - project ID: proj-1784211743396 (runtime: composed)` + renderer prewarm on the HUB route - a possible C02 §2/§121 deferred-boot concern (engine work before a project is opened). | Client boot / hydration + persistence (**C02** §2 boot-stages + §115 skeleton-removal + **§121 deferred-boot**; **C06** PlatformRouter landing/hub/editor; **C10** §1 NFT-1 first-paint TIMING only - not correctness; **C05** local project cache/thumbnails; **C13** lifecycle/isolation; `ProjectHub`, `PlatformRouter`, `PlatformShell`; ties memory SW-stale-cache-network-first + landing-skeleton-mismatch; **coverage GAP** - no spec governs hub hydration, see MISSING-CONTRACTS-AUDIT) | **OPEN (P2 - perceived quality; BUG).** Severity reasoning: purely visual flash-of-stale-content on the primary entry surface for ~1-2 s, no data-loss / no security / no functional break and the correct content does arrive - so P2 by impact rubric. FLAGGED FOR FOUNDER: strong P1-candidate on first-impression grounds (it is the first screen every user sees, on every open); the P1/P2 line is a launch-quality judgment call, not a contradiction - founder to confirm. | route -> **client boot/persistence queue (ProjectHub hydration)** - NOT sync/collab (symptom is UI render, cause is local-cache-vs-server reconciliation). CORRECT fix (multi-layer): (1) persist last-known-good project LIST + THUMBNAILS to local cache (network-first, cache-warm) so the first paint renders correct content INSTANTLY; (2) reconcile server diffs IN PLACE (patch added/removed/reordered cards) - never a full-swap re-render; (3) show a skeleton ONLY when no cache exists, never stale placeholder cards; (4) confirm + fix any C02 §121 deferred-boot violation (no PlatformShell/runtime bound to a default project on the hub route). SHORTCUT (NOT recommended): drape a spinner/skeleton over the hub until server sync completes - hides the flash but DELAYS all content + worsens perceived perf + contradicts 'instant'. GUARD: opening `#/projects` with a warm cache shows the correct list + real thumbnails on first paint with ZERO visible content swap; a cold (no-cache) open shows skeleton -> content, never stale placeholders. SS-FIX-PROJECT-HUB-HYDRATION-NO-STALE-FLASH. |
| L-353 | founder 2026-07-16 (strategy brief) | **[P3 - STRATEGY/R&D] GAUSSIAN SPLATTING as a core PRYZM capability - product-strategy + architecture review.** Founder brief (Principal-Graphics/Digital-Twin/CV lens): evaluate where 3D Gaussian Splatting adds GENUINE value across the AEC lifecycle (site capture, heritage, construction-progress, FM, inspections, scan-to-BIM, O&M) - not adopt it for novelty. Requested deliverables: product strategy; technical architecture (splats <-> Three.js/WebGPU/Cesium/IFC/GLTF/point-clouds/GIS/terrain); Splat-vs-PointCloud-vs-Mesh-vs-BIM comparison; top-10 workflows; AI opportunities; phased roadmap (MVP view -> collaboration -> AI digital-twin); and a core-vs-module-vs-workflow recommendation. NOTE: extends the EXISTING spike `spike-gaussian-splatting-photoreal-3d.md` (elevates it from a tech spike to a product-strategy review). | Reality-capture / geospatial / render (extends **spike-gaussian-splatting-photoreal-3d.md** + **spike-genrecon** + `pryzm-3d-geospatial-capabilities.md` + **SPEC-FORMA-SITE-VIEW** + **SPEC-GEODATA-ANALYTICAL-LAYERS** + **ADR-0065**-geodata-pluggable-provider; touches **C04** render/scheduling, **C10** perf-budgets (splats = millions of gaussians vs the 10k-element budget), **C05** persistence (new georeferenced asset type), **C13** isolation; **P2** single-THREE-owner + renderer-three boundary; STR-04 architecture, STR-12 site-and-cognition, geospatial-foundation strategic direction; **COVERAGE GAP** - NO contract governs reality-capture / Gaussian-Splat assets as first-class georeferenced project assets - see MISSING-CONTRACTS-AUDIT) | **OPEN (P3 - strategy/R&D; NEW-FEATURE research, NOT a bug).** Severity reasoning: forward-looking reality-capture capability exploration with ZERO V1-launch impact - a strategy/research deliverable, explicitly post-V1 (same bucket as the GenRecon watch-list reference). ARCHITECTURAL TENSIONS to resolve (flag for the research, not blockers): (1) **P2 single-THREE-owner** - splat rendering needs a custom sort/render pass which MUST live inside `renderer-three`, not a parallel renderer; (2) **C10 heavy-scene budgets + the WebGPU device-loss family** - splats are memory/GPU-heavy, must stream/tile, not load monolithically; (3) core-vs-module is itself the decision to make. | route -> **research / geospatial-reality-capture queue (post-V1 R&D strategy)** - NOT a V1 delivery queue. DELIVERABLE: a strategy doc (extend the existing spike, or a new `docs/01-strategy/` + `docs/03-execution/spikes/` pair) producing the 9-section analysis + the core/module/workflow recommendation. ARCHITECTURALLY-SOUND approach: follow the pluggable-provider pattern (ADR-0065) + Cesium 3D-Tiles georeferencing; treat splats as a first-class georeferenced project asset (like point clouds) rendered THROUGH renderer-three (P2), streamed/tiled, converted from external tools (Postshot/Luma/Polycam/Nerfstudio/RealityCapture) BEFORE any in-browser generation; recommend an OPTIONAL reality-capture MODULE until per-workflow value is proven (final call = the research's job). SHORTCUT (rejected): bolt a standalone splat viewer outside renderer-three - violates P2 + forks the render path. SS-STRATEGY-GAUSSIAN-SPLATTING-REALITY-CAPTURE. |
| L-354 | founder 2026-07-16 (site-analysis audit brief) | **[P2 - TRUST/CREDIBILITY] Site-analysis layers (Sun Hours, Wind Comfort, Temperature, Population Density) render as gradient heatmaps but their SOURCE, METHOD (measured/simulated/interpolated/estimated), resolution, accuracy, units, and CONFIDENCE are not surfaced - users cannot tell decision-grade from visualisation-only.** Founder audit asks, per layer: what it measures, why architects/planners care, how to read the gradient, colour-scale meaning, units, assumptions, static-vs-dynamic; PLUS probable source-of-truth (OSM/Cesium/Open-Meteo/Copernicus/WorldPop/NASA...), confidence score, better data sources, self-explanatory UI (legend+units+assumptions), an advanced-simulation roadmap (solar radiation kWh/m2 + facade/PV; CFD/Lawson wind; UHI/UTCI temperature; census/day-night population), and missing analyses (daylight, noise, flood, carbon, walkability, view-corridors, tree-canopy, stormwater). NOTE: sun-hours is already REAL raycast (C21/ADR-0074, @pryzm/solar-analysis); wind/temp/population credibility UNVALIDATED. | Environmental / site analysis (**C21-CLIMATE-INGESTION** + **ADR-0074** solar + **@pryzm/solar-analysis** + **SPEC-3D-ANALYTICS-FIXES-AND-OVERLAYS** + **SPEC-GEODATA-ANALYTICAL-LAYERS** + **ADR-0065** pluggable-provider; touches **C10** perf, **C18** element-preview-visual; **COVERAGE GAP** - no contract governs analysis-layer PROVENANCE / CONFIDENCE / units-labeling / the credibility bar - see MISSING-CONTRACTS-AUDIT) | **OPEN (P2 - trust/credibility; audit+gap).** Severity reasoning: layers render fine, but presenting un-provenanced/estimated environmental data with no confidence/units/'indicative-only' labeling risks users deciding on undefensible numbers - a TRUST risk; a **P1 sub-component** (provenance + confidence + decision-grade-vs-indicative labeling = honesty, ship pre-launch) sits inside the larger P2/P3 simulation roadmap. | route -> **environmental/geospatial analysis queue + data-provenance/UI.** CORRECT approach: a per-layer analysis-METADATA model (source, method, spatial+temporal resolution, accuracy, units, confidence, static/dynamic) surfaced in the Site Analysis panel (legend+units+assumptions), via the pluggable-provider pattern (ADR-0065); label each layer decision-grade vs indicative-only; THEN the phased advanced-simulation roadmap + missing-analysis ranking. SHORTCUT (rejected): add colour legends only, without provenance/confidence - cosmetically hides the credibility problem. SS-AUDIT-SITE-ANALYSIS-CREDIBILITY-PROVENANCE. |
| L-355 | founder 2026-07-16 (Forma review + console log) | **[P1 - PERF + strategy] The Forma '3D Site' view loads WAY TOO SLOWLY and needs a production-readiness review (perf/robustness/graphics).** ROOT CAUSE (from the log): view activation does heavy SYNCHRONOUS work on the critical path - the Forma 'REAL full-fidelity model' is produced by EXPORTING THE ENTIRE BIM SCENE TO A 71 MB GLB (2574 root elements, blob 71,177,808 bytes) on activation, PLUS extruding 39 storey bands + 920 opening insets + 78 stair volumes + 41 slabs, PLUS fetching + extruding 4342 OSM context footprints via the Overpass proxy. Also runs with NO real terrain/imagery ('terrain clamp degraded - keyless ellipsoid', 'no real tile provider - keyless flat-ground study'). SECOND FACET (strategy - CONTEXT DATA MATURITY): context is low-fidelity (OSM/Cesium-derived, not LOD2+, simplified terrain); founder requests a provider-agnostic **Context Engine** (Terrain/Imagery/Building/Vegetation/Road provider adapters over Cityweft / Google Photoreal 3D Tiles / Cesium ion / Esri / OSM / gov-LiDAR) for engineering-grade context, EVOLVING the existing impl not replacing it, rendered inside the Three.js canvas at global scale. | Geospatial / render / perf (**SPEC-FORMA-SITE-VIEW**; **ADR-0065** geodata-pluggable-provider; `CesiumViewport` forma path, `contextBuildings`, `engineLauncher` forma activation, BIM->GLB export; **C04** render/scheduling, **C10** perf-budgets + heavy-scene/WebGPU-device-loss family, **C13** isolation; extends the prior '3D Site took too long' memory + **L-272** facade-drape; **COVERAGE GAP** - no contract governs a provider-agnostic Context Engine / contextual-GIS fidelity tiers, see MISSING-CONTRACTS-AUDIT) | **OPEN (P1 for the load-perf facet; P2/P3 for the Context-Engine strategy facet). BUG + strategy.** Severity reasoning: the primary 3D Site view taking 'way too long' is a production-readiness blocker on a core, actively-demoed feature (P1); the context-fidelity upgrade is a large architectural roadmap (P2/P3). | route -> **geospatial/Forma-view queue (PERF, P1) + a Context-Engine architecture spike (STRATEGY, P2).** PERF (P1): get the 71 MB BIM->GLB export + heavy massing rebuild + 4342-footprint Overpass fetch OFF the synchronous activation path - confirm WHY a full GLB export runs on every Forma activation and cache/incrementalise/worker-ize it; progressive camera-radius (~500 m) tiled context-building streaming; cache Overpass responses. STRATEGY (P2): a provider-agnostic Context Engine with Terrain/Imagery/Building/Vegetation/Road interfaces + adapters, rendered THROUGH renderer-three (P2 single-THREE-owner), streamed/tiled, EPSG-transformed - a phased roadmap that evolves the current Cesium/OSM path (Cesium stays for globe-scale nav). SHORTCUT (rejected): raise the load timeout / show a longer spinner - hides the cost, fixes nothing. SS-FORMA-VIEW-PERF-AND-PROVIDER-AGNOSTIC-CONTEXT-ENGINE. |
| L-356 | founder 2026-07-16 (Cesium 3D-tiles audit + console log) | **[P2 - PERF-HARDENING] Cesium 3D-tiles globe view: location + render are SOUND (founder confirms 'rendering perfectly'); audit for MORE robustness + speed.** Same root cause as **L-355**: globe activation runs a full BIM->GLB export (2506 elements, blob 69,697,220 bytes ~= 69 MB) on the critical path, PLUS the massing is rendered TWICE (storey 0 extruded at flat base 0.0 m, then RE-PLACED at 64.1 m after the photoreal-tile clamp resolves) = redundant work, PLUS a WallFragmentBuilder RAF-drain queue (67->0) finishing just before activation. The anchor/georef chain (photoreal-tile-clamp, retriesLeft=12, seat-and-reveal) is robust but adds latency. | Geospatial / Cesium globe / perf (**SPEC-FORMA-SITE-VIEW**; **ADR-0065**; **ADR-0059** site-plan-overlay; `CesiumViewport` globe path, BIM->GLB export, §FIX-CESIUM-GLOBE-ELEVATION-AND-GEOREF clamp; **C04**, **C10** perf + heavy-scene/WebGPU-device-loss, **C13**; **SHARES L-355 root cause**; cluster with L-353/L-355/L-357) | **OPEN (P2 - perf-hardening; view functionally SOUND).** Severity reasoning: it renders correctly and the founder is happy with fidelity - this is optimization, not a blocker (P2); but it carries the SAME ~69 MB GLB-export-on-activation cost flagged P1 in L-355. | route -> **geospatial/Cesium-view queue (shared workstream with L-355).** Approach: (1) cache/incrementalise the BIM->GLB export - do NOT re-export the whole scene per activation (shared fix with L-355 P2); (2) eliminate the double massing render (place ONCE after the clamp resolves, not flat-then-reclamp); (3) 3D-tiles LOD/screen-space-error + cache tuning; (4) keep the anchor-retry chain but parallelise with content. SHORTCUT (rejected): longer spinner - hides the cost. SS-CESIUM-3DTILES-ROBUSTNESS-AND-SPEED. |
| L-357 | founder 2026-07-16 (GIS & 3D context maturity architecture review) | **[P2/P3 - STRATEGY/ARCHITECTURE] Should PRYZM CANVAS (Three.js) itself become GIS-AWARE instead of relying solely on Cesium?** Founder architecture review: today Cesium = GIS layer, Canvas = modelling layer (models in local coords, georeferenced into WGS84/ECEF for Cesium). Requested: rate maturity Level 0-5 (0 local / 1 georef-in-Cesium / 2 GIS-aware modelling / 3 streaming GIS in Three.js / 4 digital-twin / 5 city-scale); feasibility of native Three.js loading DEM/DTM/DSM/terrain-mesh/3D-Tiles/vector-tiles/CityGML/GeoJSON/GLTF/CityJSON/point-clouds/orthophotos WITHOUT Cesium; coordinate-system strategy (local engineering vs projected vs WGS84 vs ECEF vs ENU - local tangent plane + stored global georef?); terrain representation; context-data stack (Cityweft/Cesium-ion/Google-3D-Tiles/Mapbox/OSM/OpenTopography/national-LiDAR/gov-DSM-DTM); rendering architecture (A: Canvas<->Cesium dual, vs single Three.js GIS engine, vs HYBRID - Cesium=globe-nav only, Three.js=editing+context); phased roadmap; final assessment (maturity, biggest gap, biggest risk, easiest high-value win, path to Cityweft quality). This is the ARCHITECTURAL PARENT of the L-355 Context-Engine question. | Geospatial architecture (**STR-04 architecture / P2 single-THREE-owner** is CENTRAL - a native Three.js GIS engine MUST keep renderer-three the sole THREE owner; **C04** render, **C10** perf + 10k-vs-city-scale, **C13** isolation; **ADR-0065** pluggable-provider; `pryzm-3d-geospatial-capabilities.md`; geospatial-foundation strategic direction; ties L-353/L-355/L-356; **COVERAGE GAP** - no contract defines PRYZM's GIS-maturity target OR the coordinate-system authority (local-tangent-plane + global georef), see MISSING-CONTRACTS-AUDIT) | **SPIKE DELIVERED (bf5e9d9b, 2026-07-16) - docs/03-execution/spikes/spike-canvas-gis-awareness-maturity-2026-07-16.md: maturity rated Level 1.5/5 (Level-2 coordinate/data substrate feeding a Level-1 Cesium-only renderer over a Level-0 native-Three context layer); recommends HYBRID (keep Cesium for globe-nav + coordinate/terrain authority; build a provider-agnostic Context Engine INSIDE renderer-three via committer/overlay seams). KEY STRUCTURAL GAP found: renderer-three has NO public render-pass extension point (passes hardcoded in RenderPipelineManager) - the enabling seam for L-353 splats / point-clouds / terrain-clip; easiest high-value win = fix the ~70MB full-scene GLB re-export invalidated on every globe<->forma round-trip (= L-355 P1 + L-356 root). Follow-on decisions (build Context Engine, add pass registry) tracked in the cluster. Was OPEN (P2/P3 - strategy/architecture review; NEW strategic direction, not a bug).** Severity reasoning: forward architecture decision with no V1-launch impact, but it decides a MAJOR platform direction (Cesium-optional vs Cesium-essential) that gates the Context-Engine (L-355) + reality-capture (L-353) work. | route -> **GIS/geospatial ARCHITECTURE spike (strategic) - the PARENT decision for the L-353/L-355/L-356 cluster.** DELIVERABLE: the 8-section maturity review + a rendering-architecture recommendation. Likely-sound direction (for the research to confirm): HYBRID - keep Cesium for globe-scale navigation + coordinate transforms, build a provider-agnostic Context Engine INSIDE renderer-three (P2) for editing + streamed local context (DEM terrain, orthophoto drape, LOD2 buildings, instanced vegetation, roads), with PRYZM operating in a local tangent plane (ENU) while storing a global georef. SHORTCUT (rejected): bolt a SECOND Three.js GIS renderer outside renderer-three - violates P2 single-THREE-owner + forks the render path. SS-CANVAS-GIS-AWARENESS-MATURITY-AND-HYBRID-ARCHITECTURE. |
| L-358 | L-357 spike follow-up (§0.4 / §7 Phase-0 / §8 'easiest high-value win') | **[P2 - PERF BUG] The ~70MB full-scene BIM->GLB export re-runs on EVERY globe<->Forma round-trip because the `computeBuildingSignature` cache invalidates on view-mode switches, not on actual model change.** The `§CESIUM-PERF-GLOBE-GLB-CACHE` in `GISAreaLayout.ts` skips re-export only while the SAME view's primitive is still live; a cross-view round-trip (globe->forma->forma / forma->globe->globe) clears the other view's primitive (`clearRealModelOnGlobe` / `invalidateFormaRealCacheOnPhotorealGlobeEntry`), forcing a fresh full-scene `exportFragmentsToGLB` (2500+ elements) even when the BIM geometry never changed. SHARED ROOT with **L-355** (P1 facet) + **L-356**. SECONDARY FINDING: the cache-invalidation rule is an undocumented code-level tag (no contract/ADR governs it) and C10 has NO view-activation NFT to catch it - so this is a real budget concern with no gate (fold the NFT + cache rule into C-CONTEXT-ENGINE, L-359). | Rendering / export / perf (**C04** render/scheduling, **C10** perf-budgets [no view-activation NFT = secondary gap], **C12** geospatial substrate; `GISAreaLayout.ts` export call sites ~1957/2111/355/439, `exportFragmentsToGLB` @pryzm/file-format, §CESIUM-PERF-GLOBE-GLB-CACHE; ties **L-355/L-356/L-357**) | **OPEN (P2 - real felt perf cost: a multi-second stall re-serialising 2500+ elements on every globe/Forma activation; the view still FUNCTIONS + it is off the core editing path, so NOT a launch-blocker per the spike scoping -> P2. Demo-prominent, so could be treated P1 for the showcase.) BUG. FIX SHIPPED 450b9453 (2026-07-16, on main + deploying; diff-reviewed)** - caches the exported GLB *bytes* by (buildingSignature, plain|formaWhite variant), mints a fresh viewport-owned URL per placement (the viewport revokes URLs on the round-trip clear, so a cached URL would be dead - bytes are the correct decoupling); cross-view round-trips with UNCHANGED geometry re-place from cache and skip the ~70MB re-export, while a geometry edit bumps the signature -> fresh export. Bounded eviction (1 blob/variant), safe fallback to the raw uncached export on snapshot failure (never worse than pre-fix), root tsc 0. Legacy toggleGIS/placeBimOnEarth paths deliberately untouched. FOLLOW-ON (P2, still OPEN): move export off the sync path onto a Web Worker + diff export for the changed-geometry case. | route -> **geometry/export-perf queue (by ROOT CAUSE = the export pipeline, NOT the GIS queue by symptom-location).** CORRECT fix (in progress) = fix the cache KEYING: memoize the exported blob on (buildingSignature, plain|formaWhite variant) so it invalidates ONLY on real geometry change, never on a view-mode switch. This is NOT merely 'widen the key' - it repairs the actual defect (invalidating when nothing changed). FOLLOW-ON (separate, P2): move the export OFF the synchronous activation path onto a Web Worker + a partial/diff export, so even a genuine geometry change doesn't stall on a full 70MB serialization (round-trip case = most of the felt cost; changed-geometry case = the remaining slice). SS-FIX-GLB-EXPORT-CACHE-INVALIDATION. |
| L-359 | L-357 spike follow-up (§7 Phase-0 / §8 'C-CONTEXT-ENGINE coverage gap') | **[P3 - GOVERNANCE GAP, NO LAUNCH PRESSURE] No contract owns the contextual-GIS foundation.** Missing an authority for: (a) the COORDINATE authority (LTP-ENU local tangent plane + stored global georef); (b) GIS FIDELITY TIERS (LOD1 vs LOD2/3, DEM/DTM/DSM); (c) the PROVIDER model (ADR-0065-aligned Terrain/Imagery/Building/Vegetation/Road interfaces); (d) the PROVENANCE / credibility bar (L-354). This is DOCUMENTATION/GOVERNANCE debt - NOT a code bug and NOT a launch risk. **CONSOLIDATES** the prior missing-contracts gap notes filed via **L-355** (Context Engine un-contracted) + **L-357** (coordinate authority + GIS-maturity) into ONE named contract to author: **C-CONTEXT-ENGINE**. | Governance / geospatial architecture (consolidates MISSING-CONTRACTS-AUDIT L-355 + L-357 entries; aligns **ADR-0065** pluggable-provider, **C12** geospatial substrate, **C10** perf-budget for context streaming, **P2** single-THREE-owner; ties L-353/L-354/L-355/L-356/L-357) | **OPEN (P3 - BACKLOG; EXPLICITLY NOT time-pressured against the September launch - documentation debt, not a launch risk). GAP (missing contract, not a defect).** | route -> **contract-authoring / architecture-documentation queue (NOT an engineering queue) - low-priority/backlog.** Authoring scope: a **C-CONTEXT-ENGINE** contract covering (a) coordinate authority (per spike §3), (b) fidelity tiers (§4/§6), (c) provider model (ADR-0065-aligned interfaces, §6), (d) provenance/credibility bar (L-354) - WITHOUT designing Phase 1-4 implementation (contract = documentation only at this stage; no code). Gates the L-353/L-355/L-356 downstream work. SS-AUTHOR-C-CONTEXT-ENGINE-CONTRACT. |
| L-360 | founder 2026-07-16 (prod, reopen after batch-gen) + console | **[P0 - REGRESSION I SHIPPED] The L-334 snapshot CHECKSUM hard-REFUSES valid projects, bricking them.** `[ProjectLoader] §L-334 CORRUPT snapshot (checksum-mismatch) — refusing to load` on a real 1009-element project (stored 9652c199 vs computed 5c4be4a8). ROOT (strong hypothesis, under confirmation): the checksum is computed at SAVE over the PRE-compression in-memory snapshot (`ProjectSerializer.ts:980`) but VERIFIED at LOAD over the POST-decompression snapshot; the stored blob is COMPRESSED (~1.2 MB) with a codec that is NOT bit-exact round-trip (draco geometry quantization / MessagePack number coercion) → loaded != saved → the checksum (correctly) differs → HARD-REFUSE → the user is LOCKED OUT of their own project. This is WORSE than the original silent-drop it replaced. `canonicalStringify` itself is sound (sorts keys, drops undefined, NaN→null) so the fault is the compute-layer mismatch + the hard-refuse policy. | Persistence (**C05** persistence, **C47** versioning; INTRODUCED BY **L-334**; `SnapshotIntegrity.ts` computeSnapshotChecksum, `ProjectSerializer.ts:978-983`, `ProjectLoader.ts` refuse-path, persistence codecs Msgpack/draco) | **OPEN (P0 - REGRESSION). FIX IN PROGRESS** (agent dispatched 2026-07-16). 🔎 CODE-VERIFIED 2026-07-18 (register reconciliation): the checksum that bricked valid projects is NO LONGER IN THE TREE. `SnapshotIntegrity.ts` / `computeSnapshotChecksum` (cited in this row) and the `ProjectSerializer.ts:978-983` stamp + `ProjectLoader.ts` refuse-path do NOT exist in the current source — repo-wide grep of `.ts/.js` = 0 matches (docs/reports only). The regression was resolved by REVERTING the L-334 checksum ENTIRELY, not by making it round-trip-stable — so the hard-REFUSE/brick vector described here (`§L-334 CORRUPT snapshot … refusing to load`) is absent from the current code. This ALSO removes L-334's corruption detection (net: no checksum either way — see L-334 note). Do NOT mark Fixed: that a previously-bricked project now reopens is a RUNTIME claim, UNVERIFIED (needs live confirmation); this note records only the static code reality. | route -> **persistence queue.** CORRECT fix (2 parts): (A) make the checksum ROUND-TRIP-STABLE — compute + verify over the SAME representation (checksum the post-codec bytes, or checksum the snapshot after the exact codec round-trip persistence applies); (B) a checksum-mismatch must NEVER hard-brick — LOAD BEST-EFFORT + a loud persistent 'integrity check failed, may be corrupted' warning + per-element quarantine (existing L-334 machinery); keep HARD refuse ONLY for genuinely unparseable/forward-version snapshots. GUARD: a real-codec round-trip keeps the checksum stable; a wrong stored checksum LOADS with a warning (not refused); an unparseable snapshot is still refused. SS-FIX-CHECKSUM-ROUNDTRIP-AND-NEVER-BRICK. |
| L-361 | founder 2026-07-16 (prod, batch AI resi-building on WebGPU) + console + screenshots | **[P0 - CRASH CASCADE] Batch AI residential-building creation on WebGPU crashes the GPU repeatedly until the BROWSER disables GPU acceleration → then NO project can be created/opened.** Cascade: batch resi-gen (curtain-wall PSO storm, `CreateCurtainWallsOnAllSlabsCommand` re-prewarm) + `THREE.TSL: Invalid generated code, expected a "float"` (shader-gen bug) on an ~8000-mesh scene → `WebGPU Device Lost: A valid external Instance reference no longer exists` → §FIX-HEAVY-SCENE device-loss cap 2/2 → WebGL safe-mode → WebGL context ALSO lost → the browser locks GPU for the session (`GL_VENDOR=Disabled`, `BindToCurrentSequence failed`) → `§GPU-DISABLED-GRACEFUL-BOOT` recovery overlay; a page refresh does NOT clear it, only a full browser restart. App becomes UNUSABLE after the flagship batch-gen. SAME FAMILY as **L-139** (marked FIXED 2f91af03 — but STILL occurring; the fix was incomplete). PRYZM's graceful-boot overlay is CORRECT handling of the terminal state; the ROOT (the repeated device-loss trigger) is unfixed. | Render / WebGPU device-loss (**C04** render/scheduling + device-loss recovery, **C10** heavy-scene perf-budget, **ADR-0089** non-fatal-webgpu-device-loss-recovery; ties **L-139**, the L-312 reconstruction-boundary family, §SHADOW-DEVICE-LOSS-FIX, [[webgpu-heavy-scene-crash-and-instancing]]; `renderer-three` TSL/PSO, `CreateCurtainWallsOnAllSlabsCommand`, RenderPipelineManager shadow-rebuild) | **OPEN — ROOT CONFIRMED, FIX IN REVIEW (not yet Fixed; P0). UNASSIGNED / TBD.** UPDATE 2026-07-17: root cause of the `THREE.TSL: Invalid generated code, expected a "float"` seed = a **non-finite `attenuationDistance` (THREE `Infinity` default) on transmission glass** — on WebGPU the transmission/Beer-Lambert TSL node reads it and the WGSL float generator rejects it → device loss (WebGL never runs the TSL path; founder confirmed identical batch renders clean on WebGL). FIRST FIX (b7348add) DID NOT HOLD: it clamped only `ClearcoatMaterialUpgrader`, but Batch-AI mints glass DIRECTLY via `WindowBuilder.makeGlassMat` (+ furniture builders) which never passes through that upgrader (`onSceneGeometryAdded` runs the PBR upgrader only) — same "marked Fixed but incomplete" pattern as L-139. SECOND FIX (committed, awaiting human review of the live-render diff + deploy): finite `attenuationDistance` at all 4 direct factories via a shared `@pryzm/renderer-three/materials` → `transmissionSafe()` chokepoint (transmission glass stays ON, visually lossless), a static CI guard test that fails if any `MeshPhysicalMaterial` sets `transmission` unwrapped (proven RED at WindowBuilder:120 → GREEN 8/8 — the regression test missing from attempt #1), and the `VisualizationEnginePanel.ts:594` upgrader-gate call-site wired (defense-in-depth, not the batch cause). Root tsc 0 errors. **Does NOT get marked Fixed a third time without human diff review + live WebGPU confirmation.** Residual follow-up: offline `GLBExporter.getGlass()` (not live-render). **UPDATE 2026-07-17 (GROUND TRUTH via §L-361-DIAG on 6e909584):** live batch-start diagnostic printed `OK — 6 physical materials, 6 with transmission>0, ALL floats finite` and the batch STILL device-lost → **the non-finite material float is RULED OUT as the cause.** The two float-clamp fixes (b7348add upgrader, ae40bc5b factories) are correct hardening but did NOT fix this crash. Real seed per the live log = `WebGPU Device Lost: "A valid external Instance reference no longer exists" (reason: unknown)` with NO accompanying `TSL: expected a "float"` (captureStackTrace on) — i.e. a **GPU-driver-level device loss under heavy batch load** (CW PSO-compile storm via `CreateCurtainWallsOnAllSlabsCommand` re-prewarm + shadow-rebuild at ~6300 meshes); the "Disposing Cesium…" cross-context GL errors are Cesium's `recoverFromGpuReset` REACTING to the reset, downstream not causal. **REDIRECTED to the heavy-scene WebGPU device-loss family (L-139/L-312 / P2 "CW PSO storm"), NOT the material/shader path.** Investigation in progress. **ROOT CONFIRMED 2026-07-17 (mechanism trace + git bisect on founder's known-good anchor `6933d322`/#1110):** the loss is a **WebGPU GPU-process watchdog reset (TDR) from over-long SYNCHRONOUS submits** — msg `external Instance no longer exists`/`Instance dropped in popErrorScope`, `reason:"unknown"` (NOT app `device.destroy()`; THREE owns its own device, no external instance injected — `WebGPURendererAdapter.ts:135-163`). Two synchronous submits on the ~6300-mesh scene: (1) **CW prewarm's 4 back-to-back `rpm.render(0)`** full-scene renders (`CreateCurtainWallsOnAllSlabsCommand.ts:967-969,1003`) — NO mesh-count ceiling/slicing, and self-perpetuating via §PREWARM-SCALE-GUARD + device-loss `__resetCwPrewarm`; (2) **per-sub-batch full `_rebuildPipeline()`** (`RenderPipelineManager.ts:1286`, violates C04 rules 7/8). Cesium is a downstream REACTION (shares no canvas/GPUDevice; `recoverFromGpuReset` only reads recovery flags). **BISECT:** the CW prewarm 4-render storm PRE-EXISTED at known-good `6933d322` (`git show 6933d322:…CreateCurtainWallsOnAllSlabsCommand.ts` has §PREWARM-SCALE-GUARD + 4 renders) → NOT the regression; the regression is **`ec36b556` (L-314 §SS-FIX-FIRST-LOAD-SHADOW-REATTACH + shadow/TSL changes, +169 RPM lines)** which `git show 6933d322` confirms did NOT exist at known-good (`_maybeReattachFirstLoadShadow`/`FIRST-LOAD-SHADOW-REATTACH` absent) — it ADDED compounding full-pipeline-rebuild load that tips the pre-existing prewarm over the TDR watchdog. FIX DIRECTION (agent-recommended, aligns C04 7/8 + ADR-0094): (a) slice the CW prewarm's 4 synchronous renders across frame-scheduler frames / apply ADR-0094's `POSTGEOM_SYNC_COMPILE_MAX_SCENE_MESHES` ceiling to the prewarm; (b) coalesce shadow `_rebuildPipeline` to ONCE per whole generation (hold one suppression start→settle), not per sub-batch; (c) gate the L-314 reattach off the heavy-batch path. Immediate workaround: WebGL backend (renders identical building clean — founder-confirmed). | route -> **render / renderer-three (WebGPU device-loss) queue — by ROOT CAUSE, not resi-building.** CORRECT fix (multi-layer): eliminate the device-loss TRIGGER, not just recover — (1) the `THREE.TSL: Invalid generated code` shader-gen bug (a WebGPU material/node emitting invalid code — likely the actual crash seed); (2) the curtain-wall PSO/shadow-rebuild storm on batch creation at ~8000 meshes (L-312B shared CW materials but the storm persists — investigate the repeated SHADOW_REBUILD + per-floor re-compile); (3) keep the graceful-boot overlay. WORKAROUND (immediate, this Windows box): use the WebGL backend for batch generation ([[render-reconstruction-boundary-gpu-reset]] — WebGL is the stable demo backend). SHORTCUT (rejected): rely on device-loss recovery — it can't recover a browser-locked GPU. SS-FIX-WEBGPU-BATCH-RESI-DEVICE-LOSS-CASCADE. |
| L-362 | founder-approved fix for L-361 (2026-07-17) | **[P0 mitigation - RENDER] Auto-mode proactive WebGL fallback for heavy scenes.** The architecturally-sound mitigation for **L-361**: rather than keep chasing per-trigger WebGPU seeds (whack-a-mole against a driver-level TDR), stop sending heavy scenes to a backend that cannot survive them on this hardware. Changes ONLY the **Auto** backend mode: when a scene becomes device-loss-risk (the exact `LevelScoped3DCullingService.isHeavyModel` heuristic — ≥ 15 levels AND ≥ 1000 elems, OR ≥ 4000 elems) AND Auto mode is active on a **real WebGPU** device (`status.webGpuActive`), proactively live-swap WebGPU→WebGL ONCE per session BEFORE the heavy PSO-compile storm that TDRs the GPU (L-361). Fired PROACTIVELY at the batch GPU-compile-start hook (batched office/apartment/CW/slab — geometry fully in scene, still pre-render) and the per-add tier pass (non-batched residential). **Explicit WebGPU / WebGL selection is ALWAYS respected** (Auto is the only adaptive mode); the reactive device-loss recovery (§FIX-HEAVY-SCENE-3D-SCALABILITY cap → WebGL safe-mode, ADR-0089) is retained as the net. Light scenes on Auto keep WebGPU. | Render / WebGPU device-loss (**C04** §1.4 backend-selection amendment, **C10** heavy-scene budgets; **ADR-0267** the decision; reuses **ADR-0077 §RENDERER-LIVE-SWAP** live swap + **ADR-0076 §PERF-WEBGPU-FRAGMENT** toggle; complements **ADR-0089** recovery; new `apps/editor/src/rendering/autoWebGLHeavyScene.ts`, `initBatchLifecycle.ts` GPU-compile-start hook, `initScene.ts` `runTierPbrPass`, `LevelScoped3DCullingService.isHeavyModel` exported; MITIGATES **L-361**, ties **L-139**) | **OPEN — IMPLEMENTED, awaiting live WebGPU confirmation (P0 mitigation). UNASSIGNED / TBD.** Guarded strictly: Auto-only + real-WebGPU-only + once-per-session (module guard set before the async swap, mirrored to `globalThis.__pryzmAutoSwappedToWebGL`) + `isHeavyModel`-tripped; explicit-WebGPU logs a one-time "respecting override" warning and does NOT swap. Root tsc 0, `@pryzm/core-app-model` typecheck 0. **Does NOT get marked Fixed without a live WebGPU-box run confirming the heavy resi/office batch swaps to WebGL and renders clean (Auto), while explicit WebGPU still honours the override.** | route -> **render / renderer-three (WebGPU device-loss) queue.** Log to watch: `§AUTO-WEBGL-HEAVY — scene is device-loss-risk (N elems / M levels …) Auto mode switching WebGPU→WebGL …`. GUARD: Auto + heavy + real-WebGPU → exactly one swap before the PSO storm; explicit WebGPU → no swap (override warned once); light scene / WebGL2-fallback / already-swapped → no-op. SS-AUTO-WEBGL-FALLBACK-HEAVY-SCENES. |
| L-363 | orchestrator 2026-07-17 (re-apply on #1110 + Auto-WebGL base) | **[P0 - RENDER, light-scene WebGPU path] `[ScenePass] TSL module not loaded. Call initTSL() before createScenePass()` spams a loop on batch-start / first render.** A batch's `runBatch → _setupBatch → _onBatchStart → autoEnablePerf → _enablePerfMode → _setSsgi → _fullRebuild → createScenePass()` throws because `__PRYZM_TSL__` is not loaded yet: `bind()` sets `_webGpuActive = true` BEFORE awaiting `_loadTSL()`, so a batch landing in that window calls `createScenePass` pre-`initTSL`. Originally fixed by **L-319 §SS-FIX-TSL-NOT-LOADED-BEFORE-SCENEPASS** (commit `ec36b556`) but a rollback to the #1110 base removed it. **RE-APPLIED** (only the L-319 slice; the co-bundled L-314 first-load-shadow-reattach was DELIBERATELY NOT re-applied — it caused the WebGPU shadow-rebuild device-loss regression and must stay absent). | Render / WebGPU (**C04** §1.4 TSL/backend safety; `packages/renderer-three/src/pipeline/RenderPipelineManager.ts` — `_tslLoaded` getter + guards in `_buildPipeline` / `_buildPhase3Pipeline` / `_fullRebuild`; cross-refs **ADR-0267** Auto-WebGL — this hardens the NON-swapped light-scene WebGPU path that Auto-WebGL does not cover; ties **L-319**, **L-361**, **L-362**) | **OPEN — RE-APPLIED, awaiting live WebGPU confirmation (P0). NOT marked Fixed.** Re-application of the exact L-319 guard: every `createScenePass()` caller short-circuits with a warn when `__PRYZM_TSL__` is absent; `bind()` still awaits `_loadTSL()` before its own `_buildPipeline()`, so the guard only defers an EARLY external trigger (the batch autoEnablePerf path) until TSL resolves. renderer-three typecheck 0, root tsc 0. **Does NOT get marked Fixed without a live WebGPU run confirming the batch no longer spams the ScenePass-TSL error.** | route -> **render / renderer-three (WebGPU) queue.** GUARD: batch autoEnablePerf during the bind() TSL-load window → deferred (warn), NOT a throw; pipeline builds once via bind() after TSL loads. Confirm NO first-load-shadow-reattach code is present. SS-FIX-TSL-NOT-LOADED-BEFORE-SCENEPASS-REAPPLY. |
| L-364 | orchestrator 2026-07-17 (re-apply on #1110 + Auto-WebGL base) | **[P0 - RENDER, light-scene WebGPU path] `THREE.TSL: Invalid generated code, expected a 'float'` in `_renderTransparents` spams the loop.** The transmission-glass `MeshPhysicalNodeMaterial` transmission/refraction TSL node graph emits invalid WGSL on three r183's WebGPU backend during `_renderTransparents` PSO compile → device loss — INDEPENDENT of finite floats (the live §L-361-DIAG proved every glass scalar is finite). Originally fixed by **§L-361-WEBGPU-TRANSMISSION-GUARD** (commit `715a222d`) but the #1110 rollback removed it. **RE-APPLIED** (transmission neutralizer + its `setShadowPassDisabled` hook only; the co-bundled `_diagScanTransmissionFloats` diagnostic and the L-314-tied REGRESSION-GATE were skipped — L-314 is absent here). | Render / WebGPU (**C04** §1.4 TSL/backend safety; `packages/renderer-three/src/pipeline/RenderPipelineManager.ts` — `_neutralizeTransmissionForWebGPU()` + call from `setShadowPassDisabled(reason, disabled)` when `disabled && reason === 'batch'`; cross-refs **ADR-0267** Auto-WebGL — hardens the NON-swapped light-scene WebGPU path; ties **L-361**, **L-362**) | **OPEN — RE-APPLIED, awaiting live WebGPU confirmation (P0). NOT marked Fixed.** On the real WebGPU backend (`_webGpuActive`) only, at each batch boundary, every `MeshPhysicalMaterial` with `transmission > 0` is set to `transmission = 0` + opacity fallback (0.5 if opaque) + `needsUpdate`, so the node material rebuilds WITHOUT the transmission node. WebGL never runs the TSL path and is untouched (keeps refractive glass). Idempotent. renderer-three typecheck 0, root tsc 0. **Does NOT get marked Fixed without a live WebGPU run confirming the batch no longer emits the "expected a float" TSL error.** | route -> **render / renderer-three (WebGPU) queue.** GUARD: batch-start on real WebGPU → transmission glass neutralized to opacity glass before the post-batch render (transmission node never emitted); WebGL → no-op (full refractive glass). SS-FIX-L361-WEBGPU-TRANSMISSION-GUARD-REAPPLY. |
| L-365 | founder 2026-07-17 (live Fly run + console anchor-evidence log) | **[VERIFIED WORKING — canonical baseline] Cesium 3D-Tiles (photoreal globe) georeferenced building placement is SOUND end-to-end and is now mapped into the governance docs.** The residential building places correctly on the Google Photorealistic 3D-Tiles: anchored at **LAT 40.420070, LON -3.705955** = the LTP-ENU origin (anchor↔LTP **0.0 m**); ground datum resolved via `§FIX-CESIUM-GLOBE-ELEVATION-AND-GEOREF` **photoreal-tile-clamp** → base **706.90 m ELLIPSOIDAL WGS-84** (tile mesh IS the ground, no terrain provider); **seat-and-reveal** holds the building HIDDEN until the datum resolves then reveals it (L-259; `retriesLeft` countdown; §A.21.D49); `§FORMA-FULL-HEIGHT` extrudes the full tower (per-storey 4-vertex perimeter prisms + slabs/roof, 214 opening insets, 5 stair volumes; was a 19.5 m stub); framed via `§GLOBE-FIT-BUILDING` `flyToBoundingSphere` + `§GLOBE-FRAME-NO-JUMP` re-frame-once; `CesiumThreeBridge` shares only the THREE camera/scene (NOT the GPUDevice/canvas) + `§RENDERER-LIVE-SWAP`. Baseline tag **`snapshot-cesium-3d-globe-working-2026-07-17`**. | Geospatial / Cesium globe (**C12 §7** [new, Known-good ACTIVE] + **C12 §1.4**; **ADR-0268** [new] the georeferenced-placement decision; **SPEC-FORMA-SITE-VIEW §11** [new] the pipeline; builds on **ADR-0095** globe-clamp; `apps/editor/src/ui/geospatial/globeGroundAnchor.ts` [pure `resolveGlobeGroundAnchor`/`decideGroundAnchorAction`], `CesiumViewport.ts` [`renderRealModelOnGlobe:7530`, `renderFormaMassing:3021`, `resolveFullBuildingHeight:8035`, `tileBandsToFullHeight:8081`, `revealGlobeBuildingForGround:4352`, `logGlobeAnchorEvidence:4391`], `packages/renderer-three/src/geospatial/CesiumThreeBridge.ts`; ties **L-259**, **L-198**, cluster **L-355/L-356/L-358**) | **VERIFIED WORKING (live on Fly, 2026-07-17). DOCUMENTED — ADR-0268 + C12 §7 + SPEC-FORMA-SITE-VIEW §11 authored to keep the known-good behavior canonical.** C12 §7 marked **Known-good ACTIVE** with the live-run evidence noted; no Tier-1 contract flipped to ACTIVE on inspection alone (§7 is a behavior invariant, not a §6 CI gate). NOT a defect — this row records the verified-good baseline so any future regression is measured against the snapshot tag. Perf-hardening of the SAME activation path stays tracked under L-355/L-356/L-358 (69–71 MB GLB export cost) — those are separate and unaffected by this documentation. | route -> **docs / governance (mapped) + geospatial (baseline owner).** Behavior is confirmed working; the deliverable here was to pin it into the canonical ADR + contract + spec + audit referencing `snapshot-cesium-3d-globe-working-2026-07-17`, so it is permanently discoverable and regression-testable. SS-MAP-CESIUM-3DTILES-GLOBE-PLACEMENT-BASELINE. |
| L-366 | founder 2026-07-17 (live Fly runs: resi building device-lost; office tower explicit-pin log) | **[P0 - RENDER] Auto-WebGL heavy-scene fallback did NOT fire for real buildings → WebGPU device loss.** Two gaps in the ADR-0267 mitigation, both from live runs: **(§Fix-1)** a ~1,300-element / 6-level / ~1,645-mesh residential building does NOT trip `isHeavyModel` (needs ≥ 4000 elems or ≥ 15 levels), so NO swap fired and it stayed WebGPU and crashed (`THREE.TSL: Invalid generated code, expected a "float"`). The swap reused the massing-LOD `isHeavyModel` gate, which is calibrated far too high for the swap decision. **(§Fix-2)** even on an explicit-WebGPU pin the `§L-361-WEBGPU-TRANSMISSION-GUARD` did not fire: its only caller was `setShadowPassDisabled('batch', true)`, which `BatchCoordinator` invokes ONLY during a batch — the resi/office generators add glass OUTSIDE batches, so the transmission node graph reached the first WebGPU render un-neutralized. **(§Fix-3)** a 29-level / 2,115-element / ~12,700-mesh office tower on an EXPLICIT WebGPU pin logged `…EXPLICITLY pinned to WebGPU — respecting the override, NOT switching` and device-lost; the founder expects heavy scenes to fall back regardless of the toggle. | Render / WebGPU device-loss (**C04** §1.4 backend selection; **ADR-0267** amended §Fix-1/§Fix-2/§Fix-3; ties **L-361**, **L-362**, **L-364** §L-361-WEBGPU-TRANSMISSION-GUARD, **L-139**). Files: `apps/editor/src/rendering/autoWebGLHeavyScene.ts` (dedicated `isSwapWorthyHeavyScene` threshold ≥ 400 elems OR ≥ 1000 meshes; explicit-pin heavy fallback; dropped `isHeavyModel` import), `apps/editor/src/engine/initScene.ts` `runTierPbrPass` (threads live `meshCount` into the swap + calls the new transmission neutralizer on the non-batched path), `packages/renderer-three/src/pipeline/RenderPipelineManager.ts` (public `neutralizeTransmissionForWebGPU()`), `apps/editor/src/engine/initBatchLifecycle.ts` (batch hook, element-arm), `apps/editor/src/types/globals.d.ts` (typed method). | **OPEN — IMPLEMENTED, awaiting live WebGPU confirmation (P0). NOT marked Fixed.** §Fix-1: swap fires when a scene crosses ≥ 400 top-level elements OR ≥ 1000 meshes — catches a ~1,300-elem / ~1,645-mesh building with margin, ignores a < 100-elem manual edit; `isHeavyModel` unchanged (massing LOD keeps its high gate). §Fix-2: public `neutralizeTransmissionForWebGPU()` called from the same non-batched tier-pass seam the Auto-swap uses, so glass is neutralized before the first post-generation WebGPU render; real-WebGPU-gated + idempotent; WebGL untouched. §Fix-3: heavy device-loss-risk scenes swap WebGPU→WebGL for BOTH `auto` AND explicit `webgpu` (one swap/session, distinct warning + "Re-pick WebGPU to override"); explicit-WebGPU override now honoured ONLY for light scenes; explicit `webgl` still a no-op. Root `tsc --skipLibCheck --noEmit` 0 errors; `@pryzm/renderer-three` typecheck 0. **Does NOT get marked Fixed without a live WebGPU-box run confirming the resi building + office tower swap to WebGL and render clean.** | route -> **render / renderer-three (WebGPU device-loss) queue.** Logs to watch: `§AUTO-WEBGL-HEAVY — scene is device-loss-risk (N elems / M meshes / L levels …) Auto mode switching …` and (explicit pin) `…switched WebGPU→WebGL for stability despite the explicit WebGPU pin. Re-pick WebGPU to override.` GUARD: heavy on Auto OR explicit-WebGPU → exactly one swap; explicit WebGPU + light → keeps WebGPU; explicit WebGL → no-op. SS-AUTO-WEBGL-FALLBACK-THRESHOLD-AND-EXPLICIT-PIN. |
| L-367 | founder 2026-07-17 (live Fly resi-building on WebGPU, post-L-366) | **[P1 - RENDER + LOADING UX] The Auto-WebGL swap now FIRES but MID-generation, and the loading overlay FLICKERS once per sub-batch.** Two residual problems after L-366 made the swap fire: **(FIX A)** the swap fires MID-generation — the L-366 gate (`isSwapWorthyHeavyScene` ≥ 400 elems / ≥ 1000 meshes) can only trip AFTER geometry exists, so the FIRST heavy WebGPU sub-batch renders first: a lingering `THREE.TSL: Invalid generated code, expected a "float"` flash + a ~1-min `[UnifiedFrameLoop] FIRST-RENDER-POST-SUPPRESS totalSuppressedMs=11481ms — WebGPU PSO compile LONGTASK` stall precede `[RenderPipelineManager] Phase: phase4 | WebGPU: false`. For a KNOWN heavy building command (residential/office/house) the result is heavy up front — swap PROACTIVELY at generation START, before the first heavy render, so the WHOLE generation runs on WebGL. **(FIX B)** the loading modal cycles Preparing→Done→Preparing→Done, once per sub-batch (main building, per-level ceilings, lighting, furnish, …): `initBatchLifecycle` opens a `LoadingOverlayController` session per sub-batch and ENDS it on each drain, so the ref-counted overlay hits zero and hides between sub-batches. Hold ONE continuous overlay across the whole generation. | Render / loading UX (**ADR-0267** amended [start-of-generation proactive swap]; **C04** §1.4 backend selection, **C10** heavy-scene budgets; **§FEAT-VIEW-ACTIVATION-LOADING-OVERLAY** L-270 overlay registry; ties **L-361/L-362/L-366**). Files: `apps/editor/src/rendering/autoWebGLHeavyScene.ts` (new `proactivelySwitchToWebGLForBuildingGeneration()` — Gate-3-less, shares the once-per-session guard + a refactored `fireSwapToWebGL` tail), NEW `apps/editor/src/ui/generation/buildingGenerationLifecycle.ts` (`beginBuildingGeneration`/`endBuildingGeneration` — proactive swap + ONE continuous overlay held on a batch-idle settle + hard cap), `ResidentialBuildingExecutor.ts` / `OfficeBuildingExecutor.ts` / `HouseLayoutExecutor.ts` (begin at the structural-batch boundary), `runHousePostGenChain.ts` (explicit release in `finally`). | **OPEN — IMPLEMENTED, awaiting live WebGPU confirmation (P1). NOT marked Fixed.** FIX A: `proactivelySwitchToWebGLForBuildingGeneration('resi-building'|'office-building'|'house')` fires at each generator's structural-batch boundary — skips L-366 Gate 3 (caller asserts heaviness) but keeps Gates 1/2/4, so a light/explicit-WebGL/non-WebGPU context is a no-op and the reactive threshold path (initBatchLifecycle gpu-compile-start + initScene tier pass) stays the fallback for scenes that go heavy without a known command; shared `_autoSwapDone` guard means the two never double-swap. FIX B: ONE outer `LoadingOverlayController` session held for the whole generation keeps the ref-count ≥ 1 so the per-sub-batch sessions can layer on top + drive live progress without ever hiding the overlay; released at the true end — batch-idle SETTLE (6 s quiet after `pryzm-batch-ended` drains to zero, mirrors SaveOrchestrator §AUTOSAVE-BATCH-SUPPRESS), an explicit `endBuildingGeneration()` in `runHousePostGenChain`'s `finally` (precise house terminus), and a hard 6-min cap so the overlay can NEVER get stuck. Root `tsc --skipLibCheck --noEmit` 0 errors. **Does NOT get marked Fixed without a live WebGPU-box run confirming (a) NO TSL flash / PSO stall — the generation runs on WebGL from the first frame, and (b) ONE continuous overlay, no per-sub-batch flicker.** | route -> **render / renderer-three (WebGPU device-loss) + loading-UX queue.** Logs to watch: `§AUTO-WEBGL-HEAVY-PROACTIVE — a heavy building generation is starting … switching WebGPU→WebGL up front` (should precede ANY resi/office/house geometry render) and NO `§AUTO-WEBGL-HEAVY` reactive line after it. GUARD: known building generation on Auto/explicit-WebGPU real-WebGPU → exactly one proactive swap before the first heavy render; the overlay stays up continuously start→settle (no Preparing→Done cycling); a single manual batch still shows its own overlay as today (no generation lease). RISK: if a sub-batch fails / a deferred pass never settles, the batch-idle settle + the 6-min hard cap still release the overlay (never a stuck overlay). SS-PROACTIVE-START-SWAP-AND-CONTINUOUS-GEN-OVERLAY. |
| L-368 | live Fly trace (3D-Site/Forma context load, 2026-07-17) | **[P2 - PERF / GIS] Forma context-buildings did THREE serial Overpass round-trips before neighbours drew.** Root-caused with exact seams: (1) `fetchContextBuildings` awaited a WIDE 0.008° tile FIRST and only on a transient **0** awaited the NARROW 0.005° fallback — the live trace showed the wide returned **0** (transient failure, NOT size: the 0.016° superset returned 11,868), the narrow returned 2,163; empty results are never cached (client persist guard + server `MISS-EMPTY`), so the wasted wide round-trip RECURRED every visit; (2) the FAR ring (0.016°) is a strict SUPERSET of the wide/near tile yet was fetched by a SEPARATE `fetchContextBuildingsFarRing`; (3) in `CesiumViewport.loadContextBuildings` the far ring was fired fire-and-forget ONLY after the near round-trip + render, so it was gated behind near instead of parallel. **FIX A (subsumes B + C): collapse the 3 queries into 1 — fetch ONCE at the far extent and split near/far CLIENT-SIDE.** `fetchContextBuildingsNearAndFar` does the single 0.016° `fetchForBbox`, then derives NEAR (`selectNearFootprints` — centroid inside 0.008° bbox → extruded+shadows) and FAR (`selectFarRingFootprints` — superset minus near disc, dedup, nearest-first, cap 900 → flat/shadowless); the two are the exact complement (no overlap/gap). CesiumViewport renders near the instant the single fetch resolves, then far from the SAME data (no second network hop). The old wide-first-0 serial call is gone (FIX C moot); the far ring no longer waits on a separate near round-trip (FIX B). Safety fallback preserved: a genuinely-0 far tile falls back ONCE to the narrow extent (never the old wide-first storm). | Perf / Geospatial context-fetch latency (**C12-GEOSPATIAL §context-fetch** [new note — the investigation flagged NO contract governed context-fetch latency, this closes that gap]; **ADR-0088** §OVERPASS-GENTLE-MIRRORS preserved; builds on **L-187** §FEAT-FORMA-CONTEXT-EXTENT-LOD far-ring, **L-273** persistent cache, **L-323** in-flight dedup). Files: `apps/editor/src/ui/geospatial/contextBuildings.ts` (new `fetchContextBuildingsNearAndFar` + `selectNearFootprints`; `fetchContextBuildings` delegates to `.near`; removed the now-redundant `fetchContextBuildingsFarRing`), `apps/editor/src/ui/geospatial/CesiumViewport.ts` (`loadContextBuildings` uses the split; `loadContextBuildingsFarRing`→render-only `renderContextBuildingsFarRing`; dropped `contextBuildingsFarAbort`), `apps/editor/__tests__/contextBuildingsLod.test.ts` (+`selectNearFootprints` complement/partition coverage). §PERF-CTX-SINGLE-FETCH. | **OPEN — IMPLEMENTED, awaiting live confirmation (P2). NOT marked Fixed.** Behavior change: the single far-extent query is non-empty → CACHEABLE, so a repeat visit hits the persistent localStorage cache and skips Overpass entirely; cache keys per site drop **3→1** (was wide 0.008° + narrow 0.005° fallback + far 0.016°; now the one 0.016° superset), so `LS_MAX_ENTRIES=6` now covers 6 sites (left as-is — per-site storage actually decreases, the 0.016° key already existed and was the largest). Preserved: `§SITE-METRIC-OVERPASS-PARALLEL` in-flight dedup + mirror race, `§OVERPASS-GENTLE-MIRRORS` gentle-mirror throttle, the same-origin proxy cache, the far-ring LOD cap/flat/shadows-off render, and abort-signal handling. Affected vitest (`contextBuildingsLod`, `overpassMirrorThrottle`, `contextOverpassInFlightDedup`, `contextBuildingsCacheQuota`) **19/19 green**; root `tsc --skipLibCheck --noEmit` **0 errors**. **Does NOT get marked Fixed without a live Fly run confirming the Forma context neighbours draw after ONE Overpass fetch (near+far) and a repeat visit hits the cache with zero Overpass calls.** | route -> **perf / geospatial (Forma context) queue.** Log to watch: `[gis] §PERF-CTX-SINGLE-FETCH near+far from ONE 0.016° fetch: N near … + M far …` (should be the ONLY Overpass building fetch for the site) and NO `wide bbox returned 0 … retrying the narrower fallback` line. GUARD: one far-extent fetch → near+far split client-side; genuinely-empty area falls back once to narrow (far empty); repeat visit = cache hit, no Overpass. SS-PERF-CTX-SINGLE-OVERPASS-FETCH. |
| L-369 | founder 2026-07-17 (live Fly resi-building gen, post-L-367 — WORKS but TOO SLOW, tens of seconds for ~6-storey / ~580 elems) | **[P1 - PERF] Building generation (resi/office/house) is correct on WebGL fallback but far too slow — profiled to three main sinks, top two fixed here.** (1) **Hot per-element `console.log` flood** — with DevTools open every `console.log` blocks the main thread. During one generation these fire once PER element / opening / finish: `[BimManager] Registered element …` (BimKernel.ts:265, ~580+/gen), `[WallOccupancyStore] canPlace OK …` (WallOccupancyStore.ts:301, one/opening), `[RoomFinishSyncService] Synced floor|ceiling …` + `Propagated room …` (RoomFinishSyncService.ts, ~2/finish + 1/room), and `[WallFragmentBuilder] RAF_DRAIN …` (once/drain frame). Thousands of blocking log lines = likely the single biggest drain. (2) **Wall-rebuild throughput** — `WallFragmentBuilder._drainBuildQueue` built ~16 walls/frame (floor 15, +1/frame ramp, cap 40), so 192 walls ≈ 12 frames just to drain walls; renders are suppressed during a batch drain so a frame is pure geometry and can safely go far higher. (3) **Redundant room redetects** — the `RoomTopologyObserver` armed a per-sub-batch auto-redetect BETWEEN sub-batches, all against graph-authoritative levels / unchanged geometry, tripping the §FIX-ROOMREDETECT-NOPROGRESS-GUARD circuit-breaker 40–56× (pure waste). | Perf / generation (**C10-PERFORMANCE-AND-OBSERVABILITY** §generation-perf [new note: log-gating + rebuild-budget + single-redetect]; reuses the `__pryzmProjectLoadActive` log-gate pattern (§LOAD-REDETECT-FREEZE, L-63) and the `buildingGenerationLifecycle` lease (**L-367**); ties **L-63** no-progress guard, **ADR-0069** graph-authoritative rooms). Files: `apps/editor/src/ui/generation/buildingGenerationLifecycle.ts` (publish/clear `globalThis.__pryzmBuildingGenActive` for the whole generation + ONE end-of-generation `scheduleRedetectAllLevels` sweep on release), `packages/core-app-model/src/BimKernel.ts` (register/unregister log gated on the new flag), `packages/geometry-wall/src/WallOccupancyStore.ts` (canPlace-OK log gated), `packages/core-app-model/src/RoomFinishSyncService.ts` (per-element sync + per-room propagate logs gated), `packages/geometry-wall/src/WallFragmentBuilder.ts` (batch drain floor 32 + ramp-step 8 + cap 40→64; RAF_DRAIN log gated), `packages/room-topology/src/RoomTopologyObserver.ts` (`_scheduleRedetect` + `_executeRedetect` suppress observer auto-redetect while `__pryzmBuildingGenActive`). §GEN-LOG-GATING / §PERF-WALL-DRAIN-BATCH-BUDGET / §GEN-SINGLE-REDETECT. | **OPEN — IMPLEMENTED, awaiting live confirmation (P1). NOT marked Fixed.** Safe/measurable wins only: the hot logs are SUPPRESSED (not removed) only while a load or generation is in flight — interactive edits still log; per-sub-batch summary lines (e.g. `[resi-building] … N punched`) are untouched. The wall-drain floor is raised only for batch drains (renders suppressed → pure geometry), with the frameMs>20 back-off + cap 64 bounding a single frame; interactive edits keep the 15-floor/±1 ramp. The redetect suppression is ADDITIVE and mirrors the existing `isBatching` guard: explicit `ReDetectRoomsCommand`s the executors run (house pre-naming) bypass the observer and are unaffected, graph rooms come from `BatchCreateRoomsCommand`, and `release()` fires one final sweep (a no-op on graph-authoritative levels, a safety net for any non-graph level). Root `tsc --skipLibCheck --noEmit` **0 errors**; `@pryzm/geometry-wall` 259, `@pryzm/core-app-model` 453, `@pryzm/room-topology` 59, editor redetect suite 14 — **all green**. NOT touched (reported as follow-ups): the §E1-CRDT-BLACKOUT ~8.6 s/sub-batch handling, the sub-batch chain structure, and the SlabFragmentBuilder / CurtainWallBuilder drain budgets (same pattern would help but out of this scope). **Does NOT get marked Fixed without a live run confirming the generation is materially faster + rooms/finishes still land correctly.** | route -> **perf / generation queue.** Logs to watch: the per-element `Registered element` / `canPlace OK` / `Synced …` / `RAF_DRAIN` flood should be ABSENT during a generation; wall drains should report a much higher `nextBudget` (post-gen only, since the log is gated) and fewer frames; NO `redetect circuit-breaker TRIPPED` during a generation; ONE redetect sweep at the end. GUARD: logs suppressed only under `__pryzmProjectLoadActive`/`__pryzmBuildingGenActive`; batch drain floor 32/cap 64 with frameMs back-off; observer auto-redetect suppressed during generation, single sweep on release. SS-GEN-PERF-LOG-GATE-DRAIN-BUDGET-SINGLE-REDETECT. |
| L-370 | founder (live Fly log, 2026-07-17 — 3D globe: building placed correctly but off-screen, "I have to zoom in to find it") | **[P2 - GIS] Camera-framing timing bug on the photoreal "3D globe": the building is framed EARLY at base 0, the Google-tile ground datum resolves LATE and the building JUMPS up ~706.9 m to sit on the tiles, and the corrective re-frame that should follow it up is SUPPRESSED — so the camera stays pointed where the building WAS and the user must manually zoom to find it.** Placement is CORRECT (seat-and-reveal at the resolved base works); this is purely a framing-timing defect. Confirmed from the live log: `§FIX-CESIUM-GLOBE-ELEVATION-AND-GEOREF … base height used 706.90 m … re-placing` + `§A.21.D49 … HELD HIDDEN until ground datum resolves`, then `§GLOBE-FRAME-NO-JUMP — base settled but the user already moved the camera; suppressing the corrective re-frame.` The early frame is flown by GISAreaLayout's `restorePhotorealGlobeContent → reframeSiteIn3D() → flyToFormaSite()` at the unresolved base 0; by the time the datum settles and `performInitialReframe` runs, the user has nudged the empty base-0 view, so the unconditional "user moved" suppression stranded the camera. | GIS / globe framing (**ADR-0268** D6 §GLOBE-STALE-FRAME-REFRAME note; **C12-GEOSPATIAL §7** "frame once, no jump" MUST refined). **Chose Option B** (single-seam re-frame gate) over A (defer the first frame) because the early frame is orchestrated EXTERNALLY in GISAreaLayout (shared with Zoom-to-Site + the no-massing fallback) — B is contained + keyed to the confirmed jump. Files: `apps/editor/src/ui/geospatial/CesiumViewport.ts` (new `GLOBE_STALE_FRAME_BASE_JUMP_M = 20` const; new `formaFramedAtBaseHeight` field recorded in `flyToFormaSite`; `performInitialReframe` re-frames ONCE despite user movement when the base jumped > 20 m, else preserves the small-settle suppression; field reset in the 3 state-clear paths + `armGlobeReframeOnBaseSettle`), `apps/editor/__tests__/CesiumViewportFrameNoJump.test.ts` (+2 tests: large-jump override fires once; small settle still suppressed). §GLOBE-STALE-FRAME-REFRAME. | **OPEN — IMPLEMENTED, awaiting live confirmation (P2). NOT marked Fixed.** Preserved: the fire-at-most-once latch, the small-settle §GLOBE-FRAME-NO-JUMP user-control protection (jumps ≤ 20 m still honour the user), the §GLOBE-FIT-BUILDING heading/pitch, the seat-and-reveal ordering, and re-entry framing (an already-seated model has the datum resolved → ~0 m delta → frames without yanking). Root `tsc --skipLibCheck --noEmit` **0 errors**; `@pryzm/editor` CesiumViewportFrameNoJump suite **8/8 green** (6 prior + 2 new). **Does NOT get marked Fixed without a live Fly run confirming the globe opens framed on the building with NO manual zoom.** | route -> **gis / globe-framing queue.** Log to watch: on a first globe entry, `§GLOBE-STALE-FRAME-REFRAME (L-370) — the ground datum settled 706.9 m from the framed base (> 20 m) … re-framing ONCE despite the user's camera move` REPLACES the old `suppressing the corrective re-frame` line, and the camera lands on the building. GUARD: re-frame despite user move ONLY when base jumps > 20 m; small settles still suppressed; fires at most once; re-entry unaffected. SS-GLOBE-STALE-FRAME-REFRAME. |
| L-371 | founder 2026-07-17 (live Fly: Lisbon 3D-Site stalled at "tiles"; Rome 3D-Site skipped correctly) | **[P2 - GIS / LOADING UX] Intermittent false "map tiles have stopped streaming" 25 s stall on the Forma "3D Site" even though massing + context fully render.** The `§SS-FIX-FORMA-TILES-READINESS-KEYLESS-GATE` (L-327) skip only fires when `hasRealTileProvider()` returns `false`, but that method checked `photorealTilesActive` **before** the view mode: a Forma study is almost always entered AFTER a globe view, which leaves `photorealTilesActive === true` (never reset on Forma re-entry), so the stale `true` was returned before the keyless-flat-ground fact was considered → the readiness tiles-gate stayed armed and the stall watchdog tripped at 25 s with a bogus "tiles stopped streaming" card on a view that is already ready. Intermittent because it depends on whether the stale flag happened to be cleared at probe time (Rome run skipped correctly; Lisbon run stalled). | GIS / Cesium readiness (**C12-GEOSPATIAL §7**; hardens **§SS-FIX-FORMA-TILES-READINESS-KEYLESS-GATE** L-327; ties **L-270** view-activation overlay). Files: `apps/editor/src/ui/geospatial/CesiumViewport.ts` (`hasRealTileProvider()` now short-circuits to `false` FIRST when `this.formaMode` — Forma massing mode always renders its own flat keyless ground with imagery hidden and no streaming tileset, so tiles NEVER reach "loaded"; `photorealTilesActive`/`globe.show` checks kept for the non-Forma globe path). | **OPEN — IMPLEMENTED, awaiting live confirmation (P2). NOT marked Fixed.** `formaMode` is the authoritative "there will never be streaming tiles" signal and is set before the readiness probe on the normal ordering (confirmed by the Rome run's `§SS-FIX-FORMA-TILES-READINESS-KEYLESS-GATE … skipping the tiles gate` line). Downstream keyless-skip behavior is already covered by `viewActivationLoadingOverlay.test.ts` (13/13 green); root `tsc --skipLibCheck --noEmit` **0 errors**. **Does NOT get marked Fixed without a live Fly run confirming the 3D-Site never shows the false "tiles stopped streaming" card.** | route -> **gis / Cesium readiness queue.** Log to watch: `§SS-FIX-FORMA-TILES-READINESS-KEYLESS-GATE … skipping the tiles gate` should fire on EVERY Forma-site entry (not just some), and NO `readiness NEVER ARRIVED at stage "tiles"` for target `site`. GUARD: `formaMode` → no real tile provider (skip tiles gate) regardless of stale `photorealTilesActive`; non-Forma globe path unchanged. SS-FIX-FORMA-TILES-READINESS-FORMAMODE-FIRST. |
| L-372 | founder 2026-07-17 (live tower: "process is slow — WebGPU still involved for shades"; investigation CONFIRMED) | **[P1 - PERF + RENDER] The Auto-WebGL heavy-scene "fallback" still runs the full TSL node pipeline (per-material node compile + shadow-node graph + transmission node), so generation stays slow and the `expected a "float"` flash survives the swap.** Investigation (2×) confirmed the founder's hypothesis: the swap builds a `WebGPURenderer({ forceWebGL2:true })` (backend `webgl-fallback`), NOT a classic `THREE.WebGLRenderer` — it disables only the app-level post-FX (SSGI/TRAA/outlines) via `_webGpuActive=false`, while the renderer itself STILL node-compiles every material and drives shadows through its TSL shadow-node graph on the WebGL2 backend. Two low-risk fixes here (Batch 1): **(A)** the `§L-361-WEBGPU-TRANSMISSION-GUARD` neutralizer was gated on `_webGpuActive`, so it SKIPPED the `webgl-fallback` backend even though that backend still emits the transmission TSL node (the `expected a "float"` seed) → the flash survived the swap; re-gated on `isWebGPURenderer` so it fires on native WebGPU AND the WebGL2-backed fallback (both node-compile) and correctly SKIPS a classic `webgl-only` renderer (no node graph, keeps refractive glass). **(B)** shadows are now suppressed across the WHOLE generation via the single-owner ref-counted latch (`pushShadowPassDisabled('building-generation')` in `buildingGenerationLifecycle`), removing the per-frame shadow-MAP render from the generation hot loop and deferring the one shadow (re)compile until after the scene settles — the founder's "WebGPU still doing shades = slow" cost, backend-agnostic (works on WebGPU, `webgl-fallback`, and `webgl-only`). | Render / perf (**C04** §1.4; corrects the FALSE premise in **ADR-0267** §Fix-2 + **ADR-0077** that "`webgl-fallback` = no TSL" — the fallback IS a node/TSL renderer; **C10** heavy-scene budgets; builds on the **L-205** §FIX-SHADOW-ENABLE-LATCH single-owner shadow latch, ties **L-361/L-364/L-367**). Files: `packages/renderer-three/src/pipeline/RenderPipelineManager.ts` (`_neutralizeTransmissionForWebGPU()` gate `!this._webGpuActive` → `!isWebGPURenderer`, §L-361-FALLBACK-STILL-TSL), `apps/editor/src/ui/generation/buildingGenerationLifecycle.ts` (whole-generation `pushShadowPassDisabled('building-generation')` push in ctor + idempotent release in `release()`, §GEN-SHADOW-SUPPRESS). | **OPEN — IMPLEMENTED, awaiting live confirmation (P1). NOT marked Fixed.** Batch 1 stays entirely within proven seams (no init control-flow surgery). Root `tsc --skipLibCheck --noEmit` **0 errors**; `RenderPipelineManager.shadowEnableLatch` **11/11** + `viewActivationLoadingOverlay` **13/13** green. The COMPLETE fix (route heavy-gen to a classic `webgl-only` renderer = zero node compile) is tracked separately as **Batch 2** (needs the initScene Phase-5 guard lift + lightweight-render extend + guarded fallback; the material-safety audit confirmed the live scene is 100% classic materials, LOW-MEDIUM risk). **Does NOT get marked Fixed without a live WebGPU-box run confirming (a) no `expected a "float"` flash after the swap and (b) materially faster generation with shadows re-appearing correctly at the end.** | route -> **render / renderer-three (WebGPU) + perf / generation queue.** Logs to watch: `§L-361-WEBGPU-TRANSMISSION-GUARD neutralized … opacity glass on the node-compiling renderer` should now fire on the `webgl-fallback` backend too; shadows should not re-render per-frame during generation. GUARD: neutralizer fires on any `isWebGPURenderer` (native or WebGL2-fallback), skips classic `webgl-only`; shadow suppression spans the whole lifecycle, released once at the end (latch re-enables only if no other reason holds + user pref allows). SS-FALLBACK-STILL-TSL-TRANSMISSION-AND-GEN-SHADOW-SUPPRESS. |
| L-373 | audit 2026-07-17 (Principal GIS/Env-analysis review of the Forma 3D Site Analysis heatmaps) | **[P2 - GIS / DATA CREDIBILITY] The four 3D Site Analysis heatmaps (Sun Hours, Wind Comfort, Temperature, Population Density) present partly-heuristic spatial fields whose data provenance/credibility is undocumented.** Grounded in code: Sun Hours = real NOAA-replica sun geometry (±0.5°, `solarPosition.ts:12`) but single-day + FLAT-ground occlusion (no terrain, `solarCodec.ts:197-227`), not annual/kWh. Temperature/Wind = a single MEASURED scalar base from NASA POWER (~50 km reanalysis, `siteRealData.ts:120-199`) with ALL spatial variation from closed-form heuristics — UHI `ΔT=6°C×density×(1−vent)` (`HeatIslandGrid.ts:28,55`) and a Lawson centroid-shelter proxy that structurally cannot show wind acceleration (`WindComfortGrid.ts:75-87`); when the base fails, wind injects a synthetic 4.2 m/s floor (`siteMetricGrids.ts:840`). Colours are disc-normalised to each field's own min→max (`parityFieldMapper`) yet legends show absolute-looking units. Population = real WorldPop 2020 100 m (`siteRealData.ts:305-329`) when reachable, else an OSM footprint×floors proxy. Panel captions DO disclose "estimate" (`FormaSiteAnalysisControls.ts:1103-1145`), so this is trust-hardening, not fabrication. See L-373a for the P1 sub-item. | GIS / climate analytics data-credibility. **Coverage gap: no contract governs the shipped heatmaps' provenance** — C55-GEODATA (DRAFT) governs a different unbuilt draped-provider subsystem; C21-CLIMATE-INGESTION (CANONICAL) governs the temp/wind base only; ADR-0095 (ACCEPTED/IMPLEMENTED) is the real-dataset wiring; C54-IN-BROWSER-WIND-CFD (DRAFT, ADR-0064) is the intended wind replacement; ADR-0074 (Sun), ADR-0080 (VSC "relative field, not a code-compliant certificate"). Full audit: `docs/04-reference/3D-SITE-ANALYSIS-AUDIT.md`. | **OPEN — AUDIT ONLY, no code changed. NOT a defect fix.** Recommends: Phase 0 fidelity/provenance labels + absolute-vs-relative legend disclosure + a CI gate mirroring C54's `check-windcfd-beta-label.ts`; Phase 1 terrain-aware sun + ERA5-Land base via a C21 provider tier + C22 PII tag on Population; Phase 2 C54 WebGPU-LBM wind CFD + annual kWh/m² (PVGIS/NSRDB) + satellite-LST UHI; Phase 3 C55 national analytical-layers platform. | route -> **geospatial / climate queue.** Owner UNASSIGNED, target TBD. Dependencies: C54 (wind CFD), C55 (provider subsystem), C21 (climate tiers), C22/C23 (PII/provenance). |
| L-373a | audit 2026-07-17 (sub-item of L-373) | **[P1 - GIS / TRUST RISK] Wind Comfort (and to a lesser degree Temperature) render a heuristic spatial field in an authoritative, standards-named colour scale.** Wind paints a centroid-distance shelter heuristic (`WindComfortGrid.ts:75-87`) in industry-standard Lawson LDC pedestrian-comfort colours (`WindComfortGrid.ts:32-46`) — implying a Lawson wind-microclimate assessment was performed when none was (no wake, corner/Venturi acceleration, downdraught, or channelling). Temperature applies a fixed 6°C-max UHI formula shown on an absolute °C axis. Because the colour is disc-normalised, "red" means "windiest/hottest in-view", not the absolute value the legend implies. | GIS / analytics trust. Conflicts with C54 §1.1 (which reserves pedestrian-comfort assessment for the CFD engine and CI-enforces a BETA + calibration label the shipped Lawson layer lacks) and SPEC-FORMA §6:156 ("deliberately COARSE comfort cue, NOT a microclimate simulation"). | **OPEN — fast fix available (days): rename bare "Lawson" to "shelter estimate", add a "proxy — not CFD; cannot show wind acceleration" badge, print real numeric min/max on the legend, add the CI fidelity-label gate. Correct fix = C54 WebGPU-LBM CFD.** | route -> **geospatial / climate queue (fast-track the labelling).** Owner UNASSIGNED, target TBD. Blocks: nothing; unblocks credible demo of Wind/Temp. |
| L-374 | founder 2026-07-17 (Forma 3D-Site production-readiness mandate) | **[P1 - STRATEGY / GIS] Make the "3D Site" (Forma) view production-ready with a provider-agnostic Context Engine.** Today's contextual environment is OSM-LOD1 extruded footprints on keyless-ellipsoid flat ground with Esri-raster-only imagery, no real terrain (DEM/DTM/DSM), no orthophoto draping, and no 3D trees; the georeferencing substrate (proj4 LTP-ENU, C12 §1) is strong but the context data is ≈1.6/5. Founder wants engineering-grade context (LOD2+ buildings, real terrain, orthophotos, trees, water, roads) inside the Three.js/Cesium canvas, globally + performantly, EVOLVING the current impl toward provider-agnostic Terrain/Imagery/Building/Vegetation/Road/Water providers + adapters (Cityweft, Google 3D Tiles, Cesium ion, Esri, Overture/OSM, government LiDAR). Full audit + roadmap: docs/04-reference/FORMA-CONTEXT-ENGINE-AUDIT.md. | Geospatial / Forma context (C12-GEOSPATIAL §1/§1.4/§7/§8; SPEC-FORMA-SITE-VIEW §2/§8/§11; ADR-0268; ADR-0088; ADR-0065 [pattern to mirror]; COVERAGE GAPS: no contract governs terrain/DEM ingestion, LOD2/LOD3 building ingestion, vegetation, orthophoto draping, or the Context Engine abstraction — recommend new C12-CONTEXT-ENGINE). CONFLICT (human decision): "engineering-grade context" opposes the Forma abstract-flat-massing NON-GOAL (SPEC-FORMA §2/§8); Google 3D Tiles ToS restricts derivative/offline use. | **OPEN — audit delivered, not started.** owner UNASSIGNED, target TBD. Sub-items L-374a..f below. | route -> geospatial (Context Engine) queue. |
| L-374a | L-374 audit (Phase 0 loading-perf) | **[P1 - PERF] Forma/globe activation loads too slowly — 71 MB full-scene BIM→GLB export on the critical path.** Umbrella for the loading-perf quick wins (SUBSUMES the concrete rows L-355 [P1], L-356 [P2], L-358 [P2]): move exportFragmentsToGLB off the critical path (worker/idle) + Draco compress; key the §CESIUM-PERF-GLOBE-GLB-CACHE on true geometry signature not view-mode (fixes cross-view re-export); seat-once (no place-at-0-then-re-place); defer non-critical context to post-first-frame. Evidence: 71,177,808 B GLB (GISAreaLayout.ts:1958/2117, GLBExporter.ts:408), massing-then-real double render (GISAreaLayout.ts:1786-1873). | Perf (C10 — GAP: no view-activation budget; add one, target first-interactive <~2s); C12 §8 (preserve L-368 single-fetch). Ties L-355/356/358. | **OPEN — not started.** owner UNASSIGNED, target TBD. | route -> perf/geospatial queue. |
| L-374b | L-374 audit (Phase 1 terrain) | **[P2 - GIS] No real terrain provider — attach DEM/DTM/DSM (Cesium ion / Copernicus keyless).** Build never attaches a terrain provider (CesiumViewport.ts:4589-4594); ground is flat ellipsoid or tile-mesh-implicit. Wire the already-coded sampleTerrainMostDetailed to clamp model + context to true ground. | Geospatial (C12 §1.4 extend; new terrain-ingestion contract — GAP). | **OPEN.** owner UNASSIGNED, target TBD. dep: none. | route -> geospatial queue. |
| L-374c | L-374 audit (Phase 4 LOD2) | **[P2 - GIS] LOD2/LOD3 building ingestion — Overture (open) + Cityweft/Google-3D-Tiles (premium) BuildingProvider adapters; keep OSM LOD1 fallback.** Today outer-ring-only LOD1 extrusions (contextBuildings.ts:456). | Geospatial (new LOD2-ingestion contract — GAP; C12 §8). dep: L-374f. | **OPEN.** owner UNASSIGNED, target TBD. | route -> geospatial queue. |
| L-374d | L-374 audit (Phase 5 vegetation) | **[P3 - GIS] Trees/vegetation — VegetationProvider (Overture/OSM tree points → GPU-instanced impostors; premium LiDAR canopy).** Today only flat park polygons (contextParks.ts), no 3D trees. | Geospatial (new vegetation contract — GAP; relates SPEC-RENDER-TIERS entourage A.24.4). dep: L-374f. | **OPEN.** owner UNASSIGNED, target TBD. | route -> geospatial queue. |
| L-374e | L-374 audit (Phase 2 ortho) | **[P2 - GIS] Orthophoto imagery draping over terrain (Esri open → premium).** Today Esri raster basemap only, no draping, hidden in Forma flat mode (CesiumViewport.ts:1277-1317). | Geospatial (new imagery/draping contract — GAP). dep: L-374b (terrain first). | **OPEN.** owner UNASSIGNED, target TBD. | route -> geospatial queue. |
| L-374f | L-374 audit (Phase 3 abstraction) | **[P2 - ARCH] Provider-agnostic Context Engine — Terrain/Imagery/Building/Vegetation/Road/Water provider interfaces + adapters.** Refactor contextBuildings/roads/water/parks behind adapters; ContextEngine.streamContext (tile-index + EPSG transform via LTPENURebase + streaming). Cesium stays nav/transform engine. | Arch/Geospatial (new C12-CONTEXT-ENGINE contract — GAP; mirror ADR-0065 pluggable-provider pattern; P8 spans on adapters). dep: enables L-374c/d config-swap. | **OPEN.** owner UNASSIGNED, target TBD. | route -> geospatial/arch queue. |
| L-375 | founder 2026-07-17 (live Fly resi-building gen, 6 floors / 20 apts, WebGL fallback — "successful but a lot to improve") | **[UMBRELLA — resi-building generation errors + perf + architecture review]** Careful audit of the residential-building batch-AI generation console log + pipeline. Sub-items L-375a–e. Verified BENIGN (not defects): the `§SS-FIX-TSL-NOT-LOADED-BEFORE-SCENEPASS _fullRebuild deferred` (RenderPipelineManager.ts:3009 — intended L-319 guard, always fires on webgl-fallback where `_tslLoaded` is false) and `[SceneQualityTier] 71 meshes → tier=cinematic` (SceneQualityTierManager — sane; backend gate strips SSGI/probes + caps shadow=standard, negligible). Full report: `docs/04-reference/RESI-BUILDING-GENERATION-AUDIT.md`. | Editor / renderer / command (C11 element-creation, C16 command-authoring, C03 commands/state, C08 collab, ADR-0069 graph rooms). | **OPEN — audit delivered, sub-items below. UNASSIGNED / TBD.** | route -> editor / renderer / command queues (per sub-item). |
| L-375a | L-375 audit (root-caused) | **[P2 - COLLAB, silent degrade + P1/P4 regression] The CRDT applier is never wired — per-command real-time replication (G3-T2) silently does not run.** `engineLauncher.ts:825` reads `(runtime as any)?.inner?.bus`, but the composed runtime handle (`composeRuntime.ts:1463-1548`) has NO `inner` property (`inner` is a local const, never placed on the handle) → `_innerBus` is always `undefined`, the warn at `:833` (`runtime.inner.bus not accessible — CRDT applier not wired`) fires every boot, `setCrdtApplier` is never called, and `CommandBus._crdtApplier` (`CommandBus.ts:73,408`) stays `null`. Solo generation is unaffected (null-safe); MULTI-USER real-time replication is silently degraded. Also a P1/P4 regression — an `any` reach-through into runtime internals that broke silently. | Collab / runtime (**C08 §3.1** G3-T2; **P1** single composition root, **P4** no `(window as any)`/`as any` reach-through; the E2E marker `tests/e2e/crdt-batch-conflict.spec.ts:15` "✅ engineLauncher.ts L388" is STALE — real site is `:825`). Fix locus: wire `setCrdtApplier` INSIDE `composeRuntime()` (owns `inner.bus`) OR add a typed `bus.setCrdtApplier` slot — NOT another app-layer `any` reach-through. | **OPEN — NOT started.** owner UNASSIGNED (runtime-composer), target TBD. | route -> **runtime-composer / command-bus queue.** |
| L-375b | L-375 audit (quick win) | **[P2 - PERF, log flood — L-369 miss] `CreateStairCommand.canExecute` dumps all 37 store names + the full levels array PER STAIR.** `CreateStairCommand.ts:121` + `:123` unconditionally `console.log` the 37-entry `ctx.stores` list + the levels array on every stair (5–7/generation), plus create/opening logs at `:385/:410/:421/:436/:473/:483` — none gated by `__pryzmBuildingGenActive` (the L-369 pass missed this file). DevTools-open blocking `console.log`. | Perf / logging (extends **L-369** §GEN-LOG-GATING; reuse the `__pryzmBuildingGenActive` helper, BimKernel.ts:28-37). | **OPEN — quick win, NOT started.** owner UNASSIGNED, target TBD. | route -> **command-registry (stair) queue.** |
| L-375c | L-375 audit (quick win) | **[P2 - PERF, log flood — L-369 miss] `CommandManager` EXECUTE/snapshot logs fire per individual command during generation.** `CommandManagerImpl.ts:108` + `:126` log every command; only `PROJECT_LOAD` is fast-pathed (`:105`), NOT building-generation → floods per slab/stair/lift/curtain/roof. Extend the log fast-path to also skip the EXECUTE/snapshot log lines when `__pryzmBuildingGenActive` (keep the snapshot itself). | Perf / logging (extends **L-369**; **C01 §2.2**). | **OPEN — quick win, NOT started.** owner UNASSIGNED, target TBD. | route -> **command-registry queue.** |
| L-375d | L-375 audit | **[P2 - PERF / ARCH gap] Non-wall elements (slab/stair/lift/curtain-wall/roof) are dispatched as individual `cm.execute` inside the batch — each pays a `createSnapshot` (`structuredClone` over `affectedStores`).** Walls are coalesced to one `wall.batch.create`/level (L-131), but the other element types in `ResidentialBuildingExecutor.ts` (:650/:2465/:2575/:622/:658) are per-element; the stair scope `["stair","opening","slab"]` snapshots the GROWING slab store per stair → O(N·M) tail. A `stair.batch.create` handler already exists (`plugins/stair/.../CreateStairBatch.ts`) but appears UNUSED. Extend L-131 coalescing. | Perf / arch (**C16** command-authoring, **L-131** batch coalescing — ADR-worthy; **Known Violation**: batch coalescing is walls-only, other element types remain per-`cm.execute`). | **OPEN — NOT started.** owner UNASSIGNED, target TBD. **Human-decision: confirm `stair.batch.create` is registered at bootstrap** — `registerStairHandlers` found in `plugins/stair` but no call in `engineLauncher.ts` (UNVERIFIED — likely unwired). | route -> **command-registry + editor executor + plugins/stair queue.** |
| L-375e | L-375 audit | **[P3 - DIAGNOSTIC] `FIRST-RENDER-POST-SUPPRESS … WebGPU PSO compile LONGTASK` is hardcoded even on the WebGL fallback.** `UnifiedFrameLoop.ts:506-511` prints "WebGPU PSO compile" regardless of backend; the ~493 ms tail is real (the L-372/Batch-2 residual TSL compile — cross-ref, NOT re-logged) but the label misleads on a `webgl-fallback` run. Branch the label on `isRealWebGPUBackend()`. | Rendering / diagnostics (cross-ref **L-372**). | **OPEN — cosmetic, NOT started.** owner UNASSIGNED, target TBD. | route -> **core-app-model / rendering queue.** |
| L-376 | 2026-07-17 | **[P1 - OFFICE-TOWER] Commercial office-tower generation audit — 40× amplification of resi per-element dispatch debts + persistence/render noise** | Office typology / C16 §8.6, C10 §7, C03, C04, C05, C08 · OfficeBuildingExecutor.ts · CommandManagerImpl.ts. Verified OK: Auto-WebGL swap fires for office (OfficeBuildingExecutor.ts:375-380, ADR-0267/L-367); L-372 shadow suppression applies; 880 glazing windows are one batched CreateWallOpeningsBatchCommand. Full report: docs/04-reference/COMMERCIAL-TOWER-AUDIT.md | OPEN — UNASSIGNED (base row; sub-items L-376a–g) | route -> Perf/Commands |
| L-376a | 2026-07-17 | **[P2 - PERSISTENCE] ~30 local-only projects with unsaved local versions pile up in IndexedDB, never flushed to server, no eviction cap** | ProjectHub sync / C05, C08 (coverage gap — no eviction policy) · ProjectHub.ts:218-234 | OPEN — UNASSIGNED/TBD (storage-growth, not data loss; partly free-plan by-design). **Human decision: free-plan local-only retention vs a hard IDB cap — product policy.** | route -> Persistence/Sync |
| L-376b | 2026-07-17 | **[P3 - PLATFORM] `Cannot delete the default Ground level` warn+stack fires on the happy ClearProjectCommand path (L0 not excluded from level loop)** | ClearProject / C20, C03 · BimKernel.ts:381-385 · ClearProjectCommand.ts:179-181 | OPEN — UNASSIGNED (benign guard; false-alarm noise every project switch) | route -> Platform/Commands |
| L-376c | 2026-07-17 | **[P3 - RENDER] THREE deprecations — PCFSoftShadowMap silently downgraded to PCFShadowMap (shadow quality ≠ intended) + THREE.Clock deprecated** | Rendering / C04, ties L-372 · PhotorealisticRenderer.ts:101 · ShadowQualityUpgrader.ts:53/61/69 (Clock source UNVERIFIED — likely THREE-internal/Cesium; both files use the P2-legal @pryzm/renderer-three/three facade — no P2 violation) | OPEN — UNASSIGNED (tech-debt; batch with L-372) | route -> Rendering |
| L-376d | 2026-07-17 | **[P1 - PERF] Office core emits ~78 stairs + ~40-80 lifts per-element OUTSIDE runBatch (+40 slabs/~38 floors), each an O(N·M) structuredClone snapshot — 40× amplification of L-375d** | Element creation / C16 §8.6/B-6, C10 §7, C03 · OfficeBuildingExecutor.ts:992-1027 · CommandManagerImpl.ts:120-127 | OPEN — UNASSIGNED (ties L-375d). **CONFLICT/human decision: correct fix = new `*.batch.create` bus handlers (pipeline-wide, fixes resi+house+office) vs a fast office-local wrap (forks the pipeline — forbidden by the reuse mandate). Recommend pipeline-wide.** | route -> Perf/Commands |
| L-376e | 2026-07-17 | **[P2 - PERF] Generation log-flood: CommandManager EXECUTE/snapshot logs + CreateStairCommand.canExecute 2-line-per-call logs are ungated on __pryzmBuildingGenActive (C10 §7.1)** | Observability / C10 §7.1, ties L-375b/c · CommandManagerImpl.ts:108,126 · CreateStairCommand.ts:121,123,385 | OPEN — UNASSIGNED (thousands of lines/build; ties L-375b/c) | route -> Perf/Observability |
| L-376f | 2026-07-17 | **[P2 - COMMANDS] Office "generate" is not one undo unit — hundreds of per-element Create*Command each push undo history (C16 §8.6); executor header "one undo unit" claim is false** | Command authoring / C16 §8.6 · CommandManagerImpl.ts:150-152 · OfficeBuildingExecutor.ts:6,19-21 | OPEN — UNASSIGNED (folds into L-376d fix; interim undo-policy call) | route -> Commands/UX |
| L-376g | 2026-07-17 | **[P3 - COLLAB] EngineBootstrap O.8 CRDT wiring deferred to idle — office path affected if applier never wired (tie only, see L-375a)** | Collaboration / C08, ties L-375a | OPEN — UNASSIGNED (note only; track under L-375a) | route -> Collaboration |
| L-377 | founder 2026-07-17 (live Fly resi-building, post-L-369/372 — "much better execution, BUT building appears in 2-3s then takes way longer to FINISH") | **[P1 - PERF] The post-geometry "finishing up" phase is far slower than geometry creation — three sinks after the building is already on screen.** (1) **Per-wall REDETECT_ROOMS storm:** every `§P2.1: wall mirrored to legacy store wall_…` (the `wall.created`→legacy-store bridge, initTools §P2.1) fires a `CommandManager EXECUTE: REDETECT_ROOMS`, each re-running the FULL `RoomDetectionEngine` on the level (log shows rooms recomputed 1→2→…→25 PER wall, with §DIAG-PARTITION-REACH each pass). L-369 suppressed the RoomTopologyObserver's auto-redetect via `__pryzmBuildingGenActive`, but NOT this bridge-triggered per-wall redetect path → O(walls × rooms) room detection during the post-batch legacy-mirror replay. Likely the dominant "finishing up" cost. (2) **~14 s FIRST-RENDER PSO/shader-compile LONGTASK** on 6161 scene meshes (`FIRST-RENDER-POST-SUPPRESS totalSuppressedMs=14236ms frameCount=5107 — WebGPU PSO compile LONGTASK`; the founder's "Compiling GPU shaders… 96%") — the L-372/Batch-2 residual TSL node compile on the webgl-fallback, now quantified. (3) **~86 s DependencyResolver CASCADE** (`§G1-SUPPRESS-LIFTED post-CASCADE T=86265ms` — semantic-graph cascade rebuild settling long after geometry). | Perf / generation (**C10** §7 generation-perf; ties **L-369** [observer suppression didn't cover the §P2.1 bridge], **L-372/Batch-2** [shader compile], **ADR-0069** graph-authoritative rooms, **§FIX-SKIP-REDETECT-ROOMS** [BatchCoordinator already skips the FINAL sweep — but not the per-wall bridge]). Suspected sites: initTools §P2.1 `wall.created`→legacy-store bridge (engineLauncher/initTools), the redetect trigger on legacy wall mirror, RoomDetectionEngine, DependencyResolver CASCADE (semantic graph). **UNVERIFIED — subagent to root-cause the exact §P2.1→REDETECT trigger.** | **OPEN — investigation dispatched, NOT started. UNASSIGNED / TBD.** | route -> **perf / generation + room-topology + renderer queues.** Log signature: hundreds of `EXECUTE: REDETECT_ROOMS` + `Detected N room(s)` interleaved with `§P2.1: wall mirrored to legacy store` AFTER the geometry batch; 14s FIRST-RENDER LONGTASK; 86s CASCADE. |
| L-378 | founder 2026-07-17 (live Fly — "after Forma 3D-Site → PRYZM 3D+Plan I ALWAYS have to click zoom-in for the 3D scene to focus on the building") | **[P2 - VIEW/CAMERA] Returning from the Forma 3D-Site to the BIM 3D view restores a STALE Cesium/globe-scale camera slot instead of framing the building — the user must manually zoom every time.** BUG (existing behavior wrong). Confirmed from the live log: `_activate3DView — MultiViewCameraManager.restoreSlot("perspective")` → `Restored slot "perspective" — pos(-1675359.34, 660322.37, -12479386.90) target(17.53, 9.75, 9.83)` — the restored perspective slot holds an ECEF/globe-scale position (~12.5M units out, saved while the Cesium/Forma view was active via `ViewCameraStateStore.save("3D")`), aimed at the building at ~(17,9,9), so the building is a speck. The existing auto-frame hooks (`§3D-FRAME-ON-VIEW-SWITCH`, `§FIX-BATCH-GEN-AUTOFRAME-3D`, registered in initTools) do NOT win over the stale-slot restore on the GIS→3D return path. Severity P2 (UX friction on EVERY Forma→3D return; demo-visible but not data-loss). | View-switching / camera (contracts: view-state / camera-state — **UNVERIFIED which C0N governs MultiViewCameraManager slot restore; likely a coverage gap**; ties **L-370** [globe framing — DISTINCT: that was Cesium globe, this is BIM 3D]). Seam: `ViewController._activate3DView` → `MultiViewCameraManager.restoreSlot("perspective")` (engineLauncher/ViewController ~VST), `ViewCameraStateStore.save("3D")` (saves the slot WHILE Cesium camera is globe-scale), the `§3D-FRAME-ON-VIEW-SWITCH`/`§FIX-BATCH-GEN-AUTOFRAME-3D` auto-frame hooks (initTools). | **OPEN — NOT started (queued behind L-377). UNASSIGNED / TBD.** Correct fix (architecturally sound): on the GIS/Forma→3D transition, either (a) INVALIDATE the perspective slot when it was last saved during a Cesium/globe session (don't persist a globe-scale pose as the BIM camera), OR (b) fire the existing §3D-FRAME-ON-VIEW-SWITCH auto-frame (zoomToAll/frame building) on the GIS-return path instead of restoring the stale slot — reuse the existing framing hook, do NOT add a one-off. Shortcut (not recommended): unconditional zoomToAll on every 3D activation (would clobber the legitimate slot restore on normal in-editor view toggles). | route -> **view-switching / camera-state queue.** GUARD after fix: returning from Forma → building is framed with NO manual zoom; normal in-editor 3D↔plan toggles still restore the user's last BIM camera. |
| L-379 | 2026-07-17 | **[P2 - RENDER] Context "presentation mode" render style — Spacio-style matte massing + soft AO + overcast studio shadows + stylized ground/roads + instanced trees for Forma CONTEXT buildings.** Additive, opt-in, PROVIDER-AGNOSTIC render style over the existing Cesium context path; NOT L-374 data tiering. CRUCIAL: context pixels are Cesium-owned (CesiumViewport.ts:1199), so styling is Cesium-native — the Three SSGI/TSL pipeline (SSGIPass.ts) is unreachable. Much already shipped Cesium-native. Full scoping: docs/04-reference/CONTEXT-PRESENTATION-RENDER-SCOPING.md | Rendering / C04 (Cesium-owned render half), C55 §1.2/§1.7 (read-only draped context), C19/C12 (shading-only, heights stay accurate). Layers onto L-374g, doesn't block. | **OPEN.** owner UNASSIGNED, target TBD. | route -> Rendering queue. |
| L-379a | 2026-07-17 | **[P2 - RENDER] Context material theme presets — warm-white / wood-tone matte presets + presentation toggle over the one flat FORMA_PALETTE.contextFill material.** Today one cool-grey flat Cesium.Color (CesiumViewport.ts:5288/:5320); Overture/3D-Tiles swap = Cesium3DTileStyle color on the same entity path. XS. | Rendering / C04, C55. dep: none (beta). | **OPEN.** owner UNASSIGNED, target TBD. | route -> Rendering queue. |
| L-379b | 2026-07-17 | **[P2 - RENDER] AO / overcast-studio lighting tune — tune Cesium HBAO radius/intensity + soft directional light + contact-shadow "studio pool" toward Spacio reference.** Already shipped Cesium-native (AO ensureFormaPostProcess CesiumViewport.ts:2476-2524; DirectionalLight :2244; soft shadows :2128/:2132) — tuning only; Three SSGI NOT reusable. XS-S. | Rendering / C04, C10 (Cesium AO single-pass, feature-gated). dep: none (beta). | **OPEN.** owner UNASSIGNED, target TBD. | route -> Rendering queue. |
| L-379c | 2026-07-17 | **[P3 - RENDER] Stylized ground colour-ramp + road-ribbon polish — soft "studio pool" / park-tinted colour-ramp on the flat Forma ground plane; road casing + pedestrian tier.** Roads already hierarchical corridor ribbons (CesiumViewport.ts:5526-5559); ground already flat colour (:1974) — colour-ramp is small net-new Cesium material; shading-only, heights/terrain stay accurate (C19/C12). S. | Rendering / C04, C19/C12, C55. dep: L-379a/b (beta-optional). | **OPEN.** owner UNASSIGNED, target TBD. | route -> Rendering queue. |
| L-379d | 2026-07-17 | **[P3 - RENDER] Instanced stylized trees — Cesium ModelInstanceCollection/billboard impostor trees scattered from OSM tree points / park polygons, matte-styled to match massing.** Today NO 3D trees, only flat park polygons (contextParks.ts; CesiumViewport.ts:859); Three InstancedMesh path NOT reusable (Cesium renderer). Render-STYLE half of L-374d VegetationProvider (data half) — cross-linked, not duplicated. M-L. | Rendering + Geospatial / C04, C55 §1.3/§1.4, ties L-374d. dep: L-374d (tree data). | **OPEN.** owner UNASSIGNED, target TBD (post-launch). | route -> Rendering/geospatial queue. |
| L-380 | founder 2026-07-17 (strategic business-direction feature) | **[P2 launch / HIGH strategic — GIS/PRODUCT] Parcel selection → buildable-envelope: select a real cadastral parcel on the 2D GIS map, highlight it in brand purple, compute+display the buildable envelope (setbacks/height/FAR/use) from municipal zoning, feed BIM authoring.** NEW FEATURE, additive, ZERO dependency on the September launch. Live-verified pilot = **Spain-first, Barcelona** (Catastro INSPIRE WFS parcels — free/keyless, GML3.2.1, EPSG:25830, point→refcat via OVC Consulta_RCCOOR since the WFS has NO BBOX; + Catalonia MUC zoning WFS `sig.gencat.cat/ows/PLANEJAMENT/wfs` MUC_QUALIFICACIONS — GeoJSON + spatial queries, open). Madrid #2 (sigma.madrid.es ArcGIS REST). Switzerland ÖREB + Terrara = #2 country (buy option); Denmark Plandata.dk = richest structured national zoning. KEY FINDING: numeric height/FAR/setbacks are PDF-trapped everywhere except Denmark → drives a TWO-FIDELITY engine (structured vs labelled estimated-ruleset). Reuses existing scaffolding: C19 `Parcel` already carries setbacks/maxFAR/maxHeight/zoning + `site.updateZoning`; `overpassProxy.js` = the Catastro-proxy template; `SiteBoundaryMap2D`→`site.parcel-boundary-set`→`generateResidentialFromBoundary`. Full scoping + phased plan: docs/04-reference/PARCEL-ZONING-FEATURE-SCOPING.md. | Geospatial / site-feasibility (new **C57 Parcel-Data-Layer** + **C58 Zoning-Rules-Engine** [fills C19 §9/§10.2 deferred jurisdiction registry] + **SPEC-PARCEL-SELECTION** + a strategy **ADR** + a **VISION** amend [STR-02/STR-12 §2.1 already anticipate cadastral site data]; reuses C19/C12/C18; mirrors L-374 provider pattern + L-373 credibility discipline + ADR-0088 proxy). Sub-items L-380a–e. | **OPEN — scoped, PLAN authored, not started. UNASSIGNED / TBD.** **Human decisions:** (1) zoning-intelligence as a V1 pillar vs Phase-B; (2) Terrara buy-vs-build for the semantic tier; (3) default map mode select-vs-draw; (4) CSP connect-src additions for Catastro/gencat (flagged, not silently changed). | route -> **geospatial / site-feasibility queue.** |
| L-380a | L-380 scoping | **[P2 - GIS] Parcel Data Layer — provider-agnostic `ParcelProvider` (Catastro/ÖREB/Terrara adapters) behind a server proxy → WGS84 ring → existing `buildBoundaryFromLatLonRing` → `site.parcel-boundary-set`.** Mirrors the L-374 Context-Engine provider pattern. | Geospatial / new **C57** (coverage gap). dep: L-380d proxy. | **OPEN.** UNASSIGNED/TBD. | route -> geospatial queue. |
| L-380b | L-380 scoping | **[P2 - GIS] Zoning Rules Engine — pure/deterministic `ZoningProvider` adapters + curated per-jurisdiction `JurisdictionZoningContract` (zone→envelope) → `BuildableEnvelope` (setback inset + max-height volume + `confidence`) → `site.updateZoning`.** Two fidelities (structured / estimated-ruleset, mirrors rules/programRules.ts) because numbers are PDF-trapped. | Geospatial / new **C58** (fills C19 §9/§10.2). dep: L-380a. **Human decision: curation vs Terrara buy.** | **OPEN.** UNASSIGNED/TBD. | route -> geospatial queue. |
| L-380c | L-380 scoping | **[P2 - GIS/UX] Parcel-select UI/UX — select-parcel map mode on SiteBoundaryMap2D (hover-preview → click → brand-purple #6600FF highlight → parcel info card [refcat/area/zone] → "Use this parcel"), envelope render in plan (inset polygon) + 3D (max-height volume), graceful "zoning unavailable — draw manually" fallback, provenance/confidence chips (L-373 discipline).** | Geospatial/UX / new **SPEC-PARCEL-SELECTION**; C18/C19 §5.5. dep: L-380a/b. **Human decision: default mode select-vs-draw.** | **OPEN.** UNASSIGNED/TBD. | route -> geospatial/UX queue. |
| L-380d | L-380 scoping | **[P2 - INFRA/GIS] Server proxy `server/parcelZoningProxy.js` — clone of overpassProxy.js (forward-once, GML→GeoJSON, LRU cache, non-fatal empty fallback) for Catastro/gencat/Madrid; CSP connect-src additions.** | Server/geospatial / ties **ADR-0088**. **Human decision: CSP connect-src (flag, not silently change).** | **OPEN.** UNASSIGNED/TBD. | route -> server/geospatial queue. |
| L-380e | L-380 scoping | **[P2 - GOVERNANCE] Author the parcel/zoning governance — VISION amend (site-feasibility/zoning-intelligence as a named product wedge), strategy ADR (~ADR-0269: provider-agnostic + two-fidelity + Spain-first + Terrara buy-vs-build), contracts C57/C58, SPEC-PARCEL-SELECTION, C00-index update.** Gated on the 2 strategic human decisions above. | Governance / C00 index; VISION/ADR/C57/C58/SPEC. | **OPEN — awaiting founder sign-off on the strategic decisions.** UNASSIGNED/TBD. | route -> governance queue. |
| L-381 | founder 2026-07-17 ("everything should be as sound as walls/curtain-walls") | **[P2 - ARCH] Element-creation pipeline is inconsistent — FOUR tiers, not a uniform sound path. Migrate every element to the walls/curtain-walls gold standard.** Audit matrix: **GOLD (7)** — bus `*.batch.create` + CEB `*.created` fan-out + initTools render bridge: Wall, Curtain-Wall, Slab, Column, Beam, Ceiling, Furniture. **NEAR-GOLD (4)** — bus single-create + render bridge, NO batch fan-out: Roof, Floor (also dual-command debt C11 §5.4.3), Handrail, Lighting. **HOSTED (3)** — via the wall-opening §P2.3 bridge: Door, Window, Opening. **LEGACY/Path-C (5)** — no real bus handler, legacy `Create*Command` only: Stair, Stair-railing, Stair-landing, Lift/verticalCirculation, Room-bounding-line. The gold pattern **IS mandated** (C16 §8/CA-12/B-6 "one gesture = one `*.batch.create`"; C11 §11.1 tracks STAIR-BUS-MIGRATION Wave A21) → this is **Known-Debt, not a coverage gap**. Undo/snapshot coalescing is ALREADY solved by L-376d (§GEN-UNDO-COALESCE) — the migration is purely RENDER-PATH consistency + true bus batching. Full audit + matrix + gold-standard spec + phased plan: docs/04-reference/ELEMENT-PIPELINE-SOUNDNESS-AUDIT.md | Architecture / element pipeline (C11 §2/§5/§11 element-creation, C16 §3.2/§8 command authoring, ADR-0055 wall pipeline). Sub-items L-381a–c. | **OPEN — audit delivered, not started. UNASSIGNED / TBD. NOT launch-blocking (generation works post-L-376d).** | route -> pipeline / tech-debt queue. |
| L-381a | L-381 audit (highest value) | **[P2 - ARCH + correctness trap] Stairs + Lifts to gold — and fix the silent no-op stub.** `plugins/stair` ships a COMPLETE tested handler set incl. a real `CreateStairBatchHandler` (`plugins/stair/src/handlers/index.ts:57`), but `registerStairHandlers()` is **NEVER imported/called in engineLauncher.ts:48-74** → the live `stair.batch.create` is the **no-op stub (initBusHandlers.ts:159)** = a silent create-nothing trap for any caller that dispatches it. Lifts have NO bus surface at all — only `cm.execute(new CreateVerticalCirculationCommand)` (OfficeBuildingExecutor.ts:1014). Migration: build the render bridge FIRST (else elements stop rendering — the L-376d finding), THEN wire the real batch handler + coalesce in executors. Office = 78 stairs + 80 lifts (highest value). MED risk. | C11 §11.1/§7.4/§10.2, C16 §3.2 (stair = sanctioned transitional Path-A exception). **Known-Debt/violation-flavoured: dead `registerStairHandlers` + live no-op stub; lift has no bus surface (violates C11 §4 convergence).** | **OPEN. UNASSIGNED/TBD. Human decision: (a) register the real stair handlers vs delete the dead stub; (b) make lift a first-class bus element (larger than a bridge).** | route -> pipeline / tech-debt queue. |
| L-381b | L-381 audit | **[P2 - ARCH] Roof/Floor batch fan-out + executor convergence — the resi/office executors bypass gold batch commands slab/CW already have.** Roof/Floor are near-gold (single-create, no batch fan-out); slab + curtain-wall already have gold batch commands the office executor doesn't use → mostly low-risk dispatch-site cleanup. Floor also carries dual-command debt (C11 §5.4.3) — full floor retirement DEFERRED (blocked on plugin-store residency). | C11 §5.4.3/§7.4, C16 §8. | **OPEN. UNASSIGNED/TBD. Floor dual-command retirement deferred for launch.** | route -> pipeline / tech-debt queue. |
| L-381c | L-381 audit | **[P3 - DOCS/ARCH] Handrail/Lighting batch fan-out + C11 §11 matrix doc-sync — the C11 §11 element matrix is STALE (Handrail/Furniture rows say LEGACY-ONLY but were bridged in §7.0).** Lowest value; correct the contract matrix + add batch fan-out to handrail/lighting. | C11 §11 (stale matrix), C16 §8. | **OPEN. UNASSIGNED/TBD.** | route -> docs + pipeline queue. |
| L-382 | founder 2026-07-17 (live main-DiR6oqVC.js, post-L-377 — geometry fast in 2-3s but "Finishing up / Compiling GPU shaders" appears NON-STOP, even while navigating) | **[P1 - PERF + LOADING UX] The post-geometry tail shows repeated "Compiling GPU shaders" overlays + per-sub-batch PSO/shader compile, and shaders keep compiling during navigation.** Confirmed from the live log: after the main structural batch (192 walls/26 slabs, fast), the deferred sub-batches (ceilings/floors/furnish/lighting/openings) EACH open a "Finishing up — Compiling GPU shaders 0/N elements" overlay AND each does its own `FIRST-RENDER-POST-SUPPRESS totalSuppressedMs=~640-1005ms … WebGPU PSO compile LONGTASK` (frameCount ~1725-1794, 6125 meshes). Root = the L-372 finding: the "webgl-fallback" is a `WebGPURenderer(forceWebGL2)` that **node-compiles every material's shader LAZILY** — so shaders recompile per sub-batch AND on first-render as new materials enter view during navigation. Plus the L-377 P2 **87s DependencyResolver CASCADE** (`§G1-SUPPRESS-LIFTED T=87408ms`). Three parts: (A) THE root fix = **L-372 Batch 2 (classic WebGLRenderer = zero TSL/node compile → deletes "Compiling GPU shaders" entirely)** — ESCALATED to top launch priority by this test; (B) NEW overlay-churn: hold ONE overlay across the deferred sub-batches instead of a fresh "Compiling GPU shaders 0/N" per sub-batch (extend the L-367 continuous-overlay lease over the post-gen sub-batch tail); (C) the 87s cascade = L-377 P2. | Render / loading UX (C04 §1.4 backend, C10 §7; **escalates L-372 Batch 2** [webgl-only classic routing], ties **L-377 P2** [cascade], **L-367** [continuous overlay]). Files (Batch 2): initScene.ts:1772 Phase-5 `webgl-only` guard lift + :2811 lightweight-render extend + guarded fallback + autoWebGLHeavyScene swap target; (overlay) buildingGenerationLifecycle lease over deferred sub-batches. | **OPEN — Batch 2 ESCALATED to next launch fix (after L-375a clears the code lane). UNASSIGNED / TBD.** | route -> **render / renderer-three (WebGPU) + loading-UX queue.** After Batch 2: NO "Compiling GPU shaders" (classic GLSL), no per-material lazy compile on nav; after overlay fix: ONE continuous overlay through the whole generation incl. the deferred tail. |
| L-383 | founder 2026-07-17 (Denmark technical-reference direction) | **[P2 / HIGH strategic-technical — GIS] Denmark geospatial REFERENCE IMPLEMENTATION — prove the whole pipeline (parcel → zoning → real LOD2 3D buildings → real terrain) on one country's genuinely-open data; unifies L-380 (parcel/zoning) + L-374 (3D context).** Validated the open-source stack against PRYZM's actual seams (no blockers): GDAL/OGR (MIT) normalize any source → GeoJSON/WGS84 canonical, behind `parcelZoningProxy.js` (clone `overpassProxy.js`, server.js:357); proj4js already in-tree (packages/geospatial); Turf.js (MIT — NEW dep, NOT in tree today) for setback inset; citygml-tools/tyler/3d-tiles-tools/py3dtiles → Cesium3DTileset via `CesiumViewport.ts:1399/1440`; cesium-terrain-builder (use tum-gis quantized-mesh fork) → terrain at `:4588/4687` (today NEVER attached `:4593-4607`); PostGIS(GPLv2, DB-usage-exempt)+Martin optional in-house serving. Denmark data = open-WITH-KEY (Datafordeler service key, server-side): Matriklen parcels (25832), Plandata.dk STRUCTURED zoning (height/FAR/floors/use), "Danmark i 3D" national semantic LOD2 CityGML, DHM 0.4m DTM/DSM (~5cm, commercial-ok). Germany LOD2-DE generalizes. Denmark = reference adapter quartet (full LOD2+terrain); Spain/CH = footprint-extrusion path (no free LOD2). Full architecture: docs/04-reference/DENMARK-GEOSPATIAL-REFERENCE-ARCHITECTURE.md (§7 phased DK-0..DK-5, §8 rows). | Geospatial (extends L-380 C57/C58 + L-374 C12-CONTEXT-ENGINE; ties C12/C19/C10; sub-items L-383a GDAL/GeoJSON+DK parcel/zoning, L-383b DK LOD2 3D-Tiles, L-383c DK terrain quantized-mesh, L-383d PostGIS/Martin serve-in-house [optional], L-383e deploy/native-binary). | **OPEN — validated, plan authored, not started. UNASSIGNED / TBD. Human decisions: (C-1) engineering-context vs Forma-abstract default; (C-2) PostGIS GPLv2 legal sign-off; (C-3) serve-in-house scope; (C-4) confirm tyler license permissive.** | route -> geospatial / site-feasibility + devops queue. |
| L-383e | L-383 validation (biggest risk) | **[P2 - DEVOPS, decision needed] Native-binary/JVM deploy story — GDAL, citygml-tools, cesium-terrain-builder, tyler must NOT go in the Fly app image.** Run them OFFLINE in a one-shot Docker build job → pre-bake static 3D-Tiles + quantized-mesh terrain + normalized GeoJSON → host on object storage. Fly app image gains only `parcelZoningProxy.js` (pure Node, holds the server-side key); PostGIS+Martin as a separate service. Risk LOW with offline-bake discipline, MEDIUM-HIGH if any converter is naively apt-get'd into the app Dockerfile. **Needs human ratification before build.** | Devops / geospatial (Fly deploy). | **OPEN — awaiting ratification. UNASSIGNED/TBD.** | route -> devops queue. |
| L-384 | founder 2026-07-17 (live "Create & guide me" onboarding test + repo map) | **[P1 - UX] The onboarding site-authoring flow has NO BACK on the draw/confirm/program steps and NO ESC/undo-redo/re-draw — a mis-drawn boundary traps the user; also build the FULL parcel UI/UX pipeline (draw + select modes) into the step now, even before parcel-data is wired.** BUG + forward feature. Verified against `OnboardingStepController.ts` (2175 lines, StepId = location|site|confirm|generating): BACK exists ONLY on the Site/plot step (:506-511 → renderLocationStep) + overlay-import (:780-786). **MISSING BACK on: the Draw step (renderDrawingStep :871-898 — only a "Skip drawing" ghost, no return), the Generate-confirm step (:922-998 — only Generate / "Not now"=dispose), and the residential/office program steps.** No ESC-to-restart-boundary + no undo/redo/re-draw once drawn. **CORRECTION to the initial report:** the "Step N of 4" indicator is NOT removed — it renders at :264-269/:295-300 with all 6 call sites live; only a STALE "STEP 2 OF 3" comment at :869 needs fixing. P1: primary new-user entry path; a trapped/mis-drawn boundary blocks the core "raw site → building" promise. **Forward-looking (founder):** build the site-authoring step as ONE shared edit surface with TWO input modes — **draw** (today) and **parcel-SELECT** (L-380/L-383, UI present even if not yet wired to Catastro/Denmark data) — sharing BACK/ESC/undo/redo/re-draw. | UX / site-authoring onboarding (C06 §7 UI layer; C18/C19 site; **COVERAGE GAP — no contract governs the onboarding step-machine UX** → log to MISSING-CONTRACTS). Seam: `OnboardingStepController.ts` (step machine, renderLocationStep :350 / renderSiteStep :460 / renderDrawingStep :871 / renderGenerateConfirmStep :922 / renderResidentialProgramStep :1012; setStepIndicator :295; startDrawThenGenerate :584 subscribes `site.parcel-boundary-set` :634), `GISAreaLayout.startBoundaryDraw` :113/mount :123-142 (onClose :134, onCommit :139 defers teardown per O.7.2.b, closeBoundaryMap2D :162), `SiteBoundaryMap2D.ts` (draw+commit; the only uncommitted worktree change), `siteSetParcelBoundary` (C19 §1.4 immutable one-shot → re-draw needs clear-then-recreate). Window shims: pryzmStartBoundaryDraw :313/:1047, pryzmToggleGIS :1025, pryzmCloseBoundaryMap2D :1018. | **OPEN — routed to Pipeline B site-authoring worktree (folded with parcel). UNASSIGNED / TBD.** | route -> **geospatial / site-authoring UX queue.** |
| L-385 | founder 2026-07-17 (live main-0uKZoCNG.js — loading modal aesthetic) | **[P2 - UX] Unify ALL loading screens to the "rotating PRYZM prism + translucent background + just text" aesthetic (the crash/"problems rendering" fallback style), replacing the frosted-card-with-placeholder-icon modal.** UX-consistency feature. The generation/batch/view-activation loading overlay currently renders as a frosted card with a grey placeholder icon + "Finishing up / Compiling GPU shaders / N elements / 96%". The founder wants the calmer, brand-forward aesthetic already used on the viewport-crash / "problems rendering" screen (a rotating PRYZM prism over a translucent backdrop with just text) applied to EVERY loading page — one shared presentation, not per-screen. Correct approach = change the SINGLE shared loading-overlay VIEW so all producers (batch lifecycle, §FEAT-VIEW-ACTIVATION-LOADING-OVERLAY L-270, building-generation lease L-367) inherit it — reuse the crash-fallback's prism/backdrop component/CSS, do NOT fork a new overlay per screen. | UX / loading overlay (C06 §7 UI layer; **likely COVERAGE GAP — no contract governs the loading-overlay visual spec** → MISSING-CONTRACTS). Seam: `LoadingOverlayController` + `LoadingOverlayView` (apps/editor/src/ui/overlays/), the crash-fallback/`ViewportCrashGuard`/SceneCrashFallback rotating-prism component to reuse, `onboardingStyles.ts` / overlay CSS. | **OPEN — routed to UI/loading-overlay. UNASSIGNED / TBD.** | route -> **UI / loading-overlay queue.** |
| L-386 | founder 2026-07-17 (live main-0uKZoCNG.js resi gen — "Compiling GPU shaders" still interrupts nav) | **[P1 - PERF] Batch 2's classic-renderer routing never engages on a box that BOOTS into webgl-fallback — so "Compiling GPU shaders" persists per deferred sub-batch during navigation.** L-372 Batch 2 routes heavy-gen to the classic `webgl-only` renderer ONLY via a swap FROM real WebGPU (`status.webGpuActive`). But the founder's box boots directly into `webgl-fallback` (persisted `'webgl'` pref → `createRenderer resolvedPreference=webgl (forceWebGL=true)`) — a `WebGPURenderer({forceWebGL2})` that STILL lazily TSL-compiles shaders. Since it's not on real WebGPU, no swap fires, it stays on `webgl-fallback`, and the ~13s `FIRST-RENDER … WebGPU PSO compile LONGTASK` + "Compiling GPU shaders" repeats on each deferred sub-batch (ceilings/floor-finish/room-finish), interrupting navigation. Confirmed in the log: `WebGPU: false` throughout, transmission guard fires on the "node-compiling renderer (WebGL2-backed fallback)", `totalSuppressedMs=13079ms`. **Fix: extend the heavy-gen swap to ALSO upgrade `webgl-fallback` → `webgl-only` (classic), not just `webgpu`→classic (live-swap; does not touch the boot abort).** | Render (C04 §1.4; completes **L-372 Batch 2** / fixes **L-382** on the webgl-fallback-boot path; ties L-367 proactive swap). Files: `apps/editor/src/rendering/autoWebGLHeavyScene.ts` (swap condition/guard: also trigger from webgl-fallback, skip only if already webgl-only / light / explicit). | **OPEN — fix in flight (Batch-2 agent). UNASSIGNED / TBD.** | route -> render / renderer-three queue. |
| L-387 | pre-launch audit 2026-07-17 (`pnpm audit`) | **[P1 - SECURITY/DEPS] 93 open dependency advisories (7 critical / 28 high) — runtime-reachable: jsPDF LFI+injection+DoS, Multer/ws/form-data/protobufjs DoS; none fixed.** NEW. `npm audit` unusable in this pnpm repo (ENOLOCK); `pnpm audit` used. Runtime-reachable ones (jsPDF in PDF export, Multer/ws/form-data on the server) are the launch risk, not just devDeps. Raw output: reports/.pnpm-audit.txt. | Infra / security (C07/C29). | **OPEN — UNASSIGNED / TBD.** Triage: which advisories are runtime-reachable → upgrade those first (jsPDF, Multer, ws, form-data, protobufjs). | route -> infra / security queue. |
| L-388 | pre-launch audit 2026-07-17 (root `npx vitest run`) | **[P1 - TESTS] The root vitest gate is RED — 3 door/dimension specs fail at MODULE LOAD via `DoorStore.ts:227` barrel-at-load (register 0 tests).** NEW (instance of the known SCC barrel-access-at-module-load hazard — [[scc-no-barrel-access-at-module-load]]). This is the `dimensionSelectionPanel.spec.ts` collect-fail the Batch-2 agent flagged — now root-caused to a barrel accessed at DoorStore module load. A red root gate means CI can't be trusted green pre-launch. | Tests / SCC (C13/C03). Files: `DoorStore.ts:227` (barrel at load) + the 3 specs. | **OPEN — UNASSIGNED / TBD.** Fix = move the barrel access inside a function (the §SCC pattern). | route -> tests / SCC queue. |
| L-389 | pre-launch audit 2026-07-17 | **[P1 - PERF, UNVERIFIED] Real in-browser 60 FPS frame-budget + tool-latency-with-renderer are UNVERIFIED — only headless proxies exist, and the founder's live logs contradict on heavy scenes.** NEW (umbrella over L-02/117/139/372/382/386). No browser bench (`apps/editor-bench` "Wave 13") captured; the C10 perf budget has no measured pass. Cannot claim the perf budget is met at launch. | Perf (C10). | **OPEN — UNASSIGNED / TBD. UNVERIFIED — a human must run a real in-browser bench at target scale.** | route -> perf queue. |
| L-390 | pre-launch audit 2026-07-17 | **[P2 - DOCS/GOVERNANCE] Dead governance breadcrumbs — CLAUDE.md + contracts + bench notes cite `docs/03_PRYZM3/01-VISION.md` / `02-ARCHITECTURE.md`, which no longer exist (moved to `docs/01-strategy/` + archived).** NEW. The governance conflict-resolution order in CLAUDE.md points at missing files → contributors/agents can't find the canonical vision/architecture. | Docs / governance (C01). | **OPEN — UNASSIGNED / TBD.** Fix = repoint the references OR restore the canonical paths. | route -> docs queue. |
| _re-flag_ | pre-launch audit 2026-07-17 (already-tracked, escalate to LAUNCH-BLOCKING) | **P0/P1 already-logged, re-flagged by the pre-launch audit:** **L-53** (P0 — concurrent wall baseLine edit = silent last-write-wins data loss, no user-resolvable conflict, P8 violation); **L-85** (P0 — kitchen/wardrobe/lighting elements VANISH after project close→reopen = DATA LOSS); **L-72** (P1 — undo doesn't capture MOVE/ROTATE/property-change in 3D → edits silently unrecoverable); **L-133** (P1 — every deploy = 30–60s full 503 outage, no zero-downtime rollout); **L-376a** (P1 — ~30 unsaved local-only projects accumulate in IndexedDB, no eviction); **L-373a** (P1 — Wind/Temp heatmaps present a heuristic as analytical). These are NOT new items — their existing rows stand; this row records that the launch audit escalates them to launch-blocking. | see each L-NNN | **OPEN — escalated to launch-blocking. Owners on the existing rows.** | route -> per existing row. |
| L-391 | category-readiness audit 2026-07-17 | **[P0 - COLLAB, DEPLOYMENT GAP] Real-time CRDT collaboration has NO network backend — `apps/sync-server` is undeployed and no `WebsocketProvider` is ever constructed, so the Yjs doc + `CRDTConflictResolver` never see remote ops. Production collab is socket.io command-rebroadcast applied in arrival order = silent last-write-wins for move/edit/delete.** Extends **L-375a** (which fixed only the applier WIRING and framed it as "silent degrade" — it did NOT flag there is no sync backend at all; the deployment gap is NEW). `fly.toml:36 processes=["app"]`, `Dockerfile:171` single process; `RemoteCommandDispatcher.ts:157` (no merge); `server.js:534-599`. | Collaboration / infra (C08 §3.1/§3.3; engineLauncher.ts:817-838; YjsDocAdapter.ts:232; apps/sync-server undeployed). | **OPEN — UNASSIGNED / TBD. HUMAN DECISION (the September launch call): deploy sync-server + WebsocketProvider + harden CRDT, OR descope multi-user real-time for V1 and remove the conflict-UI claim.** | route -> collaboration / infra queue. |
| L-392 | category-readiness audit 2026-07-17 | **[P1 - OBSERVABILITY] P8 OTel spans are created everywhere but NO tracer provider is registered → spans are no-ops exporting nowhere; crash reporter is `NoopCrashReporter` (Sentry deferred); no save-integrity/error-rate alert. A production data-corruption is invisible until a user reports it.** NEW. `grep NodeTracerProvider|WebTracerProvider|provider.register` = none (only `sdk-trace-base` devDep); `crash-reporter/package.json:4`; `server.js:2172` only reports the endpoint in health JSON. | Observability (C10 §OTel, P8; packages/crash-reporter; server.js:2172). | **OPEN — UNASSIGNED / TBD.** Register an OTLP exporter + bind Sentry/GlitchTip + add a save-integrity alert. | route -> observability queue. |
| L-393 | category-readiness audit 2026-07-17 | **[P1 - INTEROP] No IFC/DXF/Rhino round-trip (import→export→re-import compare) test — geometry fidelity for the load-bearing BIM formats is ASSUMED. Only BCF + family/chunk formats have round-trip coverage.** NEW. `packages/file-format/src/import/{ifc,dxf,rhino}` + `export/{ifc,glb}`; only family/chunk/schema/bcf have round-trip tests. Also: adversarial malformed-file behaviour (crash vs silent drop vs graceful reject) UNVERIFIED. | Interop (C05, C47; packages/file-format/__tests__). | **OPEN — UNASSIGNED / TBD.** Add IFC round-trip fixture test first, then DXF. | route -> interop queue. |
| L-394 | category-readiness audit 2026-07-17 | **[P1 - PERSISTENCE] Snapshot schema migration (v0→v5) is UNTESTED, and a forward-version snapshot loads anyway with silent field loss.** NEW. No `MigrationEngine` test exists (only localStorage→IDB covered); `MigrationEngine.ts:241-248/233-269` returns newer-than-current snapshots unchanged with only a `console.warn`. Opening an old/newer project = unverified data-loss risk. | Persistence (C05, C47; MigrationEngine.ts:233-269). | **OPEN — UNASSIGNED / TBD.** Add a v0→v5 migration round-trip test + a forward-compat rejection/quarantine policy. | route -> persistence queue. |
| L-395 | category-readiness audit 2026-07-17 | **[P1 - SECURITY, UNVERIFIED] `/embed` serves a public HTML shell echoing projectId+token into the DOM with NO server-side token validation (X-Frame-Options removed, frame-ancestors *); the token→access-scope mapping (view vs edit, single-project vs account-wide) is unlocated — a leaked link's blast radius is unconfirmed.** NEW. `server.js:4557-4595`. | Security (C07 §6.1, C08; server.js:4557). | **OPEN — UNASSIGNED / TBD. UNVERIFIED — verify + document embed-token access scope; add a test.** | route -> security queue. |
| L-396 | category-readiness audit 2026-07-17 | **[P1 - DURABILITY] End-to-end backup RESTORE has never been drilled, and free-plan projects live ONLY in one browser's IndexedDB (0 server versions).** NEW (runbook-acknowledged, not previously an L-item). `DR-DRILL-RUNBOOK.md:340` "does not claim a real production drill has been executed"; PITR "not yet wired" (:342); `VERSION_LIMITS.free=0` (`server.js:3258`). A free-plan user clearing browser data = total project loss; no proven restore path. | Durability / infra (C48; DR-DRILL-RUNBOOK; server.js:3258). | **OPEN — UNASSIGNED / TBD.** Run a real pg_dump→restore→verify drill; decide free-plan server-version policy. | route -> infra / persistence queue. |
| L-397 | founder/briefing (2026-07-17) | **[MEDIUM] Pricing contradiction: marketing/strategy docs advertise Solo $25 / Studio £15/seat / Mid-firm $35/seat / Enterprise $100/seat (`docs/01-strategy/STR-02-product-vision.md:305-311`, `docs/01-strategy/STR-08-go-to-market.md:204-209`) but the billing CODE charges architect $59 / studio $149 / firm $349 (`packages/core-app-model/src/monetization/PlanConfig.ts:178/186/194` — `monthlyUSD`). Unreconciled; the code is what would actually bill. One authoritative source of truth required before any marketing/pricing page ships. FOUNDER DECISION (do not auto-resolve). Source: `reports/PRYZM-STRATEGY-BRIEFING-2026-07-17.md`.** | Commerce / billing — **C39-PRICING-AND-PLAN-TIERS** is the canonical contract, BUT it carries NEITHER price set and uses a THIRD tier taxonomy (`solo/studio/mid-firm/enterprise`) that the code (`architect/studio/firm` + a `free` plan C39 §444 says must not exist) contradicts → the authoritative numbers must be ratified INTO C39, or C39 superseded by an ADR. | **OPEN (MEDIUM) — UNASSIGNED / TBD. FOUNDER DECISION (do not auto-resolve).** | route -> billing / GTM decision queue. |
| L-398 | Archistar-Europe gap audit 2026-07-17 | **[P1 - COMPLIANCE/GIS, Pipeline A] Zoning Rules Engine + BuildableEnvelope solver — the core compliance value prop, ~0% built today.** Pure L2 `ZoningRulesEngine`: parcel ⊖ setbacks (Turf negative buffer) → inset polygon + `area×maxHeight` volume, two-fidelity (`structured` / `estimated-ruleset` / `none`) resolution → `site.updateZoning`. No `ZoningRulesEngine` / `computeBuildableEnvelope` / `BuildableEnvelope` / `JurisdictionZoningContract` exists in the tree; Turf is not yet a dep. The single largest gap to the Archistar loop. | Compliance / GIS (**C58-ZONING-RULES-ENGINE — NEW/UNAUTHORED, C00 index ends at C56**; ties C19 §1.4/§1.6 deferred jurisdiction registry). Source: `docs/04-reference/ARCHISTAR-EUROPE-COMPETITIVE-GAP-AUDIT-2026-07-17.md` §3 G-ENG-1/2/3, §7. | **OPEN — Pipeline A — compliance-authoring (September pillar); runs IN PARALLEL with the P0 launch-blocker track (different subsystems: geo/site/new-packages vs persistence/collab/security). Architecturally-sound order is CONTRACTS-FIRST: author C57 + C58 + SPEC-COMPLIANCE-REPORT + ADR before the engine code — that governance work is L-403, now promoted to the FIRST step of the compliance sequence (IN PROGRESS). UNASSIGNED / TBD.** | route -> geospatial / site-feasibility / compliance queue. |
| _founder-decision_ | Archistar-Europe gap audit 2026-07-17 (§4 conflicts, near L-398) | **FOUNDER DECISION — flag only, do NOT auto-resolve (three conflicts raised by the compliance-authoring audit):** **CF-1 — RESOLVED (founder, 2026-07-17): compliance = September Pipeline-A pillar, fully wired by launch.** The parcel → zoning → buildable-envelope → authoring loop is a September launch pillar, part of Pipeline A, fully wired by September (NOT a Phase-B fast-follow); it runs IN PARALLEL with the P0 launch-blocker track (different subsystems: geo/site/new-packages vs persistence/collab/security). (Superseded audit verdict: compliance ~0-5% built and ~8-11 dev-weeks — the founder accepts that scope INSIDE the September window.) **CF-2** — Switzerland canton numeric-rule data: **Terrara buy vs. build** (license a normalizer vs. hand-maintain 26-canton PDF rule packs; no public API/pricing, UNVERIFIED). **CF-3** — engineering-context (DK LOD2 fidelity) vs. Forma-abstract massing as the DEFAULT site view (inherited from L-374 §7.5). | Strategy / governance (ties L-398..404, L-373, L-383). | **CF-1 RESOLVED (founder, 2026-07-17): compliance = September Pipeline-A pillar, fully wired by launch. CF-2 (Terrara buy-vs-build) + CF-3 (default site view) remain OPEN — FOUNDER DECISION, do not auto-resolve.** | route -> founder / strategy decision queue. |
| L-399 | Archistar-Europe gap audit 2026-07-17 | **[P1 - COMPLIANCE/GIS, Pipeline A] ZoningProvider adapters + curated rule packs — DK Plandata + ES-Catalonia MUC.** `DkZoningProvider` (Plandata.dk anonymous WFS, structured fields `bebyggelsesprocent`/`maksbygningshoejde`/`maksantaletager`/`anvendelse`) + `MucZoningProvider` (Catalonia MUC zone class) + curated `JurisdictionZoningContract` packs (`da-*`, `es-barcelona`) filling the PDF-trapped numbers; mirror `rules/programRules.ts`. No `ZoningProvider` implementation exists outside the interface stub today. | Compliance / GIS (**C58 — NEW/UNAUTHORED**; L-373 credibility labelling). Ref L-383 (DK reference). Source: audit §2/§3 G-DATA-1/4, §7. | **OPEN — Pipeline A — compliance-authoring (September pillar); runs IN PARALLEL with the P0 launch-blocker track (different subsystems: geo/site/new-packages vs persistence/collab/security). UNASSIGNED / TBD.** | route -> geospatial / compliance queue. |
| L-400 | Archistar-Europe gap audit 2026-07-17 | **[P2 - GIS/PARCEL, Pipeline A] Parcel Data Layer completion — lift to L2 `@pryzm/site-parcel-data` + DK/CH adapters.** Lift the shipped keyless Catastro P0 (`apps/editor/src/ui/site/parcel/`) to an L2 `@pryzm/site-parcel-data` package; add `DkParcelProvider` (Matriklen via server-side Datafordeler service-user key) + `OerebParcelProvider` (CH EGRID-keyed); per-field provenance per L-373. | Parcel data (**C57-PARCEL-DATA-LAYER — NEW/UNAUTHORED**; ties C12/C19). Ref L-383. Source: audit §2/§3 G-DATA-2/3/5, §7. | **OPEN — Pipeline A — compliance-authoring (September pillar); runs IN PARALLEL with the P0 launch-blocker track (different subsystems: geo/site/new-packages vs persistence/collab/security). Datafordeler API key registered by founder, held as a Fly secret (to be wired here + L-399 — no key value in docs). UNASSIGNED / TBD.** | route -> geospatial / parcel queue. |
| L-401 | Archistar-Europe gap audit 2026-07-17 | **[P1 - COMPLIANCE→AUTHORING BRIDGE, Pipeline A] Envelope → authoring constraint bridge — connects compliance to PRYZM's authoring moat.** Thread the inset polygon + maxHeight (+ permitted-use → typology brief) into `generateResidentialFromBoundary` / apartment / house / typology-pipeline as generation bounds. Today generators read `getParcelBoundary()` only — no setback inset or height cap — so compliance and authoring are two disconnected halves; this connection IS the competitive thesis ("beat Archistar on authoring OF THE COMPLIANT building"). Preserve C53 slider-as-intent (no parallel knob). | Compliance→authoring (C19 §1.6, **C50** typology pipeline, **C53** layout engine). Source: audit §3 G-BRG-1/2, §5.2 B2, §7. | **OPEN — Pipeline A — compliance-authoring (September pillar); runs IN PARALLEL with the P0 launch-blocker track (different subsystems: geo/site/new-packages vs persistence/collab/security). UNASSIGNED / TBD.** | route -> geospatial / generation queue. |
| L-402 | Archistar-Europe gap audit 2026-07-17 | **[P1 - COMPLIANCE, Pipeline A] Compliance "explain-why" report + 3D envelope render — the Archistar-parity deliverable.** Envelope + rule refs + ordinance links + confidence chips (Archistar SELLS the report: "this envelope because setback X, height Y, FAR Z, ordinance ref"); translucent max-height 3D volume render (P2-safe via the existing renderer, brand `#6600FF`); CI fidelity-label gate so `estimated-ruleset` is never shown authoritative. No report artefact / provenance chip exists for zoning today. | Compliance (**SPEC-COMPLIANCE-REPORT — NEW/UNAUTHORED**; C23 provenance, L-373; C04/C18 render). Ref L-373. Source: audit §3 G-ENG-4, G-BRG-3, §5.2 B3, §7. | **OPEN — Pipeline A — compliance-authoring (September pillar); runs IN PARALLEL with the P0 launch-blocker track (different subsystems: geo/site/new-packages vs persistence/collab/security). UNASSIGNED / TBD.** | route -> geospatial / compliance queue. |
| L-403 | Archistar-Europe gap audit 2026-07-17 | **[P2 - GOVERNANCE, Pipeline A] Governance authoring — the missing contracts/specs/ADR for the compliance track.** Author **C57-PARCEL-DATA-LAYER** + **C58-ZONING-RULES-ENGINE** (incl. `JurisdictionZoningContract`) + **SPEC-PARCEL-SELECTION** + **SPEC-COMPLIANCE-REPORT** + a strategy ADR (ADR-02XX: parcel-data + per-jurisdiction zoning + Terrara buy-vs-build) + a VISION amendment elevating site-feasibility to a named wedge; reserve the C00 index numbers; record the CF-1 (V1-pillar vs Phase-B) + CF-2 (Terrara buy-vs-build) decisions. | Governance (C57/C58/SPEC-PARCEL-SELECTION/SPEC-COMPLIANCE-REPORT — all NEW; ADR-02XX; C00 index; STR-02/STR-12). Source: audit §4, §5.2 B0, §7. | **IN PROGRESS — Pipeline A — compliance-authoring (September pillar); PROMOTED to the FIRST step of the compliance track (contracts before code); runs IN PARALLEL with the P0 launch-blocker track. Datafordeler API key registered by founder, held as a Fly secret (to be wired with L-399/L-400 — no key value in docs).** | route -> governance / docs queue. |
| L-404 | Archistar-Europe gap audit 2026-07-17 | **[P2 - GIS/DEMO, Pipeline A] Denmark demo-fidelity bring-up — executes L-383a–e.** Offline-bake LOD2 3D-Tiles ("Danmark i 3D" national semantic CityGML) + DHM quantized-mesh terrain; DK Matriklen keyed parcel; the flagship parcel → envelope → LOD2 context + real terrain in one Cesium view (best demo fidelity in Europe). Native binaries (GDAL/citygml-tools/cesium-terrain-builder/tyler) run OFFLINE only — the Fly app image stays pure Node (per L-383e). | Geospatial / demo (extends **L-383**; C12-CONTEXT-ENGINE NEW gap, C55). Source: audit §5.2 B5, §7. | **OPEN — Pipeline A — compliance-authoring (September pillar); runs IN PARALLEL with the P0 launch-blocker track (different subsystems: geo/site/new-packages vs persistence/collab/security). UNASSIGNED / TBD.** | route -> geospatial / devops queue. |
| L-405 | founder 2026-07-17 (screenshot, pryzm.fly.dev) | **[P2 - UX / GIS, ties Pipeline A] Unified view-mode switcher — make the 2D parcel-select map a first-class switcher option + wire a forward slot for the enhanced-Forma (LOD200) view.** A segmented view-mode bar already exists (`mountResultToggleBar`, `GISAreaLayout.ts:789`) with three peer segments — `◧ 3D + plan` (`activeSegment='2D'`), `◉ 3D globe` (`'3D'`), `◉ 3D Site` Forma (`'forma'`) — plus the globe's `◉ Real / ▢ Massing / ⤢ Zoom / ▶ Fly-tour` sub-controls. The founder wants the switcher CLARIFIED into the intended set and TWO gaps closed: **(a)** the **2D parcel-select map** (`SiteBoundaryMap2D.ts` / `mountSiteBoundaryMap2D`, today mounted ONLY via the onboarding draw step at `GISAreaLayout.ts:128` — NOT the Forma `▦ 2D Map` sub-mode at `:2194/:2358`, which is a Forma-mode cream basemap, not the parcel-select/draw surface) must become a persistent, first-class switcher segment so the user can return to add/SELECT the parcel at any time; **(b)** an **enhanced-Forma / LOD200 "super-enhanced context" segment** must be WIRED NOW (founder: "you can already wire it — we will have the enhanced Forma view; add it now"), backed by the current Forma/Context path as a placeholder, with the enhanced-data path swapping in behind the same view id later. **(b) IS the already-designed `▤ Context` peer segment (L-374g, `CONTEXT-VIEW-DESIGN.md`) — this row is the founder-navigability framing of it + the net-new parcel-select segment.** | UI-shell / geospatial view-mode (**C06 §7** launcher/view-mode layer [governs; the segmented bar is the launcher-layer view intent — mirrors L-40/L-104 which mounted their launchers via C06 §7]; **C06 §1** PlatformRouter/`viewRegistry`; **C04** single reused Cesium viewer; **C12-GEOSPATIAL §7/§8**; **C19-SITE-MODEL-AND-PARCEL §1.4** immutable boundary; **C57-PARCEL-DATA-LAYER §5** parcel-select UI + §5.4 select-vs-draw default; **C55** enhanced context data). Specs: `CONTEXT-VIEW-DESIGN.md` (founder two-views decision + L-374g `▤ Context` segment), `FORMA-CONTEXT-ENGINE-AUDIT.md`, `CONTEXT-PRESENTATION-RENDER-SCOPING.md`; ADR-0016 (view-state command-driven), ADR-0115 (site-view launcher, L-40/L-104), the L-378 globe-scale pose guard (`MultiViewCameraManager`/`ViewCameraStateStore` `globeScaleGuard` tests). **COVERAGE GAP: no contract/spec governs the view-mode-switcher SET/registry itself — the segmented bar is ad-hoc in `GISAreaLayout.ts`, not `viewRegistry`-driven; and SPEC-CONTEXT-VIEW + SPEC-PARCEL-SELECTION are both still proposed/unauthored (logged to MISSING-CONTRACTS-AUDIT).** No conflict with the request — it aligns with CONTEXT-VIEW-DESIGN + C57. | **OPEN — P2 (post-launch-acceptable: all four surfaces are reachable today via onboarding + the existing 3-segment bar, so this is navigability/UX polish + forward-wiring, not a launch blocker; entangled with the compliance-authoring pillar because the 2D parcel-select map is that flow's entry — HIGH strategic for that pillar). UNASSIGNED / TBD. Sub-of / overlaps L-374g (enhanced-Forma segment) + the L-380/C57 parcel-select track.** | route -> view-system / UI-shell (C06) + geospatial (C12/C19/C57) queue. |
| L-406 | pre-launch evidence 2026-07-17 (§B5, "uncaptured findings" #3) | **[P1 - SECURITY] `/api/event-log` is a mutating write route with NO `authMiddleware` — unauthenticated event-log write; cross-tenant write UNVERIFIED.** NEW (the most serious of the six uncaptured findings). `server.js:3970` mounts the route outside the auth chain (three benign siblings `/csp-report` :339, `/leads` :345, `/overpass` :357 are also authless, but event-log is a persisted write); whether an anonymous caller can write into another tenant's log is not established. | Security / multi-tenant (C08 auth+roles; C07). File: `server.js:3970`. | **OPEN — UNASSIGNED / TBD. UNVERIFIED — confirm the write's tenant scope + add `authMiddleware` (or document why authless is safe) + a test.** | route -> security queue. |
| L-407 | pre-launch evidence 2026-07-17 (§A4, "uncaptured findings" #1) | **[P2 - SECURITY, UNVERIFIED] ~781 `.innerHTML=` / `dangerouslySetInnerHTML` / `eval(` / `new Function(` XSS sinks across `apps/*`,`src/*`,`packages/*` — UGC/marketplace pages highest-risk.** NEW. Sampled marketplace uses `escapeHtml()` and many are static strings, but a minority interpolate values; UGC marketplace family names/descriptions need a focused XSS pass. Full review UNVERIFIED. | Security / XSS (C08; C07). Sinks across `apps/*`/`src/*`/`packages/*`; marketplace UGC pages. | **OPEN — UNASSIGNED / TBD. UNVERIFIED — scope the interpolating sinks (esp. UGC/marketplace), enforce escaping, add a CI sink-scan.** | route -> security queue. |
| L-408 | pre-launch evidence 2026-07-17 (§A6, "uncaptured findings" #2) | **[P2 - PERF] Client bundle-size / first-paint budget is never CI-gated — `main` 2.45 MB + `engineLauncher` 3.23 MB eager (uncompressed); gzip is never gate-measured.** NEW. The `bundle-size.json` "budget" is a 0.31 KB schema-manifest proxy, not the real shell; `RoomAutoOrganiser.ts` static+dynamic import defeats a code-split. No CI budget on the real production shell. | Perf / first paint (C10). Build output `dist/`; `bundle-size.json` proxy. | **OPEN — UNASSIGNED / TBD.** Add a real gzipped app-shell budget gated in CI; fix the RoomAutoOrganiser static+dynamic split. | route -> perf queue. |
| L-409 | pre-launch evidence 2026-07-17 (§B8, "uncaptured findings" #4) | **[P2 / GAP - UNVERIFIED] Browser/device matrix is effectively one Windows box — Playwright covers desktop chromium/firefox/webkit only (no mobile/tablet device projects); tablet/mobile even read-only likely unsupported.** NEW. Real GPU verification is one Windows box (forces WebGL; WebGPU on real hardware repeatedly device-loses — L-361/366/372); no `isMobile`/touch gating found. | Browser/device coverage (C44/C45 — both DRAFT). `playwright.config.ts:28-30`. | **OPEN — UNASSIGNED / TBD. UNVERIFIED — define + run a real device/GPU matrix; decide tablet/mobile support scope.** | route -> QA / perf queue. |
| L-410 | pre-launch evidence 2026-07-17 (§B7, borderline finding) | **[P2 - DURABILITY] `§SERVER-PG-DEGRADE` fails OPEN to a volatile in-memory project store during a DB blip — a non-durable-write window: writes accepted during degrade vanish on restart.** NEW (mechanism documented as the migration-gate-deadlock fix, but the "non-durable write window" was never its own row). `server/pgClient.js:251-278`. | Durability / infra (C05; C48). File: `server/pgClient.js:251-278`. | **OPEN — UNASSIGNED / TBD.** Decide degrade-mode write policy (reject-writes vs volatile-accept) + surface a degrade banner + reconcile on recovery. | route -> infra / persistence queue. |
| L-412 | founder 2026-07-18 (redirect) | **[P2 - VIEW-SYSTEM / UI-SHELL] Native multi-pane view system — the user must be able to project ANY view (2D site map · 3D Site · BIM 3D · BIM plan · elevations) into EITHER pane and swap them, natively, no shortcuts.** Founder verbatim: *"the user should generally be able to swap from one view to another and project whichever view it wants in EITHER left or right split view, NATIVELY — without shortcuts — super robust for the long run — not just for the purpose of bringing the 3D Site to the right only."* Traced root: THREE mutually-incompatible view owners on one `#container` — (1) `SplitViewManager` fixed Canvas2D right pane (`SplitViewManager.ts:350–516`, auto-opens on project load `initScene.ts:3794`); (2) Cesium hard-targets `#container` + owns it whole (`GISAreaLayout.ts:213,257`, `CesiumViewport.ts:900–919`, `setVisible`); (3) MapLibre `inset:0` overlay in `#container` (`SiteBoundaryMap2D.ts:287–297`). None can host an arbitrary view in an arbitrary pane, so "3D Site on the right" hard-wired would be a shortcut that deepens the incompatibility. **Also confirmed (envelope trace):** the buildable-envelope render path to the `#6600FF` translucent prism is COMPLETE (`resolveFormaEnvelope`→`renderFormaMassing`→`CesiumViewport.ts:3724` extruded polygon) — the ONLY reason it's invisible during authoring is the `formaViewMode !== 'map2d'` gate (user is always on the 2D map, Cesium off). So this is a DISCOVERABILITY/architecture problem, not a render bug. **Supersedes** the "site-view enabler button" idea AND the narrow "3D-Site-on-right"; **absorbs L-405** (view-mode switcher → the registry-driven pane view-picker). | View-system / UI-shell (**C59-MULTI-PANE-VIEW-SYSTEM — NEW, authored this pass** [governs]; **C06 §7** pane z-layering + **C06 §8** view-hosting pointer; **C04** single rAF/P3; **C12/C19/C57/C58** the site/parcel/envelope views). Spec: `SPEC-MULTI-PANE-VIEW-SYSTEM.md` (design + phased plan). Subsumes historical "Contract 17 §4 split view". | **OPEN — design-first landed (C59 + SPEC + C06 §8 amendment + Phase 1a pure core `paneViewModel.ts` + 10 tests + `CesiumViewport.reflowContainer()`); live PaneHost wiring phased (1b 3D-Site-in-either-pane → 2 registry switcher (L-405) → 3 full swap + N-up + per-pane camera → 4 consolidation). NOT Fixed until the founder live-verifies each live phase. UNASSIGNED / TBD.** | route -> view-system / UI-shell (C06/C59) + geospatial (C12/C19) queue. |
| L-411 | pre-launch evidence 2026-07-17 (§B5, borderline finding) | **[P2 - AUDIT/COMPLIANCE] No durable per-user audit trail — the command log persists `user_id` but is purged after 24h (`server.js:578-596`), and `TemporalGraph` keys on `sessionId` (not a stable userId) → durable per-element edit attribution over time is partial.** NEW. Version rows keep `created_by` (`server.js:3434`) but that is coarse (whole-version), not per-edit. | Audit / compliance (C23 provenance; C08 §8). Files: `server.js:558/:578-596/:3434`; TemporalGraph. | **OPEN — UNASSIGNED / TBD.** Decide a durable audit-trail policy (retention + stable userId keying) — ties GDPR/DSAR (L-345) + the compliance pillar. | route -> compliance / infra queue. |
| L-413 | architectural review 2026-07-18 (L-412 envelope-fix follow-up) | **[P1 debt] Composition-root: thread the real runtime into the site subsystem; retire the `runtime ?? window.runtime` fallback.** An architectural review of the L-412 envelope-visible fix (`5fe0fa07`) found the fix is correct and safe (converges the emitter/subscriber onto one bus — no wrong-bus / double-subscribe) BUT patches a transitional smell rather than the root cause. **Root cause:** `apps/editor/src/engine/initUI.ts:2820` deliberately passes `createMainLayout(props, null)` (a "Phase B.2 / S73-WIRE" migration gate — threading the real runtime there would prematurely activate ~30 half-migrated child code paths), so `mountGISArea`'s captured runtime is null and site code reaches for the typed global `window.runtime`. **Sound fix (scoped, low-risk):** inject the real runtime into the site subsystem (e.g. `mountGISArea(props, {runtime})` or a `setSiteRuntime(runtime)` post-construction injector) and retire the window fallback for the site path, WITHOUT flipping the global null (which would activate the gated Phase-C child paths). **Sub-items:** (a) inject runtime into the site subsystem + retire `resolveFormaEvents`/`getFormaBoundary` window fallback; (b) route the latent same-class captured-null sites through the one shared accessor — `GISAreaLayout.ts:118`, `:1160`, `:2974` (site-location reads) and the 6 toast emits (`:186, :1890, :2986, :2994, :2997, :3021`); (c) wire the forma live-update disposer (`window.pryzmDisposeFormaLiveUpdate`, `GISAreaLayout.ts:~2902`) into a real pane-unmount hook (listeners currently persist for the session / orphan on re-mount). Terminal state = Phase-C runtime threading (`createMainLayout(props, runtime)`). NOT launch-blocking; correctness verified. | View-system / composition-root (**P1** single composition root; **C02** composition root & boot; **C59 §invariant-7**). Follow-up to **L-412** (`5fe0fa07`). | **OPEN — P1 (architectural debt / follow-up to L-412). UNASSIGNED / TBD.** | route -> view-system / composition-root (C02/C59) queue. |
| L-414 | founder (2026-07-18, during buildable-envelope Cesium-render work) | **[P3 - SPIKE / RESEARCH, QUEUED — NOT priority] Interrogate & manipulate BIM / IFC DATA inside the Cesium geospatial view.** Founder verbatim intent: *"Keep this as a SPIKE — but if in the future we want to manipulate / interrogate the DATA in the Cesium views, we should look into this. Check how the data is structured in PRYZM and how IFC data in Cesium could adapt to a super-performant and flexible process. This should be QUEUED — not priority."* Today the Cesium / Forma 3D Site renders BIM massing + the buildable envelope but is a **passive context / drape surface**: the BIM scene is baked into the view as a **whole-scene glTF `Cesium.Model` blob** (`CesiumViewport.ts` `realModelOnGlobe`/`realModelOnForma`, `:7758–7905`) with **no per-element feature IDs** — `scene.pick` (`:1707`) resolves the *whole model primitive* (`instanceof Cesium.Model`, `:1712`), so nothing in it is selectable/queryable at the element level. Making it interactive would need per-feature metadata in the geospatial payload (3D-Tiles + `EXT_mesh_features`/`EXT_structural_metadata`) + feature-ID picking + a selection bridge to Inspect/Data — a cross-layer change (BIM data model + IFC/ThatOpen + Cesium render + the bridge + perf). SPIKE STUB: `docs/03-execution/spikes/spike-bim-ifc-data-interrogation-in-cesium.md` (research NOT yet done). LOGGING ONLY. | SPIKE / geospatial data-surface. **COVERAGE GAP: no contract governs interactive BIM/IFC data interrogation in the Cesium/geospatial view** — **C27** (BIM 3.0 Inspect) + **C28** (Data panel/Pset) cover BIM-data interrogation but only on the editor canvas; **C55 §1.2** (analytical layers DRAPE, never BIM — the Cesium surface is framed context-only); **C12** (geospatial coordinate substrate); IFC-data angle **C25/C26** + ThatOpen fragments (`packages/schemas/src/ifc/IfcElementMeta.ts`, `packages/picking/src/gpu-pick.ts`); "super-performant" ties **C10**; render **C04**; BIM data structure **C03**. Not a present violation — a **forward tension** with C55's context-only stance (logged to `MISSING-CONTRACTS-AUDIT-2026-06-01.md`). Spike stub: `spike-bim-ifc-data-interrogation-in-cesium.md`. | **OPEN — SPIKE (QUEUED, NOT priority). UNASSIGNED / TBD. FOUNDER DECISION (2026-07-18) — DIRECTION CHOSEN, DEFERRED:** the founder resolved the direction — *"it would be good to queue this for the future — not priority but definitely nice to have."* So the geospatial view **SHOULD eventually become a first-class interactive BIM/IFC surface** (a wanted "nice to have"), NOT stay permanently context-only — but it is explicitly **QUEUED (not-priority, post-September)**. Actionable meaning: (1) C55 stays as-written (context-only) until a governing contract lands; (2) when this surfaces, the FIRST deliverable is that contract (C55 §1.2 amendment + C27/C28 extension, or a new geospatial-interrogation contract) BEFORE any code; (3) no implementation now. | route -> geospatial / context-engine (C12/C55/CesiumViewport) queue, with data-platform (C03/C27/C28) + interop/IFC (C25/C26 + ThatOpen) as required collaborators. |
| L-415 | founder live test 2026-07-18 (Copenhagen) | **[P2 - UX] Parcel-SELECT silently no-ops in jurisdictions without a wired `ParcelProvider` (everywhere except Spain/Catastro) — the user clicks a plot and nothing happens, with no guidance.** Founder verbatim: *"Regarding Copenhagen I was not able to select the parcel properly even."* Root: parcel-select is backed by a jurisdiction-specific cadastral adapter and only `CatastroParcelProvider` (Spain) is wired (`apps/editor/src/ui/site/parcel/ParcelProvider.ts:15-20`); Denmark's parcel geometry is the API-key-gated Datafordeler Matriklen WFS (unbuilt — **L-400b**), so a click in DK/CH/etc. resolves nothing and the "Click a plot to select its real cadastral parcel" prompt just sits there. This is a **discoverability/graceful-degradation gap**, NOT the missing DK adapter itself (that's L-400b). Fix: when no `ParcelProvider` covers the map's current jurisdiction, disable/relabel "Select parcel" and surface a toast — *"Cadastral parcel-select isn't available here yet — use Draw boundary instead"* — routing the user to the working DRAW path (which computes the envelope + real DK zoning). | Site / parcel-select UX (**C57** parcel-data layer; **C59** site-authoring shell). Files: `ParcelProvider.ts:15-20`, `CatastroParcelProvider.ts`, `SiteBoundaryMap2D.ts` (select-mode UI), `GISAreaLayout.ts`. Ties **L-400b** (the actual DK parcel adapter, Datafordeler key). | **OPEN — UNASSIGNED / TBD.** Jurisdiction-aware select-mode gating + guidance toast; the DRAW path already works everywhere. | route -> site / parcel-data (C57) queue. |
| L-416 | founder live test 2026-07-18 (Barcelona) | **[P2 - RENDER/UX] Barcelona draw→3D Site: the drawn plot commits + the envelope computes (facts card = 1,147 m²) but the paned 3D Site renders blank — camera never frames the plot.** NOT a dead render pipeline: the SAME build renders the Copenhagen 3D Site richly (context massing + a purple envelope prism visible), so the Cesium render + envelope-via-massing path work. The Barcelona case is a **framing/one-shot-fly miss** — `shouldFramePanedSiteOnUpdate` / `renderFormaMassing(true)` (§GLOBE-FIT-BUILDING `flyToBoundingSphere`) did not fire or the camera stayed at whole-city scale so the plot+envelope sit off-screen. Candidate: `getFormaOrigin()` (`GISAreaLayout.ts:1160`) reads the captured-null `runtime?.siteModelStore` with NO `window.runtime` fallback (unlike the L-412-fixed `getFormaBoundary`) — masked only by the `getCurrentSiteOrigin()` module-global branch; if that branch is empty in the onboarding draw flow, `renderFormaMassing` bails at the origin guard (`:1900`) → nothing paints while the facts card (separate cached-envelope read) still shows area. **Same class as L-413** (composition-root captured-null). NEEDS the `[gis][forma]` console lines (rendering-massing vs no-site-location vs framing) to confirm which. | Site / Forma render + composition-root (**C58** envelope; **C59** paned 3D Site; **C02/L-413** captured-null runtime). Files: `GISAreaLayout.ts:1153-1168` (`getFormaOrigin`), `:1894-1907` (render-massing origin guard), `:2799-2832` (`liveUpdateFormaMassing` framing). | **ROOT-CAUSED + FIXED (pending live verify) — `c95f5e10`.** NOT origin-bail (verified: `getCurrentSiteOrigin()` module-global populates the origin in onboarding, so `renderFormaMassing` does not bail). The real cause was the **one-shot framing guard**: `shouldFramePanedSiteOnUpdate` framed only on the FIRST commit ever (`!alreadyFramed`), so a new parcel at a different location never re-framed → its envelope+context rendered off-screen. Fix: centroid-distance re-frame (`ringCentroidXZ` + `DEFAULT_PANED_REFRAME_THRESHOLD_M=5`) — re-frames when the plot moves to a new location, holds the camera for in-place edits (no jitter). 20 pure tests green, tsc 0. | route -> site / view-system (C58/C59) queue. |
| L-417 | founder live test 2026-07-18 (jagged hand-drawn plot) | **[P3 - ROBUSTNESS] A very jagged hand-drawn boundary (many sub-metre edges) yields a DEGENERATE envelope ("Setbacks consume the whole parcel") — honest, but the inset could tolerate more.** Facts card correctly reports `status: degenerate` and gates the prism off (`resolveFormaEnvelope` returns null unless `status==='ok'`) — so this is HONEST reporting, NOT a render bug. **Evidence:** a repro over representative parcels (large rect, hexagon, mildly-jagged big block, CW winding, Barcelona quad) ALL returned `status:'ok'` with sensible insets (1925/892/872/1925/178 m²) — so the solver is robust for normal + mildly-jagged plots; only EXTREME sub-metre jaggedness (or self-intersection) collapses the per-edge inset. Improvement: simplify/regularise the boundary (drop near-collinear vertices / merge sub-threshold edges) BEFORE `insetPolygonPerEdge`, and/or fall back to a uniform min-setback buffer when per-edge collapses — so extreme hand-draws still yield a plausible envelope. | Zoning / geometry robustness (**C58 §3** inset; `packages/site-parcel-data/src/geometry/insetPolygon.ts`, `ZoningRulesEngine.ts`, `solveEstimated.ts`). Ties **L-403** (robust inset, already shipped). | **OPEN — P3 (robustness improvement; current behaviour is honest-degenerate, not broken). UNASSIGNED / TBD.** | route -> zoning / site-geometry (C58) queue. |
| L-418 | founder live test 2026-07-18 (select parcel from rendered 3D context) | **[P2 - RENDER/UX — the real "envelope not visible" root] The buildable-envelope prism IS drawn (`CesiumViewport.ts:3810`, verified) but is OCCLUDED by the un-cleared context building sitting on the selected plot — because the plot-clear only runs when context is PLACED, not when a parcel is committed against already-loaded context.** Founder verbatim: *"i chose a parcel from the 3d context data that has already rendered — but still cant see the envelope — also the parcel that i chose — the building from context data should disappear so that i can see the purple envelope — right?"* — **exactly right.** Mechanism: `renderFormaMassing` sets `committedParcelLonLat` (`CesiumViewport.ts:3159`), but the plot-clear filter (`plotClearSplit` → `partitionFootprintsByParcel`) is only applied inside `loadContextBuildings` when footprints are PLACED (`:5452`). In the select-from-context flow, context is placed FIRST (parcel null → 0 removed), then the parcel is selected — and nothing RE-FILTERS the already-placed context entities. So the opaque `#6600FF`-context white building stays on the plot and buries the translucent (α 0.34) purple envelope drawn at the same footprint. Fix: on a parcel commit (`renderFormaMassing` with a boundary), RE-APPLY the plot-clear to existing `pryzm-forma-context-building` entities (hide/remove those `footprintOnParcel`), not only on the next context load. Pure filter already exists + is unit-tested; the gap is the re-invocation on parcel-commit. | Site / Cesium context-clear (**C57/C58**; `CesiumViewport.ts:3159/5445-5540`, `contextBuildings.ts` `partitionFootprintsByParcel`). Ties **L-402c** (§PLOT-CLEAR-ENVELOPE, shipped for the load-time path). | **OPEN — P2. UNASSIGNED / TBD. The highest-impact fix for "envelope not visible" — the prism is already drawn; unblocking it = re-run plot-clear on parcel-commit.** | route -> site / geospatial (C57/C58) queue. |
| L-419 | founder live test 2026-07-18 | **[P3 - UX] 3D context buildings don't appear during parcel review/select — only after commit.** Context fetches/renders around the plot once a parcel is committed (or the 3D Site framed), not while the user is reviewing/selecting — so the right pane looks empty during review. Archistar-style expectation: neighbourhood context visible as you browse. Pre-load context at the location step. | Site / context timing (**C55/C57**; `loadContextBuildings`, `GISAreaLayout.ts`). | **OPEN — P3. UNASSIGNED / TBD.** Minor polish; not the envelope blocker. | route -> geospatial (C55) queue. |
| L-420 | founder live test 2026-07-18 (Barcelona/Copenhagen) | **[P2 - UX, HIGH-VALUE] The 60 s onboarding draw-watchdog HIJACKS an in-progress parcel select/review — force-commits a default 10×8 m plot then LOCKS it immutable (C19 §1.4), blocking the real selection/draw.** Founder live-traced: selected a real Catastro parcel (2579 m²), was reviewing; at 60 s the watchdog fired (`OnboardingStepController.ts:688` `fallbackDefaultRectToConfirm('watchdog')` → `createSiteFromRect` 10×8 at city-center, area 80 m²); the later manual draw was rejected (`parcel-already-set`). This silently sabotaged many test sessions. Fix: the watchdog must NOT fire while the user is actively in select/review/draw (only on a genuine no-activity hang), and/or must not commit an immutable default that blocks a subsequent real commit (allow replace). | Onboarding / site flow (**C57/C59**; `OnboardingStepController.ts:675-695` `armBoundaryCommitWait`, `:1988` `fallbackDefaultRectToConfirm`; C19 §1.4 immutability). | **OPEN — P2 (high-value UX; sabotages the core flow). UNASSIGNED / TBD.** | route -> onboarding / site (C57/C59) queue. |
| L-421 | founder live trace 2026-07-18 | **[P2 - RENDER] `createSiteFromRect` (watchdog-default + `pryzmCreateSiteFromRect` console path) commits a parcel boundary but does NOT compute/cache the buildable envelope — so the default-plot path shows `envelope present=n` (no prism).** The draw/select path computes via `dispatchParcelBoundary` → `computeAndCacheEstimatedEnvelope`; `createSiteFromRect` emits `site.parcel-boundary-set` without it. Fix: `createSiteFromRect` should compute + cache the envelope (same producer) so every commit path yields an envelope. | Site / envelope compute (**C58**; `createSiteFromRect` in `main`/site console, `siteDispatch.ts` `computeAndCacheEstimatedEnvelope`). | **OPEN — P2. UNASSIGNED / TBD.** | route -> site (C58) queue. |
| L-422 | founder live test 2026-07-18 | **[P2 - CONTEXT/RESILIENCE] Context buildings intermittently render 0 — the Overpass `/api/overpass` proxy returns 0 footprints when all mirrors 429 the Fly server IP under heavy demo testing.** Central Barcelona (dense) returned 0 while Copenhagen earlier returned 9,425 — a rate-limit, not a query bug. **FIXED (pending live verify) — `6bd06f5b`:** expanded `OVERPASS_MIRRORS` to 6 (added osm.ch / maps.mail.ru / openstreetmap.fr) + a 1.5 s pre-retry backoff on 429/504/network (`server/overpassProxy.js`); `backoffMs` injectable, 6 proxy tests green. Increases the odds context resolves before degrading to none; a durable fix is a rate-limit-free source (Overture/Mapbox) or pre-seeded city cache. | Context / infra (**C55**; `server/overpassProxy.js`, `contextBuildings.ts`). | **FIXED (mirror+backoff resilience) — pending live confirm; durable alt-source is a follow-up. UNASSIGNED for the alt-source.** | route -> geospatial / infra (C55) queue. |
| L-423 | founder live-traced 2026-07-18 | **[P1 - RENDER — the decisive envelope-visibility fix] The buildable-envelope prism draws (`present=y`) then VANISHES because `placeRealModelOnForma` exports+places an EMPTY GLB (0 walls → 180 bytes, 0 roots) which calls `clearFormaMassingEntitiesOnly()`, hiding the massing study — and the envelope prism lives among those massing entities.** So on a committed parcel with no authored building, the envelope was drawn then instantly hidden → blank 3D Site. **FIXED + LIVE — `19077180` (bundle main-WmEs1WzC.js):** guard `placeRealModelOnForma` (`GISAreaLayout.ts:~2286`) to skip real-model placement until an actual building (walls/slabs/roofs/stairs) exists — keep the massing + envelope study visible. **Founder confirmed live: "amazing — we have it!"** (envelope renders, 2,197 m²). | Site / Forma render (**C58** envelope; `GISAreaLayout.ts` `placeRealModelOnForma`; `CesiumViewport.ts` `clearFormaMassingEntitiesOnly`/`:3810` envelope draw). Completes the L-416/L-418 chain. | **FIXED + FOUNDER-CONFIRMED LIVE.** | route -> site (C58) queue. |
| L-424 | founder live test 2026-07-18 | **[P2 - FLOW] Onboarding "Not now — I'll design it myself" does NOT continue to the PRYZM BIM editor — it only disposed the overlay, stranding the user on the 2D-map + Cesium site-authoring split.** Founder: *"on continue doesnt really continue to pryzm view yet."* The Generate path transitions (`enterCanvasWithSitePlanUnderlay`); "Not now" (confirm/resi/office handlers) called bare `this.dispose()`. **FIXED (pending live verify) — `912443ad`:** all three "Not now" handlers now call `landInCanvasWithUnderlay()` — the SAME tested transition (close map → unmount site split → BIM 3D + plan view → frame) minus generation — so the user lands in the editor with the parcel boundary + buildable-envelope volume already drawn (`ParcelBoundarySceneRenderer`, 3D + plan) as design guides. Satisfies the founder's ask that both cadastral refs (boundary + purple envelope) land on the PRYZM 3D + plan views (boundary LINE + envelope already visible in both; only the fill slab stays hidden in pure-3D by design, A.21.D44). tsc 0. | Onboarding / view handoff (**C59**; `OnboardingStepController.ts:1082/1580/1938`, `enterCanvasWithSitePlan.ts`, `ParcelBoundarySceneRenderer.ts`). Enables **L-401** (design inside the envelope). | **FIXED (pending live verify).** | route -> onboarding / view-system (C59) queue. |
| L-425 | founder live test 2026-07-18 (Barcelona, envelope+context live) | **[P2 - RENDER] The parcel BOUNDARY outline was hidden in the PRYZM BIM 3D view — the founder needs both cadastral references (boundary + envelope) visible in the PRYZM 3D + plan views.** Founder: *"looking better [Forma context+envelope render] — just we need the boundaries in the pryzm views."* Root: the boundary LINE was tagged `isParcelBoundaryFill=true` (`ParcelBoundarySceneRenderer.ts:192`) so the `_applyParcelFillVisibilityForView` 3D gate hid it — a deliberate 2026-07-01 `§PARCEL-OUTLINE-3D-HIDE` ("confusing stray circle") that predates the envelope workflow. **FIXED (pending live verify) — `bdbc5c45`:** drop the fill flag from the LINE so the violet outline shows in every view (it's now a cadastral design reference the user authors within); the fill SLAB keeps its own flag + stays hidden in pure-3D (the A.21.D44 grey-shade artifact); envelope already shows in both. tsc 0. Enables **L-401** (design inside the envelope + boundary). | Site / BIM render (**C58/C59**; `ParcelBoundarySceneRenderer.ts:185-193`, `initScene.ts` `_applyParcelFillVisibilityForView`). | **FIXED (pending live verify).** | route -> site / view-system (C58/C59) queue. |
| L-401 | Pipeline B payoff (founder direction) | **[P1 - FEATURE] Design INSIDE the buildable envelope — compliant-by-construction.** The site→design payoff: generators must author the building within the C58 envelope (setbacks + height + FAR), not the raw lot. **SLICE 1 DONE (committed `8b514934`, HELD for deploy) — FOOTPRINT compliance:** shared `resolveBuildableFootprint`/`pickBuildableFootprint` (`siteDispatch.ts`) returns the envelope INSET ring (when `status:'ok'`, ≥3 pts) as the generation footprint, else the raw parcel (unchanged). Wired into all three from-boundary generators — apartment (`apartmentFromBoundary.ts`), house/office onboarding (`OnboardingStepController.readParcelFootprint`), residential (`residentialFromBoundary.ts`) — so a generated building sits at the setback line by construction. 5 pure tests (`buildableFootprint.test.ts`), tsc 0. **SLICE 2 (queued):** storey HEIGHT-CAP at `maxHeightM` (already plumbed through the helper) + FAR gross/net area readback; **SLICE 3:** the compliance "explain-why" report (L-402). | Site→design (**C58** envelope; **C11** element pipeline; generators). Files: `siteDispatch.ts` `resolveBuildableFootprint`, `apartmentFromBoundary.ts`, `OnboardingStepController.ts:2295`, `residentialFromBoundary.ts:198`. Depends on **L-424** (land in canvas) + **L-425** (boundary visible). | **SLICE 1 DONE (held for deploy). Slices 2–3 OPEN.** | route -> site / generation (C58/C11) queue. |
| L-426 | founder live test 2026-07-19 | **[P1 - RENDER] Parcel boundary + buildable envelope NEVER rendered in ANY PRYZM view — the cameras don't enable `EDITOR_LAYER`.** Founder: *"when we click 'I would do it myself' — no envelope is being seen on pryzm views"* (while the envelope card showed a valid 294 m²). Root: `ParcelBoundarySceneRenderer` puts the boundary outline + envelope volume on **`EDITOR_LAYER` (=1)**; the RC-B refactor (`ViewController.ts:1085-1101`) replaced `camera.layers.enableAll()` with EXPLICIT per-layer enables (to keep PLAN_SYMBOL ghosts off the WebGPU renderer) and **dropped EDITOR_LAYER** — so the 3D / elevation / section cameras never rendered those aids, regardless of L-425. **FIXED — `936204fa` (live `main-B_ISyRpg.js`):** explicit `layers.enable(EDITOR_LAYER)` in all three activations. | View-system (**C59**; `ViewController.ts` `_activate3DView`/elevation/section; `packages/scene-committer/SceneLayers.ts` EDITOR_LAYER=1; `ParcelBoundarySceneRenderer.ts`). Completes **L-425**; unblocks **L-401**. | **FIXED (pending live verify).** | route -> view-system (C59) queue. |
| L-427 | founder priority 2026-07-19 | **[P1 - CONTEXT QUALITY/PERF] 3D context quality too low + too slow — but the HIGH-QUALITY photoreal path is ALREADY BUILT and CI-wired; it is dormant only because no credential secret is set.** Founder: *"bringing a higher quality 3d contextual data into 3d site as a priority + speed and performance — it is taking too long."* Current fallback = flat OSM footprints fetched via Overpass (boxy extrusions, no roofs/trees; huge bbox fetches + rate-limited mirrors ⇒ slow / intermittently 0, see L-422). **Already implemented:** Google **Photorealistic 3D Tiles** via `createGooglePhotorealistic3DTileset` + a Cesium ion path (`CesiumViewport.ts:156-187`), `ARG VITE_CESIUM_TOKEN`/`ARG VITE_GOOGLE_MAPS_KEY` baked at build (`Dockerfile:96-106`), and both already passed as `--build-arg` from repo secrets (`deploy-fly.yml:128-129`). With neither set it logs `forcing Forma flat-ground (no photoreal globe)`. **ACTION (no code):** set repo secret `VITE_GOOGLE_MAPS_KEY` (Map Tiles API enabled) **or** `VITE_CESIUM_TOKEN` → next build bakes it → photoreal globe. 3D-Tiles LOD streaming fixes BOTH quality and speed (no mass Overpass fetch). **SECURITY:** `VITE_*` is inlined into the PUBLIC client bundle — the key is publicly visible and MUST be restricted (Google: HTTP-referrer → `pryzm.fly.dev` + Map Tiles API only; ion: scope token + allowed domains). Never paste the key in chat/transcript. | Geospatial context (**C55/C12**; `CesiumViewport.ts:156-187`, `Dockerfile:96-106`, `deploy-fly.yml:128-129`). Supersedes patching **L-422** as the durable fix; relates **L-404/L-374** (rich context tier). | **OPEN — BLOCKED ON FOUNDER CREDENTIAL (no code change needed). Restrict the key before setting it.** | route -> geospatial (C55) + founder action. |
| L-428 | founder live test 2026-07-19 | **[P1 - GENERATION] The batch generator honours the envelope for the OUTER SHELL only — SLABS and APARTMENT layouts overflow the buildable envelope.** Founder: *"the building will adapt to the boundary envelope which is great — but the engine of the Batch AI building generation is not ready for this — the walls outer are correct, but the slabs go out of the boundary and the apartments are also designed going out."* Evidence: massing rendered `area ≈ 1302 m²` while the envelope card reads `Buildable 1,035 m² footprint` — the built plate exceeds the compliant footprint. **L-401 slice 1 changed the FOOTPRINT SOURCE** (`resolveBuildableFootprint` → envelope inset) which the shell walls follow; but downstream stages (floor-plate slabs, apartment/room dissection, roof) derive their extent from a different basis (raw plate / bbox / expanded plate), so they escape the envelope. **MECHANISM CONFIRMED (code-traced):** `residentialBuildingOrchestrator.ts` `deriveLocalFrame()` (`:322-344`) de-rotates the footprint into a local frame and — per the `:228` note — runs **the WHOLE partition/packer in that axis-aligned local frame**, i.e. on the **AABB of the de-rotated polygon**, NOT on the polygon itself. For the historical input (a drawn axis-aligned/rotated RECTANGLE) AABB ≡ polygon, so it was always correct. A **buildable-envelope inset is an irregular/concave polygon** (founder's case: 12-vertex), whose AABB is strictly larger — so slabs + apartment cells fill the bounding rect and spill outside the envelope (1,035 m² envelope → 1,302 m² built). **The residential layout engine assumes a RECTANGULAR plate.** **CORRECTION (deeper trace — do NOT build a new clipper):** the packer is ALREADY polygon-aware and wired. `platePartition.ts` exposes `clipPolygon` (`:78`) with `clipRectToPolygon` (`:1801`), `decomposeToRects(clipPolygon)` (`:1662`), centre-inside rejection (`:439`) and a relaxed `minFill` for L-shaped plates (`:994`); and the orchestrator PASSES it — `partitionLevelPlate({ … clipPolygon: footprint })` (`residentialBuildingOrchestrator.ts:754`). So the AABB is only the packer's WORKING frame; cells are clipped back to `clipPolygon`. **Therefore the observed overflow is most likely a STALE-BUNDLE artifact:** the founder tested a PRE-L-401 build where `footprint` (hence `clipPolygon`) was the RAW PARCEL, so cells clipped to the parcel and overflowed the (inset) envelope. With L-401 `footprint` IS the envelope inset ⇒ `clipPolygon` IS the envelope ⇒ apartments should already comply. **NEXT ACTION: RETEST on `main-B_ISyRpg.js` (L-401) BEFORE any code.** If overflow persists, the remaining suspects are the stages that do NOT go through `partitionLevelPlate` — floor-plate SLABS and the ROOF — which must be clipped to the same polygon. Either way add a post-generate CONTAINMENT VALIDATOR (all element footprints ⊆ envelope, else report), mirroring §HOUSE-SHELL-CONTAIN-ALL-FLOORS (ADR-0097). NOTE: the same AABB assumption likely affects the apartment + office packers on non-rectangular plots. | Generation / compliance (**C58** envelope; **C11** element pipeline; `ResidentialBuildingController`, apartment dissection, slab/roof stages). Follows **L-401 slice 1**. | **OPEN — P1 (the compliance promise is only half-true until slabs+rooms are clipped). UNASSIGNED / TBD.** | route -> generation / site (C11/C58) queue. |
| L-429 | founder live test 2026-07-19 | **[P2 - CONTEXT] On the PHOTOREAL globe the EXISTING building on the plot is still present (baked into the Google 3D-Tiles mesh) so the proposed design is hidden inside it.** Founder: *"in cesium 3d tiles — the building is still present (3d globe), you cannot see it."* Unlike the OSM/Forma path (where §PLOT-CLEAR-ENVELOPE can hide a footprint entity, L-418), Google Photorealistic 3D Tiles are a single immutable textured mesh — an individual building CANNOT be deleted from it. Standard remedies: (a) **Cesium 3D-Tiles clipping planes / `ClippingPolygon`** over the parcel ring to cut a void in the tileset where the plot is (Cesium supports polygon clipping on tilesets), then place the design in the void; (b) lower tileset opacity / hide tiles near the plot; (c) offer a Forma (massing) toggle for design review. Recommend (a) — a parcel-shaped `ClippingPolygon` on the photoreal tileset is the clean, native answer. | Geospatial (**C55/C12**; `CesiumViewport` photoreal path, `createGooglePhotorealistic3DTileset`; ties L-418 plot-clear + L-427 photoreal). | **OPEN — P2. UNASSIGNED / TBD.** | route -> geospatial (C55) queue. |
| L-430 | founder 2026-07-19 (Pipeline B) | **[P1 - FRAME] PARCEL-derived PROJECT NORTH: author rotated to project north (orthogonal walls + correct normals) while the 3D Site and 3D Globe keep TRUE north.** Founder: *"the project on pryzm should be rotated to project north to work with orthogonal walls etc and normals properly, keeping the true north on 3d site and 3d globe — we worked a bit on this before, check."* **NOT a new mechanism — an EXTENSION of accepted architecture:** **ADR-0070** (project-north vs true-north authoring frame, Accepted 2026-06-11) defines the two frames; **ADR-0115** (underlay-defined project north + dual-north transform, Accepted 2026-07-02) IMPLEMENTED the first slice — the dual-north transform primitive (`apps/editor/src/ui/site/overlay/projectTrueNorth.ts`), underlay geolocation record, capture-to-model command path — and its own **§Remaining explicitly lists "plan-canvas gizmo bridge + GLOBE θ APPLICATION"**, which is precisely this ask. **THE NEW INCREMENT:** project north today is **UNDERLAY-defined** (from an imported plan); Pipeline B is **PARCEL-driven**, so θ must also be derivable from the committed PARCEL (principal/dominant edge). **REUSE, DO NOT REINVENT:** `principalAxisAngle()` + `deriveLocalFrame()` (`residentialBuildingOrchestrator.ts:322-344`) already compute a dominant-edge θ + de-rotated local frame for exactly this purpose, and the house/resi executors already carry θ. **Requirements:** (1) derive θ from the parcel (fallback: underlay, then identity); (2) authoring frame = project north (orthogonal walls, correct face normals); (3) 3D Site + Globe apply θ⁻¹ so geometry sits TRUE-north on the real world (ADR-0115 §Remaining "globe θ application"); (4) plan/elevation/sheets carry a north arrow distinguishing project vs true north (**C34** print/drawing standards, **C24** sheets); (5) **SOLAR MUST STAY TRUE-NORTH** — sun-path/sun-hours (**ADR-0074**, C21 climate) must never consume the rotated frame or every shadow study is wrong. **RISK (why this must be one coherent transform):** it spans authoring + geospatial + solar + sheets; per-subsystem ad-hoc rotations are how double-rotation defects appear. ADR-0115's dual-north transform is the SINGLE authority — extend it, do not add a parallel rotation. | Frame / geospatial (**ADR-0070**, **ADR-0115 §Remaining** [governs], **C19** site+parcel, **C58** envelope frame, **C34** north arrow, **C24** sheets, **ADR-0074/C21** solar true-north invariant). Primitive: `projectTrueNorth.ts`; θ source: `principalAxisAngle`/`deriveLocalFrame`. | **OPEN — P1. UNASSIGNED / TBD. Implement as an ADR-0115 continuation (amend that ADR), NOT a new frame system.** | route -> geospatial / frame (ADR-0115) + generation queue. |
| L-431 | founder 2026-07-19 (Pipeline B) | **[P2 - VIEWS] The buildable envelope + parcel outline must be visible in PLAN and ELEVATION views, not just 3D.** Founder: *"in plan view and elevation the envelope should also be visible."* Same root cause as **L-426**: both live on `EDITOR_LAYER`, which the view activations never enabled. **SLICE 1 DONE (this pass):** L-426 enabled EDITOR_LAYER for 3D/elevation/section; **plan was still missing** — now enabled in `_activateFloorPlanView` (`ViewController.ts:~1405`), so the parcel outline + envelope render in the three.js PLAN view too (the `_applyParcelFillVisibilityForView` gate still suppresses the translucent parcel FILL slab; only crisp outline + envelope show). tsc 0; EDITOR_LAYER now enabled consistently across all 5 activations. **SLICE 2 (REMAINING):** the Canvas2D **SplitView plan pane (SVP)** renders a PROJECTION of BIM elements, not the three.js scene — the parcel ring + envelope inset are site context, not BIM elements, so they do not project and the SVP pane still omits them. Needs them added to the plan projection as SITE-CONTEXT LINEWORK (dashed/reference weight per **C34**, never as model geometry), plus the same treatment for elevation marks. | Views (**C59** view system; **C34** print/drawing standards — context linework weight; **C24** sheets; **C58** envelope). Files: `ViewController.ts` (`_activateFloorPlanView`, 5 EDITOR_LAYER sites), `ParcelBoundarySceneRenderer.ts`, SVP plan projection. Extends **L-425**/**L-426**. | **SLICE 1 DONE (three.js plan view). SLICE 2 OPEN (Canvas2D SVP plan projection + elevation marks). UNASSIGNED / TBD.** | route -> view-system (C59/C34) queue. |
| _next_ | | _append here_ | | | |

## Pipeline A — compliance-authoring: sub-task breakdown (transcribed from `SEPTEMBER-READINESS-MASTER-PROGRAM-PLAN.md` §8)

Parents stay **OPEN** (rows L-393, L-398–L-402 above); these are the schedulable sub-items. One-line scope + contract map each, verbatim from the program plan §8.

- **L-398** (envelope solver) — parent OPEN:
  - **L-398a** — Add Turf dep (lockfile-synced) + L0 schemas `BuildableEnvelope` / `JurisdictionZoningContract` / `ZoningRecord`, P5-pure, Zod round-trip. *(C58, C57; P5)*
  - **L-398b** — `ZoningRulesEngine` core: parcel ⊖ setbacks (Turf negative buffer) → inset polygon + `area×maxHeight` volume; pure L2, deterministic. *(C58; C19 §1.4)*
  - **L-398c** — Two-fidelity resolution (`structured` / `estimated-ruleset` / `none`) + per-field provenance + confidence labels. *(C58; L-373)*
  - **L-398d** — Wire engine → `site.updateZoning` command (P6 bus); no direct store write. *(C19; P6)*
- **L-399** (zoning providers + rule packs) — parent OPEN:
  - **L-399a** — `DkZoningProvider` (Plandata anonymous WFS; `bebyggelsesprocent`/`maksbygningshoejde`/`maksantaletager`/`anvendelse`). *(C58)*
  - **L-399b** — Curated `da-*` `JurisdictionZoningContract` pack for PDF-gap fill (mirror `rules/programRules.ts`). *(C58; L-373)*
  - **L-399c** — `MucZoningProvider` (Catalonia MUC zone class) + curated `es-barcelona` pack. **SEPTEMBER — Track J fan-out, shipped "estimated — flagged"** (C58 confidence label; numeric pack human-validated post-launch). *(C58; L-373)*
  - **L-399d** — `ChZoningProvider` (cantonal structural adapter) + curated `ch-*` pack (pilot cantons). **SEPTEMBER — Track J fan-out, shipped "estimated — flagged"** (26-canton validation is the post-launch fidelity-hardening backlog). *(C58; L-373)*
- **L-400** (parcel data layer) — parent OPEN:
  - **L-400a** — Lift shipped Catastro provider to `@pryzm/site-parcel-data` (L2) + `ParcelProvider` registry. *(C57; C12)*
  - **L-400b** — `DkParcelProvider` (Matriklen via server-side Datafordeler key in the proxy clone). *(C57)*
  - **L-400c** — `ChParcelProvider` structural adapter (cantonal cadastre; parcel geometry only — estimated zoning rides L-399d). **SEPTEMBER — Track J fan-out.** *(C57)*
- **L-401** (envelope → authoring bridge) — parent OPEN:
  - **L-401a** — Thread inset polygon + maxHeight into `generateResidentialFromBoundary` + the typology-pipeline constraints stage (`joinProgramRulesWithRegulatory`) as generation bounds. *(C19 §1.6; C50)*
  - **L-401b** — Permitted-use (`anvendelse`) → typology-brief seed (brief-capture stage). *(C50; briefSchema)*
  - **L-401c** — Envelope-containment `SpatialValidator` in the validators stage (`runValidators` → `ValidationReport`): generated footprint ⊂ inset ∧ height ≤ cap; C53 slider-as-intent preserved (no parallel knob). *(C53; C19 §1.6)*
- **L-402** (report + render) — parent OPEN:
  - **L-402a** — Explain-why compliance report artefact (envelope + rule refs + ordinance links + confidence chips); reuse the pure-SVG-string overlay pattern `buildPlanGraphOverlaySvg` (`apps/editor/src/ui/apartment-layout/layoutBubbleGraph.ts`). *(SPEC-COMPLIANCE-REPORT NEW; C23)*
  - **L-402b** — 3D translucent max-height envelope volume via the Forma massing renderer (`pryzmRenderFormaMassing`, `apps/editor/src/ui/geospatial/`, same ENU frame), `#6600FF`, P2-safe. *(C04/C18)*
  - **L-402c** — CI fidelity-label gate (`estimated-ruleset` never authoritative); mirror `check-windcfd-beta-label.ts`. *(L-373)*
- **L-393** (interop round-trip — Pipeline A Track I) — parent OPEN:
  - **L-393a** — IFC/DXF/Rhino round-trip harness (import→export→re-import geometry-delta compare). *(C25/C26/C32/C33)*
  - **L-393b** — Adversarial malformed-file behaviour (crash vs silent-drop vs graceful reject). *(L-393)*

## PROCESS LESSONS — PAID FOR IN PRODUCTION, 2026-07-13

These are not reflections. Each one cost the founder a broken build, a wasted agent, or a
bug I "fixed" in the wrong place. They are recorded here because the next person (or the
next me) will otherwise pay for them again.

### L-A — A CALLER AND ITS CALLEE ARE **ONE** CHANGE. IF YOU SPLIT THEM, YOU SHIP A CRASH.

Happened **TWICE in one day**, and the second time was in *production*:

1. `PlanViewCanvas` (the CALLER of `setViewProjection`) shipped; `PlanViewAnnotationRenderer`
   (which DEFINES it) did not. Every mousedown, mouseup and dblclick threw
   `TypeError: setViewProjection is not a function`, in plan **and** elevation. The founder:
   *"I am struggling to open elevation views — before it was working perfectly."*
   **Cause: I ran `git restore --staged` on the renderer to keep a docs commit clean — which
   was correct — and then a LATER commit shipped its caller.**
2. The elevation auto-dimension ENGINE shipped (`fb9eb26f`); its BUTTON (`AIPanel.ts`) did
   not, because a scoped `git add` missed it. The founder was left looking at a pill that
   still said *"Auto-dimension **plan**"* and reasonably concluded the feature was missing.

**THE RULE: un-staging a file to keep a commit clean is only safe if NOTHING ELSE IN THAT
COMMIT DEPENDS ON IT.** Before pushing a scoped commit, ask: *does anything I am shipping
CALL something I am not?* A feature and its entry point are one change too — an engine
with no button is not shipped, it is hidden.

### L-B — VERIFY AT THE **OUTCOME**, NOT AT THE **SEAM**.

`L-246` computed a perfect plane-vs-solid wall section for every wall, on every projection,
and **threw it away** — the plan branch never read the map it was written into. It emitted
correct geometry that reached **nothing**, and I declared it fixed.

That one dropped array was the common cause of **three separately-reported defects**: no
poché, no CUT lineweight, and the founder's *"2 occluder(s), 0/1532 segments removed"*.
Two later fixes (L-260 B/C) were **algorithmically correct and sat downstream of an empty
layer**.

**THE RULE: a builder emitting geometry is not evidence. Prove the geometry REACHES THE VIEW.**

### L-C — WHEN A TEST IS RED, ASK: *IS THE TEST WRONG, OR IS THE PRODUCT WRONG?*

Both answers happened today, and getting the question backwards would have destroyed working
code **both times**:

* My `pocheAllSolids` guard forbade *every* `layerName === 'A-WALL'`. It went red — and the
  **PRODUCT was right**: `_suppressWallOpeningSeams` and `_suppressPlanViewOpeningLines` are
  wall-only **correctly**, because openings are hosted in walls and nowhere else (C15). A
  column has no openings. **The reflex to "make the test pass" would have deleted correct
  code.** The guard was narrowed to the CUT GATE, which must generalise, and left the
  opening behaviour alone, which must not.
* Conversely, `PickStrategyResolver`'s "healthy" fake returned an all-zero readback — which
  is exactly the broken-driver signature the real probe detects. **Product right, FAKE stale.**

### L-D — REPRODUCE, DO NOT CONFIRM. I WAS REFUTED ON MY OWN ROOT CAUSE **ELEVEN TIMES** TODAY.

L-239 · L-243 · L-246 · L-248 · L-250 (×2) · L-269 (×2) · L-271 · L-272 · L-273. Every one
was a bug I would otherwise have "fixed" in the wrong place. The refutations that mattered
most:

* **"A batch is not an undo unit."** I asserted nested `runBatch` fragments undo. Measured:
  `CommandBus` pushes one ring entry **per dispatch** and never reads `batchCoordinator`, so
  `runBatch` is **undo-neutral** — it could not fragment undo because it never merged
  anything. The real bug (silently dropped GUARDS) survived and was fixed. Now C16 §8.6.
* **"Eviction exhausted" never evicted a byte.** The evictor scans a key family a previous
  migration had already emptied — so it looped **zero times** and printed "exhausted".
  *The safety path silently never ran* — the same shape as L-249.
* **The storage hog was not what I guessed.** I suspected legacy underlay rasters. It was
  `pryzm:ctxbld:*` — 1.56 MB of cached OSM footprints. **The diagnostic I shipped to stop
  people guessing refuted the person who shipped it.**

### L-E — THE MOST VALUABLE THING I DID ALL DAY WAS **REFUSE TO FIX SOMETHING**.

The founder said door parity was *still* wrong after L-260A. The reflex was to re-fix the
creation path. **I read it first — and it was already sound.** Re-fixing it would have burned
an agent on a working path and left the real defect standing: **untyped legacy records**
(`0.900 × 2.100` vs `0.926 × 2.040` — *a different door, drawn correctly, forever*, because
nothing backfills). That check is now a test so nobody re-fixes it (L-274).

### L-F — A CREATION-PATH FIX IS ONLY **HALF** A FIX WITHOUT A BACKFILL.

Seven creation-path bugs have been closed by converging the paths — and **every one left
behind the records the broken path had already written.** Those records are still in the
founder's project, still rendering as different elements.

### L-G — A GATE THAT DOES NOT **COVER** IS NOT A GATE.

I reported G0 closed. CI ran `--filter @pryzm/editor` — **the editor and nothing else**; 121
workspaces were invisible to it, and a truth-audit found a RED test in `packages/picking`
within ten minutes. The same shape as L-247, one level up. And **G10** pinned "one gesture =
one undo entry" while **nothing pinned it under NESTED batching** — green, and still
breakable.

### L-H — A WARNING IS NOT A GUARD.

`BatchCoordinator` detected an unsupported nested batch, **announced it**, and proceeded with
the guards **off**. Detect-warn-continue is not a third option; it is the bug wearing a
high-vis jacket.

---

---

## §3 Functional-area conformance

### 3.1 Camera & navigation — **NEEDS HARDENING**
- Surface: `packages/renderer/CameraController`, `packages/view-state/ViewController`,
  `packages/stores/CameraPositionService`, `packages/renderer-three/LTPENUCameraService`,
  `runtime-composer/buildCameraControllerSlot`. Constraints armed (`minDist=0.2, maxDist=10000`, polar clamp).
- Issues: **L-02** (heavy-scene orbit slow — shadow-ceiling bypass + instancing + no nav-LOD), **L-05** (WebGL2
  ghost-on-rotate). **L-25** (WebGPU black 3D on rapid wall-create + mouse motion — `ShadowDepthTexture` destroyed
  mid-submit by the `§PERF-NAV-LOD` `castShadow` toggle) **FIXED** via `§FIX-SHADOW-MIDSUBMIT-DESTROY`: the nav lever
  now FREEZES the shadow map (`shadowMap.autoUpdate=false`) instead of clearing `castShadow`, so THREE never destroys
  the texture (no mid-submit destroy); restore is debounced; the heavy-tier drop-the-pass gate is decoupled from the
  transient nav gate. Zoom-fit/zoom-selected handlers exist (`§C-B1`).
- **N/V**: pan/zoom/orbit smoothness on mid projects; camera state persistence per view; frame-on-view-switch.
- Contract: C04 (rendering/scheduling).

### 3.2 Element creation — **MOSTLY OK, one BROKEN**
- Dual-write/bus-bridge paths verified for walls/curtain-walls/doors/windows/slabs/floors/roofs/stairs/columns/
  beams/plumbing/lighting (ADR-0098 §4, prior audit §3). Hardened door/window creation fallback (§DPT/§WPT).
- Issue: **L-09** wall-draw preview stutter (creation *interaction*, not the command).
- Contract: C11, C15.

### 3.3 Element editing — move / rotate / dimensions / materials / hosting — **CORE GAPS**
- **Movement**: wall=`UpdateWallBaselineCommand`, door/window=offset, furniture=params — non-uniform (F8).
  **L-01** host-wall move freeze FIXED (ADR-0099); **L-06** per-move storm amplifier in flight (Q6).
- **Rotation**: **BROKEN/absent** — no `Rotate*Command` (L-07/F5).
  Partial: **placement-time** rotation shipped — Spacebar rotates the live placement preview
  +90° CW (`§FEAT-PLACEMENT-SPACEBAR-ROTATE`, ADR-0107) for one-click placeables (furniture 3D +
  plan, carousel GLB), with the chosen yaw carried through to the committed element. The general
  post-placement first-class Rotate command/gizmo (F5/Q7) remains OPEN.
- **Dimensions**: OK coverage (`UpdateWall/Slab/Door/Window Dimensions/Height/Width/SillHeight`).
- **Materials**: uneven (L-08/F6) — structural/MEP/furnishings have no material command.
- **Hosting**: correct model (C15) — offset + dual-store; the perf failure (L-01) is fixed.
- Contract: C03, C11, C15, C16.

### 3.4 Selection — **BROKEN**
- `findSelectableRoot` walk-up correct; **L-04** gpu-pick chooses wrong candidate under density (Q4 agent).
- `§SELECT-STUCK-STATE-SELFHEAL` exists (escape un-wedges) — a symptom of the edit-storm (L-06), not a fix.
- Contract: C15 §11/§12, C06.

### 3.5 Views — plan / 3D / elevation / section / sheets / **view creation** — **VERIFIED (2 fixes shipped)**
- Commands present: `CreateViewDefinitionCommand`, `UpdateViewDefinitionCommand`, `DeleteViewDefinitionCommand`,
  `CreateViewTemplateCommand`/`Update`/`Delete`, `UpdateViewportScaleCommand`; `DefaultViewsManager` guarantees a
  3D + Ground-Floor plan on every project; split-view plan (Canvas2D) auto-opens; `EdgeProjectorService` +
  `HiddenLineRemoval` + `NativeElementMeshExporter` drive plan projection.
- **Traced end-to-end from the UI (§3.5 verdicts — replaces the N/V cells):**

  | Flow | Verdict | Evidence / fix |
  |---|---|---|
  | **View list / browser** (all types listed, grouped, badges, active pill) | **OK** | `ViewsRailPanel.ts` — `getByType()` per group; `vd:view-*` refresh subscriptions. |
  | **Create new plan / structural-plan view** (form + level picker + validation) | **OK** | `ViewsRailPanel._executeCreateView` → `view.createDefinition` → `CreateViewDefinitionCommand`. Level required + validated. |
  | **Create new 3D / section / elevation / detail / … view** (name form) | **OK** | Same path; `CreateViewDefinitionCommand.canExecute` whitelists all 12 `viewType`s. |
  | **Switch / activate view** (single-click; re-entry guard) | **OK** | `ViewsRailPanel._onActivateView` → `viewController.setActiveViewDefinitionId` + OBC-mode activate. Section/elevation route to Canvas2D `PlanViewManager`. |
  | **Duplicate / delete view** | **OK** | `view.createDefinition` (clone spatial) / `view.deleteDefinition`. |
  | **View range editing** | **OK** | `ViewPropertiesPanel._fireSetViewRange` → `view.setRange` (plugin-view `SetViewRangeHandler`, store-backed, undoable). Read by `EdgeProjectorService.resolveClipRange` (`spatial.viewRange.near/farOffset`). |
  | **View crop editing** | **OK** | `ViewPropertiesPanel._fireSetViewCrop` → `view.setCrop`. Read by `EdgeProjectorService` (`crop.region` / `crop.farClip`). |
  | **Elevation MARK placement** (4-dir click) | **OK** | `ElevationPlanToolHandler` → `elevation.create` → `CreateElevationMarkCommand` (creates elevation ViewDefinition + `elevation-mark` annotation with `linkedViewId`). |
  | **Section MARK placement** (2-click cut line) | **BROKEN → FIXED** | `SectionPlanToolHandler` fired `section.create`, whose bus key is owned by plugin-section-view's **geometry** handler (payload `{ line:{a,b,lookDepth} }`) — the tool's `{ sectionViewId, cutPointA, … }` was rejected at `canExecute` and swallowed by the caller `.catch()`, so **nothing was created**. Fixed **§FIX-SECTION-MARK-CREATE**: new distinct bus key `section.mark.create` (bridge in `initBusHandlers.ts`) → `CreateSectionMarkCommand`; tool repointed. |
  | **Click a placed section / elevation mark → navigate to its view** | **GAP → FIXED** | `PlanViewInteraction._onClick` only *selected* the mark; it never read `parameters.linkedViewId`. Fixed **§FIX-MARK-NAVIGATE**: on hit of a `section-mark`/`elevation-mark`, `_navigateToLinkedView()` activates the linked view (Revit click-to-navigate). |
  | **Viewport scale on a sheet** | **OK (unchanged)** | `UpdateViewportScaleCommand` + `sheet.addViewport` via `ViewsRailPanel` context-menu. |

- **Note (out of G8 lane, flag only):** the *3D* `SectionMarkTool` (plugins/annotations) additionally fires a generic
  `annotation.create` alongside `CreateSectionMarkCommand`, which can leave a redundant generic annotation. Belongs to
  the annotations subsystem (G9) — not touched here.
- The projection cost is the L-06 storm's biggest consumer.
- Contract: C04, C06, C09. Tests: `plugins/annotations/__tests__/section-elevation-mark.test.ts` (6, pins the
  mark-create + `linkedViewId` contract that both fixes depend on).

### 3.6 Annotations & dimensions — **OK (was GAP; fixed §G9-PERSIST 2026-07-02)**
- Commands: `CreateAnnotationCommand`/`UpdateAnnotation`/`DeleteAnnotation` (seen live), `UpdateElementMarkCommand`;
  `AnnotationManager` + `OBCAnnotationAdapter`.
- **OBC verdict — resolved, not a blocker.** The "linear/angle/slope annotations not present in this OBC build —
  skipping" logs are BENIGN: `OBCAnnotationAdapter` is a *secondary* front-end that gracefully no-ops when the OBC
  classes are absent. Every dimension/annotation type has a PRYZM-native tool
  (`plugins/annotations/src/tools/*`) that does NOT depend on OBC. The absent-OBC path was never the primary path.
- **Placing a linear dimension — OK.** UI button (`AnnotationRailPanel` → `toolManager.activateLinearDimAnnotation`)
  and the `D`+`I` shortcut both arm `LinearDimensionAnnotationTool`, whose commit dispatches
  `CreateAnnotationCommand` via `commandManager` into the subsystem `annotationStore` that the render layer reads and
  `ProjectSerializer` persists. Verified end-to-end. String/chained dims OK.
- **Placing a text annotation / tag / other dimension types — was BROKEN, now OK.** ROOT CAUSE found: 13 of 14
  native tools (`TextNote`, `ElementTag`, `Door/Window/Level` tags, `GridBubble`, `Angular/Slope/Radius/Diameter`
  dims, `SpotElevation`, `Keynote`, `RevisionCloud`) built the full `AnnotationElement` but only fired the bus
  telemetry `annotation.create` `{id,viewId,kind}` — which writes a DIFFERENT anchor-keyed Zustand store (per
  `initBusHandlers.ts` §P3.5-AN) and carries no geometry. They never called `commandManager.execute` /
  `annotationStore.add`, so the annotation neither rendered nor persisted. Only `LinearDimensionAnnotationTool`
  (and the mark/scale/north/matchline/datum tools) were wired correctly. **Fix (§G9-PERSIST):** new shared helper
  `plugins/annotations/src/tools/persistAnnotation.ts` dispatches `CreateAnnotationCommand` through
  `window.commandManager` (the same authoritative path LinearDim uses → store write + undo/redo); each of the 13
  tools now calls it before the bus telemetry.
- **Editing / deleting — OK.** `UpdateAnnotationCommand` / `DeleteAnnotationCommand` mutate the subsystem store with
  full undo snapshots; dimension edits route through the shared property panel (`AnnotationManager` §ANN-SEL);
  room-tag drag routes the legacy `UPDATE_ANNOTATION` through `commandManager` (initBusHandlers BUG-ANNO-DRAG fix).
- **Persistence + plan re-projection — OK.** `annotationStore.serialize()`/`deserialize()` are wired both sides of
  `ProjectSerializer`/`ProjectLoader` (`annotations` snapshot slice). Plan re-projection is reactive:
  `AnnotationRenderLayer` subscribes to `store.onChange` and re-reads `getByView(activeViewId)` through the live
  camera matrix each frame.
- **Room-tag auto-populate (`RoomTagAutoPopulator`) — OK.** Idempotent create/refresh/delete of `room-tag`
  annotations via `Create/Update/DeleteAnnotationCommand`; no-op when tags already match (verified live in house/apt
  gen).
- **Tests:** `plugins/annotations/__tests__/persist-annotation.test.ts` (9 cases) covers the helper, the
  create/update/delete + undo lifecycle, view-scoped read (render source), and serialize→deserialize round-trip.
- Contract: C06, C03.

### 3.7 Undo / redo — **OK (verify at scale)**
- History/redo stacks, per-command snapshot scope, remote/PROJECT_LOAD exclusions verified (prior audit §6).
- **N/V**: undo of a wall-move-with-openings after the ADR-0099 changes; undo during the L-06 storm.

### 3.8 Save / load & lifecycle — **BROKEN at scale**
- **L-03**: 30s false-timeout, autosave-during-load serialize (22.7MB), O(N) clear-on-switch, 940 degenerate RBLs
  (Q3 agent, C13/C05). Small projects load fine.
- Contract: C05, C13.

### 3.9 Performance & responsiveness (cross-cutting) — **BROKEN**
- **L-02** heavy nav, **L-06** edit storm, **L-09** preview stutter, **L-03** load. The shadow-ceiling bypass
  (12,737 casters at `shadows=standard`) and instancing gaps are the render-side; the redetect/plan storm is the
  interaction-side. Contract: C04, C10.
- **L-25 FIXED** (`§FIX-SHADOW-MIDSUBMIT-DESTROY`) — the `§PERF-NAV-LOD` shadow lever no longer destroys the
  `ShadowDepthTexture` mid-submit during rapid mouse motion (WebGPU device-loss → black 3D). Nav-LOD now freezes the
  shadow map (`shadowMap.autoUpdate=false`, texture kept alive) via `RenderPipelineManager.setShadowPassSuppressed()`
  with a debounced restore; prevention, not just recovery (ViewportCrashGuard remains the fallback). Honours
  `§SHADOW-DEVICE-LOSS-FIX` (no GPU texture is ever `.destroy()`-ed within the frame it is submitted).

---

## §4 Launch-gate summary (what MUST be green for v1)

| Gate | Requirement | State |
|---|---|---|
| G1 | No freeze on host-wall move (any element count) | F1 shipped; Q6 amplifier in flight |
| G2 | No freeze/fail opening a heavy saved project | Q3 in flight |
| G3 | Smooth camera navigation on a full building | Q2 in flight |
| G4 | Correct click-selection at any density | Q4 in flight |
| G5 | Smooth wall/element draw preview | Q8 in flight |
| G6 | No ghost/trailing on rotate (both backends) | Q5 queued |
| G7 | Move + rotate + material editable for every visible element | Q7 queued (F5/F6) |
| G8 | Create/manage plan+3D+section+elevation views from UI | GREEN — §3.5 verified end-to-end; §FIX-SECTION-MARK-CREATE (section-mark tool no longer no-ops) + §FIX-MARK-NAVIGATE (mark click-to-navigate) shipped |
| G9 | Place/edit annotations + dimensions | GREEN — native path verified; §G9-PERSIST fixed 13 bus-only tools (§3.6) |
| G10 | Undo/redo sound across all of the above | §3.7 verify at scale |

See the companion plan for the phased path to green.


---

## STATUS RECONCILIATION — 2026-07-04 (session close)

**Founder-reported this session (L-58 … L-93): ALL fixed & deployed** except the 3 the founder cancelled (L-18, L-19, L-48 HELD). Doc-sync fix applied: L-62/L-64/L-66/L-67/L-74 were shipped in code but were mislabeled BROKEN/REGRESSION — now marked FIXED.

**Earlier-wave SHIPPED (verified live on pryzm.fly.dev):** L-16, L-20, L-21, L-25, L-26, L-27, L-28, L-29, L-30, L-31, L-32, L-33, L-34, L-35, L-36, L-37, L-38, L-39, L-40, L-41, L-42, L-43, L-44, L-45, L-46, L-47, L-49, L-50, L-51, L-52, L-55, L-56, L-57, plus L-08/Q7 materials, L-12, L-13, L-15, L-17, L-22.

**HELD (user-cancelled):** L-18, L-19, L-24, L-48.

**Genuinely OPEN → IN FLIGHT now (dispatched 2026-07-04):** L-11 (real environment), L-14 (floor-finish default thickness), and a verify-then-fix pass over the original interaction gate items **L-04** (selection exact-pixel), **L-05** (WebGL2 ghost-on-rotate), **L-06** (per-move redetect/reproject storm — likely already covered by L-65/L-89/L-90), **L-09** (wall-draw preview responsiveness).

**OPEN / SCHEDULED (high-risk wall debt, not yet started):** L-53 (collab CRDT baseline conflict — needs shared CRDT id normalization first), L-54 (dual junction-resolver retirement / `_flush` simplification — high-risk refactor).

**OPEN / needs heavy-scene repro (original perf gates):** L-02 (heavy-tower nav perf), L-03 (heavy-project load) — require a large test project to validate; flag for a dedicated perf pass.

**Non-blocking engineering follow-ups:** CSG path-convergence for a door hard against a corner (`§WALL-SINGLE-VOLUME-CSG`); align root vitest config so `packages/**/*.test.ts` regression suites run in CI.

---

## SESSION HANDOFF — 2026-07-13 (start here)

**Read this first. It is what the next session needs and nothing else.**

### The one insight that reframes the board

**L-253 (WebGPU invalid render pipeline) is very likely the root of L-250 (the wall+door "freeze").**
Geometry is backend-agnostic. The founder's freeze was CURED by swapping WebGL→WebGPU, and a wall's
baseline does not care who draws it — so the fault was never in the wall pipeline. That is why **all
five** L-250 geometry hypotheses came back clean *under measurement*:

- rebuild is **O(affected)**: 200 wall bodies → 4, 48 CSG extrudes → 0 (`WallMoveRebuildCost.measure.test.ts`)
- `WallJoinResolver.resolveLevel` is a **proven fixed point**, even on the §SELF-CLUSTER topology
  (`WallJoinResolver.hostedDoorLengthEditNoHang.test.ts` — written today)
- the flush no-progress signature is millimetre-rounded geometry, so it **cannot** be defeated

**ACTION: after the L-253 deploy, ask the founder to re-test the wall+door move. If it is gone, close
L-250 as a symptom of L-253 and say so out loud in the audit.**

### Immediately actionable, in order

1. **L-253 remainder** — the WebGPU MRT fix is SHIPPED (`873832c5`). The **WebGL2 sibling fault is NOT**:
   `GL_INVALID_OPERATION: glDrawElements: Mismatch between texture format and sampler type
   (signed/unsigned/float/shadow)`. Same disease, other backend. Also still open in the same log:
   `Destroyed texture [ShadowDepthTexture] used in a submit` (§SHADOW-DISPOSE-DEFER) and
   `ViewportCrashGuard: Cannot read properties of undefined (reading 'usedTimes')` — a material/texture
   disposed while still referenced.
2. **L-247 phase 0.5c** — red surface is **34 → 3**. The 3 remaining are `FormaSiteAnalysisControls`, and
   they are **PRODUCT-SUSPECT, NOT test rot**: the wind rose paints bars from a dataset that
   `climateStore.resolveSite(site.id)` **cannot find under the site's own id** — a KEYING MISMATCH, same
   family as L-249. **Do not edit the assertions.**
3. **L-247 phase 0.5d** — flip `test-unit` from `continue-on-error` to a required check once green.
4. **L-254 / L-255 / L-256** — window symbol, floor-finish plan modal, dimension conformance. All briefed.

### The pattern that keeps producing bugs — name it when you see it

**ONE element, TWO creation paths, and the plan path silently drops what the 3D path resolves.**
Six instances now: **L-239** (wall layers), **L-240** (floor finish inner face), **L-243** (stair config
scavenged from a global), **L-246** (the plan cut), **L-251** (the mitre), **L-255** (floor-finish modal).
The fix is always the same shape: **converge at the creation chokepoint (C11); never add a fifth branch.**

### Process lessons paid for in full today

- **A hang is invisible to a cost test.** L-234 closed on a measured *cost*; the founder still froze.
- **Verify at the OUTCOME, not the seam.** L-243's lesson, re-learned twice.
- **When a red test looks like rot, ask "is the TEST wrong, or is the PRODUCT wrong?"** Three of the five
  L-247 groups were the PRODUCT's fault — including **L-249**, a live production bug where climate normals
  could NEVER load.
- **I was refuted on my own root cause SIX times today** (L-239, L-243, L-246, L-248, L-250 ×2). Reproduce.
  Do not confirm.
