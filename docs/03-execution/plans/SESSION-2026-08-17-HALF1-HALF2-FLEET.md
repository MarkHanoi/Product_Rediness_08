# SESSION 2026-08-17 — verify `0f88bbc6`, deploy, then Half 1 + Half 2 by fleet

**Base:** `370aa0bd` · **Head at writing:** `a296069d` · **21 commits** · **9 agent lanes (A–I)**
**Deployed:** `370aa0bd` to https://pryzm.fly.dev, bundle proof **6/6**, `PROOF_RC=0`

> **Rule of this document:** every number below was GENERATED by running the instrument and reading
> its exit code from a FILE (§EXIT-CODE-THROUGH-A-PIPE). Where a lane reported a result, the
> orchestrator re-ran the gate on `main` before recording it. Claims that were NOT verified are
> marked as such, in the same sentence, rather than in a footnote.

---

## 0. The instruction that opened the session, and what it was worth

`0f88bbc6` shipped four stalled lanes' work **typechecked but never lane-verified**. The handoff
demanded verification before any new code. That was the correct call, and it paid twice:

- **Two of the "failures" were contention flakes.** `WallCreateJoinIntentCensus.measure` and
  `WallMoveRebuildCost.measure` failed while three suites ran concurrently; re-run ALONE they pass
  **8/8** and **2/2**. Believing the first reading would have sent someone chasing nothing.
- **Two were real**, and one of them was mis-attributed by its own author (see §2).

**Verification results at `370aa0bd`** — every exit code captured to file:

| Check | Result | RC |
|---|---|---|
| root `tsc` (`--max-old-space-size=8192`) | 6 errors, ALL in ONE untracked file | **2** |
| `@pryzm/geometry-slab` | 166/166 | 0 |
| `@pryzm/finish-host-tracker` | 51/51 | 0 |
| `@pryzm/core-app-model` | 882/882 | 0 |
| `@pryzm/room-topology` | 227/227 | 0 |
| `check:isolation` | intact | 0 |
| `test:server` | 613/613 | 0 |
| `@pryzm/geometry-wall` | 693/695 → **both flakes**, green alone | 1 → 0 |
| `@pryzm/command-registry` | 540/543 — 3 real | 1 |
| root `vitest` | 2609/2611 | 1 |
| `apps/editor` | 3427/3495 — 65 red, long-standing | 1 |

### 0.1 L-943's revert latch is REACHABLE — traced, not assumed

The suspicion was that `FinishHostDependencyTracker` consults an OPTIONAL `isReverting?.()` that
production might not implement — the [[committed-is-not-reachable]] trap. All five links were
traced:

1. `CommandManagerImpl.ts:131` `export class CommandManager`
2. `:720` `isReverting(): boolean { return this._reverting > 0; }`
3. `:743` / `:786` `_reverting++` around BOTH undo and redo, decremented in `finally`
4. `initTools.ts:641` `commandManagerRef.current = commandManager` — the real manager
5. `initTools.ts:871` / `:885` trackers constructed with that ref, AFTER step 4

**Verdict: live.** The `?.` fallback is documented and deliberate.

### 0.2 A claimed "open founder question" that was already CLOSED

Four red arms (`L926MoveReweldPreflightStem`, `L936InteriorLPairMove` ×2,
`wallMoveGateMutualCorner`) each carried a comment asserting C83 §10.6.3 says two contradictory
things and must not be resolved by editing a test.

**Measured: there is no live contradiction.** §10.6.3 #1 quotes the old rule INSIDE A BLOCKQUOTE
marked *"This clause read:"*, explicitly **AMENDED 2026-08-17 (L-942, founder-directed)**, with the
resolution starred: **absent record ⇒ MEASURE the degree by counting endpoints.** The only two
occurrences of the old phrasing are that superseded quote (:1205) and the line forbidding it as an
ANSWER (:1228).

**Decisive:** the amendment is commit `55a2eda3` — the OLDEST commit in the range. `a6279ddf`,
`7c896c19` and `21d0890e` are all LATER re-scopes onto it. **A re-scope campaign was already under
way and these four arms were simply missed.** The engine implements the amendment exactly
(`WallMoveReweld.ts:184` stored-degree-wins, `:186` absent⇒measure, `:203` the L-922 degree≥3 guard
UNCHANGED); measured behaviour is the partner following `z=5 → 5.6` at degree 2.

**Engine correct, tests stale.** Not fixed in this session — recorded so the next lane closes all
four in one commit instead of re-litigating the contract.

---

## 1. Deploy — CI is dead, manual path, and a recovery

CI deploys have failed since **2026-08-14** (verified: runs on `de5b3fee` and both attempts on
`370aa0bd`). The founder directed the manual path.

`DEPLOY-CONTRACT-MANUAL-FLY.md` walked §2 → §5:

- **§2.1** builder `fly-builder-twilight-songbird-4866` at `shared-cpu-8x:16384MB` ✓
- **§2.3** deployed from a **clean worktree** at `370aa0bd`. ⭐ This mattered: the manual path
  uploads the WORKING TREE as Docker context, so deploying from the main tree would have shipped
  uncommitted work while stamping a SHA that did not match it. The founder had chosen HEAD-only.
- **§3.6** fail-closed passed — `cesium:257 google:39` recovered from the live bundle
- **§6.7.2** `DOCKER_CONFIG` guard with a **Windows-shaped** path; builder handshake passed first try
- **§5.3** rollback captured BEFORE deploying: `pryzm:deployment-01M07NQW5QYPH1MT7TRGN9GNM6`,
  live SHA `55a2eda3`

**The recovery.** The build and registry push both succeeded, then flyctl died:
`Error: found multiple image versions`. This is §6.5.7 §IMAGE-REF-RECOVERY — *"Pushing image done"
in the log ⇒ the failure is DELIVERY, not build; never re-run the full script."*

Root cause was NOT this deploy: release **v1295, from ~3 hours earlier, was still "running"** and
had left a stray machine on a different image. Blue-green requires one image across all machines.
With founder approval, the single stopped, 0/1-checks machine on the orphaned image was destroyed
(by explicit machine ID, never by image pattern), then the pushed image was reference-deployed.

**§6.7.1 / L-941 respected:** the bundle proof was NOT run until the deploy script had EXITED.
Result: **6/6 PASS, `PROOF_RC=0`**, GIT_SHA `370aa0bd…` matched, all four VITE values correct,
`/api/health/live` ok.

---

## 2. Half 1 — the register

### 2.1 Ratchet breaches

The tracker reported **4**. Two more were found by measurement:

| gate | rows | before | after | who |
|---|---|---|---|---|
| `check-predicate-canonical` | GE-02, GE-03 | **3** STALE LEDGER | **1** DECLARED-LEVEL | lane A |
| `check-provenance-not-invented` | PV-01, PV-03 | **3** RATCHET EXCEEDED (7/2) | **1** (2/2) | lane A |
| `check-triangulation-canonical` | GE-12 | **3** STALE LEDGER | **1** | orchestrator + lane F |
| `check-deterministic-regeneration` | — | **3** RATCHET EXCEEDED (137/134) | **OPEN** | lane G, live |

⚠ **The 5th and 6th were not in the tracker's count.** GE-12 was found by a lane measuring in a
fresh worktree; `check-deterministic-regeneration` by a lane noticing it in passing. A register that
reports "4 breaches" while 6 gates exit 3 is itself an instance of the defect the register exists to
catch.

⭐ **A gate read exit 1 in the main tree and exit 3 in a fresh worktree — same code.** The
difference was an uncommitted baseline strike sitting in one working tree. Committed as `c9977d05`
so the repository carries the evidence. **This is the session's subject in miniature.**

**No threshold was raised and nothing was added to `gate-debt.json` anywhere in this session.**

### 2.2 Rows closed

| rows | gate | before → after | verified by orchestrator |
|---|---|---|---|
| MT-02, MT-03 | `check-verb-register` | 1 → **0** | ✔ re-ran on main |
| GR-06, GR-07, GR-08, GR-10 | `check-graph-persistence` | 1 → **0** hard-0 | ✔ |
| CO-01, CO-03, CO-06 | `check-no-hidden-mock` | 1 → **0** hard-0 | ✔ |
| CO-08, CO-12 | `check-constraint-honesty` | 1 → **0** hard-0 | ✔ |
| GR-09, GR-11, GR-15, GR-16, GR-17, GR-18, PR-05, PR-06, PR-07, PR-08, PR-10, PR-12, PR-13, GE-07, CO-11, PV-07 | 6 NEW gates | CARRIED → **0** | lane B; register re-measure pending |

**16 of the 43 CARRIED rows now have a runnable gate.** Every one was watched RED against the REAL
subject (writer disabled, registrar unwired, gate file moved, event family truncated) — not only
against synthetic controls.

### 2.3 Rows deliberately NOT closed — and why that is the right answer

- **PR-11** — left CARRIED **on purpose**. It is genuinely OPEN; a hard-0 gate would mint a new
  never-absorbable exit-3 breach in the shared runner, and a declared baseline would mint debt.
- **GR-01, GR-05** — **mis-instrumented, not un-fixed.** Both rows' own subjects are green
  (`contains` writer 1/reader 2/rebuild 1; `sitsOn` 19/1/2). The gate is red on two **`partOf`**
  findings belonging to neither row, formally `⟨DECLINED⟩` pending an ADR. The tracker keys a
  per-ROW status to a whole-VOCABULARY gate, so these rows can never read 0 while any of the nine
  terms carries ledgered debt.
- **GR-14** — shrunk (declared 59 → 58, sites 114 → 110), **not closed, and not claimed**. The
  dominant family is dead defensive code rather than real ambiguity, so the fix is deletion — BUT
  `WallStore:532` and `:71` are the normalisation MAKING the invariant true, and deleting those
  would CREATE the defect. Written into the ledger so a future lane cannot bulk-delete on this row.
- **GE-12** — copies 3 → 2, findings 5 → 4, **does not exit**. See §2.4.
- **MT-09** — was NOT DETERMINED (`--fast` skipped it). Now measured: **RC=1, 34 of 93 packages NOT
  PROVEN to compile** (16 own-source, 10 cascade-only). Structural cause named by the gate: every
  workspace package sets `"types": "./src/index.ts"`, so each isolated compile drags in its
  dependencies' full source closure. Fix is built `.d.ts` + project references, repo-wide.
  ⭐ **This row went from unmeasured to OPEN, not to closed** — the predicted direction.

### 2.4 GE-12 was two questions, not one

`diff -q` said all three `planar-topology-engine` bodies differ; the LOC spread (477/407/220) said
this was not GE-04 repeating.

- **ai-host vs room-topology — PROVEN EQUIVALENT.** Identical at **7,058 characters** after
  `b704e73c`'s normalisation (comments, braces, non-null assertions, `console.*`) plus line-wrapping,
  trailing commas and one local rename. Same 4 exports, same 5 constants. **Collapsed onto
  `@pryzm/room-topology`.** Unlike GE-04 both were LIVE, so the consumer moved to the owner rather
  than a dead copy being dropped.
  ⭐ **The normaliser was watched failing first**: tampering `MIN_ROOM_AREA_M2` 0.5→2.0, and
  separately `(uIdx-1)`→`(uIdx+1)`, each flipped it to NOT-identical **at unchanged length**. That
  is the control discipline applied to the measuring instrument, not just the subject.
- **auto-dimension — GENUINELY DRIFTED → reported, NOT merged.** Six behavioural differences
  (no face-area filter at all; `localeCompare` vs codepoint tiebreak; Map-insertion vs sorted seed
  order; union-find outer face per building). It SHIPS — reachable from
  `applyAutoDimensions.ts:50 → planAutoDimensions → buildings.ts:103,128 → tracePerimeters`.
  Its blocker is measured: `@pryzm/auto-dimension` depends on `@pryzm/schemas` ALONE while
  room-topology's barrel pulls THREE through 8 files, so it cannot import the owner today.

The lane did **not** fill in the gate's `canonical:` field — that field IS the C73 §3.7 decision.
An oracle fixture was added (5 hand-computable arms, no golden file; five tampers each turned
exactly one arm red).

---

## 3. Half 2 — the honest-refusal seam

**The blocker was a 6-error root `tsc`** in an untracked test file written against an API that did
not exist (`normalizers` vs the real `normalize`; `NoPlanRefusal` without `reason`/`detail`;
`liveVerification` absent from `ApprovalStaleRefusal`).

⭐ **The lane decided the TEST was right and the CODE was wrong.** C78 §8.8 names TWO sites in one
sentence, so rather than weaken the assertion it implemented the richer API and extracted **one**
`resolveConsequencePreview()` that both call — noting that typing `planNow` separately would have
fixed the symptom and kept the cause.

**Measured on the 4,100-cell matrix** (`check-relationship-determination`, orchestrator re-ran it):

```
SILENT cells (C78 §1.1's forbidden fourth answer): 0      ← was 3649
honest-refusal cells:                              3649   ← was 0
previewEntryPoint: "TYPED (PreviewOutcome)"               ← was "BARE NULL"
→ [1] DECLARED-LEVEL — 34 findings, at or below declared 34
```

Root `tsc`: **RC=2 → RC=0, 0 errors** (orchestrator re-verified on main).

**Two certification gates were dead and nobody had run them**, and one was dangerous:
`check-preview-purity`'s floor read `plan ? 1 : 0` — and an `undetermined` outcome is a **truthy
object**, so it would have scored a PASS with no plan at all. `check-plan-determinism` now THROWS on
undetermined rather than returning `null`, because two `null`s "agree" and it would have reported
perfect determinism over zero plans.

**Proven at three DECIDING layers**, not the computing one: `confirmationFlowComposition` (passed a
normalising *callback* that had already destroyed N1-vs-N2/N3 before the flow could type either),
`ConfirmationCard` (printed `APPROVAL REFUSED — THE PLAN IS STALE` for EVERY refusal), and
`MovePlanToolHandler` (logged the constant `'NO_PLAN_AVAILABLE'` as the reason). Escape hatch kept
per C83 §10.6.7: `dispatchPlanless` still commits, and the unverifiable arm deliberately retains the
pending plan so the confirm button is a live retry, not a dead end.

### 3.1 A stale number inside the ledger whose job is stale numbers

While committing the B.4 ledger strike (89 `silent-dispatch` rows struck because they were PAID, +1
closed by the typed `hosts` reader), `measured.findings` read **35** while `declaredFindings` held
**34** rows. The row had been struck from the ARRAY and the COUNT beside it was not decremented.
**The gate reads the array, so it passed — green the whole time.** Corrected in `eebf23d7`.

### 3.2 NOT claimed

Exit is **1, not 0**. The remaining 34 are all `relationship:<r>/cannot-refuse` — bare
`getTargets`/`getSources` returning `[]` for both "none" and "unknown". A typed entry point cannot
rescue a relationship that has no way to say "I could not determine"; **that is the next seam.**
Dispatch reach also stays UNPROVEN under STATIC-REACHABILITY: these cells are answerable in
STRUCTURE, not proven answered AT DISPATCH.

---

## 4. Founder-reported defects logged (`2c114e39`)

| row | verdict |
|---|---|
| **L-946** | Level change: dispatch is CORRECT and payload byte-matches the handler. The handler writes the PLUGIN store; the renderer reads the LEGACY one. All twelve bus→legacy bridges are `.created` events — **none for mutations**. Same hole for `roof.changeLevel`. ⚠ Separately, the OTHER level dropdown is "Active Level (Creation Context)" and never acted on the selection at all. |
| **L-947** | **HIGH.** `affectedStores = ["wall"]` hard-coded while `resolveStore()` routes to FIFTEEN stores ⇒ a slab write snapshots the WALL store; undo corrupts both. Lane H live. |
| **L-948** | **HIGH.** Batch create → heavy-scene auto-WebGL → live swap → device destroyed → new context inherits GPU handles from the old one. Plan view draws all 1,611 walls; 3D draws none. Lane I live. ⛔ Logged with an explicit warning NOT to suppress: `ViewportCrashGuard` already calls the `usedTimes` throw "non-fatal", which is why this arrived as *"nothing is visible"* instead of as a crash. |
| **L-949** | RAC: "every 3 meters" and "N equally spaced" ARE already served by `create-windows-parametric`. The real gap is that `set-width`/`set-height`/`set-sill-height` are `scope:'selection'` with NO `scopeModes`, while NINE capabilities — including `set-window-type`/`set-door-type` — already carry `scope:'all'`. |

Two further defects surfaced by lane E and **pinned, not silently patched**:

- **`FilletTool` ships a geometrically wrong arc.** Its header claimed it "requires the lines to
  actually intersect"; it solves the INFINITE-line intersection with no segment-bounds check. Two
  segments that never touch return SUCCESS with an arc tangent 4 mm past a segment's end.
- **`ConstraintSolver` prefers a stale cache to admitting blindness.** A DELETED element still reads
  `satisfied`, computed against where it used to be — while the identical case without a cached
  coordinate correctly reads `unknown`.

---

## 5. Process findings — worth more than several rows

1. ⛔ **A fresh worktree has no `node_modules`, and measurements taken there are NOISE.** Three
   lanes hit this independently. One reported: *"Three of my verification runs were BLIND — `tsc`
   silently bailed on `TS2688` missing-`@types` and reported CLEAN for a file with a deliberate
   error planted in it."* Another measured an entire register baseline with every dependency-needing
   gate crashing. **Every later lane brief carries `pnpm install --frozen-lockfile` as its
   mandatory first command.**
2. ⚠ **`npx vitest` is broken inside packages with no local vitest** — resolves
   `node_modules/node_modules/.pnpm/…`, dies MODULE_NOT_FOUND, exit 1. That is **L-774, not a
   failing suite.** A lane initially reported it as red. Use `pnpm --filter`; treat MODULE_NOT_FOUND
   as *"MEASURED NOTHING"*.
3. ⭐ **A gate registration nearly minted a new exit-3 breach.** `extractGates` takes every
   `check-*` token in an instrument cell and the WORST exit. A parenthetical *explaining that a gate
   is BLIND to a row* caused that row to resolve to two gates and read exit 3 via the blind one.
   Caught by re-running the tool's own parser over the edit.
4. ⭐ **Two gate paths in this session's briefs were guessed wrong** by the orchestrator
   (`check-propagation-trackers-reach` and `check-no-empty-means-unknown` live under
   `tools/rac-conformance/certification/gates/`, not `tools/ga-gate/`). One nearly became a reported
   "the gate does not exist" finding. **Confirm a path before drawing a conclusion from its absence.**
5. **`.dockerignore` excludes `**/*.test.ts`**, which is why 6 root-`tsc` errors in an untracked test
   file did NOT block the Fly build. Worth knowing before treating a red root `tsc` as a deploy blocker.

---

## 6. Open — decisions that are the founder's, not a merge's

1. **`partOf`: are hierarchy nodes graph citizens at all**, or is `hierarchyStore` + `parentId` the
   sole substrate? Blocks GR-01/GR-05. A `partOf` writer justified only by the gate would
   **manufacture the `sitsOn` defect on purpose** (C71 §7.a).
2. **GE-12 §3.7:** which planar-engine behaviour is canonical, and where does the shared body live?
   Recommendation on the record: extract the ~45-line walk into a THREE-free home both packages can
   depend on (natural sibling: `geometry-kernel/src/pure/`), THEN decide filter/tiebreak semantics.
3. **PR-09:** does wall **height** belong in `_computeWallSig`? Both trackers rebuild hosted
   openings on height change, but rooms are detected in plan. The gate REPORTED rather than worked
   around it — *"the fix is a decision … not this gate's to make."*
4. **L-946:** either the bus→legacy bridge gains a MUTATION channel, or the rendered model stops
   being a second copy. ⛔ Not fixed by dual-writing from the UI — that reinstates the dual-write
   P2.1 Step C removed.
5. **MT-09:** built `.d.ts` + project references, repo-wide.
6. **Half 2 next seam:** the 34 `cannot-refuse` relationships.

---

## 7. Lanes

| lane | scope | outcome |
|---|---|---|
| A | 4 exit-3 breaches | GE-02/03 + PV-01/03 → exit 1; found GE-12 as a 5th |
| B | 43 CARRIED rows | **16 gated**, 6 new gates, PR-11 left carried on purpose |
| C | Half 2 seam | tsc RC=0; silent cells 3649 → **0** |
| D | graph gates | GR-06/07/08/10 → hard-0; GR-01/05 shown mis-instrumented |
| E | honesty gates | 2 gates hard-0; GR-14 shrunk, NOT claimed; 2 hidden failures surfaced |
| F | GE-12 | 3 copies → 2; drift reported, not merged; oracle fixture added |
| G | `check-deterministic-regeneration` (137/134) | live |
| H | L-947 slab undo | live |
| I | L-948 invisible model | live |

**Every lane worked in an isolated worktree except C**, which needed the main tree because the
uncommitted B.4 work lived there. No lane used `git stash` (the stack is GLOBAL across worktrees) or
`git add -A`. Docs and issue logs were reserved to the orchestrator throughout, which is why nine
concurrent lanes produced zero doc conflicts.
