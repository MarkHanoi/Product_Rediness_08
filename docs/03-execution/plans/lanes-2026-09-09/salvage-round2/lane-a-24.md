{
  "survivingTitles": [
    "FIVE hand-typed family vocabularies must agree; at least three ids are dead by construction, and nothing checks",
    "Rooms are painted outside the drawing pipeline entirely, and their category lives in the store the Visibility Intent panel does not read",
    "Lighting is drawn TWICE in plan: an ungoverned blue canvas overlay on top of the full 672-face luminaire mesh",
    "Symbol-injected lines carry no `userData.elementUUID`, so a per-ELEMENT visibility override can never reach them — and in plan, furniture IS only its symbol",
    "`VGSceneApplicator.applyToProjectionLayers()` is a DEAD WRITE for the on-screen 2D drawing, and it covers only half the sub-layers",
    "The incremental graft path is unavailable for every gesture a documentation pass actually makes",
    "Occlusion runs last, over-claims on oblique solids, and deletes rather than demotes — 17 of 22 projection occluders degraded on the founder's own plan",
    "C102 clauses this pipeline does not satisfy today",
    "THE ONE STRUCTURAL CHANGE — one per-view ViewGraphicsResolver, landing on the SymbolInjectionGate seam"
  ],
  "refutedTitles": [
    "Every view in a fresh project is governed by a READ-ONLY system intent, and the intent editor silently wires no write handlers"
  ],
  "refutations": [
    "REFUTED — F1 (\"...the intent editor silently wires no write handlers\"). The factual half is confirmed: `isSystem: true` is in the shared `makeIntent` factory (SystemIntents.ts:48), `getDefaultSystemIntentId()` returns architecturalDocumentation (:350), and `bind()` does return at VisibilityIntentPanel.ts:772 before save-meta/[data-appearance]/sliders/mass-apply. But the DEFECT does not exist, because nothing about it is silent and the escape hatch is one click. (a) Every write control renders `disabled` — `renderAppearanceForm` stamps `${disabled ? 'disabled' : ''}` on Visible, the weight slider AND number, line colour, opacity, LINE STYLE, fill style/colour/opacity and symbolicRule (:326-368), so the symptom as written (\"appears, ACCEPTS A CLICK, and changes nothing\") is false at the DOM level. (b) A purple banner at :205-211 says verbatim \"is a system intent and is read-only. Every field below is disabled. Press Duplicate above to make an editable copy — the copy is bound to this view automatically\". (c) The rival surface prints the same sentence (VisibilityIntentManagerPanel.ts:440-448, \"System intent - read only... Duplicate it to author your own version\"). That is EXACTLY the shape C102 V-RE-1 demands (disabled + reason named), not a breach of it. (d) \"Duplicating alone is not enough... the copy governed nothing\" is stale by L-779 and quotes the pre-fix state as current: `duplicateIntent()` executes AssignViewIntentCommand for `contextViewId` in the same gesture (:945-960), then sets `selectedIntentId` to the copy and re-renders (:962-963) so bind() attaches the write handlers — and the founder's own route sets contextViewId (ViewPropertiesPanelBuilders.ts:196 passes `def.id` from the \"Open Intent Editor\" button on the view Properties spine, the exact panel in his screenshot). (e) The proposed copy-on-write fix is also blocked by design: VisibilityIntentStore.update() refuses system intents (cited by the rail panel itself). (f) The symptom link fails independently: sibling commit 9babe6f5 landed the same day attributes \"I change the line type and it doesn't work\" to `output.occlusionDisposition` and `output.beyondLineStyle` being \"WRITTEN BY NO UI AT ALL\" — a missing control, with the resolvers proven live at PlanViewCanvas :418/:1944/:2047. A greyed-out, banner-labelled input is not the mechanism for that complaint.",
    "CORRECTION to F9 (survives) — the V-RE-1 clause is BREACHED ONCE, not twice. The OverridePanel half is real and confirmed (`{ id: 'curtainwall' }` / `{ id: 'railing' }` at OverridePanel.ts:255-256 render as ordinary enabled checkboxes and dispatch `view.setCategoryVisibility` with targetKind 'elementType' at :309-314, against `targetMatches` exact equality at IntentRuleResolver.ts:78-82, while GraphicsRulesEngine._intentRules passes `elementType: category` where category = penCategoryForLayerTag → 'stair' / 'wall'). The VisibilityIntentPanel half is wrong for the reasons in the F1 refutation. So is \"the panel is half-live\": BOTH shift-click handlers wired above the `isSystem` return are guarded `if ((ev as MouseEvent).shiftKey && !intent.isSystem)` (:749, :763) — for a system intent they fall through to a pure navigation re-render, never a write. F9's other three clauses check out: overridePanelDispatchReach.spec.ts:108-124 asserts only that `view.setCategoryVisibility` was dispatched with the right payload (V-ST-3 generalisation not applied); V-CN-1 is still open (SyncStateEngine.ts:248-252 reads `view.viewTemplateId`); C102 §0.1 line 19 does route appearance to C09 §4.",
    "CORRECTION to F6 (survives, and is UNDERSTATED). Everything cited is real — OBC `setVisibility` sets `child.visible` (index.mjs:24873-24883), PlanViewCanvas traverses with `traverse` and never reads `child.visible` (grep for `.visible` in that file returns only style-resolver results at :463/:2027/:2580/:2859 and the underlay ref at :1390), colon-only sub-layers at :674-676, and door lines really do land on 'A-DOOR-CUT'/'A-DOOR-PROJ'/'A-DOOR-BEYOND' via `addProjectionLines` → `layers.assign` (OBC :26130-26141), so `setVisibility('A-DOOR')` cannot reach them and the builder's own comment claiming \"both layers also carry the parent A-DOOR layer\" is false. Two refinements: (i) export does not \"lose\" the weight, it silently falls back — SVGCompositeRenderer.ts:246-251 uses `mat.linewidth` only when it is 1..6 and otherwise `_layerLineWeights[layerName] ?? ISO_LINE_WEIGHTS['projection-visible']`, so unmapped layers print at an ISO default rather than at nothing; (ii) the real consequence is much larger than the finding states — see MISSED #1.",
    "CORRECTION to F7 (survives as code, not as a symptom). PLAN_INCREMENTAL_SAFE_TYPES = {wall,slab,beam,ceiling,floor} (ViewDependencyTracker.ts:150-152), the graftDecline string at initScene.ts:1319-1322 is verbatim, `vi:instance-updated` → `this.invalidate(viewId)` at ViewTechnicalDrawingCache.ts:914-917, and the NME key `${elementId}:${viewId}:${currentVersion}:${cropKey}` with cropKey='full' is at NativeElementMeshExporter.ts:487/531-532. But NOTHING in the founder's message reports slowness — this lane is \"documentation QUALITY\". The symptom paragraph (\"the plan feels like it waits behind the scenes\") is the auditor's inference, not a reported observation. Ship it as perf, not as one of his five complaints.",
    "CORRECTION to F8 (survives only as a census). Confirmed: applyOcclusion is the last mutation (EdgeProjectorService.ts:4230-4249), the vspan arm fires on `oddDegree && !b.isCut` (HiddenLineRemoval.ts:437-448), and 9babe6f5 really did ship the first UI for `resolveOcclusionDisposition`. NOT confirmed, and the code says the opposite by default: \"over-claims\". HiddenLineRemoval.ts:441-442 states the hull is \"exact for any vertically simple silhouette, and it still respects an L-notch\", and the auditor establishes nothing about how much of the 239.5 was wrong. The title asserts a defect the evidence does not carry — re-title it to the measurement (\"17 of 22 projection occluders degrade to the vertical-span hull under disposition=remove; over-claim UNMEASURED\") before anyone quotes it as a finding.",
    "CORRECTION to F5 (survives, mechanism fully confirmed). Verified in full: `registerSegmentUUID` writes only into a module WeakMap and never touches userData (DrawingSelectionIndex.ts:42-53); WardrobePlanSymbolBuilder.ts:148-150 and DoorPlanSymbolBuilder.ts:301 (whole-object `cutSeg.userData = {...}`) stamp no elementUUID; the native path does (EdgeProjectorService.ts:3223); BoundaryLinePlanSymbolBuilder.ts:290 and SpaceEnvelopePlanSymbolBuilder.ts:213 are the two that already do it right; GraphicsRulesEngine._intentRules forwards `elementId: ctx.elementId` into the target (:394-405) so an undefined id can never match; and ProjectVisibilitySection.applyElementVisibility (:156-175) dispatches `view.hideElement` FIRST and unconditionally, then only sets `obj.visible` in the 3D scene — which the symbol builders never read (WardrobePlanSymbolBuilder reads `window.furnitureStore.getAll()` directly, :101-113). One thing to keep straight when reporting: the CATEGORY-level hide is NOT broken — EdgeProjectorService.ts:2700-2707 records it as measured-and-refuted and visibilityIntentGovernsSymbolInjectors.test.ts:230-241 proves it at `ctx`. This finding is strictly the per-element route."
  ],
  "missed": [
    "MISSED #1 — THE ISSUED SHEET IGNORES EVERY VISIBILITY AUTHORITY, AND IS MISSING TWO PAINTERS ENTIRELY. This is the larger half of finding 6 and the auditor stopped one step short of it. `SheetEditorRendererBridge.ts:66` → `composeViewportSvg` → `ViewportSvgComposer.ts:249` hands the whole drawing to `SVGCompositeRenderer.setTechnicalDrawing()`, which traverses `drawing.three` and filters ONLY on `instanceof THREE.LineSegments` (SVGCompositeRenderer.ts:232-236) — it never reads `child.visible`, so a category hidden by VG (the write that DOES set child.visible) still prints; and it derives colour/weight from `mat.color` / `mat.linewidth`, never from `graphicsRulesEngine` or the bound intent, so a family hidden by the INTENT (which the canvas kills via `pen.opacity === 0`) also still prints. Its only 'hidden' notion is a layer-name substring test (:240). On top of that, `grep -n 'roomStore|RoomColour|[Ll]ighting' packages/file-format/src/export/sheets/*.ts` → 0 hits: the room fills and the lighting symbols the founder is looking at are canvas-only and reach no sheet. Net: what he sees and what he issues are three different drawings, and the divergence is silent.",
    "MISSED #2 — PEN WIDTH IS INDEPENDENT OF ZOOM AND OF THE 1:100 DRAWING SCALE, WHICH IS THE MOST DIRECT MECHANISM FOR \"THE LINES ARE ALMOST NOT READABLE\". The audit attributes that complaint only to the lighting mesh (fixed today) and to occlusion. But `PlanViewCanvas.render()` computes `_penPx = _pen.widthMm * SCREEN_PX_PER_MM` (:520) with SCREEN_PX_PER_MM ≈ 3.779 (DrawingConstants.ts:73), and the only transform on the context is `ctx.setTransform(scale,0,0,scale,0,0)` where scale = `resolveCanvasRenderScale(devicePixelRatio)` (:369) — a DPI backing-store term, not a view term. `grep -n drawingScale` in PlanViewCanvas.ts and PenWeightTable.ts → 0 hits. So the WHOLE drawing inks between 0.34 CSS px (beyond/hidden 0.09 mm) and 1.89 CSS px (wall cut 0.50 mm) at EVERY zoom level: zooming in grows the geometry and leaves the ink at hairline, and the view's declared 1:100 scale enters the width calculation nowhere. L-288 fixed the raster FLOOR (backing scale ≥3 so 0.09 mm is no longer clamped) but not the absence of any zoom/paper-scale coupling. Nothing in CanvasRenderScale.ts, DrawingConstants.ts or PenWeightTable.ts records this as a decision — flag it as an open design question rather than a bug, but it belongs in this dimension and it is the cheapest thing to A/B against his screenshot.",
    "MISSED #3 — THE PER-VIEW QUICK TOGGLE STILL CANNOT HIDE LIGHTING, THE DAY AFTER LIGHTING BECAME GOVERNABLE. Finding 2 enumerates the five vocabularies but not this consequence: `OverridePanel.CATEGORIES` (:247-269) is 13 rows — wall, slab, door, window, stair, roof, column, beam, furniture, curtainwall, railing, ceiling, boundary-line — and has NO `lighting` row, even though 584aaab2 added `lighting` to ELEMENT_TYPE_TO_PROJECTION_LAYER, ISO_LAYER_TO_VG_CATEGORY, penCategoryForLayerTag and VisibilityIntentDefaults.ELEMENT_TYPES the same morning. It also has no plumbing, grid, level, room, annotation or spaceEnvelope row. So the family the founder complained about acquired a pen, a VG category and an intent row, and still has no per-view hide on the panel he reaches for — the four-site rule was obeyed at four sites out of five. This is the strongest single argument for the derived FAMILY_REGISTRY the audit proposes.",
    "MISSED #4 — THERE IS A SECOND, DEAD PLAN PAINTER IN THE SAME FILE, AND IT IS A TRAP FOR FINDING 10. `PlanViewCanvas.renderFromPipelineResult()` (:1915) and `scheduleWorkerRender()` re-implement the whole paint — room fills, site context, grid datums, poché, lighting symbols (:2092), annotations, crop boundary — and have NO CALLERS (`grep -rn 'renderFromPipelineResult|scheduleWorkerRender' apps packages plugins` → only the declarations; the file says so at :1971-1977 and it is still true). Anyone repointing the six painters onto a ViewGraphicsResolver will either miss it (leaving a divergent painter that comes alive the day someone wires the worker) or waste a day on it. Name it in the plan: delete it, or repoint it in the same commit."
  ]
}