{
  "dimension": "Documentation quality: ELEVATION and SECTION versus PLAN — the two view families the founder named but did not screenshot",
  "summary": "Plan is not \"ahead\" of elevation/section by degree — the three diverge at three separate layers, and fixing plan fixes none of it.\n\nWHAT IS GENUINELY SHARED (reuse, do not rebuild): all three view types run one projector, `EdgeProjectorService.project()`, ending in one occlusion pass (`applyOcclusion`, EdgeProjectorService.ts:4207) and one style pass (`VGSceneApplicator.applyToProjectionLayers`). The per-view-type policy is already centralised and correct in `packages/core-app-model/src/views/ViewScope.ts` — a frozen three-way table giving elevation `{cut:false, poche:true+requiresExplicitIntent, occlusion:'demote', beyond:'dashed'}`, section `{cut:true, poche:true, occlusion:'remove', beyond:'dashed'}`, plan `{cut:true, poche:true, occlusion:'remove', beyond:'solid'}`. Section/elevation cut geometry IS produced (`buildMeshPlaneIntersectionGeometry`, gated by `isSectionDepthView = section || elevation`, :2317). The vertical-view canvas mapping is already solved once and correctly, in `PlanViewCanvas._vertexToHV` (:337-342). Elevation auto-dimension exists and is routed (`autoDimensionActiveView.ts`); section is honestly refused with a toast, not silently broken.\n\nWHERE THEY DIVERGE:\n\n1. POCHÉ IS STRUCTURALLY IMPOSSIBLE IN A SECTION, and I proved it by running the real algorithm. `PocheFillBuilder` reads `positions[i]` and `positions[i+2]` and discards Y (\"XZ plane only — Y is ignored (plan view geometry is flat)\", :146). A section's cut ring is VERTICAL and coplanar with the section plane, so it collapses: my probe of the verbatim `_parseSegments`/`_stitchClosedLoops` gives 4 drawable-loop vertices for a plan ring and **2** for either section orientation — under the `if (points.length < 3) continue` guard at both consumers. Section poché paints NOTHING. This is the single biggest readability lever in a section and it has never worked.\n\n2. SYMBOL COVERAGE IS 15 / 3 / 1. Plan-family gets all fifteen injectors; elevation gets three (plumbing, tree, opening); section gets one (tree). Lighting has NO injector in any view — it is canvas-painted and plan-family-only, so it is absent from elevation, section AND from SVG/PDF export.\n\n3. THE LIVE DRIVER SKIPS THE ANNOTATION INJECTION THE LEGACY DRIVERS DO. `ViewController.ts:1513` routes elevation and section to `PlanViewManager.activate` (DOC-19B). None of PlanViewManager's three projection paths inject `levelDatumLineBuilder` / `sectionGridLineBuilder`; all four call sites that do are on paths that route does not use. Sections therefore have no grid lines on the path a user actually reaches.\n\n4. REACHABILITY IS WORSE THAN C59 RECORDS. Beyond the known \"not pane-assignable\", `ViewsRailPanel._onActivateView` computes `targetBimMode` as null for elevation and section (:812), so the §ONE-ORCHESTRATOR GIS exit shipped hours ago in 5d9cacf6 does not fire for them — the exact \"opening underneath Cesium\" bug the founder reported is fixed for plans and 3D and still live for the other two. And zero section views exist by default (four elevations do).\n\nRanking for the next lane: (1) section poché, (2) the ViewsRailPanel GIS-exit gap (a two-line fix on a defect the founder already reported once), (3) datum/grid injection on the live path, (4) lighting symbols, (5) elevation/section symbol breadth.",
  "findings": [
    {
      "title": "Section poché can never render: PocheFillBuilder discards world Y, so a vertical cut ring collapses to a 2-vertex degenerate and is dropped",
      "mechanism": "`PocheFillBuilder._parseSegments` reads only `positions[i]` (x) and `positions[i+2]` (z) per vertex and its own comment states \"XZ plane only — Y is ignored (plan view geometry is flat)\". A SECTION's `:cut` linework comes from `buildMeshPlaneIntersectionGeometry` (EdgeProjectorService.ts:3012), whose output is coplanar with the section depth plane — i.e. VERTICAL, varying in Y and constant in the depth axis. Projected onto XZ, the ring's two vertical edges become `aKey === bKey` and are dropped by `if (aKey === bKey) continue`, and its two horizontal edges become the same segment twice. `_stitchClosedLoops` returns a 2-vertex 'loop', which both consumers discard at `if (points.length < 3) continue`. I ran the verbatim algorithm on three fixtures: PLAN ring -> 4 segments kept, 1 drawable loop; SECTION normal=Z -> 2 kept, 0 drawable; SECTION normal=X -> 2 kept, 0 drawable. The path IS reached — `_SECTION_SCOPE` is `poche: true, pocheRequiresExplicitIntent: false`, so `PlanViewCanvas.render` calls `_renderPocheFills` unconditionally for a section. It simply produces zero polygons every time.",
      "evidence": [
        "packages/core-app-model/src/views/PocheFillBuilder.ts:146 — `* XZ plane only — Y is ignored (plan view geometry is flat).`",
        "packages/core-app-model/src/views/PocheFillBuilder.ts:159-163 — `const ax = quantise(positions[i]!); const az = quantise(positions[i + 2]!); const bx = quantise(positions[i + 3]!); const bz = quantise(positions[i + 5]!);` — indices +1 and +4 (Y) are never read",
        "packages/core-app-model/src/views/PocheFillBuilder.ts:138 — `points: loop.map(v => `${v.x.toFixed(4)},${v.z.toFixed(4)}`).join(' '),`",
        "packages/core-app-model/src/views/PocheFillBuilder.ts:165 — `if (aKey === bKey) continue;` — this is where a section ring's vertical edges die",
        "packages/core-app-model/src/views/PlanViewCanvas.ts:2702 — `if (points.length < 3) continue;` — the 2-vertex loop is discarded here",
        "apps/editor/src/engine/views/plan-canvas/PlanViewFillRenderer.ts:157 — `if (points.length < 3) continue;` — the identical guard in the split-view copy",
        "packages/core-app-model/src/views/ViewScope.ts:147-148 — `_SECTION_SCOPE`: `poche: true, pocheRequiresExplicitIntent: false, cut: true, ...` — poché is ON for sections, so the dead path is genuinely reached",
        "apps/editor/src/engine/views/EdgeProjectorService.ts:3012-3023 — `const meshCutGeo = buildMeshPlaneIntersectionGeometry(mesh, viewDef, direction, near, sectionVolumeBox); ... perElemLayerCutGeos.get(layerName)!.push({ geo: meshCutGeo, pocheLayer: readMeshPocheLayer(mesh) });` with the comment \"a layered wall pochés per layer in section too\" — the intent is explicit and the data is produced",
        "apps/editor/src/engine/views/EdgeProjectorService.ts:1602 — `positions.push(clipped[0].x, clipped[0].y, clipped[0].z, ...)` — confirms the section cut geometry is emitted in world space on the section plane, i.e. vertical",
        "PROBE (verbatim re-implementation of PocheFillBuilder.ts:145-228, run via node): `PLAN (horizontal ring) segments kept = 4/4 closed loops = 1 loops with >=3 verts (DRAWABLE) = 1` / `SECTION normal=Z (vertical ring) segments kept = 2/4 closed loops = 1 loops with >=3 verts (DRAWABLE) = 0` / `SECTION normal=X (vertical ring) segments kept = 2/4 ... DRAWABLE = 0`"
      ],
      "userVisibleSymptom": "A section drawing has no solid fill at the cut. Walls, slabs and floors that the section plane slices read as thin open outlines identical in weight-family to the projected geometry behind them, so nothing tells the eye what is cut and what is beyond. This is the founder's \"the quality atm is really bad — the lines are almost not readable\" applied to a section: there is no tonal anchor at all, only line. Poché is the single largest readability lever in a section drawing.",
      "fix": "Carry the third axis through the builder. Add a plane selector to `PocheFillBuilder` — e.g. `fromGeometry(geometry, fill, opacity, tolerance, plane: 'xz' | 'xy' | 'zy')` — or better, hand it the projection basis the canvas already owns. DO NOT write a new mapper: `PlanViewCanvas._vertexToHV` (:337-342) is the correct, in-use, section-aware world->(h,v) reduction (`h: this._worldPointToCanvasH(v.x, v.z), vert: this._sectionFlipV ? v.y : v.z`) and the linework path already uses it. The clean shape is to reduce vertices to (h,v) with `_vertexToHV` BEFORE stitching, so the stitcher becomes plane-agnostic and there is one reduction in the codebase instead of two. Then delete the wrong second reduction at PlanViewCanvas.ts:2728 and PlanViewFillRenderer.ts:262 (`return { h: x, v: sectionFlipV ? -z : z }`), which additionally ignores `_hWorldAxis` and `_hWorldSign` and so is wrong for left/right sections even on the h axis. Note `PlanViewFillRenderer.renderPipelinePolygons` (:174) has NO callers and is dead — do not extend it.",
      "effort": "medium",
      "risk": "medium",
      "confidence": "high",
      "alreadyExists": "`PlanViewCanvas._vertexToHV` (packages/core-app-model/src/views/PlanViewCanvas.ts:337-342) is the correct section-aware reduction, already used by the linework pass. `PlanViewCanvas.viewPlaneFrame()` (:328-336) already PUBLISHES the plane (`{isVertical, hWorldAxis, hSign}`) precisely so consumers stop assuming plan — its header says so. `resolveWallLayerPocheFill`, the intent/VG fill ladder, `ISO_CUT_LAYER_TO_POCHE_FILL` and the hatch library all work already and are plane-independent. The stitching graph algorithm itself is sound; only its 3D->2D reduction is wrong.",
      "filesToChange": [
        "packages/core-app-model/src/views/PocheFillBuilder.ts",
        "packages/core-app-model/src/views/PlanViewCanvas.ts",
        "apps/editor/src/engine/views/plan-canvas/PlanViewFillRenderer.ts"
      ]
    },
    {
      "title": "The test that should have caught the section/elevation poché collapse uses a HORIZONTAL fixture labelled as an elevation cut band",
      "mechanism": "`elevationCutPocheIsIntentDeclared.test.ts` builds its fixture in `elevationDrawingWithCutRegion()`, whose docstring claims it is \"shaped exactly as EdgeProjectorService leaves an elevation cut band\". The buffer is `[-3,0,-1, 3,0,-1, 3,0,-1, 3,0,1, 3,0,1, -3,0,1, -3,0,1, -3,0,-1]` — every Y is 0 and the rectangle lies in XZ. That is a PLAN footprint. It stitches to a 4-vertex drawable loop and the test's `expect(painted.fills)` passes, certifying an elevation poché feature that cannot draw on real elevation geometry. Every other poché test has the same blind spot from the other direction: `wallPocheFill.test.ts`, `wallCurvePocheFill.test.ts` and `HiddenLineRemoval.planPoche.test.ts` all drive `buildPlanCutSectionGeometry` (the horizontal plan cut). ZERO tests in the repo drive `buildMeshPlaneIntersectionGeometry` (the section cut) into `PocheFillBuilder`.",
      "evidence": [
        "apps/editor/__tests__/elevationCutPocheIsIntentDeclared.test.ts:103-104 — `* A drawing holding ONE closed rectangular cut region on `${layer}:cut`, shaped exactly * as EdgeProjectorService leaves an elevation cut band`",
        "apps/editor/__tests__/elevationCutPocheIsIntentDeclared.test.ts:110-115 — `// A closed rectangle as LineSegments pairs, in the drawing's XZ plane.` then `const r = [ -3, 0, -1, 3, 0, -1, ... ]` — the comment says XZ and every Y is 0, contradicting the docstring six lines above",
        "apps/editor/__tests__/wallPocheFill.test.ts:24 — `import { buildPlanCutSectionGeometry } from '../src/engine/views/EdgeProjectorService.js';`",
        "apps/editor/__tests__/wallCurvePocheFill.test.ts:35 — `import { buildPlanCutSectionGeometry } from '../src/engine/views/EdgeProjectorService.js';`",
        "Measured: `grep -rln 'PocheFillBuilder|renderPocheFills' --include=*.test.ts --include=*.spec.ts` returns exactly three files — elevationCutPocheIsIntentDeclared, wallCurvePocheFill, wallPocheFill. None constructs a vertical ring."
      ],
      "userVisibleSymptom": "No direct symptom — this is why the previous finding shipped and stayed. It matters because a lane fixing PocheFillBuilder will run the suite, see green, and conclude the fix is unnecessary or already done. This is the [[fake-more-capable-than-real]] pattern verbatim: a fixture built from the feature's own description cannot falsify that description.",
      "fix": "Add one arm to the existing test file whose ring is genuinely vertical — e.g. `[0,0,2.5, 4,0,2.5, 4,0,2.5, 4,2.7,2.5, 4,2.7,2.5, 0,2.7,2.5, 0,2.7,2.5, 0,0,2.5]` (a 4m x 2.7m wall face on a section plane at z=2.5) — and assert `painted.fills.length > 0`. That arm FAILS today and is the regression pin for the poché fix. Better still, derive the fixture from the real producer: call `buildMeshPlaneIntersectionGeometry` on a box mesh with a real section `viewDef` so the fixture cannot drift from what the projector actually emits, which is what `wallPocheFill.test.ts` already does correctly for the plan side.",
      "effort": "small",
      "risk": "low",
      "confidence": "high",
      "alreadyExists": "The whole harness — `makeCtx()`/`painted.fills` capture, the intent-binding helper, the `viewTypeModifiers` seeding — is in `apps/editor/__tests__/elevationCutPocheIsIntentDeclared.test.ts` and needs only a second fixture. `wallPocheFill.test.ts` demonstrates the from-the-real-producer pattern to copy.",
      "filesToChange": [
        "apps/editor/__tests__/elevationCutPocheIsIntentDeclared.test.ts"
      ]
    },
    {
      "title": "Symbol coverage is 15 for plan, 3 for elevation, 1 for section — and lighting has no injector for any view",
      "mechanism": "`EdgeProjectorService` invokes all symbol builders from one contiguous region (:3940-4130), each behind an explicit view-type set. `PLAN_SYMBOL_INJECTION_VIEW_TYPES = new Set(['plan','detail','structural-plan'])` (:324) admits door, sofa, bed, wardrobe, chair, kitchen, tree and plumbing; seven more (window, stair, roofSlope, column, wallLayer, boundaryLine, spaceEnvelope) each repeat the same `viewType === 'plan' || 'detail' || 'structural-plan'` literal. Only three run for `isElevationView` (plumbingElevation :4092, treeElevation :4102, openingElevation :4129) and only ONE for `isSectionView` (treeElevation, :4101 — the `isElevationView || isSectionView` guard). Lighting is a separate case: it is not one of the fifteen injectors at all. It is painted straight onto the canvas by `PlanViewSymbolRenderer.renderLightingPlanSymbols`, which opens `if (viewType !== 'plan' && viewType !== 'ceiling-plan' && viewType !== 'structural-plan') return;` and maps `(wx, wz)` — plan coordinates. So a light fitting has no representation in elevation or section, and because it is canvas-painted rather than injected into the drawing it is also invisible to HLR, to the `SymbolInjectionGate`, and to any SVG/PDF export that consumes `drawing.three`.",
      "evidence": [
        "apps/editor/src/engine/views/EdgeProjectorService.ts:324 — `const PLAN_SYMBOL_INJECTION_VIEW_TYPES: ReadonlySet<string> = new Set([` ... resolved by apps/editor/__tests__/rcpIsNotAPlanWithAFlippedCamera.test.ts:233 to `['plan', 'detail', 'structural-plan']`",
        "apps/editor/src/engine/views/EdgeProjectorService.ts:4091-4093 — `if (isElevationView) { if (_symbolGate('plumbing')) plumbingElevationSymbolBuilder.inject(drawing, viewDef); }`",
        "apps/editor/src/engine/views/EdgeProjectorService.ts:4101-4103 — `if (isElevationView || isSectionView) { if (_symbolGate('furniture')) treeElevationSymbolBuilder.inject(drawing, viewDef); }` — the ONLY builder a section ever gets",
        "apps/editor/src/engine/views/EdgeProjectorService.ts:4129 — `if (isElevationView && (_symbolGate('door') || _symbolGate('window'))) {`",
        "apps/editor/src/engine/views/plan-canvas/PlanViewSymbolRenderer.ts:16 — `if (viewType !== 'plan' && viewType !== 'ceiling-plan' && viewType !== 'structural-plan') return;`",
        "apps/editor/src/engine/views/EdgeProjectorService.ts:4004 — `// Applied to plan-like views only — not section/elevation/3D.` (stairs) — the exclusions are DECISIONS, written down, not accidents",
        "packages/core-app-model/src/presentation/SymbolInjectionGate.ts:22-27 — the census names all fifteen and confirms only `OpeningElevationSymbolBuilder` and `PlumbingElevationSymbolBuilder` are elevation-side"
      ],
      "userVisibleSymptom": "In an elevation, everything except plumbing, trees and door/window openings is raw `EdgesGeometry` off the 3D solid — every bevel, every mattress seam, every chair stretcher, every kitchen carcass edge. In a section, that is true of everything except trees. This is precisely \"the lines are almost not readable\": the symbol layer whose entire job in plan is to REPLACE dense mesh wireframe with clean drafted linework is almost entirely absent, so the dense wireframe is what the user sees. Stairs — the element most in need of a section representation — have no section symbol at all. Lighting fixtures have no symbol anywhere, which is the founder's explicit ask.",
      "fix": "Two separable pieces. (1) LIGHTING, the founder's named request: `renderLightingSymbols` should become an INJECTOR in the fifteen-strong region rather than a canvas painter, so it inherits HLR, the `SymbolInjectionGate`, UUID registration for selection, and export. Its geometry is already authored in `packages/core-app-model/src/views/symbols/LightingPlanSymbolRenderer.ts`; the work is the injection wrapper plus an elevation/section variant. (2) ELEVATION/SECTION BREADTH: the highest-value missing builders are stair (a section stair is the canonical drawing) and wallLayer (layered construction at the cut is what a section is FOR). Model both on `TreeElevationSymbolBuilder`, which is the one builder that already decided section-vs-elevation deliberately and documented the reasoning. Do NOT widen the plan builders' view-type sets — a door swing arc really is plan-only and the exclusions at :4004/:4016/:4026 are reasoned decisions.",
      "effort": "large",
      "risk": "medium",
      "confidence": "high",
      "alreadyExists": "`makeSymbolInjectionGate` (packages/core-app-model/src/presentation/SymbolInjectionGate.ts) already takes `viewType` and resolves the intent ladder per view type — it is view-type-general and needs no change. `OpeningElevationSymbolBuilder`, `PlumbingElevationSymbolBuilder` and `TreeElevationSymbolBuilder` are three working templates for an elevation-side builder, and `TreeElevationSymbolBuilder` specifically covers the elevation-AND-section case. `suppressSymbolisedElementLinework` (EdgeProjectorService.ts:4146) already solves the double-draw problem keyed on `coveredElementIds`, so a new builder does not need to invent mesh suppression. `WallElevationSymbol.ts` and `OpeningElevationSymbol.ts` exist in packages/core-app-model/src/drawing/ with tests.",
      "filesToChange": [
        "apps/editor/src/engine/views/EdgeProjectorService.ts",
        "apps/editor/src/engine/views/plan-canvas/PlanViewSymbolRenderer.ts",
        "packages/core-app-model/src/views/symbols/LightingPlanSymbolRenderer.ts"
      ]
    },
    {
      "title": "The LIVE elevation/section driver (PlanViewManager, DOC-19B) never injects level datums or grid lines — all four call sites that do are on paths this route does not use",
      "mechanism": "`ViewController` routes elevation and section ViewDefinitions to `this._planViewManager.activate(routedViewDef)` at :1513-1521, with a comment naming this the DOC-19B Canvas2D path and stating it must come BEFORE the legacy `'Section'`/elevation-preset branches. PlanViewManager has three projection paths — `_ensureProjection` (:829), `_ensureProjectionForSplitView` (:1096), `_reprojectActiveViewDoubleBuffered` (:981). I grepped all three: each calls `vgApplicator.applyToProjectionLayers` and NONE calls `levelDatumLineBuilder.inject` or `sectionGridLineBuilder.inject`. The four sites that do inject are `ViewController.ts:711-712` (inside `requestBackgroundProjection`, and guarded `if (viewDef.viewType === 'elevation')` so sections are excluded even there), `ViewController.ts:2310-2312` (`_activateElevationView`, the legacy ViewCube camera preset), and `SectionViewService.ts:227-229` (the legacy SectionTool path). The datum half is masked because `PlanViewCanvas` has a RIVAL canvas-side producer — `if (!isPlanLike && this._sectionFlipV) this._renderLevelDatums(ctx);` (:388) — so datums appear anyway. Grid lines have no such rival: `_renderBimGridDatums` is called only `if (isPlanLike)` (:387).",
      "evidence": [
        "apps/editor/src/engine/ViewController.ts:1513-1521 — `} else if (routedViewDef?.viewType === 'elevation' || routedViewDef?.viewType === 'section') { // DOC-19B: elevation and section ViewDefinitions created by the plan-view // tools use the Canvas2D renderer (PlanViewManager), NOT the WebGPU camera // preset path. ... this._planViewManager.activate(routedViewDef);`",
        "apps/editor/src/engine/views/PlanViewManager.ts:829-918 — `_ensureProjection` body: `.then(drawing => { ... vgApplicator.applyToProjectionLayers(drawing, viewDef.id); ... activePlanDrawingRef.drawing = drawing;` — no datum/grid injection between the VG call and the commit",
        "Measured via awk over each function body: `_ensureProjectionForSplitView` and `_reprojectActiveViewDoubleBuffered` both match only `setIfCurrent` / `vgApplicator` / `applyToProjectionLayers` — zero `inject` hits",
        "apps/editor/src/engine/ViewController.ts:709-712 — `// Inject datum lines for elevation views` / `if (viewDef.viewType === 'elevation') { levelDatumLineBuilder.inject(drawing, viewDef); sectionGridLineBuilder.inject(drawing, viewDef); }` — the sheet path, elevation-only",
        "apps/editor/src/engine/views/SectionViewService.ts:227-229 — `levelDatumLineBuilder.inject(drawing, viewDef); ... sectionGridLineBuilder.inject(drawing, viewDef);` — legacy SectionTool path only",
        "packages/core-app-model/src/views/PlanViewCanvas.ts:387-388 — `if (isPlanLike) this._renderBimGridDatums(ctx, viewDef);` / `if (!isPlanLike && this._sectionFlipV) this._renderLevelDatums(ctx);` — the rival that masks the datum half and does not cover the grid half",
        "plugins/annotations/src/tools/SectionGridLineBuilder.ts:34 — its own header names its caller as `ViewController._activateElevationView() — after elevation projection (DOC-2.5e)`, i.e. the legacy path"
      ],
      "userVisibleSymptom": "Open a section or an elevation the normal way (Views & Sheets, or the plan pane's view selector) and there are no vertical grid lines and no grid bubbles. A section without gridlines cannot be dimensioned or coordinated. Level datum lines DO appear, which makes this hard to spot — the drawing looks half-annotated rather than un-annotated, and a reader will conclude the grid builder is broken rather than unreached.",
      "fix": "Add the injection to PlanViewManager's projection paths, immediately after `applyToProjectionLayers`, gated on `viewDef.viewType === 'section' || viewDef.viewType === 'elevation'`. Because there are THREE paths, put it behind one private method (`_injectVerticalViewAnnotations(drawing, viewDef)`) and call it from all three rather than pasting it three times — this file already carries the scars of divergent copies. Separately, decide whether `PlanViewCanvas._renderLevelDatums` or `levelDatumLineBuilder` is the datum authority: wiring the injector without retiring one of them will DOUBLE-DRAW the datums on the live path. That ordering matters — fix the grid half first and verify, or you will trade a missing feature for a doubled one. Also widen `ViewController.requestBackgroundProjection`'s `viewType === 'elevation'` guard to include `'section'`, or a section placed on a sheet stays gridless.",
      "effort": "small",
      "risk": "medium",
      "confidence": "high",
      "alreadyExists": "`levelDatumLineBuilder` and `sectionGridLineBuilder` are built, exported from `@pryzm/plugin-annotations`, and working — they are called from three other sites. `plugins/annotations/src/tools/SectionGridLineBuilder.ts` and `LevelDatumLineBuilder.ts` need no change. This is [[authored-but-unwired-is-the-bottleneck]]: the expensive half exists, the one-line half does not.",
      "filesToChange": [
        "apps/editor/src/engine/views/PlanViewManager.ts",
        "apps/editor/src/engine/ViewController.ts"
      ]
    },
    {
      "title": "Clicking an elevation or a section in Views & Sheets skips the GIS exit — the L-13253 'covered by Cesium' bug the founder reported is fixed for plans and 3D only",
      "mechanism": "Commit 5d9cacf6 (today) fixed the founder's \"i clicked 3rd view but would not open\" by routing `ViewsRailPanel._onActivateView` through the declared choke point `window.pryzmActivateBimView`, whose first act is `if (_gisActive) toggleGIS(false)`. The commit message states the mechanism: \"the Cesium canvas sits at z-index:15, the BIM WebGPU canvas at z-index:2 ... every click activated the BIM view correctly and painted it UNDERNEATH the site view\". But the mode is computed as `const targetBimMode: 'Top' | '3D' | null = (['plan','ceiling-plan','structural-plan']).includes(viewType) ? 'Top' : viewType === '3d' ? '3D' : null;` — and the call is guarded `if (targetBimMode !== null && ...)`. `elevation` and `section` both fall to `null`. They still reach `VIEW_TYPE_TO_OBC` ('Front' and 'Section' respectively, :173-174) and `onViewSelect(obcId)`, so the view genuinely activates — and, exactly as the commit describes for the pre-fix case, paints under Cesium when GIS is active. Rival hypothesis I considered and could NOT fully rule out: `pryzmActivateBimView`'s parameter type is literally `(m?: 'Top' | '3D')` (PaneViewPicker.ts:433), so `null` may be a deliberate 'no declared full-screen route' rather than an oversight. But the effect is the same either way — the GIS exit does not run — and `paneViewModel.ts:66` states the rule for exactly this case: \"Absent ⇒ there is no route and a non-pane-hostable row stays genuinely unavailable.\" That is not what happens here; the row is enabled and clickable.",
      "evidence": [
        "apps/editor/src/ui/ViewBrowser/panels/ViewsRailPanel.ts:812-816 — `const targetBimMode: 'Top' | '3D' | null = (['plan', 'ceiling-plan', 'structural-plan'] as ReadonlyArray<ViewDefinition['viewType']>).includes(viewType) ? 'Top' : viewType === '3d' ? '3D' : null;`",
        "apps/editor/src/ui/ViewBrowser/panels/ViewsRailPanel.ts:817 — `if (targetBimMode !== null && typeof window.pryzmActivateBimView === 'function') {` — elevation and section never enter this block",
        "apps/editor/src/ui/ViewBrowser/panels/ViewsRailPanel.ts:170-178 — `const VIEW_TYPE_TO_OBC: Record<ViewDefinition['viewType'], string> = { '3d': '3D', 'plan': 'Top', 'section': 'Section', 'elevation': 'Front', ... }` — they DO have an OBC route, so the switch runs",
        "apps/editor/src/ui/ViewBrowser/panels/ViewsRailPanel.ts:831-833 — `window.viewController?.setActiveViewDefinitionId(viewId); this._props.onViewSelect(obcId);` — runs unconditionally, after the skipped exit",
        "git show 5d9cacf6 (commit message) — \"The Cesium canvas sits at `z-index:15`, the BIM WebGPU canvas at `z-index:2` — both stated by their own log lines — so every click activated the BIM view correctly and painted it UNDERNEATH the site view. A no-op and a covered success look identical from the outside, which is why this read as dead.\"",
        "apps/editor/src/ui/ViewBrowser/panels/ViewsRailPanel.ts:59,68 — `label: 'Sections', viewType: 'section',` / `label: 'Elevations', viewType: 'elevation',` — both are real, listed, clickable rows in the rail"
      ],
      "userVisibleSymptom": "With the 3D Site / GIS surface active, clicking an Elevation or a Section in Views & Sheets appears to do nothing — the same symptom the founder reported and that was fixed for plans and 3D hours ago. He will re-report it as a regression, and the natural conclusion (\"the elevation view is broken\") is wrong: the view is rendering correctly, underneath Cesium.",
      "fix": "Elevation and section land on the Canvas2D plan surface, which is the same surface a plan lands on, so `'Top'` is the correct full-screen route for both. Extend the ternary to `(['plan','ceiling-plan','structural-plan','elevation','section']).includes(viewType) ? 'Top' : ...`. Verify by watching for the `GIS: Deactivating geospatial view` log line on an elevation click — the commit message names that exact line as the discriminator between a no-op and a covered success, so do not judge this fix by looking at the screen alone. If `pryzmActivateBimView('Top')` turns out to force a plan-specific camera and disturb the elevation basis, the alternative is to call the GIS exit directly rather than widening the mode — but measure before assuming that, I did not.",
      "effort": "small",
      "risk": "low",
      "confidence": "high",
      "alreadyExists": "`window.pryzmActivateBimView` (`GISAreaLayout.activateView`) is the declared single orchestrator and already performs the GIS exit plus legacy-bar retirement. Nothing needs building — this is one entry added to an existing array literal. `legacyViewSwitcherRetirement.ts:36` and `SiteAuthoringPaneShell.ts:589` document the same choke point for other callers.",
      "filesToChange": [
        "apps/editor/src/ui/ViewBrowser/panels/ViewsRailPanel.ts"
      ]
    },
    {
      "title": "Selecting a section or elevation in the plan pane's view dropdown never triggers a projection — it renders whatever happens to be cached, which for a never-opened section is nothing",
      "mechanism": "C59 records that `bim-elevation-2d` / `bim-section-2d` are not independently pane-assignable and are chosen inside the plan pane's own view-definition selector. That selector is `SplitViewManager._buildViewSelectOptions` (:1110), which does list them: `viewDefinitionStore.getByType('section')` under a 'Sections' optgroup (:1146-1147) and `getByType('elevation')` under 'Elevations' (:1150-1151), each falling back to a disabled '(none created)' row. But the handler `_onViewSelectChange` (:1181) is documented read-only — its own docstring states \"Per Contract §01 §4 the SVP is read-only — this does NOT trigger a new EdgeProjector projection; it renders whatever is already cached for the selected view.\" Its body confirms it: it calls `_activateMode('plan')`, `_configureCanvasForView`, resets the camera and emits `split-view-view-changed`, and never calls `_ensureProjection` or `project()`. Meanwhile `PlanViewCanvas.render` bails to a placeholder when `viewTechnicalDrawingCache.get(viewId)` misses (:389-401). Partial mitigation exists: `PlanViewManager._onViewUpdated` calls `_ensureProjectionForSplitView` (:950) — but only on a `vd:view-updated` event for the ALREADY-active split view id, not on the user selecting a different one.",
      "evidence": [
        "apps/editor/src/engine/views/SplitViewManager.ts:1178-1179 — `* Per Contract §01 §4 the SVP is read-only — this does NOT trigger a new EdgeProjector * projection; it renders whatever is already cached for the selected view.`",
        "apps/editor/src/engine/views/SplitViewManager.ts:1146-1151 — `const secViews = viewDefinitionStore.getByType('section'); addGroup('Sections', secViews, '(none created)'); ... const elevViews = viewDefinitionStore.getByType('elevation'); addGroup('Elevations', elevViews, '(none created)');`",
        "apps/editor/src/engine/views/SplitViewManager.ts:1225-1264 — the `// ── Plan / Section / Elevation (Canvas2D) ───` branch of `_onViewSelectChange`: `_activateMode('plan')`, `this._planViewId = viewId`, `_configureCanvasForView`, `_syncPlanCanvasState`, emit — no projection call anywhere in the branch",
        "packages/core-app-model/src/views/PlanViewCanvas.ts:389-401 — `const drawing = viewTechnicalDrawingCache.get(viewId); if (!drawing) { ... ctx.fillText(placeholderText, w / 2, h / 2); return; }`",
        "apps/editor/src/engine/views/PlanViewManager.ts:944-951 — `// The split view's elevation/section scope changed while a different // view is active in the main panel.` then `viewTechnicalDrawingCache.invalidate(viewId); this._ensureProjectionForSplitView(nextDef);` — reached only from the `viewId === this._activeSplitViewId` arm of a `vd:view-updated` handler",
        "apps/editor/src/engine/views/SplitViewManager.ts:494-499 — the C59 note the founder's brief cites: \"C59 §1.1 names that as exactly what `bim-elevation-2d` / `bim-section-2d` need before they can be pane-hosted — *\"a per-pane view-definition id, which is Phase 3's per-pane view state\"* — and this select is where that id lives today.\""
      ],
      "userVisibleSymptom": "Pick a section from the plan pane's view selector and the pane shows the empty-state placeholder rather than the section, until something else independently causes that view to project. The pane is not broken and the section is not broken — they are simply never introduced to each other. Combined with the previous finding, the honest answer to \"can a user produce a section today?\" is: only by drawing a section mark AND then reaching it by a route that projects, with the GIS surface off.",
      "fix": "Do not remove the read-only contract; add an explicit warm-up. On selecting a view id whose `viewTechnicalDrawingCache.get(id)` misses, call the existing `PlanViewManager._ensureProjectionForSplitView(def)` (or `ViewController.requestBackgroundProjection(viewId)`, which already exists precisely for 'project a view that is not active' and returns a typed `ProjectionOutcome`). `requestBackgroundProjection` is the better fit — its header explicitly records that it used to return silently and was changed so callers can distinguish 'no projection will run' from 'projecting', which is exactly the ambiguity here. Render a 'Generating projection…' state while it runs rather than the 'Add walls…' placeholder, which is actively misleading for a section.",
      "effort": "small",
      "risk": "low",
      "confidence": "high",
      "alreadyExists": "`ViewController.requestBackgroundProjection(viewId)` (apps/editor/src/engine/ViewController.ts:662) already does exactly this job for the sheet editor, already returns `ProjectionOutcome`, and already emits `svp:drawing-refreshed`. `PlanViewManager._ensureProjectionForSplitView` (:1096) is a second existing entry point. Nothing needs building.",
      "filesToChange": [
        "apps/editor/src/engine/views/SplitViewManager.ts"
      ]
    },
    {
      "title": "Two rival elevation implementations share the name 'Front': the ViewCube preset is a PERSPECTIVE 3D camera with plan symbols explicitly disabled, and it is what runs whenever no ViewDefinition id is queued",
      "mechanism": "`ViewController.activate` computes `routedViewDef` from `this._activeDefinitionId` (:1478). If a definition IS queued and its type is elevation/section, DOC-19B intercepts and hands off to the Canvas2D `PlanViewManager` (:1513). If NOT — the ViewCube, `BottomActionMenu`, or any caller that passes a bare mode string — control falls through to `_activateElevationView` (:2194), which is a fundamentally different product: it calls `applyElevation` then immediately `this._camera.projection.set('Perspective')` under a comment reading \"EL-FIX-2: Elevation views are CAMERA PRESETS, not locked ortho views\", restores orbit controls, and states \"PLAN_SYMBOL_LAYER stays disabled — deactivate() already disabled it\" (:2210). So one 'Front' is a scaled orthographic technical drawing on Canvas2D and the other is a freely-orbitable perspective 3D view with the symbol layer off. Both are labelled 'Front'. Note `ViewsRailPanel` DOES queue the id (`setActiveViewDefinitionId(viewId)` at :831) before `onViewSelect`, so the rail reaches the Canvas2D one — the divergence bites on the ViewCube and any other bare-mode caller.",
      "evidence": [
        "apps/editor/src/engine/ViewController.ts:1478-1480 — `const routedViewDef = this._activeDefinitionId ? viewDefinitionStore.get(this._activeDefinitionId) : undefined;`",
        "apps/editor/src/engine/ViewController.ts:1527 — `await this._activateElevationView(viewMode, obcView);` — the final `else`, reached whenever no elevation/section definition is queued",
        "apps/editor/src/engine/ViewController.ts:2245-2249 — `// EL-FIX-2: Elevation views are CAMERA PRESETS, not locked ortho views. // Switch back to Perspective immediately after the camera is positioned so // that left-drag orbits freely in full 3-D rather than panning a flat ortho // projection.` then `this._camera.projection.set('Perspective');`",
        "apps/editor/src/engine/ViewController.ts:2210 — `// PLAN_SYMBOL_LAYER stays disabled — deactivate() already disabled it.`",
        "apps/editor/src/ui/ViewCube.ts:32-35 — `*   FRONT  → 'Front'  → _activateElevationView` (and BACK/LEFT/RIGHT) — the ViewCube routes to the perspective preset by design",
        "apps/editor/src/ui/ViewBrowser/panels/ViewsRailPanel.ts:831 — `window.viewController?.setActiveViewDefinitionId(viewId);` — the rail queues the id, so it lands on the Canvas2D path"
      ],
      "userVisibleSymptom": "Reaching 'Front' by the ViewCube gives an orbitable 3D perspective with no plan symbols; reaching the North/South/East/West elevation by the Views rail gives a flat 2D drawing. Same word, two different things, and a founder comparing them will read the difference as the elevation view being intermittently broken. It also means a bug report about 'the elevation' is ambiguous until you know which route produced it — worth insisting on before diagnosing any future elevation complaint.",
      "fix": "No code change proposed with confidence — I could not establish whether the perspective preset is still wanted. The ViewCube's own header documents the routing as deliberate, and `_activateElevationView`'s EL-FIX-1/2/3 comments read as a considered decision that a cube face should snap-then-orbit rather than lock. The actionable part is DIAGNOSTIC: label them differently in the UI and in logs (e.g. 'Front (camera)' versus the ViewDefinition's own name), so the next elevation bug report names which one. If the founder wants one elevation, the cube faces should queue the corresponding default elevation ViewDefinition id (`DEFAULT_ELEVATION_VIEWS` already gives four, N/E/S/W) before activating — that is a small change, but it changes the ViewCube's documented behaviour and needs his ruling first.",
      "effort": "small",
      "risk": "medium",
      "confidence": "medium",
      "alreadyExists": "`DefaultViewsManager` already creates four default elevation ViewDefinitions with correct `spatial.projectionDirection` (`DEFAULT_ELEVATION_VIEWS`, DefaultViewsManager.ts:682-704), so the ids the ViewCube would need to queue already exist on every project. `ViewsRailPanel._resolveElevationDirection` (:838) already maps a projectionDirection to a Front/Back/Left/Right OBC id and is the inverse mapping.",
      "filesToChange": [
        "apps/editor/src/ui/ViewCube.ts",
        "apps/editor/src/engine/ViewController.ts"
      ]
    },
    {
      "title": "Auto-dimension covers plan and elevation and honestly refuses section — this one is NOT a defect and should be left alone",
      "mechanism": "`autoDimensionActiveView` routes by view type through a pure, exported, unit-testable `resolveAutoDimensionStrategy`: plan-family to `applyAutoDimensions`, elevation-family to `applyElevationAutoDimensions`, everything else to `'unsupported'`, which emits a toast naming the reason. The header states why section is excluded rather than approximated: \"A section's vertical rule set is genuinely different again (it measures the CUT: structural zones, ceiling voids, head heights on the cut plane — not just the façade datums), and shipping the elevation rules under a section's name would produce a drawing that is confidently wrong.\" I am reporting this because the founder's brief asks how far behind elevations and sections are, and on this axis elevation is NOT behind — and the section gap is disclosed, not hidden. Any plan that treats all elevation/section gaps as the same kind of gap will mis-cost this one.",
      "evidence": [
        "apps/editor/src/ui/documentation/autoDimensionActiveView.ts:13-15 — `//   plan / ceiling-plan / structural-plan  → applyAutoDimensions          (horizontal, XZ)` / `//   elevation / building-elevation         → applyElevationAutoDimensions (vertical, world-Y)` / `//   section                                → NOT YET. See below.`",
        "apps/editor/src/ui/documentation/autoDimensionActiveView.ts:16-20 — `// SECTION is deliberately not routed. A section's vertical rule set is genuinely // different again (it measures the CUT: structural zones, ceiling voids, head heights // on the cut plane — not just the façade datums), and shipping the elevation rules // under a section's name would produce a drawing that is confidently wrong.`",
        "apps/editor/src/ui/documentation/autoDimensionActiveView.ts:63-66 — `viewType === 'section' ? 'Auto-Dimension: sections are not supported yet — open a plan or an elevation.' : 'Auto-Dimension: open a plan or an elevation view first.',` — the refusal names the reason and the escape route",
        "packages/auto-dimension/src/elevation/planElevationAutoDimensions.ts and packages/auto-dimension/__tests__/planElevationAutoDimensions.test.ts — the elevation engine exists with its own suite"
      ],
      "userVisibleSymptom": "None on plan or elevation. On a section, Auto-Dimension shows a toast explaining sections are not supported yet and pointing at plan/elevation. That is the correct behaviour for an unbuilt capability per C102 §4 V-RE-1 and [[refusing-half-needs-its-escape-hatch]].",
      "fix": "No fix. Do not route section to `applyElevationAutoDimensions` to 'close the gap' — the header explains exactly why that produces a confidently-wrong drawing, and a section rule set (structural zones, ceiling voids, head heights on the cut plane) is a genuine build, not a copy. If it is scheduled, it is a third consumer of the same `@pryzm/auto-dimension` engine and belongs behind the existing `resolveAutoDimensionStrategy` switch as a `'section'` case, following `SPEC-AUTODIMENSION` §12.",
      "effort": "large",
      "risk": "low",
      "confidence": "high",
      "alreadyExists": "`packages/auto-dimension/` with `planAutoDimensions.ts` and `elevation/planElevationAutoDimensions.ts`, both tested. `resolveAutoDimensionStrategy` is already the extension point, pure and exported specifically so the routing rule is testable without a runtime. C56-AUTODIMENSION.md, ADR-0118, ADR-0119 and SPEC-AUTODIMENSION.md all exist.",
      "filesToChange": []
    }
  ],
  "whatIcouldNotEstablish": "1. I DID NOT RUN THE APP. Every finding is static: code read, plus one node probe that re-implements `PocheFillBuilder._parseSegments`/`_stitchClosedLoops` verbatim to prove the section collapse. I did not open a section in a browser and confirm the absent poché on screen. The probe is strong evidence about the algorithm; it is not a screenshot. Given [[committed-is-not-reachable]] and [[probe-can-be-wrong-three-ways]], the next lane should open one real section before spending on the fix — the fastest confirmation is to log `polygons.length` in `PlanViewCanvas._renderPocheFills` on a section view.\n\n2. RIVAL I COULD NOT FULLY RULE OUT ON THE POCHÉ FINDING: I proved the collapse for a section plane whose normal is cardinal (along Z, and along X). For an OBLIQUE section, the cut ring's XZ projection is a genuine 2D shape rather than a line, so `PocheFillBuilder` would return a NON-empty but GEOMETRICALLY WRONG polygon — a foreshortened smear rather than nothing. So \"sections have no poché\" is proven for the cardinal case; the oblique case is likely \"wrong poché\", which is worse and which I did not probe. Either way the fix is the same.\n\n3. THE FOUNDER'S \"LINE TYPE ETC DOESN'T WORK\" — I did not trace it. The founder reported it on a PLAN, and my dimension is elevation/section, so I stopped at establishing that `VGSceneApplicator.applyToProjectionLayers` and `resolveIntentStyle` are called identically on all three paths. Whether the line-type control actually reaches the pen is UNKNOWN to me and belongs to the plan-dimension lane. I specifically did NOT verify whether the intent's line-type field is honoured in elevation/section, only that the same resolver is invoked.\n\n4. LINE WEIGHT / READABILITY IN ELEVATION AND SECTION — UNKNOWN as a measured quantity. I established the structural cause of poor section readability (no poché, 1 of 15 symbol builders, no gridlines), but I did not measure pen widths. `PenWeightByFunction.test.ts`, `CanvasHairlineFloor.test.ts` and `BeyondDashByViewType.test.ts` exist and suggest the pen table is view-type aware; I read none of them. If the founder's \"lines almost not readable\" turns out to be a stroke-width problem rather than a content problem, nothing in my report addresses it.\n\n5. WHETHER THE ELEVATION POCHÉ COLLAPSE IS REACHABLE IN PRACTICE. `_ELEVATION_SCOPE` has `pocheRequiresExplicitIntent: true`, so elevation poché paints only where a user has declared a cut fill for (elevation x category) via `viewTypeModifiers`. I did not check whether any shipped system intent declares one. If none does, the elevation half of the poché bug is latent rather than live — but it will fire the moment anyone uses the feature `elevationCutPocheIsIntentDeclared.test.ts` was written to certify.\n\n6. THE `_activateElevationView` DIVERGENCE (finding 7) is my lowest-confidence item and I marked it medium. I established the two code paths and that they produce different camera projections and different symbol-layer states. I did NOT establish which one a user hits most often, nor whether the perspective preset is still wanted — the comments defend it as deliberate. I propose no code change there, only labelling, and it needs a founder ruling.\n\n7. C102 / C101 / C59 CROSS-CHECK IS PARTIAL. I read C102 §4 (reachability) and confirmed the general shape, but I did not read C101 (Annotation) or C59 in full, and I did not check whether any of these findings is already an open row in the ISSUE-LOG under an L-number. The ISSUE-LOG grep for \"poch\" returned L-119 (elevations rendering as solid black, FIXED via ViewScope) — related but the opposite defect, and not this one. I found no existing row for the section poché collapse, but I did not read all 82KB of results, so \"this is new\" is my impression rather than a measurement."
}