{
  "refuted": false,
  "reason": "I could not construct a rival that displaces this diagnosis. Every mechanism it names is real in the code, and I checked each one rather than trusting the quote.\n\nCONFIRMED, with exact lines:\n- `GISAreaLayout.ts:5179` is the refusal branch; it returns at `:5362`. `buildMassingOptionsFold` is called at `:5790` and `wireMassingOptions(panel)` at `:5863` — both after that return, in the full-determination arm. Repo-wide there is exactly ONE call of each (grep over `apps/editor/src`, excluding tests). `renderEnvelopeAbsencePanel` (`:4831-4938`) renders no fold either.\n- `buildAuthoredMassingOptionHtml` has exactly one call site, `envelopeCardSections.ts:582`, inside the fold — so the button whose own doc says \"Present on every arm — the tool is never gated\" (`massingAuthoredOptionSection.ts:43`) is structurally unreachable on two of three arms. `wireAuthoredMassingOption` likewise has one call site, inside `wireMassingOptions` (`:4042`).\n- `envelopeCardSections.ts:598-608`: the `!set.ok` arm returns without `authoredCard`. Confirmed.\n- `enumerateMassingOptions` has exactly ONE call site (`GISAreaLayout.ts:4010`), fed `permittedRing: env?.insetPolygon ?? []` at `:4011`, and hard-refuses at `massingOptionModel.ts:404`. On a refusal `insetPolygon` is `[]` (`zoneRefusal.ts:95`), so this is a genuine capability block, not a hidden affordance. Confirmed.\n- `:5240` `const safeStudyHeightEntry = (isAbsent || isGap) ? … : ''` and `:5312` `isGap`-only manual-zone button. For 22@ the refusal is `code:'derived-plan'`, `legallyGrounded:true` (`esBarcelonaZoneClassification.ts:870,918`), so isTransient/isAbsent/isGap are all false. Confirmed.\n- `designStageModel.ts:124` NO_ENVELOPE text and `:142` `reached('massing') = hasResolvedEnvelope || hasMassingProposal`; `:5114` computes `hasResolvedEnvelope` from `permittedStudyFigures(env).footprintM2 > 0`, which is 0 on a refusal (`parcelLawModel.ts:362-370` reads only `insetAreaM2`/`insetPolygon`). Confirmed.\n- The \"capability, not coupled\" half holds: `window.pryzmOpenSiteEnvelopeTool` is registered unconditionally (`:7208-7220`); `siteEnvelopeTool.ts:279-283` carries its own \"Draw the perimeter on this view\" button; `parcelLawEnvelopeAuthoring.ts:573` ranks the drawn ring first with no envelope precondition; `CesiumViewport.renderSpaceEnvelopes` (`:8821`) needs only `formaMassingOrigin` and reads `runtime.stores.spaceEnvelope`, never the BuildableEnvelope.\n\nTWO DEFECTS IN THE WRITE-UP, neither fatal:\n1. MISCITATION. `siteDispatch.ts:7833` is the Illes Balears MUIB path, not Barcelona. The Barcelona route is `siteDispatch.ts:8278` — `buildRefusedEnvelope(clau, disposition.refusal)` with NO third argument — and `'not-applicable'` comes from the DEFAULT parameter at `packages/site-parcel-data/src/rulepacks/zoneRefusal.ts:92`. The conclusion survives; the citation does not. Separately, that the founder's parcel is specifically clau-22@ is an inference — I found no repo record pinning it (the one logged Poblenou parcel, L-1661, resolved clau 18). It does not matter: clau 18 is also a legally-grounded `derived-plan` refusal and lands on the identical arm.\n2. THE FIX ORDERING IS WRONG. \"(1) … This alone restores the founder's route\" is false by the write-up's own evidence item 10. Fix (1) passes `{kind:'idle'}`, whose fold renders a \"Generate massing options\" button; pressing it on a refused parcel hits `massingOptionModel.ts:404`, flips the fold to its `refused` arm, and that arm drops `authoredCard` — deleting the button fix (1) just added. (2) is a PREREQUISITE of (1), not a smaller sibling.\n\nThe proposed weak falsifier is non-diagnostic: `window.pryzmOpenSiteEnvelopeTool()` WILL open a working draw-and-create panel, so its passing says nothing about the primary claim.",
  "evidenceChecked": [
    "apps/editor/src/ui/layout/GISAreaLayout.ts:5100-5400 — the refusal arm in full: chip/facts/cite/manual-zone/study-entry, innerHTML at :5329-5346, wire calls :5348-5361, `return` at :5362",
    "apps/editor/src/ui/layout/GISAreaLayout.ts:5740-5880 — the full-determination arm: `buildMassingOptionsFold` at :5790, `buildStagedSectionsHtml` at :5840 with the 'massing-options' key, `wireMassingOptions(panel)` at :5863",
    "apps/editor/src/ui/layout/GISAreaLayout.ts:4790-4950 — `renderEnvelopeAbsencePanel`: no massing fold, no authored card; study entry offered only when `hasCommittedParcel`",
    "apps/editor/src/ui/layout/GISAreaLayout.ts:3980-4060 — `wireMassingOptions`, the sole `enumerateMassingOptions` call at :4010 with `permittedRing: env?.insetPolygon ?? []` at :4011, and `wireAuthoredMassingOption` at :4042",
    "apps/editor/src/ui/layout/GISAreaLayout.ts:3805-3850 — a SECOND ring-coupled consumer the write-up missed: `solveTargetFootprintArea({ permittedRing: env?.insetPolygon ?? [] })` at :3830, which is the 'plate' rung of the authoring ladder",
    "apps/editor/src/ui/layout/GISAreaLayout.ts:4355-4365 — local `permittedStudyFigures` is a pass-through to `permittedStudyFiguresOf`; :5114 uses it for `hasResolvedEnvelope`",
    "apps/editor/src/ui/layout/GISAreaLayout.ts:7180-7250 — `window.pryzmOpenSiteEnvelopeTool` registration, explicitly ungated",
    "apps/editor/src/ui/site/envelopeCardSections.ts:487-660 — `buildStagedSectionsHtml` (greys, never hides) and `buildMassingOptionsFold`'s three arms; `authoredCard` built at :582 and absent from the `!set.ok` arm at :598-608",
    "apps/editor/src/ui/site/massingAuthoredOptionSection.ts:36-50, :67, :182 — the 'never gated' doc comment and the two exported builders",
    "apps/editor/src/ui/site/massingOptionModel.ts:380-430 — `enumerateMassingOptions` and the `no-permitted-footprint` guard at :404",
    "apps/editor/src/ui/site/designStagePanel.ts:100-190 — `describeEnvelopeCardSections`: relevance is lead/neutral/gated, nothing is removed, so the section plan is not a second gate",
    "apps/editor/src/ui/site/designStageModel.ts:110-160 — NO_ENVELOPE at :124 and `reached('massing')` at :142",
    "apps/editor/src/ui/site/parcel/parcelLawModel.ts:362-371 — `permittedStudyFiguresOf` reads only `insetAreaM2`/`insetPolygon`, so a refusal yields 0",
    "apps/editor/src/ui/site/siteDispatch.ts:7790-7860 — the cited :7833 line, which is the Illes Balears MUIB path, NOT Barcelona",
    "apps/editor/src/ui/site/siteDispatch.ts:8210-8320 — the actual Barcelona route: `resolveZoneDisposition` → `buildRefusedEnvelope(clau, disposition.refusal)` at :8278 with no status argument",
    "apps/editor/src/ui/site/siteDispatch.ts:930-1000 — §ENVELOPE-ZERO-INSET-REFUSAL, which returns null from a ring resolver rather than choosing a card arm",
    "packages/site-parcel-data/src/rulepacks/zoneRefusal.ts:89-119 — `buildRefusedEnvelope` default `status = 'not-applicable'`, `insetPolygon: []`, `insetAreaM2: 0`",
    "packages/site-parcel-data/src/rulepacks/esBarcelonaZoneClassification.ts:859-925 — `barcelona22ArrobaDerivedPlanRefusal`: `code:'derived-plan'`, `legallyGrounded: true`",
    "apps/editor/src/ui/analysis/parcelLawEnvelopeAuthoring.ts:540-665 — `resolveFootprintSource`'s five rungs; the drawn rung at :573; the 'none' rung's pointer to the study-height control at :655-665",
    "apps/editor/src/ui/analysis/parcelLawEnvelopeAuthoring.ts:202-276 — the section's testids: create, storeys, discard-drawn, add-levels, lawcheck — and NO draw button of its own",
    "apps/editor/src/ui/analysis/parcelLawTab.ts:1120-1200, 1310-1320 — `authoringSlot` appended to `bodyOf('law')` as a SIBLING of the envelope-card slot, and `mountAuth(authoringSlot, allowanceSlot)` called unconditionally",
    "apps/editor/src/ui/site/siteEnvelopeTool.ts:99-115, 277-285 — the tool panel's own `SITE_ENVELOPE_DRAW_BTN_TESTID` 'Draw the perimeter on this view' button, and `mountParcelLawEnvelopeAuthoring` at :448",
    "apps/editor/src/ui/gis/gisActionRegistry.ts:479-500 — the `site.create-envelope` row, no `unavailableReason`",
    "apps/editor/src/ui/ViewBrowser/ProjectBrowserPanel.ts:989 — `renderGisActions(window)` called unconditionally in the GIS tab",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:8821-8870 — `renderSpaceEnvelopes` reads `runtime.stores.spaceEnvelope` and needs only `formaMassingOrigin`",
    "docs/03-execution/plans/SESSION-HANDOVER-2026-09-07-PM.md §1-§4 — the shipped state of massing shapes, create-it-myself and envelope draw, and the ten open founder defects",
    "docs/04-reference/ISSUE-LOG.md — searched for a 'massing-without-envelope' row; none exists, so the defect name is the orchestrator's framing, not a logged row"
  ],
  "betterHypothesis": "A REFINEMENT, not a replacement — the diagnosis is right about the mechanism but wrong about which route is deleted and therefore wrong about which fix unblocks the founder.\n\n\"The routes to a capability that already works are deleted along with the legal answer\" overstates it. TWO ungated routes survive on the refused parcel, and I verified both:\n\n(a) `renderGisActions` renders EVERY declared action, and `site.create-envelope` (gisActionRegistry.ts:489) carries no `unavailableReason`. It is rendered unconditionally in the Project Browser GIS tab (ProjectBrowserPanel.ts:989), and the panel it opens carries its own \"Draw the perimeter on this view\" button (siteEnvelopeTool.ts:281-283). The draw → extrude → Create loop is reachable TODAY on a 22@ parcel; it is simply not where the founder looked.\n\n(b) More important: on the Parcel Law tab, `mountParcelLawEnvelopeAuthoring` is mounted into its OWN slot — `authoringSlot`, appended to `bodyOf('law')` at parcelLawTab.ts:1154-1157, mounted at :1317 — a SIBLING of the envelope-card slot, not a child. So `panel.innerHTML = …` inside `refreshEnvelopePanel` never touches it, and the whole Create-envelope block (footprint ladder, storeys, discard-drawn, live law check) survives on the refusal arm intact.\n\nSo the sharper defect is a DEAD POINTER inside the surviving surface, not a deleted fold. On a refused parcel with nothing drawn, `resolveFootprintSource` falls to its `'none'` rung (parcelLawEnvelopeAuthoring.ts:655-665), whose sentence is the user's only instruction: *\"on the site card, under 'Don't know the height? Type one for a study massing', enter a height and a setback and press 'Build study from this height'\"*. That control is withheld at GISAreaLayout.ts:5240 for exactly `legallyGrounded === true` refusals — i.e. precisely the parcels on which that rung fires. The one surviving on-card instruction names a control that does not exist. That is §REFUSING-HALF-NEEDS-ITS-ESCAPE-HATCH (L-942) verbatim, and it is the item the write-up ranked LAST and marked \"OPEN FOR THE FOUNDER — do not decide unilaterally\".\n\nAnd the ladder collapse is worse than stated, because there is a FIFTH coupling site the write-up missed: `solveTargetFootprintArea` is fed the same `permittedRing: env?.insetPolygon ?? []` at GISAreaLayout.ts:3830, and it is the producer of the ladder's 'plate' rung. So on a refusal the five-rung ladder reduces to drawn → study → none, and BOTH surviving rungs are gated by the two affordances the refusal arm withholds (the draw entry inside the massing fold, and the study-height entry). That is the single sentence the diagnosis is reaching for: the refusal arm does not merely hide the massing fold — it removes every INPUT to the one authoring surface it leaves standing.\n\nConsequences for the fix, in priority order:\n- The study-height entry on the legally-grounded refusal card is not an optional extra to be deferred to the founder; it is the fix that makes the already-shipping, already-mounted authoring block usable. Ship it or change the `'none'` rung's sentence — the current pair is incoherent either way, which is a defect independent of any founder ruling.\n- (2) must land with or before (1), for the reason the write-up's own evidence item 10 gives: `{kind:'idle'}` renders a Generate button that flips the fold to the `refused` arm, which drops the authored card.\n- (3) remains the only item that changes what PRYZM can COMPUTE; (1)/(2)/(4) are affordance and wording.\n\nNone of this refutes the claim. It relocates the primary blocker from \"the fold is arm-gated\" (true, but routed around by two surviving surfaces) to \"the surviving surface's only named escape hatch is withheld on exactly the arm that needs it\"."
}