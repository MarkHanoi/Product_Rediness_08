{
  "survivingTitles": [
    "The open has no owner and no end: `await openProject()` resolves BEFORE the project loads, so the whole expensive half is unsequenced, unmeasurable and uncancellable",
    "The renderer backend is destroyed and rebuilt at load-END, on a decision that was knowable at load-START — the whole scene is uploaded to backend A and then re-uploaded to backend B",
    "Every non-new open awaits a full server snapshot fetch that is DISCARDED whenever local history exists — and it is serialized AFTER the engine boot instead of alongside it",
    "The post-load window is a 35-subscription broadcast with three uncoordinated timers — this is the interval §PERF104 explicitly declined to own, and it is where the founder's wait lives",
    "Two independent socket.io connections join the same project room on every open — two WebSocket handshakes and two server-side DB access checks per user per open",
    "Opening a project WRITES it back: a read-only open produces a full 503-element serialize, deflate, IndexedDB write, two forced synchronous renders and two thumbnail captures"
  ],
  "refutedTitles": [
    "26.3 MB of uncompressed JavaScript — including a 5.6 MB render-BLOCKING classic `<script>` — must be parsed before the startup instrument's t0, so it is invisible in every founder paste",
    "P1 and the layer gate both PASS, but the open path routes around the typed runtime through 338 `window.X =` globals — which is the mechanical reason no orchestrator can exist",
    "God objects: 31 files over 2,000 LOC hold 21% of the editor app in 2.4% of its files — a maintenance cost, and (contra the brief's hypothesis) NOT demonstrably an open-latency cost"
  ],
  "refutations": [
    "Finding 6 (26.3 MB bundle / blocking Cesium script) — REFUTED ON THIS DIMENSION, not on its facts. Every number verified: `ls` gives dist/cesium/Cesium.js = 5,909,848 bytes and dist/assets/main-Bzw-2t7Y.js = 9,357,174; dist/index.html line 5 is a bare `<script src=\"/cesium/Cesium.js\">` inside `<head>` with no defer/async; vite.config.ts:55-82 states the byte count and the blocking-classic-script fact in its own words, and NO_GLOBE = {'component-editor.html'} confirms index.html deliberately keeps it. But this is not an open-path cost and the finding concedes it in its own body ('per PAGE LOAD, not per in-tab open'). It is invisible to the founder's paste because it happens before t0 — and it also happens before the open gesture. Shipping it on the architecture-orchestrator dimension sends the next lane at Cesium while the founder's measured tail is entirely post-load. Re-file to a bundle/first-paint dimension; do not discard.",
    "Finding 7 (338 window globals) — REFUTED AS A FINDING, kept as scoping context for Finding 1. All its evidence is correct: I ran `npx tsx tools/ga-gate/check-single-compose.ts` myself → RC=0, 'files scanned: 5537 · composeRuntime definitions: 1 · rival runtime factories: 1/1 · production callers: 2/2', matching the quote verbatim; ProjectLifecycleController.ts:115-119 and :131-140 are verbatim as cited (`(window as any).__engineTeardown?.…`, `__curtainWallRebuildControl`, `__slabRebuildControl`) inside a file whose header forbids `src/` imports; my own regex reproduces the census within noise (331 distinct `window.X =` targets across apps/editor/src + src, 215 in engine, vs the claimed 338/219). But the finding says of itself 'this is not a gate violation' and 'Finding 1 restated at its root', and claims -1ms with basis unknown. It is a duplicate of Finding 1 with a scoping list attached. The ~8-global list is the right content for Finding 1's fix section, not a separate row a lane would be dispatched against.",
    "Finding 8 (god objects) — REFUTED, and the auditor refutes it themselves in the mechanism field ('On the LATENCY question the honest answer is negative'). I confirmed the substrate rather than the claim: LOC exact (initScene.ts 5571, GISAreaLayout.ts 8532, initTools.ts 4135, initUI.ts 3579, ProjectLoader.ts 3333); SPEC-PROJECT-OPEN-CREATE-PIPELINE.md:86 carries O-INV-1 verbatim including 'the heavy O4–O8 LONGTASKs are a one-time cold-boot cost per tab, not a per-open cost'; `_bootstrapped` is at src/main.ts:203 and set only after bootstrap() resolves; GISAreaLayout.ts:1084 constructs CesiumViewport only inside `ensureGisInitialized`. So every fact holds and every fact establishes that these files are NOT per-open cost. This is a maintainability observation in a performance audit. Its one live residue — the 348 `.traverse(` sites / absent SceneRegistry — is a real cost but belongs under Finding 2's mesh-count provider, not under a LOC census, where it will be read as 'refactor big files'."
  ],
  "missed": [
    "THE OPEN SERIALIZES THREE LEGS AND THE AUDIT ONLY FOUND THE LAST TWO. `buildPersistence.openProject` step 1 (packages/runtime-composer/src/buildPersistence.ts:266-274) awaits `controller.refresh()` — a server project-list round-trip — whenever `projectListStore.isEmpty()`, and then awaits it a SECOND time if the summary is still missing ('Deep-link scenario: refresh and retry once.'). Only after that does :307-309 await `attachedBootstrap.ensure()` (the whole engine boot), and only after THAT does :316 await `tier.streamLoad`. So a deep-linked or hard-refreshed open pays up to TWO list round-trips before the boot even starts. The file's own comment at :232 names the window as 'a network refresh plus an engine boot ... seconds wide on a real hub'. Finding 3 fixes leg-3-after-leg-2 and leaves leg-1-before-leg-2 entirely untouched.",
    "THE PROGRESS CHANNEL REPORTS 100% BEFORE A SINGLE ELEMENT EXISTS. Finding 1 names the un-awaited promise but not its user-visible face: `emitProgress('painting', 80)` fires at buildPersistence.ts:324 and `emitProgress('done', 100)` at :341, immediately after the `await attachedSurface.setProjectContext(...)` that returns synchronously. The `persistence.openProgress` event the overlay consumes therefore reaches 'done/100' before the hydrate begins. A founder watching a bar hit 100% and then waiting is a distinct symptom from an un-awaited promise, and it is the one he would describe as 'takes too long to open'.",
    "THE 35-WAY FAN-OUT IS A FLOOR THAT GROWS ONE HANDLER PER OPEN. `PlatformShell.setProjectContext` calls `this.saveCtrl.schedulePostLoadThumbnailCapture(id)` on EVERY open (PlatformShell.ts:253), and that method subscribes a fresh `pryzm-project-loaded` handler each time (PlatformSaveController.ts:583). It unsubscribes only from INSIDE the handler — and the handler's first branch, `if (detail.projectId !== projectId) return;` (:534), returns WITHOUT unsubscribing. Switch away from project A before its load event fires and A's handler stays subscribed for the life of the tab. Contrast `_armPostSceneMarks`, which does it correctly (`this._postSceneMarkUnsub?.(); this._postSceneMarkUnsub = null;` on re-arm, PlatformShell.ts:188-189). So Finding 4's '35 subscriptions' is a lower bound that rises monotonically within a session — a broadcast with no owner cannot notice its own audience growing, which is precisely the dimension's thesis.",
    "A SYNCHRONOUS localStorage FULL-KEY SCAN IS DISPATCHED INTO THE UNOWNED WINDOW. `PlatformShell.setProjectContext` fires `void warmVersionCache()` at :299 — fire-and-forget, concurrent with the load — and `warmVersionCache` (ProjectRepository.ts:971-996) runs `for (let i = 0; i < localStorage.length; i++)` plus per-key `getItem` / `putVersions` of compressed payloads, all synchronous main thread. Gated on legacy `bim-project-<id>-versions` keys still existing, so it may be a no-op on the founder's box — lower confidence than the three above. It is listed because it is main-thread work injected into the window nothing owns or sequences, with no completion signal, which is the exact failure mode Finding 1 describes and Finding 4 measures.",
    "EVIDENCE CORRECTION THE NEXT LANE NEEDS ON FINDING 2 (the finding survives; its reading of the log is backwards in one place). The `[pryzmPerf] NAV-BACKEND-SWAP-HEADROOM ... 162 elements ... 3762 meshes (ARM TRIPPED)` line is printed by `logSceneCensusOnce` (pryzmPerfConsole.ts:547), and its ONLY call site in the repo is inside `fireSwapToWebGL`'s `one-second-after-swap` timer (initScene.ts:5182). That line is therefore NOT the input to the swap decision — it is proof the swap already fired and completed on this run, and its counts are a POST-swap census. The '(unattributed)' line in the founder's paste comes from the same function, same call, confirming it. Consequence: the 3,762/162 pair cannot be used to calibrate a proactive threshold, which is what the finding's own 'would break' note gropes toward. Separately, the two `WebGPU device lost: reason=\"destroyed\"` lines are documented at packages/renderer-three/src/rendererRetirement.ts:393-430 as the NORMAL, deliberate end of EVERY retirement — a live swap, a device-loss rebuild, or any adapter teardown — and are quoted there verbatim as a founder sample; they corroborate a teardown but do not by themselves identify the heavy-scene swap. The census line does. Also note the second reactive caller the finding omits: initBatchLifecycle.ts:121 `maybeAutoSwitchToWebGLForHeavyScene(world?.scene?.three, 'gpu-compile-start')`, which passes NO mesh count and so rides the 400-element arm alone.",
    "COST FIGURES TO RETRACT BEFORE THIS AUDIT IS ACTED ON. (a) Finding 3's '400ms, confidence medium' is unmeasured, and the DISCARD half is unproven for the founder's run: the discard requires the synchronous mirror read at PlatformShell.ts:296 to hit, and the code itself documents that the mirror can be cold on a deep-link/hard-reload (§VERSION-QUOTA-INDEXEDDB), in which case `prefetchedVersion` IS consumed at :351-361. The paste contains neither `[PlatformShell] Auto-restoring latest local version` nor `[persistence.tier.streamLoad] version \"...\" loaded`, so which branch ran is not established. The (b) half — start the fetch before `await attachedBootstrap.ensure()` — is unconditionally correct and needs no branch determination. (b) is the shippable half. (b) Finding 5's '150ms, confidence high' is not supportable: neither socket connect is awaited by anything on the open path, so the duplication is concurrent work, not blocking latency. The honest cost is one extra WS handshake plus one extra `getSupabaseClient()` + `canUserAccessProject` per open (server.js:577-587), contending with the open's own queries. The duplication and the missing self-userId filter (initCollaboration.ts:614, against `socket.to(room).emit('user-joined')` at server.js:613-616) are both verified and both worth fixing; the millisecond figure is not. (c) Finding 9 mis-describes `getImageData` at initPersistence.ts:220 as a full-scene GPU→CPU readback — it is a 32×32 blank-detect probe off an already-downscaled ≤400×225 canvas. The real cost is the forced synchronous `rpm.render()` at :175-177, the `drawImage` off the presented canvas, and the WebP encode ladder. The finding's fix (a) (de-duplicate the two captures at PlatformSaveController.ts:357 and :551) is safe and independent; its own §CONFIDENT-REGISTER-ROWS caveat on (b) is correct and must be honoured."
  ]
}