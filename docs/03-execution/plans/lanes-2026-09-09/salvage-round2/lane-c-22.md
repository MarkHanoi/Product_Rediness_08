{
  "survivingTitles": [
    "The heavy-scene backend swap fires MID-HYDRATION, so every GPU resource is minted on WebGPU and then re-minted on a classic WebGL renderer — this is the sole source of the 'device lost: destroyed' pair",
    "Two full-project serializes on every open — the `resetDirtyAfterLoad()` hash baseline is discarded by the auto-save that follows",
    "captureThumbnail runs twice per open from two uncoordinated schedulers, and each run forces a synchronous full-scene render plus a GPU readback",
    "Two socket.io clients connect and join the same project room — two call sites, two websockets, two server-side access checks",
    "NOT A DEFECT — the duplicated 'WebGPU device lost' log line is two handlers on ONE device, and both correctly refuse to recover"
  ],
  "refutedTitles": [],
  "refutations": [
    "SURVIVES #1 (backend swap), with TWO wording corrections the next lane must carry. The causal chain is code-confirmed and exhaustive: reason=\"destroyed\" means WE called device.destroy() (rendererRetirement.ts:394-410), and there are exactly three retireRenderer() call sites in non-test code — initScene.ts:5126 (step 7 of the live swap), createRenderer.ts:626 (inside the recovery block, downstream of the `if (isDeliberateDeviceDestroy(info)) return;` at createRenderer.ts:535, AND operating on a device whose `lost` promise has already resolved, so it cannot produce a second pair), and WebGPURendererAdapter.ts:333 in `dispose()`, which has ZERO call sites anywhere in apps/ or packages/ (grepped). So on an open with no user interaction the live swap is the only path that can print that pair. Independently corroborated: ISSUE-LOG L-1483 is the founder's own console printing `§AUTO-WEBGL-HEAVY … 251 elems / 2647 meshes / 7 levels; reason=tier:post-load`. Thresholds verified verbatim (SWAP_MESH_THRESHOLD=1000 at heavySceneSwapThreshold.ts:43; the mesh arm at autoWebGLHeavyScene.ts:109/240; the tier-pass call at initScene.ts:3008). CORRECTION A: 'MID-HYDRATION' is not established — the only reason tag ever observed is `tier:post-load`, i.e. a geometry-add during the post-load sweep, AFTER bulk hydration. The substance (scene built on WebGPU, device destroyed, draw state re-minted on the classic renderer) is unchanged, but a lane briefed to 'move the decision before hydration' may be optimising a window that is already past. CORRECTION B: the 1500 ms is unmeasured and the swap is NOT awaited — only the synchronous prefix (rAF stop + rpm.dispose(), initScene.ts:4968-4974) blocks; the rest is the 'Switching renderer…' cover (initScene.ts:4890) plus the first-frame material re-compile on the new backend. That last term is the real cost and nobody has timed it. Time it before committing to a fix.",
    "SURVIVES #2 (double serialize), on code, NOT on the log. Both getHash() calls are unconditional and both are the full walk: PlatformVersionController.ts:412 `resetDirtyAfterLoad()` → SaveOrchestrator.ts:589-596 → getHash → PlatformSaveController.ts:104-119 `ctx.saveAdapter.serialize()` + `stringify()`, and the adapter at initPersistence.ts:108-111 IS `ProjectSerializer.serialize`. Then SaveOrchestrator.ts:382 `const currentHash = this.getHash();` in executeSave() — a second full walk. The L-131 P3b stash only removes the THIRD walk (PlatformSaveController.ts:247-268 reuses the second); it never touched the baseline. I confirmed the other resetDirtyAfterLoad sites are not on this path (PlatformVersionController.ts:322 is exitVersionPreview; PlatformShell.ts:309/350 are the empty-project and no-local-version branches, and the founder had 6 local versions). ⚠ ONE PIECE OF COUNTER-EVIDENCE the auditor did not address: `[ProjectSerializer] Snapshot created:` is printed unconditionally on EVERY serialize (ProjectSerializer.ts:2082, no throttle, no guard), and the founder's paste shows it ONCE. Two serializes must print two lines. The paste is curated (it omits the whole load), so this is not disconfirming — but it is the cheapest possible confirmation and it has not been done. Before any fix lands, count `Snapshot created` lines in the FULL console. If there is only one, the baseline serialize is somewhere throwing into `resetDirtyAfterLoad`'s catch and the finding collapses.",
    "SURVIVES #3 (double thumbnail), with the cost re-attributed. Structurally exact: one implementation (initPersistence.ts:141), one log line per successful capture (initPersistence.ts:305), exactly two callers in the platform save path (PlatformSaveController.ts:357 auto-save, :551 post-load), the post-load one armed unconditionally from PlatformShell.ts:254 on every open, and its only guards are the three the auditor named (:535 projectId mismatch, :539 empty, :546 activeProjectId) — there is no 'already captured' check. The per-call cost is as described: forced `rpm.render()` (initPersistence.ts:174-175), drawImage readback, getImageData, and a 6-rung encode ladder (THUMBNAIL_ENCODE_LADDER, packages/core-app-model/src/preview/thumbnailBudget.ts:68-75). BUT: of the two, only the auto-save capture is on the critical path — the duplicate half sits at +3500 ms inside requestIdleCallback and costs open time only in the 'user is trying to interact' sense. The recoverable open-time saving is therefore ONE capture, and the auditor's fix (suppress the auto-save one, keep the deferred one) is the correct shape. One hazard the auditor missed in its own fix: captureThumbnail returns null on a blank swapchain read (initPersistence.ts:235) — so the auto-save capture may ALREADY be paying the full render+readback and producing nothing, which strengthens the fix but also means the measured saving could be smaller than 300 ms if the ladder never runs.",
    "CAVEAT on #4 (two sockets) — the duplicate is REAL but the 40 ms open-time cost is REFUTED. Everything structural checks out verbatim: PlatformShell.ts:250 `initSocketCollaboration` at setProjectContext → PlatformCollabPill.ts:168-191 builds `ctx.socket` and emits join-project; initCollaboration.ts:1133-1141 `onProjectLoaded` → loadAndConnect → connectSocket at :570-594 builds its OWN closure-local socket and emits join-project again; neither disconnects the other (each `connectSocket`/`initSocketCollaboration` only disconnects ITS OWN prior reference), so two sockets sit in the room for the session. The server does run `canUserAccessProject` per join (server.js:566-587 → server/projectAccess.js:118-125, a Supabase `projects` select). The two script ids differ exactly as claimed (PlatformCollabPill.ts:175 `plat-socketio-client` vs initCollaboration.ts:1146 `pryzm-collab-socketio-client`). What does NOT hold is the cost: both handshakes are async and off the main thread, the second join lands after `pryzm-project-loaded` has already fired, and the DB round-trip is SERVER-side. This costs ~0 ms of user-perceived open latency. Fix it for correctness (a leaked socket per session, a doubled auth round-trip per open, doubled server fan-out at scale) — do NOT budget it as open-path time, and do not let it outrank #1/#2/#3 on a 'takes too long to open' lane.",
    "SURVIVES #5 (the device-lost pair is not a defect) — verified correct, and it is the finding most worth keeping. Both handlers are on the same GPUDevice (`backend.device`, WebGPURendererAdapter.ts:219-221 and createRenderer.ts:517), both log verbatim first, and both then call the single shared authority and return (WebGPURendererAdapter.ts:236 and createRenderer.ts:535). Neither fires a recovery. The auditor's refusal to count this as a duplicated RESET — while still counting the underlying single destroy as finding #1 — is the correct split, and it is the one place in this audit where a naive reading of the console would have sent a lane at a phantom."
  ],
  "missed": [
    "THE OPEN RE-PERSISTS WHAT IT JUST READ — and this is bigger than the baseline serialize the auditor found. The founder's open reads a stored version, hydrates 503 elements, and then writes a brand-new 'Auto-save' version of that same content back to IndexedDB (`[PlatformSaveController] Version saved: \"Auto-save\" (503 elements)`). The only delta is whatever the post-load sweep (wall joins, room re-detect) mutated. Finding #2 attacks one of the two serializes; the honest question is why the open produces a version write AT ALL. If the sweep's mutations are deterministic re-derivations of the loaded snapshot, the entire autosave — serialize, deflate, container write, thumbnail, server sync queue entry — is duplicated work on the critical path, and killing it is worth several times the 400 ms of the baseline serialize alone.",
    "THE VERSION-CONTAINER WRITE IS O(HISTORY), NOT O(NEW) — the auditor stopped at the serialize and never followed the bytes. `_commitSlots` (apps/editor/src/ui/platform/ProjectRepository.ts:2729-2783) re-assembles the FULL container from all slots — `_assembleContainer(entries, storedJournal)` — and does `putVersions(projectId, payload)` for the whole thing on every save. Compression is genuinely O(new) (unchanged versions reuse cached blobs, :2712), but the payload string assembly and the IndexedDB put are not: the founder's own line proves it, `6 version(s) persisted to IndexedDB (~1.4 MB (1,465,135 chars) compressed)` — five of those six were byte-identical to what was already stored. That is a ~1.4 MB string concat plus a 1.4 MB IDB write, on the open path, to persist one new version. It scales with MAX_VERSIONS_STORED (20), so at a full ring the founder pays ~4-5 MB of re-write per open.",
    "THE JOURNAL, NOT THE STORES, IS THE DOMINANT TERM IN BOTH SERIALIZES — finding #2's fix is aimed at the wrong half of the cost. The auditor attributes the serialize cost to '~30 `store.getAll().map(deepStrip)` passes' (ProjectSerializer.ts:1565-1748). The serializer's own §PROBE-SNAPSHOT-JOURNAL-WEIGHT comment (ProjectSerializer.ts:2064-2079, L-5821) says the opposite in measured numbers: 'for a 264-element project the model serialises to ~0.1 MB while the founder's stored 20-version container is ~35 MB … the difference is `temporalGraph`, embedded WHOLE in every snapshot'. The founder's own line reports `temporalGraph 2366 mutations`. So each of the two serializes stringifies 2366 journal records that were themselves just restored from the loaded snapshot — read in, written out, twice. Any fix that dedupes the store walk but keeps embedding the whole journal in the dirty-check hash will move almost none of the 400 ms. The cheap win the auditor missed: the dirty-check hash does not need the journal at all.",
    "EACH captureThumbnail FORCES A SECOND FULL-SCENE RENDER IN A TICK THE rAF LOOP HAS ALREADY RENDERED. `rpm.render()` at initPersistence.ts:174-175 is a synchronous forward pass over the live 3762-mesh scene plus shadow casters, issued outside the frame bus, purely so the drawing buffer is populated for a 400x225 readback. With two captures that is two whole extra scene renders per open — the render is duplicated work in its own right, independent of the capture being duplicated. Lower confidence than the items above (I did not measure it, and on the post-load path it lands inside requestIdleCallback), but it means the per-capture cost is a full frame, not just an encode — which raises the value of the auditor's #3 fix rather than lowering it."
  ]
}