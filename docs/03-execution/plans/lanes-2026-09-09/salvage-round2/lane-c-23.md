{
  "survivingTitles": [
    "The open path AWAITS a full-snapshot server fetch whose result is thrown away whenever local history exists",
    "GET /api/projects/:id/latest-version is two SERIAL Supabase round trips, and the whole snapshot is parsed and re-serialised inside the Fly process",
    "Two independent socket.io connections are opened per project open — double handshake, double uncached DB access check",
    "GET /api/projects still SELECTs the base64 `thumbnail` column for 50 rows and then deletes it server-side",
    "When both Supabase and DATABASE_URL are configured, GET /api/projects runs BOTH list queries serially and merges them",
    "The snapshot is one unpaginated, unstreamed blob — the client-side streaming plane exists but the server endpoints it needs were never built",
    "One shared vCPU / 512 MB with a 50 MB JSON body limit — the autosave POST that fires DURING open contends with every open-path request on the same event loop"
  ],
  "refutedTitles": [
    "No cache on the per-request project access check — every project-scoped route pays an extra Supabase round trip before doing any work",
    "Two thumbnail captures per open produce two independent PATCH /thumbnail writes of the same project row",
    "Unbounded COUNT(*) on project_versions runs TWICE per save, on a table that grows per save and is never pruned",
    "GET /api/v1/portfolio is a true serial N+1 that loads the FULL snapshot of up to 50 projects — but it is NOT on the open path",
    "NEGATIVE FINDING, recorded so it is not re-diagnosed: the indexes on the open path are present and correct"
  ],
  "refutations": [
    "No cache on the per-request project access check — REFUTED as an OPEN-TIME cost. The code fact is true: `_httpAccessResult` (server.js:960-974) has no memo. But NOTHING on the open's awaited chain calls it. (a) `/latest-version` does NOT use it — it runs its own inline `.eq('owner_id', userId)` scope at server.js:3522, which IS finding 2's first round trip, so counting it here is double-counting the same query. (b) visibility-intents is NOT fetched on open — its only client caller is initCollaboration.ts:715, the `vi:intent-updated` PEER-EDIT handler; the auditor states this correctly in finding 11 and contradicts it in finding 6. (c) What actually remains is 2 socket joins + 1 catch-up, all fire-and-forget and off the critical path. The memo is worthwhile hygiene; the 60 ms of open latency is not established by any code path.",
    "Two thumbnail captures produce two PATCHes — REFUTED as an OPEN-TIME cost. Both upload sites exist (PlatformSaveController.ts:397, :567), but the post-load one is deliberately OFF the open path: `schedulePostLoadThumbnailCapture` sets `CAPTURE_DELAY_MS = 3500`, waits for `pryzm-project-loaded`, THEN hands `runCapture` to `requestIdleCallback(…, {timeout:4000})` (PlatformSaveController.ts:521-580). Its own comment says why — it was moved off the open path to kill a ~2.5 s LONGTASK. So it fires 3.5-7.5 s AFTER the open finished. And `_uploadThumbnailToServer` is fire-and-forget at both sites (thumbnailUpload.ts:77 is never awaited by the caller). At most ONE unawaited PATCH lands inside the open window. Digest dedup is fine cleanup; it buys zero open latency.",
    "Unbounded COUNT(*) runs TWICE per save — REFUTED, and it is wrong on three counts. (1) The cited code is NOT the live path. With Supabase configured the save goes through the `pryzm_save_version` RPC (server.js:3896-3912) and returns at :3911; `pgProjectStore.createVersionTransactional` (server.js:3980) is only reached when `getSupabaseClient()` is null. The RPC does exactly ONE count (server/schema.sql:362), not two. (2) Even on the PG path the step-3 count is inside `if (maxVersions !== -1)` (projectStore.js:1255) and the founder is `owner`, whose entry in VERSION_LIMITS is `-1` (server/planLimits.js:47) — so step 3 is SKIPPED and 'twice' becomes once there too. (3) It is a SAVE cost, not an OPEN cost, and it is an index-only scan of ~746 entries. The retention/no-pruning observation is real and deserves an ISSUE-LOG row, but it is not open latency and must not be funded from this budget.",
    "GET /api/v1/portfolio N+1 — REFUTED for this lens, by the auditor's own admission (claimed cost 0 ms, 'NOT on the open path'). I confirmed both halves: the serial snapshot loop is real (server/api/v1/routes.js:1252-1268) and its only caller is inside `openPortfolioPanel()` (PlatformProjectBrowser.ts:874, invoked from :850), a user-initiated modal. A real N+1 on the wrong path. File it; do not let it consume open-path budget.",
    "The indexes are present and correct — REFUTED AS STATED, and this is the most dangerous item in the set because its whole job is to tell the next lane 'do not look here'. Every line the auditor quoted from `server/dbMigrate.js` is verbatim correct (116-117, 70, 137, 272). But dbMigrate's SCHEMA_SQL is applied ONLY through `migrateViaPg(pool)` (dbMigrate.js:586), and `runMigrations()` returns immediately when `getPgPool()` is null (dbMigrate.js:567). There is a SECOND, RIVAL schema file for the SAME tables — `server/schema.sql`, whose header says 'Apply once via: Supabase Dashboard → SQL Editor' — and it creates DIFFERENT indexes: `idx_project_versions_project_id ON project_versions(project_id)` (schema.sql:80, SINGLE column, no `created_at DESC`), `idx_projects_owner_id ON projects(owner_id)` (schema.sql:51, single column), and `idx_project_members_project_id ON project_members(project_id)` (schema.sql:99 — the PROJECT→USER direction, i.e. the opposite of what the access gate needs). schema.sql is demonstrably live: it is the ONLY definition of `pryzm_save_version`, the function the production save path calls first (server.js:3896). So the composites are proven for the Postgres path and UNPROVEN for the database PostgREST actually reads. If the live DB was seeded from schema.sql, the single hottest open-path query — `project_versions WHERE project_id=$1 ORDER BY created_at DESC LIMIT 1` — reads all ~746 index entries for that project and SORTS them over rows holding multi-MB TOASTed JSONB, and it degrades with every save. That is exactly the diagnosis the finding tells the next lane to skip. Same defect shape as the repo's own 'same rule, two implementations' and 'gate blind on the wrong axis' lessons."
  ],
  "missed": [
    "⭐ SETTLE THE INDEX QUESTION BEFORE ANYTHING ELSE ON THIS DIMENSION. Two rival schema files describe the same tables with different index sets (server/dbMigrate.js:70/116/137 composites vs server/schema.sql:51/80/99 single-column), and which one shaped the live database is decided by an env var nobody has read. One query answers it against the production DB: `SELECT tablename, indexname, indexdef FROM pg_indexes WHERE tablename IN ('projects','project_versions','project_members');`. If `idx_project_versions_project_created` is absent, the classic missing-hot-column-index diagnosis is OPEN, not closed, and it is the largest single item on this dimension. The same one command (`flyctl secrets list` for DATABASE_URL / SUPABASE_DB_URL / SUPABASE_URL) also settles finding 5.",
    "`/api/projects/:id/latest-version` is fetched TWICE on every open where the first fetch does not return a snapshot — INCLUDING every transient failure. `tier.streamLoad` swallows any non-ok response and returns null (buildPersistence.ts:157-160), which sends PlatformShell down the else branch with `prefetchedVersion` undefined; after the warm-retry misses, PlatformShell.ts:378 calls `_loadLatestVersionFromServer(id)`, which hits the IDENTICAL endpoint at PlatformShell.ts:469. So a 503 from the db-unavailable path, a slow blip, or a non-owner 404 doubles the single most expensive request on the open instead of failing once.",
    "The ETag on `/latest-version` cannot save server work, and probably saves nothing at all. `res.set('ETag', …)` at server.js:3535 runs AFTER both Supabase queries have already fetched and materialised the ENTIRE snapshot — so even a 304 pays the full DB read, the full PostgREST transfer and the full supabase-js parse; only the Fly→browser bytes are saved. And the route sets no `Cache-Control` (the only six Cache-Control sites in server.js are 1779, 1799, 2595, 2659, 5928, 5985 — none is this route), so with no Last-Modified and no freshness directive the browser has nothing stored to revalidate and never sends If-None-Match. Correct shape: select `id` alone first, compare `If-None-Match`, and skip the snapshot select on a match.",
    "A DB round trip plus an uncached access check is spent purely to read a clock, on every first connect in a session (every hard reload / new tab). `_triggerCatchUp` with no stored baseline calls `_fetchServerNow` (initCollaboration.ts:902), which issues `GET /api/projects/:id/commands?since=<now>&excludeSelf=1` (initCollaboration.ts:979-981) solely to read `body.serverNow`. The handler computes `serverNow` from `new Date()` at the TOP, before any DB work, then still runs `_httpCanAccess` (server.js:4447) and a PostgREST range query on `project_command_log` that by construction matches zero rows. The `Date` response header, or a trivial `/api/time`, removes the whole leg.",
    "The Supabase branch of `/latest-version` is OWNER-ONLY (`.eq('owner_id', userId)`, server.js:3522) while the Postgres branch serves owner-OR-member (`pgProjectStore.listVersions(id, userId)` → memberOrOwner). A collaborator therefore gets a 404 from the endpoint the open path fetches first, which makes the double-fetch above CERTAIN for every shared project. Correctness, not perf — but it lands squarely on the path this dimension is auditing.",
    "Two corrections to finding 2's cost model, in the auditor's favour and against it. (a) AGAINST the number: gzip in `compression()` (server.js:373) runs on the libuv threadpool, not the event loop, so roughly 7 of the measured ~18 ms is NOT main-thread blocking; only the supabase-js parse and the `res.json` re-stringify are. Same correction applies to finding 12's PG-path citation (projectStore.js:1268), which is on the branch that does not execute under Supabase. (b) IN FAVOUR of the fix: the 'would break' cites a rationale at server.js:3517 that is not there — 3517 is `try {`. The actual comment, at 3520, reads 'Single query: verify ownership and fetch latest version snapshot together' — a claim the next four lines contradict. Nothing in the file documents the two-step as deliberate, so collapsing it into one embedded-filter call is less risky than the finding warns."
  ]
}