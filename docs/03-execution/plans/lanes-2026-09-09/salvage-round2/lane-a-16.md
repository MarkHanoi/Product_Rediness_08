{
  "survivingTitles": [
    "Lighting mesh geometry is NOT suppressed in plan — the 672-face luminaire is fully edge-projected, and that IS the dense circular scribble",
    "The lighting plan symbol exists but is CANVAS-ONLY — it contributes no linework, so it is invisible to sheets, DXF/PDF/SVG export, hidden-line removal, VG styling and the pen table",
    "L-1332 is ALREADY OPEN for the symbol vocabulary: 46 fixture families share 11 bespoke marks and one generic disc — and the driver for a real symbol table is already authored",
    "A reflected ceiling plan is where light fittings belong, and the injector view-type set deliberately EXCLUDES 'ceiling-plan' — a new lighting builder must not reuse it",
    "Dead duplicate: `planViewSymbolRenderer` in apps/editor is constructed and exported but imported by nothing — a second lighting symbol path that has already been cited as live in three comments"
  ],
  "refutedTitles": [
    "Lighting has no ISO layer row, so its linework is UNCLASSIFIED — the visibility-intent veto, the VG hide and the pen table are all bypassed, and lighting's own authored pens are dead code",
    "'lighting' is absent from the list that DECLARES visibility-intent categories — so there is no lighting row in the Visibility Intent panel at all"
  ],
  "refutations": [
    "FINDING 3 — REFUTED AS STATED: the tree moved under the audit. Commit 584aaab2 (§LIGHTING-IS-A-CLASSIFIED-LAYER, L-13267, dated 2026-09-09 = today) landed three of the four sites this finding proposes, plus a guard test the finding does not know exists. Measured now: EdgeProjectorService.ts:261 carries `Lighting: 'A-LGHT', LightingFixture: 'A-LGHT', light: 'A-LGHT'` (the finding's fix #1, and it added a THIRD spelling the auditor did not name); DrawingLayerIdentity.ts:110 carries `'A-LGHT': 'lighting'` (fix #2); PenWeightTable.ts:391/417/461 carry the `isLighting?` flag, the `if (flags.isLighting) return 'lighting';` arm ordered deliberately AFTER furniture, and the `/A-LGHT|lighting/i` regex written narrow on purpose (fix #3). So `penCategoryForLayerTag('A-LGHT')` now returns 'lighting', the 0.13 mm #303030 pen at PenWeightTable.ts:153 is REACHABLE, and the three pens are no longer dead code. `vgCategoryForLayer('A-LGHT')` now returns 'lighting', so BOTH gates the finding says fail open now fire: the native intent veto at EdgeProjectorService.ts:2722-2727 (`_groupFamily` is no longer null) and the canvas VG hide at PlanViewCanvas.ts:458-464 (`_vgCategoryForLayer` at :2814 is a one-line delegate to the same function). A 150-line suite, packages/core-app-model/src/drawing/lightingIsAClassifiedLayer.test.ts, pins all three links and explicitly asserts the whole chain so no single site can be tidied away. Accepting this finding as written would rebuild what shipped hours ago — exactly the failure this lens exists to catch. TWO RESIDUES SURVIVE and should be re-issued as their own rows rather than lost with the title: (a) fix #4, `SVGCompositeRenderer.ISO_LINE_WEIGHTS`, is NOT done — but see 'missed', it is a five-family hole, not a lighting one; (b) the DrawingPipelineWorker fork is still unreconciled, and its `return 'wall'` default at :143 is the dangerous half. ALSO: the finding's pen mechanism is unproven and conflicts with the shipped fix's own account — the auditor derives FALLBACK_PEN 0.18 mm (correct for `resolvePen('PROJECTION','projection')`, since the PROJECTION block has no 'projection' key), while PenWeightTable.ts:389 and the new test header both say the observed weight was the `__default__` WALL pen, 0.25 mm. Two different resolution paths, both reachable, and nobody measured which one the founder's screenshot actually shows.",
    "FINDING 4 — REFUTED AS STATED: the headline claim is now literally false. `'lighting'` IS in `VisibilityIntentDefaults.ELEMENT_TYPES` — VisibilityIntentDefaults.ts:33, added by the same L-13267 commit, four lines above the `'grid'` entry and carrying its own comment quoting the founder. Because that array is the declaration site the finding correctly identifies, the seeding machinery now generates the complete four-state `ElementGraphicsRules` for lighting from the pen table, and the Visibility Intent panel HAS a Lighting row with a hide toggle and a colour. The finding's own proposed fix #1 is the line that is already in the file, and its guard is already written (lightingIsAClassifiedLayer.test.ts asserts `/^\\s*'lighting',\\s*$/m` against the source). REFUTED — but this is the finding with the largest surviving residue, and ONE PIECE OF IT IS NOW THE MOST IMPORTANT UNFIXED THING ON THIS DIMENSION, so re-issue it rather than dropping it: `CATEGORY_TO_DXF_LAYER` (VGSceneApplicator.ts:87-102) has exactly 14 rows and NO `lighting: 'A-LGHT'`. `applyToProjectionLayers` iterates that map (its own doc at :596-600: 'For every VG category that has a DXF layer name (CATEGORY_TO_DXF_LAYER)'), and the session log corroborates the count — 'styledLayers=22 ... across 14 VG CATEGORIES (max 56)', 14 rows x 4 sub-layers = 56. So the projector now CREATES the A-LGHT layer and classifies its pen, but `setVisibility('A-LGHT', ...)` and `setColor('A-LGHT', ...)` are never called on it. The founder's verbatim complaint — 'I change the line type etc.. doesnt work' — is STILL TRUE for lighting after today's fix, on a layer that now looks governed. That is a worse shape than before, not a better one: the pen and the panel row make it read as fixed. Also still missing, all as the finding says: `'lighting'` in the `VGCategory` union (VGSceneApplicator.ts:57-75), a `'Lighting': 'lighting'` row in `ELEMENT_TYPE_TO_VG_CATEGORY` (:109, typed `Record<string, VGCategory>`, so it CANNOT be added before the union is widened — the auditor sequenced this correctly), and a `lighting` entry in `ELEMENT_TYPE_REGISTRY` (ElementTypeRegistry.ts, which has `plumbing` at :185 and no lighting). The parallel 'room' observation also still holds: 'room' is a VGCategory at VGSceneApplicator.ts:65 but is absent from ELEMENT_TYPES."
  ],
  "missed": [
    "THE TREE MOVED UNDER THE AUDIT, AND NOTHING IN THE FINDINGS SAYS SO. Two commits landed today, 2026-09-09, after HEAD 921cbb01: 584aaab2 (L-13267, lighting as a classified layer) and 9babe6f5 (L-13268, 'the two line-type behaviours the drawing engine already reads finally have a UI' — the founder's 'I change the line type and it doesn't work' half). Every line number in findings 1, 2, 3 and 6 that points into EdgeProjectorService.ts is now short by ~28 lines (skipInPlan gate cited :2886, actually :2914; FALLBACK_NATIVE_LAYER cited :242, actually :271; the injection view-type set cited :325-327, actually :352-354; the plan Y filter cited :2949-2951, actually :2977-2979; resolveClipRange cited :4497, actually :4524). The content at every one of those sites is verbatim as quoted — the citations rotted, the claims did not — but a lane applying these patches by line number will land them in the wrong place. Re-read before editing.",
    "CATEGORY_TO_DXF_LAYER IS THE LOAD-BEARING GAP AND IT IS NOW MIS-RANKED. The auditor buried `lighting: 'A-LGHT'` as the third bullet of finding 4's fix, when finding 4 was mostly about a declaration that has since landed. Post-L-13267 it is the single site that still stops the founder's V/G colour and visibility from reaching lighting linework — see the refutation above for the measurement (14 rows, matching the log's '14 VG CATEGORIES (max 56)'). It should be a finding of its own, ranked above the symbol work, because it is one row and it closes a complaint the founder has already made twice.",
    "ISO_LINE_WEIGHTS IS MISSING FIVE FAMILIES, NOT ONE — so finding 3's fix #4 is a per-family patch on a systemic hole. Measured: `grep -c \"'A-FURN'\"` etc. against packages/file-format/src/export/sheets/SVGCompositeRenderer.ts returns 0 for A-FURN, A-PLMB, A-CEIL, A-BEAM **and** A-LGHT. All five fall through :250 to `ISO_LINE_WEIGHTS['projection-visible']` = 0.25 mm on sheet export, while their screen pens are 0.13 (furniture, lighting, ceiling) / 0.13 (plumbing) / 0.25 (beam). So furniture and plumbing ALREADY export at roughly double their screen weight, today, on the families the founder can already see — which is a direct, unattributed contributor to his 'THE QUALITY ATM IS REALLY BAD - THE LINES ARE ALMOST NOT READABLE'. Adding only 'A-LGHT': 0.13 fixes the family that currently draws nothing and leaves the four that draw wrong. Better: derive the export weight from `resolvePen(zone, penCategoryForLayerTag(tag))` so the screen and the sheet cannot disagree by construction, rather than hand-maintaining a sixth copy of the pen ladder.",
    "THE RCP GUARD TEST HAND-COPIES THE SET IT GUARDS, so finding 6's 'extend that test rather than writing a new one' would extend a test that cannot fail. apps/editor/__tests__/rcpIsNotAPlanWithAFlippedCamera.test.ts:234 declares its own `const PLAN_SYMBOL_INJECTION_VIEW_TYPES = new Set(['plan','detail','structural-plan'])` rather than importing the real constant from EdgeProjectorService. Its assertions at :236-238 therefore test a literal against itself. Everything else finding 6 says about that file is correct — the :228-232 comment does explicitly rule that admitting 'ceiling-plan' for the LIGHTING renderer is CORRECT, which is strong support for the finding — but the 'already exists' claim overstates what the test protects. Import the constant first, then extend.",
    "THE SYMBOL-BUILDER PATTERN HAS NO REGISTRY, WHICH VALIDATES FINDING 2 AGAINST THIS LENS RATHER THAN REFUTING IT. I searched for the thing that would have made a new builder a reinvention: `symbolBuilderRegistry|registerPlanSymbol|PlanSymbolRegistry|symbolRegistry` across apps/packages/src/plugins returns only `stairPlanSymbolRegistry` (a scene-committer O(k) traverse cache, not a symbol dispatcher). There are 13 bespoke `*PlanSymbolBuilder.ts` files, each hand-wired to its own `_symbolGate(...)` line at EdgeProjectorService.ts:3969-4111. A 14th following that pattern is the established shape, not a rebuild. The template checks out exactly as the auditor described: PlumbingPlanSymbolBuilder.ts is 113 lines and uses precisely `storeRegistry.getStoreForType(...)` (:52), `projectToDrawingSpace` (:76), `drawing.addProjectionLines` (:77) and `registerSegmentUUID` (:78); and geometry-lighting/package.json really does already declare @pryzm/core-app-model, @pryzm/renderer-three and @thatopen/components, so the no-lockfile-churn claim holds. Worth noting the current renderer reads `window.lightingStore` (LightingPlanSymbolRenderer.ts:201, :248, both tagged `TODO(TASK-08)`) — moving to `storeRegistry.getStoreForType('lighting')` retires two window casts as a side effect, which is P4-relevant.",
    "FINDING 5 HAS A ONE-OFF IN ITS TITLE, IMMATERIAL TO THE SUBSTANCE. 'll bespoke marks and one generic disc' counts 12; the drawer at LightingPlanSymbolRenderer.ts has exactly 10 `case` arms (:107,115,120,125,129,134,141,148,153,157) plus `default`, so it is 10 bespoke + 1 generic = 11 marks total. The 46-family figure is exact and I confirmed both halves independently: `grep -c \"^        id: '\"` on packages/schemas/src/lighting/Lod200FixtureCatalogue.ts returns 34, and LightingTypes.ts:45-64 hand-declares 12 members before widening with `| Lod200FixtureId`. The 14 archetypes at :161-164 are as quoted, L-1332 is OPEN at ISSUE-LOG.md:21018, and note L-1332's own text has itself rotted — it says 'all twenty LOD-200 families' against 34 rows on disk today.",
    "FINDINGS 1 AND 7 VERIFIED WITH NO CAVEAT, AND FINDING 1 IS NOW THE WHOLE DIMENSION. L-13267 did not touch geometry-lighting at all: `grep -rn skipInPlan packages/geometry-lighting` is still zero hits, the traverse at LightingFragmentBuilder.ts:742-749 still stamps only isSubElement/parentId/elementType, the gate at EdgeProjectorService.ts:2914 still reads a flag nothing sets, classifyByVertexY at :1135-1137 still has `belowFloor` as its ONLY drop arm with no upper cull, and the plan mesh filter at :2977-2979 still admits `min.y <= far + 0.5` where `far = levelElevation + farOffset` (:4524). The torus is `TorusGeometry(r*0.92, r*0.10, 8, 28)` at :1128 with SEG_TRIM=28 (:371). So the blob is real, it is now drawn at a correctly-classified 0.13 mm instead of a wrong 0.25 mm, and it is now hideable via the intent veto — but it is still a 672-face mesh dump where a symbol belongs. Finding 1 plus finding 2 are the remaining work; finding 1's same-commit warning is the important part of it and is well-sourced (WindowMeshSkipInPlan.test.ts and DoorMeshSkipInPlan.test.ts both exist and both say what the auditor quotes). Finding 7 is exact: `grep -rn planViewSymbolRenderer apps packages src plugins` returns exactly ONE line, its own definition at PlanViewSymbolRenderer.ts:144, and the class name appears elsewhere only in four comments (EdgeProjectorService.ts:342, :3957; initTools.ts:3156; ViewDependencyTracker.ts:102) and one test comment. Dead, and cited as live in four places rather than the three the auditor counted."
  ]
}