{
  "survivingTitles": [
    "THE BREAK — the only line-type control in the product refuses to write on the intent that governs his views (four gates, all on `isSystem`)",
    "The per-view \"Visual Style\" control (its options include \"Hidden Line\") and \"Display Model\" are WRITE-ONLY — zero readers in the entire draw path",
    "Two layer classifiers disagree on the same line: six ISO layer families get VG COLOUR governance but fall to the `__default__` (wall) pen for DASH and WIDTH",
    "(b) answered — \"22 styled vs 63 ISO vs 14 categories\" are three DIFFERENT UNITS; \"41 layers never styled\" is not established, and the real unstyled set is a different one",
    "The element-tier pen override (priority 10000, the strongest tier in the cascade) has zero callers — the whole per-element styling tier is dead",
    "The ViewTemplate → StrokeStyle.dash chain — a complete, schema-validated line-type system — is orphaned from the editor"
  ],
  "refutedTitles": [
    "The inverse defect: `output.beyondLineStyle` and `output.occlusionDisposition` are READ by the drawing engine and written by NO UI — the dashed-hidden-line answer to (d)",
    "The V/G (Visibility/Graphics) tier cannot express a line type at all, and has no human writer — only the AI has one"
  ],
  "refutations": [
    "FINDING 3 — REFUTED AT HEAD (the defect has already been fixed; do not action it again). The load-bearing measurement was `grep -rn \"occlusionDisposition|beyondLineStyle\" apps/editor/src/ui/` -> no output. Re-run today it returns SIX hits: apps/editor/src/ui/ViewPropertiesPanelBuilders.ts:375 `o.occlusionDisposition`, :382 `patch.occlusionDisposition = (val || undefined)`, :390 `o.beyondLineStyle`, :397 `patch.beyondLineStyle = ...`, plus the two header lines. Both selects now exist inside `buildOutputSection` (the same Output section the founder's screenshot shows, next to Scale / Detail Level) and both write through `host._fireSetViewOutput` -> `SetViewOutputCommand` -> `viewDefinitionStore.setOutput` (the command's validator does not reject the two new keys; execute persists the whole object). Cause: commit 9babe6f5 'feat(docs/§LINE-TYPE-CONTROLS-EXIST-NOW L-13268)', 2026-09-09 07:22, which IS current HEAD — it landed after the audit's 2026-09-08 measurement and implements this finding's own fix verbatim. Only the SECOND half of the finding survives as a fact worth keeping: `_PLAN_SCOPE.occlusionDisposition = 'remove'` (ViewScope.ts:155), `_ELEVATION_SCOPE = 'demote'` (:143) and the `if (isHidden && disposition === 'remove') { clipped += ...; continue; }` branch (HiddenLineRemoval.ts:882) are all exactly as quoted, so '0 sub-segment(s) demoted' in a PLAN log is the correct number, not a defect.",
    "FINDING 4 — REFUTED as a defect, though its raw greps are accurate. Three problems. (1) SYMPTOM MISATTRIBUTED, and this is the disqualifying one: the finding claims the founder's 'NO OVERRIDES' is the per-view VG override panel reading empty. It is not. That readout is computed from the ViewIntentInstance's LOCAL overrides — `countOverrides(instance?.localOverrides ?? null)` in `buildVisibilityIntentSection` (ViewPropertiesPanelBuilders.ts:36, section starts :31) — i.e. visibilityOverrides + graphicOverrides, which the live 'Ghost in View' path DOES populate (PlanViewInteraction.ts:1498, RadialMenu.ts:270, initUI.ts:3393). It never reads `vgGovernanceStore` category overrides at all, so VG's emptiness is not what the founder is seeing. (2) The tier is DEPRECATED BY CONTRACT, so 'it cannot express a line type' is a design decision, not a break: VGGovernanceStore.ts:23-29 carries `@deprecated Contract 25b — VG templates and per-category styles are superseded by VisibilityIntent records... New authoring code MUST use the Visibility Intent system. Do not add new importers.` (3) The finding's own fix is 'do NOT add a dash field... give it a UI or retire the command' — i.e. it proposes no change to line-type behaviour and reduces to translator tidiness. The verifiable facts (keep them, drop the finding): VGCategoryStyle at VGGovernanceStore.ts:31-38 has fillColor/edgeColor/lineWeight/transparency/visible/halftone and NO dash sibling anywhere in the file; PlanViewVGApplicator.ts:52-90 builds cut/proj/beyond pens from widthMm, color and opacity only; `grep -c 'lineType|linePattern|dashSize|dashed|LineDashed|gapSize' VGSceneApplicator.ts` -> 0; `SetVGViewCategoryStyleCommand` has zero non-test call sites (barrels at command-registry/src/index.ts:351 and vg/index.ts:21 only) and `SetVGCategoryStyleCommand` adds only the two VGIntentMapper AI sites."
  ],
  "missed": [
    "ROOMS HAVE NO VISIBILITY-INTENT CATEGORY — the founder's own fourth ask, and the audit never mentions it. `ELEMENT_TYPES` (packages/core-app-model/src/presentation/VisibilityIntentDefaults.ts:15-46) lists __default__, wall, slab, column, structural, beam, door, window, stair, roof, ceiling, furniture, plumbing, lighting, grid, annotation, level, boundary-line, ifc-element — there is NO 'room'. The panel's rows are literally `Object.keys(intent.elementRules)` (VisibilityIntentPanel.ts:240), so 'THE ROOMS SHOULD HAVE A CATEGORY ON THE VISIBILITY INTENT SETTINGS TO MODIFY THE COLOURS - THAT I DONT SEE' is a one-line declaration gap of exactly the shape the lighting fix (L-13267, commit 584aaab2) just closed. `room` already exists as a VG category carrying `roomColourMode` (VGGovernanceStore), so the two tiers disagree about whether a room is a governable family.",
    "A-BEAM IS SILENTLY CLASSIFIED AS `column` — the same defect as finding 5 but WORSE, because the wrong answer is plausible rather than a fallback, and the audit missed it entirely. `penCategoryForLayerTag`'s column arm is `/A-COLS|column|beam/i` (PenWeightTable.ts:456 via categoryFromFlags `if (flags.isCol) return 'column'`), so 'A-BEAM', 'A-BEAM:cut' and 'S-BEAM' all resolve to pen category `column` — verified by simulating the exact regex set. Meanwhile ISO_LAYER_TO_VG_CATEGORY has `'A-BEAM': 'beam'` / `'S-BEAM': 'beam'` (DrawingLayerIdentity.ts:99, :133) and ELEMENT_TYPES declares `beam`. Consequence: the intent panel's `beam` row can never change a beam's line type or weight (it takes the column rules), and the four `beam:` pen rows in SYSTEM_PEN_TABLE (PenWeightTable.ts:130, :146, :206, :225) are UNREACHABLE — the identical 'the pen existed the whole time and nothing could reach it' shape that L-13267 just fixed for lighting. `S-GRID` also falls to 'projection'.",
    "FINDING 5's LIGHTING HALF IS ALREADY FIXED AND ITS PANEL-ROW LIST IS WRONG. (a) 'no arm for A-LGHT ... absent from BOTH maps, so lighting linework is ungoverned on every axis' is false at HEAD: `isLighting: /A-LGHT|lighting/i.test(layerTag)` is PenWeightTable.ts:461 (blamed to 584aaab2, 2026-09-09, 'the lighting pen existed the whole time and nothing could reach it'), `'A-LGHT': 'lighting'` is DrawingLayerIdentity.ts:110, and 'lighting' is in ELEMENT_TYPES. Lighting is now governed on all three axes. (b) The panel does NOT offer `spaceEnvelope`, `handrail`, `curtain-wall` or `curtain-panel` rows — rows come from `Object.keys(intent.elementRules)` = ELEMENT_TYPES, and none of the four is declared there. So A-AREA is worse than described (no pen arm AND no panel row), and the handrail/curtain claims should be restated as 'undeclared family', not 'row that does nothing'. (c) The symptom sentence 'the Line colour row for the same categories DOES work' is unsupported: colour on those layers resolves through the SAME `_penCategory` ('projection' -> __default__ -> wall) at PlanViewCanvas.ts:471/487; the only colour path keyed on the VG category is `vgEdge` (:458-465), which needs a VG override that finding 4 itself shows no human can write. The correct statement is that intent VISIBILITY/colour via VG is reachable in principle and the pen is not — not that colour works today.",
    "THE INTENT'S OWN viewTypeModifiers SILENTLY OUTRANK THE ROW THE USER EDITS, and there is no way to see which tier won. `resolveIntentStyle` merges element rules FIRST, then `for (const modifier of intent.viewTypeModifiers) ... mergeAppearance(appearance, modifier.statePatch[state])` (IntentRuleResolver.ts:183-187). `DOCUMENTATION_MODIFIERS` — carried by the founder's 'Architectural Documentation (Auto)' intent (SystemIntents.ts:280) — contains `{ viewType:'elevation', elementType:'door', statePatch:{ projection:{ line:{ weight: 0.18 } } } }` (SystemIntents.ts:160-164). So even AFTER finding 1's copy-on-write lands, setting the door/projection line weight and looking at an elevation still shows no change. The panel does have a modifier editor (VisibilityIntentPanel.ts:517, :844, :1128) but every one of its handlers sits below the same `if (intent.isSystem) return;` at :772. Good news for finding 1: plan-type modifiers patch only `symbolicRule`, so nothing clobbers line.style in a plan.",
    "TWO CORRECTIONS THAT STRENGTHEN THE AUDIT'S OWN CASE. (i) I re-ran the proof: `cd apps/editor && npx vitest run __tests__/viewIntentGovernsEveryCategory.test.ts` -> exit 0, 'Test Files 1 passed (1) / Tests 23 passed (23)'. (ii) The 'downstream is live' claim is stronger than the audit states, and I checked the one way it could have been hollow: `GraphicsRulesEngine._resolveBinding` (:295-328) FALLS OPEN to `getDefaultSystemIntentId()` when a view has no ViewIntentInstance, so the intent chain runs on every view in the real app, not only on explicitly-bound ones; and `PlanViewManager` repaints on `window.addEventListener('vi:intent-updated', ...)` (:190), so the invalidation gate finding 1 depends on is present. Also `LINE_STYLE_TO_DASH.solid = null` (IntentRuleResolver.ts:32) with GraphicsRulesEngine's `if (s.dashPx !== undefined)` (:193) means 'solid' correctly CLEARS an inherited dash — the obvious adjacent failure mode is not present.",
    "FINDING 7's TITLE OVERSTATES BY ONE WORD. `addElementOverride` really does have zero non-test callers (only the declaration at GraphicsRulesEngine.ts:122 and PenWeightByFunction.test.ts:419), and all three UI sites really do hard-code the identical ghost patch. But 'the whole per-element styling tier is dead' is wrong: the `graphicOverrides` tier is LIVE — IntentRuleResolver.ts:220-224 merges `override.patch` into the appearance on every resolve, and 'Ghost in View' drives it today. What is dead is the priority-10000 API and any line-type/weight patch reaching that tier. Retitle to 'no UI writes a line style into the per-element tier'; the fix is unaffected."
  ]
}