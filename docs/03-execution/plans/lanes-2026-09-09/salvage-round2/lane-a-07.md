{
  "survivingTitles": [
    "`room` is a first-class category in the DEPRECATED vgGovernanceStore, and absent from the VisibilityIntent list the founder's panel actually renders",
    "THE STYLING SEAM IS ONE FUNCTION — `resolveRoomColourIntent()` — with exactly three live callers",
    "`OverridePanel.CATEGORIES` is a FOURTH category registry with no room row — the one this lane is most likely to miss",
    "`PlanViewFillRenderer` is a DEAD duplicate room painter with zero importers",
    "'Colour by room type' cannot actually change any colour — `OCCUPANCY_PALETTE` is a hardcoded table with no write path",
    "Room fills are gated to `isPlanLike` — elevation and section render no room region at all",
    "`ELEMENT_TYPE_REGISTRY` is a rival to `VisibilityIntentDefaults.ELEMENT_TYPES` and its header states a rule that is false",
    "No gate covers the visibility-intent category list, which is why this shortfall was silent",
    "REGISTRY MAP — every list that would need a `room` row, and its current state"
  ],
  "refutedTitles": [],
  "refutations": [
    "Nothing refuted. Every cited file:line was opened and says what the finding claims. Verification notes per finding — (1) VisibilityIntentDefaults.ts:15-41 read in full: 18 entries (__default__ + 17), no `room`; the auditor writes '19 category rows' in the symptom and '18 + default' in the mechanism — the true figure is 17 + default = 18 rows. Immaterial to the claim. VGGovernanceStore.ts:23-28 deprecation banner verbatim as quoted; :148/:177/:206/:235 all four carry the `room` row with `roomColourMode:'detection'`. `grep -rln vgGovernanceStore apps/editor/src/ui` → exactly one file (inspectCategories.ts), and its only hit is the exclusion string at :196. SystemIntents.ts has ZERO occurrences of 'room', so no system intent injects the key past the defaults. VisibilityIntentPanel.ts:240 `Object.keys(intent.elementRules)` and the fill controls (fill.style / fill.colour / fill.opacity) confirmed present. (2) RoomColourIntent.ts:132 hard-codes `vgGovernanceStore.resolveStyle(modelId, ROOM_VG_CATEGORY, viewId)`; repo-wide grep for resolveRoomColourIntent|activeRoomColourIntent returns exactly the four call sites named (PlanViewCanvas:2504, RoomBoundaryBuilder:251, RoomPropertySection:683, PlanViewFillRenderer:40) plus two barrel re-exports and the definitions. `_renderRoomFills` confirmed to consult ONLY `intent.visible`/`intent.transparency`/`intent.mode` — it never touches styleResolver or an ISO layer. (3) OverridePanel.CATEGORIES read in full: 13 entries, no room, no lighting; the curated-list comment is verbatim; and OverridePanel is LIVE (PlanViewManager.ts:348 and ViewHeaderButtons.ts:71 both construct it), so this is a real user-facing surface, not a dead file. (4) `grep -rn 'planViewFillRenderer'` across apps/packages/plugins/src/tests returns ONE line — its own declaration at :268. `PlanViewFillRenderer` (the class name) returns only the class decl, the singleton, and the three comments named. Confirmed dead; the initScene.ts:1003 comment does assert it as the live plan painter. (5) OCCUPANCY_PALETTE verified as a frozen object literal at RoomColourSystem.ts:114; every one of the ~20 non-test references is a READ, zero assignments; RoomSystemTypeStore's `getColour` (:126) and `makeBuiltIn` (:57) also only read it, so custom room types cannot redirect occupancy mode either. ⚠ TITLE OVERSTATES: per-room colour IS authorable (RoomPropertySection.ts:563-581 is a real `<input type=color>` dispatching `room.update`), and uniform mode gives one editable colour. The body says this correctly; only the title's 'cannot change any colour' is wrong. Judge the body. (6) isPlanLike verified at :378-383 AND duplicated at :1953-1959; the ISSUE-LOG 'NOT done, named' block at docs/04-reference/ISSUE-LOG.md:24771 is verbatim as quoted. Real, but it is a scoping guard, not a defect the founder reported. (7) ElementTypeRegistry.ts:14 header claim verified verbatim; 19 keys + __default__ verified by line; divergence in both directions from ELEMENT_TYPES verified; only live consumer is BimGridRenderer.ts:126 `getElementTypeRules('grid')`. (8) InspectCategoryCoverage.test.ts:44-54 publishedStoreGlobals scanner verified; viewIntentGovernsEveryCategory.test.ts PLAN_CASES verified (15 rows, 11 distinct categories — the auditor's mechanism says '12', its own evidence line lists 11; immaterial), no room, no lighting; recordingCanvas confirmed to bind `fill` into the noop geometry-sink list at :143-146 while only `ctx.stroke` records — so a fill-only family is structurally invisible to that gate, exactly as claimed. (9) Every registry in the map opened: CATEGORY_TO_DXF_LAYER 14 entries no room (and :716 is the `categoryCount` that prints the log line); VGCategory union carries 'room' at :65; ELEMENT_TYPE_TO_VG_CATEGORY carries room/Room/RoomVolume at :143-145; ISO_CUT_LAYER_TO_POCHE_FILL 6 entries and VG_CATEGORY_TO_ISO_LAYER 6 entries, no room, so defaultPocheFillForCategory('room') → null confirmed; resolvePen falls to FALLBACK_PEN on an unknown category (PenWeightTable.ts:305-306) confirmed; VGToIntentMigration.ts:167 `?? vgCategory` fallthrough confirmed and VG_CATEGORY_TO_ELEMENT_TYPE confirmed to have no room key; inspectCategories.ts:133 and ElementsSummarySection's 'Rooms' row confirmed present."
  ],
  "missed": [
    "MISSED — A FOURTH WRITER OF ROOM-MESH `visible` WILL FIGHT THE CATEGORY ROW, AND IT WINS. `apps/editor/src/engine/initScene.ts:1007-1041` defines `_applyRoomOverlayVisibilityForView(mode)`, which traverses the scene and sets `obj.visible = !is3DModelView` on EVERY `userData.isRoomOverlay === true` mesh — unconditionally, with no consultation of any store. It is wired to three triggers: the `view-activated` runtime event (:1024), and BOTH `bim-room-added` and `bim-room-updated` window events via `_reapplyRoomOverlay` (:1038-1039), plus a boot call at :1041. `RoomBoundaryBuilder.ts:345` stamps `isRoomOverlay = true` on the same mesh it stamps `elementType = 'room'` on (:342) — the exact mesh `VGSceneApplicator.applyToMesh()` writes `mesh.visible = style.visible` to at :914. So the moment a `room` category row can say 'hidden', two writers own one property: hide the rooms, then edit ANY room (or switch views) and the overlay is resurrected `visible = true` in every non-3D view. This is the [[view-region-one-owner]] shape and NONE of the nine findings names it — finding 3 gets as far as 'the 2-D wash will not hide' but stops at `_renderRoomFills`; the 3-D leg has its own, louder rival. Any lane that adds the row must decide who owns `isRoomOverlay.visible` FIRST.",
    "MISSED — ADDING `room` TO ELEMENT_TYPES DOES NOT GIVE THE ROW TO A SAVED USER INTENT, AND FINDING 1's FIX SAYS IT DOES. `VisibilityIntentStore` re-seeds SYSTEM intents from `cloneSystemIntents()` in its constructor (VisibilityIntentStore.ts:20-24) and `deserialize()` explicitly skips them (`if (raw?.id && raw?.name && !raw.isSystem)`, :118), so 'Architectural Documentation (Auto)' — the intent in the founder's screenshot — WOULD gain the row for free. But USER intents are restored verbatim from the project snapshot and passed through `migrateIntentToCurrent`, and `IntentSchemaMigrations.ts:33-59` contains FOUR migrators, every one of them a documented no-op that backfills nothing into `elementRules` (the v2→v3 comment even says so: 'missing rules fall back through IntentRuleResolver.rulesFor() → __default__'). Resolution falls back; the PANEL ROW does not, because it is `Object.keys(intent.elementRules)`. The founder's own authoring path makes user intents — ViewPropertiesPanelBuilders.ts:196 opens the editor and §FIX-INTENT-AUTHORING-DEAD-END (L-779) duplicates a read-only system intent into a user one. So the founder duplicates 'Architectural Documentation', saves, and his Rooms row silently does not exist in his copy. Fix needs EITHER a v5→v6 migrator that unions `ELEMENT_TYPES` into `elementRules`, OR `renderRules()` rendering `ELEMENT_TYPES ∪ Object.keys(intent.elementRules)` instead of the keys alone. The second is better: it makes the whole class of 'category added after the project was saved' impossible, not just this one.",
    "MISSED (SHARPENS FINDING 2) — THE ROOM ROW'S DEFAULT FILL IS `{style:'none', opacity:0}` IN ALL FOUR STATES, SO A NAIVE SEAM ERASES THE WASH FOR EVERY EXISTING PROJECT. `defaultStateAppearance()` (VisibilityIntentDefaults.ts:102-117) computes `fill: fillFor(category, state)`, and `fillFor` (:73-80) returns `{style:'none', opacity:0}` unless the state is `cut` AND `defaultPocheFillForCategory(category)` is non-null — and for 'room' it is null (VG_CATEGORY_TO_ISO_LAYER has 6 structural entries, no room). The auditor cites both facts as evidence that the row is LOW-RISK to add, which is true for the row. It is NOT true for finding 2's fix, which proposes the seam read `fill.opacity` and `fill.colour` off the resolved intent: the resolved answer for a fresh room row is opacity 0, i.e. every pastel wash in the founder's screenshot goes to zero the moment the seam moves. The seam must treat 'the intent declares no fill for room' as 'leave RoomColourSystem's answer alone' and only apply an EXPLICITLY authored fill — the same `requireExplicitIntentFill` discipline `_renderPocheFills` already carries for elevation poché (§ELEVATION-POCHE-IS-INTENT-DECLARED, L-1601, PlanViewCanvas.ts:2537-2542). That precedent is in the same file and the audit does not reach for it.",
    "MISSED (MINOR, BUT IT IS LITERALLY WHAT THE FOUNDER SAID HE CANNOT SEE) — THE PANEL RENDERS RAW KEYS AS ROW LABELS, UNSORTED. `VisibilityIntentPanel.ts:253-262` prints `${this.escape(type)}` as the button text, so the row will read `room`, lower-case, in `Object.keys` insertion order — i.e. appended after `ifc-element` at the bottom of a 19-row list, not alphabetised near 'roof'. Every other row has the same defect so it is consistent, but the founder's complaint is a FINDABILITY complaint ('that I don't see'), and a lower-case key at the end of an unsorted list is the weakest possible answer to it. There is a label table one directory away (`inspectCategories.ts` carries `label: 'Rooms'`), so this is a five-line fix, not a design question.",
    "NOT A MISS, RECORDED SO THE LANE DOES NOT 'FIX' IT — `LayerPanel.BIM_LAYER_DEFS` (apps/editor/src/ui/LayerPanel.ts:47-57) is a fifth hand-curated category list (9 entries: wall, slab, roof, door, window, stair, curtain-wall, furniture, annotation — no room) and finding 9's 'REGISTRY MAP' does not enumerate it. It is correctly omitted: `grep -rn 'LayerPanel|LAYER_PANEL_ID|BIM_LAYER_DEFS'` across apps/packages/plugins/src returns nothing outside the file itself except one comment in LayerLockPanel.ts:34. Same status as PlanViewFillRenderer — authored, kept plausible, imported by nobody. Adding a room row there would be finding 4's trap in a second location."
  ]
}