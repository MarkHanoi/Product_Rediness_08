{
  "refuted": false,
  "reason": "The ROOT-CAUSE MECHANISM is confirmed line-for-line and is already a written-down OPEN defect. Verified in code: (a) `initScene.ts:3038` mints `_runConsolidatedTierPbrPass = () => runTierPbrPass('post-load')`, invoked from a second `pryzm-project-loaded` listener inside `setTimeout(0)`; the guard is `maybeAutoSwitchToWebGLForHeavyScene(scene, 'tier:'+reason, meshCount)` at `initScene.ts:3008` with `meshCount` from the traverse directly above; thresholds `SWAP_ELEMENT_THRESHOLD=400` / `SWAP_MESH_THRESHOLD=1000` (`heavySceneSwapThreshold.ts:40,43`) OR'd in `isSwapWorthyHeavyScene` (`autoWebGLHeavyScene.ts:109-113`). The other two sites are genuinely unreachable on the open path, and I confirmed the batch arm INDEPENDENTLY rather than taking the claim's word: `initBatchLifecycle.ts:121` hangs off `batchCoordinator.setGpuCompileStartCallback`, while `ProjectLoader.ts:733` (and `packages/persistence-client/src/loader/ProjectLoader.ts:337`) batch through `storeEventBus.beginBatch()` — a different bus, so the batch hook never fires on a load. `buildingGenerationLifecycle.ts:307` is the only proactive caller. C04 §1.4a carries the verbatim text and ISSUE-LOG L-1414 is ⛔ OPEN.\n\n(b) verified: `this._webGpuActive = true` is at `RenderPipelineManager.ts:762`, AFTER the `if (!isWebGPU) { … return; }` at :715-760, so it is true only on native WebGPU. Guards confirmed at :1957 (`scheduleShadowRebuild`), :3280 (`onProjectLoaded`), :4912 (`_rebuildPipeline`); `SHADOW_REBUILD_COMPLETE` is logged at :2037 inside `scheduleShadowRebuild`; `dispose()` nulls `_renderPipeline`/`_scenePass`/`_zonePass`/`_outputNode` and sets `_webGpuActive=false` at :3332-3349.\n\n(c) verified: `initScene.ts:2290-2298` — `const isWebGPUCapable = rendererResult.backend !== 'webgl-only'` then `webgpuCanvas.remove(); throw` → Phase-5 catch hands the viewport back to OBC, and the \"§L-372 Batch 2 / L-382 — this BOOT abort is intentionally LEFT AS-IS (zero boot-behavior change)\" comment is quoted accurately. `isUnintendedWebglOnlySwap` exists (`createRenderer.ts:321-326`) and is used at the swap arm. WOULD-BREAK #1 is real and correctly identified.\n(d) verified in `rendererRetirement.ts` (:117-120 rationale, :184 mint, :188-192 dispose wrap, :302-305 collected WeakRef, :374-381 the `detached===0 && minted>0` warn). (e) verified: ADR-0267 line 126 \"400 sits comfortably between 'trivial edit' … and 'building'\", the single ~1,300-element Fly run at :110-112, and `heavySceneSwapThreshold.ts:33-36`'s ONE DOOR counter-evidence.\n\nBUT FOUR THINGS ARE WRONG OR MISSING, and the claimed saving is not supportable:\n\n1. THE 16,011 ms IS NOT THE LATE GUARD'S COST AND IS NOT A 16,011 ms SAVING. `SHADOW_REBUILD_COMPLETE` is emitted by `scheduleShadowRebuild`, whose only load-path trigger is `RenderPipelineManager.onProjectSwitch()` → `this.scheduleShadowRebuild()` at RPM:3230, fired from `initScene.ts:3598`'s `pryzm-project-switch` listener, emitted at `PlatformShell.ts:248` — at the TOP of the open, ~50 lines BEFORE `getLatestVersion()` and before hydrate. `SHADOW_REBUILD_DEBOUNCE_MS = 100` (RPM:212). So that 16,011 ms span opens at the start of the open and overlaps the 17,163 ms hydrate and/or the 15,628 ms settle; it is not a third additive cost, and the claim's own (c) attributes that SAME 15,628 ms window to the classic renderer's first-frame `WebGLProgram` compile + geometry upload — which the fix explicitly does NOT remove. You cannot bank 16,011 ms out of a 15,628 ms window while keeping the thing you say fills it. The arithmetic does not close. It is also causally misattributed: the rebuild fires whether or not the swap ever happens; the fix suppresses it only as a side effect of `_webGpuActive===false`.\n\n2. THE CLAIM MISSES THE STRONGER MECHANISM FOR ITS OWN GAP. `open:first-interactive-frame` is marked from `getFrameScheduler().scheduleOnce('perf104-first-interactive-frame', …)` registered inside PlatformShell's own `pryzm-project-loaded` listener (`PlatformShell.ts:200-206`). The swap's SYNCHRONOUS prefix calls `unifiedFrameLoop.stop()` (`initScene.ts:4969`) before that scheduler callback can run. So the 15,628 ms gap is the frame loop being STOPPED by the swap — a cleaner causal chain than \"shader compile\", and one that strengthens the claim's direction while invalidating its number.\n\n3. AN UNNAMED BREAK, AND IT IS THE FOURTH RECURRENCE OF A DISPROVEN GATE. `setShadowReallocFrozen()` STILL opens with `if (!this._webGpuActive) return;` (RPM:2164) and its JSDoc still reads \"WebGPU-path only; inert on the WebGL2 fallback (which owns its own shadowMap)\" — the exact sentence L-1480 measured as FALSE thirty lines below in the same class (three r183 `WebGLShadowMap.js:95` renderer-level AND `:170` PER-LIGHT), and which L-3310 records as having \"cost the same defect three times\". On a classic-first open, the whole-load shadow freeze (`§FIX-SHADOW-LOAD-TIER-DESTROY`, `initScene.ts:3583-3597`) becomes structurally inert for exactly the heaviest projects it was written for — the founder's own census is 3,274 casters of 3,762 meshes re-submitted per shadow refresh, now unfrozen across a 17 s hydrate. WOULD-BREAK #4 has this backwards: the hazard is not a leaked freeze, it is NO freeze. (Balancing: the L-1480 outline-profiles chain CANNOT recur, because its step 1 requires `setShadowReallocFrozen(true)` to have asserted; and L-1482's `auditShadowCasters()` tripwire fires on the first presented frame on BOTH branches.)\n\n4. STEP 1 IS NOT IMPLEMENTABLE AS WRITTEN. `prewarmRenderer()` is fired from `src/main.ts:629` in Phase B — before the hub is shown, before any project is chosen — and calls `createRenderer(canvas)` at `rendererPrewarm.ts:79` with nothing to consult. No parameter can carry a project decision that does not exist yet. WOULD-BREAK #3 concedes the consequence but step 1 still reads as plumbing.\n\nONE THING THE CLAIM UNDERSELLS: its step-4 code change is not merely possible, the repo pre-staged for it. `createRenderer.ts:338-343` names the boot arm's non-local dependency out loud (\"correct only because of a NON-LOCAL invariant three thousand lines away … a gate that breaks the day the other guard moves\"), and `initScene.ts:3475` already reads `isPhase5Active && isLightweightWebGlBackend(pryzmRendererBackend)` — an exhaustive complement that a boot `webgl-only` lands in correctly.",
  "betterHypothesis": "THE DECISION DOES NOT NEED TO MOVE TO BOOT — ONLY BEFORE THE SCENE IS POPULATED, WHICH IS WHAT C04 §1.4a ACTUALLY ASKS FOR. Every hazard in WOULD-BREAK #1/#2/#3 is created by the extra step of moving it to renderer-construction time; none of them is required to close L-1414.\n\nThere is an existing seam that satisfies §1.4a exactly, and it needs no new machinery at all: `PlatformShell.setProjectContext`, between `markStartupPhase('open:version-read-done')` (PlatformShell.ts:298) and `this.versionCtrl.loadVersion(latest)` (:302). At that instant `latest.elementCount` (`PlatformShellTypes.ts:51`) is in hand, the scene is EMPTY, and `window.pryzmSwapRendererBackend` is already registered — `initScene.ts:5541` runs at `boot:scene-done`, which the documented boot order (`startupBudget.ts:157-158`) puts FOUR stages before `open:shell-context-set`. Fire the EXISTING `pryzmSwapRendererBackend('webgl-classic')` there when `latest.elementCount >= SWAP_ELEMENT_THRESHOLD`.\n\nWhat that buys, all through code paths already proven by the corner toggle:\n- The swap runs against an EMPTY scene: three traverses over ~0 meshes instead of 3×3762, and `retireRenderer` sweeps a renderer that minted almost nothing.\n- `_webGpuActive` is false before hydrate, so `_rebuildPipeline`'s own guard (RPM:4912) turns the already-armed `scheduleShadowRebuild` timer into a no-op, `onProjectLoaded()` no-ops (:3280), and `_loadTSL()` + `_buildPipeline()` never run (:781-783). That is the whole WebGPU-only family the claim wants gone.\n- `fireSwapToWebGL` sets the once-per-session guard, so the `tier:post-load` arm becomes a hard no-op at `autoWebGLHeavyScene.ts:213` — no double-fire, no `globalThis` flag to hand-set.\n- ZERO change to the Phase-5 boot abort (the swap path already distinguishes intended classic via `isUnintendedWebglOnlySwap`), ZERO prewarm discard, so the 2,401 ms adapter LONGTASK stays off the open path, and `swapMayPersistPreference` / L-1483 are untouched — the pin still survives on disk and the corner toggle still re-picks WebGPU for the session.\n\nResidual cost, stated rather than hidden: the prewarmed WebGPU renderer is still built and then destroyed — but on an empty scene, one `device.destroy()`, off the critical path. That is the honest price of not touching boot.\n\nTwo things must ship WITH it, and neither is optional:\n(i) Remove the `if (!this._webGpuActive) return;` at RenderPipelineManager.ts:2164 in `setShadowReallocFrozen` and delete the \"WebGPU-path only; inert on the WebGL2 fallback\" clause from its JSDoc — it is the same disproven belief L-1480/L-3310 already removed twice in this class, and moving the backend decision earlier is what makes it load-bearing. Without it, every large open loses the whole-load shadow freeze on a renderer with 3,274 casters.\n(ii) Re-measure, do not re-quote. The 16,011 ms `SHADOW_REBUILD_COMPLETE` is a `performance.now()` span across an async promise on a starved main thread, and it starts at project-SWITCH. The only defensible instrument is `open:snapshot-loaded → open:first-interactive-frame` with and without the early swap, plus a browser profile of that window — because `open:first-interactive-frame` is a frame-scheduler callback and the swap's `unifiedFrameLoop.stop()` is what currently holds it, so that mark alone will show whether the settle collapses.\n\nThe mesh-arm-only case (C04 §1.4a's original 233-element / 3,191-mesh project) is NOT closed by this and must not be marked closed: `elementCount` cannot see it. The persisted-mesh-count idea in the claim's step 3 remains the right exit for that arm, and `logSceneCensusOnce` already computes the input."
}