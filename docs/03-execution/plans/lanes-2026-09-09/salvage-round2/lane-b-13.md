{
  "refuted": false,
  "reason": "MECHANISM CONFIRMED IN SOURCE, AND STRENGTHENED BY EVIDENCE THE AUTHOR DID NOT HAVE. Every load-bearing citation checks out at HEAD: (1) `apps/editor/src/engine/SpaceEnvelopeMeshBuilder.ts` TOP/BOTTOM arm is literally `for (let i = 1; i < n - 1; i += 1) { p0 = ring[0]; p1 = ring[i]; p2 = ring[i+1] }` with the self-declared \"Correct for a CONVEX ring only\" comment; the side arm is an explicit per-edge quad, correct for any simple ring. (2) `attachSpaceEnvelopeRender` is constructed once and called from exactly one production site (`initTools.ts:2147`, onto `world.scene.three`); `spaceEnvelopeDragSurfaceThree.ts` generates no geometry (it only raycasts through the builder), and no other three.js drawer of this family exists. (3) `prismOfSpaceEnvelopeRecord` copies `record.footprint` verbatim — no reorder, no winding fix, no simplification — so the three.js and Cesium legs consume the identical ring, and `CesiumViewport.renderSpaceEnvelopes` hands it to `PolygonHierarchy` + `extrudedHeight` (earcut). (4) `BimWorld.ts:168-170` pins the scene root to identity, killing the transform/theta rival — and a theta error rotates rigidly, it cannot fold a cap. (5) Chronology exact: `678e744f` 2026-09-04 (the fan), `9d4ee67f` 2026-09-06 (extrude the permitted inset), `bea53747` 2026-09-07 (draw surface). (6) No test instantiates the builder's cap path; every fixture in the family is an axis-aligned rectangle.\n\nI ATTACKED THE ONE UNPROVEN LINK — \"are the real rings actually concave?\" — WITH AN INDEPENDENT MEASUREMENT AND IT SURVIVED, TWICE OVER. Running the repo's own production `insetPolygonPerEdge` on the repo's own documented \"the founder's plot class\" fixture (`packages/site-parcel-data/__tests__/insetPolygon.test.ts` IRREGULAR9, a 9-vertex parcel with two reflex corners, L-403): the inset comes back as a 21-vertex, non-convex, SIMPLE ring (0 proper self-intersections, min edge 0.40 m, area 837 m²), and the vertex-0 fan misplaces between 14.1 m² and 146.9 m² of triangle area depending on start vertex — the MINIMUM over all 21 rotations is non-zero, i.e. there is no correct rotation at all on this ring. That both corroborates the mechanism on real production data and matches the \"20-corner buildable footprint\" console line in scale.\n\nSecond, independent route the author missed and which is stronger than the ones cited: the \"fitted plate\" rung of `resolveFootprintSource` OUTRANKS the permitted-inset rung, and its rings come from `massingShapeOptions.ts`, whose L family builds an explicit 6-gon \"so the re-entrant corner is explicit\" (`ellPlacements`) and whose U family builds an 8-gon court (`uPlacements`) — and `enumerateMassingShapes` then FILTERS with `hasReflexCorner(ring)` under §NOT-A-RECTANGLE-WEARING-THE-LETTER, so any L/U plate offered is concave BY GATE, then clipped to the buildable outline (so vertex 0 has no reason to sit in the kernel). A U is not star-shaped at all: a fan from any vertex paints the court. That route landed 2026-09-07, after the fan.\n\nBEST RIVAL I COULD CONSTRUCT, AND WHY IT FAILED: \"the ring is not simple (a freehand bow-tie, or a self-overlapping inset), so BOTH views are wrong and earcut would not fix it.\" I confirmed the exposure is real — `ringIsDegenerate`/`ringSelfIntersects` has exactly one enforcement site, `SpaceEnvelopeFaceMove.ts:323/378` (the face-move planner); nothing guards CREATION — but the production inset output measured simple, and the shape-family rings are clipped simple polygons, so the rival does not describe the shipped ring producers. It stays as the author's secondary (a), correctly filed.\n\nRESIDUAL DOUBT, STATED HONESTLY: two premises remain unverifiable from this repo — the founder's actual footprint array, and that his 3D Site (Cesium) view was clean on the same records. The author says so. Two smaller corrections: \"the two views differ in exactly one algorithm\" is an overstatement (Cesium also applies project-north theta via `sceneXZToEnu`, adds `formaTerrainBaseHeight`, draws a full-alpha outline, and honours a drag-preview override) — none of which fold geometry, but the sentence is not literally true.",
  "evidenceChecked": [
    "apps/editor/src/engine/SpaceEnvelopeMeshBuilder.ts:427-489 — read the whole `_faceGeometry`: the side arm is an explicit per-edge quad (correct for any simple ring); the TOP/BOTTOM arm is the vertex-0 fan with the 'Correct for a CONVEX ring only' comment naming ShapeGeometry as the honest fix. Verified.",
    "apps/editor/src/engine/SpaceEnvelopeMeshBuilder.ts:151-265 — `updateSpaceEnvelope` builds one mesh per `spaceEnvelopeFaces()` entry with `userData.spaceEnvelopeFace`; shared MeshStandardMaterial, transparent + DoubleSide + depthWrite:false, renderOrder 1. Cap meshes carry `{kind:'top'}` / `{kind:'bottom'}` — the proposed fix preserves the face-index convention.",
    "apps/editor/src/engine/attachSpaceEnvelopeRender.ts (whole file) — one `new SpaceEnvelopeMeshBuilder(deps.scene)`; the only other geometry consumer is the plan-symbol reader (2-D linework).",
    "apps/editor/src/engine/initTools.ts:2147-2149 — the single production `attachSpaceEnvelopeRender({ store, scene: world.scene.three, ... })`; grep confirms one call site plus two wire specs.",
    "packages/core-app-model/src/BimWorld.ts:167-172 — scene root position/rotation/scale reset to identity then `updateMatrixWorld(true)`. Transform rival dead.",
    "packages/geometry-space-envelope/src/SpaceEnvelopeFaceDrag.ts:238-249 — `prismOfSpaceEnvelopeRecord` maps the ring verbatim (adds y:0 only). No reorder/normalisation between the two views.",
    "packages/geometry-space-envelope/src/SpaceEnvelopeGeometry.ts:145-151 — `spaceEnvelopeFaces` = n sides + top + bottom, side index = ring-edge index.",
    "packages/geometry-space-envelope/src/SpaceEnvelopeGeometry.ts:175-219 — `ringSelfIntersects` and `ringIsDegenerate`; grep shows the ONLY enforcement is `SpaceEnvelopeFaceMove.ts:323` and `:378`. Creation is unguarded — the author's secondary (a) confirmed.",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:8821-8925 — `renderSpaceEnvelopes`: same store, `positions = ring.map(toCartesian)`, `PolygonHierarchy` + height/extrudedHeight + closeTop/closeBottom, so triangulation is Cesium's earcut. Also applies theta and terrain base height (the claim's 'exactly one difference' is slightly overstated).",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:8698-8702 — the '`${inset.length}`-corner buildable footprint' log line exists as cited.",
    "apps/editor/src/ui/analysis/parcelLawEnvelopeAuthoring.ts:420-427 and :550-629 — `readEnvelopeRing` returns `env.insetPolygon`; `resolveFootprintSource` is a four-rung ladder: drawn perimeter > fitted plate > permitted inset > user-typed study. Three of the four can be concave.",
    "apps/editor/src/ui/site/envelopeAuthoringPlan.ts:297-310, :614-660 — the caller-supplied ring is deep-copied per storey (`footprintFor`) into every `AuthoredEnvelopeSpec`; one bad ring becomes N bad prisms with 2N bad caps.",
    "apps/editor/src/ui/site/massingShapeOptions.ts:651-690 (`ellPlacements`, explicit 6-gon 're-entrant corner'), :715-760 (`uPlacements`, 8-gon court), :1407-1412 (`hasReflexCorner` filter under §NOT-A-RECTANGLE-WEARING-THE-LETTER) — the plate rung is gated to emit CONCAVE rings. This route outranks the permitted inset and the author did not cite it.",
    "apps/editor/src/ui/site/ParcelBoundarySceneRenderer.ts:1069-1090 and :1185-1205 — the sibling three.js volume renderers (buildable envelope, context study) both use THREE.Shape in (x,−z) + ExtrudeGeometry + rotateX(−π/2); the proposed fix is a reuse of an established convention in this repo, not an invention, and confirms those surfaces are NOT affected.",
    "MEASURED (independent probe, repo's own production code, run this session then deleted): `insetPolygonPerEdge(IRREGULAR9, ...)` → inset is 21 vertices, non-convex, 0 proper self-intersections, area 837.4 m²; vertex-0 fan error across ALL 21 start rotations = min 14.1 m², max 146.9 m². Parcel itself: 9 vertices, 2 reflex, fan error 0–246 m² of 1386 m².",
    "apps/editor/src/engine/__tests__/ — no `SpaceEnvelopeMeshBuilder` spec exists; every footprint fixture in the family (`spaceEnvelopeFaceGizmo.spec.ts:39`, `spaceEnvelopeDragSurfacePorts.spec.ts:54/600/612`) is an axis-aligned rectangle. The convex-only fan shipped green and stayed green.",
    "apps/editor/src/engine/SpaceEnvelopeFaceGizmoBuilder.ts:120-205 — arrows are ONE envelope at a time (`setTarget`), hover-scoped, but built with `depthTest:false` so they draw through all geometry; on a 20-corner ring that is 22 double-headed cones over the hovered prism. Secondary (b) confirmed, and it is a real screenshot confound.",
    "git log -1 on 678e744f (2026-09-04, the fan), 9d4ee67f (2026-09-06, parcel-law envelope authoring), bea53747 (2026-09-07, 3D Site draw surface); `git log -- SpaceEnvelopeMeshBuilder.ts` shows only 678e744f and 9f4140c0. Chronology exactly as claimed.",
    "docs/04-reference/ISSUE-LOG.md — no row yet for this defect (latest is L-13266); L-13146/L-13147/L-13151 confirm the founder authors multi-storey level envelopes and reads them off the Cesium `[level@3.0m …]` log line."
  ],
  "betterHypothesis": "No rival displaced it, but ONE CHEAP DISCRIMINATOR THE AUTHOR DID NOT NAME should be applied before the fix is credited, because it can falsify the diagnosis from the screenshot alone with no console access: EVERY triangle a vertex-0 fan emits is COPLANAR at a single y (the loop writes the same `y` into all three vertices). A wrong cap therefore produces flat horizontal purple sheets overhanging the side-face fence, plus per-triangle shading discontinuity — `computeVertexNormals()` on a mixed-winding fan gives some cap triangles a downward normal under MeshStandardMaterial, which is what reads as a \"folded\" sheet. It CANNOT produce anything protruding out of the horizontal plane. So: if the founder's artefact includes geometry spiking VERTICALLY or diagonally out of a floor plane, the caps are not it, and the two candidates to check next are (i) the face gizmo — 22 `depthTest:false` cones on the hovered envelope, which literally are spikes drawn through all other geometry (`SpaceEnvelopeFaceGizmoBuilder`), and (ii) a non-simple authored ring, since `ringIsDegenerate`/`ringSelfIntersects` is enforced ONLY in the face-move planner (`SpaceEnvelopeFaceMove.ts:323/378`) and NOT at creation, so a freehand/arc perimeter that crosses itself reaches the store unchallenged — and that case would defeat the proposed fix too, because earcut on a bow-tie is garbage in ShapeGeometry exactly as it is in Cesium. Practical consequence: the fix is right and should ship, but the red-first test should include a self-intersecting ring case asserting a TYPED REFUSAL at creation (reusing `ringIsDegenerate`, which already exists), not just the concave-L case; and the browser check should confirm the residual after the fix, since the caps being flat means they cannot account for out-of-plane artefacts if any remain."
}