{
  "refuted": true,
  "reason": "The MECHANISM is confirmed but the ROOT-CAUSE ATTRIBUTION and the 12,000 ms saving are not, and are partly contradicted by code the brief did not read.\n\nCONFIRMED: hydrate is per-element command replay (ImportProjectCommand.ts:326-350 runSub = canExecute+execute); FurnitureStore.ts:51-52 fires an unbatched DOMEventBus window CustomEvent (DOMEventBus.ts:46-52) alongside the batched storeEventBus; ProjectLoader.ts:733 beginBatch does not reach it. Part (b) is fully code-confirmed: `grep -nE \"^\\s*yield( |;|$)\"` on ImportProjectCommand.ts returns exactly THREE sites (1074, 1107, 1218); the other four are `if (++_x >= CHUNK) { yield; }` with CHUNK=120, and walls=56/slabs=10/cw=90 never trip; executeChunked (:415-418) awaits yieldFn once per executed yield, so ~4 macro-tasks is real. The O(n^2) rule-outs (c) and exonerations (d)(e) check out, and FurnitureFragmentBuilder.updateFurniture uses a Map lookup (furnitureRoots.get), not an O(scene) search — so the builder genuinely is not the cost.\n\nREFUTED:\n1. The brief's premise that the DOM channel is ungoverned during load is half-false. `__pryzmProjectLoadActive` is set at ProjectLoader.ts:710 and cleared at :3228, spanning the ENTIRE hydrate window. shouldDeferPerAddGeometryPass() (perAddGeometryGate.ts:45), applied at initScene.ts:3070, already kills the TWO full scene.traverse() passes per geometry-add event, and initScene.ts:4121 already kills the shadow-flag pass. Those were the O(n^2) consumers of the DOM channel; both are closed.\n2. The four subscribers the fix names cannot hold 16 s at the brief's OWN unit (7.7 ms per full-scene pass over 3,762 meshes). UnifiedBrowserPanel.ts:127-158 subscribes to level/room/lighting/ceiling/floor/handrail/opening/plumbing/curtainwall but NOT wall, slab, furniture, door or window — i.e. not the dominant families (FurniturePart 1487 meshes, KitchenCabinetPart 647). ToolsRailController.refreshIfActive is `if (this._activeId !== id || !this._activeBuilder) return;` — an O(1) no-op unless that rail section is open, which it is not on a fresh open. WallEdgeVisibilityService.ts:106 covers wall/slab/floor/ceiling only (~66 events). initScene.ts:976/1035 are floor/room re-applies. Together well under 1 s. Every other DOM subscriber I read is O(1): SelectionManager.ts:1123-1127 nulls three refs, ElementTypeSelectorZone.ts:85-87 clears a Map, registerTransformDragHandler.ts:764 -> BimWorld.ts:326-334 writes five camera properties. HierarchyTreePanel — the one per-event full DOM re-render — is constructed only by DataWorkbench.ts:585 / LeftNavRail.ts:540 and is not present on a fresh open. So 12000ms has no derivation.\n3. The load-bearing premise \"the drain is the only heavy span inside 761->2876\" is untested, and there is a THIRD instrument the brief never named: §LOAD-HYDRATE-STEPS (ProjectLoader.ts:559-565) splits hydrate into load_setup / element_import (:915) / migrations (:2403) / nonelement_stores (:2872). nonelement_stores spans ~1,930 lines including the temporal-graph deserialize of 2,366 mutations and the view/sheet/schedule/annotation restores. The fix is aimed at a bucket not yet shown to be the big one.\n4. STEP 2 is materially dangerous, not a caveat. yieldForProgress resolves on the frame bus when visible (progressScheduler.ts). Today the load pays ~4 frame-waits; a 12 ms budget makes ~1,400, each a render of a half-built scene with 3,274 shadow casters. At even 50 ms/frame that adds ~70 s.\n5. One outright error: RenderPipelineManager.ts:2033-2040 takes __t_shadow_start before `Promise.resolve(this._rebuildPipeline())` and logs elapsed at the .then(). That continuation cannot run while the main thread is inside a multi-second synchronous chunk, so SHADOW_REBUILD_COMPLETE=16011.5ms OVERLAPS hydrate=17163.5ms rather than adding to it. \"Shadow rebuild is a further 16.0s / total ~37s\" double-counts.\n6. Option (a) is killed by the brief's own WOULD BREAK (2), which is correct: initTools registers a wall in the VDT before wallStore.add() precisely because add() fires synchronously, and updateFurniture is what performs scene.add(root) + elementRegistry.registerRoot. Buffering the DOM channel leaves the scene empty until endBatch. Option (b) is the safe shape but gates only the cheap subscribers.\n\nThe brief itself concedes it could not establish which listener owns the ~16 s residual; that concession plus points 1-2 means the named cause is not established, so refuted per the default.",
  "betterHypothesis": "Read the three instruments this run already printed BEFORE attributing anything: §LOAD-HYDRATE-STEPS (ProjectLoader.ts:559-565 — splits hydrate into load_setup / element_import / migrations / nonelement_stores), §LOAD-IMPORT-STEPS (:920-926) and §LOAD-YIELD-WAIT (:906-912). Two of the three were absent from the excerpt and the first was never even named.\n\nLeading structural hypothesis given what the code rules out: the 17.16 s lives in the SYNCHRONOUS PER-ELEMENT WORK ITSELF (sub-command bodies + non-element store restores), not in the DOM fan-out. Evidence: (i) the two O(scene) DOM subscribers are already gated by isProjectLoadActive() across the whole window (perAddGeometryGate.ts:45 at initScene.ts:3070; initScene.ts:4121), (ii) every remaining DOM subscriber I read is either O(1) (cache invalidation, five camera property writes), conditional on a closed panel, lazily constructed, or subscribed to families this project barely has, and (iii) the brief's own 7.7 ms/full-scene-pass calibration caps the entire remaining fan-out family around 1 s. 550 elements / 17,163 ms = 31 ms/element is uniform enough to look like per-element command-body cost (Zod parse + Immer produce deep-freeze snapshots + registry/spatial/VDT registration) or a large non-element restore (temporalGraph 2,366 mutations), both of which §LOAD-HYDRATE-STEPS and §LOAD-IMPORT-STEPS name directly in ms.\n\nIf a single import step dominates, fix that family's command body. If element_import is small and nonelement_stores is large, the DOM-channel work is the wrong lane entirely. Only if the steps sum well short of 17 s does the fan-out hypothesis survive — and even then the fix should start by instrumenting the DOM channel (count dispatches and listener wall-time per event name) rather than by batching it, because DOMEventBus ordering is a live invariant (initTools registers in the VDT before wallStore.add() because add() is synchronous; FurnitureFragmentBuilder is what performs scene.add(root)).\n\nIndependent of the attribution question, STEP 2 should NOT be shipped as written: with only 3 unconditional yields today, a 12 ms time budget multiplies frame-waits by ~350x on a scene with 3,274 shadow casters. Chunking cadence must be tuned against measured §LOAD-YIELD-WAIT, not chosen a priori."
}