{
  "refuted": false,
  "reason": "LENS = \"did they actually read the code they cite?\" Verdict: yes, to an unusually high standard. I checked every file:line in the evidence list against the working tree and all quoted TEXT is verbatim and in context. Confirmed exactly at the cited line: CesiumViewport.ts 5082 (the \"fires BEFORE renderFormaMassing re-seats ... both still hold the PREVIOUS parcel\" comment), 5092-5099 (resolveScopeAnchor call with parcelRingLonLat/frameOrigin/sameSiteRadiusM), 5103 (the single `void this.loadContextBuildings(anchor.lat, anchor.lon, true)`), 5065 (`if (this.formaMode)` gate — and the branch really does contain that ONE load and no sibling layer), 6979/6991 + 8209 (the only non-teardown writes of the two anchor candidates), 9956 (currentContextSite prefers formaMassingOrigin), 9971 (the \"a load for the current site is already in flight or imminent\" premise), 9979 (reseatAnchorForCurrentSite), 11991/11998 (contextBuildingsAt + applySiteScopeClip on the LOAD anchor), 11178 (clampTerrainThenReplace re-entry from maybeAttachTerrainProvider), 13352 (contextFarTierState memo), 13521 (the refusal call), 1632/11828/11842/18701 (contextLoadInFlight), 4671 (`return ltp ?? address`), contextExtentBudget.ts 330 (DEFAULT_SCOPE_RADIUS_M = 1781) and 176 (CTX_SCOPE_MAX_RADIUS_M = 7071), GISAreaLayout.ts 6942/6946/6959 (the three early returns, verbatim), siteDispatch.ts 1134/1140 (the setLtpOriginIfSafe SKIP log) and 1526-1529 (rebase BEFORE emit) and 1279 (restore re-emit). MEASURED, not trusted: I ran the pure resolveScopeAnchor at the founder's separation — scopeOuterRadiusM(DEFAULT_SITE_CONTEXT_SCOPE) = 1781.0 (rectangle half-extents 1781/sqrt2, so the half-diagonal really is the default the handler passes), separation 1139.4 m -> source=parcel-frame-origin (the PREVIOUS origin adopted), and at radius 900/500/250 -> source=requested. Both branches of the author's measurement reproduce. I also independently stress-tested the claim most likely to be wrong — G3's \"no accompanying context load of any kind\". clampTerrainThenReplace does re-enter renderFormaMassing at :9885 with `_skipTerrainClamp: true`, and the entire context kickoff block (applySiteScopeClip :8343 + loadContextBuildings :8344 + siblings) sits inside `if (!input._skipTerrainClamp) {` at :8268. So the replay genuinely fires no load. G3 holds. Grep confirms contextLoadInFlight appears at only 5 sites, none inside reseatAnchorForCurrentSite (9979-9997) — the \"premise is never verified\" claim is exact. Nothing I found contradicts the diagnosis.",
  "evidenceChecked": [
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:5065-5104 — the site.location-changed Forma branch: gate, resolveScopeAnchor call, log, and the SINGLE loadContextBuildings(anchor…) at :5103. Verified verbatim; no sibling layer load in the branch.",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:5082 — the 'fires BEFORE renderFormaMassing re-seats … both still hold the PREVIOUS parcel' hazard comment. Verbatim at the cited line.",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:6979, 6991, 6994, 8209 vs 18640/18644 — grep of ALL assignments to committedParcelLonLat and formaMassingOrigin. Only renderFormaMassing writes them; 18640/18644 are project-teardown nulls (a nuance the claim omits, but it cannot seat a stale value).",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:9956-9977 — currentContextSite() prefers formaMassingOrigin over readSiteLocation(), and its doc really does assert 'Both move with the site by construction'.",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:9971 + 9979-9997 — the refusal's premise text and the whole reseatAnchorForCurrentSite body. It consults ONLY loadedAt vs currentContextSite; RESEAT_ANCHOR_TOLERANCE_M = 250 (:2185).",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:1632, 11828-11848, 18701 — grep of every contextLoadInFlight site. The coalescer joins an identical in-flight load (1e-6° window) and the field is cleared on settle and on project-scope reset; it is never read by the refusal.",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:11991/11998 and 13352 — contextBuildingsAt and applySiteScopeClip(lat, lon) both take the LOAD anchor; contextFarTierState stamps {features, lat, lon} from the same load.",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:13511-13523 — rebuildContextFarTierForBase compares the memo against reseatAnchorForCurrentSite('far-tier', …) and returns on refusal, exactly as described.",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:8268 (`if (!input._skipTerrainClamp) {`), 8285, 8343, 8344 and 9885 — INDEPENDENT CHECK of G3. clampTerrainThenReplace replays renderFormaMassing with _skipTerrainClamp:true, and the whole context kickoff is inside the 8268 guard, so the replay fires NO context load. G3 confirmed.",
    "apps/editor/src/ui/geospatial/scopeAnchor.ts:170-243 — the full resolveScopeAnchor body: candidate ladder (parcel-centroid, then parcel-frame-origin), the `if (sep > radius) { … continue; }` rejection and the unconditional adoption return below it.",
    "MEASURED: ran resolveScopeAnchor via tsx at 1139.4 m separation. radius=1781 -> source=parcel-frame-origin (previous origin adopted); radius=900/500/250 -> source=requested. Also computed scopeOuterRadiusM(DEFAULT_SITE_CONTEXT_SCOPE) = 1781.0. Both halves of the author's measurement reproduce.",
    "apps/editor/src/ui/geospatial/contextExtentBudget.ts:176, 330, 343-347, 373-377 — CTX_SCOPE_MAX_RADIUS_M = 7071; DEFAULT_SCOPE_RADIUS_M = 1781; the default scope is a rectangle of half-extents 1781/sqrt2 whose circumscribing radius is 1781; and the handler passes the UNCLAMPED siteScope.scopeOuterRadiusM, not the clamped wrapper.",
    "apps/editor/src/ui/layout/GISAreaLayout.ts:6932-6959 — liveUpdateFormaMassing's three early returns at 6942 (generation in flight), 6946 (Cesium unmounted) and 6959 (map2d && !site3dPaned), all verbatim at the cited lines.",
    "apps/editor/src/ui/layout/GISAreaLayout.ts:7041-7055 — the event list that drives liveUpdateFormaMassing: parcel-boundary-set, zoning-updated, apartment/ceiling/furnish/lighting.layout-executed. site.location-changed is genuinely absent (its only subscription in this file is :7123 ensureClimateNow).",
    "apps/editor/src/ui/site/siteDispatch.ts:1134-1146, 1271, 1279-1283, 1526-1529 — setLtpOriginIfSafe and its boundary-committed SKIP log; the restore path's setLtpOriginForce + site.location-changed re-emit; the rebase-before-emit ordering.",
    "apps/editor/src/ui/site/siteGeocodeSearchBox.ts:162 — dispatchSiteLocation from the geocode box (file lives under ui/site/, not ui/geospatial/ as the citation's bare filename might suggest).",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:4661-4671 — readSiteLocation returns `ltp ?? address`.",
    "Test files named in the fix exist: apps/editor/src/ui/geospatial/__tests__/scopeAnchor.spec.ts and apps/editor/__tests__/CesiumViewportReseatAnchorCurrentSite.test.ts."
  ],
  "betterHypothesis": "Not refuted — but three things should be recorded rather than folded in silently. (1) THREE CITATION ANCHORS DRIFT (text verbatim, line off): scopeAnchor.ts's `if (sep > radius) {` is at :214, not :213; CesiumViewport's `void this.applySiteScopeClip(originLat, originLon, { skipSide: true })` is at :8343, not :8320 (8320 is the `try {` that opens the same block); GISAreaLayout's `for (const evt of [ 'site.parcel-boundary-set', …` is at :7041-7052, not :7032-7040. Every quoted STRING is real; only the anchors are stale, so nothing in the argument moves. (2) THE MAINLINE PATH SELF-CORRECTS, which the claim does not say out loud. On an ordinary parcel commit renderFormaMassing runs with _skipTerrainClamp falsy and fires `loadContextBuildings(originLat, originLon)` at :8344 at the NEW parcel; because that lat/lon differs from the stale anchor by >1e-6° it takes the UNCOALESCED branch (:11840) and aborts/replaces the mis-anchored load. So the defect's bite is entirely carried by G1/G2/G3 plus event ordering — which is exactly the falsification the author himself names, and which code alone cannot settle. Any fix should be justified on G1/G2/G3, not on the mainline. (3) PART 3 OF THE FIX HAS AN ORDERING DEPENDENCY THE CLAIM ASSERTS RATHER THAN PROVES. It needs `frameMovedTo` (the LTP origin) to have moved on a genuine site change — but setLtpOriginIfSafe SKIPS whenever a boundary is already committed (siteDispatch.ts:1140), and the parcel polygon is a C19 §1.4 one-shot whose only legal replacement route is clear-then-recreate (canCommitParcelBoundary, :1660-1682). Part 3 therefore fires only if the boundary is cleared BEFORE dispatchSiteLocation on the reselect path; if any surface dispatches the new location first, `frameMovedTo` still holds the OLD parcel, the stale candidates are co-located with it, and the new guard adopts them — the exact failure it was written to stop. Pin that ordering with a test before shipping Part 3. Also note the project-restore path uses setLtpOriginForce (:1271), not IfSafe, so it takes a different arm of the same discriminator."
}