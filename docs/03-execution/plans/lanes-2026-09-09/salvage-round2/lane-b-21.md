{
  "refuted": false,
  "reason": "Every file:line quote in the evidence list is verbatim and in context — I read each one. houseModalHtml.ts:588 is exactly `<div class=\"alm-header\">Design your house — live</div>`, with the §3PANE plan-LEFT/graph-CENTER/tools-RIGHT comment immediately above. HouseLayoutController.ts:333 `this.modal.show(` is confirmed as the SOLE show site (HouseLayoutModal is instantiated once, at :199; no other caller in apps/packages/plugins). HouseLayoutModal.ts:613 `document.body.appendChild(overlay)` is real, and the file header at line 6 independently states \"direct document.body.appendChild — NOT PanelManager\". parcelLawCreateHouse.ts:215 is exact and lines 213-214 are verbatim the quoted \"⛔ `autoBuild` is NOT passed — its default `false` is the modal path\". The three-line fall-through at 334-336 is exact, and :337 is ALREADY `return { mode: 'build-from-design', outcome: design };`, so the proposed deletion is literally a three-line removal. buildFromDesignPlan.ts:665 and :678 are exact. houseOrchestrator.ts:19, enumerate.ts:296, roomProgrammePanel.ts:2/:528, parcelLawTab.ts:1216, parcelLawQuestionGroup.ts:323, roomEnvelopePlan.ts:238, houseLayoutTrigger.ts:34, AIAreaLayout.ts:289 all check out. I also verified claims the author asserted without quoting: there is NO feature flag anywhere on the path (generateHouseFromBoundary at houseFromBoundary.ts:130 gates only on runtime/level/bus; request() gates only on active level, ≥3-wall shell, C58 height gate, variants.length>0); the modal-vs-direct switch is genuinely absence-driven at houseFromBoundary.ts:270 (`opts?.autoBuild === true ? _controller.buildDirect : _controller.request`); buildDirect is at :400 as stated; generationChatSeam.ts:651 does pass `autoBuild: true`; and OnboardingStepController.ts:4220's `controller.request` is the OFFICE controller, not the house one, so the two-production-call-site count is right. The fix's safety also holds under inspection: buildBuildFromDesignSection (createHouseSection.ts:229-260) emits an ENABLED button on any ok plan irrespective of rooms.length, wire()'s build-from-design branch (parcelLawCreateHouse.ts:365-402) routes the click to deps.buildFromDesign, and planBuildFromDesign has no zero-rooms refusal arm (every-room-refused at :1067 is guarded by `rooms.length > 0`). Three imprecisions, none load-bearing and none in a quoted evidence line: (1) the narrative says parcelLawCreateHouse.ts:492 calls deps.buildHouse — it is line 496; (2) showResult('2D') is line 4086, not 4085; (3) enumerate.ts:296's sentence continues \"— EXCEPT a universal `mandatory` failure, which IS surfaced as an empty/structured rejection (§68.2)\", so \"the engine is designed never to refuse\" is mildly overstated: it does refuse on a missing mandatory room set, and that refusal is what surfaces as variants.length===0 at HouseLayoutController.ts:283. The least-bad-ships unsoundness remains true for the other five hard rules. One unenumerated path exists — generateHouseInExistingShell (houseFromBoundary.ts:324 → _controller.request at :344) — but it is reachable only via window.pryzmGenerateHouseFromBoundary (houseLayoutTrigger.ts:36), console-only with no UI entry, so it does not contradict \"two production BIM-entry call sites\".",
  "evidenceChecked": [
    "apps/editor/src/ui/house-layout/houseModalHtml.ts:580-600 — 'Design your house — live' header + §3PANE comment: verbatim match",
    "apps/editor/src/ui/house-layout/HouseLayoutController.ts:95-130 — the request() jsdoc 'resolves as soon as modal.show is called': verbatim",
    "apps/editor/src/ui/house-layout/HouseLayoutController.ts:235-345 — the only pre-conditions are active level, closed ≥3-wall shell, §GEN-MAXHEIGHT-GATE (C58), variants.length>0; no flag; this.modal.show( is at :333",
    "apps/editor/src/ui/house-layout/HouseLayoutController.ts — grep 'buildDirect' confirms the sibling at :400",
    "apps/editor/src/ui/house-layout/HouseLayoutModal.ts:600-625 and header line 6 — document.body.appendChild(overlay) at :613, 'NOT PanelManager' stated in source",
    "grep -rn HouseLayoutModal across apps/packages/plugins/src — instantiated exactly once (HouseLayoutController.ts:199); no rival mount",
    "apps/editor/src/ui/analysis/parcelLawCreateHouse.ts:190-240 — buildHouse dep at :215 with the '⛔ autoBuild is NOT passed' comment at :213-214, verbatim",
    "apps/editor/src/ui/analysis/parcelLawCreateHouse.ts:290-340 — decide(): !houseOutcome.ok arm at :296, planBuildFromDesign at :320, fall-through at :334-336, build-from-design return already at :337",
    "apps/editor/src/ui/analysis/parcelLawCreateHouse.ts:358-470 — wire(): the build-from-design click branch exists and calls deps.buildFromDesign; deps.buildHouse is at :496 (narrative said 492)",
    "apps/editor/src/ui/site/buildFromDesignPlan.ts:650-700 — §SHELL-WITHOUT-ROOMS at :665 and the 'PANEL'S GENERATOR FALL-THROUGH IS PRESERVED' note at :678, both verbatim; rooms filter at :664",
    "apps/editor/src/ui/site/buildFromDesignPlan.ts — grep of all refuse() arms (644/652/659/710/830/848/852/1067); no zero-rooms refusal, and :1067 every-room-refused is guarded by rooms.length>0 (:1058 comment states this)",
    "apps/editor/src/ui/site/createHouseSection.ts:150-265 — buildBuildFromDesignSection renders an ENABLED button on the ok arm regardless of rooms.length; only the !ok arm disables it",
    "apps/editor/src/ui/onboarding/OnboardingStepController.ts:4060-4135 — await generateHouseFromBoundary( at :4123 inside generateHouse(); showResult('2D') at :4086 (cited 4085); :4220's controller.request is the OFFICE controller",
    "apps/editor/src/ui/house-layout/houseFromBoundary.ts:130-150 and :258-285 — no feature flag; opts?.autoBuild === true ? buildDirect : request at :270; generateHouseInExistingShell at :324 → _controller.request at :344 (console-only third path)",
    "apps/editor/src/ui/generation/generationChatSeam.ts:640-662 — the chat arm passes autoBuild: true at :651, so it never opened the modal",
    "packages/ai-host/src/workflows/houseLayout/houseOrchestrator.ts:15-22 — generateDeterministicLayouts import from apartmentLayout/tgl at :19, verbatim",
    "packages/ai-host/src/workflows/apartmentLayout/tgl/enumerate.ts:288-302 — ':296 the pool is NEVER emptied (a loud §TOPO-HARD-REJECT-ALL warning' verbatim, but the sentence carries an EXCEPT clause for universal mandatory failure that the claim omits",
    "apps/editor/src/ui/room-programme/roomProgrammePanel.ts:1-6 and :525-532 — header line 2 and programme.add-room dispatch at :528, both verbatim",
    "apps/editor/src/ui/analysis/parcelLawTab.ts:1212-1220 — bodyOf('rooms').appendChild(roomProgrammeSlot) at :1216, live-mounted",
    "apps/editor/src/ui/analysis/parcelLawQuestionGroup.ts:318-332 — id:'rooms', ordinal 4, 'What can I fit inside it?' at :323-326",
    "apps/editor/src/ui/room-programme/roomEnvelopePlan.ts:234-242 — role:'room' at :238",
    "apps/editor/src/ui/house-layout/houseLayoutTrigger.ts:30-38 and apps/editor/src/ui/layout/AIAreaLayout.ts:284-292 — pryzmGenerateHouse at :34, installHouseLayoutConsoleTrigger at :289",
    "repo-wide grep for generateHouseFromBoundary / houseFromBoundary importers — only parcelLawCreateHouse (:215), OnboardingStepController (:4123), generationChatSeam (:649, autoBuild true), houseLayoutTrigger (:34, console)"
  ],
  "betterHypothesis": "None — the root cause stands as written. Two refinements rather than a rival hypothesis: (1) the TGL engine is not literally incapable of refusing — enumerate.ts:296's own sentence carves out a universal `mandatory` failure, which surfaces as variants.length===0 and the 'No house layout fits this plot' toast at HouseLayoutController.ts:283; the unsoundness the founder saw is the least-bad ship for the other five hard rules (window/circulation/privacy/overlap/minarea), so state it that way rather than 'never refuses'. (2) The proposed fix leaves a copy defect that should ship in the same edit: with the fall-through removed and zero rooms, buildBuildFromDesignSection still prints 'You have drawn a level envelope and room envelopes, so PRYZM builds THOSE' (createHouseSection.ts:162-164) and 'Your 0 rooms (0.00 m²)' (:244) — both false on the very case the change creates. The replacement affordance the author already proposes must therefore also branch the head copy at rooms.length===0, not merely add a pointer line."
}