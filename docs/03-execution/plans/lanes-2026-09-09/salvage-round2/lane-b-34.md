{
  "refuted": false,
  "reason": "The core mechanism is CONFIRMED at every layer I could reach, and the author's own falsifier #3 (the one they flagged as their main gap) does NOT fire.\n\nVERIFIED AS CLAIMED:\n1. `packages/schemas/src/elements/SpaceEnvelope.ts:211,218` — `baseOffset: z.number().default(0)` / `height: z.number().positive().default(3)` are independent stored fields. Exact lines.\n2. Nothing re-derives them. `grep --include=*.ts \"spaceEnvelope.setParameter\"` across apps/plugins/packages/src returns SEVEN hits and NOT ONE is a production dispatch — only the handler class, the barrel registration, two tests, a sync-disposition row, and a ChatCapabilityRegistry line. This is STRONGER than the claim: no production code writes envelope height/baseOffset after creation by ANY route, not just the level route.\n3. `packages/command-registry/src` contains ZERO `SpaceEnvelope` references — `grep -rc` returned no non-zero file. `SetLevelHeightCommand` cannot touch envelopes. Its own header table (Wall/Slab/Column/Roof/Door/Window/Furniture/Plumbing/Lighting, then Beam/Stair/CurtainWall as the named non-followers) has no envelope row.\n4. `SetLevelHeightCommand` has exactly two call sites, both legacy `commandManager`: `apps/editor/src/ui/levels/LevelManagerPanel.ts:196` and `apps/editor/src/ui/property-panel/PropertyPanel.ts:1277` (the claim wrote `PropertyPanel.ts:1277` without the `property-panel/` segment — line correct, path abbreviated). The only level bus verbs are `level.update` and `level.add` (`initBusHandlers.ts:2355,2361`). No `level.setHeight`. `_cmExecOrRefuse` exists at :664 as cited.\n5. `CesiumViewport.ts:8891` — `const bottom = baseHeight + baseOffset;` where both come from `preview?.x ?? rec?.x`. The renderer never consults a level. A stale envelope is visible, not cosmetic.\n6. Requirement 2 is wired end-to-end with no gate, at EVERY layer: `SpaceEnvelopeTypes.ts:26` closed union; `SpaceEnvelopeFaceDrag.ts:74-78` ±Y for caps, `y:0` for sides; `SpaceEnvelopeGeometry.ts:145` returns `[...sides, {kind:'top'}, {kind:'bottom'}]`; `spaceEnvelopeFaceHandles` (`SpaceEnvelopeFaceGizmo.ts:265`) iterates that list with NO filter and is called with NO options at both sites (`SpaceEnvelopeFaceGizmoBuilder.ts:229`, `siteEnvelopeDrawCesium.ts:892`); `SpaceEnvelopeFacePick.ts:207-208` enumerates both caps; `SpaceEnvelopeFaceMove.ts:215-241` implements the cap branch fully; `spaceEnvelopeDragSurface.ts:602` refuses only on ROLE and then falls straight through to `active = {...}`. My repo-wide grep for `allowedFaces|lockVertical|faceFilter|canDragFace|isFaceDraggable|omitCap|capFaces|sidesOnly|onlySide` returned only unrelated `solarSurfaceFilter` hits.\n7. The `setParameter`-refuses / `setFootprint`+`moveFace`-adapt asymmetry is real: `MutateSpaceEnvelope.ts:411` uses `containmentRefusalFor`; `:307/:323` and the `moveFace` planner path use `containmentOutcomeFor` / `planSpaceEnvelopeFaceMoveInContext`.\n8. FALSIFIER #3 DOES NOT FIRE. `initScene.ts:878-882` constructs ONE `BimManager` and assigns it to `window.bimManager`; the same instance is threaded into `initTools.ts` (param at :406) into `commandContext` (:732) and into `new CommandManager(commandContext)` (:763). `BimKernel.getLevels()` (:406) reads `this.levels`; `BimKernel.updateLevel()` (:373) writes `this.levels`. The card's `readLevels()` and `SetLevelHeightCommand` are one collection. (A second Level aggregate DOES exist — `packages/stores/src/aggregate-commands/levelUpdate.ts` over `LevelStore`, with `levelNumber`/`buildingId` — but it is not on the `level.update` bus path, so seam A is not a store-reconciliation problem.)\n\nFOUR THINGS THE AUTHOR GOT WRONG (none refutes the mechanism; all break the fix as written):\n\nA. \"The ONLY place the level is consulted is `envelopeAuthoringPlan.ts:658-659`\" is FALSE, and the counter-example is dangerous. `apps/editor/src/ui/site/adoptProposalAsEnvelope.ts:305` calls the same `resolveStoreyHeight`, and its spec at :326-336 writes `baseOffset: 0, height: heightM, role: 'level'` — reached in production from `GISAreaLayout.ts:3906`. So TWO live producers mint `role:'level'` envelopes with OPPOSITE datum conventions: the card seats at `level.elevation` (absolute), adopt seats at `0`. A `bim-level-updated` subscriber writing `baseOffset := level.elevation` (the plan's seam C) would silently TELEPORT every adopt-created envelope up by the storey elevation. The plan's §D warns \"keep whichever convention you keep\" — but it does not know the tree already holds both.\n\nB. \"`level.update` … writes the number and fires NOTHING\" is FALSE as stated. `UpdateLevelCommand.execute` emits `bim-level-updated` at :90 (and :115 on undo). The SetLevelHeightCommand header sentence being quoted is about `BimKernel.updateLevel` not dispatching `spatial-authority-reconcile` — a narrower claim. Consequence for the plan: once seam C exists, `level.update {height}` ALREADY reaches the subscriber, so seam A is required for the LEVEL-STACK δ-cascade, not for the envelope. \"Dispatching height through this bridge would be a silent no-op — exactly the authored-but-unwired shape\" is the wrong diagnosis of the wrong seam.\n\nC. The cap gate is proposed UNSCOPED and would break a working gesture. `spaceEnvelopeDragSurface.ts:602` explicitly permits `role === 'room'` as well as `'level'`, and the planner models room cap moves. An unconditional `if (face.kind !== 'side') refuse` pins ROOM caps too — killing a double-height-room drag that works today. The refusal sentence the plan drafts (\"pinned to its storey … set by the level height\") is also false for a room.\n\nD. The honesty rung the plan relies on is already dead. `BimKernel.addLevel` coerces a missing/invalid height to `3.0`, and `updateLevel` spreads it forward, so `readLevelCandidates` (`adoptProposalAsEnvelope.ts:201-215`) never sees `height: null` for a bimManager level. `resolveStoreyHeight` therefore always takes the `'level-record'` rung in production and can NEVER return `'assumed-3m'`. Plan §B's \"keep its heightSource so an assumed 3 m is still labelled as assumed\" would stamp a confident `level-record` label on a coerced default — the exact confident-but-fabricated shape this repo tracks.\n\nAlso: the \"both requirements reduce to ONE mechanism\" framing is rhetorical. They are two independent absences (no re-derivation; no gate on an existing discriminant) that happen to share a subject. Nothing in the evidence makes one the cause of the other.",
  "evidenceChecked": [
    "packages/schemas/src/elements/SpaceEnvelope.ts:150-260 — read the field block; baseOffset at :211, height at :218 confirmed exact; levelId at :174 with a doc comment declaring the pair LEVEL-RELATIVE (contradicting the absolute convention the code implements)",
    "packages/command-registry/src/levels/SetLevelHeightCommand.ts:1-200 — header table, cascade rule, MIN/MAX constants, canExecute refusals; SPACE ENVELOPE absent from the follows/refuses table",
    "packages/command-registry/src/levels/UpdateLevelCommand.ts:1-120 — REFUTES 'fires NOTHING': emits bim-level-updated at :90 and :115",
    "grep -rc 'SpaceEnvelope' packages/command-registry/src — zero non-zero files; the omission is total",
    "grep -rn 'bim-level-updated' (repo, ex node_modules) — 9 hits; production consumers are geometryMutationEvents.ts:103 (which lists it as a NON-caster with a written reason) and geometry-slab/src/floor/FloorSlabBindingHandler.ts:45. No envelope consumer. The claim guessed roofWallClashAnnouncer.ts; the real second consumer is the slab binding handler",
    "grep --include=*.ts 'spaceEnvelope.setParameter' over apps/plugins/packages/src — 7 hits, ZERO production dispatch sites (handler, barrel, 2 tests, sync-disposition row, ChatCapabilityRegistry:3873 which claims it is 'edited in the Properties panel' — itself authored-but-unwired)",
    "apps/editor/src/ui/site/envelopeAuthoringPlan.ts:180-215 (the §DATUM comment naming four rasterisers that treat baseOffset as ABSOLUTE, and L-13147) and :630-690 (the create loop, baseOffset: level.elevation, height: heightM)",
    "apps/editor/src/ui/site/adoptProposalAsEnvelope.ts:161-197 (resolveStoreyHeight ladder), :199-215 (readLevelCandidates), :298-336 — SECOND production producer of role:'level' envelopes, writing baseOffset: 0",
    "apps/editor/src/ui/layout/GISAreaLayout.ts:3906 — confirms buildAdoptProposalPlan is live, not dead code",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:8850-8905 — bottom = baseHeight + baseOffset, both from preview ?? rec; no level read",
    "apps/editor/src/engine/initBusHandlers.ts:2320-2380 (the level bridges block: only level.update and level.add) and :660-700 (_cmExecOrRefuse exists as cited)",
    "grep for level.* bus verbs repo-wide — no level.setHeight anywhere; command-bus/src/commands.ts:975 declares only level.add",
    "apps/editor/src/ui/analysis/parcelLawEnvelopeAuthoring.ts:404-430 (readLevels = window.bimManager.getLevels), :920-1000 (readAll/previewPlan/storeyRows), :1440-1500 (addLevelsBtn awaiting level.add one at a time)",
    "packages/core-app-model/src/BimKernel.ts:338-408 — addLevel COERCES a missing height to 3.0; updateLevel spreads it forward and dispatches spatial-authority-reconcile only on an elevation change; getLevels/getLevelById read the same Map",
    "apps/editor/src/engine/initScene.ts:878-882 and initTools.ts:406/732/763/3693 — ONE BimManager instance reaches both window.bimManager and the CommandContext behind window.commandManager; falsifier #3 does not fire",
    "packages/stores/src/aggregate-commands/levelUpdate.ts:1-80 — a genuinely second Level aggregate (LevelStore, levelNumber/buildingId) exists but is NOT on the level.update bus path",
    "packages/geometry-space-envelope/src/SpaceEnvelopeTypes.ts:26, SpaceEnvelopeFaceDrag.ts:74-78, SpaceEnvelopeGeometry.ts:145, SpaceEnvelopeFaceGizmo.ts:146-152 + :250-282, SpaceEnvelopeFacePick.ts:207-208, SpaceEnvelopeFaceMove.ts:215-241 — the whole cap path, ungated",
    "apps/editor/src/engine/SpaceEnvelopeFaceGizmoBuilder.ts:229 and apps/editor/src/ui/site/siteEnvelopeDrawCesium.ts:735-757 (pickFace via pickNearestSpaceEnvelopeFace) + :892 — both handle call sites pass no options",
    "apps/editor/src/engine/spaceEnvelopeDragSurface.ts:1-80 (ports header), :555-650 (pick + onPointerDown: role refusal at :602, then straight to active) — role 'room' is explicitly permitted",
    "packages/geometry-space-envelope/src/SpaceEnvelopeContext.ts:280-380 (adaptRoomToMovedLevel drives cap moves through the planner — 'level' here means the LEVEL ENVELOPE, not a BIM level), :505, :685",
    "plugins/space-envelope/src/handlers/MutateSpaceEnvelope.ts:100-175 (move), :175-265 (moveFace, contextual planner), :290-340 (setFootprint via containmentOutcomeFor), :380-430 (setParameter via containmentRefusalFor) — the asymmetry is real",
    "apps/editor/src/engine/spaceEnvelopeWallFollowComposition.ts:1-70 — the existing §ENVELOPE-*-FOLLOW consequence precedent the plan cites; confirms the pattern but also confirms no LEVEL-follow sibling exists",
    "apps/editor/src/ui/room-programme/roomEnvelopePlan.ts:151-152, :198, :236-237 — rooms COPY the level envelope's baseOffset/height, so any re-seat propagates to rooms too",
    "grep for allowedFaces|lockVertical|faceFilter|canDragFace|isFaceDraggable|omitCap|capFaces|sidesOnly|onlySide — nothing; and every kind === 'side' comparison in the tree is labelling, solar filtering, or adaptation, never a gesture gate"
  ],
  "betterHypothesis": "Keep the mechanism, fix four factual errors, and weigh two rivals the author never named.\n\nSHARPENED ROOT CAUSE (survives adversarial reading):\n(1) A `role:'level'` space envelope stores a DUPLICATE of two facts the level already owns (`height`, and a base that is meant to be the storey seat), written once at create by TWO independent producers with CONTRADICTORY datum conventions — `envelopeAuthoringPlan.ts:658` seats at `level.elevation`, `adoptProposalAsEnvelope.ts:330` seats at `0` — and read back verbatim by every rasteriser. No production code re-derives, re-seats or even writes those fields afterwards by any route (`spaceEnvelope.setParameter` has ZERO production dispatch sites in the whole repo). The duplication, not the missing subscriber, is the defect; the missing subscriber is only the most visible symptom.\n(2) Independently, the horizontal/vertical distinction exists as a closed discriminant and is honoured by geometry, picking, handles and the planner, but NO layer treats it as a GESTURE policy — so a cap drag is fully wired on both surfaces.\n\nRIVAL 1 THE AUTHOR DID NOT CONSIDER — derive, don't cache. Once requirement 2 pins the caps for `role:'level'`, that envelope's `height`/`baseOffset` become write-only-by-the-level, i.e. DERIVED. Storing them and repairing them from a `bim-level-updated` subscriber is a cache with a refresher: it is wrong for the whole window between the level write and the repair, and it diverges permanently on every path that bypasses the event — persistence load of a project saved before the fix, undo of a level edit, IFC/Revit import, any future level writer. That is a second answer to \"how tall is this storey\" (C84 EI-9), which is the rule the plan invokes against a per-envelope height field and then breaks with its own subscriber. Resolving `height`/`baseOffset` for `role:'level'` at read time from `levelId` removes the class instead of scheduling a repair for it. It is more work (the mesh builder, the Cesium rasteriser, the plan-symbol builder and `roomEnvelopePlan` all read the record), but it is the answer that cannot go stale — and the plan should at minimum state why it is rejected.\n\nRIVAL 2 THE AUTHOR DID NOT CONSIDER — route the cap drag instead of refusing it. The plan's requirement 2 removes an affordance. The alternative that satisfies the same ownership rule is to make a cap drag on a `role:'level'` envelope DISPATCH the storey-height change (through the very `level.setHeight` bridge seam A already builds) rather than raise a refusal. The user gets \"the level height owns this, and here is the handle that sets it\" instead of \"this handle is gone.\" Same single owner, no dead arrow, and it reuses seam A rather than needing an unrelated fourth file. Whether the founder wants the gesture removed or redirected is a question for him — the plan should not settle it silently on the removal side.\n\nREQUIRED CORRECTIONS TO THE PLAN AS WRITTEN:\nA. Before ANY re-seat subscriber, reconcile the two datum producers. `adoptProposalAsEnvelope.ts:330` writes `baseOffset: 0`; a subscriber writing `baseOffset := level.elevation` will teleport every envelope that path created. Pick one convention, migrate both producers, and only then add the consequence — or scope the subscriber to `height` alone and leave `baseOffset` to L-13147.\nB. Drop the \"level.update fires NOTHING / would be a silent no-op\" reasoning. `UpdateLevelCommand:90` already emits `bim-level-updated`. Seam A is needed for the δ-cascade of the levels ABOVE, and for the refusal `_cmExecOrRefuse` surfaces — not for the envelope, which seam C picks up either way. Justify the bridge on the cascade, or the reviewer will find the emit and distrust the rest.\nC. Scope the cap gate by role: `if (record.role === 'level' && face.kind !== 'side')`. Room caps drag today and the planner models them; the plan's unconditional refusal is a regression with a sentence that is untrue for rooms.\nD. Do not rely on `heightSource` for honesty. `BimKernel.addLevel` coerces a missing height to 3.0, so `resolveStoreyHeight` always reports `'level-record'` for a bimManager level and `'assumed-3m'` is unreachable in production. Either make the coercion visible (a nullable `height` on the Level record, or a flag) or drop the honesty label rather than print a false one.\nE. Settle the undo question before writing code, as the plan itself says: SetLevelHeightCommand is one legacy entry and N envelope re-seats are N bus entries. Note that `roomEnvelopePlan` copies the level envelope's `baseOffset`/`height` into every room, so the re-seat fan-out is levels x rooms, not levels."
}