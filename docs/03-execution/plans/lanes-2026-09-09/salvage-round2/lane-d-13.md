{
  "refuted": true,
  "reason": "The claim's CODE FACTS are almost all verifiable, but the ROOT CAUSE is not established and fails the arithmetic lens. Confirmed by reading the files: hydrate brackets are exactly ProjectLoader.ts:761/:2876; the drain is :879; ImportProjectCommand.ts:326-350 `runSub` is `cmd.canExecute(ctx)` then `cmd.execute(ctx)` per element (replay, not deserialise); FurnitureStore.ts:51-52 fires the DOM channel then the batched one; DOMEventBus.ts:46-52 is a bare synchronous `window.dispatchEvent` with no batch awareness; `beginBatch()` at :733 covers only storeEventBus; initBuilders.ts:883 → :843 → FurnitureFragmentBuilder:79 builds the subtree inline; IMPORT_CHUNK_SIZE=120 at :143 with yields ONLY at 632/747/1074/1077/1107/1218/1221 (three unconditional + four per-120 loops), so with walls=56/slabs=10/curtainWalls=90 the drain really is ~4-6 macro-tasks — that part is a genuine, correct contribution. The exonerations also check out (TopologyLayer.ts:365-366 lazy; LightingFragmentBuilder.ts:773-780 coalesced; VDT rides the batched bus). WHY IT IS STILL REFUTED: (1) IT DOES NOT EXPLAIN THE NUMBER, BY ITS OWN ADMISSION. Its positive attributions total <1.2 s of 17.16 s; ~93% is conceded as \"could not establish\", and it hedges the cause into two different fixes (\"the unbatched fan-out PLUS un-decomposed sub-command bodies\"). A residual is not a root cause. (2) THE REFUTATION OF \"MESH CONSTRUCTION\" MISUSES ITS OWN SOURCE. HandrailLoadBuildCost.spec.ts:29-31 says verbatim: \"There is no GPU in this process: every THREE upload, shader compile and PSO build is absent, so this is a LOWER BOUND on the real cost and must never be quoted as the whole of it.\" The claim quoted the NEXT ⛔ (the DOM fan-out one) and skipped this one, then extrapolated the lower bound to \"<6% of the 17.16 s\". Worse, the spec's subject is handrails driven through `InstancedElementRenderer` + `ElementInstanceBridge` (instancing ON vs OFF is its entire point), while the founder's dominant families in the same log are stamped `instanced? NO` — FurniturePart 1487 + KitchenCabinetPart 647 + Lighting 336 + PlumbingFixture 201 = 2,671 of 3,762 meshes, 442k tris — on a real-WebGPU device that in that very log loses the device and force-swaps to WebGL. Handrail-in-happy-dom ms/mesh does not transfer. (3) IT RULES OUT WAITING WITHOUT THE INSTRUMENT, AND THE CODE WARNS ABOUT EXACTLY THAT. `executeChunked` awaits `yieldForProgress('project-load-chunk','post-render')` — it waits for a RENDERED frame. ProjectLoader.ts's own §LOAD-YIELD-WAIT comment (L-3054) says: \"if a frame costs 500 ms, every yield costs 500 ms … with ZERO of it visible in any existing counter.\" Few yields bounds the COUNT, not the COST: 4-6 frames of a half-built non-instanced scene compiling new PSOs is a live explanation of most of 17 s, and it would read as `waiting`, not `compute`. The claim asserts compute-dominated (\"~4 unyielding macro-tasks of several seconds each\") while conceding it never read §LOAD-CHUNKED or §LOAD-YIELD-WAIT. (4) THE SUBSCRIBER ENUMERATION IS WRONG IN BOTH DIRECTIONS. `UnifiedBrowserPanel.ts:127-158` does NOT register `bim-furniture-added`, `-wall-added` or `-slab-added` (the big families), and `ToolsRailController.refreshIfActive` (:150-156) early-returns unless that exact section is active — nothing establishes any rail was open. initScene.ts:976-980/:1035-1039 are `queueMicrotask` handlers on `bim-floor-*`/`bim-room-*`, so the claim's own microtask argument from (c)(3) applies and their count is bounded by floors/rooms, not 550. Meanwhile it MISSED `HierarchyTreePanel.ts:1336-1343`, which does `this._elementCache.clear(); this._render();` synchronously and undebounced per element event — a bigger candidate than three of the four it names. And the LARGEST O(n²) on that channel is ALREADY GATED: initScene.ts:3070 `if (shouldDeferPerAddGeometryPass(batchCoordinator.isBatching)) return;` skips both full `scene.traverse` passes during load, which makes the 16 s residual HARDER for the fan-out to carry, not easier. (5) THE REPO ALREADY REACHED THIS EXACT POINT AND CALLED IT UNATTRIBUTED. ISSUE-LOG L-3058 (2026-08-21) names the same three candidates — DOM fan-out / sub-command bodies / frame-bus waiting — as mutually exclusive, ships a `window.dispatchEvent`-wrapping probe to discriminate them, and states \"The instruments now exist; the reading does not.\" The claim picks candidate #1 without running that probe. (6) ARITHMETIC AGAINST A PER-ELEMENT FAN-OUT. L-3058's run: 259 elements, ~1,843-3,898 meshes, 32-47 s ⇒ 124-182 ms/element. This run: 550 elements, 3,762 meshes, 17.16 s ⇒ 31 ms/element. Element count more than doubled at essentially the same mesh count and per-element cost fell 4-6×. A per-element DOM fan-out into a scene-sized listener set predicts flat-or-rising ms/element, not a 6× collapse. Also: the ledger row the claim cites to exonerate `CreateWallCommand.ts:446` is stale — that clone was already guarded on `__pryzmProjectLoadActive` (LEDGER row 3, DONE 2026-08-21, L-3057, 68724847), and line 446 today is `derivedFrom` field forwarding, not `_neighbourSnapshot`. Finally, CLAIMED SAVING 12,000 ms has no basis: no line in the supplied log attributes a single millisecond to the DOM channel.",
  "betterHypothesis": "Hydrate's 17.16 s is NOT YET ATTRIBUTED, and three discriminating readings already exist for this exact run — take them before any code change. The strongest untested candidate is the one the claim asserted away: hydrate is dominated by a SMALL NUMBER OF VERY EXPENSIVE FRAMES, not by per-element compute. `executeChunked` (ImportProjectCommand.ts:411-419) awaits `yieldForProgress('project-load-chunk','post-render')` at each of only ~4-6 generator yields (632/747/1074/1077/1107/1218/1221, none of the per-120 arms tripping at walls=56/slabs=10/curtainWalls=90). Each of those awaits a RENDERED frame of a half-built scene on a real-WebGPU pipeline that must compile the newly added, non-instanced, per-element-unique materials for 2,671 of 3,762 meshes — the same pipeline that in this log loses its device (\"reason=destroyed\") and force-swaps to WebGL. Four frames at 3-4 s is 17 s, and it lands in `waiting`, not `compute`. ProjectLoader's own §LOAD-YIELD-WAIT comment (L-3054) predicts precisely this shape. This also fits the arithmetic the fan-out theory cannot: 259 elements → 124-182 ms/element vs 550 elements → 31 ms/element at the same ~3,800-mesh scene means cost tracks FRAMES-AND-FAMILY-COMPOSITION, not element count. THE THREE READINGS, in order, all free: (1) §LOAD-YIELD-WAIT (ProjectLoader.ts:906-912) — if `waiting` ≫ `compute`, the fix is the frame the loader waits for (defer material/PSO work or make the yield not wait on a render), and every line of the DOM-batching plan is wasted. (2) §LOAD-IMPORT-STEPS (:920-926, unconditional, 20 steps heaviest-first) — if one family dominates, the fix is that family's Create*Command body. (3) The L-3058 probe already written out verbatim in ISSUE-LOG (wrap `window.dispatchEvent`, open the project, call `window.__pryzmEvtReport()`) — this is the ONLY thing that measures the DOM fan-out, and the ISSUE-LOG spells out what each of its three outcomes proves. Only if (3) shows a `bim-*-added` row in the tens of thousands of ms does the claim's Step 1 become the right fix; and note the single largest O(n²) subscriber (initScene.ts:3070 tier+PBR double-traverse) and the shadow pass (:4121) and the wall clone (L-3057) are ALREADY load-gated, so the fan-out's remaining unguarded surface is smaller than the claim assumes. Two things in the claim are worth keeping regardless of the reading: the chunker genuinely never chunks this project (IMPORT_CHUNK_SIZE=120 is an element count, largest family is 90), so a TIME-budget yield is a correct freeze fix independent of the root cause; and `HierarchyTreePanel.ts:1336-1343` (`_elementCache.clear(); _render()` synchronous, undebounced, per element event) is an unguarded subscriber the claim missed and is a better Step-1(b) target than `UnifiedBrowserPanel`, which does not even listen to the furniture/wall/slab add events."
}