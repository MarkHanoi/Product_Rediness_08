{
  "survivingTitles": [
    "The full server snapshot is downloaded on the critical path, serially after the engine boot — and discarded whenever local history exists",
    "The heavy-scene backend swap fires DURING the open and tears the renderer down — that is the \"WebGPU device lost\" in the founder's console",
    "A hub-open never prints the §STARTUP-BUDGET table — one call site is missing, and that is why every open reading is a guess",
    "`endBootLeg` drops the untiled tail — the brand-new builders profiler cannot see `await initBuilders(...)`, the exact 18.6 s span it was written to explain",
    "`open:first-interactive-frame` is the LAST open mark and it fires before the heaviest post-load work — the founder is waiting inside an unnamed interval, for the fourth time",
    "The open path re-fetches the project list the hub just fetched — and it is the FIRST thing in the serial chain, blocking the engine boot",
    "Every open serialises the whole project at least twice and captures the framebuffer twice — for a project the user has not touched",
    "`openProject` resolves and the loading overlay hides BEFORE the project is loaded — so every \"open took N ms\" measured from that promise is measuring the boot",
    "web-ifc (3.5 MB) is still statically on the engine-boot graph via the `@pryzm/file-format` barrel — diagnosis fix #7, not applied"
  ],
  "refutedTitles": [
    "Two socket.io clients connect and join the same project room on every open",
    "The whole-corpus version-mirror warm reads EVERY project's history, and it is awaited on the deep-link / reload open path"
  ],
  "refutations": [
    "REFUTED AS AN OPEN-TIME COST (the duplication itself is real). Verified: PlatformShell.ts:250 → PlatformCollabPill.ts:183-191 mints one client; engineLauncher.ts:1202 → initCollaboration.ts:570-597 mints a second, and both emit `join-project` for the same room. But NOTHING on the open critical path awaits either. PlatformCollabPill.ts:217-227 injects `/socket.io/socket.io.js` as a `<script>` tag with `script.onload = loadAndConnect` — fire-and-forget, off the serial chain; `initCollaboration`'s client is likewise wired at boot with no await. The 150 ms is derived from ProjectHub.ts:775-783, but that comment is about serialising base64 image UPLOADS (XHR) against the ~6-connection-per-host HTTP budget — WebSockets do not draw on that pool in Chromium, and socket.io tries `websocket` first here. The only shared-resource cost is two server-side `canUserAccessProject` checks (server.js:566), which is server CPU, not open latency. Real defect, wrong dimension: this belongs on a resource/architecture axis, not open-timeline. Sending a lane at it to shorten the founder's open would be sending it at the wrong target.",
    "REFUTED ON THE FOUNDER'S PATH, and its one supporting scenario is a misread. Verified: VersionCacheStore.ts:115-148 really does cursor the WHOLE versions store into `_versionMirror`, and warmVersionCache (ProjectRepository.ts:971-995) really does add a full localStorage scan. But the finding concedes the decisive point itself — on a hub-open the hub already ran it (`hub:warm-versions-done`), so `_warmed` (VersionCacheStore.ts:116 `if (this._warmed) return;`) makes BOTH PlatformShell call sites (:288 fire-and-forget, :329 awaited) free. The scenario it invokes to keep the cost on the critical path — 'a reopen-after-renderer-swap, which PlatformRouter.ts:1110-1118 deliberately performs via sessionStorage' — is wrong: that `LAST_OPEN_PROJECT_KEY` write exists for the MANUAL renderer-backend toggle, whose own comment says it 'persists + full-page-reloads'. The §AUTO-WEBGL-HEAVY swap in finding 2 is a LIVE swap (initScene.ts:4858 rebuilds in place, no reload), so it never reaches that path. What remains is deep-link and hard-reload — real, but not the reported complaint. The 250 ms is also unmeasured and probably overstated: warm() copies compressed strings key→value, it does not inflate or parse them (the inflate is `getLatestVersion`, already marked at 89 ms). Confidence 'high' is not earned."
  ],
  "missed": [
    "THE BIGGEST ONE: the hub-open path never pre-warms the engine chunk, so ~17 MB of JS downloads AND evaluates inside the open. `ensureEngineWarm()` has exactly two production call sites — PlatformRouter.ts:720 (`showOnboarding`) and :989 (`createAndOpenProject`). Both are ONBOARDING. A plain hub-open reaches `attachedBootstrap.ensure()` → main.ts:342-360 `workspaceMount.ensure()` → `startEngine()` → `loadEngine()` → a COLD `import('@app/engine/engineLauncher')`. main.ts:757-760 states this outright: 'Engine bundle is loaded on-demand when the user first opens a project… Three.js + web-ifc parse/evaluate.' Measured in the built artifacts: dist/assets/engineLauncher-WbeRo0_Y.js 4,899,541 B; domain-engine-qExM3Y4v.js 4,494,087 B; vendor-web-ifc-zl-EjlDS.js 3,556,771 B; vendor-thatopen 2,285,651 B; vendor-three 1,865,583 B. All of it lands between `boot:ensure-requested` and `boot:engine-start`. This SUBSUMES finding 10 (web-ifc is one chunk of five) and the fix is one line at hub mount — the exact trade engineWarmup.ts already documents as proven for onboarding. Note this also CORRECTS finding 10's own caveat: it claims the cost 'lands BEFORE hub:open-clicked'. It does not, on the hub path.",
    "Finding 5 names the post-load storm but misses the items actually printing in the founder's log. initScene.ts:4640 auto-opens the split view and rebuilds the Canvas2D plan — its own comment prices the pre-fix version at '~300 ms' of blocked main thread — and THAT is what drives the three unattributed log lines: `[EdgeProjectorService] Native projection done - 69 group(s), 5 frame yield(s), work=109ms yieldCalendar~83ms, 410 edge geometries across 63 ISO layer(s)`, `[HiddenLineRemoval] v3 - 26 occluder(s)`, `[VGSceneApplicator] styledLayers=22 across 14 VG categories`. Two more unmarked post-load listeners: initScene.ts:4173 `_runDeferredLoadShadowPass` (the deferred shadow-quality escalation regen) and initScene.ts:3723 `renderPipelineManager.onProjectLoaded()`. None of the five has a mark on either side. (I would NOT chase the `cacheHits=0 … hitRate=0%` line — 63 cacheable groups on a first open is an expected cold-cache miss, not a defect.)",
    "Finding 2 prices the swap but not the work the swap DISCARDS, and the listener order makes that compounding. `pryzm-project-loaded` handlers run in registration order: initScene.ts:3723 (`renderPipelineManager.onProjectLoaded()` — schedules the pipeline rebuild) and :4173 (shadow regen) are registered BEFORE :3758, whose `setTimeout(0)` tier pass calls `maybeAutoSwitchToWebGLForHeavyScene` → `swapRendererBackend`, whose step 2 is `rpm.dispose()`. So the open rebuilds the TSL pipeline and regenerates shadows on a renderer it destroys milliseconds later, then recompiles everything on the classic backend. The fix in finding 2 (decide the backend before the FIRST createRenderer) removes this too, which strengthens its case.",
    "Sharpening on finding 4 that the auditor left on the table: bootStepProfile.ts:37-42 already MEASURED the builders themselves — 'TOTAL 11.4 ms over 51 constructor steps' against a mark reading 18,626 ms. So the 18.6 s is either inside the tiled steps (:320→:418, already visible in the table) or inside the tail (:418→:460, the one segment `endBootLeg` excludes from both the rows and `wallMs`). The tail contains the single `await` in the leg. That makes the untiled tail the PRIME SUSPECT for the whole 18.6 s, not merely an accounting gap — which raises finding 4 from 'instrument hygiene' to 'the one line that would locate 85% of the startup'."
  ]
}