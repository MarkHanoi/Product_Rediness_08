{
  "survivingTitles": [
    "Section poché can never render: PocheFillBuilder discards world Y, so a vertical cut ring collapses to a 2-vertex degenerate and is dropped",
    "The test that should have caught the section/elevation poché collapse uses a HORIZONTAL fixture labelled as an elevation cut band",
    "Symbol coverage is 15 for plan, 3 for elevation, 1 for section — and lighting has no injector for any view",
    "The LIVE elevation/section driver (PlanViewManager, DOC-19B) never injects level datums or grid lines — all four call sites that do are on paths this route does not use",
    "Two rival elevation implementations share the name 'Front': the ViewCube preset is a PERSPECTIVE 3D camera with plan symbols explicitly disabled, and it is what runs whenever no ViewDefinition id is queued",
    "Auto-dimension covers plan and elevation and honestly refuses section — this one is NOT a defect and should be left alone"
  ],
  "refutedTitles": [
    "Clicking an elevation or a section in Views & Sheets skips the GIS exit — the L-13253 'covered by Cesium' bug the founder reported is fixed for plans and 3D only",
    "Selecting a section or elevation in the plan pane's view dropdown never triggers a projection — it renders whatever happens to be cached, which for a never-opened section is nothing"
  ],
  "refutations": [
    "FINDING 5 REFUTED — the GIS exit DOES run for elevation and section; the auditor stopped one line short. `ViewsRailPanel._onActivateView` ends with `this._props.onViewSelect(obcId)` (ViewsRailPanel.ts:832). There is exactly ONE construction of that props chain: `apps/editor/src/ui/layout/NavigationAreaLayout.ts:125` — `new ProjectBrowserPanel({ onViewSelect: (id) => gis.activateView(id as any), ... })` — and props pass through UNCHANGED at ProjectBrowserPanel.ts:212 → DocumentsBrowserPanel.ts:74 → `new ViewsRailPanel(props, ...)`. `gis` is typed `GISCallbacks` (NavigationAreaLayout.ts:28) whose `activateView` is GISAreaLayout's own (declared GISAreaLayout.ts:576, returned at :8531), and its FIRST statement is `if (_gisActive) { toggleGIS(false); }` (GISAreaLayout.ts:1640-1642). So an elevation click passes obcId 'Front' and a section click passes 'Section' straight into the same choke point, one line after the skipped `targetBimMode` block. The exit precedes any mode validation, so even the untyped 'Section' triggers it. The `targetBimMode` block added by 5d9cacf6 is therefore a REDUNDANT SECOND DISPATCH of a function the route was already calling, not the only route to it. Two supporting corrections: (a) the auditor's hedge that `pryzmActivateBimView` is typed `(m?: 'Top' | '3D')` is wrong — `apps/editor/src/types/globals.d.ts:278` declares `(mode?: 'Top' | '3D' | 'Front' | 'Back' | 'Left' | 'Right')` and GISAreaLayout.ts:2665 forwards all six, so elevations have a declared full-screen route already; (b) `git show 5d9cacf6^:apps/editor/src/ui/layout/NavigationAreaLayout.ts` shows `onViewSelect: (id) => gis.activateView(id as any)` was ALREADY there before the fix, which means the commit's stated root cause ('the rail went straight to ViewController, so it inherited neither the GIS exit nor the legacy-bar retirement') is contradicted by the code it was written against. The proposed two-line fix would add a third dispatch of the same exit and change nothing the founder can see.",
    "FINDING 6 REFUTED — the warm-up the finding says is absent exists, and it is section/elevation-specific. `SplitViewManager._onViewSelectChange` does emit `split-view-view-changed` (SplitViewManager.ts:1253), and `PlanViewManager` subscribes to that exact event at PlanViewManager.ts:185. The handler is `_onSplitViewViewChanged` at PlanViewManager.ts:1165-1175, six lines long and doing precisely the job claimed missing: `if (!viewId || viewTechnicalDrawingCache.get(viewId)) return;` — a cache MISS falls through — `const viewDef = viewDefinitionStore.get(viewId); if (!viewDef) return; if (viewDef.viewType === 'section' || viewDef.viewType === 'elevation') { this._ensureProjectionForSplitView(viewDef); }`. That is a cache-miss-gated projection warm-up for exactly the two view types the finding is about, calling exactly the entry point the finding's own fix recommends. The auditor cited the ADJACENT arm — `_onViewUpdated`'s `else if (viewId === this._activeSplitViewId)` branch at :944-951, which fires on `vd:view-updated` — and concluded from it that the select-change case was uncovered, having missed the handler 215 lines below. A second, weaker path also exists (`initScene.ts:1523` → `viewDependencyTracker.notifyViewActivated`, though that one early-returns unless the view is in `_deferredDirtyViewIds`, ViewDependencyTracker.ts:518). The read-only-contract observation and the `_onViewSelectChange` body are accurately quoted; the conclusion drawn from them is wrong."
  ],
  "missed": [
    "THE L-13253 FIX (5d9cacf6, shipped yesterday) IS A DUPLICATE OF A PATH THAT ALREADY EXISTED, SO THE FOUNDER'S 'i clicked 3rd view but would not open' HAS AN UNPROVEN ROOT CAUSE. Its commit message asserts 'This rail was a FIFTH route, and it went straight to ViewController — so it inherited neither the GIS exit nor the legacy-bar retirement.' But `git show 5d9cacf6^:apps/editor/src/ui/layout/NavigationAreaLayout.ts` line 126 already read `onViewSelect: (id) => gis.activateView(id as any)`, and `ViewsRailPanel` calls that prop on every activation. The rail was ALREADY landing on `activateView`, which already ran `if (_gisActive) toggleGIS(false)` and `retireLegacyViewBars`. The commit was 'Not browser-verified' by its own admission. This is [[same-rule-two-implementations]] and [[gate-blind-on-the-wrong-axis]] at once: a fix was shipped against a mechanism the code contradicts, so whatever the founder actually hit (a re-entry-guard early return at ViewsRailPanel.ts:766 is the first candidate — it returns BEFORE both dispatches whenever `alreadyAtView && alreadyAtMode`, and for a 3D view `targetMode==='3D'` so a second 3D ViewDefinition with `currentViewDefinitionId` already matching is skipped silently with only a console.log) is still live and will be re-reported. Verify the claimed mechanism from the log line the commit itself names as the discriminator before building anything else on top of it.",
    "ELEVATION POCHÉ IS DEAD BY THE SAME XZ BUG, AND IT IS THE HALF WITH A PASSING TEST — a strictly worse state than section. `_ELEVATION_SCOPE` is `poche: true, pocheRequiresExplicitIntent: true` (ViewScope.ts:135-146), so the moment a user declares a cut fill in the View Modifiers tab, `PlanViewCanvas.render` (:424-429) calls `_renderPocheFills` for the elevation and it returns zero polygons for the same reason a section does — `buildMeshPlaneIntersectionGeometry` runs for elevation too (`isSectionDepthView = section || elevation`, EdgeProjectorService.ts:2345). So §ELEVATION-POCHE-IS-INTENT-DECLARED (L-1601) is a shipped, documented, test-certified feature that has never painted a pixel. Finding 1 frames the defect as section-only and finding 2 treats the elevation test as a missed catch; the sharper statement is that the ELEVATION side is certified-green-and-dead, which is the [[fake-more-capable-than-real]] failure with a green tick attached, and it should be ranked with the section case rather than under it.",
    "EVEN AFTER THE BUILDER IS FIXED, THE CANVAS-SIDE REDUCTION IS STILL WRONG FOR LEFT/RIGHT SECTIONS — this is a SECOND defect, not a cleanup. `PlanViewCanvas._parsePochePoints` (:2718-2731) and its split-view twin `PlanViewFillRenderer._parsePochePoints` (:254-266) both do `return { h: x, v: sectionFlipV ? -z : z }`, taking the first component as the horizontal axis unconditionally. `PlanViewCanvas` itself owns `_hWorldAxis` / `_hWorldSign` and publishes them through `viewPlaneFrame()` (:328-336) precisely so consumers stop assuming plan, and the linework pass uses `_vertexToHV` (:337-342) which honours them. So a section whose horizontal axis is world z (an east/west-looking section) will place poché polygons on the wrong axis even once the builder carries three axes through. The auditor names this inside finding 1's fix text; it deserves its own row because a lane that only adds the `PochePlane` selector will ship a fix that still draws wrong on half the sections, and the passing arm it adds (a z-constant fixture) would not catch it.",
    "A SECTION PLACED ON A SHEET GETS NO DATUMS OR GRID EVEN ON THE PATH THAT DOES INJECT. `ViewController.requestBackgroundProjection` — the sheet-editor projection entry point, and the one the auditor recommends elsewhere as 'the better fit' — guards its injection with `if (viewDef.viewType === 'elevation')` at ViewController.ts:710, so sections are excluded from the ONLY non-legacy call site that injects at all. Combined with finding 4 (the live Canvas2D path injects nothing) and SectionViewService.ts:227-229 being the legacy SectionTool path, the honest statement is that there is NO reachable route on which a section acquires grid lines today. The auditor mentions widening this guard as a trailing sentence in finding 4's fix; it is a separate reachability hole on a separate surface (sheets, not the viewport) and will be missed if the lane only edits PlanViewManager."
  ]
}