{
  "dimension": "\"I CHANGE THE LINE TYPE ETC.. DOESN'T WORK\" — tracing a line-type / line-weight override from the UI control to the drawn pixel",
  "summary": "The render chain is NOT broken. I drove the real chain and it is green: `apps/editor/__tests__/viewIntentGovernsEveryCategory.test.ts` runs 23 tests through real OBC → real `PlanViewCanvas.render()` → a recording 2D context, and all 23 PASS today (measured 2026-09-08, `cd apps/editor && npx vitest run __tests__/viewIntentGovernsEveryCategory.test.ts` → \"Test Files 1 passed / Tests 23 passed\"). Dash reaches the pixel by a live, readable path: `line.style` → `LINE_STYLE_TO_DASH` (IntentRuleResolver.ts:143) → `PenStyle.dashPx` → `GraphicsRulesEngine.resolveStyle` (GraphicsRulesEngine.ts:193) → `ctx.setLineDash(...)` (PlanViewCanvas.ts:489).\n\nThe break is in the AUTHORING surfaces, and it is FOUR gates deep on one fact. The ONLY control in the product that writes a line type is the `Line style` select in `VisibilityIntentPanel` (line 347). It is inert whenever the governing intent is a SYSTEM intent — and all five shipped intents are `isSystem: true` (SystemIntents.ts:48), including \"Architectural Documentation (Auto)\", exactly what the founder's panel names. The select renders with the `disabled` attribute; `bindEvents()` returns at line 772 before wiring the handler; `VisibilityIntentStore.update()` returns `null` at line 72; and `UpdateVisibilityIntentCommand.validate()` refuses at line 48 with a reason the panel discards unread (line 1226-1228). He cannot have written a line type, which is also why his panel honestly reads NO OVERRIDES.\n\nCompounding it, the per-view \"Output\" tier is INVERTED: the two controls that exist in the UI (`visualStyle` — whose options literally include **\"Hidden Line\"** — and `displayModel`) are read by ZERO files in the draw path (grep count 0 across PlanViewCanvas, EdgeProjectorService, HiddenLineRemoval, GraphicsRulesEngine, ViewScope, VGSceneApplicator, IntentRuleResolver); while the two fields the drawing engine DOES read (`output.beyondLineStyle`, `output.occlusionDisposition`) have no UI writer anywhere. If he touched \"Visual Style → Hidden Line\", that is a control wired to nothing.\n\nOn (b): the three numbers are in three different units and cannot be subtracted — \"41 layers never styled\" is not established, and the real gap is a different set. On (d): \"0 demoted\" in a PLAN log is CORRECT, not a defect — plan's disposition is `remove` by design (ViewScope.ts:155).",
  "findings": [
    {
      "title": "THE BREAK — the only line-type control in the product refuses to write on the intent that governs his views (four gates, all on `isSystem`)",
      "mechanism": "`VisibilityIntentPanel` is the sole authoring surface for `line.style`. `renderAppearanceForm(appearance, intent.isSystem, ...)` (VisibilityIntentPanel.ts:264) passes `intent.isSystem` as the form's `disabled` flag, so the `Line style` select at line 347 renders with the HTML `disabled` attribute. `bindEvents()` then hits `if (intent.isSystem) return;` at line 772 and never reaches line 778 where the `[data-appearance]` change/blur handlers are attached. If both were bypassed, `VisibilityIntentStore.update()` returns `null` at line 72, and `UpdateVisibilityIntentCommand.validate()` returns `{ok:false, reason:'System VisibilityIntents are read-only.'}` at line 48 — a refusal that `updateIntent()` discards without reading (VisibilityIntentPanel.ts:1226-1228 calls `cm?.execute?.(...)` and never inspects the result). Every shipped intent is `isSystem: true` (SystemIntents.ts:48, inside `makeIntent`, applied to all five entries of `SYSTEM_VISIBILITY_INTENTS`), and the founder's panel names 'Architectural Documentation (Auto)' (SystemIntents.ts:276). Downstream of the store the chain is LIVE and proven, so nothing else needs fixing to make his gesture work.",
      "evidence": [
        "apps/editor/src/ui/VisibilityIntentPanel.ts:264 — `? this.renderAppearanceForm(appearance, intent.isSystem, batchActive, intent)`",
        "apps/editor/src/ui/VisibilityIntentPanel.ts:347 — `<select class=\"vi-select\" data-appearance=\"line.style\" ${disabled ? 'disabled' : ''}>`",
        "apps/editor/src/ui/VisibilityIntentPanel.ts:772 — `if (intent.isSystem) return;`",
        "apps/editor/src/ui/VisibilityIntentPanel.ts:778 — `this.panel.querySelectorAll<HTMLInputElement | HTMLSelectElement>('[data-appearance]').forEach(input => {` (never reached for a system intent)",
        "packages/core-app-model/src/presentation/VisibilityIntentStore.ts:72 — `if (this._systemIntents.has(intentId)) return null;`",
        "packages/command-registry/src/vg/UpdateVisibilityIntentCommand.ts:48 — `if (visibilityIntentStore.isSystem(this.intentId)) return { ok: false, reason: 'System VisibilityIntents are read-only.' };`",
        "packages/core-app-model/src/presentation/SystemIntents.ts:48 — `isSystem: true,` (in `makeIntent`, the sole constructor for all 5 SYSTEM_VISIBILITY_INTENTS)",
        "packages/core-app-model/src/presentation/SystemIntents.ts:276 — `name: 'Architectural Documentation (Auto)',`",
        "apps/editor/src/ui/VisibilityIntentPanel.ts:72 (header, already recorded by L-779) — \"all five SHIPPED intents being `isSystem` (and therefore hard read-only — `bind()` returns before wiring a single write handler)\"",
        "PROOF THE REST WORKS: packages/core-app-model/src/presentation/IntentRuleResolver.ts:143 — `dashPx: LINE_STYLE_TO_DASH[appearance.line.style],`; packages/core-app-model/src/drawing/GraphicsRulesEngine.ts:193 — `if (s.dashPx  !== undefined) resolved.dashPx  = s.dashPx;`; packages/core-app-model/src/views/PlanViewCanvas.ts:489 — `ctx.setLineDash(_pen.dashPx ? _pen.dashPx.map(v => v * dashPxScale) : []);`",
        "MEASURED 2026-09-08: `cd apps/editor && npx vitest run __tests__/viewIntentGovernsEveryCategory.test.ts` → `Test Files  1 passed (1) / Tests  23 passed (23)` — the real OBC→canvas chain honours the style tier for 15 plan + 6 elevation (category × layer) cases"
      ],
      "userVisibleSymptom": "The Line style / Line weight controls in the Visibility Intent editor are greyed out (or, if the user reaches them by another route, silently do nothing) for every view in a fresh project, because every view is bound to a system intent. Nothing on screen says why. The escape hatch — Duplicate the intent, which auto-binds the copy to the ONE view the editor was opened from (VisibilityIntentPanel.ts:945-955) — is unlabelled as the required first step, and would have to be repeated for each of his 13 views.",
      "fix": "Two-part, and the first part is the whole user-visible fix. (1) Copy-on-write at the write boundary: when `updateAppearance()` runs against a system intent, auto-duplicate it (reuse the existing `duplicateIntent()` body, which already emits `CreateVisibilityIntentCommand` + `AssignViewIntentCommand`), then apply the patch to the copy — one gesture, no dialog. That is the founder's `refusing-half-needs-its-escape-hatch` rule: a gate whose 'yes' branch is unreachable is a regression. (2) Until (1) lands, at minimum stop the silent refusal: render the disabled form with an inline banner naming the reason and offering a 'Make an editable copy for this view' button, and stop discarding the `cm.execute` result at VisibilityIntentPanel.ts:1226. Do NOT make system intents mutable — `SystemIntents.cloneSystemIntents()` byte-compares against the frozen seeds and four gates deliberately protect them.",
      "effort": "medium",
      "risk": "low",
      "confidence": "high",
      "alreadyExists": "`VisibilityIntentPanel.duplicateIntent()` (line 923-964) already does create + bind in one gesture and already carries `contextViewId` (L-779). `OverridePanel.promoteToIntent()` (apps/editor/src/ui/OverridePanel.ts:332-357) already implements clone-a-system-intent-and-rebind. The whole downstream chain is proven by `apps/editor/__tests__/viewIntentGovernsEveryCategory.test.ts` (23 passing).",
      "filesToChange": [
        "apps/editor/src/ui/VisibilityIntentPanel.ts"
      ]
    },
    {
      "title": "The per-view \"Visual Style\" control (its options include \"Hidden Line\") and \"Display Model\" are WRITE-ONLY — zero readers in the entire draw path",
      "mechanism": "`buildOutputSection` renders a `Visual Style` select with options wireframe / **hiddenLine** / shaded / shadedWithEdges / realistic (ViewPropertiesPanelBuilders.ts:322-343) and a `Display Model` select with normal / halftone / hidden (:356-372). Both write `ViewOutputSettings` through `_fireSetViewOutput`; `SetViewOutputCommand` validates them (:64-69); `DefaultViewsManager` seeds them; the ViewTemplate migration maps them. Nothing READS them. `grep -c \"visualStyle|displayModel\"` returns **0** for every file in the 2D draw path: PlanViewCanvas.ts, EdgeProjectorService.ts, HiddenLineRemoval.ts, GraphicsRulesEngine.ts, ViewScope.ts, VGSceneApplicator.ts, IntentRuleResolver.ts. A Revit user reaching for \"line type\" reaches for exactly this control first, and it is wired to persistence and to nothing else.",
      "evidence": [
        "apps/editor/src/ui/ViewPropertiesPanelBuilders.ts:326 — `{ value: 'hiddenLine',     label: 'Hidden Line' },`",
        "apps/editor/src/ui/ViewPropertiesPanelBuilders.ts:341 — `patch.visualStyle = val || undefined;`",
        "apps/editor/src/ui/ViewPropertiesPanelBuilders.ts:370 — `patch.displayModel = val || undefined;`",
        "MEASURED 2026-09-08 — `grep -c \"visualStyle\\|displayModel\"` → 0 for each of: packages/core-app-model/src/views/PlanViewCanvas.ts, apps/editor/src/engine/views/EdgeProjectorService.ts, packages/core-app-model/src/drawing/HiddenLineRemoval.ts, packages/core-app-model/src/drawing/GraphicsRulesEngine.ts, packages/core-app-model/src/views/ViewScope.ts, packages/core-app-model/src/presentation/VGSceneApplicator.ts, packages/core-app-model/src/presentation/IntentRuleResolver.ts",
        "packages/core-app-model/src/views/ViewDefinitionTypes.ts:537 — `displayModel?: 'normal' | 'halftone' | 'hidden';` (declared, never consumed)"
      ],
      "userVisibleSymptom": "Picking \"Hidden Line\" (or Wireframe, Shaded, Halftone, Hidden) from the view's Output section changes nothing on the drawing, forever, in every view type. The value persists and reloads, so it looks like it was accepted.",
      "fix": "Either wire or withdraw — do not leave a control that stores a value nothing can draw (the §ELEVATION-POCHE-NEEDS-A-VIEW-TYPE-ROW / L-3904 precedent already in this file's comments). Wiring is cheap for the two that map onto machinery that exists: `visualStyle: 'hiddenLine'` should resolve to `occlusionDisposition: 'demote'` (see the next finding), and `displayModel: 'halftone'` already has a resolved `style.halftone` concept in VGSceneApplicator.applyPhaseStatus. `wireframe`/`shaded`/`realistic` are 3D-viewport concepts with no 2D meaning — remove them from a documentation view's Output section rather than pretending.",
      "effort": "medium",
      "risk": "medium",
      "confidence": "high",
      "alreadyExists": "`resolveOcclusionDisposition(viewOutput, scope)` (ViewScope.ts:205-210) is already the ONE resolver for exactly the 'hidden line' semantic, and already reads a per-view override field. `VGSceneApplicator.applyPhaseStatus` (:575-585) already implements halftone.",
      "filesToChange": [
        "apps/editor/src/ui/ViewPropertiesPanelBuilders.ts",
        "packages/core-app-model/src/views/ViewScope.ts"
      ]
    },
    {
      "title": "The inverse defect: `output.beyondLineStyle` and `output.occlusionDisposition` are READ by the drawing engine and written by NO UI — the dashed-hidden-line answer to (d)",
      "mechanism": "`resolveBeyondLineStyle(viewDef.output, scope)` is called on the live paint path (PlanViewCanvas.ts:418) and its answer reaches `ctx.setLineDash` via `GraphicsRulesEngine._beyondDashRules` (:269-275). `resolveOcclusionDisposition(viewOutput, scope)` (ViewScope.ts:205-210) is what decides whether an occluded span is deleted or demoted to the dashed `:hidden` pen. Both fields are declared on `ViewOutputSettings` (ViewDefinitionTypes.ts:506, :517) with headers describing them as 'an explicit act of INTENT (P7)'. `grep -rn occlusionDisposition apps/editor/src/ui/` → **0 hits**; same for `beyondLineStyle`. So the only reachable values are the per-view-TYPE defaults hard-coded in ViewScope. Answering (d) directly: the founder's log line `disposition=remove, ... 0 sub-segment(s) demoted proj → HIDDEN (dashed)` is on a PLAN view, and `_PLAN_SCOPE.occlusionDisposition = 'remove'` (ViewScope.ts:155). **0 demoted is the CORRECT number for that log, not a defect.** Dashed hidden-line output IS reachable and is produced — `HiddenLineRemoval.ts:903-922` writes real geometry to a `:hidden` sibling layer, the pen table gives every category a dashed HIDDEN pen (PenWeightTable.ts:220-233), and `_ELEVATION_SCOPE.occlusionDisposition = 'demote'` (ViewScope.ts:143). But it is reachable ONLY in an elevation, and the user cannot turn it on for a plan because the field that would do so has no control.",
      "evidence": [
        "packages/core-app-model/src/views/ViewScope.ts:155 — `occlusionDisposition: 'remove' as const,` (inside `_PLAN_SCOPE`)",
        "packages/core-app-model/src/views/ViewScope.ts:143 — `occlusionDisposition: 'demote' as const,` (inside `_ELEVATION_SCOPE`)",
        "packages/core-app-model/src/drawing/HiddenLineRemoval.ts:882 — `if (isHidden && disposition === 'remove') { clipped += t1 - t0; continue; }` — the branch that makes `demoted` 0 in plan",
        "packages/core-app-model/src/views/ViewDefinitionTypes.ts:506 — `occlusionDisposition?: OcclusionDisposition;` with header \"The canonical use the founder asked for: a plan where you want to SEE the pipe behind the wall, dashed\"",
        "MEASURED 2026-09-08 — `grep -rn \"occlusionDisposition\\|beyondLineStyle\" --include=*.ts apps/editor/src/ui/` → no output",
        "packages/core-app-model/src/drawing/PenWeightTable.ts:220-233 — `HIDDEN: { wall: pen(0.09, '#6b7280', [...HIDDEN_DASH_PX], 0.55), ... }` — every category has a dashed hidden pen"
      ],
      "userVisibleSymptom": "There is no way to ask a plan for hidden-line (dashed) display of what is behind a wall, and no way to change how a section/elevation draws its `beyond` zone. The founder's log correctly reports 0 demoted and it reads like a failure.",
      "fix": "Add the two selects to `buildOutputSection` next to Scale/Detail Level — `Occluded lines: (inherit) / Remove / Show dashed` writing `output.occlusionDisposition`, and `Beyond lines: (inherit) / Solid / Dashed` writing `output.beyondLineStyle`. Both resolvers, both persisted fields and `SetViewOutputCommand` already exist; this is a UI-only change, ~40 lines, using the exact `patch.X = val || undefined` idiom the neighbouring controls already use (the §FIX-VIEW-OUTPUT-NO-BRIDGE note at :336-340 explains why it must be explicit `undefined`, not `delete`). Then map `visualStyle: 'hiddenLine'` onto `occlusionDisposition: 'demote'` so the existing control stops lying.",
      "effort": "small",
      "risk": "low",
      "confidence": "high",
      "alreadyExists": "`resolveOcclusionDisposition` + `resolveBeyondLineStyle` (ViewScope.ts:205-233) are the single resolvers and are already called from the paint path. `SetViewOutputCommand` already validates and persists `ViewOutputSettings`. `packages/core-app-model/src/drawing/BeyondDashByViewType.test.ts` already guards the dash semantics.",
      "filesToChange": [
        "apps/editor/src/ui/ViewPropertiesPanelBuilders.ts"
      ]
    },
    {
      "title": "The V/G (Visibility/Graphics) tier cannot express a line type at all, and has no human writer — only the AI has one",
      "mechanism": "`VGCategoryStyle` is a closed shape carrying `fillColor, edgeColor, lineWeight, transparency, visible, halftone` (+`roomColourMode`) — VGGovernanceStore.ts:33, :105, :130-149. There is NO dash / line-type / line-style field anywhere in it, and the three translators that convert a VG override into a `PenStyle` all set only `widthMm`, `color` and `opacity` — never `dashPx` (PlanViewVGApplicator.ts:52-90; the byte-identical duplicate at PlanViewCanvas.ts:2917-2963; and VGSceneApplicator.applyToProjectionLayers, which contains zero occurrences of `lineType|linePattern|dashSize|dashed|LineDashed|gapSize`). Separately, the commands that write this tier have no human call site: `SetVGViewCategoryStyleCommand` appears only in barrel exports and a comment; `SetVGCategoryStyleCommand` is called only from `VGIntentMapper` (the AI chat path). `VgCanvasStyleResolver` (the file that decides whether VG speaks at all) reports a contribution ONLY where `overriddenProps` records a deliberate write — so with no human writer, VG contributes `null` for colour and weight on every line in every view, by construction.",
      "evidence": [
        "packages/core-app-model/src/presentation/VGGovernanceStore.ts:33 — `lineWeight:   number;` (the whole `VGCategoryStyle` shape; no dash sibling anywhere in the file)",
        "apps/editor/src/engine/views/plan-canvas/PlanViewVGApplicator.ts:52-90 — builds `cutPen/projPen/beyondPen` from `lineWeight`, `edgeColor`, `transparency` only",
        "MEASURED 2026-09-08 — `grep -n \"lineType\\|linePattern\\|dashSize\\|dashed\\|LineDashed\\|gapSize\" packages/core-app-model/src/presentation/VGSceneApplicator.ts` → no output",
        "packages/command-registry/src/rooms/SetRoomColourModeCommand.ts:21-23 — \"`SetVGCategoryStyleCommand` and `SetVGViewCategoryStyleCommand` have zero non-test call sites (measured 2026-08-21)\" — RE-MEASURED 2026-09-08: `SetVGViewCategoryStyleCommand` → only `command-registry/src/index.ts:351`, `vg/index.ts:21`, its own definition and two comments; `SetVGCategoryStyleCommand` → adds only `ai-host/src/vg/VGIntentMapper.ts:34` and `command-registry/src/ai-vg/VGIntentMapper.ts:34`",
        "packages/core-app-model/src/presentation/VgCanvasStyleResolver.ts:113-122 — \"`overriddenProps` is VG's OWN record of 'a human decided this', maintained by `SetVGCategoryStyleCommand` / `SetVGViewCategoryStyleCommand`\"",
        "apps/editor/src/ui/OverridePanel.ts:279-330 — the per-view override panel LISTS and CLEARS graphic overrides; the only writes it binds are `view.setCategoryVisibility` and `vg.assignIntent`. No style editor."
      ],
      "userVisibleSymptom": "There is no Revit-style V/G dialog row for line pattern. The per-view override panel can only show and clear overrides it has no way to create, so it always reads empty — which is what the founder's \"NO OVERRIDES\" is truthfully reporting.",
      "fix": "Do NOT add a dash field to `VGCategoryStyle` — that would mint a second line-type authority alongside `line.style`, and the repo already carries three drifted copies of the VG→PenStyle translator. Instead make the intent tier the one authority (finding 1) and delete the ambiguity: either give `SetVGViewCategoryStyleCommand` a real UI for the four properties VG genuinely owns (colour / weight / transparency / visibility, per view), or retire the command. Also collapse the three copies of the VG→PenStyle translator (PlanViewVGApplicator.syncViewOverrides, PlanViewCanvas._syncVGViewOverrides, VGSceneApplicator.applyToProjectionLayers) onto one producer — they already disagree in scope (none of them emits a HIDDEN-zone pen).",
      "effort": "medium",
      "risk": "medium",
      "confidence": "high",
      "alreadyExists": "`SetVGViewCategoryStyleCommand`, `ApplyVGTemplateToViewCommand`, `vgGovernanceStore.setViewCategoryOverride/replaceViewCategoryOverrides` and the `overriddenProps` flag machinery are all built and tested — only the UI is missing. `VgCanvasStyleResolver` is the ONE sanctioned VG→canvas seam (L-776) and already handles the default-vs-override distinction correctly.",
      "filesToChange": [
        "apps/editor/src/ui/OverridePanel.ts",
        "packages/core-app-model/src/presentation/VGGovernanceStore.ts",
        "apps/editor/src/engine/views/plan-canvas/PlanViewVGApplicator.ts",
        "packages/core-app-model/src/views/PlanViewCanvas.ts"
      ]
    },
    {
      "title": "Two layer classifiers disagree on the same line: six ISO layer families get VG COLOUR governance but fall to the `__default__` (wall) pen for DASH and WIDTH",
      "mechanism": "`PlanViewCanvas.render()` asks TWO different questions of the same layer tag. For colour/visibility it calls `this._vgCategoryForLayer(layerTag)` → `vgCategoryForLayer` (DrawingLayerIdentity.ts:190), whose map has 18 rows including `A-PLMB`, `S-COLS`, `A-GRID`, `A-LEVL`, `A-CONS`, `A-AREA`. For the PEN (widthMm + dashPx) it calls `penCategoryForLayerTag(layerTag)` (PlanViewCanvas.ts:471 → PenWeightTable.ts:439), whose ten regexes have NO arm for `A-PLMB`, `S-COLS`, `A-GRID`, `A-LEVL`, `A-AREA` or `A-LGHT`, so all six return the `'projection'` fallback (PenWeightTable.ts:407). That string is then passed as the ELEMENT TYPE into the intent chain (`GraphicsRulesEngine._intentRules` line ~397: `resolveIntentPenStyle(virtualInstance, intent, category, zone, ...)` with `elementType: category`), and `rulesFor()` (IntentRuleResolver.ts:90-105) finds no `elementRules['projection']` and falls through to `__default__` — which `VisibilityIntentDefaults.defaultStateAppearance` seeds from the WALL pen (`const category = elementType === '__default__' ? 'wall' : elementType`, :100). Net: the panel's `plumbing`, `column` (plan symbol), `grid`, `level` and `spaceEnvelope` rows can change those lines' COLOUR but never their line type or weight. `A-LGHT` is worse — it is absent from BOTH maps, so lighting linework is ungoverned on every axis. Two further categories the panel offers (`curtain-wall`, `curtain-panel`) appear in no 2D classifier at all, and `handrail` is already PINNED as unreachable by an existing test.",
      "evidence": [
        "packages/core-app-model/src/drawing/PenWeightTable.ts:439-459 — `penCategoryForLayerTag` regex set: `A-WALL|A-DOOR|A-FLOR|A-COLS|A-STRS|A-ROOF|A-CEIL|A-FURN|A-HRAL|A-GLAZ|A-CONS` — no `A-PLMB`, no `S-COLS`, no `A-GRID`, no `A-LEVL`, no `A-AREA`, no `A-LGHT`",
        "packages/core-app-model/src/drawing/PenWeightTable.ts:407 — `return 'projection'; // safe fallback — generic projected geometry`",
        "packages/core-app-model/src/drawing/DrawingLayerIdentity.ts:101-127 — `ISO_LAYER_TO_VG_CATEGORY` DOES carry `'A-PLMB': 'plumbing'`, `'S-COLS': 'column'`, `'A-GRID': 'grid'`, `'A-LEVL': 'level'`, `'A-AREA': 'spaceEnvelope'` — and its own comment at :122 admits \"⚠ `penCategoryForLayerTag` has NO arm for this tag yet, so the strokes take the generic fallback pen\"",
        "packages/core-app-model/src/presentation/VisibilityIntentDefaults.ts:100 — `const category = elementType === '__default__' ? 'wall' : elementType;` — `__default__` IS the wall pen",
        "packages/geometry-column/src/ColumnPlanSymbolBuilder.ts:48 — `const COLUMN_LAYER = 'S-COLS';`",
        "packages/geometry-lighting/src/LightingFragmentBuilder.ts:731 — `group.userData.layerName  = 'A-LGHT';` (the only producer; `A-LGHT` appears in `DrawingPipelineWorker.ts:129` — a FOURTH, unreachable ISO→category map — and nowhere the live canvas reads)",
        "apps/editor/__tests__/viewIntentGovernsEveryCategory.test.ts:306-330 — the existing PINNED gap for `handrail`: \"Setting the panel's `handrail` row changes nothing on screen\""
      ],
      "userVisibleSymptom": "Even after the user obtains an editable intent, the Line style / Line weight rows for plumbing, columns-drawn-as-plan-symbols, grids, levels, space envelopes and all lighting change nothing — while the Line colour row for the same categories DOES work. A per-category control that honours one of its three properties is worse than one that honours none, because the user concludes the feature is random.",
      "fix": "Make `penCategoryForLayerTag` and `vgCategoryForLayer` ONE derivation instead of two, as `penCategoryForLayerTag`'s own header (L-3902) already argues for its two callers. Concretely: give `penCategoryForLayerTag` a `plumbing` / `grid` / `level` / `spaceEnvelope` / `lighting` arm keyed off the SAME `ISO_LAYER_TO_VG_CATEGORY` table (with the longest-prefix-wins rule that table already implements, so `S-COLS` and `A-STRS-HRAL` resolve correctly), add matching rows to `SYSTEM_PEN_TABLE` (it already has a `lighting` row at PenWeightTable.ts:232 — unreachable today) and to `VisibilityIntentDefaults.ELEMENT_TYPES`. Verify AT THE OUTCOME, through `graphicsRulesEngine.resolveStyle`, never through the table.",
      "effort": "medium",
      "risk": "medium",
      "confidence": "high",
      "alreadyExists": "`ISO_LAYER_TO_VG_CATEGORY` + `tagIsInFamily` + longest-prefix-wins (DrawingLayerIdentity.ts:101-186) is the correct derivation and already handles the colon, hyphen and multi-token tag forms. `SYSTEM_PEN_TABLE` already has a `lighting: pen(...)` row in all four zones. `apps/editor/__tests__/viewIntentGovernsEveryCategory.test.ts` already enumerates (category × real producer layer) and drives the real chain — the table just needs the missing rows and a dash assertion.",
      "filesToChange": [
        "packages/core-app-model/src/drawing/PenWeightTable.ts",
        "packages/core-app-model/src/presentation/VisibilityIntentDefaults.ts",
        "packages/core-app-model/src/drawing/DrawingLayerIdentity.ts"
      ]
    },
    {
      "title": "(b) answered — \"22 styled vs 63 ISO vs 14 categories\" are three DIFFERENT UNITS; \"41 layers never styled\" is not established, and the real unstyled set is a different one",
      "mechanism": "**14** = `Object.keys(CATEGORY_TO_DXF_LAYER).length` (VGSceneApplicator.ts:716) — 14 VG-category ENTRIES, but only **11 DISTINCT layer names**, because `curtain-wall`→`A-WALL` (duplicating `wall`), `curtain-panel`→`A-GLAZ` (duplicating `window`) and `handrail`→`A-STRS` (duplicating `stair`). **22** = `appliedCount`, incremented once per DrawingLayer OBJECT that exists and was mutated, over base + `:cut` + `:proj` + `:beyond` — so the three shared names are visited and counted TWICE each, last-write-wins on the material. Because object-key order puts `curtain-wall` after `wall`, `curtain-panel` after `window` and `handrail` after `stair`, the WALL category's resolved colour/weight is overwritten on `A-WALL` by curtain-wall's on every pass. **63** = `totalLayerCount` in EdgeProjectorService, incremented at line 3417 once per **(mesh group × layerName)** iteration inside the group loop — across 69 groups. It is a count of group-layer PAIRS, not of distinct layers. You cannot subtract these. What IS true and checkable: `applyToProjectionLayers` styles ONLY the 11 base names and their `:cut`/`:proj`/`:beyond` siblings — it has no `:hidden` arm at all (zero occurrences of `':hidden'` in the file), and no arm for `projection-visible`, `projection-hidden`, `S-COLS`, `A-CONS`, `A-AREA`, `A-LGHT`, `A-GRID` or `A-LEVL`. This matters only for EXPORT, because the on-screen path re-resolves per line and ignores the material — but export DOES read it.",
      "evidence": [
        "packages/core-app-model/src/presentation/VGSceneApplicator.ts:87-101 — `CATEGORY_TO_DXF_LAYER`: `wall:'A-WALL' … 'curtain-wall':'A-WALL' … 'curtain-panel':'A-GLAZ' … handrail:'A-STRS'` (14 keys, 11 distinct values)",
        "packages/core-app-model/src/presentation/VGSceneApplicator.ts:716-721 — `const categoryCount = Object.keys(CATEGORY_TO_DXF_LAYER).length; … styledLayers=${appliedCount} … across ${categoryCount} VG categories (max ${categoryCount * 4} layers)`",
        "apps/editor/src/engine/views/EdgeProjectorService.ts:3417 — `totalLayerCount++;` inside `for (const layerName of layerNames)`, itself inside the per-group loop",
        "MEASURED 2026-09-08 — `grep -n \"hidden\" packages/core-app-model/src/presentation/VGSceneApplicator.ts` → 5 hits, ALL comments or unrelated (`:607, :610, :763, :783, :784`); no `${layerName}:hidden` arm exists",
        "packages/file-format/src/export/sheets/SVGCompositeRenderer.ts:246-250 — `// linewidth on LineBasicMaterial is the PRYZM 1-6 scale stored by VGSceneApplicator` / `const rawWeight = (mat as any)?.linewidth;` — export IS a real consumer of what applyToProjectionLayers writes",
        "packages/core-app-model/src/views/PlanViewCanvas.ts:506-512 — the on-screen path deliberately DELETED its read of `material.linewidth` (\"the drawing's weight authority is the pen table + the VG override, never a THREE material default\")"
      ],
      "userVisibleSymptom": "On DXF/SVG/PDF export only: hiding or re-weighting the `wall` VG category has no effect on `A-WALL` (curtain-wall's default overwrites it in the same pass), same for `window`→`A-GLAZ` and `stair`→`A-STRS`; and the dashed hidden-line layers export at whatever material OBC gave them. On screen these are invisible because the canvas re-resolves every line.",
      "fix": "Three small, independent changes. (1) Invert the loop: iterate DISTINCT layer names and resolve the ONE category that `vgCategoryForLayer(layerName)` reports for that name, instead of iterating categories and letting duplicates race — this removes last-write-wins by construction. (2) Add the `:hidden` sibling to the sub-layer block at VGSceneApplicator.ts:663-703 so the demoted dashed layer is styled like the other three. (3) Report both counts in their own units — the log already learned this lesson once (§DIAG-VG-APPLIED-UNITS / L-706, comment at :705-715); apply the same to EdgeProjectorService's `63 ISO layer(s)`, which is a group-layer-pair count masquerading as a layer count.",
      "effort": "small",
      "risk": "low",
      "confidence": "high",
      "alreadyExists": "`vgCategoryForLayer` (DrawingLayerIdentity.ts:190) already answers layer-name→category, which is the direction the inverted loop needs. `siblingZoneLayer(layerName, 'hidden')` already exists (used at HiddenLineRemoval.ts:909) and is the canonical `:hidden` name producer. The §DIAG-VG-APPLIED-UNITS comment block is the template for the log fix.",
      "filesToChange": [
        "packages/core-app-model/src/presentation/VGSceneApplicator.ts",
        "apps/editor/src/engine/views/EdgeProjectorService.ts"
      ]
    },
    {
      "title": "The element-tier pen override (priority 10000, the strongest tier in the cascade) has zero callers — the whole per-element styling tier is dead",
      "mechanism": "`GraphicsRulesEngine.addElementOverride(elementId, zone, category, style)` (GraphicsRulesEngine.ts:122-142) accepts a full `Partial<PenStyle>` including `dashPx`, registers at `RULE_PRIORITY_ELEMENT` and is honoured by `resolveStyle`'s sort. Nothing calls it. The nominal per-element route is `SetGraphicOverrideCommand` (bus `view.setGraphicOverride`), which IS reachable from three UI sites — but all three hard-code the identical ghost patch `{ visible: true, line: { opacity: 0.35 }, fill: { opacity: 0.15 }, ghostStyle: 'fade', ghostOpacity: 0.25 }`. No UI anywhere passes a `line.style` or `line.weight` in a graphic-override patch.",
      "evidence": [
        "MEASURED 2026-09-08 — `grep -rn \"addElementOverride\" --include=*.ts packages/ apps/ plugins/ src/` (excluding dist/tests) → 1 hit: the declaration at packages/core-app-model/src/drawing/GraphicsRulesEngine.ts:122",
        "apps/editor/src/engine/views/PlanViewInteraction.ts:1498-1504 — `patch: { visible: true, line: { opacity: 0.35 }, fill: { opacity: 0.15 }, ghostStyle: 'fade', ghostOpacity: 0.25 }`",
        "apps/editor/src/ui/RadialMenu.ts:270-273 — the identical patch",
        "apps/editor/src/engine/initUI.ts:3393-3397 — the identical patch via `element.setGraphicOverride`",
        "apps/editor/src/ui/OverridePanel.ts:279-286 — `graphicRow()` renders only a label and a Clear button; there is no editor that could create one"
      ],
      "userVisibleSymptom": "There is no way to restyle a single wall / door / window differently from its category anywhere in the product — a baseline expectation for anyone coming from Revit, and the only escape hatch a user has when a category-wide change is too coarse.",
      "fix": "Give `OverridePanel`'s graphic-override row an inline editor (line style / weight / colour) that dispatches `view.setGraphicOverride` with a real `AppearancePatch` — the command, its undo, its sync disposition and its resolver tier all already exist and are honoured. Do this AFTER finding 1, so the tier below it is authorable first; a per-element override on top of an unauthorable category is a worse UX than none.",
      "effort": "medium",
      "risk": "low",
      "confidence": "high",
      "alreadyExists": "`SetGraphicOverrideCommand` (packages/command-registry/src/vg/SetGraphicOverrideCommand.ts), its bus registration (apps/editor/src/engine/initBusHandlers.ts:2462-2465), its sync disposition (packages/sync-client/src/syncDisposition.ts:748), the `graphicOverrides` resolver tier (IntentRuleResolver.ts:220-224) and `OverridePanel.applyOverridesToRules` (:358-388, promotes overrides into an intent) are all built and wired. Only the editing control is missing.",
      "filesToChange": [
        "apps/editor/src/ui/OverridePanel.ts"
      ]
    },
    {
      "title": "The ViewTemplate → StrokeStyle.dash chain — a complete, schema-validated line-type system — is orphaned from the editor",
      "mechanism": "`packages/schemas/src/view/view-template.ts:49` defines `dash: z.enum(['solid','dashed','dotted','centerline','phantom'])` inside `StrokeStyleSchema`, consumed by `CategoryVGSchema` with separate `projection` and `cut` strokes per category — i.e. exactly the Revit object-styles model. Its only resolver is `packages/geometry-kernel/src/view-resolution/resolver.ts`, whose only importer is `plugins/plan-view/src/view-template-bridge.ts`. `plugins/plan-view` is not depended on by any package.json in the repo and is not registered by `apps/editor`, so `PlanViewRenderer` and `style-resolver.ts` (158 lines, which do honour `dash`) never execute. Naming it so the next lane does not rebuild it: a second, richer line-type schema already exists and is dead.",
      "evidence": [
        "packages/schemas/src/view/view-template.ts:49 — `dash: z.enum(['solid', 'dashed', 'dotted', 'centerline', 'phantom']).default(STROKE_STYLE_DEFAULT.dash),`",
        "MEASURED 2026-09-08 — `grep -rln '\"@pryzm/plan-view\"' --include=package.json apps/ packages/ plugins/` → no output; `grep -rn '@pryzm/plan-view' --include=*.ts apps/ packages/ plugins/ src/` → 2 hits, both an OTel tracer NAME string in packages/core-app-model/src/views/otel.ts:4,25",
        "MEASURED 2026-09-08 — the only importer of `geometry-kernel/src/view-resolution/resolver` outside its own package is `plugins/plan-view/src/view-template-bridge.ts:1`",
        "packages/core-app-model/src/views/PlanViewCanvas.ts:1971-1975 — the sibling worker paint path records the same shape: \"`renderFromPipelineResult` and `scheduleWorkerRender` have NO CALLERS\" — RE-MEASURED 2026-09-08, still true (6 grep hits, all declarations and comments in that one file)"
      ],
      "userVisibleSymptom": "None directly — but it is a live trap: a lane asked to \"add line types\" will find this schema, conclude the feature is half-built, and extend the dead half. It also means `DrawingPipelineWorker.ts:129`'s `'A-LGHT': 'lighting'` row (a FOURTH ISO→category map) looks like lighting is classified when nothing reachable reads it.",
      "fix": "Do not build on it. Either delete `plugins/plan-view` + `geometry-kernel/src/view-resolution` + the ViewTemplate stroke half, or add a one-line ORPHANED banner at the top of each naming `line.style` on VisibilityIntent as the live authority. The repo's own `authored-but-unwired-is-the-bottleneck` rule applies: audit reachability, not existence.",
      "effort": "small",
      "risk": "low",
      "confidence": "high",
      "alreadyExists": "n/a — this finding IS the 'already exists, do not rebuild' warning.",
      "filesToChange": [
        "plugins/plan-view/src/style-resolver.ts",
        "packages/geometry-kernel/src/view-resolution/resolver.ts",
        "packages/schemas/src/view/view-template.ts",
        "packages/core-app-model/src/drawing/DrawingPipelineWorker.ts"
      ]
    }
  ],
  "whatIcouldNotEstablish": "1. WHICH control the founder actually touched. Three are plausible and I cannot distinguish them from the evidence given: (a) the Visibility Intent editor's `Line style` select, which he would have found greyed out; (b) the view Output section's `Visual Style → Hidden Line`, which accepts the value and is read by nothing; (c) `Display Model → Hidden/Halftone`, same. His phrase \"I CHANGE THE LINE TYPE\" implies the change was ACCEPTED, which favours (b)/(c) over (a) — a disabled select cannot be changed at all. All three are broken, by different mechanisms, and I have named all three. A single screenshot of the panel he used would settle it.\n\n2. I did NOT prove end-to-end that a NON-system intent's `line.style` reaches `ctx.setLineDash` by executing it. I read every link (IntentRuleResolver.ts:132-143 → GraphicsRulesEngine.ts:193 → PlanViewCanvas.ts:489) and the sibling COLOUR/WIDTH path through the identical resolver is proven green by 23 passing tests, but the existing harness stubs `ctx.setLineDash` as a no-op and drives the VG tier, not the intent tier. The cheap decisive experiment: extend `recordingCanvas()` in `apps/editor/__tests__/viewIntentGovernsEveryCategory.test.ts` to record `setLineDash` alongside `strokeStyle`/`lineWidth`, and add a case that writes `line.style: 'dashed'` onto a user intent bound to the view. That single change would turn my \"high confidence, read-only\" into \"measured\" and would become the standing gate for this whole dimension. I did not make it because this lane is audit-only.\n\n3. RIVAL I RULED OUT — \"the style cache is stale, so edits do not repaint.\" `GraphicsRulesEngine`'s cache key (GraphicsRulesEngine.ts:161-169) does NOT include intent content, which is exactly the shape of that bug. But the constructor subscribes `this._cache.clear()` to `vi:intent-updated` / `vi:intent-store-loaded` / `vi:instance-updated` and four siblings (:87-96), `VisibilityIntentStore.update()` dispatches `vi:intent-updated` (:87), and `PlanViewManager` (:190) plus `ViewTechnicalDrawingCache` (:907) both listen and repaint. Ruled out.\n\n4. RIVAL I RULED OUT — \"the styling never reaches the canvas because only 22 of 63 layers are styled.\" False premise: the on-screen path does not read the styled layer materials at all. `PlanViewCanvas.render()` deliberately deleted its `material.linewidth` read (:506-512, L-241 P5) and re-resolves every line from `graphicsRulesEngine.resolveStyle` + `resolveVgCanvasStyle`. The 22/63/14 mismatch is real but its blast radius is the DXF/SVG export path (SVGCompositeRenderer.ts:247), not the screen.\n\n5. RIVAL I COULD NOT FULLY RULE OUT — whether his 13 views are bound individually or inherit. `resolveBoundIntentWithInheritance` walks `parentViewId`, and `intentUsageCount` counts bound instances. If the 13 are bound individually, even the copy-on-write fix (finding 1) only repairs the ONE view he edits from; he would need a \"rebind all views on this intent\" action. That is a UX question I could not settle from code and should be asked before implementing.\n\n6. NOT INVESTIGATED (other lanes' dimensions, flagged only where they touched mine): why the lines are hard to read (pen widths are 0.09-0.50 mm ≈ 0.34-1.89 CSS px and do NOT scale with the view's 1:100 output scale — `SCREEN_PX_PER_MM` is a fixed 3.779, DrawingConstants.ts:73); why furniture still renders when excluded; and the absent LightingPlanSymbolBuilder — though I did establish that `A-LGHT` is stamped by `LightingFragmentBuilder.ts:731` and is absent from both live classifiers, so lighting is ungoverned on every axis, not just symbols."
}