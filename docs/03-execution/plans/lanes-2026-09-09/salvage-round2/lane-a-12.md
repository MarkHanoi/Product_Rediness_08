{
  "survivingTitles": [
    "The Project Browser's furniture exclusion writes NOTHING when the active view is 3D — `_activeViewId()` reads a store method that does not exist, then a field ViewController nulls in 3D",
    "Symbol-injected furniture linework carries NO element identity in `userData`, so a per-element hide can never match it — the render path and the hit-test path resolve identity two different ways in the same class",
    "`NativeElementMeshExporter.exportForView()` filters by level and Y-range ONLY — no category, no visibility, no intent, and zero reads of `Object3D.visible` in the whole file",
    "`VGSceneApplicator.applyToProjectionLayers()` can hide, but its hide is INERT on the plan canvas — it sets `Object3D.visible`, which PlanViewCanvas never reads",
    "ROOT ARCHITECTURAL FINDING — two hiding mechanisms with no shared authority: 3D honours `Object3D.visible` + the VG store and IGNORES the intent's `visible`; 2D honours the intent + the VG store and IGNORES `Object3D.visible`",
    "The regression suite that certifies \"the browser filter reaches projected views\" stubs a store method that does not exist in production"
  ],
  "refutedTitles": [
    "Lighting fixtures are STRUCTURALLY ungovernable in plan — `Lighting` is absent from the layer map, so the family key is null and the intent veto's own guard skips them; and nothing marks them `skipInPlan`, so the 672-face fixture mesh edge-dumps as the black concentric bursts in his screenshot"
  ],
  "refutations": [
    "Finding 6 — REFUTED AT HEAD, the audit is stale by three hours. Commit 584aaab2 ('fix(docs/§LIGHTING-IS-A-CLASSIFIED-LAYER L-13267)', 2026-09-09 07:14, five files, +213) landed AFTER the audit was written and closes precisely the four rows the finding says are missing. Measured at HEAD 9babe6f5: (1) EdgeProjectorService.ts:260 — `Lighting: 'A-LGHT', LightingFixture: 'A-LGHT', light: 'A-LGHT'` IS in ELEMENT_TYPE_TO_PROJECTION_LAYER, so the finding's 'no Lighting / lighting key anywhere in the map' is false and `resolveProjectionLayer` no longer returns FALLBACK_NATIVE_LAYER; (2) DrawingLayerIdentity.ts:110 and DrawingPipelineWorker.ts:129 — `'A-LGHT': 'lighting'`, so `vgCategoryForLayer` is no longer null and V/G can hide it; (3) PenWeightTable.ts:461 — `isLighting: /A-LGHT|lighting/i.test(layerTag)`, so `penCategoryForLayerTag` returns 'lighting' and reaches the `lighting: pen(0.13,'#303030')` row rather than the 0.25 mm wall default; (4) VisibilityIntentDefaults.ts:29 — `'lighting'` IS now in ELEMENT_TYPES, so the panel has the toggle the finding says does not exist. Because the family key is now non-null, the `if (_groupFamily && !_symbolGate(_groupFamily))` guard at EdgeProjectorService.ts:2725 no longer skips lighting — the specific bypass the finding names is gone, and lightingIsAClassifiedLayer.test.ts (163 lines) now pins all four. The finding's own commit-message twin confirms it diagnosed the same mechanism; it is right about the past and wrong about the tree. TWO NARROWER CLAIMS SURVIVE and are recorded under missed[], not here: `grep -rn skipInPlan packages/geometry-lighting/` still returns 0 (so the 672-face mesh is still edge-projected and still duplicates the authored cyan symbol — the founder's black bursts are lighter now, not gone), and the `_groupFamily &&` unclassified-family bypass remains a general hazard for any type still absent from the map."
  ],
  "missed": [
    "THE BIGGEST MISS — the category checkbox writes the ONE targetKind that symbol linework can never match, and the command that writes the right one is already built and already wired elsewhere. `applyCategoryVisibility` (ProjectVisibilitySection.ts:288-294) loops `applyElementVisibility` per element, which calls `writeElementVisibilityIntent` → `view.hideElement` with `targetKind: 'element'`. I verified at GraphicsRulesEngine.ts:400-404 that the pen's intent target is `{ elementId: ctx.elementId, elementType: category, category }` — so a CATEGORY or ELEMENTTYPE override matches a symbol line (the category is derived from the layerTag, which IS stamped), while an ELEMENT override needs `ctx.elementId`, which finding 2 proves is permanently undefined on symbol lines. The consequence the audit never states: even after finding 1 is fixed and a plan view is active, unticking Furniture in the Project Browser STILL leaves every sofa, bed, wardrobe, chair, kitchen, tree and door-swing symbol drawn at full weight — because the browser writes the only kind that cannot match. `SetCategoryVisibilityInViewCommand` (packages/command-registry/src/vg/SetCategoryVisibilityInViewCommand.ts:54, exported at index.ts:344) is dispatched as `view.setCategoryVisibility` by OverridePanel.ts, works end-to-end in 2D, and is one command with one undo entry. Under the already-solved lens this reorders the whole audit: the fix for the founder's literal report is to make the category checkbox dispatch the existing category command, not to add userData stamps to eighteen builders. Findings 1 and 2 are graded as independent; they are the two halves of one defect.",
    "ROOMS HAVE NO VISIBILITY-INTENT ROW — the founder's third explicit request, verified, and the audit demotes it to a parenthesis inside finding 6's evidence. `VisibilityIntentPanel.ts:240` builds its element-type list as `Object.keys(intent.elementRules)`, and `elementRules` is seeded by `cloneDefaultElementGraphicsRules()` from `VisibilityIntentDefaults.ELEMENT_TYPES` (lines 15-46), which contains no `'room'`. That is literally 'THAT I DONT SEE'. The machinery already exists on every other axis: `'room'` is already a declared VG category (VGSceneApplicator.ts:65, and the alias row at :143 maps `room`/`Room`/`RoomVolume` → `'room'`), `resolveRoomColourIntent` (RoomColourIntent.ts:127) already resolves room colour PER VIEW and is already called by PlanViewCanvas.ts:2505, and the pastel wash in his screenshot is that resolver running. So this is the identical one-row shape as the `'lighting'` row that shipped at 07:14 today — add `'room'` to ELEMENT_TYPES and the panel grows the category he asked for. Deserved its own finding.",
    "THE AUDIT IS STALE AGAINST HEAD AND SAYS SO NOWHERE — a §VERIFICATION-ARTIFACT-CAN-PREDATE-SUBJECT hazard. Two commits landed this morning after it was written: 584aaab2 07:14 (lighting classification, refuting finding 6) and 9babe6f5 07:22 (line-type UI). Related: the founder made FOUR complaints and the audit engages three. 'ALSO I CHANGE THE LINE TYPE ETC.. DOESNT WORK' gets no finding at all, yet 9babe6f5 claims to close it by giving `output.occlusionDisposition` and `output.beyondLineStyle` a UI in ViewPropertiesPanelBuilders.ts (+88) — two behaviours PlanViewCanvas already reads at :418/:1944/:2047 and that no UI could write. Whether that commit actually closes it is unverified by me and unverified by the audit; either way an audit that answers three of four named complaints should say which one it dropped.",
    "THE DEAD `getActiveId?.()` ARM IS AT FOUR SITES, NOT TWO. Finding 1's fix names ProjectVisibilitySection.ts:48 and WallFragmentBuilder.ts:714. The same call against the same non-existent method also appears at apps/editor/src/ui/RadialMenu.ts:236 and apps/editor/src/engine/initUI.ts:3381, both with the identical `?? viewController?.currentViewDefinitionId` fallback — so both degrade the same way in 3D. Fixing two of four leaves the copy-paste alive and the next hand-copy inevitable; this is one dead read that wants one helper, not four edits.",
    "FINDING 3's 'already exists' NOTE IS WRONG ON CALL ORDERING, which makes its fix more expensive than priced. It asserts `makeSymbolInjectionGate` 'is already constructed in EdgeProjectorService BEFORE the exporter's output is consumed — pass it, do not rebuild it.' Measured: `exportForView` is never called from EdgeProjectorService (its only mention there is a comment at :2683). The real call sites are PlanViewManager.ts:621, :845, :998, :1103 and SectionViewService.ts:154; the gate is constructed at EdgeProjectorService.ts:2585, i.e. INSIDE the projector, strictly after the exporter has already run. There is no live gate to pass at the exporter's call site — the factory has to be invoked at each PlanViewManager/SectionViewService site (cheap, `makeSymbolInjectionGate(viewDef.id, viewDef.viewType)`, and it memoises), but that is five new call sites plus an NME LRU cache key that must now include the gate's identity or it will serve a filtered export to an unfiltered view. The finding's mechanism is fully confirmed (908 lines, `grep -c '\\.visible'` → 0, level+Y filter only); only its cost estimate and its reuse note are wrong.",
    "FINDING 4's FIX HAS A CONFIRMED SAFETY MARGIN THE AUDIT LEFT OPEN, AND A HAZARD IT UNDERSTATES. Open half, now closed: I measured `grep -c visible` → 0 in BOTH packages/file-format/src/export/sheets/SVGCompositeRenderer.ts and DxfExportService.ts, so neither exporter reads layer visibility and the 'delete the setVisibility calls' option regresses nothing — the caveat the finding says to check first is resolved. Understated half: OBC's `DrawingLayers.assign` (index.mjs:24897-24906) also sets `object.visible = false` at INSERTION time for any hidden layer, not just on `setVisibility`. So the other option — adding `if (!child.visible) return;` to the render traverse — switches on a second, VG-store-driven hide that duplicates the canvas's own `_styleResolver` hide from the SAME store, and would also retroactively suppress anything ever assigned to a layer that was hidden at insertion. The finding's 'do NOT do both halves independently' warning is correct; the asymmetry is that deleting is now provably safe and honouring is not."
  ]
}