{
  "refuted": false,
  "reason": "EVIDENCE LENS: NOT REFUTED. I checked all 20 file:line citations against the source. Every one is real, accurate to the line, quoted verbatim, and in context. No fabricated quote, no off-by-one, no line drift, no misrepresented context.\n\nVERIFIED EXACTLY AS QUOTED:\n- globeGroundAnchor.ts:213-215 — the three-line discriminator, verbatim. `slope` IS the fall-through arm (plateau → roof-gap → else slope), so the claim that it is reached \"by elimination, not positively established\" is a correct reading of the control flow.\n- globeGroundAnchor.ts:167 `SLOPE_RAMP_MIN_SPAN_M = 6`, :172 `ROOF_GAP_M = 4` — both exact.\n- globeGroundAnchor.ts:279 `if (cls.arm === 'slope' && cls.medianM !== null) return cls.medianM - seatEpsilonM;` — exact. midIdx = `Math.min(Math.floor(0.5 * n), n - 1)`, so floor(0.5*58)=29 is right.\n- CesiumViewport.ts:619 `const GLOBE_GROUND_SEAT_EPSILON_M = 0.3;` — exact.\n- CesiumViewport.ts:9014 `safeSampleTileHeights` returning `Promise<number[]>` — exact, and the body confirms the stronger half of the claim: `out.push(h)` fires only for finite h, so index correspondence to `samplePts` is genuinely destroyed, not merely the lat/lon.\n- CesiumViewport.ts:9203 `const parcelVoidCut = this.photorealTilesActive;` and the skipped in-parcel picks — exact.\n- CesiumViewport.ts:13037, :13120-13122, :309, :14814, :9611 block (prints `anchor↔LTP` and `⚠ DIVERGED — defect (ii)`), :17457 — all exact.\n- boundaryProjection.ts:142, pointInPolygon.ts:123 (the ulp/on-boundary comment), GlobeGroundAnchor.test.ts:473 — all exact. SiteBoundaryDrawTool.ts:283 is exact; the file lives at apps/editor/src/ui/geospatial/, but the claim gave only a bare filename so this is not a path error.\n- Void-cap constants: PHOTOREAL_VOID_CAP_DEPTH_M = 60 at :653, PHOTOREAL_VOID_CAP_SEAT_EPSILON_M = 0.05 at :660 — both land on the exact lines cited.\n- commit c36b75bc is real, dated 2026-09-05, and is the Sète §GLOBE-SLOPE-SEAT / L-12919 fix.\n\nI INDEPENDENTLY CLOSED THE CAUSAL CHAIN they asserted. The epsilon really does flow unmodified: :619 → :9397 `seatEpsilonM: GLOBE_GROUND_SEAT_EPSILON_M` → resolveGlobeGroundAnchor :338-342 → reduceTileGroundHeight → :9668 `this.formaTerrainBaseHeight = sampledHeight`. Nothing between the classifier and the base height touches the number, so \"no other contributor to the vertical error\" holds.\n\nFOUR TELLS THAT THEY READ RATHER THAN PATTERN-MATCHED:\n1. At :9219 a stale comment says \"plus 8 compass points ~30 m beyond the footprint\". The actual code at :9241-9246 does two 16-point rings at ext+25 and ext+55. The claim reports the CODE and ignores the stale comment.\n2. `at(0.5)` in the §GLOBE-PICK-SPREAD-DIAG log uses `Math.floor(f * sorted.length)` — the same index formula as midIdx — so the median the log prints really is the one the slope arm seats on. The claim depends on this and it holds.\n3. maxGap is computed only over [lowIdx+1, midIdx], matching the claim's \"in the low half\".\n4. GlobeGroundAnchor.test.ts:473 is genuinely the ONLY test asserting `arm === 'roof-gap'` (I enumerated all 50+ `it(` blocks). Its fixture is 20 values at 50.0–50.6 then 28 at 65.0–74.0 — a clean ~14.4 m void, exactly as characterised.\n\nSTRONGEST CORROBORATION, which the claim under-sells: the probe at :13134-13146 hardcodes an unconditional warning string — \"⚠ anchor-in-void=n means the design stands under UN-CLIPPED tiles — buried\" — emitted whenever inVoid is 'n'. Since the anchor is by construction the parcel's FIRST VERTEX (parcelFrameOrigin :142 ← SiteBoundaryDrawTool :283), it lies exactly ON the ring being tested, which pointInPolygon.ts:123 documents as the ulp-indeterminate case. So the probe prints a confident \"buried\" verdict for a correctly placed model. The second anomaly is well-founded on code alone.\n\nWHAT I COULD NOT VERIFY (and the author should not be credited for it): the founder's console values — 58 picks, span 12.59, largest step 2.00, median 78.94, min 64.89 — exist nowhere in the repo. They are internally consistent (78.94 − 0.30 = 78.64; p10 = 66.35 gives the stated 66.05 old seat and the 12.59 span; 78.64 − 64.89 = 13.75, matching the reported 10–14 m) and the code emits exactly those fields, but the arm attribution rests entirely on numbers I cannot check.\n\nONE COSMETIC OVERSTATEMENT: \"byte-for-byte the console's `-> seat 78.64`\" is loose. No log emits that string. The code prints `seat=78.64 m ellipsoidal` at :13141 and `height=78.64 m ... → seat-and-reveal` at :9424. The number is printed; the formatting quoted is a paraphrase. Transcription looseness, not a substantive error — and notably :13141 prints `anchor-in-void`, the 28.6 m separation, and `seat=` in ONE line, which is consistent with all three founder fragments coming from the same console block.\n\nThe proposed fix's data-availability claims also check out: contextBuildings.ts:89-95 documents `coordinates` as `[[[lon,lat],…]]`, `contextBuildingPlacements` exists at :2054 carrying `.feature`, `pointInRingEvenOdd` is imported at :309 and used exactly that way at :14814, loadContextRoads is at :13718, maybeAttachTerrainProvider at :5250/:10994, and :12551 confirms contextGroundCache holds sampleTerrainMostDetailed heights.",
  "evidenceChecked": [
    "apps/editor/src/ui/geospatial/globeGroundAnchor.ts:150-300 — read the full classifier + reduceTileGroundHeight; :213-215 discriminator verbatim, :167 SLOPE_RAMP_MIN_SPAN_M=6, :172 ROOF_GAP_M=4, :279 slope→median all exact",
    "apps/editor/src/ui/geospatial/globeGroundAnchor.ts:300-400 — resolveGlobeGroundAnchor passes input.seatEpsilonM into reduceTileGroundHeight (:338-342); action union includes 'seat-and-reveal' (:357, :375)",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:9014-9036 — safeSampleTileHeights signature returns Promise<number[]>; body pushes only finite h, destroying index correspondence to samplePts",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:615-625 — GLOBE_GROUND_SEAT_EPSILON_M = 0.3 at exactly :619",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:648-665 — PHOTOREAL_VOID_CAP_DEPTH_M=60 at :653, PHOTOREAL_VOID_CAP_SEAT_EPSILON_M=0.05 at :660 (both exact lines as cited)",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:9195-9250 — parcelVoidCut at :9203 verbatim; in-parcel picks skipped when photoreal active; ring = vertices×1.7 + two 16-point rings at ext+25/ext+55 (:9241-9246). Note stale comment at :9219 says '8 compass points ~30 m' — the claim correctly reports the code, not the comment",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:9320-9400 — tileHeights sourced from safeSampleTileHeights; §GLOBE-PICK-SPREAD-DIAG and §GLOBE-SLOPE-SEAT logs; at(0.5) uses the same floor(f*n) index as midIdx",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:9397 — the ONLY consumer of GLOBE_GROUND_SEAT_EPSILON_M (grep confirmed 2 hits total: definition + this)",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:9418-9424 — ground anchor console line printing height/picks/→action",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:9655-9672 — formaTerrainBaseHeight = sampledHeight, unmodified; grep of all 6 assignment sites confirms no other modifier on this path",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:9605-9625 — ANCHOR EVIDENCE block printing 'anchor↔LTP' and '⚠ DIVERGED — defect (ii)' exactly as the falsifier describes",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:13030-13045 — void cap top = formaTerrainBaseHeight − PHOTOREAL_VOID_CAP_SEAT_EPSILON_M, verbatim",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:13110-13146 — logPhotorealVoidVsBuilding; pointInRingEvenOdd on the single anchor point at :13120-13122; hardcoded unconditional '⚠ anchor-in-void=n … buried' warning; prints seat= in the same line",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:309, :14814 — pointInRingEvenOdd import and existing same-shape usage (fix feasibility)",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:17457 — realModelOnGlobeOrigin = { lat: input.originLat, lon: input.originLon }",
    "apps/editor/src/ui/site/boundaryProjection.ts:138-148 — parcelFrameOrigin returns ring[0], verbatim",
    "apps/editor/src/ui/geospatial/SiteBoundaryDrawTool.ts:278-290 — origin = parcelFrameOrigin(this.vertices) at :283 verbatim (file is under ui/geospatial/, not ui/site/)",
    "packages/geometry-kernel/src/pure/pointInPolygon.ts:118-128 — the ulp / 'point lying exactly on the ray' comment at :123, verbatim",
    "apps/editor/__tests__/GlobeGroundAnchor.test.ts:473-480 — fixture verbatim; enumerated all it() blocks and confirmed this is the ONLY roof-gap arm test",
    "git log c36b75bc — real commit, 2026-09-05, '§GLOBE-SLOPE-SEAT … Sète … (L-12919)'",
    "apps/editor/src/ui/geospatial/contextBuildings.ts:85-98 — coordinates documented as [[[lon,lat],…]]; CesiumViewport.ts:2054 contextBuildingPlacements carries .feature",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:5250, :10994, :12551, :13718 — maybeAttachTerrainProvider, contextGroundCache = sampleTerrainMostDetailed, loadContextRoads (fix-side data availability)"
  ],
  "betterHypothesis": "No better hypothesis on the evidence lens — the code reading is sound and I found no misquote. Two refinements the author should accept, and one gap that is the real remaining risk.\n\nREFINEMENT 1 (correct the record): drop \"byte-for-byte\". The code emits `seat=78.64 m ellipsoidal` (:13141) and `height=78.64 m … → seat-and-reveal` (:9424); it never emits `-> seat 78.64`. The arithmetic reproduction is right, the quotation formatting is not, and in this repo a confidently-quoted string that does not exist is exactly the §CONFIDENT-REGISTER-ROWS shape.\n\nREFINEMENT 2 (strengthen the second anomaly): the claim under-sells it. The probe does not merely produce a misleading verdict — :13143-13145 hardcodes the sentence \"⚠ anchor-in-void=n means the design stands under UN-CLIPPED tiles — buried … that is a DIFFERENT defect\" and prints it unconditionally whenever inVoid is 'n'. Combined with the anchor being a ring vertex by construction, this probe emits a false \"different defect\" claim on essentially every correctly-placed model, which actively misdirects debugging away from the seat bug. That raises fix item (6) from cleanup to a real priority.\n\nTHE REMAINING RISK, which is NOT an evidence defect but bounds the confidence: the entire arm attribution rests on six console numbers (58 / 12.59 / 2.00 / 78.94 / 64.89 / 66.35) that exist nowhere in the repo. The code is proven to compute seat = median − 0.30 when the slope arm is taken; it is NOT proven from the repo that this run took the slope arm. Before implementing the multi-part fix, run the author's own cheapest check first — the §GLOBE-SLOPE-SEAT line at :9382 prints `arm=` explicitly, so one line of the founder's console settles whether the slope arm fired at all. If it printed `arm=plateau` or `arm=roof-gap`, the seat came from the low quantile and this entire diagnosis is aimed at the wrong branch.\n\nAlso worth pricing honestly: fix item (4), the relative ceiling, is the only part that needs no signature change to safeSampleTileHeights and would reject 78.64 on the stated data. Items (1)-(3) all depend on prerequisite (0), which changes a return type consumed on the globe seat path. Ship (4) behind the arm= confirmation, then decide."
}