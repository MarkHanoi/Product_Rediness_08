{
  "refuted": false,
  "reason": "CONFIRMED from source and reproduced numerically. Every structural link holds: (1) `SpaceEnvelopeMeshBuilder._faceGeometry` (apps/editor/src/engine/SpaceEnvelopeMeshBuilder.ts:443-488) emits side faces as explicit per-edge quads (correct for ANY ring) and the top/bottom caps as a vertex-0 fan at :477-482, with the convex-only precondition DECLARED in its own comment at :467-474; (2) there is exactly ONE `attachSpaceEnvelopeRender(` call (apps/editor/src/engine/initTools.ts:2147) onto `world.scene.three`, and `packages/core-app-model/src/BimWorld.ts:167-172` pins that root to identity — the theta rival is dead, and a theta error would rigidly rotate a prism, not fold it; (3) `CesiumViewport.renderSpaceEnvelopes` reads the SAME store records with the SAME ring order and hands them to `Cesium.PolygonHierarchy` + `height`/`extrudedHeight` (earcut), so the two views differ in exactly the cap triangulation; (4) `envelopeAuthoringPlan.ts:628-629` copies one ring into every storey with `role:'level'`, and the Cesium log format `${role}@${height}m` is literally the founder's `[level@3.0m x6]`; (5) the ISSUE-LOG citation is VERBATIM correct at line 614 (L-428: \"A buildable-envelope inset is an irregular/concave polygon (founder's case: 12-vertex)\"), and L-521b at line 702 adds a SECOND independent in-repo measurement — \"an irregular/concave hand-drawn boundary made the vertex average drift OUTSIDE the polygon\"; (6) no test anywhere covers cap triangulation.\n\nMEASURED (node + the repo's own three@0.183.2, 12-vertex concave L ring, 544 m2): the fan is WRONG at 12 of 12 start rotations; summed cap triangle area 724 m2 against a 544 m2 footprint, up to 266 m2 of triangles outside the ring. The proposed ShapeGeometry route measures 0/12 bad, exact area, exact y-plane, and every output vertex coincides with a ring vertex (no mirror). So the fix DOES fix the stated defect.\n\nWOULD IT BREAK SOMETHING — checked, and mostly no. The drag surface resolves a face from `userData['spaceEnvelopeFace']` + `hit.point` only (spaceEnvelopeDragSurfaceThree.ts:126-135); `spaceEnvelopeFaceAxis` returns +/-Y for caps by construction (SpaceEnvelopeFaceDrag.ts:74-75); the gizmo mints its own cones from pure ring arithmetic and holds ONE target at a time; `SpaceEnvelopePlanSymbolBuilder` draws line segments + hatch, not a fan. Critically, `consolidateSiblingMeshes` — the one thing that would choke on an INDEXED cap sitting beside non-indexed sides in a group where all n+2 meshes share one material, one renderOrder and one parent — is called ONLY by `packages/geometry-door/src/DoorBuilder.ts:647`. No test asserts cap vertex counts. So \"only the triangle list inside the cap changes\" holds.\n\nBUT THE FIX AS PROPOSED HAS THREE DEFECTS, one of them the exact hazard this file forbids:\n\nF1 — IT IMPORTS THE §PARCEL-SHADE-NOT-MIRRORED (L-10740) HAZARD, AND A STRICTLY BETTER FIX EXISTS. The builder's header states: \"NO `-z` MIRROR HERE, and its ABSENCE is the thing to check ... These vertices are already world-space, so negating z would mirror the envelope about the X axis — a perfectly valid prism of exactly the right size, in the wrong place, on which every seam-level assertion passes.\" The proposed fix puts `-p.z` + `rotateX(-pi/2)` into the CAP arm while the SIDE arm keeps raw world coordinates — two coordinate conventions inside one function. ParcelBoundarySceneRenderer.ts:829-831 says of itself: \"the sign disagreement between two such builders WAS the §PARCEL-SHADE-NOT-MIRRORED defect (L-10740).\" The sign the fix names is correct today; the hazard is that the next editor now has to know which arm is which. Measured alternative with no mirror at all: `THREE.ShapeUtils.triangulateShape(ring.map(p => new THREE.Vector2(p.x, p.z)), [])` returns index triples over the RAW (x,z) ring — 0/12 bad, exact 544 m2, no shape plane, no rotation, no translate, world-space invariant intact. `THREE.ShapeUtils` is reachable (`packages/renderer-three/src/three-re-export.ts` is `export * from 'three'`; verified present). It also keeps the cap NON-INDEXED, position-only + `computeVertexNormals()` — structurally identical to the side faces — whereas `ShapeGeometry` yields an INDEXED geometry carrying a `uv` attribute, making the 2 caps unlike the n sides in one group.\n\nF2 — THE try/catch GUARDS THE WRONG FAILURE. Measured: earcut does not throw on degenerate input. A collinear 3-point ring gives `ShapeGeometry` with `position.count === 3` and ZERO triangles (empty index) — a non-empty geometry drawing nothing, a case today's `if (tris.length === 0) return null` DOES catch. A self-intersecting bow-tie gives 1 triangle where the fan gives 2, so on a bow-tie the fix REDUCES coverage. The guard must be a triangle-count check, not only a try/catch. Worse, \"fall back to the existing fan (never to nothing)\" re-introduces the wrong cap under a `drew:'volume'` outcome — the exact \"failure and emptiness are the same value\" defect this builder's discriminated outcome type exists to prevent. If the fan stays as a fallback it must be recorded (outcome reason or group.userData), never silent.\n\nF3 — THE TWO TEST ASSERTIONS ARE NOT EQUALLY STRONG, AND THE STATED REASON FOR ROTATING IS HALF RIGHT. Measured at start-vertex 0 on my L-ring: EVERY fan triangle centroid is inside the ring (outside-area 0.00) while the summed area is 724 m2 against 544 m2 — the error there is pure OVERLAP, not spill. A centroid-only test is a false green at that rotation. The AREA-equality assertion is the load-bearing one; write both, and rotate because of overlap, not only because of the kernel case.\n\nF4 (not a break, but note it) — the fan's cap normals follow the ring winding (measured normal.y = -1 for a CCW-in-(x,z) ring); both earcut routes normalise regardless of input winding (ShapeGeometry+rotate gives +1, raw ShapeUtils gives -1). With `DoubleSide` + MeshStandardMaterial (three flips the normal for back faces), castShadow false and no outline pass, lighting is unaffected. Top and bottom still share one normal direction before and after.\n\nF5 — NECESSARY, POSSIBLY NOT SUFFICIENT. `baseOffset: level.elevation` with `height: heightM` (3.0 m assumed) makes storeys contiguous, so storey i's TOP cap and storey i+1's BOTTOM cap are COINCIDENT and COPLANAR — 5 duplicated cap pairs in a 6-storey stack, every one `transparent` + `depthWrite:false` + `DoubleSide`, so alpha doubles at each floor plane. Correct caps do not remove that banding. If the view still reads wrong after the patch, that is the next thing to look at — not evidence the caps were innocent.\n\nF6 (bonus the fix buys silently) — today the three.js raycast can hit a fan triangle lying OUTSIDE the footprint, so `pickFace` on a cap disagrees with the pure `pickSpaceEnvelopeFace` the other surfaces use. Post-fix they agree.\n\nRESIDUAL RISK, and it is the author's own: nobody has seen the founder's actual ring. The repo-side falsifier is cheaper than the console dump — the diagnosis holds only if the ring came from `insetPolygon`, the user-study `footprintPolygon`, or the `linear`/`ortho`/`curved` draw modes. `rectangular`, `circular` and `elliptical` (the other three of the six `DRAWN_MODE_NOUN` keys at parcelLawEnvelopeAuthoring.ts:523-530) all produce CONVEX rings on which the fan is exact, and the diagnosis would then be wrong.",
  "betterHypothesis": "The root cause stands; the FIX should change. Replace the vertex-0 fan with `THREE.ShapeUtils.triangulateShape(ring.map(p => new THREE.Vector2(p.x, p.z)), [])` applied to the RAW ring, emitting `tris.push(ring[a].x, y, ring[a].z, ...)` from the returned index triples — same earcut Cesium uses, measured 0/12 bad and exact area on the concave fixture, but with NO `-p.z` mirror, NO `rotateX`, NO `translate`, and a NON-INDEXED position-only geometry identical in shape to the side faces. This preserves the two invariants the current code is actually protecting — the header's \"these vertices are already world-space, NO -z MIRROR HERE\" (violating it is L-10740) and one geometry convention per function — which the `THREE.Shape` + `ShapeGeometry` + `rotateX(-pi/2)` route breaks by putting a second convention in the cap arm of the same function. Guard on TRIANGLE COUNT, not only try/catch (earcut returns 0 triangles for a collinear ring and 1 for a bow-tie without throwing), and if the fan is kept as a fallback, name it in the outcome instead of falling back silently. In the test, the summed-area assertion is the load-bearing one — a centroid-only check passes at start-vertex 0 on a fixture where the fan overstates the cap by 180 m2.",
  "evidenceChecked": [
    "apps/editor/src/engine/SpaceEnvelopeMeshBuilder.ts:1-489 (full read) — fan at :477-482 verbatim as quoted; convex-only precondition declared at :467-474; header at :425-441 explicitly forbids a -z mirror ('These vertices are already world-space, so negating z would mirror the envelope about the X axis'); side faces are explicit per-edge quads at :453-465, correct for any ring; shared material + renderOrder 1 + DoubleSide + depthWrite:false at :318-345; per-face userData at :222-236",
    "apps/editor/src/engine/initTools.ts:2147-2149 — the single attachSpaceEnvelopeRender call onto world.scene.three; grep confirms it is the only call site outside the definition and two wire specs",
    "packages/core-app-model/src/BimWorld.ts:167-172 — scene root pinned to identity position/rotation/scale, killing the theta rival",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:8850-8925 — same store records, same ring order, PolygonHierarchy + height/extrudedHeight, closeTop/closeBottom true; log format `${role}@${height}m` is exactly the founder's [level@3.0m x6]",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:8700-8701 — the '20-corner buildable footprint' line interpolates inset.length; verified present",
    "apps/editor/src/ui/site/envelopeAuthoringPlan.ts:600-670 — footprintFor() copies one ring per storey, role:'level', baseOffset:level.elevation, height:heightM (3.0 assumed) — contiguous storeys mean coincident coplanar caps at every floor plane",
    "apps/editor/src/ui/analysis/parcelLawEnvelopeAuthoring.ts:420-460 — three ring sources: env.insetPolygon, study.footprintPolygon, getDrawnEnvelopeFootprint; :523-530 DRAWN_MODE_NOUN enumerates six modes (linear/ortho/curved are concave-capable; rectangular/circular/elliptical are convex)",
    "docs/04-reference/ISSUE-LOG.md:614 (L-428) — quote is VERBATIM correct: 'A buildable-envelope inset is an irregular/concave polygon (founder's case: 12-vertex), whose AABB is strictly larger'",
    "docs/04-reference/ISSUE-LOG.md:702 (L-521b) — second independent in-repo measurement: 'an irregular/concave hand-drawn boundary made the vertex average drift OUTSIDE the polygon'",
    "apps/editor/src/ui/site/ParcelBoundarySceneRenderer.ts:390, 498-520, 818-845, 906-935 — the (x,-z) + rotateX(-PI/2) convention and the §PARCEL-SHADE-NOT-MIRRORED (L-10740) sign landmine, stated as 'the sign disagreement between two such builders WAS the defect'",
    "packages/renderer-three/src/three-re-export.ts — `export * from 'three'`, so THREE.ShapeUtils and THREE.ShapeGeometry are both reachable",
    "packages/renderer-three/src/consolidateSiblingMeshes.ts:1-80 + grep of all callers — called ONLY by packages/geometry-door/src/DoorBuilder.ts:647, so an indexed cap beside non-indexed sides cannot reach mergeGeometries",
    "apps/editor/src/engine/spaceEnvelopeDragSurfaceThree.ts:100-136 — pickFace resolves from userData['spaceEnvelopeFace'] + hit.point only; geometry-agnostic",
    "packages/geometry-space-envelope/src/SpaceEnvelopeFaceDrag.ts:74-105 — cap face axis is +/-Y by construction, cap centre is ring centroid; independent of triangulation",
    "packages/geometry-space-envelope/src/SpaceEnvelopeGeometry.ts:145-151 — spaceEnvelopeFaces = n sides + top + bottom; :175-190 ringSelfIntersects exists",
    "packages/geometry-space-envelope/src/index.ts — pointInRing and footprintAreaM2 are both exported, so the proposed test's imports resolve",
    "apps/editor/src/engine/SpaceEnvelopeFaceGizmoBuilder.ts:98-235 — ONE target at a time, so gizmo cones cannot be 'spikes' on all six volumes simultaneously",
    "apps/editor/src/engine/SpaceEnvelopePlanSymbolBuilder.ts:140-195 — plan leg uses line segments + hatch, not a fan; no second occurrence of the defect",
    "apps/editor/src/engine/attachSpaceEnvelopeRender.ts:130-160 — the draw path is outcome-driven and geometry-agnostic",
    "grep 'for (let i = 1; i < n - 1' across apps/packages/plugins/src — SpaceEnvelopeMeshBuilder.ts:477 is the only envelope-family vertex-0 fan",
    "MEASURED with node + node_modules/three@0.183.2 on a 12-vertex concave L ring (544 m2): FAN wrong at 12/12 start rotations, summed area 724 m2, worst outside-area 266 m2; SHAPEGEOMETRY (x,-z)+rotateX(-PI/2) 0/12 bad, exact area, exact y-plane, 0 vertices failing the mirror check; DIRECT ShapeUtils.triangulateShape on raw (x,z) 0/12 bad, exact area",
    "MEASURED degenerate inputs (no exception thrown in any case): collinear ring -> ShapeGeometry position.count 3 with ZERO triangles; bow-tie -> 1 triangle where the fan emits 2; closed loop (first==last) -> handled correctly, 2 triangles",
    "MEASURED winding: fan normal.y follows the ring winding (-1 for the CCW-in-(x,z) fixture); ShapeGeometry+rotate normalises to +1 and raw ShapeUtils to -1, both independent of input winding",
    "grep for position.count / .faces assertions in apps/editor/src/engine/__tests__/spaceEnvelopeFaceGizmo.spec.ts and apps/editor/src/ui/site/__tests__/authoredEnvelopeVisibleByDefault.spec.ts — no test pins cap geometry, confirming the fix is test-safe and confirming why the convex-only fan shipped and stayed green"
  ]
}