{
  "survivingTitles": [
    "The heavy-scene backend swap fires MID-HYDRATION, so every GPU resource is minted on WebGPU and then re-minted on a classic WebGL renderer — this is the sole source of the 'device lost: destroyed' pair",
    "Two full-project serializes on every open — the `resetDirtyAfterLoad()` hash baseline is discarded by the auto-save that follows",
    "captureThumbnail runs twice per open from two uncoordinated schedulers, and each run forces a synchronous full-scene render plus a GPU readback",
    "Two socket.io clients connect and join the same project room — two call sites, two websockets, two server-side access checks",
    "NOT A DEFECT — the duplicated 'WebGPU device lost' log line is two handlers on ONE device, and both correctly refuse to recover"
  ],
  "refutedTitles": [],
  "refutations": [
    "#1 — CONFIRMED, and the auditor's own falsification test now RESOLVES IN ITS FAVOUR. They wrote: 'grep his full console for §AUTO-WEBGL-HEAVY and §RENDERER-LIVE-SWAP live swap complete — their absence would falsify this.' It is already proven present. `logSceneCensusOnce()` has exactly ONE production call site in the repo — apps/editor/src/engine/initScene.ts:5182 — inside a `setTimeout(…, 1000)` on the SUCCESS path of the live swap, after the `§RENDERER-LIVE-SWAP live swap complete` log (initScene.ts:5151) and after `retireRenderer(oldRenderer)` (5126). It is the only emitter of `§NAV-MESH-PER-ELEMENT-CENSUS`, `§NAV-BACKEND-SWAP-HEADROOM` and the `(unattributed)` line (pryzmPerfConsole.ts:590-665). The founder's console contains BOTH of those lines, so a live backend swap completed during that open. The '162 elements / 3762 meshes' reading is a POST-swap measurement taken one second after retirement. No grep of the rest of his console is needed.",
    "#1 — PARTIAL REFUTATION of the timing clause, and it matters for the fix. The title says the swap fires MID-HYDRATION 'from the per-add tier pass'. During a project load the per-add tier pass is SUPPRESSED: `§FIX-LOAD-TRAVERSE-BATCH` (initScene.ts:2943, 3036-3038, 3751-3768) skips the per-add handler's two full-scene traverses for the whole load and runs `runTierPbrPass('post-load')` EXACTLY ONCE from a `pryzm-project-loaded` listener. That is why ISSUE-LOG L-1483's founder capture reads `reason=tier:post-load` and not `tier:add:bim-wall-added`. So the swap fires once, at the END of hydration — the entire 3762-mesh scene is minted on WebGPU and then thrown away, which is WORSE than the mid-hydration story, and it lands exactly when the user first tries to interact. The fix premise ('the loader knows elementCount before a mesh is built') is unaffected.",
    "#1 — the fix would help but is smaller than 1500 ms and has a narrow wiring window the auditor did not name. `proactivelySwitchToWebGLForBuildingGeneration()` still performs a FULL live swap (dispose RPM, mint canvas #2, `createRenderer`, rebind, retire) — it only makes that swap cheap by running it against an empty scene. The genuinely free version is to resolve the backend BEFORE the first renderer exists (rendererPrewarm.ts:79 → `createRenderer(canvas)` is fired from main.ts Phase B, long before any project id is known), so a pre-emptive swap is a second-best. Worse, the guard at autoWebGLHeavyScene.ts:194 (`backendBenefitsFromClassicSwap()`) returns false when `globalThis.pryzmRendererBackend` is still `undefined`, and `fireSwapToWebGL` returns WITHOUT setting `_autoSwapDone` when `pryzmSwapRendererBackend` is unregistered (registered only at initScene.ts:5541). On a deep-link / cold boot — the case that hurts most — a call placed 'in the loader' would silently no-op and the reactive post-load swap would fire anyway, unchanged. The call has to sit after the backend resolves and before hydration, which is a narrow seam.",
    "#1 — one break the auditor missed: deriving the decision from the SNAPSHOT's element count (503) uses a different denominator from the guard's own arm. `countElements()` (autoWebGLHeavyScene.ts:152-162) counts only top-level `scene.children` carrying `userData.id`/`levelId` — the census read 162 for this project where the snapshot says 503. Feeding 503 into a 400-element arm systematically OVER-triggers relative to today's behaviour, pinning projects to classic WebGL that the live gate would never have swapped. Their 'would break' names only the opposite direction (under-estimation). Gate on a stored MESH estimate, or on `elementCount` with a threshold calibrated against the guard's own denominator.",
    "#2 — CONFIRMED, and STRONGER than claimed: the baseline serialize is not 'usually' wasted, it is UNCONDITIONALLY incapable of doing its job. `getHash()` → `ProjectSerializer.serialize()` embeds `timestamp: Date.now()` in the snapshot (ProjectSerializer.ts:1786) and `stringify()` is a bare `JSON.stringify(snapshot)` (ProjectSerializer.ts:2102) with no volatile-field stripping. Two `getHash()` calls therefore differ unless they land in the same millisecond, which a 503-element / 2366-mutation walk (29 `getAll()`, 22 `deepStrip`, 11 `structuredClone`) never does. `executeSave()`'s `currentHash === this.lastHash` short-circuit (SaveOrchestrator.ts:383-388, '[SaveOrchestrator] Content hash unchanged — save skipped') is dead in practice.",
    "#2 — the stated 'would break' is FALSE and should not gate the fix. The auditor wrote: 'If the baseline is dropped entirely, the very first mutation after an open always writes a version even if the user reverted it.' That is TODAY's behaviour — the timestamp in the hash guarantees the comparison misses, so the baseline can never suppress a save. Deleting the `resetDirtyAfterLoad()` call costs nothing that currently works. Their fix A (feed the stored `version.snapshot` json in as the baseline) is also inert for the same reason and additionally cannot match: the stored snapshot carries a `versionLabel` while `getHash()` deliberately serializes without one. The correct order of work is: strip the volatile fields from the hash FIRST (hash a label-free, timestamp-free projection), and only then decide whether the baseline is worth one serialize — at which point it actually starts suppressing the no-edit autosave, which is the bigger prize.",
    "#3 — CONFIRMED (two call sites, one implementation, one log line per call), but the 'would break' is FACTUALLY WRONG in the direction that made the fix look risky. `VersionRecord` (PlatformShellTypes.ts:46-80) has NO thumbnail field, and `saveVersionInternal` never puts one there: `capturedThumb` goes into `ProjectMeta.thumbnail` (PlatformSaveController.ts:387) and into `_uploadThumbnailToServer` (:396). The post-load path writes the SAME two destinations — `projectRepository.saveProject({...existing, thumbnail})` and `_uploadThumbnailToServer` (:566-570) — roughly one second LATER, so in the normal case the post-load capture OVERWRITES the auto-save capture's product. The auto-save capture is not merely duplicated, its output is discarded. Suppressing it is safer than the auditor argued; the only residual is a project that has never had any thumbnail AND whose post-load capture also fails, which falls back to `existingMeta?.thumbnail` (a placeholder).",
    "#3 — the proposed flag has a lifecycle bug as specified. 'Track that with a single flag set by `schedulePostLoadThumbnailCapture` and cleared in `runCapture`' leaks: `schedulePostLoadThumbnailCapture` is called at `setProjectContext` for EVERY open, but the handler returns early on `detail.projectId !== projectId` and on `detail.empty` (PlatformSaveController.ts:534-540) and `runCapture` returns early on `activeProjectId !== projectId` and on a null capture (:546-556) — none of which would clear a flag set at schedule time, so auto-save thumbnails would stay suppressed for the rest of the session. Set the flag inside the handler AFTER the id/empty checks, and clear it on every `runCapture` exit including the early returns.",
    "#3 — the 'sits on the open critical path' framing is overstated. Neither capture blocks the open: the auto-save one runs inside `executeSave()`, which is behind a 2500 ms debounce re-armed by `handleLoadSuppressEnd()` (SaveOrchestrator.ts:539-543) plus the `_settleUntil` deferral (:531-541); the post-load one is `setTimeout(3500)` + `requestIdleCallback`. Both are main-thread jank in the first seconds of interaction, not latency to first paint. Keep the finding, retitle the cost as post-open jank so the next lane doesn't chase it as open latency.",
    "#4 — CONFIRMED verbatim at every cited line (PlatformCollabPill.ts:167-215 builds `ctx.socket` and emits `join-project`; initCollaboration.ts:840-585 builds a closure-local `socket` and emits it again; the two script ids `plat-socketio-client` / `pryzm-collab-socketio-client` really do make initCollaboration's 'Script tag already injected (e.g. by PlatformShell)' branch at :858 unreachable). But the 40 ms is not open latency for the user: the client never awaits the join, and `canUserAccessProject`'s Supabase select (server/projectAccess.js:118-126) is server-side work off the client's timeline. The real cost is one extra websocket per user held for the whole session — a 1,000-user concurrency cost, not an open-time cost. Rank it accordingly.",
    "#4 — one break the auditor missed, and it is the reason the pill exists where it does. The pill connects at `setProjectContext` (PlatformShell.ts:250); `initCollaboration` connects only from `onProjectLoaded` (initCollaboration.ts:1140 → `loadAndConnect`), which fires on `pryzm-project-loaded`. `PlatformVersionController.loadVersion()` emits that event ONLY after the load resolves and only if the stale-closure guard passes (PlatformVersionController.ts:400-418) — so on a stalled or discarded load the pill is the ONLY client in the room. Making initCollaboration the sole owner silently drops collaboration for every failed/slow open, not just for the narrow `version-saved`-in-the-gap window they named.",
    "#5 — CONFIRMED as written; no correction. Both handlers attach to the same `backend.device` (WebGPURendererAdapter.ts:220-241 and createRenderer.ts:517-537), both log verbatim, both defer to `isDeliberateDeviceDestroy(info)`. Worth adding for the next reader: because a `GPUDevice.lost` promise resolves once, the recovery site's own `retireRenderer(threeRenderer)` (createRenderer.ts:626) can never produce a SECOND `reason=\"destroyed\"` pair — which is the code-level reason #1's 'sole source' claim holds."
  ],
  "missed": [
    "THE OPEN WRITES BACK THE PROJECT IT JUST READ. With zero user edits, opening this project performs a full version save: the founder's own log shows `[VersionRepository] reason=save-version — 6 version(s) persisted to IndexedDB (~1.4 MB compressed)` followed by `[PlatformSaveController] Version saved: \"Auto-save\" (503 elements)`. The post-load sweep dirties the model, `handleLoadSuppressEnd()` arms the debounce (SaveOrchestrator.ts:536-543), and the ONE guard that exists to stop this — the content-hash comparison — cannot fire because the hash contains `Date.now()` (see the timestamp refutation above). So every open pays: 2 full serializes + a main-thread deflate + a 1.4 MB IndexedDB container write + a server version POST + 2 thumbnail POSTs, to persist a state nobody changed. Finding #2 costs one of the two serializes; this is the other ~90% of the same write amplification, and it has the same root cause.",
    "DUPLICATE THUMBNAIL UPLOAD TO THE SERVER. Both capture paths call `_uploadThumbnailToServer` — PlatformSaveController.ts:396 (auto-save) and :570 (post-load) — with the same project id, ~1 s apart. Two `PATCH /api/projects/:id/thumbnail` requests per open, the second overwriting the first, plus two `projectRepository` index writes to the same `ProjectMeta.thumbnail`. Finding #3 counts the two encodes but not the two network writes or the two index writes.",
    "THE POST-LOAD PASS TRAVERSES THE WHOLE SCENE TWICE, AND THE CENSUS MAKES IT THREE. `runTierPbrPass` runs `collectNewPbrMeshes(scene)` and then a separate `scene.traverse` mesh count (initScene.ts:2946-2956) — the repo documents this itself at apps/editor/src/engine/geometryMutationEvents.ts:44: 'costs two FULL-SCENE traverses (collectNewPbrMeshes + the mesh count)'. One second later `logSceneCensusOnce` runs `censusScene()`, a THIRD full traverse with per-family bucketing over the same 3762 meshes, unconditionally in production (initScene.ts:5175-5185, not behind `perfTraceOn()`), re-deriving the very mesh/element counts the tier pass just computed — a duplication `heavySceneSwapThreshold.ts:14-24` (L-11564) knowingly accepted for import-graph reasons. Add `reportSwapPaintsTheBuilding` twice (initScene.ts:5166, 5168) and `reportViewportBackground` once. This is diagnostic work, so the fix is a gate, not a redesign — but it is three whole-scene walks in the two seconds the founder is complaining about.",
    "THE WASTED BASELINE SERIALIZE IS MOSTLY THE JOURNAL, NOT THE MODEL. `ProjectSerializer.serialize()` embeds `temporalGraphManager.serialize()` whole (ProjectSerializer.ts:1897) — the founder's log reads `temporalGraph 2366 mutations`, and the §PROBE-SNAPSHOT-JOURNAL-WEIGHT note at ProjectSerializer.ts:2066-2078 records that for a 264-element project the model is ~0.1 MB while the stored container is ~35 MB, the difference being exactly this member. So the discarded `resetDirtyAfterLoad()` serialize is dominated by cloning 2366 journal records that are ALREADY deduped on disk as a sidecar ('JOURNAL-SIDECAR: 2366 journal record(s) shared across 6 version(s)'). Sizing finding #2 from the 503 elements understates it; the journal is the payload."
  ]
}