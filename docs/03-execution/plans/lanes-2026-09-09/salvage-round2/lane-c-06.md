{
  "dimension": "Mesh multiplication: 503 elements -> 3762 meshes (7.5x) — which families produce the meshes, whether instancing/merging can collapse them, and whether mesh count costs OPEN time or frame time",
  "summary": "The 7.5x ratio is real but the log's own denominator is not: the \"162 elements\" in the NAV-BACKEND-SWAP-HEADROOM line is a census artifact (it counts only meshes carrying BOTH userData.id and userData.elementType), so 3762/162 = 23 is wrong and 3762/503 = 7.5 is the honest figure — and even 503 understates it, because ProjectSerializer's elementCount excludes windows and doors. The mesh count is dominated by families that emit one mesh per PART with no merge and no instancing: furniture (95 items, the repo's own §PERF105 measurement puts a furniture item at 8-20 meshes; its instancing bridge is fully built and DEFAULT OFF) and opening-bearing walls (headless measurement on this tree: 200 realistic walls -> 1080 meshes / 1680 material constructions / 0% instanced, against a no-openings control of 205 meshes / 82 materials / 80% instanced). The shared merge helper `consolidateSiblingMeshes` exists and has exactly ONE production caller. On the founder's question — mesh count is mostly a FRAME cost, and I say so: the two known O(n^2) load traversals are already gated and closed, and the ledger's biggest measured load finding (CreateWallCommand's quadratic clone) is negligible at 56 walls. The one clear OPEN-path consequence of the mesh count is that the heavy-scene guard's mesh arm has inverted relative to its own calibration and now fires a full renderer teardown/rebuild during every real project open — which his log proves happened, because the census line he pasted can only be printed by the swap's own one-second timer.",
  "whatIcouldNotEstablish": "1) THE SINGLE MOST VALUABLE MISSING LINE IS IN HIS OWN LOG. `printSceneCensus` emits a `§NAV-MESH-PER-ELEMENT-CENSUS` header plus ONE PLAIN-TEXT ROW PER FAMILY (elems, meshes, m/elem, inst.grp, instances, tris, shadow, lines, instanced?) immediately BEFORE the `§NAV-BACKEND-SWAP-HEADROOM` line he pasted (pryzmPerfConsole.ts:600-630). That table names the top family and gives the exact `(unattributed)` count. Everything below labelled \"estimated\" collapses to \"measured\" with that one paste. Ask for the ~25 lines above the headroom line.\n2) THE FAMILY BREAKDOWN OF 3762 IS NOT MEASURED BY ME. My per-family numbers are (a) a headless harness run for walls, (b) the repo's own §PERF105 figures for furniture/doors/fixtures. I did not run the founder's project and have no browser. I refuse to publish an arithmetic reconciliation of 3762 from those parts — that is exactly the \"arithmetic story\" the performance ledger warns about at §10.1.\n3) THE SWAP'S DURATION IS MEASURED NOWHERE. `swapRendererBackend` (initScene.ts:~4966-5185) prints `§RENDERER-LIVE-SWAP live swap complete` with no elapsed time, and `reportSwapPaintsTheBuilding` reports draw state, not milliseconds. I therefore report the swap's cost as UNKNOWN (-1) rather than inventing one. Instrumenting it is one `performance.now()` pair and it is the cheapest next measurement in this dimension.\n4) WHICH FAMILIES SUPPLY THE CENSUS'S \"162\" IS NOT ESTABLISHED. 56 walls + 10 slabs + 95 furniture = 161, which is a suspicious near-match, but furniture roots are `THREE.Group`s (not meshes) and wall roots are groups too, so the arithmetic does not close. I did not chase it because the number is an artifact either way.\n5) THE THIRD \"(unattributed)\" EXAMPLE — `(anon) < (anon)`, i.e. an unnamed mesh parented directly to the scene root — is UNKNOWN. I ruled out the plan/section projection (EdgeProjectorService creates 0 meshes and 7 LineSegments sites, and the census counts lines separately), and the other two named examples are 2 declared app-chrome meshes. I could not identify the producer.\n6) I DID NOT ESTABLISH THAT MESH COUNT IS THE FOUNDER'S OPEN-TIME BOTTLENECK. The log carries at least three non-mesh open-cost candidates outside my dimension (temporalGraph 2366 mutations embedded per snapshot across 6 versions at ~1.4 MB, a duplicated thumbnail capture, and a duplicated socket room join). If someone else's dimension measures those larger, this dimension's findings are still true and still smaller.\n7) GPU-SIDE COSTS ARE UNMEASURED BY CONSTRUCTION. Buffer upload, program/PSO compile and first paint need a browser. STARTUP27's own header says so and refuses to estimate them; so do I.",
  "findings": [
    {
      "title": "The heavy-scene guard's mesh arm has INVERTED against its own calibration — at 7.5 meshes/element it fires ~3x earlier than the element arm it was written to backstop, so the renderer live-swap now runs on every real project open. His log proves it fired on this one.",
      "mechanism": "`isSwapWorthyHeavyScene` trips on EITHER `elements >= 400` OR `meshes >= 1000` (heavySceneSwapThreshold.ts:40,43). The mesh arm was calibrated against a scene the file itself describes as '~6-storey / ~1,300-element / ~1,645-mesh' — 1.27 meshes/element, at which 1000 meshes is ~790 elements, i.e. the LOOSER of the two arms. At the founder's 7.5 meshes/element, 1000 meshes is ~133 elements — 3x TIGHTER than the 400-element arm. The arm that was designed as a backstop is now the only arm that ever fires. When it fires, `fireSwapToWebGL` calls `pryzmSwapRendererBackend('webgl-classic')`, which stops the rAF loop, disposes the RenderPipelineManager, creates a NEW canvas, `await createRenderer(...)`, rebinds the pipeline, and retires the old renderer — after which, per initScene's own retirement comment, 'the new renderer mints its own draw state on its first frame'. That is a second full GPU upload + program compile of all 3762 meshes, on the open path. PROOF IT HAPPENED ON THIS OPEN: `logSceneCensusOnce` has exactly ONE production call site — the swap's own 1-second timer — so the `§NAV-BACKEND-SWAP-HEADROOM` line in his log cannot be printed unless a live swap completed; and the log independently carries `WebGPU device lost: reason=\"destroyed\"`, which is a deliberate destroy, not a TDR.",
      "evidence": [
        "apps/editor/src/rendering/heavySceneSwapThreshold.ts:40,43 — `export const SWAP_ELEMENT_THRESHOLD = 400;` / `export const SWAP_MESH_THRESHOLD = 1000;`",
        "apps/editor/src/rendering/heavySceneSwapThreshold.ts (⛔ DECOUPLED note) — 'a normal ~6-storey / ~1,300-element / ~1,645-mesh generation reliably TDRs the WebGPU device yet never trips isHeavyModel (L-361)' → 1645/1300 = 1.27 meshes/element, the ratio the 1000 was chosen against",
        "apps/editor/src/rendering/autoWebGLHeavyScene.ts:196-200 — `export function isSwapWorthyHeavyScene(elementCount, sceneMeshCount) { if (elementCount >= SWAP_ELEMENT_THRESHOLD) return true; if (typeof sceneMeshCount === 'number' && sceneMeshCount >= SWAP_MESH_THRESHOLD) return true; return false; }`",
        "apps/editor/src/engine/initScene.ts:5182 — `m.logSceneCensusOnce(window.scene, 'one-second-after-swap');` — the ONLY production call site (a repo-wide grep for `logSceneCensusOnce` returns this line plus the definition and doc mentions; `printSceneCensus` is otherwise reachable only via `window.pryzmPerf.census()`, label 'on-demand')",
        "FOUNDER LOG — `NAV-BACKEND-SWAP-HEADROOM ... NOW: 162 elements (238 to go), 3762 meshes (ARM TRIPPED)` and `[createRenderer] WebGPU device lost: reason=\"destroyed\"`",
        "apps/editor/src/engine/initScene.ts:5124-5133 — `§RETIRE-RENDERER-DETACHES-LISTENERS ... nothing needs re-attaching: the new renderer mints its own draw state on its first frame (measured, L-1410)` — and the same comment records that `mintedRenderObjectCount` 'has reported 0, 3181, 6936 and 6937 across a single day of founder sessions'"
      ],
      "estimatedCostMs": -1,
      "costBasis": "unknown",
      "fix": "MEASURE IT FIRST — this is the one item where a number is a `performance.now()` pair away. Wrap the swap body in initScene (`swapRendererBackend`, the try-block from step 1 'stop the rAF loop' to step 8 'resume') and print the elapsed ms on the existing `§RENDERER-LIVE-SWAP live swap complete` line. THEN, and only then, decide. Do NOT raise SWAP_MESH_THRESHOLD — heavySceneSwapThreshold.ts explicitly forbids it and it is right to. The real decision is whether the mesh arm's rationale still holds: it exists to catch heavy scenes 'whose geometry arrives with sparse top-level userData ids', and at 7.5 m/e it no longer discriminates. Candidate: derive the mesh arm from the live meshes/element ratio, or fire it only when the element arm is also within some fraction. Note the arithmetic before planning family-level fixes: removing ALL furniture meshes (760-1900) still leaves ~1900-3000, so no single family fix clears 1000.",
      "whatItWouldBreak": "The swap exists because a heavy scene on WebGPU reliably device-losses the founder's GPU (ADR-0267 / L-361, founder-confirmed repeatedly). Any change that lets a heavy scene stay on WebGPU re-opens a hard crash, not a slowdown. That is why the fix here is 'measure the swap', not 'stop swapping'.",
      "risk": "medium",
      "confidence": "high",
      "filesToChange": [
        "apps/editor/src/engine/initScene.ts",
        "apps/editor/src/rendering/heavySceneSwapThreshold.ts",
        "apps/editor/src/rendering/autoWebGLHeavyScene.ts"
      ]
    },
    {
      "title": "Furniture is the largest mesh family in the repo by an order of magnitude, and it is the ONE family whose instancing is fully built, fully wired, and switched OFF by default",
      "mechanism": "`FurnitureFragmentBuilder` routes every item to `FurnitureFactory.getBuilder(type).build(data)`, which returns a Group of one mesh per PART; the builder then stamps each child `elementType:'FurniturePart'` and adds the whole group to the scene. There is no sibling-mesh merge anywhere in the package. The GPU-instancing path exists and is complete — `FurnitureInstanceBridge.register()` bakes the group into per-part (geometry, material) pairs and hands each to the shared `InstancedElementRenderer`, whose `register()` runs `dedupInstanceMaterial` BEFORE hashing the group key, so identical parts of identical items collapse into one InstancedMesh per (geometry x look x level). It is gated by `isFurnitureInstancingEnabled()`, which is `globalThis.__pryzmFurnitureInstancingV1 === true`, and the builder's own default is `() => false`. This is the repo's own `authored-but-unwired` pattern. Magnitude: geometry-furniture contains 590 `new THREE.Mesh(` construction sites and 117 inline material constructions across its builders and engines — the next family (geometry-lighting, 116) is 5x smaller AND already merges. The repo's own §PERF105 measurement, recorded in the merge helper's header, puts a furniture item at 8-20 meshes; at 95 items that is 760-1900 of the 3762.",
      "evidence": [
        "packages/geometry-furniture/src/FurnitureFragmentBuilder.ts:64 — `private _instancingEnabled: () => boolean = () => false;`",
        "packages/core-app-model/src/rendering/FurnitureInstanceBridge.ts:125-128 — `export function isFurnitureInstancingEnabled(): boolean { return withBridgeSpan('flag', {}, () => (globalThis as { __pryzmFurnitureInstancingV1?: boolean }).__pryzmFurnitureInstancingV1 === true); }`",
        "apps/editor/src/engine/initBuilders.ts:57 — `// + its independent flag (__pryzmFurnitureInstancingV1, default OFF).`",
        "packages/renderer-three/src/consolidateSiblingMeshes.ts:10-14 — 'THE MEASUREMENT THAT FORCED IT (§PERF105): a door was 13-15 meshes typical and ~30 worst ..., a fixture 4-8, furniture 8-20'",
        "MEASURED (static census, this tree): `for d in packages/geometry-*; grep -rc 'new THREE\\.Mesh(' $d/src` → geometry-furniture 590 | geometry-lighting 116 | geometry-plumbing 54 | geometry-wall 40 | geometry-slab 26 | geometry-stair 16",
        "packages/core-app-model/src/rendering/FurnitureInstanceBridge.ts:205-224 — per-part registration: `const partKey = `${elementId}#part${i}`; this._renderer.register(partKey, part.geometry, part.material, worldMatrix, levelId, 'Furniture', elementId);` with the comment 'identical parts of identical items still collapse into one group'",
        "FOUNDER LOG — `503 elements, 7 levels, 56 walls, 10 slabs, 95 furniture`"
      ],
      "estimatedCostMs": -1,
      "costBasis": "read-from-code",
      "fix": "Two levers, cheapest first. (1) CONSOLIDATION, no flag: call the shared `consolidateSiblingMeshes(group, {...})` in `FurnitureFragmentBuilder.updateFurniture` right after `builder.build(resolved)` and before the sub-element stamp — the same three lines `DoorBuilder.ts:647` already uses, and the same thing `LightingFragmentBuilder._consolidateFixtureMeshes` does privately. Triangle-neutral by construction; collapses same-material siblings only. (2) INSTANCING: flip `__pryzmFurnitureInstancingV1` per the discipline `_FAMILY_DEFAULTS` demands — 'do NOT flip a family here without the test that proves its delete path, and do not flip one on a reason — flip it on a number'. `FurnitureInstanceBridge.test.ts` exists; check whether it covers the delete/rebuild path before flipping. Do (1) first: it needs no flag, no delete-path proof, and is measurable by re-reading the census.",
      "whatItWouldBreak": "Consolidation bakes each part's LOCAL matrix into a geometry clone and disposes the originals, so any code that reads a furniture child's `.geometry.parameters`, its individual `.position`, or counts its children breaks. The helper's `skip`/`keyOf` options exist for exactly this (a door uses `keyOf` to keep its `role` parts apart) — furniture's plan-symbol builders (BedPlanSymbolBuilder, ChairPlanSymbolBuilder, KitchenPlanSymbolBuilder, SofaPlanSymbolBuilder, TreePlanSymbolBuilder) and the kitchen `isKitchenPart`/`isKitchenCountertop` flags are the things to check first. Instancing additionally re-opens ADR-0297 L1/L2 (a per-element teardown freeing a material or buffer the InstancedMesh still draws with).",
      "risk": "medium",
      "confidence": "high",
      "filesToChange": [
        "packages/geometry-furniture/src/FurnitureFragmentBuilder.ts",
        "packages/core-app-model/src/rendering/ElementInstanceBridge.ts",
        "packages/core-app-model/src/rendering/FurnitureInstanceBridge.ts"
      ]
    },
    {
      "title": "MEASURED on this tree: a wall with openings is 5.4 meshes and 8.4 material constructions; the same wall without openings is 1.03 meshes and 0.41 materials. Openings alone disqualify 100% of a realistic building from the instanced arm.",
      "mechanism": "`WallFragmentBuilder`'s `isSimpleWall` predicate has `!_hasOpenings` as a hard clause. A wall that fails it takes the fragment arm, which emits one mesh per body segment / layer / jamb part (`elementType:'WallPart'` / `'WallLayer'`) with a freshly constructed material per part — and, crucially, NEVER reaches `InstancedElementRenderer.register()`, so it never reaches the `dedupInstanceMaterial` chokepoint either. I ran the repo's own headless project-open harness, which drives the real `refreshV2Cache -> WallJoinResolver.resolveLevel -> buildWall` replay path with a realistic-openings arm and a `openings: []` CONTROL arm on the identical grid. Every scale rejected on the SAME clause: `openings` equals `notInstanced` exactly (200/200, 3960/3960, 20240/20240), with mitre clauses two orders of magnitude smaller. So the mitre hypothesis that WallFragmentBuilder.ts:1404-1415 records as 'the live hypothesis' is refuted for this corpus, and openings ARE the answer.",
      "evidence": [
        "MEASURED — `npx vitest run __tests__/STARTUP27ProjectOpenScale.measure.test.ts` in packages/geometry-wall, 2026-09-08, 7/7 passed, report at %TEMP%/startup27-project-open-scale.txt. ALLOCATION table, 1x rows: `realistic 200 walls -> sceneMeshes 1080, geomCreated 2440, matCreated 1680, instGroups 0, mat/wall 8.40` vs `none 200 walls -> sceneMeshes 205, geomCreated 400, matCreated 82, instGroups 5, instTotal 160, mat/wall 0.41`",
        "MEASURED — same report, REJECTION CLAUSES table: `1x realistic notInst 200 | openings 200 | mitreStart 20 | mitreEnd 20 | curve 0 | rake 0 | layers 0 | profile 0 | noBridge 0`; identical shape at 20x (3960/3960) and 100x (20240/20240)",
        "MEASURED — same report, TIMING table: `1x realistic specCache 21.0 resolve 35.8 build 352.8 TOTAL 409.5` vs `1x none specCache 7.3 resolve 18.3 build 80.4 TOTAL 106.0` (ms, 200 walls, 5 levels) → 1.764 ms/wall vs 0.402 ms/wall of BUILD, on the project-open replay path",
        "packages/geometry-wall/src/WallFragmentBuilder.ts:1438-1447 — `const isSimpleWall = (this._instanceBridge !== null && !_hasOpenings && !wall.curve && !joinData?.startMN && !joinData?.endMN && isVerticalRake(...) && _layerCount <= 1 && !_hasWallProfile);`",
        "packages/geometry-wall/__tests__/STARTUP27ProjectOpenScale.measure.test.ts:29-41 — the file's own hypothesis: '`WallFragmentBuilder`'s `isSimpleWall` predicate disqualifies a wall from the GPU instanced arm if it has ANY opening ... what nobody had measured is what fraction of a REAL building it disqualifies'"
      ],
      "estimatedCostMs": 76,
      "costBasis": "estimated",
      "fix": "Two independent levers. (1) MESHES: call `consolidateSiblingMeshes` on the fragment-arm wall group — WallFragmentBuilder already has a private `WallPart` merge at :3448 ('merge abutting body box segments (each tagged elementType:WallPart) into ONE mesh'); check whether it is reached on the opening path and widen it if not. (2) INSTANCING: an opening-bearing wall cannot ride a unit-box T·R·S matrix, which is the documented reason for the clause — but nothing stops routing the wall's SOLID SEGMENTS BETWEEN openings through the instanced arm as separate box instances. That is a design change, not a flag flip. Do (1) first.",
      "whatItWouldBreak": "The `!_hasOpenings` clause is a CORRECTNESS clause of the same family as `!_hasWallProfile` and `isVerticalRake`, whose rationale is spelled out at WallFragmentBuilder.ts:1395-1435: the instanced arm draws a unit BoxGeometry positioned by one Matrix4 (translation × rotationY × scale), and a T·R·S product cannot express a pierced silhouette. A wall routed there would render SOLID while the model says otherwise — 'a silently-wrong wall'. Do not remove the clause. Merging WallParts is separately constrained by the plan/section projector, which reads per-part userData (EdgeProjectorService groups by elemType) and by the LAYERED opening path (§wall-opening-seam-two-paths: plain -> CSG single volume, LAYERED -> grid).",
      "risk": "medium",
      "confidence": "high",
      "filesToChange": [
        "packages/geometry-wall/src/WallFragmentBuilder.ts"
      ]
    },
    {
      "title": "The shared sibling-mesh merge helper exists, is owned by the THREE owner, carries the per-family measurement in its own header — and has exactly ONE production caller",
      "mechanism": "`packages/renderer-three/src/consolidateSiblingMeshes.ts` was written under §MESH110-CONSOLIDATE precisely so 'door / lighting / any future family share it instead of each growing a private merge with its own quirks'. A repo-wide grep for call sites (excluding the definition) returns one line: `DoorBuilder.ts:647`. Lighting did not adopt it — it grew the private `_consolidateFixtureMeshes` the helper's header warns against. Every other multi-part family has no merge at all: furniture (590 mesh sites), plumbing (54), slab (26), stair (16), curtain-wall (14). The helper is triangle-neutral by construction (`mergeGeometries` concatenates; only the submission count falls) and declines any bucket it cannot merge, so it drops nothing.",
      "evidence": [
        "MEASURED (grep, this tree) — `consolidateSiblingMeshes\\(` across all `**/src/**/*.ts` → 2 hits: `packages/geometry-door/src/DoorBuilder.ts:647: consolidateSiblingMeshes(group, {` and `packages/renderer-three/src/consolidateSiblingMeshes.ts:78: export function consolidateSiblingMeshes(`",
        "packages/renderer-three/src/consolidateSiblingMeshes.ts:4-8 — 'ONE implementation, owned by the THREE owner (P2), so door / lighting / any future family share it instead of each growing a private merge with its own quirks (the L-11564 lesson: two copies of one fact agree until the day only one is edited).'",
        "packages/geometry-lighting/src/LightingFragmentBuilder.ts:700-704 — `// §MESH110-FIXTURE-MERGE (L-11567 #1) — collapse same-material sibling parts ... this._consolidateFixtureMeshes(group);` — the private copy, in the package the shared helper names first",
        "packages/renderer-three/src/consolidateSiblingMeshes.ts:42-45 — '⚠ TRIANGLE-NEUTRAL by construction: `mergeGeometries` concatenates; the triangle census is unchanged and only the submission count falls.'"
      ],
      "estimatedCostMs": -1,
      "costBasis": "read-from-code",
      "fix": "Adopt the helper family by family, heaviest first: furniture, then plumbing, then stair, then curtain-wall and slab. Each adoption is the same three-line shape DoorBuilder uses (`skip` for parts whose geometry the builder does not own, `keyOf` for semantically distinct parts a downstream projector reads). Separately, retire `LightingFragmentBuilder._consolidateFixtureMeshes` in favour of the shared helper so there is one implementation — that is the helper's own stated reason for existing and lighting is currently the counter-example.",
      "whatItWouldBreak": "Per family: anything reading a merged child's `.geometry.parameters`, `.position`, or child count. The plan-symbol builders are the concrete risk for furniture and stair. Nothing breaks in a bucket of one — those are left completely untouched.",
      "risk": "low",
      "confidence": "high",
      "filesToChange": [
        "packages/geometry-furniture/src/FurnitureFragmentBuilder.ts",
        "packages/geometry-plumbing/src",
        "packages/geometry-lighting/src/LightingFragmentBuilder.ts",
        "packages/renderer-three/src/consolidateSiblingMeshes.ts"
      ]
    },
    {
      "title": "CORRECTION to the standing memory note: 'instancing defeated by per-element UNIQUE MATERIALS' is now only half true — the material problem is solved at the chokepoint, and what actually blocks these families is the GATE UPSTREAM of it",
      "mechanism": "The brief asked me to verify whether per-element unique materials still defeat instancing. Both halves are true but they no longer compose. (a) Builders DO still mint materials per element: `ChairBuilder.buildOakChair` executes `new THREE.MeshStandardMaterial({...})` on every call, as do buildOakChairSlim (:223) and eight sibling variants; the wall fragment arm constructs 8.4 materials per wall (measured). (b) BUT `InstancedElementRenderer.register()` calls `dedupInstanceMaterial(material)` BEFORE computing the group key, and that dedup is DEFAULT ON (`globalThis.__pryzmInstanceMaterialDedup === false` disables it; undefined enables it). It maps a material to the canonical instance for its visual signature, so N look-alike per-element materials collapse to one uuid and therefore one InstanceGroup. So for any family that REACHES register(), unique materials no longer defeat instancing. Furniture and opening-bearing walls are not blocked by their materials — they are blocked by a flag (`__pryzmFurnitureInstancingV1`, default false) and by a predicate clause (`!_hasOpenings`) that both sit UPSTREAM of the dedup chokepoint. This matters for planning: a 'share the materials first' project would be work with no effect on these two families.",
      "evidence": [
        "packages/core-app-model/src/rendering/InstancedElementRenderer.ts:244 — `const sharedMaterial = dedupInstanceMaterial(material);` followed at :251 by `const baseKey = this._hashGeometry(geometry, sharedMaterial, levelId, elementType);`",
        "packages/core-app-model/src/rendering/SharedMaterialCache.ts:46-52 — '## Flag — DEFAULT ON, revertible ... `globalThis.__pryzmInstanceMaterialDedup === false` disables dedup entirely ... Any other value (undefined / true) enables it.'",
        "packages/core-app-model/src/rendering/SharedMaterialCache.ts:110-124 — `export function dedupInstanceMaterial(material) { ... if (!_isDedupEnabled()) return material; const sig = materialInstanceSignature(material); if (sig === null) return material; const existing = _canonicalBySignature.get(sig); if (existing) return existing; ... }`",
        "packages/geometry-furniture/src/builders/ChairBuilder.ts:106-110 — inside `private buildOakChair(data, variant)`: `const oakMat = new THREE.MeshStandardMaterial({ color: variant === 'solid' ? 0xc8954a : 0xd4a35e, roughness: 0.55, metalness: 0.0 });` — one fresh material object per furniture element",
        "packages/geometry-furniture/src/builders/ChairBuilder.ts:223 — inside `private buildOakChairSlim(data)`: `const oak = new THREE.MeshStandardMaterial({ color: 0xc89058, roughness: 0.52, metalness: 0.0 });`",
        "MEASURED — STARTUP27 report, `matCreated` column: 1680 material constructions for 200 realistic walls (8.40/wall) on the NON-instanced arm, vs 82 (0.41/wall) on the instanced control"
      ],
      "estimatedCostMs": 0,
      "costBasis": "read-from-code",
      "fix": "No code change. This is a planning correction: order the work as (1) flip/route the family onto the instanced path, (2) do NOT budget a separate material-sharing project for it. Keep the per-element material construction on the FRAGMENT path in view as a separate, smaller allocation cost (1680 material objects per 200 walls) but not as an instancing blocker. The performance ledger §9.5 #2 already names `material.uuid` in the group key as 'the thing worth measuring' — it has since been neutralised at the chokepoint and the ledger line should be updated.",
      "whatItWouldBreak": "none known — this finding changes no code.",
      "risk": "low",
      "confidence": "high",
      "filesToChange": [
        "docs/04-reference/APPLICATION-PERFORMANCE-LEDGER.md"
      ]
    },
    {
      "title": "'162 elements' in the founder's headroom line is a MEASUREMENT ARTIFACT, not his project's element count — the census requires userData.id AND userData.elementType on the SAME MESH",
      "mechanism": "`censusScene` increments its element denominator only inside the `isMesh` branch, and only when a MESH object carries both keys: `const id = o.userData?.id; if (id && type) { ... idsByFamily[type].add(id); }`. Every builder in this repo puts `id` on the element's ROOT GROUP and puts `elementType` (as 'FurniturePart' / 'WallPart' / 'Lighting') on the child MESHES with `parentId` instead of `id`. A Group is not a mesh, so it is never counted; a child mesh has a type but no id, so it is never counted either. The result is that `c.elements` measures neither elements nor meshes. Anyone dividing 3762 by 162 gets 23.2 meshes/element and is wrong by 3x. The honest ratio is 3762/503 = 7.5 — and 503 is itself a FLOOR, because `ProjectSerializer`'s `elementCount` sums walls+slabs+ceilings+floors+columns+stairs+beams+curtainWalls+roofs+furniture+handrails+plumbing+rooms+lighting+boundaryLines+lifts+pools+balconies+components+spaceEnvelopes and does NOT include windows or doors, which are hosted openings and do produce meshes.",
      "evidence": [
        "apps/editor/src/engine/pryzmPerfConsole.ts:511-514 — `const id = o.userData?.id;` / `if (id && type) { c.elementsByType[type] = (c.elementsByType[type] ?? 0) + 1; (idsByFamily[type] ??= new Set<string>()).add(id); }` — inside the `if (!o.isMesh) return;` branch at :483",
        "packages/geometry-furniture/src/FurnitureFragmentBuilder.ts:99-108 — `root.userData = { id: data.id, elementType: 'Furniture', ... }` where `root = new THREE.Group()`",
        "packages/geometry-furniture/src/FurnitureFragmentBuilder.ts:262-266 — child stamp: `child.userData = { ...child.userData, elementType: existingElementType ?? (isKitchenPart ? 'KitchenCabinetPart' : 'FurniturePart'), modelId: 'model-default', parentId: data.id, isSubElement: true, ... }` — note `parentId`, never `id`",
        "apps/editor/src/engine/persistence/ProjectSerializer.ts:1745-1783 — the `elementCount` sum; windows and doors appear nowhere in it",
        "FOUNDER LOG — snapshot says `503 elements`, the census's headroom line says `162 elements`; 3762/503 = 7.48, 3762/162 = 23.2"
      ],
      "estimatedCostMs": 0,
      "costBasis": "read-from-code",
      "fix": "In `censusScene`, resolve an element id by walking up to the nearest ancestor carrying `userData.id` (the `chainOf` helper already walks parents six deep, so the traversal exists in the same function), or count `userData.parentId` as an element key when `id` is absent. Then print BOTH numbers side by side — 'meshes with own id' and 'distinct elements incl. inherited' — so the artifact cannot come back silently. Until then, do not quote the element figure from this line.",
      "whatItWouldBreak": "The comment at pryzmPerfConsole.ts:517-518 says the DISTINCT-ids design exists so 'a family that stamps every child mesh (stair-railing does) reports its real element count and not its mesh count under an element label'. Inheriting an id from an ancestor preserves that property (a set of distinct ids is still a set) but must not double-count an element whose root ALSO happens to be a mesh.",
      "risk": "low",
      "confidence": "high",
      "filesToChange": [
        "apps/editor/src/engine/pryzmPerfConsole.ts"
      ]
    },
    {
      "title": "(b) The '(unattributed)' bucket: the two named examples are 2 declared app-chrome meshes and cannot be the bulk; element sub-meshes are all attributed; the plan projector is ruled out; the third example is UNKNOWN and the exact count is one line above what he pasted",
      "mechanism": "The census buckets on a MISSING `userData.elementType` (pryzmPerfConsole.ts:468-472), and prints up to three parent chains as examples. Of the three in his log: `pryzm-infinite-grid-3d` is ONE shader ground-grid plane (`InfiniteGrid3D.ts:81`) and `__pryzm_ground_shadow_catcher__` is ONE invisible renderer-owned plane (`GroundShadowCatcher.ts:35`) — both are already formally DECLARED project-independent app chrome, with citations, in `PROJECT_INDEPENDENT_SCENE_ROOTS`. Two meshes. The third, `(anon) < (anon)`, is a mesh with an empty name whose parent also has an empty name and has no parent of its own — i.e. an unnamed mesh parented DIRECTLY to the scene root. I could not identify its producer. What I CAN rule out: element sub-meshes are not in this bucket (walls stamp `WallPart`/`WallLayer`, furniture stamps `FurniturePart`, lighting stamps `Lighting` on every child), and the plan/section projection is not in it either — `EdgeProjectorService` creates ZERO `new THREE.Mesh(` and seven `LineSegments` sites, and the census counts lines in a separate branch before the mesh branch.",
      "evidence": [
        "apps/editor/src/engine/pryzmPerfConsole.ts:468-472 — `const type = o.userData?.elementType; ... const fam = type ?? '(unattributed)';`",
        "packages/core-app-model/src/persistence/ProjectIsolationAudit.ts:418-445 — `PROJECT_INDEPENDENT_SCENE_ROOTS` names `pryzm-infinite-grid-3d` ('the shader ground-grid plane ... App-lifetime chrome constructed once in initScene') and `__pryzm_ground_shadow_catcher__` ('a renderer-owned invisible plane that receives ground shadows')",
        "apps/editor/src/engine/pryzmPerfConsole.ts:455-465 — `chainOf`: `const tag = cur.userData?.elementType ?? cur.userData?.id ?? cur.name ?? cur.type ?? '?'; parts.push(String(tag).slice(0, 32) || '(anon)');` walking `cur = cur.parent` up to 6 deep — a 2-link chain means the parent has no parent, i.e. the scene root",
        "MEASURED (grep, this tree) — `grep -c 'new THREE.Mesh(' apps/editor/src/engine/views/EdgeProjectorService.ts` → 0; `grep -c 'LineSegments(' ...` → 7",
        "apps/editor/src/engine/pryzmPerfConsole.ts:476-482 — the line branch `if (o.isLine || o.isLineSegments) { c.lineObjects++; ... return; }` executes BEFORE `if (!o.isMesh) return;`, so projected edges never enter the mesh count",
        "packages/geometry-wall/src/WallFragmentBuilder.ts:2314,2519,3410,4653,4799 — child meshes stamped `elementType: 'WallPart'`; :2187,:2647 `'WallLayer'`; packages/geometry-lighting/src/LightingFragmentBuilder.ts:747-751 — `child.userData.elementType = 'Lighting'` on every child mesh"
      ],
      "estimatedCostMs": -1,
      "costBasis": "unknown",
      "fix": "Read the number instead of theorising about it: the `(unattributed)` row of the per-family table sits ~20 lines above the `§NAV-BACKEND-SWAP-HEADROOM` line he already pasted, and gives elems/meshes/tris/shadow/lines for the bucket. If it turns out to be large, the `(anon) < (anon)` producer is the thing to chase. Cheap hardening regardless: extend the two declared chrome roots into the census itself so grid and shadow-catcher are reported as 'declared chrome' rather than as findings — `ProjectIsolationAudit` already models exactly that distinction and the census does not.",
      "whatItWouldBreak": "none known — reading a line changes nothing. The declared-chrome hardening must follow ProjectIsolationAudit's own rule: 'THE ONLY LEGITIMATE WAY TO SHRINK THAT NUMBER IS TO DECLARE A ROOT, NEVER TO ASSUME ONE CLEAN.'",
      "risk": "low",
      "confidence": "medium",
      "filesToChange": [
        "apps/editor/src/engine/pryzmPerfConsole.ts"
      ]
    },
    {
      "title": "(c) HONEST ANSWER: mesh count is overwhelmingly a FRAME cost. Its only established OPEN-path costs are the backend swap and one load-end per-mesh bounding-sphere sweep. The load traversals that WOULD have made it an open cost are already gated and closed — I checked, and I am reporting that rather than re-reporting them.",
      "mechanism": "The founder asked about OPENING, so the mechanism question is 'what on the load path is O(meshes)?'. THREE candidates existed and TWO are already closed. CLOSED #1 — the per-add tier+PBR pass ran two full `scene.traverse()` per geometry-add event, which during a load is O(n^2) over a growing scene; `shouldDeferPerAddGeometryPass` now returns early on `isProjectLoadActive()`, and `ProjectLoader` really does set `globalThis.__pryzmProjectLoadActive = true` at :710 and clear it at :3228, with one consolidated pass at `pryzm-project-loaded`. CLOSED #2 — `_debouncedGeomAdded` -> `PascalSceneLighting.onGeometryAdded` had the same shape on 41 events; it now returns early during load and runs ONCE at load end. STILL OPEN and O(meshes) — that one load-end pass calls `obj.geometry.computeBoundingSphere()` on every mesh that lacks one, which for per-element-unique geometries is every mesh, i.e. O(total vertices) once per open; it self-times and prints its own ms. Also on the open path but NOT mesh-driven: `EdgeProjectorService` at `work=109ms` with a 0% cache hit rate is a COLD CACHE on first projection, which is correct behaviour on open, not a defect. And the performance ledger's single biggest measured load finding — `CreateWallCommand:446` deep-cloning the whole wall store per wall, N(N-1)/2 clones — is ~1540 clones at 56 walls, i.e. single-digit milliseconds here; it is a real defect at 1000+ walls and a non-issue at this scale. CONCLUSION: for a 503-element project I cannot claim mesh count is the dominant open cost, and the largest items in his log (temporalGraph 2366 mutations embedded per snapshot, a double thumbnail capture, a double socket join) belong to other dimensions.",
      "evidence": [
        "apps/editor/src/engine/perAddGeometryGate.ts:44-45 — `export function shouldDeferPerAddGeometryPass(isBatching: boolean): boolean { const defer = isBatching || isProjectLoadActive();`",
        "apps/editor/src/engine/persistence/ProjectLoader.ts:710 — `(globalThis as unknown as { __pryzmProjectLoadActive?: boolean }).__pryzmProjectLoadActive = true;` and :3228 the matching `= false` — the gate is genuinely armed on the real load path",
        "apps/editor/src/engine/initScene.ts:4120-4123 — `if (isProjectLoadActive()) { _geomAddedSuppressedDuringLoad++; return; }` with the one-shot at :4159-4172 `pascalSceneLighting.onGeometryAdded(...)` printing `§LOAD-SHADOW-PASS-DEFERRED ran ONE full-scene shadow-flag pass in ${...}ms`",
        "packages/core-app-model/src/rendering/PascalSceneLighting.ts:537 — `if (!obj.geometry.boundingSphere) obj.geometry.computeBoundingSphere();` inside the per-mesh `scene.traverse` of `_enableShadowsOnScene`",
        "docs/04-reference/APPLICATION-PERFORMANCE-LEDGER.md §4.2 — 'Creating N walls therefore performs N(N-1)/2 full deep clones' with the measured 1.36-7.63 us/clone (plain) and the projection table starting at N=500 -> 0.19 s; at N=56 that is 1540 clones",
        "FOUNDER LOG — `PERF-EDGEPROJECTOR-CHUNK ... work=109ms yieldCalendar~83ms` and `PERF-CACHE-STATS ... cacheHits=0 cacheMisses=63 hitRate=0% cacheEntries=63/5000` — a first projection on a cold cache, which is what open looks like",
        "FOUNDER LOG — `temporalGraph 2366 mutations / 0 edges` and `~1.4 MB (1,465,135 chars) compressed, JOURNAL-SIDECAR: 2366 journal record(s) shared across 6 version(s)` — the largest single payload in the line, and outside this dimension"
      ],
      "estimatedCostMs": -1,
      "costBasis": "unknown",
      "fix": "Do not spend the open-time budget on mesh count on the strength of this dimension alone. Spend it in this order: (1) instrument the swap (finding 1) — it is the only mesh-driven open cost that is KNOWN to have fired on his run; (2) read the `§LOAD-SHADOW-PASS-DEFERRED ... in Xms` line from his console, which the code already prints and which puts a number on the one remaining O(meshes) load pass; (3) reconcile against the persistence dimension's number for the 2366-mutation temporal graph before concluding anything about which half of the open is bigger. Reducing mesh count (findings 2-4) is worth doing for FRAME time and for the swap arm, and should be justified on those grounds — not on open time.",
      "whatItWouldBreak": "none known — this finding recommends measurement order, not a code change. The one thing it forbids is quoting mesh count as the cause of slow opening without the swap number, which is precisely the 'confident-but-wrong diagnosis' shape this repo keeps recording.",
      "risk": "low",
      "confidence": "high",
      "filesToChange": []
    }
  ]
}