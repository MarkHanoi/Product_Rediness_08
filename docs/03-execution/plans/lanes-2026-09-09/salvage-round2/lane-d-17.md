{
  "refuted": false,
  "reason": "MECHANISM CONFIRMED LINE-BY-LINE; HEADLINE SAVING OVER-CLAIMED.\n\nWHAT I VERIFIED (every load-bearing line says what is claimed):\n\n(a) TIMING. initScene.ts:3038 is verbatim `_runConsolidatedTierPbrPass = () => runTierPbrPass('post-load');`. It is invoked ONLY from a second `pryzm-project-loaded` listener at :3761-3766 inside `setTimeout(() => { _runConsolidatedTierPbrPass?.(); })`. Inside `runTierPbrPass`, initScene.ts:3008 is verbatim `maybeAutoSwitchToWebGLForHeavyScene(scene, `tier:${reason}`, meshCount);`, with `meshCount` produced by a full `scene.traverse` ~25 lines above. The guard's trigger IS the load-complete event, and the founder's log reads `reason=tier:post-load` — mint site and observed string match exactly. Thresholds confirmed: heavySceneSwapThreshold.ts:40/43 = 400/1000; autoWebGLHeavyScene.ts:109-111 ORs the arms. 550 elems / 3696 meshes trips both.\n\n(b) WEBGPU-ONLY WORK. RenderPipelineManager.ts:762 `this._webGpuActive = true;` sits AFTER the `if (!isWebGPU) { … this._phase='phase2'; this._emitState(); return; }` at :715-760, so the flag is true ONLY on native WebGPU. All four consumers are guarded exactly as claimed: :1957 `scheduleShadowRebuild(): void { if (!this._webGpuActive) return;`, :4911-4912 `_rebuildPipeline(): Promise<void> { if (!this._webGpuActive) return Promise.resolve();`, :3280 `onProjectLoaded(): void { if (!this._webGpuActive) return;`, and bind()'s `_loadTSL()` + `_buildPipeline()` at :781-783. `SHADOW_REBUILD_COMPLETE` is logged at :2035 around `Promise.resolve(this._rebuildPipeline())` — structurally unreachable on a classic renderer. The swap does call `rpm.dispose()` first (initScene.ts:4973), and dispose() zeroes `_submitPauseDepth` (:3363) and nulls the pipeline refs. Built, then deleted — confirmed.\n\n(c) MAGNITUDE IS CORROBORATED INDEPENDENTLY, which is what defeats the \"a 50 ms cause cannot explain 17 s\" test. ADR-0267 §Fix-4:214 records a MEASURED `totalSuppressedMs=11481ms — WebGPU PSO compile LONGTASK` caused by EXACTLY this mechanism (the reactive guard being \"a beat too late\") on a ~1,300-element / ~1,645-mesh scene, and RenderPipelineManager.ts:1711/4929 twice record a \"~6 s rebuild\". At 3,762 meshes with per-element unique materials, a ~16 s WebGPU pipeline cost sits inside the range the repo has already measured for the same code path. ADR-0267's own remedy for the generation path was \"decide before geometry exists\" — precisely what this claim proposes for the open path. C04 §1.4a is quoted verbatim and correctly, including \"NOT DONE, and honestly so\"; its named blocker genuinely does not bind here because 550 ≥ 400 trips the ELEMENT arm, and `elementCount: number` really is a plain scalar at PlatformShellTypes.ts:51.\n\nWHERE THE CLAIM IS WRONG OR UNPROVEN:\n\n1. THE 16,011 ms IS NOT A SEPARABLE SAVING — it is the same window as the 15,628 ms gap, measured twice. `open:first-interactive-frame` is minted from `getFrameScheduler().scheduleOnce('perf104-first-interactive-frame', …)` armed at the SAME instant as `open:snapshot-loaded` (PlatformShell.ts:199-206). `_beginShadowRebuildGuard` (RPM:1738-1741) only increments `_submitPauseDepth` / sets `_shadowRebuildPaused` — it does NOT stop the frame loop. So a 15,628 ms delay to that scheduleOnce callback means ~15.6 s of BLOCKED MAIN THREAD, and any async promise spanning that window reports ≈16 s regardless of how much was its own work. `SHADOW_REBUILD_COMPLETE elapsed=16011.5ms` is equally consistent with \"the rebuild waited on a thread blocked by the 881-event flush + 5× REDETECT_ROOMS + 4× TopologyLayer adjacency rebuild\". The claim flags this caveat and then sets CLAIMED SAVING = 16011 ms anyway. That is an upper bound, not a saving.\n\n2. PART OF THE COST SURVIVES THE FIX. The claim's own (c) says the classic renderer must compile a WebGLProgram per material and upload every geometry for 3,762 non-instanced meshes, and that this lands in the 15,628 ms gap. Booting classic does not remove that — it removes only the WebGPU half and the swap mechanics.\n\n3. A THIRD FIRING SITE IS UNACCOUNTED FOR. The claim says \"the two OTHER firing sites\" but analyses only the proactive one. `initBatchLifecycle.ts:121` calls `maybeAutoSwitchToWebGLForHeavyScene(world?.scene?.three, 'gpu-compile-start')` from `batchCoordinator.setGpuCompileStartCallback`; autoWebGLHeavyScene.ts's own header names \"the two call-sites (batch GPU-compile-start + per-add tier pass)\". It passes no mesh count, so it decides on the element arm alone, and at 550 elements it would have tripped. The observed `reason=tier:post-load` is empirical proof it did not fire on this open, so the conclusion survives — but it was asserted, not established, and a pre-decision fix must reason about that seam.\n\n4. LINE-NUMBER DRIFT. The Phase-5 abort `const isWebGPUCapable = rendererResult.backend !== 'webgl-only';` is at initScene.ts:2290, not 2411, and the \"intentionally LEFT AS-IS (zero boot-behavior change)\" comment is at ~2278-2288, not 2404-2409. The substance is right (the abort is real and WOULD-BREAK #1 is the genuine hazard), but the citations are ~120 lines off.\n\n5. The single largest measured item — HYDRATE = 17,163.5 ms, 99.4% of the load — is untouched by this cause. The claim is honest about this, but it means the fix addresses at most the second half of a 37 s open.\n\nVERDICT: not refuted. The structural claim (\"one guard on the open path, and it is the late one\") is true by construction and matched by the log string; the WebGPU-only gating is real; the defect is already written down as C04 §1.4a and its stated blocker does not apply to this run; and the magnitude is corroborated by an independent in-repo measurement of the identical mechanism. What is NOT established from code is the 16,011 ms figure as a recoverable saving.",
  "betterHypothesis": "Same root cause, corrected accounting. The late backend decision causes the editor to pay a WebGPU pipeline family (bind → _loadTSL + _buildPipeline; onProjectLoaded → activateOutlines; scheduleShadowRebuild → _rebuildPipeline) that is then deleted by rpm.dispose() at the swap, AND a second renderer's first-frame WebGLProgram compile + geometry upload for 3,762 unique-material meshes. Only the FIRST is recoverable by pre-deciding the backend.\n\nDo not claim 16,011 ms. That figure is a wall-clock promise span over the same ~15.6 s blocked-main-thread window that `open:snapshot-loaded → open:first-interactive-frame` already measures (that mark is a `scheduleOnce` armed at snapshot-loaded, and the shadow guard pauses submits without stopping the loop — so the gap is blocked main thread, and the promise span cannot be shorter than it). It demonstrably contains non-renderer work: the 881-event endBatch flush, 5× REDETECT_ROOMS, 4× TopologyLayer adjacency rebuild over 528 elements / 2,835 edges, and the tier pass's own traverses. State the recoverable saving as UNQUANTIFIED-BUT-BOUNDED: > 0 and ≤ 15.6 s, with the repo's nearest measurement of the same mechanism (ADR-0267 §Fix-4: 11,481 ms PSO-compile LONGTASK at ~1,645 meshes) as the order-of-magnitude anchor.\n\nBefore implementing, run the cheap experiment that separates the halves: add three marks in the post-load window (tier-pass-done, swap-start, swap-done) so the 15.6 s is attributed rather than assigned — a single span covering four subsystems is exactly the unattributable-interval defect startupBudget.ts was written to close. Then extend step 2 of the fix to the `gpu-compile-start` seam at initBatchLifecycle.ts:121, and target the Phase-5 abort at initScene.ts:2290 (not 2411)."
}