{
  "survivingTitles": [
    "serialize() unconditionally computes a 1.46 MB canonical-string integrity checksum — including for the two dirty-check-only callers that throw it away",
    "The FIRST save of every session deflates ~1.5 MB on the main thread, because the compression worker is lazily constructed and isReady() is guaranteed false on its first call",
    "Two thumbnail captures per open, from two distinct call sites — each forces a synchronous full render of the 3762-mesh scene, a GPU readback, and up to six WebP encodes",
    "The journal-retention valve cannot fire for this project — it needs the 20-slot version ring to EVICT, and the founder has 6 versions",
    "\"temporalGraph 2366 mutations / 0 edges\" is EXPECTED, not a bug — recordEdge has zero production callers — but the journal is still ~32% of every snapshot",
    "DE-RANKED: \"6 versions in one write\" is real for I/O but is NOT where the time goes — re-compression is already incremental, and the whole-container JSON legs measure 8 ms"
  ],
  "refutedTitles": [
    "temporalGraph recording RESUMES before the post-load rebuild sweep — so every open permanently grows the journal AND is why an Auto-save is minted on every open"
  ],
  "refutations": [
    "REFUTED — #2 (temporalGraph resumeRecording). The POSITION claim is correct and I verified it: `resumeRecording()` really is at ProjectLoader.ts:2902 in the `finally`, right after `storeEventBus.endBatch()` and ~130 lines BEFORE `wallRebuildControl.resumeAndFlush()`, `topologyObserver.resume()` and the redetect dispatch. But both consequences hung on that position fail, and the fix would break a live guarantee. (a) CAUSAL HALF IS FALSE, falsified by one line the audit never opened: ProjectSerializer.ts:1786 writes `timestamp: Date.now()` into the snapshot, `CHECKSUM_EXCLUDED_TOP_KEYS` (SnapshotIntegrity.ts:190) holds exactly `['integrity','versionLabel']`, and `ProjectSerializer.stringify` (:2093) is a bare `JSON.stringify`. So `getHash()` returns a fresh millisecond every call, `currentHash === this.lastHash` (SaveOrchestrator.ts:383) is unsatisfiable, and 'Content hash unchanged — save skipped' is dead code. The Auto-save on the founder's open is minted because the hash check cannot suppress ANY save — not because the sweep moved the hash. The fix's advertised second-order benefit ('the whole post-load autosave stops firing at all') is unreachable by this change. (b) GROWTH HALF IS UNDEMONSTRATED and the code argues against it. TemporalGraph only mints on a StoreEventBus create/update/delete (TemporalGraph.ts:94-106), and no post-resume store write is shown. `_flushRestore` (WallRebuildCoordinator.ts:670-705) only READS the wall store and builds render geometry. The deferred whole-level resolve is explicitly render-only — the same file states the join 'is a RENDER-TIME footprint operation … and must NEVER mutate/persist another wall's stored baseline' (§FIX-WALL-JOIN-BASELINE-IMMUTABLE), and §FIX-WALLFLUSH-NOPROGRESS-GUARD is described as signing over 'STORE geometry, which a join provably never changes'. §LOAD-REDETECT-FREEZE skips redetect for every level carrying persisted rooms, so for the founder's 7-level project with `snapshot.rooms` the redetect queue is plausibly empty. 2366 mutations is consistent with authoring; it is not evidence of per-open growth. (c) THE FIX BREAKS A GUARANTEE HARDER THAN ADMITTED. `__closeAutosaveSuppress` is NOT a machine-owned window: ProjectLoader.ts:3040-3077 hands the close to the chunked drain, one level per frame via `scheduleProgress(..., 'post-render')`, chosen precisely so 'the load() finally block returns (and the scene paints) before any redetect runs — the viewport is never blocked synchronously at open', plus one more frame after the last level. The user is interactive throughout. Real edits made there would be permanently absent from the append-only design journal — silent data loss in the one subsystem whose whole contract is history — traded for a benefit (a) shows does not exist."
  ],
  "missed": [
    "THE ACTUAL CAUSE OF THE AUTO-SAVE-ON-OPEN, and the largest recoverable item on this dimension: the dirty-check hash is TIME-VARYING. ProjectSerializer.ts:1786 writes `timestamp: Date.now()` as a top-level snapshot key; CHECKSUM_EXCLUDED_TOP_KEYS (SnapshotIntegrity.ts:190) excludes only `integrity` and `versionLabel`; ProjectSerializer.stringify (:2093) is a plain JSON.stringify. So SaveOrchestrator.getHash() returns a different string on every call for byte-identical model state, the equality test at SaveOrchestrator.ts:383 is unsatisfiable, and 'Content hash unchanged — save skipped' has never fired in production. Consequence: every debounce tick that survives the dirty flag performs a full serialize + 1.46 MB checksum + stringify + deflate + whole-container rewrite + thumbnail + server POST, including the post-load one on an open where nothing changed. No test covers the branch (the only `lastHash` reference outside SaveOrchestrator.ts is an unrelated LRU test). Adding 'timestamp' to the excluded set is NOT sufficient — the raw stringify still carries it — so the hash must be taken over a timestamp-stripped projection, or `timestamp` must move under `integrity`. This also subsumes finding 1's sub-claim about cost 'on every autosave cycle whose hash turns out UNCHANGED': there are no such cycles.",
    "A THIRD full 1.46 MB canonical digest on the open path, on the READ side, entirely absent from the audit: ProjectLoader.ts:427 calls `verifySnapshotChecksum(snapshot)` synchronously at the top of load(), before any hydration. It short-circuits only on absent checksum, algorithm drift, in-flight migration, or a NON-EXACT journal re-attach; the founder's container re-attaches exactly (6 versions, rel=0, nothing released), so it falls through to `digest(snapshot)` at SnapshotIntegrity.ts:367 — the same canonicalStringify + FNV walk measured at 45.3 ms / 956,859 chars, i.e. ~69 ms at his size. The open pays THREE full canonical walks (verify at load, resetDirtyAfterLoad, executeSave), not two. This is the only one of the three actually doing its job, so it must not be removed — but it belongs in the ranking, and it is the strongest argument for making the walk itself cheaper rather than deleting instances of it.",
    "The audit's own journal-cost number is ~5x below the repo's measurement of the same leg. Finding 5 estimates '~14 ms per open at his 2366'. The §PERF104 comment in PlatformShell.ts, immediately above `versionRepository.getLatestVersion(id)`, records from the same spec: 'Measured at founder scale (perf104OpenPath.spec.ts): 89 ms cold, of which 80 ms is journal re-attach.' The journal READ leg on open is ~80 ms, not ~14 ms — which also gives finding 6's 'detach the journal at serialize' a much larger prize on the read side than the audit credits.",
    "`TemporalGraph.sessionId` is `crypto.randomUUID()` per page load (TemporalGraph.ts:80); `deserialize()` restores edges and mutations but never sessionId (:388-412), while `serialize()` emits it (:372-376). So the serialised temporalGraph differs on every session, guaranteeing the first save of any session writes content differing from the stored version even with zero edits — a second, independent reason content-equality dedup can never work, stacked under the `timestamp` one.",
    "Cheap size win inside finding 6's 'size half' that the audit does not name: every NodeMutationRecord carries a full 36-char sessionId UUID plus the constant strings `mutatedBy:'system'` and `commandId:'system'`, both hard-coded by the StoreEventBus subscriber (TemporalGraph.ts:99-104, minted at :429-441). At 2366 records that is ~85 KB of repeated UUID and ~30 KB of repeated literals inside the ~462 KB journal, carried through the checksum walk, the stringify, the deflate and the server POST on every save. Interning the session id at the envelope level (the serialized form already has a top-level `sessionId`) is a format change with no contract exposure, unlike the C05 detach-at-serialize proposal.",
    "Corrections that do not sink the surviving findings but change their ranking: (1) FINDING 1's 138 ms is ~2x the recoverable saving. Two serializes run on open, but the second one's snapshot IS the persisted record via the §PERF-AUTOSAVE-SINGLE-SERIALIZE stash (PlatformSaveController.ts:245-275), so its checksum must still be stamped. Only the resetDirtyAfterLoad walk is truly discardable: ~69 ms, not 138 ms. The fix is otherwise safe — I checked that `versionLabel` is already checksum-excluded, so lazily stamping after `snapshot.versionLabel = label` cannot move the digest. (2) FINDING 4's cost attribution is wrong even though the two call sites are real. The readback is not full-frame — `drawImage` downscales to at most 400x225 (initPersistence.ts:191-216) and `getImageData` reads a 32x32 centre patch only; and `fitThumbnailToBudget` is first-fit, so a 400x225 WebP at q=0.72 clears the 65,536-char budget on rung ONE, not six. Essentially all of the claimed 250 ms is the two forced `rpm.render()` calls. Both captures also land at ~2.5 s and ~3.5 s AFTER the overlay is removed, so this is post-interactive jank, not open latency — it should not be ranked against the load critical path. (3) FINDING 5's fix option (2) is self-defeating. A hard record ceiling that keeps `_releaseJournalHead`'s floor cannot release anything for this project: `releasable = min(rel, minRef, n)` and `minRef` is the SMALLEST cursor across RETAINED entries (ProjectRepository.ts:1885-1888). With 6 versions and no eviction, the oldest retained version still references the journal head, so minRef pins releasable near 0 no matter what a new trigger sets `rel` to. The proposed second trigger is a no-op unless versions are also dropped."
  ]
}