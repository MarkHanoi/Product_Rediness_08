{
  "survivingTitles": [
    "Section poché can never render: PocheFillBuilder discards world Y, so a vertical cut ring collapses to a 2-vertex degenerate and is dropped",
    "The test that should have caught the section/elevation poché collapse uses a HORIZONTAL fixture labelled as an elevation cut band",
    "Symbol coverage is 15 for plan, 3 for elevation, 1 for section — and lighting has no injector for any view",
    "The LIVE elevation/section driver (PlanViewManager, DOC-19B) never injects level datums or grid lines — all four call sites that do are on paths this route does not use",
    "Clicking an elevation or a section in Views & Sheets skips the GIS exit — the L-13253 'covered by Cesium' bug the founder reported is fixed for plans and 3D only",
    "Two rival elevation implementations share the name 'Front': the ViewCube preset is a PERSPECTIVE 3D camera with plan symbols explicitly disabled, and it is what runs whenever no ViewDefinition id is queued",
    "Auto-dimension covers plan and elevation and honestly refuses section — this one is NOT a defect and should be left alone"
  ],
  "refutedTitles": [
    "Selecting a section or elevation in the plan pane's view dropdown never triggers a projection — it renders whatever happens to be cached, which for a never-opened section is nothing"
  ],
  "refutations": [
    "REFUTED — 'Selecting a section or elevation in the plan pane's view dropdown never triggers a projection'. The warm-up the auditor says does not exist is already wired, and the auditor quoted the exact line that triggers it. `SplitViewManager._onViewSelectChange` emits `split-view-view-changed` at apps/editor/src/engine/views/SplitViewManager.ts:1253 — the auditor cites this emit in their own evidence ('emits split-view-view-changed') and then treats the branch as terminal. `PlanViewManager` subscribes to that event at :185 and handles it at apps/editor/src/engine/views/PlanViewManager.ts:1165-1175: `const viewId = payload.viewId ?? null; ... if (!viewId || viewTechnicalDrawingCache.get(viewId)) return; const viewDef = viewDefinitionStore.get(viewId); if (!viewDef) return; if (viewDef.viewType === 'section' || viewDef.viewType === 'elevation') { this._ensureProjectionForSplitView(viewDef); }`. That is literally 'on selecting a view id whose viewTechnicalDrawingCache.get(id) misses, call PlanViewManager._ensureProjectionForSplitView(def)' — the auditor's own proposed fix, already built, and gated on exactly section|elevation. The auditor found the OTHER mitigation (`_onViewUpdated` → :950, on `vd:view-updated`) and stopped one handler short, then generalised from it. The 'already-solved' lens is decisive here: this finding proposes rebuilding a wired path. RESIDUAL, and it is a different claim: the listener is registered in `activate()` and disposed in `deactivate()`, so the warm-up is absent when PlanViewManager is not active (main pane in 3D/schedule/sheet with the SVP showing a section). That is a narrow conditional gap, not 'never triggers a projection', and it does not support the finding as written. Also note the placeholder claim inside the fix is wrong: PlanViewCanvas.ts:396-398 already renders `isPlanLike ? 'Add walls to see the floor plan' : 'Generating view…'`, so a section never shows the 'Add walls' text the auditor calls actively misleading."
  ],
  "missed": [
    "⭐ A PARTIAL, UNCOMMITTED FIX FOR FINDING 1 IS ALREADY IN THE WORKING TREE, AND IT CHANGES NOTHING AT RUNTIME. `git status` shows `M packages/core-app-model/src/views/PocheFillBuilder.ts` plus a new untracked `packages/core-app-model/src/views/sectionPocheHasAPlane.test.ts`, both stamped §SECTION-POCHE-HAS-A-PLANE (L-13269, dated 2026-09-09). A concurrent lane has added `export type PochePlane = 'xz' | 'xy' | 'zy'`, a `PLANE_OFFSETS` table, and a `plane` parameter on `fromGeometry`/`fromRawBuffer`. ⛔ BUT NOT ONE PRODUCTION CALLER PASSES IT. Every call site still lands on the `= 'xz'` default: PlanViewCanvas.ts:2685, PlanViewFillRenderer.ts:140, CutSectionExtractor.ts:62, DxfExportService.ts:344, SVGCompositeRenderer.ts:170. Section poché still paints nothing on the working tree exactly as it does at HEAD. Finding 1 survives against BOTH, but a lane picking it up will collide with in-flight edits it does not expect, and must not read the new type as 'done'.",
    "⭐ THE POCHÉ REDUCTION HAS SIX CONSUMERS, NOT TWO — and three of them are EXPORT. The auditor's fix names only `PlanViewCanvas.ts:2728` and `PlanViewFillRenderer.ts:262`. Measured `grep -rn 'PocheFillBuilder\\.\\(fromGeometry\\|fromRawBuffer\\)'`: `packages/core-app-model/src/drawing/CutSectionExtractor.ts:62`, `packages/file-format/src/export/sheets/DxfExportService.ts:344` and `packages/file-format/src/export/sheets/SVGCompositeRenderer.ts:170` all hard-code the same plan reduction. So section poché is dead in DXF, SVG and PDF export as well as on canvas — a sheet-set deliverable, not just a screen. Fixing the two canvas sites leaves the export half broken and looking fixed, which is the shape this repo keeps paying for. `PocheFillBuilder`'s own `points` string is documented as 'x,z pairs in world metres', so every downstream string parser inherits the plan assumption too.",
    "⭐ FINDING 5's GAP IS 8 OF 12 VIEW TYPES, NOT 2 — AND THE ORCHESTRATOR ALREADY ACCEPTS THE ELEVATION MODES, WHICH CHANGES THE FIX. `VIEW_TYPE_TO_OBC` (ViewsRailPanel.ts:170-183) has twelve keys; `targetBimMode` resolves non-null for only four ('plan','ceiling-plan','structural-plan' → 'Top'; '3d' → '3D'). So `detail`, `drafting`, `legend`, `analysis`, `render` and `walkthrough` skip the GIS exit alongside elevation and section — and `analysis`/`render`/`walkthrough` map to OBC `'3D'` while still falling to null, which is plainly an oversight rather than a route decision. SEPARATELY, the auditor's premise that the orchestrator only takes 'Top'|'3D' is FALSE: they read a narrowed LOCAL re-declaration at PaneViewPicker.ts:433. The real signatures are `apps/editor/src/types/globals.d.ts:278` — `pryzmActivateBimView?: (mode?: 'Top' | '3D' | 'Front' | 'Back' | 'Left' | 'Right')` — and `GISAreaLayout.ts:1639` `activateView = async (mode: '3D' | 'Top' | 'Front' | 'Back' | 'Left' | 'Right')`. ⚠ AND 'Top' IS THE RISKIER CHOICE, for a reason the auditor did not measure: `activateView` calls `props._viewController.activate(mode)` (GISAreaLayout.ts:1670) BEFORE ViewsRailPanel runs `setActiveViewDefinitionId(viewId)` at :831, so `activate('Top')` sees a STALE-or-absent `_activeDefinitionId` and runs `_activateFloorPlanView` first, then the rail re-activates through the DOC-19B path. Two activations per click, the first one wrong. Verify the fix by the `GIS: Deactivating geospatial view` log line as the auditor says, but consider the Front/Back/Left/Right modes the choke point already declares.",
    "⭐ THE LIGHTING HALF OF FINDING 3 IS HALF-STALE AND ITS RANKED FIX WOULD MAKE THE PLAN WORSE. Commit 584aaab2 landed TODAY (2026-09-09 07:14) — `fix(docs/§LIGHTING-IS-A-CLASSIFIED-LAYER L-13267)` — and it establishes two things the audit misreads. (a) THE FOUNDER'S 'dense circular hatch-like symbols … fine black concentric line bursts' ARE NOT SYMBOLS: they are the real 672-face luminaire meshes, edge-projected — his own log prints `DIAG-EPS-01 edgesGeo elemType=Lighting faceCount=672` repeatedly. So the audit's 'a light fitting has no representation in elevation or section' is wrong on the letter — the SYMBOL is absent; the raw wireframe is present in every view. Confirmed: no `skipInPlan` tag exists anywhere for lighting, so in a plan the mesh blob AND the canvas symbol BOTH draw. (b) THE READABILITY ROOT WAS THE PEN, AND IT IS ALREADY FIXED: `PenWeightTable` always carried `lighting: pen(0.13,'#303030')` but nothing could reach it, so luminaire linework drew at the `__default__` 0.25 mm WALL pen. That commit added `Lighting`/`LightingFixture`/`light` → `A-LGHT` (EdgeProjectorService.ts:260), `'A-LGHT': 'lighting'` (DrawingLayerIdentity.ts:110), the `isLighting` pen flag, and the `lighting` row in `VisibilityIntentDefaults.ELEMENT_TYPES`. ⛔ Its closing paragraph rules directly against the audit's ranked item 4: 'it does not add a lighting SYMBOL. The geometry is NOT suppressed when a family gets a symbol, so adding one today would draw the symbol ON TOP of the blob and make it worse. Symbol + suppression is the next increment.' The audit's 'already exists: suppressSymbolisedElementLinework solves the double-draw' is true but only for the ELEVATION opening path (EdgeProjectorService.ts:4176, keyed on `coveredElementIds`); every plan-family injector suppresses via a `userData.skipInPlan` tag set at BUILD time, which lighting does not have. The lighting injector is therefore a two-part job, and shipping half of it regresses the exact drawing the founder complained about.",
    "THE FINDING-7 ESCALATION I EXPECTED IS CLOSED, AND THAT MATTERS FOR THE RANKING. I checked whether a queued elevation definition could hijack a ViewCube face (making Front/Back/Left/Right all re-open one elevation): it cannot. `ViewController.activate()` nulls `this._activeDefinitionId` in its `finally` block at ViewController.ts:1624, so the cube always reaches `_activateElevationView` with no definition and therefore never projects (the projection block at :2274 is guarded `if (this._edgeProjectorService && this._activeDefinitionId)`). The two 'Front's stay cleanly separated rather than interfering, which supports the auditor's decision to propose a DIAGNOSTIC label change and no code fix — this is genuinely a founder ruling, not a bug to close.",
    "MINOR CITATION DRIFT, none of it load-bearing: `isSectionDepthView` is at EdgeProjectorService.ts:2345, not :2317 (the auditor's preamble); `DefaultViewsManager.ts` lives at `packages/core-app-model/src/views/`, not under `apps/editor`; and the elevation auto-dimension engine is imported by the editor as `./applyElevationAutoDimensions.js`, with the package copy at `packages/auto-dimension/src/elevation/planElevationAutoDimensions.ts` — both exist, so the claim holds, but the two are separate files and a lane should confirm which one it is editing."
  ]
}