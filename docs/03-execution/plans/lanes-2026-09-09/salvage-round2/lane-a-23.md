{
  "survivingTitles": [
    "FIVE hand-typed family vocabularies must agree; at least three ids are dead by construction, and nothing checks",
    "Rooms are painted outside the drawing pipeline entirely, and their category lives in the store the Visibility Intent panel does not read",
    "Lighting is drawn TWICE in plan: an ungoverned blue canvas overlay on top of the full 672-face luminaire mesh",
    "Symbol-injected lines carry no `userData.elementUUID`, so a per-ELEMENT visibility override can never reach them — and in plan, furniture IS only its symbol",
    "`VGSceneApplicator.applyToProjectionLayers()` is a DEAD WRITE for the on-screen 2D drawing, and it covers only half the sub-layers",
    "The incremental graft path is unavailable for every gesture a documentation pass actually makes",
    "C102 clauses this pipeline does not satisfy today",
    "THE ONE STRUCTURAL CHANGE — one per-view ViewGraphicsResolver, landing on the SymbolInjectionGate seam"
  ],
  "refutedTitles": [
    "Every view in a fresh project is governed by a READ-ONLY system intent, and the intent editor silently wires no write handlers",
    "Occlusion runs last, over-claims on oblique solids, and deletes rather than demotes — 17 of 22 projection occluders degraded on the founder's own plan"
  ],
  "refutations": [
    "FINDING 1 — REFUTED on its two load-bearing words, 'silently' and 'no-op', and its fix rebuilds machinery that shipped. The three mechanical facts check out: SystemIntents.ts:48 `isSystem: true` inside `makeIntent`; :350 default = architecturalDocumentation; VisibilityIntentPanel.ts:772 `if (intent.isSystem) return;` ahead of save-meta/[data-appearance]/sliders/mass-apply. But the conclusion does not follow. :264 calls `this.renderAppearanceForm(appearance, intent.isSystem, ...)` and the `disabled` parameter is stamped on EVERY write control — the visible checkbox (:327), the weight slider and number (:332/:339), line colour/opacity/style (:343/:345/:347), fill style/colour/opacity (:352/:358/:362), symbolicRule (:365), the whole 3D-surface block (:457-:487), the mass-edit toolbar (:497), and the name/description inputs (:199/:200). A disabled input does not 'appear, accept a click, and change nothing' — the browser refuses the interaction. Above them, :205-211 renders a purple banner: '\"X\" is a system intent and is read-only. Every field below is disabled. Press Duplicate above to make an editable copy — the copy is bound to this view automatically.' That IS C102 V-RE-1's compliant state, not a breach of it, so proposed fix (b) is already shipped. Proposed fix (a) is worse: it rebuilds L-779. The auditor quotes the L-779 comment ('the copy governed NOTHING') as the CURRENT state, but that comment describes the PRE-FIX state and the fix sits 850 lines below it — duplicateIntent (:923-963) executes CreateVisibilityIntentCommand and then, when `contextViewId` is set, `new AssignViewIntentCommand({ viewId: this.contextViewId, intentId: intent.id })` and logs any refusal. And `contextViewId` IS set on the founder's route: ViewPropertiesPanelBuilders.ts:196 calls `open(instance?.intentId ?? intents[0]?.id, def.id)`. Finally the 'panel is half-live' sub-claim is wrong twice over — the shift-click handlers at :769 and :783 are themselves guarded by `&& !intent.isSystem`; what survives above the return is plain element-type/state SELECTION, which is navigation and does change the panel. Residual truth worth keeping: a system intent cannot be edited in place, and 'Duplicate' is one extra gesture. That is a deliberate design, not a silent dead end, and it is not a plausible root for 'I change the line type and it doesn't work' — 9babe6f5's own commit message attributes that to the two output controls (occlusionDisposition / beyondLineStyle) that had NO UI at all.",
    "FINDING 8 — REFUTED as a finding; it is a diagnostic request with a title that asserts more than the auditor establishes, and the auditor says so inside their own text ('I have NOT established how much of the 239.5 was wrongly removed', confidence low). Checked against the code, the title's claim is actively argued against by the artefact: HiddenLineRemoval.ts:438-448 states the vertical-span hull is 'exact for any vertically simple silhouette, and it still respects an L-notch', and the SCOPE note immediately below records that degrading `:cut` bands — the arm where over-claiming genuinely deletes — was DELIBERATELY not taken because there is 'no evidence behind it'. The 17/22 count is printed on every pass by design (Contract 23 §9 forbids a silent cap) and the file's own :695-700 says in terms that a rising number is not a defect. So the counts confirm the LADDER FIRED, not that it fired WRONGLY, and nothing in the code establishes a single wrongly-removed segment. The one structural claim attached to it — 'resolveOcclusionDisposition had no UI, so nobody could ever choose show dashed' — was already stale when written: 9babe6f5 shipped that control, which is why the finding's own fix is 'ask the founder to flip it and screenshot'. Under default-to-refute this is not a defect finding. Keep the A/B as a task; do not carry it as a projection-architecture defect."
  ],
  "missed": [
    "A SIXTH family vocabulary, and it already claims the throne finding 2 wants to build. `packages/core-app-model/src/presentation/ElementTypeRegistry.ts` declares itself, in its own header, 'Contract 25a §3.1 — ELEMENT_TYPE_REGISTRY source of truth for defaults' and 'Adding new element type requires one entry here only'. It holds 13 `_makeRules` entries including `curtain-wall` and `structural-wall`, which appear in NO other list — and it is effectively dead: the only importers repo-wide are `BimGridRenderer.ts:18` (via `getElementTypeRules`) and the barrel re-export at `presentation/index.ts:100`. The panel does not read it; `VisibilityIntentDefaults.ELEMENT_TYPES` (a separate 19-entry list in the same directory) is what `cloneDefaultElementGraphicsRules()` and the panel's `Object.keys(intent.elementRules)` actually use. Finding 2's fix ('derive one FAMILY_REGISTRY in packages/core-app-model/src/drawing/') would therefore mint a SEVENTH list beside a dead sixth that already says it is the one. Reconcile or retire ElementTypeRegistry in the same change, and make the proposed set-vs-set gate cover six lists, not five.",
    "A SECOND full painter inside PlanViewCanvas with a divergent pen composition — and it has ZERO callers. `renderFromPipelineResult()` (PlanViewCanvas.ts:1915-2092) is a complete parallel paint path: its own `resolveCanvasRenderScale`/`minStrokePx`/`dashScale`, its own room-fill and lighting-symbol calls (:1959, :2092), and at :2052 `ctx.lineWidth = Math.max(hairline, edge.widthMm * SCREEN_PX_PER_MM)` — WITHOUT the `_vgFactor` the live path composes at :520-524, and without `graphicsRulesEngine.resolveStyle` at all. Its only governor is the VG `resolved.visible` skip at :2025-2029; the intent tier, the per-view and per-element overrides and the L-285 function axis never reach it. Its own comment at :2043-2047 admits this ('the worker resolves pens through its OWN mini rules engine rather than graphicsRulesEngine.resolveStyle() ... Recorded, not smuggled into this ticket'). `grep -rn renderFromPipelineResult` outside the file returns NOTHING, so it is dead today — which is why I am reporting it as a trap rather than a live defect, and why finding 10's census of 'six painters' is wrong in both directions. The day anyone switches the worker path on, every intent-based category hide and every per-element override silently stops working on the plan canvas. Finding 10's repointing list must either delete this method or convert it first.",
    "Lighting has no symbol in ELEVATION or SECTION either, and no suppression flag in any view — finding 4 is plan-only, but the founder's lane says 'PLAN VIEW AND ELEVATION AND SECTIONS'. The convention finding 4 asks lighting to adopt already exists in all three view types: `EdgeProjectorService.ts:2914` gates `skipInPlan` on `isPlanView`, :2922 gates `skipInElevation` on `isElevationView`, and there is a section sibling below it (§TREE135). `ParametricTreeEngine.ts:574-575` stamps BOTH `skipInElevation` and `skipInSection`, and `PlumbingFragmentBuilder.ts:279` stamps `skipInElevation` beside `skipInPlan`. `grep -n 'skipIn' packages/geometry-lighting/src/*.ts` returns nothing at all, and `renderLightingSymbols` is guarded to plan/ceiling-plan/structural-plan only (PlanViewCanvas.ts:2293-2295). So in elevation and section a luminaire is a raw 672-face edge dump with NO symbol over it and no way to suppress it — strictly worse than the plan case, which at least has a symbol. Finding 4's fix should stamp all three flags and the builder should emit an elevation symbol, not just a plan one.",
    "The same gesture gives two different answers in plan and elevation, which is the cheapest way to confirm finding 5 with the founder. Because `skipInPlan` is gated on `isPlanView` alone (:2914), furniture meshes ARE natively projected in elevation and section — and the native path stamps `projected.userData.elementUUID` (:3223). So hiding a bed from the Project Browser (targetKind 'element') reaches `ctx.globalAlpha = 0` in ELEVATION and reaches nothing in PLAN, because in plan the bed is only its symbol and the symbol carries no elementUUID. One screenshot pair settles finding 5's 'RIVAL RULED IN, NOT OUT' without any code change. Two corrections to finding 5's own evidence, both in its favour: there are THREE compliant injectors, not two — `OpeningElevationSymbolBuilder.ts:664` also writes `projected.userData.elementUUID = elementUUID` — and the two the auditor names live in `apps/editor/src/engine/`, not in a geometry package, so a `grep` scoped to `packages/geometry-*` misses all three.",
    "Finding 6 is stronger than stated on one axis and needs a caveat on the other. STRONGER: the visibility write is dead for BOTH consumers, not just the canvas. `SVGCompositeRenderer.setTechnicalDrawing` (packages/file-format/src/export/sheets/SVGCompositeRenderer.ts:232) also uses `(drawing.three as THREE.Group).traverse(...)` and never reads `child.visible` — it decides hidden-ness from the layer NAME (`layerName.toLowerCase().includes('hidden')`) at :240-241. So `drawing.layers.setVisibility(...)` has no reader anywhere in the product. CAVEAT on the export half: the exporter reads `material.linewidth` and only falls back to `_layerLineWeights` when the value is outside 1..6 (:246-251) — and the symbol builders DO stamp a material linewidth in that range (`DoorPlanSymbolBuilder.ts:224` `LW_CUT = 2`, :226 `LW_PROJ = 1`). So the hyphen sub-layers do not 'lose' their stroke width on export; they take a THIRD authority — a literal in the geometry builder — which is why nobody noticed VGSceneApplicator never writes them. Finding 6's recommended fix (keep colour + linewidth, drive them off the derived registry, cover hyphen sub-layers) must therefore also decide what happens to those builder literals, or the export weight will change under people silently.",
    "Line readability — the founder's loudest complaint ('THE QUALITY ATM IS REALLY BAD - THE LINES ARE ALMOST NOT READABLE') — has no finding of its own, and I checked whether it needed one. It does not, on the pen axis: the machinery is present and wired. `PlanViewCanvas.ts:369/410` resolve the backing scale through `resolveCanvasRenderScale` and the floor through `minStrokePx(scale)`, and `CanvasRenderScale.ts:88-96` DERIVES `MIN_LEGIBLE_BACKING_SCALE` as `ceil(1 / (thinnestPenMm x SCREEN_PX_PER_MM))` rather than hard-coding it, so the ladder survives the raster floor on a 1x display (L-288, with `CanvasHairlineFloor.test.ts` pinning 0.50/0.25/0.09 mm at distinct device widths). The `_penPx * _vgFactor` composition at :520-524 is the corrected one. That leaves finding 4's mesh dump as the strongest remaining explanation of the screenshot — hundreds of concentric bursts per fixture, which 584aaab2 made LIGHTER (0.13 mm lighting pen) without making them FEWER, exactly as finding 4 says. Worth stating explicitly in the lane so the readability complaint is not left attached to finding 8, whose confidence is low and whose claim I could not confirm.",
    "Two caveats on finding 10 that its 'already exists' section does not name. (1) The hit test is NOT a separate painter to be collected — `_lineIsDrawn` (:2854-2871) already goes through the SAME `_styleResolver` and the SAME `penCategoryForLayerTag` + `graphicsRulesEngine.resolveStyle` as `render()`; that pair was deliberately collapsed and the header at :2840-2846 argues why. A resolver refactor must preserve that, not re-derive it. (2) `makeSymbolInjectionGate` fails OPEN by construction — `SymbolInjectionGate.ts:114/120/122/145` return `() => true` or `visible = true` on a missing viewId, a null binding or any throw, and the header states this is policy. That is safe for a boolean 'should I inject?', but a resolver that also answers `pen` and `fill` has no equivalent safe default: failing open into 'no pen' would paint at the fallback weight and reintroduce exactly the flat ladder L-288 fixed. Decide the fail-open policy per answer BEFORE generalising the seam, or the invariant the current gate protects is lost in the widening."
  ]
}