{
  "refuted": true,
  "reason": "MECHANISM: verified, every line. runSub (ImportProjectCommand.ts:326-350) really is canExecute+execute per element, not deserialisation. FurnitureStore.ts:51-52 really does fire `_bus.emit('bim-furniture-added')` then `storeEventBus.emit(...)`; DOMEventBus.ts:46-52 really is a bare synchronous `dispatchEvent`, and `beginBatch()` (ProjectLoader.ts:733) really does not reach it. initBuilders.ts:883 -> _safeFurnitureUpdate:843 -> updateFurniture builds THREE inline. IMPORT_CHUNK_SIZE=120 with unconditional yields at 1074/1107/1218 and per-120 counters at 632/747/1077/1221. TopologyLayer.ts:365-366 is lazy. perAddGeometryGate.ts:32 exists, applied at initScene.ts:4121. The brackets at :761/:2876 are exact. Nothing here is invented.\n\nBUT IT FAILS THE LENS, BY ITS OWN ARITHMETIC. The brief accounts for \"under 1.2 s of 17.16 s\" and then says plainly: \"WHAT I COULD NOT ESTABLISH — WHICH listener on the DOM channel owns the ~16 s residual.\" A claim that cannot attribute 93% of the number has identified a LOCATION, not a root cause. The residual is assigned to a disjunction — \"the unbatched fan-out PLUS un-decomposed sub-command bodies\" — and the second disjunct is just \"the import is slow somewhere we did not look.\" CLAIMED SAVING 12000ms basis \"derived-from-log\" is false: nothing in the log attributes 12 s to the DOM channel. If the 16 s lives in sub-command bodies, batching DOMEventBus saves ~0.\n\nSUB-CLAIM (b) IS UNSUPPORTED AND PROBABLY WRONG, and it is load-bearing (it is the whole \"visible freeze\" diagnosis and all of STEP 2). It lists \"walls=56, slabs=10, curtainWalls=90 — ALL below 120\" and SILENTLY OMITS FURNITURE, whose loop at ImportProjectCommand.ts:1077 carries the identical per-120 counter and which is the largest family in this scene (1487 of 3762 meshes). 550 elements minus walls/slabs/CW/levels (163) leaves ~387 unaccounted; furniture plausibly exceeds 120 and yields repeatedly. Decisively: the instrument that settles it — §LOAD-CHUNKED at ProjectLoader.ts:894-901, which prints the yield count AND \"longest synchronous chunk between yields=Xms\" — is printed UNCONDITIONALLY in this exact run and was not read. The brief's own STEP 0 forbids exactly this (\"the repo's whole documented failure mode is confident attribution without the reading\"), then commits it one section earlier.\n\nFAN-OUT BREADTH IS OVERSTATED WHERE IT MATTERS MOST. For furniture — the biggest family — `UnifiedBrowserPanel` does NOT register `bim-furniture-added`; it registers 25 OTHER names with one listener each (:127-158), not 25 listeners per event. And `ToolsRailController.refreshIfActive` early-returns on `if (this._activeId !== id || !this._activeBuilder) return;` — behind the loading overlay it is a no-op. So the fix's headline (\"the 25 UI subscribers run once instead of ~880 times\") is not the shape of the code. The surviving genuine finding is narrower: WallEdgeVisibilityService.ts:106 and initScene.ts:976/1035 queue an UNCOALESCED microtask per event (unlike LightingFragmentBuilder's guarded `_syncQueued`), so those drain N-per-chunk — but they ride wall/slab/floor/ceiling/room events (~66-100 here), and at the log's own 7.7 ms full-scene calibration that is sub-second, not 16 s.\n\nA RIVAL THE BRIEF RULES OUT NOWHERE, and it is mutually exclusive with (b).",
  "betterHypothesis": "HYDRATE IS WAITING, NOT COMPUTING — and the code says so in its own comment. ProjectLoader's §LOAD-YIELD-WAIT block (the comment above the `_yieldWaitMs` counters, ~:855-865) states the mechanism verbatim: \"the frame the loader waits for is drawn by a viewport that is itself rendering the half-built scene: if a frame costs 500 ms, every yield costs 500 ms, and a load with 30 yields pays 15 s for waiting alone with ZERO of it visible in any existing counter.\" The loader awaits `yieldForProgress('project-load-chunk','post-render')` — it is parked on the SAME frame bus that is rendering the growing 3,762-mesh / 3,274-caster scene.\n\nThe log corroborates and the brief mis-reads it. `SHADOW_REBUILD_COMPLETE elapsed=16011.5ms` is NOT \"a further 16.0s\" after load: RenderPipelineManager.ts:2034-2038 times `Promise.resolve(this._rebuildPipeline())` from `__t_shadow_start`, an ASYNC span whose continuation can only land when the main thread is free, and it is held open by `_beginShadowRebuildGuard()`. 16.0 s against a 17.16 s hydrate is not a coincidence — it almost certainly OVERLAPS hydrate. So the arithmetic is not 17 + 16 = 37; it is one ~17 s window in which the loader and a shadow-pipeline rebuild are deadlock-adjacent: the loader yields for a frame, the frame is expensive (and submits are paused mid-rebuild), the rebuild's continuation waits for the loader's synchronous chunk. That also explains why `first-interactive-frame` lands 15.6 s in.\n\nThis is falsifiable against the SAME console with zero code change, and it splits cleanly from the brief's hypothesis: §LOAD-YIELD-WAIT prints `compute=Xms waiting=Yms yields=N avgWait maxWait maxChunk`. If `waiting` dominates, the fan-out and the sub-command bodies are both exonerated and the fix is the yield cadence / decoupling the load yield from the rendering frame bus (yield to a MessageChannel macrotask, or suppress viewport rendering during hydrate) — and STEP 2 of the proposed fix, which ADDS yields on a 12 ms time budget, would make it strictly WORSE by paying more expensive frame waits. If `compute` dominates, §LOAD-IMPORT-STEPS (:920-926, 20 named steps heaviest-first) names the family in ms, and only then is the DOM channel a candidate. Read those two lines plus §LOAD-CHUNKED before touching anything; all three were printed in this run and none are in the excerpt."
}