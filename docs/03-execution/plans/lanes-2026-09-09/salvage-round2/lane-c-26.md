{
  "survivingTitles": [
    "The open has no owner and no end: `await openProject()` resolves BEFORE the project loads, so the whole expensive half is unsequenced, unmeasurable and uncancellable",
    "The renderer backend is destroyed and rebuilt at load-END, on a decision that was knowable at load-START — the whole scene is uploaded to backend A and then re-uploaded to backend B",
    "Every non-new open awaits a full server snapshot fetch that is DISCARDED whenever local history exists — and it is serialized AFTER the engine boot instead of alongside it",
    "The post-load window is a 35-subscription broadcast with three uncoordinated timers — this is the interval §PERF104 explicitly declined to own, and it is where the founder's wait lives",
    "Two independent socket.io connections join the same project room on every open — two WebSocket handshakes and two server-side DB access checks per user per open",
    "26.3 MB of uncompressed JavaScript — including a 5.6 MB render-BLOCKING classic `<script>` — must be parsed before the startup instrument's t0, so it is invisible in every founder paste",
    "P1 and the layer gate both PASS, but the open path routes around the typed runtime through 338 `window.X =` globals — which is the mechanical reason no orchestrator can exist",
    "God objects: 31 files over 2,000 LOC hold 21% of the editor app in 2.4% of its files — a maintenance cost, and (contra the brief's hypothesis) NOT demonstrably an open-latency cost",
    "Opening a project WRITES it back: a read-only open produces a full 503-element serialize, deflate, IndexedDB write, two forced synchronous renders and two thumbnail captures"
  ],
  "refutedTitles": [],
  "refutations": [
    "NONE REFUTED — every mechanism reproduced from source. But five claims need correcting before a lane acts on them, and one of them is a confident-but-wrong cost.",
    "F1 — CORRECTION (not refutation): the mechanism is exact, but the premise 'the only thing that knows the load finished is a broadcast event' is FALSE. `ProjectLoader.ts:651` dispatches `pryzm-load-suppress-end` from `__closeAutosaveSuppress()`, and its own header (`ProjectLoader.ts:629-643`) says the window covers 'the ENTIRE load/restore, INCLUDING the fire-and-forget post-load redetect + wall-resolve sweep that continues AFTER load() resolves'. `SaveOrchestrator.ts:290` and `initScene.ts:4176` already consume it. The boundary the fix wants is COMPUTED TODAY — it is just broadcast instead of returned. That re-prices the fix: `reportStartupBudget('open')` can be armed on that event now, closing the documented hub-open gap without touching a single signature.",
    "F1 — MISSED HAZARD IN THE FIX: `PlatformRouter.ts:1250-1253` sets `platformRoot.style.display='none'` AFTER the await, not just `overlay.hide()`. Threading the load promise moves the hub-root hide behind the whole load, so the hub stays painted over the editor canvas for the full open. That is the `view-region-one-owner` / resize-observer class this repo has paid for. The display flip must be hoisted ABOVE the await; only `overlay.hide()` may move.",
    "F2 — MISSED ORDERING CONSTRAINT: `proactivelySwitchToWebGLForBuildingGeneration` is fire-and-forget (`buildingGenerationLifecycle.ts:307` does not await it) and `swapRendererBackend` (`initScene.ts:4858`) is async — it tears down and rebuilds the renderer under `_swapInFlight`. Calling it synchronously immediately before `versionCtrl.loadVersion(latest)` races a renderer rebuild against ~500 element adds. F1's fix is the PRECONDITION for F2's fix, not an independent lane: once `setProjectContext` is async the swap can be awaited. Also, F2's own 'would break' contradicts itself — the founder's project is 503 elements, and `latest.elementCount` on the local branch would be 503 >= 400, so the element-keyed proactive swap WOULD fire on this exact case. The real calibration risk is the opposite one: stored `elementCount` (503) is not `countElements(scene)` (162 top-level roots), so the stored count over-fires relative to the guard's own predicate.",
    "F3 — THE FIX (a) AS WRITTEN IS A LAYER VIOLATION: `buildPersistence.ts` is `packages/runtime-composer` (L3); `versionRepository` is `apps/editor/src/ui/platform` (L7). 'ask versionRepository for the local head FIRST' is an L3→L7 import the gate would count. The layer-clean and strictly smaller fix the auditor missed: DELETE the prefetch. `PlatformShell._loadLatestVersionFromServer(id)` already exists and is already the else-branch fallback (PlatformShell.ts:382 and the catch tail), so `tier.streamLoad` only ever saves a round-trip on the rare no-local-history path while costing one on every other open. Deleting it needs no staleness comparison and breaks no invariant — which removes the entire 'opens someone's older local copy over a newer server one' risk the auditor named.",
    "F5 — THE 150 ms IS THE WRONG AXIS, AT 'high' CONFIDENCE. Neither socket is awaited by anything on the open path: `initSocketCollaboration` is a void call at `PlatformShell.ts:250` and `connectSocket` runs from a `pryzm-project-loaded` handler. Two async handshakes contribute ~0 ms to the interval the founder is watching. The finding is REAL — the duplication, the two `canUserAccessProject` DB verifications per user per open, and the confirmed order-dependent self-announce (server.js:612 `socket.to()` excludes only the SENDER, so the user's OTHER socket receives its own `user-joined`, and `initCollaboration.ts:614-617` has no self-filter) — but it should be re-filed as a correctness + server-load finding with cost 'not on the client critical path'. Quoting 150 ms of open latency here sends the next lane at the wrong target.",
    "F4 — TWO ENUMERATION CORRECTIONS: (i) 'the autosave re-arm' is NOT one of the 35 `pryzm-project-loaded` subscribers — it hangs off `pryzm-load-suppress-end` (`SaveOrchestrator.ts:290`), which is exactly the drain signal the audit missed. (ii) There are FOUR uncoordinated timers, not three: add SaveOrchestrator's 4,000 ms post-load settle window (`SaveOrchestrator.ts:473`) and its 1,500 ms `_BATCH_SETTLE_MS` (`:225`), which is what actually decides WHEN the 503-element serialize in F9 fires.",
    "F8 — SURVIVES AS AN HONEST NEGATIVE, and should be read that way. Its census reproduces (1,295 non-test files; 31 files >2,000 LOC = 119,918 LOC) and its refutation of the latency hypothesis is correct (O-INV-1 verified; `_bootstrapped` at src/main.ts:203/225). The only part a latency lane should act on is the `runTierPbrPass` extraction, because F2 has to edit that closure. Its `.traverse` point is understated, not overstated — see MISSED."
  ],
  "missed": [
    "THE RE-ENTRANCY WINDOW IS THE REAL CORRECTNESS BUG, and it is stronger than what F1 says. `openProjectInflight` is cleared in the `finally` of the async IIFE, which returns right after `emitProgress('done',100)` — i.e. BEFORE the load. So during the entire actual load `hasInflight === false`, `decideOpenDisposition` returns 'proceed', and clicking a DIFFERENT project starts a fully CONCURRENT second open: teardown for B (`pryzm-project-switch` → ProjectLifecycleController) runs while A's ProjectLoader is still executing commands into the same stores. That is verbatim the failure `buildPersistence.ts:243-247` says the supersede branch exists to prevent ('two overlapping opens would both drive projectContext and attachedSurface.setProjectContext, giving a last-writer-wins interleave of two engine loads, which is a worse defect than the one being fixed'). F1 says supersede 'chains onto a promise that never represented the previous open'; the sharper truth is that supersede NEVER FIRES during the load at all. There is no router-level guard either — grep of PlatformRouter finds no `_openInFlight`. `loadVersion`'s `activeProjectId !== targetProjectId` guard (PlatformVersionController.ts:405) only discards the RESULT; the work still runs to completion against B's stores.",
    "F1's FIX HAS A SECOND-ORDER COST NOBODY PRICED: once `openProject` awaits the real load, the supersede branch serializes project-switch-during-load — clicking B while A loads now waits for A's FULL load before B starts. Today B starts in ~1 s. Correct-but-slower is a legitimate trade, but it means the fix needs a CANCEL token, not only a completion promise; a promise alone converts an interleave bug into a 30-second unresponsive gesture.",
    "THE MESH CENSUS EXISTS THREE TIMES ON THE SAME BOUNDARY, which is the concrete version of F8's SceneRegistry point: `runTierPbrPass`'s `countMeshes` (initScene.ts:3086 region), `batchCoordinator.setSceneMeshCountProvider` (initScene.ts:3086) and `setPostBatchCallback` (initScene.ts:3113) each own a separate `scene.traverse` counting Mesh/InstancedMesh. Three independent full-scene walks answering one question, all on the post-load boundary, none sharing a result. That is a bounded, measurable extraction that serves F2 and F8 at once.",
    "F9(a) HAS A CASE THAT MUST NOT BE DEDUPED AWAY: `PlatformSaveController.ts:345-356` documents `planBlocksSync` — when the sync plan is rejected for THIS project the save-path capture is deliberately skipped and the comment states 'The renderer still captures a fresh thumbnail on the post-load path so the hub preview stays current'. The dedup flag must therefore key on 'a capture actually ran for this projectId this open', never on 'an autosave fired'. Same for a genuinely read-only open where no autosave happens at all.",
    "FOUNDER-LOG RED HERRING, flagged so the next lane does not chase it: `[EdgeProjectorService] PERF-CACHE-STATS ... cacheHits=0 cacheMisses=63 hitRate=0%` is BY DESIGN. `EdgeProjectorService.ts:3682` states in its own words 'hitRate=100% on second run with no changes; hitRate=0% on first run'. It is an in-memory per-session cache and an open is by definition the first run. It is not a defect and it is not an invalidation bug.",
    "THE `else` BRANCH LOADS THE SCENE TWICE. On any open with no local history, `PlatformShell.ts:344` calls `loadAdapter.load(this._makeEmptySnapshot(...))` — a full clear-load — and only THEN, in its `.then`, calls `loadVersion(record)` with the real snapshot. Two complete `loadAdapter.load()` cycles, two teardowns, for one open. Not the founder's path (he has local history) but it is the deep-link / new-device / cache-cleared path, i.e. every first open on a new machine, and it sits inside the same unowned window."
  ]
}