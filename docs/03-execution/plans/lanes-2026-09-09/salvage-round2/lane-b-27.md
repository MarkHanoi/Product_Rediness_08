{
  "refuted": false,
  "reason": "The MECHANISM is confirmed line-by-line and I could not break it. Install is one-shot (GISAreaLayout.ts:1006, reset only at :1451 in the .catch), nested in `if (!cesiumViewport)` (:1042, block closes :1441-1442); both callers (:1499, :2614) share the one promise and toggleGIS's re-activation branch (:1509+) re-wires nothing. mountGISArea is genuinely once-per-tab (Layout.ts:92 <- initUI.ts:3301 <- engineLauncher.ts:1135 <- src/main.ts:211 `_bootstrapped` latch) — note GISAreaLayout.ts:125 asserts the OPPOSITE (\"can run more than once in a session (project switch)\"), a stale comment the call graph refutes. The asymmetry is real: :8498 draw = disarm only; :8504/:8509 face-drag = dispose + unregister, with no `reseed` on the registration (:156-161) even though the registry supports one (ProjectScopeRegistry.ts:45,140). `gis.areaLayout` is in GIS_SWITCH_SCOPES (siteProjectScope.ts:104-120, no ownsTeardown:false), installSiteProjectScope runs at initScene.ts:1577 before the layout mounts, and buildPersistence.ts:307-337 confirms ensure() -> streamLoad -> setProjectContext ordering. Only one production registerSpaceEnvelopeFaceDragSurface (:1273); BIM 3-D installs the gesture (spaceEnvelopeFaceDragController.ts:84) but never registers a surface. CesiumViewport.ts:18568-18594 states outright that one viewport exists per tab and \"the viewer STAYS LIVE\" on a switch — which is the strongest available support for the fix's premise that the listeners are NOT per-project state.\n\nBUT ON MY LENS (would the fix work / would it break something) the proposal is right in shape and wrong in three of its four parts:\n\n(1) FIX (2) IS A NET REGRESSION IF SHIPPED ALONE, and the claim does not see it. Today, after the device-loss dispose+mount (CesiumViewport.ts:6477-6481), the captured viewer makes isTornDown() true (siteEnvelopeDrawCesium.ts:344-352), so cannotDragReason() returns the honest \"The 3D Site view is being torn down…\" and the panel button greys. Give the adapter a viewer PROVIDER and isTornDown() goes false, cannotDragReason() returns null and the button goes LIVE — while the six pointer listeners are still bound to the DESTROYED canvas and nothing re-installs them. That turns an honest refusal into a live button on a dead gesture: exactly the D7 shape (`a panel asserting a gesture the surface does not have`) that spaceEnvelopeFaceDragSurfaces.ts was written to end. The provider is only safe when paired with re-running the extracted wireSiteEnvelopeSurfaces() on re-mount.\n\n(2) FIX (1a) DEPENDS ON TWO SYMBOLS THAT DO NOT EXIST. installSpaceEnvelopeFaceDragOnSurface returns a bare `() => void` (spaceEnvelopeDragSurface.ts:923-936) — there is no cancelActiveDrag, and adding one changes the return type consumed by installSpaceEnvelopeFaceDrag (spaceEnvelopeFaceDragController.ts:84) and attachSpaceEnvelopeRender.ts:198. The focus module exports clearSpaceEnvelopeFaceDragFocus, not releaseSpaceEnvelopeFaceDragFocus. Only disposeFaceDragAffordance() (already at :8511) survives as written.\n\n(3) FIX (1a) BREAKS A LIVE SPEC ARM. siteFaceDragWire.spec.ts:62-67 greps the exact one-line source text at :8504 and :8509 (the :1198-1205 rewire block spreads the same statements over separate lines, so it will not satisfy the regex). The arm must be amended with its reasoning, not deleted. Its stated invariant is already untrue: there is no second install path, so \"gives the next project two of them\" is unreachable, and \"a register row that outlives its viewer keeps answering yes\" is guarded by isTornDown() inside cannotDragReason(), not by the unregister.\n\n(4) FIX (1b) IS THE WRONG BRANCH and worse than the claim's own caveat admits: wireSiteEnvelopeSurfaces() also re-runs `new SiteEnvelopeDrawCesium` + registerEnvelopeDrawSurface, churning the DRAW registration the current clear deliberately preserves, and it still misses the clearScopes path — which is the founder's own scenario.\n\n(5) THE FIX DOES NOT CLOSE TWO SURVIVING SILENT KILL PATHS with the identical symptom: pickFace returns null whenever a perimeter draw is armed (siteEnvelopeDrawCesium.ts:737) and a missed pick returns in total silence (spaceEnvelopeDragSurface.ts:586). The probe is therefore mandatory, not optional — but as specified it cannot be assembled: readAuthoredEnvelopes is a closure inside the `.then` body (:1183), unreachable from where the window hooks are registered (~:2600).\n\n(6) TWO SCOPE CORRECTIONS TO THE CLAIM ITSELF. The clear is broader than stated — ClearProjectCommand.clearAll() runs on every version restore too, not only a project open. And it is NARROWER on one path the claim treats as universal: a HUB open never calls requestEagerGlobeStart()/prewarmGlobe() (both are in PlatformRouter.showOnboarding only), so there the install first runs on the user's own toggleGIS(true) — AFTER the switch — and the gesture works until the next load. The defect is path-dependent, which is a falsifiable prediction the claim does not state.\n\nTwo supporting sub-claims I checked and could not break: envelopes-visible implies frame-exists (getSpaceEnvelopeSceneFrame :8755 and renderSpaceEnvelopes :8832 read the same this.formaMassingOrigin), and the deploy rival is weak (c8c62c51 IS an ancestor of HEAD, and the founder's 2026-09-08 reports L-13261/L-13266 prove he is running a build later than it — so L-13236's \"OPEN on the deploy\" no longer applies).",
  "evidenceChecked": [
    "apps/editor/src/ui/layout/GISAreaLayout.ts:118-165 — GIS_LAYOUT_SCOPE registration declares `clear` only, no `reseed`; and :125 asserts mountGISArea 'can run more than once in a session (project switch)', contradicting declaredProjectScopes.ts:251",
    "apps/editor/src/ui/layout/GISAreaLayout.ts:1005-1006, 1042, 1099-1100, 1441-1453 — one-shot gisInitPromise, wiring nested in `if (!cesiumViewport)` + `if (viewer)`, promise nulled only in the .catch at :1451",
    "apps/editor/src/ui/layout/GISAreaLayout.ts:1197-1273, 1300-1436 — the ONLY construction of SiteEnvelopeDrawCesium, registerEnvelopeDrawSurface, registerSpaceEnvelopeFaceDragSurface and installSpaceEnvelopeFaceDragOnSurface",
    "apps/editor/src/ui/layout/GISAreaLayout.ts:1467-1560 — toggleGIS: the re-activation branch re-wires no envelope surface",
    "apps/editor/src/ui/layout/GISAreaLayout.ts:2608-2616 — `void ensureGisInitialized('eager')` behind consumeEagerGlobeStart(), fire-and-forget",
    "apps/editor/src/ui/layout/GISAreaLayout.ts:8479-8526 — clearLayoutProjectState: draw disarm only (:8498) vs face-drag dispose (:8504) + unregister (:8509) + affordance (:8511); delegate installed at :8520-8526",
    "apps/editor/src/ui/Layout.ts:87-92 and apps/editor/src/engine/initUI.ts:3295-3301, 3579 lines — createMainLayout has exactly one caller",
    "src/main.ts:203-226, 290-380 — `_bootstrapped` latch makes bootstrap (hence mountGISArea) once per tab",
    "packages/runtime-composer/src/buildPersistence.ts:290-340 — openProject ordering: ensure() (boot) -> streamLoad -> setProjectContext",
    "apps/editor/src/ui/platform/PlatformShell.ts:216-252 — pryzm-project-switch emitted on every non-duplicate open",
    "apps/editor/src/ui/site/siteProjectScope.ts:104-120, 274-296, 425-450 — GIS_SWITCH_SCOPES derived from DECLARED_PROJECT_SCOPES; clearScopes on switch; listener subscribed at install",
    "apps/editor/src/engine/initScene.ts:1577 — installSiteProjectScope() runs during boot, before initUI/mountGISArea",
    "packages/core-app-model/src/persistence/declaredProjectScopes.ts:248-275 — gis.areaLayout entry, no ownsTeardown:false, resets list does NOT mention the face-drag handles",
    "packages/core-app-model/src/persistence/ProjectScopeRegistry.ts:18-45, 140-147 — reseed() is supported and runs after clearAll()",
    "packages/command-registry/src/project/ClearProjectCommand.ts:19-22, 246-250 — 'first step of ProjectLoader.load()'; clearAll() then reseedAll()",
    "apps/editor/src/engine/spaceEnvelopeDragSurface.ts:543-600, 580-600, 810-820, 908-936 — the pick/focus wrapper, the silent `if (!picked) return`, and the disposer's camera hand-back; return type is a bare `() => void`",
    "apps/editor/src/engine/spaceEnvelopeFaceDragController.ts:1-104 — BIM 3-D entry point; installs the gesture but never registers a face-drag surface",
    "apps/editor/src/ui/site/spaceEnvelopeFaceDragSurfaces.ts:52-149 — register/unregister, last-unregister releases focus, SPACE_ENVELOPE_FACE_DRAG_NO_SURFACE_REASON, spaceEnvelopeFaceDragAvailability",
    "apps/editor/src/ui/site/spaceEnvelopeFaceDragFocusState.ts — exports clearSpaceEnvelopeFaceDragFocus, NOT releaseSpaceEnvelopeFaceDragFocus",
    "apps/editor/src/ui/site/siteEnvelopeDrawCesium.ts:247-268, 335-352, 633-646, 682-760, 792-806 — captured `private readonly viewer`, isTornDown(), cannotDragReason() three states, pickFace deaf while a draw is armed (:737), dragDomElement()",
    "apps/editor/src/ui/site/siteEnvelopeDrawArming.ts:1-50, 339-495 — every exit (finish/cancel/unregister) routes through disarmAll()",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:6450-6490 — device-loss recovery does dispose() + mount(), producing a new viewer/canvas with no re-wire",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:18566-18620 — resetProjectScopedState: one viewport per tab, 'the viewer STAYS LIVE' on a project switch",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:8740-8763 and 8820-8845 — getSpaceEnvelopeSceneFrame() and renderSpaceEnvelopes() read the SAME this.formaMassingOrigin",
    "apps/editor/src/ui/layout/__tests__/siteFaceDragWire.spec.ts:44-68 — the arm that greps the exact single-line teardown text at :8504/:8509 and would FAIL under fix (1a)",
    "apps/editor/src/ui/analysis/parcelLawEnvelopeAuthoring.ts:1010-1080 — the Drag face button reads spaceEnvelopeFaceDragAvailability() per repaint",
    "docs/04-reference/ISSUE-LOG.md L-13045 / L-13236 — prior analysis, and the 'OPEN on the deploy' caveat",
    "git: `merge-base --is-ancestor c8c62c51 HEAD` -> true; founder reports L-13261/L-13266 dated 2026-09-08 prove a post-install build is live"
  ],
  "betterHypothesis": "The root cause stands; the FIX should be re-shaped. (i) Ship the probe FIRST — as a mount-scope holder, not from inside the `.then` closure — because `pickFace` is silently deaf while a perimeter draw is armed (siteEnvelopeDrawCesium.ts:737) and a missed pick is wholly silent (spaceEnvelopeDragSurface.ts:586), so two other causes produce this exact symptom and would survive the fix. (ii) Take branch (a) only — make the face-drag teardown symmetric with the draw's at :8498: keep the listeners and the register row (the viewer explicitly stays live, CesiumViewport.ts:18586), and drop only genuinely per-project state (cancel any in-flight drag, clearSpaceEnvelopeFaceDragFocus(), disposeFaceDragAffordance()). This needs a real API addition — return `{ dispose, cancel }` from installSpaceEnvelopeFaceDragOnSurface, rippling through spaceEnvelopeFaceDragController.ts:84 and attachSpaceEnvelopeRender.ts:198 — and it needs siteFaceDragWire.spec.ts:62-67 AMENDED with the reasoning, since that arm pins the very lines being removed. Drop branch (b) entirely: reseed misses the clearScopes switch path and churns the draw registration the clear deliberately preserves. (iii) Do NOT ship the viewer PROVIDER on its own — alone it flips cannotDragReason() from an honest 'torn down' to a plain YES while the listeners sit on a destroyed canvas. Pair it with calling the extracted wireSiteEnvelopeSurfaces() from the device-loss re-mount so the listeners actually land on the new canvas. (iv) Separately: BIM 3-D installs the gesture but never calls registerSpaceEnvelopeFaceDragSurface, so the panel's Drag face verdict is driven by the 3-D Site alone — the author's 'button greyed' oracle is valid for the 3-D Site case but is not a general oracle, and that gap is its own row."
}