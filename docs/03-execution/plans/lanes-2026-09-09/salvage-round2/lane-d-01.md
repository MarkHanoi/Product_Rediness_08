{
  "area": "Renderer backend selection on project open — §AUTO-WEBGL-HEAVY fires at `tier:post-load`, the last moment of the open, and discards the entire WebGPU TSL pipeline it just paid to build.",
  "rootCause": "The heavy-scene backend guard is wired ONLY to the reactive entry point, and on a project OPEN that entry point can physically only fire after hydrate is complete.\n\n(a) WHEN it runs — established, not inferred. `reason=tier:post-load` is minted at `apps/editor/src/engine/initScene.ts:3038`: `_runConsolidatedTierPbrPass = () => runTierPbrPass('post-load')`. That handle is invoked from a SECOND `pryzm-project-loaded` listener (initScene.ts:3762-3768) inside a `setTimeout(0)`. Inside `runTierPbrPass` the guard is called at initScene.ts:3008: `maybeAutoSwitchToWebGLForHeavyScene(scene, `tier:${reason}`, meshCount)`, with `meshCount` produced by a full `scene.traverse` a few lines above. So the guard's INPUT is the finished scene; it cannot run before hydrate by construction. The threshold is `heavySceneSwapThreshold.ts:40-43` — `SWAP_ELEMENT_THRESHOLD = 400`, `SWAP_MESH_THRESHOLD = 1000` — and `isSwapWorthyHeavyScene` (autoWebGLHeavyScene.ts:109-113) ORs the two arms, so 550/3762 trips both.\n\nThe two OTHER firing sites decide earlier and are unreachable on the open path: `proactivelySwitchToWebGLForBuildingGeneration` is called only from `buildingGenerationLifecycle.ts:307`, whose callers are the residential / office / house executors (ADR-0267 \"Affects\" list). `ProjectLoader` calls none of them. So a project open has exactly one guard, and it is the late one.\n\n(b) WHAT the swap actually throws away — and the target brief's framing is HALF WRONG. The scene's GPU state is NOT paid twice. `RenderPipelineManager.bind()` sets `_webGpuActive = true` only on the native-WebGPU arm (RenderPipelineManager.ts:762, after the `if (!isWebGPU) { … return; }` at :715-760). Four expensive things gate on that flag and are STRUCTURALLY IMPOSSIBLE on the classic renderer: `onProjectLoaded()` → `activateOutlines()` (:3280 `if (!this._webGpuActive) return;`), `scheduleShadowRebuild()` (:1957 same guard), `_rebuildPipeline()` (:4912 `if (!this._webGpuActive) return Promise.resolve();`), and `bind()`'s own `_loadTSL()` + `_buildPipeline()` (:781-783). The founder's `SHADOW_REBUILD_COMPLETE elapsed=16011.5ms` is emitted at RenderPipelineManager.ts:2037 from inside `scheduleShadowRebuild`, i.e. it is a WebGPU-only code path — and `§RENDERER-LIVE-SWAP` then calls `rpm.dispose()` FIRST (initScene.ts:4973), which runs `_safeDisposeRenderPipeline()` and nulls `_renderPipeline` / `_scenePass` / `_zonePass` / `_outputNode` (:3332-3340). The 16 s pipeline is built and then deleted.\n\n(c) The swap's own cost, from code (initScene.ts:4859-5155): `unifiedFrameLoop.stop()` → `rpm.dispose()` → mint a second canvas → `await createRenderer(newCanvas,'webgl-classic')` → `recoverPipelineOrBind()` → `renderPerfServiceRef.rebind()` → `retireRenderer(oldRenderer)` (→ `device.destroy()`, which is the `reason=\"destroyed\"` line) → `oldCanvas.remove()` → `clearObcBaseFramebuffer` → loop restart → TWO full `scene.traverse` passes over 3762 meshes for `§SWAP-PAINTS-THE-BUILDING` (:4825-4835, called at :5160 and again at :5163 one second later) plus a third traverse for the census (:5169). Geometry and materials are NOT rebuilt — the same `THREE.Scene` is kept (:4775-4780) — but the classic renderer must compile a `WebGLProgram` per material and upload every geometry on its first frames, and with the census showing `instanced? NO` on all 3762 meshes those are per-element unique materials. That first-frame compile+upload lands squarely between `open:snapshot-loaded` and `open:first-interactive-frame`, the measured 15,628 ms gap.\n\nThis is a KNOWN, WRITTEN-DOWN defect: C04 §1.4a (L-1414) says verbatim \"`reason=tier:post-load` means the swap decision for a project OPEN is taken **after** all 3,191 meshes exist … the swap buys nothing it was designed to buy while paying the maximum possible cost … **NOT DONE, and honestly so**\". Its stated blocker was that the pre-build path \"cannot supply a mesh count\" and the founder's then-project tripped ONLY the mesh arm (233 roots < 400). THAT BLOCKER DOES NOT APPLY TO THIS RUN: the snapshot declares 527 elements and the serializer 503, both ≥ 400, so the ELEMENT arm alone is decisive — and `VersionRecord.elementCount: number` is a plain scalar on the version index (`apps/editor/src/ui/platform/PlatformShellTypes.ts:51`), read at `PlatformShell.ts:296-298` in an 89 ms local call, which the boot order puts AFTER the renderer already exists.",
  "evidence": [
    "apps/editor/src/engine/initScene.ts:3038 — `_runConsolidatedTierPbrPass = () => runTierPbrPass('post-load');` and initScene.ts:3762-3768 — a second `pryzm-project-loaded` listener wraps it in `setTimeout(() => { _runConsolidatedTierPbrPass?.(); })`. The guard's trigger is the load-COMPLETE event.",
    "apps/editor/src/engine/initScene.ts:3008 — `maybeAutoSwitchToWebGLForHeavyScene(scene, `tier:${reason}`, meshCount);`, where `meshCount` comes from the `scene.traverse((obj) => { if (obj instanceof THREE.Mesh || obj instanceof THREE.InstancedMesh) meshCount++; })` immediately above it. The decision consumes the finished scene.",
    "apps/editor/src/rendering/heavySceneSwapThreshold.ts:40,43 — `export const SWAP_ELEMENT_THRESHOLD = 400;` / `export const SWAP_MESH_THRESHOLD = 1000;` and autoWebGLHeavyScene.ts:109-113 `isSwapWorthyHeavyScene` ORs the arms. 550 elems / 3762 meshes trips both.",
    "packages/renderer-three/src/pipeline/RenderPipelineManager.ts:762 — `this._webGpuActive = true;` sits AFTER the `if (!isWebGPU) { … this._phase = 'phase2'; this._emitState(); return; }` at :715-760. So `_webGpuActive` is TRUE only on native WebGPU.",
    "packages/renderer-three/src/pipeline/RenderPipelineManager.ts:1957 `scheduleShadowRebuild(): void { if (!this._webGpuActive) return;` and :4912 `private _rebuildPipeline(): Promise<void> { if (!this._webGpuActive) return Promise.resolve();` and :3280 `onProjectLoaded(): void { if (!this._webGpuActive) return;` — the 16.0 s `SHADOW_REBUILD_COMPLETE` (logged at :2037) plus the outline activation are WebGPU-ONLY work. On the classic renderer they are unreachable no-ops.",
    "apps/editor/src/engine/initScene.ts:4972-4974 — swap step 2: `try { rpm.dispose(); } catch (e) { console.warn('[initScene] §RENDERER-LIVE-SWAP old RPM dispose failed (non-fatal):', e); }`, and RenderPipelineManager.dispose() :3331-3340 nulls `_renderPipeline`, `_scenePass`, `_zonePass`, `_outputNode` and sets `_webGpuActive = false`. Everything the 16 s produced is deleted.",
    "docs/02-decisions/contracts/C04-RENDERING-AND-SCHEDULING.md §1.4a (L-1414): \"the swap buys nothing it was designed to buy while paying the maximum possible cost … It fires on *every* project open above the threshold, so this is not an edge case. **The correct shape is to decide the backend BEFORE the scene is populated** … ⚠ **NOT DONE, and honestly so**\". Its named blocker is the mesh count; this run trips the ELEMENT arm too, which the snapshot already carries.",
    "apps/editor/src/ui/platform/PlatformShellTypes.ts:51 — `elementCount: number;` on `VersionRecord`; packages/core-app-model/src/persistence/DelegateTypes.ts:63-67 — `elementCount: number;` is a REQUIRED field on `IProjectSnapshot`. The count needs no inflate and no multiplier.",
    "apps/editor/src/engine/startupBudget.ts:157-158 documents the measured boot order: `boot:engine-start → boot:scene-done → … → boot:ui-done → open:shell-context-set → open:version-read-start/-done → open:snapshot-loaded`. `boot:scene-done` (engineLauncher.ts:312, immediately after `await initScene(...)` at :311) precedes the version read by four stages — the renderer is built before anything knows which project is opening.",
    "src/main.ts:629-631 — `void import('@app/rendering/rendererPrewarm').then(({ prewarmRenderer }) => { prewarmRenderer(); })` in Phase B, and rendererPrewarm.ts:79 `_promise = createRenderer(canvas)` with NO backend override. The WebGPU renderer is constructed with zero project knowledge, before the hub is even shown, and consumed unconditionally at initScene.ts:2198/2215-2237.",
    "(d) THE 13 ARE NOT LEAKED. `_minted` increments once per `RenderObjects.createRenderObject` (rendererRetirement.ts:184), and the only two paths that remove an entry from `live` are (i) three's own churn dispose, wrapped at rendererRetirement.ts:188-192 `(renderObject as { dispose: () => void }).dispose = () => { live.delete(ref); disposeOnce(); };` — and `RenderObject.dispose()` is exactly what REMOVES the two listeners (RenderObject.js:906-907) — and (ii) a collected `WeakRef` at :302-305 `if (renderObject === undefined) { live.delete(ref); continue; }`, which is not counted in `released`. The module's own argument at rendererRetirement.ts:117-120 closes it: \"a render object whose material is still live is ALREADY strongly reachable (`material._listeners.dispose` holds `renderObject.onMaterialDispose`, an arrow closure over the render object), so anything a weak ref loses is precisely something that registered no surviving listener to detach.\" 8 + 13 = 21; nothing is stranded. ⚠ Diagnostic gap, not a leak: `retireRenderer` warns only when `detached === 0 && minted > 0` (:374-381), so a PARTIAL shortfall like 8/21 prints no reason — the log cannot tell GC from churn.",
    "(e) THE THRESHOLD IS A ONE-MACHINE GUESS, and the repo already says so twice. ADR-0267 §Fix-1's justification is verbatim intuition: \"400 sits comfortably between 'trivial edit' (never swap) and 'building' (always swap)\". The only measurement behind it is a SINGLE data point — \"a live Fly run generated a residential building of ~1,300 elements / 6 levels / ~1,645 meshes on Auto+WebGPU … and device-lost\" on one Windows box. No loss-probability curve, no second machine, no lower bound. heavySceneSwapThreshold.ts:33-36 records the counter-evidence in the same repo: \"the founder crossed the mesh arm with ONE DOOR (978→1005 sceneMeshes) and the backend swapped MID-EDIT. Raising the numbers moves the cliff; it does not remove it.\"",
    "(e cont.) THE PIN-OVERRIDE IS ALSO UNDOCUMENTED IN THE CONTRACT. ADR-0267's own Decision #1 says \"An explicit `'webgpu'` pick is a user override we **respect** (keep WebGPU, log a one-time warning, never swap)\", reversed later by §Fix-3 — but C04 §1.4 line 42 STILL reads \"An **explicit** `webgpu` / `webgl` selection is ALWAYS respected (Auto is the only adaptive mode)\" and still quotes the OLD `isHeavyModel` numbers (≥15 levels AND ≥1000 elems, OR ≥4000 elems). The contract text is stale against `isSwapWorthyHeavyScene`, which C04 §1.4a itself names as the real predicate.",
    "autoWebGLHeavyScene.ts:26-29 states the design intent the open path violates: the reactive entry \"fires once the live scene is measurably heavy (Gate 3) … Because it needs geometry to exist first, it fires MID-generation\". RenderPipelineManager.ts:2295-2296 confirms the coupling from the other side: \"The `tier:post-load` pass fires §AUTO-WEBGL-HEAVY (C04 §1.4) — i.e. the backend swap is fired FROM INSIDE that freeze window. Not a coincidence: both hang off the same post-load tier pass.\""
  ],
  "fix": "DECIDE THE BACKEND BEFORE THE RENDERER IS CONSTRUCTED, USING THE COUNT THE SNAPSHOT ALREADY CARRIES. Every mechanism this needs already exists; nothing new is invented.\n\n1. Thread a session-scoped override into the boot build. `createRenderer(canvas, backendOverride?)` already accepts a per-call, NON-persisting override (createRenderer.ts:397-399, resolved at :435 via `resolveEffectiveBackendPreference`), and `swapMayPersistPreference` (createRenderer.ts:283) already keeps `'webgl-classic'` out of the stored preference — L-1483's rule that \"a heuristic may override a pin for a session; it may not destroy it\" is preserved unchanged. Pass `'webgl-classic'` to `prewarmRenderer()` / initScene Phase-5 `createRenderer(webgpuCanvas)` (initScene.ts:2270) when the incoming project trips the arm.\n\n2. Source the count from the version index, not from the scene. `VersionRecord.elementCount` (PlatformShellTypes.ts:51) is already read by the hub warm (`hub:warm-versions-done` precedes `hub:open-clicked` in the boot order at startupBudget.ts:157) and again at `PlatformShell.ts:296-298` in an 89 ms local call. `503 >= SWAP_ELEMENT_THRESHOLD` answers this run with zero new heuristics. Set `globalThis.__pryzmAutoSwappedToWebGL = true` at the same moment so the reactive `tier:post-load` path (autoWebGLHeavyScene.ts:218 `if (hasAutoSwitchedToWebGL()) return;`) is a hard no-op and cannot double-fire.\n\n3. Close C04 §1.4a's own named exit condition for the mesh arm — \"until the estimate can be DERIVED (for example from the previous session's measured mesh count for the same project, persisted)\". `logSceneCensusOnce` already runs one second after every swap (initScene.ts:5165-5172) and `censusScene` already computes the mesh count. Persist it keyed by projectId and read it beside `elementCount`. This is measurement, not the per-family multiplier §INST.2 refuses on sight. First open of a brand-new project keeps today's late swap as the fallback.\n\n4. THE ONE REAL CODE CHANGE, not plumbing: initScene.ts:2411-2418 aborts Phase 5 when `rendererResult.backend === 'webgl-only'`, on the (correct, for a boot failure) assumption that webgl-only means WebGPURenderer construction failed. An INTENTIONAL classic build must be distinguished from that failure — the swap path already draws exactly this distinction with `isUnintendedWebglOnlySwap(newResult.backend, intendedClassicWebGL)` at initScene.ts:5004. Reuse that predicate at the boot arm. Without it, a pre-build classic decision drops the editor back to OBC and loses the PRYZM overlay entirely.\n\nWHAT THIS BUYS, and the bound on the claim: the whole WebGPU-only pipeline family — `_loadTSL()` + `_buildPipeline()` (RenderPipelineManager.ts:781-783), `onProjectLoaded()` → `activateOutlines()` (:3280-3304), `scheduleShadowRebuild()` → `_rebuildPipeline()` (:1957-2043) — never runs, and neither does the swap, its second canvas, its `device.destroy()`, or its three full 3762-mesh traverses. The scene reaches the classic renderer once. ⚠ THE MS FIGURE IS A SPAN, NOT CPU: `SHADOW_REBUILD_COMPLETE elapsed=16011.5ms` is `performance.now()` around an async promise (RenderPipelineManager.ts:2033-2039), so it also contains every other main-thread task in that window (the 881-event `endBatch()` flush, ×5 `REDETECT_ROOMS`, ×4 `TopologyLayer` adjacency rebuild). I cannot separate them from code. Verify with a browser performance profile across `open:snapshot-loaded → open:first-interactive-frame`, or A/B by forcing `localStorage['pryzm.renderer.backend'] = 'webgl'` before the open — that boots `webgl-fallback`, not classic, so it is an approximation of the timing question, not of the backend question.",
  "expectedSavingMs": 16011,
  "savingBasis": "derived-from-log",
  "risk": "medium",
  "confidence": "high",
  "rivalRuledOut": "RIVAL 1 — RULED OUT, and it refutes half the brief. \"The scene's GPU resources are uploaded to WebGPU, then thrown away and uploaded again to WebGL, so the app pays for two full scene builds.\" FALSE. The retirement log itself is the disproof: `8 detached of 21 minted (tracked)`. `_minted` increments exactly once per `RenderObjects.createRenderObject` (rendererRetirement.ts:183-184), and in vendored three 0.183.2 `RenderObjects.get()` calls `createRenderObject` on a chain-map MISS — one per unique (object, material, scene, camera, lightsNode, renderContext, clippingContext, passId) actually drawn (node_modules/three/src/renderers/common/RenderObjects.js:107). 21 mints across a 3762-mesh scene means the retired WebGPU renderer NEVER DREW THE MODEL — consistent with the load-window submit pause (`_beginShadowRebuildGuard`, RenderPipelineManager.ts:1990/2033) and the whole-load shadow freeze (initScene.ts:3583-3597). The wasted work is the TSL PIPELINE, not the geometry; the classic renderer's upload is the FIRST one, not the second. Note this also corrects C04 §1.4a's own wording (\"after all 3,191 meshes exist **and have rendered**\") — the count says they had not rendered.\n\nRIVAL 2 — RULED OUT. \"The swap blocks the first interactive frame by stopping the rAF loop.\" FALSE by code: `UnifiedFrameLoop.stop()` (packages/core-app-model/src/rendering/UnifiedFrameLoop.ts:302-309) only disposes its own tick-listener subscription and its doc says explicitly it \"does NOT stop the scheduler's rAF pump\". `open:first-interactive-frame` is a `getFrameScheduler().scheduleOnce` (PlatformShell.ts:202-205), a different subscription. The swap delays that mark by occupying the main thread, not by stopping the pump.\n\nRIVAL 3 — COULD NOT RULE OUT, stated plainly. \"The 16,011.5 ms is mostly `_rebuildPipeline`'s own CPU, so removing it saves ~16 s.\" I cannot establish this from code. The number is wall-clock around an async promise and `_buildPipeline` contains no `compileAsync` and no per-material loop I could find (RenderPipelineManager.ts:3753-3790 — it awaits `import('three/webgpu')` then calls `createScenePass`/`createZonePass`). The same 15.6 s window also holds the 881-event flush, five `REDETECT_ROOMS`, four `TopologyLayer` rebuilds and the swap. The saving is therefore bounded ABOVE by 16.0 s and its true value needs a browser profile. I report the span because the CODE PATH is provably WebGPU-gated, not because the CPU fraction is known.\n\nRIVAL 4 — COULD NOT RULE OUT. \"The threshold is right and the swap is load-bearing on this hardware, so pre-deciding just moves an already-necessary decision.\" Possibly true, and it does not change the fix: pre-deciding lands on the SAME backend, only earlier. But I could not evaluate whether the swap is necessary at all here, because the only evidence for it is one machine and one generated building (ADR-0267 §Fix-1) — and this run is a project OPEN, a different workload from the generation the ADR measured. If the guard is over-firing on opens, the fix above still helps; the threshold question is separate and needs a second machine.",
  "whatItWouldBreak": "1. THE PHASE-5 ABORT IS THE REAL HAZARD. initScene.ts:2411 `const isWebGPUCapable = rendererResult.backend !== 'webgl-only';` throws and hands the viewport back to OBC. A pre-build classic renderer IS `webgl-only`, so shipping step 1 without step 4 silently disables the PRYZM overlay renderer on every large project — a far worse regression than the 17 s open. The comment at initScene.ts:2404-2409 states the boot abort was \"intentionally LEFT AS-IS (zero boot-behavior change)\" precisely because the classic route was routed entirely through the swap; changing that assumption is the change.\n\n2. USER-VISIBLE BACKEND POLICY SHIFTS FROM \"WEBGPU, THEN DEMOTED\" TO \"NEVER WEBGPU\". Today a founder with an explicit WebGPU pin sees WebGPU briefly and gets a warning naming the demotion. Pre-deciding means the pin never takes effect for that project at all. The stored pin must still survive (`swapMayPersistPreference` already guarantees this for `'webgl-classic'`, createRenderer.ts:283-284 / L-1483), and the corner toggle's re-pick must still force WebGPU for the session — otherwise this recreates L-1483 one seam earlier.\n\n3. THE PREWARM SINGLETON BECOMES BACKEND-SPECIFIC. `rendererPrewarm.ts:54-55` holds one `_promise` / `_consumed` pair minted before any project is chosen. If the pre-warm built WebGPU and the open then wants classic, the prewarmed renderer must be DISCARDED (and retired via `retireRenderer`, not a bare `dispose()` — L-948), or the fast path at initScene.ts:2215 hands back the wrong backend. Discarding it re-exposes the 2,401 ms adapter LONGTASK on the open path for large projects, which is a real trade, not a free win.\n\n4. THE SHADOW-FREEZE ASYMMETRY MAY CHANGE SHAPE. C04 §SHADOW rule 12 / RenderPipelineManager.ts:2287-2310 documents the founder's \"lines of profiles only\" defect as caused by `rpm.dispose()` zeroing the freeze counters mid-freeze while the swap is fired from inside that window. Removing the swap removes that specific chain — but `setShadowReallocFrozen(true/false)` still runs on the open (initScene.ts:3583-3597) against a classic renderer that was NEVER WebGPU. The per-light thaw path must be re-verified on a boot-classic session; the existing tests (`shadowFreezeSurvivesBackendSwap.test.ts`) only cover the SWAP, not a boot with no swap.\n\n5. Projects below 400 elements whose mesh count exceeds 1000 (the founder's earlier 233-element / 3,191-mesh case in C04 §1.4a) keep today's late swap until the persisted mesh count from step 3 exists. That case is not fixed by this change — say so rather than let §1.4a be marked closed.",
  "filesToChange": [
    "apps/editor/src/rendering/rendererPrewarm.ts",
    "apps/editor/src/engine/initScene.ts",
    "apps/editor/src/rendering/autoWebGLHeavyScene.ts",
    "apps/editor/src/ui/platform/PlatformShell.ts",
    "apps/editor/src/rendering/createRenderer.ts",
    "docs/02-decisions/contracts/C04-RENDERING-AND-SCHEDULING.md",
    "docs/02-decisions/adrs/ADR-0267-auto-webgl-fallback-for-heavy-scenes.md"
  ]
}