{
  "survivingTitles": [
    "The heavy-scene guard's mesh arm has INVERTED against its own calibration — at 7.5 meshes/element it fires ~3x earlier than the element arm it was written to backstop, so the renderer live-swap now runs on every real project open. His log proves it fired on this one.",
    "MEASURED on this tree: a wall with openings is 5.4 meshes and 8.4 material constructions; the same wall without openings is 1.03 meshes and 0.41 materials. Openings alone disqualify 100% of a realistic building from the instanced arm.",
    "'162 elements' in the founder's headroom line is a MEASUREMENT ARTIFACT, not his project's element count — the census requires userData.id AND userData.elementType on the SAME MESH",
    "(c) HONEST ANSWER: mesh count is overwhelmingly a FRAME cost. Its only established OPEN-path costs are the backend swap and one load-end per-mesh bounding-sphere sweep. The load traversals that WOULD have made it an open cost are already gated and closed — I checked, and I am reporting that rather than re-reporting them."
  ],
  "refutedTitles": [
    "Furniture is the largest mesh family in the repo by an order of magnitude, and it is the ONE family whose instancing is fully built, fully wired, and switched OFF by default",
    "The shared sibling-mesh merge helper exists, is owned by the THREE owner, carries the per-family measurement in its own header — and has exactly ONE production caller",
    "CORRECTION to the standing memory note: 'instancing defeated by per-element UNIQUE MATERIALS' is now only half true — the material problem is solved at the chokepoint, and what actually blocks these families is the GATE UPSTREAM of it",
    "(b) The '(unattributed)' bucket: the two named examples are 2 declared app-chrome meshes and cannot be the bulk; element sub-meshes are all attributed; the plan projector is ruled out; the third example is UNKNOWN and the exact count is one line above what he pasted"
  ],
  "refutations": [
    "#1 SURVIVES on cost, but TWO load-bearing sub-claims are FALSE and must be struck before anyone acts on it. (a) 'INVERTED against its own calibration' is wrong. heavySceneSwapThreshold.ts's own header states the mesh arm's DESIGN INTENT verbatim: 'building geometry explodes to > 1000 sub-meshes (openings, finishes, frames) well before it reaches 400 counted top-level roots, so this arm catches heavy scenes whose geometry arrives with sparse top-level userData ids'. The mesh arm was DESIGNED to fire before the element arm. The auditor's '1.27 meshes/element calibration' is inferred from a sentence in the DECOUPLED note that is about LevelScoped3DCullingService.isHeavyModel, a different gate — the 1000 was never calibrated against that ratio. (b) 'runs on every real project open' is FALSE. autoWebGLHeavyScene.ts:121/323-324 sets `_autoSwapDone` + `globalThis.__pryzmAutoSwappedToWebGL` and NOTHING in apps/, packages/ or plugins/ ever resets either (grep: 6 hits, all inside that one file, none a reset). The swap is once per PAGE SESSION, and after it the backend is 'webgl-only' so backendBenefitsFromClassicSwap() returns false forever after. If the founder's complaint is that EVERY open is slow, the swap cannot be the whole story — and that is a free discriminator: a second open in the same tab must not pay it. What DOES survive, fully verified: logSceneCensusOnce has exactly ONE production call site (initScene.ts:5182, inside the swap's own 1-second setTimeout); printSceneCensus's only other reachable caller is pryzmPerfConsole.ts:1674 label 'on-demand' behind window.pryzmPerf.census(), which a human must type. So the headroom line does prove a swap completed. It is also already-known behaviour, not a new discovery: apps/editor/__tests__/autoWebGLHeavyScene.contractPredicate.test.ts pins this exact case ('the founder opened a project (294 elements / 3,191 meshes / 7 levels) ... §AUTO-WEBGL-HEAVY live-swapping the renderer immediately after load, reason=tier:post-load') and asserts swap('webgl-classic') fires at tier:post-load. The load path reaches it via initScene's `_runConsolidatedTierPbrPass = () => runTierPbrPass('post-load')`, which calls maybeAutoSwitchToWebGLForHeavyScene at :3008. Keep the finding, keep the 'measure the swap' fix, strike the inversion story and the 'every open'.",
    "#3 SURVIVES and is the strongest-evidenced item in the set — I independently confirmed the artifact rather than trusting the paste. C:\\Users\\LENOVO\\AppData\\Local\\Temp\\startup27-project-open-scale.txt exists, 3,849 bytes, LastWriteTime 2026-09-08 22:25:58, generated stamp 2026-09-08T21:25:58Z — written yesterday, AFTER the harness (test file mtime Aug 24), so the proof does not predate its subject. Every number the auditor quotes matches the file byte-for-byte: 1x realistic 200 walls -> specCache 21.0 / resolve 35.8 / build 352.8 / TOTAL 409.5, sceneMeshes 1080, geomCreated 2440, matCreated 1680, instGroups 0, mat/wall 8.40; control none -> 106.0 total, 205 meshes, 82 materials, 5 instGroups, 160 instTotal, 0.41 mat/wall; rejection clauses openings == notInstanced exactly at all three scales (200/200, 3960/3960, 20240/20240) with mitre at 20/72/80. WallFragmentBuilder.ts:1438-1447 carries `!_hasOpenings` in isSimpleWall exactly as quoted, and the 'live hypothesis' mitre comment is at ~1404-1435. TWO CORRECTIONS to how the 76ms should be read. (a) It is an UPPER BOUND, not a measurement of his file. buildLevel() gives EVERY wall exactly one opening (perimeter->WINDOW, interior->DOOR); the founder's 56 walls carry an unknown number, and ProjectSerializer's elementCount does not even report windows/doors so nobody knows the ratio. (b) It is NOT a recoverable saving under either proposed fix. The delta is dominated by the CSG/fragment build that having an opening requires; the control row is an unreachable state. Fix (1) consolidateSiblingMeshes ADDS open-path CPU (it clones every member geometry, runs mergeGeometries, then disposes — see consolidateSiblingMeshes.ts:127-141), and fix (2) is a design change. Quote the 76ms as 'the price of 56 pierced walls', never as 'the saving available'.",
    "#6 SURVIVES — every clause verified. pryzmPerfConsole.ts censusScene: the element denominator is incremented ONLY at `const id = o.userData?.id; if (id && type)`, which sits below `if (!o.isMesh) return;`, so a Group root is never counted and a child mesh carrying `parentId` (never `id`) is never counted. FurnitureFragmentBuilder.ts:99-108 puts `{id, elementType:'Furniture'}` on a `new THREE.Group()`; :262-266 stamps children `elementType:'FurniturePart', parentId: data.id` with no `id`. So furniture contributes ZERO to that denominator while contributing hundreds of meshes. ProjectSerializer's elementCount sum is exactly as described and `windows` / `doors` appear in the snapshot object on the very next line but NOT in the sum. 3762/162 = 23.2 is an artifact; 3762/503 = 7.48 is the floor. Zero milliseconds, but this is the denominator the entire dimension is built on and it silently corrupts the 'meshes/elem' column of the per-family table the next lane will read.",
    "#8 SURVIVES — I re-read every gate and every one says what is claimed. perAddGeometryGate.ts:44-45 `const defer = isBatching || isProjectLoadActive();`; ProjectLoader.ts:710 sets `__pryzmProjectLoadActive = true` and :3228 clears it; initScene.ts:4120-4123 `if (isProjectLoadActive()) { _geomAddedSuppressedDuringLoad++; return; }` with the one-shot at :4156-4172 printing `§LOAD-SHADOW-PASS-DEFERRED ran ONE full-scene shadow-flag pass in ${...}ms`; PascalSceneLighting.ts:537 `if (!obj.geometry.boundingSphere) obj.geometry.computeBoundingSphere();`. Both O(n^2) load traversals are genuinely closed. The verdict 'mesh count is mostly a frame cost at 503 elements' is the honest answer and I endorse it. ONE CORRECTION: it says there is exactly ONE remaining O(meshes) load-end pass. There are at least two, and it missed the bigger one — see missed[1] and missed[2].",
    "#2 REFUTED. Its central factual claim is FALSE: 'There is no sibling-mesh merge anywhere in the package' — packages/geometry-furniture/src/builders/mergedPartKit.ts exists (§DESK108, founder, 2026-08-26), imports mergeGeometries from @pryzm/renderer-three, exposes boxAt/cylAt/placeParts/mergePartsToMesh so builders emit ONE mesh per MATERIAL GROUP, and is imported by eight builders: WorkingDeskBuilders, DiningSetBuilders, SectionalSofaBuilder, WhiteSofaBuilder, BookshelfBuilder, MediaWallBuilder, AudioBuilders, FurnitureFactory. Its own header states the reason: 'PERF105 measured furniture at 8-20 meshes each with ZERO mergeGeometries calls in this package'. That is the PAST TENSE the auditor quoted as the present. So the magnitude estimate (95 items x 8-20 = 760-1900 of 3762) rests on a superseded measurement and is unsupported on this tree. The '590 new THREE.Mesh( sites' figure I reproduced exactly (590 / lighting 116 / plumbing 54 / wall 40 / slab 26 / stair 16), but it counts CODE SITES across ~100 builders, almost none of which execute for any given item — it ranks nothing about runtime mesh count. The flag facts are all TRUE and verified (FurnitureFragmentBuilder.ts:64 `_instancingEnabled: () => false`, FurnitureInstanceBridge.ts:125-128 `=== true`, initBuilders.ts:57 comment, :817 setInstanceBridge wiring, InstancedElementRenderer.ts:205-224 per-part registration) — but by the auditor's own claimed cost (-1ms) this is a DRAW-SUBMISSION lever, i.e. frame time, which this lens kills. And its cheapest-first fix makes OPEN worse, not better: consolidateSiblingMeshes clones every member geometry and runs mergeGeometries at BUILD time, on the load path. Re-file under a frame-time dimension with a fresh per-item mesh census taken AFTER §DESK108.",
    "#4 REFUTED as an open-time finding, though its headline fact is true and I confirmed it two ways. `grep -rln consolidateSiblingMeshes packages/*/src apps/*/src plugins/*/src` returns exactly three files: the definition, the barrel export (renderer-three/src/index.ts:79), and ONE caller, DoorBuilder.ts:647 (gated behind `this._consolidateMeshes`). The lighting counter-example is also real — LightingFragmentBuilder.ts:704 calls a private `_consolidateFixtureMeshes` defined at :892 with its own mergeGeometries import at :50. (Note for the next lane: the Grep tool returns 'No matches found' for `_consolidateFixtureMeshes` while bash grep finds it — that file trips the known grep-silence class; use bash grep on geometry-lighting.) But the finding establishes NO open cost. The helper is triangle-neutral by its own header ('mergeGeometries concatenates; the triangle census is unchanged and only the submission count falls') — it buys draw calls, which is frame time. And adopting it costs open time: per bucket it does `m.geometry.clone()`, `applyMatrix4`, `mergeGeometries`, then `safeDisposeGeometry` on every original, all inside the builder call that the load replay drives. This is a legitimate frame-time / draw-call finding filed on the wrong axis.",
    "#5 REFUTED as a finding, though every code citation is correct. InstancedElementRenderer.ts:244 `const sharedMaterial = dedupInstanceMaterial(material);` precedes the :251 hash exactly as claimed; SharedMaterialCache.ts:46-52 documents DEFAULT ON and :110-124 matches; ChairBuilder.ts:106-110 and :223 mint a fresh MeshStandardMaterial per call. It costs 0 ms by its own admission and changes no code. Worse, its planning advice — 'do NOT budget a separate material-sharing project for it' — is wrong for the OPEN path specifically. Per-element unique materials still cost at load end on the FRAGMENT arm, which is where 100% of opening-bearing walls and all non-instanced furniture live: PBRSceneUpgrader.upgradeNewMeshes (packages/core-app-model/src/rendering/PBRSceneUpgrader.ts:298-325) iterates every new mesh's material, inserts a snapshot into `_snapshots` and runs `_tuneMaterial` once per UNIQUE material uuid — so 1680 unique wall materials per 200 walls is 1680 Map inserts + 1680 tunes, and that loop runs on the load path. The dedup chokepoint never sees them because they never reach register(). Keep the correction to the memory note (it is accurate about instancing); do not let it close the material-sharing question for the open path.",
    "#7 REFUTED as a finding. Everything it asserts positively I confirmed — pryzmPerfConsole.ts:468-472 buckets on missing elementType; the line branch at :476-482 returns before the mesh branch so projected edges never enter the mesh count; EdgeProjectorService has 0 `new THREE.Mesh(` and 7 `LineSegments(`; ProjectIsolationAudit.ts:416-441 declares pryzm-infinite-grid-3d and __pryzm_ground_shadow_catcher__ (plus levelLinesGroup and bimGridsGroup) with the exact citations quoted. But it establishes no cost (-1ms, confidence medium by its own label), the ONE example that could have mattered — `(anon) < (anon)`, an unnamed mesh parented straight to the scene root — is left unidentified, and its actionable content is 'read a line the founder did not paste'. Under this lens that is a request for evidence, not a finding. Also: the EdgeProjector line in his log (work=109ms, hitRate=0%, cacheEntries=63/5000) is a cold first projection, which the auditor correctly calls expected-on-open — that half is right and it is the reason nothing here converts to a cost."
  ],
  "missed": [
    "THE 500-ELEMENT THRESHOLD THIS PROJECT JUST CROSSED — a SECOND full O(meshes) bounding-sphere sweep on the open path, which #8 says does not exist. packages/core-app-model/src/rendering/FrustumCullingService.ts:62 `const LARGE_MODEL_THRESHOLD = 500;` and :244 `if (!force && count < LARGE_MODEL_THRESHOLD) return;`. The founder's snapshot says 503 elements. getElementCount() counts scene.children carrying userData.id (:205-216), which is the same top-level-root shape the swap's countElements uses — so this project is a handful of elements over the arm and a 499-element project pays none of it. _runAudit (:240-306) then does `for (const child of scene.children) child.traverse(...)` over EVERY mesh under EVERY element root — ~3762 meshes — calling computeBoundingSphere() on each geometry whose sphere is missing or invalid. Its WeakSet skip-set is wiped by reset() on project switch (:184-190), so on a fresh open the cache is empty and every geometry is a candidate. It is triggered on every bim-*-added event and on pryzm-project-loaded with a 1500 ms debounce (:65-73, :233-239), so it fires 1.5 s AFTER the last add — squarely in the tail the founder experiences as 'still opening'. It is NOT gated on isProjectLoadActive(). It prints its own line: `[FrustumCullingService] Audit complete — N element(s), M mesh(es), F sphere(s) recomputed, S skipped, U left uncullable.` That line is in his console and nobody read it. This is a mesh-count-driven open cost with a hard element-count arm, and #8 explicitly claims there is only one such pass left.",
    "THE LOAD-END PBR PASS IS UNCHUNKED WHILE THE BATCH PATH CHUNKS — the clearest place mesh explosion converts into a single main-thread block at open. The BATCH path (initScene.ts:3234-3272) collects new meshes, splits them at `const PBR_CHUNK = 120`, and walks the chunks through `getFrameScheduler().scheduleOnce('p1.3-post-batch-pbr-chunk', upgradeChunk, 'post-render')` — one chunk per frame, with a `totalPbrMs=` readout. The LOAD path does not. `runTierPbrPass('post-load')` at initScene.ts:2952-2954 calls `collectNewPbrMeshes(scene)` — which during a load returns EVERY mesh, since pbrSeenMeshes is a fresh WeakSet — and hands the whole array to `renderingCoordinator.onSceneGeometryAdded(newMeshes)` in ONE synchronous call, no chunking, no yield, no timing print. At 3762 meshes that is 3762 mesh visits plus one snapshot-Map insert and one _tuneMaterial per unique material (PBRSceneUpgrader.ts:298-325) in a single task. The same file's own comment records what this class of pass has cost before: 'the repeated totalPbrMs≈1300ms/943ms post-batch lines' for ~520 meshes. Two identical passes, one chunked and instrumented, one not — and the un-instrumented one is the one that runs on open. This is the cheapest real thing to measure on this dimension and the audit does not mention it.",
    "THE FIRST GPU UPLOAD IS NEVER COUNTED, ONLY THE SWAP'S DUPLICATE OF IT. #1 correctly says the swap forces a SECOND full upload + program setup of 3762 meshes. But the FIRST one is also O(meshes), also on the open path, and is the single most plausible mesh->open link — and the repo's own harness declares it unmeasured in its header: 'NOT MEASURED HERE (needs a browser): GPU upload, shader/PSO compile, first paint, plugin registration, store hydration, culling-service escalation'. That line is printed at the top of the report the auditor read. So the honest statement is not 'the swap is the one clear open consequence of mesh count' — it is 'mesh count has one MEASURED open cost (the wall build delta) and one entirely UNMEASURED one (first-frame upload/compile), and the swap doubles the unmeasured one'. Whoever instruments the swap per #1's fix should bracket the FIRST upload in the same pass, or the swap number will be reported as the whole mesh->open cost when it is the second half of it.",
    "THE CENSUS ITSELF IS AN OPEN-PATH TRAVERSE THAT ONLY EXISTS BECAUSE THE SWAP FIRED. logSceneCensusOnce -> printSceneCensus -> censusScene does a full scene.traverse over all 3762 meshes, reading geometry.index/attributes.position per mesh to accumulate triangles, plus a 22-row sort and ~30 console.log lines — one second after the swap, i.e. inside the open window. It is self-aware about this ('an instrument that walks the scene on a timer would join the population it is measuring') and it is cheap, but it is a real added cost that the founder pays only on the runs where the swap fires, which is exactly the run he is complaining about. If #1's instrumentation lands, time this traverse too rather than leaving it inside the swap's measured window and mis-attributing it."
  ]
}