{
  "survivingTitles": [
    "serialize() unconditionally computes a 1.46 MB canonical-string integrity checksum — including for the two dirty-check-only callers that throw it away",
    "The FIRST save of every session deflates ~1.5 MB on the main thread, because the compression worker is lazily constructed and isReady() is guaranteed false on its first call",
    "Two thumbnail captures per open, from two distinct call sites — each forces a synchronous full render of the 3762-mesh scene, a GPU readback, and up to six WebP encodes",
    "The journal-retention valve cannot fire for this project — it needs the 20-slot version ring to EVICT, and the founder has 6 versions",
    "\"temporalGraph 2366 mutations / 0 edges\" is EXPECTED, not a bug — recordEdge has zero production callers — but the journal is still ~32% of every snapshot",
    "DE-RANKED: \"6 versions in one write\" is real for I/O but is NOT where the time goes — re-compression is already incremental, and the whole-container JSON legs measure 8 ms"
  ],
  "refutedTitles": [
    "temporalGraph recording RESUMES before the post-load rebuild sweep — so every open permanently grows the journal AND is why an Auto-save is minted on every open"
  ],
  "refutations": [
    "FINDING 2 — REFUTED AS TITLED. The structural half is exactly right and I confirmed every line: suspendRecording() at ProjectLoader.ts:759, resumeRecording() at :2902 inside the finally immediately after storeEventBus.endBatch(), and the sweep genuinely follows it (wallRebuildControl.resumeAndFlush() at :2745, topologyObserver.resume() at :2951, the chunked room.redetect dispatch loop from :3010). TemporalGraph.ts:94-106 subscribes to StoreEventBus and :429/:441 push a record for every create/update/delete not under suspension, and ProjectSerializer.ts:1897 embeds the whole journal in every snapshot. All true. BUT THE CAUSAL CLAUSE IN THE TITLE IS FALSE TWICE OVER, and it is the clause that drives the fix. (a) The autosave is not triggered by the journal at all. The dirty flag comes from window MUTATION_EVENTS (SaveOrchestrator.ts:88-130, handleMutation at :303, hasDirtyChanges set at :311 while _loadSuppressActive, re-armed by handleLoadSuppressEnd at :536-543). The temporalGraph is not consulted anywhere in that chain. Silencing the journal for the sweep leaves every one of those bim-* events firing, so the save still arms. (b) The hash guard the fix relies on CANNOT PASS. ProjectSerializer.serialize() stamps `timestamp: Date.now()` as a top-level snapshot member (ProjectSerializer.ts:1786), ProjectSerializer.stringify is a bare `JSON.stringify(snapshot)` (:2093-2102), and getHash() returns that string verbatim (PlatformSaveController.ts:105-119). resetDirtyAfterLoad()'s baseline is therefore taken at load-end and executeSave()'s comparison hash 2500 ms later (DEBOUNCE_MS, SaveOrchestrator.ts:254), with a different Date.now() baked in. `currentHash === this.lastHash` at :383 can never be true. So the stated second-order benefit — 'the whole post-load autosave stops firing at all on an open with no user edits' — is unreachable no matter what the model does. A lane sent to move resumeRecording() expecting the Auto-save to disappear will move it, verify nothing changed, and conclude the analysis was wrong. WHAT TO RE-FILE: the journal-growth mechanism alone, with claimed cost stated as compounding-per-open (it inflates the journal-attach leg on every subsequent open and ~32% of every serialize/stringify/checksum/deflate) and with NO claim about the Auto-save. The Auto-save has a different root cause — see missed item 1.",
    "FINDING 1 — SURVIVES, cost halved. Not a refutation: mechanism confirmed line-for-line (ProjectSerializer.ts:2039 unconditional, no guard/option; SnapshotIntegrity.ts:361-363 stamp(); PlatformSaveController.ts:112-116; SaveOrchestrator.ts:589-591; PlatformVersionController.ts:411-412 is synchronous, between overlay.remove() and the pryzm-project-loaded emit at :419 — so serialize #1 genuinely blocks the open). But the 138 ms claim double-counts. Exactly TWO serializes run on the open path, and only ONE of them is throwaway: the second (SaveOrchestrator.ts:382) stashes its snapshot, and saveVersionInternal REUSES that exact object as the persisted VersionRecord (PlatformSaveController.ts:250-269), so its checksum is the stamp that actually ships. Net recoverable = one checksum walk ≈ 69 ms by the auditor's own scaling, not 138 ms. Also: the sub-claim 'it fires on every autosave cycle whose hash turns out UNCHANGED' is void — that branch is unreachable (see missed item 1), so there are no no-op autosaves to save. And 0x16588A decodes to 1,464,458, not 1,464,970.",
    "FINDING 3 — SURVIVES, fix is incomplete. Not a refutation: confirmed there is exactly ONE production caller of getCompressWorkerPool() in the entire repo (ProjectRepository.ts:2646) — nothing warms it at boot, so isReady() is provably false for the first save and ProjectRepository.ts:2712's synchronous _compressJSON loop is guaranteed to run once per session, on the open. BUT warming the pool does not buy the auditor's own '48 ms journal TAIL chunk'. _planJournal compresses the tail chunk with a direct synchronous `_compressJSON(text)` at ProjectRepository.ts:652, on the main thread, on EVERY save, with no worker route at all — it never reaches the pool.isReady() branch. The pool-warm removes the version-blob deflate only. If the tail-chunk deflate is wanted off-thread it needs its own change.",
    "FINDING 4 — SURVIVES on the two-call-sites core; two cost components are misread. Not a refutation of the title: both sites are real and both fire on an open (PlatformSaveController.ts:357 autosave, :551 post-load via schedulePostLoadThumbnailCapture at :521 with CAPTURE_DELAY_MS=3500), a repo-wide grep confirms no others, and each pays a forced synchronous `rpm.render()` of the whole scene (initPersistence.ts:175) — that forced render IS the expensive part and deduping is the right call. But: (i) initPersistence.ts:219 (not :218) `ctx.getImageData(sampleX, sampleY, 32, 32)` reads a 32x32 patch of an ALREADY-DOWNSCALED 2D canvas capped at 400x225 (:190-201), not a GPU->CPU readback of the scene — the GPU transfer is the single `drawImage(src,...)` into that 400x225 target; (ii) fitThumbnailToBudget is FIRST-FIT and returns on the first rung under 65,536 chars (thumbnailBudget.ts:117-130) — a 400x225 WebP at q=0.72 lands well inside that, so rung 1 is the realistic case and 'up to six WebP encodes' is a bound never reached. Treat 250 ms as an unbacked estimate dominated by two full scene renders, and measure it before ranking it above the ~69 ms checksum.",
    "FINDING 5 — SURVIVES, cost UNDERSTATED not overstated. Not a refutation: every line checks out (MAX_VERSIONS_STORED=20 at :153, the rel cursor moves only under `slots.length > kept.length` at :2285, _releaseJournalHead returns null on `!j.rel || j.rel <= 0` at :1878 and on `dropChunks <= 0` at :1890, JOURNAL_CHUNK_RECORDS=2000 at :268). At 6 versions the ring has never evicted, so rel is 0 and nothing has ever been released — correct. But the 14 ms figure is low: the repo's own measurement of this leg, in the comment directly above the call, is 'measured at founder scale (perf104OpenPath.spec.ts): 89 ms cold, of which 80 ms is journal re-attach' (PlatformShell.ts:295). _readJournalRecords inflates AND FNV-digest-verifies every chunk on a cold mirror (ProjectRepository.ts:579-604), synchronously, before the loader sees an element."
  ],
  "missed": [
    "THE ACTUAL ROOT CAUSE OF 'Auto-save on every open': the dirty-check hash contains a wall-clock timestamp, so it can never match. ProjectSerializer.serialize() writes `timestamp: Date.now()` as a top-level snapshot member (ProjectSerializer.ts:1786); it is NOT in CHECKSUM_EXCLUDED_TOP_KEYS (SnapshotIntegrity.ts:190 holds exactly 'integrity' and 'versionLabel'); ProjectSerializer.stringify is a bare JSON.stringify (:2093-2102); getHash() returns that string unmodified (PlatformSaveController.ts:105-119). Consequence: `if (this.lastHash !== '' && currentHash === this.lastHash)` at SaveOrchestrator.ts:383 is DEAD CODE, and so is the identical guard in flushBeforeUnload() at :430. Every armed autosave therefore proceeds to a full write — serialize #2 + checksum + 1.4 MB deflate + whole-container assemble + IDB put + thumbnail + server POST — even when the model is byte-identical to what was just loaded. This is the single highest-leverage fix on this dimension and no finding names it. It also subsumes finding 2's fix: fix this and the guard starts working; fix finding 2 alone and nothing changes. Fix shape: hash a shallow copy with `timestamp` (and `integrity`) removed, e.g. a JSON.stringify replacer in getHash() only. DO NOT fix it by adding `timestamp` to CHECKSUM_EXCLUDED_TOP_KEYS — that changes the canonical form for every stored stamp and re-detonates L-360 (loader recomputes with a new exclusion set over old snapshots -> false 'corrupt').",
    "THE ONLY PERSISTENCE LEG THAT IS STRICTLY PRE-PAINT, and no finding names it. versionRepository.getLatestVersion() runs synchronously between markStartupPhase('open:version-read-start') and ('open:version-read-done') at PlatformShell.ts:296-298, and does, in order: mirror read -> _parseContainer of the whole 1.4 MB container (ProjectRepository.ts:1975) -> _releaseJournalHead -> _decompressJSON inflate of the newest 1.4 MB blob (:1997) -> JSON.parse of it (:1999) -> _readJournalRecords, which inflates and FNV-digest-verifies EVERY journal chunk (:2006, impl :579-604) -> attachJournalMutations. The code prints its own per-leg breakdown as '[VersionRepository] §PROBE-OPEN-PATH-STORAGE-LEG ... mirror-read N ms, envelope-parse N ms, inflate N ms, record-parse N ms, journal-attach N ms = N ms before the loader has seen a single element' (:2043-2058), and the repo's own founder-scale number for it is 89 ms cold / 80 ms journal re-attach (PlatformShell.ts:295). CALIBRATION THE NEXT LANE NEEDS: findings 1(second serialize), 3 and 4 all land AFTER first paint (the post-load autosave is DEBOUNCE_MS=2500 behind load-suppress-end, SaveOrchestrator.ts:254; the second thumbnail is 3500 ms behind, PlatformSaveController.ts:527). Only finding 1's FIRST serialize and this read leg are genuinely inside the 'takes too long to OPEN' window. The founder's console already contains the §PROBE-OPEN-PATH-STORAGE-LEG line; it is missing from the pasted excerpt — ask for it rather than estimating.",
    "The journal tail-chunk deflate is UNCONDITIONALLY synchronous on the main thread on EVERY save — not just the first of the session. _planJournal re-compresses the unsealed tail with a direct `_compressJSON(text)` at ProjectRepository.ts:652 (sealed chunks are correctly reused as bytes at :649-651, so the incremental claim holds for the sealed prefix only). This never touches CompressWorkerPool, so finding 3's boot-warm leaves it entirely in place. On the auditor's own perf104 run this is 48 ms; at the founder's 2366 records the tail chunk is 366 records and cheaper, but it grows to a full 2000-record chunk deflate every 2000 records forever.",
    "Both save paths call projectRepository.listProjects() (PlatformSaveController.ts:362 and :557), which goes through listAllProjectsUnfiltered() and JSON.parses the whole `bim-projects-index` from localStorage — an index that carries every project's base64 thumbnail at up to 65,536 chars each (THUMBNAIL_MAX_CHARS). Two full index parses per open, both to `.find()` one row. Small on a two-project account, linear in hub size, and trivially fixable with a by-id read. Not a headline, but it is on this dimension and was not counted."
  ]
}