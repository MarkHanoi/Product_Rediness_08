{
  "dimension": "Rooms as a first-class Visibility-Intent category with a colour override (\"THE ROOMS SHOULD HAVE A CATEGORY ON THE VISIBILITY INTENT SETTINGS TO MODIFY THE COLOURS - THAT I DONT SEE\")",
  "summary": "The founder is right, and the reason is precise: **`room` IS already a first-class category — in the WRONG system.** Lane ROOM1 (L-1610…L-1617, 2026-08-21) made `room` a category on `vgGovernanceStore`, which is marked `@deprecated Contract 25b — superseded by VisibilityIntent` (VGGovernanceStore.ts:23-28) and is read by **no UI panel anywhere in `apps/editor/src/ui`**. The panel the founder is looking at — \"VISIBILITY INTENT · Architectural Documentation (Auto)\" — renders its category rows from `Object.keys(intent.elementRules)` (VisibilityIntentPanel.ts:240), which is seeded from `VisibilityIntentDefaults.ELEMENT_TYPES` (:15-41), a 19-entry list with **no `room`**. So the capability was built and persisted, and the only door to it is a button row inside the *room* property inspector that requires selecting a room first (RoomPropertySection.ts:689). Classic [[authored-but-unwired]] — except this time it is authored, wired, and landed in a registry the user's surface does not read.\n\n(a) The \"14 VG CATEGORIES (max 56)\" in the log is `CATEGORY_TO_DXF_LAYER` (VGSceneApplicator.ts:87-102): wall, slab, column, beam, door, window, curtain-wall, curtain-panel, stair, handrail, roof, furniture, plumbing, ceiling. **No room** (correct — that map is ISO *linework* layers) and **no lighting** (the sibling lane's bug).\n\n(b) The pastel fills are `PlanViewCanvas._renderRoomFills()` (:2488-2534) → `RoomColourSystem.resolveForMode(room, 'detection', …)` → `resolve()` → `room.colour ?? OCCUPANCY_PALETTE[occupancyType]`. The palette hexes match the screenshot exactly: `#F8BBD9` pink, `#FFF9C4` cream, `#C8E6C9` green, `#B8D4F0` blue (RoomColourSystem.ts:114-150). Three rivals ruled out by reading: the compliance overlay paints `#ef4444`/`#f59e0b` onto **3D meshes** (RoomBoundaryBuilder.ts:556-560); poché has no room entry (`ISO_CUT_LAYER_TO_POCHE_FILL` = 6 wall/structural layers only); room envelopes are not painted in plan at all (`grep -i spaceEnvelope PlanViewCanvas.ts` → zero hits).\n\n(c) **CRUX ANSWERED: rooms sit outside the projection-layer system, but this is NOT a full re-homing.** `_renderRoomFills` reads `window.roomStore` directly and strokes canvas-2D *before* the `drawing.three` traverse; it never touches an ISO layer, never gets `styleResolver`, and `applyToProjectionLayers` never sees it. BUT a single seam already exists and every live painter goes through it: `resolveRoomColourIntent()` (RoomColourIntent.ts:127), with exactly **three live callers** (PlanViewCanvas:2504, RoomBoundaryBuilder:251, RoomPropertySection:683) plus one dead one. Re-point that one function at `visibilityIntentStore` and the intent-panel row governs both plan and 3D with zero changes at the painters.\n\n(d) CROSS-CHECK: rooms are ALREADY in all three *element-list* registries the envelope lane needed (inspectCategories:133, ElementsSummarySection:54, BrowserDataHelpers:323/421/612/657/713). But the *visibility/graphics* side is a **different set of registries**, and there are FOUR that need a room row, not three — see findings. `OverridePanel.CATEGORIES` (13 entries, no room) is the one most likely to be missed because no earlier lane touched it for rooms.",
  "findings": [
    {
      "title": "`room` is a first-class category in the DEPRECATED vgGovernanceStore, and absent from the VisibilityIntent list the founder's panel actually renders",
      "mechanism": "Two rival category systems. `VGGovernanceStore.ts:23-28` declares itself `@deprecated Contract 25b — VG templates and per-category styles are superseded by VisibilityIntent records in visibilityIntentStore … Do not add new importers`, yet it is where lane ROOM1 put `room` (four built-in templates, VGGovernanceStore.ts:148/177/206/235). The founder's panel is the OTHER system: `ViewPropertiesPanelBuilders.ts:78` renders the 'Visibility Intent' row and :196 opens `visibilityIntentPanel`; that panel builds its category rows at `VisibilityIntentPanel.ts:240` from `Object.keys(intent.elementRules)`; intents are seeded at `SystemIntents.ts:42` `const baseRules = cloneDefaultElementGraphicsRules()` → `VisibilityIntentDefaults.ts:131` reduce over `ELEMENT_TYPES` (:15-41). That list is `__default__, wall, slab, column, structural, beam, door, window, stair, roof, ceiling, furniture, plumbing, grid, annotation, level, boundary-line, ifc-element` — 18 + default, no `room`. Confirmed no UI reads vgGovernanceStore: `grep -rln vgGovernanceStore apps/editor/src/ui` returns ONE file, `inspectCategories.ts`, and its only hit is line 196, a string explaining why that store is NOT an element family. So the room category has no panel at all.",
      "evidence": [
        "packages/core-app-model/src/presentation/VGGovernanceStore.ts:23-28 — \"@deprecated Contract 25b — VG templates and per-category styles are superseded by `VisibilityIntent` records in `visibilityIntentStore`. … New authoring code MUST use the Visibility Intent system. Do not add new importers.\"",
        "packages/core-app-model/src/presentation/VGGovernanceStore.ts:148 — \"room:          { fillColor: '#ffffff', edgeColor: '#bbbbbb', lineWeight: 1, transparency: 0,  visible: true, halftone: false, roomColourMode: 'detection' },\" (repeated at :177, :206, :235 for the other three built-in templates)",
        "packages/core-app-model/src/presentation/VisibilityIntentDefaults.ts:15-41 — `const ELEMENT_TYPES = ['__default__','wall','slab','column','structural','beam','door','window','stair','roof','ceiling','furniture','plumbing','grid','annotation','level','boundary-line','ifc-element'] as const;` — the comment at :33 says it outright: \"THIS is where a category is DECLARED: every element type in this list gets a complete `ElementGraphicsRules` … in every system intent\"",
        "apps/editor/src/ui/VisibilityIntentPanel.ts:240 — \"const elementTypes = Object.keys(intent.elementRules);\" then :253 \"${elementTypes.map(type => {\" renders one clickable row per key",
        "packages/core-app-model/src/presentation/SystemIntents.ts:42 — \"const baseRules = cloneDefaultElementGraphicsRules();\"",
        "apps/editor/src/ui/ViewPropertiesPanelBuilders.ts:78 \"titleText.textContent = 'Visibility Intent';\" and :196 \"window.visibilityIntentPanel?.open?.(instance?.intentId ?? intents[0]?.id, def.id);\"",
        "`grep -rln vgGovernanceStore apps/editor/src/ui` → only apps/editor/src/ui/inspect/audit/inspectCategories.ts:196, which is the exclusion string \"vgGovernanceStore: 'Visibility/graphics governance (P7 intent).'\" — i.e. no panel reads it"
      ],
      "userVisibleSymptom": "The Visibility Intent panel lists 19 category rows and none of them is Rooms, so there is no way to change room colours from the visibility settings. The only control that exists (\"Colour by\": Room type / Size / User-defined / All white / Default / Sync state) is buried in the Appearance card of the ROOM property inspector and requires selecting a room first.",
      "fix": "Add `'room'` to `ELEMENT_TYPES` in `packages/core-app-model/src/presentation/VisibilityIntentDefaults.ts:15` — that alone makes the row appear (fill style / fill colour / fill opacity controls already exist at VisibilityIntentPanel.ts:351-362). ⛔ BUT DO NOT SHIP THAT ALONE: the row would govern nothing, because `_renderRoomFills` reads `resolveRoomColourIntent()` → `vgGovernanceStore`, not `visibilityIntentStore`. The row and the seam must move in the same commit — see the 'styling seam' finding. `resolvePen(zone,'room')` safely returns `FALLBACK_PEN` (PenWeightTable.ts:307) and `defaultPocheFillForCategory('room')` returns null, so the mechanical addition is low-risk.",
      "alreadyExists": "EVERYTHING except the join. `RoomColourIntent.ts` (the whole mode model, `ROOM_COLOUR_MODE_CHOICES` with founder-authored labels/hints), `SetRoomColourModeCommand` (with correct undo), bus verb `room.setColourMode`, `plugins/rooms/src/handlers/SetRoomColourMode.ts`, per-view + per-model scope, project-snapshot persistence (ProjectSerializer.ts:1837 / ProjectLoader.ts:2307), `RoomColourSystem.resolveForMode` with all six modes, and a passing test suite `VGSceneApplicator.roomCategory.test.ts` + `planViewRoomColourModes.test.ts`. Do not rebuild any of it.",
      "effort": "small",
      "risk": "low",
      "confidence": "high"
    },
    {
      "title": "THE STYLING SEAM IS ONE FUNCTION — `resolveRoomColourIntent()` — with exactly three live callers",
      "mechanism": "Both live room painters and the UI read-back call the same resolver, which hard-codes the deprecated store: `RoomColourIntent.ts:132` `const resolved = vgGovernanceStore.resolveStyle(modelId, ROOM_VG_CATEGORY, viewId);`. `grep -rn 'resolveRoomColourIntent|activeRoomColourIntent'` over packages+apps+plugins+src, excluding its own file and tests, returns exactly four call sites: PlanViewCanvas.ts:2504 (the 2D plan wash), RoomBoundaryBuilder.ts:251 (the 3D room meshes), RoomPropertySection.ts:683 (the UI reading back the current mode), and PlanViewFillRenderer.ts:40 (dead — see separate finding). That is a genuinely narrow seam: teaching this ONE function to consult `visibilityIntentStore` first (falling back to vgGovernanceStore for projects saved before the change) makes an intent-panel `room` row govern plan AND 3D without touching either painter.",
      "evidence": [
        "packages/core-app-model/src/presentation/RoomColourIntent.ts:127-144 — `export function resolveRoomColourIntent(modelId: string, viewId?: string): RoomColourIntent { … const resolved = vgGovernanceStore.resolveStyle(modelId, ROOM_VG_CATEGORY, viewId); const raw = (resolved.style as { roomColourMode?: unknown }).roomColourMode; return { mode: isRoomColourMode(raw) ? raw : DEFAULT_ROOM_COLOUR_MODE, uniformColour: normaliseUniform(resolved.style.fillColor, resolved.source), visible: resolved.style.visible !== false, transparency: Number(resolved.style.transparency ?? 0), source: resolved.source }; }`",
        "packages/core-app-model/src/views/PlanViewCanvas.ts:2504 — \"const intent = resolveRoomColourIntent(getRoomColourModelId(), this._lastViewId ?? undefined);\"",
        "packages/room-topology/src/RoomBoundaryBuilder.ts:251 — \"return activeRoomColourIntent();\" (inside `_readIntentOrNull`, feeding `syncFromViewIntent()` at :664-669)",
        "apps/editor/src/ui/property-inspector/RoomPropertySection.ts:683 — \"return resolveRoomColourIntent(getRoomColourModelId(), scope).mode;\"",
        "packages/core-app-model/src/presentation/IntentRuleResolver.ts:94 — \"const exact = intent.elementRules[elementType];\" — the corresponding read on the intent side, already generic over elementType"
      ],
      "userVisibleSymptom": "Nothing the user does in Visibility Intent can reach the room wash, because the wash asks a different store. Conversely, the \"Colour by\" buttons work but are invisible unless a room is selected.",
      "fix": "In `RoomColourIntent.ts:127`, resolve through the intent chain FIRST: read `viewIntentInstanceStore.get(viewId)` → `visibilityIntentStore.get(intentId)` → `resolveIntentStyle(instance, intent, 'room', 'cut', viewType, {category:'room'})` for `visible` / `fill.opacity` / `fill.colour`, and keep `vgGovernanceStore` as the fallback + as the home for `roomColourMode` until a home for the mode exists on `ElementStateAppearance`. ⚠ `roomColourMode` has NO slot in `ElementGraphicsRules` / `ElementStateAppearance` — this is the one genuinely new piece of schema the lane needs, and it is a DETERMINATION RULE, not a colour, so do not try to squeeze it into `fill.colour`. RoomColourIntent.ts:29-36 already argues this in writing.",
      "alreadyExists": "`resolveIntentStyle` / `IntentRuleResolver.ts:94-105` is the generic per-elementType resolver, already used by the 2D canvas pen path (PlanViewCanvas.ts:459). `VGToIntentMigration.ts:166-168` already migrates VG template categories into `elementRules` and, because of `VG_CATEGORY_TO_ELEMENT_TYPE[vgCategory] ?? vgCategory` at :167, would carry a `room` key through — though it drops `roomColourMode`, which is not part of `ElementGraphicsRules`.",
      "effort": "medium",
      "risk": "medium",
      "confidence": "high"
    },
    {
      "title": "`OverridePanel.CATEGORIES` is a FOURTH category registry with no room row — the one this lane is most likely to miss",
      "mechanism": "The envelope lane found three registries (inspect / project tree / left-rail elements). Those are ELEMENT-LIST registries and rooms are already in all three. The VISIBILITY side has its own separate set, and `OverridePanel.ts:247-267` is a hand-curated 13-entry array feeding the \"Categories in this view\" tick-list that dispatches `view.setCategoryVisibility`. It contains wall, slab, door, window, stair, roof, column, beam, furniture, curtainwall, railing, ceiling, boundary-line — no room, and also no lighting, plumbing, grid or level. Its own comment (:239-245) states the design rule that makes it a permanent rot site: \"A curated list, not a scan of what happens to be in the model\". Nothing gates it: `InspectCategoryCoverage.test.ts` ARM A enumerates families by scanning `window.<x>Store =` assignments and compares against `INSPECT_CATEGORIES` only — it never looks at `OverridePanel.CATEGORIES` or `VisibilityIntentDefaults.ELEMENT_TYPES`.",
      "evidence": [
        "apps/editor/src/ui/OverridePanel.ts:247-267 — `private static readonly CATEGORIES: ReadonlyArray<{ id: string; label: string }> = [ { id: 'wall', label: 'Walls' }, … { id: 'ceiling', label: 'Ceilings' }, { id: 'boundary-line', label: 'Boundary lines' } ];` — 13 entries, no room",
        "apps/editor/src/ui/OverridePanel.ts:239-245 — \"The categories offered as quick toggles. A curated list, not a scan of what happens to be in the model: the control must be STABLE across projects and must offer a category even when the current project has none of it yet\"",
        "apps/editor/src/ui/OverridePanel.ts:232 — \"<div class=\\\"ov-section-title\\\">Categories in this view</div>\" and :234 \"Unticking hides the whole category in this view only.\"",
        "apps/editor/__tests__/InspectCategoryCoverage.test.ts:44-54 — `publishedStoreGlobals()` scans `/^\\s*window\\.(\\w+Store)\\s*=/gm` over `apps/editor/src/engine` and compares only against `INSPECT_CATEGORIES` / `NON_ELEMENT_STORE_GLOBALS`",
        "apps/editor/src/ui/inspect/audit/inspectCategories.ts:133 — \"{ id: 'rooms', label: 'Rooms', icon: '▪', storeKey: 'roomStore', meshType: 'room' },\" — rooms ARE in the element-list registry, which is why this gap is easy to believe is already closed"
      ],
      "userVisibleSymptom": "The Overrides panel's \"Categories in this view\" list has no Rooms tick-box, so a user cannot turn the room wash off for one view from there either — a second surface where \"I don't see it\" is literally true.",
      "fix": "Add `{ id: 'room', label: 'Rooms' }` to `OverridePanel.CATEGORIES` (OverridePanel.ts:247). `targetKind:'elementType'` matching happens in `IntentRuleResolver.targetMatches()` and `RoomBoundaryBuilder` stamps `userData.elementType = 'room'` (per VGSceneApplicator.ts:143-145), so the id matches. ⚠ VERIFY AT THE OUTCOME: hiding the `room` category via intent overrides will NOT hide the 2D wash today, because `_renderRoomFills` only checks `intent.visible` from `resolveRoomColourIntent`, not the intent-override layer — the same seam as the previous finding.",
      "alreadyExists": "`SetCategoryVisibilityInViewCommand` (packages/command-registry/src/vg/) + bus verb `view.setCategoryVisibility` (initBusHandlers.ts:2443) + a real end-to-end proof at `apps/editor/src/ui/__tests__/perCategoryViewVisibility.spec.ts`, which asserts through `resolveIntentStyle`, not a command return value. The whole mechanism is built; only the row is missing.",
      "effort": "small",
      "risk": "low",
      "confidence": "high"
    },
    {
      "title": "`PlanViewFillRenderer` is a DEAD duplicate room painter with zero importers",
      "mechanism": "`apps/editor/src/engine/views/plan-canvas/PlanViewFillRenderer.ts:268` exports `export const planViewFillRenderer = new PlanViewFillRenderer();`. `grep -rn 'planViewFillRenderer' --include=*.ts apps packages src plugins` (excluding node_modules) returns exactly ONE line: that declaration. `grep -rn 'PlanViewFillRenderer'` returns only the class declaration, the singleton, and three COMMENTS that reference it as if it were live (initScene.ts:1003 \"…the CANVAS (PlanViewFillRenderer.renderRoomFills), so hiding this 3-D scene overlay…\"; DrawingLayerIdentity.ts:14; RoomStore.ts:29). Its own header at :20-27 calls itself a rival that was deliberately kept in sync (\"These two are duplicate painters of the same rooms; leaving this one on the MODE-LESS RoomColourSystem.resolve() would make it a silent rival\"). So a past lane spent effort keeping a dead file correct, and three comments now point future readers at a file that runs nowhere.",
      "evidence": [
        "apps/editor/src/engine/views/plan-canvas/PlanViewFillRenderer.ts:268 — \"export const planViewFillRenderer = new PlanViewFillRenderer();\" — and this is the ONLY occurrence of that identifier in the repo",
        "apps/editor/src/engine/views/plan-canvas/PlanViewFillRenderer.ts:22-27 — \"§ROOM-VG-CATEGORY (L-1612) -- resolves the room colour mode from the `room` VG category exactly as `PlanViewCanvas._renderRoomFills()` does. These two are duplicate painters of the same rooms\"",
        "apps/editor/src/engine/initScene.ts:1003 — \"// CANVAS (PlanViewFillRenderer.renderRoomFills), so hiding this 3-D scene overlay\" — a comment asserting a live role the grep does not support",
        "packages/core-app-model/src/views/PlanViewCanvas.ts:383 and :1959 — \"if (isPlanLike) this._renderRoomFills(ctx);\" — the two calls that ARE live"
      ],
      "userVisibleSymptom": "None directly — but it is a trap for this lane: an agent that 'fixes rooms' in PlanViewFillRenderer will ship a change that runs nowhere and reports success. This repo has a documented history of exactly that ([[committed-is-not-reachable]]).",
      "fix": "Do not edit PlanViewFillRenderer for this lane. Either delete it (and `renderPocheFills` with it — check that separately, it may share the same fate) or, at minimum, correct the three comments that describe it as the live plan painter. Verify with `grep -rn 'planViewFillRenderer'` before assuming anything about it.",
      "alreadyExists": "n/a — this is removal, not construction.",
      "effort": "small",
      "risk": "low",
      "confidence": "high"
    },
    {
      "title": "'Colour by room type' cannot actually change any colour — `OCCUPANCY_PALETTE` is a hardcoded table with no write path",
      "mechanism": "The founder said \"modify the colours\". Of the six modes, `occupancy` (labelled \"Room type\", and the first in `ROOM_COLOUR_MODE_CHOICES`) resolves to `OCCUPANCY_PALETTE[room.occupancyType]` — a `Record<RoomOccupancyType, string>` literal at RoomColourSystem.ts:114 with ~46 entries. Grepping every reference across packages/apps/plugins/src shows only READS: RoomColourSystem itself (3 sites), RoomSystemTypeStore.ts:57/126, spaceEnvelopeAppearance.ts:150, residentialRoomLibrary.ts:403, RoomPropertySection.ts:163, plus tests. There is no setter, no store, no persistence, no UI. Under `uniform` mode the user gets ONE flat colour (from the category `fillColor`); under `custom` they get per-room colours via `room.update`. Between those, per-room-TYPE colour — the thing an architect actually wants a legend for — is unauthorable.",
      "evidence": [
        "packages/room-topology/src/RoomColourSystem.ts:114-150 — \"export const OCCUPANCY_PALETTE: Record<RoomOccupancyType, string> = { 'bedroom': '#B8D4F0', 'living-room': '#A8C8E8', 'kitchen': '#B8D4F0', 'bathroom': '#B2EBF2', … 'patient-room': '#F8BBD9', 'classroom': '#E1BEE7', … };\"",
        "packages/room-topology/src/RoomColourSystem.ts:229 — \"case 'occupancy': return OCCUPANCY_PALETTE[room.occupancyType] ?? UNCLASSIFIED_FILL;\"",
        "packages/core-app-model/src/presentation/RoomColourIntent.ts:91 — \"{ mode: 'occupancy',  label: 'Room type',  hint: 'Colour by occupancy type (bedroom, office, corridor…)' },\"",
        "packages/room-topology/src/RoomAutoFillClassifier.ts:89 — \"…it would touch the Zod schema, `OCCUPANCY_PALETTE` (an EXHAUSTIVE…\" — the palette is exhaustive over the union by type, so adding an entry is a typed change, not a data change",
        "`grep -rn OCCUPANCY_PALETTE` across packages/apps/plugins/src: 7 non-test consumer sites, all reads; zero assignments"
      ],
      "userVisibleSymptom": "Selecting \"Room type\" recolours the plan, but there is no swatch anywhere the user can click to say \"bedrooms should be beige, not blue\". If the founder's \"modify the colours\" means per-type colours (his own L-1610 words were \"colour code by room type\"), a single `fill.colour` on a `room` intent row does NOT satisfy it.",
      "fix": "DECISION NEEDED BEFORE CODING — ask the founder whether he wants (i) one colour for all rooms (a `fill.colour` on the `room` intent row, which is what a category row naturally gives), or (ii) an editable per-occupancy-type palette (a real legend/override table). If (ii): the honest home is a per-model/per-view override map on the same cascade as `roomColourMode`, resolved in `RoomColourSystem.resolveForMode`'s `occupancy` branch (RoomColourSystem.ts:229) as `overrides[type] ?? OCCUPANCY_PALETTE[type] ?? UNCLASSIFIED_FILL`. ⛔ Do not fork OCCUPANCY_PALETTE into a second table — `residentialRoomLibrary.ts:395` already records the lesson (\"IT IS `OCCUPANCY_PALETTE`, READ AT CALL TIME, AND THAT IS THE WHOLE POINT\").",
      "alreadyExists": "The cascade to hang an override map on (`setViewCategoryOverride` / `setModelCategoryOverride`, both persisted). `RoomSystemTypeStore.ts:57` already derives a per-system-type colour from the palette and may be a better home than a new store — read it before designing.",
      "effort": "medium",
      "risk": "medium",
      "confidence": "high"
    },
    {
      "title": "Room fills are gated to `isPlanLike` — elevation and section render no room region at all",
      "mechanism": "`PlanViewCanvas.render()` computes `const isPlanLike = this._viewType === 'plan' || this._viewType === 'structural-plan' || this._viewType === 'ceiling-plan' || this._viewType === 'detail';` at :378-379 (and identically at :1953-1955 in the second render path), then `if (isPlanLike) this._renderRoomFills(ctx);`. Sections and elevations therefore never call the room painter. This is not new and it is not accidental — the ISSUE-LOG entry for this exact family names it as the deliberate NOT-DONE, with the reason.",
      "evidence": [
        "packages/core-app-model/src/views/PlanViewCanvas.ts:378-383 — \"const isPlanLike = this._viewType === 'plan' || this._viewType === 'structural-plan' || this._viewType === 'ceiling-plan' || this._viewType === 'detail'; … if (isPlanLike) this._renderRoomFills(ctx);\"",
        "packages/core-app-model/src/views/PlanViewCanvas.ts:1953-1959 — the same two lines, duplicated in the second render path",
        "docs/04-reference/ISSUE-LOG.md L-1610…L-1617 — \"**NOT done, named:** **2-D elevation/section drawings render no room region at all** (`isPlanLike` gate). Rooms *are* mode-driven in elevation/section/3-D via the room **volume meshes**, but a room poché in a vertical 2-D drawing needs a room-volume/cut-plane intersection producer that does not exist. ⛔ A bounding rectangle would put a room wash where the cut does not pass through the room — an **overstatement, not a feature**.\""
      ],
      "userVisibleSymptom": "Room colour-coding appears in plan but vanishes in section and elevation, which reads as inconsistent even though it is a deliberate refusal.",
      "fix": "LEAVE IT REFUSED unless the founder asks for it explicitly. The ISSUE-LOG's reasoning is sound and matches [[envelope-solid-overstates-partial-data]]: a bounding rectangle would draw a wash where the cut plane does not pass through the room. If it IS wanted, the missing piece is a room-volume ∩ cut-plane polygon producer; `packages/room-topology/src/RoomPolygonUtils.ts` and the room volume meshes in `RoomBoundaryBuilder` are the inputs. Do not slip this into a category-registry fix.",
      "alreadyExists": "Room volume meshes already exist and ARE mode-driven in 3D (RoomBoundaryBuilder.ts:251/301). Only the 2D vertical intersection is missing.",
      "effort": "large",
      "risk": "high",
      "confidence": "high"
    },
    {
      "title": "`ELEMENT_TYPE_REGISTRY` is a rival to `VisibilityIntentDefaults.ELEMENT_TYPES` and its header states a rule that is false",
      "mechanism": "`packages/core-app-model/src/presentation/ElementTypeRegistry.ts:31` declares `ELEMENT_TYPE_REGISTRY` with 19 keys + `__default__`, and its header at :14 claims \"Contract 25a §3.1 — Adding new element type requires one entry here only\". That is not true: the panel's rows come from `VisibilityIntentDefaults.ELEMENT_TYPES`, not from this registry, and the two lists have already diverged in BOTH directions — this registry has `structural-wall`, `curtain-wall`, `curtain-panel`, `railing` which ELEMENT_TYPES lacks; ELEMENT_TYPES has `boundary-line` and `ifc-element` which this registry lacks. Its only live consumer is `getElementTypeRules('grid')` in BimGridRenderer.ts:126. Neither list has `room`.",
      "evidence": [
        "packages/core-app-model/src/presentation/ElementTypeRegistry.ts:13-14 — \"Contract 25a §3.1 — ELEMENT_TYPE_REGISTRY source of truth for defaults / Contract 25a §3.1 — Adding new element type requires one entry here only\"",
        "packages/core-app-model/src/presentation/ElementTypeRegistry.ts keys (measured): wall :36, structural-wall :44, curtain-wall :52, slab :60, column :68, structural :76, beam :84, door :94, window :109, curtain-panel :124, stair :141, railing :149, roof :159, ceiling :167, furniture :177, plumbing :185, grid :195, annotation :203, level :211 — no room, no lighting, no boundary-line, no ifc-element",
        "`grep -rn ELEMENT_TYPE_REGISTRY` non-test consumers: only packages/core-app-model/src/BimGridRenderer.ts:126 \"const rules = getElementTypeRules('grid');\" and the barrel export at presentation/index.ts:100",
        "packages/core-app-model/src/presentation/VisibilityIntentDefaults.ts:15-41 — the divergent list, and the one the panel actually renders"
      ],
      "userVisibleSymptom": "None today (the registry is almost unused). The risk is to THIS lane: an agent that reads the header and adds `room` here only will produce no visible change and believe it shipped.",
      "fix": "Add `room` to `VisibilityIntentDefaults.ELEMENT_TYPES` (the real one). If you also touch ElementTypeRegistry, correct the false \"one entry here only\" claim in its header at :14 rather than propagating it. Better: consider whether ElementTypeRegistry should derive from ELEMENT_TYPES instead of restating it — it already imports `defaultRulesForElementType` from VisibilityIntentDefaults at :18, so the dependency direction is established.",
      "alreadyExists": "`defaultRulesForElementType` (VisibilityIntentDefaults.ts:118) is already shared by both, so half the de-duplication is done.",
      "effort": "small",
      "risk": "low",
      "confidence": "high"
    },
    {
      "title": "No gate covers the visibility-intent category list, which is why this shortfall was silent",
      "mechanism": "Three coverage gates exist nearby and none of them enumerates categories against `VisibilityIntentDefaults.ELEMENT_TYPES` or `OverridePanel.CATEGORIES`. `InspectCategoryCoverage.test.ts` ARM A scans `window.<x>Store =` in `apps/editor/src/engine` and compares SETS against `INSPECT_CATEGORIES` only. `viewIntentGovernsEveryCategory.test.ts` has hand-written `PLAN_CASES` (12 distinct categories) and `ELEVATION_CASES` — and it is a STROKE gate: `ctx.stroke = () => { strokes.push(...) }`, so a fill-only family like room is structurally invisible to it, and `lighting` is simply absent from both tables. `perCategoryViewVisibility.spec.ts` asserts the mechanism for named element types, not coverage of a list. So adding an element family to PRYZM produces no error anywhere on the visibility side — exactly the failure shape InspectCategoryCoverage's own header describes for the element-list side.",
      "evidence": [
        "apps/editor/__tests__/InspectCategoryCoverage.test.ts:12-18 — \"⭐ ARM A reads the SAME `window.<x>Store =` assignments the engine bootstrap publishes … and fails when one has neither an `INSPECT_CATEGORIES` row nor a reasoned `NON_ELEMENT_STORE_GLOBALS` entry. It compares SETS, never a count\"",
        "apps/editor/__tests__/viewIntentGovernsEveryCategory.test.ts:102-116 — `const PLAN_CASES` lists wall, slab, column, beam, door, window, roof, stair, furniture, ceiling, plumbing — no room, no lighting",
        "apps/editor/__tests__/viewIntentGovernsEveryCategory.test.ts (recordingCanvas) — \"ctx.stroke = () => { strokes.push({ colour: String(ctx.strokeStyle)…\" — it records strokes only; `fill` is bound to the geometry-sink noop, so a room wash could never register",
        "apps/editor/src/ui/__tests__/perCategoryViewVisibility.spec.ts:10-19 — \"A command that returns `{ success: true }` proves only that it ran … every case below asserts at the layer the user experiences\" — the right shape, but per-type, not coverage"
      ],
      "userVisibleSymptom": "Indirect: it is why `room` (and `lighting`) could be added to the model, given stores, given inspect rows and left-rail rows, and still be missing from every visibility surface with nothing failing.",
      "fix": "Add an ARM to a gate that compares SETS in both directions between the element families that exist (the same `window.<x>Store =` scan InspectCategoryCoverage already implements, plus `runtime.stores` keys for plugin DTO stores like spaceEnvelope) and the union of `VisibilityIntentDefaults.ELEMENT_TYPES` ∪ `OverridePanel.CATEGORIES`, with a named, reasoned exclusion list for families that legitimately have no graphic category. ⚠ Compare SETS, never counts — CLAUDE.md's contract-index box records why. Reuse `publishedStoreGlobals()` from InspectCategoryCoverage.test.ts:44 rather than writing a second scanner.",
      "alreadyExists": "`publishedStoreGlobals()` (the scanner), `NON_ELEMENT_STORE_GLOBALS` (the reasoned-exclusion pattern), and the set-comparison idiom — all in apps/editor/__tests__/InspectCategoryCoverage.test.ts. This is an ARM on an existing gate, not a new gate.",
      "effort": "medium",
      "risk": "low",
      "confidence": "high"
    },
    {
      "title": "REGISTRY MAP — every list that would need a `room` row, and its current state",
      "mechanism": "Enumerated by reading each declaration. The envelope lane's \"three registries\" are the ELEMENT-LIST set and rooms are already in all three; the VISIBILITY set is separate and is where the gaps are. NEEDS A ROW: (1) `VisibilityIntentDefaults.ts:15` ELEMENT_TYPES — the declaration site that drives the panel rows; (2) `OverridePanel.ts:247` CATEGORIES — the per-view tick-list; (3) `ElementTypeRegistry.ts:31` ELEMENT_TYPE_REGISTRY — near-dead rival, add for consistency or fix its header. ALREADY HAS ROOM: (4) `VGSceneApplicator.ts:57` `VGCategory` union; (5) `VGSceneApplicator.ts:109+` ELEMENT_TYPE_TO_VG_CATEGORY ('room'/'Room'/'RoomVolume'); (6) `VGGovernanceStore.ts:148/177/206/235` four built-in templates. DELIBERATELY EXCLUDED, LEAVE ALONE: (7) `VGSceneApplicator.ts:87` CATEGORY_TO_DXF_LAYER — the 14 in the log, ISO linework layers only; (8) `PocheFillTable.ts:50` ISO_CUT_LAYER_TO_POCHE_FILL and :71 VG_CATEGORY_TO_ISO_LAYER — rooms are not sliced solids; (9) `PenWeightTable.ts` SYSTEM_PEN_TABLE — no room pen, `resolvePen` falls back safely. FALLS THROUGH BY ACCIDENT: (10) `VGToIntentMigration.ts:55` VG_CATEGORY_TO_ELEMENT_TYPE has no room but line :167 is `VG_CATEGORY_TO_ELEMENT_TYPE[vgCategory] ?? vgCategory`, so it works — worth an explicit entry so it stops being luck. ALREADY DONE: (11) inspectCategories.ts:133, (12) ElementsSummarySection.ts:54, (13) BrowserDataHelpers.ts:323/421/612/657/713.",
      "evidence": [
        "packages/core-app-model/src/presentation/VGSceneApplicator.ts:87-102 — the 14 the log counts: \"wall:'A-WALL', slab:'A-FLOR', column:'A-COLS', beam:'A-BEAM', door:'A-DOOR', window:'A-GLAZ', 'curtain-wall':'A-WALL', 'curtain-panel':'A-GLAZ', stair:'A-STRS', handrail:'A-STRS', roof:'A-ROOF', furniture:'A-FURN', plumbing:'A-PLMB', ceiling:'A-CEIL'\" — and :716 \"const categoryCount = Object.keys(CATEGORY_TO_DXF_LAYER).length;\" is what prints \"across 14 VG categories (max 56)\"",
        "packages/core-app-model/src/presentation/VGSceneApplicator.ts:63-65 — \"// §ROOM-VG-CATEGORY (L-1610, lane ROOM1) -- rooms are FILLED REGIONS, not projected line-work. See applyToMesh()'s `fillGovernedElsewhere` arm. | 'room'\"",
        "packages/core-app-model/src/presentation/VGSceneApplicator.ts:143-145 — \"// §ROOM-VG-CATEGORY (L-1610, lane ROOM1) -- RoomBoundaryBuilder stamps userData.elementType = 'room' on both the floor fill and the room volume. 'room': 'room', 'Room': 'room', 'RoomVolume': 'room',\"",
        "packages/core-app-model/src/drawing/PocheFillTable.ts:50-57 — \"export const ISO_CUT_LAYER_TO_POCHE_FILL … 'A-WALL','A-COLS','A-FLOR','A-BEAM','A-STRS','A-ROOF'\" — six entries, no room",
        "packages/core-app-model/src/persistence/migrations/VGToIntentMigration.ts:167 — \"const elementType = VG_CATEGORY_TO_ELEMENT_TYPE[vgCategory] ?? vgCategory;\"",
        "apps/editor/src/ui/ViewBrowser/panels/unified-browser/BrowserDataHelpers.ts:323 — \"['roomStore', 'room'],\" and :421 \"case 'Rooms': return 'roomStore';\" and :612 \"case 'Rooms': return window.roomStore?.getAll?.() ?? [];\""
      ],
      "userVisibleSymptom": "n/a — this is the map that prevents the fix landing in one of N places, which is exactly what happened twice with envelopes this session.",
      "fix": "Change (1) and (2) in the same commit as the seam change (RoomColourIntent.ts:127). Add (10) explicitly. Decide on (3). Do NOT touch (7), (8) or (9) — rooms have no ISO linework layer and adding one would change DXF/SVG export output; that is the same export-visible trap the handrail/A-STRS collision is deliberately PINNED for at viewIntentGovernsEveryCategory.test.ts (KNOWN GAP block).",
      "alreadyExists": "6 of the 13 registries already carry room. The lane is 3 additions + 1 seam, not a re-homing.",
      "effort": "small",
      "risk": "low",
      "confidence": "high"
    }
  ],
  "whatIcouldNotEstablish": "1. **WHICH panel the founder had open.** He wrote \"visibility intent settings\". Two surfaces match and NEITHER has a Rooms row, so the finding holds either way — but the fix target differs: `VisibilityIntentPanel` (opened from ViewPropertiesPanelBuilders.ts:196, rows from `elementRules`, has fill colour/style/opacity controls at :351-362) versus `OverridePanel`'s \"Categories in this view\" tick-list (:232, visibility only, no colour). If he means the second, adding a row there gives him ON/OFF but still no colour control. **Ask him, or fix both.**\n\n2. **Whether a `room` row's `fill.colour` is what he actually wants.** His L-1610 words were \"colour code by room type, size, or colour defined, all white\" — a DETERMINATION RULE, not a colour. `ElementStateAppearance` has `fill.colour` but no slot for `roomColourMode`, and `RoomColourIntent.ts:29-36` argues at length that these are different things. A `room` intent row with one colour picker satisfies only the `uniform` mode. I could not establish from code alone which he means.\n\n3. **Whether changing the mode repaints promptly.** I traced `SetRoomColourModeCommand` → `vgGovernanceStore.setViewCategoryOverride` → `dispatch('vg:view-style-set')` but did NOT follow that event to a plan-canvas invalidation. L-1617 in the ISSUE-LOG records a related past bug (\"a project *saved* in 'all white' **loaded in `detection`** until an unrelated view switch, because only events read the stored mode\"), so this path has failed before. **Verify at the outcome, in a browser.**\n\n4. **I ran no tests and no build.** Every claim is from reading source at the cited line. I did not run `npx vitest run packages/core-app-model/src/presentation/VGSceneApplicator.roomCategory.test.ts` or the plan-drawing suite, so I cannot confirm the L-1610 suites are still green at HEAD (`a7e93fb3`).\n\n5. **Whether adding `'room'` to `ELEMENT_TYPES` breaks a snapshot/round-trip test.** `DEFAULT_ELEMENT_GRAPHICS_RULES` is embedded in every system intent and those are serialised; `packages/schemas/__tests__/round-trip.test.ts` and `documentationSettingsRoundTrip.spec.ts` are the likely tripwires. I did not read them.\n\n6. **Why lane ROOM1 chose vgGovernanceStore over visibilityIntentStore.** `RoomColourIntent.ts:14-28` gives a real reason (the four-tier cascade already persists through ProjectSerializer with no new schema) and it was a defensible call at the time. What I could NOT establish is whether anyone knew the consequence — that the category would be invisible in the product's only visibility UI. No comment or ADR I found addresses it. It may simply have been missed.\n\n7. **The `lighting` half.** Not my dimension, but measured in passing and consistent: `lighting` is absent from `CATEGORY_TO_DXF_LAYER` (14), from `VisibilityIntentDefaults.ELEMENT_TYPES` (19), from `ELEMENT_TYPE_REGISTRY` (20), from `OverridePanel.CATEGORIES` (13), and from both `PLAN_CASES` and `ELEVATION_CASES` in the coverage gate — while `renderLightingSymbols` exists and is called from `PlanViewCanvas._renderLightingPlanSymbols()` (per `lightingAndRoomReachThePlanView.test.ts:14-15`). Same shape as rooms, one registry worse. Hand this to the lighting lane; I did not verify the renderer itself."
}