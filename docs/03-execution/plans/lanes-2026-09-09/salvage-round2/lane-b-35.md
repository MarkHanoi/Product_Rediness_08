{
  "refuted": false,
  "reason": "Every load-bearing quote is real, at (or within a few lines of) the cited line, and says what the author claims in context. I re-derived the key structural claims independently rather than trusting the narrative:\n\nCONFIRMED VERBATIM AT THE EXACT CITED LINE\n- houseModalHtml.ts:588 — `<div class=\"alm-header\">Design your house — live</div>`, and lines 585-594 do show the §3PANE layout (plan LEFT / graph CENTER / tools RIGHT). `grep -rn \"Design your house\"` finds this as the ONLY producer in src (other hits are tests + two comments that point back at it), so the founder's screen is correctly identified.\n- HouseLayoutController.ts:333 `this.modal.show(`; :109 the \"`request()` resolves as soon as `modal.show` is called\" comment; :243-303 the four pre-conditions (active level, ≥3-wall shell, C58 max-height gate, variants.length>0) — all as described, and there is indeed no flag among them.\n- HouseLayoutModal.ts:613 `document.body.appendChild(overlay);` — direct body append, not PanelManager.\n- parcelLawCreateHouse.ts:215 `return mod.generateHouseFromBoundary(rt, storeyCount, {` with the \"⛔ `autoBuild` is NOT passed — its default `false` is the modal path\" comment immediately above at :213-214.\n- parcelLawCreateHouse.ts:334/335/336 = `if (design.ok && design.plan.rooms.length === 0) {` / `return { mode: 'create-house', outcome: houseOutcome };` / `}`, with :337 already `return { mode: 'build-from-design', outcome: design };`. The proposed deletion range 334-336 is exactly right and leaves a syntactically valid function ending in the build-from-design return.\n- buildFromDesignPlan.ts:664 `const rooms = input.envelopes.filter((e) => e.role === 'room');`, :665-666 the §SHELL-WITHOUT-ROOMS banner, :678-684 the \"PANEL'S GENERATOR FALL-THROUGH IS PRESERVED, AND DELIBERATELY MOVED RATHER THAN DROPPED … not something this lane should change silently\" passage. The paraphrase is faithful: :671-676 states shell walls come off the level plate's ring and slabs are one per storey, and that neither reads a room.\n- OnboardingStepController.ts:4123 the 4-line `await generateHouseFromBoundary(this.runtime, storeyCount, footprint ? { footprint } : undefined,)` — no autoBuild, exactly as quoted.\n- houseOrchestrator.ts:19 the `generateDeterministicLayouts` import from the apartment TGL engine; enumerate.ts:296 \"hard-invalid the pool is NEVER emptied (a loud §TOPO-HARD-REJECT-ALL warning\" — verbatim, and :297-298 confirms the least-bad ships except on a universal `mandatory` failure (a nuance the author under-stated, not over-stated).\n- roomProgrammePanel.ts:2 header, :528 `programme.add-room`, :806 `programme.link`; roomProgrammeModel.ts:112-193 carries add/remove/rename/set-area/link/unlink/pin/draw-room as claimed; parcelLawTab.ts:1216 `bodyOf('rooms').appendChild(roomProgrammeSlot);`; parcelLawQuestionGroup.ts:322-327 the rooms group (ordinal 4, \"What can I fit inside it?\") and :382-384 the bim group (ordinal 7, \"Take me into BIM.\"); roomEnvelopePlan.ts:238 `role: 'room',`; houseLayoutTrigger.ts:34 and AIAreaLayout.ts:289.\n\nSTRUCTURAL CLAIMS I VERIFIED MYSELF (not just quote-matching)\n- Single show site: `HouseLayoutModal` is constructed exactly once repo-wide (HouseLayoutController.ts:199) and `this.modal.show(` appears exactly once (:333). Nothing else can mount that overlay.\n- Call-site census: production callers of `generateHouseFromBoundary` are exactly parcelLawCreateHouse:215, OnboardingStepController:4123, generationChatSeam:649 (which does pass `autoBuild: true` at :652), and houseLayoutTrigger:34 (console). Matches the claim, including which ones omit autoBuild.\n- The modal-vs-direct fork is real: houseFromBoundary.ts:270-272 `opts?.autoBuild === true ? buildDirect : request(...)`, and the option's own doc at :50-54 says \"Default false = the modal path\".\n- Onboarding ordering: OnboardingStepController:4041-4042 `if (route === 'house') { await this.generateHouse(); }` and the `pryzmShowSiteResultView` handoff at :4081-4086 sits after it in the same try block — so the modal genuinely is concurrent with the BIM dual-pane handoff, despite 4085 being a lower line number than 4123 (different functions).\n- The rival branch the author pre-registered as falsifying: `decide()` has a SECOND `return { mode: 'create-house' }` at :296 on `!houseOutcome.ok`. I checked whether it could produce an enabled button — createHouseSection.ts:65-75 renders that arm with `<button type=\"button\" disabled … cursor:not-allowed>`. So the refusal arm cannot route a click anywhere, and :334-336 really is the only path from an enabled question-7 button into the generator+modal. The author named this exact alternative and it survives.\n- `buildBuildFromDesignSection` (the proposed home for the replacement affordance) exists at createHouseSection.ts:156.\n\nCITATION SLIPS FOUND (cosmetic; none change the analysis)\n1. \"parcelLawCreateHouse.ts:492 calls deps.buildHouse(...)\" — the call is at :496; :492 is the closing brace of the armLandingLayout try/catch. Off by four.\n2. \"pryzmShowSiteResultView('2D') at :4085\" — :4085 is `if (typeof showResult === 'function') {`; `showResult('2D');` is :4086. Off by one.\n3. Minor understatement, in the safe direction: enumerate.ts:297-298 notes the \"never refuses\" behaviour has one exception (a universal `mandatory` failure IS surfaced as a structured rejection). The unsoundness claim still stands for the other five hard rules.\n\nOne thing the author did NOT flag, which a future lane should know: `generateHouseInExistingShell` (houseFromBoundary.ts:344) is a THIRD caller of `_controller.request` and therefore a third route to `modal.show`. It is console-only (houseLayoutTrigger.ts:36), so it does not affect the BIM-entry analysis or the proposed fix, and the \"exactly one show site\" wording remains literally true — but \"two production call sites\" is specifically two BIM-ENTRY call sites, not two request() callers.\n\nI did not run the app, so the runtime console-sequence prediction in the falsification test is unverified; the static routing it depends on is confirmed.",
  "evidenceChecked": [
    "apps/editor/src/ui/house-layout/houseModalHtml.ts:555-596 — buildHouseModalHtml, header at :588 verbatim, §3PANE layout confirmed",
    "grep -rn 'Design your house' across apps/ and packages/ — houseModalHtml.ts:588 is the only producer in src",
    "apps/editor/src/ui/house-layout/HouseLayoutController.ts:95-130 (request() doc, :109 quote), :198-199 (sole HouseLayoutModal instantiation), :235-345 (pre-conditions + :333 modal.show), :395-406 (buildDirect)",
    "grep for HouseLayoutModal repo-wide — constructed once at HouseLayoutController.ts:199; grep for 'this.modal.show(' in house-layout — once at :333",
    "apps/editor/src/ui/house-layout/HouseLayoutModal.ts:610-616 — :613 document.body.appendChild(overlay); :480 uses buildHouseModalHtml",
    "apps/editor/src/ui/analysis/parcelLawCreateHouse.ts:205-232 (buildHouse dep, :213-214 autoBuild-not-passed comment, :215 call), :250-300 (decide() head + :296 refusal arm), :326-340 (fall-through at 334/335/336, build-from-design return at 337), :488-500 (actual deps.buildHouse call at :496, not :492)",
    "apps/editor/src/ui/site/createHouseSection.ts — :54 buildCreateHouseSection, :65-75 refusal arm renders a DISABLED button, :156 buildBuildFromDesignSection exists",
    "apps/editor/src/ui/site/buildFromDesignPlan.ts:655-695 — :664 role==='room' filter, :665-666 §SHELL-WITHOUT-ROOMS banner, :671-676 shell/slab rationale, :678-684 fall-through-preserved passage",
    "apps/editor/src/ui/onboarding/OnboardingStepController.ts:4041-4042 (route==='house' → generateHouse), :4081-4094 (showResult handoff; showResult('2D') at :4086 not :4085), :4105-4128 (generateHouse, generateHouseFromBoundary at :4123)",
    "apps/editor/src/ui/house-layout/houseFromBoundary.ts:40-58 (autoBuild option doc, default false = modal), :125-145 (signature), :265-285 (autoBuild fork at :270-272), :344 (generateHouseInExistingShell also calls request — console-only)",
    "grep for generateHouseFromBoundary repo-wide — production callers: parcelLawCreateHouse:215, OnboardingStepController:4123, generationChatSeam:649, houseLayoutTrigger:34",
    "apps/editor/src/ui/generation/generationChatSeam.ts:645-653 — autoBuild:true at :652",
    "apps/editor/src/ui/house-layout/houseLayoutTrigger.ts:28-39 and apps/editor/src/ui/layout/AIAreaLayout.ts:285-292 — console trigger installed",
    "packages/ai-host/src/workflows/houseLayout/houseOrchestrator.ts:15-25 — :19 TGL import verbatim",
    "packages/ai-host/src/workflows/apartmentLayout/tgl/enumerate.ts:285-315 — :296 §TOPO-HARD-REJECT-ALL quote verbatim, plus the mandatory-failure exception at :297-298",
    "apps/editor/src/ui/room-programme/roomProgrammePanel.ts:1-6, :526-529 (programme.add-room), :803-807 (programme.link)",
    "apps/editor/src/ui/room-programme/roomProgrammeModel.ts:112-193 — add/remove/rename/set-area/link/unlink/pin/draw-room intents present",
    "apps/editor/src/ui/room-programme/roomEnvelopePlan.ts:235-240 — role:'room' at :238",
    "apps/editor/src/ui/analysis/parcelLawTab.ts:1213-1216 (rooms slot) and :1263-1266 (create-house slot into bodyOf('bim'))",
    "apps/editor/src/ui/analysis/parcelLawQuestionGroup.ts:320-330 (rooms group, ordinal 4) and :382-384 (bim group, ordinal 7, 'Take me into BIM.')"
  ]
}