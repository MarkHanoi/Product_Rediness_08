{
  "survivingTitles": [
    "0% hit rate on a fresh open is EXPECTED, not a defect — the cache is per-session in-memory and this was its first pass",
    "`yieldCalendar≈83ms` is COMPUTED, not measured — it is literally `yields × 16.67`",
    "`work=109ms` is a LOWER BOUND — it excludes hidden-line removal, every symbol injector, VG styling, and the caller's mesh export",
    "The plan projection during open is DELIBERATE (Contract 17 §4 auto-opens the split plan pane) — and the user's decision to close that pane is never persisted",
    "Doors and windows mint a fresh cache version on EVERY build via `Date.now()`, so they can never hit after a rebuild — walls solved this with a content hash and doors did not"
  ],
  "refutedTitles": [],
  "refutations": [
    "SURVIVES (1, 0% hit rate) — fully confirmed, no correction. `_cwProjectionCache` is a plain instance `Map` (EdgeProjectorService.ts:1943) holding live BufferGeometry. Full-tree grep: the ONLY production invalidators are `CurtainWallBuilder.ts:1612` -> `invalidateCwElement`, and `initScene.ts:3616` -> `clearCwProjectionCache` on `pryzm-project-switch` (which the surrounding L-325 comment confirms fires BEFORE the incoming project hydrates). The service is built once per session behind a memoised promise (`_ensureEdgeProjectorService`, initScene.ts:1143). No serialisation of any kind exists. The quoted self-documenting comment is verbatim. The auditor's non-fix is the right call, and his rejection reason is stronger than he states: persisting this would need Float32Array positions PLUS the version token PLUS clipSignature, and no builder guarantees its version token is stable across a page reload — so a persisted cache would be a cross-session staleness guard resting on a property nothing enforces.",
    "SURVIVES (2, yieldCalendar) — fully confirmed, verbatim, and this is the most valuable finding in the set. Verified at EdgeProjectorService.ts:3676: `yieldCalendar≈${((_perLayerYieldCount + _groupYieldCount) * (1000 / 60)).toFixed(0)}ms`. Verified at 3570-3576 that the elapsed time across the awaited `scheduleOnce` is discarded — `_workMsTotal` is incremented BEFORE the await and `_lastYieldAt` reset AFTER it, so the interval between is accumulated nowhere. `projectionChunkPolicy.ts:267` reads `⚠ This is the CALENDAR cost only` and `estimateTimedFrameYields` (:272) is declared an estimator. The auditor's refusal to resolve the ambiguity in either direction (could be >83ms of calendar, could be ~0ms additive) is correct and is exactly what stops the next lane chasing a synthetic number. The 4-line fix is pure instrumentation; nothing functional breaks.",
    "SURVIVES (3, work=109ms is a lower bound) — confirmed, and the untimed claim is stronger than stated. `applyOcclusion` is at line 4235, inside the same `async project()` (2286-4287), i.e. ~540 lines AFTER the summary logs at ~3667/3692 — so it is in neither number, confirmed. `packages/core-app-model/src/drawing/HiddenLineRemoval.ts` contains ZERO occurrences of `performance.now()` in the entire file: HLR is not merely 'untimed in its summary', it is uninstrumented end to end. `[NME] §H2-NME-CACHE` confirmed at NativeElementMeshExporter.ts:812-819, a genuine second cold-on-open cache upstream. One precision correction: `_lastYieldAt` is seeded at line 2669, BEFORE the group loop, so `work` covers all wall-clock from that seed to the log minus yields — still a lower bound on `project()`, conclusion unchanged. Pure-instrumentation fix, breaks nothing.",
    "SURVIVES (4) with TWO MATERIAL CORRECTIONS. Mechanism fully confirmed: initScene.ts:4636 comment, `ric(_activate, { timeout: 1500 })` at 4666 with `setTimeout(_activate, 600)` fallback, `activate()` emitting `split-view-activated` (SplitViewManager.ts:351) and `split-view-view-changed` (:353), both handlers calling `notifyViewActivated`, the active-view predicate at initScene.ts:1511-1513, `deactivate()` (401-431) writing no preference, and `pryzm.splitView.gridVisible` (2179/2187) being the ONLY localStorage key in the whole file. CORRECTION A — THE 'WOULD BREAK' IS WRONG, AND WRONG IN THE DIRECTION THAT COSTS BUDGET. Contract 17 does not exist as a contract in this repo. Both `C06-UI-SHELL-AND-TOOLS.md:524` and `C59-MULTI-PANE-VIEW-SYSTEM.md:745` state the historical 'Contract 17 §4 — split view' is SUBSUMED by C59, its canonical successor. I grepped C59 for auto-open / opens-by-default / restore-open / 'both the 3D': ZERO hits. The successor contract does not restate the auto-open as an invariant at all. So persisting the close needs a code change plus a C59 row — NOT a contract amendment. The code comment at initScene.ts:4638 is citing a superseded document, which is precisely the 'guard kept because its justification quietly became false' shape this repo has shipped regressions on. CORRECTION B — THE 192ms IS NOT ESTABLISHED AS ON THE CRITICAL PATH. `notifyViewActivated` ends in `unifiedFrameLoop.queueLowPriority(() => this._flush())`, and activation itself sits behind `requestIdleCallback` fired after `pryzm-project-loaded` — i.e. after hydration completes, at low priority. The projection may land entirely after the founder perceives the project as open. The auditor books 192ms at 'confidence: high' and sells fix (1) as 'removes the cost entirely rather than moving it'; it removes 192ms of wall-clock AFTER load, which may be 0ms of perceived open. Establish that the founder's clock is still running when the CHUNK line prints before spending this lever. Also recorded so nobody re-finds it: the two events do NOT double-project — `notifyViewActivated` guards on `if (!this._deferredDirtyViewIds.has(viewId)) return;` and deletes on entry, so the second event is a no-op. Fix (1) is sound and cheaper than priced; fix (2) (dropping the rIC timeout) should NOT be taken — the auditor's own risk note is right and the same log shows a WebGPU device loss during open, so 'never idle' is a live failure mode here.",
    "SURVIVES (5) AS AN OBSERVATION — but its FIX should not be taken on this dimension. All code claims verified, including the one the auditor asserted without evidence. WallFragmentBuilder `_versionForBuild` (393-445) and `_geomVersionKey` reuse confirmed with the L-52 docstring quoted accurately (note: the Grep tool returns NOTHING for `_versionForBuild` in this repo — only bash grep finds it; this is the known NUL-byte/encoding silence, do not conclude the symbol is absent). FurnitureFragmentBuilder.ts:142 and LightingFragmentBuilder.ts:734 confirmed unconditional `_priorVersion + 1`. NME propagates `version: root.userData?.version` at :578. `_cwCacheIsValid` compares exactly. The CACHEABLE_ELEMENT_TYPES 'strictly monotonic ... no staleness risk' comment is verbatim and the auditor is right that monotonicity is the wrong property. I INDEPENDENTLY CONFIRMED THE LOAD-BEARING PREMISE the auditor only asserted: `DoorBuilder.rebuildForWall(wallId)` (packages/geometry-door/src/DoorBuilder.ts:458) is called by `WallRebuildCoordinator._flush` ONCE PER REBUILT WALL and re-enqueues every door on that wall, so a neighbour wall re-mitered by a join re-resolve re-versions its doors with byte-identical geometry. Real. EVIDENCE CORRECTION: the cited `DoorBuilder.ts:431` is the PROPERTY-ONLY PATCH path (`_PROPERTY_ONLY_FIELDS` fast path), not 'every build'. The every-build stamp is `DoorBuilder.ts:599` (`version: Date.now()` inside `rootUserData`); windows are :829 (build) and :635 (patch). Claim holds, cited line is the wrong one. WHY THE FIX SHOULD NOT BE TAKEN HERE: it has zero open-path benefit (the auditor says so), and the invariant it would break is the strongest in the set — the version token IS the membership condition for CACHEABLE_ELEMENT_TYPES, so a content key missing one determinant serves stale plan linework silently and ONLY for elements that did not change. Worse than the auditor says: doors have no `_renderVersion` equivalent, so there is no stable content signal to build a key from — the wall fix's own escape hatch is 'walls with no `_renderVersion` fall back to always-fresh'. A door key would have to be composed from raw record fields plus LOD (`_lodFor`/`_builtLod`), curved-leaf state, finish colours and the HOST WALL's token — a hand-written cache key of at least five terms, which is the L-813 shape. Take ONLY the auditor's own preliminary step: the temporary misses-by-elementType counter in `_putCwCache`. Do not apply the L-52 pattern to doors as part of an open-path lane."
  ],
  "missed": [
    "THE FOUNDER'S LOG ALREADY ITEMISES PART OF `work=109ms` AND NOBODY READ IT. `EPS_VERBOSE = false` (EdgeProjectorService.ts:169), so the `DIAG-EPS-01 … allocMs=` lines in his console are NOT verbose diagnostics — they are the SLOW-OP ALERT at line 2996, `if (EPS_VERBOSE || __edge_ms > 2)`, which prints only when a group's `EdgesGeometry` allocation exceeds 2ms. Every DIAG-EPS-01 line in that log is therefore a >2ms offender, already named: Stair at 4.92ms (faceCount=1068) plus 'several Lighting groups at 672 faces / ~2-3ms each'. That is a free, already-printed partial attribution of the 109ms that names the two families to attack. The auditor treated 109ms as opaque and asked for new instrumentation (finding 3) while the existing instrumentation was already answering part of the question in the log he was quoting. On a COLD open the cache cannot help by construction (finding 1), so per-group EdgesGeometry cost is the ONLY in-projector lever that exists on the open path — and Lighting fixtures emitting 672-face wireframes into a PLAN drawing is the cheapest thing to question.",
    "`invalidateCwView()` IS DEAD CODE. Full-tree grep (excluding node_modules) returns zero callers anywhere — the only hits are its own definition at EdgeProjectorService.ts:2220 and four comments describing it. Its docstring asserts 'Called when a view's definition (clip range, projection direction) changes.' It is not called by anything; that job is done implicitly by the `clipSignature` mismatch in `_cwCacheIsValid`, which causes `_putCwCache` to OVERWRITE (disposing the prior geometry) rather than evict. A documented invalidation path with no caller is the same shape as the CLAUDE.md defects — the next lane reading that docstring will believe view-def changes are explicitly evicted when they are only overwritten as a side effect.",
    "DELETING ANY NON-CURTAIN-WALL ELEMENT NEVER EVICTS ITS CACHE ENTRY. `invalidateCwElement` has exactly ONE production caller in the whole repo — `packages/geometry-curtain-wall/src/CurtainWallBuilder.ts:1612` — yet `CACHEABLE_ELEMENT_TYPES` has 17 members (wall, slab, roof, room, column, door, window, stair, beam, furniture, plumbingfixture, lighting, handrail, ceiling, floor, stair-railing, curtainwall). Delete a wall, door, stair or lighting fixture and its entry — holding live `BufferGeometry` — stays in `_cwProjectionCache` until either a project switch or the 5,000-entry LRU cap, and the path that eventually reclaims it (`_evictLruCwEntry`) is an O(W×V) full scan of every entry. This is retained geometry for elements that no longer exist. Not an open-path cost, but it is a real cost on this dimension and it sits next to the founder's own `3762 meshes (ARM TRIPPED)` line and a WebGPU device loss in the same log.",
    "THE AUDITOR ASSERTS A SINGLE PROJECTION TRIGGER WITHOUT CHECKING FOR RIVALS. `EdgeProjectorService.project()` has FIVE call sites: `PlanViewManager.ts` 645, 880, 1044, 1137 and initScene's `_reprojectView` at 1454. The code carries a healed scar from exactly these drivers colliding — `§PERF105-CLIP-SIGNATURE-HAS-ONE-OWNER` (L-11561, initScene.ts:1447-1452): 'This driver and PlanViewManager both re-project the same plan view on the same store change; this one said the below-level band was 0 m and the other said 1.20 m, so their clip signatures never matched and each pass disposed the other's cached geometry.' Finding 4 attributes the open-path projection entirely to the auto-open without asking which driver fired or how many times. I cannot claim a second projection occurred — the excerpt shows one CHUNK line and one CACHE-STATS line — but the excerpt is admittedly partial, and 'which of the five drivers fired, and how many times' is answerable from a full console capture. Establish that before pricing this dimension at 192ms; if two drivers fire, the number doubles and the fix target changes.",
    "LINE-NUMBER DRIFT THROUGHOUT THE EdgeProjectorService CITATIONS — flagged so a lane that greps a quoted line, finds nothing, and discards a CORRECT finding does not happen. The auditor's line numbers in that file run ~28-30 low across the board: `_putCwCache` is 2164 (cited 2136); `invalidateCwElement` 2204 (cited 2176); `invalidateCwView` 2220 (cited 2192); `clearCwProjectionCache` 2236 (cited 2208); the `yieldCalendar≈` expression 3676 (cited 3648); the §D.5 'hitRate=0% on first run' comment ~3681-3682 (cited 3653-3654); the `cacheElements=`/`cacheEntries=` line 3698 (cited 3670); `applyOcclusion` 4235 (cited 4207). The consistent offset suggests the audit was read against a slightly earlier revision. Every claim verifies at the corrected line — the substance is intact, only the coordinates rot."
  ]
}