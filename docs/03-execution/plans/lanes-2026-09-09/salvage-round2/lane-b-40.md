{
  "refuted": false,
  "reason": "MECHANISM CONFIRMED, LINE FOR LINE — I could not construct a rival mount for this overlay. `document.body.appendChild(overlay)` is at `HouseLayoutModal.ts:613` exactly as claimed; `buildHouseModalHtml` carries `<div class=\"alm-header\">Design your house — live</div>` at `houseModalHtml.ts:588` with `§3PANE (SPEC §1.1) — plan LEFT · graph CENTER · tools RIGHT` on line 587, and that string exists nowhere else in the repo (the apartment sibling reads \"Choose a layout\", `layoutModalHtml.ts:452`). `this.modal.show(` is the ONLY show site (`HouseLayoutController.ts:333`). I swept every reachable upstream: `generateHouseFromBoundary` has exactly 4 non-test callers (`parcelLawCreateHouse.ts:215`, `OnboardingStepController.ts:4123`, `generationChatSeam.ts:649` — which passes `autoBuild:true` → `buildDirect`, no modal — and `houseLayoutTrigger.ts:34`, the console command installed at `AIAreaLayout.ts:289`); `generateHouseInExistingShell` only from the same console trigger; no AI-panel leaf, no `pryzmGenerateHouse` caller anywhere in app code, no auto-`.click()` of `CREATE_HOUSE_BTN_TESTID` (the only programmatic clicks are `parcelLawChat.ts`, which drives *other* testids from a typed user utterance), and nothing on a view-activation path. The gates before `modal.show` are exactly the four named (active level · shell · C58 `checkMaxHeightGate` · `variants.length > 0`) — no flag. `houseOrchestrator.ts:19` does import `generateDeterministicLayouts` from the apartment TGL, and `enumerate.ts:296` does say the pool \"is NEVER emptied\". The three-line fall-through is verbatim at `parcelLawCreateHouse.ts:334-336`, and I confirmed the mechanics of removing it: `planBuildFromDesign` reaches `ok` with zero rooms (the `every-room-refused` guard at :1067 is `rooms.length > 0 && builtRooms.length === 0`), and `buildBuildFromDesignSection` renders an ENABLED button on any `ok` outcome. I also killed the best \"data-shape\" rival: `resolveStore` reads `rt.stores.spaceEnvelope`, the same store `roomProgrammePanel` writes via `spaceEnvelope.batch.create`, and `readDesignEnvelopes` filters on the same `role` field `roomEnvelopePlan.ts:238` writes — so the fourth arm is not silently blind.\n\n⛔ TWO LOAD-BEARING CLAIMS DO NOT SURVIVE, AND BOTH ARE ABOUT THE FIX, NOT THE MECHANISM.\n\n(1) \"REMOVING THE FALL-THROUGH COSTS NO CAPABILITY / IS STRICTLY SOUNDER\" IS FALSE, AND THE CODE SAYS SO IN ITS OWN VOICE. `BUILD_FROM_DESIGN_WILL_NOT_CREATE` (`buildFromDesignPlan.ts:397-412`) explicitly excludes **a ROOF**, **STAIRS and slab voids**, **floor finishes**, and **new project levels** (\"every storey your plates name already exists\"). On the exact case the fix targets — one level envelope, no rooms — question 7 goes from *an N-storey house with rooms, a stair, a stairwell void and a roof* to *a ring of shell walls plus one slab*. Worse, that list's own text tells the user to **\"Ask for one on the house arm, where the form is an input you give\"** — the arm the fix would make unreachable from any UI, leaving the panel pointing at a door it just bricked. And the fourth arm's head copy is hard-coded to *\"You have drawn a level envelope and room envelopes, so PRYZM builds THOSE\"* with a *\"Your 0 rooms (0.00 m²)\"* list beneath it (`createHouseSection.ts:163-166, 244-247`) — a sentence that is simply untrue in the zero-rooms case the fix routes there. Shipping the three-line deletion without rewriting that arm's copy is the §CONTEXT-DATA-HONESTY class this repo gates on. The claim's own citation of `buildFromDesignPlan.ts:678` also over-reads it: the author said the fall-through is \"not something this lane should change silently\", which is a caution about ownership, not an invitation.\n\n(2) THE ATTRIBUTION TO THIS SURFACE IS UNDER-DETERMINED, AND THE TIMING ARGUES AGAINST IT. `armLandInBimOnNextHouse` fires on `house.layout-executed` — emitted AFTER the executor lands, not on `modal.show` (its header says so and `landInBimAfterCreateHouse.ts:87-120` proves it), and the create-house click branch (`parcelLawCreateHouse.ts:489`) arms it BEFORE `buildHouse` and never navigates itself. So on the Parcel Law path the modal opens **over the site/analysis view** and BIM entry happens only after the user picks. On the onboarding path, `generateHouse()` (`:4123`) resolves the instant the chooser opens and control falls straight to `showResult('2D')` (`:4085`) — the modal and the BIM dual pane come up **together**, which is the literal reading of the defect slug \"on-bim-entry\". The counter-evidence is real and I weighed it: the L-13080 precedent recorded in `buildFromDesignPlan.ts:1-8` is the founder saying this after clicking \"Take me into BIM\", so the Parcel Law reading has a documented prior. Both fit; the claim discloses the ambiguity honestly and then fixes only one. Do not ship the Parcel Law edit and report the defect closed.\n\nI am returning refuted=false because the causal chain is right and I exhausted the search for a different mount. But the fix as written is not the free swap it claims to be.",
  "evidenceChecked": [
    "apps/editor/src/ui/house-layout/houseModalHtml.ts:547-589 — verified the '§3PANE … plan LEFT · graph CENTER · tools RIGHT' comment at :587 and the 'Design your house — live' header at :588; the string is unique in the repo (apartment sibling layoutModalHtml.ts:452 reads 'Choose a layout')",
    "apps/editor/src/ui/house-layout/HouseLayoutModal.ts:605-618 — 'document.body.appendChild(overlay)' at :613, outside PanelManager, with the '[house-layout] modal mounted to <body>' console line the falsification test predicts",
    "apps/editor/src/ui/house-layout/HouseLayoutController.ts:100-125, 195-360 — request()'s doc at :109 ('resolves as soon as modal.show is called'); the ONLY this.modal.show( in the repo at :333; the four pre-conditions confirmed (active level, analyseActiveShell, checkMaxHeightGate/C58, variants.length>0) with no flag",
    "apps/editor/src/ui/analysis/parcelLawCreateHouse.ts — header :20-24 ('autoBuild is left at its default false'); production dep :215 with the ⛔ comment at :213-214; decide() :279-338 with the three-line fall-through verbatim at :334-336; resolveStore :150-156 reads rt.stores.spaceEnvelope; generator click branch :470-518 arms the landing layout then calls buildHouse and never navigates itself",
    "apps/editor/src/ui/site/buildFromDesignPlan.ts — §SHELL-WITHOUT-ROOMS comment at :665-687; every-room-refused guard at :1067 is 'rooms.length > 0 && builtRooms.length === 0', so zero rooms reaches ok; readDesignEnvelopes :1272-1314 reads the same role field the room programme writes; ⛔ BUILD_FROM_DESIGN_WILL_NOT_CREATE :397-412 excludes ROOF, STAIRS+voids, floor finishes and new project levels and points the user 'to the house arm'",
    "apps/editor/src/ui/site/createHouseSection.ts:155-260 — the enabled button is rendered on any ok outcome (so the fix mechanically works), but the head copy at :163-166 asserts 'you have drawn … room envelopes' and :244-247 prints 'Your 0 rooms (0.00 m²)' when rooms is empty",
    "apps/editor/src/ui/onboarding/OnboardingStepController.ts:4040-4130 — route==='house' → generateHouse() → await generateHouseFromBoundary at :4123 with no autoBuild, then showResult('2D') at :4085; :4220's controller.request is the OFFICE controller, not the house one",
    "apps/editor/src/ui/house-layout/landInBimAfterCreateHouse.ts:11-30, 50-145 — the BIM handoff is armed one-shot and fires on 'house.layout-executed' AFTER the build, never on modal.show; so on the Parcel Law path the modal precedes BIM entry rather than coinciding with it",
    "apps/editor/src/ui/house-layout/houseFromBoundary.ts:130-280 — the single module-singleton controller at :62; autoBuild===true → buildDirect, else request(); confirms the modal is selected by ABSENCE of the flag",
    "apps/editor/src/ui/generation/generationChatSeam.ts:631-665 — runHouse passes autoBuild:true, so the chat/RAC path never opens the modal (rules out the AI panel as the source)",
    "apps/editor/src/ui/house-layout/houseLayoutTrigger.ts:1-40 + apps/editor/src/ui/layout/AIAreaLayout.ts:284-290 — installHouseLayoutConsoleTrigger is a DevTools-only bench; repo-wide grep finds no other pryzmGenerateHouse caller",
    "packages/ai-host/src/workflows/houseLayout/houseOrchestrator.ts:19 — imports generateDeterministicLayouts from ../apartmentLayout/tgl/runDeterministicLayout.js, confirming the house pipeline drives the apartment TGL per storey",
    "packages/ai-host/src/workflows/apartmentLayout/tgl/enumerate.ts:280-302 — 'if EVERY strategy is hard-invalid the pool is NEVER emptied (a loud §TOPO-HARD-REJECT-ALL warning names the failing rules and the least-bad ships)'",
    "apps/editor/src/ui/analysis/parcelLawQuestionGroup.ts:323-397 — group 'rooms' ordinal 4 ('What can I fit inside it?') and group 'bim' ordinal 7 ('Take me into BIM.'), plus parcelLawTab.ts:1216/1266 showing each group's single mounted slot",
    "apps/editor/src/ui/room-programme/roomProgrammePanel.ts:28-29, 1851 — 'Place envelopes in 3D' is an EXPLICIT act (§20); declaring rooms in the graph alone does not mint role:'room' envelopes, so the proposed replacement copy omits a required second step",
    "apps/editor/src/ui/analysis/parcelLawChat.ts:230-252, 400-440 — the only programmatic .click()s are utterance-driven and target other testids; nothing auto-clicks the create-house button",
    "apps/editor/src/ui/analysis/__tests__/parcelLawCreateHouse.spec.ts:405-420 + buildFromDesignPlan.ts:1-8 — the L-13080 record of the founder's PRIOR report of this exact screen after clicking 'Take me into BIM', which is the strongest evidence FOR the claim's surface attribution",
    "docs/04-reference/ISSUE-LOG.md rows L-13023 and L-13034 — both OPEN, both marked FOUNDER DECISION, both about the generator handing the whole buildable plate to the engine and §TOPO-HARD-REJECT-ALL shipping the least-bad layout"
  ],
  "betterHypothesis": "Not a different mount — there isn't one — but a different DEFECT BOUNDARY and a different fix. Rival: the modal appearing at question 7 with no rooms is not a routing accident, it is L-13080's deliberately preserved offer (`buildFromDesignPlan.ts:678`), and what the founder is actually hitting is the thing behind it — the never-refusing TGL engine (`enumerate.ts:296`) fed a programme sized to the whole buildable plate, which is already logged as L-13023 / L-13034, both OPEN and both marked FOUNDER DECISION. Under that reading, deleting `parcelLawCreateHouse.ts:334-336` removes a symptom on one of two surfaces while silently costing the roof, the stair, the stairwell void and multi-storey level minting, and leaving the panel asserting \"you have drawn … room envelopes\" over an empty room list. Better fix: on `design.ok && plan.rooms.length === 0`, keep BOTH arms on the surface — render the build-from-design arm as the primary (with rewritten zero-rooms copy: \"no rooms declared, so this builds the shell and the plate only\") plus the pointer to question 4 AND the explicit \"Place envelopes in 3D\" step, and keep the generator as a clearly-labelled secondary (\"generate a layout instead — this proposes rooms, a stair and a roof PRYZM chose\"). That answers \"it should work for walls/slabs, minimum\" without amputating the only path to a roof. Separately, and before any of it: settle which call site produced the report. The two paths are observably different — Parcel Law puts the modal over the SITE view and enters BIM only after the pick (`landInBimAfterCreateHouse` fires on `house.layout-executed`), while onboarding (`OnboardingStepController:4123` → `:4085`) puts the modal and the BIM dual pane on screen together, which is what \"on BIM entry\" literally describes. Ask the founder whether he came from the onboarding wizard or the Parcel Law tab; the one-line Parcel Law edit is a no-op for the first."
}