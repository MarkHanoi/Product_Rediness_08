{
  "survivingTitles": [
    "`room` is a first-class category in the DEPRECATED vgGovernanceStore, and absent from the VisibilityIntent list the founder's panel actually renders",
    "THE STYLING SEAM IS ONE FUNCTION — `resolveRoomColourIntent()` — with exactly three live callers",
    "`OverridePanel.CATEGORIES` is a FOURTH category registry with no room row — the one this lane is most likely to miss",
    "`PlanViewFillRenderer` is a DEAD duplicate room painter with zero importers",
    "'Colour by room type' cannot actually change any colour — `OCCUPANCY_PALETTE` is a hardcoded table with no write path",
    "Room fills are gated to `isPlanLike` — elevation and section render no room region at all",
    "`ELEMENT_TYPE_REGISTRY` is a rival to `VisibilityIntentDefaults.ELEMENT_TYPES` and its header states a rule that is false",
    "No gate covers the visibility-intent category list, which is why this shortfall was silent",
    "REGISTRY MAP — every list that would need a `room` row, and its current state"
  ],
  "refutedTitles": [],
  "refutations": [
    "NONE REFUTED OUTRIGHT — every mechanism claim reproduced from the code. But four sub-claims are wrong and one 'already exists' block is materially overstated; details below.",
    "Finding 1 — COUNT WRONG (finding stands). `ELEMENT_TYPES` (VisibilityIntentDefaults.ts:15-41) has EIGHTEEN entries total (`__default__` + 17), not '19' (summary) and not '18 + default' (evidence, which double-counts). The panel renders one row per key of `intent.elementRules`, so it shows 18 rows INCLUDING a literal `__default__` row — not '19 category rows'. Absence of `room` is confirmed.",
    "Finding 1 — 'already exists: EVERYTHING except the join' is OVERSTATED and would mislead the lane. `SetVGCategoryStyleCommand` / `SetVGViewCategoryStyleCommand` have ZERO UI call sites: the only non-test references are `packages/ai-host/src/vg/VGIntentMapper.ts:34` and `packages/command-registry/src/ai-vg/VGIntentMapper.ts:34` (the AI path). So the room category's `visible`, `transparency` and `fillColor` — the three fields `resolveRoomColourIntent` reads — have NO human authoring path at all today; only `roomColourMode` does, via `room.setColourMode`. Missing is the join AND an authoring path, not just the join. Related: the ISSUE-LOG's own L-1610 block states `SetRoomColourModeCommand` implements `undo()` but 'nothing puts it on a stack', so 'SetRoomColourModeCommand (with correct undo)' reads as a working undo that does not exist.",
    "Finding 2 — the FIX reinvents an existing solver and drops a behaviour the current code protects. It prescribes hand-rolling `viewIntentInstanceStore.get(viewId)` → `visibilityIntentStore.get(intentId)` → `resolveIntentStyle(...)` inside `RoomColourIntent.ts`. That exact chain already exists twice: `resolveBoundIntentWithInheritance()` (IntentBindingResolver.ts:78) and its packaged consumer `makeSymbolInjectionGate()` (SymbolInjectionGate.ts:112, §SYMBOL-INJECTORS-VS-INTENT / L-3903), which resolves the bound intent, loops states, calls `resolveIntentStyle(instance, intent, elementType, state, viewType, {elementType, category})`, caches per elementType and FAILS OPEN. The auditor's chain bypasses `resolveBoundIntentWithInheritance`, so a detail-off-a-section or callout-off-a-plan view — which today inherits its parent's intent (getInheritedFromViewId, :95) — would resolve to NO intent and silently fall back. The seam finding survives; the prescription should reuse the inheritance resolver, not restate it.",
    "Finding 3 — internal inconsistency. The title/mechanism says 'there are FOUR that need a room row, not three', but finding 9's own map lists THREE under 'NEEDS A ROW' (ELEMENT_TYPES, OverridePanel.CATEGORIES, ElementTypeRegistry) plus one 'add for luck' (VGToIntentMigration). The OverridePanel gap itself is confirmed exactly as described: 13 entries, wall→boundary-line, no room, no lighting/plumbing/grid/level.",
    "Finding 8 — COUNT WRONG (finding stands). `PLAN_CASES` is 15 rows over ELEVEN distinct categories (wall, slab, column, beam, door, window, roof, stair, furniture, ceiling, plumbing), not '12'. The load-bearing halves are confirmed: zero mentions of room or lighting in the whole file, and `ctx.fill` is bound to the `noop` geometry sink at :142-148 while only `ctx.stroke` records — a fill-only family is structurally invisible to that gate."
  ],
  "missed": [
    "⭐ BIGGEST REUSE MISS — the 'ask the intent whether this family is visible in this view' solver ALREADY EXISTS, package-level and tested. `packages/core-app-model/src/presentation/SymbolInjectionGate.ts:112` `makeSymbolInjectionGate(viewId, viewType)` → `resolveBoundIntentWithInheritance(viewId)` → `resolveIntentStyle(instance, intent, elementType, state, viewType, {elementType, category: elementType})` across all four states, cached, fail-open, proven by `apps/editor/__tests__/visibilityIntentGovernsSymbolInjectors.test.ts`. `EdgeProjectorService.ts:2557` builds one per projection and gates all fifteen symbol injectors through it. A room row in the intent panel needs the SAME question asked for a fill; the honest change is a fill-aware sibling of this gate (it currently keys on `appearance.visible !== false && appearance.line.opacity !== 0`, which is line-only and would answer wrongly for a wash), NOT a private chain inside `RoomColourIntent.ts`. Its header also directly refutes the founder's sibling hypothesis for this lane: 'It does not fix \"hidden furniture still renders\". MEASURED: it does not… The founder's \"maybe because is symbol elements?\" hypothesis is REFUTED at the canvas layer.'",
    "⭐ THE FOUNDER'S OWN INTENT IS READ-ONLY — adding the row makes it VISIBLE but not EDITABLE for him. His panel says 'Architectural Documentation (Auto)', i.e. `system-architectural-documentation`, and `VisibilityIntentPanel.ts:264` passes `intent.isSystem` as the form's `disabled` flag, so every Fill style / Fill colour / Fill opacity control the auditor cites at :351-362 renders `disabled` on his view. He would see a Rooms row he cannot touch until he duplicates the intent (the L-779 duplicate-on-edit flow at :201-205). Finding 1's fix says the ELEMENT_TYPES addition 'alone makes the row appear' and treats the controls as reachable — half true, and the half that is false is the half the founder will hit first.",
    "⭐ SAVED USER INTENTS WILL NOT GAIN THE ROW — the fix silently only works for system intents. `VisibilityIntentStore` rebuilds system intents from code on every construct (`cloneSystemIntents()`, :21) but `deserialize` (:112-119) restores persisted rows only when `!raw.isSystem`, passing them through `migrateIntentToCurrent`. `packages/core-app-model/src/presentation/migrations/IntentSchemaMigrations.ts` is at `CURRENT_INTENT_SCHEMA_VERSION = 5` and NO migrator back-fills new element types — the v2→v3 migrator says so in writing ('ensure `ifc-element` key present … the resolver already falls back to __default__, so this is optional' and then returns the intent untouched). Resolver fallback does NOT rescue the PANEL, because the panel enumerates `Object.keys(intent.elementRules)`. So any intent a user already duplicated will keep showing 18 rows with no Rooms, with nothing failing. The lane needs a v5→v6 migrator that seeds missing ELEMENT_TYPES keys, or the panel must render the UNION of `ELEMENT_TYPES` and the intent's own keys.",
    "⭐ THE ROOM WASH ALSO IGNORES PER-ELEMENT AND PER-CATEGORY VISIBILITY THAT ALREADY WORKS ELSEWHERE — this is the founder's exact complaint shape, applied to rooms, and it is missing from the audit. A 'Rooms' visibility tick ALREADY EXISTS in the project browser: `BrowserDataHelpers` maps `Rooms → roomStore`, and `ProjectVisibilitySection.applyCategoryVisibility()` (:288-295) walks `getCategoryElements(bag,'Rooms')` calling `applyElementVisibility()` (:156-176), which dispatches `view.hideElement` / `view.clearOverride` via `writeElementVisibilityIntent` (:66-74) — a path whose own comment claims it is 'the half that reaches plan / section / elevation / sheets' — and sets `obj.visible=false` on the 3D meshes. But `PlanViewCanvas._renderRoomFills()` (:2488-2534) reads `window.roomStore.getByLevel()` directly and consults NOTHING except `intent.visible` from the VG store: no per-element override, no `bag.elemVisible`, no isolate state. Untick Rooms (or hide one room) and the 3D volume disappears while the pastel wash stays painted on the plan. So the auditor's framing — 'there is no way to hide rooms from the visibility settings' — is incomplete: a control exists, is reachable, and the wash disobeys it. Any lane that adds an intent row without fixing this ships a second control the wash also disobeys.",
    "MINOR, but it changes the 'REGISTRY MAP' verdict for one row: `VGToIntentMigration`'s fall-through at :167 (`VG_CATEGORY_TO_ELEMENT_TYPE[vgCategory] ?? vgCategory`) does carry a `room` key into `elementRules` as the auditor says — but `vgCategoryStyleToElementGraphicsRules` builds it from `VGCategoryStyle` fields only, so `roomColourMode` is dropped AND the migrated key lands on a USER intent (`isSystem:false`), which is the one class of intent that never re-seeds from `ELEMENT_TYPES`. That makes the migration path the only way a `room` row can appear in a user intent today, and it appears only for projects that still hold legacy VG templates — the opposite of 'worth an explicit entry so it stops being luck'; it needs a decision, not a table row."
  ]
}