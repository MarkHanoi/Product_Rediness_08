{
  "refuted": false,
  "reason": "The mechanism is CONFIRMED line-by-line, and my best rivals died against the code — but the claim over-generalises its ordering and one of its ruling-out arguments is unsound.\n\nWHAT I CONFIRMED (all cited lines read):\n1. One-shot install. `GISAreaLayout.ts:1006 if (gisInitPromise) return gisInitPromise;`; the only reset is `:1451`, inside the `.catch` of a FAILED mount. The wiring is further nested in `:1042 if (!cesiumViewport) {` and `if (viewer) {`. `toggleGIS`'s re-activation branches (`:1487-1505`) do no re-wiring.\n2. Single install site. `installSpaceEnvelopeFaceDragOnSurface` has exactly one production caller (`:1336`); `registerSpaceEnvelopeFaceDragSurface` exactly one (`:1273`). The BIM path (`initTools.ts:2147` → `attachSpaceEnvelopeRender`) installs on `world.renderer.three.domElement` and registers NO surface, so the register count really does fall to 0.\n3. Once per tab is TRUE IN PRACTICE, though the claim's cited authority is contradicted by the file it is diagnosing: `GISAreaLayout.ts:126` says \"mountGISArea can run more than once in a session (project switch)\". The code settles it: one call site (`Layout.ts:92` ← `initUI.ts:3301`) behind `src/main.ts:202 let _bootstrapped = false` — bootstrap runs once per tab, and the hub is re-entered in-SPA (`PlatformRouter.showHub`), no reload.\n4. Unconditional teardown, no reseed. `clearLayoutProjectState` (`:8479`) runs `envelopeFaceDrag3dDispose?.()` (`:8504`) and `envelopeFaceDrag3dUnregister?.()` (`:8509`); the scope registration (`:156-161`) declares only `clear`. Both routes reach it: `siteProjectScope.ts:287 clearScopes(GIS_SWITCH_SCOPES)` (derived from DECLARED_PROJECT_SCOPES, and `gis.areaLayout` is declared with a different owning module and no `ownsTeardown:false`, so it is included) and `ClearProjectCommand.ts:246 clearAll()`.\n5. The DRAW asymmetry is real: `:8498` only calls `disarm()`; `envelopeDraw3dUnregister` is never called there, and `arm()` rebuilds its ScreenSpaceEventHandler on the still-live viewer.\n6. The founder's documented behaviour hits the deterministic path. `docs/03-execution/plans/SESSION-HANDOVER-2026-09-08.md §3`: he \"opened a brand-new empty project\" with console attached, in a tab that already had one — that is exactly \"second project in one tab\". The same trace (`[space-envelope] drew 15/15 authored envelope(s)`) also proves a frame WAS seated at that moment, which independently closes the frame-gate rival for that session. And the feature is deployed: `c8c62c51` (09-07 16:04) is an ancestor of HEAD and 09-08 shipped builds on top of it, so this is not a recurrence of L-13236 #1 (\"not in any build he has run\") — the rival I considered most likely given this repo's history.\n\nRIVALS I CONSTRUCTED AND KILLED: re-parenting (a DOM move keeps listeners); source-of-truth divergence (`renderSpaceEnvelopes` reads `runtime.stores.spaceEnvelope`, the identical store the pick reads); role refusal (parcel-law create mints `role:'level'`, which is draggable, and `maximumBuildable` refuses OUT LOUD); focus id mismatch (`storeyRows()` takes ids from `readLevelEnvelopes` → the store's own `r.id`); vertical frame mismatch (rasteriser `bottom = baseHeight + baseOffset` is the exact inverse of the pick's `y = o.z - baseHeightM`); the THREE-canvas overlay swallowing pointers (Cesium's own draw handler binds the SAME `viewer.scene.canvas`, so draw and drag live or die together); and viewer dispose+re-mount (it would break the DRAW too, since `SiteEnvelopeDrawCesium` captures the viewer by value).\n\nWHERE THE CLAIM IS WRONG OR OVERSTATED:\n(a) Its two orderings are mis-weighted. Case (2) (onboarding race) is WEAKER than presented: `ensureGisInitialized('eager')` is gated on `consumeEagerGlobeStart()`, and its own comment states \"a hub project open (no onboarding) never consumes the flag\". On the hub path the init runs in 'activation' mode — only at the user's first 3-D Site activation (`:1499`), which is necessarily AFTER `setProjectContext` and after `ProjectLoader.load`'s `clearAll`. So on a hub-opened FIRST project the gesture is ALIVE, and read as \"always dead\" the claim is false. Case (1) is the load-bearing one, and it holds.\n(b) Its dismissal of hypothesis (b) is logically unsound even though the conclusion survives: `clearSpaceEnvelopes()` is called only by `dispose()` (`CesiumViewport.ts:18954`) and by `renderSpaceEnvelopes` itself — NOT by `resetProjectScopedState('project-switch')`, which nulls `formaMassingOrigin` at `:18640`. Prisms already drawn therefore stay on screen while the frame read returns null. \"Visible\" and \"a frame exists now\" are reads at different times and CAN disagree; the argument \"they read the same field so they cannot disagree\" is wrong.\n(c) One silent rival is never analysed: `SiteEnvelopeDrawCesium.pickFace` returns `null` whenever `this.sink !== null` (an armed perimeter draw, `siteEnvelopeDrawCesium.ts:737`), and `cannotDragReason()` does NOT check `sink` — so an armed draw makes the gesture deaf while the panel's Drag face button stays LIVE. That state is precisely what the author's own falsification test would misread as \"diagnosis falsified\". It self-heals (`takeSpaceEnvelopeFaceDragFocus` disarms the draw) and `finish()` disarms, so it is narrower than the teardown — but it belongs in the probe's interpretation, not only in its output list.\n(d) Minor: `reseedAll()` is at `ClearProjectCommand.ts:251`, not `:249` (249 is the failure log).\n\nVERDICT: the claimed defect exists exactly as described and is severe (permanent for the tab, no recovery), the founder's known workflow reaches it, and no simpler explanation survived. What is NOT established is causation in the specific failing session — the author's own console test remains unrun, and the fix should not be shipped without the probe, because (b) and (c) can produce the identical symptom from the same click.",
  "evidenceChecked": [
    "apps/editor/src/ui/layout/GISAreaLayout.ts:1006,1019,1042,1076,1197-1273,1336,1433,1451 — one-shot init, single install/register site, reset only in the failed-mount catch",
    "apps/editor/src/ui/layout/GISAreaLayout.ts:156-161 — scope registration declares only `clear`, no `reseed`",
    "apps/editor/src/ui/layout/GISAreaLayout.ts:8479-8515 — clearLayoutProjectState: draw only DISARMED (:8498), face-drag DISPOSED (:8504) and UNREGISTERED (:8509)",
    "apps/editor/src/ui/layout/GISAreaLayout.ts:126 — comment claiming mountGISArea can run more than once, contradicting the claim's cited declaredProjectScopes authority",
    "apps/editor/src/ui/layout/GISAreaLayout.ts:1487-1505, 2608-2617 — toggleGIS re-activation does NOT re-wire; eager init gated on consumeEagerGlobeStart()",
    "apps/editor/src/ui/Layout.ts:92 + apps/editor/src/engine/initUI.ts:3301 — the single mountGISArea call site",
    "src/main.ts:202-215, 300-375 — `_bootstrapped` latch: bootstrap/initUI runs once per tab; hub re-entered in-SPA without reload",
    "packages/runtime-composer/src/buildPersistence.ts:263-348 — openProject order: ensure() [engine boot] → streamLoad → setProjectContext",
    "apps/editor/src/ui/platform/PlatformShell.ts:218-250 — setProjectContext emits pryzm-project-switch, guarded only against the same id",
    "apps/editor/src/ui/site/siteProjectScope.ts:104-110, 250-290, 441 — GIS_SWITCH_SCOPES derivation and the switch subscription",
    "packages/core-app-model/src/persistence/declaredProjectScopes.ts:247-268 — gis.areaLayout declaration (module-scope presence, no ownsTeardown:false)",
    "packages/command-registry/src/project/ClearProjectCommand.ts:21,246,251 — clearAll() then reseedAll() as the first step of ProjectLoader.load()",
    "packages/persistence-client/src/loader/ProjectLoader.ts:381 + apps/editor/src/engine/persistence/ProjectLoader.ts:962 — ClearProjectCommand callers",
    "apps/editor/src/engine/spaceEnvelopeDragSurface.ts:543-600, 583-586, 923-945 — pick/focus, silent miss, listener registration and disposer",
    "apps/editor/src/ui/site/siteEnvelopeDrawCesium.ts:254, 633-646, 682-800, 985-1100 — captured viewer, cannotDragReason (does NOT check sink), pickFace's `sink !== null` deafness, arm/disarm",
    "apps/editor/src/ui/site/siteEnvelopeDrawArming.ts:424-427, 559-585, 594-604 — finish() and first-click both disarmAll()",
    "apps/editor/src/ui/site/spaceEnvelopeFaceDragSurfaces.ts:62-118, 141-190 — register/unregister, NO_SURFACE reason, takeFocus disarms the draw",
    "apps/editor/src/ui/analysis/parcelLawEnvelopeAuthoring.ts:960-1145 — Drag face button: ids come from the store, disabled state carries the surface's verbatim reason",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:8749-8762, 8796-8930 — getSpaceEnvelopeSceneFrame and renderSpaceEnvelopes read the same origin and the same store; prism seat math is the pick's exact inverse",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:18595-18700, 18954-18959 — resetProjectScopedState nulls formaMassingOrigin but does NOT clearSpaceEnvelopes(); only dispose() does",
    "apps/editor/src/engine/initTools.ts:2085-2160 — the BIM/THREE face drag binds the THREE canvas and registers no face-drag surface",
    "docs/04-reference/ISSUE-LOG.md L-13236 — the prior report was 'not in any build he has run'; the arrows/handles and per-storey focus shipped 2026-09-07",
    "docs/03-execution/plans/SESSION-HANDOVER-2026-09-08.md §1-§3 — 09-08 deploys on top of c8c62c51, and the founder opening a brand-new project in an already-used tab",
    "git merge-base --is-ancestor c8c62c51 HEAD → true (the face-drag lane is in the shipped tree)"
  ],
  "betterHypothesis": "Keep the claim's mechanism, but restate its trigger precisely and pair the fix with the probe. PRECISE FORM: the 3-D Site face-drag is installed once per tab, at whichever moment `ensureGisInitialized` first settles, and is destroyed by the FIRST project-scope clear that follows it, with no re-install. Which sessions are hit is decided by WHEN the init runs: on a hub-opened project (activation mode) the install lands after every clear and the gesture WORKS; it dies on the next project opened in that tab, on a version restore, or on any later ProjectLoader.load — and the onboarding/eager path is a genuine race that can kill it on the first project. The founder's documented habit of opening a second project in a live tab (SESSION-HANDOVER-2026-09-08 §3) is the likely trigger, and the confirming UI signature is a greyed Drag face button reading SPACE_ENVELOPE_FACE_DRAG_NO_SURFACE_REASON while the 3-D Site is on screen. TWO ADDITIONS THE CLAIM MISSES: (1) `resetProjectScopedState('project-switch')` nulls `formaMassingOrigin` without calling `clearSpaceEnvelopes()`, so restoring the listeners alone still leaves a window with prisms on screen and `getSpaceEnvelopeSceneFrame()` null — the drag holds, silently; fix (a) should also re-render or clear the prisms on the scope clear so \"on screen\" and \"grabbable\" cannot diverge. (2) `pickFace` is silently deaf while a perimeter draw is armed and `cannotDragReason()` does not say so — make the armed-draw state a fourth reason in `cannotDragReason()`, or the panel keeps promising a gesture the surface is currently refusing. Ship the probe FIRST: with `installed`, `registeredSurfaces`, `sceneFrame`, `drawArmed` and `focus` in one line, this defect is decidable in one click instead of a third report."
}