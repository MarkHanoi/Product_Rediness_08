{
  "survivingTitles": [
    "0% hit rate on a fresh open is EXPECTED, not a defect — the cache is per-session in-memory and this was its first pass",
    "`yieldCalendar≈83ms` is COMPUTED, not measured — it is literally `yields × 16.67`",
    "`work=109ms` is a LOWER BOUND — it excludes hidden-line removal, every symbol injector, VG styling, and the caller's mesh export",
    "The plan projection during open is DELIBERATE (Contract 17 §4 auto-opens the split plan pane) — and the user's decision to close that pane is never persisted"
  ],
  "refutedTitles": [
    "Doors and windows mint a fresh cache version on EVERY build via `Date.now()`, so they can never hit after a rebuild — walls solved this with a content hash and doors did not"
  ],
  "refutations": [
    "REFUTED (wrong dimension — costs zero OPEN time, and the citation is to the wrong line). The code facts are mostly right: door IS in CACHEABLE_ELEMENT_TYPES (EdgeProjectorService.ts:2015-2016), the monotonicity comment is verbatim at 2005-2010, WallFragmentBuilder._versionForBuild is real (388-445), furniture uses `_priorVersion + 1` (FurnitureFragmentBuilder.ts:142) and lighting the same (LightingFragmentBuilder.ts:734), and the version keys a SECOND cache too (`${elementId}:${viewId}:${currentVersion}:${cropKey}`, NativeElementMeshExporter.ts:~530). But version STABILITY can only produce a hit on the second-or-later projection of a session. On a fresh open both caches are empty (finding 1, which the same audit proves), so all 63 groups miss no matter how the token is minted — a perfectly content-hashed door still misses on open. Nothing rebuilds doors between hydration and the single rIC-deferred projection, so there is no second pass during open for stability to help. This belongs on an EDITING dimension, not the project-open path. Citation error to fix before it is re-used: DoorBuilder.ts:431 is the PROPERTY-ONLY PATCH path (it sits beside `_bus.emit('bim-door-updated')`); the build-path stamp is DoorBuilder.ts:599 inside the §DOOR-AUDIT-2026 userData freeze at 585 (WindowBuilder.ts:829 is the window equivalent). Sending the next lane to :431 sends it to the wrong function.",
    "CAVEAT on surviving finding 1 — it is not 0ms. Confirmed: `_cwProjectionCache` is a plain instance Map (1943-1955), no serialisation anywhere, `invalidateCwView` has no caller outside the class, `clearCwProjectionCache` has exactly one caller (initScene.ts:3616, in the `pryzm-project-switch` handler that fires before hydration — and the facade at 1227-1232 makes it a literal no-op when the service has never been lazy-loaded, i.e. on a session's first open), and the service is constructed once per session (initScene.ts:1146-1160). So 0% on a first open is by construction. BUT the claimed cost of 0ms is wrong: the MISS path does `geo: projGeo.clone()` per emitted layer (EdgeProjectorService.ts:3231-3235) purely to populate the cache, and `_putCwCache` (2136-2163) retains those clones for the whole session. On this open that is 63 elements' worth of BufferGeometry cloned and held for a pane the founder may close — the one way the cache genuinely does charge the open. Price it as small-but-nonzero, not zero.",
    "CAVEAT on surviving finding 2 — confirmed verbatim and it is the strongest finding here. `yieldCalendar≈${((_perLayerYieldCount + _groupYieldCount) * (1000 / 60)).toFixed(0)}ms` is EdgeProjectorService.ts:3648, and at both yield sites (3470-3480 per-layer, 3542-3548 per-group) `_workMsTotal` is accumulated BEFORE the await and `_lastYieldAt` is reset AFTER it, so the elapsed time across the yield is discarded by construction. One correction to the mechanism: the auditor speculates the yield could stall; it cannot hang, because `scheduleOnce` → `addTickListener` calls `wakeIfStopped()` (FrameScheduler.ts:430-447), so an idle-stopped loop is re-armed. The 83 is still synthetic and must not be attributed to open time.",
    "CAVEAT on surviving finding 3 — confirmed, and understated. `project()` spans EdgeProjectorService.ts:2258-4258; the two summary logs print at 3633 and 3664, while 14 symbol injectors run at 3941-4083 and `applyOcclusion` at 4207 — all after the numbers are printed. `applyOcclusion` (HiddenLineRemoval.ts:777, 954-line module) contains ZERO `await`s and no `performance.now()` anywhere, so it is not merely untimed, it is UNYIELDABLE. `nativeElementMeshExporter.exportForView(viewDef)` is likewise synchronous (`exportForView(viewDef: ViewDefinition): THREE.Group[]`, NativeElementMeshExporter.ts:281) and runs in the caller at initScene.ts:1403 before `project()` at 1453.",
    "CAVEAT on surviving finding 4 — the mechanism is confirmed end-to-end but the 192ms price tag must be cut, and the line numbers are off by one. Confirmed: the Contract 17 §4 comment at initScene.ts:4635, the handler at 4639, `splitViewManager.activate()` at 4658, `ric(_activate, { timeout: 1500 })` at 4665 with `setTimeout(_activate, 600)` fallback at 4667; the event chain at 1519-1530 into the predicate at 1508-1514; `notifyViewActivated` only fires a projection for a view in `_deferredDirtyViewIds` (ViewDependencyTracker.ts:517-535), which the plan is after a 503-element hydration; `deactivate()` (SplitViewManager.ts:401-431) writes nothing, and `localStorage` appears in that whole file only twice, both for `pryzm.splitView.gridVisible` (2179/2187). The getters the pure decision reads exist (isActive:220, autoOpenSuppressed:231, activeViewId:242). THE PRICE IS WRONG: 192ms = 109 + 83, and the 83 is the synthetic number this same audit refutes in finding 2. State it as '≥109ms of loop CPU plus an unmeasured tail', never 192ms.",
    "CAVEAT on surviving finding 4, second half — the attribution to the OPEN window is unproven by the evidence offered. The founder's excerpt carries no timestamps and no `[SplitViewManager] Split view auto-opened on project load (idle)` line, and `requestIdleCallback(_activate, { timeout: 1500 })` means the pass begins up to 1.5s AFTER `pryzm-project-loaded` — i.e. it may land after the open the founder is timing, in which case it steals his first interactions rather than lengthening the open. The code chain is sound (nothing else projects a plan when the main viewport is 3D: ViewController.ts:693 and 2292 both require an explicit view activation), but 'this is inside the open' is an inference, not a measurement."
  ],
  "missed": [
    "THE UNYIELDABLE TAIL is the part most likely to be felt, and no finding names it. The chunk policy protects only the per-group loop; everything from the last yield onward runs as ONE task: 14 symbol injectors (EdgeProjectorService.ts:3941-4083) then `applyOcclusion` (4207) into HiddenLineRemoval.ts:777, a 954-line module with no `await` and no timer, doing O(occluders × targets) over 26 occluders and 410 edge geometries. Finding 3 calls this 'untimed'; the sharper fact is that it is structurally incapable of yielding, so whatever it costs it costs as a single long frame during (or immediately after) open. Time THIS before timing anything else.",
    "`nativeElementMeshExporter.exportForView()` is a synchronous, unchunked pass that runs BEFORE the projector on the open critical path with a cold `_proxyCache`. Signature is `exportForView(viewDef: ViewDefinition): THREE.Group[]` (NativeElementMeshExporter.ts:281) — no Promise, no yields — and per surviving element it does `root.updateWorldMatrix(true, false)`, crop/scope AABB tests and proxy construction (495-540, cache insert at 763-770). Called at initScene.ts:1403 and again from PlanViewManager.ts:~875 and ViewController.ts:677/2282. Finding 3 mentions its missing log line; the open-time finding is that it is the second unyieldable block in this path and it is 100% miss on open.",
    "The session's FIRST projection also pays the lazy module load. `_ensureEdgeProjectorService()` (initScene.ts:1143-1168) does `await import('./views/EdgeProjectorService')` — a 4,533-line module with 43 top-level imports, 16 of them symbol builders pulled from @pryzm/geometry-* — fetched, parsed and evaluated on the open critical path, kicked off by the same rIC(timeout 1500). It appears in no number in this dimension. (Discount it partially: the geometry-* packages are already resident from element build, so the marginal cost is the EPS chunk itself.)",
    "The one instrumentation fix that would settle this dimension is not proposed by any finding: neither the founder's log excerpt nor any of the three log lines carries a WALL-CLOCK TIMESTAMP relative to `pryzm-project-loaded`. Until `project()` prints `startedAt=<ms since project-loaded>` alongside a true end-to-end `total=`, nobody can say whether this ~109ms+ lands inside the open or 1.5s after it — which decides whether this dimension is worth any budget at all. That is a cheaper first move than any of the four proposed fixes."
  ]
}