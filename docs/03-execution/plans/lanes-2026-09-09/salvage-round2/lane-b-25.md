{
  "refuted": false,
  "reason": "MECHANISM CONFIRMED FROM CODE; VERDICT RESTS ON ONE UNVERIFIABLE INPUT; TWO FIX PREMISES ARE FALSE.\n\nWHAT VERIFIES EXACTLY (every citation checked verbatim):\n- globeGroundAnchor.ts:198-216 `classifyTileGroundPicks` is exactly as described: lowIdx=floor(0.1n), midIdx=floor(0.5n), spanM=median-p10, maxGapM scanned ONLY over [lowIdx+1, midIdx]; `plateau` if span<=6 (:167), `roof-gap` if maxGap>4 (:172), else `slope` BY ELIMINATION. It is a default arm, not a positive finding — the claim's central structural point is true independent of any console.\n- :279 `if (cls.arm === 'slope' && cls.medianM !== null) return cls.medianM - seatEpsilonM;` and CesiumViewport.ts:619 `GLOBE_GROUND_SEAT_EPSILON_M = 0.3`. resolveGlobeGroundAnchor passes that epsilon (CesiumViewport.ts:9396) and returns it straight to `commitPhotorealBase` → `formaTerrainBaseHeight`. With n=58: midIdx=29, lowIdx=5. 78.94-0.30=78.64 reconciles, and 78.64 is printed by the probe at :13141 (`seat=${formaTerrainBaseHeight}`) — the same log line that prints `anchor-in-void`, which is why both anomalies appear together.\n- safeSampleTileHeights (:9014-9036) does return bare `number[]`, pushing only `h` and dropping non-finite entries, so index correspondence to `samplePts` is destroyed too. The 1-D-domain argument is correct.\n- :9203-9210 skips centroid+boundary picks whenever `photorealTilesActive`; ring = vertices at 1.7x + 16 points at ext+25 + 16 at ext+55 (:9241-9246). Roof-heavy input is designed, not accidental.\n- GlobeGroundAnchor.test.ts:473-480 is exactly the synthetic bimodal set with a clean ~14 m gap (50.6 -> 65); the sibling \"flat city\" fixture has a clean 51.3 -> 62 gap. No fixture models a photogrammetric continuum. `git show c36b75bc` confirms the classifier is 3 days old and the ISSUE-LOG row L-12919 says outright \"Not browser-verified\".\n- Second anomaly is CORRECT: boundaryProjection.ts:142 returns ring[0]; SiteBoundaryDrawTool.ts:283 dispatches that as the site location; CesiumViewport.ts:17457 sets `realModelOnGlobeOrigin` from input.originLat/Lon. The probed point is BY CONSTRUCTION a parcel ring vertex, so `anchor-in-void=n` is not evidence of displacement. (Minor over-citation: pointInPolygon.ts:123's ulp remark is about a point on the RAY; a vertex gets a deterministic-but-arbitrary answer under the half-open straddle rule. Conclusion unchanged.)\n\nRIVALS I CONSTRUCTED AND KILLED IN THE CODE:\n(a) A second writer overwrites the seat — `ensureGroundBaseForContextInner` (:11783) and `clampTerrainThenReplace` (:9878) both write `formaTerrainBaseHeight` from bare-earth terrain. DEAD: `decideBakedTerrainAttach` (terrainCoverage.ts) returns `{attach:false, reason:'photoreal'}` when `photorealActive && !formaMode`, so no elevation provider exists on this path and `terrainProviderHasElevationData` short-circuits.\n(b) Stale base reused for a moved parcel — DEAD: `centroidUnchangedFor` (:8952) uses 1e-6 deg (~0.1 m).\n(c) Self-hit feedback: ring picks landing on PRYZM's own geometry and ratcheting the seat — DEAD: context extrusions are aborted and cleared on this exact path (:8301-8312), the model is in `objectsToExclude`, and the void cap + massing lie inside the parcel while every pick lies outside it.\n(d) The void is buffered so the 1.7x ring falls through it — DEAD: the clip polygon (:12960) is `committedParcelLonLat` un-buffered.\n(e) GLB-local vertical offset — WEAK: model, massing and cap all key off the single `formaTerrainBaseHeight`, and the cap floating implicates the datum, not the GLB.\n(f) Genuine hillside (slope arm correct) — LARGELY KILLED by independent repo evidence: committedParcelRing.spec.ts:24 pins the founder's parcel at Placa Sant Jaume (41.3825, 2.1769), flat Gothic Quarter; the repo's own geoid figure (+49 m, globeGroundAnchor.ts L-477 note) makes the quoted min 64.89 ≈ 15.5 m MSL — Placa Sant Jaume's real street elevation — and the median 78.94 ≈ 29.5 m MSL ≈ 14 m above street, i.e. a Gothic-Quarter roofline, not terrain (Mont Tàber, the local high point, is ~2 m above the placa). And the real Sète hillside ramps through its low tail (min 132 -> p10 155), whereas the quoted Barcelona floor is 1.46 m tight across its lowest 6 picks — a plateau plus a rising population.\n\nWHY THIS IS NOT UNCONDITIONALLY \"CONFIRMED\": the load-bearing input (the 58 picks, arm=slope) is not in the repo and I could not verify it. If the console actually printed `plateau` or `roof-gap`, the entire mechanism is wrong. The claim states the reproduction as fact rather than as conditional on that one log line.",
  "evidenceChecked": [
    "apps/editor/src/ui/geospatial/globeGroundAnchor.ts:159-216 (SLOPE_RAMP_MIN_SPAN_M=6, ROOF_GAP_M=4, classifyTileGroundPicks — 'slope' is reached by elimination; maxGap scanned only over [p10, median])",
    "apps/editor/src/ui/geospatial/globeGroundAnchor.ts:270-285 (reduceTileGroundHeight — slope arm returns medianM - seatEpsilonM, all other arms return the p10)",
    "apps/editor/src/ui/geospatial/globeGroundAnchor.ts:336-352 (resolveGlobeGroundAnchor passes rejectEllipsoidPicks + seatEpsilon straight through; heightM is the seat)",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:619 GLOBE_GROUND_SEAT_EPSILON_M = 0.3; :653/:660 PHOTOREAL_VOID_CAP_DEPTH_M=60 / SEAT_EPSILON=0.05",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:9014-9036 safeSampleTileHeights returns number[] (out.push(h) only; non-finite dropped, so even index correspondence to samplePts is lost)",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:9150-9250 sample-ring construction; :9203 parcelVoidCut = photorealTilesActive skips centroid+boundary picks; :9241-9246 two 16-point rings at ext+25 / ext+55",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:9330-9470 clamp -> §GLOBE-PICK-SPREAD-DIAG -> §GLOBE-SLOPE-SEAT arm log -> resolveGlobeGroundAnchor -> commitPhotorealBase",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:9643-9700 commitPhotorealBase (single writer of formaTerrainBaseHeight on this path)",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:8952-8971 centroidUnchangedFor (1e-6 deg) — kills the stale-base rival",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:8293-8312 context extrusions CLEARED and fetch ABORTED when keepPhotoreal && photorealTilesActive — kills the self-hit rival AND falsifies fix arm (1)'s 'data already in memory'",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:10994-11040 maybeAttachTerrainProvider + terrainCoverage.ts decideBakedTerrainAttach -> {attach:false, reason:'photoreal'} — kills the terrain-overwrite rival AND falsifies fix arm (2)'s premise",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:11749-11800 ensureGroundBaseForContextInner (the only other base writer; gated on terrainProviderHasElevationData)",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:12870-12985 applyParcelClipToPhotorealTiles — the clip ring is the un-buffered committedParcelLonLat",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:13018-13145 applyPhotorealVoidCap (top = base - 0.05) and logPhotorealVoidVsBuilding (:13120 anchor point test, :13141 seat= print)",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:17417-17470 real-model seat = Cartesian3.fromDegrees(originLon, originLat, formaTerrainBaseHeight); realModelOnGlobeOrigin = the input origin",
    "apps/editor/src/ui/site/boundaryProjection.ts:125-146 parcelFrameOrigin = ring[0], with the C57 rationale",
    "apps/editor/src/ui/geospatial/SiteBoundaryDrawTool.ts:275-292 dispatchSiteLocation(parcelFrameOrigin(vertices))",
    "packages/geometry-kernel/src/pure/pointInPolygon.ts:110-130 (the ulp remark is about a point on the ray, not exactly 'on the boundary')",
    "apps/editor/__tests__/GlobeGroundAnchor.test.ts:451-486 — all three classifier fixtures are synthetic; both non-slope fixtures have a clean >10 m street/roof gap",
    "git show c36b75bc (2026-09-05, classifier introduced) and git diff c36b75bc..HEAD -- globeGroundAnchor.ts (only L-13078 readable-height helpers added since; thresholds untouched)",
    "docs/04-reference/ISSUE-LOG.md L-12919 row — Sète numbers (64 picks 132->206, p10 seat 154.84) and the explicit 'Not browser-verified' acceptance clause",
    "apps/editor/src/ui/site/__tests__/committedParcelRing.spec.ts:24 — the founder's parcel is Placa Sant Jaume, Barcelona (41.3825, 2.1769); corroborated by globeGroundAnchor.ts:468 (founder Barcelona lat 41.38258, 2026-09-07)"
  ],
  "betterHypothesis": "Not a replacement — a surviving rival plus two corrections that change the FIX, and one cheaper fix the claim missed.\n\nSURVIVING RIVAL (untested by the author, and it decides whether their fix works): the ring may be so roof-heavy that even the p10 is NOT street. Placa Sant Jaume is medieval fabric with 3-6 m streets; two 16-point compass rings at ext+25 / ext+55 plus 1.7x-pushed vertices may hit almost no open street cell, and Google's mesh webs across narrow streets. The claim asserts the pre-L-12919 seat (p10 - 0.30 = 66.05) \"would be flush\" purely because it is lower — there is no evidence it is ground. Note the repo's own earlier Barcelona reading (L-479 comment: \"picks=48 -> 50.87 m (correct: Barcelona tile surface)\") is ~14 m BELOW the quoted min of 64.89; different parcel is the likely explanation, but it is unverified, and if it were the same parcel the ENTIRE ring is elevated and no quantile of it fixes anything. Reverting to p10 therefore risks the §SAME-RULE-TWO-IMPLEMENTATIONS / §COMMITTED-IS-NOT-REACHABLE outcome: shipped, tests green, founder still sees the float.\n\nTWO FIX PREMISES ARE FALSE ON THIS EXACT PATH:\n- Arm (1) \"the data is already in memory — contextBuildingPlacements\": FALSE. CesiumViewport.ts:8301-8312 aborts the fetch and calls clearContextBuildings() whenever `keepPhotoreal && photorealTilesActive`. There are no footprints on the photoreal globe. The arm needs a new data-only footprint fetch first.\n- Arm (2) \"maybeAttachTerrainProvider runs (:5250) and contextGroundCache already holds heights\": FALSE. decideBakedTerrainAttach returns {attach:false, reason:'photoreal'} for photorealActive && !formaMode, so no provider is attached and no terrain heights exist to difference against. The arm needs its own fetch, and it leans harder on ADR-0268 §156 than the proposal admits.\nArms (4) (relative ceiling min_credible + |grad h|·R_ring) and (5) (replace the synthetic fixtures with the real 58-value Barcelona set and the Sète ring) are sound, cheap, and would have caught BOTH Sète and this. Ship (4) and (5) first.\n\nTHE CHEAPER FIX NEITHER SIDE PROPOSED: the seat must estimate the ground UNDER the parcel, but §GLOBE-DONT-MEASURE-THE-HOLE forfeits every in-parcel pick because we clip the void BEFORE we measure. Sample the centroid and boundary vertices BEFORE applying `tileset.clippingPolygons` (or cache the pre-clip picks and re-use them), and the only picks that directly measure the answer come back — no footprint mask, no second data source, no plane fit.\n\nBEFORE ANY CODE CHANGES, run the author's own falsifier, because one log line carries the whole diagnosis: confirm the console actually printed `arm=slope` with n=58, and log the 58 picks WITH (lon,lat). If it printed `plateau` or `roof-gap`, the mechanism above is wrong and the seat came from the p10, which sends the investigation to why the ring's low decile is 14 m above the street instead of to the classifier."
}