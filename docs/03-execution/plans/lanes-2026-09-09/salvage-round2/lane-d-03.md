{
  "area": "renderer-three / RenderPipelineManager — the shadow-triggered POST-PROCESSING pipeline rebuild on the project-open path (packages/renderer-three/src/pipeline/RenderPipelineManager.ts)",
  "rootCause": "SHADOW_REBUILD_COMPLETE contains ZERO shadow work and zero work proportional to the scene. It is a misnamed post-processing pipeline rebuild, and its 16,011 ms is WAIT, not compute — a submit-pause held open across the whole hydrate.\n\n(a) WHAT IT TIMES. RenderPipelineManager.ts:2034-2038 starts a clock and logs it around `Promise.resolve(this._rebuildPipeline())`. `_rebuildPipeline()` (:4911) -> `_rebuildPipelineWithCurrentState()` (:4893) -> `_buildPipeline()` (:3753), because SSGI is `false` at EVERY tier (SceneQualityTierManager.ts:199/209/220/232), so the phase-3 branch is never taken. `_buildPipeline`'s entire body is: `await import('three/webgpu')` (:3763); `createScenePass` (ScenePass.ts:48-124 — O(1), one `pass(scene,camera)`); `createZonePass` (ZonePass.ts:59-78 — O(1)); ~10 TSL node ops; `new RenderPipeline(renderer)`; `_safeDisposeRenderPipeline()` (:4781), whose `RenderPipeline.dispose()` in the INSTALLED three r183.2 is one line — `this._quadMesh.material.dispose()` (three/src/renderers/common/RenderPipeline.js:151-153); `_emitState()` (:5451, three bus emits). It is NOT a shadow-map render, NOT a flag traverse (that is the separate 7.7 ms §LOAD-SHADOW-PASS-DEFERRED pass, initScene.ts:4079-4144), NOT a light/camera fit. Nothing scales with 3,762 meshes or 3,274 casters.\n\nThe only non-microtask suspension point in the window is the dynamic import at :3763 — of a module already fetched AND evaluated (WebGPURendererAdapter.ts:139 loaded `three/webgpu` to construct the live renderer; the run reports `backend=real-WebGPU`; Vite puts all of `node_modules/three/` in one `vendor-three` chunk, vite.config.ts:569). So the chain stalled on a module-map hit whose continuation task was not serviced for 16 s while the main thread ran the concurrent 17.16 s chunked hydrate. Corroborating arithmetic: HYDRATE 17,163.5 - SHADOW 16,011.5 = 1,152 ms; the trigger is `pryzm-project-switch` -> `onProjectSwitch()` -> `scheduleShadowRebuild()` (:3230, fired from initScene.ts:3608), emitted BEFORE the incoming project hydrates, plus the 100 ms `SHADOW_REBUILD_DEBOUNCE_MS` (:212).\n\nTHE ACTUAL HARM. `scheduleShadowRebuild` opens `_beginShadowRebuildGuard()` (:1739-1742) before the async work and closes it only in `_finishRebuildAndDrainQueue()` (:2047). For all 16,011 ms `render()` returns at :1257 `if (this._shadowRebuildPaused) return this._skipFrame('shadowRebuildPaused')`, and the MEASURED comment immediately below (:1268-1288) states there is no second rAF underneath — \"NOTHING repaints ... a TOTAL VIEWPORT FREEZE for its duration\". The chunked loader exists precisely so \"the user sees a progressive build\" (ProjectLoader.ts:785-796); it painted nothing for 16 of its 17 s. A guard sized for a millisecond rebuild was held for the whole load.\n\n(b) SHADOW-MAP RENDERS = 3,274 x 1 light x 1 cascade, NOT x87. PascalSceneLighting creates exactly ONE caster — the key DirectionalLight, 1024^2 (`PascalSceneLighting.ts:292-297`); fill (:324-328), rim (:331-335), ambient (:338) never set castShadow; fixture point lights never cast (shadowCasterPolicy.ts:44). No CSM anywhere. `_forEachShadowCastingLight` (:2384-2394) only visits `isDirectionalLight && castShadow`. The census prints the same model: `forward + casters = 3715 + 3274 = 6989` (pryzmPerfConsole.ts:632-636). The 87 fixtures are irrelevant to the shadow pass. And that pass runs ZERO times inside the 16 s: the guard sets `shadowMap.autoUpdate=false` AND per-light `shadow.autoUpdate=false` (:2267, :2385-2387).\n\n(c) ORDERING. `open:first-interactive-frame` is `getFrameScheduler().scheduleOnce('perf104-first-interactive-frame', ...)` (PlatformShell.ts:203-205) — the next scheduler tick after `open:snapshot-loaded`; `addTickListener` restarts a stopped loop (FrameScheduler.ts:442, :920). So +15,628 ms means 15.6 s of main-thread blockage AFTER the load: the WebGPU->WebGL live swap + device loss, 5x REDETECT_ROOMS, 4x TopologyLayer adjacency (528 elems / 2,835 edges), the ProjectSerializer snapshot. The shadow rebuild contributes ~0 CPU to it. The two windows OVERLAP but are not the same interval: the rebuild opens ~100 ms after project-switch (before hydrate), spans the hydrate, and settles inside the post-load window (its log lands after `open:snapshot-loaded` t+21,139 and before t+36,767).\n\n(d) NO PROGRESSIVE PATH EXISTS, and one is not what is needed. `ShadowQualityUpgrader` is a quality-LEVEL setter (512/2048/4096 + bias/radius, :95-118), not a deferral. The heavy-scene lever `_LARGE_SCENE_SHADOWS_OFF_MESH_COUNT = 8000` (RenderingPipelineCoordinator.ts:567) does not trip at 3,696 meshes. The shadow side of load IS already deferred correctly (initScene.ts:3580-3597 load freeze; §LOAD-SHADOW-PASS-DEFERRED coalesced 500 events into one 7.7 ms pass). The UN-deferred thing is the pipeline rebuild — and `onProjectSwitch()`'s own closing NOTE (:3252-3256) says it must be deferred: \"Pipeline rebuild is intentionally deferred to onProjectLoaded(). Rebuilding here (on pryzm-project-switch) runs longtasks while BIM elements are mid-load, producing blank render frames that make geometry appear to vanish.\" L-316 added `this.scheduleShadowRebuild()` at :3230 — twenty lines ABOVE that NOTE, in the same method — and left the NOTE standing. Separately, the sibling caster-release guard is frame-capped at 8 precisely because \"an unbounded window is a frozen viewport — the L-663 shape\" (:187, :1779-1786); THIS guard has no cap at all.\n\n(e) 87% SHOULD NOT CAST, and the repo already agrees. `shadowCasterVerdict` returns 'builder' for furniture/kitchen/lighting (shadowCasterPolicy.ts:126-155) — \"the builder decides\". The builder lever is read at BUILD TIME ONLY: FurnitureFragmentBuilder.ts:280 `child.castShadow = furnitureCastsShadowUnderBudget(data.furnitureType)`; `setFurnitureShadowBudget()` only flips a module variable with no retro-apply (furnitureShadowBudget.ts:78, :98-108). The tier hook that would lower it (initScene.ts:2653-2655) fires from the tier pass, which in this run is `reason=tier:post-load` — after every furniture mesh was built at the default `'full'`. Hence FurniturePart 1,421/1,487 and KitchenCabinetPart 647/647 cast: 2,068 of 3,274 casters (63%) from an authored lever that never reaches the meshes it was written for.",
  "evidence": [
    "RenderPipelineManager.ts:2034-2038 — the timer: `const __t_shadow_start = performance.now(); Promise.resolve(this._rebuildPipeline()).then(() => { console.log(`[RenderPipelineManager] SHADOW_REBUILD_COMPLETE elapsed=${(performance.now() - __t_shadow_start).toFixed(1)}ms`); })`. It times `_rebuildPipeline()` and nothing else.",
    "RenderPipelineManager.ts:3763 — the ONLY non-microtask suspension point in that window: `const { RenderPipeline } = await import('three/webgpu') as any;`. Everything after it in `_buildPipeline()` (:3753-3821) is synchronous O(1) node-graph construction.",
    "WebGPURendererAdapter.ts:139 — `const { WebGPURenderer } = await import('three/webgpu');` — the same module, already fetched and evaluated to build the live renderer (run reports `backend=real-WebGPU`). vite.config.ts:569 `if (id.includes('node_modules/three/')) return 'vendor-three';` — one chunk, so :3763 is a module-map hit, not a fetch.",
    "three@0.183.2 src/renderers/common/RenderPipeline.js:151-153 — `dispose() { this._quadMesh.material.dispose(); }`. Rules out the in-file claim that dispose fans out per render object; `_safeDisposeRenderPipeline()` (RenderPipelineManager.ts:4781-4836) cannot be O(3762).",
    "ScenePass.ts:59 `const scenePass: PassNode = pass(scene, camera);` and ZonePass.ts:74-76 — both O(1); no traverse, no per-mesh work anywhere in the timed window.",
    "SceneQualityTierManager.ts:199/209/220/232 — `ssgi: false` at cinematic/balanced/performance/survival. So `_rebuildPipelineWithCurrentState()` (:4893-4899) always takes the cheap `_buildPipeline()` branch, never `_buildPhase3Pipeline`.",
    "RenderPipelineManager.ts:1257 — `if (this._shadowRebuildPaused) return this._skipFrame('shadowRebuildPaused');` and the corrected comment at :1268-1288: \"There is no second rAF loop painting a base frame underneath us. When this method returns early, NOTHING repaints ... Every `return` above is therefore a TOTAL VIEWPORT FREEZE for its duration.\" The 16,011 ms window is a 16-second frozen viewport.",
    "RenderPipelineManager.ts:3229-3230 (inside `onProjectSwitch()`): `this._reconcileRenderSize(); this.scheduleShadowRebuild();` — contradicted twenty lines later at :3252-3256: \"NOTE: Pipeline rebuild is intentionally deferred to onProjectLoaded(). Rebuilding here (on pryzm-project-switch) runs longtasks while BIM elements are mid-load, producing blank render frames that make geometry appear to vanish.\" Same method, both live.",
    "initScene.ts:3607-3608 — `_freezeShadowForLoad(); renderPipelineManager.onProjectSwitch();` on `pryzm-project-switch`, which initScene.ts:3625-3628 documents is emitted \"on EVERY project-entry path ... BEFORE the incoming project hydrates\". The shadow-map freeze half of L-316 is already provided independently, one line earlier.",
    "PascalSceneLighting.ts:292-297 — the ONE shadow caster: `keyLight.castShadow = true; keyLight.shadow.mapSize.set(cfg.shadowMapSize /*1024*/, ...)`. Fill (:324-328), rim (:331-335), ambient (:338) set no castShadow. So casters x lights x cascades = 3274 x 1 x 1 = 3,274 depth draws per refresh — matching pryzmPerfConsole.ts:632-636's own `3715 + 3274 = 6989`.",
    "RenderPipelineManager.ts:2267 + :2385-2387 — `shadowMap.autoUpdate = !shouldFreeze;` and `this._forEachShadowCastingLight((sh) => { sh.autoUpdate = !shouldFreeze; ... })`. The guard freezes the depth pass, so ZERO shadow-map renders occur inside the 16 s.",
    "PlatformShell.ts:203-205 — `getFrameScheduler().scheduleOnce('perf104-first-interactive-frame', () => { markStartupPhase('open:first-interactive-frame'); })`, and FrameScheduler.ts:442/:920 `addTickListener` -> `wakeIfStopped()`. The +15,628 ms is 15.6 s of post-load main-thread blockage, not a shadow cost.",
    "RenderPipelineManager.ts:187 `const MAX_CASTER_RELEASE_PAUSED_FRAMES = 8;` with the rationale at :1779-1786 — \"an unbounded window is a frozen viewport — the L-663 shape ... A bounded guard that degrades is worth more than an unbounded one that blanks the screen.\" `scheduleShadowRebuild`'s guard (:1972, released :2047-2050) has no such cap.",
    "FurnitureFragmentBuilder.ts:280 — `child.castShadow = furnitureCastsShadowUnderBudget(data.furnitureType);` (build time), furnitureShadowBudget.ts:78 `let _budget: FurnitureShadowBudget = 'full';`, and initScene.ts:2653-2655 `setTierFurnitureBudgetHook(...)` which the founder's log shows firing at `reason=tier:post-load` — after the furniture was built. Explains FurniturePart 1421/1487 and KitchenCabinetPart 647/647 casters.",
    "ProjectLoader.ts:89-93 (§FIX-EMPTY-LOAD-HANG / L-108) — \"used to `await import(...)` the SAME package during every load — pointless indirection that added cold-import await points to the hydrate critical path (each yields the event loop ...)\". The precedent for fix step 1, applied in this repo for exactly this reason."
  ],
  "fix": "READ THE CAVEAT FIRST: this removes the measured 16,011 ms WINDOW (a total viewport freeze), not 16 s from the 37 s open. The window runs CONCURRENTLY with the 17.16 s hydrate, so time-to-interactive will move far less than 16 s. Claiming 16 s off the open would be the confident-but-wrong answer this codebase keeps logging.\n\nRUN THIS PROBE FIRST (one line, zero behaviour change, falsifies the whole diagnosis in one open): at RenderPipelineManager.ts:3763 wrap the import — `const __t=performance.now(); const { RenderPipeline } = await import('three/webgpu') as any; console.log('[RPM] webgpu-import', (performance.now()-__t).toFixed(1));`. ~16,000 ms confirms the mechanism; ~0 ms means the 16 s is in `_safeDisposeRenderPipeline()` (:4781) or `_emitState()`'s bus fan-out (:5451) and the fix changes.\n\nFIX 1 — KILL THE AWAIT (primary, lowest risk). `_loadTSL()` (:3731-3742) is already awaited by `bind()` before any pipeline build. Resolve `RenderPipeline` there once into a private field and replace the three `await import('three/webgpu')` sites (:3763, :3849, :5233) with that field. `_buildPipeline()` then has NO suspension point: the debounce timer's callback opens `_beginShadowRebuildGuard()`, builds, disposes and closes the guard inside ONE task. The submit-pause drops from 16,011 ms to sub-millisecond and the viewport paints through the entire load — which is what the chunked loader (ProjectLoader.ts:785-796) was built to deliver. This is the identical pattern ProjectLoader.ts:89-103 already applied under §FIX-EMPTY-LOAD-HANG (L-108).\n\nFIX 2 — CAP THE GUARD (belt and braces). Give `scheduleShadowRebuild`'s pause the bound its sibling already has: `MAX_CASTER_RELEASE_PAUSED_FRAMES = 8` (:187), released at the frame boundary with the same warn line as :1816-1824. Rationale is already written in this file at :1779-1786. This makes the freeze structurally impossible to recur when the next await lands in the chain.\n\nFIX 3 — MOVE THE TRIGGER (structural; matches the method's own NOTE). Delete `this.scheduleShadowRebuild()` from `onProjectSwitch()` (:3230) and let the existing `onProjectLoaded()` debounce (:3279-3300) own the rebuild, exactly as :3252-3256 says it should. L-316's shadow-texture safety is already covered independently by `_freezeShadowForLoad()` (initScene.ts:3580-3585), which runs one line before the `onProjectSwitch()` call at :3607-3608.\n\nSEPARATE, and the only change here with a real per-FRAME payoff (answer (e)): give `setFurnitureShadowBudget()` a retro-apply arm so the tier's `decorativeFurnitureShadows:false` reaches meshes that were already built — today it flips a module variable read only at FurnitureFragmentBuilder.ts:280, and the tier fires at `tier:post-load`. Route it through the existing `pascalSceneLighting.onGeometryAdded` sweep. Do NOT quote a caster reduction until measured: the budget only demotes DECORATIVE types (furnitureShadowBudget.ts:45-73), so it reaches a subset of the 1,421 FurniturePart casters and none of the 647 KitchenCabinetPart ones.",
  "expectedSavingMs": 16011,
  "savingBasis": "derived-from-log",
  "risk": "low",
  "confidence": "medium",
  "rivalRuledOut": "RULED OUT — (A) \"the 16 s is the shadow-map depth pass over 3,274 casters\": impossible inside the window. `_beginShadowRebuildGuard()` (:1739-1742) sets `shadowMap.autoUpdate=false` AND per-light `shadow.autoUpdate=false` (:2267, :2385-2387) before the work starts, and `render()` returns at :1257, so not one frame is submitted. Zero shadow renders occur in those 16 s.\nRULED OUT — (B) \"it is a shader recompile of 3,762 meshes' node materials\": `new RenderPipeline(renderer)` compiles nothing; three compiles node materials on the first `rp.render()`, which cannot happen while submits are paused. That cost is real but lands AFTER the window, inside the 15.6 s post-load settle.\nRULED OUT — (C) \"RenderPipeline.dispose() walks every render object\" (the claim written at RenderPipelineManager.ts:4788-4790): read the installed three@0.183.2 — `src/renderers/common/RenderPipeline.js:151-153` is `dispose() { this._quadMesh.material.dispose(); }`. One material.\nRULED OUT — (D) \"the dynamic import cold-fetches a lazy chunk\": `three/webgpu` is inside `vendor-three` (vite.config.ts:569), already fetched and evaluated by WebGPURendererAdapter.ts:139 to construct the live renderer (run reports `backend=real-WebGPU`).\nRULED OUT — (E) \"SSGI/phase-3 path made it expensive\": `ssgi: false` at every tier (SceneQualityTierManager.ts:199/209/220/232), so `_buildPhase3Pipeline` and `createSSGIPass` are never reached.\nRULED OUT — (F) \"the 16 s and the +15,628 ms first-interactive-frame are the same interval\": the mark is a frame-scheduler `scheduleOnce` that wakes a stopped loop (PlatformShell.ts:203-205, FrameScheduler.ts:442/:920), so that gap is post-load main-thread blockage (backend live-swap, 5x REDETECT_ROOMS, 4x adjacency rebuild, serializer snapshot). The rebuild contributes ~0 CPU.\nNOT RULED OUT — the precise browser-scheduler reason the module-resolution task went unserviced for 16 s. Task-queue starvation under the chunked loader is the hypothesis and the arithmetic fits (17,163.5 - 16,011.5 = 1,152 ms; trigger fires pre-hydrate + 100 ms debounce), but no code in this repo can prove Chrome's scheduling. That is exactly why the one-line probe at :3763 must run BEFORE the fix is claimed as the cure. If the probe reads ~0 ms, the residual candidates inside the window are `_safeDisposeRenderPipeline()` and the synchronous `_emitState()` bus fan-out — and FIX 2 (cap the guard) still holds, while FIX 1 does not.",
  "whatItWouldBreak": "The submit pause is not decoration: it exists to stop \"Destroyed texture [ShadowDepthTexture] used in a submit\" -> WebGPU device loss (§FIX-WEBGPU-DEVICE-LOSS-CESIUM-CASCADE / L-231, ADR-0111, C04 §SHADOW rule 7). FIX 1 SHORTENS the window without removing the guard — the dispose + `createScenePass()` recomposition still happens with submits paused and the shadow map frozen, just inside one task — so the invariant is preserved and this is the safe half.\n\nFIX 2 is the one that trades safety for liveness: if a rebuild ever genuinely outran the cap, submits would resume mid-rebuild, which is precisely the device-loss shape. Keep the cap generous (the sibling uses 8 frames, :187) and emit the same warn as :1816-1824 so a breach is a named line, not a silent regression.\n\nFIX 3 removes a rebuild from the project-switch teardown. L-316's stated purpose was that the OUTGOING project's shadow texture be disposed with no submit referencing it — verify `_freezeShadowForLoad()` (initScene.ts:3580-3585) alone is sufficient before shipping it, since that is the load-bearing claim.\n\nSecond-order effect that must be measured, not assumed: while the pause is up, `render()` skips at :1257, so the loader's per-yield frames are CHEAP. Restoring real painting makes each `yieldForProgress('project-load-chunk','post-render')` (ProjectLoader.ts:872) wait for a real 3,700-mesh frame — hydrate wall-clock could get WORSE even as the freeze disappears. The L-3054 instrumentation already in the loader (`_yieldWaitMs` / `_computeMs` / `_maxYieldWaitMs`, ProjectLoader.ts:855-878) is the counter to read before and after.\n\nTests to keep green: packages/renderer-three/__tests__/RenderPipelineManager.shadowDebounce.test.ts, .recoveryLoopUnbounded.test.ts, .uncapturedGpuError.test.ts; and for the (e) change, packages/core-app-model/src/rendering/shadowCasterPolicy.test.ts + packages/geometry-furniture/src/furnitureShadowBudget.test.ts.",
  "filesToChange": [
    "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\renderer-three\\src\\pipeline\\RenderPipelineManager.ts",
    "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\apps\\editor\\src\\engine\\initScene.ts",
    "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\geometry-furniture\\src\\furnitureShadowBudget.ts",
    "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\geometry-furniture\\src\\FurnitureFragmentBuilder.ts"
  ]
}