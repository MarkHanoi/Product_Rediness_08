{
  "survivingTitles": [
    "Furniture is the largest mesh family in the repo by an order of magnitude, and it is the ONE family whose instancing is fully built, fully wired, and switched OFF by default",
    "MEASURED on this tree: a wall with openings is 5.4 meshes and 8.4 material constructions; the same wall without openings is 1.03 meshes and 0.41 materials. Openings alone disqualify 100% of a realistic building from the instanced arm.",
    "The shared sibling-mesh merge helper exists, is owned by the THREE owner, carries the per-family measurement in its own header — and has exactly ONE production caller",
    "CORRECTION to the standing memory note: 'instancing defeated by per-element UNIQUE MATERIALS' is now only half true — the material problem is solved at the chokepoint, and what actually blocks these families is the GATE UPSTREAM of it",
    "'162 elements' in the founder's headroom line is a MEASUREMENT ARTIFACT, not his project's element count — the census requires userData.id AND userData.elementType on the SAME MESH",
    "(b) The '(unattributed)' bucket: the two named examples are 2 declared app-chrome meshes and cannot be the bulk; element sub-meshes are all attributed; the plan projector is ruled out; the third example is UNKNOWN and the exact count is one line above what he pasted"
  ],
  "refutedTitles": [
    "The heavy-scene guard's mesh arm has INVERTED against its own calibration — at 7.5 meshes/element it fires ~3x earlier than the element arm it was written to backstop, so the renderer live-swap now runs on every real project open. His log proves it fired on this one.",
    "(c) HONEST ANSWER: mesh count is overwhelmingly a FRAME cost. Its only established OPEN-path costs are the backend swap and one load-end per-mesh bounding-sphere sweep. The load traversals that WOULD have made it an open cost are already gated and closed — I checked, and I am reporting that rather than re-reporting them."
  ],
  "refutations": [
    "REFUTED — F1 'mesh arm has INVERTED against its own calibration'. THREE defects, and the third kills the fix direction. (a) THE 1.27 RATIO IS A MISREAD. The '~6-storey / ~1,300-element / ~1,645-mesh' sentence is inside the '⛔ Deliberately DECOUPLED from LevelScoped3DCullingService.isHeavyModel' note (heavySceneSwapThreshold.ts:26-31) and describes a scene that TDRs WebGPU yet never trips the MASSING-LOD gate. It is not the calibration of the 1000-mesh arm. (b) FIRING BEFORE THE ELEMENT ARM IS THE STATED DESIGN, NOT AN INVERSION. The same file, lines 11-15: '≥ SWAP_MESH_THRESHOLD scene meshes — building geometry explodes to > 1000 sub-meshes (openings, finishes, frames) WELL BEFORE it reaches 400 counted top-level roots, so this arm catches heavy scenes whose geometry arrives with sparse top-level userData ids'. The arm was written to fire first; the file also already records the founder crossing it with ONE DOOR (978→1005) and already rules 'the fix is fewer meshes per element, NOT a higher threshold'. There is no delta between the doc and the behaviour to report. (c) THE DECISIVE ERROR — THE AUDITOR USED THE WRONG ELEMENT DENOMINATOR, the exact three-denominator hazard its own finding 6 documents. The guard does NOT use the census's 162 nor the serializer's 503; autoWebGLHeavyScene.ts:154-165 countElements() counts DIRECT SCENE CHILDREN carrying userData.id or userData.levelId. Every element family adds its root straight to the scene root with a locked userData.id — WallFragmentBuilder.ts:1109 (id + elementType 'wall'), FurnitureFragmentBuilder.ts:~118 (95 furniture roots, and an INSTANCED item keeps its root, merely invisible), SlabFragmentBuilder.ts:786, DoorBuilder.ts:694, LightingFragmentBuilder.ts:753. On a 503-element / 7-level project that counter is very plausibly ≥400, i.e. THE ELEMENT ARM TRIPS TOO. So 'the arm designed as a backstop is now the only arm that ever fires' is unproven and probably false here, and the corollary the fix rests on — that re-deriving the mesh arm, or shrinking meshes/element, would stop the swap on this project — is false: the swap fires on the element arm regardless. (d) PROOF OF FIRING IS WEAKER THAN CLAIMED: logSceneCensusOnce does have exactly one production call site (initScene.ts:5182, inside the swap's 1 s timer — confirmed), but that timer is in the SHARED swap body, which the corner RendererBackendToggle also drives (initScene.ts:5541 registers window.pryzmSwapRendererBackend; RendererBackendToggle.ts:145 calls it). The line proves A LIVE SWAP RAN, not that the auto guard fired. The line that would prove it — '[autoWebGLHeavyScene] §AUTO-WEBGL-HEAVY — device-loss-risk scene …' (autoWebGLHeavyScene.ts:348) — is not in the paste. WHAT SURVIVES AND SHOULD BE CARRIED FORWARD: a full renderer teardown/rebuild did run on this open, and it runs LATE — the swap check lives inside runTierPbrPass, which is deferred for the whole load by shouldDeferPerAddGeometryPass and re-fired once as _runConsolidatedTierPbrPass = () => runTierPbrPass('post-load') (initScene.ts:3038), so the swap lands with all ~3762 meshes present and re-mints draw state for all of them. 'Instrument the swap body and print the elapsed ms' is the right next action; 'the mesh arm has inverted, re-derive it' is not.",
    "REFUTED — F8 'its ONLY established OPEN-path costs are the backend swap and one load-end bounding-sphere sweep'. The two closed gates check out exactly as described (perAddGeometryGate.ts:44-45 + ProjectLoader.ts:710/3228; initScene.ts:4120-4123 with the one-shot at 4159-4172), and CreateWallCommand's N(N-1)/2 clone really is negligible at 56 walls. But the exclusivity claim is false, and it is false in the same function the auditor already had open. (1) THE CONSOLIDATED POST-LOAD PASS IS ITSELF O(meshes) TWICE OVER: runTierPbrPass runs collectNewPbrMeshes (a full scene.traverse, initScene.ts:2883-2893) and countMeshes (a second full traverse) over ~3762 meshes at load end — the same class of cost as the bounding-sphere sweep it does count. (2) AND IT IS O(unique materials) IN GPU WORK, WHICH IS THE EXPENSIVE HALF: initScene.ts:2954 hands every new mesh to renderingCoordinator.onSceneGeometryAdded → PBRSceneUpgrader.upgradeNewMeshes (PBRSceneUpgrader.ts:298-325) → _tuneMaterial, which sets mat.envMap for every FIRST-SEEN material and therefore hits `if (pipelineDirty) { mat.needsUpdate = true; stats.recompiledMaterials++; }` (PBRSceneUpgrader.ts:157-160). On the fragment path every wall part and every furniture part carries its own material (the audit's own measured 8.40 materials/wall), so this queues thousands of program/pipeline recompiles at load end. The sibling apply() path carries the comment '§PRYZM-PERF (INSTR1) — the 38.7 s pass' (PBRSceneUpgrader.ts:183) — this repo has already measured this family of pass in TENS OF SECONDS. (3) EDGEPROJECTOR IS MESH-DRIVEN, and 'cold cache' explains the 0% hit rate, not the 109 ms. Its work is per-geometry EdgesGeometry extraction over the scene (EdgeProjectorService.ts:2795 'group.traverse(), N×EdgesGeometry, N×matrixWorld, mergeGeometries'), and the founder's own DIAG-EPS-01 prints faceCount=1068 allocMs=4.92ms per group — i.e. cost scaling with the very census this dimension is about. Ruling it out of the dimension is wrong. NET: the recommendation 'do not spend the open-time budget on mesh count' is the single most dangerous line in this audit, because at least two mesh/material-count-proportional open-path costs sit in the load-end pass it declared closed.",
    "PARTIAL — F2 survives on its load-bearing half but TWO supporting claims are wrong. (i) 'There is no sibling-mesh merge anywhere in the package' is FALSE: packages/geometry-furniture/src/builders/mergedPartKit.ts (§DESK108, 2026-08-26) is a merge kit whose header states the same PERF105 problem, and it is used by AudioBuilders, BookshelfBuilder, DiningSetBuilders, MediaWallBuilder, SectionalSofaBuilder, WhiteSofaBuilder, WorkingDeskBuilders and FurnitureFactory (19 references). Those builders already emit ONE mesh per material group. The '8-20 meshes per item' figure quoted from consolidateSiblingMeshes' header PREDATES that kit, so 760-1900 meshes for 95 items is a stale ceiling, not a measurement of this scene. (ii) 'the ONE family whose instancing is fully built, fully wired and default OFF' is FALSE: ElementInstanceBridge.ts:332-338 ships _FAMILY_DEFAULTS with column:false and beam:false against the same completed bridge (window/handrail/stairRailing are true). VERIFIED AND UNCHANGED: 590 vs 116 mesh-construction sites (5x, not an order of magnitude); FurnitureFragmentBuilder.ts:64 default `() => false`; initBuilders.ts:820-825 really does wire the bridge; isFurnitureInstancingEnabled requires `=== true`. AND THE INSTANCING LEVER WOULD GENUINELY WORK: InstancedElementRenderer._hashGeometry (:806-819) keys on index/vertex counts + first vertex + material uuid, NOT on geometry.uuid, so two identical chairs on one level really do collapse into one group — and FurnitureInstanceBridge._bakeParts already buckets by material before registering. Caveat for the plan: the key includes levelId, so identical items on 7 levels make 7 groups; and an instanced item KEEPS its scene root (invisible), so instancing furniture buys mesh-arm headroom but does NOT reduce the guard's element counter.",
    "PARTIAL — F3's measurements are real (I re-read %TEMP%/startup27-project-open-scale.txt: every quoted row matches, and the mtime post-dates the test file), but three inferences are overstated. (i) '100% of a REALISTIC building' is a property of the harness, not of a building: buildLevel() assigns an opening to EVERY segment by construction (perimeter→window, interior→door) and the test's own comment says 'whether that matches the founder's file is exactly the question the control row exists to bracket'. The founder's log gives no door/window count. (ii) MAGNITUDE ON HIS PROJECT IS SMALL: 56 walls × 5.4 ≈ 300 of 3762 meshes, ~8%. Walls are not what makes this scene 3762. (iii) THE 76 ms IS NOT A SAVING. It is the measured realistic-minus-none BUILD delta, and most of it is the opening geometry itself (hole-extrude / CSG / jamb-sill-lintel), which no proposed fix removes. Also, fix lever (1) is largely SPENT: the private WallPart merge IS reached on the opening path — WallFragmentBuilder.ts:3150-3160 first tries _rebuildPlainWallBodyAsHoleExtrude (one single-volume body) and falls back to _mergeWallBodySegments when a mitre end exists, so the body is already one mesh and the remaining 5.4 are jambs/frames/edge overlays. The 'do not remove !_hasOpenings' would-break reasoning is correct and should be preserved verbatim.",
    "PARTIAL — F4's title is literally TRUE (I re-ran the grep: consolidateSiblingMeshes has exactly one production call site, DoorBuilder.ts:647), but its supporting census is wrong and it changes the fix. 'Every other multi-part family has no merge at all: furniture (590), plumbing (54), slab (26), stair (16), curtain-wall (14)' — furniture HAS mergedPartKit (above), stair HAS its own private merge (StairMeshBuilder.ts:684-689, treads+risers+stringers into one combined geometry), and roof HAS one (RoofGeometryBuilder). mergeGeometries appears in furniture, lighting, roof, stair and four wall files. So the true statement is not 'nobody merges' but 'FIVE families merge and FOUR of them grew a private copy' — which is exactly the failure mode consolidateSiblingMeshes' header was written to prevent, and it makes the work UNIFICATION (with per-family regression risk) rather than greenfield adoption. Plumbing / slab / curtain-wall do appear genuinely unmerged.",
    "PARTIAL — F5's code claims are all verified (SharedMaterialCache.ts:110-124 dedup, default ON via `!== false` at :100-107; InstancedElementRenderer.ts:244 dedup BEFORE :251 _hashGeometry; ChairBuilder mints a fresh MeshStandardMaterial per call). The planning half — 'do NOT budget a separate material-sharing project' — is the part to not over-apply, and it is stated too broadly. dedupInstanceMaterial has exactly ONE non-test caller: InstancedElementRenderer.register. Nothing dedups on the FRAGMENT path, which is where 100% of opening-bearing walls and all furniture live today, and it is the fragment path's material count that (a) drives PBRSceneUpgrader's per-material needsUpdate recompile at load end and (b) drives the TSL node-compile tail on webgl-fallback that autoWebGLHeavyScene.ts:170-178 names as the founder's 'Compiling GPU shaders'. The correct framing is 'material sharing is not what blocks INSTANCING' — not 'material count does not matter'."
  ],
  "missed": [
    "THE POST-LOAD PBR PASS IS THE BIGGEST UNMEASURED MESH/MATERIAL-DRIVEN OPEN COST, and it sits in the same function the audit read for finding 8. initScene.ts:2954 `if (newMeshes.length > 0) renderingCoordinator.onSceneGeometryAdded(newMeshes)` runs once at load end over every mesh the load added; PBRSceneUpgrader.upgradeNewMeshes (:298-325) walks each mesh's material and _tuneMaterial (:108-161) sets `mat.envMap` on every first-seen material, which sets `pipelineDirty` and then `mat.needsUpdate = true` — one shader/pipeline recompile per UNIQUE material. With ~8.4 materials per opening-bearing wall and one material set per furniture item, that is thousands of recompiles queued at open. The same class of pass carries the comment '§PRYZM-PERF (INSTR1) — the 38.7 s pass' at PBRSceneUpgrader.ts:183. It already has a counter (PERF_KEYS.PHASE_PBR_UPGRADE) and prints `recompiles:` in its own log line — read that number from the founder's console before anything else on this dimension.",
    "THE HEADROOM LINE COMPARES TWO DIFFERENT ELEMENT COUNTERS, which is a defect one level worse than the artifact finding 6 names. pryzmPerfConsole.ts:646-652 computes `elemHeadroom = HEAVY_SCENE_ELEMENT_ARM - c.elements`, i.e. it measures the census's own id-on-a-mesh count (162) against the guard's 400 — but the guard's arm reads autoWebGLHeavyScene.countElements(), which counts DIRECT SCENE CHILDREN with userData.id or levelId (:154-165) and on this project is plausibly 300-500. So '238 to go' is not the guard's headroom in any units, and the census can print 'plenty of element headroom' on a scene whose element arm has already tripped. Fixing finding 6's id-inheritance alone does NOT fix this — the census must either mirror countElements() or stop printing an element headroom at all.",
    "MESH COUNT IS ALSO A SECOND-ORDER OPEN COST THROUGH THE SWAP, and the arithmetic makes the direction of work unambiguous. Because the swap check is deferred for the whole load (perAddGeometryGate) and re-fired once as runTierPbrPass('post-load'), the founder's box builds and node-compiles the ENTIRE scene on webgl-fallback (TSL, per-material compile) and then throws that away and re-mints all ~3762 meshes on webgl-classic. That is why 'measure the swap' matters — but note the corollary the audit missed: since the ELEMENT arm (400 scene roots) very likely trips on a 503-element project anyway, the swap cannot be avoided by mesh reduction, so the swap's cost must be attacked as swap cost (fire it EARLIER, before the geometry exists — proactivelySwitchToWebGLForBuildingGeneration already exists as the pattern for exactly that), not as mesh-count cost.",
    "NOBODY MEASURED THE ACTUAL PER-FAMILY TABLE, which is in the founder's console ~20 lines above the line he pasted and settles this entire dimension in one read. Every claim in findings 2, 3, 4 and 7 about WHICH family owns the 3762 is inference from static grep counts (590 vs 116 mesh-construction SITES is a source-code census, not a runtime one — a builder with 590 construction sites across a type switch emits only the branch its type takes). The census already prints elems / meshes / m per elem / inst.grp / instances / tris / shadow / lines per family, sorted by draw cost, with a PARTIAL/NO instanced verdict per row. Ask for that block before commissioning any family-level work.",
    "THE SHADOW ARM IS A SECOND SUBMISSION OF THE SAME MESH COUNT AND IS NOT IN THIS AUDIT AT ALL. pryzmPerfConsole.ts:627-637 prints §NAV-SHADOW-CENSUS — casters, the caster percentage, and explicitly 'the true submission count is forward + casters'. At 3762 meshes with furniture and lighting keeping their builder shadow flags (furnitureShadowBudget), the caster count is the multiplier on every mesh-count fix in findings 2-4 and is free to read from the same paste."
  ]
}