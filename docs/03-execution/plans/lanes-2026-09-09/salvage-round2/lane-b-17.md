{
  "refuted": false,
  "reason": "NOT REFUTED. Every code-level assertion checks out, and I could not build a rival that survives the code.\n\nWHAT I CONFIRMED\n1. `classifyTileGroundPicks` (globeGroundAnchor.ts:198-216) is exactly two scalar thresholds over a sorted `number[]`, and `slope` is the fall-through: `span<=6 -> plateau; maxGap>4 -> roof-gap; else slope`. The `maxGap` scan runs only over indices lowIdx+1..midIdx (the low half), so the roof step must land there to be seen.\n2. `reduceTileGroundHeight:279` seats the slope arm at the ring MEDIAN; every other arm seats at p10. So slope-vs-not-slope is the ONLY decision that changes the number, and 78.94-0.30 = 78.64 with `GLOBE_GROUND_SEAT_EPSILON_M = 0.3` (CesiumViewport.ts:619). With n=58, midIdx = floor(0.5*58) = 29 and lowIdx = 5 — the diag's `at(0.5)` and the classifier's median are the same index, so the quoted console numbers are internally consistent with the code that prints them.\n3. THE REGRESSION ATTRIBUTION IS EXACT. `git show c36b75bc^:globeGroundAnchor.ts` lines 218-222 is `const idx = Math.floor(GROUND_LOW_QUANTILE * sorted.length); return sorted[min(idx, n-1)] - seatEpsilonM;` — unconditional p10, no classifier. c36b75bc (2026-09-05, +113 lines, 0 deleted) is the whole delta.\n4. THE CONSOLE QUOTE IS REAL, NOT PARAPHRASED. `seat=78.64 m ellipsoidal` and `anchor-in-void=n (anchor->parcel-centroid 28.6 m)` are the SAME log line, CesiumViewport.ts:13134-13146. That corroborates that the claimant had an actual session console, which is the premise everything else rests on.\n5. ONE DATUM DRIVES ALL THREE VISUALS. Void cap top = `formaTerrainBaseHeight - 0.05` (:13037); model seat = `resolveGlobeRealModelBaseHeight(input.baseHeight, formaTerrainBaseHeight)` (:17422) with NO offset; massing re-placed at the same base. A shared float is therefore one bug, not three.\n6. Ring/sampler facts all hold: `safeSampleTileHeights` returns `Promise<number[]>` and only `out.push(h)` for finite values, so both position and index correspondence die at :9014-9036; in-parcel picks are skipped whenever `photorealTilesActive` (:9203-9210); the ring is 1.7x vertices + 16@ext+25 + 16@ext+55 (:9241-9246). 58 picks = 26 surviving ring vertices + 32 compass points, consistent.\n7. The defending fixture (GlobeGroundAnchor.test.ts:473) is 20 values in [50.0, 50.6] plus 28 in [65.0, 74.0] — a 14.4 m clean gap. A photoreal mesh drapes kerb-to-cornice and cannot produce that; measured largest step 2.00 m.\n8. SECOND ANOMALY CONFIRMED, and more strongly than claimed. `parcelFrameOrigin` = ring[0] (boundaryProjection.ts:142), pinned by `siteFrameOriginConsistency.test.ts:102` and by `scopeAnchor.spec.ts:43` (\"IT IS NOT THE FIRST VERTEX — which is exactly what `parcelFrameOrigin` returns\"), used at SiteBoundaryDrawTool.ts:284 and SiteBoundaryMap2D.ts:4054. The probe tests that vertex against the same ring (`committedParcelLonLat`, :12933 -> :13120). At a ring VERTEX the half-open straddle rule makes both incident edges compute an intersection x exactly equal to px, so neither toggles — 'n' is the deterministic degenerate answer, not merely ulp-indeterminate.\n\nRIVALS I BUILT AND KILLED\n- Bare-earth terrain overwrites the seat. This was my best rival: `ensureGroundBaseForContextInner:11783` writes `formaTerrainBaseHeight = h` from `sampleTerrainMostDetailed`, and Barcelona geoid (~+49.5 m) + Eixample terrain (~29 m) also lands near 79 m, so the NUMBER alone cannot separate the two. DEAD: `decideBakedTerrainAttach` (terrainCoverage.ts:1172) returns `{attach:false, reason:'photoreal'}` whenever `photorealActive && !formaMode`, so no elevation provider is attached, `terrainProviderHasElevationData` is false, and the function returns at :11767 without writing. Independently, the §GLOBE-SLOPE-SEAT line only prints on the photoreal branch.\n- Stale `photorealTilesActive` latch routes a Forma view down the globe clamp. DEAD: dispatch at :8276 is on `input.keepPhotoreal`, not the latch.\n- Per-model offset (GLB origin / massing / cap disagree). DEAD: verified no offsets anywhere between the anchor and the three consumers.\n- Self-hit ratchet on PRYZM's own geometry. The exclusion list at :9315-9319 is genuinely incomplete — only `realModelOnGlobe` + `contextBuildingEntities`; the void cap, massing and the new Cesium envelope are unexcluded. But on the photoreal path our context extrusions are cleared outright (:8301-8313), and the cap uses the unbuffered parcel ring while every pick is outside it. Latent, not causal here.\n\nWEAKNESSES YOU SHOULD CARRY FORWARD\n(a) The 58/12.59/2.00/78.94/64.89 read exists nowhere in the repo (no ISSUE-LOG row; latest is L-13266). Everything downstream is exact arithmetic on an UNVERIFIED premise. If the arm was actually plateau or roof-gap, the seat was p10 and the diagnosis is wrong.\n(b) The claim quotes min and median but NOT p75 or max, which the SAME §GLOBE-PICK-SPREAD-DIAG line prints and which is the most discriminating number available (a flat block with roofs is strongly right-skewed; a ramp is symmetric). That is a selection gap in evidence the author already had.\n(c) FIX ARM (2) IS NOT IMPLEMENTABLE AS WRITTEN. It asserts \"on the globe path `maybeAttachTerrainProvider` runs and `contextGroundCache` already holds `sampleTerrainMostDetailed` heights\". On the true photoreal path it does not — the attach is explicitly refused (see above). There is no bare-earth cross-check there without first attaching terrain under the Google tiles, which is the §A.21.D-GLOBE3 / L-142 hazard the codebase deliberately avoids.\n(d) \"safeSampleTileHeights destroys position\" is a touch overstated as THE architectural root: the `extract` callback already receives the full `Cesium.Cartesian3`; only the helper's `Promise<number[]>` return type discards it. Prerequisite (0) is a one-line generalisation of a crash guard, not a boundary change. The roof-contaminated ring is the root; the 1-D signature is what makes it unfixable.\n(e) Corollary the claim under-states: `span = median - p10 > 6 m` is trivially true of ANY wall-to-wall block, so `plateau` cannot protect a dense city at all and `roof-gap` fires only on a mesh hole. c36b75bc therefore broke every dense city, not Barcelona. The founder has been on 3D Site/Globe daily since 09-05 and filed ~20 other defects without this one — mild counter-evidence worth one line in the writeup, not enough to refute.",
  "evidenceChecked": [
    "apps/editor/src/ui/geospatial/globeGroundAnchor.ts:150-230 — GROUND_LOW_QUANTILE=0.1, OUTLIER_ROBUST_MIN_SAMPLES=12, SLOPE_RAMP_MIN_SPAN_M=6, ROOF_GAP_M=4, classifyTileGroundPicks body: lowIdx=floor(0.1n), midIdx=floor(0.5n), maxGap scanned ONLY over lowIdx+1..midIdx, 'slope' is the fall-through",
    "apps/editor/src/ui/geospatial/globeGroundAnchor.ts:270-285 — reduceTileGroundHeight: slope arm returns medianM - seatEpsilonM, every other arm returns sorted[floor(0.1n)] - seatEpsilonM",
    "apps/editor/src/ui/geospatial/globeGroundAnchor.ts:309-352 — resolveGlobeGroundAnchor: rejectEllipsoidPicks=true, delegates to reduceTileGroundHeight, no other height contribution",
    "git show c36b75bc^:apps/editor/src/ui/geospatial/globeGroundAnchor.ts:216-222 — PRE-REGRESSION reduction was unconditional p10 - epsilon; c36b75bc is +113/-0 lines",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:610-619 — GLOBE_GROUND_SEAT_EPSILON_M = 0.3",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:642-662 — PHOTOREAL_VOID_CAP_DEPTH_M=60, PHOTOREAL_VOID_CAP_SEAT_EPSILON_M=0.05",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:9014-9036 — safeSampleTileHeights returns Promise<number[]>, out.push(h) only for finite h; (lon,lat) and index correspondence both discarded",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:9150-9250 — samplePts construction: parcelVoidCut = photorealTilesActive skips centroid + boundary; 1.7x pushed vertices; two 16-point rings at ext+25 and ext+55",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:9313-9319 — objectsToExclude = realModelOnGlobe + contextBuildingEntities ONLY (void cap, massing, envelope NOT excluded)",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:9365-9425 — §GLOBE-PICK-SPREAD-DIAG (at(0.5) == classifier midIdx) and §GLOBE-SLOPE-SEAT arm log; resolveGlobeGroundAnchor call with seatEpsilonM",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:8276-8286 — clamp dispatch is on input.keepPhotoreal, NOT on the photorealTilesActive latch",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:8301-8313 — on keepPhotoreal && photorealTilesActive our context extrusions and the earth slab are CLEARED",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:9643-9700 — commitPhotorealBase: formaTerrainBaseHeight = sampledHeight, no adjustment",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:11759-11800 — ensureGroundBaseForContextInner, the rival writer of formaTerrainBaseHeight (terrain-sample)",
    "apps/editor/src/ui/geospatial/terrainCoverage.ts:1164-1187 — decideBakedTerrainAttach returns {attach:false, reason:'photoreal'} when photorealActive && !formaMode, killing the terrain-overwrite rival",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:12930-12980 — void clip built from committedParcelLonLat, UNBUFFERED; applyPhotorealVoidCap + logPhotorealVoidVsBuilding called with that same ring",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:13017-13060 — cap top = formaTerrainBaseHeight - 0.05",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:13113-13147 — the probe: pointInRingEvenOdd on realModelOnGlobeOrigin vs parcel, and the 'seat=NN.NN m ellipsoidal' log the console quote came from",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:17420-17475, 17553-17580 — resolveGlobeRealModelBaseHeight and reseatRealModelOnGlobe: model seat == formaTerrainBaseHeight, zero offset",
    "apps/editor/src/ui/site/boundaryProjection.ts:142-145 — parcelFrameOrigin returns ring[0]",
    "apps/editor/src/ui/geospatial/SiteBoundaryDrawTool.ts:284 and SiteBoundaryMap2D.ts:4054 — the site origin IS parcelFrameOrigin(ring)",
    "apps/editor/__tests__/siteFrameOriginConsistency.test.ts:102 and apps/editor/src/ui/geospatial/__tests__/scopeAnchor.spec.ts:43 — tests pin that the datum is the ring's FIRST VERTEX",
    "packages/geometry-kernel/src/pure/pointInPolygon.ts:110-132 — half-open straddle rule; on-ray/on-vertex is the acknowledged degenerate case",
    "apps/editor/__tests__/GlobeGroundAnchor.test.ts:455-482 — Sete ramp fixture, plateau fixture, and the roof-gap 'dense' fixture with a synthetic 14.4 m gap",
    "docs/04-reference/ISSUE-LOG.md:53495-53535 — no L-row exists for this defect; the founder console cited is not in the repo",
    "git log --since=2026-09-04 apps/editor/src/ui/geospatial/CesiumViewport.ts — no post-c36b75bc commit touches the seat ladder; f7fd5efd (L-13144) only flips show flags"
  ],
  "betterHypothesis": "Not a replacement — a re-ranking of the same causal chain, plus one gap the claim missed.\n\nRE-RANK: the ROOT is that `clampToHeightMostDetailed` returns the TOP-MOST surface at each ring point while the ring is DESIGNED to sit entirely on the neighbours (in-parcel picks skipped since L-479, :9203-9210), so in a wall-to-wall block the pick population is roof-majority by construction. The `slope` arm is the TRIGGER, not the root: p10 happened to survive the contamination and the median does not. This matters for the fix order — arm (1), the semantic building-footprint mask, is the actual repair; arms (3) and (4) are estimators over the same contaminated population and will need re-tuning if shipped alone. Ship (4) plus (1); (3) is optional once (1) lands.\n\nMISSED GAP (real, code-verified, worth folding into the same lane): the height pick's `objectsToExclude` at CesiumViewport.ts:9315-9319 lists only `realModelOnGlobe` and `contextBuildingEntities`. The photoreal void cap (`photorealVoidCapEntity`), the massing entities and the newly-added Cesium envelope renderer (§SPACE-ENVELOPE-IN-CESIUM, 7c93549e) are all unexcluded, while the clamp re-fires up to 12 times (GLOBE_GROUND_CLAMP_MAX_RETRIES) plus on every tileset load event. Today it is latent — those objects are parcel-shaped and every pick is outside the parcel — but the file's own comment at :9313 records that a self-hit once \"stacked it ~one storey per view switch\", and an authored envelope is not guaranteed to stay inside the ring. Prerequisite (0) touches this exact call site; close it there.\n\nCHEAPEST DISCRIMINATOR, cheaper than the author's own: he already has the answer on the console he quoted. The §GLOBE-PICK-SPREAD-DIAG line prints min / p25 / median / p75 / max on one line. A genuine ramp is near-symmetric about the median (median - p25 ~= p75 - median); a flat street with roofs is strongly right-skewed with max - median ~= one building height (20-25 m in Eixample) and median - min ~= a few metres. He quoted min and median and omitted p75 and max. Read those two numbers off the same log line before touching any code — no re-instrumentation, no contour map, no new build."
}