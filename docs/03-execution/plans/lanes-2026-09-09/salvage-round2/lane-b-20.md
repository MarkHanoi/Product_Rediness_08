{
  "refuted": true,
  "reason": "The DIAGNOSIS is largely confirmed; the PROPOSED FIX is refuted, and would ship a regression.\n\nCONFIRMED (do not re-investigate): (b) baseOffset/height are independent stored fields (SpaceEnvelope.ts:211,218); the only level→envelope coupling is at create (envelopeAuthoringPlan.ts:658-659), whose own doc comment at :214 already predicts this staleness (\"it would disagree with the level records the moment a floor-to-floor is edited\"); `grep -rn SpaceEnvelope packages/command-registry/src` returns ZERO. (a) SetLevelHeightCommand is reachable only via legacy commandManager (LevelManagerPanel.ts:196, PropertyPanel.ts:1277); commands.ts carries only 'level.add' and 'level.createMultiple' — no setHeight; level.update bridges to UpdateLevelCommand via _cmExec (initBusHandlers.ts:2353-2358). (c) No cap gate exists anywhere: spaceEnvelopeFaces() returns n+2, spaceEnvelopeFaceHandles iterates all of them, the 3D pick returns caps (SpaceEnvelopeFacePick.ts:205-217), and onPointerDown's only role refusal is maximumBuildable. Falsification #1 holds (no production level→envelope subscriber). Falsification #3 resolves IN THE CLAIM'S FAVOUR: resolveLevelAuthority.ts:1-46 documents that runtime.levelStore (packages/stores) has ZERO production writers and window.bimManager is the one live authority — so seam A's premise is sound.\n\nWHY THE FIX IS REFUTED — the invariant the current code is protecting:\n\n1. THE TOP-FACE DRAG IS NOT AN UNPOLICED ACCIDENT; IT IS THE ONLY PRODUCTION PATH THAT MAKES WALLS FOLLOW A STOREY HEIGHT. §ENVELOPE-TOP-FACE-HEIGHT (L-13118) is a shipped, wired subsystem: apps/editor/src/engine/spaceEnvelopeWallFollowPlan.ts (~900 lines: its own refusal vocabulary 'height-authored-since-generation', 'height-not-recorded', 'height-base-also-moved', 'height-contested-by-two-envelopes', grouping into the fewest dispatches) + spaceEnvelopeWallFollow.ts, which reads SpaceEnvelopeFaceMoveCommitted.heightBefore/heightAfter and dispatches `wall.updateHeightBatch`. It is INSTALLED at initTools.ts:2112 (installSpaceEnvelopeWallFollow) and FED at initTools.ts:2233 (BIM canvas) and GISAreaLayout.ts:1424 (3D Site). The ONLY producer of SpaceEnvelopeFaceMoveCommitted in the tree is spaceEnvelopeDragSurface.ts:846 — precisely the gesture the fix refuses at onPointerDown. A cap `spaceEnvelope.moveFace` dispatched from the bus emits nothing. So the fix silently converts the whole height leg of that planner into dead code.\n\n2. THE REPLACEMENT PATH RE-HEIGHTS NO WALL. BimKernel.ts:385 dispatches 'spatial-authority-reconcile' ONLY when updates.elevation changed — a pure {height} write fires nothing (SetLevelHeightCommand's own step-1 comment says so: \"Fires no reconcile\"). initWallLevelSubscribers.ts:23 — the sole consumer of that reconcile — re-runs builder.updateWall to re-seat at the new ELEVATION; nothing anywhere reads level.height to set a wall's height. And the fix's consequence (C) uses spaceEnvelope.setParameter, which emits no committed-move event, so spaceEnvelopeWallFollow never runs on that path either. NET: ship both change sets as written and a storey-height edit moves the envelope box while every wall stays its old height, with the one gesture that used to do it right now refused. That is the \"half-moves in silence\" outcome SetLevelHeightCommand's own header calls worse than a refusal.\n\n3. THE FIX MINTS A RIVAL SENTENCE. spaceEnvelopeWallFollowPlan's bottom-drag notice reads verbatim \"Drag the TOP face\" (asserted at spaceEnvelopeWallFollow.spec.ts:1063). The fix's refusal says the top face is pinned and to use the card instead. Two shipped sentences, one gesture — the exact C84 EI-9 shape the fix invokes elsewhere.\n\n4. THE GATE IS ROLE-BLIND AND ITS SENTENCE IS FALSE FOR ROOMS. `face.kind !== 'side'` also refuses ROOM caps. spaceEnvelopeContainment.test.ts:199-207 pins a room top-face move as legal-until-it-leaves-the-level, and spaceEnvelopeWallFollowPlan.ts:161-165 reasons explicitly about \"a room whose height differs from the storey\". A room's top is not the storey top, so \"pinned to its storey — set by the level height\" is factually wrong there, and the gate would kill partial-height rooms/mezzanines.\n\n5. THE setParameter SWAP IS HALF-STATED AND containmentGate.ts NAMES THE RESULTING DEFECT. Its §25.6 comment: \"Loosening it centrally would make setParameter... stop refusing a level that strands a room while still writing only the level — leaving the room outside its storey with nothing anywhere saying so, which is strictly worse than the refusal it replaced.\" The fix says \"use containmentOutcomeFor\" but never says execute() must also WRITE outcome.adapted (the pattern is SetSpaceEnvelopeFootprintHandler.execute, MutateSpaceEnvelope.ts:322-341). A literal reading of the fix produces exactly that defect. Related: adaptRoomToMovedLevel only SHRINKS overhanging rooms — raising a storey leaves rooms at their old height, which the fix does not state.\n\nMinor, non-load-bearing: the claim says \"both live pickFace implementations return caps\" — the plan surface's pickSpaceEnvelopeSideFaceInPlan returns SIDES ONLY and documents why (SpaceEnvelopeFacePick.ts:255-275), so the cap drag exists only in 3D. The \"repo-wide grep for allowedFaces|lockVertical|faceFilter finds nothing ⇒ nothing protects this\" inference is the [[grep-silence-has-three-causes]] shape: the invariant is real but is not spelled with any of those words.",
  "evidenceChecked": [
    "packages/geometry-space-envelope/src/SpaceEnvelopeTypes.ts:23-30 — SpaceEnvelopeFaceRef closed union CONFIRMED (starts :23, not :26)",
    "packages/geometry-space-envelope/src/SpaceEnvelopeFaceDrag.ts:70-79 — cap axes ±Y, side y:0 CONFIRMED",
    "packages/geometry-space-envelope/src/SpaceEnvelopeGeometry.ts:144-151 — spaceEnvelopeFaces returns n+2 CONFIRMED",
    "packages/geometry-space-envelope/src/SpaceEnvelopeFaceGizmo.ts:146-151 (options interface, NO cap flag) and :265-280 (handles loop over all faces) CONFIRMED",
    "packages/geometry-space-envelope/src/SpaceEnvelopeFacePick.ts:200-250 — 3D pick returns caps; :255-300 pickSpaceEnvelopeSideFaceInPlan is SIDES-ONLY (claim said both pick impls return caps — wrong, narrows blast radius)",
    "packages/geometry-space-envelope/src/SpaceEnvelopeContext.ts:280-360 — adaptRoomToMovedLevel drives {kind:'top'}/{kind:'bottom'} through the planner CONFIRMED; :505 findSharedFaces already side-only",
    "apps/editor/src/engine/spaceEnvelopeDragSurface.ts:583-660 — onPointerDown: role refusal for maximumBuildable, NO face-kind gate CONFIRMED; :844-880 — onCommitted fires for cap drags (lastPlannedRing set every planned frame, heightBefore/heightAfter attached)",
    "apps/editor/src/engine/spaceEnvelopeWallFollowPlan.ts:93-200, :261-320, :349-380, :518 — §ENVELOPE-TOP-FACE-HEIGHT planner: cap drag → wall.updateHeightBatch, with 'Drag the TOP face' guidance for bottom drags (THE MISSED INVARIANT)",
    "apps/editor/src/engine/spaceEnvelopeWallFollow.ts:1-80, :146-170, :396-410 — the wire, dispatching WALL_UPDATE_HEIGHT_BATCH_COMMAND",
    "apps/editor/src/engine/initTools.ts:2112 installSpaceEnvelopeWallFollow, :2233 onFaceMoveCommitted; apps/editor/src/ui/layout/GISAreaLayout.ts:1424 onCommitted — PROVES WIRED IN PRODUCTION, not authored-but-unwired",
    "apps/editor/src/engine/__tests__/spaceEnvelopeWallFollow.spec.ts:980-1075 — COMMITTED_TOP fixture, 'Drag the TOP face' notice assertion",
    "packages/core-app-model/src/BimKernel.ts:373-392 — updateLevel dispatches spatial-authority-reconcile ONLY on elevation change; a {height} write fires nothing",
    "apps/editor/src/engine/initWallLevelSubscribers.ts:1-90 — the sole reconcile consumer re-seats walls at a new ELEVATION; no path reads level.height into wall height",
    "packages/core-app-model/src/SpatialAuthority.ts:94-95, :187-195, :427-489 — worldY = level.elevation + offsets; height is not level-derived",
    "packages/command-registry/src/levels/SetLevelHeightCommand.ts:1-160 (header/family table, no SpaceEnvelope row) and :248-340 (execute: updateLevel{height} fires no reconcile; bim-level-updated emitted at :328,:404)",
    "packages/command-registry/src/ — grep SpaceEnvelope → ZERO hits CONFIRMED",
    "apps/editor/src/engine/initBusHandlers.ts:2352-2372 level bridges (level.update → UpdateLevelCommand via _cmExec; level.add only) and :664-700 _cmExecOrRefuse CONFIRMED",
    "packages/command-bus/src/commands.ts:975,978 — only level.add and level.createMultiple; no level.setHeight CONFIRMED",
    "plugins/space-envelope/src/handlers/MutateSpaceEnvelope.ts:283-310 setFootprint canExecute (containmentOutcomeFor), :322-341 execute WRITES adapted rooms, :405-430 setParameter (containmentRefusalFor, refuses) — asymmetry CONFIRMED",
    "plugins/space-envelope/src/handlers/containmentGate.ts (whole file) — §25.6 comment explicitly names the defect a naive setParameter swap would create: 'stop refusing... while still writing only the level'",
    "plugins/space-envelope/__tests__/spaceEnvelopeContainment.test.ts:199-207 — a ROOM top-face move is a legal gesture bounded by containment (kills the blanket kind!=='side' gate)",
    "apps/editor/src/ui/site/envelopeAuthoringPlan.ts:180-215 (baseOffset absolute-datum convention + explicit prediction of this staleness) and :620-660 (baseOffset: level.elevation, height: heightM — the one create-time coupling)",
    "apps/editor/src/ui/analysis/parcelLawEnvelopeAuthoring.ts:410-425 — readLevels = window.bimManager.getLevels() CONFIRMED",
    "apps/editor/src/ui/inspect/resolveLevelAuthority.ts:1-46 — runtime.levelStore has ZERO production writers; bimManager is the one live authority (settles falsification #3 in the claim's favour)",
    "packages/stores/src/aggregate-commands/levelUpdate.ts + grep for its callers — no production callers outside packages/stores re-exports and tests",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:8888-8892 — bottom = baseHeight + baseOffset; top = bottom + height, record-only CONFIRMED",
    "packages/schemas/src/elements/SpaceEnvelope.ts:210-218 — baseOffset/height independent stored fields CONFIRMED",
    "apps/editor/src/engine/geometryMutationEvents.ts:95-115 — bim-level-updated is a shadow-freeze exclusion, not a level→envelope path; grep of engine/init*.ts for spaceEnvelope hits only initTools.ts (the drag/wall-follow install)",
    "packages/geometry-space-envelope/__tests__/spaceEnvelopeFaceGizmo.test.ts:46-49,122-128,192-200 — asserts 6 handles incl. 'top face'/'bottom face' (survives an opt-in flag, would break a default change)"
  ],
  "betterHypothesis": "The mechanism for requirement 1 is right and should ship roughly as written (bus verb level.setHeight via _cmExecOrRefuse + a bim-level-updated follower re-seating role:'level' envelopes, with setParameter switched to containmentOutcomeFor AND its execute() writing outcome.adapted, copying MutateSpaceEnvelope.ts:322-341).\n\nRequirement 2 must be re-framed. The cap drag is not an ungated accident — it is L-13118 §ENVELOPE-TOP-FACE-HEIGHT, the ONLY production path that re-heights walls to a storey, and BimKernel fires no reconcile on a {height} write so the level-height command re-heights nothing. Therefore:\n\n(i) TREAT THIS AS A MIGRATION, NOT A GATE. Before any refusal lands, the level-height path must acquire the wall consequence the top-face drag already has. The cheapest honest shape reuses what exists: have the new bim-level-updated follower, after re-seating each role:'level' envelope, run the SAME spaceEnvelopeWallFollowPlan height leg (or synthesise a SpaceEnvelopeFaceMoveCommitted with heightBefore/heightAfter and feed applySpaceEnvelopeWallFollow) so wall.updateHeightBatch still fires. Otherwise the gate converts a working feature into a silent half-move.\n\n(ii) GATE ON ROLE AND KIND, NOT KIND ALONE: `record.role === 'level' && face.kind !== 'side'`. Room caps stay draggable — a room's top is not the storey top (spaceEnvelopeContainment.test.ts:199-207; spaceEnvelopeWallFollowPlan.ts:161-165).\n\n(iii) RETIRE THE RIVAL SENTENCE IN THE SAME COMMIT. spaceEnvelopeWallFollowPlan's bottom-drag notice says \"Drag the TOP face\"; if the top face is pinned on level envelopes, that sentence must be rewritten to point at the Envelope card, or the app tells the user two contradictory things.\n\n(iv) The gizmo half (omitCapFaces) is correct and safe as an OPT-IN option with the default unchanged — pass it only for role:'level' targets, not unconditionally at the two call sites, or room cap arrows disappear too.\n\nAn alternative worth pricing before building any of this: keep the top-face drag as the gesture and make it WRITE level.height (drag → level.setHeight → cascade → envelopes+walls follow), inverting the direction. That gives one authority, one gesture, and no dead planner — and it is closer to what the code already does than the refusal is."
}