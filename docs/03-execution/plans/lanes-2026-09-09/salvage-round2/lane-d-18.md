{
  "refuted": false,
  "reason": "CONFIRMED as a mechanism; the saving figure is overstated. Every load-bearing citation checks out in the code. (a) initScene.ts:3038 mints `_runConsolidatedTierPbrPass = () => runTierPbrPass('post-load')`, published to a hoisted handle and invoked from a SECOND `pryzm-project-loaded` listener inside `setTimeout(0)`; the guard call at :3008 consumes a `meshCount` from a full `scene.traverse` immediately above. The guard's input is the finished scene by construction. Thresholds verified exactly (heavySceneSwapThreshold.ts:40,43 = 400/1000; `isSwapWorthyHeavyScene` ORs the arms). (b) VERIFIED AT EXACT LINE NUMBERS: RenderPipelineManager.ts:762 sets `_webGpuActive = true` only AFTER the `if (!isWebGPU) { … return; }` arm at :715-760, and :1957 (`scheduleShadowRebuild`), :3280 (`onProjectLoaded`), :4912 (`_rebuildPipeline`) all open with `if (!this._webGpuActive) return`. SHADOW_REBUILD_COMPLETE is logged at :2037 inside scheduleShadowRebuild. The 16.0 s is WebGPU-only work that cannot run on a classic boot. (c) The swap body (initScene.ts ~4965-5175) does everything claimed. (d) rendererRetirement.ts verified verbatim including the `detached===0 && minted>0` diagnostic gap. C04 §1.4a quoted verbatim.\n\nTHE DECISIVE CORROBORATION IS ONE THE CLAIM UNDER-SELLS: RenderPipelineManager.ts:2293-2296 documents the identical chain independently of C04 — \"1. Project OPEN on native WebGPU. setShadowReallocFrozen(true) … 2. The `tier:post-load` pass fires §AUTO-WEBGL-HEAVY (C04 §1.4) — i.e. the backend swap is fired FROM INSIDE that freeze window. Not a coincidence: both hang off the same post-load tier pass. 3. §RENDERER-LIVE-SWAP calls rpm.dispose() FIRST, which zeroes … and sets `_webGpuActive = false`.\" Two independent in-repo records of one defect.\n\nFEASIBILITY CONFIRMED: startupBudget.ts:150-158 puts `hub:warm-versions-done` → `hub:open-clicked` → `boot:engine-start` → `boot:scene-done` → `open:version-read-*`, so the count IS available before the renderer is built. `warmVersionCache()` \"reads EVERY project's entire compressed container\" (startupBudget.ts:81). `createRenderer(canvas, backendOverride?)` (createRenderer.ts:397) is non-persisting; `swapMayPersistPreference` keeps 'webgl-classic' out of storage; `isUnintendedWebglOnlySwap` (createRenderer.ts:321) is exactly the predicate step 4 wants. THE HARDEST SEAM IS ALREADY HALF-BUILT: initScene.ts:3475 was deliberately changed (L-1191) from the literal `=== 'webgl-fallback'` to `isLightweightWebGlBackend(pryzmRendererBackend)` — which is `!isNativeWebGpuBackend` (createRenderer.ts:378-380) and therefore covers 'webgl-only' — with a comment naming this exact scenario: \"correct only because of a guard 1100 lines earlier fails silently the day that guard is relaxed, and it fails as 'the viewport never paints at all'\". The remaining seam is the Phase-5 abort, which is at :2290-2292 (NOT :2411), and its own comment confirms the hazard verbatim: \"intentionally LEFT AS-IS (zero boot-behavior change)\".\n\nERRORS / OVERSTATEMENTS:\n1. THE 16,011 ms SAVING IS OVERSTATED. `elapsed` is performance.now() around `Promise.resolve(this._rebuildPipeline())` (RPM :2033-2039) — a wall-clock span containing the 881-event endBatch flush, 5x REDETECT_ROOMS and 4x TopologyLayer rebuild, none of which the fix removes. Hydrate (17,163 ms, 99.4% of load) is untouched, so total-to-interactive stays ~21 s, not ~5 s. The defensible saving is \"the WebGPU-only pipeline family plus the swap\", unquantified from code. The claim admits this in its own bound, but the headline number does not.\n2. A THIRD CALL SITE IS OMITTED. `initBatchLifecycle.ts:121` calls the same reactive `maybeAutoSwitchToWebGLForHeavyScene(world?.scene?.three, 'gpu-compile-start')` from the batch GPU-compile-start hook, with NO mesh count. \"A project open has exactly one guard\" is true only empirically for this run (reason=tier:post-load proves the batch hook did not win), not structurally.\n3. \"THE PIN-OVERRIDE IS ALSO UNDOCUMENTED IN THE CONTRACT\" IS FALSE. C04 line 70 corrects line 42 explicitly: \"(b) 'An explicit webgpu / webgl selection is ALWAYS respected' — FALSE, and deliberately so.\" Lines 52-59 likewise correct the stale isHeavyModel numbers and name isSwapWorthyHeavyScene as normative. The claim read the stale line and missed the correction boxes immediately beneath it.\n4. STEP 1 IS SELF-CONTRADICTORY. `prewarmRenderer()` takes no argument and calls `createRenderer(canvas)` with no override (rendererPrewarm.ts:79), fired at src/main.ts:629 in Phase B before any project is chosen. Only the initScene Phase-5 half is reachable; the prewarmed WebGPU renderer must be discarded, which WOULD BREAK #3 flags but step 1 contradicts.\n5. COUNT SUBSTITUTION. `VersionRecord.elementCount` (503/527 on this run) is a different population from `countElements(scene)` (550 top-level children carrying userData.id/levelId). Fine at 503 vs 400; they disagree ~10% and would diverge near the boundary.\n6. `shadowFreezeSurvivesBackendSwap.test.ts` does not exist (the shadow-freeze tests are RenderPipelineManager.shadowFreeze / .shadowReallocFreeze), which strengthens rather than weakens WOULD BREAK #4. Line numbers drift ~120 lines throughout.\n\nBREAKAGE THE CLAIM MISSED: initScene.ts:3472 — `if (renderPipelineManager.status.webGpuActive) { await renderPipelineManager.activateOutlines(); … }` with the comment \"so selection highlighting still works\". A boot-classic session never activates outlines AT ALL, for the whole session. Today the user gets outlines until the swap disposes them, so the steady state is the same, but the fix must be verified against whatever selection-highlight path the classic renderer uses. Also ProjectHub.ts:262 holds `this._versionWarm` as an un-awaited field (awaited only in `_reconcileFromServer` / `_auditLocalOnlyResidency`), so a fast click reaches `hub:open-clicked` with the count possibly still cold — the fix needs a fallback for that race, not only for brand-new projects.\n\nINVARIANT CHECK: the abort protects \"a catastrophic WebGPURenderer boot failure still falls back to OBC\". Reusing `isUnintendedWebglOnlySwap(backend, intendedClassicWebGL)` preserves it exactly — the failure case still has intendedClassicWebGL=false and still aborts. This is a guard whose justification became partly false (it was written when classic was reachable only via the swap), and the repo already pre-corrected its downstream sibling at :3475 in anticipation.",
  "betterHypothesis": "The structural root cause stands as stated; only the accounting needs restating. Sharper form: \"On a project OPEN, the ONLY backend-decision site that can fire is the reactive guard hanging off `pryzm-project-loaded`, so the decision is taken at maximum attached state — and because the swap and the 16 s WebGPU shadow-pipeline rebuild BOTH hang off the same post-load tier pass (RenderPipelineManager.ts:2293-2296), the open pays for a WebGPU pipeline it builds and then deletes. The cost is the WebGPU-only pipeline family (`_loadTSL` + `_buildPipeline`, `activateOutlines`, `scheduleShadowRebuild` → `_rebuildPipeline`) plus the swap's second canvas, `device.destroy()` and three full 3,762-mesh traverses — NOT 16,011 ms, which is a wall-clock span co-tenanted by the 881-event flush, 5x REDETECT_ROOMS and 4x TopologyLayer rebuild that the fix does not touch.\" Sequencing: land step 4 (the `isUnintendedWebglOnlySwap` boot arm) FIRST and prove it in isolation by forcing a classic boot behind a dev flag, since without it step 1 silently hands the viewport back to OBC. Add an outline/selection-highlight check to the acceptance criteria. Attribute the 15,628 ms gap with a browser performance profile before committing to any saving figure — this repo's own §STARTUP-BUDGET doctrine is that a span containing other main-thread work is not a cost.\""
}