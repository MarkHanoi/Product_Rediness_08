{
  "area": "Furniture/kitchen mesh multiplication — `FurnitureInstanceBridge` is wired end-to-end but its feature flag is DEFAULT-OFF and nothing in production sets it",
  "rootCause": "FurniturePart (1487) and KitchenCabinetPart (647) are stamped by the SAME builder — `FurnitureFragmentBuilder.updateFurniture()` at packages/geometry-furniture/src/FurnitureFragmentBuilder.ts:265 (`elementType: existingElementType ?? (isKitchenPart ? 'KitchenCabinetPart' : 'FurniturePart')`) — so they ride ONE render path and ONE flag. That path is procedural, not GLB: `FurnitureFactory` dispatches to ~60 hand-written builders that emit one `new THREE.Mesh` per physical part (ChairBuilder.ts has 66 `new THREE.Mesh(` sites, BedEngine.ts 63, KitchenCabinetEngine.ts 58); GLB imports early-return at FurnitureFragmentBuilder.ts:~160 (`if (data.furnitureType === 'glb_import') { ... return; }`) and never rebuild meshes at all. The collapse mechanism EXISTS and is fully wired — `initBuilders.ts:817-820` unconditionally injects `new FurnitureInstanceBridge(instancedElementRenderer)` with `isFurnitureInstancingEnabled` — but the gate is `FurnitureFragmentBuilder.ts:64` `private _instancingEnabled: () => boolean = () => false;` fed by `FurnitureInstanceBridge.ts:125-129`, a bare `globalThis.__pryzmFurnitureInstancingV1 === true` with NO per-family default row. Nothing in apps/, packages/, plugins/ or src/ ever assigns that global (only tests, tools/perf/render/render-profile.spec.ts:342, and docs), so `FurnitureFragmentBuilder.ts:331` (`if (this._instanceBridge && this._instancingEnabled())`) is dead on every production load and every item takes the fragment fallback (`root.add(mesh); root.visible = true;`). The repo's own diagnostic already says this in as many words: pryzmPerfConsole.ts:1168-1170 prints `__pryzmFurnitureInstancingV1  OFF ⚠ DEFAULT  ← furniture CANNOT instance`.",
  "evidence": [
    "packages/geometry-furniture/src/FurnitureFragmentBuilder.ts:64 — `private _instancingEnabled: () => boolean = () => false;` — the default gate, never overridden at runtime because nothing sets the flag it reads.",
    "packages/core-app-model/src/rendering/FurnitureInstanceBridge.ts:125-129 — `export function isFurnitureInstancingEnabled(): boolean { ... (globalThis as {...}).__pryzmFurnitureInstancingV1 === true }`. Grep across apps/ packages/ plugins/ src/ for an ASSIGNMENT to that global: zero production sites (only FurnitureInstanceBridge.test.ts and tools/perf/render/render-profile.spec.ts:342, which READS it). audit/perf/2026-09-04/render-profile.json records `\"flagFurnitureInstancingV1\": null` in a real browser run.",
    "apps/editor/src/engine/pryzmPerfConsole.ts:1168-1170 — the repo's own console row: `p(row('__pryzmFurnitureInstancingV1', i.furnitureInstancingV1 ? 'ON' : 'OFF  ⚠ DEFAULT', i.furnitureInstancingV1 ? '' : '← furniture CANNOT instance'));`",
    "packages/geometry-furniture/src/FurnitureFragmentBuilder.ts:265 — `elementType: existingElementType ?? (isKitchenPart ? 'KitchenCabinetPart' : 'FurniturePart')`. The two largest census families are ONE builder and ONE flag: 1487 + 647 = 2134 of 3762 meshes (57%).",
    "(a) Not GLB parts — procedural. `grep -c 'new THREE.Mesh(' packages/geometry-furniture/src/{builders,engines}/*.ts` → ChairBuilder.ts:66, BedEngine.ts:63, KitchenCabinetEngine.ts:58, ApplianceBuilders.ts:37, WardrobeEngine.ts:25. GLB takes the early return in updateFurniture(): `if (data.furnitureType === 'glb_import') { ...sync position/rotation... return; }`.",
    "(a) Parts CAN be merged per instance and the kit already exists: packages/geometry-furniture/src/builders/mergedPartKit.ts (`boxAt`/`cylAt`/`placeParts`/`mergePartsToMesh`, §DESK108) — but `grep -rl 'mergedPartKit'` shows only 7 of ~60 builders import it (AudioBuilders, BookshelfBuilder, DiningSetBuilders, MediaWallBuilder, SectionalSofaBuilder, WhiteSofaBuilder, WorkingDeskBuilders).",
    "(b) RETIRED, not still true. Per-element unique materials DO still get minted (ChairBuilder.ts has 28 `new THREE.Mesh*Material` inside build(); WardrobeEngine.ts mints ~11 at :338/:411/:514/:523/:532/:548/:557/:575/:578/:632/:636) — BUT InstancedElementRenderer.ts:244 runs `const sharedMaterial = dedupInstanceMaterial(material);` BEFORE the group key at :251 `this._hashGeometry(geometry, sharedMaterial, levelId, elementType)`. SharedMaterialCache.ts:110 `dedupInstanceMaterial` is DEFAULT-ON (`__pryzmInstanceMaterialDedup !== false`) and keys on `materialInstanceSignature` (materialSignature.ts:160-194: type + scalars + colour hexes + texture uuids), so two visually identical fresh MeshStandardMaterials collapse to one canonical uuid → one InstanceGroup. FurnitureInstanceBridge._bakeParts additionally buckets leaves by `mat.uuid` WITHIN an item and merges each bucket, so even a one-off sofa collapses to one mesh per distinct material.",
    "(b) Residual exception, named honestly: materialSignature.ts:183-186 puts `t.uuid` in the signature for every TEXTURE_KEY, and ISSUE-LOG L-11384 records that carpet textures are 'deliberately NOT shared' (one canvas texture per rug). Textured rugs therefore keep a unique signature → group of one. That is a small subset, not the 1487.",
    "(c) Handrail/stairRailing use the OTHER bridge with a per-family default table: ElementInstanceBridge.ts:332-338 `_FAMILY_DEFAULTS = Object.freeze({ window: true, column: false, beam: false, handrail: true, stairRailing: true })`, resolved at :363-385 (per-family override → master override → shipped default). Injection is unconditional and the comment at initBuilders.ts:970-973 states the rule: '⭐ THIS INJECTION IS UNCONDITIONAL AND MUST STAY SO. The flag decides whether the builder USES the bridge; if the bridge were itself gated, flipping the default would change nothing at runtime.' Measured worth, initBuilders.ts:967-969: '240 railing elements 4920 → 250 draw calls, 19.7x.' Furniture has the identical shape MINUS the defaults table.",
    "(c) Curtain wall is a THIRD, unrelated mechanism — packages/geometry-curtain-wall/src/CurtainWallInstanceManager.ts:440 stamps `elementType: 'CurtainPanelInstanced'` from its own manager, and ISSUE-LOG L-11567 records that `InstancedMeshCoalescer` 'covers curtain-wall mullions ONLY and RAISES the count +1/group'. Do not copy that one; copy the handrail one.",
    "(d) Lighting: the merge lever is ALREADY PULLED. LightingFragmentBuilder.ts:704 calls `this._consolidateFixtureMeshes(group)` on the main `add()` path (implementation at :892), bucketing by (parent, material uuid, castShadow, receiveShadow, role) — so 336 meshes is POST-merge. Materials are pooled (`_matCache` :340-343, `_lensMatCache` :~920; L-11421 closed the per-instance pendant material). There is a tessellation budget (:369-373 SEG_BODY 24 / SEG_LENS 32 / SEG_TRIM 28 / SEG_THIN 10 / SEG_CABLE 6) but NO distance LOD and NO proxy, and the builder's own comment at :367-368 says 'a later instancing pass (owned by another track) can collapse them. Do NOT wire instancing here.' 96,534 tris / 336 meshes = 287 tris each — authored fidelity at fixed tessellation, not an LOD failure.",
    "(e) Mesh count DOES cost HYDRATE, synchronously, with no deferral: ImportProjectCommand.ts:1071-1076 (`__step('furniture', ...)`, `for (const f of snapshot.furniture)`) → CreateFurnitureCommand → FurnitureStore.ts:48-52 `add()` → `_bus.emit('bim-furniture-added', { id })` on a `DOMEventBus` (FurnitureStore.ts:5, synchronous window.dispatchEvent) → initBuilders.ts:883-885 listener → `updateFurniture()` → every BufferGeometry and Mesh allocated. Note the buffered bus is the OTHER one: `storeEventBus.emit` at FurnitureStore.ts:52 is what the console's '881 buffered event(s)' / event_flush=15.0ms measures, and 15 ms cannot be 2134 mesh builds.",
    "(e) But the SIZE of that share is not in the founder's paste. ProjectLoader.ts:544-566 defines `__mark`/`__marksSummary` and emits `[ProjectLoader] §LOAD-HYDRATE-STEPS ...` at :2875, immediately before `__phase('hydrate')` at :2876. That line is missing from the paste. Ask for it before sizing any load-time claim.",
    "(e) The 16.0 s shadow number is NOT attributable to mesh count from this code. RenderPipelineManager.ts:2034-2038 times `Promise.resolve(this._rebuildPipeline())`; that resolves to `_buildPipeline()` at :3753-3820, which awaits `import('three/webgpu')`, calls `createScenePass`/`createZonePass`, composes a TSL node graph and constructs `new RenderPipeline(...)`. There is NO per-mesh and NO per-caster loop anywhere in it. Calling it 'shadow rebuild over 3274 casters' is unsupported.",
    "(e) Mesh count DOES cost frame time and DID trip the backend swap — apps/editor/src/rendering/heavySceneSwapThreshold.ts:41-44 `SWAP_ELEMENT_THRESHOLD = 400` / `SWAP_MESH_THRESHOLD = 1000`, whose header states '⛔ THE FIX FOR CROSSING THESE ARMS IS FEWER MESHES PER ELEMENT, NOT A HIGHER THRESHOLD.' ⚠ HONESTY: the arms are OR'd, and this project is 550 elements ≥ 400 — so instancing furniture will NOT prevent the WebGPU→WebGL swap in this project. Do not sell that as a benefit.",
    "(e) One measured per-mesh cost that IS in the paste: '[LOAD-SHADOW-PASS-DEFERRED] ran ONE full-scene shadow-flag pass in 7.7ms' over 3762 meshes ≈ 2 µs/mesh/traverse. Removing 2134 meshes cuts every full-scene traverse (shadow-flag pass, the census, RenderPipelineManager.ts:1976-1977's own meshCount traverse, TopologyLayer) by ~57%. Real, but small in absolute ms.",
    "Census semantics caveat — pryzmPerfConsole.ts:511-521 counts an 'element' only when a MESH carries both `userData.id` and `userData.elementType`. FurnitureFragmentBuilder.ts:~262 stamps children with `parentId`, never `id`, and the root is a Group (not a mesh), so FurniturePart/KitchenCabinetPart/Lighting all report elems=0 and m/elem '—'. The headline '23.22 meshes/element' has a denominator of 162 mesh-level ids out of 550 loaded elements — treat the scene-wide ratio as a lower bound on the real per-element figure, not as a measurement.",
    "Second-order shadow win, if the flag flips: InstanceGroup.ts:87-88 sets `this.mesh.castShadow = true; this.mesh.receiveShadow = true;` on the InstancedMesh — N instances submit as ONE caster. FurniturePart alone is 1421 of the 3274 casters."
  ],
  "fix": "Two steps, measurement before code.\n\nSTEP 0 (no code change, do this first). In the founder's console on the same 550-element project: `globalThis.__pryzmFurnitureInstancingV1 = true`, reload, then `window.pryzmPerf.census()`. Ask for BOTH the census block and the `[ProjectLoader] §LOAD-HYDRATE-STEPS` line (it already exists — ProjectLoader.ts:2875 — and was simply absent from the paste). That one paste settles whether the flag helps hydrate, hurts hydrate, or only helps frame time. Everything below is unjustified without it.\n\nSTEP 1 (the code change, mirroring the handrail precedent exactly). Give `isFurnitureInstancingEnabled()` a shipped default, the way `ElementInstanceBridge._FAMILY_DEFAULTS` (ElementInstanceBridge.ts:332-338) does: honour `__pryzmFurnitureInstancingV1` as an explicit BOOLEAN in both directions so `= false` stays a true kill switch, and fall through to a `true` default. That is a ~6-line change in FurnitureInstanceBridge.ts:125-129. Do NOT touch the injection at initBuilders.ts:817-820 — it is already unconditional, which is precisely the property initBuilders.ts:970-973 says must be preserved. Move the pryzmPerfConsole.ts:1168-1170 status row in the SAME commit; a stale \"OFF ⚠ DEFAULT / CANNOT instance\" row is exactly the row-and-range drift this repo documents six times over.\n\nThis one flag covers FurniturePart AND KitchenCabinetPart together (both stamped at FurnitureFragmentBuilder.ts:265) = 2134 of 3762 meshes.\n\nRANKING by (meshes saved) x (feasibility):\n1. FurniturePart — 1487 meshes. Feasibility HIGHEST: zero code needed to TEST, ~6 lines to ship, mechanism already merged and unit-tested (FurnitureInstanceBridge.test.ts).\n2. KitchenCabinetPart — 647 meshes. Feasibility HIGHEST, and it is the SAME fix, not a second one. Its materials are already pooled module-level (KitchenCabinetEngine.ts:74/88 `_matCache` + `markSharedGpuResource`), so it dedups cleanly. Combined 1+2 = 57% of scene meshes behind one flag. Do these as one lane.\n3. PlumbingFixture — 201 meshes over 19 elements (10.6 meshes/fixture) and 91,842 tris = 457 tris/mesh, the heaviest per-mesh family in the census. Feasibility MEDIUM: `grep 'mergeGeometries|InstanceBridge' packages/geometry-plumbing/src/*.ts` returns NOTHING — no merge, no bridge. But toilets/basins are the most-repeated fixtures in a building and the handrail wiring is a 3-line precedent. Two independent levers (merge via mergedPartKit, then ElementInstanceBridge with a `plumbing: true` row).\n4. Lighting — 336 meshes / 96,534 tris. Feasibility LOWEST, because the cheap lever is already pulled: `_consolidateFixtureMeshes` runs on the main add() path (LightingFragmentBuilder.ts:704). What remains needs new design — an instancing bridge the builder explicitly declines to host (:367-368), complicated by lenses being excluded from merge by contract and `_syncLens` re-assigning `.material` per lens mesh. The bigger lever here is not geometry at all: the console says \"3 of 87 fixtures are illuminating the room\", so 84 fixtures are pure geometry — a non-illuminating-fixture proxy/LOD is a product decision worth more than instancing, and should be raised as one rather than coded.",
  "expectedSavingMs": 2000,
  "savingBasis": "estimated",
  "risk": "medium",
  "confidence": "high",
  "rivalRuledOut": "RULED OUT — \"instancing is still defeated by per-element unique materials\" (the repo memory `webgpu-heavy-scene-crash-and-instancing`, §INSTANCE-MAT-SHARE). That memory is true history and stale as a diagnosis. The trap it describes was closed GENERICALLY at the single chokepoint, not per-builder: InstancedElementRenderer.ts:244 calls `dedupInstanceMaterial(material)` and only then hashes the group key at :251; SharedMaterialCache.ts:110 is DEFAULT-ON (`__pryzmInstanceMaterialDedup !== false`) and keys on a VALUE signature (materialSignature.ts:160-194), not on `material.uuid`. Its own header names \"furniture leaves\" as one of the builders it was written to fix. So the fresh materials ChairBuilder/WardrobeEngine still mint would collapse to one canonical instance today. Per-element materials are no longer the first blocker; the unset flag is in front of them. (Residual, named not hand-waved: TEXTURE_KEYS carry `t.uuid`, and L-11384 records carpet textures as deliberately unshared — textured rugs will still form groups of one. Small subset.)\n\nNOT RULED OUT — \"flipping the flag will make HYDRATE slower, not faster.\" `FurnitureInstanceBridge._bakeParts` does real work per element at build time: it traverses the group, clones EVERY leaf geometry, applies a matrix to each clone, buckets by material uuid, and calls `mergeGeometries` per bucket (with a position-only retry on failure). That lands in the same synchronous hydrate window as the build it replaces. Whether the merge cost is smaller or larger than the 2134 scene-adds it removes is genuinely unmeasured, and I could not settle it by reading. This is why STEP 0 exists.\n\nNOT RULED OUT — what actually consumes the 16.0 s `SHADOW_REBUILD_COMPLETE`. Two candidates I could not separate from code: (i) three.js-internal RenderPipeline dispose/rebuild scaling with distinct material count — audit/perf/2026-09-04/render-profile.json records `distinctMaterials: 2793` against only 1571 meshes, so materials outnumber meshes ~1.8:1 and that ratio is itself suspicious; (ii) main-thread starvation — `_buildPipeline` awaits `import('three/webgpu')` and the timer is wall-clock ACROSS await points, so any synchronous work that ran in between is charged to it. Mechanism: UNKNOWN. Do not let anyone attribute that 16 s to caster count on the strength of the caster census sitting next to it in the log.",
  "whatItWouldBreak": "1. SHADOW POLICY IS SILENTLY OVERRIDDEN. FurnitureFragmentBuilder.ts:~283 sets `child.castShadow = furnitureCastsShadowUnderBudget(data.furnitureType)` per part — plants, lamps, rugs, wall decor and curtains are deliberately demoted (ADR-0076 Axis 2; the console shows \"demoted 66 by family policy\"). InstanceGroup.ts:87-88 hard-sets `castShadow = true` on every InstancedMesh. So a demoted decorative item starts casting again the moment it instances. That is a visible fidelity change AND it partly cancels the caster-count win. Must be fixed in the bridge (carry the budget into the group key, or set the flag on the group from its members) before shipping, not after.\n\n2. GPU RESOURCE LIFETIME. The instanced branch calls `scheduleGpuRelease(child.geometry)` on every leaf and leaves `root.visible = false`. SharedMaterialCache.ts:~125-145 warns in its own comment that an elected canonical material stops belonging to the element that minted it while every builder in the repo still frees \"its own\" materials on delete/rebuild — ADR-0297 called this its \"third instance, latent\" and it \"stops being latent the moment element instancing is switched on\". Furniture delete/edit churn is exactly where that lands. `beam` and `column` are still OFF in `_FAMILY_DEFAULTS` for this precise reason (\"OFF: ADR-0297 L2(b) dispose-in-place OPEN\", pryzmPerfConsole.ts:1159-1160) — so this is a known-open hazard, not a hypothetical, and the furniture flip must not be defended by pointing at handrail without checking the delete path.\n\n3. SELECTION AND ISOLATE. The bridge preserves both by construction (same `pickId` = real furniture id on every part; `levelId` + 'Furniture' stamped on each group) and there are tests, but the census will re-label 2134 meshes from `FurniturePart`/`KitchenCabinetPart` to `Furniture`. Anything keyed on the sub-element type strings will stop matching: apps/editor/src/engine/views/EdgeProjectorService.ts, packages/core-app-model/src/rendering/shadowCasterPolicy.ts and packages/core-app-model/src/presentation/VGSceneApplicator.ts all reference `FurniturePart` by name. Grep those three before flipping.\n\n4. PLAN VIEW. Chair meshes are tagged `skipInPlan: true` in `ChairBuilder.build()` (ChairBuilder.ts:25-31) and `EdgeProjectorService` reads it off the mesh. `_bakeParts` merges leaves and hands the renderer bare geometry — userData does not survive into the InstanceGroup. If plan-view edge projection walks scene meshes, instanced furniture either vanishes from plan or its dense 3D edges reappear there. Not verified either way; check before shipping.\n\n5. WHAT IT WILL NOT FIX, so nobody claims it: the backend swap. heavySceneSwapThreshold.ts arms are OR'd and this project is 550 elements against `SWAP_ELEMENT_THRESHOLD = 400`. The WebGPU→WebGL swap fires on the element arm whether or not furniture instances.",
  "filesToChange": [
    "packages/core-app-model/src/rendering/FurnitureInstanceBridge.ts",
    "apps/editor/src/engine/pryzmPerfConsole.ts",
    "packages/core-app-model/src/rendering/InstanceGroup.ts",
    "packages/geometry-furniture/src/FurnitureFragmentBuilder.ts"
  ]
}