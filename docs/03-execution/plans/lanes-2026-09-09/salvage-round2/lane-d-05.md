{
  "refuted": false,
  "reason": "CONFIRMED on every code anchor. (1) FurnitureFragmentBuilder.ts:64 is verbatim `private _instancingEnabled: () => boolean = () => false;`. (2) FurnitureInstanceBridge.ts:125-129 is a bare `globalThis.__pryzmFurnitureInstancingV1 === true` with no family table. (3) Grep across apps/ packages/ plugins/ src/ tools/ scripts/ returns 14 hits and ZERO production assignments — only 3 writes in FurnitureInstanceBridge.test.ts, a read at tools/perf/render/render-profile.spec.ts:342, and the two pryzmPerfConsole read sites (:913, :1168). The gate is dead on every production load, so FurnitureFragmentBuilder.ts:331 never takes the instanced branch and every item hits `root.add(mesh); root.visible = true;`. (4) :265 stamps both families from one expression; GLB early-returns at :166 and never rebuilds meshes. (5) initBuilders.ts:817-820 is unconditional (try/catch only), matching the rule stated at :971-973. (6) ElementInstanceBridge.ts:332-338 _FAMILY_DEFAULTS plus the 3-tier resolution at :363-385 is exactly the precedent, and its master flag is already honoured as an explicit BOOLEAN in both directions — so the proposed ~6-line change is a faithful mirror.\n\nI additionally verified the part the claim asserted but did not prove: that the collapse would actually happen. InstancedElementRenderer._hashGeometry (:806-819) keys on CONTENT, not geometry uuid — `elementType_levelId_idxCt_vtxCt_x0_y0_z0_material.uuid` — so two separately-built identical chairs land in one group, and dedupInstanceMaterial at :244 runs before the key at :251. No furniture builder passes a material array, so _bakeParts' `Array.isArray(m.material) -> return null` decline path does not mass-disqualify furniture. _sourceKey resolves `type:<furnitureType>` for every non-GLB item because the stamping traverse at :258-290 runs BEFORE the register attempt at :331. Supporting counts are accurate: ChairBuilder 66 / BedEngine 63 / KitchenCabinetEngine 58 `new THREE.Mesh(`; mergedPartKit imported by exactly 7 real builders of 66 builder files.\n\nTwo WOULD-BREAK items are confirmed in source, not speculative: InstanceGroup.ts:87-88 hard-sets castShadow=true while shadowCasterPolicy.ts:106-110 resolves the demotion off `furnitureType`, which the bridge never stamps on the aggregate (it registers elementType 'Furniture' only) — so ADR-0076 Axis 2 demotions silently resume casting; and SharedMaterialCache.ts:~130-145 states the ADR-0297 L1 ownership hazard verbatim (\"stops being latent the moment element instancing is switched on\"), with pryzmPerfConsole carrying the matching prohibition \"Do NOT flip a family here without the test that proves its delete path... flip it on a number.\"\n\nTwo claimed breakages resolve BETTER than stated: EdgeProjectorService.ts:228 and VGSceneApplicator.ts:126 both already map 'Furniture' alongside 'FurniturePart', and shadowCasterPolicy.ts contains no 'FurniturePart' literal at all — so the census re-label survives all three named files. Plan view fails in the vanish direction only (instanced roots are emptied and the projector reads meshes off element roots), affecting types with no *PlanSymbolBuilder — only bed/chair/kitchen/sofa/tree exist.",
  "betterHypothesis": "Keep the diagnosis; detach the 2000ms. The gate sits AFTER mesh construction, so it cannot touch the cost the founder's paste actually measures. In updateFurniture(), `builder.build(resolved)` runs unconditionally at ~:239, the stamping traverse at :258-290, and only then the instancing attempt at :331 — there is no path where a built mesh is skipped. On the instanced branch the work strictly INCREASES during hydrate: _bakeParts clones every leaf geometry (`m.geometry.clone()`), applies a matrix to each, and runs mergeGeometries per material bucket, then scheduleGpuRelease walks every child again. So flipping the flag leaves HYDRATE — 17163.5ms of the 17258.5ms load, 99.4% — the same or slower on CPU, offset only by fewer GPU geometry uploads.\n\nThe claim's own STEP 0 is right to demand measurement first, and its own text already disowns two of the three big numbers (it correctly shows RenderPipelineManager._buildPipeline at :3753-3820 has no per-mesh loop, so the 16.0s shadow rebuild is not caster-count; and it correctly notes heavySceneSwapThreshold's OR'd arms mean 550 elements >= 400 triggers the swap regardless). What remains is real but steady-state, not load-time: ~57% off every full-scene traverse (measured rate 7.7ms / 3762 meshes), 1421 of 3274 casters collapsing via InstanceGroup.ts:87-88, and per-frame draw-call reduction. Restate the saving as frame-time and traverse-time with an unknown (possibly negative) hydrate delta, and make STEP 0 report hydrate BEFORE and AFTER, not only after.\n\nSequencing correction for STEP 1: the shadow-policy regression must land in the same commit, not as a \"before shipping\" note — carry furnitureType (or the resolved castShadow verdict) into the bridge's register() call so it enters the group key, otherwise flipping the default silently reverses ADR-0076 Axis 2 for the 66 items the console reports as demoted. And per the repo's own rule above _FAMILY_DEFAULTS (\"do not flip one on a reason — flip it on a number\"), the furniture flip needs the instanced delete-path test that beam and column are still OFF for; SharedMaterialCache's own comment says the ADR-0297 L1 hazard goes live exactly at this switch, and furniture edit/delete churn is where it lands."
}