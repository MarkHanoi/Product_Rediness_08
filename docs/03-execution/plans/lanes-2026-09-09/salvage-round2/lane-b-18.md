{
  "refuted": false,
  "reason": "MECHANISM CONFIRMED — every code citation is accurate. `classifyTileGroundPicks` (globeGroundAnchor.ts:198-216) is exactly two scalar thresholds over a sorted 1-D list, `slope` is reached BY ELIMINATION (it is the fall-through `return`), the slope arm seats at the ring median (:279), `GLOBE_GROUND_SEAT_EPSILON_M = 0.3` (CesiumViewport.ts:619) closes the arithmetic, `safeSampleTileHeights` (:9014-9036) really does compact to `number[]` and drop non-finite entries, the in-parcel picks really are skipped when `photorealTilesActive` (:9203-9210), the void cap rides the same datum (:13033), the roof fixture really is synthetic with a 14 m gap (GlobeGroundAnchor.test.ts:473-480), and `git show c36b75bc` is dated 2026-09-05 and is a +113/-0 patch that added the slope arm — a three-day-old regression, as claimed. The second anomaly is confirmed INDEPENDENTLY by a module the claim never cites: `scopeAnchor.ts:40-47` states in the repo's own words that `parcelFrameOrigin` returns the ring's FIRST VERTEX and that first-vertex→centroid is \"tens of metres on a city lot\" (the reported 28.6 m), the void is cut on `committedParcelLonLat` (:12933) — the same ring — and `pointInPolygon.ts:117-126` names the on-ray point as the ulp case. So `anchor-in-void=n` is expected noise. TWO CAVEATS ON THE DIAGNOSIS. (H1) The roofs-vs-terrain half is NOT verifiable from this repo and the claim concedes it. `clampToHeightMostDetailed` is a vertical drill pick, so under the naive model it returns roof OR street and the distribution should be BIMODAL — which would take the `roof-gap` arm and seat correctly. The observed 2.00 m max step therefore means either the pick genuinely grabs facade drape, or the ring really is a ramp. Unresolved here. (H2) The claim asserts \"no other contributor\" purely from the arithmetic matching; that is consistent but not exclusive. ⛔ BUT THE FIX IS THE PROBLEM, AND THREE OF ITS FOUR ARMS ARE UNSOUND. (A) ARM 1 HAS NO DATA ON THE FAILING PATH. `§A.21.D-GLOBE3` at CesiumViewport.ts:8301-8313 (and again at :6114-6125) executes, whenever `input.keepPhotoreal && this.photorealTilesActive` — the SAME flag the claim relies on at :9203 — `clearContextBuildings(); contextBuildingsAt = null; contextRoadsAbort.abort(); clearContextRoads();` and never calls any `loadContext*`. So `contextBuildingPlacements` is `[]` and there are no roads. The mask's NEGATIVE half masks nothing and the POSITIVE half has no centrelines; it is a silent no-op that would log a confident \"0 picks masked\" — the exact §CONFIDENT-REGISTER-ROWS shape the claim invokes against the current probe. Fetching them instead means a network await inside the token-guarded clamp, which is the documented L-142/L-179 shape (a slow await inside the clamp \"returned out of the WHOLE clamp before the tile-mesh fallback ran → the base stayed at flat 0 and the real GLB sank ~650 m under Madrid\"). (B) ARM 2 IS REFUSED BY DESIGN ON THIS PATH. `decideBakedTerrainAttach` (terrainCoverage.ts:1171) returns `{attach:false, reason:'photoreal'}` when `photorealActive && !formaMode`, and L-12991's `boundedTerrainPermitted` refuses again at world framing. `contextGroundCache` is filled only by the context loaders, which do not run here. So terrain(lat,lon) = 0, r_i = pick_i, c = low quantile of the picks, and \"ground = r_i − c ≤ 2.5 m\" silently degenerates to the pre-L-12919 p10 rule — i.e. it silently restores the Sète burial while printing an independent-cross-check log line. (C) ARM 1'S STATED CONCLUSION IS FALSE AND WOULD REGRESS SÈTE. \"The slope/plateau arms then become unnecessary: they exist only because ground and roof were indistinguishable\" — no. The slope arm exists because THE RING SPANS TERRAIN. Delete every roof pick perfectly and a hillside ring still runs 132 m at the downhill corner to 206 m uphill; \"seat = low quantile of the surviving picks\" is the downhill corner, which is L-12919 verbatim. Roof contamination and slope are orthogonal. Arm 3 fixes it — so the proposal contradicts itself, and shipping in its stated order (1 = PRIMARY, 3 = FALLBACK) re-breaks Sète. (D) ARM 4, MARKED \"cheap, ship first\", IS NEITHER, AND IT RE-ENABLES A DOCUMENTED 38 m BURIAL. It needs |grad h|, which needs positions (prereq 0) and a gradient estimate (arm 3), so it cannot ship first. Worse, it anchors on `min_credible` and the claim writes 64.89 — the raw MINIMUM, not p10 (66.35). That is precisely the quantity §GLOBE-LONE-OUTLIER-CANNOT-SET-THE-DATUM (L-479) removed: the comment at globeGroundAnchor.ts:245-256 records the same parcel minutes apart, \"picks=48 → 50.87 m (correct)\" and \"picks=49 → 11.38 m (buried ~40 m)\", one fall-through pick 39 m below the rest that survives L-477's ±2 m filter. Under the proposed ceiling that run caps at ~11.4 + grad·R ≈ 13-15 m and CLAMPS the correct 50.87 m seat down to it. A hard ceiling anchored on the extreme restores every single-pick veto L-479 exists to deny. INVARIANTS THE CURRENT CODE IS PROTECTING, all live and all breakable by this fix: §GLOBE-GROUND-STREET-RING (seat on street, never a podium); §GLOBE-LONE-OUTLIER-CANNOT-SET-THE-DATUM L-479 (arm 4 breaks it); §GLOBE-ELLIPSOID-PICK-IS-NOT-GROUND L-477; §GLOBE-DONT-MEASURE-THE-HOLE L-479 (a plane fit must not be re-fed in-parcel picks for \"more data\"); the RETIRED absolute cap (Paris ~80 m is real ground); and refuse-rather-than-place-wrongly — the proposal never says what happens when the mask empties the ring, and in a wall-to-wall Eixample block a 3 m-buffered footprint mask over radii of ext+25/ext+55 can plausibly leave single digits, where the correct outcome is `unresolved` → hidden + retry, not a seat from three survivors. ONE MORE FAN-OUT THE FIX MISSES: `CesiumViewport.selectPhotorealTileBaseHeight` (:8989-8997) is a second, separately-tested public caller of `reduceTileGroundHeight` that has no positions to give and passes no `rejectEllipsoidPicks`; a position-aware signature must not orphan it, or the two call sites will disagree the way the three commandManager counters do.",
  "betterHypothesis": "Keep the diagnosis; replace the fix plan. STEP 1 — MEASURE BEFORE CHOOSING AN ARM, because roofs-vs-slope is still unproven. Do prereq (0), but cheaper than proposed: the returned items ALREADY carry their own position — `clampToHeightMostDetailed` yields Cartesian3 (→ `Cartographic.fromCartesian` gives lon/lat/height, and :9341 already builds that object and throws two thirds of it away) and the `sampleHeightMostDetailed` fallback yields Cartographic directly. So `extract` can return `{lat, lon, h}` with NO index correspondence to `samplePts` to preserve. Ship that plus a log of all 58 picks with position, and read whether height is a function of position. This is the falsification test the claim itself names, and it costs one type change and no network. STEP 2 — SHIP ARM 3 ONLY, and make it the whole classifier. Trimmed lower-envelope least squares h ≈ a·east + b·north + c over the picks, seated at the PARCEL CENTROID, not the ring median. It needs no data that is not already in the function, it discriminates on the axis the claim correctly identifies as missing, it subsumes plateau and slope into one estimator, it is the correct estimator on a real hillside (a ring median equals the centre value only for a symmetric ring and a linear slope), and its one-sided positive residual tail plus inlier fraction is the only proposed roof detector that works with zero footprints. Keep it inside the pure `globeGroundAnchor` module so both callers get it. STEP 3 — DROP ARMS 1, 2 AND 4 AS WRITTEN. Arm 1 and arm 2 are inoperable on the photoreal path (context cleared by §A.21.D-GLOBE3; terrain refused by `decideBakedTerrainAttach` reason 'photoreal'); if a footprint mask is ever wanted it must be a SEPARATE, later change that first decides where the footprints come from without adding an await to the clamp. If a relative ceiling is wanted, anchor it on the p10, never the min, or it re-runs L-479's 11.38 m burial — and prove it against the Sète ring before shipping. STEP 4 — GUARD THE REFUSAL. Whatever arm wins, when the surviving/inlier count falls below `OUTLIER_ROBUST_MIN_SAMPLES` the answer is `unresolved` → hold hidden → retry, never a seat from the remnant. STEP 5 — TESTS as proposed (real 58-value Barcelona corpus + the real Sète ring), but add the L-479 49-pick fall-through corpus as a third: any new arm must keep that run at 50.87 m, and arm 4 as written fails it. STEP 6 — the `anchor-in-void` requalification is correct and risk-free, but it is cosmetic; ship it separately so it is never mistaken for a cause.",
  "evidenceChecked": [
    "apps/editor/src/ui/geospatial/globeGroundAnchor.ts:150-285 — classifier + reduceTileGroundHeight read in full; thresholds SLOPE_RAMP_MIN_SPAN_M=6 (:167), ROOF_GAP_M=4 (:172), slope is the fall-through return (:216), median seat (:279); maxGap is computed ONLY over [lowIdx+1, midIdx] (the low half), as claimed",
    "apps/editor/src/ui/geospatial/globeGroundAnchor.ts:245-256 — the L-479 memo: same parcel, picks=48 → 50.87 m correct vs picks=49 → 11.38 m buried ~40 m. This is the corpus arm (4)'s min-anchored ceiling would re-break",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:619 — GLOBE_GROUND_SEAT_EPSILON_M = 0.3, passed at :9397; :653/:660 void-cap constants; :13033 cap rides formaTerrainBaseHeight",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:9014-9036 — safeSampleTileHeights returns number[], drops non-finite; :9333-9355 both call sites; the extract at :9341 already constructs a Cartographic and discards lon/lat, so prereq (0) is cheaper than the claim states",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:9203-9246 — in-parcel picks skipped on photorealTilesActive; ring = boundary verts ×1.7 + 16 pts at ext+25 + 16 at ext+55",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:8276-8313 — ⛔ §A.21.D-GLOBE3: the SAME keepPhotoreal && photorealTilesActive branch that routes to clampToPhotorealTilesThenReplace also clears context buildings, nulls contextBuildingsAt and skips every loadContext* call. Repeated at :6114-6125, which additionally aborts and clears context ROADS. Arm (1) has no footprints and no roads on the failing path",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:2322-2334 — the only other loadContextBuildings/Roads entry is gated on `contextBuildingsAt`, which the photoreal branch nulls, so it is inert too",
    "apps/editor/src/ui/geospatial/terrainCoverage.ts:1164-1176 — decideBakedTerrainAttach returns {attach:false, reason:'photoreal'} when photorealActive && !formaMode, and {reason:'world-framing'} when !boundedTerrainPermitted (L-12991). Arm (2)'s terrain source is refused by design on this path",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:10994-11010 + 10436/10478 — maybeAttachTerrainProvider's own header (§A.21.D-GLOBE3) states terrain is skipped on the true photoreal path; contextGroundCache is filled only by the context loaders, which do not run there",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:8989-8997 — selectPhotorealTileBaseHeight is a SECOND public caller of reduceTileGroundHeight with no positions and no rejectEllipsoidPicks; a position-aware signature must not orphan it",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:9366-9391 — the §GLOBE-SLOPE-SEAT log line prints arm, p10, median, low-half span and largest step, matching the console format the claim quotes",
    "apps/editor/__tests__/GlobeGroundAnchor.test.ts:455-485 — all four arm fixtures are synthetic: a perfect 64-step linear ramp, a 40+8 flat set, and the roof case with a clean 14 m gap. Confirmed as claimed",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:12933 + 13112-13140 — the void is cut on committedParcelLonLat and the probe tests realModelOnGlobeOrigin (set from input.originLat/Lon at :17457) against that same ring",
    "apps/editor/src/ui/geospatial/scopeAnchor.ts:40-47 — INDEPENDENT corroboration the claim did not cite: the repo states parcelFrameOrigin is the ring's FIRST VERTEX, not the centroid, and that the gap is 'tens of metres on a city lot'",
    "apps/editor/src/ui/site/boundaryProjection.ts:123-145 + SiteBoundaryDrawTool.ts:284 + SiteBoundaryMap2D.ts:4054 — parcelFrameOrigin = ring[0], used at both commit paths",
    "packages/geometry-kernel/src/pure/pointInPolygon.ts:105-134 — the ulp/on-ray comment is verbatim as quoted",
    "git show c36b75bc — 2026-09-05, +113/-0 across globeGroundAnchor.ts / CesiumViewport.ts / the test / ISSUE-LOG; the slope arm is a three-day-old addition",
    "docs/04-reference/ISSUE-LOG.md L-12919 — the Sète row, including its own admission '⚠ Not browser-verified'; and L-142's REVERTED row, which is the precedent for adding a terrain source or an await to this clamp"
  ]
}