{
  "survivingTitles": [
    "Section poché can never render: PocheFillBuilder discards world Y, so a vertical cut ring collapses to a 2-vertex degenerate and is dropped",
    "The test that should have caught the section/elevation poché collapse uses a HORIZONTAL fixture labelled as an elevation cut band",
    "Symbol coverage is 15 for plan, 3 for elevation, 1 for section — and lighting has no injector for any view",
    "The LIVE elevation/section driver (PlanViewManager, DOC-19B) never injects level datums or grid lines — all four call sites that do are on paths this route does not use",
    "Clicking an elevation or a section in Views & Sheets skips the GIS exit — the L-13253 'covered by Cesium' bug the founder reported is fixed for plans and 3D only",
    "Two rival elevation implementations share the name 'Front': the ViewCube preset is a PERSPECTIVE 3D camera with plan symbols explicitly disabled, and it is what runs whenever no ViewDefinition id is queued",
    "Auto-dimension covers plan and elevation and honestly refuses section — this one is NOT a defect and should be left alone"
  ],
  "refutedTitles": [
    "Selecting a section or elevation in the plan pane's view dropdown never triggers a projection — it renders whatever happens to be cached, which for a never-opened section is nothing"
  ],
  "refutations": [
    "REFUTED — 'Selecting a section or elevation in the plan pane's view dropdown never triggers a projection'. The auditor found the wrong handler. `SplitViewManager._onViewSelectChange` emits `split-view-view-changed` at :1253, and `PlanViewManager._onSplitViewViewChanged` (apps/editor/src/engine/views/PlanViewManager.ts:1165-1175) subscribes to exactly that event (subscription registered at :185) and does precisely the warm-up the finding says does not exist: `if (!viewId || viewTechnicalDrawingCache.get(viewId)) return; ... if (viewDef.viewType === 'section' || viewDef.viewType === 'elevation') this._ensureProjectionForSplitView(viewDef);` — cache-miss-gated, section/elevation-gated, on view SELECTION. The finding cites PlanViewManager:944-951 as 'the only partial mitigation'; that citation is accurate but it is the `vd:view-updated` arm, a different handler, and the auditor stopped there. Two further independent drivers also exist and are registered globally rather than being gated on any pane: `initScene.ts:1523` calls `viewDependencyTracker.notifyViewActivated(viewId)` on the same `split-view-view-changed` event, and `viewDependencyTracker.onReprojectionNeeded` is owned at `initScene.ts:1250`. One secondary detail is also wrong: the finding says the pane shows the 'Add walls…' placeholder, which it calls 'actively misleading for a section' — PlanViewCanvas.ts:396-400 selects the placeholder by `isPlanLike`, so a section already shows 'Generating view…'. WHAT SURVIVES, NARROWED: the :1165 subscription is registered inside `PlanViewManager.activate()`, which `ViewController` calls only for Canvas2D main-panel views (:1521, :2161). With the main viewport on 3D and the SVP auto-opened (initScene.ts:4659), PlanViewManager is inactive and that warm-up does not run — leaving only the deferred-dirty path, which early-returns for a view not in `_deferredDirtyViewIds` (ViewDependencyTracker.ts:518). That is a conditional gap, not the unconditional 'never triggers a projection' the finding asserts, and it is not the mechanism the finding evidences. Refuted as written."
  ],
  "missed": [
    "LIGHTING LINEWORK IS UNCLASSIFIED — this is the single mechanism behind all three of the founder's complaints for that family, and finding 3 stops one step short of it. `Lighting` is absent from `ELEMENT_TYPE_TO_PROJECTION_LAYER` (EdgeProjectorService.ts:203-240 — no lighting/electrical row of any kind), so `resolveProjectionLayer('Lighting')` (:328) returns `FALLBACK_NATIVE_LAYER = 'projection-visible'` (:243), and that resolver is the SOLE authority at both stamping sites (:2932 and :3758) — `LightingFragmentBuilder.ts:731`'s `group.userData.layerName = 'A-LGHT'` is never read there. `vgCategoryForLayer('projection-visible')` returns null (DrawingLayerIdentity.ts:201-208; and `ISO_LAYER_TO_VG_CATEGORY` at :96-125 has no 'A-LGHT' row either), so PlanViewCanvas.render's `if (vgCat && this._styleResolver)` (:461) is SKIPPED ENTIRELY: no `resolved.visible` check, no `edgeColor`, no `lineWeight`. `penCategoryForLayerTag('projection-visible')` (PenWeightTable.ts:439-457) matches none of its eleven regexes, so it also falls to the generic pen. Consequences, all measured from code that runs: (a) every lighting fixture's raw `EdgesGeometry` mesh dump is drawn unstyled in plan — the founder's log shows exactly this, 'repeated Lighting groups at 672 faces each', and it is what his 'dense circular hatch-like symbols / fine black concentric line bursts' screenshot is; no lighting builder tags `skipInPlan` (grep over all 23 files that set it returns zero lighting hits), so unlike sofa/bed/chair/kitchen/tree/plumbing/window the mesh is never suppressed, and `PlanViewCanvas._renderLightingPlanSymbols()` (:660, :2092) then paints the canvas symbol ON TOP — a genuine double-draw; (b) hiding lighting in Visibility Intent cannot touch that linework — 'I EXCLUDE THE FURNITURE ELEMENTS FROM VIEW BUT THEY STILL RENDER'; (c) line-type/weight changes cannot reach it — 'I CHANGE THE LINE TYPE ETC.. DOESNT WORK'. The repo already names this exact failure shape twice: L-275 ('projection-visible — outside A-WALL, outside the pen table, outside the cut gate') and L-10502 ('Without this row a boundary line's linework is UNCLASSIFIED, so the VG governance panel has no toggle for it and the founder cannot hide his own setting-out lines'). This is the third recurrence, it is a two-row fix (an `A-LGHT` entry in each table plus `skipInPlan` on the lighting builder), and it applies in elevation and section identically — it should outrank finding 3's 'build an injector' work, which is large and does not fix (b) or (c) on its own.",
    "ELEVATION POCHÉ IS DEAD BY THE SAME COLLAPSE — finding 1 under-scopes itself to sections. `isSectionDepthView = viewType === 'section' || viewType === 'elevation'` (EdgeProjectorService.ts:2317), so `sectionDepthBands` is non-null for BOTH, `buildMeshPlaneIntersectionGeometry` runs for BOTH (:3012), and the elevation branch routes `cutParts` — the same plane-coplanar geometry plus `classified.cutGeo` near-band edges — to `_layerCut(layerName)` at :3393-3396. `_ELEVATION_SCOPE.poche` is `true` (ViewScope.ts:135-144) and PlanViewCanvas.ts:427-429 calls `_renderPocheFills` whenever `_pocheScope.poche`, so L-1601's '§ELEVATION-POCHE-IS-INTENT-DECLARED' feature reaches the same builder and collapses identically. Net: the elevation half of L-1601 has never been able to paint a pixel either. This matters for the fix's scope and for the regression pin — finding 2's proposed vertical-ring arm covers both, but the lane must be told it is fixing two features, not one.",
    "A STALE CODE COMMENT WILL TALK THE NEXT LANE OUT OF THE FIX. EdgeProjectorService.ts:3387-3392 states, inside the elevation routing branch: 'solid cut fills are gated on ViewScope.poche (false for elevation) in PlanViewCanvas ... `_renderPocheFills` — the only consumer that scans `:cut` sub-layers — is never invoked for an elevation.' Both clauses are false at HEAD: `_ELEVATION_SCOPE.poche` is `true` (ViewScope.ts:140) and PlanViewCanvas.ts:427 invokes `_renderPocheFills` for it. The comment predates L-1601 and was never updated when the scope flipped. An engineer landing on finding 1 and grepping outward will read this and conclude the elevation side is out of scope by design.",
    "`apps/editor/src/engine/views/plan-canvas/PlanViewFillRenderer.ts` IS DEAD IN ITS ENTIRETY, not just `renderPipelinePolygons`. Finding 1 correctly flags :174 as callerless, but `grep -rn 'PlanViewFillRenderer|planViewFillRenderer'` across apps/packages/plugins returns only the file's own declarations (:20, :268) and three prose mentions in unrelated headers (initScene.ts:1003, DrawingLayerIdentity.ts:14, RoomStore.ts:29). Nothing imports the exported singleton. So the second copy of the wrong 3D→2D reduction at :262 (`return { h: x, v: sectionFlipV ? -z : z }`) and the duplicated `if (points.length < 3) continue` at :157 are not a 'split-view copy' that needs fixing in lock-step — they are a dead file. Delete it rather than porting the poché fix into it; carrying it forward guarantees the divergence finding 1 warns about."
  ]
}