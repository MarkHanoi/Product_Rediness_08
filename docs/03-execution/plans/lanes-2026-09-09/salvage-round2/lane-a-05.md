{
  "dimension": "Furniture is excluded from the view and still renders — the actual path a furniture element takes to the plan drawing, and every bypass of the visibility-intent filter on it",
  "summary": "The founder's own hypothesis (\"probably because they are symbols?\") is HALF RIGHT, and the half that is wrong matters more than the half that is right.\n\nThe symbol injectors DO consult visibility intent — but only at FAMILY level, and only through a caller-side gate (`makeSymbolInjectionGate`, wired at 19 call sites in `EdgeProjectorService`). What they cannot honour is a PER-ELEMENT hide, because `OBC.TechnicalDrawing.toDrawingSpace()` returns a brand-new `LineSegments` with empty `userData`, and 17 of 18 symbol builders re-attach the element id ONLY into the `DrawingSelectionIndex` WeakMap — never into `userData.elementUUID`, which is the single key the canvas's pen path reads. So per-element hides slide straight off every door swing, sofa, bed, wardrobe, chair, kitchen and tree symbol in the drawing.\n\nBut the likeliest cause of the founder's specific screenshot is upstream of all of that and simpler: the Project Browser's furniture checkbox writes NO override at all when the 3D view is active. `_activeViewId()` calls `window.viewDefinitionStore.getActiveId()` — a method that does not exist on `ViewDefinitionStore` (the only `getActiveId()` in the repo is on `ToolRegistry`) — then falls back to `viewController.currentViewDefinitionId`, which `ViewController` sets to `null` on every 3D activation. `writeElementVisibilityIntent` then returns on `if (!viewId || !elemId)`. The scene traverse still fires, so furniture vanishes in 3D and the user believes the exclusion took. The plan's properties panel then correctly reports \"NO OVERRIDES\" — exactly what his screenshot shows.\n\nAnswering the four sub-questions directly:\n(a) The VG/intent filter is applied in THREE places, none of them to the source element set: the projector's family veto (intent, at projection), the canvas's `_styleResolver` (VG governance store, per line, per paint) and the canvas's pen (`graphicsRulesEngine.resolveStyle`, per line, per paint). `VGSceneApplicator.applyToProjectionLayers` CAN hide — it calls `drawing.layers.setVisibility` which sets `child.visible = false` — but that hide is INERT: `PlanViewCanvas` never reads `Object3D.visible` (9 occurrences of `.visible` in the file, none of them an object-visibility read). It is a styler in effect, whatever its intent.\n(b) The injections DO consult intent, via the caller. Family-level only.\n(c) `NativeElementMeshExporter.exportForView()` filters by level and view-range Y overlap ONLY. Zero occurrences of `.visible` in the whole 908-line file, no category filter, no intent read.\n(d) TWO MECHANISMS, confirmed, and they share no authority in the hiding direction. 3D hides via `Object3D.visible` (scene traverses) and via the VG governance store (`mesh.visible = style.visible`); it IGNORES the intent's `visible` entirely — the intent reaches 3D only as `surface3D` colour. 2D hides via the intent and the VG store; it IGNORES `Object3D.visible` — except IFC Source C, which is the one path that reads `mesh.visible`. That asymmetry is the root architectural finding.",
  "findings": [
    {
      "title": "The Project Browser's furniture exclusion writes NOTHING when the active view is 3D — `_activeViewId()` reads a store method that does not exist, then a field ViewController nulls in 3D",
      "mechanism": "`ProjectVisibilitySection._activeViewId()` has two arms. Arm 1, `window.viewDefinitionStore?.getActiveId?.()`, is permanently `undefined`: `window.viewDefinitionStore` is the real `viewDefinitionStore` singleton (assigned at initUI.ts:736) and `ViewDefinitionStore` declares no `getActiveId` — a repo-wide search finds exactly one definition of that method name, on `ToolRegistry`. Arm 2, `window.viewController?.currentViewDefinitionId`, is set to `null` by `ViewController` on every 3D activation. So while the 3D view is active `_activeViewId()` returns null, `writeElementVisibilityIntent` returns at its guard, and NO override is written to any view. The scene traverse two lines later still runs, so the element disappears in 3D. Every 2D view is untouched. `applyCategoryVisibility` routes the whole Furniture category through this same function, once per element, so the whole category no-ops together.",
      "evidence": [
        "apps/editor/src/ui/ViewBrowser/panels/unified-browser/ProjectVisibilitySection.ts:48 — `return (window.viewDefinitionStore?.getActiveId?.()`",
        "apps/editor/src/ui/ViewBrowser/panels/unified-browser/ProjectVisibilitySection.ts:49 — `?? window.viewController?.currentViewDefinitionId`",
        "apps/editor/src/toolbar/ToolRegistry.ts:125 — `getActiveId(): string | undefined {` — the ONLY definition of that method name in the repo; `packages/core-app-model/src/views/ViewDefinitionStore.ts` has no such method (its public API is getAll/get/has/getByType/getByDiscipline/getByLevel/getByViewName/create/update/set*/delete/restore/serialize/deserialize/reset)",
        "apps/editor/src/engine/ViewController.ts:1550-1551 — `if (viewMode === '3D') {` / `this._currentViewDefinitionId = null;`",
        "apps/editor/src/ui/ViewBrowser/panels/unified-browser/ProjectVisibilitySection.ts:68 — `if (!viewId || !elemId) return;`",
        "apps/editor/src/ui/ViewBrowser/panels/unified-browser/ProjectVisibilitySection.ts:161 — `writeElementVisibilityIntent(elemId, visible);` (called first and unconditionally from applyElementVisibility)",
        "apps/editor/src/ui/ViewBrowser/panels/unified-browser/ProjectVisibilitySection.ts:288-294 — `applyCategoryVisibility` loops `applyElementVisibility(bag, id, visible)` per element"
      ],
      "userVisibleSymptom": "Untick Furniture in the Project Browser while looking at 3D: the furniture vanishes in 3D, so the toggle looks like it worked. Open the Level 2 plan and every piece is still there, and the view's properties panel reads \"NO OVERRIDES\" — which is literally true, because nothing was ever written. This matches the founder's screenshot exactly.",
      "fix": "Give `_activeViewId()` a real read. Either delete the dead `getActiveId?.()` arm (it can only ever return undefined) and make `viewController.currentViewDefinitionId` the honest single source, or — better — resolve the view the user is actually documenting rather than the one that happens to be mounted: when the active pane is 3D, refuse the write with a visible message naming which view the exclusion would apply to, rather than silently no-opping. A silent return on \"no active view\" is the §CONTEXT-DATA-HONESTY shape: refusal and success are the same value to the user. Also delete the identical dead call at packages/geometry-wall/src/WallFragmentBuilder.ts:714.",
      "effort": "small",
      "risk": "low",
      "confidence": "high",
      "filesToChange": [
        "apps/editor/src/ui/ViewBrowser/panels/unified-browser/ProjectVisibilitySection.ts",
        "apps/editor/src/ui/ViewBrowser/panels/unified-browser/__tests__/elementFilterWritesIntent.spec.ts",
        "packages/geometry-wall/src/WallFragmentBuilder.ts"
      ],
      "alreadyExists": "`writeElementVisibilityIntent` / `view.hideElement` / `view.clearOverride` / `view.clearAllOverrides` are all built and correct — only the view-id read is broken. `viewController.currentViewDefinitionId` already exists and already carries the §ANN-VIEW-INFER fallback that infers the active level's plan view (ViewController.ts:1562-1577); that inference is exactly the resolver this call site wants, and it is already written."
    },
    {
      "title": "Symbol-injected furniture linework carries NO element identity in `userData`, so a per-element hide can never match it — the render path and the hit-test path resolve identity two different ways in the same class",
      "mechanism": "`OBC.TechnicalDrawing.toDrawingSpace()` returns a brand-new `LineSegments`; every `userData` key the builder set is dropped, and `addProjectionLines` → `DrawingLayers.assign()` re-stamps only `userData.layer`. The symbol builders re-attach the element id by calling `registerSegmentUUID(drawing, projected, id)`, which writes into a module-level WeakMap and NOTHING else. `PlanViewCanvas.render()` then reads element identity as `child.userData?.elementUUID` alone and passes it as `ctx.elementId` into `graphicsRulesEngine.resolveStyle`; `IntentRuleResolver.targetMatches` requires `!!target.elementId && target.elementId === targetId` for a `targetKind: 'element'` override. Undefined elementId ⇒ no match ⇒ the hide never applies ⇒ full-opacity pen. The same class resolves identity correctly 1000 lines away — `hitTest()` and the selection-highlight pass both start with `lookupElementUUID(drawing, child)` — so the pointer knows which element a sofa symbol is while the paint does not. Census: 17 of 18 `*PlanSymbolBuilder` / bridge files stamp ZERO `userData.elementUUID` (only `OpeningElevationSymbolBuilder` does), and 4 of them (Column, RoofSlope, Stair, WallLayer) register no id at all, so they are neither pickable nor per-element governable. The native-mesh path is fine: EdgeProjectorService stamps both.",
      "evidence": [
        "packages/core-app-model/src/views/PlanViewCanvas.ts:472 — `const _elementId   = child.userData?.elementUUID as string | undefined;` (the render/pen path)",
        "packages/core-app-model/src/views/PlanViewCanvas.ts:2865 — `elementId: child.userData?.elementUUID as string | undefined,` (inside `_lineIsDrawn`, the §HIDDEN-IS-NOT-PICKABLE predicate — same gap)",
        "packages/core-app-model/src/views/PlanViewCanvas.ts:1476 — `lookupElementUUID(drawing as object, child)` (hitTest — the CORRECT resolution, used nowhere in render)",
        "packages/core-app-model/src/views/DrawingSelectionIndex.ts:47-52 — `let map = _index.get(drawing); … map.set(lineSegments, elementUUID);` — WeakMap only, no userData write",
        "packages/geometry-furniture/src/builders/SofaPlanSymbolBuilder.ts:134-136 — `const projected = projectToDrawingSpace(lineSegments, drawing);` / `drawing.addProjectionLines(projected, FURN_LAYER);` / `registerSegmentUUID(drawing, projected, sofa.id);`",
        "packages/core-app-model/src/BimWorld.ts:504-512 — `projectToDrawingSpace` is a bare wrapper over `OBC.TechnicalDrawing.toDrawingSpace(lines, drawing)`",
        "packages/core-app-model/src/presentation/IntentRuleResolver.ts:79 — `if (kind === 'element') return !!target.elementId && target.elementId === targetId;`",
        "apps/editor/src/engine/views/EdgeProjectorService.ts:3195 — `projected.userData.elementUUID = elementUUID;` (native path DOES stamp — this is why walls/bookshelves behave and sofas do not)",
        "Census (grep -c userData.elementUUID over 18 *SymbolBuilder*/*Bridge* files): OpeningElevationSymbolBuilder 1, all 17 others 0; registerSegmentUUID present in 13, absent in ColumnPlanSymbolBuilder, RoofSlopeSymbolBuilder, StairSymbolTechnicalDrawingBridge, WallLayerPlanSymbolBuilder"
      ],
      "userVisibleSymptom": "Hide one sofa (or one whole category) per-element in a plan and the swing arcs, sofa/bed/wardrobe/chair/kitchen/tree symbols keep drawing at full weight, while a bookshelf or desk on the same level — which has no plan symbol and therefore goes through native edge projection — correctly disappears. Two pieces of furniture, one command, opposite results.",
      "fix": "Make the render path resolve identity the way the hit-test path already does: `lookupElementUUID(drawing, child) ?? child.userData?.elementUUID ?? child.parent?.userData?.elementUUID`. Do it in ONE private helper on PlanViewCanvas and call it from render() (:472), `_lineIsDrawn` (:2865), hitTest (:1476) and the highlight pass (:2380) — the four sites are already four hand-copies of the same question and two of them have already drifted. `render()` does not currently hold the `drawing` reference at that point, so the helper needs it threaded in (it is available in the enclosing render scope). Separately, add `registerSegmentUUID` to the four builders that register nothing.",
      "effort": "small",
      "risk": "medium",
      "confidence": "high",
      "filesToChange": [
        "packages/core-app-model/src/views/PlanViewCanvas.ts",
        "packages/geometry-column/src/ColumnPlanSymbolBuilder.ts",
        "packages/geometry-roof/src/RoofSlopeSymbolBuilder.ts",
        "packages/geometry-stair/src/StairSymbolTechnicalDrawingBridge.ts",
        "packages/geometry-wall/src/WallLayerPlanSymbolBuilder.ts"
      ],
      "alreadyExists": "`lookupElementUUID` (packages/core-app-model/src/views/DrawingSelectionIndex.ts:67) is the solver — do not build a second index. 13 of 18 builders already populate it. `targetMatches` already handles element / elementType / category. Nothing new is needed on the resolver side at all; this is a one-line read that was written correctly twice and incorrectly twice in the same file."
    },
    {
      "title": "`NativeElementMeshExporter.exportForView()` filters by level and Y-range ONLY — no category, no visibility, no intent, and zero reads of `Object3D.visible` in the whole file",
      "mechanism": "The source element set for Source B (every PRYZM-authored element) is `levels.filter(l => (l.elevation + (l.height ?? 0)) >= minY && l.elevation <= maxY).flatMap(l => l.childrenIds)`. That is the entire filter. `grep -c '\\.visible'` over the 908-line file returns 0, and there is no `skipInPlan`, category, VG or intent read anywhere in it. So a category the user switched off is still fully exported, still proxied, still cached in the NME LRU, and still handed to the projector. The intent veto exists but lives one layer later, in EdgeProjectorService's group loop — which means the exporter's cost (the founder's `H2-NME-CACHE hits=0 misses=70`) is paid for hidden families regardless.",
      "evidence": [
        "packages/core-app-model/src/geometry/NativeElementMeshExporter.ts:322-324 — `elementIds = levels` / `.filter(l => (l.elevation + (l.height ?? 0)) >= minY && l.elevation <= maxY)` / `.flatMap(l => l.childrenIds);`",
        "packages/core-app-model/src/geometry/NativeElementMeshExporter.ts:325-328 — the exact log line the founder saw: `` `[NativeElementMeshExporter] Plan view — exporting ${elementIds.length} elements from levels overlapping Y=[…]` ``",
        "packages/core-app-model/src/geometry/NativeElementMeshExporter.ts — `grep -c '\\.visible'` → 0 (measured)",
        "apps/editor/src/engine/views/EdgeProjectorService.ts:2694-2702 — the veto is HERE, not in the exporter: `const _groupFamily = vgCategoryForLayer(resolveProjectionLayer(group.userData?.elementType …));` / `if (_groupFamily && !_symbolGate(_groupFamily)) { … continue; }`"
      ],
      "userVisibleSymptom": "Not directly visible as wrong linework (the projector's veto catches classified families downstream), but it is why hiding a category does not make the plan open faster: the mesh export, the proxy build and the LRU churn all still happen for it.",
      "fix": "Do not move the veto into the exporter blindly — the exporter is also the elevation/section/3D source and the veto key is view-scoped. The honest move is to pass the already-built `SymbolInjectionGate` (or the resolved bound intent) into `exportForView` and skip element ids whose family the gate refuses, keeping the projector-side veto as the belt-and-braces for unclassified types. Both would then fail open from the same resolver.",
      "effort": "medium",
      "risk": "medium",
      "confidence": "high",
      "filesToChange": [
        "packages/core-app-model/src/geometry/NativeElementMeshExporter.ts",
        "apps/editor/src/engine/views/EdgeProjectorService.ts"
      ],
      "alreadyExists": "`makeSymbolInjectionGate` already resolves the bound intent once and memoises per family; it is already constructed in EdgeProjectorService BEFORE the exporter's output is consumed. Pass it, do not rebuild it."
    },
    {
      "title": "`VGSceneApplicator.applyToProjectionLayers()` can hide, but its hide is INERT on the plan canvas — it sets `Object3D.visible`, which PlanViewCanvas never reads",
      "mechanism": "`applyToProjectionLayers` calls `drawing.layers.setVisibility(layerName, style.visible)` for each of 13 categories plus their `:cut`/`:proj`/`:beyond` sub-layers. OBC's implementation walks the drawing container and sets `child.visible = visible` on every line carrying that layer name. But `PlanViewCanvas.render()` paints by `drawing.three.traverse(child => …)` — `Object3D.traverse` visits hidden children too — and the file contains no object-visibility read at all (all 9 `.visible` hits are `resolved.visible` from the style resolver, `intent.visible`, `ref.visible` or comments). The category hide that DOES work on screen is the canvas's own independent call to `_styleResolver` → `resolveVgCanvasStyle` → `vgGovernanceStore.resolveStyle`. Two code paths, same store, one of them dead. And note the authority: `resolveVgCanvasStyle` reads the LEGACY VG governance store, not the visibility intent — so the canvas's category hide and the projector's family veto answer to two different authorities.",
      "evidence": [
        "packages/core-app-model/src/presentation/VGSceneApplicator.ts:634 — `drawing.layers.setVisibility(layerName, style.visible);`",
        "node_modules/@thatopen/components/dist/index.mjs:24873-24882 — `setVisibility(name, visible) { … layer.visible = visible; this._container.traverse((child) => { if (child.userData.layer === name) child.visible = visible; }); }`",
        "packages/core-app-model/src/views/PlanViewCanvas.ts:430-433 — `(drawing as any).three?.traverse?.((child: THREE.Object3D) => { if (!(child instanceof THREE.LineSegments)) return; …` — no `child.visible` test",
        "packages/core-app-model/src/views/PlanViewCanvas.ts — every `.visible` occurrence: 463, 1390, 2027, 2505, 2580, 2616, 2628, 2825, 2859 — none is an Object3D visibility read",
        "packages/core-app-model/src/presentation/VgCanvasStyleResolver.ts:102 — `const { style, overriddenProps } = vgGovernanceStore.resolveStyle(modelId, category, viewId);` — the canvas's category hide comes from the VG store, not the intent"
      ],
      "userVisibleSymptom": "Invisible on its own, but it is a permanent trap: anyone who \"fixes\" a visibility bug by calling `setVisibility` on a drawing layer will write a change that provably does nothing on screen and appears to work in the DXF/SVG exporter. It also means the log line the founder sees (`styledLayers=22 … across 14 VG CATEGORIES`) reports work that has no bearing on what he is looking at.",
      "fix": "Either make `PlanViewCanvas.render()` honour `child.visible` (one `if (!child.visible) return;` at the top of the traverse — cheap, and makes every `setVisibility` caller real, including OBC's own `assign`), or delete the `setVisibility` calls from `applyToProjectionLayers` and document that the canvas resolves category visibility itself. Do NOT do both halves independently. Check `packages/file-format/src/export/sheets/SVGCompositeRenderer.ts` first — if the exporter reads layer visibility, deleting the calls would regress export.",
      "effort": "small",
      "risk": "medium",
      "confidence": "high",
      "filesToChange": [
        "packages/core-app-model/src/views/PlanViewCanvas.ts",
        "packages/core-app-model/src/presentation/VGSceneApplicator.ts"
      ],
      "alreadyExists": "`_lineIsDrawn` (PlanViewCanvas.ts:2853) is already the single \"is this line on screen?\" predicate shared by render and hitTest — a `child.visible` arm belongs there, not copied into the traverse."
    },
    {
      "title": "ROOT ARCHITECTURAL FINDING — two hiding mechanisms with no shared authority: 3D honours `Object3D.visible` + the VG store and IGNORES the intent's `visible`; 2D honours the intent + the VG store and IGNORES `Object3D.visible`",
      "mechanism": "In 3D, an element is hidden by (i) the scene traverses in `ProjectVisibilitySection` writing `obj.visible = visible` directly, and (ii) `VGSceneApplicator.applyToMesh` doing `mesh.visible = style.visible` from the VG governance store. The visibility INTENT reaches 3D only through `threeDAppearanceResolver.resolveForView(...)`, which returns a `surface3D` descriptor — colour, opacity, metalness, roughness. There is no path by which an intent's `visible: false` hides a mesh in 3D. In 2D, the reverse: the intent reaches the drawing three times (projector family veto, symbol-injector gate, canvas pen), the VG store reaches it once (canvas `_styleResolver`), and `Object3D.visible` reaches it NOWHERE on the native path — the only `mesh.visible` read in EdgeProjectorService is in Source C, the IFC-scene-group branch. So an IFC-imported chair honours the browser toggle in plan and a PRYZM-authored chair does not, in the same drawing. The user has no way to know which of five filters he is operating.",
      "evidence": [
        "packages/core-app-model/src/presentation/VGSceneApplicator.ts:914 — `mesh.visible = style.visible;` (VG store → 3D)",
        "packages/core-app-model/src/presentation/VGSceneApplicator.ts:1088-1104 — `const descriptor = threeDAppearanceResolver.resolveForView(this.activeViewId, elementType, 'projection');` / `threeDAppearanceResolver.applyToMaterial(live, descriptor);` — the intent's ONLY reach into 3D, and it is material fields only",
        "apps/editor/src/ui/ViewBrowser/panels/unified-browser/ProjectVisibilitySection.ts:167-168 — `scene.traverse((obj: any) => { if (obj.userData?.id !== elemId) return; obj.visible = visible;` (raw scene write)",
        "apps/editor/src/engine/views/EdgeProjectorService.ts:3743 — `if (!mesh.visible) return;` — the ONE place the 2D pipeline reads Object3D.visible, and it is inside `if (ifcSceneGroups.length > 0)` (Source C)",
        "apps/editor/src/ui/ViewBrowser/panels/unified-browser/ProjectVisibilitySection.ts:22-40 — the header already states the diagnosis: \"every traverse below writes `Object3D.visible`, which ONLY the 3D viewport reads … `EdgeProjectorService` Source B … contains ZERO reads of `Object3D.visible`. So \\\"the filter does not work in plan\\\" was never a rendering bug: it was UNSATISFIABLE.\" — and then explicitly declines to remove them: \"STEP 1 writes intent IN ADDITION TO the existing scene write; it does NOT delete the traverses.\"",
        "apps/editor/src/ui/OverridePanel.ts:210-213 — a SECOND, different UI for the same user goal, writing `targetKind: 'elementType'` (which DOES work end-to-end), with its own curated 13-category list at :247-268"
      ],
      "userVisibleSymptom": "\"I exclude the furniture elements from view but they still render\" — and, symmetrically, if he instead switches furniture off in the Intent editor, the plan goes clean and the 3D view keeps showing it. Two panels, two element-visibility models, and the panel that reads most like \"the elements list\" is the one whose hides do not reach the drawings.",
      "fix": "This needs a decision, not a patch: name ONE authority for \"is this element visible in this view\" and make every surface a writer to it, with the other layers as pure readers. The intent override layer is the only candidate that is per-view, persisted, undoable and already read by 2D — so the direction is: browser toggles write intent (they partly do), the 3D applicator gains an arm that READS the intent's `visible` and `visibilityOverrides` (it currently reads only `surface3D`), and the raw `Object3D.visible` traverses become the derived output of that read rather than a parallel authority. C09 §4.7.5 already declares this ordering BINDING — the 3D read arm is the step that was deferred and never landed. Until it lands, at minimum make the two UIs stop looking like the same control.",
      "effort": "large",
      "risk": "high",
      "confidence": "high",
      "filesToChange": [
        "packages/core-app-model/src/presentation/VGSceneApplicator.ts",
        "packages/core-app-model/src/presentation/ThreeDAppearanceResolver.ts",
        "apps/editor/src/ui/ViewBrowser/panels/unified-browser/ProjectVisibilitySection.ts",
        "apps/editor/src/ui/OverridePanel.ts"
      ],
      "alreadyExists": "`resolveIntentStyle` already returns `visible: false` for hide/isolate at all three target kinds — the 3D side needs to CALL it, not reimplement it. `resolveBoundIntentWithInheritance` is already the shared binding resolver used by the projector, the pen chain and ThreeDAppearanceResolver. `IsolateElementInViewCommand` / `GhostElementInViewCommand` / `HideElementInViewCommand` / `ClearOverrideCommand` / `SetCategoryVisibilityInViewCommand` are all written and wired."
    },
    {
      "title": "Lighting fixtures are STRUCTURALLY ungovernable in plan — `Lighting` is absent from the layer map, so the family key is null and the intent veto's own guard skips them; and nothing marks them `skipInPlan`, so the 672-face fixture mesh edge-dumps as the black concentric bursts in his screenshot",
      "mechanism": "`LightingFragmentBuilder` stamps `elementType: 'Lighting'`. `ELEMENT_TYPE_TO_PROJECTION_LAYER` has no Lighting row, so `resolveProjectionLayer` returns `FALLBACK_NATIVE_LAYER` = `'projection-visible'`. `vgCategoryForLayer('projection-visible')` matches no ISO prefix and returns `null`. The native intent veto is written `if (_groupFamily && !_symbolGate(_groupFamily))` — a null family SKIPS the veto entirely, so lighting is projected unconditionally. On the canvas the same null category means `_styleResolver` is never consulted (no VG hide) and `penCategoryForLayerTag` falls through `categoryFromFlags` to its `'projection'` default, so the intent has no lighting rule to apply either (`VisibilityIntentDefaults.ELEMENT_TYPES` has no 'lighting' entry). Nothing in `packages/geometry-lighting/` sets `skipInPlan`, so the full LOD mesh reaches `EdgesGeometry` — matching the founder's `DIAG-EPS-01 … repeated Lighting groups at 672 faces each` and the dense black concentric line bursts on his plan. Meanwhile a real 2D lighting symbol DOES exist and is painted straight onto the canvas from the store, bypassing the drawing and every filter — `renderLightingSymbols(ctx, ppu, …, { levelId, selectedId })` takes no viewId, no intent, no VG. So he is getting BOTH a cyan authored symbol and a black mesh dump, and can govern neither.",
      "evidence": [
        "packages/geometry-lighting/src/LightingFragmentBuilder.ts:726 — `elementType: { value: 'Lighting', writable: false, configurable: false, enumerable: true },`",
        "apps/editor/src/engine/views/EdgeProjectorService.ts:203-241 — `ELEMENT_TYPE_TO_PROJECTION_LAYER` — no Lighting / lighting key anywhere in the map",
        "apps/editor/src/engine/views/EdgeProjectorService.ts:242 — `const FALLBACK_NATIVE_LAYER = 'projection-visible';` and :328-331 `return _CANONICAL_TYPE_TO_LAYER.get(_canonicalTypeKey(elementType)) ?? FALLBACK_NATIVE_LAYER;`",
        "apps/editor/src/engine/views/EdgeProjectorService.ts:2697 — `if (_groupFamily && !_symbolGate(_groupFamily)) {` — the `&&` is the bypass: null family ⇒ no veto",
        "packages/core-app-model/src/drawing/PenWeightTable.ts:408 — `return 'projection'; // safe fallback — generic projected geometry`",
        "packages/core-app-model/src/presentation/VisibilityIntentDefaults.ts:15-41 — `ELEMENT_TYPES` list: no 'lighting' (and no 'room')",
        "packages/geometry-lighting/ — `grep -rn skipInPlan` → no matches",
        "apps/editor/src/engine/views/plan-canvas/PlanViewSymbolRenderer.ts:21-25 — `renderLightingSymbols(ctx, ppu, (wx, wz) => worldToScreen(wx, wz), { levelId, selectedId });` — no viewId, no intent, no VG",
        "packages/core-app-model/src/views/symbols/LightingPlanSymbolRenderer.ts:13-16 — `const STROKE_DEFAULT = '#0ea5e9';` — the authored symbol is cyan, so the BLACK bursts are the mesh dump, not the symbol"
      ],
      "userVisibleSymptom": "Dense black concentric line bursts all over the Level 2 plan, unreadable at 1:100, with no category anywhere in the visibility-intent panel that turns them off — which is the founder's \"the quality atm is really bad, the lines are almost not readable\" and his \"lighting fixtures definitely should have a symbol for each\" in one defect.",
      "fix": "Four one-line rows and one flag, all in existing tables: add `Lighting: 'E-LITE'` (or `A-LITE`) to ELEMENT_TYPE_TO_PROJECTION_LAYER; add `'E-LITE': 'lighting'` to ISO_LAYER_TO_VG_CATEGORY; add `'lighting'` to VisibilityIntentDefaults.ELEMENT_TYPES; add an `isLighting` arm to categoryFromFlags/penCategoryForLayerTag. Then set `skipInPlan: true` on the fixture meshes so the existing authored 2D symbol is the only thing drawn — the same Contract-48 §5 pattern doors, sofas, beds, wardrobes, chairs, kitchens and trees already use. Separately, the `if (_groupFamily && …)` guard should be reconsidered: an UNCLASSIFIED family silently escaping every filter is the general bug, of which lighting is one instance.",
      "effort": "medium",
      "risk": "medium",
      "confidence": "high",
      "filesToChange": [
        "apps/editor/src/engine/views/EdgeProjectorService.ts",
        "packages/core-app-model/src/drawing/DrawingLayerIdentity.ts",
        "packages/core-app-model/src/drawing/PenWeightTable.ts",
        "packages/core-app-model/src/presentation/VisibilityIntentDefaults.ts",
        "packages/core-app-model/src/presentation/VGSceneApplicator.ts",
        "packages/geometry-lighting/src/LightingFragmentBuilder.ts"
      ],
      "alreadyExists": "The 2D lighting symbol is ALREADY BUILT and already drawing — `LightingPlanSymbolRenderer.drawLightingSymbol` covers downlight, pendant, linear_led, pendant_pebble, pendant_ceramic_bell, pendant_conical, floor_wood_post, floor_arc_brass, table_terracotta, floor_tripod_black plus a default for ~20 LOD-200 families, with `lightingSymbolExtentPx` as the shared drawer/hit-test extent authority (L-10081). Do NOT write a LightingPlanSymbolBuilder from scratch; the drawing exists, it just needs the mesh silenced and a layer identity so it can be governed."
    },
    {
      "title": "The regression suite that certifies \"the browser filter reaches projected views\" stubs a store method that does not exist in production",
      "mechanism": "`elementFilterWritesIntent.spec.ts` replaces `window.viewDefinitionStore` with `{ getActiveId: () => PLAN_VIEW }`. That satisfies arm 1 of `_activeViewId()`, which in production is always undefined. The test therefore exercises a code path the app can never take, and never exercises the `viewController.currentViewDefinitionId` fallback that production always uses. Its one negative test sets `getActiveId: () => null` AND `viewController = undefined` together, so \"3D active, viewController present, currentViewDefinitionId null\" — the actual failing state — is not covered by any case. This is the §FAKE-MORE-CAPABLE-THAN-REAL shape verbatim: a fake built to the header cannot falsify the header.",
      "evidence": [
        "apps/editor/src/ui/ViewBrowser/panels/unified-browser/__tests__/elementFilterWritesIntent.spec.ts:72-74 — `(window as unknown as Record<string, unknown>).viewDefinitionStore = { getActiveId: () => PLAN_VIEW, };`",
        "apps/editor/src/ui/ViewBrowser/panels/unified-browser/__tests__/elementFilterWritesIntent.spec.ts:169-171 and :174-176 — the view-switch test swaps the same fake method",
        "apps/editor/src/ui/ViewBrowser/panels/unified-browser/__tests__/elementFilterWritesIntent.spec.ts:180-184 — `getActiveId: () => null` together with `viewController = undefined` — the two arms are never varied independently",
        "packages/core-app-model/src/views/ViewDefinitionStore.ts — no `getActiveId` member exists on the class"
      ],
      "userVisibleSymptom": "None directly — but it is why finding 1 shipped green. The suite reports the browser filter reaches projected views; in the founder's session it reached nothing.",
      "fix": "Point the spec at the real singleton (`import { viewDefinitionStore }`) and drive the active view through whatever production actually uses, then add the missing case: viewController present with `currentViewDefinitionId === null` (the 3D state) must either write to a named view or refuse loudly — assert which, do not leave it silent.",
      "effort": "small",
      "risk": "low",
      "confidence": "high",
      "filesToChange": [
        "apps/editor/src/ui/ViewBrowser/panels/unified-browser/__tests__/elementFilterWritesIntent.spec.ts"
      ],
      "alreadyExists": "The spec's `projectedPen()` harness already drives the REAL `graphicsRulesEngine` end-to-end and its positive control is sound — only the active-view fake needs replacing."
    }
  ],
  "whatIcouldNotEstablish": "WHICH UI THE FOUNDER ACTUALLY USED. This is the one thing that decides whether finding 1 or finding 2 is his headline defect, and I cannot settle it from the repo. Three surfaces can \"exclude furniture from a view\", and they behave differently:\n  • Overrides panel → `view.setCategoryVisibility` with `targetKind: 'elementType', targetId: 'furniture'` → reaches the symbol gate, the native veto AND the canvas pen. This path WORKS end-to-end as far as I can trace it.\n  • Project Browser category checkbox → N per-element `view.hideElement` calls → broken two ways (finding 1 when 3D is active; finding 2 for symbol linework even when it does write).\n  • Intent editor per-state VISIBLE checkbox → edits the shared intent → works in 2D, does nothing in 3D.\nHis screenshot's \"NO OVERRIDES\" is evidence AGAINST the Overrides panel and consistent with finding 1 (nothing written at all), but it is also consistent with him having toggled in a view other than the one he screenshotted. A single line of instrumentation — log `viewId` inside `writeElementVisibilityIntent` before the guard — would settle it in one session, and I would ship that probe before any fix.\n\nI DID NOT RUN ANYTHING. No test, no build, no browser. Every claim here is read from source at the cited line. In particular I have not observed the founder's session state, so I cannot confirm his furniture is non-instanced — if any of it is aggregated (`isInstancedGroup`), the 3D traverses miss it too (ProjectVisibilitySection.ts:82-90 handles instanced groups only in the isolate/reset paths, not in `applyElementVisibility`), which would be a third, separate mechanism I have not measured.\n\nRIVALS I RULED OUT, and how:\n  • \"The symbol injectors ignore visibility intent entirely\" (the founder's hypothesis, and what `SymbolInjectionGate.ts`'s own header census of 0-of-15 would suggest). REFUTED: the gate is wired at 19 call sites in EdgeProjectorService (:3941–:4129), including six `_symbolGate('furniture')` guards. The builders are dumb; the caller is not.\n  • \"The worker pipeline draws the furniture with a different, ungated pen.\" RULED OUT: `scheduleWorkerRender` and `renderFromPipelineResult` have no callers anywhere in the repo — the code says so itself at PlanViewCanvas.ts:1971. The path is dead. (It would have been a real finding if live: `stage6_styleResolver` resolves through the worker's own mini rules engine with no intent tier at all.)\n  • \"The drawing is cached, so the toggle needs a re-projection that never happens.\" RULED OUT as the mechanism for hiding: `PlanViewManager._onIntentInstanceUpdatedCore` sets `this._lastRender = 0`, forcing a repaint, and the canvas re-resolves the pen per line on every paint. It does NOT re-project, so an already-injected symbol stays in the drawing — but it would be painted at opacity 0. Cost, not correctness.\n  • \"`applyToProjectionLayers` is the hide and it is failing.\" PARTLY: it genuinely calls setVisibility, but that write is inert on the canvas (finding 4). The working category hide is the canvas's own `_styleResolver`.\n\nNOT CHECKED, and someone should: whether `packages/file-format/src/export/sheets/SVGCompositeRenderer.ts` reads drawing-layer visibility (this decides the fix direction for finding 4); whether the elevation/section paths have the same symbol-identity gap as plan (TreeElevationSymbolBuilder and PlumbingElevationSymbolBuilder are in the same 0-uuidStamp census, so probably yes, but I traced only the plan canvas); and whether `HiddenLineRemoval`'s occluder set includes families the intent has hidden — the founder's log shows 26 occluders and 239.5 segment-equivalents removed, and if a hidden family is still occluding, that is a fourth bypass I did not measure.\n\nADJACENT TO MY DIMENSION, CONFIRMED IN PASSING (for whoever owns those lanes): `VisibilityIntentDefaults.ELEMENT_TYPES` (:15-41) has no `'room'` entry, and room fills are painted straight from `window.roomStore` in `PlanViewCanvas._renderRoomFills` (:2488) governed only by `resolveRoomColourIntent` — so the founder is exactly right that rooms have no category in the visibility-intent settings. `VGSceneApplicator.ELEMENT_TYPE_TO_VG_CATEGORY` DOES map `'room'` (:143), and `CATEGORY_TO_DXF_LAYER` does not — so rooms are governable in 3D and not in 2D, the same asymmetry as finding 5."
}