{
  "refuted": false,
  "reason": "CONFIRMED on every load-bearing edge, and the author's own worst-case falsifier resolves in their favour. I actively built and killed five rivals.\n\nVERIFIED (each read, not grepped-and-assumed):\n1. `SpaceEnvelopeFaceRef` is the closed union claimed (SpaceEnvelopeTypes.ts:23-26); `spaceEnvelopeFaceAxis` gives top +Y / bottom −Y / side y:0 (SpaceEnvelopeFaceDrag.ts:74-78).\n2. `spaceEnvelopeFaces()` returns n+2 including both caps (SpaceEnvelopeGeometry.ts:144-151), and `spaceEnvelopeFaceHandles` iterates it UNFILTERED. `SpaceEnvelopeFaceHandleOptions` (Gizmo.ts:147-152) has exactly four fields, none a face filter. BOTH surfaces call it bare: `SpaceEnvelopeFaceGizmoBuilder._handlesOf` → `spaceEnvelopeFaceHandles(prism)`, and `siteEnvelopeDrawCesium.paintHandleEntities:891` → `spaceEnvelopeFaceHandles(prismOfSpaceEnvelopeRecord(target))`. Cap arrows are drawn on both.\n3. Both picks return caps — `pickSpaceEnvelopeFace` has an explicit cap loop (FacePick.ts:205-218); Cesium routes through `pickNearestSpaceEnvelopeFace` (pure geometry, so omitting cap ARROWS there would not make caps unpickable — the claim says exactly this); THREE picks mesh-builder cap faces (`MeshBuilder.ts:475`) plus gizmo arrows.\n4. `planSpaceEnvelopeFaceMove` implements cap moves in full: top → `height + δ`, bottom → `baseOffset − δ` AND `height + δ` (FaceMove.ts:216-244).\n5. NO GATE. `onPointerDown` (dragSurface.ts:583-614) has exactly two refusals — role, and degenerate axis. A repo-wide sweep of every `face.kind` branch outside tests returns 16 hits: all are geometry, naming, or `SpaceEnvelopeSolar.ts:238` filtering caps out of the SOLAR facade set. None is a gesture gate.\n6. `adaptRoomToMovedLevel` really does drive cap moves through the planner (Context.ts:343 `{kind:'top'}`, :355 `{kind:'bottom'}`) — the \"do not gate in the planner\" warning is correct.\n7. `packages/command-registry/src` → ZERO `SpaceEnvelope` hits. Only `level.add`/`level.update` bus verbs exist (commands.ts:975,978); `level.update` → `UpdateLevelCommand`, whose own doc confirms it writes height and cascades nothing. `SetLevelHeightCommand` is reachable only from `LevelManagerPanel:196` and `PropertyPanel:1277`, both legacy `commandManager`.\n8. `CesiumViewport:8889-8890` reads `rec.height`/`rec.baseOffset` only. Staleness is visible, not cosmetic.\n9. `setParameter` refuses via `containmentRefusalFor` (MutateSpaceEnvelope.ts:409-412) while `setFootprint`/`moveFace` adapt via `containmentOutcomeFor` (:307,:323 + containmentGate.ts:141-186). The asymmetry is real.\n10. ZERO production callers of `spaceEnvelope.setParameter` anywhere; no `bim-level-updated` consumer touches envelopes.\n\nTHE AUTHOR'S FALSIFIER #3 — the gap they flagged as \"the main gap in this investigation\" — I ran it, and it CONFIRMS them. `apps/editor/src/ui/inspect/resolveLevelAuthority.ts` and `apps/editor/__tests__/levelAuthorityDivergence.test.ts` (MT-07 / ADR-0327) establish that `runtime.levelStore` (`packages/stores/src/LevelStore.ts`) is PERMANENTLY EMPTY in production — its only writers are the four aggregate commands `levelCreate/levelUpdate/levelSetActive/levelDelete`, which have zero production callers — and that the live authority is `window.bimManager` (`initScene.ts:757`, seeded `BimKernel.ts:167`). So the card's `readLevels()` and `SetLevelHeightCommand`'s `bimManager.updateLevel` ARE one collection. Seam A stands; this is not a store-reconciliation problem.\n\nRIVALS CONSTRUCTED AND KILLED:\n· \"The envelope has no level binding, so nothing can key a follow\" — dead. `SpaceEnvelope.levelId` exists (schema:174), `CreateSpaceEnvelopeBatch` REQUIRES it non-empty (:138-142) and persists it (:286).\n· \"Reload re-seats envelopes from levels, so staleness is transient\" — dead. `restoreCompoundFamilies.ts:510-560` applies the serialized record verbatim through the undo adapter and DELIBERATELY does not re-dispatch create.\n· \"Some gate exists that the greps missed\" — dead, see 5.\n· \"The renderer re-derives from the level\" — dead, see 8.\n· The one rival with teeth: \"the root cause is a denormalized cache of level geometry on the envelope; remove the stored extent rather than add a synchroniser.\" The repo already rejected this WITH reasons (envelopeAuthoringPlan.ts:196-212: four rasterisers implement the identity transform, and a half-migration makes the grab land on nothing — §L-430 / L-10740), and `role:'room'` / `role:'maximumBuildable'` envelopes have extents no level owns. Not better.\n\nDEFECTS IN THE CLAIM (refinements, not refutations — but they will bite the implementer): see betterHypothesis.",
  "evidenceChecked": [
    "packages/geometry-space-envelope/src/SpaceEnvelopeTypes.ts:1-60 — closed union verbatim as cited",
    "packages/geometry-space-envelope/src/SpaceEnvelopeGeometry.ts:144-151 — spaceEnvelopeFaces returns n+2 incl. both caps",
    "packages/geometry-space-envelope/src/SpaceEnvelopeFaceGizmo.ts:147-152, 252-281 — SpaceEnvelopeFaceHandleOptions has NO face filter; handles loop is unfiltered",
    "packages/geometry-space-envelope/src/SpaceEnvelopeFaceDrag.ts:74-78 — axis is ±Y for caps, y:0 for sides",
    "packages/geometry-space-envelope/src/SpaceEnvelopeFacePick.ts:133-218 — explicit TOP/BOTTOM cap intersection arm; caps ARE pickable",
    "packages/geometry-space-envelope/src/SpaceEnvelopeFaceMove.ts:216-244 — cap moves fully implemented (top→height, bottom→baseOffset−δ AND height+δ)",
    "packages/geometry-space-envelope/src/SpaceEnvelopeContext.ts:295-365 — adaptRoomToMovedLevel drives {kind:'top'} at :343 and {kind:'bottom'} at :355 through planSpaceEnvelopeFaceMove",
    "packages/geometry-space-envelope/src/SpaceEnvelopeSolar.ts:238 — the ONLY cap filter in the repo, and it is the solar facade set, not a gesture gate",
    "apps/editor/src/engine/spaceEnvelopeDragSurface.ts:560-700 — onPointerDown has exactly two refusals (role, degenerate axis); no face-kind gate; onRefusal port declared :425",
    "apps/editor/src/engine/spaceEnvelopeDragSurfaceThree.ts:112-135 — THREE pickFace resolves mesh cap faces + gizmo arrows by userData",
    "apps/editor/src/ui/site/siteEnvelopeDrawCesium.ts:735-757, 873-895 — Cesium pickFace = pure ray/prism (caps included); paintHandleEntities calls spaceEnvelopeFaceHandles with NO options",
    "apps/editor/src/engine/SpaceEnvelopeFaceGizmoBuilder.ts:220-230 — _handlesOf calls spaceEnvelopeFaceHandles(prism) bare",
    "apps/editor/src/engine/SpaceEnvelopeMeshBuilder.ts:218, 452, 475 — cap face meshes exist and carry face userData",
    "grep -rin 'spaceenvelope' packages/command-registry/src → 0 hits (confirms the omission is total)",
    "packages/command-registry/src/levels/SetLevelHeightCommand.ts:1-160, 240-340 — header table omits SpaceEnvelope; execute writes bimManager.updateLevel and emits bim-level-updated",
    "packages/command-registry/src/levels/UpdateLevelCommand.ts:27, 65-66, 90 — accepts updates.height, validates it, emits bim-level-updated, cascades nothing",
    "apps/editor/src/engine/initBusHandlers.ts:2352-2373 — the ONLY level bridges are level.update and level.add; both via _cmExec",
    "packages/command-bus/src/commands.ts:975,978 — only 'level.add' and 'level.createMultiple' declared; no level.setHeight",
    "grep SetLevelHeightCommand repo-wide → only LevelManagerPanel.ts:196 and PropertyPanel.ts:1277 dispatch it, both legacy commandManager",
    "apps/editor/src/ui/site/envelopeAuthoringPlan.ts:634-660 — the single create-time seed (baseOffset: level.elevation, height: heightM, levelId: level.id)",
    "apps/editor/src/ui/site/envelopeAuthoringPlan.ts:185-235 — the baseOffset ABSOLUTE convention and the recorded rejection of the derived-extent alternative",
    "apps/editor/src/ui/site/adoptProposalAsEnvelope.ts:161-215 — resolveStoreyHeight is a THREE-RUNG ladder; readLevelCandidates normalises a missing height to null",
    "packages/core-app-model/src/BimKernel.ts:167 — the seeded Ground level carries NO height field",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:8875-8900 — bottom = baseHeight + baseOffset, top = bottom + height; no level read",
    "plugins/space-envelope/src/handlers/MutateSpaceEnvelope.ts:284-330 (setFootprint→containmentOutcomeFor) vs :384-415 (setParameter→containmentRefusalFor) — the asymmetry is real",
    "plugins/space-envelope/src/handlers/containmentGate.ts:110-186 — containmentOutcomeFor adapts rooms for a LEVEL candidate; header names setParameter as still REFUSING",
    "plugins/space-envelope/src/handlers/CreateSpaceEnvelopeBatch.ts:73, 138-142, 286 — levelId is REQUIRED non-empty and persisted",
    "apps/editor/src/engine/persistence/restoreCompoundFamilies.ts:498-560 — restore applies the serialized record verbatim; no level re-derivation",
    "apps/editor/src/ui/inspect/resolveLevelAuthority.ts (whole file) — runtime.levelStore has ZERO production writers; window.bimManager is the live authority (ADR-0327 §Decision 2). THIS SETTLES FALSIFIER #3 IN THE CLAIM'S FAVOUR",
    "apps/editor/__tests__/levelAuthorityDivergence.test.ts:1-90 — the same finding pinned at the rendered DOM",
    "apps/editor/src/engine/geometryMutationEvents.ts:98-105 + grep bim-level-updated repo-wide → consumers are geometryMutationEvents, FloorSlabBindingHandler, and the two emitters; NO envelope consumer (falsifier #1 passes)",
    "grep 'spaceEnvelope.setParameter' across apps/plugins/packages → ZERO production callers",
    "apps/editor/src/ui/analysis/parcelLawEnvelopeAuthoring.ts:418, 646, 779-791, 1455-1480 — readLevels via bimManager; ONE numeric input (storey COUNT); addLevels dispatches level.add awaited one at a time",
    "apps/editor/src/ui/room-programme/roomEnvelopePlan.ts:198, 236-237 — every ROOM envelope copies the level envelope's baseOffset AND height",
    "apps/editor/src/ui/__tests__/oneRuleForTheNextStorey.spec.ts + ProjectTreeSection.ts:160-200 — L-13151/L-13146 context: a whole-building 15.3 m reached one storey's Level.height and resolveStoreyHeight minted an envelope that tall",
    "grep 'spaceEnvelope' packages/command-bus/src/*.ts → 0; plugin verbs are registered as `readonly type` handlers, a DIFFERENT registry from commands.ts"
  ],
  "betterHypothesis": "The mechanism stands; three corrections change the fix, and one of them changes the premise.\n\n(1) PREMISE CORRECTION — \"seeded from the level\" is only the FIRST RUNG of a three-rung ladder, and on a fresh project it is not the rung taken. `resolveStoreyHeight` (adoptProposalAsEnvelope.ts:161-193) uses `level.height` only when it is a positive number; otherwise it derives `maxHeightM / maxFloors` (requires maxFloors ≥ 2) or falls to an ASSUMED 3.0 m. `BimKernel`'s constructor seeds Ground as `{id:'L0', name:'Ground', elevation:0}` with NO height (BimKernel.ts:167), and `readLevelCandidates` normalises a missing height to `null`. So the envelope and the level record are out of sync AT CREATION, before any level edit — the defect is not only \"never re-derived\", it is \"never agreed in the first place\". This breaks proposed step B verbatim: a height input \"seeded from `level.height`\" renders BLANK on exactly the storey whose envelope is 3.0 m tall. The input must be seeded from the ladder's `heightM` and carry its `heightSource`, and the plan must decide explicitly what committing an assumed 3.0 does to a level that has no recorded height (write it back, or leave the level heightless forever — silently picking either is the [[context-data-honesty]] failure).\n\n(2) \"`level.height` has ONE owner already — SetLevelHeightCommand. Do not mint a second\" is FALSE AS STATED. It has at least three live writers: `AddLevelCommand` via `level.add` (which carries `height`, dispatched by four add-level controls — ProjectTreeSection.ts:200 among them), `UpdateLevelCommand` (`updates.height`, declared at :27 and validated at :65-66, reachable from the `level.update` bridge), and `SetLevelHeightCommand`. Section D says \"do not route level height through level.update\" — but `UpdateLevelCommand` already accepts it today, so the rival writer is live, not hypothetical. The plan must say what happens to that arm (reject `height` in the bridge? in the command?), not merely avoid using it.\n\n(3) THE REQUIREMENT-2 GATE IS ROLE-BLIND AND OVER-REACHES TO ROOMS. `if (face.kind !== 'side')` and an unconditional `omitCapFaces: true` at both gizmo call sites also pin every ROOM envelope's caps. There is a case for it — `roomEnvelopePlan.ts:198,236-237` states \"EVERY ROOM TAKES THE LEVEL'S OWN baseOffset AND height\" — but it is a case that has to be MADE, and the drafted refusal sentence (\"pinned to its storey — set by the level height\") is written for a level envelope and is wrong prose on a room. Gate on `record.role === 'level'` (the record is already in hand two lines above, at dragSurface.ts:602) and give the room case its own sentence or its own decision. Note `maximumBuildable` never reaches the new gate only because the existing role refusal returns first — that is ordering, not design; say so.\n\n(4) Minor but load-bearing for step A: the plan declares `level.setHeight` in `packages/command-bus/src/commands.ts` while every `spaceEnvelope.*` verb it also touches lives in a DIFFERENT registry (plugin handlers with `readonly type`, zero hits in commands.ts). Two registries, one plan, no sentence distinguishing them.\n\n(5) One gap the claim left open that I closed in its favour: `deps.onRefusal` IS wired on both surfaces (initTools.ts:2138 and :2185, forwarded conditionally at attachSpaceEnvelopeRender.ts:239), so the proposed refusal seam is live and will be seen, not swallowed."
}