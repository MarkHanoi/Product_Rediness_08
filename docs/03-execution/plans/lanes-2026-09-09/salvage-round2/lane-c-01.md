{
  "dimension": "Duplicated work on open",
  "summary": "Of the three duplicates in the founder's log, only two are real duplicated work; the third is a duplicated LOG LINE over a single event. (1) The socket join is genuinely two independent socket.io clients — `PlatformCollabPill.initSocketCollaboration()` fired from `setProjectContext`, and `initCollaboration.connectSocket()` fired later from `pryzm-project-loaded` — each opening its own websocket and each emitting `join-project`, which costs a second server-side `canUserAccessProject` DB round-trip and leaves two sockets in the room for the session. (2) `captureThumbnail` runs twice from two uncoordinated schedulers (the auto-save path and the post-load path); each one forces a synchronous full-scene render, a canvas readback and a WebP encode ladder, and one of the two sits on the open critical path. (3) The two \"WebGPU device lost: reason=destroyed\" lines are TWO HANDLERS ON ONE DEVICE — this is the L-1001 fix working; both log verbatim and both early-return through `isDeliberateDeviceDestroy()`, so no recovery pipeline runs twice. The device destroy itself is real and is the DESIGNED heavy-scene backend swap (`retireRenderer(oldRenderer)` at `initScene.ts:5126`), not an accidental extra reset — but it is triggered REACTIVELY from the per-add tier pass mid-hydration, so the scene is uploaded to WebGPU and then thrown away and re-minted on a classic WebGL renderer while the project is still loading. Separately, I found a fourth duplicate the log does not name: the open path performs TWO full-project serializes (503 elements × ~30 stores with deepStrip/structuredClone) — one for the `resetDirtyAfterLoad()` hash baseline that is then discarded, one for the auto-save's own dirty-check.",
  "findings": [
    {
      "title": "The heavy-scene backend swap fires MID-HYDRATION, so every GPU resource is minted on WebGPU and then re-minted on a classic WebGL renderer — this is the sole source of the 'device lost: destroyed' pair",
      "mechanism": "`reason=\"destroyed\"` means `device.destroy()` was CALLED by us (rendererRetirement.ts:400-404). During a clean open there is exactly one code path that calls it: `retireRenderer(oldRenderer)` at initScene.ts:5126, step 7 of the §RENDERER-LIVE-SWAP. (The other `retireRenderer` site, createRenderer.ts:626, is inside the device-loss RECOVERY, which is only reached AFTER `isDeliberateDeviceDestroy(info)` returns false — i.e. only on a real loss, which would print reason=\"unknown\".) The swap is fired by `maybeAutoSwitchToWebGLForHeavyScene(scene, `tier:${reason}`, meshCount)` at initScene.ts:3008 — the per-geometry-add tier pass — once the live scene crosses SWAP_MESH_THRESHOLD=1000. The founder's census reads 3762 meshes at 162 elements (23 meshes/element), so the mesh arm is crossed early during hydration and the swap is effectively guaranteed on this project. The swap then: stops the rAF loop, disposes the RPM/TSL pipeline (initScene.ts:4973), creates a NEW canvas + NEW renderer (`await createRenderer(newCanvas, pref)`), re-binds the pipeline, retires the old renderer (destroying the device) and restarts the loop — while showing a 'Switching renderer…' cover over the viewport. The scene graph is CPU data and survives, but every geometry/material draw state the WebGPU device had minted is discarded and re-minted by the new renderer on its first frame (the retirement log at initScene.ts:5129-5136 says this explicitly). ISSUE-LOG L-1483 records the founder hitting exactly this with `reason=tier:post-load`.",
      "evidence": [
        "packages/renderer-three/src/rendererRetirement.ts:402-404 — \"reason = \\\"destroyed\\\"  — `device.destroy()` was CALLED. That is US. It is the last step of retireRenderer() → `renderer.dispose()` → `backend.destroy()`\"",
        "apps/editor/src/engine/initScene.ts:5126 — `const _detached = retireRenderer(oldRenderer);` (step 7 of the live backend swap)",
        "apps/editor/src/engine/initScene.ts:3008 — `maybeAutoSwitchToWebGLForHeavyScene(scene, `tier:${reason}`, meshCount);` inside the per-add tier pass",
        "apps/editor/src/rendering/heavySceneSwapThreshold.ts:43 — `export const SWAP_MESH_THRESHOLD = 1000;`",
        "apps/editor/src/rendering/autoWebGLHeavyScene.ts:237 — `if (!isSwapWorthyHeavyScene(elementCount, sceneMeshCount)) return;`",
        "docs/04-reference/ISSUE-LOG.md:23364 — \"[autoWebGLHeavyScene] §AUTO-WEBGL-HEAVY — device-loss-risk scene (251 elems / 2647 meshes / 7 levels; reason=tier:post-load); switched WebGPU→WebGL…\"",
        "apps/editor/src/rendering/createRenderer.ts:626 — `try { retireRenderer(threeRenderer); }` is inside the device-lost RECOVERY block, downstream of `if (isDeliberateDeviceDestroy(info)) return;` at createRenderer.ts:537",
        "Founder log: \"[pryzmPerf] NAV-BACKEND-SWAP-HEADROOM … NOW: 162 elements (238 to go), 3762 meshes (ARM TRIPPED)\""
      ],
      "estimatedCostMs": 1500,
      "costBasis": "estimated",
      "fix": "Do not let a REACTIVE per-add tier pass decide the backend during hydration. The loader already knows the snapshot's `elementCount` (503) before a single mesh is built, and `proactivelySwitchToWebGLForBuildingGeneration()` (autoWebGLHeavyScene.ts:283) is the existing entry point that swaps BEFORE geometry exists — it is currently wired only to `buildingGenerationLifecycle.ts:307`. Wire the same pre-emptive decision into the project-open path (from the snapshot's element count, or a stored per-project mesh estimate) so the whole hydration runs on the target backend and no device is ever built and destroyed inside one open. Second, verify the swap is actually what the founder saw: grep his full console for `§AUTO-WEBGL-HEAVY` and `§RENDERER-LIVE-SWAP live swap complete` — neither line is in the excerpt I was given, and their absence would falsify this. Do NOT raise SWAP_MESH_THRESHOLD (heavySceneSwapThreshold.ts:34-37 forbids it); the 23-meshes-per-element ratio is the real target.",
      "risk": "medium",
      "confidence": "high",
      "whatItWouldBreak": "The swap exists to pre-empt a WebGPU TDR device-loss on the founder's hardware (ADR-0267 / L-361 / L-366). Moving the decision earlier must keep the once-per-session guard and must not reintroduce a heavy WebGPU PSO storm. Making the decision from a stored element count means a project whose mesh count is under-estimated stays on WebGPU longer — the reactive pass must remain as the net, not be deleted.",
      "filesToChange": [
        "apps/editor/src/rendering/autoWebGLHeavyScene.ts",
        "apps/editor/src/engine/initScene.ts",
        "apps/editor/src/ui/platform/PlatformShell.ts"
      ]
    },
    {
      "title": "Two full-project serializes on every open — the `resetDirtyAfterLoad()` hash baseline is discarded by the auto-save that follows",
      "mechanism": "`PlatformVersionController.loadVersion()` calls `orchestrator.resetDirtyAfterLoad()` (PlatformVersionController.ts:412) unconditionally at the end of every open. That calls `this.getHash()` (SaveOrchestrator.ts:591), which is `ctx.saveAdapter.serialize({...})` + `stringify` (PlatformSaveController.ts:114-116) — a full walk of ~30 stores with `deepStrip`/`structuredClone` per element (ProjectSerializer.ts:1509-1748). Its only product is a baseline string. Then the post-load sweep dirties the model, `handleLoadSuppressEnd()` arms the 2500 ms debounce (SaveOrchestrator.ts:539-543), and `executeSave()` calls `this.getHash()` AGAIN (SaveOrchestrator.ts:382) — a second full serialize of the same 503 elements — and compares it against the discarded baseline. The founder's log proves this second pass ran and the hashes differed: `[PlatformSaveController] Version saved: \"Auto-save\" (503 elements)` and `[ProjectSerializer] Snapshot created: 503 elements`. The L-131 P3b fix already removed a THIRD serialize (saveVersionInternal now reuses the stashed one, PlatformSaveController.ts:250-268); the baseline serialize was not part of that fix and is still paid on every open.",
      "evidence": [
        "apps/editor/src/ui/platform/PlatformVersionController.ts:412 — `this.saveCtrl.orchestrator.resetDirtyAfterLoad();`",
        "apps/editor/src/ui/platform/SaveOrchestrator.ts:589-596 — `resetDirtyAfterLoad(): void { try { const hash = this.getHash(); this.markClean(hash); } …`",
        "apps/editor/src/ui/platform/SaveOrchestrator.ts:382 — `const currentHash = this.getHash();` inside executeSave()",
        "apps/editor/src/ui/platform/PlatformSaveController.ts:114-116 — `const snapshot = ctx.saveAdapter.serialize({ projectName: ctx.projectName, projectId: ctx.projectId }); const json = ctx.saveAdapter.stringify(snapshot);`",
        "apps/editor/src/ui/platform/SaveOrchestrator.ts:244-246 — \"autosave does a FULL-project serialize (793 elements → ~16.6 MB) TWICE per fire (once in getHash() for the dirty-check, once in saveVersionInternal for the snapshot) plus a main-thread deflate\"",
        "apps/editor/src/engine/persistence/ProjectSerializer.ts:1565-1748 — ~30 `store.getAll().map(deepStrip)` / `structuredClone` passes per serialize"
      ],
      "estimatedCostMs": 400,
      "costBasis": "estimated",
      "fix": "The snapshot that was JUST loaded is already in hand at PlatformVersionController.ts:391 (`version.snapshot`) — hand it (or its already-stringified form, which `getLatestVersion` inflated moments earlier) to `markClean(hash)` as the baseline instead of re-serializing the stores. Add an optional `markCleanFrom(json: string)` on SaveOrchestrator and have `loadVersion` call it. If the loader legitimately mutates the model relative to the stored snapshot (wall joins, room re-detect), then the baseline is wrong TODAY too, in which case delete the baseline call entirely and let the first `executeSave()` establish `lastHash` — the `lastHash === ''` branch at SaveOrchestrator.ts:383 already handles that.",
      "risk": "medium",
      "confidence": "high",
      "whatItWouldBreak": "`resetDirtyAfterLoad()`'s baseline is what makes 'undo back to the loaded state' stop being dirty. If the baseline comes from the STORED snapshot but the post-load sweep legitimately changes the model, the first autosave still fires (hashes differ) — behaviour unchanged. If the baseline is dropped entirely, the very first mutation after an open always writes a version even if the user reverted it. Measure before choosing which of the two fixes to take.",
      "filesToChange": [
        "apps/editor/src/ui/platform/SaveOrchestrator.ts",
        "apps/editor/src/ui/platform/PlatformVersionController.ts",
        "apps/editor/src/ui/platform/PlatformSaveController.ts"
      ]
    },
    {
      "title": "captureThumbnail runs twice per open from two uncoordinated schedulers, and each run forces a synchronous full-scene render plus a GPU readback",
      "mechanism": "There is ONE `captureThumbnail` implementation (initPersistence.ts:141) and it logs once per call, so two log lines means two calls. The two callers are independent and neither knows about the other: (a) `PlatformSaveController.saveVersionInternal()` at :357 — `this.ctx.saveAdapter.captureThumbnail?.()` on the auto-save that fires on the open path; (b) `schedulePostLoadThumbnailCapture()` at :551, armed from `setProjectContext` (PlatformShell.ts:254) on a `pryzm-project-loaded` one-shot, then `setTimeout(3500ms)` + `requestIdleCallback`. Each call does: a FORCED synchronous render of the whole scene (`if (rpm) rpm.render();`, initPersistence.ts:175) — at 3762 meshes that is a full forward pass plus shadow casters; then `ctx.drawImage(src, …)` from the renderer canvas (initPersistence.ts:207), which forces a GPU→CPU copy; then `getImageData()` (initPersistence.ts:219); then the WebP encode ladder, up to 6 rungs (thumbnailBudget.ts:68-75). Only (a) is on the open critical path; (b) lands 3.5 s later, i.e. during the window the user is first trying to interact.",
      "evidence": [
        "apps/editor/src/engine/initPersistence.ts:305 — the only site that prints `[captureThumbnail] Thumbnail captured:` (one call ⇒ one line)",
        "apps/editor/src/ui/platform/PlatformSaveController.ts:357 — `: (this.ctx.saveAdapter.captureThumbnail?.() ?? null);` (auto-save path)",
        "apps/editor/src/ui/platform/PlatformSaveController.ts:551 — `const thumb = this.ctx.saveAdapter.captureThumbnail?.() ?? null;` (post-load path)",
        "apps/editor/src/ui/platform/PlatformShell.ts:254 — `this.saveCtrl.schedulePostLoadThumbnailCapture(id);` — armed on every open",
        "apps/editor/src/engine/initPersistence.ts:174-175 — `const rpm = window.renderPipelineManager; if (rpm) rpm.render();`",
        "apps/editor/src/ui/platform/PlatformSaveController.ts:521-543 — the post-load path has no check for a thumbnail already captured this open; its only guards are `detail.projectId !== projectId`, `detail.empty`, and `activeProjectId !== projectId`"
      ],
      "estimatedCostMs": 300,
      "costBasis": "estimated",
      "fix": "Drop the capture on the AUTO-SAVE path during the open window and let the deferred post-load capture own it — that path already exists, already writes the project index and already uploads to the server (PlatformSaveController.ts:566-570), and it is deliberately scheduled off the critical path. The code at PlatformSaveController.ts:344-352 already has precedent for skipping the auto-save capture and relying on the post-load one ('The renderer still captures a fresh thumbnail on the post-load path so the hub preview stays current') — that branch is currently gated on `syncQueue.isPlanRejected(projectId)`; widen it to also skip while a post-load capture is pending for this project. Track that with a single flag set by `schedulePostLoadThumbnailCapture` and cleared in `runCapture`.",
      "risk": "low",
      "confidence": "high",
      "whatItWouldBreak": "The auto-save capture is what puts a thumbnail INTO the VersionRecord. If the post-load capture is skipped (empty project, project switched during the 3.5 s delay, `requestIdleCallback` never fires) and the auto-save capture was suppressed, that open produces no fresh preview — the save path falls back to `existingMeta?.thumbnail`, so a project that has never had one keeps the placeholder. Gate the suppression on the post-load capture actually being armed, and clear the flag on the skip branches.",
      "filesToChange": [
        "apps/editor/src/ui/platform/PlatformSaveController.ts"
      ]
    },
    {
      "title": "Two socket.io clients connect and join the same project room — two call sites, two websockets, two server-side access checks",
      "mechanism": "These are two DIFFERENT modules each constructing their own socket.io client for the same project, not one function called twice. `PlatformShell.setProjectContext()` calls `initSocketCollaboration(this.ctx, id)` (PlatformShell.ts:250), which builds `ctx.socket = ioFn({...})` and on connect emits `join-project` (PlatformCollabPill.ts:182-191). Later, `pryzm-project-loaded` fires `onProjectLoaded` → `loadAndConnect(projectId)` → `connectSocket()` (initCollaboration.ts:848), which builds its OWN closure-local `socket = ioFn({...})` and emits `join-project` again (initCollaboration.ts:588-593). The two hold different references (`ctx.socket` vs the module closure), so neither disconnects the other — both stay connected for the session. Server-side each `join-project` runs `canUserAccessProject(...)` (server.js:578-587), which is at minimum one Supabase REST round-trip (server/projectAccess.js:120-125), so the open pays that twice. The two sockets do NOT double-apply remote commands: only `initCollaboration` listens for `command-executed` (initCollaboration.ts:~655) and only `PlatformCollabPill` listens for `version-saved` (PlatformCollabPill.ts:194) — the split is why nobody noticed. A secondary duplicate hides here too: the two loaders inject the socket.io bundle under DIFFERENT script ids (`plat-socketio-client`, PlatformCollabPill.ts:174, vs `pryzm-collab-socketio-client`, initCollaboration.ts:857), so neither can see the other's tag and only the `window.io` check prevents a second script request.",
      "evidence": [
        "apps/editor/src/ui/platform/PlatformShell.ts:250 — `initSocketCollaboration(this.ctx, id);`",
        "apps/editor/src/ui/platform/PlatformCollabPill.ts:182-191 — `ctx.socket = ioFn({...}); ctx.socket.on('connect', () => { console.log('[PlatformCollabPill] Socket connected — joining project room:', projectId); ctx.socket.emit('join-project', projectId); });`",
        "apps/editor/src/engine/initCollaboration.ts:578-593 — `socket = ioFn({...}); socket.on('connect', () => { console.log('[initCollaboration] Socket connected — joining project room:', projectId); … socket.emit('join-project', projectId); });`",
        "apps/editor/src/engine/initCollaboration.ts:848 — `connectSocket(projectId, ioFn);` reached from `onProjectLoaded` (initCollaboration.ts:1140) via the `pryzm-project-loaded` subscription at initCollaboration.ts:1157",
        "server.js:566-587 — `socket.on('join-project', async (projectId) => { … const access = await canUserAccessProject(socket.data.userId, projectId, {...});`",
        "server/projectAccess.js:120-125 — the Supabase `projects` select each join-project performs"
      ],
      "estimatedCostMs": 40,
      "costBasis": "estimated",
      "fix": "Give the project room ONE socket. `initCollaboration` is the richer client (cursors, catch-up replay, join-retry backoff, visibility-intent sync), so make it the owner: expose its socket on the collaboration handle and have `PlatformCollabPill` register its single `version-saved` listener on that socket instead of opening its own. `initSocketCollaboration` then becomes a listener-registration call, not a connect. If the ordering is a problem (the pill runs at `setProjectContext`, initCollaboration at `pryzm-project-loaded`), have the pill queue its handler and attach it when the collaboration handle publishes the socket. While there, unify the two script-tag ids.",
      "risk": "medium",
      "confidence": "high",
      "whatItWouldBreak": "The pill's socket currently connects EARLIER (at setProjectContext) than initCollaboration's (at pryzm-project-loaded). If a `version-saved` broadcast lands in that window it would now be missed. Also `initCollaboration.onGoHub` disconnects its socket on return-to-hub; the pill's presence notifications must not silently die with it. Ship with a test that a collaborator's save still toasts after a hub round-trip.",
      "filesToChange": [
        "apps/editor/src/ui/platform/PlatformCollabPill.ts",
        "apps/editor/src/engine/initCollaboration.ts",
        "apps/editor/src/ui/platform/PlatformShell.ts"
      ]
    },
    {
      "title": "NOT A DEFECT — the duplicated 'WebGPU device lost' log line is two handlers on ONE device, and both correctly refuse to recover",
      "mechanism": "Both `WebGPURendererAdapter` (packages/renderer-three) and `createRenderer` (apps/editor) attach a `.lost.then(...)` to the SAME `GPUDevice` obtained from `renderer.backend.device`. Both log the raw `reason`/`message` verbatim, then both call the single shared authority `isDeliberateDeviceDestroy(info)` and return. So the pair of lines is by design (L-1001 deliberately kept both logs and moved only the DECISION into one place); no recovery pipeline, no renderer rebuild and no PSO re-warm runs twice off it. Recording this so the pair is not re-diagnosed as a double reset — the underlying single reset is finding #1.",
      "evidence": [
        "packages/renderer-three/src/adapters/WebGPURendererAdapter.ts:222-241 — `gpuDevice.lost.then((info) => { console.error('[renderer-three/WebGPURendererAdapter] WebGPU device lost: …'); … if (isDeliberateDeviceDestroy(info)) return; adapter._lostCallbacks.forEach(cb => cb()); })`",
        "apps/editor/src/rendering/createRenderer.ts:526-537 — `gpuDevice.lost.then(async (info) => { console.error(`[createRenderer] WebGPU device lost: reason=\"${info.reason}\"…`); … if (isDeliberateDeviceDestroy(info)) return;`",
        "apps/editor/src/rendering/createRenderer.ts:517 — `const gpuDevice: GPUDevice | undefined = (threeRenderer as any).backend?.device;` — the SAME device the adapter wired at WebGPURendererAdapter.ts:221",
        "packages/renderer-three/src/rendererRetirement.ts:412-418 — \"TWO handlers on the SAME device, printing back to back… One of the two was already right\"",
        "docs/03-execution/lanes/UNDO93-SCENE-CLASH-FIX-PLAN.md:29 — \"`WebGPU device lost: reason=\\\"destroyed\\\"` | CORRECTLY IGNORED… This is L-1001 §DEVICE-DESTROY-IS-NOT-DEVICE-LOSS working as fixed.\""
      ],
      "estimatedCostMs": 0,
      "costBasis": "read-from-code",
      "fix": "No code change. If the noise is unwanted, demote the adapter's line to a single `console.debug` when `isDeliberateDeviceDestroy(info)` is true — but keep BOTH raw logs, because rendererRetirement.ts:428-431 states explicitly that neither call site may weaken its diagnostic.",
      "risk": "low",
      "confidence": "high",
      "whatItWouldBreak": "Suppressing either log removes the only verbatim record of the reason/message, which is what let L-1001 be diagnosed at all. Do not 'clean up' by deleting one handler: the adapter's handler feeds `_lostCallbacks` for real losses, and createRenderer's feeds the full recovery pipeline. They do different things on a real loss.",
      "filesToChange": []
    }
  ],
  "whatIcouldNotEstablish": "MEASUREMENT. Every `estimatedCostMs` above is ESTIMATED, not measured. The founder's log excerpt carries only two real timings (`work=109ms yieldCalendar~83ms` for the edge projector; nothing for the swap, the thumbnails, the sockets or the serializes), and I found no measured number in the repo for a renderer live-swap, a `captureThumbnail`, or a 503-element `serialize()`. The nearest measured anchors are for adjacent work only: the version READ leg at \"89 ms cold, of which 80 ms is journal re-attach\" (startupBudget.ts:114) and the narrow-restore fix at \"2231 ms → 78 ms\" (PlatformShell.ts:279-281). Rank these findings by mechanism, then measure before spending on any of them — `markStartupPhase` already exists and the open path is already instrumented (`open:shell-context-set` → `open:version-read-*` → `open:snapshot-loaded` → `open:first-interactive-frame`); adding marks around the swap and the two thumbnail captures is a one-line-each change.\n\nI ALSO COULD NOT CONFIRM THE SWAP ACTUALLY FIRED. The excerpt contains neither `[autoWebGLHeavyScene] §AUTO-WEBGL-HEAVY` nor `[initScene] §RENDERER-LIVE-SWAP live swap complete`, which the swap prints unconditionally. The chain (native-WebGPU boot proven by createRenderer's handler being armed at all → mesh arm tripped at 3762 → `retireRenderer` being the only `device.destroy()` reachable on a clean open) is an ELIMINATION from code, not an observation. Grep the founder's full console for those two tags before acting on finding #1; if they are absent, some other `renderer.dispose()` is destroying the device and my mechanism is wrong.\n\nWHAT I RULED OUT (so it is not re-opened): no second `composeRuntime()` — one call at src/main.ts:413, and `_bootstrapped` (src/main.ts:203, set only after `bootstrap()` resolves) guards a second engine boot. No double `setProjectContext` — idempotency guard at PlatformShell.ts:223. No duplicate `pryzm-project-loaded` on this path — the only emitter reached for a project with local versions is PlatformVersionController.ts:418; PlatformShell.ts:311/317/510 are the `empty:true` branches. No accumulating listener from `_armPostSceneMarks` (PlatformShell.ts:189 unsubscribes first). Concurrent duplicate opens are already coalesced by `packages/runtime-composer/src/openCoalescing.ts`. The historical double IndexedDB container write per autosave tick is FIXED (§SUSTAIN109 / L-11545, ProjectRepository.ts:415-443) and the founder's single `reason=save-version` line is the evidence it holds — do not re-diagnose it.\n\nNOT ESTABLISHED AS A DUPLICATE: the EdgeProjector `cacheHits=0 cacheMisses=63 hitRate=0%` line. On a cold open a 0% rate is the cache being POPULATED (`cacheEntries=63/5000` in the same line), not a re-run. I saw only one `§PERF-EDGEPROJECTOR-CHUNK` line in the excerpt and found no evidence of a second projection pass during open. docs/04-reference/GENERATIVE-PIPELINE-AUDIT-2026-08-10.md:207 records 0-3% as the documented steady-state rate for an unrelated reason. Whether the projection runs more than once on open is UNKNOWN and needs the full console.\n\nALSO UNKNOWN: whether the swap and the first thumbnail capture overlap. `captureThumbnail` calls `rpm.render()` (initPersistence.ts:175) and the swap disposes and rebinds that same RPM (initScene.ts:4973, 5054). If the auto-save capture lands inside the swap window, the capture reads a dead or half-bound pipeline — which is exactly the \"[captureThumbnail] Read a blank/transparent frame\" branch at initPersistence.ts:235. I did not find a lock between them; that is worth a targeted read if the founder ever reports a blank project card on this project."
}