{
  "dimension": "Lighting fixtures should have a plan symbol for each (documentation quality — plan/elevation/section projection)",
  "summary": "The brief's premise is HALF WRONG in a way that changes the fix. There is no `LightingPlanSymbolBuilder` (correct — nothing injects lighting linework into the TechnicalDrawing), but a lighting plan symbol renderer DOES exist and DOES run: `packages/core-app-model/src/views/symbols/LightingPlanSymbolRenderer.ts`, painted straight onto Canvas2D by `PlanViewCanvas._renderLightingPlanSymbols()` (:660, :2092). It is absent from the log because it is not a drawing injector, so it prints no `[…SymbolBuilder] Injected …` line.\n\nTHE KEY QUESTION (c) IS ANSWERED AND THE ANSWER IS THE WORST ONE: lighting geometry is NOT suppressed. `grep -rn skipInPlan packages/geometry-lighting` → 0 hits. `LightingFragmentBuilder.ts:748` stamps `child.userData.elementType = 'Lighting'` on every mesh and no skip flag, so `EdgeProjectorService.ts:2886`'s generic gate never fires and every 672-face luminaire is fully edge-projected. **The double-draw the brief feared is ALREADY SHIPPED** — a cyan canvas disc painted on top of a black mesh blob. Adding a builder without adding suppression makes it a triple-draw.\n\nThe blob is also UNGOVERNABLE, by a four-site chain I traced end to end. `ELEMENT_TYPE_TO_PROJECTION_LAYER` (EdgeProjectorService.ts:203) has no `Lighting` row → `resolveProjectionLayer('Lighting')` returns the `projection-visible` fallback → `vgCategoryForLayer('projection-visible')` returns null → (1) `EdgeProjectorService.ts:2697`'s intent veto `if (_groupFamily && !_symbolGate(...))` is skipped, so no visibility setting can stop the projection; (2) `PlanViewCanvas.ts:459-464`'s `if (resolved && !resolved.visible) return` is skipped, so no VG toggle can hide the painted line; (3) `penCategoryForLayerTag` (PenWeightTable.ts:439) has no lighting arm, so the pen resolves to `FALLBACK_PEN` 0.18 mm black — heavier than furniture's 0.13 and heavier than lighting's OWN authored pen, which exists at PenWeightTable.ts:153/213/232 and is unreachable.\n\nAnd the category the founder is looking for does not exist: `VisibilityIntentDefaults.ts:15-41` — the file whose own comment says *\"THIS is where a category is DECLARED\"* — lists 17 families and `'lighting'` is not one. Neither is it in `VGCategory` (VGSceneApplicator.ts:57-75), `ELEMENT_TYPE_TO_VG_CATEGORY` (:109), `ELEMENT_TYPE_REGISTRY` (ElementTypeRegistry.ts:31) or `ISO_LAYER_TO_VG_CATEGORY` (DrawingLayerIdentity.ts:96).\n\nFinally: because the symbol is canvas-only it contributes ZERO `LineSegments` to `drawing.three`, which is the ONLY thing `SVGCompositeRenderer.setTechnicalDrawing()` (:228-232) reads. So a printed/exported sheet has NO lighting symbol at all — only the blob.\n\nThis is a five-site fix with a very close precedent already in the tree (`BoundaryLinePlanSymbolBuilder`, whose header enumerates exactly these sites for its own family), and `geometry-lighting/package.json` already declares the three dependencies a symbol builder needs, so no lockfile churn.",
  "findings": [
    {
      "title": "Lighting mesh geometry is NOT suppressed in plan — the 672-face luminaire is fully edge-projected, and that IS the dense circular scribble",
      "mechanism": "Every family with a plan symbol tags `userData.skipInPlan = true` on its meshes; `EdgeProjectorService.ts:2886` reads that tag and returns early. `grep -rn \"skipInPlan\" packages/geometry-lighting` → ZERO hits. `LightingFragmentBuilder.ts:742-749` traverses every child mesh and stamps `isSubElement`, `parentId` and `elementType='Lighting'` — and no skip flag. The plan Y-range filter cannot save it either: `resolveClipRange` returns `far = levelElevation + farOffset` where farOffset is the level's floor-to-floor height (`EdgeProjectorService.ts:4497`), and the mesh filter at `:2949-2951` admits anything with `min.y <= far + 0.5`. A ceiling fixture at 2.7 m in a 3.0 m storey is inside [floor−1.2, 3.5]. Then `classifyByVertexY` (`:1073-1128`) drops ONLY segments BELOW the floor outside the beyond zone — there is NO upper cull at the cut plane — so every edge above the 1.2 m cut plane lands in `projPositions` and is drawn. The shape matches the founder's description exactly: a downlight is `CylinderGeometry(…, SEG_BODY=24)` + `CylinderGeometry(…, 24, 1, true)` reflector + `TorusGeometry(r*0.92, r*0.10, 8, SEG_TRIM=28)` bezel + `SphereGeometry(…, SEG_LENS=32, …)` lens (LightingFragmentBuilder.ts:1111-1128, 369-373, 449). `EdgesGeometry` at the default 1° threshold on that torus emits 8 concentric 28-gon rings plus 28 radial spokes — literally \"concentric line bursts\". The face total is ~672, matching the log's `elemType=Lighting faceCount=672` line for line.",
      "evidence": [
        "packages/geometry-lighting/src/LightingFragmentBuilder.ts:742-749 — `group.traverse((child) => { … child.userData.isSubElement = true; child.userData.parentId = data.id; child.userData.elementType  = 'Lighting'; })` — no `skipInPlan` anywhere in the package",
        "apps/editor/src/engine/views/EdgeProjectorService.ts:2885-2887 — `// Builder sets userData.skipInPlan=true.` / `if (isPlanView && mesh.userData.skipInPlan === true) { return; }`",
        "apps/editor/src/engine/views/EdgeProjectorService.ts:2949-2951 — `if (isPlanView && meshWorldBox && planBelowY !== null) { const planMaxY = far + 0.5; if (meshWorldBox.max.y < planBelowY || meshWorldBox.min.y > planMaxY) return; }`",
        "apps/editor/src/engine/views/EdgeProjectorService.ts:4497 — `const far  = levelElevation + farOffset;   // top of view range — DOC-1.5d fix`",
        "apps/editor/src/engine/views/EdgeProjectorService.ts:1106-1109 — `const belowFloor = floorY !== null && !isCut && avgY < floorY - epsilon; … if (belowFloor && !isBeyond) continue;` — the ONLY drop arm; nothing culls above `cutPlaneY`",
        "packages/geometry-lighting/src/LightingFragmentBuilder.ts:1128 — `const bezelGeo = new THREE.TorusGeometry(p.radius * 0.92, p.radius * 0.10, 8, SEG_TRIM);` with `SEG_TRIM = 28` (:371) → 8×28 quad grid of edges inside a 6.5 cm disc",
        "packages/geometry-plumbing/src/PlumbingPlanSymbolBuilder.ts:16-19 — the same problem, already solved for plumbing: \"every fixture-part mesh tags `userData.skipInPlan = true` … so EdgeProjectorService excludes it from plan-view projection, and this builder injects the clean 2D symbol instead\""
      ],
      "userVisibleSymptom": "Dense black concentric hatch-like bursts where the light fittings are, on every floor plan. They cannot be turned off by any visibility control, they print, and a cyan symbol disc is drawn on top of them.",
      "fix": "Stamp `skipInPlan: true` on every lighting mesh in `LightingFragmentBuilder._buildFixture`'s existing `group.traverse` at :742-749 (one added property in a loop that is already running) — AND ONLY IN THE SAME COMMIT AS THE SYMBOL INJECTOR. `skipInPlan` with no producer is a deletion: `WindowMeshSkipInPlan.test.ts:32` records exactly that trap (\"take the symbol away, `skipInPlan` silently becomes exactly the deletion the brief feared\"). Follow the door/window precedent and tag EVERY mesh rather than a role allowlist — `DoorMeshSkipInPlan.test.ts:76` asserts \"C-1 EVERY door mesh is tagged skipInPlan (not a hand-maintained list of roles)\" for exactly this reason. Leave elevation and section alone: `skipInElevation` / `skipInSection` are deliberately separate flags (EdgeProjectorService.ts:2890-2900), and a section legitimately cuts a pendant.",
      "effort": "small",
      "risk": "medium",
      "confidence": "high",
      "alreadyExists": "`skipInPlan` gate: EdgeProjectorService.ts:2886. Precedent tags: BedBuilder.ts:61, ChairBuilder.ts:28, WardrobeGlassBuilder.ts:215, WindowBuilder.ts:988, DoorBuilder.ts:689. Guard tests to copy: packages/geometry-door/__tests__/DoorMeshSkipInPlan.test.ts, packages/geometry-window/__tests__/WindowMeshSkipInPlan.test.ts (both assert the projector still honours the gate, by regex over EdgeProjectorService source).",
      "filesToChange": [
        "packages/geometry-lighting/src/LightingFragmentBuilder.ts"
      ]
    },
    {
      "title": "The lighting plan symbol exists but is CANVAS-ONLY — it contributes no linework, so it is invisible to sheets, DXF/PDF/SVG export, hidden-line removal, VG styling and the pen table",
      "mechanism": "`renderLightingSymbols` (LightingPlanSymbolRenderer.ts:243-275) paints `ctx.arc(...)` directly onto the Canvas2D context from `window.lightingStore`. Every other family's symbol is a `THREE.LineSegments` injected into the TechnicalDrawing via `projectToDrawingSpace` + `drawing.addProjectionLines` + `registerSegmentUUID` (PlumbingPlanSymbolBuilder.ts:73-79). The file's own header at :37-58 states the consequence for selection and it was fixed for selection ONLY (L-10081 added `hitTestLightingSymbol`, a second bespoke path). The other consequences were never closed: `SVGCompositeRenderer.setTechnicalDrawing(drawing)` opens `if (!drawing?.three) return this;` and then `(drawing.three as THREE.Group).traverse(...)` — the canvas symbol is not in `drawing.three`, so it is not in the exported sheet. It also never reaches `HiddenLineRemoval`, `VGSceneApplicator.applyToProjectionLayers` or `PenWeightTable`. Colour is the visible tell: `STROKE_DEFAULT = '#0ea5e9'` (cyan) and `STROKE_SELECTED = '#7c3aed'`, hard-coded, at a fixed `ctx.lineWidth = 1.25` px — not black ink, not a drafting colour, not scale-aware. Compare the pen table's own ruling for the boundary line (PenWeightTable.ts, `'boundary-line'` row): a committed drawing line must be a drafting colour because \"a drawing is printed, exported to DXF and read by a contractor\".",
      "evidence": [
        "packages/core-app-model/src/views/symbols/LightingPlanSymbolRenderer.ts:13-16 — `const STROKE_DEFAULT   = '#0ea5e9';` / `const FILL_DEFAULT     = 'rgba(14,165,233,0.12)';`",
        "packages/core-app-model/src/views/symbols/LightingPlanSymbolRenderer.ts:102 — `ctx.lineWidth   = 1.25;` — a fixed pixel width, bypassing SYSTEM_PEN_TABLE and SCREEN_PX_PER_MM entirely",
        "packages/core-app-model/src/views/symbols/LightingPlanSymbolRenderer.ts:44-49 — its own header: \"EVERY other plan symbol that must be clickable … is produced by a plan-symbol BUILDER that INJECTS UUID-registered `THREE.LineSegments` into the technical drawing … Lighting is the one family whose plan symbol is painted straight onto the 2-D canvas\"",
        "packages/file-format/src/export/sheets/SVGCompositeRenderer.ts:228-232 — `setTechnicalDrawing(drawing: any): this { if (!drawing?.three) return this; … (drawing.three as THREE.Group).traverse(...)` — the sole ingestion path for sheet export",
        "packages/core-app-model/src/views/PlanViewCanvas.ts:660 and :2092 — `this._renderLightingPlanSymbols();` called AFTER the linework traverse at :430, so the cyan disc paints on top of the black blob",
        "apps/editor/src/engine/views/EdgeProjectorService.ts:3928-3932 — the projector's own note: \"WHAT AN RCP *DOES* SHOW — light fittings — is rendered on the CANVAS, not here\""
      ],
      "userVisibleSymptom": "On screen: a small cyan circle sitting on top of a black scribble. On an exported sheet or PDF: the scribble alone, no symbol. In DXF: no lighting layer.",
      "fix": "Add `packages/geometry-lighting/src/LightingPlanSymbolBuilder.ts` following `PlumbingPlanSymbolBuilder` verbatim (113 lines, the smallest and newest exemplar): `inject(drawing, viewDef)`, read `viewDef.spatial.levelId`, read the store via `storeRegistry.getStoreForType('lighting')`, `drawing.layers.create(LGHT_LAYER)`, build a `BufferGeometry` of segment pairs, wrap in `THREE.LineSegments`, apply the fixture transform, `projectToDrawingSpace(...)`, `drawing.addProjectionLines(projected, LGHT_LAYER)`, `registerSegmentUUID(drawing, projected, fixture.id)`, log the count. Export the singleton from `packages/geometry-lighting/src/index.ts`, import it in `EdgeProjectorService.ts` beside the other fifteen, and call it inside the injection block behind `_symbolGate('lighting')`. `geometry-lighting/package.json` ALREADY declares `@pryzm/core-app-model`, `@pryzm/renderer-three` and `@thatopen/components` — the exact three a symbol builder needs — so unlike the boundary-line case there is no lockfile churn and it does NOT have to live in apps/editor. Once linework exists, delete the bespoke `hitTestLightingSymbol` fallback at PlanViewCanvas.ts:1540-1548 (the generic linework hit test will resolve it) and keep `renderLightingSymbols` only as the tool-preview drawer.",
      "effort": "medium",
      "risk": "low",
      "confidence": "high",
      "alreadyExists": "Template: packages/geometry-plumbing/src/PlumbingPlanSymbolBuilder.ts (whole file). Injection API: `projectToDrawingSpace`, `registerSegmentUUID`, `drawing.addProjectionLines` — all exported from @pryzm/core-app-model. Linework primitive helpers: `class LineworkBuf` in TreePlanSymbolBuilder.ts:78-135 (`circle`, `bumpyCircle`, `smallDot`, `plusMark`, `tick`). The 11 existing symbol shapes to port from Canvas2D arcs to segments: `drawLightingSymbol` in LightingPlanSymbolRenderer.ts:88-174. Detail-level hook: `resolveEffectiveDetailLevel(elementId, viewId, …)` as used at DoorPlanSymbolBuilder.ts:287 — needed because the founder's panel shows Detail Level Fine.",
      "filesToChange": [
        "packages/geometry-lighting/src/LightingPlanSymbolBuilder.ts",
        "packages/geometry-lighting/src/LightingSymbolGeometry.ts",
        "packages/geometry-lighting/src/index.ts",
        "apps/editor/src/engine/views/EdgeProjectorService.ts",
        "packages/core-app-model/src/views/PlanViewCanvas.ts",
        "packages/core-app-model/src/views/symbols/LightingPlanSymbolRenderer.ts"
      ]
    },
    {
      "title": "Lighting has no ISO layer row, so its linework is UNCLASSIFIED — the visibility-intent veto, the VG hide and the pen table are all bypassed, and lighting's own authored pens are dead code",
      "mechanism": "`ELEMENT_TYPE_TO_PROJECTION_LAYER` (EdgeProjectorService.ts:203-240) has rows for wall, slab, column, beam, door, window, stair, roof, furniture, plumbing, ceiling and even `rhino` — and NO `Lighting`. `resolveProjectionLayer('Lighting')` therefore returns `FALLBACK_NATIVE_LAYER = 'projection-visible'` (:242, :330). Three separate gates then fail open in sequence. (1) The native intent veto: `const _groupFamily = vgCategoryForLayer(resolveProjectionLayer(...)); if (_groupFamily && !_symbolGate(_groupFamily))` (:2694-2701) — `vgCategoryForLayer('projection-visible')` returns null (DrawingLayerIdentity.ts:201-208, and `ISO_LAYER_TO_VG_CATEGORY` at :96-128 has no `A-LGHT`), so `_groupFamily` is null and the veto is skipped unconditionally. (2) The canvas VG hide: `const vgCat = this._vgCategoryForLayer(layerTag); if (vgCat && this._styleResolver) { … if (resolved && !resolved.visible) return; }` (PlanViewCanvas.ts:458-464) — same null, gate skipped. (3) The pen: `penCategoryForLayerTag` (PenWeightTable.ts:439-457) tests ten regexes — wall, door, slab, column, stair, roof, ceiling, furniture, handrail, window, boundary-line — and there is no lighting arm, so `categoryFromFlags` falls to `return 'projection'` (:408), `SYSTEM_PEN_TABLE.PROJECTION['projection']` is undefined, and `resolvePen` returns `FALLBACK_PEN = pen(0.18, '#000000')` (:265, :306-307). The table's own comment concedes 0.18 mm \"is heavier than several categories' real PROJECTION pens\". Meanwhile `SYSTEM_PEN_TABLE` DOES carry `lighting: pen(0.13, '#303030')` at :153 plus BEYOND :213 and HIDDEN :232 — three authored rows that no layer tag can ever reach. This is the identical shape `BoundaryLinePlanSymbolBuilder.ts:52-56` records for its own family: \"`penCategoryForLayerTag()` had NO arm for it — so even a correctly-tagged line resolved to the generic fallback and the authored pen was DEAD\" / \"`ISO_LAYER_TO_VG_CATEGORY` had no `A-CONS` row — so the linework was UNCLASSIFIED and the VG panel had no toggle for it\". A rival map already knows the answer: `DrawingPipelineWorker.ts:129` has `'A-LGHT': 'lighting'` — a fork of the same table that diverged.",
      "evidence": [
        "apps/editor/src/engine/views/EdgeProjectorService.ts:203-240 — `const ELEMENT_TYPE_TO_PROJECTION_LAYER … PlumbingFixture: 'A-PLMB', … rhino: 'A-FURN',` — no `Lighting` key",
        "apps/editor/src/engine/views/EdgeProjectorService.ts:242 — `const FALLBACK_NATIVE_LAYER = 'projection-visible';`",
        "apps/editor/src/engine/views/EdgeProjectorService.ts:2694-2701 — `const _groupFamily = vgCategoryForLayer(resolveProjectionLayer(group.userData?.elementType as string | undefined)); if (_groupFamily && !_symbolGate(_groupFamily)) { … continue; }`",
        "packages/core-app-model/src/drawing/DrawingLayerIdentity.ts:96-128 — `ISO_LAYER_TO_VG_CATEGORY` lists A-WALL…A-CONS, A-AREA, S-COLS, S-BEAM, S-GRID; no `A-LGHT`",
        "packages/core-app-model/src/views/PlanViewCanvas.ts:458-464 — `const vgCat = this._vgCategoryForLayer(layerTag); … if (resolved && !resolved.visible) return;`",
        "packages/core-app-model/src/drawing/PenWeightTable.ts:439-457 — `penCategoryForLayerTag` — ten `isX` regex flags, none for lighting",
        "packages/core-app-model/src/drawing/PenWeightTable.ts:153 — `lighting:   pen(0.13, '#303030'),` inside the PROJECTION block — authored and unreachable",
        "packages/core-app-model/src/drawing/PenWeightTable.ts:265 — `export const FALLBACK_PEN: PenStyle = pen(0.18, '#000000');`",
        "packages/core-app-model/src/drawing/DrawingPipelineWorker.ts:129 — `'A-LGHT':  'lighting',` — the rival map that DOES have the row",
        "packages/geometry-lighting/src/LightingFragmentBuilder.ts:731 — `group.userData.layerName  = 'A-LGHT';` — the layer name is already chosen; nothing downstream knows it"
      ],
      "userVisibleSymptom": "Turning lighting off in Visibility Intent does nothing to the plan. Changing line type / weight / colour does nothing to lighting. The lighting blob draws at 0.18 mm black — heavier than the furniture it sits next to.",
      "fix": "Mint `A-LGHT` as a first-class ISO layer at all four sites, in ONE commit (this is the rule `BoundaryLinePlanSymbolBuilder.ts:88-98` states: \"A layer name that only one of the three knows about is how 'drawn but ungovernable' is born\"). (1) `EdgeProjectorService.ts:203` — add `Lighting: 'A-LGHT', LightingFixture: 'A-LGHT'` (the canonical-key normaliser at :286 handles case, so one row suffices, but add both spellings the codebase produces). (2) `DrawingLayerIdentity.ts:96` — add `'A-LGHT': 'lighting'`. (3) `PenWeightTable.ts:439` — add `isLighting: /A-LGHT|lighting/i.test(layerTag)` and a `if (flags.isLighting) return 'lighting';` arm in `categoryFromFlags` (:392), which immediately activates the three already-authored pen rows. (4) `SVGCompositeRenderer.ts:62` — add `'A-LGHT': 0.13` to `ISO_LINE_WEIGHTS` so the sheet export weight matches the screen. Then reconcile `DrawingPipelineWorker.ISO_LAYER_TO_CATEGORY` (:119-133) with `DrawingLayerIdentity` rather than leaving two tables — note the worker fork's `return 'wall'` default at :141 means an unresolved tag is silently styled as a WALL, which is worse than null.",
      "effort": "small",
      "risk": "low",
      "confidence": "high",
      "alreadyExists": "`SYSTEM_PEN_TABLE` lighting rows (PROJECTION/BEYOND/HIDDEN) at PenWeightTable.ts:153/213/232 — authored 2026 and never reachable; adding the category arm activates them with no new design. `DrawingPipelineWorker.ts:129` already carries the `A-LGHT → lighting` row. `BoundaryLinePlanSymbolBuilder.ts:1-98` is a step-by-step worked example of this exact four-site mint, written up as a header.",
      "filesToChange": [
        "apps/editor/src/engine/views/EdgeProjectorService.ts",
        "packages/core-app-model/src/drawing/DrawingLayerIdentity.ts",
        "packages/core-app-model/src/drawing/PenWeightTable.ts",
        "packages/file-format/src/export/sheets/SVGCompositeRenderer.ts",
        "packages/core-app-model/src/drawing/DrawingPipelineWorker.ts"
      ]
    },
    {
      "title": "'lighting' is absent from the list that DECLARES visibility-intent categories — so there is no lighting row in the Visibility Intent panel at all",
      "mechanism": "`VisibilityIntentDefaults.ts:15-41` declares `const ELEMENT_TYPES = ['__default__','wall','slab','column','structural','beam','door','window','stair','roof','ceiling','furniture','plumbing','grid','annotation','level','boundary-line','ifc-element']`. Its own inline comment at :32-38 is explicit that this is the declaration site: \"THIS is where a category is DECLARED: every element type in this list gets a complete `ElementGraphicsRules` (all four states, seeded from the pen table) in every system intent and in every intent cloned from the defaults.\" `'lighting'` is not in it. `ElementTypeRegistry.ts:31-312` mirrors the same 19-key set and also has no lighting. So `getElementTypeRules('lighting')` falls through its inheritance chain (:exact → ELEMENT_TYPE_PARENT → suffix → `__default__`) to `__default__`. Consequently `resolveIntentStyle(instance, intent, 'lighting', state, viewType)` — the resolver `SymbolInjectionGate.ts:139-145` calls — has no family rules to resolve, so even after the layer is minted the gate would answer from `__default__` rather than from a user-visible lighting toggle. The founder's report is literally true for lighting and is the same defect he reports for rooms ('room' is a `VGCategory` at VGSceneApplicator.ts:65 but is likewise absent from this ELEMENT_TYPES list).",
      "evidence": [
        "packages/core-app-model/src/presentation/VisibilityIntentDefaults.ts:15-41 — the 18-entry `ELEMENT_TYPES` array; no `'lighting'`",
        "packages/core-app-model/src/presentation/VisibilityIntentDefaults.ts:32-38 — \"THIS is where a category is DECLARED: every element type in this list gets a complete `ElementGraphicsRules` … in every system intent and in every intent cloned from the defaults.\"",
        "packages/core-app-model/src/presentation/ElementTypeRegistry.ts:36-312 — registry keys measured by grep: wall, structural-wall, curtain-wall, slab, column, structural, beam, door, window, curtain-panel, stair, railing, roof, ceiling, furniture, plumbing, grid, annotation, level. No lighting.",
        "packages/core-app-model/src/presentation/VGSceneApplicator.ts:57-75 — the `VGCategory` union; no `'lighting'` member",
        "packages/core-app-model/src/presentation/VGSceneApplicator.ts:109-190 — `ELEMENT_TYPE_TO_VG_CATEGORY`; no `'Lighting'` row, so `getVGCategory('Lighting')` returns null (:157-161)",
        "packages/visibility/src/ — `grep -rn lighting packages/visibility/src` → ZERO hits",
        "packages/core-app-model/src/presentation/SymbolInjectionGate.ts:139-145 — `resolveIntentStyle(instance, intent, elementType, state, viewType, { elementType, category: elementType })` — the family key must be a declared category for this to mean anything"
      ],
      "userVisibleSymptom": "The Visibility Intent settings panel has no Lighting category. The founder cannot hide, restyle, re-colour or change the line type of light fittings, and nothing in the panel explains why.",
      "fix": "Add `'lighting'` to `VisibilityIntentDefaults.ELEMENT_TYPES` (:15-41) — one line, and the seeding machinery at :58+ then generates all four state appearances FROM the pen table rows that finding 3 activates, so no appearance values need authoring by hand. Add `'lighting'` to the `VGCategory` union (VGSceneApplicator.ts:57-75), a `'Lighting': 'lighting'` row to `ELEMENT_TYPE_TO_VG_CATEGORY` (:109) so the 3-D scene node responds to the same toggle, and `lighting: 'A-LGHT'` to `CATEGORY_TO_DXF_LAYER` (:87) so `applyToProjectionLayers` targets the layer the projector creates. Add a `lighting: _makeRules('lighting', {…})` entry to `ELEMENT_TYPE_REGISTRY` (ElementTypeRegistry.ts:31) mirroring the `plumbing` entry at :185-191. Then the gate call in EdgeProjectorService becomes `_symbolGate('lighting')` and it answers a real question. NOTE: the founder's parallel ask — \"the ROOMS should have a category on the visibility intent settings to modify the colours\" — is the SAME missing declaration ('room' exists as a VGCategory but not in ELEMENT_TYPES); flag it to whichever lane owns rooms so the two are not fixed twice.",
      "effort": "small",
      "risk": "medium",
      "confidence": "high",
      "alreadyExists": "The seeding machinery: `VisibilityIntentDefaults` builds complete four-state `ElementGraphicsRules` for every listed family straight from `resolvePen` + `defaultPocheFillForCategory`, so adding a name is the whole change. `SymbolInjectionGate` (packages/core-app-model/src/presentation/SymbolInjectionGate.ts) is the per-family veto, already built, already wired to both the injector block and the native mesh loop, and it fails open by construction. `_symbolGate('boundary-line')` at EdgeProjectorService.ts:4065 is the worked precedent for a newly-declared family key.",
      "filesToChange": [
        "packages/core-app-model/src/presentation/VisibilityIntentDefaults.ts",
        "packages/core-app-model/src/presentation/ElementTypeRegistry.ts",
        "packages/core-app-model/src/presentation/VGSceneApplicator.ts",
        "apps/editor/src/engine/views/EdgeProjectorService.ts"
      ]
    },
    {
      "title": "L-1332 is ALREADY OPEN for the symbol vocabulary: 46 fixture families share 11 bespoke marks and one generic disc — and the driver for a real symbol table is already authored",
      "mechanism": "`LightingPlanSymbolRenderer.drawLightingSymbol` (:88-174) switches on `fixtureType` with cases for exactly 10 families plus a `default: ctx.arc(...)`. `lightingSymbolExtentPx` (:65-86) mirrors the same 10 + default. The vocabulary is far larger: `LightingFixtureType` (LightingTypes.ts:45-66) hand-declares 12 members and then widens itself with `| Lod200FixtureId`, which is derived from `LOD200_FIXTURE_ROWS` — 34 rows measured by `grep -c \"^        id: '\"` on packages/schemas/src/lighting/Lod200FixtureCatalogue.ts. So 46 families, 11 marks. ISSUE-LOG.md:21018-21027 already records this as **L-1332, OPEN**: \"all twenty LOD-200 families draw a GENERIC plan symbol … A bollard, a troffer and an exit sign draw the same mark, which is wrong on a drawing that a contractor reads\" and it already names the right fix: \"a symbol per **archetype**, not per family.\" C96-ELEMENT-LIGHTING.md:1047 carries the same row. The archetype axis is authored and frozen: `Lod200Archetype` (Lod200FixtureCatalogue.ts:161-164) is 14 values — can, bar, disc, cone, post, arms, yoke, sign, dome, capsule, tube, bollard, globe_post, street_arm — and each row also carries `mount` ('ceiling'|'wall'|'floor'|'table'), `face`, `recessed?`, `isEmergency?` and real `lMm`/`wMm`/`dMm` dimensions. That is exactly the shape `TreePlanSymbolBuilder` already consumes for trees: `switch (def.archetype)` over 12 archetypes read from `TREE_SPECIES_TABLE`, with per-archetype drawer methods.",
      "evidence": [
        "packages/core-app-model/src/views/symbols/LightingPlanSymbolRenderer.ts:104-172 — `switch (type) { case 'downlight': … case 'floor_tripod_black': … default: { ctx.beginPath(); ctx.arc(cx, cy, rad, 0, Math.PI * 2); ctx.stroke(); } }`",
        "packages/core-app-model/src/views/symbols/LightingPlanSymbolRenderer.ts:59-61 — its own header: \"A new family added to the drawer's switch must be added here too, and `default` covers the twenty LOD-200 families exactly as the drawer's `default` does.\"",
        "docs/04-reference/ISSUE-LOG.md:21018-21027 — \"## L-1332 — ⛔ OPEN — all twenty LOD-200 families draw a GENERIC plan symbol … The fix is derivable the same way the geometry was: a symbol per **archetype**, not per family.\"",
        "docs/02-decisions/contracts/C96-ELEMENT-LIGHTING.md:1047 — \"| 4 | **No bespoke plan symbols.** | `LightingPlanSymbolRenderer` has a `default:` arm, so all twenty render a GENERIC symbol. They appear in plan; they are not distinguishable there. L-1332. |\"",
        "packages/schemas/src/lighting/Lod200FixtureCatalogue.ts:161-164 — `export type Lod200Archetype = | 'can' | 'bar' | 'disc' | 'cone' | 'post' | 'arms' | 'yoke' | 'sign' | 'dome' | 'capsule' | 'tube' | 'bollard' | 'globe_post' | 'street_arm';`",
        "packages/schemas/src/lighting/Lod200FixtureCatalogue.ts:192-302 — `Lod200FixtureRow` carries `archetype`, `mount`, `face`, `recessed?`, `isEmergency?`, `lMm`, `wMm`, `dMm` — every field a symbol table needs",
        "packages/geometry-furniture/src/builders/TreePlanSymbolBuilder.ts:240-253 — `switch (def.archetype) { case 'round_dense': this._drawRoundDense(main, def); break; … }` — the archetype-dispatch pattern to copy",
        "packages/core-app-model/src/lighting/Lod200FixtureCatalogue.ts:1-17 — \"⛔ Do not add a declaration here. Add the row to the matrix.\" — the matrix is the single authority, so the symbol table must key off it"
      ],
      "userVisibleSymptom": "An exit sign, a bollard, a troffer panel and a recessed downlight all draw the same small circle. A contractor cannot read the fitting schedule off the plan.",
      "fix": "Build the symbol geometry as a pure module `packages/geometry-lighting/src/LightingSymbolGeometry.ts` exporting `buildPlanLinework(fixture): number[]`, keyed on `lod200Row(fixtureType)?.archetype` with a fallback branch for the 12 legacy hand-declared types — 14 archetype drawers plus 12 legacy, not 46. Size each mark from the row's real `lMm`/`wMm` rather than a second radius table, and reuse the existing per-family `lightingSymbolExtentPx` values for the 12 legacy families so today's marks are byte-comparable. Draw the standard AEC marks the archetypes imply: `can`/`disc` = circle + crosshair (recessed) or circle + centre dot (surface); `bar`/`tube` = the real rectangle at `lMm × wMm`; `sign` = rectangle with the running-man box and a directional arrow; `bollard`/`globe_post` = circle with a filled centre; `arms` = a chandelier rosette with `row.arms` radial spokes; `cone` = concentric circle pair; `yoke`/track head = circle with a tail in the aim direction. Add the two life-safety overlays `isEmergency` (a filled quadrant, per convention) and `recessed` (dashed outline) as MODIFIERS applied after the archetype drawer, so a 47th row needs no new drawer. Keep it pure (no THREE, no store) exactly as `PlumbingSymbolGeometry` is, so it is unit-testable and so the builder file stays ~110 lines. Close L-1332 and C96 §gap-row 4 in the same commit.",
      "effort": "medium",
      "risk": "low",
      "confidence": "high",
      "alreadyExists": "`LOD200_FIXTURE_ROWS` (34 rows) + `Lod200Archetype` (14 values) + `lod200Row(id)` lookup at packages/schemas/src/lighting/Lod200FixtureCatalogue.ts:956 — the whole driver, authored, frozen and already the single authority for the 3-D geometry. `lightingSymbolExtentPx` (LightingPlanSymbolRenderer.ts:65-86) — the existing per-family size table. `drawLightingSymbol` (:88-174) — 10 symbol shapes already designed, needing only Canvas2D-arc → line-segment conversion. `LineworkBuf` (TreePlanSymbolBuilder.ts:78-135) — circle/bumpyCircle/smallDot/plusMark/tick helpers, copy-paste ready. `PlumbingSymbolGeometry.buildPlanLinework` — the pure-geometry-module shape.",
      "filesToChange": [
        "packages/geometry-lighting/src/LightingSymbolGeometry.ts",
        "packages/core-app-model/src/views/symbols/LightingPlanSymbolRenderer.ts",
        "docs/04-reference/ISSUE-LOG.md",
        "docs/02-decisions/contracts/C96-ELEMENT-LIGHTING.md"
      ]
    },
    {
      "title": "A reflected ceiling plan is where light fittings belong, and the injector view-type set deliberately EXCLUDES 'ceiling-plan' — a new lighting builder must not reuse it",
      "mechanism": "`PLAN_SYMBOL_INJECTION_VIEW_TYPES` (EdgeProjectorService.ts:325-327) is `new Set(['plan','detail','structural-plan'])`. Its header at :304-324 states the exclusion is deliberate: \"'ceiling-plan' is EXCLUDED ON PURPOSE. Every symbol behind this gate describes the floor plane; a reflected ceiling plan draws the ceiling … An RCP's own symbols — light fittings — are drawn on the canvas by `PlanViewSymbolRenderer.renderLightingPlanSymbols`, which DOES admit 'ceiling-plan'.\" So the ONE family whose symbol an RCP must show is the one family the injector block cannot serve. If the new `LightingPlanSymbolBuilder` is dropped inside that `if (PLAN_SYMBOL_INJECTION_VIEW_TYPES.has(...))` block, RCPs lose their lighting symbols the moment the canvas renderer is retired — a silent regression on the drawing type that most needs them.",
      "evidence": [
        "apps/editor/src/engine/views/EdgeProjectorService.ts:325-327 — `const PLAN_SYMBOL_INJECTION_VIEW_TYPES: ReadonlySet<string> = new Set([ 'plan', 'detail', 'structural-plan', ]);`",
        "apps/editor/src/engine/views/EdgeProjectorService.ts:310-315 — \"⛔ `'ceiling-plan'` is EXCLUDED ON PURPOSE … An RCP's own symbols — light fittings — are drawn on the canvas by `PlanViewSymbolRenderer.renderLightingPlanSymbols`, which DOES admit `'ceiling-plan'`.\"",
        "apps/editor/src/engine/views/plan-canvas/PlanViewSymbolRenderer.ts:16 — `if (viewType !== 'plan' && viewType !== 'ceiling-plan' && viewType !== 'structural-plan') return;`",
        "packages/core-app-model/src/views/PlanViewCanvas.ts:2293-2295 — the LIVE copy of the same three-view gate",
        "apps/editor/src/engine/views/EdgeProjectorService.ts:4441-4451 — `resolveClipRange`'s RCP branch: `const near = ceilingHeight; const far = ceilingHeight + 0.5;` — an RCP's own Y window, which a ceiling fixture sits inside"
      ],
      "userVisibleSymptom": "If the fix is written as one more line in the existing injector block, reflected ceiling plans silently lose their light fittings — the only symbol an RCP exists to carry.",
      "fix": "Give the lighting injector its OWN named view-type set — `LIGHTING_SYMBOL_INJECTION_VIEW_TYPES = new Set(['plan','ceiling-plan','structural-plan','detail'])` — declared next to `PLAN_SYMBOL_INJECTION_VIEW_TYPES` with a comment saying WHY it differs (a luminaire is a ceiling-plane symbol, not a floor-plane one), rather than widening the existing set. The file already carries five rival plan-family literals and names the divergence as open (L-5405 at :318-324); adding a sixth that is DELIBERATE and EXPLAINED is correct, silently widening a set whose header says the exclusion is on purpose is not. Mirror the same set in the canvas gate so the two cannot drift.",
      "effort": "small",
      "risk": "low",
      "confidence": "high",
      "alreadyExists": "`resolveClipRange`'s RCP branch (EdgeProjectorService.ts:4441-4451) already computes the correct near/far window for a ceiling plan, and `apps/editor/__tests__/rcpIsNotAPlanWithAFlippedCamera.test.ts:229` already has a guard test asserting that this line gates the LIGHTING renderer and that admitting an RCP there is correct — extend that test rather than writing a new one.",
      "filesToChange": [
        "apps/editor/src/engine/views/EdgeProjectorService.ts",
        "apps/editor/__tests__/rcpIsNotAPlanWithAFlippedCamera.test.ts"
      ]
    },
    {
      "title": "Dead duplicate: `planViewSymbolRenderer` in apps/editor is constructed and exported but imported by nothing — a second lighting symbol path that has already been cited as live in three comments",
      "mechanism": "`apps/editor/src/engine/views/plan-canvas/PlanViewSymbolRenderer.ts:144` exports `export const planViewSymbolRenderer = new PlanViewSymbolRenderer();`. A repo-wide grep for that identifier and for `renderLightingPlanSymbols` across apps/, packages/, src/ and plugins/ returns only: the definition itself, three COMMENTS that cite it as the live path, one test comment, and the genuinely-live `PlanViewCanvas._renderLightingPlanSymbols` (:660, :2092, :2292). Nothing imports the apps/editor class. So `EdgeProjectorService.ts:314` and `:3929` and `initTools.ts:3155` all point a future reader at a file that does not run — and `initTools.ts:3155` cites BOTH copies as if they were two live paints. This matters here specifically because it is the file a lane would edit first when told \"lighting symbols are rendered on the canvas\".",
      "evidence": [
        "apps/editor/src/engine/views/plan-canvas/PlanViewSymbolRenderer.ts:144 — `export const planViewSymbolRenderer = new PlanViewSymbolRenderer();`",
        "grep -rn \"planViewSymbolRenderer\" apps packages src plugins → 1 hit, the definition line above; no importer",
        "apps/editor/src/engine/views/EdgeProjectorService.ts:3929 — \"`plan-canvas/PlanViewSymbolRenderer.renderLightingPlanSymbols` admits 'plan', 'ceiling-plan' AND 'structural-plan' (`:16`)\" — cites the dead copy",
        "apps/editor/src/engine/initTools.ts:3154-3157 — \"painted by `PlanViewCanvas._renderLightingPlanSymbols()` and by `views/plan-canvas/PlanViewSymbolRenderer.ts`\" — names both as if both run",
        "packages/core-app-model/src/views/PlanViewCanvas.ts:660, :2092 — `this._renderLightingPlanSymbols();` — the only live call sites"
      ],
      "userVisibleSymptom": "None directly. It is a navigation trap: an implementer told to change the lighting symbol edits the copy that does not run, sees no change, and concludes the symbol is unreachable.",
      "fix": "Verify with `grep -rn \"planViewSymbolRenderer\\|PlanViewSymbolRenderer\" apps packages src plugins` (expect: definition + comments only), then delete `renderLightingPlanSymbols` from the apps/editor class if the rest of the class has live callers, or delete the file if it has none — and correct the three comments at EdgeProjectorService.ts:314, :3929 and initTools.ts:3155 to name `PlanViewCanvas._renderLightingPlanSymbols()` as the single live path. Do this BEFORE the symbol work, not after, so the lane is not aimed at the wrong file.",
      "effort": "small",
      "risk": "low",
      "confidence": "high",
      "alreadyExists": "none found",
      "filesToChange": [
        "apps/editor/src/engine/views/plan-canvas/PlanViewSymbolRenderer.ts",
        "apps/editor/src/engine/views/EdgeProjectorService.ts",
        "apps/editor/src/engine/initTools.ts"
      ]
    }
  ],
  "whatIcouldNotEstablish": "1. **I could NOT prove the founder's specific screenshot marks are lighting rather than trees.** The rival is strong and I name it: `TreePlanSymbolBuilder` injected 4 tree symbols in the same session, and its `_drawRoundDense` (:266-278) emits `bumpyCircle(0,0,r,64,…)` plus ~14 radial `tick()` spikes plus `smallDot` scatter — which also reads as \"a dense circular hatch symbol\". What tips it toward lighting is the word CONCENTRIC in the brief's description: a tree canopy is ONE ring plus spokes, whereas a luminaire's `TorusGeometry(r*0.92, r*0.10, 8, 28)` bezel edge-projects as EIGHT nested 28-gon rings plus 28 radial spokes inside a 6.5 cm disc. That is a reasoned discrimination, not a measurement. **The one-line probe that settles it: in `EdgeProjectorService`, set `EPS_VERBOSE` or read the existing `§DIAG-EPS-01` lines and count how many carry `elemType=Lighting` versus how many tree symbols were injected on the level in the screenshot (the log quoted is L0; the screenshot is L2).** Do this before touching anything — the suppression fix is correct regardless, but the priority ordering depends on it.\n\n2. **I could not measure whether `HiddenLineRemoval` removes any lighting segments.** Reasoning only: a ceiling fixture at 2.7 m is the nearest solid to a plan viewer, `minProjectionOccluderDepth: 0` (EdgeProjectorService.ts:4220) rejects above-cut-plane geometry as an OCCLUDER, and nothing above it is in range to occlude it — so I expect zero removal. The log's `239.5 segment-equivalent(s) removed` across 26 occluders is not attributed per element, so this is unverified.\n\n3. **I did not run the app, any test, or any gate.** Every claim above is a read of source at a cited line. In particular I did not verify at runtime that the cyan symbol and the black blob are visually coincident — I established only that the paint order puts the symbol second (PlanViewCanvas.ts:430 traverse, then :660/:2092 symbol call).\n\n4. **UNKNOWN — whether ceiling-mounted fixtures SHOULD appear in a floor plan at all.** `classifyByVertexY` (EdgeProjectorService.ts:1073-1128) has a lower cull and NO upper cull, so every plan draws the full storey up to `far + 0.5` including ceiling services. Conventionally a floor plan shows what is below the cut plane and light fittings live on the RCP. This may be the deeper defect and it is NOT lighting-specific — it would equally affect ceiling-mounted ductwork, sprinklers and soffits when those families arrive. I deliberately did not fold it into the fix: changing the upper cull moves geometry on every plan in the product and nothing has measured that. Recommend it be opened as its own row rather than absorbed into a lighting lane.\n\n5. **I did not check the elevation/section half of the founder's ask.** `TreeElevationSymbolBuilder` and `PlumbingElevationSymbolBuilder` exist with a `skipInElevation` sibling flag (EdgeProjectorService.ts:2890-2896); there is no lighting elevation symbol and I did not audit whether one is wanted. A pendant in section is legitimate cut geometry, so `skipInSection` should almost certainly stay false — but that is an assertion I did not test.\n\n6. **`C102-VIEW-AND-SHEET-INTEGRITY.md` contains zero occurrences of \"lighting\" or \"luminaire\"** (grep -i, 0 hits). So the contract that governs sheet integrity has nothing to say about whether a lighting symbol must survive to the sheet — the C96 §gap row 4 and ISSUE-LOG L-1332 are the only written authority, and both address the SYMBOL VOCABULARY, not the canvas-vs-drawing sink. The sink finding (finding 2) appears to be previously unrecorded; it should get an L-row of its own before the fix, per this repo's own convention."
}