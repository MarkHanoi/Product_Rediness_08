{
  "dimension": "The auto-save that fires during project open (PlatformSaveController / SaveOrchestrator / VersionRepository / temporalGraph / compress path)",
  "summary": "Yes — a full serialise + checksum + deflate + IndexedDB container write really does happen on open, and it is not one save but a chain of avoidable work. Opening a project with local history runs `ProjectSerializer.serialize()` at least TWICE (once synchronously on the critical path via `resetDirtyAfterLoad()`, once ~2.5 s later via the post-load autosave's `getHash()`), and each of those unconditionally builds a 1.46 MB canonical string and FNV-walks it for an integrity stamp that the dirty-check throws away — the founder's own log gives that size away: `integrity 44c4f1ed-16588a`, and `0x16588a` = 1,464,970 canonical chars (SnapshotIntegrity.ts:360-363). I measured the checksum leg at 45.3 ms and the record deflate at 49.9 ms on a founder-shaped fixture 0.65× his size, and the compression worker is guaranteed NOT ready for the first save of a session, so that deflate runs on the main thread. The \"6 versions in one write\" is real for I/O but is the WRONG place to look: re-compression is already incremental (L-5801) and the whole-container JSON.parse+stringify measures 3 ms + 5 ms at 10 versions in my own run of `perf104OpenPath.spec.ts` — I am de-ranking it deliberately. The two thumbnail captures are two distinct call sites, both of which force a full synchronous render of a 3762-mesh scene. The deepest finding is that `temporalGraphManager.resumeRecording()` fires in ProjectLoader's `finally` BEFORE the post-load room-redetect / wall-join sweep, so every open mints new journal records permanently AND moves the content hash — which is precisely why an \"Auto-save\" is written on every open in the first place.",
  "whatIcouldNotEstablish": "(1) THE STORE WALK. I never measured `ProjectSerializer.serialize()` itself — the ~30-store walk + `deepStrip` + `structuredClone` legs. It needs a live browser with populated stores; my bench measures only the checksum/stringify/deflate legs that operate on an already-built snapshot object. Every \"per serialize\" number I give EXCLUDES the walk, so my totals are floors, not estimates of the whole. (2) THUMBNAIL COST. `captureThumbnail()` forces `rpm.render()` + `getImageData` + up to 6 `toDataURL('image/webp')` calls; none of that is measurable outside a browser. The code's own comment (PlatformSaveController.ts:522-527) claims it \"historically caused a ~2.5 s LONGTASK\", but that is a superseded comment justifying the 3500 ms delay, not a current measurement — do not quote it as one. (3) HOW MANY journal records ONE open adds. I established the mechanism (resume-before-sweep) from code, but not the magnitude; the repo's own \"2 056 -> 5 361 records in one day\" (perf104OpenPath.spec.ts:29) mixes real edits with open-artifacts and cannot separate them. (4) NODE vs CHROME. My ms figures are Node/V8 via tsx on this machine, linearly extrapolated by canonical-length ratio to the founder's snapshot. Same engine family, but not his machine and not his browser. (5) WHETHER THE FOUNDER'S OPEN ACTUALLY BLOCKED on any of this — no `performance` marks for the save legs appear in his paste, so ordering in that console is the only sequencing evidence I have.",
  "findings": [
    {
      "title": "serialize() unconditionally computes a 1.46 MB canonical-string integrity checksum — including for the two dirty-check-only callers that throw it away",
      "mechanism": "`ProjectSerializer.serialize()` ends with an UNCONDITIONAL `computeSnapshotChecksumWithReport(snapshot)` (ProjectSerializer.ts:2039). That builds a full canonical string of the entire snapshot and FNV-1a walks every code unit of it (SnapshotIntegrity.ts:349-368). But `SaveOrchestrator.getHash()` (PlatformSaveController.ts:112-116) calls serialize() purely to produce a dirty-check hash; the checksum inside it is computed and discarded. That happens at least twice on open: `resetDirtyAfterLoad()` (SaveOrchestrator.ts:589-591, called from PlatformVersionController.ts:412 SYNCHRONOUSLY right after the load overlay is removed and BEFORE `pryzm-project-loaded` is emitted), and again in `executeSave()` (SaveOrchestrator.ts:382). It also fires on every autosave cycle whose hash turns out UNCHANGED (SaveOrchestrator.ts:383-388 — 'Content hash unchanged — save skipped'), i.e. for a save that is then not made at all.",
      "evidence": [
        "apps/editor/src/engine/persistence/ProjectSerializer.ts:2039 — `const __integrityReport = computeSnapshotChecksumWithReport(snapshot);` (no guard, no option, runs on every serialize)",
        "packages/persistence-client/src/loader/SnapshotIntegrity.ts:360-363 — `function stamp(canonical: string): string { return fnv1a(canonical) + '-' + (canonical.length >>> 0).toString(16); }` — the suffix of the founder's own stamp `integrity 44c4f1ed-16588a` therefore decodes to 0x16588a = 1,464,970 canonical characters for ONE snapshot. That number is MEASURED, from his log.",
        "apps/editor/src/ui/platform/PlatformSaveController.ts:112-116 — getHash(): `const snapshot = ctx.saveAdapter.serialize({...}); const json = ctx.saveAdapter.stringify(snapshot);` — the checksum is inside serialize() and is never read by this caller.",
        "apps/editor/src/ui/platform/SaveOrchestrator.ts:589-591 — `resetDirtyAfterLoad(): void { try { const hash = this.getHash(); ... }` and apps/editor/src/ui/platform/PlatformVersionController.ts:411-412 — `this.saveCtrl.orchestrator.setLoading(false); this.saveCtrl.orchestrator.resetDirtyAfterLoad();` — synchronous, on the open path.",
        "MEASURED (my bench, Node/V8 this machine, founder-shaped fixture: 503 elements + 2366 mutations, canonical 956,859 chars): `computeSnapshotChecksumWithReport` median 45.3 ms (min 37.5 max 48.9) vs `JSON.stringify(snapshot)` median 7.1 ms. The checksum costs 6.4x the stringify. Scaled by canonical length to the founder's 1,464,970 chars: ~69 ms per serialize()."
      ],
      "estimatedCostMs": 138,
      "costBasis": "estimated",
      "fix": "Add an option to `ProjectSerializer.serialize()` — e.g. `{ skipIntegrity: true }` — and pass it from `PlatformSaveController.getHash()` ONLY. Leave the stamp on the path that persists (`saveVersionInternal`'s fallback serialize at :270, export at :450). Caveat that must be handled, not hand-waved: the auto-save path REUSES the getHash() snapshot as the persisted record (the §PERF-AUTOSAVE-SINGLE-SERIALIZE stash at :245-275), so a stash built without a checksum must have one stamped before it becomes a VersionRecord — otherwise every autosaved snapshot loses its integrity block and L-334/L-360 verification silently stops working. Cheapest safe shape: keep the stash, and stamp the checksum lazily in `saveVersionInternal` just before building the `VersionRecord`. Net saving: one whole checksum walk per open (the resetDirtyAfterLoad one, which is never persisted) plus one per no-op autosave, forever.",
      "risk": "medium",
      "confidence": "high",
      "whatItWouldBreak": "L-334/L-360: the snapshot content checksum that lets LOAD detect byte-corruption/truncation. If the lazy re-stamp is botched, autosaved versions ship with no `integrity` block, and the loader treats absent-checksum as legacy-and-clean (SnapshotIntegrity.ts, ABSENT -> `{present:false, ok:true}`) — so corruption would stop being detectable with NO error anywhere. That is the exact 'silently stops protecting' shape this repo keeps re-learning, so the stamp must move, never disappear.",
      "filesToChange": [
        "apps/editor/src/engine/persistence/ProjectSerializer.ts",
        "apps/editor/src/ui/platform/PlatformSaveController.ts",
        "packages/persistence-client/src/loader/ProjectSerializer.ts"
      ]
    },
    {
      "title": "temporalGraph recording RESUMES before the post-load rebuild sweep — so every open permanently grows the journal AND is why an Auto-save is minted on every open",
      "mechanism": "§FIX-TEMPORAL-LOAD-REPLAY-RATCHET (L-5820) suspends mutation recording for the element-restore replay: `temporalGraphManager.suspendRecording()` at ProjectLoader.ts:759, `resumeRecording()` at ProjectLoader.ts:2902. But :2902 sits in the `finally` block IMMEDIATELY after `storeEventBus.endBatch()` and BEFORE the post-load sweep that the same file schedules next — `wallRebuildControl.resumeAndFlush()` (deferred per-level WallJoinResolver passes), `topologyObserver.resume()`, and the chunked per-level `room.redetect` dispatches. Every store mutation those produce IS recorded (TemporalGraph.ts:420-441 mints a record per create/update/delete from the StoreEventBus subscription installed by `temporalGraphManager.init()` at initDataPlatform.ts:288). Two consequences, and they are the same fact: (1) the journal grows on every open, and it is embedded WHOLE in every snapshot (ProjectSerializer.ts:1897 `temporalGraph: temporalGraphManager.serialize()`; TemporalGraph.ts:372-376 returns `mutations: [...this._mutations]`); (2) the post-sweep model is no longer byte-equal to the snapshot that was loaded, so the content hash set by `resetDirtyAfterLoad()` no longer matches the hash computed in `executeSave()` — which is exactly why the founder's console shows `Version saved: \"Auto-save\" (503 elements)` on an open in which he changed nothing.",
      "evidence": [
        "apps/editor/src/engine/persistence/ProjectLoader.ts:2902 — `temporalGraphManager.resumeRecording();` with the comment 'resume the INSTANT the replay flush is done, and not one statement earlier: everything the buffer just delivered is a restore, everything after it is the user.' The comment's premise is FALSE for the next ~130 lines: what follows is `wallRebuildControl?.resumeAndFlush?.()`, `topologyObserver?.resume?.()` and the per-level `room.redetect` dispatch loop — machine work, not the user.",
        "apps/editor/src/engine/persistence/ProjectLoader.ts:759 — `temporalGraphManager.suspendRecording();` (the suspend half IS wired in production; an earlier grep of mine missed it and was wrong)",
        "packages/core-app-model/src/TemporalGraph.ts:441 — `this._mutations.push(record);`, reached from the StoreEventBus subscriber at :94-106; guarded only by `if (this._suspendDepth > 0) return;` at :429",
        "apps/editor/src/engine/persistence/ProjectSerializer.ts:1897 — `temporalGraph: temporalGraphManager.serialize(),` — embedded whole, in every snapshot, in every version",
        "apps/editor/src/ui/platform/SaveOrchestrator.ts:382-388 — `const currentHash = this.getHash(); if (this.lastHash !== '' && currentHash === this.lastHash) { ... save skipped }` — the save proceeds only because the sweep moved the hash",
        "FOUNDER'S LOG: `temporalGraph 2366 mutations / 0 edges` and `PlatformSaveController] Version saved: \"Auto-save\" (503 elements)` — 6 stored versions on a project he says he only opens"
      ],
      "estimatedCostMs": -1,
      "costBasis": "read-from-code",
      "fix": "Move `resumeRecording()` from the `finally` at :2902 to AFTER the post-load sweep has drained — the natural site is the same place that fires `pryzm-load-suppress-end` (`__closeAutosaveSuppress`, ProjectLoader.ts:651), which by construction already means 'the load AND its fire-and-forget post-load sweep have fully drained'. The suppression window and the recording window should be the SAME window; today they are not, and the autosave suppression is the one that already got this right. Second-order benefit: with the sweep's mutations no longer recorded, the post-sweep hash is far more likely to equal `resetDirtyAfterLoad()`'s baseline, and the whole post-load autosave stops firing at all on an open with no user edits.",
      "risk": "medium",
      "confidence": "high",
      "whatItWouldBreak": "The invariant `resumeRecording()` protects at its current position is 'a fatal load error can never leave the manager permanently deaf to real edits' (its own comment). Moving it out of `finally` reintroduces that hazard, so the move must keep a resume on every exit path — `__closeAutosaveSuppress` already has that discipline (idempotent `__suppressClosed` latch, called from both the drain and the finally-tail), which is why it is the right host. Also: real user edits made DURING the sweep window would go unrecorded. That window is machine-owned and the model is not yet interactive, but it is a genuine narrowing of what the design journal claims to be.",
      "filesToChange": [
        "apps/editor/src/engine/persistence/ProjectLoader.ts"
      ]
    },
    {
      "title": "The FIRST save of every session deflates ~1.5 MB on the main thread, because the compression worker is lazily constructed and isReady() is guaranteed false on its first call",
      "mechanism": "`_persistSlots` routes new version blobs to the compression worker only `if (pool.isReady())` (ProjectRepository.ts:2647); otherwise it falls through to the synchronous branch at :2712 — `for (const n of need) { const b = _compressJSON(n.json); ... }` — which is `encodeCompressed` -> `deflateSync` from fflate (compressCodec.ts:45-52), on the main thread. `CompressWorkerPool.isReady()` (CompressWorkerPool.ts:119-122) calls `this._ensure()` and returns `this._ready`, and its own doc says: 'Constructs the worker lazily on the first call (which therefore returns false — the worker becomes ready shortly after, so subsequent saves route through it).' The post-load autosave IS the first save of the session, so it is the one save per session that is GUARANTEED to take the synchronous path, and it is the one that lands on the open path.",
      "evidence": [
        "apps/editor/src/workers/CompressWorkerPool.ts:114-122 — `isReady(): boolean { this._ensure(); return this._ready && !this._dead && this._worker !== null; }` with the doc 'Constructs the worker lazily on the first call (which therefore returns false...)'",
        "apps/editor/src/ui/platform/ProjectRepository.ts:2647 `if (pool.isReady()) {` ... :2710-2712 `// Worker not ready (first save of the session / unavailable) -> compress the NEW version(s) synchronously.` then `for (const n of need) { const b = _compressJSON(n.json); ... }`",
        "apps/editor/src/workers/compressCodec.ts:45-52 — `export function encodeCompressed(json: string): string { const compressed = deflateSync(strToU8(json), { level: 1 }); return COMPRESSED_MARKER + _uint8ToBase64(compressed); }` — synchronous, plus a chunked base64 pass over the output",
        "MEASURED (my bench, founder-shaped 956,859-char record): `encodeCompressed(record JSON)` median 49.9 ms (min 43.8 max 128.2). Scaled to the founder's 1,464,970 chars: ~76 ms.",
        "MEASURED (my own run of apps/editor/src/ui/platform/__tests__/perf104OpenPath.spec.ts, 1.63 MB / 10-version container): 'DEFLATE one changed version 54 ms', 'DEFLATE the journal TAIL chunk 48 ms'"
      ],
      "estimatedCostMs": 80,
      "costBasis": "estimated",
      "fix": "Warm the pool once during boot, off the critical path: a single `getCompressWorkerPool().isReady()` inside a `requestIdleCallback` after runtime compose (the call itself is what constructs the worker; the handshake completes asynchronously). By the time the post-load autosave fires ~2.5 s after the load settles, `isReady()` returns true and the deflate goes off-thread. This is a one-line warm with no behavioural change — the sync path stays as the fallback it already is.",
      "risk": "low",
      "confidence": "high",
      "whatItWouldBreak": "none known. The worker path is already the default and already has a synchronous fallback on construction failure, worker error, and dispatch timeout (CompressWorkerPool.ts:79-86, :107, :144). Warming earlier only changes WHEN the worker is constructed. One thing to check before shipping: constructing a module Worker during boot competes for bandwidth with the engine chunks, so it belongs in an idle callback, not inline.",
      "filesToChange": [
        "apps/editor/src/workers/CompressWorkerPool.ts",
        "apps/editor/src/engine/initPersistence.ts"
      ]
    },
    {
      "title": "Two thumbnail captures per open, from two distinct call sites — each forces a synchronous full render of the 3762-mesh scene, a GPU readback, and up to six WebP encodes",
      "mechanism": "There are exactly two production callers of `saveAdapter.captureThumbnail()` in the editor, and BOTH fire on every open: (1) the autosave path, `PlatformSaveController.ts:357`, inside `saveVersionInternal`; (2) the post-load path, `PlatformSaveController.ts:551`, inside the handler `schedulePostLoadThumbnailCapture` (:521) registers on `pryzm-project-loaded`, which runs `CAPTURE_DELAY_MS = 3500` ms later (:527) wrapped in `requestIdleCallback`. Neither knows about the other. Each capture, in `initPersistence.ts:141-311`, does: a FORCED synchronous render (`const rpm = window.renderPipelineManager; if (rpm) rpm.render();` at :175-176), `ctx.drawImage(src, ...)` then `ctx.getImageData(...)` at :218 (a GPU->CPU readback that stalls the pipeline), and then `fitThumbnailToBudget(encodeAt)` at :288, which walks a six-rung ladder of `toDataURL('image/webp', q)` calls (thumbnailBudget.ts:68-74) until the payload fits 65,536 chars. Each successful capture then fires an independent `_uploadThumbnailToServer` PATCH. The founder's `[captureThumbnail] Thumbnail captured` appearing TWICE is these two sites, once each — NOT one site called twice, and NOT `setProjectContext` running twice (the doubled socket join is a separate pair of modules: initCollaboration.ts:588 and PlatformCollabPill.ts:190).",
      "evidence": [
        "apps/editor/src/ui/platform/PlatformSaveController.ts:357 — `: (this.ctx.saveAdapter.captureThumbnail?.() ?? null);` (autosave path)",
        "apps/editor/src/ui/platform/PlatformSaveController.ts:551 — `const thumb = this.ctx.saveAdapter.captureThumbnail?.() ?? null;` (post-load path, scheduled at :521-527 with `const CAPTURE_DELAY_MS = 3500;`)",
        "apps/editor/src/engine/initPersistence.ts:174-176 — `if (pryzmCanvas) { try { const rpm = window.renderPipelineManager; if (rpm) rpm.render(); } catch {} }` — a forced full render, synchronous, inside the capture",
        "apps/editor/src/engine/initPersistence.ts:218 — `const { data } = ctx.getImageData(sampleX, sampleY, sampleW, sampleH);` — GPU->CPU readback",
        "packages/core-app-model/src/preview/thumbnailBudget.ts:68-74 — a six-rung ladder `{q:0.72,s:1} {0.55,1} {0.40,1} {0.55,0.7} {0.40,0.5} {0.30,0.35}`, each rung a separate `toDataURL('image/webp')`",
        "grep for `captureThumbnail` across apps/ + src/ returns only these two call sites plus the implementation, the interface decl (PlatformShellTypes.ts:128) and a null stub (src/main.ts:586)"
      ],
      "estimatedCostMs": 250,
      "costBasis": "estimated",
      "fix": "Make the two sites aware of each other. Cheapest correct shape: `schedulePostLoadThumbnailCapture` already holds the project id and a one-shot handler — have `saveVersionInternal` set a per-project 'thumbnail captured at' timestamp when it captures, and have BOTH sites skip if a capture for this project happened within the last N seconds. Prefer keeping the POST-LOAD one and skipping the autosave one on the open path: the post-load site is already idle-scheduled and already delayed to let the GPU settle, which is why it is the one that reliably returns a non-blank frame (initPersistence.ts:229-236 documents the WebGPU swapchain race that makes the early capture return null). That also halves the thumbnail PATCHes.",
      "risk": "low",
      "confidence": "high",
      "whatItWouldBreak": "The autosave-path capture exists so the hub card is refreshed on every save, not only on open (PlatformSaveController.ts:340-350). Suppressing it must be time-windowed, not unconditional, or a long editing session stops refreshing its preview. Also `capturedThumb ?? existingMeta?.thumbnail` at :382 means a skipped capture correctly reuses the last good thumbnail — that fallback is what makes the skip safe.",
      "filesToChange": [
        "apps/editor/src/ui/platform/PlatformSaveController.ts"
      ]
    },
    {
      "title": "The journal-retention valve cannot fire for this project — it needs the 20-slot version ring to EVICT, and the founder has 6 versions",
      "mechanism": "§JOURNAL-RETENTION (L-11542) is the only thing that ever shrinks the stored design journal. It works in two halves: (a) at save, `saveVersionWithMeta` raises the release cursor `rel` ONLY inside `if (storedJournal !== null && _journalRetentionEnabled() && slots.length > kept.length)` (ProjectRepository.ts:2285) — i.e. only when a version was actually pushed out of `slots.slice(-MAX_VERSIONS_STORED)` (:2273, `MAX_VERSIONS_STORED = 20` at :153); (b) at open, `_releaseJournalHead` (:1867) returns null immediately unless `j.rel > 0` (:1878) and then drops only WHOLE SEALED chunks, `dropChunks = Math.floor(releasable / k)` with `k = 2000` (:1888-1890, JOURNAL_CHUNK_RECORDS at :268). The founder has SIX versions. The ring has never evicted, so `rel` has never been raised above 0, so `_releaseJournalHead` has never released a single record — and even once the ring fills at 20, nothing is released until 2000 records fall out of the retained window. Until then the journal is strictly append-only in storage, and it is embedded whole in every one of the 6 (soon 20) stored snapshots.",
      "evidence": [
        "apps/editor/src/ui/platform/ProjectRepository.ts:2273 `const kept = slots.slice(-MAX_VERSIONS_STORED);` and :2285 `if (storedJournal !== null && _journalRetentionEnabled() && slots.length > kept.length) {` — the release cursor moves ONLY on eviction",
        "apps/editor/src/ui/platform/ProjectRepository.ts:153 — `const MAX_VERSIONS_STORED = 20;`",
        "apps/editor/src/ui/platform/ProjectRepository.ts:1878 — `if (!j || !j.rel || j.rel <= 0 || !Array.isArray(j.c) || j.c.length === 0) return null;` and :1890 `if (dropChunks <= 0) return null;`",
        "apps/editor/src/ui/platform/ProjectRepository.ts:268 — `const JOURNAL_CHUNK_RECORDS = 2000;`",
        "FOUNDER'S LOG: `6 version(s) persisted to IndexedDB ... 2366 journal record(s) shared across 6 version(s) in 2 chunk(s)` — 6 versions, so zero evictions, so zero releases, and 2366 records means one sealed 2000-chunk plus a 366-record tail",
        "MEASURED (my perf104 run): 'inflate + digest-verify the whole journal 31 ms (5.783 ms/1000 records)' at 5361 records -> ~14 ms per open at his 2366, growing linearly; the same file projects '~310 ms per open, ~8259 KB stored' at 53,610 records"
      ],
      "estimatedCostMs": 14,
      "costBasis": "estimated",
      "fix": "Two independent options, and they are not alternatives — do the first regardless. (1) Fix the recording window (see the resumeRecording finding) so opens stop minting records; that removes the growth term. (2) Give the retention valve a second trigger that does not depend on the ring evicting: a hard record ceiling (e.g. release sealed chunks beyond the newest ~4000 records) with the SAME 'no retained version references it' floor check that `_releaseJournalHead` already computes from the envelope at :1885-1888. Note the honest cost of (2) already documented in the code at :1855-1863: released records make older versions' integrity stamps NOT COMPARABLE, which the loader already reports as a named disposition rather than corruption.",
      "risk": "medium",
      "confidence": "high",
      "whatItWouldBreak": "The invariant `_releaseJournalHead` protects is 'nothing a RETAINED version references may go, whatever `rel` claims' (:1885-1888, the defensive `minRef` re-derivation). Any new trigger MUST keep that floor. Beyond that, releasing records makes pre-release version stamps non-comparable — the code already chose that disposition over re-stamping, and re-stamping from the storage layer is explicitly named as the L-334/L-360/L-8700 ignition condition (:1857-1860). Do not 'fix' the non-comparable stamps.",
      "filesToChange": [
        "apps/editor/src/ui/platform/ProjectRepository.ts"
      ]
    },
    {
      "title": "\"temporalGraph 2366 mutations / 0 edges\" is EXPECTED, not a bug — recordEdge has zero production callers — but the journal is still ~32% of every snapshot",
      "mechanism": "`TemporalGraph` has two append-only halves. `_mutations` is populated automatically by the StoreEventBus subscription installed in `init()` (TemporalGraph.ts:94-106, wired at initDataPlatform.ts:288). `_edges` is populated ONLY by explicit `recordEdge()` calls, which the file's own header at :24-27 instructs command authors to make. Nothing ever did. The repo states this as a finding in its own product surface: apps/editor/src/ui/analysis/graphReadModel.ts:311-313 — '`recordEdge` / `expireEdge` / `expireEdgesForElement` have ZERO callers outside their own declaring file. `TemporalGraph.ts:26` instructs writers to call `recordEdge()`; nothing ever did.' So `0 edges` is the designed steady state, and no reader should chase it. What IS worth naming is the size: at 2366 records the journal is a substantial fraction of the snapshot that gets serialised, stringified, checksummed and deflated on every save, for a subsystem with two readers (DesignHistoryPanel, GhostOverlayRenderer).",
      "evidence": [
        "apps/editor/src/ui/analysis/graphReadModel.ts:311-313 — 'ZERO callers outside their own declaring file. `TemporalGraph.ts:26` instructs writers to call `recordEdge()`; nothing ever did.'",
        "packages/core-app-model/src/TemporalGraph.ts:195 — `recordEdge(params: {` is the sole definition; a repo-wide grep for `recordEdge(` returns only this declaration, the header comment at :26, and the graphReadModel finding above",
        "packages/core-app-model/src/TemporalGraph.ts:69-71 — `MAX_EDGES = 100_000` / `MAX_MUTATIONS = 200_000` are WARN-ONLY: :443-448 logs 'Consider archiving old project versions' and keeps pushing. There is no trim in the manager itself.",
        "MEASURED (my bench, founder-shaped fixture): the 2366-record `temporalGraph.mutations` array serialises to 0.44 MB of a 0.92 MB snapshot — 48% of that fixture, i.e. ~195 bytes per record. Against the founder's own MEASURED canonical length of 1,464,970 chars (from his `integrity ...-16588a` stamp), 2366 x ~195 B is ~462 KB, i.e. ~32% of his snapshot."
      ],
      "estimatedCostMs": -1,
      "costBasis": "measured-from-log",
      "fix": "Nothing to fix in the edge half — record it as expected and stop re-diagnosing it. For the size half, the correct question is a product one that belongs to the founder, not to a perf lane: does the design journal need to live INSIDE the version snapshot at all? It is already stored as a separate sidecar in the container (§JOURNAL-SIDECAR strips it out on the way in and re-attaches on the way out, ProjectRepository.ts:1720 / :2262-2269), so the storage layer already treats it as separable. What is NOT separated is the SERIALIZE path: ProjectSerializer.ts:1897 puts the whole live journal into the snapshot object, so every checksum walk, every stringify and every server POST still carries it. Detaching it one layer earlier — at serialize, not at store — would remove ~32% from all four of those legs at once.",
      "risk": "high",
      "confidence": "high",
      "whatItWouldBreak": "The snapshot is the wire format for the server POST and for export/import; moving `temporalGraph` out of it changes what a synced version carries, and C05 §3.8's 'not decided' clause explicitly relies on the SERVER copy still holding the journal inline as the last recoverable copy after a local release (ProjectRepository.ts:1863-1865). Detaching at serialize would remove exactly that safety net. This one is a contract change (C05), not a refactor — raise it, do not just do it.",
      "filesToChange": [
        "apps/editor/src/engine/persistence/ProjectSerializer.ts",
        "packages/core-app-model/src/TemporalGraph.ts",
        "docs/02-decisions/contracts/C05-PERSISTENCE-AND-FILE-FORMAT.md"
      ]
    },
    {
      "title": "DE-RANKED: \"6 versions in one write\" is real for I/O but is NOT where the time goes — re-compression is already incremental, and the whole-container JSON legs measure 8 ms",
      "mechanism": "The founder's reading is half right and the half that is wrong is the expensive-sounding half. Every save DOES re-parse and re-write the entire container: `_envelopeContainer` runs `_parseContainer(raw)` over the whole stored payload (ProjectRepository.ts:1937), and `_commitSlots` runs `_assembleContainer(entries, storedJournal)` then one `putVersions` of the whole payload (:2773-2774). So in the I/O sense all N versions are re-persisted per tick. But the DEFLATE is not O(history) and has not been since L-5801: unchanged versions are carried forward as already-compressed bytes via `_PendingSlot.blob` (:2237-2245 comment; the reuse is in `_persistSlots` at :2637-2645), and sealed journal chunks are carried forward untouched by `_planJournal`'s `reuse` (:645-653). Nor does the IndexedDB write block: `putVersions` sets the synchronous mirror and fires the IDB transaction asynchronously, 'never throws' (VersionCacheStore.ts:182-197). It is NOT getting worse forever in the CPU sense — it grows linearly to a 20-version ceiling.",
      "evidence": [
        "MEASURED (my own run of apps/editor/src/ui/platform/__tests__/perf104OpenPath.spec.ts, on a 1.63 MB / 10-version / 5361-journal container): 'JSON.parse (_envelopeContainer) 3 ms', 'JSON.stringify (_assembleContainer) 5 ms', 'DEFLATE one changed version 54 ms ... vs re-DEFLATE all 10 versions 288 ms <- what is AVOIDED'",
        "apps/editor/src/ui/platform/ProjectRepository.ts:2637-2645 — `const blobs: (string | null)[] = slots.map(s => s.blob); ... for (...) if (blobs[i] === null) need.push(...)` — only slots with no cached blob are compressed",
        "apps/editor/src/ui/platform/ProjectRepository.ts:645-653 — `reuse = Math.min(Math.floor(prior!.n / k), prior!.c.length); const chunks = reuse > 0 ? prior!.c.slice(0, reuse) : [];` — sealed journal chunks are carried forward as bytes",
        "apps/editor/src/ui/platform/VersionCacheStore.ts:182-197 — `putVersions(projectId, payload): void { _versionMirror.set(projectId, payload); ... const tx = this._db.transaction(VERSIONS_STORE, 'readwrite'); ...}` — mirror synchronous, IDB async, failures non-fatal",
        "apps/editor/src/ui/platform/ProjectRepository.ts:2273 — `const kept = slots.slice(-MAX_VERSIONS_STORED);` — bounded at 20, so the container does not grow without bound (the JOURNAL does; the version count does not)"
      ],
      "estimatedCostMs": 10,
      "costBasis": "estimated",
      "fix": "No change recommended on this axis, and that is the finding. Chasing 'it rewrites all 6 versions' would spend the budget on ~10 ms of CPU while the checksum (~138 ms), the sync first-save deflate (~80 ms), the doubled thumbnail and the store walk sit next to it. If anything is done here at all, it should be the IDB write VOLUME (a 1.4 MB put per autosave that grows to ~4.6 MB as the ring fills), not the CPU — and that is a durability/wear question, not a latency one.",
      "risk": "low",
      "confidence": "high",
      "whatItWouldBreak": "Making the container write incremental (per-version IDB records instead of one blob) would break the atomicity that `_commitSlots` currently gets for free — the container is one key, so a save either lands whole or not at all. Splitting it introduces partial-write states across the version ring and the shared journal sidecar, which is a much worse failure mode than 10 ms of JSON.",
      "filesToChange": []
    }
  ]
}