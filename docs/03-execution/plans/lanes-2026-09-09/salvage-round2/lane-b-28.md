{
  "refuted": false,
  "reason": "Under the \"did they read the code / are the quotes real and in context\" lens: yes, comprehensively. All 13 cited quotes exist verbatim and sit in the role claimed. Line numbers are exact on 9 of 13 (`CesiumViewport.ts:5103`, `:9971`, `:9956`, `:11991`, `:11998`, `:13352`, `:11178`, `:1632`, `:4671`, plus `contextExtentBudget.ts:330`/`:176`, `siteDispatch.ts:1140`/`:1134`/`:1279`/`:1526-29`, `siteGeocodeSearchBox.ts:162`, `GISAreaLayout.ts:6946`) and drift 1-23 lines on four (`scopeAnchor.ts:213`→214; `CesiumViewport:5096`→5093-94; `:5082`→5084; `:8320`→8343; `GISAreaLayout:6959`→6958, `:7032`→7042). Every quoted STRING is exact.\n\nSTRUCTURAL CLAIMS INDEPENDENTLY CONFIRMED:\n(1) `:5103` really is the only context load in the Forma branch of the `site.location-changed` handler — I read the whole subscriber (4994-5124). No roads/parks/water/landuse/rail/trees/street-life reload there. The `else` branch is only a `fetchContextBuildingsNearAndFar` cache prefetch.\n(2) `committedParcelLonLat` is written only at 6979/6991/6994 and `formaMassingOrigin` only at 8209 — all inside `renderFormaMassing` (starts 6593) — plus nulling at the project teardown 18640/18644. So both are stale when the handler runs. Confirmed.\n(3) I RAN the pure function. At 1140.7 m separation with radius 1781 it returns `source='parcel-frame-origin'` with the verbatim note \"centred on the site frame origin (the parcel's first vertex)\"; at 900 m and 500 m it returns `'requested'`. Both readings the author reports reproduce exactly. `DEFAULT_SITE_CONTEXT_SCOPE` (contextExtentBudget.ts:343) is a rectangle of half-extent 1781/√2, so `scopeOuterRadiusM` = 1781 — the default same-site tolerance is what they say. `RESEAT_ANCHOR_TOLERANCE_M = 250` (:2185) confirmed.\n(4) G1 confirmed: `site.location-changed` is absent from the `renderFormaMassing` trigger list (GISAreaLayout.ts:7042-7052); its only subscription in that file (:7123) drives climate. G2's three early returns are at 6941/6946/6958. G3's re-entry at :11178 is exact and carries no context load.\n(5) `contextLoadInFlight` is written at 11842, joined at 11828, cleared at 18701 — all exact — and `reseatAnchorForCurrentSite` (9979-9997) never reads it. Confirmed unused by the refusal.\n(6) A CORROBORATION the author did not cite and which strengthens the claim: `canCommitParcelBoundary` (siteDispatch.ts:1660-1676) hard-refuses a second commit, so a parcel re-select MUST route through \"↺ Redraw boundary\" → `dispatchClearParcelBoundary` → `committedParcelLonLat` is nulled while `formaMassingOrigin` survives (nulled only at project teardown). That leaves `parcel-frame-origin` as the sole surviving candidate — exactly the source the author measured — and it also makes PART 3's premise sound: with the boundary cleared, `setLtpOriginIfSafe` does NOT hit the :1140 skip, so the LTP origin really has moved on a genuine site move.",
  "evidenceChecked": [
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:4994-5124 (whole site.location-changed subscriber; :5103 is the only Forma-branch context load; :5091-5098 resolveScopeAnchor call with this.committedParcelLonLat + this.formaMassingOrigin; :5084-5087 the self-stated hazard comment)",
    "apps/editor/src/ui/geospatial/scopeAnchor.ts:190-240 (radius test at :214, unconditional adoption below radius, keepRequested fallback) + :1-60 header",
    "RAN apps/editor/src/ui/geospatial/scopeAnchor.ts resolveScopeAnchor via tsx at 1140.7 m: radius 1781 -> 'parcel-frame-origin' (verbatim note matches); 900/500 -> 'requested'; with a ring -> 'parcel-centroid' at the previous site",
    "apps/editor/src/ui/geospatial/contextExtentBudget.ts:176, :330, :343-347, :373-377 (CTX_SCOPE_MAX_RADIUS_M=7071; DEFAULT_SCOPE_RADIUS_M=1781; default scope is a rectangle of half-extent 1781/sqrt(2) so outer radius = 1781)",
    "apps/editor/src/ui/geospatial/siteScope.ts:93-94 (scopeOuterRadiusM, the UNCLAMPED body CesiumViewport imports at :148)",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:6970-6998 and :8200-8215 (committedParcelLonLat and formaMassingOrigin writes, both inside renderFormaMassing which begins at :6593); :18640/:18644 teardown nulls",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:9950-9997 (currentContextSite at :9956 preferring formaMassingOrigin; the ':9971 every site change fires one' premise; reseatAnchorForCurrentSite at :9979) and :2185 RESEAT_ANCHOR_TOLERANCE_M = 250",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:11817-11850 (coalescer: join at :11828, contextLoadInFlight written at :11842) and :18695-18701 (teardown clear)",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:11851-11998 (uncoalesced loader: unchanged-coords early-out at :11867-11875, abort at :11899, superseded bail at :11966, contextBuildingsAt stamp at :11991, applySiteScopeClip at :11998, CTX-DIAG at :11957)",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:8300-8346 (the :8308 abort is inside the keepPhotoreal branch, NOT the Forma path; :8343 applySiteScopeClip(originLat, originLon, {skipSide:true}); :8344 loadContextBuildings(originLat, originLon) — the corrective load the claim omits)",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:13345-13356 (contextFarTierState stamp at :13352) and :13515-13523 (rebuildContextFarTierForBase refusal at :13521)",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:11170-11180 (maybeAttachTerrainProvider re-entry into clampTerrainThenReplace at :11178, no context load beside it)",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:4655-4672 (readSiteLocation returns ltp ?? address at :4671) and :261 getCurrentSiteOrigin import",
    "apps/editor/src/ui/layout/GISAreaLayout.ts:6930-6990 (liveUpdateFormaMassing early returns at 6941/6946/6958) and :7042-7075 (the six-event trigger list; site.location-changed absent) and :7123 (the only location-changed subscription, climate only)",
    "apps/editor/src/ui/site/siteDispatch.ts:1130-1150 (setLtpOriginIfSafe at :1134, boundary-committed SKIP at :1140), :1270-1290 (project-restore setLtpOriginForce then re-emit at :1279), :1520-1531 (rebase at :1526 BEFORE emit at :1529), :1660-1682 (canCommitParcelBoundary refuses a second commit)",
    "apps/editor/src/ui/geospatial/SiteBoundaryMap2D.ts:4015-4062 (commit refusal path, then dispatchSiteLocation with the NEW parcel first-vertex origin at :4060 BEFORE the boundary is built) and :3799 dispatchClearParcelBoundary",
    "apps/editor/src/ui/site/siteGeocodeSearchBox.ts:145-173 (dispatchSiteLocation at :162)",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:19168-19182 (suppressNextSiteLocationFly — the one-shot that can short-circuit the handler at :5000 before it ever reaches :5103)",
    "apps/editor/__tests__/CesiumViewportReseatAnchorCurrentSite.test.ts (exists) and apps/editor/src/ui/geospatial/__tests__/scopeAnchor.spec.ts (exists, asserts the :5103 line text)"
  ],
  "betterHypothesis": "Not a different root cause — a scoping correction the evidence set has a hole in. The claim cites `applySiteScopeClip(originLat, originLon, {skipSide:true})` at :8343 but NOT the line two below it: `void this.loadContextBuildings(originLat, originLon);` at :8344. That is a corrective load at the NEW origin. Because its coordinates differ from the stale anchor it does not join the coalescer (:11828), so it enters `loadContextBuildingsUncoalesced`, aborts the in-flight mis-anchored load at :11899, and that load then bails at :11966 BEFORE it can stamp `contextBuildingsAt` (:11991), arm `applySiteScopeClip` (:11998) or stamp `contextFarTierState` (:13352). So whenever `renderFormaMassing` runs, the anchor self-heals. Two consequences the write-up should absorb: (a) on the mainstream path the defect is a RACE, not a determinism — it manifests only when the stale load completes first, which is very likely since the PREVIOUS site's bbox is already warm in `contextBuildings.ts`'s per-bbox cache while the new site's is a cold round-trip, but it should be stated as a race and the ordering probed; (b) the \"plate centred on the parcel with buildings as an off-centre lobe\" picture REQUIRES `renderFormaMassing` to have run (it is what cuts the plate at :8343) — which is exactly the case G1/G2/G3 exclude, so the founder's photograph is evidence for the race branch, not for the no-successor branch. Add `:8344` to the evidence, and add to the live probe a timestamped log of which of the two loads reached :11991 last. Separately: also worth checking `suppressNextSiteLocationFly` (:19182), which returns the handler at :5008 before :5103 — on any path that arms it, the geocode-box case in G1 fires NO load at all rather than a mis-anchored one. Two minor textual fixes: the trigger list at GISAreaLayout.ts:7042 holds SIX events (`site.zoning-updated` is omitted from the write-up's \"the four layout-executed events\"), and four line numbers drift by 1-23."
}