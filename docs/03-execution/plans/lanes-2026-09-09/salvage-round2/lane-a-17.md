{
  "survivingTitles": [
    "Lighting mesh geometry is NOT suppressed in plan — the 672-face luminaire is fully edge-projected, and that IS the dense circular scribble",
    "The lighting plan symbol exists but is CANVAS-ONLY — it contributes no linework, so it is invisible to sheets, DXF/PDF/SVG export, hidden-line removal, VG styling and the pen table",
    "Lighting has no ISO layer row, so its linework is UNCLASSIFIED — the visibility-intent veto, the VG hide and the pen table are all bypassed, and lighting's own authored pens are dead code",
    "'lighting' is absent from the list that DECLARES visibility-intent categories — so there is no lighting row in the Visibility Intent panel at all",
    "L-1332 is ALREADY OPEN for the symbol vocabulary: 46 fixture families share 11 bespoke marks and one generic disc — and the driver for a real symbol table is already authored",
    "A reflected ceiling plan is where light fittings belong, and the injector view-type set deliberately EXCLUDES 'ceiling-plan' — a new lighting builder must not reuse it",
    "Dead duplicate: `planViewSymbolRenderer` in apps/editor is constructed and exported but imported by nothing — a second lighting symbol path that has already been cited as live in three comments"
  ],
  "refutedTitles": [],
  "refutations": [
    "Nothing was refuted. All seven findings were confirmed from code that runs, not from filenames or comments. Every cited file:line was checked; the only systematic defect is line-number drift in EdgeProjectorService.ts (audit cites ~28 lines low throughout: skipInPlan gate is :2914 not :2886, plan Y filter :2977-2979 not :2949-2951, intent veto :2722-2725 not :2694-2701, PLAN_SYMBOL_INJECTION_VIEW_TYPES :352-354 not :325-327, classifyByVertexY drop arm :1135-1137 not :1106-1109, RCP clip branch :4469-4471 not :4441-4451, comment sites :342/:3957 not :314/:3929). Content at every one of those locations matches the claim exactly, so the findings stand; only the anchors need re-measuring before anyone edits."
  ],
  "missed": [
    "⛔ READ FIRST — FINDINGS 3 AND 4 HAVE BEEN PARTLY OVERTAKEN BY A COMMIT THAT LANDED AFTER THIS AUDIT WAS WRITTEN. HEAD moved 921cbb01 -> 584aaab2 -> 9babe6f5 during this session. Commit 584aaab2 (§LIGHTING-IS-A-CLASSIFIED-LAYER, L-13267, 2026-09-09 07:14) already applied: (a) `Lighting: 'A-LGHT', LightingFixture: 'A-LGHT', light: 'A-LGHT'` at EdgeProjectorService.ts:262 — finding 3 fix item (1); (b) `'A-LGHT': 'lighting'` at DrawingLayerIdentity.ts:110 — fix item (2); (c) `isLighting: /A-LGHT|lighting/i` at PenWeightTable.ts:461 plus the categoryFromFlags arm — fix item (3); (d) `'lighting'` in VisibilityIntentDefaults.ELEMENT_TYPES:28 — finding 4's first and load-bearing site. VERIFIED AT HEAD. A lane handed this audit unamended would re-implement four landed changes. WHAT IS STILL OPEN and genuinely actionable: finding 3 fix item (4) `'A-LGHT'` in SVGCompositeRenderer.ISO_LINE_WEIGHTS (still absent, confirmed) and the DrawingPipelineWorker fork reconciliation (its rival row is at :129 and its `return 'wall'` default at :143 — both exactly as the audit describes); and finding 4's other three sites (VGCategory union, ELEMENT_TYPE_TO_VG_CATEGORY, CATEGORY_TO_DXF_LAYER at VGSceneApplicator.ts:57-75/:87/:109, and ELEMENT_TYPE_REGISTRY) — `grep -in lighting packages/core-app-model/src/presentation/VGSceneApplicator.ts` and `... /ElementTypeRegistry.ts` both return ZERO at HEAD.",
    "⛔ NEW LIVE DEFECT THE L-13267 COMMIT JUST CREATED, AND IT IS THE FOUNDER'S EXACT FURNITURE COMPLAINT APPLIED TO LIGHTING. `renderLightingSymbols` (packages/core-app-model/src/views/symbols/LightingPlanSymbolRenderer.ts:243-275) filters on ONE thing — `if (levelId && f.levelId !== levelId) continue;` — and reads `window.lightingStore` directly. There is no styleResolver call, no VG check, no `_symbolGate`, no intent read anywhere in it, and its caller `PlanViewCanvas._renderLightingPlanSymbols()` (:2292-2306) checks only `_viewType`. Before 584aaab2 this was invisible because nothing could hide lighting at all. NOW that `'lighting'` is a declared ELEMENT_TYPE and `vgCategoryForLayer('A-LGHT')` returns `'lighting'`, the founder finally HAS a Lighting toggle — and switching it off suppresses the mesh blob via the `_groupFamily` veto at EdgeProjectorService.ts:2722-2725 while THE CYAN DISC KEEPS PAINTING. That is verbatim his report: \"I EXCLUDE THE ELEMENTS FROM VIEW BUT THEY STILL RENDER (PROBABLY BECAUSE THEY ARE SYMBOLS?)\". Finding 2's fix explicitly says to \"keep `renderLightingSymbols` only as the tool-preview drawer\" and does not gate it, so following the audit as written LEAVES THIS DEFECT IN. The canvas renderer needs the same gate the linework path now has, in the same commit as any symbol work.",
    "FINDING 3's PEN NUMBER IS WRONG, AND WRONG OPTIMISTICALLY. The audit says unclassified lighting resolves to `FALLBACK_PEN` 0.18 mm. `resolvePen('PROJECTION','projection')` does return that (SYSTEM_PEN_TABLE.PROJECTION has no `'projection'` key — confirmed), but that is only the SYSTEM layer. PlanViewCanvas.ts:479 calls `graphicsRulesEngine.resolveStyle(...)`, and GraphicsRulesEngine.ts:406 emits the intent rule at `RULE_PRIORITY_INTENT = 1000` over `RULE_PRIORITY_SYSTEM = 0` (:37-39). The intent's rules for an undeclared key fall to `__default__`, and VisibilityIntentDefaults.ts:108 reads `const category = elementType === '__default__' ? 'wall' : elementType;` — so `__default__` is seeded from PROJECTION.wall = **pen(0.25, '#000000')**. The luminaire scribble was drawing at the WALL WEIGHT, not 0.18. This matters directly to the founder's \"THE LINES ARE ALMOST NOT READABLE\": the blob was not merely present, it was the heaviest non-cut ink on the sheet. (The L-13267 commit message states 0.25 and is the accurate artefact here.)",
    "THE 'TWENTY LOD-200 FAMILIES' PROSE IS WRONG IN EVERY DOC THE AUDIT QUOTES — THERE ARE 34 — AND THE CATALOGUE FILE FORBIDS EXACTLY THIS BY NAME. `grep -c \"id: '\" packages/schemas/src/lighting/Lod200FixtureCatalogue.ts` -> 34, and that file's own header at :95 reads \"⛔ Cite `LOD200_FIXTURE_ROWS.length`, never a count written in prose here\" (repeated at :308). Yet ISSUE-LOG.md:20844 (L-1330), :21018 and :21021 (L-1332), and C96-ELEMENT-LIGHTING.md:841 all say \"twenty\", and LightingTypes.ts:56 says \"the twenty LOD-200 architectural, exterior and life-safety families\". Finding 5 quotes three of those sentences verbatim as evidence and derives the correct 46 (12 + 34) elsewhere in the same finding — without noticing that the number inside its own quoted evidence contradicts its own arithmetic. On a DOCUMENTATION-QUALITY audit, against a rule the source file states explicitly, that is the miss: five stale count copies, at least one of which (L-1332) is the row a lane would be told to close.",
    "FINDING 7 UNDERCOUNTS THE STALE CITATIONS AND UNDERSTATES THE DEADNESS. `grep -rn PlanViewSymbolRenderer apps packages src plugins` returns a FOURTH non-test comment the audit does not list: packages/core-app-model/src/views/ViewDependencyTracker.ts:102 — \":2092) and by `apps/editor/.../plan-canvas/PlanViewSymbolRenderer.ts`\" — in a DIFFERENT PACKAGE from the other three, so a fix scoped to apps/editor leaves it lying. And the module has no importer at all, not merely an unused `renderLightingPlanSymbols` method: `renderSelectionHighlights` is equally unreachable, so the audit's conditional (\"delete the method if the rest of the class has live callers\") resolves unambiguously to deleting the whole 144-line file.",
    "FINDING 1's Y-RANGE ARGUMENT IS TRUE BUT WEAKER THAN THE CODE ALLOWS. The audit argues the plan filter ADMITS a ceiling fixture because `min.y <= far + 0.5`. The stronger fact is at EdgeProjectorService.ts:2977: `if (isPlanView && meshWorldBox && planBelowY !== null)` — when the active intent's `belowLevelDepth` is 0, `planBelowY` is null (set at :2351) and THE FILTER DOES NOT RUN AT ALL, so every mesh in the level passes at any height whatsoever. Upstream is the same shape: NativeElementMeshExporter.ts:323-324 selects whole LEVELS whose span overlaps the range and takes `.flatMap(l => l.childrenIds)` — the logged `Y=[-1.20, 0.00]` is a level-span test, not a per-element height test, so nothing between the store and EdgesGeometry ever excludes a fixture by elevation."
  ]
}