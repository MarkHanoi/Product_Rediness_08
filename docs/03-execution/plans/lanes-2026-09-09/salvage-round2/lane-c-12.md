{
  "survivingTitles": [
    "The open path AWAITS a full-snapshot server fetch whose result is thrown away whenever local history exists",
    "GET /api/projects/:id/latest-version is two SERIAL Supabase round trips, and the whole snapshot is parsed and re-serialised inside the Fly process",
    "Two independent socket.io connections are opened per project open — double handshake, double uncached DB access check",
    "The snapshot is one unpaginated, unstreamed blob — the client-side streaming plane exists but the server endpoints it needs were never built",
    "NEGATIVE FINDING, recorded so it is not re-diagnosed: the indexes on the open path are present and correct",
    "One shared vCPU / 512 MB with a 50 MB JSON body limit — the autosave POST that fires DURING open contends with every open-path request on the same event loop"
  ],
  "refutedTitles": [
    "GET /api/projects still SELECTs the base64 `thumbnail` column for 50 rows and then deletes it server-side",
    "When both Supabase and DATABASE_URL are configured, GET /api/projects runs BOTH list queries serially and merges them",
    "No cache on the per-request project access check — every project-scoped route pays an extra Supabase round trip before doing any work",
    "Two thumbnail captures per open produce two independent PATCH /thumbnail writes of the same project row",
    "Unbounded COUNT(*) on project_versions runs TWICE per save, on a table that grows per save and is never pruned",
    "GET /api/v1/portfolio is a true serial N+1 that loads the FULL snapshot of up to 50 projects — but it is NOT on the open path"
  ],
  "refutations": [
    "REFUTED — WRONG ENDPOINT. The open path's list leg is `controller.refresh()` (buildPersistence.ts:268/273) -> `ProjectListController.refresh()` (packages/persistence-client/src/ProjectListController.ts:44-47 `const list = await this.client.list()`) -> `ProjectListClient.list()`, and that method is documented and implemented as **GET /api/v1/projects** (ProjectListClient.ts:247-248, :275). `GET /api/v1/projects` (server/api/v1/routes.js:508-526) calls `pgProjectStore.listProjects`, whose SQL projects a BOOLEAN — `(p.thumbnail IS NOT NULL AND p.thumbnail <> '') AS has_thumbnail` (server/projectStore.js:484) — and the comment above it at projectStore.js:470-479 states outright that `p.thumbnail` is NOT selected. So the L-10405 fix DID land on the path that matters. The v0 handler the finding cites (server.js:2941) is reached only by `ProjectHub._fetchSummaries`'s explicit **null-runtime fallback** (ProjectHub.ts:834 `if (this.runtime) {...}` -> :859 `apiFetch('/api/projects')`) and by `linkSourceGateway.ts:110`; PlatformRouter.ts:617-638 constructs `new ProjectHub(..., this.runtime)` with the composed runtime, so the fallback is not the production shape. ProjectListClient.ts:325-327 even records the measurement: a 50-row page is ~22 KB now, not ~2.9 MB. Sending a lane at this would rewrite a legacy branch the open never touches.",
    "REFUTED — SAME WRONG ENDPOINT, and the auditor's own confidence was already 'low'. The Supabase+PG double list and JS merge live in the v0 `GET /api/projects` handler (server.js:2955-2973). The open path's list is `GET /api/v1/projects` (see above), whose handler is a single `pgProjectStore.listProjects(userId, {limit: limit+1, offset})` call — one query, one store, no merge, no second await (server/api/v1/routes.js:513-521). The `flyctl secrets` check the finding proposes cannot make this cost open time, because no open-path request enters that branch.",
    "REFUTED AS AN OPEN COST. The mechanism is real (server.js:960-974 has no memo) but it is not on any awaited open leg, and the finding's own supporting list is wrong. `GET /api/projects/:id/latest-version` — the ONE server leg the open awaits — does NOT call `_httpAccessResult`: it does its own inline `projects.select('id').eq('owner_id',userId)` (server.js:3521-3523) on the Supabase branch and `memberOrOwner` inside the SQL on the PG branch. `GET /api/projects/:id/versions` (server.js:3452) does not call it either, and the auditor's finding 11 itself establishes that visibility-intents is not fetched on open (its only client caller is initCollaboration.ts:715, a peer-edit socket handler). The only calls inside the open window are the two socket `join-project` handlers (server.js:577-587, which call `canUserAccessProject` directly, not this helper) and the fire-and-forget catch-up `GET /commands` (server.js:4447) — none blocking, and two of the three exist only because of the duplicate socket in finding 3. A cache here buys 0 ms on the critical path; it is a scale/hygiene item, and it carries the authorisation risk the finding's own 'would break' describes. Do not spend an open-latency lane on it.",
    "REFUTED — NOT ON THE OPEN PATH, AND THE PROPOSED FIX CANNOT FIRE. Both call sites are fire-and-forget: PlatformSaveController.ts:397 `this._uploadThumbnailToServer(this.ctx.projectId, capturedThumb);` and :567 `this._uploadThumbnailToServer(projectId, thumb);` — no `await` at either, so neither PATCH is in any awaited chain. The second is deliberately deferred past the open: `schedulePostLoadThumbnailCapture` wraps `runCapture` in `setTimeout(...)` then `requestIdleCallback(runCapture, { timeout: 4000 })` (PlatformSaveController.ts:571-575). Worse for the finding, the proposed digest dedup would never trigger: the two captures are `captureThumbnail()` calls against DIFFERENT scene states (the post-load one exists precisely because the earlier one can return null / a half-settled renderer, :552-556), so their bytes differ by construction. The founder's doubled `[captureThumbnail]` line is evidence of two readPixels+WebP encodes — a client main-thread cost, which is not this dimension — not of a redundant DB write.",
    "REFUTED ON BOTH HALVES. (a) 'TWICE' is false in production for this user: step 3's `SELECT COUNT(*)` is guarded by `if (maxVersions !== -1)` (server/projectStore.js:1255), and `versionLimitFor` returns `-1` for `owner`, `studio`, `firm` and `enterprise` (server/planLimits.js:46-53, :79-84). The founder's 746 accumulated versions are themselves proof he is on an unlimited tier, so the guard short-circuits and only step 5's subquery (projectStore.js:1280-1284) runs — ONE count, not two. (b) It is a SAVE-path cost regardless: `createVersionTransactional` is reached from `POST /api/projects/:id/versions`, never from an open. The retention question (746 rows of TOASTed JSONB, no pruning) is real and worth its own founder decision, but it is a storage/cost item, not open latency — and the finding's own arithmetic-increment fix collides with `createVersion` (projectStore.js:1085) being a non-transactional insert path, which the finding correctly flags but which makes this a bad trade for a 10 ms claim.",
    "REFUTED AS AN OPEN COST BY ITS OWN CLAIM (0 ms). Verified accurate: server/api/v1/routes.js:1248 loops and awaits a full-snapshot read per project (:1255 selects `snapshot`; :1264-1265 `getLatestVersionSnapshot`), and the only caller is `openPortfolioPanel()` (PlatformProjectBrowser.ts:850 builds the modal DOM, :874 `apiFetch('/api/v1/portfolio')`) — a user-initiated modal. Correct code-reading, honestly scoped, and it belongs in a portfolio-modal lane, not in the open-path lane. Keep as backlog; it must not consume open-latency budget.",
    "SURVIVES BUT RE-SCOPED — #1 is the strongest finding here and every claim checks out. buildPersistence.ts:316 `const bundle = hint?.isNewProject ? null : await tier.streamLoad(projectId);` -> :155 `fetch('/api/projects/${projectId}/latest-version')`; PlatformShell.ts:296-299 reads `versionRepository.getLatestVersion(id)` and returns via `loadVersion(latest)`; `opts.prefetchedVersion` is read ONLY at :351/:325 inside the else branch. PlatformRouter.ts:1246-1247 awaits the whole chain before `overlay.hide()`. ONE correction: there is no `markStartupPhase` around the streamLoad leg (the marks bracket step 1 at PlatformRouter.ts:1246 and the LOCAL read at PlatformShell.ts:295-297), so the founder's 89 ms `open:version-read` number is the IndexedDB leg only — the discarded network leg is genuinely untimed and the 350 ms is an estimate, not a measurement. Land the marks in the same change as the fix so the next lane reads a number instead of inheriting one.",
    "SURVIVES BUT RE-SCOPED — #2's mechanism is confirmed (server.js:3521-3523 then :3525-3531 = two serial PostgREST calls; `app.use(compression())` at server.js:372; `express.json({limit:'50mb'})` at server.js:381; fly.toml:224-228 shared-cpu-1x/1 cpu/512 MB). TWO corrections. (i) Its 'would break' misattributes the code: server.js:3520's comment reads `// Single query: verify ownership and fetch latest version snapshot together.` — it CLAIMS one query while doing two; there is no comment anywhere about distinguishing 'no rows' from a DB error, so that stated invariant is invented. The real invariant to preserve is the C13 owner_id scope and 404-indistinguishability. (ii) Its cost is a SUBSET of #1 on the founder's scenario — fix #1 and this leg stops being awaited at all. Rank #2 as the fix for the deep-link/cold-cache case, not as additive latency on top of #1.",
    "SURVIVES BUT RE-SCOPED — #3's duplication is unambiguous: initCollaboration.ts:578-588 and PlatformCollabPill.ts:183-191 each mint their own `ioFn({transports,auth})` with separate references (`socket` module-local vs `ctx.socket`), and PlatformShell.ts:250 calls `initSocketCollaboration(this.ctx, id)` on every `setProjectContext`. The founder's log shows both joins. BUT the 80 ms is not on the awaited chain: `initSocketCollaboration` returns `void` and loads the socket.io script asynchronously, so neither handshake blocks `openProject`. The real open-time argument is CONTENTION, and the repo already makes it for itself at ProjectHub.ts:774-777 ('a browser allows ~6 connections per host... the requests that queue behind them include the two the OPEN path needs — controller.refresh() and tier.streamLoad()'). Two WebSocket upgrades plus two `canUserAccessProject` Supabase probes plus the extra `_triggerCatchUp` GET all land inside that window on one shared vCPU. Worth fixing on correctness grounds alone (double catch-up replay), but do not book 80 ms of open latency against it.",
    "SURVIVES BUT RE-SCOPED — #8's facts hold: SnapshotStreaming.ts:24-27 lists the server split as DEFERRED, `grep -n 'latest-version/header|/level/' server.js` returns nothing, and `temporalGraph: temporalGraphManager.serialize()` is a first-class snapshot member in BOTH serializers (packages/persistence-client/src/loader/ProjectSerializer.ts:926 and apps/editor/src/engine/persistence/ProjectSerializer.ts:1897) with no detach on the server wire — `detachJournalMutations`/`attachJournalMutations` appear only in ProjectRepository (the LOCAL IndexedDB store), which is why the founder's JOURNAL-SIDECAR line is local-only and the server copy still carries the journal whole. So this is the PAYLOAD-SIZE term of the same leg #1 indicts, not a second independent cost — it must not be double-counted, and its priority collapses if #1 stops awaiting the fetch. The `?includeJournal=0` half is still the right cheap move for the cold-cache case.",
    "SURVIVES AS A CORRECT NEGATIVE — all four index claims verified verbatim: dbMigrate.js:116-117 `idx_project_versions_project_created ON project_versions(project_id, created_at DESC)`, :70 `idx_projects_owner_updated ON projects(owner_id, updated_at DESC)`, :137 `idx_project_members_user_project ON project_members(user_id, project_id)`, :272 `idx_pcl_project_time ON project_command_log(project_id, created_at DESC)`. The named gap is real too: server.js:4229 `SELECT * FROM visibility_intents WHERE project_id = $1 AND is_system = false ORDER BY updated_at DESC` has no LIMIT against only `idx_visibility_intents_project (project_id)` (dbMigrate.js:258). Keep this row — it closes the 'missing index' hypothesis with evidence.",
    "SURVIVES BUT ITS NUMBER IS INFLATED 5x. Mechanism confirmed line-for-line: server.js:3629 `Buffer.byteLength(JSON.stringify(snapshot),'utf8')` re-stringifies a body express.json already size-capped, :3667-3675 runs a passthrough Zod parse, :3690 `scanSnapshotForUnsafeUrls(snapshot)` walks it on the request path though server.js:3684 says 'REPORT ONLY, REJECTS NOTHING', and projectStore.js:1268 stringifies it again as the insert parameter. The claimed 100 ms contradicts the auditor's OWN measurement in the same finding (~20 ms at founder scale). Book ~20 ms of stolen vCPU inside the open window, not 100. The size-cap re-stringify (fix 1) is a free deletion; deferring the URL scan (fix 2) is nearly free. Fix 3 (is 50 MB a real limit on a 512 MB box?) is the one that matters at scale and is an OOM question, not a latency one."
  ],
  "missed": [
    "THE PG BRANCH OF `/latest-version` IS FOUR SERIAL QUERIES WITH A DUPLICATED OWNERSHIP CHECK — and a one-query helper already exists in the same module. The auditor audited only the Supabase branch. server.js:3536-3559 does `await pgProjectStore.listVersions(id, userId)` then `await pgProjectStore.getVersionById(id, latest.id, userId)`. `listVersions` (server/projectStore.js:925-948) is an `ownerCheck` query (`SELECT p.id FROM projects p WHERE p.id=$1 AND memberOrOwner`) PLUS a `LIMIT 20` list — twenty rows fetched to use `[0]`. `getVersionById` (projectStore.js:985-1001) then runs the IDENTICAL `ownerCheck` query AGAIN plus the snapshot select. Four round trips where `getLatestVersionSnapshot(projectId)` (projectStore.js:1007-1017) answers the whole question in ONE index-covered query. If DATABASE_URL is set on the live app (fly.toml's own secrets recipe sets it, and pgClient.js:52-54 pools on it), this — not the Supabase two-step — is the shape the founder's open actually pays.",
    "A COLD/DEEP-LINK OPEN FETCHES `/latest-version` TWICE, SERIALLY, FOR THE SAME PROJECT. `tier.streamLoad` (buildPersistence.ts:316 -> :155) fetches it. When it returns null (server 404, or `{version:null}`), `prefetchedVersion` is `undefined`, so PlatformShell's else branch awaits `warmAttempt` and then calls `this._loadLatestVersionFromServer(id)` (PlatformShell.ts:376, and again in the `.catch` at :387), whose body is `await apiFetch('/api/projects/${projectId}/latest-version')` (PlatformShell.ts:469) — byte-identical request, second full server leg. This is exactly the deep-link/cold-cache case that finding 1's option (b) is meant to keep working, so whoever implements (b) must collapse this pair or they will ship a third round trip instead of removing one.",
    "THE STREAM-LOAD LEG IS SERIALISED **AFTER** THE ENGINE BOOT, THOUGH THE TWO ARE INDEPENDENT. buildPersistence.ts step 2 is `await attachedBootstrap.ensure()` (line ~307, the legacy engine lazy-start) and step 3 is `await tier.streamLoad(projectId)` (line 316). The network fetch cannot begin until the engine has finished booting, and the engine does not need the bundle to boot. Even in the case where the bundle IS used (cold cache), the two longest legs of the open run back to back instead of overlapping. This is a separate change from finding 1 — hoist the `tier.streamLoad(...)` promise ABOVE step 2 and await it at step 4 — and it is the only one of the two that helps the deep-link user.",
    "IF THE OPEN-PATH LIST LEG IS EVER ATTACKED, THE QUERY TO MEASURE IS `GET /api/v1/projects`, NOT THE v0 HANDLER. Per the refutation of findings 4/5, `controller.refresh()` lands on server/api/v1/routes.js:508 -> `pgProjectStore.listProjects(userId, {limit: limit+1, offset})`. That SQL (server/projectStore.js:481-503) runs a `LEFT JOIN LATERAL (SELECT id, element_count FROM project_versions WHERE project_id = p.id ORDER BY created_at DESC LIMIT 1)` once per row over the table holding multi-MB TOASTed snapshots, plus a `LEFT JOIN project_members`, over-fetching `limit+1` rows — and computes `has_thumbnail` as `(p.thumbnail IS NOT NULL AND p.thumbnail <> '')`, a value comparison against the largest column in the row for up to 51 rows. The bytes no longer cross the wire, which is the whole reason finding 4 is refuted, but whether that predicate and the per-row LATERAL are free is an open question that only `EXPLAIN (ANALYZE, BUFFERS)` against the live table can answer. projectStore.js:470-471 explicitly defers this ('raising [the page size] is a separate decision about the LEFT JOIN LATERAL cost per row'), so it is a known-unmeasured item, not a settled one."
  ]
}