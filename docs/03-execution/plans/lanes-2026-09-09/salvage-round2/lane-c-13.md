{
  "survivingTitles": [
    "0% hit rate on a fresh open is EXPECTED, not a defect — the cache is per-session in-memory and this was its first pass",
    "`yieldCalendar≈83ms` is COMPUTED, not measured — it is literally `yields × 16.67`",
    "`work=109ms` is a LOWER BOUND — it excludes hidden-line removal, every symbol injector, VG styling, and the caller's mesh export",
    "The plan projection during open is DELIBERATE (Contract 17 §4 auto-opens the split plan pane) — and the user's decision to close that pane is never persisted"
  ],
  "refutedTitles": [
    "Doors and windows mint a fresh cache version on EVERY build via `Date.now()`, so they can never hit after a rebuild — walls solved this with a content hash and doors did not"
  ],
  "refutations": [
    "REFUTED ON THIS LENS (real defect, zero open cost). Every citation checks out verbatim — DoorBuilder.ts:431 `version: Date.now()`, WindowBuilder.ts:635 identical, FurnitureFragmentBuilder.ts:142 and LightingFragmentBuilder.ts:734 `_priorVersion + 1`, WallFragmentBuilder._versionForBuild at :420 consumed at :1083/:1155, and the CACHEABLE_ELEMENT_TYPES comment asserting monotonicity is at :2033-2044 (auditor said 2005-2010). But the version token is READ, not minted, during projection: `currentVer = group.userData.version` (EdgeProjectorService.ts:2788) is whatever the builder stamped at BUILD time, and it is compared against an EMPTY map on a cold open. Whether that number came from `Date.now()` or a content hash cannot change a miss into a hit when there is no entry to hit. I confirmed the open path runs exactly ONE projection (see missed item 5), so there is no second pass within the open window for a stale token to spoil. Cost to open = 0ms, not the -1ms placeholder. The auditor's own text concedes this ('NOT the cause of the 0% on open'), so this is a dimension error, not a factual one — but on an is-it-real/does-it-cost-open lens it must be killed, or the next lane spends its budget rewriting four builders and the open time does not move.",
    "PARTIAL REFUTATION OF FINDING 4's COST FIGURE (the finding itself survives). The claimed 192ms is `work=109 + yieldCalendar 83`, and finding 2 — which I confirmed — establishes that the 83 is synthesised arithmetic, not a measurement. The audit cannot both argue that yieldCalendar is fabricated and then bank it as 43% of a cost estimate. The defensible statement is '>109ms of main-thread CPU, upper bound unmeasured'. Against a complaint measured in seconds that is ~2-5%, which is what the auditor's own summary concludes, so the conclusion stands even though the number backing it does not.",
    "CITATION DRIFT ON EVERY EdgeProjectorService LINE NUMBER (content correct, addresses wrong by a constant +28). Verified actuals: the cache Map is at :1971 not 1943; the 'hitRate=0% on first run' comment at :3682 not 3654; the yieldCalendar expression at :3676 not 3648; `work=` at :3675 not 3645; `_putCwCache` :2164 not 2136; `invalidateCwElement` :2204 not 2176; `invalidateCwView` :2220 not 2192; `clearCwProjectionCache` :2236 not 2208; `_cwCacheIsValid`'s comparison :2125 not 2097; `applyOcclusion` :4235 not 4207. The uniform offset means the auditor read a pre-insertion revision rather than misreading the file — the quoted text is exact at the corrected lines — but a lane that opens EdgeProjectorService.ts at :1943 lands in the RoofSlopeSymbolBuilder field and may conclude the finding is fabricated. initScene.ts and SplitViewManager.ts citations are exact (4636 / 4659 / 4666 / 4668 / 1513 / 1527 / 401-431 / 2179 / 2187 all verified as written); only EdgeProjectorService drifted."
  ],
  "missed": [
    "THE COLD PASS PAYS A CACHE-POPULATION TAX IT CAN NEVER RECOUP — which makes finding 1's 'claimed cost: 0ms' wrong. On the MISS path the projector clones every projected BufferGeometry into the cache: `geo: projGeo.clone()` (EdgeProjectorService.ts:3259-3262), then `_putCwCache` copies the collector again (`layers: new Map(layers)`, :2186). A fresh open is 63 cacheable groups and 410 edge geometries, so the open path allocates and retains a second full copy of the linework for a cache that, by finding 1's own correct argument, no run in this session will ever read unless the user reprojects. Finding 1 is right that there is nothing to REPAIR, but 'cost 0ms' is an assertion, not a reading — the true statement is 'the cold pass is strictly more expensive than it would be with the cache compiled out, by the clone cost'. Measure it before declaring it free.",
    "`work=109ms` EXCLUDES ~383 LINES OF PRE-LOOP WORK INSIDE `project()`, NOT ONLY THE POST-LOG WORK FINDING 3 NAMES. `project()` begins at EdgeProjectorService.ts:2286; the work clock `_lastYieldAt = performance.now()` is not stamped until :2669 and `_workMsTotal = 0` at :2671. Everything in between is outside the number: `this._technicalDrawings.create(this._world)` (:2470), `await this._buildModelIdMap(models)` (:2531), and `await this._edgeProjector.get(modelIdMap, this._world)` (:2539) — the ENTIRE OBC/IFC edge-projection leg — plus `resolveSectionVolumeBox(... this._bimManager ...)` (:2356) and `computeClipSignature` (:2364). Finding 3 correctly names occlusion/symbols/VG on the after-side and stops there. The excluded region is two-sided, and on a project with IFC imports the before-side is the larger half. Finding 3's proposed fix (wrap `project()` end-to-end) does catch this, but the finding does not say so, so a lane may instrument only the tail.",
    "THE PROJECTOR MODULE ITSELF IS DYNAMICALLY IMPORTED ON THE OPEN PATH, AND NO PROPOSED INSTRUMENT CATCHES IT. `_ensureEdgeProjectorService()` (initScene.ts:1143-1147) does `await import('./views/EdgeProjectorService')` on FIRST projection and logs 'Phase 6: EdgeProjectorService lazy-loaded on first projection.' (:1158). That module is 4,561 lines with 43 static imports including `@thatopen/components`, `@thatopen/fragments` and eight `@pryzm/geometry-*` symbol-builder packages (doorPlanSymbolBuilder, six furniture builders, stair bridge, RoofSlopeSymbolBuilder). The chunk fetch + parse + evaluate lands on the main thread inside the open window, triggered by the same Contract 17 auto-open finding 4 describes, and it is in NONE of the numbers — not `work`, not `yieldCalendar`, and not finding 3's end-to-end wrap around `project()`, which by definition starts after the module has already resolved. Instrument the promise in `_ensureEdgeProjectorService`, not inside the service. This is the one cost on this dimension that is genuinely open-ONLY (it never recurs in the session), which is exactly the shape the founder's complaint is about.",
    "HIDDENLINEREMOVAL HAS NO TIMING TO TURN ON — there is nothing to enable, it must be added from the caller. Verified: `packages/core-app-model/src/drawing/HiddenLineRemoval.ts` is 954 lines containing exactly ONE `console.log` (:935) and ZERO occurrences of `performance.now()`. Finding 3 says it is 'untimed', which understates it — a lane looking for a verbose flag or a DIAG section will find neither. The only place a clock can go without touching the package is around `applyOcclusion(drawing, {...})` at EdgeProjectorService.ts:4235. Given it is O(targets x occluders) over 410 geometries against 26 occluders and runs after the summary prints, this is the single largest unpriced term in the dimension.",
    "NEGATIVE RESULT WORTH RECORDING SO THE NEXT LANE DOES NOT CHASE IT: the plan is projected ONCE on open, not twice, despite two rival drivers still existing. `PlanViewManager._onSplitViewViewChanged` (:1165-1175) returns early unless `viewDef.viewType === 'section' || 'elevation'`, so the split-view PLAN never enters `_ensureProjectionForSplitView` (:1096/:1137). The single projection comes from `initScene._reprojectView` (:1453) via `split-view-activated` -> `notifyViewActivated` -> deferred-dirty flush, which matches the founder's single PERF-CACHE-STATS line. This deserves stating because the code's own comments (initScene.ts:1180-1186 and :1447-1452, PlanViewManager.ts:1129-1134) narrate a past four-way rival-driver defect where 'each pass disposed the other's cached geometry' (L-11561) and actively invite the assumption that the duplication survives. It does not, for plans.",
    "CORRECTNESS NOTE, NOT A PERF COST — flagged so nobody prices it as one: `bim-project-cleared` (initScene.ts:1544) purges `nativeElementMeshExporter.clearCache()` (:1558) and `frustumCullingService.reset()` (:1559) but does NOT call `clearCwProjectionCache()`. Only `pryzm-project-switch` does (:3616). Its own comment at :1550-1557 says the handler exists precisely because 'a project reload/version-restore on the SAME project fires bim-project-cleared but not pryzm-project-switch' — and then leaves the projection cache out of the list. A same-project version-restore therefore keeps EPS entries keyed by elementId across the rebuild. Whether that is stale depends entirely on the builder version stamps finding 5 examines, which is the one place finding 5's subject matter actually bites — as a staleness risk, not a hit-rate one. Zero open-time cost either way."
  ]
}