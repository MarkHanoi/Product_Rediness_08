{
  "dimension": "Architecture and the orchestrator — project-open sequencing, layer/P1 conformance, god-objects, and the structural changes that would cut open latency",
  "summary": "There is NO orchestrator of project open. There is a promise chain that LOOKS like one and stops four stages early: `PlatformRouter` awaits `runtime.persistence.openProject()`, whose last awaited step is `PlatformShell.setProjectContext()` — declared `: void` (apps/editor/src/ui/platform/PlatformShell.ts:217) and returning immediately after kicking off the version read, the hydrate and the post-load sweep as fire-and-forget. `packages/renderer-three/src/WorkspaceSurface.ts:169-171` says so in its own words: \"PlatformShell's production implementation returns `void` synchronously, in which case the await is a no-op.\" Everything the founder is actually waiting for happens AFTER the awaited promise resolves and the loading overlay hides, dispatched through a 35-subscription `pryzm-project-loaded` fan-out across 24 files with no ordering, no back-pressure and three uncoordinated timers. That is exactly the shape the brief warned about — an event-chained open — and it is why the repo's own instrument cannot bound it: `apps/editor/src/engine/startupBudget.ts` states \"THE HUB-OPEN PATH STILL NEVER PRINTS THE TABLE\", and the founder's paste contains not one `[§STARTUP-BUDGET]` line. Two concrete costs sit inside that unowned window: the heavy-scene guard destroys and rebuilds the renderer at load-END on a decision (element/mesh count) that was knowable at load-START, and an entire `/api/projects/:id/latest-version` snapshot is fetched, awaited and JSON-parsed on the critical path only to be discarded whenever local IndexedDB history exists. P1 and the layer gate both pass (RC=0), but the open path routes around the typed runtime through 338 distinct `window.X =` assignment targets, which is why nothing in the composed graph can see or sequence it.",
  "whatIcouldNotEstablish": "(1) I could not attribute a single millisecond of the founder's run, because his paste contains ZERO `[§STARTUP-BUDGET]`, `[ProjectLoader] §LOAD-CHUNKED`, `§LOAD-YIELD-WAIT` or `§LOAD-IMPORT-STEPS` lines — all four instruments exist and print unconditionally on the paths they cover. The single highest-value next action is to get a paste that includes them; anyone quoting a ms figure for this run without them is estimating. (2) I could not measure the renderer-swap cost. I established that it fires (log: \"3762 meshes (ARM TRIPPED)\" then two \"WebGPU device lost: reason=destroyed\" lines) and I read the code path that fires it, but I have no timer on either side of `pryzmSwapRendererBackend`. The 11 s figure in autoWebGLHeavyScene.ts:262 is from a residential GENERATION, not an open — do not transfer it. (3) I could not measure the discarded server round-trip: the payload size for 503 elements is unknown to me. The only measured anchors are C05 §3.5 (281 elements → ~0.1 MB model + ~8.7 MB journal at 30,432 mutations) and this run's 2,366 mutations, which is 13× fewer, so a linear extrapolation is a guess, not a measurement. (4) I could NOT establish which branch PlatformShell took on this run: neither \"[PlatformShell] Auto-restoring latest local version\" nor \"Using prefetched version\" is in the paste. I inferred the local branch from \"6 version(s) persisted to IndexedDB\" — that is inference, not evidence. (5) I did NOT establish that CesiumViewport (19,404 LOC) costs open time; eagerGlobeStart.ts:52 and GISAreaLayout.ts:1005/1084 say a hub open never constructs it. I am reporting it as bundle+maintenance cost only. (6) I did not verify whether the founder reloads the page per open — that decides whether the 26.3 MB entry graph and the once-per-tab engine boot (O-INV-1) are paid every time or once.",
  "findings": [
    {
      "title": "The open has no owner and no end: `await openProject()` resolves BEFORE the project loads, so the whole expensive half is unsequenced, unmeasurable and uncancellable",
      "mechanism": "`PlatformRouter._openProjectViaRuntime` awaits `runtime.persistence.openProject()` and then hides the loading overlay. That promise's last awaited step is `attachedSurface.setProjectContext(...)`. `WorkspaceSurface.setProjectContext` awaits its host's return value, but the host is `PlatformShell.setProjectContext(id, name, opts): void` — it synchronously reads the local version and then kicks `versionCtrl.loadVersion(latest)` / `loadAdapter.load(...).then(...)` as fire-and-forget. So the promise resolves before the snapshot inflate, before ProjectLoader hydrates a single element, before any geometry exists, before the 35-way post-load fan-out. No object in the system holds a handle to 'this open'; the only thing that knows the load finished is a broadcast event with no return channel. This is the root cause of every other finding here: latency cannot be attributed to a phase that nothing owns, work cannot be ordered against a completion nothing signals, and a superseded open (buildPersistence.ts:257-261 'supersede' branch) chains onto a promise that never represented the previous open.",
      "evidence": [
        "apps/editor/src/ui/platform/PlatformShell.ts:217-220 — `setProjectContext(\n        id: string,\n        name: string,\n        opts?: { isNewProject?: boolean; prefetchedVersion?: unknown },\n    ): void {`",
        "packages/renderer-three/src/WorkspaceSurface.ts:169-171 — \"Awaits the host's return value when it is a Promise ... PlatformShell's production implementation returns `void` synchronously, in which case the await is a no-op.\"",
        "packages/runtime-composer/src/buildPersistence.ts:337 — `await attachedSurface.setProjectContext(summary.id, summary.name, contextOpts);` followed at :341 by `emitProgress('done', 100);`",
        "apps/editor/src/ui/platform/PlatformRouter.ts:1247 — `await this.runtime.persistence.openProject(projectId, hint);` and :1257 `overlay.hide();`",
        "apps/editor/src/engine/startupBudget.ts (§ note) — \"⚠ THE HUB-OPEN PATH STILL NEVER PRINTS THE TABLE, and that is a KNOWN GAP ... `reportStartupBudget` is called from exactly one place (`enterCanvasWithSitePlan`, the ONBOARDING arm) ... A hub-open therefore emits every mark line but no summary.\"",
        "Founder's paste contains ZERO `[§STARTUP-BUDGET]` lines — consistent with the above."
      ],
      "estimatedCostMs": -1,
      "costBasis": "unknown",
      "fix": "Make the open a first-class object with one awaited promise. The seam is already typed for it: `WorkspaceSurfaceHost.setProjectContext` is declared `void | Promise<void>` (packages/renderer-three/src/WorkspaceSurface.ts:73-77) and `WorkspaceSurface.setProjectContext` already awaits the host. Change `PlatformShell.setProjectContext` to return the load promise (it already has one: the `loadVersion` / `loadAdapter.load(...).then(...)` chain), and thread that through `buildPersistence.openProject` so `emitProgress('done',100)` fires when the project is actually open. Then: the overlay hides at the right moment; `reportStartupBudget('open')` can be called from the one place that now knows the run ended, closing the documented hub-open gap; the `supersede` branch chains a REAL open; and every post-load step below becomes schedulable against a known boundary instead of a broadcast.",
      "risk": "medium",
      "confidence": "high",
      "whatItWouldBreak": "The `void` return is load-bearing for exactly one thing: PlatformShell's idempotency early-return at :223-228 (`already active for <id> — skipping duplicate call`) currently returns nothing, and callers that fire-and-forget must not start awaiting a promise that never settles on that path. Returning `Promise.resolve()` there preserves it. The larger risk is that the overlay would now stay up for the full (currently invisible) load, which will LOOK like a regression until the tail is fixed — that is the finding, surfaced, not caused.",
      "filesToChange": [
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\apps\\editor\\src\\ui\\platform\\PlatformShell.ts",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\runtime-composer\\src\\buildPersistence.ts",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\renderer-three\\src\\WorkspaceSurface.ts",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\apps\\editor\\src\\ui\\platform\\PlatformRouter.ts"
      ]
    },
    {
      "title": "The renderer backend is destroyed and rebuilt at load-END, on a decision that was knowable at load-START — the whole scene is uploaded to backend A and then re-uploaded to backend B",
      "mechanism": "`runTierPbrPass` counts every mesh in the scene and calls `maybeAutoSwitchToWebGLForHeavyScene(scene, 'tier:...', meshCount)`. Its Gate 3 predicate `isSwapWorthyHeavyScene` trips on ≥400 elements OR ≥1000 meshes. During a project load the per-add pass is correctly deferred (perAddGeometryGate), so the pass runs exactly once — as `runTierPbrPass('post-load')`, fired from a `pryzm-project-loaded` handler. By then the scene holds 3,762 meshes, the arm trips, and `fireSwapToWebGL` calls `window.pryzmSwapRendererBackend`, which tears down the live renderer (hence the two `WebGPU device lost: reason=\"destroyed\"` lines) and rebuilds it. Every geometry buffer and every material compiled during the load is discarded and re-created. The decision is REACTIVE because it reads the live scene — but the input it needs is in the version record before a single element is created: `VersionRecord.elementCount`, which the server row already carries as `element_count`. A PROACTIVE entry point for exactly this already exists and shares the once-per-session guard; it is called from the building-generation lifecycle and from nowhere on the open path.",
      "evidence": [
        "apps/editor/src/engine/initScene.ts:3008 — `maybeAutoSwitchToWebGLForHeavyScene(scene, `tier:${reason}`, meshCount);`",
        "apps/editor/src/engine/initScene.ts:3038 — `_runConsolidatedTierPbrPass = () => runTierPbrPass('post-load');` fired from the `pryzm-project-loaded` handler at initScene.ts:3757-3765",
        "apps/editor/src/rendering/autoWebGLHeavyScene.ts:108-112 — `export function isSwapWorthyHeavyScene(elementCount, sceneMeshCount) { if (elementCount >= SWAP_ELEMENT_THRESHOLD) return true; if (typeof sceneMeshCount === 'number' && sceneMeshCount >= SWAP_MESH_THRESHOLD) return true; return false; }`",
        "Founder log: `[pryzmPerf] NAV-BACKEND-SWAP-HEADROOM ... NOW: 162 elements (238 to go), 3762 meshes (ARM TRIPPED)` then `[renderer-three/WebGPURendererAdapter] WebGPU device lost: reason=\"destroyed\"` and `[createRenderer] WebGPU device lost: reason=\"destroyed\"`",
        "apps/editor/src/rendering/autoWebGLHeavyScene.ts:271 — `export function proactivelySwitchToWebGLForBuildingGeneration(reason: string): void` — the proactive entry, whose ONLY caller is apps/editor/src/ui/generation/buildingGenerationLifecycle.ts:307",
        "apps/editor/src/ui/platform/PlatformShell.ts:357 — `elementCount: prefetched.elementCount,` and server.js:3529 selects `element_count` — the count is available before the load",
        "apps/editor/src/rendering/autoWebGLHeavyScene.ts:262 — the reactive path's own documented failure mode: \"the founder saw a `THREE.TSL: Invalid generated code` flash and an ~11 s `WebGPU PSO compile LONGTASK` stall BEFORE the reactive swap finally landed on WebGL\" (⚠ that number is from a GENERATION, not an open)"
      ],
      "estimatedCostMs": -1,
      "costBasis": "unknown",
      "fix": "Call the existing `proactivelySwitchToWebGLForBuildingGeneration` (rename it — it is no longer generation-specific) from `PlatformShell.setProjectContext`, BEFORE `versionCtrl.loadVersion(latest)`, keyed on `latest.elementCount` against `SWAP_ELEMENT_THRESHOLD`. The once-per-session guard is already shared, so the reactive path at initScene.ts:3008 no-ops afterwards and stays as the safety net for scenes that get heavy without a stored count. Nothing new is minted — this is a second caller of an existing verb, which is exactly the shape ADR-0267 already blessed for generations.",
      "risk": "low",
      "confidence": "high",
      "whatItWouldBreak": "An element count is not a mesh count: the founder's run trips on the MESH arm (3,762) while sitting at 162 elements, so a count-keyed proactive swap would MISS this exact case unless the stored count is a proxy the threshold is recalibrated against. The honest version is: store the mesh count alongside elementCount at save time (the census already computes it), or lower the element arm for the stored-open path. Swapping proactively on a project that turns out light also costs the user the better backend for the session — bounded by the once-per-session guard and already the accepted trade in ADR-0267.",
      "filesToChange": [
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\apps\\editor\\src\\rendering\\autoWebGLHeavyScene.ts",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\apps\\editor\\src\\ui\\platform\\PlatformShell.ts",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\apps\\editor\\src\\rendering\\heavySceneSwapThreshold.ts"
      ]
    },
    {
      "title": "Every non-new open awaits a full server snapshot fetch that is DISCARDED whenever local history exists — and it is serialized AFTER the engine boot instead of alongside it",
      "mechanism": "`buildPersistence.openProject` step 3 runs `const bundle = hint?.isNewProject ? null : await tier.streamLoad(projectId)` — an unconditional `GET /api/projects/:id/latest-version` whose body is the ENTIRE snapshot (the serializer embeds `temporalGraph` whole, so the payload is model + journal), followed by `await res.json()` on the main thread. The bundle is threaded to `PlatformShell.setProjectContext` as `opts.prefetchedVersion`. But `prefetched` is read ONLY inside the `else` branch — the one taken when `versionRepository.getLatestVersion(id)` returns null. When local IndexedDB has ANY version for the project, `if (latest)` wins at PlatformShell.ts:296 and the server bundle is never touched. Separately, the fetch is issued AFTER `await attachedBootstrap.ensure()` (the whole engine boot), so on a cold tab a network round-trip that could have overlapped ~2 s of boot instead runs strictly after it.",
      "evidence": [
        "packages/runtime-composer/src/buildPersistence.ts:316 — `const bundle = hint?.isNewProject ? null : await tier.streamLoad(projectId);`",
        "packages/runtime-composer/src/buildPersistence.ts:307-309 — `if (attachedBootstrap !== null) { await attachedBootstrap.ensure(); }` — strictly before :316",
        "packages/runtime-composer/src/buildPersistence.ts:163 — `const json = await res.json() as {...}` — full-snapshot main-thread parse",
        "apps/editor/src/ui/platform/PlatformShell.ts:296-299 — `const latest = versionRepository.getLatestVersion(id); ... if (latest) { console.log('[PlatformShell] Auto-restoring latest local version:', latest.label); this.versionCtrl.loadVersion(latest); }`",
        "apps/editor/src/ui/platform/PlatformShell.ts — grep of `prefetched` shows its ONLY uses are at :321-361, entirely inside the no-local-version `else` branch",
        "apps/editor/src/engine/persistence/ProjectSerializer.ts:1897 — `temporalGraph: temporalGraphManager.serialize(),` is part of every snapshot; apps/editor/src/ui/platform/ServerSyncQueue.ts:798 posts `snapshot: version.snapshot` whole",
        "apps/editor/src/ui/platform/ProjectRepository.ts:214-217 — MEASURED anchor: \"the founder's project is 281 elements and 30 432 `temporalGraph` mutations — a model of ~0.1 MB and a journal of ~8.7 MB\"",
        "Founder log: `[VersionRepository] reason=save-version - 6 version(s) persisted to IndexedDB` — local history is present on this run, so the local branch is the live one",
        "server/projectStore.js:933-946 — the PG arm costs 3 SQL round-trips (ownerCheck, listVersions LIMIT 20, then getVersionById) per open"
      ],
      "estimatedCostMs": 400,
      "costBasis": "estimated",
      "fix": "Two independent changes, both small. (a) Do not fetch what you will not use: ask `versionRepository` for the local head FIRST (it is already a synchronous mirror read) and skip `tier.streamLoad` entirely when a local version exists and is not older than the server's `updatedAt` from the already-loaded project summary. (b) When the fetch IS needed, start it before `await attachedBootstrap.ensure()` and await it at the point of use, so the round-trip overlaps the engine boot instead of queueing behind it. (b) is a two-line reordering inside `buildPersistence.openProject` with no behavioural change.",
      "risk": "low",
      "confidence": "medium",
      "whatItWouldBreak": "The unconditional fetch currently provides a silent correctness property nobody named: if the local head is STALE (a collaborator saved from another machine), the server bundle is the fresher one — and today the code throws it away anyway, so skipping the fetch changes nothing about which snapshot loads. But (a) as described adds a staleness comparison that does not exist yet, and getting it wrong opens someone's older local copy over a newer server one. The comparison must be on server time, not client time (the same trap initCollaboration.ts:259-273 documents for the catch-up baseline). (b) alone has no invariant to break — the fetch result is not consumed until :329.",
      "filesToChange": [
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\runtime-composer\\src\\buildPersistence.ts",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\apps\\editor\\src\\ui\\platform\\PlatformShell.ts"
      ]
    },
    {
      "title": "The post-load window is a 35-subscription broadcast with three uncoordinated timers — this is the interval §PERF104 explicitly declined to own, and it is where the founder's wait lives",
      "mechanism": "`pryzm-project-loaded` has 35 handler registrations across 24 files. They run in registration order, which is boot order, which nobody declares. Among them: the consolidated tier+PBR full-scene traverse AND the renderer backend swap (initScene, behind `setTimeout(…, 0)`); `zoomToAll` behind `setTimeout(…, 150)`; a thumbnail capture behind `setTimeout(…, 3500)` then `requestIdleCallback`; CRDT wiring behind `requestIdleCallback(…, {timeout:3000})`; a second socket.io connect; an orphan-grid-annotation purge; the autosave re-arm that produces the full 503-element serialize + deflate + IndexedDB write visible in the founder's log; plus ConstraintEngine, AmbientIntelligence, FrustumCullingService, ProjectIsolationAudit and five DataWorkbench panels. There is no ordering contract, no yielding budget, no per-handler timing, and no way for a later handler to know an earlier one is still working. Because Finding 1 means nothing awaits any of this, the only signal that the open is over is the screen going quiet.",
      "evidence": [
        "`grep -c` over apps/editor + packages + plugins (tests excluded) for `pryzm-project-loaded` with an `on(`/`addEventListener`/`once(` on the line: 35 registrations, across 24 distinct files (initScene.ts ×5, initDataPlatform.ts ×4, engineLauncher.ts ×4, initUI.ts ×2, …)",
        "apps/editor/src/engine/initScene.ts:3757-3765 — post-load handler wraps `_runConsolidatedTierPbrPass?.()` in `setTimeout(…, 0)`",
        "apps/editor/src/engine/engineLauncher.ts:1502-1509 — `setTimeout(() => { zoomToAll(true).catch(() => {}); }, 150);`",
        "apps/editor/src/ui/platform/PlatformSaveController.ts:527 + :572-577 — `const CAPTURE_DELAY_MS = 3500;` … `setTimeout(() => { ... ric(runCapture, { timeout: 4000 }) ... }, CAPTURE_DELAY_MS);`",
        "apps/editor/src/engine/engineLauncher.ts:1466-1469 — `ric(() => wireCollaborationCRDT(), { timeout: 3000 })`",
        "apps/editor/src/ui/platform/__tests__/perf104OpenPath.spec.ts:52-56 — \"The founder still reports 'many minutes to open', and his log continues PAST `boot:scene-done` (wall-join resolves, §FINISH-FOLLOW-LATE-ATTRIBUTION per wall, REDETECT_ROOMS, projections). That window belongs to lane PERF105 and is deliberately not touched here\"",
        "Founder log corroborates the tail: `[ProjectSerializer] Snapshot created: 503 elements`, `[PlatformSaveController] Version saved: \"Auto-save\" (503 elements)`, `[captureThumbnail] Thumbnail captured` ×2, `[EdgeProjectorService] ... work=109ms`, `[HiddenLineRemoval] v3 - 26 occluder(s)`, `[VGSceneApplicator] applyToProjectionLayers() - styledLayers=22`"
      ],
      "estimatedCostMs": -1,
      "costBasis": "unknown",
      "fix": "Once Finding 1 lands, replace the broadcast with an ordered, measured phase list on the open session: `openSession.afterLoad(name, priority, fn)` in place of `runtime.events.on('pryzm-project-loaded', …)`. Run the steps in declared priority under the ONE frame scheduler (P3), emitting `markStartupPhase('post-load:<name>')` around each — which makes the tail attributable for the first time and lets `reportStartupBudget` finally print on the hub path. The three ad-hoc timers become priorities in that list rather than three independent races. Migrate incrementally: keep the event firing for the long tail of panels, and move only the heavy steps (tier+PBR pass, backend swap, zoomToAll, autosave re-arm, thumbnail) first.",
      "risk": "medium",
      "confidence": "high",
      "whatItWouldBreak": "The timers encode real ordering that nobody wrote down: the 150 ms before `zoomToAll` and the 3500 ms before the thumbnail exist because both need geometry AND a rendered frame, and `setTimeout(…,0)` before the tier pass exists to let fragment builders land. An ordered list must express those as explicit 'after first committed frame' / 'after builders drained' dependencies, not as smaller numbers. Handlers that today silently tolerate running twice (project switch, re-open) must stay idempotent.",
      "filesToChange": [
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\runtime-composer\\src\\ProjectLifecycleController.ts",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\apps\\editor\\src\\engine\\initScene.ts",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\apps\\editor\\src\\engine\\engineLauncher.ts",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\apps\\editor\\src\\ui\\platform\\PlatformSaveController.ts"
      ]
    },
    {
      "title": "Two independent socket.io connections join the same project room on every open — two WebSocket handshakes and two server-side DB access checks per user per open",
      "mechanism": "`PlatformShell.setProjectContext` calls `initSocketCollaboration(this.ctx, id)`, which constructs `ctx.socket = ioFn({...})` and emits `join-project` on connect. Independently, `initCollaboration` subscribes to `pryzm-project-loaded` and calls its own `connectSocket(projectId, ioFn)`, which constructs a SECOND module-local `socket = ioFn({...})` and emits `join-project` again. They are different objects with different handler sets; neither knows about the other. Server-side each join costs `await getSupabaseClient()` + `await canUserAccessProject(...)` — a DB verification round-trip. The founder's log shows both joins for one open. There is also a latent correctness edge: the server broadcasts `user-joined` to the rest of the room on each join, and `initCollaboration`'s `user-joined` handler has no self-filter, so depending on which socket connects first the user can be announced to himself as a collaborator.",
      "evidence": [
        "apps/editor/src/ui/platform/PlatformShell.ts:250 — `initSocketCollaboration(this.ctx, id);`",
        "apps/editor/src/ui/platform/PlatformCollabPill.ts:183-191 — `ctx.socket = ioFn({ transports: ['websocket','polling'], auth: ... }); ctx.socket.on('connect', () => { console.log('[PlatformCollabPill] Socket connected — joining project room:', projectId); ctx.socket.emit('join-project', projectId); });`",
        "apps/editor/src/engine/initCollaboration.ts:578-593 — `socket = ioFn({ transports: ['websocket','polling'], auth: ... }); socket.on('connect', () => { console.log('[initCollaboration] Socket connected — joining project room:', projectId); ... socket.emit('join-project', projectId); ...`",
        "apps/editor/src/engine/initCollaboration.ts:1157 — `window.runtime?.events?.on('pryzm-project-loaded', onProjectLoaded)` → :848 `connectSocket(projectId, ioFn);`",
        "Founder log shows BOTH: `[PlatformCollabPill] Socket connected - joining project room: proj-...` and `[initCollaboration] Socket connected - joining project room: proj-...`",
        "server.js:577-586 — `const supabase = await getSupabaseClient().catch(() => null); const access = await canUserAccessProject(socket.data.userId, projectId, {...});` — paid twice",
        "server.js:613-616 — `socket.to(`project:${projectId}`).emit('user-joined', {...})`; apps/editor/src/engine/initCollaboration.ts:614-617 has no self-userId filter"
      ],
      "estimatedCostMs": 150,
      "costBasis": "estimated",
      "fix": "One socket per session, owned by the runtime transport slot (there is already a `TODO(C.3.x): legacy io — replace with runtime.transport.socket` at both sites saying exactly this). Have `initCollaboration` and `PlatformCollabPill` attach their handler sets to the SAME socket instance rather than each constructing one. Independently, add the self-userId filter in the `user-joined` handler regardless of the socket work — it is one line and removes an order-dependent correctness bug.",
      "risk": "low",
      "confidence": "high",
      "whatItWouldBreak": "The two sockets currently have independent disconnect lifecycles: `initSocketCollaboration` disconnects `ctx.socket` on every `setProjectContext`, while `connectSocket` disconnects its own on every `pryzm-project-loaded`. A single shared socket must be torn down at exactly one place or a project switch will leave the previous project's room joined — which is the C13 isolation invariant, and the class of defect ProjectLifecycleController exists for. Also note the two sockets carry DIFFERENT server events (version-saved vs remote-command/remote-cursor/catch-up), so 'just delete one' loses features.",
      "filesToChange": [
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\apps\\editor\\src\\ui\\platform\\PlatformCollabPill.ts",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\apps\\editor\\src\\engine\\initCollaboration.ts",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\apps\\editor\\src\\ui\\platform\\PlatformShell.ts"
      ]
    },
    {
      "title": "26.3 MB of uncompressed JavaScript — including a 5.6 MB render-BLOCKING classic `<script>` — must be parsed before the startup instrument's t0, so it is invisible in every founder paste",
      "mechanism": "`dist/index.html` opens with `<script src=\"/cesium/Cesium.js\">` in `<head>` — no `defer`, no `async`, not a module — so the browser fetches and executes 5,909,848 bytes before it parses the rest of the document. It then loads a module entry graph of 21,628,502 bytes (main 9.36 MB, domain-engine 4.49 MB, vendor-web-ifc 3.56 MB, vendor-thatopen 2.29 MB, vendor-three 1.87 MB, three-bvh 0.07 MB). Gzip cuts wire size (~79% per the server's compression note) but parse+evaluate cost is paid on the uncompressed bytes. `beginStartupBudget()` re-bases every delta to the first mark, so this entire leg reads as `onboarding:shown +0ms` — the module's own comment says so and quotes '8.5 MB main at last build', which has itself rotted low against the 9.36 MB artifact on disk. This cost is per PAGE LOAD, not per in-tab open — but it is exactly what a founder who hard-refreshes after a deploy pays on every open.",
      "evidence": [
        "`stat` on the built artifacts: dist/cesium/Cesium.js = 5,909,848 bytes; dist/assets/main-Bzw-2t7Y.js = 9,357,174; domain-engine = 4,494,087; vendor-web-ifc = 3,556,771; vendor-thatopen = 2,285,651; vendor-three = 1,865,583; vendor-three-bvh = 68,525; modulepreload-polyfill = 711. Entry-graph total 21,628,502 bytes; grand total with Cesium 27,538,350 bytes = 26.3 MB",
        "dist/index.html line 5 — `<script src=\"/cesium/Cesium.js\"></script>` inside `<head>`, with no defer/async attribute (grep of the tag returns `<script src=\"/cesium/Cesium.js\">`)",
        "vite.config.ts:62-65 — the repo's own statement: \"`dist/cesium/Cesium.js` measures **5,909,848 bytes** in this tree, and it is a classic blocking `<script>`, not a module — the browser fetches and executes it before the page's own entry runs.\"",
        "apps/editor/src/engine/startupBudget.ts (beginStartupBudget) — \"the cost BEFORE t0 (main-chunk download + parse/eval — 8.5 MB `main` at last build) was invisible in every paste: `onboarding:shown +0ms` is tautological, not fast.\"",
        "vite.config.ts:81-83 — the opt-out set `const NO_GLOBE = new Set(['component-editor.html']);` — index.html (the editor) deliberately keeps Cesium"
      ],
      "estimatedCostMs": -1,
      "costBasis": "read-from-code",
      "fix": "Two moves, in order. (1) Add the pre-t0 leg to the instrument so it stops being invisible: `beginStartupBudget` already prints `nav+Nms`; make it also record `performance.getEntriesByType('resource')` totals for the entry chunks so a paste answers 'how much of this was download and parse?'. Without that, any bundle work is speculative. (2) Take Cesium off the blocking path for the editor entry: `cesiumWarmup.ts` already treats Cesium as a warmable chunk and `eagerGlobeStart.ts:52` documents that a hub open of an existing project NEVER constructs a globe — so the editor entry is loading 5.6 MB synchronously for a subsystem that a BIM open does not touch. Extend the existing `stripCesiumFromNonGlobePagesPlugin` seam to inject Cesium as a deferred/dynamic load driven by `warmCesiumModule()` instead of a head `<script>`.",
      "risk": "medium",
      "confidence": "high",
      "whatItWouldBreak": "`window.Cesium` is a global that `CesiumViewport` and `GISAreaLayout` read synchronously; making the script deferred means any module-evaluation-time read of `Cesium.*` becomes undefined. That is the `[[scc-no-barrel-access-at-module-load]] `/ white-screen failure mode this repo has already paid for. The migration must prove every `Cesium.` reference is inside a function body reachable only after `ensureGisInitialized`, not at module scope — grep first, and treat a single module-scope read as a blocker, not a cleanup.",
      "filesToChange": [
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\vite.config.ts",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\apps\\editor\\src\\engine\\cesiumWarmup.ts",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\apps\\editor\\src\\engine\\startupBudget.ts"
      ]
    },
    {
      "title": "P1 and the layer gate both PASS, but the open path routes around the typed runtime through 338 `window.X =` globals — which is the mechanical reason no orchestrator can exist",
      "mechanism": "Both gates are green, so this is not a gate violation — it is the gap between what the gates measure and how the open is actually wired. `composeRuntime()` returns a typed `PryzmRuntime` with typed slots, and then the open sequence is carried out through the window object: `ProjectHub` writes `window.__pendingProjectId` / `__pendingProjectName`; `PlatformShell` reaches `window.runtime?.events?.emit(...)`; `initScene` publishes the renderer live-swap verb as `window.pryzmSwapRendererBackend`; `ProjectLifecycleController` — a package at L3 that is explicitly documented as having 'zero imports from src/' — performs the entire C13 teardown by reading `(window as any).__engineTeardown`, `__curtainWallRebuildControl` and `__slabRebuildControl`. The layer rule is satisfied because the dependency is invisible to a static import graph; the coupling is total. There are 338 distinct `window.X =` assignment targets across apps/editor/src + src (219 inside apps/editor/src/engine alone). Because the open's stages talk over an untyped global bus rather than through the composed handle, there is no object that can hold the sequence — Finding 1 restated at its root.",
      "evidence": [
        "`npx tsx tools/ga-gate/check-single-compose.ts` → RC=0 · \"one composition root (packages/runtime-composer/src/composeRuntime.ts), 1/1 rival runtime factory (createFamilyEditorRuntime — declared debt, ADR-0316), 2/2 production caller(s)\" · files scanned 5537",
        "`npx tsx tools/ga-gate/check-layer-boundaries.ts` → RC=0 · \"within baselines (violations 48/102, unclassified 13/13, sdk-bypass 159/182)\" · workspace packages 170, classified 157 · violations by pair: 14 L2→L4, 12 L3→L4, 9 L1→L2, 6 L3→L6, 5 L2→L3, 1 L3→L7, 1 L6→L7 · banned third-party 87",
        "Distinct `window.<name> =` targets: 219 in apps/editor/src/engine, 338 across apps/editor/src + src",
        "packages/runtime-composer/src/ProjectLifecycleController.ts:105-110 — `const wallWasPaused = (window as any).__engineTeardown?.isWallRebuildPaused ?? false;` etc., inside a file whose own header states \"Dependency policy: zero imports from `src/`. ... so this package stays at L3 (packages/).\"",
        "packages/runtime-composer/src/ProjectLifecycleController.ts:131-140 — teardown steps 2/3/4 are `(window as any).__engineTeardown?.resetWallRebuildState()`, `(window as any).__curtainWallRebuildControl?.resumeAndFlush?.()`, `(window as any).__slabRebuildControl?.resumeAndFlush?.()`",
        "apps/editor/src/ui/platform/ProjectHub.ts:2280-2281 — `window.__pendingProjectId = id; // TODO(C.3.x): legacy __pendingProjectId — replace with runtime.persistence.openProject hint`",
        "apps/editor/src/engine/initScene.ts:5541 — `window.pryzmSwapRendererBackend = swapRendererBackend;`"
      ],
      "estimatedCostMs": -1,
      "costBasis": "unknown",
      "fix": "Do not attack 338 globals. Attack the ~8 that the OPEN path uses, and convert them into typed slots on the open session introduced in Finding 1: `__engineTeardown`, `__curtainWallRebuildControl`, `__slabRebuildControl`, `pryzmSwapRendererBackend`, `__pendingProjectId`/`__pendingProjectName`, `runtime.events` reached via window, `__pryzmProjectLoadActive`. `ProjectLifecycleController` already takes `IBatchCoordinatorTeardown` by structural injection — extend that pattern to the other three teardown handles and the swap verb, and the L3 controller stops reaching into the browser global entirely. That is a bounded change with an obvious done-condition, and it is the precondition for a real orchestrator.",
      "risk": "low",
      "confidence": "high",
      "whatItWouldBreak": "`window.__pryzmProjectLoadActive` is deliberately a global and must stay reachable from `perAddGeometryGate.ts`, a PURE module with no THREE/DOM/IO — the header says so explicitly. Converting that one to an injected handle changes a module whose purity is its whole point. Leave it; convert the other seven. Also, several of these globals are read by the diagnostic console (pryzmPerfConsole.ts) precisely because a diagnostic must not import the graph it measures — any conversion must keep a read path for the census or it will silently stop measuring.",
      "filesToChange": [
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\runtime-composer\\src\\ProjectLifecycleController.ts",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\apps\\editor\\src\\engine\\initScene.ts",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\apps\\editor\\src\\ui\\platform\\ProjectHub.ts"
      ]
    },
    {
      "title": "God objects: 31 files over 2,000 LOC hold 21% of the editor app in 2.4% of its files — a maintenance cost, and (contra the brief's hypothesis) NOT demonstrably an open-latency cost",
      "mechanism": "Measured line counts, comments and blanks separated. CesiumViewport.ts is 19,404 lines (9,033 comment, 710 blank, 9,661 CODE). siteDispatch.ts 9,430. GISAreaLayout.ts 8,532 (4,148 code). initScene.ts 5,571 (2,532 code). runtime-composer/types.ts 4,732. EdgeProjectorService.ts 4,533. SiteBoundaryMap2D.ts 4,581. OnboardingStepController.ts 4,295. initTools.ts 4,135. ResidentialBuildingExecutor.ts 3,763. initUI.ts 3,579. HouseLayoutExecutor.ts 3,595. ProjectLoader.ts 3,333. initBusHandlers.ts 3,142. CommandEventBridge.ts 2,976. ProjectRepository.ts 2,881. ViewController.ts 2,806. WallRebuildCoordinator.ts 2,766. composeRuntime.ts 2,217 (1,090 code). Across apps/editor/src the 31 files >2,000 LOC total 119,890 lines of 561,324 non-test LOC in 1,296 files. ⛔ On the LATENCY question the honest answer is negative: initScene/initTools/initUI/initBusHandlers are boot stages O4/O6/O8/O9 and the spec's O-INV-1 (verified 2026-05-24, guarded by `_bootstrapped` in src/main.ts:203) makes them once-per-tab, not once-per-open; and CesiumViewport is not constructed at all on a hub open. Their cost is bundle bytes (Finding 6) and change-risk, not per-open milliseconds.",
      "evidence": [
        "`wc -l` on apps/editor/src: CesiumViewport.ts 19404 · siteDispatch.ts 9430 · GISAreaLayout.ts 8532 · initScene.ts 5571 · SiteBoundaryMap2D.ts 4581 · EdgeProjectorService.ts 4533 · OnboardingStepController.ts 4295 · initTools.ts 4135 · initUI.ts 3579 · ProjectLoader.ts 3333 · initBusHandlers.ts 3142",
        "comment/blank/code split: initScene.ts total=5571 comment=2739 blank=300 code=2532 · CesiumViewport.ts total=19404 comment=9033 blank=710 code=9661 · GISAreaLayout.ts total=8532 comment=4145 blank=239 code=4148 · composeRuntime.ts total=2217 comment=1033 blank=94 code=1090",
        "`wc -l` on packages/runtime-composer/src: types.ts 4732 · CommandEventBridge.ts 2976 · composeRuntime.ts 2217 (12,238 total for the whole composition-root package)",
        "apps/editor/src non-test: 1,296 files / 561,324 LOC; 31 files >2,000 LOC summing 119,890 LOC",
        "docs/03-execution/specs/SPEC-PROJECT-OPEN-CREATE-PIPELINE.md §3 O-INV-1 — \"✅ Satisfied (verified 2026-05-24): `startEngine()` is guarded by the module-level `_bootstrapped` flag ... Consequence: the heavy O4–O8 LONGTASKs are a one-time cold-boot cost per tab, not a per-open cost.\"",
        "apps/editor/src/engine/eagerGlobeStart.ts:52 — \"a hub open of an existing project never calls `showOnboarding`\"; apps/editor/src/ui/layout/GISAreaLayout.ts:1084 — `cesiumViewport = new CesiumViewport(...)` only inside `ensureGisInitialized`",
        "348 `.traverse(` call sites across apps/editor/src + packages (tests excluded) — the O(n) scene walk is the app's default query primitive; the memory note 'Scene Registry to replace O(n) scene.traverse' names the missing abstraction"
      ],
      "estimatedCostMs": -1,
      "costBasis": "read-from-code",
      "fix": "Do not refactor by size. Split by the seam the latency work needs: extract from initScene.ts the ~200-line closure that owns `runTierPbrPass` / `collectNewPbrMeshes` / `countMeshes` / the backend-swap call into its own module (it is already policy-separated once — `perAddGeometryGate.ts` proves the pattern and is unit-tested), because Finding 2's fix has to touch it and today that means editing a 5,571-line composition closure. Separately, the 348 `.traverse(` sites are the real structural debt behind every 'scans the whole scene' cost in this repo: a `SceneRegistry` keyed by elementType/levelId, maintained on add/remove, retires the two full walks in `runTierPbrPass` and the mesh-count provider outright.",
      "risk": "low",
      "confidence": "high",
      "whatItWouldBreak": "none known for the extraction itself — but the initScene closure captures `world`, `scene`, `renderingCoordinator`, `toolInteractionRef` and `perfTraceOn` by lexical scope, so 'extract' means threading five dependencies, and the P2 rule means the extracted module either imports THREE (illegal outside renderer-three) or takes a structural SceneLike the way autoWebGLHeavyScene.ts:132 already does. Take the second option.",
      "filesToChange": [
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\apps\\editor\\src\\engine\\initScene.ts",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\apps\\editor\\src\\engine\\perAddGeometryGate.ts"
      ]
    },
    {
      "title": "Opening a project WRITES it back: a read-only open produces a full 503-element serialize, deflate, IndexedDB write, two forced synchronous renders and two thumbnail captures",
      "mechanism": "The post-load sweep mutates the model (wall-join resolves, REDETECT_ROOMS, late attribution), which marks the project dirty, which re-arms the autosave that `_loadSuppressActive` had deferred. `SaveOrchestrator.executeSave` then fires `onAutoSave('Auto-save')` → a full `ProjectSerializer.serialize()` of 503 elements (with `temporalGraph` embedded), deflate, and a container write to IndexedDB. That save path calls `saveAdapter.captureThumbnail()`; independently `schedulePostLoadThumbnailCapture` fires a SECOND capture 3,500 ms later. Each capture forces a synchronous `renderPipelineManager.render()` of the full 3,762-mesh scene, then a `getImageData` GPU→CPU readback and a WebP encode ladder, then an IndexedDB write and a server PATCH. The founder's log shows all of it. The design is deliberate — the comments explain the deferral and the single-snapshot consolidation — but the invariant nobody stated is that OPENING should not require WRITING, and it currently does because the open path is also an edit path.",
      "evidence": [
        "apps/editor/src/ui/platform/SaveOrchestrator.ts:349-356 — `if (this._loadSuppressActive) { this.cancelDebounce(); this.setStatus('pending'); return; }` with the comment \"handleLoadSuppressEnd() re-arms the debounce once the load fully settles, so the whole open produces exactly ONE post-load snapshot\"",
        "apps/editor/src/ui/platform/SaveOrchestrator.ts:393 — `this.onAutoSave('Auto-save');`",
        "Founder log: `[ProjectSerializer] Snapshot created: 503 elements, 7 levels, 56 walls, 10 slabs, 95 furniture (integrity 44c4f1ed-16588a) - temporalGraph 2366 mutations / 0 edges` and `[PlatformSaveController] Version saved: \"Auto-save\" (503 elements)` and `[VersionRepository] reason=save-version - 6 version(s) persisted to IndexedDB (~1.4 MB (1,465,135 chars) compressed …)`",
        "apps/editor/src/ui/platform/PlatformSaveController.ts:357 (save path) and :551 (post-load path) — two independent `this.ctx.saveAdapter.captureThumbnail?.()` calls; founder log shows `[captureThumbnail] Thumbnail captured` TWICE",
        "apps/editor/src/engine/initPersistence.ts:174-176 — `const rpm = window.renderPipelineManager; if (rpm) rpm.render();` — a forced synchronous full-scene render inside each capture",
        "apps/editor/src/engine/initPersistence.ts:220 — `const { data } = ctx.getImageData(sampleX, sampleY, sampleW, sampleH);` — GPU→CPU readback"
      ],
      "estimatedCostMs": -1,
      "costBasis": "unknown",
      "fix": "Two things. (a) De-duplicate the thumbnail: the save-path capture and the post-load capture answer the same question 3.5 s apart. Have `schedulePostLoadThumbnailCapture` no-op when the post-load autosave already produced one for this projectId — one flag, and it removes a forced synchronous render of a 3,762-mesh scene. (b) Decide, explicitly, whether the post-load sweep's mutations are AUTHORED changes or DERIVED state. Wall joins, room re-detection and late attribution are derived — recomputed identically from the same snapshot every open — so they should not mark the model dirty and should not trigger a version write. Route them through a `deriving` flag on the orchestrator (the mechanism already exists as `_loadSuppressActive`; it just ends too early) so opening a project you did not edit writes nothing.",
      "risk": "medium",
      "confidence": "medium",
      "whatItWouldBreak": "(b) is the dangerous one and I have NOT established which post-load mutations are genuinely derived. If a healing pass (e.g. the room-boundary heal, or the §GRID106 orphan-annotation purge at engineLauncher.ts:1518) repairs REAL corruption, suppressing its save means the repair is redone on every open forever and never persisted — trading a write for permanent recompute, and hiding a data defect. This needs the list of post-load mutators classified one at a time before any flag is set; §CONFIDENT-REGISTER-ROWS applies. (a) is safe and independent — do it first.",
      "filesToChange": [
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\apps\\editor\\src\\ui\\platform\\PlatformSaveController.ts",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\apps\\editor\\src\\ui\\platform\\SaveOrchestrator.ts"
      ]
    }
  ]
}