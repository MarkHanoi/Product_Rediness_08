{
  "survivingTitles": [
    "The heavy-scene backend swap fires MID-HYDRATION, so every GPU resource is minted on WebGPU and then re-minted on a classic WebGL renderer — this is the sole source of the 'device lost: destroyed' pair",
    "Two full-project serializes on every open — the `resetDirtyAfterLoad()` hash baseline is discarded by the auto-save that follows",
    "captureThumbnail runs twice per open from two uncoordinated schedulers, and each run forces a synchronous full-scene render plus a GPU readback",
    "NOT A DEFECT — the duplicated 'WebGPU device lost' log line is two handlers on ONE device, and both correctly refuse to recover"
  ],
  "refutedTitles": [
    "Two socket.io clients connect and join the same project room — two call sites, two websockets, two server-side access checks"
  ],
  "refutations": [
    "REFUTED FOR THIS DIMENSION (real duplicate, but it does not cost OPEN time). Every code claim checks out — PlatformShell.ts:250 `initSocketCollaboration(this.ctx, id)`; PlatformCollabPill.ts:184-191 builds `ctx.socket = ioFn({...})` and emits `join-project` on connect; initCollaboration.ts:578-593 builds its own closure-local `socket` and emits `join-project`; initCollaboration.ts:848 is reached from the `pryzm-project-loaded` subscription at initCollaboration.ts:1157 (`onProjectLoaded` is at :1133, not :1140); server.js:566-587 runs `canUserAccessProject` per join; the two script ids (`plat-socketio-client` at PlatformCollabPill.ts:174, `pryzm-collab-socketio-client` at initCollaboration.ts:857) really are different. So two sockets, two rooms joins, two Supabase reads — all true. BUT NOTHING ON THE OPEN PATH AWAITS ANY OF IT. `initSocketCollaboration` is fire-and-forget inside `setProjectContext` and returns before the socket connects; `connectSocket` is fired from an event handler after the load has already completed; neither the version read, the hydration, the tier pass, the serialize nor the autosave blocks on a socket. The second `canUserAccessProject` round-trip is paid by the SERVER, concurrently, off the client's critical path. The one main-thread item — the `/socket.io/socket.io.js` script fetch — is genuinely de-duplicated already by the `window.io` guard in both loaders, which the finding itself concedes. The claimed '40ms (open)' is therefore not established by anything in the code: it is server load and a leaked socket for the session, not open latency. Worth fixing for room hygiene and DB load (and the socket does leak across a hub round-trip), but sending a perf lane here to shorten the open would be sending it at nothing."
  ],
  "missed": [
    "⭐ THE LARGEST DUPLICATE ON THE OPEN PATH, AND THE AUDIT DOES NOT NAME IT: the open performs TWO full latest-version reads — one from the SERVER, awaited, and then throws it away. `buildPersistence.ts:316` does `const bundle = hint?.isNewProject ? null : await tier.streamLoad(projectId);` — an AWAITED `fetch('/api/projects/<id>/latest-version')` plus `await res.json()` of the entire snapshot (buildPersistence.ts:153-190) — and threads it into `setProjectContext` as `prefetchedVersion` (buildPersistence.ts:333-337). PlatformShell then reads `prefetchedVersion` ONLY inside the `else` branch: `const latest = versionRepository.getLatestVersion(id); if (latest) { this.versionCtrl.loadVersion(latest); }` (PlatformShell.ts:297-301) — `prefetched` is not touched until PlatformShell.ts:333, reachable only when there is NO local version. The founder HAS local history (his own log: '[VersionRepository] … 6 version(s) persisted to IndexedDB'), so on his open the server round-trip fetched and JSON.parsed a full 503-element snapshot that was discarded unread, while `getLatestVersion(id)` (PlatformShell.ts:297) did the real inflate from IndexedDB. Two full snapshot reads, one of them a blocking network fetch + parse, on the critical path — strictly larger than the 40 ms socket item and probably larger than the thumbnail item. Fix shape: skip step 3 when `versionRepository.getLatestVersion(id)` already answers, or invert so the local hit short-circuits `openProject` before `streamLoad`.",
    "`openProject` step 1 can fire the project-list server refresh TWICE, both awaited, before any load begins: `if (projectListStore.isEmpty()) { await controller.refresh(); }` then, if the id is still not in the list, `await controller.refresh();` again (buildPersistence.ts:266-274). On a deep-link / hard reload (empty store) that is two sequential list round-trips ahead of the snapshot fetch. PlatformRouter.ts:1240-1246 already flags this leg as the `open:persistence-openProject → boot:ensure-requested` cost.",
    "The audit frames finding #2 as 'two serializes' but stops short of the bigger half: the open path triggers a COMPLETE VERSION WRITE that no user edit asked for. `ProjectLoader`'s post-load room-redetect sweep (ProjectLoader.ts:2990-3060, one `room.redetect` per level) dirties the model, `handleLoadSuppressEnd()` (SaveOrchestrator.ts:539-543) arms the debounce, and the result is the founder's `[VersionRepository] reason=save-version — 6 version(s) persisted (~1.4 MB compressed)` + `[PlatformSaveController] Version saved: \"Auto-save\" (503 elements)` — a main-thread deflate, an IndexedDB write, a thumbnail encode and a server upload, all provoked by the loader's own sweep rather than by the user. Note the sweep already SKIPS levels whose rooms were persisted (`skipRedetectLevels`, ProjectLoader.ts:3008-3018), so a large part of what makes the model 'dirty' is work the snapshot had already answered. Measuring how much of the load-induced diff is real is the prerequisite to deciding whether the whole load-triggered autosave can be suppressed — which would delete the serialize, the deflate, the write, the thumbnail and the upload in one move.",
    "On finding #1, one factual correction the next lane needs or it will look in the wrong place: THE SWAP DOES NOT FIRE MID-HYDRATION. The per-add tier pass is deliberately SKIPPED during a load (§FIX-LOAD-TRAVERSE-BATCH; initScene.ts:3049-3053 logs `loadActive` precisely because 'the per-add traverse below is skipped, one pass runs at load end'), and the one consolidated pass runs from the `pryzm-project-loaded` handler at initScene.ts:3757-3766 via `_runConsolidatedTierPbrPass = () => runTierPbrPass('post-load')` (initScene.ts:3038). That is why ISSUE-LOG L-1483 records the reason as `tier:post-load`, not `tier:add:*`. The cost claim survives intact (the whole scene is still built on WebGPU and then re-minted on the new renderer inside the open window), but the trigger is a single load-END pass, and the auditor's own fix — hoisting the decision to the snapshot's element count via `proactivelySwitchToWebGLForBuildingGeneration` (autoWebGLHeavyScene.ts:283, currently wired only at buildingGenerationLifecycle.ts:307) — is if anything MORE clearly right against the corrected timing.",
    "On finding #1, the attribution is INFERRED, not proven, and the auditor's own falsification test comes back negative on the evidence supplied. A live backend swap definitely happened (reason=\"destroyed\" ⇒ our `device.destroy()`; the only production `retireRenderer` callers are initScene.ts:5126, createRenderer.ts:626 — excluded, it sits below `if (isDeliberateDeviceDestroy(info)) return;` — and `WebGPURendererAdapter.dispose()` at :333, which has NO production caller anywhere outside apps/bench). But WHICH trigger fired the swap is not settled: `RendererBackendToggle.ts:145` drives the identical `window.pryzmSwapRendererBackend` path, and docs/03-execution/lanes/UNDO93-SCENE-CLASH-FIX-PLAN.md §1 documents a founder capture where this exact `reason=\"destroyed\"` pair came from the USER clicking the corner toggle while the auto-swap gate provably could not have fired. Three lines that the auto-swap path prints UNCONDITIONALLY are all absent from the founder's excerpt: `[autoWebGLHeavyScene] §AUTO-WEBGL-HEAVY — device-loss-risk scene …` (autoWebGLHeavyScene.ts:348/354), `[initScene] §RETIRE-RENDERER-DETACHES-LISTENERS …` (initScene.ts:5127-5136) and `[initScene] §RENDERER-LIVE-SWAP live swap complete` (initScene.ts:5152). Get those three from the founder's full console before spending the 1500 ms estimate — and note the estimate is unmeasured; nothing in the repo times the swap.",
    "On finding #3, the open-time cost is roughly HALF what is claimed. The duplicate is real (two independent callers, PlatformSaveController.ts:356-357 on the autosave and :552 on the post-load path, one `captureThumbnail` implementation at initPersistence.ts:141 logging once per call at :305). But only the autosave one is on the critical path; the other is `setTimeout(3500)` + `requestIdleCallback(…, {timeout:4000})` (PlatformSaveController.ts:571-578), i.e. deliberately outside the open. And the encode ladder is 6 rungs (packages/core-app-model/src/preview/thumbnailBudget.ts:68-75 — note the path, the finding cites a bare filename) but STOPS at the first rung that fits, so a normal capture pays one WebP encode of a ≤400×225 canvas, not six. The genuinely heavy part is the forced `rpm.render()` of 3762 meshes (initPersistence.ts:174-175) plus the `drawImage` off the renderer canvas at :207; the `getImageData` at :219 reads a 32×32 patch of the already-CPU-side 2D canvas, not a GPU readback. Budget ~one full extra frame + one encode for the open, not 300 ms for two."
  ]
}