{
  "refuted": false,
  "reason": "THE DIAGNOSIS HOLDS — I confirmed essentially every load-bearing citation, including two the author could not have guessed. But THE FIX DOES NOT, and one of its four steps would restore a defect this repo fixed last week.\n\n── WHAT I CONFIRMED ────────────────────────────────────────────────────────\n1. `buildMassingOptionsFold` has EXACTLY ONE production call site: GISAreaLayout.ts:5790, inside the full-determination arm. `wireMassingOptions(panel)` at :5863 likewise. `buildAuthoredMassingOptionHtml` has exactly one call site, envelopeCardSections.ts:582, INSIDE that fold. So the un-gated authoring card is structurally inside a gated container. Correct.\n2. The refusal predicate at :5179 and the `return` at :5362 are as described. The founder's parcel does land there: `barcelona22ArrobaDerivedPlanRefusal` (esBarcelonaZoneClassification.ts:859) returns `code:'derived-plan'`, `legallyGrounded:true`, so `isTransient`/`isAbsent`/`isGap` are ALL false at :5237-5240 and both the study-height entry (:5241) and the manual-zone button are withheld. Chip reads \"No envelope applies\" — matching the founder's screen.\n3. `permittedRing: env?.insetPolygon ?? []` at :4011 is the ONLY feed into `enumerateMassingOptions` (the other `insetPolygon ?? []` at :3830 feeds `solveTargetFootprintArea`, a different function), so the :404 guard is genuinely unreachable-past on a refusal. Correct.\n4. designStageModel.ts:141 `reached('massing') = hasResolvedEnvelope || hasMassingProposal`; :5121 feeds it `resolveLiveTargetFootprintProposal(null)`, and that call CLEARS the slot (targetFootprintAreaState.ts:159-162). Correct.\n5. C58 §866 quotes verbatim; §1.20 clause 1 does literally ask for this.\n6. The authoring pipeline is decoupled, and MORE so than claimed: `window.pryzmOpenSiteEnvelopeTool` (:7210) is un-gated, both draw ports gate only on `resolveSiteDrawFrame` (siteEnvelopeDrawCesium `cannotArmReason`, siteEnvelopeDrawMap2D:120), the drawn rung outranks all three solved rungs (parcelLawEnvelopeAuthoring.ts:571), AND the Parcel Law tab's question-2 authoring section is already live on a refusal by design (`§NO-RULE-PACK-STILL-AUTHORS`, L-12993, its Córdoba case). Verdict (b) — affordance, not capability — is right and understated.\n7. The blindness explanation is exact: massingAuthoredOptionSection.spec.ts:203-206 has a section headed \"REACHABILITY\" that tests only the fold's TWO arms and never that the fold reaches the card's arms. The defect is invisible by construction.\n\n── THREE FACTUAL ERRORS (all minor) ────────────────────────────────────────\n· siteDispatch.ts:7833 is the BALEARS branch, not Barcelona. The arm accepts `'not-applicable' || 'none'` + refusal, so the conclusion survives.\n· \"three arms\" is four. `renderReducedEnvelopePanel` (the legacy-persisted branch, taken BEFORE both arms the fix names) also carries no fold, and `renderEnvelopeAbsencePanel` has six sub-arms.\n· Fix (4)'s premise is FALSE: `readLevelEnvelopes` appears at only two sites, :3914 (adopt) and :5776 (full arm). The refusal arm does not read it today.\n\n── WOULD THE FIX WORK? ─────────────────────────────────────────────────────\n(2) YES. Prepending `authoredCard` on envelopeCardSections.ts:598's refused arm is correct, one line, and no test asserts its absence. Highest value per byte of the four.\n\n(4) YES, mechanically. Two notes: it breaks two spec fixtures at compile (designStageModel.spec.ts:33, designStageWire.spec.ts:53 build `DesignStageInputs` literals), and the NO_ENVELOPE rewrite is the one item that changes a sentence about the law — it belongs WITH the founder-open question, not below it.\n\n(1) MOSTLY — and one worry I had is wrong, so state it: routed through `buildStagedSectionsHtml`, `'massing-options'` would NOT render greyed. On a committed parcel with a readable measurement, `massing` is the first unreached stage, so `describeDesignStages` marks it `'available'` → relevance `'lead'` (designStagePanel.ts:157) → rendered first, ungreyed. Good. But two real problems remain:\n  a. THE INTRO IS A FALSE STATEMENT ON THAT ARM. envelopeCardSections.ts:576 reads \"Generate several massings of the permitted footprint…\" — asserting a permitted footprint on a card whose entire content is that none exists. That is the C58 §1.4 class the card is built around.\n  b. THE GENERATE BUTTON BECOMES A GUARANTEED-REFUSAL DEAD CLICK, and the fix's own \"smallest-first\" ordering ships it before (3) can make it succeed. This file already states the invariant, at GISAreaLayout.ts:4890-4892, as the reason the study-height entry is withheld on two absence arms: \"an input that can only refuse is the dead click the strip exists to end.\" Worse, pressing it flips the fold to the `refused` arm — which, until (2) lands, DELETES the \"Create it myself\" card the fix exists to restore. (1) and (2) are not independently shippable, and the Generate button must be withheld on that arm.\n  c. Applied flatly to `renderEnvelopeAbsencePanel`, it puts a Create-it-myself route on the `runtime-unreachable` and `resolving` sub-arms, where PRYZM has not finished asking. That panel already discriminates them (:4869 `cannotJudgeStaleness`); the fix does not.\n\n(3) NO — THIS ONE BREAKS THINGS. Three measurable failures:\n  a. THE STALENESS GATE WIPES THE RESULT ON THE NEXT READ. Picking an option writes `setTargetFootprintProposal`. Every renderer reads through `resolveLiveProposedPlate` → `currentPermittedFootprintAreaM2()` = `env.insetAreaM2 || shoelace(env.insetPolygon)` (liveProposedPlate.ts:78-83). On a refusal `env` is NON-NULL with no inset, so that returns **0** — not null — and `resolveLiveTargetFootprintProposal(0)` takes the `permittedAreaM2 <= 0` branch and CLEARS the slot. The card's own refusal arm does the same at :5121. So a plate picked from a drawn-ring option is withdrawn from the plan scene, CesiumViewport and SiteBoundaryMap2D on the first read after the click — the §USE-THIS-PLATE-KEEPS-THE-LOOP defect (L-13078) restored. Fix (3) touches neither targetFootprintAreaState.ts nor liveProposedPlate.ts.\n  b. THE \"PERMITTED\" LABELS FOLLOW THE RING. The :404 guard reads `inputs.permittedFootprintM2`, so the drawn ring's area must be passed in a field named *permitted* — and then `coverageOfPermitted`, `storeysToRealisePermittedGfa` and the intro all describe a user-drawn ring as permitted. Adopting one stamps `PLATE_PROVENANCE_DETAIL` (adoptProposalAsEnvelope.ts:231) — \"fitted inside the permitted footprint by the target-area / massing-option solver\" — into the element's provenance, on a parcel where the ordinance refused. The fix proposes swapping only the caveat string.\n  c. IT IS A SECOND CHANNEL WHERE THE CONTRACT NAMES A FIRST. C58 §1.19 clause 5: \"AN AUTHORED ENVELOPE IS A FIRST-CLASS INPUT DOWNSTREAM … generation is bounded by whichever envelope is ACTIVE, and the active choice is the user's and is stated.\" §1.19 clause 1 forbids \"a second envelope pipeline\". A `ringBasis` discriminant known only to the enumerator is that second pipeline, and it is why (a) and (b) exist; routing through an ACTIVE-envelope selection fixes both as a side effect.\n\n── THE INVARIANT THE CURRENT CODE IS PROTECTING ────────────────────────────\nThree, not one, and only the first is bogus:\n  (i) \":5863 — every option is a fraction of a PERMITTED footprint, and a refusal card has none.\" TRUE of the generated options, FALSE of the authored card the same fold carries. This is the real defect, and (1)+(2) are the right answer to it.\n  (ii) massingOptionModel.ts header: \"`template ∩ buildableFootprint ⊆ buildableFootprint` BY CONSTRUCTION — so a shape produced that way can NEVER over-state what may be built. That containment is a property of the operator, not of a check someone remembered to write.\" Feeding a drawn ring preserves containment but voids the sentence; the header must be amended in place, not left standing.\n  (iii) The plate's whole meaning is \"this fits in THAT\", enforced by a staleness gate keyed on the permitted AREA. That is the one fix (3) walks into blind.",
  "evidenceChecked": [
    "apps/editor/src/ui/layout/GISAreaLayout.ts:5179-5362 — the refusal arm: innerHTML, wire list, early return. Confirmed no massing fold, no authored card, no data-stage-control=\"massing\" element.",
    "apps/editor/src/ui/layout/GISAreaLayout.ts:5758-5872 — the full arm: the ONLY buildMassingOptionsFold call (:5790) and the ONLY wireMassingOptions (:5863).",
    "apps/editor/src/ui/layout/GISAreaLayout.ts:4831-4938 renderEnvelopeAbsencePanel — six arms, no fold; :4890-4892 states the 'an input that can only refuse is the dead click' invariant the fix's step (1) would violate.",
    "apps/editor/src/ui/layout/GISAreaLayout.ts:4943-5010 refreshEnvelopePanel — FOUR arms, not three: renderReducedEnvelopePanel is a fifth surface the fix does not name.",
    "apps/editor/src/ui/layout/GISAreaLayout.ts:3996-4020 wireMassingOptions — permittedRing: env?.insetPolygon ?? [] at :4011; :3830 is solveTargetFootprintArea, a different function, so :4011 is the sole enumerator feed.",
    "apps/editor/src/ui/layout/GISAreaLayout.ts:5111-5142 — hasMassingProposal via resolveLiveTargetFootprintProposal(figures.footprintM2>0 ? … : null).",
    "apps/editor/src/ui/site/envelopeCardSections.ts:562-635 buildMassingOptionsFold — intro at :576 asserts a permitted footprint; authoredCard at :582; the refused arm at :596-608 drops it.",
    "apps/editor/src/ui/site/envelopeCardSections.ts:487-523 buildStagedSectionsHtml — a gated section is GREYED WITH ITS REASON, never hidden.",
    "apps/editor/src/ui/site/designStagePanel.ts:38-160 — ENVELOPE_CARD_SECTION_STAGE['massing-options']='massing'; 'available' maps to relevance 'lead', so the fold would render ungreyed and FIRST on the refusal arm.",
    "apps/editor/src/ui/site/designStageModel.ts:118-273 — NO_ENVELOPE at :121-124, reached('massing') at :141, describeDesignStages' single-'available' rule at :240-253.",
    "apps/editor/src/ui/site/massingOptionModel.ts:1-120 header — the 'template ∩ buildableFootprint ⊆ buildableFootprint … can never over-state what may be built' safety property; :250-300 the NEVER-0-for-unknown axis rule; :396-414 the no-permitted-footprint guard.",
    "apps/editor/src/ui/site/targetFootprintAreaState.ts:135-172 resolveLiveTargetFootprintProposal — CLEARS the slot when permittedAreaM2 is null or <= 0.",
    "apps/editor/src/ui/site/liveProposedPlate.ts:70-118 — currentPermittedFootprintAreaM2() returns 0 (not null) for a non-null refused envelope, so every renderer wipes a drawn-ring plate on read.",
    "apps/editor/src/ui/site/adoptProposalAsEnvelope.ts:229-235 PLATE_PROVENANCE_DETAIL — 'fitted inside the permitted footprint …' would become a false provenance under fix (3).",
    "apps/editor/src/ui/site/massingAuthoredOptionSection.ts (whole file) — :44 the 'Present on every arm — the tool is never gated' doc; :180-190 wireAuthoredMassingOption.",
    "apps/editor/src/ui/site/drawnEnvelopeFootprintState.ts (whole file) — ring is project-frame scene-XZ, session-only, cleared only by explicit Discard or redraw (so the fallback ring does survive a commit).",
    "apps/editor/src/ui/analysis/parcelLawEnvelopeAuthoring.ts:1-80 §NO-RULE-PACK-STILL-AUTHORS and :540-605 resolveFootprintSource — the drawn rung ranks first, and the Parcel Law tab already authors on a refused parcel.",
    "apps/editor/src/ui/analysis/parcelLawTab.ts:1140-1160 — 'data-stage-control=\"massing\" is set on three envelope-card elements and on NOTHING in this block'; the tab hosts the same envelope card via pryzmMountEnvelopeCard.",
    "apps/editor/src/ui/site/envelopeAbsenceCard.ts — no draw/author affordance on any absence arm; only the solve button.",
    "apps/editor/src/ui/site/siteEnvelopeDrawCesium.ts cannotArmReason + siteEnvelopeDrawMap2D.ts:120 cannotArmReason — both gate on resolveSiteDrawFrame only.",
    "apps/editor/src/ui/layout/GISAreaLayout.ts:7189-7222 window.pryzmOpenSiteEnvelopeTool + apps/editor/src/ui/gis/gisActionRegistry.ts:489 row 'site.create-envelope'.",
    "packages/site-parcel-data/src/rulepacks/esBarcelonaZoneClassification.ts:859-922 — code 'derived-plan', legallyGrounded: true for clau 22@.",
    "docs/02-decisions/contracts/C58-ZONING-RULES-AND-BUILDABLE-ENVELOPE.md §1.19 (clauses 1,3,4,5) and §1.20 (clauses 1-4) — the §866 quote is verbatim; clause 5 names the ACTIVE-envelope mechanism fix (3) bypasses.",
    "apps/editor/src/ui/site/__tests__/massingAuthoredOptionSection.spec.ts:200-256 — the 'REACHABILITY' block tests only the fold's two arms, never that the fold reaches the card's arms.",
    "apps/editor/src/ui/site/__tests__/designStageModel.spec.ts:33 and designStageWire.spec.ts:53 — DesignStageInputs literals that fix (4) would break at compile."
  ],
  "betterHypothesis": "The diagnosis is right; the DECOMPOSITION is wrong. Instead of \"make the gated fold reach the refusal arm\" (which drags a false intro, a dead Generate button and the staleness gate along with it), lift the un-gated half OUT of the fold:\n\nSTEP A — promote the authored card to its own section. Add `'authored-massing'` to `ENVELOPE_CARD_SECTIONS` (designStagePanel.ts:38-58) with stage `'massing'`, move `buildAuthoredMassingOptionHtml` + `wireAuthoredMassingOption` out of `buildMassingOptionsFold` into it, give its container `data-stage-control=\"massing\"`, and render it on ALL FOUR arms — reduced, absence (except `runtime-unreachable`/`resolving`), refusal, full. One section, one testid, no Generate button, no \"permitted footprint\" sentence, no exposure to the plate staleness gate. It satisfies C58 §1.20 clause 1 literally, and it finally gives the design-stage pill a jump target on the refusal arm, which today has none (the three `data-stage-control=\"massing\"` carriers — envelopeCardSections.ts:1487, :1664, :1798 — are all withheld there). This subsumes the author's steps (1) and (2) and removes their coupling to each other.\n\nSTEP B — keep the fold where it is. `wireMassingOptions`/`buildMassingOptionsFold` stay on the full arm. Its :5863 comment then becomes TRUE rather than half-true, which is the honest resolution of the contradiction the author correctly identified.\n\nSTEP C — for generated shapes on a refused parcel, use the channel C58 §1.19 clause 5 already names, not a new `ringBasis` field: make the AUTHORED envelope selectable as the ACTIVE envelope, and have `currentPermittedFootprintAreaM2()`, `permittedStudyFigures`, the `:5121` stage read and `PLATE_PROVENANCE_DETAIL` all read the ACTIVE one with its basis stated. That is the single change that makes the staleness gate, all three renderers and the adopt provenance correct at once — none of which the `ringBasis` proposal touches, and all three of which it silently breaks. It also forces the `template ∩ buildable ⊆ buildable` sentence in massingOptionModel.ts's header to be amended in place rather than left standing over a ring the ordinance never permitted.\n\nSTEP D — the author's (4), unchanged, but with its premise corrected: the refusal arm does NOT read `readLevelEnvelopes` today (only :3914 and :5776 do), so the read has to be added, and two spec fixtures need the new field.\n\nFALSIFIER TO RUN FIRST, cheaper than the author's: on the founder's parcel, open the PARCEL LAW tab rather than the site view and look at question 2. `mountParcelLawEnvelopeAuthoring` is explicitly live on a refused parcel (its §NO-RULE-PACK-STILL-AUTHORS block, written for exactly this case at Córdoba). If Create envelope works there, the capability is proven reachable and the whole defect collapses to \"the envelope card deleted its own route\", which is Step A alone — and Step C becomes a separate, contract-shaped piece of work rather than part of a bug fix."
}