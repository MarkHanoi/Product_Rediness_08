{
  "refuted": true,
  "reason": "The ANATOMY is verified; the ROOT CAUSE and the 12,000 ms saving are not. Every structural claim checks out: `ProjectLoader.ts:761/:2876` bracket hydrate; `:733 storeEventBus.beginBatch()`; `ImportProjectCommand.ts:326-350 runSub` is `canExecute` + `execute` per element (replay, not deserialise); `FurnitureStore.ts:4 const _bus = new DOMEventBus()` and `:51-52` fire the DOM channel then the batched one; `DOMEventBus.ts:46-52` is a bare synchronous `dispatchEvent`; `initBuilders.ts:883` builds the THREE subtree inline; the generator has exactly 7 yields (`:632,747,1074,1077,1107,1218,1221`) at `IMPORT_CHUNK_SIZE=120`, so walls=56/slabs=10/CW=90 never trip a chunk boundary; `TopologyLayer.ts:365` is lazy; `LightingFragmentBuilder` coalesces; VDT rides the batched bus; the §G3-STALE-FIX ordering hazard is real (`plugins/wall/src/handlers/CreateWall.ts:69-78`). None of that is in dispute. FIVE things refute the causal claim. (1) NOT NEW — `docs/04-reference/APPLICATION-PERFORMANCE-LEDGER.md` §4.4 already documents this exact mechanism (\"Three suppression mechanisms, and the channel every listener uses honours none of them\"), with the same subscriber table, and §4.5 already documents the chunking gap. The ledger deliberately assigns it NO magnitude. (2) THE HEAVIEST SUBSCRIBER IS ALREADY GATED, and the brief cites the proof as if it were an argument FOR its fix. `initScene.ts:4085-4124` — `_debouncedGeomAdded`, the 41-event full-`scene.traverse()` + shadow-flag handler, the single largest body on the DOM channel — now reads `if (isProjectLoadActive()) { _geomAddedSuppressedDuringLoad++; return; }` with the one-shot escape hatch. The log line `[LOAD-SHADOW-PASS-DEFERRED] … 7.7ms for 500 caster-mutation event(s)` IS that gate having already fired. So the fix's own best precedent has been applied to the expensive case, and the remaining 17.16 s is what is left AFTER it. (3) THE BRIEF'S OWN CALIBRATION KILLS ITS ARITHMETIC. It establishes 7.7 ms per full-scene pass over the finished 3,762-mesh scene. Sixteen seconds of fan-out therefore needs ~2,000+ full-scene passes. The four unguarded subscribers it names can queue at most low hundreds over a smaller, half-built scene — a few hundred ms. And its headline subscriber is weaker still: `UnifiedBrowserPanel`'s 26 registrations all funnel into `ToolsRailController.refreshIfActive` (`:151`), which early-returns unless `_activeId === id`, and `_activeId` initialises to `null` (`:30`) — at cold open with the rail closed, all 26 are no-ops, not \"880 runs\". (4) THE PREMISE \"the import drain is the only heavy span inside those brackets\" IS UNMEASURED, and the instrument that measures it is the one the brief did not name. `ProjectLoader.ts:2875 __marksSummary()` prints `§LOAD-HYDRATE-STEPS` UNCONDITIONALLY, splitting hydrate into `load_setup` / `element_import` / `migrations` / `nonelement_stores`. The brief names §LOAD-IMPORT-STEPS and §LOAD-YIELD-WAIT but never this one — yet `nonelement_stores` covers `:935→:2872` (ledger §4.1 row 10: \"19 subsystems in one bucket\"). Whether the drain owns hydrate at all is exactly what is unread. (5) THE SAVING HAS NO DERIVATION, and the ledger already names this failure shape. The brief's own accounting reaches <1.2 s of 17.16 s and it states plainly it cannot name the owner of the residual — then claims 12,000 ms \"derived-from-log\". Ledger §4.3 refuses precisely this move for the 14,950 ms figure, citing [[confident-register-rows-are-the-wrong-ones]]. SECONDARY: the mesh-construction refutation over-generalises one extruded-profile builder in headless vitest to FurniturePart (1,487 meshes / 229,722 tris), PlumbingFixture (91,842 tris / 201 meshes) and Lighting (ledger §4.5: each PointLight triggers a shader-permutation rebuild) — and is moot for the fix anyway, since batching the DOM channel leaves builder work untouched (\"the builder still runs\"). The brief also omits the two subscribers the ledger rates heaviest after the gated one: `HierarchyTreePanel.ts:1330-1343` (18 events → `_elementCache.clear(); this._render()`, no debounce, no guard) and `ViewportPreviewRenderer.ts:173-179` (15 events → `wallStore.getAll()`, another §4.2 deep clone). WOULD-BREAK is sound and I confirm it: fix (a) inverts the §G3-STALE-FIX ordering and would manufacture more G3 fallbacks, not fewer. STEP 2 (time-budget yield) is correct, contract-clean, and worth doing — but the brief concedes it saves nothing, and it can make the load LONGER, since each extra yield waits a full frame of a viewport the ledger measures at 6 scene-graph walks/frame (the loader's own §LOAD-YIELD-WAIT comment states this: \"if a frame costs 500 ms, every yield costs 500 ms\").",
  "betterHypothesis": "The 17.16 s is still UNATTRIBUTED, and three instruments in that same console split it — one of which the brief never names. Read all three before any code change: (1) `§LOAD-HYDRATE-STEPS` (`apps/editor/src/engine/persistence/ProjectLoader.ts:2875`, `__marksSummary()`, printed unconditionally) — decides whether `element_import` owns hydrate at all versus `nonelement_stores` (`:935→:2872`, the 19-subsystem bucket) or `migrations`; the brief's whole causal chain assumes an answer this line already prints. (2) `§LOAD-IMPORT-STEPS` (`:920-926`) — names the family within the drain. (3) `§LOAD-YIELD-WAIT` (`:906-912`) — the strongest untested candidate: with only ~5-7 yields the loader parks in the frame bus for a full frame each time, and this run's frames are drawn by a WebGPU pipeline that immediately afterwards logs a device loss, a live swap to WebGL, and a 16.0 s SHADOW_REBUILD over 3,274 casters — so `waiting` could be seconds with zero of it visible in any element counter. THEN, if and only if compute dominates and the fan-out is implicated, the target list is the ledger's, not the brief's: `HierarchyTreePanel.ts:1330-1343` (18 events → cache clear + full DOM re-render, no debounce, no guard) and `ViewportPreviewRenderer.ts:173-179` (15 events → `wallStore.getAll()` deep clone) are strictly heavier per event than three of the four sites the brief lists, and `UnifiedBrowserPanel` is a no-op at cold open. The correct fix shape is the one already proven at `initScene.ts:4121` — per-subscriber `isProjectLoadActive()` + a one-shot catch-up on BOTH load-end signals (fix option b), NOT batching `DOMEventBus` (fix option a), which inverts the §G3-STALE-FIX registration ordering the loader depends on. Land STEP 2's time-budget yield on its own merits as a freeze fix, measured against §LOAD-YIELD-WAIT before and after — and stop calling it a 12 s saving."
}