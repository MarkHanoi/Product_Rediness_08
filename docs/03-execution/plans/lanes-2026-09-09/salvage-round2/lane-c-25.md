{
  "survivingTitles": [
    "The open path AWAITS a full-snapshot server fetch whose result is thrown away whenever local history exists",
    "GET /api/projects/:id/latest-version is two SERIAL Supabase round trips, and the whole snapshot is parsed and re-serialised inside the Fly process",
    "Two independent socket.io connections are opened per project open — double handshake, double uncached DB access check",
    "No cache on the per-request project access check — every project-scoped route pays an extra Supabase round trip before doing any work",
    "The snapshot is one unpaginated, unstreamed blob — the client-side streaming plane exists but the server endpoints it needs were never built",
    "Unbounded COUNT(*) on project_versions runs TWICE per save, on a table that grows per save and is never pruned",
    "GET /api/v1/portfolio is a true serial N+1 that loads the FULL snapshot of up to 50 projects — but it is NOT on the open path",
    "One shared vCPU / 512 MB with a 50 MB JSON body limit — the autosave POST that fires DURING open contends with every open-path request on the same event loop"
  ],
  "refutedTitles": [
    "GET /api/projects still SELECTs the base64 `thumbnail` column for 50 rows and then deletes it server-side",
    "When both Supabase and DATABASE_URL are configured, GET /api/projects runs BOTH list queries serially and merges them",
    "Two thumbnail captures per open produce two independent PATCH /thumbnail writes of the same project row",
    "NEGATIVE FINDING, recorded so it is not re-diagnosed: the indexes on the open path are present and correct"
  ],
  "refutations": [
    "REFUTED — 'GET /api/projects still SELECTs the base64 thumbnail column': the code shape is real (server.js:2941 selects `thumbnail`; projectThumbnail.js:66 `delete row.thumbnail`) but the CAUSAL claim 'This endpoint is controller.refresh()' is false, and that claim is the whole finding. Traced: buildPersistence.ts:266 `controller.refresh()` -> ProjectListController.ts:48 `this.client.list()` -> ProjectListClient.ts:249 `listPage()` -> ProjectListClient.ts:275 `/api/v1/projects`. The v1 route (server/api/v1/routes.js:508-521) has NO Supabase branch at all — it calls `pgProjectStore.listProjects` only, which at projectStore.js:484 projects `(p.thumbnail IS NOT NULL AND p.thumbnail <> '') AS has_thumbnail` and never selects the bytes. The L-10405 fix the auditor says 'landed only in the Postgres listProjects' IS the open path. v0 `/api/projects` has exactly two live client callers repo-wide — ProjectHub.ts:859 (the null-runtime fallback, and PlatformRouter.ts:617 passes `this.runtime`, so it is not taken; PlatformRouter.ts:598 documents that fallback as broken) and linkSourceGateway.ts:110. Open-path cost: 0 ms, not 150 ms.",
    "REFUTED — 'both Supabase and DATABASE_URL run BOTH list queries serially': same address error. The double-read at server.js:2955-2973 is real and I confirmed the LEFT JOIN LATERAL at projectStore.js:490-499, but it lives in the v0 handler, which the open path does not call (see above). It costs the open nothing. Also, the auditor's proposed check ('do not act until you have checked flyctl secrets list') is answerable from the repo without it: `GET /api/v1/projects` reaches `pgProjectStore.listProjects` -> `query()`, and pgClient.js:250-253 THROWS when no pool exists. A production hub that lists projects therefore proves DATABASE_URL (or SUPABASE_DB_URL) is set. The uncertainty the auditor flagged here is resolvable; they left it open on the finding where it did not matter and closed it silently on the one where it did (#11).",
    "REFUTED on cost — 'Two thumbnail captures per open produce two independent PATCH writes': the two call sites (PlatformSaveController.ts:397 and :567) and the absence of any dedup in thumbnailUpload.ts:63-85 are confirmed. But neither PATCH is on the open path and neither is awaited: :567 sits inside `runCapture`, which is invoked via `setTimeout` -> `requestIdleCallback(runCapture, { timeout: 4000 })` (PlatformSaveController.ts:571-575) — explicitly deferred until after the renderer settles — and :397 fires on autosave. `_uploadThumbnailToServer` (:608) is `void`-ed. So the claimed 60 ms of OPEN latency has no mechanism. Worse, the proposed fix contradicts the auditor's own would-break: a digest dedup cannot fire, because the two captures are taken at deliberately different render states and produce different bytes — that difference is the stated reason the post-load capture exists (:552 'capture returned null — renderer not ready'). Keep it as a server-write-hygiene note; it is not an open-path finding.",
    "REFUTED AS STATED — 'the indexes on the open path are present and correct'. Every index the auditor quotes is from server/dbMigrate.js, whose DDL runs ONLY inside `migrateViaPg(pool)`: dbMigrate.js:567-568 `const pool = getPgPool(); if (!pool) return;`. The Supabase database is built from a DIFFERENT artifact — server/schema.sql, applied BY HAND via the Dashboard SQL Editor, because (supabaseMigrate.js header) 'Supabase does NOT expose a DDL-over-REST endpoint in PostgREST'. schema.sql contains NO composite index: line 80 `idx_project_versions_project_id ON project_versions(project_id)` (no `created_at DESC`), line 51 `idx_projects_owner_id ON projects(owner_id)` (no `updated_at DESC`), line 99 `idx_project_members_project_id` — the PROJECT direction, not the `(user_id, project_id)` direction the access gate needs — and grep for `idx_project_versions_project_created` across the repo returns exactly two files, dbMigrate.js and ISSUE-LOG.md. Meanwhile EVERY open-path read the auditor enumerated takes the `if (supabase)` branch first (server.js:3518, 2939, projectAccess.js:119). So the repo holds two schema sources that disagree on the three indexes this finding certifies, and the audit read only one of them. LIKELY RESOLUTION, stated so the next lane is not sent at a phantom: DATABASE_URL probably points at the Supabase pooler — dbMigrate.js's own retry comment names 'the Supabase pooler connection can time out transiently at boot' — in which case runMigrations DOES create the composites on the same database PostgREST reads and the conclusion holds after all. But that inference was never made, and this is a finding whose entire purpose is to tell the next agent NOT to look. One `\\di project_versions` against the live DB settles it. MAGNITUDE CAVEAT, so nobody over-rotates: even if the composites are absent, `created_at` lives in the heap tuple and the multi-MB `snapshot` is TOASTed out of line, so a top-1 sort over ~746 narrow rows is single-digit milliseconds. The defect here is the false certification, not a hidden 300 ms.",
    "SURVIVES (strongest finding) — 'AWAITS a full-snapshot fetch whose result is thrown away': verified end to end. buildPersistence.ts:316 awaits `tier.streamLoad` unconditionally unless `isNewProject`; streamLoad (:155) is a real `GET /api/projects/:id/latest-version`; PlatformShell.ts:296-300 reads `versionRepository.getLatestVersion(id)` SYNCHRONOUSLY (the code's own comment at :340 calls it 'the synchronous read above') and returns on a hit; `opts.prefetchedVersion` is read only at :351 inside the else. A cheap `hasLocalVersion` predicate is therefore trivially available — fix (a) is mechanically easy. ⚠ FIX (b) AS WRITTEN IS BROKEN: 'pass the promise as prefetchedVersion' collides with two live reads. PlatformShell.ts:357 gates the IndexedDB warm on `(!prefetched)` and a Promise is truthy, so the warm-retry — the §VERSION-QUOTA-INDEXEDDB path that recovers >5 MB deep-link history — would be silently disabled; and :351/:356 destructure `prefetched.snapshot` directly. Un-awaiting requires restructuring both, not just dropping the `await`. Also note buildPersistence.ts:330-336 only builds `contextOpts` when a field is defined, so an always-present promise changes that branch too.",
    "SURVIVES with a correction that makes the fix SAFER than the auditor claims — 'two SERIAL Supabase round trips': confirmed at server.js:3520-3522 (ownership select) then :3525-3531 (version select). But the would-break is fabricated: there is no comment saying the pre-check exists to tell 'no rows' from a real DB error. The actual comment at server.js:3519 reads 'Single query: verify ownership and fetch latest version snapshot together' — i.e. the code CLAIMS to be what the auditor proposes it become. The two-step is an unfulfilled intent, not a protected invariant, so the collapse is cheaper to justify than stated. The parse->stringify->gzip tax and the measured scaling are sound.",
    "SURVIVES as duplicated server work, DOWNGRADE the cost — 'two independent socket.io connections': confirmed, initCollaboration.ts:580 and PlatformCollabPill.ts:184 each mint their own socket with no lookup for an existing one, and server.js:566-587 pays an uncached `canUserAccessProject` per `join-project`. But neither connect is awaited by the open — both are fired from `connect` handlers off the critical path — so the 80 ms is server work and connection-budget pressure, not open latency. The would-break (two independent disconnect lifecycles, `dispatcher.bindProject` re-binding) is accurate and is the real cost of the fix.",
    "SURVIVES — 'no cache on the per-request access check': confirmed, server.js:960-974 has no map and no TTL, projectAccess.js:119-125 is the first round trip and :141-146 the second for non-owners, and planStore.js's `_loadedSet` is a real in-repo precedent. The would-break is correct and is the load-bearing part: the invalidation hooks, and the rule that a `retryable` / `db_unavailable` verdict must never be cached (L-136), are what keep this from becoming a sticky-refusal defect.",
    "SURVIVES — 'the snapshot is one unpaginated blob': grep for `latest-version/header|project_versions_levels|/latest-version/` across server.js returns NO MATCHES, so the server half genuinely does not exist. Cost is declared -1/0, so this is a structural finding, not a cost claim, and the sequencing is right: split the journal off the wire first (it is the term that grows), chunk per level only after the C13/C20 amendments. The digest caveat (`detachJournalMutations`/`attachJournalMutations`, 'NOT COMPARABLE' rather than 'corrupt') is the correct invariant to protect.",
    "SURVIVES with two corrections — 'unbounded COUNT(*) runs TWICE per save': confirmed at projectStore.js:1257-1260 and :1280-1284. (1) The step-3 count is CONDITIONAL on `maxVersions !== -1`, so it is twice only on limited plans, once on unlimited. (2) The would-break names the WRONG hazard: `createVersion` (projectStore.js:1085) is cited as a rival insert path that would desync an arithmetic count, but a repo-wide grep finds ZERO callers — it is a dead export. The LIVE hazard is one line below the one the auditor quoted: step 4 is `INSERT ... ON CONFLICT (id) DO NOTHING` (:1272), so on an idempotent replay NO row is inserted, and `version_count + 1` would over-count. That desyncs the exact value the optimistic lock compares (`expectedVersionCount` vs `projects.version_count`, :1220-1226) — i.e. it manufactures more of the founder's own `412 expected 1, server has 746`. The retention half of the finding stands and is the part that matters.",
    "SURVIVES exactly as written — 'portfolio is a true serial N+1 but NOT on the open path': confirmed, routes.js:1252 opens the loop and :1255-1261 awaits a `select('id, label, snapshot, ...')` per project inside it, with the PG leg at :1266. Caller is PlatformProjectBrowser.ts:874 inside a user-opened modal. Reporting it at 0 ms with the caller traced is the correct handling.",
    "SURVIVES, and fix (1) is provably zero-risk — 'one shared vCPU with a 50 MB body limit': confirmed at server.js:381-383 (express.json limit 50mb), :3629 (the re-stringify), :3690 (the URL scan), projectStore.js:1268 (the third stringify). The size cap at :3630 is UNREACHABLE by construction: the raw body already passed express.json's 50 MB limit, and `snapshot` is a subset of that body whose re-serialisation cannot exceed it, so the branch can never fire. Deleting it removes a full traversal and changes no behaviour. Fix (2) is sound given the scanner is REPORT ONLY (:3684) with its flip condition written down."
  ],
  "missed": [
    "THE SCHEMA HAS TWO SOURCES THAT DISAGREE, AND ONLY ONE IS AUTOMATED. server/dbMigrate.js runs its DDL only against a PG pool (dbMigrate.js:567 `if (!pool) return;`); the Supabase database is built manually from server/schema.sql, which is missing `idx_project_versions_project_created`, `idx_projects_owner_updated` and `idx_project_members_user_project` entirely. Nothing in the repo detects the divergence — there is no gate comparing the two artifacts, which is the same shape as the contract-index-equivalence problem CLAUDE.md already documents. Independent of the index question, this means any index or column added to dbMigrate.js silently never reaches the Supabase DB (schema.sql already needed a hand-applied `ALTER TABLE projects ADD COLUMN thumbnail`, per supabaseMigrate.js:44-58). That is the real durable finding on this dimension.",
    "controller.refresh() IS A DUPLICATE FULL LIST QUERY ON THE OPEN PATH — the auditor found a list-query cost but attributed it to the wrong endpoint and so missed the live one. ProjectHub enumerates at mount via `client.listAll()` (ProjectHub.ts:838, paging `/api/v1/projects` 50 rows at a time up to 40 pages), but it writes into its own repository, not `projectListStore`. So at buildPersistence.ts:265-267 `projectListStore.isEmpty()` is still true and step 1 re-runs `GET /api/v1/projects` — with the `LEFT JOIN LATERAL` over `project_versions` once per returned row (projectStore.js:490-499) — serially, BEFORE `tier.streamLoad` is allowed to start. The repo names both legs itself: ProjectHub.ts:774 'controller.refresh() and tier.streamLoad() — the two requests the OPEN path is waiting on'. Fix: seed `projectListStore` from the hub's enumeration so the open's `isEmpty()` check is false, removing a whole serial round trip that finding #1's fix would otherwise leave in place.",
    "THE PG BRANCH OF /latest-version IS FOUR SERIAL QUERIES, NOT TWO, AND IGNORES A NARROW HELPER THAT ALREADY EXISTS. server.js:3538 calls `pgProjectStore.listVersions(id, userId)` — which is itself an ownership SELECT plus a `LIMIT 20` list (projectStore.js:933-946) — and then server.js:3556 calls `getVersionById`, which repeats the ownership SELECT and then fetches the row (projectStore.js:985-996). Four round trips to answer one question, when `getLatestVersionSnapshot()` at projectStore.js:1007-1017 already does `WHERE project_id = $1 ORDER BY created_at DESC LIMIT 1` in one. Latent while Supabase wins the branch, but this is the failover path — it is what runs during exactly the Supabase outage that makes latency worst.",
    "THE SUPABASE BRANCH OF /latest-version IS OWNER-ONLY WHILE THE PG BRANCH IS MEMBER-OR-OWNER. server.js:3521 scopes the ownership pre-check with `.eq('owner_id', userId)`, whereas the PG leg uses `memberOrOwner` (projectStore.js:989, and listProjects at :500). A collaborator opening a SHARED project therefore gets a 404 from the endpoint that is supposed to be the durable authority; streamLoad swallows it (buildPersistence.ts:158-161 returns null on !res.ok) and PlatformShell silently serves the local copy. That is 'same rule, two implementations' on the open path, and it interacts directly with finding #1: making the server fetch conditional on local history would turn a currently-recoverable staleness into a permanent one for every shared project.",
    "THE OPEN PATH COMPETES WITH THE HUB'S OWN DEFERRED DRAINS FOR THE BROWSER'S ~6 CONNECTIONS PER HOST — already mitigated, but the mitigation is a pause, not a stop, and it belongs on the timeline. ProjectHub.ts:768-782 and :141-149 describe the thumbnail back-fill PATCHes and the per-card preview GETs being serialised and 'paused the instant an open begins'. That guard is real and should NOT be removed (it protects §FIX-THUMBNAIL-DURABILITY), but it means the number of server requests in flight during an open is a function of hub state, and no `markStartupPhase` mark covers it — so it is invisible on the same timeline as everything else, exactly the gap finding #1 correctly asks to close for streamLoad."
  ]
}