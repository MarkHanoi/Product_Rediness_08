{
  "refuted": false,
  "reason": "NOT REFUTED. The load-bearing half of the claim is confirmed line-by-line in the code; only the mechanism sub-hypothesis is unproven, and the claim itself labels it as such and ships the falsifying probe.\n\nCONFIRMED (a) — WHAT IT TIMES. packages/renderer-three/src/pipeline/RenderPipelineManager.ts:2034 starts `__t_shadow_start`, :2037 logs SHADOW_REBUILD_COMPLETE, and the chain it wraps is exactly `_rebuildPipeline()` (:4911, cited line is exact) -> `_rebuildPipelineWithCurrentState()` (:4893) -> `_buildPipeline()` (:3753). The phase-3 branch is unreachable: SceneQualityTierManager.ts:199/209/220/232 all read `ssgi: false`. `_buildPipeline`'s entire body after the import is synchronous and O(1): createScenePass (packages/renderer-three/src/pipeline/ScenePass.ts — one `pass(scene, camera)`, plus 4 MRT lines only when `needsGBuffer`, which is false here), createZonePass (ZonePass.ts:59-78 — one `pass()` + a Layers mask), ~10 TSL node ops, `new RenderPipeline(renderer)`, `_safeDisposeRenderPipeline()` (:4780) and `_emitState()` (:5451 — 3 bus emits + onStateChange). There is NO traverse, NO per-mesh work, NO shadow-map render, NO light/camera fit anywhere in the window. The scene-proportional traverse in `scheduleShadowRebuild` is the meshCount log line, and it runs BEFORE the debounce timer, i.e. outside the timed window.\n\nCONFIRMED, AND IT REFUTES A COMMENT IN THIS VERY FILE. `_safeDisposeRenderPipeline`'s own doc says \"RenderPipeline.dispose() fans out to NodeManager.delete(renderObject) for every render object the pipeline referenced\". The INSTALLED source says otherwise: node_modules/.pnpm/three@0.183.2/node_modules/three/src/renderers/common/RenderPipeline.js:151-153 is `dispose() { this._quadMesh.material.dispose(); }` — the only `dispose` token in the file. So dispose cannot be O(3762), and the one comment that would make a reader believe the 16 s is scene-proportional is wrong. That is a real doc defect worth its own log row.\n\nCONFIRMED — THE ONLY SUSPENSION POINT. I read the whole chain. `_rebuildPipeline` (:4911) is sync until it returns `_rebuildPipelineWithCurrentState().catch().finally()`; `_rebuildPipelineWithCurrentState` awaits `_buildPipeline`; `_buildPipeline` awaits exactly one thing: `await import('three/webgpu')` at :3763. Every subsequent hop (.catch/.finally/.then) is a microtask. So by elimination elapsed = (wait at :3763) + (synchronous O(1) work). Either the import waits ~16 s or a synchronous block burns ~16 s — there is no third shape. The module IS pre-loaded: WebGPURendererAdapter.ts:139 `const { WebGPURenderer } = await import('three/webgpu');` builds the live renderer (run reports backend=real-WebGPU), and vite.config.ts:569 `if (id.includes('node_modules/three/')) return 'vendor-three';` puts three/webgpu and three/tsl in ONE chunk.\n\nCONFIRMED — THE ACTUAL HARM, which is the part that does not depend on the mechanism at all. `_beginShadowRebuildGuard()` is called at :1991 (inside the debounce callback, same task as `__t_shadow_start`) and AGAIN at :4936 inside `_rebuildPipeline` — `_submitPauseDepth` is 2 for the window. Releases are at :4964 (`_rebuildPipeline`'s `.finally`) and :2057 (`_finishRebuildAndDrainQueue`), both strictly after the import resolves. So `_shadowRebuildPaused` is true for the entire 16,011 ms, `render()` returns at :1257 `return this._skipFrame('shadowRebuildPaused')`, and the corrected/measured comment at :1268-1288 is verbatim as quoted — `setObcRenderCallback` has no caller, there is no second rAF, \"Every `return` above is therefore a TOTAL VIEWPORT FREEZE for its duration.\" The freeze is real regardless of where the 16 s actually goes.\n\nCONFIRMED (b). PascalSceneLighting.ts:292-297 is the only `castShadow = true` (1024^2 via cfg.shadowMapSize); fill (:324), rim (:331) and ambient (:338) set none; `_forEachShadowCastingLight` (:2379-2388) only visits `isDirectionalLight && castShadow`; no CSM in the tree. And the guard freezes the pass — `_applyShadowFreezeState` (:2251) writes `shadowMap.autoUpdate = !shouldFreeze` plus the per-light flags — so ZERO shadow-map renders occur inside the 16 s. The 87 fixtures are irrelevant. shadowCasterPolicy.ts:44's \"fixture point lights already never cast\" is a doc line, not code, but the substance is carried by the traverse predicate.\n\nCONFIRMED (c). apps/editor/src/ui/platform/PlatformShell.ts:203-205 is `getFrameScheduler().scheduleOnce('perf104-first-interactive-frame', ...)` fired from the `pryzm-project-loaded` handler right after `markStartupPhase('open:snapshot-loaded')`; FrameScheduler.ts:441-442 `addTickListener` -> `wakeIfStopped()`. So +15,628 ms is post-load main-thread blockage, not shadow cost.\n\nCONFIRMED (d). ShadowQualityUpgrader.ts QUALITY_CONFIGS is 512/2048/4096 + radius/bias — a level setter, not a deferral. RenderingPipelineCoordinator.ts:567 `_LARGE_SCENE_SHADOWS_OFF_MESH_COUNT = 8000` does not trip at 3,696. MAX_CASTER_RELEASE_PAUSED_FRAMES = 8 at :187 with the L-663 rationale at :183-188; this guard has no cap. initScene.ts:3606-3608 is `_freezeShadowForLoad(); renderPipelineManager.onProjectSwitch();` and the §LOAD-SHADOW-PASS-DEFERRED machinery at :4079-4144 exists as described. :3229-3230 and the NOTE at :3252-3256 are both live in the same method — though note the NOTE is STALE-AND-UNAMENDED rather than unexplained: L-316 left a 20-line justification immediately ABOVE :3230. The claim states this accurately.\n\nCONFIRMED (e). FurnitureFragmentBuilder.ts:280 sets castShadow at build time; furnitureShadowBudget.ts holds `let _budget = 'full'` at module scope with `setFurnitureShadowBudget` (:98) only writing that variable; initScene.ts:2653-2655 is the tier hook; and shadowCasterVerdict (shadowCasterPolicy.ts:126-155) returns 'builder' for furniture/lighting, so the PascalSceneLighting sweep deliberately does not overwrite it. There is no retro-apply anywhere in the tree.\n\nWHY I DID NOT REFUTE ON THE MECHANISM. The specific story — \"the continuation task was starved for 16 s by the concurrent hydrate\" — is the one part I could not confirm, and I found evidence against it that the claim does not address: yieldForProgress (packages/frame-scheduler/src/progressScheduler.ts:176-190) resolves on the FRAME BUS when the tab is visible, so the 17.16 s chunked hydrate returns to the event loop once per chunk. A module-map-hit continuation should be serviced within a turn or two, not starved for 16 s. But this does not refute the ROOT CAUSE as stated — \"SHADOW_REBUILD_COMPLETE contains zero shadow work and zero scene-proportional work; the 16,011 ms is WAIT, not compute; it is a submit-pause held open across the whole hydrate\" — because I verified there is nothing in the window that can burn 16 s of CPU, and the guard is provably held for the entire window either way. The claim names its own alternative (`_safeDisposeRenderPipeline` / `_emitState` fan-out) and ships a one-line probe that separates them. That is the correct posture and I would run it before touching :3763.\n\nTHREE DEFECTS IN THE PROPOSAL ITSELF (all verified, none fatal to the diagnosis):\n1. FIX 3's body sentence is FALSE as written. `_freezeShadowForLoad()` calls `setShadowReallocFrozen(true)`, and that method (:2163-2173) only increments `_shadowReallocFreezeDepth` and runs `_applyShadowFreezeState()`. It NEVER sets `_shadowRebuildPaused` — it does not pause submits. L-316's own justification at :3216-3229 names the submit pause as the load-bearing half (\"While submits are paused the outgoing project's shadow texture can be disposed with NO submit referencing it\"). So the freeze half is covered independently and the SUBMIT-PAUSE half is not. The WOULD BREAK section hedges correctly; the fix's prose overstates. Do not ship FIX 3 on that sentence.\n2. FIX 1 has an implementation trap. `_loadTSL()` (:3732) begins `if ((globalThis as any).__PRYZM_TSL__) return;` — it early-returns on a GLOBAL. A `RenderPipeline` cached into a per-INSTANCE private field inside that method would be undefined on any second manager instance, and this very run creates one (§RENDERER-LIVE-SWAP WebGPU->WebGL). `new undefined(...)` throws inside `_buildPipeline` -> `_rebuildPipeline`'s catch -> `_failWithCause` -> crash overlay. Park it on globalThis next to `__PRYZM_TSL__`, or assign above the early return.\n3. FIX 2 must decrement the right amount. `_submitPauseDepth` is 2 inside this window (:1991 and :4936). A frame-boundary cap modelled on the sibling (which opens one) would decrement once and leave depth 1 latched — a permanent freeze, the L-900 shape the file already names at :1081.\n\nNet: the diagnosis holds, the harm is real and mechanism-independent, the caveat about not claiming 16 s off the 37 s open is correct, and the fix set is sound after the three corrections above.",
  "betterHypothesis": "Same diagnosis, different ORDER OF CONFIDENCE — and the fixes should be re-ranked to match. The CONFIRMED root cause is the UNBOUNDED submit-pause guard, not the await: `scheduleShadowRebuild` opens `_beginShadowRebuildGuard()` at RenderPipelineManager.ts:1991 (nested again at :4936, depth 2) and releases only at :4964/:2057, so `render()` skips at :1257 for the whole window and, per the measured comment at :1268-1288, nothing repaints. That is true no matter where the 16 s goes. The await at :3763 being the sink is a SUB-HYPOTHESIS, and it has a counter-argument the write-up does not answer: yieldForProgress (progressScheduler.ts:176-190) yields on the FRAME BUS when visible, so the hydrate returns to the event loop once per chunk and a module-map-hit continuation should not starve 16 s.\n\nSo: (1) run the :3763 probe, and add a second `performance.now()` around the post-import synchronous tail (`_safeDisposeRenderPipeline` + `new RenderPipeline` + `_emitState`) in the same patch, so ONE open partitions the 16 s between wait and compute instead of only testing one half; (2) ship the guard cap FIRST — it is mechanism-independent, it removes the freeze whatever the probe says, and it is the structural lesson the sibling at :187 already learned — remembering that the cap must drain `_submitPauseDepth` from 2, not 1; (3) hoist the import only if the probe says ~16,000 ms, and park the resolved `RenderPipeline` on globalThis beside `__PRYZM_TSL__`, because `_loadTSL()` early-returns on that global and a per-instance field would be undefined on the post-swap manager this run actually creates; (4) treat FIX 3 as blocked, not merely \"verify first\" — `setShadowReallocFrozen` (:2163) demonstrably does not pause submits, so `_freezeShadowForLoad()` covers only the freeze half of L-316 and the trigger cannot be deleted until the submit-pause half has a replacement owner.\n\nAlso worth a separate ISSUE-LOG row independent of the perf work: the doc comment in `_safeDisposeRenderPipeline` asserting that `RenderPipeline.dispose()` fans out per render object is contradicted by the installed three@0.183.2 (RenderPipeline.js:151-153 is one line) — that comment is exactly what would send the next reader hunting for a scene-proportional cost that does not exist."
}