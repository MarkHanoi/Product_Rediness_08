{
  "survivingTitles": [
    "`room` is a first-class category in the DEPRECATED vgGovernanceStore, and absent from the VisibilityIntent list the founder's panel actually renders",
    "THE STYLING SEAM IS ONE FUNCTION — `resolveRoomColourIntent()` — with exactly three live callers",
    "`OverridePanel.CATEGORIES` is a FOURTH category registry with no room row — the one this lane is most likely to miss",
    "`PlanViewFillRenderer` is a DEAD duplicate room painter with zero importers",
    "'Colour by room type' cannot actually change any colour — `OCCUPANCY_PALETTE` is a hardcoded table with no write path",
    "Room fills are gated to `isPlanLike` — elevation and section render no room region at all",
    "`ELEMENT_TYPE_REGISTRY` is a rival to `VisibilityIntentDefaults.ELEMENT_TYPES` and its header states a rule that is false",
    "No gate covers the visibility-intent category list, which is why this shortfall was silent",
    "REGISTRY MAP — every list that would need a `room` row, and its current state"
  ],
  "refutedTitles": [],
  "refutations": [
    "SUB-CLAIM REFUTED inside finding 9 (the finding itself survives as an inventory): its ruling \"DELIBERATELY EXCLUDED, LEAVE ALONE: (7) CATEGORY_TO_DXF_LAYER … (8) PocheFillTable … DO NOT touch (7), (8)\" is right about POCHE and wrong about LINEWORK. Rooms DO produce projected linework today — see missed item A. The title's universal quantifier (\"every list\") is therefore false: it omits ELEMENT_TYPE_TO_PROJECTION_LAYER (EdgeProjectorService.ts:203) and ISO_LAYER_TO_VG_CATEGORY (DrawingLayerIdentity.ts:96), the two rows the sibling lighting lane added one commit ago for exactly this reason.",
    "CORRECTION to finding 1 (does not change the verdict): the ELEMENT_TYPES enumeration is already stale. Commit 584aaab2 (today, §LIGHTING-IS-A-CLASSIFIED-LAYER L-13267) added 'lighting' at VisibilityIntentDefaults.ts:33, so the list is now 19 types + '__default__' = 20 panel rows, not the \"18 + default\" / \"19 category rows\" the finding states. `room` is still absent — the substance holds exactly.",
    "CORRECTION to finding 5: \"there is no swatch anywhere the user can click\" is too strong. RoomPropertySection.ts:563 has `picker.type = 'color'` — a real per-ROOM colour picker feeding `custom` mode, which the finding itself acknowledges elsewhere. The precise claim survives intact: there is no per-occupancy-TYPE write path. All OCCUPANCY_PALETTE references are reads, `resolveForMode` case 'occupancy' reads the module literal directly at RoomColourSystem.ts:229, and even `RoomSystemType.colour` (seeded from the palette at RoomSystemTypeStore.ts:57) is never consulted by that branch — so a custom room type carrying a colour still cannot recolour its occupancy.",
    "CORRECTION to finding 8: PLAN_CASES is 15 cases over 11 distinct categories (wall, slab, column, beam, door, window, roof, stair, furniture, ceiling, plumbing), not \"12 distinct categories\". Everything else verified verbatim: `fill` is in the noop list at viewIntentGovernsEveryCategory.test.ts:145 and only `ctx.stroke` records (:153), so a fill-only family is structurally invisible to the gate.",
    "CORRECTION to finding 7: the registry keys are at lines 36/44/52/60/68/76/84/94/109/124/141/149/159/167/177/185/195/203/211 — the finding's line numbers are ~5 low, though the SET matches exactly (19 keys + `__default__`). The divergence the finding names has WIDENED since it was written: ELEMENT_TYPES gained `lighting` today and ELEMENT_TYPE_REGISTRY did not, which is a second, same-day proof that the \"one entry here only\" header at :14 is false."
  ],
  "missed": [
    "⭐ THE BIG ONE — ROOMS HAVE NO ISO PROJECTION LAYER, SO ROOM LINEWORK IS UNGOVERNABLE AND DRAWN AT THE HEAVIEST FALLBACK PEN. `RoomBoundaryBuilder.ts:334-348` builds a real, scene-added, VISIBLE floor-overlay mesh stamped `userData.elementType = 'room'` (`:342`; the room volume at `:407` too). `ELEMENT_TYPE_TO_PROJECTION_LAYER` (apps/editor/src/engine/views/EdgeProjectorService.ts:203-269) has NO `room` / `Room` / `RoomVolume` row, so that geometry falls to `FALLBACK_NATIVE_LAYER = 'projection-visible'` (:271), and `ISO_LAYER_TO_VG_CATEGORY` (packages/core-app-model/src/drawing/DrawingLayerIdentity.ts:96-135) has no room layer to map. That is verbatim the three-part defect the sibling lane wrote down for lighting ONE COMMIT AGO at EdgeProjectorService.ts:241-247: *\"penCategoryForLayerTag could not classify it and returned 'projection', so it drew at the __default__ pen — the WALL weight … ISO_LAYER_TO_VG_CATEGORY had nothing to map, so V/G could not hide it … the visibility-intent list had no lighting row.\"* And the pipeline already anticipates the family: `'room'` is in `CACHEABLE_ELEMENT_TYPES` at EdgeProjectorService.ts:2031. The auditor's finding 9 instructs the lane to LEAVE THIS ALONE on the reasoning \"rooms are not sliced solids\" — true of poché, false of linework — which would ship the colour half of the founder's request and leave the *\"THE LINES ARE ALMOST NOT READABLE\"* half untouched for rooms. ⚠ `A-AREA` is already claimed by `spaceEnvelope` (DrawingLayerIdentity.ts), so a room layer needs a name of its own.",
    "`SetRoomColourModeCommand` IS NOT ON ANY UNDO STACK, and finding 1's \"already exists — with correct undo … do not rebuild any of it\" carries that forward silently. docs/04-reference/ISSUE-LOG.md:24777-24779, from the very lane the auditor cites: *\"`SetRoomColourModeCommand` implements `undo()` correctly but **nothing puts it on a stack** — `SetVGCategoryStyleCommand` has **zero non-test call sites**, so *no* VG category change in this repo is undoable today.\"* Implementing undo and being undoable are different facts; re-pointing the seam at `visibilityIntentStore` is the moment to decide which one this lane wants.",
    "THE `room` PEN DOES NOT EXIST, AND `FALLBACK_PEN` IS NOT THE SAFE ANSWER THE AUDIT CALLS IT. Findings 1 and 9 both bless `resolvePen(zone,'room') → FALLBACK_PEN` as low-risk. `FALLBACK_PEN = pen(0.18, '#000000')` (PenWeightTable.ts:265), fully opaque, versus the PROJECTION pen every other family carries — `pen(0.09, '#6b7280', null, 0.55)` (:205-215, including `lighting: pen(0.09, …)` added today at :153/:213/:232). The file's own header says it: *\"`FALLBACK_PEN` 0.18 mm is heavier than several categories' real PROJECTION pens\"* (:292). So room linework would draw twice as heavy and twice as dark as walls-in-projection. \"Safe\" is true only for as long as rooms stay unclassified — the instant missed item A is fixed, the fallback becomes the readability defect. `SYSTEM_PEN_TABLE` needs a `room` row in the same commit.",
    "THE ONE-LINE FIX DOES REACH THE FOUNDER'S EXISTING PROJECT, AND THE AUDIT NEVER ESTABLISHED IT. `VisibilityIntentStore.serialize()` persists only `_userIntents` (:105-110) and `deserialize()` rejects any record with `isSystem` (:118-119); system intents are rebuilt every load from `cloneDefaultElementGraphicsRules()` (SystemIntents.ts:42 → VisibilityIntentDefaults.ts:135). The founder's panel reads \"Architectural Documentation (Auto)\" — a SYSTEM intent — so adding `'room'` to `ELEMENT_TYPES` makes the row appear on his current file with no migration. This is the fact that separates \"the row appears\" from \"the row appears only in new projects\", and it is the [[committed-is-not-reachable]] shape this repo keeps re-running. (User-authored intents take the `migrateIntentToCurrent` path at :119 — check that it backfills new element types before assuming parity.)",
    "MINOR, so it is not mis-read later: the log line \"14 VG CATEGORIES (max 56)\" is `categoryCount` at VGSceneApplicator.ts:716 printed as `max ${categoryCount * 4} layers` at :721. The 56 is DERIVED (14 × 4 sub-layers), not an independent ceiling — nothing is \"42 short of a cap\"."
  ]
}