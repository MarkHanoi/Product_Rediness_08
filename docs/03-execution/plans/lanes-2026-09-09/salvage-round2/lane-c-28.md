{
  "survivingTitles": [
    "Every open awaits a full server snapshot fetch whose result is discarded when local history exists",
    "Opening an unedited project writes a brand-new saved version: full serialize + deflate + IndexedDB write + server queue",
    "The whole model is serialized a SECOND time on the open path just to produce a dirty-check baseline",
    "The project thumbnail is captured twice per open, and one of the two is on the critical path",
    "Two socket.io clients connect and both emit join-project on every open — one is tagged legacy",
    "ProjectIsolationAudit flattens the entire THREE scene on every project load — and the comment that says otherwise is stale",
    "DEAD: a second complete ProjectSerializer + ProjectLoader + MigrationEngine (~2,970 lines) that no production file imports",
    "DEAD BY DEFAULT: a 945-line legacy per-element load branch preserved inside the open-path loader",
    "Gate-debt ledger: the two ledgered gates both name legacy channels that run on the open path, and one is now WORSE THAN DECLARED"
  ],
  "refutedTitles": [
    "lineworkProbe does a second full-scene traverse on every 3-D activation, installed unconditionally in production"
  ],
  "refutations": [
    "REFUTED (#7 lineworkProbe) — it does not run on the open path at all, and probably not on this founder's session. The subscription is guarded: `lineworkProbe.ts:414-415` reads `const mode = (payload as {mode?:string})?.mode; if (mode !== '3D') return;` BEFORE the `setTimeout`, so a non-3D activation costs one property read. The ONLY emitter of `view-activated` in the entire tree is `ViewController.ts:1545` (inside `_activateView`, `source:'view-switch'`), and I found no open-path caller that activates 3D: `initUI.ts:2681` is `await viewController.activate('Top')` and sits inside `generatePlans()` (a user action, not boot), and `GISAreaLayout.ts:2109` is inside `applyBimDualPane` (a gesture). The founder's own paste corroborates the refutation: `runLineworkProbe` prints `[linework-probe] §LINEWORK-3D-PROBE (…, 3D entry #N)` UNCONDITIONALLY whenever it runs (lineworkProbe.ts:338-342), and there is no such line — while the diagnostics of a NON-3D activation (EdgeProjectorService native projection over 63 ISO layers, HiddenLineRemoval v3, VGSceneApplicator styledLayers=22) ARE present. So the activated view was a projection view and the probe returned at the guard. Even in the case where an open does land in 3D, this is a per-view-switch cost, which the lens excludes. The auditor's supporting sentence — 'Entering the canvas at open fires exactly that event' — is asserted, never checked, and it is the load-bearing claim."
  ],
  "missed": [
    "MISSED — the dead persistence-client cluster is FOUR files, not three, and the fourth has already DRIFTED. `packages/persistence-client/src/loader/SnapshotStreaming.ts` (398 lines) is not exported by `loader/index.ts`, not exported by the package barrel `src/index.ts`, and the ONLY `SnapshotStreaming` import anywhere in the tree is the editor's own copy (`apps/editor/__tests__/siteCaptureProvenance.test.ts:29` → `../src/engine/persistence/SnapshotStreaming`). It imports `./ProjectSerializer` (:41-42), so it is part of what keeps the dead serializer's import graph alive inside the package. It is a WORSE instance than the MigrationEngine case finding 8 names: `diff <(sed 's/[[:space:]]//g' packages/persistence-client/src/loader/SnapshotStreaming.ts) <(sed 's/[[:space:]]//g' apps/editor/src/engine/persistence/SnapshotStreaming.ts)` is NON-EMPTY (398 vs 425 lines) — the two copies of `splitSnapshotByLevel`/`mergeSnapshotFromHeaderAndLevels` have already diverged, so `[[same-rule-two-implementations]]` is not a future risk here, it is already true. Finding 8's fix should read four files / ~3,368 lines and delete this one in the same commit.",
    "MISSED — the server built a conditional-request affordance for the very fetch finding 1 indicts, and the client half was never written. `server.js:3533` sets `res.set('ETag', `\"${data.id}\"`)` on `GET /api/projects/:id/latest-version`, tagged `GAP-13: ETag on latest-version (version ID is the most precise cache key)`. `buildPersistence.streamLoad` (`packages/runtime-composer/src/buildPersistence.ts:154-157`) issues that fetch with `headers: { Accept: 'application/json' }, credentials: 'same-origin'` — no `If-None-Match`, and it never reads the ETag off the response. So the one mechanism that would have made the discarded round-trip cheap is unretired plumbing on the server with no consumer on the client. This belongs in finding 1's fix: if the fetch is kept as a freshness check rather than skipped, a conditional request that 304s is the version that costs almost nothing, and the server already emits the validator for it.",
    "MISSED — orphaned DOM listeners for a `project-loaded` event that NOTHING in the repo dispatches: the L-224 defect shape, fixed once in the isolation audit and never swept for. `grep` for `new CustomEvent('project-loaded'` / `new Event('project-loaded'` / `emit('project-loaded'` across the tree returns ZERO matches. Yet `apps/editor/src/engine/initScene.ts` registers three window listeners for it: `:275` (the loop that refreshes the level-scoped-3D-culling selector), `:690` (bulk level registration — `levelClipPlaneCache.clear()` plus a per-level walk, whose own comment claims \"'project-loaded' is the canonical event (same as ViewVisibilityMap / TopologyLayer)\"), and `:784` (`_updateGridElevation`). The same `:275` loop also listens on `window` for `pryzm-project-loaded`, which is likewise never dispatched there — the only `window.dispatchEvent(new CustomEvent('pryzm-project-loaded'…))` in the tree is `ProjectIsolationAudit.plantedLeak.test.ts:199`; production emits it only on `runtime.events` (PlatformVersionController.ts:419). This costs zero open time — the point is the inverse: it is silent NON-execution on every open (stale clip-plane cache, grid elevation never re-derived), and it is exactly the residue of a completed migration that this dimension exists to find. It is also the reason a reader auditing the open path sees three `project-loaded` handlers and assumes they are open-path work.",
    "MISSED (and it is the traverse #7 should have found) — the FIRST view activation of a session pays THREE whole-scene traverses, for ANY view mode, unlike the 3D-only linework probe. `apps/editor/src/engine/initScene.ts:943-951` says so in its own words: \"THE THREE `view-activated` VISIBILITY GATES BELOW EACH WALK THE WHOLE SCENE\" (datum lines, floor hatch, room-fill overlay; each a `scene.traverse` behind `bumpPerf(PERF_KEYS.TRAVERSE_VIEW_GATES)`, e.g. `_applyRoomOverlayVisibilityForView` at :1007-1015). §VIEW-GATE-NO-OP (L-1398) added a same-mode skip, but that same comment states the exemption that matters here: \"the first activation always runs, because `_lastX` is seeded with a guess\" — `_floorHatchGateApplied` / `_roomOverlayGateApplied` all start `false`. So whatever view the founder's open lands on, three full walks of 3,762 meshes are paid, versus the zero the linework probe actually charged. Caveat, and it is why I list this as MISSED rather than as a replacement finding: these are LIVE correctness gates, not dead code, so the action is `markStartupPhase` around them and a measurement — not deletion.",
    "MISSED, on finding 2 — the auditor's stated trigger is wrong even though its conclusion is right, and the wrong half will send the fix at the wrong line. The claim \"`lastHash` starts `''`, so the first save of a session is unconditional\" does not hold on this path: `PlatformVersionController.loadVersion` calls `this.saveCtrl.orchestrator.resetDirtyAfterLoad()` at :412 (immediately after `setLoading(false)` at :411), which seeds `lastHash` via `markClean(hash)` well BEFORE the deferred sweep closes suppression and `handleLoadSuppressEnd` re-arms the debounce. The save fires because the post-load sweep MUTATES the model so the second hash differs — which is the auditor's other stated mechanism and the correct one. Consequence for the fix: half (1) of the proposed fix (\"seed `lastHash` from the snapshot the loader consumed\") does NOT remove the write on its own, contrary to the auditor's \"Either half alone removes the write\" — `lastHash` is already seeded and the write still happens. Only half (2) (tag the sweep's mutations so they do not set `hasDirtyChanges`) removes it.",
    "MISSED, on findings 2 and 4 — a timing correction that changes what the fix buys. `SaveOrchestrator.setLoading(false)` opens a 4 s settle window (`this._settleUntil = Date.now() + 4000`, :473) and `handleLoadSuppressEnd` arms a 2500 ms debounce (`DEBOUNCE_MS = options.debounceMs ?? 2500`, :253); `executeSave` then defers again to the settle deadline (:372-379). So the version write of #2 and the inline thumbnail of #4 land roughly 2.5-4 s AFTER the load settles, not before first paint. Neither delays the scene appearing; both land in the window where the founder is trying to first interact, and both compete with the EdgeProjector chunking his own log shows (`work=109ms yieldCalendar~83ms`). Sell these as time-to-interactive, not time-to-first-pixel — an audit that books them against first paint will be contradicted by the first `markStartupPhase` trace and lose the whole set. By contrast #3's serialize is genuinely in front of everything: `resetDirtyAfterLoad()` at PlatformVersionController.ts:412 runs synchronously BEFORE `pryzm-project-loaded` is emitted at :419, so it delays the isolation audit, the thumbnail arm and the collaboration join that hang off that event.",
    "MISSED, on finding 3 — the founder's log ARGUES AGAINST the second serialize, and the auditor did not notice. `[ProjectSerializer] Snapshot created:` is printed unconditionally on every `serialize()` (`apps/editor/src/engine/persistence/ProjectSerializer.ts:2081-2087`, no throttle, no flag). Two full serializes on one open should therefore print that line TWICE — and the paste shows it ONCE, in a paste where the auditor was attentive enough to flag `[captureThumbnail]` and the two `join-project` lines as duplicates. Either the paste is partial or one of the two serializes did not run. The code path is real (I confirmed `resetDirtyAfterLoad` → `getHash` → `ctx.saveAdapter.serialize()` + `stringify()`), so the finding survives — but 450 ms is not collectable until a mark brackets it, and this is precisely the class of thing that would make a founder say the lane fixed something he never had.",
    "MISSED, on finding 4 — the cost is attributed to the wrong call. `ctx.getImageData(sampleX, sampleY, sampleW, sampleH)` (initPersistence.ts:220) samples at most 32x32 pixels (`Math.min(32, dstW/dstH)`, :215-218) — it is not the stall. The two real costs are (a) the FORCED SYNCHRONOUS full-scene render immediately above it: `const rpm = window.renderPipelineManager; if (rpm) rpm.render();` (:172-176), which re-renders all 3,762 meshes outside the frame loop purely so the drawing buffer is populated, and (b) the `THUMBNAIL_ENCODE_LADDER` of up to six fresh `toDataURL` encodes. Both captures pay both. A lane told to chase `getImageData` will measure ~0 and conclude the finding was false.",
    "MISSED, on finding 5 — the 50 ms is not serial open latency and should be struck from the total. Nothing on the client awaits either socket connect: `initSocketCollaboration(this.ctx, id)` is a bare call at PlatformShell.ts:250 and initCollaboration's socket connects on its own. The duplication is real and confirmed (PlatformCollabPill.ts:191 and initCollaboration.ts:593 both `emit('join-project', projectId)`; server.js:577-587 pays `await getSupabaseClient()` + `await canUserAccessProject(...)` per join, so the DB access check runs twice per open) — but the cost lands as two extra WebSocket handshakes contending for the connection budget during the open burst and as doubled server-side DB work, not as milliseconds on the founder's critical path. Keep the retirement, which the file's own `TODO(C.3.x): legacy io` asks for; do not book a number against it."
  ]
}