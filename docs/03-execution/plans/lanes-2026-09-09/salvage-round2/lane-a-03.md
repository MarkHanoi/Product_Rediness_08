{
  "dimension": "Drawn output quality — line weight hierarchy, colour/contrast, and scale-awareness in plan/section/elevation (\"THE QUALITY ATM IS REALLY BAD — THE LINES ARE ALMOST NOT READABLE\")",
  "summary": "The plan is painted by `PlanViewCanvas.render()` (Canvas2D, not THREE line materials). A real 8-rung mm pen ladder EXISTS and is correctly resolved — the defect is the mm→pixel CONVERSION and the raster floor, not the table.\n\n(a) WEIGHTS/HIERARCHY — YES, a hierarchy exists and it reaches the stroke. `PenWeightTable.SYSTEM_PEN_TABLE` (packages/core-app-model/src/drawing/PenWeightTable.ts:124-236) is the source: CUT wall 0.50 / column 0.70 / door 0.35 mm; PROJECTION wall 0.25 / door 0.18 / ceiling·furniture·lighting 0.13 mm; BEYOND 0.09 mm @ 55% alpha; HIDDEN 0.09 dashed. `GraphicsRulesEngine.resolveStyle()` layers the bound intent on top, but \"Architectural Documentation (Auto)\" seeds its weights FROM that same table (VisibilityIntentDefaults.ts:107 `weight: pen.widthMm`) and contributes NO plan-view weight modifier, so the founder's view resolves to the raw table. With NO OVERRIDES the VG factor is 1 (VgCanvasStyleResolver returns `lineWeight: null` unless a human overrode it). So the ladder is intact in millimetres.\n\n(b) SCALE-AWARE? NO — and this is the primary readability defect. Linework converts at a CONSTANT `SCREEN_PX_PER_MM = 96/25.4 = 3.7795` px/mm (PlanViewCanvas.ts:519), independent of zoom AND of `output.scale`. The ANNOTATION layer in the same drawing converts the opposite way — `paperMmToPx(mm, scaleDenominator, pxPerWorldM)` (annotations/paperScale.ts:107) — i.e. paper-mm → world-m → screen-px. The two agree only when the canvas zoom is exactly 37.8 px per world metre at 1:100. A fitted residential plan runs ~50-90 px/m (`fitToDrawing` → `pxPerWorldM = h_css/(2·frustumH)`), so the linework draws at roughly 40-75% of its own paper weight while the text beside it is paper-correct. Effective on-screen paper weights on a typical fit: cut wall ~0.25-0.37 mm, projected wall ~0.12-0.19 mm, furniture/ceiling ~0.06-0.10 mm, beyond ~0.04-0.07 mm — most of the drawing lands BELOW ISO 128-20's thinnest 0.13 mm pen.\n\n(c) COLOUR/BACKGROUND — background is hard `#ffffff` (PlanViewCanvas.ts:373 and CSS `.svp-plan-view-canvas{background:#ffffff}`). CUT and wall/slab PROJECTION are `#000000`. Everything below that is grey-on-white: door/window `#1f2937`, stair `#334155`, roof `#475569`, ceiling `#64748b`, furniture/lighting `#303030`, and BEYOND `#6b7280` at opacity 0.55. Combined with the sub-pixel widths below, BEYOND computes to roughly `#e5e6e9` on white — about 1.2:1 contrast, i.e. effectively invisible. Room fills sit UNDER the linework (render order 383 → 431, ruled out as an occluder) but at alpha 0.35×0.7 = 0.245 they raise local background luminance and further erode the grey pens.\n\n(e) DPI — YES, real, and the guard is measuring the wrong unit. `resolveCanvasRenderScale(0.9375)` returns 3, the backing store is 3× the CSS box, but the CSS box is only 0.9375 device px per CSS px, so the browser resamples DOWN by 3.2×. Real device width = `ctx.lineWidth × devicePixelRatio`, not `× scale`. Five of the eight rungs are sub-device-pixel on his screen: 0.25 mm → 0.886 dev px, 0.18 → 0.638, 0.13 → 0.461, 0.09 → 0.319 (×0.55 alpha). The whole L-288 fix and its guard assume dpr ≥ scale.\n\n(d) STANDARD vs CODE — SPEC-AUTODIMENSION §12.11 (line 345) states the required hierarchy and §13.3 item 6 (line 540) states, verbatim, \"⛔ §12.11 lineweight hierarchy is not enforced.\" C101 and C102 contain ZERO clauses on line weight or legibility (grepped). C09 §4.6.4/§4.6.4e is the only normative source, and its claim that the ladder \"is already right in EXPORT\" is false for the sheet/PDF path.\n\nBONUS (explains the founder's \"dense circular hatch\" bursts): lighting fixtures have no ISO layer, fall to `projection-visible`, and therefore draw at the `__default__` (wall) pen — 0.25 mm SOLID BLACK — heavier than a door, a stair or furniture, with 672 tessellated faces each.",
  "whatIcouldNotEstablish": "1. THE EXACT ZOOM in the founder's screenshot. I derived `pxPerWorldM = h_css/(2·_frustumH)` from `worldToScreen`/`getPixelsPerUnit` (PlanViewCanvas.ts:1345-1364) and `fitToDrawing` (:1586-1614, padding 1.14, frustum clamped to [3, 80] m), but I do not know his pane height in CSS px or his building's extents, so the \"40-75% too thin\" range is a computed band, not a measurement of his frame. The DIRECTION is certain (any zoom above 37.8 px/m makes the linework too thin, and a fitted residential plan is above it); the MAGNITUDE is bracketed.\n\n2. THE BROWSER'S ACTUAL RESAMPLING FILTER for the 3.2× canvas downsample. If Skia area-averages, a 0.886-device-px black line keeps ~89% of its ink (readable but soft); if it bilinear-samples the base level it can partially DROP thin lines and shimmer on pan. I could not determine this from the repo and did not run the app. The sub-device-pixel arithmetic is certain; how ugly it looks is filter-dependent.\n\n3. WHETHER THE FOUNDER'S SPECIFIC VIEW IS ACTUALLY 1:100. The panel reads 1:100, but nothing in the LINEWORK path reads `output.scale` at all (grepped `output.scale|scaleDenominator|customScale` across views/, drawing/, presentation/ — only `PlanViewAnnotationRenderer` and the sheet composers consume it). So the scale setting is inert for linework regardless of its value; I could not find a code path where changing 1:100 → 1:50 changes any stroke width on screen.\n\n4. WHETHER THE SHEET/PDF EXPORT IS THE LIVE PATH. `SVGCompositeRenderer` and `ViewportToSvg` are two DIFFERENT sheet renderers with two different (both non-pen-table) weight authorities. I did not trace which one the founder's \"Export PDF\" button reaches, so finding 8's user impact is asserted for both but proven for neither in isolation.\n\n5. HOW MUCH OF \"quality is bad\" is CONTENT rather than WEIGHT. The log's `[HiddenLineRemoval] … disposition=remove, 239.5 segment-equivalent(s) removed … [17 projection occluder(s) DEGRADED to the vertical-span hull]` means a coarse convex-hull occluder deleted linework. That could be over-deleting real edges. It is a separate failure mode from \"too thin\" and I did not audit it — it belongs to a content/completeness dimension, not this one.\n\n6. I did NOT run any test or gate. Every number here is read from source or computed from constants in source. `CanvasHairlineFloor.test.ts` presumably passes today — that is precisely the point of finding 2 (it passes because it measures the wrong unit).",
  "findings": [
    {
      "title": "Linework line-width is pinned to a constant 96-DPI mm→px factor while the annotation layer in the SAME drawing is paper-scale-and-zoom-aware — the linework draws at 40-75% of its own paper weight on a fitted plan",
      "mechanism": "Two conflicting paper-millimetre→pixel conversions coexist. LINEWORK: `PlanViewCanvas.render()` computes `_penPx = _pen.widthMm * SCREEN_PX_PER_MM` where `SCREEN_PX_PER_MM = 96/25.4 = 3.7795` — a compile-time constant that reads neither the view's `output.scale` nor the canvas zoom. ANNOTATIONS: `PlanViewAnnotationRenderer._paperPx()` calls `paperMmToPx(mm, scaleDenominator, pxPerWorldM)` = `(mm × denom / 1000) × pxPerWorldM`, the full paper→world→screen chain. The two agree only when `pxPerWorldM == SCREEN_PX_PER_MM × 1000/denom` = 37.795 px per world metre at 1:100. `fitToDrawing` sets `_frustumH = max(3, min(80, max(height×0.57, width×0.57/aspect)))` and `worldToScreen` gives `pxPerWorldM = h_css/(2·_frustumH)`, so a 12 m deep plan in a 700 px pane fits at ~51 px/m and a 10 m plan in a 900 px pane at ~79 px/m. At 51 px/m the pens draw at 37.8/51 = 74% of paper weight; at 79 px/m, 48%. Effective printed weights therefore collapse to: cut wall 0.24-0.37 mm, projected wall 0.12-0.19 mm, ceiling/furniture/lighting 0.06-0.10 mm, beyond 0.04-0.07 mm. ISO 128-20's thinnest standard pen is 0.13 mm, so most of the drawing is below the thinnest pen a drafter is allowed to use — while the room tags beside it are at their correct paper size, which is exactly what makes the linework look starved.",
      "evidence": [
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\views\\PlanViewCanvas.ts:519 — `const _penPx    = _pen.widthMm * SCREEN_PX_PER_MM;`",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\views\\PlanViewCanvas.ts:524 — `ctx.lineWidth = Math.max(hairline, _penPx * _vgFactor);`",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\drawing\\DrawingConstants.ts:73 — `export const SCREEN_PX_PER_MM: number = SCREEN_DPI / MM_PER_INCH; // ≈ 3.779`",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\annotations\\paperScale.ts:107-115 — `export function paperMmToPx(paperMm, scaleDenominator, pxPerWorldM) { ... return paperMmToWorldM(paperMm, scaleDenominator) * zoom; }`",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\views\\PlanViewAnnotationRenderer.ts:368-370 — `private _paperPx(mm: number): number { return paperMmToPx(mm, this._scaleDenominator, this._pxPerWorldM); }`",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\views\\PlanViewCanvas.ts:1353-1360 — `worldToScreen`: `const sx = (worldH - this._camTarget.x + fW) / (2 * fW) * w;` ⇒ pxPerWorldM = h/(2·_frustumH)",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\views\\PlanViewCanvas.ts:1614 — `this._frustumH = Math.max(MINIMUM_PLAN_VIEW_CANVAS_FRUSTUM, Math.min(80, Math.max(fitByHeight, fitByWidth)));`",
        "grep for `output.scale|scaleDenominator|customScale` across packages/core-app-model/src/{views,drawing,presentation}/ returns ONLY PlanViewAnnotationRenderer and the sheet types — zero hits in the pen/stroke path"
      ],
      "userVisibleSymptom": "Every line in plan/section/elevation is visibly too thin for the size the drawing is displayed at; the room tags and dimension text look correctly sized while the walls around them look like hairlines. Changing the view's Scale from 1:100 to 1:50 or 1:200 changes nothing about the linework, which is itself wrong (a 1:50 sheet should read heavier relative to the model).",
      "fix": "Make the linework travel the SAME transform the annotations already use. In `PlanViewCanvas.render()`, replace `_pen.widthMm * SCREEN_PX_PER_MM` with `paperMmToPx(_pen.widthMm, resolveScaleDenominator(viewDef.output), pxPerWorldMetre(w2s))`, resolved ONCE per pass exactly as `PlanViewAnnotationRenderer` resolves `_scaleDenominator`/`_pxPerWorldM` (lines 438-439). Keep the existing `Math.max(hairline, …)` floor but change the floor to one DEVICE pixel (see finding 2). Do NOT inflate the pen table — the millimetres are correct and are the ISO/BS-1192 series; the conversion is what is wrong. Add a guard that renders the same plate at two zoom levels and asserts the stroke width scales with `pxPerWorldM`, and a second that asserts 1:50 strokes are 2× the 1:100 strokes at equal zoom.",
      "effort": "medium",
      "risk": "medium",
      "confidence": "high",
      "alreadyExists": "`packages/core-app-model/src/annotations/paperScale.ts` already implements the entire correct transform — `paperMmToWorldM`, `paperMmToPx`, `resolveScaleDenominator`, `pxPerWorldMetre` — and its own header states the rule as normative ('paper mm ──(× view scale)──▶ world metres ──(× zoom)──▶ screen pixels'). It even names the precedent: `tierGapWorldM` (L-281) already pins the dimension tier gap to paper. Nothing needs to be invented; the linework simply never called the module.",
      "filesToChange": [
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\views\\PlanViewCanvas.ts",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\drawing\\CanvasRenderScale.ts"
      ]
    },
    {
      "title": "The stroke floor and its merge-blocking guard measure BACKING-STORE pixels and call them DEVICE pixels — on the founder's dpr 0.9375 display the guard is 3.2× optimistic and five of the eight pen rungs are sub-device-pixel",
      "mechanism": "`setSize()` allocates a backing store of `cssW × scale` where `scale = resolveCanvasRenderScale(dpr) = min(4, max(1, dpr, 3))` = 3 for dpr 0.9375, and `render()` installs `setTransform(scale,…)`. But the canvas's CSS box is `width:100%; height:100%` of the pane, so the browser composites that cssW×3 backing store into only `cssW × dpr` = cssW×0.9375 physical pixels — a 3.2× DOWNSAMPLE. Therefore the true device width of a stroke is `ctx.lineWidth × devicePixelRatio`, NOT `ctx.lineWidth × scale`. `minStrokePx(scale) = 1/scale` is described in its own docstring as 'exactly one device pixel'; it is one BACKING pixel = 0.3125 device pixels here. The guard repeats the same substitution: `widthDevice: ctx.lineWidth * scale`. So at dpr 1 the guard asserts the 0.09 mm BEYOND pen is '1.02 device px' when on the actual screen it is 0.340 device px. Real device widths at dpr 0.9375: 0.70 mm → 2.480; 0.50 → 1.772; 0.35 → 1.240; 0.25 → 0.886; 0.18 → 0.638; 0.13 → 0.461; 0.09 → 0.319. C09 §4.6.4e(d) says verbatim 'GUARD IT IN DEVICE PIXELS. A guard written in millimetres passes on the broken product' — the guard is now written in backing pixels, which is the same failure one unit further along.",
      "evidence": [
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\views\\PlanViewCanvas.ts:1835-1839 — `const scale = resolveCanvasRenderScale(window.devicePixelRatio); const pw = Math.round(this._cssW * scale); … this._canvas.width = pw;`",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\apps\\editor\\src\\ui\\styles\\panels\\splitView.ts:467-475 — `.svp-plan-view-canvas { position: absolute; inset: 0; width: 100%; height: 100%; … background: #ffffff; }` (CSS box = pane CSS px, so device px = cssW × dpr, never × scale)",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\drawing\\CanvasRenderScale.ts:123-125 — `export function minStrokePx(scale: number): number { return 1 / Math.max(1, scale); }` under the header '**Exactly ONE DEVICE PIXEL.**'",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\drawing\\CanvasHairlineFloor.test.ts:93 — `widthDevice: (ctx as { lineWidth: number }).lineWidth * scale,` with the comment '`widthDevice = ctx.lineWidth × scale` … Device pixels are what the eye receives, so device pixels are what this file asserts.'",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\drawing\\CanvasHairlineFloor.test.ts:183-185 — `expect(beyond!.widthDevice).toBeCloseTo(0.09 * SCREEN_PX_PER_MM * 3, 4);// ≈ 1.02` — the real device width at dpr 1 is 0.340",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\picking\\src\\gpu-pick.ts:840 — `const dpr = (globalThis as { devicePixelRatio?: number }).devicePixelRatio ?? 1;` — the source of the founder's logged `dpr=0.9375`",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\docs\\02-decisions\\contracts\\C09-AI-AND-VISIBILITY-INTENT.md:800 — '**(d) GUARD IT IN DEVICE PIXELS.** A guard written in millimetres passes on the broken product'"
      ],
      "userVisibleSymptom": "Fine linework renders as soft grey rather than crisp black and can shimmer or partially disappear when panning. The C09 §4.6.4e ladder that the guard certifies as visible is, on this display, five rungs of sub-pixel grey. Nothing in CI reports it because the guard measures a unit the eye never receives.",
      "fix": "Two changes, together. (1) Correct the unit: `minStrokePx` must be `1 / max(1, devicePixelRatio)` — the thinnest mark the DISPLAY can make — not `1/scale`; pass dpr, not the backing scale. (2) Correct the guard: `CanvasHairlineFloor.test.ts` must compute `widthDevice = ctx.lineWidth * dpr` and must include a dpr < 1 case (0.9375 and 0.8 are real Windows-scaling values) alongside dpr 1/2/4. Expect several existing assertions to go red — that is the point; they are currently certifying a breach as clean. Optionally clamp `resolveCanvasRenderScale` so the backing store never exceeds ~2× the device resolution, since super-sampling beyond that buys nothing and costs fill-rate quadratically.",
      "effort": "small",
      "risk": "low",
      "confidence": "high",
      "alreadyExists": "`resolveCanvasRenderScale` / `minStrokePx` / `dashScale` already separate the three concerns correctly in SHAPE — the only error is which ratio each is fed. `dashScale(devicePixelRatio)` in the very same file already uses the DISPLAY ratio and is the correct precedent to copy. `CanvasHairlineFloor.test.ts` already has a `strokesAtDpr(viewId, layers, dpr)` harness that pins `window.devicePixelRatio` — it only needs the multiplier fixed and a sub-1 dpr row added.",
      "filesToChange": [
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\drawing\\CanvasRenderScale.ts",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\drawing\\CanvasHairlineFloor.test.ts",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\views\\PlanViewCanvas.ts"
      ]
    },
    {
      "title": "The BEYOND zone renders at roughly 1.2:1 contrast — effectively invisible — because a 0.09 mm grey pen at 55% opacity lands on 0.319 device pixels",
      "mechanism": "Three de-emphasis mechanisms multiply instead of one being chosen. The BEYOND pen is `pen(0.09, '#6b7280', null, 0.55)` for every category. `ctx.globalAlpha = _pen.opacity` applies the 0.55; the colour is already a mid-grey (107,114,128 ≈ 44% luminance); and the width resolves to 0.340 CSS px = 0.319 device px on the founder's display. Composited: effective coverage×alpha = 0.319 × 0.55 = 0.175, blended onto the hard `#ffffff` background gives about rgb(229,230,233) ≈ `#e5e6e9`, a contrast ratio of roughly 1.24:1 against white. WCAG's floor for a non-text graphic is 3:1. The same arithmetic makes ceiling PROJECTION (`#64748b`, 0.13 mm → 0.461 dev px, alpha 1) land near `#b7bfca` ≈ 1.84:1. Where a pastel room fill sits underneath (alpha 0.35 × 0.7 = 0.245 of a saturated hue on white) the local background is lighter still and the contrast drops further. C09 §4.6.4d(b) already concedes the point in principle — 'Both pens are ≈ one device pixel at the canvas's backing scale … any WIDTH difference between them is sub-pixel by construction' — but draws the conclusion that the DASH carries the distinction, not that the mark is too faint to see at all.",
      "evidence": [
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\drawing\\PenWeightTable.ts:202 — `wall:       pen(0.09, '#6b7280', null, 0.55),` (identical for slab/column/beam/door/window/stair/roof/ceiling/furniture/lighting/plumbing, lines 202-214)",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\drawing\\PenWeightTable.ts:151 — `ceiling:    pen(0.13, '#64748b'),`",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\views\\PlanViewCanvas.ts:516-517 — `ctx.strokeStyle = vgEdge ?? _pen.color; ctx.globalAlpha = _pen.opacity;`",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\views\\PlanViewCanvas.ts:373 — `ctx.fillStyle = '#ffffff';` (the background is a hard literal, no theme token)",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\views\\PlanViewCanvas.ts:2517 — `ctx.globalAlpha = opacity * 0.7 * vgAlpha; // plan view is slightly more transparent than 3D` with `RoomColourSystem.defaultOpacity()` = 0.35 (packages/room-topology/src/RoomColourSystem.ts:205-207) ⇒ room fills at 24.5%"
      ],
      "userVisibleSymptom": "Geometry beyond the cut plane — the storey below, the lower run of a stair, anything in the 1.20 m below-level depth the default intent projects — is drawn but cannot be seen. The user reads the plan as missing content rather than as de-emphasised content. Ceiling and furniture projection linework reads as a faint smudge.",
      "fix": "Stop multiplying three de-emphasis knobs. Pick ONE: either keep the 0.55 opacity and raise BEYOND to a solid darker grey at a width the display can render, or keep the light grey and drop the opacity to 1. Concretely: set a contrast floor for the composited stroke (target ≥ 3:1 on white) and assert it in a test that computes the blend from `(strokeStyle, globalAlpha, deviceWidth)` — the same shape as the §12.14 millimetre assertions, which is the precedent this repo already accepted for text. Note that fixing finding 1 (paper-scale widths) roughly doubles every device width on a fitted plan and on its own lifts BEYOND from ~1.24:1 to ~1.5:1 — necessary but not sufficient, so this needs the colour/alpha decision too.",
      "effort": "small",
      "risk": "low",
      "confidence": "high",
      "alreadyExists": "`SPEC-AUTODIMENSION §12.14.1` is the exact template for this fix: it took a legibility complaint ('the text of the rooms, labels, is not readable'), converted it into MEASURED millimetres before and after, and pinned the numbers in `packages/file-format/__tests__/sheet-paper-text-standard.test.ts`. A composited-contrast assertion for strokes is the same instrument applied to the other half of the drawing. `packages/core-app-model/src/drawing/DrawingZone.ts` already owns `BEYOND_DASH_PX`/`HIDDEN_DASH_PX` so the beyond/hidden distinction survives any width change.",
      "filesToChange": [
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\drawing\\PenWeightTable.ts",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\views\\PlanViewCanvas.ts"
      ]
    },
    {
      "title": "Lighting fixtures have NO ISO layer, so their 672-face tessellation projects onto `projection-visible` and draws at the __default__ (WALL) pen — 0.25 mm SOLID BLACK, heavier than a door, a stair or furniture",
      "mechanism": "`ELEMENT_TYPE_TO_PROJECTION_LAYER` has no key for Lighting (canonical key `lighting` is absent from the 47 keys), so `resolveProjectionLayer('Lighting')` returns `FALLBACK_NATIVE_LAYER = 'projection-visible'`. Downstream, three lookups all miss and all fail OPEN toward heavier: (1) `drawingZoneFromLayerName('projection-visible')` → null → `_zone` defaults to 'projection'; (2) `penCategoryForLayerTag('projection-visible')` runs eleven regexes — wall/door/slab/column/stair/roof/ceiling/furniture/handrail/window/boundary-line — none of which matches, so it returns the fallback string `'projection'`; (3) `resolvePen('PROJECTION','projection')` has no such key, so it returns `FALLBACK_PEN` = 0.18 mm `#000000`. Then the intent tier overwrites even that: `_intentRules()` ALWAYS contributes a rule at priority 1000, `rulesFor(intent,'projection')` misses every arm and lands on `intent.elementRules.__default__`, which `VisibilityIntentDefaults` seeds from the **wall** category (`const category = elementType === '__default__' ? 'wall' : elementType`), i.e. 0.25 mm `#000000` solid. The `lighting: pen(0.13, '#303030')` row that exists at PenWeightTable.ts:153 is unreachable because nothing ever emits a layer tag that matches it. Net: each light fitting dumps hundreds of black 0.25 mm tessellation edges — the same weight as a projected wall — producing the concentric-burst blobs, and they outweigh the building.",
      "evidence": [
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\apps\\editor\\src\\engine\\views\\EdgeProjectorService.ts:203-239 — `ELEMENT_TYPE_TO_PROJECTION_LAYER` … `PlumbingFixture: 'A-PLMB', … rhino: 'A-FURN',` — no Lighting key anywhere in the map",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\apps\\editor\\src\\engine\\views\\EdgeProjectorService.ts:241-242 — `/** Layer name used for element types not covered … */ const FALLBACK_NATIVE_LAYER = 'projection-visible';`",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\apps\\editor\\src\\engine\\views\\EdgeProjectorService.ts:327-330 — `export function resolveProjectionLayer(elementType) { … return _CANONICAL_TYPE_TO_LAYER.get(_canonicalTypeKey(elementType)) ?? FALLBACK_NATIVE_LAYER; }`",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\drawing\\PenWeightTable.ts:439-452 — `penCategoryForLayerTag` builds flags from eleven regexes; there is no `isLighting`, no `isPlumbing`, no `isGrid`, no `isAnnotation`, no `isLevel`",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\drawing\\PenWeightTable.ts:265 — `export const FALLBACK_PEN: PenStyle = pen(0.18, '#000000');`",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\presentation\\VisibilityIntentDefaults.ts:104 — `const category = elementType === '__default__' ? 'wall' : elementType;` — the __default__ rules are the WALL pens",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\drawing\\GraphicsRulesEngine.ts:202-204 — `_intentRules()` ALWAYS contributes a rule (priority 1000) whose `widthMm` is the intent's own seeded width — so ANY value the table put in `resolved.widthMm` is unconditionally overwritten`",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\drawing\\PenWeightTable.ts:153 — `lighting:   pen(0.13, '#303030'),` — the correct pen, unreachable",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\drawing\\DrawingLayerIdentity.ts:96-124 — `ISO_LAYER_TO_VG_CATEGORY` has A-WALL…A-CONS, A-AREA — no `A-LGHT` row (and line 122 already records the identical defect for A-AREA: '⚠ `penCategoryForLayerTag` has NO arm for this tag yet, so the strokes take the generic fallback pen')"
      ],
      "userVisibleSymptom": "Exactly the founder's screenshot: dense circular black line bursts, drawn as heavily as the walls, that dominate every room. They are also unhideable — `vgCategoryForLayer('projection-visible')` returns null so no VG/visibility-intent toggle can reach them, which is very likely the same root as his separate 'I exclude the furniture elements from view but they still render' report.",
      "fix": "Four one-line additions, all of which land the linework on pens that already exist. (1) `ELEMENT_TYPE_TO_PROJECTION_LAYER`: add `Lighting: 'A-LGHT', LightingFixture: 'A-LGHT', light: 'A-LGHT'`. (2) `ISO_LAYER_TO_VG_CATEGORY`: add `'A-LGHT': 'lighting'` (this alone gives the founder the visibility toggle). (3) `categoryFromFlags`/`penCategoryForLayerTag`: add `isLighting: /A-LGHT|lighting/i` and the matching `if (flags.isLighting) return 'lighting';` — and while there, `A-PLMB`→plumbing, `A-GRID`→grid, `A-ANNO`→annotation, `A-LEVL`→level, `A-AREA`→spaceEnvelope, all of which have pens today and none of which can reach them. (4) `VisibilityIntentDefaults.ELEMENT_TYPES`: add `'lighting'` and `'handrail'` so they get real intent rules instead of the wall-shaped `__default__`. Separately: a family that has a plan SYMBOL should not also emit raw mesh edges — gate the projector so `A-LGHT` contributes no edge geometry in plan-family views (SymbolInjectionGate.ts is the existing seam). Add a guard that asserts `resolveProjectionLayer()` returns a non-fallback layer for every element type the model can produce — the file's own header already warns 'a projection layer that silently falls back to `projection-visible` is how geometry disappears from the pen table, the cut gate and the poché, all at once and with no error.'",
      "effort": "small",
      "risk": "low",
      "confidence": "high",
      "alreadyExists": "The pen exists (`lighting: pen(0.13,'#303030')` PROJECTION / `pen(0.09,'#6b7280',null,0.55)` BEYOND / dashed HIDDEN — PenWeightTable.ts:153, 213, 232). A layer→category mapping for `'A-LGHT': 'lighting'` already exists in `packages/core-app-model/src/drawing/DrawingPipelineWorker.ts:129` (worker path, which has NO callers — PlanViewCanvas.ts:1971 records the measurement). A lighting PLAN SYMBOL renderer already exists and is already wired: `packages/core-app-model/src/views/symbols/LightingPlanSymbolRenderer.ts` (`renderLightingSymbols` + `hitTestLightingSymbol`, per-family extents for 11 fixture types), called from PlanViewCanvas.ts:660 and :2092. `packages/core-app-model/src/presentation/SymbolInjectionGate.ts` exists for the suppress-raw-edges half.",
      "filesToChange": [
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\apps\\editor\\src\\engine\\views\\EdgeProjectorService.ts",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\drawing\\DrawingLayerIdentity.ts",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\drawing\\PenWeightTable.ts",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\presentation\\VisibilityIntentDefaults.ts"
      ]
    },
    {
      "title": "`LightingPlanSymbolRenderer` hard-codes `ctx.lineWidth = 1.25` and a saturated UI blue `#0ea5e9` — a C09 §4.6.4b breach that makes a light fitting draw heavier than a projected wall",
      "mechanism": "`drawLightingSymbol()` sets its own pen: `ctx.lineWidth = 1.25` (CSS px, fixed) and `ctx.strokeStyle = style.stroke` where `STROKE_DEFAULT = '#0ea5e9'` and `FILL_DEFAULT = 'rgba(14,165,233,0.12)'`. 1.25 CSS px is 0.331 mm-equivalent at the canvas's own 3.7795 px/mm — heavier than a projected wall (0.945 px), heavier than a door (0.680 px), 2.5× a furniture pen. It is also invariant under zoom AND under the pen table AND under the visibility intent, so no user setting can change it. C09 §4.6.4b is explicit and normative: 'A symbol renderer MUST receive a fully-resolved, screen-space pen (`SymbolPen`) and MUST NOT import the pen table, the intent resolver, or `SCREEN_PX_PER_MM`. It cannot re-decide a weight if it is not given the means to.' This renderer re-decides a weight. The door/window symbolic path was fixed exactly this way by L-280 (PlanViewCanvas.ts:588-595 builds a `SymbolPen` from the already-resolved `_pen` and hands it to `renderSymbol`); the lighting renderer was never brought onto that seam.",
      "evidence": [
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\views\\symbols\\LightingPlanSymbolRenderer.ts:104 — `ctx.lineWidth   = 1.25;`",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\views\\symbols\\LightingPlanSymbolRenderer.ts:13-16 — `const STROKE_DEFAULT = '#0ea5e9'; const FILL_DEFAULT = 'rgba(14,165,233,0.12)'; const STROKE_SELECTED = '#7c3aed'; const FILL_SELECTED = 'rgba(124,58,237,0.18)';`",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\views\\symbols\\LightingPlanSymbolRenderer.ts:100-103 — `ctx.save(); ctx.strokeStyle = style.stroke; ctx.fillStyle = style.fill;` (no SymbolPen parameter in the signature at :88-94)",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\docs\\02-decisions\\contracts\\C09-AI-AND-VISIBILITY-INTENT.md:870-899 — '§4.6.4b — THE PEN TABLE IS THE *ONLY* PEN AUTHORITY, INCLUDING FOR SYMBOLS … A symbol renderer MUST receive a fully-resolved, screen-space pen (`SymbolPen`)'",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\views\\PlanViewCanvas.ts:588-596 — the correct pattern, for doors/windows: `const _symPen: SymbolPen = { widthPx: ctx.lineWidth, color: vgEdge ?? _pen.color, dashPx: …, opacity: _pen.opacity }; renderSymbol(ctx, _symbolicRule, _segs, _symPen);`"
      ],
      "userVisibleSymptom": "Light fittings are drawn in bright sky-blue at a weight heavier than the walls, on a black-and-white technical drawing. §12.11's 'Annotations must never visually overpower cut walls' is violated by a category that is not even in the priority table. On export they are the wrong colour and the wrong weight too.",
      "fix": "Give `renderLightingSymbols` a `SymbolPen` parameter and delete both literals, exactly as L-280 did for door/window. `PlanViewCanvas._renderLightingPlanSymbols()` (line 2292) resolves the pen once via `graphicsRulesEngine.resolveStyle('PROJECTION', 'lighting', { viewId, viewType })` — which, once finding 4 lands, returns 0.13 mm `#303030` — composes it with the VG factor and the stroke floor, and passes it down. Keep a selected-state highlight, but as an OVERLAY colour applied by the canvas, not as the symbol's own pen.",
      "effort": "small",
      "risk": "low",
      "confidence": "high",
      "alreadyExists": "`SymbolPen` and `renderSymbol` already exist in `packages/core-app-model/src/drawing/SymbolicRuleRenderer.ts` (`ctx.lineWidth = pen.widthPx;` at line 117) and are already used correctly by the door/window path. This is a matter of routing the lighting renderer onto the seam that is already built, not building one.",
      "filesToChange": [
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\views\\symbols\\LightingPlanSymbolRenderer.ts",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\views\\PlanViewCanvas.ts"
      ]
    },
    {
      "title": "STANDARD-vs-CODE GAP: SPEC-AUTODIMENSION §12.11 states the lineweight hierarchy and §13.3 records verbatim that it is NOT ENFORCED; C101 and C102 contain zero line-weight clauses; and no document anywhere states a MINIMUM on-screen weight or contrast",
      "mechanism": "§12.11 requires eight ranked tiers — Cut walls Heavy, Structure Heavy, Doors Medium, Windows Medium, Furniture Light, Dimensions Thin, Tags Thin, Room fills Very light — plus '⛔ Annotations must never visually overpower cut walls'. The pen table encodes the FABRIC half of that ranking (0.50/0.70 cut, 0.35 door/window cut, 0.13 furniture) but §12.11's ANNOTATION rows are not on the same authority: `PlanViewAnnotationRenderer` mostly writes bare CSS-pixel literals — 3.5, 2.5, 2.4, 1.8, 1.5, 1.25, 1.2, 1, 0.8, 0.75 — and only three call sites route through `_annotationLineWidthPx(styleMm, scale, minPx)`, which itself uses the fixed `SCREEN_PX_PER_MM` rather than the paper transform used for the text next to it. A 3.5 px annotation literal is heavier than a 1.89 px cut wall, which is the exact prohibition §12.11 closes with. And §12.14 established the repo's own precedent — 'the absence of a number is what let the defect ship' — for TEXT; the equivalent numbers for LINES (an absolute minimum weight, and a minimum contrast) were never written.",
      "evidence": [
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\docs\\03-execution\\specs\\SPEC-AUTODIMENSION.md:345-358 — '### §12.11 — Lineweight hierarchy | Cut walls | Heavy | … | Room fills | Very light | ⛔ **Annotations must never visually overpower cut walls.**'",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\docs\\03-execution\\specs\\SPEC-AUTODIMENSION.md:540 — '6. ⛔ **§12.11 lineweight hierarchy is not enforced.**'",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\docs\\03-execution\\specs\\SPEC-AUTODIMENSION.md:376-380 — the §12.14 precedent: 'This clause did not exist until an exported PDF was unreadable … **Everything below is a NUMBER, deliberately, because the absence of a number is what let the defect ship.**'",
        "grep -rniE 'line ?weight|lineweight|pen weight|hairline|readab|legib' over C102-VIEW-AND-SHEET-INTEGRITY.md + C101-ELEMENT-ANNOTATION.md + C59-MULTI-PANE-VIEW-SYSTEM.md → 2 hits, both incidental prose ('would make both unreadable', '@deprecated readable'). Neither contract has a line-weight clause.",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\views\\PlanViewAnnotationRenderer.ts:986 — `ctx.lineWidth = 3.5;` (vs a cut wall at 1.89 CSS px)",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\views\\PlanViewAnnotationRenderer.ts:499-503 — `private _annotationLineWidthPx(styleMm, scale = 0.5, minPx = 0.5) { … return Math.max(minPx, effectiveMm * SCREEN_PX_PER_MM * scale); }` — the same zoom-blind conversion as the linework, used at only 3 of ~40 stroke sites"
      ],
      "userVisibleSymptom": "There is no written answer to 'how thick should this line be on screen', so nothing catches a regression and nothing tells a lane what 'good' is. Annotation chrome (selection halos, hover boxes, cut-plane handles) is drawn heavier than the building it annotates.",
      "fix": "Write the numbers, then gate them. Add a §12.11.1 to SPEC-AUTODIMENSION stating, in the same style as §12.14: (i) the mm value for each of the eight §12.11 tiers, cross-referenced to the pen table rows that already implement the fabric half; (ii) an ABSOLUTE FLOOR — no drawn line may composite below 1 device pixel of ink at ≥3:1 contrast on the drawing background, the stroke analogue of §12.14's 1.8 mm text floor; (iii) the rule that annotation weight ≤ cut-wall weight, so 3.5 px halos are a declared violation. Then move the ~40 bare literals in `PlanViewAnnotationRenderer` onto `graphicsRulesEngine.resolveStyle('PROJECTION','annotation',…)` (the pen already resolved at line 426-430 and then largely ignored) and add the guard. Also correct SPEC §13.3 item 6 once it lands, so the ledger stops reading NOT ENFORCED.",
      "effort": "medium",
      "risk": "low",
      "confidence": "high",
      "alreadyExists": "`packages/file-format/src/export/sheets/PaperTextStandard.ts` + `packages/file-format/__tests__/sheet-paper-text-standard.test.ts` are the finished template for a 'numbers in a spec, assertions in a gate' legibility standard — §12.14 did exactly this for text and it worked. `PlanViewAnnotationRenderer` already resolves an annotation pen at line 426 (`this._engineAnnotationPen = graphicsRulesEngine.resolveStyle('PROJECTION','annotation',…)`) and `PenWeightTable` already has `annotation: pen(0.18,'#000000')` — the authority exists, the call sites just bypass it.",
      "filesToChange": [
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\docs\\03-execution\\specs\\SPEC-AUTODIMENSION.md",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\views\\PlanViewAnnotationRenderer.ts",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\docs\\02-decisions\\contracts\\C102-VIEW-AND-SHEET-INTEGRITY.md"
      ]
    },
    {
      "title": "C09 §4.6.4e's claim that 'the hierarchy is already right in EXPORT' is FALSE for the sheet/PDF path — the exported drawing has two weights, CUT equals PROJECTION, and the pen table is never consulted",
      "mechanism": "This matters here because it is the stated reason the pen table must not be touched, and it removes a constraint the next lane will otherwise obey. Neither sheet renderer imports `PenWeightTable` (grepped: zero hits for `PenWeightTable|resolvePen|graphicsRulesEngine` across `packages/file-format/src` and `packages/drawing-primitives/src`). `SVGCompositeRenderer` re-derives weight from `material.linewidth` — the VG 1-6 integer that `VGSceneApplicator` writes onto the THREE material — via `PRYZM_WEIGHT_TO_MM = [0,0.18,0.25,0.35,0.50,0.70,1.00]`. With no user overrides the VG defaults are `wall.lineWeight = 2` and almost everything else `1`, and `effectiveCutWeight = style.cutLineWeight ?? style.lineWeight` equals `effectiveProjWeight = style.projectionLineWeight ?? style.lineWeight`, so `A-WALL:cut` and `A-WALL:proj` BOTH export at 0.25 mm and everything else at 0.18 mm. Its `ISO_LINE_WEIGHTS` table (0.50 for A-WALL, etc.) is effectively dead, because THREE's `LineBasicMaterial.linewidth` defaults to 1 — inside the [1,6] guard — so the `pryzmWeightToMm` branch always wins. The second renderer, `ViewportToSvg`, is worse: `defaultStrokeMm: 50` (model mm) / `vp.scale` → a flat 0.5 sheet mm for every line, no hierarchy at all. Meanwhile `VGSceneApplicator`'s own comment asserts the linewidth write is export-only — 'WebGL hardware cap is 1 px — rendering is unaffected' — which is true, and means the ONE channel carrying weight into export is a 1-6 integer that cannot express the 8-rung mm ladder.",
      "evidence": [
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\docs\\02-decisions\\contracts\\C09-AI-AND-VISIBILITY-INTENT.md:786-788 — '**(a) THE PENS ARE CORRECT — THE RASTERISER WAS NOT. IT IS FORBIDDEN TO \"FIX\" THIS BY INFLATING THE PEN TABLE.** The hierarchy is already right in EXPORT (at `EXPORT_DPI` those pens are 2.95 px and 2.07 px)'",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\file-format\\src\\export\\sheets\\SVGCompositeRenderer.ts:248-251 — `const rawWeight = (mat as any)?.linewidth; const lineWeightMm = (typeof rawWeight === 'number' && rawWeight >= 1 && rawWeight <= 6) ? pryzmWeightToMm(rawWeight) : (this._layerLineWeights[layerName] ?? ISO_LINE_WEIGHTS['projection-visible']);`",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\file-format\\src\\export\\sheets\\SVGCompositeRenderer.ts:85 — `const PRYZM_WEIGHT_TO_MM = [0, 0.18, 0.25, 0.35, 0.50, 0.70, 1.00];`",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\presentation\\VGSceneApplicator.ts:685-692 — `(cutLayer.material as any).linewidth = effectiveCutWeight;` / `(projLayer.material as any).linewidth = effectiveProjWeight;` with `effectiveCutWeight = (style as any).cutLineWeight ?? style.lineWeight` and `effectiveProjWeight = (style as any).projectionLineWeight ?? style.lineWeight` (lines 665-667) — identical by default",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\presentation\\VGGovernanceStore.ts:130 — `wall: { … lineWeight: 2, … }` and lines 131-149, everything else `lineWeight: 1`",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\drawing-primitives\\src\\sheet\\ViewportToSvg.ts:46 — `defaultStrokeMm: 50,` and :187-189 — `const strokeModelMm = line.strokeMm ?? o.defaultStrokeMm; const sheetStroke = strokeModelMm / vp.scale;`",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\presentation\\VGSceneApplicator.ts:658 — `(layer.material as any).linewidth = style.lineWeight;` with the header claim at :601-605 'WebGL hardware cap is 1 px — rendering is unaffected'"
      ],
      "userVisibleSymptom": "An exported PDF/SVG sheet shows a cut wall and a projected wall at the same 0.25 mm — the drawing reads flat on paper, and the poché is the only thing separating cut from projected. The contract's assurance that export is correct means nobody has looked.",
      "fix": "Do NOT fix this by inflating the pen table (that would corrupt the DXF enum snap C32 §1.3 requires). Fix the CHANNEL: make the sheet renderers resolve weight the same way the canvas does — `graphicsRulesEngine.resolveStyle(zoneFromLayerName(tag), penCategoryForLayerTag(tag), { viewId, viewType })` → `widthMm` straight into `stroke-width` (SVG user units are already paper mm in `SVGCompositeRenderer`, per its own §12.14.1 note). Delete `ISO_LINE_WEIGHTS`, `PRYZM_WEIGHT_TO_MM` and `ViewportToSvg`'s `defaultStrokeMm` as rival authorities, and stop routing weight through `material.linewidth`. Then correct C09 §4.6.4e(a) in place: the sentence 'the hierarchy is already right in EXPORT' is true only of the raster `pxPerMm(EXPORT_DPI)` path, not of the sheet path, and it should say so.",
      "effort": "medium",
      "risk": "medium",
      "confidence": "medium",
      "alreadyExists": "`penZoneFromLayerName` / `drawingZoneFromLayer` / `penCategoryForLayerTag` (PenWeightTable.ts:400-452) are the canonical tag→(zone,category) classifiers and are already the ONE authority the canvas uses — the export renderers just never call them. `SVGCompositeRenderer` already emits in paper millimetres (its viewBox is 1 unit = 1 mm, established by the §12.14.1 work), so `widthMm` can be written straight out with no conversion.",
      "filesToChange": [
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\file-format\\src\\export\\sheets\\SVGCompositeRenderer.ts",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\drawing-primitives\\src\\sheet\\ViewportToSvg.ts",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\docs\\02-decisions\\contracts\\C09-AI-AND-VISIBILITY-INTENT.md"
      ]
    },
    {
      "title": "`purposeModifiers` can never reach the 2D pen — `resolveIntentPenStyle` drops the `viewPurpose` argument, so three of the five built-in purpose profiles are dead on the drawing",
      "mechanism": "`resolveIntentStyle(intentInstance, intent, elementType, state, viewType, target, viewPurpose?)` applies `intent.purposeModifiers` only when the 7th argument is supplied. `resolveIntentPenStyle` — the only entry point `GraphicsRulesEngine._intentRules()` uses — calls it with six arguments and never passes a purpose. So `BUILT_IN_PURPOSE_MODIFIERS`' 'construction-docs' dense poché, 'design-review' (projection opacity 0.75 + blue tint), 'coordination' (wall cut recoloured `#1a3a6b` at 0.5 mm) and 'presentation' (projection opacity 0.55, beyond 0.15) are authored and unreachable from the canvas pen path. I flag it here rather than as a readability defect because it is the one lever that could ALREADY have raised or lowered drawing emphasis per purpose, and it silently does nothing.",
      "evidence": [
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\presentation\\IntentRuleResolver.ts:353-364 — `export function resolveIntentPenStyle(intentInstance, intent, elementType, zone, viewType, target = {}): Partial<PenStyle> { return appearanceToPenStyle(resolveIntentStyle(intentInstance, intent, elementType, stateFromPenZone(zone), viewType, target)); }` — no `viewPurpose` parameter and none forwarded",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\presentation\\IntentRuleResolver.ts:190-193 — `if (viewPurpose && intent.purposeModifiers) { for (const modifier of intent.purposeModifiers) { if (modifier.purpose !== viewPurpose) continue;` — gated on the argument that is never supplied",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\drawing\\GraphicsRulesEngine.ts:394-406 — the single call site, passing `(virtualInstance, intent, category, zone, ctx.viewType ?? 'plan', {elementId, elementType, category})`",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\presentation\\SystemIntents.ts:63-106 — `BUILT_IN_PURPOSE_MODIFIERS` with construction-docs / design-review / coordination / presentation entries, all carrying line and fill patches",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\presentation\\SystemIntents.ts:279 — every architectural intent passes `purposeModifiers: BUILT_IN_PURPOSE_MODIFIERS`"
      ],
      "userVisibleSymptom": "Setting a view's purpose changes nothing about its linework. A user reaching for 'presentation' or 'construction-docs' to change drawing emphasis gets no response, which reads as another broken setting.",
      "fix": "Add `viewPurpose?: string` to `StyleResolverContext`, read it from the ViewDefinition in `PlanViewCanvas.render()` alongside `viewType`, thread it through `_intentRules` → `resolveIntentPenStyle` → `resolveIntentStyle`'s 7th parameter, and include it in `styleResolverCacheKey` (the cache key at GraphicsRulesEngine.ts:161-165 currently omits it, so adding the argument without the key would serve one purpose's pen to every purpose). Verify AT THE OUTCOME — assert `graphicsRulesEngine.resolveStyle('CUT','wall',{viewPurpose:'coordination'}).color === '#1a3a6b'`, not that the intent record contains the modifier.",
      "effort": "small",
      "risk": "low",
      "confidence": "high",
      "alreadyExists": "The entire purpose-modifier resolution chain is written and tested inside `resolveIntentStyle` (IntentRuleResolver.ts:190-200), including `applyStateTransform` and `mergeAppearance`. The `viewTypeDeclaresCutFill` helper in the same file (line 263) already demonstrates the correct call shape with all seven arguments. Only the one call site is short an argument.",
      "filesToChange": [
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\presentation\\IntentRuleResolver.ts",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\drawing\\GraphicsRulesEngine.ts",
        "c:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08\\packages\\core-app-model\\src\\views\\PlanViewCanvas.ts"
      ]
    }
  ]
}