{
  "area": "Project open — ProjectLoader `hydrate` phase / ImportProjectCommand element-import drain (apps/editor + command-registry + the unbatched DOM event channel)",
  "rootCause": "Hydrate is NOT deserialise-only, and it is NOT mesh construction either — it is a synchronous per-element COMMAND REPLAY whose store writes fan out on an UNBATCHED `window` CustomEvent channel, run as ~4 unyielding macro-tasks.\n\n(a) WHAT IT DOES. `hydrate` spans ProjectLoader.ts:761 (`__phase('setup')`) → :2876 (`__phase('hydrate')`). Its dominant sub-span is the element-import drain at :879 `importResult = await this.commandManager.executeChunked(importCmd, yieldFrame);`. `ImportProjectCommand` does not deserialise — `runSub` (:326-350) calls `cmd.canExecute(ctx)` then `cmd.execute(ctx)` for a real Create*Command per element. Each store `add()` then fires TWO channels (FurnitureStore.ts:51-52 is the canonical shape): `storeEventBus.emit(...)` — BATCHED — and `_bus.emit('bim-furniture-added', {id})` on a `DOMEventBus`, whose `emit` is `this._target.dispatchEvent(new CustomEvent(...))` (DOMEventBus.ts:46-52) — a SYNCHRONOUS `window` dispatch that `beginBatch()` (ProjectLoader.ts:733) does not cover. That DOM event runs the builder inline (initBuilders.ts:883 → `_safeFurnitureUpdate` :843 → FurnitureFragmentBuilder.updateFurniture :79, which builds the whole THREE subtree) AND every other listener registered on that name.\n\n⛔ THE BRIEF'S OWN HYPOTHESIS IS REFUTED, and the refutation is the finding. \"31 ms/element = full mesh construction\" is wrong. The real builders were measured into a real THREE.Scene: `packages/geometry-handrail/src/__tests__/HandrailLoadBuildCost.spec.ts` builds 137 handrails (~3,151 meshes) twice; I RAN IT on this machine — 2 tests, tests=516 ms total. At that rate this project's 3,762 meshes cost well under 1 s, i.e. <6 % of the 17.16 s. The spec's own header says it excludes \"the 41-event DOM fan-out each `bim-handrail-added` triggers in the app — that is `initScene`'s, not this builder's.\" That excluded half is where the budget is.\n\n(b) SERIAL — and worse, effectively UNCHUNKED. No `await` sits inside any per-element loop (every `await` in the hydrate window: :872, :879, :1954, :2753, :2778). So it is not 550 microtask turns. It is the opposite failure: `IMPORT_CHUNK_SIZE = 120` (ImportProjectCommand.ts:143) with only THREE unconditional `yield`s (:1074 furniture, :1107 roofs, :1218 curtain walls) and per-120 chunk yields in the wall/slab/furniture/curtain-wall loops. This project has walls=56, slabs=10, curtainWalls=90 — ALL below 120 — so 550 elements run in roughly 4 macro-tasks of several seconds each. The \"progressive chunked build\" does not chunk this project at all; that is the visible freeze.\n\n(c) O(n²) — three candidates found, all RULED OUT for THIS run, which is itself load-bearing: (1) CreateWallCommand.ts:446 clones the whole wall store per wall (N(N-1)/2 deep clones, the ledger's #1 load finding) — but N=56 here ⇒ ~1,540 clones ≈ 7 ms at the ledger's measured 4.5 µs/clone. Not it. (2) TopologyLayer is LAZY, not per-element: `_ensureFresh()` opens `if (!this._dirty) return;` (TopologyLayer.ts:365-366) and rebuilds per QUERY — the log's 4 rebuilds are 4 queries, not 550. (3) LightingFragmentBuilder `_syncAllLights()` is O(n) over every fixture per add, but `_requestSync()` (:773-780) coalesces to ONE `queueMicrotask`, and microtasks cannot interleave inside a synchronous loop ⇒ one pass per chunk, not 87.\n\n(d) STORE-BUS BATCHING WORKS AND IS EXONERATED: 881 buffered events flushed in `event_flush = 15.0 ms`. Nothing expensive subscribes there. The batch simply does not cover the DOM channel.\n\n(e) THE 17 VDT LINES ARE NOISE: ViewDependencyTracker.ts:893-895 logs, calls `_markViewDirtyForStoreType(...)` then `_scheduleDebouncedFlush()` (:898-900). Those arrive on the BATCHED storeEventBus, so they land inside the 15.0 ms `event_flush`. The fallback is coarse (whole store-type instead of a targeted graft) but it is debounced and it is not in hydrate.\n\nWHAT I COULD NOT ESTABLISH — stated plainly rather than filled with prose: WHICH listener on the DOM channel owns the ~16 s residual. Mesh building (<1 s, measured), store-bus flush (15 ms, measured), Zod double-parse (118.7 µs/element measured in the ledger ⇒ ~66 ms), wall clones (~7 ms) and topology (4 rebuilds) together account for under 1.2 s of 17.16 s. The remainder is the unbatched synchronous fan-out plus un-decomposed sub-command bodies, and the instrument that splits those two — `§LOAD-IMPORT-STEPS` (printed UNCONDITIONALLY at ProjectLoader.ts:920-926, 20 named steps heaviest-first) and `§LOAD-YIELD-WAIT` (:906-912, compute-vs-waiting) — was in that same console for this exact run and is not in the excerpt I was given. Read those two lines before touching code; they name the family in ms.",
  "evidence": [
    "apps/editor/src/engine/persistence/ProjectLoader.ts:761 `__phase('setup');           // setup window closed — element hydration begins` and :2876 `__phase('hydrate');     // element + non-element store hydration done` — these two bracket the 17,163.5 ms the founder measured.",
    "apps/editor/src/engine/persistence/ProjectLoader.ts:879 `importResult = await this.commandManager.executeChunked(importCmd, yieldFrame);` — the element-import drain, the only heavy span inside those brackets.",
    "packages/command-registry/src/project/ImportProjectCommand.ts:326-350 `runSub` — `const validation = cmd.canExecute(ctx); … return cmd.execute(ctx);` per element. This is command REPLAY, not deserialisation.",
    "packages/geometry-furniture/src/FurnitureStore.ts:51-52 — `_bus.emit('bim-furniture-added', { id: snap.id });` IMMEDIATELY followed by `storeEventBus.emit({...})`. Two channels; only the second is batched.",
    "packages/event-bus/src/DOMEventBus.ts:46-52 `emit(...)  { this._target.dispatchEvent(new CustomEvent(event, { detail: payload, bubbles: false })); }` — synchronous window dispatch, zero batch awareness. ProjectLoader.ts:733 `storeEventBus.beginBatch();` does not reach it.",
    "apps/editor/src/engine/initBuilders.ts:883-887 `window.addEventListener('bim-furniture-added', (e: Event) => { … _safeFurnitureUpdate('bim-furniture-added', fd); })` → :843-845 `furnitureBuilder.updateFurniture(fd)` → packages/geometry-furniture/src/FurnitureFragmentBuilder.ts:79 `updateFurniture(data)` builds the entire THREE subtree inline. Geometry IS built synchronously inside the element loop.",
    "MEASURED BY ME, this session: `npx vitest run packages/geometry-handrail/src/__tests__/HandrailLoadBuildCost.spec.ts --root packages/geometry-handrail` → `Test Files 1 passed · Tests 2 passed · tests 516ms`. Two full builds of 137 handrails (~3,151 meshes each regime) through the REAL HandrailFragmentBuilder + real InstancedElementRenderer into a real THREE.Scene, in half a second. Scaled to 3,762 meshes this is <1 s — REFUTING 'hydrate is mesh construction'.",
    "packages/geometry-handrail/src/__tests__/HandrailLoadBuildCost.spec.ts:31-33 — the spec's own scope note: \"It also does not measure the 41-event DOM fan-out each `bim-handrail-added` triggers in the app — that is `initScene`'s, not this builder's.\" The measured-cheap half and the unmeasured half are named by the same file.",
    "packages/command-registry/src/project/ImportProjectCommand.ts:143 `export const IMPORT_CHUNK_SIZE = 120;` with only three unconditional `yield`s at :1074, :1107, :1218. walls=56, slabs=10, curtainWalls=90 are all < 120 ⇒ ~4 chunk boundaries for 550 elements ⇒ ~4 multi-second synchronous blocks.",
    "No per-element await: every `await` inside the hydrate window (ProjectLoader.ts 761→2876) is at :872, :879, :1954, :2753, :2778 — the frame yield, the drain itself, component definitions, and two one-shot dynamic imports. The '550 sequential round-trips' hypothesis is false.",
    "packages/room-topology/src/TopologyLayer.ts:365-366 `private _ensureFresh(): void { if (!this._dirty) return;` — adjacency is rebuilt per QUERY, not per element. The log's 4 rebuilds are 4 queries over 528 elements.",
    "packages/geometry-lighting/src/LightingFragmentBuilder.ts:773-780 `_requestSync()` { if (this._syncQueued) return; … queueMicrotask(() => { … this._syncAllLights(); }); } — the O(n) budget resync is coalesced to one microtask, and microtasks cannot run inside the synchronous element loop. 87 fixtures do NOT produce 87 resyncs.",
    "packages/core-app-model/src/views/ViewDependencyTracker.ts:893-900 — the G3 fallback is `console.warn(...)` + `this._markViewDirtyForStoreType(event.elementType);` + `this._scheduleDebouncedFlush();`. It rides the BATCHED storeEventBus, so all 17 land inside `event_flush = 15.0 ms`. Not expensive, not in hydrate.",
    "Unguarded per-event subscribers on the DOM channel, no `isProjectLoadActive()` anywhere in them: apps/editor/src/ui/ViewBrowser/panels/UnifiedBrowserPanel.ts:126 `const refresh = () => this._proxy.refreshIfActive(this._sectionId);` registered on 25 `bim-*` window events at :127-158 → apps/editor/src/ui/tools-panel/ToolsRailController.ts:150-156 `refreshIfActive(id) { if (this._activeId !== id …) return; this._body.innerHTML = ''; this._body.appendChild(builder()); }` — a full synchronous DOM teardown+rebuild per event whenever that rail section is open. Also apps/editor/src/ui/WallEdgeVisibilityService.ts:106 `const reapply = (): void => { queueMicrotask(() => this._apply()); };` (10 events, NO dedupe flag → one full scene.traverse queued per event) and apps/editor/src/engine/initScene.ts:976-980 / :1035-1039 (`queueMicrotask` → full-scene re-apply, no guard).",
    "The load-gate shape ALREADY WORKS where it was applied, and the founder's own log proves it: `[LOAD-SHADOW-PASS-DEFERRED] ran ONE full-scene shadow-flag pass in 7.7ms for 500 caster-mutation event(s) declined during the load.` That is the L-3056 `isProjectLoadActive()` guard (apps/editor/src/engine/perAddGeometryGate.ts:32, applied at initScene.ts:4121) turning 500 full-scene traversals into one 7.7 ms pass. It also calibrates the unit: a full-scene pass over the finished 3,762-mesh scene costs 7.7 ms.",
    "Ruled out arithmetically: docs/04-reference/APPLICATION-PERFORMANCE-LEDGER.md §4.2 measures `CreateWallCommand.ts:446`'s deep clone at 1.5–4.5 µs and the count at N(N−1)/2. This run has walls=56 ⇒ 1,540 clones ⇒ ~7 ms. The ledger's #1 load finding is immaterial at this N."
  ],
  "fix": "STEP 0 — READ BEFORE CHANGING ANYTHING (zero cost, decides the rest). This run already printed two lines that rank the 17.16 s and neither is in the excerpt: `§LOAD-IMPORT-STEPS` (ProjectLoader.ts:920-926, printed unconditionally — 20 named import steps with ms and n, sorted heaviest-first) and `§LOAD-YIELD-WAIT` (:906-912 — compute vs frame-bus waiting). If one step dominates, the fix is that family's command body. If the steps sum well short of 17 s, the fix is the fan-out below. Do not skip this: the repo's whole documented failure mode is confident attribution without the reading.\n\nSTEP 1 — THE FIX, and it is a shape that is already proven in this codebase. Close the third channel. `ProjectLoader.ts:733 storeEventBus.beginBatch()` batches one bus; `packages/event-bus/src/DOMEventBus.ts:46-52` dispatches synchronously to `window` with no batch, no `isBatching`, and no load flag. Two options, in order of preference:\n  (a) Give `DOMEventBus` the same begin/end batch the StoreEventBus already has, opened and closed by ProjectLoader around the drain, so the per-element `bim-*-added` storm is buffered and coalesced by (event, id) exactly once — the builder still runs, the 25 UI subscribers run once instead of ~880 times.\n  (b) If (a) is too broad for one lane, apply the ALREADY-PROVEN per-subscriber gate — `isProjectLoadActive()` from `apps/editor/src/engine/perAddGeometryGate.ts:32`, the same guard whose effect this very log records (500 events declined → one 7.7 ms pass) — to the four unguarded O(scene)/O(DOM) subscribers: `ToolsRailController.refreshIfActive` (:150-156), `UnifiedBrowserPanel`'s 25 registrations (:126-158), `WallEdgeVisibilityService` (:106), and `initScene.ts` (:976-980, :1035-1039). Each needs the same escape hatch L-3056 built: one catch-up pass on `pryzm-project-loaded` / `pryzm-load-suppress-end`, so nothing is DROPPED, only MOVED.\n\nSTEP 2 — MAKE THE CHUNKER ACTUALLY CHUNK. `IMPORT_CHUNK_SIZE = 120` (ImportProjectCommand.ts:143) is an ELEMENT count, and this project's largest per-step family is 90 curtain walls, so it never trips. Yield on a TIME budget instead — `if (performance.now() - chunkStart > 12) yield;` inside every element loop, not four of twenty — so a 17 s import becomes ~1,400 painted frames instead of ~4 frozen macro-tasks. This does not make the load faster; it makes it not a freeze, and it lets the 5 s watchdog and the frame bus run.",
  "expectedSavingMs": 12000,
  "savingBasis": "derived-from-log",
  "risk": "medium",
  "confidence": "medium",
  "rivalRuledOut": "FOUR rivals, three ruled out with numbers and one NOT ruled out — stated separately.\n\nRULED OUT 1 — \"31 ms/element is full mesh construction on the main thread\" (the brief's own leading hypothesis). Refuted by measurement, not argument: I ran `packages/geometry-handrail/src/__tests__/HandrailLoadBuildCost.spec.ts` — 2 tests, 516 ms, building 137 handrails (~3,151 meshes) TWICE through the real builder + real InstancedElementRenderer into a real THREE.Scene. That is ~0.16 µs–13 µs per mesh; 3,762 meshes ⇒ well under 1 s, <6 % of hydrate. Geometry IS built synchronously in hydrate (FurnitureFragmentBuilder.ts:79 via the DOM event) — the brief is right about the MECHANISM and wrong about the COST.\n\nRULED OUT 2 — \"the CreateWallCommand N²/2 wall-store deep clone\" (the performance ledger's own #1 load finding, CreateWallCommand.ts:446). Real, still unfixed, and immaterial HERE: walls=56 ⇒ 1,540 clones × the ledger's measured 1.5–4.5 µs ⇒ ~7 ms. Quoting it for this run would be exactly the confident-but-wrong shape the brief warns about.\n\nRULED OUT 3 — \"the 16.0 s shadow rebuild is inside hydrate.\" `SHADOW_REBUILD_COMPLETE` (RenderPipelineManager.ts:2034-2037) times a debounced async `_rebuildPipeline()`. Arithmetic from the founder's own STARTUP-BUDGET: snapshot-loaded lands at t+21,139 ms and first-interactive-frame at t+36,767 ms — a 15,628 ms gap that the 16,011.5 ms rebuild very nearly fills, so it starts at the END of hydrate and runs AFTER it. hydrate's 17,163 ms and the shadow rebuild's 16,011 ms are two costs in series (17.2 + 16.0 ≈ the 37 s total), not one counted twice. Also ruled out as a hydrate cost by the yield count: only ~4 frame yields exist in the drain, so hydrate cannot be dominated by waiting on slow frames.\n\nNOT RULED OUT, and I am saying so rather than picking one: within the unbatched DOM fan-out I could not separate (i) expensive UI subscribers — `ToolsRailController.refreshIfActive` innerHTML rebuild, `WallEdgeVisibilityService` scene.traverse, the initScene microtask re-applies — from (ii) expensive Create*Command bodies in one particular family. Both live inside the same opaque `element_import` bucket, and the two are distinguished by `§LOAD-IMPORT-STEPS`, which this run printed and the excerpt omits. The UI-subscriber arm additionally depends on WHICH rail panel was open — `refreshIfActive` early-returns unless its section is active — so the same project can load seconds faster with a panel closed, and nothing in the log would say so (ISSUE-LOG L-3059 records exactly this un-audited population).",
  "whatItWouldBreak": "Batching or gating the DOM channel MOVES work; it must never DROP it — that is the precise trap L-3056 already fell into and escaped. Concretely: (1) Any subscriber gated on `isProjectLoadActive()` MUST get a one-shot catch-up on BOTH load-end signals (`pryzm-project-loaded` on the runtime bus AND `pryzm-load-suppress-end` on window, which ProjectLoader dispatches on EVERY exit including cancelled/failed loads) — gate one without the catch-up and a restored project comes back with no shadow flags, no wall-edge gating, or a stale rail panel. ISSUE-LOG L-3060 records a sibling defect where a `pryzm-project-loaded` listener was registered on the window channel while all four emitters used `runtime.events.emit` — the two channels are NOT interchangeable, and a fix that picks the wrong one is silently dead.\n(2) Batching `DOMEventBus` changes ORDERING. Builders deliberately depend on synchronous fan-out ordering: `initTools.ts` registers a wall in the ViewDependencyTracker BEFORE `wallStore.add()` precisely because `add()` fires synchronously (§G3-STALE-FIX, quoted at plugins/wall/src/handlers/CreateWall.ts:69-78), and `CurtainPanelSyncHandler` emits one event per panel that the VDT can only attribute if the PARENT is already registered. Buffer these and you can manufacture more `§G3-STALE-EVENT` fallbacks, not fewer.\n(3) A time-budget yield inside the element loops changes how many partial frames the viewport draws mid-load, and the viewport during load is the WebGPU pipeline the ledger measures at 6 full scene-graph walks per frame — more yields means more of those frames. Measure `§LOAD-YIELD-WAIT` before and after; if `waiting` rises faster than `compute` falls, the yield cadence is wrong.\n(4) Contracts: this touches C11 §6.1 (batch geometry MUST be spread across frames via the scheduler), C05/C13 (load + isolation), C03/C16 (command authoring), and P3 (no new rAF — the yield must stay on `yieldForProgress`/FrameScheduler, never `requestAnimationFrame`).",
  "filesToChange": [
    "apps/editor/src/engine/persistence/ProjectLoader.ts",
    "packages/command-registry/src/project/ImportProjectCommand.ts",
    "packages/event-bus/src/DOMEventBus.ts",
    "apps/editor/src/engine/perAddGeometryGate.ts",
    "apps/editor/src/ui/tools-panel/ToolsRailController.ts",
    "apps/editor/src/ui/ViewBrowser/panels/UnifiedBrowserPanel.ts",
    "apps/editor/src/ui/WallEdgeVisibilityService.ts",
    "apps/editor/src/engine/initScene.ts"
  ]
}