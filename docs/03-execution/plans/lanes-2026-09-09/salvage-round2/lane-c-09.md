{
  "survivingTitles": [
    "0% hit rate on a fresh open is EXPECTED, not a defect — the cache is per-session in-memory and this was its first pass",
    "`yieldCalendar≈83ms` is COMPUTED, not measured — it is literally `yields × 16.67`",
    "`work=109ms` is a LOWER BOUND — it excludes hidden-line removal, every symbol injector, VG styling, and the caller's mesh export",
    "The plan projection during open is DELIBERATE (Contract 17 §4 auto-opens the split plan pane) — and the user's decision to close that pane is never persisted",
    "Doors and windows mint a fresh cache version on EVERY build via `Date.now()`, so they can never hit after a rebuild — walls solved this with a content hash and doors did not"
  ],
  "refutedTitles": [],
  "refutations": [
    "PARTIAL — Finding 1, the 'claimed cost: 0ms' is REFUTED (the title and the no-fix verdict stand). The cold pass is not free: the miss path stores `geo: projGeo.clone()` (EdgeProjectorService.ts:3233), so all 63 misses clone every projected BufferGeometry into the cache, and `_putCwCache` (2136) retains them for the whole session (LRU cap 5000; the only release is `clearCwProjectionCache` on project-switch). On a fresh open that is 410 edge geometries' worth of clone + retention for zero benefit to that pass. 'Expected, do not fix' is right; '0ms' is not.",
    "PARTIAL — Finding 1's evidence line 'hits+misses=63=entries, the signature of a cold first pass' is half tautology. `cacheableGroups` is DEFINED as `cacheHits + cacheMisses` (EdgeProjectorService.ts:3661), so that equality cannot fail and proves nothing. The genuinely dispositive number is `cacheEntries=63`: `_cwCacheEntryCount` is a WHOLE-CACHE counter across every element and every view, so `cacheEntries == cacheMisses` proves the map held nothing before this pass. Cite that, not the tautology.",
    "CONFIRMED and strengthened — Finding 1's 'nothing varies per run' claim survives a real attack. I checked the one term that could have carried a global: `computeClipSignature` folds `near`/`far` (2336-2347), and for a plan `resolveClipRange` (4404-4505) derives them from `level.elevation + resolvePlanViewRangeOffsets(viewRange, level.height)` — level store and viewDef only, never a scene bounding box. So adding or deleting an element cannot churn the signature for every other element. Note the history the auditor did not cite: `planBelowDepthOffset` USED to be a parameter and four rival callers passed different values, which is documented at 2266-2280 as the literal cause of a production 0% (L-11561). That defect is fixed, and its fix is why finding 1's determinism claim holds today.",
    "CONFIRMED — Finding 2 is exact. `yieldCalendar≈` at 3648 is `(yields) * (1000/60)`; both yield sites (3470-3480 per-layer, 3542-3548 per-chunk) accumulate `_workMsTotal += now - _lastYieldAt` BEFORE the await and reset `_lastYieldAt` AFTER the frame, so the interval across the await is discarded at both. The fix is ~4 lines of pure instrumentation and breaks no invariant — the only 'break' is the log field's meaning, which the auditor correctly says should be renamed so the break is loud.",
    "CONFIRMED and understated — Finding 3 is right and the exclusion is wider than claimed. `_workMsTotal = 0` / `_lastYieldAt = performance.now()` initialise at 2641-2643, i.e. at the TOP OF THE NATIVE GROUP LOOP, not at the top of `project()` (2258). So `work=` excludes the entire preamble too — `resolveClipRange`, `resolveSectionVolumeBox`, and the Source A `await this._buildModelIdMap(models)` / `await this._edgeProjector.get(...)` — as well as everything downstream. `applyOcclusion` at 4207 is confirmed inside the same method (project() runs 2258 to ~4230; next method at 4259) and therefore AFTER both log lines. HiddenLineRemoval.ts contains zero `performance.now()` calls — genuinely untimed. Pure instrumentation, nothing to break.",
    "PARTIAL — Finding 4's mechanism SURVIVES in full but its 'claimed cost: 192ms' is REFUTED by the auditor's own finding 2. 192 = 109 + 83, and finding 2's closing sentence is 'refuse to attribute 83ms of open time to yielding.' You cannot forbid that attribution on one line and bank it as a cost estimate three lines later. The defensible figure is '>109ms of loop CPU plus an unmeasured amount' — which is exactly what finding 3 says. Send the next lane with the range, not the number.",
    "CONFIRMED — Finding 4's causal chain is real end to end, and I also confirmed the fix works. initScene.ts:4636-4670 (auto-open, `ric(_activate, { timeout: 1500 })`, fallback `setTimeout(_activate, 600)`), SplitViewManager.activate() emits `split-view-activated` + `split-view-view-changed` (350-353), initScene 1522/1526-1529 convert both to `notifyViewActivated`, the predicate at 1508-1515 then reports the plan active. `deactivate()` (401-431) writes no preference; the only persisted split-view state is `pryzm.splitView.gridVisible` (2179/2187). Critically, with the pane closed the cost really does vanish rather than move: `ViewController._canMountDrawingForView` returns false immediately when `viewMode === '3D'` (540), so the plan stays deferred and never projects.",
    "CAUTION on Finding 4's second lever — the `{ timeout: 1500 }` is more load-bearing than the finding admits. Because the plan is INACTIVE during hydration, the ~503 element-create dirty marks are deferred and coalesce into ONE projection at activation. Firing earlier (the naive reading of 'move it off the open window') would let the plan go active mid-hydration and project repeatedly, each pass superseding the last — the exact `staleGen=2/3/4` storm documented at initScene 1441-1446. The auditor's recommended direction (a load-settled signal, i.e. LATER) is correct, but the reason is stronger than stated, and 'drop the timeout' should not be offered as an equal alternative.",
    "PARTIAL — Finding 5 survives but its primary citation is the wrong line. DoorBuilder.ts:431 is inside `_applyPropertyOnly` (412-434), not the build path. The unconditional per-build stamp is DoorBuilder.ts:599 — `version: Date.now()` inside the `rootUserData` literal in `rebuild()` (527+). Everything else checks out: 'door'/'window' really are in CACHEABLE_ELEMENT_TYPES (2008-2015) on exactly the 'strictly monotonic' justification quoted; WallFragmentBuilder `_versionForBuild` (392-445) is verbatim as described including the 'drove the plan-projection cache hit-rate to ~0 %' docstring; and WallRebuildCoordinator calls `doorBuilder.rebuildForWall(wallId)` per rebuilt wall (698, 1525, 2697), so an unrelated neighbouring wall edit genuinely re-versions untouched doors. Scope is also right: this costs EDITS, not the open.",
    "CORRECTION to Finding 5 — 'monotonicity is the wrong property' understates it: `Date.now()` is not strictly monotonic at all. It has 1 ms resolution and is wall-clock (NTP-adjustable, can move backwards), so two rebuilds of the SAME door inside one millisecond mint an EQUAL version, `_cwCacheIsValid` (2097) reports VALID, and the plan serves stale linework. The rAF-paced drain (`_drainBuildQueue`, one entry per door id per tick) makes that unlikely in practice, but the comment at 2010-2013 is the STATED BASIS for doors and windows being in the cacheable set, and it is false as written. Anyone reasoning from that comment is reasoning from a wrong premise."
  ],
  "missed": [
    "The cold pass has a real write cost the audit prices at zero. `geo: projGeo.clone()` (EdgeProjectorService.ts:3233) clones every projected BufferGeometry into `freshLayersCollector`, and `_putCwCache` (2136) stores it for the session. On the founder's open that is 63 misses covering 410 edge geometries, cloned and retained, with zero benefit to the pass that paid for it. It is almost certainly small — but it is the one number in this dimension the audit asserts is exactly 0, and it isn't.",
    "`_applyPropertyOnly` re-versions on a change it has just PROVEN is not geometric — a cleaner instance of finding 5 that the audit missed. DoorBuilder.ts:412-434 exists specifically to patch a door's finish colours WITHOUT a rebuild (`_isPropertyOnlyChange`, 348), and then stamps `version: Date.now()` anyway (431). That single line invalidates both the NME proxy cache (version propagated at NativeElementMeshExporter.ts:578/757) and the EPS projection cache, forcing a full re-projection of that door's linework for a colour change. Finding 5's version-hash argument has to reason about which inputs are geometric; here the code already answered that question and bumped regardless. Nothing needs the bump: the HIT path deliberately re-resolves pen weight and poché identity fresh every projection (comment at ~2788-2795) precisely because they are not geometry.",
    "Six groups per projection are permanently uncacheable and the log will not say which. The founder's line reads `groups=69 cwGroups=63`: six groups fall outside CACHEABLE_ELEMENT_TYPES and pay the full traverse + EdgesGeometry + toDrawingSpace pipeline on EVERY projection forever, not just the first. The log prints the gap but never names the families, so nobody can act on it — the same shape as the §ELEV-FURNITURE-IS-NOT-A-WIREFRAME line at 3680, which DOES name its families, sitting twenty lines away. Worse, the comment engineers actually read to decide whether a family is cached (2735-2745) lists 'openings' among the widened types, and `'opening'` is NOT in the set (2008-2058). One extra field on the existing log turns 'six unknown groups' into a targeted list, and it costs nothing.",
    "`work=` is not even 'the projector's loop time' — it starts inside the loop. `_workMsTotal = 0` and `_lastYieldAt = performance.now()` are at 2641-2643, so on top of everything finding 3 lists, the figure also excludes project()'s preamble: `resolveClipRange`, `resolveSectionVolumeBox`, and the Source A `await this._buildModelIdMap(models)` / `await this._edgeProjector.get(...)`. Cheap on this native-only model (no IFC), but it means the end-to-end `performance.now()` pair finding 3 recommends must wrap the method from 2258, not from the loop, or it will reproduce the same blind spot one level up.",
    "UNPROVEN COST, CONFIRMED ASYMMETRY — worth one grep before anyone edits this path. The driver that actually runs on the founder's open, initScene `_reprojectView`, calls `nativeElementMeshExporter.releaseGroups(nativeGroups, { disposeProxies: true })` ONLY in its catch (initScene.ts:1481). The other two drivers release on the success path as well — PlanViewManager 889/897/913, ViewController 701/719. Either the open-path driver skips a disposal the other two treat as mandatory, or the other two are doing needless work on every projection. I could not price it (both plain-mesh and instanced proxy geometries are stamped `sharedGeometry` at NativeElementMeshExporter.ts:648-650 and 693-694, so `_disposeProxyGroup` may dispose nothing in practice), so I am flagging the asymmetry rather than claiming a leak — but it sits on the open path, in a log that also shows a WebGPU device loss during open, and no comment explains the difference."
  ]
}