{
  "refuted": false,
  "reason": "I re-read every file:line cited and the quotes are real, verbatim, and in context. Nothing in the evidence chain is fabricated or misread.\n\nVERIFIED EXACT (text and line both match):\n- `packages/geometry-space-envelope/src/SpaceEnvelopeFaceDrag.ts:74` — the top/bottom ±Y, side `y:0` axis block is exactly as quoted.\n- `packages/geometry-space-envelope/src/SpaceEnvelopeGeometry.ts:145` — `spaceEnvelopeFaces` does return `[...sides, {kind:'top'}, {kind:'bottom'}]`.\n- `apps/editor/src/engine/spaceEnvelopeDragSurface.ts:602` — the `maximumBuildable` role refusal is at 602, inside `onPointerDown`, AFTER `pick()`/`getRecord` and BEFORE `active` is assigned. The claimed seam is real and correctly placed.\n- `packages/schemas/src/elements/SpaceEnvelope.ts:211,218` — `baseOffset: z.number().default(0)` and `height: z.number().positive().default(3)`, independent stored fields.\n- `apps/editor/src/ui/site/envelopeAuthoringPlan.ts:658` — `baseOffset: level.elevation,` / `height: heightM,` / `role: 'level',`.\n- `packages/command-registry/src/levels/SetLevelHeightCommand.ts:150` — class declaration on 150.\n- `apps/editor/src/engine/initBusHandlers.ts:2355` — the `level.update` → `_cmExec(new UpdateLevelCommand(...))` bridge, verbatim.\n- `apps/editor/src/ui/analysis/parcelLawEnvelopeAuthoring.ts:418` and `:646` — both verbatim.\n- `plugins/space-envelope/src/handlers/MutateSpaceEnvelope.ts:411` (`containmentRefusalFor` in setParameter), `:307` and `:323` (`containmentOutcomeFor` in setFootprint canExecute/execute) — all three land on the exact lines claimed. The asymmetry the author calls \"THE ONE REAL DESIGN DECISION\" is real.\n- `packages/geometry-space-envelope/src/SpaceEnvelopeContext.ts:346` — `planSpaceEnvelopeFaceMove({..., face:{kind:'top'}, ...})` inside `adaptRoomToMovedLevel`, and that function is genuinely production-wired (`plugins/space-envelope/src/handlers/containmentGate.ts:169`). The ⛔ \"don't gate in the planner\" warning is well-founded, not decorative.\n\nINDEPENDENT CHECKS I RAN THAT CONFIRM RATHER THAN REFUTE:\n- `grep -rni \"spaceenvelope\" packages/command-registry/src/` → **0 hits**. The claimed total omission is total.\n- Production `SetLevelHeightCommand` callers → exactly two: `LevelManagerPanel.ts:196` and `PropertyPanel.ts:1277`, both through commandManager. `grep \"level.setHeight\"` repo-wide → **0**. No bus verb exists.\n- `grep allowedFaces|lockVertical|faceFilter|canDragFace|isFaceDraggable|omitCapFaces|capFaces` → 0 relevant hits. I went further than the author: `spaceEnvelopeFaceHandle` (SpaceEnvelopeFaceGizmo.ts:210) has no cap branch, `spaceEnvelopeFaceExtentM` explicitly computes a cap extent from the ring bbox, and both `_handlesOf` (SpaceEnvelopeFaceGizmoBuilder.ts:229) and `siteEnvelopeDrawCesium.ts:892` call `spaceEnvelopeFaceHandles(prism)` with no options. `pickSpaceEnvelopeFace` (SpaceEnvelopeFacePick.ts:206-215) has an explicit TOP AND BOTTOM ray-plane block. Caps are drawn, picked and moved end-to-end today. Falsification test (2) does not fire.\n- `CesiumViewport.ts:8869-8891` — `const height = preview?.height ?? rec?.height;` / `const baseOffset = preview?.baseOffset ?? rec?.baseOffset;` then `bottom = baseHeight + baseOffset`. Record-only. The rival (\"renderer re-derives from the level\") is genuinely ruled out.\n- Falsification test (1): I ran the broader search. `grep -rn \"bim-level-updated\"` production consumers are `geometryMutationEvents.ts:103` and `packages/geometry-slab/src/floor/FloorSlabBindingHandler.ts:45` (which has **0** spaceEnvelope references). Only production `spaceEnvelope.setParameter` references are the handler, its barrel, a sync disposition row and a chat string — no subscriber. No fourth consumer exists.\n- Falsification test (3), which the author flagged as their own gap, resolves in their favour: `SetLevelHeightCommand` writes through `bimManager.updateLevel` (`:291`, `:301`) and reads `bimManager.getLevelById`, i.e. `BimKernel.updateLevel` (core-app-model/src/BimKernel.ts:373) — the same object the card's `window.bimManager.getLevels()` reads. One authority; seam A is not a store-reconciliation problem.\n\nDISCREPANCIES FOUND — all cosmetic line offsets, none change meaning:\n- `SpaceEnvelopeTypes.ts` cited as :26; the union actually begins at **:22**.\n- `CesiumViewport.ts` cited as :8891 for a two-line quote that starts at **:8890**.\n- `commands.ts:975` (fix instruction) — `'level.add'` is at **:977**.\n- `parcelLawEnvelopeAuthoring.ts:982` (fix instruction) — `storeyRows` is defined at **:986**.\n- The falsification section predicted the `bim-level-updated` consumers would be `geometryMutationEvents.ts:103` and `roofWallClashAnnouncer.ts`. `roofWallClashAnnouncer` does **not** consume that event, and `FloorSlabBindingHandler.ts:45` (which does) was missed. The prediction was wrong; the conclusion it was testing survives intact.\n\nThe mechanism as stated — seeded once at create, never re-derived, and no face-kind gate anywhere — is confirmed by the code.",
  "evidenceChecked": [
    "packages/geometry-space-envelope/src/SpaceEnvelopeTypes.ts:22 — union confirmed verbatim; cited as :26 (off by 4)",
    "packages/geometry-space-envelope/src/SpaceEnvelopeFaceDrag.ts:74 — spaceEnvelopeFaceAxis top +Y / bottom -Y / side y:0, exact",
    "packages/geometry-space-envelope/src/SpaceEnvelopeGeometry.ts:145 — spaceEnvelopeFaces returns n sides + top + bottom, exact",
    "packages/geometry-space-envelope/src/SpaceEnvelopeFaceGizmo.ts:147,210,273 — SpaceEnvelopeFaceHandleOptions has 4 fields, no cap option; spaceEnvelopeFaceHandle has no cap branch; loop iterates spaceEnvelopeFaces()",
    "packages/geometry-space-envelope/src/SpaceEnvelopeFaceGizmo.ts:168-199 — spaceEnvelopeFaceExtentM explicitly computes a cap extent from the ring bbox, so caps DO get handles",
    "packages/geometry-space-envelope/src/SpaceEnvelopeFacePick.ts:133-215 — pickSpaceEnvelopeFace has an explicit TOP AND BOTTOM ray-plane block; caps are pickable",
    "packages/geometry-space-envelope/src/SpaceEnvelopeFaceMove.ts:215-245 — planSpaceEnvelopeFaceMove implements the cap branch (newHeight/newBase), exact",
    "packages/geometry-space-envelope/src/SpaceEnvelopeContext.ts:346,357 — adaptRoomToMovedLevel drives {kind:'top'} and {kind:'bottom'} through the planner, exact",
    "plugins/space-envelope/src/handlers/containmentGate.ts:169 — adaptRoomToMovedLevel is production-wired, so the planner-gate warning is well-founded",
    "apps/editor/src/engine/spaceEnvelopeDragSurface.ts:602 — role refusal verbatim, in onPointerDown, after pick, before `active` is set",
    "apps/editor/src/engine/spaceEnvelopeDragSurface.ts:562-574,586,667,903 — pick() wrapper has exactly three consumers as claimed",
    "apps/editor/src/engine/spaceEnvelopeDragSurfaceThree.ts:112 — THREE pickFace returns whatever face userData carries, incl. caps",
    "apps/editor/src/ui/site/siteEnvelopeDrawCesium.ts:735,892 — Cesium pickFace via pickNearestSpaceEnvelopeFace; spaceEnvelopeFaceHandles called with no options",
    "apps/editor/src/engine/SpaceEnvelopeFaceGizmoBuilder.ts:229 — spaceEnvelopeFaceHandles(prism), no filter",
    "packages/schemas/src/elements/SpaceEnvelope.ts:211,218 — baseOffset and height are independent stored fields, exact lines",
    "apps/editor/src/ui/site/envelopeAuthoringPlan.ts:658 — baseOffset: level.elevation / height: heightM / role:'level', exact line",
    "apps/editor/src/ui/site/envelopeAuthoringPlan.ts:190-205 — the four-consumer 'baseOffset is ABSOLUTE' note is real",
    "apps/editor/src/ui/site/adoptProposalAsEnvelope.ts:161-170 — resolveStoreyHeight prefers level.height ('level-record'), as claimed",
    "packages/command-registry/src/levels/SetLevelHeightCommand.ts:150 — class line exact",
    "packages/command-registry/src/levels/SetLevelHeightCommand.ts:10-40 — header does say UpdateLevelCommand({height}) 'writes the number and fires NOTHING'",
    "packages/command-registry/src/levels/SetLevelHeightCommand.ts:291-331 — cascade via bimManager.updateLevel; bim-level-updated emitted ONCE for the edited level only",
    "packages/command-registry/src/levels/UpdateLevelCommand.ts:88-90,115 — DOES emit bim-level-updated and update-project-ui (nuance the header's 'fires NOTHING' does not cover)",
    "grep -rni spaceenvelope packages/command-registry/src/ → 0 hits (claim of total omission confirmed)",
    "grep SetLevelHeightCommand production callers → only LevelManagerPanel.ts:196 and PropertyPanel.ts:1277, both via commandManager",
    "grep 'level.setHeight' repo-wide → 0 hits; no bus verb exists",
    "apps/editor/src/engine/initBusHandlers.ts:2353-2358 — level.update → _cmExec(UpdateLevelCommand), verbatim",
    "apps/editor/src/engine/initBusHandlers.ts:664 — _cmExecOrRefuse exists and surfaces the refusal, as the fix assumes",
    "apps/editor/src/ui/analysis/parcelLawEnvelopeAuthoring.ts:418 — readLevels via window.bimManager.getLevels(), verbatim",
    "apps/editor/src/ui/analysis/parcelLawEnvelopeAuthoring.ts:646 — the 'storey height comes from this project's storeys' prose, verbatim",
    "apps/editor/src/ui/analysis/parcelLawEnvelopeAuthoring.ts:746,929,986,1455-1476 — heading, readLevelCandidates, storeyRows (at 986 not 982), awaited bus dispatch loop",
    "plugins/space-envelope/src/handlers/MutateSpaceEnvelope.ts:307,323,411 — containmentOutcomeFor vs containmentRefusalFor asymmetry confirmed at the exact cited lines",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:8869-8891 — height/baseOffset come from preview ?? rec, never a level; rival ruled out",
    "packages/core-app-model/src/BimKernel.ts:373 — bimManager.updateLevel is BimKernel's, the same authority the card reads (falsification test 3 resolves in the author's favour)",
    "grep -rn bim-level-updated → production consumers are geometryMutationEvents.ts:103 and geometry-slab/FloorSlabBindingHandler.ts:45 (0 spaceEnvelope refs); NOT roofWallClashAnnouncer as predicted",
    "grep 'spaceEnvelope.setParameter' production → handler, barrel, sync disposition, chat string only; no subscriber exists",
    "grep allowedFaces|lockVertical|faceFilter|canDragFace|isFaceDraggable|omitCapFaces|capFaces → 0 relevant hits repo-wide"
  ],
  "betterHypothesis": "The root cause survives unchanged. Two concrete defects in the PROPOSED FIX are worth correcting before anyone builds it, both found by reading past the cited lines:\n\n1. **Fix C subscribes to the wrong signal, and it under-fires AND over-fires.** `SetLevelHeightCommand` emits `bim-level-updated` exactly ONCE, with `{ id: levelId }` for the EDITED level only (`:327-328`), even though its whole purpose is the δ-cascade that translates every level ABOVE (`this._movedLevels`, re-seated at `:301`). A subscriber that re-seats \"every `role:'level'` envelope whose `levelId` is in the affected set\" cannot recover that set from this event — envelopes on the storeys above would stay at their old `baseOffset` and be exactly as stale as today, which is the bug the fix is for. Conversely, `UpdateLevelCommand` ALSO emits `bim-level-updated` (`:90`), so a plain `level.update({height})` — the path the plan's section D tells you not to use — would fire the follow subscriber and move the envelopes while the level stack above does not move: a NEW divergence, in the opposite direction. The consequence must key on something that names the moved set (a new event carrying `movedLevelIds`, or the command's own step 4), not on `bim-level-updated`.\n\n2. **The author's own \"fires NOTHING\" quote is narrower than they use it.** The header sentence is scoped to `spatial-authority-reconcile` / geometry cascade; `UpdateLevelCommand` does emit `update-project-ui`, `bim-level-updated` and `ai-model-update`. The conclusion (\"routing height through `level.update` cascades nothing\") is still correct, but \"silent no-op\" overstates it and is what produces defect (1) above.\n\nNeither touches the diagnosis: `baseOffset`/`height` really are seeded once at `envelopeAuthoringPlan.ts:658` and re-derived by nothing, and there really is no face-kind gate anywhere between `spaceEnvelopeFaces()`, both `pickFace` implementations, and `planSpaceEnvelopeFaceMove`."
}