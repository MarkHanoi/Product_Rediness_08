{
  "refuted": false,
  "reason": "EVERY load-bearing citation verified in the code. The mechanism is real.\n\n(b) DERIVATION GAP — CONFIRMED. `grep -rln \"SpaceEnvelope\"` and `\"spaceEnvelope\"` over `packages/command-registry/src/` both return RC=1 (zero files). `envelopeAuthoringPlan.ts:657-659` (`baseOffset: level.elevation`, `height: heightM` from `resolveStoreyHeight`, adoptProposalAsEnvelope.ts:161-186) is the only level→envelope coupling, and it fires once at create. No production dispatch of `spaceEnvelope.setParameter` exists anywhere (repo-wide grep: test files only; the only production envelope verbs are `batch.create`, `setFootprint`, `moveFace`, `delete`). `CesiumViewport.ts:8890-8891` reads `rec.height`/`rec.baseOffset`, never the level. Editing a storey height today does leave every envelope on it silently stale.\n\n(a) AUTHORITY + MISSING BUS SEAM — CONFIRMED. `SetLevelHeightCommand`'s header table omits SpaceEnvelope. It is reachable only via `window.commandManager` (LevelManagerPanel.ts:196, PropertyPanel.ts:1277). The only level bus verbs are `level.add` and `level.update` (initBusHandlers.ts:2353-2372); `level.update` → `UpdateLevelCommand`, which writes `height` and fires no reconcile (`BimKernel.updateLevel` dispatches only on elevation).\n\nTHE AUTHOR'S OWN FALSIFICATION #3 FAILS — I ran the check he did not. `initTools.ts:731-732` builds `commandContext = { bimManager, … }` from the SAME `bimManager` created at `initScene.ts:878` and assigned to `window.bimManager` at `:882`; `_cmExec`/`_cmExecOrRefuse` (initBusHandlers.ts:620,666) route through `window.commandManager`, constructed with that context. One object, one authority. Seam A stands; this is not a store-reconciliation problem.\n\nFALSIFICATION #1 ALSO FAILS. `bim-level-updated` has exactly two consumers: `geometryMutationEvents.ts:103` (a NON-caster exclusion row, not a subscriber) and `packages/geometry-slab/src/floor/FloorSlabBindingHandler.ts:45`. Neither touches envelopes. (His predicted `roofWallClashAnnouncer.ts` is not a consumer — harmless miss.)\n\n(c) NO FACE GATE — CONFIRMED. The closed union is at SpaceEnvelopeTypes.ts:23-26; `spaceEnvelopeFaces()` returns all n+2 (Geometry.ts:145-151); `SpaceEnvelopeFaceHandleOptions` (FaceGizmo.ts ~146-151) has no cap filter and `spaceEnvelopeFaceHandles` iterates unconditionally; both call sites pass no options (`SpaceEnvelopeFaceGizmoBuilder._handlesOf` :229, `siteEnvelopeDrawCesium` :892). Both live `pickFace` impls return caps (`spaceEnvelopeDragSurfaceThree.ts:112` via mesh userData, `siteEnvelopeDrawCesium.ts:735` via `pickNearestSpaceEnvelopeFace`). The five-name grep finds nothing. C114 line 744 independently confirms the cap drag ships and works (\"dragging the TOP face does not make the walls TALLER\" — the height change itself lands). The \"⛔ do not gate in the planner\" caveat is correct and load-bearing: `SpaceEnvelopeContext.ts:346,357` drives cap moves through `planSpaceEnvelopeFaceMove` for room adaptation.",
  "evidenceChecked": [
    "packages/geometry-space-envelope/src/SpaceEnvelopeTypes.ts:23-26 — closed face union confirmed verbatim",
    "packages/geometry-space-envelope/src/SpaceEnvelopeFaceDrag.ts:74-78 — cap axes ±Y, side y:0, confirmed",
    "packages/geometry-space-envelope/src/SpaceEnvelopeGeometry.ts:144-151 — spaceEnvelopeFaces returns n+2 incl. both caps",
    "packages/geometry-space-envelope/src/SpaceEnvelopeFaceGizmo.ts:145-151,255-278 — SpaceEnvelopeFaceHandleOptions has NO cap filter; handles loop is unconditional",
    "packages/geometry-space-envelope/src/SpaceEnvelopeFacePick.ts:154,197,207-208 — ray pick returns top+bottom; :258-318 plan pick is side-only BY DESIGN",
    "packages/geometry-space-envelope/src/SpaceEnvelopeContext.ts:280-365 — adaptRoomToMovedLevel drives {kind:'top'}/{kind:'bottom'} through the planner; the vertical legs only SHRINK (move a cap inward), never translate",
    "packages/geometry-space-envelope/src/SpaceEnvelopeRelations.ts:101-131 — assessSpaceEnvelopeContainment checks vertical excursion in BOTH directions",
    "apps/editor/src/engine/spaceEnvelopeDragSurface.ts:560-573 (the ONE pick), :583-650 (onPointerDown, role refusal at :601-607), :655-700 (hover), :886-907 (dblclick) — seam confirmed; no cap gate",
    "apps/editor/src/engine/spaceEnvelopeDragSurfaceThree.ts:112-135 — pickFace returns whatever userData face the raycast hits, caps included",
    "apps/editor/src/ui/site/siteEnvelopeDrawCesium.ts:735-757 (pickFace), :892 (spaceEnvelopeFaceHandles with no options)",
    "apps/editor/src/engine/SpaceEnvelopeFaceGizmoBuilder.ts:221-231 (_handlesOf), :183-193 (setActiveFace degrades safely if a cap has no node)",
    "apps/editor/src/ui/layout/GISAreaLayout.ts:1336,1383-1387 — onRefusal IS wired on the 3-D Site surface (pryzm:toast, severity warning)",
    "packages/command-registry/src/ — grep 'SpaceEnvelope' RC=1 and 'spaceEnvelope' RC=1: ZERO references, confirmed",
    "packages/command-registry/src/levels/SetLevelHeightCommand.ts:1-93 (header table, SpaceEnvelope absent), :243-360 (execute; step 4 emits bim-level-updated with ONLY {id: levelId}), :383-410 (undo ALSO emits bim-level-updated)",
    "packages/command-registry/src/levels/UpdateLevelCommand.ts:88-91,113-115 — emits bim-level-updated on execute AND undo, for ANY level field",
    "apps/editor/src/engine/initBusHandlers.ts:2352-2372 (only level.add / level.update), :620-635 (_cmExec), :666-703 (_cmExecOrRefuse)",
    "apps/editor/src/engine/initTools.ts:731-733,763,3690-3693 — commandContext.bimManager IS the initScene.ts:878/882 instance behind window.bimManager. The two-authority hypothesis is dead.",
    "apps/editor/src/ui/site/envelopeAuthoringPlan.ts:182-215 (the absolute-baseOffset ruling + L-13147), :633-661 (the one create-time coupling)",
    "apps/editor/src/ui/site/adoptProposalAsEnvelope.ts:161-190 — resolveStoreyHeight ladder, rung 1 = level.height",
    "apps/editor/src/ui/analysis/parcelLawEnvelopeAuthoring.ts:417-419 (readLevels = window.bimManager.getLevels), :646 (shipped prose), :986-995 (storeyRows), :1455-1477 (addLevelsBtn awaits bus one at a time)",
    "apps/editor/src/ui/geospatial/CesiumViewport.ts:8890-8891 — bottom = baseHeight + baseOffset, from the record only",
    "plugins/space-envelope/src/handlers/MutateSpaceEnvelope.ts:286-347 (setFootprint uses containmentOutcomeFor AND writes adaptedRooms in the same patch pair) vs :404-437 (setParameter uses containmentRefusalFor in BOTH canExecute and execute, writes only the subject)",
    "plugins/space-envelope/src/handlers/containmentGate.ts — the ⛔ block at the head of §25.6 states the exact invariant the proposed fix would breach, and containmentOutcomeFor's own doc",
    "docs/02-decisions/contracts/C114-ELEMENT-SPACE-ENVELOPE.md:744 — 'dragging the TOP face does not make the walls taller (L-13118 — the verb exists, the event field does not)'",
    "docs/04-reference/ISSUE-LOG.md L-13118 — OPEN, with a named exit that requirement 2 would delete; L-13151 — the founder's [3.0,3.0,2.6,15.3,3.0] report, the create-time half of this same family, already FIXED",
    "apps/editor/src/engine/spaceEnvelopeWallFollowPlan.ts:181,318-322,630,938 + spaceEnvelopeWallFollow.ts:312,333-337 — a live cap-move branch ('ring-unchanged' logged, bottom-drag TOASTED) that requirement 2 would orphan"
  ],
  "betterHypothesis": "The root cause needs no revision. The FIX does — four defects, ranked. Requirement 2's gizmo+gate half is sound and shippable roughly as written (the four files and the `onPointerDown` seam are all correct, `onRefusal` reaches a toast on the 3-D Site, and `setActiveFace` degrades safely when a cap has no node). Requirement 1's seam C is where this would ship a confidently wrong answer.\n\n1 · THE INVARIANT THE CODE IS PROTECTING, AND THE FIX BREACHES IT BY OMISSION. `containmentGate.ts` states it in its own header: \"That function is called by SIX verbs, and only some of them WRITE the rooms they would adapt. Loosening it centrally would make `setParameter`, `move` and `batch.create` stop refusing a level that strands a room while still writing only the level — leaving the room outside its storey with nothing anywhere saying so, which is strictly worse than the refusal it replaced. So the strict gate keeps its callers, and a verb that is prepared to WRITE the adaptation opts into this one.\" The rule is not \"use containmentOutcomeFor\"; it is \"you may use it IFF your execute writes the adapted rooms.\" `setFootprint.execute` (:320-347) does exactly that — `withMetrics` per adapted room, all in ONE `produceCommand`. The fix says only \"must use `containmentOutcomeFor` … rather than `containmentRefusalFor`\" and never states the write obligation. Implemented literally, it produces the documented catastrophic outcome verbatim. Restate seam C as: setParameter must adopt BOTH halves of setFootprint's shape — gate and commit ask the same question (C84 EI-9.2), and the adapted rooms land in the same patch pair.\n\n2 · `adaptRoomToMovedLevel` SHRINKS ROOMS; IT DOES NOT TRANSLATE THEM. This is the sharpest hazard and neither the claim nor the fix mentions it. SetLevelHeightCommand moves every level ABOVE the edited one by δ (elevation only — each keeps its own height). Seam C re-seats those envelopes' `baseOffset := level.elevation`, so the level prism translates up by δ with its height unchanged. Now run SpaceEnvelopeContext.ts:355-364 on a room inside it: `below = level.baseY − room.baseY = δ > 0` fires a `{kind:'bottom'}` move of −δ, which raises the room's FLOOR and leaves its ceiling where it was; `above = room.topY − level.topY` is now negative, so nothing raises the top. Net: every room on every storey above the edit gets δ shorter and sits wrong inside its storey — silently, in the same undo entry, with the containment check reporting `contained`. A rigid translation of a level demands a rigid translation of its rooms, and the adapter in this package cannot express one. Either add a translate path or have seam C move room `baseOffset` itself, and pin it with a test that asserts room HEIGHT is invariant under a pure level translation.\n\n3 · `bim-level-updated` IS THE WRONG TRIGGER, THREE WAYS. (a) It carries `{ id: levelId }` — the EDITED level only. SetLevelHeightCommand:328 names none of the levels it translated, so the \"affected set\" seam C wants to iterate does not exist in the payload; the subscriber must re-derive from the whole level list. (b) `UpdateLevelCommand:90` emits the same event for ANY level edit — name, colour, visibility — so a rename would re-dispatch N envelope writes unless seam C delta-guards on the actual numbers. (c) DECISIVE: it is emitted on UNDO (SetLevelHeightCommand:404, UpdateLevelCommand:115). Undoing a height edit would wake the subscriber and push N NEW FORWARD `spaceEnvelope.setParameter` entries onto the bus stack — the stack grows during an undo. The §ENVELOPE-DRAG-CONSEQUENCE precedent the fix leans on does NOT transfer: that consequence runs on the pointer-UP handler of a gesture, and nothing re-fires a pointer-up on undo. Trigger seam C from the `level.setHeight` bridge itself (where you know both the δ and the moved set, and where undo does not re-enter), not from the DOM event.\n\n4 · REQUIREMENT 2 ORPHANS LIVE MACHINERY AND SILENTLY CLOSES AN OPEN ROW. `spaceEnvelopeWallFollowPlan.ts` carries a whole cap-move branch — `ring-unchanged` (logged, deliberately not toasted, :630/:938) plus a separately TOASTED bottom-drag refusal (:181, :318-322) — and L-13118 is OPEN in ISSUE-LOG with a named, costed exit (\"what is missing is the EVENT FIELD, not the verb: add `height`/`baseOffset` to `SpaceEnvelopeFaceMoveCommitted`\"). Forbidding cap drags makes all of it unreachable. That may be precisely the founder's intent, but the fix must retire L-13118 in the same commit and say why, or a later lane builds the height leg for a gesture that can no longer fire — the [[authored-but-unwired]] shape, minted deliberately.\n\nTwo smaller notes. (i) On Cesium, `pickNearestSpaceEnvelopeFace` returns `top` for any ray hitting the roof, which from a near-overhead camera is the largest target on the envelope; every such click would now raise a warning toast, and because `onDoubleClick` runs IN ADDITION to `onPointerDown`, a double-click to open the outline editor raises the cap refusal twice. Prefer log-only, or once per gesture. (ii) The `baseOffset` datum is a genuine live contradiction — `packages/schemas/src/elements/SpaceEnvelope.ts:211` says \"above the owning level's datum\" while `envelopeAuthoringPlan.ts:182-200` documents four rasterisers treating it as absolute above the project datum, filed as L-13147. Seam C writing `baseOffset := level.elevation` follows the code, which is right; do not let anyone \"fix\" the prose half and the code half in different commits."
}