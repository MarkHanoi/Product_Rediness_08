{
  "dimension": "Architecture of the documentation pipeline (plan / elevation / section) — is it sound?",
  "summary": "MEASURED AT HEAD 9babe6f5 (two sibling lanes — 584aaab2 lighting layer, 9babe6f5 line-type UI — landed while I read; my line numbers are at that HEAD).\n\nTHE PIPELINE, AND WHAT EACH STAGE IS ALLOWED TO DECIDE.\n(1) `elementRegistry` → `NativeElementMeshExporter.exportForView()` decides WHICH ELEMENTS enter (level filter / cropRegion / elevation scopeFrame) and caches proxy descriptors on `elementId:viewId:version:cropKey` (NativeElementMeshExporter.ts:499-532). (2) `EdgeProjectorService.project()` decides WHICH MESHES emit edges — `_symbolGate(family)` intent veto (:2725) and `mesh.userData.skipInPlan` (:2914) — and WHICH ISO LAYER they land on (`ELEMENT_TYPE_TO_PROJECTION_LAYER`, :203-266). (3) Fifteen symbol injectors add authored 2D linework, each gated at the CALL SITE by the same `_symbolGate` (:3969-4130). (4) `applyOcclusion()` runs LAST, after injection (:4235), and decides what is deleted or demoted to `:hidden`. (5) `VGSceneApplicator.applyToProjectionLayers()` writes layer colour/visibility/linewidth onto the drawing (:624). (6) `PlanViewCanvas.render()` re-derives visibility and pen per line and paints (:431-630).\n\n(b) THE SUSPECTED ROOT — PARTLY CONFIRMED, AND THE TIDY VERSION IS WRONG. \"Symbols inject after the filter\" is REFUTED: `makeSymbolInjectionGate` (SymbolInjectionGate.ts:112) is wired and every one of the fifteen injectors is gated before it runs. What IS true, and is the real root, is three-fold and all of it is measurable: (i) FIVE hand-typed family vocabularies must agree and do not — `VGSceneApplicator.CATEGORY_TO_DXF_LAYER` (14), `ISO_LAYER_TO_VG_CATEGORY` (16 rows/14 keys), `penCategoryForLayerTag` (12 arms), `VisibilityIntentDefaults.ELEMENT_TYPES` (19), `OverridePanel.CATEGORIES` (13) — at least three ids are dead on arrival; (ii) THREE stores are asked \"is this visible\" by different consumers — `vgGovernanceStore` (canvas skip), `visibilityIntentStore.elementRules` (pen alpha), `viewIntentInstanceStore.localOverrides` (pen alpha + injection gate) — and one of them, the one the founder's panel edits, is READ-ONLY in the default project; (iii) THREE painters draw on the plan canvas WITHOUT going through the drawing, the layers, the pen table or any of the three stores — room fills, lighting symbols, site context. So \"rooms have no category\" and \"furniture still renders\" are not one defect but two adjacent ones with a shared cause: THERE IS NO SINGLE OBJECT THAT ANSWERS \"what does this view show, and how\".\n\n(c) The incremental path is structurally unavailable for a documentation pass: `PLAN_INCREMENTAL_SAFE_TYPES = {wall, slab, beam, ceiling, floor}` (ViewDependencyTracker.ts:150). Every gesture in a doc pass — move a door, place furniture, add lighting, delete anything, any batch — marks the view COARSE, empties `graftElementIds`, and takes the O(N) full pass (initScene.ts:1318). `hitRate=0%` in his log is a COLD exporter cache for that viewId, not necessarily a per-pass reset; I could not establish persistence from one sample.\n\n(e) THE ONE STRUCTURAL CHANGE: promote `makeSymbolInjectionGate` into a per-view `ViewGraphicsResolver` — built once per projection AND once per paint — that answers `(family, zone, elementId) → {visible, pen, fill}` over ONE derived family vocabulary, and make all six painters plus the hit test and the SVG/DXF exporter ask it. The seam already exists and is already named \"THE ONE PLACE A SYMBOL INJECTOR ASKS 'IS THIS FAMILY VISIBLE IN THIS VIEW?'\".",
  "findings": [
    {
      "title": "Every view in a fresh project is governed by a READ-ONLY system intent, and the intent editor silently wires no write handlers",
      "mechanism": "All five shipped intents are minted with `isSystem: true` (SystemIntents.ts:48, inside the shared `makeIntent` factory), and `getDefaultSystemIntentId()` returns `architecturalDocumentation` — the founder's \"Architectural Documentation (Auto)\", bound to his 13 views. `VisibilityIntentPanel.bind()` returns at line 772 (`if (intent.isSystem) return;`) BEFORE attaching a single `[data-appearance]` change listener, the line-weight slider, the mass-apply buttons or `save-meta`. The panel's own header states it: line 72 — *\"all five SHIPPED intents being `isSystem` (and therefore hard read-only — `bind()` returns before wiring a single write handler)\"*. So every element-rule edit — line style, weight, colour, and the per-state `visible` checkbox that would hide furniture — is inert until the user presses Duplicate AND re-binds the copy. Duplicating alone is not enough: L-779's own note records that the copy governed nothing because binding lives in a different surface.",
      "evidence": [
        "packages/core-app-model/src/presentation/SystemIntents.ts:48 — `isSystem: true,` (in `makeIntent`, applied to all five entries of SYSTEM_VISIBILITY_INTENTS)",
        "packages/core-app-model/src/presentation/SystemIntents.ts:276 — `name: 'Architectural Documentation (Auto)',`",
        "packages/core-app-model/src/presentation/SystemIntents.ts:350 — `export function getDefaultSystemIntentId(): string { return SYSTEM_INTENT_IDS.architecturalDocumentation; }`",
        "apps/editor/src/ui/VisibilityIntentPanel.ts:772 — `if (intent.isSystem) return;` — followed by every write listener in the file (save-meta :773, `[data-appearance]` :778, sliders :784, mass-apply :798)",
        "apps/editor/src/ui/VisibilityIntentPanel.ts:72 — the file's own header: \"all five SHIPPED intents being `isSystem` (and therefore hard read-only — `bind()` returns before wiring a single write handler)\"",
        "apps/editor/__tests__/visibilityIntentGovernsSymbolInjectors.test.ts:230 — the HIDE arm proves hiding works by writing `elementRules[cat] = rule({visible:false})` directly into the store, i.e. it exercises the path the UI cannot reach for a system intent"
      ],
      "userVisibleSymptom": "\"I change the line type etc.. doesn't work.\" Every control in the Visibility Intent panel appears, accepts a click, and changes nothing — because the intent governing all 13 views is read-only and the handlers were never attached.",
      "fix": "Two options, both small. (a) Copy-on-write: when a write is attempted against a system intent, silently `duplicateIntent()` AND `vg.assignIntent` the copy to `contextViewId` in the same gesture, then apply the edit — the L-779 machinery for this is already in the file. (b) If (a) is judged too magical, make the read-only state impossible to miss: the banner exists (:205-207) but the disabled inputs still look editable; gate them behind an explicit \"Edit a copy\" affordance so no click is ever a no-op. C102 V-RE-1 already requires exactly this shape (\"a control for an unreachable capability MUST render disabled with the reason named — it MUST NOT render enabled and no-op\").",
      "effort": "small",
      "risk": "low",
      "confidence": "high",
      "alreadyExists": "`duplicateIntent()` (VisibilityIntentPanel.ts:~905), `contextViewId` (:86, added by L-779 precisely to close this loop), `AssignViewIntentCommand` + bus verb `vg.assignIntent` (initBusHandlers.ts:2526), and the read-only banner + Duplicate button (:201-207). Nothing needs building.",
      "filesToChange": [
        "apps/editor/src/ui/VisibilityIntentPanel.ts"
      ]
    },
    {
      "title": "FIVE hand-typed family vocabularies must agree; at least three ids are dead by construction, and nothing checks",
      "mechanism": "The same question — \"what family is this?\" — is answered by five independently maintained literal lists. `VGSceneApplicator.CATEGORY_TO_DXF_LAYER` (14 rows) drives the drawing-layer write. `ISO_LAYER_TO_VG_CATEGORY` (16 rows) drives the canvas's VG lookup. `penCategoryForLayerTag` (12 regex arms) drives the pen. `VisibilityIntentDefaults.ELEMENT_TYPES` (19 keys) drives the intent panel's row list AND `IntentRuleResolver.rulesFor()`. `OverridePanel.CATEGORIES` (13 rows) drives the per-view hide toggles. Measured mismatches: (1) OverridePanel offers `railing`, but `Handrail`/`HandrailPart` map to `A-STRS` → `vgCategoryForLayer` returns `stair` and `penCategoryForLayerTag` returns `stair`; `targetMatches('elementType','railing',{elementType:'stair'})` is false — the toggle can never match anything. (2) OverridePanel offers `curtainwall`, but `CurtainWall: 'A-WALL'` → family `wall`; same dead result, and unticking \"Walls\" silently takes the curtain wall with it. (3) `penCategoryForLayerTag` has no arm for plumbing, grid, level or `A-AREA`/spaceEnvelope, so for an `A-PLMB` line the VG resolver says `plumbing` and the pen resolver says `projection` — two authorities disagreeing about what one line IS, inside the same 40-line block of `render()`. There is no ga-gate and no test cross-checking the five sets (`ls tools/ga-gate/ | grep -i categor` → nothing).",
      "evidence": [
        "packages/core-app-model/src/presentation/VGSceneApplicator.ts:87-102 — CATEGORY_TO_DXF_LAYER, 14 rows, no room/lighting/boundary-line/grid/level",
        "packages/core-app-model/src/drawing/DrawingLayerIdentity.ts:96-133 — ISO_LAYER_TO_VG_CATEGORY, includes `'A-LGHT':'lighting'` and `'A-AREA':'spaceEnvelope'` which exist in no other list",
        "packages/core-app-model/src/drawing/PenWeightTable.ts:449-470 — `penCategoryForLayerTag`: arms for wall/door/slab/col/stair/roof/ceiling/furniture/lighting/handrail/window/boundary-line only; NO plumbing, grid, level, spaceEnvelope → falls to `return 'projection'` (:417)",
        "packages/core-app-model/src/presentation/VisibilityIntentDefaults.ts:15-47 — ELEMENT_TYPES: has `structural`, `annotation`, `ifc-element`, `lighting`; has NO `room`, `handrail`, `curtain-wall`, `opening`, `spaceEnvelope`",
        "apps/editor/src/ui/OverridePanel.ts:255-256 — `{ id: 'curtainwall', label: 'Curtain walls' }, { id: 'railing', label: 'Railings' }`",
        "apps/editor/src/engine/views/EdgeProjectorService.ts:207 — `CurtainWall: 'A-WALL',` ; :223 — `Handrail: 'A-STRS',`",
        "packages/core-app-model/src/presentation/IntentRuleResolver.ts:78-82 — `targetMatches`: `if (kind === 'elementType') return target.elementType === targetId;` — exact string equality, no aliasing",
        "packages/core-app-model/src/views/PlanViewCanvas.ts:458 and :469 — `const vgCat = this._vgCategoryForLayer(layerTag);` and `const _penCategory = penCategoryForLayerTag(layerTag);` — two different producers, same line, same tag"
      ],
      "userVisibleSymptom": "Per-view category toggles that are enabled and do nothing (Railings, Curtain walls). Plumbing lines drawn at the generic fallback pen instead of their own 0.13 mm. A family that gains a layer in one file stays invisible to the panel in another — the exact shape that kept `lighting` ungovernable for months.",
      "fix": "Derive one `FAMILY_REGISTRY` (family key → ISO layer → pen category → panel label) in `packages/core-app-model/src/drawing/`, and make the other four sites READ it rather than restate it. Then add a ga-gate that compares the five key SETS in both directions and fails on any divergence — the same shape as `tools/ga-gate/check-contract-index-equivalence.ts`, which exists and works for exactly this class of drift. Do the gate FIRST: it will name every mismatch that is currently invisible, including ones I did not enumerate.",
      "effort": "medium",
      "risk": "medium",
      "confidence": "high",
      "alreadyExists": "`DrawingLayerIdentity.ts` already declares itself \"THE ONE PRODUCER of which layer is this line on, and whose category is that?\" and already survived a seven-copy consolidation (L-1600). It is the natural home. `tools/ga-gate/check-contract-index-equivalence.ts` is a working set-vs-set gate to copy the shape from.",
      "filesToChange": [
        "packages/core-app-model/src/drawing/DrawingLayerIdentity.ts",
        "packages/core-app-model/src/drawing/PenWeightTable.ts",
        "packages/core-app-model/src/presentation/VGSceneApplicator.ts",
        "packages/core-app-model/src/presentation/VisibilityIntentDefaults.ts",
        "apps/editor/src/ui/OverridePanel.ts",
        "apps/editor/src/engine/views/EdgeProjectorService.ts",
        "tools/ga-gate/"
      ]
    },
    {
      "title": "Rooms are painted outside the drawing pipeline entirely, and their category lives in the store the Visibility Intent panel does not read",
      "mechanism": "`PlanViewCanvas._renderRoomFills()` reads `window.roomStore` directly, resolves colour through `resolveRoomColourIntent()` → `vgGovernanceStore.resolveStyle(modelId,'room',viewId)`, and fills polygons straight onto the 2D context BEFORE the drawing is traversed. It never touches the TechnicalDrawing, never creates a layer, never resolves a pen, and never consults `visibilityIntentStore` or `viewIntentInstanceStore`. Meanwhile the panel the founder is looking at lists `Object.keys(intent.elementRules)` (VisibilityIntentPanel.ts:240), which comes from `cloneDefaultElementGraphicsRules()` → `ELEMENT_TYPES` — a list with NO `room`. So `room` IS a first-class category with a full colour-mode cascade (`roomColourMode`, six modes, per-view and per-model tiers) — in the OTHER store. The control that drives it is mounted on the ROOM property section (select a room → its properties), not on any view-level visibility surface. His \"that I don't see\" is literally correct: the row does not exist in the panel he is in.",
      "evidence": [
        "packages/core-app-model/src/views/PlanViewCanvas.ts:383 — `if (isPlanLike) this._renderRoomFills(ctx);` (before `const drawing = viewTechnicalDrawingCache.get(viewId)` at :389)",
        "packages/core-app-model/src/views/PlanViewCanvas.ts:2488-2530 — `_renderRoomFills`: `const roomStore = window.roomStore;` … `const intent = resolveRoomColourIntent(getRoomColourModelId(), this._lastViewId ?? undefined); if (!intent.visible) return;` … `ctx.fillStyle = color; … ctx.fill();`",
        "packages/core-app-model/src/presentation/RoomColourIntent.ts:132 — `const resolved = vgGovernanceStore.resolveStyle(modelId, ROOM_VG_CATEGORY, viewId);`",
        "packages/core-app-model/src/presentation/VGGovernanceStore.ts:148 — `room: { fillColor:'#ffffff', edgeColor:'#bbbbbb', … roomColourMode:'detection' }` in the `pryzm-default` template",
        "packages/core-app-model/src/presentation/VisibilityIntentDefaults.ts:15-47 — ELEMENT_TYPES contains no `'room'` entry",
        "apps/editor/src/ui/VisibilityIntentPanel.ts:240 — `const elementTypes = Object.keys(intent.elementRules);`",
        "apps/editor/src/ui/property-inspector/RoomPropertySection.ts:705 — `window.runtime?.bus?.executeCommand('room.setColourMode', { mode, scope, viewId })` — the only caller of the colour-mode verb",
        "apps/editor/src/ui/OverridePanel.ts:247-269 — CATEGORIES has no `room` row either, so there is no per-view room hide"
      ],
      "userVisibleSymptom": "\"THE ROOMS SHOULD HAVE A CATEGORY ON THE VISIBILITY INTENT SETTINGS TO MODIFY THE COLOURS - THAT I DONT SEE.\" Rooms render tinted pastel with no view-level control anywhere the user looks; the six-mode colour cascade that already exists is reachable only by selecting an individual room.",
      "fix": "Two increments. (1) CHEAP AND CORRECT NOW: add a `room` row to `OverridePanel.CATEGORIES` (per-view hide, which `targetMatches` resolves through `category`), and surface `ROOM_COLOUR_MODE_CHOICES` as a select in the same view-level panel dispatching the existing `room.setColourMode` with `scope:'view'`. (2) STRUCTURAL: add `'room'` to `VisibilityIntentDefaults.ELEMENT_TYPES` so it gets a row in the intent panel — but ONLY together with routing `_renderRoomFills` through the resolved intent fill (`resolveIntentStyle(...,'room','cut',...)`.fill), or the row will appear and do nothing, which is the defect this repo keeps re-minting. Do NOT add the row without the reader.",
      "effort": "medium",
      "risk": "medium",
      "confidence": "high",
      "alreadyExists": "The entire colour-mode cascade: `RoomColourIntent.ts` (RoomColourMode union, six modes, per-view + per-model tiers, `resolveRoomColourIntent`), `ROOM_COLOUR_MODE_CHOICES` (menu order + labels + hints, already written so 'a new mode cannot be added and forgotten'), the `room.setColourMode` bus verb, `RoomColourSystem.resolveForMode`, and the `room` VG category with `visible`/`transparency`. Nothing needs inventing — it needs a second mount point.",
      "filesToChange": [
        "apps/editor/src/ui/OverridePanel.ts",
        "packages/core-app-model/src/presentation/VisibilityIntentDefaults.ts",
        "packages/core-app-model/src/views/PlanViewCanvas.ts"
      ]
    },
    {
      "title": "Lighting is drawn TWICE in plan: an ungoverned blue canvas overlay on top of the full 672-face luminaire mesh",
      "mechanism": "There IS a lighting plan symbol — `renderLightingSymbols()` — but it is painted directly onto the 2D context from `LightingStore`, after the annotation pass, with hard-coded `#0ea5e9` stroke and `ctx.lineWidth = 1.25`. It contributes ZERO LineSegments (its own header at :44-56 records that this is why lighting was unselectable in plan), so it is invisible to the layer system, the pen table, `vgGovernanceStore`, the visibility intent, the hit test and the SVG/DXF exporter. Separately, `LightingFragmentBuilder` stamps `elementType: 'Lighting'` on every mesh and does NOT stamp `userData.skipInPlan` — the convention every other symbolised family uses (door, window, bed, chair, sofa, kitchen, wardrobe, tree, plumbing all set it). So the 672-face luminaire is fully edge-projected as well. The founder's \"dense circular hatch-like symbols drawn as fine black concentric line bursts\" is that mesh dump; the blue disc is drawn over it. Commit 584aaab2 (today) gave that mesh linework the correct 0.13 mm `lighting` pen — it will now be lighter, but it is still the wrong drawing.",
      "evidence": [
        "packages/core-app-model/src/views/symbols/LightingPlanSymbolRenderer.ts:13-16 — `const STROKE_DEFAULT = '#0ea5e9'; const FILL_DEFAULT = 'rgba(14,165,233,0.12)';`",
        "packages/core-app-model/src/views/symbols/LightingPlanSymbolRenderer.ts:104 — `ctx.lineWidth   = 1.25;` (a literal, not a resolved pen)",
        "packages/core-app-model/src/views/symbols/LightingPlanSymbolRenderer.ts:44-56 — \"Lighting is the one family whose plan symbol is painted straight onto the 2-D canvas from the store … It contributes ZERO LineSegments\"",
        "packages/core-app-model/src/views/PlanViewCanvas.ts:660 — `this._renderLightingPlanSymbols();` (after `planViewAnnotationRenderer.render(...)`, outside the drawing traverse)",
        "packages/geometry-lighting/src/LightingFragmentBuilder.ts:726 — `elementType: { value: 'Lighting', writable: false, … }` — and `grep -n skipInPlan packages/geometry-lighting/src/*.ts` → 0 hits",
        "apps/editor/src/engine/views/EdgeProjectorService.ts:2914 — `if (isPlanView && mesh.userData.skipInPlan === true) { … }` — the suppression gate lighting never satisfies",
        "apps/editor/src/engine/views/EdgeProjectorService.ts:260 — `Lighting: 'A-LGHT', LightingFixture: 'A-LGHT', light: 'A-LGHT',` (added today, 584aaab2)",
        "founder log: `DIAG-EPS-01 … repeated Lighting groups at 672 faces each`",
        "There is no `_symbolGate('lighting')` call site — `grep -n \"_symbolGate(\" EdgeProjectorService.ts` lists door/furniture×6/plumbing×2/window/stair/roof/column/wall/boundary-line/spaceEnvelope only"
      ],
      "userVisibleSymptom": "\"LIGHTING FIXTURES DEFINITELY SHOULD HAVE A SYMBOL FOR EACH\" — and the dense black concentric bursts all over his Level 2 plan. Also: hiding lighting in a view hides the mesh linework (now that A-LGHT is classified) but NOT the blue overlay disc, which has no governor at all.",
      "fix": "Do the two halves together or neither — the sibling lane's own commit note says adding a symbol alone \"would draw the symbol ON TOP of the blob and make it worse\", and that is already the state. (1) Stamp `userData.skipInPlan = true` on lighting meshes in `LightingFragmentBuilder` (one line, the exact convention `DoorBuilder.ts:689` and `ParametricTreeEngine.ts:570` use). (2) Convert `renderLightingSymbols` into a real `LightingPlanSymbolBuilder` that injects UUID-registered LineSegments onto `A-LGHT` and is gated by `_symbolGate('lighting')` — then it inherits the pen, the VG category, the intent, the hit test and the DXF export for free, and `PlanViewCanvas.lightingHitTest.test.ts`'s bespoke hit path can retire.",
      "effort": "medium",
      "risk": "medium",
      "confidence": "high",
      "alreadyExists": "The symbol GEOMETRY is fully authored — `LightingPlanSymbolRenderer` already has a per-family shape switch (pendant, pendant_pebble, pendant_ceramic_bell, pendant_conical, linear_led…) and a derived extents table shared with its hit test. The port is from `ctx` calls to a `number[]` position buffer + `projectToDrawingSpace` + `addProjectionLines`, exactly as `TreePlanSymbolBuilder` does. The `lighting` pen has existed in all three zones the whole time.",
      "filesToChange": [
        "packages/geometry-lighting/src/LightingFragmentBuilder.ts",
        "packages/core-app-model/src/views/symbols/LightingPlanSymbolRenderer.ts",
        "apps/editor/src/engine/views/EdgeProjectorService.ts",
        "packages/core-app-model/src/views/PlanViewCanvas.ts"
      ]
    },
    {
      "title": "Symbol-injected lines carry no `userData.elementUUID`, so a per-ELEMENT visibility override can never reach them — and in plan, furniture IS only its symbol",
      "mechanism": "`PlanViewCanvas.render()` resolves the pen with `elementId: child.userData?.elementUUID` (:472). `EdgeProjectorService` stamps that key on native projection lines (:3223 `projected.userData.elementUUID = elementUUID;`). The symbol builders do NOT: they call `registerSegmentUUID(drawing, projected, id)`, which writes only into a module-level `WeakMap` used by the hit test — it never touches `userData` (DrawingSelectionIndex.ts:42-53). `DoorPlanSymbolBuilder` goes further and REPLACES userData wholesale with `{ lineWeight, role, elementType }` (:301). So for every symbol line `_elementId` is `undefined`, `resolveIntentStyle`'s `target.elementId` is undefined, and `targetMatches('element', id, target)` is false by construction. This matters because the Project Browser — the surface a user reaches for to \"exclude the furniture elements\" — writes PER-ELEMENT overrides: `applyCategoryVisibility` loops the category's elements and calls `applyElementVisibility` → `writeElementVisibilityIntent(elemId,false)` → `view.hideElement` with an `elementId`. And every furniture mesh is `skipInPlan`, so in plan the ONLY furniture linework is the symbol. Hiding furniture element-by-element therefore hides nothing in plan, exactly as reported. RIVAL RULED IN, NOT OUT: his screenshot shows \"NO OVERRIDES\" on that view, which means no per-element override was live either — so for THAT view the read-only-intent finding above is the likelier cause. Both defects are real; I cannot tell from code which gesture he made.",
      "evidence": [
        "packages/core-app-model/src/views/PlanViewCanvas.ts:472 — `const _elementId = child.userData?.elementUUID as string | undefined;`",
        "packages/core-app-model/src/views/DrawingSelectionIndex.ts:42-53 — `registerSegmentUUID(drawing, lineSegments, elementUUID)` writes only `map.set(lineSegments, elementUUID)` into `_index`; no userData write",
        "packages/geometry-furniture/src/builders/WardrobePlanSymbolBuilder.ts:149-150 — `drawing.addProjectionLines(projected, FURN_LAYER); registerSegmentUUID(drawing, projected, w.id);` — no `projected.userData.elementUUID`",
        "packages/geometry-door/src/DoorPlanSymbolBuilder.ts:301 — `cutSeg.userData = { lineWeight: LW_CUT, role: 'cut', elementType: 'Door' };` (whole-object assignment, no elementUUID)",
        "apps/editor/src/engine/views/EdgeProjectorService.ts:3223 — `projected.userData.elementUUID = elementUUID;` — the native path DOES stamp it",
        "apps/editor/src/ui/ViewBrowser/panels/unified-browser/ProjectVisibilitySection.ts:288-295 — `applyCategoryVisibility` → per-element `applyElementVisibility(bag, id, visible)`",
        "apps/editor/src/ui/ViewBrowser/panels/unified-browser/ProjectVisibilitySection.ts:66-75 — `writeElementVisibilityIntent` dispatches `view.hideElement` with `{ viewId, elementId }` — targetKind 'element'",
        "packages/core-app-model/src/presentation/IntentRuleResolver.ts:79 — `if (kind === 'element') return !!target.elementId && target.elementId === targetId;`",
        "packages/geometry-furniture/src/engines/WardrobeEngine.ts:404, BedEngine.ts:127, ParametricTreeEngine.ts:570 — every furniture mesh sets `skipInPlan: true`",
        "apps/editor/src/engine/views/EdgeProjectorService.ts:2914 — the projector honours it, so plan furniture is symbol-only",
        "Also note: `BoundaryLinePlanSymbolBuilder.ts:290` and `SpaceEnvelopePlanSymbolBuilder.ts:213` DO write `projected.userData['elementUUID'] = elementId` — so two of the seventeen injectors already do it right, and the divergence is unpoliced"
      ],
      "userVisibleSymptom": "\"I EXCLUDE THE FURNITURE ELEMENTS FROM VIEW BUT THEY STILL RENDER (PROBABLY BECAUSE THEY ARE SYMBOLS?).\" His hypothesis is right for the per-element route: hiding a bed from the Project Browser removes nothing from the plan, because the bed's plan representation is a symbol the override cannot address.",
      "fix": "One line per injector, or better, one line in the shared helper: after `projectToDrawingSpace`, set `projected.userData.elementUUID = id` alongside the existing `registerSegmentUUID(...)`. Two builders already do exactly this. Then add an arm to the existing symbol-injector suite asserting that a per-ELEMENT hide (targetKind 'element') reaches `ctx.globalAlpha === 0` for an `A-FURN` symbol line — the current suite only exercises a category-level hide written straight into the intent store, which is why this gap survived.",
      "effort": "small",
      "risk": "low",
      "confidence": "high",
      "alreadyExists": "`BoundaryLinePlanSymbolBuilder.ts:290` and `SpaceEnvelopePlanSymbolBuilder.ts:213` are the working pattern. `apps/editor/__tests__/visibilityIntentGovernsSymbolInjectors.test.ts` is the harness to extend — it already records strokes with alpha and already knows the OBC userData-destruction behaviour.",
      "filesToChange": [
        "packages/geometry-furniture/src/builders/*PlanSymbolBuilder.ts",
        "packages/geometry-door/src/DoorPlanSymbolBuilder.ts",
        "packages/geometry-window/src/WindowPlanSymbolBuilder.ts",
        "packages/geometry-column/src/ColumnPlanSymbolBuilder.ts",
        "packages/geometry-plumbing/src/PlumbingPlanSymbolBuilder.ts",
        "packages/geometry-stair/src/StairSymbolTechnicalDrawingBridge.ts",
        "apps/editor/__tests__/visibilityIntentGovernsSymbolInjectors.test.ts"
      ]
    },
    {
      "title": "`VGSceneApplicator.applyToProjectionLayers()` is a DEAD WRITE for the on-screen 2D drawing, and it covers only half the sub-layers",
      "mechanism": "`applyToProjectionLayers` calls `drawing.layers.setVisibility(layerName, style.visible)`. OBC's implementation (@thatopen/components 3.4.6, index.mjs:24873-24883) walks the container and sets `child.visible = visible` for children whose `userData.layer` matches. `PlanViewCanvas.render()` then iterates with `(drawing as any).three?.traverse?.(...)` — THREE's `traverse` visits every descendant regardless of `.visible`; the callback never reads `child.visible` (grep for `.visible` in PlanViewCanvas returns only style-resolver results at :463/:2027/:2580/:2859 and an underlay ref at :1390). So the visibility half of that method has NO effect on the screen. It coincidentally agrees with the canvas because both read `vgGovernanceStore`, which is why the deadness has never surfaced. Second half of the defect: the method only builds `${layerName}:cut` / `:proj` / `:beyond` (colon). Every symbol builder creates HYPHEN sub-layers — `A-DOOR-CUT`, `A-DOOR-PROJ`, `A-DOOR-BEYOND`, `A-FURN-SHADOW` — and `CATEGORY_TO_DXF_LAYER` has no `A-LGHT`, `A-CONS`, `A-AREA`, `A-GRID` or `A-LEVL` at all. Those layers never receive VG colour, opacity or `linewidth`, which the DXF/SVG export pipeline is documented to read (:601-603).",
      "evidence": [
        "packages/core-app-model/src/presentation/VGSceneApplicator.ts:635 — `drawing.layers.setVisibility(layerName, style.visible);`",
        "node_modules/@thatopen/components/dist/index.mjs:24873-24883 — `setVisibility(name, visible) { … this._container.traverse((child) => { if (child.userData.layer === name) child.visible = visible; }); }`",
        "packages/core-app-model/src/views/PlanViewCanvas.ts:431 — `(drawing as any).three?.traverse?.((child: THREE.Object3D) => {` — `traverse`, not `traverseVisible`",
        "packages/core-app-model/src/views/PlanViewCanvas.ts:432-476 — the callback checks `instanceof THREE.LineSegments`, position count, layer tag, VG style and pen; it never reads `child.visible`",
        "packages/core-app-model/src/presentation/VGSceneApplicator.ts:674-676 — `const cutLayerName = \\`${layerName}:cut\\`;` etc. — colon only",
        "packages/geometry-door/src/DoorPlanSymbolBuilder.ts:190-208 — DOOR_LAYER_CUT / _PROJ / _GHOST are the hyphenated ISO sub-layers",
        "packages/geometry-furniture/src/builders/TreePlanSymbolBuilder.ts:46 — `const FURN_SHADOW_LAYER = 'A-FURN-SHADOW';` — not in CATEGORY_TO_DXF_LAYER",
        "packages/core-app-model/src/presentation/VGSceneApplicator.ts:601-603 — the comment stating `linewidth` exists so \"the DXF/SVG export pipeline can read per-category stroke widths\""
      ],
      "userVisibleSymptom": "No direct screen symptom today (the two authorities agree by accident), but: tree shadows and door ghosts survive a category hide in any consumer that reads `drawing.three` rather than the painted canvas, and DXF/SVG export loses VG colour and stroke width for every symbol sub-layer and for lighting, grids, levels and boundary lines. It is also a live trap: the next person who \"fixes\" visibility by writing layer state will find it does nothing.",
      "fix": "Decide which one is the authority and delete the other. Recommended: keep the canvas's per-line resolution as THE authority (it is the one that also drives the hit test via `_lineIsDrawn`), and reduce `applyToProjectionLayers` to the EXPORT-facing concerns (colour + linewidth) — driven off the derived family registry so it covers every layer that exists, hyphen sub-layers included — with a comment saying plainly that its visibility write is not read by the canvas. Do not add a `child.visible` check to the canvas as the fix: that would make two authorities both live and disagreeing.",
      "effort": "small",
      "risk": "low",
      "confidence": "high",
      "alreadyExists": "`vgCategoryForLayer` / `composeLayerTag` (DrawingLayerIdentity.ts) already resolve hyphen and colon sub-layers uniformly and are the documented ONE producer; `applyToProjectionLayers` is the only significant consumer still hand-rolling `${name}:cut`.",
      "filesToChange": [
        "packages/core-app-model/src/presentation/VGSceneApplicator.ts"
      ]
    },
    {
      "title": "The incremental graft path is unavailable for every gesture a documentation pass actually makes",
      "mechanism": "`ViewDependencyTracker.PLAN_INCREMENTAL_SAFE_TYPES = { 'wall','slab','beam','ceiling','floor' }`. Anything else — create or update of a door, window, furniture, stair, column, roof or lighting fixture, ANY delete, ANY batch, or a stale-id fallback — leaves the view graft-ineligible, `graftElementIds` empty, and `initScene`'s `graftDecline` resolves to `no-graft-ids`, taking the full O(N) pass: re-export all 78 elements, re-run `EdgesGeometry` per group, re-inject all fifteen symbol sets, re-run occlusion. The founder's own log line is that branch firing. Separately, `vi:instance-updated` invalidates the whole drawing (ViewTechnicalDrawingCache.ts:914-917), so toggling ONE category's visibility also forces a full re-projection — even though the only thing that changed is a pen the canvas resolves at paint time. On `hitRate=0%`: the NME proxy-cache key is `elementId:viewId:version:cropKey` with `cropKey='full'` for an uncropped plan and `version` defaulting to -1, so the key is stable; `hits=0 misses=70 cacheSize=69/500` is the signature of a COLD cache for that viewId, not of a key that churns. I could not establish from static reading whether it is 0% on every pass — that needs a second log line from a repeat projection of the same view.",
      "evidence": [
        "packages/core-app-model/src/views/ViewDependencyTracker.ts:150-152 — `export const PLAN_INCREMENTAL_SAFE_TYPES: ReadonlySet<string> = new Set(['wall','slab','beam','ceiling','floor']);`",
        "apps/editor/src/engine/initScene.ts:1318-1322 — `!graftElementIds || graftElementIds.size === 0 ? 'no-graft-ids (ViewDependencyTracker marked this view COARSE: a delete, a non-pure-projection create such as door/window/furniture/stair/column/roof, a batch, or a §G3 stale-id fallback …)'`",
        "apps/editor/src/engine/initScene.ts:1334 — the log line the founder pasted",
        "packages/core-app-model/src/views/ViewTechnicalDrawingCache.ts:914-917 — `window.addEventListener('vi:instance-updated', (e) => { const viewId = …; if (viewId) this.invalidate(viewId); });`",
        "packages/core-app-model/src/geometry/NativeElementMeshExporter.ts:532 — `const cacheKey = \\`${elementId}:${viewId}:${currentVersion}:${cropKey}\\`;` with `currentVersion = root.userData?.version ?? -1` (:531) and `cropKey = 'full'` for an uncropped plan (:480-489)",
        "apps/editor/src/engine/views/EdgeProjectorService.ts:1318-1327 (initScene) / :2781 — the SECOND cache (`_cwCache`, per-element projected layers) is gated on `CACHEABLE_ELEMENT_TYPES` (:2026-2086), which does cover furniture/lighting/door/window — so the full pass is not as expensive as it looks, but it still re-injects every symbol and re-runs occlusion"
      ],
      "userVisibleSymptom": "Every furniture move, door edit, lighting placement or category toggle costs a whole-drawing re-projection. Combined with the 300 ms coarse debounce, the plan feels like it \"waits behind the scenes\" on exactly the edits a documentation pass consists of.",
      "fix": "Two separable wins. (a) A visibility/pen-only change must NOT invalidate the drawing at all — the canvas resolves the pen per paint. Split `vi:instance-updated` handling: if the delta touched only `visibilityOverrides`/`graphicOverrides` (not the symbol-gate answer for a family), repaint without re-projecting. Careful: the SymbolInjectionGate makes a family-level hide affect the drawing's CONTENT, so the honest split is \"family-level hide ⇒ reproject; per-element or per-style ⇒ repaint\". (b) Widen `PLAN_INCREMENTAL_SAFE_TYPES` family by family, gated on each family being able to state its full plan representation from its own id — which for symbol families requires finding #5 (the `elementUUID` stamp) first, since `projectElementsInto` transplants by UUID.",
      "effort": "large",
      "risk": "high",
      "confidence": "medium",
      "alreadyExists": "`projectElementsInto()` (EdgeProjectorService.ts:~4285) is a working O(dirty) graft that reuses the SAME `project()` path, so there is no divergent projection code to drift. `ViewTechnicalDrawingCache.invalidateElement(viewId, elementId)` already exists for element-scoped invalidation. `bumpPerf(PERF_KEYS.REPROJECT_FULL / REPROJECT_GRAFT)` already counts both arms — measure before changing.",
      "filesToChange": [
        "packages/core-app-model/src/views/ViewDependencyTracker.ts",
        "packages/core-app-model/src/views/ViewTechnicalDrawingCache.ts",
        "apps/editor/src/engine/initScene.ts"
      ]
    },
    {
      "title": "Occlusion runs last, over-claims on oblique solids, and deletes rather than demotes — 17 of 22 projection occluders degraded on the founder's own plan",
      "mechanism": "`applyOcclusion()` is the last mutation before the drawing is cached (EdgeProjectorService.ts:4235), so it tests injected symbol linework too. For each occluder it picks a test: exact silhouette (even-odd over closed curves), or — when the canonical edge set has an odd-degree vertex, i.e. any solid oblique to the picture plane — the VERTICAL-SPAN HULL, which claims the full vertical extent between the silhouette's bounds. His log reads `22 projected … 17 vertical-span`, with `disposition=remove` and `239.5 segment-equivalent(s) removed`. So 77% of the projection occluders on that plan are coarse hulls and the disposition deletes rather than demotes. HONEST LIMIT: the vertical-span hull is exact for a vertically simple silhouette and the code says so; I have NOT established how much of the 239.5 was wrongly removed. The number is a place to look, not a verdict. What IS structural: `resolveOcclusionDisposition` had no UI until commit 9babe6f5 today, so nobody could ever choose \"show dashed\" and see what the hull was eating.",
      "evidence": [
        "apps/editor/src/engine/views/EdgeProjectorService.ts:4235-4249 — `applyOcclusion(drawing, { disposition: resolveOcclusionDisposition(viewDef?.output, viewScope), minProjectionOccluderDepth: isPlanView ? 0 : -Infinity })`, described at :4230 as \"Runs AFTER all symbol injections … the last mutation point\"",
        "packages/core-app-model/src/drawing/HiddenLineRemoval.ts:437-448 — `} else if (oddDegree && !b.isCut) { … test = 'vspan'; vspanFallbacks++; }` with the note \"the canonical set is not a union of closed curves, so even-odd is UNSOUND over it\"",
        "packages/core-app-model/src/drawing/HiddenLineRemoval.ts:695-700 — \"A rising number is not a defect; a number that is rising *and* the drawing looks over-hidden is where to look.\"",
        "founder log: `26 occluder(s) (4 cut, 22 projected; 9 silhouette, 17 vertical-span, 0 AABB), disposition=remove, 239.5 segment-equivalent(s) removed, 0 sub-segment(s) demoted proj -> HIDDEN (dashed)`",
        "commit 9babe6f5 — `output.occlusionDisposition` and `output.beyondLineStyle` \"were WRITTEN BY NO UI AT ALL\" until today"
      ],
      "userVisibleSymptom": "Plausible contributor to \"THE QUALITY ATM IS REALLY BAD\": linework that should be there is silently deleted, and until today there was no control to switch the disposition to dashed and see it.",
      "fix": "Do not change the hull heuristic on this evidence. Instead: (1) with the new disposition control shipped, ask the founder to flip one plan to \"Show dashed\" and screenshot — that turns 239.5 deletions into 239.5 visible dashes and answers in one image whether the hull is over-claiming; (2) if it is, the targeted fix is to close the canonical edge set for oblique solids (the odd-degree vertices come from the projected silhouette not being stitched), not to widen the test.",
      "effort": "medium",
      "risk": "medium",
      "confidence": "low",
      "alreadyExists": "`vspanFallbacks` is already counted and printed on every pass (Contract 23 §9 forbids a silent cap), and `resolveOcclusionDisposition` + the new panel control give the A/B without writing any code.",
      "filesToChange": [
        "packages/core-app-model/src/drawing/HiddenLineRemoval.ts"
      ]
    },
    {
      "title": "C102 clauses this pipeline does not satisfy today",
      "mechanism": "Read against C102 as written. (i) V-RE-1 — \"A CONTROL FOR AN UNREACHABLE CAPABILITY MUST RENDER DISABLED WITH THE REASON NAMED … It MUST NOT render enabled and no-op\": BREACHED twice over. `OverridePanel`'s Railings and Curtain walls checkboxes render enabled and dispatch a command whose targetId can never match (finding 2); and every write control in `VisibilityIntentPanel` for a system intent renders present-but-inert (finding 1 — the inputs carry `disabled` but the shift-click cell-selection and state-tab handlers are wired above the `isSystem` return, so the panel is half-live). (ii) V-ST-3 — \"A COUNTER MUST BE PROVEN BY MOTION … any panel column derived from a store binding MUST carry a test that CHANGES the binding and asserts the displayed value follows\": the generalisation is not applied to the category toggles. `overridePanelDispatchReach.spec.ts:109` asserts the verb is DISPATCHED, not that the projected pen MOVES — so a dead targetId scores 1.000, which is exactly how `railing` survived. (iii) V-CN-1 remains open (`SyncStateEngine` still reads the writer-less `viewTemplateId`). (iv) §0.1 routes \"what governs an element's appearance in a view?\" to C09 §4 — but the answer is currently distributed across three stores plus three ungoverned canvas painters, which is not a state C09 §4.1's single-authority framing describes.",
      "evidence": [
        "docs/02-decisions/contracts/C102-VIEW-AND-SHEET-INTEGRITY.md:206-209 — V-RE-1",
        "docs/02-decisions/contracts/C102-VIEW-AND-SHEET-INTEGRITY.md:133-137 — V-ST-3 \"A COUNTER MUST BE PROVEN BY MOTION\"",
        "docs/02-decisions/contracts/C102-VIEW-AND-SHEET-INTEGRITY.md:184-186 — V-CN-1",
        "apps/editor/src/ui/OverridePanel.ts:255-256 — the two dead ids, rendered as ordinary enabled checkboxes",
        "apps/editor/src/ui/__tests__/overridePanelDispatchReach.spec.ts:109-120 — asserts `seen.filter(d => d.name === 'view.setCategoryVisibility')`, i.e. dispatch, not effect",
        "apps/editor/src/ui/VisibilityIntentPanel.ts:749 and :763 — shift-click handlers wired BEFORE the `if (intent.isSystem) return;` at :772"
      ],
      "userVisibleSymptom": "Controls that look live and are not — across three separate panels. This is the failure mode C102 was written to stop, reproduced in the surfaces C102 governs.",
      "fix": "Convert the C102 clauses into the two gates they imply. (a) An `enabled` control whose command targets a family key must have that key present in the derived family registry (finding 2) — enforce at build, not review. (b) Extend the motion rule from counters to CONTROLS: every category toggle needs one arm asserting the projected pen for that family moves, keyed off the registry so a new family cannot be added without an arm. Then update C102 §4's reachability table with the three surfaces measured here (VisibilityIntentPanel writes, OverridePanel category toggles, Project Browser element filter), which it does not currently enumerate.",
      "effort": "medium",
      "risk": "low",
      "confidence": "high",
      "alreadyExists": "`apps/editor/__tests__/visibilityIntentGovernsSymbolInjectors.test.ts` and `viewIntentGovernsEveryCategory.test.ts` are two working harnesses that already assert at `ctx.strokeStyle`/`globalAlpha` — the motion arms should be added there, driven by the registry, rather than written fresh.",
      "filesToChange": [
        "docs/02-decisions/contracts/C102-VIEW-AND-SHEET-INTEGRITY.md",
        "apps/editor/src/ui/OverridePanel.ts",
        "apps/editor/src/ui/__tests__/overridePanelDispatchReach.spec.ts",
        "tools/ga-gate/"
      ]
    },
    {
      "title": "THE ONE STRUCTURAL CHANGE — one per-view ViewGraphicsResolver, landing on the SymbolInjectionGate seam",
      "mechanism": "Today six painters and two consumers each re-derive the answer to \"does this appear, and how\": the projector's native loop (`_symbolGate` + `skipInPlan`), the fifteen injectors (`_symbolGate` at the call site, plus a SECOND, different check inside Door and Window only — `vgGovernanceStore.getEffectiveStyle('Door', id).hidden` at DoorPlanSymbolBuilder.ts:280 and WindowPlanSymbolBuilder.ts:245, which no other builder has), `VGSceneApplicator` (layer state, dead for the canvas), `PlanViewCanvas.render()` (VG skip + intent pen), `_renderRoomFills` (vgGovernanceStore only), `renderLightingSymbols` (nothing at all), `planViewAnnotationRenderer` (graphicsRulesEngine, correctly), plus `_lineIsDrawn` for the hit test and `SVGCompositeRenderer` for export. Each is individually defensible and collectively they are why one founder complaint splinters into five. The seam to collapse them onto already exists and is already named: `makeSymbolInjectionGate(viewId, viewType)` resolves the bound intent ONCE, memoises per family, and fails open — it is one function call away from being a full graphics resolver.",
      "evidence": [
        "packages/core-app-model/src/presentation/SymbolInjectionGate.ts:5-6 — \"THE ONE PLACE A SYMBOL INJECTOR ASKS 'IS THIS FAMILY VISIBLE IN THIS VIEW?'\"",
        "packages/core-app-model/src/presentation/SymbolInjectionGate.ts:112-150 — `makeSymbolInjectionGate`: resolves `resolveBoundIntentWithInheritance(viewId)` once, `cache = new Map<string, boolean>()`, fails open on throw",
        "apps/editor/src/engine/views/EdgeProjectorService.ts:2585 — `const _symbolGate = makeSymbolInjectionGate(viewDef.id, viewDef.viewType);` — already built once per projection and already asked by BOTH the native loop (:2725) and all fifteen injectors (:3969-4130)",
        "packages/geometry-door/src/DoorPlanSymbolBuilder.ts:280 — `if (vgGovernanceStore.getEffectiveStyle('Door', door.id).hidden) continue;` — a second, per-element authority present in 2 of 14 builders and nowhere else",
        "packages/core-app-model/src/views/PlanViewCanvas.ts:2843-2871 — `_lineIsDrawn`, whose own header argues the case: \"'What is drawn' and 'what can be picked' must be the SAME sentence or they drift apart again the first time either is edited\" — the same argument extends to the other six painters",
        "packages/core-app-model/src/drawing/GraphicsRulesEngine.ts:85-97 — the pen half already has a per-view cache with correct invalidation on `vi:*` events; the resolver can reuse it rather than adding a rival cache"
      ],
      "userVisibleSymptom": "Not a symptom of its own — it is why each of the five complaints needed a separate lane, and why fixing one (lighting's pen, today) does not touch the next (lighting's double-draw).",
      "fix": "Generalise `SymbolInjectionGate` into `makeViewGraphicsResolver(viewId, viewType)` returning `{ familyVisible(family), pen(family, zone, elementId), fill(family, zone, elementId), colourMode(family) }`, built ONCE per projection and ONCE per paint, over the single derived family vocabulary from finding 2. Then repoint, in this order (each is independently shippable and independently verifiable at `ctx`): (1) `PlanViewCanvas.render()` — replaces `_styleResolver` + the inline `graphicsRulesEngine.resolveStyle` pair, which removes the vgCategory/penCategory disagreement in one move; (2) `_lineIsDrawn` — so pointer and pixels stay one sentence; (3) `_renderRoomFills` and `renderLightingSymbols` — the two ungoverned painters, which is what finally gives rooms and lighting a working view-level control; (4) delete the two rogue `vgGovernanceStore.getEffectiveStyle` checks inside Door and Window. Do NOT start by writing the resolver: start with the vocabulary gate (finding 2), because it will name the mismatches the resolver would otherwise inherit silently.",
      "effort": "large",
      "risk": "medium",
      "confidence": "high",
      "alreadyExists": "Nearly all of it. `makeSymbolInjectionGate` (the seam, the memoisation, the fail-open policy, the OTel rationale). `graphicsRulesEngine.resolveStyle` (the four-tier pen merge with a correct cache and correct `vi:*` invalidation). `resolveIntentStyle` (the intent ladder incl. view-type profiles, modifiers, purpose modifiers, isolate and overrides). `DrawingLayerIdentity.composeLayerTag` / `vgCategoryForLayer` (the ONE layer→family producer). `resolveViewScope` / `resolveBeyondLineStyle` / `resolveOcclusionDisposition` (per-view-type policy, already resolved rather than branched). `VgCanvasStyleResolver` (the override-vs-default discrimination via `overriddenProps`). The work is composition and repointing, not solving.",
      "filesToChange": [
        "packages/core-app-model/src/presentation/SymbolInjectionGate.ts",
        "packages/core-app-model/src/views/PlanViewCanvas.ts",
        "packages/core-app-model/src/views/symbols/LightingPlanSymbolRenderer.ts",
        "packages/core-app-model/src/presentation/VgCanvasStyleResolver.ts",
        "packages/geometry-door/src/DoorPlanSymbolBuilder.ts",
        "packages/geometry-window/src/WindowPlanSymbolBuilder.ts",
        "apps/editor/src/engine/views/EdgeProjectorService.ts"
      ]
    }
  ],
  "whatIcouldNotEstablish": "1. WHICH GESTURE THE FOUNDER ACTUALLY MADE. His screenshot shows \"NO OVERRIDES\" on the Level 2 view, and `countOverrides` reads `ViewIntentInstance.localOverrides` — so at the moment of the screenshot no per-view category override and no per-element hide was live on that view. That is consistent with finding 1 (he edited the read-only system intent) and INCONSISTENT with him having used the OverridePanel's Furniture toggle or the Project Browser on that view. But three surfaces can write visibility and I cannot see his session. Findings 1 and 5 are both real defects; which one produced HIS screenshot is unresolved. The cheapest discriminator: ask him to open V/G on that view, untick Furniture, and say whether the symbols vanish — if they do, it is finding 1; if they do not, it is something below the gate.\n\n2. WHETHER `H2-NME-CACHE hitRate=0%` PERSISTS. One log line. The key `elementId:viewId:version:cropKey` is stable for an uncropped plan (`cropKey='full'`, `version` defaults to -1), and `cacheSize=69/500` after 70 misses is the signature of a cold cache for that viewId, not of a churning key. A second projection of the SAME view would settle it in one line. I did not run the app.\n\n3. HOW MUCH OF THE 239.5 REMOVED SEGMENT-EQUIVALENTS IS OVER-OCCLUSION. 17 of 22 projection occluders fell to the vertical-span hull, which the code itself documents as exact for a vertically simple silhouette. \"Coarse test + delete disposition\" is a place to look, not a proven cause of the missing lines. The new occlusion-disposition control (9babe6f5) makes this a one-screenshot experiment.\n\n4. WHETHER THE THINNESS IS PEN RESOLUTION OR RASTER. I traced the pen chain (`SCREEN_PX_PER_MM ≈ 3.779`, `resolveCanvasRenderScale` floors the backing store at 3, `minStrokePx = 1/scale` = one device pixel) and it composes correctly: wall CUT 0.50 mm → ~5.7 device px, furniture PROJECTION 0.13 mm → ~1.5 device px, BEYOND 0.09 mm → ~1.0 device px at 0.55 alpha. I could not establish from static reading whether the founder's plan is thin because families are falling to the fallback pen (which finding 2 shows happens for plumbing, grids, levels and space envelopes today, and happened for lighting until this morning) or because three de-emphasis knobs multiply (light grey × 0.55 alpha × sub-pixel width) — the sibling lane 9babe6f5 rejected one proposed floor change on measured grounds and left \"set a contrast FLOOR on the composited stroke\" as the honest open item, which I agree with and did not re-derive.\n\n5. THE ELEVATION AND SECTION HALVES. I traced plan in full. Elevation/section share `EdgeProjectorService.project()`, the same gate, the same occlusion engine and the same canvas, but they have NO graft path at all (`initScene.ts:1319`: \"elevation/section have NO graft path\"), a different scope cull (`scopeFrame`), and their own symbol builders (`OpeningElevationSymbolBuilder`, `TreeElevationSymbolBuilder`, `PlumbingElevationSymbolBuilder`). I did not read those three or the section-specific poché gating, so findings 3, 4 and 5 are asserted for PLAN and are only presumed for elevation/section.\n\n6. WHETHER `RoomTagAutoPopulator level=L0: 0 created … out of 7 live room(s)` and `RoomBoundaryBuilder: 15 error room(s) tracked` against 7 live rooms indicate a defect. The counts do not obviously reconcile (15 error rooms on a level with 7 rooms), and room tags DO appear in his Level 2 screenshot. I did not open either file. Worth one lane's attention; I have no mechanism to offer."
}