{
  "survivingTitles": [
    "Linework line-width is pinned to a constant 96-DPI mm→px factor while the annotation layer in the SAME drawing is paper-scale-and-zoom-aware — the linework draws at 40-75% of its own paper weight on a fitted plan",
    "The stroke floor and its merge-blocking guard measure BACKING-STORE pixels and call them DEVICE pixels — on the founder's dpr 0.9375 display the guard is 3.2× optimistic and five of the eight pen rungs are sub-device-pixel",
    "The BEYOND zone renders at roughly 1.2:1 contrast — effectively invisible — because a 0.09 mm grey pen at 55% opacity lands on 0.319 device pixels",
    "`LightingPlanSymbolRenderer` hard-codes `ctx.lineWidth = 1.25` and a saturated UI blue `#0ea5e9` — a C09 §4.6.4b breach that makes a light fitting draw heavier than a projected wall",
    "STANDARD-vs-CODE GAP: SPEC-AUTODIMENSION §12.11 states the lineweight hierarchy and §13.3 records verbatim that it is NOT ENFORCED; C101 and C102 contain zero line-weight clauses; and no document anywhere states a MINIMUM on-screen weight or contrast",
    "C09 §4.6.4e's claim that 'the hierarchy is already right in EXPORT' is FALSE for the sheet/PDF path — the exported drawing has two weights, CUT equals PROJECTION, and the pen table is never consulted",
    "`purposeModifiers` can never reach the 2D pen — `resolveIntentPenStyle` drops the `viewPurpose` argument, so three of the five built-in purpose profiles are dead on the drawing"
  ],
  "refutedTitles": [
    "Lighting fixtures have NO ISO layer, so their 672-face tessellation projects onto `projection-visible` and draws at the __default__ (WALL) pen — 0.25 mm SOLID BLACK, heavier than a door, a stair or furniture"
  ],
  "refutations": [
    "FINDING 4 — REFUTED AT HEAD, ALREADY FIXED. Commit 584aaab2 (2026-09-09 07:14, §LIGHTING-IS-A-CLASSIFIED-LAYER, L-13267) landed every single one of the four 'one-line additions' this finding proposes, plus a 163-line guard suite. At HEAD: EdgeProjectorService.ts:239 has `Lighting: 'A-LGHT', LightingFixture: 'A-LGHT', light: 'A-LGHT'`; DrawingLayerIdentity.ts:110 has `'A-LGHT': 'lighting'`; PenWeightTable.ts:459 has `isLighting: /A-LGHT|lighting/i` and categoryFromFlags has `if (flags.isLighting) return 'lighting';`; VisibilityIntentDefaults.ELEMENT_TYPES has 'lighting'. This is precisely the already-solved failure mode the lens asks about — the fix is not just available, it is committed. The finding's own mechanism text is also false at HEAD: it claims penCategoryForLayerTag 'runs eleven regexes … there is no isLighting', and there are twelve flags with isLighting ordered deliberately after furniture. RESIDUAL THAT IS STILL REAL, but is NOT what the title says: the 672-face luminaire mesh is still edge-projected (now at 0.13 mm #303030 instead of 0.25 mm black), no lighting SYMBOL is injected, and SymbolInjectionGate is not applied to A-LGHT — 584aaab2's own body states this explicitly as deferred. So the founder's 'dense circular hatch bursts' are lighter but still there."
  ],
  "missed": [
    "CAVEAT ON FINDING 1 — THE PROPOSED GUARD IS ARITHMETICALLY INVERTED. The fix says 'assert 1:50 strokes are 2× the 1:100 strokes at equal zoom'. By paperScale.ts's own formula `paperMmToWorldM = mm × denom / 1000`, a 0.5 mm pen at 1:100 buys 0.05 world m and at 1:50 buys 0.025 world m — so at equal zoom 1:100 is 2× 1:50, the opposite of the asserted direction. Ship that guard as written and it pins the bug. Everything else in finding 1 checks out: PlanViewCanvas.ts:519 `_penPx = _pen.widthMm * SCREEN_PX_PER_MM`, SCREEN_PX_PER_MM = 96/25.4 at DrawingConstants.ts:73, paperMmToPx at paperScale.ts:107, _paperPx at PlanViewAnnotationRenderer.ts:368, and fitToDrawing's `padding = 1.14` → fitByHeight = height×0.57 at PlanViewCanvas.ts:1608-1614 with pxPerWorldM = h_css/(2·_frustumH) — I re-derived 51 px/m for a 12 m plan in a 700 px pane and it lands where the auditor says.",
    "CAVEAT ON FINDING 1 — A PAPER-SCALE STROKE RE-OPENS L-288 AT ZOOMED-OUT VIEWS AND MUST NOT LAND ALONE. Once width tracks zoom, zooming out drives every pen through `Math.max(hairline, …)` and the whole ladder collapses onto minStrokePx again — the exact defect CanvasRenderScale was written to kill. The paper-scale change and a re-derived floor are one change, not two.",
    "CAVEAT ON FINDING 2 — THE DIAGNOSIS SURVIVES, THE PRESCRIBED WIDTH FIX IS REFUTED AND THE REPO HAS ALREADY REJECTED IT. Diagnosis verified: setSize allocates cssW×resolveCanvasRenderScale(dpr) (PlanViewCanvas.ts:1834-1838) while the CSS box is `width:100%;height:100%` (splitView.ts:467-475), so a stroke's true device width is ctx.lineWidth × dpr, never × scale; minStrokePx is documented as 'Exactly ONE DEVICE PIXEL' (CanvasRenderScale.ts:118-125) and delivers one BACKING pixel; the guard repeats it at CanvasHairlineFloor.test.ts:93 and pins beyond at ≈1.02 (:185) when the true dpr-1 device width is 0.340. BUT the fix `minStrokePx = 1/max(1,dpr)` gives a 1.0 CSS px floor at dpr 1, which clamps 0.945 / 0.680 / 0.491 / 0.340 onto one value — four rungs flattened, L-288 restored. Commit 9babe6f5's body rejects this proposal by name and reproduces this same arithmetic. What is actionable is the GUARD half only: compute widthDevice = lineWidth × dpr, add a sub-1 dpr row, and let the assertions go red.",
    "CAVEAT ON FINDING 2 — THE dpr 0.9375 FIGURE IS UNEVIDENCED IN THIS REPO. `grep -rn '0\\.9375'` returns only Köln coordinates and wall-parity snapshots; gpu-pick.ts:840 merely reads devicePixelRatio and is not a record of its value. The finding does not need the number — the substitution is wrong at every dpr below the backing scale, dpr 1 included, where the discrepancy is already 3×. Quote dpr 1, not 0.9375.",
    "CAVEAT ON FINDING 6 — ONE CLAUSE OF THE TITLE IS FALSE. 'No document anywhere states a MINIMUM on-screen weight' is refuted by C09 §4.6.4e(c), which states it normatively — '**THE STROKE FLOOR IS ONE DEVICE PIXEL** — the thinnest mark the rasteriser can physically make' — and §4.6.4e(d) mandates guarding it in device pixels. The number that is genuinely missing is the CONTRAST floor, not the weight floor. The rest verifies: SPEC-AUTODIMENSION.md:345-358 (§12.11 table + the 'must never visually overpower cut walls' prohibition), :540 (§13.3 item 6 verbatim 'not enforced'), zero lineweight/legibility hits in C101/C102, ctx.lineWidth = 3.5 at PlanViewAnnotationRenderer.ts:986, and 46 ctx.lineWidth sites of which exactly 3 (:1002, :1011, :1621) route through _annotationLineWidthPx — which itself multiplies by SCREEN_PX_PER_MM.",
    "MISSED — THE SAME DEFECT AS FINDING 4, STILL OPEN AT HEAD, ON TWO LIVE LAYERS. `apps/editor/src/engine/SpaceEnvelopePlanSymbolBuilder.ts:73` emits plan linework onto `SPACE_ENVELOPE_LAYER = 'A-AREA'` (shipped 2026-09-05) and `EdgeProjectorService.ts:232` emits `PlumbingFixture: 'A-PLMB'`. `penCategoryForLayerTag` (PenWeightTable.ts:452-470) has arms for wall/door/slab/column/stair/handrail/roof/ceiling/furniture/lighting/window/boundary-line and NONE for plumbing or spaceEnvelope, so both fall through categoryFromFlags' `return 'projection'` to FALLBACK_PEN — 0.18 mm SOLID BLACK — while `plumbing: pen(0.13, '#374151')` sits authored and unreachable in all four zones exactly as `lighting` did. DrawingLayerIdentity.ts:122 already records the A-AREA half in its own comment. EXTRA TRAP the auditor's one-line fix would hit: 'spaceEnvelope' is not a pen-table category either, so adding the classifier arm alone still lands on FALLBACK_PEN — a pen row must be minted with it. This is the residual of L-13267 and is now the more valuable half.",
    "MISSED — FINDING 1 NAMES ONE mm→px SITE; THERE ARE TWO. `PlanViewCanvas.ts:2052` — `ctx.lineWidth = Math.max(hairline, edge.widthMm * SCREEN_PX_PER_MM)` — is the pipeline/worker-result render path, with its own `const hairline = minStrokePx(scale)` at :1936. A paper-scale fix applied only at :519 leaves this path on the constant, and the two would then stroke the same drawing at different widths depending on which render entry point ran — split-brain, worse than the present uniform wrongness. Both sites move together or neither does.",
    "MISSED — C09 §4.6.4e's NUMERIC PROOF POINTS AT A PATH THAT DOES NOT EXIST. The contract justifies 'IT IS FORBIDDEN TO FIX THIS BY INFLATING THE PEN TABLE' with '(at `EXPORT_DPI` those pens are 2.95 px and 2.07 px)'. EXPORT_DPI = 300 exists (DrawingConstants.ts:48), but `pxPerMm(EXPORT_DPI)` is called by NO production file — the only occurrences are CanvasHairlineFloor.test.ts:241/254/257. So the raster export the contract offers as the counter-example is not implemented. Combined with finding 7 (the sheet path never consults the pen table), the sentence 'the hierarchy is already right in EXPORT' is currently true of no shipped path at all. The forbidding is still correct policy — but its stated evidence is vacuous, and a lane that goes looking for the good export path will not find one.",
    "MISSED — THE LIGHTING SYMBOL OVERLAY ESCAPES FOUR GOVERNORS, NOT ONE, AND L-13267 DID NOT TOUCH THAT. `renderLightingSymbols` (LightingPlanSymbolRenderer.ts:242) reads `window.lightingStore` and paints straight to the 2D context after the drawing pass, filtered only by levelId and selection. Its own header at :51-56 states it contributes ZERO LineSegments. So it bypasses the pen table, the VG line-weight factor, the hairline floor AND the visibility-intent hide — all four. Finding 4 attributed the founder's 'I EXCLUDE THE ELEMENTS FROM VIEW BUT THEY STILL RENDER' to `vgCategoryForLayer('projection-visible')` returning null, which 584aaab2 has now fixed; but this store-driven overlay is a SECOND, independent producer of the identical symptom that the layer fix cannot reach. Classifying A-LGHT does not make the blue 1.25 px symbol hideable. Finding 5's SymbolPen fix addresses the weight and colour but not the hide — the overlay needs the intent/VG visibility check too, or the founder will report the same thing again ([[same-rule-two-implementations]]).",
    "VERIFICATION NOTE ON FINDING 3 — I RE-DERIVED THE ARITHMETIC INDEPENDENTLY AND IT HOLDS. pen(0.09,'#6b7280',null,0.55) at PenWeightTable.ts:202-214, hard `ctx.fillStyle = '#ffffff'` at PlanViewCanvas.ts:373, room fill `opacity * 0.7` at :2517 with RoomColourSystem.defaultOpacity() = 0.35 (RoomColourSystem.ts:205-207) ⇒ 0.245. Compositing 0.319 coverage × 0.55 alpha of rgb(107,114,128) on white gives rgb(229,230,233), relative luminance 0.790, contrast 1.25:1 — the auditor's ~1.24:1. It is also robust to the disputed dpr: at dpr 1 it is 1.27:1. Commit 9babe6f5's body independently concedes this one as 'still real and still open', which makes it the highest-confidence unfixed item on the dimension."
  ]
}