{
  "survivingTitles": [
    "Linework line-width is pinned to a constant 96-DPI mm→px factor while the annotation layer in the SAME drawing is paper-scale-and-zoom-aware — the linework draws at 40-75% of its own paper weight on a fitted plan",
    "The stroke floor and its merge-blocking guard measure BACKING-STORE pixels and call them DEVICE pixels — on the founder's dpr 0.9375 display the guard is 3.2× optimistic and five of the eight pen rungs are sub-device-pixel",
    "The BEYOND zone renders at roughly 1.2:1 contrast — effectively invisible — because a 0.09 mm grey pen at 55% opacity lands on 0.319 device pixels",
    "[ALREADY FIXED AT HEAD by 584aaab2 — do not re-do] Lighting fixtures have NO ISO layer, so their 672-face tessellation projects onto `projection-visible` and draws at the __default__ (WALL) pen — 0.25 mm SOLID BLACK, heavier than a door, a stair or furniture",
    "`LightingPlanSymbolRenderer` hard-codes `ctx.lineWidth = 1.25` and a saturated UI blue `#0ea5e9` — a C09 §4.6.4b breach that makes a light fitting draw heavier than a projected wall",
    "STANDARD-vs-CODE GAP: SPEC-AUTODIMENSION §12.11 states the lineweight hierarchy and §13.3 records verbatim that it is NOT ENFORCED; C101 and C102 contain zero line-weight clauses; and no document anywhere states a MINIMUM on-screen weight or contrast",
    "C09 §4.6.4e's claim that 'the hierarchy is already right in EXPORT' is FALSE for the sheet/PDF path — the exported drawing has two weights, CUT equals PROJECTION, and the pen table is never consulted",
    "`purposeModifiers` can never reach the 2D pen — `resolveIntentPenStyle` drops the `viewPurpose` argument, so three of the five built-in purpose profiles are dead on the drawing"
  ],
  "refutedTitles": [],
  "refutations": [
    "FINDING 1 — SURVIVES, all citations verbatim. PlanViewCanvas.ts:519 `const _penPx = _pen.widthMm * SCREEN_PX_PER_MM;` and :524 `ctx.lineWidth = Math.max(hairline, _penPx * _vgFactor);` confirmed. DrawingConstants.ts:73 confirmed. paperScale.ts:107-115 `paperMmToPx` confirmed, and its header states the transform as normative. PlanViewAnnotationRenderer.ts:368-370 `_paperPx` and :438 `_scaleDenominator = resolveScaleDenominator(...)` / `_pxPerWorldM = pxPerWorldMetre(worldToScreen)` confirmed. worldToScreen (:1353-1364) does give pxPerWorldM = h_css/(2·_frustumH); fitToDrawing (:1614) is `max(3, min(80, max(height*0.57, width*0.57/aspect)))` with MINIMUM_PLAN_VIEW_CANVAS_FRUSTUM = 3 at :110 — I reproduced 51.2 px/m for a 12 m plan in a 700 px pane and 78.9 px/m for a 10 m plan in 900 px. I re-ran the negative grep: `output.scale|customScale|scaleDenominator` across views/ + drawing/ + presentation/ hits ONLY PlanViewAnnotationRenderer, ViewDefinitionStore and the sheet types — zero in the pen/stroke path. ONE CAVEAT the auditor should have stated: 'linework must scale with zoom' is a product choice, not a universal — AutoCAD model space displays lineweights at a fixed pixel width by default; Revit does what the auditor proposes (and has a Thin Lines toggle precisely because of it). What is NOT a choice is that ONE drawing runs two different paper-mm conversions, and the fix direction (1.89 → 2.55-3.95 CSS px for a cut wall on a fitted plan) matches the founder's complaint rather than fighting it.",
    "FINDING 2 — the DEFECT survives; the PROPOSED FIX is refuted, and by a lane that already shipped. Verified: setSize (:1833-1839) allocates cssW × resolveCanvasRenderScale(dpr) = cssW × 3 for any dpr < 3; render() (:371) installs setTransform(scale,…) so ctx.lineWidth is in CSS px; splitView.ts:467-475 sets the CSS box to `inset:0; width:100%; height:100%`, so the composited device width really is lineWidth × dpr, never × scale. CanvasRenderScale.ts:123-125 `minStrokePx(scale) { return 1 / Math.max(1, scale); }` sits under the literal header '**Exactly ONE DEVICE PIXEL.**' — it returns one BACKING pixel. CanvasHairlineFloor.test.ts:93 `widthDevice: ctx.lineWidth * scale` and :183-185 `expect(beyond!.widthDevice).toBeCloseTo(0.09 * SCREEN_PX_PER_MM * 3, 4) // ≈ 1.02` confirmed — the guard certifies 1.02 for a mark that is 0.34 device px at dpr 1. That is a gate blind on the wrong axis and it is real. THREE CORRECTIONS. (a) The proposed fix `minStrokePx = 1/max(1,dpr)` is WRONG and commit 9babe6f5 (L-13268) rejects it with arithmetic I reproduced: at dpr 1 that floor is 1.0 CSS px while wall=0.945, furniture=0.491, beyond=0.340 — every pen clamps and the ladder flattens completely, re-opening L-288. Fix the guard's unit and the test row; do NOT raise the floor. (b) 'five of the eight pen rungs' — SYSTEM_PEN_TABLE has SEVEN distinct widths (0.70/0.50/0.35/0.25/0.18/0.13/0.09) and FOUR of them (0.25/0.18/0.13/0.09) fall below one device pixel at dpr 0.9375; the auditor's own list under the claim has four entries, so the sentence contradicts itself. (c) dpr = 0.9375 is not established by the cited evidence — picking/src/gpu-pick.ts:840 merely reads devicePixelRatio, it does not record the founder's value. The finding does not need it: at dpr 1 the guard is still 3× optimistic.",
    "FINDING 3 — SURVIVES, and a sibling lane independently conceded it in the same hours. Every citation verbatim: PenWeightTable.ts:202-214 BEYOND is `pen(0.09, '#6b7280', null, 0.55)` for all thirteen categories; :151 `ceiling: pen(0.13, '#64748b')`; PlanViewCanvas.ts:516-517 `ctx.strokeStyle = vgEdge ?? _pen.color; ctx.globalAlpha = _pen.opacity;`; :373 `ctx.fillStyle = '#ffffff'` (hard literal, plus the CSS background); :2517 `ctx.globalAlpha = opacity * 0.7 * vgAlpha` with RoomColourSystem.defaultOpacity() = 0.35 at room-topology/src/RoomColourSystem.ts:205-207 ⇒ 0.245. I re-derived the blend independently: coverage 0.319 × alpha 0.55 = 0.175 of rgb(107,114,128) over white → rgb(229,230,233), ≈1.24:1 — the auditor's number. Render order confirmed: _renderRoomFills at :383, linework after, so fills are genuinely under and only raise local ground luminance. Commit 9babe6f5's own body states 'roughly 1.2:1 contrast' and 'STILL REAL AND STILL OPEN', which is corroboration from an independent read rather than an echo. This is the finding to act on first: it needs a decision (colour or alpha or width — pick one), not a patch.",
    "FINDING 4 — the diagnosis was CORRECT and is now IMPLEMENTED; one sub-claim is REFUTED. Commit 584aaab2 (L-13267, today 07:14) landed all four sites the finding named, including its exact proposed regex: EdgeProjectorService.ts:260 `Lighting: 'A-LGHT', LightingFixture: 'A-LGHT', light: 'A-LGHT'`; DrawingLayerIdentity.ts:128 `'A-LGHT': 'lighting'`; PenWeightTable.ts:461 `isLighting: /A-LGHT|lighting/i` with the `if (flags.isLighting) return 'lighting'` arm at :417 ordered after furniture; VisibilityIntentDefaults ELEMENT_TYPES gains 'lighting'. The pre-fix mechanism was accurate in every link — I verified FALLBACK_NATIVE_LAYER = 'projection-visible', categoryFromFlags' `return 'projection'` fallback at :418, the absence of a PROJECTION×projection row (⇒ FALLBACK_PEN 0.18 at :265), and the __default__ override: IntentRuleResolver.ts:106 falls back to `intent.elementRules.__default__` and VisibilityIntentDefaults.ts:104 seeds it as `elementType === '__default__' ? 'wall'`, with GraphicsRulesEngine.ts:197-204 confirming the intent tier at priority 1000 unconditionally overwrites resolved.widthMm. REFUTED SUB-CLAIM: 'very likely the same root as his separate I exclude the furniture elements from view but they still render report.' It is not. `Furniture / FurniturePart / GenericComponent / kitchen_unit → 'A-FURN'` are all present in ELEMENT_TYPE_TO_PROJECTION_LAYER, `'A-FURN': 'furniture'` is present in ISO_LAYER_TO_VG_CATEGORY, `isFurniture: /A-FURN|furniture/i` has its arm, and PlanViewCanvas.ts:458-463 does gate on it (`if (resolved && !resolved.visible) return;`). Furniture was never on the fallback path; that complaint has a different root and is still unexplained. ALSO STILL OPEN: the founder's actual ask. 584aaab2 states it does not add a lighting symbol and does not suppress the raw mesh, so the 672-face bursts still draw — now at 0.13 mm #303030 instead of 0.25 mm black.",
    "FINDING 5 — the CODE claim survives; the SYMPTOM is refuted. Verified verbatim: LightingPlanSymbolRenderer.ts:13-16 STROKE_DEFAULT '#0ea5e9' / FILL_DEFAULT 'rgba(14,165,233,0.12)' / STROKE_SELECTED '#7c3aed'; :100-104 `ctx.save(); ctx.strokeStyle = style.stroke; ctx.fillStyle = style.fill; ctx.lineWidth = 1.25;` with no SymbolPen in the signature at :88-94; wired at PlanViewCanvas.ts:660 and :2092 via _renderLightingPlanSymbols (:2292). The arithmetic holds — 1.25 CSS px against a projected wall's 0.945 — and C09 §4.6.4b at :870-899 says verbatim that a symbol renderer MUST receive a resolved SymbolPen and MUST NOT import the pen table or SCREEN_PX_PER_MM. So the breach is real. BUT the stated symptom ('Light fittings are drawn in bright sky-blue at a weight heavier than the walls') is not what the founder is seeing. renderLightingSymbols (:242-249) reads `window.lightingStore` and returns early on a missing or empty store; it is populated only by CreateLightingCommand / LightingPlanToolHandler (initBuilders.ts:914 sets the window handle). The founder's screenshot shows BLACK concentric bursts, the session log shows no LightingPlanSymbolBuilder, and 584aaab2's independent diagnosis names those as the 672-face edge-projected meshes. This is a latent contract breach that will bite the moment the symbol path is used — which is exactly the next increment — not the cause of the unreadable drawing. Fix it, but do not sell it as the founder's defect.",
    "FINDING 6 — SURVIVES, every citation checked. SPEC-AUTODIMENSION.md:345-358 carries the §12.11 table (Cut walls Heavy … Room fills Very light) and closes with '⛔ **Annotations must never visually overpower cut walls.**'; :540 reads exactly '6. ⛔ **§12.11 lineweight hierarchy is not enforced.**'; :376-380 is the §12.14 precedent ('the absence of a number is what let the defect ship'). I re-ran the contract grep myself: C101 + C102 yield 2 hits, both incidental prose ('would make both unreadable' at C102:14, '@deprecated readable' at C102:76) — no line-weight clause in either. PlanViewAnnotationRenderer has 47 `ctx.lineWidth` assignments; exactly 3 (:1002, :1011, :1621) route through `_annotationLineWidthPx`, which at :499-503 itself uses the same zoom-blind `SCREEN_PX_PER_MM`; the rest are bare literals (3.5, 2.5, 2.4, 1.8, 1.5, 1.25, 1.2, 1, 0.8, 0.75). ONE SOFTENING: the cited `ctx.lineWidth = 3.5` at :986 is a selection halo drawn at `globalAlpha = 0.35` in #6600ff, so 'heavier than a 1.89 px cut wall' is true of width but overstated as 'overpowers' — it is translucent chrome. The structural point (no number exists, so nothing can regress-test) is untouched by that.",
    "FINDING 7 — SURVIVES, and the negative grep reproduces exactly. `PenWeightTable|resolvePen|graphicsRulesEngine` across packages/file-format/src and packages/drawing-primitives/src → ZERO hits. SVGCompositeRenderer.ts:85 `const PRYZM_WEIGHT_TO_MM = [0, 0.18, 0.25, 0.35, 0.50, 0.70, 1.00];` and :248-251 the rawWeight branch confirmed verbatim; since THREE's LineBasicMaterial defaults linewidth to 1 — inside the [1,6] guard — ISO_LINE_WEIGHTS is unreachable, as claimed. VGSceneApplicator.ts:665-667 `effectiveCutWeight = (style as any).cutLineWeight ?? style.lineWeight` / `effectiveProjWeight = (style as any).projectionLineWeight ?? style.lineWeight`, and I checked VGGovernanceStore's built-in categories (:130-149): `wall: { lineWeight: 2 }`, everything else 1, and NO category sets cutLineWeight or projectionLineWeight — so A-WALL:cut and A-WALL:proj both export at 0.25 mm. C09 §4.6.4e(a) at :786-788 reads verbatim as quoted. Reachability confirmed: SVGCompositeRenderer is instantiated live at ViewportSvgComposer.ts:231. TWO NUANCES the finding overstates: ViewportToSvg's `defaultStrokeMm: 50` applies only when a caller omits `line.strokeMm` (:187), and its only production entry is the dev-only pdfExportTestModal — so 'the second renderer is worse' is true of the code but not of the shipped sheet path. The core stands entirely on SVGCompositeRenderer.",
    "FINDING 8 — the MECHANISM survives and is in fact WORSE than stated; the SYMPTOM is refuted. Verified: IntentRuleResolver.ts:353-364 `resolveIntentPenStyle(intentInstance, intent, elementType, zone, viewType, target = {})` — six parameters, forwarding six to resolveIntentStyle; :190-193 gates purposeModifiers on the never-supplied 7th; GraphicsRulesEngine.ts:394-406 is the single call site and passes six; SystemIntents.ts:63-106 BUILT_IN_PURPOSE_MODIFIERS carries construction-docs/design-review/coordination/presentation exactly as described; the cache key at GraphicsRulesEngine.ts:161-165 does omit viewPurpose, so the auditor's warning about adding the argument without the key is correct. REFUTED SYMPTOM: 'Setting a view's purpose changes nothing about its linework' describes a control that does not exist. ViewDefinition carries `purpose?: 'construction-docs' | …` at ViewDefinitionTypes.ts:1040, but no UI writes it — the only matches are VisibilityIntentPanel.ts:873 (which AUTHORS a modifier) and SetViewSemanticsCommand, which writes `semantics.purpose`, a field ViewDefinitionTypes.ts:1037 explicitly distinguishes from this one. The user cannot reach the lever at all, so nothing 'reads as another broken setting'. UNDERSTATED HALF: the auditor implies the 3D path works. It does not — ThreeDAppearanceResolver.ts:59 reads `(view as any)?.viewPurpose`, and the field is `purpose`. The `as any` hides a property-name mismatch, so purposeModifiers are dead on BOTH paths and the whole feature has never executed once. That is a stronger, cheaper-to-prove version of the same finding."
  ],
  "missed": [
    "`A-AREA` IS THE LIGHTING DEFECT, UNFIXED — and it is the largest unclassified family left on a plan. `SpaceEnvelopePlanSymbolBuilder.ts:73` exports `SPACE_ENVELOPE_LAYER = 'A-AREA'` and emits level outlines and hatched room areas (shipped 2026-09-05, RESI-STAGE-G). `DrawingLayerIdentity.ts:128` gives it a VG row (`'A-AREA': 'spaceEnvelope'`) but `penCategoryForLayerTag` has NO arm for it, so `categoryFromFlags` falls through to `return 'projection'` (PenWeightTable.ts:418); `resolvePen('PROJECTION','projection')` has no such key so it returns FALLBACK_PEN (:265, 0.18 mm black); and then the intent tier overwrites even that — IntentRuleResolver.ts:106 falls back to `elementRules.__default__`, which VisibilityIntentDefaults.ts:104 seeds from the WALL category, at priority 1000 (GraphicsRulesEngine.ts:197-204). Net: every space-envelope line draws at 0.25 mm SOLID BLACK, the projected-wall weight. This is the exact chain L-13267 just fixed for lighting, one layer over, and the file records the gap in its own comment at DrawingLayerIdentity.ts:122-124 ('⚠ `penCategoryForLayerTag` has NO arm for this tag yet, so the strokes take the generic fallback pen'). The auditor cited that comment as supporting evidence for finding 4 and never noticed it names a SECOND live instance. Fix is the same one line, plus a `spaceEnvelope` pen row.",
    "A SECOND STROKE SITE WITH A THIRD SET OF RULES, in the same file the audit read. `PlanViewCanvas.ts:2052` (the pipeline/worker result path) strokes `ctx.lineWidth = Math.max(hairline, edge.widthMm * SCREEN_PX_PER_MM)` — same constant conversion as :519, but with NO `_vgFactor`, which :524 applies. The file's own comment at :2043-2046 records that the worker resolves pens through its own mini rules engine rather than `graphicsRulesEngine.resolveStyle()`, so the L-285 function axis never reaches this path either. Same drawing, two weight authorities, one of them missing two axes — and which one paints a given frame depends on whether the worker returned. Any fix to finding 1 or finding 2 applied at :519/:524 alone lands on one of two paths, which the file itself notes 'L-288 taught this file twice'.",
    "`handrail` HAS A CLASSIFIER ARM AND NO PEN. `categoryFromFlags` returns `'handrail'` (PenWeightTable.ts:411) and `penCategoryForLayerTag` tests `/A-HRAL|handrail/i` (:462), but SYSTEM_PEN_TABLE has no `handrail` row in ANY of the four zones, and `VisibilityIntentDefaults.ELEMENT_TYPES` has no 'handrail' entry. It is masked today only because `ELEMENT_TYPE_TO_PROJECTION_LAYER` routes `Handrail`, `HandrailPart`, `stair-railing` and `stairRailing` to `A-STRS`, so the arm is unreachable and railings borrow the stair pen. The instant anyone mints `A-HRAL` — and the regex already anticipates it — every railing drops to FALLBACK_PEN and then to the WALL `__default__`, exactly as lighting did. Latent, cheap, and worth closing in the same pass as A-AREA since it is one row in the same table.",
    "THE FOUNDER'S FIRST COMPLAINT IS STILL UNDIAGNOSED. 'I exclude the furniture elements from view but they still render' is not explained by any of the eight findings — I checked and refuted the one attempt (finding 4). Furniture is fully classified end to end, and PlanViewCanvas.ts:458-463 does honour `!resolved.visible`. Note what that gate actually reads: `this._styleResolver(vgCat, layerTag)` — the VG governance resolver — while the founder was operating the VISIBILITY INTENT panel, which is a different authority whose element rules reach the pen only through `_intentRules` (opacity/width), not through this early `return`. That is where I would point the next lane, but I am flagging it as an unproven lead rather than a finding: I did not confirm that an intent rule with `visible: false` fails to zero the stroke, and this dimension is not the right lens for it."
  ]
}