{
  "refuted": true,
  "reason": "MECHANISM CONFIRMED, ATTRIBUTION REFUTED. Every structural claim verified line-for-line: FurnitureFragmentBuilder.ts:63 (claim says 64, off-by-one) is `private _instancingEnabled: () => boolean = () => false;`; FurnitureInstanceBridge.ts:125-129 is a bare `globalThis.__pryzmFurnitureInstancingV1 === true` with no family table; a grep across apps/ packages/ plugins/ src/ server/ tools/ scripts/ finds ZERO production assignments (only FurnitureInstanceBridge.test.ts, plus pryzmPerfConsole.ts:913/1168 and tools/perf/render/render-profile.spec.ts:342 which only READ it); initBuilders.ts:817-820 injects unconditionally; FurnitureFragmentBuilder.ts:265 stamps both families from one line; ElementInstanceBridge.ts:332-338 + :363-385 is the exact precedent described. So \"the collapse mechanism is fully wired and the gate is dead in production\" is TRUE.\n\nWHAT REFUTES IT IS THE SAVING AND THE CAUSAL LINK. The measured symptom is HYDRATE=17163.5ms of a 17258.5ms load (99.4%). The proposed flag flip cannot reduce hydrate, and in fact increases it, because of the order of operations inside updateFurniture():\n\n1. `builder.build(resolved)` runs UNCONDITIONALLY at ~:236 (or wardrobeEngine.create at :233), inside the §FURN-3D-RESILIENCE try/catch. EVERY `new THREE.Mesh` and every BufferGeometry is allocated on BOTH paths.\n2. The userData traverse at :258-283 then walks every child on both paths.\n3. ONLY THEN, at :331, does `if (this._instanceBridge && this._instancingEnabled())` run.\n\nSo the instanced path is the fragment path PLUS extra work. FurnitureInstanceBridge._bakeParts (:333-376) traverses the group again, calls `m.geometry.clone()` on EVERY leaf (:358), `applyMatrix4` on each (:359), buckets by material uuid, runs `mergeGeometries` into fresh buffers (:391, with a position-only retry at :401-406), disposes the per-leaf clones (:412) — and then the caller at :366-370 traverses a THIRD time to `scheduleGpuRelease(child.geometry)` on all 1487+647 originals. Per item, flipping the flag strictly ADDS a clone + matrix-transform + merge + double-dispose over the geometry the builder just made. Hydrate goes UP, not down.\n\nThe 2000ms is therefore unsupported in both magnitude and sign. It is also arithmetically implausible from the claim's own numbers: the paste's one real per-mesh measurement is 7.7ms for a full-scene traverse of 3762 meshes (~2 µs/mesh), so deleting 2134 meshes saves ~4.3ms PER TRAVERSE — you would need ~470 full-scene traverses to reach 2000ms. From the other direction, 2134 mesh+geometry allocations at even a generous 0.1ms each is ~210ms, so furniture mesh construction cannot be the 17.1s regardless of which way the flag points. The claim's own evidence line concedes this (\"Real, but small in absolute ms\") and then the headline contradicts it.\n\nTWO SECONDARY CORRECTIONS (both in the claim's favour, and both showing the risk section was written from names not from the files):\n- WOULD-BREAK #3 is over-stated. All three named consumers ALREADY handle the 'Furniture' relabel that FurnitureInstanceBridge.register() stamps at :218: VGSceneApplicator.ts:126 maps `'Furniture'` and `'FurniturePart'` both to 'furniture'; EdgeProjectorService.ts:228 maps `Furniture: 'A-FURN', FurniturePart: 'A-FURN'`; shadowCasterPolicy.ts:107-111 FURNITURE_ELEMENT_TYPES contains 'furniture', 'furniturepart' AND 'kitchencabinetpart'. Nothing breaks there.\n- The group key is NOT uuid-based, so collapse would genuinely work: InstancedElementRenderer._hashGeometry (:806-818) keys on `elementType_levelId_idxCt_vtxCt_x0_y0_z0_material.uuid` — a content-ish signature on locally-baked geometry, so identical repeated items at different world positions DO share a group. (Minor latent hazard nobody has named: that hash would COLLIDE for two distinct merged furniture geometries with equal index/vertex counts and an equal first vertex, which is far likelier for 60 procedural builders emitting box kits than for the simple rails it was designed on.)\n\nWHAT THE CURRENT DEFAULT PROTECTS — the invariant the lens asks for, and it has NOT quietly become false. WOULD-BREAK #1 and #2 are both real and both confirmed in code. (a) FurnitureFragmentBuilder.ts:280 sets `child.castShadow = furnitureCastsShadowUnderBudget(data.furnitureType)` per part (ADR-0076 Axis 2; the paste shows \"demoted 66 by family policy\"), while InstanceGroup.ts:87-88 hard-sets `castShadow = true` on every InstancedMesh — and shadowCasterPolicy's own rule 4 comment says \"furniture / lighting — the builder owns the per-mesh choice\", which an aggregate cannot honour. Flipping the flag silently re-promotes demoted plants/lamps/rugs/curtains. (b) The furniture instanced branch already dodges half of ADR-0297 deliberately (:355-364: \"MATERIALS ARE NOT RELEASED AT ALL (invariant L1)\"), but the surrounding comment at :185-196 enumerates SIX module-level furniture material caches (KitchenCabinetEngine._matCache, WardrobeCabinetEngine._matCache, ParametricTreeEngine._matCache, foliageCards._cardMatCache/_shellMatCache/_texCache, AIElementEngine.materialCache) whose ownership this exact code path got wrong once already — and beam/column are STILL false in _FAMILY_DEFAULTS for that same open hazard. The precedent the claim wants to copy carries its own refusal: ElementInstanceBridge.ts:330-331, \"⛔ Do NOT flip a family here without the test that proves its delete path, and do not flip one on a reason — flip it on a number.\" This claim flips it on a reason, with an estimated number, against furniture — the highest delete/edit-churn family in the app.\n\nVERDICT: the dead-flag finding is real and worth a lane, but it is a FRAME-TIME and DRAW-CALL/caster-count change, not a load-time one. Sold as \"2000ms off a 17s load\" it would ship, measure nothing, and carry a visible shadow-fidelity regression.",
  "betterHypothesis": "Split the claim in two and re-price both.\n\n(1) HYDRATE (17163.5ms, 99.4% of load) — NOT mesh multiplication. Mesh construction is bounded at ~10^2 ms for 2134 meshes and the flag flip would make it slightly worse, so the 17s lives elsewhere and is currently unmeasured. The instrument already exists and was simply absent from the founder's paste: ProjectLoader.ts:549-566 defines __mark/__marksSummary and emits `[ProjectLoader] §LOAD-HYDRATE-STEPS ...` at :564. Get that ONE line before any load-time work. The paste's own super-linear suspects, all of which are whole-model recomputations running repeatedly DURING a 550-element load, outrank mesh count by inspection: TopologyLayer adjacency rebuilt 4+ times over 528 elements / 2835 edges; CommandManager EXECUTE: REDETECT_ROOMS firing 5+ times after load; StoreEventBus flushing 881 buffered events \"in emission order (no coalescing)\"; temporalGraph 2366 mutations; 90 curtain walls each running CurtainWallInstanceManager. A repeated O(n·k) sweep is the classic 17s hydrate shape; 2134 allocations is not.\n\n(2) THE FURNITURE FLAG — keep it, re-scope it, and fix the fidelity bug FIRST. Its honest value is draw calls, full-scene traverse cost (~4.3ms per traverse at the measured 2 µs/mesh) and caster count (FurniturePart alone is 1421 of 3274 casters, and each InstanceGroup submits as one caster). That is a frame-time and shadow-pass win, not a load win, and it should be pitched as fps. Before flipping the default, the shadow demotion must survive instancing: FurnitureFragmentBuilder.ts:280 computes furnitureCastsShadowUnderBudget per part and InstanceGroup.ts:87-88 overwrites it with true. Fix = carry the cast/receive verdict into the group key (or set the InstancedMesh flags from its members' verdict) so a demoted rug does not start casting the moment it instances — otherwise the flip both changes visible output and cancels part of its own caster-count benefit.\n\nSequencing, honouring ElementInstanceBridge.ts:330-331 (\"flip it on a number\", \"not without the test that proves its delete path\"):\n  STEP 0 — the claim's STEP 0 is right and costs nothing: set the global in the console, reload, capture BOTH `window.pryzmPerf.census()` and the §LOAD-HYDRATE-STEPS line. Expect hydrate flat-to-worse and frame time better. That single paste settles the split above.\n  STEP 1 — fix the shadow-verdict carry in the bridge/group.\n  STEP 2 — write the furniture delete/edit-churn test that beam and column are still waiting on (ADR-0297 L2(b) dispose-in-place), since furniture churns harder than either.\n  STEP 3 — only then add the default row, moving pryzmPerfConsole.ts:1168-1170 in the same commit.\n\nAlso drop the \"same fix covers KitchenCabinetPart\" framing to a claim about the STAMP only: the two families share FurnitureFragmentBuilder.ts:265 and therefore the flag, but they do not share eligibility. _bakeParts returns null for any leaf with an Array material and for any unmergeable bucket, declining the WHOLE item — so the realised mesh saving is whatever survives that filter, not 2134 by construction. Measure the post-flip census before quoting 57%."
}