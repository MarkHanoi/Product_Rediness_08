{
  "survivingTitles": [
    "The heavy-scene backend swap fires MID-HYDRATION, so every GPU resource is minted on WebGPU and then re-minted on a classic WebGL renderer — this is the sole source of the 'device lost: destroyed' pair",
    "Two full-project serializes on every open — the `resetDirtyAfterLoad()` hash baseline is discarded by the auto-save that follows",
    "captureThumbnail runs twice per open from two uncoordinated schedulers, and each run forces a synchronous full-scene render plus a GPU readback",
    "Two socket.io clients connect and join the same project room — two call sites, two websockets, two server-side access checks",
    "NOT A DEFECT — the duplicated 'WebGPU device lost' log line is two handlers on ONE device, and both correctly refuse to recover"
  ],
  "refutedTitles": [],
  "refutations": [
    "#1 TITLE MECHANISM IS FALSE (the finding survives; the stated trigger does not). The per-geometry-add tier pass CANNOT fire during hydration: `initScene.ts:3070` reads `if (shouldDeferPerAddGeometryPass(batchCoordinator.isBatching)) return;` and `perAddGeometryGate.ts:44` returns true whenever `isProjectLoadActive()` — that is §FIX-LOAD-TRAVERSE-BATCH, whose whole purpose is to kill the O(n²) per-add traverse during load. The reachable trigger is the CONSOLIDATED pass, `_runConsolidatedTierPbrPass = () => runTierPbrPass('post-load')` (initScene.ts:3038), fired from the `pryzm-project-loaded` listener — which is exactly why the auditor's own cited evidence reads `reason=tier:post-load`. They quoted the line that refutes their sentence. Consequence: it is not 'the mesh arm is crossed early during hydration'; the swap fires AFTER all 3,762 meshes exist and have rendered, so 100% of the WebGPU minting is discarded, not a fraction. A lane sent at `initScene.ts:3008` will find it dead during load and may wrongly conclude the finding is bogus.",
    "#1 IS NOT NEW — it is ISSUE-LOG L-1414, OPEN, MEASURED 2026-08-20 (lane SWAP1), docs/04-reference/ISSUE-LOG.md:22287: `reason=tier:post-load` means the swap decision fires **after** all meshes exist and have rendered … the swap buys nothing it was designed to buy, while paying the maximum possible price … **It fires on every project open above the threshold — this is not an edge case, it is the default path for any real project.** The auditor cited L-1483 (line 23365) but not L-1414 (the row that already owns this diagnosis and already priced the fix). Re-filing it as new work risks a duplicate lane.",
    "#1 FIX HAS A UNIT DEFECT L-1414 ALREADY REFUSED. 'Wire the pre-emptive decision from the snapshot's elementCount (503)' feeds a DIFFERENT POPULATION into a calibrated constant: `SWAP_ELEMENT_THRESHOLD = 400` is documented (heavySceneSwapThreshold.ts:39) as '≥ this many top-level BIM element ROOTS', and `countElements()` (autoWebGLHeavyScene.ts:152-162) counts only top-level `scene.children` carrying `userData.id`/`levelId`. On THIS project the founder's own census reads 162 element roots against a 503-element snapshot — a 3× mismatch. A 450-element / 600-mesh project would then swap for nothing. L-1414 records the refusal verbatim (C04 §INST.2, 'inventing a multiplier to make the timing work trades a timing defect for an arbitrary-constant defect') and names the honest exit instead: persist the PREVIOUS session's MEASURED mesh count per project and refuse (stay reactive) on first open. The auditor's parenthetical 'or a stored per-project mesh estimate' is the right half; the elementCount half must not ship.",
    "#1 OMITS THE CHEAPER CANDIDATE FIX AND ITS COST. At `tier:post-load` the WebGPU device has ALREADY survived the PSO-compile storm the swap exists to pre-empt (ADR-0267's premise is 'swap BEFORE'). So 'skip the swap when it would fire post-load' is a live option that adds no constant and removes the whole 1500 ms — the auditor never weighs it. What it WOULD break, and why it is not free: the swap also pre-empts the LATER loss on a heavy scene during navigation/editing (L-382's 'Compiling GPU shaders' tail; §NAV-BACKEND-SWAP-HEADROOM records the founder crossing the mesh arm with ONE DOOR, 978→1005, and the backend swapping MID-EDIT). Both options must be costed; only one was.",
    "#3 'WOULD BREAK' IS FACTUALLY WRONG, IN THE SAFE DIRECTION. 'The auto-save capture is what puts a thumbnail INTO the VersionRecord' — it does not. The `VersionRecord` built at PlatformSaveController.ts:332-340 has no thumbnail field; `capturedThumb` is consumed ONLY at :388 as `thumbnail: capturedThumb ?? existingMeta?.thumbnail` inside `ProjectMeta` — the SAME destination the post-load path writes at :566 (`projectRepository.saveProject({ ...existing, thumbnail: thumb })`). The two writers are interchangeable, so the stated risk does not exist and the fix is safer than advertised.",
    "#3 'ONE OF THE TWO SITS ON THE OPEN CRITICAL PATH' IS WRONG — the 300 ms does not come off time-to-open. `setLoading(false)` sets `_settleUntil = Date.now() + 4000` (SaveOrchestrator.ts:473), and `executeSave()` explicitly re-defers to that deadline (SaveOrchestrator.ts:372-379) on top of the 2500 ms debounce. So the auto-save capture lands at ~T+4 s and the post-load capture at ~T+3.5 s + `requestIdleCallback`. BOTH are post-open jank in the same window, neither blocks the open. The founder's complaint is 'TAKES TOO LONG TO OPEN'; fixing this improves first-interaction smoothness, not open latency. Rank it accordingly.",
    "#3 FIX IS CONSTRAINED BY AN EXISTING OPEN ROW THE AUDITOR DID NOT CITE: L-939 (ISSUE-LOG:5915) — `captureThumbnail()` is a SECOND render driver: a synchronous, off-rAF `render()` that drains the ADR-0297 L2 boundary, and it states explicitly **'Do NOT fix it by deleting the call'** (the thumbnail needs a fresh frame or it captures a blank swapchain, initPersistence.ts:235 §THUMB-BLANK-REUSE). Suppressing the auto-save CALLER is compatible with L-939; deleting the `rpm.render()` inside `captureThumbnail` is not. Say which one the lane is doing.",
    "#2 IS ALSO NOT NEW: L-307's ranked fix list (ISSUE-LOG:458) already carries '(4) autosave/serialize/thumbnail OFF the path (idle) … double-serialize in SaveOrchestrator'. It survives as a real cost, but file it against the existing row.",
    "#2 'DELETE THE BASELINE CALL ENTIRELY' OVER-DELETES. `resetDirtyAfterLoad()` does more than compute a hash — `markClean(hash)` also clears `hasDirtyChanges`, calls `cancelDebounce()` and sets status 'idle' (SaveOrchestrator.ts:571-582). Deleting the call leaves the toolbar reading 'unsaved' the moment an open finishes. The surgical version costing the same zero is the one the function's own catch branch already takes: call `markClean()` with NO argument. `setLoading(true)` has already zeroed `lastHash` (SaveOrchestrator.ts:465), so the `lastHash === ''` branch at :383 is live and the first `executeSave()` establishes the baseline for free. Bonus the auditor missed: dropping the baseline `getHash()` also stops it stashing a `_autosaveSerialization` that is stale by the time any non-`executeSave` autosave caller (e.g. PlatformSaveController.ts:748) consumes it.",
    "PRE-EMPTIVE ANTI-FINDING — do NOT send a lane at the loudest number in the founder's log. `PERF-CACHE-STATS … hitRate=0%` is a COLD CACHE, not duplicated work. `_putCwCache` is an in-memory, per-session map keyed by element UUID + viewId + geometry version + clipSignature (EdgeProjectorService.ts:3515), so 0 hits / 63 misses on the FIRST projection of a fresh session is the only possible reading. The log shows exactly one `PERF-EDGEPROJECTOR-CHUNK` line, i.e. one projection ran. A second projection in the same open would show a HIGH hit rate, and that is the probe if anyone doubts it."
  ],
  "missed": [
    "THE OPEN WRITES BACK A VERSION THE USER NEVER EDITED — the auditor found the double serialize but treated the save that follows it as a given. It is not: it is armed by the loader's own deterministic post-load sweep. `handleMutation()` (SaveOrchestrator.ts:303) takes the `_loadSuppressActive` branch during the sweep and sets `hasDirtyChanges = true`; `handleLoadSuppressEnd()` (SaveOrchestrator.ts:536) then arms 'exactly ONE coalesced autosave so the just-settled project is persisted'. So EVERY open above the settle threshold repays the whole save — serialize #2, a main-thread deflate, a full ~1.4 MB container re-assemble + IndexedDB put (`_commitSlots`, ProjectRepository.ts:2777), a thumbnail, and a server enqueue — for a model derived DETERMINISTICALLY from the one just read, and it burns one of the 20 retained version slots per open. The founder's log is the proof: 'Version saved: \"Auto-save\" (503 elements)' with no user edit. Probe before fixing: open the same project twice and compare the two post-sweep hashes. If they match, the sweep is reproducible and the save is pure duplicate work that should be gated on the sweep having produced something the loader cannot re-derive. If they differ, find out WHY the sweep is nondeterministic — that is a bigger bug than the save.",
    "TWO SERVER THUMBNAIL UPLOADS PER OPEN, not just two captures. `_uploadThumbnailToServer` is called from BOTH duplicate paths: PlatformSaveController.ts:397 (auto-save, `if (capturedThumb)`) and PlatformSaveController.ts:567 (post-load). Same projectId, same ~5-20 KB WebP, seconds apart, both `POST`ing to the same endpoint. The auditor noted the post-load path 'already uploads to the server' as an argument FOR their fix but never registered that today both do — so the duplicate is one capture, one encode ladder, one readback AND one network round-trip more than named.",
    "THE CONSOLIDATED POST-LOAD PASS WALKS THE WHOLE SCENE TWICE BACK-TO-BACK — and this is the pass that hosts the backend swap, i.e. it runs at the single most contended moment of the open. `runTierPbrPass` (initScene.ts:2946) calls `collectNewPbrMeshes(scene)` (a full `scene.traverse`, :2952-2953) and then immediately `countMeshes()` (a second full `scene.traverse` of the same ~5,400 objects, :2965-2971). One walk could produce both the fresh-mesh list and the mesh count. Two more identical walks follow in the same window: `batchCoordinator.setSceneMeshCountProvider` (initScene.ts:3087-3096) and `censusScene` one second after the swap. Individually a few ms; collectively four walks of a 3,762-mesh scene where one would do, all inside the open.",
    "THE DUPLICATE SOCKET IS NOT ONLY A 40 ms COST — IT CAN INJECT A PHANTOM SELF-COLLABORATOR. `server.js:613` fires `socket.to('project:'+id).emit('user-joined', { userId: socket.data.userId })` on every join, and `initCollaboration.ts:614` handles `user-joined` with NO self-filter (there is no `myUserId`/`currentUserId` anywhere in that file — I grepped), emitting `pryzm-presence-added`, which `PlatformCollabPill.ts:129` renders. Whichever of the user's TWO sockets joins second announces the user to their own other socket. Today the pill (setProjectContext) usually beats initCollaboration (pryzm-project-loaded) so the notification lands on the handler-less socket and is swallowed — but that ordering is a websocket-connect race, not a guarantee. If it inverts, the user sees themselves in the presence strip. This raises finding #4 from a perf nit to a correctness argument for collapsing the two clients, and it means the fix must ALSO add the self-filter that is missing today."
  ]
}