{
  "survivingTitles": [
    "Every open awaits a full server snapshot fetch whose result is discarded when local history exists",
    "Opening an unedited project writes a brand-new saved version: full serialize + deflate + IndexedDB write + server queue",
    "The whole model is serialized a SECOND time on the open path just to produce a dirty-check baseline",
    "The project thumbnail is captured twice per open, and one of the two is on the critical path",
    "Two socket.io clients connect and both emit join-project on every open — one is tagged legacy",
    "ProjectIsolationAudit flattens the entire THREE scene on every project load — and the comment that says otherwise is stale",
    "DEAD: a second complete ProjectSerializer + ProjectLoader + MigrationEngine (~2,970 lines) that no production file imports",
    "DEAD BY DEFAULT: a 945-line legacy per-element load branch preserved inside the open-path loader",
    "Gate-debt ledger: the two ledgered gates both name legacy channels that run on the open path, and one is now WORSE THAN DECLARED"
  ],
  "refutedTitles": [
    "lineworkProbe does a second full-scene traverse on every 3-D activation, installed unconditionally in production"
  ],
  "refutations": [
    "REFUTED #7 (lineworkProbe) — the finding is true in the small and wrong in the aim, and its fix names a switch that does not exist. (i) The fix says 'gate the install on the same diagnostics switch that pryzmPerfConsole uses'. There is no such switch. `installPryzmPerfConsole()` (pryzmPerfConsole.ts:1620) is unconditional and only hangs an API on globalThis — and the census it is praised for keeping console-only is NO LONGER console-only: `logSceneCensusOnce` is wired at apps/editor/src/engine/initScene.ts:5182 and auto-fires one second after every renderer live swap. The founder's own `[pryzmPerf] NAV-BACKEND-SWAP-HEADROOM` and `(unattributed)` lines ARE that automatic census. The instrument held up as the model of restraint runs itself on the open path. (ii) The probe is not 'a second' traverse; it is the fourth on that one event. initScene registers three `view-activated` gates that each `scene.traverse()` the WHOLE graph (initScene.ts:962 floor hatch, :1021 room overlay, :1080), and the file's own §VIEW-GATE-NO-OP comment states that the first activation of a session NEVER skips. lineworkProbe.ts:65 says so itself: 'the same order as the three overlay gates initScene already runs on that event.' Gating the smallest of four identical walks, and leaving three larger ones, is the wrong target. (iii) The founder's paste contains no `[linework-probe]` line at all — not the install line, not a census — so nothing in the trace shows it ran on his open. The real item is the four-traverse cluster; see missed.",
    "CORRECTION to #1 (survives) — fix (a) is not implementable as written. `versionRepository` is defined in apps/editor/src/ui/platform/ProjectRepository.ts (L7); `buildPersistence.ts` is packages/runtime-composer (L3). Moving the local-history probe UP into `openProject` makes the composition root import from the app it composes. The workable shapes are fix (b) (fire streamLoad, hand setProjectContext a promise) or adding a `hasLocalVersion(projectId)` probe to the PersistenceSlot/WorkspaceSurface interface so the direction stays downward. Everything else in #1 checks out: `bundle` has exactly one consumer (buildPersistence.ts:329→337), `prefetchedVersion` has exactly one production reader (PlatformShell.ts:322, inside the `else` at :320), and I confirmed there is ZERO `markStartupPhase` in buildPersistence.ts — the leg really is unbracketed, so 800 ms is an estimate with nothing behind it. Instrument before cutting.",
    "CORRECTION to #2 (survives) — one sub-claim is FALSE and it takes half the fix with it. The mechanism reads: 'the skip-guard cannot help on a cold open: `lastHash` starts `''`, so the first save of a session is unconditional.' Not so. `PlatformVersionController.loadVersion` calls `this.saveCtrl.orchestrator.resetDirtyAfterLoad()` immediately after `setLoading(false)` (:411-412), which is `markClean(getHash())` — lastHash IS seeded, and it is seeded BEFORE the post-load sweep runs and before `handleLoadSuppressEnd` re-arms. The save fires because the sweep genuinely moved the hash, not because the guard was unarmed. Consequence: fix (1) ('seed lastHash from the snapshot the loader just consumed') does not remove the write, and is strictly worse than today — `executeSave` compares `currentHash === this.lastHash` where currentHash is the full `stringify(serialize(liveStores))`, so a `computeSnapshotChecksum` baseline is a different value space and can NEVER match. 'Either half alone removes the write' is false; only half (2) can. And half (2) is bigger than described: MUTATION_EVENTS are bare `window` CustomEvents with no source field (SaveOrchestrator.ts:281, handleMutation :303-315), so tagging needs a new channel or a sweep sub-latch, not a `source:` on an existing payload.",
    "CORRECTION to #3 (survives) — the mechanism is confirmed (resetDirtyAfterLoad → getHash → `ctx.saveAdapter.serialize()` + `stringify()`, no artefact produced) but the FIX is broken and the finding double-books with #2. Passing a checksum into `markClean(hash)` defeats the guard for the rest of the session's first save cycle, for the reason above. And the two findings cannot both be banked: if #2's fix (2) lands, no autosave fires on open and `resetDirtyAfterLoad`'s serialize becomes the ONLY one — and it is then required, not redundant. If #3's fix lands first, it re-introduces the spurious save #2 exists to remove. Bank one of the two, at most, and do not add 1200 + 450 together. Also: the audit cites SaveOrchestrator.ts:244 ('a FULL-project serialize … TWICE per fire') as the code's own measurement. That comment is STALE — §PERF-AUTOSAVE-SINGLE-SERIALIZE (L-131 P3b, PlatformSaveController.ts:59-77) made an autosave ONE serialize via `_autosaveSerialization`. The '793 elements → ~16.6 MB' size datum is still usable; the 'twice' is not.",
    "CORRECTION to #5 (survives, but not as latency) — the duplication is real and the retirement is safe: `ctx.socket` has exactly one reader outside PlatformCollabPill (PlatformShell.ts:537-539, the disconnect), and the pill's socket carries a single `version-saved` listener. But the 50 ms open-time figure is not credible: both `io()` connections are asynchronous and nothing in the open path awaits either one. The real win is one fewer WebSocket per session and one fewer server-side `getSupabaseClient()` + `canUserAccessProject()` per open (server.js:576-587). Sell it as legacy retirement and server load, not as open latency, or the next lane will measure no change and conclude the audit was wrong.",
    "CORRECTION to #9 (survives) — 'move it behind a dynamic `import()`' is not the small option it is presented as. The legacy branch lives INSIDE `ProjectLoader.load()` (959-1904) and closes over roughly two dozen locals — `exec`, `LOAD_META`, `result`, `cancelled()`, `snapshot`, `__closeAutosaveSuppress`, the topology-observer handle — so extracting it to a lazily-imported module is a real refactor with a wide parameter surface. Worse, a dynamically-imported rollback path needs a network chunk fetch at exactly the moment you want it (a broken open, possibly offline, possibly a bad deploy). If the rollback is still wanted, keep it static; if it is not, delete it. Do not lazy-load an escape hatch.",
    "CORRECTION to #10 (survives on measurement, FIX ACTION (1) IS DESTRUCTIVE) — I re-ran both gates and both readings are exact: `npx tsx tools/ga-gate/check-custom-event-apps.ts` → RC=3, '[custom-event-apps] FAIL (ratchet): 30 > baseline 4.' and '❌ WORSE THAN DECLARED (exit 3): 30 > ledgered level 20.'; `node scripts/check/ci-check-no-commandmanager.mjs` → RC=0, 'PASS -- count 134 <= threshold 134', '0 headroom remaining.' But the prescription 'start with the 4 dual-dispatch one-liners, which are pure duplication: the `runtime.events.emit` on the same line already does the job' is FALSE and would break navigation. `PlatformRouter.ts:292` is `window.addEventListener('pryzm-go-hub', …)` and `:348` is `window.addEventListener('pryzm-sign-out', …)` — the platform-lifetime WINDOW bus, not `runtime.events`. Every one of those four call sites says so in its own trailing comment ('§33-NAV-FIX — PlatformRouter listens on the platform-lifetime window bus'), and `apps/editor/src/ui/platform/__tests__/perfHubBackClickSplit.spec.ts:80,83` pins both listeners by source text. Deleting the `window.dispatchEvent` half kills Back-to-hub and Sign-out. This is precisely the failure the lens is looking for: a guard whose justification is STILL TRUE, proposed for removal because the pairing looks redundant. Order of operations: migrate PlatformRouter's two listeners to `runtime.events.on` FIRST, in the same commit, then drop the dispatches."
  ],
  "missed": [
    "The pryzmPerf scene census auto-runs on the open path, and its own header says otherwise. `printSceneCensus()` walks the entire graph (`censusScene`, pryzmPerfConsole.ts:425) and prints ~10 lines plus one row per family. Its doc says 'Printed once per project load, and on demand via window.pryzmPerf.census()' and '⛔ ONE TRAVERSE, ON AN EXPLICIT EVENT, NEVER ON A TIMER' — but its only non-console wiring is initScene.ts:5182, `logSceneCensusOnce(window.scene, 'one-second-after-swap')`, fired 1 s after a renderer live swap. The founder's `[pryzmPerf] NAV-BACKEND-SWAP-HEADROOM … 3762 meshes (ARM TRIPPED)` and `(unattributed)` lines are that census running on HIS open. The audit quotes those lines as a mesh-count datapoint and quotes the module as the model of restraint, and misses that it is a fifth uninvited whole-scene walk. Same stale-comment class as the initTools.ts:3890 line finding 6 correctly flags.",
    "The three `view-activated` whole-scene visibility gates in initScene.ts — floor hatch (:962), room overlay (:1021) and the third gate (:1080) — each `scene.traverse()` the ENTIRE graph, and the §VIEW-GATE-NO-OP guard at :943-960 deliberately never skips the first activation of a session ('the first activation always runs, because `_lastX` is seeded with a guess'). Those three, plus ProjectIsolationAudit and pryzmPerf's census, are FIVE whole-scene walks around one open — the lineworkProbe finding names the smallest one. The tractable fix is the cluster, not the probe: one traverse, one visitor, three predicates. The file already carries the measured framing (6113 meshes × ~24 no-op walks under `triggerFurnishAllFloors`), so the instrumentation to prove it exists.",
    "The renderer backend LIVE-SWAPS during the founder's open and the audit does not mention it. `WebGPU device lost: reason=\"destroyed\"` appears twice in his paste, and §RENDERER-LIVE-SWAP (initScene.ts:5120-5190) retires the old renderer, disposes its canvas, clears the OBC framebuffer, restarts the frame loop and re-mints all draw state — for a 3762-mesh scene. The auto guard arms at ≥400 elements OR ≥1000 meshes, i.e. it trips on essentially every real project the moment hydration finishes, so every large open pays a renderer teardown + rebuild. Whatever the other findings are worth, this one is structural and unbudgeted.",
    "The dead shadow copy in `packages/persistence-client/src/loader/` is bigger than finding 8 states: add `SnapshotStreaming.ts` (398 lines — duplicated at `apps/editor/src/engine/persistence/SnapshotStreaming.ts`, which is the copy the tests import) and `loader/migrations/` (`ViewTemplateToIntentMigration.ts` 252 lines + a 9-line `VGToIntentMigration.ts` whose real owner is `packages/core-app-model/src/persistence/migrations/`). That is ~3,629 dead lines, not 2,970. `SnapshotStreaming.ts:42` also matters for the deletion order: it is the ONE file with a runtime VALUE import of the dead serializer (`import { SNAPSHOT_SCHEMA_VERSION } from './ProjectSerializer'`), so a naive reachability sweep can report `loader/ProjectSerializer.ts` as live. It is not — SnapshotStreaming has zero importers itself.",
    "`PlatformShell._loadLatestVersionFromServer` (:465) and `tier.streamLoad` (buildPersistence.ts:153) are two independent implementations of the SAME request — both `GET /api/projects/<id>/latest-version`, both `res.json()` the whole snapshot on the main thread, both build the same VersionRecord shape. Finding 1 mentions the former only as a fallback to preserve. It is also the second half of a `[[same-rule-two-implementations]]` pair on the open path, and it is why the discarded fetch in finding 1 was never noticed: the code already had a working server-fetch leg when the Wave-7 tier leg was bolted on in front of it. Whichever fix lands for finding 1, collapse the two rather than leaving one 'as the fallback'."
  ]
}