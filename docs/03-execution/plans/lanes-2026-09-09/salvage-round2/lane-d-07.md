{
  "refuted": true,
  "reason": "The MECHANISM verifies; the 16,011 ms SAVING does not, and the arithmetic breaks three ways.\n\nCONFIRMED FROM CODE: initScene.ts:3038 mints `runTierPbrPass('post-load')`, fired from a second `pryzm-project-loaded` listener in setTimeout(0) (~:3752-3768); the guard call is exactly at :3008 with meshCount from the traverse above. Thresholds 400/1000 are at heavySceneSwapThreshold.ts:40,43 and isSwapWorthyHeavyScene ORs them. The open path really does have ONE guard: proactivelySwitchToWebGLForBuildingGeneration is called only from buildingGenerationLifecycle.ts:307, and the THIRD site the claim glosses (initBatchLifecycle.ts:121, `gpu-compile-start`) is unreachable on open because ProjectLoader batches via storeEventBus.beginBatch()/endBatch() (ProjectLoader.ts:733/2896), never BatchCoordinator. `_webGpuActive = true` is at RenderPipelineManager.ts:763, after the !isWebGPU return at :715-761; scheduleShadowRebuild (:1957), onProjectLoaded (:3280) and _rebuildPipeline (:4912) are all guarded by it; dispose() nulls the pipeline at :3331-3340. C04 §1.4a is quoted verbatim and accurately. Minor citation drift (Phase-5 abort is at initScene.ts:2290, not :2411; swapMayPersistPreference is at createRenderer.ts:279, not :283) does not change the substance.\n\nWHY THE NUMBER IS REFUTED:\n\n(1) THE GAP IS MAIN-THREAD STARVATION, WHICH AN AWAITED GPU BUILD IS NOT. UnifiedFrameLoop.stop() (UnifiedFrameLoop.ts:302-309) says in its own doc it \"disposes the scheduler subscription but does NOT stop the scheduler's rAF pump\", and FrameScheduler.addTickListener ends with `this.wakeIfStopped()`. `open:first-interactive-frame` is a scheduleOnce armed in the SAME handler that stamps `open:snapshot-loaded` (PlatformShell.ts:200-206), so it fires on the very next tick unless synchronous JS holds the thread. A 15,628 ms gap therefore measures ~15.6 s of SYNCHRONOUS main-thread occupancy. An async WebGPU pipeline build that yields would let the tick through; if it genuinely blocked for 16 s it would appear as a LONGTASK, and this repo does log those (the paste's own comparanda: \"WebGPU PSO compile LONGTASK ~11 s\", \"768ms LONGTASK\", \"2,401 ms adapter LONGTASK\" at main.ts:622-624). No such line is in this run's paste.\n\n(2) THE 16,011.5 ms IS A WALL-CLOCK SPAN AROUND A PROMISE, MEASURED INSIDE THAT SATURATED WINDOW. It is `performance.now()` around `Promise.resolve(this._rebuildPipeline())` (RenderPipelineManager.ts:2033-2039). If the thread is blocked by other work, the continuation is delayed and the span inflates by exactly that blocking time — so the span is largely a PASSENGER of the very interval it is being used to explain. The same paste names the plausible occupants the fix does not touch: `endBatch()` flushing 881 buffered events \"in emission order (no coalescing)\", REDETECT_ROOMS ×5, TopologyLayer adjacency rebuilt 4+ times over 528 elements / 2,835 edges, plus the tier:post-load pass's own two full 3,762-mesh traversals and PBR upgrade. The claim concedes it \"cannot separate them from code\" — and then bills the whole span anyway.\n\n(3) INTERNAL ARITHMETIC CONTRADICTION — TWO 15-16 s OCCUPANTS, ONE 15,628 ms WINDOW. Part (b) places the 16,011 ms WebGPU rebuild in the post-load gap; part (c) places the classic renderer's first-frame WebGLProgram compile + geometry upload for 3,762 `instanced? NO` meshes \"squarely between open:snapshot-loaded and open:first-interactive-frame, the measured 15,628 ms gap\". Both cannot fill the same window, and the classic compile is precisely the half the fix RETAINS (C04 §1.4b confirms the classic renderer \"compiles WebGLPrograms from the same materials the scene still holds\"). Whatever share of the gap it owns is not saved. Separately, 16,011 > 15,628: the claimed saving exceeds the entire interval in which it could be realised.\n\n(4) MIS-ATTRIBUTION TO THE NAMED AREA. The swap does not CAUSE the 16 s — it only makes it WASTED. A WebGPU open with no swap at all pays the identical pipeline build. The swap's OWN cost is what \"backend-swap churn\" can bill: rpm.dispose(), a second canvas, one more `createRenderer` (the repo's own measured figure for building a renderer is the 2,401 ms prewarm LONGTASK), recoverPipelineOrBind, renderPerfService.rebind, device.destroy(), and three 3,762-mesh traverses — seconds at the outside, not 16. The proposed fix is not \"fire the guard earlier\"; it is \"never build WebGPU\", a different and larger change whose payoff is unmeasured.\n\n(5) THE FIX IS BOUNDED BY THE HALF IT DOES NOT TOUCH. HYDRATE=17,163.5 ms is 99.4% of load and ~47% of the 36.8 s to interactive, and nothing in this proposal addresses it. Also, the count needed for a pre-build decision is available at `hub:warm-versions-done` (before `hub:open-clicked`) but NOT before `prewarmRenderer()` in main.ts Phase B — so the prewarmed WebGPU renderer must be discarded and retired, re-exposing the 2,401 ms adapter LONGTASK on the open path, which the claim's own WOULD-BREAK #3 concedes is a real trade.\n\nVERDICT: the wiring defect is real, is exactly what C04 §1.4a already records as \"NOT DONE, and honestly so\", and the element arm (550 scene roots / 527 declared / 503 serialized, all >= 400) genuinely does retire §1.4a's stated mesh-count blocker for THIS run. Worth doing on correctness grounds. But it is not established as the cause of the 16,011 ms, and the 16,011 ms figure cannot be spent as a saving.",
  "betterHypothesis": "The 15,628 ms `open:snapshot-loaded -> open:first-interactive-frame` gap is SYNCHRONOUS MAIN-THREAD STARVATION, not a backend/GPU cost. Proof from code, not inference: `UnifiedFrameLoop.stop()` (packages/core-app-model/src/rendering/UnifiedFrameLoop.ts:302-309) leaves the FrameScheduler rAF pump running, and `FrameScheduler.addTickListener` calls `wakeIfStopped()`, so the `scheduleOnce('perf104-first-interactive-frame')` armed at PlatformShell.ts:200-206 would land on the next tick (~16 ms) if the thread were ever free. It did not land for 15.6 s, so JS held the thread for essentially the whole interval.\n\nThe occupants named in the same paste, none of which the backend change touches: `storeEventBus.endBatch()` flushing 881 buffered events \"in emission order (no coalescing)\" (ProjectLoader.ts:2896); REDETECT_ROOMS x5; TopologyLayer adjacency rebuilt 4+ times over 528 elements / 2,835 edges; and the consolidated `tier:post-load` pass itself, which does TWO full 3,762-mesh `scene.traverse` passes plus a per-mesh PBR upgrade before it even reaches the guard at initScene.ts:3008. `SHADOW_REBUILD_COMPLETE elapsed=16011.5ms` is wall clock around an async promise sitting inside that window (RenderPipelineManager.ts:2033-2039) and is therefore mostly a symptom of the starvation, not its cause.\n\nNEXT STEP, in this order: (i) put a LONGTASK / self-profiling attribution across `open:snapshot-loaded -> open:first-interactive-frame` — one `PerformanceObserver({type:'longtask'})` window, printed with the top attributions — before spending any renderer work; the repo already logs LONGTASKs elsewhere, so this is instrumentation that exists, unreached. (ii) A/B the endBatch flush independently: 881 events with no coalescing over 528 elements re-entering 4 topology rebuilds and 5 REDETECT_ROOMS is the shape that produces a 15 s synchronous block, and it is testable at founder scale without touching the renderer. (iii) Keep the pre-build backend decision (C04 §1.4a) as a WASTE fix priced at the swap's own cost — one extra `createRenderer`, a dispose, a `device.destroy()`, three 3,762-mesh traverses, order seconds — plus the discarded prewarm it forces, not at 16,011 ms; and ship it only with the Phase-5 abort fix (`isWebGPUCapable` at initScene.ts:2290 must use `isUnintendedWebglOnlySwap`, or an intentional classic boot silently hands the viewport back to OBC)."
}