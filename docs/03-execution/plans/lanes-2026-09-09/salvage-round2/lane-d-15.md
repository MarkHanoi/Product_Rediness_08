{
  "refuted": false,
  "reason": "CONFIRMED on the core claim; the mechanism half is unproven and internally in tension with the harm half.\n\nWHAT I VERIFIED IN THE CODE (near-verbatim, in C:\\Users\\LENOVO\\OneDrive\\Desktop\\PRYZM\\Product_Rediness_08):\n\n1. THE TIMER TIMES A PIPELINE REBUILD, NOT A SHADOW PASS. `packages\\renderer-three\\src\\pipeline\\RenderPipelineManager.ts:2034-2038` wraps exactly `Promise.resolve(this._rebuildPipeline())`. Chain: `_rebuildPipeline()` :4911 -> `_rebuildPipelineWithCurrentState()` :4893 -> `_buildPipeline()` :3753 (the phase-3 branch is unreachable: `ssgi: false` at all four tiers, SceneQualityTierManager.ts:199/209/220/232, verified).\n\n2. NOTHING IN THE WINDOW SCALES WITH 3,762 MESHES. `createScenePass` is `pass(scene,camera)` at ScenePass.ts:67 (claim said :59) with no traverse; `createZonePass` ZonePass.ts:59-78 likewise; `_setScenePass` :3443-3456 is four field writes; `_safeDisposeRenderPipeline` :4781 calls `dispose?.()` and the INSTALLED three@0.183.2 `dispose()` is one line — confirmed in BOTH `node_modules\\three\\src\\renderers\\common\\RenderPipeline.js:151-153` AND the shipped bundle `node_modules\\three\\build\\three.webgpu.js:81379`. The in-file comment at :4787-4792 claiming dispose \"fans out to NodeManager.delete(renderObject) for every render object\" is false for this version — the claim is right to rule it out. `_emitState` :5451 is three DOMEventBus emits.\n\n3. THE ONLY SUSPENSION POINT IS THE DYNAMIC IMPORT AT :3763, AND THE MODULE IS ALREADY RESIDENT. `WebGPURendererAdapter.ts:139` is `await import('three/webgpu')` on the live-renderer path; `vite.config.ts:569` `if (id.includes('node_modules/three/')) return 'vendor-three'` — one chunk. Confirmed.\n\n4. THE HARM IS EXACTLY AS DESCRIBED. `_beginShadowRebuildGuard()` :1738-1742 is taken at :1991 (and nested at :4936) and released only in `_finishRebuildAndDrainQueue` :2053 (release at :2057 — claim said :2047, drift of 6). `render()` :1257 `if (this._shadowRebuildPaused) return this._skipFrame('shadowRebuildPaused')`. The \"no second rAF\" comment at :1268-1288 is TRUE: `grep -rn setObcRenderCallback apps packages plugins` returns the definition (UnifiedFrameLoop.ts:206) plus only comments and tests — zero production callers. And `setShadowReallocFrozen` :2163-2173 does NOT touch `_shadowRebuildPaused`, and `setSuspended(true)` has exactly one production caller (initUI.ts:1549, the IFC-import path only) — so on a snapshot open the shadow-rebuild guard is the ONLY thing blanking the viewport. Confirmed.\n\n5. THE SELF-CONTRADICTION IS REAL AND VERBATIM. :3229-3230 `this._reconcileRenderSize(); this.scheduleShadowRebuild();` sits twenty lines above :3252-3256 \"NOTE: Pipeline rebuild is intentionally deferred to onProjectLoaded(). Rebuilding here ... produces blank render frames that make geometry appear to vanish.\" Both live in `onProjectSwitch()`. The sibling cap `MAX_CASTER_RELEASE_PAUSED_FRAMES = 8` :187 with the \"unbounded window is a frozen viewport — the L-663 shape\" rationale at :1779-1786 exists; this guard has no cap. `initScene.ts:3607-3608` `_freezeShadowForLoad(); renderPipelineManager.onProjectSwitch();` and the \"BEFORE the incoming project hydrates\" comment at :3625-3628 both confirmed.\n\n6. (b), (c), (e) ALL CHECK OUT. PascalSceneLighting.ts:295-297 is the only `castShadow = true` (fill :324, rim :331, ambient :338 set none); `_forEachShadowCastingLight` :2380-2390 visits only `isDirectionalLight && castShadow`; no CSM. Per-light freeze is written at :2367-2370 (claim said :2385-2387 — that is the helper's definition, drift only). PlatformShell.ts:203-205 verbatim. FurnitureFragmentBuilder.ts:280 build-time read, `let _budget = 'full'` furnitureShadowBudget.ts:78, hook at initScene.ts:2653-2655, `_LARGE_SCENE_SHADOWS_OFF_MESH_COUNT = 8000` RenderingPipelineCoordinator.ts:567 — all verbatim.\n\nDOES IT EXPLAIN THE NUMBER? YES — and the repo's OWN ISSUE-LOG is stronger evidence than anything in the claim. Four independent historical readings of this same log line:\n  - L-663 (48 meshes, idle mid-session): `SHADOW_REBUILD_COMPLETE elapsed=0.9ms`\n  - L-698 (stair edit): `elapsed=67.6ms`\n  - L-12980 (EMPTY project, 71 meshes, tier=cinematic, 19.3 s first-frame): `elapsed=1474.3ms`\n  - L-231 (3,908 meshes / 1,492 casters, heavy load): `elapsed=6019.7ms`\n0.9 ms on the same code path kills any theory of intrinsic multi-second compute in this window. 1,474 ms on 71 meshes kills any scene-proportional theory outright. The readings track CONCURRENT MAIN-THREAD OCCUPANCY, not caster count. That is exactly the claim's thesis, and the claim did not cite it.\n\nWHERE THE CLAIM IS WRONG OR UNPROVEN (none of which overturns the root cause):\n\nA. \"ZERO work proportional to the scene\" is overstated. `_beginShadowRebuildGuard()` -> `setShadowReallocFrozen(true)` -> `_applyShadowFreezeState()` -> `_forEachShadowCastingLight()` is a full `scene.traverse` over 3,762 objects, INSIDE the timed window via :4936. Sub-millisecond, so the conclusion survives — but \"zero\" is not the word.\n\nB. THE STARVATION MECHANISM IS NOT ESTABLISHED, AND IT FIGHTS THE HARM CLAIM. The hydrate yields through `yieldForProgress` (ProjectLoader.ts:872) -> `getFrameScheduler().scheduleOnce(reason, resolve, 'post-render')` (progressScheduler.ts:186), i.e. rAF-driven frame-bus ticks; chunking is default-ON (`_useChunkedLoad()` :3311-3332). Between rAF callbacks the browser drains the macrotask queue, so a pending import continuation should resume within ONE chunk, not 16 s. For the claim's mechanism to hold, the hydrate must be running as one or a few very long unbroken tasks. But if that is true, the viewport is frozen by those tasks anyway and lifting the guard buys little to nothing visually — which contradicts \"the chunked loader painted nothing for 16 of its 17 s\". Both halves cannot be maximally true.\n\nC. FIX 3 IS BROKEN AS WRITTEN. `onProjectLoaded()` :3279-3300 does NOT own an unconditional rebuild: after the first load in a tab it hits `if (this._outlineNodes) { ... 're-point only, no rebuild'; return; }` (§OI-053d). Deleting :3230 therefore leaves NO pipeline rebuild at all on the 2nd..Nth project switch — precisely the reconstruction L-316 added. The NOTE at :3252-3256 predates §OI-053d making the loaded-path conditional.\n\nD. Minor citation drift, all immaterial: `_finishRebuildAndDrainQueue` :2053 not :2047; per-light freeze :2367-2370 not :2385-2387; ScenePass `pass()` :67 not :59.",
  "betterHypothesis": "SAME ROOT CAUSE, ONE STEP DEEPER — AND READ THE FREE LINES BEFORE PAYING FOR THE PROBE.\n\nThe window is confirmed misnamed, scene-independent and wait-dominated. But \"wait\" only exists if something ELSE is holding the main thread in long unbroken tasks, so the primary defect is a pair, not a single site:\n\n(1) THE HYDRATE'S TASK GRANULARITY is the thing that actually costs 16 s of wall clock. The rebuild's promise is merely the victim that reports it.\n(2) THE UNBOUNDED SUBMIT-PAUSE turns that occupancy into a viewport freeze — and this half is a real, independent defect regardless of (1), because `render()` :1257 has no second paint path.\n\nDO THIS FIRST — IT IS FREE AND IT DECIDES EVERYTHING. The same run already printed two lines the excerpt omitted, from ProjectLoader.ts:899-913:\n  `[ProjectLoader] §LOAD-CHUNKED — yielded N time(s) ... longest synchronous chunk between yields=Xms`\n  `[ProjectLoader] §LOAD-YIELD-WAIT compute=Ams waiting=Bms yields=N avgWait=... maxWait=... maxChunk=Xms`\n- If maxChunk is multi-second: the hydrate runs as a handful of longtasks. The starvation mechanism is confirmed, AND the harm claim collapses — the viewport was frozen by those longtasks anyway, so FIX 1 removes a 16 s WINDOW that was never painting. Ship FIX 1 + FIX 2 for the structural invariant, but claim ZERO user-visible saving and go fix chunk granularity instead.\n- If maxChunk is small (tens of ms) and `waiting` dominates: the loader IS releasing the thread every frame, a pending macrotask cannot be starved 16 s, and the diagnosis's mechanism is WRONG — the elapsed is then somewhere the static read cannot see. Only then spend the :3763 probe.\nNote that `waiting` also directly measures the second-order risk the claim flags: today those yields are cheap precisely BECAUSE render() skips at :1257, so B is the before-number for the after-comparison.\n\nALSO CORRECT THE FIX SET:\n- FIX 3 must not be shipped as \"delete :3230\". `onProjectLoaded()` early-returns without rebuilding once `_outlineNodes` exists, so the deletion silently removes the project-switch reconstruction on every switch after the first. If the trigger moves, `onProjectLoaded` needs an unconditional `scheduleShadowRebuild()` added on the no-outline-rebuild path.\n- FIX 2 is the highest value-per-risk item and should not be framed as \"belt and braces\": it is the only change that makes the failure class structurally impossible, it is the exact shape already blessed at :187/:1779-1786, and it is independent of whether the mechanism turns out to be the import.\n- Fix the stale comment at RenderPipelineManager.ts:4787-4792 in the same patch — it asserts a per-render-object dispose fan-out that does not exist in three 0.183.2 (`src/.../RenderPipeline.js:151-153`, `build/three.webgpu.js:81379`), and it is the reason this window keeps being suspected of O(n) work.\n- Add the four historical readings (L-663 0.9 ms / L-698 67.6 ms / L-12980 1,474 ms on 71 meshes / L-231 6,019 ms) to the ISSUE-LOG row for this defect. The 71-mesh / 1,474 ms pair is the single cheapest refutation of every future \"the shadow rebuild is slow because the scene is heavy\" theory."
}