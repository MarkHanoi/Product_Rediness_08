{
  "reason": "STRUCTURAL CLAIMS: all confirmed, line-for-line. FurnitureFragmentBuilder.ts:64 `private _instancingEnabled: () => boolean = () => false` ✓; FurnitureInstanceBridge.ts:125-129 bare `=== true` with no per-family table ✓; zero production assignment of `__pryzmFurnitureInstancingV1` (only FurnitureInstanceBridge.test.ts and a READ at tools/perf/render/render-profile.spec.ts:342) ✓; initBuilders.ts:817-819 unconditional injection ✓ (note: the Grep tool silently returned nothing for `FurnitureInstanceBridge` in that file — bash `grep -n` found it; classic grep-silence); FurnitureFragmentBuilder.ts:265 dual stamping ✓; :331 gate + `root.add(mesh)` fallback ✓; GLB early return ✓; pryzmPerfConsole.ts:1168-1170 verbatim ✓; ChairBuilder 66 / BedEngine 63 / KitchenCabinetEngine 58 / WardrobeEngine 25 `new THREE.Mesh(` ✓; mergedPartKit imported by 7 real builders ✓; ElementInstanceBridge._FAMILY_DEFAULTS:332-338 + 3-tier resolution ✓; InstanceGroup.ts:87-88 `castShadow = true` ✓; heavySceneSwapThreshold 400/1000, OR'd ✓; RenderPipelineManager.ts:2034-2041 has no per-mesh loop ✓ (the claim's own disclaimer is right).\n\nTHE CAUSAL/QUANTITATIVE CORE IS REFUTED, on four independent grounds.\n\n(1) MECHANISM POINTS THE WRONG WAY. Flipping the flag does not skip mesh construction. FurnitureFragmentBuilder.ts:242 runs `FurnitureFactory.getBuilder(...)` and builds the entire group BEFORE the gate at :331. The instanced branch then ADDS work: `_bakeParts` (FurnitureInstanceBridge.ts:333-378) traverses, `m.geometry.clone()` + `applyMatrix4` on EVERY leaf, then `_mergeBucket` (:386-412) calls `mergeGeometries` and disposes the clones. Hydrate is 99.4% of the measured load; this flag cannot reduce it and mechanically increases it.\n\n(2) THE REPO HAS ALREADY MEASURED THIS EXACT MECHANISM AND REFUTED THIS EXACT CLAIM SHAPE. ISSUE-LOG L-3055 (`packages/geometry-handrail/src/__tests__/HandrailLoadBuildCost.spec.ts`, real builder → real ElementInstanceBridge → real InstancedElementRenderer, real THREE.Scene): OFF = 137 handrails / 3151 meshes / 42.4 ms; ON = 151 meshes / 98.5 ms. Instancing ON is **+56.2 ms, 2.33× SLOWER** at build, and **3151 meshes cost 42 ms to build**. Its verdict line: \"handrail geometry building is not the bottleneck, and instancing is not the regression\" — 56 ms was 0.4% of a 15.3 s regression. Scaled honestly, building 2134 furniture meshes is tens-to-low-hundreds of ms, not 2000 ms, and the flag adds to that number rather than subtracting.\n\n(3) THE ARITHMETIC IN THE CLAIM'S OWN EVIDENCE CONTRADICTS ITS HEADLINE. The one measured per-mesh cost in the paste is 7.7 ms / 3762 meshes ≈ 2 µs/mesh/traverse. Removing 2134 meshes saves ~4.3 ms per full-scene traverse; reaching 2000 ms needs ~460 full-scene traverses inside the load window, which nothing cited supports. The claim itself concedes the 16.0 s shadow rebuild is not per-mesh and the backend swap fires on the element arm regardless — so neither is available as the saving either. Nothing measured is left to fund 2000 ms.\n\n(4) STEP 0 NAMES THE WRONG INSTRUMENT. `§LOAD-HYDRATE-STEPS` (ProjectLoader.ts:559-564, emitted :2875) carries only FOUR coarse marks — `load_setup` (:820), `element_import` (:915), `migrations` (:2403), `nonelement_stores` (:2872). Furniture is buried inside `element_import`, so that line cannot \"settle whether the flag helps hydrate\". The line that DOES size furniture already exists and prints unconditionally: **`§LOAD-IMPORT-STEPS`** at ProjectLoader.ts:920-926, built from `importCmd.stats.stepMs`, with a literal `furniture=<ms>/n=<count>` row (ImportProjectCommand.ts:143 `IMPORT_CHUNK_SIZE = 120`, :458 `__step`, :1071 `__step('furniture', …)`; 20 named steps, ISSUE-LOG L-3050).\n\nAlso unaddressed: `§LOAD-YIELD-WAIT` (ProjectLoader.ts:905-911) splits hydrate into compute vs \"waiting is time the loader spent parked in the frame bus, not work\". With CHUNK=120 and a yield per step, 550 elements ⇒ ~25-30 yields; ISSUE-LOG L-3054 states the arithmetic verbatim — \"if a frame costs 500 ms, every yield costs 500 ms, and a load with 30 yields pays 15 s for waiting alone with zero of it in any counter.\" A 17.2 s hydrate is fully consistent with that, and with L-3056 (unguarded per-add full-scene shadow pass on 41 events during load), neither of which the claim considers.\n\nFinally, the mesh→item ratio is unmeasured: census `elems=0` for FurniturePart because :265 stamps `parentId`, never `id` (the claim notes this), so the per-item build cost the 2000 ms rests on cannot be derived from the paste at all.\n\nNet: a true and well-evidenced description of mesh multiplication and a genuinely dead flag, attached to a load-time saving the code and the repo's own prior measurement both contradict.",
  "refuted": true,
  "betterHypothesis": "Split the claim in two and keep only the half the code supports.\n\nKEEP (real, but re-scoped): `__pryzmFurnitureInstancingV1` is genuinely dead in production and FurniturePart+KitchenCabinetPart genuinely are 2134 of 3762 meshes behind one builder and one flag. That is a FRAME-TIME / DRAW-CALL / SHADOW-CASTER defect — 1421 of 3274 casters, plus ~4.3 ms off every full-scene traverse — not a load-time defect. Ship the ~6-line `_FAMILY_DEFAULTS`-style default if the frame-rate number justifies it, with the four WOULD-BREAK items handled (InstanceGroup.ts:87-88 re-promoting the 66 policy-demoted casters; ADR-0297 dispose-in-place on furniture churn — the reason `beam`/`column` are still OFF; the three `FurniturePart`-by-name consumers; `skipInPlan`). Expect load to get slightly SLOWER, exactly as L-3055 measured for handrails (+56 ms, 2.33× at build).\n\nDROP: \"2000 ms\" and any attribution of the 17.2 s hydrate to furniture mesh count. The instanced path does not skip construction — it clones and merges on top of it.\n\nINSTEAD, attribute the 17.2 s before proposing any fix, using instruments that already ship — no code change, no redeploy, same project:\n1. `[ProjectLoader] §LOAD-IMPORT-STEPS` — already printed unconditionally (ProjectLoader.ts:920-926); ranks all 20 import steps heaviest-first with n= counts, including `furniture=…ms/n=…`. This is the line to ask for, not §LOAD-HYDRATE-STEPS. With walls=56 / slabs=10 / curtainWalls=90 in the paste, curtain_walls and walls are the a-priori candidates, not furniture.\n2. `[ProjectLoader] §LOAD-YIELD-WAIT compute=… waiting=… yields=… avgWait=…` — settles whether hydrate is WORK or PARKING. If waiting dominates (L-3054's stated arithmetic makes 15 s of waiting entirely plausible at ~30 yields), the fix is the frame loop or the yield cadence, and no amount of furniture instancing touches it.\n3. ISSUE-LOG L-3058's `window.dispatchEvent` wrapper probe (paste-ready in the log) — arm it before opening the project, then `window.__pryzmEvtReport()`. It sizes the unguarded synchronous `bim-*-added` DOM fan-out (FurnitureStore.ts:23 `_bus.emit` on a DOMEventBus) that no batch or load flag covers, and names the family if that channel is the cost.\n\nOnly step 1's `furniture=` cell can justify a furniture load-time claim, and the L-3055 precedent predicts it will be small. Ranking items 3 (PlumbingFixture: 201 meshes / 91,842 tris, no merge, no bridge) and 4 (Lighting) are unaffected by this refutation and stand on their own for frame time."
}