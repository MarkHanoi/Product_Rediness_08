{
  "survivingTitles": [
    "The full server snapshot is downloaded on the critical path, serially after the engine boot — and discarded whenever local history exists",
    "The heavy-scene backend swap fires DURING the open and tears the renderer down — that is the \"WebGPU device lost\" in the founder's console",
    "A hub-open never prints the §STARTUP-BUDGET table — one call site is missing, and that is why every open reading is a guess",
    "`open:first-interactive-frame` is the LAST open mark and it fires before the heaviest post-load work — the founder is waiting inside an unnamed interval, for the fourth time",
    "The open path re-fetches the project list the hub just fetched — and it is the FIRST thing in the serial chain, blocking the engine boot",
    "Every open serialises the whole project at least twice and captures the framebuffer twice — for a project the user has not touched",
    "`openProject` resolves and the loading overlay hides BEFORE the project is loaded — so every \"open took N ms\" measured from that promise is measuring the boot",
    "web-ifc (3.5 MB) is still statically on the engine-boot graph via the `@pryzm/file-format` barrel — diagnosis fix #7, not applied",
    "The whole-corpus version-mirror warm reads EVERY project's history, and it is awaited on the deep-link / reload open path"
  ],
  "refutedTitles": [
    "`endBootLeg` drops the untiled tail — the brand-new builders profiler cannot see `await initBuilders(...)`, the exact 18.6 s span it was written to explain",
    "Two socket.io clients connect and join the same project room on every open"
  ],
  "refutations": [
    "REFUTED — `endBootLeg drops the untiled tail`. The auditor grepped `bootStep(` in `engineLauncher.ts` ONLY and concluded the last step in the leg is `:418`. It is not. `apps/editor/src/engine/initBuilders.ts:41` imports `bootStep` from the SAME module and calls it **28 times** inside the awaited body — `bootStep('column')` at :341, 'curtain-wall' :356, 'slab' :405, 'ceiling' :437, 'floor-finish' :471, 'room (stores + builders, pre-DI)' :486, `await:WorkspaceController + core-app-model` :498, 'room' :564, 'door/window stores' :572, 'wall' :595/:610, `await:geometry-wall + geometry-slab type stores` :600, `await:curtainWallTypeStore` :607, 'roof' :634/:674, `await:material-library` :637, 'plumbing' :726, 'opening' :768, 'door+window builders' :803, 'furniture' :910, 'lighting' :952, 'handrail' :1002, 'stair' :1041, 'lift' :1068, 'beam' :1088, 'grid' :1122, 'room-bounding-line' :1158, 'room-finish-sync' :1170, 'teardown wiring + window publish' :1248. `_leg` is module-scoped and both files import the same instance, so `state.lastAtMs` is advanced right through `await initBuilders(...)`. The leg is 7 rows from `engineLauncher` + 28 from `initBuilders` = ~35, and the profiler's PRIMARY subject is exactly the span the finding says it cannot see — including the `await:` prefixes that carry the own/foreign split the finding claims is missing. The real untiled tail is `initBuilders.ts:1248 → 1306` (a bare object-literal `return`, ~55 lines of property references) plus `markStartupPhase` at `engineLauncher:457`: microseconds, not 18.6 s. Fix half (a) would insert a duplicate `await:initBuilders` row spanning zero real work; fix half (b) (a defensive `(untiled tail)` row in `endBootLeg`) is harmless but buys nothing measurable here. ⛔ This is the highest-risk finding in the set: it tells the next lane the one instrument that CAN answer 'is initBuilders slow?' is blind, when the correct action is simply to read the table it already prints.",
    "REFUTED as an open-timeline COST — `Two socket.io clients connect and join the same project room on every open`. The duplication is REAL (two `ioFn(...)` clients, two `join-project` emits, two server-side room joins) and deserves a correctness/resource row. But the claimed 150 ms on the open is not supported by the code, and the evidence cited argues the opposite way. Neither connection is awaited by anything, and neither overlaps the two requests the open actually waits on. (1) `initCollaboration` at `engineLauncher.ts:1202` mints NO socket at boot — it only registers listeners; `initCollaboration.ts:319` says so (\"null before pryzm-project-loaded\"), `connectSocket` is reached only via `loadAndConnect` at `:1140`, called from `onProjectLoaded` at `:1133`. That client connects AFTER `pryzm-project-loaded`, i.e. after the last open mark. (2) `initSocketCollaboration` at `PlatformShell.ts:250` is fire-and-forget script-inject + connect inside leg 4, i.e. AFTER `controller.refresh()` (leg 1) and AFTER `tier.streamLoad()` (leg 3) have both already resolved. So the `ProjectHub.ts:775-783` quote about competing with \"`controller.refresh()` and `tier.streamLoad()` — the two requests the OPEN path is waiting on\" is inapplicable: no socket exists while either is in flight. The connection-budget argument also fails on its own terms — both clients pass `transports: ['websocket','polling']` (websocket first), and WebSocket upgrades do not sit in the HTTP/1.1 six-per-host pool. Also note the second client reuses the first's script (`initCollaboration` checks `window.io` before injecting), so there is not even a duplicate bundle fetch. Fixing this saves ~0 ms of open time while taking on the lifecycle risk the finding itself names (per-PROJECT vs per-BOOT ownership, `ctx.socket` re-pointing). Wrong dimension.",
    "CAVEAT on surviving finding 3 (§STARTUP-BUDGET table) — the finding is right, the one-line fix is INCOMPLETE. `_reported` is a per-RUN latch reset only by `beginStartupBudget()`, and `beginStartupBudget()` has exactly one call site: `PlatformRouter.showOnboarding` (`:725`). A session that onboards, enters the canvas (report fires, `_reported = true`, and `reportStartupBudget` does NOT clear `_marks`), then goes back to the hub and opens a project will satisfy the proposed `hub:open-clicked` guard and STILL print nothing — and if it ever did print, the table would splice the onboarding run's marks onto the open's. The repo already records this exact shape (`hub:back-clicked` was added because a back-hub delta was measured against a mark from the PREVIOUS open, i.e. the run spans the whole page session). The correct fix is to begin a fresh run at `ProjectHub.ts:2268` alongside `markStartupPhase('hub:open-clicked')`, then report at the last open mark.",
    "CAVEAT on surviving finding 8 (double serialize / double capture) — two corrections. (i) The cited evidence `SaveOrchestrator.ts:244-248` (\"serialize … TWICE per fire\") is a STALE COMMENT: `§PERF-AUTOSAVE-SINGLE-SERIALIZE` (`PlatformSaveController.ts:117`) stashes `_autosaveSerialization` and `saveVersionInternal` consumes it at `:251-252`, so one autosave fire = ONE serialize. The finding's substance still holds — two serializes across the open (`resetDirtyAfterLoad` → `getHash` at `PlatformVersionController.ts:412`, synchronous and immediately before the `pryzm-project-loaded` emit at `:419`; then `executeSave`'s `getHash` at settle expiry) — but the price is roughly half what the quoted comment implies. (ii) Fix (a) has the ORDERING BACKWARDS: the post-load capture fires at `CAPTURE_DELAY_MS = 3500` after `pryzm-project-loaded`, while the autosave fires at `_settleUntil = +4000` set in `setLoading(false)` just BEFORE that emit — so the post-load capture is FIRST and there is no earlier autosave capture to stand down for. The coalescing must run the other way (let `saveVersionInternal` reuse the fresh post-load thumbnail), and it must preserve `PlatformSaveController.ts:348-355`, which keeps the post-load capture precisely as the guaranteed-fresh path for plan-blocked accounts.",
    "CAVEAT on surviving finding 1 (fix mechanics) — half (a) should use the honest variant, not `countVersions`. `ProjectRepository.ts:1470-1473` states that `countVersions` \"returns `0` for THREE different conditions\" and \"cannot fail honestly\", which is why a sibling API exists. The failure direction here happens to be safe (a cold mirror answers 0 → we fetch → correct), but keying a network decision on a value that cannot say \"I could not look\" is the §CONTEXT-DATA-HONESTY defect this repo has shipped before. Half (b) (hoisting `tier.streamLoad` above the boot) is as safe as claimed — `streamLoad` is fully try/caught and 404s to `null`, so hoisting it above the OI-059 soft-fallback changes nothing.",
    "CAVEAT on surviving finding 6 (fix mechanics) — the secondary suggestion (`Promise.all` the summary resolve with `attachedBootstrap.ensure()`) is NOT free. `opts.projectContext.set({ projectId, projectName })` at `buildPersistence.ts:300` runs before the boot at `:308` and `initBuilders` is handed `projectContext` during that boot; racing the two means the engine can construct against an unset project context. The primary fix (seed `projectListStore` from the hub's `listAll()` at `ProjectHub.ts:319`) is the safe one and is sufficient.",
    "CAVEAT on surviving finding 11 (cost scope) — the 250 ms \"high confidence\" figure does not apply to the founder's paste. His run is a HUB-open: the hub already warmed (`hub:warm-versions-done`), `_warmed` short-circuits, and because `getLatestVersion(id)` returns a record (6 versions), the `else` branch that AWAITS the warm at `PlatformShell.ts:329-336` is never entered. On that run the cost is ≈0 on the critical path. The finding's own text says this; the claimed number does not. It is real on deep-link, hard reload, and the deliberate reopen-after-swap — say which."
  ],
  "missed": [
    "The post-load traverse storm is at least FOUR full-scene walks, not one. The auditor named only `initScene._runConsolidatedTierPbrPass` (which is itself TWO: `collectNewPbrMeshes` at `initScene.ts:2951` and `countMeshes` at `:2965-2969`). Also on `pryzm-project-loaded`, unconditional in production: `ProjectIsolationAudit` (`packages/core-app-model/src/persistence/ProjectIsolationAudit.ts:1404` → `getFrameScheduler().scheduleOnce('project-isolation-audit')` → `runAudit` → `gatherSceneObjects()`, which `:344` says \"traverses exactly ONE graph: `window.scene`\"), and `FrustumCullingService` (`packages/core-app-model/src/rendering/FrustumCullingService.ts:146` → `_scheduleAudit()` at `:230` → debounced `_runAudit(false)` at `:240`, which walks every scene child once the element count clears `LARGE_MODEL_THRESHOLD`). At 3,762 meshes that is four unmarked O(scene) walks landing in the window after `open:first-interactive-frame`, on top of `zoomToAll(true)`'s own bounds computation.",
    "`VersionCacheStore.warm()` has no IN-FLIGHT latch — only the terminal `_warmed` boolean (`VersionCacheStore.ts:116`: `if (this._warmed) return;`, set at the END, `:147`). `ProjectHub` starts the whole-corpus cursor scan fire-and-forget at `:260-262`, and `PlatformShell.setProjectContext:288` starts another with `void warmVersionCache()`. A user who clicks a card before the hub's scan resolves runs TWO concurrent unfiltered `openCursor()` scans over the entire versions object store, each deserialising every project's multi-MB compressed payload onto the main thread. Finding 11 discusses `_warmed` and proposes `warmOne`, but never notices the missing de-dup — which is a strictly cheaper one-line fix (`if (this._warmPromise) return this._warmPromise`) and is orthogonal to the narrow-warm proposal.",
    "No finding distinguishes the FIRST open of a page session from subsequent ones, and that split decides which of them are worth a lane. `attachedBootstrap.ensure()` is idempotent (`buildPersistence.ts:306-309`), so the whole engine boot — including the `boot:builders-done +18626ms` leg and finding 10's web-ifc module evaluation — is paid ONCE per page load, not per open. `controller.refresh()` populates `projectListStore`, so finding 6's list round trip also fires only on the first open. The backend swap (finding 2) is once per session by `_autoSwapDone`. Only findings 1, 5, 8 and 9 recur on every open. A brief that says \"it takes too long to open\" without saying which open sends the next lane at work that happens once.",
    "The projections leg is a named, measured post-load consumer that finding 5's minimum mark set omits, even though the founder's own paste prices it: `EdgeProjectorService PERF-EDGEPROJECTOR-CHUNK … work=109ms yieldCalendar~83ms, 410 edge geometries across 63 ISO layer(s)`, then `HiddenLineRemoval v3 — 26 occluder(s), 239.5 segment-equivalent(s)`, then `VGSceneApplicator applyToProjectionLayers() — styledLayers=22 across 14 VG categories`. That is ~200 ms of named work in the unmarked window, and it wants `open:projection-done` in the set. ⛔ Counter-warning for the next lane: the `cacheHits=0 cacheMisses=63 hitRate=0%` line in the same paste is NOT the L-11561 alternation bug re-opening — a first projection after an open has an empty cache by construction, and `cacheEntries=63/5000` growing to match the miss count is the tell. `planBelowDepthOffset.ts:1-60` records that this exact fingerprint has already been misread twice (ISSUE-LOG L-307, then L-11561). Do not send anyone at that 0%.",
    "`bootStepProfile` covers exactly ONE leg. `beginBootLeg('builders')`/`endBootLeg('builders')` bracket `boot:scene-done → boot:builders-done`; `boot:tools-done`, `boot:bus-handlers-done`, `boot:data-platform-done` and `boot:ui-done` have no per-step tiling and — more importantly — no PerformanceObserver long-task attribution at all, because the observer is created in `beginBootLeg` and disconnected in `endBootLeg`. `await initUI(...)` (`engineLauncher.ts:1135-1147`) is the largest uncovered span and is where `mountGISArea` (Cesium) runs and where finding 10's `@pryzm/file-format` barrel is pulled. Given the module's own harness measured the builders themselves at 11.4 ms against an 18,626 ms mark, the missing time is very likely foreign long-task time — and it may belong to a leg the profiler does not instrument. Adding `beginBootLeg`/`endBootLeg` around `initUI` costs nothing and is the actual follow-through finding 4 was reaching for."
  ]
}