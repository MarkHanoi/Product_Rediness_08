{
  "survivingTitles": [
    "The Project Browser's furniture exclusion writes NOTHING when the active view is 3D — `_activeViewId()` reads a store method that does not exist, then a field ViewController nulls in 3D",
    "Symbol-injected furniture linework carries NO element identity in `userData`, so a per-element hide can never match it — the render path and the hit-test path resolve identity two different ways in the same class",
    "`NativeElementMeshExporter.exportForView()` filters by level and Y-range ONLY — no category, no visibility, no intent, and zero reads of `Object3D.visible` in the whole file",
    "`VGSceneApplicator.applyToProjectionLayers()` can hide, but its hide is INERT on the plan canvas — it sets `Object3D.visible`, which PlanViewCanvas never reads",
    "ROOT ARCHITECTURAL FINDING — two hiding mechanisms with no shared authority: 3D honours `Object3D.visible` + the VG store and IGNORES the intent's `visible`; 2D honours the intent + the VG store and IGNORES `Object3D.visible`",
    "The regression suite that certifies \"the browser filter reaches projected views\" stubs a store method that does not exist in production"
  ],
  "refutedTitles": [
    "Lighting fixtures are STRUCTURALLY ungovernable in plan — `Lighting` is absent from the layer map, so the family key is null and the intent veto's own guard skips them; and nothing marks them `skipInPlan`, so the 672-face fixture mesh edge-dumps as the black concentric bursts in his screenshot"
  ],
  "refutations": [
    "REFUTED AT HEAD — three of the four load-bearing citations are now false, fixed by commit 584aaab2 `fix(§LIGHTING-IS-A-CLASSIFIED-LAYER L-13267)` which landed DURING this session (HEAD is 9babe6f5; the conversation opened at 921cbb01). (1) 'ELEMENT_TYPE_TO_PROJECTION_LAYER — no Lighting / lighting key anywhere in the map' is FALSE: apps/editor/src/engine/views/EdgeProjectorService.ts:260 reads `Lighting: 'A-LGHT', LightingFixture: 'A-LGHT', light: 'A-LGHT'`, under a 28-line header quoting the founder verbatim. (2) 'penCategoryForLayerTag falls through categoryFromFlags to its projection default' is FALSE: packages/core-app-model/src/drawing/PenWeightTable.ts:417 is `if (flags.isLighting) return 'lighting';` and :461 feeds it `isLighting: /A-LGHT|lighting/i.test(layerTag)`. (3) 'VisibilityIntentDefaults.ELEMENT_TYPES has no lighting entry' is FALSE: the list carries `'lighting'` with its own §LIGHTING-IS-A-CLASSIFIED-LAYER comment. (4) The auditor's proposed `'A-LGHT': 'lighting'` row already exists at DrawingLayerIdentity.ts:110 and DrawingPipelineWorker.ts:129, and there is a regression test packages/core-app-model/src/drawing/lightingIsAClassifiedLayer.test.ts asserting all three. So 'the family key is null', 'the intent veto's own guard skips them', 'V/G could not hide it' and 'the founder had no toggle' are all no longer true, and the black-at-wall-weight symptom (0.25 mm #000 __default__ pen) is gone — lighting now resolves the `lighting` pen (0.13 mm #303030). RESIDUE THAT SURVIVES, and should be re-filed as its own row rather than kept inside this refuted one: (a) `grep -rn skipInPlan packages/geometry-lighting/` still returns ZERO matches, so the 672-face fixture mesh is still edge-projected in plan on top of the authored 2D symbol — the double-draw is real and unfixed, it is merely now thin and hideable; (b) apps/editor/src/engine/views/plan-canvas/PlanViewSymbolRenderer.ts:21-25 `renderLightingSymbols(ctx, ppu, (wx,wz)=>worldToScreen(wx,wz), { levelId, selectedId })` is verbatim as cited — no viewId, no intent, no VG — so hiding the new `lighting` intent row kills the mesh linework and leaves the cyan authored symbol on screen, which is a NEW inconsistency L-13267 created; (c) the `if (_groupFamily && !_symbolGate(_groupFamily))` guard at EdgeProjectorService.ts:2697 is verified verbatim and the general point (an UNCLASSIFIED family silently escapes every filter) stands — lighting is simply no longer an instance of it."
  ],
  "missed": [
    "FOUNDER'S SECOND EXPLICIT ASK IS UNANSWERED — rooms have no Visibility Intent row, and the auditor buried this in a five-word parenthesis inside refuted finding 6 instead of raising it. He said: 'THE ROOMS SHOULD HAVE A CATEGORY ON THE VISIBILITY INTENT SETTINGS TO MODIFY THE COLOURS - THAT I DONT SEE'. Verified: `VisibilityIntentDefaults.ELEMENT_TYPES` (packages/core-app-model/src/presentation/VisibilityIntentDefaults.ts:14-47) has no `'room'` — the list is __default__/wall/slab/column/structural/beam/door/window/stair/roof/ceiling/furniture/plumbing/lighting/grid/annotation/level/boundary-line/ifc-element — and `OverridePanel.CATEGORIES` (apps/editor/src/ui/OverridePanel.ts:247-268) has no room row either. YET THE CONTROL EXISTS ONE LAYER OVER: `'room'` is a full member of the `VGCategory` union (VGSceneApplicator.ts:65, §ROOM-VG-CATEGORY L-1610), `ELEMENT_TYPE_TO_VG_CATEGORY` maps `'room'/'Room'/'RoomVolume' → 'room'` (:143), `applyToMesh` has a dedicated room arm (:786, :792 `fillGovernedElsewhere`), `VGGovernanceStore` carries `roomColourMode` (:51), and there is a whole `RoomColourIntent.ts` plus a `SetRoomColourModeCommand`. This is [[authored-but-unwired-is-the-bottleneck]] exactly: the room colour authority is built and the panel he was looking at has no row for it. His screenshot's pastel room tints are being driven by RoomColourSystem, which he cannot reach from the Visibility Intent panel.",
    "L-13267 HALF-FOLLOWED ITS OWN SYNC INSTRUCTION — the header of `ELEMENT_TYPE_TO_PROJECTION_LAYER` (apps/editor/src/engine/views/EdgeProjectorService.ts:200-202) says 'Keep in sync with CATEGORY_TO_DXF_LAYER in VGSceneApplicator.ts'. The Lighting row was added to the first and NOT the second: `CATEGORY_TO_DXF_LAYER` (packages/core-app-model/src/presentation/VGSceneApplicator.ts:87-102) still lists exactly 14 categories with no lighting, and the `VGCategory` union (:57-74) has no `'lighting'` member. Consequences: `applyToProjectionLayers` never resolves or styles `A-LGHT` or its `:cut`/`:proj`/`:beyond` sub-layers, so the per-category `layer.material.linewidth` that the DXF/SVG export pipeline reads (documented at :601-603) is never set for lighting; and `ISO_LAYER_TO_VG_CATEGORY['A-LGHT'] = 'lighting'` now returns a category string that is not in the VG union. Separately, `OverridePanel.CATEGORIES` gained no lighting row, so the per-VIEW quick toggle still cannot hide luminaires — only the shared intent can. This is the [[same-rule-two-implementations]] shape: one family, two tables, one updated.",
    "THE DEAD `getActiveId?.()` ARM HAS THREE MORE COPIES, NOT ONE — finding 1's fix names only `packages/geometry-wall/src/WallFragmentBuilder.ts:714`. A repo-wide grep (bash `grep -rn --binary-files=text` over apps/packages/plugins/src/server) returns FOUR non-test call sites and ONE definition: definition `apps/editor/src/toolbar/ToolRegistry.ts:125`; callers `apps/editor/src/engine/initUI.ts:3381` (`(viewDefinitionStore as any).getActiveId?.() ?? viewController?.currentViewDefinitionId`), `apps/editor/src/ui/RadialMenu.ts:236` (`window.viewDefinitionStore?.getActiveId?.() ?? window.viewController?.currentViewDefinitionId`), `ProjectVisibilitySection.ts:48`, and `WallFragmentBuilder.ts:714`. RadialMenu carries finding 1's defect VERBATIM into a second user surface — a right-click action scoped to 'the active view' silently no-ops in 3D there too. NOTE FOR THE TOOLING: the `Grep` tool returns only the WallFragmentBuilder hit for this pattern and silently drops the other eight — [[grep-silence-has-three-causes]]. Use bash `grep -rn --binary-files=text` for this repo.",
    "CORRECTION TO SURVIVING FINDING 2 — the census is wrong in the safe direction but should be fixed before anyone acts on it. The auditor says '17 of 18 stamp ZERO userData.elementUUID (only OpeningElevationSymbolBuilder does)'. Measured over the same 18 files: THREE stamp it — `packages/core-app-model/src/drawing/OpeningElevationSymbolBuilder.ts`, `apps/editor/src/engine/BoundaryLinePlanSymbolBuilder.ts` and `apps/editor/src/engine/SpaceEnvelopePlanSymbolBuilder.ts` — so it is 15 of 18, not 17 of 18. The four that register NOTHING (ColumnPlanSymbolBuilder, RoofSlopeSymbolBuilder, StairSymbolTechnicalDrawingBridge, WallLayerPlanSymbolBuilder) is exactly right, and every furniture/door/window/tree/kitchen/plumbing builder is exactly as described. Everything else in finding 2 verifies verbatim: OBC `toDrawingSpace` returns `new THREE.LineSegments(geo)` with no userData copy (node_modules/@thatopen/components/dist/index.mjs:26088-26105), `registerSegmentUUID` writes only a WeakMap (DrawingSelectionIndex.ts:47-53), render reads `child.userData?.elementUUID` alone (PlanViewCanvas.ts:472), `_lineIsDrawn` repeats that gap (:2865), hitTest (:1476) and the highlight pass (:2380) both start with `lookupElementUUID`, `IntentRuleResolver.targetMatches` requires `!!target.elementId` (:79), and `GraphicsRulesEngine._intentRules` passes `{ elementId: ctx.elementId, elementType: category, category }` — so category-level hides DO reach symbols and element-level hides structurally cannot.",
    "LINE-NUMBER DRIFT IN THREE SURVIVING FINDINGS — the claims all hold, the anchors are stale by 1-30 lines, which matters because the next agent will grep the number. Finding 2: the native stamp is `EdgeProjectorService.ts:3223` (`projected.userData.elementUUID = elementUUID;` + `registerSegmentUUID` at :3224), NOT :3195 — :3195 is `projected.name = targetLayerName`. Finding 3: the level filter is :323-325 and the log line :326-329, not :322-328. Finding 4: `drawing.layers.setVisibility` is :635 not :634, and there are THREE more at :683/:690/:697 for the `:cut`/`:proj`/`:beyond` sub-layers the auditor mentions but does not cite. Finding 5: the ONE `Object3D.visible` read in the 2D pipeline is `EdgeProjectorService.ts:3771` (`if (!mesh.visible) return;`), not :3743 — it is inside `if (ifcSceneGroups.length > 0)` at :3749, so the Source-C scoping claim is correct.",
    "FINDING 4 IS TRUE BUT SHOULD NOT BE RANKED AS A DEFECT — I confirmed the dead-code claim AND resolved the risk the auditor flagged but did not check. `grep -c '\\.visible'` over `packages/file-format/src/export/sheets/SVGCompositeRenderer.ts` returns ZERO, and a repo-wide grep for consumers of drawing-layer visibility finds only VGSceneApplicator itself (the DXF hits are `DxfLayerStore` / `DxfImportPanel` / `ImportManagerPanel`, an unrelated IMPORT-side layer model). So deleting the `setVisibility` calls would not regress export, and the auditor's 'check SVGCompositeRenderer first' caveat is now discharged. Also: `CATEGORY_TO_DXF_LAYER` has 14 entries, not the 13 the finding states — which is why the founder's log reads 'across 14 VG CATEGORIES'. Net: real dead code, zero user-visible symptom, no export risk in removing it.",
    "FINDING 1'S ATTRIBUTION TO THE SCREENSHOT IS UNPROVEN — the DEFECT is fully confirmed (every cited line verifies; `window.viewDefinitionStore` is the real singleton assigned at apps/editor/src/engine/initUI.ts:736, `ViewDefinitionStoreImpl` has no `getActiveId`, ViewController.ts:1551 nulls the id on every 3D activation and :1562 explicitly skips §ANN-VIEW-INFER for 3D, the guard is at :68, and the wiring runs ElementsSummarySection.ts:145/:347 and ProjectTreeSection.ts:394/:478/:529 into it). But the claim that it is 'the likeliest cause of the founder's specific screenshot' is narrative, not evidence: his screenshot is a Level 2 PLAN with the plan's own properties panel open, and if a plan view was active when he unticked Furniture, `_activeViewId()` returns a real id and the write LANDS — in which case finding 2 (per-element hide slides off every symbol) is the cause and finding 1 is not implicated at all. Ship finding 1 as a confirmed defect; do not let the screenshot attribution close the lane, because the two findings predict different fixes and only one of them explains a plan opened from a plan.",
    "LOWER-CONFIDENCE ADJUNCT TO FINDING 2 — the identity gap also disables INCREMENTAL re-projection for symbols. `_graftDirty` in EdgeProjectorService.ts (~:4340-4378) keys BOTH passes on `userData.elementUUID`: Pass 1 removes a stale line only `if (cu?.elementUUID && ids.has(cu.elementUUID))`, Pass 2 clones a line only `if (id && ids.has(id))`. A symbol-injected line has no such key, so on the graft path a stale door swing or sofa symbol can be neither removed nor replaced. The founder's own log shows the graft was BYPASSED this session ('DIAG-GRAFT-FALLTHROUGH ... FULL re-projection because: no-graft-ids'), so it did not bite here, and I did not confirm whether the injectors re-run unconditionally on the graft path — hence low confidence. Flagged because it is a second, differently-shaped cost of the same missing stamp, and fixing render()'s read alone would not close it.",
    "HALF THE FOUNDER'S QUOTE IS OUTSIDE THE AUDIT'S SCOPE — 'ALSO I CHANGE THE LINE TYPE ETC.. DOESNT WORK' is not addressed by any of the seven findings. It was closed independently during this session by commit 9babe6f5 `feat(§LINE-TYPE-CONTROLS-EXIST-NOW L-13268)` — 'the two line-type behaviours the drawing engine already reads finally have a UI ... a missing control, not a broken one'. Worth recording so the lane does not re-open it, and worth noting as an audit-coverage gap: two of the founder's four complaints (line type, room colour category) got no finding of their own."
  ]
}