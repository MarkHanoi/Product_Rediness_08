{
  "survivingTitles": [
    "`room` is a first-class category in the DEPRECATED vgGovernanceStore, and absent from the VisibilityIntent list the founder's panel actually renders",
    "THE STYLING SEAM IS ONE FUNCTION — `resolveRoomColourIntent()` — with exactly three live callers",
    "`OverridePanel.CATEGORIES` is a FOURTH category registry with no room row — the one this lane is most likely to miss",
    "`PlanViewFillRenderer` is a DEAD duplicate room painter with zero importers",
    "'Colour by room type' cannot actually change any colour — `OCCUPANCY_PALETTE` is a hardcoded table with no write path",
    "Room fills are gated to `isPlanLike` — elevation and section render no room region at all",
    "`ELEMENT_TYPE_REGISTRY` is a rival to `VisibilityIntentDefaults.ELEMENT_TYPES` and its header states a rule that is false",
    "No gate covers the visibility-intent category list, which is why this shortfall was silent",
    "REGISTRY MAP — every list that would need a `room` row, and its current state"
  ],
  "refutedTitles": [],
  "refutations": [
    "NONE REFUTED. Every finding's central mechanism reproduced from code at HEAD. Per-finding verification and the citation slips found: (1) CONFIRMED — VGGovernanceStore.ts:23-28 carries the verbatim @deprecated / 'Do not add new importers' text; `room:` appears at :148/:177/:206/:235 exactly as cited; VisibilityIntentDefaults ELEMENT_TYPES (:15-46) has NO 'room'; VisibilityIntentPanel.ts:240 is verbatim `const elementTypes = Object.keys(intent.elementRules);` and :252 maps one button per key; SystemIntents.ts:42 is verbatim `const baseRules = cloneDefaultElementGraphicsRules();` and VisibilityIntentDefaults.ts:135-140 is the reduce over ELEMENT_TYPES; ViewPropertiesPanelBuilders.ts:78 and :196 verbatim; `grep -rln vgGovernanceStore apps/editor/src/ui` returns exactly one file (inspectCategories.ts) whose only hit is the :196 exclusion string. Fill style/colour/opacity controls do exist in renderAppearanceForm (~:351-362). ONLY SLIP: the panel now renders 20 rows, not 19 — see misses. (2) CONFIRMED — RoomColourIntent.ts:132 is verbatim `const resolved = vgGovernanceStore.resolveStyle(modelId, ROOM_VG_CATEGORY, viewId);`; the repo-wide grep for resolveRoomColourIntent|activeRoomColourIntent outside the defining file, barrels and tests returns exactly four call sites — PlanViewCanvas.ts:2504, RoomBoundaryBuilder.ts:251, RoomPropertySection.ts:683, PlanViewFillRenderer.ts:40 (dead). IntentRuleResolver.ts:94 `const exact = intent.elementRules[elementType];` verbatim. The 'already exists' inventory checks out: SetRoomColourModeCommand (packages/command-registry/src/rooms/), plugins/rooms/src/handlers/SetRoomColourMode.ts:63, bus verb declared at command-bus/src/commands.ts:815, ProjectSerializer.ts:1837 + ProjectLoader.ts:2307 verbatim, and both test files exist on disk. ONE CITATION SLIP: syncFromViewIntent() is at RoomBoundaryBuilder.ts:234, not :664-669 (:660-669 is _polygonToShape). Mechanism unaffected. (3) CONFIRMED — OverridePanel.CATEGORIES is 13 entries ending at boundary-line, no room, no lighting/plumbing/grid/level; the 'curated list, not a scan' comment is verbatim at :238-245; dispatch at :309 is `view.setCategoryVisibility` and the bus handler exists at initBusHandlers.ts:2443; InspectCategoryCoverage.test.ts:44-54 publishedStoreGlobals() scans only /^\\s*window\\.(\\w+Store)\\s*=/gm over apps/editor/src/engine and never touches OverridePanel.CATEGORIES or ELEMENT_TYPES. (4) CONFIRMED AND THEN SOME — `planViewFillRenderer` has exactly ONE occurrence repo-wide (its own :268 declaration); there is no index.ts in apps/editor/src/engine/views/plan-canvas/; a second sweep over .js/.jsx/.tsx/.mjs returned zero. The self-describing 'duplicate painters' header at :22-27 and the three misleading comments (initScene.ts:1003, DrawingLayerIdentity.ts:14, RoomStore.ts:29) are all verbatim. (5) CONFIRMED — OCCUPANCY_PALETTE at RoomColourSystem.ts:114 is a frozen literal; every one of the ~15 non-test references across packages/apps/plugins/src is a READ (RoomColourSystem :182/:186/:229, RoomSystemTypeStore :57/:126, spaceEnvelopeAppearance :150, residentialRoomLibrary :403, RoomPropertySection :163, plus barrel + comments); zero assignments. resolveForMode's 'occupancy' branch at :229 is verbatim; 'Room type' is the first ROOM_COLOUR_MODE_CHOICES entry. (6) CONFIRMED — isPlanLike defined at :378-379 and again at :1953-1955, each followed by `if (isPlanLike) this._renderRoomFills(ctx);` at :383 / :1959; the ISSUE-LOG NOT-done text is on disk at docs/04-reference/ISSUE-LOG.md:24773 under the L-1610…L-1617 block at :24739. (7) CONFIRMED, but downgrade 'rival' to 'inert' — the 19 keys + __default__ are exactly as listed (wall, structural-wall, curtain-wall, slab, column, structural, beam, door, window, curtain-panel, stair, railing, roof, ceiling, furniture, plumbing, grid, annotation, level), the divergence runs both ways as claimed, the false header line is verbatim at :14, and getElementTypeRules() has exactly ONE production caller (BimGridRenderer.ts:126, hard-coded 'grid'). It cannot govern anything, so it is a documentation defect with no user symptom — real, but do not let it consume lane budget. (8) CONFIRMED — no gate compares against ELEMENT_TYPES or OverridePanel.CATEGORIES. PLAN_CASES (11 distinct categories over 15 rows) has no room and no lighting; the recorder binds only ctx.stroke (:153) and there is no ctx.fill recorder at all, so a fill-only family is structurally invisible. The one new arm added today, lightingIsAClassifiedLayer.test.ts:120-126, source-reads VisibilityIntentDefaults.ts and asserts a single literal /^\\s*'lighting',\\s*$/m — one family, not a set comparison. lightingAndRoomReachThePlanView.test.ts is an INVALIDATION gate (ViewDependencyTracker membership), not a category-registry gate. (9) CONFIRMED item by item — CATEGORY_TO_DXF_LAYER is exactly the 14 named (no room, no lighting) and :716's Object.keys().length is what prints the log line; VGCategory union carries 'room' at :65 and ELEMENT_TYPE_TO_VG_CATEGORY carries the three room spellings at :143-145; ISO_CUT_LAYER_TO_POCHE_FILL is 6 entries and VG_CATEGORY_TO_ISO_LAYER 6, neither with room; resolvePen returns FALLBACK_PEN on an unknown category (PenWeightTable.ts:306-307); VGToIntentMigration.ts:167 is verbatim `?? vgCategory`; inspectCategories.ts:133, BrowserDataHelpers.ts:323/421/612/657 all carry rooms. BUT the map is INCOMPLETE against its own claim of 'every list' — see miss 1."
  ],
  "missed": [
    "MISS 1 — ROOM LINEWORK IN PLAN IS UNGOVERNABLE, AND IT IS THE IDENTICAL DEFECT THE SIBLING LIGHTING LANE FIXED TODAY. Three tables the audit never opened have no room row: `EdgeProjectorService.ELEMENT_TYPE_TO_PROJECTION_LAYER` (apps/editor/src/engine/views/EdgeProjectorService.ts:203 — no 'room'/'Room'/'RoomVolume'), `DrawingLayerIdentity.ISO_LAYER_TO_VG_CATEGORY` (packages/core-app-model/src/drawing/DrawingLayerIdentity.ts:96-133 — no room), and `penCategoryForLayerTag` (packages/core-app-model/src/drawing/PenWeightTable.ts:449-469 — eleven arms, none for room). This matters because room planes ARE scene geometry that survives into the plan pipeline: RoomBoundaryBuilder stamps `mesh.userData.elementType = 'room'` on the floor fill (:342) and the volume (:407); initScene.ts:1008-1016 `_applyRoomOverlayVisibilityForView` sets `obj.visible = !is3DModelView`, i.e. room overlays are VISIBLE in every plan/section/elevation view and hidden only in pure 3D; and `EdgeProjectorService.CACHEABLE_ELEMENT_TYPES` (:2026-2031) already lists `'room'` with the note `room — RoomBoundingLineBuilder.ts:114`, which is only there because the projector routinely sees room groups. With no projection-layer row those edges fall to `FALLBACK_NATIVE_LAYER = 'projection-visible'` (:271), which `penCategoryForLayerTag` cannot classify and which `VGSceneApplicator.applyToProjectionLayers` cannot target (CATEGORY_TO_DXF_LAYER has no room). That is word-for-word the state the lighting lane described this morning in commit 584aaab2 before adding `Lighting: 'A-LGHT'` + `'A-LGHT': 'lighting'`, and the state `'A-AREA': 'spaceEnvelope'` was added for on 2026-09-05. Finding 9 not only omits all three, it actively tells the lane to LEAVE the ISO side alone on the ground that 'rooms are not sliced solids' — true of POCHÉ, and irrelevant to projected room-plane EDGES. It also bears on the founder's other sentence in the same paragraph ('THE LINES ARE ALMOST NOT READABLE'), because an unclassified layer gets the generic fallback pen. ⚠ ONE STEP UNCONFIRMED STATICALLY: that NativeElementMeshExporter is actually handed room roots in the plan path — the exporter (packages/core-app-model/src/geometry/NativeElementMeshExporter.ts) selects from roots passed in and has no room-specific arm either way. Confirm in the browser before acting; do not add rows on the strength of the CACHEABLE entry alone.",
    "MISS 2 — THE PROPOSED ONE-LINE FIX SILENTLY DOES NOTHING FOR ANY USER-AUTHORED INTENT. Finding 1 says adding 'room' to ELEMENT_TYPES 'alone makes the row appear'. That is true only for SYSTEM intents. The panel enumerates `Object.keys(intent.elementRules)` off the STORED record (VisibilityIntentPanel.ts:240); `visibilityIntentStore.deserialize()` (packages/core-app-model/src/presentation/VisibilityIntentStore.ts:112-124) restores from the project snapshot only rows where `!raw.isSystem`, passing each through `migrateIntentToCurrent`; and NO migrator backfills element types — `IntentSchemaMigrations.ts` has MIGRATORS for 1→2, 2→3, 3→4, 4→5 with CURRENT_INTENT_SCHEMA_VERSION = 5, and its own v3 note says the ifc-element addition needed 'no transform required either; missing rules fall back through IntentRuleResolver.rulesFor() → __default__'. That reasoning is exactly wrong for a PANEL: the RESOLVER tolerates a missing key, the panel's key enumeration does not. System intents are rebuilt from defaults each boot, so the founder's own 'Architectural Documentation (Auto)' will get the row — but anyone who duplicated a system intent to author it (the flow ViewPropertiesPanelBuilders.ts:190-196 exists to support, per §FIX-INTENT-AUTHORING-DEAD-END L-779) will silently not. The same hole shipped for 'lighting' this morning. Fix is either a v5→v6 migrator that unions missing ELEMENT_TYPES keys into elementRules, or render the rows from `ELEMENT_TYPES ∪ Object.keys(intent.elementRules)`. This is the difference between a row that appears and a row that appears for the person who already customised their intent.",
    "MISS 3 — THE AUDIT'S ELEMENT_TYPES ENUMERATION IS ALREADY STALE; 'lighting' LANDED TODAY. HEAD's list (VisibilityIntentDefaults.ts:15-46) is 20 entries: __default__, wall, slab, column, structural, beam, door, window, stair, roof, ceiling, furniture, plumbing, **lighting**, grid, annotation, level, boundary-line, ifc-element — 'lighting' added at :33 by commit 584aaab2 (§LIGHTING-IS-A-CLASSIFIED-LAYER, L-13267) hours before this audit. So the panel renders 20 rows and not 19, finding 1's 18+default enumeration is wrong, and finding 8's 'lighting is simply absent from both tables' is now half-obsolete (it is still absent from OverridePanel.CATEGORIES and from the PLAN_CASES gate, but present in the declaration list). Do NOT re-add lighting to ELEMENT_TYPES. It also blunts finding 9's item (7): CATEGORY_TO_DXF_LAYER still has no lighting even though the lighting lane added the projection-layer and ISO→VG rows, which is more evidence for miss 1 that CATEGORY_TO_DXF_LAYER is NOT the table that governs projected linework — ELEMENT_TYPE_TO_PROJECTION_LAYER and ISO_LAYER_TO_VG_CATEGORY are.",
    "MISS 4 (minor, but the same trap class as finding 4) — `DataPanelRenderer`'s VG category legend is DEAD CODE and will be mistaken for 'the VG panel'. packages/core-app-model/src/presentation/DataPanelRenderer.ts:285-293 does `const vgStore = window.vgGovernanceStore`, then `vgStore.getActiveTemplate?.()` and iterates `template.categoryStyles`. `grep -n \"categoryStyles\\|getActiveTemplate\" packages/core-app-model/src/presentation/VGGovernanceStore.ts` returns ZERO hits — the method does not exist (optional-chained away) and the field is `categories`, not `categoryStyles`. The legend can therefore never emit a single category, room included. It is the only other place in the repo that looks like it enumerates VG categories for a user, so an agent hunting for 'where the category list is drawn' will land here and 'fix' rooms into a renderer that produces nothing — the exact [[committed-is-not-reachable]] shape finding 4 correctly flags for PlanViewFillRenderer."
  ]
}