{
  "survivingTitles": [
    "THE BREAK — the only line-type control in the product refuses to write on the intent that governs his views (four gates, all on `isSystem`)",
    "The per-view \"Visual Style\" control (its options include \"Hidden Line\") and \"Display Model\" are WRITE-ONLY — zero readers in the entire draw path",
    "The V/G (Visibility/Graphics) tier cannot express a line type at all, and has no human writer — only the AI has one",
    "Two layer classifiers disagree on the same line: six ISO layer families get VG COLOUR governance but fall to the `__default__` (wall) pen for DASH and WIDTH",
    "(b) answered — \"22 styled vs 63 ISO vs 14 categories\" are three DIFFERENT UNITS; \"41 layers never styled\" is not established, and the real unstyled set is a different one",
    "The element-tier pen override (priority 10000, the strongest tier in the cascade) has zero callers — the whole per-element styling tier is dead"
  ],
  "refutedTitles": [
    "The inverse defect: `output.beyondLineStyle` and `output.occlusionDisposition` are READ by the drawing engine and written by NO UI — the dashed-hidden-line answer to (d)",
    "The ViewTemplate → StrokeStyle.dash chain — a complete, schema-validated line-type system — is orphaned from the editor"
  ],
  "refutations": [
    "REFUTED (finding 3) — the UI now exists; the fix would rebuild a commit that is already at HEAD. `buildOutputSection` renders BOTH selects unconditionally: `mkOutputSelect('Occluded lines', o.occlusionDisposition, [(inherit)/Remove/Show dashed] …)` at apps/editor/src/ui/ViewPropertiesPanelBuilders.ts:373-386 and `mkOutputSelect('Beyond lines', o.beyondLineStyle, [(inherit)/Solid/Dashed] …)` at :388-401, both writing through `host._fireSetViewOutput` with the `patch.X = val || undefined` idiom the finding prescribes. Landed in HEAD `9babe6f5` (2026-09-09 07:22, `feat(docs/§LINE-TYPE-CONTROLS-EXIST-NOW L-13268)`), i.e. AFTER the audit's 2026-09-08 measurement. `grep -rn 'occlusionDisposition\\|beyondLineStyle' apps/editor/src/ui/` now returns 6 hits, not 0. KEEP the diagnostic half — `_PLAN_SCOPE.occlusionDisposition: 'remove'` (ViewScope.ts:155) and `_ELEVATION_SCOPE: 'demote'` (:143) are confirmed verbatim, so '0 sub-segment(s) demoted' in a PLAN log is the correct number, not a defect — but the finding's mechanism ('written by NO UI') and its ~40-line fix are both stale.",
    "REFUTED (finding 8) — the measurement used the wrong package specifier and the proposed fix would break live code. The workspace is named `@pryzm/plugin-plan-view` (plugins/plan-view/package.json:2), NOT `@pryzm/plan-view`, so BOTH cited greps were false negatives. Re-measured: `grep -rln '\"@pryzm/plugin-plan-view\"' --include=package.json` → plugins/plan-view/package.json AND plugins/sheets/package.json:35; `plugins/sheets/src/sheet-editor-host.ts:56` value-imports `CanvasHost` from it and `:205` declares `export class SheetEditorHost extends CanvasHost`; `apps/editor/package.json:70` depends on `@pryzm/plugin-sheets`. The plugin is compiled and live in the sheet editor. Likewise `geometry-kernel/src/view-resolution` is re-exported from `packages/geometry-kernel/src/index.ts:463-470` and again from the L5 SDK facade `packages/plugin-sdk/src/index.ts:362-364` — deleting it, as the fix proposes, breaks the public SDK surface. What IS genuinely unreferenced is narrower and should be said that way: `view-template-bridge.ts` is not even exported from the plan-view barrel (only its own test imports `resolveSnapshotForView`), and `StyleResolver` (index.ts:75-78) has no production consumer — and note it contains ZERO occurrences of `dash`, so the claim that `style-resolver.ts` 'does honour dash' is also false; the dash lives in `packages/geometry-kernel/src/view-resolution/resolver.ts` (5 hits), a different file."
  ],
  "missed": [
    "MISSED — THE PRIORITY INVERSION THAT SURVIVES FINDING 1'S FIX. Even after the user obtains an editable intent, `line.style` on the `beyond` state is silently overwritten on every elevation and section. `RULE_PRIORITY_INTENT = 1000` (GraphicsRulesEngine.ts:39) but `_beyondDashRules` injects `dashPx: [...BEYOND_DASH_PX]` at `RULE_PRIORITY_VIEW_TYPE_MODIFIER = 5000` (:41, :268-274) with no `category` field, so it matches every category. Its own header defends this by saying it sits 'BELOW the VIEW (9000) and ELEMENT (10000) tiers, so a user who explicitly styles this zone still wins' — but the audit's own findings 4 and 7 prove BOTH of those tiers are unreachable by a human: `addElementOverride` (10000) has zero production callers, and 9000's only writer is `PlanViewVGApplicator.syncViewOverrides`, which emits `widthMm`/`color`/`opacity` and never `dashPx`. So the tier the comment says protects the user does not exist, and a view-type DEFAULT does outrank his decision — the exact founder complaint, still true after finding 1 is fixed.",
    "MISSED — THE FOUNDER'S THIRD SENTENCE IS UNANSWERED: 'THE ROOMS SHOULD HAVE A CATEGORY ON THE VISIBILITY INTENT SETTINGS TO MODIFY THE COLOURS - THAT I DONT SEE'. `room` IS a first-class VG category (VGSceneApplicator.ts:65 in the category union, :143 mapping room/Room/RoomVolume, :786 a dedicated arm, plus presentation/RoomColourIntent.ts and `roomColourMode` on `VGCategoryStyle`), but it is ABSENT from `VisibilityIntentDefaults.ELEMENT_TYPES` (VisibilityIntentDefaults.ts:15-34 — 19 entries: __default__, wall, slab, column, structural, beam, door, window, stair, roof, ceiling, furniture, plumbing, lighting, grid, annotation, level, boundary-line, ifc-element). The panel's rows are `Object.keys(intent.elementRules)` (VisibilityIntentPanel.ts:240), seeded from exactly that list — which is literally why he cannot see a room row. `spaceEnvelope` is in the same position (it has an ISO row `'A-AREA': 'spaceEnvelope'` at DrawingLayerIdentity.ts:128 and a real producer, but no intent row). ALREADY-SOLVED SHAPE: commit 584aaab2 added `lighting` to this exact list four hours ago — copy that one-line-per-family move, do not invent a mechanism.",
    "CORRECTION to finding 5 — one third of it was fixed at HEAD this morning and three of its named families are not what it says. (a) `A-LGHT` is DONE: HEAD `584aaab2` (`fix(docs/§LIGHTING-IS-A-CLASSIFIED-LAYER L-13267)`) added `isLighting?` to `categoryFromFlags` and the `/A-LGHT|lighting/i` arm to `penCategoryForLayerTag` (PenWeightTable.ts:415-417, :460-462), `'A-LGHT': 'lighting'` to `ISO_LAYER_TO_VG_CATEGORY` (DrawingLayerIdentity.ts:109), the EPS routing rows, and `'lighting'` to ELEMENT_TYPES. Re-doing it rebuilds today's commit. (b) `spaceEnvelope`, `curtain-wall`, `curtain-panel` and `handrail` are NOT rows the panel offers — none is in ELEMENT_TYPES, so 'the panel's spaceEnvelope row' and 'two further categories the panel offers' are both wrong; the existing test's own comment ('handrail is a category the view-intent panel OFFERS', viewIntentGovernsEveryCategory.test.ts:306) is wrong in the same way and should be corrected while you are in there. (c) `grid`/`level` have panel rows but NO producer stamps `A-GRID`/`A-LEVL` in the live 2D pipeline (only DrawingLayerIdentity, the unreachable DrawingPipelineWorker map, and comments), so that half is vacuous. WHAT SURVIVES AND IS REAL: `A-PLMB` (PlumbingPlanSymbolBuilder.ts:37, PlumbingElevationSymbolBuilder.ts:34), `S-COLS` (ColumnPlanSymbolBuilder.ts:48 — `/A-COLS|column|beam/i` does not match 'S-COLS') and `A-AREA` (SpaceEnvelopePlanSymbolBuilder.ts:73) all fall to `'projection'` (PenWeightTable.ts:418) → `rulesFor` finds no `elementRules['projection']` → `__default__` (IntentRuleResolver.ts:92-108) → the WALL pen (VisibilityIntentDefaults.ts:108). Confirmed exactly as described, for three families, not six.",
    "CORRECTION to finding 1 — the mechanism is confirmed line-for-line (:264 passes `intent.isSystem` as `disabled`; :347 the `line.style` select; :772 `if (intent.isSystem) return;` before the `[data-appearance]` wiring at :778; VisibilityIntentStore.ts:72 `if (this._systemIntents.has(intentId)) return null;`; UpdateVisibilityIntentCommand.ts:48 — note the method is `canExecute`, not `validate`; SystemIntents.ts:48 `isSystem: true` inside `makeIntent`, :276 'Architectural Documentation (Auto)'), and VisibilityIntentPanel really is the sole `line.style` writer (repo-wide grep: only :347-349, :1286, and the two read sites IntentRuleResolver.ts:143/:213). BUT the SYMPTOM is false and half the FIX already exists. There is no silent refusal: VisibilityIntentPanel.ts:206-211 renders a purple `vi-readonly-banner` reading '\"<name>\" is a system intent and is read-only. Every field below is disabled. Press Duplicate above to make an editable copy — the copy is bound to this view automatically', with the branch at :210 explaining the unbound case, and the Duplicate button at :202. So 'Nothing on screen says why' and 'the escape hatch is unlabelled as the required first step' are both wrong, and fix part (2) is entirely built. Only fix part (1) (copy-on-write) is new work — and note it cannot be intercepted at `updateAppearance` as written, because the inputs are `disabled` AND unwired, so the form must first be re-enabled; also `duplicateIntent` binds only `this.contextViewId` (:945), so on the founder's 13 views a copy-on-write still leaves 12 views on the system intent.",
    "CORRECTION to finding 2 — there are TWO rival 'Visual Style' controls, not one, with disjoint option sets, and the audit found only the newer. `ViewPropertiesPanel.ts:394-411` builds its own select with `['Consistent','Textures','Realistic']` writing `(this.selectedView as any).visualStyle = value` DIRECTLY on the view object (:241-243, bypassing the command bus entirely), while `ViewPropertiesPanelBuilders.ts:408-431` builds the wireframe/hiddenLine/shaded/shadedWithEdges/realistic one writing `output.visualStyle` through `_fireSetViewOutput`. Neither is read by anything (repo-wide, the only readers of `output.visualStyle` are `SetViewOutputCommand.ts:68` validating it and `SyncStateEngine.ts:321,334` comparing it; `getVisualStyle` in geometry-slab is an unrelated numeric 3D-fragment flag). 'Wire or withdraw' must handle both, or the founder finds the other one. [memory: same-rule-two-implementations]",
    "CORRECTION to finding 6 — arithmetic and mechanism confirmed exactly (14 keys / 11 distinct values in `CATEGORY_TO_DXF_LAYER` VGSceneApplicator.ts:87-101; the loop head at :627 is `for (const [category, layerName] of Object.entries(CATEGORY_TO_DXF_LAYER))` so curtain-wall/curtain-panel/handrail genuinely race wall/window/stair last-write-wins; `appliedCount++` at :659, :688, :695, :707 counts layer OBJECTS; no `:hidden` arm — all 5 'hidden' hits in the file are comments; SVGCompositeRenderer.ts:246-250 really does read `mat.linewidth`). Two notes: the EPS line number has drifted to `totalLayerCount++` at EdgeProjectorService.ts:3445 (the lighting commit inserted 28 lines), still inside `for (const layerName of layerNames)` at :3097 nested in the per-group loop, so the group-layer-pair reading holds. And a stronger companion fact the finding stops short of: `curtain-wall`, `curtain-panel` and `handrail` are unreachable on the SCREEN axis too, not just mis-ordered on export — the canvas resolves VG per line through `vgCategoryForLayer(layerTag)` (PlanViewCanvas.ts:463), whose `ISO_LAYER_TO_VG_CATEGORY` never returns any of those three names. They exist only as keys of the export-side map, so no VG decision about them can ever reach a pixel.",
    "NOTE on the headline proof — REPRODUCED, but not by the command as written. `cd apps/editor && npx vitest run __tests__/viewIntentGovernsEveryCategory.test.ts` fails in this environment with `[vitest-pool]: Failed to start forks worker` / `Timeout waiting for worker to respond` after 60 s (twice). With `--pool=threads` it returns `Test Files 1 passed (1) / Tests 23 passed (23)` at HEAD `9babe6f5`, i.e. the 23-pass claim holds today even after `584aaab2` touched PenWeightTable, DrawingLayerIdentity and VisibilityIntentDefaults. Quote the flag, or the next lane will read the pool timeout as a red chain."
  ]
}