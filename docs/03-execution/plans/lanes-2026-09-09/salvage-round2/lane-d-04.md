{
  "refuted": true,
  "reason": "The MECHANISM half is confirmed line-for-line; the CAUSAL half — that flipping the flag saves ~2000ms of the measured load — is refuted by the code.\n\nWHAT VERIFIED EXACTLY (unusually accurate citations, all checked):\n- packages/geometry-furniture/src/FurnitureFragmentBuilder.ts:64 — `private _instancingEnabled: () => boolean = () => false;` ✓\n- packages/core-app-model/src/rendering/FurnitureInstanceBridge.ts:125-129 — bare `.__pryzmFurnitureInstancingV1 === true`, no per-family default ✓\n- Grep across apps/ packages/ plugins/ src/ tools/ scripts/: ZERO production assignments. Only FurnitureInstanceBridge.test.ts (sets it), pryzmPerfConsole.ts:913/1168 (reads), tools/perf/render/render-profile.spec.ts:342 (reads), and comments. ✓\n- initBuilders.ts:817-820 injection is unconditional ✓ · gate at FurnitureFragmentBuilder.ts:331 ✓ · elementType stamp at :265 verbatim ✓ · GLB early return ✓\n- InstanceGroup.ts:87-88 `castShadow = true` ✓ · heavySceneSwapThreshold 400/1000 ✓ (and the honesty note is right: 550 elems trips the element arm regardless)\n- RenderPipelineManager.ts:2034-2038 → `_buildPipeline()` (packages/renderer-three/src/pipeline/, NOT apps/editor/src/rendering/ as cited) has NO per-mesh or per-caster loop ✓ — the claim's own rejection of the \"16s = shadow over 3274 casters\" story holds.\n\nTHE REFUTATION — the fix cannot reduce hydrate, and on its compute half can only ADD:\nThe gate at :331 sits 88 lines BELOW `mesh = builder.build(resolved)` at :243. There is exactly one early-out in updateFurniture (the glb_import return at ~:149). So on the synchronous hydrate path the claim itself traces (ImportProjectCommand:1071 → CreateFurnitureCommand → DOMEventBus → updateFurniture → \"every BufferGeometry and Mesh allocated\"), ALL 2134 meshes are still constructed with the flag ON. The flag only decides where they go afterwards.\nWorse, the instanced branch is strictly additive: FurnitureInstanceBridge._bakeParts (:333-377) does `m.geometry.clone()` + `applyMatrix4()` per leaf, then `mergeGeometries` per material bucket, then registers into InstancedElementRenderer — then FurnitureFragmentBuilder :~345 `scheduleGpuRelease` on every leaf. Net hydrate compute: unchanged build + clone + merge + register. The claim's STEP 0 frames the direction as unknown (\"helps hydrate, hurts hydrate, or only helps frame time\"); the code already determines the compute half — it cannot help, it can only cost.\n\nARITHMETIC: hydrate is 17163.5/17258.5 = 99.4% of load. 2000ms over 2134 meshes = ~940µs per BoxGeometry-class mesh — two to three orders of magnitude off. There is no code path making a trivial mesh allocation cost 1ms.\n\nTHE REPO ALREADY NAMES THE LIKELIER DOMINANT TERM, and the claim asks for the wrong log line. ProjectLoader.ts:855-862 (§LOAD-YIELD-WAIT, L-3054) states it verbatim: \"the load is COMPUTE + WAITING, and only the compute half was ever measured... if a frame costs 500 ms, every yield costs 500 ms, and a load with 30 yields pays 15 s for waiting alone with ZERO of it visible in any existing counter.\" IMPORT_CHUNK_SIZE = 120 (ImportProjectCommand.ts:143), so 550 elements yield only a handful of times per step across ~20 steps — a plausible 5-15s of pure parked-in-the-frame-bus time that no counter in the founder's paste shows.\nThe claim asks for §LOAD-HYDRATE-STEPS (real — ProjectLoader.ts:564, emitted at :2874 before `__phase('hydrate')` at :2875), but that line buckets the entire import into one `element_import` number and CANNOT attribute furniture. The line that actually sizes this claim is §LOAD-IMPORT-STEPS (ProjectLoader.ts:~919), which prints `furniture=XXXms/n=NNN` per family from the `__step('furniture', ...)` at ImportProjectCommand.ts:1071 — the claim never names it.\n\nSECONDARY: _bakeParts buckets by raw `mat.uuid` within an item, so a ChairBuilder item minting 28 distinct materials bakes to 28 parts, not 1 — the per-item collapse is ~2.4x, not 66x (cross-item dedup by signature still collapses N identical chairs into those 28 groups, so the draw-call win is real, just smaller than \"1487 meshes\" implies).\n\nVERDICT: ship the flag fix for DRAW CALLS, FRAME TIME and shadow-caster submission — that part is sound and the dead-gate finding is genuine. Do not bank it against the 17.2s load or the 16.0s shadow rebuild; neither number has a mechanism in this code, and the hydrate half moves the wrong way.",
  "betterHypothesis": "The 17.2s hydrate is dominated by two terms the founder's paste omits and this claim never measures: (1) YIELD-WAIT — the chunked loader parks in the frame bus between chunks (ProjectLoader.ts §LOAD-YIELD-WAIT, L-3054, whose own comment predicts \"30 yields pays 15 s for waiting alone\"), so hydrate scales with FRAME COST during load, not with allocation cost; (2) per-family step compute, already decomposed by §LOAD-IMPORT-STEPS as `furniture=…ms/n=…` across ~20 `__step()` buckets. Mesh count can only touch term (1) — a scene holding 2134 fewer meshes paints cheaper frames for every yield AFTER the furniture step (curtain walls, beams, rooms all come later), which is the only route by which furniture instancing could shorten load at all. But the flag pays full build cost PLUS clone+merge, so even that route is net-uncertain.\n\nCorrect next step, replacing the claim's STEP 0: ask for the two existing lines from the UNMODIFIED run first — `[ProjectLoader] §LOAD-YIELD-WAIT compute=…ms waiting=…ms yields=N avgWait=…ms maxChunk=…ms` and `[ProjectLoader] §LOAD-IMPORT-STEPS furniture=…ms/n=… …`. If `waiting` dominates `compute`, the fix is the yield cadence or the frame loop (and furniture instancing is a second-order contributor at best); if `compute` dominates and `furniture=` is a large bucket, the fix is to make the ~60 procedural builders cheaper — extend mergedPartKit past its current 7 of ~60 importers, or bake per-(furnitureType, config) geometry once and clone — none of which requires the instancing flag at all. Separately, the 16.0s SHADOW_REBUILD_COMPLETE is an independent investigation: `_buildPipeline()` awaits `import('three/webgpu')` and constructs TSL passes with no per-mesh work, so time it inside that function before attributing it to scene content."
}