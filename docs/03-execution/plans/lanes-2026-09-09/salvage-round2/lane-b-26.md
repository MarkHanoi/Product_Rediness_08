{
  "refuted": false,
  "reason": "CONFIRMED on the evidence lens. I opened every cited file and the quoted TEXT exists verbatim in all fifteen citations; the line numbers are exact for 22 of them and drift by 1-8 lines on six (spaceEnvelopeDragSurface :580 is really :572; the install console.log is :1433 not :1434; projectScopeRegistry.reseedAll() is ClearProjectCommand.ts:251 not :249; that file's \"first step of ProjectLoader.load()\" header line is :20 not :21; ensureGisInitialized('eager') is GISAreaLayout.ts:2614 not :2612; the CesiumThreeBridge provider is :1103 not :1101). No quote is invented, mis-attributed, or read out of context.\n\nThe mechanism holds under independent checks the author did not run: (1) a brace-depth pass proves the install at :1336 sits at depth 7 inside `if (!cesiumViewport) {` (:1042) inside the `.then()` of the one-shot `gisInitPromise` (:1006), and that `gisInitPromise = null` (:1451) is at depth 3 inside the `.catch` of a FAILED mount, so a successful init can never re-run; (2) grep shows the ONLY assignments to `envelopeFaceDrag3dDispose` anywhere are :1200 (null), :1336 (install) and :8504 (null-on-clear), and the ONLY call to `registerSpaceEnvelopeFaceDragSurface` in the entire repo is :1273 (the second production install of the gesture, spaceEnvelopeFaceDragController.ts:84, is the THREE/BIM-3D path and registers no surface — the claim's wording is accurate); (3) `ProjectScopeRegistry.reseedAll()` (packages/core-app-model/src/persistence/ProjectScopeRegistry.ts:140) does `if (typeof store.reseed !== 'function') continue;`, and the gis.areaLayout registration (GISAreaLayout.ts:156-160) declares only `clear`, so reseedAll provably restores nothing; (4) GIS_SWITCH_SCOPES (siteProjectScope.ts:104-120) is DERIVED from DECLARED_PROJECT_SCOPES filtered only by owning module and `ownsTeardown !== false`, so gis.areaLayout is in it and the switch route reaches the same clear; (5) the asymmetry at :8498 is real — `envelopeDraw3dUnregister` is genuinely never called in clearLayoutProjectState, so DRAW survives what FACE-DRAG does not; (6) the falsification signature is real: the two log strings are at :1433 and :8514, and the tooltip text \"Dragging a face needs a 3-D view under the pointer, and none is attached right now\" is verbatim SPACE_ENVELOPE_FACE_DRAG_NO_SURFACE_REASON at spaceEnvelopeFaceDragSurfaces.ts:93, gated at :106 on `registered.length === 0`.\n\nThe strongest corroboration is a file the author never cites: apps/editor/src/ui/layout/__tests__/siteFaceDragWire.spec.ts:60-66 pins the teardown in place with the rationale \"A listener set that outlives its scene gives the NEXT PROJECT two of them\" — a sentence that is only coherent if a fresh install runs per project. It does not. git log confirms install and teardown shipped in the same commit (c8c62c51), so the defect was born with the feature.\n\nTWO HONEST CAVEATS, neither fatal. (a) The author's load-bearing premise citation — declaredProjectScopes.ts:251 \"mountGISArea() runs once per tab\" — is directly contradicted by GISAreaLayout.ts:126, IN THE FILE THEY ARE DIAGNOSING: \"MODULE-SCOPE AND SINGLE, ON PURPOSE. `mountGISArea` can run more than once in a session (project switch)\" (echoed at eagerGlobeStart.ts:238 and by the delegate comment \"A second `mountGISArea` replaces the delegate\"). They cited the weaker of two conflicting comments without noticing the conflict. I resolved it in their favour independently rather than on their evidence: mountGISArea has exactly one invocation (Layout.ts:92) inside createMainLayout (Layout.ts:86), reached only from initUI.ts:3301 inside bootstrap(), which src/main.ts:203/213 guards with `_bootstrapped` — \"guards against re-running bootstrap() on subsequent project opens\". So once per tab is TRUE in production and the :126 comment is stale. (b) That guard has one hole the claim overstates as \"no re-install path anywhere\": src/main.ts sets `_bootstrapped = true` only AFTER bootstrap() resolves, so a FAILED first boot that the user retries does produce a second mountGISArea with a fresh gisInitPromise = null — a re-install path, but only on the boot-failure-retry edge, never on a normal project switch. Neither caveat touches the two orderings the claim relies on.",
  "evidenceChecked": [
    "GISAreaLayout.ts:1006 — EXACT: 'if (gisInitPromise) return gisInitPromise;'; grep shows gisInitPromise touched only at 618 (decl), 1006, 1019 (assign), 1451 (null), 1453 (return)",
    "GISAreaLayout.ts:1451 — EXACT, and a brace-depth pass confirms it is at depth 3 inside the .catch of the failed-mount path, so a SUCCESSFUL init can never re-run",
    "GISAreaLayout.ts:1042 — EXACT: 'if (!cesiumViewport) {'; depth 3, and the install at 1336 is depth 7 inside it",
    "GISAreaLayout.ts:1336 — EXACT: 'envelopeFaceDrag3dDispose = installSpaceEnvelopeFaceDragOnSurface({'; repo-wide grep confirms it is the only Cesium-canvas binding (the other production caller, spaceEnvelopeFaceDragController.ts:84, is the THREE/BIM-3D path)",
    "GISAreaLayout.ts:1273 — EXACT: 'envelopeFaceDrag3dUnregister = registerSpaceEnvelopeFaceDragSurface(envelopeDraw3d);'; repo-wide grep shows this is the ONLY call to that function outside its own definition (spaceEnvelopeFaceDragSurfaces.ts:62)",
    "GISAreaLayout.ts:1284 — EXACT: 'const dragCanvas = envelopeDraw3d.dragDomElement();'",
    "GISAreaLayout.ts:8479/8498/8504/8509/8514 — ALL FOUR EXACT, verified by line count over the whole clearLayoutProjectState body; :8498 really does call only envelopeDraw3d?.disarm() and never envelopeDraw3dUnregister, so the DRAW/FACE-DRAG asymmetry is real",
    "GISAreaLayout.ts:156-160 — the gis.areaLayout registration declares ONLY 'clear: () => { _gisLayoutDelegate?.clear(); },' (:159 exact). No reseed key exists",
    "ProjectScopeRegistry.ts:140 reseedAll() — 'if (typeof store.reseed !== \"function\") continue;' — proves reseedAll provably restores nothing for this scope",
    "ClearProjectCommand.ts:246 — EXACT 'const report = projectScopeRegistry.clearAll();'. reseedAll() is at :251 (claim said :249) and the ProjectLoader.load() header line is :20 (claim said :21) — text real, numbers off by 2 and 1",
    "siteProjectScope.ts:287 — EXACT; and GIS_SWITCH_SCOPES at :104-120 is DERIVED from DECLARED_PROJECT_SCOPES (filtered only on owning module and ownsTeardown !== false), so gis.areaLayout is provably a member",
    "siteProjectScope.ts:441 — EXACT: bus.on('pryzm-project-switch', () => runSiteProjectTeardown('project-switch', runtimeRef))",
    "PlatformShell.ts:248 and :231 — BOTH EXACT, including the 'Everything after this mark is per-project work; everything before it is boot' marker",
    "declaredProjectScopes.ts:249/251 — BOTH EXACT — but CONTRADICTED by GISAreaLayout.ts:126 'mountGISArea can run more than once in a session (project switch)', a conflict the author never surfaces",
    "src/main.ts:203/213 (_bootstrapped guard) + Layout.ts:86/92 + initUI.ts:3301 — the chain I ran to resolve that contradiction: bootstrap() cannot re-run on later project opens, so mountGISArea IS once per tab, EXCEPT after a bootstrap that throws (main.ts sets the flag only after resolve), which is a genuine re-install path the claim's 'anywhere in the codebase' overstates",
    "CesiumViewport.ts:18483 — EXACT 'paneEl.appendChild(this.container); // moves the single node — no clone, no 2nd viewer.' — the re-parenting rival really is ruled out",
    "CesiumViewport.ts:8755 — EXACT; the same this.formaMassingOrigin field is read by renderSpaceEnvelopes at :8832 (claim said 8833) — the 'visible prisms imply a live frame' argument holds",
    "CesiumViewport.ts:6477-6482 — EXACT: 'this.dispose()' … 'await this.mount()', so the captured-viewer staleness in siteEnvelopeDrawCesium.ts:254 (EXACT 'private readonly viewer: CesiumNS.Viewer;') is a real second bug, contrasted correctly with the provider at GISAreaLayout.ts:1103 (claim said :1101)",
    "spaceEnvelopeDragSurface.ts:586 — EXACT 'const picked = pick(ev);' / ':587 if (!picked) return;' with NO log — the silence claim is true. But the focus line quoted as :580 is actually at :572; :580 is 'if (focus === null) return null;' inside focusedRecord()",
    "spaceEnvelopeFaceDragSurfaces.ts:62-93 — the unregister closure really does release the focus when the last surface goes, registeredSpaceEnvelopeFaceDragSurfaces() returns registered.length, and the NO_SURFACE_REASON string at :93 matches the claimed confirming tooltip verbatim",
    "apps/editor/src/ui/layout/__tests__/siteFaceDragWire.spec.ts:60-66 — NOT cited by the author, and the strongest corroboration: the test pins the teardown with the rationale 'gives the NEXT PROJECT two of them', i.e. it was written believing a per-project re-install exists",
    "git log -L 8496,8513 — the teardown and the install both arrive in c8c62c51 (§ENVELOPE-FACE-DRAG-ON-SITE-VIEWS), so the defect shipped with the feature"
  ]
}