{
  "dimension": "It creates, serialises, saves and server-syncs an empty project — is any of it blocking the first interactive frame?",
  "summary": "ANSWER: almost none of it. The serialise → thumbnail → save-version → server-sync sequence in the founder's log CANNOT execute before the first interactive frame — it is fenced behind a 4,000 ms post-load settle window that opens one statement before the gate event, while the frame lands +334 ms after that same statement. It arrives ≈3.7 s AFTER the frame he is waiting for. Zero of the 9,233 ms. ServerSyncQueue is a 500 ms setTimeout, fire-and-forget; nothing awaits it. IndexedDB schema setup is not paid on the path (the version-cache warm is `void`-ed, and the one synchronous read is a Map miss plus one localStorage.getItem). The post-load thumbnail capture explicitly early-returns on `empty:true`.\n\nTHREE things in this dimension ARE on the critical path, and together they are ~0.1–0.4 s of 9,233 ms:\n(1) ⭐ THE ONE STRUCTURAL DEFECT: `await runtime.persistence.client.create(name)` — a server POST — is the FIRST await on the whole 9.2 s path, and the ENTIRE engine boot (`attachedBootstrap.ensure()` → `startEngine()`) queues strictly behind it. The boot needs nothing the POST returns until step 4. This is the same defect shape as L-13278, which landed today one layer down.\n(2) `resetDirtyAfterLoad()` runs a full serialize + JSON.stringify synchronously, one line before the gate event — that is the \"Snapshot created: 0 elements\" line he sees BEFORE the frame. Cheap on empty, but it is the reason the same line appears twice in his log.\n(3) A contention term, not a block: the \"+ New Project\" gesture never arms L-11540's `_openInFlight` latch and never destroys the hub, so ProjectHub's residency audit, thumbnail back-fill and server sync keep running on the main thread throughout the boot he is timing.\n\n⛔ CRITICAL FRAMING, ESTABLISHED FROM CODE: t0 of his §STARTUP-BUDGET run IS the \"+ New Project\" click. `ProjectHub` calls `onStartOnboarding({name, directEntry:true})`; `showOnboarding` calls `beginStartupBudget()` + `markStartupPhase('onboarding:shown')` and then, in the SAME synchronous task, emits `pryzm:onboarding-brief-ready`, which calls `createAndOpenProject`. There is NO human dwell inside the 9,233 ms. All 9.2 s is machine work — the brief's premise is correct, and the remaining ~8.5–8.9 s is NOT in my dimension (it is engine boot + globe).",
  "findings": [
    {
      "title": "The create POST is the first await on the 9.2 s path, and the entire engine boot queues strictly behind it",
      "detail": "`createAndOpenProject` awaits a server round-trip BEFORE anything that boots the engine runs. Chain, all verified: PlatformRouter.ts:1013 `await …client.create(name)` → :1033 `launchWorkspace(...)` → :1240 `await this.runtime.persistence.openProject(projectId, hint)` → buildPersistence.ts:336 `await attachedBootstrap.ensure()` → main.ts:462 `attachEngineBootstrap({ ensure: () => workspaceMount.ensure() })` → main.ts:362 `await startEngine(runtimeRef.current)`. `attachEngineBootstrap` has exactly ONE consumer (buildPersistence.ts:336) and `workspaceMount.ensure` has exactly ONE production caller (main.ts:462), so there is no second path that starts the boot earlier. ⭐ The boot consumes nothing the POST returns: the only use of `summary` before step 4 is `opts.projectContext.set(...)` (buildPersistence.ts:277) and the step-1 store lookup, which is already satisfied because `ProjectListController.create` (ProjectListController.ts:60) does `this.store.addProject(summary)` — so `projectListStore.isEmpty()` is false and NO extra list refresh is paid. `tier.streamLoad` is also skipped for `isNewProject`. So the POST is pure serial latency in front of the single slowest await on the open path. ⛔ It is same-origin (`baseUrl` defaults to '' — ProjectListClient.ts:242), so no CORS preflight; server-side it is one INSERT (server/api/v1/routes.js:579 `await pgProjectStore.createProject(name, userId)`). ⚠ It is also unbounded upward: on a 503 `migrations_in_progress` the client silently rides out up to MIGRATIONS_RETRY_BUDGET_MS = 60_000 with 1s→2s→4s→8s backoff (ProjectListClient.ts:615-617), and on a transport blip 2 retries at 400/800 ms.",
      "onCriticalPath": true,
      "costMs": "UNKNOWN — but it is ALREADY IN HIS LOG and needs no new instrument. Read `open:router-launch`'s t+ minus `globe:prewarm-start`'s t+ (both fire in the same run; `globe:prewarm-start` is the last mark before the emit, `open:router-launch` is the first after the create resolves). ESTIMATED 100–350 ms for a warm same-origin POST + single INSERT; 1,000–9,000 ms if a migrations-503 was ridden out (his console would show retry warnings).",
      "costBasis": "read-from-code",
      "quote": "const summary = (await this.runtime.persistence.client.create(name)) as {",
      "file": "apps/editor/src/ui/platform/PlatformRouter.ts",
      "line": 1013,
      "fix": "Overlap the POST with the boot instead of serialising them — the same fix L-13278 made one layer down today (§STREAM-LOAD-STARTS-BEFORE-THE-BOOT-AWAIT: start the request early, leave the await point exactly where it is). Concretely: in `createAndOpenProject`, kick `runtime.persistence.prewarmEngine()` (a new, thin passthrough to the same `attachedBootstrap.ensure()`) in the SAME synchronous tick as `client.create(name)`; `ensure()`/`startEngine()` are already documented idempotent, so buildPersistence.ts:336's await then resolves against work already in flight and NOTHING about the ordering of observable effects changes. ⚠ ONE REAL COUPLING TO CLOSE FIRST, do not skip it: the boot reads `window.currentProjectId` at engineLauncher.ts:1229 (`new YjsDocAdapter(window.currentProjectId ?? 'pryzm-project')`) and :1357 (`room: _win.currentProjectId ?? 'pryzm-project'`). Booting before the id exists would bind the CRDT doc to the fallback string. Make YjsDocAdapter's doc id re-seatable at `setProjectContext`, or gate only that one construction behind the id. Do NOT 'fix' it by racing and hoping — line 1229 sits late in the boot and would usually win, which is exactly the kind of almost-always-right that this repo has been burned by.",
      "risk": "medium",
      "confidence": "high"
    },
    {
      "title": "The whole save sequence is fenced 4,000 ms behind the gate event — it lands ≈3.7 s AFTER the first interactive frame",
      "detail": "This is the direct answer to the assigned question, and it is arithmetic, not judgement. On the new-project branch PlatformShell.ts:308-311 runs, in this order and in one synchronous block inside the empty load's `.then()`: `setLoading(false)` → `resetDirtyAfterLoad()` → emit `pryzm-project-loaded`. `setLoading(false)` executes SaveOrchestrator.ts:473 `this._settleUntil = Date.now() + 4000;`. Every autosave is then forcibly deferred to that deadline by SaveOrchestrator.ts:373-379 (`if (now < this._settleUntil) { … setTimeout(…, Math.max(50, this._settleUntil - now)); return; }`), regardless of which trigger armed it (debounce 2500 ms, `handleLoadSuppressEnd`, or `handleBatchEnd` — all funnel through `executeSave`). Meanwhile the mark he quoted is scheduled off the SAME emit: PlatformShell.ts:203-204 arms `open:first-interactive-frame` via `getFrameScheduler().scheduleOnce` on `pryzm-project-loaded`, and it stamped +334 ms. So: frame at T+334 ms, earliest possible autosave at T+4000 ms. Delta = 3,666 ms AFTER the frame. `onAutoSave` (PlatformSaveController.ts:124) is the ONLY automatic caller of `saveVersionInternal` — the other three call sites are the manual modal (:207, :218), the version controller (:309) and the quota-recovery retry (:748), none of which run here. ⛔ Therefore `Snapshot created` (autosave copy) → `captureThumbnail` → `Version saved: \"Auto-save\" (0 elements)` → ServerSyncQueue are ALL post-frame. Any plan that blames them for the 9.2 s is answering the wrong question.",
      "onCriticalPath": false,
      "costMs": "0 ms on the critical path — MEASURED from the two constants in the code (4000 ms settle floor vs the +334 ms frame in his own trace). The sequence's own duration is a post-frame stall, not a startup cost.",
      "costBasis": "read-from-code",
      "quote": "this._settleUntil = Date.now() + 4000;",
      "file": "apps/editor/src/ui/platform/SaveOrchestrator.ts",
      "line": 473,
      "fix": "Nothing to fix for STARTUP — do not spend the lane here. It is worth one cheap change for a different reason: at T+4 s the founder is looking at the globe and typing an address, and this sequence forces a full render at cinematic tier (see the captureThumbnail finding). Skip the capture when the snapshot is bare and that stall disappears.",
      "risk": "none",
      "confidence": "high"
    },
    {
      "title": "⛔ SO WHY DOES 'Version saved (0 elements)' HAPPEN AT ALL? Nothing seeds a baseline — an ordinary autosave simply is not refused when there is no prior version",
      "detail": "There is no post-create baseline save and no 'seed a version' step; the brief's hypothesis (b) is not what is happening. `resetDirtyAfterLoad()` baselines the hash to the empty model, then some mutation after the load moves the hash (the guided step flow's site/level/view writes — SaveOrchestrator's MUTATION_EVENTS list includes `bim-level-added`, `vd:view-created` and the whole document family, SaveOrchestrator.ts:88-115), the debounce arms, the settle deadline passes, and `executeSave` fires an ordinary autosave. The empty-snapshot wipe guard then WAVES IT THROUGH rather than refusing it, because there is no stored version to lose: saveWipeGuard.ts:121-127 returns `{action:'write', code:'no-stored-baseline'}` when `storedElementCount === null`. That is the correct policy — a first-ever save of a project must not be refused — but the consequence is that an empty project's very first autosave writes a 0-element version to IndexedDB and enqueues it for the server. ⚠ It is NOT synchronous with creation and it is NOT a dirty-check baseline: the dirty-check baseline is the in-memory hash from `resetDirtyAfterLoad`, which costs no write at all.",
      "onCriticalPath": false,
      "costMs": "0 ms on the critical path (see the settle-fence finding). The write itself: one IndexedDB put of a bare container + one queued POST — ESTIMATED under 20 ms of main thread, dominated by the forced render inside captureThumbnail.",
      "costBasis": "read-from-code",
      "quote": "reason: 'There is no stored version of this project to lose.',",
      "file": "apps/editor/src/ui/platform/saveWipeGuard.ts",
      "line": 125,
      "fix": "Leave the guard alone — refusing a first save would be the 'refusing half needs its escape hatch' defect. If the 0-element version is unwanted noise in version history, the right seam is `executeSave`: skip the WRITE (not the hash baseline) when `snapshot.elementCount === 0 && countVersions(projectId) === 0`, i.e. nothing has ever existed and nothing exists now. That is a policy change, not a perf fix, and it buys nothing on startup.",
      "risk": "low",
      "confidence": "high"
    },
    {
      "title": "captureThumbnail forces a full synchronous render at cinematic tier, reads it back, and throws the result away — on a project with nothing to show",
      "detail": "PlatformSaveController.ts:357 calls `this.ctx.saveAdapter.captureThumbnail?.()` unconditionally on every autosave that is not plan-blocked. The delegate (initPersistence.ts:175) does `if (rpm) rpm.render();` — a forced, off-frame, full pipeline render, outside the ONE scheduler — then `ctx.drawImage(src, …)` from the WebGPU canvas and `ctx.getImageData(...)` (a readback). His log line `[captureThumbnail] Read a blank/transparent frame` is initPersistence.ts:235, which returns null AFTER all of that has already been paid: the forced render, the drawImage and the readback happen first, and only the WebP encode is skipped. On his run the tier is `cinematic … shadows=high, decorativeShadows=on, probes=on, fullScenePbrTraverse=ON`, so this is not a cheap render. ⚠ I checked the prior audit's 'TWO captures + listener leak' shape against the CREATE path: it does NOT reproduce. The post-load capture (`schedulePostLoadThumbnailCapture`, PlatformSaveController.ts:520) early-returns on `if (detail.empty)` at :537-540 and disposes its own listener, and the new-project emit carries `empty: true` (PlatformShell.ts:311). So the create path has exactly ONE capture — the autosave one — and no leaked listener.",
      "onCriticalPath": false,
      "costMs": "UNKNOWN — one forced full-scene render at cinematic tier plus a canvas readback. ESTIMATED 30–150 ms of main thread at 71 meshes with shadows=high/probes=on (basis: the repo's own SHADOW_REBUILD_COMPLETE elapsed=103.9 ms for a shadow pass on this scene). CHEAPEST PROBE: wrap the `capturedThumb` assignment in performance.now() and log it — 2 lines, no behaviour change.",
      "costBasis": "estimated",
      "quote": "                        if (rpm) rpm.render();",
      "file": "apps/editor/src/engine/initPersistence.ts",
      "line": 175,
      "fix": "Do not capture a thumbnail of a model that has nothing in it. At PlatformSaveController.ts:355, extend the existing `planBlocksSync` short-circuit to `planBlocksSync || snapshot.elementCount === 0`. The existing fallback already does the right thing — `thumbnail: capturedThumb ?? existingMeta?.thumbnail` (:381) keeps the previous tile — and the post-load capture path already declines empty projects for exactly this reason (:537-540), so this makes the two paths agree instead of one of them being blind. This is a post-frame win only; it does not touch the 9.2 s.",
      "risk": "low",
      "confidence": "medium"
    },
    {
      "title": "The ONE save-path item that IS on the critical path: resetDirtyAfterLoad() runs a full serialize + stringify one statement before the gate event",
      "detail": "PlatformShell.ts:309 `this.saveCtrl.orchestrator.resetDirtyAfterLoad();` sits between the empty load resolving and the `pryzm-project-loaded` emit at :311. SaveOrchestrator.ts:589-592 is `const hash = this.getHash(); this.markClean(hash);`, and `getHash` (PlatformSaveController.ts:105-113) is `ctx.saveAdapter.serialize({…})` followed by `ctx.saveAdapter.stringify(snapshot)`. So a complete walk of ~30 sub-stores plus a `computeSnapshotChecksumWithReport` deep walk (ProjectSerializer.ts:2039) plus a JSON.stringify run synchronously, blocking the gate. ⭐ THIS is the `[ProjectSerializer] Snapshot created: 0 elements, 1 levels, …` line the founder sees BEFORE the frame; the identical line ~4 s later is the autosave's own `getHash`. Two lines, two different causes — do not attribute both to the save. ⚠ TWIN CHECK, because this repo has been burned by it: there are TWO ProjectSerializer.ts files. The live one is apps/editor/src/engine/persistence/ProjectSerializer.ts (constructed at initPersistence.ts:41/109); packages/persistence-client/src/loader/ProjectSerializer.ts says of itself at :118-121 'THERE ARE TWO ProjectSerializer.ts FILES AND ONLY ONE IS THE ONE PRODUCTION RUNS … this copy is not on the app's path'. Do not chase the packages copy.",
      "onCriticalPath": true,
      "costMs": "ESTIMATED 5–20 ms on a 0-element / 1-level model. Every collection is empty, so the cost is the fixed ~620 lines of store reads plus one checksum walk plus one stringify over a tiny object. CHEAPEST PROBE: bracket PlatformShell.ts:309 with two markStartupPhase calls, or just performance.now() either side.",
      "costBasis": "estimated",
      "quote": "                    this.saveCtrl.orchestrator.resetDirtyAfterLoad();",
      "file": "apps/editor/src/ui/platform/PlatformShell.ts",
      "line": 309,
      "fix": "Leave it. It is the dirty-check baseline and without it the first mutation after load would look clean. On an empty model it is single-digit-to-low-tens of ms against a 9,233 ms budget — removing it would be a shortcut that buys ~0.2% and risks a data-loss class this repo has already paid for. Report it only so the double 'Snapshot created' line in the log is not misread.",
      "risk": "none",
      "confidence": "medium"
    },
    {
      "title": "ServerSyncQueue is fire-and-forget, and creation's POST is the only server call the frame waits on",
      "detail": "Direct answer to (e). `enqueue` ends at ServerSyncQueue.ts:472 with `this.scheduleFlush(500);`, and `scheduleFlush` (:597-599) is `this.flushTimer = setTimeout(() => this.flush(), delayMs);`. Nothing on the open path holds a reference to that promise, and `saveVersionInternal` returns immediately after enqueueing (PlatformSaveController.ts:410-412). I checked every await on the path from the New Project click to `pryzm-project-loaded`: the ONLY awaited network calls are (1) `client.create` (PlatformRouter.ts:1013) and (2) `tier.streamLoad` — which is explicitly skipped for a new project (buildPersistence.ts:325-327, `hint?.isNewProject ? Promise.resolve({ ok: true, v: null }) : tier.streamLoad(...)`). `controller.refresh()` at buildPersistence.ts:273 does NOT fire either, because `ProjectListController.create` already pushed the summary into the store (:60). So: exactly one server round-trip, and it is the create.",
      "onCriticalPath": false,
      "costMs": "0 ms on the critical path — MEASURED as a structural fact (setTimeout, unreferenced promise, no await anywhere on the open path).",
      "costBasis": "read-from-code",
      "quote": "        this.scheduleFlush(500);",
      "file": "apps/editor/src/ui/platform/ServerSyncQueue.ts",
      "line": 472,
      "fix": "No change needed. Recorded so the parent agent can strike 'the server sync is blocking' off the candidate list with evidence rather than assumption.",
      "risk": "none",
      "confidence": "high"
    },
    {
      "title": "IndexedDB open + migration is NOT paid on the critical path — the version-cache warm is void-ed and the one synchronous read is a Map miss",
      "detail": "Direct answer to (d). PlatformShell.ts:264 is `void warmVersionCache();` — fire-and-forget, and L-13279 (landed today) additionally latches it to the in-flight promise so a hub-then-create sequence cannot start a second full-cursor scan (VersionCacheStore.ts:129-133). The only SYNCHRONOUS storage read on the path is PlatformShell.ts:298 `versionRepository.getLatestVersion(id)`, bracketed by `open:version-read-start`/`-done`: for a brand-new id it resolves through `_rawPayload` (ProjectRepository.ts:1566-1571) = `getVersionsSync(projectId) ?? localStorage.getItem(key)` — one Map miss and one localStorage key read, then `null`, and the code takes the `isNewProject` branch. The IDB `onupgradeneeded` (VersionCacheStore.ts:74-78) creates two object stores and only ever runs once per browser profile, off the awaited path. The legacy localStorage→IDB migration in `warmVersionCache` (ProjectRepository.ts:975-994) is inside the void-ed call. ⚠ HONEST CAVEAT: the warm is concurrent, not free — its cursor callbacks and Map inserts run on the main thread and compete with the boot. The repo's own measurement of that leg at founder corpus scale is `hub:warm-versions-done +93 ms` (ProjectHub.ts:253-256, measured 2026-08-26), so it is a real but small contention term, not a second-scale one.",
      "onCriticalPath": false,
      "costMs": "0 ms awaited. ~93 ms of CONCURRENT main-thread work — repo-measured (ProjectHub.ts:253-256, founder's 2026-08-26 run). CHEAPEST CONFIRMATION: `open:version-read-start → open:version-read-done` is already in his trace; if that delta is ~0 ms the branch is confirmed cold-and-cheap.",
      "costBasis": "repo-artifact",
      "quote": "        void warmVersionCache();",
      "file": "apps/editor/src/ui/platform/PlatformShell.ts",
      "line": 264,
      "fix": "No change needed. Recorded to close (d) with evidence.",
      "risk": "none",
      "confidence": "high"
    },
    {
      "title": "⭐ THE '+ New Project' GESTURE NEVER ARMS THE LATCH THAT TAKES HUB CORPUS-MAINTENANCE OFF THE PATH — one of two gestures got L-11540",
      "detail": "ProjectHub has three chunked deferred passes — the SUSTAIN109 thumbnail fetch (:600-606, chunkSize 1, one network GET per project), the §PERF104 residency audit (:697-701, chunkSize 8, one storage probe per local-only id — 77 on the founder's run) and the §FIX-THUMBNAIL-DURABILITY back-fill (:790-794, chunkSize 1, one network PUT per project). All three stop between chunks on `isOpenInFlight: () => this._openInFlight` or `isDestroyed: () => this._destroyed`. `_openInFlight = true` is written in EXACTLY ONE place — ProjectHub.ts:2274, inside `openProject`, the hub-CARD open — under a comment that calls itself 'THE LATCH THAT TAKES CORPUS MAINTENANCE OFF THE PATH'. The '+ New Project' gesture takes a different exit: it calls `this.callbacks.onStartOnboarding({ name, directEntry: true })` at :2060 and returns. It never sets `_openInFlight`, never emits `hub:open-clicked`, and never reaches `destroy()`. And `PlatformRouter.showOnboarding` does not destroy the hub either — I checked all five `hub?.destroy()` sites (PlatformRouter.ts:484, 613, 622, 936, 1341); 936 is inside `showPricing`, and none of them is on this path. The router only hides `#platform-root` (:803). So `_destroyed` stays false and `_openInFlight` stays false for the entire New-Project boot, and the corpus passes keep taking the main thread and the connection budget from the boot the founder is timing. ⛔ This is the 'same rule, two implementations' shape: the latch exists, is correct, and simply is not wired to this gesture.",
      "onCriticalPath": false,
      "costMs": "UNKNOWN for his specific run — it depends on whether the passes were still in flight when he clicked. Upper bound from the repo's own founder-scale measurements (ProjectHub.ts:253-260, 2026-08-26): version warm 93 ms + syncFromServer ~509 ms ≈ 600 ms of main-thread work, plus per-project network at chunkSize 1 over 47 thumbnail rows and 77 residency ids. CHEAPEST PROBE, needs nothing new: in his existing trace, check whether any `hub:sync-*` mark, or any `[ProjectHub] §SUSTAIN109` / `§PERF104` line, appears AFTER `onboarding:shown +0ms`. If yes, it was contending; if no, this finding is inert for that run.",
      "costBasis": "repo-artifact",
      "quote": "        this._openInFlight = true;",
      "file": "apps/editor/src/ui/platform/ProjectHub.ts",
      "line": 2274,
      "fix": "Arm the same latch on the guided-create gesture. In `handleGuidedCreate` (ProjectHub.ts:~2056), set `this._openInFlight = true;` immediately before `this.callbacks.onStartOnboarding({ name, directEntry: true })`, for exactly the reason L-11540 states at :2269-2273 — the surface is being left, and the corpus does not need auditing on its behalf. Better still, make it structural rather than a second copy: hoist the latch into one private `_leavingForWorkspace()` that both exits call, so a third exit cannot be added without it. ⚠ Do NOT call `destroy()` here: `pryzm-go-hub` re-shows `#platform-root` and expects the hub DOM to still exist. The latch is pause-and-resume-on-next-mount by design and both passes are documented idempotent.",
      "risk": "low",
      "confidence": "medium"
    },
    {
      "title": "The '[main/deferred]' tail IS genuinely after the first frame — deferred to requestIdleCallback, not merely later in the same task",
      "detail": "The brief asked me to establish this rather than trust the word 'deferred'. Verified: the whole block is behind `requestIdleCallback` scheduled from the tail of `initUI` (initUI.ts:825-830) — `const _scheduleDocSubsystems = () => ensureDeferredDocSubsystems(); … ric(_scheduleDocSubsystems, { timeout: 4000 }); else setTimeout(_scheduleDocSubsystems, 1500);`. rIC callbacks run as their own task after paint, so they cannot execute inside the boot task that schedules them. There is a SECOND arming on `pryzm-project-loaded` (initUI.ts:832-837) at `{ timeout: 3000 }` / `setTimeout(800)` — that one fires after the gate event, and since the first interactive frame is only +334 ms later it would have to find the main thread idle inside that 334 ms to beat the frame, which is exactly the window in which the scheduler is producing that frame. `runDeferredDocSubsystems` is idempotent (`_docSubsystemsRan`, :472-473) so the double-arming cannot double-pay. VERDICT: off the critical path. ⚠ FALSIFIER, stated so it can be checked in one grep: if any `[main/deferred]` line appears BEFORE `open:first-interactive-frame` in his console, this verdict is wrong and the rIC is firing in the gap.",
      "onCriticalPath": false,
      "costMs": "0 ms on the critical path — read-from-code (rIC callback cannot run inside the task that schedules it). The block's own cost is unmeasured and irrelevant to the 9.2 s.",
      "costBasis": "read-from-code",
      "quote": "        if (typeof ric === 'function') ric(_scheduleDocSubsystems, { timeout: 4000 });",
      "file": "apps/editor/src/engine/initUI.ts",
      "line": 827,
      "fix": "No change. Recorded so the long deferred tail in his log is not re-reported as a win.",
      "risk": "none",
      "confidence": "high"
    },
    {
      "title": "FRAMING FACT the whole plan depends on: t0 of the §STARTUP-BUDGET run is the click itself — there is zero human dwell inside the 9,233 ms",
      "detail": "This is not in my dimension but it is load-bearing for whoever sums the lanes, and this repo's own instrument doc (startupBudget.ts:96-118, the `hub:open-clicked` note) warns that a leg containing human dwell 'is not a perf number'. Here it does not: `ProjectHub` line 2060 fires `onStartOnboarding({ name, directEntry: true })`, the router's callback (PlatformRouter.ts:637-638) calls `this.showOnboarding(seedFromModal)`, and `showOnboarding` runs `beginStartupBudget()` + `markStartupPhase('onboarding:shown')` at :725-726 and then — for an authed user with a seeded typology, which `directEntry:true` guarantees via `resolveSeededTypologyId` (resolveSeededTypologyId.ts:68 returns 'apartment' on directEntry) — takes the RAC-skip branch at :802 and emits `pryzm:onboarding-brief-ready` at :814, ALL IN THE SAME SYNCHRONOUS TASK. `briefBootstrap.onBriefReady` then calls `deps.createAndOpenProject(projectName)` (briefBootstrap.ts:314) with only synchronous work in between. So `onboarding:shown +0ms (t+0ms)` IS the click, and `open:first-interactive-frame (t+9233ms)` is 9.233 s of uninterrupted machine work. ⭐ CONSEQUENCE: `open:router-launch`'s t+ in his log is the create POST's wall clock, essentially neat. That single number, which he has already captured, decides how much of finding #1 is worth.",
      "onCriticalPath": true,
      "costMs": "0 ms — this is a framing measurement, not a cost. It establishes that all 9,233 ms is machine work and that no lane may attribute any of it to the founder thinking.",
      "costBasis": "read-from-code",
      "quote": "                    this.callbacks.onStartOnboarding({ name, directEntry: true });",
      "file": "apps/editor/src/ui/platform/ProjectHub.ts",
      "line": 2060,
      "fix": "No fix. Use it: ask the founder for the first fifteen lines of his §STARTUP-BUDGET table. `onboarding:shown` → `globe:prewarm-start` → `open:router-launch` → `open:persistence-openProject` → `boot:ensure-requested` → `boot:heavy-wiring-done` → `boot:engine-start` splits the first ~2 s of the 9.2 s into named legs with no new code at all.",
      "risk": "none",
      "confidence": "high"
    }
  ],
  "whatIcouldNotEstablish": "1. ⭐ THE ACTUAL WALL CLOCK OF THE CREATE POST. This is the one number my dimension needs and I do not have it. It is NOT missing from the instrument — it is `open:router-launch`'s `t+` minus `globe:prewarm-start`'s `t+` in the founder's own trace, and the brief quoted only one line of that trace. Ask for the table. If it is ~150 ms, finding #1 is worth ~1.5% and should be fixed for correctness of structure, not for speed. If it is >1,000 ms, a migrations-503 was ridden out silently and that is a different bug (ProjectListClient.ts:615-617 waits up to 60 s with no user-visible signal).\n\n2. WHETHER THE HUB'S DEFERRED PASSES WERE ACTUALLY IN FLIGHT ON HIS RUN. The wiring gap (finding #8) is certain from the code; whether it cost him anything on this particular click depends on how long the hub had been open. His log answers it: any `[ProjectHub] §SUSTAIN109` / `§PERF104` / `hub:sync-*` line appearing after `onboarding:shown` confirms contention.\n\n3. THE COST OF THE EMPTY SERIALIZE (finding #5) is estimated, not measured. I did not run anything — the brief said measure and design only, and there is no bench for a 0-element snapshot. The probe is two `performance.now()` calls around PlatformShell.ts:309.\n\n4. THE COST OF THE FORCED `rpm.render()` INSIDE captureThumbnail. I anchored the estimate to the repo's own `SHADOW_REBUILD_COMPLETE elapsed=103.9ms` on the same scene, which is an anchor and not a measurement of this call. It is post-frame either way, so it does not change the 9.2 s arithmetic.\n\n5. ⛔ MY DIMENSION DOES NOT ADD UP TO 9,233 ms AND SHOULD NOT BE MADE TO. Honest total on the critical path: the create POST (UNKNOWN, est. 100–350 ms) + the baseline serialize (est. 5–20 ms) + up to ~600 ms of hub contention that may or may not have applied. Call it 0.1–1.0 s of 9.2 s. The remaining ~8.2–9.1 s is NOT in this dimension — it is the engine boot behind `startEngine()` and the globe. Any plan that closes on my findings alone has not found the founder's nine seconds.\n\n6. I did not verify anything by running the app; every claim above is read from source at HEAD d1771f50, with file:line and the line quoted. I edited no files."
}