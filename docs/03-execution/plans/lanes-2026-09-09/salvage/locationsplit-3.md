{
  "dimension": "Work done more than once, or done and thrown away, in one load (Barcelona geocode → site-authoring split)",
  "summary": "FIRST, THE RANKING RULE, ANSWERED. The reveal gate is `awaitContentReady` = the near-buildings warm alone, deadlined at `REVEAL_GATE_DEADLINE_MS = 1_500` (siteRevealSequence.ts:66). The founder's own marks prove the deadline fired: `activation:site:start +1568ms → descent-settled +75ms → reveal:split-mounted +0ms` at t+113012, with `context-warm:done` only arriving at t+118446 and `reveal:content-ready +128ms` after it. So `content-ready` is a LATE MARK, not a gate — nothing in this dimension blocks the mount. ⛔ But what mounts at t+113012 is an EMPTY split. From t+113012 to t+118574 the founder is looking at the screenshot's two panes filling from the layer stream. THE LAYER STREAM IS THE PERCEIVED CRITICAL PATH even though it is off the code gate, and I rank against that, saying so per finding.\n\nTHE HEADLINE, and it is (b). Water is read at THREE nested extents (25 tiles z16 / 81 tiles z16 / 49 tiles z13 — arithmetic reproduced below, matching his log exactly). The 11 km one costs 4,586 ms, decodes 1,483 areas + 1,576 waterways, and its ONLY consumer reads ONE field off it: `collection.sea` (CesiumViewport.ts:14330 — I grepped every `collection.` reference in `loadContextSea` and there are no others). On this run that field was `[]` — the walk refused — and the sea was then drawn by a live Overpass round-trip that needs none of it. ~3,059 features built and discarded for a field that is structurally always empty on the current bake.\n\nSECOND: water is the ONE layer the warm/render extent fix skipped. Twice (L-13079 roads+parks, L-13161 rail+trees) this defect has been closed one layer at a time; `contextWarmExtentAgreement.spec.ts` pins roads, parks, rail, trees and never mentions water. The warm asks 0.008° (contextLayerWarm.ts:116); the render asks `groundFetchHalfDeg(scope)` = 0.016° (CesiumViewport.ts:14107). 25 of 81 tiles warmed — 31%, the identical figure the L-13079 comment records for parks — on the slowest layer in the log.\n\nTHIRD, and it is why this keeps happening: §CTX-FETCH-BBOX-LATTICE (L-12953) exists precisely to stop four callers holding four different points from paying four reads. It was applied to BUILDINGS ONLY. Seven of eight fetchers still call `contextBboxAround(lat, lon, halfDeg)` on the raw caller point.\n\n⛔ THREE THINGS THE BRIEF SUSPECTED THAT I AM REPORTING AS NEGATIVE, because saying so is worth more than a padded list: (a) the sea mask ×3 is three DIFFERENT bboxes, pure CPU, single-digit ms against a 296 ms measured ceiling for a corpus 30× larger — that is NOT where the time is; (d) the NaN origin drives no read, only the slider caption, and its read-driving twin returns the same number either way; (e) the idempotent guard works — but it is an EXACT-float guard, so the class of duplicate it cannot catch is exactly the lattice drift in finding 3.\n\n⚠ Also: 81 tiles per layer × 6 layers at z16 + 79 at z13 ≈ 565 distinct tiles against `CTX_MAX_CACHED_TILES = 768`, whose own comment estimates \"~330\". No eviction at the default scope; a cliff one slider notch out.",
  "findings": [
    {
      "title": "The 11 km sea-extent water read decodes ~3,059 features and only one field is ever read — and that field is structurally always empty on the current bake",
      "detail": "`loadContextSea` reads water at CONTEXT_SEA_HALF_DEG (0.10°, ~11 km). I grepped EVERY `collection.` reference in its body (CesiumViewport.ts:14281–14470): seven hits, all `collection.sea`. `collection.areas` (1,483) and `collection.ways` (1,576) are built, cropped and thrown away — the inland water the user sees comes from the SEPARATE 0.016° read (his log: 257 areas + 70 waterways).\n\nNow the field that IS read. `.sea` comes from `waterFromTileFeatures` (contextWater.ts:231) by one of two routes: baked `sea` POLYGONS, else the coastline walk. Route 1 is dead — `contextTilesetManifest.ts`'s header records the live manifest as `layers = [buildings, landuse, parks, rail, roads, trees, water]`, sea is not among them, so `seaPolygons` is null at every bbox. Route 2 cannot succeed on this bake — the baked coastline arrives as FRAGMENTS with free ends strictly inside the bbox (asserted in `contextSeaBakedPolygons.test.ts:5`), which `buildSeaMask` refuses BY NAME as `incomplete-coastline` before it walks anything. That is precisely his three REFUSED lines at 16 / 105 / 66 ends.\n\nSo the read completes, `.sea` is `[]`, `seaCoverage` is 0, the `< 0.02` gate at CesiumViewport.ts:14330–14332 fires, and `fetchSeaMaskViaOverpass` does the real work over the SAME bbox — a path that reads no water tiles at all.\n\n⚠ WHAT I AM NOT CLAIMING: that the read is wrong in principle. On a city with a published `sea` layer it is exactly right. It is wrong TODAY, everywhere, because neither of its two sources can produce a value.",
      "blocksReveal": false,
      "costMs": "4,586 ms of read + 49 z13 tile downloads. MEASURED (founder log: \"1,483 areas + 1,576 waterways … 4,586 ms\"). Off the reveal gate; ON the perceived path — it is one of the ten reads sharing the origin's pool while the split sits empty from t+113012 to t+118574.",
      "costBasis": "founder-log",
      "quote": "CesiumViewport.ts:14298  `try { collection = await fetchContextWater(lat, lon, signal, CONTEXT_SEA_HALF_DEG); }`  · CesiumViewport.ts:14330  `const seaCoverage = seaFractionOfBbox(collection.sea.map((a) => a.ring), lat, lon);`",
      "file": "apps/editor/src/ui/geospatial/CesiumViewport.ts",
      "line": 14298,
      "fix": "GATE IT ON THE MANIFEST, do not delete it. `primeContextTilesetManifest()` already establishes the published layer set before any layer read (contextTiles.ts:1071, session singleton, 428 ms in his log). In `loadContextSea`: if the manifest is `ok` and does NOT name `sea`, skip the 0.10° `fetchContextWater` entirely and go straight to `fetchSeaMaskViaOverpass` — which is the path that is already drawing his sea. Drop `['water(sea)', …]` from contextLayerWarm.ts:118 under the same condition. When the sea layer IS published the read returns byte-for-byte as today. ⚠ Keep `contextSeaRingsLonLat` fed from `supplementalSea` — the §FORMA-CTX-LANDUSE-SEA-CLIP consumer is unaffected either way. Ship a one-line probe first (§CONTEXT-DATA-HONESTY): print `seaProvenance` + the manifest verdict beside the refusal, so \"the walk cannot ever succeed here\" is a reading and not my argument.",
      "risk": "low",
      "confidence": "high"
    },
    {
      "title": "§CTX-WARM-READS-THE-RENDER-EXTENT, THIRD RECURRENCE — and it is on water, the slowest layer. The agreement spec pins four layers and omits this one",
      "detail": "The warm asks for water at the NEAR default; the 3D Site reads it at the GROUND extent. Same defect L-13079 fixed for roads+parks and L-13161 fixed for rail+trees, still open on the fifth layer.\n\nTHE ARITHMETIC, reproduced rather than quoted (Barcelona 41.3874, 2.1686, the repo's own `contextBboxAround` + `tileCountCovering` + `zoomForExtent`):\n  near   0.008° → 25 tiles, z16   ← what contextLayerWarm.ts:116 warms\n  ground 0.016° → 81 tiles, z16   ← what CesiumViewport.ts:14107 reads\n  wide   0.072° → 25 tiles, z13\n  sea    0.100° → 49 tiles, z13\nThose 25/81 are the SAME numbers `contextWarmExtentAgreement.spec.ts:66-68` asserts from pure arithmetic for parks, and the same 81 his log reports for roads/parks/buildings/water/rail/trees. 25 of 81 = 31% — the exact coverage figure the L-13079 comment records as the defect.\n\n⚠ 25 ⊂ 81 at the SAME zoom, so the per-tile decode cache means this is not duplicate BYTES. What it costs is the START TIME: water's 81-tile read begins when the 3D-Site pane mounts, not when the warm fires.\n\n⛔ CHECK THE CALLERS BEFORE PATCHING: line 116 is not dead — `pastelContextGeoJson.ts:244` (the LEFT pane of his screenshot) is its real consumer at 0.008°. Moving line 116 to `groundHalfDeg` without moving the pastel read too just relocates the miss. That is finding 4.",
      "blocksReveal": false,
      "costMs": "≥1,653 ms of head start unused. MEASURED from his marks: context-warm:start t+111359 → reveal:split-mounted t+113012. The read itself is 5,743 ms (MEASURED). The upper bound on the win is larger than 1,653 ms because the warm wave shares the pool, but 1,653 ms is the floor I can defend.",
      "costBasis": "founder-log",
      "quote": "contextLayerWarm.ts:116  `['water', fetchContextWater(lat, lon)],`   vs   CesiumViewport.ts:14107  `try { collection = await fetchContextWater(lat, lon, signal, groundFetchHalfDeg(this.contextScope)); }`",
      "file": "apps/editor/src/ui/geospatial/contextLayerWarm.ts",
      "line": 116,
      "fix": "Add `['water(ground)', fetchContextWater(lat, lon, undefined, groundHalfDeg)]` to the warm list — as an ADDITION, not a replacement, until the pastel pane moves (finding 4), then collapse to one. ⭐ AND ADD WATER TO `contextWarmExtentAgreement.spec.ts`: the reason this recurred three times is that the spec was written per-layer, by hand, for the layers that had already broken. The durable fix is an arm that enumerates the warm list against the render call sites and FAILS on any layer present in one and absent from the other — the same shape as `check-contract-index-equivalence.ts`: compare SETS, never a count.",
      "risk": "none",
      "confidence": "high"
    },
    {
      "title": "§CTX-FETCH-BBOX-LATTICE was applied to buildings and stopped — seven of eight layers still key on the raw caller point, which is the mechanism that makes every re-centre a full re-read",
      "detail": "L-12953's own header names the defect exactly: *\"on the startup path four different callers hold four different points for one neighbourhood — the geocode result, the site origin, the camera ground point, and the 2D map CENTRE, which moves on every `moveend`\"*, and *\"`bboxKey` then rounds the already-computed corners to 4 dp (≈11 m), which does not SNAP anything: a centre 12 m away is a different key.\"* Its cure, `contextFetchBbox` (contextBuildings.ts:678), snaps the centre to a 0.001° lattice and GROWS the half-extent by 0.55×snap so the result is a strict superset — no footprint lost.\n\nI grepped all nine fetchers. `contextFetchBbox` has exactly two call sites, both in contextBuildings.ts (:2021, :2051). roads:164, water:736, parks:185, landuse:168, rail:104, trees:86, furniture:140 and canopy:266 all still call `contextBboxAround(lat, lon, halfDeg)` on whatever point the caller happened to hold.\n\nThe three live points on this load are demonstrable: the warm uses the geocode result (`warmContextCache: (lat, lon)`, OnboardingStepController.ts:843, fed from `best.lat/best.lon`); the 2D pastel pane uses `map.getCenter()` (SiteBoundaryMap2D.ts:2957); the 3D loaders use `resolveScopeAnchor(...)` / `formaMassingOrigin`.\n\n⚠ HONEST LIMIT ON THE COST FOR *THIS* LOAD. At mount, `getMapInitial` returns `resolveSiteFramingExtent(...)`, which REJECTS an administrative geocode bbox and falls to a ±250 m box about the anchor, and `fitBounds` on a symmetric box centres on that anchor — so the map centre and the geocode point coincide to within a metre or two here, and the 4 dp key may or may not have collided. I cannot claim it fired at mount. It certainly fires on every `moveend` (the map re-reads at the new centre) and on `frameCommittedParcel` after a parcel is picked, which is the very next thing the user does on this screen.",
      "blocksReveal": false,
      "costMs": "UNKNOWN for this load — I could not establish from the excerpts whether the mount-time keys collided. The class cost is MEASURED in L-12953's own citation of the founder's Córdoba console: five buildings reads at 8871 / 8799 / 8804 / 8817 / 8919 footprints, ~36–42 tiles each — i.e. ~4 full reads of ~8 s wasted, once the user starts panning.",
      "costBasis": "read-from-code",
      "quote": "contextWater.ts:736  `const bbox = contextBboxAround(lat, lon, halfDeg);`  — identical line at contextRoads.ts:164, contextParks.ts:185, contextLanduse.ts:168, contextRail.ts:104, contextTrees.ts:86, contextFurniture.ts:140",
      "file": "apps/editor/src/ui/geospatial/contextWater.ts",
      "line": 736,
      "fix": "One line per file: `contextBboxAround(lat, lon, halfDeg)` → `contextFetchBbox(lat, lon, halfDeg)` in the seven fetchers. `contextFetchBboxLattice.spec.ts` already pins the superset property numerically across latitudes and across offsets inside one cell, so the safety argument is already tested — it just is not applied. ⛔ Do NOT also change the near/far CLASSIFICATION: `contextFetchBbox`'s own header says FETCH ONLY, and buildings deliberately keeps `contextBboxAround` at the true centre for the near split (contextBuildings.ts:2056). Cost is ~4.5% more extent, already accepted for buildings.",
      "risk": "low",
      "confidence": "high"
    },
    {
      "title": "The 2D pastel pane — the LEFT half of the screenshot — reads five of its seven layers at an extent nothing warms, and its own comment names the trap it only fixed for one layer",
      "detail": "`loadPastelContextGeoJson` fires six reads. Land-use carries a comment explaining, correctly and at length, that asking for the default near bbox would miss the warm's key and start a second read — and land-use is the only one that was moved. roads, water, parks, rail and trees are all still on the default 0.008°.\n\nAgainst the warm (contextLayerWarm.ts:114-131): roads/parks/rail at `groundHalfDeg` 0.016°, trees at `treesHalfDeg` (= 0.016° here, since `treesRadiusM` = min(scope 1781, ceiling 7071, measured 5225) = 1781 m), water at 0.008°. So four of the five miss the warm's key outright, and the fifth (water) matches only because of the defect in finding 2 — fixing that naively would break this one.\n\n⚠ WHAT THIS IS AND IS NOT. 25 ⊂ 81 at the same z16, and `loadTile` (contextTiles.ts:1881-1888) has both a per-tile decode cache and a per-tile in-flight map that shares the FIRST caller's promise. So this is NOT duplicate bytes. It is a second bbox-keyed crop + feature build per layer, and — more to the point — the LEFT PANE's fill is gated on a collection key that the warm never populates, so it cannot short-circuit no matter how early the warm ran.\n\n⛔ I checked the founder's log for these lines and they are not in the excerpt, so I am not attaching a millisecond to them.",
      "blocksReveal": false,
      "costMs": "ESTIMATED. Five extra crop+build passes over 25 tiles each; no extra downloads. The visible effect is that the pane in the screenshot cannot be served from the warm. Measure it by logging §CTX-READ-PROVENANCE for these five reads — contextTiles.ts:1801-1812 already prints \"this read PAID FOR NOTHING\" when downloads are 0, which is exactly the reading needed to size this.",
      "costBasis": "estimated",
      "quote": "pastelContextGeoJson.ts:243-248  `fetchContextRoads(lat, lon, signal),` / `fetchContextWater(lat, lon, signal),` / `fetchContextParks(lat, lon, signal),` … `fetchContextRail(lat, lon, signal),` / `fetchContextTrees(lat, lon, signal),`",
      "file": "apps/editor/src/ui/geospatial/pastelContextGeoJson.ts",
      "line": 244,
      "fix": "Move all five onto the same extents the warm uses — `groundFetchHalfDeg()` for roads/water/parks/rail and `treesFetchHalfDeg(undefined, scopeReadCompleteCeilingM(lat, lon).radiusM)` for trees — exactly as land-use was already moved, and for the reason its own comment gives. ⚠ The pane then draws from a 2× wider collection; it already clips to the visible map, and the wide box is a strict superset, so nothing is lost. Do this in the SAME commit as finding 2 or the two fixes fight each other over water's key.",
      "risk": "low",
      "confidence": "high"
    },
    {
      "title": "Five of the eight ground layers do not await the terrain attach the other three do — so their whole first placement is a flat scalar that a re-seat then overwrites",
      "detail": "This is (c), and the answer is narrower than the brief suspected — the expensive half is already clean, and I want to say which half.\n\nALREADY CLEAN: `reseatContextGroundFeaturesForBase` (CesiumViewport.ts:10134) does no re-fetch and no re-clip; pass 2 samples only `unsampledContextGroundSeatPoints()`, i.e. points the cache does NOT already hold, so nothing is re-sampled. A conforming (`perPositionHeight`) piece is skipped by design. `ensureGroundBaseForContext` coalesces concurrent callers onto one promise (11838-11846) and all five `sampleTerrainMostDetailed` call sites now go through `samplingTerrainProvider`, so the four same-centroid probes the L-13077 comment names share one decoded tile. None of that is redone.\n\nWHAT IS REDONE: `resolveGroundDrapePieces` branches on `const relief = this.groundReliefAttached();` at :10777 and, when false, returns ONE flat scalar for the entire layer — no seat points, no probes, no split, no conform. `grep -n \"ensureGroundBaseForContext(\"` returns exactly three call sites: :12028 (buildings), :15043 (trees), :15216 (street life). roads, water, parks, landuse, rail and sea never await it; they race it. On this run they won the race (his §DRAPE-COST-ATTRIBUTION lines print for parks/landuse/rail, which only the relief branch emits), because each layer's 4–15 s tile read is far longer than the attach's `layer.json` fetch. But nothing enforces that ordering — it is bought by the slowness of the thing it depends on.\n\nSeparately, the massing itself DOES place at flat 0 and re-place: his \"terrain clamp degraded (no real terrain provider attached — clamping to flat base 0)\" then \"terrain clamp: base height 63.41 m — re-placing\" is `clampTerrainThenReplace` calling `renderFormaMassing({...input, frameCentroid:false, _skipTerrainClamp:true})` at :9973 — a second full massing render, plus `reframeAfterBaseSettle`, plus four re-seat passes.",
      "blocksReveal": false,
      "costMs": "UNKNOWN — no log line prices the second `renderFormaMassing`. The drape-side cost is 0 on this run (the race was won). The brief's headline 8,037 ms is FIFO queuing, not redone work: `§DRAPE-COST-ATTRIBUTION: 8,037 ms waiting for terrain (shared FIFO), 23 ms own work` — and see \"what I could not establish\" on whether L-13263 has already removed it.",
      "costBasis": "read-from-code",
      "quote": "CesiumViewport.ts:10777  `const relief = this.groundReliefAttached();`  — and the three awaiters, `grep -n \"ensureGroundBaseForContext(\"` → :12028, :15043, :15216 only",
      "file": "apps/editor/src/ui/geospatial/CesiumViewport.ts",
      "line": 10777,
      "fix": "Answering the brief's question directly — YES, it can attach before the first placement, and the mechanism already exists and is already awaited by three loaders. Call `await this.ensureGroundBaseForContext(originLat, originLon)` once before the first `renderFormaMassing` placement in the site-activation path, and have the other five ground loaders await the same coalesced promise. It costs ONE `layer.json` fetch plus ONE memoised centroid sample — both of which are paid a few hundred ms later on today's path anyway, and both of which overlap the 4–15 s tile reads. It removes the flat-0 placement, the second `renderFormaMassing`, and the `reframeAfterBaseSettle` one-shot. ⚠ Keep the re-seat: it is still the correct answer for a layer that arrives after the settle.",
      "risk": "medium",
      "confidence": "medium"
    },
    {
      "title": "~565 distinct tiles are read for one default-scope site against a 768-entry FIFO tile cache whose own comment budgets for 330",
      "detail": "`CTX_MAX_CACHED_TILES = 768` is a single global `Map` with FIFO eviction — `tileCache.keys().next()` (contextTiles.ts:1320-1322), so the OLDEST INSERTED goes first, which is the buildings/roads tiles a re-seat or the pastel near-read wants back, not the least recently used.\n\nCounting this load from the arithmetic above: six layers × 81 tiles at z16 (roads, parks, rail, trees, water, buildings) = 486, plus landuse 25–30 and water(sea) 49 at z13 ≈ 565 distinct entries. The constant's own justification says *\"ONE site now needs ~330 distinct tiles across all ten layers, so 512 leaves less than one further site before eviction\"* — the estimate is 1.7× low, and the \"two-site headroom\" it claims to restore is really ~1.35 sites.\n\n⛔ NOT A COST ON THIS LOAD: 565 < 768, so nothing evicted. It is a cliff, and the slider is the thing that walks off it — `CTX_SCOPE_MAX_RADIUS_M = 7071` against a default of 1781, and `groundFetchHalfDeg` is linear in that radius, so the tile count is quadratic in it.",
      "blocksReveal": false,
      "costMs": "0 ms on this load (MEASURED by arithmetic: 565 of 768, no eviction). At a widened scope, a full re-download of whatever was evicted — the same 5,743 ms class of cost as one water read.",
      "costBasis": "read-from-code",
      "quote": "contextExtentBudget.ts:1085  `export const CTX_MAX_CACHED_TILES = 768;`  — and its comment: \"ONE site now needs ~330 distinct tiles across all ten layers\"",
      "file": "apps/editor/src/ui/geospatial/contextExtentBudget.ts",
      "line": 1085,
      "fix": "Do not just raise the number — that is how it got to be wrong twice. Make the eviction LRU rather than FIFO (touch on read in `loadTile`, contextTiles.ts:1882), and print the true per-site distinct-tile count once per load beside the existing §CTX-READ-PROVENANCE line so the constant is chosen against a reading instead of an estimate. Then size it from that reading at the MAXIMUM scope, not the default.",
      "risk": "low",
      "confidence": "high"
    },
    {
      "title": "NEGATIVE FINDING — (a) the sea mask ×3 is three different questions, not one asked three times, and it is not where the time is",
      "detail": "The brief asks whether the three §SEA-LEFT-HAND-WALK refusals are one question repeated. They are not. `buildSeaMaskFromCoastline` runs once per water-bbox read, inside `waterFromTileFeatures` (contextWater.ts:231), so three water bboxes give three walks over three genuinely different coastline sets — which is why his three lines report 16, 105 and 66 free ends rather than the same number three times. A fourth call, `fetchSeaMaskViaOverpass` (CesiumViewport.ts:14512), is a different source entirely and is memoised in `FORMA_SEA_MASK_CACHE` keyed by bbox, so it runs once.\n\nAND THE COST IS NEGLIGIBLE, which is the part worth stating. The walk is pure: stitch, clip to bbox, sort perimeter params, walk. It refuses BEFORE the walk when any free end is interior — the `incomplete-coastline` arm returns at contextWater.ts:611-621, so for all three of his bboxes only the stitch+clip ran. The repo's own calibration for pure geometry of this class is §DRAPE-COST-ATTRIBUTION's measurement: the ENTIRE decomposition of 43,551 pieces from 3,442 features costs 296 ms. A ~100-way coastline stitch is two orders of magnitude smaller than that.\n\n⛔ So: chasing the triple walk would buy single-digit milliseconds. The finding that MATTERS about those three refusals is finding 1 — that all three refuse, always, structurally, which makes the 4,586 ms read that hosts one of them pointless.",
      "blocksReveal": false,
      "costMs": "Single-digit to low-tens of ms for all three combined. ESTIMATED, calibrated against the repo's own measured 296 ms for a pure-geometry corpus ~30× larger (CesiumViewport.ts:10788-10797).",
      "costBasis": "estimated",
      "quote": "contextWater.ts:231  `const sea = buildSeaMaskFromCoastline(coastlines, bbox).map(`  — reached once per water bbox, from `waterFromTileFeatures`",
      "file": "apps/editor/src/ui/geospatial/contextWater.ts",
      "line": 231,
      "fix": "No fix. Do not spend a lane here. Fix finding 1 and two of the three walks disappear as a side effect.",
      "risk": "none",
      "confidence": "high"
    },
    {
      "title": "NEGATIVE FINDING — (d) the NaN site origin costs nothing and recomputes nothing; it is an honesty defect one property away from being true",
      "detail": "The brief suspects a scope computed from a 1781 m fallback and then recomputed once the origin lands. It is not.\n\nThe NaN comes from `getCompleteScopeMark()` (CesiumViewport.ts:2612), which is a DISPLAY getter for the slider's \"complete at this scope\" caption — its own comment says \"This getter is called on every slider repaint\". `scopeReadCompleteCeilingM` on a non-finite origin returns the reference constant in one branch (scopeReadCeiling.ts:145-157); on a finite one it bisects ~13 times over `tileCountCovering`, which is pure arithmetic with no allocation. Microseconds either way. It drives no fetch.\n\nThe read-driving twin is `treeReadCeilingM()` (:2639), and it returns `null` in the same window — but that changes nothing: `treesRadiusM(scope, null)` = min(scopeOuter 1781, CTX_TREES_RADIUS_CEILING_M 7071, Infinity) = 1781, and with the measured ceiling (5,225 m at Barcelona) it is min(1781, 7071, 5225) = 1781. Identical. Same `treesFetchHalfDeg`, same bbox key, no second read.\n\n⚠ IT IS STILL WORTH FIXING, for the reason the brief smelled. `at = this.contextBuildingsAt ?? this.formaMassingOrigin` is NaN on a path that has just resolved 41.38, 2.17 — the origin exists, it is simply not in either of the two properties this getter consults. The caption then prints \"NOT MEASURED … falling back to the mid-latitude reference 1781 m. That is an admission, not a measurement: at high latitude the true ceiling is tighter\" — over a site whose true ceiling IS measurable. At Reykjavík that caption over-claims by ~2.2×.",
      "blocksReveal": false,
      "costMs": "~0 ms. READ from the code: one branch return, or a 13-iteration bisection over pure integer arithmetic. No I/O, no recompute of any read.",
      "costBasis": "read-from-code",
      "quote": "CesiumViewport.ts:2612  `const ceiling = scopeReadCompleteCeilingM(at?.lat ?? Number.NaN, at?.lon ?? Number.NaN);`",
      "file": "apps/editor/src/ui/geospatial/CesiumViewport.ts",
      "line": 2612,
      "fix": "Give the getter the third fallback it is missing — the site location the runtime already holds — so the caption is measured at the site instead of admitting it is not. Correctness/honesty only; claim no latency win from it.",
      "risk": "none",
      "confidence": "high"
    },
    {
      "title": "NEGATIVE FINDING — (e) the idempotent guard fired and worked; the class of duplicate it CANNOT catch is finding 3",
      "detail": "`§STARTUP-LOCATION-IDEMPOTENT — site location unchanged … no re-emit, no re-fly` means `dispatchSiteLocation` was called twice for the same point inside 30 s and the second was suppressed. The guard's own comment enumerates the legitimate duplicate callers: \"the §22 reveal's anchor step, `startDrawThenGenerate`'s re-anchor, the draw/select commit's ensure-location\". There is a SECOND guard downstream at CesiumViewport.ts:5035-5057 (ε 1e-7°) on the subscriber, so even an emit that got through would not re-fly or force a context re-render. Both worked. This is a saving, not a cost.\n\n⭐ THE PART THAT MATTERS FOR THIS DIMENSION. Both guards test EXACT float equality — deliberately, and the first says so: \"EXACT equality (the duplicates are the same floats passed twice)\". So they catch a pipeline ECHO and are structurally blind to the same neighbourhood arriving as three slightly different points, which is exactly the geocode-point / map-centre / scope-anchor drift that §CTX-FETCH-BBOX-LATTICE was minted to absorb and that finding 3 shows is still unabsorbed on seven of eight layers. The guard is not catching \"only this one duplicate\" — it is catching the only KIND of duplicate it can see.",
      "blocksReveal": false,
      "costMs": "0 ms — it removed cost. The comment prices what it removed: \"a redundant camera fly plus a FORCED context re-render of thousands of entities\", and the subscriber's guard adds \"a 6,000-point terrain batch re-sample\". MEASURED only as a prior founder observation (\"site.location-changed → flying camera ×2 with identical coordinates\"), not on this run.",
      "costBasis": "read-from-code",
      "quote": "siteDispatch.ts:1506  `'[gis] §STARTUP-LOCATION-IDEMPOTENT — site location unchanged ' +`  · and its rationale at :1489-1497, \"EXACT equality (the duplicates are the same floats passed twice)\"",
      "file": "apps/editor/src/ui/site/siteDispatch.ts",
      "line": 1480,
      "fix": "Leave both guards exactly as they are — widening either to a tolerance would suppress real moves. The drift class belongs one layer down, in `contextFetchBbox` (finding 3), where the snap is a strict SUPERSET and therefore cannot lose a feature. That is the correct place for a tolerance and the guards are the wrong one.",
      "risk": "none",
      "confidence": "high"
    },
    {
      "title": "The one counter that would say which terrain-FIFO regime a run used is computed, exposed, and never printed",
      "detail": "L-13263 landed today and made the FIFO conditional: `tileDedupGuaranteed: () => this.terrainTileMemoEntry !== null` (CesiumViewport.ts:10593), probed per flush at groundSampleBatcher.ts:404-410, incrementing `_flightsRunConcurrently` when the FIFO is skipped. The batcher exposes it in `stats` (:281-282) with the doc \"0 ⇒ this run serialised, as before\".\n\nThe `onFlush` console line in CesiumViewport.ts:10634-10652 prints `deferredFlushes` and `maxConcurrentFlights` and NOT `flightsRunConcurrently`. So the log still ends with the hard-coded assertion `max concurrent flights 1 (MUST be 1 — two calls in the air re-download the same tiles)` — a sentence L-13263 made false as a requirement, printed by the code L-13263 changed.\n\n⚠ THIS IS WHY I CANNOT SETTLE THE BRIEF'S 8,037 ms. `maxConcurrentFlights` can legitimately read 1 in the NEW regime too, whenever two flushes simply do not overlap in time — so the line cannot distinguish \"the FIFO held\" from \"the FIFO was skipped and nothing collided\". This is the same shape as the §RAF-GATE-COMMENT-BLIND lesson in CLAUDE.md: a counter that cannot tell two states apart reads as whichever one you already believed.",
      "blocksReveal": false,
      "costMs": "0 ms of runtime cost. The cost is diagnostic: it is what stops anyone from confirming whether today's L-13263 removed the 8,037 ms the brief is built on, or whether that number is still live.",
      "costBasis": "read-from-code",
      "quote": "groundSampleBatcher.ts:281  `readonly flightsRunConcurrently: number;`  — present in `stats`, absent from CesiumViewport.ts:10640-10652, whose line still ends `max concurrent flights ${s.maxConcurrentFlights} (MUST be 1 — …)`",
      "file": "apps/editor/src/ui/geospatial/CesiumViewport.ts",
      "line": 10646,
      "fix": "Print `flightsRunConcurrently` in the onFlush line and rewrite the trailing clause: it should say which regime ran (\"tile-dedup guaranteed — FIFO skipped on N of M flushes\") rather than assert a requirement that L-13263 retired. One line. Do it before anyone re-measures the drape, or the next reading will be as ambiguous as this one.",
      "risk": "none",
      "confidence": "high"
    }
  ],
  "whatIcouldNotEstablish": "1. ⭐ WHETHER THE FOUNDER'S LOG PREDATES L-13263. The brief's single most damning line — `§DRAPE-COST-ATTRIBUTION: 8,037 ms waiting for terrain (shared FIFO), 23 ms own work` — may already be fixed. L-13263 landed today and makes the FIFO conditional on `terrainTileMemoEntry !== null`. His log shows `max concurrent flights 1`, but that reading is compatible with BOTH regimes (see the last finding), and the counter that would separate them is never printed. ⛔ Do not re-cost the drape until that counter is in the log — and note that if L-13263 IS in the run, then ~8 s of the ~7.2 s post-geocode budget the brief attributes to queuing is already gone and every ranking downstream of it changes.\n\n2. Whether the 2D pastel pane's mount-time read actually missed the warm's key. `resolveSiteFramingExtent` rejects an administrative geocode bbox and falls to a ±250 m box about the anchor, so `map.getCenter()` should land within a metre or two of the geocode point — and `bboxKey`'s 4 dp rounding tolerates ~5.5 m. It is a knife-edge I cannot resolve without the raw console. To settle it: log the resolved bbox key beside every `§CTX-PMTILES-READER <layer>` line. It does not change finding 3's fix, which is needed for the pan/commit case regardless.\n\n3. Millisecond costs for the five pastel near-extent reads. Those `§CTX-PMTILES-READER` lines are not in the excerpt I was given. `§CTX-READ-PROVENANCE` (contextTiles.ts:1801-1812) already prints \"this read PAID FOR NOTHING\" with the cache/in-flight split when downloads are 0 — that line, from the pastel reads, is the exact measurement needed and I could not obtain it.\n\n4. The cost of the second `renderFormaMassing` after the terrain clamp (CesiumViewport.ts:9973). Nothing prices it. `reseatContextPlacementsForBase` / `rebuildContextFarTierForBase` / `rebuildSiteScopeClipForBase` all print entity counts but no duration.\n\n5. Whether the `[ProjectHub] runtime.persistence.client.delete failed: timeout (HTTP 0)` contends with the tile reads. It is same-origin, so under HTTP/2 it holds one stream and not one of six connections — but I did not find the client's timeout value or confirm the deployed protocol, and ~565 tile requests on that origin is enough that the answer is not obvious. Establish it from a HAR, not from code.\n\n6. `activation:site:start +1568ms` — I did not open it. It is outside this dimension (it is a single 1.5 s block, not repeated work) but it is 1.5 s sitting directly in front of the mount, and the reveal deadline is 1,500 ms, which is close enough to the same number that someone should check whether one is causing the other rather than coinciding with it."
}