{
  "dimension": "The globe is the screen he is waiting for — how long does IT take, and does it block?",
  "summary": "⛔ THE GLOBE IS NOT THE 9,233 ms, AND ANY PLAN THAT SPENDS THE LANE ON CESIUM IS SPENDING IT ON THE WRONG THING. Settled from code, not intuition: the \"Where is your project?\" card and `open:first-interactive-frame` hang off the SAME `pryzm-project-loaded` emit. `briefBootstrap.ts:237` arms its one-shot listener BEFORE the create is issued; `PlatformShell._armPostSceneMarks` arms its listener later, inside `setProjectContext`. So on that one emit the order is `open:project-loaded` → `startOnboardingStepFlow()` → `renderLocationStep()` → `location-step:open` (all synchronous), and only THEN `open:snapshot-loaded` + a scheduled `open:first-interactive-frame` one frame later. ⭐ THE CARD APPEARS AT ≈ t+8,899 ms — the founder's 9,233 ms IS his wait to the globe prompt, to within one frame, and the globe contributes ZERO awaits to it. What gates it is the full engine boot + project create + open, which is other lanes' dimension. WHAT THE GLOBE DOES OWN is ~2,700 ms of MAIN-THREAD Cesium work sitting inside that window, and it owns it by CONTENTION, not by awaiting. `CesiumViewport.mount()` has exactly ONE await (`_awaitRendererLive`, a proven no-op here) and then runs 660 uninterrupted lines including `new Cesium.Viewer(...)` — one synchronous long task. ADR-0369's claim that the prewarm runs \"in genuine parallel with the engine boot\" is FALSE for main-thread work: one thread cannot run two long tasks at once, so the prewarm and the boot are ADDITIVE, interleaved at whichever task grabs the thread first. The repo's own corroboration: ISSUE-LOG L-12987 records that on a healthy run of this build `globe:eager-init-done` lands **~2.7 s after** `globe:eager-init-start` — whereas ADR-0369 §8 states a near-zero delta is the success signal for an ADOPTION. ⭐⭐ AND MOST OF THOSE 2.7 s ARE SPENT ON A PICTURE NOBODY SEES. With no site location (a new project has none), `mount()` frames **Sydney Opera House at 1,000 m altitude, pitch −45°** (`CesiumViewport.ts:4076`). The location card then cuts the camera to **WORLD_HOME 15°N 5°E at 20,000,000 m** (`siteEntryModel.ts:137`, `cameraForState(..., instant=true)`). So from t≈0 the warm-hidden viewer streams city-LOD ESRI imagery (`preloadSiblings`, `preloadAncestors`, `tileCacheSize=600`) AND Google Photorealistic 3D Tiles at `maximumScreenSpaceError=12` over the Sydney CBD — and the founder's own log line `§STARTUP-GLOBE-COMPLETE-FIRST — photoreal initial tiles loaded` PROVES that stream completed. Every byte and every decode of it is discarded by an instant cut to a whole-Earth view. (b) IS THEREFORE SETTLED IN BOTH DIRECTIONS: the photoreal handoff is NOT awaited — `void Cesium.Cesium3DTileset.fromIonAssetId(2275207).then(attach)` at :3873 — but it should not be REQUESTED before the prompt is up either. (e) WHAT HE WOULD ACCEPT: everything except the base-imagery globe at the world framing can arrive after the card is interactive. He cannot see Sydney, he cannot see photoreal at 20,000 km, and he cannot use the boundary-draw or envelope-drag tools until he has typed a city. The honest fix set is three small, reversible changes inside the globe's own file, worth ≈2 s of the 9.2 s and no more — I am not claiming it is the answer to his complaint.",
  "findings": [
    {
      "title": "The globe does not gate the prompt — the prompt and the first interactive frame fire off the SAME event, and the globe contributes no await to either",
      "detail": "Chain, read end to end. `briefBootstrap.installBriefBootstrap` subscribes to `pryzm:onboarding-brief-ready`; `handleBriefReady` arms a one-shot `pryzm-project-loaded` listener at :237 and only THEN calls `deps.createAndOpenProject(projectName)` at the bottom of the function. That listener marks `open:project-loaded` (:255) and synchronously calls `startOnboardingStepFlow(...)` → `OnboardingStepController.renderLocationStep()` → `markStartupPhase('location-step:open')` (OnboardingStepController.ts:708) → the card DOM is appended → `new GlobeHeroSearch(...)`. Separately `PlatformShell._armPostSceneMarks` subscribes to the SAME `pryzm-project-loaded` and marks `open:snapshot-loaded` (:200) then schedules `open:first-interactive-frame` via `getFrameScheduler().scheduleOnce` (:204). briefBootstrap subscribed first (at brief-ready, t≈0) and PlatformShell subscribes later (inside `setProjectContext`), so briefBootstrap's handler runs first on the emit. ⭐ CONSEQUENCE: `location-step:open` precedes `open:first-interactive-frame` by roughly one frame. The founder's `+334ms (t+9233ms)` is his wait to the globe prompt, ≈ t+8,899 ms, and NOTHING in the Cesium path is awaited by it. The reveal itself (`window.pryzmToggleGIS(true)` → `finishFirstGisActivation()` → `setVisible(true)`, GISAreaLayout.ts:1481-1486) is a bare visibility flip with no await, no tiles gate, and no `beginViewActivationLoading` handle — that activation gate is only used by `applyResultView` at GISAreaLayout.ts:2226, which is a later step. ⛔ Do not re-report the globe as the cause of the 9.2 s; report it as a contention contributor only.",
      "onCriticalPath": true,
      "costMs": "0 ms of AWAIT — MEASURED by code order (no await anywhere between the reveal and the mark). The gate is the engine boot + project open, not the globe.",
      "costBasis": "read-from-code",
      "quote": "markStartupPhase('open:project-loaded');",
      "file": "apps/editor/src/ui/onboarding/briefBootstrap.ts",
      "line": 255,
      "fix": "No fix here — this is the boundary of my dimension, recorded so a later lane does not chase the globe for the 9,233 ms. The reveal chain is already a visibility flip; ADR-0369 §6's warning that 'the reveal is still engine-gated' is still exactly true and is somebody else's finding.",
      "risk": "none",
      "confidence": "high"
    },
    {
      "title": "The prewarmed globe frames Sydney at 1 km and the location card instantly cuts it to whole-Earth 20,000 km — every tile streamed before the prompt is discarded",
      "detail": "A brand-new project has no site location, so `readSiteLocation()` (:4041) returns null (`runtime.siteModelStore.getLocation()` is empty and `getCurrentSiteOrigin()` is null on a fresh tab) and mount() takes the `else` arm: `camera.setView({ destination: Cartesian3.fromDegrees(151.2153, -33.8568, 1000), pitch: -45° })`. That is a CITY-LEVEL frustum. Three settings in the same synchronous block then maximise the streaming against it: `globeSurface.preloadSiblings = true` (:3935), `preloadAncestors` (:3936), `tileCacheSize = 600` (:3937) on the ESRI World Imagery layer (maximumLevel 19), plus the photoreal tileset at `maximumScreenSpaceError = 12` with `preferLeaves = true` and `preloadFlightDestinations = true` (:3790-3793). Meanwhile the card the user will actually see frames `cameraForState(INITIAL_SITE_ENTRY_STATE, true)` — `stage:'world'`, `focus:null` ⇒ `WORLD_HOME {lat:15, lon:5}` at `SITE_ENTRY_ALTITUDE_M.world = 20_000_000` m, `instant:true` (siteEntryModel.ts:137/211/407-417, called from `SiteEntryStore.frameCurrent()` :218 via `GlobeHeroSearch.mount()` :188). An instant cut from 1 km over Sydney to 20,000 km over the Gulf of Guinea shares not one tile. ⛔ NOT ASSERTED FROM A COMMENT — the comment at :4073 calls Sydney a 'fallback only … so the globe isn't pointed at empty space', which is precisely the wrong default now that the entry model declares a world framing.",
      "onCriticalPath": true,
      "costMs": "UNKNOWN in isolation; it is the dominant share of the ~2,700 ms MEASURED `globe:eager-init-start → globe:eager-init-done` leg (ISSUE-LOG L-12987, 2026-09-06, 'a healthy run on the same build logs globe:eager-init-done ~2.7 s later'). Not awaited, but spent on the one main thread the boot needs.",
      "costBasis": "repo-artifact",
      "quote": "destination: Cesium.Cartesian3.fromDegrees(151.2153, -33.8568, 1000),",
      "file": "apps/editor/src/ui/geospatial/CesiumViewport.ts",
      "line": 4076,
      "fix": "Make the no-location default the SAME framing the card will use, so the prewarm's work is the work the user sees. Replace the Sydney `setView` with the declared world target: import `cameraForState`/`INITIAL_SITE_ENTRY_STATE` (or the already-exported `worldFramingTarget()`, which is retained for exactly this and currently has zero production callers) and `setView` to WORLD_HOME at 20,000 km, pitch `SITE_ENTRY_PITCH_DEG.world`. At that altitude the globe needs ~level 2-4 imagery — tens of tiles, not thousands — and the photoreal set is below its SSE threshold entirely. This changes no reveal semantics, adds no seam, and makes ADR-0369's parallel-prewarm claim worth something for the first time. Keep the `if (initialLoc)` arm untouched: reopening a project WITH a location must still frame it.",
      "risk": "low",
      "confidence": "high"
    },
    {
      "title": "CesiumViewport.mount() is ONE synchronous long task — 'in parallel with the engine boot' is unachievable for it on a single thread",
      "detail": "`public async mount()` at :3494 awaits `this._awaitRendererLive(12_000)` at :3507 and then has ZERO further awaits until `this.resolveReady()` at :4169 — verified by `awk 'NR>=3494 && NR<=4175 && /await /'`, which returns exactly two hits, one being the :3507 gate and one being the word 'await' inside a comment at :4166. Between them sit ~660 lines: `new Cesium.Viewer(...)` (WebGL context acquisition + widget DOM + globe-surface shader programs), MSAA, `imageryLayers.removeAll()` + ESRI provider install, screen-space camera controller, render-error guard, context-loss guard, selection handler, the camera `setView`, `subscribeToSiteLocation()`, ResizeObserver and two camera event listeners. `prewarmGlobe` calls this at t≈0 (`await globe.mount()`, eagerGlobeStart.ts:208) from `PlatformRouter.showOnboarding` (:755), and `showOnboarding` then immediately emits `pryzm:onboarding-brief-ready` which starts the create→open→boot chain. ⛔ THE ADR'S CENTRAL CLAIM IS WRONG IN KIND, NOT DEGREE: 'genuine parallel with the boot' is true only for network and worker time. A single-threaded runtime cannot overlap two synchronous long tasks; whichever grabs the thread first, the other waits. The boot's own long tasks (builders/tools/bus handlers) equally starve the prewarm's async continuations, which is the most likely reason a supposedly-adopted globe still measured ~2.7 s of eager-init at t+7,079 ms on the 2026-09-06 run.",
      "onCriticalPath": true,
      "costMs": "UNKNOWN for `new Cesium.Viewer(...)` alone — it has NEVER been profiled in this repo (see the next finding). The enclosing leg is ~2,700 ms MEASURED (ISSUE-LOG L-12987).",
      "costBasis": "read-from-code",
      "quote": "    await this._awaitRendererLive(12_000);",
      "file": "apps/editor/src/ui/geospatial/CesiumViewport.ts",
      "line": 3507,
      "fix": "Do not try to 'parallelise' it — there is nowhere to put it. Make it SMALLER instead: (1) the world-framing default above removes the tile work it kicks off; (2) move the purely cosmetic knobs out of the mount task and into `setVisible(true)` (see the MSAA finding); (3) keep the mount where it is — running it at t≈0 while the network is still fetching the project-create response is still the best slot available for a task that must run somewhere.",
      "risk": "low",
      "confidence": "high"
    },
    {
      "title": "The photoreal tileset is NOT awaited (settled) — but it is REQUESTED at t≈0 for a camera nobody will ever see, and the founder's log proves the stream completed",
      "detail": "(b) answered in both directions with code. NOT AWAITED: `void Cesium.Cesium3DTileset.fromIonAssetId(2275207).then((tileset) => attachPhotorealTileset(...))` at :3873-3877 is fire-and-forget; `attachPhotorealTileset` (:3826) adds the primitive and registers an `initialTilesLoaded` listener (:3857) that hides the ellipsoid. `resolveReady()` at :4169 does not wait for any of it, and the block comment at :3757-3770 accurately describes that this was deliberately de-awaited by §STARTUP-GLOBE-COMPLETE-FIRST. ⭐ BUT THE WORK STILL HAPPENS, AND THE FOUNDER'S OWN LOG IS THE PROOF: `[gis][cesium] §STARTUP-GLOBE-COMPLETE-FIRST — photoreal initial tiles loaded` is emitted ONLY from the `initialTilesLoaded` handler at :3862-3865. Its presence means the tileset streamed and decoded its complete initial LOD for the then-current camera — Sydney CBD at 1 km — before the card cut to 20,000 km. That is tens of MB of Draco/glTF photogrammetry decoded and uploaded on the same main thread and GPU the boot is using. Secondary waste in the same block: with a token present, `photorealAvailable` is true so the Viewer option `baseLayer:false` is omitted (:3572) and Cesium installs its default ion World Imagery layer — which line :3686 then destroys with `imageryLayers.removeAll()`. One ion asset-endpoint round trip issued and thrown away on every mount.",
      "onCriticalPath": false,
      "costMs": "UNKNOWN — never instrumented. Bounded above by the ~2,700 ms eager-init leg it runs inside; the network share is additionally invisible to that mark because the attach is async. The ONE thing that is MEASURED is that the stream ran to completion (the founder's `initialTilesLoaded` line).",
      "costBasis": "read-from-code",
      "quote": "        void Cesium.Cesium3DTileset.fromIonAssetId(",
      "file": "apps/editor/src/ui/geospatial/CesiumViewport.ts",
      "line": 3873,
      "fix": "Two changes, both inside the existing branch. (1) With the world framing from finding 2, the tileset is out of SSE range anyway — but Cesium will still fetch the root tileset.json and the top-level tiles, so ALSO (2) hold the request behind a small `ensurePhotorealTileset()` called from the first camera descent (`SiteEntryStore.dispatch` reaching stage `city`) or from `setVisible(true)`, whichever comes first, rather than from mount. Keep `attachPhotorealTileset` byte-for-byte as it is — it already handles a late attach (§PLOT-CLEAR-PHOTOREAL L-429 re-applies the parcel void, the ground clamp re-arms) so late arrival is an ALREADY-SUPPORTED state, not a new one. Separately pass `baseLayer: false` unconditionally at :3572: the ESRI layer installed at :3720 is the real base on both paths and `removeAll()` already proves the ion default is unwanted.",
      "risk": "low",
      "confidence": "high"
    },
    {
      "title": "The globe has NEVER been profiled — the 2026-09-02 audit's 'Cesium pre-warm 165 ms, acquit' is an acquittal of the CHUNK, not of the Viewer",
      "detail": "`audit/perf/2026-09-02/diagnosis.md:100` reads: `| Cesium pre-warm | 165 self | eagerGlobeStart.ts/cesiumWarmup — DELIBERATE (documented 122 ms warm); acquit. |`. That verdict does not cover this dimension at all. Measured against the artifact itself: `grep -c CesiumViewport audit/perf/2026-09-02/axisD-cold-startup.cpuprofile` → **0**, and enumerating every call frame whose url contains 'cesium' yields 58 names, all of them module-evaluation frames (`r.parseAuthority`, `fromCssColorString`, `mercatorAngleToGeodeticLatitude`, `scaleToGeodeticSurface` …) with no `Viewer` constructor among them. The 165 ms is the top-level evaluation of the 5.9 MB `/cesium/Cesium.js` classic script, nothing else — that run reached the landing page and never mounted a globe. ⭐ SO THE LARGEST SINGLE UNKNOWN IN THIS DIMENSION IS ALSO THE ONE A COMPLETED AUDIT RECORDS AS CLOSED. That is the `gate-blind-on-the-wrong-axis` shape: a green reading on a different axis.",
      "onCriticalPath": true,
      "costMs": "UNKNOWN — and this finding IS the unknown. Profile bucket for context: `(program)` self = 5,777 ms of a 15,899 ms profile, which is where parse/compile of the 5.9 MB Cesium script lands, but the Cesium share of that bucket is not separable from the artifact.",
      "costBasis": "repo-artifact",
      "quote": "| Cesium pre-warm | 165 self | `eagerGlobeStart.ts`/`cesiumWarmup` — DELIBERATE (documented 122 ms warm); acquit. |",
      "file": "audit/perf/2026-09-02/diagnosis.md",
      "line": 100,
      "fix": "CHEAPEST PROBE, ZERO CODE — from the founder's next New-Project run, read five existing marks in order and nothing else: `globe:prewarm-start` → `globe:prewarm-done` → `globe:eager-init-start` → `globe:eager-init-done` → `location-step:open`, plus WHERE the `§STARTUP-GLOBE-COMPLETE-FIRST — photoreal initial tiles loaded` line sits among them. (i) No `globe:prewarm-done` at all ⇒ ADR-0369 Stage 1 is inert on this path and the globe is being built cold inside the boot. (ii) `globe:eager-init-start → -done` far from zero ⇒ the adoption is not free and ADR-0369 §8's own success criterion is failing (L-12987 already recorded ~2.7 s). (iii) The photoreal line BEFORE `location-step:open` ⇒ finding 2 and 4 are confirmed waste, exactly as predicted. NEXT CHEAPEST, STILL ZERO CODE — one Chrome Performance recording of the New-Project click; filter the main thread for tasks whose top frame url is `/cesium/Cesium.js`. That yields the `new Cesium.Viewer` long task and the per-frame render cost directly, which is the number nobody in this repo has.",
      "risk": "none",
      "confidence": "high"
    },
    {
      "title": "MSAA 4×, sun lighting and dynamic atmosphere are configured inside the mount task on a viewer that is visibility:hidden and will be re-framed within the same second",
      "detail": "Inside the one synchronous mount block: `sc.msaaSamples = 4` at :3605, then `globe.enableLighting = true` (:3956), `globe.dynamicAtmosphereLighting = true` (:3957), `atmosphereBrightnessShift` and the ground/sky atmosphere. Each of these changes the globe-surface shader permutation, so the programs Cesium compiles during that same task are the expensive lit+atmosphere+MSAA variants — compiled at full container resolution, because `enterWarmHiddenState()` (:18678) deliberately sets `display:block` so the canvas has real dimensions, and the container is `flex:1 1 0; height:100%` inside a full-viewport `#dck-workspace` (index.html:42/62). All of it is for a surface the user cannot see: `visibility:hidden; pointer-events:none` until `setVisible(true)`. ⛔ Not asserted from the comments — the §CESIUM-QUALITY-MSAA comment claims the cost is 'paid only when the scene actually redraws', which is true of the per-frame resolve and false of the shader compile that the flag forces at mount.",
      "onCriticalPath": true,
      "costMs": "UNKNOWN — shader-program compile time is GPU-driver dependent and is not separable without the Performance recording above. It is inside the ~2,700 ms MEASURED eager-init leg.",
      "costBasis": "read-from-code",
      "quote": "          sc.msaaSamples = 4;",
      "file": "apps/editor/src/ui/geospatial/CesiumViewport.ts",
      "line": 3605,
      "fix": "Move the three cosmetic knobs (msaaSamples, enableLighting, dynamicAtmosphereLighting) out of `mount()` and into `setVisible(true)` (:18690), which is the exact moment the surface becomes something a human can judge. Nothing else reads them in between — `applyFormaMode`/`restorePhotorealMode` set their own. ⚠ Order this AFTER the world-framing change and re-measure: at 20,000 km with a handful of level-3 tiles the compile may be small enough that this is not worth the churn, and shipping a quality regression for an unmeasured saving is the wrong trade on a founder who asked for 'no shortcuts'.",
      "risk": "low",
      "confidence": "medium"
    },
    {
      "title": "RULED OUT — `_awaitRendererLive(12_000)` is a synchronous no-op on this path, despite a 12,000 ms constant sitting next to a 9,233 ms symptom",
      "detail": "Named explicitly because it is the single most inviting false lead in this dimension. `mount()`'s only await is `await this._awaitRendererLive(12_000)` at :3507. The method's first two statements are `const recovering = () => (globalThis as ...).__pryzmRendererRecovering === true;` and `if (!recovering()) return;` (:6418-6420). `__pryzmRendererRecovering` is set only by `createRenderer.ts` for the duration of a WebGPU device-loss recovery window; on a normal new-project run it is undefined, so the method returns on its first branch with no timer, no polling and no warn line. If it HAD engaged, the founder's console would carry `§FIX-WEBGPU-DEVICE-LOSS-CESIUM-CASCADE renderer is mid device-loss recovery — deferring globe activation` — and the quoted log has no such line. ⭐ CHECK THE CALLER COUNT TOO: the 12 s constant appears twice (:3507 mount, :6494 `recoverFromGpuReset`), and the second is unreachable except after a `webglcontextrestored` event.",
      "onCriticalPath": false,
      "costMs": "0 ms MEASURED — the guard returns before its first `await` whenever `__pryzmRendererRecovering` is falsy, which is every non-device-loss run.",
      "costBasis": "read-from-code",
      "quote": "    if (!recovering()) return;",
      "file": "apps/editor/src/ui/geospatial/CesiumViewport.ts",
      "line": 6420,
      "fix": "None. Recorded so no lane spends time on it. Do not 'optimise' the constant.",
      "risk": "none",
      "confidence": "high"
    },
    {
      "title": "RULED OUT — the ten-module `Promise.all` in front of the globe's readiness gate is only ~10 KB of extra download; splitting it buys nothing",
      "detail": "`ensureGisInitialized` builds `gisInitPromise = Promise.all([...])` over TEN dynamic imports (GISAreaLayout.ts:1035-1058) before it will construct or adopt a viewport, and `_resolveCameraHostReady()` — the gate `GlobeHeroSearch.mount()` awaits — resolves only inside that `.then`. Seven of the ten are parcel/envelope authoring surfaces (`SiteBoundaryDrawTool`, `siteEnvelopeDrawCesium`, `siteEnvelopeDrawArming`, `spaceEnvelopeDragSurface`, `spaceEnvelopeFaceDragFocusState`, `spaceEnvelopeFaceDragSurfaces`, `@pryzm/plugin-geospatial`) that cannot be used until the user has geocoded and drawn — so this LOOKS like a textbook critical-path defect. ⛔ IT IS NOT, AND I AM REPORTING THE NEGATIVE RATHER THAN THE PLAUSIBLE STORY. Measured against the real build output: `SiteBoundaryDrawTool-CgZgkV-U.js` = 6,294 B, `siteGeocodeSearchBox-CRHNkW6S.js` = 3,602 B; the envelope/face-drag modules do not even get their own chunks (they are folded into shared chunks already pulled by `CesiumViewport-Dcg57RuF.js`, which is itself 297,913 B and whose static imports — `main`, `domain-engine`, `siteRealData`, `solarCodec`, `climateChartData` — are already in flight from `ensureEngineWarm()` at the same instant). Splitting the batch would move ~10 KB.",
      "onCriticalPath": true,
      "costMs": "≈0 ms of incremental download — MEASURED from dist chunk sizes (6,294 B + 3,602 B for the two that have their own chunks; the rest resolve from chunks already required by CesiumViewport).",
      "costBasis": "read-from-code",
      "quote": "        gisInitPromise = Promise.all([",
      "file": "apps/editor/src/ui/layout/GISAreaLayout.ts",
      "line": 1035,
      "fix": "None — do not split this batch. Recorded so the next reader does not spend a lane on a 10 KB win, and so the ADR-0369 §4.1 rule ('the adoption replaces exactly TWO steps — construction and mount(); everything below runs on BOTH arms') stays intact. An `if (prewarmed)` shortcut around this wiring is the 'built but unreachable' defect the ADR wrote a test case against.",
      "risk": "none",
      "confidence": "high"
    },
    {
      "title": "OUT OF THE 9,233 ms WINDOW, BUT THE PRODUCT'S LARGEST GLOBE COST: /cesium/Cesium.js is a 5,909,848-byte classic BLOCKING script in the head of index.html",
      "detail": "`dist/index.html:5` is `<script src=\"/cesium/Cesium.js\"></script>` — no `defer`, no `async`, not a module — injected by `vite-plugin-cesium`'s `transformIndexHtml`. `ls -la dist/cesium/Cesium.js` → **5,909,848 bytes**, and vite.config.ts:59-66 states the mechanism in its own words: 'it is a classic blocking `<script>`, not a module — the browser fetches and executes it before the page's own entry runs.' The externalisation is real: the built `CesiumViewport-Dcg57RuF.js` imports only from `main`, `domain-engine`, `siteRealData`, `solarCodec` and `climateChartData` — no `vendor-cesium` chunk exists — so every `import * as Cesium from 'cesium'` resolves to the global that this script defines. ⚠ HONEST SCOPE, STATED BEFORE ANYONE BANKS IT: this is paid at PAGE LOAD, before `beginStartupBudget()` runs in `showOnboarding`, and the '+ New Project' gesture is pure SPA (`ProjectHub.startGuidedOnboardingDirect` → `onStartOnboarding` → `showOnboarding` → `createAndOpenProject` → `launchWorkspace`; no reload, no navigation anywhere on that path). So it is NOT part of the founder's 9,233 ms and must not be counted toward it. It IS most of why opening pryzm.app at all is slow, and it delays the hub the founder clicks New Project in.",
      "onCriticalPath": false,
      "costMs": "UNKNOWN as a separated number. Context, MEASURED: `(program)` self time is 5,777 ms of the 15,899 ms `audit/perf/2026-09-02/axisD-cold-startup.cpuprofile`, and parse+compile of this 5.9 MB script lands inside that bucket; `/cesium/Cesium.js` also carries 165 ms of its own attributed self time in the same profile.",
      "costBasis": "read-from-code",
      "quote": "    <script src=\"/cesium/Cesium.js\"></script>",
      "file": "dist/index.html",
      "line": 5,
      "fix": "NOT a one-line `defer` — that would break every module that reads the `Cesium` global during evaluation. The architecturally sound version is to make the globe pay for itself: drop `vite-plugin-cesium`'s injection for `index.html` (the existing `stripCesiumFromNonGlobePagesPlugin` at vite.config.ts:81 already demonstrates the exact seam and currently opts out only `component-editor.html`), and have `cesiumWarmup.ensureCesiumWarm()` inject and await the script tag before resolving `warmCesiumModule()`, so `prewarmGlobe`'s existing `load()` await is the one place that gates on it. ⚠ PRECONDITION, NOT OPTIONAL: `grep` every module-scope read of the Cesium global first — `CesiumViewport.ts:598` runs `Cesium.Ion.defaultAccessToken = _cesiumToken ?? ''` at module evaluation, which is inside the lazily-imported chunk and therefore safe, but it must be proven that no eagerly-evaluated module does the same. Cost one lane, browser verification mandatory, and it belongs in a page-load lane rather than this one.",
      "risk": "medium",
      "confidence": "high"
    }
  ],
  "whatIcouldNotEstablish": "1. ⭐ THE ONE NUMBER I MOST WANTED AND DO NOT HAVE: what `new Cesium.Viewer(...)` actually costs on the founder's machine. It has never been profiled in this repo — `audit/perf/2026-09-02/axisD-cold-startup.cpuprofile` contains ZERO `CesiumViewport` frames and no Viewer constructor (that run stopped at the landing page), and `audit/perf/2026-09-04/axisR-heavy-scene.cpuprofile` is a heavy-scene axis, not a startup one. Everything I attribute to the mount rests on the ~2,700 ms `globe:eager-init-start → globe:eager-init-done` figure recorded in ISSUE-LOG L-12987 for 'a healthy run on the same build', which is a WALL-CLOCK leg that also contains ten dynamic imports and the bridge/geocode/boundary wiring. I did not decompose it, and I did not run a browser (localhost dev is unusable in this repo and I hold no founder credentials, the same wall STARTUP37 hit).\n\n2. WHETHER THE PREWARM IS ACTUALLY DELIVERING ON THE NEW-PROJECT PATH. I proved the call exists and is unconditional (`PlatformRouter.ts:755`), that `#container` is a static, full-size, never-removed element (index.html:42/62/425, no removal in `src/main.ts` or `public/app-shell-boot.js`), and that the adoption arm exists (`GISAreaLayout.ts:1090`). I could NOT prove it fires and is adopted on the founder's run, because his quoted log does not include `globe:prewarm-start` / `globe:prewarm-done` / `globe:eager-init-*`. The 2026-09-06 reading of `globe:eager-init-start +2193ms (t+7079ms)` with a ~2.7 s tail is evidence AGAINST it delivering — ADR-0369 §8 says a near-zero delta there is the success signal — but one trace is not a verdict. This is probe (i) in finding 5 and it must be read before anyone acts on findings 2, 3 or 6.\n\n3. WHETHER THE FOUNDER'S RUN HAD `VITE_CESIUM_TOKEN` OR `VITE_GOOGLE_MAPS_KEY`. It had one of them — the photoreal branch cannot log without a credential — but which one changes the request shape (`fromIonAssetId(2275207)` via the ion CDN vs `createGooglePhotorealistic3DTileset({key})` direct). His console prints the answer verbatim at `CesiumViewport.ts:582` (`VITE_CESIUM_TOKEN PRESENT/ABSENT, VITE_GOOGLE_MAPS_KEY PRESENT/ABSENT`); it was not in the excerpt I was given.\n\n4. THE ORDER OF `location-step:open` VS `open:first-interactive-frame` IS DERIVED, NOT OBSERVED. I established it from listener-registration order (briefBootstrap subscribes at brief-ready; PlatformShell subscribes inside `setProjectContext`) plus the fact that `open:first-interactive-frame` is deferred one tick through `getFrameScheduler().scheduleOnce`. ⚠ Note that `startupBudget.ts`'s own vocabulary comment (:158-159) lists the opposite order — `open:first-interactive-frame → globe:eager-init-start/-done → open:project-loaded → location-step:open`. One of the two is wrong. His next paste settles it in one line and I did not want to assert from the comment, which is why I derived it from the code instead.\n\n5. HOW MANY TILES THE SYDNEY FRUSTUM ACTUALLY FETCHES. I can name every amplifier (`maximumLevel: 19`, `preloadSiblings`, `preloadAncestors`, `tileCacheSize: 600`, tileset SSE 12, `preferLeaves`) and I can prove the photoreal stream RAN TO COMPLETION from his own `initialTilesLoaded` line — but I have no count and no byte total. The Network panel on one New-Project run, filtered to `server.arcgisonline.com` and `tile.googleapis.com` before `location-step:open`, gives both in thirty seconds.\n\n6. ⛔ FINALLY, THE ARITHMETIC DOES NOT ADD UP TO 9,233 ms IN THIS DIMENSION, AND I AM NOT GOING TO MAKE IT. The globe contributes zero awaits to that number and roughly 2.7 s of contention at most, of which perhaps 2 s is recoverable. The other ~7 s is engine boot, project create and hydrate — the marks that name it are `boot:engine-start` → `boot:ui-done` → `open:version-read-done` → `open:snapshot-loaded`, and they belong to the other lanes. If the fleet's findings sum to 9.2 s only by counting the globe twice, the sum is wrong."
}