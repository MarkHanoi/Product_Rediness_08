{
  "dimension": "The same seven context layers are re-read over and over, each read \"PAYING FOR NOTHING\" — triggers, idempotence guards, render cost, and the link (or not) to the parcel highlight.",
  "summary": "The re-read loop has THREE independent causes, and two of them are named regressions from 2026-09-07.\n\n(1) THE WARM CACHE WAS MOVED OFF THE 2D MAP'S KEY. Two commits on 2026-09-07 (`7d4dea4c`, `d1ecb2fe`) widened the startup warm from the near default 0.008° to `groundHalfDeg` = 0.016° for roads/parks/rail and to `treesHalfDeg` for trees — to match the 3D render. The 2D map's pastel path (`pastelContextGeoJson.ts:243-252`) still asks for those four layers at the plain default. Before those commits the warm called `fetchContextRoads(lat, lon)` with NO fourth argument — byte-identical to what the 2D asks — so the 2D map's reads were cache HITS. Since 2026-09-07 they are cold misses over tiles the warm already downloaded. That is exactly the \"PAID FOR NOTHING — 25 tile(s) already decoded, 0 downloaded of 25\" signature, and the repo's own spec derives the founder's tile counts from this arithmetic: `contextWarmExtentAgreement.spec.ts` asserts `readTiles(CTX_NEAR_HALF_DEG).size === 25` and `readTiles(groundFetchHalfDeg()).size === 81`. His log shows 81, then 25, 25, 25, 25.\n\n(2) THE PINNED PAN-REFRESH CANNOT CLEAR ITS OWN TRIGGER. `ff60ce85` (2026-09-07) pinned the reload target to the parcel anchor but left the trigger measuring camera-to-anchor distance. The reload writes `contextBuildingsAt = {lat, lon}` back to the same point, so the distance is unchanged and only the 20 s cooldown resets — a permanent 20-second heartbeat while the camera sits >1500 m from the anchor. Answer to (b): the refresh is doing NOTHING useful when pinned, and the code already contains the guard that says so — `loadContextBuildingsUncoalesced` has an exact same-point early-out at `CesiumViewport.ts:11956-11964`, and the pan-refresh passes `force = true` to defeat it.\n\n(3) SIX OF THE SEVEN LAYERS KEY THEIR IDEMPOTENCE ON A FIELD A DIFFERENT LAYER OWNS. roads/rail/water/parks all guard on `this.contextBuildingsAt`, which is null until the slowest layer (buildings, 81 tiles / 5442 footprints) lands. For the whole startup window the guard is dead and every unforced call re-runs, aborting the previous.\n\n(c) THE MOST VALUABLE ANSWER IS A NEGATIVE: a context re-render CANNOT clear the parcel highlight. `setStyle` is called in exactly one place (`swapBasemap`, :3568, Map/Satellite button only); the highlight source is written only by `refreshParcelHighlight` (:1567); and the 2D map subscribes to exactly two things — `site.parcel-boundary-set` (:4240) and the envelope-visibility authority — neither published by a context read. The coupling runs the OTHER way: committing a parcel calls `frameCommittedParcel` → `fitBounds(duration:350)` → `moveend` → a cold six-layer re-read 350 ms after the gesture. The headline lane should look elsewhere for the highlight.\n\n(d) The sea walk is NOT repeated per re-read — `contextWater.ts:738` returns the cached collection before any walk and the refusal is cached. It IS repeated once per distinct EXTENT, and there are exactly three (near 0.008 / ground 0.016 / sea 0.10). That is his three refusals on three bboxes. Fixing (1) removes one of the three.\n\n(e) The idempotence guard that should be firing exists in THREE forms and none of them covers this: the lattice snap `contextFetchBbox` (authored 2026-09-06 for precisely this founder complaint) is wired into buildings ONLY; the same-point early-out is defeated by `force:true`; and the six ground layers' guards are conditioned on another layer's anchor.",
  "whatIcouldNotEstablish": "• THE ACTUAL MILLISECOND RENDER COST OF ONE 2D RE-PUSH. There is no instrumentation on it anywhere — the only timings in the log are read timings. I can name the deterministic work per push (a 5442-feature `.filter`, a second full 5442 pass in `summariseOfficialFootprints`, six collection→GeoJSON conversions that allocate a fresh `[lon,lat]` array per vertex, an un-memoised `synthesiseCanopies` capped at 3000, then eight `setData` calls) but I did NOT measure it and will not invent a number. A `performance.now()` bracket around `loadPastelContext`'s `run()` and around the `live.setData` at :2925 would settle it in one session.\n\n• WHETHER `treesFetchHalfDeg` AT BARCELONA IS STRICTLY GREATER THAN 0.008. It is `max(CTX_NEAR_HALF_DEG, min(1781, 7071, scopeReadCompleteCeilingM(lat,lon).radiusM)/111320)`, and `scopeReadCompleteCeilingM` is tile-count-derived at the site coordinates. I did not evaluate it. If it happens to clamp to 0.008 then trees is a HIT and only roads/parks/rail regressed (three layers, not four). The inequality the spec itself asserts (`treesHalfDeg <= groundFetchHalfDeg()`) allows either.\n\n• WHICH LAYER THE \"20 TILE\" READ IN HIS LOG IS. The 81s and the 25s map cleanly onto ground-extent and near-extent reads via the spec's own arithmetic; 20 does not, and I did not chase it.\n\n• WHETHER MAPLIBRE'S `map.resize()` FIRES A `moveend` IN THE PINNED VERSION. If it does, every pane resize is an extra trigger — though harmless in practice, because a pure resize does not move the centre and the `toFixed(3)` key would skip it.\n\n• WHETHER THE FOUNDER'S SESSION WAS IN `formaMode` AT EVERY PAN-REFRESH LINE. The loop in F2 is specific to `formaMode` (outside it, `pinned` is null, the reload re-centres on the camera and the loop terminates). The brief quotes the PINNED clause from his log, which implies formaMode was true, but I read that from the brief, not from the raw log — I do not have the log file.\n\n• THE WEBSOCKET rAF WRITE LOOP. Out of this dimension; not investigated. It is plausible CPU contention with everything above but I have no evidence either way.\n\n• WHETHER THE HIGHLIGHT REGRESSION HAS A CAUSE AT ALL IN THIS DIMENSION. I established the negative (no re-render path can clear it) rather than a cause. UNKNOWN where it actually broke.",
  "findings": [
    {
      "title": "REGRESSION: the 2026-09-07 warm-extent fixes moved the warm cache OFF the 2D map's key for roads/parks/rail/trees — the 2D pastel push went from four cache HITS to four cold reads",
      "detail": "`contextLayerWarm.ts:115-122` now warms roads/parks/rail at `groundHalfDeg` (= `farFetchHalfDeg(DEFAULT)` = 1781/111320 = 0.015999°) and trees at `treesHalfDeg`. The 2D map's pastel path asks for those same four layers with NO fourth argument, i.e. at `CTX_NEAR_HALF_DEG = 0.008` (`contextExtentBudget.ts:745`). The collection caches are keyed by bbox (`contextRoads.ts:48` `bboxKey = b.map(n => n.toFixed(4))`), so 0.008 and 0.016 are different keys and the warm can never serve the 2D map.\n\nGIT NAMES IT. `git show 7d4dea4c~1:apps/editor/src/ui/geospatial/contextLayerWarm.ts` reads:\n    ['roads', fetchContextRoads(lat, lon)],\n    ['parks', fetchContextParks(lat, lon)],\n    ['rail',  fetchContextRail(lat, lon)],\n    ['trees', fetchContextTrees(lat, lon)],\nAll four at the DEFAULT — byte-identical to what `pastelContextGeoJson.ts` still asks for. `7d4dea4c` (2026-09-07) moved roads/parks/rail; `d1ecb2fe` (2026-09-07) moved trees. Both fixes were aimed at the 3D render. Neither considered the 2D map, which is the THIRD consumer.\n\nTHE FOUNDER'S TILE COUNTS ARE THE ARITHMETIC SIGNATURE. `contextWarmExtentAgreement.spec.ts` asserts `readTiles(CTX_NEAR_HALF_DEG).size === 25` and `readTiles(groundFetchHalfDeg()).size === 81` at his exact coordinates (LAT 41.3825802, LON 2.177073 — the spec's own constants). His log: 81, then 25, 20, 25, 25, 25. The 25s are the 2D map's near-default reads that used to be free.\n\nTHIS IS THE THIRD RECURRENCE OF ONE DEFECT. The spec's own header records the first two: \"§SITE-SCOPE F-2 then moved the ROADS and PARKS render reads onto groundFetchHalfDeg(scope) and left the warm on the NEAR default\" (L-13079), then \"THE SECOND RECURRENCE (L-13161) ... It closed them ONE LAYER AT A TIME, by hand, and `trees` and `rail` were left on the near default\". The first two recurrences were one LAYER at a time. This one is one CONSUMER at a time — and the spec is blind to it, because it enumerates exactly two consumers by name and reads them as source text (`readSrc('contextLayerWarm.ts')`, `readSrc('CesiumViewport.ts')`). `pastelContextGeoJson.ts` appears nowhere in it.",
      "isRegression": true,
      "suspectCommit": "7d4dea4c 2026-09-07 wip(startup/scope/globe): land four lanes' in-flight work at the session boundary (roads/parks/rail) + d1ecb2fe 2026-09-07 perf(startup/§REVEAL-DOES-NOT-AWAIT-THE-WARM) (trees)",
      "file": "apps/editor/src/ui/geospatial/pastelContextGeoJson.ts",
      "line": 243,
      "quote": "            fetchContextRoads(lat, lon, signal),\n            fetchContextWater(lat, lon, signal),\n            fetchContextParks(lat, lon, signal),\n            fetchContextLanduse(lat, lon, signal, CONTEXT_WIDE_HALF_DEG),\n            fetchContextRail(lat, lon, signal),\n            fetchContextTrees(lat, lon, signal),",
      "fix": "Pass the SAME half-extents the warm now uses, from the 2D path: `fetchContextRoads(lat, lon, signal, groundFetchHalfDeg())`, same for parks and rail, and `fetchContextTrees(lat, lon, signal, treesFetchHalfDeg(undefined, scopeReadCompleteCeilingM(lat, lon).radiusM))`. Note landuse and water already agree (WIDE and default respectively) and must NOT be touched. ⛔ The one-layer-at-a-time fix has now failed three times — so extend `contextWarmExtentAgreement.spec.ts` to enumerate the consumer SET, not two named files: assert that every `fetchContextX` call site outside `__tests__` passes the same half-extent for a given X, by grepping the source for the call sites rather than listing them. That is the axis the gate is blind on.",
      "risk": "medium",
      "confidence": "high",
      "costMs": "Four cold collection reads per 2D re-read that were previously free. His log measures one of them directly: roads 15,314 ms on the first read vs 4 ms on a later one over the same tiles. The tiles are shared so the bytes are not re-downloaded, but the whole crop + build re-runs."
    },
    {
      "title": "The pinned pan-refresh cannot clear its own trigger — it is a permanent 20-second heartbeat that reloads the identical bbox",
      "detail": "THE GATE AND THE ACTION MEASURE DIFFERENT POINTS. `maybeRefreshContextOnPan` (`CesiumViewport.ts:13711`) computes `anchor = this.formaMassingOrigin ?? this.contextBuildingsAt` and asks `shouldRefetchContextOnPan` whether the CAMERA has left a 1500 m ring around that anchor (`contextBuildings.ts:1852` `CONTEXT_PAN_FARRING_RADIUS_M = 1500`, `:1855` `CONTEXT_PAN_COOLDOWN_MS = 20_000`, camera altitude must be <6000 m). If yes, it reloads — but PINNED to that same anchor (`:13752-13753`), and `loadContextBuildingsUncoalesced` writes `this.contextBuildingsAt = { lat, lon }` at `:12080`, i.e. back to the point it already was.\n\nSo the reload cannot change the quantity the gate measures. The only state it mutates is `this.contextLastLoadAtMs = Date.now()` (`:11968`). Every subsequent `moveEnd` past 20 s re-fires, indefinitely, for as long as the camera sits outside the ring.\n\nHIS NUMBERS: 41.38258,2.17707 → 41.36630,2.18539. dLat = -0.01628° × 111320 = -1812 m; dLon = 0.00832° × 111320 × cos(41.37°) = +695 m; hypot = 1941 m. Past the 1500 m ring by 30 %, and the reload does not move it one metre closer.\n\nGIT NAMES THE CHANGE. `git show ff60ce85~1:...CesiumViewport.ts` had `void this.loadContextBuildings(camLat, camLon, true);` — the CAMERA point, which moved `contextBuildingsAt` to the camera and terminated the loop whenever `formaMassingOrigin` was null. And on the founder's STEP 2 screen it IS null: `formaMassingOrigin` is assigned only inside `renderFormaMassing` (`:8235`), which has not run before a parcel is committed. So on that screen `anchor === pinned === contextBuildingsAt` exactly — the reload target is literally the point the distance was measured from.\n\nANSWER TO (b): no, this refresh is not doing anything useful. The comment at `:13746-13750` asserts it is idempotent and therefore harmless (\"re-reading about the anchor is idempotent ... which is the correct behaviour, not a disabled feature\"). It is idempotent at the READ layer only. It still runs `clearContextBuildings()`, re-places every entity, rebuilds the far-tier primitive and re-applies the scope clip, every 20 seconds, forever.",
      "isRegression": true,
      "suspectCommit": "ff60ce85 2026-09-07 feat(3d-site/§SITE-SCOPE): the scope read gets its own ceiling, and the backdrop goes white — lane SCOPE-CUT-2, rescued",
      "file": "apps/editor/src/ui/geospatial/CesiumViewport.ts",
      "line": 13763,
      "quote": "      void this.loadContextBuildings(to.lat, to.lon, true);",
      "fix": "Make the gate measure the thing the action changes. Since the reload is pinned, the correct trigger is not 'the camera left the ring' but 'the ANCHOR moved' — which on this screen it never does, so the refresh should simply not fire. Minimal correct fix: in `maybeRefreshContextOnPan`, once `pinned` is resolved, compare it to `this.contextBuildingsAt` and return before the setTimeout when they are the same point (the ε 1e-7 test §STARTUP-LOCATION-IDEMPOTENT already uses at `CesiumViewport.ts:5044-5047`). Keep the camera-centred branch (`pinned === null`, non-Forma) exactly as it is — there the reload genuinely moves the anchor and the loop terminates. ⛔ Do NOT lengthen the cooldown; that hides the heartbeat without removing it.",
      "risk": "low",
      "confidence": "high",
      "costMs": "One full context reload every 20 s for the life of the session while the camera is >1500 m from the anchor and <6000 m up. Each is a clearContextBuildings + re-place of ~5442 entities + far-tier rebuild + scope re-clip."
    },
    {
      "title": "The exact same-point guard that would refuse every pinned reload already exists — the pan-refresh passes force:true to defeat it",
      "detail": "`loadContextBuildingsUncoalesced` carries a same-point early-out that is byte-for-byte the guard finding 2 needs:\n\n  CesiumViewport.ts:11956-11964\n    // Skip when unchanged (≈0.1 m) and we already have entities, unless forced.\n    const prev = this.contextBuildingsAt;\n    if (\n      !force && prev &&\n      Math.abs(prev.lat - lat) < 1e-6 && Math.abs(prev.lon - lon) < 1e-6 &&\n      this.contextBuildingEntities.length > 0\n    ) {\n      return;\n    }\n\nAfter `ff60ce85` pinned the target, `lat/lon` at the pan-refresh call site IS `prev.lat/prev.lon` by construction — both are `formaMassingOrigin ?? contextBuildingsAt`. So this guard would refuse every single pinned refresh, correctly. `force = true` at `:13763` turns it off.\n\nThe `force` flag is a survival from the pre-pin code, where the reload genuinely targeted a different point (the camera) and the early-out would have been wrong to fire. The pin changed the target and left the flag.\n\nThis is the direct answer to (e) part 1: the idempotence guard that should be firing is not merely absent — it is present, correct, and explicitly disabled by its caller. The founder's own log shows the sibling pattern firing successfully one file over (`§STARTUP-LOCATION-IDEMPOTENT`, `CesiumViewport.ts:5035-5056`), whose comment says in terms what this one would prevent: \"would re-fly the camera to where it is going anyway and FORCE a full context re-render (thousands of entities + a 6,000-point terrain batch re-sample) that changes nothing on screen.\"",
      "isRegression": true,
      "suspectCommit": "ff60ce85 2026-09-07 (pinned the target without revisiting the force flag its caller passes)",
      "file": "apps/editor/src/ui/geospatial/CesiumViewport.ts",
      "line": 11956,
      "quote": "    // Skip when unchanged (≈0.1 m) and we already have entities, unless forced.\n    const prev = this.contextBuildingsAt;\n    if (\n      !force && prev &&\n      Math.abs(prev.lat - lat) < 1e-6 && Math.abs(prev.lon - lon) < 1e-6 &&\n      this.contextBuildingEntities.length > 0\n    ) {\n      return;\n    }",
      "fix": "Drop the third argument at `CesiumViewport.ts:13763` — `void this.loadContextBuildings(to.lat, to.lon);` — so the pinned refresh is subject to the same-point early-out, while the camera-centred (non-Forma) branch continues to pass through it because its point genuinely differs. This is a one-token change and is strictly safer than finding 2's gate fix, though the two are complementary: this one stops the work, that one stops the wake-up.",
      "risk": "low",
      "confidence": "high"
    },
    {
      "title": "Six of the seven layers key their own idempotence on contextBuildingsAt — a field the BUILDINGS layer owns, and which is null for the whole window the re-reads happen in",
      "detail": "Four verbatim, all identical in shape:\n\n  CesiumViewport.ts:13810 (roads)  if (!force && this.contextRoadEntities.length > 0 && this.contextBuildingsAt && ...)\n  CesiumViewport.ts:13960 (rail)   if (!force && this.contextRailEntities.length > 0 && this.contextBuildingsAt && ...)\n  CesiumViewport.ts:14096 (water)  if (!force && this.contextWaterEntities.length > 0 && this.contextBuildingsAt && ...)\n  CesiumViewport.ts:14540 (parks)  if (!force && this.contextParkEntities.length > 0 && this.contextBuildingsAt && ...)\n\nEach compares the lat/lon it was handed against `this.contextBuildingsAt` — where the BUILDINGS were last loaded, not where THIS layer was last loaded. `contextBuildingsAt` is written only at `:12080`, inside the buildings load, which is the slowest layer of the seven (81 tiles, 5442 footprints in his session). Until it lands, `this.contextBuildingsAt` is null, the `&&` short-circuits, the guard is dead, and every unforced call to any of these four re-runs the whole loader — including `this.contextRoadsAbort?.abort()` on the previous one (`:13814`).\n\nThat window is precisely the startup + parcel-select period the founder is complaining about. It also means the guard's own doc-comment premise is wrong: a layer cannot be idempotent with respect to a coordinate another layer records.\n\nThis is (e) part 2. Note the shape: the guard is not missing, it is conditioned on the wrong owner — the same class as findings 3 and 5, three times over in one subsystem.",
      "isRegression": false,
      "file": "apps/editor/src/ui/geospatial/CesiumViewport.ts",
      "line": 13810,
      "quote": "    if (!force && this.contextRoadEntities.length > 0 && this.contextBuildingsAt &&\n        Math.abs(this.contextBuildingsAt.lat - lat) < 1e-6 &&\n        Math.abs(this.contextBuildingsAt.lon - lon) < 1e-6) return;",
      "fix": "Give each loader its own anchor field, the way trees already has one (`this.contextTreesAt`, read at `:10121`): add `contextRoadsAt` / `contextRailAt` / `contextWaterAt` / `contextParksAt`, set them where the loader commits to fetch, and compare against those. ⚠ Read `§CTX-RESEAT-ANCHOR-IS-CURRENT-SITE` (L-12964, documented at `:10110-10119`) before doing it — a remembered per-layer anchor that nobody clears on a site change caused the Córdoba canopy flip-flop, which is why trees now routes through `reseatAnchorForCurrentSite`. Use that helper, not a raw field.",
      "risk": "medium",
      "confidence": "high"
    },
    {
      "title": "The bbox lattice snap — authored 2026-09-06 for exactly this founder complaint — is wired into ONE of the seven layers",
      "detail": "`contextFetchBbox` (`contextBuildings.ts:678`) snaps the fetch centre to a 0.001° lattice and grows the half-extent by 0.55 × snap so the result is a strict superset. Its header states the defect verbatim: \"§CTX-FETCH-BBOX-LATTICE (L-12953, founder Córdoba 2026-09-06: 'the same layer is re-read 5–6 times per session … each triggering a full re-render')\" and names the four callers that hold four different points — including \"the 2D map CENTRE, which moves on every `moveend` while the user pans looking for a parcel\". It even says the lattice granularity was chosen to match the 2D map: \"deliberately the SAME granularity `SiteBoundaryMap2D` already declares as 'same area, skip the refetch'\".\n\nIT IS APPLIED TO BUILDINGS ONLY. `contextFetchBbox` appears at `contextBuildings.ts:2021, 2051, 2160, 2192, 2218, 2234` and in `scopeReadCeiling.ts:123`. Every other reader still computes its key from the raw caller point:\n\n  contextRoads.ts:164    const bbox = contextBboxAround(lat, lon, halfDeg);\n  contextWater.ts:736    const bbox = contextBboxAround(lat, lon, halfDeg);\n  contextParks.ts:185    const bbox = contextBboxAround(lat, lon, halfDeg);\n  contextLanduse.ts:168  const bbox = contextBboxAround(lat, lon, halfDeg);\n  contextRail.ts:104     const bbox = contextBboxAround(lat, lon, halfDeg);\n  contextTrees.ts:86     const bbox = contextBboxAround(lat, lon, halfDeg);\n  contextCanopyBaked.ts:266, contextFurniture.ts:140, CesiumViewport.ts:14496 (the sea read)\n\n`git show --stat 2cc9c888` confirms it: among the readers that commit touched `contextBuildings.ts` and nothing else (its `SiteBoundaryMap2D.ts` hunk is an unrelated §OFFICIAL-FOOTPRINTS filter).\n\nCONSEQUENCE FOR THE 2D MAP: its own guard key is `${c.lat.toFixed(3)},${c.lng.toFixed(3)}` (`SiteBoundaryMap2D.ts:2903-2905`, ≈111 m), while the read key is `toFixed(4)` (≈11 m) computed from the unsnapped centre. The two are an order of magnitude apart, so whenever the coarse guard lets a pan through, the fine read cache is GUARANTEED to miss. That is the mechanism the lattice exists to remove, still running for six of seven layers.",
      "isRegression": false,
      "suspectCommit": "2cc9c888 2026-09-06 feat(geospatial+parcel-law): the ground-sample batcher, the bbox lattice, and the Parcel Law GIS seam — wired the lattice into contextBuildings.ts only",
      "file": "apps/editor/src/ui/geospatial/contextRoads.ts",
      "line": 164,
      "quote": "    const bbox = contextBboxAround(lat, lon, halfDeg);",
      "fix": "Swap `contextBboxAround` for `contextFetchBbox` in the six readers' cache-key derivation (roads/water/parks/landuse/rail/trees), plus `contextCanopyBaked.ts:266`, `contextFurniture.ts:140` and `CesiumViewport.ts:14496`. ⚠ FETCH ONLY — the function's own header warns \"the near/far CLASSIFICATION still uses `contextBboxAround` at the TRUE centre ... Never use this to decide what to draw.\" Buildings already honours that split (`:2055`, `:2239` keep `contextBboxAround` for classification); the six readers must too wherever they crop or classify by extent. `contextFetchBboxLattice.spec.ts` already pins the superset property numerically, so the honesty half is covered; what is missing is a caller-set assertion that no reader derives a CACHE key from the unsnapped box.",
      "risk": "medium",
      "confidence": "high"
    },
    {
      "title": "Two comments assert the 2D map costs ZERO extra reads. Both have been false since 2026-09-07, and one of them is the module header a reader would trust first",
      "detail": "SiteBoundaryMap2D.ts:189-193 —\n  // §MAP2D-PASTEL (L-12938) — the OTHER baked context collections (roads, water areas +\n  // waterways, parks, landuse, rail, mapped trees + synthesised canopies) converted to the\n  // GeoJSON the pastel style's sources expect. It reads the SAME per-bbox memoised caches\n  // `contextLayerWarm.warmAllContextLayers()` fills for the 3D Site, so the 2D map adds NO\n  // network reads: one read, two renderers.\n\npastelContextGeoJson.ts:12-20 —\n  // ONE READ, TWO RENDERERS ... Every fetcher below is the SAME per-bbox-memoised function\n  // `contextLayerWarm.warmAllContextLayers()` fires the moment a geocode resolves, at the SAME\n  // bbox (`contextBboxAround(lat, lon, CONTEXT_BBOX_HALF_DEG)` ...). So on the normal path ...\n  // every call here is an in-memory cache hit and the 2D map costs ZERO new network reads.\n\nBoth were TRUE when written. `7d4dea4c` and `d1ecb2fe` made them false for roads/parks/rail/trees on 2026-09-07 without touching either file. The second comment even names the constant that is now wrong (`CONTEXT_BBOX_HALF_DEG`), and correctly flags the ONE layer it did get right — landuse, at `:246-249`: \"⚠ THE WIDE EXTENT, NOT THE DEFAULT ... asking for the default near bbox here would miss that key and start a SECOND network read — the exact cost this module exists to avoid.\" The author understood the mechanism exactly and applied it to one layer; the other four were later moved out from under him by a different lane.\n\nI am reporting this because an agent reading either comment will conclude the 2D read path is already free and stop looking — which is what makes the regression in finding 1 survivable.",
      "isRegression": false,
      "file": "apps/editor/src/ui/geospatial/pastelContextGeoJson.ts",
      "line": 12,
      "quote": "// ONE READ, TWO RENDERERS (STR-2D-SITE-MAP-CARTOGRAPHY §4 M1). Every fetcher below is the\n// SAME per-bbox-memoised function `contextLayerWarm.warmAllContextLayers()` fires the\n// moment a geocode resolves, at the SAME bbox (`contextBboxAround(lat, lon,\n// CONTEXT_BBOX_HALF_DEG)` — the readers compute it themselves from the same constants).",
      "fix": "Correct both comments in the same commit that fixes finding 1, and state which layers agree and which had to be aligned, so the next extent move has a checklist. Do not delete the claim — restate it as the invariant it should be, and back it with the consumer-set assertion proposed in finding 1's fix, so the sentence and the gate move together.",
      "risk": "none",
      "confidence": "high"
    },
    {
      "title": "NEGATIVE RESULT (c): a context re-render CANNOT clear the parcel highlight — the coupling runs the other way",
      "detail": "I looked for this specifically because it was the highest-value thing this dimension could find. It is not there.\n\n1. THE HIGHLIGHT SOURCE IS ONLY WRITTEN BY ONE FUNCTION. `refreshParcelHighlight` (`SiteBoundaryMap2D.ts:1567`) is the sole writer of `PARCEL_SELECT_SOURCE`; its nine call sites are all in the select/commit/clear path (`:1556, :2488, :2523, :2708, :2847, :3841`). No context loader touches it.\n2. THE CONTEXT LOADERS TOUCH ONLY THEIR OWN SOURCES. `loadContextBuildings` calls `live.setData(...)` on `CONTEXT_BUILDINGS_SOURCE` (`:2925`); `loadPastelContext` calls `src.setData(fc)` on the seven `PASTEL_SOURCES` (`:2971`). Nothing else.\n3. NOTHING TEARS THE STYLE DOWN. `map.setStyle` appears exactly once in the whole 4581-line file — `swapBasemap`, `:3568` — reachable only from the Map/Satellite buttons (`:3589-3590`), and it re-installs the highlight immediately (`installRingLayers` ends with `refreshParcelHighlight()` at `:1556`).\n4. Z-ORDER IS SAFE. `CONTEXT_BUILDINGS_SOURCE` and the pastel sources are declared in the style, so their layers exist before `installRingLayers` adds `PARCEL_SELECT_FILL_LAYER`/`_LINE_LAYER` (`:1530-1541`). The violet is on top of the 5442 footprints, not under them.\n5. THE 2D MAP SUBSCRIBES TO EXACTLY TWO THINGS: `site.parcel-boundary-set` (`:4240`) and the envelope-visibility authority (`:4266`). Neither is published by a context read.\n\nWHAT I DID FIND IS THE REVERSE COUPLING, and the headline lane should have it: committing a parcel fires `frameCommittedParcel('parcel-committed')` (`:4249`) → `map.fitBounds([[w,s],[e,n]], { padding: 60, maxZoom: 19, duration: 350 })` (`:2211`) → `moveend` → both loaders 350 ms later (`:4432`, `:4435`). If the new centre crosses a 0.001° cell — and a fitBounds onto a parcel almost always does — that is a six-layer cold read plus a full 5442-footprint re-filter landing ~700 ms after his click. It does not erase the highlight, but it is main-thread work sitting directly on the gesture he says is slow.\n\nThe one thing I could not rule out is a paint-opacity path: `applySiteHighlightEmphasis` (`:2054`) writes `set(PARCEL_SELECT_FILL_LAYER, 'fill-opacity', 0.10 * parcelFill)` and `set(PARCEL_SELECT_LINE_LAYER, 'line-opacity', parcelLine)`, where the multiplier is `SITE_HIGHLIGHT_RECEDE_FACTOR` whenever `getSiteHighlight()` names a different subject. That would DIM the highlight, not remove it, and it is driven by the site-card highlight authority rather than by any context read — but it is the only writer of the highlight's visibility other than the source itself, and it belongs to the headline lane, not this one.",
      "isRegression": false,
      "file": "apps/editor/src/ui/geospatial/SiteBoundaryMap2D.ts",
      "line": 1567,
      "quote": "    function refreshParcelHighlight(): void {\n        const src = map.getSource(PARCEL_SELECT_SOURCE) as GeoJSONSource | undefined;\n        if (!src) return;\n        if (!selectedParcel || selectedParcel.ring.length < 3) { src.setData(emptyFC()); return; }",
      "fix": "No fix in this dimension — report the negative to the headline lane so it stops looking at the re-read path, and hand it the two live leads: (a) `applySiteHighlightEmphasis` at `:2054` is the only thing that can dim the highlight without emptying it, driven by `getSiteHighlight()`; (b) the `frameCommittedParcel` → `fitBounds` → `moveend` → cold-read chain puts a six-layer read on the click itself, which is a latency lead for 'it took really long to fetch the parcel' even though it is not a highlight lead.",
      "risk": "none",
      "confidence": "high"
    },
    {
      "title": "(d) The sea walk is repeated once per EXTENT, not once per re-read — three extents, three refusals, and finding 1 removes one of them",
      "detail": "NOT repeated per re-read of the same bbox. `fetchContextWater` returns the memoised collection before any walk:\n\n  contextWater.ts:736-739\n    const bbox = contextBboxAround(lat, lon, halfDeg);\n    const key = bboxKey(bbox);\n    const hit = cache.get(key);\n    if (hit) return hit;\n\nand a REFUSED walk is cached like any other result (`cache.set` at `:790`, `:823`, `:851`), so the refusal costs its chains exactly once per key. `buildSeaMaskFromCoastline` (`:695`) is reached only from inside `fetchWaterForBbox`, below that guard.\n\nTHE THREE REFUSALS ARE THREE EXTENTS, NOT THREE RE-READS. Water is asked for at exactly three different half-degrees in this app:\n  • 0.008 (CTX_NEAR_HALF_DEG) — `pastelContextGeoJson.ts:244`, the 2D map, and the warm at `contextLayerWarm.ts:116`\n  • groundFetchHalfDeg(scope) ≈ 0.016 — `CesiumViewport.ts:14107`, the 3D ground water\n  • CONTEXT_SEA_HALF_DEG ≈ 0.10 — `CesiumViewport.ts:14298` and the warm at `contextLayerWarm.ts:118`, the sea mask\nThree keys, three walks, three refusals over 10 / 61 / 37 chains. That matches his log exactly and is CORRECT behaviour given three genuinely different bboxes — the coastline geometry inside each is different.\n\nSo (d)'s answer is: this is not waste from the re-read loop. It is the unavoidable cost of three extents. Closing finding 5 (lattice on water) would not merge them — they are different half-extents, not different centres. What WOULD reduce it is finding 1: aligning the 2D map to the ground extent collapses the 0.008 water read into the 0.016 one, taking three walks to two.",
      "isRegression": false,
      "file": "apps/editor/src/ui/geospatial/contextWater.ts",
      "line": 736,
      "quote": "    const bbox = contextBboxAround(lat, lon, halfDeg);\n    const key = bboxKey(bbox);\n    const hit = cache.get(key);\n    if (hit) return hit;",
      "fix": "Nothing to fix here directly — record that the sea walk is already per-extent-memoised so nobody spends a lane on it. It falls out of finding 1: aligning the 2D water read to `groundFetchHalfDeg()` takes the walk count from three to two. ⚠ Verify the 2D map still draws the sea correctly at the wider extent before landing that — the near read is what the pastel `water` source currently shows.",
      "risk": "low",
      "confidence": "high"
    },
    {
      "title": "(a) Full trigger census for the seven-layer re-read — nine entry points, and the terrain attach is NOT one of them",
      "detail": "2D MAP (`SiteBoundaryMap2D.ts`):\n  1. mount — `loadContextBuildings(true)` `:4431` + `loadPastelContext(true)` `:4434`\n  2/3. `map.on('moveend', () => loadContextBuildings(false))` `:4432` and `... loadPastelContext(false)` `:4435`. Debounce 350 ms (`:2938`, `:2990`); guard key `${c.lat.toFixed(3)},${c.lng.toFixed(3)}` ≈ 111 m (`:2903-2905`, `:2957-2960`). THIS IS THE DOMINANT TRIGGER while he pans hunting for a plot.\n  4. `swapBasemap('map')` `:3582-3584` — sets `ctxLastKey = ''` and `pastelLastKey = ''` then forces both. Guaranteed cold.\n  5. `frameCommittedParcel` → `fitBounds(duration:350)` `:2211`, from `:4249` (parcel-committed), `:4414` (mount), `:4555` (reparent) — each produces a `moveend` into 2/3.\n\n3D (`CesiumViewport.ts`):\n  6. `camera.moveEnd` `:4137` → `maybeRefreshContextOnPan` `:13711`. Thresholds: >1500 m from anchor, camera altitude <6000 m, 20 s cooldown, 1200 ms debounce. THE VISIBLE ONE — and per finding 2 it never stops.\n  7. `setContextScope` `:2335` → EIGHT loaders, every one `force:true`, at `contextBuildingsAt` (`:2352-2360`). Early-returns only if the resolved radius moved <0.5 m (`:2340`). Reached from `:2536`.\n  8. `renderFormaMassing` `:8371-8376` — six unforced ground-layer loads. Unforced, but per finding 4 the guard is dead until buildings land, so in practice these re-run.\n\nWARM: 9. `warmAllContextLayers` — one call site, `OnboardingStepController.ts:850`, once per location. Not a loop.\n\n⭐ THE TERRAIN ATTACH IS NOT A RE-READ TRIGGER, contrary to what the brief suspected. The settle block at `:9991-10017` RE-SEATS in place: `reseatContextPlacementsForBase()` and `reseatContextGroundFeaturesForBase()` (`:10134`) rewrite existing entities' heights with no network and no clear — the comment at `:9981-9989` records why (a `loadContextBuildings(force)` there was the L-635 Madrid blank). Three primitives DO rebuild — `rebuildContextTreesForBase` `:10097`, `rebuildContextFarTierForBase` `:13600`, `rebuildStreetLifeForBase` `:15252` — and trees does so via `loadContextTrees(at.lat, at.lon, true)` `:10124`, but at the SAME anchor, so the collection cache answers and only the primitive is rebuilt. Cost is GPU/CPU rebuild, not a read.\n\nSo: two loops (6 and, while panning, 2/3), one forced fan-out (7), one dead-guard fan-out (8), and a terrain path that is innocent.",
      "isRegression": false,
      "file": "apps/editor/src/ui/geospatial/SiteBoundaryMap2D.ts",
      "line": 4431,
      "quote": "        loadContextBuildings(true);\n        map.on('moveend', () => loadContextBuildings(false));\n        // §MAP2D-PASTEL (L-12938) — the baked context layers on the same cadence.\n        loadPastelContext(true);\n        map.on('moveend', () => loadPastelContext(false));",
      "fix": "No single fix — this is the map the other findings sit on. The ranked order to close them: finding 3 (one-token, kills the pinned reload's work) → finding 2 (kills the wake-up) → finding 1 (restores four warm-cache hits on the 2D map) → finding 4 (per-layer anchors) → finding 5 (lattice on the other six readers). Findings 3 and 2 together retire trigger 6; finding 1 makes trigger 2/3 cheap instead of cold.",
      "risk": "none",
      "confidence": "high"
    },
    {
      "title": "(c) What one 2D re-push actually costs in CPU — the work is enumerable, the milliseconds are not measured",
      "detail": "Every time the 2D centre key changes, this runs on the main thread. I am listing it because the read is cheap once warm (his own 4 ms) and the RENDER is where the money goes — but I did NOT measure it, and I am not going to put a number on it.\n\nBUILDINGS PUSH (`SiteBoundaryMap2D.ts:2920-2932`), over 5442 features:\n  • `collection.features.filter((f) => shouldDrawInPlan(f.properties.official))` — one full pass, new array\n  • `summariseOfficialFootprints(collection.features.map((f) => ({ official: f.properties.official })))` — a SECOND full pass, and a `.map` that allocates 5442 wrapper objects purely to be counted\n  • `live.setData(...)` — MapLibre structured-clones the whole FeatureCollection to its worker and re-tiles it\n\nPASTEL PUSH (`pastelContextGeoJson.ts:238-268` then `SiteBoundaryMap2D.ts:2966-2972`):\n  • six collection→GeoJSON conversions, each allocating a fresh 2-element array PER VERTEX: `lineFeature` `:71` does `coords.map((p) => [p[0], p[1]])`, `polygonFeature` `:83` the same. Roads alone is 2416 ways in his session; vertex count UNKNOWN.\n  • `synthesiseCanopies` `:257` — NOT memoised (no cache in `contextCanopySynth.ts`). Re-runs the full jittered-grid synthesis over every wood/forest polygon within `PASTEL_CANOPY_RADIUS_M = 900`, up to `PASTEL_CANOPY_MAX = 3000` canopies, and rebuilds a spatial bucket index over every mapped tree first (`:222-231`).\n  • seven more `src.setData(fc)` calls.\n\nThat is eight full GeoJSON re-uploads plus two 5442-element passes plus an un-memoised point synthesis, per centre-key change, on the thread that is also trying to service his parcel click. It is a plausible contributor to the felt sluggishness, and — unlike the reads — none of it is cached at all.",
      "isRegression": false,
      "file": "apps/editor/src/ui/geospatial/pastelContextGeoJson.ts",
      "line": 257,
      "quote": "        const synth = synthesiseCanopies(canopySources, {\n            site: { lat, lon },\n            maxCount: PASTEL_CANOPY_MAX,\n            maxRadiusM: PASTEL_CANOPY_RADIUS_M,\n            mappedTrees: trees.trees,\n        });",
      "fix": "MEASURE IT FIRST — bracket `loadPastelContext`'s `run()` and the `live.setData` at `:2925` with `performance.now()` and log alongside the existing counts, so the next reader has a number instead of this list. Then the two cheap wins are visible: memoise `synthesiseCanopies` on (site-cell, polygon-set) since it is deterministic by construction, and fold the `summariseOfficialFootprints` count into the same pass as the `filter` instead of a second traversal plus 5442 throwaway objects. ⛔ Do not skip the measurement and optimise from this list — the whole dimension exists because a read that logged 15 s turned out to be 4 ms of work and 15 s of queueing.",
      "risk": "low",
      "confidence": "medium",
      "costMs": "UNKNOWN — not instrumented. Enumerated above as deterministic work per push: 2 full passes over 5442 features, 6 collection→GeoJSON conversions with per-vertex allocation, 1 un-memoised synthesis of up to 3000 canopies, 8 setData uploads."
    }
  ]
}