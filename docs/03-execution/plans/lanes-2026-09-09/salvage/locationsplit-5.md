{
  "dimension": "8,037 ms WAITING, 23 ms OF WORK — is the ground-sample FIFO the bottleneck it was built to remove?",
  "summary": "THE QUEUE IS NOT THE BOTTLENECK, AND IT IS ALREADY OFF. `tileDedupGuaranteed` exists at HEAD (groundSampleBatcher.ts:217), is probed per flush (:406), and is wired to a live reading (`() => this.terrainTileMemoEntry !== null`, CesiumViewport.ts:10591). The memo it defers to stores the tile PROMISE synchronously before the download resolves (terrainTileMemo.ts:196-215), so it is a true in-flight join, not a resident-only cache — two concurrent flights genuinely cannot duplicate a download, and the `createMesh`-on-a-shared-instance hazard was already guarded (:247-262). So parks' 8,037 ms is NOT queueing. The label \"waiting for terrain (shared FIFO)\" (CesiumViewport.ts:10955) is measuring `now() - w0` around the whole of `sampleContextGroundsBatch` (:10820) and calling all of it FIFO.\n\nWHERE THE TIME ACTUALLY GOES — MEASURED, NOT INFERRED. I fetched Barcelona's REAL production terrain from app.pryzm.so today. Its layer.json says `\"maxzoom\": 10` and one tile row per level; the deepest tile under the city core, z10/1036/747, decodes to **48,825 triangles / 489 KB**. Cesium 1.143's `QuantizedMeshTerrainData.interpolateHeight` (Cesium.js:256875 / :256801) is a **LINEAR SCAN OVER EVERY TRIANGLE IN THE TILE, PER POINT** — no spatial index, `for (let i = 0, len = indices.length; i < len; i += 3)`. Reproduced verbatim against that exact tile: **194–245 µs per point**. The founder's single largest flight — 19,365 points, logged at 6,107 ms — is **3,765 ms of pure main-thread CPU in that scan alone**, ~62% of the flight, with zero network in it. His whole session's 42,376 points extrapolate to **~8.2 s of frozen main thread**.\n\n⛔ THE MISDIAGNOSIS HAS A SINGLE ROOT AND IT IS A HARD-CODED 14. `groundSampleBatcher.ts:456` reads `const tileLevel = this.opts.tileLevel ?? 14`, nothing ever passes `tileLevel`, and Barcelona's tileset stops at level 10. So the headline `Tiles: 12 distinct terrain tile(s) at level 14` is arithmetic over tiles that do not exist. The truth is printed **in the same log line, eight words later**, by the memo's own counter: *\"1 download(s) … 1 tile(s) resident.\"* One tile. Three lanes (L-12952, L-13077, L-13263) read the fictional 12, concluded \"downloads are the cost, points are free\", and wrote that into the comments that SET THE CAPS — `contextExtentBudget.ts:69` *\"So MORE FEATURES IN THE SAME BBOX COST NOTHING\"* — under which L-13175 added up to 24,000 conform-vertex samples per layer on 2026-09-07.\n\nTHE FIX IS THE MOVE THE MEMO ALREADY MAKES, ONE LAYER IN. A 128×128 uniform triangle-bucket index over the same production tile: **built in 16 ms, then 0.43 µs/point — 152× — and bit-identical on 4,000/4,000 points (max |Δ| = 0.00 m, zero one-sided).** 19,365 points go from 3,765 ms to 25 ms including the build.",
  "findings": [
    {
      "title": "Cesium's interpolateHeight is a LINEAR SCAN over all 48,825 triangles of the tile, per point — 3,765 ms of the founder's 6,107 ms flight, and NOT one byte of it is network",
      "detail": "`QuantizedMeshTerrainData.prototype.interpolateHeight` (Cesium 1.143.0, `node_modules/cesium/Build/CesiumUnminified/Cesium.js:256801`) dispatches to `interpolateHeight2` (:256875) or `interpolateMeshHeight3` (:256829). BOTH are a bare loop over the tile's whole index buffer with a per-triangle bbox test and a barycentric solve. No spatial index, no early-out beyond the first hit, nothing cached between calls.\n\nMEASURED AGAINST PRODUCTION, NOT A GUESS. `curl https://app.pryzm.so/api/context-tiles/terrain/barcelona/layer.json?v=L639k` → HTTP 200, `\"maxzoom\": 10`, `available` carries ONE tile row per level (z10 = x 1035..1036, y 747). I fetched both z10 tiles and decoded them with the repo's own `@here/quantized-mesh-decoder`:\n  · z10/1036/747 (contains the city core, lon 2.16) — 24,525 vertices, **48,825 triangles**, 489,721 B\n  · z10/1035/747 — 5,936 vertices, 11,661 triangles, 117,997 B\nI then transcribed `interpolateHeight2` + `Intersections2D.computeBarycentricCoordinates` (:255303) verbatim and ran them against that decoded tile, at the founder's own point counts:\n     493 pts →    82 ms (165 µs/pt)   [parks, Córdoba]\n     800 pts →   158 ms (197 µs/pt)   [parks areas, Barcelona]\n   3,555 pts →   870 ms (245 µs/pt)   [roads, Córdoba]\n  12,904 pts → 2,800 ms (217 µs/pt)   [trees, Barcelona]\n  15,781 pts → 3,269 ms (207 µs/pt)   [building centroids, Barcelona]\n  19,365 pts → **3,765 ms** (194 µs/pt) [the founder's largest logged flight]\nHis log times that same flight at **6,107 ms**. So ~62% of it is this scan. 42,376 points extrapolates to **8.2 s**. Node v24 with nothing else running is a FLOOR — a browser mid-startup is slower.\n\nWHY IT LANDS ALL AT ONCE: Barcelona has ONE tile at its deepest level, so `doSampling` (:208004) buckets every point of a flight into ONE `tileRequest`, and `createInterpolateFunction` (:208046) loops that flight's entire point list inside ONE synchronous closure. Every continuation on that path is a microtask, so the scan is one uninterruptible block: no frame painted, no click handled, and a pending `setTimeout` cannot fire inside it.\n\n⛔ THIS IS THE MEASUREMENT THAT REVERSES THE LANE. `groundSampleBatcher.ts:84` states the opposite as settled fact.",
      "blocksReveal": true,
      "costMs": "3,765 ms MEASURED for the one 19,365-point flight (against the real production tile); ~8,200 ms MEASURED-BY-EXTRAPOLATION for the session's 42,376 points. The founder's own log times that flight at 6,107 ms, so this is 62% of it.",
      "costBasis": "read-from-code",
      "file": "node_modules/cesium/Build/CesiumUnminified/Cesium.js",
      "line": 256875,
      "quote": "function interpolateHeight2(terrainData, u3, v3) {\n    const uBuffer = terrainData._uValues;\n    ...\n    for (let i = 0, len = indices.length; i < len; i += 3) {",
      "fix": "Give each decoded tile a triangle bucket index, installed exactly where the memo already installs `guardCreateMesh`. Add a sibling `guardInterpolateHeight(data)` to `TerrainTileMemo` (terrainTileMemo.ts, beside :247-262) that on first use builds a uniform u/v lattice over the tile's triangles (WeakMap-keyed on the data object, rebuilt if `_mesh` appears so the post-createMesh vertex set is honoured), then answers each query by testing only that cell's candidates with the IDENTICAL bbox predicate and the IDENTICAL barycentric formula. If no candidate matches, DELEGATE to the original `interpolateHeight` — exact-or-original, never a different answer and never a fabricated one. MEASURED on the same production tile: 128x128 lattice, all 16,384 cells occupied, mean 8.6 tri/cell, worst 24; **built in 16 ms**, then **0.43 µs/point**; 19,365 points = 8.4 ms + 16 ms build = 25 ms vs 3,765 ms — **152x**. Equivalence over 4,000 points: 4,000 identical, 0 differing, max |Δ| 0.00e+0 m, 0 one-sided — which is the §CONTEXT-DATA-HONESTY / C57 §1.5 bar met by construction, not by promise. ~100 lines, pure, no Cesium import, structurally typed and testable against a fake — the same shape as `terrainTileMemo.ts` itself. ⚠ I checked for an existing solver first: `@pryzm/spatial-index`'s `SpatialGrid` is a THREE-coupled 3-D Box3 grid for BIM elements and is the wrong shape here; reusing it would drag THREE into the geospatial path for a 2-D uv lattice.",
      "risk": "low",
      "confidence": "high"
    },
    {
      "title": "The 'Tiles: 12 … at level 14' headline is fiction — the tileset's deepest level is 10 — and the memo's own counter in the SAME log line says 1 download, 1 tile resident",
      "detail": "`groundSampleBatcher.ts:456` is `const tileLevel = this.opts.tileLevel ?? 14;`. `grep -n tileLevel CesiumViewport.ts` returns exactly two hits, both inside the `onFlush` log template (:10613, :10624) — **nothing ever passes `tileLevel`**, so every flush is attributed at a hard-coded 14. `countDistinctTerrainTiles` then does honest arithmetic on a level that does not exist for this tileset: at 14 a tile spans 360/32768 = 0.010986°, so Barcelona's ~0.14° extent buckets into ~13 — which is where '12 distinct terrain tiles' comes from. At the tileset's real deepest level 10 a tile spans 0.17578°, so the same extent is **1–2 tiles**.\n\n⭐ AND THE LOG ALREADY KNEW. The very next clause of the same console line prints the memo's counter, which counts REAL `requestTileGeometry` calls: the founder's own quoted line reads *\"Tile memo: 1 download(s) + 8 hit(s) of 9 tile request(s), 1 tile(s) resident\"*. **One download. One tile.** Two numbers, twelve-fold apart, printed side by side in one sentence — and three consecutive lanes quoted the wrong one.\n\nTHE CONSEQUENCE IS NOT COSMETIC. L-13077's own header opens with *\"flight 1 … Tiles: 12 distinct terrain tile(s) at level 14 … flight 2 … 12 … The SAME 12 level-14 tiles were fetched TWICE, five seconds each.\"* They were not: there was one ~489 KB tile, and the five seconds was the triangle scan measured in finding 1. Every conclusion downstream of that reading — 'the downloads are the cost, the points inside a tile are free' — inherits the error.",
      "blocksReveal": false,
      "costMs": "0 ms of runtime cost on its own (it is a log string). Its cost is the ~8,200 ms of finding 1, which three lanes could not see because this number told them the bill was elsewhere.",
      "costBasis": "read-from-code",
      "file": "apps/editor/src/ui/geospatial/groundSampleBatcher.ts",
      "line": 456,
      "quote": "                    const tileLevel = this.opts.tileLevel ?? 14;",
      "fix": "Attribute at the level the flight actually sampled, or print nothing. The number is reachable at `sample()` time — `provider.availability` / the layer.json `maxzoom` the attach already read (`terrainCoverage.terrainTilesetCoversSite` fetches that JSON) — so pass `tileLevel` from the provider instead of defaulting. Better still, DELETE the derived count and print only `TerrainTileMemo.stats.downloads`: it is measured rather than computed, it cannot go stale against a re-bake, and it is already in the same line. A gate whose two counters disagree by 12x and are printed adjacently is the 'same rule, two implementations' shape; keep ONE.",
      "risk": "none",
      "confidence": "high"
    },
    {
      "title": "'Points inside a tile are free' is written into the two files that SET THE BUDGETS — and it is measurably false at 194–245 µs/point",
      "detail": "Two live comments state the false premise as settled, and both are load-bearing on numbers that ship:\n\n1. `groundSampleBatcher.ts:84` — *\"every point inside that tile is then answered by `interpolateHeight` on the ALREADY-DOWNLOADED mesh — free.\"* The block above it (:86-92) draws the operative conclusion: *\"'sample fewer points / drop the drape density' buys approximately NOTHING, because the tile set is fixed by the BBOX, not by how densely you sample inside it.\"*\n\n2. `contextExtentBudget.ts:66-74` — the file that chooses the caps — *\"CHEAP — ground sampling … interpolates every point inside it for free … So MORE FEATURES IN THE SAME BBOX COST NOTHING, and a modestly wider bbox costs a handful of extra tiles at level ~14\"*. Its 'level ~14' is the same fiction as finding 2, and its closing line, *\"⛔ `GroundSampleBatcher` serialises flights with a FIFO mutex … `maxConcurrentFlights` MUST stay 1 and this file cannot move it\"* (:73-74), was made stale yesterday by L-13263.\n\nWHAT WAS PRICED ON IT. `GROUND_DRAPE_MAX_VERTEX_POINTS_PER_LAYER = 24_000` (groundFeatureSeat.ts:663) landed **2026-09-07, commit c1549505** (`git log -S`), two days before this log. Its own rationale (:648-662) reasons only about honesty — *\"⛔ WHAT THE BUDGET MAY NOT BUY\"* — and never about CPU, because at the time points were believed free. Three layers can bid for it (parks :14558, landuse :14681, water areas :14122; roads/rail are corridors and ask none, sea passes `conform:false`), on top of `GROUND_DRAPE_MAX_PROBE_POINTS_PER_LAYER = 9000` each. At 194–245 µs/point that ceiling is a **4.7–5.9 s bill per layer**.\n\n⛔ THE ORIGINAL REASONING WAS RIGHT WHEN WRITTEN AND MUST NOT BE DISMISSED. Under Córdoba's download-bound regime — parks 493 pts and roads 3,555 pts both 14.1 s — the cost genuinely was flat in point count, and refusing to decimate the drape was the correct call. The memo (L-13077) removed the download. The regime flipped underneath the comment and nobody re-measured.",
      "blocksReveal": false,
      "costMs": "Sets the size of finding 1's bill. The ceilings these comments justify are 9,000 probe + 24,000 vertex points per layer = up to 33,000 points x 194–245 µs = 6.4–8.1 s per layer at the cap. The founder's actual session asked 42,376 points across 14 callers.",
      "costBasis": "read-from-code",
      "file": "apps/editor/src/ui/geospatial/contextExtentBudget.ts",
      "line": 69,
      "quote": "//     free. The founder's run resolved 9,397 heights in ONE flight. So MORE FEATURES IN THE SAME",
      "fix": "Correct all three comments IN PLACE with the measured number (194–245 µs/point on Barcelona's real z10 tile, 48,825 triangles) once finding 1 lands, and state the new invariant: points are cheap BECAUSE the tile carries a bucket index, not because Cesium makes them free. ⛔ DO NOT lower `GROUND_DRAPE_MAX_VERTEX_POINTS_PER_LAYER` as the fix — that trades away the conforming drape the founder asked for on 2026-09-07 (L-13175, 'appeared in fragments — not organic shape like it is the terrain') to buy something the index gives for nothing. Lowering it is the correct move ONLY if finding 1 is deferred, and it should be recorded as a temporary trade with its own row, never as the answer. Also delete `contextExtentBudget.ts:73-74`'s 'MUST stay 1 and this file cannot move it' — L-13263 moved it yesterday.",
      "risk": "none",
      "confidence": "high"
    },
    {
      "title": "§DRAPE-COST-ATTRIBUTION blames the FIFO for time the FIFO did not take — it measures the whole sample and labels all of it queueing",
      "detail": "The founder's most damning line — *\"8,037 ms waiting for terrain (shared FIFO), 23 ms own work\"* — is produced at `CesiumViewport.ts:10955` from a `waitMs` accumulated at :10820 and :10891:\n\n    const w0 = now();\n    try { await this.sampleContextGroundsBatch(batch); } catch { }\n    waitMs += now() - w0;\n\nThat is the ENTIRE `request()` round trip: the 120 ms coalescing window, plus any join onto another caller's in-flight flush, plus the tile download, plus the multi-second triangle scan of finding 1. The FIFO wait is one term of four, and at HEAD it is normally zero.\n\nAT HEAD THE FIFO IS ALREADY RELEASED. `runFlush` probes `tileDedupGuaranteed()` before it takes its turn (`groundSampleBatcher.ts:406-407`), and when it answers true it increments `_flightsRunConcurrently` and **skips `awaitTurn` entirely** (:408-409). The probe is wired to `() => this.terrainTileMemoEntry !== null` (CesiumViewport.ts:10591), which is true from the first sampler call onward.\n\nSO WHY DID PARKS WAIT 8 s FOR 23 ms OF WORK? Because `request()` resolves on the WHOLE flush, not on the caller's own points: `scheduleFlush()` returns the shared flush promise to every caller inside the window (`groundSampleBatcher.ts:340-341, :348`), and `request()` also `Promise.all`s any other flush that already holds one of its points (:305-306, :312). Parks' ~800 probe points were merged into a flight that also carried landuse's and water's conform vertices, and it waited out that flight's triangle scan. **The convoy is real; the queue is not.** Under a download-bound regime the convoy was free — one download served everybody. Under a CPU-bound regime it couples the cheapest caller to the most expensive one.",
      "blocksReveal": true,
      "costMs": "The 8,037 ms is real elapsed time; what is wrong is its attribution. Of it, ~0 ms is FIFO (the FIFO is skipped once the memo exists), 23 ms is parks' own work, ≤120 ms is the coalescing window, and the remainder is the shared flight — which finding 1 measures as 62% triangle scan.",
      "costBasis": "founder-log",
      "file": "apps/editor/src/ui/geospatial/CesiumViewport.ts",
      "line": 10955,
      "quote": "      `§DRAPE-COST-ATTRIBUTION: ${waitMs.toFixed(0)} ms waiting for terrain (shared FIFO), ` +",
      "fix": "Split `waitMs` into the three terms the caller can act on and label each honestly — `window` (time to flush fire), `queued` (time inside `awaitTurn`, which the batcher can report), `sampled` (time inside `opts.sample`) — and stop the word 'FIFO' appearing next to a number that is not the FIFO. This is the line that framed a CPU problem as a queueing problem for a whole lane; it costs nothing to fix and it is the difference between the next reader measuring and guessing. ⛔ DO NOT rebuild the batcher to resolve callers individually: once finding 1 lands the entire flight is ~25 ms and the convoy is worth nothing. Resolving per-caller means splitting the flush by point-set and is real complexity for a latency that will no longer exist.",
      "risk": "none",
      "confidence": "high"
    },
    {
      "title": "`flightsRunConcurrently` — the counter L-13263 added yesterday to say WHICH regime a run was in — is computed, exported, asserted in tests, and never printed",
      "detail": "L-13263 added the counter for one stated purpose: *\"§TILE-DEDUP-RETIRES-THE-FIFO (L-13263) — how many flushes skipped the FIFO because the tile memo guarantees each tile downloads once. **0 ⇒ this run serialised, as before.**\"* (`groundSampleBatcher.ts:164-166`).\n\n`grep -rn flightsRunConcurrently --include=*.ts apps/ packages/ plugins/ src/` returns SIX hits and every one is either the declaration, the increment, the stats getter, or a spec assertion (`groundSampleBatcher.spec.ts:448, :493`). It appears NOWHERE in the `onFlush` console template (CesiumViewport.ts:10614-10653), which prints `roundTrips`, `requests`, `pointsRequested`, `pointsSampled`, `pointsFromCache`, `pointsJoinedInFlight`, `pointsResolvedByAnEarlierFlight`, `deferredFlushes` and `maxConcurrentFlights` — everything except the one field that answers 'did the FIFO run?'.\n\nThat is precisely why this dimension was briefed as a queue problem: the founder's log physically cannot say whether L-13263 took effect on his run, and the only field that reads on it, `maxConcurrentFlights`, still prints the obsolete assertion *\"(MUST be 1 — two calls in the air re-download the same tiles)\"* (:10647) — a sentence L-13263 falsified yesterday and left standing in the log it disagrees with.",
      "blocksReveal": false,
      "costMs": "0 ms of runtime. The cost is diagnostic: one audit lane framed around a queue that was already off.",
      "costBasis": "read-from-code",
      "file": "apps/editor/src/ui/geospatial/CesiumViewport.ts",
      "line": 10647,
      "quote": "                `${s.maxConcurrentFlights} (MUST be 1 — two calls in the air re-download the same ` +",
      "fix": "Print `flightsRunConcurrently` in the `onFlush` line beside `deferredFlushes`, and replace the '(MUST be 1 …)' clause with the regime the run was actually in — 'N flush(es) ran concurrently (tile de-dup guaranteed below), M serialised'. A counter minted to name the regime and then omitted from the only place the regime is reported is the same defect shape as the level-14 tile count: the instrument exists and does not reach the reader.",
      "risk": "none",
      "confidence": "high"
    },
    {
      "title": "`tileDedupGuaranteed` reads a field that only `sample()` can set, so the first flush of every provider generation still serialises",
      "detail": "The probe is `tileDedupGuaranteed: () => this.terrainTileMemoEntry !== null` (`CesiumViewport.ts:10591`). `terrainTileMemoEntry` is assigned in exactly one place — `samplingTerrainProvider()` at :10686 — and that method is reached only from INSIDE the batcher's own `sample` callback (:10607) and the four bare single-point `Cesium.sampleTerrainMostDetailed` sites (:4832, :9915, :11255, :11869). So the field is null until a sampler call has already begun.\n\nCONSEQUENCE, stated at its true size: the FIRST flush of a session probes false and serialises — harmless, because nothing is ahead of it. The same happens again after every `invalidateTerrainTileMemo()` (:5654 on `detachBakedTerrain`, :18856 on project switch) and after any `viewer.terrainProvider` change, until the next `sample()` re-creates the view. On the founder's Barcelona start-up this costs approximately nothing.\n\n⚠ I am recording it because the comment above the probe claims a stronger property than the code has: *\"⚠ A READING, NOT A CONSTANT. It asks whether a memo is installed RIGHT NOW\"* (:10587-10590). It does not — it asks whether a memo has EVER been installed for the current provider, which is a different question, and it answers 'no' during exactly the window where a duplicate download would be possible. That happens to be the safe direction, so the guard is correct; the comment is what is wrong.",
      "blocksReveal": false,
      "costMs": "Approximately 0 ms on this run — the serialised flush is the first one and has nothing ahead of it. Bounded above by one flight (~3.8 s) only if two flushes were already queued before the first sampler call returned, which the 120 ms window makes unlikely.",
      "costBasis": "read-from-code",
      "file": "apps/editor/src/ui/geospatial/CesiumViewport.ts",
      "line": 10591,
      "quote": "      tileDedupGuaranteed: () => this.terrainTileMemoEntry !== null,",
      "fix": "Either install the memo eagerly when the provider is attached (call `samplingTerrainProvider(provider)` once at the end of the terrain-attach path, so the probe is true before the first flush), or leave the behaviour exactly as it is and correct the comment to say what the field means: 'a memo EXISTS for the provider currently attached'. Do not make the probe optimistic — a flush that runs with no memo genuinely must serialise, and this is the one place where being wrong re-creates L-12952.",
      "risk": "none",
      "confidence": "high"
    },
    {
      "title": "The whole terrain sample is one uninterruptible microtask drain, so 'the split mounted at 1.5 s' does not mean the user has a usable split",
      "detail": "Every step from `request()` to the last interpolated point is a promise continuation: `awaitTurn` → `opts.sample` → `sampleTerrainMostDetailed` (`Cesium.js:208115`) → `await Promise.all(promises)` → `sampleTerrain` → `doSampling` → `drainTileRequestQueue` → `requestTileGeometry(...).then(createInterpolateFunction(tileRequest))`. Microtasks drain to completion inside one task; the browser paints and dispatches input BETWEEN tasks. The only real task boundary on that path is the tile's network response — and once `TerrainTileMemo` is serving an already-resolved promise, even that is gone.\n\nSo the founder's session is not 8.2 s of gentle background load. It is a handful of frozen blocks, the largest **3,765 ms MEASURED**, during which nothing repaints: not the 2D pastel cadastral map, not the 'STEP 2 OF 4 — DRAW YOUR PLOT' chrome, not a click on a plot. A pending `setTimeout` cannot fire inside one either — including, in principle, the 1,500 ms reveal deadline in `siteRevealSequence.ts:325-330`.\n\nWHERE IT SITS RELATIVE TO THE REVEAL, precisely. `reveal:split-mounted` fires because the deadline expired, not because content arrived (`awaitContentReady` is the buildings fetch and marks `reveal:content-ready` at t+118574, 5.5 s AFTER the mount at t+113012). So the split frame appears at 1.5 s with an EMPTY 3D pane. The founder's screenshot is the pane POPULATED, and populating it is gated on these awaits: buildings are placed only after `await this.sampleContextGroundsBatch(nearGroundCentroids)` (`CesiumViewport.ts:12271`, 15,781 centroids → 3,269 ms measured) and canopies only after the trees batch (`:15095`, 12,904 points → 2,800 ms measured), each immediately before its `clearContext…()` swap.\n\n⭐ THIS IS WHY THE HONEST NUMBER IS NOT 6 s. The split is MOUNTED at 5.9 s from geocode:start and USABLE several seconds later, and the gap is dominated by main-thread scan time, not by network.",
      "blocksReveal": true,
      "costMs": "~3,765 ms in the single worst block and ~8,200 ms in total, MEASURED — all of it after `reveal:split-mounted` and all of it before the pane the founder screenshotted is populated.",
      "costBasis": "read-from-code",
      "file": "apps/editor/src/ui/geospatial/CesiumViewport.ts",
      "line": 12271,
      "quote": "    await this.sampleContextGroundsBatch(nearGroundCentroids);",
      "fix": "Finding 1 removes ~99% of this: 3,765 ms becomes 25 ms and there is nothing left to chunk. Do NOT ship chunking as the primary fix — slicing `sample()` into 2,000-point calls with a macrotask yield between turns one 3.8 s freeze into ten 380 ms freezes, which is a feel change, not a throughput change, and it is exactly the 'hide latency behind a frame' move the founder ruled out. It is worth ~15 lines ONLY as a bounded guard AFTER the index lands, so that a future city with a denser tile or a bigger scope degrades into visible progress instead of a frozen tab; it is cheap then because the memo makes the extra `sampleTerrainMostDetailed` calls free at the tile level.",
      "risk": "low",
      "confidence": "high"
    }
  ],
  "whatIcouldNotEstablish": "1. THE FOUNDER'S BROWSER NUMBER, as opposed to mine. Every timing here is Node v24.15.0 on this machine against the real production tile, running nothing else. A browser mid-startup — with Cesium rendering, a PMTiles decode, and a failed `persistence.client.delete` in flight — will be SLOWER, not faster, so 3,765 ms is a floor. I could not run the app (localhost dev is unusable per the standing memory) and I did not deploy anything.\n\n2. WHETHER THE FOUNDER'S RUN ACTUALLY SKIPPED THE FIFO. Finding 5 is the reason: `flightsRunConcurrently` is never printed. I established from the code at HEAD that it should have skipped, and that the memo is a genuine in-flight join, but the log he supplied cannot confirm it. The single cheapest thing to add before the next run is that one field.\n\n3. HOW HIS 42,376 POINTS SPLIT BETWEEN `pointsSampled` AND `pointsFromCache`. The brief quotes `pointsRequested`; cached points cost nothing. So ~8.2 s is an upper bound on the session total, while the 3,765 ms for the single 19,365-point flight is anchored on a `sampled` figure from his own log and is not affected.\n\n4. THE EXACT TRIANGLE COUNT AT OTHER SITES. 48,825 is measured for Barcelona z10/1036/747 only. `emitTileChain` bakes the finest level at `baseErrM = 0.5`, so I expect the same band city-wide (my synthetic sweep at that error gave 21,696–48,825 triangles, i.e. 132–431 µs/point), but a flat coastal tile can be denser still — my flat-plain fixture produced 65,610 triangles, because MARTINI spends its budget where the error bound bites. A flat city may therefore be WORSE, not better, which is counter-intuitive and worth one measurement before anyone assumes relief is the driver.\n\n5. THE SECOND-ORDER COST OF `availability.computeMaximumLevelAtPosition`, which `sampleTerrainMostDetailed` calls TWICE per position (Cesium.js:208126 and :208165 — once to bucket by level, once after the await to detect availability that changed). For Barcelona's layer.json that quadtree holds one rectangle per level across 11 levels, so I expect ~1–3 µs/point, i.e. ~0.17 s across the session. ESTIMATED, not measured — I did not build a TileAvailability fixture. It is not a finding at that size, but on a national tileset with a large `available` array it would grow, and the second pass is pure waste on a static bake.\n\n6. WHETHER FIXING FINDING 1 FULLY EXPLAINS THE FOUNDER'S 6,107 ms. I attribute 3,765 ms; the residual ~2.3 s is the 489 KB tile download, the availability passes, and Cesium's own promise plumbing. I did not decompose that residual, so 'the flight drops to ~25 ms' is wrong — expect it to drop to roughly the download time plus the residual, i.e. ~2.3 s for the FIRST flight and near-zero for every later one, because the memo already holds the tile.\n\n7. I EDITED NOTHING. No file in the repo was changed; the two benchmark scripts and the fetched tiles live only in the scratchpad (`interpBench.mjs`, `interpSweep.mjs`, `bcnReal.mjs`, `t_1035_747.terrain`, `t_1036_747.terrain`, `bcn-layer.json`)."
}