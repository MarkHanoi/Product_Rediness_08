{
  "surviving": [
    "The drop-down already exists, is hidden by default, and is DISABLED in exactly the state the founder is in",
    "The paragraph is `capVerdict`, one function, one production caller — and the founder's exact sentence is reproducible from it",
    "WHAT MUST SURVIVE THE COLLAPSE: this caption is the ONLY UI surface anywhere that says features are being dropped",
    "There IS a disclosure primitive to reuse — `buildPanelFold`, and its `setSummaryNote` was built for this exact obligation",
    "(e) YES — remember it per session, not persisted; and the CURRENT local forgets it on a pane swap, which is a defect on its own",
    "At the founder's scope the panel is a ~300 × 126 px pointer-capturing float over the 3D Site — and this view is now a face-drag surface",
    "CORRECTION TO THE BRIEF: there is no checkbox and no radio — those are the ▭ and ◯ shape-toggle glyphs, and being misread is itself the finding"
  ],
  "refuted": [
    "The ⓘ hit target is ~14 × 12 CSS px — under half the 24 × 24 minimum C43 names, on the control he is being asked to click"
  ],
  "reasons": [
    "SURVIVING (fully confirmed, and it is the load-bearing finding of the dimension). Every cited line reads as claimed at HEAD 7b1b9291: `caption` declares `display:'none'` at SiteScopeSlider.ts:462 with the comment 'Starts folded.' at :460-461; `paintCaption` at :508 computes `const open = pinned || captionOpen;` (:510) and sets `captionToggle.disabled = pinned;` (:514); `captionNeedsAttention` (:291) returns true on `if (verdicts.some((v) => !v.complete)) return true;` (:295). `git show afc5ce4c` → 2026-09-08, 'feat(site/§SCOPE-PANEL-50 L-13189): the scope panel folds to half its footprint'. Two production callers confirmed: SiteAuthoringPaneShell.ts:643 and :644, no rival mount. At r=5755 both the buildings and trees verdicts are incomplete (proved independently in finding 2), so the caption is force-open and the ⓘ is greyed. The spec pins this deliberately — siteScopeSlider.spec.ts:617 '⛔⛔ a reading that names a LIMIT cannot be folded away — not even by clicking' asserts `toggle.disabled === true` and that two clicks leave it open, and the biconditional arm at :677+ pins `captionNeedsAttention` to `completenessCaption` across 8 cases. The finding's conclusion — this is a REVERSAL of a rule with a test guarding it, not a missing feature — is correct and is the thing the implementing lane must be told.",
    "SURVIVING, and the most strongly verified finding in the set. `capVerdict` is at scopeClip.ts:382-398, and the quoted line `complete at a scope of ~${Math.round(completeRadiusM!)} m (density ` is exactly at :396. I reproduced ALL FOUR of the founder's numbers from the formula rather than trusting the audit: trees 5755×√(10000/24138) = 3704.2 → '~3704 m' ✓ and density 24138/(π·5755²)×1e6 = 232.0 → '232 / km²' ✓; buildings 5755×√(27694/64044) = 3784.4 → '~3784 m' ✓ and 64044/(π·5755²)×1e6 = 615.6 → '616 / km²' ✓. Four independent matches to the founder's screenshot text. That also establishes a fact the audit only assumed: the scope was a CIRCLE at r=5755 (a rectangle's `scopeAreaM2` would not give 232), matching his log line. Callers verified by grep: `capVerdict` has exactly one production call site (CesiumViewport.ts:2589) and `scopeCapReports` exactly two `set` writers ('buildings' :12574, 'trees' :15054) — which is why only two layers ever appear. Two trivial imprecisions, neither material: the audit says buildings computes to 3785 (it is 3784.4, which rounds to the printed 3784 exactly — there is no 0.03 % discrepancy to explain), and it omits that `scopeCapReports` also has a `delete('buildings')` at :13787.",
    "SURVIVING on its central claim, which I re-derived rather than accepted. `getScopeCapVerdicts` has exactly ONE production consumer: SiteAuthoringPaneShell.ts:284 `getCapVerdicts: () => host()?.getScopeCapVerdicts?.() ?? []` → SiteScopeSlider.ts:578 → `completenessCaption` → this one `<div>`. A repo-wide grep for drop reporting ('dropped by the cap', 'dropped by the nearest-first') finds the only other producers in contextBuildings.ts:2139/:2266 and contextStreetLifeRender.ts:329 — both `console.log` template strings, not UI. The COMPLETE tick's `title` (SiteScopeSlider.ts:566-573) carries 'Complete to ~X m (bound by Y)' — the mark, never a drop count, exactly as claimed. So hiding the paragraph outright does make a plate missing 36 350 buildings and 14 138 trees look complete with nothing on screen contradicting it. One citation error, immaterial to the substance: the amber precedent is at `apps/editor/src/ui/site/envelopeCostSection.ts:313-320` (the '⚠ AMBER AND 10 px, NOT PURPLE' comment at :313, `FOLD_SUMMARY_STYLE` at :319), not `envelopeCostSection.ts:355` and not under `ui/analysis/`.",
    "SURVIVING as a reuse prescription; ONE sub-claim REFUTED and ONE cost UNPRICED. Confirmed: panelFold.ts is 147 lines, landed a7e93fb3 on 2026-09-08 (same day as afc5ce4c); `setSummaryNote` is declared at :68-69 with the doc comment quoted verbatim; the note element at :116 carries 'The one fact that survives collapsing (C58 §1.2)'; the UA-marker rationale is at :102-104 and the no-px/inherit rule at :87-96; two existing callers, siteEnvelopeTool.ts:253 and parcelLawEnvelopeAuthoring.ts:880/:899. The import-edge check is correct: parcelLawQuestionGroup.ts's only top-level import is `trace` from '@opentelemetry/api' at :54, so the edge drags in no Parcel Law machinery. ⛔ REFUTED SUB-CLAIM: 'the scope panel's current ⓘ IS ALREADY THE RIVAL C115-91 FORBIDS.' C115-91 (C115-PARCEL-LAW-PANEL-INFORMATION-ARCHITECTURE.md:1675) reads 'C115-90 The panel MUST have ONE disclosure primitive. C115-91 It MUST NOT be a sixth mechanism' — 'the panel' there is the Parcel Law panel, whose five rivals §11 enumerates. The scope panel is not that surface, and the Site surface already carries TWO disclosure implementations with TWO separate session maps (`buildPanelFold` over `openState` in parcelLawQuestionGroup.ts:410, and the string-built `<details>` in envelopeCostSection.ts:338 over `envelopeCardFoldIsOpen` in envelopeCardSections.ts:160). So the ⓘ is a third site-panel disclosure, not a contract violation. Reuse is still the right call; the citation is not load-bearing and should not be quoted at the founder as a contract breach.",
    "SURVIVING as a defect report; the PRESCRIBED FIX is partly REFUTED. Confirmed: `let captionOpen = false;` at SiteScopeSlider.ts:485 is inside `mountSiteScopeSlider`; the shell mounts twice (:643 LEFT, :644 RIGHT) and BOTH roots live in the DOM simultaneously, only one displayed; paneViewModel.ts:405 states the singleton 'never clones — see assignViewToPane' and :411 puts site-3d in the RIGHT pane by default; `questionGroupFoldIsOpen`/`setQuestionGroupFoldOpen` are at parcelLawQuestionGroup.ts:432/:438 over the module map at :410, with the 'deliberately NOT persisted' rationale at :409-408 exactly as quoted; `buildPanelFold` reads it at :83 and writes at :133. ⛔ REFUTED PART OF THE FIX: 'Use ONE id for both panes — `site-scope-completeness`, NOT `site-scope-completeness-${paneId}`.' panelFold.ts:53-54 declares the id 'Becomes `data-testid` AND the key in the shared session fold memory' — one field, two jobs. Because both sliders are mounted at once, a shared id mints DUPLICATE `data-testid` attributes in the live DOM, which every other testid in this file (all pane-suffixed) avoids and which C115-92's 'a stable data-testid' clause exists to prevent. Sharing the memory across panes therefore requires panelFold to SPLIT key from testid — real work the finding prices at zero ('you would add nothing'). ⚠ SCOPE NOTE: the pane-swap loss is only reachable in the NOT-pinned (reassurance) state, since `captionOpen` has no effect when `pinned` is true — i.e. never in the state the founder is actually in.",
    "SURVIVING on mechanism, with the severity label qualified. Verified by grep: the ONLY `pointerEvents` declaration in the whole file is on the mark tick at :447 — the root's style block (:322-356) has no such key, so the box is default `pointer-events:auto` and does swallow pointer events over the Cesium canvas beneath it. The box arithmetic reproduces exactly from the declarations: border 1 + padding-top 7 (:349) + head 20 (shapeWrap = 12px line at :383 + 3px padding ×2 + 1px border ×2) + head margin-bottom 4 (:359) + track 16 (input height 14 + margin 0 at :435-436, trackWrap padding-bottom 2 at :426) + padding-bottom 6 + border 1 = 55 px folded, width `min(300px, …)` at :348. Face drag on this surface is real and shipped — siteEnvelopeTool.ts:264-269 records §ENVELOPE-FACE-DRAG-PER-LEVEL / L-13236 as a CORRECTION to copy that said it was not built, and the gesture lives in spaceEnvelopeDragSurface.ts / spaceEnvelopeFaceDragController.ts. Two qualifications: the caption content box is 278 px not 280 (the 1px borders are not subtracted), immaterial; and under the does-it-block-the-user lens this blocks INPUT in one ~300×126 rect at the bottom-centre of one pane — it blocks no frames, adds no wall-clock, and is not the 45 s the founder is complaining about. The [BLOCKS THE USER] tag is defensible only in that narrow, regional sense, and the finding says so itself.",
    "SURVIVING, and correctly self-scoped as out of lane. Verified: the segmented control is at :371-386, `b.textContent = glyph` with the literal pairs `['rectangle', '▭', 'Rectangular scope']` (:372) and `['circle', '◯', 'Circular scope']` (:373), styled `font: '600 12px/1 system-ui, sans-serif', padding: '3px 7px'` at :383, with `aria-pressed` set in paint() at :558-561. A grep for 'checkbox' across apps/editor/src/{ui/geospatial,engine/views,ui/site} returns hits only in ViewSwitcherPill.ts:159/:357 and a Cesium comment at CesiumViewport.ts:4010 — none in this panel; the only `type = 'radio'` in apps/editor/src is roomInteriorElevationModal.ts:128. Position confirmed: `bottom:'14px'` (:325), `left:'50%'` (:326), `transform:'translateX(-50%)'` (:329), pinned to C59 §2.10.3 clause 4 in the comment at :327-328; `siteAuthoringDefaultLayout` puts site-3d in the RIGHT pane at paneViewModel.ts:411. Both brief descriptions resolve to the same single control.",
    "REFUTED on its normative claim; the measurement is right but the authority is not. The physical reading is correct — `captionToggle` declares `padding: '0 1px'` and `font: '600 12px/1 system-ui, sans-serif'` at :413-414, giving a ~12 px line box with no vertical padding. But C43-ACCESSIBILITY.md:481 does NOT name 24×24 as a minimum PRYZM must meet: I traced the enclosing heading and it is '## §10 — Open questions (DRAFT-stage)', item 6, whose full text is 'WCAG 2.2 new criteria (… 2.5.8 target size minimum 24x24 …) — MOST apply to PRYZM without special effort; … Sweep separately.' The audit dropped the qualifier 'most' and promoted a draft open question into a stated contract minimum, then measured a shipped control against it. There is no normative target-size clause anywhere in C43 (grep for 'target size', '24', 'hit target' finds only :481). The underlying gap against external WCAG 2.2 AA is real and the buildPanelFold summary row would fix it as a side effect — but the finding as written misattributes a DRAFT open question to a contract, which is precisely the class of defect CLAUDE.md's correction boxes exist to stop. Report it as a WCAG observation, never as 'C43 names 24×24'."
  ],
  "missed": [
    "⭐⭐ THE PANEL IS NEVER REPAINTED WHEN THE LOAD FINISHES, SO EVERY NUMBER THE AUDIT WANTS TO COMPRESS IS STALE FOR THE WHOLE ~45 s — and this is the single most on-lens fact in the dimension. `scopeSliders` in SiteAuthoringPaneShell.ts is created at :639-645 and thereafter appears exactly once more, at :714, inside `dispose()`. There is NO `sl.refresh()` anywhere in the shell — and the contrast is decisive, because the view PICKERS one block above DO get `for (const p of pickers) p.refresh();` at :608. The slider's only repaint drivers are its own `input`/`change`/`click` handlers and `store.subscribe(...)` at :643 on the PANE-LAYOUT store, which fires on view assignment, not on context load. Nothing subscribes the slider to `site.scope-changed` (grep: the sole subscriber is CesiumViewport.ts:5157) and nothing at all fires on load COMPLETION. Now trace the consequence, all from code: `setContextScope` writes `this.contextScope = scope` SYNCHRONOUSLY at CesiumViewport.ts:2338, then kicks off the eight `void this.loadContext*` calls at :2353-2361; `getScopeCapVerdicts()` (:2589) recomputes `capVerdict(layer, r.eligible, r.cap, this.contextScope)` from `scopeCapReports`, which still holds the PREVIOUS load's eligible counts until :12574 (buildings) and :15054 (trees) fire tens of seconds later. `onRelease` calls `ports.commit(next)` — which emits `site.scope-changed` synchronously — and only THEN calls `paint()`. So the one repaint the gesture produces prints a HYBRID: last scope's eligible counts against the NEW scope's area. Both derived figures are then wrong in the reassuring direction: density = eligible/scopeAreaM2(newScope) is understated by the full area ratio (1781→5755 m is ~10.4×, so ~232/km² would have printed as ~22), and completeRadiusM = scopeOuterRadiusM(newScope)·√(cap/oldEligible) is overstated by ~3.2×. There is also a window inside the load where `clearContextBuildings()` (:12275, on the swap path) hits `scopeCapReports.delete('buildings')` (:13787) and the buildings verdict vanishes entirely. This is the §CONTEXT-DATA-HONESTY failure the entire dimension is arguing about, sitting one layer beneath the argument — and EVERY fix the audit proposes makes it worse rather than better: a compressed, always-visible, amber summary note carrying a wrong number is far harder to disbelieve than a grey paragraph. A repaint hook (a `getCapVerdicts` freshness stamp, or a load-settled port the shell drives) must land BEFORE any fold work, or the lane ships a more confident wrong answer.",
    "⭐ THE PANEL HAS NO IN-FLIGHT STATE, AND ITS PORTS CANNOT EXPRESS ONE — which is the direct answer to the founder's actual ask. `paint()` produces exactly four caption states: 'no site yet' (:539), 'could not be saved' (:610), 'shape could not be saved' (:626), and whatever `completenessCaption` returns. None of them is 'loading'. The `SiteScopeSliderPorts` interface (:57-92) has no `isLoading()`, no progress question and no load-generation counter, so the control could not say a load is running even if a lane wanted it to. During the ~45 s the founder is complaining about, the one panel he is looking at asserts a settled completeness reading and gives him nothing to act on. If his proposed build-off-to-the-side-then-swap architecture ships, THIS is the natural surface for 'new scope building — 12 s · showing the 1 781 m plate' and it is the cheapest possible place to put it (one port question, one caption arm, no new chrome). The audit never proposes it, even though its own finding 3 argues the panel is the only surface that speaks about plate completeness at all.",
    "⭐ `buildPanelFold` ADDS ~26 px OF COLLAPSED CHROME, SO THE NET SAVING IS ~44 px, NOT ~70 — an unpriced cost that works directly against the mandate the swap is meant to serve. panelFold.ts:84 sets `margin-top:6px;border-top:1px solid #efecf7;padding-top:5px;` on the `<details>`, and its `<summary>` (:97-104) deliberately declares no font-size, inheriting the panel root's `font: '600 11px/1.3'` (SiteScopeSlider.ts:354) → a ~14.3 px line box. Collapsed total: 6+1+5+14.3 ≈ 26 px of NEW height. Today, collapsed, the caption costs ZERO (display:none at :462) and the ⓘ costs zero extra height because the 20 px head row is already set by the taller shape buttons. So the swap takes the folded panel from ~55 px to ~81 px — a 47 % increase on precisely the number §SCOPE-PANEL-50 / afc5ce4c was minted to halve, and the founder's original words were 'the panel should be 50%'. Findings 1, 4 and 6 all price the fold as 'loses ~70 px' / '0 ms, ~1–2 h' and none of them subtracts the row. The honest reconciliation is either to keep the existing ⓘ-in-the-head-row geometry and borrow only panelFold's summary-note SEMANTICS (which forfeits the keyboard/screen-reader win finding 7 leans on), or to state plainly that the panel gets ~44 px smaller in the biting state and ~26 px LARGER in the reassurance state — the state it spends most of its life in.",
    "CONTEXT FOR THE PARENT, not a finding in this dimension: the lens asks whether the OLD context is torn down first, and the code says NO — it is a swap, so the user watches a complete-but-smaller plate, never an empty one. CesiumViewport.ts:2351-2352 states it ('Each loader swaps (builds the new geometry, then clears — C12 §13.4 / F-8), so the scene is never blank between the two') and three loaders confirm it by ordering: buildings §SITE-SCOPE F-8 comment at :12273 with `this.clearContextBuildings()` at :12275 AFTER the fetch/clip/ground-sample; trees at :15098-15100; roads fetch at :13820 with `clearContextRoads()` at :13837. The one exception is the honest no-op at :15056, which clears trees when the new read returns zero features."
  ]
}