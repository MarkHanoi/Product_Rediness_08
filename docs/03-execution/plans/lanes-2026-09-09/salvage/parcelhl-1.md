{
  "dimension": "A closed WebSocket is being written to on every animation frame (site-authoring split, 2026-09-09)",
  "summary": "The rAF tick that emits is CONFIRMED and it is the local cursor-presence broadcast. `apps/editor/src/engine/initCollaboration.ts:823` binds `mousemove` on `#container`; each sample is folded to one emit per frame onto the frame-scheduler bus (`:804` `scheduler.scheduleOnce('collab-cursor-emit', flush, 'overlay')`), and the flush calls `volatileEmit(socket, 'cursor-move', …)` at `:810`. The scheduler's rAF callback is literally named `tick` (`packages/frame-scheduler/src/FrameScheduler.ts:789`, driven from `:710`), which is the founder's stack frame exactly. `#container` is the element the LEFT 2D cadastral pane is mounted into (`apps/editor/src/ui/layout/GISAreaLayout.ts:7796` `const container = document.getElementById('container');` → `:7828` `mountSiteAuthoringPaneShell({ parent: container, … })`, and `engineLauncher.ts:305` resolves the same `#container` before passing it at `:1202`), so hunting for a plot with the mouse is precisely the gesture that drives this loop.\n\n(b) WHY EVERY FRAME: it is per-frame BY DESIGN and that design is two days old. Commit 21085b86 (2026-09-07 22:59) replaced a per-raw-mousemove `socket.emit` with a one-per-frame FOLD; C08 §3.4.1 clause 1 ratifies \"at most ONE emit per frame\". So it is throttled — to 60 Hz. A rival, unwired implementation in the same repo specifies 20 Hz (`packages/sync-client/src/awareness.ts:82` `const DEFAULT_CURSOR_COALESCE_MS = 50;`), and the outbound byte budget is asserted against that window, not this one.\n\n(a)+(c) THE WARNING SURVIVES THE FIX, AND I HAVE THE MECHANISM. `volatile` cannot discard on a closing socket in the shipped client: `Transport.onClose()` never clears `writable`, and `write()` restores it unconditionally in a fake drain placed OUTSIDE the try/catch that swallows the failed `doWrite`. So `discardPacket` is false on a dead socket and the packet walks the exact path in the founder's stack. The identical blind field also disables the L-13208 undelivered-command ledger, which is a live data-loss path, not cosmetics.\n\nThis dimension does NOT explain the missing parcel highlight, and I found no link to it. It is a plausible steady tax on the gesture he is performing, but it is not a 20-second stall.",
  "whatIcouldNotEstablish": "1. WHY THE SOCKET DIED — genuinely UNKNOWN. `initCollaboration.ts:629-631` logs `[initCollaboration] Socket disconnected: <reason>` with socket.io's own reason string ('transport close', 'ping timeout', 'io server disconnect', …). That single line names the cause and it is NOT in the log excerpt I was given. Ask for it before anyone theorises. My only measured candidate: `server.js:508-512` passes ONLY `cors`, `transports`, `maxHttpBufferSize`, so the server runs socket.io defaults pingInterval 25 s / pingTimeout 20 s, and the founder's log shows a 20,384 ms wait. But the arithmetic only bites if a ping lands in roughly the first 0.4 s of that window (~1.6% of a 25 s cycle), AND I could not establish that the 20,384 ms was a main-thread BLOCK rather than an async await — `§UX1-DRAW-PHASE-GATE` lives in `apps/editor/src/ui/onboarding/OnboardingStepController.ts` and I did not read its timing semantics. Do not write up \"the 20 s stall killed the socket\" as established; it is not.\n\n2. WHETHER THE WARNINGS IN HIS SESSION CAME FROM A FULLY CLOSED SOCKET OR THE CLOSING WINDOW. Both produce the identical warning by the mechanism in F1. I have no timestamps correlating his `Socket disconnected` line to the warning burst.\n\n3. WHETHER THE DEPLOYED BUNDLE CONTAINS 21085b86 / ec221a71. They are in HEAD (7b1b9291) and predate the report by ~2 days, but I did not query the live build. If the live build is older, F1's diagnosis still holds — the pre-fix code emitted plainly, which cannot discard either — but F4/F5 would not yet apply. `committed ≠ reachable` is a documented failure mode here.\n\n4. WHETHER CHROME THROWS OR WARNS on `ws.send()` in CLOSING state. I could not run a browser. engine.io wraps `doWrite` in `try { … } catch (e) {}` (`socket.io.js:1414-1416`) with the comment that send \"will throw an error\", implying at least some browsers throw — but the founder's trace shows a `websocket.js:123` console entry with a stack and no uncaught error, which reads as a browser-emitted warning. THE BRIEF'S PREMISE \"an exception thrown per frame\" IS THEREFORE NOT ESTABLISHED, and I did not assume it in the cost estimate.\n\n5. THE COST IS UNMEASURED. I ran no profile. Every number in the costMs fields is a stated order-of-magnitude range, not a measurement. A 5-minute trace with the pointer moving over the 2D map would settle it and I would not report a magnitude without one.\n\n6. NO LINK TO THE PARCEL HIGHLIGHT. I looked only at the emit path, per my dimension. I found nothing connecting the socket to selection highlighting and I did not audit the highlight code — that belongs to whoever owns the highlight dimension. Nothing here should be read as ruling it in or out.\n\n7. THE 50 ms vs 1-frame CONTRADICTION'S OLDER AUTHORITY. `awareness.ts:76` cites \"spec line 317\" for the 50 ms window; I did not resolve which SPEC file that is, so I cannot say which of the two rates is the governing one — only that two exist and disagree.",
  "findings": [
    {
      "title": "socket.volatile CANNOT discard on a closing socket in socket.io 4.8.3 — the L-13207 fix's second half is a no-op, and the ISSUE-LOG row's \"removes the warning AT THE SOURCE\" is false",
      "detail": "THE CHAIN, END TO END, ALL IN THE SERVED CLIENT (`node_modules/socket.io/client-dist/socket.io.js`, the exact file the app loads via `initCollaboration.ts:867` `script.src = '/socket.io/socket.io.js'`).\n\nThe emit: `apps/editor/src/engine/initCollaboration.ts:810` `volatileEmit(socket, 'cursor-move', {` → `apps/editor/src/engine/collabOutbound.ts:123-125` `if (vol && typeof vol.emit === 'function') { vol.emit(event, payload); return true; }`.\n\nThe discard test, socket.io.js:3781 — VERBATIM:\n    var discardPacket = this.flags[\"volatile\"] && !isTransportWritable;\nand :3779:\n    var isTransportWritable = (_b = (_a = this.io.engine) === ... === void 0 ? void 0 : _a.transport) === ... : _b.writable;\n\nSo volatile keys on ONE field: `engine.transport.writable`. Now the three places that field is written on the WebSocket transport:\n\n  :884  `this.readyState = \"closed\";`  — the WHOLE of `Transport.onClose`'s state update. ⛔ IT NEVER TOUCHES `writable`.\n  :1406 `this.writable = false;`       — first statement of `Transport.write()`.\n  :1414-1416 `try { _this2.doWrite(packet, data); } catch (e) {}`\n  :1420-1423 `nextTick(function () { _this2.writable = true; _this2.emitReserved(\"drain\"); }, ...)` — the \"fake drain\", placed OUTSIDE the try, and it runs whether doWrite succeeded or failed.\n\n⇒ `writable` is `false` ONLY for the one nextTick between `write()` starting and the fake drain. It is NOT a liveness signal, it is a back-pressure signal, and a dead socket restores it to `true` on the very next tick. On a CLOSING/CLOSED WebSocket `isTransportWritable === true`, `discardPacket === false`, and control falls to :3784-3786 `else if (isConnected) { this.notifyOutgoingListeners(packet); this.packet(packet); }` — which is the founder's stack verbatim: emit → packet → write → _sendPacket → flush → send → doWrite → \"WebSocket is already in CLOSING or CLOSED state\".\n\n⛔ TWO PLACES ASSERT THE OPPOSITE AND BOTH ARE WRONG. `initCollaboration.ts:789-791`: \"it DISCARDS the packet when `io.engine.transport.writable` is false instead of calling `ws.send()` on a closing socket. That is what removes the warning AT THE SOURCE.\" And `docs/04-reference/ISSUE-LOG.md:53502` (L-13207) says the same, and closes with **\"FIXED (code + spec); browser-unverified.\"** THE FOUNDER'S 2026-09-09 CONSOLE IS THAT BROWSER VERIFICATION AND IT FAILED. The mechanism was reasoned correctly from socket.io's intent and never checked against socket.io's code.\n\nNOT A REGRESSION: the warning predates the fix — it is what L-13207 was raised for on 2026-09-07. 21085b86 removed ~97% of the VOLUME (per-mousemove → per-frame, real and worth keeping) and 0% of the per-frame occurrence.",
      "isRegression": false,
      "fix": "Guard on liveness the client can actually see, and stop trusting `transport.writable` for it. In `collabOutbound.ts`, add a `canWriteNow(socket)` that reads the ONE field that is true only when the wire is open — `socket.io?.engine?.transport?.ws?.readyState === WebSocket.OPEN` (`1`) — treating an unreadable field as writable, exactly the way `classifyOutbound` already refuses to invent a failure from an unreadable one (`collabOutbound.ts:51-54, 107`). Call it from the cursor flush at `initCollaboration.ts:808` alongside the existing `socket?.connected` check. Keep `volatileEmit` — it is correct for real back-pressure and costs nothing — just stop relying on it as the closing-socket guard.\n\n⚠ MUST NOT BREAK: (1) the polling transport has no `.ws`, so an unreadable `readyState` must mean DELIVERABLE, never dropped — the client is configured `transports: ['websocket', 'polling']` at `initCollaboration.ts:581`. (2) Do NOT extend this guard to the durable `command-executed` emit as a silent drop — see the next finding; there the correct response is to RECORD, not to skip. (3) Do not \"fix\" this by suppressing the console; L-13207 was explicitly right about that and the correction must not undo it.",
      "risk": "low",
      "confidence": "high",
      "file": "apps/editor/src/engine/initCollaboration.ts",
      "line": 810,
      "quote": "volatileEmit(socket, 'cursor-move', {",
      "costMs": "UNMEASURED. Per frame while the pointer moves over the 2D map: one `getBoundingClientRect()` on #container (see the forced-layout finding), one socket.io packet encode, one engine.io encodePacket, one rejected `ws.send()`. The dominant term is the console entry itself — Chrome captures and formats a stack per warning, which is expensive with DevTools OPEN and much cheaper with it closed. Order of magnitude, not a measurement: roughly 0.2-2 ms/frame with DevTools open, i.e. 1-12% of a 16.7 ms frame budget, and only while the pointer is moving. It is a steady tax on exactly the gesture he is performing; it is NOT a 20-second stall and must not be written up as the cause of one.",
      "suspectCommit": "21085b86 fix(collab/§PRESENCE-IS-A-SAMPLE-STREAM + §OUTBOUND-DELIVERY-IS-NOT-FIRE-AND-FORGET L-13207 + L-13208) — 2026-09-07 22:59:04 +0100. This commit INTRODUCED the volatile call believed to fix the warning (`git diff 21085b86~1 21085b86` shows `-socket.emit('cursor-move',` / `+volatileEmit(socket, 'cursor-move',`). It did not cause the warning; it is the commit whose fix does not work."
    },
    {
      "title": "The same blind field disables the L-13208 undelivered-command ledger — a real BIM edit emitted in the CLOSING window is still lost silently, from peers AND from project_command_log",
      "detail": "⛔ THIS IS THE NON-COSMETIC HALF AND IT IS STILL OPEN, while the ISSUE-LOG reads as though the detection landed.\n\n`apps/editor/src/engine/collabOutbound.ts:106-108` — VERBATIM:\n    const writable = s.io?.engine?.transport?.writable;\n    // Only a POSITIVE report of non-writability counts. `undefined` = cannot tell = deliverable.\n    if (writable === false) return 'transport-not-writable';\n\nThat is the SAME field, with the SAME blindness proved in the previous finding: on a CLOSING or CLOSED WebSocket it is `true`, not `false`. So the verdict `'transport-not-writable'` — minted specifically, by name, for the founder's CLOSING window — can essentially never fire. Its only true window is the one nextTick after a `write()`, which has nothing to do with the socket being dead.\n\nCONSEQUENCE AT THE DURABLE PATH, `initCollaboration.ts:1045-1046`:\n    const verdict = classifyOutbound(socket, currentProjectId);\n    if (!isDeliverable(verdict)) {\nreturns `'deliverable'` in exactly the window the module was built to catch. Execution falls through to `:1078` `socket.emit('command-executed', {` — into the dead socket, where engine.io fake-drains it (`socket.io.js:1414-1423`, as documented in L-13207). Nothing is recorded in `UndeliveredCommandLedger`, no `console.error` prints, no `pryzm-collab-command-undelivered` event fires.\n\nAnd L-13208's own row (`docs/04-reference/ISSUE-LOG.md:53503`) states why that is unrecoverable: server-side that ONE event is BOTH the peer broadcast AND the only writer of `project_command_log`, and `_triggerCatchUp` is inbound-only with `excludeSelf=1` — structurally incapable of noticing this client's own emit vanished.\n\n⇒ The module written to stop failure and empty printing the same value still prints the same value for the precise case it names. §CONTEXT-DATA-HONESTY, and `gate-blind-on-the-wrong-axis`: the gate is green on membership and blind on liveness.\n\n⚠ HONEST SCOPE: `'not-connected'` (`:105`, `s.connected !== true`) DOES fire and is detected correctly. Only the `transport-not-writable` arm is blind. The ledger is not useless — it is blind to one specific, and the most likely, window.",
      "isRegression": false,
      "fix": "Replace the `writable === false` test at `collabOutbound.ts:106-108` with the same `ws.readyState` liveness read proposed in the previous finding, keeping the existing 'unreadable means deliverable' rule verbatim (`collabOutbound.ts:51-54` argues for it correctly and that argument must survive). One field change makes the `transport-not-writable` verdict able to fire at all.\n\n⛔ MUST NOT: queue-and-replay the command. `initCollaboration.ts:1038-1043` and L-13211 both spell out why — a client replay mints a SECOND commandLogId and §FIX-REPLAY-AT-MOST-ONCE (L-814) keys its dedupe on exactly that id, so a silent DUPLICATE would replace a silent loss. The scope here is DETECTION only, which is what the module already claims.\n\n⚠ MUST NOT BREAK: `isCollaborationGap` (`collabOutbound.ts:85-87`) must keep excluding `socket-missing` / `no-project`, or a solo session prints a red error on every edit — an alarm wrong every time it fires. That reasoning is sound and is the whole point of ec221a71.\n\nThis warrants a NEW ISSUE-LOG row rather than an edit to L-13208, and L-13208's status should move off 'reporting landed' — the reporting landed and cannot see the case it was built for.",
      "risk": "medium",
      "confidence": "high",
      "file": "apps/editor/src/engine/collabOutbound.ts",
      "line": 106,
      "quote": "const writable = s.io?.engine?.transport?.writable;",
      "costMs": "Not a latency cost — a correctness cost. Unbounded: any command executed inside the CLOSING window is absent from every peer and from project_command_log permanently, with no console line and no ledger entry.",
      "suspectCommit": "21085b86 (2026-09-07 22:59:04) created collabOutbound.ts with this field; ec221a71 (2026-09-07 23:16:24) refined the verdict split but not the field. `git log --oneline -- apps/editor/src/engine/collabOutbound.ts` returns exactly these two commits."
    },
    {
      "title": "Presence NEVER resumes after the first disconnect — `socket.once('disconnect')` tears the mousemove listener down and nothing re-arms it, while 'connect' keeps logging that collaboration is up",
      "detail": "`apps/editor/src/engine/initCollaboration.ts:825-828` — VERBATIM:\n    // Remove the mousemove listener on disconnect to avoid stale closures.\n    socket.once('disconnect', () => {\n        container.removeEventListener('mousemove', onMouseMove);\n    });\n\nThe listener is installed at `:823` `container.addEventListener('mousemove', onMouseMove);` — INSIDE `connectSocket()` (declared `:570`). I traced every path back:\n  · `connectSocket(projectId, ioFn)` is called from exactly ONE site, `:848`, inside `loadAndConnect` (`:838`).\n  · `loadAndConnect(projectId)` is called from exactly ONE site, `:1140`.\n(`grep -n \"connectSocket(\\|loadAndConnect(\" apps/editor/src/engine/initCollaboration.ts` → declarations at 570/838 plus call sites 848/1140 only.)\n\nsocket.io's default `reconnection: true` is left on — the `ioFn({ transports, auth })` call at `:580-583` passes no reconnection options — and socket.io reconnects on the SAME `Socket` instance, re-firing `'connect'` on the handler already registered at `:587`. `connectSocket` is NOT re-run. So the removal at `:827` is permanent for the life of the session.\n\n⚠ AND THE UI SAYS THE OPPOSITE. On that same reconnect `:588` prints `[initCollaboration] Socket connected — joining project room:` and `:593` re-emits `join-project`. Inbound still works — `remote-cursor` (`:643`), `remote-command` (`:655`) are all still bound. So after one drop the user RECEIVES everyone else's presence and edits while broadcasting no cursor of their own, and every visible signal says collaboration is healthy. That is the silent-offline case the brief asks about, and it is real.\n\n⭐ NOT A REGRESSION, AND GIT IS UNAMBIGUOUS: `git log -S\"removeEventListener('mousemove', onMouseMove)\" --oneline -- apps/editor/src/engine/initCollaboration.ts` → `8613866a Initial commit`, and nothing since. This is long-standing latent debt, not something today's lanes broke. It matters NOW only because findings 1-2 establish the socket is in fact dying in his sessions.\n\n⛔ I DID NOT ASSERT THIS FROM THE COMMENT. The comment says 'to avoid stale closures', which is a real concern; the defect is that the teardown has no matching re-arm, which I established from the call-site count, not from the comment.",
      "isRegression": false,
      "fix": "Move the mousemove binding OUT of the per-connect scope. Bind it once for the life of `initCollaboration` (the emit already no-ops safely when there is no live socket — `:819` and `:808` both early-return on `!socket?.connected`), and drop the `socket.once('disconnect')` teardown entirely; the stale-closure worry it names is already answered by those guards, since both read the module-level `socket` binding rather than capturing one. Tear the listener down in the existing `disconnect()` / dispose path (`:1165-1177`) instead, which is the one place a session really ends.\n\n⚠ MUST NOT BREAK: (1) presence must still work while the socket is alive — verify a cursor still reaches a peer after a forced `socket.io.engine.close()` and reconnect, not just on first connect; (2) `connectSocket`'s own re-entry path (`:571-573` `if (socket) { socket.disconnect(); socket = null; }`, the project-switch case) must not end up with TWO mousemove listeners — binding once outside `connectSocket` gives that for free, but it is the thing to check; (3) `clearAllCursors()` on disconnect (`:632`) must stay — remote cursors should still vanish when the wire drops.",
      "risk": "low",
      "confidence": "high",
      "file": "apps/editor/src/engine/initCollaboration.ts",
      "line": 826,
      "quote": "socket.once('disconnect', () => {",
      "costMs": "No CPU cost — a correctness/UX cost. After the first disconnect, this user is invisible to every peer for the rest of the session with no indication.",
      "suspectCommit": "8613866a Initial commit — `git log -S\"removeEventListener('mousemove', onMouseMove)\"` names this and nothing else. Pre-existing, NOT caused by any of today's L-13262..L-13282 lanes."
    },
    {
      "title": "A per-frame forced layout of the entire site-authoring split, on the exact surface he is hovering",
      "detail": "`apps/editor/src/engine/initCollaboration.ts:809`, inside the once-per-frame flush:\n    const rect = container.getBoundingClientRect();\n\n`container` is `#container`: `apps/editor/src/engine/engineLauncher.ts:305` `const container = document.getElementById('container')!;`, passed at `:1202` `initCollaboration({ container, commandManager, events: runtime?.events });`.\n\nAnd `#container` is the mount parent of the WHOLE split he is looking at — `apps/editor/src/ui/layout/GISAreaLayout.ts:7796` `const container = document.getElementById('container');` then `:7828` `const shell = mountSiteAuthoringPaneShell({ parent: container, initialLeftFraction: 0.5 });`. So the LEFT 2D pastel cadastral map and the RIGHT 3D Forma site are both inside the element being measured, and moving the pointer over the map to find a plot forces a synchronous layout of that subtree once per frame.\n\n⚠ STATED FAIRLY: this is a large IMPROVEMENT over what it replaced. `git diff 21085b86~1 21085b86` shows the rect read moved FROM per-raw-mousemove INTO the per-frame flush (`-            const rect = container.getBoundingClientRect();` / `+                const rect = container.getBoundingClientRect();`), and `:792-794` explains the reasoning correctly — the pane box is genuinely moving during a divider drag, so measuring at send time is right. I am not proposing to revert it. I am recording that the remaining per-frame cost lands on the founder's current gesture and is paid even when the packet is then thrown away by a dead socket, which is the part worth removing.\n\n⭐ THE CHEAP WIN IS ORDERING, NOT REMOVAL: the rect is read at `:809` BEFORE `volatileEmit` at `:810`, and the liveness guard proposed in finding 1 would sit at `:808`. Guarding first means a dead socket costs zero layouts instead of 60/second.",
      "isRegression": false,
      "fix": "Put the liveness guard from finding 1 ahead of the `getBoundingClientRect()` at `:809`, so a non-writable wire skips the forced layout as well as the doomed write. Separately, consider caching the rect against a ResizeObserver on the shell and invalidating on divider drag, rather than measuring every frame — but ONLY if measured to matter; the current code's reasoning for measuring at send time is sound and should not be undone on suspicion.\n\n⚠ MUST NOT BREAK: cursor coordinates are container-relative (`:811-813` `x: sample.clientX - rect.left`), so any caching must invalidate on divider drag and on window resize or peers will see cursors offset by the stale pane box — which is the exact scenario (a moving pane box) that motivated moving the read into the flush.",
      "risk": "low",
      "confidence": "high",
      "file": "apps/editor/src/engine/initCollaboration.ts",
      "line": 809,
      "quote": "const rect = container.getBoundingClientRect();",
      "costMs": "UNMEASURED — no profile was run. A forced synchronous layout on a subtree this size is typically in the 0.1-2 ms range, i.e. up to ~12% of a 16.7 ms frame, and only while the pointer moves. Plausible contributor to a feeling of sluggishness during plot-hunting; NOT an explanation for the 20,384 ms draw-phase gate or the 15,314 ms roads read, both of which belong to the other lane."
    },
    {
      "title": "Two presence implementations with different rates — the live one runs at 60 Hz against a 20 Hz spec and a byte budget computed for 20 Hz; the 20 Hz one has no callers",
      "detail": "⭐ ESTABLISHING WHICH CODE PAINTS HIS PIXELS, because this repo has the twin problem six times over. I checked caller counts on all three candidates rather than blaming the first file I found:\n\n  · `apps/editor/src/engine/initCollaboration.ts` — LIVE. Called at `engineLauncher.ts:1202`. Its log string `[initCollaboration] Socket connected — joining project room:` (`:588`) is VERBATIM the line in the founder's log. This is his code.\n  · `apps/editor/src/ui/platform/PlatformCollabPill.ts` — a near-twin with its own socket and its own `join-project` (`:191`), imported only by `PlatformShell.ts:49`. It has NO mousemove handler, NO rAF, and NO `cursor-move` emit (`grep -n \"mousemove|requestAnimationFrame|emit(|cursor\"` over its 229 lines returns exactly one hit, the `join-project` at `:191`). Its log prefix is `[PlatformCollabPill]` (`:190`), which is NOT what the founder's log shows. RULED OUT as the emitter.\n  · `plugins/multiplayer/src/tool.ts` — `MultiplayerTool`, which broadcasts `multiplayer.cursor.move` on `pointermove` (`:82-92`). ZERO callers: `grep` for `MultiplayerTool` across all .ts/.tsx/.js returns only its own file. DEAD. Ruled out.\n  · `packages/sync-client` awareness (`PryzmAwareness`) with `PresenceCursorLayer` — the intended architecture, and UNWIRED at the UI. `PresenceCursorLayer` appears in `apps/editor/src` only in its own definition, and its own body says `awareness not yet available` / `pending Phase C.5.x typed API` (`:43`, `:49`).\n\nTHE RATE DISAGREEMENT: the unwired-at-UI path specifies `packages/sync-client/src/awareness.ts:82` `const DEFAULT_CURSOR_COALESCE_MS = 50;` (20 Hz), described at `:18` as 'cursor — coalesced at 50 ms' and at `:76` as 'Default 50 (spec line 317)'. The LIVE path coalesces to one frame, ~16.7 ms — 3x that rate. `apps/bench/src/benches/awareness-throughput.bench.ts:5-6` builds its harness on 'the production cursor coalesce window (50 ms)' and `:44` asserts 'per-peer outbound bytes-per-second stays under the 5 KB/s budget' — a budget computed against a window the live path does not use. C08 §3.4.1 clause 1 (minted 2026-09-07, `docs/02-decisions/contracts/C08-COLLABORATION-AND-SECURITY.md:263`) ratifies the per-frame fold, so the two are now contractually as well as numerically in conflict.\n\nThis is the lowest-severity finding here and I am flagging it as an ordering question, not a defect to fix in this lane.",
      "isRegression": false,
      "fix": "Do NOT change the rate in this lane — it is a contract question, not a bug. Raise it against C08 §3.4.1: either the 50 ms window is the governing figure and the per-frame fold needs a minimum interval on top (a fold with a floor, still not a debounce — the distinction C08 §3.4.1 and `collabOutbound.ts:216-219` are both emphatic about, and rightly), or the per-frame fold is correct and `awareness.ts`'s 50 ms plus the bench's 5 KB/s budget need restating against it. One of the two numbers is wrong and neither is currently measured against the live path.\n\n⚠ MUST NOT BREAK: if a floor is added, it must still fire during continuous motion — the failure mode C08 §3.4.1 clause 1 names explicitly is a debounce that 'never fires during continuous motion, which is precisely when presence matters'. Latency must stay bounded, not merely averaged.",
      "risk": "none",
      "confidence": "medium",
      "file": "packages/sync-client/src/awareness.ts",
      "line": 82,
      "quote": "const DEFAULT_CURSOR_COALESCE_MS = 50;",
      "costMs": "~3x the outbound presence packet rate the 5 KB/s per-peer budget was computed for. Invisible at one user; matters at the concurrency tiers C66 governs. Not measured against a real peer count.",
      "suspectCommit": "UNKNOWN — I did not bisect the divergence. The live per-frame fold arrived in 21085b86 (2026-09-07); the 50 ms constant predates it and I did not date it."
    }
  ]
}