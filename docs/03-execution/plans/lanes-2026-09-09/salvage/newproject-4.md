{
  "dimension": "The engine chunk and the boot legs — locating the 85% the prior audit could not, and establishing whether the BIM engine belongs on the critical path of a globe with a text box",
  "summary": "THE ANSWER TO THE BRIEF IS ONE SENTENCE: the \"Where is your project?\" globe cannot render until the ENTIRE BIM engine has booted, because `startOnboardingStepFlow()` is armed inside a `pryzm-project-loaded` handler and that event is only emitted after `await attachedBootstrap.ensure()` has run `initScene → initBuilders → initTools → initBusHandlers → registerAllStores → initDataPlatform → initUI`. The founder's `open:first-interactive-frame (t+9233ms)` and his screen are the SAME instant — both handlers hang off the same event, one frame apart. Deferring the boot past the prompt is the whole 10x; everything else in this report is detail.\n\nTHE MILLISECONDS ADD UP, from a repo artifact measured on this codebase 2026-09-02 (`audit/perf/2026-09-02/baseline-outer.json`, n=5, local prod build, `axisC-local` = the canonical create+open gesture, which is exactly the founder's path):\n  · apiLeg (POST /api/v1/projects — the actual \"new project\")  ......  12 ms median (32 ms live)\n  · click → canvas visible (`hydrateToCanvas` − apiLeg)  ...........  2,681 ms median\n  · canvas visible → usable (`hydrateToUsable` − `hydrateToCanvas`)   4,496 ms median\n  · TOTAL clickToUsable ............................................  7,200 ms median, 8,769 ms p95\nThe founder's 9,233 ms sits just above that p95 (production build, WAN create, his hardware). CREATING THE PROJECT IS 12 ms OF IT. The other ~99.9% is booting an engine the screen does not use. Every data-dependent leg the prior audit measured (988 ms hydrate for 300 walls, per-wall replay, redetect) is ZERO here — his project is empty, exactly as he says.\n\nAND IT IS NOT THE BYTES. Same artifact, `axisD-local`: WARM (service-worker-cached assets) `interactive` median 13,950 ms vs COLD 11,659 ms — warm is SLOWER. `public/sw.js` cache-first-serves `/assets/*`, so on his repeat session the 1.81 MB gz engine chunk is already local. The boot is CPU-bound. Shrinking the bundle is worth doing but it is NOT the fix for his complaint; not running the boot before this screen is.\n\nON (b) — I must correct the brief's premise. \"`endBootLeg` computes wallMs from the last `bootStep()` boundary, and the leg's only `await initBuilders(...)` sits AFTER the last bootStep, so it is excluded\" is FALSE AT HEAD. `initBuilders.ts` contains 29 of its own `bootStep()` calls (`:341` `bootStep('column')` … `:1248` `bootStep('teardown wiring + window publish')`), so the leg's `lastAtMs` ends INSIDE `initBuilders` and the await is tiled. The real instrument defects are different and worse, and they are findings 5 and 6 below. The 18,626 ms has not been located because it was measured on a different path (opening a 300-wall project) and because five of the six boot stages carry no step instrument at all.\n\nON (d), plainly: no, the engine is NOT required for this screen. The Cesium viewport is already constructed and mounted warm-hidden at the moment of the click by `prewarmGlobe()` (`eagerGlobeStart.ts:178`), it is streaming photoreal tiles in parallel with the boot (the founder's own `§STARTUP-GLOBE-COMPLETE-FIRST` line), it lives in its own 341 KB / ~96 KB gz chunk with a 4-file static closure that touches neither three.js nor @thatopen nor web-ifc, and the location step's `start()` does exactly three project-independent things: `resetAppPhaseForNewProject()`, `mountOverlay()`, `renderLocationStep()`. The only thing it borrows from the engine is three `window.pryzm*` globals that `mountGISArea` installs at `initUI`, the LAST stage of bootstrap. That is an accident of where the globals are installed, not a dependency on the BIM engine.\n\nON (e): for the founder's gesture (app already loaded, hub on screen), the floor is a DOM card plus `CesiumViewport.setVisible(true)` on a viewer that has been mounted since the click — comfortably inside 1 s, with margin. 1 s IS ACHIEVABLE for what he asked for. For a genuinely cold first page load it is NOT: `dist/index.html:5` is a render-blocking classic `<script src=\"/cesium/Cesium.js\">` of 5.91 MB raw / 1.73 MB gz, and the static module graph behind it is 20.63 MB raw / ~5.6 MB gz. Measured live warm shell interactive is 852 ms median; measured live cold shell interactive is 3,547 ms median. Say that honestly rather than promising 1 s cold.",
  "findings": [
    {
      "title": "THE GATE: the location step is armed inside a `pryzm-project-loaded` handler, and that event requires the whole engine bootstrap first",
      "detail": "`briefBootstrap.ts:237` arms `const onLoaded = runtime.events.on('pryzm-project-loaded', (p) => {` and calls `startOnboardingStepFlow({` at `:268` INSIDE that handler. `startOnboardingStepFlow` → `controller.start()` (OnboardingStepController.ts:535) → `this.renderLocationStep()` (:546) → `markStartupPhase('location-step:open')` (:708) → the 'Where is your project?' prompt (:732). The event is emitted from `PlatformShell.ts:311` on the new-project branch, reached from `setProjectContext`, which `buildPersistence.ts` step 4 only calls AFTER `buildPersistence.ts:336` `await attachedBootstrap.ensure();` — which is `main.ts:360` `await startEngine(runtimeRef.current);` → `main.ts:212` `const mod = await loadEngine();` → `main.ts:224` `await mod.bootstrap(runtime);`, i.e. the full `initScene → initBuilders → initTools → initBusHandlers → registerAllStores → initDataPlatform → initUI` chain. NOTE ON THE MARK: `PlatformShell.ts:193` arms its OWN listener on the same `pryzm-project-loaded` inside `setProjectContext`, and `:204` schedules `markStartupPhase('open:first-interactive-frame')` one frame later. briefBootstrap registered first (before `createAndOpenProject`), so `location-step:open` fires first and the founder's `t+9233ms` mark is his screen ±1 frame — not a proxy for it. ⛔ The `startupBudget.ts:158-160` comment lists the reverse order; that comment is wrong and the registration order in code is what runs.",
      "onCriticalPath": true,
      "costMs": "~7,188 ms MEASURED at median / 8,757 ms at p95 (audit/perf/2026-09-02/baseline-outer.json `axisC-local.hydrateToUsable` minus `apiLeg`, n=5, local prod build, canonical create+open gesture); founder's own run 9,233 ms",
      "costBasis": "repo-artifact",
      "quote": "const onLoaded = runtime.events.on('pryzm-project-loaded', (p) => {",
      "file": "apps/editor/src/ui/onboarding/briefBootstrap.ts",
      "line": 237,
      "fix": "SPLIT THE GATE — arm the location step on GLOBE readiness, not on project readiness, and let the boot run behind it. (1) `eagerGlobeStart.ts` already holds the mounted viewport in `_prewarm`; add a NON-consuming `whenGlobePrewarmed(): Promise<PrewarmedGlobe|null>` beside the existing `consumePrewarmedGlobe()` (`:244`) — same promise, no second construction. (2) In `briefBootstrap`, hold `pryzm-project-loaded` as a PROMISE (`const projectReady = new Promise(...)` armed exactly where the listener is armed today) and call `startOnboardingStepFlow({ runtime, projectReady, ... })` as soon as the prewarm resolves. (3) In `OnboardingStepController`, the LATER steps await `projectReady` at the point they genuinely need an open project + active level — `handleGeocode`'s site-create leg and everything downstream. Typing a city is 3-10 s of human dwell; the boot fits inside it. (4) Reveal through the EXISTING owner, adding no new writer of container geometry (§View-region-has-ONE-owner): `CesiumViewport.setVisible(true)` at `CesiumViewport.ts:18690` is the documented counterpart of `enterWarmHiddenState()` at `:18678`, and `mountGISArea` keeps adopting the same instance exactly as it does today, so `toggleGIS` remains the sole steady-state owner. (5) The `_ensureEngineLoadingOverlay()` painted at `PlatformRouter.ts:1003` must stop covering the screen on this path — it becomes a small in-corner progress affordance while the globe owns the surface. THAT step is the real integration risk and it is where the review effort belongs.",
      "risk": "medium",
      "confidence": "high"
    },
    {
      "title": "Project creation itself is 12 ms — nothing in the 9.2 s is 'creating a new project'",
      "detail": "`audit/perf/2026-09-02/baseline-outer.json` `axisC-local.apiLeg` (POST /api/v1/projects, the `client.create` at PlatformRouter.ts:1014) is min 9 / median 12 / p95 51 ms locally; `diagnosis.md:78` records the LIVE figure at 32 ms. The same file's AXIS C verdict is 'Project creation cost == editor open cost'. This matters because it forecloses a whole class of wrong answer: the server, the POST, the id reconciliation, the persistence store are all acquitted with a number. It also confirms the founder's own evidence — `Snapshot created: 0 elements, 1 levels, 0 walls` — that nothing is being loaded.",
      "onCriticalPath": true,
      "costMs": "12 ms MEASURED local median (32 ms live) — i.e. 0.13% of his 9,233 ms",
      "costBasis": "repo-artifact",
      "quote": "\"apiLeg\": { \"n\": 5, \"min\": 9, \"median\": 12, \"p95\": 51, \"max\": 51 }",
      "file": "audit/perf/2026-09-02/baseline-outer.json",
      "line": 62,
      "fix": "No fix — this is the exoneration that keeps the lane pointed at the boot. Quote it whenever someone proposes optimising the create/persistence path.",
      "risk": "none",
      "confidence": "high"
    },
    {
      "title": "The boot is CPU-bound, not download-bound: warm (service-worker-cached) startup measured SLOWER than cold",
      "detail": "`public/sw.js:115` serves `/assets/*` cache-first from `ASSETS_CACHE` after first use, so on the founder's repeat session the engine chunk is local. Measured consequence, `audit/perf/2026-09-02/baseline-outer.json` `axisD-local`: WARM `interactive` min 13,058 / median 13,950 / p95 14,341 ms against COLD min 9,403 / median 11,659 / p95 13,710 ms. `diagnosis.md` states the same conclusion from the live side: 'SW serves 32/36 assets warm yet warm interactive did NOT improve => local D is CPU-bound.' The corroborating CPU evidence is `audit/perf/2026-09-02/axisD-cold-startup.cpuprofile`: over a 15,899 ms profile, `(program)` self = 5,777 ms (36%), `(idle)` = 1,718 ms, GC = 273 ms; per-URL self time is vendor-three 2,625 ms, domain-engine 1,742 ms, engineLauncher 1,311 ms, main 717 ms, vendor-thatopen 693 ms. `(program)` running at 400-580 ms/s sustained from t+6 s to t+15 s is V8 lazily compiling functions in a 20 MB graph as the boot first calls into them — a cost you pay on every load whether or not the bytes were cached.",
      "onCriticalPath": true,
      "costMs": "5,777 ms MEASURED as `(program)` self in a 15,899 ms cold profile; the warm-vs-cold delta (+2,291 ms median, warm slower) is MEASURED",
      "costBasis": "repo-artifact",
      "quote": "\"interactive\": { \"n\": 5, \"min\": 13058, \"median\": 13950, \"p95\": 14341, \"max\": 14341 }",
      "file": "audit/perf/2026-09-02/baseline-outer.json",
      "line": 186,
      "fix": "Read this as a CONSTRAINT on the other fixes, not as a fix. It rules out 'ship fewer bytes' as the answer to the founder's complaint (his bytes are already cached and it did not help) and it rules IN 'do not execute the boot before this screen'. Bundle work still earns its keep on the cold first visit and on the lazy-compile tax — see the web-ifc finding — but it must not be sold as the 10x.",
      "risk": "none",
      "confidence": "high"
    },
    {
      "title": "`ensureEngineWarm()` fires at the click, not before it — the pre-warm buys ~30-100 ms, and its own header describes a path the founder never takes",
      "detail": "Exactly two production call sites, both AFTER the gesture: `PlatformRouter.ts:720` inside `showOnboarding()` and `PlatformRouter.ts:989` inside `createAndOpenProject()` (idempotent no-op). Its header at `engineWarmup.ts:21-28` justifies itself with 'the RAC role/brief/location/draw steps take several seconds — ample time to download + evaluate the chunk'. ⛔ READ THE CODE THE COMMENT SITS ON: `PlatformRouter.ts:801` `if (getCurrentUser() && seededTypologyId) {` … `:812` emits `pryzm:onboarding-brief-ready` and `:818` RETURNS — the RAC panel is never constructed. This branch is the founder's path both ways into it: `ProjectHub.ts:2060` `this.callbacks.onStartOnboarding({ name, directEntry: true });` (the '+ New Project' gesture) and `ProjectHub.ts:2118` (the modal's Create button). So between the download starting and `main.ts:212` `const mod = await loadEngine();` awaiting it there is only: the synchronous tail of `showOnboarding`, the brief-ready handler, and one `client.create` round trip measured at 12 ms local / 32 ms live. Also note the modal dwell — the seconds the user spends typing a project name — is NOT used: `onStartOnboarding` fires from the modal's SUBMIT, so the warm starts after the typing, not during it.",
      "onCriticalPath": true,
      "costMs": "head start ≈ 30-100 ms ESTIMATED (sync tail + one 12-32 ms MEASURED create round trip), against an engine leg measured at ~2,681 ms to canvas-visible — the warm recovers roughly 1-4% of what its header claims",
      "costBasis": "read-from-code",
      "quote": "        ensureEngineWarm();",
      "file": "apps/editor/src/ui/platform/PlatformRouter.ts",
      "line": 720,
      "fix": "Call `ensureEngineWarm()` at `showHub()` (PlatformRouter.ts:~617, beside `markStartupPhase('hub:mount-start')`) wrapped in `requestIdleCallback(..., { timeout: 3000 })`, so the download+evaluate overlaps the hub dwell instead of the click. This is the same 'START EARLIER, do not skip anything' doctrine `engineWarmup.ts:19` already states, applied one screen earlier. Gate it on `navigator.connection?.saveData !== true` so a metered visitor who never opens a project is not charged 1.81 MB. Guard the main-thread cost: evaluating engineLauncher measured 1,311 ms self, which must not land in the middle of `hub:sync-*`, hence the idle callback. ⭐ And correct the header at engineWarmup.ts:21-28 in the same commit — it is currently a comment that describes a flow the product does not have, which is the exact defect class this repo keeps re-recording. NOTE: once the gate is split (finding 1) this drops to a nice-to-have — the boot then runs behind a globe the user is already typing into.",
      "risk": "low",
      "confidence": "high"
    },
    {
      "title": "(b) LOCATED: the boot-step instrument covers ONE of six boot stages, and the stage it does cover is the only one already proven cheap",
      "detail": "`grep -rln 'bootStep\\|beginBootLeg'` over apps/packages/plugins/src returns exactly three non-test files: `bootStepProfile.ts`, `engineLauncher.ts` (7 calls), `initBuilders.ts` (29 calls). The single leg is opened at `engineLauncher.ts:320` `beginBootLeg('builders')` and closed at `:460` `endBootLeg('builders')` — it tiles `boot:scene-done → boot:builders-done` ONLY. There is NO leg over `boot:engine-start → boot:scene-done` (which contains `initScene` plus everything `engineLauncher` does at :208-:311, including `new SchedulePanel(...)` at :299 and `new DataWorkbench(...)` at :301), none over `→ boot:tools-done`, none over `→ boot:bus-handlers-done`, none over `→ boot:data-platform-done`, none over `→ boot:ui-done` (which is where `Layout.ts:92` `const gis = mountGISArea(props, runtime);` runs). Meanwhile the leg that DOES exist covers `initBuilders`, whose 51 constructor steps were measured at 11.4 ms total by the harness cited in `bootStepProfile.ts:32-40`. So the instrument is aimed at the one stage already known to be innocent, and the five stages with no instrument are where `hydrateToCanvas → hydrateToUsable` (~4,496 ms measured) lives. An instrument that cannot see five sixths of its subject cannot be used to say 'the boot is fine'.",
      "onCriticalPath": false,
      "costMs": "the UNINSTRUMENTED span is ~4,496 ms MEASURED (`hydrateToUsable` − `hydrateToCanvas`, median, n=5); the instrument itself costs one `performance.now()` + one array push per step",
      "costBasis": "repo-artifact",
      "quote": "    beginBootLeg('builders');",
      "file": "apps/editor/src/engine/engineLauncher.ts",
      "line": 320,
      "fix": "Open a leg per `markStartupPhase` boot stage, using the module exactly as designed — `beginBootLeg('scene')` before `initScene` and `endBootLeg('scene')` after it, and the same for tools / bus-handlers / data-platform / ui — so every `boot:*` delta the founder pastes has a tiled table underneath it with the own/foreign/idle split. The `await:`-prefix convention already distinguishes 'our CPU' from 'suspended while somebody else ran', which is precisely the question nobody has been able to answer about the 18,626 ms. Zero behaviour change; the module is already unconditional and already guarded against throwing.",
      "risk": "low",
      "confidence": "high"
    },
    {
      "title": "(b) The tail-blindness IS real, and the spec written to catch it asserts a tautology",
      "detail": "`bootStepProfile.ts:229` `const wallMs = state.lastAtMs - state.startedAtMs;` — `lastAtMs` is set only by `bootStep()` (`:168`), never by `endBootLeg`. So anything between the final `bootStep()` and `endBootLeg()` is absent from BOTH the rows and the wall total, silently, with the table still summing to the printed wall and therefore still LOOKING complete. At HEAD that tail happens to be harmless (`initBuilders.ts:1248` `bootStep('teardown wiring + window publish')` is followed only by the return literal), which is exactly why it will not stay harmless: the next `await` added at the end of any leg vanishes. The guard that should prevent this is `__tests__/bootStepProfile.spec.ts:59` — its own comment reads 'The rows must sum to the leg's own wall time, or a cost can sit between two rows and be invisible in a table that still looks complete' — and its assertion at `:71` is `expect(sum).toBeGreaterThanOrEqual(0);`. That cannot fail. The test never does work after the last `bootStep()`, so it never constructs the shape it was written for. §gate-blind-on-the-wrong-axis, in the file whose entire purpose is not being fooled by an instrument.",
      "onCriticalPath": false,
      "costMs": "0 ms hidden at HEAD (VERIFIED by reading initBuilders.ts:1249-1306 — a return literal only); the defect is latent, not currently costing time",
      "costBasis": "read-from-code",
      "quote": "        const wallMs = state.lastAtMs - state.startedAtMs;",
      "file": "apps/editor/src/engine/bootStepProfile.ts",
      "line": 229,
      "fix": "Make the omission structurally impossible rather than currently harmless: in `endBootLeg`, take `const at = now()` and, if `at > state.lastAtMs + epsilon`, push a synthetic final row named `'(untiled tail — no bootStep before endBootLeg)'` before computing `wallMs = at - state.startedAtMs`. A row whose NAME is the accusation cannot be misread. Then fix the spec so it earns its comment: `beginBootLeg` → `bootStep('a')` → burn a measurable interval → `endBootLeg`, and assert the printed wall equals the summed rows within clock granularity. While there, `bootStep('await:initBuilders')` at `engineLauncher.ts:457` would also stop `initBuilders`' 419-455 preamble from being mis-attributed into the row named `'column'` (initBuilders.ts:341 currently closes a segment that started back at engineLauncher.ts:418).",
      "risk": "low",
      "confidence": "high"
    },
    {
      "title": "(c) `vendor-web-ifc` IS on the initial page-load static graph — via an import edge the prior audit did not name, so its planned fix would have changed nothing",
      "detail": "MEASURED from this tree's dist (built 2026-09-06 20:13, 3 days behind HEAD). `dist/index.html:354` `<link rel=\"modulepreload\" crossorigin href=\"/assets/vendor-web-ifc-zl-EjlDS.js\">` — Vite emits modulepreload only for the ENTRY's static import graph. The chain, every hop verified in the built output: `main-*.js` statically imports `./domain-engine-*.js`; `domain-engine-*.js` statically imports `./vendor-thatopen-*.js`; `vendor-thatopen-*.js` statically imports `./vendor-web-ifc-*.js`. In source: `packages/core-app-model/src/BimWorld.ts:1` `import * as OBC from '@thatopen/components';` (and `ArchitectureFragments.ts:2`), both re-exported from the BARREL at `packages/core-app-model/src/index.ts:904` and `:897`, so any consumer of any core-app-model symbol drags it; and `node_modules/@thatopen/components/dist/index.mjs` carries a top-level `from \"web-ifc\"`. ⛔ `audit/perf/2026-09-02/diagnosis.md:90` and its ranked fix #7 attribute this to `packages/file-format/src/export/ifc/*` — `import * as WEBIFC from 'web-ifc'`. That edge is REAL (`packages/file-format/src/index.ts:266-270` re-exports `exportIFC`/`auditIfcWorkflow`/`IfcExporter`) but it lands in `index-kesse6w2.js`, which is NOT in the initial closure. There are TWO independent edges; cutting the named one alone leaves the modulepreload exactly where it is. §three-invalidation-gates-in-series.",
      "onCriticalPath": false,
      "costMs": "3,556,771 B raw / 400,621 B gz of a 20.63 MB / ~5.6 MB gz initial graph. Its module EVALUATION is only 37 ms self (cpuprofile) — the cost is download + V8 pre-parse + the lazy-compile share of the 5,777 ms `(program)`, which is ESTIMATED at 150-400 ms and not separable from that bucket without a Chrome `Compile Script` breakdown",
      "costBasis": "read-from-code",
      "quote": "    <link rel=\"modulepreload\" crossorigin href=\"/assets/vendor-web-ifc-zl-EjlDS.js\">",
      "file": "dist/index.html",
      "line": 354,
      "fix": "Cut BOTH edges or neither. (a) In `BimWorld.ts` / `ArchitectureFragments.ts`, replace the top-level `import * as OBC from '@thatopen/components'` with a dynamic `await import('@thatopen/components')` inside the factory — `createBimWorld` is only ever called from `initScene`, i.e. from inside the engine boot, so the vendor moves out of `domain-engine`'s static graph and into the engine chunk where it belongs. If a dynamic import there is judged too invasive, the alternative is to stop the `core-app-model` barrel from re-exporting them (`index.ts:897`, `:904` → subpath-only `@pryzm/core-app-model/bim-world`) AND add a `manualChunks` rule routing `BimWorld.ts`/`ArchitectureFragments.ts` out of the `domain-engine` group — both halves, since the manualChunk at `vite.config.ts:519` currently forces every core-app-model file into `domain-engine` regardless of the barrel. (b) Then do the already-planned file-format fix. VERIFY WITH THE ARTEFACT, not with reasoning: rebuild and re-grep `dist/index.html` for the `vendor-web-ifc` modulepreload — its absence is the proof, and its presence after a 'fix' is the whole point of checking.",
      "risk": "medium",
      "confidence": "high"
    },
    {
      "title": "(c) The globe screen's genuine code requirement is ~96 KB gz; the click fetches 1.81 MB gz of engine and the page already carries ~5.6 MB gz",
      "detail": "MEASURED by computing the transitive static-import closure of each chunk in `dist/assets` (script over the built output; raw bytes from stat, gz at level 6). INITIAL static graph (entry + the six modulepreloads at dist/index.html:352-357) = 7 files, 21,628,502 B raw / ~5.6 MB gz: main 9,357,174 (2,364,180 gz), domain-engine 4,494,087 (1,244,580 gz), vendor-web-ifc 3,556,771 (400,621 gz), vendor-thatopen 2,285,651 (654,366 gz), vendor-three 1,865,583 (531,007 gz), vendor-three-bvh 68,525. ENGINE INCREMENT on the New Project click (engineLauncher's static closure minus the initial graph) = 20 files, 6,679,545 B raw / 1,895,446 B gz — engineLauncher 4,899,541 plus vendor-pdfjs 414,889, index-BoLl_3hR 353,389, FacadePhotoReference 244,688, ProjectLifecycleController 207,761, PlatformShell 120,125, vendor-dxf 98,584, jszip 97,158, TitleBlockValues 71,487, ifc-tree-view, facadeRaster, DataCommandCenter and eight smaller. CESIUM VIEWPORT INCREMENT = 4 files, 340,696 B raw / ~95,841 B gz for `CesiumViewport-*.js` alone (plus siteRealData 28 KB, solarCodec 12 KB, climateChartData 2 KB) — and its closure contains NO three.js, NO @thatopen, NO web-ifc. A PDF renderer, a DXF parser, a ZIP library, a title-block editor and a facade photo panel are all fetched before the founder can type a city name.",
      "onCriticalPath": true,
      "costMs": "1,895,446 B gz on the click — on his repeat session served from the SW cache, so ~0 ms of network but the full lazy-compile share of the CPU. Download cost on a cold profile MEASURED as `(idle)` 1,718 ms of 15,899 ms",
      "costBasis": "read-from-code",
      "quote": "engine increment: files 20, bytes 6679545 (6.37 MB raw), gz 1895446 — vs CesiumViewport increment: files 4, bytes 340696 (0.32 MB raw)",
      "file": "vite.config.ts",
      "line": 503,
      "fix": "Do NOT attack this with chunk surgery first — finding 3 shows the bytes are cached and warm was slower. The correct sequence is: split the gate (finding 1), which makes the entire 1.81 MB gz leg genuinely off-critical-path — started at the click, awaited only when the user submits a location; THEN prune what is provably not engine-boot code from `engineLauncher`'s STATIC closure (vendor-pdfjs, jszip, vendor-dxf, FacadePhotoReference, TitleBlockValues, ifc-tree-view are all export/import/panel surfaces that belong behind the dynamic imports `initUI` already uses for `PdfExportService`), so the deferred leg is smaller too. Re-measure with the same closure script against the rebuilt dist; a chunk-graph claim must be read off the artefact, never off the import statements.",
      "risk": "low",
      "confidence": "high"
    },
    {
      "title": "(e) `/cesium/Cesium.js` is a render-blocking classic `<script>` in `<head>` — 5.91 MB raw / 1.73 MB gz, and it is the real floor for a COLD load",
      "detail": "`dist/index.html:5` `<script src=\"/cesium/Cesium.js\"></script>` — no `defer`, no `async`, not a module, first thing after the widgets stylesheet. It is injected by the build (the source `index.html` has no such tag), so it is invisible to anyone reading the repo's own index.html. `dist/cesium/Cesium.js` is 5,909,848 B raw / 1,728,348 B gz; `du -sh dist/cesium` is 14 MB with Assets/Workers/Widgets. Its EXECUTION blocks HTML parsing, so nothing paints until 1.73 MB gz has arrived and evaluated (cpuprofile: 165 ms self evaluation — the download and parse are on top). This is the one large asset the globe screen genuinely NEEDS, which is the point: it is the floor, and it is currently paid in the most blocking way available.",
      "onCriticalPath": true,
      "costMs": "165 ms MEASURED evaluation self-time (cpuprofile); download + parse ESTIMATED 400-1,200 ms cold on a typical connection, UNKNOWN precisely without a HAR from the founder's machine",
      "costBasis": "read-from-code",
      "quote": "    <script src=\"/cesium/Cesium.js\"></script>",
      "file": "dist/index.html",
      "line": 5,
      "fix": "Make it `<script src=\"/cesium/Cesium.js\" defer></script>` and have `prewarmGlobe`'s loader await `window.Cesium` (a tiny readiness promise beside `warmCesiumModule()` in `cesiumWarmup.ts`) instead of assuming the global is present. `defer` preserves execution order and still executes before DOMContentLoaded, so nothing that runs after DCL changes — but the HTML parses and the shell paints without waiting for 1.73 MB. ⛔ Do NOT use `async`: that would let it execute at an arbitrary point relative to the module graph, and the globals contract would become a race. Verify by rebuilding and re-reading dist/index.html: this tag is emitted by a build plugin, so the change belongs where that plugin writes it, not in the source index.html.",
      "risk": "low",
      "confidence": "medium"
    },
    {
      "title": "(a)(d) The globe is already built, mounted and streaming when the click lands — the only thing missing is who is allowed to reveal it",
      "detail": "`eagerGlobeStart.ts:178` `export function prewarmGlobe(opts: GlobePrewarmOptions): void` is invoked from `PlatformRouter.ts:753` in `showOnboarding()` — the same synchronous block as `ensureEngineWarm()`. It dynamically imports `CesiumViewport`, constructs it against `#container` (a STATIC element of index.html at `index.html:425`, inside `#dck-workspace` at `:423`, so it is laid out with real dimensions at that moment), calls `globe.enterWarmHiddenState()` (`CesiumViewport.ts:18678` — `display:block; visibility:hidden; pointer-events:none`, deliberately laid out so tiles stream) and awaits `globe.mount()`, then marks `globe:prewarm-done`. The founder's own log line `[gis][cesium] §STARTUP-GLOBE-COMPLETE-FIRST — photoreal initial tiles loaded` is that work finishing. The reveal verb exists on the same object: `CesiumViewport.setVisible(true)` at `:18690`, whose comment states 'leaving the warm-hidden state by EITHER direction restores the container's normal visibility/pointer contract'. What the location step actually calls instead is `window.pryzmToggleGIS` / `pryzmGetSiteEntryCameraHost` / `pryzmGetSiteEntryCameraHostReady` (OnboardingStepController.ts:800-822), and all three are installed at `GISAreaLayout.ts:2625`/`:2645`/`:2651` inside `mountGISArea`, called from `Layout.ts:92`, called from `initUI` — the LAST stage of `bootstrap()`. So the globe waits on the engine for the name of a function, not for anything the engine computes.",
      "onCriticalPath": true,
      "costMs": "prewarm mount duration UNKNOWN on the founder's hardware — the code logs it verbatim ('constructed + mounted WARM-HIDDEN in N ms') and marks `globe:prewarm-start`/`globe:prewarm-done`; the related Cesium pre-warm was MEASURED at 165 ms self in the cpuprofile and diagnosis.md acquits it",
      "costBasis": "read-from-code",
      "quote": "export function prewarmGlobe(opts: GlobePrewarmOptions): void {",
      "file": "apps/editor/src/engine/eagerGlobeStart.ts",
      "line": 178,
      "fix": "This is the mechanism finding 1's fix stands on, and it is why that fix is small rather than a rearchitecture: the prewarm/adopt pair already exists, the reveal verb already exists on the one owner, and `#container` is already laid out at click time. The single new seam is a non-consuming accessor so the onboarding step can hold the viewport before `mountGISArea` adopts it — modelled on `consumePrewarmedGlobe()` (`:244`), which must keep its one-shot semantics so there can never be two viewports in `#container`. ⛔ Do NOT install the three `window.pryzm*` globals from the prewarmer as a shortcut: `toggleGIS` closes over layout state that only `mountGISArea` owns, and a second writer of the container's geometry is the §View-region-has-ONE-owner oscillation defect. Pass the typed viewport handle down; leave the globals to their one installer.",
      "risk": "low",
      "confidence": "high"
    },
    {
      "title": "Three prior boot-cost findings are ALREADY FIXED at HEAD — do not re-report them, and do not budget savings against them",
      "detail": "Checked against `audit/perf/2026-09-02/diagnosis.md`'s ranked fixes because a stale finding would have consumed this lane. (1) Ranked fix #1, 'eager DataWorkbench build ~850 ms': `DataWorkbench.ts:325` now reads '§PERF-DW-LAZY-BUILD — no _buildDOM() here; see _ensureDomBuilt().' and the constructor at `:322` no longer builds. FIXED. (2) Ranked fix #4, 'theme CSS scaling ~437 ms': `AppTheme.ts:121` `let _scaledCssCache: { scale: number; css: string } | null = null;` with the guard at `:139`. FIXED. (3) Ranked fix #5, 'boot default-viewpoint snapshot 560 ms': `initViewpointsPanel.ts` now carries '§PERF-VIEWPOINT-SNAPSHOT-DEFER' and captures inside a `requestIdleCallback(..., { timeout: 8000 })`. FIXED. Still OPEN from that list and relevant here: #7 web-ifc (see above, and its stated edge is the wrong one) and #8 shader/pipeline compile ~1.2 s (renderer lane, not mine). ⚠ The ~7,200 ms `axisC` baseline PREDATES all three of these fixes (measured 2026-09-02), so the founder's 9,233 ms on 2026-09-09 is what remains AFTER roughly 1.8 s of already-landed savings — which strengthens rather than weakens the case that the residue is structural.",
      "onCriticalPath": false,
      "costMs": "~1,847 ms MEASURED and ALREADY REMOVED (850 + 437 + 560) — 0 ms of remaining opportunity",
      "costBasis": "read-from-code",
      "quote": "        // §PERF-DW-LAZY-BUILD — no _buildDOM() here; see _ensureDomBuilt().",
      "file": "apps/editor/src/ui/dataworkbench/DataWorkbench.ts",
      "line": 325,
      "fix": "No action. Recorded so the next reader does not re-cost them, and so nobody subtracts 1.8 s twice. It also means the 2026-09-02 baseline is a CEILING for today's boot, not a current reading — re-run `tools/perf/outer` before quoting `axisC` as a present-tense number.",
      "risk": "none",
      "confidence": "high"
    }
  ],
  "whatIcouldNotEstablish": "1. THE PER-LEG SPLIT OF THE FOUNDER'S OWN 9,233 ms. He pasted one `[§STARTUP-BUDGET]` line; `markStartupPhase` (startupBudget.ts:270-291) logs EVERY mark immediately, so his console already contains `onboarding:shown · globe:prewarm-start/-done · hub:open-clicked · open:router-launch · boot:ensure-requested · boot:heavy-wiring-done · boot:engine-start · boot:scene-done · boot:builders-done · boot:tools-done · boot:bus-handlers-done · boot:data-platform-done · boot:ui-done · open:shell-context-set · open:version-read-start/-done · open:snapshot-loaded · open:first-interactive-frame · location-step:open`. CHEAPEST PROBE, zero code change: ask him to paste that contiguous block. Note the known gap — `reportStartupBudget()` (the console.table) is called from exactly one place, `enterCanvasWithSitePlan`, so the TABLE only prints at the very end of the flow; the individual lines are what to ask for. I substituted `audit/perf/2026-09-02/baseline-outer.json` `axisC-local`, which measures the same gesture on the same codebase, and said so at every use.\n\n2. WHETHER HIS RUN WAS SERVICE-WORKER-WARM. It determines how much of the ~2,681 ms 'click → canvas visible' leg is network versus CPU. Probe: the same paste, plus one DevTools Network screenshot filtered to `engineLauncher` — 'from ServiceWorker' or a transfer size answers it in one line.\n\n3. THE PREWARM MOUNT DURATION ON HIS HARDWARE — i.e. how early the globe was actually ready. `eagerGlobeStart.ts` logs 'constructed + mounted WARM-HIDDEN in N ms' and marks `globe:prewarm-start`/`-done`. This is the number that turns finding 1's fix from 'much faster' into a stated millisecond figure, and it is already in his console.\n\n4. THE COMPILE SHARE OF `(program)`. `(program)` self = 5,777 ms is MEASURED, but V8 does not attribute it per script in a .cpuprofile, so I cannot say how much of it is web-ifc's pre-parse versus lazy compilation of engineLauncher versus browser internals. I attempted a `vm.Script` compile bench over the real chunks and DISCARDED it: stripping ESM syntax to make them parse as classic scripts produced syntax errors that the timer silently swallowed (0 ms readings), so the numbers were meaningless. The honest probe is a Chrome Performance recording with 'Compile Script' / 'Evaluate Script' rows expanded, which attributes per URL.\n\n5. THE 18,626 ms `boot:builders-done` DELTA ITSELF. I established WHY it was never located (five of six stages have no leg; the one leg covers the stage measured at 11.4 ms) and I falsified the brief's stated mechanism, but I did not reproduce the number — it was measured opening a project WITH data, and the founder's current complaint is an empty one. Reproducing it needs a run on that path with per-stage legs installed (finding 5's fix).\n\n6. THE 71 MESHES / CINEMATIC-TIER QUESTION and the ~1.2 s shader/pipeline compile are inside `bootstrap()` and therefore inside the window I am accounting for, but they belong to the renderer dimension; I have not costed them here beyond noting `diagnosis.md`'s measured 1.2 s and that its program-count probe is still owed.\n\n7. WHETHER THE ENGINE-LOADING OVERLAY CAN CLEANLY YIELD THE SCREEN. Finding 1's fix requires `_ensureEngineLoadingOverlay()` (PlatformRouter.ts:1003) to stop covering the globe on the guided path. I read enough to know it is show-once and reused by `_openProjectViaRuntime`, but I did not trace its z-index and dismissal contract against `--location`-class overlay and `resetAppPhaseForNewProject()`. That is the one place the fix could go wrong quietly, and it is where implementation review should concentrate."
}