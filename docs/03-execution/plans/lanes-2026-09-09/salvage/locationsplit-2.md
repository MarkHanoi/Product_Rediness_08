{
  "dimension": "Geocode leg — 4,277 ms to turn \"bARCELONA\" into a lat/lon (`geocode:start` → `geocode:end`)",
  "summary": "THE GEOCODE IS THE SINGLE LARGEST ITEM ON THE BLOCKING PATH, AND IT IS FIRST. Nothing downstream can start without a lat/lon: `search()` awaits the geocode at GlobeHeroSearch.ts:255 and only then runs `descendAndHandOff` (the descent, the city-stage context warm, and the §22 reveal). So unlike roads (15.3 s) or the parks drape wait (8.0 s), which stream in AFTER `reveal:split-mounted`, every one of these 4,277 ms is time the founder stares at \"Searching…\". Against the honest number — `geocode:start` t+107079 → `reveal:split-mounted` t+113012 = 5,933 ms to a usable split — the geocode is 72.1% of it. Against the full window to `content-ready` (11,495 ms) it is 37.2%.\n\n⭐ AND IT IS NOT STRUCTURAL. Between the two marks the code does one `fetch` and one `res.json()` over ≤5 rows (geocodeAddress.ts:125,141) — there is nothing of ours in the interval. An INDEPENDENT PRIOR MEASUREMENT of the same two marks reads 261 ms (ISSUE-LOG L-753, 2026-08-07). Same code, same query, same provider, 16.4× faster. So 4,277 ms is upstream latency or upstream throttling, not our compute.\n\n⭐⭐ THE MECHANISM WITH EVIDENCE, AND IT IS A POLICY BREACH WE DOCUMENTED AND THEN DID NOT HONOUR. Nominatim's usage policy states verbatim: \"Results must be cached on your side. Clients sending repeatedly the same query may be classified as faulty and blocked.\" WE HAVE NO CACHE — not in `geocodeAddress.ts`, not in the second call site, not shared, not on the server. The founder's browser sends the identical \"Barcelona\" from the identical IP on every single startup. The named penalty is exactly the symptom. This repo has ALREADY diagnosed and fixed this precise shape once: `server/overpassProxy.js`'s own header says the public mirrors \"rate-limit PER CLIENT IP and routinely answer 429 or time out under the founder's heavy demo testing\" — and the cure was a same-origin proxy with a shared server-side cache. The geocode is the last leg of the location step still going browser→third-party direct, uncached; its sibling branch in the SAME function (`handleCadastralEntry` → `lookupParcelByRefcat`) already goes through a same-origin proxy with a 7-day cache, a timeout and one retry.\n\n⛔ TWO OF THE FOUR OPTIONS IN THE BRIEF ARE OFF THE TABLE, AND I CHECKED RATHER THAN ASSUMED. (1) Debounce-as-you-type prefetch against Nominatim is EXPLICITLY forbidden: \"Auto-complete search — This is not yet supported by Nominatim and you must not implement such a service on the client side using the API\", listed under Unacceptable Use. (2) \"Ship a cached list of major cities\" must NOT reuse the coverage registry that is already in the bundle: Barcelona's entry is declared `extentResolution: 'metropolitan'` over a box its own registration calls \"a PROXIMITY GATE ~44 × 42 km, four times the municipality\", and that bbox is threaded to `pryzmSetGeocodeFrame` to seed the 2D cadastral pane's opening frame — using it would open the pane at 44 km instead of city scale.\n\nTHE ARCHITECTURALLY SOUND FIX IS A COPY OF A MODULE THAT ALREADY SHIPS, NOT A REARCHITECTURE: a same-origin `/api/geocode` proxy with a shared TTL cache, a timeout and one retry, modelled on `server/overpassProxy.js` / `server/jurisdiction/parcelZoningProxy.js`. Warm hit ≈ 250 ms (repo-measured same-origin browser→Fly round trip, L-13111). That is 4,277 → ~250 ms, ~17×, and it takes the blocking window from 5,933 → ~1,900 ms. It also makes us COMPLIANT (we start caching, and the server can send the descriptive `User-Agent` the browser is forbidden to set), and it is what unlocks a policy-clean as-you-type resolve — against OUR cache only, with NO upstream leg, which on the founder's 97.8 s of dwell would make the geocode cost 0 ms.",
  "findings": [
    {
      "title": "RANK 1 — the geocode is 72% of the time-to-usable-split, and it is the FIRST thing on the blocking path",
      "detail": "The brief's decisive distinction, settled from code rather than from the timeline. `GlobeHeroSearch.search()` awaits the geocode and only then calls `descendAndHandOff(best)`, which runs the world→country→city→parcel descent, kicks the city-stage context warm, and fires `onParcelArrival` → `revealSplitAtParcel`. Nothing in that chain can begin without a lat/lon, so the geocode is not merely on the critical path — it gates the entire remainder of it.\n\nARITHMETIC ON THE FOUNDER'S OWN MARKS: `geocode:start` t+107079 → `geocode:end` t+111357 = 4,277 ms. `geocode:start` → `reveal:split-mounted` t+113012 = 5,933 ms ⇒ the geocode is 72.1% of the wait to a mounted split. `geocode:start` → `reveal:content-ready` t+118574 = 11,495 ms ⇒ 37.2% of the full window.\n\n⭐ THIS IS THE RANKING FACT THE BRIEF ASKED FOR. Roads (15,314 ms) and the parks drape wait (8,037 ms) are downstream of `reveal:split-mounted` and stream in behind a split the founder can already use (§STARTUP-REVEAL-NOT-GATED-ON-CONTEXT). These 4,277 ms are not. Removing 4.0 s here is worth more than removing 15 s there.\n\n⭐ AND THE MARK SEMANTICS ARE CONFIRMED, NOT ASSUMED: `startupBudget.ts:283` computes `sincePrevMs: at - prevAt`, so the printed `+4277ms` is genuinely the interval between the two marks and not a cumulative figure.",
      "blocksReveal": true,
      "costMs": "4,277 ms — MEASURED (founder §STARTUP-BUDGET log). 72.1% of the 5,933 ms to reveal:split-mounted; 37.2% of the 11,495 ms to reveal:content-ready.",
      "costBasis": "founder-log",
      "quote": "geocode:end +4277ms (t+111357ms)",
      "file": "apps/editor/src/ui/onboarding/GlobeHeroSearch.ts",
      "line": 255,
      "fix": "No fix in itself — this is the ranking. It says the geocode leg must be treated as the highest-value target in the whole startup audit, ahead of every context-layer read, because it is the only multi-second item the user is actually waiting on before the split appears.",
      "risk": "none",
      "confidence": "high"
    },
    {
      "title": "RANK 1 — THERE IS NO CACHE ANYWHERE, and Nominatim's policy says caching is mandatory and names blocking as the penalty",
      "detail": "`geocodeAddress()` builds a URL, fetches it, parses it, returns. No memo, no `Map`, no `sessionStorage`, no server cache, no cache header respected. Two independent call sites, neither caching, no shared store between them. Every startup, every reload, every retype of \"Barcelona\" from the founder's machine is a fresh request to the public endpoint from HIS IP.\n\n⭐ THE POLICY, FETCHED AND QUOTED VERBATIM (operations.osmfoundation.org/policies/nominatim/): \"Results must be cached on your side. Clients sending repeatedly the same query may be classified as faulty and blocked.\" That is a MUST clause, and the penalty it names — degraded/blocked service for repeat identical queries — is precisely the symptom: 261 ms on 2026-08-07, 4,277 ms on 2026-09-09, same query, same code.\n\n⛔ AND OUR OWN FILE HEADER CITES THE POLICY WHILE THE CODE BREACHES IT. geocodeAddress.ts:21-29 walks through the User-Agent clause, the bulk-use clause and the `limit=5` fair-use clause — and never mentions the caching clause at all.\n\n⭐ THE REPO HAS ALREADY DIAGNOSED AND FIXED THIS EXACT SHAPE FOR A SIBLING SERVICE. `server/overpassProxy.js`'s header: \"Those mirrors rate-limit PER CLIENT IP and routinely answer 429 Too Many Requests or time out under the founder's heavy demo testing\" — cured with \"a same-origin proxy with a SHARED server-side cache\" (24 h TTL, bounded LRU, timeout + one retry). `server/jurisdiction/parcelZoningProxy.js` does the same for Catastro at a 7-day TTL — and that is the proxy the CADASTRAL BRANCH OF THIS VERY FUNCTION already uses (`handleCadastralEntry` → `lookupParcelByRefcat`). The place-name branch, twenty lines away, has none of it.\n\n⭐ THE DUPLICATION IN THIS DIMENSION IS TEMPORAL, NOT PER-LAYER. The geocode is not done seven times in one run — it is done once per run, forever, for the same handful of cities, with no memory between runs. Across a demo day that is dozens of identical requests to a service whose policy says repeat identical requests get you blocked.",
      "blocksReveal": true,
      "costMs": "Up to ~4,000 ms of the 4,277 ms — MEASURED as the delta between two runs of the same code (4,277 ms today vs 261 ms at L-753), ATTRIBUTED to this mechanism with MEDIUM confidence. Warm-hit target ~250 ms (repo-artifact: L-13111's measured browser→Fly same-origin probe, 242 ms for a 38-byte response).",
      "costBasis": "founder-log",
      "quote": "geocode:start 4 820 · geocode:end 261",
      "file": "apps/editor/src/ui/site/geocodeAddress.ts",
      "line": 125,
      "fix": "Add `server/geocodeProxy.js` — a near-copy of `server/overpassProxy.js`: same-origin `GET /api/geocode?q=…`, SHA-256-of-normalised-query cache key, long TTL (a city centroid is stable — 7 days matches `PARCEL_CACHE_TTL_MS`), short TTL on empty results (mirror `OVERPASS_EMPTY_CACHE_TTL_MS` = 5 min so a transient miss self-heals), bounded LRU, upstream timeout + one retry, stale-while-revalidate so a slow upstream never blocks a query we have already seen. Point the client at it. Then: (1) we become policy-compliant on the caching MUST; (2) the server can send the descriptive `User-Agent` the policy asks for and the browser is forbidden to set; (3) upstream sees ONE request per distinct query for the whole fleet instead of one per user per startup.\n\n⚠ FOUR REAL COSTS, STATED NOT HIDDEN. (a) A GENUINE COLD MISS IS NOT IMPROVED and is ~242 ms WORSE — the extra hop. Only the timeout/retry and stale-serve mitigate that. (b) `VITE_GEOCODE_ENDPOINT` is BUILD-TIME-INLINED (SECRETS-REGISTER.md:139), so repointing needs a rebuild, not a config flip. (c) `server/__tests__/jurisdictionRouter.test.ts` asserts exact per-path route counts — a new route must move it in the SAME commit (the L-13111 precedent). (d) Keep `nominatim.openstreetmap.org` in the CSP `connect-src` so the direct-to-provider fallback still works when the proxy is unreachable, exactly as overpassProxy does.",
      "risk": "low",
      "confidence": "high"
    },
    {
      "title": "NO TIMEOUT, NO ABORT, NO RETRY — a hung provider leaves the founder on \"Searching…\" with a dead button, indefinitely",
      "detail": "`await fetch(url, { method: 'GET', headers: { Accept: 'application/json' } })` — no `AbortSignal`, no `AbortController`, no deadline. The `catch` at :129 only fires on a network ERROR, never on slowness. There is no retry either.\n\nWHAT THE USER SEES WHILE IT HANGS, read from the code and not assumed: `status.hidden = false`, `status.textContent = 'Searching…'`, `submitBtn.disabled = true` (OnboardingStepController.ts:1424-1426), all set synchronously before the await, so they DO paint. Feedback therefore already exists — the brief asked me to establish that before proposing any, and I am not proposing any. But the button stays disabled until the promise settles, and with no deadline that means until the browser's own network timeout (~300 s in Chrome). There is no cancel, no \"taking longer than usual\", no fallback.\n\n⭐ BOTH SIBLING PROXIES ALREADY HAVE THE DISCIPLINE THIS LACKS: parcelZoningProxy.js:63 \"Per-upstream timeout (ms). The server pays it ONCE per plot (then cached)\" and :269 \"Fetch a URL once with a timeout + one retry\". The pattern is written, tested and deployed three directories away.\n\n⚠ HONEST PRICE: on the measured run this cost 0 ms — the request returned. This is a robustness finding, not a latency one, and I am not going to inflate it. Its value is that a 4.3 s tail today is a 300 s hang tomorrow, on the first thing the user waits for.",
      "blocksReveal": true,
      "costMs": "0 ms on the measured run (the request returned) — MEASURED. Unbounded when the provider hangs; the whole reveal is behind it.",
      "costBasis": "read-from-code",
      "quote": "res = await fetch(url, {",
      "file": "apps/editor/src/ui/site/geocodeAddress.ts",
      "line": 125,
      "fix": "Wrap the fetch in an `AbortController` with a deadline (the sibling proxies' `CATASTRO_UPSTREAM_TIMEOUT_MS` is the precedent to copy the shape from, not the number — pick one from a measured p95, per the tolerance-from-measured-error rule). On abort, return `[]` on the existing never-throws contract so the UI's \"Location lookup failed — you can skip to use the default site\" path fires and the submit button re-enables. If the proxy lands, the deadline belongs on the SERVER leg (where a retry is also legal and cheap) and the client keeps a longer outer deadline as a backstop. ⛔ Do NOT add a spinner or a progress fiction — feedback already exists and the founder said no hiding latency.",
      "risk": "none",
      "confidence": "high"
    },
    {
      "title": "NO PRECONNECT to the geocode origin — every submit pays a cold DNS + TCP + TLS handshake that 97.8 s of dwell could have absorbed",
      "detail": "`index.html` preconnects exactly two origins, both for fonts (lines 14-15). `nominatim.openstreetmap.org` is not among them, and no `dns-prefetch` exists for it either. So the first byte of the geocode request cannot leave until DNS resolves, TCP connects and TLS negotiates — none of which has anything to do with how fast Nominatim searches.\n\n⭐ THE DWELL MAKES THIS FREE TO FIX. The founder's own mark is `geocode:start +97847ms` — 97.8 seconds sitting on the location step before he pressed Enter. A preconnect issued at `location-step:open` would be warm many times over by submit.\n\n⭐ AND IT IS UNAMBIGUOUSLY INSIDE THE POLICY, WHICH MATTERS BECAUSE THE ADJACENT IDEA IS NOT. A `<link rel=\"preconnect\">` performs DNS + TCP + TLS and issues ZERO HTTP requests. It cannot breach the \"absolute maximum of 1 request per second\" limit and it is not auto-complete querying. This is the one \"start earlier\" move available today that needs no permission from anybody.\n\n⚠ COST BASIS IS ESTIMATED AND I AM LABELLING IT AS SUCH. DNS (0-200 ms, cache-dependent) + TCP (~1 RTT) + TLS 1.3 (~1 RTT) from Europe is typically 100-350 ms. I have no measurement of this specific handshake and I am not going to invent one. It is a small, safe, honest slice of the 4,277 ms — not the bulk of it.\n\n⚠ AND IT IS SUPERSEDED BY THE PROXY. If `/api/geocode` lands, the geocode becomes same-origin over a connection the app has already been using since boot, and this finding evaporates. Do the preconnect only as the immediate, zero-risk stopgap, or skip it if the proxy ships in the same week.",
      "blocksReveal": true,
      "costMs": "~100-350 ms — ESTIMATED (standard DNS+TCP+TLS from Europe). Not separately measured on the founder's run.",
      "costBasis": "estimated",
      "quote": "<link rel=\"preconnect\" href=\"https://fonts.googleapis.com\" />",
      "file": "index.html",
      "line": 14,
      "fix": "Inject `<link rel=\"preconnect\" href=\"https://nominatim.openstreetmap.org\" crossorigin>` when `location-step:open` fires, rather than statically in `index.html` — a user who never geocodes should not open a connection to a third party. Drop it entirely once `/api/geocode` is same-origin.",
      "risk": "none",
      "confidence": "medium"
    },
    {
      "title": "⛔ DEBOUNCE-AS-YOU-TYPE PREFETCH AGAINST NOMINATIM IS EXPLICITLY FORBIDDEN — the brief's option (c-1), checked and refused",
      "detail": "The brief told me to check the policy before proposing prefetch. I did. Verbatim, under \"Unacceptable Use\": \"Auto-complete search — This is not yet supported by Nominatim and you must not implement such a service on the client side using the API.\"\n\nSo the highest-leverage-sounding option — resolve \"Barcelona\" while he is still typing, using the 97.8 s of dwell — is not available against this provider at any debounce interval. The other binding limits, for completeness: \"No heavy uses (an absolute maximum of 1 request per second)\"; \"Provide a valid HTTP Referer or User-Agent identifying the application\"; and no SLA — \"this usage policy may change without notice\" and \"you might have your access withdrawn\".\n\n⭐ WHAT THE POLICY DOES PERMIT, AND IT IS THE WHOLE PRIZE. The prohibition is on issuing REQUESTS from keystrokes. Resolving a keystroke against a cache that has NO upstream leg issues no request and is therefore not auto-complete search in the policy's sense. Once `/api/geocode` exists, a debounced (≥300 ms idle) lookup against a CACHE-ONLY endpoint answers \"Barcelona\" from memory before the founder presses Enter, and answers \"unknown\" for anything unseen — at which point Enter takes the normal path. On his run that would make `geocode:end - geocode:start` ≈ 0 ms.\n\n⛔ AND THE FAILURE MODE MUST BE DESIGNED OUT STRUCTURALLY, NOT FLAGGED. If anyone later \"improves\" the cache-miss branch to fall through to upstream, we are issuing one Nominatim request per keystroke and we get blocked — the exact outcome this finding exists to prevent. The miss path must be inert BY CONSTRUCTION: a separate route (`/api/geocode/cached`) that has no upstream client wired into it at all, so falling through is not a code change anyone could make by accident.\n\n⭐ THE REFERER CLAIM IN OUR HEADER IS ACTUALLY CORRECT — worth saying, because the brief warned that comments here are wrong in both directions. geocodeAddress.ts:22-25 asserts we rely on the browser-supplied Referer. `server/securityHeaders.js:619` sets `referrerPolicy: { policy: 'strict-origin-when-cross-origin' }`, so a cross-origin HTTPS→HTTPS request does send our origin, satisfying \"a valid HTTP Referer identifying the application\". ⚠ But a bare origin is a weak identifier next to the descriptive User-Agent the policy prefers — and only a SERVER can send one, since `User-Agent` is a forbidden header for browser `fetch`. That is a second, non-latency argument for the proxy.",
      "blocksReveal": true,
      "costMs": "0 ms today. The permitted variant (cache-only as-you-type, after the proxy lands) would take the leg from 4,277 ms to ~0 ms on any previously-seen city — ESTIMATED from the mechanism, since the cache does not yet exist to measure.",
      "costBasis": "read-from-code",
      "quote": "Auto-complete search This is not yet supported by Nominatim and you must not implement such a service on the client side using the API.",
      "file": "apps/editor/src/ui/site/geocodeAddress.ts",
      "line": 21,
      "fix": "Do NOT ship keystroke prefetch against Nominatim in any form. Ship the server cache first; then expose a strictly cache-only lookup with no upstream client in scope, and debounce the input against that. Also correct geocodeAddress.ts's header, which walks through four policy clauses and omits the caching MUST that we are breaching.",
      "risk": "medium",
      "confidence": "high"
    },
    {
      "title": "⛔ THE COVERAGE REGISTRY IS ALREADY IN THE BUNDLE AND MUST NOT BE USED AS A GAZETTEER — the brief's option (c-2), checked and refused with the reason",
      "detail": "The tempting shortcut: `siteEntryCoverageEntries()` is already passed into `GlobeHeroSearch` (OnboardingStepController.ts:924), it carries 38 registrations each with a `displayName` and an `extent`, `GlobeHeroSearch` ALREADY has a `flyToResolved({lat, lon, displayName, bbox})` entry point that runs the identical descend/hand-off body, and `displayName: 'Barcelona'` is sitting right there at registry.ts:689. Matching the typed string against that table would resolve in 0 ms, offline, with no new code path.\n\n⛔ IT WOULD BE WRONG, AND THE REGISTRY ITSELF SAYS SO. Barcelona's entry is declared `extentResolution: 'metropolitan'` (registry.ts:702), over `BARCELONA_BBOX` = 41.2-41.6 lat / 1.9-2.4 lon (barcelonaBbox.ts:16-21). The registration's own comment: \"it is a PROXIMITY GATE ~44 x 42 km, four times the municipality, and it fully contains the boxes of L'Hospitalet, Badalona, Sant Boi and Cornellà.\"\n\nTHE CONSEQUENCE IS CONCRETE, NOT THEORETICAL. The geocode `bbox` is not decorative — it is threaded through `revealSplitAtParcel` to `pryzmSetGeocodeFrame` (OnboardingStepController.ts:1081-1089, and again at :1829-1832) whose stated job is to seed the 2D pane's opening frame, \"else it opens at world zoom\". Substituting the metropolitan box would open the 2D cadastral pane — the LEFT half of the founder's screenshot, the one that says \"Click a plot to select its real cadastral parcel\" — at 44 km across instead of city scale. The centre point is also ~2 km off the city centre.\n\n⛔ AND IT WOULD MINT A SECOND GEOCODER. \"Barcelona\" would resolve from the registry while \"Barcelona, Spain\" resolved from Nominatim, to different coordinates and a wildly different bbox. That is the same-rule-two-implementations shape this repo keeps re-learning: the founder reports a bug, someone fixes the copy he is not looking at.\n\n⭐ SO: A SHIPPED CITY LIST IS NOT REFUSED IN PRINCIPLE — REUSING THIS TABLE FOR IT IS. If a bundled gazetteer is ever wanted it needs its OWN artefact with its own provenance (city centroid + a city-scale bbox, sourced and cited), and it still leaves two sources of truth to keep in agreement. The server cache reaches the same latency with ONE source of truth, which is why it is the recommendation and this is not.",
      "blocksReveal": true,
      "costMs": "Would remove all 4,277 ms — and would open the 2D pane at 44 km instead of city scale. Cost basis for the 44 km: READ from registry.ts:702 + barcelonaBbox.ts:16-21.",
      "costBasis": "read-from-code",
      "quote": "extentResolution: 'metropolitan',",
      "file": "packages/site-parcel-data/src/rulepacks/registry.ts",
      "line": 702,
      "fix": "Do not wire the coverage registry into the search box as a geocoder. If a bundled city list is later wanted, mint a separate gazetteer artefact with city-resolution centroids and bboxes and its own provenance — and only after the cache has been tried, because the cache gets the same number without a second source of truth.",
      "risk": "high",
      "confidence": "high"
    },
    {
      "title": "We ask for 5 results and use exactly one, and the disambiguation UI that exists elsewhere is deliberately skipped here",
      "detail": "Answering (e) directly. The URL is built with `limit=5` (geocodeAddress.ts:119) and `GlobeHeroSearch.search()` takes `const best = results[0]!` (GlobeHeroSearch.ts:267) with no user choice. \"bARCELONA\" returned 2; both were parsed, one was used.\n\n⭐ A DISAMBIGUATION STEP DOES EXIST IN THIS CODEBASE — the OTHER geocode call site renders a clickable candidate list (`renderResults` → `li.addEventListener('click', () => pick(r))`, siteGeocodeSearchBox.ts:204). The onboarding path skips it deliberately, and correctly: §ONE-CARD-AT-A-TIME and §STARTUP-NAME-FROM-LOCATION both record the founder removing steps from this exact flow. Picking the first costs 0 ms and saves 0 ms. It is not a time finding, and adding a picker would ADD a step the founder just spent two sessions removing.\n\n⚠ THE TIME ANGLE IS SMALL AND I AM NOT GOING TO OVERSELL IT. `limit=5` makes the provider rank and serialise five candidates when we consume one. `addressdetails=0` is already correctly set (the genuinely expensive flag). The repo's own probe already uses the tighter form: `limit=1&countrycodes=es` (tools/dataset-discovery/probeC.mjs:131). Dropping to `limit=1` is free and reduces upstream work — which matters mainly because the upstream throttles, not because it saves measurable milliseconds on the wire.",
      "blocksReveal": true,
      "costMs": "Unmeasurable — a few KB of response and marginal upstream ranking work. NOT a material share of the 4,277 ms. Cost basis: ESTIMATED, and deliberately not inflated.",
      "costBasis": "estimated",
      "quote": "`${GEOCODE_ENDPOINT}?format=json&limit=5&addressdetails=0&` +",
      "file": "apps/editor/src/ui/site/geocodeAddress.ts",
      "line": 119,
      "fix": "Keep taking `results[0]` — no picker on the onboarding path. Consider `limit=1` for the onboarding caller specifically (the GIS-rail box genuinely needs 5, since it shows a list), passed as a parameter rather than by forking the function. Marginal; bundle it with the proxy commit rather than shipping it alone.",
      "risk": "none",
      "confidence": "high"
    },
    {
      "title": "Two geocode call sites, no shared cache — checked for a startup cost and EXONERATED, but it is a second uncached door to a throttled service",
      "detail": "The brief said to check caller counts before blaming a file. `geocodeAddress` has exactly two production callers: `OnboardingStepController.ts:929` (the startup path) and `siteGeocodeSearchBox.ts:218`, mounted at `GISAreaLayout.ts:1134` inside the site-authoring surface.\n\n⭐ EXONERATED FOR THE MEASURED RUN. The second box is gesture-only — `button.addEventListener('click', () => void runSearch(input.value))` at :230 and an Enter handler at :231-234. It never auto-fires on mount, so it contributes 0 ms to the founder's startup and is not a hidden second request in his 4,277 ms window.\n\n⚠ BUT IT IS A SECOND UNCACHED DOOR. Same public endpoint, no shared memo, and the two behave differently: the GIS box has an `inFlight` re-entrancy guard (:210, :213) that the onboarding path lacks. Type \"Barcelona\" at startup and again in the GIS rail and that is two identical requests from one IP — literally the \"clients sending repeatedly the same query\" the policy penalises.\n\n⚠ TOOLING CAVEAT, LOAD-BEARING FOR ANY CALLER-COUNT CLAIM IN THIS REPO. The ripgrep-backed Grep tool returns ONE hit for `mountSiteGeocodeSearchBox` (a test file). Bash `grep -rn` over the same tree returns FOUR — including the definition and BOTH production wiring lines in `GISAreaLayout.ts`. Ripgrep silently omitted the real call sites. Any \"zero callers\" or \"dead twin\" verdict reached with the Grep tool alone in this repo is unsafe; cross-check with bash grep.",
      "blocksReveal": false,
      "costMs": "0 ms on the startup path — VERIFIED from code (gesture-only, never auto-fired on mount).",
      "costBasis": "read-from-code",
      "quote": "button.addEventListener('click', () => void runSearch(input.value));",
      "file": "apps/editor/src/ui/site/siteGeocodeSearchBox.ts",
      "line": 230,
      "fix": "Point BOTH call sites at the same `/api/geocode` so one cache serves both surfaces and one query means one upstream request. Do not merge the two UI modules — they legitimately differ (list vs first-hit); merge only the transport.",
      "risk": "none",
      "confidence": "high"
    },
    {
      "title": "Every submit fires a 1.6 s Cesium flight to 20,000 km that is thrown away — free on the first search, visibly wrong on a retry",
      "detail": "`search()` synchronously dispatches `site.entry.reset` before the geocode (GlobeHeroSearch.ts:251, comment: \"Fresh chain each search\"). That reduces to a camera effect over `INITIAL_SITE_ENTRY_STATE` (siteEntryModel.ts:619-622), `cameraForState` defaults `instant = false` (siteEntryModel.ts:407), and `flyToGeographic` therefore takes the non-instant branch: a real `viewer.camera.flyTo({ duration: 1.6 })` to WORLD_HOME at `SITE_ENTRY_ALTITUDE_M.world = 20_000_000` m (CesiumViewport.ts:19308-19314; siteEntryModel.ts:137, :196). There is no short-circuit when the camera is already at the destination.\n\n⭐ I AM NOT CHARGING THIS WITH MILLISECONDS, AND THAT IS DELIBERATE. On a FIRST search the globe is already at WORLD_HOME (the hero mounts there via `frameCurrent()`, which passes `instant = true`), so the tween moves nothing, streams no new tiles, and its real-world cost is near zero and UNMEASURED. Reporting it as a share of the 4,277 ms would be exactly the \"this looks expensive\" finding the brief forbids.\n\n⚠ THE REAL DEFECT IS ON THE RETRY PATH AND IT IS VISIBLE. Mistype, get \"No matches — check the spelling\", correct it, submit again: the globe now yanks from wherever it is back out to 20,000 km for 1.6 s, then sits there for the whole (4.3 s) geocode, then descends again. The reset exists to guarantee a fresh reducer chain — but the STATE reset and the CAMERA effect are separable, and only the state reset is load-bearing.\n\n⚠ CHECK FOR INTERACTION WITH L-13262 (§STARTUP-ARRIVES-DIRECTLY, landed today), which changed how the arrival is framed. I did not trace whether that lane already alters this reset's effect; whoever owns the descent should confirm before touching it.",
      "blocksReveal": false,
      "costMs": "Near zero on a first search (camera already at the destination) — UNMEASURED, and deliberately not attributed a share of the 4,277 ms. 1.6 s of unwanted visible camera motion on a retry.",
      "costBasis": "read-from-code",
      "quote": "this.store.dispatch({ type: 'site.entry.reset' });",
      "file": "apps/editor/src/ui/onboarding/GlobeHeroSearch.ts",
      "line": 251,
      "fix": "Make the reset's camera effect `instant: true` (a state reset is not a navigation — the same reasoning `frameCurrent()` already uses), or suppress the effect when the reducer's next state equals the current one. Low value, low risk, and it belongs to whoever owns the descent lane, not to the geocode fix. Coordinate with L-13262.",
      "risk": "low",
      "confidence": "medium"
    }
  ],
  "whatIcouldNotEstablish": "⛔ I CANNOT PROVE WHERE THE 4,277 ms WENT ON THE WIRE, AND NOTHING IN THE REPO CAN. What I can prove: (a) the interval contains exactly one `fetch` and one `res.json()` of ≤5 rows and nothing of ours (geocodeAddress.ts:111-171); (b) the marks bracket precisely that and nothing else (OnboardingStepController.ts:927-931); (c) the mark delta is a true interval, not a cumulative figure (startupBudget.ts:283); (d) the same two marks read 261 ms on 2026-08-07 (ISSUE-LOG L-753). That is enough to say the cost is NOT our compute. It is NOT enough to separate the two remaining candidates — Nominatim globally slow at that minute, versus Nominatim throttling this client for uncached repeat queries. I lean to the second (the policy names that exact penalty, and the repo already hit it on Overpass from the same founder's demo pattern), but I am calling that MEDIUM confidence, not high. ⭐ The useful part: the fix is the same either way, and only one of the two candidates is inside our control.\n\n⛔ I DID NOT MEASURE ANYTHING LIVE. I made no request to Nominatim, ran no probe, and started no build — the brief said measure and design only, do not edit. So every latency number I quote is either the founder's log, a repo artefact, or explicitly labelled ESTIMATED. The three that would settle the open question, in order of value: (1) time the SAME query from the founder's IP and from the Fly server IP back to back — if the server is fast and the browser is slow, it is per-IP throttling and the proxy is the whole answer; (2) read the `Server-Timing` / response headers Nominatim returns for a throttled versus a clean client; (3) capture the browser Resource Timing entry for the geocode fetch to split DNS + connect + TLS from TTFB, which would turn my ESTIMATED 100-350 ms preconnect finding into a measured one.\n\n⚠ I DID NOT PRICE THE SAME-ORIGIN ROUND TRIP FROM THE FOUNDER'S OWN MACHINE. The ~250 ms warm-hit figure comes from L-13111's measured probe (`app.pryzm.so/api/context-tiles/tileset-manifest.json` → 404, 38 B, 242 ms), which is a real browser→Fly measurement but from a different session and possibly a different network. If his RTT to Fly is materially worse, the proxy's warm win shrinks. It would still be ~17× on the evidence I have, but the number should be re-measured on his machine before it is quoted to him.\n\n⚠ THREE THINGS I DELIBERATELY DID NOT CHASE, because they belong to other lanes and I would only be guessing: `activation:site:start +1568ms`; whether the `[ProjectHub] runtime.persistence.client.delete failed: timeout (HTTP 0)` contends with the geocode (it is same-origin and the geocode is cross-origin, so HTTP/2 host connection limits do not apply — but I did not check whether it burns main-thread time that would delay the fetch's promise continuation); and whether L-13262's descent change interacts with the reset flight in finding 9.\n\n⚠ TOOLING: the ripgrep-backed Grep tool silently omitted BOTH production call sites of `mountSiteGeocodeSearchBox` that bash `grep -rn` found. I caught it because the brief told me to check caller counts, and I cross-checked. Any caller-count or dead-code verdict produced by other lanes using the Grep tool alone in this repo should be re-run under bash grep before it is trusted."
}