{
  "dimension": "The true wait: geocode:start → the split on screen — what actually blocks the reveal, and what only looks like it does",
  "summary": "THE HONEST HEADLINE IS 5,933 ms, NOT 11,495 ms — and of those 5,933 ms, 5,845 ms is two things and neither is the app doing work the split needs.\n\n`geocode:start` t+107,079 → `reveal:split-mounted` t+113,012 = **5,933 ms**, split as:\n  · 4,277 ms — the geocode: ONE uncached browser fetch to public Nominatim, started only on form submit (no typeahead, no cache, no proxy).\n  · 1,568 ms — NOT activation work. It is `REVEAL_GATE_DEADLINE_MS = 1_500` elapsing on a `Promise.race` whose two gates could not win: the content gate resolved 5.4 s later, and the flight gate is not in the founder's own mark list before the mount. The founder's log prints the gate saying so verbatim: *\"the context read had not landed within 1500 ms — mounting the split now\"*.\n  · 86 ms — everything else: context-warm kickoff (3 ms) + hand-off (8 ms) + mount and pane activation (75 ms).\n  ⇒ **Real machine work between the geocode landing and the split being on screen: ~86 ms.** There is nothing to rearchitect in the reveal sequence; all five of its steps together are unmeasurable.\n\nThe remaining 5,562 ms of the founder's 11,495 ms is a MARK, NOT A WAIT. `reveal:content-ready` is emitted from a `.then()` on `this.contextWarm` — the promise the reveal ABANDONED at the deadline. It fires 5,562 ms after the user already had the split. Every ranking built on the 11.5 s number is ranking against a phantom.\n\nWHAT HE SEES AT t+113,012 (split-mounted): no full-screen overlay at any point on this path (the quiet-activation window means the splash is never raised; the only chrome is a `pointer-events:none` pill at the bottom). RIGHT pane = the ONE existing Cesium viewport re-parented, already seated on the plot, with camera navigation DISABLED (`enableInputs=false`) until an `activation:site:dismissed` mark the founder's excerpt does not contain. LEFT pane = the MapLibre map is *starting* — its dynamic import fires at mount, and the plot click the screenshot's caption invites (`map.on('click', onClick)`) is bound inside `attachDrawHandlers()`, which runs only inside MapLibre's `load` handler. Between t+113,012 and t+118,574 the ONLY thing that changes is context streaming into the RIGHT pane; nothing in that window gates the LEFT pane.\n\n⛔ SO THE ANSWER TO (e) IS NOT \"6 s, it's interactive at 1.5 s\". It is: **the split is VISIBLE at 5,933 ms and neither pane is fully interactive at that instant** — the 3D pane's camera is gated by the activation, and the 2D pane's plot-click is gated by a MapLibre `load` event that emits NO startup mark at all. The founder asked \"until it lands on the screenshot\", and the budget's terminal mark fires BEFORE that surface exists while its next mark is unrelated to it. That missing mark is the highest-value single line of code in this report.\n\n10× target: 5,933 → 593 ms. Prefetching the geocode while he types (the log shows 97.8 s of idle typing before submit) and replacing the blind 1,500 ms timer with the arrival signal that is ALREADY wired (`whenStartupDescentSettled()`) gets measured-to-split-mounted to ~200–400 ms. That clears 10× against the mark that exists. It does not, on its own, prove the pastel map is painted — that is unmeasured today.",
  "findings": [
    {
      "title": "`activation:site:start +1568ms` is not activation work — it is the 1,500 ms reveal-gate timer running out on two promises that cannot win",
      "detail": "`revealSplitAtParcel` → `runSiteRevealSequence` step 3.5 races `Promise.all([awaitContentReady, awaitFlightComplete])` against `setTimeout(deadlineMs)` where `deadlineMs` defaults to `REVEAL_GATE_DEADLINE_MS = 1_500` (siteRevealSequence.ts:66, :319, :322-329). `activation:site:start` is emitted from INSIDE the mount — `startViewActivationLoading('site', …)` at GISAreaLayout.ts:7915, inside `cesiumMounter.mount`, which `mountSplit()` reaches synchronously — so its +1568 ms delta from `flight:parcel-arrival` is the deadline (1,500) plus ~68 ms of mount work, not 1.5 s of activation.\n\nTHREE INDEPENDENT PROOFS, all from the founder's own log: (1) 111,368 + 1,500 + 68 = 112,936 — the mark lands exactly there; (2) the console line the brief quotes verbatim is this gate's own expiry log, `§STARTUP-REVEAL-NOT-GATED-ON-CONTEXT: the context read had not landed within 1500 ms — mounting the split now`; (3) `reveal:flight-settled` — which `awaitFlightComplete` marks on resolution — appears NOWHERE between `flight:parcel-arrival` and `activation:site:start` in his list, so the flight gate had not resolved either. Both gates were open; the timer won.\n\nTHE GATE COULD NEVER HAVE WON HERE. `awaitContentReady` waits on `fetchContextBuildingsNearAndFar`, which settled at t+118,446 — 7,078 ms after the arrival. On a cold Barcelona read a 1,500 ms deadline is a coin flip with both sides losing: it buys nothing and costs 1.5 s unconditionally. This is the largest single removable block on the critical path after the geocode, and unlike the geocode it is a local constant.",
      "blocksReveal": true,
      "costMs": "1,568 ms — MEASURED (founder log: `activation:site:start +1568ms (t+112936ms)`), of which 1,500 ms is the constant `REVEAL_GATE_DEADLINE_MS` and ~68 ms is mount work",
      "costBasis": "founder-log",
      "fix": "Stop WAITING on a gate and start ASKING it. Two changes, both local to `siteRevealSequence.ts` step 3.5 and its two production deps in `OnboardingStepController.revealSplitAtParcel`:\n\n(1) REPLACE THE BLIND TIMER WITH THE ARRIVAL SIGNAL THAT IS ALREADY WIRED. `CesiumViewport.whenStartupDescentSettled()` (CesiumViewport.ts:19384) is non-null on exactly this path — `armStartupDescent()` is called one statement before `dispatchSiteLocation` in `anchorSiteLocation` — and `viewActivationLoading.ts` already consumes it as its quiet window. It resolves when the camera is genuinely SEATED on the plot, which is the honest 'the view behind the split is real' signal the deadline is a guess at. Gate on `Promise.race([whenStartupDescentSettled(), gates])`, not on a clock. It can never be longer than the arrival and it needs no new machinery.\n\n(2) MAKE THE WARM CASE ANSWER SYNCHRONOUSLY, SO THE COLD CASE NEVER WAITS. The gate exists for the warm case ('mount a split that is about to be fully populated one tick later'). `contextBuildings.ts:1170` already answers that synchronously — `const cached = cache.get(key); if (cached) return cached;` over an in-memory Map, with `ctxbldRead` (contextBuildingsCache.ts:93) as the localStorage tier. Export a predicate (`isContextBuildingsWarm(lat, lon, farHalfDeg)`) and hand it to the reveal: warm ⇒ await the promise (it resolves in single-digit ms, exactly as the gate's own note describes); cold ⇒ do not await at all, mount now. That preserves 100% of the gate's stated benefit and removes 100% of its measured cost, and it deletes the timer rather than tuning it — which the module's own header (`⚠ IT IS A CEILING, NOT A DELAY`) asks for.\n\n⛔ Do NOT simply lower the constant. A smaller ceiling is the same defect with a smaller number, and the invariant the module wanted ('no reveal gate may hold the split longer than X') is better served by a gate that decides than by a bound on how long it may dither.",
      "risk": "low",
      "confidence": "high",
      "file": "apps/editor/src/ui/onboarding/siteRevealSequence.ts",
      "line": 328,
      "quote": "new Promise<true>((resolve) => { timer = setTimeout(() => resolve(true), deadlineMs); }),"
    },
    {
      "title": "The geocode is 4,277 ms of a single uncached fetch to public Nominatim, and it is started only on submit — there is no typeahead, no memo and no proxy",
      "detail": "`geocodeAddress()` is one `fetch` to `https://nominatim.openstreetmap.org/search?format=json&limit=5&…` (geocodeAddress.ts:118-128). There is NO cache of any kind in that module, NO server-side proxy (`grep -rn nominatim server/ server.js` returns one hit and it is the CSP allowlist at `server/securityHeaders.js:117`, not a route), and NO prefetch: the location card binds exactly two listeners — `form.addEventListener('submit', onSubmit)` and the Skip click (OnboardingStepController.ts:963-965). There is no `input`/`keyup` handler anywhere in `renderLocationStep`.\n\nTHE COST IS 72% OF THE HONEST WAIT and it is spent on a third party the app cannot make faster. But the founder's own log shows the opportunity sitting right next to it: `geocode:start +97847ms` — 97.8 seconds of idle while he typed 'bARCELONA'. The answer could have been on the machine before he pressed →.",
      "blocksReveal": true,
      "costMs": "4,277 ms — MEASURED (founder log: `geocode:end +4277ms (t+111357ms)`)",
      "costBasis": "founder-log",
      "fix": "Two layers, both cheap and both inside `geocodeAddress.ts` + the location card:\n\n(1) A DEBOUNCED PREFETCH ON THE INPUT. Add one `input` listener in `renderLocationStep` that, ~350 ms after typing stops and only for a query ≥3 chars that `sniffCadastralRef` reads as a place, calls the same `geocodeAddress(q)` and drops the promise into a memo keyed by the trimmed lowercased query. `handleGeocode` then reads that memo first. On the founder's run this moves the whole 4,277 ms into the 97.8 s he was already spending, and the submit becomes a memo hit. This is the same shape as the existing `§CTX-PREFETCH-ON-LOCATION` optimisation one layer down: move the ONE query earlier into time the user is already spending, never add a second query.\n\n(2) A QUERY MEMO INSIDE `geocodeAddress` (Map + optional `localStorage`, keyed on the trimmed query, TTL a day). Place names do not move. This makes a re-submit, a back-then-forward, and the prefetch/submit pair all one network call rather than two or three, and it is what makes (1) safe.\n\n⚠ FAIR USE IS A REAL CONSTRAINT AND IS SATISFIED BY BOTH, NOT VIOLATED. The module header cites Nominatim's policy. A 350 ms debounce plus a memo issues FEWER requests than today's 'user submits, gets a slow answer, edits, submits again' loop, and only ever one request per settled query string. Do not implement per-keystroke suggestions — that is what the policy forbids.\n\n⭐ If the founder wants the 4.3 s gone rather than hidden, the durable fix is a server route with a shared cache (the endpoint is already a single overridable const, `GEOCODE_ENDPOINT`, and `VITE_GEOCODE_ENDPOINT` is the declared override). That is a separate decision — the prefetch alone already takes it off the critical path.",
      "risk": "low",
      "confidence": "high",
      "file": "apps/editor/src/ui/site/geocodeAddress.ts",
      "line": 125,
      "quote": "res = await fetch(url, {"
    },
    {
      "title": "The 11,495 ms headline is measured to a mark that fires 5,562 ms AFTER the user already had the split — `reveal:content-ready` is emitted by the promise the reveal abandoned",
      "detail": "`awaitContentReady` is `(this.contextWarm ?? Promise.resolve()).then((r) => { markStartupPhase('reveal:content-ready'); return r; })`. When the deadline expires, `runSiteRevealSequence` stops AWAITING that promise but nothing cancels it — the `.then` still runs when `fetchContextBuildingsNearAndFar` finally settles. That is exactly what the founder's log shows: `context-warm:done` t+118,446, then `reveal:content-ready` t+118,574 — 5,562 ms after `reveal:split-mounted` at t+113,012.\n\nThe mark's NAME says 'the reveal's content is ready'. Its POSITION in the printed budget says 'this is the end of the startup'. Both were true when the gate was unconditional; since the deadline landed, neither is. Every reading taken off the tail of this budget — including the brief's own 11,495 ms and its 7.2 s residual — is measuring a promise the user was never waiting on.\n\n⚠ THIS IS THE SAME DEFECT CLASS THE MODULE'S OWN HEADER DOCUMENTS ONE PARAGRAPH ABOVE IT: *'`reveal:content-ready` landing in the SAME MILLISECOND as `context-warm:done` is not a coincidence to be interpreted — it is the gate, printing itself.'* It still is. It is just no longer a gate.",
      "blocksReveal": false,
      "costMs": "0 ms of user time — but it is why the stated headline is 11,495 ms instead of 5,933 ms. Delta MEASURED from the founder log (t+118,574 − t+113,012 = 5,562 ms of mark, not of wait)",
      "costBasis": "founder-log",
      "fix": "Move the mark to the gate's own resolution, and name the two outcomes differently so they can never be read as one:\n  · in `runSiteRevealSequence`, after the `Promise.race`, mark `reveal:gate-settled` (gates won) or `reveal:gate-abandoned(<deadlineMs>)` (timer won) — the sequence already computes `gateDeadlineExpired` and already returns it in `SiteRevealResult`, so this is one call at an existing branch;\n  · rename the late `.then` mark to `context-warm:consumer-ready`, or drop it — it now measures a background read, and `context-warm:done` already measures that.\nThen the last mark of the reveal is the reveal, and the budget's tail stops overstating the wait by 94%.\n\n⭐ Ship this BEFORE the other fixes, not after. Every 'is it faster?' reading taken after a change will be compared against this tail, and today that tail moves with a background download rather than with anything the user sees.",
      "risk": "none",
      "confidence": "high",
      "file": "apps/editor/src/ui/onboarding/OnboardingStepController.ts",
      "line": 1147,
      "quote": "markStartupPhase('reveal:content-ready'); // §STARTUP-BUDGET"
    },
    {
      "title": "The thing the founder actually means by \"lands on the screenshot\" — the clickable pastel map — has no startup mark at all, and its click handler is bound behind MapLibre's `load` event",
      "detail": "The screenshot's caption is `Click a plot to select its real cadastral parcel`, set at SiteBoundaryMap2D.ts:3701 SYNCHRONOUSLY at map construction. The click it invites is not. `map.on('click', onClick)` lives only inside `attachDrawHandlers()` (SiteBoundaryMap2D.ts:3777), and on the onboarding path that function is called only from inside `map.on('load', …)` (:4388 → :4426). `onClick` is what routes select-mode to `handleParcelSelectClick` (:3267).\n\nSo the instruction is on screen before the gesture works, and the moment it starts working is stamped as a bare wall-clock number — `window.pryzmBoundaryDrawSurfaceReadyAt = Date.now()` (:4524) — NOT through `markStartupPhase`, even though this file already imports `markStartupPhase` (:280) and already uses it four hundred lines earlier for `parcel:selected` (:2827).\n\n⛔ CONSEQUENCE: the startup budget's terminal mark (`reveal:split-mounted`) fires BEFORE the surface in the screenshot exists, and its next mark (`reveal:content-ready`) is unrelated to it. The one interval the founder asked about is the one interval no mark brackets. And the map's whole mount begins at `mountSplit` — `mapMounter.mount: (paneEl) => startBoundaryDraw({ parent: paneEl })` behind a dynamic `import('../geospatial/SiteBoundaryMap2D')` (GISAreaLayout.ts:884) — so the chunk load, the MapLibre construction and the style fetch are all AFTER t+113,012 and all unmeasured.",
      "blocksReveal": true,
      "costMs": "UNKNOWN — this is the point. It is the interval the founder is asking about and it is the only one with no mark on either end. Everything else in this report is measured; this is not.",
      "costBasis": "read-from-code",
      "fix": "ONE LINE, and it should land before any optimisation is attempted:\n  `markStartupPhase('map2d:interactive');` immediately beside SiteBoundaryMap2D.ts:4524, inside the same `if (!opts.overlayOnly)` block, so it stamps exactly when `attachDrawHandlers()` has run and the plot click is live.\nOptionally a second at the top of `startBoundaryDraw`'s dynamic import resolution (`map2d:module-loaded`) to separate chunk-download cost from MapLibre style cost — that is the split that decides whether the fix is a preload hint or a style change.\n\nThis is the same prescription this repo has written twice already and both times it was right: L-772's *\"the fix is 3 marks, not an optimisation\"*, and `viewActivationLoading.ts:405-420`'s own §STARTUP-ACTIVATION-IS-UNMEASURED note. Do not tune the 2D pane before it emits a number.\n\n⭐ Then, and only then, the cheap suspects are worth pricing: the `SiteBoundaryMap2D` chunk is imported for the first time at mount, on a path where the location has been known for 1.6 s and the split's arrival is certain from `flight:parcel-arrival` onward. A `void import('../geospatial/SiteBoundaryMap2D')` fired alongside `warmContextCache` costs one prefetch of a chunk that is about to be needed and is on nobody's critical path when it lands early.",
      "risk": "none",
      "confidence": "high",
      "file": "apps/editor/src/ui/geospatial/SiteBoundaryMap2D.ts",
      "line": 4426,
      "quote": "            attachDrawHandlers();"
    },
    {
      "title": "L-13262 (landed today) deleted the flight that `awaitFlightComplete` exists to wait for — which is what turns the 1,500 ms deadline from a cover into pure dead time",
      "detail": "The brief asked for interactions with today's work. This is the load-bearing one. `awaitFlightComplete`'s contract, in its own words, is *\"if the data lands at t≈2.7 s and the camera is still descending until t≈3.9 s, cutting to the split on content alone interrupts the very zoom that was requested\"*. There is no longer a zoom to interrupt: `frameSiteLocationAtGround` now takes the `arriveDirectly` branch whenever `startupDescentArmed` is set — which `revealSplitAtParcel` sets one statement before it dispatches the location — and that branch is `viewer.camera.setView({ destination, orientation })` (CesiumViewport.ts:4938), with the descent promise resolved four lines earlier (:4925-4928).\n\nSo the reveal's second gate now settles on an animation of zero duration, the first gate is a cold multi-second read, and the only thing left holding the split shut is the clock. The gate's own justification — *\"the reveal fires when the LATER of the two lands, which is the whole choreography\"* — describes a choreography that no longer has two parts.\n\n⚠ AND THE COST L-13262 HONESTLY DECLARED IS BEING PAID TWICE. Its note says *\"with the descent gone, the wait it was covering becomes VISIBLE again… What the founder gains is being ON the plot in ~0 s instead of watching a 12 s glide.\"* He is on the plot in ~0 s — and then the reveal holds him on the full-screen globe for 1.5 s anyway, on a timer that was calibrated against the descent that was just deleted.",
      "blocksReveal": true,
      "costMs": "0 ms of its own — this finding costs nothing directly; it is the reason finding #1's 1,568 ms is 100% waste rather than partial cover. Basis: MEASURED via the marks in finding #1, plus the code at CesiumViewport.ts:4920-4938",
      "costBasis": "read-from-code",
      "fix": "Nothing to fix here in isolation — this is the argument that makes finding #1's fix safe, and it should be quoted in that change's commit message. Concretely it means: when the timer is replaced, `awaitFlightComplete` can stay wired (it is harmless and settles ~immediately), but it must NOT be treated as the reason to keep a floor. There is no cinematic to protect any more; the only real 'is the view behind the split real yet?' signal on this path is `whenStartupDescentSettled()`, which is what finding #1 proposes gating on.\n\n⛔ Do not delete `awaitFlightComplete`. It is still the correct gate on every route that does fly (a user-driven geocode from the GIS rail, a project restore), and `SITE_ARRIVAL_FLY_DURATION_S` still governs those.",
      "risk": "none",
      "confidence": "high",
      "file": "apps/editor/src/ui/geospatial/CesiumViewport.ts",
      "line": 4938,
      "quote": "      viewer.camera.setView({ destination, orientation });"
    },
    {
      "title": "None of the reveal sequence's five steps costs measurable time — the choreography is not the problem and must not be rearchitected",
      "detail": "Between `flight:parcel-arrival` (t+111,368) and the deadline expiry the marks leave ~0 ms unaccounted: 111,368 + 1,500 (timer) + 68 = 112,936, which is `activation:site:start` to the millisecond. So `seedGeocodeFrame` (`pryzmSetGeocodeFrame`, GISAreaLayout.ts:2660 — a validate-and-assign), `anchorSiteLocation` (`armStartupDescent` + `dispatchSiteLocation`) and `armBoundaryListener` (one `runtime.events.on`) together cost under ~68 ms, and the mount + Cesium re-parent + pane activation from `activation:site:start` to `reveal:split-mounted` costs **75 ms** (measured directly: t+112,936 → t+113,012).\n\nThat 75 ms is one synchronous main-thread block covering `mountSiteAuthoringPaneShell`, the fade setup, the map mounter kickoff, `toggleGIS(true)`, and the Cesium container re-parent into the right pane. For what it does, that is cheap.\n\n⭐ THIS IS THE 'STILL ARCHITECTURALLY SOUND' FINDING. The reveal ordering is contractual (`siteRevealSequence.ts` exists because getting it wrong shipped and was reverted the same day), it has one production caller, and it costs nothing. Every millisecond on this leg is in the geocode and in one timer constant. There is no six-week rearchitecture here and no case for one.",
      "blocksReveal": true,
      "costMs": "86 ms total — MEASURED (founder log: context-warm:start +3, flight:parcel-arrival +8, activation:site:descent-settled +75; reveal:split-mounted +0)",
      "costBasis": "founder-log",
      "fix": "No change. State it explicitly in the plan so nobody spends the budget here: the reveal sequence, the pane shell mount, the Cesium re-parent and the boundary-listener arm are collectively 86 ms of the 5,933 ms wait (1.5%). `runSiteRevealSequence` has exactly one production caller (`OnboardingStepController.ts:1085`) and `revealSplitAtParcel` exactly one (`:877`) — there is no twin path to consolidate either.",
      "risk": "none",
      "confidence": "high",
      "file": "apps/editor/src/ui/onboarding/siteRevealSequence.ts",
      "line": 230,
      "quote": "export async function runSiteRevealSequence("
    },
    {
      "title": "The camera arrival is asynchronous and unmeasured — it is the one thing the 1,500 ms deadline might legitimately be covering, and it must be measured before the deadline is removed",
      "detail": "`frameSiteLocation` is fire-and-forget into an async body: `void this.frameSiteLocationOnResolvedGround(lat, lon, opts)` (CesiumViewport.ts:4803). That body awaits `maybeAttachTerrainProvider(lat, lon)` — which on an attach verdict fetches `${url}/layer.json` per candidate slug (CesiumViewport.ts:11083, :11163-11167) — and then `Cesium.sampleTerrainMostDetailed(...)`, before `frameSiteLocationAtGround` runs the `setView`. So the plot does NOT appear under the camera synchronously when `dispatchSiteLocation` returns.\n\nHOW LONG IT TAKES IS NOT KNOWABLE FROM THE LOG. `activation:site:descent-settled` at t+113,012 only bounds it: the quiet window subscribes to that promise at t+112,936 (inside the mount), so all the mark proves is that the arrival happened at or before t+113,012 — anywhere in a 1,642 ms window.\n\n⚠ THE LIKELY ANSWER IS 'FAST', BUT IT IS INFERENCE, NOT MEASUREMENT. At the instant `site.location-changed` fires, `formaMode` is still FALSE — `cesiumViewport.setFormaMode?.(true)` runs later, inside `cesiumMounter.mount` (GISAreaLayout.ts, the same block that emits `activation:site:start`) — and the onboarding globe has been on the photoreal path, so `decideBakedTerrainAttach({ formaMode: false, photorealActive: true, … })` should return a keep-flat verdict that returns before any `layer.json` fetch, leaving `terrainProviderHasElevationData` false and skipping the terrain sample too. If that holds, the arrival is one microtask and the deadline covers nothing whatsoever.\n\n⛔ BUT 'SHOULD RETURN' IS EXACTLY THE KIND OF CLAIM THIS REPO HAS BEEN WRONG ABOUT IN BOTH DIRECTIONS. Do not remove the deadline on the strength of it.",
      "blocksReveal": true,
      "costMs": "UNKNOWN — bounded only to the interval [t+111,370, t+113,012], i.e. somewhere in 0–1,642 ms. Basis: the `descent-settled` mark bounds it from above; nothing bounds it from below",
      "costBasis": "unknown",
      "fix": "Add ONE mark inside the `arriveDirectly` branch, immediately after `viewer.camera.setView({ destination, orientation })` at CesiumViewport.ts:4938 — `markStartupPhase('flight:site-seated')`. It is the same passive one-`performance.now()`-and-a-push the rest of the budget uses, it lands in the correct time order between `flight:parcel-arrival` and `activation:site:start`, and it answers the only open question in this whole analysis: is the 1,500 ms deadline covering a real arrival, or is it covering nothing?\n\nThen pick the branch by the reading:\n  · seated at ≈ t+111,370 (the expected case) ⇒ the deadline covers nothing; take finding #1's fix as written and the split mounts on an already-correct view.\n  · seated materially later ⇒ finding #1's fix is still correct BUT gate on `whenStartupDescentSettled()` specifically rather than mounting immediately, so the split never appears over a camera that has not landed. That is the difference between removing a wait and faking a frame, and the founder named that distinction himself.",
      "risk": "low",
      "confidence": "medium",
      "file": "apps/editor/src/ui/geospatial/CesiumViewport.ts",
      "line": 4803,
      "quote": "    void this.frameSiteLocationOnResolvedGround(lat, lon, opts);"
    },
    {
      "title": "What is on screen, and what is gated, at t+113,012 versus t+118,574 — the (b) answer, per surface",
      "detail": "NO FULL-SCREEN OVERLAY AT ANY POINT ON THIS PATH. `beginViewActivationLoading` is handed a `quietWindow` (GISAreaLayout.ts:2229-2231, non-null because `whenStartupDescentSettled()` is armed), so `raiseOverlay` is never called and the load reports on `quietActivationLine.ts` — a `position: fixed` pill at `bottom: 28px`, `z-index: 60`, `pointer-events: none`, whose own header states *\"IT GATES NOTHING\"*. The presence of `activation:site:descent-settled` in the founder's log proves the quiet path ran, because that mark is emitted only on the quiet branch (viewActivationLoading.ts:398).\n\nRIGHT PANE at t+113,012: the ONE existing Cesium viewport, `reparentContainerTo(paneEl)` + `setVisible(true)` — not a fresh viewer, not black. Its camera is INPUT-GATED: `signals.setNavigationEnabled(false)` at viewActivationLoading.ts:431 sets `screenSpaceCameraController.enableInputs = false` (CesiumViewport.ts:3301-3306) and is released only in `finish()` at :505, which emits `activation:site:dismissed(...)`. That mark is NOT in the founder's excerpt, so WHEN the 3D pane becomes navigable is unestablished from this log.\n\nLEFT PANE at t+113,012: mounting, not mounted. Not gated by anything Cesium — `setNavigationEnabled` reaches only `cesiumViewport` — but not yet clickable either, per finding #4.\n\nBETWEEN t+113,012 AND t+118,574: the only change is context layers streaming into the RIGHT pane. Nothing in that window gates the LEFT pane, and `reveal:content-ready` at the end of it gates nothing at all.\n\n⛔ SO THE TEMPTING ANSWER — 'it is usable at split-mount, the honest number is 6 s' — IS HALF RIGHT AND THE WRONG HALF IS THE ONE HE CARES ABOUT. It is VISIBLE at 5,933 ms. Neither pane is fully interactive at that instant, and the gesture the screenshot's own caption asks for is the one with no measurement.",
      "blocksReveal": false,
      "costMs": "0 ms — this is an attribution finding, not a cost. The 5,562 ms between the two marks is MEASURED from the founder log and none of it blocks the left pane",
      "costBasis": "founder-log",
      "fix": "Report the wait to the founder as two numbers with their meanings attached, and instrument the second one:\n  · **5,933 ms — the split is on screen** (`geocode:start → reveal:split-mounted`, measured).\n  · **UNMEASURED — he can click a plot** (needs finding #4's one-line mark).\nAnd stop quoting 11,495 ms in either direction: it is `reveal:content-ready`, which finding #3 shows is 5,562 ms of background download after he already had the view.\n\nSeparately worth one line each and no more: `activation:site:dismissed(...)` is what releases the 3D pane's camera and is absent from the excerpt — ask for a full budget dump, not a slice, before pricing the 3D half.",
      "risk": "none",
      "confidence": "high",
      "file": "apps/editor/src/ui/geospatial/viewActivationLoading.ts",
      "line": 431,
      "quote": "    try { signals.setNavigationEnabled(false); } catch { /* viewer not up yet */ }"
    },
    {
      "title": "Evidence quality: the quoted timeline is a SLICE, not the run — at least one mark between `reveal:split-mounted` and `context-warm:done` is missing (555 ms unaccounted)",
      "detail": "`markStartupPhase` deltas are against the PREVIOUS MARK OF THE RUN — the module says so itself in the PERF-HUB-BOOT note (startupBudget.ts:~124-131: *\"the delta is against the PREVIOUS MARK of the run\"*). Running the quoted deltas forward reconciles everywhere except one place:\n  107,079 + 4,277 = 111,356 ≈ 111,357 ✓ (±1 rounding)\n  111,357 + 3 = 111,360 ≈ 111,359 ✓\n  111,359 + 8 = 111,367 ≈ 111,368 ✓\n  111,368 + 1,568 = 112,936 ✓ exact\n  112,936 + 75 = 113,011 ≈ 113,012 ✓\n  113,012 + 0 = 113,012 ✓\n  **113,012 + 4,879 = 117,891, but the log says 118,446 — 555 ms and at least one mark unaccounted for.**\n  118,446 + 128 = 118,574 ✓\n\nSo a mark exists at ≈ t+113,567. The most likely occupant is `reveal:flight-settled` (the `awaitFlightComplete` gate resolving 555 ms after the mount) — which, if true, independently confirms finding #1: the flight gate had NOT resolved when the timer fired, so both gates were open and the timer won on merit. `activation:site:quiet-start` is also certainly omitted (it is emitted unconditionally on the quiet path at viewActivationLoading.ts:425, one statement after `:start`).\n\n⚠ THIS DOES NOT CHANGE ANY NUMBER IN THIS REPORT. `reveal:split-mounted` at t+113,012 and the 1,568 ms deadline are both anchored on marks that are present and that reconcile exactly. It changes what may be quoted from the tail.",
      "blocksReveal": false,
      "costMs": "555 ms of the founder's log is unattributed to any named mark — MEASURED by reconciling his own deltas against his own absolute timestamps",
      "costBasis": "founder-log",
      "fix": "Ask for the full `reportStartupBudget()` table rather than an excerpt before any post-fix comparison is made — specifically the marks between `reveal:split-mounted` and `context-warm:done`, and `activation:site:dismissed(...)`, neither of which the slice contains.\n\n⭐ And record the reconciliation method: `markStartupPhase` deltas are against the previous mark OF THE RUN, so a printed delta and a printed absolute timestamp are two independent readings of the same table. When they disagree, marks were dropped from the paste — not from the code. That check cost nothing and found a hidden mark; run it on every budget paste.",
      "risk": "none",
      "confidence": "high",
      "file": "apps/editor/src/engine/startupBudget.ts",
      "line": 26,
      "quote": "// flight:parcel-arrival · reveal:content-ready · reveal:flight-settled · reveal:split-mounted ·"
    }
  ],
  "whatIcouldNotEstablish": "1. **WHEN THE PASTEL 2D MAP FIRST PAINTS AND WHEN THE PLOT CLICK GOES LIVE.** This is the founder's actual question (\"until it lands on the screenshot\") and it is the one interval with no mark on either end. The map's readiness is stamped as a bare `Date.now()` into `window.pryzmBoundaryDrawSurfaceReadyAt` (SiteBoundaryMap2D.ts:4524), never through `markStartupPhase`, so it does not appear in his budget. I know the click is bound inside `map.on('load')` (:4388 → :4426 → :3777) and that the whole mount starts at `mountSplit` behind a dynamic `import()` — but the chunk-load cost, the MapLibre construction cost and the style-fetch cost are all unmeasured. Finding #4 is one line of code and it closes this.\n\n2. **HOW LONG THE CAMERA ARRIVAL ACTUALLY TAKES.** `frameSiteLocation` is async (a terrain-attach decision that can fetch `layer.json`, then a terrain sample) and nothing marks the `setView`. The `descent-settled` mark only bounds it to somewhere in [t+111,370, t+113,012]. My read of `decideBakedTerrainAttach` says `formaMode` is still false at that instant — `setFormaMode(true)` runs later in the pane mounter — so the keep-flat branch should return before any network call and the arrival should be ~immediate. **That is inference from a branch condition, not a measurement, and I did not trace `decideBakedTerrainAttach`'s body.** It matters because it is the only candidate for something the 1,500 ms deadline is legitimately covering. Finding #7 says how to settle it in one mark.\n\n3. **WHEN THE 3D PANE BECOMES NAVIGABLE.** `setNavigationEnabled(true)` fires in `finish()`, which emits `activation:site:dismissed(ready|not-ready)`. That mark is not in the founder's excerpt, and the excerpt is provably incomplete (finding #9). So I can say the 3D pane's camera IS gated at split-mount and I cannot say for how long.\n\n4. **WHETHER `reveal:flight-settled` is the hidden mark at ≈t+113,567.** The arithmetic proves a mark is there; naming it requires the full table. If it is, it independently confirms finding #1. If it is something else, finding #1 still stands on the three proofs it already has.\n\n5. **WHETHER THE SEVEN LAYERS' 81-TILE READS ARE THE SAME 81 TILES**, and whether the `§DRAPE-COST-ATTRIBUTION` 8,037 ms FIFO wait touches this window. I deliberately did not chase these: every one of them lands after `reveal:split-mounted`, i.e. off the critical path for my dimension, and the brief's own ranking rule says a 15 s cost off the path is worth less than 200 ms on it. They belong to whichever lane owns the streaming half — but note for that lane that the founder's `parks` layer paying 8,037 ms to do 23 ms of work is entirely inside the window AFTER the split is already on screen.\n\n6. **WHETHER THE `[ProjectHub] persistence.client.delete timeout (HTTP 0)` CONTENDS.** It is a failed request in flight during the window. I did not price it. It is not on any await in the traced call stack, so it can only contend for browser connection slots against the Nominatim fetch and the tile reads — plausible but unmeasured, and I will not assert it from a log line."
}