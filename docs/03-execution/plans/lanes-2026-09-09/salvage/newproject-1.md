{
  "dimension": "The actual timeline of New-Project creation: click to first frame",
  "summary": "THE CLICK-TO-FRAME CHAIN IS ONE SYNCHRONOUS TASK FOLLOWED BY ONE SERIAL ENGINE BOOT, AND THE 9,233 ms IS HONEST. I followed `+ New Project` all the way to the emitter. `ProjectHub.startGuidedOnboardingDirect()` (ProjectHub.ts:2038, bound at :1108/:1220/:989) calls `onStartOnboarding({name, directEntry:true})` → `PlatformRouter.showOnboarding()` (:706), which calls `ensureEngineWarm()` (:720) then `beginStartupBudget()` (:725) — SO t0 IS THE CLICK ITSELF. `resolveSeededTypologyId` returns 'apartment' on `directEntry`, so :801 `if (getCurrentUser() && seededTypologyId)` is taken and :812 emits `pryzm:onboarding-brief-ready` SYNCHRONOUSLY (EventBus.emit is a bare for-loop, EventBus.ts:79-92) → briefBootstrap.handleBriefReady (no await before its last line) → `createAndOpenProject` (:981). ⭐ THE WHOLE OF THAT IS ONE TASK. There is NO human dwell anywhere in the 9,233 ms — which makes this the first §STARTUP-BUDGET number in this repo that is NOT the \"hub:open-clicked\" dwell defect the startupBudget.ts header records four times. THE NUMBER IS REAL AND THE OTHER DIMENSIONS MAY SPEND AGAINST IT.\n\n(b) WHAT THE MARK MEASURES. `open:first-interactive-frame` is `markStartupPhase` inside `getFrameScheduler().scheduleOnce('perf104-first-interactive-frame', …)` (PlatformShell.ts:203-204), armed by `_armPostSceneMarks` on `pryzm-project-loaded`. It is the FIRST P3 scheduler tick after the empty snapshot lands — a rAF callback BODY, not a composited pixel, and it is on the BIM canvas, not the globe. I checked whether that makes it the wrong instant and IT DOES NOT, by ~350 ms in the founder's favour: `briefBootstrap`'s own `pryzm-project-loaded` handler calls `startOnboardingStepFlow` → `controller.start()` → `renderLocationStep()` (OnboardingStepController.ts:535-546, :708) ALL SYNCHRONOUSLY in the same task, so the \"Where is your project?\" card is built at ~t+8,899 ms and painted on the very frame the mark names. 9,233 ms IS his wait, ±one frame, PROVIDED the prewarmed globe was already up (his `§STARTUP-GLOBE-COMPLETE-FIRST` line says it was). Do not re-derive the brief around a \"wrong instant\" theory — I tested it and it failed.\n\n(d) THE ARITHMETIC. 9,233 − 334 (his own measured last leg) = 8,899 ms of boot. Transferring the ONLY end-to-end span table this repo owns (audit/perf/2026-09-02/baseline-outer.json → axisD-local.startupBudgetMedianTPlusMs, median of 5, localhost prod build, WARM http cache, 300-wall EXISTING project — a different path, so these are transfers not his numbers): ensure→heavy-wiring 146 · heavy-wiring→engine-start 611 (= `await loadEngine()`, the 17.1 MB chunk group) · initScene 869 · await initBuilders 1,647 · tools+bus+stores+dataplatform 342 · initUI 663 · bootstrap-tail→shell-context-set 608 · version-read 1 · hydrate 988(300 walls; ~0 elements here). Warm-cache reconstruction with a 150 ms empty load and a 500 ms create RTT ≈ 5,824 ms. UNACCOUNTED REMAINDER ≈ 3,400 ms, AND IT HAS EXACTLY TWO CANDIDATES, separated by ONE line he already has: if `boot:heavy-wiring-done → boot:engine-start` reads multiple seconds, the engine chunk was COLD and finding 1 is the whole answer; if it reads under a second, the chunk was warm and the remainder is inside initScene/initBuilders/initUI as main-thread contention from the Cesium prewarm (finding 5) — which the axisD baseline does not contain at all, because that path never calls showOnboarding.\n\n(e) COLD vs WARM. `beginStartupBudget` prints `run started (t0 at nav+Nms)` (startupBudget.ts:~240). Small N (<5 s) = the tab had just loaded, engine chunk cold. Large N = warm tab, but STILL cold engine chunk if this is the session's first project. And a New-Project run is identified by `onboarding:shown +0ms` at t0 with NO `hub:open-clicked` (that mark is ProjectHub:2268, the card path only). ⛔ `beginStartupBudget()` WIPES the mark array, so `runtime:composed` / `platform:router-started` / `hub:*` from page load are gone — their absence in his paste is by construction, not a skipped mark.",
  "findings": [
    {
      "title": "The engine-chunk pre-warm is worthless on the New-Project path: the window it was designed to exploit is skipped, leaving a one-HTTP-POST head start on 17.1 MB",
      "detail": "`showOnboarding` starts the engine chunk at PlatformRouter.ts:720 on the strength of its own comment: \"the RAC role/brief/location/draw steps take several seconds — ample time to download + evaluate the chunk\" (engineWarmup.ts:22-24). ⛔ I read the code the comment sits on. On the founder's gesture `seed.directEntry === true`, so `resolveSeededTypologyId` returns 'apartment', the `if (getCurrentUser() && seededTypologyId)` branch at :801 is taken, and :812 emits brief-ready SYNCHRONOUSLY — the RAC panel is never constructed. EventBus.emit is a synchronous for-loop (packages/event-bus/src/EventBus.ts:79-92); briefBootstrap.handleBriefReady has no await before its final `deps.createAndOpenProject(projectName)`; createAndOpenProject calls `ensureEngineWarm()` again at :989 (idempotent no-op) and then hits its FIRST real suspension at :1013 `await this.runtime.persistence.client.create(name)`. So the entire overlap between the chunk fetch and the boot that awaits it is: one POST /api/projects, plus `openProject` step 1 (~0 ms — projectListStore is already populated by the hub sync and by client.create). Everything else — download of ~5 MB wire, and the parse/compile/evaluate of 17,101,633 B uncompressed across engineLauncher 4,899,541 + domain-engine 4,494,087 + vendor-web-ifc 3,556,771 + vendor-thatopen 2,285,651 + vendor-three 1,865,583 (measured in this tree's dist/assets) — lands on the critical path, awaited at src/main.ts:360 `await startEngine(runtimeRef.current)` → `await loadEngine()`. THE SPAN IS ALREADY BRACKETED IN HIS LOG and nobody has read it as the chunk: `boot:heavy-wiring-done → boot:engine-start` IS `await loadEngine()` and nothing else (main.ts:357 then :360).",
      "onCriticalPath": true,
      "costMs": "MEASURED 611 ms with a warm HTTP cache (audit/perf/2026-09-02/baseline-outer.json, axisD-local, median of 5). COLD: UNKNOWN on his machine and it is the largest single unknown in the whole lane. The lane doc PROJECT-OPEN-PERFORMANCE.md quotes 5,777 ms `(program)` parse/compile self on a 16.2 s cold open, but I OPENED that artifact and its `axisD-cold-profile-top-self-time` reports `(program)` selfMs = 1,756 on interactiveMs 16,217 — the 5,777 is NOT corroborated by the file it is attributed to. Treat the cold cost as bounded below by 611 ms and above by ~5,800 ms until his own `boot:heavy-wiring-done → boot:engine-start` delta is read.",
      "costBasis": "repo-artifact",
      "fix": "Start the chunk during a window that actually exists. Two seams, both architectural rather than cosmetic: (1) call `ensureEngineWarm()` when the HUB PAINTS — beside `markStartupPhase('hub:mount-start')` at PlatformRouter.ts:616, or after ProjectHub's `hub:grid-painted` so it does not contend with the thumbnail drain. The user scans cards for seconds; that is the dwell the O.14 comment believed it had. This also fixes the plain card-open path, which today has NO warm at all. (2) Separately, stop CHARGING the chunk to the boot: the founder's own log already brackets it, so any claim about 'the engine boot being slow' must first subtract `boot:heavy-wiring-done → boot:engine-start`. ⛔ Do NOT eager-import the chunk at page load — that competes with the 9.36 MB main chunk and the 5.9 MB blocking Cesium script tag in dist/index.html:5.",
      "risk": "low",
      "confidence": "high",
      "file": "apps/editor/src/ui/platform/PlatformRouter.ts",
      "line": 720,
      "quote": "        ensureEngineWarm();"
    },
    {
      "title": "`await initBuilders(...)` is excluded from its own profiler's table AND from its wall total — the shape the perf audit named is confirmed by reading, and it is the largest single measured boot span",
      "detail": "`beginBootLeg('builders')` opens at engineLauncher.ts:320. The LAST `bootStep()` in the leg is line 418, `bootStep('fragments.init (kickoff, not awaited)')`. The leg's ONLY await is line 456, `} = await initBuilders({ scene: ..., bimManager, projectContext });`. `endBootLeg('builders')` is line 460. `endBootLeg` computes `const wallMs = state.lastAtMs - state.startedAtMs;` (bootStepProfile.ts:229) and `lastAtMs` is only ever advanced by `bootStep()` (bootStepProfile.ts:166). ⇒ the segment 418→460, which contains the entire await, is in NEITHER the rows NOR wallMs. This is not academic: the leg's whole purpose, stated in its own header (bootStepProfile.ts:26-30), is to separate (a) our CPU from (b) SUSPENDED-while-somebody-else-runs, and the ONE segment where we are suspended is the one it does not tile. The `foreign` column — long-task ms inside `await:` steps — therefore reads 0 for the only await that could produce it. Note what is kicked off one line earlier and NOT awaited: line 417 `(fragments as any)._initPromise = fragments.init('/fragments-worker.mjs')` starts a WASM/worker init whose main-thread work lands squarely inside the untiled window.",
      "onCriticalPath": true,
      "costMs": "MEASURED 1,647 ms for the whole `boot:scene-done → boot:builders-done` span (audit/perf/2026-09-02/baseline-outer.json, axisD-local median: 1,774 → 3,421). The builders THEMSELVES measure 11.4 ms over 51 constructor steps on this repo's own harness (bootStepProfile.ts:37-42). ⇒ ~1,636 ms of that span is currently unattributed by construction. On the founder's 2026-09-07 trace the same span read 18,626 ms.",
      "costBasis": "repo-artifact",
      "fix": "One line: `bootStep('await:initBuilders');` immediately after engineLauncher.ts:456, before `markStartupPhase('boot:builders-done')` at :457. The `await:` prefix is exactly the convention bootStepProfile.ts:100-105 defines, and it routes the segment into the own/foreign/idle split so the next reading says whether shortening our code helps at all. Marks-only, gates nothing. This is the highest-information single line available in this lane.",
      "risk": "none",
      "confidence": "high",
      "file": "apps/editor/src/engine/engineLauncher.ts",
      "line": 456,
      "quote": "    } = await initBuilders({ scene: world.scene.three as THREE.Scene, bimManager, projectContext });"
    },
    {
      "title": "`await initUI(...)` has no boot leg at all — there is ZERO long-task attribution anywhere outside the builders leg, and initUI is where Cesium and the web-ifc barrel land",
      "detail": "The `PerformanceObserver` for `longtask` is created in `beginBootLeg` (bootStepProfile.ts:~126) and disconnected in `endBootLeg` (:~226). `beginBootLeg` has exactly one production call site: engineLauncher.ts:320 ('builders'). `endBootLeg` likewise, at :460. So from `boot:builders-done` onward — tools, bus handlers, store registration, initDataPlatform, initUI, initPersistence, initCollaboration, and the whole bootstrap tail — NOTHING observes long tasks. `await initUI({...})` at engineLauncher.ts:1135 is the second-largest measured leg and its own mark comment at :1149 names what is inside it: \"`initUI` is where `mountGISArea` runs.\" It is also where the `@pryzm/file-format` barrel drags web-ifc in (initUI.ts:57,106,107). ⚠ I checked the obvious suspect inside initUI and cleared it: `waitForNextPaints(2); await new Promise(r => setTimeout(r, 250)); await waitForNextPaints(2)` at initUI.ts:396-398 is inside `waitForIfcSceneInteractive`, an IFC-IMPORT helper — it is NOT on the boot path.",
      "onCriticalPath": true,
      "costMs": "MEASURED 663 ms for `boot:data-platform-done → boot:ui-done` (audit/perf/2026-09-02/baseline-outer.json, axisD-local median: 3,763 → 4,426) — and on that bench mountGISArea does NOT eager-mount a globe, because axisD never calls showOnboarding. On the New-Project path it does. The delta is UNKNOWN.",
      "costBasis": "repo-artifact",
      "fix": "`beginBootLeg('ui')` before engineLauncher.ts:1135 and `endBootLeg('ui')` after the await, with `bootStep('await:initUI')` tiling it. Same marks-only contract. Without this, any claim that the Cesium prewarm is or is not stealing the main thread during the boot is unfalsifiable — and finding 5 turns on exactly that number.",
      "risk": "none",
      "confidence": "high",
      "file": "apps/editor/src/engine/engineLauncher.ts",
      "line": 1135,
      "quote": "    await initUI({"
    },
    {
      "title": "608 ms sits between `boot:ui-done` and `open:shell-context-set` with nothing named inside it, and two `await import()`s at the tail of bootstrap exist only to register a console debug helper",
      "detail": "After `markStartupPhase('boot:ui-done')` (engineLauncher.ts:1149) bootstrap still runs ~490 more lines before it resolves: batchCoordinator re-registration, flushRuntimeEventListeners, inspectModeCoordinator.init, workspaceController.restoreFromStorage, the §MT-05 guards, `initPersistence({...})`, `initCollaboration({...})`, the CRDT deferral wiring, and ~6 `pryzm-project-loaded` subscriptions. Then it hits, at the very END of the function, an UNCONDITIONAL block whose only product is a console helper: `const { bindLegacyBrowserGlobals } = await import('./window-shim');` and `const { graphicsRulesEngine, viewDefinitionStore } = await import('@pryzm/core-app-model');` — two awaited dynamic imports on the boot's critical path so that `window.pryzmExplainVisibilityIntent` exists. Only after bootstrap resolves does `startEngine` return, `workspaceMount.ensure()` return, `buildPersistence.openProject` step 4 run, and `PlatformShell.setProjectContext` mark `open:shell-context-set`. ⭐ On the New-Project path `tier.streamLoad` is skipped entirely — `hint?.isNewProject ? Promise.resolve({ok:true, v:null})` (buildPersistence.ts:~318) — so today's L-13278 fix (hoisting that fetch above the boot await) saves this path ZERO. Do not count it.",
      "onCriticalPath": true,
      "costMs": "MEASURED 608 ms for the whole gap (audit/perf/2026-09-02/baseline-outer.json, axisD-local median: boot:ui-done 4,426 → open:shell-context-set 5,034). The two tail `await import()`s specifically: UNKNOWN — both target already-resident chunks (`@pryzm/core-app-model` is the domain-engine chunk, statically imported earlier in this same file), so the cost is probably two microtask hops, not two downloads. State it, do not assume it.",
      "costBasis": "repo-artifact",
      "fix": "Instrument before cutting: add `markStartupPhase('boot:persistence-done')` after `initPersistence(...)`/`initCollaboration(...)` and `markStartupPhase('boot:bootstrap-returned')` as the last line of `bootstrap()`. That splits 608 ms into engine-tail vs buildPersistence-tail and says which to attack. Independently and regardless of the number: move the `pryzmExplainVisibilityIntent` block behind a lazy getter on `window` (bind a function that does the two imports on FIRST CALL). It is a diagnostic; a diagnostic must never be awaited by the boot it is meant to explain.",
      "risk": "low",
      "confidence": "high",
      "file": "apps/editor/src/engine/engineLauncher.ts",
      "line": 1595,
      "quote": "        const { bindLegacyBrowserGlobals } = await import('./window-shim');"
    },
    {
      "title": "The Cesium globe prewarm is real parallelism on the network and FALSE parallelism on the main thread — and it is the one thing the 6,522 ms baseline does not contain",
      "detail": "`showOnboarding` fires four heavy starts within three lines of t0: `ensureEngineWarm()` (:720), `ensureCesiumWarm()` (:728), `requestEagerGlobeStart()` (:733), and `prewarmGlobe({parent: document.getElementById('container'), runtime: this.runtime})` (:~755). `prewarmGlobe` (eagerGlobeStart.ts:180-215) does `new Ctor(parent, runtime)` → `globe.enterWarmHiddenState()` → `await globe.mount()`. Cesium itself is already parsed (dist/index.html:5 loads `/cesium/Cesium.js`, 5,909,848 B, as a blocking classic script at page load), so this is not a chunk cost — it is Viewer CONSTRUCTION, WebGL context creation, imagery/photoreal tile decode and `§STARTUP-GLOBE-COMPLETE-FIRST` tile settling, all on the one main thread that is simultaneously parse/compiling 17.1 MB of engine JS and then running initScene → initBuilders → initUI. The module's own header (eagerGlobeStart.ts:37-39) claims it runs \"in genuine parallel with the boot\". Parallel on the NETWORK, yes. On the MAIN THREAD, JavaScript has one; these two workloads interleave and their wall times ADD. ⭐ THIS IS THE STRUCTURAL DIFFERENCE between the founder's path and the 6,522 ms axisD baseline: axisD is a deep-link open that never calls showOnboarding, so it pays no prewarm, and it reaches first-interactive-frame in 6,522 ms with 300 walls. He pays the prewarm and takes 9,233 ms with zero.",
      "onCriticalPath": true,
      "costMs": "UNKNOWN, and it is the single most valuable unknown after the chunk. Bounded by the delta between his run and the axisD baseline once the chunk leg is subtracted. His own log already contains the two brackets: `globe:prewarm-start → globe:prewarm-done` prints an explicit `constructed + mounted WARM-HIDDEN in N ms` line (eagerGlobeStart.ts:~208). That N is wall time, not CPU — the `foreign` column from finding 3's `beginBootLeg('ui')` is what converts it into main-thread contention.",
      "costBasis": "unknown",
      "fix": "⛔ Do NOT delete the prewarm — it is the fix for L-10560 and removing it puts the globe construction back in series AFTER the boot, which is strictly worse. The architectural move is to make the two workloads NOT contend for the same first second: (a) read `globe:prewarm-done` against `boot:engine-start` in his log — if the prewarm completes BEFORE the chunk finishes evaluating, they are already serialised and this finding is void; (b) if they overlap, the honest ordering question is which one the FIRST FRAME needs. His first frame is the globe with a location card. It needs the prewarmed viewport and the DOM card. It does NOT need initBuilders, initTools, or the 37 stores — those exist to hold a BIM model that is provably empty (`0 elements, 1 levels, 0 walls`). That is the real finding and it belongs to whichever dimension owns boot-stage ordering: the location step is gated on `pryzm-project-loaded`, which is gated on the FULL `bootstrap()`, and nothing in the location step reads a builder.",
      "risk": "medium",
      "confidence": "medium",
      "file": "apps/editor/src/engine/eagerGlobeStart.ts",
      "line": 204,
      "quote": "                await globe.mount();"
    },
    {
      "title": "NEGATIVE — the eager globe init is `void`ed, not awaited: it does not delay the frame, and neither do the deferred doc/export tail or the post-load thumbnail",
      "detail": "The brief asked me to verify the things that SAY deferred. All three check out, so nobody should spend a lane on them. (1) `mountGISArea`'s eager arm is `if (consumeEagerGlobeStart()) { markStartupPhase('globe:eager-init-start'); void ensureGisInitialized('eager').then(() => {...}); }` (GISAreaLayout.ts:2633-2638) — `void`, not awaited, so the `await consumePrewarmedGlobe()` at :1090 (which DOES await an in-flight prewarm) never blocks `initUI` and therefore never blocks `bootstrap()`. (2) The `[main/deferred]` tail (Sheet/TitleBlock/DXF/Schedule/ViewTemplate/PhaseFilter/FastPathProjector, initUI.ts:483-520) is `runDeferredDocSubsystems`, reached from a `requestIdleCallback` (initUI.ts:828) — a genuine post-paint macrotask, not a later step in the same task. Its body is nine `window.X = <already-constructed singleton>` assignments plus `dxfExportService.init` and `scheduleStore.seedDefaultSchedules()`. (3) `schedulePostLoadThumbnailCapture` (PlatformSaveController.ts:521) early-returns on `if (detail.empty)` at :539 — a brand-new project fires `pryzm-project-loaded(empty:true)` (PlatformShell.ts:~311), so the 3,500 ms capture never arms and the founder's `[captureThumbnail] Read a blank/transparent frame` line comes from the AUTOSAVE path, not this one.",
      "onCriticalPath": false,
      "costMs": "0 ms on the critical path, all three. MEASURED-BY-READING: the deferral mechanisms are requestIdleCallback / void-promise / early-return, verified at the quoted lines.",
      "costBasis": "read-from-code",
      "fix": "No action. Recorded so these are not re-reported as wins. ⚠ ONE residual worth a separate row: the `detail.projectId !== projectId` early-return at PlatformSaveController.ts:534 returns WITHOUT unsubscribing, leaking one dead `pryzm-project-loaded` listener per switched-away-from open. `_armPostSceneMarks` (PlatformShell.ts:188-189) shows the correct idiom in the same codebase. That is a leak, not a startup cost.",
      "risk": "none",
      "confidence": "high",
      "file": "apps/editor/src/ui/layout/GISAreaLayout.ts",
      "line": 2635,
      "quote": "        void ensureGisInitialized('eager').then(() => {"
    },
    {
      "title": "NEGATIVE — the 71-mesh cinematic tier and the 103.9 ms shadow rebuild live INSIDE the 334 ms post-load window, after the frame is already scheduled",
      "detail": "`applyTierForMeshCount` has two production call sites, both in initScene.ts: :2983 inside `runTierPbrPass` (the per-geometry-add path) and :3126 inside `batchCoordinator.setPostBatchCallback`. On an empty project neither fires during boot — there is no geometry add and no batch. What fires is the consolidated post-load pass: `window.runtime?.events?.on('pryzm-project-loaded', () => { setTimeout(() => { _runConsolidatedTierPbrPass?.(); ... }, 0); })` at initScene.ts:3760-3766, plus a sibling handler at :3723 that synchronously calls `renderPipelineManager.onProjectLoaded()` and `_thawShadowAfterLoad()`. Both are downstream of the SAME `pryzm-project-loaded` emit that arms the first-interactive-frame mark, so the `[SceneQualityTier] 71 meshes → tier=cinematic`, the `Rebuilding pipeline after shadow-map update` and the `SHADOW_REBUILD_COMPLETE elapsed=103.9ms` all land in the `open:snapshot-loaded → open:first-interactive-frame` gap — which is his own measured 334 ms, i.e. 3.6 % of the 9,233. The `fullScenePbrTraverse=ON` alarm is also a ghost on this scene: RenderingPipelineCoordinator.ts:722-724 says in its own text that the 38.7 s synchronous pass was dismantled in 2026-05 and ON now arms a chunked, idle-deferred pass over NEW meshes only — of which an empty project has none.",
      "onCriticalPath": false,
      "costMs": "MEASURED 334 ms for the ENTIRE post-load window, from the founder's own line `open:first-interactive-frame +334ms`, of which `SHADOW_REBUILD_COMPLETE elapsed=103.9ms` is a named 104 ms. Ceiling on any possible saving here: 334 ms = 3.6 % of his wait.",
      "costBasis": "founder-log",
      "fix": "Do not fund a tier/shadow lane off this brief. 334 ms is the whole prize and 104 ms of it is already down from 16,011 ms (RenderPipelineManager.ts:195 records the old reading) by today's L-13270. ⚠ ONE question does survive and is cheap: what ARE the 71 meshes on a project with `0 elements, 1 levels, 0 walls`? They are grid/helper/gizmo/light geometry, and 71 of them is what pushes `applyTierForMeshCount` into `cinematic`. If a helper-only scene should not be choosing the top tier, the fix is in the COUNTER (exclude `userData.isHelper`, which `collectNewPbrMeshes` and `waitForIfcSceneInteractive` already filter on), not in the tier policy. See also seam 4 of the 2026-09-09 lane doc: four rival mesh counters already disagree.",
      "risk": "none",
      "confidence": "medium",
      "file": "apps/editor/src/engine/initScene.ts",
      "line": 3762,
      "quote": "            setTimeout(() => {"
    },
    {
      "title": "The summary table is never printed at the moment the founder cares about — its single trigger is minutes downstream, past three human decisions",
      "detail": "`reportStartupBudget` has exactly one production call site: `enterCanvasWithSitePlan.ts:188`, `reportStartupBudget('enter-canvas')`. On the New-Project path that is reached only after the user has geocoded a location, flown to a parcel, selected or drawn a boundary, and clicked through to the canvas — the startupBudget.ts header itself records that the `envelope:dispatched → enter-canvas` interval was once 300,855 ms of a human reading (§STARTUP-ENTER-CANVAS-IS-NOT-DWELL, L-13040). So at t+9,233 ms, the instant the founder's complaint is about, the reporter has not fired and will not fire for minutes — or never, if he abandons or reloads. He gets the individual `[§STARTUP-BUDGET]` lines (markStartupPhase logs unconditionally, startupBudget.ts:286-289) but no reconciled table. That is why every audit of this problem has been reasoning from one pasted line.",
      "onCriticalPath": false,
      "costMs": "0 ms — this is an instrument, not a cost. But it is the reason the 3,400 ms remainder in this dimension's arithmetic is a remainder rather than a row.",
      "costBasis": "read-from-code",
      "fix": "Fire the table where THIS path ends, not where the whole onboarding ends. In `_armPostSceneMarks`'s scheduleOnce callback (PlatformShell.ts:204), after `markStartupPhase('open:first-interactive-frame')`, add a guarded report. ⚠ The lane doc's proposed guard — `getStartupBudgetMarks().some(m => m.phase === 'hub:open-clicked')` — is WRONG FOR THIS PATH: a New-Project run never marks `hub:open-clicked` (ProjectHub:2268 is the card-open handler; `startGuidedOnboardingDirect` at :2038 goes to `onStartOnboarding` instead), so that guard suppresses the table on exactly the path the founder is complaining about. Guard on `onboarding:shown` being present INSTEAD, or on neither, and accept that `reportStartupBudget`'s `_reported` latch (startupBudget.ts:313) means the later `enter-canvas` call becomes the no-op. Losing the enter-canvas table (which measures a human deciding) to gain the click-to-globe table (which measures the machine) is the right trade and should be stated in the commit, not smuggled.",
      "risk": "low",
      "confidence": "high",
      "file": "apps/editor/src/ui/site/overlay/enterCanvasWithSitePlan.ts",
      "line": 188,
      "quote": "    reportStartupBudget('enter-canvas');"
    },
    {
      "title": "There is no mark anywhere between t0 and `boot:ensure-requested` — the create hop is the New-Project twin of the hole `hub:open-clicked` closed",
      "detail": "(c) Which phases are MISSING on this path. Present: onboarding:shown, cesium:warm-start, globe:prewarm-start/-done, open:router-launch, open:persistence-openProject, boot:ensure-requested, boot:heavy-wiring-done, boot:engine-start, boot:scene-done, boot:builders-done, boot:tools-done, boot:bus-handlers-done, boot:data-platform-done, boot:ui-done, open:shell-context-set, open:version-read-start/-done, open:snapshot-loaded, open:first-interactive-frame, globe:eager-init-start/-done, open:project-loaded, location-step:open. ABSENT BY CONSTRUCTION and therefore not findings: hub:open-clicked (card path only), hub:back-clicked, and every page-load mark (runtime:composed, platform:router-started, hub:mount-start, hub:warm-*, hub:sync-*) — `beginStartupBudget()` sets `_marks = []` (startupBudget.ts:~232), so a New-Project run legitimately starts from zero and their absence in his paste is not a skipped mark. THE REAL GAP: between `cesium:warm-start` at t≈0 and `open:router-launch` sits the entire create hop — `_ensureEngineLoadingOverlay()` mounting the loader, and `await this.runtime.persistence.client.create(name)` at PlatformRouter.ts:1013, one HTTP POST to a Fly-hosted server. Nothing marks either side of it. This is the same shape as the 44.2 s hole and the enter-canvas 300 s, in a third place.",
      "onCriticalPath": true,
      "costMs": "UNKNOWN. `t0 → open:router-launch` in his log IS this span (launchWorkspace marks :1083 immediately on entry, and its only caller here is :1033, the line after the create resolves), so the number already exists in his console and nobody has quoted it. ESTIMATE 200–800 ms for one prod round trip plus overlay mount — but the mark makes estimating unnecessary.",
      "costBasis": "read-from-code",
      "fix": "Two marks, marks-only: `markStartupPhase('create:issued')` immediately before PlatformRouter.ts:1013 and `markStartupPhase('create:done')` immediately after the summary is validated at :1017. `create:issued → create:done` is then the server's number and `t0 → create:issued` is the synchronous prewarm+emit task, so the head start finding 1 depends on becomes a measured value instead of an argument. ⛔ Per startupBudget.ts:36 add phases, never rename one.",
      "risk": "none",
      "confidence": "high",
      "file": "apps/editor/src/ui/platform/PlatformRouter.ts",
      "line": 1013,
      "quote": "                const summary = (await this.runtime.persistence.client.create(name)) as {"
    },
    {
      "title": "The first frame the founder waits for is gated on a full BIM engine boot it demonstrably does not use",
      "detail": "THE STRUCTURAL STATEMENT THE ARITHMETIC POINTS AT, and the answer to 'not doing work that does not need doing yet'. The location card is rendered by `renderLocationStep()` (OnboardingStepController.ts:706-708), reached synchronously from `startOnboardingStepFlow` ← `controller.start()` (:535-546) ← briefBootstrap's one-shot `pryzm-project-loaded` handler. briefBootstrap's own comment at that handler calls it \"⭐ THE GATE ITSELF\": \"renderLocationStep() (and therefore pryzmToggleGIS(true)) cannot run until this line does, and this line cannot run until the FULL engine bootstrap + the project hydrate have finished.\" `pryzm-project-loaded` is emitted by PlatformShell's new-project branch, which is reached from `setProjectContext`, which is buildPersistence step 4, which is strictly after `await attachedBootstrap.ensure()` → `await startEngine()` → `await mod.bootstrap(runtime)`. So the globe + location card wait on: initScene (869 ms), initBuilders (1,647 ms), initTools (250 ms), ~80 bus handlers (81 ms), 37 store registrations, initDataPlatform, initUI (663 ms), initPersistence, initCollaboration. WHAT THE LOCATION STEP READS FROM ALL OF THAT: `window.pryzmToggleGIS`, `window.pryzmGetSiteEntryCameraHost`, `window.pryzmGetSiteEntryCameraHostReady`, `window.pryzmSetGeocodeFrame` — four window hooks registered in `mountGISArea`, plus a Cesium viewport that `prewarmGlobe` ALREADY BUILT at t0, independently, before the boot started. It reads no wall store, no builder, no tool. ⭐ AND THE PROJECT IS EMPTY: `Snapshot created: 0 elements, 1 levels, 0 walls, 0 slabs, 0 furniture`.",
      "onCriticalPath": true,
      "costMs": "MEASURED ~3,700 ms of boot legs strictly between `boot:engine-start` and `boot:ui-done` (audit/perf/2026-09-02/baseline-outer.json, axisD-local median: 905 → 4,426), of which the location step provably consumes only the tail of initUI. On the founder's machine, with the Cesium prewarm contending, UNKNOWN and likely larger.",
      "costBasis": "repo-artifact",
      "fix": "⛔ NOT 'skip the boot' and NOT a spinner. The architecturally sound move is to stop conflating two different readiness signals that today share one event. `pryzm-project-loaded` means BOTH 'the model is hydrated' (what PlatformShell, the tier pass, the frustum audit and 30-odd other listeners want) AND 'the guided flow may open its first step' (all briefBootstrap wants). Split them: emit a `pryzm-onboarding-surface-ready` the moment `mountGISArea` has registered its four window hooks and `consumePrewarmedGlobe()` has resolved — a point that is reached INSIDE initUI, before initPersistence, before the bootstrap tail, and before `setProjectContext` — and gate `startOnboardingStepFlow` on THAT, leaving `pryzm-project-loaded` untouched for everyone else. ⚠ PRICE IT FIRST, DO NOT SHIP IT ON THIS PARAGRAPH: (a) `renderLocationStep` calls `resetAppPhaseForNewProject()` and `mountOverlay()` — verify neither reads a store the boot has not built; (b) the step's own `handleGeocode` → `createSiteFromRect` path DOES need an open project and an active level, so the GATE moves but the COMMIT must still wait — that is a second gate, not a removed one; (c) this reorders §L-1186's phase declaration. Contracts C11 (element creation pipeline) and C06 (UI shell) both bear on it, and ADR-0369 §6-§7 already owns the 'globe before boot' question — this is Stage 3 of that ADR, so amend it rather than minting a rival.",
      "risk": "high",
      "confidence": "medium",
      "file": "apps/editor/src/ui/onboarding/briefBootstrap.ts",
      "line": 251,
      "quote": "        markStartupPhase('open:project-loaded');"
    }
  ],
  "whatIcouldNotEstablish": "1. THE 3,400 ms REMAINDER. Warm-cache reconstruction of the boot legs sums to ~5,824 ms against his 9,233 ms. I cannot close that gap from the tree because every span I have is transferred from `audit/perf/2026-09-02/baseline-outer.json` — a DIFFERENT path (deep-link open of a 300-wall existing project), a DIFFERENT machine (localhost headless chromium), and a WARM HTTP cache. Two candidates, and ONE line he already has separates them: `boot:heavy-wiring-done → boot:engine-start`. Multiple seconds ⇒ cold engine chunk ⇒ finding 1 is the whole answer. Under a second ⇒ the remainder is main-thread contention inside the boot legs, and findings 3 + 5 are.\n\n2. THE COLD PARSE/COMPILE NUMBER IS NOT WHAT THE CORPUS SAYS IT IS. The brief and the 2026-09-09 lane doc both cite \"5,777 ms parse/compile self on a cold open\" from a 2026-09-02 cpuprofile. I opened that artifact. `audit/perf/2026-09-02/baseline-outer.json → axisD-cold-profile-top-self-time` reports `(program)` selfMs = **1,756** on `interactiveMs` 16,217, and no entry anywhere near 5,777. The lane doc itself hedges (\"reported by the open-timeline lane; I did not re-open it\"). ⛔ DO NOT SIZE FINDING 1 OFF 5,777 — it is a number nobody in this session can point at a file for.\n\n3. WHETHER THE PREWARMED GLOBE ACTUALLY STEALS THE MAIN THREAD, and how much. `globe:prewarm-start → globe:prewarm-done` is wall time, not CPU. Separating \"our boot was slow\" from \"our boot was suspended while Cesium built a Viewer\" is precisely what `bootStepProfile`'s own/foreign/idle split exists for — and there is no leg around `initUI`, and the one leg that exists does not tile its only await. Both instruments (findings 2 and 3) must land before this is answerable, and both are marks-only one-liners.\n\n4. THE EMPTY-SNAPSHOT HYDRATE COST. `open:version-read-done → open:snapshot-loaded` measured 988 ms at 300 walls; at 0 elements it is `loadAdapter.load(this._makeEmptySnapshot(...))` (a ClearProjectCommand across ~30 stores) and I have no measurement of that at zero. I assumed 50–300 ms in the arithmetic. His own log carries the exact pair.\n\n5. WHETHER THE AUTOSAVE OF THE EMPTY PROJECT (`Version saved: \"Auto-save\" (0 elements)`) precedes the first frame. `resetDirtyAfterLoad()` runs synchronously between the hydrate and the `pryzm-project-loaded` emit (PlatformShell.ts:~306-311) and the save itself is debounced, so it is PROBABLY in or after the 334 ms window — but I did not read the debounce, and 334 ms is the whole prize there anyway. Not worth further budget.\n\n6. THE COLD-PAGE-LOAD HALF. `dist/index.html:5` loads `/cesium/Cesium.js` — **5,909,848 B** — as a render-blocking classic `<script>` in `<head>`, and `main-Bzw-2t7Y.js` is **9,357,174 B**. Neither is on the New-Project click path (both are page-load), but if the founder's \"new project\" gesture ever includes a reload, they dominate everything in this report. That question is one sentence to him and it is worth more than any code here: **did the tab reload, or were you already on the projects hub?**"
}