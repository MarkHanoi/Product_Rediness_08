{
  "dimension": "What the user can and cannot do while `site.scope-changed` reloads the 3D-Site context — teardown/rebuild order, main-thread blocking, input gating, and what is on screen during the wait",
  "summary": "⭐ THE HEADLINE REVERSES THE BRIEF'S WORST CASE: the old context is NEVER torn down at t=0. Every one of the eight loaders `setContextScope` fires already builds first and clears last (`CesiumViewport.ts:13837` roads, `:12275` buildings, `:15100` trees — each after the last `await`, pinned by `siteScopeSwapNeverBlank.spec.ts`). The founder is NOT staring at an empty plate for 45 s; he is looking at the r=1781 plate while a wider one is assembled behind it, and then it swaps layer by layer. Half of the architecture he proposed is already built and shipped (C12 §13.4 / L-13173).\n\n⭐ AND NOTHING GATES INPUT. There is no modal, no overlay, no disabled control, no `pointer-events` gate anywhere on this path. The only indicator that exists — the context loading badge — carries `pointerEvents:'none'` (`:2171`) AND is *not shown at all* on a scope reload, because it is armed only `if (this.contextBuildingPlacements.length === 0)` (`:11985`) and on a reload the old placements are non-empty. So the true answer to \"what can he not do\" is: **nothing is forbidden; he is given no signal that anything is happening, and the app stutters when each layer's placement loop runs.**\n\n⭐ THE ~45 s IS I/O, NOT CPU, and the repo has already measured this itself. `contextTiles.ts:691` measures decode at **67 ms for 13,339 features** against **798 ms** for the range requests; `CesiumViewport.ts:10793` measures the entire drape decomposition at **296 ms** on a corpus 3× the founder's. His own log agrees: roads `134 ms own work` against `14,292 ms waiting`. So the wall clock is almost entirely network wait — which is exactly why his \"build it off to the side\" design is achievable here: there is nothing to move off the main thread, only something to start earlier and reveal later.\n\n⛔ THE ONE REAL ARCHITECTURAL BLOCKER is that `this.contextScope` is a single mutable field (`:2318`) written synchronously by `setContextScope` before the loaders fire, and read **30 times**, many of them *after* awaits inside loader bodies (`:13579`, `:15038`). Two scopes cannot be in flight at once because there is only one slot to read them from. Making the scope a per-load parameter is the whole cost of the founder's design.\n\n⛔ AND HIS STRATEGIC PREMISE — \"at r=5755 the plate is THINNER near the site than at r=1781\" — is FALSE, and I should say so before anyone builds a slider behaviour on it. Every binding cap is core-first or nearest-first: buildings via `plateFill.ts` `coreShareOfCap: 0.5` (the nearest 12,000 of 24,000 at FULL density, ≈2,491 m at his measured 616/km² — *wider* than the whole r=1781 plate), trees nearest-first (his panel's own \"complete at ~3704 m\"), drape probes nearest-first (`groundFeatureSeat.ts:183` \"spend one layer-wide budget NEAREST FIRST\"), street life nearest-first (his own log). What he loses at r=5755 is only the RIM beyond ~2.5 km. The extra scope buys him little, but it costs him nothing near the site.",
  "findings": [
    {
      "title": "The old context is NOT removed at t=0 — every loader swaps, none blanks",
      "detail": "`setContextScope` (CesiumViewport.ts:2335) writes the field and then fires eight loaders with bare `void` — no await, no teardown: `void this.loadContextBuildings(at.lat, at.lon, true); void this.loadContextTrees(...); void this.loadStreetLife(...); void this.loadContextRoads(...); void this.loadContextRail(...); void this.loadContextParks(...); void this.loadContextWater(...); void this.loadContextLanduse(...)` (:2352–2359). I verified the clear position IN THE CODE for three of them rather than trusting the comment: roads clears at :13837, AFTER `await fetchContextRoads` (:13820) and `await this.resolveGroundDrapePieces` (:13830); buildings clears at :12275, AFTER `await this.sampleContextGroundsBatch(nearGroundCentroids)` (:12271); trees clears at :15100, AFTER its own sample await (:15095). Each clear is followed IN THE SAME SYNCHRONOUS TASK by the rebuild loop, and the viewer runs `requestRenderMode: true` (:3581) — so no frame can be painted between the removal and the replacement. This is pinned independently by `apps/editor/src/ui/geospatial/__tests__/siteScopeSwapNeverBlank.spec.ts`, which greps the method bodies with comments stripped ('a comment quoting the old order is not the order').",
      "blocksInteraction": false,
      "costMs": "0 ms of blank scene. The previous plate is continuously on screen for the whole reload.",
      "costBasis": "read-from-code",
      "fix": "Nothing to fix — record it, because it changes the whole plan. The founder asked for 'the current one stays live, then swap when ready' and that is already the shipped behaviour. What is missing is not the swap; it is (a) any signal that a build is running and (b) the ability to run two scopes at once. Scope the lane to those two, not to a rewrite.",
      "risk": "none",
      "confidence": "high",
      "file": "apps/editor/src/ui/geospatial/CesiumViewport.ts",
      "line": 2352,
      "quote": "      void this.loadContextBuildings(at.lat, at.lon, true);"
    },
    {
      "title": "Nothing gates input — no modal, no overlay, no disabled state, no pointer-events trap",
      "detail": "I looked for an input gate on this path and there is none. The only indicator in the geospatial layer is the context loading badge, and its own header states the design intent: 'Deliberately a small badge, NOT the full-screen `LoadingOverlayController`: the site, the parcel and the envelope are already useful and interactive before the neighbourhood arrives, so blocking the view would be a downgrade' (:2126). Its element carries `pointerEvents: 'none'` (:2171). The scope slider itself sets `input.disabled = false` on every paint where a scope exists (SiteScopeSlider.ts, `paint()`), so it stays draggable throughout. Grepping the two owning files for `pointerEvents = 'none'`, `cursor: 'wait'`, `aria-busy` and `LoadingOverlayController` returns only the three badge/legend elements, all of them non-interactive decorations. So the honest statement is: he can pan, click a parcel, open a panel and draw for the entire reload. The app is not blocked; it is silent and its frames get long.",
      "blocksInteraction": false,
      "costMs": "0 ms of hard input gating.",
      "costBasis": "read-from-code",
      "fix": "Say this plainly to the founder — it reframes his ask. He is not locked out; he is uninformed and occasionally stuttering. The fix is a progress signal plus smoother placement, not an unblocking mechanism.",
      "risk": "none",
      "confidence": "high",
      "file": "apps/editor/src/ui/geospatial/CesiumViewport.ts",
      "line": 2171,
      "quote": "            boxShadow: '0 1px 6px rgba(0,0,0,0.14)', pointerEvents: 'none',"
    },
    {
      "title": "⛔ He sees NO progress signal at all — the loading badge is explicitly suppressed on a scope reload",
      "detail": "The badge is armed at exactly one point on the buildings path: `if (this.contextBuildingPlacements.length === 0) this.setContextLoadingVisible(true);` (:11985). On a SCOPE change the old context is still placed, so `contextBuildingPlacements.length > 0` and the badge never appears. The guard's own comment says why: 'Only when nothing is on screen yet: a refresh that already has neighbours drawn must not flash a badge over a scene that looks complete.' That reasoning was written for a PAN refresh (seconds, invisible). At r=5755 it produces a ~45 s wait with zero feedback, on a gesture the user just made deliberately. No other layer arms any indicator — roads, parks, water, rail, landuse and trees log to the console and paint nothing. This is the single largest gap between what the code does (correctly) and what the founder experiences.",
      "blocksInteraction": false,
      "costMs": "~45 s of unsignalled wait (from his own log: roads 43,280 ms is the longest single read).",
      "costBasis": "read-from-code",
      "fix": "Arm a DIFFERENT indicator for the scope-change case, keyed on the gesture rather than on emptiness. `setContextScope` already knows both radii — it logs `scope circle 1781 m → 5755 m`. Show a small non-blocking chip on the slider itself ('Widening to 5 755 m — 3 of 8 layers ready'), driven by counting settled loader promises. `setContextScope` can collect the eight promises it currently discards with `void` and count settlements without awaiting them, so nothing on the render path changes. ⚠ Keep the existing emptiness-gated badge exactly as it is — this is a second, additive signal, not a re-purposing.",
      "risk": "low",
      "confidence": "high",
      "file": "apps/editor/src/ui/geospatial/CesiumViewport.ts",
      "line": 11985,
      "quote": "    if (this.contextBuildingPlacements.length === 0) this.setContextLoadingVisible(true);"
    },
    {
      "title": "The cap panel is NOT live — it repaints only on user interaction, so during and after the load it prints the PREVIOUS load's numbers against the NEW radius",
      "detail": "The slider has exactly two repaint triggers: `const unsubscribe = store.subscribe(() => { paint(); });` — that is the PaneLayoutStore, which fires on layout changes, not on context loads — and the exported `refresh()`. Grepping `apps/editor/src` for a caller of that handle's `refresh` finds none outside `SiteAuthoringPaneShell`'s own `dispose` loop (:714); `defaultSiteScopePorts()` (:270) is pull-only, with no event subscription of any kind. Meanwhile the numbers it prints come from `getScopeCapVerdicts()` → `this.scopeCapReports`, which each loader writes at the END of its run (buildings at :12569). So the sequence on a release is: `onRelease()` → `commit()` → `paint()` — which computes `completenessCaption(newScope, oldMark, oldVerdicts)` — and then nothing repaints for the next ~45 s while the real numbers land. The caption the founder reads immediately after a drag is arithmetic on the PREVIOUS scope's densities evaluated against the new radius.",
      "blocksInteraction": false,
      "costMs": "The panel is stale for the whole ~45 s window and stays stale until the next pointer interaction.",
      "costBasis": "read-from-code",
      "fix": "Add one port — `onMeasured(cb)` — and have `CesiumViewport` fire it after each `scopeCapReports.set(...)`; the slider calls `paint()`. This is also the natural carrier for the progress chip above: the same callback says 'buildings measured', 'trees measured'. ⚠ Do NOT make the slider poll: the ports are deliberately pull-only and a timer would repaint the caption mid-drag.",
      "risk": "low",
      "confidence": "high",
      "file": "apps/editor/src/engine/views/SiteScopeSlider.ts",
      "line": 624,
      "quote": "    const unsubscribe = store.subscribe(() => { paint(); });"
    },
    {
      "title": "The ~45 s is network wait, not computation — the repo has already measured both halves",
      "detail": "Two independent in-repo measurements settle this. (1) `contextTiles.ts:691`: 'decode (VectorTile → toGeoJSON → ring filter, 13,339 features) ... 67 ms' against 'all range requests, DIRECT R2, issued concurrently ... 798 ms'. (2) `CesiumViewport.ts:10793`: 'MEASURED 2026-09-06 on a Sète-shaped corpus THREE TIMES his size (43,551 pieces from 3,442 features, against his 13,788 from 1,342), the ENTIRE pure decomposition — seat points, probes, the split decision and every Sutherland–Hodgman grid clip — costs 296 ms.' His own r=5755 log reproduces the ratio: roads `14,292 ms waiting for terrain (shared FIFO), 134 ms own work`; parks `14,091 ms waiting, 170 ms own work`. So of the ~45 s, the overwhelming majority is a socket, not a CPU. ⭐ THAT IS WHY HIS DESIGN WORKS: there is very little computation to hide, only a wait to start earlier.",
      "blocksInteraction": false,
      "costMs": "~43,280 ms roads / ~31,791 ms buildings, of which the measured non-I/O share is ~0.3–1 %.",
      "costBasis": "repo-artifact",
      "fix": "Attack the READ, not the render. Two levers, both with existing mechanisms — see the tile-cache and fan-out-cap findings below. Do not spend the lane on off-main-thread geometry: the measurement says there is nothing there to win.",
      "risk": "none",
      "confidence": "high",
      "file": "apps/editor/src/ui/geospatial/contextTiles.ts",
      "line": 691,
      "quote": " *   • decode (VectorTile → toGeoJSON → ring filter, 13,339 features) ............   67 ms"
    },
    {
      "title": "The longest synchronous span is the road-ribbon placement loop — 10,138 unyielded `entities.add`, and it is the ONE scope-scaling layer with no count cap",
      "detail": "Between `this.clearContextRoads()` (:13837) and the closing `viewer.scene.requestRender()` (:13932) there is no `await`, no `setTimeout`, no yield of any kind — I grepped the whole file for `await new Promise`, `setTimeout(`, `requestAnimationFrame` and `scheduler.yield` and none of the eleven hits is inside a placement loop. So that block is one uninterrupted task. Its length is `roadWays.length × pieces`, which his log reports as 10,138. ⛔ AND ROADS HAVE NO CAP: grepping `contextRoads.ts` for `MAX`, `slice(0`, `cap` returns nothing but a doc reference; same for `contextRail.ts`, `contextParks.ts`, `contextLanduse.ts`. Buildings (`CTX_FAR_TIER_MAX_INSTANCES_CEILING` 24,000), trees (10,000) and street life are all capped; the five ground/linear layers are not, and their entity counts grow with r². ⚠ WHAT I CANNOT ESTABLISH: the per-entity cost. There is no `performance.now()` around any placement loop in this file (the only two are inside `resolveGroundDrapePieces`), so the actual milliseconds of the longest synchronous span are UNMEASURED. Cesium's own `GeometryVisualizer` batching may push most of the work into workers; I will not assert either way without a browser profile.",
      "blocksInteraction": true,
      "costMs": "UNKNOWN — no instrumentation exists. Bounded above by construction: ~10,138 `entities.add({corridor})` in one task at r=5755, ~1,536 near-building polygon entities, plus rail/parks/water/landuse.",
      "costBasis": "unknown",
      "fix": "FIRST measure it: wrap each placement loop in `performance.now()` and print a `§PLACEMENT-COST` line beside the existing read/drape lines. It is one line per loader and it settles the question the whole lane turns on. THEN, if it is material, chunk the roads loop — place N entities, yield through the frame bus (P3: `packages/frame-scheduler`, never a raw rAF), continue. The swap-never-blank order survives chunking only if the clear stays paired with the FIRST chunk, so `siteScopeSwapNeverBlank.spec.ts` needs its arm re-expressed before the loop moves. ⚠ A road count cap is the wrong fix here — roads are the layer that makes the plate read as a city.",
      "risk": "medium",
      "confidence": "medium",
      "file": "apps/editor/src/ui/geospatial/CesiumViewport.ts",
      "line": 13837,
      "quote": "    this.clearContextRoads();"
    },
    {
      "title": "⛔ THE ARCHITECTURAL BLOCKER: `this.contextScope` is one mutable slot, read after awaits — two scopes cannot be in flight",
      "detail": "`private contextScope: SiteContextScope = DEFAULT_SITE_CONTEXT_SCOPE;` (:2318). `setContextScope` writes it synchronously (`this.contextScope = scope;` :2338) and `this.streetLife.scope = scope;` (:2339) before firing anything. The field is then read 30 times across the file, and critically several of those reads happen INSIDE loader bodies AFTER their awaits — `treesRadiusM(this.contextScope, ...)` at :15038 (after the tree fetch), `farTierRadiusM(this.contextScope)` at :13579 (in the far-tier build), `groundFetchHalfDeg(this.contextScope)` at :13820/:13971/:14107/:14549 (at each fetch call). A load that started at 1781 m therefore finishes reading 5755 m out of the shared slot. `this.streetLife.scope` has the identical problem, and `scopeClipperCache` (:2374) is a single-entry cache keyed by scope×origin×θ, so two scopes would thrash it. THIS is what makes 'build the new one off to the side' impossible today — not the entities, not the swap, but that the loaders have no way to be told which scope they belong to.",
      "blocksInteraction": false,
      "costMs": "n/a — this is a correctness/architecture bound, not a duration.",
      "costBasis": "read-from-code",
      "fix": "Thread the scope as a PARAMETER. Each `loadContext*(lat, lon, force)` becomes `loadContext*(lat, lon, scope, force)` and every `this.contextScope` read inside a loader body becomes the local. `this.contextScope` stays as the field the SLIDER and the clip read (the 'current' scope), but ceases to be what a load in flight consults. Do the same for `streetLife.scope` (pass it into `load`) and make `scopeClipperFor(lat, lon)` take the scope. That is a mechanical change across ~12 call sites and it is the whole precondition for the founder's design. ⛔ Do NOT add a second `pendingScope` field beside the first — two slots for one value is the rival-owner shape this repo keeps paying for (see `view-region-one-owner`).",
      "risk": "medium",
      "confidence": "high",
      "file": "apps/editor/src/ui/geospatial/CesiumViewport.ts",
      "line": 2318,
      "quote": "  private contextScope: SiteContextScope = DEFAULT_SITE_CONTEXT_SCOPE;"
    },
    {
      "title": "A SECOND slider release during a load silently keeps the OLD scope's buildings — the coalescer keys on (lat, lon) only",
      "detail": "`loadContextBuildings` joins an in-flight load whenever the coordinates match: `if (running && Math.abs(running.lat - lat) < 1e-6 && Math.abs(running.lon - lon) < 1e-6) { ... return running.promise; }` (:11919–:11928). A SCOPE change is at the SAME lat/lon by construction — `setContextScope` calls `this.loadContextBuildings(at.lat, at.lon, true)` with the site origin — so a second release while the first load is running returns the first load's promise and the new scope is never fetched for buildings. The comment even records that `force` cannot rescue it: 'force is deliberately moot on a join'. Meanwhile roads, rail, water, parks, landuse and trees do NOT coalesce — they `abort()` and restart with the new scope. The result of two drags inside the ~45 s window is a plate whose ground layers are at the new radius and whose BUILDINGS are at the old one, with nothing in the log saying so. §STARTUP-CTX-COALESCE was written for the startup path, where three callers really did ask for the identical thing; the scope slider added a caller that asks for the same place at a different SIZE and the key never learned about it.",
      "blocksInteraction": false,
      "costMs": "Unbounded staleness — the buildings stay at the superseded scope until some other trigger reloads them.",
      "costBasis": "read-from-code",
      "fix": "Put the scope in the coalescer key. `contextLoadInFlight` becomes `{ lat, lon, scopeKey, promise }` and the join test adds `running.scopeKey === scopeKey`, where `scopeKey` is the same shape+size string `scopeClipperFor` already builds (:2377). A differing scope then takes the abort-and-restart branch every other layer already takes. ⚠ This must land WITH the scope-as-parameter change above, or the restarted load will still read the mutable field.",
      "risk": "low",
      "confidence": "high",
      "file": "apps/editor/src/ui/geospatial/CesiumViewport.ts",
      "line": 11921,
      "quote": "      running &&"
    },
    {
      "title": "The decoded-tile cache is smaller than one r=5755 load — the read evicts its own tiles while it is still running",
      "detail": "`export const CTX_MAX_CACHED_TILES = 768;` and its justification is measured at the DEFAULT scope: 'At 81 tiles per buildings read ... ONE site now needs ~330 distinct tiles across all ten layers, so 512 leaves less than one further site before eviction ... 768 restores the two-site headroom.' At r=5755 his own log reads 182 tiles each for roads, buildings, parks, water and rail plus 156 for trees — **1,066 distinct tiles for a single load**, 1.39× the cache. Eviction is oldest-first by insertion (`contextTiles.ts:1320`), so by the time the trees read finishes, the buildings tiles it started with are gone. Consequences: dragging the slider back down, or up again, re-downloads essentially everything; and the cheapest possible implementation of the founder's design — 'the new scope mostly overlaps the old one, so read only the delta' — is defeated before it starts.",
      "blocksInteraction": false,
      "costMs": "Turns every repeat scope change at large radius into a full cold read (~43 s), instead of the '3–4 ms repeat read' the module's own header claims for warm tiles.",
      "costBasis": "read-from-code",
      "fix": "Size the bound from the SLIDER MAX, not from the default scope — the same class of derivation `resolveFarRingCap` already uses ('never read less than the one primitive can draw'). At r=7071 the fan-out caps are 225 (area) × 5 area layers + 576 (points) ⇒ derive the ceiling from those constants rather than restating a number that rots. ⚠ It must stay BOUNDED (§L-273) and the entries are decoded feature arrays, so measure the memory before raising it — that is the axis the current number is actually protecting and nobody has re-measured it at 1,066 tiles.",
      "risk": "medium",
      "confidence": "high",
      "file": "apps/editor/src/ui/geospatial/contextExtentBudget.ts",
      "line": 1085,
      "quote": "export const CTX_MAX_CACHED_TILES = 768;"
    },
    {
      "title": "Six concurrent layer reads each open their own 24-wide pool — ~144 range requests against one origin, against a measured healthy width of 36–42",
      "detail": "`CTX_TILE_READ_CONCURRENCY = 24` is applied PER READ: `mapWithConcurrency(tiles, CTX_TILE_READ_CONCURRENCY, ...)` at `contextTiles.ts:1801`, and `readContextTileFeatures` is called once per layer. `setContextScope` fires eight loaders in one synchronous burst, so six or more of those pools are open at once — up to ~144 simultaneous same-origin range requests. The constant's own justification measured the healthy width as '36–42 concurrent ranges completing in ~800 ms direct / ~955 ms proxied, i.e. ~24 ms of wall clock per tile'. His r=5755 run achieves 1,066 tiles in 43,280 ms ≈ **41 ms/tile aggregate** — 1.7× worse than the measured best, despite 3.5× more requests in flight. The file already names the mechanism: 'caller 1's 5672 ms for 81 tiles ... is same-origin QUEUEING behind Cesium's terrain stream and the other layers, not decode time. That is the number worth attacking.'",
      "blocksInteraction": false,
      "costMs": "~43,280 ms observed for the longest layer vs ~25,600 ms if 1,066 tiles ran at the measured 24 ms/tile — so roughly 17 s attributable to contention, by arithmetic on his log.",
      "costBasis": "estimated",
      "fix": "Make the pool GLOBAL rather than per-read: one module-level semaphore of ~32 shared by every layer, so the six reads interleave inside one measured-good width instead of stacking six pools. `mapWithConcurrency` already preserves input order, so the change is to the ticket source, not the algorithm. ⚠ Raising the width is the wrong direction — the measurement says we are already past the knee. ⚠ Cesium's own terrain stream shares the origin and is NOT in this pool; that interaction is unmeasured and should be said so rather than assumed away.",
      "risk": "medium",
      "confidence": "medium",
      "file": "apps/editor/src/ui/geospatial/contextTiles.ts",
      "line": 652,
      "quote": "export const CTX_TILE_READ_CONCURRENCY = 24;"
    },
    {
      "title": "⛔ THE BRIEF'S STRATEGIC PREMISE IS FALSE: the r=5755 plate is NOT thinner near the site than the r=1781 plate",
      "detail": "Every binding cap at his scope is spent core-first or nearest-first, so the near field is protected by construction. BUILDINGS: `plateFill.ts` `PLATE_FILL_DEFAULT_TUNING = { coreShareOfCap: 0.5, ... }` — 'KEEP A CORE AT FULL DENSITY, THEN SPEND WHAT IS LEFT AT A UNIFORM RATE'. At r=5755 the far-tier cap is `farTierMaxInstances` = min(24 000, 8 000 × (5755/1781)²) = **24 000** (ceiling), so the core is 12 000 footprints at FULL density; at his own reported 616/km² that disc has radius √(12000/616/π) ≈ **2 491 m** — wider than the entire r=1781 plate, inside which the far cap (8 000 against ~6 100 candidates) never even binds. TREES: nearest-first (`contextTrees.ts:11`, 'nearest-first count cap'), and his panel prints the resulting radius itself — 'complete at a scope of ~3704 m'. DRAPE: `groundFeatureSeat.ts:183` — 'spend one layer-wide budget NEAREST FIRST ... the budget has to be spent where the founder is looking', so the 7,575 seat-point-only roads are the far ones. STREET LIFE: his log says 'dropped by the nearest-first cap' in so many words. And the two expensive building tiers do not move at all: `shadowRadiusM` = min(scope, 600) and `nearSolidRadiusM` = min(scope, 891) are fixed ceilings. ⚠ The 2 491 m figure is MY arithmetic on his log's density plus the code's constants — not a logged number.",
      "blocksInteraction": false,
      "costMs": "n/a — this corrects a premise, it does not cost time.",
      "costBasis": "estimated",
      "fix": "Do NOT change what the slider does on the strength of 'a bigger scope makes it worse'. It does not. The true statement — and it is the one worth putting in the caption — is: 'past ~2.5 km the plate is EVENLY thinner (23 % of what is there), and past ~3.7 km there are no trees. Nothing inside 2.5 km is lost.' `plateFill.ts` already computes `coreRadiusM` and returns it in its report (:126), and `CesiumViewport` already stores that report as `contextFarTierPlateFill` (:2263) — the number is measured, on hand, and simply not surfaced to the panel. Surface it. ⚠ Verify my arithmetic against one real console line at r=5755 before quoting 2 491 m to the founder.",
      "risk": "low",
      "confidence": "medium",
      "file": "apps/editor/src/ui/geospatial/plateFill.ts",
      "line": 100,
      "quote": "    coreShareOfCap: 0.5,"
    },
    {
      "title": "`warmAllContextLayers` is the ready-made seam for the founder's design — it already does exactly this, and it is never called on a scope change",
      "detail": "`warmAllContextLayers(lat, lon)` (`contextLayerWarm.ts:46`) fires all nine non-building layer reads in parallel, off any viewport, fire-and-forget, landing in the per-tile cache the render path later reads — its header: 'each warm read populates the very tile-cache keys the render path reads.' It is called from exactly one place: `OnboardingStepController.ts:850`, at geocode time. It takes NO scope and its own comment records the gap: 'the warm runs before any viewport exists, so it uses the DEFAULT scope. A user with a wider persisted scope still reads wider at render time and warms only that subset — fewer tiles warmed, never wrong ones, because a subset of a per-tile cache is always valid.' That last clause is the licence: warming a WIDER box is equally safe, for the same reason. Nothing calls it on `site.scope-changed`, and nothing calls it from the slider's preview.",
      "blocksInteraction": false,
      "costMs": "Could move most of the ~43 s read off the post-release critical path and into the seconds the user spends dragging.",
      "costBasis": "read-from-code",
      "fix": "Give it a scope parameter and call it from the slider's PREVIEW path (`previewSiteScope`, debounced ~300 ms) — the tiles for the candidate radius start downloading while he is still deciding, the old plate stays live and interactive, and the commit then finds a warm cache. This is the founder's architecture implemented on an existing, never-load-bearing function, with no change to the render path at all. ⚠ Debounce it hard and cancel on further movement, or a slow drag issues one full 1,066-tile read per pixel. ⚠ Land the cache-size finding first — warming into a cache that evicts itself buys nothing.",
      "risk": "low",
      "confidence": "high",
      "file": "apps/editor/src/ui/geospatial/contextLayerWarm.ts",
      "line": 46,
      "quote": "export function warmAllContextLayers(lat: number, lon: number): void {"
    },
    {
      "title": "THE UI ASK IS ALREADY BUILT — and it is defeated by its own pin rule at exactly the founder's scope",
      "detail": "§SCOPE-PANEL-50 (L-13189) already shipped the collapsed panel he is asking for: a 300 px box, a `ⓘ` disclosure button, and a caption that `display: 'none'` by default. But `paintCaption(text, pinned)` computes `const open = pinned || captionOpen;` and disables the toggle when `pinned`. `captionNeedsAttention` returns `true` whenever `verdicts.some((v) => !v.complete)`. At r=5755 BOTH measured layers report incomplete — his own panel text says 'trees: 10000 of 24138 ... 14138 dropped by the cap' and 'buildings: 27694 of 64044 ... 36350 dropped' — so `pinned` is `true`, the caption is force-open and the `ⓘ` is greyed out. The fold works only at scopes where the caps do not bite, i.e. exactly the scopes at which he would not have asked for it. The design rationale is sound ('progressive disclosure applied to an honesty statement is how a product quietly stops disclosing') — it is the granularity that is wrong: it pins the WHOLE paragraph when one clause carries the news.",
      "blocksInteraction": false,
      "costMs": "n/a — panel footprint only.",
      "costBasis": "read-from-code",
      "fix": "Split the caption in two rather than choosing between pinned and folded. Keep ONE always-visible line of ≤ ~60 chars carrying the fact ('⚠ 2 layers thinned past ~2.5 km'), derived from the same verdicts, and fold the full numbered paragraph behind the `ⓘ` unconditionally — `captionOpen` alone, no pin. The honesty invariant is preserved (the limit never disappears) and the panel loses its paragraph. ⛔ `siteScopeSlider.spec.ts` asserts a BICONDITIONAL between `captionNeedsAttention` and `completenessCaption`'s first words; that arm must be re-expressed against the new summary line, not deleted — it exists to stop the two routines drifting.",
      "risk": "low",
      "confidence": "high",
      "file": "apps/editor/src/engine/views/SiteScopeSlider.ts",
      "line": 289,
      "quote": "    if (verdicts.some((v) => !v.complete)) return true;"
    },
    {
      "title": "A full 4×4 matrix inverse is computed PER VERTEX in the near-building loop — every sibling loader hoists it",
      "detail": "Inside the near shadow-caster placement loop the ENU inverse is recomputed and freshly allocated for each ring vertex: `const positions = ring.map(([flon, flat]) => { ... Cesium.Matrix4.inverse(enu, new Cesium.Matrix4()), ... })` at :12330. I checked the caller count and the siblings rather than assuming: `grep -n 'Matrix4.inverse'` returns nine sites, and eight of them hoist it above the loop as `const invEnu = ...` (:13272 far ring, :13859 roads, :13991 rail, :14140 water, :14428 sea, :14569 parks, :14692 landuse, :16506). :12330 is the only one inside a per-vertex callback. ⚠ It does NOT scale with scope — the tier is bounded by `CTX_NEAR_MAX_SHADOW_CASTERS = 1600` and `shadowRadiusM` = min(scope, 600), both fixed — so this is a constant cost on every load, not a scope problem. At ~1,600 footprints × ~10–20 vertices it is ~16k–32k general 4×4 inversions plus the same number of `Matrix4` allocations.",
      "blocksInteraction": true,
      "costMs": "UNKNOWN in absolute terms — no instrumentation. It is a strict removal of ~16k–32k inversions + allocations from a synchronous span, on every context load including startup.",
      "costBasis": "read-from-code",
      "fix": "Hoist it: `const invEnu = Cesium.Matrix4.inverse(enu, new Cesium.Matrix4());` above the feature loop, exactly as the eight siblings do, and reuse it. Two lines, no behaviour change, identical arithmetic. Worth taking with the placement-cost instrumentation so the before/after is visible.",
      "risk": "low",
      "confidence": "high",
      "file": "apps/editor/src/ui/geospatial/CesiumViewport.ts",
      "line": 12330,
      "quote": "            Cesium.Matrix4.inverse(enu, new Cesium.Matrix4()),"
    },
    {
      "title": "Interaction with today's L-13263: the founder's 14 s FIFO waits are already partly fixed at HEAD, so do not re-price them",
      "detail": "His log's `14,292 ms waiting for terrain (shared FIFO), 134 ms own work` predates commit `bf96a58c`. At HEAD the batcher is constructed with `tileDedupGuaranteed: () => this.terrainTileMemoEntry !== null` (:10591), and `runFlush` skips the mutex entirely when that reads true (`groundSampleBatcher.ts:406`, `if (dedupBelow) { this._flightsRunConcurrently++; } else { ... await this.awaitTurn(ahead); }`). So roads/parks/landuse should no longer serialise behind one another for terrain. ⚠ WHAT THIS DOES NOT REMOVE: the round-trips themselves still happen — his session reads '26 round-trip(s) for 57 caller(s) asking 265,817 points; single trips of 29,098 points (7,792 ms)'. Concurrency removes the QUEUING, not the download. And the flights now compete for the same origin connection as the six tile pools (previous finding), so some of the 14 s may reappear as tile-read contention rather than as FIFO wait.",
      "blocksInteraction": false,
      "costMs": "Up to ~14 s per drape layer of queuing, expected to be largely gone at HEAD — but the net effect at r=5755 is UNMEASURED.",
      "costBasis": "read-from-code",
      "fix": "Re-capture one r=5755 console at HEAD before pricing anything in this lane off his 2026-09-08 log. The `§DRAPE-COST-ATTRIBUTION` and `§STARTUP-GROUND-SAMPLE-COALESCE` lines both print `flightsRunConcurrently` and `maxConcurrentFlights`, so the new regime identifies itself. ⛔ Do not quote his 14,292 ms as a current cost — this repo's own scar tissue (`gate-blind-on-the-wrong-axis`, the CLAUDE.md P4 oscillation) is exactly about transcribed readings that have since flipped.",
      "risk": "none",
      "confidence": "high",
      "file": "apps/editor/src/ui/geospatial/CesiumViewport.ts",
      "line": 10591,
      "quote": "      tileDedupGuaranteed: () => this.terrainTileMemoEntry !== null,"
    }
  ],
  "whatIcouldNotEstablish": "⛔ THE NUMBER THE WHOLE LANE TURNS ON IS UNMEASURED: how many milliseconds the longest SYNCHRONOUS placement span actually costs. There is no `performance.now()` around any entity/instance placement loop in `CesiumViewport.ts` — the only two timing sites in the file's context path are inside `resolveGroundDrapePieces` (:14862/:14880) and they time the drape, not the placement. Everything I can prove is structural: the loops are unyielded single tasks, roads is ~10,138 iterations at r=5755, and roads/rail/parks/water/landuse have no count cap. Whether that is 80 ms or 4,000 ms per burst I cannot say, and I will not guess. Ship the probe before the fix (`context-data-honesty-family`).\n\n⛔ I could not establish whether Cesium's own `GeometryVisualizer` pushes entity-driven `corridor`/`polygon` geometry creation into its worker pool. The primitives this code builds itself all pass `asynchronous: true` explicitly (:13554, :13566, :15141, `contextStreetLifeRender.ts:381/:441`), so the far tier, the tree canopies and the street life are off-thread by construction. The ~12,000 ENTITY-driven features are the open question, and answering it from memory of Cesium internals is precisely the kind of assertion this brief forbids. It needs one Performance-panel capture.\n\n⚠ MY 2,491 m CORE-RADIUS FIGURE IS ARITHMETIC, NOT A LOG LINE. It combines `coreShareOfCap: 0.5`, `farTierMaxInstances(5755) = 24 000` and the founder's own reported 616/km². `plateFill.ts` computes and returns the real `coreRadiusM` (:126) and `CesiumViewport` holds it as `contextFarTierPlateFill` — it just never reaches the panel. One console line at r=5755 confirms or refutes my number; get it before quoting it to him.\n\n⚠ I DID NOT VERIFY THE ~45 s END-TO-END WALL CLOCK. His log gives per-layer read durations (max 43,280 ms) and per-layer drape waits, but no single timestamped start-to-last-render figure, and the layers overlap. The serial sum of the six reads is 123,525 ms and the observed experience is ~45 s, which is consistent with concurrency plus contention — but \"45 s\" is his description, not a measured span.\n\n⚠ THE TILE-CONTENTION ATTRIBUTION (~17 s) IS AN INFERENCE from two in-repo measurements at a different width (36–42 concurrent) applied to a 1,066-tile read. It is the right order of magnitude and the file itself names the mechanism, but it is not a captured before/after of a shared pool.\n\n⚠ I DID NOT EXAMINE the coarser-zoom read idea to a conclusion. `zoomForExtent` (`contextTiles.ts:542`) already steps the zoom down until the tile count fits `scopeReadFanOutCap` (225 for area layers), and `SiteScopeSlider.ts:200` records a measurement that polygon layers are feature-identical from z16 down to z13 (Barcelona 953 vs 953, Madrid 1,305 vs 1,305). If that holds, lowering the area cap at large scope would cut the read several-fold for the same features. But the same block notes the largest z15 buildings tile is 13 % of tippecanoe's ~500 KB limit, which by area suggests z13 would trip `--drop-densest-as-needed` — so the safe floor is probably z14, and I have not verified the archives' actual minzoom. This is the largest untaken lever and it needs its own measurement lane.\n\n⚠ TWO ADJACENT LANES OWN GROUND I DELIBERATELY DID NOT RE-DERIVE: new-project startup and location→split latency. Several findings here (the tile cache bound, the shared connection pool, `warmAllContextLayers`) touch their surface — coordinate before changing `contextTiles.ts` or `contextLayerWarm.ts`."
}