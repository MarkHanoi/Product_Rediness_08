{
  "dimension": "Work backwards: what must be true for 10x, and is 10x real? (target definition, split design, what must not be deferred, correctness bugs, ranked ledger)",
  "summary": "**10x IS REAL ON ONE NUMBER AND NOT ON THE OTHER, AND THE FOUNDER'S 11,495 ms IS NEITHER OF THEM.**\n\nHis §STARTUP-BUDGET's terminal mark `reveal:content-ready` fires when the BUILDINGS warm promise settles (`OnboardingStepController.ts:1145` awaits `this.contextWarm`, set at `:851` to `fetchContextBuildingsNearAndFar`). It is not \"the screen is finished\". The two numbers that exist are:\n\n  (i) SPLIT USABLE — Enter → `reveal:split-mounted` = t+107,079 → t+113,012 = **5,933 ms**. It really is usable there: the left pane fetches the cadastral parcel ON THE CLICK (`SiteBoundaryMap2D.ts:2737 void parcelProvider.fetchParcelAtPoint(lng, lat)`), nothing pre-loads parcels, and the MapLibre basemap is a different origin that does not contend. So the honest first number is 5.9 s, not 11.5 s.\n\n  (ii) CONTEXT DRAWN — roads `warmed in 15,757 ms` from `context-warm:start` (t+111,359) ⇒ **t+127,116 = 20,037 ms after Enter**, and the parks drape (`6,908 ms read + 8,037 ms terrain`) lands around the same time. The budget under-reports what he is staring at by ~8.5 s.\n\n**VERDICT.** (i) → **400–700 ms typical / ~4.6 s on a genuinely cold geocode**: that is 8–14x, and it is achievable from three changes totalling ~5,550 ms, none of which is a rearchitecture. (ii) → **6–9 s**: that is 2.5–3.5x, and 10x on (ii) is NOT achievable without changing the baked artefact (one archive per layer ⇒ seven independent 81-tile fan-outs over the same ground). **Promise 10x on \"usable\", promise 3x on \"drawn\", and say so.**\n\n**THE ROOT OF (ii) IS NOT THE TERRAIN FIFO AND NOT DUPLICATED TILE READS.** Two tempting fixes are dead:\n · The 81 tiles are NOT re-read seven times. `<layer>.pmtiles` is one archive per layer (`contextTiles.ts:88`), so seven layers × 81 tiles is 567 DIFFERENT byte ranges. Same coordinates, different bytes.\n · `§DRAPE-COST-ATTRIBUTION: 8,037 ms waiting for terrain (shared FIFO)` — **\"(shared FIFO)\" is a hard-coded string literal** at `CesiumViewport.ts:10955`; it prints whether or not the FIFO was taken. L-13263 landed 2026-09-08 21:25 and its own regime counter (`flightsRunConcurrently`) is **never printed** by the summary at `:10617–10650`. The log cannot tell you which regime ran, and the arithmetic says it ran CONCURRENTLY (six 6.1 s round trips serialised would be 36 s, not the ~8–15 s observed). The 8 s is the terrain TILE DOWNLOAD, not queuing.\n\n**WHAT IT ACTUALLY IS:** every context tile AND every terrain tile is proxied through the app's own Express origin — `contextTiles.ts:347 CONTEXT_TILES_SAME_ORIGIN_BASE = '/api/context-tiles/'`, with `:330` stating production is deployed on it, and `server/context-delivery/index.js:102` routing terrain there too — into a **1 shared vCPU / 512 MB Fly machine** (`fly.toml:225-228`), which fully buffers each range (`contextTilesProxy.js:165/231/298 const buf = Buffer.from(await upstream.arrayBuffer())`). Ten layers × `CTX_TILE_READ_CONCURRENCY = 24` plus the terrain fan-out ⇒ ~240 concurrent buffered proxy legs on one event loop. The same file's own isolated measurement is **798 ms direct R2 / 955 ms proxied** for a 36–42-range fan-out. The founder's `[ProjectHub] runtime.persistence.client.delete failed: timeout (HTTP 0)` is the app's OWN API starving in that window — that is the proxy saturating, printed.",
  "findings": [
    {
      "title": "The reveal gate spends a full 1,500 ms waiting on a promise that cannot resolve — and it is the ONLY thing between the camera arriving and the split mounting",
      "detail": "`flight:parcel-arrival` t+111,368 → `reveal:split-mounted` t+113,012 = 1,644 ms, of which 1,500 is the deadline. The gate is `Promise.race([Promise.all([awaitContentReady, awaitFlightComplete]), setTimeout(1500)])`. `awaitContentReady` (OnboardingStepController.ts:1145) resolves `this.contextWarm`, which is `fetchContextBuildingsNearAndFar` (:851) — measured at 7,087 ms on this run (context-warm:start t+111,359 → context-warm:done t+118,446). It is arithmetically incapable of beating 1,500 ms on a cold load. The founder's own console line proves the timeout fired verbatim: \"§STARTUP-REVEAL-NOT-GATED-ON-CONTEXT: the context read had not landed within 1500 ms\". ⭐ THE OTHER GATE IS NOT THE HOLDER, and this matters because it makes the fix a real 1,450 ms rather than zero: the code's own trace, quoted at OnboardingStepController.ts:1107, reads `reveal:flight-settled +7ms` — the hero's 1.6 s site-entry tween is superseded within microseconds by frameSiteLocation's direct setView (§STARTUP-ARRIVES-DIRECTLY, CesiumViewport.ts:4938 `viewer.camera.setView({ destination, orientation })`) and settles on cancel. So content is the sole holder. ⛔ THE COMMENT AT siteRevealSequence.ts:69 IS WRONG IN THE CASE THAT MATTERS: \"IT IS A CEILING, NOT A DELAY. A gate that resolves at 4 ms mounts at 4 ms.\" True warm; on the cold first load the founder is measuring it is a flat 1,500 ms delay, every time, with a 100 % hit rate.",
      "blocksReveal": true,
      "costMs": "1,500 ms — MEASURED (founder log: parcel-arrival→split-mounted 1,644 ms; the gate's own expiry line printed)",
      "costBasis": "founder-log",
      "quote": "export const REVEAL_GATE_DEADLINE_MS = 1_500;",
      "file": "apps/editor/src/ui/onboarding/siteRevealSequence.ts",
      "line": 66,
      "fix": "Do not gate the reveal on `contextWarm` at all — gate it on what the split actually needs, which the sequence already enforces as steps 1–3 (seed frame, anchor location, arm listener) and which cost single-digit ms. Concretely: drop `awaitContentReady` from the production deps in `revealSplitAtParcel` and keep `awaitFlightComplete` with the existing deadline as the bound. ⚠ Do NOT just lower REVEAL_GATE_DEADLINE_MS to 0 — that silently deletes the warm-cache case the gate was built for (a revisited site resolves in single-digit ms and mounts fully populated). The right shape is: race the content gate against a SHORT deadline (~120 ms) so a warm hit still wins, and mount otherwise. Keep the `gateDeadlineExpired` console line — it is the only reason this was diagnosable.",
      "risk": "low",
      "confidence": "high"
    },
    {
      "title": "The geocode is a raw public-Nominatim round trip on the critical path, fired only on Enter, with no memo and no prefetch — while the user types for seconds",
      "detail": "`GlobeHeroSearch.search()` (:255 `results = await this.opts.geocode(q)`) is reached only from the form's `submit` handler (OnboardingStepController.ts:964 `form.addEventListener('submit', onSubmit)`). `geocodeAddress` has no cache: every call is a fresh `fetch` to `https://nominatim.openstreetmap.org/search`. Measured 4,277 ms for \"bARCELONA\" — 72 % of the 5,933 ms to a usable split. The module's own header already flags the exit: \"heavy / bulk use must self-host or use a commercial provider — the endpoint is `GEOCODE_ENDPOINT`, a single const a future env var can override\". ⭐ THE FREE TIME IS ALREADY IN HIS LOG: `geocode:start +97847ms` — he sat in that input for 97.8 s. The app used none of it.",
      "blocksReveal": true,
      "costMs": "4,277 ms — MEASURED (founder log: geocode:start t+107,079 → geocode:end t+111,357)",
      "costBasis": "founder-log",
      "quote": "'https://nominatim.openstreetmap.org/search';",
      "file": "apps/editor/src/ui/site/geocodeAddress.ts",
      "line": 42,
      "fix": "Two layers, both small. (1) MEMOISE `geocodeAddress` by normalised query for the session — the submit then re-reads a resolved answer instead of re-asking. (2) Fire ONE prefetch when the input has been idle ≥800 ms with ≥3 chars, cancel-on-edit, and only USE it when the submitted string is byte-identical to the prefetched one (never a near-match — that is how you fly to the wrong city). ⚠ Nominatim's usage policy forbids autocomplete; one request per SETTLED query is strictly fewer requests than per-keystroke and no more than the submit it replaces, but the unambiguous version is a same-origin `/api/geocode` route with a server-side cache — which also lets the provider be swapped without a CSP change and takes the third-party origin out of `connect-src`. ⛔ Do NOT resolve \"Barcelona\" from `siteEntryCoverageEntries()`'s metropolitan `extent` centroid as a fast path: a centroid 1–3 km off the real centre warms the wrong tiles and anchors the wrong site.",
      "risk": "low",
      "confidence": "high"
    },
    {
      "title": "Every context tile AND every terrain tile is proxied through a 1-vCPU / 512 MB Node process that buffers each range whole — this is the root of the 15 s reads, the 8 s drape wait, and the app's own API timing out",
      "detail": "`contextTilesBaseUrl()` resolves to `/api/context-tiles/` in production — stated twice in-repo and independently: contextTiles.ts:330 (\"`VITE_CONTEXT_TILES_URL` is now deployed as **`/api/context-tiles/`**\") and .github/workflows/terrain-bake.yml:268 (\"`vars.VITE_CONTEXT_TILES_URL`, which in this repo is the SAME-ORIGIN PROXY PATH\"). Terrain goes the same way (server/context-delivery/index.js:102 routes `/api/context-tiles/terrain/*`). The machine is `shared-cpu-1x`, `cpus = 1`, `memory_mb = 512` (fly.toml:225-228), and both handlers do `const buf = Buffer.from(await upstream.arrayBuffer()); return res.end(buf)` — no streaming, whole span in heap, spans up to `RANGE_COALESCE_MAX_SPAN_BYTES = 4 MB`. Ten layers each fanning out at `CTX_TILE_READ_CONCURRENCY = 24` plus the terrain fan-out puts ~240 buffered proxy legs on one event loop. ⭐ THE CONTRADICTION IS INSIDE THIS FILE: contextTiles.ts:864-871 measures the SAME shape at \"all range requests, DIRECT R2, issued concurrently 798 ms / through our same-origin proxy 955 ms\" for 36–42 ranges — in ISOLATION. The founder's roads read of 81 tiles took 15,314 ms, ~16x that, with nine other layers and the terrain sampler on the same origin. And his `[ProjectHub] runtime.persistence.client.delete failed: timeout (HTTP 0)` is the app's own API starving in exactly that window. That is not an unrelated request contending; it is the diagnostic. contextTiles.ts:317 already names the fix and the exit criterion: \"the direct route is strictly faster and keeps our server off the context hot path, which is the whole point of L-513b.\"",
      "blocksReveal": false,
      "costMs": "≈11,000–17,000 ms of the ~20 s to context-drawn — ESTIMATED from the in-file measured proxy/direct baseline (798 vs 955 ms for 36–42 ranges) against the founder-logged 15,314 ms for 81. The founder log MEASURES the 15,314 ms; the attribution to the proxy is inferred, not measured.",
      "costBasis": "read-from-code",
      "quote": "export const CONTEXT_TILES_SAME_ORIGIN_BASE = '/api/context-tiles/';",
      "file": "apps/editor/src/ui/geospatial/contextTiles.ts",
      "line": 347,
      "fix": "Take the BFF off the tile hot path. The code change is ZERO — the exit criterion is already written at contextTiles.ts:344: add `app.pryzm.so` to the R2 bucket's AllowedOrigins with `range` in AllowedHeaders and `content-range`/`accept-ranges` in ExposeHeaders (prefer a custom domain over the r2.dev endpoint, which Cloudflare rate-limits and does not intend for production), then set the repo var `VITE_CONTEXT_TILES_URL` to that base and redeploy. ⛔ PROBE BEFORE YOU CLAIM: the attribution above is inferred. The decisive measurement is one page load with the var flipped, comparing the seven per-layer `from 81 baked tiles in N ms` lines — that is a free, already-instrumented A/B. ⚠ If R2 CORS cannot be changed today, the interim is to make the proxy STREAM (`Readable.fromWeb(upstream.body).pipe(res)`) instead of buffering, and to lower the per-layer read concurrency when more than two layer reads are live — but that is treating the symptom on a box that will still be one shared vCPU.",
      "risk": "medium",
      "confidence": "medium"
    },
    {
      "title": "L-13263's own regime counter is authored but never printed, and the line that IS printed asserts the invariant L-13263 retired",
      "detail": "`GroundSampleBatcher` exposes `flightsRunConcurrently` — added by L-13263 explicitly \"so a run says WHICH regime it ran in\" (groundSampleBatcher.ts:172). The `onFlush` console template at CesiumViewport.ts:10617-10650 prints roundTrips, requests, pointsRequested, pointsSampled, pointsFromCache, pointsJoinedInFlight, pointsResolvedByAnEarlierFlight, deferredFlushes and maxConcurrentFlights — and NOT `flightsRunConcurrently`. It then hard-codes \"(MUST be 1 — two calls in the air re-download the same tiles)\", the exact justification L-13263 retired and which CesiumViewport.ts:10583 says out loud \"IS NO LONGER TRUE\". Separately, `§DRAPE-COST-ATTRIBUTION` at :10955 prints the literal \"(shared FIFO)\" unconditionally, whether the flush queued or skipped the FIFO. ⭐ RESULT: the brief's single most damning line — \"8,037 ms waiting for terrain (shared FIFO)\" — is a label, not a measurement, and it points a triage lane at a queue that (by the arithmetic: six 6.1 s round trips serialised would be ~36 s, against an observed ~8–15 s drape window) was NOT taken. This is the same defect class CLAUDE.md records for check-single-compose's hard-coded \"0 rivals\" (L-12830).",
      "blocksReveal": false,
      "costMs": "0 ms of user wait — but it is the reason a 6-hour triage would be spent on the wrong subsystem. MEASURED as zero (it is a log template).",
      "costBasis": "read-from-code",
      "quote": "`${s.maxConcurrentFlights} (MUST be 1 — two calls in the air re-download the same ` +",
      "file": "apps/editor/src/ui/geospatial/CesiumViewport.ts",
      "line": 10647,
      "fix": "Print `flightsRunConcurrently` in the onFlush line, delete the \"(MUST be 1 …)\" clause, and make `§DRAPE-COST-ATTRIBUTION`'s parenthetical conditional — \"(queued behind the FIFO)\" vs \"(tile-dedup concurrent)\" — read off the same counter. Zero behaviour change, and it is the prerequisite for ever quoting that number again.",
      "risk": "none",
      "confidence": "high"
    },
    {
      "title": "§STARTUP-BUDGET stops measuring 8.5 s before the founder stops waiting",
      "detail": "The mark vocabulary ends at `reveal:split-mounted` / `reveal:content-ready` (startupBudget.ts:25-26, :160-161). `reveal:content-ready` is emitted inside `awaitContentReady` (OnboardingStepController.ts:1147) the instant the BUILDINGS warm settles — t+118,574. But roads is still reading until t+127,116 (`warmed in 15,757 ms` from `context-warm:start` t+111,359), and the parks/landuse/rail drapes land after that. So the budget's last mark is 8.5+ s short of the screen the founder photographed, and every ratio computed from 11,495 ms is computed from the wrong denominator. ⚠ This is why the founder's brief itself reasonably reads the number as 11.5 s, and why an honest 3 s cannot be promised against a metric that does not measure the thing.",
      "blocksReveal": false,
      "costMs": "0 ms of user wait; ~8,500 ms of UNMEASURED wait. MEASURED from the founder log's own per-layer warm lines against its last mark.",
      "costBasis": "founder-log",
      "quote": "// reveal:content-ready → reveal:flight-settled → reveal:split-mounted → parcel:selected →",
      "file": "apps/editor/src/engine/startupBudget.ts",
      "line": 161,
      "fix": "Add two marks and nothing else: `context:all-layers-read` (when the last of the ten `warmAllContextLayers` promises settles — contextLayerWarm.ts already has the `for (const [name, p] of layers)` loop and the t0) and `context:drape-settled` (when the last `§DRAPE-COST-ATTRIBUTION` summary is emitted). Passive, one `performance.now()` each, and they make the (ii) number in this report checkable instead of reconstructed.",
      "risk": "none",
      "confidence": "high"
    },
    {
      "title": "The tileset manifest is a 428 ms serial prefix on six of the seven render layers, paid at geocode time when it could have been paid at app boot",
      "detail": "`primeContextTilesetManifest()` is the first line of `warmAllContextLayers` (contextLayerWarm.ts:58), i.e. it starts the moment the geocode resolves. Every layer except `buildings` then WAITS on it: contextTiles.ts:1679 `if (layer !== MANIFEST_UNGATED_LAYER)` gates each read behind the manifest for up to `CONTEXT_MANIFEST_GATE_MS = 700`. The founder's log measures the manifest read at 428 ms, so roads/water/parks/landuse/rail/trees each start 428 ms late. ⛔ The gate itself is CORRECT and must stay — contextTiles.ts:1041 explains that an un-awaited prime \"lands after the 404s it was meant to prevent\". The defect is only WHEN the prime is issued. The manifest is a whole small JSON document that depends on nothing but `contextTilesBaseUrl()`; it does not need a lat/lon.",
      "blocksReveal": false,
      "costMs": "428 ms — MEASURED (founder log: §CTX-MANIFEST-KNOWN-MISSING manifest read in 428 ms, names 7 layers)",
      "costBasis": "founder-log",
      "quote": "export const CONTEXT_MANIFEST_GATE_MS = 700;",
      "file": "apps/editor/src/ui/geospatial/contextTiles.ts",
      "line": 1058,
      "fix": "Call `primeContextTilesetManifest()` once at editor boot (it is idempotent and returns the memoised promise; it takes no coordinates). By the time a geocode resolves it has long settled and the gate resolves at ~0. Keep the call in `warmAllContextLayers` as the belt-and-braces — it is a no-op on the second call.",
      "risk": "none",
      "confidence": "high"
    },
    {
      "title": "Buildings are placed at base 0 and then re-seated to the measured 63.4 m — a wrong-then-right flash that mounting the split EARLIER makes longer and more visible",
      "detail": "The terrain-settle path re-seats the already-placed context buildings in place (CesiumViewport.ts:9980 region, §CTX-BUILDINGS-INPLACE-RESEAT L-635: \"the base height changed, so re-seat the context buildings onto the new ground. This was a `loadContextBuildings(force)` RE-FETCH\"). The re-fetch is gone, so this is not a duplicated read — but the visible state IS real: footprints sit at the pre-measurement base until `formaTerrainBaseHeight` resolves, which today waits on the same saturated proxy (one terrain flight = 6,107 ms). ⛔ THIS IS THE ONE PLACE WHERE 'MOUNT EARLIER' COSTS HONESTY, and it is the explicit answer to (c): mounting the split at ~150 ms instead of ~1,650 ms widens the window in which the founder is shown buildings buried in, or floating over, the terrain. A split he can use is fine to show early; context seated on a base nobody has measured is not.",
      "blocksReveal": false,
      "costMs": "UNKNOWN as a wait — it is not a delay, it is a period of wrong pixels. Its LENGTH is governed by the terrain round trip: 6,107 ms MEASURED (founder log, one trip resolving 19,365 points).",
      "costBasis": "founder-log",
      "quote": "this.formaTerrainBaseHeight = sampledHeight;",
      "file": "apps/editor/src/ui/geospatial/CesiumViewport.ts",
      "line": 9967,
      "fix": "Hold the context-building PLACEMENT (not the read, not the decode) until `formaTerrainBaseSource` is a measured source rather than the unmeasured fallback — the provenance is already resolved and typed by `resolveTerrainClampBase` (CesiumViewport.ts:9877). The read and decode keep streaming underneath, so nothing is serialised; only the moment of first paint moves. ⚠ This is only affordable once the terrain download stops costing 6 s — i.e. it is a FOLLOW-ON to the proxy fix, not a companion. Shipping it first would trade a wrong-pixels flash for a 6 s empty 3D pane, which is worse.",
      "risk": "medium",
      "confidence": "medium"
    },
    {
      "title": "Seven layers means seven independent 81-tile fan-outs over the same ground — this is the only route to 10x on 'context drawn', and it is a BAKE change, not a client one",
      "detail": "`ContextTileLayer` is a union of ten names and the archive is `<layer>.pmtiles` (contextTiles.ts:88). So the founder's repeated \"from 81 baked tiles\" is NOT one read done seven times — it is 567 distinct byte ranges across seven archives, plus seven header+directory reads. The per-tile decode cache (contextTiles.ts:1881 `tileCache`) and the in-flight map already de-duplicate perfectly WITHIN a layer; there is nothing left to win there. ⭐ BUT THE READER ALREADY SELECTS BY LAYER OUT OF A DECODED TILE: contextTiles.ts:1903 `new VectorTile(new PbfReader(new Uint8Array(data))).layers[layer]`. An MVT tile can carry many named layers. A single merged archive would turn 567 downloads into 81 and seven decodes into one, with a near-zero client diff (point every layer at one archive URL). ⚠ THE RISK IS REAL AND MUST BE MEASURED, NOT ASSUMED: layers read at different zooms (buildings z16, landuse/water as low as z13 per §CTX-ZOOM-FITS-EXTENT), and merging seven layers into one tile can cross tippecanoe's ~500 KB tile limit and trigger `--drop-densest-as-needed` — which would silently DELETE features, the exact §CONTEXT-DATA-HONESTY failure this subsystem exists to end.",
      "blocksReveal": false,
      "costMs": "ESTIMATED 3,000–6,000 ms of the residual (ii) after the proxy fix. Not measured, and deliberately not claimed as more.",
      "costBasis": "estimated",
      "quote": "export type ContextTileLayer = 'buildings' | 'roads' | 'water' | 'parks' | 'landuse' | 'rail' | 'trees' | 'sea' | 'furniture' | 'canopy';",
      "file": "apps/editor/src/ui/geospatial/contextTiles.ts",
      "line": 88,
      "fix": "Bake experiment first, decision second: emit one multi-layer tileset for the AREA layers (roads, parks, landuse, rail, water) at their shared zoom band and measure feature counts per layer against today's per-layer archives at Barcelona and one dense core (Île de la Cité). Ship only if the counts are identical — a merged pack that drops 2 % of the roads to save 4 s is not a win. Keep `buildings` and `trees` separate: buildings needs its own higher fan-out cap and trees has a measured 60 %-per-zoom-step point loss (scopeReadCeiling.ts) that a shared zoom would inflict on the canopy. ⛔ Do this LAST — it is worth ~3–6 s and carries the only real correctness risk in this ledger; the proxy fix is worth 3x more for a config change.",
      "risk": "high",
      "confidence": "medium"
    },
    {
      "title": "The NaN site origin is COSMETIC as to milliseconds and REAL as to honesty — it is not a startup-speed bug",
      "detail": "`getCompleteScopeMark()` does `scopeReadCompleteCeilingM(at?.lat ?? Number.NaN, at?.lon ?? Number.NaN)` where `at = this.contextBuildingsAt ?? this.formaMassingOrigin` — both null before the first context load, so at slider-repaint time during startup it is genuinely NaN. `scopeReadCeiling.ts:141` early-returns on non-finite input, so the bisection never runs and the cost is one console line. ⛔ IT DRIVES NO READ: the read-extent caller is `treeReadCeilingM()` (CesiumViewport.ts:2639), which checks `Number.isFinite` and returns `null` = \"not measured\", never a fabricated ceiling. So it costs 0 ms. ⚠ WHAT IT DOES COST is the slider's own honesty: the mark falls back to the flat 1,781 m mid-latitude reference and the caption reads \"complete at this scope\" before anyone knows where the site is — which is the §CONTEXT-DATA-HONESTY failure that file was written to close, reappearing at a moment its author did not consider.",
      "blocksReveal": false,
      "costMs": "0 ms — READ from the code (non-finite branch returns before any arithmetic).",
      "costBasis": "read-from-code",
      "quote": "const ceiling = scopeReadCompleteCeilingM(at?.lat ?? Number.NaN, at?.lon ?? Number.NaN);",
      "file": "apps/editor/src/ui/geospatial/CesiumViewport.ts",
      "line": 2612,
      "fix": "Have `getCompleteScopeMark()` return `null` when no origin has loaded, and have the slider render the mark as absent rather than at the reference constant. `scopeReadCeiling.ts` already models this correctly with `neverCompleteHere`/`tighterThanReference` — the caller is the one turning \"unknown\" into \"1,781 m\". Not a perf item; do not fund it out of the startup budget.",
      "risk": "low",
      "confidence": "high"
    },
    {
      "title": "The sea mask is computed three times on three bboxes and refuses all three — real repeated work, but unquantified and not on any path he waits on",
      "detail": "`buildSeaMaskFromCoastline` is reached from at least two distinct sites over distinct extents (CesiumViewport.ts:14304 inside `loadContextWater`, and :14512 in the promoted-sea path), and `contextLayerWarm.ts:118-120` warms `water` at BOTH the default extent and `CONTEXT_SEA_HALF_DEG` — three keys, three collection-cache misses, three coastline walks. All three refuse with `incomplete-coastline` (contextWater.ts:613). ⚠ THE REFUSAL IS ITSELF A DEFECT AT BARCELONA — it is a coastal city and the sea is not drawn — but that is a correctness lane, not a startup one, and it is already a known open item. ⛔ I could not establish the ms: the walk cost is not instrumented and no founder-log line carries a duration for it.",
      "blocksReveal": false,
      "costMs": "UNKNOWN — no duration is instrumented on the coastline walk and no log line carries one.",
      "costBasis": "unknown",
      "quote": "reason: 'incomplete-coastline',",
      "file": "apps/editor/src/ui/geospatial/contextWater.ts",
      "line": 613,
      "fix": "Before touching it, instrument it: one `performance.now()` pair around `buildSeaMaskFromCoastline` and print the bbox alongside the refusal, so the three lines can be told apart and priced. If the walk turns out to be sub-100 ms, deduplicate nothing and spend the budget elsewhere. ⛔ Do not memoise the refusal across bboxes — a refusal at one extent is not a refusal at another, and collapsing them would be the failure-equals-empty conflation this whole subsystem exists to prevent.",
      "risk": "low",
      "confidence": "medium"
    },
    {
      "title": "The seconds he spends reading the map are still unused — prefetch the parcel he is about to click",
      "detail": "This is the positive half of (b). Once the split mounts, nothing in the left pane is doing work: `fetchParcelAtPoint` runs only from the map click handler. The founder then spends several seconds reading the map before clicking, and the first click pays a full Catastro round trip. The reveal has ALREADY seeded the exact frame centre (`pryzmSetGeocodeFrame` is precondition (a) of the sequence), so the app knows, at mount, the most likely first click to within a few hundred metres.",
      "blocksReveal": false,
      "costMs": "Saves an UNMEASURED Catastro round trip on the first click (typically several hundred ms to low seconds; not in this log, which never reaches parcel:selected).",
      "costBasis": "read-from-code",
      "quote": "void parcelProvider.fetchParcelAtPoint(lng, lat).then((parcel) => {",
      "file": "apps/editor/src/ui/geospatial/SiteBoundaryMap2D.ts",
      "line": 2737,
      "fix": "On split mount, fire one `fetchParcelAtPoint` at the seeded frame centre, fire-and-forget, and let the click read the memo. ⛔ IT MUST NOT SELECT ANYTHING — no highlight, no card, no `useSelectedParcel()`. Warming a cache is invisible; pre-selecting a parcel the user did not click is the product choosing his site for him, which is the exact shape §CREATE-IT-MYSELF-SURVIVES-A-REFUSAL (L-13280, shipped today) had to undo elsewhere.",
      "risk": "low",
      "confidence": "medium"
    }
  ],
  "whatIcouldNotEstablish": "**1. Whether the proxy is really the bottleneck — the single biggest claim in this report is INFERRED, not measured.** I established that all context and terrain tiles route through `/api/context-tiles/` on a 1-vCPU/512 MB machine that buffers every range whole, that ~240 legs are concurrent, that the same fan-out measured 798 ms direct / 955 ms proxied in isolation, and that the app's own API timed out in the window. I did NOT run a load. The A/B is free and already instrumented (flip `VITE_CONTEXT_TILES_URL`, compare the seven `from 81 baked tiles in N ms` lines) and must precede the claim.\n\n**2. Which build produced the founder's log.** L-13262/L-13263 landed 2026-09-08 21:11–21:25 and the log is 2026-09-09, but I could not confirm the deployed SHA. This matters less than it looks, because the log's \"(shared FIFO)\" is a string literal and the regime counter is unprinted — so the log could not tell you either way, which is itself finding #4.\n\n**3. The actual production value of `VITE_CONTEXT_TILES_URL`.** It is a GitHub repo variable I cannot read. My evidence is two independent in-repo statements (contextTiles.ts:330, terrain-bake.yml:268) that production is on the proxy. `tools/deploy/fly-bundle-proof.sh` already greps the live bundle for it — run that to settle it in one command before doing anything else.\n\n**4. The exact ms cost of `activation:site:*` beyond the reveal deadline.** `activation:site:start` t+112,936 → `descent-settled` t+113,012 is 75 ms, and split-mounted is +0 after that, so the residual reveal work is ~144 ms. But I did not trace what runs between the mount call and `activation:site:start` — the 1,500 ms accounts for the gap and the remainder is inside the noise, so I did not chase it. If the reveal-gate fix lands and split-mount does NOT drop to ~200 ms, that unexamined 144 ms is where to look.\n\n**5. The main-thread cost of placing 15,781 footprints + 12,904 trees.** Decode is cheap and measured (67 ms for 13,339 features, contextTiles.ts:866) and is NOT the 15 s. But turning those features into Cesium primitives is a separate, uninstrumented main-thread cost that runs while the founder is looking at the pane. If the proxy fix lands and (ii) does not fall as far as predicted, this is the next suspect — and it would be the first thing in this whole path that a worker could not trivially fix, because it ends in Cesium entities.\n\n**6. The drape's residual after the terrain is fast.** `parks 8,037 ms waiting / 23 ms own work` — I attribute nearly all of the 8,037 to the terrain tile download through the proxy, on the arithmetic that six serialised 6.1 s flights would have shown ~36 s. That leaves an unknown queuing residual I cannot separate without `flightsRunConcurrently` being printed."
}