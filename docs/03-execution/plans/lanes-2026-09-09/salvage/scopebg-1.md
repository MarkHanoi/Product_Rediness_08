{
  "dimension": "The UI ask — collapse the scope panel's honesty paragraph behind a disclosure that is hidden by default",
  "summary": "The disclosure the founder is asking for ALREADY EXISTS and shipped yesterday (afc5ce4c, 2026-09-08, §SCOPE-PANEL-50 / L-13189): `apps/editor/src/engine/views/SiteScopeSlider.ts` has a ⓘ button and a caption `<div>` whose declared `display` is `'none'`. He is not seeing it because at r=5755 m the caps bite, and `paintCaption` PINS the caption open and DISABLES the ⓘ on exactly that reading (SiteScopeSlider.ts:508-518, rule at :291-297). So the panel he screenshotted is ~126 px tall against a ~55 px folded chrome — the paragraph is ~56 % of the panel — and the control that would collapse it is greyed out by design. His ask is therefore a REVERSAL of one deliberate decision, not a missing feature: today a limit CANNOT be folded; he wants it folded. The reconciliation is the one the repo's own primitive already provides — `buildPanelFold()` in `apps/editor/src/ui/site/panelFold.ts` exposes `setSummaryNote()`, documented verbatim as \"the one fact that must survive collapsing (C58 §1.2)\". Move the cap verdict from the body into that always-visible summary note: \"⚠ 50 488 features inside the scope not drawn · complete to ~3 704 m\", with the full per-layer numbers behind the fold. The panel loses ~70 px, the honesty statement loses nothing, and no new mechanism is minted. Corrections to the brief: there is NO checkbox and NO radio in this panel — the two glyph buttons ▭ / ◯ (the shape toggle, SiteScopeSlider.ts:371-386) read as an unchecked checkbox and an unchecked radio at 12 px, which is how they were described; and the panel is bottom-CENTRE of its pane (`left:50%`, :344-346), which renders bottom-right on screen because the 3D Site defaults to the RIGHT pane (paneViewModel.ts:411).",
  "whatIcouldNotEstablish": "(1) THE WRAPPED LINE COUNT IS ESTIMATED, NOT MEASURED. Every box term (300 px width, 7/10/6 padding, 20 px head, 4 px head margin, 16 px track, 10px/1.35 caption) is read verbatim from the declarations at SiteScopeSlider.ts:344-467 — those are read-from-code. The number of LINES the founder's 253-character paragraph wraps to is an estimate at 0.52 em average advance over a 280 px content box (→ 5 lines, 67.5 px). I did not run a browser; happy-dom cannot lay out text, and the existing spec (`siteScopeSlider.spec.ts`, `readBox`) deliberately measures declarations rather than pixels for that reason. If the real advance is 0.45 em it is 4 lines (54 px); at 0.60 em it is 6 (81 px). The CONCLUSION (the caption is the majority of the panel's height) survives the whole range. (2) I could not establish whether the founder's screenshot ALSO had the read-ceiling sentence on screen. His quoted text is only the two cap lines, which means `mark.kind` was `'cap'` at that moment; if a site puts `kind === 'read'` past the mark, `completenessCaption` (:216-224) PREPENDS a 320-character sentence and the same panel reaches ~207 px. I have no log line for `mark.kind` at r=5755. (3) Whether the ⓘ glyph is DISCOVERABLE at all. It is 12 px, unlabelled except by `title`/`aria-label`, and I have no session recording or founder statement that he ever found it — his ask (\"MAKE THE PANEL … WITH THE TEXT ON A DROP DOWN\") reads as someone who has not, but that is inference, not evidence. (4) No e2e or browser-rendered height budget exists for this panel — `shellFloatBudget.spec.ts` governs only body-level `left: var(--shell-canvas-cx)` floats, and this is a pane float, deliberately outside it. So nothing today would catch the panel growing in a real browser. (5) Whether the founder wants the fold to survive a SCOPE CHANGE or only a repaint — I inferred \"survive\", since `paint()` rewrites the text on every store notification and a fold that snapped shut on each drag would be, in C115 §11's own words, \"an obstacle, not a dropdown\".",
  "findings": [
    {
      "title": "The drop-down already exists, is hidden by default, and is DISABLED in exactly the state the founder is in",
      "detail": "`apps/editor/src/engine/views/SiteScopeSlider.ts` is the scope panel and `mountSiteScopeSlider()` (:315) is the builder. It has exactly two production callers — `SiteAuthoringPaneShell.ts:643` and `:644`, one per pane — so there is no rival panel and no zero-caller twin. Its parts, in order: the shape toggle ▭/◯ (:371-386), the word 'Scope' (:396), the readout '◯ 5 755 m radius' (`describeScope`, :157 / :551), the ⓘ (:408-416), the range input (:427-438) with the COMPLETE tick (:442-450), and the caption div (:454-467). The caption is DECLARED FOLDED — `display: 'none'` at :463, comment at :461: 'Starts folded.' The fold shipped yesterday: afc5ce4c, 2026-09-08, '§SCOPE-PANEL-50 L-13189: the scope panel folds to half its footprint'. He is not seeing it because `paintCaption` (:508) computes `const open = pinned || captionOpen;` (:510) and sets `captionToggle.disabled = pinned;` (:514). `pinned` is `captionNeedsAttention(...)` (:291-297), whose second clause is `if (verdicts.some((v) => !v.complete)) return true;`. At r=5755 two verdicts are incomplete (his own paragraph proves it), so the caption is force-opened and the ⓘ is greyed out with the title 'This reading names a limit or an unmeasured layer, so it stays open.' ⭐ HIS ASK IS THEREFORE A REVERSAL OF A DELIBERATE RULE, NOT A MISSING FEATURE. Do not implement it as 'add a fold' — implement it as 'change what the fold is allowed to hide', or the next reader will re-pin it citing the same §CONTEXT-DATA-HONESTY comment block that is sitting on lines 383-399.",
      "blocksInteraction": false,
      "costMs": "0 ms runtime — the fold is layout only and touches no load path. Build: ~2 h.",
      "costBasis": "read-from-code",
      "quote": "captionToggle.disabled = pinned;",
      "file": "apps/editor/src/engine/views/SiteScopeSlider.ts",
      "line": 514,
      "fix": "Split `pinned` into two grades instead of one boolean. Grade 1 (BODY MAY FOLD, SUMMARY MUST CARRY THE FACT): a biting cap, or past-the-mark — the numbers move into an always-visible one-line summary and the per-layer detail folds. Grade 2 (STAYS OPEN): `mark === null` (unmeasured), 'no site yet', 'could not be saved' — these are not completeness readings and there is nothing to summarise. Keep `captionNeedsAttention` as the single judge (its biconditional arm in siteScopeSlider.spec.ts:672+ is what stops it drifting from `completenessCaption`), but have it return the GRADE rather than a boolean, and update that arm to assert the grade against the caption's arm.",
      "risk": "medium",
      "confidence": "high"
    },
    {
      "title": "The paragraph is `capVerdict`, one function, one production caller — and the founder's exact sentence is reproducible from it",
      "detail": "The text he quoted is `biting.map(v => v.line).join(' · ')` (SiteScopeSlider.ts:227). Each `line` is built in `capVerdict()` at `apps/editor/src/ui/geospatial/scopeClip.ts:382-399`. The '~3704 m' is NOT a lookup: it is `scopeOuterRadiusM(scope) * Math.sqrt(cap / eligible)` (:389-391) — the radius at which, at the SAME measured density, the cap would stop biting. The 'density 232 / km²' is `eligible / scopeAreaM2(scope) * 1e6` (:397). Check the arithmetic against his own numbers: trees 5755 × √(10000/24138) = 3703.7 → '~3704 m'. Buildings 5755 × √(27694/64044) = 3785 → '~3784 m' (0.03 % off, consistent with his eligible count being rounded in the panel he screenshotted). So the figures are real derived measurements from that load, not estimates. `capVerdict` has exactly ONE production caller: `CesiumViewport.ts:2589`, inside `getScopeCapVerdicts()`, iterating `this.scopeCapReports` — a Map with exactly TWO writers, `set('buildings', …)` at CesiumViewport.ts:12574 and `set('trees', …)` at :15054. That is why only two layers ever appear in the paragraph; roads, rail, water, parks and land use report no density at all, which the caption's own `none`-arm text at :249-256 admits.",
      "blocksInteraction": false,
      "costMs": "0 ms — pure arithmetic on counts already held; called on every slider repaint.",
      "costBasis": "read-from-code",
      "quote": "`complete at a scope of ~${Math.round(completeRadiusM!)} m (density ` +",
      "file": "apps/editor/src/ui/geospatial/scopeClip.ts",
      "line": 396,
      "fix": "No change to `capVerdict`. Add a pure sibling in the same file — `capVerdictDigest(verdicts): string | null` — returning the one-line summary from the SAME `CapVerdict` objects (sum of `dropped`, min of `completeRadiusM`). Putting it beside `capVerdict` rather than in the slider keeps one owner of the sentence and lets `scopeClip.spec.ts` (which already asserts the long line at :280) assert the short one against the same fixture.",
      "risk": "none",
      "confidence": "high"
    },
    {
      "title": "WHAT MUST SURVIVE THE COLLAPSE: this caption is the ONLY UI surface anywhere that says features are being dropped",
      "detail": "I traced every consumer of the drop counts. `capVerdict` → `getScopeCapVerdicts()` (CesiumViewport.ts:2587) → `defaultSiteScopePorts().getCapVerdicts` (SiteAuthoringPaneShell.ts:284) → `completenessCaption` → this one `<div>`. There is no second surface. The track's COMPLETE tick has only a `title` tooltip (SiteScopeSlider.ts:576-582) — hover-only, and it says 'Complete to ~X m (bound by Y)', which is the MARK, never the drop counts. Everything else is `console.log`. So hiding this paragraph outright makes a plate that is missing 36 350 buildings and 14 138 trees look complete, with nothing on screen contradicting it — precisely the §CONTEXT-DATA-HONESTY failure the memory [[context-data-honesty-family]] and [[envelope-solid-overstates-partial-data]] legislate against. ⭐ THE ONE LINE THAT MUST SURVIVE, and what each token is for: '⚠ 50 488 of 88 182 inside the scope not drawn · complete to ~3 704 m'. It must convey (a) that the number drawn is NOT the number inside the scope — a ratio, not a bare count, because '50 488 dropped' alone does not tell him whether that is 5 % or 60 %; (b) the RADIUS at which it would be complete, because that is the only actionable instruction the panel can give a man holding a slider; (c) a warning glyph, so the state is distinguishable at a glance from the folded reassurance state. It must NOT convey which layers, how many each, or the densities — those are the detail the fold is for. ⭐ AND IT DOUBLES AS THE ANSWER TO HIS OTHER QUESTION: 'complete to ~3 704 m' at a scope of 5 755 m is the panel telling him, on one line he cannot miss, that he is paying ~45 s for a plate thinner than the one at 3 704 m.",
      "blocksInteraction": false,
      "costMs": "0 ms — the counts are already computed for the long line.",
      "costBasis": "read-from-code",
      "quote": "if (biting.length > 0) parts.push(...biting.map((v) => v.line));",
      "file": "apps/editor/src/engine/views/SiteScopeSlider.ts",
      "line": 227,
      "fix": "Render the digest into `buildPanelFold`'s `setSummaryNote()` slot, which is always visible and is documented for exactly this. Colour it amber/warning rather than the muted grey — `envelopeCostSection.ts:355` already establishes the amber-for-a-ceiling precedent ('AMBER AND 10 px, NOT PURPLE … de-weighted, never hidden'). Assert in `siteScopeSlider.spec.ts` that with a biting verdict the summary note contains both the dropped total and the complete radius, and that it is present while the body is closed — the arm that stops a later lane collapsing the summary too.",
      "risk": "high",
      "confidence": "high"
    },
    {
      "title": "There IS a disclosure primitive to reuse — `buildPanelFold`, and its `setSummaryNote` was built for this exact obligation",
      "detail": "⛔ DO NOT BUILD A NEW ONE. `apps/editor/src/ui/site/panelFold.ts` (147 lines, landed a7e93fb3 on 2026-09-08 — the SAME DAY as the scope panel's bespoke ⓘ) is titled 'the ONE disclosure used by the floating Site-view panels (the Envelope tool and anything that grows the same way)'. The scope panel IS a floating Site-view panel. It gives you: a real `<details>`+`<summary>` (keyboard and screen-reader native), a UA-independent ▸/▾ marker drawn by the module because 'a `list-style:none` summary drops it entirely in WebKit' (:106-108), font-size INHERITED rather than declared so the C43 10 px floor cannot be breached (:88-97), an OTel span (P8), and — the decisive part — `setSummaryNote: (text: string) => void`, whose own doc comment reads 'The one fact that survives collapsing (C58 §1.2). Empty until a caller writes it.' Its header states the rule directly: 'It may hide EXPLANATION. It may not hide a REFUSAL, a WARNING, or a figure's confidence … `summaryNote` exists so a fold can carry the one fact that must survive collapsing.' Two existing callers: `siteEnvelopeTool.ts:253` and `parcelLawEnvelopeAuthoring.ts:880`/`:899`. ⚠ AND THE SCOPE PANEL'S CURRENT ⓘ IS ALREADY THE RIVAL C115-91 FORBIDS — a bespoke button plus a `display:none` div plus a private `captionOpen` local, built the same day, in ignorance of the primitive. This is the [[same-rule-two-implementations]] shape, caught one commit in.",
      "blocksInteraction": false,
      "costMs": "0 ms runtime. Build: ~1 h to swap the ⓘ+div for `buildPanelFold`.",
      "costBasis": "read-from-code",
      "quote": "/** Write the one fact that must stay readable while collapsed. `''` clears it. */",
      "file": "apps/editor/src/ui/site/panelFold.ts",
      "line": 70,
      "fix": "Replace `captionToggle` + `caption` + `captionOpen` (SiteScopeSlider.ts:408-416, 454-467, 485) with one `buildPanelFold({ id: 'site-scope-completeness', summary: 'What this scope draws', open: false })`. Write the long text into `.body`, the digest into `setSummaryNote()`. The import direction is already blessed: this file imports `../../ui/geospatial/siteScope` at :41 with a header note that '`engine/views` → `ui/*` is the established direction'. Check the one non-trivial consequence first: `panelFold.ts` imports `../analysis/parcelLawQuestionGroup` for the memory, and that module imports only `@opentelemetry/api` at its top — so the edge drags in no Parcel Law machinery. Keep the existing `data-testid`s (`site-scope-caption-${paneId}`) as aliases or update the four spec references at siteScopeSlider.spec.ts:604-670 in the same commit.",
      "risk": "low",
      "confidence": "high"
    },
    {
      "title": "(e) YES — remember it per session, not persisted; and the CURRENT local forgets it on a pane swap, which is a defect on its own",
      "detail": "YES, and the mechanism already exists and is already shared: `questionGroupFoldIsOpen` / `setQuestionGroupFoldOpen` (`apps/editor/src/ui/analysis/parcelLawQuestionGroup.ts:433` and `:438`), a module-level `Map<string, boolean>` at :410, keyed by `data-testid`. `buildPanelFold` reads and writes it for you (panelFold.ts:82, :130-133) — you would add nothing. WHY YES: `paint()` runs on every `store.subscribe` notification (SiteScopeSlider.ts:643) and on every drag frame via `onInput` (:598), so without a memory the fold state is only as durable as the local closure. WHY NOT PERSISTED: the map's own doc gives the reason — 'deliberately NOT persisted, so a new session gets the designed defaults back'; a user who once opened it would otherwise get the 'too large' panel back forever, which is the complaint arriving by memory. ⭐ AND THERE IS A REAL DEFECT IN TODAY'S LOCAL, INDEPENDENT OF THE FOUNDER'S ASK: `let captionOpen = false;` is declared INSIDE `mountSiteScopeSlider` (:485), and the shell mounts the control TWICE, once per pane (SiteAuthoringPaneShell.ts:643-644). The 3D Site is a singleton that MOVES between panes rather than cloning (`assignViewToPane`, paneViewModel.ts:411 and its comment 'the singleton MOVES (never clones)'). So swapping the 3D Site from the right pane to the left hands the user the OTHER instance, whose `captionOpen` is still `false` — the fold he just opened snaps shut. The shared map keyed by one id fixes that for free, because both instances read the same key.",
      "blocksInteraction": false,
      "costMs": "0 ms — one Map lookup per repaint.",
      "costBasis": "read-from-code",
      "quote": "let captionOpen = false;",
      "file": "apps/editor/src/engine/views/SiteScopeSlider.ts",
      "line": 485,
      "fix": "Use ONE id for both panes — `'site-scope-completeness'`, NOT `` `site-scope-completeness-${paneId}` `` — so the fold survives the singleton moving pane. Delete `captionOpen`; `buildPanelFold` owns the state. Add a spec arm: mount into the left pane, open the fold, dispose, mount into the right pane, assert it is still open. Do not add `localStorage`; the module comment at parcelLawQuestionGroup.ts:400-409 gives the reason and is correct.",
      "risk": "low",
      "confidence": "high"
    },
    {
      "title": "At the founder's scope the panel is a ~300 × 126 px pointer-capturing float over the 3D Site — and this view is now a face-drag surface",
      "detail": "Every term is declared, so this is arithmetic on read-from-code values, not a pixel measurement: border 1 + padding-top 7 (:349) + head 20 (button padding 3×2 at :383 + 12 px line at :382 + 2) + head margin-bottom 4 (:359) + track 16 (padding 0 0 2 at :426 + height 14 at :437) + padding-bottom 6 + border 1 = 55 px folded, at width 300 px (:348). His caption is 253 characters at `500 10px/1.35` (:459) in a 280 px content box → ~5 lines → 67.5 px, + 3 px margin-top = 70.5 px, total ~126 px. If a `read`-kind mark also fires, `completenessCaption` prepends a 320-character sentence (:216-224) and the same panel reaches ~207 px. The root sets no `pointerEvents` (the full style block is :340-355 — position, bottom, left, transform, zIndex 60, display, boxSizing, width, padding, borderRadius, border, background, boxShadow, font, color; there is no `pointerEvents` key), so the whole box swallows clicks on the model beneath it. That matters more than it did last week: `siteEnvelopeTool.ts:266-270` records that face drag SHIPPED on this surface (§ENVELOPE-FACE-DRAG-PER-LEVEL, L-13236) and that 'the 3D Site is now a FACE-DRAG surface'. So the panel is not inert chrome over a picture — it is 37 800 px² of dead zone over a drag surface, at the bottom-centre where a slab's near face sits. ⚠ SCOPED HONESTLY: this blocks a click in ONE region of ONE view, not the application. It is not a freeze and it is not a wait.",
      "blocksInteraction": true,
      "costMs": "0 ms of wait; ~37 800 px² of occluded, click-swallowing surface at r=5755 (~16 500 px² folded).",
      "costBasis": "read-from-code",
      "quote": "font: '500 10px/1.35 system-ui, sans-serif', color: MUTED, marginTop: '3px',",
      "file": "apps/editor/src/engine/views/SiteScopeSlider.ts",
      "line": 459,
      "fix": "The fold recovers ~70 px of it for free. Beyond that, add `pointerEvents: 'none'` to the ROOT and `pointerEvents: 'auto'` to the three interactive children (shapeWrap, input, the fold's summary) — the panel then only eats clicks where it actually has a control, and the padding, the border and the caption text stop blocking the drag surface. Cheap, local, and independently testable by asserting the two declarations.",
      "risk": "low",
      "confidence": "high"
    },
    {
      "title": "The ⓘ hit target is ~14 × 12 CSS px — under half the 24 × 24 minimum C43 names, on the control he is being asked to click",
      "detail": "`captionToggle` is declared `padding: '0 1px'` and `font: '600 12px/1 system-ui, sans-serif'` (SiteScopeSlider.ts:413-414). A unitless line-height of 1 on a 12 px font is a 12 px line box, and the padding adds nothing vertically — so the button's box is ~12 px tall and ~14 px wide. `docs/02-decisions/contracts/C43-ACCESSIBILITY.md:481` names 'WCAG 2.2 new criteria (… 2.5.8 target size minimum 24x24 …)' and says these 'apply to PRYZM without special effort'. This one did not get it. It matters specifically here because the founder's sentence is 'ONLY IF THE USER WANTS TO READ WILL OPEN' — the whole feature is a click on this target, and today it is a 12 px glyph with no text label. ⚠ Note this is a genuine additional finding, not a restatement of the disabled-state one: even in the reassurance state where the ⓘ IS enabled, it is too small to hit reliably.",
      "blocksInteraction": false,
      "costMs": "0 ms.",
      "costBasis": "read-from-code",
      "quote": "appearance: 'none', border: 'none', background: 'transparent', padding: '0 1px',",
      "file": "apps/editor/src/engine/views/SiteScopeSlider.ts",
      "line": 413,
      "fix": "Solved by the swap to `buildPanelFold`: its `<summary>` is a full-width flex row (panelFold.ts:100-103) carrying marker + label + note, so the target becomes the whole row rather than a 12 px glyph, and it is keyboard-operable natively. If the ⓘ is kept for any reason instead, give it `min-width:24px;min-height:24px;display:inline-flex;align-items:center;justify-content:center` and keep the glyph at 12 px inside it.",
      "risk": "none",
      "confidence": "high"
    },
    {
      "title": "CORRECTION TO THE BRIEF: there is no checkbox and no radio — those are the ▭ and ◯ shape-toggle glyphs, and being misread is itself the finding",
      "detail": "The brief asks for 'the checkbox, the radio'. The panel contains neither: `grep checkbox` across `apps/editor/src/**/{geospatial,views,site}/**` returns three hits, all in `ViewSwitcherPill.ts` and a Cesium comment, none in this panel. What is there is a two-button segmented control at SiteScopeSlider.ts:371-386, `b.textContent = glyph` with glyphs '▭' (U+25AD, white rectangle) and '◯' (U+25EF, large circle), `aria-pressed` toggled at :560-565, styled at 12 px with 3×7 px padding. At that size ▭ is visually an unchecked checkbox and ◯ is an unchecked radio — which is exactly how they were read. That is a real affordance defect: a segmented shape picker that reads as two independent form controls invites the user to think he can have both, or neither. Separately, the brief places the panel 'bottom-right'. It is declared bottom-CENTRE of its PANE (`bottom:'14px'`, `left:'50%'`, `transform:'translateX(-50%)'`, :342-346, with a comment pinning it to C59 §2.10.3 clause 4 — never `document.body`). It appears bottom-right on screen because `siteAuthoringDefaultLayout()` puts `site-3d` in the RIGHT pane (paneViewModel.ts:411). Both descriptions are of the same one control; there is no second scope panel.",
      "blocksInteraction": false,
      "costMs": "0 ms.",
      "costBasis": "read-from-code",
      "quote": "['rectangle', '▭', 'Rectangular scope'],",
      "file": "apps/editor/src/engine/views/SiteScopeSlider.ts",
      "line": 374,
      "fix": "Out of scope for this lane and NOT worth bundling with the fold — flag it and let the founder rule. If he wants it: keep the segmented control (it is correct semantics) but replace the ambiguous glyphs with filled/outlined state, or add the word 'Rect' / 'Circle' at the same 12 px. Do not convert it to a real radio group — that would be a third pattern for one idea on a panel that is already about to lose one.",
      "risk": "none",
      "confidence": "high"
    }
  ]
}