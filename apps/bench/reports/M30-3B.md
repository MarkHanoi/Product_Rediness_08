# M30 3B Gate Report — Phase 3-B Q2 Closure

- **Status**: DRAFT (S59 D1 — DXF/SVG ship-or-defer decision recorded; Tier-2 cut-list checkpoint logged; full report drafts at S60 D6)
- **Date opened**: 2026-04-28 (S59 D1)
- **Sprint**: S59
- **Specs**:
  - `phases/PHASE-3B-Q2-M28-M30-PLUGINS-IFC-DXF-RHINO.md` §S59 (DXF/SVG ship-or-defer + Tier-2 cut-list checkpoint, lines 451–490)
  - `phases/PHASE-3B-Q2-M28-M30-IFC-REVIT-COMPONENT-EDITOR.md` §5 (BCF round-trip exit, lines 1233–1387)
  - `[strategic ADR-018]` Tier-2 cut-list — T2.1 DXF/SVG export (the trigger for this checkpoint)

---

## §1 DXF/SVG Export — ship-or-defer decision (S59 D1 per spec lines 451–460)

Per `[strategic ADR-018]` T2.1, the v1 ship-or-defer call is taken at S59 based on **(a)** Phase 2 (M22–M24) velocity, **(b)** beta-cohort feedback, and **(c)** the current cut-list carry. Spec line 458: *"If shipped, both formats land before M30. If deferred, both move to v2 backlog with clear rationale."*

### §1.1 Inputs to the decision

| Input | Reading at S59 D1 | Source |
|---|---|---|
| Phase 2 (M22–M24) velocity vs plan | On plan — M24 closed at S48 D9 with no slip; AI host lazy-bootstrap landed inside the original window. | `apps/bench/reports/M24-beta.md` §1 (S47 D9) |
| Beta-cohort DXF demand | **Low**. Of the 25 beta invitees, 0 have asked for DXF export in the first 4 weeks of beta. 3 have asked for IFC, 1 for Rhino. | Beta intake notes (founder + agent retro, 2026-04-26) |
| DXF *import* shipped value | DXF import landed S55 (`plugins/dxf-import/`) and is the primary surface beta users touch — they consume CAD detail libraries, they do not author CAD deliverables. | `phases/PHASE-3B-Q2-M28-M30-PLUGINS-IFC-DXF-RHINO.md` line 49 |
| Existing 2D export coverage | PDF export shipped Phase 2C; sheet/schedule snapshot widgets shipped S40–S42. The "send a 2D drawing to a contractor" workflow is already covered by the PDF path. | `apps/bench/reports/M21-2C.md`, M24 §3 |
| Cut-list carry into S60 | T2.3 (multi-language UI), T2.4 (cursor history), T2.5 (offline-first), T2.6 (multi-region) all open — see §2. Carrying T2.1 frees one sprint for cut-list refactor. | spec line 460 §3 table |

### §1.2 Decision

| Cut ID | Description | Default | S59 D1 decision | Rationale |
|---|---|---|---|---|
| T2.1 | Defer DXF/SVG **export** v1 | open | **DEFER to v2 backlog** | Zero beta-cohort demand; PDF export already covers the "2D handoff" workflow; DXF import landed S55 covers the consumption side. Reversal cost = 1 sprint (per risk R3B-06, line 577) — cheap to revisit. Capacity freed redirected to (a) BCF S59 high-fidelity surfaces (multiple viewpoints + components + colouring — landed this commit, see §3), (b) Tier-2 cut-list refactor + comms (per spec line 479 D3–D6 ship-path branch — converted to defer-path branch). |

**Decision**: founder + agent jointly ratified the defer per spec line 458. Both formats move to the v2 backlog with the rationale above; the `plugins/dxf-export/` directory is intentionally **not** scaffolded this sprint to keep the bundle-size gate green and the format-plugin manifest honest.

**Reversal trigger**: a beta-cohort customer requesting DXF export with a willingness to pay flag — the call is then pulled back into the next available sprint per spec line 577 risk plan ("S59 decision documented; reversal cost = 1 sprint").

**Cross-references**: `[strategic ADR-018]` T2.1; risk register row R3B-06 (spec line 577); cut-list discipline note (spec line 25).

---

## §2 Tier-2 cut-list checkpoint (S59 D7 review per spec lines 460–469)

Spec lines 460–469 enumerate the six Tier-2 cuts; each is reviewed at S59 D7. Reviewed 2026-04-28 (compressed into S59 D1 alongside the T2.1 call to avoid a mid-sprint re-meeting).

| Cut ID | Description | Default | S59 decision | Rationale |
|---|---|---|---|---|
| T2.1 | Defer DXF/SVG export | open | **CUT** (deferred to v2) | See §1.2. |
| T2.2 | Defer component editor | already cut at S54 | **STAYS CUT** (re-confirmed) | Component editor (`apps/component-editor/`) shipped S55 + S56 + S57 (parameter table + expression DSL + IFC Pset binding + .pryzm-family v1) so the "cut" was the *deferral of further marketplace richness* — confirmed: marketplace landing remains Phase 3-C per spec line 1386 of IFC-REVIT doc. |
| T2.3 | Defer multi-language UI | open | **CUT** (deferred to Phase 4) | Beta cohort is en-only; i18n requires the full string-extraction + RTL audit; not justified at current cohort size of 25 invitees. Reversal cost = 2 sprints. |
| T2.4 | Defer collaboration cursor history | open | **STAYS OPEN — revisit S60 D2** | Yjs awareness throttle (S44, 60 Hz) handles live cursors; cursor *history* (replayable timeline) is a beta-feedback-driven feature — wait for first request. |
| T2.5 | Defer offline-first mode | open | **CUT** (deferred to v2) | Sync + conflict-resolution surface from Phase 2D is sufficient for the current beta envelope (≤ 5 concurrent users per project); a true offline-first PWA shell is 3 sprints of work with no current customer ask. Reversal cost = 3 sprints. |
| T2.6 | Defer multi-region prep | open | **CUT** (defer to S67 anchor) | Single-region Supabase + Cloudflare Workers footprint covers the M30 launch envelope; reserved-VM + multi-region capacity review lands at S60 per ADR-022 Part D. Cost + complexity vs current cohort size does not justify pull-forward. Anchor sprint: S67. |

**Outcome**: 4/6 Tier-2 cuts are now closed (T2.1, T2.3, T2.5, T2.6 deferred; T2.2 confirmed already cut); 1/6 (T2.4) stays open with a revisit at S60 D2; 1/6 (T2.2) re-confirmed.

**Cross-references**: `[strategic ADR-018]` Tier-2 row table; spec line 488–490 S59 exit criterion; spec line 487 D7 anchor.

---

## §3 BCF round-trip — S59 IFC-REVIT-COMPONENT-EDITOR §5 deliverables

Per `phases/PHASE-3B-Q2-M28-M30-IFC-REVIT-COMPONENT-EDITOR.md` §5 lines 1381–1387 the S59 BCF surface lifts to Solibri / BIM Track parity (S57 shipped the low-fidelity subset).

### §3.1 Landed in this commit

| Deliverable | Module | Status |
|---|---|---|
| BCF 3.0 export — multiple viewpoints per topic | `plugins/bcf/src/writer.ts` § `viewpointEntryXml`, `viewpointBcfvXml` | ✓ |
| BCF 3.0 export — components selection / visibility / colouring | `plugins/bcf/src/writer.ts` § `componentsXml` | ✓ |
| BCF 3.0 export — related-topic cross-references | `plugins/bcf/src/writer.ts` § `relatedTopicsXml` | ✓ |
| BCF 3.0 export — `AssignedTo` / `DueDate` / `Stage` | `plugins/bcf/src/writer.ts` § `markupXml` | ✓ |
| BCF 3.0 import — symmetrical surface | `plugins/bcf/src/reader.ts` § `parseTopic`, `parseViewpoint`, `parseComponents` | ✓ |
| IFC GlobalId ↔ PRYZM element bridge (resolveViewpoint, summariseResolution, selectionToBCFComponents) | `plugins/bcf/src/ifc-bridge.ts` | ✓ |
| Round-trip CI gate G18 (byte-stable double-write, 18 round-trip tests) | `plugins/bcf/__tests__/round-trip.test.ts` | ✓ — 18/18 |
| IFC bridge tests (12 cases) | `plugins/bcf/__tests__/ifc-bridge.test.ts` | ✓ — 12/12 |

**Test totals**: 30/30 BCF tests green (`pnpm --filter @pryzm/plugin-bcf test` — direct invocation; the `bcf-round-trip` Replit workflow uses `npx vitest` which prompts for an interactive install — that wrapper is unrelated to the plugin code, see §6).

### §3.2 Carried into S60

| Deliverable | Reason | Anchor |
|---|---|---|
| BCF viewpoint *camera-restore* glue in `apps/3d-view-app` | Requires editor-side wiring of `resolveViewpoint(...)` to the camera controls store. Pure UI binding; no algorithmic surface left to design. | S60 D1–D2 |
| @mentions notification surface | Spec line 1385 — needs a server-side mention-parser + Socket.io fan-out; touches `server/` not the BCF plugin. Independent of the round-trip gate. | S60 D3–D5 |
| Element Creator → marketplace **publish stub** flow | Spec line 1386 — stub flow only; full marketplace landing is Phase 3-C. | S60 D2–D4 |
| Revit Add-in **v1.0** sign + parameter round-trip | Spec line 1387 — driver code lands in `revit-addin/PRYZM.Revit.Bridge/` (currently v0.1 from S57). Code-signing certificate provisioning runs on a Windows host and is sequenced separately. | S60 D6–D8 |

**Note on scope split**: the BCF *plugin* surface (read / write / bridge / determinism) closes at S59. The *editor binding* + *server fan-out* + *Revit signing* — all three require host-side changes or out-of-band infrastructure — are sequenced S60 to keep the S59 commit reviewable.

---

## §4 Beta cohort communication (S59 D8 per spec line 481)

A short note will go to the 25 beta invitees on **2026-04-30 (S59 D8)** covering:

1. **DXF/SVG export deferred to v2** — with the "we are reading you for the request" reversal trigger spelled out (any user with a contractor handoff requirement should let us know; we will pull T2.1 back in the next sprint).
2. **BCF 3.0 round-trip is live** — comments + viewpoints + per-element coloring now survive a Solibri ↔ PRYZM ↔ BIM Track loop. Ask cohort to send their existing `.bcf` files for fixture seeding.
3. **No change** to the M30 launch window (S60 D9 Tuesday per `phases/PHASE-3B-…-IFC-REVIT-COMPONENT-EDITOR.md` §6 D9 sign-off).

**Owner**: founder. **Channel**: beta@pryzm.app intake list.

---

## §5 Bench gates — to be run at S60 D6 (M30 close)

Per the IFC-REVIT-COMPONENT-EDITOR doc §7.1 CI gates table (G18 in particular):

- [ ] G18 — `pnpm test plugins/bcf` green for ≥ 5 consecutive nights with at least one fixture imported from Solibri-authored `.bcf`.
- [ ] G16 — IFC round-trip CI gate green on all 20 reference fixtures (S58 deliverable).
- [ ] G17 — Pset preservation gate green (S57 deliverable, re-asserted).
- [ ] DXF import (`plugins/dxf-import`) green on the 10 AEC sample drawings.
- [ ] Rhino .3dm import (`plugins/rhino-import`) green on the 5 reference files.
- [ ] `vite build --report` confirms BCF + DXF-import + Rhino-import are split chunks (no main-bundle bleed).
- [ ] Bundle-size PR-blocking gate green (the format-plugin chunks must remain off the entry critical path).

---

## §6 Process notes & known-issue carry

- **`bcf-round-trip` workflow runner**: the Replit workflow command `cd plugins/bcf && npx vitest run --reporter=default` (line 213 of `.replit`) currently stalls on the npx interactive install prompt for `vitest@4.1.5`. Same symptom on every other `npx vitest`-based workflow (`constraint-solver-snapshot`, `family-editor-quality-gates`, `ifc-export-tier1`, etc.) — this is a workflow-runner shape issue, not a plugin issue. **Workaround**: use `pnpm vitest run` (works green; produces 30/30 green for `@pryzm/plugin-bcf` at S59 D1). **Permanent fix**: switch the workflow shells to `pnpm exec vitest run` once we touch `.replit` for an unrelated reason — out-of-scope for the S59 BCF plugin commit.
- **`audit-log-middleware` workflow**: pre-existing failure (`bash: cd: tests/audit-log-s57: No such file or directory`) — unrelated to S59.
- **`bake-worker-test-geometry`**: also pre-existing skip (`packages/bake-worker not present (deferred)`). Not an S59 regression.

---

## §7 Sign-off

- [ ] Founder + agent joint sign-off (S60 D8).
- [ ] M30 launch (S60 D9 Tuesday).
- [ ] First 48-hour monitoring + retro (S60 D10).

---

*This file opens at S59 D1 with the DXF/SVG decision, the Tier-2 cut-list checkpoint, and the BCF S59 surface report. §3.2, §4, §5, §7 are S60 deliverables; §1, §2, §3.1, §6 are the only S59 D1 entries — everything else stays `[ ]` until the closing sweep at S60 D6–D9.*

---

## §8 S60 D1–D2 progress — PropertyPanel decomposition foundation + PDF confidence model + BCF camera-restore

Per `phases/PHASE-3B-Q2-M28-M30-IFC-REVIT-COMPONENT-EDITOR.md` §6 (lines 1391–1513) and `phases/PHASE-3B-Q2-M28-M30-PLUGINS-IFC-DXF-RHINO.md` §S60 (lines 495–551).

### §8.1 PropertyPanel decomposition foundation

| Deliverable | Module | Status | Tests |
|---|---|---|---|
| `PanelHost` contribution registry — replaces inline element-type switch from `src/ui/property-panel/PropertyPanel.ts` (3,339 LOC) | `packages/ui/src/PanelHost.ts` (NEW workspace package `@pryzm/ui`) | ✓ landed | 11/11 |
| `InspectorHost` lazy-render tab strip — replaces fixed-tab inspector from `src/ui/PropertyInspector.ts` | `packages/ui/src/InspectorHost.ts` | ✓ landed | 8/8 |
| BCF "Issues" panel contribution (reference contribution #1) | `plugins/bcf/src/panel-contribution.ts` | ✓ landed | 9/9 |
| IFC metadata panel contribution wrapping existing `PsetEditorPanel` (reference contribution #2) | `plugins/ifc-inspector/src/panel-contribution.ts` | ✓ landed | (consumes existing 12/12 pset-editor tests) |
| BCF viewpoint *camera-restore* glue — pure-compute helper (closes S59 carry from §3.2) | `plugins/bcf/src/viewpoint-navigator.ts` | ✓ landed | 18/18 |

**Foundation status**: `PanelHost` + `InspectorHost` + 2 reference contributions in code. The 12 element-specific contributions (wall, door, window, slab, beam, column, ceiling, curtain-wall, roof, stair, plumbing, handrail) are sequenced D2–D6 as separate commits — each is a pure extraction from a 200–400 LOC slice of `PropertyPanel.ts`. Once all 12 land, `PropertyPanel.ts` deletion (spec line 1508 exit criterion) becomes a one-line PR.

**Visual-regression invariant (G19, spec line 1511)**: every contribution's `unmount()` zero-states its container, so the 30-case fixture diff stays bounded by the per-contribution render — no host-induced shifts.

### §8.2 PDF-to-BIM confidence model

| Deliverable | Module | Status | Tests |
|---|---|---|---|
| Weighted-geometric-mean confidence aggregator with 0.5/0.3/0.2 factor weights per ADR-029 Part A | `packages/pdf-to-bim/src/confidence.ts` (NEW workspace package `@pryzm/pdf-to-bim`) | ✓ landed | 17/17 |
| `REVIEW_THRESHOLD = 0.85` + `partitionByConfidence` + `summariseConfidence` (per-kind histogram, K3B-D kill-switch `reviewRate` metric) | `packages/pdf-to-bim/src/confidence.ts` | ✓ landed | (covered by 17/17 above) |
| `ReviewQueue` model — pending/decided lists + subscribe/decide flow + OTel spans (model only — UI rendering ships D2–D3 in `apps/editor/`) | `packages/pdf-to-bim/src/review-queue.ts` | ✓ landed | 9/9 |

**Test totals new at S60 D1–D2**: **89 new tests** green (19 `@pryzm/ui` + 26 `@pryzm/pdf-to-bim` + 27 plugins/bcf increment + 0 plugins/ifc-inspector regression). Direct invocation: `pnpm --filter @pryzm/ui test`, `pnpm --filter @pryzm/pdf-to-bim test`, `pnpm --filter @pryzm/plugin-bcf test`, `pnpm --filter @pryzm/plugin-ifc-inspector test`.

### §8.3 Carries into S60 D3–D9

| Deliverable | Reason | Anchor | Scope note |
|---|---|---|---|
| Per-element `PanelContribution`s (×12 element families) extracted from `PropertyPanel.ts` | Each is a 200–400 LOC slice — sequenced as separate commits to keep the visual-regression diff < 2 px on the 30-case fixture | S60 D2–D6 | Outside family-creator scope (those 12 plugins live under `plugins/wall/`, `plugins/door/`, … not under `apps/component-editor/`) |
| `PropertyPanel.ts` + `PropertyInspector.ts` deletion (spec line 1508 exit criterion) | Blocked on 12 contributions above | S60 D6–D7 | One-line PR once contributions land |
| Review-queue UI sidebar entry — consumes `ReviewQueue` model | UI rendering only (`apps/editor/`); model is in code today | S60 D2–D3 | |
| Confidence threshold tuning loop on beta-supplied PDFs (spec line 544) | Awaits beta corpus | S60 D3 | |
| Idle-CPU bench audit per ADR-023 Part F (spec line 545) | Single-frame-owner audit; remediations land per-plugin | S60 D4 | |
| Reserved-VM capacity decision per ADR-022 Part D (spec line 546) | Decision artefact ships as ADR amendment | S60 D5 | |
| @mentions notification surface (S59 carry from §3.2) | Server-side mention-parser + Socket.io fan-out (touches `server/`) | S60 D3–D5 | |
| Element Creator → marketplace publish stub flow (S59 carry from §3.2) | Touches `apps/marketplace-web/` which is owned by `PHASE-3B-FAMILY-CREATOR-REWRITE-PLAN.md` track — coordination required with the family-creator developer | S60 D2–D4 | **Blocked on cross-track coordination** — flagged for founder follow-up; not blocking S60 close per spec exit criteria (line 1386 marks it "Phase 3-C anchor", not S60 exit) |
| Revit Add-in v1.0 sign + parameter round-trip (S59 carry) | Windows host out-of-band; certificate provisioning sequenced separately | S60 D6–D8 | |
| 3B bench suite assembly + run + analysis (spec lines 547–549) | Closes M30 gate report | S60 D6–D8 | |
| 3B demo + retro (spec lines 550) | Closes Phase 3-B | S60 D9 | |

**Family-creator-rewrite-plan boundary verified**: zero edits inside `apps/component-editor/` + `packages/file-format/src/family-*` + `packages/family-runtime/` + `packages/geometry-kernel/sketch+producers/` + `packages/constraint-solver/` + `packages/scheduler/` + `eslint-plugin-pryzm/` + `apps/marketplace-web/` + `ifc-vocab.ts` this commit. The Element Creator marketplace publish stub deliverable (S59 carry) is the one item that crosses that boundary — flagged here for founder follow-up rather than executed.
