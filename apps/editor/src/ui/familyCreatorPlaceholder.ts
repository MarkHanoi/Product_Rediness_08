/**
 * familyCreatorPlaceholder.ts — stub for the future Family Creator panel.
 *
 * Phase E placeholder: opens a toast/alert until the real panel is built.
 * TODO(E.family-creator): replace with actual FamilyCreatorPanel implementation.
 */
export function openFamilyCreatorPlaceholder(): void {
    console.log('[FamilyCreator] placeholder — not yet implemented (Phase E)');
    // No-op until the real panel lands. Intentionally silent in production.
}
