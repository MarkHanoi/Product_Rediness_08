# ─────────────────────────────────────────────────────────────────────────────
# deploy-fly.yml — Continuous deployment to Fly.io.
#
# Canonical contract: ADR-055 (docs/02-decisions/adrs/ADR-055-one-pryzm-cloudflare-supabase.md).
# Trigger: every push to `main` (+ manual). The image is built ON THIS RUNNER
# via `flyctl deploy --local-only` (GitHub's ubuntu-latest = 16GB RAM + Docker),
# NOT on Fly's managed builder — that builder OOM-kills this ~6GB-heap, 3624-
# module client build at exit 137 and cannot be resized from the CLI. The runner
# pushes the finished image to Fly's registry.
#
# Prerequisites (one-time setup):
#   1. `flyctl auth token` → store output as the `FLY_API_TOKEN` repo secret.
#   2. `flyctl secrets set` for every runtime env var listed in fly.toml's header.
#   3. The `pryzm` app exists in the Fly org (`flyctl apps create pryzm`).
# ─────────────────────────────────────────────────────────────────────────────

name: Deploy to Fly.io

on:
  # §A.10.q — PUSH-DEPLOY re-enabled (repo PUBLIC) to ship v40 (A.21.D29 + D28 #7/#9).
  # Builds on the 16GB runner WITH the VITE_CESIUM_TOKEN secret (real 3D-Tiles globe)
  # when set. Revert to manual-only (§A.10.i) before going private again.
  push:
    branches: [main]
  workflow_dispatch:

# Latest-wins: a newer push to main cancels an older in-flight deploy so prod
# always converges on the newest commit (correct for continuous deployment).
concurrency:
  group: deploy-fly
  cancel-in-progress: true

jobs:
  deploy:
    name: build (16GB runner) + deploy
    runs-on: ubuntu-latest
    # Hard ceiling — building the full minified client on the runner (~10-15 min)
    # + image push + machine bounce. A 30-min cap catches a hang without burning
    # an hour of Actions minutes.
    timeout-minutes: 30

    # Surface a deploy URL in the Actions UI ("View deployment" button).
    environment:
      name: production
      url: https://pryzm.fly.dev

    permissions:
      # We only need to read the repo + write deployment statuses; nothing else.
      contents: read
      deployments: write

    steps:
      - name: Check out code
        # actions/checkout@v5 runs on Node.js 24 — v4 (Node 20) is deprecated and will be
        # force-migrated 2026-06-16 / removed 2026-09-16 (GitHub runner Node-20 deprecation).
        uses: actions/checkout@v5
        with:
          # Shallow clone is fine — flyctl uploads the working tree as a tarball
          # to the remote builder, history isn't used.
          fetch-depth: 1

      - name: Install flyctl
        # Pinned to the action's `master` per Fly's published install pattern.
        # If we ever need reproducibility on the CLI version itself, pin to a
        # tagged release of the action AND pass `version:` here.
        uses: superfly/flyctl-actions/setup-flyctl@master

      - name: Deploy to Fly
        # --local-only: build the image with THIS runner's Docker daemon (16GB)
        #   instead of Fly's managed builder, which OOM-kills this build at exit
        #   137 (and can't be resized from the CLI). The runner then pushes the
        #   finished image to Fly's registry — nothing builds/uploads from a dev box.
        # --build-arg LOWMEM=0: full esbuild minify (the runner has the RAM the
        #   managed builder lacked — see Dockerfile `ARG LOWMEM`).
        # --strategy=rolling: explicit single-machine rolling bounce.
        # --build-arg VITE_CESIUM_TOKEN: optional Cesium ion token (GIS-CESIUM-
        #   PHOTOREAL). When the `VITE_CESIUM_TOKEN` repo secret is set, vite bakes
        #   it into the client bundle and the "3D globe" streams Google Photorealistic
        #   3D Tiles + the ion satellite base. When the secret is empty/absent the
        #   build arg is "" → the keyless ESRI satellite basemap (current behaviour),
        #   so adding the secret is the ONLY step needed to switch on full photoreal.
        # --build-arg VITE_GOOGLE_MAPS_KEY: optional Google Maps Platform API key
        #   (GIS-CESIUM-GOOGLE-KEY, A.21.D31) — the ALTERNATIVE real-tiles credential.
        #   When the `VITE_GOOGLE_MAPS_KEY` repo secret is set (and no ion token), the
        #   globe streams the SAME Google Photorealistic 3D Tiles directly via the key.
        #   Empty/absent (the default) → no effect, keyless ESRI fallback unchanged.
        #   Branch order is ion-token → google-key → keyless.
        run: flyctl deploy --local-only --build-arg LOWMEM=0 --build-arg VITE_CESIUM_TOKEN="${{ secrets.VITE_CESIUM_TOKEN }}" --build-arg VITE_GOOGLE_MAPS_KEY="${{ secrets.VITE_GOOGLE_MAPS_KEY }}" --strategy=rolling
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

# deploy-marker: v35 — public-window build 2026-06-06 (house wiring + D11 + D16 + Living Graph + crash-fix + governance docs)

# deploy-marker: v36 public window 2026-06-06b — all seven fixes (D21x5 + D15 + D22 + D23)
# deploy-marker: v38 — A.21.D25 (4 deep house defects: re-projection loop · furnish-all-storeys · plate subdivision + wall joins · Forma floors/roof/furniture) + A.21.D26 (stairwell-void guardrail)
# deploy-marker: v39 — A.21.D28 (Forma elements+crash+overlays · window-openings · ground-subdivision · resizable graph)
# deploy-marker: v40 — A.21.D29 (floor/ceiling void · railing render-fix · entrance door · env-drivers E.1/E.2) + A.21.D28 #7/#9 (furnish-all modal · bottom level selector). HELD for founder go.
# deploy-marker: v40-retry — re-trigger after transient Fly 504 (build was fine; registry/API timeout)
# deploy-marker: v41 — A.21.D30 (Forma single-prism shell) + stair space-efficiency objective + A.21.D33 (railing-align · level-interrogate crash · compliance-overlay-off · interior-wall-vs-window · forma roof+furniture · living-graph bottom-right grip)
# deploy-marker: v42 — A.21.D27 climate dataset ingest (heat+wind overlays, offline lat-driven fallback + site auto-key) + A.21.D31 3D-Tiles Google-Maps-key fallback path
# deploy-marker: v43 — next-demo wave: D12 windows real void+glazing · D14 missing-rooms junction-repair · D9 boundary live dimensions (D11 partition-clamp already on main)
# deploy-marker: v44 — Phase A: first-class Inspect panel (ModelTree + Provenance, A.24+A.31.e) + advisory axe-core a11y CI job (A.32.β)
# deploy-marker: v45 — quality: env-drivers E.3 acoustic-zoning + E.4 ventilation objectives (+ vertical-stack acoustic pref) + kitchen island (fridge/wardrobe-variants already shipped)
# deploy-marker: v45-trigger — explicit deploy (env E.3/E.4 + kitchen island)
# deploy-marker: v46 — A.21.D34: skewed-plate geometry (stair+window in-shell, room self-intersect, degenerate wall) · Forma extras (windows/doors/stairs/furniture) · per-floor datum planes hidden in 3D · living-graph spacing
# deploy-marker: v46-trigger — explicit deploy (D34 wave: skewed-geometry · Forma extras · datum-planes · living-graph)
# deploy-marker: v47 — D36 layout completeness (no room-drop + multi-hop circulation + window-in-shell) + Forma window/door insets (proud of face) + D37 Living Graph (3D select/isolate + Miro zoom-pan) + SECURITY: removed hardcoded Cesium token
# deploy-marker: v48 — house geometry (roof-by-storey · doors-in-wall · D38 wall-slab continuity) + grid-planes recurrence (floor-tile-hatch hidden in 3D) + D35 Forma climate viz (wind streamlines + comfort heat field) + railing-bus log cleanup
# deploy-marker: v48-trigger — explicit deploy (house geometry + grid-planes + D35 climate viz + railing cleanup)
# deploy-marker: v49 — D39: ground-floor room-closure ROOT FIX (weld partitions to pre-drawn shell → real rooms, fixes furniture-overlap cascade) + corner-window drop + house placed on photoreal globe + window glass (shadow-through) + climate ingest on Forma (wind rose populates)
# deploy-marker: v49-trigger — explicit deploy (D39 room-closure root fix + house-on-globe + window-glass + climate-on-Forma)
# deploy-marker: v50 — D40: mirror-shadow fix (drop buried ground bottom-face) + house ON photoreal 3D-tiles (sampleHeight reseat) + transparent BIM window glass (MeshPhysical transmission) + window full-span corner-clearance + per-wall de-overlap + ground-walls-stay-put (rebuildWallBodies, no whole-level re-trim)
# deploy-marker: v50-trigger — explicit deploy (D40: mirror-shadow · house-on-tiles · glass · window corner/overlap · ground-walls-stay-put)
# deploy-marker: v51 — D40 DEEPER/QUEUED: central-blob subdivision (private rooms always walled — isOpenPlanEligible guard on open-zone union-find) · generation perf (room-naming sync fast-path kills 5s/storey hard-timeout + scoped PBR upgrade + skip rename compile) · climate-on-Forma root fix (dead projectContext.projectId fallback → __pendingProjectId) · wall-join pass-through T-junction flush (square caps, no gap/overlap) · per-room door/window types reach the LIVE opening (door finish was on the dead batch payload; global fallback was a non-real id → dt-solid-timber) · furnish bedside lamps (D-FLE) · A.25.3 living-design sliders (adjacency/accessibility/climate/space → existing substrate, neutral=identity). ai-host 1949
# deploy-marker: v52 — D42/D43 founder-defect wave: #6 room-fill overlay hidden in 3D (the 'boundary shadow') · #7 seamless wall body around openings (WallHoleBodyBuilder ExtrudeGeometry, no T-junction seam) · Cesium context-dedup-on-photoreal-tiles + house in real BIM scene colours · D43 Forma context-on-ground (formaTerrainBaseHeight=0 reset) + larger context bbox (~2.8km) + translucent Forma window glass. geometry-wall 39/39; editor changed-files tsc clean. #5 perimeter worst-aspect stair + central-stair enclosure (regressions fixed: variant-0=single-best via bestStoreyOptionIndex; §STAIR-CARVE-NO-DROP keeps the en-suite). ai-host 1975/1975.
# deploy-marker: v53 — A.26.3 editable Living Graph (BIM 2.0/3.0 headline): edit a room's area on the graph inspect card → per-room override (roomAreasByName) → existing triggerApartmentLayout re-runs the deterministic engine → layout updates + graph re-lays-out; baseline-identity invariant (empty stash = byte-identical). ADR-0061 (building graph = bidirectional edit substrate, PROPOSED). ai-host 1982/1982, editor changed-files tsc clean.
# deploy-marker: v53-retry — re-trigger after transient Fly 504 (registry/API timeout; build was fine). Same content as v53 (36-commit v51+v52+v53 batch).
# deploy-marker: v54 — A.26.2 (Living Graph adopts the Inspect-tab purple-gradient chrome) + A.26.4 (editable room OCCUPANCY/TYPE on the inspect card → roomTypesByName per-instance override → bubbleGraph re-types in place → existing triggerApartmentLayout re-runs → layout updates) + C52-EDITABLE-BUILDING-GRAPH contract CANONICAL + ADR-0061 ACCEPTED. baseline-identity invariant test-guarded. ai-host 1990/1990, editor changed-files tsc clean. (Superset of v53 — push supersedes the in-flight v53-retry.)
# deploy-marker: v55 — post-v54 batch: A.21.D44 (parcel-fill hidden in 3D) + A.26.1-.5 editable Living Graph (BIM 2/3, C52+ADR-0061) + A.21.D45 window corner setback + A.21.D47 door minimums + A.21.D48 floor-finish-on-slab + A.21.D49 real BIM model on 3D tiles (glTF). ai-host 2006/2006, editor changed-files tsc clean. (D46 corridor reverted — has an every-room-access regression, being re-done.)
# deploy-marker: v55-trigger — explicit deploy (founder opened git). Ships D44 + A.26.1-.5 + D45 + D47 + D48 + D49. ai-host 2006/2006.
# deploy-marker: v56 — post-v55 batch: A.21.D46 corridor narrow-strip (hard no-seal) + D51 picker modal 90%+legend + D52 STAIR-CENTERS-IN-REAL-HOUSES fix (shell-cull 150mm jitter tolerance) + D53 perimeter diff-thickness L-corner fill + D54 globe heading pin + Forma-context regression (bbox dial-back) + layout-algorithm reference doc. ai-host 2018/2018, geometry-wall 43/43, editor changed-files tsc clean.
# deploy-marker: v56-trigger — explicit deploy (founder go). Ships D46 corridor + D51 modal + D52 stair-not-central + D53 perimeter joins + D54 globe/Forma + algorithm doc.
# deploy-marker: v57 (staged, push on founder go) — A.21.D55 daylight-in-every-room (wet-room obscure windows + daylightReach objective) + A.21.D56 GLB exporter world-transform bake (precise globe location) + A.21.D50 Forma massing perimeter ring + translucent glazing. ai-host 2024/2024, editor+file-format changed-files clean.
# deploy-marker: v58 (founder go) — ships the 17 held v57 commits (D55 daylight + D56 GLB world-bake + D50 Forma ring + D57 corridor-ends-at-last-door + D58 self-intersect room repair + D60 ortho boundary draw + D61 perimeter corner flush + A.27 spec doc) PLUS A.27 Phase 2 D-TGL layout quality: F1-2 kitchen↔corridor pref 0.3→0.6, F3 §MASTER-SURPLUS (master visibly larger than other bedrooms), F2 §HALL-ENTRANCE-FACE soft gate. ai-host 2038/2038.
# deploy-marker: v59 (founder go) — A.27 Phase 3 §DOOR-APPROACH-QUALITY (centre doors on the longest clear wall-run) + Phase 4 §ADJACENCY-SORT (pre-placement zone ordering by adjacency preference, +3 tests) + demo-prep docs (tracker §22 founder queue: DEMO-1/2/3, BND-90, FORMA-CTX, FLR-VIEWS, VIEW-ZOOM, GIS-LOC + §22.4 observed bugs; residential demo script). ai-host 2041/2041. Engine-only + docs — no editor build change.
# deploy-marker: v60 (founder go) — EDITOR fixes for two HIGH demo blockers: §GIS-LOC (house seated on real ground, not a tile-building roof — Cesium 3D-tiles min-over-plot terrain sample) + §VIEW-ZOOM (auto-frame the geometry on 3D entry; no manual "Home"). Both typecheck-clean for their files; need prod verification (Cesium/3D is browser-only, localhost dev unusable). + tracker §22.6 demo code-audit + §22.7 rotated-plate wall-join cascade diagnosis (WJ-SKEW/ROOM-MERGE/WIN-DROP/STAIR-OFF-SHELL/HALL-NO-ENTRANCE — queued, skewed-plate cascade).
# deploy-marker: v61 (founder go) — MULTI-AGENT fix wave for the rotated-plate prod-test defects (§22.8): §WJ-SKEW (weld tol 0.05→0.20 m closes the resolver-cluster gap → fixes ROOM-MERGE + WIN-DROP at the root) + §STAIR-OFF-SHELL (snapRectInsidePoly re-validates the stair core vs the rotated shell polygon, D18-safe) + §HALL-NO-ENTRANCE (entrance door restricted to a hall-fronting shell wall) + §BND-90 (orthogonal boundary lock now opt-in, first line free). ai-host 2041/2041, editor typecheck clean. + house-generator deep reference + audit (HSE-AUDIT-1..5) in LAYOUT-GENERATION-ALGORITHM.md.
# deploy-marker: v62 (founder go) — §FLR-VIEWS (auto floor-plan ViewDefinition per generated upper storey — bus view.createDefinition, skip ground + dedupe) + §HSE-AUDIT-1 fix (perStoreyLayout STRICTLY index-aligned with storeys via null placeholders → no mis-paired floors on a blank storey; HouseLayoutResult.perStoreyLayout now (ScoredLayoutOption|null)[]). ai-host 2041/2041, editor typecheck clean. NOTE: re-test on a HARD-RELOADED bundle — v61's rotated-plate fixes (WJ-SKEW/STAIR/HALL/windows) are confirmed live (Actions success @12:27Z); a stale SW cache shows the OLD layout.
# deploy-marker: v63 (founder go) — §DEMO-2 the hero beat: the layout-option modal now shows each option's LIVING GRAPH (room-adjacency bubble diagram) via a per-card Plan/Graph toggle — read the graph BEFORE committing geometry. New pure SVG renderer layoutBubbleGraph.ts (node per room at centroid, edge per adjacency, occupancy-coloured; centroid→polygon→ring fallback). Inert graph (no click-through to "Use this layout"). Editor typecheck clean for all touched files.
# deploy-marker: v64 (founder go) — DEMO COMPLETION: §WJ-SKEW-2 (weld 0.45 m — CONFIRMED fixed: founder's 38.5°-rotated plate logged ZERO self-clusters + 9 rooms vs the old merged blob) + §DEMO-1 (guided onboarding skips the persona/role step) + §SITE-PANEL-UI (Forma site-analysis panel openable via ☀ Analysis toolbar toggle + closeable via header ✕, state persisted). ai-host 2041/2041, editor typecheck clean. Demo path end-to-end: House→map+boundary→option modal w/ Living Graph→clean layout→3D tiles→climate panel. CORRIDOR-NO-TRIM diagnosed + deferred (structural single-rect-carve identity — see §22.11).
# deploy-marker: v65 (founder go) — demo polish: §DEMO-3 ("Regenerating layout…" brand-#6600FF toast on live area/type edits, covers the 450ms debounce, dismissed on apartment.layout-executed) + §BUG-ANNO-DRAG (room-tag drag in plan view threw UPDATE_ANNOTATION CommandBusError → registered the missing bridge forwarding the legacy command to commandManager; the move now persists w/ undo). Editor typecheck clean for both files.
# deploy-marker: v66 (founder go) — DEEP RE-DIAGNOSIS after v65 prod test (all v60-65 layout fixes were DEPLOYED but INEFFECTIVE — wrong root causes). 4 multi-agent-verified REAL root causes: §CONSENSUS-PROXIMITY-GUARD (WallJoinResolver was trimming partition endpoints to an AVERAGED consensus of DISTINCT junctions swept together by the ≤1.0m cluster radius >> 0.45m weld band → dragged partitions ~1m off the perimeter → merged rooms; now skip-trim when >min(snapRadius,0.45m) from consensus, defer to pair-wise pass — THE keystone for "walls don't reach perimeter / Living+Bedroom+Corridor merged blob / no distinct hall") + §STAIR-RUN-BOUND (reserved I-core was 3.0m deep but the real flight runs risers×tread≈4.59m → stair overran the cell past the shell; reserved depth now sized to totalRisers×0.27+margin so snapRectInsidePoly bounds the GEOMETRY not a too-small proxy) + §BEIGE-WALL-FIX (instanced simple-wall path defaulted to #d4c5b0 beige vs the mesh path's white 0xe8e8e8 → join/opening-free ground walls rendered tan; unified to white) + §WINDOW-VOID-FIX (interior-wall windowOpeningCommands were BUILT but never dispatched → frame w/o hole; now punched through the same CreateWallOpenings batch + host walls rebuilt). ai-host 2041/2041, all touched files typecheck-clean. + FORMA-CTX road/pedestrian context centre-lines on the Cesium globe (keyless Overpass). Apartment-executor window mirror + L/U stair overrun = follow-ups.
# deploy-marker: v67 (founder go) — v66 prod test: wall colours FIXED ✓, windows render w/ holes ✓, rooms went from 1 merged blob → 6 distinct (§CONSENSUS-PROXIMITY-GUARD helped). Founder verdict: room-PLACEMENT CONCEPT is still wrong (entrance door missing, hall merged w/ bathroom, living merged w/ bedroom, stair still slightly proud, residual cut-off walls). This deploy ships the requested SPEC-ROOM-PLACEMENT-RULES.md (authoritative placement-composition spec: privacy gradient + entrance sequence P1-P4 + connectivity matrix + door rules D1-D3 + NEW D-PROX entrance→core distance metric + window rules W1-W4 + the placement pipeline + a 9-gap current-state audit G1-G9 with hard/soft validation gates + acceptance criteria) + the typecheck-debt cleanup (envDrivers unused var + 5 ai-host test annotations, zero runtime change). NO new engine fix this round — the placement gaps G1-G9 are the next work block, governed by the new spec. Roots: G4 entrance-door-overlaps-shell-window (skipped), G5 door-on-too-short-wall, G1 residual WJR self-cluster on upper floor.
# deploy-marker: v68 (founder go) — REAL engine fixes after v66/v67 prod test (v67 was docs-only). THREE layout fixes: §ENTRANCE-DOOR-CLEAR (G4 — the entrance door was centred on a shell wall already holding a window → overlap → skipped → "no entrance door"; now placed in a CLEAR gap avoiding window spans, with fallback to another hall-fronting wall; +2 tests) + §DOOR-LIVE-CLAMP (CRITICAL accessibility — interior doors, incl. the BATHROOM, were dispatched against the PRE-trim wall, so a WallJoinResolver trim made the door "extend beyond wall length" → SKIPPED → sealed room; now re-clamped to the LIVE wall span exactly like the entrance door, so every room keeps its door) + §SELF-CLUSTER-FLOOR (G1 — the engine kept 0.05–0.5 m wall stubs that the resolver self-clustered + dropped → missing walls + "Kitchen/Hall merged"; engine degeneracy floor raised 0.05→0.50 m to match the resolver's 0.5 m cluster band). ai-host 2043/2043 green, editor typecheck clean. + full §22.13 audit of the prod log on the master tracker (G1-G13: the BIG new root is G12 over-program room-drops — footprint too small for the brief + the target-area slider isn't wired G13). Carries the earlier typecheck-debt cleanup. Still OPEN: G12/G13 (over-program + brief wiring), G8 stair U/L overrun, G10 colour-bleed, G11 window orientation, G9 room naming, G1 residual hairline trims.
# deploy-marker: v69 (founder go) — G12 keystone nudges + RICH §DIAG instrumentation. v68 verdict: ground-floor walls now ALL GOOD ✓, stair off one side only ✓; remaining = generic "Room 00-00x" voids + most rooms lack WINDOWS (both downstream of G12 over-program drops) + first-floor walls + "Entrance Hall" wrongly on floor 1. Fixes: §AREA-AGREEMENT (don't cap presentedArea below the true plate for an enriched storey → rooms sized to the real plate not a starved budget) + §STAIR-FRAGMENT (DOMINANT_FRACTION 0.55→0.45 so the corridor-spine carve fires on a stair-fragmented plate; never worse on drops). ai-host 2043/2043 green. + §DIAG logs (founder request, for console-paste diagnosis): §DIAG-STOREY (true-vs-presented area + program + stair), §DIAG-RECTS/§DIAG-BRANCH (decomposition fragments + carve-vs-generic pick + dropped types), §DIAG-ROOMS (per-room type/area/windowCount + ⚠ WINDOWLESS habitable list). + governance: founder's Room Layout Engine SPEC v2.0 noted as superseding SPEC-ROOM-PLACEMENT-RULES v1.0 (dining+study windows mandatory, every habitable room ≥1 window, ground-only entrance hall, aspect/min-width, furniture-fit, walking-distance proximity, per-room solar). Keystone G12 complete cure (stop stair fragmenting the plate, tied to G8) + G14 upper-floor landing-not-hall queued; next console paste's §DIAG lines will pinpoint.
# deploy-marker: v70 (founder go) — THE KEYSTONE ROOM-MERGE FIX (§CONSENSUS-ON-CENTRELINE). The v69 §DIAG logs PROVED the engine is now correct (8 distinct rooms, doors+windows, ZERO drops — G12 fixed) but the editor's WallJoinResolver was MERGING them ("Living/Kitchen/Dining/Hall 259.8m²"): the ~15 §MULTI-CLUSTER primary=0 trimmed=3 junctions trimmed interior partition endpoints to the AVERAGED consensus, which sits 25-40mm OFF each wall's centreline (centroid of a triangle of distinct pairwise crossings), ROTATING each wall off-axis so RoomDetection's boundary trace leaked between rooms. Fix: project the consensus onto each wall's OWN centreline → walls stay exactly on-axis (zero rotation), the joining ends land ≤60mm apart and RoomDetection._snapNearbyCorners(0.30) fuses them → junction seals, rooms stop merging. Byte-identical on true star junctions; geometry-wall 45/45 green. + deeper §DIAG instrumentation across the engine (DIAG-ALLOC/ENRICH/BUBBLE/ENUM/WINNER/WIN/WIN-DIST/DOORS/STAIR — ai-host 2043 green) so the next console paste shows program split, room targets, per-room window why-zero, door passes, stair corner-vs-mid-edge. + MASTER algorithm/orchestration ruler doc aligned to the C-contract hierarchy + ADRs + the layout spec family. Founder SPEC v3.0 (GIS/climate/acoustic/multi-storey) noted as the new governing target.
# deploy-marker: v71 (founder go) — RE-DEPLOY of v70 (the §CONSENSUS-ON-CENTRELINE keystone room-merge fix). v70's run #151 FAILED on infrastructure, not code: local build gates all GREEN (check:isolation ✓, tsc --skipLibCheck ✓ 0 errors, commandmanager ✓, geometry-wall 45/45, ai-host 2043/2043) — the Docker build OOM'd / flyctl flaked (known pattern; a GH "Re-run" replays the OLD commit so a FRESH push is required). This fresh push re-attempts the SAME keystone fix (project the §MULTI-CLUSTER consensus trim onto each wall's own centreline so interior partitions stay on-axis and stop merging the social rooms) + the deeper §DIAG engine instrumentation. No code change vs v70 — pure re-trigger.
# deploy-marker: v72 (founder go) — IMPL F1 (ADR-0062 D8): §ENRICH-DENSITY-CAP — cap the bedroom enrichment by plate CIRCULATION capacity (~45 m²/bedroom proxy for window-facade), never below the brief. Fixes the §DIAG-ENRICH runaway the founder's console audit exposed (a 1-bed brief grown to 5 bedrooms on a 176 m² plate → every candidate topoOK=false / all rooms sealed / shipped topologyQuality=0.00). PROVEN in test logs: the upper floor now stays at 2-3 bedrooms and candidates flip from all-invalid to topoOK=true circRouted=true connected=true (x-rev-id weighted 0.700) — so the existing tier-ranking now picks a VALID topology instead of the §CIRCULATION-REROUTE garbage. This single root fix addresses F1 (runaway) + unblocks F2 (valid topo achievable) + reduces PM-0 (upper-floor merge from cramming) + PM-3 (tunnel aspect) + PM-4 (unfurnishable). ai-host 2043/2043 green, byte-identical where the cap doesn't bind. Next queued: PM-0 upper-floor no-merge, F3 habitable window priority, M2 slider-as-intent.
# deploy-marker: v73 (founder go) — IMPL F3 (§WINDOW-HABITABLE-PRIORITY): the shell-window de-overlap now keeps a HABITABLE room's window (living/kitchen/dining/master/bedroom/study) over a WET room's when two rooms' windows conflict on the same shell wall — fixes the §DIAG-WIN trace where an En-suite + Bathroom kept 3 windows each while a Bedroom's ONLY window was deleted. Byte-identical when no cross-priority conflict. + master-tracker §23.7 LIVE implementation status (F1 done v72, F3 done v73, F2 largely satisfied by F1, PM-0 likely reduced by F1 — all to re-confirm on prod via §DIAG paste). ai-host 2043/2043 green.
# deploy-marker: v74 (founder go) — IMPL F2 + PM-5: §TOPO-ROUTED-PREFERENCE (ranking inserts legalRouted + connectedRouted tiers below the clean tiers, so when every candidate fails the shape gate the engine prefers a circulation-ROUTED plan instead of shipping topologyQuality=0.00; D4-safe, never empties the pool) + §SOCIAL-CAVERN-CAP (maxAreaFrac on living 0.32 / kitchen 0.16 / dining 0.16 which had NONE, so a social room cannot stretch into a deep dark cavern on a large plate). Completes the F1/F3/F2 console-post-mortem trio + caps PM-5. ai-host 2043/2043 green, determinism-safe.
# deploy-marker: v75 (founder go) — DIAG §DIAG-LEVELS (house wall-per-level audit, authoritative legacy store vs intended per-storey: flags MISSING=mirror-fail / EXTRA / orphan-level walls) to isolate the founder report "L1 elements appear on Ground". Code-traced verdict: level stamping is CORRECT end-to-end (per-wall levelId preserved through CommandEventBridge + the P2.1 bridge; AddLevelCommand + legacy WallStore share the BimManager singleton) so (A) mis-stamp + (C) mirror-fail are ruled out; strongest candidate is (B) PLAN-VIEW BLEED — the WALL-SLAB-CONTINUITY raise pushes ground SHELL walls to 3.1m, poking into the L1 view band [3.0,6.0], so a wall correctly on Ground renders in the L1 plan (== v74 PM-8 "protruding, visible floor-above"). Diagnostic-only + the pure additive PERIMETER-CLASS classifier (no caller yet, byte-identical). Ship ALONE so the DIAG-LEVELS paste settles A/B/C before any view-side PM-8 fix. ai-host 2048/2048, editor typecheck clean.
# deploy-marker: v75b (founder go) — adds §DIAG-SHAPE alongside §DIAG-LEVELS: classifies the plate the generator receives via the now-LIVE §PERIMETER-CLASS classifier (CONVEX-RECT/POLY/L/T-U/COMPLEX + aspect), logged at house-build start. Tells us per prod test whether poor layouts stem from a hard plate shape (elongated/L → top-down slicing tunnels+merges → needs rectangular-dual solver) or a clean plate the engine still mishandles. classifyPerimeter exported from @pryzm/ai-host (was dead code). Read-only, byte-identical to layout behavior; editor typecheck clean, ai-host source clean (only pre-existing TEST-file strict-null noise remains).
# deploy-marker: v76 (founder go) — FIX §T-JUNCTION-WELD (PM-6 ground merge). §DIAG-LEVELS prod paste CONFIRMED levels are correct (Level 01 live=20/intended=20; Ground EXTRA 4 = the pre-drawn shell walls the intended-count omits) → the "L1 elements on Ground" report is plan-view beyond-linework, NOT mis-assignment. The real defect is the ground MERGE (Living/Kitchen/Dining/Bedroom2 → one 601m² blob); console root §MULTI-CLUSTER trimmed=3 pinned=0 ×12 = interior partition junctions trimmed not pinned → gaps → RoomDetectionEngine floods. ROOT: weldPartitionsToShell snapped endpoint→shell-span + welded endpoint↔endpoint but NOTHING snapped a partition endpoint onto another partition MID-SPAN, so interior T-junctions never sealed. FIX: new Pass 1.5 snaps an endpoint within shellSnapTolM of another partition span INTERIOR exactly onto that centreline → the T closes, resolver sees a pinnable junction. Byte-identical on clean axis-aligned plates (corner hits excluded by TJUNC_MARGIN). ai-host 2051/2051 (+3 tests). Also rides: §DIAG-SHAPE plate classifier (live). Expect on re-test: Bedroom2 splits from the open-plan core, §MULTI-CLUSTER pinned>0, more rooms detected on L0.
# deploy-marker: v76b (founder go) — adds §DIAG-WIN-UNMATCHED breakdown alongside the §T-JUNCTION-WELD merge fix. The windowless-habitable-rooms defect (Bedroom1 ground, Bathroom2+Bedroom4 upper; unmatchedToShell=10/11) has SEVERAL possible causes lumped into one count — this breaks the unmatched total down by CAUSE (hostNotExternal = engine emitted the window on an interior wall → a LAYOUT issue; noShellMatch = matcher tolerance too tight on the rotated plate; cornerFitDrop / cornerClearanceUnfittable / finalInvariantDrop / centreOutOfShellSpan / shellWallTooShort = a corner/span guard too aggressive). The next prod test paste of §DIAG-WIN-UNMATCHED will say EXACTLY which gate to fix instead of guessing. Pure diagnostic (optional reason tally, byte-identical behaviour). ai-host 2051/2051.
# deploy-marker: v77 (founder go) — CRITICAL-LAYOUT batch. §BND-90-DEFAULT-ON: the orthogonal-to-previous boundary lock is now ON by default (was opt-in OFF) + snap band 8°→22° — the upstream root fix, because a freehand plot classifies as T-U-SHAPE (§DIAG-SHAPE) which fragments the stair carve and forces §FEASIBILITY-ALLOC room drops (dining/hall/corridor/bedroom). First edge free at any rotation; every subsequent edge auto-snaps orthogonal → clean rectangular plot; uncheck ⟂ for a non-rectilinear site. §WALL-TOP-AT-SLAB-BOTTOM: ground shell wall head = L1 floor (=slab bottom), not +slab/2 — stops ground walls protruding into the first-floor plan. §DIAG-STAIR: logs the stair core vs shell containment (already proved the stair fragments T-U plates). Rides v76 §T-JUNCTION-WELD (interior-wall merge seal) + v76b §DIAG-WIN-UNMATCHED. EXPECT on re-test with a rectangular plot: §DIAG-SHAPE=CONVEX-RECT → no room drops → clean stair → sealed interior walls (§MULTI-CLUSTER pinned>0). Editor typecheck clean; ai-host 2051/2051.
# deploy-marker: v78 (founder go) — RE-TRIGGER of v77 (last deploy failed on Fly/Docker INFRA, not code: local gates all green — check:isolation ✓, root tsc --skipLibCheck ✓ 0 errors, ai-host 2054/2054, editor typecheck ✓; a GitHub Re-run replays the OLD commit so a FRESH push is the documented fix). Ships everything in v77 (§BND-90-DEFAULT-ON orthogonal boundary + §WALL-TOP-AT-SLAB-BOTTOM + §DIAG-STAIR) PLUS §STAIR-ANTI-FRAGMENT: the stair-placement scorer now penalizes plate FRAGMENTATION so a true CORNER carve (flush to a side wall AND the rear wall = one dominant rect) beats a MID-EDGE one (the back-corner candidate the shell-containment nudge pulled off the corner) — stops §FEASIBILITY-ALLOC dropping rooms (the founder critical "stair conflicts the layout"). Aspect-path only → legacy byte-identical, +3 tests. EXPECT with a rectangular plot (BND-90 default-on): §DIAG-SHAPE=CONVEX-RECT → §DIAG-STAIR pos=CORNER → no room drops → sealed interior walls.
# deploy-marker: v79 (founder go) — DOC-AUTO analysis/plan + Node-24 checkout fix. Ships: (1) docs/03-execution/plans/AUTO-DOCUMENTATION-SHEETS-PLAN.md + master-tracker §24 — the founder auto-documentation-sheets brief (per-level plan sheets, building elevations, per-room interior elevations + cropped plans, wall/door/window SET-OUT plan with core dimensions, all auto-placed on PDF-ready sheets) analysed via a 4-agent codebase+contracts sweep: ~80% of the substrate already EXISTS (Sheet/Viewport/TitleBlock schemas, drawing-primitives, plugins/sheets + book-exporter, DrawingSetStore, sheetToPdfBytes, ViewDefinition + §FLR-VIEWS per-storey plan views, EdgeProjector elevation/section, CreateElevationMark room auto-scope, 6-kind DimensionString + 5-mode produceDimensions, grid/datum injection); the gap is the ORCHESTRATION layer (DS1-DS6). Governance: complies C24/C29/C30/C34 + SPEC-04/30; needs new C24.1. NOT started (pending go). (2) deploy-fly.yml actions/checkout@v4 → @v5 (Node 24; clears the Node-20 deprecation warning). NO production code change — docs + CI only; all prior local gates green.
# deploy-marker: v80 (founder go) — DOC-AUTO DS5: set-out auto-dimension mode (core dimensions). Pure additive producer mode in geometry-kernel: emits the builder set-out plan dims (every wall opening = offset-from-host-wall-start + width, cross-element refs; plus each wall overall length). Adds the 'set-out' DimensionAutoMode to the schema enum + hostWallId to DoorLike/WindowLike (read only by set-out). Existing 5 modes byte-identical (new switch case only). +6 tests; geometry-kernel 600/600; schemas typecheck clean. First DOC-AUTO build increment (AUTO-DOCUMENTATION-SHEETS-PLAN.md §5 DS5). Backend-only, no UI consumer yet.
# deploy-marker: v81 (founder go) — 3 parallel-agent fixes + stair doc. (1) STAIR HALF-LANDING INWARD: U-stair second flight/half-landing now folds toward the plate INTERIOR (StairCore.interiorSide threaded from the position kind: left-flush→+x, right-flush→−x, back→−z; executor _buildFlights uses it for the perp offset). Byte-identical for I/L + axis-aligned. (2) §WJ-SKEW-3 rotated-plate MERGE: partition weld tol 0.45→0.60m so Y-junction endpoints on strongly-rotated (~−44°) plates fuse (closes §MULTI-CLUSTER pinned=0 trimmed=3 → no room merge); no-op on axis-aligned; self-endpoint guard + min-len drop intact; safe factor-2 margin below corridor 1.2m. (3) DOC-AUTO DS1: pure buildSheetFromViews factory (drawing-primitives) — lays N views on a PDF-ready sheet (scale-fit grid), sibling of buildSheetFromRooms. + docs/04-reference/STAIR-CREATION-PIPELINE-AND-ANCHOR-ANALYSIS.md confirming the founder hypothesis (systematic cornersInShell=1/4 = start-corner anchor + fixed-direction growth, not random; cure = anchor inward + validate full footprint in world frame). ai-host 2060/2060, drawing-primitives 138/138, editor typecheck clean.
# deploy-marker: v82 (founder go) — §STAIR-CONTAIN: the deeper stair cure (founder hypothesis confirmed). The systematic "stair pokes OUT of the rotated shell" defect (§DIAG-STAIR cornersInShell=1/4 every rotated run) is fixed by validating the FULL world-frame stair footprint (all flights+landings via computeStairFootprintRect) against the shell polygon and, when any corner is outside, NUDGING THE WHOLE BODY INWARD (along the interiorSide direction) until contained — BEFORE dispatching CreateStairCommand. Pure decision = computeInwardContainmentOffset (ai-host, +5 tests). Byte-identical when already contained (offset {0,0}) → axis-aligned plates unchanged. Builds on v81 stair half-landing inward; together = anchor-inward + full-footprint-containment (per docs/04-reference/STAIR-CREATION-PIPELINE-AND-ANCHOR-ANALYSIS.md §3). ai-host 2065/2065; editor typecheck clean.
# deploy-marker: v83 (founder go) — DOC-AUTO DS3: pure building-exterior elevation marks. computeBuildingElevationMarks(footprint) → the 4 N/S/E/W elevation marks (anchor OUTSIDE each façade + INWARD facing normal + label) auto-derived from the building footprint bbox — the auto-PLACEMENT piece the existing elevation projection (EdgeProjectorService + CreateElevationMarkCommand) was missing. Pure/deterministic, +5 tests, exported from @pryzm/ai-host. Editor wiring (mark→elevation view→sheet) is a later step. ai-host 2070/2070. AUTO-DOCUMENTATION-SHEETS-PLAN.md §5 DS3.
# deploy-marker: v84 (founder go) — DOC-AUTO DS4 pure foundation: per-room documentation. roomCropRegion(roomPolygon, margin) → the plan CROP region (room bbox + margin) for a per-room cropped plan view; computeRoomInteriorElevationMarks(roomPolygon) → 4 INTERIOR elevation marks (viewer at the room centroid looking OUTWARD at each N/S/E/W wall — the inverse of building-exterior elevations). The auto-placement pieces the per-room documentation needs (the projection + room-aware elevation machinery already exist). Pure/deterministic, +7 tests, exported from @pryzm/ai-host. Editor wiring (crop→plan view; marks→elevation views; →per-room sheet) is a later step. ai-host 2077/2077. AUTO-DOCUMENTATION-SHEETS-PLAN.md §5 DS4.
# deploy-marker: v85 (founder go) — DOC-AUTO DS6: documentation-set ORCHESTRATION keystone. planDocumentationSet({levels,rooms,footprint}) → the ordered, NUMBERED sheet plan (A-1xx level plans · A-2xx the 4 building elevations · A-3xx per-level set-out plans · A-4xx per-room sheets = cropped plan + 4 interior elevations), consuming DS3 (building elevations) + DS4 (room crop + interior elevations). The deterministic "what documents to make" plan the editor executor will turn into real ViewDefinitions + Sheets (DS1 buildSheetFromViews) + PDF (DS2). Pure, +5 tests. This COMPLETES the pure DOC-AUTO pipeline (DS1+DS3+DS4+DS5+DS6 cores all shipped + tested); remaining = editor WIRING (executor + sheet.export.pdf) + the C24.1 contract. ai-host 2082/2082. AUTO-DOCUMENTATION-SHEETS-PLAN.md §5 DS6.
# deploy-marker: v86 (founder go) — docs: C24.1 Auto-Documentation Sheets Protocol (DRAFT). The AUTO sibling of C24 governing automatic generation of a documentation SET (per-level plans · 4 building elevations · per-level set-out plans · per-room cropped plan + interior elevations) auto-placed on numbered A-1xx/2xx/3xx/4xx sheets, PDF-ready. Pins: PURE deterministic DS1-DS6 decision layer (P5) · executor mutates only via the bus (P6) · spans (P8) · coverage rule (no silent room omission) · rule-based label placement (force-directed deferred) · C34 styles · C29 vector PDF · idempotent re-gen. Registered in contracts README. Unblocks the DOC-AUTO editor wiring (docSheets executor + sheet.export.pdf). Docs only.
# deploy-marker: v87 (founder go) — 2 parallel-agent fixes for the prod feedback. (1) §WJ-SKEW-4 ROOM-SAFE WELD: fixes the small-room MERGE regression (Bathroom+Hall, Kitchen+Corridor "sharing the space missing a wall"). The §WJ-SKEW-3 0.60m bump was grabbing BOTH ends of a SHORT small-room partition and pulling them to different cluster centroids → shortening/mis-placing the sealing wall → merge. Fix: a per-endpoint room-safety guard — a clustered endpoint only collapses to its centroid if that keeps its partition ≥0.8m USABLE and moves it ≤ the weld tol; else it keeps its shell-snapped position. Tolerance settled 0.60→0.50 (guard, not raw radius, handles the near-45° Y-junction). Byte-identical on axis-aligned. ai-host 2083/2083. (2) §ROOF-LEVEL (founder request): the roof now lives on its own minted "Roof" level above the top storey (world-Y unchanged: roof-level elevation = old cap height, baseOffset 0) + a dedicated "Roof Plan" view — so the roof no longer renders in the first-floor plan. Editor typecheck clean.
# deploy-marker: v88 (founder go) — AI "Documentation" command. New command family in the AI Design Assistant panel (next to Create/Modify/Visibility/Query): "Generate documentation set" (direct action, C17 CB-8 style) runs generateDocumentationSet → gathers the live levels + detected rooms + footprint, computes the DS6 planDocumentationSet (numbered A-1xx/2xx/3xx/4xx), and creates the per-level PLAN + per-room CROPPED-PLAN ViewDefinitions via view.createDefinition (P6); toasts the plan summary (building elevations + set-out + PDF wiring queued per C24.1 §4). + Floor-plan-per-level / Building-elevations / Export-PDF query entries. Editor typecheck clean.
# deploy-marker: v127 (founder go) — IT-4e: REMOVE the PROJECT-BRIEF onboarding panel (founder 2026-06-11: "First screen shot panel needs to go - delete project brief … I believe everything is covered on the modal now"). The live "Design your house — live" 3-pane modal now owns the whole program brief (floors/bedrooms/bathrooms/sizes/design sliders + the node inspector), so the separate RAC "PRYZM Onboarding / PROJECT BRIEF" panel is redundant. PlatformRouter.showOnboarding: for the GUIDED path (authed user + a generator-ready seeded typology from the New-Project modal) SKIP building RACChatbotPanel and emit pryzm:onboarding-brief-ready directly with a default brief (role architect + seededTypologyId + seedMetadata) → the proven pipeline (create project → location → draw-or-skip → generate → live modal) runs unchanged; the user adjusts the program live in the modal. The standalone anonymous "Build something" RAC entry (not authed / no seed) STILL shows the panel (it owns lead capture + the auth handoff). Reversible + scoped (authed+seeded gate). Editor typecheck clean; RACChatbotPanel 20/20 (untouched).
# deploy-marker: v126 (founder go) — §PROJECT-NORTH (ADR-0070 Model B) RIGID-TRANSFORM-LAST weld, default ON (founder's Revit "project north vs true north" root-fix for the 43.2°-rotated plate). New pure `projectNorthWeld.ts` (deriveProjectNorthFrame θ+pivot · rectifyShellRing snap-near-axis-to-exact · projectNorthWeld = de-rotate −θ → rectify → weld at original-tight tol → re-rotate +θ) wired into HouseLayoutExecutor ground WELD-FALLBACK + §UPPER-SHELL-WELD behind `window.__pryzmProjectNorth` (default ON; `=== false` = legacy). 45° plate maxSeal 0.355→0.000 m, 0 open seams (angle sweep 10/30/43.2/60/75° all ≤0.30 m). ai-host 2242/2242 (+8); θ=0 + flag-OFF + axis-aligned BYTE-IDENTICAL; rigid +θ preserves wall length/coincidence (asserted). HONEST SCOPE (verified at θ=0): this dissolves the geometric seam residual → the rotated-plate ROOM-MERGE ("Room NN-xxx" blobs) ONLY; the sealed-room / §TOPO-HARD-REJECT [circulation] / 1-door-upstairs verdicts reproduce IDENTICALLY axis-aligned → they are pre-weld ENGINE layout-quality bugs (door placement / topo gate), tracked at §55, NOT fixed here. ADR-0070 Consequences amended with the proof. Pre-existing unrelated TS error (__wallRebuildControl) not introduced.
# deploy-marker: v125 (founder go) — §54 living-graph node INSPECTOR cards (founder 2026-06-11: "each node … work like an individual card the user can select - interrogate - means a panel will pop up with information - dependencies - adjacency - circulation"). Clicking a Miro-canvas node now opens a richer CARD: a read-only INSPECTOR section (INFORMATION name/type/area · DEPENDENCIES type-derived role · ADJACENCY the room's adjacentTo as chips · CIRCULATION derived "On circulation ✓ via <Corridor/Hall>" / "Not ✗") ABOVE the existing Area·Type·Floor·Connect edit controls — all drag/connect/move-between-floors + selection-highlight preserved. Pure `buildNodeInspectorHtml(room, storeyRooms)` in houseModalHtml.ts (8 new tests, 16/16 total); modal caches `_options` + looks the LayoutRoom up by name in `perStoreyLayout[srcStorey].rooms`. Editor typecheck zero-new-errors. Governance: tracker §54, SPEC-DYNAMIC-PROGRAM-CANVAS §5.9.
# deploy-marker: v124 (founder go) — §HOUSE-GROUND-PUBLIC-SET — fill the multi-storey GROUND plate (founder "blank areas / if I add more rooms still those areas are not being filled in"). Root (tracker §52.6.1): a multi-storey GROUND floor was FLAT at grossTarget≈91 for every plate (fillGroundPlate collapsed to 1 guest bedroom) → §HOUSE-MAX-CAP clamped presented=min(plate,grossMax=218) → 32 m² blank @250, 71 @289; the Bedrooms stepper grows UPPER storeys, never the ground. Fix (engine, packages/ai-host only): new ApartmentProgram.includeStudy/includeUtility flags → bubbleGraph mints a study/utility room linked OFF THE CORRIDOR SPINE (corridor-served by the rules DB → carve/comb reaches them → they NEVER seal) → houseEnvelope.storeyRoomTypes mirrors them so the band's grossTarget grows → fillGroundPlate OR-ins study (≥200 m²), utility (≥240), a 2nd corridor-served guest bedroom (≥270) on a large multi-storey ground. Blank eliminated at every plate size (289: 71→0); small plates + axis-aligned + apartment path BYTE-IDENTICAL (flags absent). ai-host 2234/2234 (+6, incl. a new §HOUSE-GROUND-PUBLIC-SET no-sealed-room hard-gate on a real 289 m² 2-storey run); §HALL-SINGLETON + I1/I3/I4 hold. Independent of + complementary to the PROJECT-NORTH rotated-plate root-fix (ADR-0070).
# deploy-marker: v123 (founder go) — §3PANE iteration 4b/4c: DRAG A NODE on the Miro canvas (founder 2026-06-11: "move a bedroom from first floor to ground floor … the plan view will update automatically … also user can connect spaces … a link will create connections via circulation … now we need a door"). HouseLayoutModal._wireMiroNodeDrag: press+drag a living-graph node (ghost label follows the cursor, drop targets highlight); DROP onto the OTHER storey lane → setRoomFloorOverride (the v120 floor-move seam) → regen → plans update; DROP onto ANOTHER node → addRoomAdjacency (the v121 §ROOM-ADJACENCY seam; engine adds a door IFF the pair is permitted) → regen. Sub-threshold press falls through to the node CLICK (selection + C52 editor); a completed drag sets _suppressNodeClick so it doesn't also open the editor. Screen-space hit-test (elementFromPoint) → independent of pan/zoom. houseModalHtml.liveModal 7/7, editor typecheck zero-new-errors. Next: IT-4d per-ground-room resize, IT-4e retire onboarding Project-brief panel, + PROJECT-NORTH/TRUE-NORTH (founder root-fix for the rotated-plate residual). Governance: SPEC-DYNAMIC-PROGRAM-CANVAS §5.6/§5.8, C52 E3, ADR-0061/0069.
# deploy-marker: v122 (founder go) — §3PANE iteration 4a: UNIFIED MIRO/MURAL CANVAS (founder 2026-06-11: "both [floor graphs] on the same canvas … like mural/miro … zoom in and out … move the nodes"). The CENTER pane is no longer two stacked graph boxes — it is ONE pan/zoom WORLD (buildHouseMiroCanvasHtml) holding both storeys' living graphs as labelled LANES (Ground→top), each tagged data-storey-index (the cross-floor move handle for IT-4b). Wired in HouseLayoutModal._wireMiroCanvas: wheel-zoom toward the cursor, drag-the-background pan (skips nodes + toolbar so node-click selection/C52-editor still fires), +/−/Reset toolbar; the {x,y,scale} transform is PRESERVED on the instance so a live regen keeps the user's zoom/pan. Node click → selection-sync + the C52 inline editor (Area·Type·Floor·Connect) unchanged. houseModalHtml.liveModal 7/7 (new IT-4 Miro assertion), editor typecheck zero-new-errors. Next: IT-4b node-drag-between-floors, IT-4c draw-connection-line→door, IT-4d per-ground-room resize, IT-4e retire the onboarding Project-brief panel. Governance: SPEC-DYNAMIC-PROGRAM-CANVAS §1.1 + R-D, ADR-0069.
# deploy-marker: v121 (founder go) — §3PANE iteration 3b: CONNECT TWO ROOMS (SPEC-DYNAMIC-PROGRAM-CANVAS §5.6, R10, C52 E3, founder "user can connect the rooms and the plan view updates"). End-to-end seam: (1) ENGINE — new `ApartmentProgram.roomAdjacencyByName?: ReadonlyArray<[nameA,nameB]>`; the bubble graph (`bubbleGraph.ts`) adds a DESIRED `door` edge for each pair IFF the pair is PERMITTED (`doorAllowedBetween` — a forbidden pair e.g. bedroom↔bedroom is ignored + logged §ROOM-ADJACENCY skipped, never forced) and not already linked. Empty ⇒ no extra edge ⇒ byte-identical (ADR-0061 I2; ai-host 2228/2228 green). (2) STASH — new `activeRoomAdjacencyOverrides.ts` (addRoomAdjacency/get/clear, order-independent dedup). (3) CONTROLLER — `_mergeOverrides` merges `roomAdjacencyByName` into the program (preview + build), preserving baseline identity. (4) UI — the inline node editor gains a "Connect to" select listing the OTHER rooms on the SAME storey; choosing one writes addRoomAdjacency(room, target) → regen → the engine prefers a layout where they share a door + the plan updates. The node editor is now Area · Type · Floor · Connect-to — all flowing through the live pre-execution regen. houseModalHtml.liveModal 6/6, ai-host 2228/2228, editor typecheck zero-new-errors. Governance: SPEC-DYNAMIC-PROGRAM-CANVAS §5.6, C52 §2.2 (E3), ADR-0061.
# deploy-marker: v120 (founder go) — §3PANE iteration 3c: MOVE A ROOM BETWEEN FLOORS (SPEC-DYNAMIC-PROGRAM-CANVAS §5.3, R5, founder "drag a room from graph first floor to ground floor"). Two parts: (1) §26 XE — `HouseLayoutController._mergeOverrides` now MERGES `roomFloorByName` (via `getRoomFloorOverrides()`) into the program, so the cross-storey move stash finally flows to BOTH preview (`_computeVariants`) and build (`_build`); the engine half (XA `allocateProgramToStoreys` override-aware + `applyFloorOverrides` parsing `storey:<s>/<name>`) + the stash already shipped — this was the missing wire. Empty stash ⇒ same program ref ⇒ byte-identical baseline (C52 I2). (2) UI — the per-storey panes now carry `data-storey-index`; clicking a room node/plan derives its SOURCE storey + the storey count, and the inline node editor gains a FLOOR selector (Ground / Level 1 / …, only when >1 storey); choosing a different floor writes `setRoomFloorOverride("storey:<src>/<roomName>", target)` → regen → the room re-allocates to that floor + the panes rebuild. (A functional move-between-floors via the editor; full drag is later polish.) houseModalHtml.liveModal 6/6, editor typecheck zero-new-errors. Governance: SPEC-DYNAMIC-PROGRAM-CANVAS §5.3, C52 (roomFloorByName override family), ADR-0061, §26 XE.
# deploy-marker: v119 (founder go) — §3PANE iteration 3a: SELECTION-SYNC (SPEC-DYNAMIC-PROGRAM-CANVAS §5.8, R12, founder "when the user selects a node the room highlighted on the modal"). Clicking a living-graph node OR a plan room polygon (both already carry `data-room-name` — the same minted room name) now (a) HIGHLIGHTS that room across ALL panes (the CENTER graph node + every LEFT plan polygon get `.hlm-selected` → #6600FF outline) and (b) still opens the inline C52 area/type editor. New `HouseLayoutModal._highlightRoom(name)` (read-only projection — no program edit, clears on regen) + the click handler now matches `.alm-graph-node, .alm-room-polygon` + CSS. Remaining IT-3 (drag-to-add palette · drag room between storeys · connect-rooms adjacency) + IT-2.5 (per-storey bedroom seam) follow. houseModalHtml.liveModal 6/6, editor typecheck zero-new-errors. Governance: SPEC-DYNAMIC-PROGRAM-CANVAS §5.8, C52, ADR-0061 (v112 §BUBBLE-SELECT-HIGHLIGHT lineage).
# deploy-marker: v118 (founder go) — §3PANE iteration 2 (SPEC-DYNAMIC-PROGRAM-CANVAS §5.2). (1) Per-room SIZE SLIDERS: the tools-rail per-RoomType size controls (Living/Kitchen/Dining/Bedroom/Master/Bath) are now RANGE SLIDERS with a live `<output>` m² readout (value 0 ⇒ "auto"/engine default, >0 ⇒ roomAreas[type] clamped to the type minimum) — the founder's "increase size of room with a slider". Same `area_t_<type>` name so the form reader is unchanged (0/blank ⇒ no override). (2) LEVEL-STEPPER LIVE + RESULT REFRESH: the Floors stepper already regenerates → the LEFT/CENTER panes rebuild; now `refresh()` ALSO rebuilds the RIGHT-rail result summary (`buildHouseResultHtml`, extracted + exported) so the score / storey-count / Execute update live when you change levels or a slider. Live slider readout folded into the form-input handler (immediate, before the debounced regen). Deferred to IT-2.5: per-STOREY bedroom counts (needs the XFLOOR-GRAPH XA per-storey allocation override seam). houseModalHtml.liveModal 6/6, editor typecheck zero-new-errors. Governance: SPEC-DYNAMIC-PROGRAM-CANVAS §5.2/§5.5, ADR-0069, C52.
# deploy-marker: v117 (founder go) — §3PANE iteration 1: the house "Choose a layout" modal is now the THREE-PANE workspace (SPEC-DYNAMIC-PROGRAM-CANVAS §1.1 / ADR-0069 / C52 §2.2). Replaced the stacked [form-on-top + card grid] body with LEFT = a stacked PLAN view per storey · CENTER = the living GRAPH per storey · RIGHT = the tools rail (the program-edit form + room-type legend + result/score + the single terminal "Use this layout" Execute). The header reads "Design your house — live". Mechanism: new `buildHousePanesHtml(card, thumbs, graphs)` renders the LEFT+CENTER inside the regenerated `[data-role="grid"]` region (so `HouseLayoutModal.refresh()` rebuilds both plans+graphs in lock-step on every live edit), the RIGHT tools rail is static; existing §MODAL-DYNAMIC form-change regen + the graph node-click area/type editor (C52) + Cancel/Esc wiring all preserved. Pre-execution single panel: NO geometry lands until Execute (HouseLayoutExecutor, P6). Brand white+#6600FF (plans = light-purple top border, graphs = #6600FF). `buildHouseCardGridHtml` kept for the §LIVE-MODAL.B/D tests. Iteration 2 (level stepper live + per-storey bedroom controls + size sliders) and iteration 3 (palette drag-to-add · drag-between-storeys · connect-rooms · selection-sync) follow. houseModalHtml.liveModal 6/6, editor typecheck zero-new-errors. Governance: SPEC-DYNAMIC-PROGRAM-CANVAS, ADR-0069, C52, ADR-0061.
# deploy-marker: v116 (founder go) — Phase A: A.33.b keyboard-shortcut cheat-sheet now renders from the canonical registry. The `?`/Shift-/ overlay (apps/editor/src/ui/ShortcutCheatSheet.ts) was wired (initUI.ts:2867) but rendered a HARD-CODED, drift-prone shortcut list that wasn't platform-aware. Rewired to render from `@pryzm/keyboard-registry` via `buildCheatSheetData(detectPlatform())` — the single source of truth (CI-guarded 35 shortcuts in 7 categories), platform-aware glyphs (⌘ on macOS / Ctrl on Win-Linux per Apple/MS style guides), experimental rows muted, ARIA role=dialog. Added `@pryzm/keyboard-registry` as an editor workspace dep. Net-new surface for a DONE backend (the asymmetric Phase-A pattern: foundations ahead of UI). Flips capability 7.1 + tracker A.33.b → ✅ DONE (A.33.c registry-routing audit still pending). Editor typecheck zero-new-errors in the touched file. Governance: C43 §1.3 accessibility, ADR-0061.
# deploy-marker: v115 (founder go) — §VIEW-AUTOFRAME (3D+plan auto-zoom-to-building) + §GLOBE-LOCATION-SPIKE doc. (1) §VIEW-AUTOFRAME (SplitViewManager.ts): on entering the "3D + plan" combined view the main 3D pane was EMPTY until the user manually hit camera>Home/zoom — SplitViewManager.activate() only framed the plan pane's Canvas2D camera (_fitCamTargetToScene), never the main WebGPU 3D viewport, and the one-shot §3D-FRAME-ON-VIEW-SWITCH handler only fires on a 'view-activated' perspective event (split-view emits 'split-view-activated', not that). Fix: activate() now dispatches the existing 'zoom-fit' bus command (deferred ~320ms, re-guarded on this._active) so the building is framed automatically on every 3D+plan entry. Reuses zoomToAll (self-guards on empty scene). Additive — 2D Map / Plan / Site-3D paths untouched. (2) §GLOBE-LOCATION-SPIKE (master-tracker §51): documented the "model not in correct place on the Cesium 3D-tiles globe" root cause — NOT a wrong transform (the LTP-ENU frame is internally consistent: origin=geocoded lat/lon, boundary projected about it, both Forma massing + GLB anchor the same origin; Cesium upAxis/forwardAxis already Y/Z). The real offset is geocode-address-point ≠ true-parcel (~10-15m); fix is architectural (re-anchor ENU on parcel centroid + proj4-UTM rebase at the draw surface) → documented, NOT implemented this pass. Q2≠Q3 (independent). master-tracker §50 (autoframe) + §51 (globe spike) appended. Editor typecheck zero-new-errors. Governance: C04, C19, ADR-0061.
# deploy-marker: v114 (founder go) — modal program-editor + first-floor-bleed + bedroom-ensuite 2-door + Miro-canvas governance. (1) §MODAL-FILL + §MODAL-PROGRAM-EDIT (houseModalHtml.ts/HouseLayoutController.ts/HouseLayoutModal.ts): the house generation modal is now a program editor — opens seeded to the PLATE-FILLED resolved program (no "form says 1 bed, preview shows 4" mismatch), adds a per-room-type size (m²) row + Kitchen toggle, raises Bedrooms 0→8 / Bathrooms 1→4, every edit live-regenerates via the existing §MODAL-DYNAMIC loop; per-room resize writes the C52 roomAreas override. Brand white+#6600FF. (2) §DIAG-WALL-LEVEL (initTools.ts): the "first-floor rooms overlap on the GROUND plan" bug — the §P2.1 wall bridge defaulted a missing levelId to 'L0' (and `?? 'L0'` didn't catch the empty-string CEB emits), filing an upper wall onto Ground's childrenIds so the native plan exporter projected it. Fix: resolve the level ONCE (treat ''/undefined alike), feed the same value to VDT+bimManager+legacy store, and on a genuinely-unknown level SKIP registration + warn (never silently dump onto Ground). (3) §BEDROOM-ENSUITE-2DOOR (programRules.ts/bubbleGraph.ts/wallsAndDoors.ts): a bedroom paired with its own ensuite may now have 2 doors (corridor + ensuite) via a per-instance ensuiteHostId pairing — global rules untouched so no bedroom↔bedroom/over-cap leak, master byte-identical; +3 tests, ai-host 2228. (4) DOCS: SPEC-DYNAMIC-PROGRAM-CANVAS + ADR-0069 (dynamic Miro-style room-card canvas as the primary authoring surface, replacing the static brief; layout/graph/plan are derived projections per ADR-0061) + SPIKE-DYNAMIC-PROGRAM-CANVAS phase-0 plan. NOTE: the §PLATE-FILL "fill the whole plate" engine change is NOT in this build (founder rejected that approach mid-run — deferred to canvas-driven fill). Editor typecheck zero-new-errors. Governance: C52, ADR-0058/0061, C50, ADR-0063/0066, SPEC-ARCHITECTURAL-PROGRAM-RULES.
# deploy-marker: v113 (founder go) — house layout-quality wave (4 fixes, all gated byte-identical for apartment/rectilinear, ADR-0061). (1) §NO-PUBLIC-CARVE (engine, subdivide.ts): THE upper-storey "no corridor, no doors" root cause — trySingleRectCarve hard-required a PUBLIC room, but an upper floor has none (corridor+beds+baths only), so the carve returned null and the whole program squarified with only the master touching the corridor → r2/r3/r4/baths/stair SEALED (roomsWithDoor=3/9, §DIAG-TOPO-GATE failed=[circulation]). Fix: a double-loaded corridor carve when publicRooms=0 — central corridor spine, private rooms combed off BOTH sides, ensuite carved from master → every private room abuts the corridor → doors get placed. + §STAIR-SPINE-TOUCH (enumerate.ts): grow the corridor rect across the empty clearance to touch the minted stair so §STAIR-ROOM-DOOR places the corridor↔stair door (was stair0→NO DOOR). (2) §DIVIDER-RETAIN + §DIAG-SEAL-DROP (editor, HouseLayoutExecutor.ts): the "Room 00-001 68m²" giant-merge root cause was the weld silently DROPPING a collapsed partition that was the only divider between two rooms (invisible to §DIAG-SEAL, which reads the POST-weld set → openSeams=0 yet rooms merge). Fix: retain a collapsed-but-usable divider (orig len ≥0.5m) at its un-welded baseline instead of dropping it; §DIAG-SEAL-DROP now logs every weld-dropped divider (distinguishes weld-drop vs engine never-emitted). (3) §DIAG-ENTRANCE-FIX (editor, houseEntranceWall.ts): boundsHall=⚠ — the resolver's strict 0.2m vertex test fails on a drifted/rotated plate so the main door landed on a neighbour's façade; re-seat the entrance onto the shell wall with the longest hall-frontage edge-overlap (0.65m perp tol to survive plate drift), in a window-clear gap. (4) §ROOF-CONCAVE-DECOMPOSE (geometry-roof, roofDecompose.ts): an L/T/U roof was flat-degraded (founder: "roof in L ends up flat — not good"); now decomposes a rectilinear concave footprint into convex wings and builds a real pitched gable per wing at one slope/eave → a proper pitched L-roof (flat only for non-rectilinear concave, logged). (5) §KITCHEN-LU-PREFER (engine, kitchenLayout.ts): the parametric kitchen now prefers L (2 perpendicular usable walls) / U (3 walls) over a straight I run — perpendicular-arm test via buildChain, U back-wall cap relaxed 3.3→3.6m + area cap dropped, planKitchenRun no longer silently collapses L/U→I; + §DIAG-KITCHEN. ai-host 2225 + 33 kitchen + furnish 85, geometry-roof 36/36, editor typecheck zero-new-errors. Governance: ADR-0061/0063/0066, C53, C11, SPEC-CASA-UNIFAMILIAR-TYPOLOGY, SPEC-ARCHITECTURAL-PROGRAM-RULES, SPEC-FURNITURE-LAYOUT-ENGINE.
# deploy-marker: v112 (founder go) — Living Graph accuracy + interactivity. §DOOR-REFS-CIRCULATION: the Circulation (master) graph now draws an edge for EVERY realised door (the founder's missing Entrance-Hall↔Corridor edge) — was a divergent geometric connectsTo projection that dropped while shared-wall adjacency survived; now every door node's refs → a circulation edge, idempotent with connectsTo. + §DIAG-GRAPH (per-view node/edge counts + doors-without-edge ⚠). §BUBBLE-SELECT-HIGHLIGHT: selecting a graph bubble now highlights that room in the active 2D plan + 3D canvas (applySelection sends the ROOM id as the primary selection through window.selectionBus; was passing only child ids → empty → no highlight). P6/P2-safe (view-state only). Governance: C52, ADR-0058, C04.
# deploy-marker: v111 (founder go) — §ROOF-CONCAVE (L/T/U flat-roof degrade) + §KITCHEN-PARAMETRIC-RUN. (1) §ROOF-CONCAVE: an L-shape house roof came out as two clashing planes at the inner corner — _createRoof degraded gable→hip but the hip builder (_shrinkPolygon) is convex-only (a re-entrant corner self-intersects the ridge). Fix: detect concavity (new isConvexPolygon) → degrade a concave footprint to FLAT (one coherent slab over the true L); convex footprints byte-identical. + §DIAG-ROOF + §DIAG-CORNER-TURN (CCW/CW on §DIAG-WALL-JOIN — L corner joints verified clean). Proper L hip-and-valley scoped. (2) §KITCHEN-PARAMETRIC-RUN: the auto-furnished kitchen was a concatenation of individual boxes; now planKitchenRun emits ONE parametric KitchenCabinetConfig (I/L/U run, swappable cabinets, glass/drawer fronts, integrated appliances, unified countertop) along the kitchen's longest door-free wall via the real KitchenCabinetEngine — the user can still swap units/appliances in the existing inspector. ai-host 2224/2224, geometry-roof 16/16. Governance: ADR-0063, ADR-0055, SPEC-FURNITURE-LAYOUT-ENGINE, SPEC-KITCHEN-WARDROBE-APPLIANCES, C11.
# deploy-marker: v110 (founder go) — §DEPLOY-LAZY-CHUNK-404 (durable furnish unblock) + §FIVE-GRAPH (Living Graph dropdown). (1) §DEPLOY-LAZY-CHUNK-404: the v107 SW guard made the furnish lazy-chunk 404 fail gracefully (504) but the root persisted — FurnishLayoutExecutor + ceiling/lighting/apartment-executor + registration did await import('@pryzm/ai-host') → a DUPLICATE lazy index-*.js chunk that 404s across deploys when the shell is stale. @pryzm/ai-host is ALREADY 100% eager (engineLauncher static-imports aiService), so the split duplicated bytes + created the whole 404 surface for nothing. Fix: convert all 5 sites to STATIC top-level imports → features fold into the main graph, no separate chunk hash to go missing, zero bundle cost. @pryzm/file-format stays lazy (web-ifc WASM). (2) §FIVE-GRAPH: the founder's 'Living Graph too messy' — a view-selector DROPDOWN (Circulation master / Access / Adjacency / Separation / Service) renders ONE sparse graph at a time, keeping the §GRAPH-UNIFY blob aesthetic + node-edit; Separation derived from the privacy gradient; Access stubbed (soon). + ADR-0068 (Five-Graph Circulation-First, PROPOSED) + tracker §49 (S3 node-roles / S4 metrics / S5 circulation-as-generation-source queued). Governance: C07 §7, ADR-055, C51, ADR-0068/0067/0058, C52/C53.
# deploy-marker: v109 (founder go) — §FRONTAGE-RECTIFY-FRAME + §STAIR-ROOM-DOOR (rotated-plate quality fix). v107 prod (218m² rotated -26° plate): every one of the 8 strategies was window-hard-invalid because frontageFail=[living,kitchen,hall,bedroom,bedroom] → it shipped the least-bad layout (big interior rooms, non-perimeter entrance hall, daylight=0.12). ROOT CAUSE: rooms tile inside the §RECTIFY-QUAD bbox, but validateFrontage + §DIAG-HALL-PERIMETER tested them against the RAW rotated quad whose edges aren't axis-aligned (rectTouchesPerimeter skips diagonal edges) → every frontage-required room FALSE-failed → window hard-reject for the whole pool. Fix: judge frontage against rectifyConvexQuad(shellPolygon) — the SAME frame the rooms tile in; non-rectifying shells (axis-aligned rect, L/U/T, >4-vtx) byte-identical (ADR-0061). + §DIAG-FRONTAGE-DIST (per-fail-room distance-to-perimeter so the next run proves false-negative vs genuine-interior). §STAIR-ROOM-DOOR: the minted stair room (privacy=circulation) was excluded from the door reroute passes → stair0→NO DOOR; the stair is now a legal reroute target (corridor/hall wall only) so it connects to the landing. ai-host 2219/2219. Governance: C53, ADR-0063/0066/0061, SPEC-ARCHITECTURAL-PROGRAM-RULES.
# deploy-marker: v108 (founder go) — §GRAPH-UNIFY: the editable Living Graph now wears the blob Building-Graph aesthetic. Founder ask: 'can the Living Graph have the same STRUCTURE and GRAPHIC as the graph?' STRUCTURE was already shared — both the full-screen blob BuildingGraphOverlay and the modal's editable Living Graph are projections of the ONE Unified Building Graph (@pryzm/building-graph, ADR-0058). GRAPHIC: ported the blob visual vocabulary into LivingGraphCanvas.draw() — lavender radial field wash + additive metaball glow halos (room-type colour) + flowing tapered edge gradients + gentle breathe — keeping the Living Graph's semantics (per-layer edge colour/dash, sun/acoustic rings, area badges) + ALL edit interactions (node area/type → debounced regen, §25/C52). Canvas2D, P2/P3/P6-safe, deterministic. + docs: ADR-0067 Graph-IR/Intent-First Building Graph (BIM 3.0 manifesto → north-star, honest already-built-vs-delta split) + tracker §48 (IMMINENT/PRIORITIZED). Governance: C52, ADR-0058/0066, C04, C50.
# deploy-marker: v107 (founder go) — §SW-LAZY-CHUNK-404 (unblocks batch-furnish + globe) + trustworthy §DIAG-EXEC/§DIAG-SEAL. (1) §SW-LAZY-CHUNK-404: batch-furnish-all-rooms + the globe real-model overlay failed on prod with 'Failed to fetch dynamically imported module …/assets/index-BbFNqnZ7.js' — the service worker 'pryzm-v3' served a STALE cached shell + eager chunks (whose hashes 404 on the moved-on server), so a fresh lazy index-*.js import hit the SPA text/html fallback → module MIME failure. Fix: SW cache v3→v4 (evicts the poisoned shell + fires the one-time auto-reload) + /assets/* branch treats any text/html/non-OK response as a MISS (never serves the SPA fallback as a module). FurnishExecutor lazy-imports @pryzm/ai-host; globe imports @pryzm/file-format. (2) Trustworthy diagnostics: §DIAG-EXEC-WINDOWS WINDOW-ON-PARTITION no longer false-flags shell perimeter walls (decided by the authoritative shell wall-id set, new houseShellWalls.ts) + §DIAG-EXEC-STAIR HABITABLE-ON-STAIR keys off the resolveStairRooms keep ids (was reading un-committed occupancy) + §DIAG-SEAL gains partSpan= (point-to-segment) so the next run says partition↔shell open-seam vs partition↔partition T-join. v106 PROVED the thesis: engineRooms=9 vs detected=8 = Living+Bedroom1 merged (a partition didn't seal); the engine design is correct. Governance: C07 §7, ADR-055, C53, ADR-0066/0063/0061.
# deploy-marker: v106 (founder go) — room.delete legacy bridge makes §STAIR-VOID-EXCLUDE actually delete. v105 shipped §STAIR-VOID-EXCLUDE but its `room.delete` no-op'd: room.rename is an F-1.x BRIDGE to window.commandManager (the legacy RoomStore + bimManager where DETECTED rooms live), but room.delete instead mutated the plugin SDK RoomsState → deleting a detected room (the phantom stair cell) silently failed canExecute('room not found'). Fix: DeleteRoomHandler now bridges to the legacy DeleteRoomCommand exactly like RenameRoomHandler — full removal (roomStore.remove + bimManager.unregister + elementRegistry.unregister + semantic-graph cleanup) + legacy-undo inverse. Fixes room.delete for ALL callers (the property-inspector room delete was also silently broken on detected rooms). So the §DIAG-EXEC-STAIR rollup should now read roomsOverStairVoid=1 with NO ⚠ HABITABLE-ON-STAIR. Governance: C11, ADR-0063/0066.
# deploy-marker: v105 (founder go) — §STAIR-VOID-EXCLUDE: phantom stair-void rooms collapsed to ONE non-habitable Stair. Founder defect #7: the editor redetect traces the BUILT wall graph and the stair body spans a wall line → the stair footprint splits into TWO enclosed faces (the 8.1m² + 25.3m² phantom rooms). §ROOM-NAME-BIJECTIVE (v104) stopped the duplicate NAME but the 2nd cell survived as a ~25m² habitable-looking room. Fix (resolveStairRooms.ts): keep the centre-nearest cell (named "Stair", occupancy stair), DELETE the rest in the same rename batch (room.delete, skipRedetectRooms), and exclude both kept+dropped ids from the engine→detected matcher so no engine room re-names a stair cell. §DIAG-STAIR-VOID-EXCLUDE logs per-rect counts; after it the §DIAG-EXEC-STAIR rollup should read roomsOverStairVoid=1 with NO ⚠ HABITABLE-ON-STAIR. Apartment byte-identical (no stair rects ⇒ no-op, ADR-0061). +6 tests, ai-host 2209. Governance: ADR-0063/0066, C53, C11.
# deploy-marker: v104 (founder go) — duplicate-room naming fix + execution-boundary diagnostics. (1) §ROOM-NAME-BIJECTIVE: the editor room-naming matcher was single-pass with no uniqueness → one engine room could name TWO detected cells (the 53m² ghost 'En-suite' beside the real one) + a partner-less cell kept the empty 'Room 00-008' fallback. Fix: matchDetectedRooms.ts makes a BIJECTION (pass-1 centroid-containment, pass-2 nearest over UNUSED engine rooms only) → no duplicate 'Stair'/'En-suite', no fallback name; §DIAG-STAIR-NAME flags stairRooms>1/unmatched. (2) §DIAG-EXEC-*: always-on editor-side designed-vs-shipped diagnostics per level — §DIAG-EXEC-ROOMS (engine N vs detected M + mismatch), §DIAG-EXEC-AREA (detected vs engine target + §AREA-FRACTIONS cap → OVER-CAP), §DIAG-EXEC-DOORS (per-room door count + roomsWithDoor rollup), §DIAG-EXEC-WINDOWS (per-room window count + WINDOW-ON-PARTITION), §DIAG-EXEC-ROTATION (principal axis°), §DIAG-EXEC-STAIR (HABITABLE-ON-STAIR) — so a console paste pinpoints WHICH layer (engine vs detection vs naming) loses quality. The engine designs good rooms (a 53m² en-suite is impossible from its caps) → the divergence is the engine→editor boundary. + ADR-0066 access-graph-first doctrine + SPEC-ACCESS-GRAPH-AND-SPATIAL-GRAMMAR + tracker §47. ai-host 2209, editor matcher 5/5. Governance: ADR-0066/0063/0061, C53, C04, C11, SPEC-ARCHITECTURAL-PROGRAM-RULES.
# deploy-marker: v103 (founder go) — over-capacity program growth + floor-spike guard. (1) §ENVELOPE-FIT-GROWTH: a shell much larger than the program's max area inflated a fixed small program (fillRatio ~1.02) → rooms collide/merge, every strategy §TOPO-HARD-REJECTs. Root: scaleProgramToShell's 130 m²/bed density was far sparser than the §3.1 envelope table (~37-55 m²/bed) so 206.7m² stayed 2-bed (2-bed grossMax=120m²). Fix: grow the bedroom count until the shell fits the count's §3.1 band (apartment-only; house suppressed); §D3.5 gate validates the scaled count; §DIAG-PROGRAM-FIT. 206.7m²→4-bed, all 8 strategies hard-valid, comb APPLIES; 90/120m² byte-identical. (2) §DIAG-FLOOR-INSET: the v102 §FLOOR-INNER-FACE inset produced degenerate floor 'spikes' on rotated/irregular rooms (shallow-angle miter cross→0 → far-flung vertex); miter clamp (near-parallel + runaway-distance) → local-bevel fallback + per-vertex sanity → centreline fallback. Orthogonal rooms byte-identical, door-gap preserved. ai-host 2207/2207, room-topology 23/23. Governance: SPEC-ARCHITECTURAL-PROGRAM-RULES · dimensional-constraints §3.1 · ADR-0063 · ADR-0061 · C11/C15/C04 · C53.
# deploy-marker: v102 (founder go) — residential layout HARDENING wave (§40-§45). (1) §PARTITION-SHELL-INNER-FACE: interior partitions welded onto the shell centreline were square-capped through the full shell thickness → poked out the outer façade; new WallJoinResolver clamp lands every partition endpoint on the shell INNER face (shell unmoved), + §DIAG-WALL-JOIN PARTITION→SHELL. (2) WINDOW-RULES: every glazable room fronting a façade keeps ≥1 window (minus blind party-walls) — §DIAG-WINDOW-RULE flags any windowless perimeter room; + §DIAG-WINDOW-OVERLAP per-wall de-overlap proof (two windows can never overlap; disjoint spans + 0.1m gap). (3) §HALL-SINGLETON: exactly ONE entrance hall, ground-only (hard invariant: force ground / strip upper / never zero); the main entrance door is bound to a perimeter segment of the hall's OWN room boundary (§DIAG-ENTRANCE). (4) §STAIR-SHELL-CLAMP: v101 regression — the new stair-room mint inflated the keep-out past the shell on a perimeter-abutting ground stair → out-of-façade wall stub + ground 'EXTRA 4'; clamp the stair rect to the shell bbox so the perimeter edge drops as exterior (no duplicate/outside wall). (5) §FLOOR-INNER-FACE: floor finishes now bounded by the wall INNER faces (inset by half-thickness), not centrelines — no flooring under partitions; adjacent floors meet only at door thresholds (§FLOOR-DOOR-GAP). (6) §DIAG-CUTAWAY-RESTORE: the Wall-Low/High toggle-back re-cuts every wall's door/window voids (was restoring walls solid — same family as §DIAG-OPENING-VOID). ai-host 2203/2203, geometry-wall 54/54, room-topology 19/19. Apartment determinism preserved (ADR-0061). Governance: ADR-0055 · C15 · C11 · C04 · ADR-0063 · C53 · SPEC-ARCHITECTURAL-PROGRAM-RULES · tracker §40-§46.
# deploy-marker: v101 (founder go) — residential layout-rules + 3D fixes wave. (1) §STAIR-ROOM-TYPE: first-class 'stair' (vertical-circulation) RoomType in programRules ROOM_RULES; the house stair keep-out now mints a NAMED Stair room so the modal "Stair" == the executed stair (no bedroom tiles into the core). (2) §HALL-PERIMETER: hall.frontage='required' + ground-only — the entrance hall always abuts a perimeter wall (it hosts the main door); §DIAG-HALL-PERIMETER. (3) §ROOM-OVERLAP-HARD: pairwise interior-floor-area overlap detector wired into the D-TGL topology HARD gate (new 'overlap' reason) + §DIAG-ROOM-OVERLAP + founder "Room Overlap Detected" surfacing; a non-overlapping strategy now ranks above an overlapping one (root: subdivide post-passes snapAxisLines/comb-carve move rects independently → can overlap on over-capacity shells). (4) §DIAG-OPENING-VOID: house interior partition-wall doors showed a leaf against a SOLID wall (body-only rebuild fast-path reused a stale/invalid cached join) → now verifies the void cut + falls back to the apartment-proven whole-level rebuild for any un-cut opening wall. (5) §U-LANDING-GUARD: U-stair half-landing open edge now guarded (handrail+balusters+posts) on the correct secondRunSide — was skipped by buildLandingSegment's U early-return. (6) §ROOM-LABELS-TOGGLE: bottom-toolbar on/off for the 3D room-name labels. ai-host 2182/2182, geometry-stair 26/26. Governance: SPEC-ARCHITECTURAL-PROGRAM-RULES · ADR-0063 · C53 · C15/C11 · C04 · tracker §36-§39. Apartment determinism preserved (ADR-0061).
# deploy-marker: v100 (founder go) — PW.2 party-wall DETECTION: resolveBlindFacades now computes the blind set from neighbour footprints (neighbourFootprintStore captured at both GIS fetch sites → projected world-XZ via boundaryProjection about the C19 origin → pure blindFacadeProximity parallel+setback+outward-side, default 1.0m). A façade against an existing building is auto-marked blind → no windows/doors. Additive (no neighbours ⇒ byte-identical). +13 tests. Residual: needs a GIS view to have fetched once.
# deploy-marker: v99 (founder go) — party-wall mechanism + rule-compliance diagnostics. (1) PW.1 §DIAG-PARTY-WALL: a façade snapped against a neighbour building must be BLIND (no windows/doors). Audit verdict — NOT considered today (context buildings are visual-only). Shipped the additive, deterministic blindFacadeWallIds mechanism threaded through windowEmission + the shell-window matcher + the entrance resolver (no window/entrance on a blind wall; rescue honours it). Empty/absent ⇒ byte-identical (apartment+house unaffected). Editor seam resolveBlindFacades.ts (empty default + window.__pryzmBlindFacadeWallIds injection). Detection (PW.2: neighbour footprints via C19 origin → per-wall proximity) is the documented follow-up. +9 tests. (2) §DIAG-STAIR-RULE (4 stair rules R1 corner-not-central / R2 worst-aspect / R3 no-room-overlap / R4 footprint-in-shell, each ✓/⚠ per generation) + §DIAG-WALL-JOIN (per-corner L/SHALLOW-L/COLLINEAR + jointGap mm + closed ✓/⚠) — verified rules hold + L-corners clean (jointGap 0.0mm, no notch). Always-on console logs the founder asked for. ai-host 2167/2167, geometry-wall 50/50, 0 new typecheck errors.
# deploy-marker: v98 (founder go) — EVERY ROOM A DOOR + WINDOW + faithful modal preview. (1) §EVERY-ROOM-ACCESS-COMB: prod showed upper storey doors=2/8 rooms → circulation=0.00 (rooms inaccessible). Root: squarify laid private rooms in ROWS so only the front row touched the corridor → buried bedrooms had no corridor-adjacent wall → no door. Fix: sliceZoneAlongFace lays private rooms as a COMB (full-depth slices perpendicular to the corridor) so EVERY room shares a wall with the corridor → every room gets a permitted circulation door (3-bed verified doors=8, unrouted=[none], permission-violations=0). (2) Window-rescue widened (windowDesiredFor): every room with external frontage keeps ≥1 window (Living/Bathroom were windowless). (3) Rule diagnostics: §DIAG-ADJACENCY (each room → door partners ✓/✗ permission), §DIAG-DOOR-RULE, §DIAG-WINDOW-RULE. (4) MODAL preview now faithfully predicts the build — complete perimeter ring + stair drawn at its contained position (not on a bedroom) + entrance on the hall. ai-host 2158/2158, apartment byte-identical, editor 40 tests.
# deploy-marker: v97 (founder go) — HOUSE M-B: converge house program sizing onto the apartment's scaleProgramToShell (plateRole). The house produced enormous rooms (Living 696m², Bedroom 144m²) + generic 'Room 00-00x' voids because a parallel program sizer starved a large storey of ROOM COUNT, so squarify stretched each room to fill the plate. Fix: scaleProgramToShell(program, area, plateRole 'single'|'ground'|'upper') — apartment ('single') BYTE-IDENTICAL (130m²/bed @≤5); house storeys denser with a higher count ceiling so a big floor gets enough rooms that each lands in-band. enrich/fill delegate to the shared sizer; retired the parallel houseStoreyBand grow-loop. Medium plates now apartment-grade; 144/696 blob class gone. Residual (queued M-C): ≥~400m²/storey plates can still inflate (squarify rescales to fill — needs envelope/per-room cap). +11 tests; ai-host 2155/2155, apartment proven byte-identical. ADR-0063 H1.
# deploy-marker: v96 (founder go) — §LEVEL-STACK rooms+furniture lift with their storey on explode. Founder: on level-stack explode, room name labels + furniture stayed at the wrong height while walls lifted. Root: RoomLabelRenderer builds THREE.Sprites stamped only with userData.roomId (no id/levelId/storeyName) → _isBimObject rejected them → never bucketed → floated at absolute Y; room fills/volumes + furniture roots already carry userData.levelId. Fix: a pre-pass stamps each level-less room annotation's level from roomStore.getById(roomId).levelId in BOTH explode paths (BottomActionMenu._buildLevelRootMap + LevelExplodeController._buildLevelGroups) → labels enter the same byLevel bucket as the walls → lift together; collapse restores exact (captured in _levelOriginalY); §LEVEL-STACK diag now reports per-level rm/lbl/fur counts. editor typecheck clean. + DOCS (no runtime): SPIKE Gaussian-Splatting (Apple Flyover inaccessible → reject; CesiumJS now does georeferenced 3DGS natively → §30 GS.1-GS.4 work items queued under C55) + PIPELINE-ARCHITECTURE-APARTMENT-VS-HOUSE (23 shared / 11 spine / 4 bolt-ons + 4 reuse seams + M-A..M-D convergence + §TOPO-HARD-REJECT-ALL robustness flag) + tracker §30/§31.
# deploy-marker: v95 (founder go) — THE room-merge ROOT FIX (two by-construction cures). The v94 §DIAG-SEAL prod data pinpointed the real seams: (1) §RECTIFY-SHELL-PROJECT — §RECTIFY-QUAD rectifies a sheared convex-quad shell to its bbox for tiling, so partition endpoints terminating on the perimeter land up to ~2.1m off the real ring; now projected onto the real shell after emit. (2) §FRACTURE-SEAL (the keystone) — on a stair-carved plate §STAIR-OBSTACLE-CARVE tiles every room into the DOMINANT rect and leaves the other fragment EMPTY; the sealing wall facing that empty fragment bounds only ONE room (boundsRoomIds.length===1) so it was mis-flagged isExternal → skipExteriorWalls SKIPPED it → never built → interior partitions dangle (§DIAG-SEAL part=1.2-3.7m) → RoomDetection floods → 1 merged room. Fix: a one-sided wall is external ONLY if its body lies ON the real shell perimeter (segmentOnPerimeter); the interior seal now builds BY CONSTRUCTION → rooms seal without relying on the weld. Both apartment/axis-aligned/L-U-T BYTE-IDENTICAL (no empty fragment → classification unchanged). ai-host 2144/2144, geometry-wall 50/50, room-topology 13/13. Also rides §STAIR-DEFAULT-BIAS+CONTAIN-UPSTREAM, §TOPO-HARD-REJECT, §WINDOW-MANDATORY-RESCUE, §LEVEL-STACK, modal §26 XA. + QUEUED docs: §27 daylight colour-field + §28 in-browser wind CFD (C54 + SPEC-WIND-CFD-LBM + ADR-0064).
# deploy-marker: v94 (founder go) — ROOM-MERGE deep-audit: bounded mitigation + decisive §DIAG-SEAL. Forensic audit PROVED the merge root: on rotated plates the engine tiles partitions in the principal-axis BBOX frame so perimeter-terminating partition endpoints land up to ~1.9m off the real rotated perimeter ring → the 0.30m weld can't reach → open seam → RoomDetection (node-grid 20mm) floods → 1 merged room. DISPROVEN: resolver re-opening (it never reduced the count) + ring not closing (_buildPerimeterShell already dedupes/wraps/exact-shared). Ships: §SHELL-SNAP-WIDEN (weld shellSnap 0.30→0.60m + span-interior guard so it only snaps endpoints running up to a shell BODY; interior T-junction snap decoupled at 0.30m) — absorbs MODERATE rotations; + §DIAG-SEAL (per partition endpoint: post-build distance to nearest perimeter + nearest partition endpoint, flags ⚠OPEN-SEAM>0.30m) so this run shows the EXACT residual. Strongly-rotated worst case (~1.9m) needs the engine-frame cure (tile against the real rotated perimeter) — in progress; §DIAG-SEAL confirms. + headless weld→resolver→detection chain test. ai-host 2131/2131. Also rides v93 (§STAIR-CONTAIN-UPSTREAM, §TOPO-HARD-REJECT, §LEVEL-STACK) + modal §26 XA room→floor override + ADR-0063.
# deploy-marker: v93 (founder go) — stair-containment + topology gate + level-stack fix. (1) §STAIR-CONTAIN-UPSTREAM: the carved keep-out now EQUALS the shipped stair footprint — containment is solved UPSTREAM in the orchestrator (rebuild the exact world footprint, solve the inward offset, derive keepOutRectsWorld from the CONTAINED footprint, carry the offset to the executor via StairCore.containOffsetWorld); the executor APPLIES it then VERIFIES (no-op on the normal path, loud §STAIR-CONTAIN ⚠ DESYNC + residual nudge only if they ever disagree). Closes the position→keep-out→tile→nudge desync that made the stair clash internal walls + cut sealing partitions → merge. (2) §TOPO-HARD-REJECT (founder Stage-5): the ranker now HARD-REJECTS candidates with a windowless mandatory-window room / a room with no door onto circulation / a bedroom reachable directly from the hall — preferring a clean strategy of the 8; SAFE FLOOR keeps the least-bad + warns if all 8 fail (pool never empties). + CI invariant test (45° 2-storey: stair kind≠central, no '/' merged room names, every storey ≥ programmed rooms — all pass). (3) §LEVEL-STACK: instanced (batch) walls now lift with their level on expand (were keyed by per-element id which instanced groups lack → bucket by userData.levelId); the Level-stack button is now a 2-state toggle (was 3-state cycling into SOLO → 'collapse' dropped into solo = hidden elements + sun-looking solo icon); collapse restores the 3D exactly. ai-host 2118/2118, editor typecheck clean. + DESIGN docs: SPEC §9 concatenated cross-floor living graph (plan-left/graph-right, Miro-like, move a room between floors) + tracker §26.
# deploy-marker: v92 (founder go) — 2 commits, the STAIR keystone + window rescue. (1) §STAIR-DEFAULT-BIAS (founder Fix 1, THE root cure for the merged private zone): chooseStairCorePosition's corner-preferring terms (PERIMETER_PREFERENCE 1.0 + FRAGMENT_PENALTY) are gated on an aspectBias, but houseOrchestrator passed undefined when solar was absent (the common modal path) → waste-only scorer → a CENTRAL stair → the plate fractures into a 4-way picture-frame with NO dominant rect → §STAIR-OBSTACLE-CARVE can't run the corridor spine → the private zone merges into one blob. Fix: always supply a default Northern-hemisphere bias (lat 45° → back wall worst-aspect) when solar is absent → the stair takes a back/side CORNER → one dominant rect (~75-80%) → the corridor carve fires → distinct rooms. Solar-present path byte-identical; apartment has no stair so untouched. + DOMINANT_FRACTION 0.45→0.40 (Fix 4, defence-in-depth, stairCarved-gated) + §DIAG-STAIR-RESERVE (logs kind=corner/central — the key verification field) + §DIAG-BRANCH dominantFrac/path. (2) §WINDOW-MANDATORY-RESCUE: a windowMandatory room (bedroom/master/living) never ends up windowless — when the shell-match guards (cornerFit/noShellMatch/de-overlap) would drop ALL its windows, a last-resort rescue retains ONE (relax corner setback → keep habitable span over a lower-priority conflicter → widen match tol → smaller variant). Byte-identical when the room already keeps >=1 window. ai-host 2105/2105, apartment byte-identical. Docs: LAYOUT-GENERATION-ALGORITHM master summary + §8.4 why-apartment-beats-house + §8.5 stair-root-cause + §8.2.1/§8.5.4.1 (Fix1/Fix4 shipped); tracker §24.2.
# deploy-marker: v91 (founder go) — 3 commits. (1) §UPPER-SHELL-WELD: the UPPER storeys now weld their partitions onto the minted _buildPerimeterShell ring when the footprint is NOT an axis-aligned rect (mirrors the ground weld) — fixes the first-floor merge ('Bedroom 2 / Bedroom 1 / Bathroom 101.8m²') that v89 §SHELL-ANCHOR-PRESERVE only fixed on the ground (the upper partitions sat OFF the ring on rotated plates → §MULTI-CLUSTER trimmed them apart). Byte-identical on axis-aligned plates; flag window.__pryzmHouseUpperShellWeld. (2) LIVE single-option modal (§25 A-F): the house layout modal now shows ONE option (variant[0], A.21.D18) + a per-storey Plan/Graph toggle with the living graph + a bigger plan; the living-graph nodes are INTERACTIVE — clicking a node edits its area/type into the C52 stash and fires the SAME 250ms debounce the sliders use → re-runs generateHouseLayoutOptions with the overrides merged into program → plan+graph re-render LIVE. Apartment modal byte-identical (all opt-in). (3) §LANDING-NOT-HALL (G14): 'Entrance Hall' is now GROUND-ONLY (front door lands there); upper floors get a Landing (the stair-arrival corridor) — fixes the impossible 'Entrance Hall on first+second floor'. ai-host 2091/2091, editor typecheck clean. Docs: tracker §25 + G14, SPEC-CASA §3, LAYOUT-GENERATION-ALGORITHM.
# deploy-marker: v90 (founder go) — post-v89 prod-test wave. v89 CONFIRMED on prod (stair perfectly aligned 4/4 via §STAIR-CONTAIN-GATE inward nudge; ground 8 rooms NO merge). Ships: (1) §GROUND-ENGINE-PERIMETER (audit Stage-1 unification slice) — the ground storey skips the fragile weld and closes rooms via the engine footprint ring WHEN the plate is axis-aligned convex-rect AND drawn-shell endpoints are within the 20mm room-detection grid; otherwise FALLS BACK to the weld (flag-gated window.__pryzmHouseGroundEnginePerimeter, default ON; weld retained). Composes with v89 §SHELL-ANCHOR-PRESERVE; byte-identical on rotated plates (weld path). (2) §PREVIEW-SHELL-FIDELITY — the layout-modal thumbnail now draws a clear perimeter shell ring + clamps window/door marks to the shell (was: openings walked off the host wall / unclamped world spans poked past the perimeter). +4 tests, 30/30. (3) DOCS: SPEC-LIVE-SINGLE-OPTION-LAYOUT-MODAL + tracker §25 (the single-option + living-graph + LIVE slider/graph→preview redesign — IMPLEMENTATION is the next slice, NOT in this build). ai-host 2084/2084, editor edited-file typecheck clean.
# deploy-marker: v89 (founder go) — 6-FIX prod-feedback wave (8 commits, multi-agent). (1) §SHELL-ANCHOR-PRESERVE (geometry-wall) THE room-merge keystone: WallJoinResolver §MULTI-CLUSTER classified every interior junction primary=0 pinned=0 trimmed=3 (no shell endpoint → interior-only consensus trimmed the welded partition OFF the perimeter → gap → RoomDetection floods → "Living/Bedroom/Kitchen/Hall 82.4m²" blob); fix leaves a body-anchored endpoint untouched so the pairwise T-join trims it onto the shell face → partition stays on perimeter, room seals. +§MULTI-CLUSTER-WHY diag; 50 tests. (2) §SHARED-FLOOR-BOUNDS: option-modal floor thumbnails render at ONE shared scale (was per-floor fit → Ground vs First looked different-sized). (3) §GHOST-FIX: belowLevelDepth-0 system intent auto-assigned to generated storey/roof/doc plan views → ground-floor walls stop ghosting through the first-floor plan (wired the dead CreateViewDefinitionCommand.intent param). (4) §DOC-ROOM-CROP: per-room documentation plans now fit+clip to the room (set crop:ViewCropSettings, not just the inert spatial.cropRegion). (5) STAIR: U half-landing mesh side mirrors secondRunSide (was hardcoded LEFT → poked past footprint on right-fold) + §STAIR-CONTAIN hardened from warning to HARD GATE (nudge full world footprint inward until cornersInShell=4/4). (6) §ROOF-FORMA: roof reappears in the massing view (GISAreaLayout level-elevation map now seeds from bimManager.getLevels() so the wall-less roof level resolves its true height). + house↔apartment unification audit doc (engine is shared; ground-storey weld is the gap). geometry-wall 50/50, geometry-stair 12/12, ai-host 2084/2084, editor typecheck clean.
