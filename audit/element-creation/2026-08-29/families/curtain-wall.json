{
  "family": "curtain-wall",
  "date": "2026-08-29",
  "batch": "B1",
  "doctrine": "D1 absence-with-a-command / D2 unreachable-beats-absent / D3 RC=0 is not 0 violations / D5 seven separate facts / D6 a plugin suite cannot discharge a registration proof / D7 file:line only with its grep / D9 no production code changed",

  "A_registration": {
    "registration_site": "apps/editor/src/PluginRegistry.ts:364-370 — INLINE descriptor inside ALL_PLUGINS (`id: 'curtain-wall'`, `buildStore: () => new CurtainWallStore()`, `buildHandlers: () => buildCurtainWallHandlerSet()`). No plugins/curtain-wall/src/registration.ts exists.",
    "declared_storeKey": "curtainwall — ONE WORD, NO HYPHEN. `grep -n storeKey apps/editor/src/PluginRegistry.ts` -> `367:    storeKey: 'curtainwall',`. ⚠ The plugin ID is 'curtain-wall' (hyphenated, matches the directory) and the storeKey is 'curtainwall'. THIS IS THE EXACT SHAPE OF THE TEN-TIMES DEFECT and it is the one place a directory-name check would have been wrong.",
    "storeKey_matches_ctx_stores_key": "YES for the create path — verified against affectedStores AND ctx.stores, never the directory name. `grep -rn affectedStores plugins/curtain-wall/src --include=*.ts` -> ['curtainwall'] on 17 handlers, [] on 4 (BulkUpdateCurtainPanels, BulkUpdateCurtainWallParameter, CreateCurtainWallsOnAllSlabs, ReplacePanel). `grep -roh \"ctx\\.stores\\.[A-Za-z_]*\" plugins/curtain-wall/src` -> `56 ctx.stores.curtainwall`, `1 ctx.stores.curtainPanelStore`, `1 ctx.stores.curtainpanel`. ChangeCurtainWallLevel.ts:91-96 documents the trap in place: \"affectedStores=['curtainwall'] (one word, lowercase) — that EXACT key MUST be…\".",
    "the_ONE_key_that_does_NOT_resolve": "`ctx.stores['curtainPanelStore']` — read at ReplacePanel.ts:117 and :131, and named in BulkUpdateCurtainPanels.ts:13. NO descriptor contributes it. Because ReplacePanel declares `affectedStores: []` (:97) CommandBus.buildContext does NOT throw; instead `canExecute` returns `{valid:false, reason:'curtainPanelStore not available in handler context'}` on EVERY dispatch (:117-118). This is L-1054, already pinned by apps/editor/__tests__/CurtainReplacePanelIsDead.test.ts (ARM 1 reads the REAL ALL_PLUGINS and asserts no descriptor contributes the key; ARM 2/3 run the real handler against a production-shaped context). It is still OFFERED from CurtainPanelEditor.ts:250 and CurtainSubElementPanel.ts:319 and listed as AI-reachable in ChatCommandClassification.ts:109. MY PROBE P-6 reached only the prior guard (`newPanelType is required`), so I cite the existing test rather than claiming my own reproduction.",
    "how_the_key_is_bound": "apps/editor/src/bootstrap.everything.ts:143-145. CommandBus.buildContext (packages/command-bus/src/CommandBus.ts:285-292) throws on a missing REQUIRED key; an UNDECLARED read (affectedStores: []) yields `undefined` silently — which is exactly how ReplacePanel dies quietly.",
    "in_ELEMENT_PLUGIN_IDS": true,
    "ELEMENT_PLUGIN_IDS_site": "apps/editor/src/PluginRegistry.ts — 'curtain-wall' row (the HYPHENATED id, not the storeKey). sed -n '780,840p' apps/editor/src/PluginRegistry.ts",
    "in_STORE_ONLY_PLUGIN_IDS": false,
    "proof_reads_real_composition_root": "PARTIAL. CurtainReplacePanelIsDead.test.ts ARM 1 reads the REAL `ALL_PLUGINS` and constructs no store — a genuine D6-compliant registration proof, but for the NEGATIVE (replacePanel). For `curtain-wall.create` there was NO composition-root proof before this audit: apps/editor/__tests__/CurtainWallUpdateReachesGeometryStore.test.ts and CeilingUpdateReachesGeometryStore.test.ts build their OWN `new CommandBus()` and seed their own geometry store, so by D6 they cannot observe a provider failure. bootstrap.everything.test.ts:53 `expect(rt.stores.curtainwall).toBeInstanceOf(CurtainWallStore)` is the closest existing artefact.",
    "MY_OWN_composition_root_probe": "audit/element-creation/2026-08-29/probe/b1Dispatch.probe.test.ts P-4 / P-4b — REAL composeRuntime, constructs nothing. `[P-4] curtain-wall.create outcome = OK` and `[P-4b] curtainwall.create (alias) outcome = OK`.",
    "verdict": "REGISTERED and DISPATCHABLE under BOTH the canonical type and its declared alias."
  },

  "B_handler": {
    "path": "plugins/curtain-wall/src/handlers/CreateCurtainWall.ts",
    "type": "curtain-wall.create (:49), with `aliases = ['curtainwall.create']` (:51, §FIX-COMMAND-NAMESPACE L-796)",
    "affectedStores": "['curtainwall'] (:52)",
    "alias_does_not_break_the_bridge": "MEASURED. CommandBus.register maps aliases to the SAME entry (:126-140) and the committed record carries `type: handler.type` — the CANONICAL name (CommandBus.ts:533). PROBE P-10: dispatching `curtainwall.create` produced `curtain-wall.created id=curtainwall_… baseLine=[…]`. So the alias is safe; C11 §11's naming of the CEB case is nonetheless wrong (see E).",
    "validates_before_mutation": "YES — canExecute :54-88: finite Vec3 endpoints, non-zero baseLine, `> 0 && finite` for height/mullionThickness/bayWidth/bayHeight/panelThickness, finite (signed, deliberately not `>0`) baseOffset, non-empty and unique panel ids. Typed DomainErrors from execute: CurtainWallGeometryError (:107), CurtainWallSchemaError (:111).",
    "mutates_only_via_immer_draft": "YES — :113-115 `produceCommand<CurtainWallsState>(ctx.stores.curtainwall, draft => { draft[cw.id] = cw; })`. No store-singleton import anywhere on the path.",
    "geometry_deferred_to_FrameScheduler": "N/A at the handler. The mesh is built by CurtainWallBuilder, wired at apps/editor/src/engine/initUI.ts:2311-2314 (`window.curtainWallBuilder = new CurtainWallBuilder(...)`, '§3.8 §Critical #3 FIX: Wire CurtainWallStore → CurtainWallBuilder') off the STORE's own subscribe list — NOT off the `bim-curtainwall-added` DOM event. `grep -rn requestAnimationFrame plugins/curtain-wall/src` -> 0.",
    "emits_typed_domain_event": "NOT BY THE HANDLER. CommandEventBridge emits `curtain-wall.created`.",
    "otel_span": "YES — `withHandlerSpan(this.type + '.handler', {'pryzm.command.type': this.type}, …)` wraps the whole execute body (:91, closed :117).",
    "id_pre_generated_in_tool": "YES at the live tool, and the tool says WHY. CurtainWallPlanToolHandler.ts:512 `const cwId = createId('curtainwall')` with the comment at :475-477/:505-511: 'The CEB extracts id from record.payload → emits ev.id on curtain-wall.created. The initTools §P3.1-CW bridge guards on !ev.id — if id is omitted the guard silently drops every event → no mesh, no plan view.' ⚠ THE HAZARD IS REAL, NOT HISTORICAL: the handler still accepts `cmd.id ?? createId('curtainwall')` (:92) and the CEB still reads `id: p.id` (:731). PROBE P-11: a `curtain-wall.create` with no id emits `curtain-wall.created id=undefined`, and curtainWallCreatedMirror.ts:123 guards `!ev.id`. The tool comment is a convention, not an invariant.",
    "must_not_violations": [
      "window.commandManager — 3 CODE sites: BulkUpdateCurtainPanels.ts:125, BulkUpdateCurtainWallParameter.ts:120, CreateCurtainWallsOnAllSlabs.ts:49",
      "window.dispatchEvent — 4 CODE sites: BulkUpdateCurtainPanels.ts:144, :174, BulkUpdateCurtainWallParameter.ts:139, :173",
      "NONE of these is in CreateCurtainWall.ts — the same grep over that file returns 0. The CREATE verb is clean.",
      "No DOM access, no requestAnimationFrame, no import from src/engine or src/ui anywhere in plugins/curtain-wall/src."
    ],
    "must_not_grep": "grep -rn \"window\\.commandManager|commandManager\\.execute|window\\.dispatchEvent|requestAnimationFrame\\(|document\\.getElementById|document\\.querySelector|from '@app/|src/engine|src/ui\" plugins/curtain-wall/src --include=*.ts | grep -vE \":\\s*(//|\\*|/\\*)\""
  },

  "C_lifecycle": {
    "ceb_case": "YES x2, and the SPELLING IS HYPHENATED: CommandEventBridge.ts:694 `case 'curtain-wall.create'`, :758 `case 'curtain-wall.batch.create'`. There is NO `case 'curtainwall.create'` — grep -n \"case '\" over the file returns 0 such row. GEOMETRY-COMPLETE: baseLine, height, baseOffset, bayWidth (default 1.2), bayHeight (default 1.5), mullionThickness (default 0.05), panelThickness, materialId (`p.materialId ?? p.systemTypeId`, folded to match the handler), panels, plus the four §CW90-PLAN-TYPE-PARITY fields systemTypeId / mullionMaterialId / mullionColor / glazingMaterialId.",
    "ceb_reads": "record.payload (the REQUEST), not record.forward.",
    "initTools_bridge_tag": "§P3.1-CW",
    "bridge_grep": "$ grep -n \"bridge registered\" apps/editor/src/engine/initTools.ts   ->   2146:        console.log('[initTools] §P3.1-CW: curtain-wall.created bus→legacy-store bridge registered.')",
    "bridge_body": "apps/editor/src/engine/initTools.ts:2063-2147; the guard + field mapping were EXTRACTED to apps/editor/src/engine/curtainWallCreatedMirror.ts (§FIX-CW-BRIDGE-AUTHORED-VALUES, L-972) precisely so a test can execute them — as a closure they hid FIVE constant-false reads.",
    "bridge_guard": "curtainWallCreatedMirror.ts:122-123 — `if (!ACCEPTED_CURTAIN_WALL_COMMAND_TYPES.has(ev.commandType ?? '')) return null; if (!ev.id || !ev.baseLine || ev.baseLine.length < 2) return null;` then initTools.ts:2091 `if (curtainWallStoreInstance.has(cwRecord.id)) return;` (dedup).",
    "vdt_registerElement": "YES — initTools.ts:2092, BEFORE add(), OUTSIDE the try around add() but AFTER the dedup return.",
    "bimManager_registerElement": "YES — initTools.ts:2094, same position.",
    "bridge_invariant_7": "HELD, but DELIBERATELY INVERTED relative to the C11 §11 column legend. §G3-STALE-FIX-CW (OI-054(a), 2026-05-24) hoists both registrations ABOVE `add()` and states why: `curtainWallStoreInstance.add()` synchronously drives CurtainPanelSyncHandler, which fires a storeEventBus event per panel; the VDT attributes each panel to its parent (§CW-PANEL-PARENT) only if the PARENT is already registered. Registering after add would reintroduce the §G3-STALE storm. ⚠ The dedup guard still RETURNS before both registrations.",
    "bimManager_via_window_any": "NO. Injected ToolsParams field (initTools.ts:267). `grep -n \"window\\.bimManager\" apps/editor/src/engine/initTools.ts` -> 0 hits. An `any` seam (L-980), not a C14 LP-01 / P4 violation. ⚠ SEPARATELY: `window.curtainWallBuilder` IS assigned at initUI.ts:2311 — a genuine window-as-namespace seam, but outside the create bridge.",
    "in_GEOMETRY_ELEMENT_TYPES": true,
    "geometry_types_site": "packages/core-app-model/src/views/ViewDependencyTracker.ts:42-49 — 'curtainwall' AND 'curtain-panel' both present. ⚠ 'curtain-wall' (hyphenated) is NOT in the set; the STORE emits 'curtainwall' (CurtainWallStore.ts:546-551) so the match holds. NOT in PLAN_INCREMENTAL_SAFE_TYPES (:69-71 is wall/slab/beam/ceiling/floor only) — a curtain-wall create forces a full reprojection, not an O(dirty) graft. That is a perf fact, not a defect.",
    "legacy_store_emits_storeEventBus": "YES — and this is the TODO-CW-STORE-BUS answer. `CurtainWallStore.add()` (packages/geometry-curtain-wall/src/CurtainWallStore.ts:326-356) ends in `this.emit(isNew ? 'add' : 'update', withDefaults, prev)` (:355), and `private emit(...)` (:540-552) calls `storeEventBus.emit({elementId, elementType:'curtainwall', operation: event === 'add' ? 'create' : …})` (:546). `addMany()` emits per-item too (:238, :280, :301).",
    "TODO_CW_STORE_BUS_verdict": "BOTH HALVES EXIST — the compensation was never removed and the store half now works, so the net effect is a DOUBLE EMIT, not a gap. initTools.ts:2129-2135 still fires `storeEventBus.emit({elementType:'curtainwall', operation:'create'})` after add(), and its own comment (L-1056, 2026-08-19) says so verbatim: 'the line below is a DUPLICATE, and every curtain wall mirrored through this bridge fires TWO plan-invalidation events… C87 §11 row 14 and CW-C-2 are derived from this comment and are REFUTED by it.' The duplicate is left in place DELIBERATELY because removing it changes the ORDER subscribers observe the wall relative to the synchronous panel storm inside add(), and the bridge is not reachable from any suite.",
    "update_verbs_covered": "NO — curtain-wall is a CREATE-MIRROR-WITHOUT-UPDATE-MIRROR family, and it is the WORST of the four. ELEMENT_UPDATE_VERBS (CommandEventBridge.ts:241-292) has ZERO curtain-wall rows (8 rows total: slab x5, roof x2, column x1). MEASURED on the real bus (probe P-13): `curtain-wall.resize` -> outcome OK, and the only `element.updated` event emitted in the whole run was slab.setThickness. tools/ga-gate/mirror-debt.json carries 17 curtain-wall rows — 16 UNMIRRORED (addGridLine, addPanel, batch.delete, batch.update, delete, move, removeGridLine, removePanel, resize, rotatePanel, setGrid, setMullionType, setOutline, setPanelType, setTransomType, swapPanel) and 1 'refuses' (setMaterial). More UNMIRRORED verbs than any other family in the ledger.",
    "mirror_gate": "npx tsx tools/ga-gate/check-mirror-completeness.ts -> RC=0. D3: '200 verbs WRITE a plugin DTO store — 44 with a bridge case, 156 without' · 'ledger: 156 row(s) — 109 kind=UNMIRRORED, 47 declared-exempt' · 'census (initTools.ts): 19 *.created mirror(s) · 1 *.updated mirror(s)'."
  },

  "D_seven_facts": {
    "authored":     {"v": "YES", "proof": "plugins/curtain-wall/src/handlers/CreateCurtainWall.ts:49 `readonly type = 'curtain-wall.create'`; 21 handler files under plugins/curtain-wall/src/handlers/."},
    "dispatchable": {"v": "YES", "proof": "PROBE P-4 / P-4b on the REAL composeRuntime bus (constructs nothing): both `curtain-wall.create` and the alias `curtainwall.create` -> outcome OK, neither matched /is missing from HandlerContext/. storeKey 'curtainwall' (PluginRegistry.ts:367) === affectedStores ['curtainwall'] === ctx.stores.curtainwall (56 reads). bootstrap.everything.test.ts:53 asserts rt.stores.curtainwall instanceof CurtainWallStore on the real registry walk."},
    "reachable":    {"v": "YES", "proof": "W4i RECOVERY 2026-08-31 — was UNVERIFIED (correctly distinguished from NO), now EXECUTED. audit/element-creation/2026-08-29/probe/w4i/w4iR.probe.test.ts arm R-3 (RC=0, 7/7; LOG-R.txt, runR2.txt) joins the chain on ONE real composeRuntime: `[R-3] curtain-wall.create outcome = OK` -> the event captured on rt.events('curtain-wall.created'), the SAME channel and name initTools:2064 subscribes to -> the REAL exported production mirror `curtainWallRecordFromCreatedEvent(ev)` returns a record -> the REAL CurtainWallStore (`new cwMod.CurtainWallStore()`, the class initBuilders.ts:340 news) accepts it -> `[R-3] CurtainWallStore readback = true`, and the store's own plan-invalidation signal fires: `[R-3] storeEventBus curtainwall events = [{\"elementId\":\"curtainwall_…\",\"elementType\":\"curtainwall\",\"operation\":\"create\",\"timestamp\":…}]`. ⚠ THE ONE LINK STILL NOT EXECUTED, NAMED EXACTLY: initTools.ts:2064 `runtime.events.on('curtain-wall.created', (ev) => {…})` — the SUBSCRIPTION. `initTools.bootstrap()` needs a canvas + WebGL so it cannot be called headless. Both sides of that line are executed on the same emitter; the residual risk is 'does that one .on() line run', not 'does any of this work'. The 2026-08-29 premise (`rt.stores.elements` has no curtainwall kind) is CORRECT and IRRELEVANT: curtain-wall has no module singleton — composeRuntime registers only door/window/wall/slab/room — so the authoritative store is the engine's own `new CurtainWallStore()`, which is what R-3 drives."},
    "renders_3d":   {"v": "YES", "proof": "W4i RECOVERY 2026-08-31 — was UNVERIFIED, now EXECUTED, twice independently. Same run as `reachable`: the REAL CurtainWallBuilder (`new cwMod.CurtainWallBuilder(scene, {materialMap: undefined})`) is given the record read back out of the REAL store, into a real THREE.Scene -> `[R-3] MESHES = 10 · verts = 240` · `[R-3] scene.children[0].userData.id = curtainwall_… · elementType = CurtainWall · version = 1`. Unlike the ceiling, `updateCurtainWall()` builds SYNCHRONOUSLY — no frame pump needed. The prior lane's independent run measured the same numbers (`[D-2] scene.children before/after = 0 / 1` · `[D-2] MESHES = 10 · verts = 240`, LOG.txt). ⚠ The MEASURED FAILURE MODE from 2026-08-29 is UNCHANGED and still real: an id-less create emits `curtain-wall.created id=undefined` and curtainWallCreatedMirror.ts:123 returns null — silent, no mesh (B1-CW-02). renders_3d = YES for a create that carries an id, which every live tool does by convention only."},
    "renders_plan": {"v": "YES", "proof": "W4i RECOVERY 2026-08-31 — was UNVERIFIED, now EXECUTED. probe/w4i/w4iPlan.probe.test.ts (RC=0, 10/10; runPlan1.txt) drives the REAL ViewDependencyTracker with the same four heavy-singleton mocks views/__tests__/lightingAndRoomReachThePlanView.test.ts uses. P-4/P-5/P-6: a curtainwall create/update/delete -> `reprojected == ['vd-plan-l0']`, no §G3-STALE-EVENT warn. FOUR CONTROLS: C1 'water' -> [] (the harness can say NO); C2 an UNREGISTERED id -> BOTH plans + §G3-STALE-EVENT (which is precisely why §G3-STALE-FIX-CW registers BEFORE add()); C3 level scoping; C4 the spelling control below. The elementType string is MEASURED not transcribed — `[R-3]` above shows the REAL CurtainWallStore emitting 'curtainwall'. PRECONDITION (1) 'something draws it' HOLDS: 'curtainwall' IS in EdgeProjectorService.CACHEABLE_ELEMENT_TYPES (:1995) and A-WALL IS cut-eligible — CUT_ELIGIBLE_PLAN_LAYERS:453 `'A-WALL', // walls + curtain walls (shipped in L-261)` — and the version stamp that gate depends on is real (`version = 1`, measured). ⚠ THREE SPELLINGS, RECONCILED BY MEASUREMENT RATHER THAN ASSUMED: (a) store event 'curtainwall' matches GEOMETRY_ELEMENT_TYPES (ViewDependencyTracker.ts:81) — LIVE, P-4/5/6; (b) mesh userData 'CurtainWall' (PascalCase, MEASURED in R-3) is lowercased at EdgeProjectorService.ts:2748 before the CACHEABLE check, so it matches — NOT a defect; (c) the command/CEB spelling 'curtain-wall' MISSES the Set — measured as control C4, `reprojected == []` with NO §G3-STALE warn, i.e. dropped SILENTLY at the membership check. Nothing emits it on storeEventBus today, so (c) is a LATENT trap of the exact 'stairRailing' shape the tracker's own header records, not a live defect — recorded so a future emitter does not walk into it."},
    "persists":     {"v": "YES", "proof": "snapshotFamilyCoverage.ts:127 `{storeKey:'curtainwall', status:'via-legacy-twin', snapshotKey:'curtainWalls'}`. ProjectSerializer.ts:170 declares `curtainWalls: any[]`, :1397 `const curtainWalls = curtainWallStore.getAll().map(serializeCurtainWall)`. ProjectLoader.ts:1692-1693 restores them. npx tsx tools/ga-gate/check-snapshot-family-coverage.ts -> RC=0, '26 persistence claim(s) verified … 3/3 declared losses' (curtainwall is not one). ⚠ DECLARED PARTIAL LOSS: ProjectSerializer.ts:1440-1446 warns '§L-1057 no curtainPanelStore was passed, so per-panel authoring on N curtain wall(s) is NOT being saved' — the wall persists, per-panel authoring may not. Loud, not silent."},
    "exports":      {"v": "YES", "proof": "W4i RECOVERY 2026-08-31 — was YES BY CITATION, now YES BY EXECUTION, end-to-end from a real dispatch. Probe arm R-4 runs the REAL FragmentReader over the REAL CurtainWallStore the REAL mirror wrote and the scene the REAL CurtainWallBuilder filled in R-3: `[R-4] elements = 1` · `[R-4] ids = [\"curtainwall_…\"]` · `[R-4] classes = [\"IfcCurtainWall\"]` · `[R-4] curtain-wall element = PRESENT ifcClass=IfcCurtainWall`, with `[R-4c] CONTROL empty-scene elements = 0` proving the reader is mesh-gated and the pass is not vacuous. This matters because CurtainWallReader, like every reader here, silently contributes zero for an element with no mesh — the citation alone never established that the production builder stamps an id `ctx.findMesh` can resolve. It does. ORIGINAL CITATION CHAIN, unchanged: packages/file-format/src/export/ifc/FragmentReader.ts:134 (was cited as :125) `new CurtainWallReader(this.stores.curtainWallStore, this).read()`; readers/CurtainWallReader.ts:36 -> 'IfcCurtainWall'; IfcModelBuilder.ts:42 maps it to WEBIFC.IFCCURTAINWALL. ⚠ `grep -rn curtainWall plugins/ifc-export/src --include=*.ts` -> 0 hits: the SECOND exporter (IFC4X3Exporter / orchestrator, which does cover wall, slab, column, beam, space) has NO curtain-wall arm. One pipeline of two."}
  },
  "first_failing_link": "NONE for a create that carries an id — as of W4i RECOVERY (2026-08-31) all SEVEN CREATE facts are YES with executed evidence. The 2026-08-29 verdict read 'reachable — UNVERIFIED, not NO … the chain was NEVER EXECUTED end to end'; that distinction was correct and the chain has now been EXECUTED end to end (bus -> event -> mirror -> store -> builder -> 10 meshes -> IfcCurtainWall, plus the plan-invalidation event and the tracker's reprojection). ⚠ THE ROW IS NOT 'CURTAIN WALL IS FINE'. THE FIRST MEASURABLE FAILURE IS UNCHANGED AND STILL LIVE: the CEB's `id: p.id` read (CommandEventBridge.ts:731) — a create with NO id emits `curtain-wall.created id=undefined` and curtainWallCreatedMirror.ts:123 returns null SILENTLY, so no mesh and no plan (B1-CW-02). ONE link in `reachable` is source-pinned rather than executed: initTools.ts:2064 `runtime.events.on('curtain-wall.created', …)`, which needs a canvas + WebGL. And the seven facts are about CREATE ONLY — for the family as a whole the first failing link is the UPDATE path: ZERO curtain-wall rows in ELEMENT_UPDATE_VERBS and 16 UNMIRRORED verbs, more than any other family (B1-CW-03), alongside `curtain-wall.replacePanel` refusing on every dispatch while two panels still offer it (B1-CW-04).",
  "W4i_recovery_2026_08_31": {
    "artefacts": "audit/element-creation/2026-08-29/probe/w4i/ — w4iR.probe.test.ts + vitest.w4iR.config.ts -> LOG-R.txt / runR1.txt / runR2.txt (RC=0, 7/7); w4iPlan.probe.test.ts + vitest.w4iPlan.config.ts -> runPlan1.txt (RC=0, 10/10). Prior lane: w4i.probe.test.ts -> LOG.txt (RC=0, 9/9). NO production file was modified.",
    "capability_search_before_wiring": "Asked by WHAT IT DOES, never by a name: composedBusElementReadback.test.ts (MT-01) REUSED as the readback model; views/__tests__/lightingAndRoomReachThePlanView.test.ts REUSED for the plan harness and its four control shapes; curtainWallCreatedMirror.ts CALLED, not reimplemented (it was already extracted for exactly this purpose by §FIX-CW-BRIDGE-AUTHORED-VALUES L-972); FakeRafAdapter USED, not written. NOTHING WAS MISSING; the gap was that executable halves had never been JOINED. No rival was built.",
    "stub_ledger": "S1 FakeRafAdapter (ceiling arm only — the curtain wall builds synchronously and needed no clock). S3 THREE.Scene is a real new Scene(). S4 (plan probe only) the heavy singletons ViewTechnicalDrawingCache / ViewDefinitionStore / ElementRegistry / UnifiedFrameLoop / StoreEventBus, mocked exactly as the pre-existing lightingAndRoomReachThePlanView suite mocks them; ViewDependencyTracker itself is REAL and is the object under measurement. Nothing else is substituted; the runtime is a REAL composeRuntime({bootstrapFn: bootstrapWithEverything}).",
    "UPGRADED_evidence_for_B1_CW_01": "The refutation of C11 §11 cell 3 was previously made from a SOURCE LINE (CurtainWallStore.ts:546). It is now made by MEASUREMENT: `[R-3] storeEventBus curtainwall events = [{\"elementType\":\"curtainwall\",\"operation\":\"create\",…}]` — a REAL add() on the REAL store landing a REAL event on the REAL storeEventBus. `CurtainWallStore.add()` DOES emit; the matrix inverts the fact and the initTools:2129 emit IS a duplicate, exactly as L-1056 records. B1-CW-01 stands on stronger evidence."
  },

  "E_c11_ss11_crosscheck": {
    "matrix_row": "| **Curtain Wall** | ✅ `curtainwall.create` + `curtain-wall.batch.create` | ✅ §P3.1 (+ explicit `storeEventBus.emit`) | ⚠️ `CurtainWallStore.add()` omits storeEventBus — compensated by bridge emit | ✅ `'curtainwall'` | ✅ (Rev 4 — FIX-PLAN-VDT-BIMMANAGER-ALL-BRIDGES) | ✅ | ✅ FULL (TODO-CW-STORE-BUS active) |  (C11 §11, dated 2026-05-19)",
    "verdict": "REFUTED on cell 3, cell 5's legend, and cell 1's spelling. The §11.1 gap row TODO-CW-STORE-BUS is ALSO refuted.",
    "refutation_cell_3": "The matrix says `CurtainWallStore.add()` OMITS storeEventBus and is 'compensated by bridge emit'. MEASURED: `add()` (CurtainWallStore.ts:326-356) ends in `this.emit('add', …)` at :355, and `emit()` at :540-552 calls `storeEventBus.emit({elementType:'curtainwall', operation:'create'})` at :546. The store DOES emit. The bridge emit at initTools.ts:2129-2135 therefore is NOT a compensation — it is a DUPLICATE, and every mirrored curtain wall fires TWO plan-invalidation events. The code already says this (L-1056, initTools.ts:2118-2128) and further records that C87 §11 row 14 and CW-C-2 are derived from the same false premise and are refuted by it. The duplicate is retained deliberately, so the DEFECT IS THE DOC, not the code.",
    "refutation_ss11_1": "§11.1 orders 'TODO-CW-STORE-BUS | Curtain Wall | Fix CurtainWallStore.add() to emit storeEventBus directly (eliminate bridge workaround) | Wave A16'. Half of that work is already done (the store emits) and doing the other half — deleting the bridge emit — is precisely the change L-1056 declined to make blind, because it reorders subscribers relative to the synchronous CurtainPanelSyncHandler storm inside add(). The row as written would authorise a rival, unwatched behaviour change.",
    "refutation_cell_1_spelling": "The matrix names the CEB case `curtainwall.create`. MEASURED: the case is `case 'curtain-wall.create'` (CommandEventBridge.ts:694), hyphenated; `curtainwall.create` exists only as a handler ALIAS (CreateCurtainWall.ts:51). This is HARMLESS in effect — CommandBus stamps `type: handler.type` (the canonical name) on the record (CommandBus.ts:533), and probe P-10 confirms the alias still produces `curtain-wall.created` — but it is a false citation in a normative table, and it points a reader at a case that does not exist.",
    "refutation_cell_5_legend": "The VDT+bimManager column legend says 'AFTER LegacyStore.add() (Bridge Invariant 7)'. Curtain wall registers BEFORE add (initTools.ts:2092/2094), by name and on purpose (§G3-STALE-FIX-CW, OI-054(a)). Following the legend literally would reintroduce the §G3-STALE storm it removed.",
    "what_the_matrix_gets_RIGHT": "cells 2 (§P3.1 bridge exists), 4 ('curtainwall' in GEOMETRY_ELEMENT_TYPES) and the existence of the batch case."
  },

  "F_findings": [
    {"id": "B1-CW-01", "severity": "P1", "class": "DOC-CONFLICT", "claim": "C11 §11 cell 3 says `CurtainWallStore.add()` omits storeEventBus; it emits at :546 via :355. The 'compensation' at initTools:2129 is a documented duplicate (L-1056), so the matrix inverts the fact and §11.1 orders work whose first half is done and whose second half L-1056 declined.", "site": "docs/02-decisions/contracts/C11-ELEMENT-CREATION-PIPELINE.md §11 + §11.1", "disposition": "DOCUMENT"},
    {"id": "B1-CW-02", "severity": "P1", "class": "ID-MINTED-IN-HANDLER-BRIDGE-GUARDS-ON-ID", "claim": "`curtain-wall.create` with no `id` emits `curtain-wall.created id=undefined`; curtainWallCreatedMirror.ts:123 returns null. The plan tool's comment names the hazard, but nothing enforces it.", "site": "packages/runtime-composer/src/CommandEventBridge.ts:731 `id: p.id` (contrast wall :508-517)", "disposition": "WIRE"},
    {"id": "B1-CW-03", "severity": "P1", "class": "CREATE-MIRROR-WITHOUT-UPDATE-MIRROR", "claim": "ZERO curtain-wall rows in ELEMENT_UPDATE_VERBS and 16 UNMIRRORED verbs in mirror-debt.json — more than any other family. Measured: `curtain-wall.resize` returns OK and emits no `element.updated`.", "site": "packages/runtime-composer/src/CommandEventBridge.ts:241-292 + tools/ga-gate/mirror-debt.json", "disposition": "WIRE"},
    {"id": "B1-CW-04", "severity": "P2", "class": "REGISTERED-BUT-UNDISPATCHABLE (undeclared store key)", "claim": "`curtain-wall.replacePanel` reads `ctx.stores['curtainPanelStore']`, a key no descriptor contributes, and declares `affectedStores: []` so the bus never throws — it refuses on every dispatch while two property panels and the AI classifier still offer it.", "site": "plugins/curtain-wall/src/handlers/ReplacePanel.ts:97,117 · CurtainPanelEditor.ts:250 · CurtainSubElementPanel.ts:319 · ChatCommandClassification.ts:109 (already pinned by CurtainReplacePanelIsDead.test.ts, L-1054)", "disposition": "REMOVE"},
    {"id": "B1-CW-05", "severity": "P2", "class": "DOC-CONFLICT", "claim": "C11 §11 names the CEB case `curtainwall.create`; the case is `curtain-wall.create` and the unhyphenated spelling is only a handler alias. Harmless in effect (probe P-10), false as a citation.", "site": "docs/02-decisions/contracts/C11-ELEMENT-CREATION-PIPELINE.md §11 vs CommandEventBridge.ts:694", "disposition": "DOCUMENT"},
    {"id": "B1-CW-06", "severity": "P2", "class": "PARTIAL-EXPORT", "claim": "Curtain walls reach IFC through FragmentReader/CurtainWallReader only. `grep -rn curtainWall plugins/ifc-export/src --include=*.ts` -> 0 hits: the IFC4X3Exporter/orchestrator pipeline has no curtain-wall arm.", "site": "plugins/ifc-export/src/exporters/IFC4X3Exporter.ts (absence) vs packages/file-format/src/export/ifc/FragmentReader.ts:125", "disposition": "DOCUMENT"},
    {"id": "B1-CW-07", "severity": "P3", "class": "MISSING-PROOF", "claim": "No composition-root (D6) proof exists for `curtain-wall.create`; the existing CurtainWall*ReachesGeometryStore suites build their own CommandBus and geometry store, so they cannot observe a provider failure. There is also no authoritative curtain-wall store in storeRegistry (probe P-1).", "site": "apps/editor/__tests__/CurtainWallUpdateReachesGeometryStore.test.ts", "disposition": "WIRE"}
  ]
}
