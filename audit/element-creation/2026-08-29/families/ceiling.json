{
  "family": "ceiling",
  "date": "2026-08-29",
  "batch": "B1",
  "doctrine": "D1 absence-with-a-command / D2 unreachable-beats-absent / D3 RC=0 is not 0 violations / D5 seven separate facts / D6 a plugin suite cannot discharge a registration proof / D7 file:line only with its grep / D9 no production code changed",

  "A_registration": {
    "registration_site": "apps/editor/src/PluginRegistry.ts:412-418 — INLINE descriptor inside ALL_PLUGINS (`id: 'ceiling'`, `buildStore: () => new CeilingStore()`, `buildHandlers: () => buildCeilingHandlerSet()`). No plugins/ceiling/src/registration.ts exists.",
    "declared_storeKey": "ceiling  (`grep -n storeKey apps/editor/src/PluginRegistry.ts` -> `415:    storeKey: 'ceiling',`)",
    "storeKey_matches_ctx_stores_key": "YES — verified against affectedStores AND ctx.stores, never the directory name. `grep -rn affectedStores plugins/ceiling/src --include=*.ts` -> ['ceiling'] on 8 handlers, [] on 1 by design (UpdateCeilingsSystemTypeBatch). `grep -roh \"ctx\\.stores\\.[A-Za-z_]*\" plugins/ceiling/src` -> `20 ctx.stores.ceiling` and NOTHING else. `grep -rn \"ctx\\.stores\\[\" plugins/ceiling/src` -> 0 hits.",
    "how_the_key_is_bound": "apps/editor/src/bootstrap.everything.ts:143-145. CommandBus.buildContext (packages/command-bus/src/CommandBus.ts:285-292) throws `\"<type>: required store '<key>' is missing from HandlerContext.stores\"` on any miss.",
    "in_ELEMENT_PLUGIN_IDS": true,
    "ELEMENT_PLUGIN_IDS_site": "apps/editor/src/PluginRegistry.ts — 'ceiling' row inside the canonical block. sed -n '780,840p' apps/editor/src/PluginRegistry.ts",
    "in_STORE_ONLY_PLUGIN_IDS": false,
    "proof_reads_real_composition_root": "NONE for `ceiling.create` before this audit. apps/editor/__tests__/CeilingUpdateReachesGeometryStore.test.ts imports `composeRuntime` in the file list but its `boot()` constructs `new CommandBus()` and `seedGeometryStore()` itself — by D6 a suite that supplies the provider cannot observe its absence. apps/editor/__tests__/CeilingBridgeCarriesAuthoredFinish.test.ts exercises `ceilingRecordFromCreatedEvent` in isolation. The nearest real-root artefact is bootstrap.everything.test.ts:59 `expect(rt.stores.ceiling).toBeInstanceOf(CeilingStore)`.",
    "MY_OWN_composition_root_probe": "audit/element-creation/2026-08-29/probe/b1Dispatch.probe.test.ts P-5 — REAL `composeRuntime({bootstrapFn: bootstrapWithEverything})`, constructs nothing. `[P-5] ceiling.create outcome = OK`, no /is missing from HandlerContext/ match.",
    "verdict": "REGISTERED and DISPATCHABLE. The ten-times storeKey defect does NOT apply."
  },

  "B_handler": {
    "path": "plugins/ceiling/src/handlers/CreateCeiling.ts",
    "type": "ceiling.create (:32)",
    "affectedStores": "['ceiling'] (:33)",
    "validates_before_mutation": "YES — canExecute :35-50: validateCeilingBoundary, `ceilingHeight > 0 && finite`, `thickness > 0 && finite`, and `thickness < ceilingHeight`. Typed DomainErrors from execute: CeilingGeometryError (:65, :69, :77), CeilingSchemaError (:74). ⚠ ONE INVARIANT IS EXECUTE-ONLY: the duplicate-id check `if (existing[id]) throw new CeilingGeometryError(...)` at :76-77 has NO canExecute counterpart, so a duplicate id surfaces as a THROW rather than a validation refusal. That is the only asymmetry in the four B1 create handlers.",
    "mutates_only_via_immer_draft": "YES — :79-81 `produceCommand<CeilingsState>(ctx.stores.ceiling, draft => { (draft as Record<string, CeilingData>)[ceiling.id] = ceiling; })`. The pre-check at :76 READS `ctx.stores.ceiling` (not a singleton). No store-singleton import anywhere.",
    "geometry_deferred_to_FrameScheduler": "N/A at the handler — geometry is built by CeilingPanelBuilder off the legacy-store DOM event (initBuilders.ts:410 `window.addEventListener('bim-ceiling-added', …) -> ceilingBuilder.buildCeiling(data)`). `grep -rn requestAnimationFrame plugins/ceiling/src` -> 0.",
    "emits_typed_domain_event": "NOT BY THE HANDLER. CommandEventBridge emits `ceiling.created`.",
    "otel_span": "YES — `withHandlerSpan(this.type + '.handler', {'pryzm.command.type': this.type}, …)` wraps the whole execute body (:53, closed :83).",
    "id_pre_generated_in_tool": "YES at the live tools — CeilingPlanToolHandler.ts:300 and :406 both `const ceilingId = createId('ceiling')` and pass it. BUT the handler still accepts `cmd.id ?? createId('ceiling')` (:54) and the CEB reads `id: p.id` (:1024) off the REQUEST payload, so an id-less caller is dropped: PROBE P-11 measured `ceiling.created id=undefined`, and ceilingCreatedMirror.ts:135 guards `!ev.id`. Same class as slab and curtain-wall; wall is the only immune family (CommandEventBridge.ts:508-517).",
    "must_not_violations": [
      "window.commandManager — 1 CODE site: UpdateCeilingsSystemTypeBatch.ts:94",
      "window.dispatchEvent — 2 CODE sites: UpdateCeilingsSystemTypeBatch.ts:118, :155",
      "NONE of these is in CreateCeiling.ts — the same grep over that file returns 0. The CREATE verb is clean; the batch verb is not.",
      "No DOM access, no requestAnimationFrame, no import from src/engine or src/ui anywhere in plugins/ceiling/src."
    ],
    "must_not_grep": "grep -rn \"window\\.commandManager|commandManager\\.execute|window\\.dispatchEvent|requestAnimationFrame\\(|document\\.getElementById|document\\.querySelector|from '@app/|src/engine|src/ui\" plugins/ceiling/src --include=*.ts | grep -vE \":\\s*(//|\\*|/\\*)\""
  },

  "C_lifecycle": {
    "ceb_case": "YES x2, GEOMETRY-COMPLETE and the ONLY B1 case whose field set I could not fault. CommandEventBridge.ts:1005 `case 'ceiling.create'`, :1038 `case 'ceiling.batch.create'`. Fields: id, levelId, boundary (Vec3[]), ceilingHeight, thickness, materialId, materialColor. The last two were added by §FIX-CEILING-BRIDGE-FINISH (L-973 · C84 EI-2a) — before that the bridge hardcoded the whole finish specification over an L0 schema that carried both. MEASURED (probe P-9): `ceiling.created id=ceiling_… boundary=[{x:0,y:0,z:0},…]` — the ceiling is the ONLY B1 family whose event carried its geometry field verbatim in my run.",
    "ceb_reads": "record.payload (the REQUEST), not record.forward.",
    "initTools_bridge_tag": "§P3.2-CL",
    "bridge_grep": "$ grep -n \"bridge registered\" apps/editor/src/engine/initTools.ts   ->   2202:        console.log('[initTools] §P3.2-CL: ceiling.created bus→legacy-store bridge registered.')",
    "bridge_body": "apps/editor/src/engine/initTools.ts:2159-2203; the guard + record mapping were EXTRACTED to apps/editor/src/engine/ceilingCreatedMirror.ts (§FIX-CEILING-BRIDGE-FINISH, L-973) so a test can execute them.",
    "bridge_guard": "ceilingCreatedMirror.ts:134-135 — `if (ev.commandType !== 'ceiling.create') return null; if (!ev.id || !ev.boundary || ev.boundary.length < 3) return null;` then initTools.ts:2183 `if (ceilingStore.has(ceilingRecord.id)) return;` (dedup). ⚠ The mirror's commandType guard accepts ONLY 'ceiling.create' — the batch CEB case at :1038 deliberately re-stamps `commandType: 'ceiling.create'` on each per-element event so batch creates pass it. MEASURED (probe P-12): `ceiling.batch.create` -> one `ceiling.created id=ceiling_…`.",
    "vdt_registerElement": "YES — initTools.ts:2191, INSIDE the try, AFTER ceilingStore.add().",
    "bimManager_registerElement": "YES — initTools.ts:2192, same position, wrapped in try/catch marked non-fatal.",
    "bridge_invariant_7": "HELD as the C11 §11 legend literally states it (registration AFTER add). ⚠ Same two structural notes as slab: both calls sit INSIDE the try that wraps add(), so an add() throw skips spatial registration (the catch logs '[initTools] §P3.2-CL: failed to mirror ceiling … mesh may not build'); and the dedup guard RETURNS before either registration.",
    "bimManager_via_window_any": "NO. Injected ToolsParams field (initTools.ts:267 `bimManager: any`, :312 `ceilingStore: CeilingStore`). `grep -n \"window\\.bimManager\" apps/editor/src/engine/initTools.ts` -> 0 hits. An `any` seam on bimManager (L-980), not a C14 LP-01 / P4 window-as-namespace violation. ⚠ SEPARATELY: initBuilders.ts:399 assigns `window.ceilingStore = ceilingStore` (marked TODO(TASK-08)) — a real window seam, but outside the create bridge.",
    "in_GEOMETRY_ELEMENT_TYPES": true,
    "geometry_types_site": "packages/core-app-model/src/views/ViewDependencyTracker.ts:42-49 — 'ceiling' present. ALSO in PLAN_INCREMENTAL_SAFE_TYPES (:69-71) so a ceiling create is graft-eligible O(dirty) reprojection.",
    "legacy_store_emits_storeEventBus": "YES — packages/core-app-model/src/stores/CeilingStore.ts:159-164 `storeEventBus.emit({elementId: clone.id, elementType: 'ceiling', operation: 'create', …})` inside add(), immediately after `_bus.emit('bim-ceiling-added', {id: clone.id})` at :156. (C11 §11 cites 'line 148'; the emit is at :159 in the file as it stands today — a stale line number, not a wrong claim.)",
    "update_verbs_covered": "NO — ceiling is a CREATE-MIRROR-WITHOUT-UPDATE-MIRROR family. ELEMENT_UPDATE_VERBS (CommandEventBridge.ts:241-292) has ZERO ceiling rows (8 rows total: slab x5, roof x2, column x1). MEASURED on the real bus (probe P-13): `ceiling.setHeight` -> outcome OK, and NO `element.updated` was emitted (the only one in the run was slab.setThickness). `ceiling.updateLayers` has its own CEB case at :1342. tools/ga-gate/mirror-debt.json carries 5 ceiling rows: 3 UNMIRRORED (ceiling.delete, ceiling.setBoundary, ceiling.setHeight), 1 'refuses' (ceiling.setMaterial), 1 'mirrored-elsewhere' (ceiling.changeLevel, carried by the §L-946 element.level-changed channel).",
    "the_ceiling_update_hazard_already_on_record": "apps/editor/__tests__/CeilingUpdateReachesGeometryStore.test.ts documents that TWO handlers claim `ceiling.update` — the plugin UpdateCeilingHandler (writes the DTO store nothing reads) and the initBusHandlers.ts:631-636 legacy bridge (writes the geometry store) — and that the plugin wins because composeRuntime registers first and CommandBus.register throws on a duplicate. Every ceiling move, material pick and property edit 'reported success and reached nothing'. That is an UPDATE-path finding, but it is the same root as the missing ELEMENT_UPDATE_VERBS rows.",
    "mirror_gate": "npx tsx tools/ga-gate/check-mirror-completeness.ts -> RC=0. D3: '200 verbs WRITE a plugin DTO store — 44 with a bridge case, 156 without' · 'ledger: 156 row(s) — 109 kind=UNMIRRORED, 47 declared-exempt' · 'census (initTools.ts): 19 *.created mirror(s) · 1 *.updated mirror(s)'."
  },

  "D_seven_facts": {
    "authored":     {"v": "YES", "proof": "plugins/ceiling/src/handlers/CreateCeiling.ts:32 `readonly type = 'ceiling.create'`; 9 handler files under plugins/ceiling/src/handlers/."},
    "dispatchable": {"v": "YES", "proof": "PROBE P-5 on the REAL composeRuntime bus (constructs nothing): `[P-5] ceiling.create outcome = OK`. storeKey 'ceiling' (PluginRegistry.ts:415) === affectedStores ['ceiling'] === ctx.stores.ceiling (20 reads, no other key). bootstrap.everything.test.ts:59 asserts rt.stores.ceiling instanceof CeilingStore on the real registry walk."},
    "reachable":    {"v": "YES", "proof": "W4i RECOVERY 2026-08-31 — was UNVERIFIED, now EXECUTED. audit/element-creation/2026-08-29/probe/w4i/w4iR.probe.test.ts arm R-1 (RC=0, 7/7; LOG-R.txt, runR2.txt) joins the halves on ONE real composeRuntime: `[R-1] ceiling.create outcome = OK` -> `[R-1] real ceiling.created captured = true` (subscribed on rt.events, the SAME channel and event name initTools uses) -> the REAL exported production mirror `ceilingRecordFromCreatedEvent(ev, {existingCeilingCount})` returns a record -> the REAL CeilingStore (`new camMod.CeilingStore()`, the class initBuilders.ts:399 news) accepts it -> `[R-1] CeilingStore readback = PRESENT`. Prior run additionally measured the store's own downstream signals: `[C-1] DOM bim-ceiling-added details = [{\"id\":\"ceiling_…\"}]` and `[C-1] storeEventBus ceiling events = [{\"elementType\":\"ceiling\",\"operation\":\"create\"}]`. ⚠ THE ONE LINK STILL NOT EXECUTED, NAMED EXACTLY: initTools.ts:2161 `runtime.events.on('ceiling.created', (ev) => {…})` — the SUBSCRIPTION. `initTools.bootstrap()` needs a canvas + WebGL so it cannot be called headless. Both sides of that line are executed on the same emitter; the residual risk is 'does that one .on() line run', not 'does any of this work'. Note the 2026-08-29 premise `rt.stores.elements` has no 'ceiling' kind is CORRECT and IRRELEVANT: ceiling has no module singleton (composeRuntime registers only door/window/wall/slab/room), so the authoritative store is the engine's own `new CeilingStore()`, which is what R-1 drives."},
    "renders_3d":   {"v": "YES", "proof": "W4i RECOVERY 2026-08-31 — was UNVERIFIED, now EXECUTED, and the prior run's `[D-1] MESHES = 0` is REFUTED as a HEADLESS CLOCK ARTEFACT, not a defect. ROOT: `CeilingPanelBuilder.buildCeiling()` (packages/geometry-slab/src/ceiling/CeilingPanelBuilder.ts:108-113) does not build — it ENQUEUES and defers to `getFrameScheduler().schedule('pre-render', () => this._drainBuildQueue())` per C11 §2 step 3 / P3. happy-dom never pumps rAF, so the tick never fired; a 500 ms setTimeout cannot substitute for a frame. Probe R-1 gives the REAL FrameScheduler singleton a clock via the frame-scheduler package's OWN exported `FakeRafAdapter` (stub-ledger S1 — TIME is substituted, the scheduler, its tick-listener registry and `_drainBuildQueue` all run for real): `[R-1] scheduler running = true` · `[R-1] AFTER enqueue, BEFORE any frame: meshes = 0` · `[R-1] pumpCount = 4 · totalTicks = 4` · `[R-1] MESHES before/afterEnqueue/afterPump = 0 / 0 / 1 · verts = 36` · `[R-1] builder root = PRESENT · root.userData.id = ceiling_… · root.userData.elementType = ceiling · root.userData.version = 1`. CONTROL `[R-1c] CONTROL no-pump MESHES = 0` — so R-1 measured the DRAIN, not the constructor. The bimManager stand-in is stub-ledger S2 (the builder calls exactly getLevelById), the same substitution composedBusElementReadback.test.ts makes for WallStore's level authority."},
    "renders_plan": {"v": "YES", "proof": "W4i RECOVERY 2026-08-31 — was UNVERIFIED, now EXECUTED. The prior `[E-1] GEOMETRY_ELEMENT_TYPES = null` is a fact about the MODULE SURFACE, not the family: ViewDependencyTracker.ts:80 is `const GEOMETRY_ELEMENT_TYPES = new Set([` with NO `export`. Reading the literal would prove only that a string is in a Set, which the existing suite's own C4 control shows is insufficient ('stair-railing' is a member and matches NOTHING). So the tracker was DRIVEN: probe/w4i/w4iPlan.probe.test.ts (RC=0, 10/10; runPlan1.txt) instantiates the REAL ViewDependencyTracker with the same four heavy-singleton mocks views/__tests__/lightingAndRoomReachThePlanView.test.ts uses. P-1/P-2/P-3: a ceiling create/update/delete -> `reprojected == ['vd-plan-l0']` with no §G3-STALE-EVENT warn. FOUR CONTROLS: C1 'water' -> [] (the harness can say NO); C2 an UNREGISTERED ceiling id -> BOTH plans + §G3-STALE-EVENT (registration is load-bearing, which is what §P3.2-CL initTools:2191 supplies); C3 an L0 ceiling does not dirty the L1 plan; C4 the spelling control. The elementType string is MEASURED not transcribed — 'ceiling' is what the REAL CeilingStore put on storeEventBus in `[C-1]`. PRECONDITION (1) 'something draws it' ALSO HOLDS and was measured: 'ceiling' IS in EdgeProjectorService.CACHEABLE_ELEMENT_TYPES (:2047, §57 Day 5 Round 37), the layer map keys `ceiling: 'A-CEIL'` (:207), and the version stamp that gate depends on is real (`root.userData.version = 1`, measured in R-1). ⚠ NAMED ARCHITECTURAL LIMIT, NOT A DEFECT: A-CEIL is DELIBERATELY excluded from CUT_ELIGIBLE_PLAN_LAYERS (EdgeProjectorService.ts:436-456 — 'above the cut plane by definition; it is a reflected-ceiling concept, not a cut one'). A ceiling draws in the REFLECTED CEILING PLAN ('ceiling-plan'), not poché'd in a 1.2 m floor-plan cut. YES here means the mutation reaches the plan-view pipeline and the family is projectable, cacheable and version-stamped — it does NOT mean a ceiling appears in a floor plan, and it should not."},
    "persists":     {"v": "YES", "proof": "snapshotFamilyCoverage.ts:133 `{storeKey:'ceiling', status:'via-legacy-twin', snapshotKey:'ceilings', reason:'Serialized from the legacy CeilingStore; optional key, omitted when none authored.'}`. ProjectSerializer.ts:1550 `const ceilings = ceilingStore ? ceilingStore.getAll().map(c => deepStrip(c)) : []` and :1658 `ceilings: ceilings.length > 0 ? ceilings : undefined`. npx tsx tools/ga-gate/check-snapshot-family-coverage.ts -> RC=0, '30 family/families, 30 row(s), sets equal in both directions' · '26 persistence claim(s) verified against the snapshot's own fields and writers · 3/3 declared losses' (bathroomPod, section, structural — ceiling is not one). The gate prints 'NOT ESTABLISHED HERE: that any family ROUND-TRIPS.'"},
    "exports":      {"v": "YES", "proof": "⭐ MOVED NO -> YES by Wave 4b (§W4B-EXPORT / §W4B-CEILING-EXPORTS, 2026-08-31), and RE-MEASURED end-to-end by W4i RECOVERY. The 2026-08-29 NO was CORRECT WHEN WRITTEN; the missing arm has since been built and is UNCOMMITTED in the working tree. (1) `git status --porcelain` -> `?? packages/file-format/src/export/ifc/readers/CeilingReader.ts`, ` M .../FragmentReader.ts`, ` M .../ExportIFC.ts`. (2) `grep -n -i ceiling .../FragmentReader.ts` -> `:49 import { CeilingReader } from './readers/CeilingReader'` · `:68 ceilingStore?: CeilingStore` · `:145 if (this.stores.ceilingStore) {` · `:147 elements.push(...new CeilingReader(this.stores.ceilingStore, this).read())`. (3) `grep -n -i ceiling .../ExportIFC.ts` -> `:58 ceilingStore: window.ceilingStore ?? undefined` (window.ceilingStore is assigned at initBuilders.ts:399). EXECUTED, Wave 4b's own acceptance: `cd packages/file-format && npx vitest run __tests__/ifc-export-floor-ceiling-lift.test.ts` -> RC=0, `Test Files 1 passed (1) · Tests 6 passed (6)`; the CEILING case asserts ifcClass IfcCovering, predefinedType CEILING, geometry.vertices.length > 0, and TWO CONTROLS reproduce the pre-fix state (empty scene -> undefined; unsupplied store -> 0 elements) so the pass is not vacuous. ⚠ THAT TEST HAND-BUILDS THE MESH (`meshFor()` makes a THREE.Group with userData.id), and CeilingReader is mesh-gated (`const mesh = this.ctx.findMesh(ceiling.id); if (!mesh) continue;`) — a hand-made mesh cannot establish that the REAL builder makes one the reader can find. W4i RECOVERY arm R-2 CLOSES THAT: it runs the REAL FragmentReader over the REAL CeilingStore the REAL mirror wrote and the scene the REAL CeilingPanelBuilder filled in R-1 — `[R-2] elements = 1 · ceiling element = PRESENT` · `[R-2] ifcClass = IfcCovering · predefinedType = CEILING · verts = 108`, with `[R-2c] CONTROL empty-scene elements = 0`. The IFC class was already defined and mapped before the fix (CoreElement.ts:80 `ceiling: 'IfcCovering'`, CeilingDataSchema.ts:108, IfcModelBuilder.ts:49) — this was AUTHORED-BUT-UNWIRED and Wave 4b wired it rather than minting a rival class. ⚠ STILL PARTIAL, one pipeline of two: `grep -rn 'snapshot.ceilings|\\.ceilings' plugins/ifc-export/src` -> ZERO hits; the IFC4X3Exporter / orchestrator pipeline has no ceiling arm. HISTORICAL RECORD OF THE 2026-08-29 NO: CEILINGS DID NOT REACH IFC. THREE independent absence commands, each with its output: (1) `grep -rn \"CeilingReader\" --include=*.ts .` (node_modules excluded) -> ZERO hits repo-wide, exit 0 with no lines; (2) `ls packages/file-format/src/export/ifc/readers/` -> Beam, Column, CurtainWall, Furniture, Handrail, Plumbing, ReaderContext, Roof, Room, Slab, Stair, Wall, WindowDoor, commonPsets — NO CeilingReader.ts, and FragmentReader.ts:87-129 instantiates 13 readers, none of them a ceiling one; (3) `grep -rn \"snapshot.ceilings|\\.ceilings\" plugins/ifc-export/src packages/file-format/src/export --include=*.ts` -> ZERO hits, while `snapshot.slabs` hits at IFC4X3Exporter.ts:320 and orchestrator.ts:127. The IFC CLASS is defined and mapped (CoreElement.ts:80 `ceiling: 'IfcCovering'`, CeilingDataSchema.ts:108 `ifcClass: z.literal('IfcCovering')`, IfcModelBuilder.ts:49 maps IfcCovering -> WEBIFC.IFCCOVERING) — so this is AUTHORED-BUT-UNWIRED at the export layer (D2: build the reader, do not invent a rival class), not an unmodelled family."}
  },
  "first_failing_link": "NONE — as of W4i RECOVERY (2026-08-31) all SEVEN CREATE facts are YES with executed evidence. The 2026-08-29 verdict read 'exports — NO, the only outright NO' with reachable / renders_3d / renders_plan UNVERIFIED; all four have since moved (exports by Wave 4b's CeilingReader; the other three by execution — see each fact). ⚠ THE ROW IS NOT 'CEILING IS FINE'. TWO residuals and the whole UPDATE path remain: (a) ONE link in `reachable` is source-pinned, not executed — initTools.ts:2161 `runtime.events.on('ceiling.created', …)`, which needs a canvas + WebGL; (b) `renders_plan = YES` means the mutation reaches the plan pipeline and the family is projectable/cacheable/version-stamped — A-CEIL is deliberately NOT cut-eligible in a floor plan (it is a reflected-ceiling concept); (c) the seven facts are about CREATE ONLY. The FIRST FAILING LINK FOR THE FAMILY AS A WHOLE is now the UPDATE path: ZERO ceiling rows in ELEMENT_UPDATE_VERBS and 3 UNMIRRORED verbs (B1-CEIL-02, measured: `ceiling.setHeight` -> OK with no element.updated), plus the id-less-create guard (B1-CEIL-03) and the rival `ceiling.update` handler (B1-CEIL-05).",
  "W4i_recovery_2026_08_31": {
    "artefacts": "audit/element-creation/2026-08-29/probe/w4i/ — w4iR.probe.test.ts + vitest.w4iR.config.ts -> LOG-R.txt / runR1.txt / runR2.txt (RC=0, 7/7); w4iPlan.probe.test.ts + vitest.w4iPlan.config.ts -> runPlan1.txt (RC=0, 10/10); w4b-export-run.txt (RC=0, 6/6). Prior lane: w4i.probe.test.ts -> LOG.txt (RC=0, 9/9). NO production file was modified.",
    "capability_search_before_wiring": "Asked by WHAT IT DOES, never by a name: 'what answers did-the-create-land-in-the-store-the-renderer-reads' -> composedBusElementReadback.test.ts (MT-01), REUSED as the model; 'what answers does-family-X-reach-the-plan-view' -> views/__tests__/lightingAndRoomReachThePlanView.test.ts, its harness and four control shapes REUSED verbatim; 'what turns a *.created event into a legacy record' -> ceilingCreatedMirror.ts, already extracted for exactly this reason (§FIX-CEILING-BRIDGE-FINISH L-973) — CALLED, not reimplemented; 'what pumps a headless frame' -> FakeRafAdapter, the frame-scheduler package's OWN exported adapter — USED, not written. NOTHING WAS MISSING; the gap was that four executable halves had never been JOINED. No rival was built.",
    "stub_ledger": "S1 FakeRafAdapter — TIME only; the real FrameScheduler, its tick-listener registry and _drainBuildQueue all run. S2 bimManager level authority — CeilingPanelBuilder calls exactly getLevelById (same substitution composedBusElementReadback.test.ts makes for WallStore). S3 THREE.Scene is a real new Scene(). S4 (plan probe only) the four heavy singletons ViewTechnicalDrawingCache / ViewDefinitionStore / ElementRegistry / UnifiedFrameLoop / StoreEventBus, mocked exactly as the pre-existing lightingAndRoomReachThePlanView suite mocks them; ViewDependencyTracker itself is REAL and is the object under measurement. Nothing else is substituted; the runtime is a REAL composeRuntime({bootstrapFn: bootstrapWithEverything}).",
    "REFUTED_prior_finding": "⛔ The prior run's `[C-1] record materialId/materialColor = undefined / undefined` is NOT a loss and must not be carried forward. It read the WRONG KEYS: ceilingCreatedMirror.ts:156/161 maps the L0 fields ONTO finishSpec. MEASURED AND ASSERTED in R-1: `[R-1] authored finish carried: finishSpec.soffitColor = #eeeeee · finishSpec.soffitMaterialId = MAT-GYPSUM · (top-level materialId = undefined, which is NOT where it lives)`, with expect(...).toBe('#eeeeee') / .toBe('MAT-GYPSUM'). §FIX-CEILING-BRIDGE-FINISH (L-973) is INTACT.",
    "REFUTED_prior_measurement": "⛔ The prior run's `[D-1] MESHES = 0` was a HEADLESS CLOCK ARTEFACT, not a defect, and reading it as a failure would have been the pessimistic-stale error. buildCeiling() only ENQUEUES; the drain is a pre-render tick. See the renders_3d fact."
  },

  "E_c11_ss11_crosscheck": {
    "matrix_row": "| **Ceiling** | ✅ `ceiling.create` + `ceiling.batch.create` | ✅ §P3.2-CL | ✅ `CeilingStore.add()` (line 148) | ✅ `'ceiling'` | ✅ (Rev 4 — FIX-PLAN-VDT-BIMMANAGER-ALL-BRIDGES) | ✅ | ✅ FULL |  (C11 §11, dated 2026-05-19)",
    "verdict": "CONFIRMED on all six cells as the matrix DEFINES them — ceiling is the one B1 family whose row I could not refute cell-by-cell. The Status column 'FULL' is nonetheless MISLEADING, because the matrix's six columns stop at the mesh.",
    "what_CONFIRMS": "cell 1 CEB case exists and is geometry-complete (:1005, :1038, and probe P-9 carries `boundary` verbatim); cell 2 §P3.2-CL bridge exists (initTools.ts:2202); cell 3 CeilingStore.add() DOES emit storeEventBus (:159, inside add(); the cited 'line 148' is stale by 11 lines, not wrong); cell 4 'ceiling' IS in GEOMETRY_ELEMENT_TYPES (:42); cell 5 VDT + bimManager ARE called AFTER add() (:2191/:2192) — ceiling is the family the column legend was written FOR; cell 6 3D via bus, not commandManager.",
    "what_FULL_does_NOT_cover": "The matrix has no EXPORT column and no UPDATE column. Measured: ceilings reach NO IFC pipeline (three absence commands above), and ceiling has ZERO ELEMENT_UPDATE_VERBS rows plus 3 UNMIRRORED verbs — so `ceiling.setHeight` and `ceiling.setBoundary` commit, report success and change nothing on screen (probe P-13: setHeight OK, no element.updated). A row can be honestly FULL under this matrix and still be a family the user cannot edit or export.",
    "column_legend_note": "The VDT+bimManager legend ('AFTER LegacyStore.add()') is correct FOR CEILING and slab, and contradicted by WALL and CURTAIN-WALL, both of which hoist the registrations ABOVE add() by name (§G3-STALE-FIX / §G3-STALE-FIX-CW). The legend should describe the ordering as family-specific rather than universal."
  },

  "F_findings": [
    {"id": "B1-CEIL-01", "severity": "P1", "class": "AUTHORED-BUT-UNWIRED (export)", "status": "RESOLVED 2026-08-31 by Wave 4b (§W4B-CEILING-EXPORTS) — CeilingReader.ts now exists, FragmentReader:147 runs it, ExportIFC:58 supplies window.ceilingStore. Verified twice: packages/file-format __tests__/ifc-export-floor-ceiling-lift.test.ts RC=0 6/6, and W4i RECOVERY arm R-2 end-to-end from the REAL builder's mesh (IfcCovering/CEILING, 108 verts). ⚠ RESIDUAL, NOT CLOSED: the plugins/ifc-export IFC4X3Exporter/orchestrator pipeline still has no ceiling arm — one pipeline of two. The claim below is the 2026-08-29 record.", "claim": "Ceilings reach NO IFC export pipeline. No CeilingReader exists repo-wide, FragmentReader instantiates 13 readers and none is a ceiling one, and `snapshot.ceilings` appears in neither ifc-export exporter — while `ceiling -> IfcCovering` is already defined in CoreElement.ts:80, CeilingDataSchema.ts:108 and IfcModelBuilder.ts:49.", "site": "packages/file-format/src/export/ifc/FragmentReader.ts:87-129 (absence) · plugins/ifc-export/src (absence)", "disposition": "WIRE"},
    {"id": "B1-CEIL-02", "severity": "P1", "class": "CREATE-MIRROR-WITHOUT-UPDATE-MIRROR", "claim": "ZERO ceiling rows in ELEMENT_UPDATE_VERBS; 3 UNMIRRORED verbs in mirror-debt.json. Measured: `ceiling.setHeight` returns OK and emits no `element.updated`, so the height change never reaches the geometry store, the plan projector or the snapshot.", "site": "packages/runtime-composer/src/CommandEventBridge.ts:241-292 + tools/ga-gate/mirror-debt.json", "disposition": "WIRE"},
    {"id": "B1-CEIL-03", "severity": "P1", "class": "ID-MINTED-IN-HANDLER-BRIDGE-GUARDS-ON-ID", "claim": "`ceiling.create` with no `id` emits `ceiling.created id=undefined`; ceilingCreatedMirror.ts:135 returns null. The plan tools pre-generate by convention only.", "site": "packages/runtime-composer/src/CommandEventBridge.ts:1024 `id: p.id` (contrast wall :508-517)", "disposition": "WIRE"},
    {"id": "B1-CEIL-04", "severity": "P2", "class": "DOC-CONFLICT", "claim": "C11 §11 marks Ceiling FULL. Every cell it defines is confirmed, but the matrix has no export and no update column, and the family fails both. 'FULL' therefore reads as a production-readiness verdict the six columns cannot support.", "site": "docs/02-decisions/contracts/C11-ELEMENT-CREATION-PIPELINE.md §11", "disposition": "DOCUMENT"},
    {"id": "B1-CEIL-05", "severity": "P2", "class": "RIVAL-HANDLER", "claim": "Two handlers claim `ceiling.update`; the plugin DTO handler wins registration order and the initBusHandlers.ts:631-636 legacy bridge that writes the geometry store is never registered. Already documented by its own suite, and it is the same root as B1-CEIL-02.", "site": "apps/editor/__tests__/CeilingUpdateReachesGeometryStore.test.ts header · apps/editor/src/engine/initBusHandlers.ts:631-636", "disposition": "DOCUMENT"},
    {"id": "B1-CEIL-06", "severity": "P3", "class": "VALIDATION-ASYMMETRY", "claim": "The duplicate-id invariant is enforced only in execute() (`if (existing[id]) throw`), with no canExecute counterpart, so a duplicate surfaces as a thrown DomainError rather than a validation refusal.", "site": "plugins/ceiling/src/handlers/CreateCeiling.ts:76-77 vs :35-50", "disposition": "DOCUMENT"},
    {"id": "B1-CEIL-07", "severity": "P3", "class": "MISSING-PROOF", "claim": "No composition-root (D6) proof existed for `ceiling.create`; CeilingUpdateReachesGeometryStore.test.ts builds its own CommandBus and geometry store, so it cannot observe a provider failure. This audit's probe P-5 is the first.", "site": "apps/editor/__tests__/CeilingUpdateReachesGeometryStore.test.ts", "disposition": "WIRE"},
    {"id": "B1-CEIL-08", "severity": "P3", "class": "BRIDGE-ORDERING", "claim": "VDT + bimManager registration sits INSIDE the try that wraps `ceilingStore.add()`, so an add() throw skips spatial registration; the dedup guard also returns before both.", "site": "apps/editor/src/engine/initTools.ts:2183-2192", "disposition": "DOCUMENT"}
  ]
}
