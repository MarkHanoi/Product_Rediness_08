{
  "workstream": "W5 — Contract truth: land every proposed patch, close the ten doc conflicts, and gate the CLASS so they cannot rot again",
  "addresses": ["DC-01", "DC-02", "DC-03", "DC-04", "DC-05", "DC-06", "DC-07", "DC-08", "DC-09", "DC-10"],
  "audit_head": "064a838e",
  "head_at_planning": "dd2830c6",
  "measured_at": "2026-08-30",

  "STATE_CHANGE_DISCOVERED_DURING_PLANNING": {
    "HEADLINE": "⭐ W5-01 COMPLETED ITSELF DURING THIS PLANNING RUN. The eight patches went pristine -> applied-uncommitted -> COMMITTED as 19b696e1 ('docs(audit/§ELEM-AUDIT): land the measured corrections — C11 §11 covered 14 of 24 families') across three tool calls, by a concurrent lane in the shared tree. HEAD moved 064a838e -> dd2830c6 -> 19b696e1 while this plan was being written. W5-01's acceptance command now prints its target, 0 0 0 0 0, at HEAD. p08 was NOT included in that commit (ISSUE-LOG still 480 rows, L-12830 absent), so W5-02 is untouched and open.",
    "what": "First observed: the eight proposed patches ALREADY APPLIED IN THE SHARED WORKING TREE, UNCOMMITTED, HEAD pristine. Then committed as 19b696e1 (10 files: CLAUDE.md, C01, C11, plus the audit patch files and APPLY-ORDER.md).",
    "how_it_was_found": "git status --porcelain at the START of this planning run listed ONLY files under audit/. Nine tool calls later, a scoped git status listed CLAUDE.md, C01 and C11 as ' M'. Six tool calls after that, git status listed none of them and git log showed 19b696e1.",
    "why_this_is_recorded_rather_than_edited_away": "It is the same defect class W5 exists to close, one level up: a plan that transcribes a state read once is stale by the time it is executed. EVERY task below must re-read `git status --porcelain` and `git rev-parse --short HEAD` immediately before acting, and treat the readings in this file as evidence of a moment, not as the current state. [[multi-agent-shared-tree-collisions]]: never git stash (the stack is global across worktrees), commit scoped work, re-read before acting.",
    "proof": [
      "git show HEAD:docs/02-decisions/contracts/C01-ARCHITECTURE-AND-GOVERNANCE.md | grep -c 'Status — RE-MEASURED 2026-08-29' -> 0 ; worktree -> 1",
      "git show HEAD:docs/02-decisions/contracts/C11-ELEMENT-CREATION-PIPELINE.md | grep -c '| ❌ No bridge |' -> 3 ; worktree -> 0",
      "git show HEAD:<C01> | grep -cE '^[*][*]54 packages, 12 apps, 46 plugins[.][*][*]' -> 1 ; worktree -> 0",
      "git show HEAD:<C01> | grep -cE '[*][*]FAILING: 5 owner files' -> 2 ; worktree -> 0",
      "git status --porcelain -- docs/04-reference/ISSUE-LOG.md -> empty (p08 unapplied)"
    ],
    "consequence_for_this_plan": "W5-01 is NOT 'run git apply'. It is 'verify the applied state against a re-measured HEAD, then COMMIT'. Re-running the APPLY-ORDER sequence over the already-patched tree produces eight 'patch does not apply' failures that read as patch defects and are not. This is [[multi-agent-shared-tree-collisions]] exactly: never git stash, commit scoped work, re-read git status immediately before acting.",
    "APPLY-ORDER.md_is_now_STALE_in_one_respect": "It states §5's table 'sits at line 225 in the pristine file and at 301 after the three preceding patches'. Measured on the PRISTINE HEAD blob, '## §5 — CI Gate Inventory' is at line 267. The ORDER constraint it documents is still correct and load-bearing; only that line-arithmetic sentence is not re-derivable. Do not use it as a verification criterion."
  },

  "measurements_reproduced_this_run": {
    "packages/*/package.json": 103,
    "ls apps/": 13,
    "ls plugins/": 51,
    "ls tools/ga-gate/*.ts": 75,
    "withHandlerSpan files (packages apps plugins, .ts, no node_modules)": 293,
    "(window as any) in apps/editor/src/ui": 70,
    "apps/editor/src/ui .ts/.tsx files": 1118,
    "find src -mindepth 1 -maxdepth 1 -type d": 0,
    "find src -mindepth 1 -maxdepth 1 -type f": 7,
    "PLUGIN_CATALOG descriptors with kind 'element'": 18,
    "ELEMENT_PLUGIN_IDS rows": 27,
    "UNION of those two id sets": 28,
    "C11 §11 matrix rows (24 family + 3 member)": 27,
    "§11.x headings stamped (2026-05-19)": 17,
    "'(2026-05-19)' occurrences anywhere in C11": 19,
    "ISSUE-LOG max L- id": 12824,
    "ISSUE-LOG L- rows": 480,
    "p08 rows proposed": 17,
    "gates whose subject is C11": 0,
    "gates whose subject is C01": 0
  },

  "the_class_fix_in_one_paragraph": "Ten DOC-CONFLICTs, one shape: a NUMBER or a SET was TRANSCRIBED into a document and the subject moved. check-contract-index-equivalence.ts already proves the cure for one instance — it compares ls contracts/ against README's row SET in BOTH DIRECTIONS, derives everything, transcribes nothing, and types every declared absence as RESERVED so silence can never pass for a decision. NO GATE ANYWHERE HAS C11 OR C01 AS ITS SUBJECT (grep -ln 'C11-ELEMENT-CREATION' tools/ga-gate/check-*.ts -> 0 files; same for C01). W5 builds the two that close the class: check-element-matrix-equivalence.ts (C11 §11's row set vs the element-family id set derived from code, both directions) and check-doc-derived-counts.ts (every count CLAUDE.md / C01 §3/§4/§5 asserts is re-derived by the gate and compared). Landing the eight patches without those two gates fixes ten conflicts and guarantees an eleventh.",

  "gate_specification_check_element_matrix_equivalence": {
    "answer_to_the_brief_question": "YES. An equivalent of check-contract-index-equivalence.ts CAN compare C11 §11's matrix rows against the measured family census, and can do so WITHOUT transcribing a single family name, because the repo already contains TWO independent machine-readable declarations of the element-family id set. Nothing needs inventing. The only prerequisite is that the matrix row KEY be the canonical registry id (W5-05) — today three rows use the verb stem instead (annotation/dimension/room vs annotations/dimensions/rooms).",
    "subject_set_derived_from": [
      "SOURCE 1 — packages/runtime-composer/src/PluginHost.ts, PLUGIN_CATALOG entries whose third desc() argument is 'element'. MEASURED 18: balcony beam boundary-line ceiling column curtain-wall door floor furniture handrail lift plumbing pool roof slab stair wall window.",
      "SOURCE 2 — apps/editor/src/PluginRegistry.ts, ELEMENT_PLUGIN_IDS. MEASURED 27.",
      "UNION = 28 ids. Union and not either source alone, because the two disagree BY DESIGN and check-plugin-census-equivalence.ts arms F/G already police that disagreement — this gate must not become a second referee of it (C84 EI-9)."
    ],
    "row_set_derived_from": "Lines matching ^[|] [*][*]<key>[*][*] [|] between the '## §11 — Per-Element Pipeline Compliance Matrix' heading and the next '### §11.' heading. MEASURED 27 (24 family + 3 member). Prose mentions are NOT rows — the precedent gate records that a sweep keyed on a NAME is defeated by using a different name.",
    "arms": {
      "F0 (floors, exit 2)": "C11 parses; >= MIN_ROWS(20) matrix rows; >= MIN_KIND_ELEMENT(15) from SOURCE 1; >= MIN_EPI(20) from SOURCE 2; two planted controls run INSIDE every invocation (a synthetic id absent from the matrix must fire arm D; a synthetic row key absent from the union must fire arm B). Without F0 an emptied PluginHost.ts reports a clean sweep of nothing — L-827, and M2/R7 is that exact defect at suite scale.",
      "A (hard 0) FAMILY-WITHOUT-ROW": "every SOURCE 1 (kind='element') id has a matrix row. Measured today: 0.",
      "B (hard 0) ROW-WITHOUT-BASIS": "every matrix row key is in the UNION, or is named in the contract's declared 'ELEMENT BY SIGNAL, NOT BY REGISTRY' table. Measured today: 1 — lighting (kind='overlay', absent from ELEMENT_PLUGIN_IDS, yet carries a live §FT-LIGHTING bridge and a defineElement schema). W5-05 declares it; then 0.",
      "D (hard 0) EXCLUSION-NOT-DECLARED": "every UNION id with no matrix row must be named in the contract's declared 'NOT AN ELEMENT FAMILY' table with a reason. Measured today: 2 — selection (kind collab) and section-view (kind view), both currently asserted in PROSE, which the parser correctly refuses to read. W5-05 tables them; then 0. C76 §6 applied to the matrix: absence is typed, named and counted, never silent.",
      "E (ratchet, shrink-only, pinned in gate-newly-measured.json) STALE-SUBSECTION": "every '### §11.x' heading carrying a (YYYY-MM-DD) stamp older than the §11 matrix's own stamp must carry an explicit 'NOT RE-MEASURED' or 'RE-MEASURED <date>' marker. FIRST HONEST READING = 17. W5-07 drives it to 0."
    },
    "why_a_COUNT_would_not_have_caught_any_of_this": "DC-04 is a SET difference (14 of 24), not a count error; DC-02 is a within-section contradiction; DC-03 is three ids present in prose and absent from the table. A row-count assertion would have been satisfied by 14 correct-looking rows. The gate compares SETS and never a number — the precedent gate's own reason, one level down.",
    "registration": "tools/ga-gate/run-all.ts GATES array, beside check-contract-index-equivalence.ts at :726 — LAST, after the R5 meta-gate, because its subject is documentation and a failure here must never mask a code failure above it. run-all.ts registration is SUFFICIENT for CI: the merge-blocking ga-gate job in .github/workflows/ci.yml invokes pnpm run ga-gate:all -> run-all.ts. Adding a separate ci.yml step would mint a second registration authority."
  },

  "forbidden_fixes_named_so_they_are_not_reinvented": [
    "Raising ANY existing shrink-only ceiling, or adding ANY of the seven exit-3 gates to tools/ga-gate/gate-debt.json, to make the ga-gate job green so a doc patch can land. §RATCHET-EXCEEDED-IS-NEVER-DEBT. The ga-gate job is ALREADY red at HEAD on R1-R8 (W2/W3/W4 own those); W5 must not be blocked on it and must not clear it by ledger.",
    "Pinning a NEW gate's first honest reading in gate-debt.json. The correct home is tools/ga-gate/gate-newly-measured.json (32 entries, created 2026-08-12), per C76 §6.3 and run-all.ts:710-724: a defect that PREDATES the gate is neither debt nor regression. That is a DIFFERENT act from absorbing an exit-3 and is the only baseline W5 may create.",
    "Hard-coding the 24-family list into check-element-matrix-equivalence.ts. That mints a THIRD authority for 'what is an element family' beside PLUGIN_CATALOG.kind and ELEMENT_PLUGIN_IDS — C84 EI-9 violation, and the gate could then rot exactly the way the matrix did.",
    "Deleting the curtainwall.create alias to make a grep reach 0. It is a LIVE registered bus alias (plugins/curtain-wall/src/handlers/CreateCurtainWall.ts:51, canonicalised in packages/command-bus/__tests__/command-aliases.test.ts:92) and a command type is a wire identifier replayed out of project_command_log.",
    "Re-running the APPLY-ORDER git apply sequence over the already-patched working tree, then reporting the eight failures as patch defects.",
    "Using a bare substring grep as an acceptance criterion on any of these files. Four of the patches' first-revision criteria were self-defeating for exactly this reason: each correction QUOTES the text it retracts, and a substring grep cannot tell an assertion from a quotation of a retracted assertion. Every criterion below anchors on an ASSERTION FORM (line-initial **, a full table CELL) — never a substring.",
    "Writing a new *-AUDIT-*.md derivative doc for any of this. Discipline Rule 1: edit the canonical C0N-*.md in place."
  ],

  "residuals_this_workstream_does_not_close": [
    "DC-05's second half: check-otel-spans.ts is RC=3 (Zone B 66 of 87 vs shrink-only baseline 52; Zone C UNGATED at 2100 of 2399). W5 corrects what C11 SAYS about it; W3 fixes the 14 files. State both halves or neither.",
    "The open question p01 records and does not settle: whether CurtainWallPlanToolHandler._commit() dispatches the ALIAS curtainwall.create (which the bus canonicalises but the CEB, switching on the raw case label, does not). W5-04 measures it; if it dispatches the alias that is a CODE defect for W2, not a doc defect.",
    "check-doc-derived-counts.ts closes a count that is MARKED. It cannot see a count written with no marker. Named, not hidden — arm A (value mismatch) and arm B (unknown derivation id) are hard-0, and there is deliberately NO fuzzy 'unmarked numeric claim' arm because it would be a false-positive factory."
  ],

  "tasks": [
    {
      "id": "W5-01",
      "title": "Commit the eight already-applied doc patches, after re-deriving every number they assert — ⭐ LANDED AS 19b696e1 DURING THIS PLANNING RUN; VERIFY step (2) WAS DONE",
      "STATUS_AT_WRITING": "LANDED by a concurrent lane as 19b696e1 while this plan was being written. The acceptance command prints 0 0 0 0 0 at that SHA — verified. ⚠ WHAT IS NOT VERIFIED IS STEP (2): whether the six counts were re-derived before committing, or merely committed as the patches wrote them. The residual task is to run step (2) against 19b696e1 and, if any of the six differs, correct the doc in a follow-up commit. All six reproduce at HEAD as of 2026-08-30 (103 / 13 / 51 / 75 / 293 / 70), so this is expected to be a no-op — but 'expected to be a no-op' is not a measurement, and the withHandlerSpan figure alone rotted from 253 to 293 inside twelve days.",
      "disposition": "WIRE",
      "addresses": ["DC-01", "DC-02", "DC-03", "DC-04", "DC-05", "DC-07", "DC-08", "DC-09", "DC-10"],
      "files": [
        "CLAUDE.md",
        "docs/02-decisions/contracts/C01-ARCHITECTURE-AND-GOVERNANCE.md",
        "docs/02-decisions/contracts/C11-ELEMENT-CREATION-PIPELINE.md",
        "audit/element-creation/2026-08-29/proposed-patches/APPLY-ORDER.md"
      ],
      "change": "The eight patches are ALREADY APPLIED to these three files in the shared working tree and UNCOMMITTED (HEAD blobs are pristine — verified by git show). DO NOT run git apply again. (1) Run `git diff --stat -- CLAUDE.md docs/02-decisions/contracts/C01-ARCHITECTURE-AND-GOVERNANCE.md docs/02-decisions/contracts/C11-ELEMENT-CREATION-PIPELINE.md` and confirm exactly three files, no others. (2) RE-DERIVE the six counts the patched text now asserts and confirm each equals what the docs say — `ls packages/*/package.json | wc -l` (C01 §3 must say 103), `ls apps/ | wc -l` (13), `ls plugins/ | wc -l` (51), `ls tools/ga-gate/*.ts | wc -l` (C01 §5 must say 75), `grep -rIl \"withHandlerSpan\" --include=*.ts --exclude-dir=node_modules packages apps plugins | wc -l` (C11 §0.0 and §7.2 must BOTH say 293 — p02 exists precisely because §0.0 said 253 and §7.2 said 'no span'), `grep -rn '(window as any)' apps/editor/src/ui | wc -l` (C01 §4 boolean #2 must say 70). If any differs, EDIT THE DOC TO THE FRESH NUMBER before committing — this figure has rotted at least once inside twelve days. (3) Commit all three files in ONE commit so no reader can see C11 corrected while C01 still contradicts it. Suggested subject: `docs(C01/C11/CLAUDE §DOC-TRUTH DC-01..DC-10): the matrix covered 14 of 24 families and four counts were stale — every cell in this commit was re-derived, none transcribed`. (4) In the same commit, correct the one stale sentence in APPLY-ORDER.md that says §5's table 'sits at line 225 in the pristine file': measured on the pristine HEAD blob, '## §5 — CI Gate Inventory' is at line 267.",
      "why_this_shape": "WIRE, not REPLACE. The patches EXIST, are correct, are applied, and are simply not committed — D2's canonical 'exists, nothing calls it'. Ranking this as REPLACE (rewrite the sections afresh) would discard a verified eight-patch set whose ordering constraints are load-bearing and re-derive the same analysis. The one genuine risk is the opposite of absence: an uncommitted shared working tree is destroyed by any `git checkout --` or `git stash` from a concurrent lane, so the fix is to commit it, now.",
      "acceptance_command": "cd <repo>; C11=docs/02-decisions/contracts/C11-ELEMENT-CREATION-PIPELINE.md; C01=docs/02-decisions/contracts/C01-ARCHITECTURE-AND-GOVERNANCE.md; echo \"$(grep -c '| ❌ No bridge |' $C11) $(grep -c '^| \\*\\*Curtain Wall\\*\\* | ✅ `curtainwall.create`' $C11) $(grep -cE '^[*][*]54 packages, 12 apps, 46 plugins[.][*][*]' $C01) $(grep -cE '[*][*]FAILING: 5 owner files' $C01) $(git status --porcelain -- CLAUDE.md $C01 $C11 | wc -l)\"",
      "acceptance_number": "0 0 0 0 0 — four retracted ASSERTION FORMS gone (they print 3 / 1 / 1 / 2 at HEAD) and zero uncommitted changes remaining in the three files. NOT a bare substring grep on any of the four: each correction quotes the text it retracts, so `grep -c \"No bridge\"` prints 4 on the patched copy and `grep -c \"curtainwall.create\"` prints 6 — both are correct post-patch readings, and both were wrong criteria in the patches' first revision.",
      "effort": "S",
      "depends_on": [],
      "risk": "HIGH IF DELAYED, LOW IF DONE NOW. The work exists only in an uncommitted shared working tree that other lanes are writing to; a single `git checkout --` or `git stash` elsewhere destroys eight patches of verified analysis with no recovery path. Second risk: committing without step (2) re-locks four numbers that have each already rotted — that would be this audit's own defect, committed.",
      "contract_clause": "C01 §6 Discipline Rule 1 (edit the canonical C0N-*.md in place; never a derivative *-AUDIT-*.md) · C84 EI-9 (one authority per concept — §0.0 and §7.2 must not print two readings of the withHandlerSpan count) · C01 §6.1 (an absence claim is a measurement, not an observation)"
    },
    {
      "id": "W5-02",
      "title": "Append p08's 17 L- rows to the ISSUE-LOG, renumbered from the freshly measured maximum",
      "disposition": "WIRE",
      "addresses": ["DC-01", "DC-04", "DC-05", "DC-07", "DC-08", "DC-09", "DC-10"],
      "files": [
        "docs/04-reference/ISSUE-LOG.md",
        "audit/element-creation/2026-08-29/proposed-patches/p08-ISSUE-LOG-rows.md"
      ],
      "change": "p08 is NOT a diff and has NOT been applied (git status shows ISSUE-LOG unmodified). Re-measure the maximum id FIRST — `grep -o '[*][*]L-[0-9]\\+[*][*]' docs/04-reference/ISSUE-LOG.md | grep -o '[0-9]\\+' | sort -n | tail -1` prints 12824 today, and p08 claims L-12830..L-12846 (17 rows), leaving a deliberate five-number gap so a concurrent lane appending 12825-12829 does not collide. If the maximum has moved past 12830, renumber the whole block from max+6 keeping it contiguous, and update every internal cross-reference inside p08's own row text. Append the 17 rows VERBATIM to the END of §2's table in the row format the file already uses — `| **L-NNNNN** | <text> | **STATUS** |` — then tick the affected areas in §3's area table, which is the file's own stated instruction. Every row is **OPEN**: a row whose only deliverable is a document correction stays OPEN with the proposed patch named in its text, because the patch being written is not the patch being landed.",
      "why_this_shape": "WIRE. The rows exist, fully drafted and twice-corrected, and nothing has consumed them. REPLACE would mean re-deriving 17 findings that are already measured with commands attached. REMOVE would delete the only durable register entry for defects that no gate yet catches — and MEMORY names ISSUE-LOG.md as THE living defect register (and explicitly warns that V1-LAUNCH-READINESS-AUDIT.md, cited by five earlier lane briefs, does not exist).",
      "acceptance_command": "cd <repo>; echo \"$(grep -c '^| \\*\\*L-[0-9]' docs/04-reference/ISSUE-LOG.md) $(grep -o '^| \\*\\*L-[0-9]\\+\\*\\*' docs/04-reference/ISSUE-LOG.md | grep -o '[0-9]\\+' | sort -n | uniq -d | wc -l)\"",
      "acceptance_number": "497 3 — 480 rows today plus exactly 17 new ones, and the duplicate-id count UNCHANGED at 3. ⚠ THE SECOND NUMBER IS NOT 0 AND MUST NOT BE WRITTEN AS 0: the register already carries three duplicated row ids at HEAD — L-11006, L-12084, L-12700 — measured with the row-ANCHORED form above. An unanchored `grep -o '[*][*]L-[0-9]+[*][*]'` prints 121 duplicates because L- ids are legitimately cross-referenced inside other rows' text; that form was this task's first-written criterion and was wrong, in exactly the way four of the patches' own criteria were wrong. The criterion is that 3 does not RISE: any rise means one of the 17 new ids collided, and two defects sharing an id are permanently indistinguishable in a 6 MB register. The pre-existing 3 are a separate, pre-existing defect — record them as an L- row, do not silently renumber them here.",
      "effort": "S",
      "depends_on": ["W5-01"],
      "risk": "Renumber collision. Two lanes appending to a 6 MB register at the same time both read max=12824 and both claim 12830. The uniq -d arm in the acceptance command is what catches it; without it a duplicate id is invisible and permanently ambiguous. Second risk: marking a document-correction row FIXED because W5-01 landed — the code defects those rows describe (R1-R8, G1-G6) are untouched by W5.",
      "contract_clause": "C01 §6 Discipline Rule 1 (no derivative *-AUDIT-*.md; findings land in the canonical register) · C01 §6.1 (an absence claim is a measurement, not an observation — every row carries its falsifying command) · ISSUE-LOG.md's own instruction: 'Append new issues to the bottom of §2 and tick the area table in §3.'"
    },
    {
      "id": "W5-03",
      "title": "Close p01 residual (1): C11 §0.0 line 20 still describes, in the present tense, a matrix that no longer exists",
      "disposition": "REPLACE",
      "addresses": ["DC-01", "DC-02"],
      "files": ["docs/02-decisions/contracts/C11-ELEMENT-CREATION-PIPELINE.md"],
      "change": "C11:20 reads: \"§11's normative matrix marks **handrail**, **furniture** and **lighting** as **\\\"❌ No bridge\\\"**, and **§11.1 carries an OPEN work order to build one of them.**\" After W5-01 that sentence is false — the matrix carries none of those cells and §11.1's HANDRAIL-BUS-MIGRATION row is marked DISCHARGED / DO-NOT-EXECUTE. Rewrite it in the PAST tense, naming what replaced it: \"§11's normative matrix MARKED handrail, furniture and lighting as '❌ No bridge', and §11.1 CARRIED an OPEN work order to build one of them. Both were retracted 2026-08-29 by the §11 replacement; the retracted cells are quoted in §11's own DC-01/DC-02 evidence table.\" Do not delete the quotation — it is the record of what rotted, and a correction that erases what it corrected leaves the next reader unable to tell a fixed defect from one that never existed.",
      "why_this_shape": "REPLACE, not REMOVE. The sentence is a live present-tense assertion, and p01 deliberately left it untouched so the matrix patch had exactly one subject; p01's own header names it as residual (1) and prescribes this fix in the same commit. Leaving it is the DC-01 defect restored one section up: a stale ABSENCE claim about a live bridge is what authorises a RIVAL implementation, which is strictly worse than the gap it imagined.",
      "acceptance_command": "cd <repo>; grep -n 'No bridge' docs/02-decisions/contracts/C11-ELEMENT-CREATION-PIPELINE.md | grep -vcE '^(12[0-9][0-9]|13[0-9][0-9]):'",
      "acceptance_number": "0 live occurrences of 'No bridge' OUTSIDE the §11 block (lines 1200-1399, which legitimately hold the DC-01/DC-02 evidence table quoting the retracted cells and the two 'this row previously read' history notes). VERIFIED: today that command prints 1, and the single line it prints is `20:§11's normative matrix marks **handrail**, **furniture** and **lighting** as **\\\"❌ No bridge\\\"**, and` — i.e. exactly the sentence this task rewrites. ⚠ The line-anchor `^(12..|13..):` is load-bearing: written as `:(12..|13..):` it matches nothing and the command prints 5, which was this task's first-written criterion. Re-derive the §11 range with `grep -n '^## §11 \\|^## §12' C11` before quoting the 1200-1399 literals; the numbers move.",
      "effort": "S",
      "depends_on": ["W5-01"],
      "risk": "Deleting the sentence instead of tensing it. §0.0 is the correction record; erasing it makes the next reader unable to distinguish 'this was fixed' from 'this never happened' — which is the exact confusion that produced four self-defeating acceptance criteria in the patches' first revision.",
      "contract_clause": "C84 EI-9 (one authority per concept — §0.0 and §11 must not both describe the current matrix) · C01 §6.1"
    },
    {
      "id": "W5-04",
      "title": "Close p01 residual (2): five live 'curtainwall.create is the CEB case' claims survive outside the matrix — and the open question p01 could not settle is now SETTLED, in the doc's disfavour",
      "disposition": "REPLACE",
      "addresses": ["DC-04", "DC-06"],
      "files": ["docs/02-decisions/contracts/C11-ELEMENT-CREATION-PIPELINE.md"],
      "change": "Three spellings exist for one domain and all three are real: `curtain-wall` is CANONICAL and is what the CEB switches on (`case 'curtain-wall.create'` :694, `case 'curtain-wall.batch.create'` :758; `grep -c \\\"case 'curtainwall\\\" packages/runtime-composer/src/CommandEventBridge.ts` -> 0); `curtainwall` is a REGISTERED BUS ALIAS (plugins/curtain-wall/src/handlers/CreateCurtainWall.ts:51, canonicalised per packages/command-bus/__tests__/command-aliases.test.ts:92); `curtainWall.changeLevel` (ChangeCurtainWallLevel.ts:79) is UNALIASED and is check-command-naming's actual finding, owned by W2. p01 fixed the matrix CELL only. Outside the matrix, §11.6 and §11.13 still state as live fact that the CEB case is `curtainwall.create` — MEASURED at HEAD 19b696e1: `grep -c 'curtainwall.create' C11` -> 6, of which `grep -n 'curtainwall.create' C11 | grep -vc alias` -> 5 sit in sentences that never say the word 'alias'. Rewrite those five to name the canonical case and identify `curtainwall.create` explicitly as the alias. ⭐ AND CORRECT C11:1425, WHICH IS NOW MEASURED FALSE: it states that `CurtainWallPlanToolHandler._commit()` 'dispatches `curtainwall.create`'. `grep -nE \\\"executeCommand\\\" apps/editor/src/engine/views/plantools/CurtainWallPlanToolHandler.ts` -> **:479 and :513, BOTH `window.runtime?.bus?.executeCommand('curtain-wall.create', ...)`** — the CANONICAL hyphenated type, not the alias. The seven `curtainwall` hits in that file are all `createId('curtainwall')`, a BRANDED-ID PREFIX enforced by `CurtainWall.parse()`'s `^curtainwall_[ULID]{26}$` — a different concept from a command type, and the likely source of the confusion. DO NOT DELETE THE ALIAS.",
      "why_this_shape": "REPLACE, and now PURELY a doc fix — no production file is touched and no W2 hand-off is needed. p01 recorded this as an unclosed question and named the exact next command; running it settles it against the document. D2 in its sharpest form, twice over: p01's first revision said `curtainwall.create` 'does not exist anywhere' (it is a live alias — deleting it would break replay out of project_command_log), and C11:1425 said the plan tool dispatches it (it dispatches the canonical type). Both errors point the same way — a NAME was reasoned about instead of the SITE being read.",
      "acceptance_command": "cd <repo>; C11=docs/02-decisions/contracts/C11-ELEMENT-CREATION-PIPELINE.md; echo \"$(grep -c 'curtainwall.create' $C11) $(grep -n 'curtainwall.create' $C11 | grep -vc alias) $(grep -cE \\\"executeCommand[(]'curtain-wall.create'\\\" apps/editor/src/engine/views/plantools/CurtainWallPlanToolHandler.ts)\"",
      "acceptance_number": "non-zero, then 0, then 2. The FIRST number must stay NON-ZERO (it is 6 today) — `curtainwall.create` is a live alias that MUST keep appearing in prose describing the alias; 'must print 0' was stated in p01's first revision and was wrong. The SECOND is the criterion: 5 today, must reach 0 — every surviving occurrence sits in a sentence that names it as an alias. The THIRD is the control that keeps the correction honest: 2 canonical `executeCommand('curtain-wall.create')` sites at :479 and :513; if it ever prints 0 the plan tool changed and the doc must follow the CODE, not this line.",
      "effort": "S",
      "depends_on": ["W5-01"],
      "risk": "Two ways to get this wrong, and the second is worse. (1) Chasing `grep -c curtainwall.create` to 0 and deleting real prose about a live alias. (2) 'Fixing' the now-falsified C11:1425 by changing the plan tool to dispatch the alias so the document becomes true — production code edited to satisfy a stale sentence. Note families/curtain-wall.json still grades `reachable` UNVERIFIED and probe P-1 printed BOTH curtainwall=undefined and curtain-wall=undefined; this task settles the SPELLING question and settles nothing about reachability, which needs a browser-context probe.",
      "contract_clause": "C16 (command authoring — canonical name on `type`, old spelling in `aliases`, which check-command-naming's own remedy text prescribes and which curtain-wall already did correctly) · C84 EI-9"
    },
    {
      "id": "W5-05",
      "title": "Make C11 §11 machine-comparable: row keys become canonical registry ids, and the two prose exclusions become a declared table",
      "disposition": "REPLACE",
      "addresses": ["DC-03", "DC-04"],
      "files": ["docs/02-decisions/contracts/C11-ELEMENT-CREATION-PIPELINE.md"],
      "change": "The new matrix is correct in content and NOT parseable against any in-repo id set, for three reasons, all measured. (1) THREE ROW KEYS ARE VERB STEMS, NOT IDS: `| **annotation** |`, `| **dimension** |`, `| **room** |` against registry ids `annotations`, `dimensions`, `rooms`. Rename the bold key to the canonical registry id and leave the verb where it already correctly lives — the CEB column, which already reads `✅ \\`annotation.create\\` (CEB :1266)`. This mints NO alias map: the row key is the id, the verb is in the verb column, and neither fact acquires a second home. (2) THE TWO EXCLUSIONS ARE PROSE: 'Two ids in ELEMENT_PLUGIN_IDS have no create verb and no defineElement schema and are therefore not element families: selection (kind collab) and section-view (kind view)'. A parser correctly refuses to read prose (the precedent gate records that a sweep keyed on a NAME is defeated by a different name). Convert to a two-row table `| Id | Kind | Why not an element family | Falsifying command |`. (3) LIGHTING HAS A ROW AND NO REGISTRY BASIS — it is kind='overlay' in PLUGIN_CATALOG and absent from ELEMENT_PLUGIN_IDS, yet has a live §FT-LIGHTING bridge and a defineElement schema. Add a one-row declared table 'ELEMENT BY SIGNAL, NOT BY REGISTRY' naming lighting with those two signals as its basis. Also give the three member rows (water, liftPart, bathroomPod) the same `| **<id>** |` key form the family rows use, so one parser reads both tables.",
      "why_this_shape": "REPLACE three keys and two prose paragraphs — a surgical edit, not a rewrite. This is the PREREQUISITE that makes W5-06 possible at all: without it arm B fires on three renamed rows that are not defects, and arm D cannot see two exclusions that are real decisions. It is the same move the precedent gate forced on contracts/README.md — an absence must be TYPED, NAMED and COUNTED in a form a machine can read, or silence passes for a decision (C76 §6).",
      "acceptance_command": "cd <repo>; C11=docs/02-decisions/contracts/C11-ELEMENT-CREATION-PIPELINE.md; S=$(grep -n '^## §11 ' $C11 | cut -d: -f1); E=$(awk -v s=$S 'NR>s && /^### §11\\./{print NR; exit}' $C11); sed -n \"${S},${E}p\" $C11 | grep -oE '^\\| \\*\\*[A-Za-z-]+\\*\\*' | sed 's/| \\*\\*//;s/\\*\\*//' | sort > /tmp/rows.txt; grep -oE \"desc\\('[a-zA-Z-]+', *'[^']*', *'element'\\)\" packages/runtime-composer/src/PluginHost.ts | grep -oE \"^desc\\('[a-zA-Z-]+\" | sed \"s/desc('//\" | sort > /tmp/k.txt; sed -n '/export const ELEMENT_PLUGIN_IDS/,/\\] as const/p' apps/editor/src/PluginRegistry.ts | grep -oE \"^ *'[a-zA-Z-]+',\" | tr -d \" ',\" | sort > /tmp/e.txt; sort -u /tmp/k.txt /tmp/e.txt > /tmp/u.txt; echo \"$(wc -l < /tmp/rows.txt) $(wc -l < /tmp/u.txt) $(comm -13 /tmp/u.txt /tmp/rows.txt | wc -l) $(comm -23 /tmp/u.txt /tmp/rows.txt | wc -l)\"",
      "acceptance_number": "27 28 1 2 — 27 matrix row keys, 28 union ids, exactly ONE row key with no registry basis (lighting, which the new 'ELEMENT BY SIGNAL' table must name) and exactly TWO union ids with no row (selection, section-view, which the new 'NOT AN ELEMENT FAMILY' table must name). TODAY the same command prints 27 28 4 5 — the extra 3 on each side are annotation/dimension/room vs annotations/dimensions/rooms, i.e. the renames. Falling from 4 to 1 and 5 to 2 IS the acceptance.",
      "effort": "M",
      "depends_on": ["W5-01"],
      "risk": "Renaming the row key while leaving the CEB cell's verb unchanged is correct; renaming the VERB to match the id would be a fabrication — `annotation.create` is the real CEB case label at :1266 and `annotations.create` does not exist. Second risk: 'fixing' the mismatch the other way, by renaming the plugin directories or ELEMENT_PLUGIN_IDS entries — that is a production-code change to satisfy a document, and it would break every storeKey and CEB case that already agrees with the current ids.",
      "contract_clause": "C84 EI-9 (the row key is the id; the verb stays in the verb column; neither fact acquires a second home) · C76 §6 (absence is typed, named and counted, never silent) · C11 §11's own clause: 'This matrix is the normative record ... It MUST be updated whenever a bridge is added, removed, or repaired.'"
    },
    {
      "id": "W5-06",
      "title": "Build and register check-element-matrix-equivalence.ts — the matrix can never again cover 14 of 24 families silently",
      "disposition": "WIRE",
      "addresses": ["DC-01", "DC-02", "DC-03", "DC-04", "DC-06"],
      "files": [
        "tools/ga-gate/check-element-matrix-equivalence.ts",
        "tools/ga-gate/run-all.ts",
        "tools/ga-gate/gate-newly-measured.json"
      ],
      "change": "New gate, modelled directly on tools/ga-gate/check-contract-index-equivalence.ts (read it first — its header states the doctrine this gate inherits: compare SETS never a number, derive everything, transcribe nothing, type every declared absence). SUBJECT SET is the UNION of two in-repo declarations, both parsed at run time, neither transcribed: PLUGIN_CATALOG descriptors with kind 'element' in packages/runtime-composer/src/PluginHost.ts (18 today) and ELEMENT_PLUGIN_IDS in apps/editor/src/PluginRegistry.ts (27 today) = 28. ROW SET is `^\\| \\*\\*<key>\\*\\* \\|` lines between the '## §11 —' heading and the next '### §11.' heading (27 today). ARMS: F0 floors exit 2 (C11 parses; >= 20 rows; >= 15 kind='element' ids; >= 20 ELEMENT_PLUGIN_IDS ids; two planted controls executed INSIDE every invocation — a synthetic id must fire arm D and a synthetic row key must fire arm B, so an emptied PluginHost.ts cannot report a clean sweep of nothing). A hard-0 FAMILY-WITHOUT-ROW (every kind='element' id has a row). B hard-0 ROW-WITHOUT-BASIS (every row key is in the union or named in the contract's 'ELEMENT BY SIGNAL' table). D hard-0 EXCLUSION-NOT-DECLARED (every union id with no row is named in the contract's 'NOT AN ELEMENT FAMILY' table with a reason). E shrink-only ratchet STALE-SUBSECTION (every '### §11.x' heading whose (YYYY-MM-DD) stamp predates the §11 matrix stamp must carry 'NOT RE-MEASURED' or 'RE-MEASURED <date>'; first honest reading 17). Register at tools/ga-gate/run-all.ts in the GATES array beside check-contract-index-equivalence.ts (:726) — LAST, after the R5 meta-gate, because its subject is documentation and a failure here must never mask a code failure above it. Pin arm E's 17 in gate-newly-measured.json (NOT gate-debt.json) with `gate`, `runner: 'ga-gate'`, `addedAt`, `builtBy`, `firstReading`, `pinnedTo`, `predatesTheGate` — the shape the file's 32 existing entries use.",
      "why_this_shape": "WIRE — the enforcement does not exist and everything it needs to read already does. This is NOT the 'absent vs unreachable' trap: `grep -ln 'C11-ELEMENT-CREATION' tools/ga-gate/check-*.ts` returns ZERO files and `grep -ln 'C01-ARCHITECTURE' tools/ga-gate/check-*.ts` returns ZERO files — measured, not assumed. No gate anywhere has a contract's CONTENT as its subject; the only two doc gates (contract-cited-paths, contract-index-equivalence) both take contracts/README.md. A COUNT arm would have caught none of DC-01..DC-04: DC-04 is a SET difference (14 of 24) that 14 correct-looking rows satisfy, DC-02 is a within-section contradiction, DC-03 is three ids present in prose and absent from the table.",
      "acceptance_command": "cd <repo>; npx tsx tools/ga-gate/check-element-matrix-equivalence.ts > /tmp/emx.log 2>&1; echo \"RC=$?\"; tail -3 /tmp/emx.log; npx tsx tools/ga-gate/run-all.ts 2>&1 | grep -c 'element-matrix-equivalence'",
      "acceptance_number": "RC=0 and a terminal line printing all six integers — '27 matrix rows · 28 registry ids · arm A 0 · arm B 0 · arm D 0 · arm E 17/17' — and the run-all grep printing >= 1 (the gate is registered and actually ran). RC=0 ALONE IS NOT ACCEPTANCE (D3): a gate whose arms all read 0 because it walked nothing exits 0 too, which is precisely M2/R7's defect at suite scale. The six numbers are the criterion. NOTE: this task does NOT make the ga-gate job green — that job is already red at HEAD on the seven exit-3 ratchets (R1-R8), which belong to W2/W3/W4 and which no ledger may absorb.",
      "effort": "L",
      "depends_on": ["W5-01", "W5-05"],
      "risk": "THREE ways to get this wrong, in descending order. (1) Hard-coding the family list in the gate — mints a THIRD authority for 'what is an element family' beside PLUGIN_CATALOG.kind and ELEMENT_PLUGIN_IDS, C84 EI-9 violation, and the gate then rots exactly as the matrix did. (2) Shipping without F0 floors and planted controls — the gate would report a clean sweep over an emptied file, which is the M2 defect being reproduced inside the fix for another one. (3) Editing tools/ga-gate/run-all.ts's GATES array concurrently with W1's M3 task (registering check-render-aggregate-seam) — same file, same array, near-certain merge conflict; sequence them or land both edits in one commit.",
      "contract_clause": "C00 / contracts/README.md precedent (a gate asserts the file set equals the row set IN BOTH DIRECTIONS) · C69 §0.1 / C64 §2.13 / C76 §0.1 (derive, never transcribe) · C76 §6 (absence is typed, named and counted) · C76 §6.3 (a defect predating a new gate is newly-measured, not debt and not a regression) · C11 §11's own 'MUST be updated' clause, which had no enforcement"
    },
    {
      "id": "W5-07",
      "title": "Stamp the 17 stale §11.x per-element subsections, driving arm E to 0 without pretending they were re-measured",
      "disposition": "DOCUMENT",
      "addresses": ["DC-06"],
      "files": ["docs/02-decisions/contracts/C11-ELEMENT-CREATION-PIPELINE.md"],
      "change": "Seventeen headings carry the stamp (2026-05-19) — §11.3 through §11.20, measured with `grep -cE '^### §11[.][0-9]+ .*[(]2026-05-19' C11`. They now sit BENEATH a §11 matrix stamped 2026-08-29 that refutes at least four of the facts they assert. Do NOT re-measure eighteen pipelines (that is an XL audit, not a doc pass) and do NOT delete them (they carry real per-element pipeline detail that exists nowhere else). Append to each heading: ' — NOT RE-MEASURED since 2026-05-19; superseded on the WIRING facts by the §11 matrix (2026-08-29). Read the matrix row first.' Add one §11.0 banner above §11.1 stating the same rule once, so the per-heading marker is a pointer and not a seventeen-fold restatement (C84 EI-9). Where a subsection is refuted OUTRIGHT by a matrix row — §11.9 on Handrail's 'geometry-free payload (levelId only)' and §11.11 on Lighting — say so in that subsection with the falsifying command, rather than leaving the reader to diff two sections.",
      "why_this_shape": "DOCUMENT, not REPLACE and not REMOVE. REPLACE means re-executing eighteen per-element pipeline audits — the Phase 3 work already cost 29 family rows and is captured in audit/element-creation/2026-08-29/families/*.json. REMOVE destroys detail that has no other home. The honest third option is the one the audit itself uses everywhere: mark what is measured, mark what is not, and never let the two look alike. An unstamped stale subsection and a fresh one are the same number of pixels to the reader — the same shape as [[context-data-honesty-family]].",
      "acceptance_command": "cd <repo>; C11=docs/02-decisions/contracts/C11-ELEMENT-CREATION-PIPELINE.md; echo \"$(grep -cE '^### §11[.][0-9]+ .*[(]2026-05-19' $C11) $(grep -cE '^### §11[.][0-9]+ .*[(]2026-05-19.*NOT RE-MEASURED' $C11)\"",
      "acceptance_number": "17 17 — every one of the seventeen stale-stamped headings carries the NOT RE-MEASURED marker. Today the command prints '17 0'. The first number must NOT fall: dropping the (2026-05-19) stamp to satisfy the second arm destroys the evidence that the subsection is stale, which is the defect, not the fix.",
      "effort": "M",
      "depends_on": ["W5-01", "W5-06"],
      "risk": "Quietly re-dating the seventeen headings to 2026-08-29 to make arm E green. That is the documentation equivalent of raising a ratchet ceiling: it converts 'not measured' into 'measured and fine' with no measurement, and it is unrecoverable — once the stamp moves, nothing records that these seventeen were never re-run.",
      "contract_clause": "C76 §6 (absence — here, absence of measurement — is typed, named and counted, never silent) · C01 §6.1 (an absence claim is a measurement, not an observation) · C84 EI-9 (the rule stated once in §11.0, pointed at seventeen times)"
    },
    {
      "id": "W5-08",
      "title": "Build and register check-doc-derived-counts.ts — the class fix for DC-07/08/09/10, so no count in CLAUDE.md or C01 can rot silently again",
      "disposition": "WIRE",
      "addresses": ["DC-07", "DC-08", "DC-09", "DC-10"],
      "files": [
        "tools/ga-gate/check-doc-derived-counts.ts",
        "tools/ga-gate/run-all.ts",
        "CLAUDE.md",
        "docs/02-decisions/contracts/C01-ARCHITECTURE-AND-GOVERNANCE.md"
      ],
      "change": "Four of the ten conflicts are ONE defect: a count was transcribed and its subject moved (C01 §3 said 54/12/46, measured 103/13/51; C01 §5 said 31 gates, measured 75; C01 §4 said 5/9 booleans, measured 2/9; CLAUDE.md's P4 bullet — itself a correction dated 2026-08-18 — asserted both arms within ceiling, measured RC=3). The gate holds a TYPED REGISTRY OF DERIVATIONS implemented in Node — no shelling of strings out of a document, which would be both non-deterministic and an injection surface. Initial derivation ids, each with its measured value today: packages-manifests 103, apps-dirs 13, plugins-dirs 51, ga-gate-scripts 75, withHandlerSpan-files 293, window-any-apps-editor-src-ui 70, src-subdirs 0, contract-files (per check-contract-index-equivalence's own reading), adr-files, spec-files. The DOCS carry a marker beside each assertion — `<!-- DERIVED: id=packages-manifests value=103 -->` — and the gate re-derives and compares. ARMS: F0 floors exit 2 (both docs parse; >= MIN_MARKERS(8) markers found; a planted marker with a deliberately wrong value must fire arm A inside every invocation). A hard-0 VALUE-MISMATCH. B hard-0 UNKNOWN-DERIVATION-ID (a marker naming a derivation the gate does not implement — this is what stops a marker being invented to launder a number). Register in tools/ga-gate/run-all.ts's GATES array beside the other two doc gates, LAST. There is deliberately NO fuzzy 'unmarked numeric claim' arm: it would be a false-positive factory, and the residual — a count written with no marker is invisible to this gate — is named in this plan rather than hidden.",
      "why_this_shape": "WIRE. The enforcement is absent, measured: ZERO gates take CLAUDE.md or C01 as a subject whose CONTENT they verify (six gates merely mention CLAUDE.md in prose; grep -ln 'C01-ARCHITECTURE' tools/ga-gate/check-*.ts -> 0). The alternative — keep hand-correcting counts — has been tried and is documented failing SIX times in CLAUDE.md's own correction boxes for the contract range alone, with the fifth recurrence landing INSIDE the correction notice for the fourth. That is the strongest available evidence that a rule stated in three places and enforced by nothing does not hold. C84 EI-9 is satisfied because the DERIVATION lives only in the gate and the VALUE lives only in the doc: neither fact acquires a second home, and the gate asserts they agree.",
      "acceptance_command": "cd <repo>; npx tsx tools/ga-gate/check-doc-derived-counts.ts > /tmp/ddc.log 2>&1; echo \"RC=$?\"; tail -3 /tmp/ddc.log; npx tsx tools/ga-gate/run-all.ts 2>&1 | grep -c 'doc-derived-counts'",
      "acceptance_number": "RC=0 and a terminal line printing '10 markers · 10 derivations resolved · arm A 0 mismatches · arm B 0 unknown ids', and the run-all grep printing >= 1. RC=0 alone is not acceptance (D3): a gate that finds zero markers exits 0 too, which is why F0's MIN_MARKERS floor and the planted wrong-value control must both be in the printed line. Prove the gate can FAIL before believing it passes: change one marker's value by 1 and confirm RC=3 with arm A printing 1.",
      "effort": "L",
      "depends_on": ["W5-01", "W5-06"],
      "risk": "(1) Letting the marker carry a SHELL COMMAND instead of a derivation id — that hands a document arbitrary execution inside CI and makes the gate's result depend on the runner's PATH, which is exactly how three gates once died with `spawnSync rg ENOENT` and had the crash absorbed as declared debt (C01 §5's own L-811/L-812 record). (2) Shipping without the planted wrong-value control, leaving a gate nobody has ever seen fail. (3) Editing run-all.ts's GATES array concurrently with W5-06 and W1's M3 — three tasks, one array; land them in a defined order.",
      "contract_clause": "C69 §0.1 / C64 §2.13 / C76 §0.1 (derive, never transcribe — stated in three contracts and, for CLAUDE.md and C01, enforced by nothing) · C84 EI-9 (derivation in the gate, value in the doc, one home each) · C01 §5 ('a gate inventory that lists gates which do not exist is worse than an empty one, because it reads as coverage')"
    },
    {
      "id": "W5-09",
      "title": "Mark the counts CLAUDE.md and C01 §3/§4/§5 assert, so W5-08's gate actually covers them",
      "disposition": "DOCUMENT",
      "addresses": ["DC-07", "DC-08", "DC-09", "DC-10"],
      "files": [
        "CLAUDE.md",
        "docs/02-decisions/contracts/C01-ARCHITECTURE-AND-GOVERNANCE.md"
      ],
      "change": "W5-08 builds an instrument with no subject until the assertions are marked. Add one `<!-- DERIVED: id=<derivation> value=<n> -->` marker beside each derived count these two files assert, using the values re-derived in W5-01: C01 §3's package/app/plugin census (103 / 13 / 51), C01 §5's gate-script count (75), C01 §4 boolean #2's successor-scope cast count (70) and boolean #1's src-subdirectory count (0), CLAUDE.md's layer-table counts (13 apps / 51 plugins / 103 package manifests — note CLAUDE.md's own measured-2026-08-11 block says 13 / 48 / 97, all three now stale) and its contract-suite range paragraph. Do NOT mark a number the gate cannot derive — a marker whose id the gate does not implement fires arm B, hard-0, by design, and inventing an id to silence it is the laundering move that arm exists to stop.",
      "why_this_shape": "DOCUMENT. The numbers exist and are correct after W5-01; what is missing is the machine-readable handle that makes them checkable. This is the same relationship W5-05 has to W5-06 — an instrument is only as good as the declared subject it can read, and a gate with no subject floor cannot tell '0 violations' from 'walked nothing' (M2/R7, and check-gate-subject-floors' own §1.2a).",
      "acceptance_command": "cd <repo>; echo \"$(grep -c 'DERIVED: id=' CLAUDE.md) $(grep -c 'DERIVED: id=' docs/02-decisions/contracts/C01-ARCHITECTURE-AND-GOVERNANCE.md)\"",
      "acceptance_number": "Each must print >= 4, for a total of >= 10 markers — the same total W5-08's terminal line must print as 'markers'. If the two numbers disagree with the gate's count, one of the markers is malformed and the gate is silently covering fewer assertions than the doc appears to declare.",
      "effort": "S",
      "depends_on": ["W5-08"],
      "risk": "Marking a count with a derivation id the gate does not implement, then adding a stub derivation to make arm B green. That inverts the gate: the derivation would be written to fit the number instead of the number being checked against the tree — the same inversion as check-single-compose.ts printing a hard-coded '0 rivals' while its own body names one (M1).",
      "contract_clause": "C69 §0.1 (derive, never transcribe) · C76 §6 (typed, named, counted) · C01 §6.1"
    }
  ]
}
