{
  "workstream": "W3 — Ratchet breaches: back under ceiling without ever raising a ceiling",
  "generated": "2026-08-30",
  "head": "064a838e",
  "doctrine": {
    "D2": "Every task states WIRE / REPLACE / REMOVE and justifies it from the MEASURED state. Four of the thirteen turned out to be WIRE — the thing exists and the instrument or the facade cannot see it — and would have been mis-specified as REPLACE from the name alone. W3-R6 is the sharpest case: the 'missing' readers exist, and the naive fix would have restored a shipped defect.",
    "D3": "Every acceptance_command names a PRINTED NUMBER, never a tick.",
    "RATCHET-EXCEEDED-IS-NEVER-DEBT": "No task raises a shrink-only ceiling, extends a named baseline, or adds a gate-debt.json entry. Two tasks (W3-R1-B, W3-R2-B) PIN a first honest reading for an axis measured by NOTHING today — that is not absorbing an exit-3, it is giving an unmeasured axis its first ceiling, and each is explicitly forbidden from relieving the arm beside it.",
    "C84-EI-9": "W3-R3-B and W3-R3-D each create ONE authority where there are now many; neither adds a second home for a fact."
  },
  "corrections_to_the_brief": [
    "R2 numbers in the headline brief are STALE against raw/P8-otel-spans.log on disk. Measured: ZONE A 275/275 (not 246/246); ZONE B 66 uninstrumented of 87 against baseline 52, with 14 NEW files (not 54/70 with 2 new); ZONE C census 2100 of 2399 (not 1772/2023). Tasks use the LOG.",
    "R1 is TWO breached arms, not one. check-cast-count.ts checks repo-wide FIRST (main(), line 271) and returns exit 3 at 101/100 BEFORE the strict arm is reached. Fixing only the strict 11/3 leaves RC=3. W3-R1-A fixes both.",
    "R5 is THREE breached arms. Only the literal arm (12 > LEDGERED_LITERAL 11) drives exit 3; window 57/2 and cm.execute 62/49 fail at their DECLARED level and exit 1. W3-R5-A clears the exit-3; RC=0 additionally needs W3-R5-B.",
    "The P4 blind-spot figure '409 casts across 151 files' is the UNION of three evasion spellings, measured today as: `window as unknown as` 267 sites / 131 files, `globalThis as unknown as` 104 / 64, `(globalThis as any)` 28 / 16 = 399 sites. raw/p4-perfile.txt is the source.",
    "R3's four arms were each attributed to their NEW edges by re-running the gate's own predicate at the commit that froze each baseline (upward: 5d377bca; sdk-bypass: 2b1e7e99; unclassified: b220d88b; restricted: acae9ea4). The restricted reconstruction reproduces the frozen 113 exactly, which is why its delta list is trusted."
  ],
  "tasks": [
    {
      "id": "W3-R1-A",
      "title": "P4 cast-count — both arms under ceiling by deleting casts the typed Window declaration already makes redundant",
      "disposition": "REMOVE",
      "addresses": ["R1"],
      "contract_clause": "C14 §Legacy elimination (P4 — no `(window as any)`); STR-03 §2 P4",
      "files": [
        "apps/editor/src/engine/views/plantools/__tests__/curtainWallPlanTypeParity.spec.ts",
        "apps/editor/src/engine/views/plantools/__tests__/curtainWallPlanEnterCloses.spec.ts",
        "apps/editor/src/engine/views/plantools/__tests__/LinearDimThirdClickOffset.spec.ts",
        "apps/editor/src/ui/UiPreferences.ts",
        "apps/editor/src/ui/RadialMenu.ts",
        "apps/editor/src/ui/views/ViewHeaderButtons.ts",
        "apps/editor/src/ui/property-panel/RoofPropertySheet.ts",
        "apps/editor/src/ui/intent/HeaderIntentPicker.ts"
      ],
      "change": "STRICT ARM (11 -> 0, ceiling 3). Delete the `(window as any)` wrapper at each of these 11 sites, leaving the property access: curtainWallPlanTypeParity.spec.ts:46,76,79,80,105; curtainWallPlanEnterCloses.spec.ts:23,57,58; LinearDimThirdClickOffset.spec.ts:98,116. Every property they touch — `runtime`, `curtainWallTool`, `commandManager`, `wallStore` — is ALREADY declared on `interface Window` inside `declare global` in src/global-window.d.ts (lines 301, 141, 60, 71 respectively, all `?: any`), which is in the root tsconfig program these specs typecheck under. `(window as any).runtime = {...}` becomes `window.runtime = {...}`; `delete (window as any).curtainWallTool` becomes `delete window.curtainWallTool` (legal — the property is optional). No behavioural change: `any`-typed properties give identical inference. REPO-WIDE ARM (101 -> 96, ceiling 100). Delete the same redundant wrapper at the five `(window as any).runtime` sites: UiPreferences.ts:100, RadialMenu.ts:262, ViewHeaderButtons.ts:199, RoofPropertySheet.ts:316, HeaderIntentPicker.ts:95 — all read `.runtime?.bus` or `.runtime?.events`, all covered by the same declaration. Do NOT touch the 38 room-topology / 13 core-app-model / 11 input-host casts in this task; those need real injection and are not needed to restore the ceiling.",
      "why_this_shape": "REMOVE, not WIRE and not a shim allowlist. The gate's own fix text offers three routes (`runtime.<service>`, the allowlisted shim, a typed global declaration). Measured: the typed global declaration ALREADY EXISTS and already covers every one of these 16 properties — src/global-window.d.ts is 25 KB and declares `commandManager?`, `wallStore?`, `curtainWallTool?`, `runtime?`. So these casts are not load-bearing debt, they are dead syntax that postdates the declaration. Ranking them as 'needs runtime injection' would authorise 16 needless refactors of working code. Note the trap the baseline file records: the 4th historical cast at PlanViewToolOverlay.ts:786 was load-bearing ACROSS TWO ratchets (removing it would have incremented check-no-commandmanager's window arm). None of these 16 is: they touch `runtime`, `curtainWallTool` and `wallStore`, and the two `commandManager` sites are inside a __tests__ file, which check-no-commandmanager also scans — see risk.",
      "acceptance_command": "npx tsx tools/ga-gate/check-cast-count.ts > /tmp/w3r1.txt 2>&1; echo \"RC=$?\" >> /tmp/w3r1.txt; cat /tmp/w3r1.txt",
      "acceptance_number": "RC=0 with BOTH printed lines present: `[cast-tripwire] repo-wide scope: 5315 files · 96/100 cast(s) (tests excluded)` and `[cast-tripwire] OK: 0 (ratchet lowered from 3).`  Was: `101/100` and `FAIL: (window as any) count = 11 > baseline 3`.",
      "effort": "S",
      "depends_on": [],
      "risk": "LinearDimThirdClickOffset.spec.ts:98/116 write `window.commandManager`, and check-no-commandmanager scans apps/editor/src including __tests__, counting matching LINES rather than casts — so removing the cast leaves the line and the window count UNCHANGED at 57. Re-run check-no-commandmanager.ts after this task and confirm the window arm still reads 57, not 58; a rise means an edit split one line into two matches. Second risk: if a spec's tsconfig excludes src/global-window.d.ts the build breaks — the fix is then to add the .d.ts to that include, NEVER to restore the cast."
    },
    {
      "id": "W3-R1-B",
      "title": "P4 blind spot — give the three evasion spellings their own arm instead of widening the regex that carries the ratchet's history",
      "disposition": "WIRE",
      "addresses": ["R1"],
      "contract_clause": "C14 §Legacy elimination (P4); C09 §gate honesty (a count over a silently-changed subject is not comparable with itself)",
      "files": ["tools/ga-gate/check-cast-count.ts", "tools/ga-gate/gate-debt.json"],
      "change": "DO NOT widen the existing `(window as any)` regex. Add a THIRD counter, `countEvasionSpellings()`, alongside `count()` (strict) and `countRepoWide()`, over the same repo-wide dirs (src, apps, packages, plugins, server, tools), the same `scanFilesStripped` comment-stripping and the same tests-excluded rule, matching three literal spellings: `window as unknown as`, `globalThis as unknown as`, `(globalThis as any)`. Give it its OWN constant `MAX_EVASION_SPELLINGS`, pinned at the FIRST HONEST READING and env-overridable exactly as MAX_REPO_WIDE is (`process.env.PRYZM_P4_MAX_EVASION ?? 399`). Print a per-spelling and per-package breakdown on every run, in the shape the repo-wide arm already prints, so the number is attributable. Exit 3 above the ceiling, like its siblings. WHAT IT WOULD THEN MEASURE, from raw/p4-perfile.txt at HEAD: `window as unknown as` 267 code sites across 131 files (top holders apps/editor/src/PluginRegistry.ts 19, apps/editor/src/ui/onboarding/OnboardingStepController.ts 19, apps/editor/src/ui/ai/AIPanel.ts 12, apps/editor/src/ui/house-layout/HouseLayoutExecutor.ts 12, apps/editor/src/engine/initBusHandlers.ts 10); `globalThis as unknown as` 104 across 64 files (apps/editor/src/engine/WallRebuildCoordinator.ts 9, apps/editor/src/engine/persistence/ProjectLoader.ts 6, packages/frame-scheduler/src/PerfCounters.ts 5); `(globalThis as any)` 28 across 16 files (packages/geometry-wall/src/WallJoinResolver.ts 6, packages/renderer-three/src/pipeline/RenderPipelineManager.ts 6). Total 399. Also rewrite the gate-debt.json prose that still says '409 across 151 files, MEASURED NOT FIXED' to point at the new arm rather than repeat an unowned number. Add one clause in the new arm's header: this arm may NEVER be used to relieve the `(window as any)` arm — a cast rewritten from one spelling into the other is moving debt, and BOTH arms must fall.",
      "why_this_shape": "WIRE: the instrument exists and has two working arms; the blind spot is a missing third arm, not a missing gate. Widening the EXISTING regex was rejected on the gate family's own stated principle — check-layer-boundaries.ts argues at MAX_RESTRICTED_IMPORTS that 'a ratchet must be comparable with itself'. Folding 399 evasion sites into the 100-ceiling arm would (a) make it instantly unsatisfiable, the L-716 class this repo has already been burned by, and (b) destroy the 4->3 history the cast-count baseline file preserves verbatim. Pinning a NEW ceiling at 399 is not ratchet-debt: this axis is measured by nothing today, so 399 is its first reading, not a relaxation of an existing one.",
      "acceptance_command": "npx tsx tools/ga-gate/check-cast-count.ts > /tmp/w3r1b.txt 2>&1; echo \"RC=$?\" >> /tmp/w3r1b.txt; grep -E 'evasion|repo-wide|OK:' /tmp/w3r1b.txt",
      "acceptance_number": "RC=0 with a new printed line naming all three spellings and totalling `399/399` — e.g. `[cast-tripwire] evasion spellings: 5315 files · 399/399 (window-as-unknown 267 · globalThis-as-unknown 104 · globalThis-as-any 28)`. NEGATIVE CONTROL required in the same PR: plant a 400th site in any of the three spellings, record RC=3, then delete it.",
      "effort": "M",
      "depends_on": ["W3-R1-A"],
      "risk": "Two failure modes. (1) Pinning the ceiling from a DIFFERENT scan than the arm implements — pin it from the gate's own first run, never from raw/p4-perfile.txt, or the arm starts life over or under its subject. (2) Someone later 'fixes' arm 1 by rewriting `(window as any)` into `window as unknown as`, decrementing arm 1 and incrementing arm 3. The header clause is the only defence; without it this task makes P4 easier to fake, not harder."
    },
    {
      "id": "W3-R2-A",
      "title": "P8 Zone B — instrument the 14 new uninstrumented files; the named baseline stays at 52",
      "disposition": "WIRE",
      "addresses": ["R2"],
      "contract_clause": "C10 §2 (>=1 OpenTelemetry span per exported function); STR-03 §2 P8",
      "files": [
        "packages/command-registry/src/attachments/ReseatWallAnchoredElementsCommand.ts",
        "packages/command-registry/src/attachments/WallAnchor.ts",
        "packages/command-registry/src/boundaryLine/boundaryLineDependentAdapters.ts",
        "packages/command-registry/src/floors/floorFinishPattern.ts",
        "packages/command-registry/src/handrails/CreateHandrailRunOnSlabCommand.ts",
        "packages/command-registry/src/lighting/lightingAuthoredParams.ts",
        "packages/command-registry/src/resolveDeleteCommand.ts",
        "packages/command-registry/src/rooms/roomLossCensus.ts",
        "packages/command-registry/src/rooms/roomTombstoneRegister.ts",
        "packages/command-registry/src/stair/stairPiercedLevels.ts",
        "packages/command-registry/src/stair/StairVoidCascade.ts",
        "plugins/balcony/src/handlers/index.ts",
        "plugins/boundary-line/src/handlers/index.ts",
        "plugins/lift/src/handlers/index.ts"
      ],
      "change": "Add >=1 span CALL to each file's principal exported function — the gate's detector at check-otel-spans.ts:111 requires a CALL, not an import (`withHandlerSpan(` | `withAsyncHandlerSpan(` | `startActiveSpan(` | `startSpan(`). For the ELEVEN packages/command-registry files use the pattern already established in that package; copy it verbatim from packages/command-registry/src/boundaryLine/MoveBoundaryLineCommand.ts:54 (`import { trace, type Tracer } from '@opentelemetry/api';`), :78 (`function _tracer(): Tracer { ... }`) and :254 (`return _tracer().startActiveSpan('pryzm.boundary_line.move', (span) => { ... })`). Do NOT reach for `withHandlerSpan` from @pryzm/plugin-sdk inside command-registry: command-registry is L2 and plugin-sdk is L5, so that import would be a NEW upward violation and would break W3-R3-D's acceptance number in the same commit. One span name per file, following the `pryzm.<domain>.<verb>` convention already in use: pryzm.attachment.reseat_wall_anchored, pryzm.attachment.wall_anchor, pryzm.boundary_line.dependent_adapters, pryzm.floor.finish_pattern, pryzm.handrail.create_run_on_slab, pryzm.lighting.authored_params, pryzm.delete.resolve_command, pryzm.room.loss_census, pryzm.room.tombstone_register, pryzm.stair.pierced_levels, pryzm.stair.void_cascade. For the THREE plugin barrels instrument `registerBalconyHandlers` / `registerBoundaryLineHandlers` / `registerLiftHandlers` — these already import from @pryzm/plugin-sdk (plugins/balcony/src/handlers/index.ts:8), so `withHandlerSpan('pryzm.plugin.balcony.register', () => { ... })` is legal there. FORBIDDEN: adding any of the 14 to ZONE_B_BASELINE at tools/ga-gate/check-otel-spans.ts:202 — that list is a shrink-only named baseline and extending it is the one forbidden fix.",
      "why_this_shape": "WIRE. The span helper, the tracer factory and the naming convention all EXIST and are used by dozens of sibling files in the same directories; these 14 never picked them up. Nothing needs replacing. The two-mechanism split (raw @opentelemetry/api in command-registry, the plugin-sdk helper in plugin barrels) is not inconsistency — it is what the layer table forces, and standardising on one mechanism everywhere would trade a P8 breach for a P-layer breach.",
      "acceptance_command": "npx tsx tools/ga-gate/check-otel-spans.ts > /tmp/w3r2a.txt 2>&1; echo \"RC=$?\" >> /tmp/w3r2a.txt; grep -E 'ZONE|FAIL' /tmp/w3r2a.txt",
      "acceptance_number": "RC=0 and the line `[otel-spans] ZONE B (command-registry + app handlers + plugin barrels): 52 uninstrumented of 87 (baseline allows 52).`  Was `66 uninstrumented of 87` plus `FAIL (Zone B): 14 NEW ...`. ZONE A must still print `275 / 275 instrumented`.",
      "effort": "M",
      "depends_on": [],
      "risk": "Two. (1) Reaching for `withHandlerSpan` inside packages/command-registry adds an L2->L5 upward import and silently breaks W3-R3-D's number in the same commit — re-run the layer gate AFTER this task, not before. (2) `startActiveSpan` returns the callback's value; a careless wrap that drops the return turns eleven working functions into ones returning undefined. Every one of the 11 must keep `return _tracer().startActiveSpan(...)`, with `span.end()` in a finally so a throwing handler does not leak the span."
    },
    {
      "id": "W3-R2-B",
      "title": "P8 Zone C — give the 'every exported function' clause its first arm, so it stops being printed and starts being measured",
      "disposition": "WIRE",
      "addresses": ["R2"],
      "contract_clause": "C10 §2 (the literal clause: every new exported function adds >=1 span); C09 §gate honesty",
      "files": ["tools/ga-gate/check-otel-spans.ts"],
      "change": "Zone C today prints `2100 / 2399 files declaring an exported function have NO span (5281 source files read)` and gates NOTHING — so C10 §2 is enforced for ZERO of the 2399. Convert it to a gated arm with the SAME shape Zone B already proves works. (a) Add `const ZONE_C_BASELINE: readonly string[]` holding the 2100 uninstrumented paths, generated by the gate's own `--print-baseline` flag (check-otel-spans.ts:106 already implements that flag for Zone B — extend it to emit Zone C rather than hand-writing the list). (b) Fail with exit 3 when a file declaring an exported function is NOT on the list and has no span — exactly the Zone B `zoneBNew` predicate applied to Zone C. (c) Keep Zone B's both-directions check, which reports entries now instrumented or deleted but still listed, so the list can only shrink. (d) Keep the census line printing so the ratio stays visible. WHY A LIST AND NOT A COUNT, to be stated in the header: a count of 2100 lets one package instrument ten files while another loses ten and reports 'no change' — the C69 §7.c defect, and the reason Zone B is already a list. SIZE CAVEAT: 2100 paths is a ~120 KB literal; if that is judged unmanageable the ONLY acceptable alternative is a checked-in sibling data file (tools/ga-gate/baselines/otel-zone-c.json) read at startup behind a MIN_ENTRIES floor that exits 2 if it fails to parse — never a bare number.",
      "why_this_shape": "WIRE, and it is exactly why P8's exit condition is written as 'Zone B reaches 0 AND Zone C acquires an arm'. The gate EXISTS, walks the right 5281 files, and already computes the precise set — it just prints it. Ranking this as 'build a span gate' (REPLACE) would authorise a rival instrument beside a working one. Pinning 2100 as a first ceiling is not ratchet-debt: this axis has no ceiling at all today, so any number is stricter than what exists, and the list is shrink-only from the moment it lands.",
      "acceptance_command": "npx tsx tools/ga-gate/check-otel-spans.ts > /tmp/w3r2b.txt 2>&1; echo \"RC=$?\" >> /tmp/w3r2b.txt; grep -E 'ZONE C|FAIL' /tmp/w3r2b.txt",
      "acceptance_number": "RC=0 and a ZONE C line reporting a GATED figure against a named list — `[otel-spans] ZONE C (GATED, named baseline): 2100 uninstrumented of 2399 (baseline names 2100).` NEGATIVE CONTROL required in the same PR: add one exported function in a new file under packages/command-registry/src/ with no span, re-run, record RC=3 with that path named, then delete it.",
      "effort": "L",
      "depends_on": ["W3-R2-A"],
      "risk": "The dominant risk is landing a Zone C arm nobody can go green against: 2100 entries means almost every PR touches a listed file, and if the predicate is 'file is listed AND still uninstrumented => must be removed from the list', every ordinary edit becomes a gate failure. The predicate must be exactly Zone B's — only a file NOT on the list and WITHOUT a span fails. Getting that backwards makes the gate permanently red, which is precisely how the @thatopen eslint rule stopped enforcing anything (check-layer-boundaries.ts:307-320)."
    },
    {
      "id": "W3-R3-A",
      "title": "Layer gate arm 4 — classify the two geometry packages that have no layer; unclassified 15 -> 13",
      "disposition": "WIRE",
      "addresses": ["R3"],
      "contract_clause": "C01 §Architecture (the 8-layer model; a new package MUST be given a layer)",
      "files": ["eslint.config.js"],
      "change": "Add two rows to the L2 element-family block in eslint.config.js immediately after line 143 (`packages/geometry-balcony/**`) so the list stays alphabetical: `{ type: 'L2', pattern: 'packages/geometry-boundary-line/**' },  // [family] verified: imports @pryzm/schemas + @opentelemetry/api only` and `{ type: 'L2', pattern: 'packages/geometry-handrail/**' },  // [family] verified: command-registry/core-app-model (L2), renderer-three/snapping (L1), geometry-stair/geometry-wall (L2), schemas (L0) — nothing above L2`. MEASURED import sets justifying L2: geometry-boundary-line/src imports exactly `@pryzm/schemas` and `@opentelemetry/api` — the same profile as geometry-pool and geometry-balcony, both already L2. geometry-handrail/src imports `@pryzm/command-registry`, `@pryzm/core-app-model`, `@pryzm/geometry-stair`, `@pryzm/geometry-wall`, `@pryzm/renderer-three`, `@pryzm/snapping`, `@pryzm/schemas` and `@thatopen/components` — every @pryzm target is L2 or below, so classifying it adds ZERO upward violations. The other 13 unclassified names stay unclassified: they are the documented backend set (admin-overrides, ai-spend, api-rbac, api-spec, beta-signup, email-transport, rate-limit, webhooks) plus tooling (bench-visual-diff, eslint-plugin-pryzm, legacy-shim, release, wcag-audit), and CLAUDE.md records 'does the backend need its own model?' as an OPEN question — inventing a layer for them to make a number fall is the failure this gate exists to catch.",
      "why_this_shape": "WIRE, not REPLACE. Both packages EXIST, both are real element families with manifests and (for boundary-line) tests, and all fifteen geometry-* siblings are already classified L2 — the layer table simply never got the two new rows. Ranking this as 'unclassifiable' leaves the gate blind to every import in and out of them, which is what the unclassified ratchet exists to prevent: the gate SKIPS unclassified sources entirely at check-layer-boundaries.ts:481, so today nothing about geometry-handrail is judged at all.",
      "acceptance_command": "npx tsx tools/ga-gate/check-layer-boundaries.ts > /tmp/w3r3a.txt 2>&1; echo \"RC=$?\" >> /tmp/w3r3a.txt; grep -E 'UNCLASSIFIED|banned third-party' /tmp/w3r3a.txt",
      "acceptance_number": "The line `[check-layer-boundaries] workspace packages: 167 · classified: 154 · UNCLASSIFIED: 13` (was `classified: 152 · UNCLASSIFIED: 15`), and no `FAIL — 15 unclassified package(s), baseline 13` line.",
      "effort": "S",
      "depends_on": [],
      "risk": "THIS TASK MAKES THE BANNED-THIRD-PARTY ARM WORSE BY ONE, and that is not a reason to skip it. Because unclassified SOURCES are skipped before the restricted check (`if (!fromLayer) continue;` at check-layer-boundaries.ts:481 precedes the RESTRICTED_MODULES loop at :506), packages/geometry-handrail/src/HandrailTool.ts:42 `import * as OBC from '@thatopen/components'` is invisible today. Classifying the package makes it countable: 123 -> 124. W3-R3-B's arithmetic already carries that +1. Landing R3-A WITHOUT R3-B pushes the banned arm further over ceiling — ship them together, or R3-B first."
    },
    {
      "id": "W3-R3-B",
      "title": "Layer gate arm 3 — one owner for the OBC TechnicalDrawing seam; banned third-party 124 -> 108",
      "disposition": "REPLACE",
      "addresses": ["R3"],
      "contract_clause": "C01 §Architecture (banned third-party homes); C84 EI-9 (one authority per concept); precedent: P2 / packages/renderer-three owning THREE",
      "files": [
        "packages/renderer-three/src/technical-drawing.ts",
        "packages/renderer-three/package.json",
        "packages/core-app-model/src/drawing/OpeningElevationSymbolBuilder.ts",
        "apps/editor/src/engine/BoundaryLinePlanSymbolBuilder.ts",
        "packages/geometry-furniture/src/builders/BedPlanSymbolBuilder.ts",
        "packages/geometry-furniture/src/builders/ChairPlanSymbolBuilder.ts",
        "packages/geometry-furniture/src/builders/KitchenPlanSymbolBuilder.ts",
        "packages/geometry-furniture/src/builders/SofaPlanSymbolBuilder.ts",
        "packages/geometry-furniture/src/builders/TreeElevationSymbolBuilder.ts",
        "packages/geometry-furniture/src/builders/TreePlanSymbolBuilder.ts",
        "packages/geometry-furniture/src/builders/WardrobePlanSymbolBuilder.ts",
        "packages/geometry-column/src/ColumnPlanSymbolBuilder.ts",
        "packages/geometry-door/src/DoorPlanSymbolBuilder.ts",
        "packages/geometry-window/src/WindowPlanSymbolBuilder.ts",
        "packages/geometry-roof/src/RoofSlopeSymbolBuilder.ts",
        "packages/geometry-wall/src/WallLayerPlanSymbolBuilder.ts",
        "packages/geometry-plumbing/src/PlumbingElevationSymbolBuilder.ts",
        "packages/geometry-plumbing/src/PlumbingPlanSymbolBuilder.ts",
        "packages/geometry-stair/src/StairSymbolTechnicalDrawingBridge.ts"
      ],
      "change": "CREATE packages/renderer-three/src/technical-drawing.ts as the SINGLE non-ifc-import home for `@thatopen/components`, exporting exactly two things: `export type TechnicalDrawing = OBC.TechnicalDrawing;` and `export function projectToDrawingSpace(lines: THREE.LineSegments, drawing: TechnicalDrawing): THREE.LineSegments { return OBC.TechnicalDrawing.toDrawingSpace(lines, drawing); }`. Add `\"./technical-drawing\": \"./src/technical-drawing.ts\"` to the `exports` map in packages/renderer-three/package.json (which already carries `./three`, `./background`, `./geospatial`) and add `@thatopen/components` to its dependencies. THEN convert the 17 builders listed. Each one's ENTIRE OBC surface is exactly two symbols — verified by enumerating `OBC.*` per file: `OBC.TechnicalDrawing` used as a TYPE in an `inject(drawing: OBC.TechnicalDrawing, ...)` signature, and one call to `OBC.TechnicalDrawing.toDrawingSpace(lines, drawing)`. So each conversion is mechanical: delete `import * as OBC from '@thatopen/components';`, add `import { type TechnicalDrawing, projectToDrawingSpace } from '@pryzm/renderer-three/technical-drawing';`, rewrite `OBC.TechnicalDrawing` -> `TechnicalDrawing` and `OBC.TechnicalDrawing.toDrawingSpace(a, b)` -> `projectToDrawingSpace(a, b)`. Add the `@pryzm/renderer-three` workspace dependency to any of the seven owning package.json files lacking it and re-run `pnpm install` so pnpm-lock.yaml stays in sync. DELIBERATELY OUT OF SCOPE: the ~30 OBC importers using `OBC.World` / `OBC.Components` / `OBC.Raycasters` / `OBC.RendererMode` (the *Tool.ts files), the six `import('@thatopen/components').TechnicalDrawing` type positions in packages/core-app-model/src/drawing/HiddenLineRemoval.*.test.ts, and the four `import * as OBC` parity probes (ElevationViewBasis.test.ts, OpeningElevationSymbol.probe.test.ts, OpeningElevationSymbolBuilder.test.ts, WallElevationSymbol.probe.test.ts). Those probes deliberately drive the REAL OBC prototype and `toDrawingSpace` — that is their entire evidential value — and stripping OBC from them leaves a fake that cannot falsify the thing it tests. ARITHMETIC: 123 measured + 1 (geometry-handrail becomes countable under W3-R3-A) − 17 (converted) + 1 (the facade module's own import) = 108, five under the 113 ceiling.",
      "why_this_shape": "REPLACE — seventeen direct dependencies on a banned third-party module become one owned seam. D2 check first: is there an existing OBC facade that is merely unwired? Measured NO — `git grep \"export .* from '@thatopen/components'\"` across packages/apps/plugins returns ZERO, so there is nothing to WIRE. The owner is not arbitrary: packages/renderer-three is ALREADY the repo's single-owner package for a monopolised 3D third-party surface (P2 gives it THREE, hard-0 across 8171 files, and it already publishes a `./three` re-export subpath). Putting the OBC seam anywhere else would create a SECOND third-party-ownership convention — the C84 EI-9 breach. Layer-wise renderer-three is L1, so all seventeen consumers (L2 geometry-*, L2 core-app-model, L7 apps/editor) import DOWNWARD: no upward violation and no SDK bypass is created, verified by the fact that none of the 17 lives under plugins/.",
      "acceptance_command": "npx tsx tools/ga-gate/check-layer-boundaries.ts > /tmp/w3r3b.txt 2>&1; echo \"RC=$?\" >> /tmp/w3r3b.txt; grep -E 'banned third-party' -A 8 /tmp/w3r3b.txt",
      "acceptance_number": "`[check-layer-boundaries] banned third-party imports (OBC / express) outside their allowed homes: 108` (was 123), and no `FAIL — 123 banned third-party import(s), baseline 113` line. The per-package breakdown must show packages/geometry-furniture, geometry-column, geometry-door, geometry-window, geometry-roof, geometry-wall, geometry-plumbing and geometry-stair GONE, and `1  packages/renderer-three  [@thatopen/components]` present.",
      "effort": "L",
      "depends_on": [],
      "risk": "Four. (1) `OBC.TechnicalDrawing.toDrawingSpace` is a STATIC on a class the facade also re-exports as a type — `export type TechnicalDrawing = OBC.TechnicalDrawing` yields the INSTANCE type, so any caller reaching for the static through the alias will not compile; every static use must go through `projectToDrawingSpace`. After conversion, `grep -n 'OBC\\.' ` across the 17 files must return zero. (2) Adding @thatopen/components to renderer-three widens the dependency graph of a package almost everything imports — confirm the server bundle still builds (`build:server-deps` externals; a barrel import has dragged @thatopen/ui into the SERVER bundle before). (3) Seven package.json files change, so pnpm-lock.yaml must be regenerated or CI's frozen-lockfile install fails. (4) Landing this WITHOUT W3-R3-A gives 107 and W3-R3-A then makes it 108 — both pass, but the acceptance number above assumes both landed."
    },
    {
      "id": "W3-R3-C",
      "title": "Layer gate arm 2 — route the four plugin->schemas imports through the SDK facade; sdk-bypass 186 -> 182",
      "disposition": "WIRE",
      "addresses": ["R3"],
      "contract_clause": "C07 §Plugin SDK (a plugin reaches the platform through @pryzm/plugin-sdk; widen the facade, never bypass it)",
      "files": [
        "packages/plugin-sdk/src/index.ts",
        "plugins/ifc-export/src/guid.ts",
        "plugins/ifc-export/src/provenance.ts",
        "plugins/ifc-export/src/exporters/revit-variant.ts",
        "plugins/schedules/src/view.ts"
      ],
      "change": "Four edits at the plugins, one at the facade. (1) plugins/ifc-export/src/guid.ts:34 — change `} from '@pryzm/schemas/ifc';` to `} from '@pryzm/plugin-sdk';`. NO facade change needed: packages/plugin-sdk/src/index.ts:501 already carries `export * from '@pryzm/schemas/ifc';`. (2) plugins/ifc-export/src/exporters/revit-variant.ts:46 — change `import type { RevitExportOptions, RevitWorkset } from '@pryzm/schemas';` to `... from '@pryzm/plugin-sdk';`. NO facade change: index.ts:246 already carries `export * from '@pryzm/schemas';`. (3) plugins/schedules/src/view.ts:220 — the bypass is a TYPE-POSITION dynamic import, `function formatCell(value: import('@pryzm/schemas/schedule').FormulaResult, ...)`. Replace with a top-of-file `import type { FormulaResult } from '@pryzm/plugin-sdk';` and use the bare name. NO facade change: index.ts:509 already re-exports `type FormulaResult` from '@pryzm/schemas/schedule'. (4) plugins/ifc-export/src/provenance.ts:44 — change `} from '@pryzm/schemas/provenance';` to `} from '@pryzm/plugin-sdk';` AND add one line to packages/plugin-sdk/src/index.ts in the schemas sub-path block beside lines 495-501: `export * from '@pryzm/schemas/provenance';`. packages/schemas/package.json:28 already publishes `\"./provenance\": \"./src/provenance/index.ts\"`, so the sub-path resolves. Add `@pryzm/plugin-sdk` to plugins/schedules and plugins/ifc-export package.json dependencies if absent, and re-run pnpm install.",
      "why_this_shape": "WIRE, and three of the four need no facade work at all — the SDK ALREADY re-exports the exact symbols and the plugins simply reach past it. Measured before assuming: index.ts carries `export * from '@pryzm/schemas'` (:246), `export * from '@pryzm/schemas/ifc'` (:501) and `type FormulaResult` (:509). Only `@pryzm/schemas/provenance` is genuinely missing, and adding it is the gate's OWN remedy verbatim: 'If the SDK does not re-export what you need, WIDEN THE FACADE — do not import the package directly and do not raise this threshold.' Widening a facade is not raising a ceiling: the bypass COUNT falls. These four were chosen over the geometry-* and command-registry bypasses because they cross into L0 pure schemas — zero runtime behaviour, zero risk — whereas routing plugins/lift -> @pryzm/geometry-lift through the SDK would publish element geometry on a public marketplace surface, a product decision rather than a ratchet repair.",
      "acceptance_command": "npx tsx tools/ga-gate/check-layer-boundaries.ts > /tmp/w3r3c.txt 2>&1; echo \"RC=$?\" >> /tmp/w3r3c.txt; grep -E 'bypassing the L5 SDK facade|SDK-facade bypass' /tmp/w3r3c.txt",
      "acceptance_number": "`[check-layer-boundaries] L6 plugin imports bypassing the L5 SDK facade: 182` (was 186), and no `FAIL — 186 SDK-facade bypass(es), baseline 182` line. This lands EXACTLY at the ceiling with zero headroom — the next plugin that reaches past the SDK fails the gate.",
      "effort": "S",
      "depends_on": [],
      "risk": "Two. (1) `export *` collisions: adding `export * from '@pryzm/schemas/provenance'` to a barrel that already does `export * from '@pryzm/schemas'` fails to compile if provenance re-exports a name the root barrel also exports. Build the SDK (`pnpm --filter @pryzm/plugin-sdk build`) before assuming a one-line change; on a collision switch to the EXPLICIT named-list form the schedule block at :503-521 already uses. (2) Zero headroom afterwards means a concurrent lane adding a plugin bypass turns this green red without touching these files — land it late and re-run the gate immediately before merge."
    },
    {
      "id": "W3-R3-D",
      "title": "Layer gate arm 1 — remove three upward edges by deleting a shim and completing an existing injection seam; 105 -> 102",
      "disposition": "REMOVE",
      "addresses": ["R3"],
      "contract_clause": "C01 §Architecture (a layer may import from any LOWER layer, never a higher one); C16 §command authoring (dispatch by type, not by importing the command class); C101 §Annotation (the element shape belongs in L0 schemas)",
      "files": [
        "packages/command-registry/src/annotations/CreateManyAnnotationsCommand.ts",
        "packages/command-registry/src/index.ts",
        "packages/room-topology/src/RoomTagAutoPopulator.ts",
        "packages/file-format/src/export/sheets/ViewportSvgComposer.ts",
        "packages/schemas/src/annotation/index.ts",
        "plugins/annotations/src/subsystem/AnnotationTypes.ts"
      ],
      "change": "THREE edges, and the delta needed is exactly three. EDGE 1 (L2->L6, 1 occurrence): packages/command-registry/src/annotations/CreateManyAnnotationsCommand.ts is a SHIM whose only content is line 15, `export { CreateManyAnnotationsCommand } from '@pryzm/plugin-annotations';` — an L2 package re-exporting an L6 symbol. DELETE the file and its barrel line `export * from './annotations/CreateManyAnnotationsCommand';` at packages/command-registry/src/index.ts:22. Its one production consumer is packages/room-topology/src/RoomTagAutoPopulator.ts:32 (`import { CreateManyAnnotationsCommand } from '@pryzm/command-registry'`); repoint it to dispatch by TYPE STRING through the bus (`bus.executeCommand('annotation.createMany', payload)`) rather than importing the class — the C16 shape, which also removes RoomTagAutoPopulator's own pre-existing L2->L6 edge as a bonus. EDGES 2 and 3 (L3->L6, 2 occurrences, one file): packages/file-format/src/export/sheets/ViewportSvgComposer.ts:55 `import { annotationStore } from '@pryzm/plugin-annotations'` and :56 `import type { AnnotationElement } from '@pryzm/plugin-annotations'`. The injection seam ALREADY EXISTS — the options interface declares `annotations?: AnnotationElement[]` at :92 with the doc 'Omit to read annotationStore.getByView(viewId)', and `_readAnnotations()` at :391 is the only place the store is touched. Make the reader a REQUIRED injected function on the options (`readAnnotations: (viewId: string) => AnnotationElement[]`), delete `_readAnnotations` and the store import, and bind `annotationStore.getByView` at the composition root in apps/editor, where L7->L6 is legal. For the TYPE: MOVE `AnnotationElement` from its sole definition at plugins/annotations/src/subsystem/AnnotationTypes.ts:138 into packages/schemas/src/annotation/ (the directory already exists with index.ts and dimension.ts), have plugins/annotations re-export it from there so nothing downstream breaks, and import it in ViewportSvgComposer from `@pryzm/schemas/annotation` — L3->L0, legal.",
      "why_this_shape": "REMOVE for edge 1; REMOVE-by-completing-an-existing-seam for 2 and 3. D2 was decisive twice. Edge 1 LOOKS like 'command-registry needs the annotations command' — measured, it needs nothing: the file has no implementation, only a re-export, so there is nothing to invert, only a shim to delete. Edges 2/3 look like 'file-format needs an injection seam built' — measured, the seam is already declared and documented at :88-92 and only the DEFAULT falls back to the store, so this finishes a half-applied injection rather than designing one. THE EDGES NOT CHOSEN, and why: packages/spatial-index/src/RoomAutoFillClassifier.ts's two L1->L2 edges (to @pryzm/core-app-model and @pryzm/room-topology) are a LAYER-TABLE question, not a code defect — spatial-index sits at L1 while importing two L2 domain packages, the same shape CLAUDE.md already records for frame-scheduler. Reclassifying a package to make a violation count fall is how a gate stops meaning anything, so it is left alone and flagged for a separate table decision. packages/headless/src/minimalHeadlessBootstrap.ts -> @pryzm/scene-committer is documented in the gate's own header (commit 472ece67) as genuine and already inside the baseline; it is not new debt.",
      "acceptance_command": "npx tsx tools/ga-gate/check-layer-boundaries.ts > /tmp/w3r3d.txt 2>&1; echo \"RC=$?\" >> /tmp/w3r3d.txt; grep -E 'upward imports|within baselines' /tmp/w3r3d.txt",
      "acceptance_number": "`[check-layer-boundaries] upward imports between classified packages: 102` (was 105), and no `FAIL — 105 upward import(s), baseline 102` line. With W3-R3-A/B/C also landed, the gate's final line must read `✓ within baselines (violations 102/102, unclassified 13/13, sdk-bypass 182/182).` at RC=0.",
      "effort": "M",
      "depends_on": [],
      "risk": "Three. (1) Deleting a barrel export from @pryzm/command-registry removes a PUBLIC surface — grep every consumer of `CreateManyAnnotationsCommand` first (measured: packages/command-registry/src/index.ts:22, packages/room-topology/src/RoomTagAutoPopulator.ts:32, plugins/annotations/src/index.ts:208) and confirm nothing else resolves through the barrel at runtime. (2) Dispatch-by-type only works if `annotation.createMany` is actually registered on the bus when RoomTagAutoPopulator runs; if it is not, this converts a compile-time import into a silent runtime no-op — the exact 'committed is not reachable' failure. Prove the dispatch at the layer the user experiences, not by a function returning. (3) Moving AnnotationElement to schemas must be a MOVE with a re-export, never a copy: two definitions of one element shape is a C84 EI-9 breach and would be worse than the layer violation it fixes."
    },
    {
      "id": "W3-R4",
      "title": "Command naming — five non-canonical prefixes: two new domains, two camelCase drift, one L-796 duplicate spelling",
      "disposition": "REPLACE",
      "addresses": ["R4"],
      "contract_clause": "C16 §Command authoring (one prefix spelling per family; a command type is a wire identifier — rename on `type`, keep the old spelling in `aliases`); C87 curtain wall; C104 lift; C106 boundary line; C109 bathroom pod",
      "files": [
        "tools/ga-gate/check-command-naming.ts",
        "plugins/curtain-wall/src/handlers/ChangeCurtainWallLevel.ts",
        "plugins/boundary-line/src/handlers/AttachToBoundaryLine.ts",
        "plugins/boundary-line/src/handlers/CreateBoundaryLine.ts",
        "plugins/boundary-line/src/handlers/UpdateBoundaryLine.ts",
        "plugins/boundary-line/src/handlers/index.ts",
        "plugins/plumbing/src/handlers/CreateBathroomPod.ts",
        "plugins/plumbing/src/handlers/DeleteBathroomPod.ts",
        "plugins/balcony/src/handlers/index.ts",
        "plugins/lift/src/handlers/index.ts"
      ],
      "change": "THREE DIFFERENT REMEDIES — do not apply one rule to all five. (A) `curtainWall.*` (1 type) IS the L-796 defect: a SECOND spelling of a domain whose canonical form `curtain-wall` is already in CANONICAL_PREFIXES and carries 190 call sites against this one's 3. At plugins/curtain-wall/src/handlers/ChangeCurtainWallLevel.ts:79 set `type = 'curtain-wall.changeLevel'` and add `aliases: ['curtainWall.changeLevel']` on the handler — the old spelling is already in project_command_log and in replayed history, so the alias is mandatory. FORBIDDEN: adding `curtainWall` to CANONICAL_PREFIXES; the gate says so in its own failure text. (B) `bathroomPod.*` (2 types) and `boundaryLine.*` (5 types) are REAL new domains with no rival spelling anywhere (measured: `bathroom-pod` 0 hits, `boundary-line` 0 hits as a command prefix) but they are spelled camelCase, and the canonical convention for a multi-word domain in this set is KEBAB — `curtain-wall` is the only existing multi-word member and it is kebab. Rename to `bathroom-pod.create` / `bathroom-pod.delete` (CreateBathroomPod.ts:117, DeleteBathroomPod.ts:64) and `boundary-line.attach` / `.detach` / `.create` / `.update` / `.delete` (AttachToBoundaryLine.ts:75,173; CreateBoundaryLine.ts:67; UpdateBoundaryLine.ts:65,149), each carrying the camelCase spelling in `aliases`, then ADD `bathroom-pod` and `boundary-line` to CANONICAL_PREFIXES at tools/ga-gate/check-command-naming.ts:51-55. Update the handler-type const arrays in plugins/boundary-line/src/handlers/index.ts and every registry/dispatch site: grep `'boundaryLine.` (67 hits) and `'bathroomPod.` (25 hits) across plugins/, packages/command-registry/ and apps/editor/ and move each one; the `aliases` field keeps unmigrated callers and replayed logs resolving during the sweep. (C) `balcony.*` (3 types) and `lift.*` (2 types) are genuinely new single-word element domains with no rival spelling and no convention problem: ADD `balcony` and `lift` to CANONICAL_PREFIXES, change no command type, touch no call site. FORBIDDEN: adding anything to BASELINE_SPLITS (line 64) — that map is shrink-only and currently EMPTY, a state worth preserving.",
      "why_this_shape": "REPLACE for A/B, and the split is forced by MEASURED evidence rather than by the names. The gate itself draws the line: 'If this is a genuinely new element type, add its canonical prefix to CANONICAL_PREFIXES. If it is a second spelling of an existing domain, that is the L-796 defect — do NOT add it.' Deciding which is which required counting rival spellings, and the counts differ by two orders of magnitude: curtainWall 3 against curtain-wall 190 (a drifted duplicate of a live domain) versus balcony 46 / lift 49 / bathroomPod 25 / boundaryLine 67 each with a rival count of ZERO (genuinely new). Adding `balcony` and `lift` to CANONICAL_PREFIXES is the gate's sanctioned move and is NOT a ceiling raise — the ratchet in this gate is BASELINE_SPLITS.size, which stays at 0. ON THE SPELLING DRIFT THE AUDIT FOUND: `stair-landing` / `stair-railing` versus `stairLandingStore` / `stairRailingStore` is NOT a command-naming defect and must not be swept in here. Measured: the kebab forms are DOM event names and semantic kinds (`bim-stair-landing-added` at apps/editor/src/engine/geometryMutationEvents.ts:71, semantic 'stair-railing' at :73) while the camelCase forms are JavaScript identifiers (authoritativeStores.ts:55-56, initBuilders.ts:227-228). There are NO `stair-landing.*` or `stairLanding.*` COMMAND types — the gate reports 34 domains in use and neither spelling is among the five offenders. The rule that survives: wire strings are kebab, JS identifiers are camelCase, and the real defect in this task is four command TYPES that carried the identifier convention onto the wire.",
      "acceptance_command": "npx tsx tools/ga-gate/check-command-naming.ts > /tmp/w3r4.txt 2>&1; echo \"RC=$?\" >> /tmp/w3r4.txt; cat /tmp/w3r4.txt",
      "acceptance_number": "RC=0 and the line `[check-command-naming] domains in use: 36 · non-canonical prefixes: 0 (baseline 0)` — the domain count RISES to 36 because bathroomPod->bathroom-pod and boundaryLine->boundary-line are renames while balcony and lift join CANONICAL_PREFIXES; the load-bearing figure is the printed `non-canonical prefixes: 0`. No `FAIL — new non-canonical command prefix(es)` line.",
      "effort": "M",
      "depends_on": [],
      "risk": "The renames touch a WIRE IDENTIFIER persisted in project_command_log and replayed on load. Omitting `aliases` on any renamed handler silently breaks undo/redo replay and collaborative sync for every project that already logged the old spelling — surfacing as a load-time 'unknown command type' long after merge, on other people's data. Every rename in (B) must land WITH its alias, and the alias may only be deleted once no persisted log carries the old spelling. Second risk: 92 call sites move (67 boundaryLine + 25 bathroomPod); a partial sweep is safe ONLY because of the aliases, so do not delete them in the same commit."
    },
    {
      "id": "W3-R5-A",
      "title": "P6 commandManager — clear the exit-3 by removing all 12 literal `commandManager.execute` call sites",
      "disposition": "REPLACE",
      "addresses": ["R5"],
      "contract_clause": "C14 §Legacy elimination; C03 §4 (commands are the only mutation path); STR-03 §2 P6",
      "files": [
        "apps/editor/src/engine/engineLauncher.ts",
        "apps/editor/src/engine/persistence/ProjectLoader.ts",
        "apps/editor/src/engine/RemoteCommandDispatcher.ts",
        "apps/editor/src/engine/views/plantools/LinearDimPlanToolHandler.ts",
        "apps/editor/src/engine/views/plantools/StairPath3DToolHandler.ts",
        "apps/editor/src/ui/create/batchCatalogue.ts",
        "apps/editor/src/ui/documentation/autoTagActiveView.ts",
        "apps/editor/src/ui/documentation/commitAnnotationSet.ts",
        "apps/editor/src/ui/documentation/reconcileDimensions.ts",
        "apps/editor/src/ui/property-panel/replaceCurtainPanelType.ts"
      ],
      "change": "THE TWELVE, named with the line numbers the gate prints: engineLauncher.ts:754 `const res = commandManager.execute(`; engineLauncher.ts:1458 `commandManager.execute(`; persistence/ProjectLoader.ts:655 `const exec = (cmd: any) => this.commandManager.execute(cmd, LOAD_META);`; RemoteCommandDispatcher.ts:392 `this.commandManager.execute(command);`; views/plantools/LinearDimPlanToolHandler.ts:273 `commandManager.execute(new CreateAnnotationCommand(annotation));`; views/plantools/StairPath3DToolHandler.ts:560 `this._deps.commandManager.execute(new AddLevelCommand({ levelId, name, elevation, height }));`; ui/create/batchCatalogue.ts:592 `const res = deps.commandManager.execute(cmd, { source: 'CREATE_PANEL_BATCH' });`; ui/documentation/autoTagActiveView.ts:500 `commandManager.execute(cmd);`; ui/documentation/commitAnnotationSet.ts:154 `for (const el of removals) commandManager.execute(new DeleteAnnotationCommand(el.id));`; commitAnnotationSet.ts:156 `commandManager.execute(new CreateManyAnnotationsCommand(annotations));`; ui/documentation/reconcileDimensions.ts:268 `commandManager.execute(new UpdateAnnotationCommand(tagId, {...}));`; ui/property-panel/replaceCurtainPanelType.ts:110 `const r = args.commandManager.execute(cmd);`. Migrate each to `runtime.bus.executeCommand(...)` per the plan the gate cites (docs/03_PRYZM3/04-PLAN-FORWARD/54-COMPLETE-LEGACY-ELIMINATION-PLAN.md §5), preserving the metadata argument where one is passed (LOAD_META; `{ source: 'CREATE_PANEL_BATCH' }`) — the bus takes the same options object. THREE of the twelve need care rather than a textual swap: ProjectLoader.ts:655 wraps execute in a closure used across the whole load path (change the closure BODY, not its 40+ call sites); RemoteCommandDispatcher.ts:392 executes a command arriving off the wire from another client and must keep whatever undo/echo semantics it has today (verify against the sync path first); commitAnnotationSet.ts:154 executes inside a LOOP, so route it through the batch path rather than N bus dispatches, or it trades a P6 breach for the per-add traversal cost check-batch-creation-coverage exists to catch.",
      "why_this_shape": "REPLACE. This is not an unwired capability — `runtime.bus.executeCommand` exists, is the declared single mutation path, and is already used throughout the same directories; these twelve are residue of the legacy manager. D2 check against the opposite error: the ONE historically load-bearing site, PlanViewToolOverlay.ts:786's `(window as any)['commandManager']`, is GONE (deleted by afe0cdad on 2026-08-16 — verified: `grep -n commandManager apps/editor/src/engine/views/PlanViewToolOverlay.ts` returns 3 hits, none a cast, none at :786), so the cross-ratchet trap the P4 baseline file documents does not apply to any of the twelve. Note this task clears the EXIT-3 only; the gate's other two arms fail at their declared level and exit 1 — see W3-R5-B.",
      "acceptance_command": "npx tsx tools/ga-gate/check-no-commandmanager.ts > /tmp/w3r5a.txt 2>&1; echo \"RC=$?\" >> /tmp/w3r5a.txt; grep -E 'code-only|OK \\(literal\\)|FAIL|WORSE' /tmp/w3r5a.txt",
      "acceptance_number": "The line `[no-commandmanager] OK (literal): 0 / 0` and a header reading `code-only (ENFORCED) literal=0 ...` (was `literal=12`). The `❌ WORSE THAN DECLARED (exit 3): literal 12 > 11` line must be GONE. RC stays 1 until W3-R5-B — that is the ledgered level and the correct intermediate state.",
      "effort": "L",
      "depends_on": [],
      "risk": "Three sites are not textual swaps and each can fail silently. ProjectLoader.ts:655's closure carries LOAD_META, which almost certainly suppresses undo-stack entries during load — losing it makes a project load push hundreds of undo entries. RemoteCommandDispatcher.ts:392 applies REMOTE commands; if executeCommand re-broadcasts where execute did not, this creates an echo storm across collaborating clients. commitAnnotationSet.ts:154 is a creation loop — swapping it to N bus dispatches would put a NEW unbatched loop in front of check-batch-creation-coverage in the same window that W3-R8 fixes its ledger. Prove each of the three at the layer the user experiences: load a project, edit from two clients, commit an annotation set."
    },
    {
      "id": "W3-R5-B",
      "title": "P6 commandManager — the two ledgered arms, window 57/2 and cm.execute 62/49, that stand between exit 1 and RC=0",
      "disposition": "REPLACE",
      "addresses": ["R5"],
      "contract_clause": "C14 §Legacy elimination; C03 §4; STR-03 §2 P6",
      "files": [
        "apps/editor/src/engine/initTools.ts",
        "apps/editor/src/engine/initScene.ts",
        "apps/editor/src/engine/BimService.ts",
        "apps/editor/src/engine/views/PlanViewToolOverlay.ts",
        "apps/editor/src/engine/views/SvpPlanToolOverlay.ts",
        "apps/editor/src/ui/layout/DockingLayout.ts",
        "apps/editor/src/ui/layout/NavigationAreaLayout.ts",
        "apps/editor/src/ui/house-layout/HouseLayoutExecutor.ts",
        "apps/editor/src/ui/office-building/OfficeBuildingExecutor.ts",
        "apps/editor/src/ui/residential-building/ResidentialBuildingExecutor.ts",
        "apps/editor/src/ui/apartment-layout/ApartmentLayoutExecutor.ts"
      ],
      "change": "Two arms, one root. THE ROOT is apps/editor/src/engine/initTools.ts:3132 `window.commandManager = commandManager;` — the single publication that makes all 57 window-alias reads possible; remove it LAST, after its readers are gone. WINDOW ARM (57 -> <=2, ceiling 2): the 57 matching lines are enumerated in audit/element-creation/2026-08-29/raw/check-no-commandmanager.log. The concentrations are composition sites that read the global as a FALLBACK beside an already-injected manager — initScene.ts:836, PlanViewToolOverlay.ts:495, SvpPlanToolOverlay.ts:573, DockingLayout.ts:161/174, NavigationAreaLayout.ts:115, BimService.ts:40 — for these, DELETE the `?? window.commandManager` fallback and make the injected path required, a strictly smaller change than a migration. The remainder are `window.commandManager` reads inside UI panels (AIPanel, ValidatePanel, AuditBucket, StrategizeBucket, LeftNavRail, KitchenCabinetTool, CreatePanelLayout, BriefInputPanel, VariantBrowserPanel, GridPlanToolHandler, OpeningPlanToolHandler, StairPathPlanToolHandler, StairPlanToolHandler, canvasSubjectDelete, AnnotationPlanToolHandlers, LinearDimPlanToolHandler, WallPerfBench) which need `runtime.bus` threaded in. CM.EXECUTE ARM (62 -> <=49, ceiling 49): dominated by the four generation executors — HouseLayoutExecutor.ts (9 sites: :423, :451, :1394, :2581, :2749, :2882, :3243, :3450, :3457), OfficeBuildingExecutor.ts (9: :894, :1015, :1052, :1106, :1187, :1197, :1302, :1323, :1381 plus officeFurnish.ts :115, :160, :178), ResidentialBuildingExecutor.ts (:382, :399, ...) and ApartmentLayoutExecutor.ts (:414, :427, :438) — all calling `cm.execute?.(new XCommand(...))` on an aliased manager. Migrating ONE executor clears the 13-site overage. FORBIDDEN: raising LEDGERED_WINDOW (62), LEDGERED_CM_EXEC (62), WINDOW_CEILING (2) or CM_EXEC_CEILING (49) at tools/ga-gate/check-no-commandmanager.ts:113/120/385/386, and setting the CMDMGR_* env overrides in CI to step them back up.",
      "why_this_shape": "REPLACE, scoped as its own XL task rather than folded into W3-R5-A because the two are different in kind: R5-A clears an EXIT-3 (a hard regression the ledger may never absorb) and is required now; this clears an exit-1 that gate-debt.json has legitimately declared. Naming it separately is the point — the brief's framing 'literal 12 against ceiling 0' describes one of three breached arms, and a plan stopping at R5-A would leave a reader believing check-no-commandmanager reaches RC=0, which it does not. D2 note: `window.commandManager` is not an absent capability being worked around — `runtime.bus` exists and is ALREADY injected at most of these call sites, sitting unused beside the global fallback, which is why most of the 57 are a DELETION of a fallback rather than a migration. The effort is XL from breadth, not difficulty.",
      "acceptance_command": "npx tsx tools/ga-gate/check-no-commandmanager.ts > /tmp/w3r5b.txt 2>&1; echo \"RC=$?\" >> /tmp/w3r5b.txt; grep -E 'code-only|OK \\(' /tmp/w3r5b.txt",
      "acceptance_number": "RC=0 with all three OK lines printed: `OK (literal): 0 / 0`, `OK (window): 2 / 2` (or lower), `OK (cm.execute): 49 / 49` (or lower). SEPARATELY re-run `npm run check:commandmanager` — a DIFFERENT counter (scripts/check/ci-check-no-commandmanager.mjs), currently `FAIL -- count 139 exceeds threshold 136 (+3)` — and record its own number; never quote one gate's figure for the other.",
      "effort": "XL",
      "depends_on": ["W3-R5-A"],
      "risk": "Removing `window.commandManager = commandManager` (initTools.ts:3132) deletes a load-bearing publication read by 57 sites plus anything reaching it at runtime through a name this gate cannot see — dev-console tooling, e2e specs, and any `(window as unknown as ...)` spelling the P4 arm is blind to (W3-R1-B's new arm would surface those). Delete the publication only after a grep in EVERY spelling returns zero, and expect the failure mode to be a silent no-op in a UI panel rather than a compile error. Second risk: three of the four generation executors are the house/office/residential pipelines, whose defect history is long; changing their dispatch path risks re-opening layout regressions that have their own issue-log rows."
    },
    {
      "id": "W3-R6",
      "title": "Graph write coverage — the four refusal-bearing readers already exist and are already consumed; the gate's CLOSED reader map never learned their names",
      "disposition": "WIRE",
      "addresses": ["R6"],
      "contract_clause": "C71 §1.3 / §3 / §4.4 (a typed, refusal-bearing reader; an untyped sweep is not a reader); C70 C-INV-4",
      "files": ["tools/ga-gate/check-graph-write-coverage.ts"],
      "change": "Add four entries to the CLOSED `DEDICATED_READERS` map at tools/ga-gate/check-graph-write-coverage.ts:343-350, each carrying the two-line comment shape the three existing entries use (the §-tag and the named CONSUMER, because the map's own rule is that a helper with no production caller earns no credit): `getHostedOpenings: 'hosts',` — §HOSTS-FORWARD-READER, consumer packages/ai-host/src/SemanticQueryEngine.ts:461 (also :185); `getBoundingWalls: 'boundedBy',` — §BOUNDEDBY-READER, consumer apps/editor/src/engine/consequence/WallDeleteConsequencePlanner.ts:767 (also packages/ai-host/src/SemanticQueryEngine.ts:176); `getConnectedRooms: 'connectedTo',` — §GR13-ADJACENCY-READER, consumer packages/ai-host/src/SemanticQueryEngine.ts:164 (also packages/ai-host/src/WorldModelAdapter.ts:218); `getContainedElements: 'contains',` — §GR13-CONTAINS-READER, consumer apps/editor/src/ui/dataworkbench/HierarchyTreePanel.ts:618 (also packages/ai-host/src/WorldModelAdapter.ts:219). Each of the four is declared on SemanticGraphManager in packages/core-app-model/src/SemanticGraph.ts (documented at :366 getHostedOpenings, :252/:300 getBoundingWalls, :457 getConnectedRooms, :474 getContainedElements) and each returns an `{ok:...}` result distinguishing 'covered, empty' from 'cannot answer' — exactly the C71 §4.4 property the map's header requires. CHANGE NO PRODUCTION CODE, and do NOT touch the §LEDGER: it stays at 0 findings, and the four findings disappear because they were never real.",
      "why_this_shape": "WIRE — the clearest D2 case in the workstream, and reading it as REPLACE would have been actively destructive. The gate reports 'REQUIRED family hosts has NO typed production reader — write-only state nobody can query' for all four, which sounds like four missing readers. Measured, all four readers EXIST, are refusal-bearing, and have named production consumers; what is missing is four lines in the gate's CLOSED recognition map, which by design credits only names it has been told about. Worse, the naive fix — adding a bare `getTargets(id, 'hosts')` to satisfy rule (r1) — is EXPLICITLY WRONG here and the production comments say why at length: SemanticQueryEngine.ts:453 records that the bare `getTargets(wall.id,'hosts')` printed '0 element(s) hosted in wall' for a wall the graph had never heard of, and :149 records that the bare `getTargets(r.id,'connectedTo')` indicted every room in the project as having no door before any detection pass had run. Those bare reads were DELETED ON PURPOSE and replaced by refusal-bearing surfaces; re-adding them to make a gate green would restore a shipped defect with a contract citation attached. Adding four names to DEDICATED_READERS is also not ledger-absorption: the ledger is the shrink-only artefact and stays at 0, while DEDICATED_READERS is the gate's vocabulary — precisely how `getHostWall`/`hostedBy` and `getElementsSittingOn`/`sitsOn` were paid on 2026-08-13.",
      "acceptance_command": "npx tsx tools/ga-gate/check-graph-write-coverage.ts > /tmp/w3r6.txt 2>&1; echo \"RC=$?\" >> /tmp/w3r6.txt; grep -E 'C-INV-4|UNLEDGERED|RATCHET' /tmp/w3r6.txt",
      "acceptance_number": "RC=0 and `C-INV-4 — RATCHET: 0 finding(s) against a NAMED ledger of 0.` with no `⛔ UNLEDGERED` lines. The matrix rows for hosts / boundedBy / connectedTo / contains must change from `✗` in the reader column to a credited site (was `hosts  ● 2  ✗  ● 1  ○53  rebuilt`).",
      "effort": "S",
      "depends_on": [],
      "risk": "The failure mode is crediting a reader that is not actually refusal-bearing, which hands the gate a false green on the one property it exists to check. Before adding each entry, open packages/core-app-model/src/SemanticGraph.ts and confirm the method's return type is the `{ok:true,...} | {ok:false, reason}` union and NOT a bare array — a method returning `[]` for both 'empty' and 'unknown' must not enter this map whatever it is called. Second risk: crediting a reader whose only caller is a test; all four consumers named above are production files, but verify none is under __tests__ before landing."
    },
    {
      "id": "W3-R8",
      "title": "Batch-creation coverage — delete the ledger line for debt that was actually paid",
      "disposition": "REMOVE",
      "addresses": ["R8"],
      "contract_clause": "C17 §Batch creation catalogue; C87 §curtain wall; C11 §4.2 (bulk creation uses BatchCoordinator); gate-debt.json rule 2 (a ledger that keeps paid debt rots into a list of things that are secretly fine)",
      "files": ["tools/ga-gate/check-batch-creation-coverage.ts"],
      "change": "Delete the three-line LEDGER entry for `CreateCurtainWallsFromSlabCommand` at tools/ga-gate/check-batch-creation-coverage.ts:97-99, leaving the other three entries (CreateAllSlabsFromLevelToTopLevelCommand, CreateMultipleLevelsCommand, CreateGridSystemCommand) intact. VERIFY FIRST — do not take the gate's word for it: packages/command-registry/src/curtainwall/CreateCurtainWallsFromSlabCommand.ts:275-278 now reads `if (batchCoordinator.isBatching) { ... } else { batchCoordinator.runBatch(_createCurtainWalls, {...}) }`, and `batchCoordinator.runBatch(` is exactly the literal the gate's classifier tests for at check-batch-creation-coverage.ts:90 to return 'BATCHED'. The §CW4-CW-BY-SLAB-BATCH comment at :270 records the JOIN-or-open decision and cites C11 §4.2 and C16 §8.7 (L-1162). So the classification change is a real fix, not detector drift.",
      "why_this_shape": "REMOVE. The gate is not failing because something broke — it is failing because something was FIXED and the ledger line outlived it, which the gate detects deliberately (`paidDebt` computed at :292, reported at :335). Deleting a paid line is the required action and the opposite of raising a ceiling: the ledger shrinks 4 -> 3 and cannot grow back without an explicit decision. D2 check: could this instead be a detector that stopped seeing an unbatched loop still present? Ruled out by reading the SOURCE rather than the gate — the `runBatch` call is on line 278 with a JOIN arm above it, and the ledger's own text ('fires bim-curtainwall-added per panel, so it DOES pay the per-add traversal') describes behaviour the batch bracket now defers.",
      "acceptance_command": "npx tsx tools/ga-gate/check-batch-creation-coverage.ts > /tmp/w3r8.txt 2>&1; echo \"RC=$?\" >> /tmp/w3r8.txt; cat /tmp/w3r8.txt",
      "acceptance_number": "RC=0 and the line `[batch-creation-coverage] OK: 3 unbatched creation loop(s), exactly the 3 on the ledger` — with the header still reading `live catalogue entries: 16 (floor 10) · command classes indexed: 307 · batched: 13 · unbatched loops: 3`. Was: `FAIL: 1 ledger entr(ies) no longer loop unbatched`.",
      "effort": "S",
      "depends_on": [],
      "risk": "Small but real: this gate is one of the seven flagged by check-gate-subject-floors (R7) as 'declares a floor but has no reachable process.exit(2)', so if its catalogue parse silently yields nothing it can print 0 unbatched loops over an unread subject and the deletion would look confirmed by a blind run. Confirm the header prints `live catalogue entries: 16` and `command classes indexed: 307` — non-zero subject counts — before trusting the OK. The floor repair itself belongs to W1/R7; sequence this after it if both are in flight."
    }
  ]
}
