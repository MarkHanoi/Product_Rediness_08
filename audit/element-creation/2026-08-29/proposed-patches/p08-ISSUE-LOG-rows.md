# p08 — PROPOSED `L-` ROWS FOR `docs/04-reference/ISSUE-LOG.md`

**PROPOSED, NOT APPLIED (D9).** Audit `audit/element-creation/2026-08-29` · measured 2026-08-29 ·
**revised 2026-08-30** (two corrections, both recorded in place rather than silently edited: L-12839's *"a spelling that does not exist anywhere"* was FALSE — `curtainwall.create` is a live registered bus alias; and L-12844's acceptance criteria were self-defeating substring greps) ·
HEAD `064a838e` · branch `main`.

## How to apply

Append the rows below **verbatim, in order, to the END of §2's table** in
`docs/04-reference/ISSUE-LOG.md`. The file's own instruction is *"Append new issues to the bottom
of §2 and tick the area table in §3."*

**Row format, matched exactly to the file's existing rows** (measured, not assumed):

```
| **L-NNNNN** | <text> | **STATUS** |
```

**Numbering.** The current maximum is **L-12824**, measured with:

```bash
grep -o '\*\*L-[0-9]\+\*\*' docs/04-reference/ISSUE-LOG.md | grep -o '[0-9]\+' | sort -n | tail -1
# -> 12824      (480 L- rows total: grep -c '^| \*\*L-[0-9]' -> 480)
```

This block claims **L-12830 – L-12846**, leaving a five-number gap after 12824 so a concurrent lane
appending 12825–12829 does not collide. **Re-run the command before applying** — if the maximum has
moved past 12830, renumber the block from `max + 6` and keep it contiguous.

**Status vocabulary**, measured from the file (`| **FIXED** |` ×181, `| **OPEN** |` ×138, plus a
small tail of one-off verdicts): every row below is **OPEN**, because none of them is fixed. Rows
whose only deliverable is a document correction are marked **OPEN** with the proposed patch named
in the text, not **FIXED** — the patch is proposed, not applied.

---

## The rows

| **L-12830** | ⛔ **OPEN (P0) — `check-single-compose.ts` PRINTS A HARD-CODED `0 rivals` WHILE ITS OWN BODY NAMES A RIVAL. A false green manufactured inside the instrument.** `npx tsx tools/ga-gate/check-single-compose.ts > /tmp/sc.txt 2>&1; echo "RC=$?"` → **RC=0**. The body prints *"files scanned: 5030 · composeRuntime definitions: 1 · **rival runtime factories: 1** · production callers: 2"* and then lists it by name: `apps/component-editor/src/app/familyEditorRuntime.ts:85  createFamilyEditorRuntime`. Eight lines later the terminal line reads *"✓ one composition root (…), **0 rivals**, 2/2 production caller(s)."* — and `0 rivals` at **`check-single-compose.ts:216` is a STRING LITERAL, not the count**. `MAX_RIVALS` defaults to 1 (`:103`), so the gate passes **at a ratcheted ceiling of one rival while reporting zero**. ⭐ **A reader who trusts the terminal line — which is what CI logs show — gets the wrong number from the right gate.** This is the exact defect class the audit exists to hunt, sitting inside the instrument. **Disposition: REPLACE the literal with the counted value** (and print the ceiling: `N rivals / MAX_RIVALS`). ⚠ **Do NOT "fix" this by editing `CLAUDE.md:140`** (*"1 definition / 0 rivals"*) — correcting the prose while the gate keeps printing a literal leaves the false green in the place CI reads. **Acceptance (D3):** the gate's terminal line must print the same integer as its body — `1 rival / ceiling 1` — not a tick. | **OPEN** |
| **L-12831** | ⛔ **OPEN (P0) — THE MERGE-BLOCKING `ga-gate` JOB FAILS AT `064a838e`. SEVEN registered gates exit 3, and NO LEDGER MAY ABSORB ANY OF THEM.** `check-cast-count` (P4) scoped strict **11 / 3**, headroom **−8**, repo-wide **101 / 100** · `check-otel-spans` (P8) Zone B **66 uninstrumented of 87 / baseline 52** · `check-layer-boundaries` **all four arms**: upward **105/102**, SDK-bypass **186/182**, banned-3p **123/113**, unclassified **15/13** · `check-command-naming` **5 / 0** · `check-no-commandmanager` literal `commandManager.execute` **12 / 0** · `check-graph-write-coverage` **4 findings / NAMED ledger of 0** · `check-gate-subject-floors` **7 unfloored / 0**. Plus `check-batch-creation-coverage` at **RC=1**. `run-all.ts:1162` sets `anyFailed=true` on `code===3` **UNCONDITIONALLY** (§RATCHET-EXCEEDED-IS-NEVER-DEBT, R7 / L-836). ⭐ **These are honest FAILs with named subjects, not misconfigurations** — the adversarial pass measured `exit_code_2 = 0 · ENOENT = 0 · empty_scope = 0` across the suite, and all 98 registered scripts resolve on disk. ⛔ **Raising any ceiling, or adding any of the seven to `gate-debt.json`, is the ONE FORBIDDEN FIX.** **Acceptance (D3):** `npx tsx tools/ga-gate/run-all.ts; echo "RC=$?"` → RC=0 **and** the printed per-gate table carries **zero** rows with exit code 3. | **OPEN** |
| **L-12832** | ⛔ **OPEN (P1) — SEVEN OF 73 GATES HAVE NO SUBJECT FLOOR, SO THEY CAN SCAN AN EMPTIED DIRECTORY AND REPORT `0` FOREVER. THREE OF THEM CURRENTLY RETURN RC=0.** `npx tsx tools/ga-gate/check-gate-subject-floors.ts; echo "RC=$?"` → **RC=3**, *"gates inspected: 73 · floored: 66 · unfloored: 7"*, against a ceiling of 0. In the gate's own words: *"A gate without a subject floor cannot tell '0 violations' from 'walked nothing'."* The seven: `check-material-id-required.ts` (no floor, no exit-2 path — **returns RC=0**), `check-material-maps-tiling.ts` (exit-2 path, no declared floor — **RC=0**), `check-runtime-arg-omitted.ts` (neither — **RC=0**), `check-property-rac-matrix.ts`, `check-render-aggregate-seam.ts`, `check-tool-activator-coverage.ts`, `check-batch-creation-coverage.ts` (declares a floor but has **no reachable `process.exit(2)`**). ⭐ **Those three greens are not refuted — they are UNFALSIFIABLE, which is a different and worse state than wrong.** The gate that polices the instrument's honesty is itself the exit-3. **Disposition: WIRE** — every one of the seven exists and runs; each needs a declared floor plus a reachable exit-2. **Acceptance (D3):** the gate must print *"unfloored: 0"*. | **OPEN** |
| **L-12833** | ⛔ **OPEN (P1) — `check-render-aggregate-seam.ts` IS COMMITTED, TRACKED, FAILING — AND REGISTERED IN NO RUNNER. A gate policing nothing.** `git ls-files -- tools/ga-gate/check-render-aggregate-seam.ts` returns the path (TRACKED, committed `377dd06b`, L-10530). `grep -c check-render-aggregate-seam tools/ga-gate/run-all.ts` → **0**; the same grep over `.github/workflows/ci.yml` → **0**. Run by hand: `npx tsx tools/ga-gate/check-render-aggregate-seam.ts; echo "RC=$?"` → **RC=1**, arm A 0/0, **arm B 50 / 49**, across **7,984 files** — a non-empty scope. ⛔ **`run-all.ts:1009-1041` (`GATE-AUTHORED-BUT-UNWIRED`) exists to catch exactly this and CANNOT, because the gate is invisible to the set it compares.** ⭐ **Disposition: WIRE, not REMOVE — this is D2 in one line.** The gate is not absent and not vestigial: it exists, its scope is non-empty, and it is failing on a real arm. Deleting it deletes a live finding; ranking it as missing authorises a second render-seam gate beside it. **Acceptance (D3):** the gate appears in `run-all.ts`'s `GATES` array **and** prints arm B ≤ 49. | **OPEN** |
| **L-12834** | ⚠ **OPEN (P1) — A `gate-newly-measured.json` LEDGER KEY THAT CAN NEVER MATCH, AND NO CHECK EXISTS TO NOTICE.** The ledger carries `check-dependent-adapts-on-host-move.ts` with `runner: "ga-gate"`, while `run-all.ts` registers the same gate as `../rac-conformance/certification/gates/check-dependent-adapts-on-host-move.ts`. `run-all.ts:1181` does `newlyMeasured.get(gate.script)` against the **exact registered string**, so the lookup can never hit: the gate is on **no** ledger and its measured exit 1 falls through to the REGRESSION branch at `run-all.ts:1212`, which sets `anyFailed=true`. ⭐ **`run-all.ts` has NO orphan-ledger-key check** — the only key checks are `inBoth` (`:915`) and `malformed` (`:941`, missing `exitCondition`/`reviewBy`/`runner`). The other 7 of the 8 orphan keys carry `runner: certify` and are deliberately absent from `GATES` (disclosed at `run-all.ts:1303`); **this one is the real defect.** **Disposition: WIRE** — correct the key to the registered path **and** add the orphan-key arm, or the next rename reproduces it silently. **Acceptance (D3):** a run of `run-all.ts` prints 0 orphan ledger keys. | **OPEN** |
| **L-12835** | ⛔ **OPEN (P0) — `door.create` REFUSES EVERY WELL-FORMED PAYLOAD BY CONSTRUCTION, AND THE VERB THAT DOES EXECUTE WRITES A STORE NOTHING READS. THE FAMILY IS NEVERTHELESS REACHABLE — VIA A DIFFERENT VERB.** Three separate facts, and collapsing them is how this stayed invisible: (1) `plugins/door/src/handlers/CreateDoor.ts:130-136` rejects every well-formed payload — *dispatchable* is the first failing link; (2) `door.batch.create` **is** registered, validated and undoable, and writes a plugin DTO store that **nothing renders, projects, persists or exports** — DISPATCHABLE BUT DETACHED; (3) the door family reaches 3-D and plan **through `wall.opening.create`** (`§P2.3` / `§P2.3-DOOR`, `initTools.ts:1530-1653`), which is why no user-visible symptom exposed (1) or (2). ⛔ **Its registration proof asserts the RIVAL object** — the test that "proves" the family is wired reads the detached DTO store, not the store the render path consumes, so it would keep passing if the working path were deleted. **`window` is the same shape** (`CreateWindow.ts:95-101`; `window.batch.create` is never dispatched by any production executor). ⭐ **Disposition: REMOVE or REPLACE — decide explicitly, do NOT wire a second door path.** The working path exists; a third implementation is strictly worse than the two already here. **Acceptance (D3):** either `door.create` accepts a payload the plan tool actually emits and a read-back test proves the element in the store the renderer consumes, **or** the verb and its DTO store are deleted and the proof re-pointed at `wall.opening.create` — **the printed criterion is a non-zero element count read back from the AUTHORITATIVE store, never RC=0.** | **OPEN** |
| **L-12836** | ⛔ **OPEN (P0) — EIGHT FAMILIES ARE DISPATCHABLE AND INVISIBLE, AND READING THE PER-FAMILY ROWS SPLITS THEM INTO THREE DIFFERENT DEFECTS.** The Phase-1 count is CEB `.create` cases **22** vs `initTools` `.created` bridges **15** → gap **8**: `room grid plumbing structural annotation dimension balcony pool`. ⭐ **The Phase-3 rows show they are NOT one bug**, and treating them as one would produce eight wrong fixes: **(a) CORRECT, no bridge needed — `balcony` and `pool` mirror by MEMBER re-stamping** (`§FT1`, `§P3.2-FL`, `§FT-HANDRAIL`, `§FT-WATER`, `§MIRROR-UPDATE`) and `room` renders through `initBuilders.ts:495-509` (`bim-room-added`/`-updated`/`-removed`). Their first failing links are `exports`, `renders_plan` (water only) and `renders_plan` respectively — **not the bridge.** **(b) GENUINELY UNREACHABLE — `grid`, `structural`, `dimension`**: each has a live CEB case, a resolving `affectedStores`, and **no live dispatcher**. `grid`'s `GridPlacementTool` is constructed only inside `runtime.tools.register('grid:tool', …)` and `'grid:tool'` is never activated; `structural`'s tool is the same shape; `dimension`'s `DimensionTool` **is not exported from the plugin barrel**. `grep -rnoE 'new [A-Za-z]+Committer\(' apps packages plugins src` (tests INCLUDED) returns **zero** hits for `GridCommitter`, `RoomCommitter`, `StructuralCommitter`, `DimensionCommitter`, `StairCommitter` — **only Wall / Slab / Door / Window committers are ever constructed**, all at `bootstrap.render.everything.ts:135-144`. **(c) SPLIT — `plumbing` has TWO create verbs with opposite verdicts**: `plumbing.createFixture` works to `exports`, while the DTO verb `plumbing.create` emits a `plumbing.created` with **zero subscribers repo-wide**. **(d) `annotation`** renders on its own layer and fails at `exports`. **Disposition: WIRE (b) — the tools EXIST and are unactivated; do NOT rewrite them. NO ACTION on (a). Decide (c) explicitly.** **Acceptance (D3):** for each of `grid`/`structural`/`dimension`, a read-back test that dispatches through the composed bus and prints a non-zero element count from the store the renderer consumes. | **OPEN** |
| **L-12837** | ⛔ **OPEN (P0) — READ-BACK IS PROVEN FOR 7 VERBS ACROSS 3 FAMILIES OF 361 VERBS AND 24 FAMILIES, AND BOTH MIRROR GATES EXIT 0.** `npx tsx tools/ga-gate/check-mirror-completeness.ts` → **RC=0**: **200 verbs write a plugin DTO store · 44 have a bridge case · 156 do not** (109 UNMIRRORED + 47 declared-exempt). That gate establishes only that a channel is **DECLARED**. `npx tsx tools/ga-gate/check-mirror-reachability.ts` → **RC=0**: 14 verbs dispatched across 3 families — **REACHES-SUBSCRIBER 7 · NO-TYPED-EVENT 4 · REFUSED 3 · 9 of 28 channels ORPHANED**. ⭐ **Read the two rows together: the read-back half of the pipeline is measured for roughly 2% of the surface, and both gates are green.** This is D3 at scale — RC=0 over an unmeasured majority. **Disposition: WIRE** — extend `check-mirror-reachability` family-by-family; the 109 UNMIRRORED verbs are create-mirror-without-update-mirror at scale (a command that commits, reports success, and changes nothing on screen). **Acceptance (D3):** the reachability gate prints a `verbs dispatched` numerator that is a stated fraction of 361, and the orphaned-channel count drops below 9 — never a tick. | **OPEN** |
| **L-12838** | ⛔ **OPEN (P1) — `water` IS A BRIDGE LISTENING FOR AN EVENT NOTHING EMITS BY ITS OWN NAME; `lift` IS AN ELEMENT FAMILY WITH NO L0 SCHEMA. Opposite defects, and neither is what its name suggests.** **`water`:** `grep -n "case 'water" packages/runtime-composer/src/CommandEventBridge.ts` → **0 hits**; there is no `water.create` case. `water.created` IS emitted — from **inside** `case 'pool.create'` (`CommandEventBridge.ts:2049-2068`), geometry-complete. The `§FT-WATER` bridge (`initTools.ts:1943`) therefore works **only** as a pool member and is dead for any standalone dispatch. ⭐ **Disposition: this is CORRECT AS BUILT for the pool path — the defect is that the family is registered as though it had an independent create verb.** Do not add a `water.create` case to "fix" it; either document the member-only contract or remove the standalone registration. **`lift`:** `grep -rhoE "defineElement\('[a-zA-Z-]+'" packages/schemas/src | sort -u` does **not** contain `lift` — there is **no L0 Zod record**, no `BaseNodeShape`, while `plugins/lift`, `packages/geometry-lift` and a live `§FT-LIFT` bridge all exist and the family dispatches, renders and persists. **Disposition: WIRE — author the missing `defineElement('lift')`.** ⚠ Ranking either of these as "missing" produces the wrong fix in **both** directions. **Acceptance (D3):** `grep -c "defineElement('lift')" packages/schemas/src/elements/*.ts` → ≥ 1; and a named decision recorded for `water`'s registration. | **OPEN** |
| **L-12839** | ⛔ **OPEN (P1) — FIVE NON-CANONICAL COMMAND PREFIXES AGAINST A BASELINE OF ZERO, AND ALL FIVE ARE ELEMENT FAMILIES.** `npx tsx tools/ga-gate/check-command-naming.ts; echo "RC=$?"` → **RC=3**, **5 / 0**: `balcony`, `bathroomPod`, `boundaryLine`, `curtainWall`, `lift`. ⚠ **This interacts with a measured spelling defect and must be fixed in one move, not two:** the CEB cases are **hyphenated** — `case 'curtain-wall.create'` (`CommandEventBridge.ts:694`) and `case 'curtain-wall.batch.create'` (`:758`) — and `grep -n "case 'curtainwall" packages/runtime-composer/src/CommandEventBridge.ts` returns **0**. C11 §11's matrix cell named `curtainwall.create` as though it were the CEB case. ⛔ **CORRECTED 2026-08-30 — that spelling DOES exist, as a REGISTERED BUS ALIAS**: `plugins/curtain-wall/src/handlers/CreateCurtainWall.ts:51` `aliases = ['curtainwall.create']`, canonicalised to `curtain-wall.create` (`packages/command-bus/__tests__/command-aliases.test.ts:92`). It is a real, dispatchable identifier at the WRONG LAYER, not a phantom — and `check-command-naming.ts`'s own remedy text prescribes exactly that alias pattern, so **the alias must NOT be deleted** (a command type is a wire identifier replayed out of `project_command_log`). ⚠ **The gate's actual curtain-wall finding is a THIRD spelling:** `curtainWall.changeLevel` (camelCase, `ChangeCurtainWallLevel.ts:79`), which has no alias at all. Renaming the verbs without simultaneously fixing the CEB case labels, the bridge tags and the docs converts a naming violation into a dead dispatch. **Disposition: REPLACE the prefixes, one family per commit, each commit carrying the CEB case, the bridge and the doc cell together.** ⛔ **Baseline 0 is hard: it may not be raised to 5.** **Acceptance (D3):** the gate prints `0 / 0` **and** a read-back test for each renamed family still prints its element from the authoritative store. | **OPEN** |
| **L-12840** | ⛔ **OPEN (P1) — `commandManager.execute` LITERAL COUNT IS 12 AGAINST A CEILING OF 0. A HARD REGRESSION, AND THERE ARE THREE RIVAL COUNTERS DISAGREEING ABOUT IT.** `npx tsx tools/ga-gate/check-no-commandmanager.ts; echo "RC=$?"` → **RC=3**, printed *"code-only literal=12 window=57 cm.execute=62 (incl. comments 53/102/69); files scanned 1455, floor 700"*, `LITERAL_CEILING 0`, ledgered literal 11. Meanwhile `npx tsx tools/ga-gate/check-commandmanager-any.ts` → **RC=0**, `OK: 25 / 25`, and `npm run check:commandmanager` (which resolves to `scripts/check/ci-check-no-commandmanager.mjs`, **not** a `tools/ga-gate/` script) → **RC=1**, `FAIL -- count 139 exceeds threshold 136`. ⭐ **Three denominators, three verdicts, one subject — name the gate you ran or do not quote a number.** ⚠ **The deeper defect survives all three:** a gate that classifies by NAME can be satisfied by RENAMING. **Disposition: REPLACE the 12 literal sites with `runtime.bus.executeCommand`; do NOT extend the ledger from 11 to 12.** **Acceptance (D3):** `check-no-commandmanager.ts` prints `literal=0`. | **OPEN** |
| **L-12841** | ⛔ **OPEN (P1) — ALL FOUR ARMS OF `check-layer-boundaries` ARE BREACHED SIMULTANEOUSLY.** `npx tsx tools/ga-gate/check-layer-boundaries.ts; echo "RC=$?"` → **RC=3** over **167 workspace packages, 152 classified**: upward imports **105 / 102 (+3)** · SDK-facade bypasses **186 / 182 (+4)** · banned third-party **123 / 113 (+10)** · unclassified packages **15 / 13 (+2)**. Concentration: `L2→L6` **40**, `L2→L4` 14, `L3→L4` 12, `L3→L6` 11, `L1→L2` 11. Top sources: `packages/command-registry` **23**, `packages/core-app-model` 14, `packages/runtime-composer` 12, `packages/spatial-index` 11. The single largest attractor of upward edges is **`@pryzm/plugin-annotations`** — `command-registry`, `core-app-model`, `file-format`, `geometry-roof`, `input-host`, `persistence-client` and `room-topology` all reach up into it. ⚠ **SDK-bypass is tracked SEPARATELY by the gate itself and must stay that way:** `plugin → renderer-three` is a **downward** edge — a facade-encapsulation breach, not a layer violation — and merging the two counts makes both unreadable. **Disposition: WIRE** — the biggest single win is inverting the `plugin-annotations` registration edges. ⛔ **No arm's baseline may be raised.** **Acceptance (D3):** all four printed pairs at or below baseline, e.g. `violations 102/102`. | **OPEN** |
| **L-12842** | ⛔ **OPEN (P1) — `check-batch-creation-coverage` FAILS: `CreateCurtainWallsFromSlabCommand` NO LONGER LOOPS UNBATCHED, AND THE GATE THAT SAYS SO CANNOT PROVE ITS OWN SCOPE.** `npx tsx tools/ga-gate/check-batch-creation-coverage.ts; echo "RC=$?"` → **RC=1**. ⚠ **Read this row together with L-12832:** this gate is one of the seven with no subject floor — it *declares* a floor but has **no reachable `process.exit(2)`**, so it cannot distinguish "0 violations" from "walked nothing". Its RC=1 today is a real finding; its RC=0 tomorrow would not be evidence of anything. **Disposition: WIRE both halves in one commit** — restore the batched path **and** give the gate a reachable exit-2, or the fix is unverifiable by construction. **Acceptance (D3):** RC=0 **and** the gate prints a non-zero scanned-file count. | **OPEN** |
| **L-12843** | ⛔ **OPEN (P1) — TWO OF C01 §4's FIVE "ACHIEVED" CONVERGENCE BOOLEANS ARE SATISFIED BY THE ABSENCE OF A DIRECTORY, NOT BY CLEAN CODE. MEASURED 2/9 STRICT AGAINST A DOCUMENTED 5/9; PHASE F NEEDS ≥ 6/9.** `#1 legacy_src_folders == 1`: `find src -mindepth 1 -maxdepth 1 -type d | wc -l` → **0**, and `ls -d src/ui` → *No such file or directory*. **The predicate names a folder that is gone: it was never true and CANNOT BECOME TRUE AS WORDED.** `#2 window_any_in_src_ui == 0`: the 0 is produced by that same non-existent scope — the honest successor `apps/editor/src/ui` holds **1,120 files and 70 casts**, and the gate enforcing the same principle exits 3 at **101 / 100** repo-wide. ⭐ **Absence of the folder and absence of the defect are THE SAME VALUE — `§CONTEXT-DATA-HONESTY` restated as a convergence metric.** `#6 all_workflows_green` is measurably FALSE (8 workflows, **6 are `workflow_dispatch`-only**; the merge-blocking `ga-gate` job is red at HEAD — see L-12831). `#7`/`#8`/`#9` are independently FALSE: `npm view @pryzm/plugin-sdk version` **and** `npm view @pryzm/sdk version` (the `publishConfig` rename) → **E404** both; `npm view @pryzm/headless version` → **E404** while the package is `private:false`, `access:public`, `1.0.0-rc.2` — **publish-READY and unpublished**; `curl https://marketplace.pryzm.app` → `code=000 "Could not resolve host"` with control `https://pryzm.app` → **302** from the same shell in the same minute. **Disposition: REPLACE #1 and #2 — REWORD the predicates, do not merely re-score them.** A misconfigured predicate scored FALSE is still unsatisfiable. Proposed patch: `p04-C01-section4-booleans.diff`. **Acceptance (D3):** `grep -rn "(window as any)" apps/editor/src/ui | wc -l` → **0** (prints 70 today). | **OPEN** |
| **L-12844** | ⚠ **OPEN (P1) — TEN DOCUMENTED STATEMENTS ABOUT THE ELEMENT PIPELINE WERE FALSIFIED AT `064a838e`, AND C11 §11 CONTRADICTS ITSELF TWENTY LINES APART.** **DC-01** the §11 matrix marks `handrail`/`furniture`/`lighting` *"❌ No bridge"*; `grep -n "bridge registered" apps/editor/src/engine/initTools.ts` shows **all three registered** (`§FT-HANDRAIL`, `§FT-LIGHTING`, `§FT-FURNITURE`) — and **C11 §0.0 already said so on 2026-08-18**. **DC-02** the Furniture matrix row says *"No bridge"* while the §11.1 gap table **20 lines below** says *"✅ DONE"*; both are labelled the normative record. **DC-03** §11.11 audits Plumbing/Grid/Annotation in prose while the matrix has **no row for any of them**. **DC-04 (P0)** the matrix covers **14** families; the 8-source census measures **24** — ten families had no normative compliance record at all. **DC-05** §7.2 claims *"no OTel span, and plugin-sdk exposes no tracer"*; `grep -rIl "withHandlerSpan" … | wc -l` → **293 files** *(⚠ re-measured 2026-08-30; C11 §0.0 itself recorded **253** on 2026-08-18 and is stale — `p02` corrects BOTH sections so one fact has one reading (C84 EI-9). ⚠ this does NOT mean P8 holds — the gate is RC=3)*. **DC-06** every `§11.x` audit is stamped **2026-05-19**. **DC-07** `CLAUDE.md`'s P4 bullet asserts both arms within ceiling; measured RC=3. **DC-08** C01 §3 says 54/12/46; measured **103/13/51**. **DC-09** C01 §4 says 5/9; measured **2/9**. **DC-10** C01 §5 says 31 gates; **75** scripts on disk, **98** registered, **120** across both homes. ⭐ **The matrix is the half an engineer reads to decide what to BUILD — three bridges were ordered built that already shipped, and executing that order produces a RIVAL of a live bridge, strictly worse than the gap it imagined (D2).** **Disposition: REPLACE the canonical text in place** — proposed patches `p01`…`p07` under `audit/element-creation/2026-08-29/proposed-patches/`. ⛔ **No new `*-AUDIT-*.md` derivative** (Discipline Rule 1). **Acceptance (D3) — ⛔ RESTATED 2026-08-30, THE FIRST VERSION WAS SELF-DEFEATING.** It read *"`grep -c 'No bridge' C11` → 0 inside §11; `grep -c '54 packages, 12 apps, 46 plugins' C01` → 0"*. **Measured on the PATCHED copy, both print NON-ZERO and always will** — the corrections QUOTE the retracted text on purpose, so a substring grep cannot tell an assertion from a quotation of a retracted assertion. Anchor on the ASSERTION FORM: `grep -c '| ❌ No bridge |' docs/02-decisions/contracts/C11-ELEMENT-CREATION-PIPELINE.md` → **0** (prints 3 today: C11:1291/:1292/:1293) and `grep -cE '^**54 packages, 12 apps, 46 plugins.**' docs/02-decisions/contracts/C01-ARCHITECTURE-AND-GOVERNANCE.md` → **0** (prints 1 today). ⭐ This row is itself the case for the rule: an acceptance criterion that cannot be satisfied is the same defect as a gate that cannot fail. | **OPEN** |
| **L-12845** | ⚠ **OPEN (P2) — `check-plugin-census-equivalence` EXITS 0 WHILE CERTIFYING THAT 24 OF 51 PLUGIN DIRECTORIES CONTRIBUTE NOTHING AT BOOT. D3 IN ITS PUREST FORM.** `npx tsx tools/ga-gate/check-plugin-census-equivalence.ts; echo "RC=$?"` → **RC=0** with arm A `DISK − REGISTRY` = **24 / baseline 24** (`ai-floorplan ai-generative ai-query ai-rules ai-voice bcf cross dxf export-pdf family-editor geospatial ifc-export ifc-import ifc-inspector levels multiplayer navigate plan-view render rhino-import schedules sheets toy-cube visibility-intent`), arm B `DISK − CATALOG` = **8 / 8**, arm F = **3 / 3**. **47% of the plugin tree is AUTHORED-BUT-UNREACHABLE and the gate is designed to stay green while that is true.** ⚠ **The gate reads only 4 of the 8 census sources** — it never looks at `GEOMETRY_ELEMENT_TYPES`, `CommandEventBridge`, `initTools` or `packages/schemas/src/elements/`, so ten of the audit's nineteen census-drift rows are **structurally invisible to it**. **Disposition: WIRE the four missing sources into the gate** — the arms exist and are correct; the scope is the gap. ⛔ Do **not** lower arm A's baseline to make the number look better, and do not read RC=0 as "the census agrees". **Acceptance (D3):** the gate prints comparisons across **8** sources and arm A's numerator is stated per run, never a tick. | **OPEN** |
| **L-12846** | ⚠ **OPEN (P2) — THREE GATES REPORT `0` OVER SCOPES THAT CANNOT CONTAIN THE DEFECT THEY POLICE.** (a) **P6 does not scan `packages/`.** `npx tsx tools/ga-gate/check-no-direct-store-writes.ts` → **RC=0 at 37 / 37 — zero headroom at a NON-ZERO ceiling** — and a direct store write from a package is **invisible by design**. P6 is the principle undo, CRDT and AI batch-apply all depend on. (b) **P7 is PROXY-ONLY.** `check-visibility-intent-not-ui.ts` → RC=0, arm A hard-0 over 20 files, arm B **40 / 43** over 772 UI files, and the gate **prints its own NOT-CHECKED list on every run**: persistence, per-view scoping, the AI intent path. *"P7 holds"* is not what a green here establishes. (c) **P4's regex is blind to a second spelling.** `gate-debt.json`'s own `$comment` records **409 `window as unknown as` casts across 151 files** the P4 pattern cannot see — carried, **not re-measured** in this audit; the exact next command is `rg -c "window as unknown as" --glob '!node_modules' -g '*.ts'`. ⭐ **A green over a scope that excludes the defect is a false green with a correct exit code.** **Disposition: WIRE — extend each scope; do NOT relax any ceiling.** **Acceptance (D3):** P6 prints a scanned-file count that includes `packages/`; P7 gains at least one behavioural arm in `packages/visibility/__tests__/`; P4 prints a second numerator for the `as unknown as` spelling. | **OPEN** |

---

## §3 area-table ticks that follow from these rows

The file's instruction is *"Append new issues to the bottom of §2 **and tick the area table in §3**."*
These rows touch, at minimum: **element creation pipeline** (L-12835, L-12836, L-12838),
**measurement / CI gates** (L-12830, L-12831, L-12832, L-12833, L-12834, L-12842, L-12845, L-12846),
**architecture conformance** (L-12839, L-12840, L-12841), and
**documentation accuracy** (L-12843, L-12844). ⚠ **The §3 table was NOT re-read in this pass** —
tick it against its own current rows rather than against this list.

## What is NOT proposed here, and why

Per **§RATCHET-EXCEEDED-IS-NEVER-DEBT**, **no row above proposes raising a shrink-only ceiling or
adding a `gate-debt.json` entry.** Seven gates exit 3 at HEAD; every one of them is remediated by
fixing the named subjects. That is the only admissible fix, and `run-all.ts:1162` enforces it by
refusing to absorb code 3 under any ledger.

Per **D2**, every row states **WIRE / REPLACE / REMOVE** and justifies it from the measured state.
Four rows deliberately say *"do NOT wire"* (L-12830, L-12835, L-12836(a), L-12838-water) because the
thing exists and a second implementation would be worse than the gap.
