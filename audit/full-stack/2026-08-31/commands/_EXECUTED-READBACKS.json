{
  "phase": "P3 AXIS C",
  "date": "2026-08-31",
  "head_at_start": "d91d30d4af51bc67317ef8afc0b46b1dbef94038",
  "what_this_is": "EVIDENCE CLASS (c) ONLY. Every row below is a vitest run I EXECUTED in this session, in the foreground, with its RC captured. Nothing here is inherited from a gate's word. Commands are recorded verbatim so they can be re-run.",
  "runs": [
    { "cmd": "cd apps/editor && npx vitest run __tests__/composedBusElementReadback.test.ts", "RC": 0, "result": "Test Files 1 passed (1) - Tests 4 passed (4)" },
    { "cmd": "cd apps/editor && npx vitest run __tests__/CommittedPatchReachesTheStore.test.ts __tests__/RoofUpdateReachesGeometryStore.test.ts __tests__/CeilingUpdateReachesGeometryStore.test.ts __tests__/CurtainWallUpdateReachesGeometryStore.test.ts", "RC": 0, "result": "Test Files 4 passed (4) - Tests 29 passed (29)" },
    { "cmd": "cd apps/editor && npx vitest run __tests__/StairCreateReachesGeometryStore.test.ts __tests__/StairMoveReachesGeometryStore.test.ts __tests__/PoolAndSlabUpdateReachTheRenderStore.test.ts __tests__/SheetAddViewportReachesSheetStore.test.ts __tests__/selectionVerbsReachTheComposedSelectionStore.test.ts", "RC": 0, "result": "Test Files 5 passed (5) - Tests 23 passed (23)" },
    { "cmd": "cd apps/editor && npx vitest run __tests__/boundaryLineFootprintThroughComposedRuntime.test.ts __tests__/bathroomPodReachableThroughComposedRuntime.test.ts __tests__/liftUndoRoundTrip.test.ts __tests__/CurtainReplacePanelIsDead.test.ts __tests__/DoorBatchCreateLiveness.probe.test.ts __tests__/WindowBatchCreateLiveness.probe.test.ts", "RC": 0, "result": "Test Files 6 passed (6) - Tests 45 passed (45)" }
  ],
  "total": "16 test files, 101 tests, ALL PASSING, executed by this lane on 2026-08-31 in the foreground.",
  "POSITIVE_store_readback": [
    { "verb": "wall.create", "proof": "apps/editor/__tests__/composedBusElementReadback.test.ts:131 A-3 - dispatch on the REAL composeRuntime() bus, then authoritative('wall').getById(wallId) returns the record with id/levelId/thickness/height/baseLine[1].x all asserted.", "against": "the module-singleton WallStore ProjectSerializer reads (identity asserted at :100)" },
    { "verb": "roof.create", "proof": "CommittedPatchReachesTheStore.test.ts:154 ARM 1 READ-BACK - materialId + materialColor reach the LEGACY RoofStore", "against": "legacy RoofStore (fragment builder + plan projector + IFC exporter + ProjectSerializer all read it)" },
    { "verb": "roof.setPitch", "proof": "CommittedPatchReachesTheStore.test.ts:249 - writes legacy slope = tan(pitch)", "against": "legacy roof record" },
    { "verb": "roof.setShape", "proof": "CommittedPatchReachesTheStore.test.ts:260 - writes legacy roofType; mono becomes shed", "against": "legacy roof record" },
    { "verb": "roof.setOverhang", "proof": "RoofUpdateReachesGeometryStore.test.ts:134 - changing the overhang mutates the GEOMETRY record the renderer and serializer read", "against": "geometry roof record" },
    { "verb": "ceiling.update", "proof": "CeilingUpdateReachesGeometryStore.test.ts:148 property edit + :162 boundary MOVE", "against": "geometry ceiling record" },
    { "verb": "wall.updateCurtainWall", "proof": "CurtainWallUpdateReachesGeometryStore.test.ts:148 baseLine MOVE + :170 property/material", "against": "geometry curtain-wall record" },
    { "verb": "stair.create", "proof": "StairCreateReachesGeometryStore.test.ts - the live StairPlanToolHandler payload mints the stair in the GEOMETRY store", "against": "geometry stair store" },
    { "verb": "stair.move", "proof": "StairMoveReachesGeometryStore.test.ts - the live gizmo payload { stairId, delta } moves the GEOMETRY record, all three anchors", "against": "geometry stair record" },
    { "verb": "slab.addHole", "proof": "PoolAndSlabUpdateReachTheRenderStore.test.ts ARM 2 - puts the hole in the LEGACY record, in PLAN coordinates; ARM 1 also proves the element.updated emit names the committed field", "against": "legacy slab record" },
    { "verb": "slab.setThickness", "proof": "PoolAndSlabUpdateReachTheRenderStore.test.ts ARM 3 - moves the legacy thickness and does NOT annihilate the rest of the record", "against": "legacy slab record" },
    { "verb": "pool.create", "proof": "PoolAndSlabUpdateReachTheRenderStore.test.ts ARM 5/6/7 - one member event PER MEMBER, the void punched into the LEGACY host slab, water on its OWN channel", "against": "legacy host slab record + per-member channels" },
    { "verb": "sheet.addViewport", "proof": "SheetAddViewportReachesSheetStore.test.ts - the ViewsRailPanel payload lands the viewport in the AUTHORITATIVE sheetStore (executed read-back)", "against": "authoritative sheetStore" },
    { "verb": "selection.select", "proof": "selectionVerbsReachTheComposedSelectionStore.test.ts ARM C - the write LANDED in the store the provider fronts (read back through copy-selection)", "against": "composed selection store" },
    { "verb": "selection.deselect", "proof": "same file, ARM D - removes exactly one", "against": "composed selection store" },
    { "verb": "selection.clear", "proof": "same file, ARM E - the DEFAULT POINTER TOOL activator verb empties the store", "against": "composed selection store" },
    { "verb": "bathroomPod.create", "proof": "bathroomPodReachableThroughComposedRuntime.test.ts B-2 - the pod READS BACK from the store with every member (C67 rule 12)", "against": "runtime.stores.bathroomPod, asserted SAME INSTANCE the bus writes through (A-2)" },
    { "verb": "bathroomPod.delete", "proof": "same file, B-3 - dispatches and the pod is gone", "against": "runtime.stores.bathroomPod" },
    { "verb": "boundaryLine.create", "proof": "boundaryLineFootprintThroughComposedRuntime.test.ts J-1/J-2/J-3 - composed runtime exposes stores.boundaryLine, SAME INSTANCE, and the seam resolves the drawn line into the generator footprint", "against": "runtime.stores.boundaryLine + the generator footprint seam" },
    { "verb": "lift.create", "proof": "liftUndoRoundTrip.test.ts R-3..R-7 - Ctrl+Z removes the compound, its cabin parts AND heals every slab void; redo restores all three voids; undo-redo-undo stable", "against": "lift + liftPart stores and the slab voids; this is the strongest UNDO proof in the repo" },
    { "verb": "door.batch.create", "proof": "DoorBatchCreateLiveness.probe.test.ts - one dispatch stores every door in the batch, read back from the STORE", "against": "ctx.stores.door - the PLUGIN DTO store. The SAME file proves the batch lands in the plugin DTO store and NOT in the geometry-door singleton." },
    { "verb": "window.batch.create", "proof": "WindowBatchCreateLiveness.probe.test.ts - one dispatch stores every window in the batch, read back from the STORE", "against": "ctx.stores.window (plugin DTO store; the geometry-window singleton is minted fresh and can never BE it)" }
  ],
  "NEGATIVE_measured_by_the_same_executed_runs": [
    { "verb": "slab.create", "finding": "RETURNS OK AND WRITES NOTHING TO THE AUTHORITATIVE STORE.", "proof": "composedBusElementReadback.test.ts:205-206 - expect(slabOutcome).toBe('OK'); expect(authoritative('slab').getById(slabId)).toBeUndefined(). Executed by this lane, RC=0. The committed plugin record carries boundary; the authoritative store demands polygon/position. A SHAPE defect, not a wiring one.", "class": "c", "rollup": "5 - VERBS WITH NO CONSEQUENCE" },
    { "verb": "room.create", "finding": "REFUSES BY NAME in a composed process - legacy command manager is not available.", "proof": "composedBusElementReadback.test.ts:234-236. CreateRoomHandler declares affectedStores: [] and delegates to window.commandManager, so there is no patch pair to mirror.", "class": "c" },
    { "verb": "curtain-wall.replacePanel", "finding": "DEAD. No plugin descriptor contributes a curtainPanelStore key; the REAL handler refuses on a production-shaped context; a VALID payload still fails, so it is not payload validation refusing.", "proof": "CurtainReplacePanelIsDead.test.ts ARM 1/2/3, executed RC=0.", "class": "c", "rollup": "5 - VERBS WITH NO CONSEQUENCE" },
    { "verb": "door.batch.create", "finding": "READBACK-POSITIVE BUT UNREACHABLE - doorBatch is BUILT and FILTERED but dispatched NOWHERE. The same scanner finds wallBatch dispatched (POSITIVE CONTROL), so the scan is not blind.", "proof": "DoorBatchCreateLiveness.probe.test.ts, executed RC=0.", "class": "c", "ladder": "AUTHORED + COMPOSABLE, NOT REACHABLE" },
    { "verb": "window.batch.create", "finding": "ZERO production dispatch sites - and the payload IS constructed, so the gap is dispatch, not emission. POSITIVE CONTROL: the same scanner DOES find wall.batch.create dispatched.", "proof": "WindowBatchCreateLiveness.probe.test.ts, executed RC=0.", "class": "c", "ladder": "AUTHORED + COMPOSABLE, NOT REACHABLE" },
    { "verb": "paste-clipboard", "finding": "still has no port at the composition root.", "proof": "selectionVerbsReachTheComposedSelectionStore.test.ts ARM G (MEASUREMENT, NOT A PASS), executed RC=0.", "class": "c" },
    { "verb": "lift.create (undo)", "finding": "THE UNDO ENTRY IS STILL ONE KEY SHORT - door.", "proof": "liftUndoRoundTrip.test.ts R-2, executed RC=0.", "class": "c" }
  ],
  "what_this_does_NOT_establish": "That any of these reaches a PIXEL. Every proof above stops at a store record or an emitted event. None of the 16 files renders. Two of them (StairMove, CurtainWallUpdate) assert against a 'GEOMETRY record the renderer reads', which is a claim about the renderer made by the test's own comment, not a measurement of the renderer."
}
