LANE HR — BARREL ADDITIONS (for the orchestrator to apply / re-apply at commit time).
The adapter files under packages/site-parcel-data/src/countryAdapters/hr/ + the test +
the fixture are self-contained and touch NO shared file. The items below WERE applied to the
two shared files this session AND are queued here as the re-apply source, because both shared
files were under concurrent edit by other adapters-2 lanes this session (registry.ts +319 lines
vs HEAD; the Gulf lane added ARE/KWT/BHR/OMN to the boundary set). If my edits were clobbered,
re-apply exactly the following.

=====================================================================
1. packages/site-parcel-data/src/index.ts  — one line in the countryAdapters block
=====================================================================
After `export * from './countryAdapters/ee/index.js';` add:

export * from './countryAdapters/hr/index.js';

(Every HR export is Hr/HR_/CROATIA_-prefixed — the star export cannot collide.)

=====================================================================
2. packages/site-parcel-data/src/parcelProviders/registry.ts  — 3 additions
=====================================================================
2a. IMPORT (beside the other adapter-bbox imports, after the ESTONIA_BBOX line):

import { CROATIA_BBOX } from '../countryAdapters/hr/hrJurisdiction.js';

2b. ROW (in the L-12871 batch block, immediately AFTER the EE row, before the LT row).
    kind = footprint-fallback + proxyPath = null (NOT `cadastral`): the cadastre IS live and
    keyless, but BOTH downstream wirings are pending (server proxy + resolver HRV claim), and a
    `cadastral` row with no reachable proxy would resolve null on every click — the exact
    failure≠empty collapse (§CONTEXT-DATA-HONESTY / parcelRegistryWiring.test.ts §4). This is
    the LU/SE registration shape. `contains: claimsNation('HR')` NEVER a rectangle (L-12871).

    {
        // LANE HR (2026-09-03). The Croatian cadastre IS live + keyless — countryAdapters/hr/
        // resolveHrParcelAtWgs84Point PROVED a real Zagreb parcel (k.č. 2379, k.o. CENTAR 335240)
        // through DGU/Uređena zemlja cp_wms:CP.CadastralParcel this session. footprint-fallback
        // until (a) server proxy /api/parcel/hr is wired and (b) the resolver can claim HR (§3).
        regionCode: 'HR',
        countryName: 'Croatia',
        providerId: 'hr-dgu-dkp-cp',
        label: 'Katastarska čestica (Croatia · DGU / Uređena zemlja DKP)',
        proxyPath: null,
        kind: 'footprint-fallback',
        contains: claimsNation('HR'),
        note: '<the full note as written in registry.ts this session — the live cp_wms probe + the two declared-deferred wirings, gate named, reviewBy 2026-10-03>',
    },

2c. REGION_BBOX entry (specificity metric — REQUIRED, or parcelRegistryWiring.test.ts §1 fails:
    every row must have a finite specificity). After `EE: ESTONIA_BBOX,`:

    HR: CROATIA_BBOX, // lane HR — specificity metric only; the row's `contains` is claimsNation('HR'), never this box

=====================================================================
3. DECLARED FOLLOW-UP — the HR NATIONAL-CLAIM lane (shared decider; NOT done here)
   Gate: `claimsNation('HR')` is FALSE everywhere until HRV is in the national resolver.
   reviewBy: 2026-10-03.
=====================================================================
Until this lands, the HR registry row (2b) is correct-by-construction but INERT — it can never
misroute (the resolver can never emit regionCode 'HR' without HRV geometry), and a Zagreb click
falls to the OSM footprint. This is deliberately NOT done in the parcel lane: it edits the shared
national decider (jurisdiction/nationalJurisdictionResolver.ts + data/nationalBoundaries.json) and
carries its own red-pin border-test obligation — a lane-sized change, exactly like the concurrent
Gulf lane that added ARE/KWT/BHR/OMN this session.

The turnkey steps for that lane:
  (i)   Extract HRV rings from the SAME public-domain source the set already uses
        (natural-earth-vector / ne_10m_admin_0_countries), simplified to the set's
        simplifiedToleranceM, and add `"HRV": { "regionCode": "HR", "rings": [...] }` to
        `countries` in data/nationalBoundaries.json.
  (ii)  Add HR's LAND neighbours as refusal-only `neighbours` so border points refuse rather
        than annex (L-12887): HUN, SRB, BIH, MNE. (SVN is ALREADY a neighbour.) Without these,
        a Hungarian/Bosnian point within coastalToleranceM of the HR border could be
        nearest-polygon-rescued to HR.
  (iii) Add to CANDIDATE_PREFILTERS in nationalJurisdictionResolver.ts:
            import { isInCroatia } from '../countryAdapters/hr/hrJurisdiction.js';
            ['HRV', isInCroatia],
        (Safe to stage BEFORE (i): the loop guards `&& set.countries[iso3]`, so HRV is a no-op
        candidate until its rings exist.)
  (iv)  Red-pin border tests (the L-12871/12887 discipline) — witness towns each side:
            HR↔SI  Zagreb 45.8132,15.9771 (HR)      ↔ Ljubljana 46.0569,14.5058 (SI, un-modelled→refuse)
            HR↔HU  Osijek 45.5550,18.6955 (HR)       ↔ Mohács    45.9935,18.6845 (HU, →refuse)
            HR↔RS  Vukovar 45.3550,18.9983 (HR)      ↔ the Danube RS bank            (→refuse)
            HR↔BA  Dubrovnik 42.6507,18.0944 (HR)    ↔ Trebinje  42.7113,18.3436 (BA, →refuse)
            HR↔ME  Cavtat 42.5850,18.2199 (HR)       ↔ Herceg Novi 42.4531,18.5375 (ME, →refuse)
  (v)   Update __tests__/nationalJurisdictionResolver.test.ts CONTROLS to include
        ['Zagreb', 45.8132, 15.9771, 'HRV'] so "covers every country in the shipped boundary set"
        stays green. (⚠ NOTE: that test is ALREADY RED on the current tree because the concurrent
        Gulf lane added ARE/KWT/BHR/OMN to the boundary set WITHOUT updating CONTROLS — 16 controls
        vs 20 countries. That is the Gulf lane's fix, not HR's; recorded here so it is not
        attributed to HR.)
  (vi)  Then FLIP the registry row (2b) to kind:'cadastral' + proxyPath:'/api/parcel/hr' once the
        server proxy is wired (euCadastreProxy.js `hr` row, cp_wms:CP.CadastralParcel, WGS84 bbox
        urn:4326 lat,lon, no srsName → native 3765 ring).
