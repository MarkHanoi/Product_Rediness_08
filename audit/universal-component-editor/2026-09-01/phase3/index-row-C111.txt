LANE C111 — the C00 index row for docs/02-decisions/contracts/README.md
=======================================================================

WHERE IT GOES
  In the "## Contract suite" table (header `| # | Contract | Governs | Key principle |`),
  in id order: AFTER the **C110** row (lane C110's own row) and BEFORE the **C112** row
  (lane C112's own row). At HEAD the last row in that table is **C109**.

WHY THE ORCHESTRATOR APPLIES IT, NOT THIS LANE
  README.md is shared by three minting lanes this phase (C110, C111, C112). Per the lane
  brief, each lane writes its row here and the orchestrator applies all three in ONE edit,
  so the count and the range move together — the shape that has failed six times.

GATE STATE THIS ROW CLOSES (measured, foreground, $? read from a redirect)
  BEFORE any Phase-3 file:  RC=0 · 108 files · 91 rows · max C109 · arm A 18 = baseline · B/C/D clean
  AFTER the three files:    RC=3 · 111 files · 91 rows · arm A **21** vs baseline 18 · B/C/D still clean
                            ("ARM A RATCHET EXCEEDED — 21 files with no index row, declared level 18.
                              Add the row in the SAME commit that adds the contract.")
  Applying all three rows returns arm A to 18 and the gate to RC=0.
  ⛔ Do NOT raise BASELINE_UNRESOLVED / the arm-A baseline. The rows are the fix.

ALSO UPDATE, IN THE SAME EDIT (the range moves with the row)
  README.md's item 4 in the conflict-resolution list currently reads "C01–C108 + C24.1" and
  says "C61 is the ONLY RESERVED, unminted slot" in one clause and "TWO reserved-unminted
  slots now, not one: C61 AND C103" in a later correction. With C110/C111/C112 minted the
  range becomes **C01–C112 + C24.1**, reserved **C61 and C103**. ⛔ Re-measure in the same
  commit rather than transcribing this line:
      ls docs/02-decisions/contracts/ | grep -c '^C[0-9]'
      npx tsx tools/ga-gate/check-contract-index-equivalence.ts

--- THE ROW (one line; copy verbatim, including the leading and trailing pipes) ---

| **C111** | [Component Definition & the `.pryzm-family` Model](./C111-COMPONENT-DEFINITION-AND-FAMILY-FORMAT.md) | The **ComponentDefinition model and the `.pryzm-family` v1 envelope** — and it exists because **[C05 §4](./C05-PERSISTENCE-AND-FILE-FORMAT.md) described a format that does not exist** for four months (`grep -rn "family-descriptor"` → **rc=1, no output**; there is no `metadata.json.type='family'` either), while the real format's only cited authority is a phase plan that **41 files cite and this repository does not contain**. **§0.2 states the [ADR-0376](../adrs/ADR-0376-universal-component-editor-founding-rulings.md) D5 equivalence ONCE and forbids restating it**: `FamilyDefinition ≡ ComponentDefinition`, **`Component` is canonical**, `family-*` / `.pryzm-family` / the seven id prefixes are **FROZEN LEGACY WIRE NAMES** under [C69 §1.1](./C69-API-VERB-REGISTER.md), and **no NEW symbol may use `Family`** — the `curtainwall.create` treatment. **§0.4 discloses that the word is already partly spent** (`genericComponent` in two AI type unions · `GenericComponent` routed to `furniture`/`A-FURN` in two graphics tables · ⛔ **`window.componentInstanceStore`, a store with exactly ONE reference in the tree — its own reader** — so `getAllGenericComponents()` returns `[]` forever and the AI cannot tell *none* from *unavailable*). **§1 corrects the audit's own phrasing**: there are **eight identity spaces and SEVEN prefixes**; the eighth is a bare `ULID` **shared by three object kinds**, so `entityIds` cannot say what it points at and a reference MUST fail closed. **§1.4 is the headline gap, measured on four axes with each `rc` read from a redirect: NO BUS VERB PLACES A COMPONENT INTO A PROJECT** — zero `component.*` verbs, zero register rows, and `apps/editor` imports **none** of the three family packages — and the section **reserves the `component.*` namespace and the C69/C16-CA-21 obligations while ⛔ REFUSING to design the placement verb**, which is D9's ADR. **§3 refuses the easy summary "authored but unreachable" and splits it**: **TRANSPORT IS LIVE** (`server.js` mounts `/api/v1/families`; `POST` Ed25519-verifies through `unpackFamily`), **AUTHORING IS DARK**, **PLACEMENT DOES NOT EXIST**, and ⭐ **there is NO `.pryzm-family` corpus anywhere in the tree** — the one fact that makes every repair cheap and that expires the day the first component is saved. **§3.2 finds the audit's CI hole one level deeper: the ENTIRE component stack is silently skipped by `pnpm run test:ci`** — `file-format`, `family-runtime`, `family-loader`, `family-instance`, `component-editor` and both integration workspaces are all on the `--if-present` skip roll, so the round-trip, signature and migration suites, the only assertions the format has, never run. ⛔ **§2.4 `§C111-FAKE-TYPE-CHECKSUM`, with an EXECUTED proof**: `FamilyType.checksum` is typed `sha256:` + 64 hex and is computed by an **FNV-1a/32 hash padded to 8 chars and repeated eight times** — it passes the Zod regex, its 64 chars are eight identical blocks, and **it collided on the 200,000th trivial input**; its own comment claims `packFamily` recomputes the real digest and **`packFamily` never mentions `checksum`**, so the fake survives into the signed bytes and nothing anywhere verifies it. ⛔ **§8 `§C111-MIGRATION-EXIT-UNSATISFIABLE` is the deeper half of D12**: `formatVersion` is a Zod **literal**, so `'2.0'` is not *a later version* but *not a manifest* — which makes `unpackFamily`'s `unsupported-future-version` branch **unreachable for every input** (a user with a v2 file is told it is malformed) — **and every legal migrator fails its own exit gate**, proven by an executed probe with a passing control and a falsification control (`identityMigrator('1.0','1.1')` → *"exit schema invalid: expected \"1.0\""*; the same op with `validateExit:false` → **ok**), because the registry refuses `from === to` and the exit schema refuses everything else. ⭐ **The suite's one green end-to-end migration test passes by writing the OLD version number back into the migrated document** — the workaround, not the proof. **§9.3 CORRECTS the audit's constraint arithmetic**: not *"12 persisted, 5 executable, seven unimplemented"* — **8 persisted kinds have no solver counterpart at all**, the solver's `fixed` has no persisted home, the two vocabularies disagree on the spelling of `coincident`, and ⛔ **`ProfileConstraintSchema` has ZERO writers and ZERO readers**, so the array is a shape with no producer and no consumer and **all twelve** kinds are unevaluable the moment one is written — hence the [C74 §4.1/§4.2](./C74-CONSTRAINT-HONESTY.md) refusal rule: **author nothing you cannot evaluate, name both sides, and answer (a)/(b) first because `horizontal`/`vertical`/`equalLength` are closed-form assignments that belong in the expression engine, not a solver.** **§5 maps every field carried / inert / DROPPED and finds a SECOND DEFAULT CHANNEL** — `document.defaults`, maintained by three migration ops and read by no resolver, because `resolveParameter` never sees the document — **declared DEAD, with adding a reader named as the forbidden fix.** **§7 forbids this format ever acquiring an undo mechanism** and forbids reading `event-log.ndjson` as one ([C03 §4.6 U-12](./C03-SCHEMAS-COMMANDS-AND-STATE.md)). **§10 declares what the format CANNOT express**: no boolean feature (so a window cannot state "frame minus glazing void" although `produceBoolean` works), no feature graph (a flat array in document order — **and document order is not an identity**), an LOD triple **no producer honours**, no `Representation[]`, and **no nesting at all** — with D6's three rival answers named so no lane picks one without an ADR. **§12 carries ten REFUSALS**, including ⛔ no second `ComponentDefinitionSchema`, ⛔ no rename of the frozen spellings, ⛔ no `validateExit:false` to make migration "pass", and ⛔ no unit conversion at the profile seam before ADR-0376 D3's owed metres migration decides the sketch unit. **Status: CANONICAL — deliberately NOT ACTIVE**; §12.1 states the exit condition and §12.2 registers four NOT-MEASURED axes. | P5, P6, P8 |
