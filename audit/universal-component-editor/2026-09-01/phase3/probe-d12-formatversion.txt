npm warn Unknown project config "package-manager-strict". This will stop working in the next major version of npm. See `npm help npmrc` for supported config options.
CONTROL — formatVersion "1.0" parses            : true
PROBE   — formatVersion "2.0" parses            : false
PROBE   — formatVersion "1.1" parses            : false
=> unpackFamily reaches its FamilyManifestSchema.safeParse FIRST and
   returns reason "manifest-invalid". The later branch
   `if (manifest.formatVersion !== "1.0")` -> "unsupported-future-version"
   is UNREACHABLE for every input.
RC=0
