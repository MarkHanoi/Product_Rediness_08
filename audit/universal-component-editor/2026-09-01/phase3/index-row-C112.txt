ORCHESTRATOR: apply this row VERBATIM to docs/02-decisions/contracts/README.md,
in the "Contract suite" table (columns: | # | Contract | Governs | Key principle |),
immediately AFTER the C110 row and BEFORE any C113 row.

⛔ ALSO UPDATE THE RANGE in README.md item 4 ("This contract suite — C01–C109 …") in the SAME
EDIT. The row and the range move together — that rule has already failed six times in this file
(C67 → C81 → C84/C85–C99 → C100 → C76 → C104–C107). After C110/C111/C112 land, the range is
C01–C112 and the RESERVED-unminted slots are still C61 and C103 (C103 remains UNMINTED-AND-CITED,
L-7060, OPEN — C112 adds no citation to it).

⛔ DO NOT STAMP THE GATE READING UNTIL ALL THREE FILES **AND** ALL THREE ROWS ARE PRESENT.
Measured 2026-09-01 with C110 + C112 on disk and C111 absent:
  npx tsx tools/ga-gate/check-contract-index-equivalence.ts -> RC=3
    ARM A 20 files with no index row, declared level 18   (C110 and C112, both awaiting their rows)
    ARM D 1 finding: "C111 — in range, no file, no row, not declared RESERVED"
  Both are the expected transient state of a three-lane parallel mint and both clear only when
  C111's file plus all three rows land. Re-measure after applying; do not transcribe this reading.

--------------------------------- ROW BEGINS (one line) ---------------------------------
| **C112** | [Connectors](./C112-CONNECTORS.md) | The **declared joining site** on a component — spec §27's *"connectors are first-class … identity, position, orientation, type, allowed connections, dimensions, direction, compatibility"* — and the spec §35 query it exists to serve: *"components connected to this MEP connector."* ⭐ **This is the only genuinely GREENFIELD subsystem in the Universal Component Editor programme, and §0.2 proves it rather than assuming it**: `connector` returns **141 hits across `packages`/`plugins`/`apps` and ZERO are a building object** (an NL-grammar connector in `FloorFinishIntent`/`ZeroTokenResolver`, a corridor connector spine in `platePartition.ts`, a radiator part in `ToiletRadiatorBuilder`, a SIEM log connector in `entitlements`); `attachmentPoint|insertionPoint|anchorPoint` returns **8, all unrelated**; `MEP` returns **115 and not one is an element** — it is a discipline LABEL in `packages/building-graph/src/discipline.ts`, a furnish-scoring axis name and floor-plate zone text. **Four-axis reachability 0/0/0/0.** In a repository where C107 §0.1 records fifteen built-but-unreachable surfaces found in one session, proving a subsystem honestly **unbuilt** rather than merely **unwired** is itself the finding. ⛔ **§0.3 IS THE CLAUSE THAT MATTERS AND IT IS A RESTRAINT RULING: a contract for a subsystem nobody has built must be SMALLER than one for a subsystem in production, not larger** — one that specifies more than its first writer needs cannot be checked against anything, so it cannot be wrong, so it can never be corrected, which is the `C103` failure mode this suite has already paid for twice (C104 §0.2, C109 §0.2). It therefore makes **five explicit refusals**: it mints **no schema**, adds **no `RelationshipType` member**, does **not unpark `servesZone`** (C71 §2.5 forbids writer-first, and no ADR names a consumer), specifies **no new gate**, and names **no symbol using `Family`** (ADR-0376 D5). ⭐⭐ **§2.3 is the ruling that makes it small, and it is §76 gate B rather than a convenience: THE POSE IS NOT STORED.** C15 §2.1 already rules that *"a hosted element's frame IS its host's frame … including the RAKE"*, so a host-resolved connector stores **neither position nor orientation** — position is the wall `Opening`'s existing `offset` + `sillHeight`, orientation is the host's frame — and **a connector record carrying its own world position or normal is REFUSED**, because two authorities over one pose is C84 EI-9 and the stored copy goes stale the moment the host rakes or curves. This is **C106 §3.2's proven device — THE ANCHOR IS PARAMETRIC — applied one subsystem over**, making a host move a re-evaluation rather than a synchronisation someone must remember to run. Measured in support and **not** as a dependency: `ReferencePlane` in `packages/file-format/src/family-schema.ts` already carries `id`/`origin`/`normal`/`isHost` — **the first four of spec §27's eight axes, already on disk** — but its `isHost` is written by **two test files and read by nothing**, `bakeFamilyInstance` never consults `referencePlanes`, and nothing outside `apps/component-editor` imports it, so it is an **ADR-0376 D1 harvest candidate, not a shipping mechanism**. ⭐ **§3.2 is the one normative claim EXECUTED rather than argued, with a scramble control, because a probe that cannot fail proves nothing**: six arms against `SemanticGraphManager` at HEAD show that two connectors joining the same two components **collapse to ONE edge when unkeyed** (both writes return the same id), stay **TWO distinct edges when `authoredBy`-keyed**, keep **per-connector idempotency** (a re-write returns the same id rather than inserting), and that **`authoredBy` SURVIVES a real serialize → JSON → deserialize round-trip** (`loaded: 2, dropped: 0`) and still matches after reload — while arm 6, stripping the key at the wire, **reproduces the collapse**, proving the probe can fail. ⭐ Arm 6 also sharpens the damage: stripping the key does **not** merge the persisted rows, it makes them indistinguishable to every later write and to delete — **the failure is a STRANDED SURVIVOR, not a lost row**. **§4 states the four C71 §2.6 obligations for `connectsVia` and deliberately does NOT discharge them** (a contract is not a writer, a reader, a rebuild disposition or a delete behaviour), following C106 §3.2-b rather than re-arguing it, and rules the rebuild disposition **persist-only on the persist-or-lose ledger** because **proximity is not a rebuild rule** — two pipe ends that touch were not necessarily connected, and inferring the edge fabricates provenance. **§5 reports a MEASURED gap the audit did not have**: `SemanticGraphManager` keeps exactly two indices, `_bySource` and `_byTarget`, and **none on `authoredBy`**, so **deleting the connector does not remove its edge** — while the naive repair (purging by endpoint) would tear down every *other* connector's edge between the same pair, the over-purge C71 §5.6 warns against; the rule therefore reuses the shipped **`§FIX-STAIR-DELETE-LEAVES-GRAPH-EDGES`** edge-wise purge, which is why the connector id is required in **both** `authoredBy` (write-time identity) and `metadata` (delete-time matching), each closing a different half. ⚠ It also flags, without fixing (out of lane), that `DeleteStairCommand`'s `§UPSTREAM-LIMITATION` docblock is **stale in the pessimistic direction** — it still calls the collapse live and cites a test file that records it **CLOSED**, so a future connector author reading it would conclude the keying mechanism does not work and would build a rival. **§2.2 opens the `kind` vocabulary only to `insertion` and `opening` (what the Window slice needs) and PARKS `mep`/`structural`/`facade`/`attachment` on the C71 §2.2 precedent** — parked is not a gap (§2.3), parked may not be deleted (§2.4) — and rules that an **empty** `allowedHostClasses` ("joins nothing") and a **missing** one ("not yet declared") are **different values no reader may conflate** (C70 L-INV-1, applied at the moment the field is minted rather than after it has been wrong). **§2.4 defers free connectors, flow `direction` and `compatibility` with a CONCRETE trigger rather than a vague one**: `packages/schemas/src/elements/Plumbing.ts` already carries `diameter` and `systemTag`, which are exactly what a compatibility rule would match on, so the axis becomes REQUIRED when a writer must refuse a `waste` end joined to a `cold-water` end — and the contract states plainly that `plumbing` has **no routing today**, so the trigger is genuinely ahead of us. **§6.2 keeps "unconnected" and "I could not tell" as different values** (C71 §4.4 / C78 §1.4) and **§6.3 requires the §35 query be read back at the authoritative store**, never a DTO store the handler wrote and never a pure function's return. **§8 specifies NO new gate**, routing to `tools/ga-gate/check-graph-write-coverage.ts` (**executed 2026-09-01: RC=0, hard-0, 0 findings against a NAMED ledger of 0**) — and then quotes that gate's own printed blind spots (*a writer never reached counts as PRESENT*; *a writer emitting the WRONG edge passes every arm*) to forbid reading its green as evidence of reachability. **§9 declares seven absences so no blank reads as a clearance**, and **§2.5 explicitly asserts NOTHING about slab or roof host surfaces**, since C15 §0.1.1's rows there are under correction by Phase 3C. **Status: CANONICAL — deliberately NOT ACTIVE**, with the exit condition written: the Phase-6C PR lands `connectsVia` with all four §2.6 elements, the gate stays RC=0 with the new member counted, and the §35 query-7 read-back passes at the authoritative store. | P6, P8 |
---------------------------------- ROW ENDS ----------------------------------
