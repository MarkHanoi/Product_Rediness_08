LANE U — OWED EDIT, for the ORCHESTRATOR to apply. NOT applied by this lane.

WHY THIS LANE DID NOT APPLY IT
The lane brief says, verbatim: "⛔ Do NOT 'fix' it by editing the CLAUDE.md line that quotes it —
the false green is in the place CI reads." That prohibition is honoured literally here. The gate
itself needed no repair (see below), so the ONLY thing left carrying the false green is this
documentary line — but it is not this lane's to touch under the brief, and CLAUDE.md is a
high-traffic shared file in a multi-lane phase. Applying it is a one-line orchestrator edit.

THE MEASUREMENT
  npx tsx tools/ga-gate/check-single-compose.ts   →  RC=0
  terminal line, verbatim:
    [single-compose] ✓ one composition root (packages/runtime-composer/src/composeRuntime.ts),
    1/1 rival runtime factory (createFamilyEditorRuntime — declared debt, ADR-0316),
    2/2 production caller(s), controls: arms proven to fire [C1, D1, R1].

  The gate prints a MEASURED rival count of 1. CLAUDE.md still says "0 rivals". The gate is right;
  the line is stale. It is also wrong in a second, independent way: the R1 arm is a shrink-only
  RATCHET at MAX_RIVALS=1 (ADR-0316), not a "hard-fail at the invariant" — only the D1 arm is hard.

FILE:  CLAUDE.md
LINE:  141  (the P1 bullet under "The 8 principles")

REPLACE THIS EXACT LINE:
   → **hard-fail at the invariant** (`tools/ga-gate/check-single-compose.ts`, 1 definition / 0 rivals).

WITH THESE LINES:
   → **D1 arm hard-fails at the invariant; R1/C1 are shrink-only ratchets**
   (`tools/ga-gate/check-single-compose.ts`). Measured 2026-09-01: **1 definition · 1/1 rival
   (`createFamilyEditorRuntime`, blessed by ADR-0316) · 2/2 production callers**, RC=0.
   > ⚠ **This line read `1 definition / 0 rivals` until 2026-09-01, and BOTH halves of that were
   > wrong.** The rival count was transcribed from a terminal line that hard-coded the string
   > `"0 rivals"` while the gate's own body named one (**L-12830**); that literal was removed from
   > the gate on **2026-08-30** (`4a4b35c0`), and this doc line was the last uncorrected copy.
   > And "hard-fail at the invariant" was never true of R1: a rival is BASELINED, not allowlisted,
   > deliberately — so it stays counted and printed on every run. **Read the gate, never this line.**

DO NOT
  ⛔ Do not raise MAX_RIVALS. 1 → 2 is a new ADR or a bug (the gate's own §MAX_RIVALS says so, and
     raising it disarms the gate's planted controls, which then exit 2 rather than green).
  ⛔ Do not add apps/component-editor to FACTORY_ALLOWLIST to make a zero true. ADR-0316 blessed the
     rival CONDITIONALLY on it remaining counted and NAMED on every CI run.
