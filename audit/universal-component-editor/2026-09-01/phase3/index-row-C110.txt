LANE C110 — the C00 index row, for the ORCHESTRATOR to apply to
docs/02-decisions/contracts/README.md. This lane did NOT edit README.md (three lanes share it).

PLACEMENT: in the main contract table (header `| # | Contract | Governs | Key principle |`,
four columns), immediately AFTER the `| **C109** | …` row and BEFORE any C111/C112 row, so the
table stays in id order.

ALSO OWED, in the SAME commit (the "the row and the range move together" rule, which the README's
own banner records as having failed SEVEN times):
  - Conflict-resolution row 4's range: currently reads "C01–C108 + C24.1". At the time of writing
    C109, C110 and C112 are all on disk. ⛔ Do NOT transcribe a number from this file — re-run
    `npx tsx tools/ga-gate/check-contract-index-equivalence.ts` in the commit and quote ITS
    reading, which set-compares in both directions.
  - Arm A of that gate is a shrink-only ratchet and is RED at 20/18 until the C110 and C112 rows
    land. It read 18/18 (RC=0) with both files removed — measured, both directions, 2026-09-01.
    ⛔ The fix is the rows. Raising the baseline to 20 is the one forbidden fix
    (§RATCHET-EXCEEDED-IS-NEVER-DEBT, R7 / L-836).

--- BEGIN ROW (one line; do not wrap) ---
| **C110** | [Parameter, Unit & Expression Model](./C110-PARAMETER-UNIT-AND-EXPRESSION-MODEL.md) | The **parameter, unit and expression semantics** of the component model — the contract [Lane C GAP-1](../../../audit/universal-component-editor/2026-09-01/c-parameters-formulas-constraints.md) measured as the highest-priority gap in the programme, since *"the most spec-relevant package in this lane cites only a PHASE PLAN in every file header — a document that sits BELOW SPECs in the conflict-resolution order and is not in this suite at all."* ⭐ **§0.2 opens with the fact that governs how every other clause may be quoted: the subject is TEST-REACHABLE ONLY**, measured on all four C84 §3.5.1 axes — `apps/editor` imports `@pryzm/family-runtime` **zero** times, **zero** `family.*`/`component.*` verbs are registered, exactly **three** manifests declare it (none an app, none a plugin), and its one production call chain ends in a bake-worker job whose exported entry point is called from tests only. **This is the INVERSE of [C107](./C107-ELEMENT-ADAPTIVE-COMPONENT.md) §0.1's finding and must not be confused with it**: C107 proved a family honestly UNBUILT; this one is **built, tested and unreachable** — 1,118 source lines, 628 test lines, **63 tests passing (RC=0, re-run in the lane)** — which is why every TO-BE here is cheap today and expensive later. **§2 carries [ADR-0376](../adrs/ADR-0376-universal-component-editor-founding-rulings.md) D4 as normative text under the tag `§PARAM-PRECEDENCE`, which production code was ALREADY CITING with no document to resolve to**: the order is **`instance > type > expression > definition default`**, and the sentence it defends is *a default is a fallback, and a fallback that beats design intent is not a fallback.* The inverted code made the founder's own §64 demo — *"make the glass width always the opening width minus twice the frame width"* — **parse, persist, validate, dependency-sort and then never evaluate, returning the authored default with `ok:true` and zero diagnostics**; §2.3 records that this is spec §75's exact prohibition already shipped. **§2.2 also reconciles the ordering with spec §12, which is written the other way round**: §12 is a PIPELINE order (derived computed *from* resolved inputs, last), §2.2 is a PRECEDENCE order, and *reading spec §12 as a precedence list is what produced the inverted code.* **§2.4 makes the both-present case a `warn` that NAMES the parameter rather than a silent resolution**, and **§2.5 forbids the repair's own most tempting regression: an expression that THREW may not fall back to the default** — a substituted value is invisible where a missing one is not. **§2.6 guards the single legitimate exception** (a `string` parameter keeps its default; the evaluator is numeric) so *"expression beats default"* is never over-applied. ⛔ **§2.8 mints `§TWO-DEFAULT-STORES` for a defect found by this lane and reported by none before it**: a document has **two** places to hold a default — `FamilyParameter.defaultValue`, which the resolver reads, and `FamilyDocument.defaults`, which **three migration operators write and no resolver anywhere reads** — and the D4 repair clears the first while leaving the second untouched, so a superseded default survives, inert **only for as long as nobody adds a reader**. **§3 carries ADR-0376 D3 — metres canonical at every model boundary, millimetres a first-class authoring literal converted at parse with the authored spelling retained as provenance — and then states plainly that THE CODE SAYS MILLIMETRES TODAY**, so §3.1 is a TO-BE and the contract describes the repository that exists rather than the one it wants. **§3.3 refuses the half-migration in advance**: the seam already disagrees with itself inside one function (`bakeFamilyInstance` converts the extrude HEIGHT mm→m and passes the profile polygon, declared in METRES, straight through), the mismatch is **LATENT not live** because no writer joins the sketch to `profiles[]` yet, and the blocker is a sketch coordinate system whose millimetre choice carries a **stated solver-tolerance rationale that nobody has measured**. **§3.3-a names the decay** — `find . -name "*.pryzm-family"` returns nothing, so there is no corpus and no format bump *today*, and the cost changes CLASS the day a component is first saved — and **§3.3-b names the negative control the migration will otherwise fake**: a fixture whose numbers are unit-ambiguous (`1`, `2`, `10`) **cannot falsify a 1000× error**, since both readings pass. ⛔ **§3.5 mints `§UNIT-KIND-ERASURE` and forbids the claim the header invites**: `UnitMismatchError` is declared, exported and documented as thrown, and `grep` returns **four hits — the re-export, the comment, the declaration and the name assignment, with NO throw site and NO caller**; the mechanism is that `EvalScope` is `Record<string, number>`, so **a value's unit kind is erased the moment it enters scope and `walk()` could not compare kinds even in principle**. The exit is one change — type the scope with the `CanonicalKind` that already exists — and §3.7 forbids the tenth unit vocabulary while nine rivals stand. **§4 CONTRACTS the engine that exists rather than proposing one** (audit R1): no `eval`, no `Function()`, a closed six-arm AST and a grammar reproduced verbatim; cycle detection by Kahn sort **at edit time, with every non-cyclic parameter still resolving** so an editor can render mid-edit. ⛔ **§4.4 mints `§DIAG-CLOSED-SET` and catches a count going stale INSIDE ONE DAY**: the audit and Lane C both say *"14 typed diagnostic codes"*, measured before the D4 repair added `superseded-default`; the enumeration is now **15 slots / 14 DISTINCT names** (`unknown-identifier` appears in both unions) — *and the coincidence that "14" is still a true number of something is exactly how a stale count survives review.* **§5 is an INSTANCE of [C73](./C73-GEOMETRY-DETERMINISM-AND-TOLERANCE.md) §1 and mints no rival determinism scale**, proven by an executed probe rather than by reading: the same parameter SET in two array orders resolves to the same values, while **`order[]` legitimately differs** and is therefore forbidden as a cache key, hash input or golden assertion. ⭐⭐ **§5.4 mints `§APPROX-BUILTIN` for the one determinism hazard C73 §1.2 does not name and no lane reported: FOUR of the twelve built-ins — `sin`, `cos`, `tan`, `pow` — are IMPLEMENTATION-APPROXIMATED in ECMA-262**, so a bit-identity claim holds only for expressions using none of them — precisely across the browser / bake-worker / AI-worker split this package was built to span. It is a **disclosure, not a prohibition**: the four functions stay, and *pretending the guarantee is stronger than it is* is what the clause refuses. **§6 applies spec §75 as six MUST-NOTs**, including §6.2's autopsy of the sibling defect — `introduce-expression`'s header claimed it *"replaces a parameter's constant defaultValue"* and `apply` never touched it, **because the only test arm asserted the expression was WRITTEN and never that the default it superseded was REMOVED** — and ⛔ **§6.6 refuses to be read as solver authorisation**: §4.3's observation that `equal`/`symmetric`/`horizontal`/`vertical`/`fixed` are closed-form assignments expressible as expressions reaches [C74](./C74-CONSTRAINT-HONESTY.md) §4.2(b) **and stops there**, against C74 §4.5's UNPROVEN verdict backed by 20 families / 17 ADVISORY / 1 ENFORCEMENT / 2 VALIDATION / **0 SOLVING**. **§7 carries the audit's per-requirement score AS-IS rather than re-scoring it** (a second score of one subject is C84 EI-9), marks the two cells ADR-0376 D4 moved, and registers **thirteen gaps** — G-2, the join between this model and the `element.updateParameter` path the product actually uses, named as the headline and explicitly NOT annexed; G-7 recorded as **UNMEASURED** rather than guessed. **§8 names two gates and calls both UNBUILT in the first person**, with §8.2's negative control written in advance (*swapping the two branches must fail it by name*). ⚠ **§0.4 states the measurement window rather than letting a reader assume it**: the D4 repair is UNCOMMITTED WORKING-TREE state, so every cell marked REPAIRED is true of the tree and false of `6e15af2f` — *the same care the audit itself failed to take when it re-transcribed a stale gate reading.* **Status: CANONICAL — deliberately NOT ACTIVE**, and §9.1 states the four-part exit condition. | P5, P8 |
--- END ROW ---
