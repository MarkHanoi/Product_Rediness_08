npm warn Unknown project config "package-manager-strict". This will stop working in the next major version of npm. See `npm help npmrc` for supported config options.

$ cd packages/site-parcel-data && npx tsc -p tsconfig.json --noEmit > pkg-tsc.txt 2>&1; echo "PKG_TSC_RC=$?"
PKG_TSC_RC=0
(the npm warn line above is the entire output; zero diagnostics)
