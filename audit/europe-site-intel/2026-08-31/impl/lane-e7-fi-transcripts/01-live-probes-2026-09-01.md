# E7-FI · TRANSCRIPT 01 — LIVE PROBES, 2026-09-01

Every command below was executed against the live services from this machine on 2026-09-01.
Outputs are pasted verbatim (long bodies summarised by the counting one-liner that produced the
number, so every figure in `lane-e7-fi.md` is reproducible).

---

## §1 — THE RYHTI OPEN CHANNEL: WHAT COLLECTIONS EXIST

```
$ curl -sS -m 60 -w "\nHTTP=%{http_code} BYTES=%{size_download}\n" \
    "https://paikkatiedot.ymparisto.fi/geoserver/ryhti_plan/ogc/features/v1/collections?f=json"
HTTP=200 BYTES=17024
```

```
n collections = 4
---
id: pub_prep_ld_plan_ix_gs    title: Valmisteilla olevat asemakaavat
id: pub_prep_lm_plan_ix_gs    title: Valmisteilla olevat yleiskaavat
id: pub_valid_ld_plan_ix_gs   title: Asemakaavahakemisto
id: pub_valid_lm_plan_ix_gs   title: Yleiskaavahakemisto
extent (all four): bbox [15.05378527, 58.60745652, 33.99353747, 70.26415663] CRS84
```

### Feature counts (`limit=1`, so `numberMatched` is the TRUE total — see §4)

```
pub_valid_ld_plan_ix_gs : numberMatched=5635 returned=1
pub_valid_lm_plan_ix_gs : numberMatched=647  returned=1
pub_prep_ld_plan_ix_gs  : numberMatched=0    returned=0
pub_prep_lm_plan_ix_gs  : numberMatched=0    returned=0
```

⭐ **The two `prep` collections are DECLARED, SERVED, and EMPTY.**

---

## §2 — IS THERE A HIDDEN PLAN-OBJECT LAYER? (the "GetCapabilities is not an inventory" check)

Root OGC-API collections and root WFS are both closed; only the workspace-scoped services answer.

```
$ curl .../geoserver/ogc/features/v1/collections?f=json                    -> HTTP 404
$ curl ".../geoserver/wfs?service=WFS&version=2.0.0&request=GetCapabilities"
     -> HTTP 200, ows:ExceptionReport, ExceptionText> Service WFS is disabled
$ curl .../geoserver/web/                                                 -> HTTP 403 (nginx)
```

Workspace-scoped WFS lists exactly the same four:

```
$ curl ".../geoserver/ryhti_plan/wfs?service=WFS&version=2.0.0&request=GetCapabilities"
HTTP=200 BYTES=105574
<Name>ryhti_plan:pub_valid_ld_plan_ix_gs</Name>
<Name>ryhti_plan:pub_prep_ld_plan_ix_gs</Name>
<Name>ryhti_plan:pub_prep_lm_plan_ix_gs</Name>
<Name>ryhti_plan:pub_valid_lm_plan_ix_gs</Name>
```

WMS lists **8** `<Name>` elements — the near-miss:

```
$ curl ".../geoserver/ryhti_plan/wms?service=wms&version=1.3.0&request=GetCapabilities"
HTTP=200 BYTES=11591
<Name>WMS</Name>
<Name>pub_prep_ld_plan</Name>          <-- these four
<Name>pub_prep_ld_plan_ix_gs</Name>
<Name>pub_prep_lm_plan</Name>
<Name>pub_prep_lm_plan_ix_gs</Name>
<Name>pub_valid_ld_plan</Name>
<Name>pub_valid_ld_plan_ix_gs</Name>
<Name>pub_valid_lm_plan</Name>
<Name>pub_valid_lm_plan_ix_gs</Name>
grep -c "<Layer" -> 5
```

⛔ **NEGATIVE RESULT — the four extra names are STYLE names, not layers.** Dumping the
`<Capability>` tree shows each `<Layer queryable="1">` block ending in

```xml
<Style>
  <Name>pub_valid_ld_plan</Name>
  <Title>Ryhti plan</Title>
  <LegendURL .../>
</Style>
```

WMS and WFS/OGC-API expose the SAME four `_ix_gs` index layers. **No hidden plan-object layer
exists.** Recorded because this is exactly the shape that produces false "hidden layer" claims.

### Workspace sweep on the same host

```
ryhti            -> 404      ryhti_plan_object  -> 404    ryhti_plan_objects -> 404
ryhti_kaava      -> 404      ryhti_rakennus     -> 404    rytj               -> 404
kaava            -> 404      plan               -> 404    planobject         -> 404
ryhti_regulation -> 404      ryhti_plan_matter  -> 404    ryhti_valid        -> 404
ryhti_public     -> 404      inspire            -> 404    ryhti_plu / plu    -> 404
ryhti_building   -> 200  ⭐ DISCOVERY (recorded, NOT actioned — control 10)
```

```
$ curl ".../geoserver/ryhti_building/ogc/features/v1/collections?f=json"
HTTP=200 BYTES=20793 · n = 5
 - avoimet_lupa_rakennukset | Rakennushankkeet
 - avoimet_rakennukset      | Valmiit rakennukset
 - open_address             | open_address
 - open_address_deleted     | Poistuneet osoitteet
 - open_building            | open_building
```

---

## §3 — `offset` IS SILENTLY IGNORED; THE PAGING PARAMETER IS `startIndex`

```
$ for off in 0 2000 4000; do curl ".../pub_valid_ld_plan_ix_gs/items?limit=2000&offset=$off&skipGeometry=true&f=json"; done
off=0    HTTP=200 BYTES=18604569
off=2000 HTTP=200 BYTES=18604572
off=4000 HTTP=200 BYTES=18604572
```

```
first ids: ...000357ff-... | ...000357ff-... | ...000357ff-...
last  ids: ...59474f75-... | ...59474f75-... | ...59474f75-...
identical 0 vs 2000: true
```

The server's OWN `next` link gives the answer:

```
next | .../items?limit=2000&offset=0&skipGeometry=true&f=json&startIndex=2000
```

```
$ curl ".../items?limit=2000&startIndex=2000&f=json"  -> returned 2000, first id 2c22e9a8c4ed (different)
$ curl ".../items?limit=2000&startIndex=4000&f=json"  -> returned 1635
2000 + 2000 + 1635 = 5635 = numberMatched  ✓
```

⛔ A paginator written to the OGC API Features standard's `offset` reads page one forever and
concludes the dataset holds 2,000 rows. (`skipGeometry=true` is likewise accepted and ignored.)

---

## §4 — PAGE CAP, AND `numberMatched` LYING ON AN OVER-LIMIT REQUEST

```
$ curl ".../pub_valid_ld_plan_ix_gs/items?limit=10000&f=json"
HTTP=200 BYTES=18604315 · matched 2000 returned 2000     <-- capped, and numberMatched follows the cap
$ curl ".../pub_valid_ld_plan_ix_gs/items?limit=1&f=json"
matched 5635                                              <-- the true total
```

---

## §5 — AXIS ORDER: CRS84 (lon,lat), AND THE SWAP FAILS SILENTLY

```
--- lon,lat (correct) ---
$ curl ".../items?bbox=27.6770,62.8924,27.6771,62.8925&limit=1&f=json"
matched 4

--- lat,lon SWAPPED CONTROL ---
$ curl ".../items?bbox=62.8924,27.6770,62.8925,27.6771&limit=1&f=json"
HTTP=200
{"type":"FeatureCollection","features":[],"totalFeatures":0,"numberMatched":0,"numberReturned":0,"timeStamp":"2026-09-01T16:46:47.779Z","crs":null}
```

⛔ **HTTP 200, zero features, no error.** The same silent-zero trap as EE's CQL `POINT(N E)`.

---

## §6 — NATIVE CRS (EPSG:3067) IS AVAILABLE AND CONFIRMED IN THE RESPONSE

```
$ curl ".../items?limit=1&crs=http%3A%2F%2Fwww.opengis.net%2Fdef%2Fcrs%2FEPSG%2F0%2F3067&f=json"
first coord: [533414.69155533, 6993969.95529465]
crs field  : {"type":"name","properties":{"name":"urn:ogc:def:crs:EPSG::3067"}}
```

bbox (CRS84) + output CRS (3067) together, on the Kuopio point:

```
matched 4 · crs urn:ogc:def:crs:EPSG::3067 · coord0 [534495.91541228, 6973690.77635244]
```

`bbox-crs=.../EPSG/0/3067` with a native easting/northing bbox also works.

---

## §7 — FAILURE SHAPES (JSON documents, NOT ows:ExceptionReport)

```
$ curl ".../collections/pub_valid_XX_bogus/items?limit=1&f=json"
HTTP=404
{"type":"NotFound","title":"Unknown collection pub_valid_XX_bogus"}

$ curl ".../pub_valid_ld_plan_ix_gs/items?filter-lang=cql2-text&filter=bogus_field%3D%27x%27&limit=1&f=json"
HTTP=400
{"type":"InvalidParameterValue","title":"Illegal property name: bogus_field for feature type pub_valid_ld_plan_ix_gs"}
```

## §8 — CQL2-TEXT WORKS; `/items/{id}` RETURNS A FeatureCollection

```
$ curl ".../items?filter-lang=cql2-text&filter=permanent_plan_identifier%3D%27AK-000480%27&limit=5&f=json"
HTTP=200 -> the plan, geometry included

$ curl ".../items/pub_valid_ld_plan_ix_gs.000357ff-0d39-4fb7-8767-4977d2f5e65b?f=json"
HTTP=200 · j.id -> undefined · j.properties -> undefined
head: {"type":"FeatureCollection","features":[{"type":"Feature","id":"pub_valid_ld_plan_ix_gs.000357ff-...
```

⚠ The OGC single-item path returns a **FeatureCollection**, not a Feature. This adapter never
uses it.

---

## §9 — THE ATTACHMENT SERVICE RESOLVES, KEYLESS

```
$ curl -sSL "https://uri.rakennetunymparistontietojarjestelma.fi/planattachmentdocument/54f4e444-b9ab-4547-b458-709b9a6c4747/file"
HTTP=200 BYTES=11738411 CT=application/pdf FINAL=<same url, no redirect>
$ head -c 8 doc.pdf | od -c
0000000   %   P   D   F   -   1   .   4
```

(The Kuopio `AK-000480` attachment the index cites as
`"Kaavakartta ja kaavamääräykset, 297-31-05-669-1.pdf"`.)

---

## §10 — LICENCE, VERBATIM

```
$ curl "https://ckan.ymparisto.fi/api/3/action/package_show?id=rakennetun-ympariston-tietojarjestelman-kaavatiedot"
HTTP=200 BYTES=15874
```

Verbatim from `result.notes`:

> "Laki rakennetun ympäristön tietojärjestelmästä velvoittaa kuntia ja maakunnan liittoja
> toimittamaan järjestelmään asemakaava-, yleiskaava- ja maakuntakaavatiedot … Velvoite koskee
> lain voimaan tulon 1.1.2024 jälkeen hyväksyttyjä kaavoja ja sille on **viiden vuoden
> siirtymäaika**. Aineistossa tulee olemaan uudet kaavat valtakunnallisesti kattavasti siis
> **1.1.2029 lähtien**, siihen asti aineisto sisältää kaavoja niiltä alueilta, joilta kunnat ja
> maakuntien liitot ovat toimittaneet tietoja. Voimassa olevien kaavojen osalta aineistoa on
> tuotettu **VOOKA**-projektissa … **Etelä- ja Pohjois-Savossa**."
>
> "Ennen lain siirtymäajan päättymistä voimassa olevien kaavojen osalta tietosisältö voi olla
> **suppeampi**: https://iri.suomi.fi/model/vooka/"
>
> "Avoimena tietona on tarjolla OGC API Features-rajapinta. … Muut kuin avoimet tietotuotteet
> edellyttävät **tietolupaa** ja/tai käyttösopimusta … ryhti@syke.fi."
>
> "**Aineisto kuuluu SYKEn avoimiin aineistoihin (CC BY 4.0)** avoimen tietosisällön osalta."

`result.resources` — **2**, and neither is a plan-object service:

```
* Kaavat OGC API Features latauspalvelu | https://paikkatiedot.ymparisto.fi/geoserver/ryhti_plan/ogc/features/v1
* Kaavat WMS katselupalvelu       | WMS | https://paikkatiedot.ymparisto.fi/geoserver/ryhti_plan/wms?...
```

---

## §11 — HSY SeutuRAMAVA — REACHABILITY RE-VERIFIED (recorded, NOT wired)

```
$ curl "https://kartta.hsy.fi/geoserver/wfs?service=WFS&version=2.0.0&request=GetCapabilities"
HSY GetCapabilities HTTP=200 BYTES=349217
distinct SeutuRAMAVA_* layer names: 26
newest kortteli layers: ..._22021 _22022 _22023 _22024 _22025  (and _12026, _I_2024, tila_*)

$ curl ".../wfs?...GetFeature&typeNames=asuminen_ja_maankaytto:SeutuRAMAVA_kortteli_12026&count=1&outputFormat=application/json"
HTTP=200 BYTES=7946
id: SeutuRAMAVA_kortteli_12026.fid-40451fad_1a05db6e590_7bf8
   kunta      = "049"          (Espoo)
   korttunnus = "0490100001"
   kala       = 137777         (rakennusoikeus, m2)
   karayht    = 116900         (käytetty kerrosala)
   laskvar_yh = 27659          (CALCULATED RESERVE, total)
   laskvar_ak = 20850  laskvar_y = 6809
   laskvar_ap = 0  laskvar_k = 0  laskvar_t = 0  laskvar_nn = 0   (GENUINE zeros, not UNKNOWN)
   rakerayht  = 8092           (under construction)
   rekpvm     = "20251219"
```

Byte-for-byte the payload E5 §A-1 recorded, from an independent request. **REACHABLE.**

### And the finding that matters more — the EMPTY INTERSECTION

```
$ for each HSY kunta, CQL count against BOTH valid indexes:
Helsinki(091)   pub_valid_ld_plan_ix_gs -> 0     pub_valid_lm_plan_ix_gs -> 14
Espoo(049)      pub_valid_ld_plan_ix_gs -> 0     pub_valid_lm_plan_ix_gs -> 15
Vantaa(092)     pub_valid_ld_plan_ix_gs -> 0     pub_valid_lm_plan_ix_gs -> 0
Kauniainen(235) pub_valid_ld_plan_ix_gs -> 0     pub_valid_lm_plan_ix_gs -> 0
--- controls ---
Tampere(837)    0 / 0        Turku(853) 0 / 0        Kuopio(297) 1053 / 64
```

⛔ **Not one asemakaava — the instrument that carries rakennusoikeus — exists in the open Ryhti
index for ANY of HSY's four municipalities.** The HSY cross-check has no overlapping parcel to
run on today.
