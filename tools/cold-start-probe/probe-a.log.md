# PROBE A — Cold Start Probe log (AMB Refós publisher)

**Rule: every line is appended BEFORE or IMMEDIATELY AFTER the event it records. No retrospective reconstruction.**

Clock source: `date -u +%H:%M:%SZ` via Bash, every entry.

| UTC | Stage | Event |
|---|---|---|
| 08:43:09Z | setup | Probe started. Read PROBE-DISCIPLINE.md. |
| 08:43:30Z | setup | Read MACHINE-READABLE-EVIDENCE-REGISTER.md + BLOCKER-CLASSIFICATION-STANDARD.md. Created `tools/cold-start-probe/`. |
| 08:43:30Z | setup | Listed `packages/site-parcel-data/src/rulepacks/` (51 files) and `providers/` (36 files). NOTE: `esBadalona.ts`, `esCornella.ts`, `esLHospitalet.ts`, `esSantBoi.ts` already exist — four AMB municipalities are NOT cold. They must be excluded from the draw or the probe measures nothing. |
| **08:44:03Z** | **STAGE 1 START — dataset discovery** | Clock started. |
| 08:44:03Z | 1 | `GET .../MapServer/16?f=json` → **HTTP 200, 38,624 B**. Layer name `QU_Trames`. Fields include `CODI_INE`, `NOMMUNI`, `CLAU_URB`, `PLAN`, `NORMATIV`, `DESCRIP`, `SINTETIC`. `maxRecordCount` 2000. **Spatial extent VERIFIED** (guard #2): EPSG:3857 x 205,054→255,872, y 5,051,118→5,097,477 ⇒ lon 1.842°→2.298°, lat 41.31°→41.62° = Barcelona metropolitan area. Not an acronym collision. |
| 08:44:20Z | 1 | `query?where=1=1&outFields=CODI_INE,NOMMUNI&returnDistinctValues=true` → HTTP 200, 2,624 B, **36 municipalities**. Full list saved to scratchpad `munis.json`. |
| **08:44:39Z** | **SELECTION RULE (stated BEFORE work on the city begins)** | **(a)** universe = the 36 distinct `CODI_INE` returned by layer 16, sorted ascending as strings; **(b)** exclude `08019` Barcelona (reference city, already shipped); **(c)** exclude the four AMB municipalities that already have a rulepack file in `packages/site-parcel-data/src/rulepacks/` — `08015` Badalona, `08073` Cornellà, `08101` L'Hospitalet, `08200` Sant Boi — they are not cold starts; **(d)** N = 31 remain; take the **median by 0-based index `floor(31/2) = 15`**. |
| 08:44:39Z | 1 | **RESULT → `08196` SANT ANDREU DE LA BARCA.** Verified cold: `grep -rn "08196\|Sant Andreu de la Barca"` across the repo (excluding node_modules) returns **0 real hits** (7 hits are coincidental digit substrings inside test-fixture float literals). No rulepack, no provider, no dossier, no bbox. |
| 08:45:20Z | 1 | `layer 16 query where CODI_INE='08196'` → HTTP 200, **134,932 B, 637 polygons, 43 distinct `CLAU_URB`**. ⭐ The `NORMATIV` attribute names the governing instrument PER ZONE, machine-readably: `num_poum_sant_andreu_de_la_barca.titol_4` (residential/industrial claus), `…titol_6` (clau 18/19a), `…titol_7` (systems), and `Asterisc` for claus `11d 11e 3b 8f` whose `PLAN` = `PD*`. **The legal stack is DISCOVERED, not searched for.** |
| 08:45:51Z | 1 | `GET MapServer?f=json` → HTTP 200, 8,023 B. 18 layers + **2 TABLES**: `18 QUAL_MUNI`, `19 AE_USOS`. Layer 17 `OV_Trames` is the volumetric layer `bcnRefosOVProvider.ts` already consumes. |
| 08:47:00Z | 1 | `GET MapServer/18?f=json` → HTTP 200, 3,478 B. ⭐⭐ **`QUAL_MUNI` is a per-municipality per-clau NUMERIC ENVELOPE PARAMETER TABLE**: `IE` (índex d'edificabilitat = FAR), `ARM` (alçada reguladora màxima = height), `N_PLANTES`, `OCUP_MAX` (coverage), `SEP_FVIAL` / `SEP_LAT` / `SEP_FONS` (front/side/rear setbacks), `S_MIN_PAR`, `F_MIN_PAR`, `ST_MAX`, `DENS_U`, `ESPLADERIVAT`, `ASTERISC`, `OBSERV`. **Every variable the KPI names.** |
| 08:47:49Z | 1 | `grep -rn "QUAL_MUNI\|OCUP_MAX\|SEP_FVIAL\|MapServer/18"` across the repo → **0 hits**. This table is UNKNOWN to PRYZM after five cities and a Barcelona GIS audit. FINDING, not a fix. |
| 08:48:00Z | 1 | ⚠ **FALSE-NEGATIVE AVOIDED (guard #1/#3).** `table 18 query where CODI_INE='08196'` → HTTP 200, **0 rows**. Not "absent": `CODI_INE` is **100 % NULL** across all 486 distinct rows. The real key is **`CODI_MUN` = `<INE>_<CLAU_URB>`**. Re-queried `CODI_MUN LIKE '08196%'` → HTTP 200, **28,613 B, 60 rows**. Had I stopped at the first empty I would have recorded "no parameter table for this city". |
| **08:48:41Z** | **STAGE 1 END** | Publisher = AMB *Servei d'Informació Urbanística*; zoning service = `qualificacio_refos_3857` layers 16/17 + table 18; plan document = **POUM de Sant Andreu de la Barca**, named by the service itself. **Stage 1 = 4 min 38 s.** |
| 08:48:41Z–08:58:51Z | — | ⏸ **SESSION GAP (~10 min).** The probe's first session ended after Stage 1; this log resumes in a NEW session at Stage 2. The gap is **NOT counted** in any stage's wall-clock — stage clocks are start→end of that stage's own work. Recorded so the total is auditable rather than reconstructed. |
| **08:58:51Z** | **STAGE 2 START — legal stack** | Clock started. Carried forward from Stage 1: the service names the instrument per zone in `NORMATIV` (`num_poum_sant_andreu_de_la_barca.titol_4/6/7`) and flags `Asterisc` where `PLAN='PD*'`. Stage 2 must (a) obtain the POUM text, (b) verify the títol mapping is real, (c) establish what `PD*` delegates. |
| 08:58:55Z | 2 (parallel) | Kicked off `node catastroParcelFrame.mjs --ine 08196 --name "SANT ANDREU DE LA BARCA"` in the **BACKGROUND** (province-08 ATOM already cached from Barcelona). Its download overlaps Stages 2–3; cost attributed to Stage 4 and the overlap is declared, not hidden. |
| 08:59:30Z | 4 (bg, done) | ⭐⭐ **THE DGC/INE COLLISION FIRED AGAIN — AND THIS TIME THE COLLIDING NAME IS NEARLY IDENTICAL.** Builder warned: *"code 08196 is **SANT ANDREU DE LLAVANERES** but INE 08196 is **SANT ANDREU DE LA BARCA** ⇒ used DGC 08195"*. València/Turís was a collision between two unrelated names; this is a collision between two `SANT ANDREU DE …` municipalities 40 km apart. A human eyeballing the ATOM title would plausibly have accepted it. **The name-authoritative rule is load-bearing, and its failure mode is not exotic — it is the second occurrence in two cities.** Frame built: **2,472 parcels**, 5.3 MB GML, 6.4 s. Independent verification deferred to Stage 4 (a spatial join against the AMB layer-16 polygons: Llavaneres is Maresme, wholly outside the AMB extent, so a wrong frame would score ~0 %). |
| 09:00:40Z | 2 | Layer 16 `outFields=*` on 08196 → HTTP 200, 3,172 B. **21 fields.** The per-zone legal citation is finer than Stage 1 recorded: `NORMATIV` = `num_poum_sant_andreu_de_la_barca.titol_4.**capitol_2**` — títol AND capítol. `INE_URB` = `08196_1` is the literal join key into table 18's `CODI_MUN`. No document-URL field exists on the feature. |
| 09:02Z | 2 | ⚠ **FALSE POSITIVE CAUGHT — HTTP 200 IS NOT "FOUND" ON THIS HOST.** 4 of 9 guessed normative URLs on `geoportal.amb.cat` returned **HTTP 200** — all with **byte-identical 1,657 B** bodies equal to the site root. A probe recording status alone would have banked 4 hits. `geoportalplanejament.amb.cat` serves a **SOFT 404**: 200 + a JS `window.location.replace` stub. All subsequent classification is by BODY, not status. |
| 09:04Z | 2 | ⭐ Real normative index page found: `…/Informacio/Contingut/Normativa/ca/default.html` → **200, 12,957 B**. It is a **publisher-level, INE-keyed table for all 36 AMB municipalities** (`../../../Normativa/<INE>_INDEX.htm`). AMB states the refós is *"la transcripció literal de les normatives dels Plans Generals o POUM vigents"* — an official consolidated restatement, not a summary. **08196 sits in the left column = "Municipis amb PLA GENERAL o POUM"** ⇒ its own POUM, its own independent refós (consistent with `NORMATIV`). |
| 09:06Z | 2 | ⛔ **BUT THE LINKED DOCUMENT DOES NOT EXIST.** Base path PROVEN correct by a co-located asset (`/Informacio/css/Planejament.css` → 200, 3,679 B), yet `/Informacio/Normativa/08196_INDEX.htm` → **404**. Tested **6 municipalities** (08015, 08904, 08196, 08101, 08073, 08125) → **404 for every one**; 6 filename/case/extension variants → 404; directory listing → 404. **Programme-wide, not city-specific.** |
| 09:08Z | 2 | **VARIED CLIENT AND PROTOCOL BEFORE RECORDING THE NEGATIVE** (guard #4): browser-UA https / browser-UA+Referer / plain http / no UA at all → **404 every time**; `www3.amb.cat` → 404; `www.amb.cat` → **403** (different host, different failure — recorded as UNKNOWN for that host, not as a 404). Wayback: `{"archived_snapshots":{}}` — **never archived**. ⇒ **This is a REAL negative, recorded as such.** AMB publishes a link table whose targets are all gone. |
| 09:09Z | 2 | Alternate route: `seu-e.cat` for 08196 hosts **no** POUM document; it forwards to **RPUC** (`municipiSel=08196`) and CIDO. RPUC's old `dtes.gencat.cat` host → 301. New portal is an Angular SPA; extracted the API base from `main-es2015.js` (2.36 MB): **`/RPUC-portal/rest/consulta`** with endpoints `/basica /detall /codi /documents /arbre /arbreFitxa /convenis /avancada`, called by **GET** with a flattened filter object. |
| 09:10:51Z | 2 | RPUC API is **LIVE, not absent**: `/basica` GET → **HTTP 500 `java.lang.NullPointerException` (42 B)** = params missing; POST → **405** = wrong method; the `/rpucportal/…` mount → 404. `municipi=<name>` returns a **different** error (`SystemException`, 72 B) from `municipi=<code>` (NPE, 42 B), so `municipi` is a real key expecting a dropdown-sourced id. **8 param names + 3 value shapes tried; the query contract was NOT reversed inside the time box.** Classification: **`Investigate` / Data acquisition — NOT `No`.** (Failure ≠ empty.) |
| **09:10:51Z** | **STAGE 2 END** | **Stage 2 = 12 min 00 s.** Outcome, split honestly: **the legal CITATION is machine-readable and per-zone** (`titol_4.capitol_2`, straight off the feature) — but **the legal TEXT is NOT obtainable** by any route tried. ⇒ We can name the governing article; we cannot quote it from bytes we hold. Under ADR-0284 (*derived geometry permissible, derived law is not*) that gap is the probe's central legal finding, and it is a **HUMAN JUDGEMENT point**, logged at Stage 5. |
| **09:11:20Z** | **STAGE 3 START — variable resolution** | Clock started. Target: table 18 `QUAL_MUNI`, 60 rows keyed `CODI_MUN = '08196_<CLAU_URB>'`. Question: for how many of 08196's 43 distinct claus is every envelope variable (`IE`, `ARM`, `N_PLANTES`, `OCUP_MAX`, `SEP_*`) actually POPULATED? |
| 09:12:30Z | 3 | `stage3-variables.mjs` → table 18 HTTP 200, 28,586 B, **60 rows**; layer 16 HTTP 200, 137,023 B, **637 polygons / 43 distinct claus / 5.62 km²**. |
| **09:13:06Z** | **STAGE 3 END — AND IT REFUTES STAGE 1's OWN ⭐⭐** | **Stage 3 = 1 min 46 s.** `QUAL_MUNI` has a row for **43/43 claus (100 % of zoned area)** — but the rows are **overwhelmingly EMPTY**. Measured over all 60 rows: **`IE` (FAR) is NULL 60/60 — never populated for this municipality at all.** `ARM`, `N_PLANTES`, `OCUP_MAX`, `SEP_FVIAL/LAT/FONS`, `S_MIN_PAR`, `F_MIN_PAR` are NULL **54/60**. `ST_MAX`, `DENS_SH`, `ESPLADERIVAT`, `ASTERISC`, `OBSERV` are NULL **60/60**. **Zero empty strings** — the null semantics are unambiguous, so this is a real absence, not a parsing artefact. ⇒ Only **6 claus (`8a 8b 8c 8d 8e 9`)** carry parameters, = **8.78 % of zoned area**. All six are *edificació aïllada* (detached) zones — e.g. `08196_9` = `{ARM:9, N_PLANTES:"3", OCUP_MAX:30, SEP_FVIAL:6, SEP_LAT:3, SEP_FONS:3, S_MIN_PAR:400, F_MIN_PAR:15}`. ⚠ **Stage 1 recorded this table as "⭐⭐ every variable the KPI names". That was a SCHEMA reading, not a DATA reading. It is corrected here — a table can be a perfect schema and an empty dataset, and the first four minutes of this probe believed the schema.** |
| **09:13:10Z** | **STAGE 4 START — constraint resolution** | Clock started. Reuse check first: `packages/site-parcel-data/src/providers/bcnRefosOVProvider.ts` **already reads this exact AMB service (layer 17 `OV_Trames`)** for Barcelona clau 18 — footprint + `PLANTES` ⇒ an extrudable envelope. It takes a lat/lon, so its mechanism is publisher-generic, not Barcelona-specific. Refusing to declare a refusal without checking it. |
| 09:16Z | 4 | `stage4-constraints.mjs`. Layer 17 `CODI_INE='08196'` → **221 polygons** (so the OV path is NOT empty here). Layer 16 geometry HTTP 200, **1,029,292 B**, 637 rings in EPSG:4326. Parcel frame: **2,472** parcels. **Extent cross-check** — zoning bbox lon 1.9461–1.9850 / lat 41.4392–41.4647 vs parcel bbox lon 1.9472–1.9815 / lat 41.4406–41.4638: **the parcel frame nests inside the zoning extent.** Sant Andreu de **Llavaneres** is at lon ≈ 2.49 / lat ≈ 41.61 — 45 km away and disjoint. ⇒ **the DGC-08195 frame is INDEPENDENTLY VERIFIED to be the right municipality**, not merely name-matched. |
| 09:16Z | 4 | **R2 INDEPENDENT ORACLE.** 15 seeded parcels re-classified by the **ArcGIS server's own** `esriSpatialRelIntersects` point query — a different algorithm on a different machine from my local even-odd PIP. **agree 15 / disagree 0 / server-error 0**, and every server row returned `CODI_INE=08196`. Local PIP is trusted for the remaining 2,457. |
| 09:16Z | 4 | Classification over the **cadastral-parcel denominator (N = 2,472, every parcel weighs 1)**: `envelope` **951 = 38.47 %** · `refuse-no-parameters` **830 = 33.58 %** · `refuse-delegated` **490 = 19.82 %** · `refuse-terminal` **197 = 7.97 %** · `outside-zoning` **4 = 0.16 %**. |
| 09:17Z | 4b | **THE OV_TRAMES PASS — and it is worth 18 points.** 221 OV polygons over claus `1,2,3a,3b,3c,4,13a` (the dense core). `PLANTES` distribution: `B+2`×40, `B+4`×32, `B+3`×28, `B+5`×22, `B+1`×12, `B+6`×6, `B+7`×2, `B+3+A`×1, `B+4+A`×1, **`ED`×77 (unparseable ⇒ must refuse, exactly the provider's `unparseable-plantes` path)**. **515 parcels (20.83 %)** fall inside an OV polygon; **452 (18.28 %)** have a parsable `PLANTES`; **all 452 were previously refusals** (425 `refuse-no-parameters` + 27 `refuse-delegated`). ⇒ **Ignoring layer 17 would have under-reported this city by 18.28 points.** |
| **09:15:21Z** | **STAGE 4 END** | **Stage 4 = 2 min 11 s** (incl. the 4b OV pass; the parcel-frame download was pre-warmed in the background during Stage 2 and is declared, not hidden). |
| **09:15:30Z** | **STAGE 5 START — determination** | Clock started. |
| 09:17:14Z | 5 | First determination emitted → **Determination 99.84 % = Envelope 56.76 % + Refusal 43.08 %**. ⛔ **REJECTED ON INSPECTION.** The worked envelope landed on parcel `2700401DF1920S`, clau 9 — whose polygon carries `NORMATIV='Asterisc'` / `PLAN='PD*'`, i.e. **the POUM delegates that land to a plà derivat**, yet it scored `envelope` because my classifier tested QUAL_MUNI parameters **before** the delegation flag. **This is the `envelope-solid-overstates-partial-data` family, reproduced live inside the probe that was measuring it.** A published baseline parameter set does not survive a derived plan that may override it (ADR-0283: Unknown over inferred entitlement). |
| 09:18:23Z | 5 | `stage5b-reclassify.mjs` — precedence **stated first, then applied**: (1) OV footprint + parsable `PLANTES` → envelope (the volumetric ordering RESOLVES a delegation, it is the derived plan's content vectorised); (2) `Asterisc`/`PD*` → refuse-delegated; (3) Títol 6/7 → refuse-terminal; (4) QUAL_MUNI params → envelope; (5) all-NULL row → refuse-gated; (6) no row → refuse; (0) no polygon → **not a determination**, counted separately. **The fix moved 2.71 points from ENVELOPE to REFUSAL.** |
| **09:19:30Z** | **STAGE 5 END** | **Stage 5 = 4 min 00 s** (incl. finding and fixing my own defect and a full re-run). **FINAL: Determination 99.84 % = Envelope 54.05 % + Refusal 45.79 %**, `no-determination` 0.16 % counted separately. Envelope splits **35.60 % shippable today** (QUAL_MUNI) + **18.45 % behind an UNSIGNED per-city certification gate** (OV_Trames). Refusals: legally terminal **7.97 %**, delegated **20.87 %**, gated on external authority **16.95 %**. |
| 09:20:16Z | — | `probe-a.result.json` emitted (11,424 B) from `stage5b.final.json` only — no figure retyped by hand (R8). |
| **09:20:16Z** | **PROBE A COMPLETE** | **Total measured wall clock = 24 min 35 s** across five stages (4.63 + 12.00 + 1.77 + 2.18 + 4.00). The ~10 min session gap is excluded and declared. **Municipality-specific product code written: 0 lines.** The city entered every query as a parameter, never a branch. |
