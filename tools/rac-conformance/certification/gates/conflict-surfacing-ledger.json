{
  "gate": "check-conflict-surfacing",
  "note": "§S2-INTERVENING — PAID 2026-08-14, same commit as the fix (C70 5.4). The FIRST HONEST READING (2026-08-14, this gate's first run) pinned 103 rows here: one MERGE PATH per disposition that the gate DROVE and watched discard a peer's authored value WITHOUT emitting a CRDTConflict. They shared ONE root cause and fell together: YjsDocAdapter._discloseOverwrittenLocalWrites ended with `this._localPendingWrites.delete(docKey)`, clearing the WHOLE doc's pending bookkeeping after ANY merge - including edits still in flight that had not yet met their rival. §PENDING-PER-PROP made the lifecycle per-property: an entry now leaves the books only when a merge touched its property (disclosed, or converged to the local value) or when it ages past a bounded TTL - the trade-off is named at the site. Post-fix reading: S1-DIRECT 103/103 disclosed, S2-INTERVENING 103/103 disclosed, S3-AGREEING 0 artefacts (no noise). Unit reproduction: packages/sync-client/__tests__/conflict-surfacing-intervening.test.ts. This file stays present with an EMPTY row set so a regression re-appears as UNLEDGERED (exit non-zero), never as absorbable debt.",
  "rootCause": "PAID - was: packages/sync-client/src/YjsDocAdapter.ts - _discloseOverwrittenLocalWrites: `this._localPendingWrites.delete(docKey)` at the end of the pass discarded disclosure eligibility for in-flight local writes that the merge did not touch. Fixed by §PENDING-PER-PROP (per-property cleanup + bounded TTL).",
  "silentMerges": []
}
