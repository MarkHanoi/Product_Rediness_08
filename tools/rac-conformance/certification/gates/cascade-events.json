{
  "//": [
    "The DECLARED cascade-event ledger read by check-propagation-reaches.",
    "",
    "PHASE 5 (CONNECT-0) RESOLUTION, 2026-08-12 — C72 §2.1 wire-or-delete executed.",
    "The generic protocol declared FOUR events; each has reached a terminal state:",
    "",
    "  * pryzm-dep-cascade        WIRED. ARM A: apps/editor/src/engine/",
    "    initDependencyCascade.ts registers the production listener (routes the",
    "    sitsOn/supports structural tasks — the one pair no bespoke tracker serves —",
    "    into the EXISTING rebuild entry points; skips hosts/hostedBy and",
    "    boundedBy/adjacentTo, which Door/WindowDependencyTracker and",
    "    RoomTopologyObserver own, per C72 §2.4). ARM B: StoreChangeEvent now",
    "    declares `prevState?` (B1), WallStore's bus bridge forwards the §STEP7",
    "    snapshot into it, and the event's catalog payload declares `prevState?`",
    "    which the default dispatcher populates (B2).",
    "  * pryzm-room-reval         DELETED (dispatch + catalog entry + this ledger's",
    "    row, one commit). Duplicated RoomTopologyObserver; zero listeners ever.",
    "  * pryzm-hosted-reval       DELETED (same commit). Duplicated Door/Window-",
    "    DependencyTracker; zero listeners ever.",
    "  * pryzm-structural-cascade DELETED (same commit). Its sitsOn/supports",
    "    coverage travels inside pryzm-dep-cascade's tasks and is what the wired",
    "    listener routes.",
    "",
    "declaredFindings is a NAMED, SHRINK-ONLY ledger. Each entry is '<event>:<arm>'.",
    "It is EMPTY as of the Phase 5 commit: the eight findings (four ARM-A, four",
    "ARM-B) left the books in the commit that paid them, per this gate's own rule.",
    "A name that reappears is a regression, not debt."
  ],
  "events": [
    {
      "name": "pryzm-dep-cascade",
      "emitterFile": "packages/core-app-model/src/DependencyResolver.ts",
      "triggerTypeFile": "packages/core-app-model/src/StoreEventBus.ts",
      "triggerType": "StoreChangeEvent",
      "why": "general-purpose cascade fan-out; carries { tasks, triggerElementId, operation, prevState? }"
    }
  ],
  "catalogFile": "packages/event-bus/src/catalog.ts",
  "declaredFindings": []
}
