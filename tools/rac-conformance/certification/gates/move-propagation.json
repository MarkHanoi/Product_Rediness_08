{
  "//": [
    "NAMED, SHRINK-ONLY ledger for check-move-propagation (GR-12 · C78 §6 · C79 §5).",
    "",
    "PINNED 2026-08-13 at the FIRST HONEST READING — 7 findings over 4 region-derived",
    "families, 7 arms, 4/4 controls proven. The placeholder this replaces declared 0,",
    "which was aspirational: a level nobody had measured. Roadmap §0.1 — a gate lands",
    "at what it measures, and landing RED at a named ledger IS the deliverable.",
    "",
    "⚠ A FIRST ATTEMPT PINNED THIS AT 3 AND WAS WRONG. That run had a failing positive",
    "control (3/4), so four arms never reached a verdict and their findings were",
    "invisible. Pinning 3 would have declared a level BELOW the truth and let four real",
    "defects re-enter silently. The gate caught it by exiting 2 MISCONFIGURED rather",
    "than scoring over an unproven subject — C70 §5.2's floor doing exactly its job.",
    "",
    "WHAT THIS GATE FOUND, and why it outranks most rows in the register:",
    "C79 §6.3 made 11 of 11 region paths CONFORMING AT CREATION on 2026-08-13. The",
    "references are correct and resolvable. Nothing consumes them when a wall moves.",
    "That is C70 §4.2 — machinery present, capability unreachable — sitting on the",
    "product's core promise, and it is the founder's original question answered NO.",
    "",
    "THE SLAB ROOT CAUSE, pinned to the line, and it has a PRECEDENT:",
    "SlabStore.emit fires `bim-slab-{added,updated,removed}` with the F.events.18",
    "payload `{ id }` (event-bus/src/catalog.ts:95-97), while SlabDependencyTracker",
    ":57-67 guards on `e.detail.slab` / `e.detail.slabId`. registerSlab() is therefore",
    "UNREACHABLE from the event path and the dependency graph stays empty; only",
    "bootstrap() (initTools.ts:828, run ONCE at wiring) ever fills it — which PC1",
    "proves works, so the machinery is sound and only the wire is cut.",
    "initBuilders.ts:367-383 was FIXED FOR THIS EXACT SHAPE MISMATCH",
    "(§DOM-EVENT-LISTENER-AUDIT-2026-05-18); the two trackers were not.",
    "SlabWallConnectivityService.ts:115-122 carries the identical defect.",
    "",
    "EXIT CONDITION — this ledger reaches [] when each family's reference is read on a",
    "move and a re-derivation reports one of C79 §5's five states. The slab pair is a",
    "payload-shape fix with a worked precedent; the finishes need a tracker AND a",
    "{x,z}/{x,y} coordinate adapter; roof needs an L0 schema field before anything",
    "else, since Zod strips the reference in transit.",
    "",
    "NOT A BROKEN DETECTOR — control N3 runs the identical scan against the SLAB's",
    "HostReferenceEdge and finds 9 consumer files, so a 0 for the finishes is a real 0."
  ],
  "declaredFindings": [
    "slab·region · a slab created AFTER wiring re-projects when a bounding wall moves",
    "slab·region · the STORED polygon follows the move, not only the drawn mesh",
    "all families · C79 §5.2.1 — `preserved` and `undetermined` are distinguishable at the caller",
    "all families · C79 §5.2 — a re-derivation reports exactly one of the five states",
    "floor-finish · the host reference written at creation is READ by something",
    "ceiling · the host reference written at creation is READ by something",
    "roof·region · the region attribution reaches a reference-capable field on the roof record"
  ]
}
