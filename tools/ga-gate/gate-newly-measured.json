{
  "$comment": [
    "§NEWLY-MEASURED (2026-08-12) — THE THIRD STATE. NOT DEBT. NOT A REGRESSION.",
    "",
    "gate-debt.json declares exactly two states, and BIM 3.0 Phase 1 produces a third",
    "one that fits neither:",
    "",
    "  • REGRESSION      — a gate fails and is NOT on gate-debt.json. Something got",
    "                      WORSE. Blocks.",
    "  • DECLARED DEBT   — a gate is ON gate-debt.json. Somebody CHOSE to ship a known",
    "                      contract violation (rule 3: never added without an explicit",
    "                      founder/architect decision).",
    "  • NEWLY MEASURED  — THIS FILE. A gate that was BUILT THIS WEEK and lands RED on",
    "                      defects that PREDATE it. Nothing got worse; the INSTRUMENT",
    "                      ARRIVED. And nobody chose to ship these — they were never",
    "                      measured, so there was no decision to make.",
    "",
    "Filing a newly-measured gate under either existing label is a lie in a different",
    "direction each way. Calling it a REGRESSION says the tree got worse on the day",
    "someone wrote a scanner, which trains people to read red as noise — that is how",
    "the whole suite ended up unwired in the first place (L-774). Calling it DECLARED",
    "DEBT backdates a decision nobody made and quietly converts 'we have never looked",
    "at this' into 'we accepted this', which is the §CONTEXT-DATA-HONESTY failure this",
    "repository keeps paying for: two very different facts printing the same value.",
    "",
    "─── WHY A SIBLING FILE AND NOT A SECTION INSIDE gate-debt.json ───────────────",
    "Three reasons, in order of weight:",
    "",
    "  1. DIFFERENT CONSENT RULES. gate-debt.json rule 3 attaches a founder decision to",
    "     the ACT OF ADDING A LINE, because adding one there means choosing to ship a",
    "     known violation. This category's entries are the opposite act — choosing to",
    "     START MEASURING. Putting both under one roof means the strongest rule in that",
    "     file (rule 3) either leaks onto entries that should not need it, or gets",
    "     weakened to accommodate them. Rule 3 is left untouched and un-diluted by",
    "     keeping this out of that file entirely.",
    "  2. DIFFERENT SHAPE. gate-debt.json's `failing` is a bare `string[]` — a name and",
    "     nothing else. An entry here is meaningless without its pinned reading, its",
    "     exit condition and its review date. Bolting a structured array beside a flat",
    "     one in the same file gives readers of `failing` a partial view of what the",
    "     suite tolerates, which is exactly the shape of defect §3.20 K2 of",
    "     BIM30-READINESS-GATES names: one list that grows to fit whatever is failing.",
    "  3. TWO LISTS ARE AUDITABLE; ONE MERGED LIST IS NOT. The sizes of these two files",
    "     mean different things and must be readable separately. Declared debt going up",
    "     is bad news. This file going up is, on its face, GOOD news — it means more of",
    "     the estate is instrumented — right up until an entry expires unfixed.",
    "",
    "─── DOES ADDING HERE NEED A FOUNDER DECISION? NO — WITH THREE HARD EDGES ─────",
    "STATED EXPLICITLY BECAUSE THE ANSWER DIFFERS FROM RULE 3 AND MUST NOT BE INFERRED.",
    "",
    "Adding a FIRST READING for a gate does NOT require a founder decision. Requiring",
    "one would put a human approval step in front of TURNING AN INSTRUMENT ON, and the",
    "predictable response to that friction is to not land the gate — which is precisely",
    "what BIM30-READINESS-GATES §2.3 forbids: *deferring a gate until its subject is",
    "fixed is how a subject stays unfixed*. The founder's authority is over what we",
    "SHIP, not over what we are ALLOWED TO KNOW. An entry here asserts no tolerance; it",
    "asserts that a number has been written down and may now only fall.",
    "",
    "The three edges, each of which IS a decision and therefore DOES need the founder:",
    "  (a) AN ENTRY MAY ONLY BE ADDED IN THE COMMIT THAT INTRODUCES ITS GATE. An entry",
    "      added later is not a first reading — the gate ran and something changed — so",
    "      it is either a regression or debt, and it goes through rule 3.",
    "  (b) RAISING A PINNED READING IS FORBIDDEN OUTRIGHT, founder or not. That is R7:",
    "      a measurement raised becomes a permission. The only legal edit to a reading",
    "      is downward.",
    "  (c) EXTENDING A `reviewBy`, OR MOVING AN ENTRY FROM THIS FILE TO gate-debt.json,",
    "      REQUIRES THE FOUNDER. That is the moment 'we have not fixed this yet' turns",
    "      into 'we are choosing to live with this', and it is exactly the decision",
    "      rule 3 exists to gate. This is the join between the two files, and it is",
    "      deliberately the only one.",
    "",
    "─── THE RATCHET LIVES IN THE GATE, NOT IN THIS FILE ─────────────────────────",
    "Every entry below is a gate that reports through the ONE exit-code implementation",
    "at tools/rac-conformance/certification/contract.ts. That implementation already",
    "returns 3 when findings exceed the declared level and 3 when a declared entry is",
    "no longer measured. So:",
    "",
    "    exit 1 → at or below the pin. run-all absorbs it AS NEWLY-MEASURED.",
    "    exit 3 → the pin was exceeded, or the gate's own ledger is stale.",
    "             NEVER ABSORBED HERE. run-all's §RATCHET-EXCEEDED-IS-NEVER-DEBT branch",
    "             fires before this file is consulted, and it exits 1 for the suite.",
    "",
    "This file therefore does NOT re-implement the ratchet, and must not: a second",
    "comparator over the same number is a second thing that can disagree. `pinnedTo`",
    "below is a POINTER to where the authoritative number lives plus a human-readable",
    "copy of it — if the two ever disagree, THE GATE IS RIGHT and this file is stale.",
    "",
    "─── TIME-BOUNDED, AND THE BOUND IS ENFORCED ─────────────────────────────────",
    "Every entry carries `reviewBy`. run-all FAILS when today > reviewBy. That is the",
    "whole point of the field: a category with no exit is how 'temporary' becomes",
    "permanent, and a date nobody enforces is a comment. `reviewBy` is NOT a fix",
    "deadline — it is the date by which the entry must be re-argued: fixed and struck,",
    "or moved to gate-debt.json as an explicit founder decision to live with it. Both",
    "outcomes are honest. Silently sitting here for a year is not.",
    "",
    "─── PAID INSTRUMENT DEBT LEAVES THIS FILE TOO ───────────────────────────────",
    "The gate-debt.json rule-2 discipline applies verbatim: an entry here whose gate",
    "starts PASSING is a FAILURE of this file and run-all says so. Otherwise this rots",
    "into a list of things that are secretly fine, which is the one failure mode both",
    "ledgers share."
  ],
  "createdAt": "2026-08-12",
  "createdBecause": "BIM 3.0 Phase 1 Tier 1 landed three gates RED on pre-existing production defects (BIM30-READINESS-GATES §2.3), and gate-debt.json had no category that described them without lying.",
  "entries": [
    {
      "gate": "check-provenance-not-invented.ts",
      "runner": "ga-gate",
      "addedAt": "2026-08-12",
      "builtBy": "BIM 3.0 Phase 1 Tier 1 · BIM30-READINESS-GATES §3.18 · C75 §2.1/§2.2/§2.3/§2.5",
      "firstReading": "exit 1 DECLARED-LEVEL — 7 findings against a declared level of 7 (V1 ×5, V3 ×1, V5 ×1), 2026-08-12. Real-tree positive control SlabFragmentBuilder.ts reads clean (it refuses rather than substitutes).",
      "pinnedTo": "the `LEDGER` array in tools/ga-gate/check-provenance-not-invented.ts — 7 NAMED file:line entries, checked in both directions.",
      "predatesTheGate": [
        "V1 · roomSnapshotUtils.ts:156 — `|| 'auto-topology'` for an absent detectionMethod. This is C75's own canonical violation, named in the contract before the gate was written.",
        "V3 · RoomDetectionEngine.ts:475 — a repair path that console.debug()s what it did and writes 'auto-topology' to the model. The console is not the model.",
        "V5 · roomSnapshotUtils.ts:156 — an `as any` at the provenance boundary.",
        "V1 · from-pipeline.ts:225, esMurciaAnchoDeCalle.ts:330, ZoningRulesEngine.ts:129, contextBuildingQuery.ts:93 — four further `??` origin fallbacks, none of which any document had enumerated before this run."
      ],
      "exitCondition": "each of the 5 remaining named sites records UNKNOWN-with-reason, or refuses, or is proven not to be a provenance assignment. LEDGER reaches 0 and the gate flips hard-0. V2/V4 are NO LONGER unproven — they are EVALUATED and negative-tested (C75 §6.2) — but they are scoped to the CANONICAL five-value union alone, because §2.2/§2.5 name members (authored/inferred) and an UNKNOWN-with-reason record that the 22 legacy vocabularies do not have. ⚠ ADOPTION OF THAT UNION IS NEAR ZERO, so a clean V2/V4 reading proves only that the new union is not misused — it is NOT evidence that defaults are honest repo-wide. The gate prints this caveat on every run. Of the 5 remaining: 3 (roomSnapshotUtils ×2, RoomDetectionEngine ×1) are C75 §7.1/§7.2 canonical violations left untouched because packages/room-topology was outside this change territory and under concurrent edit; from-pipeline.ts keeps the ?? SHAPE but the HARM is closed (RegisteredFamily now carries optional originProvenance, read via familyOriginProvenance(), which turns absence into UNKNOWN-with-reason) — fully closing it needs FamilyOrigin itself to become nullable, a persisted-enum migration under C05/C47; ZoningRulesEngine keeps its ?? deliberately because DerivationEntry.fieldProvenance is a required C58-owned enum with no not-stated member, and Resolved.provenanceStatedByPack now records whether the pack actually stated it.",
      "territory": "packages/schemas/ (the union + family-registry), packages/site-parcel-data/, apps/editor/src/ui/geospatial/, tools/ga-gate/. packages/room-topology/ was EXCLUDED — concurrent agent.",
      "reviewBy": "2026-11-12",
      "currentReading": "exit 1 DECLARED-LEVEL — 5 findings against a declared level of 5 (V1 ×3, V3 ×1, V5 ×1), 2026-08-12 after C75 Task 1-3. ALL FIVE ARMS NOW EVALUATED: V2 and V4 were implemented once the five-value union landed at packages/schemas/src/provenance/ValueOrigin.ts, and both fire against planted violations in the executed control (5 distinct arms, now a floor). Two ledger entries left because the defect is GONE: esMurciaAnchoDeCalle.ts widthProvenance is now a REQUIRED parameter (C75 §2.8 unrepresentable, zero behaviour change — all 41 call sites already passed it), and contextBuildingQuery.ts now branches on the absent case ahead of the union dispatch. Three line numbers were re-measured in the same change.",
      "stillUnproven": "C75 §6.3 is UNCHANGED by this work and the gate reprints it every run: (a) semantic truth — a path writing OBSERVED while actually computing passes every arm; (b) runtime provenance decided by a branch; (c) the EXPORT boundary (§5) — no IFC/DXF mapping exists, the largest open risk; (d) CROSS-SESSION REGENERATION — §2.7 prior-value chains are not statically verifiable. The union can now EXPRESS a survived regeneration (regeneratedProvenance carries the prior origin, and the schema refuses regenerated without replaced), but NOTHING is wired to write it on a real regeneration pass, so the founder-facing claim re-run the generator and my hand edits survive remains UNPROVEN end-to-end."
    },
    {
      "gate": "check-epsilon-policy.ts",
      "runner": "ga-gate",
      "addedAt": "2026-08-12",
      "builtBy": "BIM 3.0 Phase 1 Tier 1 · BIM30-READINESS-GATES §3.15 · C73 §2.1–§2.5/§5.1",
      "firstReading": "exit 1 DECLARED-LEVEL — 385 findings against a declared level of 385: E1 ×1 (no declared tolerance module in packages/geometry-kernel) + E2 ×271 declarations outside the module (from 279 sites across 164 files, 50 distinct values) + E5 ×113 unit-unqualified names. E4 reads 0 widenings. E3 is NOT EVALUATED while E1 is red, and prints that every run.",
      "pinnedTo": "tools/ga-gate/epsilon-policy-baseline.json — every entry is `file:NAME → value`, never a bare count, so a swap of one epsilon for another cannot read as 'no change'. Shrink-only; `--write-baseline` regenerates it downward only.",
      "predatesTheGate": [
        "There has never been a declared tolerance module in packages/geometry-kernel. Every one of the 271 declarations was written before this gate could see it.",
        "C73 §0.1 states 267 for a recipe it does not fully specify. This gate pins at ITS OWN reproducible reading (271 production declarations by the recipe printed on every run) rather than importing a number it cannot re-derive — a pinned number nobody can reproduce is a number nobody will trust."
      ],
      "exitCondition": "E1 green when packages/geometry-kernel exports the three named roles (numeric-zero dimensionless · model-space coincidence in metres · parallelism/collinearity in rad or dot). E3 begins evaluating the moment E1 goes green — until then the gate's silence on E3 is NOT coverage. The entry is struck when E2 and E5 both reach 0 and the module is the only place a tolerance is declared.",
      "territory": "packages/geometry-kernel/ owns E1; E2/E5 are spread across 164 files in packages/, apps/ and plugins/.",
      "reviewBy": "2027-02-12",
      "reviewByNote": "Longer than its two Tier-1 siblings on purpose, and the difference is argued rather than assumed: they are 4 and 7 NAMED sites, fixable in a sitting. This is 271 declarations across 164 files whose first step is building a module that does not exist. A three-month date on this entry would expire before anyone could honestly act on it, and an expiry nobody can meet teaches people to extend dates rather than to meet them."
    },
    {
      "gate": "check-suppression-is-reversible.ts",
      "runner": "ga-gate",
      "addedAt": "2026-08-12",
      "builtBy": "BIM 3.0 Phase 1 Tier 2 · BIM30-READINESS-GATES §3.11 · C72 §4.1–§4.4/§6.3",
      "firstReading": "exit 1 DECLARED-LEVEL — 45 findings against a declared level of 45: S1 ×1 (clearGraphAuthoritative: 1 definition, 0 production callers, 1 test caller — the spec's positive-control defect, red the day the gate landed as §3.11 requires) + S2 ×3 (initPersistence.ts:362 and :366 pause across ProjectLoader.load with no finally — the spec's named red — plus PlanViewToolOverlay.ts:368, measured not specified) + S3 ×41 (not one suppression flag in the estate declares a scope). Floors met: 4444 files (min 500), 18 production pause() sites across 8 files (PART-A floor ≥15/≥6 — a walk finding ~2 exits 2 as broken, not green), 41+ markers (min 1), controls 1/1.",
      "pinnedTo": "the `LEDGER` array in tools/ga-gate/check-suppression-is-reversible.ts — 45 NAMED entries (S1 by release method, S2 by pause site file:line, S3 by file:field), checked in both directions.",
      "predatesTheGate": [
        "S1 · RoomTopologyObserver._graphAuthoritativeLevels marked from four+ production sites; public release clearGraphAuthoritative has 0 production callers — every generated level keeps its rooms suppressed for the session. Named in C72 §4.1 before this gate existed.",
        "S2 · initPersistence.ts:362/:366 — pause with no finally around a fallible load. Named in C72 §4.2.",
        "S3 · 41 suppression flags, zero scope declarations — C72 §4.4 had no enforcement of any kind until this run."
      ],
      "exitCondition": "S1 and S2 reach hard-0 with no exemptions: clearGraphAuthoritative gains a production caller (or the suppression design is replaced per C72 §4.1), and every pause spanning a fallible call is wrapped try/finally (the gate's own clean-tree control proves the wrapped form reads green). S3's unscoped list reaches empty: each flag gains a @suppression-scope annotation the code honours, or is struck with a recorded argument that it is not propagation suppression. Then the entry is struck and the gate flips hard-0.",
      "territory": "packages/room-topology/, apps/editor/src/engine/, plus the 20+ files named in the S3 ledger — NOT tools/.",
      "reviewBy": "2026-11-12"
    },
    {
      "gate": "check-no-hidden-mock.ts",
      "runner": "ga-gate",
      "addedAt": "2026-08-12",
      "builtBy": "BIM 3.0 Phase 1 Tier 2 · BIM30-READINESS-GATES §3.14 · C74 §3.2/§3.4/§3.5/§6",
      "firstReading": "exit 1 DECLARED-LEVEL — 63 findings against a declared level of 63: M-A ×1 (PlanegcsAdapter declares kind='planegcs' while constructing MockSolver — the positive control, red until CO-01 resolves) + M-B ×60 (scaffold headers with no owner+date+retiring-assertion, of 62 headers found) + M-C ×2 (PlanegcsAdapter.test.ts:67/:94 inject `underlying:`, the field its own docstring forbids production to pass). Honest stand-ins (MockSolver, MockAnthropicRelay, MockVoiceTranscriber — kind='mock') reported present-and-declared, not counted. Floors met: 4518 files (min 500), 62 scaffold headers (min 1), controls 1/1.",
      "pinnedTo": "the `LEDGER` array in tools/ga-gate/check-no-hidden-mock.ts — 15 NAMED entries, checked in both directions. CURRENT READING (2026-08-14, re-measured by running the gate, not copied forward): exit 1 DECLARED-LEVEL, 15 finding(s), at or below the declared level of 15 — all M-B; M-A and M-C are both PAID and armed with an empty ledger, so any new hidden mock or any new injection of a docstring-forbidden field exits 3 as NOT ON THE LEDGER. History: 63 at first reading → 61 after the 2026-08-12 adapter-truthfulness pass (struck M-A, PlanegcsAdapter now declares kind='mock' with a one-time first-call boundary warning, and the M-B PlanegcsAdapter.ts entry; the 2 M-C entries re-pinned to :121/:150) → 29 after the 2026-08-14 CO-03 and CO-06 rounds (M-C paid by DELETING the `underlying?:` seam rather than by disclosure; M-B paid for WallRegionExtractor, 11 command-registry and 16 core-app-model rows) → 15 after CO-06 lane F. ⚠ THIS FIELD READ '61 NAMED entries' UNTIL 2026-08-14 — stale across two payment rounds, exactly the drift §84-85 above says resolves in the GATE's favour; it is a human-readable copy and the array is the authority. Lane F is also why the gate was exit 3 STALE LEDGER when this was corrected: its 14 payments existed only as UNCOMMITTED worktree edits, so the rows could not be struck alone without making the gate green locally and exit 3 in the opposite direction at HEAD — payment and strike landed in one commit, per C70 §5.4.",
      "predatesTheGate": [
        "M-A · PlanegcsAdapter.ts — the mock behind a real name, recorded in C74 §0 before the gate was written.",
        "M-B · 60 scaffold headers, including the S52-vs-S53 disagreement C74 §3.4 names, and the src/ + apps/editor twin copies of familyCreatorPlaceholder.ts (the two-copies anti-pattern wearing a scaffold header).",
        "M-C · PlanegcsAdapter.test.ts binds to the injected double; the `??` fallback every production construction takes is the untested path (C74 §3.5, C74 §0's '31 of 33 passing tests test the mock')."
      ],
      "exitCondition": "M-A green when PlanegcsAdapter's identity matches its work (truthfulness FIRST, in its own commit — C74 §4.3; this gate prefers neither exit). M-B entries leave as each header gains owner+date+retiring-assertion, or the scaffold is retired, or the marker is shown not to be a scaffold (struck with the argument). M-C leaves when the test names what the injection does NOT cover or the production construction is tested. LEDGER reaches 0 → per C74 §6 exit condition 4 the gate flips hard-0.",
      "territory": "packages/constraint-solver/ plus the 60 M-B files across packages/, apps/, src/ — NOT tools/.",
      "reviewBy": "2027-02-12",
      "reviewByNote": "Aligned with epsilon-policy's date rather than the two siblings', for the same argued reason: M-B is 60 files whose fix is a per-file authorship judgement (owner, date, retiring assertion), not a mechanical strike-list. A three-month bound on 60 files teaches date-extension, not compliance. M-A and M-C are 3 entries in one package and should be gone long before the date."
    },
    {
      "gate": "check-secrets-register.ts",
      "runner": "ga-gate",
      "addedAt": "2026-08-12",
      "builtBy": "C77 §6 (minted 2026-08-12, gate UNBUILT at stamp — this commit un-marks it) · BIM30-SECRETS-MAPPING-AUDIT.md §5",
      "firstReading": "exit 1 DECLARED-LEVEL — 12 findings against a declared level of 12, all arm a (env names READ in production sources with no declaration row in tools/ga-gate/secrets-declarations.json). Arm b (build-time-inlined SECRET) reads 0; arm c (sharedWith carried in deploy guidance) reads 0 — SESSION_SECRET and DATABASE_URL both carried. Floors met: 3688 files scanned (min 400), 190 env-read sites (min 40), 86 distinct names (min 25), in-run planted controls 1/1 (arms a/b/c each watched firing on FAKE names; clean fixture 0), value-leak self-check 0 value-shaped strings.",
      "pinnedTo": "the `LEDGER` array in tools/ga-gate/check-secrets-register.ts — 12 NAMED `A::NAME` entries, checked in both directions (an unledgered finding exits 3; a ledgered finding no longer measured exits 3). Declarations were seeded from the audit's EXECUTED inventory only — declaring everything the scan finds would make arm a a tautology, so what the audit never established lands here instead.",
      "predatesTheGate": [
        "All 12 names were read in production sources before C77 existed; none had any owner, classification, requiredness or failure mode anywhere (the audit's §3 ownership matrix reads NO OWNER on every row).",
        "A::PRYZM_PUBLISHER_TOKEN (packages/plugin-sdk/src/dev/publish-command.ts:56) reads like a credential and is the highest-value strike: an undeclared probable-SECRET is the exact class the register exists to catch.",
        "The rest are service ports, bake/CI knobs and deploy env (API_GATEWAY_PORT, BAKE_PORT, MARKETPLACE_PORT, NSHARDS, SHARD, WORKER_CONCURRENCY, OTEL_RESOURCE_ATTRIBUTES, PNPM_HOME, PRYZM_DEV_PRINT_SRCDOC, PRYZM_MARKETPLACE_URL, PRYZM_PUBLISHER_KEY_PATH) — almost certainly CONFIG, but 'almost certainly' is a judgement nobody has recorded, and recording it is the fix."
      ],
      "exitCondition": "each of the 12 names gains an argued declaration row (classification · required · sharedWith · failureMode) in secrets-declarations.json, or its read-site is deleted — struck from the LEDGER in the same commit. PRYZM_PUBLISHER_TOKEN's classification decision comes first. LEDGER reaches 0 and the gate flips hard-0 on arm a. NOTE: §6 arm d (deploy-time presence probe + sharedWith digest comparison) is OPTIONAL, NOT RUN, and stated so on every run — a green reading here asserts declaration and static registration, never prod presence (C77 §0.2).",
      "territory": "tools/ga-gate/secrets-declarations.json (the declarations are judgements — most rows need only a classification argued), plus packages/plugin-sdk/ and apps/api-gateway|bake-worker|marketplace-api/ if any read is to be deleted instead.",
      "reviewBy": "2026-11-12"
    },
    {
      "gate": "../rac-conformance/certification/gates/check-propagation-trackers-reach.ts",
      "runner": "certify",
      "addedAt": "2026-08-12",
      "builtBy": "BIM 3.0 Phase 0R · C72 §6.1.2(d) · BIM30-GAP-REGISTER §10.1 (GATE · UNMAPPED · PR-09)",
      "firstReading": "exit 1 DECLARED-LEVEL — 8 pairs exercised, 5/5 in-run controls proven, 1 finding.",
      "pinnedTo": "tools/rac-conformance/certification/gates/tracker-pairs.json — `declaredFindings`, 1 named entry, with the full measured/negativeControl transcript beside it.",
      "predatesTheGate": [
        "RoomTopologyObserver._computeWallSig (RoomTopologyObserver.ts:566-584) built its no-progress signature from id + baseline X/Z + thickness, omitting height and baseline y — while both dependency trackers treat height and y as geometry-changing. Half the propagation spine calls an edit a move and the other half calls it byte-identical. That divergence has been in the tree since the observer was written; nothing measured it until this gate. PARTIALLY PAID 2026-08-17 (§PR-09-WALLSIG-HEIGHT): the founder ruled canonical HEIGHT in, and it is now folded as integer millimetres (`^<mm>`, the same mm() quantiser as thickness), so the A8 detail line reads `height-only edit changes it: true`. Baseline y is NOT in and the finding therefore stays declared at 1 — see tracker-pairs.json declaredFindingNotes for why y is a separate ruling (WallTypes.ts §WALL-AUDIT-2026-M7 documents baseLine[*].y as a stale world-space mirror of level.elevation; the authored vertical placement is baseOffset, which neither the signature nor _wallGeometryChanged reads)."
      ],
      "exitCondition": "the baseline-y / baseOffset question is DECIDED (the height half was decided by the founder 2026-08-17 and is paid) and _computeWallSig either includes the field or documents why it must not. The decision belongs with the C71 topology gates. declaredFindings reaches 0 and the entry is struck.",
      "territory": "packages/room-topology/src/RoomTopologyObserver.ts:566-584 — another agent's territory. Handoff is recorded in tracker-pairs.json declaredFindingNotes.",
      "runnerNote": "GRADED BY certify.ts, NOT BY run-all. run-all does not execute this gate — it composes real stores and drives real commands, which is why it lives in the certification tree (BIM30-READINESS-GATES §2.1 as amended 2026-08-12). run-all therefore reports this entry as INFORMATIONAL and enforces only its `reviewBy`, because a date is runner-agnostic and a reading is not. run-all must never claim a verdict on a gate it did not run.",
      "reviewBy": "2026-11-12"
    },
    {
      "gate": "../rac-conformance/certification/gates/check-ai-human-parity.ts",
      "runner": "certify",
      "addedAt": "2026-08-12",
      "builtBy": "BIM 3.0 R7 of BIM30-REASONING-LOOP-PLAN.md - G-REASON-04 - ADR-0324 §3 - C75-PROVENANCE",
      "firstReading": "exit 1 DECLARED-LEVEL - 1 finding against a declared level of 1. All 8 floors met: reachability sources 4/4, normalizeForParity imported from production 1/1, REAL wall.updateBaseline handler registered 1/1, human dispatch mutated the store 1/1, AI dispatch mutated the store 1/1, both records carried patches 1/1, positive control (i) behavioural-difference-fails-parity 1/1 (all three arms: metrics, affected set, patch body), positive control (ii) provenance-only-difference-passes-parity 1/1. Clause (A): the two normalized residues are IDENTICAL - the bus is actor-blind for wall.updateBaseline.",
      "pinnedTo": "the `declared: 1` level inside tools/rac-conformance/certification/gates/check-ai-human-parity.ts, whose sole permitted finding is AI-PATH-NOT-INDEPENDENTLY-DISPATCHABLE. Every other clause is hard-0: a parity violation, a misclassified normalizer field, or a dead control is red and stays red. The finding is RE-MEASURED FROM SOURCE on every run (clause iii) rather than trusted from the ledger, so the day the gap closes the gate says so itself.",
      "predatesTheGate": [
        "R7's golden operation has NO AI-side dispatcher. `wall.updateBaseline` is family-blocked from chat at packages/ai-host/src/capabilities/ChatCommandClassification.ts:98 (blockedBy: 'sub-entity reference resolution'), and `wall.move` is refused at packages/ai-host/src/capabilities/ChatCapabilityRegistry.ts:2130 ('Moving a wall from chat needs a target position I cannot infer'). There is therefore no AI caller to compare a human caller against for the verb R7 was scoped to.",
        "NO production call site - human or AI - populates `CommandExecutionContext`. `executeCommand` accepts `opts.context` (CommandBus.ts:331) and carries it onto the EventRecord, but the only constructors of one in the tree are the defensive default at apps/editor/src/engine/consequence/ConsequenceExecutionService.ts:370-374 (`actor: context?.actor ?? { kind: 'system' }`) and two unit tests. Both actors are anonymous at the funnel, so an AI-originated mutation currently lands in the event log indistinguishable from a human one. ADR-0324's audit/authorization story is TYPED but not WIRED.",
        "Consequence for what this gate can claim: the AI CALLER in clause (A) is SIMULATED - the gate supplies the two invocation envelopes the production callers do not. The gate prints this every run rather than passing off two human dispatches as a parity proof."
      ],
      "exitCondition": "the AI path reaches the same executor for the golden operation with a populated invocation envelope - i.e. (1) a chat-safe wall-move verb leaves the ChatCommandClassification block list, AND (2) the chat bridge (apps/editor/src/ui/ai/ZeroTokenChatBridge.ts, whose ChatBus type at :1067 currently has no third argument) threads `{ context: { actor: {kind:'ai'}, origin: 'ai-proposal', approval } }` into executeCommand, with the human tools threading their own. On that day clause (A) is rewritten to drive the REAL AI caller instead of a simulated envelope, `declared` drops to 0, and this entry is struck. Clause (iii) re-measures both halves from source every run and will report when either lands.",
      "territory": "packages/ai-host/src/capabilities/ (chat classification) and apps/editor/src/ui/ai/ZeroTokenChatBridge.ts - NOT R7's territory. R7 reports the request rather than making the change: unblocking a verb from chat is a capability-surface decision (C68), and wiring the envelope at call sites touches the R6 agent's live UI territory.",
      "runnerNote": "GRADED BY certify.ts, NOT BY run-all - it composes real stores and drives real commands through the real bus, which is why it lives in the certification tree (BIM30-READINESS-GATES §2.1 as amended 2026-08-12). run-all reports this entry as INFORMATIONAL and enforces only its `reviewBy`, because a date is runner-agnostic and a reading is not.",
      "reviewBy": "2026-11-12"
    },
    {
      "gate": "../rac-conformance/certification/gates/check-authoritative-state.ts",
      "runner": "certify",
      "addedAt": "2026-08-12",
      "builtBy": "BIM 3.0 Phase 1 Tier 3 · BIM30-READINESS-GATES §3.2 · C70 A-INV-1/A-INV-2/A-INV-3/L-INV-4",
      "firstReading": "exit 1 DECLARED-LEVEL — 6 findings against a declared level of 6 (S3 ×2 duplicate live stores, S1 ×1 no-op-reporting-success, S4-VOICE ×3 swallowed refusals). Floors met: 18 mutating verbs dispatched (min 15), 3 refusal cases (min 3), 18 kinds reached (min 10), 19 pre-mutation records (min 12), 21/21 dispatch outcomes recorded, DTO view reached, 0 registration failures, 2 kind-ownership pairs probed (min 2), and all THREE controls proven in-run (positive 1/1, negative 1/1, widening 1/1). S2 reads 0 DTO-only writes among the 18 measured verbs.",
      "pinnedTo": "tools/rac-conformance/certification/gates/authoritative-state-ledger.json — 6 NAMED entries keyed by arm+case label, each with measured/why/territory/exitCondition, checked in BOTH directions (an unledgered finding exits 3; a ledgered entry no longer measured exits 3 via contract.ts's `stale` channel). Both directions were WATCHED FIRING before the gate was trusted: removing one entry produced exit 3 RATCHET EXCEEDED, adding a bogus entry produced exit 3 STALE LEDGER.",
      "predatesTheGate": [
        "S3 · @pryzm/geometry-door.doorStore and @pryzm/core-app-model.doorStore are DISTINCT LIVE INSTANCES answering for the same `door` kind, holding 1 and 0 records respectively after real command seeding. Identical shape for window. This is the duplicate-store hazard §3.2 names as the gate's core subject, and nothing measured it until this run.",
        "S1 · plugins/slab UpdateSlabsSystemTypeBatch — the command refuses correctly and NAMES its rule ('The slab type catalogue is not available here.') on a CustomEvent; the bridge discards that result and returns an empty HandlerResult, so the bus resolves the dispatch as SUCCESS over a verb that moved nothing.",
        "S4-VOICE · apps/editor/src/engine/initBusHandlers.ts ~:978-982 — wall.updateDimensions aimed at an absent wall console.warn()s and returns; the bus reports success. The console is not the model and it is not the caller. Same mechanism for roof.update and for plugins/rooms room.setName. S4-STATE passes on all three (nothing was mutated) — the defect is exclusively that success and refusal are the same observable at the dispatch site."
      ],
      "exitCondition": "S3 entries are struck when each kind names ONE owner (one singleton deleted or re-exported as the other, so `===` holds). S1 slab is struck when either the catalogue is reachable in a composed runtime or the bridge stops resolving a refused batch as success. The three S4-VOICE entries are struck when their bridges propagate a named refusal to the dispatch site. When `declared` reaches 0 the gate is hard-0 and this entry is struck. NOTE: this gate measures the 18 mutating verbs the harness composes, NOT the register's 110 LIVE rows — it prints that caveat every run, and a clean reading is never repo-wide coverage.",
      "territory": "packages/core-app-model/, packages/geometry-door/, packages/geometry-window/, plugins/slab/, plugins/rooms/, apps/editor/src/engine/initBusHandlers.ts — NOT tools/. Every finding is routed to another lane; the gate lane measures and reports.",
      "runnerNote": "GRADED BY certify.ts, NOT BY run-all. run-all does not execute this gate — it composes real stores, spawns vitest and drives real commands, which is why it lives in the certification tree (BIM30-READINESS-GATES §2.1a: it needs something that does not exist until something runs). run-all therefore reports this entry as INFORMATIONAL and enforces only its `reviewBy`, because a date is runner-agnostic and a reading is not. run-all must never claim a verdict on a gate it did not run.",
      "reviewBy": "2026-11-12"
    },
    {
      "gate": "check-collab-graph-integrity.ts",
      "runner": "ga-gate",
      "addedAt": "2026-08-13",
      "builtBy": "A.13 · audit of a gate that was green in a way nobody should trust · C70 §3.4 + §7 (exit-condition row) + §8.k · C66 §1 · C78 §15.4 · BIM30-READINESS-GATES §0.1 + §3.23",
      "residency": "The gate script is tools/ga-gate/check-collab-graph-integrity.ts; its harness is apps/sync-server/src/collab-gate/collabGraphIntegrity.ts (it needs ws / y-websocket / @pryzm/sync-client, which are that workspace's deps and are not linked into the repo root). The split is a STANDING FINDING under C70 §7.2, not a blessing, and this entry does not resolve it.",
      "firstReading": "exit 1 UNPROVEN. This is a RELABELLING, not a new defect: the gate previously exited 0 and printed a bare tick. NOTHING IT MEASURES CHANGED and no assertion was added, removed or weakened. Floors met and printed on the run: 10 element records compared against a floor of 8, and 4 hosting edges against a floor of 4; transport probe crossed the wire in ~250 ms; built-in negative control ran and flagged 3 violations on a deliberately broken pair (so the comparator is not blind); 5 required verbs all carry a sync disposition; both scenarios INTACT on both documents with the host resolving. So the subject was never empty — this was never the empty-seed shape C70 §0 forbids, and C70 §5.2 was already satisfied (it prints how many objects it compared, with the floors, on every run).",
      "pinnedTo": "Nothing numeric. This entry does not tolerate a count — the RATCHET is still 0 violations and is still NOT on gate-debt.json. What is ledgered is the STATE 'UNPROVEN', which is neither a pass nor a fail (C70 §2.2).",
      "predatesTheGate": [
        "THE DEFECT WAS THE LABEL, NOT THE MEASUREMENT. Exit 0 with a '✓' headline is the channel every consumer reads. C70 §7's exit-condition row for this gate is explicit: 'green AGAINST A REAL TRANSPORT; until then it reports UNPROVEN, never green (§3.4)'. C70 §3.4 exists, in its own words, to stop 'we have no transport' from silently reading as 'collaboration is fine'. The gate carried the narrowness in PROSE beneath a green exit code, and prose beneath a zero is not what a CI runner, a status table, or another gate's ledger reads.",
        "THE FALSE GREEN HAD ALREADY PROPAGATED. Measured 2026-08-13: this very file's check-two-client-convergence entry cited this gate by path as 'owns the transport question and is GREEN', resting one gate's caveat on another gate's mislabelled zero — C70 §8.k reached through a citation rather than a blank cell. That citation was corrected in the same change.",
        "THREE AXES WERE UNREACHED AND UNNAMED. real transport (production runs socket.io last-writer-wins and is untouched); two processes (both peers share one event loop, one heap, one scheduler — C78 §15.4 says the concurrency arm needs two processes and is not measurable in one Node realm); network partition (no latency, reordering, loss or server-side linearisation; the partition was a provider.disconnect(), not a partitioned network). A gate that cannot reach an axis must name it, or its silence reads as coverage (C70 §7.1). All three are now printed as UNPROVEN on every run."
      ],
      "positiveControlNote": "The narrow claim is REAL and is deliberately still reported as proven: two real y-websocket peers, disableBc so they cannot converge via the in-process BroadcastChannel, a genuine disconnect-write-reconnect partition, and a second scenario in which both peers concurrently rewrite the HOST ITSELF and the surviving wallId must still resolve in both documents. This is the only executed evidence in the repo that the hosting edge survives a partition-and-merge over real sockets. The fix does not discard it — it renames it.",
      "negativeControlNote": "Negative-tested 2026-08-13 by planting a violation rather than by reading the code. (1) RELATIONSHIP ARM — scenario 1's B-side concurrent edit (door.setWidth width 1.2) was deleted so it never replicated. VERBATIM: two findings '✗ [concurrent-edit-lost] doc A: door width is 0.9, expected 1.2 (B's concurrent edit)' and the same for doc B, then '✗ RATCHET EXCEEDED — 2 relationship/edit violation(s), tolerated 0.' / 'Collaboration converged the BYTES and lost the MODEL. C8 = FAIL.' EXIT 3 — never 1, so a lost relationship can never be absorbed by this very ledger entry. Note both documents reported the loss, and the hosting-edge observations stayed INTACT: the gate distinguishes a lost EDIT from a lost RELATIONSHIP rather than collapsing them. (2) THE UNPROVEN ARM CANNOT BE FAKED — PRYZM_COLLAB_GATE_URL was pointed at a dead port (ws://127.0.0.1:1) with a fake token. VERBATIM: '✗ MISCONFIGURED [transport-absent] — could not establish a WebSocket session against ws://127.0.0.1:1 within 12000 ms — no transport' and '\"no transport\" is NOT \"converged fine\". C8 cannot be scored from this run.' EXIT 2, with 'compared 0 element records · 0 hosting edges (floors 8 / 4)' printed — an empty comparison announcing its own emptiness against its floors, which is the C70 §0 empty-seed shape being refused out loud. So naming a URL is not a route to exit 0; only a transport that actually accepts the upgrade is. Both plants were reverted and the gate re-run to its ledgered exit 1.",
      "exitCondition": "Exit 0 becomes reachable when a DEPLOYED sync transport exists and the gate is run against it (PRYZM_COLLAB_GATE_URL + PRYZM_COLLAB_GATE_TOKEN) with all arms clean. At that point this entry is STRUCK and the gate returns to a plain hard-0. Until then the correct reading of this row in any status document is UNPROVEN — never a blank, never a zero, never an inherited green (C70 §8.k, BIM30-READINESS-GATES §0.2).",
      "territory": "NOT tools/. The remaining step is deploying a sync transport, which is a FOUNDER DECISION recorded in docs/03-execution/plans/L-391-COLLAB-DEPLOY-DECISION.md, not engineering work in the gate suite. Whoever owns the gate cannot close this by editing the gate — and an earlier attempt to do exactly that is what produced the exit 0 being corrected here.",
      "cannotSee": "production behaviour (socket.io last-writer-wins is untouched by any run of this gate) · anything beyond the hosting edge — rooms, junctions and boundaries are check-collab-model-integrity's and future arms' work · more than two peers · whether a merge that DISCARDS authored state raises a conflict artefact (that is C70 K-INV-2 and belongs to check-collab-model-integrity, which is SPECIFIED-NOT-BUILT and has no file at HEAD — verified absent 2026-08-13).",
      "reviewBy": "2026-11-13"
    },
    {
      "gate": "../rac-conformance/certification/gates/check-two-client-convergence.ts",
      "runner": "certify",
      "addedAt": "2026-08-12",
      "builtBy": "BIM 2.0 certification · the COLLABORATION row · C08 · C66 §1 · C03 §4.5-4.8 · BIM20-ACCEPTANCE-10-OF-10 §1 C8",
      "firstReading": "exit 3 RATCHET EXCEEDED — 4 findings against a hard-0. Floors ALL met: 2/2 client worlds composed; client A 1 mutation and client B 1 mutation replicated from their OWN buses (counted separately, so one client cannot cover the other); 8 update crossings transported; 3 properties compared; negative control fired (a forced divergence read DIVERGED and two identical values read agree, so the comparator is neither blind nor always-red); positive store-isolation control fired (WallStore, CommandBus, CommandManager and Y.Doc all distinct across the two clients). Determinism: two consecutive runs both exit 3 with the same 4 finding kinds.",
      "pinnedTo": "tools/rac-conformance/certification/two-client-ledger.json — maxFindings, shrink-only. The gate reads it; it is NOT hard-coded in the script, so lowering it is a visible reviewable act.",
      "predatesTheGate": [
        "F-C1 · RIVAL RECORD MINT loses properties SILENTLY. packages/sync-client/src/YjsDocAdapter.ts:690-700 (_applyDeclaredProperties) and :598-602 (applyCommand) both do get-or-create — read the element record, and if it is absent construct a new Y.Map and set it into the elements map under that id. Two PARTITIONED clients that each first-touch the same element therefore each mint a RIVAL Y.Map into the same key; on merge Yjs keeps ONE container and discards the other WITH EVERY PROPERTY INSIDE IT. Reproduced in ~20 lines with no harness involved. CONTROL proves it is the rival mint and not concurrency as such: the identical two edits on a record created once and replicated BEFORE the partition keep every value. P8-relevant: the loss emits NO CRDTConflict, because _discloseOverwrittenLocalWrites compares property VALUES, not rival CONTAINERS — so a property nobody concurrently edited disappears with no conflict raised anywhere.",
        "F-C2 · UNDO CROSSES THE USER BOUNDARY. After client B's materialColor edit had been applied into client A's store, client A undoing ITS OWN height gesture reverted B's colour on A ('#c0ffee' before A's undo, '#aabbcc' after). C03 §4.5-4.8 makes undo per-gesture; one user's Ctrl+Z must never revert another user's work. The remote edit is applied through the bus as element.updateParameters, which the legacy CommandManager stack treats as ordinary local history.",
        "F-C3 · A's undo did not revert A's own gesture in the same run (height stayed 5), so the undo stack is wrong in BOTH directions under concurrency, not merely over-broad.",
        "F-C4 · read-leg-did-not-reach-store: after sync both Y.Docs agree on a value that at least one client's authoritative WallStore does not hold. Attributed by the gate: when the documents agree and the stores do not, the defect is in the read leg (ElementSyncReader → sink → bus), not in the merge."
      ],
      "exitCondition": "F-C1 green when a concurrent first-touch of the same element cannot destroy a property nobody edited — either by reconciling rival containers, or by refusing to mint a second one, or (P8) by emitting a CRDTConflict so the loss is at least DISCLOSED rather than silent. F-C2/F-C3 green when a remote-applied change does not enter the local undo history as a local gesture, so that A's undo reverts A's gesture and only A's. F-C4 green when a merged document value reaches the authoritative store on both clients. When maxFindings reaches 0 the gate is hard-0 and this entry is struck.",
      "territory": "packages/sync-client/ (YjsDocAdapter, ElementSyncReader), apps/editor/src/engine/initRemoteElementSync.ts, and the undo owners in packages/command-registry + packages/command-bus — NOT tools/. This cannot be fixed by whoever owns the gate suite.",
      "caveat": "THE WIRE IS SIMULATED. Updates cross by Y.applyUpdate(encodeStateAsUpdate), not a WebSocket; latency, reordering, loss and server linearisation are NOT MEASURED and are printed as such on every run. Real LOOPBACK sockets are covered by tools/ga-gate/check-collab-graph-integrity.ts, which owns the transport question and is UNPROVEN (exit 1) — the two gates measure different legs and neither subsumes the other. CORRECTED 2026-08-13 (A.13): this line previously said that gate 'is GREEN'. It was reading a mislabelled exit 0. That gate had never touched a deployed transport, and C70 §7's exit-condition row for it requires it to report UNPROVEN, never green, until it does; it now exits 1 UNPROVEN on every local run. Citing one gate's zero from another gate's ledger is how C70 §8.k is reached through a citation rather than through a blank cell — so this caveat may not be read as 'the sockets leg is covered'. C66 §1: no capacity tier moves CLAIMED → HELD on this evidence, and C8 remains FAIL — BLOCKED on the founder decision in docs/03-execution/plans/L-391-COLLAB-DEPLOY-DECISION.md.",
      "reviewBy": "2026-11-12"
    },
    {
      "gate": "check-constraint-honesty.ts",
      "runner": "ga-gate",
      "addedAt": "2026-08-12",
      "builtBy": "BIM 3.0 · BIM30-READINESS-GATES §3.12 · C70 §7 G-INV-1 + G-INV-2 · C74 §3.1/§3.2/§3.5 · §2.2",
      "residency": "tools/ga-gate/ per BIM30-READINESS-GATES §2.1a. The boundary test — does this gate need something that does not exist until something runs? — answers NO for both arms. G-INV-1 reads adapter delegation against declared kind; G-INV-2 parses rule families out of ConstraintEngine.ts source and cross-references 1974 executable-evidence files. Importing constraintEngine to drive it would be EXECUTED (the engine touches window at module scope, ConstraintEngine.ts:110) AND would answer the wrong question: G-INV-2 asks whether evidence EXISTS in the repository, not what one run computes. A gate that ran the engine itself would BE the evidence.",
      "firstReading": "exit 1 DECLARED-LEVEL — 21 findings against a declared level of 21. Floors ALL met: 4 adapters discovered (min 1, the C70 §7 floor), 20 constraint families enumerated (min 1), 4506 source files scanned (min 500), 1974 executable-evidence files read (min 100), 1/1 executed controls proven. G-INV-1 reads CLEAN against production (0 findings) — the PlanegcsAdapter truthfulness fix landed earlier the same day — and that clean reading is a verdict ONLY because the negative control fires on a planted dishonest adapter in the SAME run. G-INV-2 is where the 21 live: 18 of 20 families have no executable evidence at their declared strength.",
      "pinnedTo": "the LEDGER array in tools/ga-gate/check-constraint-honesty.ts — 21 NAMED entries checked in BOTH directions (an unledgered finding exits 3; a ledgered finding no longer measured exits 3). 18×H3 + 1×H4 + 1×H5, with H5 double-counted through its H3 sibling because StairValidationAuthority is both unevidenced and duplicated.",
      "predatesTheGate": [
        "H3 · packages/constraint-solver/src/ConstraintEngine.ts registers 17 rule families (tiers 1-2, severities error/warning/info) and packages/constraint-solver/ contains exactly TWO test files — engine.test.ts and PlanegcsAdapter.test.ts — BOTH of which bind the MockSolver/adapter layer. NEITHER opens ConstraintEngine.ts. The component doing the repo only real ADVISORY constraint work, wired to a live event bus at apps/editor/src/engine/initDataPlatform.ts:298, has no suite at all.",
        "H3 · ROOM_MIN_AREA is the ONLY family any executable file names, and all THREE files that name it SYNTHESISE the violation rather than calling the engine: apps/editor/__tests__/WallMoveRoomAreaPrediction.test.ts:40 declares itself \"a minimal stand-in for the real ROOM_MIN_AREA rule\"; tools/rac-conformance/certification/gates/check-approval-binding.ts:386 hand-writes the violation into a SCRIPTED validator and says so at :379; apps/editor/src/ui/consequence/confirmationPolicy.ts:152 only references it in prose. A rule with a string that matches its name is not a rule with evidence.",
        "H3 · packages/geometry-stair/src/StairValidationAuthority.ts (VALIDATION, C74 §2) — zero executable files name it.",
        "H3 · annotationConstraints (VALIDATION, C74 §2) — the ONE persisted constraint family in the system, written to the snapshot and read back, and zero executable files name it.",
        "H5 · StairValidationAuthority exists in packages/geometry-stair/src AND packages/constraint-solver/src; production imports the geometry-stair copy, the constraint-solver copy has zero production importers (C74 §2.2).",
        "H4 · packages/constraint-solver/__tests__/PlanegcsAdapter.test.ts injects `underlying` without stating which production configuration is thereby not covered (C74 §3.5) — recorded in C74 §0 before this gate existed."
      ],
      "positiveControlNote": "WallOccupancyStore.canPlace reads REAL (ENFORCEMENT, witnessed at packages/geometry-wall/__tests__/CurvedWallOpeningCarve.test.ts:344) and is the H3 POSITIVE CONTROL against the production tree. If it ever joins the ledger the ARM is broken, not the estate, and the fix is the gate. Both synthetic controls also run every invocation: a planted dishonest adapter must fire H1+H2, a planted unevidenced family must fire H3, a planted HAND-BUILT STAND-IN must classify STAND-IN rather than REAL, and a clean tree must read 0. Any of these failing exits 2 as a BLIND COMPARATOR.",
      "exitCondition": "Per family: the H3 entry is struck when an executable test, spec or certification gate exercises that family THROUGH ConstraintEngine (or its owning authority) rather than synthesising a violation object with the right ruleId — i.e. the witness calls the real path. H4 is struck when PlanegcsAdapter.test.ts states which production configuration its injected double leaves uncovered (C74 §3.5). H5 is struck when StairValidationAuthority has ONE owner (C74 §2.2, C74 §7.5). When LEDGER reaches 0 the gate is hard-0 and this entry is struck. NOTE: G-INV-1 is already at 0 and is NOT pinned — a regression there exits 3 immediately, it cannot hide inside this entry.",
      "territory": "packages/constraint-solver/, packages/geometry-stair/, packages/schemas/, apps/editor/__tests__/ — NOT tools/. This cannot be fixed by whoever owns the gate suite. Every finding is a MISSING TEST or a DUPLICATED AUTHORITY in product territory.",
      "cannotSee": "runtime DI substitution · semantic correctness (an honest rule computing the wrong answer passes every arm — C74 governs IDENTITY, not ACCURACY) · whether a family truly NEEDS solving (C74 §4.2 is a written judgement, and C74 §4.5 stands: no solver is authorised) · whether the evidence PASSES (a red test is evidence; a missing one is not) · families outside a register({id,tier,severity}) shape, which come from a NAMED list printed in full every run so its rot is visible.",
      "reviewBy": "2026-11-12",
      "notImplementedHere": "BIM30-READINESS-GATES §3.12 also specifies H4 (a suite substituting a double states which production configuration is thereby NOT covered, C74 §3.5). It was BUILT, FIRED, and REMOVED before this pin: check-no-hidden-mock M-C already owns that exact defect at finer resolution (keyed per injection site, PlanegcsAdapter.test.ts:121 and :150, both on ITS ledger). A finding carried by two gates is a finding whose fix strikes one ledger and leaves the other STALE — exit 3 from the gate that did not get the memo. Named here rather than silently dropped: a spec row this gate does not implement is a fact a reader is entitled to. H4 is COVERED, ELSEWHERE."
    },
    {
      "gate": "../rac-conformance/certification/gates/check-authored-state-protection.ts",
      "runner": "certify",
      "addedAt": "2026-08-12",
      "builtBy": "BIM 3.0 · BIM30-REASONING-LOOP-PLAN.md R8 · STR-06 §14-15 · C75 §1-§2/§2.7 · BIM30-CONTRACT-REVIEW-PART-E §6 finding E-13",
      "residency": "tools/rac-conformance/certification/gates/ per BIM30-READINESS-GATES §2.1a. The boundary test — does this gate need something that does not exist until something runs? — answers YES. Three of its five clauses drive live runtime state: the authority question is asked over rooms held in a REAL RoomStore, and the load-bearing clause DISPATCHES the real room.delete through the real CommandBus into the real plugins/rooms handler and the real window.commandManager bridge, then reads the store back to see what survived. A source scan cannot see a deletion; C75 §6.3(d) concedes exactly this about its own three static provenance gates, which is why E-13 exists.",
      "firstReading": "exit 1 DECLARED-LEVEL — 7 findings against a declared level of 7. ALL TWELVE FLOORS MET, including four executed controls: the authority function returns three DISTINCT answers rather than one constant; the refusable clear does NOT refuse an all-system-produced set; a declared prior run yields determined and finds the survivor; and the clear loop actually removed rooms (an arm that deleted nothing would prove nothing about protection). The load-bearing measurement is clause (b): a level holding one AUTHORED room (detectionMethod=manual-boundary, what RoomTool stamps when a human draws) and one flood-filled room was put through the live §GRAPH-CLEAR-FIRST pattern — seeded=2, remaining=0, the authored room survived=FALSE. R8 names this defect and the gate reproduces it rather than describing it.",
      "pinnedTo": "the `declared: 7` constant in the gate, with all seven findings NAMED in findingNames on every run. Checked in both directions by contract.ts: an eighth finding exits 3, and dropping to six without striking this entry also exits 3.",
      "predatesTheGate": [
        "R8-a · PV-01 IS LIVE. `auto-topology` is simultaneously a real detection method (RoomDetectionEngine.ts:499) and the `|| auto-topology` deserialisation default at packages/room-topology/src/roomSnapshotUtils.ts:156, where an `as any` defeats the union at exactly the point that would have caught it. A room reading auto-topology is therefore ambiguous between flood-filled and origin-not-recorded, and no consumer can separate them. C75 §2.5 / PV-01.",
        "R8-b · THE HOUSE EXECUTOR DESTROYS AUTHORED ROOMS, MEASURED. apps/editor/src/ui/house-layout/HouseLayoutExecutor.ts:1757-1766 (§GRAPH-CLEAR-FIRST) reads roomStore.getByLevel(levelId) UNFILTERED and dispatches room.delete for every id. Nothing consults provenance; the block comment asserts the rooms are \"pre-existing (detection)\" artefacts and cannot know that. The deletes are fire-and-forget (void, never awaited before BatchCreateRoomsCommand on the next line), the failure path is an empty catch, and the user is never told. A room the user drew is deleted silently. R8 exit condition, verbatim: \"the house executor’s silent room deletion becomes a reported, refusable consequence.\"",
        "R8-d · ALREADY-GENERATED AWARENESS IS NOT PERSISTED. generationId / generatedBy / isGenerated / sourceGenerator return ZERO first-party hits across packages/, plugins/ and apps/ (measured 2026-08-12). Generators carry only a command-level `source` label (HOUSE_GRAPH_ROOMS et al.) passed to cm.execute for undo/telemetry; it is never persisted onto the created elements, so nothing downstream can query which elements a generator produced. Consequence: HouseLayoutExecutor mints fresh levels per run with Date.now()+Math.random() ids and clears only ROOMS — walls, doors, windows, slabs, stairs, roofs, furniture and lighting from a previous run are never removed, so a second run stacks a whole building on the first.",
        "R8-e1 · NOT EVALUATED — authored wall byte-intact. No wall carries provenance; every `origin:` in packages/schemas/src/elements/*.ts is a geometric Vec3 (C75 §0 Finding 2, re-measured and still true). An authored wall is indistinguishable from a generated one at HEAD, so the verification has no subject.",
        "R8-e2 · NOT EVALUATED — authored opening byte-intact / generated opening revalidated. Doors and windows carry no provenance either, and no regeneration pass over openings exists to run.",
        "R8-e3 · NOT EVALUATED — generated wall UPDATED by the regeneration. There is NO *.regenerate verb in the repository. Only GENERATE_STAIR_GEOMETRY (one stair from its params) and ai.floorplan.generate exist; neither regenerates an area over mixed provenance. The house/apartment generators are not bus verbs at all — they are UI controllers (houseLayoutTrigger.ts / HouseLayoutController.ts), which is precisely why no verb can enforce clear-then-rebuild centrally.",
        "R8-e4 · NOT EVALUATED — the AI-proposed element still marked INFERRED. No AI-proposed element persists an origin; ai.floorplan.generate stamps nothing on what it creates. Compounded by R7’s own declared finding that no production call site builds a CommandExecutionContext at all."
      ],
      "positiveControlNote": "FOUR controls run every invocation and are FLOORS, not prose. (1) DISCRIMINATION — mayRegenerate must return three DISTINCT kinds across an authored, a system-produced and a provenance-less element; a function hard-wired to any single answer would satisfy one assertion and is caught here. (2) NEGATIVE — planRegenerationClear over two system-produced rooms must NOT refuse, so refuse=true in the main arm is a decision rather than a constant. (3) UNDETERMINED-IS-A-READING — queryPriorGeneration with a declared run must return determined and correctly split stillPresent from gone, so the undetermined verdict in the no-memory case is not a stub. (4) TEETH — the clear loop must actually have removed rooms from the real store; an arm where nothing was deleted cannot testify about what survived, and exits 2 rather than reporting protection.",
      "exitCondition": "Per finding. R8-a is struck when roomSnapshotUtils.ts:156 records UNKNOWN-with-reason instead of the `|| auto-topology` default and the serialised field is typed RoomDetectionMethod rather than bare string (roadmap Phase 8 change 2). R8-b is struck when §GRAPH-CLEAR-FIRST consults planRegenerationClear (or an equivalent authority check) before deleting, surfaces the refusal to the user, and this gate’s clause (b) measures the authored room SURVIVING — at which point clause (b) flips from a finding to a floor. R8-d is struck when elements carry a persisted generation marker and queryPriorGeneration reads it rather than a caller’s in-process declaration. R8-e1..e4 are struck one at a time as their substrate lands (Phase 8 element field; a *.regenerate bus verb), and each strike REPLACES a NOT-EVALUATED line with a real executed arm — the NOT_EVALUATED floor of 4 must be LOWERED deliberately in the same commit, never left to drift. When all seven are struck the gate is hard-0 and this entry leaves the ledger.",
      "territory": "The gate and apps/editor/src/engine/provenance/ElementProvenanceIndex.ts are new files. The gate IMPORTS but never edits packages/schemas/src/provenance (the L0 vocabulary), packages/room-topology (RoomStore, RoomTypes) and plugins/rooms (DeleteRoomHandler) — the landed precedent of check-approval-binding importing plugins/wall read-only.",
      "cannotSee": "FIVE named blind spots. (1) The §GRAPH-CLEAR-FIRST loop in clause (b) is TRANSCRIBED from HouseLayoutExecutor.ts:1757-1766, not imported — the executor is a 254 KB UI class requiring a full runtime, and it was another agent’s live territory when this gate landed. The gate therefore proves the PATTERN destroys authored rooms, not that this exact executor instance does; this is its weakest link and is stated in the gate header. (2) Provenance for walls, doors, windows, slabs and every non-room element — there is no field to read, which is finding R8-e1/e2. (3) Whether a REGENERATION preserves prior provenance (C75 §2.7 replaced chain) — no regeneration verb exists to run. (4) The export boundary: how AUTHORED/OBSERVED/COMPUTED/INFERRED/REGENERATED land in IFC and DXF is C75 §5 / PV-04, UNPROVEN and untouched here. (5) The BROWSER half — whether a human running the house generator in a live browser sees a refusal, since the refusable form is not yet wired into the executor.",
      "reviewBy": "2026-11-12",
      "notImplementedHere": "This gate does NOT claim review §6’s six-element scenario passes. THREE of its seven verifications are executed (an authored element is protected, a system-produced one is clearable, and a report names which and why); FOUR are printed as NOT EVALUATED with the reason and the substrate that would close each. E-13 asked for the scenario to be \"named now, RED, per the roadmap’s own §0.1 rule (a gate lands before its implementation, and lands RED)\" — that is exactly what this is. Reading its exit code as scenario coverage would be the C70 §8.i capability claim the whole suite exists to prevent."
    },
    {
      "gate": "check-graph-write-coverage.ts",
      "runner": "ga-gate",
      "addedAt": "2026-08-12",
      "builtBy": "BIM 3.0 · C71 §6 (which OWNS and SPECIFIES this gate; C70 defers to it) · C70 §7 row C-INV-1 + C-INV-4 · ADR-0320 · ADR-0321 · BIM30-READINESS-GATES §2.1a/§2.2/§2.3",
      "residency": "tools/ga-gate/ per BIM30-READINESS-GATES §2.1a — a DELIBERATE, STATED DEVIATION from C71 §6's preamble, which places all three graph gates in tools/rac-conformance/certification/gates/. §2.1a is the later and more specific rule and replaces suite-affinity with one mechanical boundary test: does this gate need something that does not exist until something runs? For this gate the answer is NO. Its entire subject — does every REQUIRED type have a writer/reader/rebuild/delete path — is answerable from the tree at HEAD by a single comment-stripped pass over 4569 files. It composes no runtime, seeds no world, dispatches no verb, and grades no artefact written in the same invocation. §2.1a's own worked precedent agrees: check-solver-is-real, check-provenance-not-invented and check-epsilon-policy are recorded there as CORRECTLY PLACED in tools/ga-gate/ as static single-pass scans. §2.1b keeps the deviation safe and is what C70 §7.2 actually objected to — this gate hand-rolls no exit code and imports the one contract.ts implementation. If C71 §6 is later amended to bind on address regardless of runner, MOVE THE FILE and re-register the path; nothing else in the gate depends on its address.",
      "firstReading": "exit 1 DECLARED-LEVEL — 5 findings against a declared level of 5. Floors ALL met: 26 RelationshipType members parsed from source (min 20 — C70 §7's own floor for this row is >0, and 20 is STRICTER not laxer), 9/9 REQUIRED families located in the parsed union (min 9), 4569 production source files scanned (min 1500), 1 rebuild implementation located (min 1), 6/6 executed controls proven (min 5 declared). Per-type matrix as measured (W=writers/R=typed readers/rebuild disposition/delete): hosts W1 R2 rebuilt cascade · hostedBy W2 R0 rebuilt cascade · boundedBy W2 R1 rebuilt cascade · adjacentTo W5 R3 rebuilt cascade · connectedTo W6 R2 rebuilt cascade · sitsOn W18 R0 rebuilt cascade · supports W8 R2 rebuilt cascade · contains W0 R2 named-unreconstructable cascade · partOf W0 R0 rebuilt cascade · joinedTo W1 R1 REGENERATED type-aware. PARKED twelve: 0 have acquired a writer — the writer-first ratchet (ADR-0320 para 3 / C71 §2.5) is GREEN at first reading, recorded because it is the one arm that could have been red for free.",
      "pinnedTo": "the LEDGER array in tools/ga-gate/check-graph-write-coverage.ts — 5 NAMED entries keyed family/obligation, never a bare count (C70 §5.5), checked in BOTH directions: an unledgered finding exits 3, and a ledger entry no longer measured is STALE and also exits 3 (C70 §5.4). The four obligations are scored and reported SEPARATELY per type and never aggregated into a coverage percentage — a type with a writer and no reader, a type with no rebuild coverage, and a type whose delete path strands its edges are three different defects, and one number hides all three.",
      "predatesTheGate": [
        "hostedBy/reader · packages/command-registry/src/walls/CreateWallOpeningCommand.ts:239 writes it on every opening creation and packages/persistence-client/src/loader/rebuildSemanticGraph.ts:122 rebuilds it, but NO production path typed-reads it. The inverse half of the reference-shape pair is write-only state. C71 §2.1 row 1 requires the PAIR.",
        "sitsOn/reader · 18 writer sites across every element kind (CreateBeamCommand.ts:221, CreateSlabCommand.ts:163, CreateColumnCommand.ts:187, CreateRoofCommand.ts:237, CreateStairCommand.ts:397, CreateFurnitureCommand.ts:202, CreateHandrailCommand.ts:112, CreatePlumbingFixtureCommand.ts:103, CreateLightingCommand.ts:133, CopyElementCommand.ts:162 and the delete-undo restores) against ZERO typed readers. THIS IS THE sitsOn DEFECT ADR-0320 WAS WRITTEN ABOUT (C71 §0, EV-05 §1): a writer census alone graded it healthy. apps/editor/src/engine/initDependencyCascade.ts:59 filters task.relationshipType !== 'sitsOn' — that reads a DERIVED scheduler task, not the graph, and is correctly refused.",
        "contains/writer · C71 §2.1 #7 names this gap in the contract itself. Two production readers ask — apps/editor/src/ui/dataworkbench/HierarchyTreePanel.ts:541 and packages/ai-host/src/WorldModelAdapter.ts:152 — and no first-party writer answers. MEASURED WORSE THAN THE CONTRACT RECORDS — see F-1 in findingsRoutedElsewhere: the IFC-import path both C71 §0 and rebuildSemanticGraph.ts:211-214 credit as its writer does not actually emit the edge.",
        "partOf/writer · the ONLY emitter is the loader's reconstruction at rebuildSemanticGraph.ts:165 from the authoritative room.unitId field. No LIVE command writes it, so a room assigned to a unit in-session carries no edge until the project is reloaded — the graph and the authoritative field disagree for the whole session.",
        "partOf/reader · no production path typed-reads it either. Both halves are gaps and are ledgered separately, so paying one cannot mask the other."
      ],
      "findingsRoutedElsewhere": [
        "F-1 (NEW — not previously recorded) · packages/file-format/src/import/ifc/IfcImporter.ts:100 vs :490-491 — the 'contains' arm of ImportedRelationship is UNREACHABLE. :100 declares type: 'adjacentTo' | 'boundedBy' | 'contains'; the single push at :491 uses relType from the ternary at :490, which can only ever produce 'adjacentTo' or 'boundedBy'. Both C71 §0 and rebuildSemanticGraph.ts:211-214 state that contains is written by IFC import; measured at HEAD it is written by nothing, on any path, native or imported. TERRITORY: packages/file-format/ — NOT the gate suite.",
        "F-2 · C71 §2 classifies 21 of the union's 26 members. connectedByStair (5 writers, 0 readers), connectedByLift (3 writers, 0 readers), measuredAt (1 writer, 1 reader) and decidedBy (1 writer, 0 readers) are in NEITHER §2.1 nor §2.2. The gate reports them in an UNCLASSIFIED table and does NOT count them as findings — a gate inventing a classification the contract declined to make is inventing vocabulary. But connectedByStair and connectedByLift are writer-without-reader in exactly the sitsOn shape, so the classification gap is load-bearing rather than cosmetic. TERRITORY: C71 §2 — a contract amendment, not a code change.",
        "F-3 · packages/ai-host/src/graph/GraphQueryService.ts:72-86 SUPPORTED_RELATIONSHIP_TYPES is a 14-member allowlist gating ONE dynamic read (getTargets(elementId, relationshipType as RelationshipType) at :239, where relationshipType is a string from a command payload). It is NOT 14 typed readers, and this gate refuses to count it (C71 §1.3). Counting it would hand reader credit to hostedBy, sitsOn, partOf, unitOf, levelOf, connectedByStair and connectedByLift on the strength of one generic call site — the exact 'a name found by grep is not a wired capability' error of C71 §0. Reported per-type as ALLOWLIST-ONLY so the exclusion is visible rather than silent."
      ],
      "exitCondition": "Per C71 §6: when the LEDGER reaches 0 entries the gate becomes hard-0 and this entry is struck. Per entry: hostedBy/reader green when some production path typed-reads opening→wall; sitsOn/reader green when dependency scheduling reads the edge it is handed rather than a derived task; contains/writer green when a first-party writer exists (F-1 must be resolved first — the IFC path it is currently credited to does not run); partOf/writer green when a live command emits the edge at unit-assignment time; partOf/reader green when a production path typed-reads it. NOTE: the gate CANNOT see move-time invalidation (C71 §1.2 semantic 5, §6.2c) — that remains UNPROVEN for every family and is NOT covered by this exit condition.",
      "territory": "packages/command-registry/, packages/ai-host/, packages/file-format/, packages/persistence-client/, apps/editor/ — NOT tools/. All 5 ledgered findings and all 3 routed findings are defects in PRODUCT code that the gate measures; none can be fixed by whoever owns the gate suite. This gate was authored in a MEASUREMENT-ONLY lane and edited no packages/ or apps/ source.",
      "reviewBy": "2026-11-12"
    },
    {
      "gate": "../rac-conformance/certification/gates/check-derived-classification.ts",
      "runner": "certify",
      "addedAt": "2026-08-12",
      "builtBy": "BIM 3.0 · C70 §7 row `check-derived-classification` (B-INV-2, I-INV-1) · ADR-0319 (THE specification) · BIM30-READINESS-GATES §3.20 (K1-K4 + both named controls) · §2.1a residency",
      "residency": "tools/rac-conformance/certification/gates/, registered in certify.ts. §2.1a boundary test — \"does this gate need something that does not exist until something runs?\" — answers YES on both of its subjects. (1) K1 grades results/persistence.json `excludedByKind`: the set of field paths the comparator ACTUALLY dropped on a real seed -> serialize -> reload. No source scan can produce that set; it exists only after the suite runs, and a stale copy is caught by a freshness floor against capture.ts mtime. (2) K1b enumerates the field universe by INSTANTIATING the live Zod registry (Schema.parse({}), with a declared-.shape fallback for schemas that legitimately refuse an empty default), not by grepping for field names. Per §2.1b it imports the ONE exit-code implementation from ../contract.js and hand-rolls no codes.",
      "firstReading": "exit 1 DECLARED-LEVEL — 1 finding against a declared level of 1; honest reading \"0 NEW + 1 DECLARED\", printed on the VERDICT ARITHMETIC line of every run so the headline never reads cleaner than the estate. Floors met: persistence artefact present (min 1); classification-ledger present (min 1); capture.ts ADR-0319 predicates loaded as EXECUTABLE VALUES (min 1); SCHEMA_REGISTRY loaded (min 1); 27 element kinds enumerated from the live registry (min 10); 482 persisted field paths enumerated (min 100); 4 fields classified — the C70 §7 floor \"fields classified >= minimum\" (min 4); artefact not older than capture.ts (min 1); 16 exclusion decisions actually taken by the comparator, the K1 subject (min 1). All FOUR controls fired: CONTROL+ a classified+enforced field reads clean; CONTROL-a a planted unclassified field is FLAGGED; CONTROL-b merging the two lists turns K2 red; CONTROL-c the ADR’s own worked example metadata.modifiedAtBy is matched by a PREFIX rule and correctly NOT matched by the enumerated matcher.",
      "answersTheQuestionItWasBuiltFor": "THE PERSISTENCE 0 IS FIDELITY, NOT AN ARTEFACT OF EXCLUSION — measured, not assumed. cert-ratchet.json candidly notes persistence went 5 -> 0 partly by ADOPTING the ADR-0319 exclusions, which leaves open whether a field was excluded from comparison WITHOUT being classified. Measured: the comparator took exactly 16 exclusion decisions across 17 compared kinds, spanning 4 distinct field paths (metadata.createdAt x5, metadata.modifiedAt x6, metadata.version x3, _renderVersion x2). ALL 4 carry an ADR-0319 class ENFORCED by an exported predicate over an enumerated list; ZERO excluded-but-unclassified paths. Unexcluded divergences across all kinds: 0. So 0 of the 18 rows were rescued by an unclassified exclusion, and 5 of the 18 (wall, roof, ceiling, floor, room) were rescued by a CLASSIFIED one. The exclusions did not widen: a class-1 tamper (height) still goes red WITH the exclusions active, and the undo comparator still fails on any class-2 counter.",
      "predatesTheGate": [
        "metadata.createdBy is declared on the SHARED Metadata primitive at packages/schemas/src/base/primitives.ts:43 (createdBy: z.string().default('system')), so ALL 27 element kinds carry it. It is actively written with meaningful DIFFERING values — 'ai' at packages/command-registry/src/ai-vg/ViewAuthoringIntentMapper.ts:120 and :323, 'user' at packages/command-registry/src/catalog/AddAssetCatalogEntryCommand.ts:85, command names at packages/command-registry/src/beam/CreateBeamCommand.ts:225 — so it is neither dead nor constant. ADR-0319 names it in NONE of its three classes: it is the ONLY member of the Metadata block no class mentions. That is a GAP IN THE GOVERNANCE ACT, not a defect in the comparator, and it predates this gate by the whole life of the ADR.",
        "CLASS 1 IS NOT ENFORCED ANYWHERE. ADR-0319 §1 makes AUTHORITATIVE the default for every authored geometric/parametric field, but no type, no schema brand and no runtime check marks a field class 1 at HEAD. \"Every field carries its class\" is therefore true today only in the WEAK sense that unlisted implies class 1 — a documented convention, not an enforced one. The gate prints this under NOT CHECKED on every run rather than letting an empty ledger read as done."
      ],
      "whyNotFixedHere": "metadata.createdBy is deliberately left UNCLASSIFIED-AND-NAMED rather than guessed. The two readings differ materially: class 1 AUTHORITATIVE if 'ai' vs 'user' is authored provenance (continuous with ADR-0319 §1, which already places authored provenance in class 1, and the reading C75 would favour), or class 3 DERIVED-INCIDENTAL if it is a bookkeeping stamp nothing reads. Assigning class 3 to a field that is really class 1 is INVISIBLE to this gate — §3.20’s own \"Cannot see\" row says a mislabelled field is caught only by the round-trip comparator going quiet. Guessing would be precisely the tolerance-list-written-to-pass that ADR-0319’s own Alternatives section rejects on process grounds. This is an ADR amendment, owned by whoever owns ADR-0319, not by the agent that built the gate.",
      "territory": "docs/02-decisions/adrs/ADR-0319-audit-fields-are-derived-not-authored.md (the classification decision) and tools/rac-conformance/certification/capture.ts (the enforced lists, once the ADR decides). NOT fixable inside the gate: the gate MEASURES the classification, it does not author it. Deliberately NOT touched by this lane: the persistence cert suite and its comparator, which are at 0 FAILED and owned elsewhere.",
      "exitCondition": "TWO conditions, and the first alone is NOT sufficient. (1) classification-ledger.json reaches 0 entries — i.e. ADR-0319 assigns metadata.createdBy a class AND that class is added to the corresponding enforced list in capture.ts (never by deleting the ledger line). (2) The class-1 population acquires an ENFORCEMENT mechanism — a type, a schema brand, or a runtime check that marks a field AUTHORITATIVE — so that \"every field carries its class\" stops resting on the unlisted-implies-class-1 convention. C70 §7’s exit condition (\"every field carries its ADR-0319 class and the class is ENFORCED, not documented\") is UNMET until BOTH hold, and the gate says so on every run even at zero ledger entries.",
      "reviewBy": "2026-11-12"
    },
    {
      "gate": "check-no-dark-test-files.ts",
      "runner": "ga-gate",
      "addedAt": "2026-08-13",
      "builtBy": "L-849 follow-through (lane L10) - C70 4.2 authored-but-unreachable applied to the instruments; sibling axis to scripts/check/check-test-ci-coverage.mjs",
      "firstReading": "exit 1 DECLARED-LEVEL - 137 dark test files against a named ledger of 137 (132 no-runner, 2 excluded-by, 3 compiled-artefact), measured over 2,043 test files x 163 runners. Watched red in both directions: a planted package-root .test.ts exits 3 named by exact path; a bogus ledger row exits 3 STALE. Clean fixture repo reads 0 findings every run (green REACHABLE, L-716).",
      "pinnedTo": "tools/ga-gate/dark-test-files-ledger.json - 137 named rows, shrink-only, checked in both directions. Exit condition: the ledger reaches 0 (every test file matched by at least one runner include, or explicitly quarantined beside a comment naming its failure).",
      "territory": "The RUNNER CONFIGS, not the gate: root vitest.config.ts (whose two founding `src/ui/**` patterns point at a directory that does not exist — L-851), the per-workspace vitest configs, and playwright.config.ts. A dark file leaves the ledger by being CLAIMED by a runner include, never by being deleted from the ledger. Explicitly NOT this gate's territory: whether a claimed runner is INVOKED by CI — that is scripts/check/check-test-ci-coverage.mjs, a different axis, and the gate header says so. Also not its territory: whether a running test ASSERTS anything.",
      "exitCondition": "TWO conditions, and the first alone is NOT sufficient. (1) dark-test-files-ledger.json reaches 0 rows — every one of the 137 named files is matched by at least one runner `include` and removed by no `exclude`, OR is explicitly quarantined beside a comment naming the failure that keeps it out (L-851 quarantined five red suites exactly this way). Striking a row without either is forbidden: this is a shrink-only ledger and C70 §5.3 makes lowering it to go green identical to deleting the gate. (2) The 57 files under tests/{parity,visual-diff,contract-44,ci,playwright} that belong to NO runner at all acquire one — that needs a runner CREATED and is a founder scope call (BIM30-NEXT-SESSION-BRIEF §2.5 item 5), not a config edit. Until (2) is decided the ledger CANNOT reach 0 by any amount of include-fixing, and the gate prints the split so the remainder never reads as ordinary debt.",
      "reviewBy": "2026-11-13"
    },
    {
      "gate": "check-generator-circulation.ts",
      "runner": "ga-gate",
      "addedAt": "2026-08-13",
      "builtBy": "SPEC-49-CIRCULATION-INTEGRITY §4 rows CI-1/CI-2/CI-4 (CI-2 built 2026-08-13; CI-1/CI-4 ledgered 2026-08-14 by FOUNDER DECISION — the standing §4 ledger question, closed) · C70 §5 (shrink-only, both directions, executed controls) · C70 §2.2 (UNPROVEN is neither pass nor fail) · C75 §1.2 (two distinct facts must never print the same value) · founder production report 2026-08-13",
      "firstReading": "exit 1 DECLARED-LEVEL — 18 findings against a declared level of 18, all class DOORLESS (apartmentLayout ×7, houseLayout ×11, residentialBuilding ×0). Per generator: apartmentLayout 7/106 shipped winners (7%), houseLayout 11/24 shipped storeys (46%), residentialBuilding 0/34 shipped units (0%, AFFIRMATIVELY clean — 34/34 carried a verdict, so this is a measurement and not an absence of data). 0 UNMEASURED findings (164/164 shipped options carry a CI-0 verdict) and 0 DISAGREE findings (ARM B: the carried verdict matches the shipped artefact's own door graph on every measured option). ALL SEVEN FLOORS MET: 108 apartment cases driven (min 100), 24 house storeys (min 20), 34 residential units (min 25), 164 options shipped (min 140), 164 carrying a CI-0 verdict (min 140), 1460 rooms examined (min 700), 7/7 controls proven in-run (min 7). 166 generator runs in 8.6s. This reproduces SPEC-49 §2's independently-executed table EXACTLY, from a different code path — §2 replicated the reachability predicate in a test helper because it lives at L7; this gate reads the CARRIED CI-0 verdict that 1559275e stopped discarding at the emit boundary. Two instruments, two paths, same three numbers.",
      "secondReading": "2026-08-14, the CI-1/CI-4 pin — exit 1 DECLARED-LEVEL, 38 findings against a declared level of 38: the 18 DOORLESS rows unchanged (0 stale), plus 12 UNREACHABLE (apartmentLayout ×5 — every one a bedroom from that plate's own doorless set; houseLayout ×7 — five of them raw-id rooms `r5`/`r7` NOT in any doorless set, genuinely stranded behind a bad spine) and 8 CORRIDOR-GAP (houseLayout only: 6 stair-gaps, all on F1; 2 hall-gaps, both on F0; apartment and residential 0, affirmatively). 8/8 controls (C7 added with the pin: CI-1/CI-4 field spelling via hasOwnProperty + both planted gap directions, independently). Watched red in BOTH directions same-day before the pin was trusted: striking one still-measured row exited 3 RATCHET EXCEEDED naming `UNREACHABLE::houseLayout::17.491x13.416-b3-s1/F0`; planting a bogus row exited 3 STALE naming it. 166 generator runs in 3.0s.",
      "thirdReading": "2026-09-06 (lane RESI-CI0-EMIT-BOUNDARY), a STALE-LEDGER read, not a new pin — the gate was exiting 3 at HEAD: `11 declared entr(ies) are no longer measured`. Nothing in this entry had regressed; 11 rows had been PAID by earlier commits whose authors never struck them, which this ledger's own note forbids. Struck 38 → 27 and the gate returns to exit 1 DECLARED-LEVEL: `FINDINGS: 27 against a NAMED ledger of 27 (27 still measured).` ⚠ THE STRIKE WAS GUARDED, because the gate computes `stale` as declared − findings and that CONFLATES 'defect fixed' with 'sweep case vanished' — striking a row whose case had disappeared would delete the subject rather than record a win. All 11 were first proven `shipped=true measured=true` in the 166-case enumeration, so all 11 are genuine fixes. Per-generator at this reading: CI-2 DOORLESS apartmentLayout 5/107 (5%) · houseLayout 9/24 (38%) · residentialBuilding 0/34; CI-1 UNREACHABLE (carried verdict) 3/107 · 4/24 · 0/34; CI-4 GAP 0/107 · 6/24 · 0/34. ARM B 0 disagreements across 165 measured options; CI-0 verdict carried 107/107 · 24/24 · 34/34. NO GENERATOR CODE CHANGED in that commit — the improvement is other lanes' work, banked here as accounting.",
      "pinnedTo": "tools/ga-gate/generator-circulation-ledger.json — 27 NAMED rows keyed `<CLASS>::<generator>::<sweep-case>` with CLASS ∈ { DOORLESS ×14 · UNREACHABLE ×7 · CORRIDOR-STAIR-GAP ×4 · CORRIDOR-HALL-GAP ×2 } (was 38 = 18/12/6/2 until the 2026-09-06 strike), each carrying the room names (or the disconnected pair) it ships broken, checked in BOTH directions. `declared` is computed as the number of ledger rows STILL MEASURED, so an UNLEDGERED finding pushes findings > declared and exits 3 even when the totals match — a count-only comparison would let one fix and one new break cancel out and read as no change. A row no longer measured exits 3 STALE through contract.ts's `stale` channel.",
      "predatesTheGate": [
        "GENERATOR OUTPUT QUALITY WAS ENTIRELY UNGATED IN CI. SPEC-49 §3 item 6 verified by enumeration of all 56 gates at HEAD: not one read `doorAdjacentTo`, read `unreachableHabitableRoomIds`, or DROVE any generator. The two that mention `apartmentLayout` (check-refusal-identity, check-suppression-is-reversible) are about refusal MESSAGING. The founder's production report — 'rooms without doors · the corridor doesn't reach the relevant bedrooms' — could not have been caught by anything in this suite.",
        "THE DETECTION WAS NEVER MISSING; THE REFUSAL WAS. enumerate.ts computes reach, circulation, served-through, corridor-stair and corridor-hall correctly at CANDIDATE level. §TOPO-HARD-REJECT-ALL then ships the least-bad HARD-INVALID candidate by design ('never an empty result'), and until CI-0 the verdict had nowhere to go at the emit boundary. Measured by this gate at HEAD: 15/106 apartment and 19/24 house winners are hard-INVALID and shipped anyway.",
        "FOUR ROWS ARE A DOORLESS `Stair` — a storey whose vertical circulation element has no opening at all (14x11-b2-s2/F0, 14x11-b3-s2/F0, 17.491x13.416-b2-s2/F1, 17.491x13.416-b3-s2/F1). SPEC-49 §1 CI-2 names this the most severe form and §2 named the last of those cases by hand before this gate existed.",
        "HOUSE IS 6.5× WORSE THAN APARTMENT ON THE SAME ENGINE. `isHousePath = input.envelopeValidator !== undefined` is always true for a house storey and the empty-result branch is guarded `if (viable.length === 0 && !isHousePath)`, so a house storey must always produce a layout. Same door router, one fewer refusal (SPEC-49 §3 item 2).",
        "BATHROOMS DOMINATE THE VICTIM SET BY CONSTRUCTION: §DOOR-RESCUE-REACH deliberately EXCLUDES wet rooms from its rescue pass (SPEC-CIRCULATION-GRAPH §9.5 — they are owned by the privacy passes), so a landlocked bathroom has no rescuer."
      ],
      "negativeControlNote": "SEVEN controls execute INSIDE every run against a REAL engine-emitted option — never a hand-built literal, because a hand-built fixture cannot catch a field the ENGINE spells differently from the gate, which is the exact defect SPEC-49 §7 records (a sibling probe reported a clean 0/288 first run; `RectXZ` is {minX,minZ,maxX,maxZ} and the probe passed {x0,z0,x1,z1}, so every rect was garbage and the arm could never fire — Vitest does not typecheck test files, so nothing else would have caught it). Any control failing exits 2 MISCONFIGURED and NO verdict is published. C0 substrate generated. C1 CLEAN CORPUS READS ZERO on BOTH readers (watched GREEN; this is also the L-716 satisfiability demonstration — a state in which the gate exits 0 demonstrably exists — and it is the spelling check, since a misspelled `doorAdjacentTo` would read EVERY room doorless here). C2 A PLANTED DOORLESS ROOM IS FLAGGED BY NAME by the independent reader (watched RED — first run sealed 'Corridor' and the reader reported exactly ['Corridor']). C3 the ARM B agreement check watched in BOTH directions: agrees on the untouched option, DISAGREES on the planted one. C4 a defect planted in the CARRIED VERDICT is flagged BY NAME by the carried reader while the independent reader correctly stays silent — the only control that would notice `circulation.doorlessRoomNames` being read from the wrong field. C5 `circulation: undefined` classifies as SHIPPED-BUT-NOT-MEASURED, never sound. C6 the three room sets stay separate: a room planted into `unreachableRoomNames` does NOT move the doorless headline. C7 (added 2026-08-14 with the CI-1/CI-4 pin — a reader whose findings feed a ratchet graduates to the same discipline) the gap/unreachable readers: the ENGINE-EMITTED block must expose `corridorStairGap`/`corridorHallGap`/`unreachableRoomNames` under EXACTLY those names via hasOwnProperty (a renamed engine field would read undefined → false → a 0% gap rate FOREVER, the §7 struct-mismatch failure mode in boolean form, invisible to a clean/planted pair because false ≡ absent), the clean substrate reads false/false/0, and a planted stair-gap and a planted hall-gap each fire through read() independently without cross-contaminating the other flag. EIGHT controls total.",
      "countsReachedBehaviourNotArtefacts": "The gate IMPORTS AND EXECUTES the three real production entries — generateDeterministicLayouts, generateHouseLayout, orchestrateResidentialBuilding — over the committed sweeps and reads the option that ACTUALLY SHIPS. It counts no files, no field declarations and no test names. This is deliberate: a gate that counts ARTEFACTS can be satisfied by producing artefacts, which is how check-move-propagation went green over a consumer file wired into nothing.",
      "exitCondition": "generator-circulation-ledger.json's THREE class arrays (`doorlessCases`, `unreachableCases`, `corridorGapCases`) ALL reach 0 — every case in the three committed sweeps ships every room with at least one realised opening, BFS-reaches every room from the entrance, and seats every corridor against its origin — at which point the gate flips hard-0 and THIS ENTRY IS STRUCK. The cheapest first move is the four `Stair` rows: a doorless vertical-circulation element is the most severe form of CI-2 and is unlikely to need the product decision the bathroom rows may, since §DOOR-RESCUE-REACH's wet-room exclusion is DELIBERATE and closing those rows means deciding who rescues a landlocked wet room. Note the UNMEASURED and DISAGREE finding classes are already at 0 and are NOT pinned — a regression in either exits 3 immediately and cannot hide inside this entry.",
      "territory": "packages/ai-host/src/workflows/ — apartmentLayout/tgl/wallsAndDoors.ts (§DOOR-RESCUE-REACH), apartmentLayout/tgl/enumerate.ts (§TOPO-HARD-REJECT-ALL) and houseLayout/houseOrchestrator.ts. NOT tools/. This cannot be fixed by whoever owns the gate suite; the gate lane measures and reports.",
      "cannotSee": "SHIPPED SWEEP ARTEFACTS, not a live user session — the sweeps are synthetic convex quads and founder-scale rectangles, and a real project shell is neither (SPEC-49 §6 CI-5-INSTRUMENT, which does not exist), so a green reading here would never mean 'the generators are sound in production' · DOORS ARE COUNTED, NOT JUDGED USABLE — width, swing clearance and whether furniture stands in the arc are SPEC-49 CI-3 and are outside this gate; a room with one 400 mm door into a wardrobe reads identically to a room with a proper entrance · THE ENGINE PAYLOAD, NEVER THE BUILT MODEL — everything downstream of LayoutOption (executePlan, the wall-opening cascade, room re-detection) is invisible, so a door emitted and then dropped at build time is not caught · SERVED-THROUGH (no door onto circulation) and HARD-INVALID-WINNER rates are MEASURED AND PRINTED under their own names on every run but NOT LEDGERED — served-through is not CI-1 (an en-suite may legitimately be served through its master) and no decision covers it; hard-invalid winners are §TOPO-HARD-REJECT-ALL, the union of many rules rather than one class — nothing fails if either doubles, and that silence is named rather than left to read as coverage · the CI-4 ledger covers the CARRIED storey-scale contiguity flags only (building-scale core reachability is §RESI-CORE-CIRCULATION's, enforced in the orchestrator and measured 0/34 orphans; the direct-door half of CI-4 is the served-through set above) · the reachability figure is the ENGINE's own — the CI-1 ledger pins the engine's verdict about itself, no independent BFS exists in the gate — which SPEC-49 §7 records as duplicate-name LATENT-unsafe (latent only because §DUP-NAME-UNIQUE mints unique names — a guard, not a fix); C7 watches the field SPELLING and C6 the reader, neither re-derives the BFS.",
      "reviewBy": "2026-11-13"
    },
    {
      "gate": "check-per-package-compile.ts",
      "runner": "ga-gate",
      "addedAt": "2026-08-14",
      "builtBy": "Phase H · C01 §5 · Task 7.2 (the gate FILE is old). The INSTRUMENT, however, is dated 2026-08-12 `2b1e7e99` — see predatesTheGate. Entry added 2026-08-14 by L-RUNNER.",
      "addedLateAndWhy": "⚠ THIS ENTRY BREAKS EDGE (a) OF THIS FILE`S OWN RULE — \"an entry may only be added in the commit that introduces its gate\" — AND THAT IS RECORDED RATHER THAN GLOSSED. The rule exists because a late entry usually means the gate ran and something changed, which is a regression or debt, not a first reading. That is NOT the case here: the gate spawned `tsc` without `shell: true`, which on win32 returns ENOENT with empty output, read as \"no errors\" — so it printed ~90 FABRICATED PASS lines per run for its ENTIRE LIFE and was never on any ledger because it was never red. `2b1e7e99` (2026-08-12) fixed the spawn and produced the FIRST honest reading in the gate`s history. Everything it now reports therefore predates the instrument in the only sense that matters, and none of it was ever chosen. The correct lateness is 2 days, not the gate`s age. FOUNDER APPROVED 2026-08-14; recorded as an open decision at BIM30-NEXT-SESSION-BRIEF §11.3 item 3 before approval.",
      "supersededBy": "⚠⚠ THE 27 IS NOT A COMPILE RESULT — CORRECTED 2026-08-24 BY LANE PKGCOMPILE23 (L-10360). Read this BEFORE `firstReading` below, which is retained verbatim but must not be quoted on its own. MEASURED 2026-08-24, replaying the gate's OWN `spawnSync('npx', ['tsc', …], { shell: true })` invocation across ALL 97 tsconfig-bearing packages: in 59 OF THE 97, `npx tsc` NEVER STARTED A COMPILER. It exited 1 having printed a Node module-loader stack trace — `Error: Cannot find module '…\\Product_Rediness_08\\node_modules\\node_modules\\.pnpm\\typescript@5.9.3\\…\\bin\\tsc'` (note the DOUBLED node_modules), code MODULE_NOT_FOUND. 76 packages carry their own node_modules/.bin/tsc shim (pnpm writes one wherever `typescript` is a direct devDependency) and npx prefers the nearest shim, which resolves one directory short of the repo root. The old predicate `hasErrors = output.includes(': error TS') || status !== 0` scored every one of those loader crashes as `FAIL packages/x [own 0 · foreign 0]`. So `27 PACKAGES FAIL ISOLATED tsc --noEmit` was, in substantial part, A CENSUS OF pnpm'S BIN SHIMS — packages/schemas was one of them and compiles CLEANLY (exit 0, empty output, compiler spawned directly). This is §PER-PACKAGE-COMPILE-WAS-GREEN-AND-BLIND for the THIRD time, inverted: fabricated FAILURES rather than fabricated passes. ⛔ ALSO INVALIDATED: the ledger's own verdicts. ai-host, command-registry, constraint-solver, core-app-model, family-instance and family-loader were all in the 59, so every `exclusion still warranted` line this gate printed for them was a launcher crash, and §MT-09-SKIP-LEDGER RULE 2 (a paid exclusion must leave the ledger) could not fire for ANY of them — the ledger was permanent by construction, which is exactly the property RULE 3 was written to prevent. The compiler was never the problem and never varied: all 76 package-local node_modules/typescript symlinks and the root one resolve to typescript 5.9.3. FIXED in the same lane — the gate now spawns `process.execPath` on a resolved typescript/bin/tsc path (no npx, no shell, no PATH, no .bin shim) and PRINTS that path and version in its header; a non-zero exit carrying no `error TSnnnn:` line is classified UNMEASURED, never FAIL. See §PPC-ONE-NAMED-COMPILER and §PPC-UNMEASURED-IS-A-CLASS in the gate source. ⭐ THE REPLACEMENT READING, measured 2026-08-24 with the direct-spawn compiler over ALL 97 tsconfig-bearing packages (63.1 min wall clock at concurrency 4, on a tree three other agents were editing concurrently): population 97 · MEASURED 95 · UNMEASURED 2 (wcag-audit, webhooks — BOTH `tsc exited 3221225794` = 0xC0000142 STATUS_DLL_INIT_FAILED at 0s, a transient Windows process-creation failure, see §PPC-TRANSIENT-PROCESS-CREATION) · excluded by ledger 8 of ceiling 8 · FAILED 28, split 16 OWN-SOURCE (actionable in the package) and 12 CASCADE-ONLY (zero own errors — red purely via a broken dependency) · PASSING IN ISOLATION 59 · NOT PROVEN TO COMPILE 38 of 97 ← quote THIS. ⚠ IT IS NOT COMPARABLE TO THE 27/85 ABOVE AND MUST NOT BE DIFFED AGAINST IT: the population moved (85 eligible → 97), the launcher changed, and the old numerator counted loader crashes. This is a NEW first reading, not a delta. It is still UNPINNED — the gate has no per-package ledger, so this number can drift without exit 3; that half of the exitCondition below is unchanged and unpaid.",
      "firstReading": "⚠ SUPERSEDED 2026-08-24 — READ `supersededBy` ABOVE FIRST; most of this 27 was a launcher crash, not a compile failure. exit 1 — 27 PACKAGES FAIL ISOLATED `tsc --noEmit`, 9 SKIPPED, out of 85 that carry a tsconfig and are not on SKIP_PACKAGES. The arithmetic is written out because the gate prints two different denominators on two different branches and they must not be confused: `packages/` holds 98 directories; 5 have NO tsconfig.json and are skipped silently (bench-visual-diff, eslint-plugin-pryzm, legacy-shim, release, render-pipeline), leaving 93; 8 are on SKIP_PACKAGES with documented reasons (headless, runtime-composer, ai-host, command-registry, constraint-solver, core-app-model, family-instance, family-loader), leaving 85 eligible; one more is AUTO-SKIPPED at runtime (all its errors are the global-window.d.ts isolation root), giving the gate`s own summary line \"packages compiled: 84 · floor 40 · skipped 9\". So 27/85/9 and 27/84-compiled/9-skipped are the same reading counted either side of the auto-skip. PROVENANCE: first honest run 2026-08-12 `2b1e7e99` (84 compiled, 27 FAIL — the run that replaced ~90 fabricated PASS lines); re-read 27/85/9 by the 2026-08-13 fleet session and recorded at BIM30-NEXT-SESSION-BRIEF §11.3 item 3. Floors held on both runs (≥40 compiled subjects; a failed spawn now exits 2 MISCONFIGURED instead of reading as a pass). NOT RE-MEASURED END-TO-END BY L-RUNNER, stated rather than implied: the gate spawns ~85 real `tsc` runs and was observed at 10 subjects after 20 minutes in this shared tree — hours, not minutes. The pin is the two recorded measurements above, not an estimate. A partial run was started and abandoned; its first 10 subjects agreed (8 PASS, 0 FAIL, 2 SKIP — ai-host by SKIP_PACKAGES, bench-visual-diff for no tsconfig), and it independently confirmed the 85/93/98 arithmetic above.",
      "pinnedTo": "⚠ NOTHING — AND THAT IS A REAL LIMITATION, STATED SO IT IS NOT MISTAKEN FOR A RATCHET. Every other entry in this file points at a NAMED ledger inside its gate, so exceeding the level exits 3 and run-all refuses to absorb it. check-per-package-compile has no ledger and no exit-3 path: it exits 1 on ONE failing package and exits 1 on FIFTY. So this entry`s 27/85/9 is a reading WRITTEN DOWN, not a reading ENFORCED — the number can silently rise and this file will not notice. Giving the gate a named per-package ledger is part of the exit condition below, not a nice-to-have.",
      "predatesTheGate": [
        "THE DEFECTS ARE OLDER THAN THE MEASUREMENT, AND THE MEASUREMENT IS 2 DAYS OLD. Until `2b1e7e99` (2026-08-12) this gate compiled NOTHING on win32 and printed ~90 fabricated PASS lines per run — L-774 living inside a gate. Not one of the 27 failures was ever shipped as a decision; there was no decision, because there was no reading.",
        "ROOT 1 — THE WINDOW-GLOBAL / AMBIENT-SCOPE ROOT, recurring in 23 of the 27 failing packages. `apps/editor/src/engine/global-window.d.ts` declares the `window.*Store` slots, and it is OUT OF SCOPE when any package under packages/ compiles in isolation, so TS2339 fires across the geometry-* / views / spatial-index / picking swath. LARGELY FIXED by making the declaration travel with the file as a narrow structural window cast (`ccef72e9` PluginRegistry, `62f9b504` CommandManagerImpl) — P4-compliant, no `(window as any)`, so the cast-count ratchet is untouched.",
        "ROOT 2 — THE IN-BOUNDS-GRID-READ ROOT: ai-host `platePartition` produced TS18048/TS2532 in 22+ isolated compiles. FIXED `3977cf95` by asserting the structurally in-bounds reads (spatial-index re-compiled to 0 errors). Two further roots closed alongside: schemas `6e1df658` and climate-host `liveNormalsAdapter` `8ff72e7d`.",
        "⚠ HONEST SWATH CORRECTION — THE ROOTS ARE NOT THE BULK, AND THIS IS THE LINE THAT MUST NOT BE READ AS ORDINARY DEBT. Both named roots are now largely fixed, and they account for roughly 11 of the ~1,800 errors PER PACKAGE. NO package flips to PASS from them alone. The remainder is LATENT `noUncheckedIndexedAccess` DEBT, and it is latent for a structural reason: `tsconfig.base.json` sets `noUncheckedIndexedAccess: true` and every package tsconfig extends it, while the ROOT `tsconfig.json` does not extend base and does not set the flag at all — so `npm run build` has never once compiled this code under the rule the packages declare for themselves. The isolated compile is not stricter by accident; it is the FIRST compile that honours what each package says its own settings are. Top offenders measured 2026-08-13: PlanViewAnnotationRenderer 103, WallJoinResolver 86, SelectionManager 73.",
        "TWO TRIAGE CITATIONS FROM THE 2026-08-13 PASS WERE DISPROVEN BY MEASUREMENT and are recorded so nobody re-chases them: `AiPlane.ts:169` — no such error exists; `epwParser.ts:86` — clean. `polygonOffset.ts:333` is a deferred one-liner (conditional spread) — the file was contended by another lane at session close."
      ],
      "exitCondition": "THIS IS NOT ONE NUMBER GOING DOWN; IT IS THREE SEPARATE THINGS, and collapsing them is how the remainder would get mistaken for ordinary debt. (1) MEASUREMENT — the gate gets a NAMED per-package ledger (the 27 packages, each with its error count) checked in BOTH directions, so it exits 3 when the set grows or goes stale instead of exiting 1 forever. Until that lands the 27/85/9 above is unenforced. (2) THE CASCADE ROOTS — both are largely fixed already (`ccef72e9`/`62f9b504` window-global, `3977cf95` platePartition, + `6e1df658`/`8ff72e7d`); what is left of them is small and named. (3) THE REMAINDER, which is the real work and is NOT ordinary debt: ~1,800 `noUncheckedIndexedAccess` errors per package that the root build has never compiled, because the root tsconfig does not set the flag its own packages declare. Closing that is EITHER a genuine per-file audit of every indexed read (the correct fix, and it is large) OR an explicit, founder-level decision to align the two configs — and aligning them DOWNWARD (turning the flag off in tsconfig.base.json) would be deleting the finding, not fixing it, and is forbidden by R7 in spirit even though it touches no number in this file. The entry is struck when the gate exits 0 with the ledger empty; it may NOT be struck by widening SKIP_PACKAGES.",
      "territory": "packages/** (27 failing workspaces spanning geometry-*, views, spatial-index, picking, room-topology, file-format, persistence-client, snapping, input-host, physics-host and more) plus tsconfig.json / tsconfig.base.json at the repo root. NOT tools/. Whoever owns the gate suite cannot fix any of this; the root-vs-base flag divergence in particular is a repo-wide decision, not a gate-owner decision.",
      "reviewBy": "2026-11-14",
      "reviewByNote": "Three months, matching this file`s other Phase-1 entries. ⚠ A review here is NOT \"is 27 lower yet\". It is: has the gate been given a ledger (exit condition 1), and has the root-vs-base `noUncheckedIndexedAccess` divergence been DECIDED rather than left to read as ordinary debt. Extending this date, or moving this entry to gate-debt.json, requires the founder — that is the moment \"not fixed yet\" becomes \"we live with this\"."
    },
    {
      "gate": "check-triangulation-canonical.ts",
      "runner": "ga-gate",
      "addedAt": "2026-08-14",
      "builtBy": "GE-12 (Tier 3, BIM30-GAP-REGISTER) · C73 §3.1–§3.4 structural counting, §0.3 R3 recipe · C70 §5.2 (exit-2 floor) + §5.6 (planted negative test) · reference implementations check-offset-implementations.ts and check-predicate-canonical.ts",
      "firstReading": "exit 1 DECLARED-LEVEL — 13 findings against a declared level of 13: C1 ×11 (7 polygon-triangulation bodies across 6 files — vendored earcut, TWO rival O(n²) ear clips (roof + extrude), one index fan, three centroid-fan emissions (room ×1, ceiling ×2) — plus 4 planar-topology-engine copies: the 3 PlanarTopologyEngine.ts files AND auto-dimension/perimeter.ts traceFaces, an independent reimplementation the class name never matched) + C2 ×2 (neither family has a named canonical file — that decision belongs to the collapse PR per C73 §3.6/§3.7, not to this gate). Floors met: 6,589 files scanned (min 500), 8 executed controls proven. The register's '~5'/'~3' were name/file censuses; the measured reading is 7 bodies + 4 copies.",
      "pinnedTo": "tools/ga-gate/triangulation-canonical-baseline.json — 11 body keys (`file:line::signature` / `file::planar-topology-engine`) + `c2: 2`, checked in BOTH directions (a struck body must leave the ledger or the gate exits 3 stale; a new body exits 3 exceeded).",
      "negativeControlNote": "EIGHT controls execute INSIDE every run: five planted bodies (one per signature, the fan-centroid plant multi-line as room.ts writes it, the planar engine with both anchors) must be detected; four rejects must stay silent — a call-site file (triangulate/earcut/ShapeUtils names with no arithmetic), a quad emitter, each planar-engine anchor ALONE (the atan2 bearing alone is RoomDetectionEngine's real shape), a modular-predecessor walk over an IMMUTABLE ring (the Sutherland–Hodgman shape the first measurement run miscounted nine times before the same-list-shrinks conjunction was added), and a shared-height emitter of earcut OUTPUT triples (slab.ts's real shape, miscounted before the ring-successor lookbehind was added); plus a clean tree that must read 0. Any control failing exits 2 MISCONFIGURED. Additionally the C70 §5.6 real-tree test was performed at landing: a planted duplicate ear clip in geometry-kernel drove exit 3 with the plant NAMED (verbatim output in the gate header), and its removal returned exit 1.",
      "exitCondition": "Per C73 §6, per family: the collapse PR names the canonical file (C2 for that family leaves), migrates the rivals so C1 for that family reads the canonical body only, and adds an ORACLE FIXTURE at a known answer (a concave polygon with known triangulation for the triangulators; a known wall graph with known faces for the planar engine). One family per PR (C73 §3.5). The entry is struck when both families are collapsed and the gate flips hard-0; it may NOT be struck by adding exclusions.",
      "territory": "packages/geometry-kernel/src/producers/** (earcut, roof/triangulate, extrude, linear-structural, room, ceiling), packages/room-topology, packages/core-app-model/src/ai, packages/ai-host, packages/auto-dimension. NOT tools/ — the gate lane measures and reports; the collapse is production-geometry work this lane's fence forbids.",
      "cannotSee": "correctness of ANY surviving body (counting gates are blind to it by design — no oracle fixture exists yet) · the THREE.ShapeUtils.triangulateShape call-site axis (geometry-slab, geometry-roof, IFC readers triangulate through THREE's bundled earcut — rival PATHS with no body in this tree) · a monotone/Delaunay triangulator (no live instance measured; a new one needs a signature added) · a from-scratch planar engine that renames the axes or keys half-edges without the arrow.",
      "reviewBy": "2026-11-14"
    },
    {
      "gate": "check-deterministic-regeneration.ts",
      "runner": "ga-gate",
      "addedAt": "2026-08-14",
      "builtBy": "C73 §1.3/§5.4/§6 · one of three instruments GE-07 (BIM30-GAP-REGISTER) is recorded CLOSED on · registered by the orchestrator completing Lane A's §GATE-AUTHORED-BUT-UNWIRED round 2 (e737b877 registered its two born-passing siblings and named why this one could not follow yet)",
      "addedLateAndWhy": "⚠ The gate FILE predates this entry, which strains edge (a) — recorded rather than glossed, on the per-package-compile precedent above. The rule exists because a late entry usually means the gate ran and something changed. Here the gate ran in NO runner from the day it was committed (run-all's committed-but-unregistered pre-flight named it on every run); its readings were real but enforced only while a human remembered to type the command, so none of the 134 was ever a shipping decision. THIS commit is the one that introduces the gate to a runner, and the entry lands in it — a first reading in the only sense that matters. The one thing that HAD got worse since the baseline — a post-baseline D2 site at packages/geometry-kernel/src/pure/polygonBoolean.ts:307, uncovered when 28a25791 re-anchored the three stale ledger rows instead of striking them — was FIXED (13d48db7, cut-parameter order made spec-defined) before registration, never absorbed: registration waited at exit 3 precisely because exit 3 is never absorbable (C70 §5.1).",
      "firstReading": "exit 1 DECLARED-LEVEL — 134 findings against a declared level of 134, EXECUTED at HEAD by the registering commit after 13d48db7: D1 ×84 FLOWING forbidden-input sites (Date.now()/Math.random() values that REACH model/geometry state — store-event timestamps, metadata createdAt/modifiedAt, version stamps, one id minting, plant-builder transforms; DISCARDED timing spans are deliberately not counted) + D2 ×41 order-dependence sites (numeric sort comparators, hash-container iteration) + D3 ×9 PERSIST-OR-LOSE field paths. new: 0 · struck: 0 on all three arms. Floors met: 15 geometry packages (min 10), 409 non-test files (min 200), 159 forbidden-input reads BEFORE classification (min 40 — a collapse to single digits is a broken detector, not a clean repo), D3 classification input present 1/1, executed controls 1/1 (D1 discarded-vs-flowing pair, D1 random, D2 detect, D1/D2 zero-tree, test-scope exclusion).",
      "pinnedTo": "tools/ga-gate/deterministic-regeneration-baseline.json — per-site keys, shrink-only (--write-baseline regenerates DOWNWARD only), checked in both directions: a new site exits 3 EXCEEDED — proven live by the polygonBoolean.ts:307 episode, which is this ratchet's first captured kill — and a ledgered site no longer measured exits 3 STALE. Since 28a25791 all three arms print `new:` plus the names, so an excess is named on the spot rather than hand-diffed out of the totals.",
      "predatesTheGate": [
        "All 84 D1 sites are the estate's standing wall-clock/unseeded-random habits in geometry stores and builders, written before this gate could see them; nothing measured them, so there was no decision to make.",
        "The 41 D2 comparator/iteration sites likewise. The 42nd (polygonBoolean.ts:307, born 3f004a58 AFTER the baseline) is the one that did NOT predate the gate — and it is gone, not ledgered.",
        "D3's 9 persist-or-lose field paths read the derived-ledger owned by check-derived-regenerable; this arm re-ratchets that reading, it does not re-measure it."
      ],
      "exitCondition": "per the gate's own C73 §6 print: D1 and D2 read 0 across every geometry package AND the persist-or-lose ledger is empty; the baseline file is then deleted with the finding path and the gate flips hard-0. NOT PROVEN by this gate, printed on every run (C73 §5.4b/c): cross-machine floating-point determinism, GPU-side geometry, and forbidden values passed ACROSS a function boundary into a stamper. ⚠ Also printed every run and repeated here so a green reading is never over-read: the GEOMETRY level of C73 §1.3 has NO classification input in this repo today — the A1→A2→A3 cycle serialises MODEL FIELDS; no entry classifies a vertex buffer, a triangle winding, or a mesh transform. Closing that needs a harness comparing BUILT GEOMETRY across a save/reload, which does not exist.",
      "territory": "packages/geometry-*/src — 15 packages at the reading. NOT tools/. The gate lane measures and reports; the D1/D2 drain is production-geometry work.",
      "reviewBy": "2026-11-14"
    },
    {
      "gate": "../rac-conformance/certification/gates/check-relationship-determination.ts",
      "runner": "ga-gate",
      "addedAt": "2026-08-15",
      "builtBy": "C78 §20 · U-INV-1 · tracker §-3 (BAR 3) · registered by the §GATE-AUTHORED-BUT-UNWIRED round-3 lane, which found THIRTEEN certification-gate files registered in NEITHER runner",
      "addedLateAndWhy": "⚠ The gate FILE predates this entry, and the deterministic-regeneration precedent above governs: the rule exists because a late entry usually means the gate ran and something changed. Here the gate ran in NO RUNNER from the day it was committed. run-all's cross-home orphan pre-flight named it on every run; its readings were real but were enforcement only for as long as a human remembered to type the command, so not one of the 126 was ever a shipping decision. THIS commit introduces the gate to a runner, and the entry lands in it — a first reading in the only sense that matters. THIS IS THE MOST CONSEQUENTIAL OF THE THIRTEEN: the programme's headline metric, 126 findings, came from a gate nothing invoked, so nothing prevented it regressing.",
      "firstReading": "exit 1 DECLARED-LEVEL — 126 findings against a declared level of 126, EXECUTED AT HEAD by the registering commit: 91 silent-dispatch verbs + 35 refusal-less relationships. This CONFIRMS the hand-run bar-3 reading of 126 from a runner that now actually invokes it. All 13 floors met.",
      "pinnedTo": "tools/rac-conformance/certification/gates/relationship-determination.json — 126 NAMED keys (`verb:<name>/silent-dispatch`, relationship rows), checked in BOTH directions by the shared contract.ts: a new finding exits 3 EXCEEDED, a ledgered finding no longer measured exits 3 STALE.",
      "predatesTheGate": [
        "All 91 silent-dispatch verbs are the estate's standing dispatch shape, written long before this gate could see them: no normaliser, a composed planner that reaches the verb, and a preview entry point returning bare null (§0.e) — so every one of the verb's 41 cells yields C78 §1.1's forbidden fourth answer: no plan, no typed UNDETERMINED, no report. annotation.create/delete/move/update and beam.create/beam.batch.create are the alphabetical head of that list.",
        "The 35 refusal-less relationships likewise. Nothing measured any of this in an automated run, so there was no decision to make.",
        "The gate was committed and left unregistered; the 126 was quoted as a programme metric while being produced by nothing on a schedule."
      ],
      "exitCondition": "the 126 named keys drain to 0 — each silent verb gains a normaliser and a planner route that yields a plan or a typed UNDETERMINED rather than bare null, and each refusal-less relationship gains a typed refusal path (C78 §1.1/§1.4). relationship-determination.json reaches 0 entries and the gate flips hard-0, at which point this entry is struck. It may NOT be struck by narrowing the verb sweep.",
      "territory": "the verb handlers across plugins/ and packages/command-registry/, plus apps/editor/src/engine/consequence/ (the planner + preview entry point). NOT tools/ — the registration lane measures and reports; the drain is production work.",
      "reviewBy": "2027-02-15",
      "reviewByNote": "Longer than the 2026-11-15 siblings in this round, and argued rather than assumed on the epsilon-policy precedent: this is 126 keys spanning 91 distinct bus verbs, whose first step is a planner/normaliser route that does not exist for most of them. A three-month bound on 91 verbs would expire before anyone could honestly act on it, and an expiry nobody can meet teaches date-extension rather than compliance."
    },
    {
      "gate": "../rac-conformance/certification/gates/check-no-empty-means-unknown.ts",
      "runner": "ga-gate",
      "addedAt": "2026-08-15",
      "builtBy": "C78 §20 · U-INV-4 · §1.4/§5 (the GR-14 drain) · §GATE-AUTHORED-BUT-UNWIRED round 3",
      "addedLateAndWhy": "Same shape as the entry above and same justification: committed, registered in neither runner, never executed in an automated run. Hand-run at 63; this commit is the first time a runner invokes it.",
      "firstReading": "exit 1 DECLARED-LEVEL — 63 findings against a declared level of 63, EXECUTED AT HEAD by the registering commit. Confirms the GR-14 hand-run reading of 63. All 5 floors met.",
      "pinnedTo": "tools/rac-conformance/certification/gates/no-empty-means-unknown-debt.json — 63 NAMED file-keyed rows with per-site line references, checked in both directions by the shared contract.ts.",
      "predatesTheGate": [
        "Every ARM C site is the estate's standing `?? []` habit at a query boundary, where an absent answer and an empty answer collapse to the same value — the §CONTEXT-DATA-HONESTY defect this repo keeps paying for, in the small.",
        "WallRebuildCoordinator.ts (3 sites: @33, @1173, @1278 on wall.openings), buildBuildingGraph.ts (3 sites incl. `topology.getAdjacencyRelationships(id) ?? []`), ConsequenceExecutionService.ts (2 sites on predictedGeometry), WallMoveConsequencePlanner.ts:251, apartmentLayoutWallMapper.ts:38, initBusHandlers.ts:2088 are the head of the list. All predate the gate; nothing measured them."
      ],
      "exitCondition": "each of the 63 sites either distinguishes absent from empty (a typed UNDETERMINED / refusal per C78 §1.4) or is struck with a recorded argument that the collapse is safe at that site. no-empty-means-unknown-debt.json reaches 0 and the gate flips hard-0; the entry is struck then. It may NOT be struck by adding exclusions to the scan.",
      "territory": "apps/editor/src/engine/** and the packages/ query surfaces named in the ledger. NOT tools/.",
      "reviewBy": "2027-02-15",
      "reviewByNote": "Aligned with relationship-determination's date for the same argued reason: 63 sites across many files, each needing a per-site judgement about whether absent and empty are genuinely the same fact there. That is not a mechanical strike-list."
    },
    {
      "gate": "../rac-conformance/certification/gates/check-dependency-fields-honoured.ts",
      "runner": "ga-gate",
      "addedAt": "2026-08-15",
      "builtBy": "C79 §7.1/§7.3/§8 · C78 U-INV-6 · §GATE-AUTHORED-BUT-UNWIRED round 3",
      "addedLateAndWhy": "Committed, registered in neither runner, never executed in an automated run until this commit.",
      "firstReading": "exit 1 DECLARED-LEVEL — 13 findings against a declared level of 13, EXECUTED AT HEAD by the registering commit. Floors met: dependency-fields.json ledger present 1/1, 15 dependency-naming fields in the census (min 14, the C79 §0.1(5) count).",
      "pinnedTo": "tools/rac-conformance/certification/gates/dependency-fields.json — 13 NAMED `field@Type` rows in declaredFindings, checked in both directions.",
      "predatesTheGate": [
        "boundingWallIds@FloorData and @CeilingData — UNREACTED. Every boundingWallIds reactor in the tree (SyncStateEngine.ts:722,749,779,856,1051) is keyed on ROOMS, so a wall moved, split or deleted leaves the array stale. (The older C79 §7.1 EMPTY-literal half was already paid by 9fd9c5b6; the unreacted half was never measured.)",
        "hostRoomId@FloorData/@CeilingData — PARTIAL: populated and reacted to, but the reactor propagates FINISH COLOUR only (RoomFinishResolver.ts:91/:151, RoomFinishSyncService.ts:77); the BOUNDARY is never re-derived. C79 §7.3's partial-honouring-recorded-as-full.",
        "coveredRoomIds@FloorData/@CeilingData — written from hostRoomId, read for colour, no reactor on room change.",
        "roomId@LightingData — resolved when the LIGHT moves (MoveLightingCommand.ts:50), never when the ROOM changes.",
        "hostId@LightingData — declared on the type (LightingTypes.ts) with NO writer and NO reader: C72 §0's mechanism in the schema layer, reading like wiring to anyone auditing by grep."
      ],
      "exitCondition": "per C78 U-INV-6 the unhonoured fields reach 0 by HONOURING (a reactor re-derives the field when the named element changes) OR by DELETION (the field stops promising a dependency it does not carry). declaredFindings reaches 0 and the gate flips hard-0; the entry is struck then.",
      "territory": "packages/command-registry/src/{floors,ceilings,lighting}/, packages/core-app-model/src/stores/, the SyncStateEngine reactors. NOT tools/.",
      "reviewBy": "2026-11-15"
    },
    {
      "gate": "../rac-conformance/certification/gates/check-graph-delete-integrity.ts",
      "runner": "ga-gate",
      "addedAt": "2026-08-15",
      "builtBy": "C71 §6 · C70 F-INV-2 (deletes propagate; never an empty cascade BY DESIGN) · §GATE-AUTHORED-BUT-UNWIRED round 3",
      "addedLateAndWhy": "Committed, registered in neither runner, never executed in an automated run until this commit.",
      "firstReading": "exit 1 DECLARED-LEVEL — 10 findings against a declared level of 10, EXECUTED AT HEAD by the registering commit. All 9 floors met (every parsed ElementType kind has a mapping row; every mapped file exists).",
      "pinnedTo": "tools/rac-conformance/certification/gates/graph-delete-integrity-debt.json — 10 NAMED `kind/finding:file` rows, checked in both directions.",
      "predatesTheGate": [
        "ARM B RECONSTRUCTED (slab, column, beam, furniture, …): undo RE-AUTHORS edges from the kind's own snapshot instead of restoring the captured set verbatim, so edges authored by OTHER elements' commands (boundedBy from room detection, supports from AssignBeamSupports, contains) are invisible in that snapshot and are silently dropped by delete+undo. Commit 3ee632f6 established the correct verbatim shape for walls; the other kinds never followed.",
        "ARM B NONE · curtain-wall: DeleteElementCommand.ts (branch 'curtainwall') purges ALL relationships on delete and undo restores NONE — delete+undo permanently deletes the kind's graph presence, and it looks correct while doing it.",
        "curtain-panel has no delete path at all; the mapping DECLARES the absence rather than skipping the kind (panels are REPLACED, never deleted)."
      ],
      "exitCondition": "each RECONSTRUCTED kind adopts the 3ee632f6 capture/restore pair so undo restores the purged edge set VERBATIM; curtain-wall's undo restores what its purge removed. graph-delete-integrity-debt.json reaches 0 and the gate flips hard-0. ⚠ Printed by the gate on every run and repeated here so a future green is never over-read: this gate is STATIC — ARM B's VERBATIM reading proves the RESTORE CALL EXISTS, never that the restored edges are byte-equal to the purged ones, and ARM C cannot see a cascade that computes empty at runtime. Both halves print UNPROVEN.",
      "territory": "packages/command-registry/src/**/Delete*Command.ts. NOT tools/.",
      "reviewBy": "2026-11-15"
    },
    {
      "gate": "../rac-conformance/certification/gates/check-no-silent-partial.ts",
      "runner": "ga-gate",
      "addedAt": "2026-08-15",
      "builtBy": "C68 §5 (a batch that drops items must SAY SO) · §GATE-AUTHORED-BUT-UNWIRED round 3",
      "addedLateAndWhy": "Committed, registered in neither runner, never executed in an automated run until this commit.",
      "firstReading": "exit 1 DECLARED-LEVEL — 3 findings against a declared level of 3, EXECUTED AT HEAD by the registering commit. Floors met: 36 candidate batch files established from the union of two independent conventions (min 20), 25 batch commands actually CLASSIFIED (min 15), 13 with at least one per-item DROP site (min 1 — zero would make every arm vacuous), ledger loaded 1/1.",
      "pinnedTo": "tools/rac-conformance/certification/gates/no-silent-partial-debt.json — 3 NAMED rows, checked in both directions.",
      "predatesTheGate": [
        "All 3 are batch commands that drop items per-item without surfacing the partial to the caller — the same family of defect as R4's report-payload discard, one layer earlier. They were written before any instrument measured the drop sites, and nothing ran this gate to notice."
      ],
      "exitCondition": "each of the 3 batch commands reports its dropped items to the caller (count + identities), so a partial application can never render as a completed one. no-silent-partial-debt.json reaches 0 and the gate flips hard-0.",
      "territory": "packages/command-registry/src/** batch commands. NOT tools/.",
      "reviewBy": "2026-11-15"
    },
    {
      "gate": "../rac-conformance/certification/gates/check-generation-is-consequential.ts",
      "runner": "ga-gate",
      "addedAt": "2026-08-15",
      "builtBy": "C80 §7 · GEN-GAP-1 (named UNBUILT at C80's stamp) · §GATE-AUTHORED-BUT-UNWIRED round 3",
      "addedLateAndWhy": "Committed, registered in neither runner, never executed in an automated run until this commit.",
      "firstReading": "exit 1 DECLARED-LEVEL — 1 finding against a declared level of 1, EXECUTED AT HEAD by the registering commit. Floors met: 284 handler files swept (min 150), 1 generation-family bus verb discovered (min 1 — C80 §7 is emphatic that a run reading 0 is MISCONFIGURED, not passing), 1 verb whose plan-or-typed-refusal route was PROVEN (min 1).",
      "pinnedTo": "the gate's own declared level (C80 §7), checked in both directions by the shared contract.ts.",
      "predatesTheGate": [
        "The finding is the C80 §0.1(1) coverage gap itself: the NINE UI-controller generators are not bus verbs and are therefore invisible to a bus-verb sweep — including HouseLayoutExecutor.ts:1757-1766, the §GRAPH-CLEAR-FIRST loop MEASURED destroying a hand-drawn room. The gate reports this as a finding on every run rather than as a silence. It long predates the instrument."
      ],
      "exitCondition": "the generation surface is reachable as bus verbs so the sweep can see it — or the nine UI-controller generators are routed through plan-or-typed-refusal and the finding is struck with the argument. The gate flips hard-0 when its declared level reaches 0. ⚠ Printed every run: arm (b) is a SOURCE check and proves the handler CONTAINS no mutation site, never that dispatching it mutates nothing; the executed half lives in plugins/rooms/__tests__/roomRegenerate.test.ts, which this gate CITES rather than duplicates.",
      "territory": "apps/editor/src/engine/** (the nine UI-controller generators incl. HouseLayoutExecutor) and plugins/rooms/. NOT tools/.",
      "reviewBy": "2026-11-15"
    },
    {
      "gate": "../rac-conformance/certification/gates/check-move-propagation.ts",
      "runner": "certify",
      "addedAt": "2026-08-15",
      "builtBy": "GR-12 · C79 §5.5/§8.3(b) · C78 §6 ('move: the unmeasured lifecycle phase') · C71 §6.2 · §GATE-AUTHORED-BUT-UNWIRED round 3",
      "addedLateAndWhy": "Committed, registered in neither runner, never executed in an automated run until this commit. It is the ONE of the thirteen that answers YES to the §2.1a residency question, so it is registered in certify.ts, not run-all.",
      "firstReading": "exit 1 DECLARED-LEVEL — 1 finding against a declared level of 1, EXECUTED AT HEAD by the registering commit. All 7 floors met: PC1 positive control observed the re-projection (24 m² → 36 m²) so the harness can see a follow at all, N1/N2/N3 negative controls all behaved, 7 arms driven to a verdict, 0 harness throws, 4638 source files walked for the consumer scan, 4 region-derived families enumerated.",
      "pinnedTo": "tools/rac-conformance/certification/gates/move-propagation.json — 1 NAMED row, checked in both directions.",
      "predatesTheGate": [
        "roof·region — the region attribution has nowhere to land: RoofTypes.ts declares NO field that can carry a boundary reference (RoofFootprint.polygon is a bare [number, number][]). RoofRegionTrace.ts computes and RETURNS the five attribution counts, which are then DISCARDED for want of anywhere to put them. The L0 bus schema compounds it — packages/schemas/src/elements/Roof.ts types `boundary` as Vec3[] and Zod strips unknown keys, so a reference cannot even TRANSIT the bus. Roof does not follow, cannot follow, and unlike the finishes has no correct data sitting unread. A NAMED gap with an owner, not a silent one."
      ],
      "exitCondition": "RoofFootprint (and the L0 Roof schema's `boundary`) gain a field that can carry a HostReferenceEdge, so the attribution RoofRegionTrace already computes has somewhere to land and a roof can follow its walls. move-propagation.json reaches 0 and the gate flips hard-0. ⚠ Printed every run: A5–A7 (floor finish · ceiling · roof) are STRUCTURAL, not executed, because there is no listener to drive — driving a move at a family with no subscriber would produce a zero meaning 'I built no harness'. The gate also cannot see rendered correctness, collaboration merge, undo/redo of a move, multi-level moves, or whether a USER is told which edges follow (C79 §10.6).",
      "territory": "packages/geometry-roof/ and packages/schemas/src/elements/Roof.ts. NOT tools/.",
      "runnerNote": "GRADED BY certify.ts, NOT BY run-all. run-all does not execute this gate — it composes real stores and drives a real wall move, which is why it lives in and now runs from the certification tree (§2.1a). run-all reports this entry as INFORMATIONAL and enforces only its `reviewBy`, because a date is runner-agnostic and a reading is not.",
      "reviewBy": "2026-11-15"
    },
    {
      "gate": "check-predicate-canonical.ts",
      "runner": "ga-gate",
      "addedAt": "2026-08-15",
      "builtBy": "C73 Phase C · lane F8 · C73 §3.1/§3.3/§3.5 · BIM30-IMPLEMENTATION-ROADMAP §7 and §2.4 (one family per commit)",
      "firstReading": "exit 1 DECLARED-LEVEL — 88 findings against a declared level of 88 (segment-segment-intersection 11 + polygon-area-and-winding 77). Was exit 0 CLEAN/hard-0 before family 1, on ONE counted family (point-in-polygon, C1/C2/C3 all 0, unchanged and still 0 after both flips). Intermediate reading after family 1 alone: exit 1, 11 vs 11. Floors met: 6704 source files (min 500), 18 test-tree ray casts (min 10), 2 segment/segment bodies in the canonical file (min 1), 1 polygon-area body in the canonical file (min 1), 1/1 executed control group proven. NOTE the source-file count drifts run to run (6701 → 6704 within this session) because other lanes are writing the tree concurrently; it is a FLOOR at min 500, never a ratchet, so the drift cannot manufacture a verdict.",
      "scope": "TWO FAMILIES, landed in the binding order of C73 §3.1 and in ONE COMMIT EACH per roadmap §2.4: segment-segment-intersection (pinned 11), then polygon-area-and-winding (pinned 77). The remaining two §3.1 families are NOT counted and this entry does not cover them. point-to-segment-distance is deferred on the tolerance policy (most sites compare the resulting distance against a PRIVATE epsilon, so pinning it before check-epsilon-policy E2 lands would ratchet a number a tolerance migration is about to move underneath it). polygon-containment-overlap is BLOCKED, not merely deferred, and must be counted LAST: it is almost always BUILT OUT OF point-in-polygon or segment/segment, so counting it now would count the same bodies a second time under a different name and move two ratchets for one fix — which reads as two wins and is one.",
      "pinnedTo": "the `census` map in tools/ga-gate/predicate-canonical-baseline.json — keyed PER FILE with a body count, not per file:line. 88 bodies across 79 concurrently-edited files would otherwise report a body 'struck' and an identical body 'new' on any unrelated reformat, i.e. a false exit 3, which is never absorbable as debt. Shrink-only in BOTH directions and watched as an executed control (4 assertions incl. a line-churn negative): a file that grew (or a file never recorded) is a FINDING; a file that shrank or vanished is STALE and must leave the ledger in the commit that pays it. Raising either pinned reading is FORBIDDEN OUTRIGHT.",
      "predatesTheGate": [
        "segment-segment-intersection · 11 production rivals across 9 files. The canonical (packages/geometry-kernel/src/pure/segmentIntersection.ts, §C73-SEGSEG-CANONICAL) and its one-family proof landed 2026-08-13; nine rivals collapsed onto it then. These 11 are what remained, and none was created by this arm — they were simply never counted, because SEGMENT_CROSS_SHAPE saw only the cross-product spelling and the PARAMETRIC spelling had no detector at all.",
        "polygon-area-and-winding · 77 production rivals across 70 files (74 shoelace cross, 3 trapezoid). The canonical (polygonSignedAreaOrdinates, §C73-AREA-CANONICAL) and the kernel's own five producer clones collapsed 2026-08-13. The register carried '~80' as an unmeasured estimate for months; 77 is the first measured reading.",
        "ONE body did NOT predate the arm and was NOT absorbed: packages/geometry-wall/src/WallJunctionInfill.ts held a private shoelace (`_polygonArea2D`) minted by 762f896b AFTER the census was pinned, taking the gate to exit 3 (78 vs 77). It was fixed AT THE RIVAL — the file already imported @pryzm/geometry-kernel, so consuming polygonSignedAreaOrdinates cost one identifier — not by raising the pin. Recorded here because the honest reading of this entry depends on it: the 77 are genuinely pre-existing, and the one that was not is gone rather than baselined.",
        "4 of the 11 segment/segment rivals are BLOCKED, not merely unfixed: packages/snapping (×2), packages/auto-dimension and packages/finish-host-tracker would each need a NEW @pryzm/geometry-kernel dependency plus a pnpm-lock.yaml sync, and the lockfile is single-owner. Named here rather than silently excluded from the count."
      ],
      "exitCondition": "segment-segment-intersection leaves when its census reads 0 — which requires the lockfile owner to add @pryzm/geometry-kernel to snapping / auto-dimension / finish-host-tracker, and a decision on the three deferred-by-semantics rivals (PlanSnapEngine's ±0.001 snap band and HiddenLineRemoval's interior-only band extend beyond the closed [0,1] the canonical answers; site-parcel-data's EPS-signed variants sit under legally-scoped fixtures). polygon-area-and-winding leaves when its census reads 0; at 77 bodies across 70 files this is many PRs, and C73 §0.4 binds each to ONE family. BOTH families then leave this file and the gate returns to hard-0. NOTE, because it is the exact trap this gate exists to name: a CLEAN verdict on either counted arm would NOT mean 'no duplicates'. The segment/segment arm is blind to a determinant computed by a helper (finish-host-tracker/src/reprojectFinishBoundary.ts:186 is the live instance — seeing it needs the callee NAME, which §3.2 forbids); the area arm's ring-successor condition can only ever undercount; and both, like every counting gate, are blind to whether the surviving body is CORRECT (§5.4a). The families also need an ORACLE FIXTURE at a known answer before §6 is satisfied — segment/segment has one (segmentIntersection.oracle.test.ts), area has polygonAreaWinding.oracle.test.ts, point-in-polygon still does not.",
      "territory": "tools/ga-gate/check-predicate-canonical.ts · packages/geometry-kernel/src/pure/{segmentIntersection,polygonOffset,pointInPolygon}.ts · the 79 files holding counted rival bodies across packages/, apps/, plugins/",
      "reviewBy": "2026-11-15"
    },
    {
      "gate": "../rac-conformance/certification/gates/check-index-can-refuse.ts",
      "runner": "ga-gate",
      "addedAt": "2026-08-16",
      "builtBy": "C78 §20 · U-INV-7 · §5.2 · cleared and registered by lane G2 (roadmap A.12, 'clear the exit-3 gates')",
      "addedLateAndWhy": "⚠ The gate FILE predates this entry and the deterministic-regeneration precedent governs, so the lateness is argued rather than assumed. This gate ran in NO RUNNER from the day it was committed: it was one of the thirteen §GATE-AUTHORED-BUT-UNWIRED files, and it was one of the TWO that round REFUSED to register, because it stood at exit 3 STALE LEDGER and exit 3 is never absorbable (C70 §5.1). A.12 cleared it. Its exit 3 had FOUR causes and only ONE was real: DoorDependencyTracker had acquired a typed refusal path and its ledger row was never struck (paid, unstruck — struck in commit 8177babe). The other three were FALSE POSITIVES OF THE GATE'S OWN ANALYSER and were fixed at the instrument, NOT ledgered: ARM B read ONE FILE per subject, so CeilingHostDependencyTracker and FloorHostDependencyTracker — constructor-only subclasses of a base THIS GATE scores as able to refuse — read as unable; and ARM B policed C78 §8.1's ELEVEN-member closed union against a hand-copied FIVE, so RoofDependencyTracker's ENGINE_NOT_AVAILABLE refusal (§8.1 member #2) was invisible. The union is now PARSED from packages/command-bus/src/consequence.ts. THIS entry is the gate's first reading in the only sense that matters: the first taken by a runner that actually invokes it.",
      "firstReading": "exit 1 DECLARED-LEVEL — 8 findings against a declared level of 8, RE-RUN AT HEAD by the registering commit. 6 of 14 established indexes CAN refuse (DependencyResolver, DoorDependencyTracker, FinishHostDependencyTracker, CeilingHostDependencyTracker, FloorHostDependencyTracker, RoofDependencyTracker). All 7 floors met: 11/11 executed controls (both directions), 11 §8.1 union members parsed (min 8), 4649 production source files (min 1500), 14 dependency indexes established (min 8), 11 index classes discovered from source (min 5), 10/10 declared subjects still present. No exit 2.",
      "pinnedTo": "tools/rac-conformance/certification/gates/index-can-refuse-debt.json — 8 NAMED keys of the form <IndexName>/no-typed-refusal, checked in BOTH directions by the shared contract.ts: an unledgered finding exits 3 EXCEEDED, a ledgered finding no longer measured exits 3 STALE. The list may never grow. It was NOT grown when the three false positives appeared — growing it would have recorded three defects that do not exist and backdated a decision nobody made.",
      "predatesTheGate": [
        "All 8 findings are the estate's standing index shape and long predate the gate: AnnotationDependencyGraph, FloorStore.getByHostSlab, the joinedTo junction index (WallRebuildCoordinator), LightingStore.getAllForRoom, SlabDependencyTracker, SlabWallConnectivityService, ViewDependencyTracker, WindowDependencyTracker. Each answers 'nothing depends on this' and 'I hold no entry for this' with the same value, which C78 §1.4 makes a defect rather than a style note.",
        "The gate was committed and left unregistered, then refused registration at exit 3, so not one of these readings was ever a shipping decision — nothing prevented any of them regressing.",
        "SlabDependencyTracker is the ledger's single most consequential disagreement with its own source document: C78 appendix 0B §3 marks it '✓ can refuse via DegradeSlabSketchCommand.canExecute()'; measured, that branch console.warn()s and returns void. A log is not a refusal and no caller can branch on it."
      ],
      "exitCondition": "the 8 named keys drain to 0 — each index gains a determination shape that answers 'cannot determine' distinctly from 'empty' (C78 §1.4 / §5.2), which is C78 §20's stated exit for U-INV-7, 'the measured 1-of-9 refusing indexes reaches all'. index-can-refuse-debt.json reaches 0 entries and the gate flips hard-0, at which point this entry is struck. It may NOT be struck by narrowing the discovery regex, by removing a declared subject from DECLARED_SUBJECTS, or by widening the refusal vocabulary beyond what C78 §8.1's closed union declares — the union is parsed from source precisely so that widening it requires a contract edit.",
      "territory": "packages/core-app-model/ (ViewDependencyTracker, LightingStore), packages/geometry-slab/ (SlabDependencyTracker, SlabWallConnectivityService, FloorSlabBindingHandler), packages/geometry-window/, plugins/annotations/, apps/editor/src/engine/WallRebuildCoordinator.ts. NOT tools/ — this lane measures and reports; the drain is production work.",
      "reviewBy": "2027-02-15",
      "reviewByNote": "Longer than the 2026-11-15 siblings, argued on the check-relationship-determination precedent rather than assumed. These 8 are not 8 token edits: each needs a determination TYPE, every caller taught to branch on it, and — per the gate's own named UNPROVEN limits — the shape is not enough on its own, since a determination every consumer immediately spreads back into a bare array satisfies this static gate and still violates U-INV-7 in effect. A three-month bound on eight cross-package index rewrites would expire before anyone could honestly act on it, and an expiry nobody can meet teaches date-extension rather than compliance."
    },
    {
      "gate": "check-contract-cited-paths.ts",
      "runner": "ga-gate",
      "addedAt": "2026-08-19",
      "builtBy": "L-960 · lane REG1 · CONTRACT-AMENDMENT-REGISTER.md §0 (\"the cheapest durable fix\") · founder ask: one focused lane on the register",
      "firstReading": "exit 0 AT A BASELINE PINNED TO THIS READING — 101 contract files read; 2960 backticked path citations → 1527 distinct; 1028 RESOLVE on disk; **491 UNRESOLVED** (the pinned baseline); 8 unresolved but EXEMPT (PLANNED or struck). Floors met: 101 contract files (min 90), 1527 distinct citations (min 600), 4/4 planted controls fired (positive resolve, negative phantom, PLANNED exemption, glob rejection). The FIRST run, before the baseline was pinned, exited 3 at 491 vs a placeholder 268 — recorded because the placeholder was a guess and the gate refused it.",
      "pinnedTo": "BASELINE_UNRESOLVED in tools/ga-gate/check-contract-cited-paths.ts, a single shrink-only integer with its full derivation in the adjacent doc comment. Deliberately NOT a JSON ledger of named paths: 491 citations across 100 concurrently-edited contracts would report a path 'struck' and an identical path 'new' on any reflow, i.e. a false exit 3, which is never absorbable.",
      "predatesTheGate": [
        "ALL 491 predate the gate. The register measured two axes of this by hand across ONE range (C21–C50) and reported 97 of 99 cited gate paths absent and 22 of 43 cited packages/* paths absent; this gate covers seven roots across all 100 contracts and finds 491.",
        "⭐ THE REGISTER SIZED THIS AT '119+ defects'. The measured figure is 491 — roughly four times the hand-audited estimate. The estimate was not wrong so much as SCOPED, and recording the gap matters because 'the confident number was low' is the register's own recurring finding (§9: 15 → 22 → 24).",
        "The single largest holder is `scripts/ci-check-spans.ts` at 24 citation sites across C01/C10/C31/C33–C49 — a file CLAUDE.md already records as never having existed (L-812). Register §9 is the row that owns it.",
        "The next largest are whole packages ordered built that shipped elsewhere or not at all: server/parcelZoningProxy.js (12 sites, C57), packages/schedule-4d/ (8, C37), packages/schemas/src/standards/ (6, C34), packages/schemas/src/pii-registry.ts (5, C22 — already a register §11 row).",
        "A distinct sub-family, NOT separated by this gate and named so it is not mistaken for phantom work: `src/ui/`, `src/engine/**` (C06, C11). These paths WERE real and moved to apps/editor/src/**. The citation is stale, not fictional — a different amendment from 'this was never built'."
      ],
      "exitCondition": "491 → 0, at which point BASELINE_UNRESOLVED is deleted and the gate flips hard-0. ⛔ It may NOT be discharged by bulk-adding `PLANNED` to silence findings — that is laundering, and arm B prints the EXEMPT census on every run precisely so it is visible as a number that moves. Nor by widening the exemption vocabulary: `NOT BUILT` / `UNBUILT` / `does not exist` are deliberately NOT exemptions, because register §11C measured eleven gates described as unbuilt that all exist, and §13 measured 97 of 99 cited gates that do not — the prose claim is exactly what cannot be trusted, so it cannot be the escape hatch.",
      "notMeasured": "⚠ This gate checks that a cited PATH resolves. It does NOT check that a cited `file:line` still holds what the contract says it holds — the trailing `:1025-1031` is stripped before resolution. Register §2 and §11C both record rotted line anchors (C96 §3.1's own evidence went stale in ONE DAY). That is a different gate and this one does not claim it.",
      "territory": "tools/ga-gate/check-contract-cited-paths.ts. The DRAIN is docs/02-decisions/contracts/**, which is owned by the contract lanes — this gate measures and reports.",
      "reviewBy": "2026-11-19"
    },
    {
      "gate": "check-contract-index-equivalence.ts",
      "runner": "ga-gate",
      "addedAt": "2026-08-19",
      "builtBy": "lane REG1 · the exit condition stated verbatim in docs/02-decisions/contracts/README.md's own banner and in CLAUDE.md §Governance, both of which record that it 'does not exist yet'",
      "firstReading": "exit 0 AT A BASELINE PINNED TO THIS READING — 100 contract FILES on disk; 83 index ROWS (max id C100); 1 declared RESERVED id (C61); **arm A FILE-WITHOUT-ROW = 18** (the pinned baseline); arm B ROW-WITHOUT-FILE = 0 (hard); arm C ROW/LINK-MISMATCH = 0 (hard); arm D GAP-NOT-DECLARED-RESERVED = 0 (hard). Floors met: 100 files (min 90), 83 rows (min 60), planted controls fired.",
      "pinnedTo": "BASELINE_FILE_WITHOUT_ROW in tools/ga-gate/check-contract-index-equivalence.ts. Arms B, C and D carry NO baseline and are hard-0 — each measured 0 at first reading and each is a one-commit fix, so a baseline would only license a defect nobody has.",
      "predatesTheGate": [
        "All 18 predate the gate: C81, C84, the entire C85–C99 per-element block, and C100 exist as files with no row in the index table.",
        "⭐ This is not bookkeeping. CLAUDE.md defers the CONFLICT-RESOLUTION ORDERING to this table, so all 18 are currently ordered by nothing — including **C84 (Element Integrity)**, binding on every PR touching an element family, which an agent reading the ordering literally ranks BELOW an ADR. That is register §1, the highest-blast-radius row in the suite.",
        "The staleness this gate replaces has recurred FIVE times (C67 → C81 → C84/C85–C99 → C100 → C76), the fifth within a day of the fourth being corrected. Every recurrence was a range or count written as a LITERAL. This gate compares SETS and never a number — a count can be right while the set is wrong, and the README has been exactly that more than once."
      ],
      "exitCondition": "18 → 0: every landed contract acquires an index row, in the same commit that lands it. The baseline is then deleted and arm A joins B/C/D at hard-0. ⛔ It may NOT be discharged by deleting rows, by relaxing ROW_RE to count prose mentions as rows (register §9 records that a sweep keyed on a NAME is defeated by using a different name), or by widening SELF_ROW_IDS beyond the index's own C00 self-row.",
      "notMeasured": "⚠ This gate asserts that a row EXISTS for every file and a file for every row. It does NOT assert that the row's DESCRIPTION is true, that its Status column is current, or that the prose ranges in rows 4–6 agree with the table — README:219's undated present-tense '69 contract files exist (C01–C60 + C62–C69 + C24.1)' is FALSE at HEAD and this gate cannot see it (register §1, L-1131). A prose-range arm is the obvious next extension and is deliberately not claimed here.",
      "territory": "tools/ga-gate/check-contract-index-equivalence.ts. The DRAIN is docs/02-decisions/contracts/README.md's table, owned by C00 — unclaimed by any live lane as of 2026-08-19.",
      "reviewBy": "2026-11-19"
    },
    {
      "gate": "check-dependent-adapts-on-host-move.ts",
      "runner": "ga-gate",
      "addedAt": "2026-08-21",
      "builtBy": "L-2090..L-2093 - lane PROP1 - ADR-0344 - founder ask 2026-08-21: “elements should propagate when one moves, all contexts; audit all elements against this principle”",
      "exitCondition": "the 39 SILENT cells in host-move-propagation-matrix.json `declaredSilent` reach 0 — each becomes a PROPAGATES cell with resolving wiring evidence (ARM A) or a REFUSES cell with a real refusal string (ARM B) — and the gate flips hard-0 on SILENT. ⛔ The list is NAMED and shrink-only, never a bare integer, precisely because 64 cells are edited concurrently and a count cannot tell 'furniture × wall was fixed' from 'furniture × wall was replaced by a new silent cell'. ⚠ The entry is STRUCK, not re-baselined, if the matrix is regenerated against a different cell set: a ledger pinned to a reading cannot survive its subject changing shape underneath it.",
      "firstReading": "exit 1 DECLARED-LEVEL at a ledger pinned to THIS reading - 64 matrix cells, 19 PROPAGATES / 6 REFUSES / 39 SILENT; 259 production files read while resolving evidence. ARM A 19/19 (every PROPAGATES cell's wiring evidence still resolves), ARM B 6/6 (every REFUSES string still exists), ARM C 39 SILENT cells, each named. All four floors met.",
      "pinnedTo": "tools/rac-conformance/certification/gates/host-move-propagation-matrix.json - `declaredSilent`, a NAMED shrink-only list, never a bare integer: the subject is 64 concurrently-edited cells and a count alone could not tell 'furniture x wall was fixed' from 'beam x slab regressed'.",
      "negativeControl": "EXECUTED before the ledger was trusted (C74 6.2). The 'floor finish x wall' cell's marker `new FloorHostDependencyTracker(` was temporarily replaced with a symbol that does not exist; the gate went RC=3 'REGRESSED-TO-SILENT: floor finish x wall', 40 findings vs 39. Restored, RC=1 at 39. ARM A discriminates - it is not reading its own ledger back to itself.",
      "predatesTheGate": [
        "ALL 39 SILENT cells predate the gate. Two cells - 'floor finish x wall' and 'ceiling finish x wall' - were SILENT for every finish whose relationship was never recorded at creation, and were CLOSED by this same lane (L-2090); they are enrolled as PROPAGATES with the repair itself as evidence, so deleting the repair turns the gate red rather than turning the product quiet again.",
        "The largest holders by user-visible impact: furniture (no host field at all - a wardrobe cannot record the wall it stands against), plumbing (same), curtain wall (no host-wall field), and handrail x stair (the cascade rule is fully AUTHORED and registered by nothing).",
        "THE MOST DANGEROUS SHAPE is not 'no subscriber' but 'a handler that early-returns with no log': lighting, plumbing, ceiling, floor and standalone-opening ids are all DELIVERED to the level-rebuild callback and dropped at `const wall = store.getById(id); if (wall) {` with no else and no line. Those read as handled."
      ],
      "reviewBy": "2026-11-21"
    },
    {
      "gate": "check-property-rac-matrix.ts",
      "runner": "ga-gate",
      "addedAt": "2026-08-21",
      "builtBy": "L-2200..L-2213 - lane RAC3 - ADR-0345 - founder ask 2026-08-21: “deep review of ALL element properties against RAC; all dims and properties of all elements should be queryable and executable; audit, document, plan, fix”",
      "exitCondition": "`declaredSilent` (75) and `declaredExecuteOnly` (1) in property-rac-matrix.json both reach 0 — every panel-visible (family × property) cell is BOTH queryable and executable — while ARM B's hard-0 on QUERY-ONLY is held throughout. ⛔ ARM B may never be relaxed to buy progress on ARM D: a property the user can ASK about but cannot SET is the asymmetry this gate exists to forbid, and closing SILENT cells by making them query-only would satisfy the count while inverting the point. ⚠ The entry is STRUCK, not re-baselined, if the panel set changes: the 112 cells are pinned to 4 named panel files, and a fifth panel makes this reading a statement about a corpus that no longer exists.",
      "firstReading": "exit 1 DECLARED-LEVEL at a ledger pinned to THIS reading - 112 panel-visible (family x property) cells across 15 families: 36 BOTH / 1 EXECUTE-ONLY / 0 QUERY-ONLY / 75 SILENT. ARM A 17/17 (every query row mirrors its write twin), ARM B 0 QUERY-ONLY (hard-0), ARM C 0/0 refusals, ARM D 75 SILENT + 1 EXECUTE-ONLY, each NAMED. All five floors met (4 panel files, 112 cells, 63 capabilities executed, 40 settable, 46 askable).",
      "pinnedTo": "tools/ga-gate/property-rac-matrix.json - `declaredSilent` and `declaredExecuteOnly`, NAMED shrink-only lists, never bare integers: the subject is 112 concurrently-edited cells and a count could not tell 'door.handleHeight was wired' from 'wall.height regressed'.",
      "negativeControl": "EXECUTED before the ledger was trusted (C74 6.2), THREE ways. (1) ARM A: a query row's `capabilityId` was repointed to a capability that does not exist; the gate went RC=3 'ARM A - 1 query row(s) no longer mirror their EXECUTE capability'. (2) ARM D forward: `door.handleHeight` was removed from declaredSilent; RC=3 '1 NEW SILENT cell'. (3) ARM D stale: a cell that never existed was added; RC=3 '1 SILENT cell(s) on the ledger are PAID and still listed'. Restored after each; RC=1 at 75/1. The gate discriminates in both directions and is not reading its own ledger back to itself.",
      "predatesTheGate": [
        "ALL 75 SILENT cells predate the gate. The largest holders are the two families the founder asks about most: WINDOW 15 of 18 and DOOR 14 of 17 - and both were INVISIBLE to the pre-existing check-chat-capability-coverage check 9, whose own header names the reason ('WindowSection / DoorSection own width/height/sillHeight/type/colour ... so window and door look far emptier here than they are'). The shared denominator now reads those dedicated sections; 33 of the 112 cells come from them.",
        "THE MOST COMMON SILENT PROPERTY IS `mark` - eleven families carry an editable Mark/Name row in the Properties panel and NOTHING in chat sets or reports it. A user can rename a room by sentence and cannot mark a wall.",
        "stair-railing is 0/6 - not one of its six editable properties is reachable either way, despite `set-stair-railing-type` existing for its TYPE.",
        "TWO defects were found by this gate IN THE FEATURE IT SHIPPED WITH, which is the strongest evidence it is not decorative: (a) the `roof-pitch` query row read `pitch` in radians from the L0 Zod schema, while the write lands `slope` as a GRADIENT on the geometry RoofData - the gate reported `roof.slope` as EXECUTE-ONLY, which is exactly what a read pointed at the wrong field looks like from outside; (b) `wall.rakeAngleDeg` was EXECUTE-ONLY, i.e. settable by sentence and not askable. Both fixed in the same lane."
      ],
      "reviewBy": "2026-11-21"
    },
    {
      "gate": "check-claim-carries-evidence.ts",
      "runner": "ga-gate",
      "addedAt": "2026-09-01",
      "builtBy": "lane E8-GATES - spec 20 (AI INTERPRETS, DETERMINISTIC COMPUTES) - E4-EXECUTION-CONTROL controls 3/8/9 - founder brief 2026-09-01: \"the gate battery that makes extraction safe to ship\". Built on lane E8-SCOUT’s measured finding F-3: ZERO of 76 GA gates name, import or assert anything about packages/ordinance-extraction.",
      "exitCondition": "The 48 NAMED rows in claim-evidence-ledger.json reach 0 - each closed the ONLY two honest ways: (1) curate the missing rase.requirement verbatim span from the held source, or (2) downgrade the claim off tier 4 to the derivation that is actually true - and the gate flips hard-0 on arm A. ⛔ NOT by deleting rows, NOT by relaxing the auditor, and NEVER by fabricating a quote: a fabricated span is strictly worse than a named gap, because a named gap is reviewable and a fabricated one is not. ⚠ The entry is STRUCK, not re-baselined, if esBarcelona20aAillada.decl.json is regenerated against a different rule set: a ledger pinned to a reading cannot survive its subject changing shape underneath it. ⚠ The ledger is a NAMED row set, never a bare integer, because the subject is 66 concurrently-editable rules and a count could not tell \"20a/6 maxHeight got its span\" from \"20a/9b lost one\".",
      "firstReading": "exit 1 DECLARED-LEVEL at a ledger pinned to THIS reading - 48 rows, every one arm A. ARM A: 1 declarative document, 66 rules, 57 of them tier-4 / AI_EXTRACTED, only 9 carrying a verbatim span and 0 carrying a page (the article locator is present on all 66, which is why the gate requires article OR page, not both). ARM B: 88 registered pack zones walked, 43 carrying at least one pipeline-extracted field, ALL 43 naming an ordinanceRef - clean. ARM C: 5,405 files scanned repo-wide, 0 value-position AI_EXTRACTED producer sites - the honest current reading, the seat is built and empty. ARM D: the extraction core DRIVEN over real German ordinance text, 4 cited rules emitted, every one carrying citation.document + citation.sentence at pipeline-extracted-unverified / pipeline-extracted - clean. All four honesty floors met; 9 controls executed in memory with their outcomes printed.",
      "pinnedTo": "tools/ga-gate/claim-evidence-ledger.json - `rows`, a NAMED shrink-only set compared in BOTH directions. A NEW row exits 3 (regression); a row with NO finding also exits 3 (the debt was paid and the line was left standing, which is how a baseline drifts into a list of things that are actually fine). Neither direction is absorbable by any ledger.",
      "negativeControl": "EXECUTED before the ledger was trusted (C74 6.2), THREE ways. (1) FORWARD, on disk: a planted tools/e8-planted-control/aiproducer.ts holding an evidence-free `derivation: \"AI_EXTRACTED\"` object literal - the gate went RC=3, \"1 new\", naming the file. Removed; tree verified clean. (2) STALE: a row that no finding produces was appended to the ledger - RC=3, \"0 new / 1 no longer found\", naming the row for striking. Restored; RC=1 at 48. (3) IN MEMORY, every run: 4 planted violations must FIRE and 5 calibrations must stay CLEAN, each outcome printed. The gate discriminates in both directions and is not reading its own ledger back to itself.",
      "predatesTheGate": [
        "ALL 48 rows predate the gate. They are the E1bc Barcelona 20a migration pilot, whose own header already names the gap (\"no-verbatim-span-curated\") and explicitly refuses to fabricate a quote - which is the RIGHT call, and is why these are AUTHORING debt rather than dishonesty. They are measured rather than excused because spec 20 says a tier-4 claim a human cannot review is not a claim.",
        "NOT YET LIVE, WHICH IS WHY IT IS CHEAP TO FIX NOW. esBarcelona20aAillada.decl.ts is exported from the site-parcel-data barrel but registered in NO rule-pack registry: no parcel resolves through it, and only declarativeGoldenParity.test.ts drives it. It goes live as-is the day the hand-written TS pack is retired.",
        "THE GATE CAUGHT ITSELF ON ITS FIRST LIVE READING, and that is recorded here rather than quietly fixed. Arm C flagged packages/ordinance-extraction/src/spine/types.ts, where a sibling lane writes `readonly derivation: 'AI_EXTRACTED';` to narrow RuleProvenance so a tier-1/2/3/5 claim does not compile - i.e. the tier lock expressed in the TYPE SYSTEM, the exact opposite of the defect. The SCANNER was fixed (two sound one-way discriminators: a `readonly` prefix is legal only in a type member, and a `;` terminator is illegal in an object literal) and a permanent calibration control was added. The LEDGER WAS NOT EDITED to swallow it: the first false positive is how a gate gets muted."
      ],
      "reviewBy": "2026-12-01"
    },
    {
      "gate": "check-unvalidated-claim-not-a-constraint.ts",
      "runner": "ga-gate",
      "addedAt": "2026-09-01",
      "builtBy": "lane E8-GATES - spec 20 - C58 1.2/1.6 - PACK-CONFIDENCE-CEILING (L-665) - E4-EXECUTION-CONTROL controls 3/5/8/9/10. The founder brief names the failure exactly: \"a pipeline that lets an AI-extracted number reach a buildable-envelope calculation unlabelled is the single failure this wave exists to prevent.\"",
      "exitCondition": "The 20 NAMED rows in unvalidated-claim-ledger.json reach 0 by making the LABELS agree with the CLAIMS in the declarative pilot - either stamp the migrated rules with the derivation that is actually true, or carry the machine tier through to the pack meta defaultConfidence and the zone fieldProvenance flags. BOTH are honest; the current pair is not, because the two axes contradict each other. ⛔ The gate does NOT decide which side is wrong and must not: that is a curation decision. ⛔ NOT by deleting rows and NOT by relaxing the auditor; and the invariant arms (A/B/C, currently GREEN) may NEVER acquire a ceiling - raising one there would license publishing an unchecked machine read as a human determination. ⚠ The entry is STRUCK, not re-baselined, if the pilot document is regenerated against a different zone set.",
      "firstReading": "exit 1 DECLARED-LEVEL at a ledger pinned to THIS reading - 20 rows: 11 arm D + 9 arm E. ARMS A/B/C GREEN and hard-0: 6 pack-bearing jurisdictions, 180 zone-solves (137 ok, 43 refused and counted), 67 SOLVED machine-extracted zone-solves - every one publishing at exactly pipeline-extracted-unverified, none classified complete, all carrying the C58 1.6 MACHINE-EXTRACTED caveat. ARMS D/E RED: the E1bc loader turns 57 rules stamped AI_EXTRACTED / tier 4 into a C58 contract labelling all 10 zones ordinance-pdf (\"a HUMAN transcribed this\", C58 1.6) at defaultConfidence estimated-ruleset, and arm E then solves that derived contract through the REAL computeBuildableEnvelope and reads estimated-ruleset back off NINE buildable envelopes. All five honesty floors met; 4 controls executed in memory with their outcomes printed.",
      "pinnedTo": "tools/ga-gate/unvalidated-claim-ledger.json - `rows`, a NAMED shrink-only set compared in BOTH directions; either difference exits 3 and is never absorbable.",
      "negativeControl": "EXECUTED, and it runs INSIDE every invocation rather than once at authoring time (4 controls, each outcome PRINTED so \"fired\" and \"never reached\" cannot print the same). CONTROL 1: a REAL registered estimated-ruleset pack (es-08019-barcelona / 20a/6) copied IN MEMORY with ONE zone field flipped to pipeline-extracted and solved through the REAL engine - arm A FIRED, \"A|PLANTED/20a/6/classified|confidence-estimated-ruleset\". CONTROL 2: the same pack UNMODIFIED read 0 (L-716 satisfiability). CONTROL 3: a planted tier-4 zone labelled HONESTLY (fields pipeline-extracted, pack default pipeline-extracted-unverified) driven through the REAL deriveC58Contract read 0 - so the gate does not punish correct labelling, which would teach people to relabel. CONTROL 4: the same zone labelled ordinance-pdf FIRED 2 arm-D findings. ⛔ The rulepacks/ tree is E4-owned and was NOT touched; no planted fixture was written anywhere.",
      "predatesTheGate": [
        "ALL 20 rows predate the gate. Nobody chose to ship them: the seam had no instrument. The E1bc lane documented its migration honestly and the golden-parity test byte-compares the derived contract against the HUMAN-authored TS pack - which is exactly why the pack-level labels match the human pack while the rule-level derivation says AI_EXTRACTED. The contradiction is structural, not careless.",
        "NOT YET LIVE. The document is registered in no pack registry, so no parcel resolves through it today and nothing user-facing is affected. It goes live as-is the day the hand-written TS pack retires - which is precisely why the row is pinned before then rather than after.",
        "D-1, A LATENT ENGINE GAP FOUND BY ARM A AND RECORDED, NOT FIXED (control 10 - scope). ZoningRulesEngine clamps envelope confidence via capEnvelopeConfidenceToPackDefault, which reads rulePack.defaultConfidence - the PACK level - and NOTHING reads the per-field fieldProvenance flag. The live corpus is clean only because the two machine-extracted packs (Madrid, Cordoba) are WHOLLY machine-extracted and declare the tier at pack level. A single pipeline-extracted field inside an estimated-ruleset pack would ship at estimated-ruleset - measured directly by CONTROL 1 above. Arm A is what notices; fixing the engine is a separate, sanctioned change."
      ],
      "reviewBy": "2026-12-01"
    }
  ]
}
